---
phase: 25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based
verified: 2026-09-15T14:30:00Z
status: passed
score: 6/6 requirements verified
behavior_unverified: 0
overrides_applied: 0
human_verification: []
gaps: []
---

# Phase 25.1 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-15)

Goal-backward check of the inserted phase goal: *the dismissible card is for decisions and big updates only, everything else is a lingering narrative toast; the Oracle fills the screen and opens at the newest line; a full party swaps its Joiner with a snarky exit; party members fight by class; Make Camp refuses with the numbers and counts party appetites like the automatic new day.*

## Automated evidence (re-run by the orchestrator after 25.1-03)

- `npm test`: **1397/1397** (1330 at phase start → +27 wave 1, +20 wave 2, +20 wave 3). Parity **33/33**, `git status test/parity` clean, `test/parity/prototype-master.js.txt` byte-identical. `npm run build:www` succeeds.
- Files changed across the phase (9 in engine/content/src/shell, +708/−62): `engine/state.js` (`swapPartyMember`), `engine/encounters.js` (swap ordering), `engine/movement.js` (`nightlyEats`, `campFailed {need, have}`, member `spellsUsed` reset), `engine/derived.js` (`memberToHit`, `bestAttackSpell`), `engine/combat.js` (class-based `alliesTurn`, self-contained `allyCast`), `content/flavor.js` (`JOINER_EXIT_LINES`), `src/browser/toasts.js`, `src/browser/eventNarration.js`, `mazeworld.html`. `engine/magic.js` untouched (hero `castSpell` byte-identical); no `combat.js → magic.js` import.
- Three plans, each with a SUMMARY.md and no `Self-Check: FAILED`; every task committed atomically; working tree clean.
- Engine gate: zero rng in the Joiner swap and the camp gate (rngState asserted unchanged); every ally-combat draw sits inside the pre-existing `C.allies` branch of `alliesTurn` (no parity fixture has a party); no new persistent serialized field (Thief `backstabUsed` is transient combat state on the `C.allies` entry, never synced out by `endCombat`); `campFailed` for a solo hero deep-equals `{type, reason, need, have}`; new event types `joinerLeft`, `allyCast`, `allySpellHit`, `allySpellMissed` have `EVENT_NARRATION` + `TOAST_FOR`/`ORACLE_ONLY` entries (coverage guards green); voice safety scan green.
- Bot smoke: `node tools/tune-difficulty.mjs --seeds=12 --party` exit 0 with class-based allies casting and backstabbing (no throws).

## Success criteria

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Card only for decisions/big updates; minor events toast-only with the narrative sentence | Verified | `CARD_EVENTS = {floorChanged, leveled}` gates the html→beats fallback at both sites (engineMove, mzMakeCamp) — asserted by `shell-toast-wiring`; preDeath/ambush/Joiner/find branches untouched; `narrativeToastText` + `ctx.narrate` for `NARRATIVE_ACTIONS = {move, camp, resolveJoiner}` (`narrativeToasts.test.js`, 12 tests: tags, entities, dice spans, empties, ordering); teleport rides the move path; `tableFour`/`tableFourNoop` promoted to real toasts. |
| 2 | Toasts linger, stack-aware, tap-to-dismiss, reduced-motion, no CLS | Verified | `toastLifetime(len, visible) = min(9000, 3000 + 60·len) + 1200·visible`, integer ms, max 12 600 at a full stack (boundary tests at len 0/1/99/100/101, visible 0/3); `mzToast` measures visible-before-append, clears the timer on tap; host stays fixed with cap 4 and `pointer-events:none` (only `.mw-toast` is clickable). |
| 3 | Oracle fills the screen, opens at newest, "newer" indicator | Verified | `#screen-oracle`/`#log-panel`/`.log` fill chain; `window.__mzOracleToNewest` called from `showTab("oracle")` (scrollTop = 0, newest is prepended); `#log-newer` pill toggled at a 24 px threshold, tap returns to top — `shell-oracle-panel.test.js` (8). |
| 4 | Joiner swap with snark; offer names who walks; decline keeps roster; zero rng | Verified | `swapPartyMember` splices index 0 then adds; `joinerLeft {name, sub, replacedBy}` precedes `joinerJoined`; five family-friendly exit lines picked by `(name.length + replacedBy.length) % 5` (no draw); offer head built with `textContent` and `window.__mzPartyCap`; party key-set unchanged — `joiner-acquisition`/`party-model` tests, voice scan. |
| 5 | Members fight by class; narrated + toasted; solo parity byte-identical | Verified | Fighter: `memberToHit` + `weaponDamage(view)` with crit on 1; Thief: one backstab per combat then normal; Magic User: `bestAttackSpell` with own `spellsUsed`, `allyCast` mirroring the hero's dice shapes (Freeze → `killFoe`), staff swing when out of charges; sheet-less entries take the verbatim legacy strike; `party-combat.test.js` 39 tests; parity 33/33. |
| 6 | Camp refuses with the numbers, counts party appetites like newDay; auto day unchanged | Verified | `nightlyEats(state)` shared by `newDay` and `makeCamp`; refusal is the first branch (no draw, no mutation); "You eat 2 a night (Bram eats 1 more). You have 1. Find rations first." in Oracle and toast; camp button shows a dimmed `data-short` state with the numbers via `window.__mzNightlyEats` (deliberately not `disabled`, so the refusal toast can still fire); `movement.test.js` Troll/Human/party cases; newDay arithmetic pinned. |

## Requirements

| ID | Status | Evidence |
|---|---|---|
| DFB-01 | Verified | Criterion 1. |
| DFB-02 | Verified | Criterion 2. |
| DFB-03 | Verified | Criterion 3. |
| DFB-04 | Verified | Criterion 4. |
| DFB-05 | Verified | Criterion 5. |
| DFB-06 | Verified | Criterion 6. |

## Device checklist (non-blocking — items attached in the three SUMMARY.md files)

Pixel 7 debug build pushed after this verification: trap → toast only, no card; teleport → toast, map recenters; new floor and level-up → card; four toasts readable one by one, tap removes one; Oracle opens at the newest line, pill appears when scrolled, tap returns; accept a second Joiner → exit line toast, roster of one; Magic User Joiner casts ("Ysolde casts Freeze — Goblin frozen solid"), Thief Joiner backstabs, Fighter Joiner's weapon named in the Oracle; Make Camp with a Troll and 1 ration → refusal with "You eat 2 a night. You have 1."; long narrative toasts wrap to 3–4 uppercase mono lines — readability to be judged on device.

## Findings carried forward

- The `state.*` planning tools do not resolve decimal phase numbers (`25.1`) — STATE.md was patched by hand this phase; worth a note in the GSD tooling backlog, not the game.
- Two executors' final `docs(...)` commits were made through the SDK commit helper and lack the session attribution footer; all feature commits carry it.
- Toast CSS is uppercase mono 14 px; narrative sentences wrap to several lines. If the user finds them heavy on device, a lowercase/sentence-case variant for narrative toasts is a one-rule CSS change (v1.3 candidate).

_Verified: 2026-09-15 — orchestrator (Claude), no verifier agent._
