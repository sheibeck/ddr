---
phase: 25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based
plan: 02
type: execute
wave: 2
depends_on: ["25.1-01"]
files_modified:
  - engine/state.js
  - engine/encounters.js
  - engine/movement.js
  - content/flavor.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - mazeworld.html
  - test/unit/joiner-acquisition.test.js
  - test/unit/party-model.test.js
  - test/unit/movement.test.js
  - test/voice/safety-scan.test.js
  - test/unit/shell-party-camp.test.js
autonomous: true
requirements: [DFB-04, DFB-06]
tags: [engine, party, joiner-swap, camp, rations, narration, toasts, zero-rng, parity, dfb-04, dfb-06]

must_haves:
  truths:
    - "DFB-04 empty: accepting a Joiner with an EMPTY party (undefined, [] or under PARTY_CAP) adds the member with NO joinerLeft event (the existing path, unchanged); with a FULL party (length >= PARTY_CAP) the member at index 0 is spliced out FIRST, `joinerLeft { name, sub, replacedBy }` is emitted, then `joinerJoined` — joinerLeft precedes joinerJoined in the events array; the party length is exactly 1 afterwards and the party array shape is unchanged (no new field on any member). Declining with a full party leaves the roster untouched and emits only joinerDeclined. Zero rng: resolveJoiner still takes no rng argument and rngState never shifts. Tests: empty -> 1 member, no joinerLeft; full -> swap with ordering; decline unchanged."
    - "DFB-04 encoding: the joinerLeft Oracle line interpolates the leaving member's name and the newcomer's name through a local HTML-escaping helper (& < > escaped), so a name containing '<' renders as text; the snark line is chosen deterministically WITHOUT rng from content/flavor.js#JOINER_EXIT_LINES by index (name.length + replacedBy.length) % JOINER_EXIT_LINES.length; the five lines are family-friendly sarcasm under the voice scan; the shell's offer head ('Take Ysolde along? Bram walks.') is set via textContent."
    - "DFB-06 adjacency: nightlyEats(state) = the hero's RACES[race].eats||1 plus every party member's RACES[m.race]?.eats||1; makeCamp refuses iff c.rations < need (strict) — rations === need camps and eats them all; campFailed carries { reason: 'noRations', need, have } plus, only when a party exists, `members: [{ name, eats }]` via conditional spread; for a solo hero need === RACES[race].eats||1 exactly as before, so every fixture's camp path is byte-identical. Tests: Troll (eats 2) with 1 -> refuse need 2 have 1; Troll with 2 -> camps (rations 0 after); Human + Human member with 1 -> refuse need 2 have 1; solo Human with 1 -> camps."
    - "DFB-06 empty: no party (undefined or []) -> nightlyEats equals the hero's eats; 0 rations -> refuse with need N have 0 and the narration 'You have 0.'; a member whose race has no eats counts as 1 (same default as newDay); the shell's camp button state is computed by the SAME exported helper (window.__mzNightlyEats = nightlyEats) so the button and the engine never disagree; narration and toast both render the numbers ('You eat 2 a night (Bram eats 1 more). You have 1. Find rations first.')."
    - "DFB-06 ordering: makeCamp checks need BEFORE any rng draw or state mutation (the refusal is the first branch after the combat/store/dead/won guard), so a refused camp draws nothing (fakeRng([]) never throws) and changes nothing (day, rations, wp unchanged); newDay is untouched in order (eat, then heal-or-starve, then the 8 wandering checks); the helper is defined once in movement.js and used by both newDay and makeCamp; a test asserts newDay's rations delta equals nightlyEats(state) for a solo hero and for a hero + member."
    - "Every new event type is covered: joinerLeft has an EVENT_NARRATION entry, a TOAST_FOR builder and a FEATURE_EVENTS entry; campFailed's narration and toast render need/have; the Phase 25 coverage guards (test/unit/formatEventsCoverage.test.js, test/unit/toastsCoverage.test.js) and the voice scan stay green; parity 33/33 with zero fixture changes."
  artifacts:
    - path: "engine/state.js"
      provides: "swapPartyMember(state, member) -> { added: true, left: member|null } — zero rng; splices index 0 when full, plain push otherwise; addPartyMember unchanged"
      exports: ["swapPartyMember"]
    - path: "engine/encounters.js"
      provides: "resolveJoiner accept path: swapPartyMember, joinerLeft BEFORE joinerJoined"
    - path: "engine/movement.js"
      provides: "nightlyEats(state) used by newDay and makeCamp; campFailed { reason, need, have, members? }"
      exports: ["nightlyEats"]
    - path: "content/flavor.js"
      provides: "JOINER_EXIT_LINES — five {name}/{new} templates, family-friendly sarcasm"
      exports: ["JOINER_EXIT_LINES"]
    - path: "src/browser/eventNarration.js"
      provides: "joinerLeft entry (escaped names, deterministic line pick), campFailed with numbers"
    - path: "src/browser/toasts.js"
      provides: "joinerLeft builder (beat, feature priority), campFailed builder rendering need/have, FEATURE_EVENTS + joinerLeft"
    - path: "mazeworld.html"
      provides: "offer head 'Take X along? Y walks.' via textContent when the roster is full (window.__mzPartyCap); camp button data-short + aria-label from window.__mzNightlyEats in paint(); module bridges for PARTY_CAP and nightlyEats"
    - path: "test/unit/shell-party-camp.test.js"
      provides: "source assertions for the two bridges, the offer head, and the paint() camp-button state"
      min_lines: 30
  key_links:
    - from: "engine/encounters.js#resolveJoiner"
      to: "engine/state.js#swapPartyMember"
      via: "import { addPartyMember, swapPartyMember } from \"./state.js\"; accept path calls swapPartyMember and pushes joinerLeft when `left` is non-null, then joinerJoined"
      pattern: "swapPartyMember\\(state, pending\\)"
    - from: "engine/movement.js#makeCamp and #newDay"
      to: "engine/movement.js#nightlyEats"
      via: "both read the ONE helper; newDay's `eats` comes from nightlyEats(state) while its member loop keeps summing upkeep into cost"
      pattern: "nightlyEats\\(state\\)"
    - from: "src/browser/eventNarration.js#joinerLeft"
      to: "content/flavor.js#JOINER_EXIT_LINES"
      via: "import { JOINER_EXIT_LINES } from \"../../content/flavor.js\"; index (name.length + replacedBy.length) % length"
      pattern: "JOINER_EXIT_LINES"
    - from: "mazeworld.html paint()"
      to: "engine/movement.js#nightlyEats"
      via: "window.__mzNightlyEats bridge assigned in the module script from the import"
      pattern: "window\\.__mzNightlyEats"
  prohibitions:
    - "No rng draw added anywhere: resolveJoiner/swapPartyMember take no rng; makeCamp's refusal happens before newDay is entered; newDay's draw order is untouched"
    - "No new serialized field: the party array and its member sheets keep their shape; campFailed's new fields are event payload, not state"
    - "PARTY_CAP stays 1; addPartyMember's contract (returns false when full, no mutation) is unchanged and its party-model test still passes"
    - "test/parity/prototype-master.js.txt and every parity fixture are never edited; parity 33/33 byte-identical"
    - "engine/combat.js and engine/derived.js are not touched in this plan (they belong to 25.1-03)"
---

<objective>
Land the two zero-rng engine rules from the device batch with their narration and toasts: (DFB-04) accepting a Joiner while the roster is full SWAPS the existing member out — `joinerLeft { name, sub, replacedBy }` fires before `joinerJoined`, the Oracle/toast carry a family-friendly snark exit line picked deterministically from `content/flavor.js#JOINER_EXIT_LINES`, the offer card names who would walk ("Take Ysolde along? Bram walks."), declining keeps the roster; (DFB-06) one exported `nightlyEats(state)` helper drives both `newDay` and `makeCamp`, the camp refusal carries `{ need, have }` and reads "You eat 2 a night (Bram eats 1 more). You have 1. Find rations first." in the Oracle and the toast, and the shell's camp button derives its short-on-food state from the same helper so the button and the engine never disagree.

Purpose: 25.1-CONTEXT.md "Joiner swap" and "Camp refusal says why" (user decisions 2026-09-15). Solo fixtures stay byte-identical: the swap never fires without a party and the camp gate only WIDENS for characters with a party.
Output: engine/state.js, engine/encounters.js, engine/movement.js, content/flavor.js, the two presentation tables, mazeworld.html, updated tests, three commits, `npm test` green, parity 33/33, `npm run build:www` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-CONTEXT.md
@.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-01-SUMMARY.md
@engine/state.js
@engine/encounters.js
@engine/movement.js
@test/unit/joiner-acquisition.test.js
@test/unit/movement.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Joiner swap — swapPartyMember, resolveJoiner ordering, JOINER_EXIT_LINES, joinerLeft narration + toast + FEATURE_EVENTS, tests, voice scan</name>
  <files>engine/state.js, engine/encounters.js, content/flavor.js, src/browser/eventNarration.js, src/browser/toasts.js, test/unit/joiner-acquisition.test.js, test/unit/party-model.test.js, test/voice/safety-scan.test.js</files>
  <read_first>
    - engine/state.js lines 44–72 (PARTY_CAP doc + addPartyMember — keep both byte-identical; the new helper sits directly below)
    - engine/encounters.js lines 37 (the state.js import) and 490–509 (resolveJoiner — the accept/decline branches and the ALWAYS-clear of pendingJoiner)
    - src/browser/eventNarration.js lines 456–470 (joinerJoined/joinerDeclined/joinerRefused entries — the register to match), and its top-of-file import area (currently no imports; add one)
    - src/browser/toasts.js lines 91–155 (FEATURE_EVENTS — append joinerLeft) and 1056–1064 (joinerJoined/joinerDeclined/joinerRefused builders)
    - content/flavor.js lines 1–24 (header + export style)
    - test/unit/joiner-acquisition.test.js lines 87–180 (fixedPending, the "second accept is refused at PARTY_CAP" test to REWRITE, the rng-free test to extend); test/unit/party-model.test.js lines 116–130 (the addPartyMember cap test — stays; add a sibling for swapPartyMember)
    - test/voice/safety-scan.test.js lines 51–66 (imports) and 230–275 (collectAuthoredStrings — where MISS_LINES is pushed; mirror for JOINER_EXIT_LINES)
    - test/unit/formatEventsCoverage.test.js lines 40–80 and test/unit/toastsCoverage.test.js lines 60–100 (how event types are enumerated from engine/*.js — `type: "joinerLeft"` must be a literal string at the push site)
  </read_first>
  <action>
    `engine/state.js`: below `addPartyMember` add `export function swapPartyMember(state, member)` — fail-open `if (!Array.isArray(state.party)) state.party = [];` then `const left = state.party.length >= PARTY_CAP ? state.party.splice(0, 1)[0] : null;` then `state.party.push(member); return { added: true, left };`. Doc-comment (DELIBERATE RULES CHANGE, Phase 25.1, 2026-09-15, DFB-04): accepting a Joiner with a full roster replaces the LONGEST-SERVING member (index 0 — the only slot at PARTY_CAP 1, FIFO if the cap ever rises); zero rng, plain data mutation, party array shape unchanged; addPartyMember keeps its refuse-when-full contract for every other caller.

    `engine/encounters.js`: import `swapPartyMember` beside `addPartyMember`. Rewrite resolveJoiner's accept branch: `if (accept && pending) { const { left } = swapPartyMember(state, pending); if (left) events.push({ type: "joinerLeft", name: left.name, sub: left.sub, replacedBy: pending.name }); events.push({ type: "joinerJoined", name: pending.name, sub: pending.sub, lvl: pending.lvl }); } else { events.push({ type: "joinerDeclined", name: pending ? pending.name : undefined }); } state.pendingJoiner = null; return events;`. Update the doc comment: accept with a full roster swaps (joinerLeft BEFORE joinerJoined); decline/no-candidate unchanged; still zero rng; no parity fixture meets a Joiner. (addPartyMember is no longer called here — drop it from the import if unused, or keep it imported only if something else in the file uses it; run the file's own import check by node --test afterwards.)

    `content/flavor.js`: append `export const JOINER_EXIT_LINES = [ ... ]` with exactly these five templates (`{name}` = the member who leaves, `{new}` = the newcomer), family-friendly sarcasm in the game's register:
    1. "{name} has heard how this ends and would rather it end for someone else."
    2. "{name} takes one look at {new}, does the arithmetic, and walks."
    3. "{name} wishes {new} the very best, from a safe and steadily increasing distance."
    4. "{name} has seen how this goes and would rather not be the one it goes to."
    5. "{name} leaves without a fuss. Somewhere out there is a longer life expectancy, and {name} intends to find it."
    Doc-comment: Phase 25.1 DFB-04; picked by eventNarration.js WITHOUT rng (index by name lengths); scanned by test/voice/safety-scan.test.js.

    `src/browser/eventNarration.js`: add `import { JOINER_EXIT_LINES } from "../../content/flavor.js";` at the top (content is pure data — this module still never imports engine/). Add a local `const escapeHtml = (s) => String(s).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");` beside `plural`. Add, next to joinerJoined: `joinerLeft: (e) => { const rawName = e.name ?? "Your companion"; const rawNew = e.replacedBy ?? "the new arrival"; const idx = (String(rawName).length + String(rawNew).length) % JOINER_EXIT_LINES.length; const line = JOINER_EXIT_LINES[idx].replaceAll("{name}", escapeHtml(rawName)).replaceAll("{new}", escapeHtml(rawNew)); return `<span class="beat">${line}</span>`; }` with a comment (DFB-04: deterministic pick, no rng; names escaped because a member name is interpolated twice into markup).

    `src/browser/toasts.js`: add `"joinerLeft"` to FEATURE_EVENTS (next to joinerRefused) and the builder `joinerLeft: (e) => ({ text: `${e?.name ?? "Your companion"} walks. ${e?.replacedBy ?? "Someone new"} is in.`, tone: "beat", priority: PRIORITY.feature })` beside joinerJoined (on the resolveJoiner action the narrative ctx replaces this with the snark sentence — Plan 01's NARRATIVE_ACTIONS includes resolveJoiner — so this table text is the fallback/coverage form).

    Tests:
    - `test/unit/joiner-acquisition.test.js`: import `swapPartyMember` too. REWRITE "resolveJoiner: a second accept is refused at PARTY_CAP and still clears the candidate" as "resolveJoiner: a second accept SWAPS the roster — joinerLeft fires before joinerJoined, the newcomer is the only member": accept Ada, then pending Bo, accept → `state.party.length === 1`, `state.party[0].name === "Bo"`, `state.pendingJoiner === null`, events contain `joinerLeft` with `{ name: "Ada", replacedBy: "Bo" }` and `sub` equal to Ada's sub, `events.findIndex(joinerLeft) < events.findIndex(joinerJoined)`, no joinerDeclined; also assert `Object.keys(state.party[0]).sort()` deep-equals `Object.keys(fixedPending({ name: "Bo" })).sort()` (no new field on the member). ADD "resolveJoiner: declining with a full roster keeps the roster and emits only joinerDeclined" (accept Ada; pending Bo; `resolveJoiner(state, false, [])` → party still [Ada], events has joinerDeclined and NO joinerLeft/joinerJoined). ADD "resolveJoiner: accepting into an EMPTY roster never emits joinerLeft" (events lack joinerLeft; party length 1). EXTEND the rng-free test with a swap case (`rngState` still 12345 after a full-roster accept). ADD "swapPartyMember: fail-open on a missing party, splices index 0 when full, returns the leaver" (delete state.party → `{ added: true, left: null }`, length 1; fill then swap → `left.name === "Ada"`, party [Bo]).
    - `test/unit/party-model.test.js`: after the addPartyMember cap test add "swap: swapPartyMember replaces index 0 at PARTY_CAP and adds NO rng draw" (newRun(3), two makeMember sheets; after swap `s.party.length === PARTY_CAP`, the member is the second sheet, `s.rngState` unchanged before/after both calls).
    - `test/voice/safety-scan.test.js`: add `JOINER_EXIT_LINES` to the content/flavor.js import and push each line in collectAuthoredStrings (`JOINER_EXIT_LINES.forEach((s, i) => push(`JOINER_EXIT_LINES[${i}]`, s));`), directly after the MISS_LINES push with a matching one-line comment. Also add, in a suitable narration test file (test/unit/narrativeToasts.test.js from Plan 01 is fine — append), "joinerLeft: a name containing '<' renders escaped and the pick is deterministic": `EVENT_NARRATION.joinerLeft({ type: "joinerLeft", name: "<b>Ada", sub: "Knight", replacedBy: "Bo" })` contains `&lt;b&gt;Ada`, does NOT contain `<b>`, and calling it twice returns identical strings; every JOINER_EXIT_LINES entry contains `{name}`; `narrativeToastText(EVENT_NARRATION.joinerLeft({ type: "joinerLeft", name: "Ada", sub: "Knight", replacedBy: "Bo" }))` starts with "Ada ".
    Run `node --test test/unit/joiner-acquisition.test.js test/unit/party-model.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/narrativeToasts.test.js test/voice/safety-scan.test.js` — green. Commit: `feat(25.1-02): Joiner swap — joinerLeft before joinerJoined, snark exit lines, offer-aware roster` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/joiner-acquisition.test.js test/unit/party-model.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/narrativeToasts.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/joiner-acquisition.test.js` exits 0 with `# pass` >= 13 (9 existing, one rewritten, + 4 new) and `# fail 0`; `node --test test/unit/party-model.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/voice/safety-scan.test.js` exits 0 with `# fail 0`.
    - `node -e "import('./engine/encounters.js').then(async ({resolveJoiner})=>{const s={party:[{name:'Ada',sub:'Knight'}],pendingJoiner:{name:'Bo',sub:'Cleric',lvl:1},rngState:7};const ev=resolveJoiner(s,true,[]);console.log(ev.map(e=>e.type).join(','), s.party.length, s.party[0].name, s.rngState)})"` prints `joinerLeft,joinerJoined 1 Bo 7`.
    - `node -e "import('./content/flavor.js').then(m=>console.log(m.JOINER_EXIT_LINES.length, m.JOINER_EXIT_LINES.every(l=>l.includes('{name}'))))"` prints `5 true`.
    - `node -e "import('./src/browser/eventNarration.js').then(m=>{const h=m.EVENT_NARRATION.joinerLeft({type:'joinerLeft',name:'<b>Ada',sub:'Knight',replacedBy:'Bo'});console.log(h.includes('&lt;b&gt;Ada'), h.includes('<b>'))})"` prints `true false`.
    - `node -e "import('./src/browser/toasts.js').then(m=>console.log(m.FEATURE_EVENTS.includes('joinerLeft'), typeof m.TOAST_FOR.joinerLeft, m.ORACLE_ONLY.has('joinerLeft')))"` prints `true function false`.
    - `grep -v '^\s*//' engine/state.js | grep -c "export function swapPartyMember"` prints `1` and `grep -v '^\s*//' engine/state.js | grep -c "export const PARTY_CAP = 1;"` prints `1`.
    - `git status --porcelain engine/combat.js engine/derived.js test/parity` prints nothing.
    - `git log -1 --format=%s` starts with `feat(25.1-02)`.
  </acceptance_criteria>
  <done>Accepting a second Joiner swaps the first out with a snarky, escaped, deterministically-chosen exit line in the Oracle and the toast; declining keeps the roster; the party shape and rng cursor are untouched; every coverage guard and the voice scan are green.</done>
</task>

<task type="auto">
  <name>Task 2: nightlyEats + campFailed with numbers — engine helper shared by newDay/makeCamp, narration + toast render need/have, movement tests</name>
  <files>engine/movement.js, src/browser/eventNarration.js, src/browser/toasts.js, test/unit/movement.test.js</files>
  <read_first>
    - engine/movement.js lines 379–420 (newDay's day/charge/upkeep prologue and the PARTY-10 member loop that sums `cost` and `eats`), 520–532 (makeCamp), 103–104 (maxCharges — export style)
    - src/browser/eventNarration.js line 138 (campFailed entry) and src/browser/toasts.js line 775 (campFailed builder) plus its `block()` helper at ~L159
    - test/unit/movement.test.js lines 840–905 (member() helper, the solo/party newDay ration tests, the makeCamp tests) and its fakeRng/fixedState helpers near the top
    - test/unit/toastTable.test.js lines 38–56 and test/unit/toastsForAction.test.js lines 95–110 (campFailed must stay tone block / priority 0 / non-empty for a bare `{ type }` payload)
    - test/unit/identity-contract.test.js lines 1138–1148 (the Troll bad: `makeCamp(state, fakeRng([]), [])` with 1 ration expects campFailed reason noRations — must keep passing with the additive fields)
  </read_first>
  <action>
    `engine/movement.js`:
    1. Add, above newDay, `export function nightlyEats(state)` — `const c = state.c; let eats = RACES[c.race].eats || 1; if (state.party?.length) for (const m of state.party) eats += RACES[m.race]?.eats || 1; return eats;`. Doc-comment (Phase 25.1, DFB-06): the ONE definition of "how much this party eats per night", read by newDay AND makeCamp (and, read-only, by the shell's camp button through window.__mzNightlyEats), so the camp gate and the automatic new day can never disagree; pure read, zero rng; with no party it equals the hero's own appetite exactly as before.
    2. In newDay replace `let eats = R.eats || 1;` with `const eats = nightlyEats(state);` and remove the `eats += RACES[m.race]?.eats || 1;` line from the member loop (the loop keeps `cost += upkeep(m);`). Nothing else in newDay moves — the eat-then-heal-or-starve order and every draw are untouched. Add one sentence to the PARTY-10 comment: the food half now comes from nightlyEats (shared with makeCamp, DFB-06).
    3. makeCamp: after the combat/store/dead/won guard compute `const need = nightlyEats(state); const have = state.c.rations;` and `if (have < need) { const members = (state.party ?? []).map((m) => ({ name: m.name, eats: RACES[m.race]?.eats || 1 })); events.push({ type: "campFailed", reason: "noRations", need, have, ...(members.length ? { members } : {}) }); return events; }` then `return newDay(state, true, rng, events, now);`. Doc-comment (DELIBERATE RULES CHANGE, Phase 25.1, 2026-09-15, DFB-06): the gate now counts every live member's appetite exactly as newDay does (the old gate counted the hero only, so a hero with a member could pass the gate and still go hungry); the refusal states the numbers; solo heroes keep need === R.eats||1 so every fixture's camp is byte-identical; `members` is spread in only when a party exists so the solo event shape is `{ type, reason, need, have }`.

    `src/browser/eventNarration.js` — replace the campFailed entry: `campFailed: (e) => { const need = e.need ?? "?"; const have = e.have ?? "?"; const extra = (e.members ?? []).map((m) => `${m.name ?? "Your companion"} eats ${m.eats ?? 1} more`).join(", "); return `<span class="miss">You eat ${need} a night${extra ? ` (${extra})` : ""}. You have ${have}.</span> Find rations first.`; }` (bare `{type}` renders "You eat ? a night. You have ?. Find rations first." — never undefined/NaN).
    `src/browser/toasts.js` — replace the campFailed builder: `campFailed: (e) => block(e?.need != null && e?.have != null ? `You eat ${e.need} a night, you have ${e.have}. Find rations first.` : "Not enough food to make camp.")` (still amber block, priority 0, non-empty for a bare payload; on the camp action Plan 01's narrative ctx shows the Oracle sentence instead).

    `test/unit/movement.test.js` — import `nightlyEats` and add, after the existing makeCamp tests:
    1. "nightlyEats: solo hero = own appetite (Human 1, Troll 2); no party field and party [] agree": fixedState Human → 1; `fixedState({ c: { race: "Troll" } })` → 2; with `party: []` and with `delete state.party` → same numbers.
    2. "nightlyEats: every live member adds its race appetite; a member with an unknown race counts as 1": Human + Human member → 2; Human + Troll member → 3; Human + `member({ race: "Nonesuch" })` → 2.
    3. "makeCamp DFB-06: a Troll with 1 ration is refused with need 2 have 1, drawing nothing and changing nothing": `fixedState({ c: { race: "Troll", rations: 1, wp: 40 } })`, `makeCamp(state, fakeRng([]), [])` → the campFailed event deep-equals `{ type: "campFailed", reason: "noRations", need: 2, have: 1 }` (exact solo shape — no members key), `state.day === 1`, `state.c.rations === 1`, `state.c.wp === 40`.
    4. "makeCamp DFB-06: a Troll with exactly 2 rations camps (strict less-than) and eats both": rations 2; rng = the existing successful-camp sequence used by "makeCamp: camps successfully..." (heal roll + 8 safe monster-check draws); after: `state.day === 2`, `state.c.rations === 0`, no campFailed event.
    5. "makeCamp DFB-06: a Human hero with a Human member and 1 ration is refused with need 2 have 1 and names the member": `fixedState({ c: { rations: 1 }, party: [member({ name: "Bram", race: "Human" })] })`, fakeRng([]) → campFailed with `need 2`, `have 1`, `members` deep-equals `[{ name: "Bram", eats: 1 }]`; party untouched.
    6. "makeCamp DFB-06: a solo Human with 1 ration still camps (the gate is unchanged for solo heroes)": rations 1 + the successful-camp rng → day 2, rations 0.
    7. "makeCamp DFB-06: zero rations refuses with have 0": Human, rations 0 → `have === 0`, `need === 1`.
    8. "newDay DFB-06: the rations delta equals nightlyEats(state) for solo and for hero + member (arithmetic unchanged)": for `fixedState({ c: { rations: 6 } })` and `fixedState({ c: { rations: 6 }, party: [member({ race: "Troll" })] })` compute `before = state.c.rations; need = nightlyEats(state); newDay(state, false, <existing safe rng sequence>, [])` → `before - state.c.rations === need`.
    9. "campFailed narration and toast render the numbers and the member clause": `EVENT_NARRATION.campFailed({ type: "campFailed", reason: "noRations", need: 2, have: 1, members: [{ name: "Bram", eats: 1 }] })` stripped of tags equals "You eat 2 a night (Bram eats 1 more). You have 1. Find rations first."; the solo payload `{ need: 2, have: 1 }` renders "You eat 2 a night. You have 1. Find rations first."; `TOAST_FOR.campFailed({ type: "campFailed", need: 2, have: 1 })` has tone block and text containing "2" and "1"; a bare `{ type: "campFailed" }` toast is still tone block with non-empty text (put this test in test/unit/narrativeToasts.test.js or movement.test.js — importing the two presentation modules from a unit test is the existing pattern).
    Run `node --test test/unit/movement.test.js test/unit/identity-contract.test.js test/unit/engineAdapter.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/voice/safety-scan.test.js` — green. Commit: `feat(25.1-02): nightlyEats shared by newDay/makeCamp; campFailed carries need/have and says the numbers` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/movement.test.js test/unit/identity-contract.test.js test/unit/engineAdapter.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/movement.test.js` exits 0 with `# fail 0` and `# pass` at least 9 higher than before this task.
    - `node -e "import('./engine/movement.js').then(({makeCamp,nightlyEats})=>{const st=(race,rations,party)=>({c:{race,rations,wp:40,level:1,items:[],skills:{}},party,day:1,combat:null,store:null,dead:false,won:false});const a=st('Troll',1,[]);const ea=makeCamp(a,{d(){throw new Error('drew')}},[]);const b=st('Human',1,[{name:'Bram',race:'Human'}]);const eb=makeCamp(b,{d(){throw new Error('drew')}},[]);console.log(JSON.stringify(ea[0]), JSON.stringify(eb[0]), nightlyEats(st('Human',0,undefined)))})"` prints `{"type":"campFailed","reason":"noRations","need":2,"have":1} {"type":"campFailed","reason":"noRations","need":2,"have":1,"members":[{"name":"Bram","eats":1}]} 1`.
    - `grep -v '^\s*//' engine/movement.js | grep -c "= nightlyEats(state);"` prints `2` (`const eats = nightlyEats(state);` in newDay, `const need = nightlyEats(state);` in makeCamp) and `grep -v '^\s*//' engine/movement.js | grep -c "export function nightlyEats"` prints `1`.
    - `node -e "import('./src/browser/eventNarration.js').then(m=>console.log(m.EVENT_NARRATION.campFailed({type:'campFailed',need:2,have:1,members:[{name:'Bram',eats:1}]}).replace(/<[^>]+>/g,'')))"` prints `You eat 2 a night (Bram eats 1 more). You have 1. Find rations first.`.
    - `node --test "test/parity/**/*.test.js" 2>&1 | grep -E "^# (pass|fail)"` shows `# fail 0` and `git status --porcelain test/parity` prints nothing (solo camp gate byte-identical).
    - `git log -1 --format=%s` starts with `feat(25.1-02)`.
  </acceptance_criteria>
  <done>One helper defines the party's nightly appetite; the camp gate counts the party exactly like the automatic new day; the refusal event, Oracle line and toast all state "you eat N a night, you have M"; solo camps are byte-identical and parity is green.</done>
</task>

<task type="auto">
  <name>Task 3: Shell — offer card names who walks, camp button short-state from nightlyEats, module bridges; shell-party-camp test; parity + full suite + build</name>
  <files>mazeworld.html, test/unit/shell-party-camp.test.js</files>
  <read_first>
    - mazeworld.html lines 4803–4830 (renderEncounter's pendingJoiner branch — the `enc-head` line to change), 2815–2830 (paint(): the m-rations / m-rat-wrap lines where the camp-button state is added), 1353 (the `<button class="small" id="btn-camp">Make camp</button>` markup), 476–484 (button.small + #btn-camp CSS), 5447–5470 (module imports of engine modules + the window.__mzBags/__mzSellPrice bridge block), 5156–5158 (btn-camp onclick — unchanged, the button stays tappable)
    - test/unit/shell-toast-wiring.test.js lines 1–60 (stripComments/HTML/CODE setup to copy)
  </read_first>
  <action>
    Module script: add `import { nightlyEats } from "./engine/movement.js";` and `import { PARTY_CAP } from "./engine/state.js";` beside the existing `import { conditionsOf } from "./engine/derived.js";` line, and in the bridge block beside `window.__mzBags = BAGS;` add `window.__mzNightlyEats = nightlyEats;` and `window.__mzPartyCap = PARTY_CAP;` with a comment (Phase 25.1 DFB-04/DFB-06: read-only helpers for the classic script's offer card and camp button; the ENGINE alone enforces the swap and the camp gate).

    renderEncounter pendingJoiner branch (classic script): compute `const cap = window.__mzPartyCap ?? 1; const walker = Array.isArray(S.party) && S.party.length >= cap ? S.party[0] : null;` (index 0 mirrors engine/state.js#swapPartyMember). Replace the `<p class="enc-head" ...>${j.name || "A wanderer"} wants to tag along</p>` string with a DOM-built element so the names go in as text: `const head = document.createElement("p"); head.className = "enc-head"; head.style.color = "var(--moss)"; head.textContent = walker ? `Take ${j.name || "them"} along? ${walker.name || "Your companion"} walks.` : `${j.name || "A wanderer"} wants to tag along`; body.appendChild(head);` — then keep the following `body.innerHTML += \`<p class="enc-sub">...` block and the two buttons exactly as they are (the button handlers are attached after every innerHTML append, so the re-parse is safe). Comment: DFB-04 — an informed decision names who walks; textContent because a name is interpolated.

    paint() (classic script), right after the `m-rat-wrap` warn toggle: `const campBtn = document.getElementById("btn-camp"); if (campBtn) { const need = window.__mzNightlyEats ? window.__mzNightlyEats(S) : 0; const short = c.rations < need; campBtn.dataset.short = short ? "1" : "0"; campBtn.setAttribute("aria-label", short ? `Make camp — you eat ${need} a night, you have ${c.rations}` : "Make camp"); }`. Comment (Phase 25.1 DFB-06, Claude's discretion): the button is NOT disabled — the shell has never disabled it and a disabled button could never show the refusal toast the user asked for — instead it carries a visible short-on-food state computed by the SAME exported helper the engine's makeCamp uses (window.__mzNightlyEats), so the dimmed state and the engine's refusal can never disagree; tapping still dispatches "camp" and the engine's campFailed toast states the numbers. CSS: add `#btn-camp[data-short="1"]{opacity:.55;border-style:dashed}` next to the #btn-camp rules.

    Create `test/unit/shell-party-camp.test.js` (same stripComments/HTML/CODE setup as shell-toast-wiring): 
    1. "DFB-04/06: the module bridges nightlyEats and PARTY_CAP from the engine": `/import \{ nightlyEats \} from "\.\/engine\/movement\.js";/`, `/import \{ PARTY_CAP \} from "\.\/engine\/state\.js";/`, `/window\.__mzNightlyEats = nightlyEats;/`, `/window\.__mzPartyCap = PARTY_CAP;/`.
    2. "DFB-04: the Joiner offer names who walks, via textContent": `/head\.textContent = walker \? `Take \$\{j\.name \|\| "them"\} along\? \$\{walker\.name \|\| "Your companion"\} walks\.`/` and `/S\.party\[0\]/` inside the pendingJoiner branch (slice CODE from `if (S.pendingJoiner && !S.combat && !S.store)` to `if (S.pendingFind && !S.combat && !S.store)`).
    3. "DFB-06: paint() derives the camp button's short state from window.__mzNightlyEats": in CODE `/window\.__mzNightlyEats\(S\)/`, `/campBtn\.dataset\.short = short \? "1" : "0";/`, and the CSS `/#btn-camp\[data-short="1"\]\{/` in HTML; and the button is still wired to camp: `/document\.getElementById\("btn-camp"\)\.onclick/`.
    4. "DFB-06: the camp button is never disabled by the shell": the region of CODE inside paint() that mentions `campBtn` never assigns the button's disabled property — build the forbidden pattern from fragments (`"campBtn" + ".disabled"`) so the test file itself never spells it, and scope the check to the campBtn region only.
    Then the gate: `npm test 2>&1 | tail -8` (`# fail 0`), `node --test "test/parity/**/*.test.js" 2>&1 | tail -4` (33 files, `# fail 0`, `git status --porcelain test/parity` empty), `npm run build:www` (exit 0). Commit: `feat(25.1-02): offer card names who walks; camp button short-state from nightlyEats` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/shell-party-camp.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-oracle-panel.test.js && npm test 2>&1 | tail -8 && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-party-camp.test.js` exits 0 with `# pass` >= 4 and `# fail 0`; `node --test test/unit/shell-toast-wiring.test.js test/unit/shell-oracle-panel.test.js test/unit/parley-button-mirror.test.js test/unit/foe-effect-chip.test.js test/unit/tutorial.test.js test/unit/save-validation.test.js` exits 0.
    - `grep -c "window.__mzNightlyEats = nightlyEats;" mazeworld.html` prints `1`; `grep -c "window.__mzPartyCap = PARTY_CAP;" mazeworld.html` prints `1`; `grep -c 'walks.`' mazeworld.html` prints `1`; `grep -c 'campBtn.dataset.short' mazeworld.html` prints `1`; `grep -c 'campBtn.disabled' mazeworld.html` prints `0`.
    - `npm test 2>&1 | tail -8` shows `# fail 0` and `# pass` >= 1357 + 4 + 9 + 4 + 1 + 1 = 1376 (Plan 01's floor plus this plan's joiner-acquisition, movement, shell-party-camp, party-model and narrativeToasts additions).
    - `node --test "test/parity/**/*.test.js" 2>&1 | grep -E "^# (pass|fail)"` shows `# fail 0`; `git status --porcelain test/parity` prints nothing; `git diff --quiet HEAD -- test/parity/prototype-master.js.txt; echo rc=$?` prints `rc=0`.
    - `npm run build:www` exits 0.
    - `git status --porcelain engine/combat.js engine/derived.js` prints nothing (25.1-03's files untouched).
    - `git log -1 --format=%s` starts with `feat(25.1-02)`.
  </acceptance_criteria>
  <done>The offer card says who walks, the camp button visibly reflects the same appetite math the engine refuses on (and tapping it still surfaces the numbers), both read-only helpers are bridged from the engine, and the full suite, parity and build are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine event -> narration HTML | member/newcomer names are interpolated twice into markup (joinerLeft) |
| engine <-> shell | PARTY_CAP and nightlyEats are bridged read-only; the shell never mutates the roster or rations |
| parity fixtures | solo runs must not observe the widened camp gate or the swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-07 | Tampering | joinerLeft name interpolation into markup | medium | mitigate | escapeHtml on both names; test with a '<' name; offer head via textContent |
| T-25.1-08 | Tampering | rng drift from the swap or the camp gate | high | mitigate | resolveJoiner/swapPartyMember take no rng; refusal precedes newDay; fakeRng([]) tests prove zero draws; parity 33/33 |
| T-25.1-09 | Tampering | party shape drift (new serialized field) | high | mitigate | swap is splice+push; key-set test on the surviving member; campFailed fields are event payload only |
| T-25.1-10 | Repudiation | camp button and engine disagreeing on "enough food" | medium | mitigate | one exported helper (nightlyEats) read by newDay, makeCamp and the shell bridge; test asserts newDay's delta equals the helper |
| T-25.1-11 | Information Disclosure | banned copy in the new snark lines | medium | mitigate | JOINER_EXIT_LINES added to the voice safety scan corpus |
| T-25.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/joiner-acquisition.test.js test/unit/party-model.test.js test/unit/movement.test.js test/unit/shell-party-camp.test.js test/voice/safety-scan.test.js` green; `npm test` `# fail 0`, `# pass` >= 1376.
- `node --test "test/parity/**/*.test.js"` 33 files green, zero fixture changes, `test/parity/prototype-master.js.txt` untouched.
- `npm run build:www` exits 0; engine/combat.js and engine/derived.js untouched.
- Device acceptance (non-blocking): accept a second Joiner → snark exit toast + Oracle line, the offer card said "Take X along? Y walks."; Make Camp with too little food → toast "You eat N a night (…). You have M. Find rations first." and the camp button is dimmed with the same numbers in its accessible label; the automatic new day still eats/starves as before.
</verification>

<success_criteria>
- DFB-04: swap on accept with a full roster (joinerLeft before joinerJoined, snark line, escaped names, offer names who walks), decline keeps the roster, zero rng, no new serialized field.
- DFB-06: nightlyEats shared by newDay and makeCamp; refusal states need/have counting every member; solo gate unchanged; the shell's button state uses the same helper.
</success_criteria>

<output>
Create `.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-02-SUMMARY.md` when done. Record: the exact joinerLeft and campFailed event shapes as committed, the five exit lines, the offer-head text, the camp-button state decision (short-state, not disabled) and why, test counts, parity result, `npm test` pass count.

Commit message footer for every commit in this plan:
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS
</output>
