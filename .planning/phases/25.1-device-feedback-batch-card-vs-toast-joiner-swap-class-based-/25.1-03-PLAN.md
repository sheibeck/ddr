---
phase: 25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based
plan: 03
type: execute
wave: 3
depends_on: ["25.1-02"]
files_modified:
  - engine/derived.js
  - engine/combat.js
  - engine/movement.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - test/unit/party-combat.test.js
  - test/unit/joiner-acquisition.test.js
autonomous: true
requirements: [DFB-05]
tags: [engine, combat, party, class-based-allies, ally-cast, backstab, narration, toasts, parity, dfb-05]

must_haves:
  truths:
    - "alliesTurn branches on the member's class read LIVE from state.party[ally.partyIdx] (the persistent sheet): Fighter -> to-hit need from memberToHit (CLASSES[cls].toHit floored by the race's toHit, Acrobat 5, Cleric 4, Kata), strike die from strikeDie(sheet view), damage from weaponDamage(sheet view, rng) (its own Club fallback stands in for a weaponless sheet), crit on a natural 1 (never for a Guard or Soldier, mirroring the hero); Thief -> the first LANDED blow of the fight is a backstab (crit x2, `backstab: true`, `crit: true`) tracked by `backstabUsed` on the transient C.allies entry (set only when the blow lands, so a missed opener keeps the backstab; heavy armor uses the hero's exact list), thereafter normal; Magic User -> bestAttackSpell({ c: view }) (highest effective level via spellLevelFor, thrown preferred at equal level, SPELLS order breaks ties) cast at the hero's current live target while maxCharges(view) - spellsUsed > 0, paying the charge on the PERSISTENT sheet (sheet.spellsUsed++), else a staff swing with the Magic User to-hit (3). A C.allies entry whose persistent sheet is missing or has no cls/race takes the LEGACY strike (d(STRIKE_DICE[lvl-1]) <= 5, lvl*lvl + d6) byte-identical to before."
    - "Every new rng draw sits inside the existing `if (!C || !C.allies || !C.allies.length) return events;` gate of alliesTurn (an absent C.allies still draws zero — fakeRng([]) never throws); startCombat, endCombat, foeTurn, playerStrike and the hero's castSpell are byte-identical (git diff on engine/magic.js is empty); parity 33/33 with zero fixture changes and test/parity/prototype-master.js.txt untouched."
    - "allyCast is self-contained in engine/combat.js (combat.js cannot import magic.js — magic.js imports combat.js): thrown = d8 vs 4 (Freeze d10 vs 6) with bonus schoolBonus(sub, school) + eff(throw), hit when roll - bonus <= need, damage rollDice(sp.dmg) * max(1, level - sp.lvl) + eff(spellDmg) through damageFoe kind 'spell' (no armor draw), Freeze freezes and routes through killFoe (kill-twice revives un-frozen) exactly like the hero's Phase 23 rule; status/stun/weaken = resistRoll(rng, t.intel) first (only an intel >= 12 target draws), a resisted cast is allySpellMissed { resisted: true }, otherwise status/stun put the target to sleep max(asleep, d4) rounds and weaken sets C.weakened/C.foeToHitPenalty = 3. Events: allyCast { name, spell, target, roll?, need?, bonus? } then exactly one of allySpellHit { name, spell, target, effect: 'damage'|'frozen'|'asleep'|'weakened', dmg?, rounds? } or allySpellMissed { name, spell, target, resisted, roll? }."
    - "allyStruck/allyMissed are extended ADDITIVELY via conditional spread: allyStruck gains `weapon` (only when the sheet has one), `crit: true` (only when critical) and `backstab: true` (only on the Thief opener); allyMissed gains `target`, `roll`, `need` on the classed path and `weapon` when present; the legacy path emits the pre-25.1 shapes exactly; the summoned ally's allyTurn is untouched."
    - "Member spell charges recover per day: newDay resets each member's spellsUsed to 0 inside the existing party-gated loop, conditionally (`if (m.spellsUsed) m.spellsUsed = 0;`) so a sheet without the field never gains one (no new serialized field); the hero's reset line is unchanged."
    - "Every new event type is narrated and toasted: allyCast is Oracle-only (its allySpellHit/allySpellMissed sibling always follows in the same action and toasts the outcome naming the spell — 'Ysolde casts Freeze — Goblin frozen solid'); allySpellHit, allySpellMissed, allyStruck, allyMissed are in FEATURE_EVENTS with TOAST_FOR builders and EVENT_NARRATION entries that render the class flags ('Bram backstabs Dante (14)', '· CRIT'); the Phase 25 coverage guards, the voice scan and every existing toast test stay green; the legacy allyStruck/allyMissed wording is byte-identical when the new fields are absent."
    - "Edge: the target dying mid-round ends the party's turn for that round exactly as today (the liveFoes re-check and break are unchanged); a member with wp <= 0 is skipped (unchanged); a Magic User whose only attack spell is above its level, or whose charges are spent, swings the staff (tested); the Thief backstab fires at most once per combat and resets with a new startCombat (the C.allies entry is rebuilt); `node tools/tune-difficulty.mjs --seeds=12 --party` completes with exit 0 (nothing throws with class-based allies over real seeds)."
  artifacts:
    - path: "engine/derived.js"
      provides: "memberToHit(m) and bestAttackSpell(state) — pure reads beside toHit/castableAttackSpells"
      exports: ["memberToHit", "bestAttackSpell"]
    - path: "engine/combat.js"
      provides: "class-based alliesTurn (memberView, memberStrike, allyCast helpers; legacy fallback branch); allyCast/allySpellHit/allySpellMissed events; additive allyStruck/allyMissed fields"
    - path: "engine/movement.js"
      provides: "newDay resets member spellsUsed (party-gated, conditional)"
    - path: "src/browser/eventNarration.js"
      provides: "allyCast, allySpellHit, allySpellMissed entries; allyStruck/allyMissed render weapon/crit/backstab/target"
    - path: "src/browser/toasts.js"
      provides: "ORACLE_ONLY + allyCast; TOAST_FOR allySpellHit/allySpellMissed; allyStruck/allyMissed flags; FEATURE_EVENTS + the four ally events"
    - path: "test/unit/party-combat.test.js"
      provides: "class-branch tests (Fighter weapon/crit/noCrit, Thief backstab once + heavy armor + miss-keeps-opener, MU cast Freeze/Doze/resist/miss, charge exhaustion -> staff, best-spell choice, legacy fallback, newDay reset, zero-draw gate)"
  key_links:
    - from: "engine/combat.js#alliesTurn"
      to: "engine/derived.js#memberToHit / #bestAttackSpell / strikeDie / weaponDamage / schoolBonus / resistRoll / castableAttackSpells"
      via: "extend the existing derived.js import line in combat.js"
      pattern: "memberToHit, bestAttackSpell"
    - from: "engine/combat.js#alliesTurn"
      to: "state.party[ally.partyIdx]"
      via: "the persistent sheet is read live each swing; only spellsUsed is written back (the charge)"
      pattern: "state\\.party\\[ally\\.partyIdx\\]"
    - from: "src/browser/toasts.js#toastsForAction"
      to: "allySpellHit / allySpellMissed builders"
      via: "direct-mapped in step 7 (no new fold); allyCast is ORACLE_ONLY so a cast produces exactly one toast"
      pattern: "allySpellHit"
  prohibitions:
    - "engine/magic.js is not edited (the hero's castSpell stays byte-identical); combat.js never imports magic.js"
    - "No draw outside alliesTurn's existing C.allies gate; startCombat's party sync, endCombat's sync-out, foeTurn and playerStrike are not edited"
    - "No new field on the persistent member sheet (backstabUsed lives on the transient C.allies entry, which is nulled with combat on every save load and is never present in a parity fixture); no new field on state.c"
    - "No edit to test/parity/prototype-master.js.txt or any parity fixture; tools/lib/tuning-bot.mjs is not edited"
    - "Existing party-combat draw sequences ([3, 4] hit for the weaponless Fighter fixture, [9] miss, [3, 6, 4, 1, 20] kill) keep passing unchanged"
---

<objective>
Party members fight according to their class (DFB-05): a Fighter strikes with its real weapon on its class/race to-hit and crits on a natural 1; a Thief opens the fight with a backstab crit and then fights normally; a Magic User casts its best castable attack spell at the hero's current target with its own daily charges (a self-contained `allyCast` in engine/combat.js that mirrors the hero's thrown/status dice shapes) and swings its staff when it cannot. Every new draw sits behind the existing `C.allies` gate so solo parity stays byte-identical; the new events (`allyCast` Oracle-only, `allySpellHit`, `allySpellMissed`) and the additive `allyStruck`/`allyMissed` flags are narrated and toasted so ally combat reads like the hero's ("Ysolde casts Freeze — Goblin frozen solid", "Bram backstabs Dante (14)").

Purpose: 25.1-CONTEXT.md "Joiners fight by class"; runs before Phase 26 so the ledger's engine pin includes it (the AFTER matrix is party=off and unaffected).
Output: engine/derived.js, engine/combat.js, engine/movement.js, the two presentation tables, tests, three commits, parity 33/33, `npm test` green, bot smoke green, `npm run build:www` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-CONTEXT.md
@.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-02-SUMMARY.md
@engine/combat.js
@engine/derived.js
@engine/magic.js
@engine/foeDamage.js
@test/unit/party-combat.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Engine — memberToHit + bestAttackSpell (derived.js), class-based alliesTurn + allyCast (combat.js), member spellsUsed reset (movement.js); party-combat tests; joiner-acquisition integration pin</name>
  <files>engine/derived.js, engine/combat.js, engine/movement.js, test/unit/party-combat.test.js, test/unit/joiner-acquisition.test.js</files>
  <read_first>
    - engine/combat.js lines 52–61 (imports — extend the derived.js line), 250–276 (startCombat's C.allies sync: entries are { partyIdx, name, lvl, sub, wp, maxWP } — do NOT change), 368–530 (playerStrike: the to-hit/crit/noCrit/heavy-armor/backstab/`opening` semantics to mirror, the damageFoe call shape `{ kind, casterClass, casterSub, crit }`, the `struck` conditional-spread precedent), 942–960 (endCombat syncs only `wp` out — backstabUsed on the entry dies with combat), 1037–1108 (allyTurn for the SUMMON — untouched — and alliesTurn, the function to rewrite), 1113–1130 (downMember)
    - engine/magic.js lines 48–60 (charge check shape), 93–110 (resistRoll usage for non-thrown spells), 139–146 (stun), 147–152 (weaken), 339–344 (status/Doze), 345–400 (the thrown resolution: dieN/need/bonus/mult/damageFoe kind 'spell'/Freeze -> frozenSolid + killFoe + kill-twice unfreeze) — mirror these dice shapes; do not edit this file
    - engine/derived.js lines 250–257 (strikeDie), 299–318 (toHit — memberToHit mirrors its first lines only), 425–460 (weaponDamage — needs level/prof/magicWpn/might/items/skills and a valid race), 513–517 (resistRoll), 574–582 (schoolGate/schoolBonus), 600–650 (spellLevelFor, ATTACK_SPELL_KINDS, castableAttackSpells, canCast — all read state.c only)
    - engine/foeDamage.js lines 60–109 (damageFoe contract: kind 'ally' is physical and soakable; crit bypasses the soak; kind 'spell' never draws)
    - engine/movement.js lines 103–104 (maxCharges(c) = 2*level + 2 + eff(charges)) and 395–420 (newDay's party loop, now `cost += upkeep(m)` only after Plan 02)
    - test/unit/party-combat.test.js in full (fakeRng throw-on-underflow, fixedState/fixedFoe/fixedCombat/fixedAlly/fixedMember helpers, the existing alliesTurn and afterPlayerAction draw sequences that MUST keep passing) and test/unit/joiner-acquisition.test.js lines 76–86 + 181–205 (fixedPending is a seeded rollCharacter — seed 4321 is a Magic User Illusionist with Doze in the grimoire, so the integration test must pin an explicit Fighter sheet)
    - content/kit.js (every rolled member has a KIT weapon: Magic Users carry a Quarter Staff — the "staff swing"); content/weapons.js Dagger (halve) / Club / Quarter Staff (d6) / Long Sword (d8)
  </read_first>
  <action>
    `engine/derived.js` — add two exported pure reads (zero rng, no mutation):
    - `memberToHit(m)` beside toHit: `let h = CLASSES[m.cls]?.toHit ?? 5; const R = RACES[m.race]; if (R && R.toHit) h = Math.max(h, R.toHit); if (m.sub === "Acrobat") h = 5; if (m.sub === "Cleric") h = Math.max(h, 4); if (skill(m, "Kata")) h = Math.max(h, m.cls === "Fighter" ? 6 : 5); return h;` — doc: the class/race/sub part of toHit for a PARTY MEMBER sheet (no combat.inspired, no darkness, no foeEffect — those read the hero's state); `?? 5` keeps the pre-25.1 "hit on 5" for a sheet with no class.
    - `bestAttackSpell(state)` beside castableAttackSpells: iterate `castableAttackSpells(state)`; keep the spell with the highest `spellLevelFor(state.c.sub, sp)`, preferring `kind === "thrown"` at equal level, first-in-SPELLS-order on remaining ties (strict `>` comparisons); return the spell object or `null`. Doc: DFB-05 "best castable attack spell" — damage beats a nap at equal level; deterministic.

    `engine/combat.js` — extend the derived.js import with `memberToHit, bestAttackSpell, schoolBonus, resistRoll` (keep every existing name). Rewrite `alliesTurn` (keep its signature, the `if (!C || !C.allies || !C.allies.length) return events;` gate, the `for (const ally of C.allies.slice())` snapshot loop, the `ally.wp <= 0` skip and the `liveFoes` re-check/break — LOOP SAFETY unchanged). Per live ally:
    1. `const sheet = Array.isArray(state.party) ? state.party[ally.partyIdx] : null; const classed = !!(sheet && sheet.cls && RACES[sheet.race]);`
    2. If `!classed`: run the LEGACY body verbatim (roll `rng.d(STRIKE_DICE[clamp(ally.lvl, 1, 5) - 1])`, hit on `<= 5`, `ally.lvl * ally.lvl + rng.d(6)`, `damageFoe(..., { kind: "ally", crit: false }, ...)`, `allyStruck { name, target, dmg }` when not soaked, killFoe on `wp <= 0`, else `allyMissed { name }`) and `continue`. Comment: the pre-25.1 strike for an entry with no persistent sheet (defensive; every real member has one).
    3. Build `const view = { ...sheet, level: ally.lvl, prof: sheet.prof ?? 0, magicWpn: sheet.magicWpn ?? 0, might: sheet.might ?? 0, items: sheet.items ?? [], skills: sheet.skills ?? {}, grimoire: sheet.grimoire ?? [], spellsUsed: sheet.spellsUsed ?? 0 };` (a READ view — the combat level wins; every field weaponDamage/maxCharges/canCast touch is defaulted; writes go to `sheet`).
    4. If `sheet.cls === "Magic User"`: `const sp = bestAttackSpell({ c: view }); if (sp && maxCharges(view) - view.spellsUsed > 0) { const cur = C.foes[C.target]; const target = cur && cur.alive ? cur : foes[0]; allyCast(state, ally, sheet, view, sp, target, rng, events); continue; }` — otherwise fall through to the staff swing.
    5. `memberStrike(state, ally, sheet, view, foes[0], rng, events)`.

    Add module-private helpers below alliesTurn:
    - `memberStrike(state, ally, sheet, view, t, rng, events)`: `const need = memberToHit(view); const roll = rng.d(strikeDie(view)); const weapon = sheet.weapon;` miss when `roll > need` → `events.push({ type: "allyMissed", name: ally.name, target: t.name, roll, need, ...(weapon ? { weapon } : {}) }); return;`. Hit: `let dmg = weaponDamage(view, rng); const noCrit = view.sub === "Guard" || view.sub === "Soldier"; let crit = roll === 1 && !noCrit; let backstab = false; const heavy = view.cls === "Thief" && ["Studded Leather", "Chain Mail", "Plate"].includes(view.armor);` (copy the hero's list VERBATIM from playerStrike — fidelity to the hero path, even though only Plate matches a real armor name; not this phase's bug) `if (view.cls === "Thief" && !ally.backstabUsed && !heavy) { crit = true; backstab = true; ally.backstabUsed = true; } if (crit) dmg *= 2; const hit = damageFoe(state, t, dmg, { kind: "ally", crit }, rng, events); if (!hit.soaked) events.push({ type: "allyStruck", name: ally.name, target: t.name, dmg: hit.applied, ...(weapon ? { weapon } : {}), ...(crit ? { crit: true } : {}), ...(backstab ? { backstab: true } : {}) }); if (t.wp <= 0) killFoe(state, t, rng, events);`. Doc-comment: DELIBERATE RULES CHANGE (Phase 25.1, 2026-09-15, DFB-05) — members fight by class; kind stays "ally" (a member never takes the hero-only Fighter-vs-Trachea row, D-20); backstabUsed is transient combat state on the C.allies entry (rebuilt by every startCombat, never synced to the sheet, nulled with combat on load).
    - `allyCast(state, ally, sheet, view, sp, t, rng, events)`: `sheet.spellsUsed = (sheet.spellsUsed || 0) + 1;` (the persistent sheet pays); `const base = { name: ally.name, spell: sp.n, target: t.name };`
      - thrown: `const freeze = sp.n === "Freeze"; const dieN = freeze ? 10 : 8; const need = freeze ? 6 : 4; const bonus = schoolBonus(view.sub, sp.s) + eff(view, "throw"); const roll = rng.d(dieN); events.push({ type: "allyCast", ...base, roll, need, bonus });` hit when `roll - bonus <= need`: `const mult = Math.max(1, view.level - sp.lvl); const dmg = rollDice(rng, sp.dmg) * mult + eff(view, "spellDmg"); const hit = damageFoe(state, t, dmg, { kind: "spell", school: sp.kind, casterSub: view.sub }, rng, events); events.push({ type: "allySpellHit", ...base, effect: freeze ? "frozen" : "damage", dmg: hit.applied }); if (freeze) { t.frozen = true; killFoe(state, t, rng, events); if (t.alive) t.frozen = false; return; } if (t.wp <= 0) killFoe(state, t, rng, events);` else `events.push({ type: "allySpellMissed", ...base, resisted: false });`
      - otherwise (status / stun / weaken — the only other ATTACK_SPELL_KINDS): `events.push({ type: "allyCast", ...base }); const res = resistRoll(rng, t.intel); if (res.rolled && res.resisted) { events.push({ type: "allySpellMissed", ...base, resisted: true, roll: res.roll }); return; }` then weaken: `if (C) { C.weakened = true; C.foeToHitPenalty = 3; } events.push({ type: "allySpellHit", ...base, effect: "weakened" });` else (status/stun): `t.asleep = Math.max(t.asleep || 0, rng.d(4)); events.push({ type: "allySpellHit", ...base, effect: "asleep", rounds: t.asleep });`
      Doc-comment: self-contained because magic.js imports combat.js (a back-import would be a cycle); mirrors the hero's dice shapes (d8 vs 4 / Freeze d10 vs 6 + school bonus; level multiplier; spell damage bypasses armor; Freeze kills through killFoe with the kill-twice unfreeze; intel >= 12 resists non-thrown casts); single target = the hero's current live target; the hero's castSpell is untouched. Every literal event type stays a plain string at its push site (the coverage guards enumerate engine/*.js by `type: "..."`).

    `engine/movement.js` newDay: inside the existing `if (state.party?.length) { for (const m of state.party) { cost += upkeep(m); } }` loop add `if (m.spellsUsed) m.spellsUsed = 0;` with a comment (DFB-05: a member's charges recover per day like the hero's `c.spellsUsed = 0` above; conditional so a sheet without the field never gains one).

    Tests — `test/unit/party-combat.test.js` (append; reuse its helpers; add a `classedMember(overrides)` helper = `fixedMember({ cls: "Fighter", sub: "Knight", race: "Human", weapon: "Club", prof: 2, magicWpn: 0, might: 0, items: [], skills: {}, armor: "Studded", grimoire: [], spellsUsed: 0, ...overrides })`; foes are `fixedFoe({ type: "Humans", wp: 30, maxWP: 30 })` unless stated; import `bestAttackSpell`, `memberToHit` from derived.js and `newDay` from movement.js):
    1. "DFB-05 Fighter: strikes with its weapon on the class to-hit — Knight + Club + prof 2, roll 3 hits (need 5), d6 4 -> 7": fakeRng([3, 4]) → allyStruck `{ name, target, dmg: 7, weapon: "Club" }` with no `crit`/`backstab` keys; foe wp 23.
    2. "DFB-05 Fighter: a natural 1 is a critical (x2, crit: true) and bypasses the armor soak draw": fakeRng([1, 4]) → dmg 14, `crit === true`; with a foe carrying `sp: { ar: 10 }` the same sequence still lands 14 with NO extra d20 (fakeRng of length 2 proves it).
    3. "DFB-05 Fighter: a Guard or Soldier never crits": `classedMember({ sub: "Soldier" })`, fakeRng([1, 4]) → dmg 7, no crit key.
    4. "DFB-05 to-hit: memberToHit follows class, race floor, Acrobat, Cleric": Fighter/Human 5; Magic User/Human 3; Thief/Human 4; Thief/Elven 5; Magic User sub Cleric 4; Thief sub Acrobat 5; `{}` (no class) 5.
    5. "DFB-05 Thief: the first landed blow is a backstab (x2, backstab + crit flags), the next is normal": `classedMember({ cls: "Thief", sub: "Pickpocket", weapon: "Dagger", prof: 0, armor: "Leather" })`, fakeRng([4, 4]) → need 4 hit; Dagger halves d6 4 -> 2; dmg (1 + 2) * 2 = 6, `backstab === true`, `crit === true`, `state.combat.allies[0].backstabUsed === true`; a second `alliesTurn` with fakeRng([4, 4]) → dmg 3 and no backstab key.
    6. "DFB-05 Thief: a missed opener keeps the backstab for the next landed blow": fakeRng([9]) → allyMissed `{ target, roll: 9, need: 4, weapon: "Dagger" }`, `backstabUsed` falsy; then fakeRng([4, 4]) → 6 with backstab.
    7. "DFB-05 Thief: Plate armor denies the backstab (hero's rule mirrored)": `armor: "Plate"`, fakeRng([4, 4]) → dmg 3, no backstab key.
    8. "DFB-05 Magic User: casts Freeze at the hero's current target — d10 5 with bonus 3 hits need 6, d6 4 damage, the target is frozen and killed through killFoe, the SHEET pays the charge": `classedMember({ cls: "Magic User", sub: "Wizard", weapon: "Quarter Staff", grimoire: ["Freeze"], spellsUsed: 0 })`, foe type Humans; fakeRng([5, 4, 4, 1, 20]) (cast roll, d6 damage, then killFoe's d6 sp / d10 coin / d20 treasure exactly as the existing afterPlayerAction test documents) → events in order contain allyCast `{ roll: 5, need: 6, bonus: 3 }` then allySpellHit `{ effect: "frozen", dmg: 4 }`; `foe.alive === false`; `state.party[0].spellsUsed === 1`; no allyStruck.
    9. "DFB-05 Magic User: a Freeze miss (d10 10 - 3 > 6) is allySpellMissed { resisted: false } and still spends the charge": fakeRng([10]) → foe wp 30, spellsUsed 1.
    10. "DFB-05 Magic User: Doze on a dim foe draws no resist roll and puts it to sleep d4": grimoire ["Doze"], foe intel 1, fakeRng([3]) → allyCast without roll, allySpellHit `{ effect: "asleep", rounds: 3 }`, `foe.asleep === 3`.
    11. "DFB-05 Magic User: an intelligent foe can resist Doze (d20 below its intel) -> allySpellMissed { resisted: true }": foe intel 15, fakeRng([10]) → resisted, `foe.asleep === 0`, spellsUsed 1.
    12. "DFB-05 Magic User: no charge left -> staff swing on the Magic User to-hit (3)": grimoire ["Freeze"], spellsUsed 4 (maxCharges at level 1 is 4); fakeRng([2, 5]) → allyStruck `{ dmg: 6, weapon: "Quarter Staff" }`, no allyCast event; fakeRng([4]) alone → allyMissed (4 > 3).
    13. "DFB-05 Magic User: an attack spell above the member's level is not castable -> staff swing": grimoire ["Fireball"] at level 1 → `bestAttackSpell({ c: view })` is null (call it on the member sheet with level 1) and fakeRng([2, 5]) → allyStruck.
    14. "DFB-05 bestAttackSpell: highest effective level wins, thrown beats status at equal level, none -> null": `{ c: { cls: "Magic User", sub: "Wizard", level: 1, grimoire: ["Doze", "Freeze"] } }` → Freeze; `["Doze"]` → Doze; `[]` → null; `{ level: 3, grimoire: ["Freeze", "Fireball"] }` → Fireball.
    15. "DFB-05 legacy fallback: an allies entry with no persistent sheet strikes exactly as before": `fixedState({ party: [] })` with `allies: [fixedAlly()]`, fakeRng([3, 4]) → allyStruck `{ dmg: 5 }` with no `weapon` key; fakeRng([9]) → allyMissed with only `type`/`name` keys (deepEqual `{ type: "allyMissed", name: "Ada" }`).
    16. "DFB-05 gate: an absent C.allies still draws ZERO rng" (fakeRng([]) does not throw — keep beside the existing gate test).
    17. "DFB-05 newDay: member spell charges reset with the day; a sheet without the field never gains one": `fixedState({ c: { rations: 6 }, party: [classedMember({ cls: "Magic User", sub: "Wizard", spellsUsed: 2 })] })` + the existing safe newDay rng → `state.party[0].spellsUsed === 0`; a member built without `spellsUsed` (delete the key) → `"spellsUsed" in state.party[0]` is false after newDay.
    `test/unit/joiner-acquisition.test.js` — in "integration: a recruited joiner syncs into C.allies and lands a strike", pin the pending sheet explicitly: `fixedPending({ name: "Ada", cls: "Fighter", sub: "Soldier", race: "Human", weapon: "Club", prof: 0, magicWpn: 0, might: 0, items: [], skills: {}, grimoire: [], lvl: 1, level: 1, wp: 20, maxWP: 20 })` and update its comment (Fighter to-hit 5, roll 3 hits, Club d6 4 -> 1 + 4 = 5; the seeded sheet is a Magic User and would cast under DFB-05).
    Run `node --test test/unit/party-combat.test.js test/unit/joiner-acquisition.test.js test/unit/combat.test.js test/unit/movement.test.js test/unit/identity-contract.test.js test/unit/identity-combat.test.js test/unit/foe-turn-draw-count.test.js test/unit/freeze-pays-out.test.js test/determinism/rng-no-math-random.test.js` — green. Commit: `feat(25.1-03): party members fight by class — weapon strikes, Thief backstab, Magic User allyCast` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/party-combat.test.js test/unit/joiner-acquisition.test.js test/unit/combat.test.js test/unit/movement.test.js test/unit/identity-contract.test.js test/unit/identity-combat.test.js test/unit/foe-turn-draw-count.test.js test/unit/freeze-pays-out.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/party-combat.test.js` exits 0 with `# fail 0` and `# pass` at least 17 higher than before this task; `node --test test/unit/joiner-acquisition.test.js test/unit/combat.test.js test/unit/movement.test.js test/unit/identity-contract.test.js` exits 0 with `# fail 0`.
    - `git diff --quiet HEAD -- engine/magic.js; echo rc=$?` prints `rc=0` (the hero's cast path is untouched) and `grep -v '^\s*//' engine/combat.js | grep -c 'from "./magic.js"'` prints `0`.
    - `grep -v '^\s*//' engine/derived.js | grep -c "export function memberToHit\|export function bestAttackSpell"` prints `2`.
    - `grep -v '^\s*//' engine/combat.js | grep -c 'type: "allyCast"'` prints a number >= 1, `grep -v '^\s*//' engine/combat.js | grep -c 'type: "allySpellHit"'` prints a number >= 1, and `grep -v '^\s*//' engine/combat.js | grep -c 'type: "allySpellMissed"'` prints a number >= 1 (literal string types at the push sites, so the coverage guards can enumerate them).
    - `node -e "import('./engine/combat.js').then(async ({alliesTurn})=>{const foe={name:'Goblin',type:'Humans',lvl:1,size:'S',intel:1,wp:30,maxWP:30,alive:true,asleep:0,sp:{},lives:1};const st={c:{cls:'Fighter',sub:'Soldier',race:'Human',level:1,wp:55,maxWP:55,items:[],skills:{},kills:0,sp:0,gold:0,rations:6},floor:{g:[[{}]],px:0,py:0,depth:1},party:[{name:'Ysolde',cls:'Magic User',sub:'Wizard',race:'Human',level:1,weapon:'Quarter Staff',prof:0,magicWpn:0,items:[],skills:{},grimoire:['Doze'],spellsUsed:0,wp:20,maxWP:20}],combat:{foes:[foe],type:'Humans',round:1,target:0,allies:[{partyIdx:0,name:'Ysolde',lvl:1,sub:'Wizard',wp:20,maxWP:20}]}};const seq=[3];let i=0;const ev=alliesTurn(st,{d(){if(i>=seq.length)throw new Error('extra draw');return seq[i++]}},[]);console.log(ev.map(e=>e.type).join(','),foe.asleep,st.party[0].spellsUsed)})"` prints `allyCast,allySpellHit 3 1`.
    - `grep -v '^\s*//' engine/combat.js | grep -c "ally.backstabUsed = true"` prints `1` and `grep -v '^\s*//' engine/combat.js | grep -c "state.party\[ally.partyIdx\]"` prints a number >= 1.
    - `node -e "import('./engine/movement.js').then(({newDay})=>{const st={c:{cls:'Fighter',sub:'Soldier',race:'Human',level:1,wp:55,maxWP:55,rations:6,items:[],skills:{},spellsUsed:0,might:0,armorWP:0,armorMax:0,affliction:null,potions:0},floor:{g:[[{}]],px:0,py:0,depth:1},party:[{name:'Ysolde',cls:'Magic User',sub:'Wizard',race:'Human',level:1,items:[],skills:{},spellsUsed:2,wp:20,maxWP:20},{name:'Bram',cls:'Fighter',sub:'Knight',race:'Human',level:1,items:[],skills:{},wp:20,maxWP:20}],day:1,combat:null};const seq=[5,2,2,2,2,2,2,2,2];let i=0;newDay(st,false,{d(){return seq[i++]}},[]);console.log(st.party[0].spellsUsed, 'spellsUsed' in st.party[1])})"` prints `0 false` (a member's charges reset with the day; a sheet without the field never gains one).
    - `node --test "test/parity/**/*.test.js" 2>&1 | grep -E "^# (pass|fail)"` shows `# fail 0`; `git status --porcelain test/parity` prints nothing.
    - `git log -1 --format=%s` starts with `feat(25.1-03)`.
  </acceptance_criteria>
  <done>A Fighter member swings its weapon, a Thief member opens with a backstab, a Magic User member casts its best attack spell on its own charges and falls back to the staff — every draw behind the party gate, the hero's paths untouched, parity byte-identical, and the class behaviors pinned by exact-draw tests.</done>
</task>

<task type="auto">
  <name>Task 2: Narration + toasts for the ally events — allyCast (Oracle-only), allySpellHit/allySpellMissed, enriched allyStruck/allyMissed, FEATURE_EVENTS; coverage, toast, and voice tests</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, test/unit/party-combat.test.js</files>
  <read_first>
    - src/browser/eventNarration.js lines 280–295 (allyStruck/allyMissed/allyDeparted/memberStruck entries) and 300–330 (the hero's spellThrown/spellHit/spellMissed/frozenSolid/dozed entries — the register to match)
    - src/browser/toasts.js lines 58–82 (ORACLE_ONLY — add allyCast with a reason), 91–155 (FEATURE_EVENTS), 226–230 (CRIT_SUFFIX), 876–882 (allyStruck/allyMissed builders), 444–540 (spellChain — read only to confirm no fold is needed for the ally events: allyCast is Oracle-only, so one cast yields one toast)
    - test/unit/toastsCoverage.test.js in full (partition, ORACLE_ONLY reasons/size band, FEATURE_EVENTS subset + non-empty bare-call text) and test/unit/formatEventsCoverage.test.js lines 80–140 (bare `{type}` invocation of every narration builder — no throw, non-empty)
    - test/voice/safety-scan.test.js lines 150–200 (EVENT_NARRATION + TOAST_FOR branch/token scanning — new builders are auto-covered)
  </read_first>
  <action>
    `src/browser/eventNarration.js` (deadpan, family-friendly; every field defended):
    - Replace `allyStruck`: `(e) => { const who = e.name ?? "Your ally"; const t = e.target ?? "it"; const w = e.weapon ? ` with a ${e.weapon}` : ""; if (e.backstab) return `<span class="hit">${who} backstabs ${t}${w}</span> — <span class="roll">${e.dmg ?? 0}</span> hp. A blade in the back, as advertised.`; return `${who} lands a hit on ${t}${w} for <span class="roll">${e.dmg ?? 0}</span> hp.${e.crit ? ` <span class="hit">Critical.</span>` : ""}`; }` — with no new fields the output is byte-identical to today's line (assert in a test).
    - Replace `allyMissed`: `(e) => e.target ? `${e.name ?? "Your ally"} swings${e.weapon ? ` a ${e.weapon}` : ""} at ${e.target} and misses.${e.roll != null ? ` <span class="roll">${e.roll} vs ${e.need ?? "?"}.</span>` : ""}` : `${e.name ?? "Your ally"} swings and misses.``
    - Add `allyCast: (e) => `${e.name ?? "Your ally"} casts <span class="hit">${e.spell ?? "a spell"}</span> at ${e.target ?? "the nearest foe"}.${e.roll != null ? ` <span class="roll">${e.roll} vs ${e.need ?? "?"}${e.bonus ? ` (+${e.bonus})` : ""}.</span>` : ""}``
    - Add `allySpellHit: (e) => { const who = e.name ?? "Your ally"; const sp = e.spell ?? "The spell"; const t = e.target ?? "the foe"; if (e.effect === "frozen") return `<span class="hit">${who}'s ${sp} — ${t} frozen solid.</span> It will keep.`; if (e.effect === "asleep") return `<span class="hit">${who}'s ${sp} — ${t} nods off.</span> <span class="roll">${e.rounds ?? "?"} rounds.</span>`; if (e.effect === "weakened") return `<span class="hit">${who}'s ${sp} — the foes' arms go soft.</span>`; return `<span class="hit">${who}'s ${sp} hits ${t}</span> for <span class="roll">${e.dmg ?? 0}</span> hp.`; }`
    - Add `allySpellMissed: (e) => e.resisted ? `<span class="miss">${e.target ?? "The foe"} shrugs off ${e.name ?? "your ally"}'s ${e.spell ?? "spell"}.</span>${e.roll != null ? ` <span class="roll">${e.roll} against its wits.</span>` : ""}` : `<span class="miss">${e.name ?? "Your ally"}'s ${e.spell ?? "spell"} goes wide of ${e.target ?? "the foe"}.</span> The maze absorbs the effort without comment.``

    `src/browser/toasts.js`:
    - ORACLE_ONLY: add `"allyCast", // the allySpellHit/allySpellMissed sibling that always follows in the same action toasts the outcome and names the spell (one toast per cast)`.
    - FEATURE_EVENTS: add `"allyStruck", "allyMissed", "allySpellHit", "allySpellMissed"` (class behavior firing for a member).
    - Replace `allyStruck`: `(e) => { const who = e?.name ?? "Your ally"; const t = e?.target ?? "it"; const text = e?.backstab ? `${who} backstabs ${t} (${e?.dmg ?? 0})` : `${who} lands a hit on ${t} (${e?.dmg ?? 0}).${e?.crit ? CRIT_SUFFIX : ""}`; return { text, tone: "hit", priority: PRIORITY.feature }; }` (legacy text byte-identical without flags).
    - Replace `allyMissed`: `(e) => ({ text: e?.target ? `${e?.name ?? "Your ally"} misses ${e.target}` : `${e?.name ?? "Your ally"} swings and misses.`, tone: "miss", priority: PRIORITY.feature })`.
    - Add `allySpellHit: (e) => { const who = e?.name ?? "Your ally"; const sp = e?.spell ?? "a spell"; const t = e?.target ?? "it"; const tail = e?.effect === "frozen" ? `${t} frozen solid` : e?.effect === "asleep" ? `${t} nods off` : e?.effect === "weakened" ? "the foes weaken" : `${t} (${e?.dmg ?? 0})`; return { text: `${who} casts ${sp} — ${tail}`, tone: "magic", priority: PRIORITY.feature }; }` and `allySpellMissed: (e) => ({ text: `${e?.name ?? "Your ally"} casts ${e?.spell ?? "a spell"} — ${e?.resisted ? `${e?.target ?? "it"} resists` : `misses ${e?.target ?? "it"}`}`, tone: "miss", priority: PRIORITY.feature })`.

    Tests — append to `test/unit/party-combat.test.js` (import `EVENT_NARRATION` and `TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, toastsForAction` from the two presentation modules — the existing unit-test pattern):
    1. "DFB-05 narration: legacy allyStruck/allyMissed lines are byte-identical without the new fields": `EVENT_NARRATION.allyStruck({ type: "allyStruck", name: "Ada", target: "Dante", dmg: 5 })` === `Ada lands a hit on Dante for <span class="roll">5</span> hp.` and `EVENT_NARRATION.allyMissed({ type: "allyMissed", name: "Ada" })` === `Ada swings and misses.`; the toasts likewise equal `Ada lands a hit on Dante (5).` and `Ada swings and misses.`.
    2. "DFB-05 narration + toast: backstab, crit and weapon render": backstab event → narration contains `backstabs Dante with a Dagger` and toast text === `Bram backstabs Dante (14)`; crit event → narration contains `Critical.` and toast ends with ` · CRIT`.
    3. "DFB-05 toast: a cast is exactly one toast naming the spell and the outcome": `toastsForAction("attack", [{ type: "allyCast", name: "Ysolde", spell: "Freeze", target: "Goblin", roll: 5, need: 6, bonus: 3 }, { type: "allySpellHit", name: "Ysolde", spell: "Freeze", target: "Goblin", effect: "frozen", dmg: 4 }], {})` → length 1, text `Ysolde casts Freeze — Goblin frozen solid`, tone magic; the resisted pair → `Ysolde casts Doze — Goblin resists` (tone miss); the wide pair → `Ysolde casts Fireball — misses Goblin`; the damage pair → `Ysolde casts Fireball — Goblin (12)`.
    4. "DFB-05 coverage: allyCast is Oracle-only, the other four ally events are FEATURE_EVENTS with narration and toast, and none throws on a bare payload": `ORACLE_ONLY.has("allyCast")`, `typeof EVENT_NARRATION.allyCast === "function"`; for each of allyStruck/allyMissed/allySpellHit/allySpellMissed: in FEATURE_EVENTS, `TOAST_FOR[t]({ type: t }).text` non-empty, `EVENT_NARRATION[t]({ type: t })` non-empty and contains neither `undefined` nor `NaN`.
    Run `node --test test/unit/party-combat.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/narrativeToasts.test.js test/voice/safety-scan.test.js` — green. Commit: `feat(25.1-03): narrate and toast ally casts, backstabs and crits` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/party-combat.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/narrativeToasts.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/voice/safety-scan.test.js` exits 0 with `# fail 0` (every new event narrated and toasted or Oracle-only; no dead entries; FEATURE_EVENTS honest; copy family-friendly).
    - `node -e "import('./src/browser/toasts.js').then(m=>console.log(m.ORACLE_ONLY.has('allyCast'), ['allyStruck','allyMissed','allySpellHit','allySpellMissed'].every(t=>m.FEATURE_EVENTS.includes(t)&&typeof m.TOAST_FOR[t]==='function'), m.toastsForAction('attack',[{type:'allyCast',name:'Ysolde',spell:'Freeze',target:'Goblin'},{type:'allySpellHit',name:'Ysolde',spell:'Freeze',target:'Goblin',effect:'frozen',dmg:4}],{}).map(t=>t.text).join('|')))"` prints `true true Ysolde casts Freeze — Goblin frozen solid`.
    - `node -e "import('./src/browser/eventNarration.js').then(m=>console.log(m.EVENT_NARRATION.allyStruck({type:'allyStruck',name:'Ada',target:'Dante',dmg:5})))"` prints `Ada lands a hit on Dante for <span class="roll">5</span> hp.` (legacy line byte-identical).
    - `node -e "import('./src/browser/toasts.js').then(m=>console.log(m.TOAST_FOR.allyStruck({type:'allyStruck',name:'Bram',target:'Dante',dmg:14,backstab:true,crit:true,weapon:'Dagger'}).text))"` prints `Bram backstabs Dante (14)`.
    - `git status --porcelain engine test/parity mazeworld.html` prints nothing (this task touches only the two presentation modules and the test).
    - `git log -1 --format=%s` starts with `feat(25.1-03)`.
  </acceptance_criteria>
  <done>Ally combat reads like the hero's in both the Oracle and the toasts — one toast per cast naming the spell and the outcome, backstabs and crits called out — and every Phase 25 guard confirms nothing new fires silently.</done>
</task>

<task type="auto">
  <name>Task 3: Gate — parity 33/33, full suite, tuning-bot --party smoke, build</name>
  <files>engine/combat.js</files>
  <read_first>
    - tools/tune-difficulty.mjs lines 15–30 (CLI flags: `--seeds=N`, `--party`) and tools/lib/tuning-bot.mjs lines 632–648 (forceParty: meetJoiner + resolveJoiner on a fresh state — the bot's member fights the whole run through the new alliesTurn)
    - test/parity/README.md (how to run the 33-file suite and read a divergence)
  </read_first>
  <action>
    Run, in this order, and record each result for the SUMMARY:
    1. `node --test "test/parity/**/*.test.js" 2>&1 | tail -6` — 33 files, `# fail 0`; then `git status --porcelain test/parity` (must be empty) and `git diff --quiet HEAD -- test/parity/prototype-master.js.txt; echo rc=$?` (rc=0). If any parity test diverges, the ONLY acceptable fix is inside alliesTurn/allyCast (a draw leaking outside the C.allies gate) — never a fixture edit.
    2. `npm test 2>&1 | tail -8` — `# fail 0`, `# pass` >= 1376 + 17 + 4 = 1397 (Plan 02's floor plus this plan's party-combat additions).
    3. `timeout 300 node tools/tune-difficulty.mjs --seeds=12 --party 2>&1 | tail -20; echo rc=$?` — exits 0 and prints its report (the `Bot:` line shows `party=on`); this proves class-based allies throw nothing across real seeded runs (including Magic User members casting and Thief members backstabbing). The numbers are NOT a gate (tuning proxy, never a CI assertion — Phase 22 rule); do not tune anything.
    4. `npm run build:www` — exit 0.
    5. `node --test test/determinism/rng-no-math-random.test.js test/unit/tuning-bot.test.js` — green (no Math.random slipped into combat.js; the bot's own tests still pass with the new ally events).
    If everything is green and the tree is clean, no commit is needed for this task (record the outputs in the SUMMARY). If step 1 or 2 required a fix inside combat.js, commit it as `fix(25.1-03): keep every ally draw behind the party gate` (with the attribution lines) and re-run steps 1–5.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" 2>&1 | tail -4 && npm test 2>&1 | tail -8 && timeout 300 node tools/tune-difficulty.mjs --seeds=12 --party 2>&1 | tail -5 && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "test/parity/**/*.test.js" 2>&1 | grep -E "^# (pass|fail)"` shows `# fail 0`; `git status --porcelain test/parity` prints nothing; `git diff --quiet HEAD -- test/parity/prototype-master.js.txt; echo rc=$?` prints `rc=0`.
    - `npm test 2>&1 | tail -8` shows `# fail 0` and `# pass` >= 1397.
    - `timeout 300 node tools/tune-difficulty.mjs --seeds=12 --party >/dev/null 2>&1; echo rc=$?` prints `rc=0`.
    - `npm run build:www` exits 0.
    - `git diff --quiet HEAD -- engine/magic.js engine/foeDamage.js tools/lib/tuning-bot.mjs; echo rc=$?` prints `rc=0`.
    - `git status --porcelain` prints nothing (clean tree at the end of the phase's code work).
  </acceptance_criteria>
  <done>Solo parity is byte-identical, the whole suite is green, a real party bot run completes without throwing, the www bundle builds, and nothing outside the planned files moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persistent member sheet -> combat | the sheet is read live each swing; only spellsUsed is written (the charge) |
| solo fixtures vs party runs | every new draw must stay behind the C.allies gate |
| combat.js <-> magic.js | no back-import (cycle); the ally cast is a mirror, not a call |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-12 | Tampering | a draw leaking outside the party gate | critical | mitigate | all new code lives inside alliesTurn's existing gate; fakeRng([]) gate test; parity 33/33 run as an explicit task |
| T-25.1-13 | Tampering | hero cast/strike drift while mirroring | high | mitigate | engine/magic.js untouched (git diff gate); playerStrike not edited; combat.js never imports magic.js |
| T-25.1-14 | Denial of Service | NaN damage from a sparse member sheet | medium | mitigate | memberView defaults prof/magicWpn/might/items/skills/grimoire/spellsUsed; sheets without cls/race take the legacy path; weaponless sheets use weaponDamage's Club fallback (tested) |
| T-25.1-15 | Tampering | new serialized field on the member sheet | high | mitigate | backstabUsed lives on the transient C.allies entry; spellsUsed reset is conditional; key-set assertions in tests |
| T-25.1-16 | Repudiation | ally combat firing silently | medium | mitigate | allyCast Oracle-only with an always-following toasted sibling; four ally events in FEATURE_EVENTS; coverage guards + voice scan |
| T-25.1-17 | Denial of Service | infinite/unbounded ally loop | low | mitigate | the bounded snapshot loop, wp<=0 skip and liveFoes break are unchanged; bot smoke over 12 seeds |
| T-25.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/party-combat.test.js` green with the 21 new class/narration tests; `npm test` `# fail 0`, `# pass` >= 1397.
- `node --test "test/parity/**/*.test.js"` 33 files green, zero fixture changes, `test/parity/prototype-master.js.txt` untouched; `engine/magic.js` untouched.
- `node tools/tune-difficulty.mjs --seeds=12 --party` exits 0; `npm run build:www` exits 0.
- Device acceptance (non-blocking): with a Magic User Joiner, the fight shows "Ysolde casts Freeze — Goblin frozen solid"; with a Thief Joiner the first landed blow toasts "Bram backstabs …"; a Fighter Joiner's hits name its weapon in the Oracle.
</verification>

<success_criteria>
- DFB-05: Fighter weapon/to-hit/crit, Thief one backstab per combat, Magic User best-attack-spell cast on its own charges with staff fallback; new events narrated + toasted + in FEATURE_EVENTS; every draw behind the party gate; solo parity byte-identical.
</success_criteria>

<output>
Create `.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-03-SUMMARY.md` when done. Record: the exact event shapes (allyCast/allySpellHit/allySpellMissed/allyStruck/allyMissed) as committed, the bestAttackSpell tie rule, the memberToHit table, the parity result, the bot smoke `Bot:` line, test counts, `npm test` pass count, and the on-device checklist above for the orchestrator's phone-push offer.

Commit message footer for every commit in this plan:
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS
</output>
