---
phase: 48-stale-docs-comments-test-names-purge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/stale-terms.mjs
  - tools/comment-only-diff.mjs
  - tools/ident-sweep.mjs
  - engine/character.js
  - engine/movement.js
  - engine/effects.js
  - engine/items.js
  - engine/derived.js
  - engine/combat.js
  - content/treasure-tables.js
  - content/foe-abilities.js
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "DOCS-01 (measure): `node tools/stale-terms.mjs` prints the ROADMAP criterion-1 grep as one markdown table (one row per term: total hits / allowed survivors / unlisted) over mazeworld.html, src/, engine/, content/, tools/ and test/ — the same regexes the criterion names, plus the supplementary rows (Round Card, retired __mz bridge names, renamed/deleted file names) — and exits 1 while any enforced row has an unlisted hit or any allow-list entry matches nothing"
    - "DOCS-01 (engine fence): every engine/ and content/ file this plan touches differs from HEAD 649de2b on comment lines only — `node tools/comment-only-diff.mjs 649de2b engine content` prints `comment-only:` for each changed file and exits 0"
    - "DOCS-01 (engine/content/tools sweep): after this plan, `node tools/stale-terms.mjs --paths engine content tools` reports unlisted 0 on every enforced row — the only engine survivors are engine/saveState.js#foldLegacyCounters's reads and doc comment (tolerant-load legacy keys), each listed in ALLOWED with a reason"
    - "Behaviour: `npm test` pass 3288 / fail 0 at every commit; `git hash-object test/parity/prototype-master.js.txt` stays a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat 649de2b -- test/parity/fixtures` is empty"
  artifacts:
    - "tools/stale-terms.mjs — TERMS (10 rows), ALLOWED (seeded with the class-C tolerant-load survivors), scan()/renderTable()/defaultSources()/unusedAllowEntries(), CLI with --paths / --json / --self-test"
    - "tools/comment-only-diff.mjs — the stripped-diff proof: strips comments on both sides of every changed .js/.mjs/.html file under the given prefixes and reports comment-only vs CODE CHANGED"
    - "engine/*.js + content/*.js — 15 stale comment sites rewritten or deleted (list in the action), zero code-line changes"
  key_links:
    - "tools/stale-terms.mjs -> nothing in src/ (a standalone scanner over raw lines; it does NOT strip comments because comments are the subject)"
    - "tools/comment-only-diff.mjs -> tools/ident-sweep.mjs#stripJs/stripHtml (the same stripper Phase 47's bridge-registry test uses, so 'comment' means the same thing everywhere)"
    - "Plans 02-05 -> tools/stale-terms.mjs: each later plan appends its own ALLOWED entries and re-runs the scoped scan; Plan 05 pins the final table with test/unit/stale-terms.test.js"
  prohibitions:
    - statement: "MUST NOT change any non-comment byte under engine/, content/, test/parity/fixtures/ — the stripped diff is the proof, `npm test` 3288/0 is the second proof"
      requirement_id: DOCS-01
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT edit test/parity/prototype-master.js.txt (hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0)"
      requirement_id: DOCS-01
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT purge comment volume — only a sentence that describes a retired pattern as live is rewritten or deleted; a comment that explains a retirement and still helps a reader stays"
      requirement_id: DOCS-01
      category: values
      status: unverified
      verification: judgment
---

<objective>
Build the phase's measuring stick — `tools/stale-terms.mjs`, which prints ROADMAP Phase 48 criterion 1's grep list as a reproducible table with an explicit allow-list of survivors — and `tools/comment-only-diff.mjs`, the stripped-comments proof criterion 4 demands. Then run the smallest slice of the sweep with it: the 15 stale comment sites in `engine/` and `content/` (comment lines only, proven by the stripped diff) and the two stale lines in `tools/ident-sweep.mjs`.

Purpose: DOCS-01 needs a recorded grep list "over the final layout"; making that grep a tool with teeth means Plans 02-05 measure themselves against the same regexes and the closing SUMMARY pastes one table instead of hand-run greps. Landing the engine slice first (the only slice with the engine-gate stripped-diff obligation) proves the proof tool on the smallest possible diff.

Output: `tools/stale-terms.mjs`, `tools/comment-only-diff.mjs`, comment-only edits in six `engine/` files, two `content/` files, and `tools/ident-sweep.mjs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-stale-docs-comments-test-names-purge/48-CONTEXT.md
@tools/ident-sweep.mjs
</context>

<!-- planner-discipline-allow: toast -->
<!-- planner-discipline-allow: toastsCoverage -->
<!-- planner-discipline-allow: parley-button-mirror -->
<!-- planner-discipline-allow: dpad -->
<!-- planner-discipline-allow: Phase 48's -->
<!-- planner-discipline-allow: flightLeft -->
<!-- planner-discipline-allow: flightCooldown -->
<!-- planner-discipline-allow: c.ether -->
<!-- planner-discipline-allow: retired -->

<phase_ground_rules>
These rules apply to every task in every Phase 48 plan; they are restated verbatim in each plan.

- **Engine-gate fence (every commit):** `engine/`, `content/` and `test/parity/fixtures/` change on COMMENT LINES ONLY, proven by a comment-stripped diff (`node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` — every changed file reports `comment-only:`; fixtures are JSON and must not appear at all). `test/parity/prototype-master.js.txt` is never edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **Test invariant:** `npm test` pass count stays exactly **3,288** / fail 0 at every commit of Plans 01-04 (renames only; no test added or deleted — a test that must go needs its reason in the SUMMARY). Plan 05 adds the tripwire's own tests and records the new count as "3,288 + N".
- **Phase 47 DOM-snapshot smoke:** `node --test test/unit/shell-tab-snapshots.test.js` stays 10/10 with `git diff --stat -- test/unit/fixtures/` empty — a comment edit cannot move it, so a fixture diff means a code line was touched.
- **Never `git add -A`.** Stage by path. Never touch `icons/` or `sfx/`. `.planning/` is history — out of scope for the sweep (SUMMARY/STATE/REQUIREMENTS updates at plan close are the normal exceptions).
- **No wholesale comment-volume purge** — Phase 47's line-budget ruling closed that. This phase removes stale *meaning*, not comment volume. A comment that explains a retirement by name ("was a toast; the rail owns feedback since Phase 35") stays when it still helps a reader — it must not describe the retired thing as live.
- **Judgment call 1 (CONTEXT, user-reversible):** `classic script` is a LIVE term — it names `mazeworld.html`'s non-module `<script>` block. It stays; the tool reports it as an informational row. A comment that says the classic script *is an engine*, *mirrors the engine*, or names a specific classic `X()` duplicate as if it still existed IS stale and goes.
- **Judgment call 2 (CONTEXT, user-reversible):** allowed survivors are (A) tests/lines that ASSERT a retirement (the Phase 35 invariant suite, `assert.equal(... , 0)`-style pins and their titles), (B) prose retirement statements that still help a reader, (C) tolerant-load legacy-key reads and their pins (`engine/saveState.js#foldLegacyCounters`, the parity comparables' prototype-side strips, `test/unit/item-activation.test.js`'s fold pins, `save-validation.test.js`'s OLD-SAVE inputs), (D) inert legacy keys inside hand-built test-state literals (`flightLeft: 0, flightCooldown: 0` / `won: false` in `fixedState()`-style objects) — Plan 04's last task removes these in one revertable commit (the 46-02 SUMMARY handed the `won: false` list to this phase); until then they are counted, listed, and not edited. Every survivor is an `ALLOWED` entry in `tools/stale-terms.mjs` with file + match + reason — nothing else survives.
- **Windows executor environment:** Git Bash, no `jq`; `mazeworld.html` is CRLF in the working copy (split lines on `/\r?\n/`); heredocs and `node -e` one-liners mangle backslashes — write any regex-heavy script to a file with the Write tool (scratchpad or the tool file itself), never a heredoc.
- **Line numbers drift:** anchor by the quoted text; numbers below were measured at HEAD `649de2b`.
- **Verification agents are off:** the acceptance criteria ARE the verification — every `<verify>` command is literal, with its expected output.
- **Test bodies: the bounded rule.** The CONTEXT says "test bodies and assertions are not touched (a body that needs changing is a behaviour question — flag it, don't fix it here)". This phase reads that as: no expected value, condition, fixture literal, event type or field name changes. Two edits are NOT behaviour questions and ARE in scope (Plans 03-04): (i) renaming a local identifier whose name contains `toast`/`Toast`/`TOAST` to its `line`/`Line`/`LINE` equivalent, consistently within its scope; (ii) rewording an assertion MESSAGE string. A test TITLE is not a body: rename it. A comment inside a body is a comment: rewrite it. If a rename or comment rewrite trips another test that pins the old text: reword so the pinned substring survives when it is not itself stale; if it IS the stale text, revert and list the line as a survivor with the pinning test's name — never edit an expected value.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: tools/stale-terms.mjs (the criterion-1 table with an allow-list and teeth) + tools/comment-only-diff.mjs (the stripped-diff proof) + the two stale lines in tools/ident-sweep.mjs</name>
  <files>tools/stale-terms.mjs, tools/comment-only-diff.mjs, tools/ident-sweep.mjs</files>
  <read_first>
    - tools/ident-sweep.mjs (whole file: `stripJs`/`stripHtml` exports, `collectFiles`/`walk`, the direct-invocation guard at the bottom, the head-comment Usage block — copy the CLI/guard shape; note its head comment's line 8-10 sentence about "Phase 48's docs/comments sweep" written in the future tense and its `"dpad"` string-literal example — both are this task's rewrite targets)
    - tools/bridge-doc.mjs (the `--check` / `--write` CLI shape and the `path.resolve(process.argv[1]) === url.fileURLToPath(import.meta.url)` guard)
    - .planning/phases/48-stale-docs-comments-test-names-purge/48-CONTEXT.md `<domain>` — the baseline table these rows reproduce (measured at 1a4193f; re-measure, do not copy)
    - .planning/ROADMAP.md Phase 48 criterion 1 (the regex list, verbatim) and criterion 4 (the stripped-diff proof)
  </read_first>
  <action>
    1. **tools/stale-terms.mjs** — node built-ins only (`node:fs`, `node:path`, `node:url`, `node:child_process` not needed). Head comment: Phase 48 (DOCS-01) standing tripwire; what it scans (RAW lines — comments and strings are the subject, so it deliberately does NOT strip comments, unlike ident-sweep.mjs); the survivor classes A-D from the ground rules; how to add an `ALLOWED` entry; that `test/unit/stale-terms.test.js` (Plan 05) pins the table to zero unlisted.
       - `export const TERMS` — an array of frozen `{ id, regex, enforced, note }` (regex is a JS regex SOURCE string; every term is matched case-insensitively) with exactly these rows, in this order:
         1. `dpad` — `d-pad|dpad` — enforced
         2. `toast` — `toast` — enforced
         3. `classic-engine` — `dead classic|classic engine` — enforced
         4. `classic-script` — `classic script` — NOT enforced (note: live term, CONTEXT judgment call 1 — reported for the record)
         5. `wornSlots` — `wornSlots` — enforced
         6. `legacy-counters` — `flightLeft|flightCooldown|c\.ether` — enforced
         7. `recentre` — `recent(er|re).*(every|each) step` — enforced
         8. `round-card` — `round card|roundcard` — enforced (supplementary: the Phase 32 surface Phase 34 retired)
         9. `retired-bridges` — `__mz(ToHit|StrikeDie|ItemRowState|Abilities|Eff|SlotFor|WornSlots|Gear|WornKeysOf|SellPrice|RenderGrimoire|Bags)\b` — enforced (supplementary: `window.__mz*` names Phases 47-02/03/04 deleted; bridge-registry.test.js is comment-stripped so it cannot see these)
         10. `retired-files` — `toasts\.js|toastsCoverage|toastTable\.test|narrativeToasts\.test|shell-toast-wiring|toastsForAction|tutorial\.js|parley-button-mirror|round-card-worst-case` — enforced (supplementary: file names Phase 46 renamed/deleted, plus the test file Plan 03 renames)
         11. `legacy-won` — `won: false|state\.won|winGame` — NOT enforced (note: the 46-02 SUMMARY's 54-file inert `won: false` fixture-literal inventory — counted for the record, test bodies untouched)
       - `export const ALLOWED` — an array of `{ term, file, match, reason }`: `term` is a TERMS id; `file` is a repo-relative forward-slash path, OR a directory prefix ending in `/` (matches every file under it); `match` is a regex source tested (case-sensitively) against the raw line; `reason` is one sentence naming the survivor class (A/B/C/D) and why. Seed it with the class-C entries this plan can already name (verify each against the live file before adding — an entry that matches nothing is a hard error):
         - `legacy-counters` / `engine/saveState.js` / `flightLeft|flightCooldown|c\.ether` / "C — foldLegacyCounters: the tolerant-load reads of the pre-Phase-39 counter keys and the doc comment that explains the fold"
         - `legacy-counters` / `test/parity/harness/comparables.js` / same match / "C — the prototype-side strip of the legacy counters (the frozen master still sets them)"
         - `legacy-counters` / `test/parity/combat-parity.test.js`, `test/parity/magic-parity.test.js`, `test/parity/movement-parity.test.js` (three entries) / same match / "C — local comparable's prototype-side strip and its comment"
         - `legacy-counters` / `test/parity/full-suite.test.js` / same match / "C — states the counters are removed entirely from the engine side"
         - `legacy-counters` / `test/unit/item-activation.test.js` / same match / "C — the foldLegacyCounters pin: its input MUST carry the legacy keys and its assertions prove they are deleted"
         - `legacy-counters` / `test/unit/` / `flightLeft: 0, flightCooldown: 0` (unanchored — the key pair sits mid-line in some literals, e.g. after `name:` or before `bag:`) / "D — inert legacy keys in hand-built test-state literals (33 files at 649de2b); test bodies are out of DOCS-03's scope — counted, not edited"
         Plans 02-04 append class A/B entries for the survivors they keep; do not pre-add entries for files you have not verified.
       - `export function defaultSources()` → `[{ path, text }]` for `mazeworld.html` plus every `.js/.mjs/.cjs` under `src/`, `engine/`, `content/`, `tools/`, `test/` (recursive, `node_modules` skipped, paths repo-relative with forward slashes), EXCLUDING `tools/stale-terms.mjs` itself and `test/unit/stale-terms.test.js` (they spell the terms on purpose). `.txt`/`.json`/`.md` are never scanned (so the prototype master and the parity fixtures are out by construction).
       - `export function scan(sources, { terms = TERMS, allowed = ALLOWED } = {})` → `{ rows, hits, unusedAllow }`. Split each text on `/\r?\n/`; for every term and every line that matches, push a hit `{ term, file, line (1-based), text (trimmed), allowedBy (index into allowed or null) }` — a hit is allowed when some `allowed` entry has the same `term`, its `file` equals the path or is a `/`-terminated prefix of it, and its `match` regex tests true on the raw line. `rows` = one per term: `{ id, regex, enforced, total, allowed, unlisted }`. `unusedAllow` = every allowed entry that matched zero hits (allow-list rot).
       - `export function renderTable(rows)` → a markdown table `| Term | Regex | Hits | Allowed (listed) | Unlisted | Enforced |` in TERMS order.
       - CLI (direct-invocation guard like ident-sweep): `node tools/stale-terms.mjs [--paths p1 p2 ...] [--json] [--self-test]`. `--paths` keeps only sources whose path equals one of the given prefixes or starts with `<prefix>/` (so `--paths engine content tools` scopes the run; `mazeworld.html` is selectable by name). Default output: the table, then a `## Unlisted` block with every unlisted hit as `file:line: text` (enforced terms only), then a `## Allowed survivors` block grouped by reason with `file:line: text` under each, then a `## Allow-list rot` block naming every unused ALLOWED entry. Exit 1 when any enforced row has `unlisted > 0` OR `unusedAllow` is non-empty (when `--paths` is given, rot is judged only against entries whose `file` falls inside the given paths); else exit 0. `--json` prints `{ rows, hits, unusedAllow }`.
       - `--self-test` (no disk reads; synthetic sources only) proves: (a) a line comment mentioning the toast host is reported unlisted under term `toast` when `allowed` is empty; (b) the same line is counted allowed and NOT unlisted when an entry `{ term: "toast", file: "x/a.js", match: "host", reason }` is passed; (c) a directory-prefix entry (`file: "x/"`) allows a hit in `x/sub/b.js`; (d) a `classic script` line lands on the `classic-script` row with `enforced: false`; (e) an allowed entry that matches nothing is returned in `unusedAllow`; (f) a CRLF source (`"a\r\nb"`) yields line numbers 1 and 2, not one line. Prints `self-test: PASS` / `FAIL` with expected vs actual, exit 0/1.

    2. **tools/comment-only-diff.mjs** — node built-ins + `import { stripJs, stripHtml } from "./ident-sweep.mjs"`. Usage: `node tools/comment-only-diff.mjs <base-ref> [path-prefix ...]`. Lists changed files with `git diff --name-status <base-ref> -- <prefixes>` (`child_process.spawnSync("git", ...)`, `encoding: "utf8"`; default prefixes when none given: `engine content test/parity/fixtures`). For each entry: status `D` → print `DELETED: <file>` (counts as a failure); a file whose extension is not `.js/.mjs/.cjs/.html` → print `NON-CODE FILE CHANGED: <file>` (failure — a JSON fixture moved); otherwise read `git show <base-ref>:<file>` and the working-tree file, strip both (`stripHtml` for `.html`, `stripJs` otherwise), normalise (`\r\n` → `\n`, then collapse every whitespace run to one space, trim), compare: equal → `comment-only: <file>`; different → `CODE CHANGED: <file>` plus the first 120 characters around the first differing index on each side. Final line `comment-only-diff <base-ref>: <n changed>, <k code-changed>`; exit 1 if any failure, else 0. No changed files at all → prints `comment-only-diff <base-ref>: 0 changed` and exits 0. Head comment: Phase 48 criterion 4's proof; the normalisation rule (whitespace-insensitive by design — a comment deletion removes a line, which must not count); that `npm test` and the parity suite remain the behavioural proof.

    3. **tools/ident-sweep.mjs** — two comment-only edits in the head comment: (a) the sentence that says retired-identifier prose is "left behind on purpose in Phase 48's docs/comments sweep" is written in the future tense — rewrite it in the present ("retired-identifier prose a retirement statement keeps on purpose — see tools/stale-terms.mjs for the comment-side sweep"); (b) the string-literal example that names the retired D-pad option value — replace it with a neutral example (a retired option value like a legacy save key can still live inside a string). `--self-test` behaviour and every code line stay byte-identical (`node tools/comment-only-diff.mjs 649de2b tools/ident-sweep.mjs` → `comment-only:`).

    4. Run `node tools/stale-terms.mjs` once (expect exit 1 — the table is the phase-start baseline; paste it into this plan's SUMMARY as "Baseline at 649de2b, measured by the tool"). Commit the three tools files: `feat(48-01): tools/stale-terms.mjs (criterion-1 table + allow-list) and tools/comment-only-diff.mjs (stripped-diff proof)`.
  </action>
  <verify>
    <automated>node tools/stale-terms.mjs --self-test && node tools/ident-sweep.mjs --self-test && node tools/comment-only-diff.mjs 649de2b tools/ident-sweep.mjs && node tools/stale-terms.mjs --json > /dev/null; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/stale-terms.mjs --self-test` prints `self-test: PASS` and exits 0
    - `node -e "import('./tools/stale-terms.mjs').then(m => console.log(m.TERMS.length, m.TERMS.filter(t => !t.enforced).map(t => t.id).join(','), typeof m.scan, typeof m.renderTable, typeof m.defaultSources))"` prints `11 classic-script,legacy-won function function function`
    - `node tools/stale-terms.mjs | grep -c "^| "` prints a number >= 13 (header + separator + 11 rows)
    - `node tools/stale-terms.mjs; echo "exit=$?"` ends with `exit=1` at this commit (unlisted hits exist until Plans 02-04 land) and its `## Allow-list rot` block is empty (every seeded ALLOWED entry matches a live line)
    - `node tools/stale-terms.mjs --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const r=Object.fromEntries(j.rows.map(x=>[x.id,x]));console.log(r['legacy-counters'].allowed >= 40, r['classic-script'].enforced, j.unusedAllow.length)})"` prints `true false 0`
    - `node tools/ident-sweep.mjs --self-test` prints `self-test: PASS`
    - `grep -c "Phase 48's" tools/ident-sweep.mjs` prints 0; `grep -c '"dpad"' tools/ident-sweep.mjs` prints 0
    - `node tools/comment-only-diff.mjs 649de2b tools/ident-sweep.mjs` prints `comment-only: tools/ident-sweep.mjs` and exits 0
    - `node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` prints `comment-only-diff 649de2b: 0 changed` and exits 0 (nothing under the fence has moved yet)
    - `node tools/comment-only-diff.mjs 649de2b src/browser/bridge.js; echo "exit=$?"` ends with `exit=0` (unchanged file, 0 changed — proves the tool accepts an explicit file path)
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3288` and `# fail 0`
  </acceptance_criteria>
  <done>The criterion-1 table is one command with an allow-list that rots loudly; the stripped-diff proof is one command; ident-sweep's own two stale lines are gone; both tools are committed.</done>
</task>

<task type="auto">
  <name>Task 2: engine/ + content/ comment sweep (comment lines only, stripped-diff proven)</name>
  <files>engine/character.js, engine/movement.js, engine/effects.js, engine/items.js, engine/derived.js, engine/combat.js, content/treasure-tables.js, content/foe-abilities.js</files>
  <read_first>
    - `node tools/stale-terms.mjs --paths engine content` — the exact unlisted lines (expect ~17 at 649de2b; every one is listed below)
    - engine/saveState.js `foldLegacyCounters` and its doc comment (lines ~479-524, ~620) — READ ONLY: these are the class-C survivors already in ALLOWED; do not edit them
    - src/browser/narrationLines.js head comment (lines 1-12) — the current vocabulary: `LINE_FOR` builders produce narration LINES; `rail.js` shows them as one RAIL card out of combat; `fightLog.js` shows them as fight-log lines in combat; the Oracle (`eventNarration.js`) is the full-sentence record. Use these words in every rewrite.
    - engine/timers.js / engine/effects.js exports (`startEffect`, `startCooldown`, `tickSquares`) — the live item-timer model the rewrites name
  </read_first>
  <action>
    Rewrite each site so the sentence describes what the code does NOW, or delete it when it only explained the retired pattern. Match the surrounding voice (deadpan, specific). Comment lines only — never touch a code token, string literal, or blank-line structure inside code. Sites (anchor by the quoted text, not the line number):

    **engine/character.js** (rollCharacter's field block, ~562-570):
    - The four-line comment "Phase 39 (GEAR-02): the retired Cloak-of-Flying … pair — its effect/cooldown now rides a c.timers … record instead, started by engine/movement.js's climb/gorge block (no chargen field needed at all)" → replace with one line stating the live fact: the Cloak of Flying's effect/cooldown is a `c.timers` record started by `engine/movement.js`'s climb/gorge block, so chargen initialises no flight field.
    - "placed alongside the darkFor/flightLeft/flightCooldown plain scalars above" → "placed alongside the `darkFor` plain scalar above" (the other two no longer exist).

    **engine/movement.js**:
    - The ether block (~209-215) "the Cloak of Ether … set `c.ether=20` (items.js:"ether") but the field was only ever set + ticked down (per-step below), READ NOWHERE. Wired here by MIRRORING isFlying" → rewrite as the current rule: while the Cloak of Ether's `item:Cloak of Ether` effect is live (`itemEffectActive(state.c, "ether")`, a `c.timers` record since Phase 39), you phase through the wall/crevice with no climb/leap roll and no fall damage — mirroring `isFlying`. Keep the "DELIBERATE RULES CHANGE (Phase 15 …)" lead-in and the "Deliberately NOT touching …" tail if they still read true.
    - "(an 8-cell pool never stacks eight toasts)" (~345) → "(an 8-cell pool never stacks eight rail lines)".
    - "Phase 39 (GEAR-02): the retired Cloak-of-Flying … pair — its effect/cooldown now rides the same c.timers squares tick below like every other item." (~446) → one line: the Cloak of Flying's effect/cooldown rides the same `c.timers` squares tick below like every other item.

    **engine/effects.js** head comment (~4-9): "Do NOT retrofit the existing bespoke fields (darkFor/haste/invis/ether/acute/flightLeft/flightCooldown/ward/afraid/foeEffect/it.usedAt) onto this shape — those stay exactly as they are" is stale (Phase 39 moved haste/invis/ether/acute/the flight pair and `it.usedAt` onto this shape). Rewrite to name only the fields that still keep a bespoke shape — verify each candidate with `grep -n "c\.<field>" engine/*.js` before naming it (expected survivors: `darkFor`, `ward`, `afraid`, `foeEffect`) — and state that item effects/cooldowns ride this module's timers since Phase 39.

    **engine/items.js**:
    - "the Oracle/toasts/rail narrate" (~1104) → "the Oracle/rail/fight log narrate".
    - "Phase 39 (GEAR-02): the retired scattered counters (c.might += 8/4 never expiring, c.haste/c.acute/c.invis/c.ether) — applyActivation … starts the item's own c.timers record instead; no direct character-field write happens here anymore." (~1300) → keep as a retirement statement only if you shorten it to one sentence that does not enumerate the dead field names: applyActivation (after the switch) starts the item's own `c.timers` record; this switch writes no character field directly.

    **engine/derived.js**:
    - "test/unit/toastsCoverage.test.js's event-vocabulary scanner" (~336) → the file's current name, `test/unit/narrationLinesCoverage.test.js` (confirm with `ls test/unit/narrationLinesCoverage.test.js`).
    - "(the fight log, toasts, the rail, the combat submenu)" (~933) → "(the fight log, the rail, the combat submenu)".

    **engine/combat.js**:
    - "(e.g. a combat-start toast/report)" (~280) → "(e.g. a combat-start rail card or report)".
    - "(this event, the fight log, toasts, the rail, the combat submenu)" (~975) → drop "toasts,".
    - The three-line "IMPORTANT — mazeworld.html's classic (non-module) `canParley()` duplicate (D-17, ~line 4200) and test/unit/parley-button-mirror.test.js (20-03) MUST mirror this exact decision order line for line: change one, change both." (~1029-1031): both the shell duplicate and that test file are gone (Phase 44; `ls test/unit/parley-button-mirror.test.js` fails). Replace with one line: this function is the ONLY canParley — the shell reaches it through the engine bridge; there is no duplicate to keep in step.

    **content/treasure-tables.js** "the chip/toast state the exact numbers" (~215) → "the chip and the rail line state the exact numbers".

    **content/foe-abilities.js** "a complete, toast-length, family-friendly deadpan sentence" (~23) → "a complete one-line, family-friendly deadpan sentence".

    Then: `node tools/stale-terms.mjs --paths engine content` must show unlisted 0 on every enforced row (if a line you did not expect remains, treat it the same way — rewrite or delete — and note it in the SUMMARY). Run the gates: `node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` (every changed file `comment-only:`; exit 0), `npm test` (3288/0), `node --test test/parity/*.test.js` (fail 0), master hash. Commit with the stripped-diff proof pasted into the message body: `docs(48-01): engine/content comment sweep — stale counter/toast/mirror prose rewritten, comment-only (stripped diff empty)`.
  </action>
  <verify>
    <automated>node tools/stale-terms.mjs --paths engine content; echo "exit=$?"; node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures; echo "exit=$?"; git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/stale-terms.mjs --paths engine content; echo "exit=$?"` ends with `exit=0` and its table shows `Unlisted` = 0 on every enforced row; the `## Allowed survivors` block lists only `engine/saveState.js` lines (class C)
    - `node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` prints one `comment-only:` line per changed file (expected 8: `engine/character.js`, `engine/movement.js`, `engine/effects.js`, `engine/items.js`, `engine/derived.js`, `engine/combat.js`, `content/treasure-tables.js`, `content/foe-abilities.js`), zero `CODE CHANGED:` / `NON-CODE FILE CHANGED:` / `DELETED:` lines, and exits 0
    - `git diff --stat 649de2b -- test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `grep -c "parley-button-mirror" engine/combat.js` prints 0; `grep -c "toastsCoverage" engine/derived.js` prints 0
    - `grep -ciE "toast" engine/*.js content/*.js | grep -v ":0$" | wc -l` prints 0
    - `node --test test/parity/*.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3288` and `# fail 0`
    - `git log --format=%B -1 -- engine/combat.js | grep -c "comment-only"` prints a number >= 1 (the proof is in the commit message)
  </acceptance_criteria>
  <done>engine/ and content/ carry no comment that presents a retired counter, the toast surface, or a deleted shell mirror as live; the only engine survivors are foldLegacyCounters's tolerant-load lines, listed; the stripped diff proves comment-only; 3288/0.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (all five plans)

- `tools/stale-terms.mjs` — the standing tripwire: TERMS + ALLOWED + table (Plan 01; ALLOWED grows in 02-04; pinned in 05)
- `tools/comment-only-diff.mjs` — the engine-fence stripped-diff proof (Plan 01)
- `engine/*.js`, `content/*.js`, `tools/ident-sweep.mjs` — comment-only sweep (Plan 01)
- `mazeworld.html`, `src/browser/*.js` — comment-only sweep (Plan 02)
- `test/**` — test titles renamed, comments swept, `round-card-worst-case.test.js` → `fight-log-worst-case.test.js` (Plans 03-04)
- `.claude/CLAUDE.md` Android-only; `docs/*.md` bucketed (one deletion: `docs/COMBAT-NARRATIVE-DESIGN.md`) (Plan 05)
- `test/unit/stale-terms.test.js` — pins the table at zero unlisted and the allow-list at zero rot (Plan 05)
- `48-05-SUMMARY.md` — criteria 1-4 verbatim, the final table, every survivor with reason, the docs bucket list, the two judgment calls for the user to reverse (Plan 05)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tools -> repo files | both tools read the working tree and `git show`; neither writes any file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01 | Tampering | engine/ comment edits | high | mitigate | comment-only-diff.mjs stripped comparison + npm test 3288/0 + parity suite at every commit |
| T-48-02 | Repudiation | allow-list entries | low | mitigate | every ALLOWED entry carries file + match + reason; unused entries fail the run (rot is loud) |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 48 |
</threat_model>

<verification>
- `node tools/stale-terms.mjs --self-test` → `self-test: PASS`; `node tools/stale-terms.mjs --paths engine content` → exit 0
- `node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` → all `comment-only:`, exit 0
- `npm test` → `# pass 3288`, `# fail 0`; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
</verification>

<success_criteria>
- The criterion-1 grep is a tool with an allow-list and teeth; the stripped-diff proof is a tool
- engine/ + content/ + tools/ident-sweep.mjs carry no stale comment on any enforced term; foldLegacyCounters's lines are the listed survivors
- Both commits leave the engine fence comment-only and the suite at 3288/0
</success_criteria>

<output>
Create `.planning/phases/48-stale-docs-comments-test-names-purge/48-01-SUMMARY.md` when done. It MUST carry: the phase-start table printed by `node tools/stale-terms.mjs` at 649de2b (before any sweep) labelled as the tool-measured baseline; the post-sweep `--paths engine content tools` table; the `comment-only-diff` output; the list of the engine/content lines rewritten vs deleted; `npm test` totals; the master hash; and a `## Human verification (deferred to end of run)` section stating nothing user-visible changed.
</output>
