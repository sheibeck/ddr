---
phase: 26-mass-playtest-class-pass-ledger
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/class-pass-diff.mjs
  - test/unit/class-pass-diff.test.js
autonomous: true
requirements: [PLAY-02]
tags: [class-pass, dev-tool, diff, fun-band, ledger, zero-deps]

must_haves:
  truths:
    - "Band edges are exact and documented: a row whose mean depth equals 0.75*mu or 1.35*mu is 'fine' (closed interval), strictly below/above is too weak/too strong; a cell with meanKills exactly 0.5 is NOT cannot-act (strictly below is); asserted by unit tests on the diff script with synthetic rows at each edge."
    - "A cell with completed 0 (all runs stuck) is cannot-act by the stuck rule and its depth stats are null, never NaN; the diff script tolerates a missing BEFORE cell (reports delta as n/a) but the ledger test asserts BEFORE and AFTER have identical cell key sets (143) and identical meta parameters modulo commit; the cannot-act gate is asserted against after.json as a hard zero."
    - "Rankings use the harness's documented tie-break (mean depth desc, p50 desc, reach5 desc, sub asc, race asc) so BEFORE and AFTER rank the same way; the comparison table is ordered by AFTER rank; the out-of-band appendix is ordered by band then by AFTER mean; output is byte-stable across runs of the diff script on the same inputs (a test runs it twice and compares)."
    - "mu is computed over RUNS (run-weighted: sum of meanDepth x completed over the 143 cells divided by the sum of completed) and the script header plus the rendered Markdown both say so; with zero stuck runs and equal seeds per cell it coincides with the mean over cells, and the header says that too (Claude's Discretion, 26-CONTEXT.md)."
    - "The script emits docs/class-pass/verdicts.json with verdict/reason/lever fields present on every sub/race row and null on all of them unless copied from a --verdicts input file; the script never invents editorial text (the accept/revisit column is the one hand-written column, 26-CONTEXT.md)."
    - "The script's Markdown for the AFTER section begins with the cannot-act headline line, then the by-class BEFORE->AFTER roll-up, then the bottom-five/top-five sub-classes, then the race table (26-CONTEXT.md Specific Ideas) — and the depth-20 slice tables carry floors-gained/encounters-survived only, never a band or verdict."
  artifacts:
    - path: "tools/class-pass-diff.mjs"
      provides: "dev-only zero-dep diff of before/after class matrices: mu, bands, cannot-act, per-sub/per-race rows, out-of-band appendix, depth-20 roll-ups, Markdown sections (after/outliers/handoff), verdicts.json, --gate"
      min_lines: 250
      exports: ["BAND_LOW", "BAND_HIGH", "CANNOT_ACT_KILLS", "BAND_ORDER", "cellKey", "muOverRuns", "bandFor", "isCannotAct", "rowBand", "metaParity", "rankRollupRows", "pooledReach", "cannotActCells", "buildVerdicts", "mergeEditorial", "renderMarkdown"]
    - path: "test/unit/class-pass-diff.test.js"
      provides: "edge/ordering/null-safety/determinism/gate/purity tests for the diff script using before.json as a synthetic AFTER"
      min_lines: 150
  key_links:
    - from: "tools/class-pass-diff.mjs"
      to: "tools/lib/class-matrix.mjs"
      via: "imports rankCells (the ONLY ordering comparator the script uses — rollup rows are ranked through rankCells by mapping key -> sub and race -> empty string)"
      pattern: "import \\{ rankCells \\} from \"./lib/class-matrix.mjs\""
    - from: "test/unit/class-pass-diff.test.js"
      to: "tools/class-pass-diff.mjs"
      via: "imports the pure helpers and spawns the CLI twice via child_process.execFileSync for the determinism test"
      pattern: "class-pass-diff.mjs"
    - from: "tools/class-pass-diff.mjs"
      to: "docs/class-pass/before.json"
      via: "default --before path; the meta/cells/rollups shape documented in tools/lib/class-matrix.mjs is the only input contract"
      pattern: "docs/class-pass/before.json"
  prohibitions:
    - "No change to tools/tune-classes.mjs, tools/lib/class-matrix.mjs or tools/lib/tuning-bot.mjs — the harness stays byte-identical to the BEFORE pin 5565b22 so AFTER parameters are comparable"
    - "The diff script never plays a run, never imports the engine or content, never spawns git or any child process, and never touches engine/, content/, src/ or mazeworld.html"
    - "No Math.random, Date.now, new Date, timestamps or environment-dependent text in the script or its output — byte-stable on the same inputs"
    - "The script never invents a verdict, reason or lever — those three fields are emitted null and only ever copied from a --verdicts input file"
    - "No band or verdict is ever attached to a depth-20 row"
    - "Never a CI gate on balance: the tests assert rules, ordering and determinism on synthetic rows and self-diffs, never a real death-depth number"
---

<objective>
Build the machine layer of PLAY-02: `tools/class-pass-diff.mjs`, a dev-only, zero-dependency Node ESM script that reads the BEFORE/AFTER class-matrix JSON pairs (natural + depth-20), computes mu over runs, the fun bands (too weak < 0.75mu; fine [0.75mu, 1.35mu]; too strong > 1.35mu; cannot act = meanKills < 0.5 OR stuck > 0), the per-sub-class (24) and per-race (6, Human included) BEFORE/AFTER/delta/reach5/band rows, the out-of-band cell appendix with an in-band count, the depth-20 floors-gained/encounters-survived roll-ups (no verdicts), and prints the Markdown the ledger pastes verbatim — plus `docs/class-pass/verdicts.json` (bands + null accept/revisit placeholders the editorial plan fills) and a `--gate` mode that exits 3 on any cannot-act cell. Reuses `rankCells` from `tools/lib/class-matrix.mjs` for every ordering so BEFORE and AFTER rank the same way. Tested against `before.json` as a synthetic AFTER (no matrix run needed here), including a run-it-twice byte-stability test.

Purpose: 26-CONTEXT.md locks "numbers are never retyped" and "the accept/revisit sentences are the one hand-written column" — this script is what makes both true. Plan 26-02's capture quotes its `--gate` verdict; Plan 26-03 pastes its Markdown; Plan 26-04's ledger test re-renders through it.
Output: `tools/class-pass-diff.mjs`, `test/unit/class-pass-diff.test.js`, one or two commits, `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md
@tools/tune-classes.mjs
@tools/lib/class-matrix.mjs
@test/unit/class-matrix.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `tools/class-pass-diff.mjs` — dev-only, zero-dep diff script. CLI: `--before P` (default `docs/class-pass/before.json`), `--after P` (default `docs/class-pass/after.json`), `--before-deep P` (default `docs/class-pass/before-depth20.json`), `--after-deep P` (default `docs/class-pass/after-depth20.json`), `--verdicts P` (existing verdicts file whose verdict/reason/lever fields are merged in by key), `--out-verdicts P` (write the verdicts JSON atomically, tmp-then-rename), `--section after|outliers|handoff|all` (default `all`), `--json` (print the verdicts object instead of Markdown), `--gate` (load only `--after`; print the cannot-act cells; exit 3 if any, else 0). Exit 2 on bad flags / unreadable input. [26-01]
- `test/unit/class-pass-diff.test.js` — edge, null-safety, ordering, self-diff, editorial merge, section rendering, CLI determinism, `--gate`, purity. [26-01]
- `docs/class-pass/after.json`, `docs/class-pass/after-depth20.json` — AFTER aggregates (143 x 40 natural, 143 x 10 depth-20), commit-pinned to post-Phase-25 HEAD, same `Bot:` lines as BEFORE. [26-02]
- `docs/CLASS-PASS.md` AFTER section skeleton: heading replaced in place with the full pin hash, `### Pin and provenance (Phase 26 capture)`, both verbatim transcripts under `### AFTER transcript — ...` headings. [26-02]
- `docs/class-pass/verdicts.json` — schema `class-pass-verdicts/1`: meta (commits, parity, mu, band edges), cannotAct, subs (24), races (6), cells {inBand, outOfBand}, deep {byClass, bySub, byRace}; verdict/reason/lever filled by the editorial task on every out-of-band sub/race row. [26-03]
- `docs/CLASS-PASS.md` AFTER comparison block (script `--section after` output pasted between the AFTER heading and the provenance sub-heading), `## Outliers (Phase 26 — revisit list)`, `## Handoff to Phase 27` (script `--section outliers` / `--section handoff` output). [26-03]
- `test/unit/class-pass-ledger.test.js` — section presence + order, 30-row good/bad table (once), IDENT ruling headings, AFTER hash == both `meta.commit`, meta parity modulo commit, identical 143 cell keys, zero cannot-act cells, verdicts schema, Outliers rows == revisit rows, ledger contains the script's rendering verbatim. [26-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/class-pass-diff.mjs — pure helpers, verdicts builder, Markdown renderer, CLI with --gate</name>
  <files>tools/class-pass-diff.mjs</files>
  <read_first>
    - tools/lib/class-matrix.mjs in full: the JSON SHAPE contract (meta fields, cells fields, rollups arrays), `rankCells` (the documented tie-break; note `rollups` rows carry `key` not sub/race), `summarizeRows` (null-not-NaN discipline when completed is 0), `fmt`/`causesStr` (formatting conventions to mirror), the file-header purity discipline
    - tools/tune-classes.mjs: `usage()`/`fail()`/`parseArgs()` (accept both `--flag value` and `--flag=value`; exit 2 with usage on error), the atomic `--out` write (tmp path then renameSync), the `isMainThread` guard pattern (this script uses an `import.meta.url` main-module guard instead — see action)
    - docs/class-pass/before.json: `meta` only, the first two `cells` entries, and `rollups.byClass[0]`/`rollups.bySub[0]`/`rollups.byRace[0]` (do not dump all 143 cells); docs/class-pass/before-depth20.json `meta` and `cells[0]` (note `meanFloorsGained`/`meanEncountersSurvived`)
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md, sections "The fun band (PLAY-02)", "The capture and the ledger", "Outliers and the handoff", "Claude's Discretion", "Specific Ideas"
    - .claude/CLAUDE.md: zero runtime deps; dev tools under tools/ are never shipped
  </read_first>
  <action>
    Create `tools/class-pass-diff.mjs` (Node ESM, imports only `node:fs`, `node:path`, `node:url` and `{ rankCells }` from `./lib/class-matrix.mjs`; nothing from engine/ or content/; no process spawning of any kind). Header comment (mirroring tune-classes.mjs's header): dev-only / zero-dep / NOT shipped / NOT a node:test file / TUNING PROXY NOT A GATE; the mu basis sentence ("mu is run-weighted: sum(meanDepth_i x completed_i) / sum(completed_i) over cells with completed > 0 — equal to the mean over all completed runs up to each cell's 2-decimal rounding of meanDepth; with zero stuck runs and equal seeds per cell it also equals the mean over cells"); the band rule with the closed interval spelled out; the cannot-act rule; the ordering rule (every table is ordered through class-matrix.mjs's `rankCells` — rollup rows are ranked by mapping `key` to `sub` and `race` to the empty string so the identical comparator applies: meanDepth desc, p50 desc, reach5 desc, key asc); the byte-stability promise (no timestamps, no randomness, no environment text); the verdicts.json schema; the CLI usage block.

    Exports (all pure except the CLI main):
    - Constants `BAND_LOW = 0.75`, `BAND_HIGH = 1.35`, `CANNOT_ACT_KILLS = 0.5`, `BAND_ORDER = ["cannot act", "too weak", "too strong"]` (appendix order; "fine" never appears in the appendix), `PARITY_FIELDS = ["seeds", "seedList", "workers", "maxActions", "startDepth", "exploreBudget", "cells", "bot"]`.
    - `cellKey(cell)` -> `cls/sub/race` string.
    - `muOverRuns(report)` -> run-weighted mean over cells with completed > 0 (full precision, not rounded — rounding only at render time); `null` when no cell has a completed run.
    - `bandFor(meanDepth, mu)` -> `null` when meanDepth is null/undefined; `"too weak"` when meanDepth < BAND_LOW * mu; `"too strong"` when meanDepth > BAND_HIGH * mu; otherwise `"fine"` (so exactly 0.75mu and exactly 1.35mu are fine — closed interval).
    - `isCannotAct(row)` -> true when `row.stuck > 0`, or `row.completed === 0`, or (`row.meanKills` is a number and `row.meanKills < CANNOT_ACT_KILLS`); exactly 0.5 is NOT cannot-act.
    - `rowBand(row, mu)` -> `"cannot act"` when isCannotAct, else `bandFor(row.meanDepth, mu)`; works for cells and rollup rows alike.
    - `metaParity(a, b)` -> `{ ok, mismatches }` comparing the PARITY_FIELDS of two `meta` objects (strict equality; `bot` byte-for-byte); `commit` is deliberately excluded.
    - `rankRollupRows(rows)` -> a ranked copy of rollup rows via `rankCells(rows.map(r => ({ ...r, sub: r.key, race: "" })))`, then drop the helper `sub`/`race` fields; each row gains `rank`.
    - `pooledReach(report)` -> `{ runs, reach5, reach10, reach20: null, maxP90 }` where reach5/reach10 are pooled over completed runs (sum(reach_k / 100 x completed) / sum(completed) x 100, one decimal) and `maxP90` is the largest non-null `p90Depth` over cells; reach20 is null because the harness JSON carries reach5/reach10 only (the renderer explains this).
    - `cannotActCells(report)` -> cells satisfying isCannotAct, in `rankCells` order, each with `key`, `meanKills`, `stuck`, `completed`, `topCauses`.
    - `buildVerdicts({ before, after, beforeDeep, afterDeep, prior })` -> the verdicts object, schema string `"class-pass-verdicts/1"`, shape: `meta` { before/after/beforeDeep/afterDeep: { commit, seeds, startDepth, cells, bot }, parity: { natural: metaParity(before.meta, after.meta), deep: metaParity(beforeDeep.meta, afterDeep.meta) }, mu: { basis: "runs", before, after } (4 decimals), bands: { tooWeakBelow: BAND_LOW, tooStrongAbove: BAND_HIGH, cannotActKillsBelow: CANNOT_ACT_KILLS, edges: { low, high } } (4 decimals, relative to AFTER mu), runs: { natural: { n, stuck, completed }, deep: { n, stuck, completed } } summed over cells }; `cannotAct` = cannotActCells(after); `subs` (24) and `races` (6) rows = rankRollupRows(after.rollups.bySub / byRace) joined to the BEFORE rollup row by key: `{ key, cls (subs only: the cls of any AFTER cell with that sub), rank, before: { meanDepth, p50Depth, reach5, reach10, meanKills } | null, after: { same }, delta (after.meanDepth - before.meanDepth, 2 decimals, null when either side is null or BEFORE row missing), band: rowBand(afterRow, muAfter), verdict: null, reason: null, lever: null }`; `cells` = `{ inBand, outOfBand }` where outOfBand lists every AFTER cell whose band is not "fine" as `{ key, cls, sub, race, rank, band, before: { meanDepth, meanKills } | null, after: { meanDepth, meanKills, stuck, completed, reach5, topCauses }, delta }` ordered by BAND_ORDER index then AFTER rank, and `inBand` = 143 minus outOfBand.length (a missing BEFORE cell gives before null and delta null — never a throw); `deep` = `{ byClass, bySub, byRace }` from the depth-20 pair, each row `{ key, rank, before: { meanFloorsGained, p50FloorsGained, meanEncountersSurvived, meanKills }, after: { same }, delta: { floorsGained, encountersSurvived } }` ordered by rankRollupRows of the AFTER deep rollup — NO band, NO verdict on any deep row. When `prior` is given, finish with `mergeEditorial`.
    - `mergeEditorial(verdicts, prior)` -> copies `verdict`, `reason`, `lever` by `key` from prior.subs/prior.races onto the freshly built rows (only those three fields; everything else is recomputed); throws when a verdict is not one of `"accept"`, `"revisit"`, `null`; returns the same object (idempotent when re-fed its own output).
    - `renderMarkdown(verdicts, section)` -> a string for `"after"`, `"outliers"`, `"handoff"`, or `"all"` (the three joined by a blank line). Deterministic formatting: means/deltas 2 decimals, deltas signed with an explicit `+`, reach percentages 1 decimal, `n/a` for null, `|`-tables with a spaced `| --- |` separator row.
      - `"after"`: line 1 is the headline — `**Zero cannot-act cells.** 0 of <cells> AFTER natural-start cells have meanKills < 0.5 or stuck > 0 (<stuck> of <n> natural-start runs stuck; depth-20 slice: <stuckDeep> of <nDeep> runs stuck).` when cannotAct is empty, otherwise `**CANNOT-ACT GATE FAILED.** <k> of <cells> AFTER cells ...` followed by a table (`| # | Class | Sub | Race | kills | stuck | completed | top causes |`). Line 2: `**Parameter parity (BEFORE vs AFTER, modulo commit):** ...` naming each pair identical or listing its mismatched fields. Line 3: the mu line — `**mu (mean death depth over all completed AFTER natural-start runs) = <after mu>** (BEFORE mu <before mu>, delta <signed>). Bands relative to AFTER mu: too weak < <low> (0.75mu) · fine <low>–<high> (closed) · too strong > <high> (1.35mu) · cannot act = meanKills < 0.5 or stuck > 0. mu is run-weighted: sum(meanDepth x completed) / sum(completed) over the <cells> cells.` Then, in this order, these H3 headings with their tables: `### By class — BEFORE → AFTER` (`| # | Class | BEFORE mean | AFTER mean | Δ | BEFORE p50 | AFTER p50 | BEFORE ≥5 | AFTER ≥5 | AFTER ≥10 | AFTER kills |`, ordered by rankRollupRows of after.rollups.byClass); `### Sub-classes — bottom five and top five (AFTER)` (a `**Bottom five**` table = the last five subs by rank in rank order, then a `**Top five**` table = the first five; columns `| # | Sub | Class | BEFORE | AFTER | Δ | Band | Verdict | Reason |`); `### Races — BEFORE → AFTER (Human is the control)` (`| # | Race | BEFORE | AFTER | Δ | ≥5 BEFORE | ≥5 AFTER | Band | Verdict | Reason |`); `### Sub-classes — all 24 rows (AFTER rank order)` (`| # | Sub | Class | BEFORE | AFTER | Δ | ≥5 BEFORE | ≥5 AFTER | Band | Verdict | Reason |`); `### Reach table (AFTER natural, pooled over completed runs)` (`| Floor | BEFORE | AFTER |` rows `≥5`, `≥10`, `≥20`; the ≥20 row shows `n/a` on both sides and is followed by the sentence `≥20 is not carried per cell by the Phase 22 harness JSON (reach5/reach10 only); the highest per-cell p90 in the AFTER matrix is <maxP90>, so no cell reaches 20 in 10% or more of its runs; the depth-20 slice below is the ≥20 yardstick.`); `### Depth-20 slice — Phase 27's yardstick (no verdicts)` (three tables, by class / by sub-class / by race: `| # | <Key> | floors gained BEFORE | AFTER | Δ | encounters survived BEFORE | AFTER | Δ |`); `### Out-of-band cells (appendix)` (`In band: <inBand> of <cells> cells (fine).` then `| Band | # | Class | Sub | Race | BEFORE | AFTER | Δ | AFTER kills |` in BAND_ORDER then AFTER rank; when outOfBand is empty print `No out-of-band cells.`). The Verdict column renders `accept` / `revisit` / empty for an in-band row with null verdict / `(verdict pending)` for an out-of-band row with null verdict; Reason renders the reason text or empty.
      - `"outliers"`: `## Outliers (Phase 26 — revisit list)`, a blank line, then EITHER the exact sentence `No revisit rows — every out-of-band row was accepted.` OR a table `| Row | Kind | Band | BEFORE | AFTER | Δ | Reason | Suggested lever (v1.3) |` listing every sub row (rank order) then every race row (rank order) whose verdict is `revisit` (Kind = `sub` / `race`), followed by the fixed sentence `Every lever above is a v1.3 candidate — Phase 26 measures and judges; it changes no engine, content or tool code.`
      - `"handoff"`: `## Handoff to Phase 27`, then `### Yardstick summary` (mu, band edges, pooled reach table incl. the ≥20 caveat, cannot-act count, the four commits), `### AFTER natural roll-ups — by class` / `— by sub-class` / `— by race` (`| # | Key | mean | p50 | p90 | ≥5 | ≥10 | kills | lvl | actions |` straight from after.rollups via rankRollupRows), `### Depth-20 roll-ups — by class` / `— by sub-class` / `— by race` (`| # | Key | floors gained (mean) | floors gained (p50) | encounters survived | kills |` from afterDeep.rollups), `### Accepted-but-strong rows` (every sub/race row with band `too strong` and verdict `accept`, as `- <key> (<kind>): AFTER <mean> — <reason>`; or `None — no row is both too strong and accepted.`), `### Revisit rows` (same list shape for verdict `revisit`, or `None.`), `### Caveats` (four fixed sentences: tuning proxy not a gate and not a substitute for the Phase 27 human DR round; pairing is by seed not by path — engine changes shift downstream dice; the natural matrix's ≥20 rate and its overall median are not carried per cell by the harness JSON, so Phase 27's own readout supplies them; bands are relative to the AFTER mu and the AFTER mu only).
    - `parseArgs(argv)` (both `--flag value` and `--flag=value`; unknown flag -> exit 2 with usage), and a `main()` that runs only when this file is the entry module (compare `path.resolve(process.argv[1])` with `fileURLToPath(import.meta.url)`), so the test can import the helpers without side effects. `main()`: in `--gate` mode read only `--after`, print `cannot-act cells: <k> of <cells>` plus one line per cell (`<key>  kills=<meanKills>  stuck=<stuck>  completed=<completed>  top causes: <causesStr>`), exit 3 when k > 0 else 0. Otherwise read all four inputs (exit 2 with a clear message naming the missing/unparseable file), read `--verdicts` when given (exit 2 if unparseable), build, write `--out-verdicts` atomically (tmp then renameSync, 2-space JSON with a trailing newline) when given, print `--json` (the verdicts object, 2-space) or the requested Markdown section to stdout, exit 0.
  </action>
  <verify>
    <automated>node tools/class-pass-diff.mjs --after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json --section after | head -5 && node tools/class-pass-diff.mjs --gate --after docs/class-pass/before.json; echo rc=$?</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/class-pass-diff.mjs --after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json --section after | head -1` starts with `**Zero cannot-act cells.**` (BEFORE has 0 stuck and no cell under 0.5 kills, so the synthetic self-diff passes the gate).
    - `node tools/class-pass-diff.mjs --after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json --section after | grep -c "^### "` prints `7` (By class; bottom/top five; Races; all 24 rows; Reach table; Depth-20 slice; Out-of-band appendix).
    - `node tools/class-pass-diff.mjs --after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json --section outliers | grep -c "No revisit rows — every out-of-band row was accepted."` prints `1` (no editorial file -> no revisit rows).
    - `node tools/class-pass-diff.mjs --after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json --section handoff | grep -c "^## Handoff to Phase 27$"` prints `1`.
    - `node tools/class-pass-diff.mjs --gate --after docs/class-pass/before.json; echo rc=$?` ends with `rc=0` and prints `cannot-act cells: 0 of 143`.
    - `node tools/class-pass-diff.mjs --after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const v=JSON.parse(s);console.log(v.schema,v.subs.length,v.races.length,v.cells.inBand+v.cells.outOfBand.length,v.subs.every(r=>r.verdict===null&&r.reason===null&&r.lever===null),v.deep.bySub.length,v.meta.mu.basis)})"` prints `class-pass-verdicts/1 24 6 143 true 24 runs`.
    - `node tools/class-pass-diff.mjs --bogus; echo rc=$?` ends with `rc=2` and the usage text appears on stderr.
    - `grep -c 'import { rankCells } from "./lib/class-matrix.mjs"' tools/class-pass-diff.mjs` prints `1`; `grep -v '^\s*//' tools/class-pass-diff.mjs | grep -c "child_process\|worker_threads\|from \"../engine\|from \"../content"` prints `0` (non-comment lines only — the script spawns nothing and imports no engine/content module).
    - `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib; echo rc=$?` prints `rc=0` (harness untouched).
  </acceptance_criteria>
  <done>The diff script renders the AFTER/Outliers/Handoff Markdown and the verdicts JSON from any before/after pair, orders everything through class-matrix.mjs's rankCells, states its mu basis, and refuses nothing except bad input; --gate is a one-command cannot-act check.</done>
</task>

<task type="auto">
  <name>Task 2: test/unit/class-pass-diff.test.js — edges, null-safety, ordering, self-diff, editorial merge, sections, determinism, --gate, purity</name>
  <files>test/unit/class-pass-diff.test.js</files>
  <read_first>
    - test/unit/class-matrix.test.js in full (node:test + assert/strict shape; REPO_ROOT resolution; the purity test's comment-stripping regex; the "never asserts a real death-depth number" discipline in its header)
    - tools/class-pass-diff.mjs as written in Task 1 (exports and the verdicts schema)
    - tools/lib/class-matrix.mjs `rankCells` (to build the synthetic tie cases)
  </read_first>
  <action>
    Create `test/unit/class-pass-diff.test.js` (node:test, `assert` from `node:assert/strict`, `fs`/`path`/`url`/`os`/`child_process`). Load `docs/class-pass/before.json` and `before-depth20.json` once at module top via `fs.readFileSync` + `JSON.parse` (as BEFORE and, again, as the synthetic AFTER). Tests, each named as below:
    1. "bandFor: closed interval — exactly 0.75mu and 1.35mu are fine; strictly below/above are too weak/too strong": with mu = 3 assert `bandFor(0.75 * mu, mu) === "fine"`, `bandFor(1.35 * mu, mu) === "fine"`, `bandFor(0.75 * mu - 1e-9, mu) === "too weak"`, `bandFor(1.35 * mu + 1e-9, mu) === "too strong"`, `bandFor(null, mu) === null`; repeat the four edge calls with `mu = muOverRuns(before)`.
    2. "isCannotAct/rowBand: meanKills exactly 0.5 is not cannot-act; 0.49 is; stuck > 0 is; completed 0 is cannot-act with null depth stats and no NaN": synthetic rows `{ meanKills: 0.5, stuck: 0, completed: 40, meanDepth: 3 }` -> false; `{ meanKills: 0.49, ... }` -> true; `{ meanKills: 9, stuck: 1, completed: 39 }` -> true; an all-stuck row built with class-matrix's shape (`n: 40, completed: 0, stuck: 40, meanDepth: null, meanKills: null, reach5: null, p50Depth: null`) -> `isCannotAct` true and `rowBand(row, 3) === "cannot act"`; then build verdicts with a modified AFTER copy whose first cell is that all-stuck row and walk the whole verdicts object asserting no numeric value is NaN (`Number.isNaN`).
    3. "muOverRuns: run-weighted over cells with completed > 0, recomputed independently; null with no completed runs": recompute sum(meanDepth x completed)/sum(completed) inline over `before.cells` and assert `Math.abs(muOverRuns(before) - recomputed) < 1e-9`; assert a report whose cells all have completed 0 returns null.
    4. "metaParity: the natural pair is identical modulo commit; natural vs depth-20 reports seeds/startDepth/bot": `metaParity(before.meta, before.meta).ok === true`; `metaParity(before.meta, beforeDeep.meta).mismatches` includes `seeds`, `startDepth`, `bot`; a copy of before.meta with a different `commit` still reports ok.
    5. "ordering: subs/races/cells follow rankCells; re-ranking before.json's cells reproduces the stored rank; appendix is band-ordered then AFTER rank; ties break by key asc": `rankCells(before.cells).map(c => c.rank)` deep-equals `before.cells.map(c => c.rank)`; `rankRollupRows(before.rollups.bySub).map(r => r.key)` deep-equals `before.rollups.bySub.map(r => r.key)` (the harness already emits that order); a synthetic three-row rollup with identical meanDepth/p50/reach5 ranks by key asc; in the self-diff verdicts the `cells.outOfBand` band sequence is non-decreasing in `BAND_ORDER` index and, within a band, `rank` is strictly increasing.
    6. "missing BEFORE cell: delta null, Markdown shows n/a, no throw": delete one cell from a deep copy of BEFORE (and its `rollups.bySub` row) and build verdicts with the full file as AFTER — the matching row's `delta === null`, `before === null`, and `renderMarkdown(v, "after")` contains `n/a`.
    7. "self-diff: before vs before -> every delta is 0 or null; inBand + outOfBand.length === 143; verdict/reason/lever null on every row; no deep row carries a band or verdict": assert all of that on `buildVerdicts({ before, after: before, beforeDeep, afterDeep: beforeDeep })`; also `v.cannotAct.length === 0` and `v.meta.mu.after === v.meta.mu.before`.
    8. "mergeEditorial: copies verdict/reason/lever by key, rejects unknown verdict values, idempotent": prior with one sub row `{ key: <first sub key>, verdict: "revisit", reason: "r", lever: "l" }` and one race row `{ verdict: "accept", reason: "ok" }` -> merged rows carry those fields, every other row stays null; `mergeEditorial(v, { subs: [{ key, verdict: "maybe" }], races: [] })` throws; merging the output into a fresh build yields deep-equal objects.
    9. "renderMarkdown: outliers says the no-revisit sentence when empty and lists the revisit row with its lever when present; handoff has its heading and the accepted-but-strong list; after starts with the cannot-act headline and its seven H3 headings in order": assert the exact sentence `No revisit rows — every out-of-band row was accepted.` for the empty case; with the prior from test 8 the outliers table contains the key, `revisit`, `r` and `l`; `renderMarkdown(v, "handoff")` starts with `## Handoff to Phase 27`; `renderMarkdown(v, "after")` starts with `**Zero cannot-act cells.**` and its H3 headings, in order, are exactly the seven named in Task 1.
    10. "CLI determinism: two runs on the same inputs are byte-identical (stdout and --out-verdicts); --json equals the written file": `execFileSync(process.execPath, [SCRIPT, "--after", BEFORE, "--after-deep", BEFORE_DEEP, "--out-verdicts", tmp1])` twice into two temp files under `fs.mkdtempSync(path.join(os.tmpdir(), "class-pass-diff-"))`; assert stdout strings equal and the two files' bytes equal; run with `--json` and assert it parses deep-equal to the file.
    11. "CLI --gate: exit 0 with 'cannot-act cells: 0 of 143' on before.json; exit 3 listing the cell on a synthetic copy with one meanKills 0.3 cell": write the modified copy to the temp dir; use `spawnSync` to read `status` (3) and stdout containing the cell key.
    12. "purity: no Math.random/Date.now/new Date/process-spawning/engine or content imports on non-comment lines": same comment-stripping scan as class-matrix.test.js's purity test over `tools/class-pass-diff.mjs`.
    Clean up temp dirs in an `after` hook or `finally`. Then run `npm test` and confirm `# fail 0`. Commit both files (`feat(26-01): class-pass diff script + tests`).
  </action>
  <verify>
    <automated>node --test test/unit/class-pass-diff.test.js && node --test test/unit/class-matrix.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/class-pass-diff.test.js` exits 0 with `# pass` >= 12 and `# fail 0`.
    - `grep -c "bandFor(0.75 \* mu, mu)" test/unit/class-pass-diff.test.js` >= 1 and `grep -c "bandFor(1.35 \* mu, mu)" test/unit/class-pass-diff.test.js` >= 1 (edge cases are literal).
    - `grep -c "meanKills: 0.5" test/unit/class-pass-diff.test.js` >= 1 and `grep -c "meanKills: 0.49" test/unit/class-pass-diff.test.js` >= 1.
    - `grep -c "execFileSync\|spawnSync" test/unit/class-pass-diff.test.js` >= 2 (determinism and gate tests spawn the CLI).
    - `grep -c "No revisit rows — every out-of-band row was accepted." test/unit/class-pass-diff.test.js` >= 1.
    - `grep -c "Number.isNaN" test/unit/class-pass-diff.test.js` >= 1.
    - `npm test 2>&1 | tail -8` shows `# fail 0` and `# pass` >= 1342 (1330 measured before this phase + the 12 new tests).
    - `git status --porcelain engine content src mazeworld.html tools/lib tools/tune-classes.mjs` prints nothing and `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib; echo rc=$?` prints `rc=0`.
    - `git log -1 --format=%s` starts with `feat(26-01)` and `git show --stat --format= HEAD | grep -c "tools/class-pass-diff.mjs\|test/unit/class-pass-diff.test.js"` prints `2` (or the two files land in two consecutive commits that together touch exactly these two paths).
  </acceptance_criteria>
  <done>Every band/cannot-act edge, the null-not-NaN rule, the harness ordering, the missing-BEFORE tolerance, the editorial merge, the three Markdown sections, CLI byte-stability and the --gate exit code are pinned by tests that run in seconds without playing a single bot run; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON snapshot -> diff script | the script trusts the class-matrix.mjs JSON shape; a malformed or mismatched pair must be reported, never silently diffed |
| --verdicts file -> rendered ledger | editorial text is copied verbatim into Markdown; only three fields are ever copied and only by key |
| diff script -> ledger test (26-04) | the ledger test re-renders through this script, so non-determinism here would make the ledger test flaky |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01 | Tampering | ordering re-derived instead of reused | medium | mitigate | the ONLY comparator is class-matrix.mjs's rankCells (import asserted; re-ranking before.json reproduces its stored ranks) |
| T-26-02 | Tampering | band edge off-by-one (open vs closed interval) | high | mitigate | bandFor tested at exactly 0.75mu/1.35mu and 1e-9 either side; isCannotAct tested at exactly 0.5 and 0.49 |
| T-26-03 | Repudiation | non-deterministic output (timestamps, Map iteration, env) | high | mitigate | run-twice byte-equality test on stdout and the verdicts file; purity scan forbids Date/Math.random |
| T-26-04 | Tampering | script inventing editorial text | medium | mitigate | verdict/reason/lever emitted null; mergeEditorial copies only those fields and only by key; self-diff test asserts all null |
| T-26-05 | Denial of Service | NaN propagating from an all-stuck cell | medium | mitigate | null-not-NaN walk over the verdicts object with a synthetic all-stuck cell |
| T-26-06 | Information Disclosure | script or test shipping in the www bundle | low | accept | tools/ and test/ are never copied by tools/build-www.mjs (existing project rule) |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero deps) |
</threat_model>

<verification>
- `node --test test/unit/class-pass-diff.test.js` green (>= 12 tests) and `npm test` ends `# fail 0`.
- The synthetic self-diff (`--after docs/class-pass/before.json --after-deep docs/class-pass/before-depth20.json`) renders seven AFTER H3 headings, the no-revisit sentence, the handoff heading, and passes `--gate` with rc=0.
- Harness parity: `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib` exits 0; no engine/content/src change.
</verification>

<success_criteria>
- Plan 26-02 can run `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json` as its cannot-act gate, and Plan 26-03 can paste `--section after|outliers|handoff` output into the ledger with the accept/revisit column flowing from verdicts.json.
- Every PLAY-02 probe rule (closed band edges, 0.5 not cannot-act, completed-0 => cannot-act with null stats, missing BEFORE cell => n/a, harness tie-break ordering, byte-stability) is a passing test.
</success_criteria>

<output>
Create `.planning/phases/26-mass-playtest-class-pass-ledger/26-01-SUMMARY.md` when done. Record: the export list, the exact seven AFTER H3 headings as emitted, the verdicts.json schema as emitted, the test count, and the `npm test` pass count.
</output>
