---
phase: 26-mass-playtest-class-pass-ledger
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - docs/class-pass/after.json
  - docs/class-pass/after-depth20.json
  - docs/CLASS-PASS.md
autonomous: true
requirements: [PLAY-02]
tags: [class-pass, after-snapshot, capture, ledger, commit-pin, dev-only]

must_haves:
  truths:
    - "Section order in docs/CLASS-PASS.md is fixed and asserted: Bot proxy -> How to reproduce -> BEFORE -> Outliers/Findings -> Rulings -> AFTER -> Outliers (revisit) -> Handoff to Phase 27; the AFTER placeholder heading is replaced in place, not appended, so the existing anchors Phase 22-24 wrote remain valid."
    - "The AFTER matrix is captured at BEFORE volume with the SAME parameters — natural 143 x 40 seeds (`--seeds 40 --workers 4 --max-actions 5000`) and depth-20 143 x 10 (`--seeds 10 --workers 4 --max-actions 5000 --start-depth 20`) — and after.json's meta.bot is byte-identical to before.json's, after-depth20.json's to before-depth20.json's (26-CONTEXT.md, 'Capture both slices at BEFORE volume')."
    - "The engine is pinned to the post-Phase-25 HEAD that includes Plan 26-01's diff script: the ledger records the full and short hash, `git diff --quiet <pin> -- engine content src mazeworld.html tools` exited 0 immediately before each run and again after this plan's commit, and `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib` exited 0 (the harness is byte-identical to the BEFORE pin, so the parameters are comparable)."
    - "Zero cannot-act cells is checked BEFORE anything is written to the ledger: `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json` exits 0; if it exits 3 the plan STOPS (Deviation Rule 4 — present to the user), commits nothing, and reports the cell(s) with their top death causes instead of judging around them (26-CONTEXT.md, 'Cannot act is a hard gate')."
    - "Both AFTER JSONs are aggregates only (no per-run rows), carry meta.commit equal to the pin's short hash, 143 cells each, every cell n equal to its seed count, and the natural JSON has 0 stuck runs; the transcripts are pasted verbatim under the AFTER heading (ledger discipline, docs/DIFFICULTY-RETUNE.md)."
  artifacts:
    - path: "docs/class-pass/after.json"
      provides: "natural-start AFTER matrix aggregates (143 cells x 40 seeds, 5720 runs), commit-pinned"
      contains: "\"startDepth\": 1"
    - path: "docs/class-pass/after-depth20.json"
      provides: "depth-20 AFTER slice aggregates (143 cells x 10 seeds, 1430 runs), commit-pinned"
      contains: "\"startDepth\": 20"
    - path: "docs/CLASS-PASS.md"
      provides: "AFTER heading with the full pin hash (replaced in place), `### Pin and provenance (Phase 26 capture)`, and the two verbatim AFTER transcripts"
      contains: "### Pin and provenance (Phase 26 capture)"
  key_links:
    - from: "docs/CLASS-PASS.md"
      to: "docs/class-pass/after.json"
      via: "the AFTER heading's full hash starts with after.json meta.commit; the pasted natural transcript's Bot: line equals meta.bot"
      pattern: "Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5\\(caster\\)  potion<0.5  camp<0.5  seeds=40  workers=4  startDepth=1"
    - from: "docs/class-pass/after.json"
      to: "docs/class-pass/before.json"
      via: "metaParity (tools/class-pass-diff.mjs) — identical seeds/seedList/workers/maxActions/startDepth/exploreBudget/cells/bot, differing only in commit"
      pattern: "\"seedList\": \"i\\*7919\\+1\""
    - from: "docs/CLASS-PASS.md"
      to: "tools/class-pass-diff.mjs"
      via: "the provenance paragraph quotes the --gate command and its `cannot-act cells: 0 of 143` result"
      pattern: "class-pass-diff.mjs --gate"
  prohibitions:
    - "No engine, content, shell or harness code change — the AFTER capture is never taken on a tree that differs from the pin, and tools/tune-classes.mjs plus tools/lib stay byte-identical to 5565b22"
    - "Never commit a partial snapshot, a non-zero-EXIT run, or a run that passed the 90-minute (natural) / 45-minute (depth-20) abort threshold"
    - "No edits to any ledger section above the AFTER heading (Bot proxy, How to reproduce, BEFORE, Outliers/Findings, Rulings stay byte-identical)"
    - "No numbers typed by hand into the ledger — transcripts are pasted verbatim from the scratchpad files and every provenance figure is copied from command output"
    - "The phase stops on any cannot-act cell — no judging around it, no bot-policy tweak, no engine tweak, no commit"
    - "No fun-band verdict, no band word and no editorial sentence is written in this plan — Plan 26-03 owns the editorial column"
---

<objective>
Capture the PLAY-02 AFTER snapshot on the post-identity-pass, post-feedback engine with the Phase 22 harness at exactly BEFORE volume and parameters — natural 143 x 40 (5,720 runs) and depth-20 143 x 10 (1,430 runs), `--workers 4 --max-actions 5000`, engine pinned to the current HEAD (which now includes Plan 26-01's diff script under tools/) and proven clean before and after — then run the cannot-act hard gate, replace the ledger's AFTER placeholder heading in place with the pinned hash, write the provenance paragraph and paste both transcripts verbatim, and commit the ledger with both JSONs in one commit, mirroring Phase 22-04 step for step.

Purpose: PLAY-02 needs an AFTER matrix paired to BEFORE by seed, parameter and harness; the cannot-act gate is the milestone's promise (Phases 22-23 were built so it comes back zero). This plan measures; Plan 26-03 judges.
Output: `docs/class-pass/after.json`, `docs/class-pass/after-depth20.json`, the AFTER skeleton in `docs/CLASS-PASS.md`, one commit — or a STOP report if the gate fails.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md
@.planning/phases/22-class-aware-harness-before-matrix/22-04-SUMMARY.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-01-SUMMARY.md
@docs/DIFFICULTY-RETUNE.md
@tools/tune-classes.mjs
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `tools/class-pass-diff.mjs` — dev-only diff script (`--before/--after/--before-deep/--after-deep/--verdicts/--out-verdicts/--section/--json/--gate`). [26-01]
- `test/unit/class-pass-diff.test.js`. [26-01]
- `docs/class-pass/after.json`, `docs/class-pass/after-depth20.json` — AFTER aggregates, commit-pinned, same `Bot:` lines as BEFORE (this plan). [26-02]
- `docs/CLASS-PASS.md` AFTER skeleton: heading `## AFTER — commit <full hash> (Phase 26 — PLAY-02)` replaced in place, `### Pin and provenance (Phase 26 capture)`, `### AFTER transcript — tune-classes --seeds 40 --workers 4 --max-actions 5000`, `### AFTER transcript — tune-classes --seeds 10 --workers 4 --max-actions 5000 --start-depth 20` (this plan). [26-02]
- `docs/class-pass/verdicts.json` + the AFTER comparison block + `## Outliers (Phase 26 — revisit list)` + `## Handoff to Phase 27`. [26-03]
- `test/unit/class-pass-ledger.test.js`. [26-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pin the engine, prove it clean, run the natural-start AFTER matrix in the background, run the cannot-act gate</name>
  <files>docs/class-pass/after.json</files>
  <read_first>
    - .planning/phases/22-class-aware-harness-before-matrix/22-04-SUMMARY.md (the BEFORE capture's exact discipline and timings: 687.2s natural / 38.4s depth-20 on 4 workers; sentinel files; bounded polling; one ledger commit)
    - docs/DIFFICULTY-RETUNE.md lines 87-112 (the pin/diff/sentinel/poll paragraph shape this ledger mirrors)
    - docs/CLASS-PASS.md lines 101-135 (How to reproduce + the BEFORE pin paragraph — the AFTER provenance paragraph mirrors it) and lines 860-879 (the AFTER placeholder this plan replaces)
    - tools/tune-classes.mjs header (flags, JSON shape, the stderr `elapsed:` line, atomic `--out` write)
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md, section "The capture and the ledger" and "Outliers and the handoff" (the cannot-act hard gate)
  </read_first>
  <action>
    Preflight (every line must hold before launching; stop and fix if any fails — never capture on a dirty or unpinned tree):
    1. `git status --porcelain` prints nothing (Plan 26-01 is committed; nothing else is pending).
    2. `npm test` ends with `# fail 0`.
    3. `PIN=$(git rev-parse HEAD)`; `PIN_SHORT=$(git rev-parse --short HEAD)`; `git diff --quiet "$PIN" -- engine content src mazeworld.html tools; echo rc=$?` prints `rc=0`; `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib; echo rc=$?` prints `rc=0` (harness byte-identical to the BEFORE pin); record `git log --oneline 5565b22.."$PIN" -- engine content src mazeworld.html | wc -l` (the count of engine/content/shell commits between the two pins — Phases 23-25) and `git diff --stat 5565b22 "$PIN" -- engine content src mazeworld.html | tail -1` for the provenance paragraph. Record PIN and PIN_SHORT — the ledger commit in Task 3 moves HEAD, so never re-derive the pin from HEAD afterwards.
    4. `docs/class-pass/` exists (it does); `docs/class-pass/after.json` must NOT exist yet (`test ! -f docs/class-pass/after.json`).

    Launch (background, sentinel, bounded polling — never a foreground wait): run `node tools/tune-classes.mjs --seeds 40 --workers 4 --max-actions 5000 --out docs/class-pass/after.json > "<scratchpad>/after-natural.txt" 2>&1; echo "EXIT=$?" >> "<scratchpad>/after-natural.txt"` with the Bash tool's `run_in_background: true`, where `<scratchpad>` is this session's scratchpad directory (forward slashes). The Bash tool re-invokes you when the background command exits — that notification is the completion signal; the `EXIT=` line is the proof. While waiting, poll only in bounded checks (a short `tail -3` of the transcript, no more often than every 60 s, no single Bash call longer than 60 s; never a blocking sleep). Abort threshold: 90 minutes wall (expected ~12-20 min: BEFORE took 687.2 s and casters now act, so runs are somewhat longer). If the threshold passes, kill the process, record the partial progress in the SUMMARY, and STOP — never commit a partial snapshot.

    On `EXIT=0`, verify the JSON exactly like 22-04 did: parses; `meta.seeds === 40`, `meta.startDepth === 1`, `meta.maxActions === 5000`, `meta.workers === 4`, `meta.exploreBudget === 50`, `cells.length === 143`, `meta.commit === PIN_SHORT`, no object anywhere has a `rows` key, every cell `n === 40`; `meta.bot` is byte-identical to `before.json`'s `meta.bot`; the stderr `elapsed:` line is in the transcript. Keep the transcript file — Task 3 pastes it verbatim.

    Cannot-act hard gate (26-CONTEXT.md): run `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json; echo rc=$?`. Expected `cannot-act cells: 0 of 143` and `rc=0`. If rc is 3: this is a Deviation Rule 4 STOP, not a Rule 1-3 auto-fix — do NOT run Task 2, do NOT edit the ledger, do NOT commit anything, do NOT touch the bot or the engine. Leave `docs/class-pass/after.json` on disk untracked as evidence, and end the plan by presenting to the user: the gate's printed cell list (key, kills, stuck, completed, top causes), the pin hash, the transcript path, and the sentence that the fix is an engine change outside Phase 26's scope (a gap-closure phase must land it and the AFTER must then be re-captured on the new pin). Also record it via the SUMMARY's Issues Encountered and `state.add-blocker`.
  </action>
  <verify>
    <automated>node -e "const r=require('./docs/class-pass/after.json');const b=require('./docs/class-pass/before.json');const walk=(o)=>o&&typeof o==='object'?Object.keys(o).some(k=>k==='rows')||Object.values(o).some(walk):false;console.log(r.meta.seeds,r.meta.startDepth,r.meta.maxActions,r.meta.workers,r.cells.length,walk(r),r.cells.every(c=>c.n===40),r.meta.bot===b.meta.bot)" && node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json</automated>
  </verify>
  <acceptance_criteria>
    - The verify command's first line prints `40 1 5000 4 143 false true true` and its gate line prints `cannot-act cells: 0 of 143` with exit code 0.
    - `test "$(node -e "console.log(require('./docs/class-pass/after.json').meta.commit)")" = "$PIN_SHORT"` succeeds, where PIN_SHORT was recorded in preflight (NOT re-derived from HEAD).
    - `git diff --quiet "$PIN" -- engine content src mazeworld.html tools; echo rc=$?` prints `rc=0` and `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib; echo rc=$?` prints `rc=0`.
    - The scratchpad transcript file ends with `EXIT=0`, contains exactly one line starting with `Bot: exploreBudget=50  maxActions=5000  party=off`, that line ends with `seeds=40  workers=4  startDepth=1`, and contains one line starting with `elapsed: `.
    - `node -e "const r=require('./docs/class-pass/after.json');console.log(r.cells.reduce((s,c)=>s+c.stuck,0), r.cells.filter(c=>c.completed===0).length, r.cells.filter(c=>c.meanKills!==null&&c.meanKills<0.5).length)"` prints `0 0 0`.
    - `git status --porcelain` prints exactly one line, `?? docs/class-pass/after.json` (nothing committed yet; nothing else changed).
  </acceptance_criteria>
  <done>The natural-start AFTER matrix (143 x 40) completed against the pinned, proven-clean engine with the BEFORE Bot: line byte-for-byte; its aggregate JSON is on disk with the correct meta; the cannot-act gate passed with zero cells (or the plan stopped and reported).</done>
</task>

<task type="auto">
  <name>Task 2: Run the depth-20 AFTER slice in the background</name>
  <files>docs/class-pass/after-depth20.json</files>
  <read_first>
    - The Task 1 transcript file (to reuse the exact same launch/poll discipline and to confirm the depth-20 Bot: line differs from Task 1's ONLY in `seeds=` and `startDepth=`)
    - docs/CLASS-PASS.md lines 335-345 and 480-535 (the BEFORE depth-20 transcript's extra `gained(mean) gained(p50) survived` columns and its Bot: line, for the parity check)
    - tools/tune-classes.mjs header (`--start-depth`; the deep columns appear only when startDepth > 1)
  </read_first>
  <action>
    Re-check `git status --porcelain` prints only `?? docs/class-pass/after.json` and `git diff --quiet "$PIN" -- engine content src mazeworld.html tools` still exits 0. Launch in the background exactly like Task 1: `node tools/tune-classes.mjs --seeds 10 --workers 4 --max-actions 5000 --start-depth 20 --out docs/class-pass/after-depth20.json > "<scratchpad>/after-depth20.txt" 2>&1; echo "EXIT=$?" >> "<scratchpad>/after-depth20.txt"`. Poll in bounded checks until `EXIT=<code>`; abort threshold 45 minutes (BEFORE took 38.4 s; deep deaths are fast). On `EXIT=0` verify: parses; `meta.seeds === 10`, `meta.startDepth === 20`, `meta.workers === 4`, `meta.maxActions === 5000`, `cells.length === 143`, `meta.commit === PIN_SHORT`, no `rows` key anywhere, every cell `n === 10`, every non-null `meanDepth >= 20`, `meta.bot` byte-identical to `before-depth20.json`'s `meta.bot`; the transcript's table has the `gained(mean)  gained(p50)  survived` columns and its `Bot:` line ends with `seeds=10  workers=4  startDepth=20`. Record the total stuck count for the deep slice (`sum of cell.stuck`) for the provenance paragraph — the cannot-act gate is defined on the natural matrix only (deep cells routinely have meanKills under 0.5 by design), but a deep `stuck > 0` is still reported verbatim in the ledger, never hidden. Do not commit yet.
  </action>
  <verify>
    <automated>node -e "const r=require('./docs/class-pass/after-depth20.json');const b=require('./docs/class-pass/before-depth20.json');const walk=(o)=>o&&typeof o==='object'?Object.keys(o).some(k=>k==='rows')||Object.values(o).some(walk):false;console.log(r.meta.seeds,r.meta.startDepth,r.cells.length,walk(r),r.cells.every(c=>c.n===10),r.cells.every(c=>c.meanDepth===null||c.meanDepth>=20),r.meta.bot===b.meta.bot)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `10 20 143 false true true true`.
    - `test "$(node -e "console.log(require('./docs/class-pass/after-depth20.json').meta.commit)")" = "$PIN_SHORT"` succeeds.
    - The scratchpad transcript ends with `EXIT=0`, its `Bot:` line ends with `seeds=10  workers=4  startDepth=20`, and it contains one line starting with `elapsed: `.
    - `node -e "const a=require('./docs/class-pass/after.json').meta,d=require('./docs/class-pass/after-depth20.json').meta;console.log(a.commit===d.commit, a.bot.replace(/seeds=\d+/,'seeds=X').replace(/startDepth=\d+/,'startDepth=X')===d.bot.replace(/seeds=\d+/,'seeds=X').replace(/startDepth=\d+/,'startDepth=X'))"` prints `true true` (same pin, identical bot parameters apart from seeds/startDepth).
    - `git status --porcelain` prints exactly two lines: `?? docs/class-pass/after.json` and `?? docs/class-pass/after-depth20.json`.
  </acceptance_criteria>
  <done>The depth-20 AFTER slice (143 x 10) completed against the same pinned engine with the BEFORE depth-20 Bot: line byte-for-byte; its aggregate JSON and transcript are on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the AFTER placeholder in place (pin heading, provenance, verbatim transcripts) and make the single AFTER commit</name>
  <files>docs/CLASS-PASS.md, docs/class-pass/after.json, docs/class-pass/after-depth20.json</files>
  <read_first>
    - docs/CLASS-PASS.md lines 119-135 (the BEFORE pin paragraph — mirror its voice and its proof sentences exactly) and lines 860-879 (the AFTER placeholder: heading + body, all of which this task replaces)
    - Both scratchpad transcripts from Tasks 1 and 2 (pasted verbatim, including the `Stuck:`, `Bot:`, `elapsed:` and `EXIT=0` lines, exactly as the BEFORE section did)
    - .planning/phases/22-class-aware-harness-before-matrix/22-04-PLAN.md Task 3 action (the ledger-writing and single-commit discipline)
  </read_first>
  <action>
    Edit `docs/CLASS-PASS.md` with scoped Edit calls only (never a whole-file Write; never touch any line above the AFTER heading):
    1. Replace the placeholder heading line `## AFTER — commit \`<hash>\` (Phase 26 — PLAY-02)` IN PLACE with `## AFTER — commit <PIN full 40-hex> (Phase 26 — PLAY-02)` (same shape as the BEFORE heading: bare hash, no backticks). It stays the sixth H2 in the file; nothing is appended above or below any earlier section.
    2. Replace the placeholder body (from the line after the heading through the final `Nothing recorded yet.` line — everything that was under the placeholder) with, in this order:
       - a blank line, then the sub-heading `### Pin and provenance (Phase 26 capture)` and one paragraph in the BEFORE pin paragraph's voice: captured date; full hash and `(short \`<PIN_SHORT>\`)`; `git diff --quiet <PIN> -- engine content src mazeworld.html tools` exited 0 immediately before both runs and again after this ledger's own commit; `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib` exited 0 — the harness (bot policy, cell enumeration, ranking, JSON shape) is byte-identical to the BEFORE pin, so `seeds=40/10, workers=4, maxActions=5000, exploreBudget=50, startDepth=1/20` and the seed list `i*7919+1` are the same runs re-played on the new engine; the engine/content/shell commits between `5565b22` and the pin are Phases 23 (casters can act), 24 (one good, one bad per sub-class and race) and 25 (feature feedback) — quote the commit count and the `git diff --stat` summary line recorded in Task 1; both `Bot:` lines are byte-identical to the BEFORE section's two `Bot:` lines (state the `node -e` equality checks printed `true`); wall times from the two `elapsed:` lines; the cannot-act gate: `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json` printed `cannot-act cells: 0 of 143` (exit 0) and the depth-20 slice's stuck total; then the sentence that the machine-derived comparison tables, the fun-band verdicts and the Phase 27 handoff are rendered by `node tools/class-pass-diff.mjs --verdicts docs/class-pass/verdicts.json --out-verdicts docs/class-pass/verdicts.json --section after` (and `--section outliers`, `--section handoff`) and pasted verbatim above this sub-heading by Plan 26-03 — numbers are never retyped, the accept/revisit sentences in `verdicts.json` are the one hand-written column. Every number in this paragraph is copied from a command's output recorded in Tasks 1-2, none is typed from memory.
       - `### AFTER transcript — tune-classes --seeds 40 --workers 4 --max-actions 5000` followed by the Task 1 stdout+stderr transcript pasted verbatim inside a fenced block (including its `Stuck:`, `Bot:`, `elapsed:` and `EXIT=0` lines).
       - `### AFTER transcript — tune-classes --seeds 10 --workers 4 --max-actions 5000 --start-depth 20` followed by the Task 2 transcript verbatim in a fenced block.
       - one closing line naming `docs/class-pass/after.json` and `docs/class-pass/after-depth20.json` as the machine-diffable aggregates (aggregates only, no per-run rows), mirroring the BEFORE section's closing line.
    3. Confirm the file still has exactly six H2 headings in the original order (Bot proxy, How to reproduce, BEFORE, Outliers / Findings, Rulings, AFTER) and that everything above the AFTER heading is byte-identical to HEAD (`git diff HEAD -- docs/CLASS-PASS.md` shows changes only at/after the AFTER heading line).
    Then commit ALL THREE files in ONE commit via the GSD commit helper (message: `docs(26-02): AFTER class matrix — 143x40 natural + 143x10 depth-20, engine pinned <PIN_SHORT>`). After the commit, re-run `git diff --quiet <PIN> -- engine content src mazeworld.html tools` and record its exit code (must be 0) in the SUMMARY.
  </action>
  <verify>
    <automated>PIN_SHORT=$(node -e "console.log(require('./docs/class-pass/after.json').meta.commit)") && grep -c "^## AFTER — commit [0-9a-f]\{40\} (Phase 26 — PLAY-02)$" docs/CLASS-PASS.md && grep -c "^### Pin and provenance (Phase 26 capture)$" docs/CLASS-PASS.md && grep -c "$(node -e "console.log(require('./docs/class-pass/after.json').meta.bot)")" docs/CLASS-PASS.md && git diff --quiet "$(grep -oE '^## AFTER — commit [0-9a-f]{40}' docs/CLASS-PASS.md | grep -oE '[0-9a-f]{40}')" -- engine content src mazeworld.html tools && echo ENGINE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " docs/CLASS-PASS.md` prints `6` and `grep -n "^## " docs/CLASS-PASS.md | awk -F: '{print $2}' | paste -sd '|'` starts with `## Bot proxy (HARN-02)|## How to reproduce|## BEFORE — commit 5565b222564f8ee2a944e03c1e4a3c27cae09b40 (pre-identity-pass engine)|## Outliers / Findings (Phase 22)|## Rulings (Phase 24 — PLAY-03)|## AFTER — commit ` (order preserved; heading replaced in place).
    - `grep -c "^## AFTER — commit [0-9a-f]\{40\} (Phase 26 — PLAY-02)$" docs/CLASS-PASS.md` prints `1`; `grep -c '<hash>' docs/CLASS-PASS.md` prints `0`; `grep -c "Nothing recorded yet" docs/CLASS-PASS.md` prints `0`.
    - With `H=$(grep -oE '^## AFTER — commit [0-9a-f]{40}' docs/CLASS-PASS.md | grep -oE '[0-9a-f]{40}')` and `PIN_SHORT=$(node -e "console.log(require('./docs/class-pass/after.json').meta.commit)")`: `test "${H:0:${#PIN_SHORT}}" = "$PIN_SHORT"` succeeds; `git rev-parse --verify "$H^{commit}"` succeeds; `grep -c "short \`$PIN_SHORT\`" docs/CLASS-PASS.md` >= 1.
    - `grep -c "^### Pin and provenance (Phase 26 capture)$" docs/CLASS-PASS.md` prints `1`; `grep -c "^### AFTER transcript — tune-classes --seeds 40 --workers 4 --max-actions 5000$" docs/CLASS-PASS.md` prints `1`; `grep -c "^### AFTER transcript — tune-classes --seeds 10 --workers 4 --max-actions 5000 --start-depth 20$" docs/CLASS-PASS.md` prints `1`.
    - `grep -c "$(node -e "console.log(require('./docs/class-pass/after.json').meta.bot)")" docs/CLASS-PASS.md` prints `2` (the BEFORE natural line and the AFTER natural line are byte-identical, so the same string now appears twice) and `grep -c "$(node -e "console.log(require('./docs/class-pass/after-depth20.json').meta.bot)")" docs/CLASS-PASS.md` prints `2`.
    - `grep -c "^EXIT=0$" docs/CLASS-PASS.md` prints `4` (two BEFORE transcripts + two AFTER transcripts); `grep -c "^elapsed: " docs/CLASS-PASS.md` prints `4`.
    - `grep -c "class-pass-diff.mjs --gate" docs/CLASS-PASS.md` >= 1 and `grep -c "cannot-act cells: 0 of 143" docs/CLASS-PASS.md` >= 1.
    - `git diff HEAD~1 -- docs/CLASS-PASS.md | grep -c '^@@'` prints `1` (a single hunk) and that hunk header's old-side start line (`@@ -N,…`) has N >= 860 — the placeholder heading's line in HEAD~1 — so nothing above the AFTER heading changed; additionally `git diff HEAD~1 -- docs/CLASS-PASS.md | grep '^-' | grep -v '^---' | wc -l` equals the placeholder body's line count (20: heading + 19 body lines), i.e. only the placeholder was removed.
    - `git status --porcelain` prints nothing; `git log -1 --format=%s` starts with `docs(26-02): AFTER class matrix`; `git show --stat --format= HEAD | grep -c "docs/CLASS-PASS.md\|docs/class-pass/after.json\|docs/class-pass/after-depth20.json"` prints `3`.
    - `git diff --quiet "$H" -- engine content src mazeworld.html tools; echo rc=$?` prints `rc=0` after the ledger commit; `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib; echo rc=$?` prints `rc=0`; `git log --oneline 1b4daed..HEAD -- test/parity/prototype-master.js.txt | wc -l` prints `0`.
    - `npm test 2>&1 | tail -8` still shows `# fail 0` (docs-only commit).
  </acceptance_criteria>
  <done>The ledger's AFTER placeholder is replaced in place with the pinned heading, a provenance paragraph whose every number came from a recorded command, and both verbatim transcripts; the ledger and both AFTER JSONs are committed together on an engine tree proven identical to the pin before and after — Plan 26-03 can now render and judge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> snapshot | the capture is only meaningful on a committed, clean tree matching the pin — including tools/, since Plan 26-01 added a file there |
| tool output -> ledger | transcripts are pasted verbatim; provenance numbers are copied from command output; a hand edit would break the grep-diff contract |
| natural matrix -> cannot-act gate | the gate reads after.json only; a gate bypass would let Plan 26-03 judge around a broken cell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-07 | Repudiation | AFTER provenance (which engine produced these numbers) | critical | mitigate | full hash in the heading + meta.commit in both JSONs; git diff --quiet against the pin over engine content src mazeworld.html tools before each run and after the commit; harness parity vs 5565b22 proven and quoted |
| T-26-08 | Tampering | parameter drift between BEFORE and AFTER | high | mitigate | Bot: lines asserted byte-identical to BEFORE's via node -e; meta parity re-asserted by Plan 26-04's ledger test |
| T-26-09 | Tampering | partial or non-zero-EXIT snapshot committed | high | mitigate | EXIT=0 sentinel required; abort thresholds; --out is tmp-then-rename; three files in one commit |
| T-26-10 | Tampering | judging around a cannot-act cell | high | mitigate | --gate runs before any ledger edit; rc=3 => Rule 4 STOP, no commit, cells + top causes presented to the user |
| T-26-11 | Denial of Service | runaway or hung matrix run | medium | mitigate | 90-min / 45-min abort thresholds; bounded polling; background execution with sentinel |
| T-26-12 | Information Disclosure | ledger contents | low | accept | aggregate numbers about a fictional dungeon; no personal data |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | no installs in this plan |
</threat_model>

<verification>
- Both AFTER JSONs parse, carry `meta.commit` = the pin's short hash, 143 cells each, no `rows` key, every cell n = seeds, natural stuck total 0, `meta.bot` byte-identical to the matching BEFORE file.
- `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json` exits 0 with `cannot-act cells: 0 of 143`.
- `docs/CLASS-PASS.md` has six H2 headings in the original order, the AFTER heading carries the full pin hash, the provenance and both transcript sub-headings exist, all four `EXIT=0` lines are present, and nothing above the AFTER heading changed.
- `git diff --quiet <pin> -- engine content src mazeworld.html tools` exits 0 after the ledger commit; working tree clean; `npm test` green.
</verification>

<success_criteria>
- An AFTER matrix (natural + depth-20) is committed to the ledger against a commit-pinned engine, at BEFORE volume and parameters, with zero cannot-act cells proven by the gate — or the plan stopped and reported the cells instead of judging around them.
- Plan 26-03 can run the diff script on the committed pair and paste its output above the provenance sub-heading.
</success_criteria>

<output>
Create `.planning/phases/26-mass-playtest-class-pass-ledger/26-02-SUMMARY.md` when done. Record: the pin (full + short), both wall times, the natural and depth-20 stuck totals, the `--gate` output line, the engine/content/shell commit count between 5565b22 and the pin, the ledger commit hash, and the post-commit `git diff --quiet` exit code. If the gate failed: the cell list with top causes, and the explicit statement that Plans 26-03/26-04 must not start until an engine fix lands and the AFTER is re-captured on a new pin.
</output>
