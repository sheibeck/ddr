---
phase: 26-mass-playtest-class-pass-ledger
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - test/unit/class-pass-ledger.test.js
  - docs/CLASS-PASS.md
autonomous: true
requirements: [PLAY-03, PLAY-02]
tags: [class-pass, ledger, standing-test, structure, cannot-act, provenance]

must_haves:
  truths:
    - "The AFTER section's commit hash equals after.json meta.commit and after-depth20.json meta.commit (the same pin); the good/bad table remains the 30-row table Phase 24 transcribed (not duplicated); the Outliers section lists exactly the rows marked revisit in verdicts.json (no more, no fewer) — asserted by the ledger test."
    - "If the revisit list is empty the Outliers section says so explicitly ('No revisit rows — every out-of-band row was accepted') rather than being omitted; every required section heading exists even when its content is a single line; the ledger test asserts presence of BEFORE / Rulings / AFTER / Outliers / Handoff headings."
    - "Section order in docs/CLASS-PASS.md is fixed and asserted: Bot proxy -> How to reproduce -> BEFORE -> Outliers/Findings -> Rulings -> AFTER -> Outliers (revisit) -> Handoff to Phase 27; the AFTER placeholder heading is replaced in place, not appended, so the existing anchors Phase 22-24 wrote remain valid."
    - "A cell with completed 0 (all runs stuck) is cannot-act by the stuck rule and its depth stats are null, never NaN; the diff script tolerates a missing BEFORE cell (reports delta as n/a) but the ledger test asserts BEFORE and AFTER have identical cell key sets (143) and identical meta parameters modulo commit; the cannot-act gate is asserted against after.json as a hard zero."
    - "The ledger test asserts structure, provenance and the cannot-act invariant — it never asserts a balance number (no mean depth, rank, band count or verdict count is pinned), so the suite stays a sanity floor, not a CI gate on balance (26-CONTEXT.md 'Established Patterns')."
    - "The ledger's AFTER, Outliers (Phase 26) and Handoff blocks are byte-identical to a fresh render of tools/class-pass-diff.mjs from the committed JSONs and verdicts.json — a standing proof that numbers were never retyped; every `### Ruling:` heading for IDENT-10 and Freeze exists and the Rulings section names IDENT-05, IDENT-06, IDENT-07, IDENT-09 and IDENT-10."
  artifacts:
    - path: "test/unit/class-pass-ledger.test.js"
      provides: "standing structural/provenance guard for docs/CLASS-PASS.md + docs/class-pass/*.json"
      min_lines: 140
  key_links:
    - from: "test/unit/class-pass-ledger.test.js"
      to: "tools/class-pass-diff.mjs"
      via: "imports metaParity, cannotActCells, cellKey, buildVerdicts, renderMarkdown to re-derive parity, the cannot-act gate and the three rendered blocks"
      pattern: "from \"../../tools/class-pass-diff.mjs\""
    - from: "test/unit/class-pass-ledger.test.js"
      to: "docs/CLASS-PASS.md"
      via: "fs.readFileSync + heading/section parsing (H2 order, AFTER hash, Outliers rows, good/bad table row count)"
      pattern: "docs\", \"CLASS-PASS.md\""
    - from: "test/unit/class-pass-ledger.test.js"
      to: "docs/class-pass/after.json + after-depth20.json + before.json + before-depth20.json + verdicts.json"
      via: "JSON.parse of all five; identical 143 cell keys; meta parity modulo commit; zero cannot-act; verdicts schema and revisit set"
      pattern: "verdicts.json"
  prohibitions:
    - "The ledger test never asserts a balance number — no death-depth mean, rank, band count, out-of-band count or verdict count is pinned; it is not a CI gate on balance"
    - "Never weaken an assertion to make the ledger pass — if a structural assertion fails, fix docs/CLASS-PASS.md (or re-render verdicts.json through the script) to the contract"
    - "No harness, engine, content or shell change; tools/tune-classes.mjs and tools/lib stay byte-identical to 5565b22"
    - "No edit to any ledger content other than a structural fix a failing assertion demands (and none is expected)"
---

<objective>
Turn the ledger's contract into a standing test — `test/unit/class-pass-ledger.test.js` — so the class-pass ledger can never silently drift: section presence and fixed order (eight H2s, Handoff last), the AFTER placeholder replaced in place (no `<hash>`, no "Nothing recorded yet"), the Rulings anchors (sub-class/race rulings, the IDENT-10 dagger KEEP and Freeze rulings, IDENT-05/06/07/09/10 named), the 30-row good/bad table present exactly once, the AFTER heading hash equal to both AFTER JSONs' `meta.commit`, BEFORE/AFTER meta parity modulo commit, identical 143-cell key sets, ZERO cannot-act cells in after.json (hard), verdicts.json's schema and editorial rules, the Outliers (Phase 26) rows equal to the revisit set, and the three rendered blocks byte-identical to a fresh render of the diff script. Then close the phase with a full suite run and the final pin proof.

Purpose: PLAY-03's ledger is a design record other phases (27, v1.3) build on; the test makes its structure, provenance and the zero-cannot-act promise checkable forever without ever gating on a balance number.
Output: `test/unit/class-pass-ledger.test.js`, one commit, `npm test` green, engine tree proven identical to the pin at phase close.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-03-SUMMARY.md
@tools/class-pass-diff.mjs
@test/unit/class-pass-diff.test.js
@test/parity/fixture-inventory.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `tools/class-pass-diff.mjs` (`--before/--after/--before-deep/--after-deep/--verdicts/--out-verdicts/--section/--json/--gate`) + `test/unit/class-pass-diff.test.js`. [26-01]
- `docs/class-pass/after.json`, `docs/class-pass/after-depth20.json`; ledger AFTER heading (full pin hash), `### Pin and provenance (Phase 26 capture)`, both verbatim transcripts. [26-02]
- `docs/class-pass/verdicts.json` (schema `class-pass-verdicts/1`, editorial verdict/reason/lever); ledger AFTER comparison block, `## Outliers (Phase 26 — revisit list)`, `## Handoff to Phase 27`. [26-03]
- `test/unit/class-pass-ledger.test.js` — the standing ledger guard (this plan). [26-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: test/unit/class-pass-ledger.test.js — section order, anchors, 30-row table once, AFTER hash, parity, 143 keys, zero cannot-act, verdicts rules, Outliers == revisit, blocks byte-identical to a fresh render</name>
  <files>test/unit/class-pass-ledger.test.js</files>
  <read_first>
    - test/unit/class-pass-diff.test.js (REPO_ROOT resolution, how the JSONs are loaded once at module top, the import list from the diff script)
    - test/parity/fixture-inventory.test.js header and DOC_PATH pattern (a doc-consistency test that reads a Markdown file from the repo and compares it to a machine rendering)
    - docs/CLASS-PASS.md: the eight `## ` headings (grep them; do not read the transcripts), the `### Good / bad table` block (header row `| Sub / Race | GOOD | BAD |`, spaced separator row, 30 data rows), the `### Ruling:` headings, the AFTER section's first lines, the `## Outliers (Phase 26 — revisit list)` section body, the `## Handoff to Phase 27` sub-headings
    - tools/class-pass-diff.mjs exports (`metaParity`, `cannotActCells`, `cellKey`, `buildVerdicts`, `renderMarkdown`, `isCannotAct`) and the verdicts.json schema in its header
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md "Ledger structural test" bullet (the locked assertion list)
  </read_first>
  <action>
    Create `test/unit/class-pass-ledger.test.js` (node:test, `assert` from `node:assert/strict`, `fs`/`path`/`url`; header comment: PLAY-03 standing guard — asserts STRUCTURE, PROVENANCE and the cannot-act INVARIANT of the class-pass ledger, never a balance number; if it fails after an intentional ledger change, re-render through tools/class-pass-diff.mjs rather than editing the assertion). Load once at module top: `docs/CLASS-PASS.md` as text, and `before.json`, `before-depth20.json`, `after.json`, `after-depth20.json`, `verdicts.json` via `JSON.parse(fs.readFileSync(...))`. Helper `section(doc, headingRegex)` returning the text from a matching `## ` heading to the next `## ` heading (or EOF); helper `h2s(doc)` returning the list of `## ` heading lines in order. Tests, named as below:
    1. "sections: exactly eight H2 headings, in the fixed order, Handoff last": `h2s(doc)` has length 8 and, matched in order against these regexes, each matches: `^## Bot proxy`, `^## How to reproduce$`, `^## BEFORE — commit [0-9a-f]{40} `, `^## Outliers / Findings \(Phase 22\)$`, `^## Rulings \(Phase 24 — PLAY-03\)$`, `^## AFTER — commit [0-9a-f]{40} \(Phase 26 — PLAY-02\)$`, `^## Outliers \(Phase 26 — revisit list\)$`, `^## Handoff to Phase 27$`.
    2. "AFTER placeholder was replaced in place, BEFORE/Rulings anchors intact": the doc contains no literal `<hash>` and no `Nothing recorded yet`; the BEFORE heading's 40-hex hash starts with `before.meta.commit` and equals `beforeDeep.meta.commit`'s prefix; the BEFORE section contains `before.meta.bot` and `beforeDeep.meta.bot` verbatim; the Rulings section contains `### Sub-class rulings`, `### Race rulings`, `### Ruling: level-1 Thief dagger (IDENT-10) — KEEP`, `### Ruling: Freeze pays out`, `### Good / bad table` and mentions each of `IDENT-05`, `IDENT-06`, `IDENT-07`, `IDENT-09`, `IDENT-10`.
    3. "good/bad table: exactly 30 data rows, present exactly once": within the Rulings section, the lines between `### Good / bad table` and the next `### ` that start with `| ` number 32 (header + separator + 30), and the header row `| Sub / Race | GOOD | BAD |` occurs exactly once in the whole doc.
    4. "AFTER hash equals after.json and after-depth20.json meta.commit": the AFTER heading's 40-hex hash starts with `after.meta.commit`; `after.meta.commit === afterDeep.meta.commit`; the AFTER section contains the literal `` `<after.meta.commit>` `` (backticked short hash) and both `after.meta.bot` and `afterDeep.meta.bot` verbatim; the section contains `### Pin and provenance (Phase 26 capture)` and two `### AFTER transcript — tune-classes` sub-headings; its first non-blank line starts with `**Zero cannot-act cells.**`.
    5. "meta parity modulo commit (natural pair and depth-20 pair)": `metaParity(before.meta, after.meta).ok === true` and `metaParity(beforeDeep.meta, afterDeep.meta).ok === true` (the message prints `mismatches`); `after.meta.bot === before.meta.bot`; `afterDeep.meta.bot === beforeDeep.meta.bot`; `before.meta.commit !== after.meta.commit` (they are different pins — a self-diff would be a mistake).
    6. "identical 143 cell keys in every pair": the sets of `cellKey(cell)` for before/after and beforeDeep/afterDeep are equal and of size 143; `after.meta.cells === 143`.
    7. "ZERO cannot-act cells in after.json (hard)": `cannotActCells(after)` deep-equals `[]` with a message listing any offenders (key, meanKills, stuck, topCauses); additionally `after.cells.every(c => c.stuck === 0 && c.completed === c.n && c.meanKills !== null && c.meanKills >= 0.5)`; `afterDeep.cells.every(c => c.stuck === 0)` (the deep slice has no stuck run either; its kills are not gated); no `NaN` anywhere in either AFTER file (walk).
    8. "verdicts.json: schema, commits, editorial rules": `verdicts.schema === "class-pass-verdicts/1"`; `verdicts.meta.after.commit === after.meta.commit` and `verdicts.meta.before.commit === before.meta.commit`; `subs.length === 24`, `races.length === 6`, `cannotAct.length === 0`; `cells.inBand + cells.outOfBand.length === 143`; every sub/race row with `band !== "fine"` has `verdict` in `{accept, revisit}` and a non-empty `reason`; every `revisit` row has a non-empty `lever`; no `band === "fine"` row has `verdict === "revisit"`; every row's `band` is one of `fine | too weak | too strong | cannot act` and none is `cannot act`; no `deep.*` row has a `band` or `verdict` key. (No count of out-of-band rows, revisit rows or accept rows is asserted — those are balance facts.)
    9. "Outliers (Phase 26) lists exactly the revisit rows": collect `expected = [...subs, ...races].filter(r => r.verdict === "revisit").map(r => r.key)`; parse the Outliers (Phase 26) section: if `expected` is empty the section must contain the exact sentence `No revisit rows — every out-of-band row was accepted.` and no `| ` table data row; otherwise the table's data rows (lines starting with `| ` after the header and separator) must have first-column values deep-equal to `expected` in order, and the section must contain `Suggested lever (v1.3)`.
    10. "AFTER, Outliers and Handoff blocks are byte-identical to a fresh render": `const v = buildVerdicts({ before, after, beforeDeep, afterDeep, prior: verdicts })`; for each of `"after"`, `"outliers"`, `"handoff"`, `doc.includes(renderMarkdown(v, section).trim())` is true (message names the section); also assert `JSON.stringify(v) === JSON.stringify(verdicts)` (the committed verdicts file is the script's own fixed point).
    11. "Handoff to Phase 27 carries the yardstick": the Handoff section contains `### Yardstick summary`, `### AFTER natural roll-ups — by class`, `### AFTER natural roll-ups — by sub-class`, `### AFTER natural roll-ups — by race`, `### Depth-20 roll-ups — by class`, `### Depth-20 roll-ups — by sub-class`, `### Depth-20 roll-ups — by race`, `### Accepted-but-strong rows`, `### Revisit rows`, `### Caveats`, and the Handoff section is the last section of the file.
    Run `node --test test/unit/class-pass-ledger.test.js`. If any assertion fails, fix the LEDGER or re-render verdicts.json through the script — never the assertion — and record the fix in the SUMMARY (none is expected: Plans 26-02/26-03's acceptance criteria already checked each of these).
  </action>
  <verify>
    <automated>node --test test/unit/class-pass-ledger.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/class-pass-ledger.test.js` exits 0 with `# pass` >= 11 and `# fail 0`.
    - `grep -c 'from "../../tools/class-pass-diff.mjs"' test/unit/class-pass-ledger.test.js` prints `1`; `grep -c "cannotActCells\|metaParity\|renderMarkdown\|buildVerdicts\|cellKey" test/unit/class-pass-ledger.test.js` >= 5.
    - `grep -c "No revisit rows — every out-of-band row was accepted." test/unit/class-pass-ledger.test.js` >= 1; `grep -c "IDENT-06" test/unit/class-pass-ledger.test.js` >= 1; `grep -c "Ruling: level-1 Thief dagger (IDENT-10) — KEEP" test/unit/class-pass-ledger.test.js` >= 1.
    - `grep -c "| Sub / Race | GOOD | BAD |" test/unit/class-pass-ledger.test.js` >= 1 and `grep -c "32" test/unit/class-pass-ledger.test.js` >= 1 (header + separator + 30 rows).
    - `grep -c "Zero cannot-act cells" test/unit/class-pass-ledger.test.js` >= 1; `grep -c "Handoff to Phase 27" test/unit/class-pass-ledger.test.js` >= 2.
    - `grep -v '^\s*//' test/unit/class-pass-ledger.test.js | grep -Ec '\.(meanDepth|p50Depth|reach5|reach10|meanKills|delta), -?[0-9]|=== "revisit"\)\.length, [0-9]|=== "accept"\)\.length, [0-9]'` prints `0` (no balance number pinned: no metric field is compared to a numeric literal and no revisit/accept count is asserted).
  </acceptance_criteria>
  <done>The ledger's structure, provenance and zero-cannot-act promise are a passing standing test that re-derives parity, the gate and the three rendered blocks through the diff script and pins no balance number.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite, final pin proof, commit — phase close</name>
  <files>test/unit/class-pass-ledger.test.js</files>
  <read_first>
    - docs/CLASS-PASS.md: the AFTER heading line only (the pin hash for the final proof)
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-02-SUMMARY.md (the pin and the post-commit exit code recorded there — the phase-close proof repeats it)
  </read_first>
  <action>
    1. `npm test` — must end `# fail 0` with `# pass` >= 1353 (1330 measured before this phase + 12 diff-script tests + 11 ledger tests; more is fine). Also run `node --test "test/parity/**/*.test.js"` and confirm every test passes (the phase touched no engine file, so parity is unchanged — this is the phase-close proof, not a new check).
    2. Final pin proof: `PIN=$(grep -oE '^## AFTER — commit [0-9a-f]{40}' docs/CLASS-PASS.md | grep -oE '[0-9a-f]{40}')`; `git diff --quiet "$PIN" -- engine content src mazeworld.html tools; echo rc=$?` must print `rc=0` (the engine, content, shell and every tools/ file — including the diff script — are byte-identical to the pin the AFTER matrix was captured on); `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib; echo rc=$?` must print `rc=0` (harness identical to the BEFORE pin); `git log --oneline 1b4daed..HEAD -- test/parity/prototype-master.js.txt | wc -l` prints `0`.
    3. Commit `test/unit/class-pass-ledger.test.js` via the GSD commit helper (message: `test(26-04): class-pass ledger standing guard — structure, provenance, zero cannot-act`). `git status --porcelain` is empty afterwards.
    4. In the SUMMARY record the final `npm test` pass count, the parity pass count, the two `rc=0` proofs with the pin hash, and the sentence that Phase 26 changed no engine/content/shell/harness code (list the phase's commits with `git log --oneline <pin-of-26-01-start>..HEAD` — they touch only tools/class-pass-diff.mjs, test/unit/class-pass-*.test.js, docs/CLASS-PASS.md and docs/class-pass/*.json).
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git diff --quiet "$(grep -oE '^## AFTER — commit [0-9a-f]{40}' docs/CLASS-PASS.md | grep -oE '[0-9a-f]{40}')" -- engine content src mazeworld.html tools && git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib && echo PIN_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `npm test 2>&1 | tail -8` shows `# fail 0` and `# pass` >= 1353.
    - `node --test "test/parity/**/*.test.js" 2>&1 | tail -8` shows `# fail 0`.
    - The verify command prints `PIN_CLEAN`.
    - `git log --oneline 1b4daed..HEAD -- test/parity/prototype-master.js.txt | wc -l` prints `0`.
    - `git status --porcelain` prints nothing; `git log -1 --format=%s` starts with `test(26-04)`; `git show --stat --format= HEAD | grep -c "test/unit/class-pass-ledger.test.js"` prints `1`.
    - `git log --format=%H f7f294b..HEAD | while read h; do git show --stat --format= "$h"; done | grep -Ec "^ (engine|content|src)/| mazeworld.html | tools/tune-classes.mjs| tools/lib/"` prints `0` (no phase commit touched the engine, content, shell or harness).
  </acceptance_criteria>
  <done>The full suite and the parity suite are green, the engine/content/shell/tools tree is proven identical to the AFTER pin and the harness to the BEFORE pin, the ledger guard is committed, and Phase 26 closes having changed no engine code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger + JSONs -> standing test | the test trusts the committed files; its job is to catch drift between them (a hand-edited table, a re-captured JSON without a re-render, a duplicated table) |
| test -> balance | the test must never become a CI gate on balance numbers (project rule: the harness is a proxy, not a gate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-19 | Tampering | ledger drift (numbers retyped, tables edited, sections reordered) | high | mitigate | byte-identical re-render assertion for the three blocks; fixed H2 order; single good/bad table; AFTER hash == both meta.commit |
| T-26-20 | Repudiation | AFTER JSON re-captured on a different pin without updating the ledger | high | mitigate | heading hash prefix == after.meta.commit == afterDeep.meta.commit; parity modulo commit; before.commit != after.commit |
| T-26-21 | Tampering | a cannot-act cell slipping into a committed after.json | critical | mitigate | hard zero assertion via cannotActCells plus the raw stuck/completed/meanKills check |
| T-26-22 | Denial of Service | the test becoming a balance gate that fails on every retune | medium | mitigate | no balance number pinned (grep-asserted); counts of revisit/out-of-band rows deliberately not asserted |
| T-26-23 | Tampering | weakening an assertion to make a drifted ledger pass | medium | mitigate | prohibition + SUMMARY must record any ledger fix; the re-render path is the documented remedy |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | no installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/class-pass-ledger.test.js` green (>= 11 tests); `npm test` ends `# fail 0` with >= 1353 passing; parity suite green.
- `git diff --quiet <AFTER pin> -- engine content src mazeworld.html tools` and `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib` both exit 0 at phase close; prototype-master untouched.
</verification>

<success_criteria>
- The class-pass ledger's structure, provenance, zero-cannot-act invariant, verdict rules and machine-rendered blocks are guarded by a standing test that pins no balance number.
- Phase 26 closes with a fully green suite and an engine tree proven identical to the pin the AFTER matrix was captured on — ROADMAP success criteria 1-4 (AFTER captured at BEFORE volume; ranked by sub-class and race with a written verdict; every cannot-act row zero; ledger complete with rulings and outliers) are all checkable from the repo.
</success_criteria>

<output>
Create `.planning/phases/26-mass-playtest-class-pass-ledger/26-04-SUMMARY.md` when done. Record: the ledger test count, the final `npm test` and parity pass counts, both `rc=0` pin proofs with hashes, any ledger fix the test forced (expected none), and the phase's commit list proving no engine/content/shell/harness file changed.
</output>
