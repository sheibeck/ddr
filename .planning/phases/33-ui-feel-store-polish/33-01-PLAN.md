---
phase: 33-ui-feel-store-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/state.js
  - engine/saveState.js
  - engine/economy.js
  - content/store-stock.js
  - content/index.js
  - src/browser/engineAdapter.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/unit/newrun.test.js
  - test/unit/save-validation.test.js
  - test/unit/engineAdapter.test.js
  - test/unit/store-roll.test.js
autonomous: true
requirements: [STORE-01]

must_haves:
  truths:
    - "STORE-01 (CONTEXT Area 3): a run started from the shell (engineAdapter#startNewRun, the ONE tap-to-roll entry point, dev start-at-depth included) carries `state.storeRoll === true`; every parity fixture (all of which call `newRun(seed)` with no options — economy-parity.test.js L67 `newRun(ECONOMY_FIXTURE.seed)`), every unit test's `newRun(seed)`, every tools/ bot and every pre-Phase-33 save carries `storeRoll: false` — so `openStore` takes the flag-off path for all of them and is byte-identical to today, INCLUDING the rng draw order `rng.shuffle(arms) → rng.d(2) → rollBlade | rollMailPiece`"
    - "The flag follows the `dev` boolean precedent exactly: a plain boolean present on EVERY fresh state (engine/state.js#newRun, beside `dev: startAt > 1`), derived from a newRun OPTION fixtures never pass (`storeRoll = false` in the third-argument destructure, mirroring `startDepth`), boolean-coerced on load (`storeRoll: !!obj.storeRoll` in validateSave AND rehydrate — a save missing the key reads false), and destructured OUT (a pure strip, no reconcile — there is no in-state stock to replay; `store` is nulled on rehydrate) in all SIX parity comparables: the three `*Comparable()` fns in test/parity/harness/comparables.js AND the three test-local comparables in combat-/magic-/movement-parity.test.js — REQUIRED because diffState compares sorted key sets and the frozen prototype's `S` has no such key (RESEARCH assumption A2 was wrong: fixtures are `newRun()` output, not hand-authored snapshots, so the field IS present on them — value false — and must be stripped exactly like `dev`)"
    - "Flag on: the store's food, lockpick, armor-repair, scroll and Rations lines are byte-identical to the flag-off store at the same array positions; the Healing potion line stays; the three other potion lines become 3 drawn (rng.shuffle, take 3) from the tier's potion pool — an explicit allow-list (content/store-stock.js STORE_POTION_POOL) that never contains the trap potion at POTIONS index 8; the two weapon lines become 2 drawn (rng.shuffle, take 2) from the class-legal weapons inside the tier's cost band (falling back to `cost <= band.hi` when the band holds fewer than 2 legal weapons — a Magic User at tier 3); the armor line is the BEST class-legal upgrade at or under the tier's armor cap (or absent when none fits, and never present where the flag-off store had none); the premium item keeps its existing draw but its enchantment is re-derived from the tier (+1 shallow / +2 mid / +3 deep) with its cost recomputed through the unchanged formula"
    - "Every new rng draw sits AFTER every existing draw (the Phase 29 bagUpgradeTier placement rule): flag-on consumes exactly today's draws plus N extra where N = (potionPool.length - 1) + (weaponPool.length - 1) — pinned by advancing the flag-off rng by N `next()` calls and asserting the cursors coincide"
    - "Depth tiers reuse the existing content bands: tier = the number of BAG_FLOORS floors (medium 2 / large 5 / exlarge 9, content/bags.js) at or below floor.depth → 0 (depth 1), 1 (2–4), 2 (5–8), 3 (9+); no new floor table — content/store-stock.js holds only the per-tier pools/bands/caps/bonuses indexed by that tier"
    - "test/parity/prototype-master.js.txt is never edited; zero fixture edits; the one store-opening fixture (test/parity/fixtures/action-script.economy.json, seed 3, depth 1) replays byte-identical through the untouched economy-parity test; the seed-3 unit pins (Katana 656 / Axe 63 / Studded 938 / Casket 3750 / Rations 38) stay green without edits; content/store-stock.js is pure data (content-is-pure-data tripwire)"
  artifacts:
    - "engine/state.js: newRun(seed, exclude, { startDepth, force, storeRoll = false }) sets `storeRoll: !!storeRoll` beside `dev` (arity still 1)"
    - "engine/saveState.js: `storeRoll: !!obj.storeRoll` in validateSave's value and in rehydrate's state, beside the existing `dev` lines"
    - "engine/economy.js: STORE_TIER_FLOORS derived from BAG_FLOORS; exported pure helpers storeTier / storePotionPool / storeWeaponPool / storeArmorFor / enchantForTier / replaceStockLines; openStore's `mk` entry builder + potionLine/weaponLine/armorLine/premiumLine builders shared by both paths; the ONE `if (state.storeRoll === true)` block after the premium add"
    - "content/store-stock.js (new, re-exported from content/index.js): STORE_POTION_POOL, STORE_WEAPON_BANDS, STORE_ARMOR_CAP, STORE_PREMIUM_BONUS"
    - "src/browser/engineAdapter.js: startNewRun passes `{ startDepth, storeRoll: true }` to initRun"
    - "test/parity/harness/comparables.js + combat/magic/movement-parity.test.js: `storeRoll` added to the six destructure lines beside `dev`"
    - "test/unit/store-roll.test.js (new): tier/band/pool pins, flag-off identity, draw-count pin, flag-on stock-shape pins per depth and class; newrun/save-validation/engineAdapter pins extended"
  key_links:
    - "shell tap → engineAdapter#startNewRun → initRun(seed, exclude, { startDepth, storeRoll: true }) → newRun → state.storeRoll === true → serializeRun spreads it → validateSave/rehydrate coerce it → openStore reads `state.storeRoll === true` → the flag-on block"
    - "fixture / test / bot → newRun(seed) → storeRoll: false → openStore flag-off path byte-identical → comparables strip the key → diffState null"
---

<objective>
Ship STORE-01 as the phase's ONLY engine/rng change: depth-appropriate, randomly rolled store stock behind a run-level boolean `state.storeRoll` that follows the `dev` boolean precedent line for line (engine/state.js#newRun L194-200, engine/saveState.js L216/L272, test/parity/harness/comparables.js L264/L341/L730 — plus the three test-local comparables the harness comment forgets to mention). Flag off (every fixture, every existing test, every old save, every tools/ bot) leaves `openStore` byte-identical including rng draw order; flag on (every run the shell starts from this version on) re-rolls potions, weapons, armor and the premium enchantment by depth tier, with every new draw placed after every existing draw.

Correction to 33-RESEARCH.md assumption A2, applied here: parity fixtures are NOT hand-authored snapshots — every parity test builds its engine state with `newRun(seed)` (comparables.js's own `dev` comment: "always false on a fixture (every fixture calls newRun(seed))"). So (1) `newRun` must NOT set the flag unconditionally — it comes from an option only the shell passes, exactly as `dev` comes from `startDepth`; and (2) the comparables carve-out IS required — `diffState` fails on a key-set mismatch, and a fresh state now carries `storeRoll: false` while the prototype's `S` never will. Six destructure lines, a pure strip like `dev`, no reconcile function.

Purpose: the store stops selling the same four things forever ("store always sells the same things" — the device-round feedback that seeded STORE-01) without moving a single byte of parity.
Output: engine/state.js, engine/saveState.js, engine/economy.js, content/store-stock.js (+ barrel), src/browser/engineAdapter.js, the six carve-out lines, extended newrun/save-validation/engineAdapter pins, test/unit/store-roll.test.js, `npm test` `# fail 0` with the master and every fixture untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Commit trailers (every commit in this phase, no exceptions — task commits and the metadata commit alike): end every commit message with these two lines, in this order:

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS

The gsd-tools `commit` helper does NOT append these trailers — if you use it, `git commit --amend` afterwards to add them (or commit with plain `git commit -m` including the trailers from the start).

Hard scope rules for this plan: the ONLY engine files touched are engine/state.js, engine/saveState.js and engine/economy.js; the ONLY parity-harness edits are the six one-line `storeRoll` destructure additions (no other line in test/parity changes; `test/parity/prototype-master.js.txt` and every file under test/parity/fixtures/ stay byte-identical — `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` must print nothing at every commit). No shell (mazeworld.html) change in this plan — the store header copy is Plan 03's. UAT is deferred to the end of the autonomous run — never pause for a device check; record Pixel 7 items in the SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-ui-feel-store-polish/33-CONTEXT.md
@.planning/phases/33-ui-feel-store-polish/33-RESEARCH.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md
@engine/economy.js
@engine/state.js
@engine/saveState.js
@content/bags.js
@content/potions.js
@content/weapons.js
@content/armors.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: The storeRoll run flag — newRun option, tolerant save read, six-line parity carve-out, shell entry point, and the flag pins</name>
  <files>engine/state.js, engine/saveState.js, src/browser/engineAdapter.js, test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, test/unit/newrun.test.js, test/unit/save-validation.test.js, test/unit/engineAdapter.test.js</files>
  <read_first>
    - engine/state.js L128-217 (newRun: the `{ startDepth = 1, force = null } = {}` third-argument destructure, the `dev: startAt > 1` field at ~L200 with its Phase 21 comment — the placement and comment style to copy; the JSDoc @param line for the options object)
    - engine/saveState.js L157-224 (validateSave: `dev: !!obj.dev` at ~L216 inside `value`), L226-280 (rehydrate: `dev: !!obj.dev` at ~L272), L22-24 (serializeRun spreads the whole state — nothing to add there)
    - test/parity/harness/comparables.js ~L250-264 (movementComparable's `dev` comment + destructure line), ~L331-341 (combatComparable), ~L720-730 (economyComparable) — the three harness lines; the Phase 21/29 comment blocks above each are the wording precedent
    - test/parity/combat-parity.test.js ~L106-119, test/parity/magic-parity.test.js ~L70-81, test/parity/movement-parity.test.js ~L52-64 — the three TEST-LOCAL comparables that ALSO destructure `dev` ("mirrored here because this file defines its own local comparable()"); test/parity/harness/diffState.js L74-110 (findDivergence: `key set mismatch` — why the strip is mandatory)
    - test/parity/economy-parity.test.js L58-70 (`let engineState = newRun(ECONOMY_FIXTURE.seed);` then `runAction` per fixture action — the proof that fixtures ARE newRun output and that `{type:"openStore"}` runs through the live engine)
    - src/browser/engineAdapter.js L142-155 (initRun forwards `options` to newRun), L276-306 (startNewRun: `const state = initRun(safeSeed, exclude, { startDepth });` — the ONE real new-run entry point; boot()'s `initRun(freshSeed, exclude)` at ~L339 is a throwaway pre-title state and dispatch()'s fail-closed `initRun(seed)` at ~L414 is a corrupt-state recovery — both stay flag-off by design)
    - test/unit/newrun.test.js L74-100 (the D-14 tests: `newRun.length === 1` arity pin, `newRun(seed)` deep-equals `newRun(seed, [], { startDepth: 1 })`), test/unit/save-validation.test.js L24-32 (fresh-run round-trip deepStrictEqual) and L320-355 (the `dev` missing-key / survives / coercion-table tests — copy their shape), test/unit/engineAdapter.test.js L759-772 (startNewRun dev test — the fake-storage harness to reuse)
    - test/determinism/forced-chargen.test.js L120-135 (`Object.keys(newRun(1))` baseline — both sides gain the key, stays green)
  </read_first>
  <action>
    1. engine/state.js — the option and the field. Extend newRun's third-argument destructure to `{ startDepth = 1, force = null, storeRoll = false } = {}` (declared arity stays 1 — newrun.test.js pins `newRun.length === 1`). Directly below `dev: startAt > 1,` add `storeRoll: !!storeRoll,` with a comment block in the Phase 21 style: Phase 33 (STORE-01, CONTEXT Area 3) — storeRoll: true only for a run the SHELL starts (engineAdapter#startNewRun passes it); a plain boolean present on EVERY fresh state exactly like dev above, so serializeRun/validateSave/rehydrate round-trip it and the fresh-run round-trip test stays deepStrictEqual. It gates the ONLY new rng draws of Phase 33 (engine/economy.js#openStore's depth-rolled stock). Every parity fixture, every unit test, every tools/ bot and every existing caller calls newRun(seed) with no option → false → openStore is byte-identical to the frozen prototype. The parity harness strips it in all six comparables (three in harness/comparables.js, three test-local), exactly like dev. Update the JSDoc @param type to include `storeRoll?: boolean` with one clause ("storeRoll: shell-started run, enables the depth-rolled store stock; default false").
    2. engine/saveState.js — tolerant read at BOTH sites: in validateSave's `value` add `storeRoll: !!obj.storeRoll,` right after `dev: !!obj.dev,`; in rehydrate's `state` add the same line after its `dev: !!obj.dev,`. One shared-wording comment at each: Phase 33 (STORE-01) — boolean-coerced like dev/dead/won; absent on a pre-Phase-33 save → false, so an old save keeps today's fixed store stock and never gains the new draws mid-run. Do NOT touch serializeRun (it spreads the state).
    3. The six-line carve-out. In test/parity/harness/comparables.js add `storeRoll` to each of the three destructure lines immediately after `dev` (so `..., pendingLoot, dev, storeRoll, ...state0 } = state;`), and above each add a two-line comment: Phase 33 (STORE-01): strip the new top-level `state.storeRoll` too — a sixth analog of party/pendingJoiner/pendingFind/pendingLoot/dev; always false on a fixture (every fixture calls newRun(seed) with no option); the prototype master has no such field; a plain strip (no reconcile — `store` is transient and the flag gates only the NEXT openStore call). Apply the identical one-word destructure addition + comment to the test-local comparables in test/parity/combat-parity.test.js, test/parity/magic-parity.test.js and test/parity/movement-parity.test.js (each says "mirroring harness … since this file defines its own local comparable()" — keep that wording). These six lines are the ONLY edits under test/parity; no fixture JSON, no master.
    4. src/browser/engineAdapter.js — the one real entry point: change startNewRun's `initRun(safeSeed, exclude, { startDepth })` to `initRun(safeSeed, exclude, { startDepth, storeRoll: true })` and extend its doc comment: Phase 33 (STORE-01) — every run the player starts from here (including a dev start-at-depth run, which is exactly how the deep-tier stock gets checked on device) carries state.storeRoll: true; boot()'s throwaway pre-title fresh run and dispatch()'s fail-closed recovery run deliberately do NOT pass it (flag-off = the parity-identical store), and tools/ bots call newRun(seed) directly so the mass-playtest ledgers are unchanged by this phase. Do not touch initRun's own signature (tests call `initRun(seed)` and expect the plain engine roll).
    5. Pins. test/unit/newrun.test.js — add a test "STORE-01: newRun(seed).storeRoll is false; newRun(seed, [], { storeRoll: true }) differs ONLY in that field": for seeds [1, 42, 303] assert `a.storeRoll === false`, `b.storeRoll === true`, `assert.deepStrictEqual({ ...a, storeRoll: true }, b)` (same rngState, same c, same floor — the option consumes no rng), and `newRun.length === 1` still. test/unit/save-validation.test.js — mirror the three `dev` tests: (a) a valid save with the key deleted validates to `storeRoll: false` and rehydrates to false; (b) `storeRoll: true` survives serializeRun → JSON → validateSave → rehydrate; (c) the coercion table (`1`, `"yes"`, `null`, `undefined`, `0`) coerces identically via both entry points. test/unit/engineAdapter.test.js — add "STORE-01: startNewRun flags the run storeRoll: true (plain and dev start); initRun(seed) does not": inside withFakeLocalStorage assert `(await startNewRun(4300)).storeRoll === true`, `(await startNewRun(4301, { startDepth: 20 })).storeRoll === true`, and after `initRun(4302)` `getState().storeRoll === false`.
    6. Run `node --test test/unit/newrun.test.js test/unit/save-validation.test.js test/unit/engineAdapter.test.js test/roundtrip/serialize-rehydrate.test.js test/determinism/forced-chargen.test.js test/determinism/same-seed-same-result.test.js test/parity/*.test.js` → `# fail 0`; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing. Commit: `feat(33-01): storeRoll run flag — newRun option, tolerant save read, six-line parity strip, startNewRun sets it`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/newrun.test.js test/unit/save-validation.test.js test/unit/engineAdapter.test.js test/roundtrip/serialize-rehydrate.test.js test/determinism/forced-chargen.test.js test/determinism/same-seed-same-result.test.js test/parity/*.test.js 2>&1 | tail -5 && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'storeRoll = false } = {}' engine/state.js` == 1 and `grep -c 'storeRoll: !!storeRoll,' engine/state.js` == 1 and `grep -c 'dev: startAt > 1,' engine/state.js` == 1
    - `grep -c 'storeRoll: !!obj.storeRoll,' engine/saveState.js` == 2 and `grep -c 'dev: !!obj.dev,' engine/saveState.js` == 2
    - `grep -c 'pendingLoot, dev, storeRoll, ' test/parity/harness/comparables.js` == 3 and `grep -c 'pendingLoot, dev, storeRoll, ' test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js | grep -c ':1$'` == 3
    - `git diff --stat 97a0e8a..HEAD -- test/parity | grep -c 'comparables.js\|combat-parity\|magic-parity\|movement-parity'` == 4 and `git diff 97a0e8a..HEAD -- test/parity | grep -c '^[-+][^-+]' ` <= 40 (six one-line destructure edits plus their comments, nothing else)
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing and `git diff --stat 97a0e8a..HEAD -- test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing
    - `grep -c 'initRun(safeSeed, exclude, { startDepth, storeRoll: true })' src/browser/engineAdapter.js` == 1 and `grep -c 'return initRun(freshSeed, exclude);' src/browser/engineAdapter.js` == 1 (boot unchanged)
    - `grep -c 'storeRoll' test/unit/newrun.test.js` >= 3 and `grep -c 'storeRoll' test/unit/save-validation.test.js` >= 4 and `grep -c 'storeRoll' test/unit/engineAdapter.test.js` >= 3
    - node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>A shell-started run carries storeRoll: true; every newRun(seed) caller, every fixture and every old save reads false; the six comparables strip the key so every parity test is green with the master and fixtures untouched; the flag round-trips through save/load and is pinned at all three layers.</done>
</task>

<task type="auto">
  <name>Task 2: content/store-stock.js tiers + engine/economy.js helpers and the guarded flag-on block in openStore</name>
  <files>content/store-stock.js, content/index.js, engine/economy.js</files>
  <read_first>
    - engine/economy.js L18-20 (imports from ./items.js and ../content/index.js — extend the content import), L229-323 (openStore verbatim: the `add` closure at L244-245, the fixed potion indices L248-249, `arms`/rng.shuffle L260-262, the `mails` filter + `mails[0]` L276-286, the premium `rng.d(2)`/rollBlade/rollMailPiece at L291 and its pCost formula L292-296, the Rations add at L306 with its "add() consumes no rng … placed last" comment, the haggle at L308-309)
    - engine/items.js L120-147 (rollBlade: `{ kind:"weapon", n, base, bonus, txt }`; rollMailPiece: `{ kind:"armor", n, armor, ar, wp, min, cls, txt }` — the two premium shapes enchantForTier rewrites), L100-103 (hasPicks)
    - content/bags.js L38 (`BAG_FLOORS = { medium: 2, large: 5, exlarge: 9 }` — the ONLY depth-band table; the header comment style for TUNING KNOB tables), content/potions.js (10 entries; index 8 is the trap potion with eff "death" — never on a shelf), content/weapons.js L14-40 (cost/cls per weapon), content/armors.js L8-14 (ARMORS ascending by ar: Cloth 3 / Leather 6 / Studded 10 / Mail 12 / Plate 15)
    - content/index.js (barrel — add the new module), test/determinism/content-is-pure-data.test.js L1-60 (every content export must be function-free and JSON-serializable — no Infinity, no closures)
    - engine/rng.js L46-76 (shuffle = n-1 `gen.next()` draws, pick = 1, d = 1 — the draw arithmetic Task 3 pins)
    - 33-RESEARCH.md "Engine Deep Dive" (exact draw order) and Pitfall 5 (the trap potion)
  </read_first>
  <action>
    <!-- planner-discipline-allow: "Death" -->
    1. content/store-stock.js (new, pure data, no imports, no functions). Header comment: Phase 33 (STORE-01) — the depth-rolled store's per-tier tables. The TIER INDEX (0..3) is computed by engine/economy.js#storeTier as the number of BAG_FLOORS floors (content/bags.js: medium 2 / large 5 / exlarge 9) at or below floor.depth — the same ladder bag upgrades climb, so the whole game has ONE set of depth breakpoints (CONTEXT Area 3: reuse the existing bands, invent no new floor table). Every array below is indexed by that tier. TUNING KNOBS, same status as BAG_FLOORS. Exports:
       - `STORE_POTION_POOL` — an array of `{ n, from }` where `n` is a POTIONS name and `from` the first tier it may roll: Cure Poison 0, Strength 0, Cure Disease 0, Enlarge 0, Invisible 1, Speed 1, Xtra Healing 1, Acuteness 2. Comment: an explicit ALLOW-LIST, never `POTIONS` itself — the trap potion at POTIONS index 8 (eff "death") is deliberately absent and must never be added (RESEARCH Pitfall 5); Healing is always stocked and lives outside the roll. Pool at tier t = every entry with `from <= t` (cumulative: 4 / 7 / 8 / 8 entries), the store draws 3.
       - `STORE_WEAPON_BANDS` — `[{ lo: 0, hi: 250 }, { lo: 100, hi: 500 }, { lo: 200, hi: 700 }, { lo: 400, hi: 900 }]` (WEAPONS base cost, inclusive; deeper = pricier band). Comment the fallback: when a class's legal weapons inside the band number fewer than 2 (a Magic User's priciest legal weapon costs 250), engine/economy.js#storeWeaponPool drops `lo` and uses every legal weapon with cost <= hi.
       - `STORE_ARMOR_CAP` — `["Leather", "Studded", "Mail", "Plate"]`: the best ARMORS entry (by name) a tier's store will stock; the offered line is the BEST class-legal upgrade at or under the cap.
       - `STORE_PREMIUM_BONUS` — `[1, 1, 2, 3]`: the enchanted premium item's bonus per tier (+1 shallow … +3 deep).
       Add `export * from "./store-stock.js";` to content/index.js after the bags line.
    2. engine/economy.js — helpers (all exported, all pure, no rng inside except where stated). Extend the content import to add `BAG_FLOORS, STORE_POTION_POOL, STORE_WEAPON_BANDS, STORE_ARMOR_CAP, STORE_PREMIUM_BONUS`. Add, above openStore:
       - `export const STORE_TIER_FLOORS = [BAG_FLOORS.medium, BAG_FLOORS.large, BAG_FLOORS.exlarge];` (derived, not restated — the single source of truth stays content/bags.js).
       - `export function storeTier(depth)` → `STORE_TIER_FLOORS.filter((f) => d >= f).length` where `d` is `Number.isFinite(depth) ? depth : 1` (0 for depth 1, 1 for 2–4, 2 for 5–8, 3 for 9+).
       - `export function storePotionPool(tier)` → a FRESH array of POTIONS entries: `STORE_POTION_POOL.filter((e) => e.from <= tier).map((e) => POTIONS.find((p) => p.n === e.n))` (fresh because rng.shuffle mutates in place).
       - `export function storeWeaponPool(letter, tier)` → a FRESH array of weapon NAMES: legal = `Object.keys(WEAPONS).filter((w) => WEAPONS[w].cls.includes(letter))` (the SAME predicate openStore's `arms` uses — never a second legality rule); band = `STORE_WEAPON_BANDS[tier]`; pool = legal filtered to `lo <= cost <= hi`; if pool.length < 2 → legal filtered to `cost <= hi`.
       - `export function storeArmorFor(mails, tier)` → given openStore's already-filtered `mails` (class-legal, ar > c.ar, canEquipArmor), the LAST element of `mails.filter((a) => ARMORS.indexOf(a) <= capIdx)` where `capIdx = ARMORS.findIndex((a) => a.name === STORE_ARMOR_CAP[tier])`, or null when none (ARMORS is ascending by ar, so last = best within the cap). No rng.
       - `export function enchantForTier(premium, tier)` → a NEW object: `b = STORE_PREMIUM_BONUS[tier]`; for `kind === "weapon"`: `{ ...premium, bonus: b, txt: \`${WEAPONS[premium.base].lab} +${b}\` }` (name, base unchanged); for `kind === "armor"`: with `a = ARMORS.find((x) => x.name === premium.armor) || ARMORS[0]`: `{ ...premium, ar: a.ar + b, wp: a.wp + 10 * b, txt: \`AR ${a.ar + b}, ${a.wp + 10 * b} hp\` }`. Never mutates its input, consumes no rng.
       - `export function replaceStockLines(stock, pred, lines)` → `at = stock.findIndex(pred)`; if -1 return stock unchanged (a line the flag-off store never had is never introduced); remove every entry matching pred (iterate from the end); `stock.splice(at, 0, ...lines)`; return stock. Keeps every other line at its position.
    3. openStore — builders shared by both paths (byte-identical flag-off output). Replace the `add` closure with `const mk = (n, cost, effectId, effectParams, sub) => ({ n, sub: sub ?? null, cost: Math.max(1, Math.round(cost)), effectId, effectParams: effectParams ?? null, sold: false });` and `const add = (...args) => stock.push(mk(...args));` — same object literal, same key order, same rounding. Then introduce four entry builders that wrap the EXISTING argument expressions unchanged: `potionLine(p)` (the givePotion mk with `{ kind: "potion", n: \`${p.n} potion\`, txt: p.txt, eff2: p.eff, uses: 1 }` and sub `p.txt`), `weaponLine(w)` (buyWeapon: `priceFor(WEAPONS[w].cost, race, c.sub) * (race === "Troll" ? 2 : 1)`, the item literal, sub `WEAPONS[w].lab`), `armorLine(a)` (buyArmor with its `AR ${a.ar}, ${a.wp} hp` sub), `premiumLine(p)` (computes the existing pCost expression from `p` and returns the buyPremium mk with sub `${p.txt} · enchanted`). Rewrite the flag-off code paths to `stock.push(potionLine(p))` / `stock.push(weaponLine(w))` / `stock.push(armorLine(a))` / `stock.push(premiumLine(premium))` — every existing draw (`rng.shuffle(arms)`, `rng.d(2)`, `rollBlade`/`rollMailPiece`) and every condition (`!hasPicks(c)`, the repair gate, `mails.length && !RACES[race].noArmor`, the Magic User scroll) stays exactly where it is and in the same order.
    4. openStore — the ONE guarded block, placed immediately after `stock.push(premiumLine(premium))` (the last existing rng draw) and BEFORE the Rations add and the haggle (haggle must still multiply every line last): `if (state.storeRoll === true) { … }` with: `const tier = storeTier(d);` then the two NEW draws in this order — `const pPool = storePotionPool(tier); rng.shuffle(pPool);` and `const wPool = storeWeaponPool(letter, tier); rng.shuffle(wPool);` — then the four rewrites (no rng): `replaceStockLines(stock, (l) => l.effectId === "givePotion" && l.effectParams.item.eff2 !== "heal", pPool.slice(0, 3).map(potionLine))` (Healing stays at its position; the three fixed lines Xtra Healing/Strength/Speed become the draw), `replaceStockLines(stock, (l) => l.effectId === "buyWeapon", wPool.slice(0, 2).map(weaponLine))`, `const capped = storeArmorFor(mails, tier); replaceStockLines(stock, (l) => l.effectId === "buyArmor", capped ? [armorLine(capped)] : [])`, `replaceStockLines(stock, (l) => l.effectId === "buyPremium", [premiumLine(enchantForTier(premium, tier))])`. Head the block with a comment: Phase 33 (STORE-01, CONTEXT Area 3) — depth-rolled stock, ONLY on a run whose newRun set storeRoll (engine/state.js; the shell's startNewRun) — every parity fixture, unit pin and old save reads false and never enters here. Every new rng draw sits AFTER every existing draw (rng.shuffle(arms) → rng.d(2) → rollBlade|rollMailPiece are untouched above), the Phase 29 bagUpgradeTier placement rule, so the flag-off cursor never shifts; the lines are rewritten IN PLACE so food/potions/picks/repair/weapons/armor/scroll/premium/Rations keep their positions. Draw count flag-on = flag-off + (pPool.length - 1) + (wPool.length - 1) (rng.shuffle is n-1 draws) — pinned by test/unit/store-roll.test.js. Also extend openStore's doc comment with a Phase 33 paragraph naming the flag and the tier ladder, and note beside `rollBlade(rng, d, true)` that rollBlade never reads its depth argument (pre-existing dead parameter — the depth scaling lives in enchantForTier, not there).
    5. Sanity: `node --test test/unit/economy.test.js test/unit/store-sell.test.js test/unit/bag-cap-gate.test.js test/determinism/content-is-pure-data.test.js test/parity/economy-parity.test.js test/parity/full-suite.test.js` → `# fail 0` (the seed-3 pins and the economy fixture prove the flag-off path is byte-identical — no edit to those files). Commit: `feat(33-01): depth-rolled store stock behind state.storeRoll — content/store-stock.js tiers, openStore flag-on block after every existing draw`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/economy.test.js test/unit/store-sell.test.js test/unit/bag-cap-gate.test.js test/determinism/content-is-pure-data.test.js test/parity/economy-parity.test.js test/parity/full-suite.test.js 2>&1 | tail -5 && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt test/unit/economy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f content/store-stock.js` and `grep -c 'export const STORE_POTION_POOL\|export const STORE_WEAPON_BANDS\|export const STORE_ARMOR_CAP\|export const STORE_PREMIUM_BONUS' content/store-stock.js` == 4 and `grep -c '^import' content/store-stock.js` == 0 and `grep -c 'Infinity' content/store-stock.js` == 0
    - `grep -v '^\s*//' content/store-stock.js | grep -c '"Death"'` == 0 and `grep -c 'export \* from "./store-stock.js";' content/index.js` == 1
    - `grep -c 'export const STORE_TIER_FLOORS = \[BAG_FLOORS.medium, BAG_FLOORS.large, BAG_FLOORS.exlarge\];' engine/economy.js` == 1
    - `grep -c 'export function storeTier(\|export function storePotionPool(\|export function storeWeaponPool(\|export function storeArmorFor(\|export function enchantForTier(\|export function replaceStockLines(' engine/economy.js` == 6
    - `grep -c 'if (state.storeRoll === true) {' engine/economy.js` == 1 and `grep -v '^\s*\(//\|\*\|/\*\)' engine/economy.js | grep -c 'state.storeRoll'` == 1 (one gate, one non-comment read)
    - `grep -c 'rng.shuffle(arms);' engine/economy.js` == 1 and `grep -c 'rng.d(2) === 1 ? rollBlade(rng, d, true) : rollMailPiece(rng)' engine/economy.js` == 1 (existing draws untouched)
    - the line number of `if (state.storeRoll === true) {` in engine/economy.js is greater than the line number of `rollMailPiece(rng)` inside openStore and less than the line number of `add("Rations (+1 ration)"`
    - `grep -c 'const mk = (n, cost, effectId, effectParams, sub) =>' engine/economy.js` == 1 and `grep -c 'const potionLine = \|const weaponLine = \|const armorLine = \|const premiumLine = ' engine/economy.js` == 4
    - `git diff --stat 97a0e8a..HEAD -- engine | grep -c 'state.js\|saveState.js\|economy.js'` == 3 and `git diff --stat 97a0e8a..HEAD -- engine | grep -vc 'state.js\|saveState.js\|economy.js\|files changed'` == 0 (no other engine file)
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt test/unit/economy.test.js` prints nothing; node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>content/store-stock.js holds the four per-tier tables (pure data, trap potion excluded); economy.js exposes the pure tier/pool/cap/enchant helpers; openStore's flag-off path is byte-identical (seed-3 pins + economy fixture green untouched) and the flag-on block draws only after every existing draw, rewriting potion/weapon/armor/premium lines in place.</done>
</task>

<task type="auto">
  <name>Task 3: test/unit/store-roll.test.js — tier/band pins, flag-off identity, draw-count pin, per-depth stock-shape pins; full suite gate</name>
  <files>test/unit/store-roll.test.js</files>
  <read_first>
    - test/unit/economy.test.js L1-60 (imports, `fixedFighter`/`fixedState` literals, the file's assertion style), L264-292 (the seed-3 `newRun(3)` + `makeRng(state.rngState)` + `openStore(state, rng, [])` harness — reuse exactly)
    - test/unit/bag-cap-gate.test.js L272-284 (the bagUpgradeTier boundary-pin style: bare state literals across every tier floor)
    - test/unit/newrun.test.js L84-90 (`newRun(seed, [], { force: { sub } })` — the forced-chargen option; `force: { cls: "Magic User" }` etc. gives a hero of each class letter deterministically)
    - engine/rng.js L46-76 (`next()` is public — advancing the flag-off rng by N `next()` calls reproduces exactly N extra draws)
    - engine/economy.js (Task 2's exported helpers and the flag-on block), content/store-stock.js, content/bags.js L38, content/potions.js, content/weapons.js, content/armors.js
  </read_first>
  <action>
    <!-- planner-discipline-allow: "Death" -->
    1. Create test/unit/store-roll.test.js with a header comment (Phase 33, STORE-01: what it proves — tiers from BAG_FLOORS, allow-list pools, flag-off identity incl. rng cursor, the N-extra-draws pin, per-depth stock shape) importing `makeRng`, `newRun`, `openStore` and the six helpers + `STORE_TIER_FLOORS` from engine/economy.js, the four tables from content/index.js, and `BAG_FLOORS, POTIONS, WEAPONS, ARMORS` from content/index.js. Helper `heroOf(seed, cls, depth)`: `const s = newRun(seed, [], { force: { cls } }); s.floor.depth = depth; return s;` (a plain field write, no rng — the tests never need a real descent) and `openWith(state, storeRoll)`: clone via structuredClone, set `storeRoll`, `const rng = makeRng(state.rngState); openStore(clone, rng, []); return { stock: clone.store.stock, rng };`.
    2. Tier + table pins: `STORE_TIER_FLOORS` deepStrictEquals `[BAG_FLOORS.medium, BAG_FLOORS.large, BAG_FLOORS.exlarge]` and `[2, 5, 9]`; `storeTier` maps 1→0, 2→1, 4→1, 5→2, 8→2, 9→3, 99→3, and NaN/undefined→0; each of the four tables has length 4 (one entry per tier); every STORE_POTION_POOL name exists in POTIONS and none has eff "death" (assert by effect, and assert no pool entry's name equals the trap potion's name read from `POTIONS[8].n`); `storePotionPool(t).length` is 4/7/8/8 for t = 0..3 and every entry has `eff !== "death"`; STORE_ARMOR_CAP names exist in ARMORS in ascending ar order; STORE_PREMIUM_BONUS deepStrictEquals [1, 1, 2, 3]; every STORE_WEAPON_BANDS entry has `lo <= hi` and hi non-decreasing across tiers.
    3. Pool pins per class letter: for letter in ["F", "T", "M"] and tier 0..3: `storeWeaponPool(letter, tier).length >= 2`, every name is class-legal (`WEAPONS[w].cls.includes(letter)`), every cost `<= STORE_WEAPON_BANDS[tier].hi`, and — when the strict band holds ≥ 2 legal weapons — every cost `>= lo`; assert explicitly that `storeWeaponPool("M", 3)` is the fallback (every Magic User weapon costs ≤ 250 < 400, so the pool equals all class-legal weapons with cost ≤ 900, length 8). Two consecutive calls return distinct array objects (fresh — shuffle-safe).
    4. Flag-off identity: for seeds [3, 7, 42] with `newRun(seed)` (storeRoll false): (a) `openWith(s, false)` and a clone with the key DELETED produce deepStrictEqual stock and identical `rng.getState()` (missing ≡ false); (b) a truthy non-boolean (`"yes"`) also takes the flag-off path (strict `=== true`); (c) `newRun(3)`'s flag-off stock still pins Katana 656 / Axe 63 / Studded 938 / Casket 3750 / Rations 38 (the same numbers economy.test.js pins — restated here so this file fails first if the builders drift).
    5. The draw-count pin (CONTEXT: flag off = today's count; flag on = N extra): for cls in ["Fighter", "Thief", "Magic User"] and depth in [1, 2, 4, 5, 8, 9, 20]: `off = openWith(hero, false)`, `on = openWith(hero, true)`; compute `N = (storePotionPool(tier).length - 1) + (storeWeaponPool(letter, tier).length - 1)` with `tier = storeTier(depth)` and `letter` derived like openStore does; call `off.rng.next()` N times; assert `off.rng.getState() === on.rng.getState()` — i.e. flag-on consumed exactly today's draws plus N, in sequence, after them. Also assert `on.rng.getState() !== openWith(hero,false).rng.getState()` when N > 0 (the extra draws are real).
    6. Stock-shape pins (flag on), same matrix: exactly 4 `givePotion` lines, the first named "Healing potion" at the same index as flag-off, the other three distinct, each name present in `storePotionPool(tier)` (as `${p.n} potion`), none the trap potion; exactly 2 `buyWeapon` lines, both in `storeWeaponPool(letter, tier)`, distinct; every non-rolled line (effectId in eatRation/giveLockpicks/repairArmor/buyScroll/buyRations) deepStrictEquals its flag-off counterpart at the same index; a `buyArmor` line is present only if the flag-off stock had one, and when present it is `storeArmorFor(mails, tier)` (recompute `mails` in the test with openStore's predicate: class letter, `a.ar > c.ar`, `canEquipArmor(c, a)` imported from engine/items.js) — its ARMORS index ≤ the cap's index and its ar is the highest of the eligible set; exactly one `buyPremium` line whose item, for a weapon, has `bonus === STORE_PREMIUM_BONUS[tier]` and txt ending `+${bonus}` and cost `priceFor(WEAPONS[base].cost, race, c.sub) * (2 + bonus)` (rounded, before haggle — use a non-Wilmsry hero or apply the 0.7 factor), or, for armor, `ar === base.ar + b` and `wp === base.wp + 10 * b`; `stock.length === offStock.length` (in-place rewrite, no growth). Add the explicit boundary case: a Fighter at depth 1 wearing Leather (set `c.armor = "Leather"; c.ar = 6`) gets NO armor line flag-on (cap Leather, nothing better fits) while flag-off offers Studded; the same Fighter at depth 5 gets Mail (best within the Mail cap, not Studded), at depth 9 Plate.
    7. Serialization/purity: an open flag-on store JSON round-trips deepStrictEqual and `structuredClone` does not throw (the economy.test.js closure-fix check, repeated for the new lines); `enchantForTier` never mutates its input (deepStrictEqual the input before/after) and `replaceStockLines` on a predicate with no match returns the same array unchanged.
    8. Gate: `npm test` → `# fail 0`, `# pass` ≥ 1902 + the new tests (report the count in the SUMMARY); `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` empty; `git diff --stat 97a0e8a..HEAD -- test/parity` lists only the four carve-out files. Commit: `test(33-01): pin store tiers, allow-list pools, flag-off identity and the N-extra-draws contract`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/store-roll.test.js test/unit/economy.test.js test/parity/economy-parity.test.js 2>&1 | tail -5 && npm test 2>&1 | grep -E '^# (pass|fail)' && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/store-roll.test.js` and `node --test test/unit/store-roll.test.js` → `# fail 0` and `# pass` >= 10
    - `grep -c 'off.rng.next()' test/unit/store-roll.test.js` >= 1 and `grep -c 'getState()' test/unit/store-roll.test.js` >= 3 (the cursor-advance pin exists)
    - `grep -c 'BAG_FLOORS.medium, BAG_FLOORS.large, BAG_FLOORS.exlarge' test/unit/store-roll.test.js` >= 1 and `grep -c 'STORE_PREMIUM_BONUS' test/unit/store-roll.test.js` >= 2
    - `grep -c 'Katana' test/unit/store-roll.test.js` >= 1 (the seed-3 flag-off restatement)
    - `npm test` → `# fail 0` and `# pass` >= 1915
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing; `git diff --stat 97a0e8a..HEAD -- test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing
    - `git diff --stat 97a0e8a..HEAD -- mazeworld.html` prints nothing (no shell change in this plan)
  </acceptance_criteria>
  <done>Tiers, pools, bands, caps and bonuses are pinned to the existing BAG_FLOORS ladder; flag-off is proven identical in stock and rng cursor (missing key, strict check, seed-3 numbers); flag-on is proven to be today's draws plus exactly N in sequence, with the stock rewritten in place per depth and class; the full suite is green with the master and fixtures untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted save → engine state | an untrusted JSON blob supplies `storeRoll` on load |
| content tables → engine rng | pool/band data decides what rolls; a malformed table could roll an unsellable item |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-01 | Tampering | validateSave/rehydrate `storeRoll` | low | mitigate | `!!obj.storeRoll` boolean coercion at both entry points (the `dev` pattern); openStore checks `=== true`; coercion table pinned |
| T-33-02 | Tampering | store potion pool | medium | mitigate | explicit allow-list (content/store-stock.js), never `POTIONS` unfiltered; test asserts no pool entry carries eff "death" |
| T-33-03 | Repudiation | parity harness carve-out | low | mitigate | the strip is a one-word destructure addition per comparable; fixtures/master untouched — proven by `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` at every commit |
</threat_model>

<verification>
- `npm test` `# fail 0` (≥ 1915 pass); `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` empty; `git diff --stat 97a0e8a..HEAD -- test/parity` lists only comparables.js + the three parity test files; `git diff --stat 97a0e8a..HEAD -- mazeworld.html` empty.
- economy-parity (seed 3, depth 1, `{type:"openStore"}` through the live engine) green untouched; economy.test.js seed-3 pins green untouched; content-is-pure-data green with the new table.
</verification>

<success_criteria>
- ROADMAP success criterion 5 is engine-complete: store stock is rolled randomly and floor-appropriately for the current depth on each visit for every run the shell starts, with parity byte-identical for fixtures (draw order and count unchanged when the flag is off).
- The comparables carve-out decision is recorded (required — six lines, pure strip, because fixtures are `newRun()` output and diffState compares key sets).
</success_criteria>

<output>
Create `.planning/phases/33-ui-feel-store-polish/33-01-SUMMARY.md` when done. The SUMMARY MUST carry: `## Self-Check` (files exist, commit hashes found, `npm test` counts, the empty `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt`, the four-file `git diff --stat 97a0e8a..HEAD -- test/parity` list, the empty mazeworld.html diff), `## Deviations` (or "None"; explicitly record the RESEARCH-A2 correction — fixtures are newRun output, the flag is an option, the carve-out is six lines), and `## Human verification (deferred to end of run)` with Pixel 7 bullets, at minimum: (1) start a NEW run, walk to a store at depth 1 — Healing plus three cheap utility potions (cures/Strength/Enlarge), two weapons costing ≤ 250, at most a Leather upgrade, a +1 premium item; (2) Settings → long-press the version → start at depth 9 → first store — pricier weapons (400+ for a Fighter/Thief), Plate offered to a Fighter in lighter mail, a +3 premium, Acuteness possible; (3) leave and re-enter a store on the same floor — the roll changes; (4) resume a save made BEFORE this build (if one exists on the device) — its store still shows the old fixed four potions (flag off). Never instruct a pause for device checks.
</output>
