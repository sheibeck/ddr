---
phase: 33-ui-feel-store-polish
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - mazeworld.html
  - src/browser/settings.js
  - test/unit/settings.test.js
  - test/unit/shell-gear-toolbar.test.js
autonomous: true
requirements: [UIF-01, UIF-05]

must_haves:
  truths:
    - "UIF-01 (CONTEXT Area 1): on the GEAR tab every carried row reads `[name · detail]` on top and ONE flex action row beneath it — `[Use|Equip] … [Drop]` — with Drop pinned to the far right (margin-left:auto) and Use/Equip immediately to its left; every button ≥ 48 dp (class `small` → min-height:48px) and the row carries touch-action:manipulation; the rows are still `<li>` children of `ul.skills` (column-flex), the action row is a NEW `<div class=\"mw-gear-actions\">` sub-container appended once per row ONLY when the host passes `gearRow: true` (RESEARCH Pitfall 2 — reordering the actions array alone cannot produce side-by-side)"
    - "Tapping Drop turns that button, in place, into `Drop it? [Yes] [No]`; Yes dispatches the EXISTING engine action through the EXISTING bridge `window.mzDropItem(i)` → `{ type: \"dropItem\", i }` (engine/items.js#dropItem removes ANY bag item, potions included — no new engine action, no engine edit); No, a ~3 s `setTimeout` (DROP_CONFIRM_MS = 3000, never a CSS transition), any pointerdown outside the confirm (document capture listener), or a second row's Drop tap reverts it; at most one row is armed at a time; no modal card (card-vs-toast rule)"
    - "Equipped weapon/armor slots keep the existing Unequip path (wornRow) — no Drop on a worn slot; the Store's sell-list rows (`actions: [\"sell\", \"drop\"]` when the bag is full) and the combat use-list and loot card render byte-identically — `gearRow: true` is passed at exactly ONE call site, the GEAR tab's `renderCarriedList(carry, items, {…})`; `opts.guard` stays false for the GEAR host (Phase 32: the Drop confirm is not an encounter button)"
    - "UIF-05 (CONTEXT Area 1): `#btn-camp` is the LAST child of `.mw-viewport-chips` (the Marks/Centre row) styled as a `.mw-chip`, pushed to the far right by margin-left:auto with the row stretched `left:8px;right:8px`; `.mazefoot` holds only the D-pad, which is centered (`justify-content:center`); the button keeps its id, its `data-short` dimming rule `#btn-camp[data-short=\"1\"]{`, its paint() aria-label/short-state block and its `document.getElementById(\"btn-camp\").onclick` wiring (shell-party-camp pins)"
    - "The Handedness settings row, the `#app` data-handedness attribute, the `#app[data-handedness=…] .mazefoot` CSS rules, applySettings' setAttribute line and every comment mentioning the option are gone from mazeworld.html; `handedness` is gone from src/browser/settings.js (SETTINGS_DEFAULTS + ALLOWED_VALUES + comments); a persisted blob still carrying a `handedness` key is ignored silently on read (readSettings pulls only SETTINGS_DEFAULTS keys) and `writeSetting(\"handedness\", …)` is a no-op — no migration, no error, pinned by test"
    - "Tests: settings.test.js re-pinned to the five remaining fields (+ the ignored-stored-key test); new test/unit/shell-gear-toolbar.test.js pins the gear-row region (container, confirm copy, DROP_CONFIRM_MS/setTimeout, the single gearRow call site inside paint()'s carry block, no gearRow in the store region, no transition/animation token in the new CSS rules, mzDropItem still the only drop dispatch), the camp chip placement and the handedness removal; shell-party-camp / shell-input-guards / shell-loot-screen / shell-armor-display / shell-fight-gate stay untouched on disk and green; engine/content/test/parity diff empty"
  artifacts:
    - "mazeworld.html: DROP_CONFIRM_MS + revertDropConfirm() beside renderCarriedList; the gearRow branch of the drop action (confirm builder) and the post-loop .mw-gear-actions wrap; `.mw-gear-actions` / `.mw-drop-confirm` CSS; `gearRow: true` at the GEAR call site; `#btn-camp` moved into `.mw-viewport-chips` as a chip; `.mw-viewport-chips{…right:8px}` + `.mw-viewport-chips #btn-camp{margin-left:auto}`; the two `#btn-camp:hover/:active` parchment rules and both `#app[data-handedness…]` rules deleted; the Handedness settings row deleted; applySettings' setAttribute deleted"
    - "src/browser/settings.js: five-field SETTINGS_DEFAULTS / ALLOWED_VALUES, header comment updated"
    - "test/unit/settings.test.js re-pinned (five fields; stored handedness ignored); test/unit/shell-gear-toolbar.test.js (new)"
  key_links:
    - "GEAR tab paint() → renderCarriedList(carry, items, { actions: [\"use\",\"equip\",\"drop\"], gearRow: true, … }) → per-row .mw-gear-actions → Drop → confirm → Yes → window.mzDropItem(i) → inventoryAction({type:\"dropItem\", i}) → dispatch → paint() re-render (the armed confirm is reverted before dispatch)"
    - "Marks/Centre row → #btn-camp (last chip) → onclick → window.mzMakeCamp → dispatchWithToasts({type:\"camp\"}) (unchanged)"
    - "settings.js readSettings() → only SETTINGS_DEFAULTS keys merge → a stored handedness value never reaches applySettings (which no longer reads it)"
---

<objective>
Rebuild the two toolbar/gear-panel surfaces the device rounds complained about, now that the combat UI (Phases 30–32) is final: (UIF-01) the GEAR tab's carried rows get a real side-by-side action row — Use/Equip left, Drop pinned far right — and Drop becomes an inline two-tap confirm ("Drop it? [Yes] [No]", setTimeout revert, revert on any other tap) that dispatches the engine's existing `dropItem` action for any bag item, potions included; (UIF-05) Make Camp moves into the Marks/Centre chip row as its far-right button, the D-pad sits alone and centered in the control bar, and the Handedness option is removed end to end (settings row, `handedness` field, `data-handedness` attribute and CSS, applySettings line, tests) with a stored preference silently ignored. Shell + settings module + tests only.

Purpose: "Drop hidden/awkward in Gear" and "Make Camp under the wrong thumb" — the two feel items from the device rounds that were deferred until the combat surface stopped moving.
Output: mazeworld.html edits (renderCarriedList region, CSS, static markup, applySettings), src/browser/settings.js, test/unit/settings.test.js re-pins, test/unit/shell-gear-toolbar.test.js, `npm test` `# fail 0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Commit trailers (every commit in this phase, no exceptions — task commits and the metadata commit alike): end every commit message with these two lines, in this order:

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS

The gsd-tools `commit` helper does NOT append these trailers — if you use it, `git commit --amend` afterwards to add them (or commit with plain `git commit -m` including the trailers from the start).

Hard scope rules for this plan: shell/presentation/settings/tests only — do not edit anything under `engine/`, `content/`, or `test/parity/` (including `test/parity/prototype-master.js.txt`); no new engine action type (the GEAR Drop uses `window.mzDropItem` → `dropItem`, which already accepts any bag index); the Store's Sell/Drop rows, the combat use-list and the loot card render byte-identically (the new behaviour is opt-in via `gearRow: true` at the GEAR call site only); no tutorial toggle, no Settings row, no `tutorial` boolean (UIF-04 is DROPPED from v1.3 — CONTEXT, 2026-09-16). UAT is deferred to the end of the autonomous run — never pause for a device check; record Pixel 7 items in the SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-ui-feel-store-polish/33-CONTEXT.md
@.planning/phases/33-ui-feel-store-polish/33-RESEARCH.md
@.planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md
@src/browser/settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: GEAR rows — the .mw-gear-actions flex row (Use/Equip left, Drop far right) and the inline two-tap Drop confirm with a setTimeout revert</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html ~L3520-3600 (renderCarriedList: the header comment naming the four hosts; `mkBtn` at ~L3551-3560 with its inline marginTop/marginRight/alignSelf and the Phase 32 `opts.guard ? guardTap(bt, onClick) : (bt.onclick = onClick)` ternary — keep that literal; the per-row loop: `li.innerHTML = ...` then `for (const a of (opts.actions || []))` with the `use`/`equip`/`drop`/`sell`/loot branches — every `li.appendChild(mkBtn(` literal is pinned by shell-fight-gate (`it.kind === "potion" || it.use) li.appendChild(mkBtn("Use"`) and must stay byte-identical)
    - mazeworld.html ~L3090-3100 (the GEAR call site: `renderCarriedList(carry, items, { clear: false, actions: ["use", "equip", "drop"], emptyHtml: … })` — add `gearRow: true` here and nowhere else), ~L3070-3090 (`wornRow` — the equipped weapon/armor rows with their Unequip path: untouched)
    - mazeworld.html ~L5230-5238 (the store's `renderCarriedList(document.getElementById("sell-list"), S.c.items, { actions: usage.full ? ["sell", "drop"] : ["sell"] })` — untouched), the loot card's and combat use-list's `guard: true` call sites (untouched)
    - mazeworld.html L224-229 (`ul.skills{…flex-direction:column}` / `ul.skills li{display:flex;flex-direction:column;gap:1px;…}` — the column layout the new sub-container sits inside), L448 area (global `button{…touch-action:manipulation}`), L476 (`button.small{…padding:13px 14px;min-height:48px}` — the 48 dp guarantee the confirm's Yes/No inherit)
    - mazeworld.html L6724 (`window.mzDropItem = (i) => inventoryAction({ type: "dropItem", i });`), engine/items.js L506-516 (dropItem splices ANY c.items index — potions are bag items with kind "potion", so no new action is needed)
    - test/unit/shell-input-guards.test.js L78-79 + L195-198 (the renderCarriedList region slice `function renderCarriedList(` → `function renderDropShelf(` and the `opts.guard ? guardTap(bt, onClick)` pin), test/unit/shell-loot-screen.test.js L112-143 + L211-215 (region pins incl. `window\.mzDropItem` and the paint() carry-region anchor `renderCarriedList(carry,`), test/unit/shell-fight-gate.test.js L101-104
    - 33-RESEARCH.md Pitfall 2 (column-flex rows) and "Don't Hand-Roll" (no guardTap for the confirm)
  </read_first>
  <action>
    <!-- planner-discipline-allow: transition -->
    <!-- planner-discipline-allow: animation -->
    1. Constants + the one-armed-row state, declared directly ABOVE `function renderCarriedList(` (same classic script): `const DROP_CONFIRM_MS = 3000;` and `let dropConfirmRevert = null;` and `function revertDropConfirm() { if (!dropConfirmRevert) return; const r = dropConfirmRevert; dropConfirmRevert = null; r(); }`. Comment: Phase 33 (UIF-01, CONTEXT Area 1) — the GEAR tab's inline two-tap Drop confirm. DOM-local presentation state, never on S (serializeRun spreads S). Exactly one row can be armed; the revert is a setTimeout of DROP_CONFIRM_MS (never a CSS transition — reduced-motion safe and the guard rules of Phase 32 never depend on a transition either), and any pointerdown outside the confirm reverts it (a document-level capture listener registered only while armed). Not an encounter button: no guardTap here by design (Phase 32 scope).
    2. Inside renderCarriedList's action loop, change ONLY the drop branch: `else if (a === "drop") { if (opts.gearRow) li.appendChild(mkDropConfirm(i)); else li.appendChild(mkBtn("Drop", () => window.mzDropItem?.(i))); }` where `mkDropConfirm(i)` is a function declared inside renderCarriedList (after `mkBtn`, so it can reuse it) that: builds `const drop = mkBtn("Drop", arm); drop.classList.add("mw-gear-drop");` and returns it; `arm` does: `revertDropConfirm();` (any other armed row reverts first) → build `const wrap = document.createElement("span"); wrap.className = "mw-drop-confirm";` with a `<span>` whose textContent is `"Drop it?"`, `const yes = mkBtn("Yes", () => { revertDropConfirm(); window.mzDropItem?.(i); });` and `const no = mkBtn("No", () => revertDropConfirm());` appended in that order (Yes then No, both class `small` → 48 dp) → `drop.replaceWith(wrap);` → `const timer = setTimeout(revertDropConfirm, DROP_CONFIRM_MS);` → `const onAnyTap = (e) => { if (!wrap.contains(e.target)) revertDropConfirm(); };` and `document.addEventListener("pointerdown", onAnyTap, true);` → `dropConfirmRevert = () => { clearTimeout(timer); document.removeEventListener("pointerdown", onAnyTap, true); if (wrap.isConnected) wrap.replaceWith(drop); };`. Yes reverts BEFORE dispatching so the re-render (paint() after inventoryAction) never finds a stale armed confirm; a re-render that happens while armed simply drops the detached wrap (`isConnected` guard) and the timer's revert is a no-op. Because the wrap replaces the Drop button in place inside the action row, Yes/No sit exactly where Drop was (far right).
    3. The side-by-side row (RESEARCH Pitfall 2 — a markup change, not a reorder): after the action loop and BEFORE `container.appendChild(li)`, add: `if (opts.gearRow) { const btns = Array.from(li.querySelectorAll(":scope > button")); if (btns.length) { const row = document.createElement("div"); row.className = "mw-gear-actions"; for (const b of btns) row.appendChild(b); const dropBtn = row.querySelector(".mw-gear-drop"); if (dropBtn) { dropBtn.style.marginLeft = "auto"; dropBtn.style.marginRight = "0"; } li.appendChild(row); } }`. This keeps every existing `li.appendChild(mkBtn(` literal intact (the buttons are appended to `li` first, then moved into the row), so the pinned literals in shell-fight-gate/shell-loot-screen/shell-input-guards keep matching, and every non-gear host (store sell list, loot card, combat use-list) is byte-identical because the branch is opts-gated. Comment the block: Phase 33 (UIF-01) — the GEAR host's `[name · detail]` line stays first; its action buttons are re-parented into ONE flex row so Use/Equip sit immediately left of Drop and Drop is pinned to the row's right edge; `ul.skills li` is column-flex (CSS above), which is why appending buttons directly to the li stacks them.
    4. CSS, beside `ul.skills li i{…}` (L228): `.mw-gear-actions{display:flex;flex-wrap:wrap;align-items:center;gap:6px;width:100%;margin-top:4px;touch-action:manipulation}` and `.mw-drop-confirm{display:inline-flex;align-items:center;gap:6px;margin-left:auto;font-family:var(--mono);font-size:11px;letter-spacing:.06em;color:var(--ink-soft)}` and `.mw-drop-confirm button.small{margin:0}` (the Yes/No buttons drop mkBtn's inline top/right margins visually — set `yes.style.marginTop = no.style.marginTop = "0"` inline too since inline beats the sheet). No `transition`/`animation` token in these rules. Comment: Phase 33 (UIF-01) — the GEAR row's action row + the in-place Drop confirm; min-height 48px comes from button.small; the revert is a timer, so there is deliberately no transition here.
    5. The call site: in paint()'s carried-items block change the GEAR call to `renderCarriedList(carry, items, { clear: false, actions: ["use", "equip", "drop"], gearRow: true, emptyHtml: … })` (keep `clear: false` and the emptyHtml literal) and extend the comment above it: Phase 33 (UIF-01) — gearRow:true is passed HERE ONLY (the store sell list, the combat use-list and the loot card keep the Phase 14/29/32 rendering). Do NOT add `guard: true` (Phase 32's ratified list; shell-input-guards pins `guard: true` at exactly two sites).
    6. Run `node --test test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-fight-gate.test.js test/unit/shell-round-card.test.js` → `# fail 0` with none of those files edited. Commit: `feat(33-02): GEAR rows — one flex action row (Use/Equip left, Drop far right) and the inline two-tap Drop confirm with a setTimeout revert`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-fight-gate.test.js test/unit/shell-round-card.test.js 2>&1 | tail -5 && git status --porcelain test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-fight-gate.test.js engine content test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const DROP_CONFIRM_MS = 3000;' mazeworld.html` == 1 and `grep -c 'setTimeout(revertDropConfirm, DROP_CONFIRM_MS)' mazeworld.html` == 1 and `grep -c 'function revertDropConfirm()' mazeworld.html` == 1
    - `grep -c 'gearRow: true' mazeworld.html` == 1 and the line number of `gearRow: true` is greater than the line number of `const carry = document.getElementById("s-carry");` and less than the line number of `document.getElementById("doss-who")`
    - `grep -c 'className = "mw-gear-actions"' mazeworld.html` == 1 and `grep -c 'className = "mw-drop-confirm"' mazeworld.html` == 1 and `grep -c '"Drop it?"' mazeworld.html` == 1
    - `grep -c 'li.appendChild(mkBtn("Drop", () => window.mzDropItem?.(i)))' mazeworld.html` == 1 (the non-gear hosts' Drop is untouched) and `grep -c 'window.mzDropItem?.(i)' mazeworld.html` >= 3 (gear Yes, non-gear Drop, renderDropShelf)
    - `grep -c 'it.kind === "potion" || it.use) li.appendChild(mkBtn("Use"' mazeworld.html` == 1 and `grep -c 'opts.guard ? guardTap(bt, onClick) : (bt.onclick = onClick)' mazeworld.html` == 1 (pinned literals intact)
    - `grep -c 'guard: true' mazeworld.html` == 2 (unchanged)
    - `grep -c '^\.mw-gear-actions{' mazeworld.html` == 1
    - `grep '^\.mw-gear-actions{' mazeworld.html | grep -c 'touch-action:manipulation'` == 1
    - `grep '^\.mw-gear-actions{\|^\.mw-drop-confirm' mazeworld.html | grep -ic 'transition'` == 0 (region-scoped: only the two new CSS rules; `.mw-flash` elsewhere legitimately transitions)
    - `grep '^\.mw-gear-actions{\|^\.mw-drop-confirm' mazeworld.html | grep -ic 'animation'` == 0 (region-scoped, same two rules)
    - `grep -c 'document.addEventListener("pointerdown", onAnyTap, true)' mazeworld.html` == 1 and `grep -c 'document.removeEventListener("pointerdown", onAnyTap, true)' mazeworld.html` == 1
    - `git diff --stat 97a0e8a..HEAD -- engine content test/parity` prints nothing; node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>GEAR rows show name/detail above one action row with Use/Equip left and Drop far right; Drop arms an in-place "Drop it? [Yes] [No]" that reverts after 3 s, on No, or on any other tap and dispatches the existing dropItem on Yes; every other renderCarriedList host and every existing shell pin is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Make Camp into the Marks/Centre row (far right), D-pad centered, Handedness removed end to end (markup, CSS, applySettings, settings.js, settings.test.js)</name>
  <files>mazeworld.html, src/browser/settings.js, test/unit/settings.test.js</files>
  <read_first>
    - mazeworld.html L1362-1365 (`.mw-viewport-chips` markup: Marks + Centre chips), L1367-1384 (`.mazefoot`: the DR11 comment, `<button class="small" id="btn-camp">Make camp</button>`, then `.dpad`), L1288 (`<div id="app" class="mw-app" data-handedness="left">`), L1240-1246 (the Handedness settings row — delete the whole `.mw-settings-row`), L1229 + L1084 (comments naming the option)
    - mazeworld.html CSS L311-312 (`.mw-viewport-chips{position:absolute;top:8px;left:8px;display:flex;gap:8px;z-index:3}` and `.mw-chip{min-height:48px;…}`), L363-388 (the 04-DR11/DR13 `.mazefoot` comment blocks and the two `#app[data-handedness="left"|"right"] .mazefoot{…}` rules — delete both rules and rewrite the comments), L476-486 (`button.small`, the two `#btn-camp:hover/:active` parchment rules — delete them; `#btn-camp[data-short="1"]{opacity:.55;border-style:dashed}` — KEEP, shell-party-camp pins it), L318-322 (`.mw-flash` sits top-center over the same strip — pointer-events:none, transient)
    - mazeworld.html ~L2938-2944 (paint()'s `const campBtn = document.getElementById("btn-camp");` short-state block — untouched), L5518-5520 (`document.getElementById("btn-camp").onclick = () => { … mzMakeCamp … }` — untouched), L5599/L5606 (the chip listeners — untouched), ~L5933-5949 (applySettings: the `appEl.setAttribute("data-handedness", settings.handedness);` line and its 04-DR9 comment — delete), ~L5926-5928 (the currentSettings comment listing "handedness layout")
    - src/browser/settings.js (full file: header comment "six persisted UX settings … plus 04-DR9's handedness", SETTINGS_DEFAULTS' handedness field + its 04-DR11 comment, ALLOWED_VALUES' handedness line, readSettings' "only recognized keys" loop — the mechanism that makes a stale stored key harmless)
    - test/unit/settings.test.js L57-62 (the defaults test to delete), L64-86 (the six-field round-trip — becomes five), L101-121 (the invalid-value test's handedness lines), L132-139 (partial-blob test's handedness assertion)
    - test/unit/shell-party-camp.test.js L74-90 (the three btn-camp pins that must keep passing)
  </read_first>
  <action>
    <!-- planner-discipline-allow: handedness -->
    <!-- planner-discipline-allow: data-handedness -->
    <!-- planner-discipline-allow: Handedness -->
    <!-- planner-discipline-allow: data-setting="handedness" -->
    <!-- planner-discipline-allow: <button -->
    <!-- planner-discipline-allow: #btn-camp:hover -->
    <!-- planner-discipline-allow: #btn-camp:active -->
    1. Markup — move the camp button: delete `<button class="small" id="btn-camp">Make camp</button>` from `.mazefoot` and insert `<button type="button" class="mw-chip" id="btn-camp">Make camp</button>` as the LAST child of `.mw-viewport-chips` (after the Centre chip). Replace the DR11 HTML comment inside `.mazefoot` with: Phase 33 (UIF-05) — the D-pad is the ONLY child of the control bar and sits centered (justify-content:center); MAKE CAMP now lives at the far right of the Marks/Centre chip row above the map (`.mw-viewport-chips`), keeping its id so paint()'s short-state block and the mzMakeCamp wiring are unchanged. The button text stays "Make camp" (the chip's text-transform renders it MAKE CAMP).
    2. CSS: change `.mw-viewport-chips{position:absolute;top:8px;left:8px;…}` to add `right:8px` (the row now spans the viewport width) and add `.mw-viewport-chips #btn-camp{margin-left:auto}` directly under `.mw-chip:active…` with a comment (Phase 33 UIF-05: MAKE CAMP is the row's far-right chip; Marks/Centre stay left). Delete the two parchment rules `#btn-camp:hover:not(:disabled){…}` and `#btn-camp:active:not(:disabled){…}` (the `.mw-chip:hover/:active` rules now style it — an id rule would out-specify them and flash parchment on tap) and rewrite their comment to say the chip rules apply. Keep `#btn-camp[data-short="1"]{opacity:.55;border-style:dashed}` byte-identical. Delete both `#app[data-handedness="left"] .mazefoot{flex-direction:row}` and `#app[data-handedness="right"] .mazefoot{flex-direction:row-reverse}` and replace the 04-DR11/DR13 comment blocks above `.mazefoot{…}` with one short comment (Phase 33 UIF-05: the control bar holds only the centered D-pad; the camp button moved to the chip row; the former handed-layout option is gone). `.mazefoot{display:flex;align-items:flex-end;justify-content:center;gap:16px;margin-top:10px}` itself stays.
    3. `#app`: change `<div id="app" class="mw-app" data-handedness="left">` to `<div id="app" class="mw-app">`. Settings sheet: delete the whole Handedness `.mw-settings-row` (label + `data-setting="handedness"` options). applySettings: delete `appEl.setAttribute("data-handedness", settings.handedness);` and its 04-DR9 comment; update the currentSettings comment (~L5927) and the sheet's HTML comments (L1084, L1229) so no mention of the option remains — after this task `grep -ci handedness mazeworld.html` must be 0 INCLUDING comments (rewrite, don't leave history notes).
    4. src/browser/settings.js: remove `handedness: "left"` and its 04-DR11 comment from SETTINGS_DEFAULTS, remove `handedness: ["left", "right"]` from ALLOWED_VALUES, and rewrite the header/field-count comments to the five fields (sound, haptics, textSize, controlScheme, confirmBeforeQuit) with one added sentence: Phase 33 (UIF-05) — the former handed-layout field was removed; a persisted blob that still carries it is ignored on read because readSettings only merges SETTINGS_DEFAULTS keys, and writeSetting rejects unknown keys — no migration needed. After this `grep -ci handedness src/browser/settings.js` must be 0.
    5. test/unit/settings.test.js: delete the "defaults to 'left'" test; in the round-trip test drop the handedness write and expected key and retitle it to "each of the 5 fields"; in the invalid-value test drop the handedness lines; in the partial-blob test replace the handedness assertion with `assert.equal(settings.confirmBeforeQuit, SETTINGS_DEFAULTS.confirmBeforeQuit)`; ADD "Phase 33 (UIF-05): a stored handed-layout key is ignored silently" — seed the store with `JSON.stringify({ textSize: "S", handedness: "right" })`, assert `readSettings()` deepEquals `{ ...SETTINGS_DEFAULTS, textSize: "S" }` (no extra key, no throw), assert `Object.keys(SETTINGS_DEFAULTS)` has length 5 and does not include the old key, and assert `await writeSetting("handedness", "right")` returns the current settings unchanged (deepEqual to a fresh `readSettings()`) with the persisted blob still lacking the key.
    6. Run `node --test test/unit/settings.test.js test/unit/shell-party-camp.test.js test/unit/shell-input-guards.test.js` → `# fail 0` (shell-party-camp unedited). Commit: `feat(33-02): Make Camp joins the Marks/Centre row (far right), D-pad centered; Handedness option removed end to end`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/settings.test.js test/unit/shell-party-camp.test.js test/unit/shell-input-guards.test.js 2>&1 | tail -5 && echo "html:$(grep -ci handedness mazeworld.html) settings:$(grep -ci handedness src/browser/settings.js)" && git status --porcelain test/unit/shell-party-camp.test.js engine content test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci 'handedness' mazeworld.html` == 0
    - `grep -ci 'handedness' src/browser/settings.js` == 0
    - `grep -c 'id="btn-camp"' mazeworld.html` == 1 and `grep 'id="btn-camp"' mazeworld.html | grep -c 'class="mw-chip"'` == 1
    - the line number of `id="btn-camp"` is greater than the line number of `id="mw-chip-centre"` and less than the line number of `<div class="mw-flash" id="mw-flash"`
    - `sed -n '/<div class="mazefoot">/,/<div class="dpad" id="dpad">/p' mazeworld.html | grep -c '<button'` == 0 (region-scoped: the control bar between its opening div and the D-pad holds no button)
    - `grep -c '^\.mw-viewport-chips{' mazeworld.html` == 1 and `grep '^\.mw-viewport-chips{' mazeworld.html | grep -c 'right:8px'` == 1 and `grep -c '^\.mw-viewport-chips #btn-camp{margin-left:auto}' mazeworld.html` == 1
    - `grep -c '#btn-camp:hover' mazeworld.html` == 0
    - `grep -c '#btn-camp:active' mazeworld.html` == 0
    - `grep -c '#btn-camp\[data-short="1"\]{' mazeworld.html` == 1
    - `grep -c 'document.getElementById("btn-camp").onclick' mazeworld.html` == 1 and `grep -c 'const campBtn = document.getElementById("btn-camp");' mazeworld.html` == 1
    - `grep -c 'data-setting="handedness"' mazeworld.html` == 0
    - `grep -c '<div id="app" class="mw-app">' mazeworld.html` == 1
    - `grep -c 'sound:\|haptics:\|textSize:\|controlScheme:\|confirmBeforeQuit:' src/browser/settings.js` >= 10 (five fields × defaults + allowed) and `node -e "import('./src/browser/settings.js').then(m => { const k = Object.keys(m.SETTINGS_DEFAULTS); if (k.length !== 5) process.exit(1); })"` exits 0
    - `grep -c 'handedness' test/unit/settings.test.js` >= 2 (the ignored-stored-key test keeps the literal) and `node --test test/unit/settings.test.js` → `# fail 0`
    - `git status --porcelain test/unit/shell-party-camp.test.js engine content test/parity` prints nothing
  </acceptance_criteria>
  <done>Make Camp is the far-right chip of the Marks/Centre row, the D-pad is centered alone in the control bar, and no trace of the handed-layout option remains in the shell or the settings module — a stored value is ignored silently and pinned as such.</done>
</task>

<task type="auto">
  <name>Task 3: test/unit/shell-gear-toolbar.test.js — pin the gear row, the confirm, the single gearRow call site, the camp chip and the handedness removal; run the shell suites</name>
  <files>test/unit/shell-gear-toolbar.test.js</files>
  <read_first>
    - test/unit/shell-party-camp.test.js L1-45 (RAW_HTML/HTML/stripComments/CODE helpers — copy verbatim), test/unit/shell-input-guards.test.js L58-84 (`sliceBetween` + `renderCarriedListRegion()` + `storeRegion()` helpers — copy), test/unit/shell-loot-screen.test.js L211-215 (`paintCarryRegion()` anchor `const carry = document.getElementById("s-carry");`)
    - test/unit/shell-input-guards.test.js L116-122 (the no-transition/animation-token assertion style)
    - mazeworld.html after Tasks 1–2 (the regions being pinned)
  </read_first>
  <action>
    <!-- planner-discipline-allow: handedness -->
    1. Create test/unit/shell-gear-toolbar.test.js with the standard helpers (RAW_HTML, HTML, stripComments, CODE, sliceBetween) and regions: `renderCarriedListRegion()` = `function renderCarriedList(` → `function renderDropShelf(`; `paintCarryRegion()` = `const carry = document.getElementById("s-carry");` → `document.getElementById("doss-who")`; `storeRegion()` = `if (S.store) {` → `document.getElementById("a-leave").onclick`; `chipsMarkup()` = HTML sliced `<div class="mw-viewport-chips">` → `<div class="mw-flash"`; `mazefootMarkup()` = HTML sliced `<div class="mazefoot">` → `<div class="dpad" id="dpad">`.
    2. UIF-01 pins: (a) CODE has `const DROP_CONFIRM_MS = 3000;` once and `setTimeout(revertDropConfirm, DROP_CONFIRM_MS)` once, both inside or directly above the renderCarriedList region (the setTimeout inside the region); (b) the region contains `className = "mw-gear-actions"` once, `className = "mw-drop-confirm"` once, the literal `"Drop it?"`, `mkBtn("Yes"`, `mkBtn("No"`, `document.addEventListener("pointerdown", onAnyTap, true)` and `document.removeEventListener("pointerdown", onAnyTap, true)`, `wrap.isConnected`, and `opts.gearRow` at least 2 times; (c) the region still contains `li.appendChild(mkBtn("Drop", () => window.mzDropItem?.(i)))` (non-gear hosts) and `opts.guard ? guardTap(bt, onClick)`; (d) the Yes handler dispatches through `window.mzDropItem?.(i)` — count occurrences of `window.mzDropItem?.(i)` in the region ≥ 2; (e) `gearRow: true` occurs exactly once in CODE and that occurrence lies inside `paintCarryRegion()`; the store region contains no `gearRow`; `guard: true` still occurs exactly twice in CODE; (f) CSS: `.mw-gear-actions{` rule carries `display:flex`, `touch-action:manipulation` and `width:100%`; neither the `.mw-gear-actions{` nor any `.mw-drop-confirm` rule contains `transition` or `animation` (case-insensitive); (g) no new engine action: `fs.readFileSync("engine/actions.js")` — the action whitelist still lists `"dropItem"` and the engine diff is empty (assert via `git diff --stat 97a0e8a..HEAD -- engine` in the SUMMARY, not the test).
    3. UIF-05 pins: (a) `chipsMarkup()` contains `id="mw-chip-marks"`, `id="mw-chip-centre"` and `id="btn-camp"` in that order and the camp button carries `class="mw-chip"`; (b) `mazefootMarkup()` contains no `<button` (the bar's only child is the D-pad); (c) HTML contains no `handedness` (case-insensitive) and no `data-setting="handedness"`; the raw HTML contains no `#app[data-` rule; (d) `.mw-viewport-chips{` rule carries `left:8px` and `right:8px`; `.mw-viewport-chips #btn-camp{margin-left:auto}` exists; no `#btn-camp:hover` / `#btn-camp:active` rule; `#btn-camp[data-short="1"]{` still exists; `.mazefoot{` still carries `justify-content:center`; (e) `document.getElementById("btn-camp").onclick` and `const campBtn = document.getElementById("btn-camp");` each occur exactly once in CODE (wiring + short-state untouched); (f) src/browser/settings.js (readFileSync) contains no `handedness` (case-insensitive) and its SETTINGS_DEFAULTS literal names exactly sound/haptics/textSize/controlScheme/confirmBeforeQuit (import the module and assert `Object.keys(SETTINGS_DEFAULTS)` deepStrictEquals that list in that order).
    4. Run `node --test test/unit/shell-gear-toolbar.test.js test/unit/settings.test.js test/unit/shell-party-camp.test.js test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-fight-gate.test.js test/unit/shell-round-card.test.js test/unit/shell-toast-wiring.test.js` → `# fail 0`; then `npm test` → `# fail 0`; `git diff --stat 97a0e8a..HEAD -- engine content test/parity` — after Plan 01 this lists ONLY Plan 01's files (engine/state.js, engine/saveState.js, engine/economy.js, content/store-stock.js, content/index.js, the four parity carve-out files); assert nothing from THIS plan appears there (`git diff --stat 33-01-tail..HEAD -- engine content test/parity` empty, where 33-01-tail is Plan 01's last commit hash from its SUMMARY). Commit: `test(33-02): pin the GEAR action row + Drop confirm, the camp chip placement and the handedness removal`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-gear-toolbar.test.js test/unit/settings.test.js test/unit/shell-party-camp.test.js test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-fight-gate.test.js test/unit/shell-round-card.test.js test/unit/shell-toast-wiring.test.js 2>&1 | tail -5 && npm test 2>&1 | grep -E '^# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/shell-gear-toolbar.test.js` and `node --test test/unit/shell-gear-toolbar.test.js` → `# fail 0` and `# pass` >= 8
    - `grep -c 'DROP_CONFIRM_MS' test/unit/shell-gear-toolbar.test.js` >= 2 and `grep -c 'gearRow' test/unit/shell-gear-toolbar.test.js` >= 3 and `grep -c 'btn-camp' test/unit/shell-gear-toolbar.test.js` >= 4
    - `git status --porcelain test/unit/shell-party-camp.test.js test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-fight-gate.test.js test/unit/shell-round-card.test.js test/unit/shell-toast-wiring.test.js` prints nothing
    - `npm test` → `# fail 0` and `# pass` >= 1925
    - `git diff --stat 97a0e8a..HEAD -- engine content test/parity | grep -vc 'state.js\|saveState.js\|economy.js\|store-stock.js\|content/index.js\|comparables.js\|combat-parity\|magic-parity\|movement-parity\|files changed'` == 0 (only Plan 01's files under engine/content/test/parity)
  </acceptance_criteria>
  <done>The gear action row, the two-tap confirm (timer + outside-tap revert + single call site), the camp chip's position and the complete handedness removal are pinned by source assertions; every pre-existing shell suite is green and untouched; the full suite is green with no engine/content/parity change from this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| item data → DOM (gear rows) | item names/txt reach `li.innerHTML` (pre-existing); the new nodes use textContent |
| stored settings blob → applySettings | an old blob may carry a removed key |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-04 | Tampering | Drop confirm nodes | low | mitigate | "Drop it?", Yes, No are built with textContent/createElement — no template interpolation added; the row's existing innerHTML line is unchanged |
| T-33-05 | Elevation of Privilege | accidental drop | low | mitigate | two-tap confirm with a 3 s timeout and outside-tap revert; equipped slots have no Drop (unequip path only) |
| T-33-06 | Tampering | stale `handedness` in the settings blob | low | accept | readSettings merges only SETTINGS_DEFAULTS keys and writeSetting rejects unknown keys — the value never reaches the DOM; pinned by settings.test.js |
</threat_model>

<verification>
- `npm test` `# fail 0`; shell-gear-toolbar + re-pinned settings green; shell-party-camp / shell-input-guards / shell-loot-screen / shell-armor-display / shell-fight-gate / shell-round-card / shell-toast-wiring unchanged on disk and green.
- `grep -ci handedness mazeworld.html` == 0 and `grep -ci handedness src/browser/settings.js` == 0; `git diff --stat` under engine/content/test/parity shows only Plan 01's files.
</verification>

<success_criteria>
- ROADMAP success criteria 1 and 4 are code-complete: Use and Drop side by side (Drop far right) with a confirm step before a drop is applied; Make Camp lives in the Marks/Centre row (far right), movement buttons are centered, and the handedness option is gone.
- No engine change, no new engine action; the store/loot/combat lists render as before.
</success_criteria>

<output>
Create `.planning/phases/33-ui-feel-store-polish/33-02-SUMMARY.md` when done. The SUMMARY MUST carry: `## Self-Check` (files exist, commit hashes found, `npm test` counts, the untouched shell-test files, the engine/content/test/parity diff limited to Plan 01's files), `## Deviations` (or "None"), and `## Human verification (deferred to end of run)` with Pixel 7 bullets, at minimum: (1) Gear tab with a potion, a spare weapon and a treasure in the bag — each row shows the name/detail line and beneath it Use (or Equip) immediately left of Drop, Drop flush to the right edge, all ≥ 48 dp; (2) tap Drop — the button becomes "Drop it? [Yes] [No]" in place; wait ~3 s — it reverts; tap Drop then tap anywhere else — it reverts; tap Drop then No — it reverts; tap Drop then Yes on the potion — the potion leaves the bag (toast/Oracle line as today); (3) tap Drop on one row then Drop on another — only the second is armed; (4) the worn weapon/armor rows still show Unequip, never Drop; (5) open a store with a full bag — the Sell/Drop rows look exactly as before (no confirm); (6) Map tab — MAKE CAMP is the right-most chip of the Marks/Centre strip above the map, the D-pad is centered alone under the map, camp still dims when short on food and still camps on tap; the transient top-center flash label never permanently covers the chips; (7) Settings — no Handedness row; text size / sound / haptics / confirm-quit still round-trip. Never instruct a pause for device checks.
</output>
