---
phase: 33-ui-feel-store-polish
plan: 03
type: execute
wave: 3
depends_on: ["33-01", "33-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-map-store-polish.test.js
autonomous: true
requirements: [UIF-02, UIF-03, STORE-01]

must_haves:
  truths:
    - "UIF-03 (CONTEXT Area 2): the module-scope `let zoom = 1.5;` is the default — the literal midpoint of `ZOOM_MIN = 0.6` and `ZOOM_MAX = 2.4` (both unchanged); pinch zoom persists for the session only (the variable is never persisted, never written to settings or S) so every cold start returns to 1.5; no zoom settings row"
    - "UIF-02 (CONTEXT Area 2 + RESEARCH Pitfall 3): the map recenters on the party — via the EXISTING `window.mzCenterMap()` (centerMap: `pan = {x:0,y:0}; positionCanvas()`) — at all THREE 'map visible again' choke points: (1) renderEncounter()'s `encWasActive && !active` dismissal transition (Store Leave, beats Move on, death, joiner, find, loot), (2) showTab() when `name === \"maze\"` (returning from Hero/Gear/Oracle/Dead), (3) closeSettingsSheet() (scrim tap and Close button — its two callers); plus (4) on pinch-gesture RELEASE only — inside the viewport's `release` handler when a two-finger pinch just ended (never inside pointermove, RESEARCH Pitfall 4) — mirroring the existing textSize-change recenter (`window.fit?.(); window.mzCenterMap?.();`)"
    - "STORE-01 shell half (CONTEXT Area 3 feedback): the store header shows ONE extra `enc-sub` line under the purse line, from `const STORE_ROLL_COPY`, ONLY when `S.storeRoll === true` (a new run); an old save (flag off) renders today's header byte-identically (RESEARCH Open Question 3 → omit, not a different line); the copy is sarcastic, family-friendly, names the roll, and is scanned against content/safety-wordlist.js BANNED by test (the shell-round-card pattern); no toast, no Oracle line"
    - "Tests: source-assertion pins on the constant, the three hook sites + the pinch-release site (and the ABSENCE of any recenter inside the pointermove handler), the gated header line, and the copy's voice; every pre-existing shell suite untouched and green"
    - "Phase gate: `npm test` `# fail 0`, `npm run build:www` exit 0, `test/parity/prototype-master.js.txt` and every fixture untouched, the engine/content/test/parity diff for the whole phase limited to Plan 01's files; SUMMARY carries the milestone hand-off and the PROJECT.md Key Decision row text"
  artifacts:
    - "mazeworld.html: `let zoom = 1.5;`; `window.mzCenterMap?.()` inside the dismissal-transition block of renderEncounter, inside showTab's maze branch, inside closeSettingsSheet, and inside the viewport `release` handler on pinch end; `const STORE_ROLL_COPY`; the gated header line in the S.store branch"
    - "test/unit/shell-map-store-polish.test.js (new)"
    - ".planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md with ## Milestone hand-off and ## Key Decision row (for PROJECT.md)"
  key_links:
    - "panel dismissed → renderEncounter() → encWasActive && !active → lastDismissAt stamp (Phase 32) + window.mzCenterMap() → pan reset → positionCanvas()"
    - "tab tap → showTab(\"maze\") → window.mzCenterMap?.() (undefined-safe at init, since showTab(\"maze\") runs before the classic script assigns the bridge)"
    - "settings Close/scrim → closeSettingsSheet() → window.mzCenterMap?.()"
    - "two-finger pinch → pointermove scales zoom (no recenter) → second finger lifts → release() → window.mzCenterMap?.()"
    - "openStore (flag on) → S.storeRoll === true → renderEncounter store branch → STORE_ROLL_COPY line"
---

<objective>
Close the phase: (UIF-03) the map's default zoom becomes 1.5, the literal midpoint of the 0.6–2.4 range, session-only; (UIF-02) the map recenters on the party whenever it becomes visible again — the three real choke points RESEARCH found (renderEncounter's dismissal transition, showTab, closeSettingsSheet) plus pinch release, never per pointermove; (STORE-01, shell half) the store header names the depth roll in the sarcastic voice, only on a run whose `S.storeRoll` is true (Plan 01's flag), scanned for voice safety. Then run the full phase gate (`npm test`, `npm run build:www`, master/fixtures untouched) and write the hand-off SUMMARY the v1.3 audit/complete step will read.

Purpose: "map lost the party after closing the store", "default zoom too far out", "store always sells the same things" — the last three device-round items of v1.3, done against the finished combat UI. Last plan of the last phase of the milestone.
Output: mazeworld.html edits (zoom constant, four recenter sites, store header copy), test/unit/shell-map-store-polish.test.js, `npm test` `# fail 0`, `npm run build:www` exit 0, 33-03-SUMMARY.md with `## Milestone hand-off` and `## Key Decision row (for PROJECT.md)`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Commit trailers (every commit in this phase, no exceptions — task commits and the metadata commit alike): end every commit message with these two lines, in this order:

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS

The gsd-tools `commit` helper does NOT append these trailers — if you use it, `git commit --amend` afterwards to add them (or commit with plain `git commit -m` including the trailers from the start).

Hard scope rules for this plan: shell/presentation/tests only — do not edit anything under `engine/`, `content/`, or `test/parity/` (including `test/parity/prototype-master.js.txt`); the only engine surface this plan READS is `S.storeRoll` (Plan 01). `centerMap()`'s body is not modified — the four sites call the existing bridge. Never call a recenter inside the pinch `pointermove` handler. No zoom persistence of any kind. UAT is deferred to the end of the autonomous run — never pause for a device check; record Pixel 7 items in the SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-ui-feel-store-polish/33-CONTEXT.md
@.planning/phases/33-ui-feel-store-polish/33-RESEARCH.md
@.planning/phases/33-ui-feel-store-polish/33-01-SUMMARY.md
@.planning/phases/33-ui-feel-store-polish/33-02-SUMMARY.md
@.planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md
@content/safety-wordlist.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default zoom 1.5; recenter at the three panel-closed choke points and on pinch release (never per pointermove)</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html L2457-2462 (`let pan = { x: 0, y: 0 };` … the DR13 comment … the zoom declaration line, `const ZOOM_MIN = 0.6, ZOOM_MAX = 2.4;`, `clampZoom`), L2470-2484 (fit(): `CELL = Math.max(6, Math.round(baseCell * zoom))`), L2490-2503 (positionCanvas: the pan-dependent transform — why a panned-then-zoomed view drifts, RESEARCH Pitfall 4)
    - mazeworld.html L5602-5615 (`function centerMap() { pan = { x: 0, y: 0 }; positionCanvas(); }` + `window.mzCenterMap = centerMap;` and its Pass B1 doc comment — the bridge every site calls; body untouched)
    - mazeworld.html ~L4961-4975 (renderEncounter's top: `const active = hasActiveEncounter();` then the Phase 32 stamp `if (encWasActive && !active) lastDismissAt = Date.now();` then `encWasActive = active;` — shell-input-guards pins `encWasActive && !active` and `lastDismissAt = Date\.now\(\)` as regexes, so a braced block keeps them matching)
    - mazeworld.html L1617-1655 (initTabs/showTab: the `if (name === "oracle") window.__mzOracleToNewest?.();` line is the style to mirror; note `showTab("maze")` runs at L1654 BEFORE the classic script defines centerMap's bridge → the call must be `window.mzCenterMap?.()` (undefined-safe), never a bare `centerMap()`)
    - mazeworld.html ~L5972-5978 (`function closeSettingsSheet() { const sheet = …; if (sheet) sheet.hidden = true; }` and its two listeners: `mw-settings-scrim` click and `mw-settings-close` click)
    - mazeworld.html L5631-5678 (initMazeViewportControls: `pts`, `pointerStart`, `pinch`; pointerdown sets `pinch = { dist: spread(), zoom }` at 2 pointers; pointermove's `if (pinch && pts.size >= 2) { … zoom = clampZoom(…); fit(); positionCanvas(); return; }` — NO recenter goes here; `const release = e => { pts.delete(e.pointerId); if (pts.size < 2) pinch = null; … }` — the pinch-end site)
    - mazeworld.html ~L6027-6034 (the textSize settings handler: `window.fit?.(); window.mzCenterMap?.();` — the existing settle-then-recenter precedent), L6361 (`if (events.some((e) => e.type === "moved")) window.mzCenterMap?.();` — the post-move recenter, untouched)
    - 33-RESEARCH.md Pattern 3 (the three sites) and Pitfall 4 (pinch release)
  </read_first>
  <action>
    <!-- planner-discipline-allow: let zoom = 1; -->
    1. UIF-03: change the declaration `let zoom = 1;` to `let zoom = 1.5;` and extend the DR13 comment above it: Phase 33 (UIF-03, CONTEXT Area 2) — the default is the LITERAL midpoint of ZOOM_MIN…ZOOM_MAX ((0.6 + 2.4) / 2 = 1.5, the user chose literal over geometric); SESSION-ONLY — this variable is never persisted (no settings row, never on S), so every cold start returns here. Do not touch `ZOOM_MIN`, `ZOOM_MAX`, `clampZoom`, `fit()` or `positionCanvas()`.
    2. UIF-02 site 1 — renderEncounter's dismissal transition: turn the one-line stamp into a braced block: `if (encWasActive && !active) { lastDismissAt = Date.now(); window.mzCenterMap?.(); }` with the Phase 32 comment kept and one added sentence: Phase 33 (UIF-02, RESEARCH Pattern 3 site 1) — the same true→false transition is the one moment every over-map overlay (store, beats, death, joiner, find, loot) has just closed, so recenter the viewport on the party here; centerMap only touches pan/positionCanvas, never S.floor. (Keep `encWasActive = active;` and the `if (!active) { … return; }` block exactly as they are; the recenter fires before the panel-hide, which is fine — positionCanvas reads the viewport rect, which does not depend on the overlay.)
    3. Site 2 — showTab: after the `if (name === "oracle") window.__mzOracleToNewest?.();` line add `if (name === "maze") window.mzCenterMap?.();` with a comment: Phase 33 (UIF-02, site 2) — returning to the map from Hero/Gear/Oracle/Dead recenters on the party; optional-chained because the initial `showTab("maze")` at tab-init runs before the classic script assigns the bridge (a harmless no-op then). Site 3 — closeSettingsSheet: add `window.mzCenterMap?.();` after `if (sheet) sheet.hidden = true;` with a comment: Phase 33 (UIF-02, site 3) — the Settings sheet is reachable from any tab and outside the tab/overlay systems, so it needs its own recenter; both callers (scrim, Close) funnel through here. Do not add a fourth call anywhere else (the textSize handler already recenters; the post-move recenter stays).
    4. Pinch release (RESEARCH Pitfall 4, CONTEXT "recenter after any zoom change" read as after the gesture settles): in `release`, capture `const wasPinch = !!pinch;` as the FIRST statement, keep the existing lines, and after `if (pts.size < 2) pinch = null;` add `if (wasPinch && pts.size < 2) window.mzCenterMap?.();` with a comment: Phase 33 (UIF-02/03) — recenter ONCE when a two-finger pinch ends (the second finger lifted), mirroring the settings textSize recenter; NEVER inside pointermove — a per-tick reset would fight the live gesture and make pinch-to-zoom feel broken. The one-finger hand-off that follows (`startPan`) then starts from the recentered pan. Leave the pointermove handler byte-identical.
    5. Sanity: `node --test test/unit/shell-input-guards.test.js test/unit/shell-round-card.test.js test/unit/shell-fight-gate.test.js test/unit/shell-party-camp.test.js test/unit/shell-gear-toolbar.test.js` → `# fail 0` (the Phase 32 stamp pins still match the braced block). Commit: `feat(33-03): default zoom 1.5; recenter on the party at every panel-closed choke point and on pinch release`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-input-guards.test.js test/unit/shell-round-card.test.js test/unit/shell-fight-gate.test.js test/unit/shell-party-camp.test.js test/unit/shell-gear-toolbar.test.js 2>&1 | tail -5 && grep -c 'window.mzCenterMap?.()' mazeworld.html && git status --porcelain engine content test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^let zoom = 1.5;' mazeworld.html` == 1 and `grep -c 'let zoom = 1;' mazeworld.html` == 0 and `grep -c '^const ZOOM_MIN = 0.6, ZOOM_MAX = 2.4;' mazeworld.html` == 1
    - `grep -c 'if (encWasActive && !active) {' mazeworld.html` == 1 and `sed -n '/if (encWasActive && !active) {/,/^  }/p' mazeworld.html | grep -c 'lastDismissAt = Date.now();\|window.mzCenterMap?.();'` == 2
    - `grep -c 'if (name === "maze") window.mzCenterMap?.();' mazeworld.html` == 1
    - `sed -n '/function closeSettingsSheet() {/,/^  }/p' mazeworld.html | grep -c 'window.mzCenterMap?.();'` == 1
    - `grep -c 'const wasPinch = !!pinch;' mazeworld.html` == 1 and `grep -c 'if (wasPinch && pts.size < 2) window.mzCenterMap?.();' mazeworld.html` == 1
    - `sed -n '/vp.addEventListener("pointermove", e => {/,/const release = e => {/p' mazeworld.html | grep -c 'mzCenterMap\|centerMap('` == 0 (never per pointermove)
    - `grep -c 'window.mzCenterMap?.()' mazeworld.html` == 10 (6 pre-existing at HEAD 97a0e8a — textSize handler ~L6032, boot ~L6104, post-move L6361, the new-run centers L6395/L6412/L6442 — plus exactly the 4 new sites)
    - `grep -c 'function centerMap() {' mazeworld.html` == 1 and `grep -c 'window.mzCenterMap = centerMap;' mazeworld.html` == 1 (bridge and body untouched)
    - `git status --porcelain engine content test/parity` prints nothing; node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>Cold start opens the map at zoom 1.5; leaving the store/any overlay, returning to the Map tab, closing Settings, and lifting out of a pinch each snap the viewport back onto the party; pinch scaling itself never recenters mid-gesture.</done>
</task>

<task type="auto">
  <name>Task 2: Store header roll line gated on S.storeRoll; test/unit/shell-map-store-polish.test.js pins the zoom, the four recenter sites, the pointermove absence, the gated line and the copy's voice</name>
  <files>mazeworld.html, test/unit/shell-map-store-polish.test.js</files>
  <read_first>
    - mazeworld.html ~L5199-5210 (the S.store branch: `body.innerHTML += \`<p class="enc-head" …>A store</p> <p class="enc-sub">${S.c.gold.toLocaleString()} wilmst in your purse…</p> <div class="shelf" id="shelf"></div> …\`` — the purse line the new line follows), ~L2815-2830 (CONDITION_COPY / ROUND_CARD_COPY const-object placement — put STORE_ROLL_COPY beside ROUND_CARD_COPY)
    - test/unit/shell-round-card.test.js L1-60 (helpers) and L244-280 (the BANNED/ALLOWLIST word-boundary matcher + the ROUND_CARD_COPY extraction — copy the matcher verbatim), test/unit/shell-input-guards.test.js L58-84 (sliceBetween + storeRegion())
    - content/safety-wordlist.js (BANNED categories, ALLOWLIST — the copy must clear it)
    - .planning/phases/33-ui-feel-store-polish/33-01-SUMMARY.md (confirms the flag name `storeRoll` and that the shell's `S` is the engine state)
  </read_first>
  <action>
    1. Copy (Claude's discretion, sarcastic + family-friendly, names the roll): next to `const ROUND_CARD_COPY = …` add `const STORE_ROLL_COPY = "Stock rolled fresh for this floor. Deeper down, pricier regrets.";` with a comment: Phase 33 (STORE-01, CONTEXT Area 3 "Feedback") — the store header's one-line nod to the depth roll; shown ONLY on a run whose S.storeRoll is true (engine/state.js — a run this build started), so an old save's header is byte-identical to today; no toast, no Oracle line (the store is a screen, not an event). Player-facing: keep it clear of content/safety-wordlist.js (scanned by shell-map-store-polish.test.js). If you change the wording, keep it one sentence pair, no profanity/gore, and re-run the scan.
    2. Header line: in the S.store branch's template, directly after the purse `<p class="enc-sub">…</p>` line, add `${S.storeRoll === true ? \`<p class="enc-sub mw-store-roll">${STORE_ROLL_COPY}</p>\` : ""}` (a constant string — no user/item data interpolated, so a template literal is safe here). Add `.mw-store-roll{color:var(--ink-soft);font-style:italic}` beside the other `.enc-sub` rules only if such a rule block exists nearby; otherwise no new CSS (the enc-sub styling is enough). Comment above the template: Phase 33 (STORE-01) — the roll line is gated on the run flag, never on stock contents, so an old save (flag off, fixed stock) shows exactly the pre-Phase-33 header.
    3. Create test/unit/shell-map-store-polish.test.js with the standard helpers (RAW_HTML/HTML/stripComments/CODE/sliceBetween) and pins: (a) UIF-03: CODE matches `/^let zoom = 1\.5;/m` exactly once; parse `ZOOM_MIN`/`ZOOM_MAX` from `/const ZOOM_MIN = ([\d.]+), ZOOM_MAX = ([\d.]+);/` and assert `(min + max) / 2 === 1.5`; CODE contains no `zoom` write inside any `writeSetting`/`localStorage`/`mzStorage` expression (assert `!/writeSetting\([^)]*zoom/.test(CODE)` and `!/S\.zoom|state\.zoom/.test(CODE)` — session-only); (b) UIF-02 site 1: the renderEncounter region (`function renderEncounter()` → `function noteCombat(`) contains `if (encWasActive && !active) {` and, inside that braced block (slice from it to the next `}`), both `lastDismissAt = Date.now();` and `window.mzCenterMap?.();`; (c) site 2: the showTab region (`function showTab(name) {` → `for (const btn of tabs) btn.addEventListener("click"`) contains `if (name === "maze") window.mzCenterMap?.();` exactly once; (d) site 3: the closeSettingsSheet region (`function closeSettingsSheet() {` → `document.getElementById("mw-gear-btn")`) contains `window.mzCenterMap?.();` exactly once and both listeners (`mw-settings-scrim` and `mw-settings-close`) still route to `closeSettingsSheet`; (e) pinch: the release region (`const release = e => {` → `vp.addEventListener("pointerup", release)`) contains `const wasPinch = !!pinch;` and `if (wasPinch && pts.size < 2) window.mzCenterMap?.();`; the pointermove region (`vp.addEventListener("pointermove", e => {` → `const release = e => {`) contains NO `mzCenterMap` and NO `centerMap(` and still contains `zoom = clampZoom(pinch.zoom * (d / pinch.dist)); fit(); positionCanvas();`; (f) the bridge: `function centerMap() {` once, `window.mzCenterMap = centerMap;` once, the centerMap body contains `pan = { x: 0, y: 0 };` and `positionCanvas();` and nothing referencing `S.floor` (camera only); (g) STORE-01 header: CODE has `const STORE_ROLL_COPY = "…"` once; the store region (`if (S.store) {` → `document.getElementById("a-leave").onclick`) contains `S.storeRoll === true` exactly once and `STORE_ROLL_COPY` exactly once and the conditional's else-branch is the empty string (regex `S\.storeRoll === true \? `[^`]*` : ""`); `storeRoll` appears in CODE only inside that store region (count in CODE === count in region) — the shell reads the flag nowhere else; (h) voice: extract the STORE_ROLL_COPY string literal, assert non-empty, and assert zero BANNED word-boundary hits outside ALLOWLIST using the shell-round-card matcher; also assert the string contains no `<`/`>` (it is interpolated into a template).
    4. Run `node --test test/unit/shell-map-store-polish.test.js test/unit/shell-input-guards.test.js test/unit/shell-round-card.test.js test/unit/shell-gear-toolbar.test.js test/voice/safety-scan.test.js` → `# fail 0`. Commit: `feat(33-03): store header names the depth roll on a flagged run; pin zoom default, the four recenter sites and the header copy voice`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-map-store-polish.test.js test/unit/shell-input-guards.test.js test/unit/shell-round-card.test.js test/unit/shell-gear-toolbar.test.js test/voice/safety-scan.test.js 2>&1 | tail -5 && git status --porcelain engine content test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/shell-map-store-polish.test.js` and `node --test test/unit/shell-map-store-polish.test.js` → `# fail 0` and `# pass` >= 8
    - `grep -c 'const STORE_ROLL_COPY = "' mazeworld.html` == 1 and `grep -v '^\s*//' mazeworld.html | grep -c 'S.storeRoll === true'` == 1 and `grep -v '^\s*//' mazeworld.html | grep -c 'storeRoll'` == 1 (the shell reads the flag at exactly one non-comment site — the store header; comments may name it)
    - the line number of `S.storeRoll === true` in mazeworld.html is greater than the line number of `if (S.store) {` and less than the line number of `<div class="shelf" id="shelf"></div>`
    - `grep -c 'BANNED' test/unit/shell-map-store-polish.test.js` >= 1 and `grep -c 'mzCenterMap' test/unit/shell-map-store-polish.test.js` >= 5 and `grep -c 'pointermove' test/unit/shell-map-store-polish.test.js` >= 1
    - `git status --porcelain engine content test/parity` prints nothing; node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>A new run's store header carries the sarcastic roll line; an old save's header is unchanged; the zoom default, all four recenter sites, the pointermove absence, the gated line and the copy's voice are pinned by source assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate (npm test, build:www, master/fixtures untouched, phase-wide engine diff audit) and the milestone hand-off SUMMARY</name>
  <files>.planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md L206-245 (the Self-Check / Phase 33 hand-off / Human verification structure to mirror; the "deliberately-unguarded set" and haptics bullets to carry forward)
    - .planning/phases/33-ui-feel-store-polish/33-01-SUMMARY.md and 33-02-SUMMARY.md (their Pixel 7 lists — merge them ahead of this plan's items so the milestone gets ONE end-of-run pass; their commit hashes for the phase-wide diff audit)
    - .planning/PROJECT.md L156-170 (the Key Decisions table columns: Decision | Rationale | Outcome) and L60-61 (the two v1.3 checklist bullets "UI feel" and "Store stock random and floor-appropriate" the complete-milestone step will tick)
    - .planning/ROADMAP.md L246-260 (Phase 33 entry + success criteria), .planning/REQUIREMENTS.md L55-63 + L137-142 (UIF-04 dropped row)
  </read_first>
  <action>
    <!-- planner-discipline-allow: handedness -->
    <!-- planner-discipline-allow: Handedness -->
    1. Gate: `npm test` → `# fail 0` (record `# pass`; expected ≥ 1935: 1902 baseline + Plan 01 + Plan 02 + this plan's tests); `npm run build:www` → exit 0 (record the tail); `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` → empty; `git diff --stat 97a0e8a..HEAD -- engine content test/parity` → exactly Plan 01's files (engine/state.js, engine/saveState.js, engine/economy.js, content/store-stock.js, content/index.js, test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js) and nothing else; `git diff --stat <33-01 last commit>..HEAD -- engine content test/parity` → empty (Plans 02/03 touched no engine/content/parity file); `grep -ci handedness mazeworld.html src/browser/settings.js` → 0 0; `grep -c 'tutorial' mazeworld.html` → unchanged from baseline (UIF-04 dropped — nothing added; record the number).
    2. Write 33-03-SUMMARY.md with, in addition to the template's sections: `## Self-Check` (files exist, commit hashes found via `git log --oneline`, the counts and outputs from step 1), `## Deviations` (or "None"), `## Human verification (deferred to end of run)` — MERGE 33-01's and 33-02's Pixel 7 bullets first, then this plan's: (1) cold start — the map opens at the new default zoom (visibly closer than before; pinch shows it sits mid-range between fully-in and fully-out); pinch in, kill the app, relaunch — back to the default; (2) drag the map off the party, enter a store, Leave — the party is centered again; same after Move on from a feature card, after a loot card, after a Joiner card; (3) drag the map away, open Hero/Gear/Oracle/Dead, return to Map — centered; (4) drag away, open Settings, Close (and separately tap the scrim) — centered; (5) two-finger pinch: the map scales smoothly under the fingers without snapping mid-gesture; on lifting, it recenters on the party once; (6) new run's store header shows the roll line under the purse line; a pre-Phase-33 save (if one exists) shows no such line; the line reads family-friendly on device. `## Milestone hand-off` — everything the v1.3 audit/complete step should know: UIF-04 was DROPPED from v1.3 on 2026-09-16 (tutorial UI unwired; moved to the UX-06 backlog — REQUIREMENTS.md L58/L140 already say so; PROJECT.md's "UI feel" bullet still lists "tutorial on/off setting" and must be edited to drop it when ticked); the 32-03 hand-off items still open and NOT pulled into Phase 33 per CONTEXT (the deliberately-unguarded button set — GEAR tab actions, store goods/sell/Leave rows, renderDropShelf rows, the beats `a-evt` button, `btn-again`, the spell-menu cast buttons — and the haptics-on-hit/kill candidate) → a future feel pass; the store flag name is `state.storeRoll` (newRun option, set true only by engineAdapter#startNewRun; false on every fixture/bot/old save; stripped in six comparables), the tier ladder (BAG_FLOORS 2/5/9 → tiers 0–3), the content file (content/store-stock.js) and that tools/ bots still run flag-off (a future tuning pass can pass `storeRoll: true` if the ledgers should see rolled stock); the final Phase 33 test count and that the master + fixtures are untouched for the whole milestone; the GEAR Drop confirm is unguarded by design (Phase 32 scope); zoom is session-only by decision (no settings row); the Handedness setting is gone and a stale stored key is ignored (no migration). `## Key Decision row (for PROJECT.md)` — the exact table row to paste into PROJECT.md's Key Decisions table: `| Store stock rolled by depth behind a run flag (`state.storeRoll`, v1.3 Phase 33) | Parity must stay byte-identical for fixtures/old saves while new runs get floor-appropriate stock; a newRun option only the shell sets (the `dev` precedent) keeps every fixture, bot and pre-Phase-33 save on the frozen roll, with the new draws placed after every existing draw | ✓ Good — zero fixture edits, six one-line comparable strips, tiers reuse the BAG_FLOORS 2/5/9 ladder; on-device check of shallow vs. deep stock deferred to the end-of-run UAT batch |`.
    3. Commit the SUMMARY as the plan's metadata commit: `docs(33-03): complete map zoom/recenter, store header copy, full gate and milestone hand-off` (with the trailers).
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && npm test 2>&1 | grep -E '^# (pass|fail)' && npm run build:www 2>&1 | tail -2 && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt && git diff --stat 97a0e8a..HEAD -- engine content test/parity | tail -12 && grep -c '## Milestone hand-off\|## Key Decision row (for PROJECT.md)\|## Self-Check\|## Deviations\|## Human verification (deferred to end of run)' .planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` → `# fail 0` and `# pass` >= 1935
    - `npm run build:www` exit code 0
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing
    - `git diff --stat 97a0e8a..HEAD -- engine content test/parity | grep -vc 'state.js\|saveState.js\|economy.js\|store-stock.js\|content/index.js\|comparables.js\|combat-parity\|magic-parity\|movement-parity\|files changed'` == 0
    - `grep -ci 'handedness' mazeworld.html` == 0
    - `grep -ci 'handedness' src/browser/settings.js` == 0
    - `test -f .planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md` and `grep -c '^## Milestone hand-off' .planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md` == 1 and `grep -c '^## Key Decision row (for PROJECT.md)' .planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md` == 1 and `grep -c 'state.storeRoll' .planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md` >= 2 and `grep -c 'UIF-04' .planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md` >= 1
  </acceptance_criteria>
  <done>The full suite and the www build are green, the master and every fixture are byte-identical for the whole milestone, the phase-wide engine/content/parity diff is exactly Plan 01's, and the SUMMARY hands the v1.3 audit/complete step everything it needs — the dropped UIF-04, the still-open 32-03 items, the store flag, and the Key Decision row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state → DOM (store header) | `S.storeRoll` gates a constant string into `body.innerHTML` |
| touch input → camera state | pinch/pan gestures move `pan`/`zoom` (never `S.floor`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-07 | Tampering | store header template | low | mitigate | only a compile-time constant (STORE_ROLL_COPY) is interpolated; no item/user data; the test asserts the constant carries no angle brackets |
| T-33-08 | Denial of Service | recenter inside pointermove | medium | mitigate | the recenter is placed in `release` on pinch end only; the test asserts the pointermove region contains no recenter call |
| T-33-09 | Information Disclosure | zoom persisted to storage/save | low | mitigate | zoom stays a module-level variable; the test asserts no writeSetting/S.zoom/state.zoom expression exists |
</threat_model>

<verification>
- `npm test` `# fail 0` (≥ 1935); `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` empty; `git diff --stat 97a0e8a..HEAD -- engine content test/parity` = Plan 01's nine files only.
- shell-map-store-polish + shell-gear-toolbar + shell-input-guards + shell-round-card + shell-party-camp green; the pre-existing shell suites unchanged on disk.
</verification>

<success_criteria>
- ROADMAP success criterion 2 is code-complete: the map recenters on the party icon whenever the view returns from a full-screen panel (Store, sheet, Oracle, Settings, etc.), and the default zoom sits at the midpoint between fully zoomed in and fully zoomed out.
- ROADMAP success criterion 5's player-visible half (the header names the roll) is done on top of Plan 01's engine half.
- The milestone hand-off and the PROJECT.md Key Decision row are written for the v1.3 audit/complete step.
</success_criteria>

<output>
Create `.planning/phases/33-ui-feel-store-polish/33-03-SUMMARY.md` when done. The SUMMARY MUST carry: `## Self-Check`, `## Deviations` (or "None"), `## Human verification (deferred to end of run)` (33-01's and 33-02's Pixel 7 bullets merged ahead of this plan's six, one end-of-run pass — never instruct a pause for device checks), `## Milestone hand-off` (dropped UIF-04 + the PROJECT.md bullet edit it implies, the 32-03 unguarded set + haptics still open, the `state.storeRoll` flag/tier/content facts, bots flag-off, final counts, master/fixtures untouched), and `## Key Decision row (for PROJECT.md)` with the exact STORE-01 flag-guarded-roll table row text from Task 3 step 2.
</output>
