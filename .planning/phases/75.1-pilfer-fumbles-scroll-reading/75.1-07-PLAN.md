---
phase: 75.1-pilfer-fumbles-scroll-reading
plan: 07
type: execute
wave: 6
depends_on:
  - 75.1-06
files_modified:
  - src/browser/rollOdds.js
  - src/browser/gearTab.js
  - src/browser/combatMenu.js
  - content/skills.js
  - content/flavor.js
  - test/unit/rollOdds.test.js
  - test/unit/gear-view-models.test.js
  - test/unit/gear-tab-dom.test.js
  - test/unit/combatMenu.test.js
  - test/unit/roll-sign-consistency.test.js
  - test/unit/hp-not-wp.test.js
  - test/voice/safety-scan.test.js
  - test/unit/scroll-read-surfaces.test.js
autonomous: true
requirements:
  - RULES-10
must_haves:
  truths:
    - "rollOdds.scrollReadOdds(state) is the one source of the reader's odds text: an automatic reader reads 'without fail' naming Magic User or Runes/Signs; any other reader sees the reading range, the die, their intel and the fumble band, e.g. intel 14: reads on 8–20 (d20, intel 14); 1–3 backfires"
    - "The Gear tab's SCROLLS row and the combat ITEMS SCROLL row both append that text to their description, so the two surfaces always agree"
    - "Every range on those rows prints through rollRange.js (rangeText, bottomRangeText) and equals the range the engine's scrollReadBands gives and the read events print (a guard test ties surface, bands and a real readScroll event together); an intel that can never read says so ('nothing'), and one that can never fumble shows no fumble band"
    - "The Runes/Signs skill text says it reads any scroll without fail and that, without it, reading is an intelligence roll that can backfire"
    - "CLASS_NOTE for Magic User says every scroll reads for them without fail; the Fighter and Thief notes say a scroll is an intelligence roll that can backfire"
    - "The new copy is family-friendly and says hp, never WP: the voice safety scan and the hp-not-wp walk cover it"
  artifacts:
    - path: src/browser/rollOdds.js
      provides: "scrollReadOdds(state) and its frozen copy"
      contains: "export function scrollReadOdds"
    - path: test/unit/scroll-read-surfaces.test.js
      provides: "the odds text per reader, the two rows' agreement, the engine tie-in, the blurbs"
  key_links:
    - from: src/browser/rollOdds.js#scrollReadOdds
      to: engine/derived.js#scrollReadBands / scrollReaderOf
      via: "a pure read of the engine's own bands, never a restated formula"
      pattern: "scrollReadBands\\("
    - from: src/browser/gearTab.js and src/browser/combatMenu.js (SCROLLS rows)
      to: src/browser/rollOdds.js#scrollReadOdds
      via: "one odds text on both surfaces"
      pattern: "scrollReadOdds\\("
  prohibitions:
    - statement: "MUST NOT hide the odds before a read: a reader who must roll sees their reading range and fumble band on both SCROLLS rows before choosing to read"
      requirement_id: RULES-10
      category: transparency
      status: resolved
      verification: test
---

<objective>
Show every reader their scroll odds before they read, and make the class and skill descriptions state the new reading rule (RULES-10, ROADMAP criterion 4: "every affected class's descriptions state the new rules").

Purpose: anyone can now try a scroll, and a bad miss backfires. A player should see that risk on the SCROLLS rows (Gear tab and combat ITEMS) before tapping, in the same range format every other roll uses (Phase 74's one formatter).

Output: one odds helper, the two SCROLLS rows reading it, the Runes/Signs and class blurbs, and tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75.1-07 creates or changes.)

- 75.1-01: the Pilfer's magic-item fumble in `useItem`; the `pilferFumble` death cause; `SUB_NOTE.Pilfer`; the identity and CLASS-PASS rows; the bot as a Pilfer; readouts
- 75.1-02: `content/scroll-fumbles.js` (`SCROLL_FUMBLE`); docs/SPELLS.md "Scroll reading and fumbles"
- 75.1-03: foe-side mechanics (foe ward, Bubble catch-and-rebound, foe Mirror Self, foe Strength, foe Regeneration)
- 75.1-04: reader-side mechanics (`C.selfDot` with Ice's freeze, fight-long hexes), the `scrollFumble` death cause
- 75.1-05: `engine/scrollFumble.js#resolveScrollFumble` and its events
- 75.1-06: the intelligence read in `readScroll`, `canRead` removed, the read events, `bottomRangeText`, the bot reading for every class; readouts
- **This plan (75.1-07):** `scrollReadOdds` in rollOdds.js; the odds on the Gear tab and combat ITEMS SCROLLS rows; the Runes/Signs skill text; the Magic User, Fighter and Thief class notes; `test/unit/scroll-read-surfaces.test.js`
- 75.1-08: the fixture inventory, the readout record, the voice sample, the gates

## Flagged assumptions (surfaced, not silently decided)

- **Fighter and Thief notes gain a clause.** No class blurb mentioned scrolls before; ROADMAP criterion 4 asks every affected class's description to state the rule, so all three class notes gain one short clause. Phase 79 (VOX-04) owns the wider blurb pass.
- **Odds live in the row description,** not a new field, so the existing row layout and DOM need no change.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-CONTEXT.md
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-06-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-08-SUMMARY.md

<interfaces>
- engine/derived.js (75.1-06): scrollReaderOf(c) returns magicUser, runes or intel; scrollReadBands(intel) returns { atLeast, fumbleAtLeast, dieN }.
- src/browser/rollOdds.js: pure reads of engine/derived.js, formatted only through rollRange.js; existing exports heroHitOdds, heroHitOddsVs, foeHitOddsVs, fleeOdds; tested in test/unit/rollOdds.test.js.
- src/browser/rollRange.js: rangeText(atLeast, dieN) ("8–20", "nothing" above the die), bottomRangeText(below) ("1–3", "nothing" at 1 or less), dieText(dieN).
- src/browser/gearTab.js#gearConsumablesModel: the SCROLLS row `{ key: "scroll", name, qty, qtyText, desc: GEAR_COPY.scrollDesc, verb, enabled, reason, dispatch }` (75.1-06 made it always enabled). src/browser/combatMenu.js: the ITEMS submenu's scroll row `{ id: "scroll", label, cost, desc: COMBAT_MENU_COPY.scrollDesc, enabled: true, dispatch }`; combatMenu.js already imports fleeOdds from rollOdds.js.
- content/skills.js: FIGHTER_SKILLS "Runes/Signs" `{ cost: 2, txt: … }` (keys, order and costs are pinned by test/unit/abilities-catalog.test.js; only txt changes).
- content/flavor.js: CLASS_NOTE Fighter, Thief, Magic User; SUB_NOTE.Pilfer is 75.1-01's.
- test/unit/roll-sign-consistency.test.js: the range-pin and range-format guards and the one-formatter source scan; test/unit/hp-not-wp.test.js and test/voice/safety-scan.test.js register the COPY banks they scan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One odds helper, shown on both SCROLLS rows</name>
  <files>src/browser/rollOdds.js, src/browser/gearTab.js, src/browser/combatMenu.js, test/unit/rollOdds.test.js, test/unit/gear-view-models.test.js, test/unit/gear-tab-dom.test.js, test/unit/combatMenu.test.js, test/unit/roll-sign-consistency.test.js, test/unit/scroll-read-surfaces.test.js</files>
  <read_first>
    - src/browser/rollOdds.js and test/unit/rollOdds.test.js
    - src/browser/rollRange.js (rangeText, bottomRangeText, dieText)
    - engine/derived.js (scrollReaderOf, scrollReadBands)
    - src/browser/gearTab.js (gearConsumablesModel, GEAR_COPY) and test/unit/gear-view-models.test.js (its scroll-row cases)
    - src/browser/combatMenu.js (the ITEMS scroll row, COMBAT_MENU_COPY) and test/unit/combatMenu.test.js
    - test/unit/roll-sign-consistency.test.js (the range pin and the one-formatter scan)
  </read_first>
  <behavior>
    - A Wizard's odds text says it reads without fail and names Magic User; a Fighter with Runes/Signs says without fail and names Runes/Signs.
    - A Fighter with intel 14 reads "reads on 8–20 (d20, intel 14); 1–3 backfires" (wording per the copy, ranges exactly these).
    - Intel 1: the reading range is "nothing" and the fumble band is 1–10. Intel 20: no fumble band is shown.
    - A Pilfer with intel 11 sees its own ranges (the Pilfer reads like anyone).
    - The Gear tab's SCROLLS desc and the combat ITEMS scroll desc both contain the same odds text for the same state.
    - Guard: for intel 3, 9, 14 and 19, the surface's reading range equals rangeText(scrollReadBands(intel).atLeast, 20), and a real readScroll by that reader (forced as in test/unit/scroll-read.test.js) emits an event whose atLeast gives the same range.
    - The odds text contains no hyphen-minus between digits, no "%" and no "N+" shorthand.
  </behavior>
  <action>
1. src/browser/rollOdds.js: export scrollReadOdds(state), a pure read of scrollReaderOf and scrollReadBands formatted only with rangeText, bottomRangeText and dieText, from a frozen SCROLL_ODDS_COPY (automatic reader lines naming Magic User or Runes/Signs; the intel line with the reading range, die, intel and fumble band; the fumble clause omitted when the band is "nothing"). JSDoc: RULES-10 (Phase 75.1).
2. gearTab.js and combatMenu.js: each SCROLLS row's desc becomes its existing description followed by scrollReadOdds(state). Update each file's JSDoc for the row.
3. Tests: add scrollReadOdds cases to rollOdds.test.js; re-pin the scroll-row desc expectations in gear-view-models.test.js, gear-tab-dom.test.js and combatMenu.test.js; add the scroll reading range to roll-sign-consistency.test.js's range guards; create test/unit/scroll-read-surfaces.test.js with every behaviour above.
  </action>
  <verify>
    <automated>node --test test/unit/scroll-read-surfaces.test.js test/unit/rollOdds.test.js test/unit/gear-view-models.test.js test/unit/combatMenu.test.js test/unit/roll-sign-consistency.test.js test/unit/gear-tab-dom.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function scrollReadOdds" src/browser/rollOdds.js` equals 1.
    - `grep -c "scrollReadOdds(" src/browser/gearTab.js` and `grep -c "scrollReadOdds(" src/browser/combatMenu.js` are each at least 1.
    - `node --test test/unit/roll-sign-consistency.test.js` passes (the one-formatter scan included).
    - `node --test test/unit/scroll-read-surfaces.test.js` passes with at least 7 tests.
  </acceptance_criteria>
  <done>A reader sees exactly the odds the engine will roll, on both SCROLLS rows, before reading.</done>
</task>

<task type="auto">
  <name>Task 2: The Runes/Signs and class blurbs, and the copy scans</name>
  <files>content/skills.js, content/flavor.js, test/unit/hp-not-wp.test.js, test/voice/safety-scan.test.js, test/unit/scroll-read-surfaces.test.js</files>
  <read_first>
    - content/skills.js (FIGHTER_SKILLS; the "Runes/Signs" row)
    - content/flavor.js (CLASS_NOTE)
    - test/unit/abilities-catalog.test.js (what is pinned about the skill tables)
    - test/unit/hp-not-wp.test.js and test/voice/safety-scan.test.js (how COPY banks are registered)
  </read_first>
  <action>
1. content/skills.js: rewrite the Runes/Signs txt to say it reads any scroll without fail, and that without it a scroll is an intelligence roll that can backfire. Keys, order and costs unchanged.
2. content/flavor.js: add one short deadpan clause to CLASS_NOTE "Magic User" (every scroll reads for you, without fail) and to "Fighter" and "Thief" (a scroll is an intelligence roll, and a bad one reads itself back at you). Family-friendly; "hit points"/"hp", never WP.
3. Register SCROLL_ODDS_COPY in test/unit/hp-not-wp.test.js's walk and in test/voice/safety-scan.test.js's authored-copy collection.
4. Add blurb tests to scroll-read-surfaces.test.js: the Runes/Signs txt mentions both "without fail" and an intelligence roll; each of the three class notes mentions scrolls.
5. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/scroll-read-surfaces.test.js test/unit/abilities-catalog.test.js test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "without fail" content/skills.js` is at least 1.
    - `grep -ci "scroll" content/flavor.js` rises by at least 3 relative to the plan base.
    - `grep -c "SCROLL_ODDS_COPY" test/unit/hp-not-wp.test.js` and `grep -c "SCROLL_ODDS_COPY" test/voice/safety-scan.test.js` are each at least 1.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every affected class and the Runes/Signs skill state the new reading rule, and the new copy is voice-scanned.</done>
</task>

</tasks>

<verification>
- Both SCROLLS rows show the reader's true odds through the one formatter; the skill and class descriptions state the rule; npm test is green.
  <human-check>
  Pixel 7, at milestone close: on the Gear tab and in a fight's ITEMS menu, a Fighter without Runes/Signs sees "reads on …; … backfires" with their intel, a Magic User sees "without fail", and the two surfaces agree. The Runes/Signs skill and the class notes on the hero sheet describe the rule.
  </human-check>
</verification>

<success_criteria>
The reading rule is visible before every read and stated in every affected description (ROADMAP criterion 4, the RULES-10 half).
</success_criteria>

<output>
Create `.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-07-SUMMARY.md` when done.
</output>
