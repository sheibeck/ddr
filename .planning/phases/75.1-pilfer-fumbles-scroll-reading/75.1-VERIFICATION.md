---
phase: 75.1-pilfer-fumbles-scroll-reading
status: passed
verified: 2026-09-26
verifier: orchestrator (deferred-UAT protocol; gsd-verifier disabled in config)
score: 2/2 requirements
human_verification:
  - "RULES-09: as a Pilfer, use a worn ring, cloak or wielded staff's power repeatedly. About one use in twenty, the Oracle says it came apart, HP drops by the stated d10 (unsoaked) and the item is gone from its slot. Potions, scrolls, tools and equipping never fumble. The Pilfer's blurb states both the good and the bad and never names a diagnosis."
  - "RULES-10 reading: as a Fighter without Runes/Signs, read scrolls out of combat and in a fight. Most cast, some crumble with a 'can't make out the runes' line (never a refusal), and a bad miss backfires with a line naming who it hit. The SCROLLS count drops on every attempt. A Magic User or Runes/Signs holder always reads."
  - "RULES-10 odds: on the Gear tab and in a fight's ITEMS menu, a non-reader sees 'reads on N–20 (d20, intel X); 1–M backfires', a Magic User sees 'without fail', and both surfaces agree. The Runes/Signs skill and the class notes describe the rule."
  - "RULES-10 fumble severity (user rulings): a fumbled Death, Petrify or Freeze scroll never kills outright. It deals a heavy unsoaked blow (d10 + depth) plus Afraid. A fumbled Doze, Stun, Stupidity or Insane shows only LET THE ROUND PLAY with the reason, for at most four taps, with a 'Can't act' chip, and the normal menu returns after. A fumbled Blind shows 'Blinded' and a one-face STRIKE range. A fumbled Shrink shows 'Shrunk' and halves HP. Neither takes the menu away."
  - "RULES-10 helpful fumbles: a fumbled Shield shields the targeted foe (your blows soak into its ward); a fumbled Bubble throws your next blow back at you; a fumbled Summon adds a demon to the foes (never more than 4 live foes)."
---

# Phase 75.1: Pilfer Fumbles & Scroll Reading — Verification

**Verdict:** passed on automated evidence. The device checks above are batched into the milestone-close Pixel 7 checklist.

## Requirement coverage

| Req | Evidence | Status |
|-----|----------|--------|
| RULES-09 | 75.1-01: the IDENT-07 heal-only refusal is deleted. `useItem` rolls a derived d20 (`pilferFumble` stream) once per Pilfer use of jewelry, a cloak or a staff. A 1 fails the use, blasts the Pilfer for an unsoaked d10 and turns the item to dust; a lethal blast dies through `die(..., "pilferFumble")`, which has its own epitaphs. `SUB_NOTE.Pilfer`, the identity contract and CLASS-PASS were updated, and the bot uses items as a Pilfer. 13 tests | ✓ |
| RULES-10 | 75.1-02: `SCROLL_FUMBLE` classifies all 33 spells with a pin. 75.1-03: foe-side effects (ward/Bubble rebound, Mirror Self, Strength, Regeneration). 75.1-04: reader-side effects (`fumbleHeavyBlow`, `C.selfDot`, `C.heroOut` at most d4 with the `loseTurn` action and a soft-lock guard, `C.heroBlind` / `C.heroShrunk`). 75.1-05: `resolveScrollFumble` (harmful to the reader, area to the reader's side, helpful to the targeted foe including the summon reinforcement), with 42 tests. 75.1-06: anyone can read. `readScroll` uses the intelligence roll-high check on the `scrollRead` stream with no intel-12 floor; a fumble is below half the target, the scroll is consumed every time, and `canRead` is removed. 75.1-07: the odds show on both SCROLLS rows through `rollOdds.scrollReadOdds`, and the Runes/Signs text and class notes are updated. 75.1-09: the out-state combat menu (LET THE ROUND PLAY) and the Can't act / Blinded / Shrunk chips | ✓ |

## User rulings honoured

- No scroll fumble kills outright: kill effects become a d10 + depth unsoaked blow plus Afraid. A 200-HP reader survives every row, and that is tested.
- Turn loss lasts at most d4 (Doze/Stun/Stupidity/Insane). Blind and Shrink behave as they do on a foe and cost no turns. Foes hit an out hero normally.
- A fumbled Summon joins the foes, capped at 4 live.

## Automated gates

- Full `npm test` on master after the final merge: **6412/6412 pass, 0 fail**.
- Parity is **58/58**. **Zero parity fixtures moved across the phase** (every plan measured zero). A standing exposure guard in `divergence-records.test.js` checks 18 event types plus the out/blind/shrunk state, with a teeth test. The prototype master hash is unchanged (`a1f4d0dc…`).
- Declared state-pin re-pins, with traced causes: `solo-thief-pilfer` (a Pilfer's item use and Thief reads) and `deep-14` (Thief reads). The bot-tactics no-stall seeds were swapped where the new scroll-reading policy made them genuinely unresolvable.
- `npm run boot:check`: 4/4 PASS on master.

## Balance

The readouts are in `docs/DIFFICULTY-RETUNE.md` (the v2.1 Phase 75.1 H2) and are machine-checked. Mean death depth went 7.54 → 7.65 (75.1-01), then 7.65 → 7.60 (75.1-06). Reach-20 went 1.1% → 0.0%.

**Flagged for the user:** the per-floor survival band shows one miss at **floor 11** (dS −4.0; 31 reached, 6 deaths) in both 75.1-06's AFTER readout and the phase FINAL. The sample is small, and it was recorded, not retuned, per the no-compensation and checkpointed-fit rulings. Phase 75.3's sweep does not refit floors 1–12, so if this persists at 1,000 seeds it is a separate call for the user.
