---
phase: 75.1-pilfer-fumbles-scroll-reading
plan: 08
type: execute
wave: 7
depends_on:
  - 75.1-01
  - 75.1-02
  - 75.1-03
  - 75.1-04
  - 75.1-05
  - 75.1-06
  - 75.1-07
files_modified:
  - docs/DIFFICULTY-RETUNE.md
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - docs/ROLL-LEDGER.md
  - tools/voice-sample-output.txt
  - tools/readouts/75.1-08-final.txt
autonomous: true
requirements:
  - RULES-09
  - RULES-10
must_haves:
  truths:
    - "docs/DIFFICULTY-RETUNE.md has one `## v2.1 Pilfer fumbles & scroll reading (Phase 75.1) — bot readouts` H2, placed before the v1.2 H2 (the ledger test passes), recording 75.1-01's and 75.1-06's BEFORE/AFTER readouts and the phase FINAL readout, each block machine-checked with readout-compare --recorded, and a reading against the depth-20 unicorn and the floor 5–7 average (a 'Flag for the user' paragraph if the average run ending left floors 5–7; no retune here)"
    - "test/parity/FIXTURE-INVENTORY.md has a Phase 75.1 section recording, per rule, the predictor and the live measurement (commands and results), and any declared moves; the prototype master hash is unchanged"
    - "A standing guard in divergence-records.test.js replays every replay site and asserts zero Phase 75.1 events (pilferFumbled, scrollDeciphered, scrollGarbled, scrollFumbled, fumbleOnReader, fumbleOnSide, fumbleOnFoe, selfDotTick, readerFrozenSolid and the seven foe-side events) unless a Phase 75.1 divergence record declares them, and it fails on a doctored event list"
    - "docs/ROLL-LEDGER.md records the two new check sites (the Pilfer's d20 at atLeast 2 and the scroll read's d20 at 22 − intel with its fumble band), both on derived streams, without disturbing the Modifier ledger the sync test reads"
    - "tools/voice-sample-output.txt is regenerated and contains the new lines"
    - "The whole-phase gates hold at the head: npm test 0 failures, parity green, the roll-high guard and direction tests green"
  artifacts:
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 75.1 readout record"
      contains: "Phase 75.1"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the Phase 75.1 fixture section"
      contains: "Phase 75.1"
    - path: test/parity/divergence-records.test.js
      provides: "the Phase 75.1 exposure guard"
      contains: "RULES-10"
    - path: tools/readouts/75.1-08-final.txt
      provides: "the phase FINAL readout"
  key_links:
    - from: docs/DIFFICULTY-RETUNE.md (Phase 75.1 blocks)
      to: tools/readouts/75.1-0{1,6}-{before,after}.txt and 75.1-08-final.txt
      via: "readout-compare --recorded"
      pattern: "75.1-0"
---

<objective>
Close Phase 75.1's engine gate: record both balance changes against their readouts, write the fixture story with a standing exposure guard, note the new check sites in the roll ledger, refresh the voice sample, and prove every gate at the head.

Purpose: the standing engine gate (moved fixtures declared, a before/after readout wherever a rule moves balance, every new event narrated) and ROADMAP criterion 4 ("every moved fixture is declared and regenerated"). Each balance plan committed its own readouts so parallel waves never collided on the shared ledger; this plan writes the ledger once.

Output: the DIFFICULTY-RETUNE.md section and final readout, the FIXTURE-INVENTORY.md section and guard, the ROLL-LEDGER note, the voice sample, and the gate results with the phase's human checks rolled up.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75.1-08 creates or changes.)

- 75.1-01: the Pilfer's magic-item fumble in `useItem`; the `pilferFumble` death cause; `SUB_NOTE.Pilfer`; the identity and CLASS-PASS rows; the bot as a Pilfer; readouts
- 75.1-02: `content/scroll-fumbles.js` (`SCROLL_FUMBLE`); docs/SPELLS.md "Scroll reading and fumbles"
- 75.1-03: foe-side mechanics (foe ward, Bubble catch-and-rebound, foe Mirror Self, foe Strength, foe Regeneration)
- 75.1-04: reader-side mechanics (`C.selfDot` with Ice's freeze, fight-long hexes), the `scrollFumble` death cause
- 75.1-05: `engine/scrollFumble.js#resolveScrollFumble` and its events
- 75.1-06: the intelligence read in `readScroll`, `canRead` removed, the read events, the bot reading for every class; readouts
- 75.1-07: the reader's odds on the SCROLLS rows; the Runes/Signs and class blurbs
- **This plan (75.1-08):** the DIFFICULTY-RETUNE.md Phase 75.1 record and `tools/readouts/75.1-08-final.txt`; the FIXTURE-INVENTORY.md Phase 75.1 section and the exposure guard; the ROLL-LEDGER.md check-site note; the regenerated voice sample; the whole-phase gates

## Flagged assumptions (surfaced, not silently decided)

- **Readings are recorded, not acted on.** If the average run ending leaves floors 5–7, this plan writes a "Flag for the user" paragraph and does not retune (the checkpointed fit protocol: engine changes go to the user).
- **The bot never reads a scroll in combat,** so the fumble effects (75.1-03 to 75.1-05) cannot show up in any readout; the reading says so.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-CONTEXT.md
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-01-SUMMARY.md
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-06-SUMMARY.md

<interfaces>
- docs/DIFFICULTY-RETUNE.md: the Phase 72, Phase 73 and Phase 75 bot-readout H2s are the format to copy; `## v1.2 retune (Phase 27)` must stay the last H2 (test/unit/difficulty-retune-ledger.test.js). tools/readout-compare.mjs --recorded proves a fenced block against its source file.
- test/parity/FIXTURE-INVENTORY.md: the Phase 73 and Phase 75 sections are the format ("measured zero" subsections list the rule, the predictor and the measurement).
- test/parity/divergence-records.test.js: replaySiteEvents(seed, actions, opts) and the JOIN-02 / GRULE-01 / Phase 75 exposure tests, which replay every site and count event types.
- docs/ROLL-LEDGER.md is CRLF; 74-08 appended its section with a small Node script to keep CRLF and a zero-deletion diff; test/unit/roll-ledger-sync.test.js reads only its `## Modifier ledger` section.
- tools/voice-sample.mjs regenerates tools/voice-sample-output.txt.
- Every plan's SUMMARY carries its fixture measurement, readout headline lines and human checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The final readout and the DIFFICULTY-RETUNE.md Phase 75.1 record</name>
  <files>docs/DIFFICULTY-RETUNE.md, tools/readouts/75.1-08-final.txt</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md (the Phase 73 and Phase 75 H2s in full)
    - tools/readout-compare.mjs
    - 75.1-01-SUMMARY.md and 75.1-06-SUMMARY.md (their readout notes)
    - test/unit/difficulty-retune-ledger.test.js
  </read_first>
  <action>
1. Start `node tools/tune-difficulty.mjs --seeds=200` at the phase head with run_in_background, stdout to tools/readouts/75.1-08-final.txt, and wait for it.
2. Insert `## v2.1 Pilfer fumbles & scroll reading (Phase 75.1) — bot readouts` immediately before the v1.2 H2 (after the Phase 75 H2). It holds: a "Change under measurement" list (75.1-01: the Pilfer's fumble and the bot using items as a Pilfer; 75.1-06: anyone reads scrolls, the bot reading out of combat for every class); the parameters (200 seeds, start depth 1, the shipped dials); `### Plan 01 — BEFORE`, `### Plan 01 — AFTER`, `### Plan 06 — BEFORE`, `### Plan 06 — AFTER` and `### Phase 75.1 — FINAL`, each followed by a fenced block of the readout's headline lines copied verbatim from its file and a short reading; and a reading against the depth-20 unicorn and the floor 5–7 average, with a "Flag for the user" paragraph if the average left floors 5–7.
3. Prove every block with `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<file>` and run the ledger test.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## v2.1 Pilfer fumbles & scroll reading (Phase 75.1) — bot readouts" docs/DIFFICULTY-RETUNE.md` equals 1 and the ledger test passes.
    - For each of the five headings, `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<matching file>` exits 0 (the five commands listed in the SUMMARY).
    - tools/readouts/75.1-08-final.txt exists and is non-empty.
  </acceptance_criteria>
  <done>Both Phase 75.1 balance changes are recorded against machine-checked readouts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The fixture section, the exposure guard, the roll-ledger note, the voice sample and the gates</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js, docs/ROLL-LEDGER.md, tools/voice-sample-output.txt</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 73 and Phase 75 sections; any Phase 75.1 subsection a plan already added)
    - test/parity/divergence-records.test.js (replaySiteEvents; the Phase 75 exposure guard)
    - docs/ROLL-LEDGER.md (its closing sections) and .planning/phases/74-roll-display-modifier-honesty/74-08-SUMMARY.md (the CRLF append method)
    - every Phase 75.1 SUMMARY's fixture measurement and human-check notes
    - tools/voice-sample.mjs
  </read_first>
  <behavior>
    - Across every replay site (the test asserts the current site count): zero events of each Phase 75.1 type listed in the truths, unless a Phase 75.1 divergence record declares the holder.
    - The declared Phase 75.1 holder set equals the set the plans measured (empty if none moved).
    - The guard fails on a doctored event list carrying one pilferFumbled and one scrollFumbled.
  </behavior>
  <action>
1. FIXTURE-INVENTORY.md: add (or complete) `## Phase 75.1: Pilfer fumbles & scroll reading (RULES-09, RULES-10)`. Per rule (the Pilfer fumble; the scroll read; the foe-side, reader-side and resolver mechanics), state the predictor (why no replay site reaches it, or what moved), the measurement commands and results from each SUMMARY, and any declared moves with their records. Close with the prototype master hash and the guard's name.
2. divergence-records.test.js: add the Phase 75.1 exposure guard in the behaviour list, using replaySiteEvents over every site, with a teeth test on a doctored list.
3. docs/ROLL-LEDGER.md: append `## Phase 75.1 check sites (RULES-09, RULES-10)` with a Node script that keeps CRLF and deletes nothing: the Pilfer's steady-hands d20 (atLeast 2, a 1 fumbles, derived stream "pilferFumble"), and the scroll read's d20 (atLeast 22 − intel, reading faces intel − 1, fumble below ceil(atLeast / 2), derived stream "scrollRead"), both with no modifiers, plus a one-line note that the fumble effects' draws use the same scroll stream.
4. Run `node tools/voice-sample.mjs` and commit the regenerated output; confirm the new lines appear.
5. Gates: `npm test` 0 failures; `node --test "test/parity/**/*.test.js"` passes; `git hash-object test/parity/prototype-master.js.txt` is unchanged from the phase base; `node --test test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js` passes.
6. Roll every plan's human checks into one list in the SUMMARY for the milestone's batched Pixel 7 round.
  </action>
  <verify>
    <automated>node --test test/parity/divergence-records.test.js "test/parity/**/*.test.js" test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 75.1" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `grep -c "Phase 75.1" test/parity/divergence-records.test.js` is at least 1, and the new guard passes.
    - `git diff --numstat <plan-base> -- docs/ROLL-LEDGER.md` shows 0 deleted lines, and `grep -c "Phase 75.1 check sites" docs/ROLL-LEDGER.md` equals 1.
    - `grep -c "pilferFumbled\|scrollGarbled" tools/voice-sample-output.txt` is at least 2.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The phase's fixture story is complete and guarded, the ledgers and voice sample are current, and every gate is green at the head.</done>
</task>

</tasks>

<verification>
- Every balance change has a machine-checked BEFORE/AFTER; every moved fixture (if any) is declared; a standing guard keeps Phase 75.1 events off every replay site; the roll ledger names the new checks; the voice sample is current; npm test is green.
</verification>

<success_criteria>
ROADMAP Phase 75.1 criterion 4 holds: the descriptions state the rules (75.1-01, 75.1-07), both outcomes are narrated in voice, the new rolls come from derived streams, and every moved fixture is declared and regenerated, with the phase's human checks batched for the milestone close.
</success_criteria>

<output>
Create `.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-08-SUMMARY.md` when done. Include the five readout-compare commands, the gate results and the rolled-up human-check list.
</output>
