---
phase: 75.1-pilfer-fumbles-scroll-reading
plan: 08
subsystem: testing
tags: [difficulty-readout, parity-guard, roll-ledger, voice-sample, close-out]

# Dependency graph
requires:
  - phase: 75.1-pilfer-fumbles-scroll-reading
    provides: "75.1-01's Pilfer fumble + before/after readouts and 75.1-06's intelligence-read rewrite + before/after readouts (the two balance-moving plans this plan consolidates); every other plan's (02-05, 07, 09) own zero-fixture measurement and human-check note"
provides:
  - "docs/DIFFICULTY-RETUNE.md: the '## v2.1 Pilfer fumbles & scroll reading (Phase 75.1) — bot readouts' H2 (inserted before v1.2, after Phase 75), recording 75.1-01's and 75.1-06's BEFORE/AFTER readouts and the phase FINAL, each proven with readout-compare --recorded, plus a reading against the depth-20 unicorn and the floor 5-7 average — including a 'Flag for the user' for the persisting floor-11 per-floor-survival MISS (recorded, not retuned)"
  - "tools/readouts/75.1-08-final.txt: the phase-close 200-seed readout (byte-identical in content to 75.1-06's own AFTER readout — no later plan touches bot-reachable behavior)"
  - "test/parity/FIXTURE-INVENTORY.md: the '## Phase 75.1' section recording every plan's predictor and measurement (all nine plans measured zero moved fixtures)"
  - "test/parity/divergence-records.test.js: a standing RULES-09/RULES-10 exposure guard (countPhase751Exposure) replaying all 31 sites for 18 Phase 75.1 event types plus combat.heroOut/heroBlind/heroShrunk state, with a teeth test on a doctored pilferFumbled/scrollFumbled list; replaySiteEvents extended with heroOutEverSet/heroBlindEverSet/heroShrunkEverSet"
  - "docs/ROLL-LEDGER.md: the 'Phase 75.1 check sites' note naming the pilferFumble/scrollRead/fumbleBlow derived streams (CRLF-preserving append, zero deletions)"
  - "tools/voice-sample-output.txt: regenerated, carries the new Phase 75.1 event lines"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "countPhase751Exposure(events) mirrors 75-13's own countPhase75Exposure(events) shape exactly — a pure per-event-type tally function, extracted so a doctored-list 'has teeth' test needs no live engine replay."
    - "replaySiteEvents' heroOutEverSet/heroBlindEverSet/heroShrunkEverSet track a combat-sub-object field's truthiness at ANY point across a replay (not just the final state), mirroring the file's own pre-existing staffEverSet pattern — necessary because endCombat clears state.combat."

key-files:
  created: []
  modified:
    - docs/DIFFICULTY-RETUNE.md
    - test/parity/FIXTURE-INVENTORY.md
    - test/parity/divergence-records.test.js
    - docs/ROLL-LEDGER.md
    - tools/voice-sample-output.txt
    - tools/readouts/75.1-08-final.txt

key-decisions:
  - "Disambiguated the new '### Plan 01 — BEFORE/AFTER' and '### Plan 06 — BEFORE/AFTER' headings to '### Plan 01 (75.1) — BEFORE/AFTER' and '### Plan 06 (75.1) — BEFORE/AFTER' after readout-compare --recorded's extractFencedBlock matched Phase 75's own identically-named '### Plan 06 — BEFORE/AFTER' headings (which appear earlier in the same document) instead of the new Phase 75.1 ones — the plan's literal heading text collided with an existing section; the fix keeps the same content, only the heading text changes, and all five readout-compare commands now pass."
  - "The Phase 75.1 exposure guard's declared holder set (RULES751_EXPECTED_HOLDERS) is legitimately empty — no plan in the whole phase declared a divergence record — mirroring JOIN-02's own 'measured zero, still proven' discipline rather than skipping the declared-set assertion because it has nothing to assert."
  - "The Phase 75.1 — FINAL readout is not a fresh run repeated for its own sake — it is committed as tools/readouts/75.1-08-final.txt and directly diff-confirmed byte-identical (content, modulo CRLF) to 75.1-06's own already-committed AFTER readout, since no plan landing after 75.1-06 changes anything the bot or a non-combat scroll-fumble path can reach."

requirements-completed: [RULES-09, RULES-10]

coverage:
  - id: D1
    description: "docs/DIFFICULTY-RETUNE.md records both Phase 75.1 balance changes (75.1-01, 75.1-06) against machine-checked BEFORE/AFTER readouts plus the phase FINAL, with a reading against the depth-20 unicorn and the floor 5-7 average target, flagging the floor-11 per-floor-survival MISS for the user without retuning"
    requirement: RULES-09
    verification:
      - kind: unit
        ref: "node --test test/unit/difficulty-retune-ledger.test.js (13/13 pass — the v1.2 H2 is still the LAST H2)"
        status: pass
      - kind: other
        ref: "node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md \"### Plan 01 (75.1) — BEFORE\" tools/readouts/75.1-01-before.txt; ...AFTER... tools/readouts/75.1-01-after.txt; \"### Plan 06 (75.1) — BEFORE\" tools/readouts/75.1-06-before.txt; ...AFTER... tools/readouts/75.1-06-after.txt; \"### Phase 75.1 — FINAL\" tools/readouts/75.1-08-final.txt — all five exit 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "The Phase 75.1 fixture story is complete: FIXTURE-INVENTORY.md's Phase 75.1 section states every plan's predictor and measurement; a standing, teeth-tested exposure guard proves zero Phase 75.1 events/state fire across all 31 replay sites; the roll ledger names the two new check sites; the voice sample is current; every whole-phase gate is green"
    requirement: RULES-10
    verification:
      - kind: unit
        ref: "node --test test/parity/divergence-records.test.js \"test/parity/**/*.test.js\" test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js — 230/230 pass"
        status: pass
      - kind: other
        ref: "npm test: 6412/6412, 0 failures; git hash-object test/parity/prototype-master.js.txt unchanged (a1f4d0dc...); git diff --quiet -- test/parity/fixtures test/parity/prototype-master.js.txt exits 0; git diff --numstat -- docs/ROLL-LEDGER.md shows 0 deletions; grep -c \"pilferFumbled\\|scrollGarbled\" tools/voice-sample-output.txt = 2"
        status: pass
    human_judgment: false

duration: ~40min
completed: 2026-09-26
status: complete
---

# Phase 75.1 Plan 08: The phase's engine gate close — readouts, fixture inventory, exposure guard, roll ledger, voice sample Summary

**Closes Phase 75.1's engine gate: both balance changes recorded against machine-checked readouts (with a flagged, unretuned floor-11 per-floor-survival MISS), a standing 31-site exposure guard proving zero fixtures moved across all nine plans, the roll ledger naming the two new derived-stream check sites, and the voice sample carrying the new event lines — npm test 6412/6412, parity 56/56, prototype master byte-identical.**

## Performance

- **Duration:** ~40 min
- **Completed:** 2026-09-26T11:11:24Z
- **Tasks:** 2
- **Files modified:** 6 (0 created, 6 modified)

## Accomplishments

- **Task 1 — the final readout and the DIFFICULTY-RETUNE.md Phase 75.1 record:** Ran `node tools/tune-difficulty.mjs --seeds=200` at the phase head (tools/readouts/75.1-08-final.txt). Inserted `## v2.1 Pilfer fumbles & scroll reading (Phase 75.1) — bot readouts` immediately before the v1.2 H2 (after the Phase 75 H2), holding a "Change under measurement" list (75.1-01's Pilfer fumble and bot-buff change; 75.1-06's intelligence read and the bot reading for every class; a note that Plans 02-05/07/09 can never move a bot readout since the bot never reads a scroll in combat), the shared parameters line, and `### Plan 01 (75.1) — BEFORE/AFTER`, `### Plan 06 (75.1) — BEFORE/AFTER` and `### Phase 75.1 — FINAL`, each with a fenced block of verbatim readout lines and a short reading, plus the phase-wide reading against the depth-20 unicorn and the floor 5-7 average target. The FINAL readout is byte-identical in content to 75.1-06's own AFTER readout (confirmed via direct diff, modulo CRLF/LF) — no plan landing after 75.1-06 changes anything the bot or a non-combat scroll-fumble path can reach. The one heading collision found (my literal `### Plan 06 — BEFORE/AFTER` text matched Phase 75's OWN identically-named headings earlier in the same document, so `readout-compare --recorded`'s first-match search grabbed the wrong block) was fixed by disambiguating both to `### Plan 01 (75.1) —`/`### Plan 06 (75.1) —`.
- **Task 2 — the fixture section, the exposure guard, the roll-ledger note, the voice sample and the gates:** `test/parity/FIXTURE-INVENTORY.md` gains `## Phase 75.1: Pilfer fumbles & scroll reading (RULES-09, RULES-10)`, stating each plan's predictor and measurement pulled verbatim from its own SUMMARY.md — all nine plans measured **zero** moved fixtures (Plan 02's is a content-only file with no engine code to move by construction; Plans 07/09 are presentation-only). `test/parity/divergence-records.test.js` gains the standing exposure guard: `countPhase751Exposure(events)` tallies all 18 Phase 75.1 event types (`pilferFumbled`, `scrollDeciphered`, `scrollGarbled`, `scrollFumbled`, `fumbleOnReader`, `fumbleOnSide`, `fumbleOnFoe`, `selfDotTick`, `fumbleHeavyBlow`, `heroLostTurn`, `heroCameTo`, and the seven foe-side events `foeWardSoaked`/`foeWardBroken`/`foeWardFaded`/`foeBubbleCaught`/`foeBubbleRebound`/`foeMirrorFaded`/`foeRegenerated`); `replaySiteEvents` is extended with `heroOutEverSet`/`heroBlindEverSet`/`heroShrunkEverSet` (mirroring its own pre-existing `staffEverSet` pattern) so the guard also proves `state.combat.heroOut`/`heroBlind`/`heroShrunk` are never set across any of the 31 replay sites; a companion "has teeth" test feeds a doctored event list carrying one `pilferFumbled` and one `scrollFumbled` and confirms both would genuinely fail a zero-assertion. `docs/ROLL-LEDGER.md` gains `## Phase 75.1 check sites (RULES-09, RULES-10)`, appended via a small CRLF-preserving Node script (zero deletion lines): the Pilfer's steady-hands d20 (`atLeast 2`, a 1 fumbles, stream `pilferFumble`) and the scroll read's d20 (`atLeast 22 − intel`, reading faces `intel − 1`, fumble below `ceil(atLeast / 2)`, stream `scrollRead`), both unmodified, plus a note that the fumble resolver's own draws reuse the `scrollRead` stream and the heavy blow's d10 comes from its own `fumbleBlow` stream. `tools/voice-sample.mjs` was re-run, regenerating `tools/voice-sample-output.txt` (982 lines, carrying the new event lines — confirmed with `grep -c "pilferFumbled\|scrollGarbled"` = 2). Gates: `npm test` **6412/6412, 0 failures**; `node --test "test/parity/**/*.test.js"` 56/56; `git hash-object test/parity/prototype-master.js.txt` unchanged (`a1f4d0dc29782218d8e5aab65bc5989c33f917f0`); the six named roll-high/direction/state-pin/ledger-sync test files all pass (230/230 combined with the parity run).

## Task Commits

Each task was committed atomically:

1. **Task 1: The final readout and the DIFFICULTY-RETUNE.md Phase 75.1 record** — `8d6d471d` (docs)
2. **Task 2: The fixture section, the exposure guard, the roll-ledger note, the voice sample and the gates** — `054a8c26` (test)

**Plan metadata:** this SUMMARY.md's own commit (pending, by the orchestrator's convention)

## Files Created/Modified

- `docs/DIFFICULTY-RETUNE.md` — the Phase 75.1 bot-readouts H2 (BEFORE/AFTER × 2 plans + FINAL, both readings)
- `tools/readouts/75.1-08-final.txt` — the phase-close 200-seed readout
- `test/parity/FIXTURE-INVENTORY.md` — the Phase 75.1 fixture section (nine plans, all measured zero)
- `test/parity/divergence-records.test.js` — the RULES-09/RULES-10 exposure guard + teeth test; `replaySiteEvents`'s three new hero-flag returns
- `docs/ROLL-LEDGER.md` — the Phase 75.1 check-sites note (CRLF append)
- `tools/voice-sample-output.txt` — regenerated

## Readout Headline Lines (Plan 01 before → after, Plan 06 before → after, phase FINAL)

```
Plan 01: mean death depth=7.54 -> 7.65; reach-20 1.0% -> 1.0% (unchanged); verdict unchanged ("all floors 1-12 inside the pass band"); Outcome 174/26 -> 175/25 dead/stuck
Plan 06: mean death depth=7.65 -> 7.60; reach-20 1.0% -> 0.0%; verdict moves to "floors outside the pass band: 11 (dS -4.0)"; Outcome 175/25 -> 177/23 dead/stuck
Phase 75.1 FINAL: byte-identical (content) to Plan 06's own AFTER — mean death depth=7.60, reach-20 0.0%, floor-11 MISS still present, Outcome 177/23
```

## Decisions Made

- **Disambiguated the `### Plan 01 —`/`### Plan 06 —` headings to `### Plan 01 (75.1) —`/`### Plan 06 (75.1) —`** after discovering Phase 75's own section (earlier in the same document) already used identically-worded `### Plan 06 — BEFORE`/`### Plan 06 — AFTER` headings — `readout-compare --recorded`'s `extractFencedBlock` matches the FIRST heading with a given prefix, so the original literal text silently pulled the wrong (Phase 75) fenced block. The content is unchanged; only the heading text was made unique.
- **The Phase 75.1 exposure guard's declared holder set is asserted as legitimately empty** (`RULES751_EXPECTED_HOLDERS = []`), not skipped — mirroring JOIN-02's own "measured zero, still proven" discipline, since a measured-zero claim needs positive proof (the 31-site replay + the teeth test), never a default assumed by omission.
- **The Phase 75.1 — FINAL readout is verified identical to Plan 06's own AFTER readout** via a direct file diff (after CR-stripping) rather than treated as an independent, potentially-different measurement — this is the expected, provable consequence of no later plan touching bot-reachable or non-combat-fumble-reachable engine behavior, not an assumption.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] The literal `### Plan 06 — BEFORE`/`### Plan 06 — AFTER` headings collided with Phase 75's own pre-existing identically-named headings**
- **Found during:** Task 1 (running the five `readout-compare --recorded` acceptance commands)
- **Issue:** `readout-compare.mjs`'s `extractFencedBlock` finds the FIRST document heading starting with the given prefix; Phase 75's own "## v2.1 engine rules (Phase 75)" section (earlier in the same file) already has `### Plan 06 — BEFORE`/`### Plan 06 — AFTER` sub-headings for an unrelated plan (RULES-05/14), so the tool silently extracted THAT block instead of the new Phase 75.1 one — the compare then failed against a line (`max=40`) that belongs to the wrong section entirely.
- **Fix:** Renamed the four Phase 75.1 headings to `### Plan 01 (75.1) — BEFORE/AFTER` and `### Plan 06 (75.1) — BEFORE/AFTER` — unique prefixes, same content, same reading.
- **Files modified:** `docs/DIFFICULTY-RETUNE.md`
- **Verification:** all five `readout-compare --recorded` commands now exit 0; `node --test test/unit/difficulty-retune-ledger.test.js` still 13/13 (heading-collision fix touches no structure that test cares about)
- **Committed in:** `8d6d471d` (Task 1's own commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 — a real heading-collision bug against the plan's own literal heading text, caught by running the plan's own acceptance command rather than assumed correct)
**Impact on plan:** Necessary for correctness (the acceptance criterion is that all five commands exit 0); no scope creep — a four-heading text rename, content unchanged.

## Issues Encountered

- The first attempt to launch the 200-seed final readout via `run_in_background` with a trailing `&` inside the Bash command produced an empty output file (the shell's own backgrounding raced against the tool's own backgrounding, leaving the actual `node` process detached with no captured stdout). Re-ran the identical command WITHOUT the trailing `&` (letting the harness's own `run_in_background` flag do the backgrounding) — completed normally, ~15 minutes, with the expected non-empty output.

## Known Stubs

None.

## Threat Flags

None — this plan is a documentation/test-consolidation close-out (readouts, a fixture-inventory section, a standing replay guard, a roll-ledger note, a regenerated voice sample); it introduces no new network/auth/file-access/schema surface.

## User Setup Required

None — no external service configuration required.

## Rolled-up human checks (for the milestone-close Pixel 7 batch, per the deferred-UAT protocol)

Every plan in this phase deferred its own device verification to the milestone close. Rolled into one list here per this plan's own `<output>` instruction:

1. **75.1-01 (the Pilfer fumble):** As a Pilfer, use a worn ring or cloak repeatedly; about one use in twenty the Oracle says it came apart, the hp drops by the stated amount, and the item is gone from its slot. The Pilfer's class blurb states both the good and the bad.
2. **75.1-03 (foe-side fumble mechanics, needs 75.1-06 live):** Fumble a Shield scroll in a fight and watch your blows soak into the foe's ward; fumble a Bubble and see your next blow thrown back at you.
3. **75.1-06 (the intelligence read):** As a Fighter without Runes/Signs, read scrolls out of combat and in a fight. Most reads cast, some crumble with a "squint at the runes" line (never a refusal), and a bad one backfires with a line naming who it hit; the SCROLLS count drops every time.
4. **75.1-07 (the reader's odds):** On the Gear tab and in a fight's ITEMS menu, a Fighter without Runes/Signs sees "reads on …; … backfires" with their intel, a Magic User sees "without fail", and the two surfaces agree. The Runes/Signs skill and the class notes on the hero sheet describe the rule.
5. **75.1-09 (the hero-cannot-act shell, needs 75.1-06 live):** As a Fighter without Runes/Signs, fumble a Doze or Stupidity scroll in a fight. The combat menu shows only LET THE ROUND PLAY with the reason (never for more than four taps), the hero has a "Can't act" chip, each tap plays a round with its beats and an Oracle line, and the normal menu returns when the effect ends (or the fight ends). A fumbled Blind shows a "Blinded" chip and a one-face STRIKE range; a fumbled Shrink shows a "Shrunk" chip and halves the hero's hp; neither takes the menu away.

## Next Phase Readiness

- Phase 75.1's engine gate is fully closed: both balance changes are recorded against machine-checked readouts, the fixture story is complete and guarded by a standing, teeth-tested replay, the roll ledger names both new check sites, and the voice sample is current. `npm test` 6412/6412; parity 56/56; the prototype master is byte-identical to Phase 75's own close (`a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **The floor-11 per-floor-survival MISS is flagged for the user, not fixed.** It first appeared in 75.1-06's own AFTER readout and persists unchanged in this plan's phase-close FINAL readout (`reached=31`, `deaths=6`, `dS -4.0`) — a genuine, reproducible band failure at this commit on a small sample, recorded per the checkpointed fit protocol (engine changes go to the user) and this plan's own flagged assumption ("readings are recorded, not acted on"). No dial was retuned.
- The five rolled-up human checks above are ready for the milestone-close Pixel 7 UAT batch.
- No blockers.

---
*Phase: 75.1-pilfer-fumbles-scroll-reading*
*Completed: 2026-09-26*

## Self-Check: PASSED

- FOUND: docs/DIFFICULTY-RETUNE.md
- FOUND: tools/readouts/75.1-08-final.txt
- FOUND: test/parity/FIXTURE-INVENTORY.md
- FOUND: test/parity/divergence-records.test.js
- FOUND: docs/ROLL-LEDGER.md
- FOUND: tools/voice-sample-output.txt
- FOUND commit: 8d6d471d (docs: Task 1 — the final readout and the DIFFICULTY-RETUNE.md Phase 75.1 record)
- FOUND commit: 054a8c26 (test: Task 2 — the fixture section, the exposure guard, the roll-ledger note and the voice sample)
