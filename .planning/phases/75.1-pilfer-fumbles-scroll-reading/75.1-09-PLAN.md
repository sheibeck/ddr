---
phase: 75.1-pilfer-fumbles-scroll-reading
plan: 09
type: execute
wave: 4
depends_on:
  - 75.1-04
files_modified:
  - src/browser/combatMenu.js
  - mazeworld.html
  - test/unit/combatMenu.test.js
  - test/unit/shell-combat-actions.test.js
  - test/unit/hero-out-shell.test.js
  - src/browser/conditionEffects.js
  - test/unit/conditionEffects.test.js
autonomous: true
requirements:
  - RULES-10
must_haves:
  truths:
    - "While state.combat.heroOut is set, combatMenuViewModel's prompt says the hero can't act, why (asleep, stupefied or maddened) and for how many turns (at most four); slot 1 is the only enabled action, LET THE ROUND PLAY, dispatching { type: 'loseTurn' }; slots 2 to 4 are disabled with that reason; every submenu row is disabled"
    - "Without heroOut the view model is exactly what it was (a deep-equal pin against the plan base's output for the same state)"
    - "The Phase 71 locked state still applies: while a round's beats play, the single action is locked like any other, and it is enabled again after"
    - "mazeworld.html routes loseTurn through COMBAT_DISPATCH to window.mzLoseTurn, which calls engineCombatAction('loseTurn'), so the round's beats, fight log, rail, sounds and paint run exactly as for any other combat action; any other input that still reaches the engine (a keyboard shortcut, a stale tap) is turned into the same lost turn by 75.1-04's conversion"
    - "The hero chip: conditionsOf's heroOut entry paints as a bad-toned 'Can't act' chip showing the turns left; its tap text says the round goes on without you, nothing wakes you early, and foes hit you as usual"
    - "Hero Blind and hero Shrink (75.1-04) paint as bad-toned 'Blinded' and 'Shrunk' chips for the rest of the fight; the Blinded chip's tap text reports the to-hit change through conditionEffects.js's what-if (a WHAT_IF.heroBlind builder dropping combat.heroBlind), so it reads the same one-face range the hero sheet and combat menu show; the Shrunk chip says your blows land for half"
    - "No soft-lock through the shell's own model: from a lone hero out for 4 turns against harmless foes, a lone hero out for 4 turns against a foe that hits, and a hero with a party, repeatedly dispatching the view model's single enabled action through applyAction always advances the fight (round up, combat over, death, or the state spent) and reaches an acting hero, a cleared fight or a death within HERO_OUT_MAX (4) dispatches; at every out step exactly one action is enabled"
  artifacts:
    - path: src/browser/combatMenu.js
      provides: "the hero-cannot-act menu shape and its COMBAT_MENU_COPY strings"
      contains: "loseTurn"
    - path: mazeworld.html
      provides: "COMBAT_DISPATCH.loseTurn, window.mzLoseTurn, the heroOut, heroBlind and heroShrunk chip copy and tone"
      contains: "mzLoseTurn"
    - path: test/unit/hero-out-shell.test.js
      provides: "the menu-driven no-soft-lock drivers and the unchanged-menu pin"
  key_links:
    - from: src/browser/combatMenu.js (slot 1 while out)
      to: mazeworld.html#COMBAT_DISPATCH.loseTurn
      via: "dispatch { type: 'loseTurn' }"
      pattern: "loseTurn"
    - from: mazeworld.html (window.mzLoseTurn)
      to: engine/combat.js#loseTurn (through engineCombatAction and applyAction)
      via: "one lost round per tap"
      pattern: "engineCombatAction\\(\"loseTurn\"\\)"
    - from: engine/derived.js#conditionsOf (heroOut entry)
      to: mazeworld.html (CONDITION_COPY.heroOut, paintConditions)
      via: "the Can't act chip"
      pattern: "heroOut"
  prohibitions:
    - statement: "MUST NOT leave the player without a way forward while the hero cannot act: the combat screen always offers exactly one enabled action, and it always advances the fight"
      requirement_id: RULES-10
      category: safety
      status: resolved
      verification: test
---

<objective>
Give the hero-cannot-act state (75.1-04) its shell: a combat menu with one way forward, LET THE ROUND PLAY; the bridge that sends it to the engine; a "Can't act" chip, and "Blinded" and "Shrunk" chips for hero Blind and hero Shrink (RULES-10, user rulings 2026-09-25: "The effect needs an Oracle line and a chip saying the hero cannot act", and a lone hero who has lost turns must never be stuck).

Purpose: the engine keeps the fight moving one lost round per input (75.1-04), but the combat screen offers STRIKE / SPELLS / ITEMS / SOCIAL. While the hero can't act, those must give way to one honest action that plays the round, and the hero chip must say why. The Oracle and rail lines already exist (75.1-04's heroLostTurn and heroCameTo).

Output: the combat menu's out shape, the shell bridge and chip, and tests that drive the menu until the fight moves on.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75.1-09 creates or changes.)

- 75.1-01: the Pilfer's magic-item fumble in `useItem`; the `pilferFumble` death cause; `SUB_NOTE.Pilfer`; the identity and CLASS-PASS rows; the bot as a Pilfer; readouts
- 75.1-02: `content/scroll-fumbles.js` (`SCROLL_FUMBLE`); docs/SPELLS.md "Scroll reading and fumbles"
- 75.1-03: foe-side mechanics (foe ward, Bubble catch-and-rebound, foe Mirror Self, foe Strength, foe Regeneration)
- 75.1-04: reader-side mechanics: the reader's burn `C.selfDot`, the heavy blow `fumbleHeavyBlow` (d10 + depth, unsoaked, plus Afraid) replacing every instant kill, the hero-cannot-act state `C.heroOut` (at most four turns) with the `loseTurn` action, hero Blind (`C.heroBlind`) and hero Shrink (`C.heroShrunk`), the `scrollFumble` death cause
- 75.1-05: `engine/scrollFumble.js#resolveScrollFumble` and its events
- 75.1-06: the intelligence read in `readScroll`, `canRead` removed, the read events, the bot reading for every class; readouts
- 75.1-07: the reader's odds on the SCROLLS rows; the Runes/Signs and class blurbs
- 75.1-08: the fixture inventory, the readout record, the voice sample, the gates
- **This plan (75.1-09):** the combat menu's hero-cannot-act shape (one LET THE ROUND PLAY action dispatching `loseTurn`); `COMBAT_DISPATCH.loseTurn` and `window.mzLoseTurn` in mazeworld.html; the "Can't act", "Blinded" and "Shrunk" chips (copy, tone, tap text) and `WHAT_IF.heroBlind` in conditionEffects.js; `test/unit/hero-out-shell.test.js`

## Flagged assumptions (surfaced, not silently decided)

- **One tap per lost round, no auto-advance (decided, user 2026-09-25).** Each lost turn is one tap of LET THE ROUND PLAY, at most four in a row, so the player sees each round's beats at their own pace.
- **The chip lives in today's condition strip.** Phase 77's CMBUI-13 indicators may restyle it; this plan only makes it exist and say "Can't act".

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-CONTEXT.md
@.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-04-SUMMARY.md

<interfaces>
- 75.1-04: `state.combat.heroOut = { kind, left, spell }` with left 1..4; engine/combat.js exports loseTurn and HERO_OUT_MAX (4); applyAction accepts { type: "loseTurn" } and turns every hero combat action into a lost turn while heroOut is set; `state.combat.heroBlind` and `state.combat.heroShrunk` are fight-long flags (Blind drops the hero's weapon to-hit to one face inside derived.js#toHit; Shrink halves the hero's landed weapon damage); conditionsOf emits `{ key: "heroOut", polarity: "bad", kind, remaining: left }`, `{ key: "heroBlind", polarity: "bad" }` and `{ key: "heroShrunk", polarity: "bad" }`; the Oracle and rail lines for heroLostTurn and heroCameTo exist.
- src/browser/conditionEffects.js: WHAT_IF (key to a what-if state builder, e.g. afraid, foeEffect, darkness) and conditionEffectText(cn, state), tested in test/unit/conditionEffects.test.js.
- src/browser/combatMenu.js: combatMenuViewModel(state, opts) wraps combatMenuViewModelUnlocked(state) and applies the Phase 71 `locked` shape; the unlocked model returns `{ prompt, actions: [strike, spells-or-abilities, items, social], submenus }`; each action is `{ key, num, label, sub, enabled, accent, opens, dispatch? }`; COMBAT_MENU_COPY holds every string (scanned by test/unit/hp-not-wp.test.js and test/voice/safety-scan.test.js).
- mazeworld.html: `COMBAT_DISPATCH` (the one map from a row's `dispatch.type` to a window.mz* bridge; `const fn = COMBAT_DISPATCH[row.dispatch.type]`); the bridges `window.mzAttack = () => engineCombatAction("attack")` and siblings; engineCombatAction(type, extra) runs dispatchWithNarration, the beat plan (NARRATIVE_ACTIONS and friends), paint and renderEncounter; the combat keyboard map; the condition chip tables CONDITION_COPY, CONDITION_TONE, the chip tap-text map and paintConditions' detail rules (ward and flight have special cases; `typeof cn.remaining === "number"` prints the count).
- test/unit/shell-combat-actions.test.js reads mazeworld.html's source with comments stripped and asserts on the dispatch table; test/unit/combatMenu.test.js tests the pure view model.
- engine/engine.js#applyAction(state, action) returns { state, events }.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The combat menu while the hero cannot act, and the menu-driven no-soft-lock drivers</name>
  <files>src/browser/combatMenu.js, test/unit/combatMenu.test.js, test/unit/hero-out-shell.test.js</files>
  <read_first>
    - src/browser/combatMenu.js (combatMenuViewModel, combatMenuViewModelUnlocked, COMBAT_MENU_COPY)
    - test/unit/combatMenu.test.js
    - engine/combat.js (loseTurn, HERO_OUT_MAX) and engine/engine.js (the conversion)
    - test/unit/hero-out.test.js (75.1-04's engine drivers, to mirror their starting states)
  </read_first>
  <behavior>
    - heroOut { kind asleep, left 2 }: prompt names asleep and 2 turns; actions[0] is enabled with dispatch { type: "loseTurn" } and the LET THE ROUND PLAY label; actions[1..3] are disabled; every submenu row is disabled.
    - heroOut { kind stupefied, left 4 }: the prompt names stupefied and 4 turns.
    - combatMenuViewModel(state, { locked: true }) locks the single action; unlocked it is enabled.
    - Without heroOut, the view model deep-equals the output of the plan-base code for the same state (Fighter, Magic User, Bard and Thief fixtures).
    - Driver A (lone hero, out for 4, every foe stupefied): dispatching the one enabled action through applyAction reaches an acting hero after exactly 4 dispatches; each step shows exactly one enabled action.
    - Driver B (lone low-hp hero, out for 4, a foe that hits): reaches an acting hero or death within 4 dispatches, and each dispatch advances the round, ends the combat or kills.
    - Driver C (hero with a party, out for 4): the party's events appear on every lost turn and the fight reaches a clear, a death or an acting hero within 4 dispatches.
    - A blinded or shrunk hero who can act gets the normal four-action menu (Blind and Shrink cost no turns).
    - After the state is spent, the normal four-action menu returns.
  </behavior>
  <action>
1. src/browser/combatMenu.js: when state.combat.heroOut is set, the unlocked model returns the out shape: a prompt from COMBAT_MENU_COPY (the kind's word and the turns left), slot 1 as LET THE ROUND PLAY (accented, enabled, dispatch loseTurn, sub naming the kind), slots 2 to 4 disabled with the same reason, and every submenu's rows disabled. Add the strings and a frozen kind-to-word map to COMBAT_MENU_COPY (deadpan, family-friendly). The locked wrapper stays as is. JSDoc: RULES-10 (Phase 75.1, user ruling 2026-09-25).
2. Extend test/unit/combatMenu.test.js with the shape cases and the unchanged-menu pin; create test/unit/hero-out-shell.test.js with the three drivers.
  </action>
  <verify>
    <automated>node --test test/unit/combatMenu.test.js test/unit/hero-out-shell.test.js test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "loseTurn" src/browser/combatMenu.js` is at least 1.
    - `node --test test/unit/hero-out-shell.test.js` passes with the three drivers.
    - `node --test test/unit/combatMenu.test.js` passes, including the unchanged-menu pin.
  </acceptance_criteria>
  <done>While the hero can't act, the combat screen offers exactly one action, and it always moves the fight on.</done>
</task>

<task type="auto">
  <name>Task 2: The shell bridge and the Can't act, Blinded and Shrunk chips</name>
  <files>mazeworld.html, test/unit/shell-combat-actions.test.js, src/browser/conditionEffects.js, test/unit/conditionEffects.test.js</files>
  <read_first>
    - mazeworld.html (COMBAT_DISPATCH; the window.mz* bridges beside mzAttack; engineCombatAction and every action-type set it consults, such as NARRATIVE_ACTIONS; the combat keyboard map; CONDITION_COPY, CONDITION_TONE, the chip tap-text map and paintConditions)
    - test/unit/shell-combat-actions.test.js
    - src/browser/conditionEffects.js (WHAT_IF, conditionEffectText) and test/unit/conditionEffects.test.js
  </read_first>
  <action>
1. mazeworld.html:
   - Add `loseTurn` to COMBAT_DISPATCH, routed to a new `window.mzLoseTurn` defined beside window.mzAttack as a call to engineCombatAction("loseTurn").
   - Wherever engineCombatAction or the beat plan keys behaviour on the action type (NARRATIVE_ACTIONS and any similar set), treat loseTurn as a combat round exactly like attack.
   - Leave the keyboard map alone: a key that still sends attack while the hero is out is turned into a lost turn by the engine.
   - Add CONDITION_COPY.heroOut (label "Can't act", unit "rds") and CONDITION_TONE.heroOut ("bad"), with a tap text saying the round goes on without you, nothing wakes you early, and foes hit you as usual. The numeric remaining uses the generic count branch.
   - Add CONDITION_COPY.heroBlind (label "Blinded") and CONDITION_COPY.heroShrunk (label "Shrunk"), both tone "bad", both lasting the fight: heroBlind's tap text comes from conditionEffectText; heroShrunk's says your blows land for half.
   - Comment: RULES-10 (Phase 75.1).
2. src/browser/conditionEffects.js: add WHAT_IF.heroBlind (a what-if state with combat.heroBlind dropped), so the Blinded chip's tap text reports the live one-face range against the unblinded one through the existing toHitNow path. Add a conditionEffects.test.js case for it.
3. test/unit/shell-combat-actions.test.js: assert, on comment-stripped source, that COMBAT_DISPATCH has loseTurn, that window.mzLoseTurn calls engineCombatAction("loseTurn"), and that CONDITION_COPY has heroOut ("Can't act"), heroBlind and heroShrunk entries.
4. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-combat-actions.test.js test/unit/shell-narration-wiring.test.js test/unit/conditionEffects.test.js test/unit/roll-sign-consistency.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mzLoseTurn" mazeworld.html` is at least 2 (definition and dispatch entry).
    - `grep -c 'engineCombatAction("loseTurn")' mazeworld.html` equals 1.
    - `grep -c "heroOut" mazeworld.html` is at least 2 (copy and tone), and `grep -c "heroBlind" mazeworld.html` and `grep -c "heroShrunk" mazeworld.html` are each at least 1.
    - `grep -c "heroBlind" src/browser/conditionEffects.js` is at least 1.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>A tap on LET THE ROUND PLAY plays one round through the normal combat path, and the hero wears a chip saying they can't act.</done>
</task>

</tasks>

<verification>
- While the hero cannot act, the combat screen shows why and for how long, offers one action that always plays the round, and the chip says "Can't act"; nothing about the menu changes otherwise; npm test is green.
  <human-check>
  Pixel 7, at milestone close (after 75.1-06): as a Fighter without Runes/Signs, fumble a Doze or Stupidity scroll in a fight. The combat menu shows only LET THE ROUND PLAY with the reason (never for more than four taps), the hero has a "Can't act" chip, each tap plays a round with its beats and an Oracle line, and the normal menu returns when the effect ends (or the fight ends). A fumbled Blind shows a "Blinded" chip and a one-face STRIKE range; a fumbled Shrink shows a "Shrunk" chip and halves the hero's hp; neither takes the menu away.
  </human-check>
</verification>

<success_criteria>
The user's turn-loss ruling is playable on the device: an Oracle line and a chip say the hero cannot act, and the player always has exactly one way forward until it ends.
</success_criteria>

<output>
Create `.planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-09-SUMMARY.md` when done.
</output>
