---
phase: 75.1-pilfer-fumbles-scroll-reading
plan: 01
subsystem: engine-rules
tags: [items, combat, rng, narration, bot, difficulty-readout]

requires:
  - phase: 75-engine-rules-character-economy-grimoire-combat-bugs
    provides: "75-07/75-09's staff wield model (weaponRow/wieldedStaff, c.staff, useItem({slot:'weapon'}), the notWielded refusal) — read for the flagged assumption that a Pilfer's staff fumble is unreachable (wrongClass always fires first)"
provides:
  - "RULES-09: the Pilfer's heal-only useItem refusal is replaced by a fumble risk on jewelry/cloaks/staves — PILFER_FUMBLE_KINDS, pilferFumbles(c, it), pilferFumbleRng(state, rng, it), and the fumble step in engine/items.js#useItem (last rung before itemUsed)"
  - "On a derived d20 roll of 1 (rollCheck at atLeastFor(19, 20)): the use fails, a d10 blast (no armor/ward soak) hits the Pilfer, and the item leaves its bag index or worn slot with nothing left behind (pilferFumbled event); a blast that zeroes hp dies through die(state, 'pilferFumble', it.n, ...)"
  - "content/epitaphs.js: EPITAPHS.pilferFumble, CAUSE_TEXT.pilferFumble, CAUSE_TEXT_TOKENS.pilferFumble; src/browser/scoreTag.js TAG_CAUSES gains 'pilferFumble' (append-only)"
  - "src/browser/eventNarration.js#pilferFumbled and src/browser/narrationLines.js#pilferFumbled (Oracle + rail lines, FEATURE_EVENTS entry); the retired useRefused 'pilfer' reason removed from both files and narrationLinesTable.test.js"
  - "content/flavor.js SUB_NOTE.Pilfer restated to name both the good (traps/locks) and the new bad (the fumble); docs/CLASS-PASS.md and docs/USABLE-FEATURES-AUDIT.md updated with a 'Superseded by Phase 75.1' note, IDENT-07 history kept"
  - "tools/lib/tuning-bot.mjs: the round-1 buff tier's Pilfer skip is removed — the bot buffs like anyone now; test/unit/roll-high-state-pins.test.js's solo-thief-pilfer pin re-measured and re-pinned (bisected to a newly-allowed Pilfer item use, isolated from the bot-tactics change by a scratch revert)"
  - "tools/readouts/75.1-01-before.txt / -after.txt: a 200-seed bot readout measuring the balance effect"
affects: [75.1-02, 75.1-03, 75.1-04, 75.1-05, 75.1-06, 75.1-07, 75.1-08, 75.1-09]

tech-stack:
  added: []
  patterns:
    - "A Pilfer's fumble check and blast draw from ONE pilferFumbleRng(state, rng, it) instance per use — derivedRng(<main cursor or 0>, 'pilferFumble', state.acts, it.n) — called once, then drawn from sequentially (rollCheck's d20, then, only on a fumble, a d10); the main rng is never touched, so a non-fumbling Pilfer use costs the exact same main-rng draws a non-Pilfer's identical use would."
    - "The fumble step sits as the LAST rung of useItem's refusal ladder — after itemReady, before itemUsed — so every earlier refusal (pending fight, wrongClass, notWielded, notWorn, combatOnly, notDark, cooldown/recharging) still returns before any draw."

key-files:
  created:
    - test/unit/pilfer-fumble.test.js
  modified:
    - engine/items.js
    - content/epitaphs.js
    - content/flavor.js
    - src/browser/scoreTag.js
    - src/browser/eventNarration.js
    - src/browser/narrationLines.js
    - tools/lib/tuning-bot.mjs
    - docs/CLASS-PASS.md
    - docs/USABLE-FEATURES-AUDIT.md
    - test/unit/identity-contract.test.js
    - test/unit/identity-world.test.js
    - test/unit/usable-features-audit.test.js
    - test/unit/narrationLinesTable.test.js
    - test/unit/bot-tactics.test.js
    - test/unit/roll-high-guard.test.js
    - test/unit/roll-high-state-pins.test.js
    - test/unit/scoreTag.test.js
    - test/parity/roll-high-invariant.test.js
    - tools/readouts/75.1-01-before.txt
    - tools/readouts/75.1-01-after.txt

key-decisions:
  - "pilferFumbled carries item as a plain string (it.n), and either `slot` (a worn use) or `index` (a bag use) — mirroring die()'s own detail argument (it.n), never a nested item object, so EPITAPHS/CAUSE_TEXT's {foe} token and the Oracle/rail builders all read one flat field."
  - "The Pilfer bad row in identity-contract.test.js searches state.acts (0..5000) with a local findPilferFumbleActs/predictPilferFumble helper pair, mirroring pilfer-fumble.test.js's own search — every action-file test suite keeps its own local copy rather than cross-importing, per this repo's established per-file convention."
  - "solo-thief-pilfer's pin-hash move was bisected AWAY from this plan's own bot-tactics.mjs edit: a scratch copy of tuning-bot.mjs with ONLY the round-1-buff Pilfer skip restored reproduced the SAME (new) hash, proving the divergence is entirely the engine's useItem change (a previously-silent useRefused-pilfer no-op now actually resolving, first at a bagged Amulet of Light use, action 333) — not the bot decision-policy edit. The scratch file was never committed."

requirements-completed: [RULES-09]

coverage:
  - id: D1
    description: "A Pilfer uses a use-activated jewel/cloak/staff under the normal rules; the heal-only refusal is gone; a derived d20 roll of 1 fails the use, blasts the Pilfer for a d10 (no armor/ward soak) and dusts the item; a roll of 2+ proceeds exactly as for anyone"
    requirement: RULES-09
    verification:
      - kind: unit
        ref: "test/unit/pilfer-fumble.test.js (13 tests: worn/bagged fumble, roll>=2 success, adjacency, boundary hp, potion/torch/Pickpocket no-draw, empty input, refusal ordering, PILFER_FUMBLE_KINDS, stream isolation, armor/ward no-soak, voice)"
        status: pass
      - kind: unit
        ref: "test/unit/identity-contract.test.js (Pilfer bad row re-pinned to the fumble)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Potions, tools, scrolls and equipping never fumble for a Pilfer; every refusal in the useItem ladder fires before the fumble draw; both draws come from a derived stream, leaving the main rng cursor identical to a non-Pilfer's same use"
    requirement: RULES-09
    verification:
      - kind: unit
        ref: "test/unit/pilfer-fumble.test.js (ordering test; stream-isolation test); test/unit/identity-world.test.js (Pilfer Strength potion now succeeds); test/unit/usable-features-audit.test.js (Pilfer potion case is now `ok:true`)"
        status: pass
      - kind: integration
        ref: "node --test test/unit/roll-high-guard.test.js test/parity/roll-high-invariant.test.js (DRAW_INVENTORY/OUTCOME updated and green)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every surface narrates the new rule: SUB_NOTE.Pilfer states both sides, the Oracle and rail name the item and hp lost, the identity contract and CLASS-PASS/USABLE-FEATURES-AUDIT docs are updated, and no player-facing Pilfer text names a diagnosis"
    requirement: RULES-09
    verification:
      - kind: unit
        ref: "test/unit/pilfer-fumble.test.js (voice test, diagnosis-term scan built by concatenation); test/voice/safety-scan.test.js; test/unit/hp-not-wp.test.js; test/unit/narrationLinesCoverage.test.js; test/unit/formatEventsCoverage.test.js; test/unit/class-pass-ledger.test.js"
        status: pass
      - kind: other
        ref: "grep -c \"does not heal\" src/browser/eventNarration.js src/browser/narrationLines.js content/flavor.js -> 0 for every file; grep -c \"Phase 75.1\" docs/CLASS-PASS.md -> >=1"
        status: pass
    human_judgment: false
  - id: D4
    description: "The bot plays a Pilfer under the new rule (buffs like anyone), zero parity fixtures or the prototype master move, and the balance effect is measured with a before/after 200-seed readout"
    requirement: RULES-09
    verification:
      - kind: unit
        ref: "test/unit/bot-tactics.test.js (the Pilfer buff-pick case now expects the buff)"
        status: pass
      - kind: integration
        ref: "node --test \"test/parity/**/*.test.js\" (56/56); git diff --quiet against the plan base for test/parity/fixtures and prototype-master.js.txt (both exit 0); git hash-object test/parity/prototype-master.js.txt unchanged"
        status: pass
      - kind: other
        ref: "tools/readouts/75.1-01-before.txt vs -after.txt: mean death depth 7.54 -> 7.65, per-floor-survival verdict unchanged (\"all floors 1-12 inside the pass band\"), reach-20 unchanged at 1.0%"
        status: pass
      - kind: unit
        ref: "test/unit/roll-high-state-pins.test.js (solo-thief-pilfer re-pinned with a bisected rationale comment; every other pin unchanged)"
        status: pass
    human_judgment: true
    rationale: "The tune-difficulty readout is an informational proxy, not a pass/fail gate (per its own header) — a human DR round is the real signal for whether the fumble's feel is right in play (per the plan's own <human-check>)."

duration: 95min
completed: 2026-09-25
status: complete
---

# Phase 75.1 Plan 01: Pilfer magic-item fumble (RULES-09) Summary

**The Pilfer's heal-only useItem lockout is replaced by a d20 fumble risk on jewelry/cloaks/staves — a roll of 1 fails the use, blasts the Pilfer for an unsoaked d10 and dusts the item, narrated everywhere and played correctly by the bot**

## Performance

- **Duration:** ~95 min
- **Started:** 2026-09-25T20:05:00-04:00 (approx.)
- **Completed:** 2026-09-25T21:40:00-04:00 (approx.)
- **Tasks:** 3
- **Files modified:** 21 (1 created, 20 modified)

## Accomplishments

- **Task 1 — the fumble in useItem, its death cause, and the engine tests:** `engine/items.js` gains `PILFER_FUMBLE_KINDS` (`["jewel", "cloak", "staff"]`, frozen), `pilferFumbles(c, it)` and `pilferFumbleRng(state, rng, it)` (a single derived stream per use: `derivedRng(<main cursor or 0>, "pilferFumble", state.acts, it.n)`), and deletes the IDENT-07 heal-only Pilfer refusal branch entirely. The fumble step sits as the LAST rung of `useItem`'s refusal ladder — after `itemReady`, before `itemUsed` — drawing a d20 via `rollCheck(fumbleRng, 20, atLeastFor(19, 20))`; on a fumble (roll 1, not `chk.ok`) it draws the blast (`fumbleRng.d(10)`), subtracts it directly from `c.wp` (no armor/ward soak), removes the item from its worn slot or bag index, pushes `pilferFumbled` (item name, slot-or-index, `rollFields(chk)`, `dmg`), and calls `die(state, "pilferFumble", it.n, rng, events, now)` when `c.wp <= 0`. `content/epitaphs.js` gains `EPITAPHS.pilferFumble` (4 deadpan lines), `CAUSE_TEXT.pilferFumble`, `CAUSE_TEXT_TOKENS.pilferFumble`; `src/browser/scoreTag.js` appends `"pilferFumble"` to `TAG_CAUSES` (append-only). `test/unit/roll-high-guard.test.js`'s DRAW_INVENTORY row for `engine/items.js` moves to `rollCheck: 1, amount: 8`; `test/parity/roll-high-invariant.test.js` gains `pilferFumbled: () => false`. `test/unit/pilfer-fumble.test.js` (new, 13 tests) covers the worn/bagged fumble, roll>=2 success, adjacency (roll 1 vs 2, both keys found by search), boundary hp (dies exactly at the blast, survives at blast+1), potions/tools/Pickpocket drawing nothing new, empty input, every refusal firing before the draw (with the main rng cursor unchanged), `PILFER_FUMBLE_KINDS`'s exact contents, stream isolation (a non-fumbling Pilfer use costs the SAME main-rng draws a Pickpocket's would), and armor/ward not soaking the blast.
- **Task 2 — the narration, the Pilfer blurb, the identity rows and the audit:** `src/browser/eventNarration.js`/`narrationLines.js` gain `pilferFumbled` builders (Oracle: names the item, shows the roll via `rollVsText`, states the hp lost with U+2212, says "dust"; rail: item + hp lost, tone "hurt") and `"pilferFumbled"` joins `FEATURE_EVENTS`; the retired `useRefused` "pilfer" branch/reason-map entry is deleted from both files (the fallback is now a generic "does not work for you"). `content/flavor.js#SUB_NOTE.Pilfer` restates both sides in voice. `test/unit/identity-contract.test.js`'s Pilfer bad row is re-pinned to the fumble via a local `findPilferFumbleActs`/`predictPilferFumble` search-helper pair (mirroring Task 1's own search, per-file convention — no cross-import). `identity-world.test.js` and `usable-features-audit.test.js`'s Pilfer potion cases are now success cases, not refusals; `narrationLinesTable.test.js`'s `pilferUse` check becomes a `pilferFumbled` check. `docs/CLASS-PASS.md` and `docs/USABLE-FEATURES-AUDIT.md` gain "Superseded by Phase 75.1 (RULES-09)" notes beside the retained IDENT-07 history, and their Good/bad table row / potion table's Pilfer column are rewritten.
- **Task 3 — the bot plays the new rule; fixtures, state pins and the AFTER readout:** `tools/lib/tuning-bot.mjs`'s round-1 buff tier drops its `c.sub !== "Pilfer"` skip — a Pilfer now takes the buff pick like anyone (potions never fumble; a worn buff carries the same risk any other Pilfer use does). `test/unit/bot-tactics.test.js`'s Pilfer case now expects the buff. Fixtures: `node --test "test/parity/**/*.test.js"` 56/56, and `git diff --quiet` against the plan base for `test/parity/fixtures`/`prototype-master.js.txt` both exit clean — **zero fixtures moved**, as predicted (no replay site has a Pilfer using a jewel/cloak/non-heal potion). State pins: `solo-thief-pilfer` (seed 303, forced Human Pilfer) moved; every other of the eight pins stayed byte-identical. Bisected: a scratch copy of `tuning-bot.mjs` with ONLY the round-1-buff skip restored reproduced the exact same NEW hash — ruling out this plan's own bot edit as the cause and isolating the divergence to `engine/items.js`'s useItem change (a Pilfer's non-heal item use, previously a silent zero-draw `useRefused` no-op, now genuinely resolves — first hit at a bagged Amulet of Light use, action 333 of 400). Re-pinned via `node tools/roll-high-baseline.mjs pins`, with a Phase 75.1 (RULES-09) rationale comment in the established format. `npm test`: **6231/6231, 0 failures.**

## Task Commits

Each task was committed atomically:

1. **Task 1: The fumble in useItem, its death cause, and the engine tests** — `705c98c6` (feat)
2. **Task 2: The narration, the Pilfer blurb, the identity rows and the audit** — `cd3f4106` (docs)
3. **Task 3a: The bot plays the new rule; re-pin solo-thief-pilfer** — `32fb1b3b` (feat)
3. **Task 3b: before/after 200-seed readouts** — `92bf260b` (docs)

**Plan metadata:** this SUMMARY.md's own commit (pending, by the orchestrator's convention)

## Files Created/Modified

- `engine/items.js` — `PILFER_FUMBLE_KINDS`/`pilferFumbles`/`pilferFumbleRng`; the fumble step in `useItem`; the IDENT-07 heal-only refusal deleted
- `content/epitaphs.js` — `EPITAPHS.pilferFumble`, `CAUSE_TEXT.pilferFumble`, `CAUSE_TEXT_TOKENS.pilferFumble`
- `content/flavor.js` — `SUB_NOTE.Pilfer` restated for both sides
- `src/browser/scoreTag.js` — `TAG_CAUSES` gains `"pilferFumble"` (append-only)
- `src/browser/eventNarration.js`, `src/browser/narrationLines.js` — `pilferFumbled` Oracle/rail lines, `FEATURE_EVENTS`; the retired `useRefused` "pilfer" branch removed
- `tools/lib/tuning-bot.mjs` — the round-1 buff tier's Pilfer skip removed
- `docs/CLASS-PASS.md`, `docs/USABLE-FEATURES-AUDIT.md` — Pilfer rows updated with a "Superseded by Phase 75.1" note
- `test/unit/pilfer-fumble.test.js` — new, 13 tests, full behavior coverage
- `test/unit/identity-contract.test.js`, `test/unit/identity-world.test.js`, `test/unit/usable-features-audit.test.js`, `test/unit/narrationLinesTable.test.js`, `test/unit/bot-tactics.test.js` — re-pinned for the deliberate rule change
- `test/unit/roll-high-guard.test.js`, `test/parity/roll-high-invariant.test.js` — the new draw counted; `pilferFumbled` added to OUTCOME
- `test/unit/roll-high-state-pins.test.js` — `solo-thief-pilfer` re-pinned with a bisected rationale
- `test/unit/scoreTag.test.js` — the pinned `TAG_CAUSES` list and an out-of-range-index fixture updated
- `tools/readouts/75.1-01-before.txt`, `tools/readouts/75.1-01-after.txt` — the 200-seed bot readouts

## Readout Headline Lines (before -> after)

```
Death-depth distribution:  min=2 p50=7 p90=12 max=28  ->  min=2 p50=7 p90=13 max=28
Four-band: mean death depth=7.54 -> 7.65; floors gained mean=6.54 -> 6.65; encounters survived mean=16.32 -> 16.53
reach-20: 1.0% -> 1.0% (unchanged)
Per-floor-survival verdict: "all floors 1-12 inside the pass band" -> unchanged
Thief class row: reach5 90.3%->90.5%  reach10 27.4%->28.6%  reach20 3.2%->3.2%
                 dmgTaken/fight 7.40->7.56  potions/run 2.07->2.06  backstabs/run 10.19->10.67
Outcome: 174 dead, 26 stuck  ->  175 dead, 25 stuck
AFTER death-cause breakdown: one new row — "fiddled with a Cloak of Regeneration until it came apart" 1 (0.5%)
```

A small, expected effect: a Pilfer's item uses now really resolve (or fumble) instead of silently refusing — well inside the readout's own "informational proxy, not a gate" framing, and the per-floor pass-band verdict is unchanged. The AFTER breakdown shows the new `pilferFumble` death cause firing at least once across the 200-seed sweep, confirming the mechanic is live in bot play.

## Decisions Made

- **`pilferFumbled` carries `item` as a plain string (`it.n`), never a nested object** — matching `die()`'s own `detail` argument and the `{foe}` token every EPITAPHS/CAUSE_TEXT template already expects, so the Oracle/rail builders and the death-copy fill share one flat field.
- **The identity-contract re-pin duplicates its own small search helper** (`findPilferFumbleActs`/`predictPilferFumble`) rather than importing pilfer-fumble.test.js's — every file in this suite family keeps its own local helpers by established convention (identity-world.test.js's own header states this explicitly), so this file reads standalone.
- **The `solo-thief-pilfer` pin-hash move is bisected to the ENGINE change, not Task 3's bot-tactics edit** — a scratch copy of `tuning-bot.mjs` with only the round-1-buff Pilfer skip restored reproduced the identical NEW hash (not the old pinned one), proving `chooseCombatItem`'s "buff" branch never actually fires for this seed/force (no potion or worn buff exists in this run's starting kit) and isolating the real cause to `useItem`'s previously-silent, zero-draw `pilfer` refusal now genuinely resolving.

## Deviations from Plan

None — plan executed exactly as written. The one predicted, bounded uncertainty (whether `solo-thief-pilfer` would move, and if so why) resolved exactly along the lines the plan's own `<interfaces>` block anticipated ("a newly allowed use, or a fumble").

## Issues Encountered

None beyond the state-pin investigation described above (expected, not a bug — resolved per the plan's own instructions).

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- RULES-09 is fully landed: the fumble fires correctly, is narrated everywhere, the bot plays it, and the balance effect is measured. `npm test` 6231/6231; parity 56/56; zero fixtures moved; the prototype master is byte-identical to the plan base.
- Handoff for 75.1-06 (scroll reading): `docs/USABLE-FEATURES-AUDIT.md`'s `scrollRefused pilfer` row is UNCHANGED by this plan (RULES-10 territory) — a Pilfer still cannot read a scroll under the old rule until 75.1-06 lands.
- Handoff for 75.1-08 (the whole-phase gates, FIXTURE-INVENTORY, DIFFICULTY-RETUNE): this plan's fixture/pin measurement (zero fixtures moved, one pin re-measured with rationale) and both readouts are already committed; 75.1-08 consolidates the phase-wide record — `docs/DIFFICULTY-RETUNE.md` was deliberately left untouched here, per the plan's own interfaces note.
- A staff is one of the three `PILFER_FUMBLE_KINDS`, but a Pilfer (a Thief) can never wield one — `wrongClass` always fires first. This remains a flagged assumption (documented in `docs/USABLE-FEATURES-AUDIT.md`'s Staves section and in `pilfer-fumble.test.js`'s own ordering test), never reachable in play, consistent with the plan's own note.

---
*Phase: 75.1-pilfer-fumbles-scroll-reading*
*Completed: 2026-09-25*
