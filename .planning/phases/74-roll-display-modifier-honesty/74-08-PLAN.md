---
phase: 74-roll-display-modifier-honesty
plan: 08
type: execute
wave: 4
depends_on:
  - 74-03
  - 74-04
  - 74-05
  - 74-06
  - 74-07
files_modified:
  - test/unit/roll-sign-consistency.test.js
  - test/unit/hp-not-wp.test.js
  - docs/ROLL-LEDGER.md
  - tools/voice-sample-output.txt
autonomous: true
requirements:
  - ROLL-02
  - ROLL-03
must_haves:
  truths:
    - "A guard test fails the build if the same modifier renders with opposite signs on two surfaces (ROADMAP success criterion 3; 74-CONTEXT consistency guard): insulted, Sidestep, Guard, Battle Roar, Elven and Weaken on the Oracle foe line, the fight-log reveal and foe details; afraid on the strike line and the Afraid chip; a heavy and a light weapon on lootCompare and the purchase-bagged Oracle and rail lines; the Thief flee bonus on the combat menu and the flee lines; the heights phobia on the Oracle and rail"
    - "The same modifier renders with the identical player-view signed value on every surface that shows it (edge ROLL-03 adjacency)"
    - "The guard pins the range format: every harvested range is 'lo–hi' with U+2013, or a single face, or 'nothing'; no output uses '%' or an 'N+' shorthand, and no signed modifier uses the ASCII hyphen-minus (edge ROLL-03 encoding)"
    - "The guard pins ONE formatter: no src/browser module other than rollRange.js defines a modifier-sign helper or a local delta-sign ternary"
    - "For a level-1 Human Fighter, Thief and Magic User on a Club, the hero sheet, the combat menu's STRIKE row, foe details against a plain foe and a real playerStrike event's range all read 16–20, 17–20 and 18–20 (ROADMAP success criterion 1)"
    - "The new copy banks (CONDITION_EFFECT_COPY, MOD_LABEL, ROLL_COPY) are walked by the standing HP-not-WP guard and pass the voice safety word list"
    - "docs/ROLL-LEDGER.md records the Phase 74 display closure: the sign rule, the one formatter, the non-event surfaces, the O3/needModsClause handoff and the device-trigger case closed, and what stays for Phase 77 and Phase 79"
    - "Across the whole phase, no parity fixture, prototype master, comparable, content file or state pin moved, and engine/ changed only by 74-01's pure extraction"
  artifacts:
    - path: test/unit/roll-sign-consistency.test.js
      provides: "the cross-surface sign guard, the range-format pin and the one-formatter source scan"
    - path: docs/ROLL-LEDGER.md
      provides: "## Phase 74 display closure (ROLL-02/03)"
      contains: "## Phase 74 display closure"
  key_links:
    - from: test/unit/roll-sign-consistency.test.js
      to: every Phase 74 surface (eventNarration, narrationLines, foeDetails, heroTab, combatMenu, viewModels#lootCompare, conditionEffects)
      via: "renders each surface for the same modifier and compares the signed tokens"
      pattern: "foeDetailsCard|lootCompare|conditionEffectText|combatMenuViewModel"
  prohibitions:
    - statement: "MUST NOT let any surface show a roll, range or modifier that differs from what the engine resolves against, or the same modifier with opposite signs on two surfaces; the guard must fail if either happens"
      requirement_id: ROLL-03
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT weaken the guard to pass (no skipped scenario, no loosened pattern) and MUST NOT move a parity fixture, a state pin or a content number to satisfy it"
      requirement_id: ROLL-02
      category: integrity
      status: resolved
      verification: test
    - statement: "MUST NOT present odds as percentages or 'N+' shorthand anywhere the guard renders"
      requirement_id: ROLL-02
      category: values
      status: resolved
      verification: test
---

<objective>
Close the phase with the consistency guard, the copy-bank registration and the ledger record.

Purpose:
- ROADMAP success criterion 3 ("The same modifier never shows opposite signs on two different surfaces").
- 74-CONTEXT: "ONE signed-modifier formatter feeds every surface. A guard test fails if the same modifier renders with opposite signs on two surfaces, and it pins the range format."
- The standing guards must also see the new copy.
- docs/ROLL-LEDGER.md carries the Phase 74 handoffs, so it records their closure.

Output: test/unit/roll-sign-consistency.test.js, the hp-not-wp registration, the ledger section, and the phase's final gates with the rolled-up human-verification list.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 74-08 creates or changes.)

- engine/derived.js `targetStrikeFaces`, `heroStrikeFacesVs`, `foeSwingVsHero`; engine/combat.js calls them; test/unit/odds-helpers.test.js (74-01)
- src/browser/rollRange.js, the ONE formatter (74-02)
- The event-driven Oracle, fight-log, dice-reveal and rail lines re-signed from the player's side; heights/water fear lines (74-03)
- src/browser/rollOdds.js; the hero sheet and combat menu ranges; hero snapshots re-pinned (74-04)
- Item/loot/store/find comparisons: "−2 to hit, worse than your Club", crit ranges; store snapshots re-pinned (74-05)
- The foe-details odds line and the foe condition-chip effects (74-06)
- src/browser/conditionEffects.js and the hero condition-chip effects on the chip-tap card (74-07)
- **This plan:** test/unit/roll-sign-consistency.test.js (the cross-surface sign guard, the range-format pin, the one-formatter scan); CONDITION_EFFECT_COPY, MOD_LABEL and ROLL_COPY walked by test/unit/hp-not-wp.test.js; docs/ROLL-LEDGER.md `## Phase 74 display closure (ROLL-02/03)`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-03-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-04-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-05-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-06-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-07-SUMMARY.md

<interfaces>
Surfaces and how to render each in a test (all pure, no DOM):
- Oracle line: EVENT_NARRATION[type](e) (src/browser/eventNarration.js). Plain text via narrativeLineText or by stripping tags. Fight-log reveal and rail roll line: oracleDetailText(narrateEvent(e)) (src/browser/narrationLines.js).
- Rail line: LINE_FOR[type](e).text (src/browser/narrationLines.js).
- Foe details: foeDetailsCard(i, state).lines (src/browser/foeDetails.js). foeConditionEffect(chip, foe, state) gives the chip effect.
- Hero sheet: characterSheetViewModel(state).stats with key "toHit" (src/browser/heroTab.js).
- Combat menu: combatMenuViewModel(state), the actions[0].sub STRIKE and the social submenu's flee row (src/browser/combatMenu.js).
- Comparisons: lootCompare(c, it).line (src/browser/viewModels.js). purchaseBagged uses EVENT_NARRATION.purchaseBagged / LINE_FOR.purchaseBagged with `why: gearCompareParts(c, it)` (engine/derived.js), which is exactly what engine/economy.js puts on the event.
- Hero chips: conditionEffectText(cn, state) (src/browser/conditionEffects.js) with a conditionsOf(state) descriptor.
- Formatter: src/browser/rollRange.js (modsText, MOD_LABEL, ROLL_COPY).

Real engine events: drive foeTurn, playerStrike and flee (engine/combat.js) on a hand-built state with a scripted rng, the way test/unit/odds-helpers.test.js (74-01) and test/unit/feedback-payload.test.js do (copy the small local helpers; do not import another test file). Use one foe with no abilities, no party, and a non-frenzy race.

test/unit/hp-not-wp.test.js L147-L179 walks a list of COPY objects for a standalone wp/WP token. Add the new banks to that list.
test/unit/upgrade-why.test.js shows the BANNED/ALLOWLIST voice-scan pattern (content/safety-wordlist.js).

docs/ROLL-LEDGER.md:
- `test/unit/roll-ledger-sync.test.js` reads only the `## Modifier ledger` tables. Append the new H2 at the END of the file and edit no earlier section.
- Keep bracketed site:source style ids out of the new section.

Phase base for the whole-phase checks: commit 0f6dc5c (Phase 73 complete).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The cross-surface sign guard, the range-format pin and the copy-bank registration</name>
  <files>test/unit/roll-sign-consistency.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - src/browser/rollRange.js, src/browser/rollOdds.js, src/browser/conditionEffects.js
    - src/browser/eventNarration.js (the roll lines and purchaseBagged), src/browser/narrationLines.js (oracleDetailText, LINE_FOR roll and fear lines)
    - src/browser/foeDetails.js, src/browser/heroTab.js (characterSheetViewModel), src/browser/combatMenu.js, src/browser/viewModels.js (lootCompare)
    - test/unit/odds-helpers.test.js and test/unit/feedback-payload.test.js (state and scripted-rng helpers)
    - test/unit/hp-not-wp.test.js (L1-L60 imports, L140-L180 the walked list)
  </read_first>
  <behavior>
    - Scenario "foe side": a level-1 Human Guard with Sidestep live, a failed parley (combat.parleyInsulted) and one level-1 foe. Take a real foeMissed or struckByFoe from foeTurn. Its Oracle line, its fight-log reveal and the foe details odds line all show "Guard +1", "Sidestep +2" and "insulted −1", with no opposite sign anywhere.
    - Scenario "Weaken": combat.foeToHitPenalty 3 and combat.weakened. The real foe line and the foe details line both show "Weaken +2" for a plain Soldier. The Weakened chip effect's range equals the foe details' foe-side range without its modifiers.
    - Scenario "Battle Roar" and scenario "Elven": the foe line and foe details agree on "Battle Roar +2" and on "Elven −1".
    - Scenario "afraid": a real playerStrike with combat.afraid 2 shows "afraid −3" on its Oracle line and reveal, and the Afraid chip effect starts "−3 to hit".
    - Scenario "weapons": a Human Fighter on a Club. The need −2 weapon reads "−2 to hit" on lootCompare, on EVENT_NARRATION.purchaseBagged and on LINE_FOR.purchaseBagged. A Dagger reads "+1 to hit" on all three.
    - Scenario "flee": a real Human Thief flee. The fleeRolled Oracle line, LINE_FOR.fleeRolled and the combat menu flee row all show "Thief +5".
    - Scenario "heights": heightsFear with penalty 2 shows "−2" on both the Oracle and the rail line.
    - Range pin: for level-1 Human Fighter, Thief and Magic User on a Club, four readings all equal "16–20", "17–20" and "18–20" respectively: the sheet's toHit value (without the die), the STRIKE sub's range, foe details' "You hit it on" range for a plain foe, and rangeText(e.atLeast, e.dieN) of a real playerStrike event against that foe.
    - Every range harvested from every scenario output matches ^(\d+(–\d+)?|nothing)$ with U+2013. No output contains "%", a digit directly followed by "+", or an ASCII hyphen-minus before a digit in a modifier.
    - One-formatter scan: with comments stripped, no src/browser/*.js file except rollRange.js defines a function named modsText, modsClause, needModsText, needModsClause, signedNeed or critRange, or a flee-modifier helper. No such file (or mazeworld.html) contains a local sign ternary on a modifier delta, meaning a `.delta` comparison against 0 followed by a sign character.
  </behavior>
  <action>
1. Create test/unit/roll-sign-consistency.test.js with one test per behaviour above. Build a small `signedTokens(text, names)` helper. It strips tags, then for each expected modifier name collects every "name ±N" occurrence (either sign character) and returns the list. Each scenario asserts that every surface yields at least one token per expected name, and that every token equals the expected player-view value. That is how "opposite signs" fails loudly.
2. Drive the engine for the foe-side, afraid, flee and range-pin scenarios so the guard binds to engine truth, not hand-typed events. Hand-built events are fine only for heightsFear and purchaseBagged, whose payloads are built from the engine's own gearCompareParts and the engine's event shape.
3. The one-formatter scan reads the files with node:fs and strips // and /* */ comments first. Its file list is every src/browser/*.js except rollRange.js, plus mazeworld.html. A failure message names the file and the matched text.
4. test/unit/hp-not-wp.test.js: import CONDITION_EFFECT_COPY (conditionEffects.js), MOD_LABEL and ROLL_COPY (rollRange.js) and add them to the walked list. In the new guard test, add a BANNED/ALLOWLIST voice scan over the same three banks plus the new FOE_DETAILS_COPY, COMBAT_MENU_COPY and UPGRADE_WHY_COPY leaves, copying upgrade-why.test.js's pattern.
5. If any scenario fails, the surface is wrong: report it and fix that surface's formatter call in the owning module. Never loosen the guard. List any such fix in the SUMMARY as a deviation.
  </action>
  <verify>
    <automated>node --test test/unit/roll-sign-consistency.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/roll-sign-consistency.test.js` passes with at least 10 tests, covering every scenario named above (grep the test titles for "insulted", "Weaken", "Battle Roar", "Elven", "afraid", "weapon", "flee", "heights", "range", "one formatter").
    - Sanity check that the guard has teeth: temporarily change the roller on foeMissed's modifier clause in eventNarration.js to the hero roller, run the guard and confirm it FAILS, then revert. Record the failing test name in the SUMMARY.
    - `grep -c "CONDITION_EFFECT_COPY" test/unit/hp-not-wp.test.js` is at least 2 (import and list) and `node --test test/unit/hp-not-wp.test.js` passes.
  </acceptance_criteria>
  <done>A build-failing guard proves every modifier reads the same way everywhere and every range is written one way.</done>
</task>

<task type="auto">
  <name>Task 2: The ledger's Phase 74 closure and the phase's final gates</name>
  <files>docs/ROLL-LEDGER.md, tools/voice-sample-output.txt</files>
  <read_first>
    - docs/ROLL-LEDGER.md (`## The device trigger` L29-L31; `## New findings` row O3; `## Handoffs → Phase 74` L445-L462; `## Handoffs → Phase 79` L464-L479; the end of the file)
    - test/unit/roll-ledger-sync.test.js (what it reads)
    - the six 74-0N-SUMMARY.md files (every human-verification note)
  </read_first>
  <action>
1. Append `## Phase 74 display closure (ROLL-02/03)` at the end of docs/ROLL-LEDGER.md. Record:
   - (a) The sign rule: + is better for the player. The engine's mods stay signed for the roller, and the one formatter (src/browser/rollRange.js) negates foe-roll lines only. Give the roller-per-line table: you, ally, foe.
   - (b) The non-event surfaces and where their numbers come from: rollOdds.js reading the engine's derived functions, including 74-01's pure extraction (targetStrikeFaces, heroStrikeFacesVs, foeSwingVsHero), measured with zero moved fixtures. Then the hero sheet, combat menu, foe details, chips and comparisons with one example each.
   - (c) The O3 / needModsClause handoff: CLOSED by 74-03.
   - (d) The device trigger: CLOSED by 74-05 ("−2 to hit, worse than your Club").
   - (e) The consistency guard (test/unit/roll-sign-consistency.test.js).
   - (f) The hooks left for Phase 77 (the formatter, conditionEffects.js WHAT_IF, foeConditionEffect) and for Phase 78's climb card (the range format).
   - (g) What stays for Phase 79 beyond the existing `## Handoffs → Phase 79` list: the Smoke and Battle Roar narration prose, the "They need two better" chip and narration lines, the authored CONDITION_EXPLAIN and FOE_CONDITION_DESC sentences, and the bestiary notes.
   Edit no earlier section.
2. Re-run `node tools/voice-sample.mjs` on the merged tree. 74-03 and 74-05 ran in parallel, so the committed sample may predate one of them. Commit tools/voice-sample-output.txt only if it changed, and name the changed line count in the SUMMARY.
3. Whole-phase gates (record each result in the SUMMARY):
   - `node --test "test/parity/**/*.test.js"` exits 0;
   - `git diff --quiet 0f6dc5c -- test/parity/fixtures test/parity/prototype-master.js.txt test/parity/harness/comparables.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/fixtures/roll-high test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js content` exits 0;
   - `git diff --name-only 0f6dc5c -- engine` lists exactly engine/combat.js and engine/derived.js;
   - `git diff --name-only 0f6dc5c -- test/unit/fixtures/shell-snapshots` lists exactly mu.hero.txt, thief.hero.txt and thief-store.store.txt;
   - `npm test` reports 0 failures.
4. In the SUMMARY, roll up every "Human verification (deferred to end of run)" note from 74-03 through 74-07 into one list for the milestone-close Pixel 7 batch (deferred UAT protocol; no device pause here).
  </action>
  <verify>
    <automated>node --test test/unit/roll-ledger-sync.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 74 display closure (ROLL-02/03)" docs/ROLL-LEDGER.md` equals 1, and it is the file's last H2 (`grep "^## " docs/ROLL-LEDGER.md | tail -1` prints it).
    - `git diff 0f6dc5c -- docs/ROLL-LEDGER.md | grep -c "^-[^-]"` equals 0 (append-only).
    - Every whole-phase gate in the action holds; `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The ledger records the closure and the phase's invariants are measured.</done>
</task>

</tasks>

<verification>
- The consistency guard passes and is proven to fail on a flipped roller.
- The parity suite, pins, direction tests and content are untouched since 0f6dc5c; only the declared engine files and shell fixtures changed; npm test is green.
</verification>

<success_criteria>
ROADMAP Phase 74 success criteria 1-3 hold and are pinned by tests: the engine's own ranges on every surface (a caster 18–20, a thief 17–20, a fighter 16–20), every modifier signed from the player's side, and no modifier with opposite signs on two surfaces.
</success_criteria>

<output>
Create `.planning/phases/74-roll-display-modifier-honesty/74-08-SUMMARY.md` when done. Include the guard's scenario list, the teeth check's failing test name, every whole-phase gate result, and the rolled-up human-verification list for the milestone-close Pixel 7 batch.
</output>
