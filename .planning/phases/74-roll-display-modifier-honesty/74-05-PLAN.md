---
phase: 74-roll-display-modifier-honesty
plan: 05
type: execute
wave: 2
depends_on:
  - 74-02
files_modified:
  - src/browser/upgradeWhy.js
  - src/browser/viewModels.js
  - test/unit/upgrade-why.test.js
  - test/unit/lootCompare.test.js
  - test/unit/gear-sheet-dom.test.js
  - test/unit/gear-sheet-model.test.js
  - test/unit/store-rows.test.js
  - test/unit/store-delivery.test.js
  - test/unit/fixtures/shell-snapshots/thief-store.store.txt
autonomous: true
requirements:
  - ROLL-02
  - ROLL-03
must_haves:
  truths:
    - "Every item, loot, store and find comparison says which way the to-hit change goes: '−2 to hit, worse than your Club', '+1 to hit, better than your Long Sword' (74-CONTEXT: comparisons say which way is better); '+' is always easier to hit and '−' always harder"
    - "The ROLL-LEDGER device-trigger case is closed: a dropped heavy weapon (need −2) compared with a Club reads '−2 to hit, worse than your Club' on the find card, loot screen, gear sheet and store row, which all share lootCompare"
    - "A crit comparison names the winning crit range on the hero's current strike die: 'crits on 19–20 vs your 20' on a d20. Where no die is known (the purchase-bagged Oracle and rail line, whose event carries only the compare parts) the crit term is left out instead of printing a roll-under range; the swing number already includes crit chance"
    - "AR stays a stat ('AR 8 vs your AR 6'); no soak range is added to a comparison (74-CONTEXT: a soak range appears only where a soak roll is printed)"
    - "upgradeWhy.js formats signs and ranges only through rollRange.js (its one import) and stays otherwise pure"
    - "thief-store.store.txt is the only shell fixture that changes, deliberately re-pinned and named in the SUMMARY"
  artifacts:
    - path: src/browser/upgradeWhy.js
      provides: "upgradeWhyText(parts, { dieN, haveName }) with the better/worse verdict and roll-high crit ranges"
      contains: "./rollRange.js"
    - path: src/browser/viewModels.js
      provides: "lootCompare passes the strike die and the wielded weapon's name"
      contains: "strikeDie(c)"
  key_links:
    - from: src/browser/viewModels.js#lootCompare
      to: src/browser/upgradeWhy.js#upgradeWhyText
      via: "upgradeWhyText(gearCompareParts(c, it), { dieN: strikeDie(c), haveName })"
      pattern: "upgradeWhyText\\(gearCompareParts\\(c, it\\), \\{"
    - from: src/browser/upgradeWhy.js
      to: src/browser/rollRange.js
      via: "toHitText and facesRangeText"
      pattern: "toHitText|facesRangeText"
  prohibitions:
    - statement: "MUST NOT show a comparison whose sign or verdict reads the wrong way for the player (a '+' that is worse, a 'better' that is worse), and MUST NOT print a crit range in the pre-mirror roll-under form"
      requirement_id: ROLL-03
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT change the upgrade verdict itself (weaponUpgradeDelta/armorUpgradeDelta decide it) or gate BUY/TAKE/EQUIP on the explanation; new words stay family-friendly deadpan and say HP, never WP"
      requirement_id: ROLL-03
      category: values
      status: resolved
      verification: judgment
---

<objective>
Make every gear comparison state which way a to-hit change goes, and put roll-high crit ranges on the hero's own die.

Purpose: 74-CONTEXT "Item, loot, store and find comparisons":
- "+1 to hit" and "−2 to hit" keep their bigger-is-better meaning.
- Comparisons say which way is better, e.g. "−2 to hit, worse than your Sword".
- AR stays a stat.
- This closes the ledger's device-trigger case.

upgradeWhy.js also prints the crit term in the pre-mirror form ("crits on 1–2 vs your 1"). That is now wrong: since Phase 73 crits sit on the top faces.

Output: upgradeWhyText with the verdict and die-aware crit ranges; lootCompare passes the die and the wielded weapon's name; the compare-text tests and the thief store snapshot are re-pinned.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 74-05 creates or changes.)

- engine/derived.js `targetStrikeFaces`, `heroStrikeFacesVs`, `foeSwingVsHero`; engine/combat.js calls them; test/unit/odds-helpers.test.js (74-01)
- src/browser/rollRange.js, the ONE formatter (74-02)
- The event-driven Oracle, fight-log, dice-reveal and rail lines re-signed from the player's side; heights/water fear lines (74-03)
- src/browser/rollOdds.js; the hero sheet and combat menu ranges; hero snapshots re-pinned (74-04)
- **This plan:** upgradeWhy.js `upgradeWhyText(parts, { dieN, haveName })`: "−2 to hit, worse than your Club", "crits on 19–20 vs your 20"; viewModels.js lootCompare passes the strike die and the weapon name; the compare-text tests and thief-store.store.txt re-pinned
- The foe-details odds line and the foe condition-chip effects (74-06)
- src/browser/conditionEffects.js and the hero condition-chip effects on the chip-tap card (74-07)
- The consistency guard test, the new copy banks walked by the standing scans, and the ROLL-LEDGER display closure (74-08)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-02-SUMMARY.md
@src/browser/upgradeWhy.js
@src/browser/rollRange.js

<interfaces>
engine/derived.js#gearCompareParts(c, it) is unchanged. For a weapon it returns:
- `got: { lab, need, crit, strike }`, where need is WEAPONS[base].need (a to-hit face modifier: +1 light, −1/−2 heavy) and crit is the count of crit faces (1 or 2; 0 for a noCrit hero);
- `have` in the same shape, or `{ lab: null, need: 0, crit: null, strike }` bare-handed;
- `lostProf`.
For armor it returns `{ got: { ar }, have: { ar } }`. `strikeDie(c)` is the hero's current strike die.

src/browser/rollRange.js (74-02): `toHitText(delta)` ("−2 to hit"), `facesRangeText(faces, dieN)` (2 faces on a d20 is "19–20", 1 is "20").

src/browser/upgradeWhy.js today (the whole file, 110 lines):
- UPGRADE_WHY_COPY with `toHit: "{signed} to hit"` and `crit: "crits on {got} vs your {have}"`;
- a module-private crit-range helper that writes 1 as "1" and 2 as "1–2" (the pre-mirror reading);
- a module-private signed-delta helper;
- upgradeWhyText(parts) with the term order dice, to-hit, crit, swing, lost proficiency.
The module is import-free; test/unit/upgrade-why.test.js L203-L225 asserts zero import statements.

Callers of upgradeWhyText:
- src/browser/viewModels.js#lootCompare (weapon L361-L374 and armor branches) has `c`, and it serves the find card, loot screen, gear sheet and store rows;
- src/browser/eventNarration.js#purchaseBagged and src/browser/narrationLines.js#purchaseBagged format the engine event's `why` parts with no hero in reach. They keep calling upgradeWhyText(e.why) with no options, and this plan does not edit them.

Tests pinning the compare text:
- upgrade-why.test.js L94, L104-L128;
- lootCompare.test.js L63;
- gear-sheet-dom.test.js L246;
- gear-sheet-model.test.js L333;
- store-rows.test.js L82 and L287;
- store-delivery.test.js L130, L428 and L436.

Snapshots: test/unit/fixtures/shell-snapshots/thief-store.store.txt L25 and L27 carry to-hit and crit terms. mu-store.store.txt's two compare lines carry neither, so it must stay byte-identical.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: upgradeWhyText says which way, and writes crits roll-high</name>
  <files>src/browser/upgradeWhy.js, test/unit/upgrade-why.test.js</files>
  <read_first>
    - src/browser/upgradeWhy.js (the whole file)
    - src/browser/rollRange.js
    - test/unit/upgrade-why.test.js (the whole file)
    - content/weapons.js L40-L75 (need and crit values: Dagger/Rapier need +1 crit 2; the heavy weapons need −1/−2)
  </read_first>
  <behavior>
    - With no options, a Spiked Staff bought over a Quarter Staff reads "d8 vs your d6 · −1 to hit, worse than yours · 4.1 vs 5.0 a swing".
    - With { dieN: 20, haveName: "Quarter Staff" }, a Rapier compared with a Quarter Staff for a crit-capable hero contains "+1 to hit, better than your Quarter Staff" and "crits on 19–20 vs your 20".
    - With { dieN: 12 } the same crit term reads "crits on 11–12 vs your 12".
    - With no dieN, no "crits on" term appears at all.
    - A Guard (noCrit) never gets a "crits on" term; a bare-handed hero with { haveName: "bare hands" } reads "−1 to hit, worse than your bare hands" and no crit term.
    - The armor term is unchanged ("AR 8 vs your AR 6").
    - No output contains "1–2", a hyphen-minus sign on a number, or "%".
  </behavior>
  <action>
1. upgradeWhy.js: import toHitText and facesRangeText from ./rollRange.js. This is its only import; rollRange.js is itself pure and import-free, so eventNarration.js and narrationLines.js keep their purity.
2. UPGRADE_WHY_COPY:
   - Change `toHit` to "{toHit}, {verdict} than {whose}" and add the leaves `better: "better"`, `worse: "worse"`, `yours: "yours"` and `yourNamed: "your {name}"`.
   - Keep `crit`, `dice`, `bareHands`, `swing`, `lostProf`, `armor`, `upgrade`, `notUpgrade` and `sep`.
3. upgradeWhyText(parts, opts): opts is `{ dieN, haveName }`, both optional.
   - To-hit term (when the need delta is non-zero): toHitText(delta), then "better" for a positive delta or "worse" for a negative one, then "your {haveName}" when a name is given, else "yours".
   - Crit term: only when opts.dieN is a finite number, have.crit is not null and the crit counts differ; got and have are written with facesRangeText(crit, dieN).
   - Delete the module-private crit-range and signed-delta helpers.
   - Keep the term order and the armor branch.
4. Rewrite the header comment: purity now means "one import, the pure rollRange.js formatter". Cite 74-CONTEXT (comparisons say which way is better) and the roll-high crit reading (Phase 73: crits sit on the top faces).
5. upgrade-why.test.js: update the pins to the behaviours above (retitle the "crits on 1–2" test to the roll-high reading). Change the purity test from "zero imports" to "exactly one import, from ./rollRange.js", keeping the Math.random/Date.now and frozen-copy checks.
  </action>
  <verify>
    <automated>node --test test/unit/upgrade-why.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/browser/upgradeWhy.js | grep -cE "^\s*import\s"` equals 1, and that line imports from "./rollRange.js".
    - `grep -v '^\s*//' src/browser/upgradeWhy.js | grep -cE "function (critRange|signedNeed)\b"` equals 0.
    - `node --test test/unit/upgrade-why.test.js` passes with the "better than your Quarter Staff", "crits on 19–20 vs your 20" and "worse than yours" pins.
  </acceptance_criteria>
  <done>Every comparison states which way the to-hit change goes, and crits read on the top faces.</done>
</task>

<task type="auto">
  <name>Task 2: lootCompare passes the die and the weapon; every compare pin and the store snapshot</name>
  <files>src/browser/viewModels.js, test/unit/lootCompare.test.js, test/unit/gear-sheet-dom.test.js, test/unit/gear-sheet-model.test.js, test/unit/store-rows.test.js, test/unit/store-delivery.test.js, test/unit/fixtures/shell-snapshots/thief-store.store.txt</files>
  <read_first>
    - src/browser/viewModels.js (L1-L15 imports; L345-L392 lootCompare)
    - src/browser/upgradeWhy.js (after Task 1)
    - test/unit/lootCompare.test.js (L1-L80)
    - test/unit/gear-sheet-dom.test.js L235-L250, test/unit/gear-sheet-model.test.js L325-L340, test/unit/store-rows.test.js L75-L90 and L280-L290, test/unit/store-delivery.test.js L120-L135 and L420-L450
    - test/unit/rollDirection.test.js L246-L256 (the device-trigger weapon pair: a need −2 weapon vs a Club)
  </read_first>
  <action>
1. viewModels.js: import strikeDie from engine/derived.js (extend the existing derived.js import). In lootCompare's weapon branch, call upgradeWhyText(gearCompareParts(c, it), { dieN: strikeDie(c), haveName }). haveName is c.weapon when WEAPONS has it, else UPGRADE_WHY_COPY.bareHands. The armor branch is unchanged.
2. lootCompare.test.js: re-pin L63 to the real new line. Add the device-trigger test: a Human Fighter holding a Club compared with the need −2 weapon rollDirection.test.js uses reads "−2 to hit, worse than your Club", and a Dagger over a Club reads "+1 to hit, better than your Club".
3. Re-pin the other compare-text tests to the exact new strings:
   - gear-sheet-dom, gear-sheet-model and store-rows go through lootCompare, so they get the named weapon and the crit range when it applies;
   - store-delivery's purchaseBagged Oracle and rail lines go through upgradeWhyText with no options, so they get "worse than yours" and no crit term.
   Never loosen an exact match into a pattern.
4. Regenerate the snapshots with `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`, then rerun without the flag. `git status --short test/unit/fixtures/shell-snapshots` must show only thief-store.store.txt changed: its Flail and Spear rows gain "worse than your Dagger" and crit terms of "crits on 20 vs your 19–20". mu-store.store.txt stays byte-identical. Name the fixture and the rationale in the SUMMARY.
5. Run `npm test`. Any other test pinning the old compare text gets the same exact re-pin, listed in the SUMMARY as a Rule 1 deviation.
  </action>
  <verify>
    <automated>node --test test/unit/lootCompare.test.js test/unit/gear-sheet-dom.test.js test/unit/gear-sheet-model.test.js test/unit/store-rows.test.js test/unit/store-delivery.test.js test/unit/shell-tab-snapshots.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "upgradeWhyText(gearCompareParts(c, it), {" src/browser/viewModels.js` equals 1 (the weapon branch; the armor branch keeps its one-argument call).
    - `grep -c "worse than your Club" test/unit/lootCompare.test.js` is at least 1.
    - `grep -cE "crits on 1 vs|vs your 1–2" test/unit/fixtures/shell-snapshots/thief-store.store.txt` equals 0 (no pre-mirror crit range left in the store fixture).
    - The Flail and Spear rows of thief-store.store.txt each carry the named-weapon verdict: `grep -c "than your Dagger" test/unit/fixtures/shell-snapshots/thief-store.store.txt` is at least 2.
    - `git diff --name-only -- test/unit/fixtures/shell-snapshots` lists exactly thief-store.store.txt; `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every comparison surface reads which way a weapon swap goes, and crits read on the hero's own die.</done>
</task>

</tasks>

<verification>
- The device-trigger case reads "−2 to hit, worse than your Club"; crit terms are roll-high on the strike die or absent.
- Only thief-store.store.txt changed among the shell fixtures; npm test is green.
</verification>

<success_criteria>
ROLL-03 for item, loot, store and find comparisons: + always better, − always worse, and the comparison says so. ROLL-02 for the crit ranges.
</success_criteria>

<output>
Create `.planning/phases/74-roll-display-modifier-honesty/74-05-SUMMARY.md` when done. Include a before/after of the thief store's Flail row and the device-trigger line, every test file touched, and a "Human verification (deferred to end of run)" note: on the Pixel 7, open the store or find a heavy weapon and confirm the line reads "−N to hit, worse than your <weapon>"; a precise blade reads "crits on 19–20 vs your 20" at level 1.
</output>
