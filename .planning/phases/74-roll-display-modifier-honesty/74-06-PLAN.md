---
phase: 74-roll-display-modifier-honesty
plan: 06
type: execute
wave: 3
depends_on:
  - 74-01
  - 74-04
files_modified:
  - src/browser/foeDetails.js
  - test/unit/foeDetails.test.js
autonomous: true
requirements:
  - ROLL-02
  - ROLL-03
must_haves:
  truths:
    - "The long-press foe card shows both directions against the hero right now: 'You hit it on 17–20 (d20) · it hits you on 16–20 (d20)', with every live modifier applied, from rollOdds.js (74-CONTEXT: foe details show both directions against the hero RIGHT NOW)"
    - "The foe's side of the line lists its modifiers signed from the player's side through the one formatter: 'it hits you on 17–20 (d20; Sidestep +2, insulted −1)'"
    - "The old static 'hit only on a {n}' defence label is gone; a sp.toHit cap now shows as a real range in the odds line ('You hit it on 17–20 (d20)' for a Fighter against a toHit-4 foe)"
    - "Untouchable, magic-only and dagger-only foes keep their rule line in the defence line; with no way to touch them the odds line reads 'You cannot touch it'; a foe that never swings has no 'it hits you' part"
    - "Each foe condition that moves a to-hit roll states its effect from the player's side with the resulting range on the long-press card: 'Weakened · 2 — it hits you only on 18–20 (d20). …', 'Blind — it hits you only on 20 (d20). …', 'Asleep · 2 — you hit it on 16–20 (d20). …' (74-CONTEXT condition chips)"
    - "The card never throws and never mutates: a state without a full hero leaves the odds line out, and the existing D-09 order holds with the odds line placed right after the defence line"
  artifacts:
    - path: src/browser/foeDetails.js
      provides: "the odds line and foeConditionEffect (exported so Phase 77's effect indicators can reuse it)"
      contains: "export function foeConditionEffect"
    - path: test/unit/foeDetails.test.js
      provides: "the odds line, the modifier signs, the rule-line foes and the chip effects on a real hero"
  key_links:
    - from: src/browser/foeDetails.js
      to: src/browser/rollOdds.js#heroHitOddsVs / #foeHitOddsVs
      via: "the odds line and the chip effects read the same helpers the engine rolls against (74-01)"
      pattern: "heroHitOddsVs\\(|foeHitOddsVs\\("
  prohibitions:
    - statement: "MUST NOT compute a foe-details range from a restated formula or from the static bestiary number; the range is the engine's own right-now odds via rollOdds.js"
      requirement_id: ROLL-02
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT show a foe-side modifier with a sign that reads the wrong way for the player, or reveal a hidden rules value the card deliberately leaves out (fleesBelow, cadences, uses)"
      requirement_id: ROLL-03
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT reword the authored condition descriptions or the bestiary notes (Phase 79); only the computed effect clause is added, in the family-friendly deadpan voice, HP never WP"
      requirement_id: ROLL-03
      category: values
      status: resolved
      verification: judgment
---

<objective>
Put the live, two-way odds on the long-press foe card and give each to-hit-moving foe condition its effect and resulting range.

Purpose: 74-CONTEXT:
- "Foe details show both directions against the hero RIGHT NOW: 'You hit it on 17–20 · it hits you on 16–20', with the live modifiers applied. The old 'Hit only on a {n} or under' becomes a real range. Untouchable/magic-only/dagger-only foes keep their existing rule line."
- "Condition chips state their effect from your side, with the resulting range where one exists: … 'Weakened: it hits you only on 18–20'."

Phase 71 D-16 made the long-press card the place a foe condition is explained, so the chip effects go on its effect lines. The chips on the foe card stay short.

Output: the odds line and exported foeConditionEffect in foeDetails.js, with tests on a real hero.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 74-06 creates or changes.)

- engine/derived.js `targetStrikeFaces`, `heroStrikeFacesVs`, `foeSwingVsHero`; engine/combat.js calls them; test/unit/odds-helpers.test.js (74-01)
- src/browser/rollRange.js, the ONE formatter (74-02)
- The event-driven Oracle, fight-log, dice-reveal and rail lines re-signed from the player's side; heights/water fear lines (74-03)
- src/browser/rollOdds.js; the hero sheet and combat menu ranges; hero snapshots re-pinned (74-04)
- Item/loot/store/find comparisons: "−2 to hit, worse than your Club", crit ranges; store snapshots re-pinned (74-05)
- **This plan:** foeDetails.js odds line "You hit it on … · it hits you on … (d20; mods)"; the static to-hit defence label removed; exported `foeConditionEffect` on the long-press effect lines; test/unit/foeDetails.test.js
- src/browser/conditionEffects.js and the hero condition-chip effects on the chip-tap card (74-07)
- The consistency guard test, the new copy banks walked by the standing scans, and the ROLL-LEDGER display closure (74-08)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-04-SUMMARY.md
@src/browser/foeDetails.js
@src/browser/rollOdds.js

<interfaces>
src/browser/rollOdds.js (74-04):
- `heroHitOddsVs(state, foe)` returns `{ faces, dieN, range, text, untouchable }`, e.g. "17–20 (d20)";
- `foeHitOddsVs(state, foe)` returns `{ faces, dieN, mods, range, text, plainText }`, e.g. text "17–20 (d20; Sidestep +2, insulted −1)" and plainText "17–20 (d20)".
Both read the engine's heroStrikeFacesVs/foeSwingVsHero (74-01), so they include dozing/stupid, the sp.toHit cap, sp.fast, magicOnly/daggerOnly, Afraid, blind, the Weaken cap and the insult. They need a full hero on state.c (race, cls, sub, level, weapon) and throw on a bare `c`.

src/browser/foeDetails.js today (322 lines):
- FOE_DETAILS_COPY L60-L113, including `toHit: "Hit only on a {n} or under"`, `magicOnly`, `daggerOnly` and `halfDmg`;
- `safe(fn, fallback)` L132, `fill` L119, `line(text)` L142, `capFirst` L143;
- defenceLine(sp) L185-L195 pushes AR, the toHit label, magicOnly, daggerOnly and halfDmg;
- attackLine L197-L220 reads `sp.never_melee`;
- effectLines(foe, state) L264-L268 writes "<chip text> — <chip desc>" per foeConditionChips chip;
- foeDetailsCard L290-L322 builds lines in D-09 order.
The module never throws: every read is guarded.

Foe chips that move a to-hit roll (src/browser/foeConditions.js keys, unchanged by this plan):
- asleep and stupid (the hero's floor of 5 faces);
- blind (the foe finds the hero only on its top face);
- weakened (combat-wide; the Weaken spell also sets combat.foeToHitPenalty = 3 and caps the foe at 3 faces, 18–20 on a d20).
Every other chip has no to-hit effect.

test/unit/foeDetails.test.js today:
- stateWith(foes) builds `c: { name, wp, maxWP, timers: {} }` with no race or class, so the odds line must quietly drop out there;
- (b) L130 pins the line order;
- (b) L174 pins the defence labels, including the to-hit label this plan removes;
- (d) L262-L305 pins the effect lines as "<text> — <desc>".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The two-way odds line on the long-press foe card</name>
  <files>src/browser/foeDetails.js, test/unit/foeDetails.test.js</files>
  <read_first>
    - src/browser/foeDetails.js (the whole file)
    - src/browser/rollOdds.js
    - test/unit/foeDetails.test.js (L1-L200)
    - test/unit/rollOdds.test.js (how 74-04 builds a full-hero state with newRun)
  </read_first>
  <behavior>
    - A level-1 Human Fighter (Club) against a plain level-1 foe gets the line "You hit it on 16–20 (d20) · it hits you on 16–20 (d20)", placed right after the defence line (or right after HP when there is no defence line).
    - Against a sp.toHit 4 foe the Fighter reads "You hit it on 17–20 (d20)", and the defence line no longer carries any to-hit label.
    - With the hero's Sidestep live and combat.parleyInsulted set, the foe side reads "it hits you on 17–20 (d20; Sidestep +2, insulted −1)"; a Guard hero reads "(d20; Guard +1)".
    - A magicOnly foe with no magic weapon reads "You cannot touch it · it hits you on …" and its defence line still reads "Only magic touches it"; with c.magicWpn 1 it reads "You hit it on …".
    - A never_melee foe's odds line has no "it hits you" part.
    - With the minimal stateWith hero (no race or class) there is no odds line, no throw, and every existing line-order assertion still holds.
  </behavior>
  <action>
1. foeDetails.js:
   - Import heroHitOddsVs and foeHitOddsVs from ./rollOdds.js.
   - In FOE_DETAILS_COPY, delete the `toHit` leaf and add `oddsYou: "You hit it on {range}"`, `oddsUntouchable: "You cannot touch it"` and `oddsIt: "it hits you on {range}"`.
   - defenceLine stops reading sp.toHit (the cap now shows as a range).
2. Add oddsLine(foe, sp, state):
   - It returns null unless state.c is an object. The whole computation is inside safe(…, null).
   - The you-part is oddsUntouchable when heroHitOddsVs(...).untouchable, else oddsYou filled with its text.
   - The it-part is omitted when sp.never_melee is set, else oddsIt filled with foeHitOddsVs(...).text (the formatter already signs the mods for the player).
   - Join the parts with " · ".
3. In foeDetailsCard, push the odds line right after the defence line (after HP when defence is omitted), before the attack line. Update the header comment's D-09 order and add a Phase 74 paragraph: both directions against the hero right now, from rollOdds.js, never a restated formula; the old static to-hit label became this range (74-CONTEXT).
4. foeDetails.test.js: add a full-hero state helper (newRun with a Human Fighter, Club, level 1, a combat holding the foe) and one test per behaviour above. Update (b) line order to include the odds line for a full hero, and (b) defence labels to drop the to-hit label and pin the range instead. Keep every minimal-state test unchanged and passing.
  </action>
  <verify>
    <automated>node --test test/unit/foeDetails.test.js test/unit/foe-inspect-shell.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/browser/foeDetails.js | grep -c "or under"` equals 0.
    - `grep -c "heroHitOddsVs(" src/browser/foeDetails.js` is at least 1 and `grep -c "foeHitOddsVs(" src/browser/foeDetails.js` is at least 1.
    - `node --test test/unit/foeDetails.test.js` passes, including the plain-foe line "You hit it on 16–20 (d20) · it hits you on 16–20 (d20)" exactly.
  </acceptance_criteria>
  <done>The long-press card shows the real right-now odds both ways.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Foe condition effects with their ranges on the card</name>
  <files>src/browser/foeDetails.js, test/unit/foeDetails.test.js</files>
  <read_first>
    - src/browser/foeDetails.js (after Task 1: effectLines, FOE_DETAILS_COPY)
    - src/browser/foeConditions.js (FOE_CONDITIONS keys; the chip shape { key, label, tone, rounds, text, desc })
    - test/unit/foeDetails.test.js (the (d) effect-line tests L262-L310)
  </read_first>
  <behavior>
    - foeConditionEffect({ key: "weakened", … }, foe, state) for a Fighter against a level-1 foe while combat.weakened and combat.foeToHitPenalty 3 are set returns "it hits you only on 18–20 (d20)"; with combat.parleyInsulted also set it returns "it hits you only on 17–20 (d20)".
    - For a blind foe it returns "it hits you only on 20 (d20)"; blind and insulted gives "it hits you only on 19–20 (d20)".
    - For an asleep foe against a level-1 Human Magic User it returns "you hit it on 16–20 (d20)"; a stupid foe reads the same.
    - Every other chip key (stunned, hamstrung, marked, frozen, acid, dot, shrunk, fixated, frenzied) returns null.
    - The long-press effect line for the weakened fight reads "Weakened · 2 — it hits you only on 18–20 (d20). " followed by the unchanged Weakened description; a chip with no effect keeps "<text> — <desc>" exactly as today.
    - With a minimal hero (no race or class) foeConditionEffect returns null and the effect lines are byte-identical to today.
  </behavior>
  <action>
1. FOE_DETAILS_COPY gains `effectYou: "you hit it on {range}"` and `effectIt: "it hits you only on {range}"`.
2. Export foeConditionEffect(chip, foe, state), computed inside safe(…, null):
   - asleep and stupid fill effectYou with heroHitOddsVs(state, foe).text;
   - blind and weakened fill effectIt with foeHitOddsVs(state, foe).plainText;
   - any other key returns null.
   JSDoc: it states each condition's effect from the player's side with the resulting range (74-CONTEXT), it reads only rollOdds.js, and Phase 77's effect indicators (CMBUI-13) are expected to reuse it.
3. effectLines(foe, state): when foeConditionEffect returns text, the line is "<chip text> — <effect>. <chip desc>"; otherwise it is unchanged. Every descriptions leaf in foeConditions.js stays untouched (authored prose, Phase 79).
4. foeDetails.test.js: one test per behaviour above, using the full-hero helper from Task 1. Keep the existing (d) tests (minimal hero) passing byte-identically.
5. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/foeDetails.test.js test/unit/foe-conditions.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function foeConditionEffect(" src/browser/foeDetails.js` equals 1.
    - `git diff --stat -- src/browser/foeConditions.js` is empty.
    - `node --test test/unit/foeDetails.test.js` passes with the exact "it hits you only on 18–20 (d20)" and "you hit it on 16–20 (d20)" cases; `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every to-hit-moving foe condition states its effect and resulting range from the player's side.</done>
</task>

</tasks>

<verification>
- The long-press card's odds and chip effects come from rollOdds.js (the engine's helpers) and the one formatter.
- foeConditions.js and every authored description are untouched; npm test is green.
</verification>

<success_criteria>
ROLL-02 for foe details (the engine's own range both ways, no translation). ROLL-03 for foe-side modifiers and foe condition chips (signed from the player's side, with the resulting range).
</success_criteria>

<output>
Create `.planning/phases/74-roll-display-modifier-honesty/74-06-SUMMARY.md` when done. Include a sample card before/after and a "Human verification (deferred to end of run)" note: on the Pixel 7, long-press a foe and confirm "You hit it on … · it hits you on …" with Sidestep and an insult showing "(Sidestep +2, insulted −1)"; cast Weaken and confirm the card's Weakened line reads "it hits you only on 18–20 (d20)".
</output>
