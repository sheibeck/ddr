---
phase: 74-roll-display-modifier-honesty
plan: 04
type: execute
wave: 2
depends_on:
  - 74-01
  - 74-02
files_modified:
  - src/browser/rollOdds.js
  - test/unit/rollOdds.test.js
  - src/browser/heroTab.js
  - test/unit/characterSheetViewModel.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/fixtures/shell-snapshots/mu.hero.txt
  - test/unit/fixtures/shell-snapshots/thief.hero.txt
  - src/browser/combatMenu.js
  - test/unit/combatMenu.test.js
autonomous: true
requirements:
  - ROLL-02
  - ROLL-03
must_haves:
  truths:
    - "src/browser/rollOdds.js computes every non-event 'right now' odds only from the engine's own derived functions (toHit, afraidNeed, strikeDie, heroStrikeFacesVs, foeSwingVsHero, foeDie, fleeBreakdown) and formats them only through rollRange.js"
    - "At level 1 with a need-0 weapon the hero sheet's To hit row reads '18–20 (d20)' for a Human Magic User, '17–20 (d20)' for a Human Thief and '16–20 (d20)' for a Human Fighter (ROADMAP success criterion 1; 74-CONTEXT specifics)"
    - "The hero sheet's To hit value is the range of the hero's current strike die, including a live Afraid penalty ('19–20 (d20)' for an afraid level-1 Fighter), in both characterSheetViewModel and the DOM (#s-hit)"
    - "The combat menu's STRIKE row reads 'Hit 16–20 (d20) · 2–7 dmg', replacing the old die-then-roll-under text, and reads the same value the hero sheet shows"
    - "The combat menu's FLEE row cost is the flee winning range on the d20 ('9–20 (d20)' for a Thief, '14–20 (d20)' with no modifiers); its description lists the flee modifiers through the one formatter ('Thief +5')"
    - "The two hero-tab snapshot fixtures (mu.hero.txt, thief.hero.txt) are the only shell fixtures that change, each deliberately re-pinned and named in the SUMMARY"
  artifacts:
    - path: src/browser/rollOdds.js
      provides: "heroHitOdds, heroHitOddsVs, foeHitOddsVs, fleeOdds"
      contains: "export function foeHitOddsVs"
    - path: src/browser/heroTab.js
      provides: "the sheet's To hit row and #s-hit from heroHitOdds"
      contains: "heroHitOdds(state)"
    - path: src/browser/combatMenu.js
      provides: "STRIKE sub 'Hit {range} · {dmg} dmg'; FLEE cost from fleeOdds"
      contains: "fleeOdds("
  key_links:
    - from: src/browser/rollOdds.js
      to: engine/derived.js
      via: "toHit/afraidNeed/strikeDie/heroStrikeFacesVs/foeSwingVsHero/foeDie/fleeBreakdown"
      pattern: "from \"../../engine/derived.js\""
    - from: src/browser/combatMenu.js
      to: src/browser/heroTab.js#characterSheetViewModel
      via: "the STRIKE sub reads the sheet's toHit value, so the two surfaces can never disagree"
      pattern: "statValue\\(\"toHit\"\\)"
  prohibitions:
    - statement: "MUST NOT compute a displayed range from a restated to-hit formula; every range comes from the engine's derived functions through rollOdds.js"
      requirement_id: ROLL-02
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT show odds as a percentage or 'N+' shorthand, or a flee/strike modifier whose sign reads the wrong way for the player"
      requirement_id: ROLL-03
      category: transparency
      status: resolved
      verification: test
---

<objective>
Create the shared "right now" odds module and put roll-high ranges on the hero sheet and the combat menu.

Purpose: 74-CONTEXT "Hero sheet, combat menu and foe details": "The hero sheet and combat menu show 'Hit 16–20 (d20)' on the current strike die. This replaces `1–${toHit}` and '5 to hit'." The derived functions still return winning faces (Phase 73 handoff), so every non-event surface needs a range computed from them. rollOdds.js is that single place. 74-06 (foe details) and 74-07 (chips) also read it.

Output: rollOdds.js with its tests, the hero sheet on heroHitOdds, and the combat menu's STRIKE and FLEE rows on the new ranges. The two hero snapshots are re-pinned.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 74-04 creates or changes.)

- engine/derived.js `targetStrikeFaces`, `heroStrikeFacesVs`, `foeSwingVsHero`; engine/combat.js calls them; test/unit/odds-helpers.test.js (74-01)
- src/browser/rollRange.js, the ONE formatter (74-02)
- The event-driven Oracle, fight-log, dice-reveal and rail lines re-signed from the player's side; heights/water fear lines (74-03)
- **This plan:** src/browser/rollOdds.js (`heroHitOdds`, `heroHitOddsVs`, `foeHitOddsVs`, `fleeOdds`) and test/unit/rollOdds.test.js; the hero sheet To hit "16–20 (d20)" (view model and #s-hit); the combat menu STRIKE "Hit 16–20 (d20) · 2–7 dmg" and FLEE "9–20 (d20)"; mu.hero.txt and thief.hero.txt re-pinned
- Item/loot/store/find comparisons: "−2 to hit, worse than your Club", crit ranges; store snapshots re-pinned (74-05)
- The foe-details odds line and the foe condition-chip effects (74-06)
- src/browser/conditionEffects.js and the hero condition-chip effects on the chip-tap card (74-07)
- The consistency guard test, the new copy banks walked by the standing scans, and the ROLL-LEDGER display closure (74-08)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-01-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-02-SUMMARY.md
@src/browser/rollRange.js

<interfaces>
engine/derived.js:
- `toHit(state)` gives the hero's base winning faces. It includes the dazed and dark-cap terms and inspired; it excludes Afraid.
- `afraidNeed(state, faces)` applies the live Afraid penalty.
- `strikeDie(c)` gives the current strike die (STRIKE_DICE [20, 12, 10, 8, 6] by level; Acuteness and Elven step swap it).
- `foeDie(c, f)` gives the foe's strike die against this hero.
- `fleeBreakdown(c)` gives `{ need, mods, bonus }`. The engine flee check is `roll >= need - bonus` on a d20 (engine/combat.js#flee, the fleeRolled event's atLeast).
- From 74-01: `heroStrikeFacesVs(state, t)` and `foeSwingVsHero(state, f)` returning `{ faces, mods }` (foe-signed deltas).

src/browser/rollRange.js (74-02): `facesRangeText(faces, dieN)`, `dieText(dieN)`, `hitRangeText(faces, dieN, { mods, roller })`, `rangeText(atLeast, dieN)`, `modsText(mods, roller)`, `ROLLERS`.

src/browser/heroTab.js today:
- import from engine/derived.js L24 (strikeDie, toHit, …);
- characterSheetViewModel stats L171-L181: the toStrike row `d${strikeDie(c)}` and a toHit row built as the old 1-to-N readout, with a Phase 31 comment explaining the low-roll reading;
- renderHeroTab L610-L614 writes #s-die and #s-hit, the latter with the same old readout.

src/browser/combatMenu.js today:
- COMBAT_MENU_COPY L24-L62;
- a module-private flee modifier formatter L65-L70;
- strikeSub L147 joins the toStrike value, the toHit value plus " to hit", and the damage;
- the flee row L339-L353 (cost built from a signed net bonus and `fb.need` plus "+"; desc lists the modifiers);
- the parley row L355-L362 (cost "d20").

Tests that pin the old text:
- characterSheetViewModel.test.js L54-L55;
- combatMenu.test.js L90 (strike sub regex), L112 ("d20, 14+"), L186 ("d20+5, 14+" with "(Thief +5)"), L205 ("d20−3, 14+");
- shell-gear-39.test.js L187-L195 (a source pin on the #s-hit write in heroTab.js);
- test/unit/fixtures/shell-snapshots/thief.hero.txt L23 and mu.hero.txt L23 (the rendered #s-hit).

Snapshot regeneration: `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js` writes every fixture; only the two hero fixtures may differ afterwards.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rollOdds.js, the one place non-event odds are computed</name>
  <files>src/browser/rollOdds.js, test/unit/rollOdds.test.js</files>
  <read_first>
    - engine/derived.js (toHit, afraidNeed, strikeDie, foeDie, fleeBreakdown, heroStrikeFacesVs, foeSwingVsHero)
    - src/browser/rollRange.js
    - engine/combat.js#flee (~L1200-L1215: atLeast = need − bonus)
    - test/unit/combatMenu.test.js L1-L60 (how it builds a newRun state, for the test's fixtures)
  </read_first>
  <behavior>
    - heroHitOdds(state) for a level-1 Human Fighter, Thief and Magic User each wielding a Club gives text "16–20 (d20)", "17–20 (d20)" and "18–20 (d20)", with faces 5, 4, 3 and dieN 20.
    - The same Fighter with state.combat.afraid 2 gives "19–20 (d20)"; with a live dazed foeEffect gives "18–20 (d20)"; at level 2 gives "8–12 (d12)".
    - heroHitOddsVs(state, foe) for that Fighter against a sp.toHit 4 foe gives "17–20 (d20)"; against a magicOnly foe without c.magicWpn gives untouchable true and faces 0; for the Magic User against an asleep foe gives "16–20 (d20)".
    - foeHitOddsVs(state, foe) for a level-1 foe against a Human Soldier gives "16–20 (d20)"; against a Guard gives "17–20 (d20; Guard +1)"; with Sidestep live and combat.parleyInsulted gives "17–20 (d20; Sidestep +2, insulted −1)"; against a blind foe with the insult gives "19–20 (d20; blind +4, insulted −1)"; under combat.foeToHitPenalty 3 gives "18–20 (d20; Weaken +2)". Its plainText omits the modifiers ("18–20 (d20)").
    - fleeOdds(c) for a Human Fighter in unarmoured kit gives atLeast 14 and text "14–20 (d20)" with no mods; for a Human Thief gives "9–20 (d20)" and modsText "Thief +5"; its atLeast equals fleeBreakdown(c).need − fleeBreakdown(c).bonus.
    - No function mutates its state or foe (deep-frozen inputs), and two calls return deep-equal results.
  </behavior>
  <action>
1. Create src/browser/rollOdds.js, a presentation-only pure module with no DOM, rng or mutation. It imports the derived functions listed in `<interfaces>` and the rollRange.js formatter. Exports:
   - `heroHitOdds(state)` returns `{ faces, dieN, range, text }`, where faces = afraidNeed(state, toHit(state)) and dieN = strikeDie(state.c).
   - `heroHitOddsVs(state, foe)` returns `{ faces, dieN, range, text, untouchable }`, from heroStrikeFacesVs(state, foe) on strikeDie(state.c).
   - `foeHitOddsVs(state, foe)` returns `{ faces, dieN, mods, range, text, plainText }`, from foeSwingVsHero(state, foe) on foeDie(state.c, foe). text = hitRangeText(faces, dieN, { mods, roller: ROLLERS.foe }); plainText = hitRangeText(faces, dieN).
   - `fleeOdds(c)` returns `{ atLeast, dieN: 20, mods, range, text, modsText }`, from fleeBreakdown(c) with atLeast = need − bonus. text uses the same range/die form (convert with dieN + 1 − atLeast faces through hitRangeText, or rangeText plus dieText). modsText = modsText(mods, ROLLERS.you).
2. The header comment states the rule: every range here is a read of the engine's own derived functions (74-CONTEXT: never a re-derived formula), formatted only through rollRange.js. It names 74-04/74-06/74-07 as consumers and notes that Phase 77's effect indicators can reuse it. The only arithmetic in the module is the flee `need − bonus`, which mirrors engine/combat.js#flee exactly; say so.
3. Create test/unit/rollOdds.test.js covering every behaviour above. Build states with newRun plus explicit overrides (race "Human", level, cls, sub, weapon "Club", timers, combat), the same way combatMenu.test.js does.
  </action>
  <verify>
    <automated>node --test test/unit/rollOdds.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export function (heroHitOdds|heroHitOddsVs|foeHitOddsVs|fleeOdds)\(" src/browser/rollOdds.js` equals 4.
    - `grep -v '^\s*//' src/browser/rollOdds.js | grep -cE "document\.|window\.|Math\.random|Date\.now"` equals 0.
    - `node --test test/unit/rollOdds.test.js` passes, including the three level-1 class ranges exactly.
  </acceptance_criteria>
  <done>Every non-event odds read has one module that reads only the engine and formats only through rollRange.js.</done>
</task>

<task type="auto">
  <name>Task 2: The hero sheet reads the strike range</name>
  <files>src/browser/heroTab.js, test/unit/characterSheetViewModel.test.js, test/unit/shell-gear-39.test.js, test/unit/fixtures/shell-snapshots/mu.hero.txt, test/unit/fixtures/shell-snapshots/thief.hero.txt</files>
  <read_first>
    - src/browser/heroTab.js (L18-L30 imports; L160-L185 the stats rows; L596-L620 renderHeroTab)
    - src/browser/rollOdds.js
    - test/unit/characterSheetViewModel.test.js (L1-L70)
    - test/unit/shell-gear-39.test.js L180-L200
    - test/unit/shell-tab-snapshots.test.js L40-L80 (the MZ_SNAPSHOT_UPDATE rule: a declared, deliberate DOM change with a written rationale)
  </read_first>
  <action>
1. heroTab.js: import heroHitOdds from ./rollOdds.js on its own import line (the codebase's separate-import-line convention). In characterSheetViewModel, the toHit row's value becomes heroHitOdds(state).text; keep the key "toHit" and the label "TO HIT", and keep the toStrike row as the die. Replace the Phase 31 low-roll comment with a Phase 74 comment: roll-high, the winning range of the current strike die, Afraid included, per 74-CONTEXT.
2. renderHeroTab: #s-hit's textContent becomes heroHitOdds(state).text. Drop `toHit` from the derived.js import if nothing else in the file uses it.
3. characterSheetViewModel.test.js: the toHit assertions pin the new value against heroHitOdds(state).text. Add a test for the 74-CONTEXT specifics: level-1 Human Magic User, Thief and Fighter each on a Club read "18–20 (d20)", "17–20 (d20)" and "16–20 (d20)" exactly.
4. shell-gear-39.test.js: update the #s-hit source pin to the heroHitOdds read, keeping the "classic script must no longer write #s-hit" assertion.
5. Regenerate the snapshots with `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`. Confirm with `git status --short test/unit/fixtures/shell-snapshots` that ONLY mu.hero.txt and thief.hero.txt changed: thief.hero's #s-hit becomes "16–20 (d20)" (Thief 4 plus the Dagger's +1) and mu.hero's becomes "18–20 (d20)". Then rerun the snapshot test without the flag. Name both fixtures and the rationale in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/characterSheetViewModel.test.js test/unit/shell-gear-39.test.js test/unit/shell-tab-snapshots.test.js test/unit/heroTab.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "heroHitOdds(state).text" src/browser/heroTab.js` equals 2.
    - `grep -v '^\s*//' src/browser/heroTab.js | grep -c '"1–" +'` equals 0.
    - `grep -c 'text="16–20 (d20)"' test/unit/fixtures/shell-snapshots/thief.hero.txt` equals 1 and `grep -c 'text="18–20 (d20)"' test/unit/fixtures/shell-snapshots/mu.hero.txt` equals 1.
    - `git diff --name-only -- test/unit/fixtures/shell-snapshots` lists exactly mu.hero.txt and thief.hero.txt.
  </acceptance_criteria>
  <done>The hero sheet shows the engine's own roll-high range on the current strike die.</done>
</task>

<task type="auto">
  <name>Task 3: The combat menu's STRIKE and FLEE rows</name>
  <files>src/browser/combatMenu.js, test/unit/combatMenu.test.js</files>
  <read_first>
    - src/browser/combatMenu.js (L14-L70; L138-L150; L335-L365)
    - src/browser/rollOdds.js and src/browser/rollRange.js
    - test/unit/combatMenu.test.js (L80-L120, L180-L210)
  </read_first>
  <action>
1. COMBAT_MENU_COPY gains `strikeSub: "Hit {range} · {dmg} dmg"`. The STRIKE action's sub fills it from the sheet's toHit value and damage value (statValue("toHit"), statValue("damage")), so the menu and the hero sheet can never disagree.
2. FLEE: build the row from fleeOdds(c). The cost is its text ("9–20 (d20)"; the WITHDRAW cost is unchanged). The description appends modsText(mods, ROLLERS.you) in parentheses when there are modifiers, exactly as today's wording does. Delete the module-private flee formatter and the signed net-bonus string; drop the fleeBreakdown import if nothing else uses it.
3. PARLEY keeps "d20". There is no engine-derived parley faces function and the row prints no range or modifier today. Note this in a one-line comment.
4. combatMenu.test.js: the strike-sub regex becomes a match for "Hit <range> (d<N>) · <lo>–<hi> dmg". The flee cost pins become "14–20 (d20)" (L112), "9–20 (d20)" with the same "(Thief +5)" desc (L186), and "17–20 (d20)" (L205). Add one test asserting the STRIKE sub's range equals characterSheetViewModel's toHit value for the same state.
5. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/combatMenu.test.js test/unit/rollOdds.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strikeSub: \"Hit {range} · {dmg} dmg\"" src/browser/combatMenu.js` equals 1 and `grep -c "fleeOdds(c)" src/browser/combatMenu.js` equals 1.
    - `grep -v '^\s*//' src/browser/combatMenu.js | grep -c "function fleeModsText"` equals 0.
    - `node --test test/unit/combatMenu.test.js` passes with the new pins; `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The combat menu reads the same ranges as the hero sheet, and flee shows its real winning range.</done>
</task>

</tasks>

<verification>
- rollOdds.js reads only the engine's derived functions; the sheet, the #s-hit DOM and the combat menu agree.
- Only the two hero snapshots changed, deliberately; npm test is green.
</verification>

<success_criteria>
ROLL-02 for the hero sheet and combat menu (the engine's own range, a caster 18–20, a thief 17–20, a fighter 16–20). ROLL-03 for the flee modifier list (one formatter, the player's sign).
</success_criteria>

<output>
Create `.planning/phases/74-roll-display-modifier-honesty/74-04-SUMMARY.md` when done. Include the rollOdds.js API, both re-pinned fixtures with their before/after #s-hit values, and a "Human verification (deferred to end of run)" note: on the Pixel 7, a new level-1 hero's sheet reads the class range on d20 (thieves with a dagger read 16–20); the combat menu's STRIKE reads "Hit 16–20 (d20) · …" and FLEE reads a "N–20 (d20)" range.
</output>
