---
phase: 74-roll-display-modifier-honesty
plan: 03
type: execute
wave: 2
depends_on:
  - 74-02
files_modified:
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/roll-display-lines.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/clarity-cause-lines.test.js
  - docs/CLARITY.md
  - tools/voice-sample-output.txt
autonomous: true
requirements:
  - ROLL-02
  - ROLL-03
must_haves:
  truths:
    - "A foe's roll line is signed from the player's side: 'The Zit swings, 12 vs 19–20 (Sidestep +2, insulted −1), and misses.' Foe-roll lines (foeMissed, struckByFoe, memberStruck) negate the engine deltas; the hero's own lines (strikeMissed, struck, spellThrown, fleeRolled) and ally lines (allyMissed, allyCast) print them as they are (74-CONTEXT: one formatter, keyed on who rolled)"
    - "The ROLL-LEDGER 'Handoffs → Phase 74' needModsClause ambiguity is closed: a '+' on any Oracle, fight-log, dice-reveal or rail roll line is good news for the player, and the Weaken cap reads 'Weaken', never 'penalty'"
    - "The heights and water phobia lines are signed from the player's side and name the check they cost: the Oracle ends 'Heights: … −2 on the climb.' / 'Bodies of water: … −1 on the leap.' and the rail reads 'Heights: −2 on the climb.' / 'Bodies of water: −1 on the leap.'"
    - "The parley line's fluency clause and every flee modifier list go through the same formatter (hero roller, unchanged values)"
    - "The fight log's tap-to-reveal dice and the rail's roll line carry exactly the Oracle sentence's signed tokens, because both read oracleDetailText(narrateEvent(e))"
    - "Modifier lists print in the order the engine recorded them (the insult stays last) and each entry keeps its place (edges ROLL-02/ROLL-03 ordering)"
    - "Narrating an event never mutates it: the same frozen event narrated twice gives identical text and its mods deltas stay roller-signed (edge ROLL-03 idempotency)"
    - "The foe line's 'would have hit' reason (LINE_FOR.foeMissed) keeps its roller-signed logic and names modifiers through the formatter's labels"
  artifacts:
    - path: src/browser/eventNarration.js
      provides: "every event-driven roll line formats its modifiers through rollRange.js with an explicit roller"
      contains: "ROLLERS.foe"
    - path: src/browser/narrationLines.js
      provides: "flee and fear rail lines through the formatter; foeMissed names via modLabel"
      contains: "modLabel"
    - path: test/unit/roll-display-lines.test.js
      provides: "the per-line sign contract for every roll-carrying line"
  key_links:
    - from: src/browser/eventNarration.js (foeMissed/struckByFoe/memberStruck)
      to: src/browser/rollRange.js#modsClause
      via: "modsClause(e.mods, ROLLERS.foe)"
      pattern: "modsClause\\(e\\.mods, ROLLERS\\.foe\\)"
    - from: src/browser/fightLog.js / rail.js
      to: src/browser/eventNarration.js
      via: "oracleDetailText(narrateEvent(e)) reuses the Oracle sentence for dice reveals and rail roll lines"
      pattern: "oracleDetailText"
  prohibitions:
    - statement: "MUST NOT print a '+' modifier that is worse for the player or a '−' that is better on any Oracle, fight-log, dice-reveal or rail line"
      requirement_id: ROLL-03
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT change an engine event, its mods values or any engine file to make a line read better; the flip happens only at render time"
      requirement_id: ROLL-03
      category: integrity
      status: resolved
      verification: test
    - statement: "MUST NOT reword authored roll-direction prose that belongs to Phase 79 (the Smoke and Battle Roar lines, 'They need two better', content/ notes, the Lockpicks text); only computed numbers and signs, plus the minimum words a flipped sign needs, change here; new words stay family-friendly deadpan and say HP, never WP"
      requirement_id: ROLL-03
      category: values
      status: resolved
      verification: judgment
---

<objective>
Re-sign every event-driven roll line from the player's side through the one formatter.

Purpose: 74-CONTEXT "Oracle and fight-log roll lines" and "A foe's roll against you". Phase 73 moved these lines to the final "17 vs 18–20 (mods)" shape but printed the mods as the engine signs them, for the roller. This is the ROLL-LEDGER O3 / "Handoffs → Phase 74" ambiguity: "insulted +1" on a foe line is bad news written with a good-news sign. The fight log's dice reveal and the rail's roll line reuse the Oracle sentence, so fixing the Oracle builders fixes all three surfaces.

The heights/water phobia lines print "+N" for a penalty (with a roll-under clause). This is the same bug on a non-combat surface, and it is fixed here.

Output: eventNarration.js and narrationLines.js on the formatter, a per-line sign test, and the regenerated voice sample.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 74-03 creates or changes.)

- engine/derived.js `targetStrikeFaces`, `heroStrikeFacesVs`, `foeSwingVsHero`; engine/combat.js calls them; test/unit/odds-helpers.test.js (74-01)
- src/browser/rollRange.js, the ONE formatter (74-02)
- **This plan:** eventNarration.js/narrationLines.js roll lines re-signed by roller (foe lines negated); heightsFear/waterFear "−N on the climb/leap"; parley fluency and flee mods through the formatter; test/unit/roll-display-lines.test.js; docs/CLARITY.md rows; tools/voice-sample-output.txt regenerated
- src/browser/rollOdds.js; the hero sheet and combat menu ranges; hero snapshots re-pinned (74-04)
- Item/loot/store/find comparisons: "−2 to hit, worse than your Club", crit ranges; store snapshots re-pinned (74-05)
- The foe-details odds line and the foe condition-chip effects (74-06)
- src/browser/conditionEffects.js and the hero condition-chip effects on the chip-tap card (74-07)
- The consistency guard test, the new copy banks walked by the standing scans, and the ROLL-LEDGER display closure (74-08)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-02-SUMMARY.md
@src/browser/rollRange.js

<interfaces>
src/browser/rollRange.js (74-02): `modsClause(mods, roller)`, `modsText(mods, roller)`, `modLabel(name)`, `signedText(n)`, `ROLLERS` ({ you, ally, foe }), `rangeText`, `rollVsText`. A missing roller reads as "you".

src/browser/eventNarration.js today (line numbers drift):
- import from ./rollRange.js L61;
- module-private modsText L86-L91 and modsClause L93-L99;
- heightsFear L190 and waterFear L192: a beat sentence, then a "+N" penalty clause written in the roll-under reading;
- strikeMissed ~L397, struck ~L421, fleeRolled ~L462-L467, parleyRolled L490 (the "(+N for the tongue)" clause built with a literal plus), allyMissed ~L530-L533, allyCast L539, memberStruck L556, foeMissed L563-L564, struckByFoe L591-L592, spellThrown L784.

Rollers:
- "you": strikeMissed, struck, spellThrown, fleeRolled;
- "ally": allyMissed, allyCast (a party member's or summon's own roll);
- "foe": memberStruck, foeMissed, struckByFoe (a foe's roll, including pursuit).

src/browser/narrationLines.js today:
- import from ./rollRange.js L54;
- module-private flee formatter L323-L328;
- heightsFear L1031 and waterFear L1033 ("Heights: +N on the roll.");
- fleeRolled L1193-L1200;
- foeMissed L1279-L1291: wouldHaveHit uses the roller-signed negative deltas (keep that logic) and appends their raw names.

Engine event facts (unchanged): heightsFear/waterFear carry `penalty` (a positive number of winning faces the phobia removes from the climb (heights) or leap (water) d10 check, engine/movement.js ~L270/~L296). parleyRolled carries `fluency`; the fluency bonus is 2 × fluency winning faces for the hero.

Tests that pin the old text:
- test/unit/feedback-payload.test.js L661-L691: struckByFoe with Guard −1 asserts a minus. After re-signing it must assert "Guard +1".
- test/unit/clarity-cause-lines.test.js: L343-L344 (Oracle fear lines) and L396-L397 (rail fear lines).
- docs/CLARITY.md rows 35-36 quote the fear lines. The ledger test reads only the event keys, but the quoted text must stay truthful.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The Oracle roll lines re-signed by roller</name>
  <files>src/browser/eventNarration.js, test/unit/roll-display-lines.test.js, test/unit/feedback-payload.test.js, test/unit/clarity-cause-lines.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (L50-L100 imports and helpers; L185-L195; L390-L470; L485-L495; L520-L600; L780-L790)
    - src/browser/rollRange.js
    - test/unit/feedback-payload.test.js L655-L730
    - test/unit/clarity-cause-lines.test.js L325-L360
    - src/browser/narrationLines.js L148-L170 (oracleDetailText: how the fight log and rail reuse the Oracle sentence)
  </read_first>
  <behavior>
    - EVENT_NARRATION.foeMissed({ type:"foeMissed", name:"Zit", roll:12, atLeast:19, dieN:20, mods:[{name:"Sidestep",delta:-2},{name:"insulted",delta:1}] }) contains "12</span> vs 19–20 (Sidestep +2, insulted −1), and misses."
    - struckByFoe with mods [{Guard,-1}] contains "(Guard +1)"; with [{penalty,-2}] contains "(Weaken +2)"; memberStruck with [{Smoke,-4},{insulted,1}] contains "(Smoke +4, insulted −1)".
    - strikeMissed with mods [{afraid,-3}] contains "(afraid −3)"; struck with [{overhead,-2}] contains "(Overhead Blow −2)"; spellThrown with [{school,3}] contains "(school +3)"; allyMissed with [{overhead,-2}] contains "(Overhead Blow −2)"; fleeRolled with [{Thief,5},{Mail,-1}] still reads "(Thief +5, Mail −1)".
    - parleyRolled with fluency 1 contains "(+2 for the tongue)".
    - heightsFear with penalty 2 reads, after narrativeLineText, "Heights: your stomach reaches the ground well before your feet do. −2 on the climb."; waterFear with penalty 1 ends "−1 on the leap."
    - oracleDetailText(narrateEvent(e)) for the foeMissed event above contains "Sidestep +2, insulted −1" (the fight log's dice reveal and the rail read the same tokens).
    - Narrating a deep-frozen foeMissed event twice returns identical strings and does not throw.
  </behavior>
  <action>
1. eventNarration.js: delete the module-private modsText and modsClause. Extend the rollRange.js import with modsClause, signedText and ROLLERS, and pass an explicit roller at every call site: ROLLERS.you on strikeMissed, struck, spellThrown and fleeRolled; ROLLERS.ally on allyMissed and allyCast; ROLLERS.foe on memberStruck, foeMissed and struckByFoe. Line shapes do not change otherwise.
2. parleyRolled: build the fluency clause with signedText(e.fluency * 2) followed by " for the tongue" (hero roller, same text as today).
3. heightsFear and waterFear: keep each beat sentence exactly. Replace the trailing penalty clause with the player-signed cost of the check: signedText of the negated penalty followed by " on the climb." (heights) or " on the leap." (water). A missing penalty reads "0".
4. Rewrite the helper-area comment block to state the sign rule (74-CONTEXT: + is better for the player; the engine's mods stay signed for the roller and the flip happens here, per line, through rollRange.js). Remove every comment that still says the foe-side signs are Phase 74's job.
5. Create test/unit/roll-display-lines.test.js covering every behaviour above.
6. Update the old-text pins: feedback-payload.test.js's struckByFoe Guard assertion becomes an exact "(Guard +1)" match (keep the plain-sentence byte-identity assertion); clarity-cause-lines.test.js's two Oracle fear rows get the new endings.
  </action>
  <verify>
    <automated>node --test test/unit/roll-display-lines.test.js test/unit/feedback-payload.test.js test/unit/clarity-cause-lines.test.js test/unit/fightLog.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/browser/eventNarration.js | grep -cE "function (modsText|modsClause)\b"` equals 0.
    - `grep -c "modsClause(e.mods, ROLLERS.foe)" src/browser/eventNarration.js` equals 3 and `grep -c "ROLLERS.you" src/browser/eventNarration.js` is at least 4.
    - `grep -v '^\s*//' src/browser/eventNarration.js | grep -c "wanted low"` equals 0.
    - `node --test test/unit/roll-display-lines.test.js` passes with every behaviour above as its own test.
  </acceptance_criteria>
  <done>Every Oracle roll line signs its modifiers from the player's side.</done>
</task>

<task type="auto">
  <name>Task 2: The rail lines, the would-have-hit names, the docs and the voice sample</name>
  <files>src/browser/narrationLines.js, test/unit/roll-display-lines.test.js, test/unit/clarity-cause-lines.test.js, docs/CLARITY.md, tools/voice-sample-output.txt</files>
  <read_first>
    - src/browser/narrationLines.js (L45-L60 imports; L318-L330; L1025-L1035; L1190-L1225; L1275-L1295)
    - test/unit/clarity-cause-lines.test.js L385-L400
    - test/unit/narrationLinesTable.test.js L160-L175 (the foeMissed would-have-hit assertions)
    - docs/CLARITY.md L30-L40
    - tools/voice-sample.mjs (header: how it writes tools/voice-sample-output.txt)
  </read_first>
  <action>
1. narrationLines.js: delete the module-private flee formatter. fleeRolled uses modsText(mods, ROLLERS.you) from rollRange.js (identical output). heightsFear reads "Heights: " plus the player-signed cost plus " on the climb."; waterFear reads "Bodies of water: " plus the player-signed cost plus " on the leap.". Both use signedText of the negated penalty. LINE_FOR.foeMissed keeps its wouldHaveHit arithmetic (roller-signed negative deltas) but names the entries through modLabel, so a Weaken cap reads "Weaken".
2. Add rail-line cases to test/unit/roll-display-lines.test.js: LINE_FOR.heightsFear({ penalty: 2 }).text is "Heights: −2 on the climb."; LINE_FOR.waterFear({ penalty: 1 }).text is "Bodies of water: −1 on the leap."; LINE_FOR.foeMissed with roll 16, atLeast 17 and mods [{penalty,-2}] names "Weaken"; LINE_FOR.fleeRolled still reads "Flee: 8 vs 10–20 (Thief +5, Mail −1)".
3. clarity-cause-lines.test.js: update the two rail fear rows to the new text.
4. docs/CLARITY.md rows 35-36: update the quoted example text to the new Oracle lines.
5. Regenerate the voice sample with `node tools/voice-sample.mjs` and commit tools/voice-sample-output.txt (only lines whose rendered modifiers or fear clauses changed should differ; name the count in the SUMMARY).
6. Run `npm test`. For any other test that pinned an old foe-line sign or fear clause, apply the same rule (assert the player-view sign exactly; never loosen). List each such file in the SUMMARY as a Rule 1 deviation.
  </action>
  <verify>
    <automated>node --test test/unit/roll-display-lines.test.js test/unit/clarity-cause-lines.test.js test/unit/narrationLinesTable.test.js test/unit/flee-retune.test.js test/unit/linesForAction.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/browser/narrationLines.js | grep -c "function fleeModsText"` equals 0.
    - `grep -c "on the climb" src/browser/narrationLines.js` equals 1 and `grep -c "on the leap" src/browser/narrationLines.js` equals 1.
    - `git diff --stat -- engine content test/parity` is empty.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The rail, fight-log and Oracle roll lines all read one formatter, signed from the player's side.</done>
</task>

</tasks>

<verification>
- Every roll-carrying line's modifiers read + as good for the player; foe lines negate, hero and ally lines do not.
- No engine, content or parity file changed; npm test is green.
</verification>

<success_criteria>
ROLL-03 for the fight log's need breakdown (the ledger's O3 handoff is closed). ROLL-02 for the Oracle, fight log, dice reveals and rail cards: the engine's own roll and range, with no translation layer and honest signs.
</success_criteria>

<output>
Create `.planning/phases/74-roll-display-modifier-honesty/74-03-SUMMARY.md` when done. Include a before/after of one foe line, one fear line and the parley line, every test file touched with a one-line reason, and a "Human verification (deferred to end of run)" note: on the Pixel 7, get insulted after a failed parley while Sidestep is up and confirm the Oracle foe line reads "(Sidestep +2, insulted −1)"; tap that fight-log row and confirm the reveal shows the same signs; trigger a Heights climb and confirm "−2 on the climb".
</output>
