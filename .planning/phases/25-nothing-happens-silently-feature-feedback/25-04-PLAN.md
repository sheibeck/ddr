---
phase: 25-nothing-happens-silently-feature-feedback
plan: 04
type: execute
wave: 3
depends_on: ["25-03"]
files_modified:
  - mazeworld.html
  - test/unit/shell-toast-wiring.test.js
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-05, FEED-06]

must_haves:
  truths:
    - "Every dispatch call site in mazeworld.html's module script (move, camp, attack/flee/parley/sing/castSpell/drinkPotion/readScroll/useItem via engineCombatAction, buyItem, leaveStore, sellItem, resolveJoiner, takeFind/leaveFind/dropItem/equipItem/unequipSlot via inventoryAction, abandon) goes through ONE shell helper that dispatches, then raises `toastsForAction(action.type, events, {})` through `window.mzToast(text, tone)` — so exploration, camp, store, Joiner, inventory and combat events all toast, not only the combat bar (D: Hook at the single dispatch seam; FEED-01/FEED-02/FEED-06)"
    - "The old per-action switch consumer is deleted along with its spell-name helper; the DR18 hand-written equip-rejection toast is deleted (equipRejected now toasts from the table with its reason in the block tone); the two pre-dispatch 'already at full health' guard toasts (the only non-event toasts left) use the block tone (D: replace the switch with a thin consumer; route every call site)"
    - "The toast host keeps at most 4 visible toasts (cap raised from 3); lifetime formula and the reduced-motion rule are unchanged; the host stays position:fixed with pointer-events:none so toasts cause no layout shift (D: host cap 3 → 4; keep the fixed-position no-CLS contract)"
    - "mazeworld.html defines explicit colors for all seven tones: hit (bright moss), miss (muted moss — no longer the ink-soft grey), hurt (stamp red), dodge (muted stamp), magic (ditto gold), block (amber), beat (ink neutral); the four combat tones resolve to four distinct color values with hit/miss in the green family and hurt/dodge in the red family (FLAGGED ASSUMPTION FEED-03 — CSS-distinctness half; on-device legibility is a non-blocking checklist item) (D: Tone palette; FEED-03)"
    - "A shell source-assertion test proves the wiring: the module script imports toastsForAction; no bare `dispatch(` call remains outside the helper; the old switch name has zero occurrences; the cap literal is 4; all seven `data-tone` CSS rules exist with distinct colors; the reduced-motion rule survives (FEED-03/FEED-04 wiring proof)"
    - "The Oracle still receives every narration line for every action exactly as before (the helper logs `html` from dispatch, which 25-02 already formats from the decorated events, so a quipped miss reads the roll first, then the quip) (FEED-05 Oracle half; the Oracle is the record)"
    - "The SUMMARY carries a NON-BLOCKING Pixel 7 checklist (Fridgian hide soak note, Guard credited miss, Wizard refusal names Freeze, Bard camp-wake toast, Pickpocket store note, multi-attack X of N, 3+ foes collapse, level-2 quip vs level-3 plain miss, red vs green legibility, amber refusals, reduced-motion, no layout shift); no device round blocks completion (D: Verification = automated + a device checklist)"
  artifacts:
    - "mazeworld.html — CSS: tone rules for miss/dodge/block/beat (+ unchanged hit/hurt/magic); mzToast cap 4; the module-script helper `dispatchWithToasts(action)`; every call site routed; old switch + spell-name helper removed; equip-rejection literal removed; full-health guards retoned"
    - "test/unit/shell-toast-wiring.test.js — new source-assertion test over mazeworld.html"
    - "25-04-SUMMARY.md — includes the device checklist section"
  key_links:
    - "mazeworld.html module script: import { toastsForAction } from './src/browser/toasts.js' -> dispatchWithToasts(action) -> dispatch(action) (engineAdapter, events already quip-decorated) -> for each toast: window.mzToast(text, tone)"
    - "window.mzToast(text, tone) -> .mw-toast[data-tone=…] CSS rules (all seven tones explicit)"
    - "engineCombatAction / engineMove / inventoryAction / mzBuyItem / mzLeaveStore / mzResolveJoiner / mzMakeCamp / mzAbandon all call dispatchWithToasts instead of dispatch"
  prohibitions:
    - "No engine or presentation-module change in this plan — mazeworld.html and the new test only"
    - "No layout shift: the host stays position:fixed, pointer-events:none, outside the panel flow; no toast may push content"
    - "Reduced-motion handling is preserved: the prefers-reduced-motion rule that disables the in/out animations stays"
    - "No Math.random in the toast path; no toast text authored in the shell (every string comes from toasts.js or the two pre-dispatch guards)"
    - "The Oracle log path (logLine of dispatch html) is not reduced or filtered — toasts are additive feedback"
---

<objective>
Wire the pure toast pipeline into the shell: add the four new tone styles with explicit colors (miss re-coloured to muted moss; dodge, block, beat new), raise the host cap to 4, replace the per-action switch with a thin `dispatchWithToasts(action)` helper that every dispatch call site uses, delete the dead switch/spell-name helper and the DR18 hand-written equip toast, retone the two pre-dispatch guards, and prove the wiring with a source-assertion test. Attach the Pixel 7 checklist to the SUMMARY.

Purpose: this is where FEED-01..06 become visible: every action's events now reach the toast host through one seam, in the you/they/refusal/magic/neutral palette the decisions specify.
Output: modified mazeworld.html; new test/unit/shell-toast-wiring.test.js; a SUMMARY with the non-blocking device checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-03-SUMMARY.md
@src/browser/toasts.js
@src/browser/engineAdapter.js
@test/unit/foe-effect-chip.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- Shell helper (module script scope): `dispatchWithToasts(action) → { state, events, html }` — the single post-dispatch seam that raises toasts.
- CSS: `.mw-toast[data-tone="miss"]` (muted moss, explicit hex), `.mw-toast[data-tone="dodge"]` (muted stamp), `.mw-toast[data-tone="block"]` (amber), `.mw-toast[data-tone="beat"]` (ink neutral); hit/hurt/magic unchanged.
- `window.mzToast` host cap 4.
- Removed: the per-action switch consumer global and its spell-name helper global; the DR18 hand-written equip-rejection toast line.
- New test file: test/unit/shell-toast-wiring.test.js.
- SUMMARY section: "Device checklist (non-blocking, Pixel 7)".
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: CSS tones (explicit colors, no CLS, reduced-motion kept) + host cap 4</name>
  <files>mazeworld.html</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: Tone palette — two families + utilities, every tone explicit; "host's visible cap goes from 3 to 4; existing lifetime formula and reduced-motion handling stay"; Claude's Discretion: amber/muted color values)
    - mazeworld.html: the `:root` palette (~L66-80: --paper/--ink/--ink-soft/--ditto/--stamp/--moss), the DR13 toast CSS block (~L663-687: .mw-toast-host fixed positioning, .mw-toast base, the four data-tone rules, the keyframes, the prefers-reduced-motion rule), the host markup (~L1303-1305), `window.mzToast` (~L2639-2651: the `> 3` cap loop and the lifetime formula)
    - src/browser/toasts.js TONES doc block (the family semantics the colors must express)
  </read_first>
  <action>
    <!-- planner-discipline-allow: ink-soft -->
    1. In the DR13 toast CSS block: keep the host `position:fixed; pointer-events:none` contract and the `.mw-toast` base untouched. Change the `miss` rule to a muted moss (an explicit hex, e.g. a desaturated/darker moss such as #6f8556 — NOT var(--ink-soft) any more) so your misses sit in the green family; add `dodge` = muted stamp (explicit hex, e.g. #a8695e), `block` = amber (explicit hex, e.g. #d99a2b), `beat` = the neutral default made explicit (color var(--ink), border var(--ink-soft)). Each new rule sets `border-color`, `border-left-color` and `color` like the existing ones. Add a one-line comment naming the families (YOU green: hit/miss; THEM red: hurt/dodge; utilities: magic gold, block amber, beat ink). Optionally register the three new colors as `:root` tokens (--moss-soft, --stamp-soft, --amber) and reference them — either way the rules resolve to explicit colors. Keep the `@media (prefers-reduced-motion:reduce)` rule byte-identical.
    2. In `window.mzToast`: raise the visible cap so the host trims to 4 (`host.children.length > 4`). Leave the lifetime formula and the out-animation timing unchanged. Update the DR13 comment above it to say the tone vocabulary now lives in src/browser/toasts.js and the consumer is `dispatchWithToasts` (Task 2).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('mazeworld.html','utf8'); for (const t of ['hit','miss','hurt','dodge','magic','block','beat']) if (!new RegExp('\\\\.mw-toast\\\\[data-tone=\"'+t+'\"\\\\]').test(s)) { console.error('missing tone', t); process.exit(1); } if (!/host\\.children\\.length > 4/.test(s)) process.exit(2); if (!/prefers-reduced-motion:reduce\\)\\{\\.mw-toast,\\.mw-toast\\.out\\{animation:none\\}/.test(s)) process.exit(3); if (/data-tone=\"miss\"\\]\\{[^}]*--ink-soft/.test(s)) process.exit(4);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.mw-toast\[data-tone="dodge"\]' mazeworld.html` == 1 and `grep -c '\.mw-toast\[data-tone="block"\]' mazeworld.html` == 1 and `grep -c '\.mw-toast\[data-tone="beat"\]' mazeworld.html` == 1 and `grep -c '\.mw-toast\[data-tone="miss"\]' mazeworld.html` == 1
    - `grep '\.mw-toast\[data-tone="miss"\]' mazeworld.html | grep -c "ink-soft"` == 0 (miss is no longer grey)
    - `grep -c "host.children.length > 4" mazeworld.html` == 1
    - `grep -c "prefers-reduced-motion:reduce){.mw-toast,.mw-toast.out{animation:none}" mazeworld.html` == 1
    - `grep -c "position:fixed;left:0;right:0;" mazeworld.html` >= 1 (the host's fixed contract line survives)
    - the `<verify>` node one-liner exits 0
  </acceptance_criteria>
  <done>All seven tones have explicit colors in the two families plus utilities, the host shows up to 4 toasts, and the fixed/no-CLS/reduced-motion contract is intact.</done>
</task>

<task type="auto">
  <name>Task 2: dispatchWithToasts helper; route every dispatch call site; delete the old switch, the spell-name helper and the DR18 equip toast; retone the two guards</name>
  <files>mazeworld.html</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: "Hook at the single dispatch seam … If the shell has more than one dispatch call site, route them all through one call"; "The shell's mzCombatFeedback switch is replaced by a thin consumer")
    - mazeworld.html module script imports (~L5459-5482: `import { boot, dispatch, startNewRun, waitForPending } from "./src/browser/engineAdapter.js"` and the other ./src/browser imports — add the toasts.js import beside them)
    - every dispatch call site — grep `dispatch(` and read each: engineMove (~L5840), mzAbandon (~L6184), mzBuyItem (~L6202), mzLeaveStore (~L6209), mzResolveJoiner (~L6229), inventoryAction (~L6247), mzEquipItem (~L6264-6268: the DR18 hand-written toast to delete), engineCombatAction (~L6307 and its toast tail ~L6333-6337), mzMakeCamp (~L6434)
    - the classic-scope consumer block (~L2652-2720: the spell-name helper, the charges helper — KEEP `window.mzSpellCharges`, it is used at ~L6385 — and the per-action switch function to delete entirely with its comment)
    - the two pre-dispatch guards (~L5091 and ~L5381: the "already at full health" potion guards with tone "miss")
    - src/browser/toasts.js toastsForAction signature and return shape
  </read_first>
  <action>
    <!-- planner-discipline-allow: mzCombatFeedback -->
    <!-- planner-discipline-allow: mzSpellName -->
    1. Module script: add `import { toastsForAction } from "./src/browser/toasts.js";` beside the engineAdapter import. Define ONE helper in the module scope, above the first call site, named `dispatchWithToasts(action)`: it calls `dispatch(action)`, then `for (const t of toastsForAction(action.type, result.events, {})) window.mzToast?.(t.text, t.tone);`, and returns the result unchanged (`{ state, events, html }`). Comment it as the Phase 25 single post-dispatch seam (D: every action flows through it — exploration, camp, store, Joiner, inventory, combat), noting the events already carry the quip from engineAdapter's decorateMisses and that html is formatted from the same decorated events, so the Oracle and the toast agree.
    2. Replace every `dispatch(` call in the module script with `dispatchWithToasts(` — engineMove, mzAbandon, mzBuyItem, mzLeaveStore, mzResolveJoiner, inventoryAction, engineCombatAction, mzMakeCamp — leaving each site's render tail (state set, logLine of html, paint, renderEncounter, noteCombat, hapticForEvents, beats synthesis) byte-identical. In engineCombatAction delete the DR13 toast tail (the two lines that called the old consumer and raised its single toast) and its comment — the helper now toasts. In mzEquipItem delete the DR18 hand-written rejection toast (the `if (events.some(equipRejected)) mzToast(...)` block and its comment) so mzEquipItem is again a plain `inventoryAction({ type: "equipItem", i })` — equipRejected now toasts from the table with its reason (woodsman / acrobat / noArmor / tooHeavy / wrongClass) in the block tone.
    3. Classic scope: delete `window.mzCombatFeedback` (the whole switch function and the DR13 comment paragraph describing it) and `window.mzSpellName` (its only consumer was the switch; confirm with grep before deleting). Keep `window.mzSpellCharges` (used by the grimoire renderer). Update the remaining DR13 comment above `window.mzToast` to point at src/browser/toasts.js.
    4. The two pre-dispatch guards ("already at full health" — the only toasts not driven by an engine event, because no dispatch happens) change their tone argument from "miss" to "block": a refusal must never look like a missed swing (D: block tone — "couldn't" never looks like "missed").
    5. Sanity-run in the browser dev loop is optional (no device round blocks completion); the source-assertion test in Task 3 is the gate.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('mazeworld.html','utf8'); const strip=x=>x.replace(/\/\*[\s\S]*?\*\//g,'').replace(/<!--[\s\S]*?-->/g,'').split('\n').map(l=>{const i=l.indexOf('//');return i===-1?l:l.slice(0,i)}).join('\n'); const c=strip(s); const bare=(c.match(/(?<![A-Za-z_$.])dispatch\(/g)||[]).length; const routed=(c.match(/dispatchWithToasts\(/g)||[]).length; console.log({bare, routed}); if (bare!==1) process.exit(1); if (routed<9) process.exit(2); if (/mzCombatFeedback/.test(c)) process.exit(3); if (/mzSpellName/.test(c)) process.exit(4); if (!/import \{ toastsForAction \} from \"\.\/src\/browser\/toasts\.js\"/.test(c)) process.exit(5); if (!/mzSpellCharges/.test(c)) process.exit(6);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { toastsForAction } from "./src/browser/toasts.js";' mazeworld.html` == 1
    - `grep -c "function dispatchWithToasts(action)" mazeworld.html` == 1 and `grep -c "toastsForAction(action.type" mazeworld.html` == 1
    - `grep -c "dispatchWithToasts(" mazeworld.html` >= 9 (the definition + 8 call sites)
    - the `<verify>` one-liner exits 0 and prints `bare: 1` (the only comment-stripped bare `dispatch(` call is inside dispatchWithToasts)
    - `grep -c "mzCombatFeedback" mazeworld.html` == 0
    - `grep -c "mzSpellName" mazeworld.html` == 0
    - `grep -c "window.mzSpellCharges" mazeworld.html` >= 1
    - `grep -c 'You cannot use that.' mazeworld.html` == 0
    - `grep -c 'Already at full health.", "block")' mazeworld.html` == 2
    - `grep -c 'Already at full health.", "miss")' mazeworld.html` == 0
    - `grep -c "window.mzEquipItem = (i) => inventoryAction({ type: \"equipItem\", i });" mazeworld.html` == 1
    - `node --test test/unit/parley-button-mirror.test.js test/unit/foe-effect-chip.test.js test/unit/tutorial.test.js test/unit/save-validation.test.js` exits 0 (the other mazeworld.html source-assertion tests still pass)
  </acceptance_criteria>
  <done>Every dispatch flows through dispatchWithToasts, the old switch and its helper are gone, the hand-written equip toast is gone, the two guards are amber, and the pre-existing shell source tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: shell-toast-wiring.test.js (source assertions) + full suite + device checklist for the SUMMARY</name>
  <files>test/unit/shell-toast-wiring.test.js</files>
  <read_first>
    - test/unit/foe-effect-chip.test.js and test/unit/parley-button-mirror.test.js (the mazeworld.html fs-read + regex source-assertion pattern; comment stripping)
    - mazeworld.html after Tasks 1-2 (the CSS block, mzToast, the module-script helper and call sites)
    - src/browser/toasts.js TONES (the seven tone names — derive the CSS check from the export, never a hand-copied list)
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md `<specifics>` (the device checklist items: Fridgian hide soak, Guard need shift, Wizard refusal names Freeze, Bard camp wake, Pickpocket store markup note, multi-attack X of N)
  </read_first>
  <action>
    <!-- planner-discipline-allow: mzCombatFeedback -->
    1. Create test/unit/shell-toast-wiring.test.js (node:test; read mazeworld.html once; comment-strip for code assertions, raw for CSS):
       - imports `{ TONES }` from src/browser/toasts.js and asserts, for every tone, a `.mw-toast[data-tone="<tone>"]{…}` rule exists and declares `color:`; extract each rule's `color:` value (resolve `var(--x)` through the `:root` block) and assert hit/miss/hurt/dodge resolve to FOUR distinct values, magic/block/beat are distinct from each other and from those four, and the `miss` rule no longer references the ink-soft token (FEED-03 flagged assumption — CSS half).
       - the host cap: the mzToast body contains `host.children.length > 4` and not `> 3`.
       - the reduced-motion rule and the `.mw-toast-host{ position:fixed` contract are present.
       - the module script imports toastsForAction from ./src/browser/toasts.js; `function dispatchWithToasts(action)` exists exactly once; comment-stripped bare `dispatch(` (not preceded by an identifier char, `.` or `$`) occurs exactly once; `dispatchWithToasts(` occurs >= 9 times; the old switch name and spell-name helper name have 0 occurrences (build these two names in the test from string fragments or a regex so the test file itself never spells them — the prohibition on dead names should not be tripped by the test that enforces it); the DR18 equip toast literal has 0 occurrences; the two full-health guards use tone block.
       - a behavioural check: import toastsForAction and assert that the events an equipRejected(reason woodsman) dispatch would produce yield one block toast (proving the deleted hand-written toast is replaced, not lost).
    2. Run `npm test` and the parity suite; both must be green (this plan touched no engine file).
    3. Draft the SUMMARY's "Device checklist (non-blocking, Pixel 7)" section with numbered steps and the exact expected toast strings, each marked as look-for only: (1) roll a Fridgian, take a hit → "X hits you (N) · hide 2 soaked" in red; (2) roll a Guard, get missed on a roll of 5 → "X misses you · Guard" in muted red; (3) roll a Wizard with Freeze, tap Strike → amber "Wizards don't punch. Cast Freeze."; (4) roll a Bard, camp until disturbed → "Camp disturbed · Bard: the singing carried"; (5) roll a Pickpocket, open a store → amber "Shop open · Pickpocket: buys ×1.25, sells ×0.75"; (6) fight a multi-attack foe → one "X hits you K of M (N)" toast, never M toasts; (7) 3+ foes → "3 foes swing, K land (N)"; (8) at level 1-2 a miss reads "You miss X — <quip>" and the Oracle shows the roll then the quip; at level 3 "You miss X"; (9) red vs green is unmistakable at arm's length; (10) refusals are amber, never grey; (11) with Android "Remove animations" on, toasts appear/disappear without motion; (12) no panel shifts when 4 toasts stack. State explicitly that no item blocks completion and that the orchestrator offers a phone push at phase close.
  </action>
  <verify>
    <automated>node --test test/unit/shell-toast-wiring.test.js && npm test && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-toast-wiring.test.js` exits 0 with >= 8 passing tests
    - `grep -c 'from "../../src/browser/toasts.js"' test/unit/shell-toast-wiring.test.js` == 1 and `grep -c "TONES" test/unit/shell-toast-wiring.test.js` >= 2 and `grep -c "dispatchWithToasts" test/unit/shell-toast-wiring.test.js` >= 2
    - `grep -c "mzCombatFeedback" test/unit/shell-toast-wiring.test.js` == 0 (the test builds the dead name indirectly)
    - `npm test` output ends with `# fail 0`
    - `node --test "test/parity/**/*.test.js"` shows `# pass 33` and `git status --porcelain test/parity` prints nothing
    - the SUMMARY contains a heading matching `Device checklist` with >= 10 numbered items and the words "non-blocking"
  </acceptance_criteria>
  <done>The shell wiring is locked by a source test (tones, cap, single seam, dead names gone), the full suite and parity are green, and the SUMMARY carries the Pixel 7 checklist the user can run after the orchestrator's phone-push offer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toast text → DOM | Strings become `textContent` (never innerHTML) in mzToast — kept that way |
| dispatch → shell render | The helper must not change the order or content of the existing render tail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-16 | Tampering | a call site left on bare dispatch (silent action) | medium | mitigate | comment-stripped bare-`dispatch(` count === 1 in the wiring test |
| T-25-17 | Denial of Service | toast flood / layout shift on the fixed host | medium | mitigate | cap 4 + pipeline cap; host stays fixed + pointer-events:none (asserted) |
| T-25-18 | Information Disclosure | HTML injection via toast text | low | mitigate | mzToast keeps `textContent`; assert no innerHTML in mzToast in the wiring test |
| T-25-19 | Elevation of Privilege | a toast intercepting taps (blocking the D-pad) | low | mitigate | pointer-events:none on host and toast (existing, asserted) |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/shell-toast-wiring.test.js` green; the other mazeworld.html source tests (parley-button-mirror, foe-effect-chip, tutorial, save-validation) green.
- `npm test` ends `# fail 0`; parity 33/33 with a clean test/parity tree.
- SUMMARY carries the non-blocking device checklist.
</verification>

<success_criteria>
- Every action in the app toasts through one seam with the seven-tone palette; the old switch is gone; the host holds 4 with no layout shift and reduced-motion respected.
- The wiring is provable from source; the phase's device checklist is written and explicitly non-blocking.
</success_criteria>

<output>
Create `.planning/phases/25-nothing-happens-silently-feature-feedback/25-04-SUMMARY.md` when done — it MUST include the "Device checklist (non-blocking, Pixel 7)" section from Task 3.
</output>
