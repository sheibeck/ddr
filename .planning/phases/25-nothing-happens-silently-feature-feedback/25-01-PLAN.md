---
phase: 25-nothing-happens-silently-feature-feedback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/combat.js
  - engine/derived.js
  - engine/movement.js
  - engine/magic.js
  - engine/items.js
  - src/browser/eventNarration.js
  - test/unit/feedback-payload.test.js
  - test/unit/inventory-actions.test.js
autonomous: true
requirements: [FEED-01, FEED-02, FEED-06]

must_haves:
  truths:
    - "struckByFoe and foeBolted carry an additive `soaked` object ({ hardiness?, hide?, ward? } — only the keys that actually reduced the blow, each the integer amount removed) whenever any passive soak applied, and NO `soaked` key at all for a plain Human with no Hardiness/ward, so test/unit/combat.test.js's pinned key order [type, name, roll, need, dmg, ignoresArmor, critical] still holds byte-for-byte (D: Passive modifiers as additive payload, FEED-01)"
    - "struckByFoe, foeMissed (hero, member and pursuit branches) and memberStruck carry an additive `needMods` array of { name, delta } entries in application order (race name, Acrobat, Agility, Guard, gear, Silence, Mirror Self, invisible, floor, blind, penalty, insulted) whenever at least one modifier changed the base 5, and omit the key entirely when none did; foeToHitBreakdown(state).need === foeToHitVs(state) across the full race x sub x skill x override matrix (D: Passive modifiers — Guard/Agility need shift, FEED-01)"
    - "armorSoaked carries `wear` (the durability actually subtracted this blow, 0 for magic plate or a blow at/under av.min) and `halved: true` only when the Dwarven armorWear fraction applied (D: armor durability reports wear + the Dwarven halving, FEED-01)"
    - "struck carries `need` (the to-hit threshold that swing used — fixing the Oracle's existing 'N vs ?' hole) and `critBy` (one of roll | silence | stealth | backstab | ninja | cutthroat) only when critical is true; struckByFoe carries `soldierCrit: true` only for a Soldier hit on a foe roll of exactly 2 (D: struck carries the crit breakdown where cheap, FEED-01)"
    - "rested carries `doubled` (\"Soldier\" or the race name) only when the camp heal was doubled; potionDrunk carries `doubled` (race name) only for a heal2x race; armorPatched carries `by` (\"Sewing\" | \"Master of Arms\") always (D: rested.doubled for Soldier/Wilmsry, FEED-01)"
    - "readScroll with no scrolls or a non-reader emits a new `scrollRefused` event with reason noScrolls | pilfer | noRunes, draws zero rng and changes no state; itemRejected/equipRejected report reason \"acrobat\" (not wrongClass) when the only bar is the Acrobat dagger-only rule (D: refusals name the reason — Pilfer can't read, Acrobat can't equip, FEED-02)"
    - "Every new/extended field has an EVENT_NARRATION clause in the deadpan voice: 'hits you for 4 (hide soaked 2)', 'needs 4 (Guard −1)', the armour wear + Dwarven halving, the Soldier double, 'Rest restores +16 hp (Soldier, doubled)', the crit reason, and a strikeMissed builder that appends `e.quip` after the roll and the plain miss sentence when the field is present; every unflagged sentence stays byte-identical (FEED-01, FEED-05 Oracle half)"
    - "Refusal Oracle lines precede any other narration for the action because the engine pushes the refusal first and returns (probe FEED-02 ordering — the engine half; the toast half is 25-03)"
    - "No engine RULE changes: parity stays byte-identical — `node --test \"test/parity/**/*.test.js\"` 33/33 with `git status --porcelain test/parity` empty and prototype-master.js.txt untouched; npm test ends `# fail 0` (Engine gate)"
  artifacts:
    - "engine/derived.js — exported foeToHitBreakdown(state) → { need, mods: [{ name, delta }] }; foeToHitVs value-identical"
    - "engine/combat.js — needMods threaded through foeTurn (hero + member branches) and pursuitStrike into struckByFoe/foeMissed/memberStruck; applyFoeDamageToPlayer computes `soaked`, `soldierCrit`, armorSoaked `wear`/`halved`; playerStrike's struck carries need + critBy"
    - "engine/movement.js — rested.doubled, armorPatched.by; engine/magic.js — scrollRefused, potionDrunk.doubled; engine/items.js — exported weaponRefusalReason(c, it) and the acrobat reason at the three weapon-refusal sites"
    - "src/browser/eventNarration.js — scrollRefused entry; extended struckByFoe/foeBolted/foeMissed/memberStruck/armorSoaked/struck/strikeMissed/rested/potionDrunk/armorPatched/itemRejected/equipRejected builders"
    - "test/unit/feedback-payload.test.js — new; test/unit/inventory-actions.test.js — the one Acrobat reason pin updated"
  key_links:
    - "foeToHitBreakdown(state).mods -> foeTurn/pursuitStrike post-mods (blind/penalty/insulted) -> event.needMods -> EVENT_NARRATION 'needs N (…)' clause and (25-02/25-03) the toast's credited-miss note"
    - "applyFoeDamageToPlayer soak chain (Hardiness -> hide -> pendant -> ward -> armour) -> struckByFoe.soaked / foeBolted.soaked / armorSoaked.wear -> narration clauses -> (25-02) toast soak note"
    - "readScroll guard -> scrollRefused -> EVENT_NARRATION (coverage guard requires it) -> (25-02) TOAST_FOR.scrollRefused block tone"
  prohibitions:
    - "No engine rule change: every edit is a read of values already computed, an extra event field, or a new event pushed on an existing early-return path"
    - "No new rng draw anywhere; if a payload would need a draw, drop the payload"
    - "No new serialized state field on state, state.c or state.combat; comparables.js untouched"
    - "Never edit test/parity/prototype-master.js.txt; zero parity fixture changes; no fixture regeneration"
    - "Additive fields are emitted only when applicable (conditional spread) so no existing event-shape pin in test/unit needs rewriting, except the single Acrobat reason string"
    - "Narration copy stays sarcastic, deadpan, family-friendly — test/voice/safety-scan.test.js is the gate"
---

<objective>
Make every passive class/race/skill modifier and every refusal self-describing in the events the engine already emits, as ADDITIVE payload only: what soaked a blow (Hardiness/hide/ward), why a foe needed a different number to hit (Guard, Agility, race, gear, mirror…), how much armour durability a soaked blow really cost (and the Dwarven halving), why a crit was a crit, why a camp heal doubled, why a scroll would not read, why a weapon would not equip — and narrate every one of them in the Oracle. This is the engine half of "nothing happens silently"; 25-02/25-03 read these fields to build toasts.

Purpose: FEED-01 (features that fire say what and why), FEED-02 (refusals say why — Pilfer can't read, Acrobat can't equip), FEED-06 (the soak/need details make both directions legible). The Oracle keeps the roll and the record; parity stays byte-identical because every field is computed from values the engine already has and no draw is added.
Output: modified engine/combat.js, engine/derived.js, engine/movement.js, engine/magic.js, engine/items.js, src/browser/eventNarration.js; new test/unit/feedback-payload.test.js; one pin updated in test/unit/inventory-actions.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md
@engine/combat.js
@engine/derived.js
@engine/movement.js
@engine/magic.js
@engine/items.js
@src/browser/eventNarration.js
@test/unit/combat.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New exported helper: `foeToHitBreakdown(state)` in engine/derived.js → `{ need, mods }`, `mods` = `[{ name, delta }]` in application order; `need` value-identical to `foeToHitVs(state)`.
- New exported helper: `weaponRefusalReason(c, it)` in engine/items.js → `"wrongClass" | "acrobat" | null` (mirrors `armorRefusalReason`).
- New event type: `scrollRefused` (`{ type, reason: "noScrolls" | "pilfer" | "noRunes" }`), zero draws, no state change, with an EVENT_NARRATION entry.
- New additive event fields (all conditional unless noted): `struckByFoe.soaked` / `foeBolted.soaked` (`{ hardiness?, hide?, ward? }`), `struckByFoe.needMods` / `foeMissed.needMods` / `memberStruck.needMods` (`[{ name, delta }]`), `struckByFoe.soldierCrit`, `armorSoaked.wear` (always) + `armorSoaked.halved` (conditional), `struck.need` (always) + `struck.critBy` (conditional), `rested.doubled`, `potionDrunk.doubled`, `armorPatched.by` (always).
- New reason string: `itemRejected` / `equipRejected` `reason: "acrobat"`.
- Presentation-only field read by narration: `strikeMissed.quip` (set by 25-02's decorateMisses, never by the engine) — the strikeMissed builder appends it when present.
- New test file: test/unit/feedback-payload.test.js.
- No new RACES flags, no new serialized fields, no comparables.js change, no fixture change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: foeToHitBreakdown + needMods on every foe swing event; soaked / soldierCrit / armour wear in applyFoeDamageToPlayer; struck.need + critBy in playerStrike</name>
  <files>engine/derived.js, engine/combat.js</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: "Passive modifiers surface inside the event they affect, as ADDITIVE payload fields"; Scope guard; Claude's Discretion on the split across applyFoeDamageToPlayer/foeTurn/playerStrike)
    - engine/derived.js foeToHitVs (~L337-350: base 5, R.foeToHit, Acrobat 3, Agility -1, Guard -1, eff foeToHit, Silence-in-dark 1, mirror 1, invis 1, Math.max(1, h))
    - engine/combat.js: applyFoeDamageToPlayer (~L1189-1298: Hardiness -3 floor 1, R.hide floor 1, halfNext, ward absorb/reflect, armour soak roll with R.armorWear, the struckByFoe/foeBolted pushes), foeTurn (~L1309-1461: member branch mNeed/mRoll with blind/foeToHitPenalty/parleyInsulted post-mods, hero branch need/roll, the Soldier roll <= 2 doubling), pursuitStrike (~L596-617: same post-mods), playerStrike (~L368-518: crit/noCrit, opener block silence/stealth/backstab, Ninja roll <= 2, Cutthroat C.cut, the struck push without need)
    - test/unit/combat.test.js ~L554-566 (the struckByFoe key-order pin that MUST keep passing unchanged) and ~L1230-1245 (the foeBolted exact-shape pins)
    - test/unit/foe-turn-draw-count.test.js header (FID-02 pins: draw counts must not move)
  </read_first>
  <action>
    All edits carry a `// Phase 25 (FEED-01, additive payload): ...` comment. engine/combat.js uses CRLF line endings — keep them. Zero new rng draws; every value below is already computed or is a pure read.

    1. engine/derived.js — add and export `foeToHitBreakdown(state)`. Reproduce foeToHitVs's steps in the same order, recording an entry `{ name, delta }` for every step whose value actually changed `h` (delta = after − before, so an override such as Acrobat's `h = 3` records `3 - hBefore`): race step name = `c.race` (only when `R.foeToHit` is truthy), then `"Acrobat"`, `"Agility"`, `"Guard"`, `"gear"` (the `eff(c, "foeToHit")` sum, only when non-zero), `"Silence"` (dark + Silence), `"Mirror Self"`, `"invisible"`, and finally `"floor"` when `Math.max(1, h)` raised it. Return `{ need, mods }`. Then make foeToHitVs value-identical by EITHER leaving its body untouched (preferred — zero risk) OR rewriting it as `return foeToHitBreakdown(state).need;` — either way Task 3's matrix test and the parity suite prove equality. JSDoc: state that this is a narration breakdown, pure, zero draws, and that the two functions must never diverge.

    2. engine/combat.js foeTurn + pursuitStrike — beside each existing `let need = foeToHitVs(state);` / `let mNeed = foeToHitVs(state);` line (three sites: member branch, hero branch, pursuitStrike) build `const needMods = foeToHitBreakdown(state).mods.slice();` and, in the SAME order as the existing post-mods, push `{ name: "blind", delta: 1 - need }` when `f.blind` changed it, `{ name: "penalty", delta: <capped - previous> }` when `C.foeToHitPenalty` lowered it, and `{ name: "insulted", delta: 1 }` when `C.parleyInsulted` raised it (only entries with a non-zero delta). Leave the existing need arithmetic lines byte-identical. Attach `...(needMods.length ? { needMods } : {})` as the LAST key of every `foeMissed` push (3 sites) and the `memberStruck` push, and pass `needMods` into applyFoeDamageToPlayer's options object (`{ dmg, roll, need, needMods }`) from foeTurn's hero branch and pursuitStrike. Import foeToHitBreakdown from ./derived.js beside foeToHitVs.

    3. engine/combat.js applyFoeDamageToPlayer — destructure the new optional `needMods` from the options object. Build `const soaked = {};` and record integer reductions as they happen: after the Hardiness line `if (before - dmg > 0) soaked.hardiness = before - dmg` (capture `before` on the line above; same pattern for `R.hide` → `soaked.hide`), and inside the ward block `if (warded > 0) soaked.ward = warded`. In the armour block compute `const wear = (!av.magic && dmg > av.min) ? <the exact amount subtracted, i.e. Math.min(previous armorWP, R.armorWear ? Math.ceil(dmg * R.armorWear) : dmg)> : 0` WITHOUT changing the existing `c.armorWP = Math.max(0, ...)` expression, and push `armorSoaked` as `{ type, name, amount: blocked, wear, ...(R.armorWear && wear > 0 ? { halved: true } : {}) }`. Extend the two final pushes with trailing conditional spreads, in this order after the existing keys: `...(Object.keys(soaked).length ? { soaked } : {})`, `...(needMods && needMods.length ? { needMods } : {})` (struckByFoe only), `...(roll === 2 && c.sub === "Soldier" ? { soldierCrit: true } : {})` (struckByFoe only). foeBolted gets only the `soaked` spread. Because every spread is conditional, a plain hero's struckByFoe/foeBolted keeps today's exact key set (the combat.test.js pins).

    4. engine/combat.js playerStrike — declare `let critBy = null;` next to `let crit = roll === 1 && !noCrit;` and set `critBy = "roll"` when that initial crit is true; in the opener block set `"silence"` / `"stealth"` / `"backstab"` beside each `crit = true`; `"ninja"` for the Ninja roll <= 2 line; `"cutthroat"` for the Cutthroat `C.cut` block (later assignments overwrite earlier ones — the most specific reason wins, matching the code order). Extend the struck push to `{ type: "struck", target: t.name, roll, need, dmg: landed.applied, critical: crit, ...(crit && critBy ? { critBy } : {}) }` — `need` is the swing's own `need` local (always present: it fixes the Oracle's 'N vs ?' hole for struck; no test pins struck's key set — confirm with `grep -rn 'Object.keys(struck' test/`).
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/foe-turn-draw-count.test.js test/unit/identity-combat.test.js test/unit/identity-race.test.js test/unit/party-combat.test.js test/unit/foe-abilities.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function foeToHitBreakdown" engine/derived.js` == 1
    - `grep -c "foeToHitBreakdown" engine/combat.js` >= 4 (import + three swing sites)
    - `grep -c "needMods" engine/combat.js` >= 8 (three foeMissed pushes, memberStruck, two options objects, the destructure and the struckByFoe spread)
    - `grep -c "soaked.hardiness" engine/combat.js` == 1 and `grep -c "soaked.hide" engine/combat.js` == 1 and `grep -c "soaked.ward" engine/combat.js` == 1
    - `grep -c "soldierCrit: true" engine/combat.js` == 1
    - `grep -c "halved: true" engine/combat.js` == 1 and `grep -c "wear," engine/combat.js` >= 1
    - `grep -c 'critBy = "cutthroat"' engine/combat.js` == 1 and `grep -c 'critBy = "backstab"' engine/combat.js` == 1 and `grep -c 'critBy = "ninja"' engine/combat.js` == 1
    - `grep -c 'type: "struck", target: t.name, roll, need,' engine/combat.js` == 1
    - `node --test test/unit/combat.test.js` exits 0 with the "key order pinned" test and both foeBolted exact-shape tests UNCHANGED and passing
    - `node --test test/unit/foe-turn-draw-count.test.js` exits 0 (FID-02 draw pins unmoved)
    - `node --test "test/parity/**/*.test.js"` exits 0 (33 pass) and `git status --porcelain test/parity` prints nothing
  </acceptance_criteria>
  <done>foeToHitBreakdown exists and agrees with foeToHitVs; every foe swing event names its need modifiers; struckByFoe/foeBolted report what soaked the blow; armorSoaked reports real wear and the Dwarven halving; struck reports need and the crit reason; plain-hero event shapes and every parity fixture are byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: rested.doubled / potionDrunk.doubled / armorPatched.by, scrollRefused, weaponRefusalReason("acrobat"), and every Oracle clause in EVENT_NARRATION</name>
  <files>engine/movement.js, engine/magic.js, engine/items.js, src/browser/eventNarration.js, test/unit/inventory-actions.test.js</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: refusal list incl. `scrollRefused`/canRead false (Pilfer, no Runes), itemRejected/equipRejected by reason incl. acrobat; "rested (soldier x2 — add a doubled flag)", "armorPatched(masterOfArms/sewing)")
    - engine/movement.js newDay (~L405-490: `heal *= 2` for R.heal2x || Soldier, the rested push, the Sewing / Master of Arms armorPatched pushes)
    - engine/magic.js readScroll (~L440-460: the combined `if (!c.scrolls || !canRead(state)) return events;` guard) and drinkPotion (~L410-420: heal2x doubling)
    - engine/items.js canEquipWeapon (~L188-194), armorRefusalReason (~L208-213, the helper shape to mirror), takeItem's two weapon `wrongClass` sites (~L243, ~L278) and equipItem's (~L409)
    - src/browser/eventNarration.js: header conventions (every builder survives a bare `{ type }`; dice inside `<span class="roll">`), the struckByFoe/foeBolted/foeMissed/memberStruck/armorSoaked/struck/strikeMissed/rested/potionDrunk/armorPatched/itemRejected/equipRejected/scrollRead builders
    - test/unit/inventory-actions.test.js ~L178-182 (the Acrobat equip test whose reason pin changes to "acrobat")
    - test/unit/formatEventsCoverage.test.js (the new scrollRefused type must have an entry or the guard fails)
  </read_first>
  <action>
    1. engine/movement.js newDay: capture whether the camp heal doubled (`const doubledBy = c.sub === "Soldier" ? "Soldier" : R.heal2x ? c.race : null;` — the Soldier label wins for a Wilmsry Soldier; the `heal *= 2` line itself is untouched) and push rested as `{ type: "rested", amount: c.wp - before, ...(doubledBy ? { doubled: doubledBy } : {}) }`. Push armorPatched with `by: "Sewing"` in the tier branch and `by: "Master of Arms"` in the Master of Arms branch (always present — no test pins armorPatched's key set; confirm with grep).
    2. engine/magic.js: readScroll — split the combined guard into two early returns that each push one event before returning: no scrolls → `{ type: "scrollRefused", reason: "noScrolls" }`; `!canRead(state)` → `{ type: "scrollRefused", reason: c.sub === "Pilfer" ? "pilfer" : "noRunes" }`. Both sit BEFORE `c.scrolls--` and the rng.pick, so zero draws and no mutation. drinkPotion — push potionDrunk with `...(RACES[c.race].heal2x ? { doubled: c.race } : {})` after `remaining`.
    3. engine/items.js: add and export `weaponRefusalReason(c, it)` beside armorRefusalReason: returns `null` when canEquipWeapon is true; otherwise `"acrobat"` when the ONLY failing clause is the Acrobat dagger rule (the class letter IS in `WEAPONS[it.base].cls`), else `"wrongClass"`. Use it at the three weapon-refusal sites (takeItem x2, equipItem x1) so the pushed `reason` is the helper's result; canEquipWeapon's own body stays byte-identical. Update test/unit/inventory-actions.test.js's "REJECTS an Acrobat wielding a non-dagger" assertion to expect reason "acrobat" (the Magic-User-with-a-Broadsword test above it still expects wrongClass).
    4. src/browser/eventNarration.js — add `scrollRefused` (reason pilfer: a Pilfer's hands know locks, not letters — the scroll stays rolled; noRunes: the runes mean nothing to you; noScrolls: you have no scroll to read; unknown reason: the generic "it stays rolled" line — all inside `<span class="miss">`). Extend builders with clauses that render ONLY when the new field is present, so every existing sentence is byte-identical when the field is absent:
       - `struckByFoe`: after the hp figure append a parenthetical listing `soaked` parts in voice, e.g. `(hide soaked 2)` / `(Hardiness soaked 3, ward soaked 4)`; render the roll clause as `<span class="roll">7</span> vs 4` and, when `needMods` is present, append inside the roll span `(needs 4: Guard −1)` with every mod as `name ±delta` joined by commas; `soldierCrit` renders `Critical! ` exactly like `critical` (the Soldier's roll of 2). `foeBolted`: same soaked parenthetical. `foeMissed` and `memberStruck`: the same needMods clause inside their roll spans.
       - `armorSoaked`: when `wear` is present append ` It costs the armour ${wear}.` and when `halved` add a Dwarven clause (built to be hit, half the wear).
       - `struck`: when `critBy` is present replace the bare `Critical!` with a reasoned one — roll: `Critical!`, silence: `Silent. Critical!`, stealth: `Unseen. Critical!`, backstab: `From behind. Critical!`, ninja: `A Ninja's two. Critical!`, cutthroat: `The Cutthroat's first blow. Critical!` (exact wording discretionary, deadpan, family-friendly).
       - `strikeMissed`: append ` ${e.quip}` after the existing "You miss X." sentence when `e.quip` is a non-empty string (the roll span stays first — probe FEED-05: the Oracle keeps the roll before the quip). Untouchable branch unchanged.
       - `rested`: when `doubled` is present append ` (${doubled}: twice as fast, as promised.)`; `potionDrunk`: same shape; `armorPatched`: when `by` present prefix with who did the mending.
       - `itemRejected` / `equipRejected`: add an `"acrobat"` reason clause ("An Acrobat carries a dagger. A dagger. That is the whole list.") ahead of the existing generic lines, which stay byte-identical.
       Every builder must still render from a bare `{ type }` (guard with `??`/`?.`), and the voice scan must stay green.
  </action>
  <verify>
    <automated>node --test test/unit/movement.test.js test/unit/magic.test.js test/unit/items.test.js test/unit/inventory-actions.test.js test/unit/identity-world.test.js test/unit/identity-contract.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/engineAdapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'doubled: doubledBy' engine/movement.js` == 1 and `grep -c 'by: "Sewing"' engine/movement.js` == 1 and `grep -c 'by: "Master of Arms"' engine/movement.js` == 1
    - `grep -c '"scrollRefused"' engine/magic.js` == 2 (noScrolls + the pilfer/noRunes push) and `grep -v '^\s*//' engine/magic.js | grep -c 'if (!c.scrolls || !canRead(state)) return events;'` == 0
    - `grep -c 'doubled: c.race' engine/magic.js` == 1
    - `grep -c "export function weaponRefusalReason" engine/items.js` == 1 and `grep -c '"acrobat"' engine/items.js` >= 1 and `grep -c "weaponRefusalReason(c, it)" engine/items.js` >= 3
    - `grep -c 'reason === "acrobat"' test/unit/inventory-actions.test.js` == 1
    - `grep -c "  scrollRefused:" src/browser/eventNarration.js` == 1 and `grep -c "e.soaked" src/browser/eventNarration.js` >= 2 and `grep -c "e.needMods" src/browser/eventNarration.js` >= 3 and `grep -c "e.critBy" src/browser/eventNarration.js` >= 1 and `grep -c "e.quip" src/browser/eventNarration.js` >= 1 and `grep -c "e.doubled" src/browser/eventNarration.js` >= 2 and `grep -c "e.halved" src/browser/eventNarration.js` >= 1 and `grep -c '"acrobat"' src/browser/eventNarration.js` >= 2
    - `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js` exits 0 (scrollRefused narrated; no dead entries; voice clean)
    - `node --test test/unit/movement.test.js test/unit/magic.test.js test/unit/items.test.js test/unit/inventory-actions.test.js test/unit/identity-world.test.js test/unit/identity-contract.test.js` exits 0
  </acceptance_criteria>
  <done>rested/potionDrunk/armorPatched say who doubled or mended, readScroll refuses out loud with a reason, the Acrobat's dagger rule has its own reason string, and every new field has a deadpan Oracle clause with the unflagged sentences unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: feedback-payload tests (matrix equality, every field present/absent, zero-draw refusal, narration clauses) + full suite and parity proof</name>
  <files>test/unit/feedback-payload.test.js</files>
  <read_first>
    - test/unit/combat.test.js helpers (~L47-112: fakeRng, fixedFighter, fixedFloor, fixedState, fixedFoe, fixedCombat — copy the shapes, do not import) and its applyFoeDamageToPlayer section (~L554-700) for the direct-call idiom
    - test/unit/identity-race.test.js (Fridgian hide / Dwarven armorWear scenarios — reuse the setups), test/unit/identity-combat.test.js (Guard foeToHitVs cases), test/unit/identity-world.test.js (Pilfer useItem / canRead setups), test/unit/casters-can-act.test.js (countingRng idiom for zero-draw proofs)
    - engine/derived.js foeToHitVs + foeToHitBreakdown (Task 1)
    - src/browser/eventNarration.js (Task 2's clauses — assert on the rendered strings)
  </read_first>
  <action>
    Create test/unit/feedback-payload.test.js (node:test, local helper copies) covering, at minimum:
    1. foeToHitBreakdown equality matrix: for race in Object.keys(RACES) x sub in ["Soldier", "Guard", "Acrobat", "Wizard"] x Agility {0,1} x { mirror: 0|1 } x { invis: 0|1 } x dark+Silence {off,on}: `foeToHitBreakdown(state).need === foeToHitVs(state)`; mods sum to `need - 5` (deltas are integers; the sum of deltas equals need minus the base 5); a plain Human Soldier yields `mods.length === 0`; a Guard yields exactly one entry `{ name: "Guard", delta: -1 }`; a Guard with Agility yields two entries in the order Agility then Guard.
    2. needMods on events: run foeTurn for a Guard hero vs one foe with a scripted miss → the foeMissed event carries `needMods` deep-equal `[{ name: "Guard", delta: -1 }]`; a plain Human Soldier's foeMissed has NO `needMods` key (`assert.equal("needMods" in e, false)`); a landed hit on a Guard carries needMods on struckByFoe; the member branch (party ally present, scripted pick) carries needMods on memberStruck/foeMissed; pursuitStrike's foeMissed carries it; `C.parleyInsulted` adds `{ name: "insulted", delta: 1 }` as the last entry.
    3. soaked: applyFoeDamageToPlayer for a Fridgian (hide 2) with `{ dmg: 5, roll: 3, need: 5 }` → struckByFoe.soaked deep-equal `{ hide: 2 }` and dmg 3; a Hardiness hero → `{ hardiness: 3 }`; a Fridgian with Hardiness and dmg 10 → `{ hardiness: 3, hide: 2 }`; a ward pool 4 vs dmg 9 → `{ ward: 4 }`; the plain hero → no `soaked` key AND `Object.keys(struck)` still deep-equal `["type", "name", "roll", "need", "dmg", "ignoresArmor", "critical"]`; the ability path (`{ dmg: 6, ability: "krupkeFreeze" }`) on a Fridgian → foeBolted.soaked `{ hide: 2 }`.
    4. soldierCrit: struckByFoe carries `soldierCrit: true` for a Soldier with roll 2, absent for roll 1 (plain `critical: true`) and absent for a Guard with roll 2.
    5. armorSoaked: a Dwarven hero in armour (av.ar > 0, armorWP 10, min 0) with a scripted soak roll and dmg 5 → `wear: 3, halved: true`; a Human with the same setup → `wear: 5` and no `halved` key; magic plate (Cloak of Armor / av.magic) → `wear: 0`.
    6. struck: a landed strike carries `need` equal to `toHit(state)` for the hero and `critBy` absent when not critical; a Thief's opening backstab strike carries `critBy: "backstab"`; a Cutthroat's first landed blow in heavy-armour setup carries `critBy: "cutthroat"`; a Ninja's later roll of 2 carries `critBy: "ninja"`; a plain roll of 1 carries `critBy: "roll"`.
    7. rested/potionDrunk/armorPatched: newDay for a Soldier → rested.doubled === "Soldier"; a Wilmsry → "Wilmsry"; a Human → no `doubled` key; drinkPotion for a Wilmsry → potionDrunk.doubled === "Wilmsry", Human → absent; a Master of Arms with damaged armour → armorPatched.by === "Master of Arms"; a Sewing-tier hero → "Sewing".
    8. scrollRefused: readScroll on a Pilfer with scrolls 1 → exactly `[{ type: "scrollRefused", reason: "pilfer" }]`, `c.scrolls` still 1, countingRng draws 0; a Fighter without Runes/Signs → reason "noRunes"; a Magic User with scrolls 0 → reason "noScrolls"; a Magic User with a scroll still reads (scrollRead present, no scrollRefused).
    9. weaponRefusalReason: Acrobat + Long Sword → "acrobat"; Magic User + Broadsword → "wrongClass"; Acrobat + Dagger → null; takeItem/equipItem for the Acrobat push reason "acrobat".
    10. Narration clauses: `EVENT_NARRATION.struckByFoe({ type, name: "Dante", roll: 3, need: 4, dmg: 4, soaked: { hide: 2 }, needMods: [{ name: "Guard", delta: -1 }] })` contains "hide soaked 2" and "Guard" and "−1" (or "-1"); the same call WITHOUT soaked/needMods equals the pre-phase string (pin the literal `<span class="roll">3</span> vs 4. Dante hits you for <span class="hurt">4 hp</span>.`); `strikeMissed({ type, target: "Dante", roll: 7, need: 5, quip: "Wide. Impressively wide." })` matches `/<span class="roll">7<\/span> vs 5\. .*You miss Dante\.<\/span> Wide\. Impressively wide\.$/` and without `quip` ends exactly at the miss sentence; `rested({ type, amount: 16, doubled: "Soldier" })` contains "Soldier"; `scrollRefused({ type, reason: "pilfer" })` mentions Pilfer; `scrollRefused({ type })` is a non-empty string; `equipRejected({ type, reason: "acrobat", item: { n: "Long Sword" } })` mentions dagger.
    Then run the full suite and the parity suite and confirm zero fixture drift.
  </action>
  <verify>
    <automated>node --test test/unit/feedback-payload.test.js && npm test && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/feedback-payload.test.js` exits 0 with >= 25 passing tests
    - `grep -c "foeToHitBreakdown" test/unit/feedback-payload.test.js` >= 3 and `grep -c '"needMods" in' test/unit/feedback-payload.test.js` >= 1 and `grep -c "soldierCrit" test/unit/feedback-payload.test.js` >= 2 and `grep -c "scrollRefused" test/unit/feedback-payload.test.js` >= 3 and `grep -c "critBy" test/unit/feedback-payload.test.js` >= 4
    - `grep -c 'Object.keys(struck)' test/unit/feedback-payload.test.js` >= 1 (the plain-hero key set re-pinned here too)
    - `npm test` output ends with `# fail 0` and `# pass` >= 1213 (1188 baseline + at least 25 new)
    - `node --test "test/parity/**/*.test.js"` output shows `# pass 33` and `# fail 0`
    - `git status --porcelain test/parity` prints nothing and `git diff --stat -- test/parity/prototype-master.js.txt` prints nothing
    - `grep -rn "Math.random\|Date.now" engine/combat.js engine/derived.js engine/movement.js engine/magic.js engine/items.js | grep -v "^\S*:\s*//"` prints nothing new (engine purity gate, test/determinism/rng-no-math-random.test.js still green)
  </acceptance_criteria>
  <done>Every additive field is proven present when applicable and absent otherwise, foeToHitBreakdown never disagrees with foeToHitVs, scrollRefused draws nothing, the Oracle clauses render as specified, npm test is fully green and every parity fixture is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events → presentation | Event payloads cross from the pure rules engine into narration/toast builders; a raw engine identifier or a thrown builder is the failure mode |
| engine → parity fixtures | Any accidental state or rng change here shows up as a fixture diff |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Tampering | foeToHitVs / foeToHitBreakdown drift | high | mitigate | Task 3 matrix equality test + parity suite; foeToHitVs body left untouched by default |
| T-25-02 | Tampering | conditional spreads breaking pinned event shapes | medium | mitigate | plain-hero key-order pin re-asserted in feedback-payload.test.js; combat.test.js pins unchanged |
| T-25-03 | Information Disclosure | engine identifiers (ability ids, reason strings) leaking into Oracle copy | low | mitigate | narration clauses map reasons/mod names to voiced text; voice scan renders every branch |
| T-25-04 | Denial of Service | a narration builder throwing on a bare `{ type }` and breaking the render loop | medium | mitigate | every new clause guarded with `??`/`?.`; coverage guard calls each builder bare |
| T-25-05 | Tampering | scrollRefused accidentally mutating scrolls or drawing rng | medium | mitigate | countingRng zero-draw + unchanged `c.scrolls` assertions |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero runtime deps policy) |
</threat_model>

<verification>
- `node --test test/unit/feedback-payload.test.js` green (>= 25 tests).
- `npm test` ends `# fail 0`; `node --test "test/parity/**/*.test.js"` 33/33; `git status --porcelain test/parity` empty.
- test/unit/combat.test.js's struckByFoe key-order pin and both foeBolted exact-shape pins pass unchanged.
- test/unit/formatEventsCoverage.test.js and test/voice/safety-scan.test.js green (scrollRefused narrated, no dead entries, voice clean).
</verification>

<success_criteria>
- Every passive modifier that affected a blow, a to-hit number, an armour wear, a crit, a camp heal or a potion is present as an additive field on the event it affected — and absent when it did not apply.
- Every refusal in this plan's scope (scroll read, Acrobat weapon) names its reason.
- The Oracle narrates every new field in voice; unflagged sentences are byte-identical.
- Zero engine rule changes, zero new draws, zero serialized fields, zero fixture changes.
</success_criteria>

<output>
Create `.planning/phases/25-nothing-happens-silently-feature-feedback/25-01-SUMMARY.md` when done (list every additive field and reason string landed, with the exact key names, so 25-02/25-03 executors can read them from one place).
</output>
