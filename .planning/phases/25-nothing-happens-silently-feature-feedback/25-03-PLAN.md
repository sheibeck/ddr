---
phase: 25-nothing-happens-silently-feature-feedback
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - src/browser/toasts.js
  - test/unit/toastsForAction.test.js
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04, FEED-06]

must_haves:
  truths:
    - "toastsForAction(type, events, ctx) is a pure function exported from src/browser/toasts.js that maps a dispatched action's events through TOAST_FOR, aggregates, prioritises, dedupes per event type, caps at MAX_TOASTS (4), and returns an array of { text, tone, priority } — the same input always yields a deepEqual output; an empty or non-array events list yields [] (D: Toast architecture — several toasts per action, prioritised and capped; probe FEED-01 empty)"
    - "Toast order within an action is deterministic: refusals/blocks (priority 0) → your outcome (1) → enemy outcome (2) → feature call-outs (3) → other (4); ties keep engine event order; the cap of 4 drops the lowest-priority (highest number) toasts first, later engine order first within a priority; asserted over a mixed synthetic event list (probe FEED-01 ordering)"
    - "When one action fires two feature events of equal priority (backstab AND a Cutthroat crit on the same blow; frenzy AND a Fridgian hide soak in the same round) both toasts are emitted in engine event order, deduplicated per event type per action, never merged into a wrong sentence (probe FEED-01 adjacency)"
    - "A refused action produces exactly one block toast and no outcome toast, per refusal type (strikeRefused, fleeRefused, parleyRefused, itemRejected, equipRejected, useRefused, scrollRefused, noChargesLeft, spellAboveLevel, campFailed, joinerRefused, buyFailed); withdrawalDenied/vanishDenied are block toasts that precede the flee-chain outcome toast in the same action (probe FEED-02 adjacency)"
    - "Refusal toasts are always first in the action's toast order (priority 0) and are never dropped by the cap even when 5+ other toasts compete (probe FEED-02 ordering)"
    - "Enemy-round aggregation thresholds are exact and grouped by foe name: 1 swing → `${name} hits you (${dmg})` / `${name} misses you` (no 'of'); 2+ swings → `${name} hits you ${K} of ${M} (${sum})` with K >= 1, or `${name} misses you ${M} times` when K = 0; 1–2 distinct foe names → one toast per foe; 3+ distinct names → the single collapsed toast `${F} foes swing, ${K} land (${sum})` (or `${F} foes swing, none land`), tone hurt when K > 0 else dodge, ` · CRIT` appended when any hit was critical/soldierCrit, the soak note summed per key across the group (D: Enemy rounds aggregate per foe; 3+ foes collapse; FEED-04; probe FEED-04 boundary)"
    - "Damage totals in aggregated toasts are integer sums of the events' dmg fields (no rounding); K/M derive from event counts, never from foe.sp.atk (probe FEED-04 precision)"
    - "Your multi-attack rounds mirror the wording, grouped by target: 1 swing → `You hit ${t} (${dmg})` / `You miss ${t}` (+ ` — ${quip}` when present); 2+ swings → `You hit ${t} ${K} of ${M} (${sum})` + ` · CRIT`, or `You miss ${t} ${M} times` (+ the first quip) when K = 0 (D: Your multi-attack rounds mirror the wording, FEED-04)"
    - "Every enemy-outcome toast has tone in {hurt, dodge} and text starting with the foe name or `${F} foes`; every player-outcome toast has tone in {hit, miss} and text starting with 'You' (FLAGGED ASSUMPTION FEED-03 — visual/wording contract; the CSS-distinctness half lands in 25-04; Pixel 7 legibility check is non-blocking) (FEED-03)"
    - "A spell that both hits and kills (spellHit followed by frozenSolid or foeKilled for the same target in the same action) produces one combined toast (`Freeze — Dante frozen solid`, or the hit toast with ` · felled`), not two; a resisted spell produces the resist toast and no hit toast; a resistFailed followed by its effect event yields only the effect toast (probe FEED-06 adjacency)"
    - "Direction is encoded in tone family, not order: your spell outcomes are magic/hit/miss family, foe ability outcomes are hurt/dodge family; when both occur in one action (your cast, then the foe's turn) your outcome toasts before theirs (priority 1 before 2) (probe FEED-06 ordering)"
    - "encounterStarted plus the events that follow it in the same action (trackingRolled, trackable, allyJoined, warlockBoost, foeFled reason knight/conArtist, foeBored, phobiaFrozen, combatInDark) compose into ONE encounter toast; the same events without an encounterStarted in the action fall back to their own builders (D: One combined encounter-start toast)"
    - "Chains fold their roll detail into the outcome: fleeRolled+fled/fleeFailed → one toast carrying `(${roll}+${bonus} vs ${need})`; parleyRolled+goldGained(why parley)/parleyFailed/beastsSoothed → one toast with the roll; chestLockRolled+chestOpened/chestLocked → one toast with the roll; foeKilled right after struck/spellHit for the same target folds into that toast as ` · felled` (D: Keep toasts short; the Oracle carries the roll)"
  artifacts:
    - "src/browser/toasts.js — toastsForAction(type, events, ctx = {}) plus internal aggregators (enemyRound, yourRound, spellChain, fleeChain, parleyChain, chestChain, encounterStart, killFold) and the pipeline (aggregate → map remaining → dedupe per type → stable sort by priority → cap)"
    - "test/unit/toastsForAction.test.js — new"
  key_links:
    - "engineAdapter.dispatch (decorated events) -> (25-04 shell) toastsForAction(action.type, events, {}) -> window.mzToast(text, tone) per returned toast"
    - "aggregators reuse TOAST_FOR builders for single-event groups so the locked single-event wordings from 25-02 are the 1-swing case here (one source of truth)"
    - "foeMissed's credited-mod note and struckByFoe's soak note come from the 25-01 fields (needMods, soaked, soldierCrit) via the 25-02 builders"
  prohibitions:
    - "No Math.random, Date.now, DOM access or engine import in toasts.js — toastsForAction is pure and replay-stable"
    - "Never more than MAX_TOASTS (4) toasts per action; never zero toasts for an action that carried a refusal, an outcome, or a feature event"
    - "Never show four 'hits you' toasts for one round — per-foe aggregation is mandatory, not optional"
    - "The Oracle never loses information a toast shows: aggregation only summarises; it never introduces numbers the events do not carry"
    - "Toast copy stays sarcastic, deadpan and family-friendly (the fixed aggregate formats add no new authored words beyond hits/misses/swing/land/foes/times/felled/CRIT)"
---

<objective>
Turn the 25-02 toast table into the per-action pipeline the shell calls: `toastsForAction(type, events, ctx)` runs the aggregators (the enemy round per foe with the 3+ collapse, your multi-attack round with the level-≤2 quip, the spell chain, the flee/parley/chest chains, the combined encounter-start toast, the kill fold), maps every remaining event through `TOAST_FOR`, dedupes per event type, sorts by priority (refusals first), caps at 4, and returns `[{ text, tone, priority }]`.

Purpose: FEED-04 ("X hits you 2 of 4 (N)" instead of four toasts; 3+ foes collapse), FEED-03 (you/they never confusable — every toast's family and prefix), FEED-01/FEED-02 ordering and adjacency (refusals first and alone; two features in one action both show), FEED-06 (spell hit+kill combined, resisted shows resist only, both directions ordered yours-then-theirs).
Output: `toastsForAction` added to src/browser/toasts.js; new test/unit/toastsForAction.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-01-SUMMARY.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-02-SUMMARY.md
@src/browser/toasts.js
@src/browser/missLines.js
@engine/combat.js
@engine/magic.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New export in src/browser/toasts.js: `toastsForAction(type, events, ctx = {}) → Array<{ text, tone, priority }>` (at most MAX_TOASTS entries, sorted by priority, engine order within a priority).
- Locked aggregate formats (text): `${name} hits you ${K} of ${M} (${sum})`, `${name} misses you ${M} times`, `${F} foes swing, ${K} land (${sum})`, `${F} foes swing, none land`, `You hit ${t} ${K} of ${M} (${sum})`, `You miss ${t} ${M} times`, the ` · CRIT`, ` · felled`, ` — ${quip}`, ` · ${parts} soaked`, and the `(${roll}+${bonus} vs ${need})` roll fold.
- New test file: test/unit/toastsForAction.test.js.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Round aggregators — enemy round per foe (+3-foe collapse, member hits), your round per target (+quip), kill fold</name>
  <files>src/browser/toasts.js</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: Combat wording — per-foe aggregation, 3+ foes collapse, party-member hits red family lower priority, your multi-attack rounds; Claude's Discretion: whether member toasts show when 3+ already queue — drop lowest priority first)
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-02-SUMMARY.md and src/browser/toasts.js (TOAST_FOR.struckByFoe / foeMissed / memberStruck / struck / strikeMissed / foeKilled — the single-event wordings and the soak/credited-mod notes to reuse)
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-01-SUMMARY.md (field names: soaked, needMods, soldierCrit, critBy, quip)
    - engine/combat.js foeTurn (~L1309-1461: event order within a foe's swings; member branch emits memberStruck / foeMissed with `member`; frenzy = 2 swings; sp.atk multiplies swings) and playerStrike (~L368-518: struck/strikeMissed per swing, foeKilled pushed by killFoe right after the killing struck)
    - engine/combat.js killFoe (~L520-575: foeKilled `{ name, spGained }` then cooked/goldGained/leveled)
  </read_first>
  <action>
    Add, in src/browser/toasts.js under a "toastsForAction pipeline" section, internal aggregator functions (non-exported, or exported for tests — executor's choice; document it in the header). Each aggregator takes the action's events array and returns `{ toasts: [...], consumed: Set<index> }` so later stages skip consumed events. Group key = the foe's `name` (events carry no foe index; same-named foes intentionally merge — "Goblin hits you 1 of 2 (5)" reads better than two Goblin toasts).

    1. `enemyRound(events)`: collect struckByFoe (never has member) and foeMissed WITHOUT `member` → groups by name in first-seen order. For each group with M = swings, K = hits, sum = integer Σ dmg of hits, crit = any hit critical/soldierCrit, soaked = per-key integer sums across hits: M === 1 → reuse `TOAST_FOR[event.type](event)` (the locked single-swing text incl. the credited-mod note on a miss); M >= 2 and K >= 1 → `${name} hits you ${K} of ${M} (${sum})` + ` · CRIT` + soak note; M >= 2 and K === 0 → `${name} misses you ${M} times`; tone hurt when K >= 1 else dodge; priority PRIORITY.them. If the number of distinct names >= 3 → replace all per-foe toasts with ONE: `${F} foes swing, ${K} land (${sum})` (K >= 1) or `${F} foes swing, none land`, tone hurt/dodge, ` · CRIT` when any crit. Separately, memberStruck and foeMissed WITH `member` → groups by (name, member): M === 1 → the single builder; M >= 2 → `${name} hits ${member} ${K} of ${M} (${sum})` / `${name} misses ${member} ${M} times`; tone hurt/dodge; priority PRIORITY.feature (D: lower priority — the cap drops them first). All consumed.
    2. `yourRound(events)`: struck and strikeMissed (non-untouchable) grouped by `target`: M === 1 → single builder (struck text incl. ` · CRIT` and the critBy note; strikeMissed incl. ` — ${quip}`); M >= 2 and K >= 1 → `You hit ${t} ${K} of ${M} (${sum})` + ` · CRIT` when any critical (+ the critBy note of the first critical hit); M >= 2 and K === 0 → `You miss ${t} ${M} times` + ` — ${quip}` using the FIRST quipped miss; tone hit when K >= 1 else miss; priority PRIORITY.you. Untouchable misses keep their own single builder (not grouped).
    3. `killFold(events, toasts)`: for each foeKilled whose `name` equals the target of an already-built your-round or (Task 2) spell-chain toast in this action, append ` · felled` to that toast (once) and consume the foeKilled; a foeKilled with no such toast (a ward reflect, an ally's kill, an acid tick) falls through to its own builder.
    Keep every format string in ONE place (small helpers such as `hitsLine(subject, object, K, M, sum)`) so the tests' exact-string assertions have a single source.
  </action>
  <verify>
    <automated>node -e "import('./src/browser/toasts.js').then(m => { const ev = [{type:'struckByFoe',name:'Dante',dmg:5,roll:2,need:5},{type:'foeMissed',name:'Dante',roll:9,need:5},{type:'struckByFoe',name:'Dante',dmg:6,roll:3,need:5},{type:'foeMissed',name:'Dante',roll:8,need:5}]; const out = m.toastsForAction('attack', ev, {}); console.log(out); if (out.length !== 1 || out[0].text !== 'Dante hits you 2 of 4 (11)' || out[0].tone !== 'hurt') process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function toastsForAction" src/browser/toasts.js` == 1
    - `grep -c "hits you \${" src/browser/toasts.js` >= 1 and `grep -c "foes swing" src/browser/toasts.js` >= 1 and `grep -c "misses you \${" src/browser/toasts.js` >= 1 and `grep -c "You hit \${" src/browser/toasts.js` >= 1 and `grep -c "You miss \${" src/browser/toasts.js` >= 1 and `grep -c "· felled" src/browser/toasts.js` == 1
    - the `<verify>` one-liner exits 0 (four Dante swings → exactly one toast "Dante hits you 2 of 4 (11)", tone hurt)
    - `node -e "import('./src/browser/toasts.js').then(m => { const ev = ['A','B','C'].flatMap(n => [{type:'struckByFoe',name:n,dmg:4,roll:2,need:5},{type:'foeMissed',name:n,roll:9,need:5}]); const out = m.toastsForAction('attack', ev, {}); if (out.length !== 1 || out[0].text !== '3 foes swing, 3 land (12)') { console.error(out); process.exit(1); } })"` exits 0
    - `node -e "import('./src/browser/toasts.js').then(m => { const out = m.toastsForAction('attack', [{type:'struck',target:'Dante',dmg:8,roll:2,need:5,critical:true},{type:'struck',target:'Dante',dmg:6,roll:3,need:5},{type:'foeKilled',name:'Dante',spGained:12}], {}); if (out.length !== 1 || out[0].text !== 'You hit Dante 2 of 2 (14) · CRIT · felled' || out[0].tone !== 'hit') { console.error(out); process.exit(1); } })"` exits 0
    - `grep -v '^\s*//' src/browser/toasts.js | grep -c "Math.random\|Date.now\|document\.\|window\."` == 0
  </acceptance_criteria>
  <done>Enemy rounds aggregate per foe (with the 3+ collapse and the member branch), your rounds aggregate per target with the first quip, and a kill folds into the blow that caused it — all through one set of format helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Spell chain, flee/parley/chest chains, the combined encounter-start toast, and the pipeline (map → dedupe → sort → cap)</name>
  <files>src/browser/toasts.js</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: "Several toasts per action, prioritized and capped" — the order and the cap of 4; "One combined encounter-start toast" flag list; Spell and item legibility — both directions; Specifics: "X of N is the whole point")
    - engine/magic.js castSpell (~L95-112 resist gate: spellResisted returns early, resistFailed then the effect event; ~L345-400 thrown loop: spellThrown then spellHit(+frozenSolid+killFoe) or spellMissed per target; Lightning targets every live foe) and readScroll (~L440-460: scrollRead → scrollCopiedToGrimoire or scrollCast → castSpell)
    - engine/combat.js flee (~L620-700: fleeRefused / vanishDenied / withdrawalDenied / fleeRolled → fled | fleeFailed, pursuitStrike events between), parley (~L700-840: parleyRefused / parleyExhausted / parleyRolled → goldGained why parley | parleyFailed | beastsSoothed, parleyInsulted), startCombat (~L153-330: encounterStarted, trackable, allyJoined, warlockBoost, foeFled knight/conArtist, foeBored, then phobiaFrozen/combatInDark, then the first foeTurn when foes win initiative)
    - engine/encounters.js chest path (~L110-140: chestLockRolled then chestOpened/chestLocked; chestOpened reason pilfer without a roll)
    - src/browser/toasts.js Task 1 aggregators and the TOAST_FOR builders for spellThrown/spellHit/spellMissed/spellResisted/resistFailed/frozenSolid/fled/fleeFailed/fleeRolled/parleyRolled/chestLockRolled/encounterStarted and the encounter followers
  </read_first>
  <action>
    1. `spellChain(events)`: group spellThrown by target with the outcome event(s) that follow for the same target (spellHit, spellMissed, frozenSolid, foeKilled): per target → spellHit alone: `${spell} hits ${target} (${dmg})` + ` ×${mult}` (mult > 1) (magic, you); spellHit + frozenSolid: `${spell} — ${target} frozen solid` (magic); spellHit + foeKilled: the hit text + ` · felled`; spellMissed: `${spell} misses ${target}` (miss). 3+ targets (Lightning) → one toast `${spell}: ${T} targets, ${K} hit (${sum})` (magic when K >= 1 else miss). spellResisted → the single resist builder and NO hit toast (the engine returns early anyway; assert it). resistFailed followed in the same action by any of dozed, stunned, weakened, stupefied, blinded, shrunk, acidApplied, petrified, walkingDeadTurned, planeGated, insaneRolled, insaneFled, frozenSolid for the same target (or with no target field) → consume resistFailed (only the effect toasts); a bare resistFailed keeps its builder. Consume spellThrown always when an outcome followed.
    2. `fleeChain(events)`: fleeRolled + (fled | fleeFailed) → ONE toast using the outcome builder's text + ` (${roll}+${bonus} vs ${need})` (bonus omitted when 0), the outcome's tone/priority; fled without fleeRolled (cloaker/tracked) → its own builder; withdrawalDenied/vanishDenied stay separate block toasts (priority 0) BEFORE the outcome. `parleyChain(events)`: parleyRolled + (goldGained why parley | parleyFailed | beastsSoothed) → the outcome text + ` (${roll} vs ${need})`. `chestChain(events)`: chestLockRolled + (chestOpened | chestLocked) → the outcome text + ` (${roll} vs ${need})`.
    3. `encounterStart(events)`: when an encounterStarted is present, build ONE toast (tone beat, priority PRIORITY.feature) from the encounterStarted builder's base text (the foe list: names with `×N` for repeats, prefixed "Wandering: " when wandering) plus, in engine order, one short clause per follower event in the same action: trackingRolled(tracked)/trackable → " · unnoticed"; allyJoined → ` · ${name} joins`; warlockBoost → ` · the dead stiffen (+${amount})`; foeFled reason knight → ` · ${name} flees a Knight`; reason conArtist → ` · ${name} talked out of it`; foeBored → ` · ${name} loses interest`; phobiaFrozen → " · frozen by fear"; combatInDark → " · in the dark"; plus the encounterStarted flag clauses from the builder (knightBigFoe / samuraiNeverFirst / fridgianSlow / courtMageTalksFirst / acuteHearing). Consume all of them. Without an encounterStarted in the action (e.g. a mid-fight foeFled lowHp, a foeBored never happens mid-fight, allyJoined via a pending summon) each keeps its own builder.
    4. `toastsForAction(type, events, ctx = {})` — the exported pipeline: guard non-array/empty → []; run encounterStart, enemyRound, yourRound, spellChain, fleeChain, parleyChain, chestChain, then killFold over the built toasts; map every unconsumed event whose type is a TOAST_FOR key (skip ORACLE_ONLY and unknown types) through its builder, tagging each toast with its source `type` and engine index; dedupe per type: keep the first toast of each event type in engine order and, when N > 1 of that type were mapped with differing text, append ` ×${N}`; stable-sort by priority ascending (ties keep engine order — aggregated toasts take the index of their first consumed event); cap: keep the first MAX_TOASTS after sorting (so priority-0 refusals can never be dropped and the lowest-priority, latest toasts go first); strip the internal tags and return `[{ text, tone, priority }]`. `ctx` is accepted and reserved (documented; currently unread — the quip arrives on the event via decorateMisses). Update the module header to describe the pipeline order.
  </action>
  <verify>
    <automated>node -e "import('./src/browser/toasts.js').then(m => { let out = m.toastsForAction('castSpell', [{type:'spellThrown',spell:'Freeze',target:'Dante',roll:3,need:6},{type:'spellHit',target:'Dante',dmg:9,mult:1},{type:'frozenSolid',target:'Dante'},{type:'foeKilled',name:'Dante',spGained:10}], {}); if (out.length !== 1 || out[0].text !== 'Freeze — Dante frozen solid') { console.error(out); process.exit(1); } out = m.toastsForAction('castSpell', [{type:'spellResisted',target:'Dante',spell:'Doze',roll:3,intel:4}], {}); if (out.length !== 1 || out[0].text !== 'Dante resists Doze' || out[0].tone !== 'miss') { console.error(out); process.exit(2); } out = m.toastsForAction('attack', [{type:'strikeRefused',reason:'wizard',spell:'Freeze'}], {}); if (out.length !== 1 || out[0].tone !== 'block' || out[0].priority !== 0 || !/Freeze/.test(out[0].text)) { console.error(out); process.exit(3); } })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "frozen solid" src/browser/toasts.js` >= 1 and `grep -c "targets, \${" src/browser/toasts.js` >= 1
    - `grep -c "MAX_TOASTS" src/browser/toasts.js` >= 2 (the constant and the cap)
    - the `<verify>` one-liner exits 0 (Freeze hit+frozen+kill → one toast; resisted → resist only; Wizard refusal → block, priority 0, names Freeze)
    - `node -e "import('./src/browser/toasts.js').then(m => { const ev=[{type:'encounterStarted',wandering:false,foes:[{name:'Goblin'},{name:'Goblin'}],knightBigFoe:false,samuraiNeverFirst:false,fridgianSlow:false,acuteHearing:false,courtMageTalksFirst:false},{type:'foeFled',name:'Goblin',reason:'knight'},{type:'foeFled',name:'Goblin',reason:'knight'},{type:'encounterCleared'}]; const out=m.toastsForAction('move',ev,{}); const enc=out.filter(t=>/Goblin/.test(t.text)); if (enc.length!==1||!/flees a Knight/.test(enc[0].text)||out.some(t=>t.text.startsWith('Goblin flees'))) { console.error(out); process.exit(1); } })"` exits 0 (one combined encounter toast; the two knight flees fold into it)
    - `node -e "import('./src/browser/toasts.js').then(m => { const ev=[{type:'fleeRolled',roll:7,bonus:5,need:11},{type:'fleeFailed'},{type:'struckByFoe',name:'Rat',dmg:3,roll:2,need:5}]; const out=m.toastsForAction('flee',ev,{}); if (out.length!==2||!/\(7\+5 vs 11\)/.test(out[0].text)||out[0].tone!=='miss'||out[1].text!=='Rat hits you (3)') { console.error(out); process.exit(1); } })"` exits 0 (flee roll folded into the failure; your outcome before theirs)
    - `node -e "import('./src/browser/toasts.js').then(m => { const ev=[{type:'frenzy'},{type:'struck',target:'Dante',dmg:5,roll:2,need:5},{type:'struck',target:'Dante',dmg:4,roll:3,need:5},{type:'struckByFoe',name:'Dante',dmg:4,roll:2,need:5,soaked:{hide:2}},{type:'leveled',level:2,wpGain:6},{type:'goldGained',amount:3},{type:'wardFaded'},{type:'mirrorFaded'}]; const out=m.toastsForAction('attack',ev,{}); if (out.length!==4||out[0].priority>out[1].priority||out[1].priority>out[2].priority||out[2].priority>out[3].priority||out[0].text!=='You hit Dante 2 of 2 (9)'||out[1].text!=='Dante hits you (4) · hide 2 soaked'||out.some(t=>t.priority===4)) { console.error(out); process.exit(1); } })"` exits 0 (cap 4 drops the priority-4 fades first; order you → them → features)
    - `node -e "import('./src/browser/toasts.js').then(m => { const a=m.toastsForAction('attack',[{type:'strikeMissed',target:'Rat',roll:7,need:5,quip:'A swing. Technically.'}],{}); const b=m.toastsForAction('attack',[{type:'strikeMissed',target:'Rat',roll:7,need:5,quip:'A swing. Technically.'}],{}); if (JSON.stringify(a)!==JSON.stringify(b)||a[0].text!=='You miss Rat — A swing. Technically.') { console.error(a); process.exit(1); } if (m.toastsForAction('move',[],{}).length!==0||m.toastsForAction('move',null,{}).length!==0) process.exit(2); })"` exits 0
  </acceptance_criteria>
  <done>toastsForAction composes spell/flee/parley/chest chains and the encounter-start toast, maps the rest through the table, dedupes per type, sorts refusals → you → them → features → other, caps at 4, and is replay-stable.</done>
</task>

<task type="auto">
  <name>Task 3: toastsForAction.test.js — every locked contract, boundary and probe truth as a unit test</name>
  <files>test/unit/toastsForAction.test.js</files>
  <read_first>
    - src/browser/toasts.js (Tasks 1-2: the pipeline and the format helpers)
    - src/browser/missLines.js (MISS_LINES / decorateMisses — to build realistic quipped events and prove the level gate end-to-end through decorateMisses → toastsForAction)
    - test/unit/toastTable.test.js (25-02 — do not duplicate single-builder assertions; this file tests the pipeline)
    - test/unit/haptics.test.js (events → side-effect mapper test idiom)
    - the resolved edge-coverage list in this plan's must_haves (each truth becomes at least one test name that quotes its probe tag, e.g. "probe FEED-04 boundary: 1 swing has no 'of'")
  </read_first>
  <action>
    Create test/unit/toastsForAction.test.js with synthetic event lists (no engine calls needed; import only from src/browser/toasts.js and src/browser/missLines.js). Cover at minimum, each as its own `test(...)`:
    - Empty/garbage: `[]`, `null`, `undefined`, `[{ type: "nonsenseType" }]` → `[]`; an action with only ORACLE_ONLY events (dayBegan, spGained, moved) → `[]`; an event missing optional fields (goldGained without why, foeFled without reason) → one toast, never throws (probe FEED-01 empty).
    - Ordering + cap (probe FEED-01 ordering): a 9-event mix spanning all five priorities → output priorities are non-decreasing, length 4, the priority-4 toasts are the ones dropped, and within priority 3 the engine order is preserved; a list with one refusal plus 6 features → the refusal is index 0 and present (probe FEED-02 ordering).
    - Refusal adjacency (probe FEED-02 adjacency): for each refusal type in the must_haves list, a one-event action yields exactly one toast with tone block; strikeRefused with spell "Freeze" yields text containing "Freeze" and no struck/strikeMissed toast; withdrawalDenied + fleeRolled + fleeFailed → [block toast, miss toast] in that order.
    - Feature adjacency (probe FEED-01 adjacency): `[backstab, struck(critical, critBy backstab), foeKilled]` → the backstab feature toast AND the your-round toast both present, in that relative order after sorting (you before feature); `[frenzy, struck, struck, struckByFoe(soaked hide 2)]` → frenzy toast present, one your-round "2 of 2" toast, one enemy toast with the soak note; two `backstab` events → one toast (dedupe per type).
    - Enemy boundaries (probe FEED-04 boundary/precision): 1 hit → "Dante hits you (6)" (no "of"); 1 miss → "Dante misses you"; 2 swings 1 hit → "Dante hits you 1 of 2 (6)"; 4 swings 2 hits → "Dante hits you 2 of 4 (11)" with sum 5+6 (integers); 3 swings 0 hits → "Dante misses you 3 times"; crit → " · CRIT"; soldierCrit → " · CRIT"; two foes → two toasts in first-seen order; three distinct foes → exactly one "3 foes swing, K land (sum)"; three foes none landing → "3 foes swing, none land" tone dodge; same-named foes merge into one group; member events → "Dante hits Bram (5)" at priority 3 and "Dante hits Bram 2 of 2 (9)" for two member hits; K/M come from event counts (an events list with 5 swings for a foe → M = 5 regardless of any sp.atk field).
    - Your round: 1 hit → "You hit Dante (8)"; 2 of 2 → "You hit Dante 2 of 2 (14) · CRIT" when one is critical; 0 of 2 → "You miss Dante 2 times"; with quips on both misses → "You miss Dante 2 times — <first quip>"; single quipped miss → "You miss Dante — <quip>"; through decorateMisses: level 2 events get a quip toast, level 3 events (same input) get plain "You miss Dante" (probe FEED-04 boundary — the level gate end-to-end); an untouchable miss stays its own toast.
    - FEED-03 flagged assumption: over a generated matrix of enemy events (struckByFoe/foeMissed/memberStruck/foeBolted/foeDrained/foeDebuffed/foeHealed/heroResistFailed with name "Dante") every output toast has tone ∈ {hurt, dodge} and text matching `/^(Dante|\d+ foes)/`; over player events (struck/strikeMissed/heroResisted) every toast has tone ∈ {hit, miss} and text starting "You".
    - Spell chain (probe FEED-06 adjacency/ordering): Freeze hit + frozenSolid + foeKilled → exactly one toast "Freeze — Dante frozen solid"; Fireball spellHit + foeKilled → "Fireball hits Dante (12) · felled"; spellResisted alone → "Dante resists Doze", tone miss, and no magic toast; resistFailed + dozed → only the dozed toast; Lightning at 3 targets → one "Lightning: 3 targets, 2 hit (18)"; your cast then a foe bolt in the same action → your toast index < the foe toast index and tones are magic vs hurt.
    - Encounter start: encounterStarted + trackable + allyJoined + warlockBoost → one toast containing "unnoticed", the ally name and "stiffen"; encounterStarted + foeFled(knight) ×2 → one toast, no standalone flee toasts; foeFled(lowHp) with NO encounterStarted → its own toast; encounterStarted with knightBigFoe → contains "Knight".
    - Chains: fleeRolled+fled → one hit toast with "(7+5 vs 11)"; fleeRolled+fleeFailed → one miss toast with the roll; parleyRolled+parleyFailed → one toast with "(roll vs need)"; chestLockRolled+chestOpened → one toast with the roll; chestOpened(reason pilfer) alone → the Pilfer toast.
    - Purity/determinism: same input twice → deepEqual; the input events array and its objects are not mutated (deepEqual before/after); source scan of toasts.js for Math.random/Date.now/document./window. → none.
  </action>
  <verify>
    <automated>node --test test/unit/toastsForAction.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/toastsForAction.test.js` exits 0 with >= 40 passing tests
    - `grep -c "probe FEED-04 boundary" test/unit/toastsForAction.test.js` >= 2 and `grep -c "probe FEED-01 ordering" test/unit/toastsForAction.test.js` >= 1 and `grep -c "probe FEED-02 adjacency" test/unit/toastsForAction.test.js` >= 1 and `grep -c "probe FEED-06 adjacency" test/unit/toastsForAction.test.js` >= 1 and `grep -c "FEED-03" test/unit/toastsForAction.test.js` >= 1
    - `grep -c '"Dante hits you 2 of 4 (11)"' test/unit/toastsForAction.test.js` == 1 and `grep -c '"Dante misses you 3 times"' test/unit/toastsForAction.test.js` == 1 and `grep -c '"3 foes swing, none land"' test/unit/toastsForAction.test.js` == 1 and `grep -c '"You hit Dante 2 of 2 (14) · CRIT"' test/unit/toastsForAction.test.js` == 1 and `grep -c '"Freeze — Dante frozen solid"' test/unit/toastsForAction.test.js` == 1 and `grep -c '"Fireball hits Dante (12) · felled"' test/unit/toastsForAction.test.js` == 1
    - `grep -c "decorateMisses" test/unit/toastsForAction.test.js` >= 2 (the level-gate end-to-end test)
    - `npm test` output ends with `# fail 0`
  </acceptance_criteria>
  <done>Every probe truth, locked wording, boundary (1 vs 2+ swings, 1–2 vs 3+ foes, level 2 vs 3), refusal-first rule, chain fold and direction contract is a named passing unit test; the pipeline is proven pure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events → aggregated toast | Sums/counts are derived from event fields; a wrong group key or a non-integer sum misreports what happened |
| toast pipeline → shell | The shell trusts the array is <= 4 and sorted; a broken cap would flood the fixed host |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-11 | Tampering | enemyRound grouping by name merging distinct foes | low | accept | documented as intended ("Goblin hits you 1 of 2"); the Oracle keeps every swing |
| T-25-12 | Denial of Service | cap regression flooding the host / dropping a refusal | medium | mitigate | cap + refusal-never-dropped unit tests; MAX_TOASTS is the single constant |
| T-25-13 | Tampering | aggregation inventing numbers (rounding, sp.atk-based M) | medium | mitigate | integer-sum and event-count tests (probe FEED-04 precision) |
| T-25-14 | Repudiation | a folded event (foeKilled, resistFailed, fleeRolled) losing its Oracle line | low | mitigate | folding happens only in toasts.js; the Oracle html is formatted from the full event list (engineAdapter) |
| T-25-15 | Information Disclosure | a builder called with a partial payload leaking "undefined" into text | low | mitigate | all builders defend fields; pipeline tests assert no "undefined"/"NaN" substrings in any output |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/missLines.test.js` green.
- The five `node -e` gates in Tasks 1-2 exit 0.
- `npm test` ends `# fail 0` (no engine file touched; parity unaffected).
</verification>

<success_criteria>
- One action → at most 4 toasts, refusals first, you before them, features next, bookkeeping never.
- A multi-attack foe round is one "X hits you K of M (N)" toast; 3+ foes collapse; your rounds mirror it; early misses carry the quip; spells fold hit+kill and show resists; the encounter start is one composed toast.
- The pipeline is pure, deterministic, and every contract is a named test.
</success_criteria>

<output>
Create `.planning/phases/25-nothing-happens-silently-feature-feedback/25-03-SUMMARY.md` when done (record the final aggregate format strings and the pipeline order so 25-04's smoke test and the device checklist quote them exactly).
</output>
