---
phase: 76-darkness-unification-relaunch-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/derived.js
  - engine/maze.js
  - test/unit/dark-waiver.test.js
  - test/unit/darkness-filter.test.js
  - test/unit/darkness-vignette.test.js
  - test/unit/roll-high-state-pins.test.js
  - test/unit/roll-high-save-compat.test.js
  - test/unit/fixtures/roll-high/pre-switch-save.json
  - test/unit/bot-tactics.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/DIFFICULTY-RETUNE.md
  - docs/TERRAIN.md
  - tools/readouts/76-01-before.txt
  - tools/readouts/76-01-after.txt
autonomous: true
requirements:
  - DARK-01
must_haves:
  truths:
    - "DARK-01: engine/derived.js exports ONE darkness-waiver predicate. `darkWaiver(c)` names the waiver holding the dark back (`nightVision`, `amuletLight` or `litTorch`) or returns null; `darkWaived(c)` is `darkWaiver(c) !== null`. The waivers are Night Vision (`skill(c, \"Night Vision\")`), a live Amulet of Light (`eff(c, \"light\") > 0`) and a lit torch (`itemEffectActive(c, \"lit\")`), per CONTEXT's darkness-unification decision (one shared predicate)"
    - "DARK-01: `revealRadius(state)` is `(inDark(state) && !darkWaived(state.c) ? 1 : 2) + eff(state.c, \"sight\")` and `mapViewRadius(state)` is `DARK_VIEW_RADIUS` exactly when `inDark(state) && !darkWaived(state.c)`, else Infinity. A lit torch now widens what you reveal as you walk (1 to 2), and a live Amulet widens it from 2 to 3 (its `sight` stays the separate additive)"
    - "DARK-01: revealRadius and mapViewRadius can never disagree: across every waiver (none, Night Vision, live Amulet, lit torch, a COOLING torch, a COOLING Amulet) x tile (dark, lit) x darkFor (0, 30), `revealRadius === (limited ? 1 : 2) + eff(sight)` and `mapViewRadius === (limited ? DARK_VIEW_RADIUS : Infinity)` with `limited = inDark && !darkWaived`, pinned by test/unit/dark-waiver.test.js"
    - "DARK-01 precedence (fixed, most-durable-first, exported as the frozen `DARK_WAIVERS`): with more than one waiver live, darkWaiver names Night Vision, then the Amulet, then the torch"
    - "DARK-01 empty: darkWaiver(undefined), darkWaiver(null) and darkWaiver({}) return null and never throw"
    - "DARK-01 adjacency: a torch or Amulet record in its effect phase with `left: 1` waives; the same record in its cooldown phase, or at `left: 0`, does not"
    - "Combat's darkness rules are untouched: toHit's in-dark cap, no-crit-in-dark, combatInDark, the silent-thief clause and the Darkness phobia still waive only on Night Vision and Sense Presence (`git diff` of engine/combat.js is empty for this plan; a unit test pins toHit in the dark with a lit torch as unchanged)"
    - "Measured, not assumed: the parity suite passes with zero fixture edits and the master untouched (the predicted exposure is zero: no replay site stands in the dark with a live torch or Amulet); every moved bot pin is traced to steps where the old and new reveal radius differ, re-pinned with a DARK-01 rationale comment, and declared in the Phase 76 section of test/parity/FIXTURE-INVENTORY.md"
    - "A 200-seed `tools/tune-difficulty.mjs` readout is taken before and after, and both are recorded under a new Phase 76 H2 in docs/DIFFICULTY-RETUNE.md with NO compensating nerf (CONTEXT: the user judges at milestone close)"
  artifacts:
    - path: engine/derived.js
      provides: "DARK_WAIVERS, darkWaiver, darkWaived; revealRadius and mapViewRadius both on the one predicate"
      contains: "export function darkWaiver"
    - path: test/unit/dark-waiver.test.js
      provides: "the waiver x tile x darkFor agreement matrix, precedence, empty, adjacency and the combat-untouched pin"
    - path: tools/readouts/76-01-before.txt
      provides: "the 200-seed readout at the plan base"
    - path: tools/readouts/76-01-after.txt
      provides: "the 200-seed readout after the unification"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the Phase 76 H2 and its Plan 01 measured subsection"
      contains: "## Phase 76"
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 76 darkness readout record (before and after)"
      contains: "(Phase 76)"
  key_links:
    - from: engine/derived.js#revealRadius
      to: engine/derived.js#darkWaived
      via: "the one waiver predicate"
      pattern: "darkWaived\\(state\\.c\\)"
    - from: engine/derived.js#mapViewRadius
      to: engine/derived.js#darkWaived
      via: "the same predicate, so the two radii agree"
      pattern: "darkWaived\\("
    - from: engine/movement.js (move, teleport, descend) and engine/state.js#newRun
      to: engine/derived.js#revealRadius
      via: "reveal(floor, revealRadius(state)) — unchanged call sites that now honour the torch and the Amulet"
      pattern: "reveal\\(.*revealRadius\\("
  prohibitions:
    - statement: "MUST NOT extend the new waiver to combat's darkness rules (toHit's in-dark cap, no-crit-in-dark, combatInDark, the silent-thief clause, the Darkness phobia); CONTEXT scopes the unification to what the player sees"
      requirement_id: DARK-01
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT add a compensating nerf or retune any dial to offset the kinder dark; the readout is recorded for the user's judgment (CONTEXT: no compensating nerf)"
      requirement_id: DARK-01
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT keep the old reveal waiver alive behind a flag, option or second code path (greenfield), MUST NOT edit test/parity/prototype-master.js.txt, and MUST NOT regenerate or re-pin anything the change did not measurably move"
      requirement_id: DARK-01
      category: transparency
      status: resolved
      verification: test
---

<objective>
Make one darkness-waiver rule govern both what the party reveals as it walks and what the map renders (DARK-01). A lit torch, the Amulet of Light or Night Vision now lights your way as you walk, not just the map you have already explored (CONTEXT's darkness-unification decision: "One shared predicate ... waives the dark for BOTH revealRadius AND mapViewRadius").

Purpose: the 2026-09-21 device report ("It looks like I'm in the dark, but it's not limiting my vision"). Today a lit torch reopens the whole explored map but does not show one square further as you walk. This is a declared canon divergence: the 1994 `reveal()` radius line waived only on Night Vision.

Output: the predicate and the two radii on it, a pinning agreement test, the measured moved set, and before/after readouts recorded for the user.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 76-01 creates or changes.)

- **This plan (76-01):** `DARK_WAIVERS` / `darkWaiver` / `darkWaived` in `engine/derived.js`; `revealRadius` and `mapViewRadius` on the one predicate; `test/unit/dark-waiver.test.js`; the flipped Phase 57 characterisations in `test/unit/darkness-filter.test.js`; `tools/readouts/76-01-before.txt` / `-after.txt`; the Phase 76 H2 in `docs/DIFFICULTY-RETUNE.md`; the Phase 76 H2 and its Plan 01 subsection in `test/parity/FIXTURE-INVENTORY.md`; `docs/TERRAIN.md`'s darkness section; any measured, declared bot-pin re-pins
- 76-02: `src/browser/darknessView.js` collapsed (two-way `waiverFor`, one-radius vignette); the DARK chip and its tap card read `darkWaiver`; `draw()` paints waived dark tiles with a faint lit tint (`MAP_PALETTE.floorDarkLit` / `waterDarkLit`); `test/unit/dark-surfaces.test.js`; `docs/SHELL-MODULES.md`
- 76-03: `engine/saveState.js` resumes a validated `combat`, `store`, `pendingFind`, `pendingHazard` and `pendingTile`; conditional foe-effect and round-timer clears; `resumeEventsFor` with `fightResumed` / `storeResumed` narration; `test/unit/save-resume.test.js`; the flipped reset-on-load pins; `test/persistence/resume-mid-encounter.test.js` rewritten
- 76-04: `src/browser/engineAdapter.js` one-shot `takeBootResumeEvents()`; `test/roundtrip/resume-roundtrip.test.js` (the generic every-field walk and resume determinism); the adapter-level relaunch proofs
- 76-05: the Oracle resume line at boot in `mazeworld.html`; `test/unit/shell-resume-line.test.js`; the persistence subsection of FIXTURE-INVENTORY's Phase 76 section; the Phase 76 device checklist

## Flagged assumptions (surfaced, not silently decided)

- **DARK-01 edge probe: unclassified, reviewed manually (left unresolved by the probe).** Assumed the edge surface is (a) the waiver x tile x darkFor matrix, (b) the effect-phase/cooldown-phase boundary of a torch or Amulet record, and (c) the Amulet's `sight` staying additive on top of the waiver (a live Amulet in the dark reveals radius 3). All three are pinned in `test/unit/dark-waiver.test.js`. If the user meant a different edge, it is not covered.
- **Combat darkness is out of scope (reading of CONTEXT's phase boundary).** The five surfaces CONTEXT names are reveal, the render window, per-tile painting, the chip and the vignette. The in-fight dark penalties keep Night Vision and Sense Presence as their only waivers. This is flagged to the user as a design question: "a light source should mean the same thing everywhere" could be read to include combat.
- **The waiver key names are the shell's existing ones** (`nightVision`, `amuletLight`, `litTorch`, from `mazeworld.html#WAIVER_LABEL`), so 76-02 needs no label migration.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-darkness-unification-relaunch-persistence/76-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
**Run order:** Phases 75, 75.1, 75.2 and 75.3 are merged before this executes. Read their SUMMARYs for anything they did to engine/derived.js (75.1-03/04/06, 75.2-01/02 and 75.3-04 all edit it: toHit gains heroBlind/size terms, conditionsOf gains chips). Reference functions by name; line numbers have moved.

engine/derived.js (as of the 75 close):
- `skill = (c, n) => !!(c.skills && c.skills[n])` throws on a missing `c`, so darkWaiver must guard `c` first.
- `eff(c, key)` sums `act.eff[key]` over `liveItemEffects(c)`; `liveItemEffects` keeps only `item:` timer records with `phase === "effect"` and `left > 0`. The Amulet of Light's activation is kind `glow` with `eff: { sight: 1, light: 1 }` (content/treasure-tables.js); the Torch's is kind `lit` (content/tools.js, `TOOL_ACTIVATION_OF.Torch`).
- `itemEffectActive(c, kind)` is `liveItemEffects(c).some(e => e.act.kind === kind)`.
- `inDark(state)`: the current tile's `.dark` flag OR `state.c.darkFor > 0`.
- `revealRadius(state)`: today waives only on Night Vision (the ported 1994 line).
- `mapViewRadius(state)`: today waives on Night Vision, `eff(c, "light") > 0` and `itemEffectActive(c, "lit")`. Its preceding block comment (the Phase 41 DARK_VIEW_RADIUS note) claims the waiver set is "the SAME one revealRadius ... already use[s]", which was never true.
- `toHit` has its own in-dark line `inDark(state) && !skill(c, "Night Vision") && !c.senses` and combat.js has combatInDark / no-crit-in-dark. Leave every one of them alone.

engine/maze.js#reveal(floor, radius): its JSDoc says every caller passes revealRadius "matching the prototype's dark/Night Vision/sight behavior exactly". That sentence becomes false; reword it.

Callers of revealRadius (unchanged; they pick up the new rule for free): engine/movement.js (move, teleport, descend) and engine/state.js#newRun (at chargen no torch or Amulet is live, so newRun's opening reveal is unchanged).

Existing tests that pin today's divergence and must flip:
- test/unit/darkness-filter.test.js: the three "Phase 57 characterisation" tests for darkFor WITH a lit torch, WITH a live Amulet and WITH Night Vision. The torch case asserts revealRadius 1 (becomes 2); the Amulet case asserts 2 (becomes 3).
- test/unit/darkness-vignette.test.js: one sanity assertion that revealRadius stays at 1 under the torch waiver (becomes 2). Change ONLY that assertion and its message here; 76-02 reworks the rest of the file.
- test/unit/movement.test.js's two revealRadius tests (darkFor with no waiver gives 1; Night Vision gives 2) stay true and need no edit.

Bot surfaces likely to move (the tuning bot lights a carried torch, or a ready worn Amulet, whenever it stands in the dark: tools/lib/tuning-bot.mjs#chooseFieldItem):
- test/unit/roll-high-state-pins.test.js (PINNED hashes over PIN_RUNS; regenerate with `node tools/roll-high-baseline.mjs pins`; the file's own comments show the Phase 75 Plan 05 and Plan 12 re-pin precedent).
- test/unit/roll-high-save-compat.test.js (the pre-switch save loaded and played on; it replays the fixture's recorded `dispatched` actions against its `expected` { dead, depth, hash } in test/unit/fixtures/roll-high/pre-switch-save.json. Only `expected` may be re-recorded for this deliberate rule change, from a live replay; the fixture's `save`, `note`, `baseCommit` and `rollLogicUnchangedSince` never change, and `node tools/roll-high-baseline.mjs save` rewrites the whole fixture, so never use it here (the 75.2-01 precedent)).
- test/unit/bot-tactics.test.js (the no-stall seed trios; 75-12 swapped a stalled seed for the smallest untaken seed that still dies naturally).
- Any other failing bot-driven test (class-matrix, band-readout, tuning-bot, test/difficulty/*): measure; do not loosen a band.

**Readout protocol (the 75-12 precedent):** record `git rev-parse HEAD` as the plan base before the first edit.
- BEFORE: `git worktree add --detach <session scratchpad>/76-01-base <plan-base>`; from that directory start `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, stdout to this worktree's `tools/readouts/76-01-before.txt` (absolute path). Remove the scratch worktree when done.
- AFTER: once the engine edit is committed, run the same command here into `tools/readouts/76-01-after.txt` with run_in_background, and wait for it.

docs/DIFFICULTY-RETUNE.md: `test/unit/difficulty-retune-ledger.test.js` requires `## v1.2 retune (Phase 27)` to stay the LAST H2. Insert the new H2 immediately before it, shaped like the Phase 73 section (`### Change under measurement`, `### Parameters`, `### BEFORE — commit <sha>`, `### AFTER — commit <sha>`, `### Reading`).

test/parity/FIXTURE-INVENTORY.md: sections are appended in phase order; the 75.x sections are last. Model the new H2 on the Phase 65 / Phase 73 "measured" sections (The rule, The predictor, The live-scan results, Moved set, Byte-identical elsewhere).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the plan base, start the BEFORE readout, and predict the parity exposure</name>
  <files>tools/readouts/76-01-before.txt</files>
  <read_first>
    - tools/tune-difficulty.mjs (the header's usage lines)
    - tools/lib/tuning-bot.mjs (chooseFieldItem: when the bot lights a torch or uses the Amulet)
    - test/parity/harness/comparables.js and test/parity/divergence-records.test.js (the fixture replay shape, for a scratch exposure scan)
    - .planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-12-SUMMARY.md (the readout and re-pin precedent)
  </read_first>
  <action>
1. Record `git rev-parse HEAD` as the plan base in your notes (it goes in the SUMMARY and both records).
2. Start the BEFORE readout exactly as the interfaces' readout protocol says: a detached scratch worktree at the plan base, `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, stdout to this worktree's `tools/readouts/76-01-before.txt`. Do not wait for it here; Task 3 checks it finished.
3. Exposure predictor (a scratch script in the session scratchpad, never committed): replay every parity fixture scenario through the parity harness and count states in which `inDark(state)` is true while the hero has a live Amulet light (`eff(c, "light") > 0`) or a lit torch (`itemEffectActive(c, "lit")`) and no Night Vision. Those are the only states where the old and new reveal radius differ. Record the count per fixture file (predicted: zero everywhere).
4. Also record the committed PINNED labels in test/unit/roll-high-state-pins.test.js so Task 3 can compare.
  </action>
  <verify>
    <automated>git rev-parse HEAD && test -f tools/readouts/76-01-before.txt</automated>
  </verify>
  <acceptance_criteria>
    - `tools/readouts/76-01-before.txt` exists (the background run is writing to it).
    - The SUMMARY notes carry the plan base SHA and the exposure count per parity fixture file.
    - No file under engine/ has changed yet (`git diff --quiet -- engine/` exits 0).
  </acceptance_criteria>
  <done>The baseline readout is running from the untouched engine, and the parity exposure is predicted from a live replay, not assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One darkness-waiver predicate for both radii (DARK-01)</name>
  <files>engine/derived.js, engine/maze.js, test/unit/dark-waiver.test.js, test/unit/darkness-filter.test.js, test/unit/darkness-vignette.test.js, docs/TERRAIN.md</files>
  <read_first>
    - engine/derived.js (skill, eff, liveItemEffects, itemEffectActive, inDark, revealRadius, DARK_VIEW_RADIUS and its block comment, mapViewRadius, inViewWindow, toHit's in-dark line)
    - engine/maze.js (reveal's JSDoc)
    - content/tools.js (the Torch activation) and content/treasure-tables.js (the Amulet of Light row and its activation)
    - test/unit/darkness-filter.test.js (the wallGrid/open/fixedFighter/fixedState helpers and the three Phase 57 characterisation tests)
    - test/unit/darkness-vignette.test.js (the torch-waiver sanity assertion only)
    - docs/TERRAIN.md (the Phase 41 darkness render filter section)
  </read_first>
  <behavior>
    - darkWaiver: no waiver gives null; Night Vision gives "nightVision"; a live Amulet (an `item:Amulet of Light` record, phase effect, left above 0) gives "amuletLight"; a lit torch (`item:Torch`, phase effect, left above 0) gives "litTorch"; Night Vision plus Amulet plus torch gives "nightVision"; Amulet plus torch gives "amuletLight".
    - darkWaiver(undefined), darkWaiver(null) and darkWaiver({}) return null without throwing; `DARK_WAIVERS` deep-equals ["nightVision", "amuletLight", "litTorch"] and is frozen.
    - Adjacency: an effect-phase torch record with left 1 waives; a cooldown-phase torch record (the existing "COOLING torch" fixture shape) and a left-0 record do not; the same for the Amulet.
    - The matrix: for every waiver in {none, Night Vision, live Amulet, lit torch, cooling torch, cooling Amulet} x tile {dark, lit} x darkFor {0, 30}, with `limited = inDark(state) && !darkWaived(state.c)`: revealRadius equals `(limited ? 1 : 2) + eff(c, "sight")`, mapViewRadius equals `limited ? DARK_VIEW_RADIUS : Infinity`, and inViewWindow at a seen cell two squares away equals `!limited`.
    - Concrete values: darkFor 30 on a lit tile with a lit torch gives revealRadius 2 and mapViewRadius Infinity (the measured divergence, now agreeing); with a live Amulet, revealRadius 3; with no waiver, 1 and 1.
    - Combat untouched: toHit for a fixed Fighter in the dark (darkFor 30) with a lit torch equals toHit for the same Fighter in the dark with no torch; with Night Vision it still differs as it does today.
  </behavior>
  <action>
1. Write test/unit/dark-waiver.test.js first with every behaviour above, reusing darkness-filter.test.js's hand-built grid helpers (copy them locally; that file's header documents the same copy-the-helper convention). Run it and watch it fail.
2. engine/derived.js, per CONTEXT's darkness-unification decision:
   - Add the frozen export `DARK_WAIVERS` with the three keys in the precedence order above (Night Vision is innate and most durable; the Amulet is a worn effect; the torch is the shortest-lived; carry the Phase 57 most-durable-first rationale into the JSDoc).
   - Add `darkWaiver(c)`: null for a missing or non-object `c`; otherwise the first live of Night Vision (`skill`), the Amulet's light (`eff(c, "light") > 0`) and a lit torch (`itemEffectActive(c, "lit")`), in that order, else null. Add `darkWaived(c)` returning whether darkWaiver is non-null. Both pure, no rng, no mutation.
   - Rewrite revealRadius to waive through darkWaived instead of the Night Vision skill read, keeping `+ eff(state.c, "sight")` as the separate additive. Rewrite mapViewRadius to return DARK_VIEW_RADIUS exactly when inDark and not darkWaived, else Infinity.
   - JSDoc on all three: DARK-01 (Phase 76, user ruling 2026-09-22, accepted 2026-09-25), a declared canon divergence (the 1994 reveal line waived only on Night Vision; a torch or the Amulet now lights the way as you walk), and the two radii can no longer disagree. Rewrite the Phase 41 DARK_VIEW_RADIUS block comment so it no longer claims revealRadius already shared the waiver set.
   - Do not touch toHit, conditionsOf, combatInDark or any combat read (the combat darkness rules keep Night Vision and Sense Presence only; see the flagged assumption).
3. engine/maze.js: reword reveal's JSDoc sentence about matching the prototype's dark/Night Vision behaviour exactly: callers pass revealRadius, which since Phase 76 (DARK-01) also waives on a lit torch or a live Amulet.
4. test/unit/darkness-filter.test.js: rewrite the three Phase 57 characterisation tests (darkFor with a torch, with the Amulet, with Night Vision) to assert the unified answers (torch: revealRadius 2; Amulet: 3; Night Vision: 2; mapViewRadius Infinity for all three) and retitle them "Phase 76 (DARK-01): ..." with a one-line pointer to this phase. Keep the no-waiver and mirror-case characterisations as they are.
5. test/unit/darkness-vignette.test.js: change only the torch-waiver sanity assertion to revealRadius 2, with a DARK-01 message. Leave the rest for 76-02.
6. docs/TERRAIN.md: update the darkness render-filter section so it states the one rule: Night Vision, a lit torch or a live Amulet of Light waives the dark for both what you reveal as you walk and what the map shows; `sight` still adds on top; the in-fight penalties are unchanged. Name `darkWaiver` / `darkWaived` in `engine/derived.js`.
  </action>
  <verify>
    <automated>node --test test/unit/dark-waiver.test.js test/unit/darkness-filter.test.js test/unit/darkness-vignette.test.js test/unit/movement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/dark-waiver.test.js` passes, including the matrix, precedence, empty, adjacency and combat-untouched cases.
    - `grep -c "export function darkWaiver" engine/derived.js` equals 1, `grep -c "export function darkWaived" engine/derived.js` equals 1 and `grep -c "export const DARK_WAIVERS" engine/derived.js` equals 1.
    - `grep -c "darkWaived(state.c)" engine/derived.js` is at least 2 (revealRadius and mapViewRadius).
    - `git diff --quiet <plan-base> -- engine/combat.js engine/difficulty.js content/` exits 0.
    - `node --test test/unit/darkness-filter.test.js test/unit/darkness-vignette.test.js test/unit/movement.test.js` passes.
    - `grep -c "darkWaiver" docs/TERRAIN.md` is at least 1.
  </acceptance_criteria>
  <done>A torch, the Amulet or Night Vision widens the reveal and the render by one rule; combat is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the moved set, declare it, take the AFTER readout and record both</name>
  <files>test/unit/roll-high-state-pins.test.js, test/unit/roll-high-save-compat.test.js, test/unit/fixtures/roll-high/pre-switch-save.json, test/unit/bot-tactics.test.js, test/parity/FIXTURE-INVENTORY.md, docs/DIFFICULTY-RETUNE.md, tools/readouts/76-01-after.txt</files>
  <read_first>
    - test/unit/roll-high-state-pins.test.js (the header and the Phase 75 Plan 05 / Plan 12 re-pin comments)
    - test/unit/harness/rollHighBaseline.js (PIN_RUNS, pinRun) and tools/roll-high-baseline.mjs (the `pins` mode)
    - test/unit/roll-high-save-compat.test.js
    - test/unit/bot-tactics.test.js (the no-stall seed trios and their declaring comments)
    - test/parity/FIXTURE-INVENTORY.md (the Phase 65 and Phase 73 sections as models, and the end of the file)
    - docs/DIFFICULTY-RETUNE.md (the `## v2.1 roll-high mirror (Phase 73) — bot readout` section as a model, and `## v1.2 retune (Phase 27)`)
    - test/unit/difficulty-retune-ledger.test.js
  </read_first>
  <action>
1. Parity: run `node --test "test/parity/**/*.test.js"`. It must pass with `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exiting 0. If a parity test fails, STOP: report the site and the exposure count from Task 1. Do not regenerate or declare anything without the orchestrator. Also run `node tools/fixture-inventory.mjs` and confirm `node --test test/parity/fixture-inventory.test.js` passes (the roster is unchanged).
2. Run `npm test` and list every failing test.
3. Causation check before any re-pin (scratch script, not committed): for each PIN_RUNS label (and the save-compat run, and each failing bot-tactics seed), replay it and count steps where the old reveal radius (Night Vision as the only waiver) and the new one differ. A label with zero such steps MUST stay byte-identical. If one moved anyway, STOP and report it as a bug. A label with differing steps may move.
4. Re-pin only the causally moved entries:
   - roll-high-state-pins.test.js: regenerate with `node tools/roll-high-baseline.mjs pins`, hashing each moved label identically twice before pinning. Add a comment block in the file's existing style naming DARK-01 (Phase 76), the mechanism (the bot lights a torch or the Amulet in the dark, which now widens the reveal and so the bot's exploration) and which labels stayed byte-identical.
   - roll-high-save-compat.test.js and bot-tactics.test.js: the same discipline. A no-stall seed that now stalls is swapped for the smallest untaken seed that still dies naturally, re-measured live, with a declaring comment (the 75-12 precedent).
   - Any other failing bot-driven test (class-matrix, band-readout, tuning-bot, test/difficulty/*): if the failure is a difficulty band or survival floor, STOP and report the numbers. Do not loosen a band and do not nerf anything (CONTEXT: no compensating nerf; the user judges).
5. Commit the engine and test changes, then take the AFTER readout per the protocol and wait for it. Confirm the BEFORE run finished (its file ends with the readout's final summary line).
6. docs/DIFFICULTY-RETUNE.md: insert `## v2.1 darkness unification (Phase 76) — bot readout` immediately before `## v1.2 retune (Phase 27)`, with the sub-sections named in the interfaces. The Reading states the headline deltas (mean death depth, the reach lines, the floor bands), says the dark got kinder to light carriers on purpose, and says no compensating change was made and the user judges at milestone close.
7. test/parity/FIXTURE-INVENTORY.md: append `## Phase 76: darkness unification and relaunch persistence (DARK-01/02, SAV-06/07) — measured per plan` with an intro line saying each plan records its own subsection (76-05 adds the persistence one), then `### Plan 01 — one darkness waiver (DARK-01): measured`. Include the rule, the predictor (Task 1's exposure counts), the live-scan results (parity pass count, the empty fixture/master diff, the master's `git hash-object`), the moved set (a table of every re-pinned bot pin with before/after hash or seed and the causation count, or "measured zero"), and byte-identical elsewhere.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && node --test test/unit/difficulty-retune-ledger.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
    - `tools/readouts/76-01-before.txt` and `tools/readouts/76-01-after.txt` both exist and are non-empty.
    - `grep -c "(Phase 76)" docs/DIFFICULTY-RETUNE.md` is at least 1, and `node --test test/unit/difficulty-retune-ledger.test.js` passes.
    - `grep -c "## Phase 76" test/parity/FIXTURE-INVENTORY.md` equals 1 and `grep -c "DARK-01" test/parity/FIXTURE-INVENTORY.md` is at least 1.
    - Every re-pinned entry carries a comment naming DARK-01 (`grep -c "DARK-01" test/unit/roll-high-state-pins.test.js` is at least 1 when that file changed).
    - `git diff --quiet <plan-base> -- engine/difficulty.js content/` exits 0 (no compensating change).
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The unification is live, its moved set is measured, traced and declared, and its balance effect is recorded for the user with nothing nerfed.</done>
</task>

</tasks>

<verification>
- revealRadius and mapViewRadius share darkWaived and are pinned to agree across every waiver, tile and darkFor combination.
- Combat's darkness rules, the difficulty dials and content are byte-identical.
- Parity passes with zero fixture edits; every moved bot pin is causally traced and declared; both readouts are committed and recorded.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause):
  - On a dark patch with no light source, only the 3x3 around the party is revealed and shown.
  - Light a torch on a dark patch and walk: squares two away are revealed as you walk, and the explored map stays visible.
  - Use the Amulet of Light on a dark patch: squares three away are revealed as you walk.
  - A Night Vision race (for example Wilmsry) on a dark patch: squares two away are revealed as you walk.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 76 criterion 1 holds: a lit torch, the Amulet or Night Vision widens what a player reveals while walking by the same rule that widens what is rendered, and revealRadius and mapViewRadius never disagree again.
</success_criteria>

<output>
Create `.planning/phases/76-darkness-unification-relaunch-persistence/76-01-SUMMARY.md` when done. Include the plan base, the exposure counts, the causation counts per moved pin, both readouts' headline lines, and a note for 76-02: the exported names (`DARK_WAIVERS`, `darkWaiver`, `darkWaived`) and the waiver keys.
</output>
