---
phase: 76-darkness-unification-relaunch-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/derived.js
  - engine/combat.js
  - engine/phobias.js
  - engine/maze.js
  - test/unit/dark-waiver.test.js
  - test/unit/darkness-filter.test.js
  - test/unit/darkness-vignette.test.js
  - test/unit/rollDirection.test.js
  - test/unit/rollDirection-checks.test.js
  - docs/ROLL-LEDGER.md
  - test/unit/roll-high-state-pins.test.js
  - test/unit/roll-high-save-compat.test.js
  - test/unit/fixtures/roll-high/pre-switch-save.json
  - test/unit/bot-tactics.test.js
  - test/parity/divergence-records.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/DIFFICULTY-RETUNE.md
  - tools/readouts/76-01-before.txt
  - tools/readouts/76-01-after.txt
autonomous: true
requirements:
  - DARK-01
must_haves:
  truths:
    - "DARK-01: engine/derived.js exports ONE darkness-waiver predicate. `darkWaiver(c)` names the waiver holding the dark back (`nightVision`, `amuletLight` or `litTorch`) or returns null; `darkWaived(c)` is `darkWaiver(c) !== null`; `darkLimited(state)` is `inDark(state) && !darkWaived(state.c)`. The waivers are Night Vision (`skill(c, \"Night Vision\")`), a live Amulet of Light (`eff(c, \"light\") > 0`) and a lit torch (`itemEffectActive(c, \"lit\")`), per CONTEXT's darkness-unification decision"
    - "DARK-01 map: `revealRadius(state)` is `(darkLimited(state) ? 1 : 2) + eff(state.c, \"sight\")` and `mapViewRadius(state)` is DARK_VIEW_RADIUS exactly when `darkLimited(state)`, else Infinity. A lit torch now reveals 2 squares as you walk and a live Amulet 3 (its `sight` stays the separate additive); the two radii can never disagree"
    - "Combat too (user ruling after planning, 2026-09-25): every hero-side in-fight dark penalty reads the same `darkLimited`: toHit's dark cap (`Math.min(h, 2)`), the `combatInDark` line, the no-crit-in-the-dark rule, the Darkness phobia's fight-join trigger, and the Darkness phobia's terrain arm (engine/phobias.js#regionActive). A lit torch or a live Amulet now lifts all of them, Night Vision now also stops the Darkness phobia, and Sense Presence (`c.senses`) keeps lifting the cap, the line and the no-crit rule as a fight-only relief layered on top"
    - "The thief's silent strike: no live dark clause exists (foeToHitVs's Silence-in-the-dark term was retired in Phase 38); a grep over engine/ confirms no dark read is left outside `darkLimited` except the physical-darkness reads that are not penalties (the torch's own 'not dark' refusal and fallDark), and derived.js#inDark's JSDoc stops naming the retired clause"
    - "The matrix pins agreement across every waiver (none, Night Vision, live Amulet, lit torch, COOLING torch, COOLING Amulet) x tile (dark, lit) x darkFor (0, 30): revealRadius, mapViewRadius, inViewWindow, toHit's dark cap and the no-crit rule all follow `darkLimited`, pinned by test/unit/dark-waiver.test.js"
    - "DARK-01 precedence (fixed, most-durable-first, the frozen `DARK_WAIVERS`): with more than one waiver live, darkWaiver names Night Vision, then the Amulet, then the torch"
    - "DARK-01 empty: darkWaiver(undefined), darkWaiver(null) and darkWaiver({}) return null and never throw"
    - "DARK-01 adjacency: a torch or Amulet record in its effect phase with `left: 1` waives; the same record in its cooldown phase, or at `left: 0`, does not"
    - "The direction tests and the ledger tell the combat truth: new odds rows show a lit torch and a live Amulet each waive the dark cap and restore crits in the dark, the `[hero-strike:dark-cap]` and `[hero-crit:dark]` ledger rows name every waiver, and test/unit/roll-ledger-sync.test.js stays green"
    - "Measured, not assumed: the parity exposure of both the map and the combat change is predicted from a live replay; any parity fixture that moves is inside the predicted exposure set, declared with before and after in FIXTURE-INVENTORY.md and regenerated alone (a move outside the set is a STOP); every moved bot pin is traced to steps where the old and new rule differ, re-pinned with a DARK-01 comment, and declared"
    - "A 200-seed `tools/tune-difficulty.mjs` readout is taken before and after the combined map and combat change, and both are recorded under a new Phase 76 H2 in docs/DIFFICULTY-RETUNE.md with NO compensating nerf; if a difficulty band or survival floor test fails, the executor stops and reports (CONTEXT)"
  artifacts:
    - path: engine/derived.js
      provides: "DARK_WAIVERS, darkWaiver, darkWaived, darkLimited; revealRadius, mapViewRadius and toHit's dark cap on the one predicate"
      contains: "export function darkLimited"
    - path: engine/combat.js
      provides: "combatInDark, no-crit-in-the-dark and the Darkness phobia's fight-join trigger on darkLimited"
      contains: "darkLimited("
    - path: engine/phobias.js
      provides: "the Darkness phobia's terrain arm on darkLimited"
      contains: "darkLimited("
    - path: test/unit/dark-waiver.test.js
      provides: "the waiver x tile x darkFor agreement matrix for the map and the fight, precedence, empty, adjacency and phobia cases"
    - path: tools/readouts/76-01-before.txt
      provides: "the 200-seed readout at the plan base"
    - path: tools/readouts/76-01-after.txt
      provides: "the 200-seed readout after the map and combat unification"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the Phase 76 H2 and its Plan 01 measured subsection"
      contains: "## Phase 76"
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 76 darkness readout record (before and after)"
      contains: "(Phase 76)"
  key_links:
    - from: engine/derived.js#revealRadius and #mapViewRadius
      to: engine/derived.js#darkLimited
      via: "the one predicate"
      pattern: "darkLimited\\(state\\)"
    - from: engine/derived.js#toHit (the dark cap)
      to: engine/derived.js#darkLimited
      via: "the same predicate in the fight"
      pattern: "darkLimited\\(state\\)"
    - from: engine/combat.js (combatInDark, noCrit, the phobia trigger) and engine/phobias.js#regionActive
      to: engine/derived.js#darkLimited
      via: "one import, one rule"
      pattern: "darkLimited\\("
    - from: engine/movement.js (move, teleport, descend) and engine/state.js#newRun
      to: engine/derived.js#revealRadius
      via: "reveal(floor, revealRadius(state)) — unchanged call sites that now honour the torch and the Amulet"
      pattern: "reveal\\(.*revealRadius\\("
  prohibitions:
    - statement: "MUST NOT let a light source mean one thing on the map and another in a fight: no hero-side dark penalty may keep its own waiver list outside `darkLimited`"
      requirement_id: DARK-01
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT add a compensating nerf or retune any dial to offset the kinder dark; the readout is recorded for the user's judgment (CONTEXT: no compensation unless a band fails, and then stop and report)"
      requirement_id: DARK-01
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT keep the old waiver behaviour alive behind a flag, option or second code path (greenfield), MUST NOT edit test/parity/prototype-master.js.txt, and MUST NOT regenerate or re-pin anything the change did not measurably move"
      requirement_id: DARK-01
      category: transparency
      status: resolved
      verification: test
---

<objective>
Make one darkness-waiver rule govern what the party reveals as it walks, what the map renders, and every in-fight dark penalty (DARK-01 plus the user's 2026-09-25 "combat too" ruling). A lit torch, the Amulet of Light or Night Vision now lights your way as you walk AND lifts the to-hit cap, the no-crit rule and the Darkness phobia in a fight; Sense Presence keeps its fight relief.

Purpose: the 2026-09-21 device report ("It looks like I'm in the dark, but it's not limiting my vision"), and CONTEXT's rulings: "One shared predicate ... waives the dark for BOTH revealRadius AND mapViewRadius" and "A light source means the same in combat too ... It is one predicate for map and combat". Both are declared canon divergences: the 1994 reveal line and in-fight dark rules waived only on Night Vision.

Output: the predicate and every dark read on it, a pinning agreement test, new direction rows and ledger rows, the measured moved set, and before/after readouts recorded for the user.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 76-01 creates or changes.)

- **This plan (76-01):** `DARK_WAIVERS` / `darkWaiver` / `darkWaived` / `darkLimited` in `engine/derived.js`; `revealRadius`, `mapViewRadius`, toHit's dark cap, `combatInDark`, no-crit-in-the-dark and both Darkness-phobia triggers on the one predicate; `test/unit/dark-waiver.test.js`; the flipped Phase 57 characterisations; the new torch/Amulet direction rows and ledger rows; `tools/readouts/76-01-before.txt` / `-after.txt`; the Phase 76 H2 in `docs/DIFFICULTY-RETUNE.md`; the Phase 76 H2 and its Plan 01 subsection in `test/parity/FIXTURE-INVENTORY.md`; any measured, declared bot-pin re-pins and fixture regenerations
- 76-02: `src/browser/darknessView.js` collapsed (two-way `waiverFor`, one-radius vignette); the DARK chip, its tap card and the darkness explain copy tell the map and fight truth; `draw()` paints waived dark tiles with a faint lit tint (`MAP_PALETTE.floorDarkLit` / `waterDarkLit`); `test/unit/dark-surfaces.test.js`; `docs/TERRAIN.md` and `docs/SHELL-MODULES.md`
- 76-03: `engine/saveState.js` resumes a validated `combat`, `store`, `pendingFind`, `pendingHazard`, `pendingTile` and `pendingJoiner`; conditional foe-effect and round-timer clears; `resumeEventsFor` with `fightResumed` / `storeResumed` narration; `test/unit/save-resume.test.js`; the flipped reset-on-load pins; `test/persistence/resume-mid-encounter.test.js` rewritten
- 76-04: `src/browser/engineAdapter.js` one-shot `takeBootResumeEvents()`; `test/roundtrip/resume-roundtrip.test.js` (the generic every-field walk and resume determinism); the adapter-level relaunch proofs
- 76-05: the Oracle resume line at boot in `mazeworld.html`; `test/unit/shell-resume-line.test.js`; the persistence subsection of FIXTURE-INVENTORY's Phase 76 section; the Phase 76 device checklist

## Flagged assumptions (surfaced, not silently decided)

- **DARK-01 edge probe: unclassified, reviewed manually (left unresolved by the probe).** Assumed the edge surface is (a) the waiver x tile x darkFor matrix, now for the fight reads too, (b) the effect-phase/cooldown-phase boundary of a torch or Amulet record, and (c) the Amulet's `sight` staying additive on top of the waiver (a live Amulet in the dark reveals radius 3). All three are pinned in `test/unit/dark-waiver.test.js`.
- **Sense Presence is layered, not merged (reading of "one predicate for map and combat").** The one predicate is the light waiver (`darkLimited`), used by the map and the fight alike. Sense Presence can be cast before a fight and lasts until the next fight ends, so putting it inside the predicate would also make it widen the reveal while walking, a map change nobody asked for. It therefore stays a fight-only relief read beside the predicate (`darkLimited(state) && !c.senses`) at the three sites that honour it today (the cap, the line, the no-crit rule). If the user wants Sense Presence to light the map too, it is one entry in `DARK_WAIVERS`.
- **"The thief's silent strike" has no live dark clause.** The Silence-in-the-dark term was retired in Phase 38 (derived.js#foeToHitVs's JSDoc says so); only the stale inDark JSDoc still names it. The executor confirms by grep and records it; nothing to route.
- **The dark phobia means both of its triggers:** the fight-join check in engine/combat.js and the terrain arm in engine/phobias.js (which arms fear for the next fight). Both now read `darkLimited`, so lighting a torch inside a dark region reads as leaving the dark, and a torch burning out there re-arms it on the next step. Night Vision now also stops the Darkness phobia (the ruling: "Night Vision and Sense Presence keep lifting them too").
- **Party members are unaffected**: memberToHit has no dark term today, so there is nothing to route.
- **The waiver key names are the shell's existing ones** (`nightVision`, `amuletLight`, `litTorch`, from `mazeworld.html#WAIVER_LABEL`), so 76-02 needs no label migration.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-darkness-unification-relaunch-persistence/76-CONTEXT.md
@.planning/ROADMAP.md

<interfaces>
**Run order:** Phases 75, 75.1, 75.2 and 75.3 are merged before this executes. Read their SUMMARYs for anything they did to engine/derived.js, engine/combat.js and engine/phobias.js (75.1-03/04/06, 75.2-01/02 and 75.3-04 edit derived.js and combat.js: toHit gains heroBlind and size terms, playerStrike gains fumble branches). Reference functions by name; line numbers have moved.

engine/derived.js (as of the 75 close):
- `skill = (c, n) => !!(c.skills && c.skills[n])` throws on a missing `c`, so darkWaiver must guard `c` first.
- `eff(c, key)` sums `act.eff[key]` over `liveItemEffects(c)`; `liveItemEffects` keeps only `item:` timer records with `phase === "effect"` and `left > 0`. The Amulet of Light's activation is kind `glow` with `eff: { sight: 1, light: 1 }` (content/treasure-tables.js); the Torch's is kind `lit` (content/tools.js).
- `itemEffectActive(c, kind)` is `liveItemEffects(c).some(e => e.act.kind === kind)`.
- `inDark(state)`: the current tile's `.dark` flag OR `state.c.darkFor > 0` (physical darkness). Its JSDoc lists the consumers, including a "foeToHitVs's silent-thief clause" that no longer exists.
- `revealRadius(state)`: today waives only on Night Vision (the ported 1994 line).
- `mapViewRadius(state)`: today waives on Night Vision, `eff(c, "light") > 0` and `itemEffectActive(c, "lit")`. Its preceding block comment (the Phase 41 DARK_VIEW_RADIUS note) claims the waiver set is "the SAME one revealRadius ... already use[s]", which was never true.
- `toHit(state)`: its last terms include `if (inDark(state) && !skill(c, "Night Vision") && !c.senses) h = Math.min(h, 2);` (75.1 may have added a heroBlind term; keep every other term and its order).

engine/combat.js:
- `fight` (the join): `phobiaCondition = c.phobiaType === type || (c.phobia === "Darkness" && inDark(state)) || nearDeathPanic || armed;` then a Hardiness d2 drawn only when phobiaCondition holds, then `if (inDark(state) && !skill(c, "Night Vision") && !c.senses) events.push({ type: "combatInDark" });`.
- `playerStrike`: `const noCrit = c.sub === "Guard" || c.sub === "Soldier" || (inDark(state) && !skill(c, "Night Vision") && !c.senses) || eff(c, "noCrit") > 0;`.
- Note: with a waiver live the phobia no longer fires, so its gated Hardiness d2 is not drawn; that changes the rng stream of that fight. It is part of the declared, measured shift, not a bug.

engine/phobias.js#regionActive(state, phobia): `if (phobia === "Darkness") return inDark(state);` drives checkTerrainPhobias' enter/leave toggle, which arms fear (fearArmed) for the next fight.

Physical-darkness reads that are NOT penalties and stay on inDark: engine/items.js (a torch used while not dark is refused and not consumed), engine/encounters.js#fallDark (a lit torch resists a Darkness result), engine/movement.js (the Amulet's light dispels c.darkFor), tools/lib/tuning-bot.mjs (lights a torch when in the dark).

engine/maze.js#reveal(floor, radius): its JSDoc says every caller passes revealRadius "matching the prototype's dark/Night Vision/sight behavior exactly". Reword it.

Existing tests that pin today's behaviour and must flip or stay:
- test/unit/darkness-filter.test.js: the three "Phase 57 characterisation" tests for darkFor WITH a lit torch, WITH a live Amulet and WITH Night Vision. The torch case asserts revealRadius 1 (becomes 2); the Amulet case asserts 2 (becomes 3).
- test/unit/darkness-vignette.test.js: one sanity assertion that revealRadius stays at 1 under the torch waiver (becomes 2). Change ONLY that assertion and its message; 76-02 reworks the rest.
- test/unit/sense-presence.test.js (combatInDark honours senses) and test/unit/movement.test.js's two revealRadius tests stay true.
- test/unit/phobia-triggers.test.js, test/unit/afraid.test.js and test/unit/combat.test.js pin Darkness-phobia and combatInDark behaviour: any case that combined a waiver with the dark flips, with a comment.

Direction tests and ledger:
- test/unit/rollDirection.test.js: `[hero-strike:night-vision]` and `[hero-strike:senses]` build a Fighter/Soldier in a fight on a `.dark` tile and `assertBonus(faceOdds(with), faceOdds(without))`; `[hero-crit:dark]` asserts zero crits with a Rapier on a dark tile. Model the new rows on them: `[hero-strike:torch]`, `[hero-strike:amulet-light]` (each waives the cap; assertBonus), and `[hero-crit:light]` (a lit torch in the dark restores the Rapier's crits: wins above 0).
- docs/ROLL-LEDGER.md: the `## Modifier ledger` table rows `[hero-strike:dark-cap]`, `[hero-strike:night-vision]`, `[hero-strike:senses]`, `[hero-crit:dark]`. test/unit/roll-ledger-sync.test.js requires every non-N/A ledger id to have a bracketed test title in rollDirection.test.js or rollDirection-checks.test.js, and every such title to have a ledger row.

Bot surfaces likely to move (the tuning bot lights a carried torch, or a ready worn Amulet, whenever it stands in the dark: tools/lib/tuning-bot.mjs#chooseFieldItem; with the ruling, that light now also lifts the fight penalties):
- test/unit/roll-high-state-pins.test.js (PINNED hashes over PIN_RUNS; regenerate with `node tools/roll-high-baseline.mjs pins`; the file's own comments show the Phase 75 Plan 05 and Plan 12 re-pin precedent).
- test/unit/roll-high-save-compat.test.js: it replays the fixture's recorded `dispatched` actions against its `expected` { dead, depth, hash } in test/unit/fixtures/roll-high/pre-switch-save.json. Only `expected` may be re-recorded for this deliberate rule change, from a live replay; the fixture's `save`, `note`, `baseCommit` and `rollLogicUnchangedSince` never change, and `node tools/roll-high-baseline.mjs save` rewrites the whole fixture, so never use it here (the 75.2-01 precedent).
- test/unit/bot-tactics.test.js (the no-stall seed trios; 75-12 swapped a stalled seed for the smallest untaken seed that still dies naturally).
- Any other failing bot-driven test (class-matrix, band-readout, tuning-bot, test/difficulty/*): measure; do not loosen a band.

Parity: a declared move follows the 75-05 precedent: the divergence records inside the moved fixture file, the declared-holder constant in test/parity/divergence-records.test.js (add a Phase 76 set beside `RULES75_EXPECTED_HOLDERS`, empty if nothing moved), and the FIXTURE-INVENTORY subsection.

**Readout protocol (the 75-12 precedent):** record `git rev-parse HEAD` as the plan base before the first edit.
- BEFORE: `git worktree add --detach <session scratchpad>/76-01-base <plan-base>`; from that directory start `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, stdout to this worktree's `tools/readouts/76-01-before.txt` (absolute path). Remove the scratch worktree when done.
- AFTER: once the engine edit is committed, run the same command here into `tools/readouts/76-01-after.txt` with run_in_background, and wait for it.

docs/DIFFICULTY-RETUNE.md: `test/unit/difficulty-retune-ledger.test.js` requires `## v1.2 retune (Phase 27)` to stay the LAST H2. Insert the new H2 immediately before it, shaped like the Phase 73 section (`### Change under measurement`, `### Parameters`, `### BEFORE — commit <sha>`, `### AFTER — commit <sha>`, `### Reading`).

test/parity/FIXTURE-INVENTORY.md: sections are appended in phase order; the 75.x sections are last. Model the new H2 on the Phase 65 / Phase 73 "measured" sections (The rule, The predictor, The live-scan results, Moved set, Byte-identical elsewhere).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the plan base, start the BEFORE readout, and predict the parity exposure</name>
  <files>tools/readouts/76-01-before.txt</files>
  <read_first>
    - tools/tune-difficulty.mjs (the header's usage lines)
    - tools/lib/tuning-bot.mjs (chooseFieldItem: when the bot lights a torch or uses the Amulet)
    - test/parity/harness/comparables.js and test/parity/divergence-records.test.js (the fixture replay shape, for a scratch exposure scan)
    - .planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-12-SUMMARY.md and 75-05-SUMMARY.md (the readout, re-pin and declared-fixture precedents)
  </read_first>
  <action>
1. Record `git rev-parse HEAD` as the plan base in your notes (it goes in the SUMMARY and both records).
2. Start the BEFORE readout exactly as the interfaces' readout protocol says: a detached scratch worktree at the plan base, `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, stdout to this worktree's `tools/readouts/76-01-before.txt`. Do not wait for it here; Task 3 checks it finished.
3. Exposure predictor (a scratch script in the session scratchpad, never committed): replay every parity fixture scenario through the parity harness and count, per fixture file and per scenario, (a) states where the hero is in the dark with a live Amulet light or a lit torch and no Night Vision (the map and fight rule both change there), and (b) fight joins where the hero has the Darkness phobia, is in the dark, and has any waiver including Night Vision (the phobia rule changes there). Those are the only states where old and new behaviour differ. Record the counts; predicted zero everywhere.
4. Record the committed PINNED labels in test/unit/roll-high-state-pins.test.js so Task 3 can compare.
  </action>
  <verify>
    <automated>git rev-parse HEAD && test -f tools/readouts/76-01-before.txt</automated>
  </verify>
  <acceptance_criteria>
    - `tools/readouts/76-01-before.txt` exists (the background run is writing to it).
    - The SUMMARY notes carry the plan base SHA and both exposure counts per parity fixture scenario.
    - No file under engine/ has changed yet (`git diff --quiet -- engine/` exits 0).
  </acceptance_criteria>
  <done>The baseline readout is running from the untouched engine, and the parity exposure of both the map and the fight change is predicted from a live replay, not assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One darkness-waiver predicate for the map and the fight (DARK-01, "combat too")</name>
  <files>engine/derived.js, engine/combat.js, engine/phobias.js, engine/maze.js, test/unit/dark-waiver.test.js, test/unit/darkness-filter.test.js, test/unit/darkness-vignette.test.js, test/unit/rollDirection.test.js, test/unit/rollDirection-checks.test.js, docs/ROLL-LEDGER.md</files>
  <read_first>
    - engine/derived.js (skill, eff, liveItemEffects, itemEffectActive, inDark and its JSDoc, revealRadius, DARK_VIEW_RADIUS and its block comment, mapViewRadius, inViewWindow, toHit, foeToHitVs's JSDoc on the retired Silence clause)
    - engine/combat.js (fight's phobiaCondition, its Hardiness d2 and the combatInDark push; playerStrike's noCrit)
    - engine/phobias.js (regionActive, checkTerrainPhobias)
    - engine/maze.js (reveal's JSDoc)
    - content/tools.js (the Torch activation) and content/treasure-tables.js (the Amulet of Light row and its activation)
    - test/unit/darkness-filter.test.js (the grid helpers and the three Phase 57 characterisation tests)
    - test/unit/darkness-vignette.test.js (the torch-waiver sanity assertion only)
    - test/unit/rollDirection.test.js (`[hero-strike:night-vision]`, `[hero-strike:senses]`, `[hero-crit:dark]` and their harness helpers)
    - test/unit/roll-ledger-sync.test.js and docs/ROLL-LEDGER.md (the Modifier ledger rows for the dark cap, Night Vision, senses and the dark crit)
    - test/unit/phobia-triggers.test.js and test/unit/sense-presence.test.js (the Darkness phobia and combatInDark cases)
  </read_first>
  <behavior>
    - darkWaiver: no waiver gives null; Night Vision gives "nightVision"; a live Amulet (an `item:Amulet of Light` record, phase effect, left above 0) gives "amuletLight"; a lit torch (`item:Torch`, phase effect, left above 0) gives "litTorch"; Night Vision plus Amulet plus torch gives "nightVision"; Amulet plus torch gives "amuletLight". `c.senses` alone gives null (it is not a light).
    - darkWaiver(undefined), darkWaiver(null) and darkWaiver({}) return null without throwing; `DARK_WAIVERS` deep-equals ["nightVision", "amuletLight", "litTorch"] and is frozen.
    - Adjacency: an effect-phase torch record with left 1 waives; a cooldown-phase torch record and a left-0 record do not; the same for the Amulet.
    - Map matrix: for every waiver in {none, Night Vision, live Amulet, lit torch, cooling torch, cooling Amulet} x tile {dark, lit} x darkFor {0, 30}: revealRadius equals `(darkLimited ? 1 : 2) + eff(c, "sight")`, mapViewRadius equals `darkLimited ? DARK_VIEW_RADIUS : Infinity`, and inViewWindow at a seen cell two squares away equals `!darkLimited`.
    - Concrete map values: darkFor 30 on a lit tile with a lit torch gives revealRadius 2 and mapViewRadius Infinity; with a live Amulet, revealRadius 3; with no waiver, 1 and 1.
    - Fight matrix (same waivers, a fixed Fighter in a fight): toHit equals the lit-tile toHit whenever `darkLimited` is false or `c.senses` is live, and equals `Math.min(litToHit, 2)` otherwise.
    - fight (the join) on a dark tile: with a lit torch, a live Amulet or Night Vision, no `combatInDark` event; with none of them, `combatInDark` fires (and, as today, not with senses up).
    - A Darkness-phobic hero joining a fight on a dark tile is afraid with no waiver, and not afraid with a lit torch, a live Amulet or Night Vision.
    - regionActive(state, "Darkness") is true in the dark with no waiver and false with a lit torch; checkTerrainPhobias arms fear on entering an unlit dark region and not on entering it with a torch lit.
    - Direction rows: `[hero-strike:torch]` and `[hero-strike:amulet-light]` each assertBonus over the no-waiver dark baseline; `[hero-crit:light]` shows a Rapier in the dark with a lit torch landing crits (wins above 0) where `[hero-crit:dark]` shows none.
  </behavior>
  <action>
1. Write the tests first: test/unit/dark-waiver.test.js with every behaviour above except the direction rows (copy darkness-filter.test.js's grid helpers locally, as that file's header documents), and the three new direction rows in test/unit/rollDirection.test.js modelled on `[hero-strike:night-vision]` / `[hero-crit:dark]`. Run them and watch them fail.
2. engine/derived.js, per CONTEXT's darkness-unification decision and its "combat too" ruling:
   - Add the frozen export `DARK_WAIVERS` with the three keys in the precedence order above (Night Vision is innate and most durable; the Amulet is a worn effect; the torch is the shortest-lived; carry the Phase 57 most-durable-first rationale into the JSDoc).
   - Add `darkWaiver(c)` (null for a missing or non-object `c`; otherwise the first live of Night Vision, the Amulet's light and a lit torch, in that order, else null), `darkWaived(c)`, and `darkLimited(state)` (in the dark and not waived). All pure, zero rng, no mutation.
   - revealRadius and mapViewRadius read darkLimited (revealRadius keeps `+ eff(state.c, "sight")` as the separate additive). toHit's dark cap becomes `darkLimited(state) && !c.senses`, keeping every other toHit term and its order.
   - JSDoc on each: DARK-01 (Phase 76, user rulings 2026-09-22 and 2026-09-25), a declared canon divergence (the 1994 reveal line and fight rules waived only on Night Vision; a torch or the Amulet now lights the way and the fight). Rewrite the Phase 41 DARK_VIEW_RADIUS block comment so it no longer claims revealRadius already shared the waiver set, and update inDark's JSDoc: it is physical darkness; the penalties read darkLimited; drop the retired silent-thief clause from its consumer list.
3. engine/combat.js: import darkLimited. The combatInDark push and playerStrike's no-crit dark term become `darkLimited(state) && !c.senses`; the Darkness phobia term in fight's phobiaCondition becomes `c.phobia === "Darkness" && darkLimited(state)`. Update the RULES-05 comments beside them. Change nothing else in combat.js.
4. engine/phobias.js#regionActive: the Darkness branch returns `darkLimited(state)`, with a DARK-01 comment (a torch lit inside a dark region reads as leaving it; one burning out re-arms on the next step).
5. Grep engine/ for every remaining `inDark(` and "Night Vision" read and confirm each is either on darkLimited or one of the physical-darkness reads the interfaces list; record the list in the SUMMARY. Confirm no silent-thief dark clause exists.
6. engine/maze.js: reword reveal's JSDoc sentence: callers pass revealRadius, which since Phase 76 (DARK-01) also waives on a lit torch or a live Amulet.
7. Flip the tests: the three Phase 57 characterisations in test/unit/darkness-filter.test.js assert the unified answers (torch: revealRadius 2; Amulet: 3; Night Vision: 2; mapViewRadius Infinity for all three) and are retitled "Phase 76 (DARK-01): ..."; darkness-vignette.test.js's single torch sanity assertion becomes revealRadius 2; any phobia-triggers / afraid / combat test that combined a waiver with the dark flips with a DARK-01 comment. rollDirection-checks.test.js's climb and leap builds use "Darkness" as a neutral phobia on a lit tile; confirm they stay green, and edit only if one breaks.
8. docs/ROLL-LEDGER.md: add Modifier ledger rows for `[hero-strike:torch]`, `[hero-strike:amulet-light]` and `[hero-crit:light]` (terrain/item source, the plain-language claim, the direction, the Phase 73 reading, verdict OK, pointing at DARK-01 Phase 76), and reword `[hero-strike:dark-cap]` and `[hero-crit:dark]` so the cap and the crit ban apply "in the dark with no light waiver (Night Vision, a lit torch, a live Amulet) and no Sense Presence".
  </action>
  <verify>
    <automated>node --test test/unit/dark-waiver.test.js test/unit/darkness-filter.test.js test/unit/darkness-vignette.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-ledger-sync.test.js test/unit/sense-presence.test.js test/unit/phobia-triggers.test.js test/unit/movement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/dark-waiver.test.js` passes, including the map and fight matrices, precedence, empty, adjacency and both phobia cases.
    - `grep -c "export function darkWaiver" engine/derived.js`, `grep -c "export function darkWaived" engine/derived.js`, `grep -c "export function darkLimited" engine/derived.js` and `grep -c "export const DARK_WAIVERS" engine/derived.js` each equal 1.
    - `grep -c "darkLimited(state)" engine/derived.js` is at least 3 (revealRadius, mapViewRadius, toHit) and `grep -c "darkLimited(state)" engine/combat.js` is at least 3 (combatInDark, noCrit, the phobia trigger); `grep -c "darkLimited(state)" engine/phobias.js` is at least 1.
    - `node --test test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-ledger-sync.test.js` passes, and `grep -c "hero-strike:torch" docs/ROLL-LEDGER.md` is at least 1.
    - `git diff --quiet <plan-base> -- engine/difficulty.js content/` exits 0.
    - `node --test test/unit/darkness-filter.test.js test/unit/darkness-vignette.test.js test/unit/sense-presence.test.js test/unit/phobia-triggers.test.js test/unit/movement.test.js` passes.
  </acceptance_criteria>
  <done>A torch, the Amulet or Night Vision lifts the dark on the map and in the fight by one rule; Sense Presence keeps its fight relief; the ledger and direction rows say so.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the moved set, declare it, take the AFTER readout and record both</name>
  <files>test/unit/roll-high-state-pins.test.js, test/unit/roll-high-save-compat.test.js, test/unit/fixtures/roll-high/pre-switch-save.json, test/unit/bot-tactics.test.js, test/parity/divergence-records.test.js, test/parity/FIXTURE-INVENTORY.md, docs/DIFFICULTY-RETUNE.md, tools/readouts/76-01-after.txt</files>
  <read_first>
    - test/unit/roll-high-state-pins.test.js (the header and the Phase 75 Plan 05 / Plan 12 re-pin comments)
    - test/unit/harness/rollHighBaseline.js (PIN_RUNS, pinRun) and tools/roll-high-baseline.mjs (the `pins` mode)
    - test/unit/roll-high-save-compat.test.js and the metadata of test/unit/fixtures/roll-high/pre-switch-save.json
    - test/unit/bot-tactics.test.js (the no-stall seed trios and their declaring comments)
    - test/parity/divergence-records.test.js (RULES75_EXPECTED_HOLDERS and its guard)
    - test/parity/FIXTURE-INVENTORY.md (the Phase 65, Phase 73 and Phase 75 Plan 05 sections as models, and the end of the file)
    - docs/DIFFICULTY-RETUNE.md (the `## v2.1 roll-high mirror (Phase 73) — bot readout` section as a model, and `## v1.2 retune (Phase 27)`)
    - test/unit/difficulty-retune-ledger.test.js
  </read_first>
  <action>
1. Parity: run `node --test "test/parity/**/*.test.js"`.
   - If it passes, `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` must exit 0 (a measured zero); add an empty Phase 76 holder set beside RULES75_EXPECTED_HOLDERS in divergence-records.test.js with its guard, following that constant's pattern.
   - If a scenario fails, check it against Task 1's exposure set. A failing scenario inside the set is a declared move: regenerate ONLY that fixture's affected records following the 75-05 precedent (the divergence records inside the fixture file, the Phase 76 holder set naming exactly those holders), with before and after values. A failing scenario outside the set is a STOP: report it and declare nothing.
   - Also run `node tools/fixture-inventory.mjs` and confirm `node --test test/parity/fixture-inventory.test.js` passes (the roster is unchanged).
2. Run `npm test` and list every failing test.
3. Causation check before any re-pin (scratch script, not committed): for each PIN_RUNS label, the save-compat replay and each failing bot-tactics seed, replay it and count steps where the old and new rules differ (in the dark with a live Amulet or lit torch and no Night Vision, or a Darkness-phobic join with any waiver). A label with zero such steps MUST stay byte-identical; if one moved anyway, STOP and report it as a bug. A label with differing steps may move.
4. Re-pin only the causally moved entries:
   - roll-high-state-pins.test.js: regenerate with `node tools/roll-high-baseline.mjs pins`, hashing each moved label identically twice before pinning. Add a comment block in the file's existing style naming DARK-01 (Phase 76), the mechanism (the bot lights a torch or the Amulet in the dark, which now widens the reveal and lifts the fight's dark cap and crit ban) and which labels stayed byte-identical.
   - roll-high-save-compat: re-record only the fixture's `expected` from a live replay (see the interfaces), with the reason in the test's comment.
   - bot-tactics.test.js: a no-stall seed that now stalls is swapped for the smallest untaken seed that still dies naturally, re-measured live, with a declaring comment (the 75-12 precedent).
   - Any other failing bot-driven test (class-matrix, band-readout, tuning-bot, test/difficulty/*): if the failure is a difficulty band or survival floor, STOP and report the numbers. Do not loosen a band and do not nerf anything (CONTEXT: no compensation unless a band fails, and then stop and report).
5. Commit the engine and test changes, then take the AFTER readout per the protocol and wait for it. Confirm the BEFORE run finished (its file ends with the readout's final summary line).
6. docs/DIFFICULTY-RETUNE.md: insert `## v2.1 darkness unification (Phase 76) — bot readout` immediately before `## v1.2 retune (Phase 27)`, with the sub-sections named in the interfaces. The change under measurement names both halves (the reveal and render waiver, and the fight's cap, crit ban and Darkness phobia on the same waiver). The Reading states the headline deltas (mean death depth, the reach lines, the floor bands), says the dark got kinder to light carriers on purpose, and says no compensating change was made and the user judges at milestone close.
7. test/parity/FIXTURE-INVENTORY.md: append `## Phase 76: darkness unification and relaunch persistence (DARK-01/02, SAV-06/07) — measured per plan` with an intro line saying each plan records its own subsection (76-05 adds the persistence one), then `### Plan 01 — one darkness waiver for the map and the fight (DARK-01): measured`. Include the rule, the predictor (Task 1's two exposure counts), the live-scan results (parity pass count, the fixture/master diff, the master's `git hash-object`), the moved set (every regenerated fixture record and every re-pinned bot pin with before/after and the causation count, or "measured zero"), and byte-identical elsewhere.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && node --test test/unit/difficulty-retune-ledger.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet <plan-base> -- test/parity/prototype-master.js.txt` exits 0, and every changed file under test/parity/fixtures is named in the FIXTURE-INVENTORY Plan 01 moved-set table (none when the diff is empty).
    - `tools/readouts/76-01-before.txt` and `tools/readouts/76-01-after.txt` both exist and are non-empty.
    - `grep -c "(Phase 76)" docs/DIFFICULTY-RETUNE.md` is at least 1, and `node --test test/unit/difficulty-retune-ledger.test.js` passes.
    - `grep -c "## Phase 76" test/parity/FIXTURE-INVENTORY.md` equals 1 and `grep -c "DARK-01" test/parity/FIXTURE-INVENTORY.md` is at least 1.
    - Every re-pinned entry carries a comment naming DARK-01 (`grep -c "DARK-01" test/unit/roll-high-state-pins.test.js` is at least 1 when that file changed).
    - `git diff --quiet <plan-base> -- engine/difficulty.js content/` exits 0 (no compensating change).
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The unification is live on the map and in the fight, its moved set is measured, traced and declared, and its balance effect is recorded for the user with nothing nerfed.</done>
</task>

</tasks>

<verification>
- One predicate (`darkLimited`, built on `darkWaiver`) drives the reveal, the render window, toHit's dark cap, combatInDark, the dark crit ban and both Darkness-phobia triggers; Sense Presence stays a fight-only relief beside it.
- The direction rows and the ledger match the new rule; the difficulty dials and content are byte-identical.
- Parity moves only inside the predicted exposure set (declared) or not at all; every moved bot pin is causally traced and declared; both readouts are committed and recorded.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause):
  - On a dark patch with no light source, only the 3x3 around the party is revealed and shown.
  - Light a torch on a dark patch and walk: squares two away are revealed as you walk, and the explored map stays visible.
  - Use the Amulet of Light on a dark patch: squares three away are revealed as you walk.
  - Start a fight on a dark patch with no light: "You cannot see what you are fighting." and the odds are capped. Start one with a torch lit: no such line, and the strike odds match a lit square.
  - A Darkness-phobic hero entering a dark patch with a torch lit is not frightened; with no light they are.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 76 criterion 1 holds (a lit torch, the Amulet or Night Vision widens what a player reveals while walking by the same rule that widens what is rendered; revealRadius and mapViewRadius never disagree), and the user's "combat too" ruling holds: the same light lifts the in-fight dark penalties.
</success_criteria>

<output>
Create `.planning/phases/76-darkness-unification-relaunch-persistence/76-01-SUMMARY.md` when done. Include the plan base, the exposure counts, the dark-read inventory from Task 2 step 5, the causation counts per moved pin, any declared fixture move, both readouts' headline lines, and a note for 76-02: the exported names (`DARK_WAIVERS`, `darkWaiver`, `darkWaived`, `darkLimited`), the waiver keys, and that Sense Presence is a separate fight relief (the darkness explain copy must say so).
</output>
