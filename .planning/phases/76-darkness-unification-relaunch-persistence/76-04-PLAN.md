---
phase: 76-darkness-unification-relaunch-persistence
plan: 04
type: execute
wave: 2
depends_on:
  - 76-03
files_modified:
  - src/browser/engineAdapter.js
  - test/unit/engineAdapter.test.js
  - test/persistence/resume-mid-encounter.test.js
  - test/roundtrip/resume-roundtrip.test.js
autonomous: true
requirements:
  - SAV-06
  - SAV-07
must_haves:
  truths:
    - "boot() computes `resumeEventsFor(state)` once when it rehydrates a save and exposes it through a one-shot `takeBootResumeEvents()` (a second call returns null), mirroring takeBootWornReport; a fresh-run boot, a corrupt-save fallback, initRun and startNewRun leave it null"
    - "Every dispatch persists the live fight: after each combat action dispatched through the adapter (the fight join, attack rounds, a spell, a failed flee), the flushed save's `combat` JSON-equals the live `getState().combat`, so Save & Quit (which writes nothing) and a force-close both leave the latest fight on disk (CONTEXT: 'Confirm the save after each action includes the live combat')"
    - "A relaunch (boot from that save) after any round resumes the identical fight: same foes, HP, round, target, hero effects and timers; a `move` dispatched right after is refused and the party's position is unchanged, so force-closing cannot escape a fight"
    - "A store purchase followed by a relaunch keeps the sold line sold, the gold spent and every other line and price as they were"
    - "SAV-06 boundary: a save taken at the encounter step (pending true, round 1) resumes to the encounter with pending true; a save after round N resumes at round N; the action that kills the last foe saves combat null, so the relaunch shows the spoils and no fight; a hero effect with one round left resumes with one round left and expires on the same foe turn as the unbroken fight"
    - "SAV-06 precision: for every live combat, store and pending decision in the corpus, every key path survives serialize -> validateSave -> rehydrate with Object.is equality (no Infinity, NaN, undefined or -0 reaching a save, which JSON would silently turn into null, a dropped key or 0), and the rng cursor round-trips exactly"
    - "The generic walk: test/roundtrip/resume-roundtrip.test.js compares every key path of the live state against the loaded state (combat, c, party, store and the pending fields; only the out-of-scope pendingJoiner and the wall-clock fields are excluded), across the parity combat/magic/economy/encounters fixture scenarios at every action and a tuning-bot corpus (solo, party, forced casters and thieves, deep starts that meet elites), naming any mismatching path; it asserts non-vacuity (a floor on combat states seen, at least one with allies, at least one open store)"
    - "Resume determinism: for every sampled resumable state, the same next action applied to the live state and to the loaded state gives deep-equal next states and events (the fight continues on the same dice)"
    - "Old-shape fight: a combat reduced to only the keys startCombat's literal writes (every optional field added since, by 75.x or later, stripped) resumes and survives `fight`, `attack` and `flee` without throwing"
    - statement: "SAV-07 concurrency: a force-close between a dispatch and its storage write resumes from the previous action's save, so at most the last action is lost and a half-written save never exists (storage.js's per-key queue writes whole JSON strings; nativeChrome.js flushes on pause). The kill timing cannot be reproduced under node; confirm on the device"
      verification: backstop
  artifacts:
    - path: src/browser/engineAdapter.js
      provides: "the one-shot boot resume events"
      contains: "export function takeBootResumeEvents"
    - path: test/roundtrip/resume-roundtrip.test.js
      provides: "the every-key-path walk, precision, non-vacuity, resume determinism and old-shape fight proofs"
    - path: test/persistence/resume-mid-encounter.test.js
      provides: "the per-action save, relaunch-each-round, move-refused, store-purchase and boundary proofs through the real adapter"
  key_links:
    - from: src/browser/engineAdapter.js#boot
      to: engine/saveState.js#resumeEventsFor
      via: "computed once on the rehydrate path"
      pattern: "resumeEventsFor\\("
    - from: src/browser/engineAdapter.js#persist (every dispatch)
      to: the stored save
      via: "serializeRun(currentState) carries the live combat and store"
      pattern: "serializeRun\\(currentState\\)"
  prohibitions:
    - statement: "MUST NOT let a force-close or relaunch escape a live fight: after the relaunch the fight is up and movement is refused"
      requirement_id: SAV-06
      category: fairness
      status: resolved
      verification: test
    - statement: "MUST NOT restore the UI animation state (beats): the resumed fight starts at the player's turn with no replayed damage lines"
      requirement_id: SAV-06
      category: values
      status: resolved
      verification: test
---

<objective>
Prove the relaunch end to end (SAV-06, SAV-07) and hand the shell its resume line. The adapter gains a one-shot `takeBootResumeEvents()`. The real adapter with fake storage proves every combat action is saved and that a relaunch after any round resumes the identical fight. A generic round-trip test walks every field of every fight, store and pending decision a real run produces, so the 75.x fields are covered without naming them.

Purpose: CONTEXT: "A resumed fight picks up at a clean round boundary ... with foes, HP, the round number, active effects ... cooldowns and foe order all intact", and "force-closing can no longer escape one". The orchestrator asked for a generic proof rather than a per-field one.

Output: the adapter hook, the adapter-level relaunch proofs, and test/roundtrip/resume-roundtrip.test.js.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 76-04 creates or changes.)

- 76-01: `DARK_WAIVERS` / `darkWaiver` / `darkWaived`; both radii on one predicate; `test/unit/dark-waiver.test.js`; readouts; the Phase 76 H2s in DIFFICULTY-RETUNE.md and FIXTURE-INVENTORY.md; `docs/TERRAIN.md`
- 76-02: the chip, tap card, vignette and per-tile tint on the one predicate; `MAP_PALETTE.floorDarkLit` / `waterDarkLit`; `test/unit/dark-surfaces.test.js`; `docs/SHELL-MODULES.md`
- 76-03: the load resumes a validated `combat`, `store`, `pendingFind`, `pendingHazard` and `pendingTile`; `resumeEventsFor` with its Oracle lines; `test/unit/save-resume.test.js`; flipped reset pins
- **This plan (76-04):** `takeBootResumeEvents()` in `src/browser/engineAdapter.js`; the adapter-level relaunch proofs in `test/persistence/resume-mid-encounter.test.js` and `test/unit/engineAdapter.test.js`; `test/roundtrip/resume-roundtrip.test.js`
- 76-05: the Oracle resume line at boot in `mazeworld.html`; `test/unit/shell-resume-line.test.js`; the persistence subsection of FIXTURE-INVENTORY's Phase 76 section; the Phase 76 device checklist

## Flagged assumptions (surfaced, not silently decided)

- **Corpus size is a runtime budget.** The corpus is sized so `node --test test/roundtrip/resume-roundtrip.test.js` stays under about 60 seconds; sample states (for example every resumable state up to a per-run cap) rather than shrink the configuration list. Record the counts in the SUMMARY.
- **A JSON-unsafe value found in a live fight is an engine finding, not a test tweak.** If the walk finds Infinity, NaN, undefined or -0 in a live combat, fix it at its engine source only if the fix is representation-only (every reader treats the new value identically) and moves no fixture; otherwise STOP and report it. Never normalise it away in the test or the loader.
- **pendingJoiner stays out** (not in CONTEXT's list; 76-03 flags it to the user), so the walk excludes that one key.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-darkness-unification-relaunch-persistence/76-CONTEXT.md
@.planning/phases/76-darkness-unification-relaunch-persistence/76-03-SUMMARY.md

<interfaces>
From 76-03 (engine/saveState.js): `validateSave` and `rehydrate` carry a validated `combat`, `store`, `pendingFind`, `pendingHazard` and `pendingTile` wholesale (else null); `beats` is always null; a surviving fight keeps `c.foeEffect` and rounds-cadence timers. `resumeEventsFor(state)` returns `[{ type: "fightResumed", round, pending, foes }]`, `[{ type: "storeResumed" }]` or `[]`, pure and zero-rng.

src/browser/engineAdapter.js:
- Module state: `currentState`, the one-shot `bootWornReport` with `takeBootWornReport()` (read-and-null), `deathRecord`.
- `boot(freshSeed)`: migrateLegacyKeys, loadBests, loadGraveyard, then reads SAVE_KEY; on `validateSave(raw, { freshSeed }).ok` it sets `bootWornReport` and `currentState = rehydrate(check.value)` and returns; otherwise `initRun(freshSeed, exclude)`.
- `initRun(seed, exclude, options)` resets `deathRecord` and builds a new run; `startNewRun` wraps it.
- `persist()` writes `JSON.stringify(serializeRun(currentState))` through storage.js (fire-and-enqueue); `dispatch(action)` calls applyAction then persist. Its catch replaces the run with `initRun(seed)` on a throw.
- `formatEvents(events)` maps events through EVENT_NARRATION.

test/persistence/resume-mid-encounter.test.js (rewritten by 76-03): helpers `withFakeLocalStorage(fn)`, `relaunch(store)` (flush, snapshot the saved JSON, `boot(1)`), `plain(state)`; imports boot/initRun/getState/dispatch, storage flush, serializeRun, startCombat, openStore, offerLoot, makeRng, stripVolatileFields.

test/unit/engineAdapter.test.js: its own `withFakeLocalStorage`; the takeBootWornReport one-shot tests are the model (fresh boot gives null; a rehydrated save reports once and a second call returns null).

Corpus sources for the walk:
- tools/lib/tuning-bot.mjs: `playRun(seed, opts, onStep)` (onStep(events, state) after every applied action; opts: maxActions, party, startDepth, force { cls, sub, race }), `forceParty`, `RUN_FLAGS`. test/unit/harness/rollHighBaseline.js#PIN_RUNS shows working configurations, including deep starts at 8 and 14 (elites begin at floor 16 after 75.3; add a start near 20).
- The parity action scripts under test/parity/fixtures (combat, magic, economy, encounters) and test/roundtrip/serialize-rehydrate.test.js's `applyStartCombat` / `applyInternal` replay shapes (startCombat and openStore are internal functions, not actions).
- test/parity/harness/diffState.js#stripVolatileFields (deathAt, graves[].when).
- engine/actions.js lists the valid action types: for the determinism probe use `fight` for a pending combat, `attack` for a joined one (75.1 converts it to loseTurn while the hero is out, which is fine), `leaveStore` for a store, `leaveFind` for a find, and `move` in the hazard's `dir` for a pending hazard.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The one-shot resume events and the adapter-level relaunch proofs</name>
  <files>src/browser/engineAdapter.js, test/unit/engineAdapter.test.js, test/persistence/resume-mid-encounter.test.js</files>
  <read_first>
    - src/browser/engineAdapter.js (module state, takeBootWornReport, initRun, startNewRun, boot, persist, dispatch)
    - engine/saveState.js (resumeEventsFor, validateSave, rehydrate as left by 76-03)
    - test/unit/engineAdapter.test.js (the takeBootWornReport tests)
    - test/persistence/resume-mid-encounter.test.js (whole file, as 76-03 left it)
    - src/browser/nativeChrome.js (the pause flush) and src/browser/storage.js (the per-key write queue), read only
  </read_first>
  <behavior>
    - boot with no save: takeBootResumeEvents() is null. boot with a quiet save: []. boot with a save holding a pending fight: [{ type: "fightResumed", round: 1, pending: true, foes: n }], and a second call returns null. boot with a save holding an open store: [{ type: "storeResumed" }]. initRun after a boot that resumed a fight leaves it null.
    - Through the real adapter: start a fight, dispatch `fight`, then `attack` until round 3 or the fight ends; after every dispatch, flush and assert the stored save's combat JSON-equals getState().combat.
    - After each of those dispatches, a relaunch (boot from the stored save) gives a combat deep-equal to the live one, c.foeEffect and every c.timers record equal to the live ones, and state.beats null; then dispatch `move` and the fight is still up and the position unchanged.
    - Boundary: relaunching at the encounter step gives pending true, round 1; relaunching after the action that kills the last foe gives combat null and the spoils pile (when one dropped).
    - Store: open a store, buy an affordable line, relaunch: that line is sold, gold equals the post-purchase gold, every other line and price is unchanged; relaunch again: identical store.
  </behavior>
  <action>
1. Write the tests first: the takeBootResumeEvents cases in test/unit/engineAdapter.test.js, and the per-action save, relaunch-each-round, move-refused, boundary and store-purchase cases in test/persistence/resume-mid-encounter.test.js (reuse its helpers; pick a seed where the fight lasts several rounds, found by trying seeds, and record it in a comment). Run them and watch the new cases fail.
2. src/browser/engineAdapter.js: add a module-level one-shot `bootResumeEvents` (null by default) beside bootWornReport, with a comment in the same style (adapter-side, never serialized, consumed on read; SAV-06/07, Phase 76). In boot's rehydrate branch set it from `resumeEventsFor(currentState)` (import it from engine/saveState.js alongside validateSave/rehydrate/serializeRun). Reset it to null in initRun. Export `takeBootResumeEvents()` that returns it and nulls it, with a JSDoc naming 76-05's boot path as the caller. Change nothing in persist or dispatch.
3. Run the tests to green and commit.
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js test/persistence/resume-mid-encounter.test.js test/persistence/lifecycle.test.js test/persistence/flush-drain.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function takeBootResumeEvents" src/browser/engineAdapter.js` equals 1 and `grep -c "resumeEventsFor(" src/browser/engineAdapter.js` is at least 1.
    - `node --test test/unit/engineAdapter.test.js test/persistence/resume-mid-encounter.test.js` passes, including the one-shot, per-action save, relaunch-each-round, move-refused, boundary and store-purchase cases.
    - `node --test test/persistence/lifecycle.test.js test/persistence/flush-drain.test.js` passes (persistence plumbing unchanged).
  </acceptance_criteria>
  <done>The real adapter saves every round of a fight, a relaunch after any round resumes it exactly, movement stays refused, and the shell has a one-shot list of resume events to narrate.</done>
</task>

<task type="auto">
  <name>Task 2: The generic every-field round-trip and resume-determinism proof</name>
  <files>test/roundtrip/resume-roundtrip.test.js</files>
  <read_first>
    - tools/lib/tuning-bot.mjs (playRun, forceParty, RUN_FLAGS, decideAction's contract)
    - test/unit/harness/rollHighBaseline.js (PIN_RUNS, loadSave)
    - test/roundtrip/serialize-rehydrate.test.js (the fixture replay shapes: applyStartCombat, applyInternal, ECONOMY_INTERNAL_FNS)
    - test/parity/harness/diffState.js (stripVolatileFields)
    - engine/actions.js (the action types used as probes)
    - engine/combat.js (startCombat's combat and foe literals, for the old-shape case)
  </read_first>
  <action>
1. Create test/roundtrip/resume-roundtrip.test.js with a header naming SAV-06/SAV-07 (Phase 76) and the orchestrator's rule: the proof is generic over fields, so a field added later is covered with no edit here.
2. A `diffPaths(live, loaded)` helper that walks both values recursively and returns every key path (for example `combat.foes[1].elite`) where the key sets differ or a leaf differs under Object.is. The load under test is `rehydrate(validateSave(JSON.stringify(serializeRun(state))).value)`. Compare after stripVolatileFields on both sides and deleting only `pendingJoiner`.
3. Fixture sweep: replay every scenario of the parity combat, magic, economy and encounters action scripts (with the serialize-rehydrate replay shapes), and at EVERY action assert `diffPaths` is empty.
4. Bot corpus: run `playRun` over a configuration list covering solo seeds, two party runs, a forced Magic User (spells and effects), a forced Thief, and deep starts near 14 and 20 (elites), with a maxActions cap. In onStep, for every state with a live combat, an open store, a pendingFind, a pendingHazard or a pendingTile (sampled up to a per-run cap if needed for runtime), assert `diffPaths` is empty, then apply the matching probe action (see the interfaces) to both the live and the loaded state with applyAction and assert the next states (plain, volatile-stripped) and the events are deep-equal. Count what was seen.
5. Non-vacuity: assert the corpus saw at least 50 combat states, at least one combat with `allies`, at least one open store, and at least one joined (pending false) fight past round 2. Record every count in the SUMMARY.
6. Precision: assert, over every combat, store and pending value in the corpus, that JSON round-tripping each is Object.is-equal at every path (this is what diffPaths already checks; add an explicit named test so a failure reads as "a JSON-unsafe value in a live fight"). Follow the flagged assumption if one turns up.
7. Old-shape fight: build a fight with startCombat, delete every combat and foe key that startCombat's literal (and its foe literal) does not write, save and load it, then apply `fight`, then `attack` and `flee` on clones: none throws and the fight is still resumed after the load.
8. Keep the whole file under about 60 seconds (`node --test` timing); tune caps, not coverage breadth.
  </action>
  <verify>
    <automated>node --test test/roundtrip/resume-roundtrip.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/roundtrip/resume-roundtrip.test.js` passes in under about 60 seconds, with the fixture sweep, bot corpus, non-vacuity, precision, determinism and old-shape tests.
    - `grep -c "diffPaths" test/roundtrip/resume-roundtrip.test.js` is at least 2.
    - `grep -c "playRun" test/roundtrip/resume-roundtrip.test.js` is at least 1.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every field of every fight, store and pending decision a real run produces survives a relaunch, and the fight continues on the same dice, proven without naming a single field.</done>
</task>

</tasks>

<verification>
- The adapter saves every round, a relaunch resumes the identical fight or store, movement is refused, and the resume events are one-shot.
- The generic walk and the determinism probe pass across the fixtures and the bot corpus, with non-vacuity asserted.
- npm test is green.
  <human-check>
  Pixel 7, at milestone close (76-05 compiles the full relaunch checklist):
  - Mid-fight, swipe the app away from recents and relaunch: the same fight, same round and HP; the map does not accept a move until the fight ends.
  - Mid-fight, use SAVE & QUIT, close the app, relaunch, ENTER: the same fight.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 76 criteria 3 and 4 hold at the adapter: a player who Saves & quits, or whose app is killed, mid-fight relaunches into the exact same fight (same foes, HP, round and active effects) and cannot escape it by force-closing; a player who relaunches with the store open returns to the same store with the same stock.
</success_criteria>

<output>
Create `.planning/phases/76-darkness-unification-relaunch-persistence/76-04-SUMMARY.md` when done. Include the corpus configuration and the counts seen, the runtime of the new test, any JSON-unsafe finding, and a note for 76-05: `takeBootResumeEvents()`'s contract.
</output>
