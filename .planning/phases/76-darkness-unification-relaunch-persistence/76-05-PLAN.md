---
phase: 76-darkness-unification-relaunch-persistence
plan: 05
type: execute
wave: 3
depends_on:
  - 76-01
  - 76-02
  - 76-03
  - 76-04
files_modified:
  - mazeworld.html
  - test/unit/shell-resume-line.test.js
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements:
  - SAV-06
  - SAV-07
must_haves:
  truths:
    - "On a relaunch into a live fight or an open store, the Oracle logs the in-voice resume line (the `fightResumed` / `storeResumed` events from takeBootResumeEvents(), formatted by formatEvents) right after the existing 'Delve resumed.' banner and hero line; a fresh run, a dead save and a quiet save log no resume line (CONTEXT: 'The Oracle adds a short in-voice resume line')"
    - "The resume line is an Oracle line only: no rail card and no combat card (the card-vs-rail ruling: a relaunch is a minor event); the fight or store screen itself is what the player sees"
    - "The engineAdapter import line byte-pinned by test/unit/shell-account.test.js and test/unit/shell-pgs.test.js stays byte-identical; takeBootResumeEvents and formatEvents arrive on a second import line from the same module"
    - "test/parity/FIXTURE-INVENTORY.md's Phase 76 section gains the persistence subsection: parity measured zero (no fixture edit, no comparable carve-out, the master's git hash-object unchanged), the full list of flipped reset-on-load pins with before and after, and the new standing guards (save-resume, resume-roundtrip, the rewritten resume-mid-encounter)"
    - "At the phase head, `npm test`, `node --test \"test/parity/**/*.test.js\"` and `npm run boot:check` are green, and `git diff --quiet <phase-base> -- test/parity/prototype-master.js.txt` exits 0"
    - "The Phase 76 device checklist (relaunch mid-fight, SAVE & QUIT mid-fight, force-close mid-fight, relaunch mid-store, relaunch on a find, hazard or Joiner card, torch, Amulet and Night Vision darkness on every surface, and a light lifting the dark in a fight) is compiled into the SUMMARY for the milestone-close Pixel 7 batch, with no mid-run device pause"
  artifacts:
    - path: mazeworld.html
      provides: "the resume line logged at boot after the 'Delve resumed.' banner"
      contains: "takeBootResumeEvents"
    - path: test/unit/shell-resume-line.test.js
      provides: "the boot wiring pins and the resume-line formatting checks"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the Phase 76 persistence subsection"
      contains: "SAV-06"
  key_links:
    - from: mazeworld.html (module script boot, the hadSaveAtLaunch branch)
      to: src/browser/engineAdapter.js#takeBootResumeEvents
      via: "a one-shot read right after the 'Delve resumed.' lines"
      pattern: "takeBootResumeEvents\\(\\)"
    - from: mazeworld.html (the same branch)
      to: src/browser/eventNarration.js (fightResumed, storeResumed)
      via: "formatEvents(...) then window.logLine for each line"
      pattern: "formatEvents\\(takeBootResumeEvents"
  prohibitions:
    - statement: "MUST NOT surface the resume as a card or rail line, and MUST NOT let its copy break the family-friendly deadpan voice"
      requirement_id: SAV-06
      category: values
      status: resolved
      verification: test
---

<objective>
Finish the phase: the Oracle says the fight (or the shop) is still on after a relaunch, the persistence half's measurement is recorded beside the darkness half's in FIXTURE-INVENTORY.md, every gate is run at the phase head, and the device checks are compiled for the milestone-close batch.

Purpose: CONTEXT's resumed-fight decision ("The Oracle adds a short in-voice resume line"), the standing engine gate (declare what moved, with before and after), and the deferred-UAT protocol.

Output: a small boot-sequence change in mazeworld.html, a shell test, the FIXTURE-INVENTORY subsection, and the compiled device checklist.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 76-05 creates or changes.)

- 76-01: `DARK_WAIVERS` / `darkWaiver` / `darkWaived` / `darkLimited`; the reveal, the render window and every in-fight dark penalty on one predicate; `test/unit/dark-waiver.test.js`; the torch/Amulet direction rows and ledger rows; readouts; the Phase 76 H2s in DIFFICULTY-RETUNE.md and FIXTURE-INVENTORY.md
- 76-02: the chip, tap card, vignette and per-tile tint on the one predicate, with map-and-fight copy; `MAP_PALETTE.floorDarkLit` / `waterDarkLit`; `test/unit/dark-surfaces.test.js`; `docs/TERRAIN.md` and `docs/SHELL-MODULES.md`
- 76-03: the load resumes a validated `combat`, `store`, `pendingFind`, `pendingHazard`, `pendingTile` and `pendingJoiner`; `resumeEventsFor` with its Oracle lines; `test/unit/save-resume.test.js`; flipped reset pins
- 76-04: `takeBootResumeEvents()`; the adapter-level relaunch proofs; `test/roundtrip/resume-roundtrip.test.js`
- **This plan (76-05):** the Oracle resume line at boot in `mazeworld.html`; `test/unit/shell-resume-line.test.js`; the persistence subsection of FIXTURE-INVENTORY's Phase 76 section; the compiled Phase 76 device checklist

## Flagged assumptions (surfaced, not silently decided)

- **Placement.** The resume line is logged at boot, right under the "Delve resumed." banner, so it is in the Oracle before the player taps ENTER. It is not repeated on ENTER.
- **Phase 77 handoff, not in scope.** CONTEXT notes Phase 77's effect indicators must read the rehydrated effects after a relaunch. This plan records that in the SUMMARY as a handoff; it builds no indicator.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-darkness-unification-relaunch-persistence/76-CONTEXT.md
@.planning/phases/76-darkness-unification-relaunch-persistence/76-01-SUMMARY.md
@.planning/phases/76-darkness-unification-relaunch-persistence/76-02-SUMMARY.md
@.planning/phases/76-darkness-unification-relaunch-persistence/76-03-SUMMARY.md
@.planning/phases/76-darkness-unification-relaunch-persistence/76-04-SUMMARY.md

<interfaces>
From 76-04 (src/browser/engineAdapter.js): `takeBootResumeEvents()` returns the events boot() computed with `resumeEventsFor` when it rehydrated a save (an array, possibly empty) and nulls itself; null after a fresh-run boot. `formatEvents(events)` returns the Oracle HTML lines (EVENT_NARRATION, which 76-03 extended with fightResumed and storeResumed).

mazeworld.html (module script boot sequence):
- `import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from "./src/browser/engineAdapter.js";` is byte-pinned (occurrence count 1) by test/unit/shell-account.test.js and test/unit/shell-pgs.test.js. Leave it untouched.
- `hadSaveAtLaunch` is true only for a present, parseable, not-dead save. After `const engineState = await boot(freshSeed); window.__mzState.set(engineState);` the `if (hadSaveAtLaunch) { ... }` block logs the "Delve resumed." banner and the hero line with `window.logLine`, then `window.paint(); window.draw();`. paint() calls renderEncounter(), which raises the fight or store overlay from `S` (hasActiveEncounter reads S.combat / S.store), so no rendering change is needed.
- Tests read the page through `stripHtml` from tools/ident-sweep.mjs (see test/persistence/resume-mid-encounter.test.js's CODE constant and its source-slice checks for the ENTER branch).

test/parity/FIXTURE-INVENTORY.md: 76-01 created `## Phase 76: darkness unification and relaunch persistence (DARK-01/02, SAV-06/07) — measured per plan` and its Plan 01 subsection. Append the persistence subsection after it. 76-03's SUMMARY lists every flipped pin and the parity result; 76-04's lists the corpus counts.

Every plan's `<human-check>` block (76-01 to 76-04) lists its device checks; compile them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The Oracle's resume line at boot</name>
  <files>mazeworld.html, test/unit/shell-resume-line.test.js</files>
  <read_first>
    - mazeworld.html (the module script's engineAdapter import line and the boot sequence from `const freshSeed` through `window.mzCenterMap?.()`)
    - src/browser/engineAdapter.js (takeBootResumeEvents, formatEvents)
    - src/browser/eventNarration.js (the fightResumed and storeResumed entries)
    - test/unit/shell-account.test.js and test/unit/shell-pgs.test.js (the import-line pins)
    - test/persistence/resume-mid-encounter.test.js (the stripHtml CODE constant and its source-slice style)
  </read_first>
  <behavior>
    - The page has exactly one `import { takeBootResumeEvents, formatEvents } from "./src/browser/engineAdapter.js";` line, and the pinned boot/dispatch/startNewRun/waitForPending/takeBootWornReport import line still occurs exactly once.
    - Inside the `if (hadSaveAtLaunch) {` block, after the "Delve resumed." banner and the hero line, the block reads takeBootResumeEvents() once, formats it with formatEvents and logs each line with window.logLine; the read happens before `window.paint()`.
    - The slice of that block contains no rail or card call (mzRailLine, mzConditionCard, a card builder).
    - formatEvents([{ type: "fightResumed", round: 3, pending: false, foes: 2 }]) returns one non-empty line; the pending variant returns a different non-empty line; formatEvents([{ type: "storeResumed" }]) returns one non-empty line; all three pass the safety word-list scan.
  </behavior>
  <action>
1. Write test/unit/shell-resume-line.test.js first with the behaviour above (source slices through stripHtml, plus formatEvents imported from src/browser/engineAdapter.js and the word list from content/safety-wordlist.js). Run it and watch it fail.
2. mazeworld.html (module script): add the second import line for `takeBootResumeEvents` and `formatEvents` next to the pinned one, leaving the pinned line byte-identical. In the `if (hadSaveAtLaunch)` block, after the hero line, log every line of `formatEvents(takeBootResumeEvents() || [])` through window.logLine. Comment: SAV-06/SAV-07 (Phase 76), the Oracle's resume line (Oracle only, per the card-vs-rail ruling); the fight or store overlay itself comes back through paint() -> renderEncounter() from the rehydrated S.
3. Run the new test, the two pin tests and `npm run boot:check` (rerun once if it flakes).
  </action>
  <verify>
    <automated>node --test test/unit/shell-resume-line.test.js test/unit/shell-account.test.js test/unit/shell-pgs.test.js test/persistence/resume-mid-encounter.test.js && npm run boot:check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-resume-line.test.js` passes.
    - `grep -c 'import { takeBootResumeEvents, formatEvents } from "./src/browser/engineAdapter.js";' mazeworld.html` equals 1.
    - `node --test test/unit/shell-account.test.js test/unit/shell-pgs.test.js` passes (the pinned import line is untouched).
    - `npm run boot:check` passes (rerun once if it flakes).
  </acceptance_criteria>
  <done>A relaunch into a fight or a shop says so on the Oracle, in voice, and nowhere else.</done>
</task>

<task type="auto">
  <name>Task 2: Record the persistence measurement, run every gate, compile the device checklist</name>
  <files>test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 76 H2 and its Plan 01 subsection; the Phase 65 section as the measured-zero model)
    - .planning/phases/76-darkness-unification-relaunch-persistence/76-03-SUMMARY.md and 76-04-SUMMARY.md (the flipped pins, the parity result, the corpus counts)
    - .planning/phases/76-darkness-unification-relaunch-persistence/76-01-PLAN.md through 76-04-PLAN.md (their human-check blocks)
  </read_first>
  <action>
1. Record the phase base: the commit before 76-01's first commit (from 76-01's SUMMARY).
2. test/parity/FIXTURE-INVENTORY.md: append `### Plans 03–05 — relaunch persistence (SAV-06/07): measured zero` under the Phase 76 H2. Include the rule (validateSave/rehydrate carry a validated fight, store, find, hazard decision, pending tile and pending Joiner offer wholesale; invalid ones drop; the fight's hero-side state survives only with it), the predictor (the loader never runs inside a parity replay and no new serialized field exists, so zero fixtures and zero carve-outs), the live-scan results (parity pass count, `git diff --stat <phase-base> -- test/parity/fixtures test/parity/prototype-master.js.txt test/parity/harness/comparables.js` empty for the persistence plans, the master's `git hash-object`), the declared test-pin flips (a table: file, test, old assertion, new assertion, pointer), and the new standing guards (test/unit/save-resume.test.js, test/roundtrip/resume-roundtrip.test.js with its corpus counts, the rewritten test/persistence/resume-mid-encounter.test.js).
3. Run every gate at the phase head: `npm test`, `node --test "test/parity/**/*.test.js"`, `npm run boot:check` (rerun once if it flakes), and confirm `git diff --quiet <phase-base> -- test/parity/prototype-master.js.txt` exits 0.
4. Compile the Phase 76 device checklist into the SUMMARY (numbered, one line each, for the milestone-close Pixel 7 batch; no device pause now), merging every human-check from 76-01 to 76-05 and adding: relaunch while a find card is up (the same find is offered); relaunch while a ladder/rope hazard card is up (the same choice is offered); relaunch while a Joiner offer is up (the same Joiner is offered, and accepting adds them); relaunch mid-fight after a debuff landed (the debuff chip is still there); relaunch with a torch lit in the dark (the tint and the chip come back as they were).
5. Add the Phase 77 handoff line to the SUMMARY (its effect indicators must read the rehydrated c.foeEffect, c.timers and combat fields after a relaunch).
  </action>
  <verify>
    <automated>npm test && node --test "test/parity/**/*.test.js" && npm run boot:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SAV-06" test/parity/FIXTURE-INVENTORY.md` is at least 1 and the new subsection sits under the Phase 76 H2 (`grep -c "## Phase 76" test/parity/FIXTURE-INVENTORY.md` still equals 1).
    - `git diff --quiet <phase-base> -- test/parity/prototype-master.js.txt` exits 0.
    - `npm test` reports 0 failures, the parity suite passes, and `npm run boot:check` passes.
    - The SUMMARY contains the numbered Phase 76 device checklist and the Phase 77 handoff line.
  </acceptance_criteria>
  <done>The phase is measured, recorded and green, and the device checks wait in one list for the milestone close.</done>
</task>

</tasks>

<verification>
- The Oracle narrates a resumed fight or shop at boot, Oracle-only, and the pinned import line is untouched.
- FIXTURE-INVENTORY's Phase 76 section records both halves; every gate is green at the phase head; the master is untouched.
  <human-check>
  Pixel 7, at milestone close (the compiled list lives in 76-05-SUMMARY.md):
  - Relaunch into a live fight: the Oracle's first lines after "Delve resumed." say the fight is still on, and the fight screen is up.
  - Relaunch into an open shop: the Oracle says the shop is still open, and the shop screen is up with the same stock.
  </human-check>
</verification>

<success_criteria>
All four ROADMAP Phase 76 success criteria hold end to end: one darkness rule on every surface (1, 2), a relaunch or force-close returns the player to the exact same fight (3) and the same store with the same stock (4), with the device confirmations batched for the milestone close.
</success_criteria>

<output>
Create `.planning/phases/76-darkness-unification-relaunch-persistence/76-05-SUMMARY.md` when done. Include the gate results, the phase base, the compiled device checklist and the Phase 77 handoff.
</output>
