---
phase: 51-initiative-once-per-combat
plan: 03
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/initiative-line.test.js
autonomous: true
requirements: [INIT-02]
tags: [narration, oracle, fight-log, voice, shell]

must_haves:
  truths:
    - "SC3: one narrated line ('Initiative — you N, them M. You go first.') appears exactly once per fight in the Oracle and the fight log, pinned by a test that fails on a second appearance (CONTEXT: 'combatJoined already fires exactly once per fight, so once, not per round is structural')"
    - "The line is prototype-faithful with the dice in .roll spans so the fight log can reveal them through the existing oracleDetailText fold: 'Initiative — you 14, Stalka Beast 9. You go first.' — the foe's name when the fight has exactly one live foe, 'them' for a group (CONTEXT: 'Line shape is prototype-faithful with the dice in .roll spans')"
    - "When an override decided the roll, the verdict names it in voice instead of the bare You/They go first: Samurai -> 'Samurai honour — they go first.'; slow -> '…they go first.' with the slowness named; foreseen -> 'Foresight — you go first.'; Acute Hearing -> 'Acute Hearing — you go first.'; senses -> 'Nothing gets the jump on you.' (existing copy); Knight big-foe -> 'A Knight's welcome — it comes straight at you.'; Court Mage -> 'Court Mage — you talk first, they swing first.' (CONTEXT 'The initiative line (INIT-02)')"
    - "No new event type and no new EVENT_NARRATION key — the existing combatJoined entries in eventNarration.js and narrationLines.js are extended; existing consumers (senses: true short form, first) keep working"
    - "Every narration branch passes the family-friendly BANNED scan; the fight-log entry reveals both dice via the existing .roll fold"
  artifacts:
    - "src/browser/eventNarration.js — combatJoined(e) renders the dice line with two .roll spans and a why-keyed verdict span"
    - "src/browser/narrationLines.js — LINE_FOR.combatJoined(e) renders the roll-free 'Initiative — <verdict>' text with tone hit/hurt"
    - "test/unit/initiative-line.test.js — per-why snapshot + BANNED scan, oracleDetailText reveal pin, once-per-fight pin over a 3-round engine replay"
  key_links:
    - "fightLog.js: text = LINE_FOR (roll-free), roll = oracleDetailText(narrateEvent(event)) — the .roll spans in the Oracle html are what make the dice revealable"
    - "combatJoined field names from Plan 02: first, mine, theirs, why (optional), foe (optional), senses (optional)"
---

<objective>
Show the player the initiative result — once per fight — in the Oracle and the fight log, with the dice revealable, and with the verdict spoken in voice when an override (Samurai, slow race, foresight, Acute Hearing, senses, Knight-vs-big-foe, Court Mage) decided it.

Purpose: INIT-02. The prototype stored `C.initNote` ("Initiative — you N, them M") every round; the engine never ported it because it is a presentation concern. `combatJoined` — which fires exactly once per fight — is its replacement, now carrying `mine`/`theirs`/`why`/`foe` from Plan 02.

Output: extended `combatJoined` narration in both narration modules; `test/unit/initiative-line.test.js`.

Requirement traceability: INIT-02 (all tasks).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-initiative-once-per-combat/51-CONTEXT.md
@.planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md
@src/browser/eventNarration.js
@src/browser/narrationLines.js
@src/browser/fightLog.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: the initiative line — Oracle html (eventNarration.js) and the roll-free fight-log/rail text (narrationLines.js)</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js</files>
  <read_first>
    - .planning/phases/51-initiative-once-per-combat/51-CONTEXT.md ("The initiative line (INIT-02)" — every bullet; "Claude's Discretion: exact override wording within the voice rules")
    - engine/combat.js `fight()` as landed by Plan 02 (the exact `combatJoined` push: `first`, `mine`, `theirs`, optional `why`, optional `foe`, optional `senses`) and `resolveInitiative`'s `why` value list
    - test/parity/prototype-master.js.txt lines 1865-1876 (canon `initNote` shape: `Initiative — you <span class="roll">${mine}</span>, them <span class="roll">${theirs}</span>.` — READ ONLY, never edited)
    - src/browser/eventNarration.js lines 100-125 (the roll-span convention comment), 286-311 (`encounterStarted` — its `knightBigFoe`/`courtMageTalksFirst` clauses stay; and the current `combatJoined` entry with the Phase 31/40 comments), 350-380 (how `struck`/`strikeMissed` embed `<span class="roll">` and `?? "?"` fallbacks)
    - src/browser/narrationLines.js lines 125-155 (`narrativeLineText` strips roll spans; `oracleDetailText` keeps dice — the fold), 222-235 (the EVENT list entry `combatJoined` — unchanged), 1040-1052 (the current `LINE_FOR.combatJoined` — `{ text, tone, priority }`)
    - src/browser/fightLog.js lines 26-60 (text from LINE_FOR, `roll` from `oracleDetailText(narrateEvent(events[idx]))`)
    - test/unit/spell-utility.test.js lines 295-302 (the existing pin: `senses: true` must still include "Nothing gets the jump on you" in BOTH modules — keep it green)
    - test/unit/fightLog.test.js lines 60-66 (a `combatJoined` event without dice must still render — use `?? "?"` fallbacks, never throw)
  </read_first>
  <action>
    `src/browser/eventNarration.js` `combatJoined: (e) => ...` (no new key, per INIT-02): build the Oracle html as `Initiative — you <span class="roll">{mine}</span>, {foeName} <span class="roll">{theirs}</span>. ` followed by ONE verdict span — `<span class="beat">…</span>` when `e.first === "you"`, `<span class="hurt">…</span>` when "foe" — where `{foeName}` is `e.foe` when present, else `them`, and dice fall back to `?` when absent (`e.mine ?? "?"`). The verdict is keyed on `e.why` first, then the legacy `e.senses` flag, then `e.first`: `samurai` -> "Samurai honour — they go first."; `slow` -> a line ending "they go first." that names the slowness (e.g. "Slow feet — they go first."; exact wording is discretion, family-friendly sarcasm, no profanity); `foreseen` -> "Foresight — you go first."; `acuteHearing` -> "Acute Hearing — you go first."; `senses` (or no why + `e.senses`) -> "You go first. Nothing gets the jump on you." (the existing copy must remain a substring — spell-utility pins it); `knight` -> "A Knight's welcome — it comes straight at you."; `courtMage` -> "Court Mage — you talk first, they swing first."; no why -> "You go first." / "They go first." Keep the Phase 31/40 comments and add a Phase 51 (INIT-02) comment explaining that this is the prototype's `initNote`, emitted once per fight because `combatJoined` fires once, with the dice in roll spans so `oracleDetailText` reveals them. Leave `encounterStarted`'s Knight/Court Mage clauses exactly as they are (the pre-Fight! warning Phase 24 tests pin).

    `src/browser/narrationLines.js` `LINE_FOR.combatJoined`: return `{ text, tone, priority: PRIORITY.feature }` where `text` is roll-free — `Initiative — ` + the same verdict sentence chosen by the same `why`/`senses`/`first` keying (share the verdict table between the two modules only if a shared pure helper already exists in narrationLines.js and eventNarration.js can import it without creating a new cross-module dependency; otherwise keep two small parallel tables and pin their agreement in Task 2) — `tone` "hit" for a you-first verdict, "hurt" for foe-first. The `senses: true` short form keeps "Nothing gets the jump on you." in its text.

    Both modules stay pure (no engine import, no DOM, no Math.random — `narrationLinesCoverage.test.js` pins this).
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/spell-utility.test.js test/unit/fightLog.test.js test/unit/narrationLinesCoverage.test.js test/unit/shell-fight-log.test.js test/unit/formatEventsCoverage.test.js 2>&1 | grep -E "^# (tests|pass|fail) " ; node -e "import('./src/browser/eventNarration.js').then(m=>{const h=m.EVENT_NARRATION.combatJoined({type:'combatJoined',first:'you',mine:14,theirs:9,foe:'Stalka Beast'});console.log(h)})"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./src/browser/eventNarration.js').then(m=>console.log(m.EVENT_NARRATION.combatJoined({type:'combatJoined',first:'you',mine:14,theirs:9,foe:'Stalka Beast'})))"` prints html containing exactly two `<span class="roll">` spans, the text `Initiative — you `, `Stalka Beast`, and `You go first.`
    - `node -e "import('./src/browser/narrationLines.js').then(m=>{const l=m.LINE_FOR.combatJoined({type:'combatJoined',first:'foe',mine:3,theirs:17,why:'samurai'});console.log(l.text,'|',l.tone)})"` prints a text starting `Initiative — ` that contains `Samurai honour` and `they go first` and tone `hurt`, with no digit in the text (roll-free)
    - `node -e "import('./src/browser/narrationLines.js').then(m=>{const {oracleDetailText}=m;import('./src/browser/eventNarration.js').then(n=>console.log(oracleDetailText(n.narrateEvent({type:'combatJoined',first:'you',mine:14,theirs:9,foe:'Stalka Beast'}))))})"` prints `Initiative — you 14, Stalka Beast 9. You go first.`
    - `node -e "import('./src/browser/eventNarration.js').then(m=>console.log(m.EVENT_NARRATION.combatJoined({type:'combatJoined',first:'you',senses:true})))"` prints html containing `Nothing gets the jump on you` (legacy short form still honoured) and no thrown error
    - `grep -c "combatJoined" src/browser/narrationLines.js` is unchanged in the EVENT list (still one list entry) — `grep -c '^  "combatJoined",' src/browser/narrationLines.js` prints 1
    - `node --test test/unit/spell-utility.test.js test/unit/fightLog.test.js test/unit/narrationLinesCoverage.test.js test/unit/shell-fight-log.test.js` prints `# fail 0`
    - `git diff --stat -- engine/ test/parity/` is empty (shell-only plan)
  </acceptance_criteria>
  <done>The Oracle renders the once-per-fight initiative line with both dice in roll spans and an in-voice verdict per `why`; the fight log text is the roll-free form; every existing narration pin stays green.</done>
</task>

<task type="auto">
  <name>Task 2: pins — per-why snapshot + BANNED scan, the .roll reveal, the once-per-fight line over a real 3-round replay; gates; SUMMARY</name>
  <files>test/unit/initiative-line.test.js, .planning/phases/51-initiative-once-per-combat/51-03-SUMMARY.md</files>
  <read_first>
    - test/unit/rail.test.js lines 15-25 and 430-470 (the BANNED/ALLOWLIST voice-scan pattern: `import { BANNED, ALLOWLIST } from "../../content/safety-wordlist.js"`, the word-boundary MATCHERS, the allowlist skip)
    - test/unit/fightLog.test.js lines 55-90 (`linesForAction(action, events, ctx, { limit: Infinity, withIdx: true })` and the `oracleDetailText` test shape)
    - test/unit/combat.test.js — the Plan 02 SC2 test ("INIT-01 (SC2)…": Samurai vs a 999-wp foe, `fight` on `[10, 1, 20]` then three `playerStrike` on `[20, 20]` each) and its `fixedState`/`fixedFoe`/`fixedCombat`/`fakeRng` helpers (copy the helpers verbatim into the new file, as spell-utility.test.js does)
    - src/browser/fightLog.js lines 26-90 (`fightLogLines`/the export that maps an action's events to `{ text, tone, roll }` — use the exported function name you find there)
    - src/browser/eventNarration.js and src/browser/narrationLines.js `combatJoined` entries as landed in Task 1
  </read_first>
  <action>
    Create `test/unit/initiative-line.test.js` (node:test, ESM, pure imports of the two narration modules + fightLog + engine/combat.js for the replay) with these tests:
    1. "INIT-02: every why branch renders in voice, both modules agree on the verdict, and no line trips BANNED" — for each of `samurai`, `slow`, `foreseen`, `acuteHearing`, `senses`, `knight`, `courtMage` and the two bare cases (you-first / foe-first with no why), build the event with `mine: 14, theirs: 9` (and `foe: "Stalka Beast"`), assert `EVENT_NARRATION.combatJoined(e)` contains two `<span class="roll">` and the branch's expected phrase (Samurai honour / they go first / Foresight / Acute Hearing / Nothing gets the jump on you / A Knight's welcome / Court Mage / You go first. / They go first.), assert `LINE_FOR.combatJoined(e).text` starts with `Initiative — ` and contains the same phrase with NO digits, assert the tone is "hit" for first:"you" and "hurt" for first:"foe", and run every string through the BANNED scan (word-boundary regex over `BANNED`, skipping `ALLOWLIST` entries).
    2. "INIT-02: the foe's name appears when the event carries `foe`, 'them' otherwise" — `foe: "Stalka Beast"` vs no `foe`: the Oracle html contains `Stalka Beast 9` in the first case and `them 9` in the second.
    3. "INIT-02: the fight log reveals both dice through the existing .roll fold" — `oracleDetailText(narrateEvent(e))` === `Initiative — you 14, Stalka Beast 9. You go first.` exactly, and `narrativeLineText` of the same html contains no `14`/`9`.
    4. "INIT-02 (SC3): a 3-round fight narrates the initiative line exactly once in the Oracle and the fight log — a second appearance fails" — replay the Plan 02 SC2 scenario through the engine (`fight` + three `playerStrike` on one fakeRng), collect all events, then: `events.map(narrateEvent).filter(h => h.includes("Initiative — ")).length === 1`; the fight-log lines for the whole event stream (via `linesForAction` with `limit: Infinity`, or the fightLog export that builds `{ text, tone, roll }` entries) contain exactly one entry whose text starts with `Initiative — `, and that entry's `roll` (or `oracleDetailText` of its event) contains both dice; assert the count is not 2 or more by construction (`assert.equal(..., 1)`).
    5. "INIT-02: a combatJoined without dice still renders (legacy shape)" — `{ type: "combatJoined", first: "you" }` renders without throwing and shows `?` for both dice.

    Gates: `npm test` (fail 0; count = Plan 02's count + the five tests above) and `npm run build:www` (green), recorded verbatim in the SUMMARY with the master hash. `npm run boot:check` skipped (environment-blocked — note, not a gate). End the SUMMARY with `## Human verification (deferred to end of run)`: (1) after Fight!, the Oracle shows exactly one "Initiative — you N, <foe> M. …" line per fight, and it does not repeat on later rounds; (2) in the fight log the initiative entry reads roll-free and tapping it reveals both dice; (3) a Samurai fight's line reads "Samurai honour — they go first." and the foe's turn follows immediately. Commit: `feat(51-03): initiative line once per fight in the Oracle and fight log (INIT-02)`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/initiative-line.test.js 2>&1 | grep -E "^# (tests|pass|fail) " ; npm test 2>&1 | grep -E "^# (tests|pass|fail) " ; npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/initiative-line.test.js` prints `# tests 5` (or more) and `# fail 0`
    - `grep -c "INIT-02 (SC3)" test/unit/initiative-line.test.js` prints 1; `grep -c "safety-wordlist" test/unit/initiative-line.test.js` prints 1
    - `npm test` prints `# fail 0`; `npm run build:www` exits 0
    - `grep -c "^## Human verification (deferred to end of run)" .planning/phases/51-initiative-once-per-combat/51-03-SUMMARY.md` prints 1
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat 0cacf50..HEAD -- test/parity/prototype-master.js.txt` empty; `git status --porcelain` empty after the commit
  </acceptance_criteria>
  <done>Five pins prove the line's voice, its once-per-fight emission over a real replay, the .roll reveal, the foe-name rule and the legacy fallback; both gates green; SUMMARY ends with the deferred Pixel 7 checks.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 03's share)

| Artifact | Kind | Where |
|---|---|---|
| `EVENT_NARRATION.combatJoined(e)` — `Initiative — you <roll>, {foe|them} <roll>. <verdict span>` keyed on `why`/`senses`/`first` | extended entry (no new key) | src/browser/eventNarration.js |
| `LINE_FOR.combatJoined(e)` — roll-free `Initiative — <verdict>` with tone hit/hurt | extended entry | src/browser/narrationLines.js |
| Verdict copy per why: "Samurai honour — they go first." / slow (names the slowness) / "Foresight — you go first." / "Acute Hearing — you go first." / "Nothing gets the jump on you." / "A Knight's welcome — it comes straight at you." / "Court Mage — you talk first, they swing first." / "You go first." / "They go first." | player-facing strings | both modules |
| `test/unit/initiative-line.test.js` — 5 tests: per-why snapshot + BANNED scan; foe-name rule; .roll reveal; SC3 once-per-fight replay; legacy no-dice shape | new test file | test/unit/ |

Phase-wide artifact index: see 51-01-PLAN.md (scan tool + output, ledger H2/H3/BEFORE, before-smoke JSON) and 51-02-PLAN.md (resolveInitiative, combatJoined.mine/theirs/why/foe, SC1/SC2/once pins, the three records, the inventory section, the MOVED SET guard, AFTER ledger rows, after-smoke JSON).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine event -> Oracle innerHTML | `e.foe` (bestiary name) and `e.mine`/`e.theirs` (integers) are interpolated into HTML; content-sourced, same convention as every existing `e.target`/`e.name` line; no user-typed text reaches this path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-07 | Tampering | HTML interpolation of `e.foe` in eventNarration.combatJoined | low | accept | Names come from content/bestiary (not user input); the existing `struck`/`encounterStarted` lines interpolate the same names; a save-file-tampered name would only affect the tamperer's own offline device. |
| T-51-08 | Information Disclosure | dice values shown in the fight log | low | accept | Deliberate — INIT-02 requires the dice to be revealable, as every other roll already is. |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase. |
</threat_model>

<verification>
- SC3: the once-per-fight pin passes on a real 3-round engine replay; a second line would fail `assert.equal(count, 1)`.
- INIT-02 voice: every why branch renders in voice and passes the BANNED scan; the legacy `senses: true` pin in spell-utility.test.js stays green.
- Reveal: `oracleDetailText(narrateEvent(combatJoined))` yields the prototype-shaped sentence with both dice; the fight-log text is roll-free.
- `npm test` fail 0; `npm run build:www` green; no engine/parity bytes changed by this plan.
</verification>

<success_criteria>
- ROADMAP SC3 satisfied with a failing-on-second-appearance pin.
- INIT-02 satisfied: one line per fight in the Oracle and the fight log, dice revealable, in voice, `initNote`'s shape honoured via `combatJoined`.
</success_criteria>

<output>
Create `.planning/phases/51-initiative-once-per-combat/51-03-SUMMARY.md` when done. It must contain `## Gates` with the verbatim `# tests` / `# pass` / `# fail` lines and the `npm run build:www` result, the master hash, the final verdict copy per why (quoted), and must END with `## Human verification (deferred to end of run)` listing the three Pixel 7 checks above.
</output>
