---
phase: 68-global-boards-submissions-you-placed-x
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/placement.js
  - content/boards.js
  - test/unit/placement-copy.test.js
  - test/unit/boards-copy.test.js
  - test/voice/safety-scan.test.js
  - test/unit/hp-not-wp.test.js
  - src/browser/placement.js
  - test/unit/placement.test.js
autonomous: true
requirements: [PLACE-01, PLACE-02]
user_setup: []

must_haves:
  truths:
    - "content/placement.js holds the D-13 bank PLACEMENT_LINES banded first / ten / hundred / rest with {rank}, {total} and {ahead} tokens, plus a standing band for a run that did not beat the player's best; every line is deadpan, family-friendly, says HP never WP, and passes the safety scan"
    - "placementLine({ rank, total, newBest, hash }) gives the DEEPEST rank quip, e.g. 'You placed 3,117th of 9,044. The 3,116 ahead of you are also dead.'; it returns null unless rank is an integer of at least 1 and total an integer of at least rank, so a failed or partial rank never renders (D-10, D-11)"
    - "Bands switch exactly at rank 1 / 2, 10 / 11 and 100 / 101; newBest false always uses the standing band; the pick is deterministic from the run hash"
    - "deferredPlacementCard({ count, rank, total, newBest, hash }) gives one rail card { title, line, tone, hold } covering every run a flush delivered: one or many, best or standing, e.g. 'Your earlier death placed 412th of 9,044 on DEEPEST.' (D-12)"
    - "seasonDropLine(count) gives the one Oracle line for queued deaths dropped at a season bump, singular or plural, and null for a count below 1 (D-03)"
    - "content/boards.js gains BOARDS_PANEL_COPY.global (the ALL/FRIENDS scope lines, SEASON {n}, loading, unreachable, closed, empty, consent note and button, YOU and FRIEND, the sampled LINEAGE footnote, the worldwide / among-friends standing lines) and GLOBAL_STANDING_LINES, all safety- and HP-scanned (D-05, D-06, D-07, D-08, D-09)"
    - "No placement, card, Oracle or panel line contains the characters <, > or &, because the Oracle renders through innerHTML"
  prohibitions:
    - statement: "Must not name, quote or single out another player (by handle or otherwise) in any quip, placement line, card or panel note; the joke is always on the player's own adventurer or on death in general"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "content/placement.js"
      provides: "PLACEMENT_LINES, PLACEMENT_CARD, SEASON_DROP_LINES"
      contains: "PLACEMENT_LINES"
    - path: "src/browser/placement.js"
      provides: "ordinalText, placementBand, placementLine, deferredPlacementCard, seasonDropLine"
      contains: "export function placementLine"
    - path: "content/boards.js"
      provides: "BOARDS_PANEL_COPY.global and GLOBAL_STANDING_LINES"
      contains: "GLOBAL_STANDING_LINES"
    - path: "test/unit/placement.test.js"
      provides: "band boundaries, token filling, null cases, card variants and determinism"
      min_lines: 110
  key_links:
    - from: "src/browser/placement.js"
      to: "content/placement.js"
      via: "the banks and card copy, filled with {rank}/{total}/{ahead}/{count}"
      pattern: "from \"../../content/placement.js\""
---

<objective>
Write every new word Phase 68 shows, and the pure view model that turns a DEEPEST rank into the death-panel quip and the deferred rail card. Implements D-13 (the new content/ bank banded 1st / top 10 / top 100 / the rest with {rank} and {total} tokens, safety-scanned), D-10 (DEEPEST is the only ranked quip), D-11 (the view returns nothing unless the rank is complete, so waiting and failure show nothing), D-12 (one card covering several queued runs, best-placed plus the count), D-03 (the one Oracle line for a season drop) and the panel copy for D-05 (ALL rows' standing lines), D-06 (the friends consent note and button), D-07 (the loading and "world is unreachable" notes), D-08 (the SEASON label) and D-09 (the honest LINEAGE sample footnote).

Claude's discretion used here (68-CONTEXT: exact quip wording): a fifth "standing" band covers a run that did not beat the player's existing best, because Play Games keeps only a player's best score, so its rank is the best run's rank, not this run's. Saying "You placed Nth" for a worse run would be false.

Purpose: all copy lands once, scanned, before the panel and the shell use it.
Output: content/placement.js, the content/boards.js additions, src/browser/placement.js, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-CONTEXT.md

<interfaces>
content/boards.js today (Phase 66 plus 67-04, merged before this plan runs; read the live file): BOARD_COPY, BOARD_FOOTNOTES, BOARDS_PANEL_COPY (deep-frozen: head, scope, strip incl. strip.live, chips, note incl. note.live, empty, divider, standing, stats, lineage, level, sep, dock), STANDING_LINES, NEW_BEST_HEAD, NEW_BEST_LINES, FIRST_DEATH_LINES. This plan only ADDS keys; 68-06 retires note.live.
src/browser/newBest.js: the pickIndex(hash, length) pattern (an 8-hex hash parsed with parseInt >>> 0, modulo the bank length; anything else picks 0).
Rail card shape the shell delivers through window.mzRailLine(title, line, tone, hold): tones in use are good, bad, odd and dull; src/browser/rail.js RAIL_HOLD.level is 12000.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: content/placement.js and the content/boards.js global copy, pinned and scanned</name>
  <files>content/placement.js, content/boards.js, test/unit/placement-copy.test.js, test/unit/boards-copy.test.js, test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - content/boards.js (the whole file, including the 67-04 strip.live/note.live additions)
    - test/unit/boards-copy.test.js (the verbatim pins and deep-freeze checks)
    - test/voice/safety-scan.test.js (the recursive string-leaf walks near the Phase 66 block, ~395-420)
    - test/unit/hp-not-wp.test.js (the imported banks list ~41 and ~139)
    - test/determinism/content-is-pure-data.test.js
    - tools/stale-terms.mjs (retired words to avoid)
  </read_first>
  <behavior>
    - PLACEMENT_LINES is deep-frozen with keys first, ten, hundred, rest, standing; first has at least 3 lines, ten at least 3, hundred at least 3, rest at least 4, standing at least 3
    - Every ten, hundred and rest line contains both {rank} and {total}; every first line contains {total}; at least one rest line contains {ahead}; every standing line contains {rank} and {total}; no line contains a token other than {rank}, {total} and {ahead}
    - PLACEMENT_CARD is deep-frozen { title, tone: "good", hold: 12000, one, many, oneStanding, manyStanding } where each variant is a non-empty array; one/oneStanding lines use {rank} and {total}; many/manyStanding lines also use {count}
    - SEASON_DROP_LINES is deep-frozen { one, many }; many contains {count}; neither contains another token
    - BOARDS_PANEL_COPY.global is deep-frozen with scope { all, friends }, season (containing {n}), loading, unreachable, closed, empty, consent, consentButton, you "YOU", friend "FRIEND", anon, foe, leanRateUnit, noEntry, ofWorld (containing {n} and "worldwide"), ofFriends (containing {n} and "among friends"), ofSampled (containing {n}), sampledFoot (containing {n}); GLOBAL_STANDING_LINES is deep-frozen { first, ten, hundred, rest } with at least 3 lines each and no {token}
    - Every Phase 66/67 key of BOARDS_PANEL_COPY is unchanged (the existing verbatim pins still pass)
    - No new string contains "<", ">", "&", "WP", or a player handle marker "@"
    - The safety scan and the HP-not-WP scan walk PLACEMENT_LINES, PLACEMENT_CARD, SEASON_DROP_LINES, BOARDS_PANEL_COPY.global and GLOBAL_STANDING_LINES
  </behavior>
  <action>
Per D-03, D-05..D-09, D-12 and D-13 (the voice rulings: deadpan, family-friendly, HP not WP, the joke on the player's own adventurer or on death in general, never on another player):

1. Create content/placement.js with a header comment (Phase 68, PLACE-01/02, D-10..D-13; tokens are filled by src/browser/placement.js, content/ holds no functions; the Oracle renders the season line through innerHTML, so no markup characters). Export deep-frozen:
   - PLACEMENT_LINES. Use these lines (tokens exactly as written; the executor may add lines but keep these):
     first: "You placed 1st of {total}. Everyone else is also dead, just less impressively." / "You placed 1st of {total}. The top of the pile is still the pile." / "You placed 1st of {total}. Savour it. The dungeon is already planning a sequel."
     ten: "You placed {rank} of {total}. Top ten. The dungeon will pretend it did not notice." / "You placed {rank} of {total}. Close enough to the top to see it, not close enough to matter." / "You placed {rank} of {total}. Top ten. Somebody out there will have to try harder."
     hundred: "You placed {rank} of {total}. Top hundred, which is a crowd, but an exclusive one." / "You placed {rank} of {total}. Respectable, in the way a tidy grave is respectable." / "You placed {rank} of {total}. The {ahead} ahead of you would like a word. They cannot have one."
     rest: "You placed {rank} of {total}. The {ahead} ahead of you are also dead." / "You placed {rank} of {total}. Somewhere in the middle of the heap. It is warm there, at least." / "You placed {rank} of {total}. Statistically, you happened." / "You placed {rank} of {total}. The ledger has room for everyone. That is its whole problem."
     standing: "This one did not beat your best. Your best still holds {rank} of {total}." / "Not a new record. Your best run is still {rank} of {total}, and still dead." / "The ledger kept your better corpse. {rank} of {total}, unchanged."
   - PLACEMENT_CARD: title "THE LEDGER CAUGHT UP", tone "good", hold 12000; one: ["Your earlier death placed {rank} of {total} on DEEPEST."]; many: ["{count} earlier deaths reached the ledger. The best placed {rank} of {total} on DEEPEST."]; oneStanding: ["Your earlier death reached the ledger. It did not beat your best, which holds {rank} of {total}."]; manyStanding: ["{count} earlier deaths reached the ledger. None beat your best, which holds {rank} of {total}."].
   - SEASON_DROP_LINES: one "One unsent death belonged to a closed season. That ledger is sealed, so it was let go."; many "{count} unsent deaths belonged to a closed season. That ledger is sealed, so they were let go."

2. content/boards.js: add a `global` key inside BOARDS_PANEL_COPY (deep-frozen like its siblings), after `note`, holding: scope { all: "Global. Every delve this season.", friends: "Your friends’ dead only. This season." }; season "SEASON {n}"; loading "Asking the world who died. It keeps records, slowly."; unreachable "The world is unreachable. Your own dead are still here."; closed "This board has not opened yet. The ledger is still being ruled."; empty "Nobody has died on this board yet this season. Somebody has to go first."; consent "Play Games will not show us your friends until you say so."; consentButton "SHOW MY FRIENDS"; you "YOU"; friend "FRIEND"; anon "A nameless delver"; foe "foe"; leanRateUnit "SQ / FLOOR"; noEntry "Nothing of yours on this board yet this season."; ofWorld "of {n} interred worldwide."; ofFriends "of {n} among friends."; ofSampled "of {n} lineages in the sample."; sampledFoot "Sampled from the top {n} deepest corpses in the world. Rare lineages may be buried further down.". Add `export const GLOBAL_STANDING_LINES` (deep-frozen, no tokens): first "First place. The others have been told, and are not thrilled." / "Top of the heap. Mind the drop." / "Nobody has done better. Nobody will admit it, either."; ten "Top ten. Strangers are studying your corpse." / "In the top ten. Your ghost has earned a small nod." / "Top ten. The rest are taking notes, grudgingly."; hundred "Top hundred. A large room, but a respectable one." / "Somewhere in the top hundred. The view is mostly other graves." / "Top hundred. Frame it before the season ends."; rest "Out in the crowd. Everyone here is dead too, if that helps." / "Not near the top. Not near the bottom either, probably." / "A face in a very large, very quiet crowd.". Add a Phase 68 line to the block comment. Leave every existing key untouched.

3. Tests: create test/unit/placement-copy.test.js for the content/placement.js bullets (shape, token rules, deep-freeze, no markup characters, no "WP", no "@"); extend test/unit/boards-copy.test.js with verbatim pins for every new BOARDS_PANEL_COPY.global string and GLOBAL_STANDING_LINES; register PLACEMENT_LINES, PLACEMENT_CARD, SEASON_DROP_LINES and GLOBAL_STANDING_LINES in the recursive string-leaf walks of test/voice/safety-scan.test.js and test/unit/hp-not-wp.test.js (BOARDS_PANEL_COPY.global is already covered by the existing BOARDS_PANEL_COPY walk; assert that in a one-line test).
  </action>
  <verify>
    <automated>node --test test/unit/placement-copy.test.js test/unit/boards-copy.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js test/determinism/content-is-pure-data.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const PLACEMENT_LINES" content/placement.js` equals 1 and `grep -c "export const GLOBAL_STANDING_LINES" content/boards.js` equals 1
    - `grep -c "PLACEMENT_LINES" test/voice/safety-scan.test.js` is at least 1 and `grep -c "PLACEMENT_LINES" test/unit/hp-not-wp.test.js` is at least 1
    - `node --test test/unit/placement-copy.test.js test/unit/boards-copy.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js` passes
  </acceptance_criteria>
  <done>Every Phase 68 word exists once in content/, pinned, frozen and scanned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/browser/placement.js, the rank quip and deferred card view model</name>
  <files>src/browser/placement.js, test/unit/placement.test.js</files>
  <read_first>
    - src/browser/newBest.js (pickIndex, the never-throw posture, the header style)
    - content/placement.js (Task 1)
  </read_first>
  <behavior>
    - ordinalText: 1 "1st", 2 "2nd", 3 "3rd", 4 "4th", 11 "11th", 12 "12th", 13 "13th", 21 "21st", 22 "22nd", 101 "101st", 111 "111th", 112 "112th", 3117 "3,117th", 1000000 "1,000,000th"
    - placementBand: 1 "first"; 2 and 10 "ten"; 11 and 100 "hundred"; 101 and 9044 "rest"
    - placementLine({ rank: 3117, total: 9044, newBest: true, hash: h }) is a PLACEMENT_LINES.rest line with {rank} "3,117th", {total} "9,044" and {ahead} "3,116"; a hash that picks the "{ahead} ahead of you are also dead" line yields "You placed 3,117th of 9,044. The 3,116 ahead of you are also dead."
    - rank 1 uses the first band; newBest false uses the standing band at any rank; newBest null uses the rank band
    - placementLine returns null for rank 0, -1, 1.5, "3", null; total null, 0, or less than rank; and a non-object input
    - The same input always returns the same line; two different hashes can pick different lines; an invalid hash picks index 0
    - deferredPlacementCard({ count: 1, rank: 412, total: 9044, newBest: true, hash }) is { title: "THE LEDGER CAUGHT UP", line: "Your earlier death placed 412th of 9,044 on DEEPEST.", tone: "good", hold: 12000 }; count 3 uses the many line with {count} "3"; newBest false uses oneStanding/manyStanding; count 0, a missing rank or total, or total below rank returns null
    - seasonDropLine(1) is the one line; seasonDropLine(4) is the many line with "4"; 0, -1 and "2" return null
    - The results never contain a leftover "{" token
  </behavior>
  <action>
Create src/browser/placement.js (D-10, D-11, D-12, D-13, D-03) with a header comment: Phase 68 (PLACE-01/02), the DEEPEST rank quip on THAT IS THAT and the one deferred rail card; pure, deterministic, never throws; returns null for anything incomplete, so waiting or failure shows nothing (D-11); en-US digit grouping to match the panel's PURSE values.

Exports: ordinalText(n) (the teens rule on n % 100, then n % 10; the number grouped with toLocaleString("en-US")); placementBand(rank); placementLine({ rank, total, newBest, hash }); deferredPlacementCard({ count, rank, total, newBest, hash }); seasonDropLine(count). A module-private fill(template, vars) replaces {rank}, {total}, {ahead} and {count}; a module-private pick(bank, hash) copies newBest.js's pickIndex rule. The validity rules are in the behaviour list: rank an integer of at least 1, total an integer of at least rank, count an integer of at least 1. The card object is frozen.

Create test/unit/placement.test.js covering every behaviour bullet.
  </action>
  <verify>
    <automated>node --test test/unit/placement.test.js test/unit/placement-copy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function placementLine" src/browser/placement.js` equals 1 and `grep -c "export function deferredPlacementCard" src/browser/placement.js` equals 1
    - `node --test test/unit/placement.test.js` passes with at least 16 tests, including the band boundaries 1/2, 10/11, 100/101, the worked D-13 example and the null cases
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>A rank becomes one in-voice line or one card, deterministically, and anything incomplete becomes nothing.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step
</verification>

<success_criteria>
- PLACE-01: the rank quip bank and its view exist, with the D-13 example reproducible
- PLACE-02: the deferred placement card covers one or many runs; incomplete ranks produce nothing
- All copy is safety- and HP-scanned
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| content/placement.js | new content | PLACEMENT_LINES, PLACEMENT_CARD, SEASON_DROP_LINES |
| `BOARDS_PANEL_COPY.global`, `GLOBAL_STANDING_LINES` | content additions | content/boards.js |
| src/browser/placement.js | new pure module | ordinalText, placementBand, placementLine, deferredPlacementCard, seasonDropLine |
| test/unit/placement-copy.test.js, test/unit/placement.test.js | new tests | |

## Flagged assumptions (unresolved edge rows)

- PLACE-01 (edge probe: unclassified): the rank shown is Play Games' all-time public rank of the player's best DEEPEST score. When the submit result says this run was not a new best, the standing band says so rather than claiming this run's place (Play Games stores no per-run rank for a non-best score).
- PLACE-02 (edge probe: unclassified): a deferred card is produced only when the post-flush rank request succeeds; a flush whose rank request fails reports nothing (D-11's "nothing on failure"), and those runs are not reported again.

<output>
Create `.planning/phases/68-global-boards-submissions-you-placed-x/68-03-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: read the rank quip and the deferred card on the Pixel 7 at the default and largest text sizes (no clipping; the voice reads right). Do NOT create `.planning/phases/68-*/deferred-items.md`.
</output>
