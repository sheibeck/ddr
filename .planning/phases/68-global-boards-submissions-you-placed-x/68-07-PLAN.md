---
phase: 68-global-boards-submissions-you-placed-x
plan: 07
type: execute
wave: 3
depends_on: ["68-01", "68-02", "68-03", "68-04", "68-05", "68-06"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/shell-pgs.test.js
  - test/unit/harness/shellSandbox.js
autonomous: true
requirements: [PGS-03, PGS-04, PGS-05, PLACE-01, PLACE-02]
user_setup: []

must_haves:
  truths:
    - "Every non-dev death reaches the submission queue through the adapter's run-recorded listener; with Compete ON it is stored in ddr.pgsqueue.v1 and a flush starts, and with Compete OFF nothing is queued (D-01, D-02, D-04)"
    - "The queue flushes when sign-in succeeds (at launch or from the Sign in row), when the device comes back online, and when the app becomes visible again (respecting backoff); turning Compete OFF purges the queue and clears the global cache (D-02, D-04)"
    - "When a flush submits the run whose THAT IS THAT panel is still up, the DEEPEST rank quip appears under the NEW PERSONAL BEST block (or under the panel line when there is none) and fades in once; the blanket prefers-reduced-motion rule removes the fade; nothing shows while waiting, and nothing on failure (D-10, D-11)"
    - "Runs submitted later from the queue (or after their death panel closed) produce one rail card in voice at that flush, covering the best-placed run and the count, parked until the dungeon is visible and delivered after any parked account card's hold, never on top of it (D-12)"
    - "A season drop is noted once in the Oracle, in voice, through window.logLine (D-03)"
    - "The Leaderboards panel's ALL and FRIENDS views read the global-boards controller through the panel's global seam, the SHOW MY FRIENDS button requests consent through it, and every finished fetch re-renders the open panel; signed out or Compete OFF, no leaderboard call is made (D-05, D-06, D-07)"
    - "The browser dev loop uses the fake provider with dev leaderboard IDs and the real score orders, so submissions, the rank line, the card and the global views can be exercised without a device; native uses content/leaderboards.js (placeholders until Phase 69)"
    - "The background flush on pause awaits the queue's pending writes alongside the adapter's, so a death's queue entry survives the app being backgrounded (D-02)"
    - "window.__mzPlacement is the one new bridge, registered in src/browser/bridge.js and documented by tools/bridge-doc.mjs; it is presentation state only, never a field on S"
    - "A signed-out or Compete-OFF run shows no rank line, no card and no error (PLACE-02)"
  prohibitions:
    - statement: "Must not show a rank, placement or leaderboard error as a modal, a toast or a blocking overlay; the rank line lives on THAT IS THAT and every other report is a rail card or an Oracle line"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "mazeworld.html"
      provides: "the Phase 68 module wiring, the classic renderRankLine, the rank-line CSS"
      contains: "createSubmissionQueue({"
    - path: "src/browser/bridge.js"
      provides: "the __mzPlacement registry entry"
      contains: "__mzPlacement"
    - path: "docs/SHELL-MODULES.md"
      provides: "the Phase 68 section and the regenerated bridge table"
      contains: "Global boards, submissions and placement (Phase 68)"
    - path: "test/unit/shell-pgs.test.js"
      provides: "source pins and region-evaluated behaviour tests for the Phase 68 wiring"
      min_lines: 180
  key_links:
    - from: "mazeworld.html (module)"
      to: "src/browser/engineAdapter.js#setRunRecordedListener"
      via: "setRunRecordedListener(onRunRecorded) → pgsQueue.enqueue(summary)"
      pattern: "setRunRecordedListener\\(onRunRecorded\\)"
    - from: "mazeworld.html (module) handlePgsFlush"
      to: "src/browser/placement.js placementLine / deferredPlacementCard"
      via: "the live death's line into window.__mzPlacement; the rest into one parked card"
      pattern: "deferredPlacementCard\\("
    - from: "mazeworld.html (classic) renderCombatOver"
      to: "renderRankLine(over, window.__mzPlacement)"
      via: "directly after the NEW PERSONAL BEST block call"
      pattern: "renderRankLine\\(over, window\\.__mzPlacement\\)"
    - from: "mazeworld.html (module) createBoardsPanel"
      to: "src/browser/globalBoards.js#createGlobalBoards view / requestFriendsAccess"
      via: "global: (q) => ..., onFriendsConsent: () => ..."
      pattern: "onFriendsConsent:"
---

<objective>
Wire Phase 68 into the running shell. Implements D-01 and D-02 (every Compete-ON death is queued durably, then flushed; the queue flushes again after sign-in; its writes survive backgrounding), D-03 (the season-drop Oracle line), D-04 (Compete OFF purges the queue; signed-out-but-competing deaths wait for sign-in), D-05, D-06 and D-07 (the panel's ALL/FRIENDS views read the global-boards controller, the consent button requests access, finished fetches re-render the panel, zero calls when inactive), D-10 and D-11 (the DEEPEST rank quip fades in on THAT IS THAT under the NEW PERSONAL BEST block, and nothing shows while waiting or on failure) and D-12 (one parked rail card for runs placed by a later flush). Sequencing the rank request after the submit is Claude's discretion per 68-CONTEXT: the queue asks for the DEEPEST standing once per successful flush (68-04), and this plan routes the answer to the death panel or the card.

Purpose: the player's death reaches the world, and the world's answer reaches the player, without ever blocking or nagging.
Output: mazeworld.html wiring and CSS, the __mzPlacement bridge, docs/SHELL-MODULES.md, and test/unit/shell-pgs.test.js. No Android build or device step in this phase (the milestone's debug APK and AAB are Phase 69's).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-08-PLAN.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-03-SUMMARY.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-04-SUMMARY.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-05-SUMMARY.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-06-SUMMARY.md

<interfaces>
Phase 67's shell wiring (67-08; merged before this plan runs; read the live module script, it is authoritative): `let account = null;` declared before the boards panel instance; createBoardsPanel({ ..., identity: () => ... }); the account block after `applySettings(await readSettings())`: pgsNative, the provider (createPlayGames() on native, createFakePlayGames({ signedIn: currentSettings?.pgsDevSignedIn === true }) otherwise), account = createAccountController({ provider, settings, notify: parkAccountCard }), account.subscribe(...); pendingAccountCard, dungeonVisible(), parkAccountCard(card), flushAccountCard() (called from hideTitleScreen, the roller's onCommit and routeFromBoards' dungeon branch); account.boot().catch(() => {}) after initTitleScreen; account.state() (includes compete and status: "off" | "pending" | "signedIn" | "signedOut") and account.identity() ({ signedIn, player: { id, displayName } | null }).
Phase 65/66 shell facts: dispatchWithNarration sets window.__mzDeathRecord = newBestView(takeDeathRecord()) on a died event; the module's bridge init sets window.__mzDeathRecord = null; showTitleScreen resets it to null; the classic renderCombatOver calls `if (kind === "dead") renderNewBestBlock(over, window.__mzDeathRecord);` right after appending the panel line and before .cb-over-actions (pinned by test/unit/shell-new-best.test.js); the THAT IS THAT panel shows while S.dead; window.logLine(html) writes the Oracle through innerHTML; window.mzRailLine(title, line, tone, hold); window.paint(); registerNativeChrome({ storage: window.mzStorage, waitForPending, getGameContext }).
From 68-01: leaderboardIdsFor({ native }), knownSeasons(), scoreOrdersFor(ids). From 68-03: placementLine, deferredPlacementCard, seasonDropLine. From 68-04: setRunRecordedListener(fn) (engineAdapter.js); createSubmissionQueue({ storage, provider, ids, season, isCompeting, isSignedIn, onFlushed, onSeasonDrop }) → { load, enqueue, flush, purge, state, waitForPending }; onFlushed({ submitted: [{ hash, newBest }], standing: { rank, total } | null }). From 68-05: createGlobalBoards({ provider, ids, isActive, playerId, onChange }) → { view, requestFriendsAccess, clear }. From 68-06: createBoardsPanel({ ..., global, seasons, onFriendsConsent }).
Rules: the pinned engineAdapter import line stays byte-identical (new imports go on their own lines); a new window.__mz name needs a src/browser/bridge.js entry plus `node tools/bridge-doc.mjs --write`; module-side state is presentation only, never a field on S; comments and new lines use none of tools/stale-terms.mjs's retired words.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the THAT IS THAT rank line (classic), its CSS, and the __mzPlacement bridge</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/shell-pgs.test.js, test/unit/harness/shellSandbox.js</files>
  <read_first>
    - mazeworld.html classic script: renderCombatOver (the `kind === "dead"` block and renderNewBestBlock right after it), the .cb-over-best CSS rules (~791-794) and the blanket prefers-reduced-motion rule (~976)
    - mazeworld.html module script: the bridge init where window.__mzDeathRecord = null is set, and showTitleScreen
    - src/browser/bridge.js (the __mzDeathRecord entry, the shape to copy)
    - test/unit/shell-new-best.test.js (the region-extraction helpers and the renderNewBestBlock pins)
    - test/unit/bridge-registry.test.js and tools/bridge-doc.mjs
    - test/unit/harness/shellSandbox.js (whether it asserts every registered bridge is wired)
  </read_first>
  <behavior>
    - renderRankLine(host, placement), extracted by region and run against a recording document: a placement { hash, line, fresh: true } adds one p#cb-over-rank.cb-over-rank with the line as textContent and data-fresh "1", inserted before .cb-over-actions when present (else appended), and window.__mzPlacement becomes the same placement with fresh false
    - A second call with the now-not-fresh placement replaces the element (still one #cb-over-rank) without data-fresh
    - A null placement, a placement without a string line, or an empty line removes any existing #cb-over-rank and adds nothing; a null host is a no-op
    - Source pins: renderCombatOver contains `if (kind === "dead") renderRankLine(over, window.__mzPlacement);` directly after the renderNewBestBlock line (the shell-new-best pins still pass); renderRankLine builds DOM with createElement/textContent only (no innerHTML)
    - CSS: .cb-over-rank has a rule; .cb-over-rank[data-fresh="1"] declares an animation whose @keyframes is defined once; the blanket reduced-motion rule still exists
    - src/browser/bridge.js registers __mzPlacement (owner mazeworld.html (module), consumers naming renderCombatOver/renderRankLine and the module's placement handler and showTitleScreen); bridge-doc --check passes after --write
  </behavior>
  <action>
Per D-10 and D-11 (the rank line on THAT IS THAT, under the NEW PERSONAL BEST block, fading in, reduced motion respected; nothing while waiting or on failure):

1. Classic script, next to renderNewBestBlock: add `function renderRankLine(host, placement)` per the behaviour list, with a "Phase 68 (PLACE-01, D-10/D-11)" comment saying the module sets window.__mzPlacement only when the just-died run's DEEPEST rank has returned, so waiting or failure never shows anything. In renderCombatOver add `if (kind === "dead") renderRankLine(over, window.__mzPlacement);` on the line directly after the renderNewBestBlock call.
2. CSS next to the .cb-over-best rules: .cb-over-rank in the .cb-over-best-quip type style (mono, bold, the same size and line height, colour #e8c97a, margin-top 10px); .cb-over-rank[data-fresh="1"] with a 600ms ease-out opacity animation named mwrankin, and `@keyframes mwrankin` from opacity 0 to 1. The existing blanket prefers-reduced-motion rule already disables it; do not add a second one.
3. Module script: in the bridge init block set `window.__mzPlacement = null;` next to window.__mzDeathRecord, with a comment (presentation-only parcel { hash, line, fresh } for the just-died run's rank line; never a field on S). In showTitleScreen, next to its __mzDeathRecord reset, also reset window.__mzPlacement to null.
4. src/browser/bridge.js: add the __mzPlacement entry (alphabetical position per the file's order), then run `node tools/bridge-doc.mjs --write` and `--check`. If test/unit/harness/shellSandbox.js asserts or requires every registered bridge to be wired, set window.__mzPlacement = null there too.
5. Start test/unit/shell-pgs.test.js with the Task 1 bullets (CRLF-normalized source, comment-stripped the way test/unit/shell-new-best.test.js does it).
  </action>
  <verify>
    <automated>node --test test/unit/shell-pgs.test.js test/unit/shell-new-best.test.js test/unit/shell-combat-over.test.js test/unit/bridge-registry.test.js && node tools/bridge-doc.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'renderRankLine(over, window.__mzPlacement);' mazeworld.html` equals 1 and `grep -c "function renderRankLine" mazeworld.html` equals 1
    - `grep -c "__mzPlacement" src/browser/bridge.js` is at least 1 and `node tools/bridge-doc.mjs --check` exits 0
    - `node --test test/unit/shell-pgs.test.js test/unit/shell-new-best.test.js test/unit/bridge-registry.test.js` passes
  </acceptance_criteria>
  <done>THAT IS THAT can show one rank line under the new-best block, fading in once, from a registered presentation-only bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: module wiring: queue, death listener, flush triggers, placement routing, card parking, Oracle note and the panel's global seams</name>
  <files>mazeworld.html, test/unit/shell-pgs.test.js</files>
  <read_first>
    - mazeworld.html module script: the import block (the pinned engineAdapter line and the Phase 65/66/67 separate lines); `let account = null;` and the createBoardsPanel({ ... }) instance; the Phase 67 account block (pgsNative, the provider, createAccountController, account.subscribe); pendingAccountCard/dungeonVisible/parkAccountCard/flushAccountCard; registerNativeChrome({ storage, waitForPending, getGameContext })
    - src/browser/pgsQueue.js, src/browser/globalBoards.js, src/browser/placement.js, src/browser/boardScores.js, src/browser/boardsPanel.js (68-01..68-06 APIs)
    - test/unit/shell-account.test.js (67-08's region-extraction-and-evaluate helpers for module functions)
  </read_first>
  <behavior>
    - Source pins over the comment-stripped module script: one import line each for setRunRecordedListener (engineAdapter.js), createSubmissionQueue, createGlobalBoards, the three placement functions, the boardScores helpers and SEASON; the pinned engineAdapter import line still counts exactly 1; `let globalBoards = null;`, `let pgsQueue = null;`, `let liveDeathHash = null;` and `let pendingPgsCard = null;` appear before the createBoardsPanel({ region, which contains `global:`, `seasons:` and `onFriendsConsent:`; createSubmissionQueue({ receives storage: window.mzStorage; `setRunRecordedListener(onRunRecorded)`; an "online" window listener and a "visibilitychange" document listener each call pgsQueue flush; registerNativeChrome's waitForPending awaits both the adapter's and the queue's; the fake provider receives orders from scoreOrdersFor; the plugin package name is absent from the whole comment-stripped shell; no fetch(, XMLHttpRequest, WebSocket, EventSource or sendBeacon
    - onRunRecorded({ hash: "h1", ... }) (evaluated with a recording fake queue) sets liveDeathHash "h1", sets window.__mzPlacement to null and calls enqueue once with the summary
    - handlePgsFlush({ submitted: [...], standing: null }) does nothing (no placement, no card)
    - Live death "h1" with S.dead true and #cb-over present: handlePgsFlush({ submitted: [{ hash: "h1", newBest: true }], standing: { rank: 3117, total: 9044 } }) sets window.__mzPlacement to { hash: "h1", line: placementLine(...), fresh: true } and calls renderRankLine once; no card is parked
    - The same flush with S.dead false parks one card (count 1) instead and sets no placement
    - Live "h1" plus older "h0" and "h2" with the panel up: the rank line for h1 and one card with count 2; newBest false for h1 gives the standing-band line
    - onAccountForPgs: a state with compete false calls purge() and globalBoards.clear() once and resets window.__mzPlacement; a transition to status "signedIn" calls flush({ force: true }) once, and a repeated signedIn state does not call it again
    - Card parking: with the dungeon visible, parkPgsCard(card) delivers it through window.mzRailLine(title, line, tone, hold) once; with an account card also parked, the account card is delivered first and the placement card is delivered by the timer after the account card's hold; with the title visible nothing is delivered until flushAccountCard runs with the dungeon visible
    - notePgsSeasonDrop(2) calls window.logLine once with the seasonDropLine(2) text inside a beat span; notePgsSeasonDrop(0) does nothing
    - The boards panel's global arrow returns null before globalBoards exists and otherwise forwards { board, scope, season } to globalBoards.view; onFriendsConsent calls requestFriendsAccess
  </behavior>
  <action>
All changes are in mazeworld.html's module script, each block commented "Phase 68 (...)". Write the new functions as hoisted function declarations over module-level lets, so the tests can extract and evaluate them.

1. Imports, each on its own line after the Phase 67 lines: setRunRecordedListener from ./src/browser/engineAdapter.js; createSubmissionQueue from ./src/browser/pgsQueue.js; createGlobalBoards from ./src/browser/globalBoards.js; placementLine, deferredPlacementCard, seasonDropLine from ./src/browser/placement.js; leaderboardIdsFor, knownSeasons, scoreOrdersFor from ./src/browser/boardScores.js; SEASON from ./content/season.js. The pinned engineAdapter line stays byte-identical.

2. Module-level lets beside `let account = null;` (textually before the boards panel instance, so no arrow or early showTitleScreen call can hit a temporal dead zone): globalBoards, pgsQueue, liveDeathHash (all null) and pendingPgsCard (null). Add to the createBoardsPanel({ ... }) call (D-05..D-08): `global: (q) => (globalBoards ? globalBoards.view(q) : null)`, `seasons: () => ({ current: SEASON, all: knownSeasons() })`, and `onFriendsConsent: () => { globalBoards?.requestFriendsAccess(); }`.

3. In the Phase 67 account block: compute `const pgsIds = leaderboardIdsFor({ native: pgsNative });` before the provider, and pass `orders: scoreOrdersFor(pgsIds)` to createFakePlayGames (the browser dev loop then exercises real submissions against dev IDs; native keeps content/leaderboards.js).

4. After the account controller and its Phase 67 subscribe (D-01, D-02, D-04, D-07): create pgsQueue = createSubmissionQueue({ storage: window.mzStorage, provider, ids: pgsIds, season: SEASON, isCompeting: () => account.state().compete === true, isSignedIn: () => account.identity().signedIn === true, onFlushed: handlePgsFlush, onSeasonDrop: notePgsSeasonDrop }); create globalBoards = createGlobalBoards({ provider, ids: pgsIds, isActive: () => account.identity().signedIn === true, playerId: () => account.identity().player?.id || "", onChange: () => boardsPanel.refresh() }); call pgsQueue.load() without awaiting; setRunRecordedListener(onRunRecorded); account.subscribe(onAccountForPgs); window.addEventListener("online", () => pgsQueue?.flush({ force: true })); document.addEventListener("visibilitychange", () => { when document.visibilityState is "visible", pgsQueue?.flush() }).

5. Function declarations:
   - onRunRecorded(summary): liveDeathHash = summary.hash; window.__mzPlacement = null; pgsQueue?.enqueue(summary) (not awaited; the queue itself refuses when Compete is OFF, D-04).
   - handlePgsFlush({ submitted, standing }) (D-10..D-12): return when standing is null or submitted is empty. Find the live entry (hash === liveDeathHash). When it exists and window.__mzState?.get()?.dead is true, build placementLine({ rank, total, newBest, hash }); when that gives a line, set window.__mzPlacement = { hash, line, fresh: true } and call renderRankLine(document.getElementById("cb-over"), window.__mzPlacement) (the classic global; a missing element is a no-op). Every other submitted entry (including the live one when its panel is gone) goes into one deferredPlacementCard({ count, rank, total, newBest: any of them true, hash: the last one's hash }); park it with parkPgsCard.
   - notePgsSeasonDrop(count) (D-03): the seasonDropLine text inside `<span class="beat">…</span>` through window.logLine; nothing for a null line.
   - onAccountForPgs(state) (D-02, D-04): track the previous status and compete in module lets; when compete is false and was not already false, call pgsQueue.purge(), globalBoards.clear() and reset window.__mzPlacement; when the status becomes "signedIn" from anything else, call pgsQueue.flush({ force: true }); when it leaves "signedIn", call globalBoards.clear().
   - parkPgsCard(card): ignore null; pendingPgsCard = card (latest wins); flushAccountCard().
   - Extend flushAccountCard(): keep the Phase 67 account-card delivery first; then, when pendingPgsCard is set and the dungeon is visible, deliver it through window.mzRailLine(card.title, card.line, card.tone, card.hold) after clearing the slot, unless an account card was delivered in this same call, in which case schedule flushAccountCard() again with setTimeout after that account card's hold plus 600 ms (the rail holds one card; a second push would replace the first).

6. registerNativeChrome: replace the bare waitForPending property with a function awaiting Promise.all of the adapter's waitForPending() and pgsQueue?.waitForPending() (D-02, the background flush). Keep its comment and add a Phase 68 sentence.

7. Finish test/unit/shell-pgs.test.js with every behaviour bullet, extracting the function declarations by region and evaluating them with recording fakes (queue, global boards, account state, window.mzRailLine, window.logLine, a fake timer and a fake document), as 67-08's shell-account test does.
  </action>
  <verify>
    <automated>node --test test/unit/shell-pgs.test.js test/unit/shell-account.test.js test/unit/shell-boards-panel.test.js test/unit/shell-boards-entry.test.js test/unit/shell-new-best.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js test/unit/shell-no-content-copies.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "setRunRecordedListener(onRunRecorded)" mazeworld.html` equals 1 and `grep -c "createSubmissionQueue({" mazeworld.html` equals 1 and `grep -c "createGlobalBoards({" mazeworld.html` equals 1
    - `grep -c "onFriendsConsent:" mazeworld.html` equals 1 and `grep -c "function handlePgsFlush" mazeworld.html` equals 1
    - The pinned line `import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from "./src/browser/engineAdapter.js";` still counts exactly 1
    - `node --test test/unit/shell-pgs.test.js` passes with at least 22 tests, and the Phase 66/67 shell tests listed in verify still pass
  </acceptance_criteria>
  <done>Deaths flow into the queue, flush results flow back to the death panel or one card, Compete OFF purges, and the panel's global views run through the controller, all without a modal or a blocked tap.</done>
</task>

<task type="auto">
  <name>Task 3: docs/SHELL-MODULES.md Phase 68 section and the full-suite gate</name>
  <files>docs/SHELL-MODULES.md</files>
  <read_first>
    - docs/SHELL-MODULES.md (the intro paragraph, the Leaderboards panel (Phase 66) section and the Play Games account (Phase 67) section)
    - mazeworld.html (Tasks 1 and 2)
  </read_first>
  <action>
1. docs/SHELL-MODULES.md: name scoreTag.js, boardScores.js, pgsQueue.js, globalBoards.js and placement.js in the intro paragraph. In the Phase 67 section, replace the sentence about reserved Phase 68 methods with the bound leaderboard methods (submitScore, loadTopScores, loadPlayerScore, loadStanding, friendsAccess). Add "### Global boards, submissions and placement (Phase 68)" after the Phase 67 section covering: the tag and score encodings (scoreTag.js, boardScores.js, content/leaderboards.js with placeholder IDs until Phase 69); the queue (ddr.pgsqueue.v1, enqueue-then-flush at death through setRunRecordedListener, flush triggers: sign-in, online, visibility; ack per run and board; season drop to the Oracle; purge on Compete OFF; the background flush waits for it); the global-boards controller (cache per season/board/scope for about 5 minutes, the consent request only from the panel button, zero calls when inactive) and the panel seams (global, seasons, onFriendsConsent); the placement routing (the rank line on THAT IS THAT through window.__mzPlacement and renderRankLine; later placements as one parked rail card delivered after any account card); and the dev loop (fake provider, dev IDs). Keep the product name "Delve, Die, Repeat" wherever a product name is needed.
2. Run `node tools/bridge-doc.mjs --check` and `npm test`. Record the test totals in the SUMMARY.
  </action>
  <verify>
    <automated>node tools/bridge-doc.mjs --check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Global boards, submissions and placement (Phase 68)" docs/SHELL-MODULES.md` equals 1 and `node tools/bridge-doc.mjs --check` exits 0
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow); parity stays green and `git diff --stat -- test/parity/prototype-master.js.txt` is empty
  </acceptance_criteria>
  <done>The Phase 68 wiring is documented beside the Phase 66 and 67 sections and the whole suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| copy → Oracle innerHTML | the season-drop line is rendered through window.logLine |
| PGS rank → DOM | rank and total numbers reach THAT IS THAT and the rail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-12 | Tampering (injection) | notePgsSeasonDrop | low | mitigate | the line is content copy pinned free of <, > and & (68-03) with only a numeric count filled in; no remote text reaches logLine |
| T-68-13 | Tampering (injection) | renderRankLine | low | mitigate | textContent only; the line comes from placementLine with integer tokens |
| T-68-14 | Information disclosure | Compete OFF | high | mitigate | onAccountForPgs purges the queue and clears the global cache on Compete OFF; the queue and the global controller both refuse while inactive (68-04, 68-05) |
</threat_model>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow), including parity 48/48, bridge-registry, stale-terms, shell-no-content-copies and every Phase 66/67 shell test
- `node tools/bridge-doc.mjs --check` exits 0
- No Android build, APK, AAB or device step in this phase
</verification>

<success_criteria>
- PGS-03/PGS-04: deaths are queued durably and submitted; offline and signed-out-but-competing runs flush later; nothing is sent twice
- PGS-05: the panel's global views are live behind the account state
- PLACE-01: the DEEPEST rank quip appears on THAT IS THAT after the submit
- PLACE-02: later placements arrive as one rail card; signed-out or Compete-OFF runs show nothing
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `window.__mzPlacement` | module-owned bridge | { hash, line, fresh } or null; registered in src/browser/bridge.js |
| `renderRankLine(host, placement)` | classic function | the THAT IS THAT rank line |
| `.cb-over-rank`, `@keyframes mwrankin` | CSS | fade-in, removed by the blanket reduced-motion rule |
| `pgsQueue`, `globalBoards`, `liveDeathHash`, `pendingPgsCard`, `pgsIds` | module state | presentation/shell state, never on S |
| `onRunRecorded`, `handlePgsFlush`, `notePgsSeasonDrop`, `onAccountForPgs`, `parkPgsCard` | module functions | flushAccountCard extended for the second slot |
| createBoardsPanel `global`, `seasons`, `onFriendsConsent` | shell wiring | |
| docs/SHELL-MODULES.md Phase 68 section | doc | |
| test/unit/shell-pgs.test.js | new test | |

## Flagged assumptions (unresolved edge rows)

- PLACE-01 (edge probe: unclassified): if the rank returns after the player has already buried the run, it is reported as the D-12 rail card instead of being dropped, so every submitted run reports its placement once when the rank request succeeds.
- PLACE-02 (edge probe: unclassified): the card parks until the dungeon is visible (the Phase 67 rule); a card raised during combat is handed to the rail as usual and the rail's own combat hiding decides whether it is seen before it expires (the same judgement 67-08 recorded).
- The 'visibilitychange' flush respects the backoff, while 'online' forces one, because a reconnect is the strongest signal that a retry will work.

<output>
Create `.planning/phases/68-global-boards-submissions-you-placed-x/68-07-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch (docs/UAT-v2.0.md): (1) signed in with real IDs, a death shows "You placed Nth of M." under the NEW PERSONAL BEST block, fading in; (2) the same with reduced motion on shows it without the fade; (3) die in airplane mode, relaunch online: one "THE LEDGER CAUGHT UP" rail card once the map shows; (4) Compete OFF: no rank line, no card, no Play Games traffic; (5) the panel's ALL, FRIENDS (consent button) and LINEAGE views on the device; (6) the browser dev loop with the dev "simulate signed-in" setting shows the rank line and ALL rows. Do NOT create `.planning/phases/68-*/deferred-items.md`.
</output>
