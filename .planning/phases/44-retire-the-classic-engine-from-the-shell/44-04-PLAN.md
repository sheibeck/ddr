---
phase: 44-retire-the-classic-engine-from-the-shell
plan: 04
type: execute
wave: 4
depends_on: ["44-03"]
files_modified:
  - mazeworld.html
  - test/persistence/dual-write-convergence.test.js
  - test/persistence/harness/sandboxClassicPersistence.js
  - test/unit/shell-combat-over.test.js
  - test/unit/shell-input-guards.test.js
  - test/unit/shell-map-rail.test.js
autonomous: true
requirements: [DEAD-01, DEAD-03]
tags: [dead-code, shell, persistence, boot, orphan-sweep, closing-gates]

must_haves:
  truths:
    - "The classic run-save path is gone: no classic save()/load(), no classic SAVE_KEY, no @gsd:dual-write-convergence-extract:save sentinel; src/browser/engineAdapter.js#persist()/boot() is the one run-save path and the graves sentinel block is byte-identical to ba45dfd"
    - "window.__mzClassicBoot runs only renderGravesLoading → await loadGraves → renderGraves → fit → paint; a cold boot with no save no longer rolls a classic character or writes a classic-shaped save, and the two resume Oracle lines ('Delve resumed.' + '{name}, {race} {sub}, skill level {ROMAN}, on floor {depth} of the dungeon.') are emitted by the module boot path only when hadSaveAtLaunch is true"
    - "No else-newGame() / else-classic fallback survives: the death-card CONFIRM calls window.mzReturnToTitle(), the won-path AGAIN calls window.mzStartRoll(), Save & quit calls window.mzAbandonRun(), Abandon-character calls window.mzStartRoll() (dead) or window.mzAbandonCharacter() (alive) — and the module's now-callerless window.newGame override is deleted too"
    - "The fixed-point orphan sweep leaves no classic top-level declaration that has zero references outside its own body (tools/shell-sweep.mjs orphans reports only the by-ruling exception saveGraves and any name recorded as kept-with-live-reference); every deleted symbol reports refs 0"
    - "ROADMAP success criteria 1–5 all hold at the closing commit: the 16-name mirror grep prints 0, wc -l < mazeworld.html ≤ 6710, the DEAD-02 pin is green, grep -rl 'new Function' test/ is empty, npm test ends '# fail 0' (shell-map-invariants included), npm run build:www exits 0, npm run boot:check passes, and git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt prints nothing"
    - statement: "Dropping the save half of the dual-write test and its harness changes the outcome or relative order of no other persistence test: node --test test/persistence reports '# fail 0' and the graves case still extracts and runs the live sentinel block"
      verification: backstop
  artifacts:
    - path: "mazeworld.html"
      provides: "the live shell only — ≤ 6,710 lines, no classic engine, no classic persistence, slimmed __mzClassicBoot"
      contains: "window.__mzClassicBoot = async function classicBoot()"
    - path: "test/persistence/harness/sandboxClassicPersistence.js"
      provides: "vm extraction of the graves sentinel block only"
      contains: "dual-write-convergence-extract:graves:start"
  key_links:
    - from: "mazeworld.html module boot (after window.__mzState.set(engineState))"
      to: "window.logLine"
      via: "resume banner gated on hadSaveAtLaunch"
      pattern: "Delve resumed\\."
    - from: "mazeworld.html module boot"
      to: "window.__mzClassicBoot"
      via: "await after window.mzStorage is assigned + migrated"
      pattern: "await window\\.__mzClassicBoot\\(\\)"
  prohibitions:
    - statement: "MUST NOT change any byte under engine/, content/, test/parity/fixtures/ or test/parity/prototype-master.js.txt, and MUST NOT touch src/browser/engineAdapter.js or storage.js — the engine's persistence is the surviving path, not something to 'tidy' here"
      status: resolved
      verification: test
    - statement: "MUST NOT keep any else-newGame()/else-classic fallback alive 'for safety' and MUST NOT resurrect a classic newGame/save/load under another name — a missing bridge is a boot bug to surface"
      status: resolved
      verification: test
    - statement: "MUST NOT delete the graves sentinel block, saveGraves()/loadGraves()/renderGraves(), or the player-facing resume Oracle lines — the lines move, they do not disappear"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete a declaration the sweep reports as referenced from live code and MUST NOT purge comment archaeology inside surviving live functions (Phase 48 DOCS-01) — only comments attached to deleted code go"
      status: resolved
      verification: judgment
---

<objective>
Retire the last live foothold of the classic engine — its run-save persistence and its cold-boot character roll — slim `window.__mzClassicBoot` to the graves + fit + paint sequence, re-home the two player-facing resume lines in the module boot path, delete the five `else newGame()` fallbacks and the module's now-callerless `window.newGame` override, drop the `save` half of the dual-write extraction test and harness (DEAD-03), then sweep the shell to a fixed point (every classic declaration with zero references goes) and run the phase's closing gates (ROADMAP success criteria 1–5, DEAD-01).

Purpose: this is the one plan with live-code edits (`__mzClassicBoot`, the module boot, the button handlers) — kept small, in their own commits, each proven by the headless boot gate. It closes the phase with the consolidated deleted-symbol list and the per-test reason lines the ROADMAP requires.
Output: `mazeworld.html` ≤ 6,710 lines; `dual-write-convergence.test.js` + harness graves-only; three module-region test anchors re-pointed; `44-04-SUMMARY.md` with the phase-wide consolidated lists and gate outputs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-CONTEXT.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-01-SUMMARY.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-02-SUMMARY.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-03-SUMMARY.md
@src/browser/engineAdapter.js
@test/persistence/harness/sandboxClassicPersistence.js
@test/persistence/dual-write-convergence.test.js
</context>

<phase_facts>
- Verified at planning: the module's `window.newGame = async function engineNewRun()` (`ba45dfd` L8020–8040) has NO caller once the five classic fallback sites are gone — zero non-comment `newGame(` calls exist in the module script, `src/`, or `tools/`; the roller commits through `window.mzStartRoll` → `commitRolledState`. It exists only to override the classic global (its own RUN-05 comment says so) → it is a helper only the dead code referenced (DEAD-01).
- Verified at planning: the module does NOT emit any resume banner today (`grep -n "resumed" mazeworld.html` hits only the classic boot) — so the two lines MUST move, not be dropped (CONTEXT ruling).
- Verified at planning: the classic `SAVE_KEY` literal equals engineAdapter's (`"ddr.delve.v1"`), so today's cold boot with no save writes a classic-shaped save over the engine's key via `classicNewGame()` → `save()` — the "last live foothold" the CONTEXT names.
- `hadSaveAtLaunch` (module, `ba45dfd` L7652–7659) is true only for a live (not dead/won) save. The classic `load()` also "resumed" dead/won saves and printed the banner for them; the module gate does not — record this as the one deliberate behaviour difference (nothing to resume into; ENTER opens the roller).
- Three tests use `"window.newGame = "` as a region END marker (`shell-combat-over.test.js` L117, `shell-input-guards.test.js` L91, `shell-map-rail.test.js` L89); the next module-level assignment after it is `window.mzDevStartAtDepth = ` (`ba45dfd` L8048).
- Line numbers are `ba45dfd` numbers; locate by name.
</phase_facts>

<flagged_assumptions>
- **A-1 (carried):** the fixed-point sweep candidates (`D`, `say`, `clamp`, `skill`, `mwActiveTab`, `newBeat`, `CAPTURE`, `GH`, `canRead`, `escText`, `maxCharges`, `upkeep`, `R_`, `skillTable`, `eff`, `logEl`, `cv`, `ctx`) are decided by `refs`, not by this list; most are LIVE and must stay.
- **A-2 (carried):** `saveGraves()` stays inside the untouched graves sentinel block by CONTEXT ruling although `bury()` (its last classic caller) is gone; the harness still exercises it. Recorded as "kept by ruling".
- **A-5:** the classic `eff(key)` keeps its legacy sum-over-`c.items` fallback loop — that dual path is Phase 45's (HEDGE-01..03), not this phase's; do not collapse it here.
</flagged_assumptions>

<artifacts_this_phase_produces>
- The two "Delve resumed." Oracle lines re-homed in the module boot path, gated on `hadSaveAtLaunch` (this plan; text unchanged).
- `test/persistence/harness/sandboxClassicPersistence.js` — graves-only extraction (export name `loadClassicPersistenceSandbox` unchanged) (this plan).
- (Plan 44-01) `tools/shell-boot-check.mjs`, `tools/shell-sweep.mjs`, `boot:check`, the 504-case parley matrix. (Plan 44-03) `window.__mzTables`, `test/unit/shell-no-content-copies.test.js`.
- Deleted symbols: consolidated across all four plans in this plan's SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Retire classic persistence + the cold-boot classic roll — slim __mzClassicBoot, re-home the resume lines, delete the five fallbacks and the callerless window.newGame; graves-only dual-write test</name>
  <files>mazeworld.html, test/persistence/dual-write-convergence.test.js, test/persistence/harness/sandboxClassicPersistence.js, test/unit/shell-combat-over.test.js, test/unit/shell-input-guards.test.js, test/unit/shell-map-rail.test.js</files>
  <read_first>
    - mazeworld.html (ba45dfd): L1792 (`const SAVE_KEY`), L5544–5552 (`wireDeathConfirm` fallback), L6272–6276 (`btn-again` fallback), L6713–6740 (`/* save / load */` banner + the `save` sentinel block: comment, `save()`, `load()`), L6742–6759 (`/* boot */` banner, `newGame`, the `classicNewGame` capture + its comment), L6760–6791 (`btn-save-quit` and `btn-abandon-character` handlers with their fallback comments), L7153–7181 (`__mzClassicBoot` + its 02-03 comment), L7636–7670 (module boot: `TITLE_SAVE_KEY`, `hadSaveAtLaunch`, `boot(freshSeed)`, `__mzState.set`, first `paint()`/`draw()`), L8005–8040 (the RUN-05 comment + `window.newGame = async function engineNewRun()`), L8216–8224 (the comment above `await window.__mzClassicBoot()` that describes the deleted `classicNewGame` fallback), L2868–2877 (`logLine`/`say`)
    - src/browser/engineAdapter.js L40–75 (its own `SAVE_KEY`/`GRAVE_KEY`; `persist()`/`boot()` are the surviving run-save path — READ ONLY, never edited)
    - test/persistence/harness/sandboxClassicPersistence.js (whole file, 95 lines), test/persistence/dual-write-convergence.test.js L1–60 (setup, `SAVE_KEY` const used by the engineAdapter cases), L81–123 (the two cases that call `classic.save()`/`classic.load()`), L125–150 (the graves case — stays)
    - test/unit/shell-combat-over.test.js L115–119, test/unit/shell-input-guards.test.js L89–93, test/unit/shell-map-rail.test.js L81–92 (the `"window.newGame = "` end anchors)
    - test/unit/engineAdapter.test.js L440–450 (a comment mentioning `__mzClassicBoot` / `loadGraves` — still true after the slimming; no edit)
  </read_first>
  <action>
    Classic script: delete `const SAVE_KEY = "ddr.delve.v1";`; delete the `/* ---------------- save / load ---------------- */` banner and the entire `@gsd:dual-write-convergence-extract:save` block (start marker, its 02-03 comment, `save()`, `load()`, end marker); delete `function newGame()` and the `const classicNewGame = newGame;` capture with its comment; keep the `/* ---------------- boot ---------------- */` banner only if code still follows it. Collapse the five fallbacks to their single bridge calls: `wireDeathConfirm`'s `guardTap` body becomes `window.mzReturnToTitle();`; `btn-again`'s onclick becomes `() => window.mzStartRoll()`; `btn-save-quit`'s onclick body becomes `window.mzAbandonRun();`; `btn-abandon-character`'s body becomes `if (st && st.dead) { window.mzStartRoll(); } else { window.mzAbandonCharacter(); }` (keep the `st` read and the DR16 comment explaining dead → new roll); rewrite the two comment blocks above `btn-save-quit`/`btn-abandon-character` so they no longer describe a `|| newGame()` fallback (they now say the bridge is the only path). Slim `window.__mzClassicBoot` to exactly: `renderGravesLoading(); await loadGraves(); renderGraves(); fit(); paint();` and rewrite its comment to say what it now is (the classic script's graves load + first fit/paint, deferred until the module has assigned `window.mzStorage`), removing the `load`/`newGame` prose.

    Module script: in the boot sequence, immediately after `window.__mzState.set(engineState);` and before `window.paint();`, add the resume banner gated on `hadSaveAtLaunch`: `window.logLine('<span class="banner">Delve resumed.</span>')` followed by `window.logLine(` + a template producing `${engineState.c.name}, ${engineState.c.race} ${engineState.c.sub}, skill level ${ROMAN[engineState.c.level - 1]}, on floor ${engineState.floor.depth} of the dungeon.` + `)` using the module's own `ROMAN` import (added in 44-03); the two strings are byte-identical to the classic boot's. Add a short comment: moved from the classic boot (Phase 44); emitted only for a live save because a dead/won save is not resumed (ENTER opens the roller) — the one deliberate difference from the classic `load()` banner. Delete the `window.newGame = async function engineNewRun() { … };` block together with its RUN-05 doc comment (no caller remains — verify first with `grep -nE "(^|[^.A-Za-z_])newGame\(" mazeworld.html` and `grep -rn "newGame" src/ tools/` showing only comments). Rewrite the comment above `await window.__mzClassicBoot();` (`ba45dfd` L8216–8223) to describe the slimmed boot (graves + fit + paint after `mzStorage` is assigned) — no `classicNewGame` prose. Leave `commitRolledState`'s and `mzDevStartAtDepth`'s comments alone (mentions of `engineNewRun` inside surviving live code are Phase 48 DOCS-01).

    Tests, same commit: `sandboxClassicPersistence.js` — remove the `saveBlock` extraction and the `SAVE_KEY` sandbox global, run only the graves block, and rewrite the header + JSDoc to describe graves-only extraction (keep `extractBlock`'s loud-failure behaviour and the `graves` getter/setter); `dual-write-convergence.test.js` — delete the two cases "mazeworld.html's OWN extracted save()/load() functions … read/write through window.mzStorage" and "dual-write convergence: engineAdapter's write and the classic script's write land in the SAME backend/key …" (reason: the classic save path no longer exists; engineAdapter persistence is pinned by the file's own first two cases and `engineAdapter.test.js`), keep the graves case and the engineAdapter cases (the file's `SAVE_KEY` const stays — those cases read it), and update the file's header comment accordingly; `shell-combat-over.test.js`, `shell-input-guards.test.js`, `shell-map-rail.test.js` — change the region END marker `"window.newGame = "` to `"window.mzDevStartAtDepth = "` (same start anchors; each region still holds the code its assertions target).

    Gates: `node tools/shell-sweep.mjs refs SAVE_KEY save load newGame classicNewGame` (all 0 — `load`/`save` bare identifiers must be 0; `window.mzStorage.setItem` etc. are not matches); `grep -c "Delve resumed." mazeworld.html` prints `1` (module only); `npm run build:www`; `npm run boot:check` (cold boot, no save: proves the slimmed classic boot + first paint still work and that no classic roll runs — the `painted`/`graves` checks). The resume-with-a-save path cannot be driven headlessly without interaction (a save is written only by a dispatched action), so it is proven statically here — the two `window.logLine(` lines sit inside `if (hadSaveAtLaunch) { … }` between `window.__mzState.set(engineState);` and `window.paint();` (grep the line numbers in order) — and on the Pixel 7 at milestone close (force-stop + relaunch with a live save → "Delve resumed." in the Oracle). Then `npm test`. Commit: `refactor(44-04): retire classic persistence and the cold-boot classic roll — slim __mzClassicBoot, resume lines in the module boot, no newGame fallbacks (DEAD-01/DEAD-03)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/shell-sweep.mjs refs SAVE_KEY save load newGame classicNewGame && grep -c "Delve resumed." mazeworld.html && grep -c "dual-write-convergence-extract:save" mazeworld.html test/persistence/harness/sandboxClassicPersistence.js; npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/shell-sweep.mjs refs SAVE_KEY save load newGame classicNewGame` exits 0 with `refs NAME: 0` for all five.
    - `grep -c "dual-write-convergence-extract:save" mazeworld.html` prints `0`; `grep -c "dual-write-convergence-extract:graves:start" mazeworld.html` prints `1`; `git diff ba45dfd -- mazeworld.html | grep -cE "^[-+].*(GRAVE_KEY|async function loadGraves|async function saveGraves)"` prints `0` (the graves block is byte-identical to ba45dfd).
    - `grep -c "Delve resumed." mazeworld.html` prints `1`, and `grep -n 'window.__mzState.set(engineState);\|Delve resumed.\|on floor ${engineState.floor.depth} of the dungeon.\|^\s*window.paint();' mazeworld.html` lists the set line, then the banner line, then the character line, then a `window.paint();` line, in that order; `grep -c "if (hadSaveAtLaunch)" mazeworld.html` prints `1`.
    - `sed -n '/^window.__mzClassicBoot = async function classicBoot() {$/,/^};$/p' mazeworld.html` prints exactly 7 lines: the header, `renderGravesLoading();`, `await loadGraves();`, `renderGraves();`, `fit();`, `paint();`, `};` (indentation aside).
    - `grep -cE "else newGame\(\)|: newGame\(\)|window\.newGame = " mazeworld.html` prints `0`; `grep -c "window.mzReturnToTitle();" mazeworld.html` ≥ 1; `grep -c "() => window.mzStartRoll()" mazeworld.html` ≥ 1; `grep -c "window.mzAbandonRun();" mazeworld.html` ≥ 1; `grep -c "window.mzAbandonCharacter();" mazeworld.html` ≥ 1.
    - `grep -c "classic.save()" test/persistence/dual-write-convergence.test.js` prints `0`; `grep -c "saveBlock" test/persistence/harness/sandboxClassicPersistence.js` prints `0`; `grep -c '"window.mzDevStartAtDepth = "' test/unit/shell-combat-over.test.js test/unit/shell-input-guards.test.js test/unit/shell-map-rail.test.js` prints `1` for each file; `grep -c '"window.newGame = "' test/unit/*.test.js` prints `0` for every file.
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines; `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `node --test test/persistence test/unit/shell-map-invariants.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt src/browser/engineAdapter.js src/browser/storage.js` prints nothing.
  </acceptance_criteria>
  <done>No classic persistence, no classic boot roll, no fallbacks, no callerless override; the resume lines live in the module boot; the dual-write test is graves-only; build + headless boot + suite green at the commit.</done>
</task>

<task type="auto">
  <name>Task 2: Fixed-point orphan sweep — every classic declaration with zero references goes, with its attached comments; nothing live is touched</name>
  <files>mazeworld.html</files>
  <read_first>
    - The `## Kept, with the live reference` and `## Flagged assumptions` sections of 44-01/02/03-SUMMARY.md (names already ruled on)
    - mazeworld.html: the current output of `node tools/shell-sweep.mjs orphans` (run it first), and for each candidate the lines `node tools/shell-sweep.mjs refs <name>` prints
    - mazeworld.html L1793–1795 (ba45dfd: `D`, `clamp`), L1733 (`mwActiveTab`), L2225 (`maxCharges`), L2830–2842 (`newBeat`/`CAPTURE`, if still present), L2868–2877 (`logEl`, `logLine`, `say`), L3881–3895 (`eff` — KEEP, A-5), L5286–5330 (the graves sentinel block — KEEP, A-2)
  </read_first>
  <action>
    Iterate to a fixed point: run `node tools/shell-sweep.mjs orphans`; for every listed classic declaration EXCEPT `saveGraves` (A-2) and any name a previous SUMMARY recorded as kept-with-live-reference, run `refs <name>`; if the only hits are the declaration itself (and, for a function, references inside its own body), delete the declaration together with the doc comment attached directly above it and any section banner that now heads nothing; then re-run `orphans` and repeat until it lists nothing deletable. Expected to fall out here (decide by `refs`, not by this list): `D` (its last readers were the classic `WEAPONS`/`CLASSES` closures), `say` (alias of `logLine`; the module calls `window.logLine`), possibly `mwActiveTab`, `newBeat`/`CAPTURE` if 44-02 left them. Expected to stay LIVE (do not delete): `clamp`, `skill`, `skillTable`, `R_`, `upkeep`, `maxCharges`, `eff`, `canRead`, `escText`, `logEl`, `logLine`, `cv`, `ctx`, `GW`/`GH` (one declaration line — leave it if either is live), `S`, `window.__mzState`, the graves block, every `paint`/`draw`/`render*`/`show*`/`fit`/camera/`tapStep`/`wireDeathConfirm`/`COMBAT_DISPATCH` surface. If a top-level declaration becomes dead only because a `window.X =` classic assignment referenced it and that assignment itself has no module reader, treat the pair as a unit and delete both only if `refs X` and `refs __mzX`-style checks are all 0 — otherwise keep (record it). Do NOT edit comments inside surviving functions even when they mention deleted names ("dead classic X()" archaeology is Phase 48 DOCS-01); DO delete a standalone comment block whose only subject is deleted code.

    Gates after the last iteration: `node tools/shell-sweep.mjs orphans` output (record it — must be only `saveGraves` plus recorded kept names, or empty); `node tools/shell-sweep.mjs refs <every name deleted in this task>` all 0; `npm run build:www`; `npm run boot:check`; `npm test`. Commit: `refactor(44-04): fixed-point orphan sweep of the classic script (DEAD-01)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/shell-sweep.mjs orphans && npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/shell-sweep.mjs orphans` lists no classic declaration other than `saveGraves` and names recorded in a SUMMARY as kept-with-live-reference (paste the output into the SUMMARY).
    - `node tools/shell-sweep.mjs refs <every name deleted in this task>` exits 0 with `refs NAME: 0` for each.
    - `grep -cE "^const GW = 21, GH = 21;" mazeworld.html` prints `1`; `grep -cE "^function eff\(key\)" mazeworld.html` prints `1`; `grep -c "async function saveGraves()" mazeworld.html` prints `1`; `grep -c "window.__mzState = { get: () => S, set: (v) => { S = v; } };" mazeworld.html` prints `1`.
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines; `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`.
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing.
  </acceptance_criteria>
  <done>The classic script has reached its fixed point: no orphan remains except the by-ruling exception; every deletion is proven by `refs 0`; gates green.</done>
</task>

<task type="auto">
  <name>Task 3: Closing gates — ROADMAP success criteria 1–5 verbatim, the phase-wide consolidated lists, and the deferred human-verification list</name>
  <files>.planning/phases/44-retire-the-classic-engine-from-the-shell/44-04-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md L128–140 (Phase 44 goal, baseline and the five success criteria — the commands below are those criteria verbatim, adjusted for the measured 8,710-line start)
    - .planning/phases/44-retire-the-classic-engine-from-the-shell/44-01-SUMMARY.md, 44-02-SUMMARY.md, 44-03-SUMMARY.md (the deleted-symbol lists, the per-test reason lines and the kept-with-live-reference lists to consolidate)
    - .planning/REQUIREMENTS.md L15–17 (DEAD-01/02/03 wording)
  </read_first>
  <action>
    Run every closing gate and capture its exact output for the SUMMARY: (1) `grep -cE "^\s*function (castSpell|parley|startCombat|meetJoiner|genFloor|rollCharacter|descend|makeCamp|takeItem|useItem|playerStrike|foeTurn|killFoe|openStore|readScroll|drinkPotion)\s*\(" mazeworld.html` → `0`; `npm run build:www` → exit 0; `npm run boot:check` → four PASS (the app parses and boots to a painted map tab behind the title screen; the interactive ENTER → roller → map walk is the deferred Pixel 7 item). (2) `wc -l < mazeworld.html` → ≤ 6710 (phase start 8,710; record the exact number and the delta). (3) `node --test test/unit/shell-no-content-copies.test.js` → `# fail 0` with `# pass 5`. (4) `grep -rl "new Function" test/` → empty; `node --test test/unit/shell-map-invariants.test.js` → `# fail 0`; `npm test` → `# fail 0` (record the final test count vs the 3,257 start and reconcile the difference against the declared test deletions/additions across the four plans). (5) `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` → prints nothing; `git hash-object test/parity/prototype-master.js.txt` unchanged vs `git rev-parse ba45dfd:test/parity/prototype-master.js.txt`. Also `git status --porcelain` → empty after the SUMMARY commit, and `grep -rn "classicNewGame\|__mzCanCast\|dual-write-convergence-extract:save" mazeworld.html test/ src/` → nothing.

    Write `44-04-SUMMARY.md` with, in addition to this plan's own sections: `## All deleted symbols (phase total)` — the union of the four plans' lists, one line per symbol with its `ba45dfd` line range and the plan/commit that removed it (this is ROADMAP criterion 1's "lists every deleted symbol from an orphan sweep"); `## All tests re-pointed or deleted (phase total)` — the union of the per-test reason lines (ROADMAP criterion 4); `## Kept by ruling / kept with live reference` — `saveGraves` (A-2), `eff`'s legacy loop (A-5, Phase 45), any A-1 survivors; `## Deliberate behaviour differences` — the dead/won-save resume banner no longer printed (nothing to resume), the Hero/Gear/graves text now showing `content/` values where the classic copies had drifted; `## Success criteria` — the five ROADMAP criteria with their command outputs; `## Human verification (deferred to end of run)` — the consolidated Pixel 7 checklist for milestone close: cold boot with no save → title → ENTER → roller → map (no classic roll behind the title); force-stop + relaunch with a live save → title → ENTER resumes and the Oracle shows "Delve resumed." + the character line; death → CONFIRM returns to the title and the graveyard gains the stone (graves path untouched); Save & quit and Abandon character both work with no dead button; the Hero-tab dossier for a Thief/Fighter/Magic User; MAKE CAMP → SLEEP; tap-to-move; SPELLS/PARLEY/SING/USE/DRINK/READ in a fight. Commit: `docs(44-04): phase 44 closing summary — consolidated deleted symbols, test re-points, gates`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test "$(grep -cE '^\s*function (castSpell|parley|startCombat|meetJoiner|genFloor|rollCharacter|descend|makeCamp|takeItem|useItem|playerStrike|foeTurn|killFoe|openStore|readScroll|drinkPotion)\s*\(' mazeworld.html)" = "0" && test "$(wc -l < mazeworld.html)" -le 6710 && test -z "$(grep -rl 'new Function' test/)" && test -z "$(git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt)" && npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - The 16-name mirror grep prints `0`.
    - `wc -l < mazeworld.html` prints a number ≤ `6710`.
    - `node --test test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# pass 5"` prints `# pass 5`.
    - `grep -rl "new Function" test/` prints nothing; `node --test test/unit/shell-map-invariants.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`.
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing; `test "$(git hash-object test/parity/prototype-master.js.txt)" = "$(git rev-parse ba45dfd:test/parity/prototype-master.js.txt)"` succeeds.
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines.
    - `grep -c "## All deleted symbols (phase total)" .planning/phases/44-retire-the-classic-engine-from-the-shell/44-04-SUMMARY.md` prints `1`; `grep -c "## Human verification (deferred to end of run)" …/44-04-SUMMARY.md` prints `1`; `grep -c "## All tests re-pointed or deleted (phase total)" …/44-04-SUMMARY.md` prints `1`.
    - `git status --porcelain` prints nothing after the final commit.
  </acceptance_criteria>
  <done>All five ROADMAP success criteria hold with captured outputs; the phase-wide consolidated symbol and test lists exist; the Pixel 7 checklist is batched for milestone close; the tree is clean.</done>
</task>

</tasks>

<verification>
- Three commits (persistence/boot, sweep, summary), suite green at each; headless boot passes at each.
- ROADMAP success criteria 1–5 verified verbatim in Task 3.
- Engine gate: zero bytes under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt` since `ba45dfd`.
</verification>

<success_criteria>
- `mazeworld.html` ≤ 6,710 lines, carrying only the live shell: no classic mirrors, engine, tables, persistence or boot roll; no fallbacks; the resume lines re-homed.
- `tools/shell-sweep.mjs orphans` is at fixed point (only the by-ruling exception).
- Consolidated deleted-symbol list and per-test reason lines delivered in the closing SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/44-retire-the-classic-engine-from-the-shell/44-04-SUMMARY.md` with: `## Deleted symbols (this plan)` (Task 1 + Task 2 with `ba45dfd` ranges, including the module's `window.newGame` override); `## Tests re-pointed or deleted` (dual-write-convergence: two save cases deleted + harness graves-only; shell-combat-over / shell-input-guards / shell-map-rail end anchor → `"window.mzDevStartAtDepth = "`); `## All deleted symbols (phase total)`; `## All tests re-pointed or deleted (phase total)`; `## Kept by ruling / kept with live reference`; `## Deliberate behaviour differences`; `## Success criteria` (five, with outputs); `## Gate outputs` (per commit); `## Human verification (deferred to end of run)`; `## Flagged assumptions` (A-1..A-5 final status).
</output>
