---
phase: 44-retire-the-classic-engine-from-the-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/shell-boot-check.mjs
  - tools/shell-sweep.mjs
  - package.json
  - test/unit/parley.test.js
  - test/unit/parley-button-mirror.test.js
  - test/unit/spell-menu-mirror.test.js
  - test/unit/shell-fight-gate.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-armor-display.test.js
  - mazeworld.html
autonomous: true
requirements: [DEAD-01, DEAD-03]
tags: [dead-code, shell, tripwire-tests, boot-gate]

must_haves:
  truths:
    - "grep -rl \"new Function\" test/ prints nothing after this plan — the two extraction tripwires are gone (spell-menu-mirror deleted with its reason recorded; parley-button-mirror folded into test/unit/parley.test.js as a 504-case engine-vs-prose-oracle matrix)"
    - "The re-pointed parley matrix in test/unit/parley.test.js iterates exactly the mirror's case set — Object.keys(RACES) (6) × the 7 subs [Con Artist, Woodsman, Bard, Soldier, Ninja, Master of Arms, Court Mage] × helm [false,true] × ENC_TYPES (6) = 504 — in the mirror's loop order race → sub → helm → type, and asserts assert.equal(cases, 504)"
    - "grep -rl \"new Function\" test/ printing nothing is the SUCCESS state (zero extraction sites), not an empty-input anomaly: the only vm-based extractors left in test/ read test/parity/prototype-master.js.txt (untouched) or the live graves sentinel block"
    - "After Task 3 the 16 mirror functions plus their combat-cluster helpers have zero word-boundary references in mazeworld.html outside comments and string literals (node tools/shell-sweep.mjs refs ... exits 0), npm run build:www exits 0, npm run boot:check passes, and npm test ends with '# fail 0'"
    - statement: "Deleting/re-pointing the tripwire tests changes no other test's outcome or relative order: node --test reports '# fail 0' at every commit boundary of this plan and the suite's test count moves only by the declared deletions/additions the SUMMARY lists"
      verification: backstop
  artifacts:
    - path: "tools/shell-boot-check.mjs"
      provides: "install-free headless-Chrome boot gate for www/index.html (dump-dom + console capture), with --self-test fail-first proof"
      min_lines: 60
    - path: "tools/shell-sweep.mjs"
      provides: "refs <names>: zero-reference gate for deleted classic symbols; orphans: advisory reachability listing of classic top-level declarations"
      min_lines: 60
    - path: "test/unit/parley.test.js"
      provides: "the 504-case canParley availability matrix against the independent prose oracle (extended with IDENT-05/IDENT-06 rules)"
      contains: "assert.equal(cases, 504)"
  key_links:
    - from: "package.json"
      to: "tools/shell-boot-check.mjs"
      via: "npm script boot:check"
      pattern: "\"boot:check\": \"node tools/shell-boot-check.mjs\""
    - from: "test/unit/parley.test.js"
      to: "engine/combat.js"
      via: "import { canParley } and the expectedCanParley prose oracle (never calls the engine)"
      pattern: "expectedCanParley"
  prohibitions:
    - statement: "MUST NOT change any rule outcome or any byte under engine/, content/, test/parity/fixtures/ or test/parity/prototype-master.js.txt — the game plays identically through engine/ (engine gate, ROADMAP success criterion 5)"
      status: resolved
      verification: test
    - statement: "MUST NOT keep or add any 'else classicX()' / just-in-case fallback or a second code path for the same player action — a missing window.mz* bridge surfaces as a boot error, never as a silent classic-engine run (greenfield ruling 2026-09-17)"
      status: resolved
      verification: test
    - statement: "MUST NOT delete or weaken a live persistence path (the graves sentinel block, engineAdapter.js#persist/boot, storage.js) or the player-facing resume/death Oracle text — the two 'Delve resumed.' lines survive in the module boot path (Plan 44-04)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT delete or re-point a tripwire test silently, and MUST NOT turn a re-pointed matrix into a self-comparison (engine vs engine) — every deletion/re-point gets a one-line reason in the SUMMARY and every replayed matrix compares the engine to an independent prose oracle"
      status: resolved
      verification: judgment
---

<objective>
Stand up the phase's two mechanical gates (headless boot check + deleted-symbol reference sweep), retire the two `new Function` extraction tripwires (DEAD-03) BEFORE the classic code they extract is deleted, then delete deletion layer 1: the 16 pre-extraction classic-engine mirrors and the combat-cluster helpers only they reach (DEAD-01), re-pointing the shell source pins that layer breaks in the same commit.

Purpose: Phase 44 is a bisectable, layer-by-layer deletion of a dead engine from an 8,710-line shell with no verifier agents — the executor's own gates are the verification. This plan builds those gates first (fail-first proven), then makes the first cut with the suite green at every commit boundary (ROADMAP success criteria 1, 4).
Output: `tools/shell-boot-check.mjs`, `tools/shell-sweep.mjs`, `package.json` `boot:check` script; `test/unit/parley.test.js` 504-case matrix; `test/unit/parley-button-mirror.test.js` and `test/unit/spell-menu-mirror.test.js` deleted; `mazeworld.html` minus the 16 mirrors + combat cluster; 5 shell test pins re-pointed; `44-01-SUMMARY.md`.
</objective>

<assumption_delta_decision>
noun: the rules engine (`engine/` + `src/browser/engineAdapter.js`)
decision: promote
rationale: `engine/` is already the sole primary (every live action dispatches through `engineAdapter.dispatch()`); the classic copy in `mazeworld.html` is DELETED rather than kept alongside, per the greenfield ruling of 2026-09-17 ("no dual-path code, no legacy branches kept for safety"). The DEAD-02 source-pin test (Plan 44-03) is the "no second copy" invariant; no further invariant test is required.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-CONTEXT.md
@test/unit/parley.test.js
@test/unit/parley-button-mirror.test.js
@test/unit/spell-menu-mirror.test.js
@engine/combat.js
</context>

<phase_facts>
- Phase-start baseline (commit `ba45dfd`, verified unchanged at planning time): `mazeworld.html` = 8,710 lines; classic `<script>` = L1693–L7182 (the `<script>` tag is at column 0 on L1693, `</script>` on L7182); module `<script type="module">` = L7184–L8707. `npm test` = 3,257 tests, `# fail 0`. All line numbers in this phase's plans are `ba45dfd` numbers — locate by name (`grep -n`) before every edit, because numbers shift after each deletion.
- The planner's own reachability probe (2026-09-19; roots = HTML `on*=` attributes + the module script + classic top-level statements, object-literal keys and comment/string text excluded, `window.move`/`window.newGame` overrides and the `else makeCamp()` fallback treated as cuts) marks **128 of the 253 classic top-level declarations dead (~1,890 body lines before doc comments)**. That list is an ESTIMATE that seeds each layer; the authority is the post-deletion `tools/shell-sweep.mjs refs` gate + `npm run build:www` + `npm run boot:check` + `npm test` (flagged assumption A-1 below).
- Windows executor: Git Bash for node/npm; no `jq`; do not use heredocs for multi-line scripts (backslashes get mangled) — create every script/test file with the Write tool. Line counts via `wc -l < mazeworld.html`.
- Engine gate scope fence: zero bytes change under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt`. `test/parity/harness/sandboxPrototype.js` extracts the frozen prototype, not the shell — untouched.
- Human verification is deferred to the milestone-close Pixel 7 batch: no checkpoints in this phase; each SUMMARY carries a `## Human verification (deferred to end of run)` list.
</phase_facts>

<flagged_assumptions>
- **A-1 (DEAD-01, edge row "unclassified"):** the grep-graph deletion set (128 declarations / ~1,890 body lines, and the per-layer name lists in Plans 44-01..44-04) is an estimate with known false edges (prose words in flavor strings, object keys like `flee: () => window.mzFlee?.()`, the module's own `const reveal`/imported `RACES`/`CLASSES` bindings sharing a classic name). No declaration is deleted on the list's word alone: each layer's commit must pass `node tools/shell-sweep.mjs refs <every name deleted in that layer>` (exit 0), `npm run build:www`, `npm run boot:check` and `npm test` (`# fail 0`). A name the sweep reports as still referenced from LIVE code is NOT deleted — it is recorded in the SUMMARY under "kept, with the live reference" instead.
- **A-2:** `saveGraves()` (inside the `@gsd:dual-write-convergence-extract:graves` sentinel block) has no live caller once `bury()` goes, but the CONTEXT.md ruling keeps the whole graves block untouched this phase (the dual-write test still exercises it through the harness). Record it in the 44-04 SUMMARY as "kept by ruling", not deleted.
</flagged_assumptions>

<artifacts_this_phase_produces>
- `tools/shell-boot-check.mjs` — install-free headless boot gate (Chrome/Edge `--headless=new --dump-dom` over a built-in static server for `www/`), `--self-test` fail-first proof. (this plan)
- `tools/shell-sweep.mjs` — `refs <name...>` zero-reference gate and `orphans` advisory reachability listing over `mazeworld.html`. (this plan)
- `package.json` script `boot:check`. (this plan)
- `test/unit/parley.test.js` new describe name: `"LANG-02 / D-11 + IDENT-05/06 (Phase 44 DEAD-03): availability matrix — 6 races × 7 subs × Helm × 6 types (504 cases) match the rule oracle"`. (this plan)
- `window.__mzTables` — module-assigned, classic-read bridge object handing `content/` tables (`RACE_NOTE`, `CLASS_NOTE`, `SUB_NOTE`, `ROMAN`, `THRESHOLDS`, `WEAPONS`, `FIGHTER_SKILLS`, `THIEF_SKILLS`, `RACES`) to the surviving classic renderers. (Plan 44-03)
- `test/unit/shell-no-content-copies.test.js` — DEAD-02 source-pin test. (Plan 44-03)
- The two "Delve resumed." Oracle lines re-homed in the module boot path, gated on `hadSaveAtLaunch`. (Plan 44-04)
- Deleted symbols are NOT listed here — every SUMMARY lists the symbols its plan deleted.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build the two phase gates — headless boot check and deleted-symbol sweep — and prove the boot gate fails first</name>
  <files>tools/shell-boot-check.mjs, tools/shell-sweep.mjs, package.json</files>
  <read_first>
    - tools/store-screenshots/serve.js (the existing 10-line static server for www/ on :8765 — the boot check embeds its own equivalent so it never depends on that CommonJS file)
    - tools/store-screenshots/capture.js L50–70 (how the repo already drives the locally installed Chrome; this tool uses the Chrome binary directly, no playwright)
    - tools/build-www.mjs L1–40 (what `npm run build:www` produces; the boot check serves `www/`)
    - package.json scripts block (L7–16)
    - mazeworld.html L5349–5352 (`renderGravesLoading` writes `#yard`), L3293–3296 (`paint()` writes `#s-level` as `"Lvl " + ROMAN[...]`), L1693 and L7184 (script-tag lines the sweep uses as region boundaries)
    - .planning/phases/44-retire-the-classic-engine-from-the-shell/44-CONTEXT.md — "Measured reachability" and "Known false edges" (the sweep's `orphans` mode must implement exactly those exclusions)
  </read_first>
  <action>
    Create `tools/shell-boot-check.mjs` (ESM, node:http + node:fs + node:child_process only, no packages). Behaviour: (1) start a static server for `./www` on 127.0.0.1:8765 (content-types for .html .js .mjs .css .png .json .svg .woff2 .ico; 404 otherwise) unless `--url <url>` is given; (2) resolve the browser binary from `--chrome <path>`, else env `CHROME`, else the first existing of `C:/Program Files/Google/Chrome/Application/chrome.exe` and `C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`, else exit 2 with a message naming the flag; (3) spawn it with `--headless=new --disable-gpu --no-sandbox --user-data-dir=<out>/profile --virtual-time-budget=<budget, default 8000> --enable-logging=stderr --v=0 --dump-dom <url>` (out dir default `os.tmpdir()/shell-boot-check`, `--out` overrides), writing stdout to `<out>/boot-dom.html` and stderr to `<out>/boot-console.log`; (4) evaluate four checks and print one `PASS`/`FAIL` line each: `no-uncaught` = no stderr line contains the substring `"Uncaught ` (headless Chrome prints page console output as `CONSOLE(...)] "Uncaught TypeError: ..."` lines — verified on this machine); `painted` = the DOM contains `id="s-level">Lvl ` (proves `paint()` ran on the engine state); `graves` = the DOM's `#yard` markup contains `mw-empty` or `mw-grave` and does NOT contain `mw-error` (proves the classic boot's loadGraves/renderGraves ran); `title` = the DOM contains `id="mw-title"`; (5) exit 0 iff all four pass, else 1, always printing the two artifact paths. Add `--self-test`: serve a scratch HTML (written to `<out>/selftest.html`) containing a script that throws an Error, run the same pipeline against it and exit 0 ONLY IF `no-uncaught` FAILED on it (fail-first proof that the console capture has teeth), else exit 1. Kill the server and the browser on exit (also on SIGINT).

    Create `tools/shell-sweep.mjs` (ESM, node:fs only) with two subcommands over `mazeworld.html` (path overridable with `--file`): `refs NAME [NAME...]` (also `--list <file>` with one name per line) prints, for each NAME, every line number + line where NAME occurs as a whole identifier outside comments and string literals, where `window.NAME` / `globalThis.NAME` COUNT as references, template-literal `${...}` interpolations are KEPT while the rest of the template text is dropped, and `//`/`/* */` comments plus `'…'`/`"…"` strings are dropped. Scanned regions are exactly: the classic script (between the column-0 `<script>` line and its `</script>`), the module script (between the column-0 `<script type="module">` line and its `</script>`), and the values of `on*="..."` attributes in the HTML outside those scripts — HTML prose, markup text and CSS are never scanned (the head's title text and flavor prose contain words like `die`/`move`/`save`). Prints `refs NAME: <n>` per name and exits 1 if any n > 0; an optional `--ignore-window` flag makes `window.NAME`/`globalThis.NAME` NOT count (used once, for the `move` check in Plan 44-02 where `window.move(...)` bridge calls are the expected survivors). `orphans` prints the classic top-level declarations (column-0 `function`/`async function`/`const`/`let`/`var` between the `<script>` line and the `<script type="module">` line) that are unreachable from the roots {HTML `on*=` attribute values, the module script, classic top-level statements} through word-boundary references inside declaration bodies, with object-literal KEYS (`name:` at the start of a property) excluded, comment/string text excluded as above, the bare calls `move(`/`newGame(`/`makeCamp(` treated as cut points (they resolve to module overrides / a deleted fallback), and names the module script itself imports or declares (e.g. `RACES`, `CLASSES`, `reveal`, `ctx`) NOT counted as module roots for a same-named classic declaration. `orphans` output is ADVISORY (prints that caveat in its header); `refs` is the gate.

    Add `"boot:check": "node tools/shell-boot-check.mjs"` to package.json scripts (keep every existing script byte-identical).

    Run the gates on the untouched shell to record the baseline: `npm run build:www`, `npm run boot:check` (all four PASS), `node tools/shell-boot-check.mjs --self-test` (exit 0 = the throw was caught), `node tools/shell-sweep.mjs refs castSpell parley` (must report non-zero refs and exit 1 — the names still exist), `node tools/shell-sweep.mjs orphans` (save its output to the SUMMARY as the seed list; expected ~125–130 names), `wc -l < mazeworld.html` (8710), `npm test` (`# fail 0`). Commit: `chore(44-01): shell boot-check + sweep gates (fail-first proven)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && npm run build:www && npm run boot:check && node tools/shell-boot-check.mjs --self-test && (node tools/shell-sweep.mjs refs castSpell parley; test $? -eq 1) && grep -c '"boot:check": "node tools/shell-boot-check.mjs"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `npm run boot:check` prints four `PASS` lines (`no-uncaught`, `painted`, `graves`, `title`) and exits 0 on the untouched shell.
    - `node tools/shell-boot-check.mjs --self-test` exits 0 and prints that the injected uncaught error WAS detected (fail-first proof).
    - `node tools/shell-sweep.mjs refs castSpell parley` exits 1 and prints `refs castSpell: <n>` with n ≥ 1 (the declaration line counts).
    - `node tools/shell-sweep.mjs orphans` prints a header line containing the word `ADVISORY` and lists at least 100 names, including `castSpell`, `genFloor`, `newDay`, `BESTIARY`, `SUB_NOTE`, and NOT including `paint`, `draw`, `renderEncounter`, `tapStep`, `loadGraves`, `renderGraves`, `wireDeathConfirm`, `eff`, `upkeep`, `R_`.
    - `grep -c '"boot:check": "node tools/shell-boot-check.mjs"' package.json` prints `1`; `git diff --stat -- package.json` shows exactly one insertion and zero deletions.
    - `wc -l < mazeworld.html` prints `8710` (this task changes no shell bytes; `git diff --quiet -- mazeworld.html` exits 0).
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`.
  </acceptance_criteria>
  <done>Both tools exist, the boot gate is proven fail-first, the sweep gate reports live references, `boot:check` is an npm script, and the baseline numbers (8710 lines, `# fail 0`, orphan seed list) are recorded for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the two `new Function` tripwires — fold the parley matrix into the engine oracle test, delete the spell-menu mirror (DEAD-03)</name>
  <files>test/unit/parley.test.js, test/unit/parley-button-mirror.test.js, test/unit/spell-menu-mirror.test.js</files>
  <read_first>
    - test/unit/parley-button-mirror.test.js (whole file, 206 lines — the case set + loop order at L139–154: `for race of Object.keys(RACES)` → `for sub of ["Con Artist","Woodsman","Bard","Soldier","Ninja","Master of Arms","Court Mage"]` → `for helm of [false,true]` → `for t of ENC_TYPES`, `assert.equal(cases, 504)`)
    - test/unit/parley.test.js L1–60 (imports, `mk()` fixture builder), L150–210 (`expectedCanParley` prose oracle + the 288-case matrix test), L212–230 (the planted-Language no-op test)
    - test/unit/spell-menu-mirror.test.js (whole file, 128 lines)
    - test/unit/spell-level-overrides.test.js L77–125 (the engine `canCast` diff walk over every (sub, spell, level 1–5) cell + the empty-grimoire walk — the coverage that makes the spell mirror redundant)
    - test/unit/identity-combat.test.js L313–338 (engine canParley: Ninja/Master of Arms refused for every type; Court Mage vs Humans/Beasts/Walking Dead)
    - engine/combat.js L1040–1066 (`canParley` rule order — READ ONLY, never edited: parleyTried → Ninja/Master of Arms → Walking Dead → Magical needs fluency 2 → Con Artist → Woodsman → Bard → Court Mage+Humans → talkative → Wilmsry → Elven+Humans)
  </read_first>
  <action>
    In `test/unit/parley.test.js`, re-point the mirror's 504-case replay at the engine per DEAD-03: extend `expectedCanParley(c, t, tried)` — the INDEPENDENT prose oracle that must never call the engine — with the two Phase 24 identity rules in the position the locked decisions give them: immediately after the `tried` check and BEFORE the Walking Dead check, `if (c.sub === "Ninja" || c.sub === "Master of Arms") return false;` (IDENT-05: a Ninja never speaks, a Master of Arms attacks without question), and after the Bard rule `if (c.sub === "Court Mage" && t === "Humans") return true;` (IDENT-06: courtly manners). Then widen the matrix test's sub list from the 4 subs to the mirror's 7 in the mirror's order (`"Con Artist", "Woodsman", "Bard", "Soldier", "Ninja", "Master of Arms", "Court Mage"`), keep the loop nesting race → sub → helm → type exactly as it is, change `assert.equal(cases, 288)` to `assert.equal(cases, 504)`, update the three rollup assertions' comments if needed (`magicalTrue`/`walkingDeadTrue`/`plainHumanSoldierTrue` stay 0), and rename the test to `"LANG-02 / D-11 + IDENT-05/06 (Phase 44 DEAD-03): availability matrix — 6 races × 7 subs × Helm × 6 types (504 cases) match the rule oracle"`. Update the doc comment above the oracle to state the two added rules and that this matrix absorbs the retired `parley-button-mirror.test.js` replay (Phase 44, DEAD-03). Do not touch any other test in the file.

    Delete `test/unit/parley-button-mirror.test.js` (reason for the SUMMARY: its 504-case classic-vs-engine replay now runs engine-vs-prose-oracle in `parley.test.js`; its Ninja/Master-of-Arms and Court-Mage named cases are already engine-pinned by `identity-combat.test.js`; its "hides after the one attempt / no combat" case is `parley.test.js` D-12; its D-17 / Phase 24 source pins asserted lines of the classic `canParley()` that Task 3 deletes).

    Delete `test/unit/spell-menu-mirror.test.js` (reason for the SUMMARY: the engine `canCast` it guarded is pinned cell-for-cell by `spell-level-overrides.test.js`'s diff walk over every (sub, spell, level 1–5) with-grimoire cell plus its empty-grimoire walk, and the live in-combat SPELLS filter is `src/browser/combatMenu.js` (pinned by `combatMenu.test.js`), not the classic `canCast(sp)` wrapper Task 3 deletes).

    Run `npm test` (must be `# fail 0`; expected count: 3,257 − 6 parley-mirror cases − 8 spell-menu cases = 3,243, the matrix test itself is one case before and after — record the exact number). Commit: `test(44-01): retire new-Function tripwires — parley matrix folded into parley.test.js (504 cases), spell-menu mirror deleted (DEAD-03)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test ! -f test/unit/parley-button-mirror.test.js && test ! -f test/unit/spell-menu-mirror.test.js && grep -c "assert.equal(cases, 504)" test/unit/parley.test.js && node --test test/unit/parley.test.js test/unit/identity-combat.test.js test/unit/spell-level-overrides.test.js 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rl "new Function" test/` prints nothing.
    - `grep -c "assert.equal(cases, 504)" test/unit/parley.test.js` prints `1` and `grep -c "assert.equal(cases, 288)" test/unit/parley.test.js` prints `0`.
    - `grep -c '"Con Artist", "Woodsman", "Bard", "Soldier", "Ninja", "Master of Arms", "Court Mage"' test/unit/parley.test.js` prints `1` (the mirror's sub list, in the mirror's order).
    - `grep -cE 'c\.sub === "Ninja" \|\| c\.sub === "Master of Arms"' test/unit/parley.test.js` prints `1` and `grep -cE 'c\.sub === "Court Mage" && t === "Humans"' test/unit/parley.test.js` prints `1` — both inside `expectedCanParley`, which still contains no call to `canParley(` (`sed -n '/^function expectedCanParley/,/^}/p' test/unit/parley.test.js | grep -c "canParley("` prints `0`).
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `git diff --quiet -- mazeworld.html engine/ content/` exits 0 (this task edits only tests).
  </acceptance_criteria>
  <done>Zero `new Function` sites in `test/`; the 504-case matrix runs against the engine with an independent oracle; both mirror files are gone with their reasons recorded for the SUMMARY; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Deletion layer 1 — the 16 mirrors and the combat-cluster helpers only they reach, with the broken shell pins re-pointed in the same commit (DEAD-01)</name>
  <files>mazeworld.html, test/unit/shell-fight-gate.test.js, test/unit/shell-gear-39.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-worn-slots.test.js, test/unit/shell-armor-display.test.js</files>
  <read_first>
    - mazeworld.html L2190–2199 (the classic `canCast(sp)` wrapper + its comment), L2356–2430 (`rollCharacter` + doc comment), L2483–2567 (`genFloor` + doc comment), L3770–3795 (`makeCamp` + its comment), L3855–3866 (`descend`), L3947–4015 (`takeItem`, `itemReady`, `useItem`), L4255–4307 (`openStore` + comment), L4470–4479 (`meetJoiner`), L4518–4620 (the cluster comment block + `startCombat` + `rollInitiative`/`liveFoes`), L4621–4782 (`playerStrike`, `killFoe`, `checkLevel`), L4783–5044 (`castSpell`, `drinkPotion`, `flee`, `TALKATIVE`, `fluency`, `canParley`), L5045–5166 (`parley`, `SONGS`, `songReady`, `sing`, `canRead` — KEEP `canRead`, `readScroll`, `endCombat`, `afterPlayerAction`, `allyTurn`), L5167–5233 (`foeTurn`), L7368–7378 (the module's `window.__mzCanCast = canCast;` bridge + its comment), L7281–7283 (the module's `engine/derived.js` import line that names `canCast`)
    - mazeworld.html L5924–5940 (`COMBAT_DISPATCH` — its keys `castSpell:`, `parley:`, `flee:`, `sing:`, `useItem:`, `readScroll:`, `drinkPotion:` are object KEYS routed to `window.mz*` bridges, NOT references to the classic functions; the object stays)
    - test/unit/shell-fight-gate.test.js L125–135 (the `__mzCanCast` bridge pin), test/unit/shell-gear-39.test.js L61, test/unit/shell-loot-screen.test.js L298, test/unit/shell-worn-slots.test.js L186 (the three byte-exact pins on the derived.js import line), test/unit/shell-armor-display.test.js L84–92 (`renderCarriedListRegion` end anchor `function openStore()`) and L133–140 (`renderDropShelfRegion`, self-adjusting)
    - src/browser/combatMenu.js L15–21 (the LIVE in-combat menu imports engine `canCast`/`canParley` directly — proof the classic wrappers are dead)
  </read_first>
  <action>
    Delete from the classic script, each with the doc-comment block attached directly above it (a comment that only describes the deleted function goes with it; a comment describing surviving code stays): the 16 mirrors `rollCharacter`, `genFloor`, `makeCamp`, `descend`, `takeItem`, `useItem`, `openStore`, `meetJoiner`, `startCombat`, `playerStrike`, `killFoe`, `castSpell`, `drinkPotion`, `parley`, `readScroll`, `foeTurn`; and the combat-cluster helpers only they reach: `itemReady`, `rollInitiative`, `liveFoes`, `checkLevel`, `flee`, `TALKATIVE`, `fluency`, `canParley`, `SONGS`, `songReady`, `sing`, `endCombat`, `afterPlayerAction`, `allyTurn`, the classic `canCast(sp)` wrapper, and the L4520–4541 cluster comment ("Left in place rather than deleted … canParley()/songReady()/canRead()/canCast() … are NOT dead") which describes only deleted code. Leave `canRead()` (live: `paint()` reads it for the Scrolls row) and every table/helper the layer-2/3 plans own (`die`, `bury`, `EPITAPHS`, `SPELLS`, `MU_CHART`, `schoolBonus`, `rollGrimoire`, `NAMES`, `nameFor`, `bfs`, `shuffle`, `levelFromSP`, `giveItem`, `gainWilmst`, `rollTreasureItem`, `priceFor`, `WEAPON_MAX`, `ENC_TYPES`, `BESTIARY`, …) in place this commit even though they are now orphaned — one layer per commit.

    In the module script delete the `window.__mzCanCast = canCast;` assignment together with its Phase 31 comment block (its only consumer was the classic wrapper), and remove `canCast` from the `engine/derived.js` import line so no unused binding remains (the line becomes `import { conditionsOf, eff, slotFor, WORN_SLOTS, WORN_KEYS_OF, hasTool, toHit, strikeDie, mapViewRadius, inViewWindow } from "./engine/derived.js";`); the module uses `canCast` nowhere else (verify with the sweep before editing).

    Re-point the pins this layer breaks, in the same commit: (a) `shell-fight-gate.test.js` — delete the test case "CMB-02: the spell gate bridge — window.__mzCanCast = canCast, and the classic canCast(sp) delegates to it" (reason: bridge and wrapper deleted with the classic engine; the engine gate is pinned by spell-level-overrides + combatMenu tests); (b) `shell-gear-39.test.js` L61, `shell-loot-screen.test.js` L298, `shell-worn-slots.test.js` L186 — drop `canCast, ` from the three byte-exact import-line regexes so they pin the new line; (c) `shell-armor-display.test.js` `renderCarriedListRegion` — change the end anchor from `function openStore()` to `function canRead()` (the first surviving top-level declaration after the deleted store block; the region's single positive assertion is unaffected).

    Then run, in order: `node tools/shell-sweep.mjs refs rollCharacter genFloor makeCamp descend takeItem useItem openStore meetJoiner startCombat playerStrike killFoe castSpell drinkPotion parley readScroll foeTurn itemReady rollInitiative liveFoes checkLevel flee TALKATIVE fluency canParley SONGS songReady sing endCombat afterPlayerAction allyTurn canCast __mzCanCast` — every name must report 0 (note: `canCast` as a bare identifier must be 0 in the whole file; `window.__mzCanCast` must be 0); `npm run build:www`; `npm run boot:check`; `npm test`. If a name reports a live reference the probe missed, keep that declaration, restore its comment, and record it in the SUMMARY (assumption A-1). Record `wc -l < mazeworld.html` before/after. Commit: `refactor(44-01): delete the 16 classic mirrors + combat cluster from the shell (layer 1, DEAD-01)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/shell-sweep.mjs refs rollCharacter genFloor makeCamp descend takeItem useItem openStore meetJoiner startCombat playerStrike killFoe castSpell drinkPotion parley readScroll foeTurn itemReady rollInitiative liveFoes checkLevel flee TALKATIVE fluency canParley SONGS songReady sing endCombat afterPlayerAction allyTurn canCast __mzCanCast && npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*function (castSpell|parley|startCombat|meetJoiner|genFloor|rollCharacter|descend|makeCamp|takeItem|useItem|playerStrike|foeTurn|killFoe|openStore|readScroll|drinkPotion)\s*\(" mazeworld.html` prints `0` (ROADMAP success criterion 1's grep; 16 before this task).
    - `node tools/shell-sweep.mjs refs <the 32 names in the action>` exits 0 and prints `refs NAME: 0` for every name.
    - `grep -c "window.__mzCanCast" mazeworld.html` prints `0`; `grep -c 'import { conditionsOf, eff, slotFor, WORN_SLOTS, WORN_KEYS_OF, hasTool, toHit, strikeDie, mapViewRadius, inViewWindow } from "./engine/derived.js";' mazeworld.html` prints `1`.
    - `grep -c "function canRead()" mazeworld.html` prints `1` (kept) and `grep -c 'CODE.indexOf("function canRead()")' test/unit/shell-armor-display.test.js` prints `1`.
    - `grep -c "__mzCanCast" test/unit/shell-fight-gate.test.js` prints `0`; `grep -c "conditionsOf, eff, slotFor" test/unit/shell-gear-39.test.js test/unit/shell-loot-screen.test.js test/unit/shell-worn-slots.test.js` prints `1` for each file.
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines and exits 0; `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `node --test test/unit/shell-map-invariants.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `wc -l < mazeworld.html` prints a number ≤ 7800 (layer 1 removes ≥ 900 lines; record the exact value).
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing.
  </acceptance_criteria>
  <done>The 16 mirrors and their combat cluster are gone with zero surviving references, the dead `__mzCanCast` bridge is gone, the five shell pins are re-pointed, and build + headless boot + full suite are green at the commit boundary.</done>
</task>

</tasks>

<verification>
- `grep -rl "new Function" test/` prints nothing (success criterion 4, first half).
- The success-criterion-1 mirror grep prints `0`.
- `npm run build:www` exit 0; `npm run boot:check` all PASS; `npm test` `# fail 0`; `shell-map-invariants.test.js` green.
- Engine gate: `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing.
- Three commits, suite green at each: gates → tripwire re-point → layer 1.
</verification>

<success_criteria>
- Both gate tools exist and are proven (boot gate fail-first; sweep reports live refs before deletion, 0 after).
- Zero `new Function` sites in `test/`; the parley matrix runs 504 engine-vs-oracle cases in the mirror's order; the spell-menu mirror is deleted with its reason recorded.
- The 16 mirror declarations and 16 combat-cluster helpers are deleted; every layer-1 name reports `refs 0`.
- `mazeworld.html` line count recorded before/after (expected ≤ 7,800 after layer 1).
</success_criteria>

<output>
Create `.planning/phases/44-retire-the-classic-engine-from-the-shell/44-01-SUMMARY.md` with these REQUIRED sections (the orchestrator authors VERIFICATION.md from the SUMMARYs — verification agents are off):
1. `## Deleted symbols (layer 1)` — every classic declaration deleted in Task 3 with its `ba45dfd` line range, plus the module-side `window.__mzCanCast` bridge and the import trim; and a `### Kept, with the live reference` list for any name the sweep proved live (may be empty — say so).
2. `## Tests re-pointed or deleted` — one line per file with the reason: `parley-button-mirror.test.js` (deleted — folded into parley.test.js 504 cases), `spell-menu-mirror.test.js` (deleted — engine canCast pinned by spell-level-overrides + combatMenu), `parley.test.js` (re-pointed — 7 subs / 504 cases / IDENT-05/06 oracle rules), `shell-fight-gate.test.js` (case deleted — bridge gone), `shell-gear-39` / `shell-loot-screen` / `shell-worn-slots` (import-line regex re-pinned), `shell-armor-display.test.js` (region anchor → `function canRead()`).
3. `## Gate outputs` — the exact commands and results: `npm run boot:check` (4 PASS), `--self-test` (exit 0), `shell-sweep refs` (all 0), `npm run build:www`, `npm test` (`# fail 0`, test count before/after), `wc -l < mazeworld.html` before/after, the engine-gate `git diff --stat` (empty), and the `orphans` seed list captured in Task 1.
4. `## Human verification (deferred to end of run)` — Pixel 7 items batched to milestone close: a fight reached through the SPELLS / PARLEY / SING / USE / DRINK / READ combat buttons still resolves through the engine (each button dispatches); no button dead-ends.
5. `## Flagged assumptions` — restate A-1 and A-2 with their status after this plan.
</output>
