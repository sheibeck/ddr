---
phase: 37-equipment-slot-model-eff-refactor
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - engine/items.js
  - engine/actions.js
  - engine/engine.js
  - src/browser/toasts.js
  - src/browser/eventNarration.js
  - test/voice/safety-scan.test.js
  - test/unit/worn-slots.test.js
  - docs/USABLE-FEATURES-AUDIT.md
autonomous: true
requirements: [GEAR-03]
tags: [engine, worn-slots, equip-swap, auto-wear, use-item-slot-form, refusals, feed-02, deferred-uat]
notes:
  rng_draw_impact: "zero — equipItem/unequipSlot/takeFind/takeLoot/takeAllLoot/takeItem are pure (no rng) today and stay pure; useItem's new slot resolution and the notWorn refusal both fire BEFORE it.usedAt/itemUsed and before any draw (the Phase 31 refusal-ladder rule); every new branch is gated on c.worn being present, which no fixture/bot/newRun-without-option state has"
  serialized_field_impact: "c.worn only (moved into/out of by equip/unequip/auto-wear; already carved out in Plan 01); usedAt/every ride on the item object and move with it; no new field on any item; no new event TYPE — one new reason string (useRefused.notWorn) and one additive payload field (itemEquipped.replaced)"
  canon_change: "the ONE deliberate GEAR-03 change (stacking -> one per slot; equipping into an occupied slot is a swap) applies only when c.worn is present; legacy states keep today's equipRejected notEquippable for cloaks/jewelry/staves and today's bag-use behaviour, so zero fixtures move"

must_haves:
  truths:
    - "With c.worn present, equipItem on a bag Ring of Power moves it to c.worn.ring, removes it from c.items, and pushes itemEquipped { item, slot: 'ring' }; equipping a second ring swaps: the new ring is worn, the old one occupies the exact freed bag index, and the event carries replaced: <old item> (test/unit/worn-slots.test.js)"
    - "With c.worn present, a non-Magic-User equipping a staff gets equipRejected { reason: 'wrongClass' } and nothing moves; a potion/picks/bag item still gets notEquippable; WITHOUT c.worn a cloak/jewel/staff still gets notEquippable exactly as today"
    - "unequipSlot(state, 'ring') stows the worn ring back into the bag via stowItem and pushes itemUnequipped { item, slot: 'ring' }; on a full bag it pushes bagFull and leaves the ring worn; an empty slot is a silent no-op; slot values outside WORN_SLOTS/weapon/armor are rejected by validateAction"
    - "With c.worn present, takeFind/takeLoot/takeAllLoot/takeItem auto-wear a slot item whose slot is empty (domain take event first, then itemEquipped) without consuming a bag slot; when the slot is occupied the item goes to the bag exactly as today; WITHOUT c.worn every take path is byte-identical to today"
    - "useItem accepts { slot } (engine action { type: 'useItem', slot: 'cloak' }) and runs the worn item's effect with usedAt stamped on the worn object; with c.worn present a use: cloak/jewel/staff still in the BAG is refused with useRefused { reason: 'notWorn' } before any side effect; legacy states keep bag-use"
    - "TOAST_FOR.useRefused and EVENT_NARRATION.useRefused render the notWorn line; itemEquipped renders the replaced branch; test/voice/safety-scan.test.js BRANCH_TOGGLES exercise both; both coverage guards and the safety scan pass"
    - "npm test prints '# fail 0'; fixtures untouched; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - path: "engine/items.js"
      provides: "equipItem/unequipSlot slot branches, autoWearSlot/wearItem, auto-wear at the four take sites, useItem slot addressing + notWorn refusal"
    - path: "engine/actions.js"
      provides: "useItem { i } | { slot } and unequipSlot.slot over EQUIP_SLOTS ∪ WORN_SLOTS validation"
    - path: "engine/engine.js"
      provides: "useItem dispatch of the slot form"
    - path: "src/browser/toasts.js"
      provides: "useRefused notWorn text; itemEquipped replaced text"
    - path: "src/browser/eventNarration.js"
      provides: "useRefused notWorn Oracle line; itemEquipped replaced Oracle line"
    - path: "test/unit/worn-slots.test.js"
      provides: "every behaviour above, plus legacy-path identity proofs"
  key_links:
    - from: "engine/items.js#equipItem slot branch"
      to: "c.worn[slotFor(it)]"
      via: "const worn = c.worn[slot] || null; c.worn[slot] = it; if (worn) c.items[i] = worn; else c.items.splice(i, 1);"
      pattern: "c\\.worn\\[slot\\] = it"
    - from: "engine/engine.js#applyAction useItem"
      to: "engine/items.js#useItem"
      via: "useItem(next, action.slot !== undefined ? { slot: action.slot } : action.i, rng, events)"
      pattern: "action\\.slot !== undefined"
    - from: "engine/actions.js#validateAction"
      to: "engine/derived.js#WORN_SLOTS"
      via: "import { WORN_SLOTS } from \"./derived.js\"; unequipSlot accepts EQUIP_SLOTS or WORN_SLOTS; useItem accepts slot in WORN_SLOTS"
      pattern: "WORN_SLOTS"
  prohibitions:
    - "No new event TYPE (the coverage guards must not gain an entry); no edit to engine/state.js, engine/saveState.js, engine/character.js, src/browser/engineAdapter.js, src/browser/rail.js, src/browser/combatMenu.js or mazeworld.html (Plans 03/04)"
    - "No change to takeLoot's equip:true form for non-weapon/armor items (it keeps equipRejected notEquippable; the loot card never offers Equip now for slot items — Phase 43 territory)"
    - "No edit to any fixture or the prototype master; no fixture regeneration; no package/font change; no checkpoint:human-verify, device step or APK build"
    - "Commit with explicit paths only — never stage store-listing/ or tools/store-screenshots/"
---

<objective>
Give the worn model its behaviours, every one gated on `c.worn` being present so no fixture, bot or un-migrated save changes by a byte: `equipItem` grows a branch for `it.kind === "cloak" | "jewel" | "staff"` that performs a DIRECT SWAP into the freed bag index (mirroring the weapon branch), pushing `itemEquipped { item, slot, replaced? }` (additive payload, Phase 25 precedent); `unequipSlot` extends to the six new slots via the same `stowItem` gate (`bagFull` on a full bag); taking a slot item (encounter-dot find, victory loot, store/faerie `takeItem`) auto-wears it when the slot is empty and bags it otherwise; `useItem` gains slot addressing (`{ type: "useItem", slot: "cloak" }` alongside the bag-index form) and refuses a bagged activatable with the named reason `notWorn` on the existing `useRefused` event; `engine/actions.js` validates the two slot forms; toasts + Oracle narration carry the new copy; the voice scan exercises the new branches; the refusal vocabulary ledger gains its row.

Per CONTEXT (locked): "Activatables must be worn to work in the new model"; "Swap on an occupied slot — direct swap, no pending-swap state, no new serialized field; the explicit choice is the shell's two-tap confirm (Plan 04)"; "unequipSlot extends to the six new slots via the same stowItem gate"; "Taking a slot item: auto-wear when the slot is empty, narrated through the existing itemEquipped event; only in the new model"; "Refusals reuse the existing vocabulary: equipRejected notEquippable / the staff class reason; UNEQUIP on a full bag -> bagFull; bag-use of an unworn activatable -> a named refusal (new reason string on the existing refusal event if one fits)". Discretion resolved: `itemEquipped` gains an additive `replaced` field (no second event); the staff class reason is the existing `wrongClass` (already in equipRejectText's fallback); the not-worn refusal reason string is `notWorn` on `useRefused`; `useItem` takes `i` as a non-negative integer OR `{ slot }` and engine.js maps the action's `slot` field into that object form.

Purpose: ROADMAP SC-1 (a second ring/cloak/staff/bracelet prompts a swap instead of stacking — the engine half: swap semantics + refusals; the confirm UI is Plan 04) and SC-2 (bonuses reflect only the worn set — already true from Plan 01's `eff`; this plan makes the worn set actually change through play).
Output: the items.js/actions.js/engine.js changes, the toast/narration copy, `test/unit/worn-slots.test.js`, the `notWorn` row in docs/USABLE-FEATURES-AUDIT.md §1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-CONTEXT.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-01-SUMMARY.md
@engine/items.js
@engine/actions.js
@engine/engine.js
@engine/derived.js
@src/browser/toasts.js
@src/browser/eventNarration.js
@docs/USABLE-FEATURES-AUDIT.md
</context>

<artifacts_this_phase_produces>
See 37-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list. This plan creates the **[02]** items: `engine/items.js` — `autoWearSlot(state, it)` (pure predicate -> slot key or null), `wearItem(state, it, slot, events)` (mutator; pushes `itemEquipped { item, slot }`), the slot branch in `equipItem` (`itemEquipped { item, slot, replaced? }`, `equipRejected { reason: "wrongClass" }` for a non-caster's staff), the six-slot branch in `unequipSlot`, auto-wear calls in `takeFind`/`takeLoot`/`takeAllLoot`/`takeItem`, `useItem(state, ref, rng, events, now)` with `ref` = index | `{ slot }` and the `useRefused { reason: "notWorn" }` refusal; `engine/actions.js` — `useItem { slot }` and `unequipSlot.slot ∈ WORN_SLOTS` validation (imports `WORN_SLOTS` from derived.js); `engine/engine.js` — the slot dispatch form; `src/browser/toasts.js` — `useRefused` map entry `notWorn`, `itemEquipped` `replaced` suffix; `src/browser/eventNarration.js` — the same two branches; `test/voice/safety-scan.test.js` — `{ reason: "notWorn" }` and `{ replaced: { n: "Ring of Power" } }` toggles; `docs/USABLE-FEATURES-AUDIT.md` — §1 `notWorn` row; `test/unit/worn-slots.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: equipItem/unequipSlot slot branches + autoWearSlot/wearItem + auto-wear at the four take sites (tests first)</name>
  <files>engine/items.js, engine/actions.js, test/unit/worn-slots.test.js</files>
  <read_first>
    - engine/items.js lines 281-330 (`takeItem` — staff `wrongClass` gate at ~320, the `giveItem` tail), 332-400 (`bagCap`/`canStow`/`stowItem` — the ONE bag gate; `giveItem` applies `eff.wp`), 481-497 (`takeFind`), 509-518 (`dropItem`), 519-578 (`equipItem` — the weapon branch's `if (worn) c.items[i] = worn; else c.items.splice(i, 1);` swap is the template; the final `notEquippable` fallback at ~575), 580-634 (`unequipSlot` — `worn` resolution, the destroyed-armor branch, the `stowItem` gate), 660-720 (`takeLoot` — non-equip and equip:true forms), 735-760 (`takeAllLoot` — per-item scratch events, ONE bagFull)
    - engine/items.js lines 1-60 (imports — `eff, skill, slotItems, afraidDamage` from derived.js; add `slotFor, WORN_SLOTS` there)
    - engine/derived.js (Plan 01's `slotFor`, `WORN_SLOTS`, `carriedItems`, `reconcileWorn` JSDocs)
    - engine/actions.js lines 55-70 (`EQUIP_SLOTS`, `isInt`) and 121-140 (dropItem/equipItem/sellItem/unequipSlot validation)
    - test/unit/items.test.js lines 100-180 (state/rng helper style for items tests), test/unit/loot-pile.test.js (takeLoot/takeAllLoot test fixtures to mirror), test/unit/effects.test.js lines 1-60 (import style)
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-CONTEXT.md §"Worn Model" (Swap bullet) and §"Gear-Tab Surface & Take Flow" (Taking a slot item; Refusals)
  </read_first>
  <behavior>
    - helper: hero(state) fixture = a Fighter with bag "medium", c.worn = {} planted by hand (Plan 03 owns the newRun option), items as listed per test; RING = { kind:"jewel", n:"Ring of Power", eff:{dmg:1}, txt:"..." } built fresh per test (never aliasing content)
    - equipItem new model: items [RING] -> equipItem(state, 0) => c.worn.ring === that ring object, c.items deepStrictEqual [], events deepStrictEqual [{ type:"itemEquipped", item: ring, slot:"ring" }] (no replaced key when nothing was worn)
    - swap: worn { ring: ringA }, items [potion, ringB] -> equipItem(state, 1) => c.worn.ring === ringB, c.items deepStrictEqual [potion, ringA] (ringA at the freed index 1), events[0] deepStrictEqual { type:"itemEquipped", item: ringB, slot:"ring", replaced: ringA }; slotItems count unchanged (net zero)
    - a cloak and a jewel occupy different slots: worn { ring }, equip a Cloak of Speed -> both worn, no replaced
    - staff gate: Fighter with c.worn, items [Oak Staff] -> equipItem => events [{ type:"equipRejected", item, reason:"wrongClass" }], nothing moves; Magic User -> worn.staff set
    - non-slot items in the new model: potion/picks/bag -> equipRejected notEquippable (unchanged)
    - legacy identity: the SAME calls on a state WITHOUT a worn key -> equipRejected notEquippable for the ring/cloak/staff, c deepStrictEqual before (byte-identical), and JSON.stringify(state) unchanged
    - unequipSlot(state, "ring") with a free slot: c.worn has no ring key afterwards (delete, not null), c.items ends with the ring (via stowItem -> giveItem), events deepStrictEqual [{ type:"itemUnequipped", item: ring, slot:"ring" }]; with a FULL bag (fill c.items to bagCap with picks-like gear): events [{ type:"bagFull", ... }] only and the ring stays worn; unequipSlot(state, "helm") with nothing worn -> events [] and no change; unequipSlot on a legacy state (no worn) for "ring" -> events [] and no change
    - weapon/armor unequip paths are untouched: the existing items.test.js/armor-durability tests stay green (run them)
    - autoWearSlot(state, it): returns "ring" for a RING when c.worn exists and worn.ring is empty; null when worn.ring is occupied; null when c has no worn key; null for a potion; null for a staff on a Fighter; "staff" for a staff on a Magic User
    - takeFind auto-wear: pendingFind = RING, worn {} -> takeFind => pendingFind null, c.items deepStrictEqual [] (no bag slot consumed), c.worn.ring === ring, events deepStrictEqual [{ type:"findTaken", item: ring }, { type:"itemEquipped", item: ring, slot:"ring" }] (take first, wear second); with worn.ring occupied -> the ring goes to the bag and events deepStrictEqual [{ type:"findTaken", item: ring }] exactly as today; a FULL bag with an EMPTY slot still wears it (no bagFull — wearing needs no slot); legacy state (no worn) -> today's behaviour byte-for-byte
    - takeLoot(state, 0) (non-equip) mirrors takeFind: [lootTaken, itemEquipped] on an empty slot; bag + [lootTaken] on an occupied slot; takeLoot(state, 0, true) on a slot item -> equipRejected notEquippable (unchanged, deliberately)
    - takeAllLoot: pile [ringA, ringB, potion] with worn {} -> ringA worn, ringB bagged, potion bagged; events in pile order: [lootTaken ringA, itemEquipped ringA, lootTaken ringB, lootTaken potion]; pile empties; with a full bag and an empty ring slot: ringA is worn even though ringB is refused with ONE bagFull
    - takeItem: a Magic User with worn {} taking an Oak Staff -> worn.staff set, events [{ type:"itemGiven", item }, { type:"itemEquipped", item, slot:"staff" }]; a Fighter taking a staff -> itemRejected wrongClass (unchanged, fires first); a Fighter with worn {} taking a RING -> worn; legacy -> giveItem only
    - no eff.wp is ever applied on wear (maxWP/wp unchanged across every wear/swap/unequip in these tests — the Plan 01 tripwire guarantees no slot row carries eff.wp)
    - validateAction: { type:"unequipSlot", slot:"ring" } ok; slot:"helm" ok; slot:"weapon" ok (unchanged); slot:"hat" -> ok:false with a reason mentioning the six slot names or WORN_SLOTS; slot: 3 -> ok:false
  </behavior>
  <action>
    Write `test/unit/worn-slots.test.js` FIRST with every behavior above (RED on the missing branches), then implement in `engine/items.js`:

    1. Extend the derived.js import with `slotFor` and `WORN_SLOTS`.
    2. Add two helpers directly above `takeItem` (with JSDocs): `export function autoWearSlot(state, it)` — pure predicate: returns `null` unless `state.c` has an own `worn` key that is a plain object, `it` is an object, `slotFor(it)` is non-null, `c.worn[slot]` is empty, and (when `it.kind === "staff"`) `c.cls === "Magic User"`; else returns the slot key. `export function wearItem(state, it, slot, events)` — assigns `c.worn[slot] = it` (same object) and pushes `{ type: "itemEquipped", item: it, slot }`; document that it deliberately does NOT apply `eff.wp` (no slot row carries it; Plan 01 tripwire) and adds no rng draw.
    3. `takeItem`: after the existing staff `wrongClass` gate and before `giveItem`, compute `const slot = autoWearSlot(state, it)`; when non-null push `{ type: "itemGiven", item: it }` then call `wearItem` and return; otherwise fall through to `giveItem` unchanged.
    4. `takeFind`: before the `stowItem` call, `const slot = autoWearSlot(state, it)`; when non-null: `state.pendingFind = null`, push `findTaken`, `wearItem`, return. Otherwise unchanged.
    5. `takeLoot` non-equip form: same pattern before its `stowItem` (splice the pile entry, push `lootTaken`, then `wearItem`). Leave the `equip === true` form exactly as it is (its `notEquippable` rejection for non-weapon/armor stays — add a one-line comment naming Phase 43 as the owner of loot-card equip for slot items).
    6. `takeAllLoot`: inside the per-item loop, before `stowItem`, if `autoWearSlot` is non-null push `lootTaken` then `wearItem` and `continue` (never consumes a slot, never trips `refused`).
    7. `equipItem`: replace the final `notEquippable` fallback with a slot branch: when `it.kind` is `"cloak"`, `"jewel"` or `"staff"` AND `c.worn` is a plain object: if `it.kind === "staff" && c.cls !== "Magic User"` push `equipRejected { item, reason: "wrongClass" }` and return; `const slot = slotFor(it)`; if null push `notEquippable` and return; `const worn = c.worn[slot] || null; c.worn[slot] = it; if (worn) c.items[i] = worn; else c.items.splice(i, 1);` then push `itemEquipped { item: it, slot }` with an additive `replaced: worn` field ONLY when `worn` is non-null (build the event object conditionally so the no-swap event has no `replaced` key). When `c.worn` is absent (legacy), keep pushing `notEquippable` exactly as today. Update the branch comment ("staves, cloaks, jewelry — worn slots in the new model (Phase 37, GEAR-03); potions, picks, bags — never an equip slot").
    8. `unequipSlot`: resolve `worn` for a WORN_SLOTS key as `c.worn && c.worn[slot]` (falsy -> the existing `if (!worn) return events;` no-op); after the existing `stowItem` gate succeeds, for a WORN_SLOTS key `delete c.worn[slot]` and push `{ type: "itemUnequipped", item: worn, slot }`; the weapon/armor branches stay byte-identical (keep the destroyed-armor branch first, untouched).

    `engine/actions.js`: `import { WORN_SLOTS } from "./derived.js";` (derived.js is the cycle-free leaf) and make the `unequipSlot` case accept `EQUIP_SLOTS.has(slot) || WORN_SLOTS.includes(slot)` with the reason string `"unequipSlot.slot must be 'weapon', 'armor' or one of ring, bracelet, amulet, helm, cloak, staff"`.

    Run the new test file (GREEN), then `node --test test/unit/items.test.js test/unit/loot-pile.test.js test/unit/armor-durability.test.js test/unit/item-wiring.test.js` and `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/worn-slots.test.js && node --test test/unit/items.test.js test/unit/loot-pile.test.js test/unit/armor-durability.test.js && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function autoWearSlot\|export function wearItem" engine/items.js` prints 2
    - `grep -c "autoWearSlot(state, it)" engine/items.js` >= 5 (definition + takeItem + takeFind + takeLoot + takeAllLoot)
    - `grep -c "c.worn\[slot\] = it" engine/items.js` >= 2 (equipItem swap + wearItem)
    - `grep -c "delete c.worn\[slot\]" engine/items.js` prints 1
    - `grep -c "WORN_SLOTS" engine/actions.js` >= 2 (import + check)
    - `node -e "import('./engine/actions.js').then(m=>{const v=m.validateAction; console.log(JSON.stringify([v({type:'unequipSlot',slot:'ring'}).ok, v({type:'unequipSlot',slot:'hat'}).ok, v({type:'unequipSlot',slot:'weapon'}).ok]))})"` prints `[true,false,true]`
    - `node --test test/unit/worn-slots.test.js` passes with >= 24 tests; `npm test` ends `# fail 0`; `git status --porcelain test/parity/fixtures` empty
  </acceptance_criteria>
  <done>Slot equip/swap/unequip and auto-wear behave per the locked decisions with c.worn present and are byte-identical no-ops without it; worn-slots.test.js green; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: useItem slot addressing + notWorn refusal + engine.js dispatch + actions validation + toast/Oracle copy + safety-scan toggles (tests first)</name>
  <files>engine/items.js, engine/engine.js, engine/actions.js, src/browser/toasts.js, src/browser/eventNarration.js, test/voice/safety-scan.test.js, test/unit/worn-slots.test.js</files>
  <read_first>
    - engine/items.js lines 775-880 (`itemReady`, `TARGETED_KINDS`, `useItem`'s refusal ladder: refuseIfPending -> wrongClass -> pilfer -> combatOnly -> cooldown, then `it.usedAt = state.steps` + `itemUsed`) and lines 990-1014 (the `it.kind === "potion" || it.uses === 1` consume path `c.items.splice(i, 1)`)
    - engine/engine.js lines 95-100 (`case "useItem": useItem(next, action.i, rng, events);`)
    - engine/actions.js lines 99-105 (`useItem.i` validation) and the `WORN_SLOTS` import added in Task 1
    - src/browser/toasts.js lines 1350-1370 (`useRefused` reason map — add `notWorn`), 1380-1384 (`itemEquipped` builder — add the `replaced` suffix), and the `block()` helper it uses
    - src/browser/eventNarration.js lines 660-675 (`useRefused` if-ladder — add `notWorn`), 700-702 (`itemEquipped` — add the `replaced` suffix)
    - test/voice/safety-scan.test.js lines 118-152 (BASE_EVENT + BRANCH_TOGGLES — append the two new toggles beside the Phase 36 line) and lines 156-170 (renderEventVariants)
    - test/unit/item-combat-gate.test.js (the Phase 31 refusal-ladder tests — the "refused use never burns usedAt / never draws" proof style to mirror for notWorn)
    - test/unit/toastsCoverage.test.js + test/unit/formatEventsCoverage.test.js headers (why no new event TYPE is needed and how `type:` keys are scanned)
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-CONTEXT.md `<specifics>` (the not-worn refusal register line)
  </read_first>
  <behavior>
    - useItem slot form: Magic User, worn { staff: Poplar Staff (use:"heal") }, wp below max -> useItem(state, { slot:"staff" }, rng, events) => events include itemUsed { item: staff } and healed; staff.usedAt === state.steps (stamped on the WORN object); c.items untouched; a second immediate use -> useRefused { reason:"cooldown", left: N } (cooldown rides on the worn object)
    - useItem slot form on an empty slot ({ slot:"cloak" } with nothing worn) -> events [] and no change (mirrors the bag out-of-range no-op)
    - notWorn: Fighter with worn {} and items [Cloak of Speed (use:"haste", every:50)] -> useItem(state, 0, rng, events) => events deepStrictEqual [{ type:"useRefused", item: cloak, reason:"notWorn" }]; cloak.usedAt undefined; c.haste unchanged; rngState unchanged (count draws with a counting rng: 0)
    - notWorn ordering: a Fighter's bagged Oak Staff -> wrongClass (fires before notWorn); a Pilfer's bagged Cloak of Speed with worn {} -> notWorn (fires before pilfer); a bagged passive Ring of Power (no use) with worn {} -> notWorn (a slot item in the bag is never usable in the new model)
    - legacy identity: the same bagged Cloak of Speed on a state WITHOUT worn -> today's haste effect (itemUsed + the haste event), byte-identical to a pinned pre-change run
    - consume path: a worn item is never `uses: 1` today, but when a hand-planted worn potion-like `{ uses: 1, use:"heal", kind:"jewel", n:"Ring of Power" }` is used via { slot:"ring" } the slot is deleted (delete c.worn.ring) and itemConsumed is pushed — the bag splice path must not run
    - validateAction: { type:"useItem", slot:"cloak" } ok; { type:"useItem", slot:"hat" } ok:false; { type:"useItem", i:0 } ok (unchanged); { type:"useItem" } ok:false with the existing reason "useItem.i must be a non-negative integer"; { type:"useItem", i:0, slot:"cloak" } ok:false (exactly one addressing form)
    - applyAction({ type:"useItem", slot:"staff" }) through engine.js reaches the worn staff (assert via the returned events)
    - TOAST_FOR.useRefused({ type:"useRefused", item:{ n:"Cloak of Speed" }, reason:"notWorn" }).text === "Cloak of Speed is in your bag, doing what things in bags do: nothing. Wear it first." and tone "block"; EVENT_NARRATION.useRefused(same) contains "Wear it first"
    - TOAST_FOR.itemEquipped({ item:{ n:"Ring of Power" }, slot:"ring", replaced:{ n:"Ring of Power" } }).text === "Equipped: Ring of Power (ring). Ring of Power goes back in the bag."; without replaced the text is byte-identical to today's "Equipped: Ring of Power (ring)."; EVENT_NARRATION.itemEquipped with replaced contains "goes back in the bag" and without replaced is byte-identical to today's line
  </behavior>
  <action>
    Append the behaviors to `test/unit/worn-slots.test.js` (RED), then:

    `engine/items.js#useItem`: change the signature to `useItem(state, ref, rng, events = [], now = Date.now)` and resolve `const slot = ref && typeof ref === "object" ? ref.slot : null; const i = slot ? null : ref; const it = slot ? (c.worn && c.worn[slot]) : (c.items || [])[i];` keeping `if (!it) return events;`. Insert the notWorn refusal directly AFTER the existing staff `wrongClass` check and BEFORE the Pilfer check: when `slot === null` and `c.worn` is a plain object and `slotFor(it)` is non-null, push `{ type: "useRefused", item: it, reason: "notWorn" }` and return. Change the consume path to `if (slot) delete c.worn[slot]; else c.items.splice(i, 1);` inside the existing `it.kind === "potion" || it.uses === 1` guard. Update the JSDoc ladder list (pending -> wrongClass -> notWorn -> pilfer -> combatOnly -> cooldown) and document the `ref` forms.

    `engine/engine.js`: `case "useItem": useItem(next, action.slot !== undefined ? { slot: action.slot } : action.i, rng, events);` with a Phase 37 comment.

    `engine/actions.js#useItem` case: if `action.slot !== undefined` require `action.i === undefined` and `WORN_SLOTS.includes(action.slot)` (reason `"useItem.slot must be one of ring, bracelet, amulet, helm, cloak, staff (and excludes i)"`); else keep the existing `i` check and its exact reason string.

    `src/browser/toasts.js`: in `useRefused`'s map add `notWorn: \`${item} is in your bag, doing what things in bags do: nothing. Wear it first.\``; in `itemEquipped` append `${e?.replaced?.n ? \` ${e.replaced.n} goes back in the bag.\` : ""}` after the existing period so the no-replaced text is byte-identical. `src/browser/eventNarration.js`: add `if (e.reason === "notWorn") return \`<span class="miss">${item} is in your bag,</span> doing what things in bags do: nothing. Wear it first.\`;` to the `useRefused` ladder (before the Pilfer fallback), and append ` ${e.replaced.n} goes back in the bag — the maze is not a jeweller.` to `itemEquipped` only when `e.replaced?.n` is set (keep today's line byte-identical otherwise). `test/voice/safety-scan.test.js`: append `{ reason: "notWorn" }, { replaced: { n: "Ring of Power" } },` after the Phase 36 toggles with a `// Phase 37 (GEAR-03)` comment.

    Run `node --test test/unit/worn-slots.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/item-combat-gate.test.js`, then `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/worn-slots.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/item-combat-gate.test.js && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'reason: "notWorn"' engine/items.js` prints 1; `grep -c "notWorn" src/browser/toasts.js` >= 1; `grep -c "notWorn" src/browser/eventNarration.js` >= 1; `grep -c 'reason: "notWorn"' test/voice/safety-scan.test.js` prints 1
    - `grep -c "action.slot !== undefined" engine/engine.js` prints 1; `grep -c "action.slot !== undefined" engine/actions.js` >= 1
    - `grep -c "goes back in the bag" src/browser/toasts.js src/browser/eventNarration.js` shows 1 per file
    - `grep -c "export function useItem(state, ref" engine/items.js` prints 1
    - the coverage guards report no missing narration/toast entry (their tests pass) and the safety scan passes
    - `node --test test/unit/worn-slots.test.js` passes with >= 38 tests; `npm test` ends `# fail 0`
  </acceptance_criteria>
  <done>The slot use form works end to end through applyAction, bagged activatables are refused by name in the new model, legacy bag-use is untouched, all copy is scanned and family-friendly.</done>
</task>

<task type="auto">
  <name>Task 3: Refusal-vocabulary ledger row, legacy-path identity sweep, plan gate and the deferred-UAT SUMMARY section</name>
  <files>docs/USABLE-FEATURES-AUDIT.md, test/unit/worn-slots.test.js</files>
  <read_first>
    - docs/USABLE-FEATURES-AUDIT.md lines 17-42 (§1 refusal vocabulary table — add the row in the same column shape) and lines 113-165 (§4 items with `use` — read only, to keep the row wording consistent)
    - test/unit/worn-slots.test.js (as written in Tasks 1-2)
    - test/unit/items.test.js lines 161-176 (the existing staff/giveItem test — the legacy behaviour this sweep re-pins)
  </read_first>
  <action>
    1. `docs/USABLE-FEATURES-AUDIT.md` §1: add a table row `| notWorn | useRefused | (Phase 37, GEAR-03) a cloak/jewelry/staff activatable used from the BAG while the character is on the worn-slot model — activatables must be worn to work; legacy states (no c.worn) keep bag-use | useRefused |` directly after the `pilfer` row. Do not restructure anything else in the doc (Plan 04 owns the canon-change ledger).
    2. Append a `// --- Plan 02 Task 3: legacy identity sweep ---` section to `test/unit/worn-slots.test.js`: for each of the five entry points (equipItem on a cloak, unequipSlot "ring", takeFind of a ring, takeLoot of a ring, useItem of a bagged Cloak of Speed) run the call on TWO deep clones of a legacy state built from `newRun(3)` (Thief, starting cloak in the bag, no worn key): one through the current engine, one against a hand-pinned expected end state recorded in the test (events array + `JSON.stringify(state.c)`), asserting identity — this pins that Plan 02 changed nothing for a state without the model.
    3. Plan gate: `npm test` `# fail 0`; `git status --porcelain test/parity/fixtures` empty; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat -- package.json package-lock.json test/parity/fixtures` empty; `git diff --stat 6671921 -- engine content src mazeworld.html docs` lists exactly content/treasure-tables.js, engine/derived.js (Plan 01), engine/items.js, engine/actions.js, engine/engine.js, src/browser/toasts.js, src/browser/eventNarration.js, docs/USABLE-FEATURES-AUDIT.md.
    4. SUMMARY `## Human verification (deferred to end of run)`: state that no shell path can yet create `c.worn` (Plan 03/04), so nothing is player-visible in this plan and it adds NOTHING to the Pixel 7 checklist; list the engine behaviours Plan 04's checklist will exercise (swap, unequip on a full bag, auto-wear on a find, notWorn refusal) so the aggregator can reference them. Commit with explicit paths only.
  </action>
  <verify>
    <automated>grep -c "| notWorn |" docs/USABLE-FEATURES-AUDIT.md && node --test test/unit/worn-slots.test.js && npm test 2>&1 | grep -E "^# (pass|fail)" && git diff --stat 6671921 -- engine content src mazeworld.html docs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "| notWorn |" docs/USABLE-FEATURES-AUDIT.md` prints 1
    - `grep -c "legacy identity sweep" test/unit/worn-slots.test.js` prints 1 and the file has >= 43 tests passing
    - `npm test` ends `# fail 0`; fixtures untouched; master hash unchanged
    - `git diff --stat 6671921 -- engine content src mazeworld.html docs` names exactly the eight files listed in step 3 and nothing else
    - `git status --porcelain store-listing tools/store-screenshots` shows only `??` lines
    - the SUMMARY contains `## Human verification (deferred to end of run)`
  </acceptance_criteria>
  <done>Ledger row added, legacy identity pinned for every entry point, gate recorded, SUMMARY written, atomic commits with explicit paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action payload -> engine | `useItem.slot` / `unequipSlot.slot` arrive from the shell (or a future peer) — validated against the closed WORN_SLOTS list before any handler runs |
| save file -> engine | `c.worn` entries may be tampered (non-objects) — every read is truthiness/object-guarded; Plan 03's `sanitizeWorn` neutralises them on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-04 | Tampering | engine/actions.js useItem/unequipSlot | low | mitigate | closed-list validation of `slot`; exactly one addressing form; existing non-negative-int contract for `i` retained |
| T-37-05 | Elevation of Privilege | engine/items.js#equipItem staff branch | low | mitigate | the class gate (`wrongClass`) fires before any mutation, mirroring takeItem/useItem's existing gate |
| T-37-06 | Tampering | engine/items.js#useItem worn lookup | low | mitigate | a missing/tampered `c.worn[slot]` is a silent no-op (`if (!it) return events;`), never a throw |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed or changed this phase |
</threat_model>

<verification>
- `node --test test/unit/worn-slots.test.js` → >= 43 tests pass
- `node --test test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js` → pass (no new event type; new reason/replaced branches rendered and clean)
- `npm test` → `# fail 0`; fixtures untouched; master hash unchanged; no package change
- `git diff --stat 6671921 -- engine content src mazeworld.html docs` = the eight declared files
</verification>

<success_criteria>
- ROADMAP SC-1 engine half: an occupied slot is swapped (direct swap, `replaced` payload), never stacked; SC-2: through play the worn set is the only set `eff` counts; SC-4: every legacy path is pinned byte-identical
- CONTEXT bullets "Activatables must be worn to work", "Swap on an occupied slot", "unequipSlot extends…", "Taking a slot item", "Refusals reuse the existing vocabulary" each map to passing tests named above
</success_criteria>

<output>
Create `.planning/phases/37-equipment-slot-model-eff-refactor/37-02-SUMMARY.md` when done (with `## Human verification (deferred to end of run)`).
</output>
