---
phase: 54-four-band-retune-and-roster-decision
plan: 03
type: execute
wave: 3
depends_on: ["54-02"]
files_modified:
  - tools/damage-curve-audit-output.txt
  - content/BESTIARY-REBALANCE.md
  - docs/DIFFICULTY-RETUNE.md
  - test/parity/FIXTURE-INVENTORY.md
  - content/bestiary.js
  - test/unit/content-tables.test.js
  - tools/initiative-fixture-scan-output.txt
  - docs/class-pass/v17-p54-after-smoke.json
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/after-retune-solo.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/after-retune-party.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/after-retune-start-depth-20.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/after-retune-smoke.txt
autonomous: true
requirements: [TUNE-08, BAND-02, BAND-03]
tags: [damage-curve-audit, bestiary, roster-decision, ledger, deep-tier, forced-20]

must_haves:
  truths:
    - "`tools/damage-curve-audit.mjs` is re-run on the AFTER curve (Plan 02's final constants commit, AFTER_SHA) with the LOCKED yardstick (the default `--smoke=docs/class-pass/v17-p51-after-smoke.json` band levels — so the cell count is apples-to-apples with Phase 52's 65) for both rules the tool takes (`--rule=whole` then `--rule=dice` appended, the committed file's two-section shape); the executor VERIFIES the tool folds the curve into the damage it reports (`dmgBonusForBand` = max over the band's floors of `foeDmgBonusFor(T, difficultyCurve(d))` — the flat lvl² term foePower scales) and states this in the addendum; no `--curve` flag is needed because the fold already exists — if the verification ever fails, a `--curve` fold is added first (constraint 12; 54-CONTEXT Area 2 'verify it does')"
    - "`content/BESTIARY-REBALANCE.md` gains `### Phase 54 addendum (BAND-02 / TUNE-08, 2026-09-21)` at EOF: the audit cell count BEFORE (65, rule=dice, Phase 52 AFTER) → AFTER (the new FLAGGED line, verbatim), the full rule=dice still-flagged table reproduced verbatim with a refreshed `Disposition` per row, the rows cleared by the curve, the own-band mapping definition, any in-place retune with before/after, and the four-row roster table — nothing silently skipped (54-CONTEXT Area 2 'The 65 curve-height cells')"
    - "A row is retuned in place ONLY if it is still a ≥ 100 % one-shot at its own tier's band after the curve (crit ≥ that band's MU bar in the rule=dice audit) — USER RULING B (2026-09-21) applies this to the Drake: `2d10+4` → `2d8+2`, `drakeBreath` untouched, declared end-to-end with post-retune readouts — by a `strikesAs` / dice-shape change in the Herman style, declared per row (bestiary.js comment, content-tables re-pin, yardstick AFTER block regenerated, fixture scan re-run, FIXTURE-INVENTORY note, the phase's four readouts re-taken post-retune); no other dice trim anywhere (BAND-02, ROADMAP SC5); NO tier move for any creature (54-CONTEXT Deferred)"
    - "The tier-3/5 roster decision (TUNE-08) is recorded per creature — Herman, Drarl, Vampire, Djinni — as `stays — deliberate deep-tier threat` or `retuned in place`, each with two pieces of evidence: the AFTER audit's Endgame-band row (crit / MU bar / %) and the forced-20 slice's per-foe death count (`--seeds=50 --start-depth=20`, the final rung's transcript or the post-retune one); the forced-20 untaken rungs (Phase 27's p50 0 / mean 0.84 / reach ≥ 20 0.1 %, by reference) are named as the deliberate Endgame shape, the 21+ dials stay untouched, and any parity divergence is declared (expected zero — measured) (54-CONTEXT Area 3; ROADMAP SC4)"
    - "The ledger's Phase 54 H3 gains `#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)` and `#### Roster decision (TUNE-08)` (the last two H4s before `## v1.2 retune (Phase 27) — TUNE-05..07`); `test/unit/difficulty-retune-ledger.test.js`, `test/unit/bestiary-yardstick.test.js`, `test/parity/fixture-inventory.test.js` green; `npm test` fail 0; `npm run build:www` exit 0; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `tools/lib/tuning-bot.mjs` and `comparables.js` untouched; no persisted-schema change"
  artifacts:
    - path: "tools/damage-curve-audit-output.txt"
      provides: "the AFTER-curve audit, rule=whole + rule=dice, locked yardstick (and post-retune if a retune landed)"
    - path: "content/BESTIARY-REBALANCE.md"
      provides: "`### Phase 54 addendum (BAND-02 / TUNE-08, 2026-09-21)` — cell count before/after, re-dispositioned table, cleared rows, own-band mapping, any in-place retune, the roster table; (retune branch) the regenerated yardstick AFTER block"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "`#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)` and `#### Roster decision (TUNE-08)`"
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "the Phase 54 section's `### Roster / one-shot retune (TUNE-08)` subsection (zero exposure — measured; or the declared retune)"
    - path: "content/bestiary.js"
      provides: "(retune branch only) the declared in-place row change with a `DELIBERATE RULES CHANGE (Phase 54, TUNE-08 / BAND-02, 2026-09-21)` note"
    - path: "test/unit/content-tables.test.js"
      provides: "(retune branch only) the re-pinned row dice with the rationale"
  key_links:
    - from: "engine/difficulty.js (AFTER curve) → tools/damage-curve-audit.mjs#dmgBonusForBand"
      to: "the AFTER FLAGGED count and every row's own-band %"
      via: "`foeDmgBonusFor(T, difficultyCurve(d))` maxed over the band's floors"
      why: "the cell count is honest only if the tool reads the new dial — verified before the re-run, stated in the addendum"
    - from: "readouts/rung<last>-start-depth-20.txt (or after-retune-start-depth-20.txt)"
      to: "the roster table's per-foe death counts"
      via: "the transcript's `Death-cause breakdown` (`cut down by a Herman/Drarl/Vampire/Djinni`)"
      why: "TUNE-08 decides from bot evidence at depth 20, not from the audit alone"
    - from: "content/bestiary.js (retune branch)"
      to: "tools/initiative-fixture-scan-output.txt, test/parity/FIXTURE-INVENTORY.md roster block, content/BESTIARY-REBALANCE.md yardstick AFTER block, test/unit/content-tables.test.js"
      via: "scan re-run (expected byte-identical: no fixture rolls a tier-4/5 creature), `node tools/fixture-inventory.mjs`, `node tools/bestiary-yardstick.mjs`, the re-pin"
      why: "a bestiary row is a rule change under the engine gate — measured, declared, every pinned copy regenerated in the same commit"
  prohibitions:
    - "never edit test/parity/prototype-master.js.txt or test/parity/harness/comparables.js; never edit tools/lib/tuning-bot.mjs"
    - "never touch floor 1 identity or any engine/difficulty.js constant in this plan (the curve is Plan 02's final state)"
    - "no flat-damage nerfs / dice trims except a declared still-≥100 %-one-shot in-place retune (strikesAs or same-family dice shape; the smallest change that drops the crit below 100 % of the own band's MU bar — never the 60 % wouldBeTrim)"
    - "no tier moves (they re-roll the encounter tables); no 21+ easing; no sub-identity at 16–20"
    - "no draw added anywhere; no schema change"
    - "never change the audit's band-level yardstick to make the count look better (the committed file uses the default v17-p51 smoke; a v17-p54-smoke run is informational only, in the ledger, never the committed output)"
    - "never record a roster decision without both evidence cells filled from measured output"
---

<objective>
Close Phase 54: re-run the damage-curve audit on the landed curve (verifying the tool folds the curve's foePower into the flat lvl² damage term — it does, via `foeDmgBonusFor`), refresh the disposition of every still-flagged cell Phase 52 handed here (in-place retune only for a still-≥100 % one-shot at its own tier's band — USER RULING B (2026-09-21, 54-CONTEXT.md `## USER RULINGS at plan approval (2026-09-21)`): the Drake, tier 4, 120 % at the Breakaway bar, IS retuned in place `2d10+4` → `2d8+2` with `drakeBreath` untouched, declared end-to-end with the post-retune readouts; the four roster creatures read 62–69 % at the Endgame bar under the live dice rule and record `stays`), record the per-creature tier-3/5 roster decision (TUNE-08) with the AFTER audit + the forced-20 slice as evidence and the forced-20 untaken rungs as the deliberate Endgame shape, complete the ledger, and hand the orchestrator the verifier-grade phase roll-up.

Purpose: BAND-02 requires that no flat-damage nerf chased the median — this plan proves the audit was re-read on the new dial honestly and that the only bestiary change (if any) is the one exception the user allowed; TUNE-08 closes the roster question that v1.2 left open.

Output: `tools/damage-curve-audit-output.txt` (re-run), `content/BESTIARY-REBALANCE.md` (Phase 54 addendum), `docs/DIFFICULTY-RETUNE.md` (audit + roster H4s), `test/parity/FIXTURE-INVENTORY.md` (roster subsection), and — retune branch only — `content/bestiary.js`, `test/unit/content-tables.test.js`, the regenerated yardstick block, re-run scan output, post-retune readouts and `v17-p54-after-smoke.json`.

Decisions implemented (54-CONTEXT.md, quoted): Area 2 — "The 65 curve-height cells (Phase 52 hand-off): no per-row dice trims. After the curve lands, re-run `node tools/damage-curve-audit.mjs` (both `--rule=whole|dice` as the tool takes them), commit the output, record the new flagged-cell count in the ledger, and refresh each remaining row's Disposition in `content/BESTIARY-REBALANCE.md` (a Phase 54 addendum). A row is retuned only if it is still a ≥ 100 % one-shot (crit ≥ the band's max HP bar) at its own tier's band after the curve — and then by a `strikesAs` / dice-shape change in the Herman style, declared per row. Note the audit's HP bars may need the band's new foePower folded in (the tool reads the curve — verify it does)." Area 3 — "Per creature, from the AFTER audit + a forced-20 bot slice … Herman, Drarl, Vampire, Djinni each get one of `stays — deliberate deep-tier threat` (the default), or `retuned in place` (only if still a ≥ 100 % one-shot at its own tier's band after the curve; strikesAs / dice shape, never a tier move …). Herman is already retuned (Phase 52, strikesAs: 5, crit 37) and is expected to record as 'stays'"; "The forced-20 untaken rungs are the deliberate Endgame shape: floors gained p50 0 / mean 0.84 and reach ≥ 20 0.1 % are recorded as intended (victory rare and celebrated); no further 21+ easing (FOE_POWER_MAX, ABILITY_THREAT_MAX, FOE_CAP_MAX untouched); the depth-20 slice is the yardstick that proves 16–20 did not move"; "Any roster change is measured with the fixture scan and declared per the engine gate — expected zero (no fixture meets these four)."

Claude's Discretion (resolved by this plan) — "own tier's band": the band whose baseline hero level a tier-T creature is met by, i.e. Filter (1–4) ↔ tiers 1–2, Wall (5–8) ↔ tier 3, Breakaway (9–15) ↔ tier 4, Endgame (16–20) ↔ tier 5 (matches the CONTEXT's tier-5 ↔ Endgame reading; a tier-T row's effective hero level in the audit is `max(T, bandLevel)`). Note the CONTEXT's "today's Endgame-bar readings to re-measure: Drarl 62 (116 %), Vampire 58 (108 %), Djinni 58 (108 %)" are the rule=whole numbers; under the live rule=dice audit at HEAD they read Drarl 37 (69 %), Vampire 33 (62 %), Djinni 33 (62 %), Herman 37 (69 %) — the plan re-measures on the AFTER curve and records whichever the tool prints.

Schema gate: no persisted schema change; no migration. No engine/difficulty.js edit in this plan. No shell change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-CONTEXT.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-01-SUMMARY.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-02-SUMMARY.md
@.planning/phases/52-foe-cadence-damage-curve/52-03-SUMMARY.md
</context>

<ground_rules>
- Base = Plan 02's final state (HEAD after 54-02's SUMMARY commit); `AFTER_SHA` = the final constants commit named in 54-02-SUMMARY.md; `git diff --stat AFTER_SHA -- engine/ content/ tools/lib/` must be empty before Task 1.
- Commits: plain `git commit -F "$SCRATCH/msg.txt"`; every message ends with exactly the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
- Gates at every commit: `npm test` (fail 0) and `npm run build:www` (exit 0); `boot:check` not a gate; skip `state.advance-plan`.
- Master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every task.
- Every number in the addendum/ledger is pasted from tool output or a quoted `node -e` one-liner; the ledger and the rebalance doc are CRLF — splice with `\r\n`; read only the lines you need (`grep`, `sed -n`) — never cat the 700-line audit output into context.
- No device / human checkpoints; the SUMMARY ends with `## Human verification (deferred to end of run)`.
</ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: Verify the audit folds the curve, re-run it on the AFTER curve (locked yardstick, both rules), compute every flagged row's own-band %, apply the one allowed exception (in-place retune of any still-≥100 % one-shot — declared, re-pinned, re-measured), and write the BESTIARY-REBALANCE Phase 54 addendum</name>
  <files>tools/damage-curve-audit-output.txt, content/BESTIARY-REBALANCE.md, content/bestiary.js, test/unit/content-tables.test.js, tools/initiative-fixture-scan-output.txt, test/parity/FIXTURE-INVENTORY.md, docs/class-pass/v17-p54-after-smoke.json</files>
  <read_first>
    - tools/damage-curve-audit.mjs lines 1-86 (header: the CLI, the yardstick — band hero level = median `meanLevel` of the smoke's cells whose `p50Depth` falls in the band, default smoke `docs/class-pass/v17-p51-after-smoke.json`; `dmgBonus` = MAX over the band's floors of `foeDmgBonusFor(T, difficultyCurve(d))`; `critMax` per rule; the FLAG rule `maxSingleHit >= 0.6 * MUbar`; `wouldBeTrim` targets 60 % — NOT the 100 % rule this plan applies), lines 104-116 (`parseArgs`), lines 197-208 (`dmgBonusForBand` — THE FOLD: `foeDmgBonusFor(T, difficultyCurve(d))` maxed over `d = max(band.min, T) .. band.max`), lines 252-260 (`main` — the `# damage-curve-audit — rule=…` header and `Band levels:` line)
    - tools/damage-curve-audit-output.txt lines 1-10 (the BEFORE file's header — `Band levels: median meanLevel from docs/class-pass/v17-p51-after-smoke.json (commit 608a0e5)`; Filter L2 / Wall L3 / Breakaway L5 / Endgame L5; MU bars 35.0 / 39.5 / 53.5 / 53.5), lines 619-690 (`## Flagged (rule=dice)` — the table `band | tier | type | name | dice | maxSingleHit | MU bar | % | category | wouldBeTrim` and the `FLAGGED: 65 rows across 209 row×band cells (deep-tier 21, level-base 14, row-dice 30, bolt 0)` line — the BEFORE count), lines 692-705 (`## Herman` block)
    - engine/difficulty.js `foeDmgBonusFor` (lines ~470-476: `curve.foePower === 1 ? 0 : Math.round((curve.foePower - 1) * lvl * lvl) || 0`) and the landed band constants (the AFTER curve)
    - content/BESTIARY-REBALANCE.md lines 389-402 (`### Regenerating this document` — the yardstick AFTER block between `<!-- yardstick:after:begin -->` / `<!-- yardstick:after:end -->` is regenerated by `node tools/bestiary-yardstick.mjs`; the BEFORE block never), lines 502-640 (the Phase 52 addendum — the crit-rule table, the Herman before/after table, `#### Still flagged after the fixes (rule=dice) — ruled` with its `Disposition` column vocabulary, the closing "USER RULING" paragraph; the new addendum goes at EOF after it)
    - content/bestiary.js lines 1-45 (the header's declared-change register — Phase 18 / Phase 27 / Phase 52 paragraphs; a retune adds a Phase 54 paragraph in the same register) and the candidate row(s) (`grep -n 'Drake' content/bestiary.js` — `{ n: "Drake", sz: "B", i: 15, wp: 38, sp: { dmg: { n: 2, sides: 10, bonus: 4 }, every: 4, note: "breathes fire every four rounds" }, abilities: ["drakeBreath"] }`; `drakeBreath` in content/foe-abilities.js line 81 is the kit's OWN 2d10+4 bolt — untouched by an `sp.dmg` change)
    - test/unit/content-tables.test.js lines 98-107 (`BESTIARY: Bat/Rat sp.dmg is a flat 1, atk 2; Drake dmg is 2d10+4`) and lines 206-212 (`BESTIARY Phase 18 / D-18: Drake wp 38 (was 135), dmg 2d10+4 and every:4 unchanged`) — the two pins a Drake retune re-pins with a rationale
    - test/unit/bestiary-yardstick.test.js lines 269-296 (the D-04 doc-consistency test — the AFTER block must equal a fresh render)
    - test/parity/FIXTURE-INVENTORY.md lines 30-120 (the generated roster block — which creatures fixtures roll; no tier-4/5 creature is listed) and Plan 02's `## Phase 54: …` section (append the roster subsection to it)
    - .planning/phases/52-foe-cadence-damage-curve/52-03-SUMMARY.md (how the two-section audit file and the addendum table were produced verbatim)
  </read_first>
  <action>
    Step 1 — preconditions and the fold verification: `git diff --stat AFTER_SHA -- engine/ content/ tools/lib/` empty; `grep -n 'foeDmgBonusFor(T, difficultyCurve(d))' tools/damage-curve-audit.mjs` prints the line inside `dmgBonusForBand` — the tool folds the curve's `foePower` into the flat lvl² term of every row's hit/crit (the only curve term that reaches foe damage; the wp scaling is foe HP). Cross-check numerically: `node -e "import('./engine/difficulty.js').then(m=>{for(const T of [3,4,5]){const rows=[];for(let d=5;d<=20;d++)rows.push(d+':'+m.foeDmgBonusFor(T,m.difficultyCurve(d)));console.log('T'+T,rows.join(' '))}})"` — paste it; the Wall/Breakaway floors show the negative bonus the band produces, Endgame shows 0. Conclusion to record: the fold exists; NO `--curve` flag is added (constraint 12's branch is not taken — say so explicitly in the addendum and the SUMMARY). If — and only if — the grep or the numbers contradict this, add a `--curve` fold to the tool first (report-only, exit 0), commit it, and proceed.

    Step 2 — the AFTER audit, locked yardstick: `node tools/damage-curve-audit.mjs --rule=whole > tools/damage-curve-audit-output.txt && node tools/damage-curve-audit.mjs --rule=dice >> tools/damage-curve-audit-output.txt` (the same two-section shape as the committed file; the default `--smoke` keeps the Phase 51 band levels — Filter L2 / Wall L3 / Breakaway L5 / Endgame L5 — so the count compares to 65). Extract: `grep -n '^FLAGGED:' tools/damage-curve-audit-output.txt` (two lines: rule=whole, rule=dice — paste both), and the rule=dice flagged table (`sed -n '/^## Flagged (rule=dice)/,/^FLAGGED:/p' tools/damage-curve-audit-output.txt`). Informational only (NOT committed to the output file): `node tools/damage-curve-audit.mjs --rule=dice --smoke=docs/class-pass/v17-p54-after-smoke.json | grep -E '^(Band levels|Filter|Wall|Breakaway|Endgame|FLAGGED)'` — the band levels under the retuned smoke and the count they give; quote it in the ledger as a footnote.

    Step 3 — own-band evaluation: with a scratch `node -e` over the rule=dice flagged table (parse the `band | tier | … | %` rows), compute for every DISTINCT (type, name, tier) row its own band per the mapping (tier 1–2 → Filter, 3 → Wall, 4 → Breakaway, 5 → Endgame) and read that band's `%` cell (if the row is not flagged in its own band, its own-band % is below 60 — read the per-band table in the same file for the exact figure); list every row with own-band % >= 100. Expected (USER RULING B was made on this reading — paste the measured list regardless): exactly one — Drake (Beasts tier 4, 2d10+4, crit 64 vs the Breakaway MU bar 53.5 = 120 %; the Endgame band is identity, so no curve value can change this cell); the four roster creatures read 62–69 % at Endgame. If the measured list differs from the expectation, record the difference in the SUMMARY's Issues and still apply Ruling B to the Drake (the ruling is explicit) while applying the rule as written to any other ≥ 100 % row.

    Step 4 — the one allowed exception, per row >= 100 % — USER RULING B (2026-09-21): the Drake retune is DECIDED, not conditional: `content/bestiary.js` Drake (Beasts tier 4) `sp.dmg` `{ n: 2, sides: 10, bonus: 4 }` → `{ n: 2, sides: 8, bonus: 2 }` (diceMax 18, crit 16 + 0 + 36 = 52 = 97 % of the Breakaway MU bar 53.5; `strikesAs` would have to fall to 2 to clear and is flavor-wrong for a Drake; the kit bolt `drakeBreath` in content/foe-abilities.js keeps its own 2d10+4 — untouched). For any OTHER row the measured list shows at >= 100 % (expected none), apply the same rule: the SMALLEST same-family change that drops `critMax` (rule=dice: `levelBase + dmgBonus + 2 × diceMax`) below the own band's MU bar, declared the same way. The four roster creatures are NOT retuned (Ruling B: they record `stays`). Apply: (a) `content/bestiary.js` — the row's `sp.dmg` (both tier copies if the creature appears in two tiers), plus a Phase 54 paragraph in the file header: "Phase 54 (TUNE-08 / BAND-02, 2026-09-21): DELIBERATE RULES CHANGE — <name> `sp.dmg` <old> → <new>: the only row still a ≥ 100 % one-shot (crit ≥ its own band's MU bar) after the four-band curve, retuned in place per 54-CONTEXT Area 2 (Herman-style declared change; no tier move); every other flagged row keeps its dice"; (b) `test/unit/content-tables.test.js` — re-pin the two Drake assertions to the new notation with a comment citing Phase 54 and the audit % (never delete the tests); (c) regenerate the yardstick AFTER block: `node tools/bestiary-yardstick.mjs` stdout pasted verbatim between `<!-- yardstick:after:begin -->` / `<!-- yardstick:after:end -->` in content/BESTIARY-REBALANCE.md (replace the whole block including the `Mechanics: canon | …` line); `node --test test/unit/bestiary-yardstick.test.js test/unit/content-tables.test.js` green; (d) parity: `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt` then `git diff --stat tools/initiative-fixture-scan-output.txt` — expected EMPTY (no replay site rolls a tier-4 creature — the generated roster block in FIXTURE-INVENTORY.md lists none); `node --test test/parity/` green, `git diff --stat AFTER_SHA -- test/parity/fixtures/` empty; `node tools/fixture-inventory.mjs` → the generated block byte-identical (`git diff --stat test/parity/FIXTURE-INVENTORY.md` shows only your later subsection); MOVER BRANCH: a non-empty scan diff means a fixture rolls the retuned creature — declare a `+54` record with measured before/after, regenerate only that fixture, extend the BAND-02 guard's `EXPECTED`, record in the SUMMARY; (e) re-run the audit AFTER the retune exactly as Step 2 (the committed output file must describe the tree it sits on) and paste the post-retune `FLAGGED:` lines; the ledger keeps BOTH counts (pre-retune curve-only, post-retune); (f) re-take the phase's four readouts on the retune commit (the bestiary row is a rule change under the measurement gate): commit the retune first (`feat(54-03): in-place retune — <name> sp.dmg <old> -> <new> (the one still-≥100 % one-shot at its own band; TUNE-08 / BAND-02)` with the two trailers, green), then `--party` in the background to `readouts/after-retune-party.txt`, solo to `after-retune-solo.txt`, `--seeds=50 --start-depth=20` to `after-retune-start-depth-20.txt`, the smoke to `docs/class-pass/v17-p54-after-smoke.json` (OVERWRITING Plan 02's copy — the phase's AFTER is post-retune; the pre-retune smoke survives as `v17-p54-rung<last>-smoke.json`) with stdout to `after-retune-smoke.txt`; the depth-20 slice is EXPECTED to differ from BEFORE now (the retuned creature is reachable at 20) — record the diff summary as "moved by the roster retune, not the curve: the final rung's slice was byte-identical" and compare solo `>=5` against the FINAL RUNG's value from 54-02-SUMMARY.md (33.0 % only if no Filter rung landed — USER RULING A; a tier-4 creature is reachable at floor 4 only by a level-4+ hero, so a change here is floor-4 exposure — if the number moves, quantify it from the histogram and record it honestly).

    Step 5 — `content/BESTIARY-REBALANCE.md` — append at EOF `### Phase 54 addendum (BAND-02 / TUNE-08, 2026-09-21)` (CRLF) with: an opening paragraph (the four-band curve landed — the constants and values from Plan 02's change table by reference; the audit's fold verified: `dmgBonusForBand` reads `foeDmgBonusFor(T, difficultyCurve(d))`, so the Wall/Breakaway cells carry the band's negative flat bonus and the Endgame cells are identity; no `--curve` flag was needed; the yardstick is LOCKED to the Phase 51 smoke's band levels so the count compares to 65); `#### Audit cell count — BEFORE (Phase 52 AFTER, rule=dice: 65) → AFTER (Phase 54 curve, commit <AFTER_SHA short>)` with both new `FLAGGED:` lines verbatim (rule=whole and rule=dice) and, if a retune landed, the post-retune pair too; `#### Own-band mapping (the ≥ 100 % one-shot rule)` — the definition and the computed list of rows ≥ 100 % (or "none"); `#### In-place retune (declared)` — the row table `| Creature | Tier | Field | Before | After | Crit (rule=dice, own band) before → after | % of own MU bar |` or the sentence "none"; `#### Still flagged after the four-band curve (rule=dice) — re-dispositioned` — the post-curve (post-retune if any) rule=dice flagged table reproduced VERBATIM from the committed audit output with a `Disposition` column appended per row using exactly one of: `stays — deliberate deep-tier threat (Endgame band) — TUNE-08` (tier 5), `curve height on the landed band — <own-band %> at its own band, below the one-shot bar; no trim (BAND-02)` (tiers 1–4 under 100 %), `retuned in place (Phase 54): <old> → <new>; see the declared table above` (a retuned row's remaining cells); `#### Cleared by the curve` — every (band, tier, type, name) cell flagged in the Phase 52 table that no longer appears (or "none"); and a closing sentence that the Phase 52 addendum's Dispositions are superseded by this table for the rows listed (historical lines left as-is, the Herman convention).

    Step 6 — gates and commit: `node --test test/unit/bestiary-yardstick.test.js test/unit/content-tables.test.js test/parity/` green; `npm test` fail 0; `npm run build:www` exit 0; master hash; `git diff --stat AFTER_SHA -- engine/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js` empty. Commit `docs(54-03): damage-curve audit re-run on the AFTER curve (fold verified, locked yardstick; FLAGGED <before> -> <after>) + BESTIARY-REBALANCE Phase 54 addendum — every cell re-dispositioned (BAND-02)` with the two trailers (the retune, if any, is already its own earlier commit).
  </action>
  <verify>
    <automated>grep -c 'foeDmgBonusFor(T, difficultyCurve(d))' tools/damage-curve-audit.mjs && grep -c '^FLAGGED:' tools/damage-curve-audit-output.txt && grep -c '^# damage-curve-audit — rule=whole' tools/damage-curve-audit-output.txt && grep -c '^# damage-curve-audit — rule=dice' tools/damage-curve-audit-output.txt && grep -c '^### Phase 54 addendum (BAND-02 / TUNE-08, 2026-09-21)' content/BESTIARY-REBALANCE.md && grep -c '^#### Still flagged after the four-band curve (rule=dice) — re-dispositioned' content/BESTIARY-REBALANCE.md && grep -c '^#### Own-band mapping' content/BESTIARY-REBALANCE.md && node --test test/unit/bestiary-yardstick.test.js test/unit/content-tables.test.js test/parity/divergence-records.test.js test/parity/fixture-inventory.test.js && node tools/damage-curve-audit.mjs --rule=dice | diff - <(sed -n '/^# damage-curve-audit — rule=dice/,$p' tools/damage-curve-audit-output.txt) && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'foeDmgBonusFor(T, difficultyCurve(d))' tools/damage-curve-audit.mjs` prints 1 (the fold exists) and the addendum states it (`grep -c 'no .--curve. flag' content/BESTIARY-REBALANCE.md` >= 1 or the equivalent sentence naming `dmgBonusForBand`); `git diff --stat AFTER_SHA -- tools/damage-curve-audit.mjs` empty (no tool change was needed) — or, under the fallback, the `--curve` commit is named in the SUMMARY
    - `tools/damage-curve-audit-output.txt` has exactly two `# damage-curve-audit — rule=` headers (whole then dice), its `Band levels:` lines name `docs/class-pass/v17-p51-after-smoke.json`, and `node tools/damage-curve-audit.mjs --rule=dice | diff - <(sed -n '/^# damage-curve-audit — rule=dice/,$p' tools/damage-curve-audit-output.txt)` prints nothing (the committed file describes the committed tree)
    - the two new `FLAGGED:` lines are quoted verbatim in the addendum (`grep -c 'FLAGGED: ' content/BESTIARY-REBALANCE.md` grows by >= 2) and the BEFORE count 65 is named beside the AFTER count
    - the addendum's re-dispositioned table has exactly as many data rows as the committed rule=dice `FLAGGED: N rows` says (`N` — computed by a quoted one-liner over both files and pasted) and every row's Disposition cell is one of the three vocabulary strings; `grep -c 'stays — deliberate deep-tier threat (Endgame band) — TUNE-08' content/BESTIARY-REBALANCE.md` >= 1
    - the own-band list is pasted (`grep -c '^#### Own-band mapping' content/BESTIARY-REBALANCE.md` 1); every row with own-band % >= 100 has a row in `#### In-place retune (declared)` — or that H4 contains the sentence `none`
    - RETUNE BRANCH (USER RULING B — taken for the Drake): `git diff AFTER_SHA -- content/bestiary.js | grep -c '^+.*sides: 8, bonus: 2'` >= 1; `grep -c 'Phase 54 (TUNE-08 / BAND-02, 2026-09-21)' content/bestiary.js` >= 1; `node --test test/unit/content-tables.test.js` green with both Drake pins re-pinned (not deleted: `grep -c 'Drake' test/unit/content-tables.test.js` unchanged); `node --test test/unit/bestiary-yardstick.test.js` green (block regenerated); `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` empty; `git diff --stat AFTER_SHA -- test/parity/fixtures/` empty; `readouts/after-retune-{solo,party,start-depth-20,smoke}.txt` exist with `Bot:` lines; `docs/class-pass/v17-p54-after-smoke.json` `meta.commit` = the retune commit's prefix; the retune commit ends with the two trailers
    - NO-RETUNE BRANCH: not applicable under USER RULING B (the Drake is retuned); the `#### In-place retune (declared)` table has exactly one Drake row unless the measured own-band list adds another ≥ 100 % row
    - `npm test` fail 0; `npm run build:www` exit 0; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat AFTER_SHA -- engine/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js` empty; the addendum commit ends with the two trailers
  </acceptance_criteria>
  <done>The audit is re-read on the landed dial with a verified fold and a locked yardstick; every one of the handed-off cells carries a fresh disposition; the single allowed exception is either applied and declared end-to-end or explicitly recorded as not needed.</done>
</task>

<task type="auto">
  <name>Task 2: The roster decision (TUNE-08) — per creature from the AFTER audit + the forced-20 slice, the deliberate Endgame shape, the parity declaration — in the ledger (audit + roster H4s), the addendum's roster table, and the FIXTURE-INVENTORY roster subsection</name>
  <files>docs/DIFFICULTY-RETUNE.md, content/BESTIARY-REBALANCE.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - the forced-20 slice transcript: `readouts/after-retune-start-depth-20.txt` if the retune branch ran, else `readouts/rung<last>-start-depth-20.txt` (Plan 02) — `Death-depth distribution`, `Death-cause breakdown` (the `cut down by a Herman/Drarl/Vampire/Djinni` lines), the `Four-band readout` block (`floors gained p50=… mean=…`, `encounters survived mean=…`, `reach: … >=20 …`), `Stuck:`
    - tools/damage-curve-audit-output.txt — the rule=dice per-band tables' Endgame rows for Herman (T4 + T5), Drarl (T4 + T5), Vampire (T5), Djinni (T4 + T5) (`grep -n 'Herman\|Drarl\|Vampire\|Djinni' tools/damage-curve-audit-output.txt` within the `## Endgame (depth 16-20` section of the rule=dice half) and the `## Herman` block
    - docs/DIFFICULTY-RETUNE.md lines 4056-4070 (`#### tune-classes forced 20 — 143 x 10 (docs/class-pass/retune-after-depth20.json)` — the Phase 27 forced-20 pooled rows: `meanFloorsGained` 0.84 / `p50FloorsGained` 0 / `reach20`; quote by reference with the exact heading), lines 4349-4360 (`#### tune-difficulty --seeds=200 --start-depth=20` Phase 27 slice — the v1.2 comparison point), lines 4756-4805 (`### Depth-20 slice — Phase 27's yardstick (no verdicts)`), the Phase 54 H3's `#### Parity — MOVED SET (0) (BAND-02)` (the last H4 before the v1.2 H2 — the audit and roster H4s go after it)
    - content/bestiary.js rows for the four (`grep -n 'Herman\|Drarl\|Vampire\|Djinni' content/bestiary.js`): Herman T4/T5 `wp 36, sp: { invis, ar: 15, strikesAs: 5 }`; Drarl T4/T5 `wp 19, sp: { acid, noArmor }`; Vampire T5 `wp 71, sp: { atk: 2, awe, caster, seesInvis, noTurn, dmg 1d4 }` + kit; Djinni T4/T5 `wp 65, sp: { caster, dmg 1d4, fleesBelow: 0.25 }` + kit
    - test/parity/FIXTURE-INVENTORY.md — the generated roster block (none of the four appear) and the Phase 54 section (append the subsection at its end)
    - .planning/REQUIREMENTS.md line 48 (TUNE-08's exact wording — "decide per creature whether it stays, moves tier, or is retuned (the DMG-02 rows feed this), declare any parity divergence, and record the untaken rungs so the Endgame band is a deliberate shape")
  </read_first>
  <action>
    Step 1 — evidence extraction (pasted, never typed): from the forced-20 transcript, a one-liner giving per-foe death counts for the four (`grep -E 'cut down by a (Herman|Drarl|Vampire|Djinni)' <file>`), the total completed runs, the top-5 causes, floors gained p50/mean and encounters survived mean from the band block, `reach: … >=20`; from the audit (rule=dice, post-curve/post-retune), each creature's Endgame-band row(s): tier, dice, maxSingleHit (crit), MU bar, % (Herman T4 and T5, Drarl T4 and T5, Vampire T5, Djinni T4 and T5).

    Step 2 — the decision per creature — USER RULING B (2026-09-21): all four record `stays — deliberate deep-tier threat` (the rule — `retuned in place` iff the own-band (tier 5 → Endgame) crit >= 100 % of the MU bar — does not fire for any of them; the pasted rows are the evidence). Never `moves tier`. Expected readings (paste the measured rows): Herman stays (Phase 52's `strikesAs: 5`, crit 37 / 53.5 = 69 %), Drarl stays (crit 37, 69 %), Vampire stays (crit 33 × `atk: 2` swings — worstTurn 66 is two hits, not one; the one-shot rule is single-hit — 62 %), Djinni stays (crit 33, 62 %; its bolts 24 max). Each decision gets a one-sentence rationale in the family-friendly register of the ledger (deadpan, no profanity).

    Step 3 — the ledger (splice CRLF, before the v1.2 H2, after `#### Parity — MOVED SET (0) (BAND-02)`): `#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)` — the fold sentence (`dmgBonusForBand` → `foeDmgBonusFor(T, difficultyCurve(d))`; no `--curve` flag needed), the `node -e` dmgBonus-by-floor lines for T3/T4/T5, the BEFORE (65) and AFTER `FLAGGED:` lines verbatim (and post-retune if any), the informational v17-p54-smoke yardstick footnote, the own-band ≥ 100 % list, the in-place retune table or "none", the pointer to the addendum's re-dispositioned table; then `#### Roster decision (TUNE-08)` — the table `| Creature | Tier(s) | AFTER audit, Endgame band (rule=dice): dice / crit / MU bar / % | Forced-20 slice (--seeds=50 --start-depth=20, commit <sha>): deaths to this foe of N completed | Decision | Rationale |` with the four rows; then the paragraph "The forced-20 untaken rungs are the deliberate Endgame shape": Phase 27's forced-20 band rows quoted by reference (`#### tune-classes forced 20 — 143 x 10 (docs/class-pass/retune-after-depth20.json)`: `p50FloorsGained` 0, `meanFloorsGained` 0.84, `reach20` 0.1 %) and this phase's own slice numbers (floors gained p50 / mean, encounters survived, reach ≥ 20 from the band block), with the untaken rungs named: (1) sub-identity at 16–20 — refused: BAND-01's depth-20 yardstick (16+ literal identity by construction; every rung's slice byte-identical); (2) 21+ easing of `FOE_POWER_MAX` 1.15 / `ABILITY_THREAT_MAX` 1.3 / `FOE_CAP_MAX` 4 — refused: Phase 27's "no dial-back past 20" user rule and 54-CONTEXT Area 3; (3) tier moves — refused: they re-roll the encounter tables (54-CONTEXT Deferred); "victory rare and celebrated" is therefore recorded as intended, not as residue; then the parity declaration: "Roster parity divergence: <zero — measured: the fixture scan re-run is byte-identical and no replay site rolls a tier-4/5 creature (FIXTURE-INVENTORY roster block) | or the declared +54 record>".

    Step 4 — `content/BESTIARY-REBALANCE.md`: append `#### Roster (TUNE-08)` to the Phase 54 addendum — the same four-row table and the untaken-rungs sentence.

    Step 5 — `test/parity/FIXTURE-INVENTORY.md`: append to the `## Phase 54: …` section `### Roster / one-shot retune (TUNE-08)` — either "No bestiary row changed for the roster decision (all four `stays`); the one in-place retune (<name>, if any) is declared above/below with the scan re-run byte-identical; MOVED SET (0) stands" or the declared record. `node tools/fixture-inventory.mjs` → generated block unchanged; `node --test test/parity/fixture-inventory.test.js test/unit/difficulty-retune-ledger.test.js` green.

    Step 6 — gates and commit: `npm test` fail 0; `npm run build:www` exit 0; master hash. Commit `docs(54-03): roster decision recorded per creature — Herman / Drarl / Vampire / Djinni (AFTER audit + forced-20 slice), the forced-20 untaken rungs named as the deliberate Endgame shape, parity declared (TUNE-08)` with the two trailers.
  </action>
  <verify>
    <automated>grep -c '^#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)' docs/DIFFICULTY-RETUNE.md && grep -c '^#### Roster decision (TUNE-08)' docs/DIFFICULTY-RETUNE.md && grep -c '^#### Roster (TUNE-08)' content/BESTIARY-REBALANCE.md && grep -c '^### Roster / one-shot retune (TUNE-08)' test/parity/FIXTURE-INVENTORY.md && for n in Herman Drarl Vampire Djinni; do grep -c "^| $n |" docs/DIFFICULTY-RETUNE.md | grep -qv '^0$' || exit 1; done && node --test test/unit/difficulty-retune-ledger.test.js test/parity/fixture-inventory.test.js && grep -n '^## ' docs/DIFFICULTY-RETUNE.md | tail -1 | grep -c 'v1.2 retune (Phase 27)' && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#### Roster decision (TUNE-08)' docs/DIFFICULTY-RETUNE.md` prints 1; the roster table has exactly four data rows starting `| Herman |`, `| Drarl |`, `| Vampire |`, `| Djinni |`, each with a non-empty audit cell (contains `%`), a non-empty forced-20 cell (contains ` of `), and a Decision cell that is exactly `stays — deliberate deep-tier threat` or `retuned in place`; the string `moves tier` appears only in the untaken-rungs sentence (as refused), never as a Decision
    - the section contains `p50FloorsGained` and `0.84` and `0.1 %` (the Phase 27 rows by reference), `floors gained p50=` (this phase's slice), `sub-identity at 16–20`, `FOE_POWER_MAX`, `ABILITY_THREAT_MAX`, `FOE_CAP_MAX`, and a `Roster parity divergence:` line (each `grep -c` >= 1)
    - `grep -c '^#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)' docs/DIFFICULTY-RETUNE.md` prints 1 and that section contains `FLAGGED: 65 rows` (the BEFORE) and at least one other `FLAGGED:` line (the AFTER) plus `dmgBonusForBand`
    - `grep -c '^#### Roster (TUNE-08)' content/BESTIARY-REBALANCE.md` prints 1 with the same four rows; `grep -c '^### Roster / one-shot retune (TUNE-08)' test/parity/FIXTURE-INVENTORY.md` prints 1; `node tools/fixture-inventory.mjs` leaves the generated block unchanged (`git diff --stat test/parity/FIXTURE-INVENTORY.md` shows only the appended subsection)
    - the v1.2 H2 is still the last `## ` heading; `node --test test/unit/difficulty-retune-ledger.test.js test/parity/fixture-inventory.test.js` green
    - `npm test` fail 0; `npm run build:www` exit 0; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; the commit ends with the two trailers
  </acceptance_criteria>
  <done>TUNE-08 is closed: four recorded per-creature decisions with measured evidence, the Endgame shape declared deliberate with its untaken rungs named, parity declared.</done>
</task>

<task type="auto">
  <name>Task 3: Gates recorded verbatim + the verifier-grade phase roll-up SUMMARY (constants change table, every rung's four readouts, final AFTER vs BAND-01 with the miss table, the roster table, audit cell count before/after, scan / parity / MOVED SET lines, master hash, npm test totals, build:www)</name>
  <files>.planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/54-four-band-retune-and-roster-decision/54-01-SUMMARY.md and 54-02-SUMMARY.md (the sections to roll up — copy their tables, do not recompute)
    - docs/DIFFICULTY-RETUNE.md — the Phase 54 H3's `#### Change table (BAND-03)`, `#### Miss table (BAND-03)`, `#### Roster decision (TUNE-08)`, `#### Damage-curve audit …` (quote verbatim)
    - .planning/ROADMAP.md Phase 54 section (the five success criteria — the proof map's rows)
    - .planning/phases/53-joiner-level-cap/53-02-SUMMARY.md (SUMMARY shape; `## Human verification (deferred to end of run)` register)
  </read_first>
  <action>
    Run and paste verbatim: `npm test 2>&1 | tail -12` (fail 0), `npm run build:www` (exit 0), `git hash-object test/parity/prototype-master.js.txt`, `git diff --stat 78572c5 -- test/parity/harness/comparables.js tools/lib/tuning-bot.mjs src/browser/ mazeworld.html engine/saveState.js test/parity/fixtures/` (empty), `git diff 78572c5 -- test/unit/foe-turn-draw-count.test.js test/unit/combat.test.js | grep -c '^-[^-]'` (0), `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` (empty), `git log --oneline 78572c5..HEAD -- engine/ content/ tools/ test/ docs/` (the phase's commits), and `node -e` printing every engine/difficulty.js constant (the 14 Phase 54 ones plus the Phase 21/27 dials) as the final state.

    Write `$PHASE_DIR/54-03-SUMMARY.md` as the PHASE roll-up the orchestrator's VERIFICATION.md is written from: (1) `## Success criteria → proof` — one row per ROADMAP SC: SC1 → the ledger H4s `#### Target — the four bands, verbatim (BAND-01)` / `#### Target as numbers (BAND-01)` (grep counts); SC2 → the change table + `grep -c 'Phase 54 ladder rung' engine/difficulty.js` + the rung count + breather/DENSITY pins unchanged; SC3 → the final reading table and the miss table verbatim (solo p50/p90/reach ≥5/≥10/≥16/≥20, party p50/p90/stuck, pooled smoke mean; every miss with untaken rung + reason); SC4 → the roster table verbatim + the untaken-rungs sentence + the parity line; SC5 → `git diff --stat 78572c5 -- content/` (empty, or exactly the declared in-place retune) + the gates; (2) `## Constants change table (before → after)` — verbatim from the ledger; (3) `## Rung readouts` — per rung: solo / party / class smoke / start-depth-20 headline numbers and the one-line reading (copied from 54-02-SUMMARY); (4) `## Final AFTER vs BAND-01` — the reading + miss table; (5) `## Roster table (TUNE-08)` — verbatim; (6) `## Audit cell count` — BEFORE 65 → AFTER (and post-retune) `FLAGGED:` lines, the fold verification sentence, the in-place retune (or none); (7) `## Parity` — scan diff empty, `node --test test/parity/` totals, `MOVED SET (0)`, the BAND-02 guard name, FIXTURE-INVENTORY section + subsection names; (8) `## Master hash re-check` — the hash line; (9) `## Gates` — `npm test` totals (the final count), `build:www` exit 0, the untouched-file diffs, `boot:check` environment-blocked (not run); (10) `## Schema` — no persisted schema change anywhere in Phase 54 (difficultyCurve is a pure lookup on depth; a bestiary dice row is content, not save schema); (11) `## Commits` — every Phase 54 commit SHA + subject in order; (12) `## Issues` — anything the mover/fallback branches produced, or "none"; (13) end with `## Human verification (deferred to end of run)` — the consolidated Pixel 7 checklist for the Phase 55 batch (dedupe Plans 01/02's items): (a) a natural run on the post-retune debug APK feels like it dies at floors 5–7, not 3–4; (b) start-at-depth 20 feels unchanged from the v1.5/v1.6 rounds; (c) a floor-5–8 fight against a tier-2/3 foe no longer one-shots a full-HP hero; (d) a floor-16+ Herman / Drarl / Vampire / Djinni still feels like the Endgame (the deliberate shape); (e) if a retune landed: a Drake bite on floor 9–15 no longer takes a full-HP Magic User in one crit, while its breath still hurts; (f) the Oracle/rail copy for hits, traps and falls reads exactly as before. Commit the SUMMARY with plain `git commit -F` as `docs(54-03): complete audit-and-roster plan 03 — gates + phase roll-up summary` ending with the two trailers.
  </action>
  <verify>
    <automated>grep -c '^## Success criteria → proof' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Constants change table (before → after)' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Rung readouts' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Final AFTER vs BAND-01' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Roster table (TUNE-08)' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Audit cell count' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Parity' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Master hash re-check' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && grep -c '^## Human verification (deferred to end of run)' .planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - every one of the thirteen H2s listed in the action exists exactly once in `54-03-SUMMARY.md` (`grep -c` = 1 each): `## Success criteria → proof` (with rows SC1–SC5), `## Constants change table (before → after)`, `## Rung readouts`, `## Final AFTER vs BAND-01`, `## Roster table (TUNE-08)`, `## Audit cell count`, `## Parity`, `## Master hash re-check`, `## Gates`, `## Schema`, `## Commits`, `## Issues`, `## Human verification (deferred to end of run)` (the last line-group of the file, with at least five Pixel 7 checks)
    - `grep -c 'a1f4d0dc29782218d8e5aab65bc5989c33f917f0' 54-03-SUMMARY.md` >= 1; `grep -c 'MOVED SET (0)' 54-03-SUMMARY.md` >= 1; `grep -c 'FLAGGED: 65 rows' 54-03-SUMMARY.md` >= 1; `grep -c '| Herman |' 54-03-SUMMARY.md` >= 1; `grep -c 'USER RULING A' 54-03-SUMMARY.md` >= 1 and `grep -c 'USER RULING B' 54-03-SUMMARY.md` >= 1 (both rulings carried through; the solo-median row appears in the miss table only if still outside 5–7)
    - the pasted `npm test` tail shows `fail 0`; the pasted `build:www` shows exit 0; the pasted untouched-file diff is empty
    - the SUMMARY commit's last two message lines are the `Co-Authored-By:` and `Claude-Session:` trailers
  </acceptance_criteria>
  <done>The orchestrator can author 54-VERIFICATION.md from this file alone: every success criterion has a named proof, every number is pasted, every deferred device check is listed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (offline, single-process) | A report tool re-run, a content row (only under the declared exception), docs; no network, no installs, no persisted-schema change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-14 | Repudiation | an audit count recorded from a tool that ignores the new dial | high | mitigate | the fold is grep-verified (`foeDmgBonusFor(T, difficultyCurve(d))`) and numerically cross-checked before the re-run; stated in the addendum; `--curve` fallback written into the task |
| T-54-15 | Tampering | the band-level yardstick changed to lower the count | medium | mitigate | the committed output uses the default v17-p51 smoke; a v17-p54 run is informational only in the ledger; `Band levels:` line asserted |
| T-54-16 | Tampering | a dice trim landing outside the declared exception (chasing the median) | high | mitigate | `git diff --stat AFTER_SHA -- content/` must be empty or exactly the declared retune; the ≥ 100 % own-band rule computed and pasted; content-tables pins re-pinned with rationale, never deleted |
| T-54-17 | Tampering | test/parity/prototype-master.js.txt / comparables.js / fixtures | high | mitigate | master hash asserted; scan re-run byte-identical (or a declared +54 record); fixture diffs empty |
| T-54-18 | Repudiation | a roster decision recorded without evidence | medium | mitigate | both evidence cells pasted from the audit and the forced-20 transcript; decision vocabulary fixed; `moves tier` forbidden as a decision |
| T-54-19 | Information Disclosure | the committed audit output describing a different tree than HEAD | low | mitigate | `node tools/damage-curve-audit.mjs --rule=dice | diff -` against the committed rule=dice half is empty at commit time |
| T-54-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- The fold is verified and stated; the audit output is regenerated on the AFTER curve with the locked yardstick, two sections, and matches a fresh run.
- Every still-flagged rule=dice cell carries a fresh Disposition; the own-band ≥ 100 % list is computed; the single exception (if any) is applied as the smallest same-family change, declared in bestiary.js, re-pinned in content-tables, yardstick block regenerated, scan re-run byte-identical, four readouts re-taken, `v17-p54-after-smoke.json` refreshed.
- The roster table has four rows with two evidence cells each; the untaken forced-20 rungs are named; parity declared.
- Ledger H4s `#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)` and `#### Roster decision (TUNE-08)` present; addendum `### Phase 54 addendum (BAND-02 / TUNE-08, 2026-09-21)` present; FIXTURE-INVENTORY roster subsection present; ledger / yardstick / fixture-inventory tests green; v1.2 H2 last.
- `npm test` fail 0 and `build:www` exit 0 at every commit; master hash unchanged; frozen files untouched; every commit carries the two trailers; the phase roll-up SUMMARY carries every number the orchestrator's VERIFICATION needs.
</verification>

<success_criteria>
- SC4 (ROADMAP): the tier-3/5 roster decision is recorded per creature (stays / retuned in place — never a tier move) with the forced-20 untaken rungs named as a deliberate shape and any parity divergence declared.
- SC5: no flat-damage nerf chased the median — the only bestiary change is the declared still-≥100 %-one-shot exception (the Drake, USER RULING B); `npm test` and `build:www` green throughout.
- BAND-02's 65-cell hand-off is closed: cell count before/after recorded, every remaining row re-dispositioned, the audit provably read the new dial.
- The phase roll-up SUMMARY exists.
</success_criteria>

<artifacts_this_phase_produces>
See 54-01-PLAN.md's phase-wide table; this plan produces the rows marked 03:

| Artifact | Plan | What it is |
|---|---|---|
| `tools/damage-curve-audit-output.txt` — re-run on the AFTER curve (rule=whole + rule=dice, locked v17-p51 yardstick; post-retune if a retune landed) | 03 | the honest cell count |
| `content/BESTIARY-REBALANCE.md` — `### Phase 54 addendum (BAND-02 / TUNE-08, 2026-09-21)` with `#### Audit cell count — BEFORE … → AFTER …`, `#### Own-band mapping (the ≥ 100 % one-shot rule)`, `#### In-place retune (declared)`, `#### Still flagged after the four-band curve (rule=dice) — re-dispositioned`, `#### Cleared by the curve`, `#### Roster (TUNE-08)`; (retune branch) the regenerated `yardstick:after` block | 03 | dispositions + roster |
| `docs/DIFFICULTY-RETUNE.md` — `#### Damage-curve audit on the AFTER curve — the 65 cells (BAND-02)`, `#### Roster decision (TUNE-08)` | 03 | the ledger close |
| `test/parity/FIXTURE-INVENTORY.md` — `### Roster / one-shot retune (TUNE-08)` under the Phase 54 section | 03 | parity declaration |
| `content/bestiary.js` Drake `sp.dmg` 2d10+4 → 2d8+2 (USER RULING B) + `Phase 54 (TUNE-08 / BAND-02, 2026-09-21)` header paragraph; `test/unit/content-tables.test.js` re-pins (two Drake tests); `tools/initiative-fixture-scan-output.txt` re-run; `readouts/after-retune-{solo,party,start-depth-20,smoke}.txt`; `docs/class-pass/v17-p54-after-smoke.json` refreshed (post-retune) | 03 | the declared exception |
| `54-03-SUMMARY.md` — the phase roll-up | 03 | verifier-grade evidence |

Never touched: `test/parity/prototype-master.js.txt`, `test/parity/harness/comparables.js`, `tools/lib/tuning-bot.mjs`, `engine/difficulty.js` (this plan), `src/browser/`, `mazeworld.html`, `engine/saveState.js`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/54-four-band-retune-and-roster-decision/54-03-SUMMARY.md` when done (Task 3 writes it; it must end with `## Human verification (deferred to end of run)`).
</output>
