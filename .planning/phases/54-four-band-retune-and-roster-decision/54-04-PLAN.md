---
phase: 54-four-band-retune-and-roster-decision
plan: 04
type: execute
wave: 4
depends_on: ["54-01"]
files_modified:
  - tools/lib/tuning-bot.mjs
  - tools/lib/band-readout.mjs
  - tools/lib/class-matrix.mjs
  - tools/tune-difficulty.mjs
  - tools/tune-classes.mjs
  - test/unit/tuning-bot.test.js
  - test/unit/bot-tactics.test.js
  - test/unit/band-readout.test.js
  - test/unit/class-pass-usage.test.js
  - docs/DIFFICULTY-RETUNE.md
  - docs/class-pass/v17-p54-global-before-smoke.json
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-solo.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-party.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-start-depth-20.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-smoke.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/54-04-SUMMARY.md
autonomous: true
requirements: [BAND-01]
tags: [tuning-bot, fair-bot, readout, per-floor-survival, pace, class-identity, before-readout, ledger, user-ruling-d]

must_haves:
  truths:
    - "The tuning bot is a FAIR player before anything else is measured (USER RULING D `### Bot fairness first`): `BOT_DEFAULTS.potionThreshold` 0.5 -> 0.6, `fleeThreshold` 0.3 -> 0.4, `casterFleeThreshold` 0.5 -> 0.6, `campThreshold` stays 0.5 (camp when rations allow and HP < 0.5 — today's rule, kept), a pending Joiner is ACCEPTED when `state.party.length === 0` and declined otherwise (D-20 superseded); the Magic User policy plays BOTH modes — a DEFENSIVE cast (heal / ward / dome / status-sleep / disable) outranks a DAMAGE cast when the fight is going badly (`c.wp / c.maxWP < potionThreshold`, or >= 2 live foes with no KILL-tier one-shot castable) and OFFENSIVE otherwise; the Thief's backstab opener and flee bonus are exercised by the existing engine paths and COUNTED. This is the ONE bot-policy commit of the phase; every later number in Phase 54 compares to the BEFORE this plan measures with it"
    - "The readout reports per-floor deaths in THREE classes everywhere — `combat` / `dot` (trap, fall, leap, a -HP table-four dot: everything the map does) / `starvation-exhaustion` (the `starve` cause only) — replacing the hazard/starvation split (`spent by the dungeon itself` is a -HP dot death, NOT starvation); a `pace` block prints per floor reached: runs reaching it, mean hero level, mean gold on hand, mean armor AR, mean weapon cost (the weapon-tier proxy), mean maxWP, mean potions on hand, afraid triggers on that floor, deaths while afraid; a `class identity` block prints per CLASS POOL (Fighter / Thief / Magic User pooled over every race and sub — never per cell): n, p50 death depth (interpolated median), reach >= 5 / 10 / 20, damage taken per fight, rounds per fight, foe miss rate, defensive vs offensive casts, potions used, backstab openers, flees; all three blocks always-on in `tune-difficulty` and carried in `--json`; `tune-classes` cells and class rollups carry the identity columns; the survival block's VERDICT covers floors 1–12 only (user cut, 2026-09-21: ±8 on 1–10, ±3 on 11–12) — floors 13–20 print `tail` with dS only and reach-20 vs 3–5 % is reported, never pass/fail"
    - "A fresh BEFORE is taken on the UNTOUCHED engine (`git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html` empty at run time) with the fair bot: solo 200 / `--party` 200 (backgrounded) / class smoke (`docs/class-pass/v17-p54-global-before-smoke.json`) / `--seeds=50 --start-depth=20`, every transcript carrying the three new blocks, committed under `readouts/global-before-*.txt`"
    - "The ledger's Phase 54 H3 gains `#### Why floor bands failed (rungs 1–5)` (one paragraph: foe level = min(heroLevel, depth) made difficulty a function of the HERO's level; the knots scaled only lvl^2 + foe HP, never dice; foe count stepped at hero level 3; rung 5's constants committed at 1cb56c6 but never measured) and `#### Global model (USER RULING D) — the dial table` (every dial: engine hook, identity value, starting value, search bounds, search order, direction arrow), the governing principle verbatim (FOE-side power keys to DEPTH; HERO-side power keys to the hero's LEVEL, paced by HERO_SP_SCALE; the ECONOMY keys to depth through foe tier; the fit sets the gap), the class-pool ruling (race/sub/class cells are NOT meant to be equal — the spread is a recorded readout, never a target), and the roster note (tier 5 has 7 creatures; under the starting map it appears from floor 20; recorded as a v1.8 content candidate if the fit pulls tier 5 shallower than ~17)"
  artifacts:
    - path: "tools/lib/tuning-bot.mjs"
      provides: "BOT_DEFAULTS (0.6 / 0.4 / 0.6 / 0.5), the Joiner-accept-when-empty branch, chooseSpell's defensive/offensive mode, playRun's floorSnapshots + identity tallies on the run record"
    - path: "tools/lib/band-readout.mjs"
      provides: "DOT_CAUSES / STARVATION_CAUSES (three-class split), paceReadout / formatPaceReadout, classIdentityReadout / formatClassIdentityReadout, interpolatedMedian"
    - path: "tools/tune-difficulty.mjs + tools/tune-classes.mjs + tools/lib/class-matrix.mjs"
      provides: "the three blocks printed and in --json; identity columns per cell and per class rollup"
    - path: ".planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-{solo,party,start-depth-20,smoke}.txt + docs/class-pass/v17-p54-global-before-smoke.json"
      provides: "the fresh BEFORE every later Phase 54 number compares to"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "the two new H4s + the BEFORE (global model) section inside the Phase 54 H3"
  key_links:
    - from: "tools/lib/tuning-bot.mjs BOT_DEFAULTS + decideAction"
      to: "readouts/global-before-*.txt"
      via: "the Bot: line (flee=0.4/0.6(caster) potion<0.6 camp<0.5) printed in every transcript"
      why: "a moved instrument is unreadable unless the readout names the instrument it was taken with"
    - from: "playRun's per-run record (cls, floorSnapshots, identity tallies)"
      to: "band-readout's pace and class identity blocks"
      via: "pure aggregation over the results array (no engine access)"
      why: "54-06's fit tool rejects candidates on the class-pool constraints computed from these same fields — the instrument must exist before the fit"
  prohibitions:
    - "never edit engine/, content/, src/, mazeworld.html, test/parity/prototype-master.js.txt or test/parity/harness/comparables.js in this plan (the BEFORE is taken on the untouched engine)"
    - "never change the bot policy anywhere but this plan's Task 1 (one commit; later plans never touch tools/lib/tuning-bot.mjs's policy)"
    - "never lower a seed count or skip a readout; never treat a tool timeout as failure"
    - "never a race- or sub-class-keyed constant or readout target; class pools only"
---

<objective>
Make the tuning bot a fair player (USER RULING D `### Bot fairness first`), give the instrument the three readout blocks the global model's fit needs (three-class death split, per-floor pace, per-class-pool identity), take a fresh BEFORE on the untouched engine with that bot, and open the ledger's global-model record (why floor bands failed; the dial table with hooks / identity / start / bounds / order / direction; the governing principle; the class-pool ruling; the roster note).

State of the tree on entry (HEAD `5550002`): Plan 01 complete; the ladder (54-02) halted after rung 5's constants commit `1cb56c6` (its readouts never ran — `readouts/rung5-solo.txt` / `rung5-party.txt` are empty files, left as history). `engine/difficulty.js` holds the knot table at rung-5 values (FOE_GRACE_AT_2/3/4 0.32/0.44/0.31, Wall 0.25/0.27, Breakaway 0.27/0.29, Endgame 0.41/0.37, hazard 0.46/0.44/0.49/0.6, ENCOUNTER_DOT_CAP 12) — this plan does NOT touch it; 54-05 removes it. Last measured table (rung 4, solo 200, `readouts/rung4-solo.txt`): p_L 90.5 / 89.5 / 84.0 / 70.6 / 58.3 / 62.5 / 54.3 / 36.8 % on floors 1–8, reach ≥ 5 48 %, reach ≥ 10 2 %, reach-20 0 %; deaths ≈ 60 % combat (Werebeast, Drarl, Herman, Gremlin, Drake lead), 13.5 % traps/falls/leaps, 7.5 % `spent by the dungeon itself` (−HP dots), 8 % starvation. Bot: `flee=0.3/0.5(caster) potion<0.5 camp<0.5`, Joiners always declined.

Purpose: BAND-01's instrument (the per-floor survival curve of USER RULING C stands as the target; per the user's cut at plan approval the VERDICT applies its bands to floors 1–12 only — ±8 pts S_L on 1–10, ±3 on 11–12 — while floors 13–20 and reach-20 vs 3–5 % are the measured TAIL, printed with dS only) is re-read with a fair bot and extended with the pace / identity columns the fit's class-fairness constraint (54-06) and the ledger's per-floor tables (54-07) require. Decisions implemented: USER RULING D `### Bot fairness first` (quoted: "potion threshold 0.5 → 0.6, flee 0.3 → 0.4 (caster 0.5 → 0.6), accept a Joiner when the party is empty (D-20 superseded), camp when rations allow and HP < 0.5. One commit, then a fresh BEFORE readout … on the untouched engine — every later number compares to THAT"); the coordinator's readout additions (pace columns, potions on hand, afraid, three-class death split, class identity readout, class pools only — race/sub/class cells are NOT meant to be equal); the MU/Thief policy check ("verify the Magic User policy actually plays both modes … if it only does one, add the missing branch in the same commit and pin it. Same for the Thief: backstab opener used, flee bonus exercised").

Output: `tools/lib/tuning-bot.mjs` (policy + instrumentation), `tools/lib/band-readout.mjs` (+ tests), `tools/tune-difficulty.mjs`, `tools/tune-classes.mjs` + `tools/lib/class-matrix.mjs`, the four `readouts/global-before-*.txt`, `docs/class-pass/v17-p54-global-before-smoke.json`, the ledger H4s, `54-04-SUMMARY.md`.

Schema gate: no engine edit, no persisted-schema change. The run record's new fields (`cls`, `sub`, `race`, `floorSnapshots`, `identity`) are harness-only, never on `state`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-CONTEXT.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-01-SUMMARY.md
</context>

<ground_rules>
- `PHASE_DIR=.planning/phases/54-four-band-retune-and-roster-decision`; `SCRATCH` = the session scratchpad. Entry: `git rev-parse HEAD` = `5550002`, `git status --porcelain` empty.
- Commits: plain `git commit -F "$SCRATCH/msg.txt"`; every message ends with exactly the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Skip `state.advance-plan`.
- Gates at every commit: `npm test` (fail 0) and `npm run build:www` (exit 0); `boot:check` is NOT a gate; `node --test test/difficulty/` (bare dir) fails on this machine — always explicit file paths. Master hash `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every commit.
- Timing: launch `--party` FIRST in the background (`run_in_background`, `> "$PHASE_DIR/readouts/global-before-party.txt" 2>&1`, ~21 min), then solo (~1.5 min) → `--start-depth=20` (< 1 min) → the smoke (`--workers 4`, ~1.5 min); poll the party file for `^Outcome:`; never treat a tool timeout as failure.
- Context discipline: never cat a whole transcript; read `grep -A32 'Per-floor survival'`, `grep -A25 'Pace'`, `grep -A12 'Class identity'`, `grep -A6 'Death-depth distribution'`, the `Stuck:` / `Bot:` lines; the ledger splice embeds files verbatim.
- Every pin and ledger number is pasted from tool output, never hand-typed.
- No device / human checkpoints; the SUMMARY ends with `## Human verification (deferred to end of run)`.
</ground_rules>

<tasks>
<!-- planner-discipline-allow: accept: (state.party -->
<!-- planner-discipline-allow: "spent by the dungeon itself" -->
<!-- planner-discipline-allow: DOT_CAUSES -->
<!-- planner-discipline-allow: HAZARD_CAUSES -->

<task type="auto">
  <name>Task 1: The fair bot — thresholds 0.6 / 0.4 / 0.6 (camp 0.5 kept), accept a Joiner when the party is empty, the Magic User's defensive/offensive modes, Thief opener/flee counted; per-floor snapshots + identity tallies on the run record; every bot test re-pinned; one commit</name>
  <files>tools/lib/tuning-bot.mjs, test/unit/tuning-bot.test.js, test/unit/bot-tactics.test.js</files>
  <read_first>
    - tools/lib/tuning-bot.mjs lines 60-110 (`BOT_DEFAULTS`, `BOT_TACTICS`), 400-520 (`chooseSpell` and its tier table: KILL 400+ / DAMAGE 300+ / DISABLE 200+ / HEAL 100+ / WARD-OPENER 50 — today DAMAGE always outranks HEAL and DISABLE, so the MU is never defensive while a damage spell is castable), 700-830 (`hardFight`, `chooseCombatItem`, `chooseFieldItem`), 930-1100 (`decideAction`: the (a0)…(n) chain; line 1034 `resolveJoiner accept: false` // D-20), 1104-1290 (`makeTallies`, `tallyEvents`, `tallyUsage`, `observe`), 1283-1300 (`forceParty` — HARNESS-ONLY bypass, unchanged), 1344-1395 (`playRun` and the run record)
    - test/unit/tuning-bot.test.js: the tests named `D-06: caster threshold raises flee/parley to 0.5 …`, `D-05: potion in combat drinks below potionThreshold …`, `D-05: camp needs rations …`, `pending prompts: decline every Joiner …`, `D-20: forceParty …`, `HARN-02: Heal fires below potionThreshold once potions run out …`, `HARN-02: disables score only at 2+ live foes …`, `HARN-04: botLine emits the grep-stable Bot: line …`, `decideAction is unchanged for one in-combat and one exploration decision (no drift)` (bot-tactics.test.js) — every literal that encodes 0.3 / 0.5 / accept:false / the tier ranking is re-pinned from the NEW policy, never loosened
    - engine/combat.js lines 718-760 (the Thief backstab opener: `critBy = "backstab"`, the `backstab` event — the bot need not do anything for it to fire; count it) and engine/combat.js `flee` (`fleeRolled` event carries the Thief bonus in its breakdown — count `fled` per class)
    - content/spells.js `kind` values (heal / ward / status / stun / weaken / shrink / stupid / damage kinds) — the defensive set is `heal`, `ward`, `status`, `stun`, `weaken`, `shrink`, `stupid`; everything KILL/DAMAGE-tier is offensive
  </read_first>
  <action>
    (1) `BOT_DEFAULTS`: `potionThreshold` 0.6, `fleeThreshold` 0.4, `casterFleeThreshold` 0.6, `campThreshold` 0.5 (unchanged); update the D-05/D-06 comments to cite `USER RULING D (54-CONTEXT.md, 2026-09-21)`.

    (2) `decideAction` pending-Joiner branch: `return { type: "resolveJoiner", accept: (state.party?.length ?? 0) === 0 }` with a comment `USER RULING D: D-20 superseded — accept when the party is empty, decline when full`. `forceParty` stays as is (it is the `--party` distribution's own bypass; the acceptance path above is what a natural run now exercises).

    (3) `chooseSpell` gets a MODE: `defensive` when `c.maxWP > 0 && c.wp / c.maxWP < ctx.opts.potionThreshold`, OR when `liveFoes(state).length >= 2` and no KILL-tier (400+) spell is castable; else `offensive`. In `defensive` mode the tier scores are re-ranked ABOVE DAMAGE: HEAL 460 + expected heal (only when the ratio is below the potion threshold), DISABLE 420-450 by kind (stun 450, weaken 440 unless `C.weakened`, shrink 435, status 430, stupid 425 — only at >= 2 live foes), WARD 410 (`kind === "ward"`, only when `!c.ward` — in defensive mode the round-1 restriction is lifted), then the existing DAMAGE 300+ tier; in `offensive` mode the table is exactly today's. Document the mode in the JSDoc tier table. The returned pick MUST also expose `mode` on the bot context (`ctx.lastSpellMode = mode`) so the tallies can count defensive vs offensive casts.

    (4) `playRun` instrumentation (report-only; `state` is never mutated by it): the run record gains `cls: state.c.cls`, `sub: state.c.sub`, `race: state.c.race`; `floorSnapshots: [{ depth, level, gold, ar, weaponCost, maxWP, potions, afraidTriggers, diedAfraid }]` — one entry per floor the run reached, captured on every `floorChanged` event (for the floor being LEFT: snapshot taken just before the depth changes — implement by watching `state.floor.depth` change across the dispatch: when `after.floor.depth !== before.floor.depth`, push the snapshot of `before`) plus a final snapshot of the death/stuck floor when the loop ends; `weaponCost` = `WEAPONS[state.c.weapon]?.cost ?? 0` (import WEAPONS from content); `afraidTriggers` counts `afraid`-family events on that floor (grep engine/events.js / engine/combat.js for the phobia-trigger event name — today's `fearArmed`/`afraid` event type; pin the exact name in the test), `diedAfraid` = the run died while `state.combat?.afraid > 0` on that floor. `identity` tallies (accumulated over the run in `tallies`): `fights` (encounterStarted count), `rounds` (foeTurn visits — count `struckByFoe` + `foeMissed` swings is NOT rounds; count the number of `applyAction` dispatches of type `attack`/`cast`/`useAbility`/`flee`/`parley` while `state.combat` is live — one per hero turn — document the definition), `dmgTaken` (sum of `struckByFoe.dmg` + `foeBolted.dmg` on the hero), `foeSwings` (struckByFoe + foeMissed on the hero), `foeMisses` (foeMissed on the hero), `castsDefensive` / `castsOffensive` (per `ctx.lastSpellMode` at each `cast` dispatch), `potionsUsed` (drinkPotion dispatches + useItem heal picks), `backstabs` (`backstab` events), `flees` (`fled` events). All additive; existing fields untouched.

    (5) Tests (`test/unit/tuning-bot.test.js`): re-pin `D-06` (0.4 / 0.6: the wp:16/maxWP:40 = 0.4 boundary cases move — recompute each fixture's wp so the test still exercises both sides of each threshold), `D-05` potion (0.6), `pending prompts` → rename to `USER RULING D: a pending Joiner is accepted when the party is empty and declined when full` with both cases, `botLine` (the new `flee=0.4/0.6(caster)  potion<0.6  camp<0.5` string), new `USER RULING D: chooseSpell defensive mode — below the potion threshold Heal outranks Fireball; at 2 live foes with no one-shot, a disable outranks damage; above threshold with 1 foe the damage pick is unchanged` (three assertions on synthetic states), new `USER RULING D: playRun records cls/sub/race, one floorSnapshot per floor reached, and identity tallies (fights/rounds/dmgTaken/foeSwings/foeMisses/castsDefensive/castsOffensive/potionsUsed/backstabs/flees) — a seeded 200-action Thief run counts >= 1 backstab` (use `playRun(seed, { maxActions: 300, force: <a Thief-forcing option if newRun's force supports it — else pick a seed that rolls a Thief> })`); `bot-tactics.test.js`'s no-drift test re-pinned from the new policy's actual output (paste, never type).

    (6) Gates, then commit `feat(54-04): fair bot — potion 0.6 / flee 0.4 (caster 0.6) / camp 0.5 kept, accept a Joiner when the party is empty (D-20 superseded), MU defensive+offensive modes, Thief opener/flee counted; per-floor snapshots + identity tallies on the run record (USER RULING D)` with the two trailers. Record `BOT_SHA`.
  </action>
  <verify>
    <automated>node --test test/unit/tuning-bot.test.js test/unit/bot-tactics.test.js test/unit/bot-buy-policy.test.js test/unit/class-pass-usage.test.js && node -e "import('./tools/lib/tuning-bot.mjs').then(m=>{const d=m.BOT_DEFAULTS;console.log(d.potionThreshold,d.fleeThreshold,d.casterFleeThreshold,d.campThreshold)})" && git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html test/parity/ | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - the verify `node -e` prints exactly `0.6 0.4 0.6 0.5`
    - `grep -c 'accept: (state.party' tools/lib/tuning-bot.mjs` >= 1 and `grep -c 'accept: false }; // D-20' tools/lib/tuning-bot.mjs` == 0
    - `grep -c 'defensive' tools/lib/tuning-bot.mjs` >= 4 (mode computation, tier re-rank, JSDoc, tally) and `grep -c 'lastSpellMode' tools/lib/tuning-bot.mjs` >= 2
    - `grep -c 'floorSnapshots' tools/lib/tuning-bot.mjs` >= 2 and `grep -c 'castsDefensive\|castsOffensive\|potionsUsed\|backstabs' tools/lib/tuning-bot.mjs` >= 4
    - `node --test test/unit/tuning-bot.test.js` prints `# fail 0` and its output contains the strings `USER RULING D: a pending Joiner is accepted when the party is empty`, `USER RULING D: chooseSpell defensive mode`, `USER RULING D: playRun records cls/sub/race`
    - `git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html test/parity/` is empty; `npm test` fail 0; `build:www` exit 0; the commit message carries the two trailers
  </acceptance_criteria>
  <done>The bot is the fair instrument USER RULING D specifies, plays both MU modes, counts the Thief's opener and flees, and every run record carries the per-floor and identity fields the readouts and the fit consume — pinned, one commit.</done>
</task>

<task type="auto">
  <name>Task 2: The readout — three-class death split (combat / dot / starvation-exhaustion), the `Pace` block, the `Class identity` block (class pools only), wired into tune-difficulty (print + --json) and tune-classes (cells + class rollups); tests; one commit</name>
  <files>tools/lib/band-readout.mjs, tools/tune-difficulty.mjs, tools/tune-classes.mjs, tools/lib/class-matrix.mjs, test/unit/band-readout.test.js, test/unit/class-pass-usage.test.js</files>
  <read_first>
    - tools/lib/band-readout.mjs WHOLE FILE (`HAZARD_CAUSES` / `STARVATION_CAUSES` at lines 183-187 — `spent by the dungeon itself` is wrongly filed under starvation; `survivalReadout` lines 208-292 with the `hazard / starvation / combat` split; `formatSurvivalReadout` line 350; the 1-dp rounding convention)
    - content/epitaphs.js lines 158-178 (`CAUSE_TEXT`: `starve` = "starved in the dark" is the ONLY starvation-exhaustion cause; `trap` / `fall` / `gorge` / `maze` ("spent by the dungeon itself" — the table-four -HP dot and any other `die(state, "maze")`) are the DOT class; `combat` "cut down by a {foe}"; every other cause (teleport / backfire / summon / quake / potion / insanity / poison / entombed) is filed under `other` and printed but never chased)
    - tools/tune-difficulty.mjs lines 120-170 (`printReport` block order) and 214-235 (the `--json` object)
    - tools/tune-classes.mjs + tools/lib/class-matrix.mjs (the cell shape `{ rank, cls, sub, race, n, …, meanLevel, … topCauses }`, `summarizeRows`, `rollups.byClass`) and test/unit/class-pass-usage.test.js (the cell-shape pins to extend)
    - tools/lib/tuning-bot.mjs (Task 1's run-record fields: `cls`, `floorSnapshots`, `identity`)
  </read_first>
  <action>
    (1) `band-readout.mjs`: replace `HAZARD_CAUSES`/`STARVATION_CAUSES` with `export const DOT_CAUSES = ["undone by a trap", "fell off a wall", "came up short on a leap", "spent by the dungeon itself"]`, `export const STARVATION_CAUSES = ["starved in the dark"]`, `export const COMBAT_CAUSE_PREFIX = "cut down by a"`; `survivalReadout`'s per-floor split becomes `combat / dot / starvation / other` (four counters; `other` = every cause matching none of the three — printed as a fourth number, never chased); the printed line reads `deaths=N (combat a / dot b / starvation-exhaustion c / other d)`. The VERDICT bands change per the user's cut (2026-09-21): `SURVIVAL_PASS = { shallowFloors: [1, 10], shallowTolerance: 8, deepFloors: [11, 12], deepTolerance: 3, tailFloors: [13, 20], reach20Band: [3.0, 5.0] }` — floors 1–12 print PASS/MISS and feed the `verdict:` line; floors 13–20 print `tail` (dS shown, never PASS/MISS, never in the verdict); the `reach-20:` line prints `<x> % (band 3.0-5.0 %, reported — tail)` and is NOT part of the verdict; floors 21+ stay `info`. Every other field is byte-identical.

    (2) `export function interpolatedMedian(values)` (sorted; even n → mean of the two middle values; empty → null) and `export function paceReadout(results)` → `{ floors: [{ floor, n, meanLevel, meanGold, meanAr, meanWeaponCost, meanMaxWP, meanPotions, afraidTriggers, diedAfraid }] }` aggregated from every result's `floorSnapshots` (one row per floor with n >= 1; means to 2 dp; stuck runs included — they reached those floors), and `formatPaceReadout(r)` printing a header `Pace (per floor reached — mean hero level / gold / AR / weapon cost / maxWP / potions; afraid triggers, died afraid):` then one line per floor `L=1  n=200  level=1.00  gold=61.2  ar=1.2  weapon=63.0  maxWP=41.9  potions=1.4  afraid=3  diedAfraid=0`.

    (3) `export function classIdentityReadout(results)` → per CLASS POOL (`Fighter`, `Thief`, `Magic User`, in that order, pooled over every race and sub): `{ cls, n, p50 (interpolatedMedian of completed deathDepth), reach5, reach10, reach20 (% of runs), dmgTakenPerFight, roundsPerFight, foeMissRate (foeMisses / foeSwings), castsDefensive, castsOffensive, potionsPerRun, backstabsPerRun, fleesPerRun }` and `formatClassIdentityReadout(r)` printing `Class identity (class pools only — Fighter = ABSORB, Thief = AVOID, Magic User = CHOOSE; race/sub cells are not targets):` then one line per class. Also `export function classSpreadReadout(smokeCells)` for tune-classes: per class the min / max cell `p50Depth` (named `sub/race`) and the reach5 range — a RECORDED spread, no verdict.

    (4) `tune-difficulty.mjs`: print the three blocks always-on after the `Per-floor survival` block and before `Outcome:` (order: survival → pace → class identity); `--json` gains `pace` and `classIdentity` keys. `tune-classes.mjs` / `class-matrix.mjs`: every cell gains `dmgTakenPerFight`, `roundsPerFight`, `foeMissRate`, `castsDefensive`, `castsOffensive`, `potionsPerRun` (run-weighted means over the cell's rows); `rollups.byClass` gains the same plus `spread: { p50Min: { value, cell }, p50Max: { value, cell }, reach5Min, reach5Max }`; the stdout matrix prints a `Class spread (recorded, not a target)` block. `meta` untouched except `bot` (which now carries the new thresholds by construction).

    (5) Tests: `band-readout.test.js` — re-pin the split test (a synthetic results set with one death per class incl. `spent by the dungeon itself` → dot), re-pin the verdict test to the 1–12 bands (`a floor-13 S_L 30 points off prints tail and never enters the verdict; a floor-12 S_L 4 points off is a MISS; reach-20 0 % is reported and never in the verdict`), new `paceReadout aggregates floorSnapshots per floor and interpolatedMedian is the mean of the two middle values on even n`, new `classIdentityReadout pools by class only (a Fighter Knight and a Fighter Bard land in ONE row) and computes dmgTakenPerFight / roundsPerFight / foeMissRate from the identity tallies`, and extend the source-wiring test (`tune-difficulty.mjs` source contains `formatPaceReadout(` and `formatClassIdentityReadout(`; the `--json` literal contains `pace:` and `classIdentity:`); `class-pass-usage.test.js` — the cell-shape pin gains the six identity keys and the byClass `spread` shape.

    (6) Gates, then commit `feat(54-04): readouts — three-class death split (combat / dot / starvation-exhaustion), per-floor Pace block, Class identity block (class pools only) in tune-difficulty + --json; identity columns + recorded spread in tune-classes (USER RULING D)` with the two trailers.
  </action>
  <verify>
    <automated>node --test test/unit/band-readout.test.js test/unit/class-pass-usage.test.js test/unit/tuning-bot.test.js && node tools/tune-difficulty.mjs --seeds=6 --max-actions=400 | grep -c 'Per-floor survival\|^Pace (\|^Class identity (' && node tools/tune-difficulty.mjs --seeds=4 --max-actions=300 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);console.log(Object.keys(j).includes('pace'),Object.keys(j).includes('classIdentity'),j.survival.floors[0].dot!==undefined,j.survival.floors[0].starvation!==undefined)})"</automated>
  </verify>
  <acceptance_criteria>
    - the first verify grep prints `3`; the JSON check prints `true true true true`; `node -e "import('./tools/lib/band-readout.mjs').then(m=>console.log(JSON.stringify(m.SURVIVAL_PASS)))"` prints an object with `deepFloors:[11,12]` and `tailFloors:[13,20]`; a `verdict:` line never names a floor >= 13 (pinned by the re-pinned verdict test)
    - `grep -c '"spent by the dungeon itself"' tools/lib/band-readout.mjs` == 1 and it is inside the `DOT_CAUSES` array (`grep -c 'DOT_CAUSES' tools/lib/band-readout.mjs` >= 2); `grep -c 'HAZARD_CAUSES' tools/lib/band-readout.mjs` == 0
    - `grep -c 'starvation-exhaustion' tools/lib/band-readout.mjs` >= 1 (the printed label)
    - `grep -c 'class pools only' tools/lib/band-readout.mjs` >= 1; the class identity block never keys on `race` or `sub` for its rows (a `node -e` over a synthetic result set with two Fighter subs prints 1 Fighter row — pasted in the SUMMARY)
    - `node tools/tune-classes.mjs --seeds 1 --workers 2 --sub Knight --race Human --json` shows a cell with `dmgTakenPerFight`, `roundsPerFight`, `foeMissRate`, `castsDefensive`, `castsOffensive`, `potionsPerRun` keys and `rollups.byClass.Fighter.spread`
    - `git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html test/parity/` empty; `npm test` fail 0; `build:www` exit 0; two trailers
  </acceptance_criteria>
  <done>Every transcript now carries the three-class death split, the per-floor pace table and the per-class-pool identity table; the class matrix records the cell spread without a target.</done>
</task>

<task type="auto">
  <name>Task 3: The fresh BEFORE on the untouched engine (solo / party backgrounded / class smoke / depth-20 slice, all with the fair bot and the three blocks) + the ledger's global-model opener (why floor bands failed; the dial table with hooks / identity / start / bounds / order / direction; the governing principle; class pools; roster note) + the verifier-grade SUMMARY</name>
  <files>.planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-solo.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-party.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-start-depth-20.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-smoke.txt, docs/class-pass/v17-p54-global-before-smoke.json, docs/DIFFICULTY-RETUNE.md, .planning/phases/54-four-band-retune-and-roster-decision/54-04-SUMMARY.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md lines 3251-3300 (the Phase 54 H3 opener), the `#### Rung 4 — commit 09b944b …` section (the LAST rung section; the new H4s go AFTER it and BEFORE `## v1.2 retune (Phase 27) — TUNE-05..07`), lines 1177-1200 (the v1.7 H2 — must stay immediately before the v1.2 H2 in H2 order)
    - test/unit/difficulty-retune-ledger.test.js lines 120-135 (the v1.2 H2 must remain the LAST H2) and test/unit/class-pass-ledger.test.js (no v17 pin — confirm nothing asserts on `v17-p54-*` files)
    - 54-CONTEXT.md `## USER RULING D` (the system analysis, the remove list, the dial table, the roster note) and this plan's objective
    - .planning/phases/54-four-band-retune-and-roster-decision/54-06-PLAN.md `## The dial table` (the source of truth for every dial's hook / identity / start / bounds / order / direction — copy it verbatim into the ledger; do not restate from memory)
  </read_first>
  <action>
    Step 1 — BEFORE readouts on Task 2's commit (tree clean; `git diff --stat 5550002 -- engine/ content/` empty — paste it): background `node tools/tune-difficulty.mjs --seeds=200 --party > "$PHASE_DIR/readouts/global-before-party.txt" 2>&1`; then `node tools/tune-difficulty.mjs --seeds=200 > "$PHASE_DIR/readouts/global-before-solo.txt" 2>&1`; `node tools/tune-difficulty.mjs --seeds=50 --start-depth=20 > "$PHASE_DIR/readouts/global-before-start-depth-20.txt" 2>&1`; `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p54-global-before-smoke.json > "$PHASE_DIR/readouts/global-before-smoke.txt" 2>&1`; poll the party file for `^Outcome:`. If any SOLO run hits the 20,000 cap (`Stuck: N of 200` with N > 0 in the solo transcript), re-run solo AND party with `--max-actions=40000` and record the flag in the ledger's Parameters bullets (party runs are expected to have ~25 % stuck — own bucket, normal).

    Step 2 — the ledger. Splice, after the `#### Rung 4` section and before `## v1.2 retune`, in this order:
    (a) `#### Why floor bands failed (rungs 1–5) — USER RULING D (2026-09-21)` — ONE paragraph quoting 54-CONTEXT.md's `### The system, as measured` (foe level = min(heroLevel, depth) → difficulty tracked the HERO's level; every knot scaled only lvl^2 + foe HP while dice stayed untouched → the ladder saturated at floor 4 (55 → 63 → 71 % then flat, floors 1–3 byte-identical across rungs 3–4); foe count stepped 2 → 3 at hero level 3; no regeneration; hazards ≈ 25 % and starvation ≈ 10–15 % of deaths), then the sentence: "Rung 5's constants were committed at `1cb56c6` (F2/F3/F4 0.32/0.44/0.31; Wall 0.25/0.27; Breakaway 0.27/0.29; Endgame 0.41/0.37; ENCOUNTER_DOT_CAP 13→12) and its readouts were never run — `readouts/rung5-solo.txt` / `rung5-party.txt` are empty by design; the knot table is removed in 54-05."
    (b) `#### Global model (USER RULING D) — the dial table` — the governing principle verbatim: "FOE-side power keys to DEPTH; HERO-side power keys to the hero's LEVEL (paced by HERO_SP_SCALE); the ECONOMY keys to depth through foe tier (LOOT_SCALE); the fit sets the gap between the ramps." Then the table `| Dial | Engine hook | Identity value | Starting value | Search bounds | Search order | Direction (↑ =) |` — copy EVERY row from 54-06-PLAN.md's `## The dial table` verbatim, the `available, canon` rows included (TIER_SPREAD, FLEE_NEED_MOD, PARLEY_NEED_MOD, DOT_MIX.harm/loot/help, STARTING_GOLD, STARTING_POTION_BONUS, CLASS_MITIGATION's three rows — with the note that the base Thief has no innate evasion today (only Acrobat / pool abilities), so `evasion: -1` is the first manual candidate). Below the table: the class-pool ruling ("race / class / sub-class combinations are NOT meant to be equal — the random draw is part of the design; the fairness and identity constraints apply ONLY to the three class pools; the per-cell spread is a recorded readout, never a target; never a race- or sub-class-keyed constant"), the roster note ("54 creatures, 6 types × 5 tiers (12/11/12/12/7); tier 5 has 7 creatures and under the starting FOE_LEVEL map (0.6 + 0.2·depth: tier 1 on 1–4, 2 on 5–9, 3 on 10–14, 4 on 15–19, 5 from 20) it appears from floor 20 only; recorded as a v1.8 content candidate ('Endgame roster: +5–8 tier-5 creatures, ≥ 1 per type') if the fit pulls tier 5 shallower than ~17"), and the Ruling C target restated as STANDING (pointer to `#### Target — the per-floor survival curve (USER RULING C, 2026-09-21)`) with the user's three cuts at plan approval (2026-09-21) recorded under a `**Cuts at plan approval:**` bullet list: (1) the automated fit searches the CORE 10 coordinates only (named, in order); every other dial is implemented and HELD at its start value — "held (available)"; (2) MAZE_SIZE is cut from the phase — "available, not implemented (v1.8 candidate)"; (3) the verdict and the fit's objective cover floors 1–12 (±8 on 1–10, ±3 on 11–12); floors 13–20 and reach-20 vs 3–5 % are the TAIL, measured at AFTER with a 1,000-seed solo and a `--start-depth=10` slice, recorded with dS only.
    (c) `#### BEFORE (global model) — commit <40-hex of Task 2's commit> (fair bot; untouched engine at 1cb56c6's knot values)` — Parameters bullets (the four commands, the `Bot:` line verbatim, `--max-actions` if raised), then the four fenced transcripts' key blocks (solo: `Death-depth distribution`, `Death-cause breakdown` top 12, `Per-floor survival` whole block, `Pace` whole block, `Class identity` whole block, `Stuck:`, `Bot:`; party: the survival + class identity blocks + `Stuck:`; slice: the survival block; smoke: the pooled line + the `Class spread` block), a compact table `| Floor | p_L | S_L | target S_L | dS | combat/dot/starve | level | gold | maxWP | verdict |` for floors 1–20 (solo), and `**Reading:**` — three sentences: what the fair bot changed vs rung 4 (p50, reach ≥ 5, the death-class shares), the pace at floors 4/9/14 (level, gold) vs the target pace (level 2 by ~4, 3 by ~9, 4 by ~14, 5 by ~18), and the class-pool p50 spread.

    Step 3 — gates: `node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js test/unit/band-readout.test.js` green; `npm test` fail 0; `build:www` exit 0; master hash; `git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html test/parity/` empty. Commit `docs(54-04): fresh BEFORE with the fair bot (solo / --party / class smoke / depth-20 slice, three-class split + pace + class identity) + ledger: why floor bands failed, the global-model dial table, the governing principle, class pools, roster note (USER RULING D, BAND-01)` with the two trailers.

    Step 4 — `$PHASE_DIR/54-04-SUMMARY.md` (verifier-grade; the frontmatter in the 54-01 SUMMARY's shape with `requirements-completed: [BAND-01]`): `## Success criteria → proof` (the bot policy table old → new with the test names; the readout blocks with the test names; the BEFORE commands + commit); `## Bot policy change table`; `## BEFORE (fair bot) — key numbers` (p50/p90, reach 5/10/20, per-floor p_L 1–10, S_L, the death-class shares, class-pool p50s, pace at 4/9/14, party p50 + stuck, slice p_20, smoke pooled mean); `## Test names` (every new/re-pinned test); `## Gates` (npm test totals, build:www, master hash, the empty engine diff); `## Schema` (none); `## Commits` (three SHAs); `## Deviations`; `## Next Phase Readiness` (54-05 removes the knot table and lands the model at identity; the dial table in the ledger is the spec); `## Human verification (deferred to end of run)` (the Pixel 7 batch: nothing new to look at this plan — instrument only; say so).
  </action>
  <verify>
    <automated>for f in solo party start-depth-20 smoke; do test -s ".planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-$f.txt" && echo "ok $f"; done; grep -c 'Per-floor survival\|^Pace (\|^Class identity (' .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-solo.txt; grep -o 'Bot: .*' .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-before-solo.txt; test -s docs/class-pass/v17-p54-global-before-smoke.json && node -e "const j=require('./docs/class-pass/v17-p54-global-before-smoke.json');console.log(j.meta.commit,j.cells.length,Object.keys(j.rollups.byClass))"; grep -c '^#### Why floor bands failed (rungs 1–5)\|^#### Global model (USER RULING D) — the dial table\|^#### BEFORE (global model) — commit ' docs/DIFFICULTY-RETUNE.md; node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js; git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - the four `ok …` lines print; the solo transcript's block grep prints `3`; the `Bot:` line contains `flee=0.4/0.6(caster)`, `potion<0.6` and `camp<0.5`; the smoke JSON's `meta.commit` equals Task 2's short SHA and `cells.length` is 143
    - the ledger heading grep prints `3`; the dial-table section contains every dial name that 54-06-PLAN.md's `## The dial table` lists (`for k in FOE_LEVEL HERO_SP_SCALE LOOT_SCALE FOE_HIT_SCALE FOE_HP_SCALE FOE_COUNT_SKEW ROUND_DAMAGE_CEILING HERO_HP_SCALE HERO_REGEN_PER_FLOOR CAMP_HEAL_FRACTION DOT_HP_FRACTION ENCOUNTER_DOTS HAZARD_SCALE FOOD_CLOCK ABILITY_THREAT STORE_TIER FIGHT_SHARE WANDER_RATE FOE_ACCURACY MAZE_SIZE DARK_BLOBS DARK_RADIUS TIER_SPREAD FLEE_NEED_MOD PARLEY_NEED_MOD STARTING_GOLD STARTING_POTION_BONUS CLASS_MITIGATION; do grep -c "$k" docs/DIFFICULTY-RETUNE.md; done` — every count >= 1) and the strings `FOE-side power keys to DEPTH`, `class pools`, `v1.8 content candidate`
    - the BEFORE section embeds the solo `Per-floor survival` block verbatim (`grep -c 'L=1  reached=200' docs/DIFFICULTY-RETUNE.md` is one more than at HEAD 5550002) and the `Pace (` and `Class identity (` headers
    - `git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html test/parity/` empty at the readout commit; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm test` fail 0; `build:www` exit 0; every commit carries the two trailers
    - `54-04-SUMMARY.md` exists, ends with a `## Human verification (deferred to end of run)` section, and names every test added/re-pinned and the three commit SHAs
  </acceptance_criteria>
  <done>The fair bot's BEFORE is on disk with the three new blocks; the ledger records why floor bands failed and the complete global-model dial table with directions; the SUMMARY carries verifier-grade evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tools → repo | `tune-*.mjs` / `band-readout.mjs` are dev-only Node scripts, never shipped (`build-www.mjs`'s copy list excludes `tools/`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-04-01 | Tampering | readout transcripts as evidence | low | mitigate | every transcript is taken on a committed tree (`git diff --stat` pasted) and the smoke's `meta.commit` pins the SHA |
| T-54-04-02 | Repudiation | bot policy drift | low | mitigate | the `Bot:` line names every threshold in every transcript; the policy is pinned by unit tests and changed in exactly one commit |
| T-54-SC | Tampering | npm/pip/cargo installs | high | accept | no package installs in this plan (zero new dependencies; `git diff --stat 5550002 -- package.json package-lock.json` empty) |
</threat_model>

<verification>
- `node --test test/unit/tuning-bot.test.js test/unit/bot-tactics.test.js test/unit/band-readout.test.js test/unit/class-pass-usage.test.js test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js` green
- `npm test` fail 0 and `npm run build:www` exit 0 at each of the three commits; master hash unchanged
- `git diff --stat 5550002 -- engine/ content/ src/ mazeworld.html test/parity/` empty at the plan's last commit
- the four `readouts/global-before-*.txt` transcripts exist, carry the three blocks, and their `Bot:` line names the new thresholds
</verification>

<success_criteria>
- Bot policy per USER RULING D landed in one commit with tests re-pinned (0.6 / 0.4 / 0.6 / 0.5, Joiner accepted when the party is empty, MU both modes, Thief opener + flees counted)
- Readout: three-class death split, Pace block, Class identity block (pools only), cell spread recorded — printed and in `--json`; tune-classes carries the identity columns
- Fresh BEFORE (solo / party / smoke / depth-20) committed on the untouched engine with the fair bot
- Ledger: `#### Why floor bands failed (rungs 1–5)` + `#### Global model (USER RULING D) — the dial table` (every dial with hook / identity / start / bounds / order / direction) + `#### BEFORE (global model)`; the v1.2 H2 still last
- SUMMARY with verifier-grade evidence, ending with `## Human verification (deferred to end of run)`
</success_criteria>

<artifacts_this_phase_produces>
- `tools/lib/tuning-bot.mjs` — fair-bot policy + run-record instrumentation (`cls`/`sub`/`race`, `floorSnapshots`, `identity` tallies)
- `tools/lib/band-readout.mjs` — `DOT_CAUSES` / `STARVATION_CAUSES`, `interpolatedMedian`, `paceReadout` / `formatPaceReadout`, `classIdentityReadout` / `formatClassIdentityReadout`, `classSpreadReadout`
- `tools/tune-difficulty.mjs`, `tools/tune-classes.mjs`, `tools/lib/class-matrix.mjs` — the blocks printed and in JSON; identity columns + recorded spread
- `test/unit/tuning-bot.test.js`, `test/unit/bot-tactics.test.js`, `test/unit/band-readout.test.js`, `test/unit/class-pass-usage.test.js` — re-pinned + new tests
- `readouts/global-before-{solo,party,start-depth-20,smoke}.txt`, `docs/class-pass/v17-p54-global-before-smoke.json` — the fresh BEFORE
- `docs/DIFFICULTY-RETUNE.md` — the three new H4s inside the Phase 54 H3
- `54-04-SUMMARY.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/54-four-band-retune-and-roster-decision/54-04-SUMMARY.md` when done (verifier-grade; ends with `## Human verification (deferred to end of run)`).
</output>