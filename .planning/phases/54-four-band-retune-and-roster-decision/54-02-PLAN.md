---
phase: 54-four-band-retune-and-roster-decision
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - engine/difficulty.js
  - test/difficulty/difficulty.test.js
  - test/unit/combat-scaling.test.js
  - test/determinism/foe-abilities.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - tools/initiative-fixture-scan-output.txt
  - docs/DIFFICULTY-RETUNE.md
  - docs/class-pass/v17-p54-rung1-smoke.json
  - docs/class-pass/v17-p54-rung2-smoke.json
  - docs/class-pass/v17-p54-rung3-smoke.json
  - docs/class-pass/v17-p54-rung4-smoke.json
  - docs/class-pass/v17-p54-after-smoke.json
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-solo.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-party.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-start-depth-20.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-smoke.txt
autonomous: true
requirements: [BAND-02, BAND-03, BAND-01]
tags: [difficulty-curve, four-band, iteration-ladder, bot-readout, measurement-gate, parity, ledger]

must_haves:
  truths:
    - "The ladder runs at most FOUR rungs from Plan 01's scaffold commit (COMMIT_B); each rung moves ONLY Phase 54 band constants in `engine/difficulty.js` or — on a Filter rung, USER RULING A (2026-09-21) — the EXISTING Phase 27 dials `FOE_GRACE_AT_2` (>= 0.35) / `HAZARD_SCALE_AT_START` (never floor 1: FOE_GRACE_AT_1 / HAZARD_FROM_DEPTH; never a NEW floor 1–4 dial: DENSITY_CANON_THROUGH_DEPTH / DARK_HOLD_THROUGH_DEPTH / ENCOUNTER_DOT_* / DARK_*; never FOE_GRACE_CANON_FROM_DEPTH / HAZARD_CANON_FROM_DEPTH — the ramps still end at exactly 1.0 at 5; never COMBAT_SCALE_FROM_DEPTH / *_MAX / *_SOFT_K; never a bestiary number), every moved constant's JSDoc cites the readout and the rung, the 5..15 pins are re-pinned to measured values (never loosened), and the rung is committed green (`npm test` fail 0, `build:www` exit 0) BEFORE its readouts are taken on that commit — BAND-02 'every dial change cited against a bot readout' (54-CONTEXT Area 2, constraint 11)"
    - "EVERY rung gets the FULL readout on its constants commit: `node tools/tune-difficulty.mjs --seeds=200` (solo), `node tools/tune-difficulty.mjs --seeds=200 --party` (background, output to `readouts/rungN-party.txt`, polled), `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p54-rungN-smoke.json`, `node tools/tune-difficulty.mjs --seeds=50 --start-depth=20` — all four transcripts committed verbatim under `#### Rung N — commit …` in the ledger (USER OVERRIDE, 54-CONTEXT Area 2 Q1; constraint 6)"
    - "On EVERY rung the FLOOR-1-untouched proof holds: the `--start-depth=20` transcript is byte-identical to `readouts/before-start-depth-20.txt` (`diff` empty — the deep-lethality yardstick), `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` is empty (Part B on the floor-1 fixtures), and the `Phase 54 (BAND-01) floor-1 parity` pin is green; solo / party `reach: >=5` is RECORDED per rung as a target-side number (33.0 % / 55.3 % at BEFORE; it moves only when a Filter rung lands — USER RULING A). If the slice diff or the scan diff is non-empty the executor STOPS, finds the cause, fixes it and re-measures before the next rung (BAND-01; constraint 6)"
    - "A rung that would touch floor 1 (FOE_GRACE_AT_1, HAZARD_FROM_DEPTH, floor-1 dots/dark), add a NEW floor 1–4 dial (any density/dark change on 1–4), push FOE_GRACE_AT_2 below 0.35 or HAZARD_SCALE_AT_START below 0.3, or set WALL_FOE_POWER_AT_START below graceFor(4) is NOT taken — recorded as untaken with the reason 'floor-1 parity' / 'no new Filter dial' / 'Filter ceiling (USER RULING A)' in the miss table and surfaced in the SUMMARY (constraint 3)"
    - "The ledger's Phase 54 H3 gains `#### Rung N — commit <40-hex> (<dial summary>)` per rung (constants Old→New table, curve 5..16 table, four transcripts, reading table incl. the class-cell collapse check, 'what to turn next'), then `#### Change table (BAND-03)` with ONE ROW PER CONSTANT before → after (the 14 band constants plus every Phase 21/27 dial listed as unchanged), `#### Miss table (BAND-03)` (every BAND-01 number not met → the untaken rung + the reason; the solo median row appears ONLY if the median is still outside 5–7 after the ladder, naming the untaken Filter notch — it is a live target under USER RULING A), `#### Depth-20 slice — identity proof (BAND-01)` and `#### Parity — MOVED SET (0) (BAND-02)`"
    - "The moved parity set is MEASURED on the final curve — `tools/initiative-fixture-scan.mjs` re-run (Part B byte-identical to the committed output), `node --test test/parity/` green with zero fixture edits, the BAND-02 guard's 31-site depth exposure — and declared as MOVED SET (0) in a `## Phase 54: …` section of `test/parity/FIXTURE-INVENTORY.md`; an unexpected mover is declared with before/after and regenerated per the engine gate, never hidden (constraint 3)"
    - "`docs/class-pass/v17-p54-after-smoke.json` is a byte-identical copy of the final rung's smoke file (`cmp` silent); `tools/lib/tuning-bot.mjs`, `test/parity/harness/comparables.js`, `test/parity/prototype-master.js.txt` (hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0), `content/`, floor-1 identity, the no-new-Filter-dial constants (density/dark/HAZARD_FLAT_THROUGH_DEPTH), the canon-from depths (5) and the 21+ dials are untouched (`git diff --stat COMMIT_B -- …` empty); no persisted-schema change"
  artifacts:
    - path: "engine/difficulty.js"
      provides: "the landed band constants (rung values) with per-rung JSDoc citations"
    - path: "test/difficulty/difficulty.test.js"
      provides: "BAND_PINS re-pinned to the landed 5..15 values (the 1..4 and 16+ blocks byte-unchanged)"
    - path: "test/unit/combat-scaling.test.js"
      provides: "PHASE_54_PINS at the landed values; hazard/ability re-scopes only when those dials moved"
    - path: "test/determinism/foe-abilities.test.js"
      provides: "the tier-5 (depth-5) FULL_FIGHT_PINS re-measured live per rung that moves depth 5 (comment cites the rung)"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "the Rung sections, change table, miss table, depth-20 identity proof, parity subsection"
    - path: "docs/class-pass/v17-p54-rungN-smoke.json + v17-p54-after-smoke.json"
      provides: "the per-rung class smokes and the AFTER copy"
    - path: ".planning/phases/54-four-band-retune-and-roster-decision/readouts/rungN-{solo,party,start-depth-20,smoke}.txt"
      provides: "the raw transcripts (the evidence)"
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "`## Phase 54: four-band curve on floors 5–15 (BAND-02) — measured zero: no replay site reaches floor 5`"
    - path: "tools/initiative-fixture-scan-output.txt"
      provides: "re-run on the final curve (expected byte-identical)"
  key_links:
    - from: "engine/difficulty.js band constants (rung N)"
      to: "readouts/rungN-*.txt + docs/class-pass/v17-p54-rungN-smoke.json"
      via: "measured on the rung's constants commit (meta.commit provenance), then quoted verbatim in `#### Rung N`"
      why: "BAND-02: a dial is only kept because a readout moved toward the bands; the JSDoc cites that readout"
    - from: "readouts/before-start-depth-20.txt + tools/initiative-fixture-scan-output.txt + the floor-1 literal pin"
      to: "readouts/rungN-start-depth-20.txt + the per-rung scan re-run + node --test test/difficulty/"
      via: "`diff` empty on every rung (slice AND scan), pin green"
      why: "BAND-01's deep-lethality yardstick (16–20 identity by construction) and floor-1 parity (the only floor a Filter rung must never reach); a non-empty diff is a stop-the-line defect"
    - from: "the per-band top causes in each rung's Four-band readout"
      to: "the next rung's dial choice"
      via: "solo median still <= 4 after rung 1 → a Filter rung (FOE_GRACE_AT_2 one notch and/or HAZARD_SCALE_AT_START when hazards >= 20 % of floor 2–4 deaths — USER RULING A); hazards >= 20 % of Wall deaths → WALL_HAZARD_SCALE; caster share → the ability band; starvation → not a dial (miss reason); then alternate by the readout"
      why: "constraint 7: a non-combat dial follows the death-cause breakdown of the previous rung"
  prohibitions:
    - "never edit test/parity/prototype-master.js.txt or test/parity/harness/comparables.js"
    - "never edit tools/lib/tuning-bot.mjs (frozen); never change a bot flag or seed count between rungs"
    - "never touch floor 1 identity: FOE_GRACE_AT_1 1.0, HAZARD_FROM_DEPTH 2, floor-1 dots/dark canon; never a NEW floor 1–4 dial: HAZARD_FLAT_THROUGH_DEPTH 3, DENSITY_CANON_THROUGH_DEPTH 2, DARK_HOLD_THROUGH_DEPTH 3, ENCOUNTER_DOT_CAP 13, DARK_BLOB_CAP 3, DARK_RADIUS_CAP 7, FOE_GRACE_CANON_FROM_DEPTH 5, HAZARD_CANON_FROM_DEPTH 5 — all unchanged; the Filter rung dials (USER RULING A) move ONLY within their ceilings: FOE_GRACE_AT_2 one notch per rung (0.5 → 0.4 → 0.35, never below 0.35), HAZARD_SCALE_AT_START one notch per rung (0.5 → 0.4 → 0.3, never below 0.3); WALL_FOE_POWER_AT_START never below graceFor(4) (= FOE_GRACE_AT_2 + (1 − FOE_GRACE_AT_2) × 2/3 at the landed FOE_GRACE_AT_2)"
    - "no flat-damage nerfs / bestiary dice trims / foeDmgBonusFor shape change / hero-side buffs to chase the median"
    - "no tier moves; no 21+ easing (COMBAT_SCALE_FROM_DEPTH 21, FOE_POWER_MAX 1.15, ABILITY_THREAT_MAX 1.3, FOE_CAP_MAX 4 untouched); no sub-identity at 16–20 (ENDGAME_CANON_FROM_DEPTH stays 16)"
    - "no draw added to difficultyCurve; no schema change"
    - "never a fifth rung; never loosen a pin (re-pin to the measured value with the rung cited); never hand-compute a determinism pin"
    - "never declare MOVED SET (0) without the three measurements (scan diff, parity suite, guard) pasted"
---

<objective>
Run the iteration ladder toward the BAND-01 numbers on Plan 01's scaffold: up to four rungs, each a constants-only move in `engine/difficulty.js` (Wall 5–8 / Breakaway 9–15 foePower first; the Wall hazard band and the caster-cadence band as rung-2+ dials chosen by the previous rung's per-band death causes), each committed green and then measured with the FULL four-readout set, each readout committed verbatim under the ledger, the depth-20 slice proven byte-identical on every rung; then measure and declare the parity moved set (expected zero) on the final curve, write the change table and the miss table, and copy the final smoke to `v17-p54-after-smoke.json`.

Purpose: BAND-02 is "every dial change gated on the bot readout moving toward the bands"; BAND-03 is "inside BAND-01's numbers or each miss recorded with the untaken rung and reason"; BAND-01's deep-lethality yardstick is the depth-20 slice. This plan is where the curve is actually tuned — once, on the corrected cadence/damage/Joiner baseline.

Output: `engine/difficulty.js` (landed constants), re-pinned tests, `docs/DIFFICULTY-RETUNE.md` (Rung sections + change/miss tables + identity proof + parity), `docs/class-pass/v17-p54-rung{1..N}-smoke.json` + `v17-p54-after-smoke.json`, `readouts/rungN-*.txt`, `test/parity/FIXTURE-INVENTORY.md` Phase 54 section, `tools/initiative-fixture-scan-output.txt` (re-run).

Decisions implemented (54-CONTEXT.md, quoted): Area 1 — "Rung 1 of the ladder is a band-shaped piecewise foePower (and, if needed, abilityThreat) curve on 5–15: a step at 5 (the Wall — foePower steps UP from the floor-4 grace value but may land below 1.0), an eased slope through 9–15, and exactly 1.0 from floor 16 onward"; "Non-combat dials are rung 2, taken only if rung 1 alone does not reach the bands … hazards on 5–8 (a second hazard band is allowed since falls/traps/leaps are ≈ 25 % of deaths) … Breather floors stay"; "No flat-damage nerf to chase the median"; "The bands are bot numbers. The tuning bot's policy is frozen … Misses are recorded per BAND-03 with the untaken rung and the reason." Area 2 — "Every rung gets the FULL readout … Budget ≈ 25 min of bot time per rung; ladder cap 4 rungs, then stop and record the misses. The executor runs the --party run in the background … Each rung's four transcripts are committed under the ledger; the rung's dial values are cited against the readout that motivated them"; "A rung that would touch floor 1 is escalated, not taken"; "Expected parity outcome: MOVED SET (0) — measured … declared in a FIXTURE-INVENTORY.md Phase 54 section". Constraint 11 (orchestrator): "rung 1: Wall foePower 0.85 at 5 → 0.95 at 8, Breakaway eased 0.95 → 1.0 at 15, literal 1.0 from 16; subsequent rungs move by ≤ 0.05 per band toward the readout … and the stop rule (all BAND-01 numbers inside the band, or 4 rungs, or a floor-1 escalation)". Claude's Discretion (resolved): rung order after rung 1 is chosen by the previous rung's per-band death causes (the `Four-band readout` block); `abilityThreat` stays identity unless the caster share justifies it (measure first).

Two commits per rung (a deliberate refinement of "one commit per rung"): `feat(54-02): rung N — …` lands the constants + re-pins green FIRST, so every readout — including the class smoke's `meta.commit` — is provably taken on the tree that carries the dial (Phase 53 measured on its AFTER_SHA for the same reason); `docs(54-02): rung N readouts — …` then commits the four transcripts, the smoke JSON and the ledger section. The rung's dial is still cited against its own readout; nothing is measured on an uncommitted tree.

Structural bound and USER RULING A (2026-09-21 — recorded in 54-CONTEXT.md `## USER RULINGS at plan approval (2026-09-21)`; restated in Plan 01's ledger section; do not re-litigate): a 5–15-only dial cannot move the solo median (runs are byte-identical through floor 4; reach ≥ 5 would stay 33.0 %), so the user ruled that the ladder MAY move the EXISTING Phase 27 floor 2–4 dials as rungs — `FOE_GRACE_AT_2` (0.5, one notch per rung, as far as Phase 27's noted 0.35 ceiling) and `HAZARD_SCALE_AT_START` (0.5, one notch per rung, not below 0.3) — with floor 1 exact identity kept (`FOE_GRACE_AT_1 = 1.0`, `HAZARD_FROM_DEPTH >= 2`, floor-1 dots/dark canon — parity), the ramps still reaching exactly 1.0 at `FOE_GRACE_CANON_FROM_DEPTH` / `HAZARD_CANON_FROM_DEPTH` (5 — formula endpoints; floor 5 is served by the band), and NO new floor 1–4 dials. The solo median 5–7 is therefore a LIVE target; reach ≥ 5 is recorded per rung; the per-rung "floors did not move" proof is a FLOOR-1 proof (depth-20 slice identity + scan Part B diff empty + the floor-1 literal pin). Rung order: rung 1 = the 5–15 band curve as written; if the solo median is still ≤ 4 after rung 1, rung 2 is a Filter rung (`FOE_GRACE_AT_2` 0.5 → 0.4 and/or `HAZARD_SCALE_AT_START` 0.5 → 0.4 when hazards are ≥ 20 % of floor 2–4 deaths); then alternate band / Filter / non-combat by the readout. Stop condition: solo median ∈ [5, 7], solo p90 ∈ [10, 13], solo reach ≥ 16 ∈ [2.0 %, 6.0 %], solo reach ≥ 20 ≤ 0.5 %, party median ∈ [5, 7]; or rung 4; or the only remaining move would touch floor 1 / add a new Filter dial / cross a Filter ceiling.

Schema gate: no persisted schema change (pure lookup on depth); no migration. No shell change. No bestiary change in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-CONTEXT.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-01-SUMMARY.md
@.planning/phases/53-joiner-level-cap/53-02-SUMMARY.md
</context>

<ground_rules>
- Base = Plan 01's scaffold commit `COMMIT_B` (read it from 54-01-SUMMARY.md; `git log --oneline --grep='feat(54-01): four-band curve scaffolding' -1`). `git diff --stat COMMIT_B -- engine/ content/ tools/lib/` must be empty before rung 1.
- Commits: plain `git commit -F "$SCRATCH/msg.txt"`; every message ends with exactly the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
- Gates at every commit: `npm test` (fail 0) and `npm run build:www` (exit 0); `boot:check` is not a gate; skip `state.advance-plan`.
- Master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every rung.
- Timing per rung on the 4-core machine: launch `--party` FIRST in the background (`run_in_background`, `> "$PHASE_DIR/readouts/rungN-party.txt" 2>&1`, ~21 min, ~60/200 stuck is normal), then solo (~1.5 min) → `--start-depth=20` (< 1 min) → the smoke (`--workers 4`, ~1.5–3 min with the party run contending) — then re-pin/ledger drafting while polling the party file for its `Outcome:` line (`grep -c '^Outcome:' file`; never a foreground sleep — poll between other steps or with a background bounded loop). Never treat a tool timeout as failure; never lower a seed count.
- Context discipline: never cat a whole transcript into context — read `head -20`, `grep -A12 'Four-band readout'`, `grep -A6 'Death-depth distribution'`, `grep -A40 'Death-cause breakdown' | head -25`, and the `Party (`/`Stuck:`/`Bot:` lines; the ledger splice script embeds the files verbatim.
- Every pin and ledger number is pasted from tool output; the ledger is CRLF — splice with `\r\n`.
- No device / human checkpoints; the SUMMARY ends with `## Human verification (deferred to end of run)`.
</ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: Rung 1 — the prescribed starting notch (Wall 0.85 → 0.95, Breakaway 0.95 → 1.0, literal 1 from 16): constants + JSDoc + re-pins → green commit → the four readouts on that commit → identity checks → the `#### Rung 1` ledger section → readouts commit</name>
  <files>engine/difficulty.js, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/determinism/foe-abilities.test.js, docs/DIFFICULTY-RETUNE.md, docs/class-pass/v17-p54-rung1-smoke.json, .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-solo.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-party.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-start-depth-20.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-smoke.txt</files>
  <read_first>
    - engine/difficulty.js — the Phase 54 block Plan 01 added (search `Phase 54 (BAND-02): the four-band curve`), `bandLerp`/`bandFoePowerFor`/`bandAbilityThreatFor`, and lines 204-243 (the Phase 27 grace/hazard constants — `FOE_GRACE_AT_2`'s JSDoc is the per-notch citation register to imitate: "Ladder rung 1, notch 2 (27-03 iteration 2): 0.75 -> 0.5 — the pooled smoke median was still 3 …")
    - test/difficulty/difficulty.test.js — the `BAND_PINS` block (the only block rung 1 re-pins; `FILTER_PINS` is the Filter rungs' block — Task 2) and the `Phase 54 (BAND-02) the Wall steps UP…` test (must stay green: 0.85 >= graceFor(4) = 0.8333… at FOE_GRACE_AT_2 0.5)
    - test/unit/combat-scaling.test.js — `PHASE_54_PINS` (re-pin), `band 5..15 (Phase 54, BAND-02)` (formula-driven — stays green once the pins move), `band wiring (Phase 54, BAND-02)` (helper-equality — stays green), `cap boundaries` (parametrised lower bound — stays green)
    - test/determinism/foe-abilities.test.js lines 95-130 (`SPECS`: `demons-t5` Djinni, `walking-dead-t5` Vampire, `beasts-t5` Stalka Beast all at `state.floor.depth = 5`; `magical-t4` depth 4 and `humans-t2` depth 2 are UNAFFECTED), lines 220-280 (`FULL_FIGHT_PINS` and its header rule: "re-measured live via this file's own runFullFight, never hand-computed" — each prior re-measure is a dated comment; add the Phase 54 rung-1 comment in the same register), the `runFullFight`/`runVisits` helpers and the `VISIT_PINS` (if any tier-5 visit pin exists, re-measure it the same way)
    - docs/DIFFICULTY-RETUNE.md — Plan 01's Phase 54 H3 (`#### Target as numbers (BAND-01)` — the numbers to read against; `#### Scaffold — commit …` — the last H4 before `## v1.2 retune (Phase 27) — TUNE-05..07`, the insertion point for `#### Rung 1`), lines 3581-3672 (Phase 27's `### Iteration log` — the per-iteration shape: constants table Old/New, curve table, smoke readout, "What moved", "what to turn next")
    - $PHASE_DIR/readouts/before-start-depth-20.txt (the diff target)
    - .planning/phases/53-joiner-level-cap/53-02-SUMMARY.md "Task 1" (the meta-parity / cells one-liners; the background-party polling pattern)
  </read_first>
  <action>
    Step 0 — preconditions: `git diff --stat COMMIT_B -- engine/ content/ tools/lib/` empty; `git status --porcelain` empty; record `PRE_RUNG=$(git rev-parse HEAD)`.

    Step 1 — constants (engine/difficulty.js): `WALL_FOE_POWER_AT_START` 1.0 → **0.85**, `WALL_FOE_POWER_AT_END` 1.0 → **0.95**, `BREAKAWAY_FOE_POWER_AT_START` 1.0 → **0.95**, `BREAKAWAY_FOE_POWER_AT_END` stays 1.0 (the Breakaway eases back to identity by 15; 16 is the literal by the guard). Everything else unchanged (hazard band 1.0, ability band 1.0). On each moved constant's JSDoc add the rung line in the Phase 27 register: "Phase 54 ladder rung 1 (2026-09-21, BAND-02): 1.0 -> 0.85 — the prescribed starting notch (54-CONTEXT Area 1 / constraint 11); motivated by the BEFORE readout (78572c5): solo death-depth 1/4/6/9, reach >=5 33.0 % / >=10 0.0 %, canon identity at 5 is the cliff the surviving third dies on; readout recorded under docs/DIFFICULTY-RETUNE.md `#### Rung 1`". Print the new curve for 4..17 and paste it into the JSDoc of `WALL_FOE_POWER_AT_START` as a one-line table (foePower at 4 / 5 / 8 / 9 / 15 / 16).

    Step 2 — re-pins (never loosen): capture `node -e "import('./engine/difficulty.js').then(m=>{const o={};for(let d=5;d<=15;d++){const c=m.difficultyCurve(d);o[d]={foePower:c.foePower,abilityThreat:c.abilityThreat,hazardScale:c.hazardScale}}console.log(JSON.stringify(o))})"` and paste it as the new `BAND_PINS` literal in test/difficulty/difficulty.test.js (comment: "rung 1 (commit …)"); update `PHASE_54_PINS` in test/unit/combat-scaling.test.js to the three new values. Run `node --test test/difficulty/ test/unit/combat-scaling.test.js` — green. Then `node --test test/determinism/foe-abilities.test.js`: the three depth-5 specs are EXPECTED to fail on `FULL_FIGHT_PINS` (their foes' copy-time wp is now `foeWpFor(wp, curve5)` = round(wp × 0.85) and they carry a negative `dmgBonus`, so the fights can resolve at different totalDraws/attacks/outcome); read the ACTUAL values from the assertion messages (or add a temporary `console.log` of the measured record inside the test, run once, remove it), paste them into `FULL_FIGHT_PINS` for exactly the specs that failed, with a comment block "Phase 54 rung 1 (BAND-02, 2026-09-21): depth-5 foePower 1.0 -> 0.85 — demons-t5 / walking-dead-t5 / beasts-t5 re-measured live via this file's own runFullFight (never hand-computed); humans-t2 (depth 2) and magical-t4 (depth 4) untouched — floors 1-4 are outside the band." If a spec does NOT fail, leave its pin and say so in the comment. Re-run — green. Then the broad targeted set: `node --test test/difficulty/ test/unit/ test/determinism/ test/parity/` — any OTHER failure is triaged under the re-pin-never-loosen rule (state the test, the old and new value, why the band moved it) and recorded in the SUMMARY's re-pin ledger; a failure that is not explained by a depth-5..15 foePower change is a STOP (investigate before continuing).

    Step 3 — gates + commit R1a: `npm test` (fail 0, totals pasted), `npm run build:www` (exit 0), master hash, `git diff --stat COMMIT_B -- content/ tools/ test/parity/harness/ test/parity/fixtures/` empty. Commit `feat(54-02): rung 1 — Wall foePower 0.85 -> 0.95 (5-8), Breakaway 0.95 -> 1.0 (9-15), literal 1 from 16; BAND_PINS / PHASE_54_PINS re-pinned; tier-5 determinism pins re-measured (BAND-02)` with the two trailers. `R1A=$(git rev-parse HEAD)`.

    Step 4 — the four readouts ON R1A (`git rev-parse HEAD` = R1A; tree clean): launch `node tools/tune-difficulty.mjs --seeds=200 --party > "$PHASE_DIR/readouts/rung1-party.txt" 2>&1` in the background; then `node tools/tune-difficulty.mjs --seeds=200 > "$PHASE_DIR/readouts/rung1-solo.txt" 2>&1`; then `node tools/tune-difficulty.mjs --seeds=50 --start-depth=20 > "$PHASE_DIR/readouts/rung1-start-depth-20.txt" 2>&1`; then `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p54-rung1-smoke.json > "$PHASE_DIR/readouts/rung1-smoke.txt" 2>&1`. Poll the party file for `^Outcome:` between the steps below.

    Step 5 — identity checks (STOP-THE-LINE if any fails): (a) `diff "$PHASE_DIR/readouts/before-start-depth-20.txt" "$PHASE_DIR/readouts/rung1-start-depth-20.txt"` MUST print nothing — if it prints anything, the 16+ identity is broken: capture the curve at 16..50 and diff against `$SCRATCH/phase53-curve.json`'s entries, find the leak (a band constant reaching past 15, a guard off by one), fix, re-pin, re-commit, re-run the slice — only then continue; (b) the FLOOR-1 proof (USER RULING A replaces the old reach-≥5 invariance): `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` MUST print nothing (Part B on the floor-1 fixtures; the movement seed-256 site's floor 2 has no fight and no hazard — the BAND-02 guard's Part (d)), and `node --test test/difficulty/` green (the `Phase 54 (BAND-01) floor-1 parity` literal pin); a non-empty scan diff means a floor-1 value moved: same stop-and-fix rule. On rung 1 (a 5–15-only move) reach ≥5 is EXPECTED to still read `>=5 33.0%` solo / `55.3%` party — record it; on a later Filter rung it is expected to move (that is the point) and is recorded as the target-side number. Also `node -e "const a=require('./docs/class-pass/v17-p53-after-smoke.json'),b=require('./docs/class-pass/v17-p54-rung1-smoke.json');const k=Object.keys(a.meta).filter(x=>x!=='commit'&&JSON.stringify(a.meta[x])!==JSON.stringify(b.meta[x]));console.log('metaParity',k.length===0,k,'cells',b.cells.length,'commit',b.meta.commit)"` → `metaParity true [] cells 143 commit <R1A prefix>`.

    Step 6 — the reading (numbers pasted): from the solo file — death-depth min/p50/p90/max, action p50, the `Four-band readout` block (histogram, mean, reach ≥5/8/9/10/13/16/20, band share, top causes per band), top-5 causes overall, caster rate 6-10, ability-damage share, parley line; from the party file — the same plus `Party (` and `Stuck:`; from the smoke — `node -e "const j=require('./docs/class-pass/v17-p54-rung1-smoke.json');const p=j.rollups.pooled;console.log(p.meanDepth,p.p50Depth,p.p90Depth,p.reach5,p.reach10,p.reach20,p.n)"` and the class-cell collapse check: `node -e "const a=require('./docs/class-pass/v17-p53-after-smoke.json'),b=require('./docs/class-pass/v17-p54-rung1-smoke.json');const shift=b.rollups.pooled.meanDepth-a.rollups.pooled.meanDepth;const key=c=>c.cls+'|'+c.sub+'|'+c.race;const bm=new Map(b.cells.map(c=>[key(c),c]));const bad=a.cells.map(c=>[c,bm.get(key(c))]).filter(([x,y])=>y&&(y.meanDepth-x.meanDepth)< -Math.max(0,shift)).map(([x,y])=>key(x)+' '+x.meanDepth+'->'+y.meanDepth);console.log('pooledShift',shift.toFixed(2),'collapsed',bad.length);console.log(bad.join('\n'))"` (a cell "collapses" when its mean depth fell by more than the pooled shift; list them — evidence, not a rollback trigger; at n = 5 per cell most are seed noise past floor 4, say so if so). Compose the reading table `| Measure | BEFORE (78572c5) | Rung 1 (R1A) | Target | In band? |` for: solo p50 (target 5–7), solo p90, solo reach ≥5 (recorded; 33.0 at BEFORE and expected unchanged on this 5–15-only rung), ≥10, ≥16, ≥20, solo mean death depth, party p50/p90/stuck/reach ≥5 (55.3 at BEFORE), pooled smoke mean / p50 / reach5, depth-20 slice (`identical`), scan Part B (`identical`). Then "What moved" (two sentences) and "What to turn next" — one rationale sentence per candidate knob: FIRST, per USER RULING A, if the solo median is still ≤ 4 → rung 2 is a Filter rung: `FOE_GRACE_AT_2` 0.5 → 0.4 (one notch; the Wall's floor becomes graceFor(4) = 0.8) and/or `HAZARD_SCALE_AT_START` 0.5 → 0.4 when the `top causes — Filter:` line shows hazards (`fell off a wall`, `undone by a trap`, `came up short on a leap`) at ≥ 20 % of floor 2–4 deaths — cite the Filter band-share and cause lines; OTHERWISE choose by the per-band causes: if `top causes — Wall:` is led by hazards (`fell off a wall`, `undone by a trap`, `came up short on a leap` ≥ 20 % of Wall deaths) → `WALL_HAZARD_SCALE` next; if casters lead (Drudge/Djinni/Vampire/Krupke/Rinkle-type kit bearers, or `ability damage` ≥ 10 % of damage taken) → the ability band next; if `starved in the dark` / `spent by the dungeon itself` lead → not a difficulty.js dial (record as a miss reason, keep moving foePower); otherwise the next foePower notch (≤ 0.05 per band; a 0.10 notch on ONE band is allowed only because rung 1 moved solo p90 by less than one floor — state which case applies).

    Step 7 — the ledger section: splice (CRLF, immediately before `## v1.2 retune (Phase 27) — TUNE-05..07`) `#### Rung 1 — commit <40-hex R1A> (Wall foePower 0.85 -> 0.95 on 5-8; Breakaway 0.95 -> 1.0 on 9-15; literal 1 from 16)` containing: the constants table `| Constant | Old | New |` (three rows), the curve table `| d | foePower | abilityThreat | hazardScale |` for d = 4..16 (from `node -e`, `(exact)` where `Object.is(x, 1)`), the four transcripts each introduced by its command line and fenced (solo, party, smoke stdout, start-depth-20) read from the readouts files, the identity lines (`diff … : empty` for the slice AND the scan, `>=5` solo / party as read, `metaParity true []`), the reading table, the class-cell check output, "What moved", "What to turn next", and the re-pin ledger for this rung (BAND_PINS, PHASE_54_PINS, FULL_FIGHT_PINS specs with old → new). Verify `node --test test/unit/difficulty-retune-ledger.test.js` green.

    Step 8 — commit R1b: stage `docs/DIFFICULTY-RETUNE.md docs/class-pass/v17-p54-rung1-smoke.json "$PHASE_DIR/readouts/rung1-*.txt"`; `npm test` fail 0 (docs-only, but the gate is per commit) and `build:www` exit 0; commit `docs(54-02): rung 1 readouts — solo / --party / class smoke / --start-depth=20 on R1A; depth-20 slice identical; reading + what to turn next (BAND-02)` with the two trailers.
  </action>
  <verify>
    <automated>diff .planning/phases/54-four-band-retune-and-roster-decision/readouts/before-start-depth-20.txt .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-start-depth-20.txt && node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && grep -o '>=5 [0-9.]*%' .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-solo.txt | head -1 && grep -o '>=5 [0-9.]*%' .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung1-party.txt | head -1 && node -e "import('./engine/difficulty.js').then(m=>console.log(m.WALL_FOE_POWER_AT_START,m.WALL_FOE_POWER_AT_END,m.BREAKAWAY_FOE_POWER_AT_START,m.BREAKAWAY_FOE_POWER_AT_END,[16,17,18,19,20].every(d=>Object.is(m.difficultyCurve(d).foePower,1)),m.difficultyCurve(5).foePower>=m.difficultyCurve(4).foePower))" && grep -c '^#### Rung 1 — commit ' docs/DIFFICULTY-RETUNE.md && node --test test/difficulty/ test/unit/combat-scaling.test.js test/determinism/foe-abilities.test.js test/unit/difficulty-retune-ledger.test.js && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./engine/difficulty.js').then(m=>console.log(m.WALL_FOE_POWER_AT_START,m.WALL_FOE_POWER_AT_END,m.BREAKAWAY_FOE_POWER_AT_START,m.BREAKAWAY_FOE_POWER_AT_END,m.WALL_HAZARD_SCALE,m.WALL_ABILITY_THREAT_AT_START,m.ENDGAME_CANON_FROM_DEPTH,m.COMBAT_SCALE_FROM_DEPTH,m.FOE_GRACE_AT_1,m.FOE_GRACE_AT_2))"` prints `0.85 0.95 0.95 1 1 1 16 21 1 0.5`
    - `Object.is(difficultyCurve(d).foePower, 1)` for d = 16..20 (printed `true`); `difficultyCurve(5).foePower >= difficultyCurve(4).foePower` (`true`); `difficultyCurve(15).foePower === 1` (the endpoint-exact lerp hits BREAKAWAY_FOE_POWER_AT_END exactly)
    - `grep -c 'Phase 54 ladder rung 1' engine/difficulty.js` >= 3 (one citation per moved constant)
    - `git diff COMMIT_B -- test/difficulty/difficulty.test.js | grep -c 'PHASE_53_FILTER_PINS\|PHASE_53_ENDGAME_PINS'` prints 0 (the 1..4 and 16+ blocks are byte-unchanged — only `BAND_PINS` moved); `git diff COMMIT_B -- test/determinism/foe-abilities.test.js | grep -c 'Phase 54 rung 1'` >= 1 (the re-measure comment) — or, if no spec failed, the SUMMARY says so with the green test output
    - the four readouts files exist on disk with their closing `Bot:` lines (`grep -c '^Bot:' readouts/rung1-solo.txt` 1, `rung1-party.txt` 1, `rung1-start-depth-20.txt` 1) and `rung1-smoke.txt` contains `POOLED` or the ranked-matrix header; `docs/class-pass/v17-p54-rung1-smoke.json` has 143 cells, seeds 5, `meta.commit` = the R1A short prefix; `metaParity true []` vs v17-p53-after-smoke.json
    - `diff readouts/before-start-depth-20.txt readouts/rung1-start-depth-20.txt` prints nothing; `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` prints nothing; `node --test test/difficulty/` green (floor-1 literal pin); solo `>=5` and party `>=5` are recorded in the reading table (expected `33.0%` / `55.3%` on this 5–15-only rung — a different number is investigated and explained before the rung is accepted)
    - `grep -c '^#### Rung 1 — commit ' docs/DIFFICULTY-RETUNE.md` prints 1 and the section (up to the next `#### ` or `## `) contains `| Constant | Old | New |`, `Four-band readout (BAND-01` at least 3 times (solo, party, slice), `What to turn next` naming the Filter-rung decision (`grep -c 'FOE_GRACE_AT_2'` in the section >= 1), `pooledShift`; ledger tests green; v1.2 H2 still last
    - two commits R1a (`feat(54-02): rung 1`) and R1b (`docs(54-02): rung 1 readouts`) exist, each with the two trailers; `npm test` fail 0 and `build:www` exit 0 at both; master hash unchanged; `git diff --stat COMMIT_B -- content/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js test/parity/fixtures/` empty
  </acceptance_criteria>
  <done>Rung 1 is landed green, measured with all four readouts on its own commit, proven not to have moved floors 1–4 or 16+, recorded verbatim in the ledger with the next-rung rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Rungs 2–4 — the conditional ladder under the stop rule (≤ 0.05 per band per rung; the hazard / ability band by the previous rung's per-band causes; overshoot back-off; floor 1–4 escalation never taken), each with the identical protocol (constants → re-pins → green commit → four readouts → identity checks → ledger section → readouts commit)</name>
  <files>engine/difficulty.js, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/determinism/foe-abilities.test.js, docs/DIFFICULTY-RETUNE.md, docs/class-pass/v17-p54-rung2-smoke.json, docs/class-pass/v17-p54-rung3-smoke.json, docs/class-pass/v17-p54-rung4-smoke.json</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md — the previous rung's `#### Rung N` section: its reading table and "What to turn next" (the ONLY input to the next rung's dial choice), and `#### Target as numbers (BAND-01)` (the stop rule)
    - engine/difficulty.js — the Phase 54 block; for a hazard rung the `hazardScale` ternary in `difficultyCurve` (the `WALL_HAZARD_SCALE` branch on `HAZARD_CANON_FROM_DEPTH..WALL_TO_DEPTH`); for an ability rung `bandAbilityThreatFor` and `abilityCadenceFor` (lines ~494-500: `every` = `Math.max(1, Math.round(a.every / t))`, `uses` = `Math.max(1, Math.round(a.uses * t))` — at t = 0.9 an `every: 2` kit stays 2, `every: 3` → 3, `every: 4` → 4; only t <= ~0.83 moves an `every: 3` to 4 — pick the ability notch with this rounding in mind and say so)
    - test/difficulty/difficulty.test.js — `BAND_PINS`; `hazardScale is exactly 1 at depths 1, 5, 6, 20 …` (a hazard rung re-pins its depth list to `[1, 9, 20]` and adds the band assertion — comment the rung; a non-hazard rung leaves it)
    - test/unit/combat-scaling.test.js — `PHASE_54_PINS`; the re-scoped `monotone … abilityThreat exactly 1 on floors 1-4 and non-decreasing from WALL_FROM_DEPTH on` test (an ability rung with a Wall dip below the Breakaway start keeps it green only if the band is non-decreasing 5..15 — choose values accordingly, e.g. Wall 0.90 → 0.90, Breakaway 0.90 → 1.0); `synthetic-curve helper pins` (unchanged); `D-18 … identity at depth 20` (unchanged)
    - test/determinism/foe-abilities.test.js — `FULL_FIGHT_PINS` (re-measure the depth-5 specs again ONLY when a rung changes depth-5 foePower or abilityThreat; an ability rung can move the caster cadence in `demons-t5`/`walking-dead-t5`/`beasts-t5` and any `VISIT_PINS` at tier 5)
    - test/unit/foe-abilities.test.js, test/unit/combat.test.js, test/unit/foe-turn-draw-count.test.js (depth-1 fixtures — must stay untouched: the draw-count pins are never edited this phase)
  </read_first>
  <action>
    Loop for N = 2, 3, 4 — ENTER the loop only if the previous rung's reading has at least one BAND-01 number outside its band (solo p50 ∉ [5, 7], solo p90 ∉ [10, 13], solo reach ≥16 ∉ [2.0, 6.0] %, solo reach ≥20 > 0.5 %, party p50 ∉ [5, 7]); STOP the ladder (skip to Task 3) when all five are inside, or after rung 4, or when the only move the previous reading points at would touch floor 1, add a NEW floor 1–4 dial, or cross a Filter ceiling (record it as untaken — "floor-1 parity" / "no new Filter dial" / "Filter ceiling (USER RULING A: FOE_GRACE_AT_2 >= 0.35, HAZARD_SCALE_AT_START >= 0.3)" — and stop).

    Choose rung N's dial from rung N−1's "What to turn next" (one dial group per rung; never two unrelated dials in one rung so the readout stays attributable). Precedence: when the solo median is still ≤ 4 after rung 1, rung 2 IS a Filter rung (USER RULING A); after that, alternate band / Filter / non-combat by the readout (the median row is served by the Filter dials, the p90 / reach-16 / reach-20 rows by the band dials — pick the group whose target is furthest from its band):
    - Filter rung (USER RULING A — the EXISTING Phase 27 floor 2–4 dials, one notch each per rung): `FOE_GRACE_AT_2` 0.5 → 0.4 → 0.35 (never below 0.35; floors 2–4 foePower = FOE_GRACE_AT_2 + (1 − FOE_GRACE_AT_2) × (d − 2) / 3 — the ramp still ends at exactly 1.0 at FOE_GRACE_CANON_FROM_DEPTH 5 by formula; floor 5 is the Wall band), and/or `HAZARD_SCALE_AT_START` 0.5 → 0.4 → 0.3 (never below 0.3; taken when hazards are ≥ 20 % of floor 2–4 deaths in the `top causes — Filter:` line; flat through HAZARD_FLAT_THROUGH_DEPTH 3, then the existing linear ease to exactly 1.0 at 5). Floor 1 never moves (`FOE_GRACE_AT_1` 1.0, `HAZARD_FROM_DEPTH` 2); no density/dark change on 1–4. JSDoc: APPEND to each moved constant's Phase 27 note a `DELIBERATE RULES CHANGE (Phase 54, BAND-02, 2026-09-21) — ladder rung N: a -> b — <readout that motivated it>` paragraph, keeping the Phase 27 history verbatim. Re-pins (never loosened, rung cited): `FILTER_PINS` (test/difficulty/difficulty.test.js, depths 2..4 foePower/hazardScale), `PHASE_27_PINS.FOE_GRACE_AT_2` / `.HAZARD_SCALE_AT_START` (test/unit/combat-scaling.test.js — the `Phase 27 retune pins match …` test; the `grace band` test is formula-driven and stays green), the `startCombat at depth 2 / level 2 copies a NEGATIVE dmgBonus … none on a lvl-1 foe` test (at FOE_GRACE_AT_2 ≤ 0.4 a lvl-1 foe's bonus rounds to −1 and DOES carry the key — re-pin its lvl-1 branch with the arithmetic in the comment), the depth-2 hazard pins in test/unit/movement.test.js (`move: at depth 2, hazardScale (0.5) halves the canon climb-fall damage — … = 2` / gorge `= 4`) and test/unit/encounters.test.js (`springTrap: at depth 2, hazardScale (0.5) … = 8`) — re-pin the numbers AND the titles' `(0.5)` to the landed value, and the `humans-t2` (depth 2) / `magical-t4` (depth 4) specs of test/determinism/foe-abilities.test.js re-measured live via the file's own helpers (as Phase 27 27-03 iteration 2 did). The Wall's floor moves with it: `WALL_FOE_POWER_AT_START` >= graceFor(4) at the NEW FOE_GRACE_AT_2 (0.8 at 0.4; 0.7833… at 0.35) — the `Wall steps UP` pin stays green as long as the Wall start is not lowered below it in the same rung.
    - foePower notch: each band's constant moves by at most 0.05 toward the readout (lower = easier); `WALL_FOE_POWER_AT_START` never below `graceFor(4)` at the landed `FOE_GRACE_AT_2` (0.8333… at 0.5, 0.8 at 0.4, 0.7833… at 0.35 — a request to go lower is the untaken "Wall steps UP" rung); within a band `AT_END >= AT_START` is NOT required, but the Wall-steps-up pin (`curve(5) >= curve(4)`) and foePower non-decreasing 5..20 (Plan 01's pin (e)) must stay green — so the Breakaway start must be >= the Wall end, and the Breakaway end may sit below 1.0 to make 16 a visible step UP ("steepens 16–20"). A 0.10 notch on ONE band is allowed only when rung N−1 moved solo p90 by less than one floor (say so in the JSDoc).
    - hazard band (taken when hazards are >= 20 % of Wall-band deaths in rung N−1's `top causes — Wall:`): `WALL_HAZARD_SCALE` 1.0 → 0.75 (a second notch later → 0.6, never below the landed `HAZARD_SCALE_AT_START`); applies to 5..8 only; 9+ literal 1. Re-pins: `BAND_PINS` hazardScale 5..8, `PHASE_54_PINS`, the `hazardScale is exactly 1 at depths …` test's depth list → `[1, 9, 20]` plus `difficultyCurve(5..8).hazardScale === WALL_HAZARD_SCALE`, and the two depth-5 "hazardScale is exactly 1 (canon)" tests in test/unit/movement.test.js and test/unit/encounters.test.js (re-target them to depth 9 or re-pin to the band value, with the rung cited).
    - ability band (taken when kit-bearing foes lead the Wall/Breakaway causes or `ability damage` >= 10 % of damage taken): `WALL_ABILITY_THREAT_AT_START/END` and `BREAKAWAY_ABILITY_THREAT_AT_START` 1.0 → 0.85 with `BREAKAWAY_ABILITY_THREAT_AT_END` 1.0 (non-decreasing 5..15, literal 1 from 16); a later notch → 0.75. Re-pins: `BAND_PINS` abilityThreat, `PHASE_54_PINS`, the re-scoped `depth-6 first divergence` test (formula-driven — stays green), the depth-5 determinism specs (re-measure live).
    - starvation / exhaustion leading: NOT a difficulty.js dial — record the reason in the miss table draft and take the next best dial.
    - overshoot (solo p90 > 13, or reach ≥20 > 0.5 %, or reach ≥16 > 6.0 %): rung N backs off HALF a notch (0.025 → round to 0.03) on the band that moved last.

    Then the identical protocol as Task 1, with N substituted: (1) constants + JSDoc citation lines ("Phase 54 ladder rung N (2026-09-21, BAND-02): a -> b — rung N−1 read solo p50/p90 x/y, reach >=16 z %, …; readout under `#### Rung N`"); (2) re-pins (`BAND_PINS`, `PHASE_54_PINS`, plus the dial-specific pins above; depth-5 determinism specs re-measured live when depth 5 moved; never loosen); (3) `npm test` fail 0 + `build:www` + master hash → commit `feat(54-02): rung N — <dial summary> (BAND-02)`; (4) the four readouts on that commit into `readouts/rungN-*.txt` and `docs/class-pass/v17-p54-rungN-smoke.json` (party first, background); (5) identity checks — `diff before-start-depth-20.txt rungN-start-depth-20.txt` empty, `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` empty, `node --test test/difficulty/` green (floor-1 literal pin), `metaParity true []` — STOP-THE-LINE on any failure; solo / party `>=5` RECORDED (on a Filter rung it is expected to rise; on a band-only rung it is expected to equal the previous rung's value — a move on a band-only rung is investigated and explained); (6) the reading table with columns `| Measure | BEFORE (78572c5) | Rung N−1 | Rung N | Target | In band? |` (solo p50 first — the live median target), the class-cell collapse check vs BOTH v17-p53-after-smoke.json and the previous rung, "What moved", "What to turn next" (or "STOP — <reason>"); (7) splice `#### Rung N — commit <40-hex> (<dial summary>)` before the v1.2 H2; (8) commit `docs(54-02): rung N readouts — … (BAND-02)`.

    After the last rung taken (whichever N), write into `$SCRATCH/miss-draft.md` the miss table draft: one row per BAND-01 measure not inside its band → `| Measure | Final reading | Target | Untaken rung | Reason |` — the solo median row is CONDITIONAL (USER RULING A made it a live target): present only if the final solo p50 is outside 5–7, naming the concrete Filter notch not taken (e.g. "FOE_GRACE_AT_2 0.4 -> 0.35" / "HAZARD_SCALE_AT_START 0.4 -> 0.3") with the reason ("ladder cap 4" / "Filter ceiling 0.35 reached — the next lever is floor 1 (FOE_GRACE_AT_1 < 1.0), refused: floor-1 parity" / "a new floor 1–4 dial (density/dark), refused: USER RULING A"), plus the structural sentence for context ("a 5–15 dial cannot move it: reach >=5 read 33.0 % on every band-only rung; the Filter rungs moved it to X %"); other rows name the concrete next notch not taken (e.g. "rung 5: Breakaway 0.85 -> 0.80" / "ladder cap 4 (54-CONTEXT Area 2)") or the non-dial reason ("starvation 10 % of deaths — food economy, not a difficulty.js dial; deferred").
  </action>
  <verify>
    <automated>for f in .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung*-start-depth-20.txt; do diff .planning/phases/54-four-band-retune-and-roster-decision/readouts/before-start-depth-20.txt "$f" || exit 1; done && node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && for f in .planning/phases/54-four-band-retune-and-roster-decision/readouts/rung*-solo.txt; do grep -o '>=5 [0-9.]*%' "$f" | head -1; done && node -e "import('./engine/difficulty.js').then(m=>{const ok=[16,17,18,19,20].every(d=>Object.is(m.difficultyCurve(d).foePower,1)&&Object.is(m.difficultyCurve(d).abilityThreat,1)&&Object.is(m.difficultyCurve(d).hazardScale,1));let mono=true;for(let d=5;d<20;d++)if(m.difficultyCurve(d+1).foePower<m.difficultyCurve(d).foePower)mono=false;const c1=m.difficultyCurve(1);console.log('endgame identity',ok,'monotone 5..20',mono,'wall>=grace4',m.difficultyCurve(5).foePower>=m.difficultyCurve(4).foePower,'wallStart',m.WALL_FOE_POWER_AT_START,'grace2',m.FOE_GRACE_AT_2,'hazStart',m.HAZARD_SCALE_AT_START,'floor1',Object.is(c1.foePower,1)&&Object.is(c1.hazardScale,1)&&c1.dots===10&&c1.darkBlobs===0,'ceilings',m.FOE_GRACE_AT_2>=0.35&&m.HAZARD_SCALE_AT_START>=0.3&&m.FOE_GRACE_AT_1===1&&m.HAZARD_FROM_DEPTH===2&&m.FOE_GRACE_CANON_FROM_DEPTH===5&&m.HAZARD_CANON_FROM_DEPTH===5,'rungs',$(grep -c '^#### Rung [1-4] — commit ' docs/DIFFICULTY-RETUNE.md))})" && node --test test/difficulty/ test/unit/combat-scaling.test.js test/unit/movement.test.js test/unit/encounters.test.js test/determinism/foe-abilities.test.js test/unit/difficulty-retune-ledger.test.js && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#### Rung [1-4] — commit ' docs/DIFFICULTY-RETUNE.md` prints the number of rungs taken (1..4), never 5; each `#### Rung N` section names a 40-hex commit whose `git show --stat` touches engine/difficulty.js
    - for every rung N taken: `readouts/rungN-solo.txt`, `rungN-party.txt`, `rungN-start-depth-20.txt`, `rungN-smoke.txt` and `docs/class-pass/v17-p54-rungN-smoke.json` exist; `diff readouts/before-start-depth-20.txt readouts/rungN-start-depth-20.txt` prints nothing; the scan diff on the rung's commit was empty (recorded in the rung section); solo / party `>=5` recorded in the rung's reading table (unchanged on band-only rungs; moved on Filter rungs); the smoke's `meta.commit` equals that rung's `feat(54-02): rung N` commit prefix
    - `wall>=grace4 true`; `monotone 5..20 true`; `endgame identity true`; `floor1 true`; `ceilings true` (FOE_GRACE_AT_2 >= 0.35, HAZARD_SCALE_AT_START >= 0.3, FOE_GRACE_AT_1 1, HAZARD_FROM_DEPTH 2, FOE_GRACE_CANON_FROM_DEPTH 5, HAZARD_CANON_FROM_DEPTH 5); `COMBAT_SCALE_FROM_DEPTH` 21, `FOE_POWER_MAX` 1.15, `ABILITY_THREAT_MAX` 1.3, `FOE_CAP_MAX` 4, `HAZARD_FLAT_THROUGH_DEPTH` 3, `ENCOUNTER_DOT_CAP` 13, `DARK_BLOB_CAP` 3, `DARK_RADIUS_CAP` 7, `DARK_HOLD_THROUGH_DEPTH` 3, `DENSITY_CANON_THROUGH_DEPTH` 2 unchanged (`node -e` print pasted); `WALL_HAZARD_SCALE >= HAZARD_SCALE_AT_START`; `FOE_GRACE_AT_2` / `HAZARD_SCALE_AT_START` either unchanged (0.5) or at a Filter-rung notch (0.4 / 0.35; 0.4 / 0.3) with the rung's JSDoc paragraph appended to the Phase 27 note (`grep -c 'DELIBERATE RULES CHANGE (Phase 54, BAND-02, 2026-09-21)' engine/difficulty.js` grows by 1 per moved Filter constant; the Phase 27 text above it is byte-unchanged: `git diff COMMIT_B -- engine/difficulty.js | grep -c '^-.*Phase 27'` prints 0)
    - `git diff COMMIT_B -- test/difficulty/difficulty.test.js | grep -c 'PHASE_53_FLOOR1_PIN\|PHASE_53_ENDGAME_PINS'` prints 0 (the floor-1 literal and the 16+ block are byte-unchanged; only `BAND_PINS` and, on Filter rungs, `FILTER_PINS` moved); `git diff COMMIT_B -- test/unit/foe-turn-draw-count.test.js test/unit/combat.test.js | grep -c '^-[^-]'` prints 0 (no pinned draw line removed); every changed pin in test/determinism/foe-abilities.test.js, test/unit/movement.test.js, test/unit/encounters.test.js and test/unit/combat-scaling.test.js carries a `Phase 54 rung N` comment
    - every rung's JSDoc citation exists: `grep -c 'Phase 54 ladder rung N' engine/difficulty.js` >= 1 for each N taken (on Filter rungs the citation lives in the appended paragraph on `FOE_GRACE_AT_2` / `HAZARD_SCALE_AT_START`)
    - `$SCRATCH/miss-draft.md` exists with one row per out-of-band measure (the solo-median row present only if p50 is still outside 5–7, naming the untaken Filter notch) or the line `all measures inside the band` when the stop rule fired on success
    - each rung's two commits carry the two trailers; `npm test` fail 0 and `build:www` exit 0 at each; master hash unchanged; `git diff --stat COMMIT_B -- content/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js test/parity/fixtures/` empty
  </acceptance_criteria>
  <done>The ladder has run to the stop rule (in-band, cap 4, or an escalation refused), every rung measured on its own commit and recorded, with the miss table drafted from the final reading.</done>
</task>

<task type="auto">
  <name>Task 3: The final curve measured for parity (scan Part B + parity suite + BAND-02 guard) and declared in FIXTURE-INVENTORY.md; the ledger's change table, miss table, depth-20 identity proof and parity subsection; v17-p54-after-smoke.json; gates; the verifier-grade SUMMARY</name>
  <files>tools/initiative-fixture-scan-output.txt, test/parity/FIXTURE-INVENTORY.md, docs/DIFFICULTY-RETUNE.md, docs/class-pass/v17-p54-after-smoke.json, .planning/phases/54-four-band-retune-and-roster-decision/54-02-SUMMARY.md</files>
  <read_first>
    - tools/initiative-fixture-scan.mjs lines 1-60 (Run line; Part A = Phase 51's invariant predictor — `MOVED SET (3)` / `INITIATIVE EXPOSURE: 3 of 31` are PHASE 51's set, not this phase's; Part B = per combat/magic site lockstep diff `fields.after (engine @end)`) and tools/initiative-fixture-scan-output.txt (the committed Phase 53 AFTER copy)
    - test/parity/FIXTURE-INVENTORY.md lines 2089-2230 (the Phase 53 section — copy its H2/H3 shape: rule paragraph, `### The predictor — …`, `### The live scan (…)`, `### Moved set — declared records`, `### Draw-count pins`, `### Byte-identical elsewhere (Phase 53)`; the new section is appended at EOF after it), lines 1-30 (the generated-block markers — `node tools/fixture-inventory.mjs` regenerates ONLY that block)
    - test/parity/divergence-records.test.js — the `BAND-02: the holders declaring Phase 54 …` guard (its 31-site `maxDepthEver` loop and the floor-2 no-fight/no-hazard assertion on the movement site are the exposure predictor to cite; its floor-1 literal is the pin)
    - docs/DIFFICULTY-RETUNE.md lines 3534-3580 (the Phase 27 change table: `| Knob | File | Before (…) | After (…) | Rationale | Record |` — one row per constant INCLUDING the unchanged ones marked `(unchanged)`), the Phase 54 H3's rung sections (the source of every before/after), `#### Target as numbers (BAND-01)`
    - $SCRATCH/miss-draft.md; $SCRATCH/phase53-curve.json (from Plan 01 — if the scratchpad was cleared, re-capture it from `git show COMMIT_B~1:engine/difficulty.js` written to a temp file and imported, or from the ledger's BEFORE curve table)
    - .planning/phases/53-joiner-level-cap/53-01-SUMMARY.md (the measured-zero evidence wording; the SUMMARY shape)
  </read_first>
  <action>
    Step 1 — parity measurement on the FINAL curve (HEAD = the last rung's readouts commit; tree clean): `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt` then `git diff --stat tools/initiative-fixture-scan-output.txt` — expected EMPTY (every replay site is floor 1 or 2 — movement seed 256 — the band starts at 5, and a Filter rung reaches floor 2 only through a fight or a hazard, which the seed-256 site has neither of — the guard's Part (d); Part A's `MOVED SET (3)` / `INITIATIVE EXPOSURE: 3 of 31` are Phase 51's invariant set; this is the SAME scan that was re-run on every rung — Task 3 records the final one); `node --test test/parity/` — paste the pass/fail totals (expected all green, one more test than Phase 53's 42: the BAND-02 guard); `git diff --stat COMMIT_B -- test/parity/fixtures/` — empty; `node --test test/parity/divergence-records.test.js` green (the guard's 31 sites, max depth 2). MOVER BRANCH (only if the scan diff or the suite is NOT clean): do not hide it — identify the site, replay it to find the first divergent action and the depth it reached (a site reaching floor 5+, or the floor-2 site fighting or springing a hazard, would be a new fact — the guard would already have failed), declare a `+54` action-path divergence record with `before`/`after` measured (never typed), regenerate only that fixture per the engine gate, update the guard's `EXPECTED` list and the FIXTURE-INVENTORY section accordingly, and record the whole chain in the SUMMARY's Issues.

    Step 2 — `test/parity/FIXTURE-INVENTORY.md`: append at EOF `## Phase 54: four-band curve on floors 5–15 (BAND-02) — measured zero: no replay site reaches floor 5` with: one paragraph on the rule (the band constants and their landed values; `bandFoePowerFor` selected for `WALL_FROM_DEPTH <= d < COMBAT_SCALE_FROM_DEPTH`; the literal-1 `>=` guard from `ENDGAME_CANON_FROM_DEPTH`; the Filter rungs taken, if any (`FOE_GRACE_AT_2` / `HAZARD_SCALE_AT_START` old → new — USER RULING A) with floor 1 byte-identical (the Plan 01 floor-1 literal pin) and floors 2–4 MEASURED on every rung (scan Part B diff empty each time — the seed-256 floor-2 site fights nothing and springs nothing); 16+ byte-identical — the depth-20 slice diffs); `### The predictor — depth exposure per site` (a table `| Fixture#site | Seed | actions | max floor.depth reached | fight or hazard at depth >= 2? | reaches WALL_FROM_DEPTH? |` — generate it with a scratch replay reusing the guard's `replaySiteEvents` shape, 31 rows, every `max` <= 2, `no` / `no` everywhere — the floor-2 column is what makes a Filter rung parity-clean); `### The live scan (tools/initiative-fixture-scan.mjs, re-run on the final curve)` — the empty diff, Part A invariance, the parity totals, zero fixture edits; `### Moved set — declared records` — `MOVED SET (0): none.`; no `+54` record exists (`grep -rc '"phase":.*54' test/parity/fixtures/*.json` is 0 everywhere); the BAND-02 guard pins declared == `[]` AND the depth exposure AND the floor-2 no-fight/no-hazard assertion AND the floor-1 curve literal; `### Draw-count pins` — `difficultyCurve` consumes no rng (Plan 01's source pin); `foeWpFor`/`foeDmgBonusFor` are post-draw arithmetic (no draw shape changes — `test/unit/combat-scaling.test.js`'s `draw-shape equality (D-17/D-19)` test is green with `draws5 === 8`); `git diff COMMIT_B -- test/unit/foe-turn-draw-count.test.js test/unit/combat.test.js | grep -c '^-[^-]'` = 0; the tier-5 determinism pins (`test/determinism/foe-abilities.test.js`) re-measured live per band rung and the `humans-t2` / `magical-t4` pins per Filter rung (list the specs and the rung); the depth-2 hazard pins in `test/unit/movement.test.js` / `test/unit/encounters.test.js` re-pinned per Filter hazard rung; `### Byte-identical elsewhere (Phase 54)` — master hash unchanged; `comparables.js` untouched (no new serialized field — the curve object's key set is unchanged); `tools/lib/tuning-bot.mjs` untouched; `content/` untouched (no bestiary change in Plans 01–02); shell untouched. Then `node tools/fixture-inventory.mjs` and confirm `git diff test/parity/FIXTURE-INVENTORY.md` shows ONLY the appended section (the generated roster block is byte-identical); `node --test test/parity/fixture-inventory.test.js` green.

    Step 3 — the ledger close-out (splice before the v1.2 H2, after the last Rung section): `#### Change table (BAND-03)` — `| Constant | File | Before (78572c5) | After (final rung commit <short>) | Rationale | Record |` with one row for EACH of the 14 Phase 54 constants (scaffold value → landed value, or `(unchanged — rung-2+ dial not taken)`), followed by the two Filter-rung rows `FOE_GRACE_AT_2` (0.5 → landed, or `0.5 (unchanged — no Filter rung taken)`) and `HAZARD_SCALE_AT_START` (0.5 → landed, or unchanged), each with the rung and the readout that motivated it and the note 'USER RULING A (2026-09-21): an EXISTING Phase 27 dial re-opened as a rung; floor 1 untouched; ramp still 1.0 at 5', then one row for each remaining Phase 21/27 dial marked `(unchanged)` (`COMBAT_SCALE_FROM_DEPTH` 21, `FOE_CAP_BASE`/`MAX`/`SOFT_K`, `FOE_POWER_BASE`/`MAX`/`SOFT_K`, `ABILITY_THREAT_BASE`/`MAX`/`SOFT_K`, `FOE_LVL_BIAS`, `FOE_GRACE_AT_1` (1.0, MUST stay), `FOE_GRACE_CANON_FROM_DEPTH` 5, `HAZARD_FROM_DEPTH` 2 (MUST stay), `HAZARD_FLAT_THROUGH_DEPTH` 3, `HAZARD_CANON_FROM_DEPTH` 5, `ENCOUNTER_DOT_*`, `DENSITY_CANON_THROUGH_DEPTH`, `DARK_*`, `BREATHER_EVERY`, `WATER_POOL_*`) — every row's Rationale cites the rung and the readout number that moved; then the final curve table `| d | dots | darkBlobs | darkRadius | foePower | hazardScale | foeCap | abilityThreat |` for d = 1..5, 8, 9, 10, 15, 16, 20, 21, 35, 50 (from `node -e`; `(exact)` where `Object.is(x, 1)`; floors 2–4 show the landed Filter values). `#### Miss table (BAND-03)` — `| Measure | BEFORE (78572c5) | AFTER (final) | Target | Untaken rung | Reason |` from `$SCRATCH/miss-draft.md` (the solo-median row present only if p50 is still outside 5–7, naming the untaken Filter notch — USER RULING A) plus a one-line verdict per measure that DID land, and the per-rung solo / party reach ≥5 series (33.0 % / 55.3 % at BEFORE → each rung) as the Filter-rung evidence. `#### Depth-20 slice — identity proof (BAND-01)` — the list of `diff readouts/before-start-depth-20.txt readouts/rungN-start-depth-20.txt` results (all `empty`), the per-rung scan Part B diff results (all `empty`) and the floor-1 literal pin as the FLOOR-1 proof, and the curve capture at 1 and 16..25, 35, 50 on the final tree diffed against the Phase 53 capture (`node -e` JSON diff — empty). `#### Parity — MOVED SET (0) (BAND-02)` — the scan diff (empty), the parity totals, the guard name, the FIXTURE-INVENTORY section pointer.

    Step 4 — `cp docs/class-pass/v17-p54-rung<last>-smoke.json docs/class-pass/v17-p54-after-smoke.json`; `cmp` the two (silent); note in the ledger's `#### Change table` intro that `v17-p54-after-smoke.json` is the final rung's smoke byte-for-byte (its `meta.commit` = the final rung's constants commit).

    Step 5 — gates and commit: `node --test test/unit/difficulty-retune-ledger.test.js test/parity/` green; `npm test` fail 0 (totals pasted); `npm run build:www` exit 0; master hash; `git diff --stat COMMIT_B -- content/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js test/parity/fixtures/ src/browser/ mazeworld.html engine/saveState.js` empty. Commit `docs(54-02): parity measured on the final curve — MOVED SET (0) declared (FIXTURE-INVENTORY Phase 54) + change table / miss table / depth-20 identity proof + v17-p54-after-smoke.json (BAND-01, BAND-02, BAND-03)` with the two trailers.

    Step 6 — the SUMMARY `$PHASE_DIR/54-02-SUMMARY.md`: (1) `## Success criteria → proof` — ROADMAP SC2 (curve reshaped; every dial cited) → the rung count, the change table pointer, `grep -c 'Phase 54 ladder rung'` count; SC3 (AFTER inside BAND-01 or misses recorded; one row per constant) → the final reading table + the miss table verbatim; SC5 → `git diff --stat COMMIT_B -- content/` empty + gates; BAND-01's depth-20 yardstick → the per-rung diff lines; (2) `## Constants change table (before → after)` verbatim; (3) `## Rung readings` — per rung: the four readouts' headline numbers (solo p50/p90/reach ≥5/≥10/≥16/≥20, party p50/p90/stuck, pooled smoke mean, depth-20 `identical`) and the reading sentence; (4) `## Final AFTER vs BAND-01` with the miss table; (5) `## Re-pin ledger` — per file, per rung: test → old → new → why (BAND_PINS, PHASE_54_PINS, FULL_FIGHT_PINS specs, any hazard/ability re-scope); (6) `## Parity — MOVED SET (0)` — scan diff empty, `node --test test/parity/` totals, the guard's 31 sites/max depth 2, the FIXTURE-INVENTORY section name; (7) `## Gates` — `npm test` totals, `build:www` exit 0, master hash, the untouched-file diffs; (8) `## Schema` — none; (9) `## Commits` — every rung's two SHAs + the parity commit; the FINAL constants commit named as `AFTER_SHA` for Plan 03; (10) `npm run boot:check` not run (environment-blocked); (11) end with `## Human verification (deferred to end of run)` — Pixel 7 checks for the Phase 55 batch: (a) a natural run on the post-retune debug APK feels like it dies at floors 5–7, not 3–4 — floor 5's first fights should feel survivable for a full-HP hero where they used to one-shot; (b) start-at-depth 20 (Settings) feels unchanged from the v1.5/v1.6 rounds; (c) a floor-5–8 fight against a tier-2/3 foe no longer one-shots a full-HP hero (the Wall is a wall, not a cliff); (d) if the hazard band landed: a floor-5–8 trap/wall-fall hurts less than a floor-9 one; (e) if the ability band landed: a floor-6–10 caster casts a little less often than on floor 16. Commit the SUMMARY with plain `git commit -F` as `docs(54-02): complete four-band ladder plan 02 — gates + summary` ending with the two trailers.
  </action>
  <verify>
    <automated>node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && git diff --stat HEAD -- test/parity/fixtures/ | cat && node --test test/parity/ test/unit/difficulty-retune-ledger.test.js && grep -c '^## Phase 54: four-band curve on floors 5–15 (BAND-02)' test/parity/FIXTURE-INVENTORY.md && grep -c '^#### Change table (BAND-03)' docs/DIFFICULTY-RETUNE.md && grep -c '^#### Miss table (BAND-03)' docs/DIFFICULTY-RETUNE.md && grep -c '^#### Depth-20 slice — identity proof (BAND-01)' docs/DIFFICULTY-RETUNE.md && grep -c '^#### Parity — MOVED SET (0) (BAND-02)' docs/DIFFICULTY-RETUNE.md && cmp docs/class-pass/v17-p54-after-smoke.json "$(ls docs/class-pass/v17-p54-rung*-smoke.json | sort -V | tail -1)" && grep -c '^## Human verification (deferred to end of run)' .planning/phases/54-four-band-retune-and-roster-decision/54-02-SUMMARY.md && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` prints nothing and `git diff --stat COMMIT_B -- tools/initiative-fixture-scan-output.txt` prints nothing (byte-identical to the Phase 53 AFTER) — OR, under the mover branch, the changed lines are quoted in the SUMMARY, every moved holder carries a `+54` record with measured before/after, and the guard's `EXPECTED` lists exactly those holders
    - `node --test test/parity/` all pass, fail 0 (totals pasted); `git diff --stat COMMIT_B -- test/parity/fixtures/` prints nothing (zero fixture edits); `grep -rc '"phase":.*54' test/parity/fixtures/*.json` is 0 for every file
    - `grep -c '^## Phase 54: four-band curve on floors 5–15 (BAND-02) — measured zero: no replay site reaches floor 5' test/parity/FIXTURE-INVENTORY.md` prints 1; the section contains `### The predictor — depth exposure per site` (31 rows, every max depth <= 2), `### The live scan (tools/initiative-fixture-scan.mjs, re-run on the final curve)`, `### Moved set — declared records`, `MOVED SET (0)`, `### Draw-count pins`, `### Byte-identical elsewhere (Phase 54)` (each `grep -c` prints 1); `node tools/fixture-inventory.mjs` then `git diff --stat test/parity/FIXTURE-INVENTORY.md` shows only the appended section; `node --test test/parity/fixture-inventory.test.js` green
    - the ledger carries, in order after the last Rung section and before the v1.2 H2: `#### Change table (BAND-03)` (the table has >= 14 rows naming each Phase 54 constant — `grep -c '^| `WALL_\|^| `BREAKAWAY_\|^| `ENDGAME_' docs/DIFFICULTY-RETUNE.md` >= 14 — plus the two Filter-dial rows (`grep -c '^| `FOE_GRACE_AT_2`' docs/DIFFICULTY-RETUNE.md` and `grep -c '^| `HAZARD_SCALE_AT_START`' docs/DIFFICULTY-RETUNE.md` each grow by 1) and the unchanged Phase 21/27 rows: `grep -c '(unchanged' docs/DIFFICULTY-RETUNE.md` grows by >= 20), the final curve table header `| d | dots | darkBlobs | darkRadius | foePower | hazardScale | foeCap | abilityThreat |`, `#### Miss table (BAND-03)` (contains the per-rung reach ≥5 series — `grep -c 'reach ≥5' docs/DIFFICULTY-RETUNE.md` grows by >= 1 — and, only if the final solo p50 is outside 5–7, the solo-median row naming the untaken Filter notch), `#### Depth-20 slice — identity proof (BAND-01)` (one `empty` diff line per rung taken), `#### Parity — MOVED SET (0) (BAND-02)`; `node --test test/unit/difficulty-retune-ledger.test.js` green; the v1.2 H2 still the last H2
    - `cmp docs/class-pass/v17-p54-after-smoke.json docs/class-pass/v17-p54-rung<last>-smoke.json` is silent; `node -e "const j=require('./docs/class-pass/v17-p54-after-smoke.json');console.log(j.cells.length,j.meta.seeds,j.meta.commit)"` prints `143 5 <final constants commit prefix>`
    - `npm test` fail 0; `npm run build:www` exit 0; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat COMMIT_B -- content/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js src/browser/ mazeworld.html engine/saveState.js` empty
    - `54-02-SUMMARY.md`: `grep -c '^## Success criteria → proof'` 1; `grep -c '^## Constants change table (before → after)'` 1; `grep -c '^## Rung readings'` 1; `grep -c '^## Final AFTER vs BAND-01'` 1; `grep -c '^## Re-pin ledger'` 1; `grep -c '^## Parity — MOVED SET (0)'` 1; `grep -c '^## Gates'` 1; `grep -c '^## Schema'` 1; `grep -c 'AFTER_SHA'` >= 1; `grep -c '^## Human verification (deferred to end of run)'` 1 with at least three Pixel 7 checks
    - the parity commit and the SUMMARY commit end with the two trailer lines
  </acceptance_criteria>
  <done>The final curve's parity moved set is measured three ways and declared as MOVED SET (0); the ledger carries the change table (one row per constant), the miss table, the depth-20 identity proof and the parity subsection; the AFTER smoke is on disk; the SUMMARY names AFTER_SHA for Plan 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (offline, single-process) | Engine constants + test pins + bot transcripts + ledger/inventory docs; no network, no installs, no persisted-schema change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-07 | Tampering | test/parity/prototype-master.js.txt / comparables.js / fixtures | high | mitigate | master hash asserted per rung; `git diff --stat COMMIT_B -- test/parity/harness/comparables.js test/parity/fixtures/` empty; scan Part B diff + parity suite on the final curve |
| T-54-08 | Tampering | a rung leaking past 15 (16–20 no longer identity) | high | mitigate | per-rung `diff` of the depth-20 slice against BEFORE (stop-the-line); `Object.is(…, 1)` pins on 16..20; the 16+ literal pins never re-pinned |
| T-54-09 | Tampering | a rung moving floor 1 (parity exposure) or adding a new Filter dial / crossing a Filter ceiling | high | mitigate | per-rung scan Part B diff empty + the floor-1 literal pin never re-pinned + the depth-20 slice diff; BAND-02 guard's 31-site depth exposure and floor-2 no-fight/no-hazard assertion; FOE_GRACE_AT_2 >= 0.35 / HAZARD_SCALE_AT_START >= 0.3 / FOE_GRACE_AT_1 === 1 / HAZARD_FROM_DEPTH === 2 structural pins; density/dark constants unchanged (git diff); WALL_FOE_POWER_AT_START >= graceFor(4) pin |
| T-54-10 | Repudiation | a dial kept without a readout that moved toward the bands / a readout taken on an uncommitted tree | high | mitigate | constants commit BEFORE readouts (smoke meta.commit provenance); JSDoc cites the rung + readout; four transcripts committed per rung; the class-cell collapse check listed |
| T-54-11 | Tampering | a determinism pin hand-computed or loosened | medium | mitigate | re-measure live via the file's own helpers, comment the rung; `grep -c '^-[^-]'` = 0 on the draw-count pin files |
| T-54-12 | Denial of Service | the ~21-min `--party` run exceeding the Bash timeout / a stuck poll | medium | mitigate | `run_in_background` to a file, polled between steps; never treat a timeout as failure; never lower seeds |
| T-54-13 | Tampering | the frozen instrument (tools/lib/tuning-bot.mjs) edited to make a rung read better | high | mitigate | `git diff --stat COMMIT_B -- tools/lib/tuning-bot.mjs` empty at every commit |
| T-54-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- 1–4 rungs, each: constants + JSDoc citation (Filter rungs: a Phase 54 paragraph appended to the Phase 27 note) → re-pins (BAND_PINS / PHASE_54_PINS / depth-5 determinism specs on band rungs; FILTER_PINS / PHASE_27_PINS / depth-2 hazard pins / humans-t2 + magical-t4 on Filter rungs — never loosened) → green `feat(54-02): rung N` commit → four readouts on that commit → depth-20 slice diff empty + scan Part B diff empty + floor-1 pin green + reach ≥5 recorded + metaParity → `#### Rung N — commit …` with the four transcripts, reading table, class-cell check, what-to-turn-next → `docs(54-02): rung N readouts` commit.
- Stop rule honoured: in-band on solo median / p90 / reach ≥16 / reach ≥20 / party median, or rung 4, or a refused floor-1 / new-Filter-dial / Filter-ceiling escalation (recorded); rung 2 is a Filter rung whenever rung 1 left the median ≤ 4 (USER RULING A).
- Final curve: 16..20 literal 1; foePower non-decreasing 5..20; Wall >= floor-4 grace at the landed FOE_GRACE_AT_2; floor 1 byte-identical; FOE_GRACE_AT_2 >= 0.35, HAZARD_SCALE_AT_START >= 0.3, ramps still 1.0 at 5; density/dark and 21+ dials unchanged.
- Parity: scan Part B diff empty, `node --test test/parity/` green, zero fixture edits, BAND-02 guard green, FIXTURE-INVENTORY Phase 54 section, fixture-inventory generated block byte-identical.
- Ledger: change table (one row per constant incl. the two Filter dials and the unchanged rows), miss table (every miss with untaken rung + reason; the solo-median row only if still outside 5–7), depth-20 + floor-1 identity proof (slice diffs, scan diffs, floor-1 pin), parity subsection; ledger tests green; v1.2 H2 last.
- `v17-p54-after-smoke.json` byte-identical to the final rung's smoke; `npm test` fail 0 and `build:www` exit 0 at every commit; master hash unchanged; every commit carries the two trailers; SUMMARY names AFTER_SHA.
</verification>

<success_criteria>
- SC2 (ROADMAP): `difficultyCurve` is reshaped on 5–15 (step at 5–8, eased slope 9–15, literal identity from 16 so 16–20 is the steepest band by construction), breather floors kept, `DENSITY_CANON_THROUGH_DEPTH = 2` respected, every dial change cited against a bot readout.
- SC3: the AFTER readout (solo + `--party` + class smoke) either lands inside BAND-01's numbers or each miss is recorded with its untaken rung and reason; the change table has one row per constant with before/after.
- BAND-01's yardstick: the depth-20 slice is byte-identical on every rung.
- BAND-02's parity: MOVED SET (0) measured and declared; master never edited.
- SC5: no flat-damage nerf; gates green throughout.
</success_criteria>

<artifacts_this_phase_produces>
See 54-01-PLAN.md's phase-wide table; this plan produces the rows marked 02:

| Artifact | Plan | What it is |
|---|---|---|
| `engine/difficulty.js` — landed `WALL_FOE_POWER_AT_START/END`, `BREAKAWAY_FOE_POWER_AT_START/END` (+ `WALL_HAZARD_SCALE`, the four `*_ABILITY_THREAT_*` constants if a rung took them; + `FOE_GRACE_AT_2` / `HAZARD_SCALE_AT_START` if a Filter rung was taken — USER RULING A — with a `DELIBERATE RULES CHANGE (Phase 54, BAND-02, 2026-09-21)` paragraph APPENDED to their Phase 27 notes), each with `Phase 54 ladder rung N (2026-09-21, BAND-02)` JSDoc lines citing the readout | 02 | the rule, tuned |
| `test/difficulty/difficulty.test.js` `BAND_PINS` (re-pinned per band rung) and `FILTER_PINS` (re-pinned per Filter rung) — the floor-1 literal and the 16+ block untouched | 02 | pins |
| `test/unit/combat-scaling.test.js` `PHASE_54_PINS` (re-pinned per rung; hazard/ability re-scopes only if taken); on Filter rungs `PHASE_27_PINS.FOE_GRACE_AT_2` / `.HAZARD_SCALE_AT_START` and the depth-2 negative-dmgBonus test; `test/unit/movement.test.js` / `test/unit/encounters.test.js` depth-2 hazard pins (Filter hazard rung) and depth-5 canon pins (Wall hazard rung) | 02 | pins |
| `test/determinism/foe-abilities.test.js` — `demons-t5` / `walking-dead-t5` / `beasts-t5` `FULL_FIGHT_PINS` re-measured live on band rungs, `humans-t2` / `magical-t4` on Filter rungs, with `Phase 54 rung N` comments | 02 | draw/outcome pins |
| `readouts/rungN-solo.txt`, `rungN-party.txt`, `rungN-start-depth-20.txt`, `rungN-smoke.txt` (N = 1..rungs taken) | 02 | the evidence |
| `docs/class-pass/v17-p54-rungN-smoke.json` (N = 1..rungs taken) + `v17-p54-after-smoke.json` (byte-identical copy of the last) | 02 | class-matrix yardstick |
| `docs/DIFFICULTY-RETUNE.md` — `#### Rung N — commit <40-hex> (…)` per rung; `#### Change table (BAND-03)` (incl. the `FOE_GRACE_AT_2` / `HAZARD_SCALE_AT_START` rows); `#### Miss table (BAND-03)` (with the per-rung reach ≥5 series); `#### Depth-20 slice — identity proof (BAND-01)` (slice + scan + floor-1 pin); `#### Parity — MOVED SET (0) (BAND-02)` | 02 | the ledger body |
| `test/parity/FIXTURE-INVENTORY.md` — `## Phase 54: four-band curve on floors 5–15 (BAND-02) — measured zero: no replay site reaches floor 5` with `### The predictor — depth exposure per site`, `### The live scan (…)`, `### Moved set — declared records` (`MOVED SET (0)`), `### Draw-count pins`, `### Byte-identical elsewhere (Phase 54)` | 02 | the declaration |
| `tools/initiative-fixture-scan-output.txt` — re-run on the final curve (expected byte-identical) | 02 | the Part B measurement |
| `54-02-SUMMARY.md` (names `AFTER_SHA` = the final constants commit) | 02 | verifier-grade evidence |

Never touched: `test/parity/prototype-master.js.txt`, `test/parity/harness/comparables.js`, `tools/lib/tuning-bot.mjs`, `content/`, `src/browser/`, `mazeworld.html`, `engine/saveState.js`, floor-1 identity (`FOE_GRACE_AT_1`, `HAZARD_FROM_DEPTH`, floor-1 dots/dark), the no-new-Filter-dial constants (`DENSITY_CANON_THROUGH_DEPTH`, `DARK_HOLD_THROUGH_DEPTH`, `ENCOUNTER_DOT_*`, `DARK_*`, `HAZARD_FLAT_THROUGH_DEPTH`), `FOE_GRACE_CANON_FROM_DEPTH` / `HAZARD_CANON_FROM_DEPTH`, `COMBAT_SCALE_FROM_DEPTH` / `*_MAX` / `*_SOFT_K`, the draw-count pin files. Movable by Filter rungs only (USER RULING A): `FOE_GRACE_AT_2` (>= 0.35), `HAZARD_SCALE_AT_START` (>= 0.3).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/54-four-band-retune-and-roster-decision/54-02-SUMMARY.md` when done (Task 3 writes it; it must end with `## Human verification (deferred to end of run)` and name `AFTER_SHA`).
</output>