---
phase: 52-foe-cadence-damage-curve
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - engine/combat.js
  - content/bestiary.js
  - tools/bestiary-yardstick.mjs
  - content/BESTIARY-REBALANCE.md
  - test/unit/foe-crit-curve.test.js
  - test/unit/combat.test.js
  - test/unit/identity-contract.test.js
  - test/unit/content-tables.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/divergence-records.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - tools/initiative-fixture-scan-output.txt
  - tools/damage-curve-audit-output.txt
autonomous: false
requirements: [DMG-02, DMG-01]
tags: [combat, crit, damage-curve, herman, parity-fixtures, draw-count-pins, engine-gate]

must_haves:
  truths:
    - "A foe critical doubles the DICE, not the whole sum: hero branch, party-member branch and the pursuit strike all compute `foeLevelBase(f) + (f.dmgBonus || 0) + 2 × dice` on a crit (natural 1, or a roll of 2 vs a Soldier on the hero/pursuit branches); the weakened/shrunk/hamstrung halvings and the Brace/Hardiness/hide/ward/armour pipeline keep their order (D-08)"
    - "Herman 'strikes as a level five' is exactly what the code does: both Herman rows carry `sp.strikesAs: 5` and no `sp.dmg`; a Herman hit is 25 + d6 (26–31) at tier 4 and tier 5, a Herman crit is at most 37 (was 82 / 100) — a floor-5 crit cannot one-shot a full-HP level-5 hero (D-09, DMG-02, SC4)"
    - "Per-swing draw shape is unchanged: a crit hit draws exactly to-hit + one damage die, identical to a non-crit hit; every per-foeTurn draw pin (foe-turn-draw-count.test.js Sections 1/3/4/5/6) and the OPENER_DRAWS table are byte-identical; the seven FULL_FIGHTS totals move only if a crit's smaller number changed a fight's length (predicted: none) — re-measured, never loosened (D-10)"
    - "The moved parity set is measured (initiative-fixture-scan Part B against the edited engine + the parity suite), declared per site with `+52` records whose after/stateAfter are engine-measured, only those fixtures regenerated, guarded by a Phase 52 MOVED SET test; predicted set: exactly `action-script.combat.json#lose` (D-10)"
    - "`test/parity/prototype-master.js.txt` is untouched (hash a1f4d0dc…); `test/parity/harness/comparables.js` is untouched (no carve-out); `npm test` fail 0 and `npm run build:www` green at every commit"
    - "The AFTER damage-curve reading (`--rule=dice` on the edited bestiary) is appended to `tools/damage-curve-audit-output.txt`; the cadence audit re-run is byte-identical to Plan 01's (the crit rule changed no cadence); the still-flagged rows are tabulated by category and put to a decision checkpoint before Plan 03 acts on them (D-07)"
  artifacts:
    - path: "engine/combat.js"
      provides: "exported `foeLevelBase(f)`; the dice-doubling crit at all three foe-damage sites; DELIBERATE RULES CHANGE (Phase 52, DMG-02, 2026-09-20) paragraphs; header flag list includes sp.strikesAs"
    - path: "content/bestiary.js"
      provides: "Herman ×2 with `sp.strikesAs: 5` (flat-25 notation gone)"
    - path: "test/unit/foe-crit-curve.test.js"
      provides: "DMG-02 pins: hero/member/pursuit/Soldier crit = dice doubled; Herman strikesAs T4/T5 + crit <= 37; crit draws 2; halving order; foeLevelBase"
    - path: "test/parity/fixtures/action-script.combat.json"
      provides: "`+52` on every measured moved holder (predicted: #lose, phase 24+31+51 → 24+31+51+52)"
    - path: "test/parity/divergence-records.test.js"
      provides: "DMG-02 MOVED SET guard (holders declaring 52 == the measured set)"
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "## Phase 52 section: rule, predictor (crit exposure per site), the scan diff, moved records, draw-pin statement"
    - path: "content/BESTIARY-REBALANCE.md"
      provides: "regenerated yardstick AFTER block; `### Phase 52 addendum (DMG-02, 2026-09-20)` with the Herman before/after rows and the still-flagged category table"
    - path: "tools/damage-curve-audit-output.txt"
      provides: "the AFTER section (`# damage-curve-audit — rule=dice`) appended below Plan 01's BEFORE section"
  key_links:
    - from: "engine/combat.js#foeTurn (hero + member branches) and #pursuitStrike"
      to: "engine/combat.js#foeLevelBase + applyFoeDamageToPlayer"
      via: "one shared level-base read, `crit` decided before the damage line, dice doubled inside the sum, then the unchanged halving/pipeline order"
      why: "three sites with one formula is the only way the audit's analytic max can be trusted for every foe hit, pursuit included"
    - from: "content/bestiary.js Herman rows"
      to: "engine/combat.js#foeLevelBase and tools/bestiary-yardstick.mjs"
      via: "`sp.strikesAs`"
      why: "the rulebook note and the code now agree; the yardstick doc block must be regenerated in the same commit or the doc-consistency test goes red"
    - from: "tools/initiative-fixture-scan.mjs (Part B, re-run on the edited engine)"
      to: "test/parity/fixtures/action-script.combat.json + divergence-records.test.js + FIXTURE-INVENTORY.md"
      via: "measured after/stateAfter values, never hand-typed"
      why: "the engine gate: declared, measured, regenerated only where moved"
---

<objective>
Land the two DMG-02 fixes in ONE executor and ONE green commit: the general crit rule (a foe crit doubles the dice, not the `lvl² + dmgBonus + dice` sum) at all three foe-damage sites, and Herman's `sp.strikesAs: 5` replacing his flat 25 — together with every re-pinned crit value, the new DMG-02 pins, the measured moved parity set declared and regenerated, the Phase 52 MOVED SET guard, the regenerated yardstick block and the FIXTURE-INVENTORY / BESTIARY-REBALANCE records. Then take the AFTER damage-curve reading and put the rows that are STILL flagged to a decision checkpoint (trim now vs hand to Phase 54) so Plan 03 acts on a ruling, not a guess.

Purpose: SC4/SC5 — "no single hit can spike past a smooth depth-scaled damage curve"; the Herman floor-5 crit (82) becomes at most 37; the tier-5 d6 crit cliff (52–62) becomes 27–37; a level-1 Bat/Rat barely moves (4 → 3). Deliberate canon divergence under the greenfield ruling: measured first, declared per moved fixture, master never edited.

Output: `engine/combat.js` (foeLevelBase + three sites), `content/bestiary.js` (Herman ×2), `tools/bestiary-yardstick.mjs`, `test/unit/foe-crit-curve.test.js`, re-pins in `combat.test.js`/`identity-contract.test.js`, `content-tables.test.js` pin, `action-script.combat.json` `+52` record(s), `divergence-records.test.js` guard, `FIXTURE-INVENTORY.md` Phase 52 section, `BESTIARY-REBALANCE.md` (AFTER block + Phase 52 addendum), `tools/damage-curve-audit-output.txt` AFTER section, `tools/initiative-fixture-scan-output.txt` re-run.

Decisions implemented (52-CONTEXT.md, quoted): D-08 "General crit rule: a foe crit doubles the DICE, not the whole sum — `crit → lvl² + dmgBonus + 2 × dice` (today `2 × (lvl² + dmgBonus + dice)`), on both the hero branch (`engine/combat.js:2541-2552`, including the Soldier's roll ≤ 2 crit range) and the party-member branch (`:2460-2472`). The `weakened`/`shrunk`/`hamstrung` halvings and Brace/Hardiness/ward/armour pipeline are untouched in order." — planner extension (consistency, same rule): the third foe-hit site `pursuitStrike` (`:947-952`, same formula, same crit range) gets the same treatment so the audit's "max single hit" is true for a pursuit strike too; no fixture-exposed foe carries `sp.pursues`, so it moves nothing. D-09 "Herman: `sp.strikesAs: 5` replaces `dmg: { bonus: 25 }` in `content/bestiary.js` (both Herman rows, tiers 4 and 5): the `lvl²` term reads `sp.strikesAs` when present, the dice stay `d6` (the default when `sp.dmg` is absent). Tier 4: 41 → 26–31 (crit 27–37); tier 5: 50 → 26–31." D-10 "measure with `tools/initiative-fixture-scan.mjs` Part B run against the edited engine … declare each with a `+52` action-path record (`after`/`stateAfter` measured, never hand-typed), extend the `divergence-records.test.js` MOVED SET guard (a Phase 52 set), add a FIXTURE-INVENTORY.md Phase 52 section; crit pins … re-pinned to the new formula; `test/parity/prototype-master.js.txt` never edited; no comparables carve-out." Claude's Discretion (resolved): `foeLevelBase(f)` shared helper (the CONTEXT's preferred form). D-07 (the checkpoint): "Every flagged row is fixed (DMG-02): … any row still flagged afterwards gets its own before/after line in `content/BESTIARY-REBALANCE.md`, or an explicit 'left as a deliberate deep-tier threat' ruling if it is tier 5 (Endgame band) — recorded, never silently skipped."
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-foe-cadence-damage-curve/52-CONTEXT.md
@.planning/phases/52-foe-cadence-damage-curve/52-01-SUMMARY.md
@.planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md
</context>

<ground_rules>
- ONE executor touches `engine/combat.js` in this wave (this plan). Task 1 lands the engine edit, the content edit, every re-pin, the new pins, the measured fixture records and the regenerated yardstick block in ONE commit — the parity suite and the yardstick doc-consistency test are red in between, and a red tree must never be committed.
- Measured, never hand-typed: every `after`/`stateAfter` value, every re-pinned number comes from running the engine/test helper, not from arithmetic in your head. The planner's predictions below are expectations to VERIFY, not values to type.
- `test/parity/prototype-master.js.txt` is NEVER edited: `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every task. `test/parity/harness/comparables.js` is untouched (`git diff --stat <phase-start> -- test/parity/harness/comparables.js` empty).
- Draw-count pins are NEVER loosened. The crit rule and `strikesAs` add zero draws per swing by construction (same to-hit die, same single damage die in the same position). If a per-foeTurn pin fails, the ENGINE edit is wrong — fix the engine. Herman's new d6 is a genuinely new draw only in Herman fights, and no fixture or draw pin contains a Herman (grep-confirmed).
- Commit trailers: `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
- Deferred, do not touch: player-side crit (`weaponCrit`, `playerStrike` ~L783, member ally crit ~L1811); the ability cadence ×2 knob; the four-band curve (Phase 54).
</ground_rules>

<planner_predictions>
Measured by the planner on the phase-start tree with a scratch replay of every combat/magic replay site (engine-only, counting `struckByFoe`/`memberStruck` events with `critical`/`soldierCrit`): exactly ONE site contains a foe crit hit — `action-script.combat.json#lose` (seed 14, Fighter Soldier Fridgian): the Shriek's natural 1 at action index 3 (`roll 1`, dmg 2 after the Fridgian hide's −2; base 1 + d6 1 → whole-sum crit 4 → hide → 2; under the dice rule 1 + 2×1 = 3 → hide → 1). Expected end field: `wp` 50 → 51; `sp/kills/rations` and `dead: false` unchanged; `fromAction` stays 0 (the record already declares from action 0). No other combat/magic site has a foe crit hit; the only roll-less hit event anywhere is `lose-apprentice`'s `armorSoaked` amount 2 — a non-crit Bat/Rat blow (a crit would be 4). Chargen/movement/economy/encounters sites never enter combat. The seven FULL_FIGHTS seeds: 14 (one crit hit, hero ends 50/52) and 17 (two crit hits — Viper dmg 4, Shriek dmg 10 — hero ends 10/40) contain crit hits but neither fight ends anywhere near a death boundary, so `totalDraws`/`attacks`/`outcome` are predicted unchanged (only the hero's end wp shifts by +1 and +2). Seeds 3/303/8/127/1119: no crit hit. Verify all of this — it is a prediction.
</planner_predictions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The crit rule + Herman strikesAs — engine edit, re-pins, new pins, measured fixtures declared + regenerated, yardstick AFTER block, Phase 52 guard (ONE commit)</name>
  <files>engine/combat.js, content/bestiary.js, tools/bestiary-yardstick.mjs, content/BESTIARY-REBALANCE.md, test/unit/foe-crit-curve.test.js, test/unit/combat.test.js, test/unit/identity-contract.test.js, test/unit/content-tables.test.js, test/parity/fixtures/action-script.combat.json, test/parity/divergence-records.test.js, tools/initiative-fixture-scan-output.txt</files>
  <read_first>
    - engine/combat.js lines 20-58 (header: the mechanical `sp.*` flag list at ~L30-31 — add `sp.strikesAs`), 905-955 (`pursuitStrike`: the damage line at 947, halvings, the crit doubling at 952, `applyFoeDamageToPlayer` call), 2241-2260 (`foeTurn` JSDoc), 2455-2497 (member branch: `mDmg` at 2460, halvings 2461-2471, the crit doubling at 2472, Brace, `memberStruck` payload `critical: mRoll === 1`), 2528-2556 (hero branch: `dmg` at 2541, halvings, the crit doubling at 2552, `applyFoeDamageToPlayer(... { dmg, roll, need, needMods })`), 2032-2210 (`applyFoeDamageToPlayer`: Hardiness/hide/halfNext/ward/soak pipeline, `struckByFoe` payload `critical: roll === 1`, `soldierCrit` flag — all unchanged), 117-160 (the existing `DELIBERATE RULES CHANGE (Phase 51, INIT-01, 2026-09-20)` paragraph style to copy)
    - engine/difficulty.js lines 460-472 (`foeDmgBonusFor(lvl, curve)` — stays keyed to the real `lvl`; strikesAs affects only the level-base term)
    - engine/dice.js (`rollDice` — an `n: 0` notation draws nothing; a missing `sp.dmg` means `rng.d(6)`, one draw)
    - content/bestiary.js lines 1-20 (header on flat values → zero-dice notation; update the Herman mention), 107-111 (Craig + Herman T4 row 109, Herman T5 row 111)
    - tools/bestiary-yardstick.mjs lines 150-185 (foe-side formula `L * L + avgDice` and the `dmg` column) and its header; test/unit/bestiary-yardstick.test.js lines 172-192 (precision test uses an inline flat-25 row — unaffected) and 268-296 (the "D-04 doc consistency" test: the AFTER block between `<!-- yardstick:after:begin -->`/`<!-- yardstick:after:end -->` must equal a fresh `toMarkdown(computeYardstick(BESTIARY, { mechanics: "canon" }))`)
    - content/BESTIARY-REBALANCE.md lines 253-335 (the AFTER block + markers) and 389-402 ("Regenerating this document": regenerate ONLY the AFTER block, never the BEFORE block)
    - test/unit/combat.test.js lines 652-663 ("foeTurn: a landed critical hit damages the player…" — pins dmg 14 with fakeRng([1, 6]); new value 1 + 2×6 = 13, comment must be rewritten), 885-895 (`applyFoeDamageToPlayer` critical flag — unchanged), 1-60 (helpers)
    - test/unit/identity-contract.test.js lines 568-595 (Soldier `bad` clause: `soldierHit.dmg === controlHit.dmg * 2` with fakeRng([2, 3]) — new relation: control 1 + 3 = 4, soldier 1 + 2×3 = 7)
    - test/unit/foe-turn-draw-count.test.js lines 154-232 (Section 1 micro pins — must not move), 266-302 (FULL_FIGHTS rows — predicted not to move), 414-440 (runner), 635-700 (OPENER_DRAWS + Section 6)
    - test/unit/feedback-payload.test.js lines 340-392 (soldierCrit flags — verify still pass), test/unit/afraid.test.js 172-186, test/unit/identity-race.test.js 124-160 (hide on crits — verify), test/determinism/foe-abilities.test.js (seeded tier 4/5 fights — draw pins must not move)
    - test/parity/fixtures/action-script.combat.json (`lose` record: phase `24+31+51`, `fromAction 0`, `fields` wp/sp/kills/rations, `after {wp: 50 …}`, `stateAfter {dead: false}`, rationale with per-phase paragraphs), test/parity/fixtures/action-script.schema.md (action-path record rules: `before`/`after` verbatim, never hand-typed)
    - test/parity/divergence-records.test.js lines 147-172 (the INIT-01 guard — copy its shape for a Phase 52 guard; note `holderId` vocabulary `action-script.combat.json#lose`)
    - tools/initiative-fixture-scan.mjs lines 1-60 (Part A invariant vs Part B measured) and tools/initiative-fixture-scan-output.txt (the committed Phase 51 AFTER; `MOVED SET (3)` and `INITIATIVE EXPOSURE` lines are Part A and must stay byte-identical; `fields.after (engine @end)` for #lose is the Part B value that moves)
    - test/parity/combat-parity.test.js lines 60-80 and any `lose` comments (Phase 51 touched a comment there — check for "wp 51->50"-style prose that must now read 50->51)
    - .planning/phases/52-foe-cadence-damage-curve/52-01-SUMMARY.md (Plan 01's committed BEFORE numbers)
  </read_first>
  <behavior>
    New `test/unit/foe-crit-curve.test.js` (copy the fakeRng/fixedState/fixedFoe/fixedCombat helpers from test/unit/foe-abilities.test.js verbatim; hero `fixedFighter` has `sub: "Soldier"` by default — override `sub: "Knight"` where a non-Soldier is needed):
    - "DMG-02 (hero branch): a natural 1 doubles the dice, not the level base — tier-5 d6 foe": Knight hero, `fixedFoe({ lvl: 5 })`, fakeRng([1, 6]) → `struckByFoe.dmg === 37`, `critical === true`, exactly 2 draws; control fakeRng([3, 6]) on a fresh state → dmg 31, `critical === false`.
    - "DMG-02 (hero branch): a level-1 foe's crit moves by one — 1 + 2×d6": fakeRng([1, 6]) → 13 (the old 14); Bat/Rat shape `sp: { atk: 2, dmg: {n:0,sides:0,bonus:1} }` fakeRng([1, 20]) → first swing dmg 3 (was 4), second swing misses; exactly 2 draws.
    - "DMG-02 (Soldier): a roll of 2 vs a Soldier doubles the dice only; a Knight's 2 is plain": Soldier fakeRng([2, 3]) → dmg 7, `soldierCrit === true`; Knight fakeRng([2, 3]) → dmg 4, no `soldierCrit`.
    - "DMG-02 (member branch): mRoll 1 doubles the dice only": party of one (`state.party = [fixedMember()]`, `state.combat.allies = [fixedAlly()]` — copy the mould from test/unit/party-combat.test.js's member-branch tests; pickFoeTarget must target the member — follow that file's setup), `fixedFoe({ lvl: 3 })`, a roll of 1 then dice 4 → `memberStruck.dmg === 9 + 8 = 17`, `critical === true`.
    - "DMG-02 (pursuit strike): a pursuer's natural 1 doubles the dice only": `fixedFoe({ lvl: 2, sp: { pursues: true } })`, drive `flee` so the pursuit strike runs (copy the setup from the existing pursuit tests — grep `foePursued` in test/unit/*.test.js) → `struckByFoe` with `critical: true` and `dmg === 4 + 2 × dice`.
    - "DMG-02 (Herman): strikesAs 5 — both live Herman rows hit 25 + d6 and crit at most 37": foes built from the LIVE BESTIARY Herman rows (find by `n === "Herman"` in `BESTIARY.Humans[3]` and `[4]`, copy `sp`), `lvl` 4 and 5 respectively; fakeRng([3, 6]) → dmg 31 for both; fakeRng([1, 6]) → 37 for both; exactly 2 draws each (the d6 is now a real draw for Herman); assert `BESTIARY` Herman rows have `sp.strikesAs === 5` and no `dmg` key.
    - "DMG-02 draws: a crit hit draws exactly 2 (to-hit + dice), identical to a non-crit hit; a `0d0+1` crit draws 1 (to-hit only)": countingRng-style assertions via fakeRng exhaustion.
    - "DMG-02 pipeline order: weakened halves AFTER the dice doubling": `state.combat.weakened = true` (check the exact field the hero branch reads — `C.weakened`), lvl 1, fakeRng([1, 5]) → `ceil((1 + 10) / 2) = 6` (old rule gave `2 × ceil((1 + 5) / 2) = 6` too — so ALSO pin a case where they differ: lvl 5, dice 4: new `ceil((25 + 8) / 2) = 17` vs old `2 × ceil(29 / 2) = 30`).
    - "foeLevelBase: reads sp.strikesAs when present, else lvl²": `foeLevelBase({ lvl: 3 }) === 9`, `foeLevelBase({ lvl: 4, sp: { strikesAs: 5 } }) === 25`, `foeLevelBase({ lvl: 4, sp: {} }) === 16`.
    Re-pins: combat.test.js L652-663 → 13 with the comment rewritten to the dice rule; identity-contract.test.js Soldier clause → assert `soldierHit.dmg === controlHit.dmg + 3` (the doubled die adds one more die, 3) or the literal 7 vs 4, and rename the clause to "a foe's roll of 2 crits a Soldier, doubling the dice". content-tables.test.js: new test "BESTIARY Phase 52 / DMG-02: both Herman rows carry sp.strikesAs 5 and no sp.dmg; no other row carries strikesAs".
  </behavior>
  <action>
    Step 0 — pre-edit sanity (no bytes changed yet): `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` must be empty (the committed Phase 51 AFTER equals the current tree); record `git rev-parse HEAD` as the pre-edit commit.

    Step 1 — engine. In `engine/combat.js`: (a) header comment ~L30-31: add `sp.strikesAs` to the list of `sp.*` fields with a mechanical effect (Phase 52). (b) Add `export function foeLevelBase(f)` (place it just above `pursuitStrike`'s JSDoc, or beside the other small exported helpers near `applyFoeDamageToPlayer` — one definition): returns `f.sp.strikesAs * f.sp.strikesAs` when `f.sp && f.sp.strikesAs` is present, else `f.lvl * f.lvl`; JSDoc carries a `DELIBERATE RULES CHANGE (Phase 52, DMG-02, 2026-09-20)` paragraph: the level-base term is what "strikes as a level five" means; `foeDmgBonusFor` stays keyed to the real `lvl` (a Herman's deep-floor bonus scales with his tier, not his strike level). (c) At the three foe-damage sites — `pursuitStrike` (~L947-952), the member branch (~L2460-2472), the hero branch (~L2541-2552) — restructure identically: decide the crit BEFORE the damage line (`const crit = roll === 1 || (roll <= 2 && c.sub === "Soldier")` on the hero/pursuit sites; `const mCrit = mRoll === 1` on the member site); draw the damage dice into a local (`const dice = f.sp && f.sp.dmg ? rollDice(rng, f.sp.dmg) : rng.d(6)` — the SAME single draw in the SAME position as today, immediately after the to-hit roll and before anything else draws); compute `let dmg = foeLevelBase(f) + (f.dmgBonus || 0) + (crit ? 2 * dice : dice)`; keep the `C.weakened` / `f.shrunk` / `f.hamstrung` halvings in their existing order and the Brace buffer (member) exactly as they are; DELETE the trailing whole-sum doubling statement at each site (the one that used to run after the halvings); keep the `applyFoeDamageToPlayer(...)` call and the `memberStruck` payload (`critical: mRoll === 1`) byte-identical. Add a `DELIBERATE RULES CHANGE (Phase 52, DMG-02, 2026-09-20)` comment paragraph at each of the three sites in the style of the Phase 51 paragraphs (what changed, why — the user's floor-5 Herman 80 and the tier-5 d6 cliff — and the zero-draw statement). Do NOT touch `playerStrike`'s crit (~L783), the member-ally crit (~L1811) or `weaponCrit` (Deferred Ideas).

    Step 2 — content + yardstick. `content/bestiary.js`: both Herman rows (L109 tier 4, L111 tier 5) → `sp: { invis: true, ar: 15, strikesAs: 5, note: "turns invisible; strikes as a level five" }` (the zero-dice flat-25 `dmg` notation is removed; `wp 36`, `sz H`, `i 9`, `ar 15`, the note text all unchanged); one `// DELIBERATE RULES CHANGE (Phase 52, DMG-02, 2026-09-20): flat 25 -> sp.strikesAs 5 (+ the default d6); see content/BESTIARY-REBALANCE.md Phase 52 addendum` comment line above each; update the file header's flat-value sentence (~L6-7) to say the only remaining zero-dice flat is the Bat/Rat's 1 and that Herman's "strikes as" is `sp.strikesAs`. `tools/bestiary-yardstick.mjs`: the foe-side formula reads the level base as `sp.strikesAs ? strikesAs² : L*L` (mirror of `foeLevelBase`, say so in the comment at ~L161-162); render a strikesAs row's `dmg` column as `d6 (as L5)` (discretion — keep `0d0+25`-style rendering for real notations so the precision test's inline row still prints `0d0+25`). Regenerate the AFTER block: run `node tools/bestiary-yardstick.mjs` and paste its stdout verbatim between `<!-- yardstick:after:begin -->` and `<!-- yardstick:after:end -->` in `content/BESTIARY-REBALANCE.md`, replacing the old block exactly (including the `Mechanics: canon | …` summary line); never touch the BEFORE block.

    Step 3 — tests. Create `test/unit/foe-crit-curve.test.js` per `<behavior>`; re-pin `combat.test.js` L652-663 and `identity-contract.test.js`'s Soldier clause; add the Herman content pin in `content-tables.test.js`. Run `npm test`. Classify every remaining failure: a crit VALUE pin → re-measure through the test's own helper and re-pin with a comment naming Phase 52; a per-foeTurn DRAW pin (foe-turn-draw-count.test.js Sections 1/3/4/5/6, test/determinism/foe-abilities.test.js, identity-race/normalizeTarget sequences) → the engine edit is wrong, fix the engine; a FULL_FIGHTS row → allowed to move ONLY if `runFullFight` shows a different `attacks`/outcome caused by the smaller crit number (predicted: none — if one moves, re-measure and record old→new in FIXTURE-INVENTORY with the seed's crit hits listed); parity-suite failures → the moved set (Step 4).

    Step 4 — measure the moved set. Run `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt` and `git diff tools/initiative-fixture-scan-output.txt`: the Part A columns (`first live-fight round advance`, `moved`), the `MOVED SET (3): …` line and the `INITIATIVE EXPOSURE: 3 of 31` line MUST be byte-identical (the crit rule changes no round advance); the Part B changes (`firstDivergentAction`/`maxRound` columns, and the `fields.after (engine @end)` / `state.after` lines in "Record values") name the moved sites — predicted: only `### action-script.combat.json#lose` with `fields.after` wp 50 → 51. Cross-check with the parity failures from Step 3 (`node --test test/parity/`): the set of failing sites must equal the scan's changed sites; a site with `firstDivergentAction` already 0 in Phase 51 (`parley`, `cast-damage`) can only surface through the parity tests — confirm both stay green. Also capture the crit-exposure predictor for the inventory: for every combat/magic site, the count of `struckByFoe`/`memberStruck` events with `critical`/`soldierCrit` and the count of roll-less hit events (`armorSoaked`/`wardAbsorbed`/`wardReflected`) under the PRE-edit engine (a scratch replay via `newRun`/`applyStartCombat`/`applyAction`, not committed as a tool) — record the per-site table in FIXTURE-INVENTORY (Task 2). Expected: `lose` 1 crit hit; `lose-apprentice` 0 crit + 1 opaque soak (amount 2 = non-crit Bat/Rat); all others 0.

    Step 5 — declare + regenerate ONLY the moved holders. For `#lose` (and any other measured mover): `phase` → append `+52` (e.g. `24+31+51+52`), `requirements` += `"DMG-02"`, `after`/`stateAfter` ← the measured engine end values (from the scan's Record values or the failing test's actual), `rationale` += a Phase 52 paragraph: "Phase 52 (DMG-02, 2026-09-20): a foe critical now doubles the damage dice, not the whole `lvl² + dmgBonus + dice` sum. This scenario's Shriek rolls a natural 1 at action 3: whole-sum crit 2 × (1 + 1) = 4 minus the Fridgian hide's 2 = 2; dice-doubled crit 1 + 2 × 1 = 3 minus 2 = 1 — the hero ends one hp higher (wp 50 → 51). Zero draw change (the same to-hit die and the same single damage die). Measured by tools/initiative-fixture-scan.mjs Part B on the edited engine — never hand-typed." A site with no record that moves gets a NEW `kind: "action-path"` record with `fromAction` = the scan's `firstDivergentAction`, `before` = `fields.before (prototype @end)`, `after` = `fields.after (engine @end)`, `stateFields ["dead"]`. Add the guard in `test/parity/divergence-records.test.js`: test "DMG-02: the holders declaring Phase 52 are exactly the measured moved set" — the INIT-01 test's shape, but the expected set is the literal measured holder-id list (there is no crit predictor line in the initiative scan; the comment names the measurement: the Part B diff at this commit + the parity suite) — `assert.deepStrictEqual([...declared].sort(), EXPECTED)` where `EXPECTED` = the measured list (predicted `["action-script.combat.json#lose"]`). Fix any stale prose in `combat-parity.test.js` about `lose`'s end wp.

    Step 6 — green, then ONE commit: `npm test` fail 0; `node --test test/parity/` green; `git hash-object test/parity/prototype-master.js.txt` unchanged; `git diff --stat <phase-start> -- test/parity/harness/comparables.js` empty; the per-foeTurn draw pins unchanged (`git diff <pre-edit> -- test/unit/foe-turn-draw-count.test.js | grep -c '^-[^-]'` prints 0 — no pinned line removed; an ADDED crit-draw pin there is fine but keep the new pins in foe-crit-curve.test.js). Commit everything from Steps 1–5 as `feat(52-02): crit doubles the dice + Herman strikes as a level five — engine, re-pins, measured fixtures declared + regenerated (DMG-02)`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -12 && node --test test/unit/foe-crit-curve.test.js test/parity/divergence-records.test.js test/parity/combat-parity.test.js test/parity/full-suite.test.js test/unit/bestiary-yardstick.test.js test/unit/foe-turn-draw-count.test.js && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` reports fail 0 (paste the totals); `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat <phase-start-sha> -- test/parity/harness/comparables.js` prints nothing
    - `grep -c '^export function foeLevelBase' engine/combat.js` prints 1; `grep -c 'foeLevelBase(' engine/combat.js` >= 4 (definition + pursuit + member + hero)
    - `awk '/^function pursuitStrike/,/^}/' engine/combat.js | grep -v '^\s*//' | grep -c '\*= 2'` prints 0 and `awk '/^export function foeTurn/,/^}/' engine/combat.js | grep -v '^\s*//' | grep -c '\*= 2'` prints 0 (no whole-sum doubling left at the three foe sites); `awk '/^export function playerStrike/,/^}/' engine/combat.js | grep -c 'if (crit) dmg \*= 2;'` prints 1 (the player-side crit at ~L783 is untouched)
    - `grep -c 'DELIBERATE RULES CHANGE (Phase 52, DMG-02, 2026-09-20)' engine/combat.js` >= 4 (helper + three sites)
    - `grep -c 'strikesAs: 5' content/bestiary.js` prints 2; `grep -c 'sides: 0, bonus: 25' content/bestiary.js` prints 0; `grep -c 'sides: 0, bonus: 1 }' content/bestiary.js` prints 1 (Bat/Rat untouched)
    - `grep -c 'strikesAs' tools/bestiary-yardstick.mjs` >= 1; `node --test test/unit/bestiary-yardstick.test.js` green (the AFTER block was regenerated in this commit); `grep -c 'as L5' content/BESTIARY-REBALANCE.md` >= 2 (both Herman rows in the regenerated block)
    - `grep -c '^test("DMG-02' test/unit/foe-crit-curve.test.js` >= 8 and `grep -c '^test("foeLevelBase' test/unit/foe-crit-curve.test.js` prints 1; the tests assert the literals 37 (tier-5 crit / Herman crit), 13 (level-1 crit) and 7 vs 4 (Soldier)
    - `grep -c 'dmg === 14' test/unit/combat.test.js` prints 0 and `grep -c 'dmg === 13' test/unit/combat.test.js` >= 1
    - `git diff <pre-edit-sha> -- test/unit/foe-turn-draw-count.test.js | grep -c '^-[^-]'` prints 0 (no draw pin removed or changed); `node --test test/unit/foe-turn-draw-count.test.js test/determinism/foe-abilities.test.js` green
    - `git diff <pre-edit-sha> -- tools/initiative-fixture-scan-output.txt | grep -c '^[-+].*| [0-9]* | \(true\|false\) |'` — every changed table row differs ONLY in the firstDivergentAction/maxRound columns (verify by eye and state it); `grep -c '^MOVED SET (3): action-script.combat.json#lose, action-script.combat.json#lose-apprentice, action-script.combat.json#lose-plain$' tools/initiative-fixture-scan-output.txt` prints 1; `grep -c '^INITIATIVE EXPOSURE: 3 of 31 replay sites$'` prints 1
    - `node -e "const j=require('./test/parity/fixtures/action-script.combat.json');console.log(j.scenarios.filter(s=>String(s.divergence?.phase||'').split('+').includes('52')).map(s=>s.name).join(','))"` prints exactly the measured moved set (predicted `lose`); for `lose`: `divergence.phase` is `24+31+51+52`, `requirements` includes `DMG-02`, `after.wp` equals the scan's `fields.after` wp (predicted 51)
    - `grep -c 'DMG-02: the holders declaring Phase 52 are exactly the measured moved set' test/parity/divergence-records.test.js` prints 1; `node --test test/parity/divergence-records.test.js` green
    - exactly ONE commit for this task (`git log --oneline <pre-edit-sha>..HEAD | wc -l` prints 1)
  </acceptance_criteria>
  <done>The crit rule and Herman's strikesAs are live at all three foe-damage sites with zero draw-shape change; every crit value pin is re-pinned and the new DMG-02 pins pass; the measured moved set (predicted: `#lose` only) is declared and regenerated with a standing guard; yardstick block regenerated; master and comparables untouched; one green commit.</done>
</task>

<task type="auto">
  <name>Task 2: Records — FIXTURE-INVENTORY Phase 52 section, BESTIARY-REBALANCE Phase 52 addendum (Herman + the still-flagged category table), the AFTER damage-curve section, cadence-audit invariance, gates</name>
  <files>test/parity/FIXTURE-INVENTORY.md, content/BESTIARY-REBALANCE.md, tools/damage-curve-audit-output.txt</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md lines 1750-1900 (the Phase 51 section — the style: rule paragraph, "### The live scan" with the output quoted, the Part A/Part B note, "### Moved set — declared records" table, the shared rationale text, "### Draw-count pins re-pinned")
    - test/parity/fixture-inventory.test.js line 209 (the generated block check — do not edit the generated roster block)
    - content/BESTIARY-REBALANCE.md lines 224-240 (Review verdicts: Herman L234 "UNCHANGED — flat 25 base damage is explicit rulebook text"), 403-452 (Change ledger + "Unchanged by decision" Herman row L434), 453-500 (the Phase 27 addendum — the style to copy for a Phase 52 addendum: what changed, why, yardstick rows from the regenerated AFTER block, pointer to the ledger)
    - tools/damage-curve-audit-output.txt (Plan 01's BEFORE section — the `## Flagged (rule=whole)` table and the `## Herman` block) and tools/damage-curve-audit.mjs header (the `--rule=dice` run appends the AFTER section)
    - tools/cadence-audit-output.txt (Plan 01's committed readout — must be byte-identical when re-run now)
    - .planning/phases/52-foe-cadence-damage-curve/52-01-SUMMARY.md and this plan's Task 1 commit (`git show --stat HEAD`)
  </read_first>
  <action>
    (1) Append the AFTER damage-curve section: `printf '\n' >> tools/damage-curve-audit-output.txt && node tools/damage-curve-audit.mjs --rule=dice >> tools/damage-curve-audit-output.txt`. The file now holds the BEFORE section (`# damage-curve-audit — rule=whole`, Plan 01) followed by the AFTER section (`# damage-curve-audit — rule=dice`, this commit). Read the AFTER `## Herman` block: tier-4/tier-5 critMax must be 37 (hitMax 31). Read the AFTER `## Flagged (rule=dice)` table and its `FLAGGED: … (deep-tier a, level-base b, row-dice c, bolt d)` line — these are the D-07 "still flagged afterwards" rows.

    (2) Cadence invariance: `node tools/cadence-audit.mjs | diff - tools/cadence-audit-output.txt` must be EMPTY — the crit rule adds no draws and changes no to-hit outcome, so every sampled fight's attack counts are identical (a hero's HP now differs after a crit, which could in principle change a fight's length; if the diff is NOT empty, inspect: only rows whose fight length changed may differ, the INVARIANT lines must still read HOLDS, and the committed file is NOT overwritten here — Plan 03 re-quotes; record the finding in the SUMMARY).

    (3) `test/parity/FIXTURE-INVENTORY.md`: append (after the Phase 51 section, before EOF) `## Phase 52: crit doubles the dice + Herman strikes as a level five (DMG-02) — measured moved set, declared per site`: the rule paragraph (old vs new formula at the three sites; Herman `strikesAs`; zero draw change per swing; Herman's d6 a new draw only in Herman fights, no fixture/pin contains one); `### The predictor — crit exposure per site` (the per-site table from Task 1 Step 4: crit hits / opaque hits / Herman seen, for all 10 combat+magic sites, and a line that chargen/movement/economy/encounters never enter combat); `### The live scan (tools/initiative-fixture-scan.mjs, re-run on the edited engine)` quoting the `git diff` of the output (Part A byte-identical: the MOVED SET (3)/INITIATIVE EXPOSURE lines and the round-advance columns; Part B moved rows); `### Moved set — declared records` table in the Phase 51 style (holder, site/seed, hero, record phase before → after, fromAction, fields before, fields after old → new, dead, rationale pointer); the shared Phase 52 rationale text; `### Draw-count pins` — the statement that every per-foeTurn pin and OPENER_DRAWS row is byte-identical and the FULL_FIGHTS totals are unchanged (or, if one moved, its old → new with the crit hits that caused it).

    (4) `content/BESTIARY-REBALANCE.md`: add `### Phase 52 addendum (DMG-02, 2026-09-20)` directly after the Phase 27 addendum (before any trailing pointer lines): the crit-rule change (engine-wide, not a bestiary number — say so) with the tier-1..5 default-d6 crit maxima before → after (14/20/30/44/62 → 13/16/21/28/37); the Herman before/after table (`| Creature | Tier | Field | Before | After | Hit | Crit max |` — T4 `dmg 0d0+25` → `strikesAs 5 (+d6)`, hit 41 → 26–31, crit 82 → 37; T5 50 → 26–31, crit 100 → 37) and the sentence that the Phase 18 verdict (L234) and the "Unchanged by decision" row (L434) are superseded by this addendum (leave those historical lines as they are); the Herman yardstick rows copied from the regenerated AFTER block; then `#### Still flagged after the fixes (rule=dice) — pending ruling` reproducing the AFTER `## Flagged (rule=dice)` table verbatim (band, tier, type, name, dice, maxSingleHit, MU bar, %, category, wouldBeTrim) with the counts line, and the sentence "Disposition per row is decided at the Plan 02 checkpoint and recorded by Plan 03 — nothing here is silently skipped (D-07)". Do NOT regenerate the BEFORE block or the Phase 18 tables.

    (5) Gates: `npm test` fail 0 (paste totals), `npm run build:www` exit 0, master hash unchanged. Commit as `docs(52-02): FIXTURE-INVENTORY Phase 52 + BESTIARY-REBALANCE Phase 52 addendum + AFTER damage-curve section`.
  </action>
  <verify>
    <automated>node tools/damage-curve-audit.mjs --rule=dice > "$TMPDIR/dca-after.txt" && diff "$TMPDIR/dca-after.txt" <(sed -n '/^# damage-curve-audit — rule=dice/,$p' tools/damage-curve-audit-output.txt) && node tools/cadence-audit.mjs | diff - tools/cadence-audit-output.txt && npm test 2>&1 | tail -12 && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^# damage-curve-audit — rule=whole' tools/damage-curve-audit-output.txt` prints 1 and `grep -c '^# damage-curve-audit — rule=dice' tools/damage-curve-audit-output.txt` prints 1; the whole-rule section precedes the dice-rule section; the dice-rule section equals a fresh `--rule=dice` run (diff empty)
    - the AFTER `## Herman` block shows critMax 37 for both Herman rows (quote the lines in the SUMMARY); `grep -c '^FLAGGED: ' tools/damage-curve-audit-output.txt` prints 2 (one per section)
    - `node tools/cadence-audit.mjs | diff - tools/cadence-audit-output.txt` is empty (or the deviation is documented per step 2)
    - `grep -c '^## Phase 52: crit doubles the dice + Herman strikes as a level five (DMG-02)' test/parity/FIXTURE-INVENTORY.md` prints 1; `grep -c '^### The predictor — crit exposure per site' test/parity/FIXTURE-INVENTORY.md` prints 1; `grep -c '^### Moved set — declared records' test/parity/FIXTURE-INVENTORY.md` >= 2 (Phase 51's and Phase 52's)
    - `grep -c '^### Phase 52 addendum (DMG-02, 2026-09-20)' content/BESTIARY-REBALANCE.md` prints 1; `grep -c '^#### Still flagged after the fixes (rule=dice) — pending ruling' content/BESTIARY-REBALANCE.md` prints 1; `grep -c 'strikesAs 5' content/BESTIARY-REBALANCE.md` >= 2
    - `node --test test/unit/bestiary-yardstick.test.js test/parity/fixture-inventory.test.js test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js` green
    - `npm test` fail 0; `npm run build:www` exit 0; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>Every Phase 52 engine/content change is recorded where the repo keeps such records; the AFTER damage-curve reading is committed and reproducible; the cadence readout is proven unchanged by the crit rule; the still-flagged rows are tabulated by category, ready for the ruling.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Checkpoint — ruling on the rows still flagged after the crit rule + Herman (D-07): trim now vs hand to Phase 54</name>
  <files>content/BESTIARY-REBALANCE.md (read-only at this task — the "Still flagged after the fixes (rule=dice) — pending ruling" table is what the user rules on)</files>
  <read_first>
    - content/BESTIARY-REBALANCE.md `#### Still flagged after the fixes (rule=dice) — pending ruling` (Task 2) and tools/damage-curve-audit-output.txt's AFTER `## Flagged (rule=dice)` section + its `FLAGGED: …` counts line
    - .planning/REQUIREMENTS.md TUNE-08 (Phase 54: "decide per creature whether it stays, moves tier, or is retuned (the DMG-02 rows feed this)") and 52-CONTEXT.md Deferred Ideas ("the four-band curve reshape / tier-3/5 roster decision" → Phase 54)
  </read_first>
  <action>Present the measured AFTER table verbatim (every still-flagged row with its band, tier, name, dice, maxSingleHit, MU bar, %, category and wouldBeTrim, plus the counts line), then the three options below. Do not edit any file at this task. Record the reply under "Decisions Made" in this plan's SUMMARY so Plan 03 Task 1 can apply it.</action>
  <verify>
    <automated>grep -c '^#### Still flagged after the fixes (rule=dice) — pending ruling' content/BESTIARY-REBALANCE.md</automated>
  </verify>
  <done>The user has replied `trim-now`, `hand-to-54`, or `custom: …`, and the reply is recorded in 52-02-SUMMARY.md under "Decisions Made".</done>
  <decision>Rows STILL flagged by the damage-curve audit after the crit rule + Herman landed (D-07): trim their dice now, or record them and hand them to Phase 54's per-creature decision (TUNE-08)?</decision>
  <context>
    The locked yardstick (D-05) flags a row when its max single hit (a crit) is >= 60 % of a Magic User's mean max HP at the band's median bot level. CONTEXT expected "the general crit rule plus Herman … to clear most" flags; the planner's scratch model says otherwise and Task 2's measured AFTER table (quoted in `content/BESTIARY-REBALANCE.md` under "Still flagged after the fixes") is the ground truth — present it here verbatim, with the `FLAGGED: … (deep-tier a, level-base b, row-dice c, bolt d)` line.

    What the categories mean: `deep-tier` (tier 5) — D-07 already allows an explicit "left as a deliberate deep-tier threat (Endgame band)" ruling; no decision needed. `level-base` — the tier's DEFAULT d6 crit (`T² + 12`, e.g. 28 at tier 4 vs a level-4 MU bar of 27.6) trips the flag, so the `lvl²` curve itself, not the row's dice, is the cause — that curve is Phase 54's deferred "four-band curve reshape" (52-CONTEXT Deferred Ideas); Herman post-fix (37 = the tier-5 default, by rulebook design) lands here. `row-dice` — the row's own `sp.dmg` is what trips the flag (Rast 1d8+4, Sterling 1d12, Wolf 1d6+2, Frank 1d8+6, Primp 1d8+2, Blumble 1d12, Werebeast 1d10, Trachea 1d10, Craig 1d12, Drake 2d10+4 are the likely members); these are the rows where "fix now" is even possible without touching the curve, and the `wouldBeTrim` column shows the largest same-family notation that clears the bar (the tier-3 rows all collapse to 1d6+1; Craig/Drake to 1d8 at the Breakaway level). REQUIREMENTS TUNE-08 (Phase 54) says "decide per creature whether it stays, moves tier, or is retuned (the DMG-02 rows feed this)" — i.e. Phase 54 expects these rows as INPUT.

    Tree state: Task 1 + Task 2 are committed and green (`npm test` fail 0, `build:www` green). Nothing in this decision changes the engine; option A changes `content/bestiary.js` numbers in Plan 03.
  </context>
  <options>
    <option id="trim-now">
      <name>A — Trim the row-dice rows now (Plan 03 applies `wouldBeTrim` to each, before/after row each, yardstick block regenerated, audit re-run must show them clean); level-base rows get the "Phase 54 curve dial" ruling; deep-tier rows get the deep-tier ruling</name>
      <pros>D-07's letter ("every flagged row is fixed") is met for every row that CAN be fixed without moving the curve; the AFTER audit shows zero row-dice flags; Phase 54 inherits a flatter within-tier spread</pros>
      <cons>~10 creature identities are homogenised (every big-dice tier-3 row becomes 1d6+1; Craig's two-handed sword 1d12 → 1d8) on a yardstick the user has not seen; overlaps TUNE-08's per-creature decision; a second bestiary readout must be taken before the bot AFTER run</cons>
    </option>
    <option id="hand-to-54">
      <name>B — Record only: every still-flagged row gets an explicit per-row ruling line (deep-tier / level-base → Phase 54 curve dial / row-dice → handed to Phase 54 TUNE-08 with its wouldBeTrim), no bestiary numbers change in Plan 03</name>
      <pros>Herman's cliff and the general crit cliff — the user's actual complaint — are fixed and measured; Phase 54 decides per creature with the full table (as TUNE-08 says); no untested identity changes land mid-milestone</pros>
      <cons>D-07's literal "gets its own before/after line" is read as "gets its own ruling line" for row-dice rows; the AFTER audit still shows flags (all explicitly ruled, none silent)</cons>
    </option>
    <option id="custom">
      <name>C — A per-row list: name the rows to trim now (with the notation you want) and the rows to hand to Phase 54</name>
      <pros>exact control</pros>
      <cons>needs the user to read the table</cons>
    </option>
  </options>
  <resume-signal>Reply `trim-now`, `hand-to-54`, or `custom:` followed by the per-row list. Plan 03 Task 1 applies the ruling.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (offline, single-process) | Rules-engine arithmetic + content data; no network, no external input; the shell is unchanged (event payloads unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Tampering | test/parity/prototype-master.js.txt | high | mitigate | hash asserted after every task; `comparables.js` diff empty |
| T-52-03 | Tampering | test/parity/fixtures (silent blanket regeneration) | high | mitigate | only measured movers get `+52`; the DMG-02 guard pins the declared set; FIXTURE-INVENTORY quotes the scan diff |
| T-52-04 | Repudiation | draw-count pins loosened to make tests pass | medium | mitigate | acceptance: no `-` line in the foe-turn-draw-count diff; per-swing draw pin added |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `npm test` fail 0 at the Task 1 commit and the Task 2 commit; `npm run build:www` green.
- `foeLevelBase` exists and is read at all three foe-damage sites; no whole-sum doubling remains in `pursuitStrike`/`foeTurn`; the player-side crit is untouched.
- Herman rows: `strikesAs: 5`, no `dmg`; audit AFTER `## Herman` critMax 37 ×2.
- Moved set measured (scan diff + parity suite), declared with `+52`, guarded; Part A lines byte-identical; master + comparables untouched.
- FIXTURE-INVENTORY Phase 52 section and BESTIARY-REBALANCE Phase 52 addendum present; AFTER audit section appended; cadence readout unchanged.
- Checkpoint answered (recorded in the SUMMARY under "Decisions Made") before Plan 03 starts.
</verification>

<success_criteria>
- SC4 second half for Herman: a floor-5 Herman crit is at most 37 (was 82), measured by the audit and pinned by test.
- SC5: every damage-curve fix has a before/after row in `content/BESTIARY-REBALANCE.md` (Herman ×2, the crit rule's tier maxima); moved fixtures measured, declared, regenerated per the engine gate.
- The still-flagged rows have a ruling path (the checkpoint), never a silent skip.
</success_criteria>

<artifacts_this_phase_produces>
See 52-01-PLAN.md's "Artifacts this phase produces" table (the phase-wide list). This plan produces: `engine/combat.js#foeLevelBase`, the three-site dice-doubling crit, `content/bestiary.js` Herman `sp.strikesAs: 5` ×2, `tools/bestiary-yardstick.mjs` strikesAs read + the regenerated yardstick AFTER block, `test/unit/foe-crit-curve.test.js` (tests named `DMG-02 (hero branch)…`, `DMG-02 (Soldier)…`, `DMG-02 (member branch)…`, `DMG-02 (pursuit strike)…`, `DMG-02 (Herman)…`, `DMG-02 draws…`, `DMG-02 pipeline order…`, `foeLevelBase…`), the `content-tables.test.js` Herman pin, `action-script.combat.json` `+52` record(s), `divergence-records.test.js` "DMG-02: the holders declaring Phase 52 are exactly the measured moved set", `FIXTURE-INVENTORY.md` `## Phase 52: …`, `BESTIARY-REBALANCE.md` `### Phase 52 addendum (DMG-02, 2026-09-20)` + `#### Still flagged after the fixes (rule=dice) — pending ruling`, `tools/damage-curve-audit-output.txt` AFTER section, `tools/initiative-fixture-scan-output.txt` re-run.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/52-foe-cadence-damage-curve/52-02-SUMMARY.md` when done (style: `51-02-SUMMARY.md`). Record verbatim: pre-edit SHA, the Task 1 commit SHA, `npm test` totals at both commits, `build:www` exit, the measured moved set and each record's old → new values, the FULL_FIGHTS statement, the AFTER `## Herman` lines, the FLAGGED counts line, the cadence invariance diff result, and the checkpoint ruling under "Decisions Made". End with `## Human verification (deferred to end of run)` listing the Phase 55 Pixel 7 checks: (1) a Herman crit on floor 5 no longer one-shots a full-HP level-5 hero (a CRIT line reads at most 37); (2) a Stalka Beast turn is never two hits AND a bolt — either one cast line or up to two swing lines; (3) a Bat/Rat or China Wolf fight at depth 5 shows at most 2 foe attack lines between your actions.
</output>
