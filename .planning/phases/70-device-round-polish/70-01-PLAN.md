---
phase: 70-device-round-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/hudMenu.js
  - test/unit/hudMenu.test.js
  - test/voice/safety-scan.test.js
  - test/unit/hp-not-wp.test.js
  - src/browser/account.js
  - content/account.js
  - src/browser/accountChip.js
  - test/unit/account.test.js
  - test/unit/account-copy.test.js
  - test/unit/accountChip-dom.test.js
  - test/unit/accountChip.test.js
autonomous: true
requirements: [POLISH-02, POLISH-03]
tags: [vanilla-js, presentation-only, hud, hamburger-menu, play-games, account, milestone-v2.0]

must_haves:
  truths:
    - "hudMenu.js exports HUD_MENU_GLYPH (the ☰ codepoint 9776), a frozen HUD_MENU_QUIT_COPY { saveQuit: 'SAVE & QUIT', abandon: 'ABANDON THIS CHARACTER', armed: 'TAP AGAIN TO BURY THEM', newCharacter: 'NEW CHARACTER' }, ABANDON_ARM_MS = 3000, a frozen ABANDON_ROW_EVENTS ['tap','timeout','close'] and abandonRowNext(armed, kind, ctx), and stays DOM-, timer- and storage-free (D-06)"
    - "abandonRowNext is the whole two-tap arm (D-06): dead tap -> { armed:false, act:'newCharacter' }; first live tap with confirm -> armed, no act; second -> { armed:false, act:'abandon' }; confirm false -> one tap abandons; a missing ctx or missing confirm is treated as confirm required (fail-safe for a destructive row); timeout, close and unknown kinds disarm; always frozen, never throws, armed strictly boolean"
    - "accountMenuView(state) gives the ☰ face: signed in -> face 'avatar' with the same initials/colour maths as the account chip and label 'Menu — signed in as {name}' (a missing display name uses ACCOUNT_COPY.sheet.unnamed); signed out, signing in or Compete OFF -> face 'menu', glyph HUD_MENU_GLYPH, label 'Menu' (D-03)"
    - "renderMenuFace(button, view) repaints only the button's existing .mw-hud-menu-face and its aria-label; renderAccountMenu(host, sheetView, handlers) builds the ACCOUNT block (identity, action when present, help when present, Compete) with no title and no Settings row, routing to onSignIn / onStopCompeting / onCompete(value); both are no-ops on null input and build DOM through ownerDocument only (D-04)"
    - "The controller exposes menuView(); ACCOUNT_CLASSES stays the pinned 14 entries; ACCOUNT_COPY gains menuLabel and only the tail of cards.failed.line changes; HUD_MENU_QUIT_COPY and ACCOUNT_COPY pass the voice safety scan and the HP-not-WP scan"
    - "No shell file changes in this plan: mazeworld.html, docs/ and every shell-* test are untouched (plan 70-03 wires these exports); engine/ and test/parity/ are byte-identical"
  artifacts:
    - path: "src/browser/hudMenu.js"
      provides: "HUD_MENU_GLYPH, HUD_MENU_QUIT_COPY, ABANDON_ARM_MS, ABANDON_ROW_EVENTS, abandonRowNext (pure)"
      exports: ["HUD_MENU_GLYPH", "HUD_MENU_QUIT_COPY", "ABANDON_ARM_MS", "ABANDON_ROW_EVENTS", "abandonRowNext"]
    - path: "src/browser/account.js"
      provides: "accountMenuView(state): the ☰ face view { face, initials, bg, glyph, label }"
      exports: ["accountMenuView"]
    - path: "src/browser/accountChip.js"
      provides: "renderMenuFace(button, view), renderAccountMenu(host, view, handlers), controller menuView()"
      exports: ["renderMenuFace", "renderAccountMenu"]
    - path: "content/account.js"
      provides: "ACCOUNT_COPY.menuLabel { signedIn, plain }; cards.failed.line tail points at the menu in the corner"
      contains: "menuLabel"
  key_links:
    - from: "src/browser/account.js#accountMenuView"
      to: "src/browser/hudMenu.js#HUD_MENU_GLYPH"
      via: "import — the signed-out face is the ☰ glyph"
      pattern: "HUD_MENU_GLYPH"
    - from: "src/browser/accountChip.js#createAccountController"
      to: "src/browser/account.js#accountMenuView"
      via: "menuView() = accountMenuView(state())"
      pattern: "menuView"
    - from: "test/voice/safety-scan.test.js and test/unit/hp-not-wp.test.js"
      to: "src/browser/hudMenu.js#HUD_MENU_QUIT_COPY"
      via: "import + the recursive string-leaf walk"
      pattern: "HUD_MENU_QUIT_COPY"
---

<objective>
Build the pure, DOM-free layer the ☰ changes of POLISH-02 and POLISH-03 stand on: the ☰ account face view (D-03), the ACCOUNT block renderer and face renderer (D-04), and the Save & quit / Abandon row copy plus the two-tap arm reducer (D-06). This plan is Task 1 of the validated draft quick plan `260924-56z` (reused per D-05). The draft's quit-row pieces are included here so plan 70-03 only wires the shell.

Purpose: plan 70-03 (wave 2) turns the ☰ into the account surface and moves the quit actions into it. Plan 70-04 (wave 3) opens it on every screen. Both import only what this plan exports, so the shell plans never re-derive avatar maths, copy or arm rules.

Output: new exports in hudMenu.js, account.js and accountChip.js, one copy addition in content/account.js, and their tests plus the voice/HP-not-WP scan registrations.

Precondition (D-01): quick task 260924-51h (POLISH-01, title theme) is already merged into master (commit c8e25d3). This plan touches none of its files. Do not re-implement or edit any title-music code.

Wave note: plan 70-02 (LINEAGE) runs in parallel in this wave. It owns engine/records.js, the boards files and mazeworld.html. This plan must not touch any of them. Do not rename or remove `initialsOf` / `avatarColour` in src/browser/boardsView.js. account.js imports them, and 70-02 edits that file.

Rulings carried over from the draft (the orchestrator relayed the user's words; locked):
- Per D-03 (supersedes Phase 67 D-05): the ☰ face is the initials avatar when signed in, and the plain ☰ glyph when signed out, signing in or Compete OFF. The aria-label names the player when signed in.
- Per D-04: the ACCOUNT block reuses accountSheetView / ACCOUNT_COPY and the existing controller. The title keeps its own chip and account sheet. The "?" nobody face stays only there and on the signed-out Leaderboards strip.
- Per D-06: Abandon has an in-row two-tap arm (TAP AGAIN TO BURY THEM) with no modal. Settings › Confirm before quit governs the arm: On (the default) means two taps, Off means one tap. When the hero is dead, the row is NEW CHARACTER.
- DISC-4 (draft): the one unavoidable copy change is that ACCOUNT_COPY.cards.failed.line changes its tail to "from the menu in the corner.", because the band-2 face it named is going away.
- DISC-5 (draft): the only new copy is ACCOUNT_COPY.menuLabel and HUD_MENU_QUIT_COPY. Both are registered in the voice safety scan and the HP-not-WP scan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-device-round-polish/70-CONTEXT.md
@.planning/quick/260924-56z-merge-the-account-chip-into-the-hamburge/260924-56z-PLAN.md
@.claude/CLAUDE.md
@src/browser/hudMenu.js
@src/browser/account.js
@content/account.js
@src/browser/accountChip.js

<interfaces>
Existing contracts (extracted; no need to re-explore):

src/browser/hudMenu.js (pure). hudMenu.test.js pins that the comment-stripped source contains no document, window, globalThis, setTimeout or localStorage identifier.
- HUD_MENU_ITEMS: 4 frozen rows {key,id,label,glyph,color,size}. The ids are mw-chip-marks, mw-chip-centre, btn-camp and mw-gear-btn. Glyphs are written as \u escapes so the file stays ASCII.
- HUD_MENU_EVENTS: toggle, select, outside, tab, encounter, escape.
- hudMenuNext(open, kind, ctx). Leave it UNCHANGED in this plan; plan 70-04 lifts its encounter refusal per D-08.

src/browser/account.js:
- normalizeAccountState, nameOf (private), faceOf(state) (private) -> { face: avatar|pending|nobody, initials, bg, glyph }.
- accountChipView(state) -> { face, initials, bg, glyph, label }. It uses ACCOUNT_COPY.chipLabel[status] and fills {name} with replace("{name}", () => name).
- accountSheetView(state) -> { title, identity:{face,initials,bg,glyph,name,status}, action:{id: signIn|stopCompeting|pending, label, disabled}|null, help, compete:{label,on,options[{value,label}]}, settings:{label} }.
- It imports initialsOf/avatarColour (boardsView.js), RAIL_HOLD (rail.js) and ACCOUNT_COPY.

src/browser/accountChip.js:
- ACCOUNT_CLASSES is a frozen 14-class list, pinned literally in account-layout.test.js RENDERER_CLASSES. Leave it unchanged.
- Private builders: el, paintFace(doc, faceEl, view), buildIdentity(doc, identity), buildAction(doc, action, handlers), buildCompete(doc, compete, handlers).
- renderAccountChip(button, view) and renderAccountSheet({rows,title}, view, handlers).
- createAccountController({provider, settings, notify, ...}) returns frozen {boot, signIn, setCompete, stopCompeting, state, identity, chipView, sheetView, subscribe}. accountChip.test.js iterates this key list (~line 155).

Scans: test/voice/safety-scan.test.js walks ACCOUNT_COPY recursively (~line 425). test/unit/hp-not-wp.test.js lists copy banks in its (b) COPY list (~line 145).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hudMenu.js — the ☰ glyph, the quit-row copy and the two-tap abandon reducer (TDD)</name>
  <files>src/browser/hudMenu.js, test/unit/hudMenu.test.js, test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js</files>
  <behavior>
    - HUD_MENU_GLYPH === String.fromCodePoint(9776).
    - HUD_MENU_QUIT_COPY is frozen: saveQuit "SAVE & QUIT", abandon "ABANDON THIS CHARACTER", armed "TAP AGAIN TO BURY THEM", newCharacter "NEW CHARACTER".
    - ABANDON_ARM_MS === 3000. ABANDON_ROW_EVENTS deep-equals ["tap","timeout","close"] and is frozen.
    - abandonRowNext(armed, "tap", { dead: true }) is { armed:false, act:"newCharacter" } for armed true or false.
    - abandonRowNext(false, "tap", { dead:false, confirm:true }) is { armed:true, act:null }.
    - abandonRowNext(true, "tap", { dead:false, confirm:true }) is { armed:false, act:"abandon" }.
    - abandonRowNext(false, "tap", { dead:false, confirm:false }) is { armed:false, act:"abandon" }.
    - A tap with ctx undefined, null, a non-object or an object without confirm counts as confirm required: the first tap arms.
    - "timeout", "close", an unknown kind or a missing kind gives { armed:false, act:null } from either armed state.
    - The result is always frozen. armed is a strict boolean, act is null, "abandon" or "newCharacter", and the function never throws, even on a hostile ctx such as a getter that throws.
    - Adjacency: a tap that arrives after "timeout" has disarmed the row arms it again. Only a tap on an already-armed row abandons.
    - hudMenuNext, HUD_MENU_ITEMS and HUD_MENU_EVENTS are unchanged, and the purity pin still passes.
    - safety-scan.test.js and hp-not-wp.test.js import HUD_MENU_QUIT_COPY from ../../src/browser/hudMenu.js and walk it recursively, the way they walk ACCOUNT_COPY. Both pass.
  </behavior>
  <action>
RED: extend test/unit/hudMenu.test.js with every behavior bullet above. Name the new tests with the "quit rows (D-06):" prefix. The truth table must include the hostile-ctx case (an object whose confirm getter throws) and the timeout-then-tap adjacency sequence. Register HUD_MENU_QUIT_COPY in test/voice/safety-scan.test.js next to the ACCOUNT_COPY walk, and in test/unit/hp-not-wp.test.js's (b) COPY list, each with a comment naming Phase 70 D-06. Run the three files and confirm they fail on the missing exports. Commit as test(70-01).

GREEN in src/browser/hudMenu.js (per D-06, D-03):
- Add HUD_MENU_GLYPH as the ☰ escape, so the file stays ASCII like HUD_MENU_ITEMS.
- Add HUD_MENU_QUIT_COPY, ABANDON_ARM_MS and ABANDON_ROW_EVENTS.
- Add abandonRowNext(armed, kind, ctx), which implements the truth table exactly. Read ctx inside a try/catch and derive two booleans:
  - dead: strictly ctx.dead === true;
  - confirm required: every case except ctx.confirm === false, so a missing, hostile or non-boolean ctx is fail-safe.
  The dead branch wins over armed.
- Extend the file header comment: per Phase 70 D-06 the two quit rows live in this menu; the ☰ glyph is also the account face's signed-out look (D-03); and Settings › Confirm before quit governs the arm (draft DISC-1). Keep the file free of DOM, timers and storage; the shell owns the 3 s timer.
Run the three files green. Commit as feat(70-01): quit-row copy and the two-tap abandon reducer.
  </action>
  <verify>
    <automated>node --test test/unit/hudMenu.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <done>All three test files pass. The five new names are each exported exactly once from hudMenu.js. hudMenuNext's existing tests still pass unchanged, and the purity pin passes. No file outside the Task 1 list changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: account.js / accountChip.js / content — the ☰ face view, the face and ACCOUNT-block renderers, menuView (TDD)</name>
  <files>src/browser/account.js, content/account.js, src/browser/accountChip.js, test/unit/account.test.js, test/unit/account-copy.test.js, test/unit/accountChip-dom.test.js, test/unit/accountChip.test.js</files>
  <behavior>
    - accountMenuView for a signed-in "Hilda Ferrow" gives:
      - face "avatar";
      - initials and bg equal to initialsOf / avatarColour of the name;
      - glyph "";
      - label "Menu — signed in as Hilda Ferrow".
    - A signed-in state with no display name uses ACCOUNT_COPY.sheet.unnamed in the label. A name made of non-Latin characters takes its initials from the existing initialsOf, unchanged.
    - accountMenuView for signed out, pending and off (Compete OFF, even with a stale signed-in status) gives face "menu", glyph HUD_MENU_GLYPH, initials "", bg "" and label "Menu". The result is frozen and never throws on null, undefined or garbage input.
    - ACCOUNT_COPY.menuLabel is { signedIn: "Menu — signed in as {name}", plain: "Menu" }. The {name}-only token test extends to menuLabel.signedIn.
    - cards.failed.line ends "…turn Compete off, from the menu in the corner." It still matches /playable/i, /try again/i and /Compete off/.
    - renderMenuFace(button, avatarView) reuses the button's existing .mw-hud-menu-face. It sets data-state "avatar", keeps aria-hidden, makes a single .mw-acct-initials child, sets the inline background, and sets the button's aria-label to view.label.
    - renderMenuFace(button, menuView) on the same element sets data-state "menu", replaces the children with the glyph text only, clears the inline background and sets aria-label "Menu".
    - A null button, a null view, or a button with no .mw-hud-menu-face is a no-op that creates nothing.
    - renderAccountMenu(host, sheetView, handlers) replaces the host's children in this order: identity, then the action when present, then help when present, then the Compete row. There is no title and no Settings row.
    - In renderAccountMenu, signIn goes to onSignIn, stopCompeting to onStopCompeting, and a Compete option to onCompete(value). A disabled or pending action calls nothing. Missing handlers never throw. Re-rendering replaces the rows and never duplicates them. A null host or view is a no-op.
    - The controller's frozen return object includes menuView(), equal to accountMenuView(state()). accountChip.test.js's key list includes "menuView".
    - ACCOUNT_CLASSES is unchanged at 14 entries. The class-completeness walk also covers renderAccountMenu's output and the menu face's avatar child.
    - accountChip-dom.test.js source pins: renderMenuFace and renderAccountMenu are each exported exactly once. The file still has no HTML-string assignment (innerHTML, outerHTML or insertAdjacentHTML).
  </behavior>
  <action>
RED first: write or extend the behavior tests across account.test.js, account-copy.test.js, accountChip-dom.test.js and accountChip.test.js. Run them and see them fail on the missing exports and keys. Commit as test(70-01).

GREEN:
(a) content/account.js, per D-03 and D-04. Add menuLabel { signedIn, plain } (draft DISC-5). Change only the tail of cards.failed.line (draft DISC-4). Update the header comment: the ☰ button now wears the account face (D-03), and the title keeps its own chip and account sheet (D-04). Touch no other key and no other content/ file.
(b) src/browser/account.js. Import HUD_MENU_GLYPH from ./hudMenu.js. Add the exported accountMenuView(input), built from normalizeAccountState plus the existing faceOf/nameOf helpers, so the avatar maths is never re-implemented. The avatar shows only when signed in; pending, signed out and Compete OFF show the menu glyph (D-03). Fill {name} with the same function-replacer form accountChipView uses.
(c) src/browser/accountChip.js:
  - Import accountMenuView.
  - Add renderMenuFace(button, view). It finds .mw-hud-menu-face with querySelector and is a no-op without it. It follows paintFace's dataset/style/replaceChildren/textContent discipline and builds nodes only through ownerDocument.
  - Add renderAccountMenu(host, view, handlers). It reuses buildIdentity, buildAction and buildCompete, and deliberately omits the title and the Settings row, because the ☰ already has SETTINGS (D-04).
  - Add menuView to the controller's frozen return object.
  - Update the module header and the ACCOUNT_CLASSES comment: the menu renderers emit only listed classes.
Run the Task 2 files green, then the full `npm test`. It must report 0 failures; in a worktree the only allowed failures are the 7 known CRLF doc-ledger ones (class-pass-ledger / flee-ledger). Commit as feat(70-01): the ☰ account face view and ACCOUNT-block renderers.

Write .planning/phases/70-device-round-polish/70-01-SUMMARY.md. List the new exports for plan 70-03 to wire, and restate DISC-4 and DISC-5 as carried decisions. There is no device check for this plan: it ships nothing visible until 70-03.
  </action>
  <verify>
    <automated>node --test test/unit/account.test.js test/unit/account-copy.test.js test/unit/accountChip-dom.test.js test/unit/accountChip.test.js test/unit/hudMenu.test.js test/unit/account-layout.test.js test/unit/shell-account.test.js && npm test</automated>
  </verify>
  <done>Every listed test file passes, and npm test has 0 failures on master (worktree: only the 7 known CRLF ones). accountMenuView, renderMenuFace, renderAccountMenu and the controller's menuView exist and are pinned. ACCOUNT_CLASSES is still the pinned 14. content/account.js is the only content/ file changed. mazeworld.html, docs/, engine/ and test/parity/ are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Play Games profile → DOM | The display name, supplied by Google, reaches the ☰ aria-label and the ACCOUNT identity line |
| Player tap → destructive run action | The abandon reducer decides whether one tap buries a character |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-01 | Tampering | renderMenuFace / renderAccountMenu | medium | mitigate | Names go through textContent/setAttribute only, and nodes are built via ownerDocument. The accountChip-dom.test.js source pins forbid HTML-string assignment |
| T-70-02 | Denial of service (of the player's run) | abandonRowNext | medium | mitigate | Fail-safe: a missing, hostile or non-boolean confirm is treated as confirm required, so the first tap only arms. A single-tap abandon happens only when confirm is strictly false (the player turned Confirm before quit Off). This is pinned by the truth-table tests |
| T-70-03 | Information disclosure | accountMenuView label | low | accept | The label shows only the player's own display name, which is already shown on the title chip and the Leaderboards strip |
</threat_model>

<verification>
- `node --test` on each task's files, then `npm test`: 0 failures on master (worktree: the 7 known CRLF doc-ledger failures only).
- `git diff --stat` over this plan's commits touches only the 11 files in files_modified.
- hudMenu.js purity pin green. ACCOUNT_CLASSES unchanged. Voice safety and HP-not-WP scans walk HUD_MENU_QUIT_COPY and ACCOUNT_COPY.menuLabel.
</verification>

<success_criteria>
- D-03: the face view exists (avatar when signed in, ☰ glyph otherwise; aria-label names the player).
- D-04: the ACCOUNT block renderer exists and routes only to the existing controller.
- D-06: the quit-row copy and the two-tap arm reducer exist, fail-safe and pinned.
- Nothing visible ships yet: plan 70-03 wires these exports into the shell.
</success_criteria>

<output>
Create `.planning/phases/70-device-round-polish/70-01-SUMMARY.md` when done
</output>
