---
phase: 70-device-round-polish
plan: 04
type: execute
wave: 3
depends_on: ["70-02", "70-03"]
files_modified:
  - src/browser/hudMenu.js
  - test/unit/hudMenu.test.js
  - src/browser/bridge.js
  - mazeworld.html
  - test/unit/hud-menu-layout.test.js
  - test/unit/shell-gear-toolbar.test.js
  - docs/SHELL-MODULES.md
  - test/unit/shell-boards-panel.test.js
  - test/unit/boards-css.test.js
  - test/persistence/resume-mid-encounter.test.js
  - docs/UAT-v2.0.md
autonomous: true
requirements: [POLISH-01, POLISH-03]
tags: [vanilla-js, presentation-only, hud, hamburger-menu, persistence, uat, milestone-v2.0]

must_haves:
  truths:
    - "The ☰ opens on every in-game screen: the map, combat, a store, a loot pile, the stair prompt, THAT IS THAT (dead), the Oracle and all five tabs including DEAD; hudMenuNext opens on toggle whatever the context (D-08)"
    - "Rows that cannot act in the current context are shown disabled (dimmed, the disabled attribute plus aria-disabled=\"true\", no action, the menu stays open on a tap): MAKE CAMP whenever an over-map encounter is up, the hero is dead or there is no hero, and CENTRE MAP whenever an over-map encounter covers the map; MARKS, SETTINGS, the ACCOUNT block, SAVE & QUIT and ABANDON / NEW CHARACTER are always available; an open menu re-syncs its rows when the encounter state changes under it (D-08)"
    - "The dropdown and its scrim layer above the encounter overlay and the death panel (rail 4 < overlay 8 < scrim < ☰ wrap < sheets 45 < title 50); a scrim tap closes the menu and never reaches an overlay button beneath; opening the ☰ lands a live combat beat first, the way a tab switch does (D-08)"
    - "Android back with the ☰ open closes only the menu on that press: a store, loot pile, stair prompt or beat underneath stays exactly as it was (D-08)"
    - "An encounter STARTING closes an open menu; a re-render of an ongoing encounter leaves an open menu alone; hudMenuEvent('select') on a closed menu and a repeated row sync are no-ops (idempotent) (D-08)"
    - "The HUD, and so the ☰, shows on all five in-game tabs including DEAD; only the title-opened Leaderboards panel hides it (no ☰ on the title area, D-04), keeping its safe-area top padding (D-08)"
    - "A mid-encounter Save & quit resumes exactly that state: in session ENTER only hides the title over the untouched shell; across a relaunch boot() rehydrates the last dispatch's save with its combat, open store or loot pile intact, because Save & quit writes nothing and every dispatch persists; a dead save relaunches to the roller (proven by test/persistence/resume-mid-encounter.test.js) (D-08)"
    - "POLISH-01 is confirmed against D-02 from the merged quick task 260924-51h with no reimplementation (D-01, D-02): theme.mp3 tracked byte-for-byte (2,318,486 B, sha256 0e3bed0a…), AUD-06 30 + 1, and every D-02 clause mapped to a passing pin"
    - "docs/UAT-v2.0.md carries every Phase 70 device check (51h, 70-02, 70-03, 70-04) in a new section L in the house format, each row source-mapped, and the rows Phase 70 replaced are re-worded or marked superseded (D-15)"
  artifacts:
    - path: "src/browser/hudMenu.js"
      provides: "hudMenuNext opens on toggle whatever the context; hudMenuRowStates(ctx) -> the six rows' { key, id, enabled } in dropdown order"
      exports: ["hudMenuNext", "hudMenuRowStates"]
    - path: "mazeworld.html"
      provides: "syncHudMenuRows, the widened hudMenuEvent, the rising-edge encounter close, the disabled-row CSS, the raised ☰ z-ladder, the back-button menu-first return, the HUD on the DEAD tab"
      contains: "syncHudMenuRows"
    - path: "test/persistence/resume-mid-encounter.test.js"
      provides: "the persist/boot proof that a mid-encounter Save & quit resumes exactly, plus the shell source pins that Save & quit and ENTER's resume branch dispatch nothing"
    - path: "docs/UAT-v2.0.md"
      provides: "section L (Phase 70 device checks) and the source-map entries"
      contains: "Phase 70"
  key_links:
    - from: "mazeworld.html classic setHudMenuOpen(true) and renderEncounter"
      to: "window.__mzHudMenu.rows (src/browser/hudMenu.js#hudMenuRowStates)"
      via: "syncHudMenuRows() writes disabled + aria-disabled on the six row ids"
      pattern: "syncHudMenuRows\\(\\)"
    - from: "mazeworld.html closeModal (registerNativeChrome getGameContext)"
      to: "hudMenuEvent(\"escape\") then return"
      via: "the menu-first early return"
      pattern: "hudMenuIsOpen\\(\\)"
    - from: "test/persistence/resume-mid-encounter.test.js"
      to: "src/browser/engineAdapter.js dispatch()/boot()"
      via: "fake localStorage; the save a dispatch wrote mid-encounter rehydrates with the sub-state intact"
      pattern: "boot\\("
---

<objective>
Open the ☰ on every screen (the last of POLISH-03, D-08). Confirm the merged title theme against D-02 (POLISH-01, D-01/D-02). Fold every Phase 70 device check into docs/UAT-v2.0.md (D-15).

Purpose: the user's Pixel 7 round found that the ☰ refuses to open during combat, every other encounter and after death. Today `hasActiveEncounter()`, which includes `S.dead`, gates it, and the DEAD tab hides the whole HUD. The user ruled that the ☰ opens everywhere and dims what cannot act. This plan also closes the phase's paperwork: POLISH-01 was delivered by quick task 260924-51h, and the device round needs one batched checklist.

Output:
- a widened pure policy (hudMenuNext, hudMenuRowStates) and its bridge;
- the shell writer for disabled rows;
- the z-ladder, back-button, beat and DEAD-tab changes;
- a persist/boot resume proof;
- a D-02 conformance table in the SUMMARY;
- the UAT fold.

D-08 (locked, verbatim intent): the ☰ opens on the map, in combat and every other encounter, on the Oracle, on all tabs, and while dead. Lift the encounter gate for opening. Rows whose action is not valid in the current context are shown disabled (dimmed, not hidden, with no action), e.g. MAKE CAMP and anything the engine would refuse mid-encounter. Save & quit and Abandon stay available everywhere, and a resume after a mid-encounter Save & quit must restore exactly where the player was (prove it with the persist/boot path and a test). Settings and the ACCOUNT block are always available. The dropdown layers above the combat/encounter overlays and the death panel, and Android back closes it first. This resolves the draft's DISC-3: the dead-state NEW CHARACTER row from plan 70-03 is now reachable.

Planner rulings (flag in SUMMARY):
- R-A, CENTRE MAP: disabled while an over-map encounter covers the map (hasActiveEncounter()), because there is no visible map to centre. It stays enabled on the other tabs, as today. MAKE CAMP is the only engine-refused row: disabled mid-encounter, while dead, or with no hero. Its short-on-food state (Phase 25.1 DFB-06) is unchanged: dimmed but tappable, because the engine's refusal line still has to reach the rail.
- R-B, "all tabs" includes DEAD: the Phase 57 discovery-B rule that hid the HUD on the DEAD tab is retired, so the ☰ is reachable on every in-game tab. Only the title-opened Leaderboards panel still hides the HUD, because D-04 keeps the ☰ off the title area. This is the literal reading of D-08. If the user prefers the old full-height DEAD tab, the revert is the Task 2 CSS/showTab hunk alone.
- R-C, a live combat beat is landed (window.__mzBeat?.hurry?.()) before the menu opens, the same way a tab switch lands it, so no action ever runs under a half-played round.
- R-D, an encounter STARTING still closes an open menu (a safety net; the scrim makes it practically unreachable). A re-render of an ongoing encounter no longer does.

D-01/D-02: quick task 260924-51h is merged (c8e25d3). Its title-music block, showTitleScreen, hideTitleScreen, routeFromBoards and initTitleScreen stay byte-identical. This plan only verifies POLISH-01 and never re-implements it. D-15: the orchestrator rebuilds the debug APK and installs it on the Pixel 7 after this plan merges. This plan does NOT build, install or touch adb; it only folds the checks into docs/UAT-v2.0.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-device-round-polish/70-CONTEXT.md
@.planning/phases/70-device-round-polish/70-02-SUMMARY.md
@.planning/phases/70-device-round-polish/70-03-SUMMARY.md
@.planning/quick/260924-51h-loop-theme-mp3-on-the-title-screen-v2-0/260924-51h-SUMMARY.md
@.claude/CLAUDE.md
@src/browser/hudMenu.js
@src/browser/bridge.js
@docs/UAT-v2.0.md

<interfaces>
Post-70-03 master. Line numbers are approximate; grep each anchor.

src/browser/hudMenu.js (pure; purity pin):
- HUD_MENU_ITEMS: 4 rows with ids mw-chip-marks, mw-chip-centre, btn-camp, mw-gear-btn.
- HUD_MENU_EVENTS.
- hudMenuNext(open, kind, ctx): toggle opens unless ctx.encounter; every other kind closes.
- The 70-01 quit exports (HUD_MENU_QUIT_COPY, abandonRowNext, …).
- The two quit-row ids in the markup are mw-menu-save-quit and mw-menu-abandon (70-03).

src/browser/bridge.js: the BRIDGE map entry __mzHudMenu { owner, consumers, purpose }. tools/bridge-doc.mjs --write regenerates the docs/SHELL-MODULES.md table between the bridge-table markers from it; --check compares.

mazeworld.html:
- CSS: .mw-rail z 4; .mw-overlay (the encounter/death overlay, #enc-panel inside .mazebox) z 8; .mw-hud-menu-scrim z 5; .mw-hud-menu-wrap z 6; .mw-legend-sheet z 45; .mw-title-screen z 50; .mw-roller-screen z 51; #mw-acct-sheet/#mw-settings-sheet z 55. No stacking context sits between them: .mw-hud, .mw-hud-band2 and .mw-stage declare no z-index or transform.
- CSS ~1134-1136: `.mw-hud[data-offtab="1"]{display:none}`, `.mw-cond-strip[data-offtab="1"]{display:none}`, and the #screen-dead safe-area padding-top line.
- CSS ~1149: `#screen-dead{padding-left:0;padding-right:0;padding-bottom:0;height:100%}`.
- CSS ~1232-1233: the title-mode rules `body[data-boards-entry="title"] #mw-tabbar{display:none}` and `… #mw-rail{display:none}`.
- ~569 `button:disabled{opacity:.35;…}`; ~591 `#btn-camp[data-short="1"]{opacity:.55;border-style:dashed}`, a specificity of (1,1,0).
- ~3385 hasActiveEncounter(): true for S.dead, S.combat, S.store, a pending loot pile, S.beats groups, window.__mzStair, or a live window.__mzBeat.
- ~3490 armEncounterButtons: removes aria-disabled only inside #enc-panel, #mw-rail and .mw-legend-sheet. The ☰ menu is NOT in that sweep; keep it out.
- ~4186 renderEncounter(): `const active = hasActiveEncounter();`, then `if (active) hudMenuEvent("encounter");`, and later `encWasActive = active;`.
- ~2384 showTab(name): the Dead-tab offtab predicate, writing hudEl/condEl dataset.offtab, then hudMenuEvent("tab"). It also calls window.__mzBeat?.hurry?.() at its top.
- The ☰ block (~5096+): hudMenuIsOpen, setHudMenuOpen(open) (70-03 added the data-dead / data-armed stamps), hudMenuEvent(kind) (computes `encounter` and asks window.__mzHudMenu.next(open, kind, { encounter })), closeMenuThen(fn) (70-03), and the three onclick lines. The container's onclick raises "select".
- Module: `window.__mzHudMenu = { next: hudMenuNext };` (~5729).
- registerNativeChrome getGameContext (~7630): hasOpenModal already includes hudMenuIsOpen(). closeModal runs, in order:
  1. the account-sheet early return;
  2. the title-panel early return;
  3. an unconditional `hudMenuEvent("escape");`;
  4. the gear-sheet early return;
  5. `window.__mzStair = null`;
  6. closeCampSheet / closeMarksLegend;
  7. `S.beats = null; S.store = null`;
  8. paint.
- initTitleScreen's ENTER onclick: hideTitleScreen(), then `if (resumeIntent) { surfaceWornReconcile(); return; }`, then window.mzStartRoll().
- Module: `hadSaveAtLaunch = !!(parsed && !parsed.dead)` (~6483).
- mzAbandonRun (70-03) is showTitleScreen({ allowResume: hasActiveDelveSave() }).

src/browser/engineAdapter.js: boot(freshSeed), initRun(seed), getState() (the live state object), dispatch(action) (persists every call, fire-and-enqueue), and the SAVE_KEY "ddr.delve.v1". Node has no localStorage. test/unit/engineAdapter.test.js's withFakeLocalStorage installs a Map-backed globalThis.localStorage. storage.js's flush() must be awaited before reading the store. Engine helpers for building sub-states: startCombat (engine/combat.js), openStore (engine/economy.js) and makeRng (engine/rng.js). test/roundtrip/serialize-rehydrate.test.js and stripVolatileFields (test/parity/harness/diffState.js) show the JSON-equality idiom.

Sandbox (test/unit/harness/shellSandbox.js): freshSandbox(state) → { doc, sandbox }; sandbox.setState(state); sandbox.context exposes the classic globals (hudMenuEvent, renderEncounter, window.__mzShowTab, window.__mzStair, …). recordingDom: addEventListener is a no-op, closest() returns null, and `disabled` is a plain property. Invoke handlers through .onclick, and pass a fake event object with a closest stub when a test needs a target.

51h pins (must stay green): title-music-shell.test.js, titleMusic.test.js, sfx-music.test.js, sfx-assets.test.js (AUD-06 30 clips + EXPECTED_MUSIC ["theme"]), test/persistence/lifecycle.test.js.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The ☰ opens everywhere with context-disabled rows, above the overlays, closed first by back (D-08)</name>
  <files>src/browser/hudMenu.js, test/unit/hudMenu.test.js, src/browser/bridge.js, mazeworld.html, test/unit/hud-menu-layout.test.js, test/unit/shell-gear-toolbar.test.js, docs/SHELL-MODULES.md</files>
  <behavior>
    - hudMenuNext(false, "toggle", ctx) is true for every ctx, including { encounter: true }, null and undefined. (true, "toggle") is false. Every other kind, and any unknown or missing kind, is false. It remains total: a strict boolean, never a throw.
    - hudMenuRowStates(ctx) returns a frozen array of six frozen { key, id, enabled } entries in dropdown order:
      - marks/mw-chip-marks, centre/mw-chip-centre, camp/btn-camp and settings/mw-gear-btn, with ids equal to HUD_MENU_ITEMS';
      - then saveQuit/mw-menu-save-quit and abandon/mw-menu-abandon.
    - Enablement:
      - marks, settings, saveQuit and abandon are always enabled;
      - centre is enabled unless ctx.encounter === true;
      - camp is enabled only when ctx.hero === true, ctx.dead !== true and ctx.encounter !== true.
    - A missing, null, non-object or hostile ctx (a getter that throws) never throws. It yields camp disabled (fail-safe for the one dispatching row) and every other row enabled.
    - The purity pin still passes.
    - The bridge is `window.__mzHudMenu = { next: hudMenuNext, rows: hudMenuRowStates }`. BRIDGE.__mzHudMenu's purpose and consumers name both. The regenerated doc table matches (bridge-doc --check), and bridge-registry passes with no new name.
    - Classic hudMenuEvent(kind) asks the bridge for next(open, kind) with no encounter context, and fails closed without the bridge. When the answer opens a closed menu, it first calls window.__mzBeat?.hurry?.() (R-C), then setHudMenuOpen(true).
    - Classic syncHudMenuRows():
      - it is TDZ-safe: state comes from window.__mzState?.get?.(), and encounter from `window.__mzState ? hasActiveEncounter() : false`;
      - it passes { encounter, dead, hero: !!state?.c } to window.__mzHudMenu?.rows?.();
      - for each row it sets the disabled property and aria-disabled="true" when disabled, and clears both when enabled;
      - it never touches #mw-hud-menu-acct or its children, and is a no-op without the bridge.
      - setHudMenuOpen(true) calls it after the data-dead stamp.
    - renderEncounter: `if (active && !encWasActive) hudMenuEvent("encounter");` replaces the unconditional close (R-D), followed by `if (hudMenuIsOpen()) syncHudMenuRows();`.
    - The container's onclick receives the event and returns without raising "select" when e?.target?.closest?.('[aria-disabled="true"]') finds a row. Otherwise it raises "select" as before. It still works when called with no event.
    - CSS: a disabled-row rule scoped under #mw-hud-menu (for example `#mw-hud-menu .mw-hud-menu-item:disabled`) dims the row and its glyph, shows no press, and outranks #btn-camp[data-short="1"]. The exact look is Claude's discretion per CONTEXT, with no motion. The scrim gets z-index 9 and the ☰ wrap z-index 10.
    - closeModal: `if (hudMenuIsOpen()) { hudMenuEvent("escape"); return; }` replaces the unconditional escape line, placed after the account-sheet and title-panel returns and before the gear-sheet branch.
  </behavior>
  <action>
RED first:
- test/unit/hudMenu.test.js: rewrite "toggle … stays closed during an encounter" into "toggle opens whatever the context (D-08)". Add the hudMenuRowStates cases from behavior, prefixed "row states (D-08):".
- test/unit/hud-menu-layout.test.js:
  - (5) z-ladder rewrite: rail(4) < overlay(8) < scrim < wrap < .mw-legend-sheet(45) < .mw-title-screen(50). Keep the no-z-index/no-transform checks on .mw-hud, .mw-hud-band2 and .mw-stage.
  - (9): replace the renderEncounter pin with the rising-edge form; the other close triggers are unchanged.
  - (10) rewrite: with window.__mzStair truthy, the ☰ onclick OPENS the menu. btn-camp and mw-chip-centre carry disabled plus aria-disabled="true"; mw-chip-marks, mw-gear-btn, mw-menu-save-quit and mw-menu-abandon carry neither; #mw-hud-menu-acct's children carry no row-sync writes.
  - Add (19) BEHAVIOUR:
    - a state with combat set opens the menu, with camp and centre disabled;
    - a dead state opens it, with data-dead "1" and camp and centre disabled;
    - a live, idle state enables all six rows;
    - with the menu open over the stair prompt, clearing window.__mzStair and calling renderEncounter() re-enables camp and centre while the menu stays open;
    - with the menu open during an ongoing encounter, a second renderEncounter() leaves it open;
    - the container onclick with a fake event whose target.closest returns a row leaves the menu open, while onclick() with no event closes it;
    - hudMenuEvent("select") on a closed menu changes nothing.
  - Add (20) SOURCE: in closeModal's region, the menu-first early return precedes the gear-sheet branch and the S.beats/S.store clears. In hudMenuEvent's region, window.__mzBeat?.hurry?.() precedes setHudMenuOpen(true). The armEncounterButtons selector string contains no #mw-hud-menu.
- test/unit/shell-gear-toolbar.test.js: the wrap-rule pin expects z-index:10.
Run them and see them fail. Commit as test(70-04).

GREEN, per D-08:
1. src/browser/hudMenu.js:
   - hudMenuNext's toggle branch returns !isOpen whatever the context. Keep the parameter list as it is, and document that ctx is no longer read (D-08 lifted the Phase 57 T-57-17 refusal).
   - Add hudMenuRowStates(ctx) per behavior. Read ctx inside try/catch. Build the six entries from HUD_MENU_ITEMS plus the two quit ids.
   - Update the header comment: per D-08, the menu opens on every screen and rows are disabled by context, not hidden.
2. src/browser/bridge.js: update __mzHudMenu's consumers and purpose. The classic hudMenuEvent asks next for the open/close policy, and the classic syncHudMenuRows asks rows for the per-row availability (Phase 70 D-08). Then run `node tools/bridge-doc.mjs --write` and `--check`. Save docs/SHELL-MODULES.md with LF if --check complains only about CRLF.
3. mazeworld.html:
   - Module: extend the hudMenu.js import line in place with hudMenuRowStates, and set the bridge object to { next, rows }.
   - Classic ☰ block:
     - rewrite hudMenuEvent (no encounter context; the beat hurry before opening);
     - add syncHudMenuRows after it, with a comment that states D-08, the TDZ rule and "never the ACCOUNT block";
     - call it from setHudMenuOpen(true);
     - change the container's onclick to the event-aware form.
     The shell must not declare any name hudMenu.js exports (shell-no-content-copies criterion 3). syncHudMenuRows is a shell-only name.
   - renderEncounter: the rising-edge close plus the open-menu re-sync, with a comment naming R-D.
   - CSS: add the disabled-row rule, and raise the scrim to 9 and the wrap to 10. Rewrite the T-57-19 z-ladder comment and the scrim markup comment for the new ladder (rail 4 < overlay 8 < scrim 9 < ☰ wrap 10 < sheets 45 < title 50 < roller 51 < account/settings sheets 55). Update the Phase 25.1 DFB-06 comment beside the camp short-state block: context-disabling mid-encounter (D-08) is separate from the short-on-food dim, which is never disabled.
   - closeModal: apply the menu-first early return, with a comment. The menu can now sit over a store, loot pile, stair prompt or beat, which the rest of closeModal would otherwise clear on the same press.
4. docs/SHELL-MODULES.md: in the section that describes the ☰, add a D-08 paragraph covering: opens everywhere; hudMenuRowStates via the bridge's rows; syncHudMenuRows; the new z-ladder; back closes it first; the beat hurry. Keep the generated table between the markers as bridge-doc wrote it.
Run the Task 1 files green. Commit as feat(70-04): the ☰ opens on every screen with context-disabled rows.
  </action>
  <verify>
    <automated>node --test test/unit/hudMenu.test.js test/unit/hud-menu-layout.test.js test/unit/shell-gear-toolbar.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/shell-menu-quit.test.js test/unit/shell-account.test.js test/unit/rail-overlay.test.js test/unit/title-music-shell.test.js test/unit/shell-pgs.test.js && node tools/bridge-doc.mjs --check</automated>
  </verify>
  <done>The ☰ opens during encounters and while dead. CAMP and CENTRE are disabled by context and the rows re-sync while the menu is open. The dropdown layers above the overlay. Back closes only the menu. A live beat is landed before the menu opens. Every listed test passes and bridge-doc --check exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The HUD on the DEAD tab, and the mid-encounter Save & quit resume proof (D-08)</name>
  <files>mazeworld.html, test/unit/hud-menu-layout.test.js, test/unit/shell-boards-panel.test.js, test/unit/boards-css.test.js, test/persistence/resume-mid-encounter.test.js</files>
  <behavior>
    - showTab no longer writes data-offtab, so switching to DEAD leaves #mw-hud and #mm-conditions with no offtab "1". hudMenuEvent("tab") still runs before the keep-in-view call.
    - CSS:
      - the two [data-offtab="1"] rules are gone;
      - `body[data-boards-entry="title"] #mw-hud{display:none}` and `body[data-boards-entry="title"] #mm-conditions{display:none}` sit beside the existing title-mode #mw-tabbar/#mw-rail rules;
      - the in-game #screen-dead has padding-top 0, so the panel sits flush under the HUD like the map;
      - `body[data-boards-entry="title"] #screen-dead{padding-top:calc(14px + var(--safe-area-inset-top, env(safe-area-inset-top, 0px)))}` keeps the title-mode panel clear of the status bar;
      - `#screen-dead{padding-left:0;padding-right:0;padding-bottom:0;height:100%}` is unchanged.
    - In-game DEAD tab (sandbox): the ☰ onclick opens the menu there.
    - Resume proof, test/persistence/resume-mid-encounter.test.js, run against the real engineAdapter with a fake localStorage. For each of (a) a live combat sub-state, (b) an open store and (c) a pending loot pile:
      1. build the sub-state through engine functions on getState() after initRun(seed);
      2. persist it through dispatch() of an action the engine refuses or no-ops in that context, and assert the sub-state is still present before booting;
      3. await flush(), then snapshot the saved state;
      4. await boot(1);
      5. assert the rehydrated state JSON-equals the snapshot (strip volatile fields the way the roundtrip test does), with the same sub-state, the same floor position and the same hero.
    - Dead save: after dispatch({ type: "abandon" }), the saved JSON has dead === true. The shell's launch contract expression (hadSaveAtLaunch = !!(parsed && !parsed.dead)) is pinned in the comment-stripped shell, so a relaunch after a dead Save & quit opens the roller.
    - Shell pins in the same file (stripHtml source):
      - window.mzAbandonRun's body dispatches nothing and writes no state (no dispatch call, no __mzState.set);
      - the ENTER onclick's resume branch is hideTitleScreen() then surfaceWornReconcile() and a return, with no dispatch, so an in-session resume shows the untouched shell (combat overlay, store, stair prompt) exactly as it was left.
  </behavior>
  <action>
RED first:
- test/unit/hud-menu-layout.test.js: rewrite (13) per behavior. __mzShowTab over all five tabs leaves no offtab "1"; the title-mode hide rules exist; no [data-offtab rule remains; the #screen-dead padding pins. Add to (19), or a new (21): on the DEAD tab (after __mzShowTab("dead")) the ☰ onclick opens the menu.
- test/unit/shell-boards-panel.test.js (A6): rewrite to "the in-game DEAD tab keeps the HUD and the condition strip (Phase 70 D-08); only the title-opened panel hides them, through the body marker". (A5) already proves tab mode sets no marker. If the sandbox exposes openFromTitle, add one BEHAVIOUR check that title mode sets the marker the new CSS keys on.
- test/unit/boards-css.test.js (6): pin the new in-game padding-top 0 rule and the title-mode safe-area rule, and keep the side/bottom reset pin.
- Create test/persistence/resume-mid-encounter.test.js. Its header comment explains D-08's resume guarantee:
  - in session, the title only covers the untouched shell;
  - across a relaunch, every dispatch persists and Save & quit writes nothing;
  - the stair prompt and a live beat are presentation-only, never saved, so a relaunch rebuilds from the last dispatched GameState.
  Copy the withFakeLocalStorage pattern from test/unit/engineAdapter.test.js. Cover the behavior's (a)(b)(c), the dead-save case and the shell pins.
Run and see the shell tests fail. The persistence cases may already pass; that is the point of a proof. If one fails, stop and report it: an engine or adapter persistence gap is outside this presentation plan and goes to the user.

GREEN in mazeworld.html, per D-08 and ruling R-B:
- In showTab, delete the offtab predicate and the two dataset writes. Rewrite that comment: Phase 70 D-08 retires the Phase 57 discovery-B DEAD-tab HUD hide, so the ☰ is reachable on every in-game tab; the title-opened panel hides the HUD through its body marker (D-04). Keep hudMenuEvent("tab") and its position.
- CSS: delete the two [data-offtab] rules and rewrite their comment. Update the .mw-hud band comment that says the HUD hides on DEAD. Add the two title-mode hide rules beside the D-01 title-mode rules, with a comment. Change the #screen-dead top-padding line to the in-game 0, and add the title-mode safe-area rule.
Run the Task 2 files green. Commit as feat(70-04): the ☰ on the DEAD tab, and a resume proof for mid-encounter Save & quit.
  </action>
  <verify>
    <automated>node --test test/unit/hud-menu-layout.test.js test/unit/shell-boards-panel.test.js test/unit/boards-css.test.js test/persistence/resume-mid-encounter.test.js test/unit/shell-boards-entry.test.js test/unit/boardsPanel.test.js test/unit/title-music-shell.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <done>The HUD and ☰ show on all five in-game tabs, and the title-opened panel keeps its full-screen, HUD-free look. The resume proof passes for combat, an open store and a loot pile. The dead-save relaunch contract and the no-dispatch shell pins pass. All listed tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: POLISH-01 conformance against D-02, the Phase 70 UAT fold, and the phase gates (D-01, D-02, D-15)</name>
  <files>docs/UAT-v2.0.md</files>
  <action>
1. POLISH-01 conformance (D-01, D-02; no code change). Run `node --test test/unit/titleMusic.test.js test/unit/sfx-music.test.js test/unit/sfx-assets.test.js test/unit/title-music-shell.test.js test/persistence/lifecycle.test.js`. Confirm that `git ls-files sfx/theme.mp3` lists the file, its size is 2,318,486 bytes, and its sha256 is 0e3bed0a820e6ed02db5b4be074c69b8f0022cf606622dc3188fb69f79d702d0 (use node's crypto; no new tooling).
   Then build the D-02 conformance table for the SUMMARY by reading the merged code and tests. Each clause maps to the pin or test that proves it:
   - starts at launch on native with no tap (the native one-frame-after-paint unlock);
   - falls back silently to the first tap, and in the browser dev loop;
   - continues from the title into the roller and over title-opened panels and sheets (titleMusicShowing plus the observer);
   - fades over about 500 ms on reaching the map, instantly under reduced motion (MUSIC_FADE_MS, reduced);
   - restarts from the top when the title or the roller shows again;
   - respects Sound;
   - stops in the background (the nativeChrome hooks and visibilitychange);
   - streams through an HTMLAudioElement into the Web Audio gain path (createMediaElementSource, not a full decode);
   - is committed byte-for-byte, with AUD-06 at 30 + 1.
   Also note that the ☰'s NEW CHARACTER (70-03) reaches the roller through window.mzStartRoll, so the theme restarts there by 51h's design.
   If any clause has no passing pin, do NOT implement it (D-01). List it under "POLISH-01 gaps for the orchestrator" in the SUMMARY.

2. Fold into docs/UAT-v2.0.md (D-15), keeping the house format.
   - Add a new section "## L. Device-round polish — Phase 70 (N)", after K and before Z, with rows L1..Ln (`| # | Step | Who | Result |`, Who `user`, Result `open`). Use the existing markers: *(edge)*, *(after console setup; …)* and *(if available: …)*. Order the rows for one pass:
     - the title theme: 260924-51h SUMMARY "Human verification (deferred)" items 1–8;
     - the ☰ face and ACCOUNT block: the 70-03 SUMMARY checks;
     - the quit rows: 70-03;
     - the ☰ everywhere (D-08): this plan's checks, listed in the verify block below;
     - LINEAGE: the 70-02 SUMMARY checks.
     Merge duplicates into one row.
   - The 51h browser-only item 9 becomes a Z-section row (Z2), since it is a desk check.
   - Rows Phase 70 replaced: E1 (the band-2 chip left of ☰ and its L budget), E2's HUD half, E3 ("both chips"), E4 (the band-2 chip ignored during combat), E5 ("both chips"), D1 and D3 ("Both chips"), B17 (grouped race-and-class rows), C1 (the DEAD tab now carries the HUD) and C9 (the account sheet no longer opens over the map).
     - Re-word each to the Phase 70 behaviour when the check still stands on the new surface: for example, D1 and D3 read "the ☰ face and the title chip", and C1 adds the HUD.
     - Otherwise replace the Step with "Superseded by Phase 70 — see L#." and set Result to `superseded`.
   - Update the section-header counts, the **Sources:** paragraph (add the Phase 70 plan SUMMARYs 70-02, 70-03 and 70-04 and quick task 260924-51h's SUMMARY, with the item count), **Suggested order** (place section L; its console-dependent rows follow step 5), and the Tally line (still 0 walked, dated 2026-09-24).
   - In the Source map:
     - add one line per folded item, `[51h-Sk]` and `[70-PP-Sk]` with k in the SUMMARY's list order, each appearing exactly once with its row and disposition (own row / merged / superseded);
     - set the disposition of every existing source line that pointed at a replaced row to "re-worded for Phase 70 (L#)" or "superseded by Phase 70 (L#)".
   - Leave the **Build:** line for the orchestrator, which records the Phase 70 debug APK there after the D-15 install. Do not build, install, or touch adb.

3. Phase gates. Run `npm test`: 0 failures on master, and in a worktree only the 7 known CRLF doc-ledger failures. Run `node tools/bridge-doc.mjs --check`.
   Over the whole phase, git diff from the parent of the first 70-01 commit must show:
   - in engine/, only engine/records.js;
   - test/parity/ (including prototype-master.js.txt) byte-identical;
   - under test/unit/fixtures/, only the two hero DOM snapshots declared by 70-03;
   - in content/, only content/account.js and content/boards.js.
   Record the counts.

4. Write .planning/phases/70-device-round-polish/70-04-SUMMARY.md. Include:
   - the D-02 conformance table and any POLISH-01 gaps;
   - rulings R-A..R-D;
   - DISC-3 marked resolved;
   - the resume-proof results;
   - the UAT fold tally (rows added, re-worded, superseded);
   - the phase gate results;
   - the deferred device checks.
   Commit as docs(70-04): fold the Phase 70 device checks into UAT-v2.0, and docs(70-04) for the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/titleMusic.test.js test/unit/sfx-music.test.js test/unit/sfx-assets.test.js test/unit/title-music-shell.test.js test/persistence/lifecycle.test.js && git ls-files --error-unmatch sfx/theme.mp3 && node -e "const b=require('fs').readFileSync('sfx/theme.mp3');const h=require('crypto').createHash('sha256').update(b).digest('hex');if(b.length!==2318486||h!=='0e3bed0a820e6ed02db5b4be074c69b8f0022cf606622dc3188fb69f79d702d0')process.exit(1)" && grep -q "^## L\. Device-round polish — Phase 70" docs/UAT-v2.0.md && npm test && node tools/bridge-doc.mjs --check</automated>
    <human-check>Deferred to the Phase 70 device round (folded into docs/UAT-v2.0.md section L; the orchestrator installs the debug APK per D-15):
      1. The ☰ opens on the map, in a fight, in a store, on a loot pile, at the stair prompt, over THAT IS THAT, on the Oracle (via REVIEW THE ORACLE while dead) and on all five tabs including DEAD. Its dropdown sits above the overlay, and a scrim tap closes it without pressing anything underneath.
      2. Mid-fight, MAKE CAMP and CENTRE MAP are dimmed and do nothing. MARKS, SETTINGS, the ACCOUNT rows, SAVE & QUIT and ABANDON all work.
      3. SAVE & QUIT mid-fight, then ENTER: the same fight, the same round and the same foes. Repeat with a force-close and relaunch between them: ENTER returns to the same fight.
      4. Dead: the ☰ opens over THAT IS THAT and its last row reads NEW CHARACTER, which opens the roller (the title theme starts). SAVE & QUIT goes to the title, and ENTER there rolls a new hero.
      5. Android back with the ☰ open over a store or the stair prompt closes only the menu. The store or prompt stays.
      6. Tapping ☰ while a combat round is still typing lands the round at once, with no half-typed round under the menu.
      7. The DEAD tab shows the HUD above the Leaderboards panel. At text size L the list still has usable room; note it as a finding if it feels cramped. VIEW THE DEAD from the title shows no HUD and no ☰.</human-check>
  </verify>
  <done>The SUMMARY holds a D-02 conformance table in which every clause maps to a passing pin, or the gaps are listed for the orchestrator without code. docs/UAT-v2.0.md has section L with every Phase 70 device check source-mapped once, and the replaced rows are re-worded or superseded. npm test has 0 failures (worktree: the 7 CRLF ones), and the phase-wide diff gates hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Player tap → run state | The ☰ now opens over live encounters, so its rows can act mid-fight |
| Device storage → boot | A mid-encounter save is rehydrated on relaunch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-13 | Tampering (of the run by a stray tap) | the ☰ over an encounter | medium | mitigate | The scrim (z 9) sits above the overlay (z 8), so an outside tap only closes the menu. MAKE CAMP is disabled by context. A live beat is landed before the menu opens (R-C). Pinned by hud-menu-layout (5)(10)(19)(20) |
| T-70-14 | Denial of service | Android back over a store or stair prompt | medium | mitigate | A menu-first early return in closeModal means one press closes only the menu and never clears S.store or S.beats (pinned by (20)) |
| T-70-15 | Tampering | resume after a mid-encounter Save & quit | low | mitigate | Save & quit and ENTER's resume branch dispatch nothing. Rehydration goes through the existing fail-closed validateSave in boot(). Proven by resume-mid-encounter.test.js |
| T-70-16 | Information disclosure | the HUD on the DEAD tab | low | accept | The HUD shows only the player's own hero; the title-opened panel (which can sit over the boot throwaway hero) hides it through the body marker |
</threat_model>

<verification>
- All three tasks' verify commands pass; npm test has 0 failures (worktree: the 7 known CRLF doc-ledger failures only).
- bridge-doc --check exits 0; bridge-registry passes with no new name; shell-no-content-copies passes.
- The 51h pins (title-music-shell, titleMusic, sfx-music, sfx-assets, lifecycle) are green; theme.mp3 is byte-identical.
- Phase-wide diff: engine/records.js is the only engine/ change; test/parity/ untouched; fixtures: only the two declared hero DOM snapshots; content/: account.js and boards.js only.
- docs/UAT-v2.0.md section L exists and every folded item appears once in the Source map.
</verification>

<success_criteria>
- D-08: the ☰ opens on every in-game screen and tab, with context-disabled rows, above the overlays, and back closes it first; a mid-encounter Save & quit resumes exactly (proven).
- D-01/D-02: POLISH-01 is confirmed from the merged 51h work with no reimplementation.
- D-15: every Phase 70 device check is in docs/UAT-v2.0.md, ready for the orchestrator's debug-APK install and the batched Pixel 7 walk.
</success_criteria>

<output>
Create `.planning/phases/70-device-round-polish/70-04-SUMMARY.md` when done
</output>
