---
phase: 70-device-round-polish
plan: 03
type: execute
wave: 2
depends_on: ["70-01", "70-02"]
files_modified:
  - mazeworld.html
  - test/unit/hud-menu-layout.test.js
  - test/unit/account-layout.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/shell-account.test.js
  - docs/SHELL-MODULES.md
  - src/browser/heroTab.js
  - test/unit/heroTab.test.js
  - test/unit/harness/shellSandbox.js
  - test/unit/fixtures/shell-snapshots/mu.hero.txt
  - test/unit/fixtures/shell-snapshots/thief.hero.txt
  - test/unit/shell-menu-quit.test.js
autonomous: true
requirements: [POLISH-02, POLISH-03]
tags: [vanilla-js, presentation-only, hud, hamburger-menu, play-games, account, quit-actions, milestone-v2.0]

must_haves:
  truths:
    - "Signed in, the ☰ button's face is the player's initials avatar (gold on-ring, colour hashed from the display name) and its aria-label names the player; signed out, signing in or Compete OFF it shows the plain ☰ glyph with aria-label Menu; its size, id, aria-haspopup/aria-controls/aria-expanded wiring are unchanged (D-03)"
    - "HUD band 2 carries no separate account chip and no actions wrapper: the counters, then the ☰ wrap; the counters' gap is back to 8px; the width budget computes band 2 at S = 336.4px and M = 377.8px (both ≤ 411) and L = 446.8px (the pre-Phase-67 figure, logged and pinned) (D-03)"
    - "Tapping ☰ opens the dropdown with, in order: the ACCOUNT block (identity line, then Sign in | Stop competing | disabled SIGNING IN…, then the helper line when present, then Compete ON/OFF), then MARKS / CENTRE MAP / MAKE CAMP / SETTINGS, then SAVE & QUIT, then ABANDON THIS CHARACTER last in the danger look; every tappable row is at least 44px tall, the dropdown is at most 288px wide and scrolls inside itself when taller than the space below the header; the title keeps its corner chip and the account sheet (D-04, D-06)"
    - "Every row closes the menu before its action runs: the four legacy rows, the ACCOUNT rows, SAVE & QUIT and a firing ABANDON tap all raise hudMenuEvent('select') first; only the arming ABANDON tap leaves the menu open (D-07)"
    - "SAVE & QUIT goes to the title with no dialog; with a live hero ENTER resumes exactly that run, while a dead hero's Save & quit arms no resume (ENTER rolls a new hero, the same as the relaunch path for a dead save) (D-06, D-08)"
    - "ABANDON THIS CHARACTER: the first tap arms it (TAP AGAIN TO BURY THEM, the menu stays open); a second tap closes the menu then buries the character through window.mzAbandonCharacter with no dialog; the arm expires after ABANDON_ARM_MS (3000 ms) or whenever the menu closes; with Settings › Confirm before quit Off a single tap abandons; when the hero is dead the row reads NEW CHARACTER and a tap closes the menu then calls window.mzStartRoll (D-06)"
    - "The HERO tab no longer has the Delve panel or either of its buttons (no dual path); the only fixture movement is the two hero DOM snapshots losing the removed button's block; npm test reports 0 failures (worktree: only the 7 known CRLF ones); engine/, content/ and test/parity/ are untouched by this plan"
  artifacts:
    - path: "mazeworld.html"
      provides: "the ☰ face renderer wiring, the #mw-hud-menu-acct ACCOUNT host, the #mw-menu-save-quit and #mw-menu-abandon rows, closeMenuThen, the Delve panel removed"
      contains: "id=\"mw-hud-menu-acct\""
    - path: "test/unit/shell-menu-quit.test.js"
      provides: "BEHAVIOUR pins for the two quit rows (order, arm, timeout, close-disarm, Confirm-before-quit Off, dead-state NEW CHARACTER, dead Save & quit), the dialog removal and the D-07 close-first helper"
    - path: "docs/SHELL-MODULES.md"
      provides: "the Play Games account section updated: the ☰ wears the face, the ACCOUNT block and the quit rows live in the dropdown, the band-2 chip retired"
  key_links:
    - from: "mazeworld.html module script (account.subscribe)"
      to: "renderMenuFace(#mw-hud-menu-btn, account.menuView()) and renderAccountMenu(#mw-hud-menu-acct, account.sheetView(), handlers)"
      via: "renderAccountSurfaces() on every account change and once before boot"
      pattern: "renderAccountSurfaces\\(\\)"
    - from: "mazeworld.html classic setHudMenuOpen"
      to: "#mw-menu-abandon data-armed / data-dead"
      via: "closing writes data-armed 0; opening stamps data-dead from window.__mzState"
      pattern: "mw-menu-abandon"
    - from: "mazeworld.html module quit-row listeners"
      to: "hudMenuEvent(\"select\") then window.mzAbandonRun / window.mzAbandonCharacter / window.mzStartRoll"
      via: "abandonRowNext(armed, \"tap\", { dead, confirm: shouldConfirmQuit(currentSettings) })"
      pattern: "abandonRowNext\\("
    - from: "mazeworld.html legacy row wiring (MARKS, CENTRE MAP, MAKE CAMP, SETTINGS)"
      to: "closeMenuThen(handler)"
      via: "the menu closes first, then the existing handler runs (D-07)"
      pattern: "closeMenuThen\\("
---

<objective>
Make the ☰ the account surface and the one place to quit (POLISH-02 and most of POLISH-03), wiring plan 70-01's pure exports into the shell. This plan is Tasks 2 and 3 of the validated draft quick plan `260924-56z` (reused per D-05), plus D-07 applied to every row and one dead-state ruling. The draft is provenance only; this plan supersedes it.

Purpose: user device feedback, 2026-09-24: "I wanted the hamburger menu icon to be our signed in profile icon, and clicking that would open the menu of options plus have the sign in option in it". Save & quit and Abandon move into the same dropdown. Band 2 gets its room back.

Output:
- Task 1: the ☰ face, the ACCOUNT block and the band-2 chip removal (D-03, D-04).
- Task 2: the quit rows, the in-row arm with no dialogs, the Delve panel deletion, and every row closing the menu first (D-06, D-07).
- Test pins for both, and the SHELL-MODULES.md update.

Scope boundary: the ☰'s availability rules are NOT changed here. Opening is still refused during encounters and while dead. Plan 70-04 (wave 3) lifts that gate, disables rows by context, raises the z-ladder and handles the DEAD tab, all per D-08. That also resolves the draft's DISC-3: the dead-state NEW CHARACTER row becomes reachable in 70-04. Do NOT report DISC-3 as an open reachability issue.

Merge context (D-01): quick task 260924-51h (POLISH-01) is already on master. Its title-music block (titleMusic, titleMusicShowing, syncTitleMusic, unlockAudioAndSync, setAppActive, the MutationObserver after initTitleScreen, and the registerNativeChrome hooks) stays byte-identical. showTitleScreen, hideTitleScreen, routeFromBoards and initTitleScreen stay UNEDITED:
- title-music-shell.test.js (11) pins their bodies;
- shell-account E6/E7 and shell-boards-panel E1–E5 run them through new Function with a fixed scope.
Plan 70-02 (wave 1) edited the createBoardsPanel options block and the .mw-bd CSS; leave both alone.

Decisions implemented (locked, from 70-CONTEXT.md):
- D-03 (supersedes Phase 67 D-05): the separate band-2 account chip is removed (greenfield, no dual path), and the counters get their gap back. The ☰ face is the initials avatar when signed in, and the plain ☰ otherwise. The aria-label names the player when signed in.
- D-04: the ACCOUNT block sits at the top of the dropdown: identity, Sign in (silent: false) / Stop competing / disabled pending, the helper line, and Compete ON/OFF. It reuses ACCOUNT_COPY and the existing controller. The title keeps its corner chip opening the account sheet.
- D-06: delete the HERO tab's Delve panel and both buttons. Dropdown order: ACCOUNT, the existing rows, SAVE & QUIT, then ABANDON THIS CHARACTER last in the danger style.
  - Save & quit has no confirm.
  - Abandon is two-tap armed in the row and reverts after ABANDON_ARM_MS or on menu close. The Confirm-before-quit setting governs the arm, and there is no modal.
  - Dead means the row is NEW CHARACTER (mzStartRoll).
- D-07: the menu closes before any row's action runs. This applies to all rows: the four legacy rows, the ACCOUNT rows and the quit rows.

Carried draft decisions (record in SUMMARY):
- DISC-1: the in-row arm IS the Confirm-before-quit confirm. On (the default) means two taps, Off means one tap. The dialog is removed.
- DISC-2: Save & quit's confirm dialog is removed; the action is non-destructive.
- DISC-4 and DISC-5: carried from plan 70-01 (copy).
- DISC-3: resolved by D-08 in plan 70-04. Record it as resolved, not open.

Planner ruling (flag in SUMMARY), dead-state Save & quit, per D-06/D-08: window.mzAbandonRun calls showTitleScreen({ allowResume: hasActiveDelveSave() }).
- A live hero resumes on ENTER exactly as before.
- A dead hero's Save & quit arms no resume, so ENTER rolls a new hero. That matches what the relaunch path already does for a dead save (hadSaveAtLaunch is false when the saved state is dead), so in-session and cross-launch behaviour agree.
- The ruling is reachable only once 70-04 opens the ☰ while dead.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-device-round-polish/70-CONTEXT.md
@.planning/phases/70-device-round-polish/70-01-SUMMARY.md
@.planning/quick/260924-56z-merge-the-account-chip-into-the-hamburge/260924-56z-PLAN.md
@.planning/quick/260924-51h-loop-theme-mp3-on-the-title-screen-v2-0/260924-51h-SUMMARY.md
@.planning/phases/67-play-games-integration-account-chip/67-08-SUMMARY.md
@.claude/CLAUDE.md
@src/browser/hudMenu.js
@src/browser/accountChip.js
@docs/SHELL-MODULES.md

<interfaces>
From plan 70-01 (already merged when this runs):
- hudMenu.js: HUD_MENU_GLYPH, HUD_MENU_QUIT_COPY {saveQuit, abandon, armed, newCharacter}, ABANDON_ARM_MS (3000), ABANDON_ROW_EVENTS, and abandonRowNext(armed, kind, ctx) -> frozen { armed, act: null|"abandon"|"newCharacter" }.
- account.js: accountMenuView(state).
- accountChip.js: renderMenuFace(button, view), renderAccountMenu(host, view, handlers), and controller.menuView().

mazeworld.html on post-51h master. Line numbers are approximate; grep each anchor before editing.
- CSS:
  - ~1046-1127: .mw-hud-band2 (gap:10px; padding:8px 14px 9px); .mw-hud-counters (gap:7px, set by Phase 67); .mw-hud-menu-wrap (z 6); .mw-hud-menu-btn (48×48, margin -7px -8px -7px 0); .mw-hud-menu-face (34×34, border 2px #6b5c3c).
  - The Phase 67 comment with the .mw-hud-actions / .mw-acct-chip / .mw-acct-face rules.
  - .mw-hud-menu: absolute, right:0, top:calc(100% + 7px), min-width:168px, with transitions pinned by hud-menu-layout (7).
  - .mw-hud-menu-item: min-height 48px, padding 13px 14px, border-top 2px #241f16, plus its :first-child reset and the glyph rules.
  - The global *{box-sizing:border-box} applies.
- ~591: #btn-camp[data-short="1"]. ~598: button.mw-danger-btn with :hover/:active.
- Markup:
  - ~1894-1939: band 2 = counters, then the .mw-hud-actions wrapper holding the Phase 67 chip button (#mw-acct-chip) and the .mw-hud-menu-wrap. The wrap holds the ☰ button and #mw-hud-menu with its 4 role=menuitem rows. The scrim follows.
  - HERO tab ~2125-2143: the Delve panel section with #btn-save-quit and #btn-abandon-character.
- Classic script:
  - ~4889-4905: the two legacy onclick blocks for those buttons, including the DR16 dead branch that calls window.mzStartRoll.
  - ~4977 mw-chip-marks addEventListener(openMarksLegend); ~5010 btn-camp onclick = openCampSheet; ~5076 mw-chip-centre addEventListener(glideCenterMap).
  - ~5096-5124: hudMenuIsOpen, setHudMenuOpen(open), hudMenuEvent(kind), and the three onclick wiring lines. The container's onclick raises "select" on bubble.
- Module script:
  - the accountChip.js import line, the settings.js import (shouldConfirmQuit) and the hudMenu.js import (hudMenuNext);
  - currentSettings and window.__mzSettings;
  - ~5967: mw-gear-btn addEventListener(openSettingsSheet);
  - ~6000: accountSheetOpen, renderAccountSheetNow, openAccountSheet({ fromHud }), closeAccountSheet, and the chip / title-chip / scrim / close listeners;
  - ~6260: the controller, renderAccountChips() (renders both chips), subscribe and the pre-boot render;
  - ~7182 / ~7209: window.mzAbandonRun and window.mzAbandonCharacter (each opens a confirm dialog today), directly after showTitleScreen (~7163);
  - hasActiveDelveSave() (~7146) is the module's "S.c and not dead" predicate.

Tests (draft-verified pins to update):
- hud-menu-layout.test.js: (2)(4)(5)(12)(14) name the chip or its wrapper. (14) is the width budget (LABEL_ADVANCE_PX 6.5, NUMBER_ADVANCE_PX 9.6, COUNTER_SLOT_CH, totalAt(scale)).
- account-layout.test.js: (1)(2)(6)(7)(8)(11)(12).
- shell-map-hud.test.js: ~208 pins the full .mw-hud-counters rule with gap:7px; ~540 pins the exact camp onclick line.
- shell-account.test.js:
  - (A1)(B2)(D1)(D2)(D3)(F2)(F3);
  - F2 (~389-392) pins the four legacy listener lines verbatim;
  - the accountSheet() helper slices from "function accountSheetOpen() {" to the band-2 chip's listener line.
- shellSandbox.js: SNAPSHOT_IDS.hero lists btn-abandon-character. The hero fixtures carry a "## #btn-abandon-character" block (regenerate with MZ_SNAPSHOT_UPDATE=1).
- heroTab.test.js ~152: a paint() id-literal regex. src/browser/heroTab.js ~603-609 holds the DR16 label flip.
- shell-gear-toolbar.test.js counts the camp onclick prefix: it must stay exactly 1.
- shell-no-content-copies criterion 3: the shell may not declare any name a src/browser module exports.
- bridge-registry: no new window.__mz name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shell — the ☰ wears the account face, the ACCOUNT block joins the dropdown, the band-2 chip goes</name>
  <files>mazeworld.html, test/unit/hud-menu-layout.test.js, test/unit/account-layout.test.js, test/unit/shell-map-hud.test.js, test/unit/shell-account.test.js, docs/SHELL-MODULES.md</files>
  <behavior>
    - Markup: band 2 is .mw-hud-counters, then .mw-hud-menu-wrap directly. There is no account chip and no actions wrapper anywhere on band 2. #mw-title-acct-chip and #mw-acct-sheet are unchanged.
    - The static ☰ face is a span with class mw-hud-menu-face, data-state="menu" and aria-hidden="true", holding &#9776;. Its decoded glyph equals HUD_MENU_GLYPH. The button's own markup attributes, including aria-label="Menu" (which equals ACCOUNT_COPY.menuLabel.plain), are unchanged.
    - #mw-hud-menu's first child is the ACCOUNT host: a div with id mw-hud-menu-acct, class mw-hud-menu-acct, role="group" and aria-label="Play Games account". The four legacy rows follow in the same order with the same ids.
    - CSS:
      - .mw-hud-counters has gap:8px; the full rule otherwise matches the pre-Phase-67 string;
      - the actions-group rule is gone;
      - the .mw-acct-chip rule loses the band-2 overlap margin (margin:0), and .mw-title-acct needs no margin override;
      - .mw-hud-menu-face[data-state="avatar"] carries border:0 and the chip avatar's on-ring box-shadow, so the 34px outer box is unchanged.
    - CSS for .mw-hud-menu:
      - width:min(288px,calc(100vw - 40px)) replaces min-width:168px;
      - a max-height derived from the header and tab-bar heights at text size L, written with the HUD's own safe-area custom properties;
      - overflow-y:auto and overscroll-behavior:contain;
      - position:absolute and both pinned transitions are byte-identical;
      - the open-state rules still declare only the (6) whitelist.
    - CSS for .mw-hud-menu-acct: 14px horizontal padding and a divider before MARKS. The now-dead :first-child border reset on the menu-item rule is removed.
    - Module:
      - one renderAccountSurfaces() renders #mw-title-acct-chip with account.chipView(), #mw-hud-menu-btn via renderMenuFace(…, account.menuView()), and #mw-hud-menu-acct via renderAccountMenu(…, account.sheetView(), handlers);
      - it runs on every subscribe tick (plus the open-sheet re-render and boardsPanel.refresh()) and once before boot;
      - per D-07, each ACCOUNT handler raises hudMenuEvent("select") FIRST, then calls account.signIn(), account.stopCompeting() or account.setCompete(value).
    - Module: openAccountSheet() takes no options and has no encounter guard; only the title chip opens it. There is no listener for the removed chip. The back button's accountSheetOpen/closeAccountSheet branch is unchanged.
    - Width budget (14): no chip term; computed at textScaleForSize S/M/L as S 336.4 ≤ 411, M 377.8 ≤ 411, L 446.8. Assert each equal at toFixed(1), and log L as the pre-Phase-67 figure.
    - A new layout pin parses .mw-hud-menu's width and asserts min(288, 411 − 40) ≤ 385, i.e. 411 minus band 2's 14px right padding minus a 12px margin. It also asserts overflow-y:auto and a max-height.
    - 44px rows: .mw-acct-action and .mw-acct-opt keep their 48px floor, .mw-acct-id keeps at least 44px, and .mw-hud-menu-item keeps 48px.
  </behavior>
  <action>
Edit only band 2, the ☰ dropdown, its CSS and the module account wiring. Modify existing import lines in place rather than adding adjacent new lines. Do not touch the title-music block or the four title functions named in the objective.

1. Markup (D-03, D-04).
   - Remove the Phase 67 band-2 chip button and the .mw-hud-actions wrapper div with its comment. The .mw-hud-menu-wrap becomes band 2's second child again, as in Phase 57.
   - Add data-state="menu" to the static ☰ face span.
   - Insert the ACCOUNT host div as the first child of #mw-hud-menu (attributes as in behavior, empty; the module renders it).
   - Update the ☰ markup comment: the button wears the account face and the dropdown opens on the ACCOUNT block (Phase 70 D-03/D-04, superseding Phase 67 D-05).
   - Update the account-sheet markup comment: it is now opened only from the title chip.
2. CSS (D-03). Apply the behavior rules.
   - Rewrite the Phase 67 CSS comment block: the chip rules now serve only the title chip, the ☰ face carries the avatar, and the width budget is back to the Phase 57 shape with the counters gap at 8px.
   - For max-height, compute from the existing CSS the header's worst-case height at text size L: band 1 padding plus the scaled identity line, the 4px HP strip, band 2, plus the 7px offset. Add the bottom tab bar's height plus the bottom safe-area inset. Write the result as a calc() using the same safe-area custom properties the HUD already uses, so the dropdown never runs under the tab bar on a Pixel 7 at L.
   - Leave the z-index values alone; plan 70-04 owns the z-ladder change.
3. Module wiring (D-03, D-04, D-07).
   - Extend the accountChip.js import line with renderMenuFace and renderAccountMenu.
   - Replace renderAccountChips with renderAccountSurfaces (behavior above; the view objects come from the controller only). Its three ACCOUNT handlers each raise the classic hudMenuEvent("select") before calling the controller, the way the module already calls classic globals such as hasActiveEncounter.
   - Update the subscribe block and the pre-boot call.
   - In the account-sheet section: drop openAccountSheet's options parameter and its encounter guard, delete the removed chip's click listener line, and make the title chip's listener call openAccountSheet().
   - Update the section comments: the ACCOUNT rows live in the ☰ dropdown, and the ☰'s own availability rule covers them (70-04 widens it per D-08).
   - Leave closeModal/hasOpenModal, the card parking, the Leaderboards identity seam and account.boot() untouched.
4. Tests:
   - hud-menu-layout.test.js:
     - Rewrite (2): the wrap directly follows the counters; no chip and no wrapper token on band 2.
     - (4): drop the chip id and add mw-hud-menu-acct. (5): drop the wrapper entry and leave the ladder values unchanged.
     - Rewrite (14) per behavior, importing textScaleForSize from ../../src/browser/settings.js and renaming the test to S/M/L.
     - Add (16): the ACCOUNT host is the first child with its aria attributes; the static face markup plus HUD_MENU_GLYPH equality; the avatar-face rule; and the dropdown width/max-height/overflow pin.
   - account-layout.test.js:
     - Rewrite the header and (1): band 2 has no chip and no wrapper, and the title chip is intact.
     - Make (2) title-chip only, plus a zero-count check for the removed id. Drop the chip id from (6)'s list. Make (7) the title chip's hit box. Update (8) for the margin cleanup.
     - Delete (11). In (12), remove the wrapper from STATIC_CLASSES and add mw-hud-menu-acct.
   - shell-map-hud.test.js: pin gap:8px in the full .mw-hud-counters rule, with a comment naming Phase 70 D-03.
   - shell-account.test.js:
     - (A1): the new import line.
     - (B2): renderAccountSurfaces pins — the title chip plus chipView, the ☰ plus menuView via renderMenuFace, the ACCOUNT host plus sheetView via renderAccountMenu, and the pre-boot call.
     - (D1): title-chip only, with no removed-chip literal in MODULE and an openAccountSheet with no guard.
     - Replace (D2)/(D3) with one BEHAVIOUR test: the title chip opens the sheet even with an encounter flag set. Retarget the accountSheet() helper's end marker to the title chip's listener line.
     - Rewrite (F2): the four legacy rows are intact and the ACCOUNT host is first. Leave the listener-line regexes for Task 2, which changes those lines. Retarget (F3)'s slice markers.
     - Add (G1) BEHAVIOUR: region-evaluate renderAccountSurfaces with a fake account, recording renderers and a recording hudMenuEvent. It must render three surfaces with the right views. Each ACCOUNT handler must record "select" BEFORE routing to signIn, stopCompeting or setCompete(false|true) (D-07).
5. docs/SHELL-MODULES.md, "Play Games account (Phase 67)" section: the ☰ wears the face (accountMenuView/renderMenuFace); the ACCOUNT block is in the dropdown (renderAccountMenu); the title chip and sheet are used from the title only; the band-2 chip was retired by Phase 70 D-03. Run node tools/bridge-doc.mjs --check. If it reports only a line-ending mismatch, save the doc with LF endings (no content change).
Commit as feat(70-03): the ☰ wears the account face.
  </action>
  <verify>
    <automated>node --test test/unit/hud-menu-layout.test.js test/unit/account-layout.test.js test/unit/shell-map-hud.test.js test/unit/shell-account.test.js test/unit/accountChip-dom.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js test/unit/hp-not-wp.test.js test/unit/shell-no-content-copies.test.js test/unit/title-music-shell.test.js && node tools/bridge-doc.mjs --check && test "$(grep -v '^\s*//' mazeworld.html | grep -c 'id="mw-acct-chip"')" = "0" && test "$(grep -v '^\s*//' mazeworld.html | grep -c 'class="mw-hud-actions"')" = "0" && test "$(grep -c 'id="mw-hud-menu-acct"' mazeworld.html)" = "1"</automated>
  </verify>
  <done>Band 2 has no chip or wrapper, and the counters' gap is 8px. (14) logs and pins S 336.4 / M 377.8 / L 446.8. The ☰ face renders the avatar or the glyph from the controller. The ACCOUNT block renders at the top of the dropdown and closes the menu before routing to the controller. The title chip and sheet still work from the title. All listed tests pass, and bridge-doc --check exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save & quit and Abandon move into the ☰ (in-row arm, no dialogs), every row closes the menu first, the HERO-tab Delve panel goes</name>
  <files>mazeworld.html, src/browser/heroTab.js, test/unit/heroTab.test.js, test/unit/harness/shellSandbox.js, test/unit/fixtures/shell-snapshots/mu.hero.txt, test/unit/fixtures/shell-snapshots/thief.hero.txt, test/unit/hud-menu-layout.test.js, test/unit/shell-account.test.js, test/unit/shell-map-hud.test.js, test/unit/shell-menu-quit.test.js, docs/SHELL-MODULES.md</files>
  <behavior>
    - Markup: after SETTINGS, #mw-hud-menu ends with two role=menuitem buttons.
      - #mw-menu-save-quit has classes mw-hud-menu-item, mw-hud-menu-quit and mw-hud-menu-split, and one .mw-hud-menu-label reading SAVE &amp; QUIT.
      - #mw-menu-abandon has classes mw-hud-menu-item, mw-hud-menu-quit and mw-danger-btn, with data-armed="0" and data-dead="0". It holds three .mw-hud-menu-label spans with data-when idle / armed / dead, reading ABANDON THIS CHARACTER / TAP AGAIN TO BURY THEM / NEW CHARACTER.
      - All visible labels equal HUD_MENU_QUIT_COPY. The menu's role=menuitem count is 6, and hud-menu-layout (3)'s four glyph rows are unchanged.
    - CSS:
      - exactly one of the abandon row's label spans shows, chosen from data-dead (which wins) and then data-armed; only display is toggled, with no animation;
      - the quit rows' labels align with the glyph rows (padding-left 42px = 14 + 18 + 10);
      - .mw-hud-menu-split draws a heavier divider above SAVE & QUIT;
      - the abandon row renders in the button.mw-danger-btn look, and hover/active never flash the generic menu-item fill;
      - rows keep min-height 48px.
    - The HERO tab no longer contains the Delve panel section or either legacy button id. The classic script no longer wires either legacy id. heroTab.js no longer flips a label on a legacy button. SNAPSHOT_IDS.hero drops the legacy id, and the two hero fixtures lose only their legacy-button block.
    - Classic setHudMenuOpen(open): opening stamps #mw-menu-abandon's data-dead as "1" when window.__mzState?.get?.()?.dead === true, else "0". Closing sets data-armed to "0". It stays TDZ-safe and calls no camera or positioning function, which (6) still pins.
    - D-07: a classic helper closeMenuThen(fn) returns a handler that calls hudMenuEvent("select") and then fn(). The four legacy row wirings (MARKS → openMarksLegend, CENTRE MAP → glideCenterMap, MAKE CAMP → openCampSheet, SETTINGS → openSettingsSheet) each pass their existing handler through closeMenuThen, keeping their existing wiring form (addEventListener, or the single camp onclick assignment). The container's bubble "select" stays as the Phase 57 safety net; a second select on a closed menu is a no-op.
    - Module, SAVE & QUIT click: stop propagation, hudMenuEvent("select") (the menu closes first), then window.mzAbandonRun(). mzAbandonRun is now the single call showTitleScreen({ allowResume: hasActiveDelveSave() }), with no dialog.
    - Module, ABANDON click:
      - stop propagation;
      - compute r = abandonRowNext(row armed?, "tap", { dead: window.__mzState?.get?.()?.dead === true, confirm: shouldConfirmQuit(currentSettings) });
      - write data-armed from r.armed.
    - When r.act is null, the menu stays open and one ABANDON_ARM_MS timer is (re)started; when it fires it writes data-armed "0". Re-arming clears the previous timer.
    - When r.act is set: clear the timer, call hudMenuEvent("select"), then call window.mzStartRoll() for "newCharacter" or window.mzAbandonCharacter() for "abandon".
    - mzAbandonCharacter keeps its hasActiveDelveSave() early return and its dispatchWithNarration({ type: "abandon" }) / __mzState.set / logLine / showTitleScreen() body, minus the dialog block.
    - The comment-stripped shell contains no confirm-dialog call at all.
  </behavior>
  <action>
Keep every edit away from showTitleScreen, hideTitleScreen, routeFromBoards, initTitleScreen and the 51h title-music block. The mzAbandonRun/mzAbandonCharacter edits stay inside those two function bodies and their own doc comments. Put the new module quit-row listener block right after the account-sheet listeners from Task 1, not near the title functions.

1. Markup and CSS (D-06).
   - Append the two rows (behavior) to #mw-hud-menu after the SETTINGS row, with a comment naming Phase 70 D-06 and the user's 2026-09-24 request.
   - Delete the HERO tab's Delve panel section entirely, including its h2 and the long Pass B1 comment (greenfield, no dual path).
   - Add the quit-row CSS from behavior next to the menu-item rules. Confirm, by reading the existing hover/active rules around mazeworld.html ~555-600, that the danger fill wins over .mw-hud-menu-item on hover and press. Add a scoped override only if it doesn't.
2. Classic script (D-06, D-07).
   - Delete the two legacy onclick blocks (the Save & quit and Abandon button wiring, ~4889-4905) together with their comments.
   - Add the data-dead / data-armed writes to setHudMenuOpen (behavior), with a comment.
   - Add closeMenuThen(fn) directly after hudMenuEvent. It is a hoisted function declaration, so the module can call it. Its comment explains D-07: every row closes the menu before its action; the container's bubble select stays as a no-op safety net.
   - Route the three classic legacy wirings (MARKS, CENTRE MAP, MAKE CAMP) through it. The camp line stays one `document.getElementById("btn-camp").onclick = …` assignment (shell-gear-toolbar counts that prefix).
   - Update the comment near ~1348 that names the HERO tab as mzAbandonRun's caller. Also update the Phase 25.1 DFB-06 comment near the camp short-state block only if it names the HERO tab.
3. Module script (D-06, D-07).
   - Extend the existing hudMenu.js import line in place with abandonRowNext and ABANDON_ARM_MS.
   - Route the SETTINGS wiring (mw-gear-btn) through closeMenuThen.
   - Add the quit-row block (behavior): a module-scope abandonArmTimer plus two listeners on #mw-menu-save-quit and #mw-menu-abandon, both with optional chaining. The block calls the classic globals hudMenuEvent and closeMenuThen the way the module already calls hasActiveEncounter. Delimit the block so a test can slice it: it begins at the abandonArmTimer declaration and ends at a stable next statement named in the test.
   - In window.mzAbandonRun: remove the dialog and its early return (DISC-2), and pass allowResume: hasActiveDelveSave() (the dead-state ruling in the objective).
   - In window.mzAbandonCharacter: remove the dialog block gated on shouldConfirmQuit (DISC-1). The gate moved to the row, and shouldConfirmQuit stays imported and used there.
   - Rewrite both doc comments: the callers are the ☰ rows; there are no dialogs; Settings › Confirm before quit governs the two-tap arm; and a dead hero's Save & quit arms no resume.
4. src/browser/heroTab.js: remove the DR16 label-flip block and its comment from renderHeroTab. The dead-state label now lives on the ☰ row via data-dead. Nothing else in the file changes.
5. Tests:
   - shellSandbox.js: drop the legacy id from SNAPSHOT_IDS.hero. Regenerate the hero fixtures with `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`, then re-run without the flag. git diff must show only the removed legacy-button block in mu.hero.txt and thief.hero.txt; no other fixture moves. This is the plan's one declared fixture movement.
   - heroTab.test.js: drop the legacy id from the ~152 regex alternation.
   - hud-menu-layout.test.js:
     - (12) expects 6 role=menuitem.
     - Add (17) structural: the rows are the ACCOUNT host, the four HUD_MENU_ITEMS ids, #mw-menu-save-quit, then #mw-menu-abandon last. The abandon row carries mw-danger-btn and data-armed="0" data-dead="0". The label texts equal HUD_MENU_QUIT_COPY after decoding &amp;. The label-switch CSS rules exist. Neither legacy id nor the Delve heading appears anywhere in HTML.
     - Add (18) BEHAVIOUR (sandbox): set #mw-menu-abandon's data-armed to "1", then open and close via the scrim; it reads "0". Repeat for select, escape, tab and encounter. Opening via setHudMenuOpen(true) with a dead state stamps data-dead "1", and with a live state "0".
   - shell-account.test.js (F2) and shell-map-hud.test.js (~540): update the listener-line pins to the closeMenuThen form. The camp line count stays 1.
   - New test/unit/shell-menu-quit.test.js, in shell-account.test.js's comment-strip plus region-evaluate style. Thread in fakes for document (two rows with datasets), window (mzAbandonRun, mzAbandonCharacter, mzStartRoll, __mzState), hudMenuEvent, setTimeout/clearTimeout and currentSettings. Use the real abandonRowNext, ABANDON_ARM_MS and shouldConfirmQuit. Cover:
     - Save & quit: select, then mzAbandonRun, with propagation stopped;
     - the first abandon tap arms: no select, no abandon, and one 3000 ms timer;
     - a second tap clears the timer, then calls select, then mzAbandonCharacter;
     - the timer firing disarms, so the next tap arms again (never abandons);
     - with confirmBeforeQuit false, a single tap gives select, then mzAbandonCharacter;
     - dead, armed or not: select, then mzStartRoll, never mzAbandonCharacter;
     - re-arming clears the previous timer;
     - D-07: region-evaluate closeMenuThen with a recording hudMenuEvent and fn, and assert the order ["select", "fn"]; source pins show all four legacy wirings pass through closeMenuThen;
     - mzAbandonRun, region-evaluated with fake showTitleScreen and hasActiveDelveSave: allowResume true when alive and false when dead, dispatching nothing and writing no state;
     - source pins: no confirm-dialog call in the comment-stripped shell; mzAbandonCharacter still contains the hasActiveDelveSave() guard and the "abandon" dispatch.
6. docs/SHELL-MODULES.md: if any section names the HERO tab's quit buttons or the ☰ rows, update it to the new ☰ order and note D-07's close-first helper. Re-run node tools/bridge-doc.mjs --check (LF).
7. Run npm test: 0 failures on master; in a worktree, only the 7 known CRLF doc-ledger failures. Confirm git diff --stat over this plan's commits shows nothing under engine/, content/ or test/parity/.
8. Write .planning/phases/70-device-round-polish/70-03-SUMMARY.md. Include:
   - DISC-1, DISC-2, DISC-4 and DISC-5 as carried decisions, and DISC-3 as RESOLVED by D-08 (plan 70-04);
   - the dead-state Save & quit ruling;
   - the declared hero-fixture movement;
   - the deferred Pixel 7 checks from the verify block below.
   Commit as feat(70-03): Save & quit and Abandon move into the ☰, and docs(70-03) for the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/shell-menu-quit.test.js test/unit/hud-menu-layout.test.js test/unit/heroTab.test.js test/unit/shell-tab-snapshots.test.js test/unit/shell-account.test.js test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js test/unit/hp-not-wp.test.js test/unit/title-music-shell.test.js && node tools/bridge-doc.mjs --check && test "$(grep -v '^\s*//' mazeworld.html | grep -c 'id="btn-save-quit"\|id="btn-abandon-character"')" = "0" && test "$(grep -v '^\s*//' src/browser/heroTab.js | grep -c 'getElementById("btn-abandon-character")')" = "0" && npm test</automated>
    <human-check>Deferred to the Phase 70 device round (docs/UAT-v2.0.md, folded by plan 70-04):
      1. The ☰ shows the initials avatar when signed in and the plain ☰ when signed out, signing in or Compete OFF. The button doesn't change size between faces. Band 2 at S/M/L shows Depth/Day/Squares/Rations with the ☰ flush right.
      2. The dropdown shows the ACCOUNT block first: Sign in works from it, and Compete OFF flips the ☰ back to the glyph. Then MARKS, CENTRE MAP, MAKE CAMP and SETTINGS, then SAVE & QUIT, then a red ABANDON THIS CHARACTER. Nothing clips at 411px wide, and the dropdown scrolls if it runs too tall at L. Every row closes the menu as it acts.
      3. SAVE & QUIT goes straight to the title with no dialog, and ENTER resumes the run.
      4. ABANDON: one tap shows TAP AGAIN TO BURY THEM. Waiting about 3 s, or closing the menu, reverts it. Two quick taps bury the hero with no dialog. With Settings › Confirm before quit Off, one tap buries.
      5. The HERO tab has no Delve panel. The title chip and account sheet still work from the title.</human-check>
  </verify>
  <done>The two quit rows work from the ☰: the menu closes before the action, the arm works in the row, there are no dialogs, and the dead branches work. Every legacy and ACCOUNT row closes the menu first. The Delve panel and all its wiring are gone. The fixtures moved only by the removed block. npm test has 0 failures (worktree: the 7 CRLF ones). The SUMMARY records DISC-1/2/4/5, DISC-3 as resolved, the dead Save & quit ruling and the deferred device checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Play Games profile → DOM | The display name reaches the ☰ aria-label and the ACCOUNT identity line |
| Player tap → destructive run action | A quick-tap menu now holds Abandon this character |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-09 | Tampering | renderAccountSurfaces | medium | mitigate | Only the 70-01 renderers write the face and the ACCOUNT block (textContent/setAttribute only); the shell adds no HTML-string assignment for them |
| T-70-10 | Denial of service (of the player's run) | #mw-menu-abandon | medium | mitigate | Several guards: the in-row two-tap arm (abandonRowNext, fail-safe); a 3 s expiry; disarm on every menu close; the danger styling; and a heavier divider. A single tap works only when the player turned Confirm before quit Off. All are pinned by shell-menu-quit.test.js and hud-menu-layout (18) |
| T-70-11 | Information disclosure | ACCOUNT block / Compete OFF | low | mitigate | The rows route only through the existing controller. Boot with Compete OFF still calls no provider method (67-07 Proxy test unchanged), and there is no new network identifier (shell-account F3) |
| T-70-12 | Repudiation | Save & quit while dead | low | accept | It arms no resume, matching the relaunch path; the death is already recorded at the moment of death by the adapter's died-event choke point |
</threat_model>

<verification>
- Both tasks' verify commands pass; npm test has 0 failures (worktree: the 7 known CRLF doc-ledger failures only).
- git diff --stat over this plan's commits: no engine/, no content/, no test/parity/; the only fixture change is the two hero snapshots' removed block.
- bridge-doc --check exits 0; no new window.__mz name (bridge-registry passes unchanged).
- Grep gates (comment-filtered): zero band-2 chip ids, zero legacy quit-button ids, one ACCOUNT host.
- The 51h title functions and title-music block are unchanged (title-music-shell.test.js green).
</verification>

<success_criteria>
The ☰ is the player's signed-in face, or the plain ☰ otherwise (D-03). Tapping it opens one dropdown with the ACCOUNT block (D-04), the four existing rows, SAVE & QUIT and a guarded ABANDON THIS CHARACTER (D-06). Every row closes the menu first (D-07). Band 2 fits its pre-chip budget again. The HERO tab's Delve panel is gone. Every pin is updated with no skipped tests. The device look is queued in the SUMMARY for the Phase 70 UAT fold.
</success_criteria>

<output>
Create `.planning/phases/70-device-round-polish/70-03-SUMMARY.md` when done
</output>
