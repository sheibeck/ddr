---
phase: 60-performance-footprint-close
plan: 03
type: execute
wave: 2
depends_on: ["60-01", "60-02"]
files_modified:
  - docs/PERF-BASELINE.md
  - docs/UAT-v1.8.md
  - .planning/todos/pending/
autonomous: false
requirements: [PERF-03]

must_haves:
  truths:
    - "The v1.8 APK measured is current. At session start, no shipped path has changed between its commit (`sizes.json` `v1.8.sha`) and HEAD. If one has, the APK and AAB are rebuilt and re-recorded before any launch is timed."
    - "Cold start is measured on the Pixel 7 for both builds in one sitting, back to back, with the same app data. First v1.7 is installed and launched 1 warm-up + 10 COLD times; then v1.8 is installed over it and launched the same way. For each build, `docs/PERF-BASELINE.md` records the raw samples in run order plus the median, p95, min, max and LaunchStates. This is PERF-03 criterion 1 and CONTEXT 'Side-by-side baseline' / 'Cold start'."
    - "Step time is measured on the Pixel 7 for both builds in the same session. The user walks the Phase 49 protocol once per build. The four rows are recorded exactly as the user reported them, with the as-run coverage, jank and n. A later read on the same run supersedes an earlier one, and both are recorded, never averaged (the Phase 49 n=47 → n=61 precedent). This is PERF-03 criterion 2 and CONTEXT 'Step time'."
    - "Every verdict comes from `node tools/cold-start.mjs judge` with all three measures supplied (`complete: true`). Its table and its `[perf03]` JSON are pasted into `### Verdicts (PERF-03)`, and the AAB row uses plan 60-02's recorded bytes. This covers PERF-03 criterion 3, the recorded AAB change, judged against the threshold."
    - "Every measurement past a threshold gets a disposition in `### Dispositions`, in the user's own words. It is either a fix (naming the lever, filing a todo, and flagging gap closure) or an explained, accepted miss. If nothing regressed, the section says so in words. The executor never writes an acceptance the user did not give. This is PERF-03 criterion 4 and CONTEXT 'Regression thresholds & dispositions'."
    - "The UAT-v1.8 checklist runs in the same session. Every item's Result starts with pass, fail or not run, followed by the user's words. Every fail or other finding becomes a `.planning/todos/pending/` todo. This plan changes no code file. This is CONTEXT 'The device session & UAT' and the deferred-UAT protocol."
    - "The phone ends on the v1.8 debug APK, the milestone's one debug APK. Its commit and sha256 are recorded in `docs/UAT-v1.8.md` under `## Session record`."
  artifacts:
    - docs/PERF-BASELINE.md
    - docs/UAT-v1.8.md
  key_links:
    - "`C:/projects/mazeworld-perf60/artifacts/sizes.json` (from plan 60-02) is the single source for the two APK paths, their commits and sha256, and the two AAB byte counts. `cold-v1.7.json` / `cold-v1.8.json` are written next to it by `tools/cold-start.mjs run --out`, and `judge` reads their `summary.median`."
    - "Install-over works between the two debug builds, because both are signed with this machine's debug keystore, and `install -r` keeps the app data. A Play-installed build has a different signer. It can only be replaced by an uninstall, which wipes the save and the graveyard, so that needs the user's explicit OK (STATE Ground Truth, Device)."
    - "After every install-over, the WebView is reloaded with `am force-stop` plus `monkey -p com.darktierstudios.delvedierepeat 1`, because an install alone does not reload it (STATE Ground Truth, Device). The one exception is the fresh v1.8 install for 56-3, which is deliberately left unlaunched so that the user's airplane-mode launch really is the first."
    - "adb resolves through `node tools/cold-start.mjs adb-path`. adb is not on PATH on this machine; it lives in the SDK's `platform-tools`, which the tool finds through `android/local.properties`."
  prohibitions:
    - "MUST NOT change any code or test file: mazeworld.html, src/, engine/, content/, tools/ or test/. Findings become todos, never mid-run edits (deferred-UAT protocol)."
    - "MUST NOT uninstall the app or clear its data unless the user explicitly said yes to that in the Task 1 reply. `pm clear` is never used."
    - "MUST NOT write, paraphrase into, or imply a disposition or an acceptance the user did not give. MUST NOT average, drop or 'clean' a reading. Every reported line is recorded as given."
    - "MUST NOT change device settings over adb (animation scales, stay-awake, airplane mode, TalkBack). The user controls the phone's settings."
    - "MUST NOT build a Play release, bump the version, upload or push. Asking about a Play internal-testing push is a separate, post-milestone question (standing rule, CONTEXT Deferred)."
    - "MUST NOT edit REQUIREMENTS.md, ROADMAP.md or STATE.md (the orchestrator owns them)."
---

<objective>
Run the milestone's one batched Pixel 7 session: cold start and step time for v1.7 and v1.8 side by side, the tested PERF-03 verdicts with a disposition for every regression, and the merged UAT-v1.8 checklist. The milestone then closes with the v1.8 debug APK on the phone.

This implements CONTEXT 'Side-by-side baseline', 'Cold start', 'Step time', 'Regression thresholds & dispositions' and 'The device session & UAT'. Under the deferred-UAT protocol this is the one allowed device pause of the whole milestone.

Why six tasks, three of them checkpoints: both builds share one appId, so the phone can hold only one at a time, and the executor has to switch builds between the user's two step walks. The shape is: connect → (cold start both builds, then install v1.7) → walk v1.7 → (record, install v1.8) → walk v1.8 + checklist + rulings → (record and close). Each checkpoint starts a small continuation, so no single agent carries more than about two tasks of context. The alternative, splitting this into two plans, would break up one physical session across two plans for no gain.

Purpose: PERF-03 criteria 1, 2 and 4 (and 3's verdict). This plan also closes the milestone's deferred device checks.

Output: the filled `docs/PERF-BASELINE.md` v1.8 section and `docs/UAT-v1.8.md` results, todos for findings, and a SUMMARY that states the PERF-03 outcome per criterion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-performance-footprint-close/60-CONTEXT.md
@.planning/phases/60-performance-footprint-close/60-01-SUMMARY.md
@.planning/phases/60-performance-footprint-close/60-02-SUMMARY.md
@docs/PERF-BASELINE.md
@docs/UAT-v1.8.md
</context>

<discovery>
## Read before Task 1

- **Inputs.** `C:/projects/mazeworld-perf60/artifacts/` holds `ddr-v1.7-6c299ab-debug.apk`, `ddr-v1.8-<sha7>-debug.apk`, `sizes.json`, `www-v1.7.json` and `www-v1.8.json` (plan 60-02). The two AABs were deleted after recording, so their byte counts live only in `sizes.json` and the `### AAB size` table. If the directory or `sizes.json` is missing, stop: plan 60-02 did not finish.
- **Telling the builds apart on the phone.** Both builds show "Version 1.5.0 (6)". v1.8 has the ☰ HUD menu and the Sound / Set dressing Settings rows; v1.7 has the ⚙ chip right of MAKE CAMP. The executor's install log in the UAT Session record is the authority on which build is installed.
- **Device (STATE Ground Truth).** The Pixel 7 connects over wireless adb (mDNS name `adb-28051FDH200H0R`; the IP:port rotates, so rediscover it with `adb mdns services`). The phone auto-locks quickly, which is why Task 1 asks the user to turn on Stay awake while charging. A locked screen during a timed launch shows up as a non-COLD or failed launch, and `tools/cold-start.mjs` rejects that with exit 2 or exit 1.
- **Reading back the numbers.** The dev overlay also logs `[mzperf] {json}` to logcat every 10 recorded steps (the Phase 49 Method). When adb is still connected after a walk, dumping logcat gives an optional machine read to cross-check the user's pasted line. The pasted line is always the record. A disagreement is noted in one line and never "corrected".
- **The regression lever named by the ROADMAP.** The ROADMAP's lever is "lazy-load / sprite the dressing images". CONTEXT adds that the dressing is already lazy-loaded after the first paint and never loaded when Set dressing is Off. So if cold start regresses, the honest levers are the eager party frames (~142 KB) or the Phase 57–59 shell work, not the props. Record that as context next to the user's ruling, never in place of it.
</discovery>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Connect the Pixel 7 and set it up for measurement</name>
  <action>Connect the Pixel 7 over wireless adb, prepare it so timed launches are clean, and answer two data-safety questions.</action>
  <instructions>
Already done without the phone:
- Plan 60-01 built and tested `tools/cold-start.mjs`.
- Plan 60-02 built a v1.7 debug APK (tag `v1.7`, `6c299ab`) and a v1.8 debug APK (HEAD) outside the repo, and measured both release AABs. The AAB bytes are already in `docs/PERF-BASELINE.md`.
- `docs/UAT-v1.8.md` holds the merged checklist.

What you need to do (only the phone needs you):
1. Put the phone on its charger, with battery at 50 % or more, and let it cool if it is warm. Close other apps and turn on Do Not Disturb.
2. In Developer options, turn on **Stay awake** (while charging) and **Wireless debugging**, on the same Wi-Fi as this PC. If this PC is no longer paired, choose "Pair device with pairing code" and put the code and its ip:port in your reply.
3. Under Accessibility, make sure **Remove animations is OFF** and TalkBack is off. Both are switched on later, only for their own passes in the checklist.
4. Unlock the phone, leave it on the home screen, and don't touch it until I say so. The cold-start series launches the app 22 times, which takes about 3 minutes.
5. Answer two questions:
   - **(a)** Is the Play-store (internal-testing) build installed right now, or a build from this PC? If it is the Play build, may I uninstall it? That wipes its save and graveyard.
   - **(b)** Checklist item 56-3 needs a first-ever launch in airplane mode. May I do a FRESH install of v1.8 later in the session? That means uninstall then install, which wipes this phone's save and graveyard. Answer yes or no. If you say no, 56-3 is recorded as not run.
  </instructions>
  <verification>`node tools/cold-start.mjs adb-path` resolves adb. Running that adb with `devices -l` lists the Pixel 7 (`model:Pixel_7`) in state `device`, after an `adb pair` / `adb connect` via `adb mdns services` if needed.</verification>
  <resume-signal>Reply "connected", then answers to (a) and (b), plus a pairing code and ip:port if pairing was needed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Freshness gate, device facts, and the cold-start series for both builds</name>
  <files>docs/PERF-BASELINE.md, docs/UAT-v1.8.md</files>
  <read_first>
    - `C:/projects/mazeworld-perf60/artifacts/sizes.json`: the APK files, the full commits, sha256 and AAB bytes.
    - `tools/cold-start.mjs` (header comment): the `run` / `judge` / `adb-path` flags and exit codes (0 ok, 1 failed launch or bad input, 2 non-COLD launch).
    - `docs/PERF-BASELINE.md` `## v1.8 — Phase 60` → `### Device` and `### Cold start (TotalTime, ms)`: the empty tables this task fills.
    - `docs/UAT-v1.8.md` `## A. Performance` row A1 and `## Session record`.
    - `.planning/phases/60-performance-footprint-close/60-02-PLAN.md` Task 1: the per-build procedure, which is needed only if the freshness gate fails.
  </read_first>
  <action>
Record `BASE_60_03` as `git rev-parse HEAD`, and let `ROOT=C:/projects/mazeworld-perf60`.

**1. Freshness gate (before any install).**
- `git diff --quiet "<sizes.json v1.8.sha>" HEAD -- mazeworld.html src/ engine/ content/ fonts/ icons/ sfx/ assets/ tools/build-www.mjs android/ capacitor.config.json package.json package-lock.json` must exit 0.
- If it does not, tell the user in one line that the v1.8 build is stale and is being rebuilt. Rebuild the v1.8 APK and AAB with plan 60-02 Task 1's per-build procedure (the v1.8 half only, same `ROOT`). Re-record `sizes.json`, the `### AAB size` table and the UAT `**Build:**` line, commit that on its own, then continue.
- Confirm that each APK file's sha256 equals `sizes.json`.

**2. Connect.**
- Set `ADB` to the output of `node tools/cold-start.mjs adb-path`.
- If the user gave a pairing code, run `"$ADB" pair <ip:port> <code>` first.
- Run `"$ADB" devices -l`. If the Pixel 7 is not listed, use `"$ADB" mdns services` to find the `adb-28051FDH200H0R` `_adb-tls-connect._tcp` ip:port, run `"$ADB" connect` to it, and check again.
- Set `SERIAL` to the serial `adb devices` shows. Pass `-s "$SERIAL"` on every adb call and `--serial "$SERIAL"` on every tool call.
- If the phone still cannot be reached, return a human-action checkpoint describing exactly what adb reported.

**3. Device facts.** Record in the `### Device` table:
- `getprop ro.product.model`, `ro.build.version.release` and `ro.build.display.id`.
- The session date.
- Battery level and temperature from `dumpsys battery`, where temperature is in tenths of a °C.

**4. What is installed now.** Read the app's `versionName`, `lastUpdateTime` and installer. Use `pm list packages -i com.darktierstudios.delvedierepeat` for the installer.
- If the installer is `com.android.vending` (a Play build), uninstall it ONLY if the user answered yes to (a). Otherwise stop and return a human-action checkpoint.
- Record what was found in the Session record.

**5. Cold start, v1.7.**
- `"$ADB" -s "$SERIAL" install -r` the v1.7 APK and expect `Success`. If the result is `INSTALL_FAILED_UPDATE_INCOMPATIBLE`, a different signer is on the phone, and step 4's rule applies.
- Log the install time.
- Run `node tools/cold-start.mjs run --label v1.7 --runs 10 --warmup 1 --serial "$SERIAL" --out "$ROOT/artifacts/cold-v1.7.json"`.
- On exit 1 or 2, re-run that build's whole series once. If it fails again, stop and return a human-action checkpoint: the phone must stay unlocked and awake. Never splice runs from two series.

**6. Cold start, v1.8.** `install -r` the v1.8 APK and log it. Run the same command with `--label v1.8 --out "$ROOT/artifacts/cold-v1.8.json"`, under the same retry rule. Then read battery level and temperature again.

**7. Provisional verdicts, for the user's information.** Run `node tools/cold-start.mjs judge --cold-base "$ROOT/artifacts/cold-v1.7.json" --cold-head "$ROOT/artifacts/cold-v1.8.json" --aab-base <v1.7 aabBytes> --aab-head <v1.8 aabBytes>`. The step row reads "not measured" here. Keep the output for Task 3's message. The final verdict is Task 6's full `judge` call.

**8. Record.**
- `### Cold start (TotalTime, ms)`: both rows, with the commit, n, median, p95, min, max, the LaunchStates (all COLD) and the samples in run order, all from the JSONs.
- `### Device`: the install order and the battery/temperature at the start and end of the series.
- UAT row A1's Result: pass, with both medians.
- UAT Session record: the install log so far.

**9. Set up the v1.7 walk.**
- `install -r` the v1.7 APK again.
- Run `am force-stop com.darktierstudios.delvedierepeat`, then `monkey -p com.darktierstudios.delvedierepeat 1`.
- Run `"$ADB" -s "$SERIAL" logcat -c` so that the optional cross-check only sees this walk.
- Log the install.

Commit `docs/PERF-BASELINE.md` and `docs/UAT-v1.8.md` together.
  </action>
  <acceptance_criteria>
    - For both `cold-v1.7.json` and `cold-v1.8.json`: `summary.n` is 10, every `launchStates` entry equals "COLD", and `invalid` is empty.
    - A node check slices `### Cold start (TotalTime, ms)` out of `docs/PERF-BASELINE.md`. It asserts that the rows anchored `^\| v1\.7 \|` and `^\| v1\.8 \|` carry exactly the medians and p95s of the matching JSON files.
    - The freshness gate's `git diff --quiet` exited 0, or the rebuild is recorded as its own commit.
    - `git diff --quiet "$BASE_60_03" HEAD -- mazeworld.html src/ engine/ content/ tools/ test/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "for(const b of ['v1.7','v1.8']){const j=require('C:/projects/mazeworld-perf60/artifacts/cold-'+b+'.json');if(j.summary.n!==10||j.launchStates.some(s=>s!=='COLD')||j.invalid.length)process.exit(1)}"</automated>
  </verify>
  <done>Both builds' cold start is measured on one phone in one sitting, with the same data and 10 COLD launches each, and recorded. The phone is on v1.7, relaunched and ready for the first walk.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Walk the Phase 49 step protocol on v1.7</name>
  <action>Walk the step protocol on the v1.7 build (checklist row A2) and paste the `#mw-dev-perf` line.</action>
  <instructions>
Already done: both builds' cold-start series. Paste into this message:
- The two medians and p95s.
- Task 2's provisional `judge` table (cold start and AAB). Say plainly if either is past its threshold. You will be asked to rule on it in Task 5, not now.

Also state: v1.7 is now installed and relaunched on your phone. It is the one with the ⚙ chip, and it has no ☰ menu.

What the user needs to do:
1. Title → ENTER → take any roll.
2. On the map, tap the ⚙ chip right of MAKE CAMP to open Settings.
3. Hold the "Version 1.5.0 (6)" label for about 1.2 s until the "Start at depth (dev)" row appears.
4. Enter 5 and tap Start.
5. Walk at least 50 steps, mixed the way you normally play. Include: a water pool; into a dark region and back out; at least one encounter card (fight, flee or parley); and at least one tab switch (Gear or Hero, then back to the map).
6. Open Settings again (⚙) and read the line under Start. It reads `step … · dispatch … · paint … · draw … ms (med / p95 / max, n=…)`. If n is below 50, keep walking the same run and read it again.

Reply with:
- The line, pasted exactly.
- Coverage: water yes/no, dark in+out yes/no, encounter yes/no, tab switch yes/no.
- Jank: "none", or where you saw it.
  </instructions>
  <verification>The pasted line parses into the four rows step / dispatch / paint / draw, each with a med / p95 / max, and has n ≥ 50.</verification>
  <resume-signal>The pasted `#mw-dev-perf` line, the four coverage answers, and the jank note.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Record the v1.7 walk and switch the phone to v1.8</name>
  <files>docs/PERF-BASELINE.md, docs/UAT-v1.8.md</files>
  <read_first>
    - `docs/PERF-BASELINE.md` `### Step time (ms)`: the empty table, the v1.7 verbatim-readout block and the As-run line.
    - The user's Task 3 reply.
  </read_first>
  <action>
**1. Record the v1.7 walk.**
- Paste the user's line verbatim into the v1.7 readout code block.
- Fill the v1.7 columns of the Step time table by parsing each `<row> <med> / <p95> / <max>` group and the trailing `n=`.
- Write the v1.7 As-run line (the coverage answers as given, and the jank note in quotes).
- If the user gave two reads, record both, mark the later one authoritative, and do not average them.
- UAT row A2's Result: pass (or fail, with the reason, if n < 50 could not be reached), plus the step med / p95.

**2. Optional cross-check.** Do this only if adb is still connected. Run `"$ADB" -s "$SERIAL" logcat -d` and take the last line matching `\[mzperf\] \{`. Compare its step median and p95 with the pasted line at 0.1 ms. Record agreement in one line. If they disagree, the pasted line stays the record.

**3. Threshold for the user.** Let X = the v1.7 step p95 + 2.0 ms. This is the level above which the v1.8 step p95 would regress. It is only for the Task 5 message; the verdict itself is Task 6's `judge`.

**4. Switch to v1.8.**
- If the user answered yes to (b): `"$ADB" -s "$SERIAL" uninstall com.darktierstudios.delvedierepeat`, then `install` the v1.8 APK. Do NOT launch it: 56-3 needs the user's own first launch in airplane mode.
- Otherwise: `install -r` the v1.8 APK, then force-stop and relaunch it with `monkey`.
- Either way, run `logcat -c` and log the install (fresh or over) in the Session record.

Commit both docs.
  </action>
  <acceptance_criteria>
    - A node check on the `### Step time (ms)` slice: the rows anchored `^\| step \|`, `^\| dispatch \|`, `^\| paint \|` and `^\| draw \|` each have their four v1.7 cells filled with numbers, and those numbers equal the ones parsed from the verbatim v1.7 readout block in the same slice.
    - The Session record's install log shows v1.8 as the latest install, marked fresh or over.
    - `git diff --quiet "$BASE_60_03" HEAD -- mazeworld.html src/ engine/ content/ tools/ test/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/PERF-BASELINE.md','utf8');const i=t.indexOf('### Step time (ms)');const j=t.indexOf('### Verdicts (PERF-03)');const s=t.slice(i,j);const rows=['step','dispatch','paint','draw'].map(r=>s.split(/\r?\n/).find(l=>l.startsWith('| '+r+' |')));process.exit(rows.every(l=>l&&l.split('|').slice(2,6).every(c=>/^\s*\d+(\.\d+)?\s*$/.test(c)))?0:1)"</automated>
  </verify>
  <done>The v1.7 walk is recorded verbatim and parsed into the table. The phone holds v1.8, installed fresh if the user OK'd it, otherwise installed over and relaunched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 5: Walk v1.8, run the UAT-v1.8 checklist, and rule on every regression</name>
  <action>Walk the step protocol on v1.8 (row A3), run checklist sections B–E, and give a ruling for each measurement past its threshold.</action>
  <instructions>
Already done: the v1.7 walk is recorded. Paste into this message:
- The v1.7 step med / p95 and the threshold X (a v1.8 step p95 above X ms regresses).
- The cold-start and AAB verdicts from Task 2, naming any that regress.
- Whether v1.8 was installed fresh (unlaunched) or over the top.

What the user needs to do, in this order:
1. **Only if v1.8 was installed fresh (item 56-3).** Turn on airplane mode, with Wi-Fi off too. Open the app from the launcher: this is its first launch ever. Check that every clip plays and nothing is fetched. Then turn airplane mode off.
2. **A3, the v1.8 walk.** Title → ENTER → any roll → ☰ → SETTINGS → hold the Version label about 1.2 s → depth 5 → Start. Walk at least 50 mixed steps with the same coverage as before, then read the line under Start. If your step p95 is above X, you may keep walking the same run and read it again; the later read counts, and both are recorded.
3. **Checklist sections B–E** of `docs/UAT-v1.8.md`. For each id, answer pass, fail (plus what you saw), or not run (plus why). Do all the "reduced-motion pass" items together at the end with Android's Remove animations ON, then turn it back off. Do all the "TalkBack pass" items together.
4. **Rulings.** For each measurement flagged as regressing (cold start or AAB from above, and step if your p95 is above X), reply either "accept: <your reason>" or "fix". Ignore this step if nothing regressed.

Reply template:
- A3 line: <paste>
- Coverage: water / dark / encounter / tab
- Jank: none, or where
- 56-3 … 59-N: one line each (pass / fail … / not run …)
- Rulings: cold start: … / step: … / AAB: …
  </instructions>
  <verification>The A3 line parses into the four rows with n ≥ 50. Every B–E id has an answer, and every regression flagged by the executor has a ruling.</verification>
  <resume-signal>The A3 line with coverage and jank, one result per checklist id, and a ruling per regression (or "no regressions").</resume-signal>
</task>

<task type="auto">
  <name>Task 6: Record the v1.8 walk, apply the tested verdicts, write the dispositions and UAT results, file todos, close</name>
  <files>docs/PERF-BASELINE.md, docs/UAT-v1.8.md, .planning/todos/pending/</files>
  <read_first>
    - The user's Task 5 reply.
    - `docs/PERF-BASELINE.md` `### Step time (ms)`, `### Verdicts (PERF-03)` and `### Dispositions`.
    - `docs/UAT-v1.8.md` sections A–E and `## Session record`.
    - One existing todo, for the house format: `.planning/todos/pending/2026-09-22-climb-leap-retry-card-is-stale-under-one-and-done.md`. It has frontmatter `created` / `title` / `area` / `files`, then `## Problem` in the user's words, then `## Solution`.
  </read_first>
  <action>
**1. Step, v1.8.** Record it exactly as Task 4 step 1 did for v1.7: the verbatim block, the table columns and the As-run line, with superseded reads kept. Do the optional logcat cross-check only if adb is still connected. If airplane mode dropped the connection, skip it and say so; do not ask the user to reconnect for this alone.

**2. Verdicts.** Run `node tools/cold-start.mjs judge` with all six numeric inputs:
- The two cold JSONs.
- `--step-base` = the v1.7 authoritative step p95 and `--step-head` = the v1.8 authoritative step p95.
- The two AAB byte counts from `sizes.json`.

Paste the Markdown table into `### Verdicts (PERF-03)`, with the `[perf03]` JSON in a fenced block beneath it. `complete` must be true. Add one line per measure with a positive but sub-threshold delta, saying it was recorded and needs no disposition (CONTEXT).

**3. Dispositions.** Write one bullet per measure whose `regresses` is true, starting `- **Cold start**`, `- **Step p95**` or `- **AAB**`, then the numbers and the user's ruling verbatim in quotes:
- For an "accept" ruling, label it "Accepted miss —" followed by the user's reason. Underneath, add the factual context from the discovery section, labelled "Context (executor)".
- For a "fix" ruling, label it "Fix — pending gap closure" and name the lever. For cold start or the AAB, that is the dressing images' lazy-load / sprite from the ROADMAP; note that the props are already lazy and Off-aware, so spriting or deferring the eager party frames or the clip set is the live lever. For step, name the row. File the todo and note it here.
- If a regression has no ruling, stop and return a human-action checkpoint asking for that one ruling. Never infer one.
- If nothing regressed, write the single bullet `- None — no measurement regressed past a PERF-03 threshold (verdict table above).`

**4. UAT results.**
- Fill each B–E Result cell with the user's answer, starting pass / fail / not run.
- Fill rows A3–A5 (A4: the AAB verdict; A5: the rulings, or "no regressions").
- Complete `## Session record`: the install log; the final build on the phone (the v1.8 APK file name, full commit and sha256); the rulings pointer; and the findings → todos list.

**5. Todos.** Write one file per fail, and one per extra finding the user volunteered, as `.planning/todos/pending/<today>-<slug>.md` in the house format:
- `title`: the problem, in plain words.
- `area`: ui, audio, perf or tooling.
- `files`: the best-known locations, as pointers only.
- `## Problem`: the UAT id and the user's words.
- `## Solution`: a direction, not a plan.

Edit no code (deferred-UAT protocol).

**6. Gates.**
- `git diff --quiet "$BASE_60_03" HEAD -- mazeworld.html src/ engine/ content/ tools/ test/` exits 0.
- `git diff --quiet v1.7 HEAD -- engine/ content/ test/parity/` exits 0.
- `package.json` / `package-lock.json` are unchanged.
- `npm test` fail 0 and `npm run build:www` exit 0 on the main checkout. In an isolated worktree without `node_modules`, record "the orchestrator runs it after merge" (59-01 precedent).

Commit the two docs and the todos.

**7. SUMMARY.** It must state:
- Per ROADMAP criterion 1–4: met or not, with the numbers.
- The verdict table and the dispositions.
- The UAT pass / fail / not-run counts, and the todo list.
- A `## Gaps for gap closure` heading listing every "fix" disposition (so the orchestrator runs `/gsd-plan-phase 60 --gaps`), or saying there are none.
- The final build on the phone.
- A closing note for the orchestrator: per the standing rule, ask the user separately whether to push a Play internal-testing build. That push is not part of this phase.
  </action>
  <acceptance_criteria>
    - `### Verdicts (PERF-03)` holds a line anchored `^\[perf03\] \{` (inside its fenced block) whose JSON has `complete: true`, and the table header line `| Measure | v1.7 | v1.8 | Delta | Threshold | Regresses? |`.
    - Dispositions cover every regression. For each measure with `regresses: true` in that JSON, `### Dispositions` has exactly one line anchored `^- \*\*(Cold start|Step p95|AAB)\*\*` for it, containing a quoted user ruling. When `anyRegression` is false, the only bullet is the anchored `^- None — no measurement regressed`.
    - Every UAT row anchored `^\| (5[6-9]-\d+|A[1-5]) \|` has a non-empty last cell. For the B–E rows, that cell matches `^(pass|fail|not run)\b` (case-insensitive, after trim).
    - Every fail has a todo. For each B–E row whose Result starts with fail, at least one todo file added in this plan (`git diff --name-only --diff-filter=A "$BASE_60_03" HEAD -- .planning/todos/pending/`) contains that id bounded by non-digits (regex `(^|[^0-9])<id>([^0-9]|$)`).
    - `git diff --quiet "$BASE_60_03" HEAD -- mazeworld.html src/ engine/ content/ tools/ test/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/UAT-v1.8.md','utf8').split(/\r?\n/);const rows=t.filter(l=>/^\| (5[6-9]-\d+|A[1-5]) \|/.test(l));const ok=rows.length>=30&&rows.every(l=>{const c=l.split(String.fromCharCode(92)+'|').join('').split('|').slice(1,-1);const last=(c[c.length-1]||'').trim();return last.length>0&&(!/^\| 5/.test(l)||/^(pass|fail|not run)\b/i.test(last))});process.exit(ok?0:1)"</automated>
  </verify>
  <done>PERF-03 is measured, judged by the tested thresholds and recorded, and every regression has the user's own disposition. The UAT-v1.8 batch is recorded item by item with todos for every finding, and the milestone's one debug APK is on the phone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Executor → phone | adb installs and uninstalls can destroy the player's save and graveyard. |
| User report → the record | Numbers and rulings cross from chat into the permanent perf record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-09 | Denial of Service (data loss) | Uninstall or fresh install on the Pixel 7 | high | mitigate | An uninstall happens only after an explicit yes to question (a) or (b) in Task 1. `install -r` keeps data otherwise. `pm clear` is prohibited. The measurement tool itself cannot uninstall (plan 60-01). |
| T-60-10 | Repudiation | The executor self-accepting a regression, or averaging away a bad read | high | mitigate | Dispositions quote the user's ruling verbatim, and a missing ruling stops the plan (Task 6 step 3). Superseded reads are kept, never averaged. Verdicts come only from the tested `judge` (plan 60-01). |
| T-60-11 | Tampering | A stale APK measured as "v1.8" | medium | mitigate | Task 2's freshness gate compares the APK's commit against HEAD over every shipped path, and the sha256 is checked against `sizes.json` before any install. |
| T-60-12 | Tampering | A mid-session "quick fix" breaking the presentation gate | medium | mitigate | No code path is in `files_modified`, and the anchored `git diff --quiet` gate over code paths runs in Tasks 2, 4 and 6. Findings become todos. |
| T-60-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package install. `package.json` / `package-lock.json` are asserted unchanged. |
</threat_model>

<verification>
- `docs/PERF-BASELINE.md`'s v1.8 section has both cold-start rows, all eight step-time columns, the `judge` table with `complete: true`, and a disposition for every regression (or the recorded "None").
- `docs/UAT-v1.8.md` has a result for every row, and every fail has a todo.
- `git diff --quiet v1.7 HEAD -- engine/ content/ test/parity/` exits 0, and `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
- No code file changed in this plan. `npm test` fail 0 and `npm run build:www` exit 0 on the main checkout, or the orchestrator runs them after merge.
</verification>

<success_criteria>
- Cold start (criterion 1) and step time (criterion 2) for v1.7 and v1.8 are measured on the Pixel 7 in one session and recorded in `docs/PERF-BASELINE.md`.
- The AAB change (criterion 3) is judged against its threshold from plan 60-02's recorded bytes.
- Every regression carries a recorded disposition in the user's words, and none is silent (criterion 4).
- The merged UAT-v1.8 checklist is run and recorded, findings are filed as todos, and the milestone closes with the v1.8 debug APK on the phone.
</success_criteria>

<output>
Create `.planning/phases/60-performance-footprint-close/60-03-SUMMARY.md` when done.
</output>
