---
phase: 77-combat-screen-oracle-readability
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/foeConditions.js
  - src/browser/foeDetails.js
  - test/unit/foe-conditions.test.js
  - test/unit/foeDetails.test.js
autonomous: true
requirements:
  - CMBUI-13
must_haves:
  truths:
    - "CMBUI-13 foe side (CONTEXT: 'Extend coverage so every foe-side effect has a chip, including effects from this milestone (a foe shielded by a fumbled scroll, Phase 75.1)'): every per-foe field Phase 75.1 gives a foe through a fumbled helpful scroll (its ward pool or armed Bubble, Mirror Self, Strength, Regeneration, Sense Presence, and any sibling field 75.1-03/75.1-05 introduced) is a FOE_CONDITIONS entry with a house-style label, a tone and a FOE_CONDITION_DESC line, instead of a parked NOT_A_CONDITION exclusion"
    - "Each new foe chip reads its own count the Phase 71 way: a timed one shows ' · n' (a ward's rounds, Mirror Self's rounds) and an untimed one its bare label; a foe BUFF is tone bad (bad for the player), matching Frenzied"
    - "Every new description is written from the engine code that applies and consumes the effect (read only), in the deadpan family-friendly voice, says HP never WP, and states no number the player cannot already see (the existing digit rule of foe-conditions.test.js holds)"
    - "Foe Sense Presence has no mechanic once a fight is joined (75.1-03), so its description says so plainly rather than implying an effect"
    - "The long-press foe card states each new chip's to-hit effect where one exists, measured through rollOdds.js (a mirrored foe: 'you hit it only on <range>' from heroHitOddsVs); every other new chip keeps the plain 'label — desc' line"
    - "A tap on a foe card, chips included, still only aims (Phase 71); descriptions stay on the long press"
    - "The foe coverage guard passes with every assigned foe field covered or on NOT_A_CONDITION with a reason, and its self-check finds the new fields"
  artifacts:
    - path: src/browser/foeConditions.js
      provides: "FOE_CONDITIONS entries, labels and descriptions for every fumble-given foe effect"
      contains: "FOE_CONDITION_DESC"
    - path: src/browser/foeDetails.js
      provides: "foeConditionEffect for the to-hit-moving new chips"
      contains: "export function foeConditionEffect"
    - path: test/unit/foe-conditions.test.js
      provides: "the new chip cases, the order pin and the guard without the parked exclusions"
  key_links:
    - from: src/browser/foeConditions.js#FOE_CONDITIONS (new entries)
      to: "the per-foe fields engine/scrollFumble.js#resolveScrollFumble sets and engine/combat.js / engine/foeDamage.js consume (75.1-03, 75.1-05)"
      via: "fields and when()"
      pattern: "fields: \\["
    - from: src/browser/foeDetails.js#foeConditionEffect
      to: src/browser/rollOdds.js#heroHitOddsVs
      via: "the measured range for a mirrored foe"
      pattern: "heroHitOddsVs\\("
  prohibitions:
    - statement: "MUST NOT hide a foe's gains: a foe the player's fumble made stronger shows it on its card like any other condition"
      requirement_id: CMBUI-13
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT make a foe chip a separate tap target or change the foe card's aim-on-tap behaviour, and MUST NOT edit the engine to satisfy the guard"
      requirement_id: CMBUI-13
      category: safety
      status: resolved
      verification: test
---

<objective>
Finish the foe half of CMBUI-13: every effect a foe can carry this milestone, including the gifts a fumbled scroll hands it, shows as a chip on its card with a description on the long press.

Purpose: CONTEXT's foe-side decision ("keep Phase 71's model ... Extend coverage so every foe-side effect has a chip, including effects from this milestone (a foe shielded by a fumbled scroll, Phase 75.1)") and 75.1-03's handoff ("left for Phase 77's indicator").

Output: new FOE_CONDITIONS entries with descriptions, the long-press effect clause for the ones that move a roll, and the guard without parked exclusions.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 77-04 creates or changes.)

- 77-01: `src/browser/combatMenu.js` spell sort and ITEMS EQUIPPED/NOT EQUIPPED rows; `mazeworld.html` submenu CSS; `test/unit/combat-submenu-fit.test.js`; `test/unit/combat-items-equipped.test.js`
- 77-02: `src/browser/narrationLines.js` event-order mode; `fightLog.js` and `combatBeat.js` on it; `test/unit/event-order-fold.test.js`
- 77-03: `engine/derived.js` conditionsOf extended and `memberConditionsOf`; `src/browser/heroConditions.js`; `src/browser/conditionEffects.js` WHAT_IF and the "instead of" lead; `test/unit/hero-conditions.test.js`
- **This plan (77-04):** `src/browser/foeConditions.js` entries, labels and descriptions for every fumble-given foe effect (ward/Bubble, Mirror Self, Strength, Regeneration, Sense Presence and any sibling field); `src/browser/foeDetails.js#foeConditionEffect` for the to-hit-moving ones; the guard without parked exclusions
- 77-05: the scroll copy note; `test/unit/scroll-cast-copy-note.test.js`
- 77-06: `combatPanel.js#foeFamily` and the foe card family; the encounter-start row's dice; `test/unit/foe-family-card.test.js`, `test/unit/fight-log-oldest-row.test.js`
- 77-07: condition terms in strike `mods`, `DAZED_TO_HIT_PENALTY`, onset lines that name the effect; `test/unit/condition-roll-mods.test.js`; the Phase 77 record in `test/parity/FIXTURE-INVENTORY.md`
- 77-08: YOUR LOT chip rows and the chip tap sheet; `combatPanel.js#yourLotViewModel` chips; the new bridges; `test/unit/your-lot-chips.test.js`

## Flagged assumptions (surfaced, not silently decided)

- **The exact foe fields are whatever 75.1 shipped.** 75.1-03's plan names a foe ward (a Shield pool with rounds, or an armed Bubble), a rebound, mirror, might, strengthBoost, regen and senses; the executor reads the merged engine and 75.1-03/75.1-05 SUMMARYs for the real names and shapes and covers each one. How 75.1 got its new fields past the foe guard (NOT_A_CONDITION entries, or chips) is read from the merged test file; parked exclusions become chips here.
- **Foe Sense Presence gets a chip even though it does nothing mid-fight**, with a description that says so: the player caused it with a fumble and should see it, honestly labelled.
- **Labels follow the one-word house style**, for example "Shielded", "Bubbled", "Mirrored", "Strong", "Regenerating", "Senses"; the executor picks the words within that rule.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-combat-screen-oracle-readability/77-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-06-SUMMARY.md

<interfaces>
**Run order:** Phases 75.1, 75.2, 75.3 and 76 are merged before this executes. Read 75.1-03's and 75.1-05's SUMMARYs (the foe ward, the Bubble catch and rebound, foe Mirror Self's strike cap and tick, foe Strength in foeLevelBase, foe Regeneration, foe senses; the resolver that sets them) and 75.3-03/75.3-04's (elite in NOT_A_CONDITION; `held` and `resisted` chips already added). Reference functions by name.

src/browser/foeConditions.js: FOE_CONDITION_COPY (labels), FOE_CONDITION_DESC (one sentence per label key), FOE_CONDITIONS (frozen entries: key, label, desc, tone, fields, when(foe, state), rounds(foe, state), optional labelFor/descFor), foeConditionChips(foe, state) -> frozen { key, label, tone, rounds, text, desc }. Chip order is table order; the order test pins it.

src/browser/foeDetails.js#foeConditionEffect(chip, foe, state): asleep/stupid fill `effectYou` from heroHitOddsVs(state, foe).text; blind/weakened fill `effectIt` from foeHitOddsVs(state, foe).plainText; every other key returns null. effectLines(foe, state) writes "<chip text> — <effect>. <chip desc>" when an effect exists, else "<chip text> — <chip desc>". Every read is guarded (a partial hero drops the clause, never throws).

test/unit/foe-conditions.test.js: sections (a) one case per condition, (b) rounds, (c) tone/order/determinism ("tone: every foe debuff is good; Frenzied alone is bad" must now allow the new foe buffs as bad), (d) malformed/hostile, (e) the guard (ENGINE_FILES list: add engine/scrollFumble.js if 75.1-05 created it and it assigns foe fields), (f) copy, (g) descriptions (the digit rule: only "2" and "3").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Chips for every fumble-given foe effect</name>
  <files>src/browser/foeConditions.js, test/unit/foe-conditions.test.js</files>
  <read_first>
    - src/browser/foeConditions.js (the whole module)
    - test/unit/foe-conditions.test.js (all sections, the ENGINE_FILES list and NOT_A_CONDITION as merged)
    - engine/scrollFumble.js (resolveScrollFumble's helpful branch) and engine/foeDamage.js / engine/combat.js (where the foe ward, Bubble, mirror, might, regen and senses are consumed and ticked)
    - .planning/phases/75.1-pilfer-fumbles-scroll-reading/75.1-03-SUMMARY.md and 75.1-05-SUMMARY.md
  </read_first>
  <behavior>
    - One case per new effect, built by setting the field exactly as the resolver sets it (cite the function): e.g. a foe with a Shield ward of pool 12 and 3 rounds shows "Shielded · 3" tone bad; an armed Bubble shows its bare label; Mirror Self with 2 rounds shows "Mirrored · 2"; Strength shows "Strong"; Regeneration shows "Regenerating"; senses shows "Senses".
    - When the engine clears or ticks a field to 0 the chip goes (a ward at 0 rounds, mirror at 0).
    - The order test lists the new entries at their chosen positions; tone test: foe buffs (the new ones and Frenzied) are bad, every debuff good.
    - Hostile getters and odd values drop only that chip.
    - The guard passes with no parked Phase 77 exclusion left; the self-check asserts the scan finds each new field.
    - Each new FOE_CONDITION_DESC line obeys the existing rules (one sentence, voice-safe, no WP, digits only "2" or "3").
  </behavior>
  <action>
1. Tests first in test/unit/foe-conditions.test.js: add the cases above; move any 75.1 foe field parked on NOT_A_CONDITION (for example "Phase 77 draws its indicator") off the list; extend ENGINE_FILES to every engine module that assigns foe fields (add engine/scrollFumble.js when it exists); extend the tone and order tests and the copy-label list. Watch them fail.
2. foeConditions.js, per CONTEXT's foe-side decision: add one FOE_CONDITIONS entry per effect (key, label from FOE_CONDITION_COPY, desc from FOE_CONDITION_DESC, tone "bad", `fields` naming every foe field it reads, `when`, `rounds` via posInt). Write each description from the engine code (read only) with a comment citing the function that applies and the one that consumes it. Update the module header: foe buffs from a fumble are tone bad like Frenzied; Phase 77 (CMBUI-13) completes the foe side.
  </action>
  <verify>
    <automated>node --test test/unit/foe-conditions.test.js test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/foe-conditions.test.js` passes, including the new cases, the order and tone tests, the guard and its self-check.
    - `grep -c "Phase 77" test/unit/foe-conditions.test.js` is at least 1 (the section header naming CMBUI-13), and no NOT_A_CONDITION reason defers a foe field to Phase 77.
    - `node --test test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js` passes.
  </acceptance_criteria>
  <done>A foe that a fumbled scroll shielded, mirrored, strengthened or healed shows it on its card.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The long press states the new chips' to-hit effect</name>
  <files>src/browser/foeDetails.js, test/unit/foeDetails.test.js</files>
  <read_first>
    - src/browser/foeDetails.js (foeConditionEffect, effectLines, FOE_DETAILS_COPY, oddsLine)
    - src/browser/rollOdds.js (heroHitOddsVs, foeHitOddsVs)
    - test/unit/foeDetails.test.js (the fullHeroState helper and the Phase 74 condition-effect block)
  </read_first>
  <behavior>
    - A mirrored foe's long-press line reads "Mirrored · 2 — you hit it only on <range>. <desc>", the range from heroHitOddsVs(state, foe) for the same state (asserted equal).
    - Every other new chip returns null from foeConditionEffect and keeps "<text> — <desc>".
    - A minimal state without a full hero drops the clause and never throws.
    - Every previously pinned chip's line is byte-identical.
  </behavior>
  <action>
1. Tests first in test/unit/foeDetails.test.js (the Phase 74 block's fullHeroState pattern). Watch them fail.
2. foeDetails.js: extend foeConditionEffect with the new to-hit-moving keys (at least the mirrored foe; any other new chip whose effect changes the hero's odds against it or its odds against the hero, measured through heroHitOddsVs/foeHitOddsVs, never a restated number). If a new copy template is needed, add it to FOE_DETAILS_COPY.
3. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/foeDetails.test.js test/unit/foe-conditions.test.js test/unit/roll-sign-consistency.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/foeDetails.test.js` passes, including the mirrored-foe measured range and the unchanged earlier lines.
    - `git diff --quiet <plan-base> -- engine/ mazeworld.html` exits 0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Long-pressing a foe explains every effect on it, with the measured odds where the effect moves a roll.</done>
</task>

</tasks>

<verification>
- Every per-foe effect field has a chip or a reasoned exclusion; the fumble gifts show as tone-bad chips with honest descriptions.
- The long press measures the to-hit-moving ones; foe card taps still only aim.
- engine/ and mazeworld.html are untouched; `npm test` is green.
  <human-check>
  Pixel 7, at milestone close:
  - Fumble a Shield scroll in a fight (a non-Magic-User with low intelligence): the targeted foe's card shows "SHIELDED · n" in its tag line, and a long press explains what the shield does.
  - A fumbled Mirror Self shows "MIRRORED · n" on the foe, and its long press gives the range you now hit it on.
  </human-check>
</verification>

<success_criteria>
The foe half of ROADMAP Phase 77 criterion 4 holds: every effect on a foe, including this milestone's fumble gifts, shows an indicator on the foe it affects with its rounds, a description on the long press, and it clears when the effect ends.
</success_criteria>

<output>
Create `.planning/phases/77-combat-screen-oracle-readability/77-04-SUMMARY.md` when done. Include the plan base SHA, the new entries (field, label, tone, rounds, description), the exclusions removed from NOT_A_CONDITION, and the new long-press lines.
</output>
