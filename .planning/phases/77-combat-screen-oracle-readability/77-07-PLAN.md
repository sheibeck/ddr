---
phase: 77-combat-screen-oracle-readability
plan: 07
type: execute
wave: 3
depends_on:
  - 77-03
  - 77-05
files_modified:
  - engine/derived.js
  - engine/combat.js
  - engine/foeAbilities.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/condition-roll-mods.test.js
  - test/unit/roll-sign-consistency.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/foe-abilities.test.js
  - test/unit/narrationLinesTable.test.js
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements:
  - CMBUI-13
must_haves:
  truths:
    - "Dazed honesty (CONTEXT, user 2026-09-25): every hero strike roll a live condition changes carries that condition in its `mods` list: a dazed strike lists 'dazed' with the engine's own delta (−2 faces, floored), and so do Inspired ('inspired', +1), the dark cap ('dark', the cap's delta) and 75.1's hero Blind ('blind'), whichever is live; the Oracle line and the fight-log reveal print them through rollRange.js exactly like 'afraid' today"
    - "The itemised terms come from ONE breakdown beside toHit in engine/derived.js that mirrors toHit's arithmetic step for step and is proven equal to toHit(state) by a matrix test (the foeToHitBreakdown precedent); playerStrike prepends its condition entries to the ability and afraid mods; the faces, the roll and every draw are unchanged"
    - "The onset line names the effect (CONTEXT: 'The onset line names the effect'): a Djinni's daze reads that you are −2 to hit for N rounds, the number coming from the event's own payload (engine/derived.js exports DAZED_TO_HIT_PENALTY, used by toHit, and foeDebuffed carries it), printed through rollRange.js; a weakening reads that your blows do half damage for N rounds; the fade lines say the effect ended; both the Oracle table and the fold table say it"
    - "Audit (CONTEXT: 'Audit weakened and every other condition for the same gap'): the SUMMARY carries a table of every hero condition chip, whether it moves a roll, and whether that roll's mods list names it after this plan; any remaining gap is named with its reason"
    - "Measured, not assumed: this is an event-payload change only; the parity suite passes with test/parity/fixtures and prototype-master.js.txt byte-identical, every state pin in test/unit/roll-high-state-pins.test.js is unchanged, and the draw inventory is unchanged; a Phase 77 section in test/parity/FIXTURE-INVENTORY.md records the measured zero"
    - "The cross-surface sign guard covers dazed: a real dazed playerStrike reads 'dazed −2' on the Oracle line and on the fight-log reveal (U+2212), and never with the opposite sign anywhere"
  artifacts:
    - path: engine/derived.js
      provides: "DAZED_TO_HIT_PENALTY and the toHit breakdown whose condition entries feed strike mods"
      contains: "DAZED_TO_HIT_PENALTY"
    - path: engine/combat.js
      provides: "playerStrike's mods carry the live condition terms"
      contains: "toHitBreakdown("
    - path: engine/foeAbilities.js
      provides: "foeDebuffed carries the to-hit delta for a daze"
      contains: "DAZED_TO_HIT_PENALTY"
    - path: test/unit/condition-roll-mods.test.js
      provides: "the breakdown matrix, the strike mods per condition, the payload and the onset lines"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the Phase 77 measured-zero record"
      contains: "## Phase 77"
  key_links:
    - from: engine/combat.js#playerStrike
      to: engine/derived.js (the toHit breakdown)
      via: "the condition entries prepended to mods"
      pattern: "toHitBreakdown\\(state"
    - from: src/browser/eventNarration.js#foeDebuffed
      to: src/browser/rollRange.js#toHitText
      via: "the onset line's signed number from the payload"
      pattern: "toHitText\\("
  prohibitions:
    - statement: "MUST NOT change any roll, face count, draw or state: this plan only adds what already happened to the event payload and the lines (engine gate: measured zero, declared)"
      requirement_id: CMBUI-13
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT state an effect's number in copy by hand: the onset and mods numbers come from the engine's own constant and delta, formatted by rollRange.js"
      requirement_id: CMBUI-13
      category: transparency
      status: resolved
      verification: test
---

<objective>
Make the conditions honest in the rolls themselves: a dazed (or inspired, blinded, dark-capped) strike says so in its modifier list, and the moment you are dazed or weakened the Oracle and the fight log say what it does and for how long.

Purpose: the user's 2026-09-25 report ("i was dazed in combat, but it seems like it doesn't do anything. The description is too vague to know") and CONTEXT's Dazed honesty decision ("The onset line names the effect. Every roll affected carries the modifier in its mods list. Audit weakened and every other condition for the same gap."). The chip's tap description half lives in 77-03 (measured lead) and 77-08 (the sheet).

Output: a toHit breakdown feeding strike mods, the DAZED constant and the foeDebuffed payload, the reworded onset and fade lines, the sign guard's dazed case, and the measured-zero record.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 77-07 creates or changes.)

- 77-01: `src/browser/combatMenu.js` spell sort and ITEMS EQUIPPED/NOT EQUIPPED rows; `mazeworld.html` submenu CSS; `test/unit/combat-submenu-fit.test.js`; `test/unit/combat-items-equipped.test.js`
- 77-02: `src/browser/narrationLines.js` event-order mode; `fightLog.js` and `combatBeat.js` on it; `test/unit/event-order-fold.test.js`
- 77-03: `engine/derived.js` conditionsOf extended and `memberConditionsOf`; `src/browser/heroConditions.js`; `src/browser/conditionEffects.js` WHAT_IF and the "instead of" lead; `test/unit/hero-conditions.test.js`
- 77-04: `src/browser/foeConditions.js` chips for every fumble-given foe effect; `src/browser/foeDetails.js#foeConditionEffect`
- 77-05: the scroll copy note; `test/unit/scroll-cast-copy-note.test.js`
- 77-06: `combatPanel.js#foeFamily` and the foe card family; the encounter-start row's dice; `test/unit/foe-family-card.test.js`, `test/unit/fight-log-oldest-row.test.js`
- **This plan (77-07):** `engine/derived.js` `DAZED_TO_HIT_PENALTY` and a toHit breakdown; `engine/combat.js#playerStrike` mods carry the live condition terms (dazed, inspired, dark, blind); `engine/foeAbilities.js` foeDebuffed payload; onset and fade lines in both narration tables; `test/unit/condition-roll-mods.test.js`; the dazed case in `test/unit/roll-sign-consistency.test.js`; the Phase 77 measured-zero section in `test/parity/FIXTURE-INVENTORY.md`
- 77-08: YOUR LOT chip rows and the chip tap sheet; `combatPanel.js#yourLotViewModel` chips; the new bridges; `test/unit/your-lot-chips.test.js`

## Flagged assumptions (surfaced, not silently decided)

- **Which rolls count.** "Every roll affected carries the modifier in its mods list" is read as every roll whose to-hit a live hero condition changes: the hero's strike (dazed, inspired, the dark cap, hero Blind; Afraid already does). The foe's swing already lists its hero-side terms (Sidestep, Smoke, Battle Roar, Mirror, invisible, size, insulted) through foeToHitBreakdown and foeSwingVsHero. Damage halving (Weakened, Afraid, hero Shrink) is not a roll modifier; its honesty is the onset line and the chip sheet. The audit table in the SUMMARY records every case.
- **Each term is itemised as applied**, in toHit's own order, even when a later per-target rule (a dozing foe's floor, a cap) overrides it: the foeToHitBreakdown precedent (Sidestep and Smoke both listed when Smoke's override follows).
- **The frenzy second swing** reads the same condition entries (its faces are toHit minus one, Phase 72).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/77-combat-screen-oracle-readability/77-CONTEXT.md
@.planning/todos/pending/2026-09-25-dazed-works-but-nothing-says-what-it-does.md
@.planning/phases/77-combat-screen-oracle-readability/77-03-SUMMARY.md
@.planning/phases/77-combat-screen-oracle-readability/77-05-SUMMARY.md

<interfaces>
**Run order:** 77-03 (engine/derived.js conditionsOf) and 77-05 (the narration tables) are merged, as are 75.1-04 (the heroBlind term at the end of toHit), 75.2 (size terms) and 76-01 (toHit's dark cap is `darkLimited(state) && !c.senses`). Read the current toHit; reference functions by name.

engine/derived.js#toHit(state) (shape at the 75 close): classNeed(c); `+ combat.inspired`; `+ eff(c, "toHit")`; `+ weaponNeedMod(c)`; floor 1; dazed `h = Math.max(1, h - 2)` while `c.foeEffect.kind === "dazed" && rounds > 0`; the dark cap `h = Math.min(h, 2)`; then 75.1-04's heroBlind (one face) and any 75.2 term. foeToHitBreakdown (same file) is the model: it mirrors foeToHitVs step for step, records `{ name, delta }` for each step that changed the value, and test/unit/feedback-payload.test.js proves `need === foeToHitVs(state, vs)` by matrix.

engine/combat.js#playerStrike: `let faces = a === 1 && frenzyFired ? Math.max(1, toHit(state) - 1) : toHit(state)`; `faces = targetStrikeFaces(c, t, faces)`; abilityMods (Overhead Blow's needShift, name "overhead"); afraidMods (name "afraid"); `const mods = [...abilityMods, ...afraidMods]`; strikeMissed/struck carry `...(mods.length ? { mods } : {})`.

engine/foeAbilities.js: the debuff kind sets `c.foeEffect = { kind: a.effect, rounds: rng.d(4) }` and pushes `{ type: "foeDebuffed", name, ability, kind, rounds }`; the fade is pushed from combat.js's foeTurn tail as foeEffectFaded { kind }.

Lines to reword: eventNarration.js foeDebuffed ("The room keeps moving after you stop. Dazed for N rounds." / "Your arms feel like someone else's. Weakened for N rounds.") and foeEffectFaded; narrationLines.js LINE_FOR.foeDebuffed and foeEffectFaded. rollRange.js: toHitText(delta) fills "{signed} to hit"; signedText uses U+2212.

Measurement: parity compares state (test/parity/harness/comparables.js), the state pins hash state (test/unit/harness/rollHighBaseline.js#pinRun/stateHash), and test/unit/roll-high-guard.test.js inventories draws; none should move. Tests that deep-equal a strike event or a foeDebuffed event (feedback-payload, foe-abilities, rollDirection and others) may need the new fields re-pinned, each with a CMBUI-13 comment. FIXTURE-INVENTORY.md appends sections in phase order after the 75.x and 76 sections; model the measured-zero subsection on 75-13's.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Strike mods carry the live condition terms</name>
  <files>engine/derived.js, engine/combat.js, test/unit/condition-roll-mods.test.js, test/unit/feedback-payload.test.js</files>
  <read_first>
    - engine/derived.js (toHit, classNeed, weaponNeedMod, darkLimited, foeToHitBreakdown and its JSDoc)
    - engine/combat.js (playerStrike's faces, abilityMods, afraidMods, the strikeMissed and struck pushes)
    - test/unit/feedback-payload.test.js (the foeToHitBreakdown matrix proof)
    - test/unit/roll-high-guard.test.js (the draw inventory)
  </read_first>
  <behavior>
    - The breakdown's `need` equals toHit(state) over a matrix of class, race, weapon, inspired, dazed, darkness (lit, dark with and without a waiver, senses), heroBlind and floor-clamp states.
    - A dazed level-1 Human Fighter's strike event carries mods containing `{ name: "dazed", delta: -2 }` (floored cases carry the floored delta); inspired carries `{ name: "inspired", delta: 1 }`; a dark-capped strike carries `{ name: "dark", delta: <cap delta> }`; hero Blind carries `{ name: "blind", ... }` unless 75.1-04 already itemised it (then unchanged).
    - Condition entries come first, then overhead, then afraid; a strike with no live condition has exactly today's mods (absent when empty).
    - The roll, the faces, the outcome and the draw count of every strike are identical to the plan base (a counting rng over the same scenarios).
  </behavior>
  <action>
1. Tests first: test/unit/condition-roll-mods.test.js for every behaviour; the matrix beside feedback-payload's foeToHitBreakdown proof. Watch them fail.
2. engine/derived.js: export DAZED_TO_HIT_PENALTY (2) and use it in toHit's dazed step (byte-identical arithmetic). Add `export function toHitBreakdown(state)` returning `{ need, mods }` that mirrors toHit's steps in order and records `{ name, delta }` for the condition steps (inspired, dazed, dark, blind) that changed the value; JSDoc: CMBUI-13 Dazed honesty, the foeToHitBreakdown precedent, "need MUST equal toHit(state)". toHit's own body stays the source of truth (do not make it delegate).
3. engine/combat.js#playerStrike: prepend the breakdown's condition entries to `mods` (`[...conditionMods, ...abilityMods, ...afraidMods]`), computed once per attack from the same state; change nothing else. Comment the change as a payload-only CMBUI-13 addition.
4. Re-pin any test that deep-equals a strike event's mods for a state with one of these conditions live, each with a CMBUI-13 comment.
  </action>
  <verify>
    <automated>node --test test/unit/condition-roll-mods.test.js test/unit/feedback-payload.test.js test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js "test/parity/**/*.test.js" test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const DAZED_TO_HIT_PENALTY" engine/derived.js` equals 1, `grep -c "export function toHitBreakdown" engine/derived.js` equals 1 and `grep -c "toHitBreakdown(state" engine/combat.js` is at least 1.
    - `node --test test/unit/condition-roll-mods.test.js` passes, including the breakdown matrix and the unchanged-draws check.
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt test/unit/roll-high-state-pins.test.js` exits 0 and the parity suite and state pins pass.
  </acceptance_criteria>
  <done>A dazed strike's line lists "dazed −2" beside the roll, and nothing about the roll itself changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The onset and fade lines say what the effect does</name>
  <files>engine/foeAbilities.js, src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/condition-roll-mods.test.js, test/unit/foe-abilities.test.js, test/unit/narrationLinesTable.test.js, test/unit/roll-sign-consistency.test.js</files>
  <read_first>
    - engine/foeAbilities.js (the debuff branch and its foeDebuffed push)
    - src/browser/eventNarration.js (foeDebuffed, foeEffectFaded, the strike lines' modsClause use) and src/browser/narrationLines.js (LINE_FOR.foeDebuffed, foeEffectFaded)
    - src/browser/rollRange.js (toHitText, signedText, modsClause, ROLLERS)
    - test/unit/roll-sign-consistency.test.js (how a scenario drives the real engine and harvests tokens)
  </read_first>
  <behavior>
    - A real Djinni daze (drive foeAbilities through foeTurn with a forced ability turn, or call its resolver as foe-abilities.test.js does) pushes foeDebuffed with `toHit: -DAZED_TO_HIT_PENALTY` for kind dazed and no such field for weakened.
    - The Oracle onset line for a daze says you are "−2 to hit" (U+2212, via toHitText from the payload) for N rounds, in voice; for a weakening it says your blows do half damage for N rounds; the fold lines say the same briefly; a bare `{ type }` call still returns a non-empty line.
    - The fade lines say the effect has ended in the same terms (no longer −2 to hit; your blows land full again).
    - The sign guard's new dazed scenario: a real dazed playerStrike's Oracle line and fight-log reveal both contain "dazed −2" and never "dazed +2".
  </behavior>
  <action>
1. Tests first (condition-roll-mods.test.js, foe-abilities.test.js payload pin, narrationLinesTable.test.js, roll-sign-consistency.test.js's new scenario). Watch them fail.
2. engine/foeAbilities.js: for a dazed debuff, add `toHit: -DAZED_TO_HIT_PENALTY` to the foeDebuffed payload (import the constant); payload only, no draw or state change.
3. eventNarration.js and narrationLines.js: reword foeDebuffed and foeEffectFaded per CONTEXT ("The onset line names the effect"), keeping the deadpan voice (the todo's example: "The room keeps moving after you stop. −2 to hit for 3 rounds."), the number from `toHitText(e.toHit)` when present; weakened names half damage in words.
4. Run the voice scans and `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/condition-roll-mods.test.js test/unit/foe-abilities.test.js test/unit/narrationLinesTable.test.js test/unit/roll-sign-consistency.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "DAZED_TO_HIT_PENALTY" engine/foeAbilities.js` is at least 1.
    - `node --test test/unit/condition-roll-mods.test.js test/unit/roll-sign-consistency.test.js` passes, including the onset lines and the dazed sign scenario.
    - `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/narrationLinesTable.test.js` passes.
  </acceptance_criteria>
  <done>Getting dazed tells you "−2 to hit for N rounds", getting weakened tells you your blows do half damage, and both say when it ends.</done>
</task>

<task type="auto">
  <name>Task 3: Measure, declare the zero, and audit every condition</name>
  <files>test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the 75-13 measured-zero subsections and the end of the file)
    - .planning/phases/77-combat-screen-oracle-readability/77-03-SUMMARY.md (the chip list the audit walks)
  </read_first>
  <action>
1. Run `node --test "test/parity/**/*.test.js"`, `node --test test/unit/roll-high-state-pins.test.js test/unit/roll-high-guard.test.js` and `node tools/fixture-inventory.mjs` with `node --test test/parity/fixture-inventory.test.js`; confirm `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
2. Append `## Phase 77: combat screen & Oracle readability (CMBUI-07..14) — measured per plan` to test/parity/FIXTURE-INVENTORY.md with `### Plan 07 — condition terms in strike mods and the foeDebuffed payload (CMBUI-13): measured zero`: the change (payload only), the predictor (no state, no draw changed), the live results (parity pass count, the fixtures/master diff, the master's `git hash-object`, the pins unchanged), and "Phase 77's other plans are presentation-only: no engine change".
3. Write the audit table in the SUMMARY: every hero condition chip from 77-03's list, whether it moves a roll (which roll), and whether that roll's mods names it now; name any remaining gap with its reason.
4. Run `npm test`.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" test/parity/fixture-inventory.test.js test/unit/roll-high-state-pins.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 77" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
    - The SUMMARY contains the condition audit table.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The engine touch is proven to change only what the player is told, and every condition's roll honesty is audited.</done>
</task>

</tasks>

<verification>
- Strike mods name every live condition that changed the strike; the onset and fade lines say what dazed and weakened do.
- Parity, the state pins and the draw inventory are unchanged; the zero is declared; `npm test` is green.
  <human-check>
  Pixel 7, at milestone close:
  - Get dazed by a Djinni: the Oracle and the fight log say "−2 to hit" and the rounds; a missed swing's revealed dice list "dazed −2".
  - Get weakened by Krupke: the line says your blows do half damage, and when it wears off it says so.
  </human-check>
</verification>

<success_criteria>
CONTEXT's Dazed honesty decision holds end to end with 77-03 and 77-08: the onset names the effect, every affected roll carries the modifier, and the audit covers every condition.
</success_criteria>

<output>
Create `.planning/phases/77-combat-screen-oracle-readability/77-07-SUMMARY.md` when done. Include the plan base SHA, the breakdown's entry names, the re-pinned tests, the measured-zero numbers, the condition audit table, and the new onset/fade lines verbatim for the Phase 79 narrative pass.
</output>
