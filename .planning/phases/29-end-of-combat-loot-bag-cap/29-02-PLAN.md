---
phase: 29-end-of-combat-loot-bag-cap
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - engine/state.js
  - engine/saveState.js
  - engine/items.js
  - engine/actions.js
  - engine/engine.js
  - engine/combat.js
  - engine/death.js
  - test/parity/harness/comparables.js
  - src/browser/toasts.js
  - src/browser/eventNarration.js
  - test/unit/loot-pile.test.js
  - test/unit/loot-narration.test.js
  - test/unit/save-validation.test.js
  - test/roundtrip/serialize-rehydrate.test.js
autonomous: true
requirements: [LOOT-01, LOOT-02, LOOT-05, LOOT-06]

must_haves:
  truths:
    - "LOOT-01: engine/combat.js no longer references takeItem; a killFoe treasure hit pushes the rolled item onto state.pendingLoot and emits lootDropped {name, kind} — c.items and the equipped weapon/armor fields are untouched mid-fight (test/unit/loot-pile.test.js)"
    - "LOOT-02 empty/single: an empty pile produces no loot events and takeAllLoot/leaveAllLoot on [] emit nothing; a single-item pile is handled by takeAllLoot/leaveAllLoot exactly like a multi-item one (test/unit/loot-pile.test.js)"
    - "LOOT-02 ordering: pendingLoot order is kill order; takeAllLoot and leaveAllLoot iterate in that order and serialize->rehydrate preserves it deepStrictEqual (test/unit/loot-pile.test.js, test/roundtrip/serialize-rehydrate.test.js)"
    - "LOOT-02 adjacency: two identical drops are two separate pile entries; leaveLoot {i} removes only index i (test/unit/loot-pile.test.js)"
    - "LOOT-04 ordering (take-all): takeAllLoot takes every item that fits in pile order, leaves the refused ones in place in their original order, and emits exactly ONE bagFull for the first refusal (test/unit/loot-pile.test.js)"
    - "LOOT-05: killFoe draws exactly one extra d20 only when bagUpgradeTier(state) is non-null — depth 1 small bag: N draws; depth 2 small bag: N+1 and a roll <= BAG_DROP_UNDER puts a kind:\"bag\" item in the pile instead of the rolled item; depth 2 exlarge bag: N; depth 2 with a bag already in the pile: N (test/unit/loot-pile.test.js)"
    - "LOOT-06 idempotency: serializeRun -> validateSave -> rehydrate -> serializeRun is deepStrictEqual on a state with a 3-item pile; a pre-v1.3 save with no pendingLoot loads as []; forfeitLoot on an empty pile emits nothing and adds no key (test/unit/loot-pile.test.js, test/unit/save-validation.test.js)"
    - "LOOT-06 forfeit: every flee success exit and die() (any cause) with a non-empty pile emits exactly one lootForfeited {items, reason} BEFORE fled/died and empties the pile (test/unit/loot-pile.test.js)"
    - statement: "LOOT-06 concurrency: the engine is synchronous and every applyAction structuredClones its input, so a pending pile can never be mutated by two actions at once — no interleaving exists to test"
      verification: backstop
    - "Every new event type (lootDropped, lootTaken, lootLeft, lootForfeited) has an EVENT_NARRATION entry and a TOAST_FOR entry; formatEventsCoverage, toastsCoverage and the voice safety scan are green"
    - "reconcilePendingLoot is applied in movementComparable, combatComparable and economyComparable; a state with a jewel in pendingLoot compares equal to the same state with the jewel giveItem'd (test/unit/loot-pile.test.js); every parity suite passes with zero fixture edits"
  artifacts:
    - "engine/state.js: pendingLoot: [] on every fresh run"
    - "engine/saveState.js: sanitizeLoot; validateSave/rehydrate carry pendingLoot (never reset)"
    - "engine/items.js: offerLoot, takeLoot, leaveLoot, takeAllLoot, leaveAllLoot"
    - "engine/actions.js + engine/engine.js: takeLoot/leaveLoot/takeAllLoot/leaveAllLoot validated and dispatched"
    - "engine/combat.js: killFoe -> offerLoot with the guarded bag d20; flee's three success exits call forfeitLoot"
    - "engine/death.js: forfeitLoot; die() forfeits first"
    - "test/parity/harness/comparables.js: reconcilePendingLoot wired into all three comparables"
    - "src/browser/toasts.js + eventNarration.js: lootDropped/lootTaken/lootLeft/lootForfeited entries"
    - "test/unit/loot-pile.test.js, test/unit/loot-narration.test.js, roundtrip + save-validation additions"
  key_links:
    - "engine/combat.js#killFoe calls offerLoot (items.js) instead of takeItem — the ONLY drop site"
    - "engine/death.js#die calls forfeitLoot unconditionally (13+ die() call sites covered at once)"
    - "engine/items.js#takeLoot/takeAllLoot route every stow through stowItem (Plan 01's single gate)"
    - "test/parity/harness/comparables.js destructures pendingLoot out and reconciles it before reconcilePendingFind"
  prohibitions:
    - requirement_id: LOOT-01
      category: transparency
      statement: "The engine must never discard a drop without an event — every leave, forfeit and refused stow is narrated; no code path may set pendingLoot to [] or splice it without pushing lootLeft/lootTaken/lootForfeited/bagFull"
      status: unresolved
      verification: judgment
    - requirement_id: LOOT-05
      category: values
      statement: "The bag draw must never change the frozen prototype's rng consumption on any fixture-exposed path — it fires only behind bagUpgradeTier (depth >= 2 with an upgrade available) and sits AFTER every existing killFoe draw; test/parity/prototype-master.js.txt and every fixture JSON stay byte-identical"
      status: unresolved
      verification: judgment
---

<objective>
Turn the mid-fight auto-take into a pending pile: `state.pendingLoot` (serialized, carved out of all three comparables), four pure player actions, the forfeit rule on flight/death, and LOOT-05's one guarded bag draw — with narration + toasts for every new event so nothing is silent.

Purpose: LOOT-01/02/06 are the phase's core promise — no drop is auto-equipped, auto-rejected, or lost without a line. The pile must survive save/resume byte-identically and be forfeited honestly. LOOT-05's draw is the ONLY new rng in the phase and must be provably invisible to every parity fixture.

Output: pile model + serialization, `offerLoot`/`takeLoot`/`leaveLoot`/`takeAllLoot`/`leaveAllLoot`, `forfeitLoot`, `killFoe` redirect + guarded bag draw, `reconcilePendingLoot`, four new event types with presentation entries, tests, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-CONTEXT.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-01-SUMMARY.md

Engine gate (non-negotiable): pure/deterministic; parity byte-identical for solo play; `test/parity/prototype-master.js.txt` NEVER edited; no fixture JSON regenerated (RESEARCH measured zero action-path divergences: only combat/lose seed 14 rolls a drop — a jewel, and that scenario already carries a Phase 24 action-path record, so its per-action byte diff is skipped from action 1); the LOOT-05 d20 is the ONLY new draw, placed AFTER every existing killFoe draw and fired only when `bagUpgradeTier(state)` is non-null (never on a depth-1 fixture); `pendingLoot` carved out of ALL THREE `*Comparable()` fns via `reconcilePendingLoot`; every new event type gets `EVENT_NARRATION` + `TOAST_FOR` entries (coverage guards); voice safety scan green.

Plan 01 landed (read 29-01-SUMMARY.md): `slotItems`, `bagCap`, `canStow`, `stowItem(state, it, events, quiet) -> boolean` (handles kind bag as an upgrade, pushes bagFull {item, have, slots} on refusal), `bagUpgradeTier(state)`, `bagItemFor(tier)`, `BAG_DROP_UNDER`, `bagUpgraded` event + entries.

Decisions honoured from CONTEXT.md: "Pending pile & loot screen" (pendingLoot sibling of pendingFind; killFoe pushes instead of takeItem with the gate/rolls unchanged; serialized and tolerant; reconcilePendingLoot mirror; actions takeLoot {i, equip?}/leaveLoot {i}/takeAllLoot/leaveAllLoot; every action emits an event — lootTaken, lootLeft, and the existing itemEquipped for equip-now; forfeit on every fled reason and on death with ONE lootForfeited {items, reason}); "Compare-to-equipped & take semantics" (Equip now = direct swap, displaced piece through the single stow gate, no slot needed when nothing is displaced; illegal gear still stowable); "Bigger bags as treasure" (one extra d20 on a successful drop, after the existing draws, guarded by depth >= 2 AND an upgrade available; the bag replaces the rolled item on a low roll; taking it upgrades c.bag immediately via bagUpgraded); Claude's discretion locked here: Take all takes what fits in order and surfaces ONE bagFull for the remainder — never loses an item (RESEARCH A1).

Comment-text discipline: Task 2 negative-greps the legacy auto-take call name (see its acceptance criteria) in engine/combat.js. Do not leave that call name in a comment in combat.js — describe the old behaviour as "the legacy auto-take" instead.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pendingLoot model + serialization + the four pure handlers</name>
  <files>engine/state.js, engine/saveState.js, engine/items.js, test/unit/loot-pile.test.js, test/unit/save-validation.test.js, test/roundtrip/serialize-rehydrate.test.js</files>
  <read_first>
    - engine/state.js lines 150-200 (newRun's state literal — pendingFind's placement and comment style)
    - engine/saveState.js lines 60-104 (sanitizeParty/migrateCarry precedents), 145-215 (validateSave's explicit value object), 217-263 (rehydrate — pendingFind is RESET there; pendingLoot must NOT be)
    - engine/items.js: stowItem/canStow/slotItems/bagCap (Plan 01), takeFind/leaveFind (the handler shape to mirror), equipItem lines 420-480 (the weapon/armor swap fields to mirror for equip-now), wornWeaponItem/wornArmorItem (displaced-piece reconstruction; null when bare-handed/Nothing/destroyed)
    - test/unit/inventory-actions.test.js (fixedChar/fixedState/WEAPON/ARMOR helpers to copy; pendingLoot must be added to the new file's fixedState)
    - test/unit/carry-model.test.js lines 70-100 (the pendingFind newRun/rehydrate pins — pendingLoot gets the OPPOSITE rehydrate assertion)
    - test/unit/save-validation.test.js lines 104-137 (the old-shape save test to extend)
    - test/roundtrip/serialize-rehydrate.test.js lines 80-115 (the fresh-run deepStrictEqual round-trip: newRun must gain pendingLoot: [] and rehydrate must reproduce it exactly)
    - test/determinism/forced-chargen.test.js lines 120-136 (asserts every forced run has the same key set as newRun(1) — a uniform pendingLoot: [] keeps it green)
    - .planning/phases/28-armor-integrity-durability/28-01-SUMMARY.md (left/patches on a swapped-out piece; destroyed armor leaves no bag copy)
  </read_first>
  <behavior>
    - newRun(1).pendingLoot deepStrictEqual [] and "pendingLoot" in newRun(1)
    - serializeRun(state) -> validateSave -> rehydrate keeps a 3-item pile deepStrictEqual and in order; rehydrate(serializeRun(rehydrate(serializeRun(s)))) is deepStrictEqual to rehydrate(serializeRun(s)) (idempotent)
    - validateSave of a save with NO pendingLoot key -> value.pendingLoot deepStrictEqual []; rehydrate of that value -> []; a tampered pendingLoot of "x" / 42 / {} -> []; an array with [null, 1, {kind:"jewel", n:"A"}] -> [{kind:"jewel", n:"A"}]
    - the existing pendingFind-is-reset assertions in test/unit/carry-model.test.js and save-validation.test.js still pass
    - offerLoot(state, it, events): appends it to state.pendingLoot (creating the array if the hand-built state lacks it), pushes {type:"lootDropped", name: it.n, kind: it.kind}, does not touch c.items
    - takeLoot(state, 0): stows pile[0] via the gate, splices it out, pushes lootTaken {item}; at cap: pushes bagFull {item, have, slots}, pile unchanged, no lootTaken
    - takeLoot(state, 0, true) on a legal weapon with a worn Broadsword and a free slot: c.weapon/prof/magicWpn = it.base/0/it.bonus, the displaced Broadsword item (from wornWeaponItem) is in c.items, pile shrinks, events contain itemEquipped {item, slot:"weapon"} and NOT lootTaken
    - takeLoot(state, 0, true) on a weapon while c.weapon is "Fists" and the bag is FULL: equips, c.items unchanged (no slot needed), itemEquipped emitted
    - takeLoot(state, 0, true) on a weapon with a worn weapon and a FULL bag: bagFull {item: <the worn piece>}, nothing changes (c.weapon, c.items, pile all deepStrictEqual before)
    - takeLoot(state, 0, true) on a class-illegal weapon (Magic User + Broadsword): equipRejected {reason:"wrongClass"}, pile unchanged
    - takeLoot(state, 0, true) on armor {ar 15, wp 45, min 2}: c.armor/ar/armorMin/armorMax/armorWP/patches = armor/15/2/45/45/0 and the displaced worn piece carries left/patches (Phase 28); with a DESTROYED worn piece (armorWP 0) no bag copy is made and no slot is needed
    - takeLoot(state, 0, true) on a jewel: equipRejected {reason:"notEquippable"}, pile unchanged (equip-now is only for weapon/armor)
    - takeLoot(state, 5) on a 1-item pile: no events, no change
    - leaveLoot(state, 1) on [A, B, B2]: pile [A, B2], events [{type:"lootLeft", item: B}]; leaveLoot(state, 9): no-op
    - takeAllLoot on [gearA, gearB, potion, gearC] with ONE free slot: gearA taken, gearB refused, potion taken (exempt), gearC refused; pile === [gearB, gearC]; events = [lootTaken A, bagFull(B, have, slots), lootTaken potion] — exactly one bagFull; nothing lost
    - takeAllLoot on [mediumBag, gearA] with a full small bag: bagUpgraded first, then gearA fits (6 slots) — pile [], two lootTaken
    - takeAllLoot on []: no events; leaveAllLoot on [A, B]: pile [], events [lootLeft A, lootLeft B] in order; leaveAllLoot on []: no events
    - none of the five handlers draws rng (they take no rng argument) and none touches c.items except through stowItem
  </behavior>
  <action>
    1. `engine/state.js#newRun`: add `pendingLoot: [],` directly after `pendingFind: null,` with a comment in the style of the pendingFind block: Phase 29 (LOOT-01/06) — the end-of-combat drop pile, a top-level sibling of pendingFind; plain assignment (no rng); UNLIKE pendingFind it IS serialized and survives load; the parity harness destructures it out and reconciles it (Task 3).
    2. `engine/saveState.js`: add `function sanitizeLoot(raw)` returning `Array.isArray(raw) ? raw.filter((it) => it && typeof it === "object" && !Array.isArray(it)) : []` (doc: fail-open like sanitizeParty — a pre-v1.3 save has no field -> []; malformed entries dropped, never a rejected save; no STATE_VERSION bump — purely additive). In `validateSave`'s value object add `pendingLoot: sanitizeLoot(obj.pendingLoot),` after `party`. In `rehydrate` add `pendingLoot: sanitizeLoot(obj.pendingLoot),` after the `pendingFind: null,` line with a comment contrasting it: pendingLoot is PERSISTENT run state (the player must still get their loot screen on resume — LOOT-06) so it is carried through, never reset.
    3. `engine/items.js` — add, after unequipSlot, five exported pure handlers (no rng):
       - `offerLoot(state, it, events = [])`: `state.pendingLoot = state.pendingLoot || []; state.pendingLoot.push(it); events.push({ type: "lootDropped", name: it.n, kind: it.kind }); return events;` Doc: the ONE producer (killFoe, Task 2) — replaces the legacy auto-take.
       - `takeLoot(state, i, equip = false, events = [])`: `const pile = state.pendingLoot || []; const it = pile[i]; if (!it) return events;` Then:
         - if `!equip`: `if (!stowItem(state, it, events, true)) return events; pile.splice(i, 1); events.push({ type: "lootTaken", item: it }); return events;`
         - if `equip`: only weapon/armor — otherwise push `{ type: "equipRejected", item: it, reason: "notEquippable" }` and return. Weapon: `const reason = weaponRefusalReason(c, it); if (reason) { push equipRejected {item, reason}; return; } const worn = wornWeaponItem(c); if (worn && !stowItem(state, worn, events, true)) return events;` then set `c.weapon = it.base; c.prof = 0; c.magicWpn = it.bonus || 0;` splice from pile, push `{ type: "itemEquipped", item: it, slot: "weapon" }`. Armor: same shape with armorRefusalReason / wornArmorItem (null for bare/destroyed -> no slot needed), then `c.armor = it.armor; c.ar = it.ar; c.armorMin = it.min; c.armorMax = it.wp; c.armorWP = it.left ?? it.wp; c.patches = it.patches ?? 0;` splice, push itemEquipped slot "armor". Doc: CONTEXT §Compare-to-equipped — a direct swap; the displaced piece goes through the single stow gate so a slot is needed only when something is displaced; equip-now emits the existing itemEquipped (not lootTaken).
       - `leaveLoot(state, i, events = [])`: splice index i if present; push `{ type: "lootLeft", item: it }`.
       - `takeAllLoot(state, events = [])`: iterate a snapshot of the pile in order; for each item run `stowItem` against a scratch events array; on true, append the scratch events + `{ type: "lootTaken", item }`; on false keep the item in a `remaining` array and append the scratch (its bagFull) ONLY for the first refusal; finally `state.pendingLoot = remaining`. Doc: takes what fits in pile order (potions/bags still processed behind a blocked gear item), ONE bagFull, never loses an item (CONTEXT Claude's-discretion default, locked here).
       - `leaveAllLoot(state, events = [])`: for each item in order push `{ type: "lootLeft", item }`; then `state.pendingLoot = []`.
       Extend the inventory-actions header comment (the "All FIVE are PURE" paragraph) to mention the loot handlers.
    4. Create `test/unit/loot-pile.test.js` (header: Phase 29 LOOT-01/02/05/06 — the pending pile) with fixedChar/fixedState copied from inventory-actions.test.js PLUS `pendingLoot: []` in fixedState and helpers `WEAPON(base,bonus)`, `ARMOR(name, ar, cls)`, `JEWEL(n)`, `POTION(n)` (kind potion), `BAG(tier)` (spread of BAG_ITEMS[tier]). Write the Task 1 behaviours (RED first), importing the five handlers + stowItem from engine/items.js, newRun from engine/state.js, serializeRun/validateSave/rehydrate from engine/saveState.js.
    5. `test/unit/save-validation.test.js`: in the old-shape save test add `assert.deepStrictEqual(state.pendingLoot, [])` beside the pendingFind assertion, and a new test "Phase 29 (LOOT-06): a non-empty pendingLoot survives serializeRun -> validateSave -> rehydrate in order, and a tampered value degrades to []". `test/roundtrip/serialize-rehydrate.test.js`: add one test "Phase 29 (LOOT-06): a state carrying a 3-item pendingLoot pile round-trips losslessly and idempotently" (JSON round-trip deepStrictEqual + the double serialize/rehydrate equality).
  </action>
  <verify>
    <automated>node --test test/unit/loot-pile.test.js test/unit/save-validation.test.js test/roundtrip/serialize-rehydrate.test.js test/unit/carry-model.test.js test/determinism/forced-chargen.test.js test/unit/inventory-actions.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/loot-pile.test.js test/unit/save-validation.test.js test/roundtrip/serialize-rehydrate.test.js test/unit/carry-model.test.js test/determinism/forced-chargen.test.js test/unit/inventory-actions.test.js` exits 0
    - `grep -c "pendingLoot: \[\]," engine/state.js` == 1
    - `grep -c "function sanitizeLoot" engine/saveState.js` == 1; `grep -c "pendingLoot: sanitizeLoot(obj.pendingLoot)" engine/saveState.js` == 2 (validateSave + rehydrate)
    - `grep -c "export function offerLoot" engine/items.js` == 1; `grep -c "export function takeLoot" engine/items.js` == 1; `grep -c "export function leaveLoot" engine/items.js` == 1; `grep -c "export function takeAllLoot" engine/items.js` == 1; `grep -c "export function leaveAllLoot" engine/items.js` == 1
    - `grep -c "type: \"lootDropped\"" engine/items.js` == 1; `grep -c "type: \"lootTaken\"" engine/items.js` == 2 (takeLoot + takeAllLoot); `grep -c "type: \"lootLeft\"" engine/items.js` == 2 (leaveLoot + leaveAllLoot)
    - `grep -c "^s*giveItem(state, it," engine/items.js` == 2 (unchanged from Plan 01 — the loot handlers stow only through stowItem); `grep -c "if (!stowItem(state, it, events, true)) return events;" engine/items.js` == 2 (takeFind + takeLoot); `grep -c "if (worn && !stowItem(state, worn, events, true)) return events;" engine/items.js` == 2 (equip-now weapon + armor)
  </acceptance_criteria>
  <done>The pile exists on every fresh run, survives save/resume, and the four player actions (+ offerLoot) are pure, gated through stowItem, and pinned by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dispatch/validation, killFoe redirect + guarded bag draw, forfeit on flee/die</name>
  <files>engine/actions.js, engine/engine.js, engine/combat.js, engine/death.js, test/unit/loot-pile.test.js</files>
  <read_first>
    - engine/actions.js lines 10-45 (ACTION_TYPES) and 58-125 (validateAction per-type checks — the dropItem/equipItem `i` contract and the resolveJoiner boolean contract to mirror)
    - engine/engine.js lines 15-25 (imports) and 100-136 (the takeFind/leaveFind/dropItem cases to mirror)
    - engine/combat.js line 57 (the items.js import), lines 552-610 (killFoe — the treasure line at ~595 and the draw order comment), lines 684-745 (flee — three success exits: cloaker, tracked, escaped; each guarded by pursuitStrike then pushes `fled` then endCombat)
    - engine/death.js lines 83-113 (die — state.combat = null is unconditional; forfeit goes right after it)
    - engine/items.js: offerLoot (Task 1), bagUpgradeTier, bagItemFor (Plan 01); content/bags.js BAG_DROP_UNDER
    - test/unit/combat.test.js lines 40-110 (fakeRng/fixedState/fixedFoe/fixedCombat helpers) and 438-455 (the existing killFoe draw-order test: sp d6, coin d10, treasure d20 — no cooking for Humans)
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md "Parity Risk Enumeration" + "Pitfall 3" + "Open Questions" #3 (draw placement: right after the treasure roll, before cooking)
  </read_first>
  <behavior>
    - validateAction({type:"takeLoot", i:0}).ok true; {i:-1} / {i:"0"} / {} -> ok false; {type:"takeLoot", i:0, equip:true} ok; {equip:"yes"} -> ok false; {type:"leaveLoot", i:2} ok; {type:"leaveLoot"} -> false; {type:"takeAllLoot"} and {type:"leaveAllLoot"} ok with no fields
    - applyAction(newRun-derived state with pendingLoot seeded, {type:"takeLoot", i:0}) returns a NEW state whose pendingLoot shrank while the input state is unchanged; {type:"takeLoot", i:0, equip:true} on a legal weapon returns itemEquipped; leaveLoot/takeAllLoot/leaveAllLoot dispatch and emit their events
    - killFoe at floor depth 1, Humans lvl 1, c.bag "small", pendingLoot []: with a counting rng scripted [1 (sp d6), 1 (coin d10), 1 (treasure gate d20 <= 3), 5 (picks d12 != 1), 6 (kind d10 -> jewel), 1 (jewel d8)] draws EXACTLY 6 values; afterwards pendingLoot is [JEWELRY[0]-shaped jewel], events contain lootDropped {name, kind:"jewel"} and foeKilled, and contain none of itemTaken / itemRejected / itemGiven; c.items deepStrictEqual [] (unchanged); c.sp === 5 (no level-up draw)
    - same setup at floor depth 2: draws EXACTLY 7 values; with the 7th value 1 (<= BAG_DROP_UNDER) pendingLoot is [{kind:"bag", tier:"medium", ...}] (the jewel is NOT in the pile); with the 7th value 20 pendingLoot is [the jewel]
    - depth 2 with c.bag "exlarge": 6 draws; depth 2 with pendingLoot already holding a bag item: 6 draws; depth 4 with c.bag "medium": 6 draws (large needs floor 5); depth 5 with c.bag "medium": 7 draws
    - a Beasts foe at depth 1 still draws its cooking d6 AFTER the treasure block (7 values: the 6 above + cooking) — the existing draw order is untouched
    - the Plan-01 property still holds: killFoe never calls the legacy auto-take (source assertion: the legacy auto-take call name no longer appears in engine/combat.js — see the acceptance grep)
    - flee: a Cloaker (c.sub "Cloaker", combat.opened2 falsy) with pendingLoot [A, B]: events = [lootForfeited {items:[A,B], reason:"fled"}, fled {reason:"cloaker"}, combatEnded] in that order; pendingLoot []; with an empty pile no lootForfeited is emitted
    - flee: a tracked round-1 Fighter (combat.tracked true, round 1) with a pile forfeits before fled {reason:"tracked"}; an ordinary escape (fleeRolled d20 = 20) forfeits before fled {reason:"escaped"}; a FAILED flee roll (d20 = 1, then a foeTurn) does NOT forfeit
    - die(state, "starve", null, rng, events) with a pile: events contain lootForfeited {items, reason:"died"} BEFORE died; pendingLoot []; die on a state with an EMPTY pile emits no lootForfeited; die on a hand-built state WITHOUT a pendingLoot key adds no such key (deepStrictEqual on Object.keys before/after minus the fields die already sets)
    - forfeitLoot(state, "fled", events) is exported from engine/death.js and returns events
  </behavior>
  <action>
    1. `engine/actions.js`: add "takeLoot", "leaveLoot", "takeAllLoot", "leaveAllLoot" to ACTION_TYPES with a Phase 29 comment. In validateAction: `case "takeLoot": case "leaveLoot":` require `isInt(action.i) && action.i >= 0` (reason `${action.type}.i must be a non-negative integer`); for takeLoot additionally `if (action.equip !== undefined && typeof action.equip !== "boolean") return { ok: false, reason: "takeLoot.equip must be a boolean when present" }`; `case "takeAllLoot": case "leaveAllLoot":` no payload (comment like takeFind/leaveFind).
    2. `engine/engine.js`: extend the items.js import with `takeLoot, leaveLoot, takeAllLoot, leaveAllLoot`; add cases `takeLoot -> takeLoot(next, action.i, !!action.equip, events)`, `leaveLoot -> leaveLoot(next, action.i, events)`, `takeAllLoot -> takeAllLoot(next, events)`, `leaveAllLoot -> leaveAllLoot(next, events)`, each with a one-line Phase 29 comment (pure, no rng).
    3. `engine/death.js`: add `export function forfeitLoot(state, reason, events = [])`: `const pile = state.pendingLoot; if (!Array.isArray(pile) || !pile.length) return events; state.pendingLoot = []; events.push({ type: "lootForfeited", items: pile, reason }); return events;` (doc: ONE event, the whole pile; never adds a key to a state that lacks one — the fresh-run key-set tests). In `die()`, call `forfeitLoot(state, "died", events);` immediately after `state.combat = null;` (RESEARCH Pitfall 3: inside the one terminator, never at the 13+ call sites).
    4. `engine/combat.js`: change the items.js import to `{ offerLoot, bagUpgradeTier, bagItemFor, gainWilmst, rollTreasureItem, LOOT_DIVISOR }` (drop the legacy auto-take import entirely), extend the death.js import to `{ die, forfeitLoot }`, and add `BAG_DROP_UNDER` to the content import. In killFoe replace the treasure line with a block: `if (rng.d(20) <= 2 + f.lvl) { let drop = rollTreasureItem(rng, state.floor.depth, c); const tier = bagUpgradeTier(state); if (tier && rng.d(20) <= BAG_DROP_UNDER) drop = bagItemFor(tier); offerLoot(state, drop, events); }` — with a DETERMINISM GATE comment: the gate d20 and every rollTreasureItem draw are unchanged and stay first; the bag d20 is the ONLY new draw in Phase 29, sits after them and before the cooking check, and fires only when bagUpgradeTier is non-null (never on a depth-1 fixture — RESEARCH "LOOT-05 guard safety"); the destination changes from the legacy auto-take to the pile (LOOT-01). Update killFoe's doc comment ("treasure via rollTreasureItem/…") accordingly without naming the old call.
    5. `engine/combat.js#flee`: at each of the three success exits insert `forfeitLoot(state, "fled", events);` between the `pursuitStrike(...).died` return and the `events.push({ type: "fled", ... })` line (cloaker, tracked, escaped). Not on the fleeFailed path and not on the post-foeTurn encounterCleared path (the pile shows normally there). Doc: CONTEXT §Pending pile — fleeing (every fled reason) forfeits with ONE narrated lootForfeited.
    6. Extend `test/unit/loot-pile.test.js` with the Task 2 behaviours (RED first). Add a `countingRng(seq)` helper (like combat.test.js's fakeRng but recording `calls`; `pick` returns arr[0]; `shuffle` identity) and a `fixedFoe`/`fixedCombat` pair copied from combat.test.js. For the flee tests build `state.combat` with `fixedCombat([liveFoe])` plus `opened2`/`tracked`/`round` as needed and a Cloaker/Fighter character; assert event ORDER with `events.map(e => e.type)`. For dispatch tests start from `newRun(7)` and set `state.pendingLoot = [WEAPON("Dagger", 1)]` before applyAction.
  </action>
  <verify>
    <automated>node --test test/unit/loot-pile.test.js test/unit/combat.test.js test/unit/death.test.js test/unit/permadeath.test.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/loot-pile.test.js test/unit/combat.test.js test/unit/death.test.js test/unit/permadeath.test.js` exits 0
    - `node --test test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js test/parity/economy-parity.test.js test/parity/full-suite.test.js` exits 0 and `git status --porcelain test/parity` prints nothing
    - `grep -v '^\s*//' engine/combat.js | grep -v '^\s*\*' | grep -c "takeItem("` == 0
    - `grep -c "offerLoot(state, drop, events)" engine/combat.js` == 1; `grep -c "rng.d(20) <= BAG_DROP_UNDER" engine/combat.js` == 1
    - `grep -c "forfeitLoot(state, \"fled\", events)" engine/combat.js` == 3
    - `grep -c "export function forfeitLoot" engine/death.js` == 1; `grep -c "forfeitLoot(state, \"died\", events)" engine/death.js` == 1; `grep -c "type: \"lootForfeited\"" engine/death.js` == 1
    - `grep -c "\"takeLoot\"" engine/actions.js` >= 1; `grep -c "\"leaveLoot\"" engine/actions.js` >= 1; `grep -c "\"takeAllLoot\"" engine/actions.js` >= 1; `grep -c "\"leaveAllLoot\"" engine/actions.js` >= 1
    - `grep -c "case \"takeLoot\":" engine/engine.js` == 1; `grep -c "case \"leaveLoot\":" engine/engine.js` == 1; `grep -c "case \"takeAllLoot\":" engine/engine.js` == 1; `grep -c "case \"leaveAllLoot\":" engine/engine.js` == 1
  </acceptance_criteria>
  <done>Drops land in the pile with an event; the one bag draw is guarded and draw-count-pinned; flight and death forfeit with one narrated event; the four actions dispatch through applyAction; every parity suite is green untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: reconcilePendingLoot carve-out, presentation entries for the four new events, full-suite gate</name>
  <files>test/parity/harness/comparables.js, src/browser/toasts.js, src/browser/eventNarration.js, test/unit/loot-narration.test.js, test/unit/loot-pile.test.js</files>
  <read_first>
    - test/parity/harness/comparables.js lines 24-52 (the takeItem import and reconcilePendingFind — the exact template), 185-222 (movementComparable's destructure + reconcile order), 264-284 (combatComparable), 636-660 (economyComparable)
    - test/parity/harness/diffState.js (diffState(a, b) returns null when equal — use it in the reconcile test, never bare deepStrictEqual across realms)
    - test/unit/parity-action-path-divergence.test.js (unit-test style for harness helpers)
    - src/browser/toasts.js lines 148-180 (ORACLE_ONLY — do NOT add the new types there), 1238-1255 (item toasts, `block()` helper, PRIORITY use), Plan 01's bagUpgraded entry
    - src/browser/eventNarration.js lines 596-625 (findOffered/findTaken/findLeft/bagFull/itemEquipped lines — the voice to match) and Plan 01's bagUpgraded entry
    - test/unit/formatEventsCoverage.test.js + test/unit/toastsCoverage.test.js (the guards this task must satisfy: every literal `type: "..."` in engine/*.js needs an Oracle line and a toast-or-ORACLE_ONLY entry; no dead entries)
    - test/voice/safety-scan.test.js header (builders are called with bare {type} and with injected vocab — defend `e.items` with `?? []`)
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md "Pattern 3" (reconcile template) and "Open Questions" #2 (per-item lootTaken/lootLeft)
  </read_first>
  <behavior>
    - reconcilePendingLoot precedes reconcilePendingFind in all three comparables; a state S1 with pendingLoot [jewel] and c.items [] and a state S2 with pendingLoot [] and the same jewel appended to c.items give diffState(combatComparable(S1), combatComparable(S2)) === null, likewise movementComparable and economyComparable
    - the same holds for a two-item pile [jewel, cloak] applied in order; a kind:"bag" pile entry is ignored by the reconcile (S1 with pendingLoot [bag] compares equal to S2 with an empty pile and unchanged c.items)
    - the comparable output has no pendingLoot key
    - TOAST_FOR.lootDropped({type:"lootDropped", name:"Warded leather"}).text === "Dropped: Warded leather. It will keep." (tone "beat"); bare {type} returns a non-empty text
    - TOAST_FOR.lootTaken({item:{n:"X"}}).text === "Taken: X." (tone "hit"); TOAST_FOR.lootLeft({item:{n:"X"}}).text === "Left behind: X." (tone "miss")
    - TOAST_FOR.lootForfeited({items:[{n:"A"},{n:"B"}], reason:"fled"}).text === "Left on the floor in your hurry: A, B." (tone "miss"); reason "died" -> "Left where they fell: A, B."; bare {type} -> non-empty text
    - EVENT_NARRATION.lootDropped / lootTaken / lootLeft / lootForfeited each return a non-empty string from a bare {type}; lootForfeited with reason "fled" contains "not to be on the floor yourself"; with reason "died" contains "stay where they fell"; both list every item name
    - none of the four new types is in ORACLE_ONLY
    - test/unit/formatEventsCoverage.test.js, test/unit/toastsCoverage.test.js and test/voice/safety-scan.test.js pass
  </behavior>
  <action>
    1. `test/parity/harness/comparables.js`: add `function reconcilePendingLoot(rest, pendingLoot)` directly below reconcilePendingFind: `if (!Array.isArray(pendingLoot) || !pendingLoot.length || !rest.c) return rest; const proxy = { c: structuredClone(rest.c) }; for (const it of pendingLoot) { if (it && it.kind !== "bag") <apply the already-imported legacy auto-take helper to (proxy, structuredClone(it), [])>; } return { ...rest, c: proxy.c };` (the helper is `takeItem`, imported at the top of comparables.js already) Doc comment mirroring reconcilePendingFind's: Phase 29 (LOOT-01/06) — killFoe now defers the rolled drop into state.pendingLoot instead of the prototype's mid-fight auto-take; applying the SAME legacy takeItem per pending drop, in kill order, onto a clone proves the engine offers byte-identically what the prototype auto-took (RESEARCH: only combat/lose seed 14 ever rolls a drop, a jewel, and that scenario's per-action diff is already skipped by its Phase 24 action-path record — so this is a structural tripwire like stripBagArmorFields); kind:"bag" entries are engine-only content with no prototype equivalent and are skipped, like stripBagField. In movementComparable, combatComparable and economyComparable add `pendingLoot` to the top-level destructure (after `pendingFind`) and change the reconcile line to `const rest = reconcilePendingFind(reconcilePendingLoot(state0, pendingLoot), pendingFind);` (loot first: drops happen in the fight, a find afterwards; no fixture carries both). Add a fifth-analog comment line beside the existing pendingFind/dev comments in each.
    2. `src/browser/toasts.js` — add to the items section (NOT to ORACLE_ONLY): `lootDropped: (e) => ({ text: \`Dropped: ${e?.name ?? "something"}. It will keep.\`, tone: "beat", priority: PRIORITY.other })`, `lootTaken: (e) => ({ text: \`Taken: ${e?.item?.n ?? "something"}.\`, tone: "hit", priority: PRIORITY.you })`, `lootLeft: (e) => ({ text: \`Left behind: ${e?.item?.n ?? "it"}.\`, tone: "miss", priority: PRIORITY.you })`, `lootForfeited: (e) => { const names = (e?.items ?? []).map((i) => i?.n ?? "something").join(", ") || "the spoils"; return { text: e?.reason === "died" ? \`Left where they fell: ${names}.\` : \`Left on the floor in your hurry: ${names}.\`, tone: "miss", priority: PRIORITY.you }; }`.
    3. `src/browser/eventNarration.js` — add in the inventory section, deadpan and family-friendly: `lootDropped: (e) => \`<span class="beat">Something falls out of the fight: ${e.name ?? "something"}.</span> It will wait. It has nowhere else to be.\``; `lootTaken: (e) => \`<span class="hit">Into the bag:</span> ${e.item?.n ?? "something"}. Your back sends its regards.\``; `lootLeft: (e) => \`<span class="miss">You leave ${e.item?.n ?? "it"} on the floor.</span> Someone will be thrilled. Not you.\``; `lootForfeited: (e) => { const names = (e.items ?? []).map((i) => i?.n ?? "something").join(", ") || "the spoils"; return e.reason === "died" ? \`<span class="miss">${names} stay where they fell.</span> So, for that matter, do you.\` : \`<span class="miss">You leave ${names} on the floor</span> in your hurry not to be on the floor yourself.\`; }`.
    4. Create `test/unit/loot-narration.test.js` (RED first) asserting the toast/Oracle behaviours above plus `assert.ok(!ORACLE_ONLY.has(t))` for the four types; and add the reconcile tests to `test/unit/loot-pile.test.js` (import movementComparable/combatComparable/economyComparable from ../parity/harness/comparables.js and diffState from ../parity/harness/diffState.js; build S1/S2 from newRun(3) so the states carry every real field).
    5. Gate: run the FULL suite — green, parity untouched.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/loot-narration.test.js test/unit/loot-pile.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/voice/safety-scan.test.js` exits 0
    - `npm test` exits 0 with `# fail 0` and `# pass` >= 1525
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing
    - `grep -c "function reconcilePendingLoot" test/parity/harness/comparables.js` == 1; `grep -c "reconcilePendingFind(reconcilePendingLoot(state0, pendingLoot), pendingFind)" test/parity/harness/comparables.js` == 3
    - `grep -c "pendingJoiner, pendingFind, pendingLoot, dev" test/parity/harness/comparables.js` == 3
    - `grep -c "lootDropped:" src/browser/toasts.js` == 1; `grep -c "lootTaken:" src/browser/toasts.js` == 1; `grep -c "lootLeft:" src/browser/toasts.js` == 1; `grep -c "lootForfeited:" src/browser/toasts.js` == 1
    - `grep -c "lootDropped:" src/browser/eventNarration.js` == 1; `grep -c "lootTaken:" src/browser/eventNarration.js` == 1; `grep -c "lootLeft:" src/browser/eventNarration.js` == 1; `grep -c "lootForfeited:" src/browser/eventNarration.js` == 1
    - test/unit/loot-narration.test.js asserts `ORACLE_ONLY.has(t) === false` for each of the four new types (the ORACLE_ONLY set gains no entry for them)
  </acceptance_criteria>
  <done>The pile is carved out of all three comparables with a reconcile that proves byte-identical offers; every new event has a toast and an Oracle line in voice; the full suite is green with parity untouched.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 02's share marked ✔)

| Artifact | Kind | Owner |
|---|---|---|
| `BAG_ORDER`, `BAG_FLOORS`, `BAG_DROP_UNDER`, `BAG_ITEMS`; `slotItems`; `bagCap`/`canStow`/`stowItem`; `weaponUpgradeDelta`/`armorUpgradeDelta`; `bagUpgradeTier`/`bagItemFor`; store pre-pay gate; `bagFull` have/slots; `bagUpgraded`; `lootCompare`/`bagUsage` | Plan 01 | 01 |
| `state.pendingLoot: []` on every fresh run (engine/state.js) | state field | ✔ 02 |
| `sanitizeLoot`; `validateSave`/`rehydrate` carry `pendingLoot` (pre-v1.3 save -> []) | serialization | ✔ 02 |
| `offerLoot(state, it, events)` — the one producer | engine helper | ✔ 02 |
| `takeLoot(state, i, equip, events)`, `leaveLoot(state, i, events)`, `takeAllLoot(state, events)`, `leaveAllLoot(state, events)` | engine handlers | ✔ 02 |
| Actions `takeLoot {i, equip?}`, `leaveLoot {i}`, `takeAllLoot`, `leaveAllLoot` (ACTION_TYPES + validateAction + applyAction cases) | actions | ✔ 02 |
| `forfeitLoot(state, reason, events)` (engine/death.js); `die()` forfeits; flee's three success exits forfeit | engine | ✔ 02 |
| `killFoe` → `offerLoot` + guarded `rng.d(20) <= BAG_DROP_UNDER` bag swap (the phase's only new draw) | engine change | ✔ 02 |
| Events `lootDropped {name, kind}`, `lootTaken {item}`, `lootLeft {item}`, `lootForfeited {items, reason}` | new event types | ✔ 02 |
| `TOAST_FOR` + `EVENT_NARRATION` entries for the four events | presentation | ✔ 02 |
| `reconcilePendingLoot` + `pendingLoot` destructured out of all three `*Comparable()` | parity harness | ✔ 02 |
| `test/unit/loot-pile.test.js`, `test/unit/loot-narration.test.js`, save-validation + roundtrip additions | tests | ✔ 02 |
| Shell bridges, loot screen, readouts, `renderDropShelf`, `test/unit/shell-loot-screen.test.js` | shell | 03 |
</artifacts>

<verification>
- `npm test` green; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` empty.
- The legacy auto-take call is gone from engine/combat.js (Task 2 grep); three `forfeitLoot(state, "fled", events)` in flee; one in die().
- Four new event types each have both presentation entries and are absent from ORACLE_ONLY.
- Draw-count tests pin exactly one extra d20 at depth >= 2 with an upgrade available, none otherwise.
</verification>

<success_criteria>
- LOOT-01: no drop is auto-equipped/auto-rejected — killFoe only offers into the pile with lootDropped.
- LOOT-02 engine half: take/leave per index, take-all/leave-all, all narrated; take-all never loses an item.
- LOOT-05: the guarded bag draw fires only past floor 1 with an upgrade available; parity untouched.
- LOOT-06: pile serialized and reconciled in all three comparables; flight/death forfeit with one narrated line.
</success_criteria>

<human_verification>
Deferred to the end of the autonomous run (the executor copies this block into SUMMARY.md under "## Human verification (deferred to end of run)"):
- On device (after Plan 03): kill a foe that drops something, then flee the next foe in the same fight — the Oracle shows "You leave <item> on the floor in your hurry not to be on the floor yourself." and the loot never appears in the bag.
- Die with a pending pile: the death card shows and the Oracle's last lines include the "stay where they fell" line.
</human_verification>

<output>
Create `.planning/phases/29-end-of-combat-loot-bag-cap/29-02-SUMMARY.md` when done (include the deferred human-verification block above).
</output>
