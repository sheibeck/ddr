---
phase: 29-end-of-combat-loot-bag-cap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/bags.js
  - engine/derived.js
  - engine/items.js
  - engine/economy.js
  - src/browser/viewModels.js
  - src/browser/toasts.js
  - src/browser/eventNarration.js
  - test/unit/bag-cap-gate.test.js
  - test/unit/lootCompare.test.js
autonomous: true
requirements: [LOOT-03, LOOT-04, LOOT-05]

must_haves:
  truths:
    - "LOOT-04 boundary: with a small bag (4 slots) and exactly 4 slot-consuming items, stowItem refuses with bagFull {have: 4, slots: 4} and the bag is unchanged; with 3 it stows (test/unit/bag-cap-gate.test.js)"
    - "LOOT-04 adjacency/exempt: a kind:\"potion\" item stows even when slotItems(c).length === bagCap(c); slotItems never counts kind:\"potion\" entries even when c.items.length exceeds the cap (test/unit/bag-cap-gate.test.js)"
    - "LOOT-04 empty: a character with no bag key has bagCap Infinity and stowItem never refuses; slotItems of a missing/empty items array is [] (test/unit/bag-cap-gate.test.js)"
    - "LOOT-04 precision: bagFull.have and bagFull.slots are Number.isInteger values equal to slotItems(c).length and BAGS[c.bag].slots (test/unit/bag-cap-gate.test.js)"
    - "LOOT-04 store: buyFrom on the lockpicks stock entry with a full bag spends no gold, leaves sold false, emits exactly one bagFull and no bought event; a potion buy on a full bag still succeeds (test/unit/bag-cap-gate.test.js)"
    - "LOOT-04 kit: for every class (forced chargen) and seeds 1..30, newRun leaves slotItems(c).length <= bagCap(c) (test/unit/bag-cap-gate.test.js)"
    - "LOOT-03 rule parity: for every WEAPONS base x bonus 0..2 that the character can legally wield, lootCompare(c, it).upgrade === (takeItem on a clone emits itemTaken) (test/unit/lootCompare.test.js)"
    - "LOOT-05 content: BAG_ORDER is [small, medium, large, exlarge]; BAG_FLOORS is {medium: 2, large: 5, exlarge: 9}; BAG_ITEMS has one kind:\"bag\" entry per upgrade tier; bagUpgradeTier(state) returns null below the tier's floor, when the carried bag is already that tier or better, and when a bag already sits in state.pendingLoot (test/unit/bag-cap-gate.test.js)"
    - "The engine has exactly ONE array-length capacity comparison left: engine/items.js#canStow reads slotItems(c).length against bagCap(c); takeFind, unequipSlot and giveLockpicks all route through stowItem (grep gates in Task 3)"
    - "npm test is green at 1485 + the new tests; parity suites unchanged with zero fixture edits"
  artifacts:
    - "content/bags.js: BAG_ORDER, BAG_FLOORS, BAG_DROP_UNDER, BAG_ITEMS (pure data, documented tuning knobs)"
    - "engine/derived.js: slotItems(c); clampCarry trims slot-consuming overflow while preserving potions"
    - "engine/items.js: exported bagCap, canStow, stowItem, weaponUpgradeDelta, armorUpgradeDelta, bagUpgradeTier, bagItemFor; slotItems re-export"
    - "engine/economy.js: STOWING_EFFECTS pre-pay gate in buyFrom; giveLockpicks via stowItem"
    - "src/browser/viewModels.js: lootCompare(c, it), bagUsage(c)"
    - "src/browser/toasts.js + eventNarration.js: bagFull renders have/slots; bagUpgraded entries"
    - "test/unit/bag-cap-gate.test.js, test/unit/lootCompare.test.js"
  key_links:
    - "engine/items.js#takeItem uses weaponUpgradeDelta/armorUpgradeDelta (the ONE better-than rule) and lootCompare imports the same two functions"
    - "engine/economy.js#buyFrom consults canStow BEFORE deducting gold for STOWING_EFFECTS entries"
    - "engine/items.js#stowItem is the only caller of giveItem outside takeItem's legacy branch"
  prohibitions:
    - requirement_id: LOOT-04
      category: values
      statement: "A refused stow must never cost the player gold, mark a stock slot sold, or discard the item — the refusal is a pure no-op plus one bagFull event"
      status: unresolved
      verification: judgment
    - requirement_id: LOOT-03
      category: transparency
      statement: "The compare readout must never restate the better-than rule in the presentation layer — it must read takeItem's own delta helpers so the screen can never disagree with what the engine would auto-equip"
      status: unresolved
      verification: judgment
---

<objective>
Land the single bag-cap gate (LOOT-04), the bigger-bag content tiers and their pure availability helper (LOOT-05 content half), and the compare-to-equipped view-model (LOOT-03 helper) — everything the pending pile (Plan 02) and the loot screen (Plan 03) build on, with no behaviour change to any parity-exposed path.

Purpose: LOOT-04 requires ONE capacity rule (`slotItems(c).length >= bagCap(c)`, potions exempt) enforced at ONE engine site (`stowItem`) that every stow path routes through — find, unequip, store lockpicks, and (Plan 02) loot takes. Today the cap is checked at two sites with a raw array length that counts special potions (RESEARCH Pitfall 2), and the store deducts gold before any stow could be refused (RESEARCH Pitfall 1). This plan closes both gaps and extracts takeItem's better-than rule into two pure helpers so Plan 03's readout cannot drift from it.

Output: `slotItems`/`bagCap`/`canStow`/`stowItem`/`weaponUpgradeDelta`/`armorUpgradeDelta`/`bagUpgradeTier`/`bagItemFor` in the engine, `BAG_ORDER`/`BAG_FLOORS`/`BAG_DROP_UNDER`/`BAG_ITEMS` content, `lootCompare`/`bagUsage` view-models, richer `bagFull` toast text, `bagUpgraded` toast + Oracle entries, two new test files, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-CONTEXT.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md
@.planning/phases/28-armor-integrity-durability/28-01-SUMMARY.md
@.planning/phases/28-armor-integrity-durability/28-02-SUMMARY.md

Engine gate (non-negotiable): engine pure/deterministic; parity byte-identical for solo play; `test/parity/prototype-master.js.txt` NEVER edited; no fixture JSON regenerated; every new event type needs an `EVENT_NARRATION` entry AND a `TOAST_FOR` entry (coverage guards `test/unit/formatEventsCoverage.test.js` + `test/unit/toastsCoverage.test.js`); voice safety scan (`test/voice/safety-scan.test.js`) stays green. This plan draws NO new rng anywhere.

Baseline: `npm test` = 1485/1485 (confirmed 2026-09-16 before Phase 29 work).

Decisions honoured from CONTEXT.md: "The single bag-cap gate (LOOT-04)" (slotItems excludes kind potion; ONE engine helper; bagFull with have/slots; the engine never spends gold or discards on a refused stow; chargen never exceeds bagCap — add the test), "Bigger bags as treasure (LOOT-05)" (kind bag items one tier above the carried bag; floors medium 2 / large 5 / exlarge 9 as documented knobs; never offered below the floor or when carrying that tier or better), "Compare-to-equipped & take semantics (LOOT-03)" (lootCompare in viewModels.js reading takeItem's own rule; class/race-illegal reads "can't use (Fighter only)" via weaponRefusalReason/armorRefusalReason — one rule, no duplication).

Comment-text discipline: several acceptance criteria below negative-grep for the OLD capacity expressions. Do NOT echo those old expressions verbatim into new comments or doc strings — describe them by concept ("the raw array-length capacity check") so the commit-time gate stays clean.
</context>

<tasks>

<!-- planner-discipline-allow: WEAPON_MAX -->

<task type="auto" tdd="true">
  <name>Task 1: Bag content tiers + slotItems + potion-preserving clampCarry</name>
  <files>content/bags.js, engine/derived.js, test/unit/bag-cap-gate.test.js</files>
  <read_first>
    - content/bags.js (the BAGS table and its tuning-knob comment — extend, never change BAGS values; test/unit/carry-model.test.js pins them)
    - engine/derived.js lines 55-71 (clampCarry — the slot trim to rewrite; the BAGS import at line 10)
    - test/unit/carry-model.test.js lines 134-162 (existing clampCarry tests that must stay green)
    - test/determinism/content-is-pure-data.test.js (content exports must be closure-free plain data)
    - test/unit/inventory-actions.test.js lines 1-60 (fixedChar/fixedState helper shape to copy for the new test file)
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md "Pitfall 2" (why potions must be preserved by the trim)
  </read_first>
  <behavior>
    - slotItems({items: [gear, potion, gear]}) returns the two gear entries in order; slotItems({}) and slotItems({items: null}) return []
    - slotItems skips null/undefined entries defensively (no throw)
    - clampCarry on a small bag with items [a, potionP, b, c, d, e] (5 gear + 1 potion) leaves [a, potionP, b, c, d] — the potion survives, the trailing gear e is dropped, order preserved
    - clampCarry on a small bag with 4 gear + 3 potions leaves all 7 entries untouched (potions are exempt, gear is at cap not over)
    - the three existing clampCarry tests in test/unit/carry-model.test.js still pass unchanged
    - BAGS is deepStrictEqual to its current four tiers (unchanged); BAG_ORDER deepStrictEqual ["small","medium","large","exlarge"]; BAG_FLOORS deepStrictEqual {medium: 2, large: 5, exlarge: 9}; BAG_DROP_UNDER is an integer 1..20; BAG_ITEMS has keys medium/large/exlarge, each {kind:"bag", tier:<key>, n:<string>, txt:<string>} and BAG_ITEMS[k].tier === k
  </behavior>
  <action>
    1. `content/bags.js` — append four exports beneath BAGS (BAGS itself stays byte-identical; test/unit/carry-model.test.js deepStrictEquals its tiers):
       - `export const BAG_ORDER = ["small", "medium", "large", "exlarge"];` — tier order, lowest first.
       - `export const BAG_FLOORS = { medium: 2, large: 5, exlarge: 9 };` — the floor from which each upgrade tier may turn up as a foe drop. Document as TUNING KNOBS in the same voice as the existing knob comment (CONTEXT §Specific Ideas: "Depth tiers for bags (2 / 5 / 9) are tuning knobs — document them as such alongside the existing knob comment").
       - `export const BAG_DROP_UNDER = 3;` — on a successful treasure drop at or past a tier's floor, one extra d20 at or under this value swaps the rolled item for the bag (Plan 02 wires the draw). TUNING KNOB.
       - `export const BAG_ITEMS = { medium: { kind: "bag", tier: "medium", n: "Medium bag", txt: "6 slots. Room to regret more things." }, large: { kind: "bag", tier: "large", n: "Large bag", txt: "8 slots. Your spine has filed a complaint." }, exlarge: { kind: "bag", tier: "exlarge", n: "Enormous bag", txt: "10 slots. Technically luggage." } };` — pure data, family-friendly deadpan; slot counts must match BAGS[tier].slots (assert in the test by deriving from BAGS, do not hard-code 6/8/10 twice).
       Everything is plain data (no functions) — test/determinism/content-is-pure-data.test.js scans every content export.
    2. `engine/derived.js` — add `export function slotItems(c)` returning `(c && Array.isArray(c.items) ? c.items : []).filter((it) => it && it.kind !== "potion")`. Doc comment: LOOT-04 user rule (2026-09-15) — only gear and treasure consume slots; healing potions (`c.potions`) and scrolls (`c.scrolls`) are scalars, special potions (`kind:"potion"`) live in `c.items` but are exempt. This is THE capacity count every engine and shell site must read.
    3. `engine/derived.js#clampCarry` — replace the slot-trim line: compute `const keep = slotItems(c)`; when `keep.length > cap.slots`, rebuild `c.items` as the original array filtered to entries that are either `kind:"potion"` OR among the first `cap.slots` slot-consuming entries (preserve original relative order — e.g. walk `c.items` once with a counter of slot-consuming entries seen so far). Leave the gold/rations clamps untouched. Doc-comment the change: a naive length truncation would drop a trailing potion instead of trailing gear (RESEARCH Pitfall 2, assumption A3 resolved here rather than deferred). Do NOT mention the old expression verbatim in the comment.
    4. Create `test/unit/bag-cap-gate.test.js` with a header comment (Phase 29, LOOT-04/LOOT-05: the single bag-cap gate and bag content) and a `fixedChar`/`fixedState` helper pair copied from test/unit/inventory-actions.test.js (Fighter, Broadsword, Leather AR 6, bag small, items []). Write the Task 1 behaviours above as `node:test` cases importing `slotItems`, `clampCarry` from engine/derived.js and `BAGS`, `BAG_ORDER`, `BAG_FLOORS`, `BAG_DROP_UNDER`, `BAG_ITEMS` from content/bags.js. Use the RED -> GREEN order: write the tests, run them (they must fail on the missing exports), then implement steps 1-3.
  </action>
  <verify>
    <automated>node --test test/unit/bag-cap-gate.test.js test/unit/carry-model.test.js test/determinism/content-is-pure-data.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/bag-cap-gate.test.js` exits 0
    - `node --test test/unit/carry-model.test.js test/determinism/content-is-pure-data.test.js` exits 0
    - `grep -c "export const BAG_ORDER" content/bags.js` == 1; `grep -c "export const BAG_FLOORS" content/bags.js` == 1; `grep -c "export const BAG_DROP_UNDER" content/bags.js` == 1; `grep -c "export const BAG_ITEMS" content/bags.js` == 1
    - `grep -c "export function slotItems" engine/derived.js` == 1
    - `grep -v '^\s*//' engine/derived.js | grep -v '^\s*\*' | grep -c "c.items.length > cap.slots"` == 0 (the raw length trim is gone)
    - `grep -c "small: { slots: 4, wilmst: 2000, rations: 10 }" content/bags.js` == 1 (BAGS untouched)
  </acceptance_criteria>
  <done>Content tiers/floors/items/knob exported as pure data; slotItems exists; clampCarry trims gear not potions; new test file green alongside the untouched carry-model tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stowItem — the one gate; delta helpers; bagUpgradeTier; store pay-order fix</name>
  <files>engine/items.js, engine/economy.js, test/unit/bag-cap-gate.test.js</files>
  <read_first>
    - engine/items.js lines 1-70 (imports, giveItem), 240-305 (takeItem — the better-than rule to extract), 306-372 (bagCap, wornWeaponItem, wornArmorItem), 374-395 (takeFind), 495-540 (unequipSlot)
    - engine/economy.js lines 180-225 (STORE_EFFECTS: givePotion/giveLockpicks call giveItem), 240-250 (the stock `add` shape — effectParams.item for potions/lockpicks), 322-345 (buyFrom — gold is deducted and `sold` set BEFORE the effect runs)
    - engine/derived.js#slotItems (Task 1)
    - content/bags.js BAG_ORDER/BAG_FLOORS/BAG_ITEMS (Task 1)
    - test/unit/inventory-actions.test.js (existing takeFind/unequipSlot/bagFull tests — must stay green; note they assert `bagFull` by type only)
    - test/unit/economy.test.js and test/unit/store-sell.test.js (how a store state with stock is built for buyFrom tests — reuse the pattern for the lockpick/potion buy tests)
    - test/unit/items.test.js lines 100-170 (takeItem tests that pin itemTaken/itemRejected notBetter — the extracted deltas must keep them green)
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md "Pitfall 1" (option (a): check before paying)
    - .planning/phases/28-armor-integrity-durability/28-01-SUMMARY.md (wornArmorItem carries left/patches; destroyed armor returns null — unequipSlot's destroyed branch must stay slot-free)
  </read_first>
  <behavior>
    - bagCap({bag:"small"}) === 4; bagCap({}) === Infinity; canStow(c) is true with 3 slot items in a small bag and false with 4
    - stowItem(state, gear) with 3 slot items: returns true, item appended to c.items, no bagFull event, no itemGiven event (quiet default)
    - stowItem(state, gear, events, false) pushes itemGiven (non-quiet)
    - stowItem(state, gear) with 4 slot items: returns false, c.items unchanged (deepStrictEqual before/after), events === [{type:"bagFull", item: gear, have: 4, slots: 4}]
    - stowItem(state, potion) with 4 slot items and 2 potions already: returns true, potion appended (exempt)
    - stowItem applies eff.wp on stow exactly like giveItem (an item with eff:{wp:3} raises maxWP and wp by 3)
    - stowItem(state, {kind:"bag", tier:"medium", ...}) with c.bag "small": returns true, c.bag becomes "medium", c.items unchanged, events contain bagUpgraded {from:"small", to:"medium", slots: 6, item}
    - stowItem with a medium bag item while c.bag is "medium" or "large": returns true, c.bag unchanged, events contain itemRejected {reason:"notBetter"}, no bagUpgraded
    - takeFind at cap: bagFull carries have/slots integers, pendingFind kept; takeFind of a potion find at cap succeeds (findTaken, pendingFind null)
    - unequipSlot("weapon") at cap: bagFull carries have/slots; the worn weapon stays equipped
    - weaponUpgradeDelta(c, it) === (WEAPON_MAX[it.base]+it.bonus) - (WEAPON_MAX[c.weapon]+c.prof+c.magicWpn); armorUpgradeDelta(c, it) === it.ar - c.ar; takeItem still emits itemRejected notBetter when the delta is <= 0 and itemTaken when > 0 (existing test/unit/items.test.js stays green)
    - bagUpgradeTier({c:{bag:"small"}, floor:{depth:1}, pendingLoot:[]}) === null; depth 2 -> "medium"; c.bag "medium" at depth 2 -> null (large needs 5); depth 5 -> "large"; c.bag "exlarge" any depth -> null; c.bag missing -> null; depth 2 small bag but pendingLoot already holds a kind:"bag" item -> null; state without a pendingLoot key -> treated as empty (no throw)
    - bagItemFor("medium") deepStrictEquals BAG_ITEMS.medium and is NOT the same object reference (fresh copy)
    - buyFrom(lockpicks idx) with a full bag: c.gold unchanged, stock[idx].sold === false, events deepStrictEqual [{type:"bagFull", item: <the lockpicks item>, have: 4, slots: 4}] (no bought, no itemGiven), c.items unchanged
    - buyFrom(lockpicks idx) with room: gold deducted, sold true, events include bought then itemGiven, lockpicks in c.items (unchanged behaviour)
    - buyFrom(potion idx) with a full bag: succeeds (gold deducted, bought + itemGiven, potion in c.items) — potions are exempt
    - chargen kit: for each cls in Object.keys(CLASSES) and seeds 1..30, newRun(seed, [], {force:{cls}}) yields slotItems(c).length <= bagCap(c)
  </behavior>
  <action>
    1. `engine/items.js` imports: extend the derived.js import to `{ eff, skill, slotItems }` and the content import with `BAG_ORDER, BAG_FLOORS, BAG_ITEMS`. Change the re-export line to `export { eff, slotItems };`.
    2. Make `bagCap` an `export function` (same body). Add `export function canStow(c)` returning `slotItems(c).length < bagCap(c)` — doc: THE capacity predicate; every stow path and the store's pre-pay check read this one line.
    3. Add `export function weaponUpgradeDelta(c, it)` = `((WEAPON_MAX[it.base] || 0) + (it.bonus || 0)) - ((WEAPON_MAX[c.weapon] || 0) + (c.prof || 0) + (c.magicWpn || 0))` and `export function armorUpgradeDelta(c, it)` = `it.ar - c.ar`. Rewrite takeItem's weapon branch to compute `const delta = weaponUpgradeDelta(c, it)` and reject `notBetter` when `delta <= 0` (drop the local now/then variables); armor branch rejects when `armorUpgradeDelta(c, it) <= 0`. Byte-identical outcomes (same arithmetic) — doc-comment that Plan 03's lootCompare imports these two so the readout cannot drift.
    4. Add `export function stowItem(state, it, events = [], quiet = true)` returning a boolean:
       - `kind:"bag"` branch: `const c = state.c; const from = c.bag; const have = BAG_ORDER.indexOf(from); const to = BAG_ORDER.indexOf(it.tier);` if `to > have` set `c.bag = it.tier` and push `{ type: "bagUpgraded", from, to: it.tier, slots: BAGS[it.tier].slots, item: it }`; else push `{ type: "itemRejected", item: it, reason: "notBetter" }`. Return true either way (a bag is consumed, never stowed, never a slot).
       - otherwise: if `!canStow(state.c)` push `{ type: "bagFull", item: it, have: slotItems(state.c).length, slots: bagCap(state.c) }` and return false (nothing else mutated); else `giveItem(state, it, quiet, events)` and return true.
       Doc: the ONE path that adds to c.items from outside chargen (CONTEXT §The single bag-cap gate); callers push their own domain event (findTaken/itemUnequipped/lootTaken) after a true return.
    5. Re-route: `takeFind` replaces its capacity check + giveItem call with `if (!stowItem(state, it, events, true)) return events;` (then clear pendingFind and push findTaken as today). `unequipSlot` replaces its capacity check + `c.items.push(worn)` with `if (!stowItem(state, worn, events, true)) return events;` (the destroyed-armor branch above it stays slot-free and untouched). After this, the only direct giveItem CALL sites left in items.js are takeItem's legacy tail and stowItem.
    6. Add `export function bagUpgradeTier(state)`: `const c = state.c; const idx = BAG_ORDER.indexOf(c && c.bag); if (idx < 0) return null; const next = BAG_ORDER[idx + 1]; if (!next) return null; if (!(state.floor && state.floor.depth >= BAG_FLOORS[next])) return null; if ((state.pendingLoot || []).some((x) => x && x.kind === "bag")) return null; return next;` Pure read, no rng. Doc: LOOT-05 guard — Plan 02's killFoe fires its one extra d20 only when this is non-null; every parity fixture fights at depth 1 so it is null there (RESEARCH "LOOT-05 guard safety"). Add `export function bagItemFor(tier)` returning `{ ...BAG_ITEMS[tier] }` (fresh copy so state never aliases content).
    7. `engine/economy.js`: import `{ giveItem, takeItem, stowItem, canStow, slotItems, bagCap }` from ./items.js (keep existing names). Add `const STOWING_EFFECTS = new Set(["giveLockpicks"]);` with a doc comment: effects that land a slot-consuming item in the bag; givePotion is deliberately absent (potions are exempt, LOOT-04). In `buyFrom`, AFTER the insufficient-gold guard and BEFORE `state.c.gold -= item.cost`, add: `if (STOWING_EFFECTS.has(item.effectId) && !canStow(state.c)) { events.push({ type: "bagFull", item: item.effectParams && item.effectParams.item, have: slotItems(state.c).length, slots: bagCap(state.c) }); return events; }` — no gold spent, `sold` untouched, no `bought` event (RESEARCH Pitfall 1, option (a)). Change `giveLockpicks` to `stowItem(state, params.item, events, false)` (non-quiet keeps today's itemGiven). Leave `givePotion` on `giveItem` with a one-line comment naming the exemption.
    8. Extend `test/unit/bag-cap-gate.test.js` with the Task 2 behaviours (RED first). For the store tests build `state.store = { stock: [...], haggle: false, race }` by hand with two entries shaped like openStore's `add` output: a lockpicks entry `{ n: "Set of lockpicks", sub: null, cost: 450, effectId: "giveLockpicks", effectParams: { item: { kind: "picks", n: "Lockpicks", txt: "1–5 on d10 against any lock" } }, sold: false }` and a potion entry with effectId "givePotion" and a `kind:"potion"` item; give the character 1000 gold. For the chargen test import `newRun` from engine/state.js and `CLASSES` from content/index.js and use the dev-only `force` option exactly as test/determinism/forced-chargen.test.js does.
  </action>
  <verify>
    <automated>node --test test/unit/bag-cap-gate.test.js test/unit/inventory-actions.test.js test/unit/items.test.js test/unit/economy.test.js test/unit/store-sell.test.js test/parity/economy-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/bag-cap-gate.test.js test/unit/inventory-actions.test.js test/unit/items.test.js test/unit/economy.test.js test/unit/store-sell.test.js` exits 0
    - `node --test test/parity/economy-parity.test.js test/parity/combat-parity.test.js` exits 0 with zero edits under test/parity/fixtures/ (`git status --porcelain test/parity` is empty)
    - `grep -c "export function stowItem" engine/items.js` == 1; `grep -c "export function canStow" engine/items.js` == 1; `grep -c "export function bagCap" engine/items.js` == 1; `grep -c "export function weaponUpgradeDelta" engine/items.js` == 1; `grep -c "export function armorUpgradeDelta" engine/items.js` == 1; `grep -c "export function bagUpgradeTier" engine/items.js` == 1; `grep -c "export function bagItemFor" engine/items.js` == 1
    - `grep -c "^s*giveItem(state, it," engine/items.js` == 2 (call sites: takeItem's legacy tail + stowItem only — takeFind's direct call is gone)
    - `grep -c "if (!stowItem(state, it, events, true)) return events;" engine/items.js` == 1 (takeFind); `grep -c "if (!stowItem(state, worn, events, true)) return events;" engine/items.js` == 1 (unequipSlot)
    - `grep -c "c.items.push" engine/items.js` == 1 (giveItem only)
    - `grep -v '^\s*//' engine/items.js | grep -v '^\s*\*' | grep -c "items || \[\]).length >= bagCap"` == 0
    - `grep -c "giveItem(state, params.item, false, events)" engine/economy.js` == 1 (givePotion only); `grep -c "stowItem(state, params.item, events, false)" engine/economy.js` == 1
    - `grep -c "STOWING_EFFECTS" engine/economy.js` >= 2
    - `grep -c "type: \"bagUpgraded\"" engine/items.js` == 1
  </acceptance_criteria>
  <done>One capacity predicate, one stow gate, two delta helpers, the bag-tier availability read, and a store that never pays for a refused stow — all pinned by tests; parity suites untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: lootCompare + bagUsage view-models, bagFull/bagUpgraded presentation, full-suite gate</name>
  <files>src/browser/viewModels.js, src/browser/toasts.js, src/browser/eventNarration.js, test/unit/lootCompare.test.js</files>
  <read_first>
    - src/browser/viewModels.js lines 1-12 (imports) and 85-137 (armorDisplay/bagArmorText — the helper style to mirror)
    - engine/items.js: weaponRefusalReason, armorRefusalReason, weaponUpgradeDelta, armorUpgradeDelta, bagCap, canStow, slotItems (Task 2)
    - content/weapons.js WEAPONS[base].cls letters (F/T/M) and content/armors.js ARMORS[].cls — source of the "Fighter only" text
    - src/browser/toasts.js lines 148-180 (ORACLE_ONLY), 1238-1253 (item toasts incl. the current bagFull builder and the `block()` helper)
    - src/browser/eventNarration.js lines 596-610 (findTaken/findLeft/bagFull Oracle lines — voice to match)
    - test/unit/armorDisplay.test.js (test style for view-model helpers and for TOAST_FOR/EVENT_NARRATION branch assertions)
    - test/unit/toastsCoverage.test.js + test/unit/formatEventsCoverage.test.js (why bagUpgraded needs BOTH entries now that Task 2 emits it)
    - test/voice/safety-scan.test.js header (every builder is invoked with a bare {type} and with injected vocab — defend every field with ?? / ?.)
  </read_first>
  <behavior>
    - lootCompare(fighter{Broadsword, prof 0, magicWpn 0}, {kind:"weapon", base:"Broadsword", bonus:2}) -> { kind:"weapon", legal:true, reason:null, delta:2, upgrade:true, equipNow:true, line:"+2 damage", sub:<it.txt> }
    - lootCompare(fighter, {kind:"weapon", base:"Dagger", bonus:0}) -> upgrade:false, equipNow:false, line:"not an upgrade"
    - lootCompare(magicUser, {kind:"weapon", base:"Broadsword", bonus:3}) -> legal:false, reason:"wrongClass", equipNow:false, line starts with "can't use (" and names the letters of WEAPONS.Broadsword.cls as class names joined by " or " followed by " only)" — e.g. "can't use (Fighter only)"
    - lootCompare(acrobat, {kind:"weapon", base:"Long Sword"}) -> reason:"acrobat", line "can't use (Acrobat: dagger only)"
    - lootCompare(fighter{ar 6}, {kind:"armor", armor:"Plate", ar:15, wp:45, min:2, cls:"F"}) -> delta:9, upgrade:true, equipNow:true, line "AR 15 vs your AR 6 · upgrade", sub "45/45 hp" (a piece with left 30 -> "30/45 hp"; left 0 -> "destroyed")
    - lootCompare(fighter{ar 6}, armor ar 6) -> upgrade:false, line "AR 6 vs your AR 6 · not an upgrade"
    - lootCompare(fridgian, any armor) -> reason:"noArmor", line "can't use (your kind wears no armour)"; woodsman + Plate -> reason "woodsman", line "can't use (no mail or plate for a Woodsman)"; a plain Thief + Mail (cls "F") -> reason "tooHeavy", line "can't use (Fighter only)"
    - lootCompare(c, {kind:"bag", tier:"medium", n, txt}) -> equipNow:false, upgrade:null, line "6 slots — you carry 4" for a small-bag character, sub ""
    - lootCompare(c, {kind:"jewel", txt:"x"}) -> equipNow:false, upgrade:null, legal:true, line:"x", sub:""
    - the shell joins line then sub ("+2 damage · Long Sword +2", "AR 15 vs your AR 6 · upgrade · 45/45 hp") — lootCompare itself never joins them
    - property: for every base in Object.keys(WEAPONS) and bonus in [0,1,2], with a Fighter Soldier Human wielding Broadsword (prof 0, magicWpn 0): when canEquipWeapon(c, it) is true, lootCompare(c, it).upgrade === (takeItem(structuredClone({c}), it, []) contains itemTaken)
    - bagUsage({bag:"small", items:[gear x3, potion]}) -> { have:3, slots:4, full:false, text:"3 / 4" }; with 4 gear -> full:true, text "4 / 4"; no bag key and 2 items -> { have:2, slots:null, full:false, text:"2" }; no bag and no items -> text ""
    - TOAST_FOR.bagFull({type:"bagFull", have:4, slots:4}).text === "Bag full (4/4) — drop something to make room." with tone "block"; TOAST_FOR.bagFull({type:"bagFull"}).text === "No room in the bag."
    - TOAST_FOR.bagUpgraded({type:"bagUpgraded", to:"medium", slots:6}).text === "Bigger bag: 6 slots." tone "hit"; a bare {type} call returns a non-empty string
    - EVENT_NARRATION.bagUpgraded({type:"bagUpgraded", item:{n:"Medium bag"}, slots:6}) is a non-empty string containing "6 slots" and a <span class="hit"> opener; a bare {type} call does not throw
    - EVENT_NARRATION.bagFull({type:"bagFull", item:{n:"X"}, have:4, slots:4}) contains "(4/4)"; without have/slots it equals today's wording
  </behavior>
  <action>
    1. `src/browser/viewModels.js`: import `{ weaponRefusalReason, armorRefusalReason, weaponUpgradeDelta, armorUpgradeDelta, bagCap, canStow, slotItems }` from "../../engine/items.js" and `BAGS` from "../../content/index.js" (keep existing imports). Add a private `classNames(letters)` mapping F->"Fighter", T->"Thief", M->"Magic User" over the letters of a cls string, joined with " or ". Add a private `refusalText(reason, clsLetters)`: wrongClass/tooHeavy -> `${classNames(clsLetters)} only`; acrobat -> "Acrobat: dagger only"; noArmor -> "your kind wears no armour"; woodsman -> "no mail or plate for a Woodsman"; fallback -> "not for you".
    2. Add `export function lootCompare(c, it)` returning a plain object `{ kind, legal, reason, delta, upgrade, equipNow, line, sub }`:
       - weapon: reason = weaponRefusalReason(c, it); delta = weaponUpgradeDelta(c, it); legal = reason === null; upgrade = delta > 0; equipNow = legal && upgrade; line = !legal ? `can't use (${refusalText(reason, WEAPONS[it.base]?.cls ?? "")})` : upgrade ? `+${delta} damage` : "not an upgrade"; sub = it.txt ?? "".
       - armor: reason = armorRefusalReason(c, it); delta = armorUpgradeDelta(c, it); legal/upgrade/equipNow as above; line = !legal ? `can't use (${refusalText(reason, it.cls ?? "")})` : `AR ${it.ar} vs your AR ${c.ar} · ${upgrade ? "upgrade" : "not an upgrade"}`; sub = the durability fragment `${left}/${it.wp} hp` with `const left = it.left ?? it.wp` (Phase 28's tolerant read — a fresh drop has no `left`), or "destroyed" when left <= 0 (the AR is already in the line, so the sub carries only the pool).
       - bag: legal true, reason null, delta null, upgrade null, equipNow false; line = `${BAGS[it.tier]?.slots ?? "?"} slots — you carry ${bagCap(c) === Infinity ? "no bag" : bagCap(c)}`; sub = "" (the item txt would repeat the slot count).
       - anything else: legal true, reason null, delta null, upgrade null, equipNow false, line = it.txt ?? "", sub "".
       Doc comment: mirrors armorDisplay's single-source pattern — the verdict is takeItem's own delta helpers, never a restated formula (CONTEXT §Compare-to-equipped).
    3. Add `export function bagUsage(c)`: `const have = slotItems(c).length; const cap = bagCap(c); const slots = cap === Infinity ? null : cap; return { have, slots, full: !canStow(c), text: slots ? `${have} / ${slots}` : have ? `${have}` : "" };` — the ONE readout the shell's gear panel, find card, loot screen and store display (Plan 03 bridges it as window.__mzBagUsage).
    4. `src/browser/toasts.js`: change `bagFull` to `(e) => block(e?.have != null && e?.slots != null ? \`Bag full (${e.have}/${e.slots}) — drop something to make room.\` : "No room in the bag.")`. Add `bagUpgraded: (e) => ({ text: \`Bigger bag: ${e?.slots ?? "more"} slots.\`, tone: "hit", priority: PRIORITY.you })` in the items section. Do NOT add anything to ORACLE_ONLY.
    5. `src/browser/eventNarration.js`: extend `bagFull` to append ` (${e.have}/${e.slots})` after "No room." when both are present (today's wording otherwise). Add `bagUpgraded: (e) => \`<span class="hit">A bigger bag.</span> ${e.item?.n ?? "It"} holds ${e.slots ?? "more"} slots — more room to make worse decisions in.\`` in the inventory section (deadpan, family-friendly; the safety scan covers it automatically).
    6. Create `test/unit/lootCompare.test.js` (RED first) covering every behaviour above; import `lootCompare, bagUsage, bagArmorText` from viewModels.js, `TOAST_FOR` from toasts.js, `EVENT_NARRATION` from eventNarration.js, `WEAPONS` from content, `takeItem, canEquipWeapon` from engine/items.js. Build characters with the same fixedChar shape as bag-cap-gate.test.js (add race "Fridgian" / sub "Woodsman" / cls "Thief" sub "Acrobat" variants).
    7. Gate: run the FULL suite (`npm test`) — it must be green (1485 baseline + every new test in this plan) with `git status --porcelain test/parity` empty.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/lootCompare.test.js` exits 0
    - `npm test` exits 0; the summary line reports `# fail 0` and `# pass` >= 1500
    - `git status --porcelain test/parity` prints nothing (no fixture or master edits)
    - `grep -c "export function lootCompare" src/browser/viewModels.js` == 1; `grep -c "export function bagUsage" src/browser/viewModels.js` == 1
    - `grep -c "weaponUpgradeDelta" src/browser/viewModels.js` >= 2 (import + use)
    - `grep -c "WEAPON_MAX" src/browser/viewModels.js` == 0 (no restated formula in the view-model; the token belongs to engine/items.js only)
    - `grep -c "bagUpgraded:" src/browser/toasts.js` == 1; `grep -c "bagUpgraded:" src/browser/eventNarration.js` == 1
    - `grep -c "Bag full (" src/browser/toasts.js` == 1
    - `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js` exits 0
  </acceptance_criteria>
  <done>lootCompare and bagUsage exist and are pinned against takeItem's own rule; bagFull shows have/slots; bagUpgraded has both presentation entries; the full suite is green with parity untouched.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 01's share marked ✔; Plans 02/03 own the rest)

| Artifact | Kind | Owner |
|---|---|---|
| `BAG_ORDER`, `BAG_FLOORS`, `BAG_DROP_UNDER`, `BAG_ITEMS` (content/bags.js) | content data | ✔ 01 |
| `slotItems(c)` (engine/derived.js, re-exported from engine/items.js) | helper | ✔ 01 |
| `clampCarry` potion-preserving slot trim | behaviour change | ✔ 01 |
| `bagCap(c)` (now exported), `canStow(c)`, `stowItem(state, it, events, quiet)` | engine helpers | ✔ 01 |
| `weaponUpgradeDelta(c, it)`, `armorUpgradeDelta(c, it)` (takeItem now reads them) | engine helpers | ✔ 01 |
| `bagUpgradeTier(state)`, `bagItemFor(tier)` | engine helpers | ✔ 01 |
| `STOWING_EFFECTS` + pre-pay `bagFull` gate in `buyFrom`; `giveLockpicks` via stowItem | engine change | ✔ 01 |
| `bagFull` event gains additive `have`/`slots` fields | event payload | ✔ 01 |
| `bagUpgraded {from, to, slots, item}` | new event type | ✔ 01 (emitted by stowItem) |
| `lootCompare(c, it)`, `bagUsage(c)` (src/browser/viewModels.js) | view-models | ✔ 01 |
| `TOAST_FOR.bagFull` richer text; `TOAST_FOR.bagUpgraded`; `EVENT_NARRATION.bagUpgraded`; `EVENT_NARRATION.bagFull` have/slots | presentation | ✔ 01 |
| `test/unit/bag-cap-gate.test.js`, `test/unit/lootCompare.test.js` | tests | ✔ 01 |
| `state.pendingLoot: []` (newRun; serialized via validateSave/rehydrate + `sanitizeLoot`) | state field | 02 |
| Actions `takeLoot {i, equip?}`, `leaveLoot {i}`, `takeAllLoot`, `leaveAllLoot` (+ validateAction + dispatch) | actions | 02 |
| `offerLoot(state, it, events)`, `takeLoot/leaveLoot/takeAllLoot/leaveAllLoot` handlers (engine/items.js) | engine helpers | 02 |
| `forfeitLoot(state, reason, events)` (engine/death.js) called in `die()` and flee's 3 success exits | engine helper | 02 |
| `killFoe` → `offerLoot` + guarded bag d20 (`bagUpgradeTier` && `rng.d(20) <= BAG_DROP_UNDER`) | engine change | 02 |
| Events `lootDropped {name, kind}`, `lootTaken {item}`, `lootLeft {item}`, `lootForfeited {items, reason}` + toast/Oracle entries | new event types | 02 |
| `reconcilePendingLoot` in all three `*Comparable()` (test/parity/harness/comparables.js) | parity harness | 02 |
| `test/unit/loot-pile.test.js`, `test/unit/loot-narration.test.js`, save/roundtrip additions | tests | 02 |
| `window.__mzBagUsage`, `window.__mzLootCompare`, `window.__mzLootReport`; `window.mzTakeLoot/mzLeaveLoot/mzTakeAllLoot/mzLeaveAllLoot` | shell bridges | 03 |
| Loot screen branch in `renderEncounter`; `hasActiveEncounter` parks the map on a pending pile; `noteCombat` folds the combat report into the loot screen; `renderDropShelf`; `renderCarriedList` loot actions | shell | 03 |
| `test/unit/shell-loot-screen.test.js` | tests | 03 |
</artifacts>

<verification>
- `npm test` green (baseline 1485 + new tests), `git status --porcelain test/parity` empty.
- Grep gates: exactly one `giveItem(state, params.item, false, events)` in economy.js (givePotion); only two direct giveItem call sites in items.js (takeItem tail + stowItem); the raw length trim absent from derived.js; the raw capacity comparison absent from items.js.
- `bagUpgraded` has both a TOAST_FOR and an EVENT_NARRATION entry (coverage guards green).
</verification>

<success_criteria>
- LOOT-04 engine half: ONE predicate (`canStow`) and ONE gate (`stowItem`) — takeFind, unequipSlot and the store's lockpick buy route through it; potions never count; a refused store stow spends nothing; chargen never exceeds the cap (test).
- LOOT-05 content half: tiers, floors, drop knob and items exist as pure data; `bagUpgradeTier` returns null on every parity-fixture state (depth 1).
- LOOT-03 helper: `lootCompare` verdicts equal takeItem's for every weapon; illegal gear reads "can't use (Fighter only)"-style text from the shared refusal reasons.
- Full suite green; parity untouched.
</success_criteria>

<human_verification>
Deferred to the end of the autonomous run (the executor copies this block into SUMMARY.md under "## Human verification (deferred to end of run)"):
- On device, at a store with a full small bag (4 gear items), tap "Set of lockpicks": the toast reads "Bag full (4/4) — drop something to make room.", the purse total does not change, and the lockpicks row is not marked sold.
- Carry an Acuteness potion plus 4 gear items in a small bag: the GEAR tab readout shows "4 / 4" (the potion is not counted) — visible only after Plan 03 lands the readout.
</human_verification>

<output>
Create `.planning/phases/29-end-of-combat-loot-bag-cap/29-01-SUMMARY.md` when done (include the deferred human-verification block above).
</output>
