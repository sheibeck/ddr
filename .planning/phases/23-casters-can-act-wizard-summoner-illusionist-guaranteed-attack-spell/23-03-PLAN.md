---
phase: 23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - engine/combat.js
  - src/browser/eventNarration.js
  - content/flavor.js
  - test/unit/combat.test.js
  - test/unit/casters-can-act.test.js
autonomous: true
requirements:
  - IDENT-01
  - IDENT-03
  - IDENT-04
tags: [engine, combat, wizard-rule, strikeRefused, narration, flavor, summon, phantom-host, casters-can-act]

must_haves:
  truths:
    - "A Wizard with an EMPTY castable-attack set (no attack-kind spell in the grimoire, or every one level/school-locked, or spellsUsed == maxCharges) strikes normally — no strikeRefused event; unit tests cover: utility-only grimoire, attack spells all spent for the day, zero charges."
    - "'Attack spell' has ONE definition: the exported attack-kind set in engine/derived.js (kinds status/thrown/stun/weaken at any level, i.e. Doze/Freeze/Stun/Weaken plus higher-level thrown spells); castableAttackSpells(state) filters the grimoire by that set AND canCast. Spell identity is the exact SPELLS[].n string; the strikeRefused event's `spell` field carries that exact name."
    - "FLAGGED PLANNER ASSUMPTION (spec-less probe, IDENT-04 unresolved): Phantom Host at level 1 for Illusionists, not doubled, d20 strike die kept — tests in test/unit/casters-can-act.test.js: an Illusionist casts Phantom Host at level 1 and gets a lvl-1 ally with d4+2 rounds (not doubled, no d8 backfire draw); a non-Illusionist Magic User at level 1 is still refused (spellAboveLevel, need 3); strikeDie(Illusionist, level 1..2) === 20 unchanged and level 3 !== 20."
    - "A level-1 Summoner casts Summon (IDENT-03) with the UNCHANGED formula: ally lvl = min(5, level + 1) = 2, rounds = 2*d4 + 2, one-in-eight backfire (lvl^2 + d6 self-damage) kept as the bad; outside combat the ally is queued on c.pendingAlly (allyPending) — asserted by tests in this plan."
  artifacts:
    - path: "engine/combat.js"
      provides: "playerStrike: Wizard refuses only while charges remain AND castableAttackSpells(state) is non-empty; strikeRefused gains spell: <SPELLS[].n> (IDENT-01)"
      contains: "castableAttackSpells(state)"
    - path: "src/browser/eventNarration.js"
      provides: "strikeRefused narration names e.spell when present; falls back to the old line when absent (coverage test calls builders with {type} only)"
      contains: "e.spell"
    - path: "content/flavor.js"
      provides: "Wizard, Summoner, Illusionist SUB_NOTE strings updated to the landed rules; voice-safe"
      contains: "SUB_NOTE"
    - path: "test/unit/casters-can-act.test.js"
      provides: "Wizard rule tests (utility-only book, attacks spent, zero charges, refusal names the spell, refusal still fires with a castable attack); Summoner L1 Summon; Illusionist L1 Phantom Host not doubled; non-Illusionist refused; strikeDie unchanged; SUB_NOTE sanity"
      contains: "strikeRefused"
    - path: "test/unit/combat.test.js"
      provides: "the existing Wizard-refusal test updated: a castable attack spell in the grimoire, expected event carries spell"
      contains: "spell: \"Freeze\""
  key_links:
    - from: "engine/combat.js#playerStrike"
      to: "engine/derived.js#castableAttackSpells"
      via: "the refusal condition's second clause; [0].n is the named spell"
      pattern: "castableAttackSpells\\(state\\)"
    - from: "src/browser/eventNarration.js#strikeRefused"
      to: "engine/combat.js strikeRefused event"
      via: "reads e.reason and e.spell"
      pattern: "e\\.spell"
    - from: "engine/magic.js#castSpell summon branch"
      to: "engine/derived.js#canCast (override-aware since 23-01)"
      via: "a level-1 Summoner/Illusionist now passes the castability gate; the summon branch itself is unchanged"
      pattern: "doubled = c\\.sub === \"Summoner\""
  prohibitions:
    - "The Illusionist's d20 strike die until level 3 is NOT softened — engine/derived.js#strikeDie is not edited by this plan."
    - "Phantom Host allies are NOT doubled — only `c.sub === \"Summoner\"` doubles; engine/magic.js's summon branch is not edited by this plan (magic.js is Plan 04's file)."
    - "Summon's strength/rounds/backfire formula and Mirror Self are unchanged."
    - "Flavor edits are limited to the three SUB_NOTE strings Wizard, Summoner, Illusionist in content/flavor.js — no other flavor key changes; the safety scan stays green."
    - "No new event TYPE is introduced (strikeRefused only gains a field), so no new EVENT_NARRATION key is needed; the coverage guard must stay green."
    - "No tools/ bot-policy edits (the bot already reacts to strikeRefused via ctx.strikeBlocked)."
    - "test/parity/prototype-master.js.txt is NEVER edited; no fixture is touched by this plan (no parity fixture drives a Wizard melee strike)."
    - "The attack-kind definition is NOT re-declared in combat.js — it imports castableAttackSpells from derived.js (one definition)."
---

<objective>
Land IDENT-01 — a Wizard refuses to strike ONLY while a spell charge remains AND an attack-kind spell is castable right now (known, level-legal, school-legal, override-aware) — and make the refusal name the spell to cast instead (`strikeRefused { reason: "wizard", spell }`), with the `EVENT_NARRATION` line using the name (voice-safe). Lock IDENT-03/IDENT-04's cast-level behavior with tests against Plan 01's override (a level-1 Summoner summons with the unchanged doubled formula; a level-1 Illusionist raises an UNdoubled Phantom Host; a non-Illusionist at level 1 is still refused; the d20 strike die stays). Update the three `SUB_NOTE` blurbs these rules make false (Wizard, Summoner, Illusionist) in the game's deadpan, family-friendly voice.

No fixture is affected: no parity fixture drives a Wizard melee strike (combat fixtures are Fighter/Thief seeds; the magic fixture's Wizard, seed 7, casts Heal and reads a scroll), and no fixture casts Summon/Phantom Host. `npm test` stays fully green at the end of this plan.

Purpose: a Wizard is never helpless — the staff is always available when the book cannot hurt anything; the refusal, when it fires, tells the player exactly what to cast (Phase 25 will toast it).
Output: `engine/combat.js` edit, `src/browser/eventNarration.js` edit, `content/flavor.js` three strings, `test/unit/casters-can-act.test.js`, `test/unit/combat.test.js` update; one or two commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-01-SUMMARY.md

@engine/combat.js
@engine/derived.js
@engine/magic.js
@engine/movement.js
@src/browser/eventNarration.js
@content/flavor.js
@test/unit/combat.test.js
@test/unit/magic.test.js
@test/unit/formatEventsCoverage.test.js
@test/voice/safety-scan.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `test/unit/chargen-rng-pin.test.js` — 20-seed rng cursors + per-sub rollGrimoire draw pins (pre-change). [23-01]
- `content/spell-level-overrides.js` — `SPELL_LEVEL_OVERRIDES`; barrel export. [23-01]
- `engine/derived.js` — `spellLevelFor`, `ATTACK_SPELL_KINDS`, `isAttackSpell`, `castableAttackSpells`; `canCast` via `spellLevelFor`. [23-01]
- `test/unit/spell-level-overrides.test.js`. [23-01]
- `engine/character.js#rollGrimoire` — zero-draw attack top-up, Summoner exempt; override-aware `usableNow`. [23-02]
- `test/unit/guaranteed-attack-spell.test.js`. [23-02]
- `test/parity/fixtures/action-script.chargen.json` — `divergences` block (seeds 15, 24). [23-02]
- `test/parity/harness/comparables.js` — `chargenDivergenceFor`, `stripDeclaredFields` [23-02]; `stripScenarioDivergence` [23-04].
- `test/parity/chargen-parity.test.js`, `test/parity/full-suite.test.js` (chargen sub-test). [23-02]
- `test/parity/fixtures/action-script.schema.md` — `divergences` / `divergence`. [23-02]
- `engine/combat.js#playerStrike` — Wizard rule; `strikeRefused.spell`. [23-03]
- `src/browser/eventNarration.js` — `strikeRefused` names the spell. [23-03]
- `content/flavor.js` — Wizard/Summoner/Illusionist `SUB_NOTE`. [23-03]
- `test/unit/casters-can-act.test.js`; `test/unit/combat.test.js` update. [23-03]
- `engine/magic.js` — Freeze via `killFoe`; `spellAboveLevel` via `spellLevelFor`. [23-04]
- `test/unit/freeze-pays-out.test.js`. [23-04]
- `test/parity/fixtures/action-script.magic.json` — `cast-damage` `divergence` record. [23-04]
- `test/parity/magic-parity.test.js`, `test/parity/full-suite.test.js` (magic sub-test). [23-04]
- `test/parity/FIXTURE-INVENTORY.md` — "Phase 23 caster divergences" section. [23-04]
- `docs/CLASS-PASS.md` — "Phase 23 note" under Outliers / Findings. [23-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wizard melee rule in playerStrike + strikeRefused.spell + narration</name>
  <files>engine/combat.js, src/browser/eventNarration.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/combat.js lines 323-345 (`playerStrike` head: the current Wizard refusal `c.sub === "Wizard" && maxCharges(c) - c.spellsUsed > 0`) and line 13 (the derived.js import list to extend)
    - engine/derived.js (Plan 01: `castableAttackSpells(state)` returns SPELLS-ordered spell objects, ignores charges)
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md section "Wizard melee rule (IDENT-01)" (refusal condition, event payload, rule location)
    - src/browser/eventNarration.js lines 1-30 (header: builders must tolerate a bare `{type}` payload) and lines 147-150 (the current `strikeRefused` builder)
    - test/unit/combat.test.js lines 47-115 (fakeRng/fixedFighter/fixedState/fixedFoe/fixedCombat helpers; `fixedFighter` defaults `grimoire: []`) and lines 304-309 (the existing Wizard refusal test — it MUST change: under the new rule an empty grimoire strikes)
    - test/unit/formatEventsCoverage.test.js lines 85-110 (how the vocabulary is derived — keep the literal `type: "strikeRefused"` at the push site)
  </read_first>
  <action>
    1. engine/combat.js: add `castableAttackSpells` to the `./derived.js` import. Replace the Wizard refusal so it reads: if `c.sub === "Wizard"` and `maxCharges(c) - c.spellsUsed > 0`, compute `const castable = castableAttackSpells(state)`; if `castable.length` is non-zero, push `{ type: "strikeRefused", reason: "wizard", spell: castable[0].n }` and return; otherwise fall through to the normal strike. Use the name `castable` (not `attacks` — that identifier is declared later in the same function). Add a `// DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, IDENT-01):` comment naming the prototype behavior (line 2331-2408 port: a Wizard refused to melee while ANY charge remained, even holding only Heal/Shield — the "cannot act" state Phase 22's ledger recorded) and the new rule (refuse only while an attack-kind spell is castable RIGHT NOW; the event names it so the UI can say what to cast). Update the playerStrike JSDoc's first clause accordingly. `reason: "wizard"` is kept so the bot's `ctx.strikeBlocked` observer and existing tests keep working.
    2. src/browser/eventNarration.js `strikeRefused`: when `e.reason === "wizard"` and `e.spell` is a non-empty string, render a `<span class="miss">` line that names the spell in the deadpan voice — target text: `A Wizard does not stoop to fisticuffs while ${e.spell} is still in the book.`; when `e.reason === "wizard"` without a spell keep the existing line verbatim; otherwise keep `You hold back.` Must not throw on `{ type: "strikeRefused" }` alone (coverage test).
    3. test/unit/combat.test.js: update the test "playerStrike: a Wizard refuses to melee while a spell charge remains" to give the Wizard `grimoire: ["Freeze"], level: 1` and expect `deepStrictEqual(events, [{ type: "strikeRefused", reason: "wizard", spell: "Freeze" }])`; rename it to say "...while an attack spell is castable".
    4. Run `node --test test/unit/combat.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/tuning-bot.test.js` — all green.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/tuning-bot.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `# fail 0`.
    - `grep -c "castableAttackSpells(state)" engine/combat.js` prints `1` and `grep -c "spell: castable\[0\].n" engine/combat.js` prints `1`.
    - `grep -c "DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, IDENT-01)" engine/combat.js` prints `1`.
    - `grep -c 'type: "strikeRefused", reason: "wizard"' engine/combat.js` prints `1` (literal type kept for the coverage guard).
    - `grep -c "e.spell" src/browser/eventNarration.js` prints >= 1.
    - `node -e "import('./src/browser/eventNarration.js').then(m=>{const f=m.EVENT_NARRATION.strikeRefused;console.log(f({type:'strikeRefused'}).length>0, f({type:'strikeRefused',reason:'wizard',spell:'Freeze'}).includes('Freeze'), f({type:'strikeRefused',reason:'wizard'}).includes('fisticuffs'))})"` prints `true true true`.
    - `grep -c 'spell: "Freeze"' test/unit/combat.test.js` prints >= 1.
  </acceptance_criteria>
  <done>A Wizard refuses only when a castable attack spell exists and charges remain; the event names that spell; the narration reads it; the coverage guard, voice scan and bot tests are green.</done>
</task>

<task type="auto">
  <name>Task 2: Tests — Wizard fights when the book cannot hurt; Summoner and Illusionist cast at level 1; the bads stay</name>
  <files>test/unit/casters-can-act.test.js</files>
  <read_first>
    - test/unit/combat.test.js lines 47-115 (helpers to copy: fakeRng with `pick` default, fixedFighter, fixedFloor, fixedState, fixedFoe, fixedCombat) and lines 310-325 (the hit/kill rng sequence example)
    - test/unit/magic.test.js lines 41-100 (fixedWizard/fixedFloor at full GW x GH, `SPELL_IDX` lookup) and lines 319-325 (existing Summon pendingAlly test)
    - engine/magic.js lines 109-133 (summon branch: `doubled`, `lvl`, d8 backfire only when doubled, `rounds = (doubled ? 2 : 1) * d4 + 2`, `rng.pick` name) and lines 56-70 (the refusal diagnostics spellNotKnown/spellAboveLevel/spellSchoolLocked)
    - engine/derived.js#strikeDie (lines 250-256) and content/misc-tables.js STRIKE_DICE [20, 12, 10, 8, 6]
    - engine/movement.js line 104 (`maxCharges = 2*level + 2 + eff(charges)` — at level 1 with no items: 4)
  </read_first>
  <action>
    Create `test/unit/casters-can-act.test.js` (copy the small helper set from combat.test.js/magic.test.js; import `playerStrike` from engine/combat.js, `castSpell` from engine/magic.js, `strikeDie`, `canCast`, `castableAttackSpells` from engine/derived.js, `maxCharges` from engine/movement.js, `SPELLS`, `STRIKE_DICE`, `SUB_NOTE` from content/index.js). Tests:

    IDENT-01 (Wizard):
    1. Utility-only book: Wizard L1, grimoire ["Heal","Shield","Detect Magic"], spellsUsed 0, one live foe — `playerStrike` produces NO `strikeRefused` and DOES produce a strike outcome (`struck` or `strikeMissed`/`foeArmorSoaked` — assert `events.some(e => e.type !== "strikeRefused")` and `!events.some(e => e.type === "strikeRefused")`), giving fakeRng enough values for one swing plus a possible kill.
    2. Attack spells all spent: Wizard L1, grimoire ["Freeze","Doze"], `spellsUsed = maxCharges(c)` — no refusal, a swing happens.
    3. Zero charges by items: same book, `spellsUsed = 4`, no items — no refusal (same as 2 but phrased from the probe's "zero charges" angle; keep both).
    4. Refusal names the spell: Wizard L1, grimoire ["Heal","Doze","Freeze"], spellsUsed 0 — events deepStrictEquals `[{ type: "strikeRefused", reason: "wizard", spell: "Doze" }]` (SPELLS order: Doze precedes Freeze); `fakeRng([])` proves zero draws.
    5. Level-locked attack does not block: Wizard L1 with grimoire ["Fireball"] (lvl 3) — no refusal, strikes. School-locked attack does not block: a Summoner is not a Wizard, so instead assert directly that `castableAttackSpells` is empty for a Summoner L1 with ["Stun"] and that a Wizard L1 with ["Stun"] is refused with spell "Stun".
    6. Non-Wizard casters never refuse: Sorcerer L1 with ["Freeze"], charges left — no `strikeRefused`.

    IDENT-03 (Summoner at level 1, formula unchanged):
    7. In combat: Summoner L1, grimoire ["Summon"], `fakeRng([2, 3])` (d8=2 no backfire, d4=3) — events include `allySummoned` with `lvl: 2` and `rounds: 8` (2*3+2), `C.ally.lvl === 2`, `spellsUsed === 1`.
    8. Out of combat: same character, `combat: null`, `fakeRng([5, 1])` — `allyPending` with lvl 2, rounds 4; `c.pendingAlly` set.
    9. Backfire kept: `fakeRng([1, 4])` (d8=1 -> backfire, d6=4) — `summonBackfired` with amount `2*2 + 4 = 8`, wp reduced by 8, no ally.
    10. Before the override a level-1 Summoner was refused: assert a level-1 WIZARD with ["Summon"] still gets `spellAboveLevel` (need 2, have 1) and `spellsUsed === 0` — proving the override is Summoner-only.

    IDENT-04 (Illusionist at level 1 — FLAGGED PLANNER ASSUMPTION tests):
    11. Illusionist L1, grimoire ["Phantom Host"], in combat, `fakeRng([3])` — `allySummoned` with `lvl: 1` (min(5, level), NOT doubled) and `rounds: 5` (1*3+2); no `summonBackfired`; the single fakeRng value being consumed by the d4 proves no d8 backfire draw happened (assert the events contain exactly one ally event and `C.ally.rounds === 5`).
    12. Non-Illusionist at level 1 (Wizard with ["Phantom Host"]) — `spellAboveLevel` with `need: 3, have: 1`, `spellsUsed === 0`.
    13. strikeDie: Illusionist Human level 1 and level 2 -> `STRIKE_DICE[0]` (20); level 3 -> `STRIKE_DICE[2]` (10); a Wizard Human level 1 -> `STRIKE_DICE[0]` too (the Illusionist bad is the level-2 stall).
    14. Mirror Self unchanged: Illusionist L1 with ["Mirror Self"], `fakeRng([4])` — the `mirror` kind still sets `c.mirror` (assert `c.mirror > 0` and a `mirrored`-family event exists — read magic.js for the exact event type before asserting).

    Voice:
    15. `SUB_NOTE.Wizard` mentions the staff/hand AND the phrase "attack spell"; `SUB_NOTE.Summoner` mentions "first day" or "day one"; `SUB_NOTE.Illusionist` mentions "Phantom Host" and "d20" (these guard Task 3's edits; write them to match the final strings).

    Run the file; then `node --test test/unit test/determinism`.
  </action>
  <verify>
    <automated>node --test test/unit/casters-can-act.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/casters-can-act.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `# pass` >= 14 (test 15 may fail until Task 3 lands — run the file again after Task 3 and it must be >= 15 pass).
    - `grep -c "spellAboveLevel" test/unit/casters-can-act.test.js` prints >= 2.
    - `grep -c "rounds: 8\|rounds === 8" test/unit/casters-can-act.test.js` prints >= 1 and `grep -c "lvl: 1\|lvl === 1" test/unit/casters-can-act.test.js` prints >= 1.
    - `grep -c "STRIKE_DICE\[0\]" test/unit/casters-can-act.test.js` prints >= 2.
    - `grep -c 'spell: "Doze"' test/unit/casters-can-act.test.js` prints >= 1.
  </acceptance_criteria>
  <done>Every IDENT-01 branch (fights with utility-only book / after spending attacks / at zero charges; refusal names the spell) and the IDENT-03/IDENT-04 level-1 casts with intact bads (doubling only for Summoner, backfire kept, d20 kept, Mirror Self unchanged) are locked by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Flavor — the three SUB_NOTE blurbs these rules make false</name>
  <files>content/flavor.js</files>
  <read_first>
    - content/flavor.js lines 39-66 (`SUB_NOTE`: the current Wizard, Illusionist, Summoner strings and the surrounding voice — deadpan, sarcastic, family-friendly, "the book" as narrator)
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md "Flavor now, not later" and "Specific Ideas" (keep the bads visible)
    - content/safety-wordlist.js (BANNED corpus: profanity/slurs/sexual/gore — the scan is whole-word)
    - test/voice/safety-scan.test.js header (flavor.js is scanned)
    - test/unit/casters-can-act.test.js test 15 (the phrases it expects)
  </read_first>
  <action>
    Replace exactly three values in `SUB_NOTE` (Wizard, Illusionist, Summoner) so each states the landed rule while keeping its bad visible, in the existing voice (two to three sentences, no profanity/gore, no exclamation marks). Target texts (adjust wording freely but keep every fact and the phrases test 15 checks):
    - Wizard: "Every school of magic, and a flat refusal to teach anybody who isn't an Apprentice. You will not raise a hand while an attack spell is left in the book; once the book cannot hurt anything, the staff will do. Two wizards in a party fight each other; there is only one of you, which helps."
    - Illusionist: "You choose where the teleport squares put you, which in a dungeon is very close to owning the floor. A Phantom Host from day one, three illusions, and a d20 to strike until level three — let the host do the hitting."
    - Summoner: "You can call something up from your very first day. Everything you call arrives twice as strong and twice as long-lived, and one time in eight it arrives on the wrong side. The book declines to say whose fault that is."
    Change nothing else in the file. Run `node --test test/voice/safety-scan.test.js test/unit/casters-can-act.test.js test/unit/content-tables.test.js test/determinism/content-is-pure-data.test.js`, then `npm test`. Commit: `feat(23-03): Wizard fights when the book cannot hurt; refusal names the spell; L1 Summon/Phantom Host tests; flavor`.
  </action>
  <verify>
    <automated>node --test test/voice/safety-scan.test.js test/unit/casters-can-act.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `# fail 0`.
    - `node -e "import('./content/index.js').then(m=>{const s=m.SUB_NOTE;console.log(/attack spell/.test(s.Wizard), /staff/.test(s.Wizard), /Phantom Host/.test(s.Illusionist), /d20/.test(s.Illusionist), /(first day|day one)/.test(s.Summoner), /one time in eight/.test(s.Summoner))})"` prints `true true true true true true`.
    - `git diff --numstat HEAD -- content/flavor.js` shows exactly `3	3	content/flavor.js` (three lines replaced, nothing added or removed elsewhere) — or, if committed already, `git show --numstat --format= HEAD -- content/flavor.js` shows `3	3`.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# fail 0`.
  </acceptance_criteria>
  <done>The Wizard, Illusionist and Summoner blurbs describe the rules as they now work, the bads are still stated, and the voice scan is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine event -> browser narration | `strikeRefused.spell` is a SPELLS[].n string produced by the engine, rendered by eventNarration into an HTML span (same path every other event name already takes; no user-controlled text). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-05 | Tampering | src/browser/eventNarration.js#strikeRefused (template interpolation) | low | accept | `e.spell` originates from the compiled SPELLS table (engine-side constant strings), never from a save or user input; identical exposure to the existing `e.target`/`e.name` interpolations |
| T-23-06 | Denial of Service | engine/combat.js#playerStrike refusal loop (bot/UI re-issuing a refused strike) | low | accept | Refusal is now bounded: it fires only while a castable attack exists; the bot already breaks the loop via ctx.strikeBlocked (Phase 22) |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/combat.test.js test/unit/casters-can-act.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/tuning-bot.test.js` green.
- `node --test test/parity` 30/30 green with no fixture or comparables change from this plan (`git diff --stat HEAD~1 -- test/parity | wc -l` prints 0 for this plan's commit).
- `npm test` green.
</verification>

<success_criteria>
- A Wizard with no castable attack spell fights with the staff; a Wizard with one is refused and the event carries `spell: <exact SPELLS[].n>`; the narration names it.
- A level-1 Summoner summons (doubled, backfire kept); a level-1 Illusionist raises an undoubled lvl-1 Phantom Host; a non-Illusionist is still refused; strikeDie is unchanged.
- The three SUB_NOTE strings match the landed rules and pass the voice scan.
</success_criteria>

<output>
Create `.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-03-SUMMARY.md` when done. Quote the final three SUB_NOTE strings and the exact `strikeRefused` event shape.
</output>
