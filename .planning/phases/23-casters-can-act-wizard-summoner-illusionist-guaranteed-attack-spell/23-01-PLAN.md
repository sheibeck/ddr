---
phase: 23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/chargen-rng-pin.test.js
  - content/spell-level-overrides.js
  - content/index.js
  - engine/derived.js
  - test/unit/spell-level-overrides.test.js
autonomous: true
requirements:
  - IDENT-01
  - IDENT-03
  - IDENT-04
  - FID-06
tags: [engine, derived, content, spell-level-override, attack-spell-set, rng-pin, casters-can-act]

must_haves:
  truths:
    - "'Attack spell' has ONE definition: the exported attack-kind set in engine/derived.js (kinds status/thrown/stun/weaken at any level, i.e. Doze/Freeze/Stun/Weaken plus higher-level thrown spells); castableAttackSpells(state) filters the grimoire by that set AND canCast. Spell identity is the exact SPELLS[].n string; the strikeRefused event's `spell` field carries that exact name."
    - "rollCharacter's rng-consumption order is unchanged for every class: the attack top-up adds no draws (uses the existing shuffled list), the override table is a pure read, and Freeze->killFoe draws only inside an already-successful cast; a test compares rng.getState() after rollCharacter for the 14 chargen seeds before vs after the change and asserts equality."
    - "FLAGGED PLANNER ASSUMPTION (spec-less probe, IDENT-03 unresolved): the override table applies ONLY to (Summoner, Summon) and (Illusionist, Phantom Host); canCast for every other (sub, spell) pair is byte-for-byte the old sp.lvl check — asserted by test/unit/spell-level-overrides.test.js walking all 8 Magic User subs x 32 spells x levels 1-5 against the pre-change predicate and finding exactly the three differing (sub, spell, level) triples Summoner/Summon/1, Illusionist/Phantom Host/1, Illusionist/Phantom Host/2."
  artifacts:
    - path: "content/spell-level-overrides.js"
      provides: "SPELL_LEVEL_OVERRIDES pure-data table { Summoner: { Summon: 1 }, Illusionist: { \"Phantom Host\": 1 } } (IDENT-03, IDENT-04)"
      contains: "export const SPELL_LEVEL_OVERRIDES"
    - path: "content/index.js"
      provides: "barrel re-export of the override table"
      contains: "spell-level-overrides.js"
    - path: "engine/derived.js"
      provides: "spellLevelFor(sub, sp), ATTACK_SPELL_KINDS, isAttackSpell(sp), castableAttackSpells(state); canCast's level check routed through spellLevelFor"
      contains: "export function castableAttackSpells"
    - path: "test/unit/chargen-rng-pin.test.js"
      provides: "post-rollCharacter rng-state pins for all 20 fixture seeds + per-sub rollGrimoire draw-count pins, measured on the pre-Phase-23 engine (FID-06 ordering proof)"
      contains: "rollGrimoire"
    - path: "test/unit/spell-level-overrides.test.js"
      provides: "override table shape, spellLevelFor cells, canCast diff walk (8 subs x 32 spells x levels 1-5), castableAttackSpells semantics, ATTACK_SPELL_KINDS membership"
      contains: "castableAttackSpells"
  key_links:
    - from: "engine/derived.js#canCast"
      to: "content/spell-level-overrides.js"
      via: "spellLevelFor(c.sub, sp) replaces the bare sp.lvl in canCast's level check"
      pattern: "spellLevelFor\\(c\\.sub, sp\\) > c\\.level"
    - from: "engine/derived.js#castableAttackSpells"
      to: "engine/derived.js#canCast"
      via: "filters SPELLS by isAttackSpell AND canCast(state, sp), in SPELLS array order"
      pattern: "isAttackSpell\\(sp\\) && canCast\\(state, sp\\)"
    - from: "content/index.js"
      to: "content/spell-level-overrides.js"
      via: "export * barrel line, so engine/derived.js imports SPELL_LEVEL_OVERRIDES from ../content/index.js like every other table"
      pattern: "export \\* from \"./spell-level-overrides.js\""
  prohibitions:
    - "test/parity/prototype-master.js.txt is NEVER edited."
    - "content/spells.js is not touched — no new spell, no edited lvl; the 32-entry count stays locked by test/unit/content-tables.test.js (Summon stays lvl 2 and Phantom Host lvl 3 for every sub except the two override cells)."
    - "No new serialized engine-state field is introduced, so no `*Comparable()` field carve-out in test/parity/harness/comparables.js is made by this plan."
    - "The attack-kind set is exactly the four kinds status, thrown, stun, weaken — no other kind (acid/volley/quake/death/blind/shrink/petrify/insane/stupid/vapor) is added to it (D: 'What counts as an attack spell')."
    - "The override table contains exactly two rows in this phase (Summoner/Summon, Illusionist/Phantom Host); Phase 24 may add rows, this phase does not."
    - "No tools/ bot-policy edits."
    - "The rng-pin test is created and committed green BEFORE any engine/ or content/ edit of this phase, and its pinned numbers are never edited afterwards in this phase."
---

<objective>
Lay the cycle-free foundation every other Phase 23 plan builds on: (1) pin the pre-change chargen rng order (FID-06) with a test written and committed BEFORE any engine edit; (2) add the pure-data per-sub-class spell-level override table (`content/spell-level-overrides.js`, exported via the barrel) and route `canCast`'s level check through ONE helper `spellLevelFor(sub, sp)` so a level-1 Summoner can cast Summon and a level-1 Illusionist can cast Phantom Host (IDENT-03, IDENT-04); (3) add the ONE engine-wide definition of "attack spell" — `ATTACK_SPELL_KINDS`, `isAttackSpell(sp)`, `castableAttackSpells(state)` in `engine/derived.js` — that Plan 02's grimoire top-up and Plan 03's Wizard rule both consume (IDENT-01/IDENT-02).

All of it is data + pure reads: no rng draw is added anywhere. After this plan `npm test` is fully green (989 baseline + the new tests) and parity is 30/30 with zero carve-outs, because no fixture ever attempts a level-1 Summon or Phantom Host and canCast is unchanged for every other cell.

Purpose: one definition of "effective spell level for this sub" and one definition of "attack spell" for the whole engine (CONTEXT: "One definition of 'attack spell' for the whole engine"; "Data-driven so Phase 24 can add rows without engine edits").
Output: `content/spell-level-overrides.js`, barrel export, `engine/derived.js` helpers, `test/unit/chargen-rng-pin.test.js`, `test/unit/spell-level-overrides.test.js`, three commits (pin test first).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md

@engine/derived.js
@engine/character.js
@content/spells.js
@content/mu-chart.js
@content/index.js
@test/determinism/forced-chargen.test.js
@test/unit/content-tables.test.js
@test/determinism/content-is-pure-data.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `test/unit/chargen-rng-pin.test.js` — post-`rollCharacter` `rng.getState()` pins for the 20 fixture seeds (1,2,3,4,6,7,8,13,14,15,17,19,24,29,32,35,38,160,256,303), `newRun(seed).rngState` pins for the same seeds, and per-sub `rollGrimoire` draw-count pins (Wizard 39, Warlock 33, Sorcerer 35, Summoner 31, Cleric 34, Illusionist 34, Court Mage 34, Apprentice 38) measured on the pre-Phase-23 engine. [23-01]
- `content/spell-level-overrides.js` — `SPELL_LEVEL_OVERRIDES` (pure data, two rows) + barrel export in `content/index.js`. [23-01]
- `engine/derived.js` — `spellLevelFor(sub, sp)`, `ATTACK_SPELL_KINDS` (Set of the four kinds), `isAttackSpell(sp)`, `castableAttackSpells(state)`; `canCast` level check via `spellLevelFor`. [23-01]
- `test/unit/spell-level-overrides.test.js`. [23-01]
- `engine/character.js#rollGrimoire` — `dayOnePool` (unchanged pool predicate that drives the `spare` shuffle) vs override-aware `usableNow` (drives `ready()`), plus the zero-draw attack top-up loop over the already-shuffled `spare` (Summoner exempt). [23-02]
- `test/unit/guaranteed-attack-spell.test.js`. [23-02]
- `test/parity/fixtures/action-script.chargen.json` — new top-level `divergences` block (seeds 15 and 24: `fields`, `before`, `after`, `rationale`); `seeds` array unchanged. [23-02]
- `test/parity/harness/comparables.js` — `chargenDivergenceFor(fixture, seed)`, `stripDeclaredFields(c, fields)` [23-02]; `stripScenarioDivergence(state, divergence)` [23-04].
- `test/parity/chargen-parity.test.js`, `test/parity/full-suite.test.js` (chargen sub-test) — seed-scoped before/after assertions + strip. [23-02]
- `test/parity/fixtures/action-script.schema.md` — documents `divergences` (seed-set fixtures) and per-scenario `divergence`. [23-02]
- `engine/combat.js#playerStrike` — Wizard refusal only while charges remain AND `castableAttackSpells(state)` is non-empty; `strikeRefused` gains `spell: <SPELLS[].n>`. [23-03]
- `src/browser/eventNarration.js` — `strikeRefused` line names the spell when present. [23-03]
- `content/flavor.js` — Wizard, Summoner, Illusionist `SUB_NOTE` strings updated. [23-03]
- `test/unit/casters-can-act.test.js`; `test/unit/combat.test.js` (the existing Wizard-refusal test gains a castable attack spell and the `spell` field). [23-03]
- `engine/magic.js` — Freeze routes the kill through `killFoe` (after `frozenSolid`); `spellAboveLevel` diagnostic reports `spellLevelFor(c.sub, sp)`. [23-04]
- `test/unit/freeze-pays-out.test.js`. [23-04]
- `test/parity/fixtures/action-script.magic.json` — `cast-damage` scenario gains a `divergence` record (fields sp/gold/kills/rations, before/after, rationale). [23-04]
- `test/parity/magic-parity.test.js`, `test/parity/full-suite.test.js` (magic sub-test) — scenario-scoped strip + before/after assertions. [23-04]
- `test/parity/FIXTURE-INVENTORY.md` — "Phase 23 caster divergences" section (chargen seeds 15/24 + magic cast-damage before/after tables; byte-identical-elsewhere paragraph). [23-04]
- `docs/CLASS-PASS.md` — a short "Phase 23 note" under Outliers / Findings (smoke readout, explicitly NOT the AFTER matrix). [23-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pin the pre-change chargen rng order (FID-06) BEFORE touching the engine</name>
  <files>test/unit/chargen-rng-pin.test.js</files>
  <read_first>
    - test/determinism/forced-chargen.test.js lines 1-60 (the `countingRng` wrapper to copy verbatim — shuffle counts `max(0, arr.length - 1)` draws, matching engine/rng.js's Fisher-Yates loop)
    - engine/character.js lines 1-30 (the documented chargen draw order) and lines 160-190 (`rollGrimoire`: two shuffles, one d10, one shuffle of `spare`)
    - engine/rng.js `shuffle` (lines 63-72) — the draw count is `arr.length - 1`, which is WHY the `spare` list's length is load-bearing for Plan 02
    - test/parity/fixtures/action-script.chargen.json (the 14 chargen seeds) and test/parity/FIXTURE-INVENTORY.md's roster table (the other fixture seeds: 256, 14, 17, 303, 38, 160)
    - content/classes.js line 18 (the 8 Magic User subs, in order)
  </read_first>
  <action>
    Preflight: `git status --porcelain` is empty and `npm test` reports `# pass 989` / `# fail 0` on the untouched tree. This task MUST run and commit before Task 2 edits any engine/ or content/ file — the pins are only meaningful if measured on the pre-Phase-23 engine.

    Create `test/unit/chargen-rng-pin.test.js` (node:test + node:assert/strict, ESM, importing `makeRng` from engine/rng.js, `rollCharacter`/`rollGrimoire` from engine/character.js, `newRun` from engine/engine.js, `SPELLS`/`CLASSES` from content/index.js, `canLearn`/`schoolGate` from engine/derived.js). Header comment: what is pinned, that it was measured on the pre-Phase-23 engine at commit f344642 on 2026-09-14 BEFORE any Phase 23 engine edit, and that a change to any number below is a chargen rng-ORDER regression (FID-06) — never edit a pin to make it pass.

    Three tests:
    1. "rollCharacter leaves the rng at the pinned cursor for every parity-fixture seed": for each seed in the 20-seed union `[1,2,3,4,6,7,8,13,14,15,17,19,24,29,32,35,38,160,256,303]`, `const rng = makeRng(seed); rollCharacter(rng); assert.equal(rng.getState(), PIN[seed])`. Measure PIN by running exactly that on the untouched engine; the planner's measurement (must match yours, else stop and report): 1: -2023389403, 2: -759718062, 3: 1071847752, 4: -759718060, 6: 440012085, 7: -447588372, 8: -1015482844, 13: -2023389391, 14: -2023389390, 15: 2079754316, 17: 1071847766, 19: 816082980, 24: 2015813128, 29: 1447918660, 32: -2023389372, 35: 1447918666, 38: 1071847787, 160: 1071847909, 256: -759717808, 303: 1071848052.
    2. "newRun leaves the rng at the pinned cursor for every parity-fixture seed" (same seeds; pins `newRun(seed).rngState`; planner's measurement: 1: -1692776321, 2: 266671887, 3: 1466402031, 4: 266671889, 6: 202730694, 7: 514860380, 8: 74848302, 13: -365163772, 14: 2034296515, 15: 10907112, 17: 1466402045, 19: -1188823027, 24: -556987352, 29: -1820658687, 32: -1692776290, 35: 642742802, 38: -996999417, 160: -1564893768, 256: 2098237954, 303: -429104679).
    3. "rollGrimoire draws a constant, pinned number of rng values per sub-class, equal to the pool-derived formula": for each of the 8 Magic User subs and for 50 seeds (`i * 7919 + 1`), wrap `makeRng(seed)` in `countingRng` (copied from forced-chargen.test.js), call `rollGrimoire(counting, sub)`, and assert `counting.draws` equals BOTH the pinned constant (Wizard 39, Warlock 33, Sorcerer 35, Summoner 31, Cleric 34, Illusionist 34, Court Mage 34, Apprentice 38) AND the independently derived formula `max(0, low-1) + max(0, high-1) + 1 + max(0, spare-1)` where pool = SPELLS filtered by `canLearn(sub, sp)`, low = pool with lvl <= 2, high = pool with lvl > 2, spare = pool with lvl === 1 and `schoolGate(sub, sp.s) <= 1` (the d10 is the `+ 1`). Also assert the book has no duplicate names for every (sub, seed).

    Run `node --test test/unit/chargen-rng-pin.test.js` — must pass on the UNTOUCHED engine. Commit this file alone: `test(23-01): pin pre-Phase-23 chargen rng order for FID-06`.
  </action>
  <verify>
    <automated>node --test test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `grep -E "^# pass"` prints `# pass 3`.
    - At the moment the test first passes, `git diff --quiet HEAD -- engine content src; echo rc=$?` prints `rc=0` (engine untouched when the pins were taken).
    - `grep -c "2079754316" test/unit/chargen-rng-pin.test.js` prints a number >= 1 (seed 15's rollCharacter pin is present) and `grep -c "2015813128" test/unit/chargen-rng-pin.test.js` prints >= 1 (seed 24's).
    - `grep -c "countingRng" test/unit/chargen-rng-pin.test.js` prints >= 2.
    - `git log --oneline -1 -- test/unit/chargen-rng-pin.test.js` shows a commit that contains NO engine/ or content/ path: `git show --stat --format= HEAD -- engine content | wc -l` prints `0` for that commit.
  </acceptance_criteria>
  <done>The 20-seed rng cursors and the 8 per-sub rollGrimoire draw counts are pinned in a committed, green test taken on the untouched engine; Plans 02-04 must keep it green, which is the FID-06 "ordering" proof for the whole phase.</done>
</task>

<task type="auto">
  <name>Task 2: Override table + spellLevelFor + attack-spell set in the cycle-free leaf</name>
  <files>content/spell-level-overrides.js, content/index.js, engine/derived.js</files>
  <read_first>
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md sections "Summoner at level 1 (IDENT-03)", "Illusionist at level 1 (IDENT-04)", "Wizard melee rule (IDENT-01)" (rule location + "one definition of attack spell")
    - engine/derived.js lines 1-12 (imports) and lines 494-520 (schoolAllowed/schoolGate/schoolBonus/canLearn/canCast)
    - content/spells.js (Summon lvl 2 s special kind summon; Phantom Host lvl 3 s illusion kind summon; the four lvl-1 offense spells Doze/status, Freeze/thrown, Stun/stun, Weaken/weaken; higher thrown spells Ice/Fireball/Lightning/Mangle)
    - content/mu-chart.js (Summoner special allowed, no special gate; Illusionist illusion allowed, no illusion gate — so the ONLY thing blocking these two casts at level 1 is sp.lvl)
    - content/index.js (barrel style) and test/determinism/content-is-pure-data.test.js (every barrel export must be JSON-serializable data — a plain object of objects of numbers passes)
    - engine/derived.js's existing DELIBERATE RULES CHANGE comment style (e.g. the inDark block, lines 258-275)
  </read_first>
  <action>
    1. Create `content/spell-level-overrides.js` exporting `SPELL_LEVEL_OVERRIDES`, a plain object keyed by Magic User sub-class name whose values are `{ [spellName]: effectiveLevel }`: `Summoner: { Summon: 1 }` and `Illusionist: { "Phantom Host": 1 }` — exactly these two rows (D: IDENT-03, IDENT-04). Header comment (no code): `// DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, IDENT-03/IDENT-04):` the prototype casts Summon at spell level 2 and Phantom Host at spell level 3 for every sub, so a level-1 Summoner could not summon and a level-1 Illusionist could only stall (Mirror Self); this table lowers the EFFECTIVE level of a spell for ONE sub without editing content/spells.js (Summon stays lvl 2 and Phantom Host lvl 3 for everyone else); it is read by exactly one engine helper, `engine/derived.js#spellLevelFor`, which `canCast` and `rollGrimoire` consume; pure data so Phase 24 can add rows with no engine edit. Note the spell-name keys must equal `SPELLS[].n` exactly.
    2. In `content/index.js` add `export * from "./spell-level-overrides.js";` next to the spells/mu-chart lines.
    3. In `engine/derived.js`: extend the content import with `SPELLS` and `SPELL_LEVEL_OVERRIDES`. Below the school-gate helpers add, with JSDoc:
       - `export function spellLevelFor(sub, sp)` — returns `SPELL_LEVEL_OVERRIDES[sub]?.[sp.n]` when it is a number, else `sp.lvl`. Pure read, no rng.
       - `export const ATTACK_SPELL_KINDS = new Set(["status", "thrown", "stun", "weaken"])` — the ONE engine-wide definition of "attack spell" (D: "What counts as an attack spell": Doze/Freeze/Stun/Weaken at level 1; the same kinds at any level, i.e. Ice/Fireball/Lightning/Mangle too). Document explicitly that acid/volley/quake/death and the other disabling kinds are NOT in the set by decision — the Wizard rule and the grimoire top-up key off these four kinds only.
       - `export function isAttackSpell(sp)` — `ATTACK_SPELL_KINDS.has(sp.kind)`.
       - `export function castableAttackSpells(state)` — `SPELLS.filter((sp) => isAttackSpell(sp) && canCast(state, sp))`, returning spell OBJECTS in SPELLS array order (deterministic; the first element is what `strikeRefused.spell` names in Plan 03). It does NOT check charges — the charge test stays in the Wizard check (D: refuse only while `maxCharges(c) - c.spellsUsed > 0` AND this list is non-empty).
       - Change `canCast`'s level line to `if (spellLevelFor(c.sub, sp) > c.level) return false;` with a one-line DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, IDENT-03/IDENT-04) comment. Grimoire-membership and schoolGate checks are untouched.
    Keep derived.js a leaf: it may import only from ./dice.js and ../content/index.js (no engine/ imports). Run `node --test test/unit test/determinism` to confirm nothing regressed (content-is-pure-data, content-tables, engine-purity, chargen-rng-pin all green).
  </action>
  <verify>
    <automated>node --test test/unit/content-tables.test.js test/determinism/content-is-pure-data.test.js test/unit/chargen-rng-pin.test.js test/unit/engine-purity.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./content/index.js').then(m=>console.log(JSON.stringify(m.SPELL_LEVEL_OVERRIDES)))"` prints exactly `{"Summoner":{"Summon":1},"Illusionist":{"Phantom Host":1}}`.
    - `node -e "import('./engine/derived.js').then(m=>{console.log(typeof m.spellLevelFor, typeof m.castableAttackSpells, typeof m.isAttackSpell, m.ATTACK_SPELL_KINDS instanceof Set, [...m.ATTACK_SPELL_KINDS].sort().join(','))})"` prints `function function function true status,stun,thrown,weaken`.
    - `node -e "import('./engine/derived.js').then(async m=>{const {SPELLS}=await import('./content/index.js');const summon=SPELLS.find(s=>s.n==='Summon'),ph=SPELLS.find(s=>s.n==='Phantom Host');console.log(m.spellLevelFor('Summoner',summon),m.spellLevelFor('Wizard',summon),m.spellLevelFor('Illusionist',ph),m.spellLevelFor('Cleric',ph))})"` prints `1 2 1 3`.
    - `grep -c "spellLevelFor(c.sub, sp) > c.level" engine/derived.js` prints `1`.
    - `grep -c 'export \* from "./spell-level-overrides.js"' content/index.js` prints `1`.
    - `grep -c "DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, IDENT-03/IDENT-04)" content/spell-level-overrides.js engine/derived.js` prints `1` for EACH file.
    - `grep -E '^import .* from "\./(combat|magic|character|movement|state|engine)\.js"' engine/derived.js | wc -l` prints `0` (derived.js stays a leaf).
    - The verify command prints `# fail 0`.
  </acceptance_criteria>
  <done>The override table exists as pure data, is barrel-exported, and is read by exactly one helper; canCast honors it; the attack-spell set and castableAttackSpells exist in the leaf module for Plans 02 and 03 to import; all existing tests stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Tests — override cells, canCast diff walk, castableAttackSpells semantics</name>
  <files>test/unit/spell-level-overrides.test.js</files>
  <read_first>
    - test/unit/magic.test.js lines 41-80 (`fixedWizard`/`fixedState` helper shapes to mirror for a minimal state)
    - engine/derived.js (as edited in Task 2: canCast, spellLevelFor, castableAttackSpells, ATTACK_SPELL_KINDS)
    - content/mu-chart.js (gates: Summoner offense gate 3 — so a Summoner's Stun is in the grimoire but NOT castable at level 1)
    - content/classes.js line 18 (MU subs) and lines 31/44 (a Fighter sub and a Thief sub to include in the walk)
  </read_first>
  <action>
    Create `test/unit/spell-level-overrides.test.js` covering, as separate `test(...)` blocks:
    1. Table shape: `SPELL_LEVEL_OVERRIDES` has exactly the keys `Summoner` and `Illusionist`; every spell-name key exists in `SPELLS` (`SPELLS.some(sp => sp.n === key)`); every value is the integer 1.
    2. `spellLevelFor`: Summoner/Summon = 1, Illusionist/Phantom Host = 1, and for a Wizard, a Cleric and an Apprentice `spellLevelFor(sub, sp) === sp.lvl` for ALL 32 spells; for a Summoner and an Illusionist it equals `sp.lvl` for the other 31 spells.
    3. FLAGGED PLANNER ASSUMPTION (IDENT-03, spec-less probe unresolved) — canCast diff walk: define an inline `oldCanCast(state, sp)` reproducing the pre-change predicate exactly (grimoire includes sp.n, NOT `sp.lvl > c.level`, `c.level >= schoolGate(c.sub, sp.s)`). For each sub in the 8 Magic User subs plus `Knight` and `Pickpocket`, each level 1..5, with `grimoire` = all 32 spell names, and each of the 32 spells, collect every `[sub, sp.n, level]` where `canCast` and `oldCanCast` differ. Assert the collected list deepStrictEquals exactly `[["Summoner","Summon",1],["Illusionist","Phantom Host",1],["Illusionist","Phantom Host",2]]` (order: iterate subs in CLASSES order, then level ascending, then SPELLS order — or sort before comparing). Also assert with an EMPTY grimoire that canCast is false for every (sub, spell, level), including the two override cells (the override never bypasses grimoire membership).
    4. `castableAttackSpells` semantics: (a) Wizard level 1 with grimoire ["Heal","Freeze","Doze"] returns objects named exactly ["Doze","Freeze"] in that (SPELLS) order; (b) Wizard level 1 with ["Heal","Shield","Detect Magic"] returns []; (c) Summoner level 1 with ["Stun","Summon"] returns [] (offense gate 3) while `canCast(state, Summon)` is true; (d) Wizard level 4 with ["Lightning"] returns ["Lightning"] (thrown at any level counts); (e) Wizard level 4 with ["Fireballs"] returns [] (volley is deliberately not an attack kind); (f) the result ignores charges: Wizard level 1 with ["Freeze"] and `spellsUsed` = 99 still returns ["Freeze"].
    5. `ATTACK_SPELL_KINDS` is a Set of exactly {status, thrown, stun, weaken}; `isAttackSpell` is true for Doze/Freeze/Stun/Weaken/Ice/Fireball/Lightning/Mangle and false for Heal/Shield/Summon/Phantom Host/Acid/Fireballs/Earthquake/Death.
    Run the file, then the full suite.
  </action>
  <verify>
    <automated>node --test test/unit/spell-level-overrides.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/spell-level-overrides.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `# pass` is >= 5.
    - `grep -c "Phantom Host\",2\]\|\"Phantom Host\", 2\]" test/unit/spell-level-overrides.test.js` prints >= 1 (the level-2 Illusionist cell is in the expected diff list).
    - `grep -c "oldCanCast" test/unit/spell-level-overrides.test.js` prints >= 2.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# fail 0` and a pass count >= 997 (989 baseline + 3 pin tests + >= 5 here).
    - `node --test test/parity 2>&1 | grep -E "^# fail"` prints `# fail 0` (no carve-out was needed: no fixture attempts a level-1 Summon or Phantom Host).
  </acceptance_criteria>
  <done>The override is proven to touch exactly the two intended (sub, spell) pairs and nothing else; castableAttackSpells and the attack-kind set have locked, tested semantics; the whole suite including parity is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Fully offline single-player engine; this plan adds pure data and pure reads. No new input surface, no network, no storage change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01 | Tampering | content/spell-level-overrides.js (save-independent data) | low | accept | Table is compiled into the app bundle, never read from a save; a malformed row can only make a spell castable one level early — no crash path (spellLevelFor falls back to sp.lvl for non-number values) |
| T-23-02 | Denial of Service | engine/derived.js#castableAttackSpells | low | accept | O(32) filter over a constant table per call; called at most once per playerStrike |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase (zero runtime deps policy); any future install requires the RESEARCH.md legitimacy audit + blocking human checkpoint |
</threat_model>

<verification>
- `node --test test/unit/chargen-rng-pin.test.js` green on the untouched engine (Task 1) AND after Task 2/3 edits (proves the override table and helpers add no rng draw).
- `npm test` green (>= 997 pass, 0 fail); `node --test test/parity` 30/30 with no carve-out added by this plan.
- `git diff --stat f344642 -- test/parity/prototype-master.js.txt content/spells.js` prints nothing (frozen master and the 32-spell table untouched).
</verification>

<success_criteria>
- A level-1 Summoner with Summon in the grimoire has `canCast === true`; a level-1 Illusionist with Phantom Host has `canCast === true`; every other (sub, spell, level) cell is byte-for-byte the old predicate (diff-walk test).
- `castableAttackSpells(state)` and `ATTACK_SPELL_KINDS` exist in engine/derived.js with the locked four-kind definition and SPELLS-order results.
- The 20-seed rng pins and 8 per-sub rollGrimoire draw counts are committed BEFORE the first engine edit and remain green after it.
</success_criteria>

<output>
Create `.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-01-SUMMARY.md` when done. Record: the measured pin values (confirm they equal the plan's table), the exact diff-walk result, and the commit hashes (pin test first).
</output>
