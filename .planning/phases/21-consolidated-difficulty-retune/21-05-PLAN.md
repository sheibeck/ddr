---
phase: 21-consolidated-difficulty-retune
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
files_modified:
  - docs/DIFFICULTY-RETUNE.md
autonomous: true
requirements: [TUNE-04]
tags: [ledger, dr-checklist, human-signoff, phase-gate, debug-build, no-engine-change]

must_haves:
  truths:
    - "TUNE-04 (D-15): docs/DIFFICULTY-RETUNE.md ends with a DR checklist section prescribing exactly THREE on-device runs at depths 20, 35 and 50, each with a fill-in table for: session-length feel (start/end clock, floors cleared, 5–10 min verdict), one caster fight (bolt / drain / debuff legible? resist felt fair?), one parley attempt AND one parley failure (insulted aggro legible?), party affordability (a Joiner accepted? did feeding the party bite? — noting per D-20 that no hire cost exists), and 'did the run end for a reason I understood?'; plus per-run free notes and ONE overall verdict: pass / tune-again"
    - "TUNE-04 (D-13/D-22 how-to): the checklist tells the tester exactly how to reach a depth — install the debug build, start a normal run, HUD gear → Settings, hold the 'Version …' line ≈ 1.2 s, type the depth, Start — and what a dev run looks like (DEV chip, level-5 hero, 300 × depth purse, not buried, no best depth)"
    - "TUNE-04 (D-16): the checklist states the two outcomes — pass closes the milestone's deferred UAT (TUNE-04); tune-again = ONE /gsd-quick pass editing only engine/difficulty.js constants plus a ledger addendum, then a second DR round, no re-plan — and lists the 'flag if noticed' items the DR round may surface (18-06 canon-mode: Sterling halfDmg TTK, Djinni / Stalka pre-ability discount; 19: one-round-ahead bolt telegraph) as observations, not tasks"
    - "TUNE-04 (debug build note): the checklist carries the exact commands (npm run android:debug; adb install -r android/app/build/outputs/apk/debug/app-debug.apk; am force-stop + monkey relaunch) and a pointer to docs/RELEASING.md and STATE.md's device notes (wireless adb, signer mismatch → uninstall the Play build first)"
    - "Phase gate: npm test fully green (count recorded), parity 30/30, frozen files identical to 04eb229, npm run build:www succeeds and www/index.html carries the stamped version, both tuning tools still run (--seeds=3 --json), the yardstick still runs, and no 'Filled by plan' placeholder remains in the ledger"
    - "This plan changes NO engine, content, src, tool or test file — the retune is frozen at 21-04's last commit"
    - statement: "The human DR round — three dev runs at depths 20 / 35 / 50 played on the Pixel 7 against the retuned engine — is the phase's exit criterion (D-15); the user reports per-run notes and a pass / tune-again verdict. This cannot be automated and the phase's VERIFICATION must end human_needed on exactly this item."
      verification: backstop
  artifacts:
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "## DR checklist — TUNE-04 sign-off: how-to (build, install, dev start), the three run tables (20 / 35 / 50), flag-if-noticed list, verdict block, D-16 outcomes, ship-after-pass note"
      contains: "## DR checklist — TUNE-04 sign-off"
  key_links:
    - from: "docs/DIFFICULTY-RETUNE.md DR checklist"
      to: "the 21-03 dev toggle (Settings version line long-press → mw-dev-start-depth → Start)"
      via: "step-by-step how-to naming the visible labels ('Version …', 'Start at depth (dev)', 'Start') and the DEV chip"
      pattern: "Start at depth \\(dev\\)"
    - from: "docs/DIFFICULTY-RETUNE.md DR checklist"
      to: "docs/RELEASING.md + STATE.md device notes"
      via: "debug-build + adb install commands and the signer-mismatch warning"
      pattern: "npm run android:debug"
    - from: "the tester's verdict"
      to: "D-16 outcome procedure"
      via: "pass → TUNE-04 closes the deferred UAT; tune-again → one /gsd-quick constants pass + ledger addendum + second DR round"
      pattern: "tune-again"
  prohibitions:
    - requirement_id: TUNE-04
      category: safety
      statement: "MUST NOT modify engine/, content/, src/, mazeworld.html, tools/ or test/ in this plan — the retune under test is 21-04's last commit"
      status: flagged-unverified
      verification: "git diff --quiet $BASE -- engine content src mazeworld.html tools test exits 0 at the end of the plan (BASE = HEAD at the start of this plan = 21-04's final commit, recorded in the SUMMARY)"
    - requirement_id: TUNE-04
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt or any fixture; MUST NOT commit www/ build output"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures exits 0; git status --porcelain shows no www/ entry"
    - requirement_id: TUNE-04
      category: values
      statement: "MUST NOT mark TUNE-04 or the phase as verified/complete on the basis of the harness numbers — the checklist's verdict block is filled by the USER, never by the executor"
      status: flagged-unverified
      verification: "the verdict block in the ledger reads '(to be filled by the tester)' at the end of the plan; the SUMMARY states the phase ends human_needed"
---

<objective>
Package the sign-off (TUNE-04): write the DR checklist into `docs/DIFFICULTY-RETUNE.md` exactly as D-15 prescribes (three runs at depths 20 / 35 / 50, the per-run checks, per-run notes, one pass / tune-again verdict) with the how-to for the 21-03 dev toggle and the debug-build/adb steps, state the D-16 outcomes, and run the phase gate (full suite, parity, frozen files, build). The plan changes no code; the human DR round is the phase's deliberate `human_needed` exit.

Purpose: the harness numbers earned the right to ask for the DR round; this plan hands the user everything needed to play it and record a verdict, and proves the tree is green and frozen for it.

Output: the ledger's DR checklist section; a green phase gate; a debug APK on the device (or the exact path/command for the user); the SUMMARY stating the phase ends human_needed on the DR round.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md
@.planning/phases/21-consolidated-difficulty-retune/21-03-SUMMARY.md
@.planning/phases/21-consolidated-difficulty-retune/21-04-SUMMARY.md
@docs/DIFFICULTY-RETUNE.md
@docs/RELEASING.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| Shared bot module, tool flags, tuning-bot tests, ledger BEFORE half | tooling / ledger | 21-01 | landed |
| Curve fields + helpers + wiring; combat-scaling tests | engine | 21-02 | landed |
| Dev start-at-depth: engine option + `dev` flag, adapter gates, Settings toggle, DEV chip, build stamp | engine / client | 21-03 | landed |
| Retuned constants, conditional counterweights, ledger change table / AFTER / comparison | engine / ledger | 21-04 | landed |
| `## DR checklist — TUNE-04 sign-off` in docs/DIFFICULTY-RETUNE.md (how-to, three run tables, flag-if-noticed, verdict block, D-16 outcomes, ship-after-pass note) | ledger | **21-05** | D-13/D-15/D-16/D-22 |
| Phase gate evidence (suite count, parity, frozen files, build stamp, tool smoke) + debug APK handoff | gate | **21-05** | recorded in the SUMMARY |

## Checklist contract (transcribed from CONTEXT.md D-15 — every row must appear)

| Per run (depth 20, 35, 50) | What the tester records |
|---|---|
| Session-length feel | start / end clock, floors cleared, "did 5–10 minutes feel right?" |
| One caster fight | which caster; bolt / drain / debuff legible? resist (heroResisted / heroResistFailed line) felt fair? |
| One parley attempt + one failure | success line read right? failure → insulted aggro noticed? |
| Party affordability | a Joiner met / accepted? feeding the party bit (rations / wp)? — no hire cost exists (D-20) |
| Ended for a reason I understood | yes / no + the epitaph |
| Free notes | anything else |
| Overall (once) | **pass** / **tune-again** |

<tasks>

<task type="auto">
  <name>Task 1: Write the DR checklist section into docs/DIFFICULTY-RETUNE.md — how-to (build, install, dev start), three run tables, flag-if-noticed list, verdict block, D-16 outcomes, ship-after-pass note</name>
  <files>docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-13, D-15, D-16, D-20, D-22, "Specific Ideas" (the DR round is the exit criterion), Deferred Ideas (the flag-if-noticed items)
    - .planning/phases/21-consolidated-difficulty-retune/21-03-SUMMARY.md (the exact Settings labels, the long-press constant, the DEV chip, the banner text as landed)
    - .planning/phases/21-consolidated-difficulty-retune/21-04-SUMMARY.md (the final constants and the D-09 verdicts — the checklist's preface cites them)
    - docs/DIFFICULTY-RETUNE.md (whole file — replace the last placeholder heading; keep everything else byte-identical)
    - docs/RELEASING.md (the debug-APK sentence and the versionCode/Play notes) and .planning/STATE.md "Ground Truth" → Device (wireless adb rediscovery, install-then-force-stop-then-monkey, signer mismatch → uninstall the Play build first)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md and content/BESTIARY-REBALANCE.md (the "deferred to 21" canon-mode items — Sterling halfDmg TTK, Djinni / Stalka pre-ability discount — to list as flag-if-noticed) and .planning/phases/19-foe-abilities-spellcasting-symmetric-int-resistance/19-CONTEXT.md deferred list (one-round-ahead bolt telegraph)
  </read_first>
  <action>
    Replace the `## DR checklist — TUNE-04 sign-off (21-05)` placeholder (and its italic line) with a section titled exactly `## DR checklist — TUNE-04 sign-off` containing, in order:

    1. **Preface** (3–4 sentences): this round is the phase's exit criterion (D-15); the harness numbers above are a sanity floor only; the retune under test is the constant block recorded in the change table (cite the final FOE_CAP_MAX / FOE_POWER_MAX / ABILITY_THREAT_MAX values and any counterweight that landed); the tester is the user, on the Pixel 7.

    2. **`### Getting the build on the device`** — a numbered list: `npm run android:debug` (builds `android/app/build/outputs/apk/debug/app-debug.apk`; JDK/gradle notes live in docs/RELEASING.md and STATE.md); if a Play-installed build is on the phone, uninstall it first (different signer — Preferences data is lost, which is fine for a dev round); `adb devices` / `adb mdns services` to rediscover the wireless device; `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`; then `adb shell am force-stop com.darktierstudios.delvedierepeat` and a `monkey -p com.darktierstudios.delvedierepeat 1` relaunch (install alone does not reload the WebView).

    3. **`### Starting a run at depth N`** — a numbered list using the landed labels: start (or resume) a normal run; tap the HUD gear → Settings; press and HOLD the last row, "Version 1.0.1 (2)" (≈ 1.2 s — a tap does nothing); the "Start at depth (dev)" row appears; type 20 / 35 / 50; tap Start; the sheet closes, the log shows "Floor N." with the dev banner, the HUD shows a DEV chip; the hero is level 5 with a 300 × N wilmst purse; the run is never buried and never counts as a best depth (D-13). Note: each of the three runs is a fresh dev start (die or abandon between them).

    4. **`### Run 1 — depth 20`**, **`### Run 2 — depth 35`**, **`### Run 3 — depth 50`** — each an identical fill-in table with the seven rows from this plan's "Checklist contract" (Session-length feel; One caster fight; One parley attempt + one failure; Party affordability — with the D-20 note that no hire cost exists, so record whether a Joiner appeared / was accepted and whether feeding the party bit; Ended for a reason I understood; Free notes) with an empty "Tester's notes" column, plus one line under each table: "What to expect here:" citing the curve at that depth (foeCap / foePower / abilityThreat values from `difficultyCurve(20|35|50)` — compute them with a one-liner `node -e` and transcribe; and the caster-encounter rate the AFTER readout showed for that band).

    5. **`### Flag if noticed (observations, not tasks)`** — bullets: Sterling's halfDmg time-to-kill and the Djinni / Stalka Beast pre-ability discount in canon mode (18-06 deferred); bolts feeling unfair without a one-round-ahead telegraph (19 deferred); anything about summoned reinforcements (unscaled by design); any place the Oracle voice slipped.

    6. **`### Verdict`** — a two-line block: `**Overall verdict:** (to be filled by the tester) — pass / tune-again` and `**Per-run notes above complete:** (to be filled by the tester)`.

    7. **`### What happens next (D-16)`** — **pass**: TUNE-04 closes the milestone's deferred UAT; the phase's VERIFICATION flips from human_needed to passed on the user's word; per the standing rule, the assistant then ASKS whether to push a versionCode-bumped signed AAB to the Play internal-testing track (`npm run play:release`, docs/RELEASING.md) — never pushes unasked. **tune-again**: ONE `/gsd-quick` pass editing only `engine/difficulty.js` constants (+ a `### Addendum` under the change table with before/after/rationale), then a second DR round with this same checklist; no re-plan.

    Keep the rest of the file byte-identical (verify with `git diff --stat` — only additions in the tail).
  </action>
  <verify>
    <automated>grep -c '^## DR checklist — TUNE-04 sign-off$' docs/DIFFICULTY-RETUNE.md | grep -qx 1 && grep -c '^### Run 1 — depth 20\|^### Run 2 — depth 35\|^### Run 3 — depth 50' docs/DIFFICULTY-RETUNE.md | grep -qx 3 && grep -c 'Filled by plan 21-0' docs/DIFFICULTY-RETUNE.md | grep -qx 0 && grep -c 'to be filled by the tester' docs/DIFFICULTY-RETUNE.md | grep -qE '^[2-9]' && git diff --quiet HEAD -- engine content src mazeworld.html tools test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## DR checklist — TUNE-04 sign-off$' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c 'Filled by plan 21-0' docs/DIFFICULTY-RETUNE.md` prints 0
    - `grep -c '^### Getting the build on the device\|^### Starting a run at depth N\|^### Flag if noticed\|^### Verdict\|^### What happens next (D-16)' docs/DIFFICULTY-RETUNE.md` prints 5 and `grep -c '^### Run 1 — depth 20\|^### Run 2 — depth 35\|^### Run 3 — depth 50' docs/DIFFICULTY-RETUNE.md` prints 3
    - `grep -c 'npm run android:debug' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'adb install -r android/app/build/outputs/apk/debug/app-debug.apk' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'docs/RELEASING.md' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'am force-stop' docs/DIFFICULTY-RETUNE.md` prints >= 1
    - `grep -c 'Start at depth (dev)' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'DEV chip' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c '1.2 s\|1\.2 s' docs/DIFFICULTY-RETUNE.md` prints >= 1
    - each of the three run sections contains the strings "Session-length feel", "caster fight", "parley", "Party affordability", "reason I understood", "Free notes" (grep -c each prints >= 3) and "What to expect here:" (prints 3)
    - `grep -c 'Sterling' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'telegraph' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'no hire cost' docs/DIFFICULTY-RETUNE.md` prints >= 1
    - `grep -c 'to be filled by the tester' docs/DIFFICULTY-RETUNE.md` prints >= 2 and `grep -c 'tune-again' docs/DIFFICULTY-RETUNE.md` prints >= 3 and `grep -c '/gsd-quick' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'play:release' docs/DIFFICULTY-RETUNE.md` prints >= 1
    - `git diff --quiet HEAD -- engine content src mazeworld.html tools test` exits 0 and `git diff --stat HEAD -- docs/DIFFICULTY-RETUNE.md` shows insertions in the tail only (deletions == the placeholder heading + its one italic line)
  </acceptance_criteria>
  <done>The ledger ends with a complete, self-contained DR checklist: build/install steps, the dev-start how-to with the landed labels, three depth-specific run tables with the D-15 checks and expected curve values, the flag-if-noticed list, an empty verdict block, and the D-16 outcomes including the ship-after-pass ask.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full suite, parity, frozen files, build stamp, tool and yardstick smoke, debug APK handoff; record that the phase ends human_needed on the DR round</name>
  <files>docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-04-SUMMARY.md (the last engine commit hash — the base for the frozen-tree gate — and the final test count)
    - .planning/STATE.md "Ground Truth" → Build/env and Device (JDK pin, `tools/pin-jdk.mjs`, wireless adb, install/force-stop/monkey)
    - docs/RELEASING.md lines 1-40 (debug vs release; the AAB is a separate, user-asked step)
    - .planning/ROADMAP.md Phase 21 success criteria 1-4 (criterion 4 is this human round)
  </read_first>
  <action>
    0. Record `BASE=$(git rev-parse HEAD)` before any edit (this is 21-04's final commit — the engine under test).

    1. Run the gate and record every result in the SUMMARY: `npm test` (full, green, exact count); `node --test "test/parity/**/*.test.js"` (30 pass); `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js package.json package-lock.json` (exit 0); `npm run build:www` (exit 0; `grep -c 'id="mw-app-version">1.0.1 (2)</span>' www/index.html` prints 1 — or the current version.properties values); `node tools/tune-difficulty.mjs --seeds=3 --json` and `node tools/tune-economy.mjs --seeds=3 --json` (both parse as JSON — smoke only, numbers not compared); `node tools/bestiary-yardstick.mjs` (exit 0); `git status --porcelain` shows no `www/` entry.

    2. Debug build handoff (best effort, never a plan failure): run `npm run android:debug` (expect several minutes; run it in the background with an `EXIT=` sentinel and poll in bounded ≤ 120 s checks, exactly like the readouts). If it exits 0: note the APK path `android/app/build/outputs/apk/debug/app-debug.apk`; if `adb devices` lists a device, `adb install -r` it, `am force-stop` + `monkey` relaunch, and record the result; if no device is reachable, record the APK path and the install command for the user. If the build fails for an environment reason (JDK/gradle/SDK), record the exact error and the command for the user — the DR round can still proceed from a user-run build. Do NOT run `npm run play:release` or bump the version (that is the user-asked Play step, see the checklist's D-16 note).

    3. Append ONE line to the ledger's `### Verdict` block: `**Gate at hand-off:** npm test <count>/<count>, parity 30/30, frozen files identical to 04eb229, build stamped <version>, APK: <path | install result | build error> — <date>.` (the only ledger edit in this task).

    4. In the SUMMARY: state explicitly that ROADMAP Phase 21 criteria 1–3 are met by 21-02 / 21-01 / 21-04 and that criterion 4 (the human DR round) is OUTSTANDING by design — the phase's VERIFICATION must end `human_needed` on the DR checklist's verdict; list what the user does next (install, play 3 runs, fill the tables, report pass / tune-again) and the standing rule to ASK about a Play internal-testing push after a pass.
  </action>
  <verify>
    <automated>npm test && node --test "test/parity/**/*.test.js" && git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js package.json package-lock.json && npm run build:www && node tools/tune-difficulty.mjs --seeds=3 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>JSON.parse(s))" && node tools/tune-economy.mjs --seeds=3 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>JSON.parse(s))" && node tools/bestiary-yardstick.mjs > /dev/null && grep -c 'Gate at hand-off:' docs/DIFFICULTY-RETUNE.md | grep -qx 1 && test -z "$(git status --porcelain | grep '^?? www/')"</automated>
    <human-check>DR round (D-15): on the Pixel 7 debug build, play three dev runs at depths 20, 35 and 50 following docs/DIFFICULTY-RETUNE.md's "DR checklist — TUNE-04 sign-off"; fill each run's table (session feel, one caster fight, one parley attempt + one failure, party affordability, ended-for-a-reason, notes) and record ONE overall verdict: pass or tune-again. This is the phase's exit criterion and cannot be automated.</human-check>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with every test passing (count recorded in the SUMMARY and in the Gate line) and `node --test "test/parity/**/*.test.js"` reports 30 pass
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js package.json package-lock.json` exits 0
    - `npm run build:www` exits 0 and `grep -c 'id="mw-app-version">' www/index.html` prints 1 with the version.properties versionName inside it, and `git status --porcelain | grep -c '^?? www/'` prints 0
    - `node tools/tune-difficulty.mjs --seeds=3 --json` and `node tools/tune-economy.mjs --seeds=3 --json` both parse as JSON; `node tools/bestiary-yardstick.mjs` exits 0
    - `grep -c 'Gate at hand-off:' docs/DIFFICULTY-RETUNE.md` prints 1 and, before this task's commit, `git diff --stat HEAD -- docs/DIFFICULTY-RETUNE.md` shows exactly one inserted line
    - `git diff --quiet $BASE -- engine content src mazeworld.html tools test` exits 0 (BASE = `git rev-parse HEAD` captured before this plan's first edit — 21-04's final commit; no code change in this plan)
    - the SUMMARY records: the APK path and install result OR the exact build error; the sentence "the phase ends human_needed on the DR checklist verdict"; the list of what the user does next; the standing Play-internal-push ask
    - the `### Verdict` block's overall verdict still reads "(to be filled by the tester)" — the executor did not fill it
  </acceptance_criteria>
  <done>The tree is proven green and frozen (suite, parity, frozen files, build stamp, tool smoke), the debug APK is on the device or its exact path/command is handed over, the ledger carries the gate line, and the SUMMARY states plainly that the phase now waits on the human DR verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → human verdict | The verdict belongs to the user; the executor must not fill or infer it |
| debug build → device | A debug APK and a Play build have different signers; installing over the wrong one fails or wipes data — the checklist warns |
| gate → tree | The gate must run against exactly 21-04's final engine; no code changes are allowed here |
| ledger → future readers | The checklist must describe the landed UI labels, not planned ones |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-22 | Repudiation | Executor (or a later automation) marks TUNE-04 verified from harness numbers | high | mitigate | verdict block reads "(to be filled by the tester)"; must_haves backstop item; SUMMARY states human_needed; verify `<human-check>` |
| T-21-23 | Tampering | Gate run against a tree that differs from the retune under test | medium | mitigate | `git diff --quiet $BASE -- engine content src mazeworld.html tools test` exits 0 (BASE = 21-04's final commit); no code files in files_modified |
| T-21-24 | Information Disclosure | The dev toggle how-to in a committed doc "leaks" the hidden gesture | low | accept | the repo is public already; the toggle is a dev aid, not a cheat with any leaderboard value (offline, single-player, dev runs are never buried/recorded) |
| T-21-25 | Denial of Service | The gradle debug build stalls the executor or fails on JDK/SDK drift | medium | mitigate | background run with `EXIT=` sentinel + bounded polling; failure is recorded, not fatal; the user can build per docs/RELEASING.md |
| T-21-26 | Tampering | Play release pushed or version bumped without the user's ask | medium | mitigate | explicit prohibition; the checklist's D-16 note makes the push a user-asked step (standing rule) |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet 04eb229 -- package.json package-lock.json` |
</threat_model>

<verification>
- Ledger: `## DR checklist — TUNE-04 sign-off` with the build/install how-to, the dev-start how-to, three run tables (20 / 35 / 50) carrying every D-15 row, flag-if-noticed, an unfilled verdict, the D-16 outcomes, the gate line.
- Gate: `npm test` green (count), parity 30/30, frozen files identical to 04eb229, `npm run build:www` stamped, tools + yardstick smoke, no www/ in git status, no code diff since 21-04.
- Human (end-of-phase): the DR round verdict — the phase's VERIFICATION ends human_needed on exactly this.
</verification>

<success_criteria>
- ROADMAP Phase 21 criterion 4 is set up to be met by the user: a human can sign off via an on-device DR round at depth 20–50+ against caster foes with a ready build, a ready dev toggle, a ready checklist and a green, frozen tree; the harness numbers are recorded as a sanity floor, not the exit criterion.
</success_criteria>

<output>
Create `.planning/phases/21-consolidated-difficulty-retune/21-05-SUMMARY.md` when done (record: the gate results with the exact test count, the APK handoff outcome, the ledger sections added, the statement that the phase ends human_needed on the DR verdict, the user's next steps, and the standing Play-internal-push ask).
</output>
