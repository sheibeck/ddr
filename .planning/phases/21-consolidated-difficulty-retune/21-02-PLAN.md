---
phase: 21-consolidated-difficulty-retune
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - engine/difficulty.js
  - engine/combat.js
  - engine/foeAbilities.js
  - test/unit/combat-scaling.test.js
autonomous: true
requirements: [TUNE-01]
tags: [engine, difficulty-curve, combat, foe-abilities, parity-safe, identity-band, node-test]

must_haves:
  truths:
    - "TUNE-01 (D-01): difficultyCurve(depth) returns, alongside depth/breather/dots/darkBlobs/darkRadius, the combat-scaling fields foeCap, foeBonus, foeLvlBias, foePower, abilityThreat — computed only from depth, 0 rng, via the new non-rounding softCapFloat over `over = max(0, depth - 5)` so the identity band is structural, not a rounding accident"
    - "TUNE-01 (D-04/D-19, identity probe 1..5): for every depth 1..5 difficultyCurve(d) deep-equals exactly { depth: d, breather: false, dots: 9 + d, darkBlobs: d - 1, darkRadius: 3 + d, foeCap: 3, foeBonus: 0, foeLvlBias: 0, foePower: 1, abilityThreat: 1 } with Object.is(foePower, 1) and Object.is(abilityThreat, 1) (exact 1, never 0.999…)"
    - "TUNE-01 (cap-boundary probe): at depths 6, 10, 20, 30, 50, 100, 1000 every combat field stays inside [BASE, MAX] (foeCap ∈ [FOE_CAP_BASE, FOE_CAP_MAX], foePower ∈ [FOE_POWER_BASE, FOE_POWER_MAX], abilityThreat ∈ [ABILITY_THREAT_BASE, ABILITY_THREAT_MAX]), foeBonus === foeCap - FOE_CAP_BASE, and all three are non-decreasing across depths 1..200 with NO breather dip (combat knobs never soften on a breather floor — only dots/darkBlobs do)"
    - "TUNE-01 (this plan's constants): FOE_CAP_MAX === FOE_CAP_BASE === 3, FOE_POWER_MAX === FOE_POWER_BASE === 1, ABILITY_THREAT_MAX === ABILITY_THREAT_BASE === 1, FOE_LVL_BIAS === 0 — the whole curve is identity at EVERY depth until 21-04 sets the retune values, so this plan's engine diff is provably inert (the 21-01 BEFORE readout would reproduce byte-for-byte)"
    - "TUNE-01 (D-17, count probe): startCombat keeps the canon `wandering ? 1 : Math.min(cap, d4 <= 2 ? 1 : d4 <= 3 ? 2 : 3)` draw shape verbatim (the d4s are drawn only on the non-wandering branch, the level<=2 cap=2 rule intact) and then applies foeCountFor(canon, curve) = Math.min(curve.foeCap, canon + curve.foeBonus) — synthetic pins: foeCountFor(3, {foeCap:5, foeBonus:2}) = 5, foeCountFor(1, {foeCap:5, foeBonus:2}) = 3, foeCountFor(2, {foeCap:4, foeBonus:1}) = 3, foeCountFor(k, {foeCap:3, foeBonus:0}) = k for k = 1..3"
    - "TUNE-01 (D-02 copy-time scaling probe): each foe instance gets wp = maxWP = foeWpFor(picked.wp, curve) (=== picked.wp exactly when foePower === 1 — asserted for all 53 BESTIARY rows, whose wp are all integers) and a dmgBonus key ONLY when foeDmgBonusFor(lvl, curve) = round((foePower - 1) * lvl * lvl) > 0 — at depth 1..5 no foe object carries a dmgBonus key; synthetic pins: foeWpFor(10, {foePower:1.6}) = 16, foeDmgBonusFor(5, {foePower:1.6}) = 15, foeDmgBonusFor(1, {foePower:1.6}) = 1"
    - "TUNE-01 (damage seam probe): the three foe melee damage sites (foeTurn hero branch, foeTurn member branch, pursuitStrike) add `(f.dmgBonus || 0)` / `(pursuer.dmgBonus || 0)` as post-draw arithmetic — a foe with dmgBonus 5 and lvl 1 hitting with d6 = 4 deals 1 + 5 + 4 = 10, the same foe without the key deals 5; damageFoe stays the only foe-wp decrement seam (test/unit/foe-damage.test.js invariant green)"
    - "TUNE-01 (D-03/D-18, cadence probe): tickAbilityCooldowns(state, f) (arity 2, one call site in foeTurn passing state first) and firstReadyAbility/resolveFoeAbility read every/uses through abilityCadenceFor(a, curve) = { every: max(1, round(a.every / abilityThreat)), uses: max(1, round(a.uses * abilityThreat)) } — synthetic pins: {every:2, uses:4} at threat 2 → {every:1, uses:8}; {every:3} at threat 1.5 → every 2, uses undefined; {every:1} at threat 4 → every 1; every descriptor at threat 1 → its own numbers; content/foe-abilities.js is unchanged data"
    - "TUNE-01 (draw-shape equality, depth 1 vs 5 vs 30): with the same fakeRng sequence, countingRng draws for startCombat(forced Beasts, level-5 hero, no Tracking) are equal at depth 1 and depth 5 (8 draws: 2 count d4 + 2 × (d4 + pick) + 2 initiative) and at depth 30 equal 2 + foeCountFor(2, difficultyCurve(30)) × 2 + 2 — i.e. the curve read adds ZERO draws and any extra draws deep are exactly the extra foes' own canon draws"
    - "Parity after this plan: npm test fully green; node --test test/parity/**/*.test.js 30/30 with ZERO new carve-outs; test/determinism/foe-abilities.test.js (D-15 seeds, tiers 2/4/5) and test/unit/foe-turn-draw-count.test.js (FID-02 pins) pass with ZERO edits; prototype master + fixtures untouched"
  artifacts:
    - path: "engine/difficulty.js"
      provides: "COMBAT_SCALE_FROM_DEPTH, FOE_CAP_BASE/MAX/SOFT_K, FOE_POWER_BASE/MAX/SOFT_K, ABILITY_THREAT_BASE/MAX/SOFT_K, FOE_LVL_BIAS; softCapFloat; the five new curve fields; pure helpers foeCountFor, foeWpFor, foeDmgBonusFor, abilityCadenceFor"
      exports: ["COMBAT_SCALE_FROM_DEPTH", "FOE_CAP_BASE", "FOE_CAP_MAX", "FOE_CAP_SOFT_K", "FOE_POWER_BASE", "FOE_POWER_MAX", "FOE_POWER_SOFT_K", "ABILITY_THREAT_BASE", "ABILITY_THREAT_MAX", "ABILITY_THREAT_SOFT_K", "FOE_LVL_BIAS", "foeCountFor", "foeWpFor", "foeDmgBonusFor", "abilityCadenceFor"]
      contains: "export function foeCountFor(canonCount, curve)"
    - path: "engine/combat.js"
      provides: "startCombat reads difficultyCurve once (foeLvlBias in the maxLvl clamp, foeCountFor after the canon draw, foeWpFor + conditional dmgBonus per foe); three melee sites add the bonus; foeTurn calls tickAbilityCooldowns(state, f)"
      contains: "const n = wandering ? 1 : foeCountFor("
    - path: "engine/foeAbilities.js"
      provides: "tickAbilityCooldowns(state, f); abilityCadenceFor at the lazy-init, the readiness uses check, the cast-time cooldown reset and the uses decrement"
      contains: "export function tickAbilityCooldowns(state, f)"
    - path: "test/unit/combat-scaling.test.js"
      provides: "identity pins 1..5, cap/monotone bounds, synthetic-curve helper pins, per-foe copy assertions at depth 1/5/30, dmgBonus melee test, cadence tests, draw-shape equality"
      contains: "foeCountFor(2, difficultyCurve(30))"
  key_links:
    - from: "engine/combat.js startCombat"
      to: "engine/difficulty.js difficultyCurve + foeCountFor/foeWpFor/foeDmgBonusFor"
      via: "import { difficultyCurve, foeCountFor, foeWpFor, foeDmgBonusFor } from \"./difficulty.js\"; const curve = difficultyCurve(state.floor.depth); (one read per encounter, 0 draws)"
      pattern: "const curve = difficultyCurve\\(state\\.floor\\.depth\\);"
    - from: "engine/combat.js foeTurn"
      to: "engine/foeAbilities.js tickAbilityCooldowns(state, f)"
      via: "tickAbilityCooldowns(state, f); immediately before firstReadyAbility(state, f)"
      pattern: "tickAbilityCooldowns\\(state, f\\);"
    - from: "engine/foeAbilities.js (tick / ready / resolve)"
      to: "engine/difficulty.js abilityCadenceFor"
      via: "const cad = abilityCadenceFor(a, difficultyCurve(state.floor.depth)); cad.every / cad.uses at all four cadence sites"
      pattern: "abilityCadenceFor\\(a, "
    - from: "per-foe instance dmgBonus (startCombat)"
      to: "foeTurn hero/member swings and pursuitStrike"
      via: "f.lvl * f.lvl + (f.dmgBonus || 0) + (dice)"
      pattern: "\\(f\\.dmgBonus \\|\\| 0\\)|\\(pursuer\\.dmgBonus \\|\\| 0\\)"
  prohibitions:
    - requirement_id: TUNE-01
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any test/parity/fixtures/*.json, test/parity/harness/comparables.js, test/determinism/foe-abilities.test.js or test/unit/foe-turn-draw-count.test.js — zero new carve-outs for scaling (the only Phase 21 carve-out is 21-03's dev flag)"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js exits 0; git diff --quiet bd0ba7c -- test/parity/harness/comparables.js exits 0"
    - requirement_id: TUNE-01
      category: safety
      statement: "MUST NOT add any rng draw to startCombat, foeTurn, pursuitStrike or foeAbilities.js — every new term is arithmetic on already-drawn values or a pure curve read; the canon d4/d4 count ternary stays on the non-wandering branch exactly as today"
      status: flagged-unverified
      verification: "combat-scaling draw-shape test (depth 1 === depth 5 === 8 draws); FID-02 full-fight pins (12/101/111/66/32) unchanged; D-15 determinism seeds byte-identical"
    - requirement_id: TUNE-01
      category: values
      statement: "MUST NOT set any *_MAX constant above its *_BASE in this plan (identity everywhere) and MUST NOT touch content/bestiary.js, content/foe-abilities.js, engine/items.js, engine/encounters.js or any economy number — 21-04 owns the retune"
      status: flagged-unverified
      verification: "node -e import check: FOE_CAP_MAX===3 && FOE_POWER_MAX===1 && ABILITY_THREAT_MAX===1; git diff --quiet bd0ba7c -- content engine/items.js engine/encounters.js engine/economy.js engine/state.js engine/saveState.js exits 0"
    - requirement_id: TUNE-01
      category: safety
      statement: "MUST NOT decrement any foe's wp outside engine/foeDamage.js#damageFoe (foePower changes the STARTING wp only) and MUST NOT scale summoned reinforcements (resolveFoeAbility's summon literal stays as is — a documented Claude's-Discretion choice: reinforcements are already tier-limited)"
      status: flagged-unverified
      verification: "test/unit/foe-damage.test.js seam invariant green; grep -v '^\s*//' engine/foeAbilities.js | grep -c 'foeWpFor(' prints 0"
    - requirement_id: TUNE-01
      category: safety
      statement: "MUST NOT use DOM, Math.random or Date in engine/difficulty.js, engine/combat.js or engine/foeAbilities.js; difficultyCurve keeps arity 1 (depth only)"
      status: flagged-unverified
      verification: "test/unit/engine-purity.test.js and test/determinism/rng-no-math-random.test.js green; difficultyCurve.length === 1 (existing difficulty.test.js signature check)"
---

<objective>
Give the engine its one source of combat-scaling truth (TUNE-01): `difficultyCurve(depth)` gains `foeCap`, `foeBonus`, `foeLvlBias`, `foePower`, `abilityThreat` computed by a non-rounding soft cap over the depth PAST the identity band (≤ 5, D-19); `startCombat` reads the curve once and applies the D-17 zero-draw count bonus, the D-02 copy-time wp/maxWP scaling and a conditional per-foe `dmgBonus`; `tickAbilityCooldowns` becomes `(state, f)` (D-18) and every cadence read goes through `abilityCadenceFor` (D-03). Every constant lands at its identity value (MAX === BASE) so this plan's diff is provably inert at every depth; 21-04 turns the dials.

Purpose: the retune (21-04) must be a pure constants pass against wiring that is already proven parity-safe — so the wiring and its proofs (identity pins, draw-shape equality, synthetic-curve helper pins) land here, separately, with the whole suite green.

Output: `engine/difficulty.js` (+11 constants, `softCapFloat`, 5 fields, 4 pure helpers), `engine/combat.js` (startCombat / three melee sites / foeTurn call), `engine/foeAbilities.js` (signature + four cadence sites), `test/unit/combat-scaling.test.js` (new).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md
@.planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md
@.planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md
@engine/difficulty.js
@engine/combat.js
@engine/foeAbilities.js
@test/difficulty/difficulty.test.js
@test/unit/foe-turn-draw-count.test.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `tools/lib/tuning-bot.mjs`, the tool flags, `test/unit/tuning-bot.test.js`, `docs/DIFFICULTY-RETUNE.md` (BEFORE half) | dev tooling / ledger | 21-01 | landed before this plan |
| `engine/difficulty.js`: `COMBAT_SCALE_FROM_DEPTH = 6`, `FOE_CAP_BASE/MAX/SOFT_K`, `FOE_POWER_BASE/MAX/SOFT_K`, `ABILITY_THREAT_BASE/MAX/SOFT_K`, `FOE_LVL_BIAS`, `softCapFloat` (private), curve fields `foeCap`/`foeBonus`/`foeLvlBias`/`foePower`/`abilityThreat`, helpers `foeCountFor`/`foeWpFor`/`foeDmgBonusFor`/`abilityCadenceFor` | engine | **21-02** | D-01, D-04, D-17, D-19; MAX === BASE here |
| `engine/combat.js`: `startCombat` curve read + `foeLvlBias` in the clamp + `foeCountFor` + `foeWpFor` + conditional `dmgBonus`; `(f.dmgBonus \|\| 0)` ×2 and `(pursuer.dmgBonus \|\| 0)` ×1; `tickAbilityCooldowns(state, f)` call | engine | **21-02** | D-02 (mechanism per D-17), D-18 |
| `engine/foeAbilities.js`: `tickAbilityCooldowns(state, f)`; `abilityCadenceFor` at four sites | engine | **21-02** | D-03, D-18 |
| New per-foe instance key `dmgBonus` (present only when > 0; combat-scoped; never on a depth ≤ 5 foe) | engine state | **21-02** | absent ≡ 0; no carve-out needed (never on a fixture foe) |
| `test/unit/combat-scaling.test.js` | tests | **21-02** | 12 tests |
| `newRun(seed, exclude, { startDepth })`, `state.dev`, save/comparables carve-out, adapter + Settings dev toggle, DEV chip, build-www version injection | engine / client | 21-03 | D-13/D-14/D-22/D-23 |
| Retuned constants + rules-change blocks + ledger change table / AFTER / comparison; conditional `lootDepth` / member-upkeep scale | engine / ledger | 21-04 | D-09..D-12, D-20, D-21 |
| DR checklist + debug-build note; phase gate | ledger / gate | 21-05 | D-15/D-16 |

## Exact identity values (transcribed from CONTEXT.md — cite in code comments)

| Field | Identity value (depth ≤ 5) | Where it applies | Decision |
|---|---|---|---|
| `foeCap` | 3 (the canon Math.min ceiling for a level ≥ 3 hero; the level ≤ 2 `cap = 2` rule is applied BEFORE the bonus and is untouched) | `startCombat` count | D-02 / D-17 / D-19 |
| `foeBonus` | 0 | `startCombat` count (added after the canon roll, before the foeCap clamp) | D-17 |
| `foeLvlBias` | 0 (reserved) | `startCombat` maxLvl clamp | D-01 |
| `foePower` | 1.0 exactly | `foeWpFor` (wp/maxWP), `foeDmgBonusFor` (flat melee bonus) | D-02 |
| `abilityThreat` | 1.0 exactly | `abilityCadenceFor` (every ÷, uses ×) | D-03 / D-18 |
| identity band | depth ≤ 5 (`over = max(0, depth − 5)`) | all five | D-19 |

<tasks>

<task type="auto">
  <name>Task 1: Extend engine/difficulty.js with the combat-scaling constants, softCapFloat, the five curve fields and the four pure application helpers — all identity-valued</name>
  <files>engine/difficulty.js, test/unit/combat-scaling.test.js</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-01, D-02 (intent; mechanism superseded by D-17), D-03, D-04, D-17, D-19 and "Claude's Discretion" (soft-cap constants are 21-04's job; damage-bonus placement)
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — "Pattern 1: Extending difficultyCurve" (the softCap rounding warning, Assumption A4), Pitfall 1 (foeCap alone is a no-op), Pitfall 7 (the ≤ 5 identity band is already proven by the D-15 seeds)
    - .planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md — the `engine/difficulty.js` entry
    - engine/difficulty.js (whole file: header comment lines 1-22 — stale "No consumer wires this module yet" paragraph to replace; softCap lines 56-68; difficultyCurve lines 93-118 and its JSDoc field list)
    - test/difficulty/difficulty.test.js (whole file — it stays UNMODIFIED; note its "tolerates a non-integer depth" test iterates every numeric field, so the new fields must be finite for NaN/Infinity inputs too, and its arity check pins difficultyCurve.length === 1)
    - content/bestiary.js (skim: every row's `wp` is an integer — the identity assertion below covers all 53 rows via BESTIARY)
    - test/unit/combat.test.js lines 46-56 (fakeRng) and test/unit/foe-turn-draw-count.test.js lines 41-80 (countingRng — copy both helpers locally into the new test file; never import from another test file)
  </read_first>
  <action>
    In `engine/difficulty.js`:

    1. Replace the header's stale last two paragraphs ("The constants below are the 03-RESEARCH.md starting-point defaults…" and "No consumer wires this module yet…") with a Phase 21 paragraph: the module now also owns the COMBAT-scaling knobs (TUNE-01, D-01) consumed by engine/combat.js#startCombat (foe count / level bias / hit points / flat melee bonus) and engine/foeAbilities.js (caster cadence); the identity band is depth ≤ 5 (D-19 — proven by test/determinism/foe-abilities.test.js's tier 2/4/5 seeds and every depth-1 parity fixture); the dial VALUES are set by the Phase 21 retune and recorded in docs/DIFFICULTY-RETUNE.md; this module still consumes no rng and reads no DOM.

    2. After the DARK_* constants add, each with a one-line JSDoc citing its decision: `export const COMBAT_SCALE_FROM_DEPTH = 6;` (D-19: the first floor on which the combat knobs may leave identity; `over = max(0, depth - (COMBAT_SCALE_FROM_DEPTH - 1))` is the soft cap's argument); `export const FOE_CAP_BASE = 3;` (canon ceiling); `export const FOE_CAP_MAX = 3;` (21-02 IDENTITY — 21-04 sets the D-02 target, ≈ 5); `export const FOE_CAP_SOFT_K = 20;`; `export const FOE_POWER_BASE = 1.0;`; `export const FOE_POWER_MAX = 1.0;` (21-02 IDENTITY — D-02 target ≈ 1.6); `export const FOE_POWER_SOFT_K = 25;`; `export const ABILITY_THREAT_BASE = 1.0;`; `export const ABILITY_THREAT_MAX = 1.0;` (21-02 IDENTITY — D-03 target ≈ 2); `export const ABILITY_THREAT_SOFT_K = 20;`; `export const FOE_LVL_BIAS = 0;` (D-01 reserved: 0 unless the retune needs it).

    3. Below `softCap` add the private `function softCapFloat(base, cap, over, k) { return base + (cap - base) * (1 - Math.exp(-over / k)); }` with a JSDoc: the NON-rounding sibling of softCap for fractional multipliers (RESEARCH A4 — softCap's Math.round would flatten 1.0→1.6 into whole steps); note the exactness argument: at `over === 0`, `Math.exp(-0) === 1` so the product is exactly 0 and the result is exactly `base` (no floating-point drift — this is what makes the ≤ 5 identity a structural guarantee), and with `cap === base` the result is `base` at every depth.

    4. In `difficultyCurve`, after `breather`, compute `const over = Math.max(0, d - (COMBAT_SCALE_FROM_DEPTH - 1));` and `const foeCap = Math.round(softCapFloat(FOE_CAP_BASE, FOE_CAP_MAX, over, FOE_CAP_SOFT_K));` and extend the returned object with, in this order after `darkRadius`: `foeCap`, `foeBonus: foeCap - FOE_CAP_BASE`, `foeLvlBias: FOE_LVL_BIAS`, `foePower: softCapFloat(FOE_POWER_BASE, FOE_POWER_MAX, over, FOE_POWER_SOFT_K)`, `abilityThreat: softCapFloat(ABILITY_THREAT_BASE, ABILITY_THREAT_MAX, over, ABILITY_THREAT_SOFT_K)`. Combat knobs do NOT dip on breather floors (Claude's Discretion: a breather is lighter in density and darkness, not in the power of what you meet; monotone power keeps the DR-round depth numbers readable). Extend the JSDoc field list with the five fields (one line each, citing D-17 for foeBonus: "added AFTER the canon count roll, before the foeCap clamp — zero new draws").

    5. Append four exported pure helpers (JSDoc each; no rng; the curve object is the only input besides plain numbers/descriptors — this keeps every consumer's arithmetic testable with a synthetic curve):
       - `export function foeCountFor(canonCount, curve) { return Math.min(curve.foeCap, canonCount + curve.foeBonus); }` (D-17 — canonCount is the prototype's `Math.min(cap, d4-ternary)` result, already capped by the level rule).
       - `export function foeWpFor(baseWp, curve) { return curve.foePower === 1 ? baseWp : Math.round(baseWp * curve.foePower); }` (D-02 copy-time scaling; the strict-equality fast path makes identity structural even for a non-integer wp).
       - `export function foeDmgBonusFor(lvl, curve) { return curve.foePower === 1 ? 0 : Math.round((curve.foePower - 1) * lvl * lvl); }` (Claude's Discretion resolved: the flat bonus scales the `lvl * lvl` base term of every foe melee swing, never the `sp.dmg` dice — so no draw changes and damageFoe stays the one seam).
       - `export function abilityCadenceFor(a, curve) { const t = curve.abilityThreat; return { every: a.every === undefined ? undefined : Math.max(1, Math.round(a.every / t)), uses: a.uses === undefined ? undefined : Math.max(1, Math.round(a.uses * t)) }; }` (D-03/D-18 — the kits in content/foe-abilities.js are unchanged data; at threat 1 both fields are the descriptor's own numbers).

    Create `test/unit/combat-scaling.test.js` (header: Phase 21 TUNE-01 — combat-scaling knobs: identity band, bounds, synthetic-curve helper pins, startCombat/foeTurn/foeAbilities wiring, draw-shape equality; copy fakeRng + countingRng + a fixedFighter/fixedFloor/fixedState/fixedFoe subset locally). Imports: `difficultyCurve, isBreather` and all eleven new constants and four helpers from ../../engine/difficulty.js; `startCombat, foeTurn` from ../../engine/combat.js; `tickAbilityCooldowns, firstReadyAbility` from ../../engine/foeAbilities.js; `BESTIARY, FOE_ABILITIES` from ../../content/index.js. Tests 1-6 in this task (7-12 are added in Task 2):
    1. "D-19 identity: difficultyCurve(1..5) deep-equals the exact pre-Phase-21 object plus the identity fields" — for d in 1..5 `assert.deepStrictEqual(difficultyCurve(d), { depth: d, breather: false, dots: 9 + d, darkBlobs: d - 1, darkRadius: 3 + d, foeCap: 3, foeBonus: 0, foeLvlBias: 0, foePower: 1, abilityThreat: 1 })` and `assert.ok(Object.is(difficultyCurve(d).foePower, 1))`, same for abilityThreat.
    2. "21-02 constants are identity (MAX === BASE) — the retune (21-04) is the only thing allowed to move them" — pin all eleven constants: COMBAT_SCALE_FROM_DEPTH 6, FOE_CAP_BASE 3, FOE_CAP_MAX 3, FOE_CAP_SOFT_K 20, FOE_POWER_BASE 1, FOE_POWER_MAX 1, FOE_POWER_SOFT_K 25, ABILITY_THREAT_BASE 1, ABILITY_THREAT_MAX 1, ABILITY_THREAT_SOFT_K 20, FOE_LVL_BIAS 0 — with a comment that 21-04 rewrites the three MAX pins to the retuned values and re-records them in the ledger.
    3. "cap boundaries: every combat field stays within [BASE, MAX] at depths 6, 10, 20, 30, 50, 100, 1000; foeBonus === foeCap - FOE_CAP_BASE; foeLvlBias === FOE_LVL_BIAS" (use the imported constants, not literals, so the test survives 21-04).
    4. "monotone: foeCap, foePower, abilityThreat are non-decreasing across depths 1..200 including breather floors (no dip)".
    5. "non-finite depth tolerance: for [0, -3, 1.5, NaN, Infinity, -Infinity] every new field is finite and equals its depth-1 identity value".
    6. "synthetic-curve helper pins" — foeCountFor: (3,{5,2})→5, (1,{5,2})→3, (2,{4,1})→3, (k,{3,0})→k for k 1..3; foeWpFor: (10,{foePower:1.6})→16, (7,{1.5})→11, and for EVERY row of every BESTIARY tier `foeWpFor(row.wp, { foePower: 1 }) === row.wp` plus `Number.isInteger(row.wp)`; foeDmgBonusFor: (5,{1.6})→15, (1,{1.6})→1, (3,{1})→0; abilityCadenceFor: ({every:2,uses:4},{abilityThreat:2})→{every:1,uses:8}, ({every:3},{1.5})→{every:2,uses:undefined}, ({every:1},{4})→every 1, and for EVERY descriptor in FOE_ABILITIES `abilityCadenceFor(a, { abilityThreat: 1 })` deep-equals `{ every: a.every, uses: a.uses }`.
  </action>
  <verify>
    <automated>node --test test/unit/combat-scaling.test.js test/difficulty/difficulty.test.js test/difficulty/fairness.test.js test/unit/maze.test.js test/unit/engine-purity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export const COMBAT_SCALE_FROM_DEPTH = 6;' engine/difficulty.js` prints 1 and `grep -c '^export const FOE_CAP_BASE = 3;\|^export const FOE_CAP_MAX = 3;\|^export const FOE_POWER_BASE = 1.0;\|^export const FOE_POWER_MAX = 1.0;\|^export const ABILITY_THREAT_BASE = 1.0;\|^export const ABILITY_THREAT_MAX = 1.0;\|^export const FOE_LVL_BIAS = 0;' engine/difficulty.js` prints 7 and `grep -c '^export const FOE_CAP_SOFT_K = \|^export const FOE_POWER_SOFT_K = \|^export const ABILITY_THREAT_SOFT_K = ' engine/difficulty.js` prints 3
    - `grep -c '^function softCapFloat(base, cap, over, k)' engine/difficulty.js` prints 1 and `awk '/^function softCapFloat/,/^}/' engine/difficulty.js | grep -c 'Math.round'` prints 0
    - `awk '/^export function difficultyCurve/,/^}/' engine/difficulty.js | grep -c 'const over = Math.max(0, d - (COMBAT_SCALE_FROM_DEPTH - 1));'` prints 1 and the same block greps 1 each for `foeCap,`, `foeBonus: foeCap - FOE_CAP_BASE,`, `foeLvlBias: FOE_LVL_BIAS,`, `foePower: softCapFloat(FOE_POWER_BASE, FOE_POWER_MAX, over, FOE_POWER_SOFT_K),`, `abilityThreat: softCapFloat(ABILITY_THREAT_BASE, ABILITY_THREAT_MAX, over, ABILITY_THREAT_SOFT_K),`
    - `grep -c '^export function foeCountFor(canonCount, curve)\|^export function foeWpFor(baseWp, curve)\|^export function foeDmgBonusFor(lvl, curve)\|^export function abilityCadenceFor(a, curve)' engine/difficulty.js` prints 4 and `grep -c 'rng' engine/difficulty.js` prints only comment-line hits (`grep -v '^\s*//\|^\s*\*' engine/difficulty.js | grep -c 'rng'` prints 0)
    - `node -e "import('./engine/difficulty.js').then(m=>{const c=m.difficultyCurve(5);if(c.foeCap!==3||c.foeBonus!==0||!Object.is(c.foePower,1)||!Object.is(c.abilityThreat,1))process.exit(1);const d=m.difficultyCurve(30);if(d.foeCap!==3||!Object.is(d.foePower,1)||!Object.is(d.abilityThreat,1))process.exit(2);console.log('identity ok')})"` prints identity ok (this plan's constants are identity at EVERY depth)
    - `node --test test/unit/combat-scaling.test.js` exits 0 with 6 passing tests (12 after Task 2) and `node --test test/difficulty/difficulty.test.js` exits 0 with ZERO edits to that file (`git diff --quiet bd0ba7c -- test/difficulty/difficulty.test.js` exits 0)
    - `node --test test/unit/engine-purity.test.js test/determinism/rng-no-math-random.test.js` exits 0
  </acceptance_criteria>
  <done>The curve carries the five combat fields with a structural ≤ 5 identity band and identity-valued constants, the four pure helpers exist, and six tests pin identity, bounds, monotonicity, non-finite tolerance and the synthetic-curve arithmetic — with the existing difficulty test file untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the curve into startCombat (D-17 count bonus, D-02 wp/dmgBonus copy-time scaling), the three melee sites, foeTurn's tick call (D-18) and foeAbilities' four cadence sites (D-03); prove draw-shape equality and the D-15/FID-02 pins untouched</name>
  <files>engine/combat.js, engine/foeAbilities.js, test/unit/combat-scaling.test.js</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-01 (startCombat reads ONLY the curve), D-02, D-03, D-17, D-18, D-19
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — Pitfall 1, Pitfall 2, the "Reading the curve inside startCombat" code example (note: it draws the count unconditionally — do NOT copy that; keep the draws on the non-wandering branch), Pitfall 7
    - .planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md — the `startCombat` and `tickAbilityCooldowns` entries
    - engine/combat.js lines 52-60 (import block — add the difficulty.js import beside the foeAbilities.js one), 109-152 (startCombat: the maxLvl clamp at 124, `cap` at 126, the count ternary at 132 with its NOTE comment, the per-foe `foes.push` literal at 135-152 including the Phase 19 DETERMINISM GATE comment), 518-538 (pursuitStrike's damage line at 536), 1222-1236 (the ability gate: the tick call at 1227), 1266 and 1285 (the member-branch and hero-branch damage lines)
    - engine/foeAbilities.js lines 26-29 (imports), 37-54 (tickAbilityCooldowns), 68-83 (firstReadyAbility — the `uses` read at 73), 140-150 (resolveFoeAbility — the cooldown reset at 143-146 and the uses decrement at 147-150)
    - test/unit/foe-turn-draw-count.test.js lines 82-110 (how the D-15/FID-02 tests force a tier by setting c.level and floor.depth — they stay byte-identical) and test/determinism/foe-abilities.test.js lines 1-40 (its header contract: never adjust a pin)
    - test/unit/combat.test.js lines 96-140 (fixedFoe/fixedCombat shapes to copy) and the D-06/D-20 foeTurn test near line 860 (the fakeRng([6, 3]) / need 5 arithmetic the dmgBonus test reuses)
  </read_first>
  <action>
    In `engine/combat.js`:

    1. Add `import { difficultyCurve, foeCountFor, foeWpFor, foeDmgBonusFor } from "./difficulty.js";` next to the foeAbilities.js import (line 59).

    2. In `startCombat`, directly after `const c = state.c;` add `const curve = difficultyCurve(state.floor.depth);` with a comment: Phase 21 (TUNE-01, D-01) — the ONE combat-scaling read, pure and 0 draws, safe unconditionally; identity at depth ≤ 5 (D-19). Change the maxLvl line to `const maxLvl = clamp(Math.min(c.level, state.floor.depth) + curve.foeLvlBias, 1, 5);` (D-01 — foeLvlBias is 0/reserved; the canon clamp is otherwise verbatim). Keep `const cap = c.level <= 2 ? 2 : 3;` and its comment verbatim. Rewrite the count line so the d4 ternary stays INSIDE the non-wandering branch: `const n = wandering ? 1 : foeCountFor(Math.min(cap, rng.d(4) <= 2 ? 1 : rng.d(4) <= 3 ? 2 : 3), curve);` — keep the existing NOTE comment above it and add a second paragraph: Phase 21 (D-17): `foeCountFor` adds the curve's zero-draw `foeBonus` AFTER the canon roll and clamps at `foeCap`; the d4/d4 short-circuit is untouched and a wandering encounter still draws nothing here; at depth ≤ 5 `foeBonus === 0` and `foeCap === 3`, so `n` is byte-identical to the prototype. Inside the per-foe loop compute `const wp = foeWpFor(picked.wp, curve);` and `const dmgBonus = foeDmgBonusFor(lvl, curve);` before the push; in the literal set the two hit-point fields to `wp` and `maxWP: wp`, and add — directly after the existing `abilities` spread and with the same "absent, not zero" DETERMINISM GATE reasoning — `...(dmgBonus > 0 ? { dmgBonus } : {}),` with a comment: Phase 21 (TUNE-01, D-02): copy-time power scaling; the key is added ONLY when the curve's foePower is above 1 (never at depth ≤ 5), so every fixture-exposed foe object is byte-identical; `damageFoe` (engine/foeDamage.js) is still the only place a foe's wp ever decreases — this only changes the STARTING number. Add one "DELIBERATE RULES CHANGE (Phase 21, TUNE-01, D-01/D-02/D-17)" block above startCombat's JSDoc summarising the two additive terms and pointing at docs/DIFFICULTY-RETUNE.md for the values.

    3. Three melee damage sites — add the bonus as a middle term, post-draw arithmetic, comment `// Phase 21 (D-02): flat foePower bonus on the lvl*lvl base — absent at depth <= 5, 0 draws`: pursuitStrike line 536 becomes `let dmg = pursuer.lvl * pursuer.lvl + (pursuer.dmgBonus || 0) + (pursuer.sp && pursuer.sp.dmg ? rollDice(rng, pursuer.sp.dmg) : rng.d(6));`; the member-branch line 1266 becomes `let mDmg = f.lvl * f.lvl + (f.dmgBonus || 0) + (f.sp && f.sp.dmg ? rollDice(rng, f.sp.dmg) : rng.d(6));`; the hero-branch line 1285 becomes `let dmg = f.lvl * f.lvl + (f.dmgBonus || 0) + (f.sp && f.sp.dmg ? rollDice(rng, f.sp.dmg) : rng.d(6));`. Nothing else in these blocks changes (need/roll/crit/weakened lines untouched).

    4. In `foeTurn`'s ability gate change the one-argument tick call to `tickAbilityCooldowns(state, f);` (D-18) and extend the gate's comment with one sentence: Phase 21 (D-18) — the tick now takes `state` so the cadence can read the curve; still 0 draws.

    In `engine/foeAbilities.js`:

    5. Add `import { difficultyCurve, abilityCadenceFor } from "./difficulty.js";` (difficulty.js is a leaf — no cycle).

    6. `export function tickAbilityCooldowns(state, f)`: compute `const curve = difficultyCurve(state.floor.depth);` first; the lazy init becomes `if (f.cd[id] === undefined) f.cd[id] = abilityCadenceFor(a, curve).every;`; the decrement line is unchanged. Update the JSDoc (signature, the "identity at depth ≤ 5 / abilityThreat === 1 reproduces every D-15 pin" sentence, still 0 draws) and add a "DELIBERATE RULES CHANGE (Phase 21, TUNE-01, D-03/D-18)" block: deep floors shorten `every` and raise `uses` through the curve; the kits are unchanged data.

    7. `firstReadyAbility(state, f)`: compute `const curve = difficultyCurve(state.floor.depth);` at the top; the uses readiness line becomes `if (a.uses !== undefined && (f.uses?.[id] ?? abilityCadenceFor(a, curve).uses) <= 0) continue;`.

    8. `resolveFoeAbility(state, f, a, rng, events)`: compute `const cad = abilityCadenceFor(a, difficultyCurve(state.floor.depth));` right after `const c = state.c;`; the cooldown reset becomes `f.cd[a.id] = cad.every;` and the uses decrement becomes `f.uses[a.id] = (f.uses[a.id] ?? cad.uses) - 1;`. The summon literal (wp/maxWP from `picked.wp`) is deliberately NOT scaled — add a one-line comment saying so without naming the wp scaler (Claude's Discretion: reinforcements are already tier-limited weak foes; scaling them is a 21-04 option only if the DR round asks).

    Append tests 7-12 to `test/unit/combat-scaling.test.js`:
    7. "D-19 wiring identity: at depth 1..5 every foe built by startCombat has wp === maxWP === the roster row's wp and NO dmgBonus key" — fixedState with `c.level = 5`, forced "Beasts", `fakeRng([3, 3, 2, 2, 10, 5])` (count 2; no downgrade; initiative), for depth 1 and 5: `foes.length === 2`, each `f.wp === BESTIARY.Beasts[f.lvl - 1][0].wp`, `"dmgBonus" in f === false`.
    8. "draw-shape equality (D-17/D-19): startCombat draws 8 at depth 1 and at depth 5, and 2 + foeCountFor(2, difficultyCurve(30)) * 2 + 2 at depth 30" — countingRng(fakeRng([3, 3, 2, 2, 2, 2, 2, 10, 5])) per depth; assert `draws1 === 8`, `draws5 === 8`, `draws30 === 2 + foeCountFor(2, difficultyCurve(30)) * 2 + 2` (with this plan's identity constants that is also 8 — the formula keeps the test valid after 21-04); also assert the depth-30 foes' `wp === foeWpFor(BESTIARY.Beasts[f.lvl - 1][0].wp, difficultyCurve(30))` and `("dmgBonus" in f) === (foeDmgBonusFor(f.lvl, difficultyCurve(30)) > 0)`.
    9. "wandering encounters still draw no count dice" — `startCombat(state, true, "Beasts", countingRng(fakeRng([2, 10, 5])))` at depth 1 and depth 30: exactly 1 foe, draws === 1 + 1 + 2 (foe d4 + pick + initiative) at both depths.
    10. "dmgBonus is post-draw arithmetic on the hero swing" — fixedState + awake fixedFoe (Beasts lvl 1, no sp.dmg): `foeTurn(state, fakeRng([3, 4]), [])` → `struckByFoe` with `roll === 3`, `dmg === 5` (1 + d6 4), `state.c.wp === 50`; a fresh state whose foe carries `dmgBonus: 5` → `dmg === 10`, `state.c.wp === 45`; both calls consume exactly 2 draws (countingRng).
    11. "D-18: tickAbilityCooldowns(state, f) lazily inits from the cadence and counts down; identity at depth 5" — `tickAbilityCooldowns.length === 2`; foe `{ abilities: ["drudgeFireball"] }` (every 2) on a fixedState at depth 5: after one tick `f.cd.drudgeFireball === 1` and `firstReadyAbility(state, f) === null`; after a second tick `=== 0` and `firstReadyAbility(state, f).id === "drudgeFireball"`; a foe with `["djinniFreeze"]` (uses 4, no every) is ready immediately and after `f.uses = { djinniFreeze: 0 }` is not.
    12. "the D-15 / FID-02 contract is untouched by Phase 21 wiring" — read `test/determinism/foe-abilities.test.js` and `test/unit/foe-turn-draw-count.test.js` from disk and assert neither contains the string `Phase 21` (a structural guard that no one 'adjusted' a pin to make this plan green — mirrors the header contract of both files).
  </action>
  <verify>
    <automated>node --test test/unit/combat-scaling.test.js test/unit/combat.test.js test/unit/foe-abilities.test.js test/unit/foe-damage.test.js test/unit/foe-turn-draw-count.test.js test/unit/party-combat.test.js test/unit/bestiary-yardstick.test.js test/determinism/foe-abilities.test.js && node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { difficultyCurve, foeCountFor, foeWpFor, foeDmgBonusFor } from "./difficulty.js";' engine/combat.js` prints 1 and `grep -c 'import { difficultyCurve, abilityCadenceFor } from "./difficulty.js";' engine/foeAbilities.js` prints 1
    - `awk '/^export function startCombat/,/^}/' engine/combat.js | grep -c 'const curve = difficultyCurve(state.floor.depth);'` prints 1 and the same block greps 1 for `const maxLvl = clamp(Math.min(c.level, state.floor.depth) + curve.foeLvlBias, 1, 5);`, 1 for `const cap = c.level <= 2 ? 2 : 3;`, 1 for `const n = wandering ? 1 : foeCountFor(Math.min(cap, rng.d(4) <= 2 ? 1 : rng.d(4) <= 3 ? 2 : 3), curve);`, 1 for `const wp = foeWpFor(picked.wp, curve);`, 1 for `const dmgBonus = foeDmgBonusFor(lvl, curve);`, 1 for `...(dmgBonus > 0 ? { dmgBonus } : {}),`, 1 for `maxWP: wp,`
    - `grep -v '^\s*//' engine/combat.js | grep -c 'wp: picked.wp'` prints 0 (the roster row's hit points now flow through foeWpFor; the summon literal in foeAbilities.js is a different file and keeps its own copy)
    - `grep -c '(f.dmgBonus || 0)' engine/combat.js` prints 2 and `grep -c '(pursuer.dmgBonus || 0)' engine/combat.js` prints 1
    - `grep -c 'tickAbilityCooldowns(state, f);' engine/combat.js` prints 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'tickAbilityCooldowns(f)'` prints 0
    - `grep -c '^export function tickAbilityCooldowns(state, f)' engine/foeAbilities.js` prints 1 and `grep -c 'abilityCadenceFor(a, curve).every' engine/foeAbilities.js` prints 1 and `grep -c 'abilityCadenceFor(a, curve).uses' engine/foeAbilities.js` prints 1 and `grep -c 'const cad = abilityCadenceFor(a, difficultyCurve(state.floor.depth));' engine/foeAbilities.js` prints 1 and `grep -c 'f.cd\[a.id\] = cad.every;' engine/foeAbilities.js` prints 1 and `grep -c '(f.uses\[a.id\] ?? cad.uses) - 1' engine/foeAbilities.js` prints 1 and `grep -v '^\s*//' engine/foeAbilities.js | grep -c '= a.every;'` prints 0 and `grep -v '^\s*//' engine/foeAbilities.js | grep -c 'foeWpFor('` prints 0 (the summon literal is not routed through the scaler; do not name the helper in that comment)
    - `grep -c 'DELIBERATE RULES CHANGE (Phase 21' engine/combat.js` prints >= 1 and `grep -c 'DELIBERATE RULES CHANGE (Phase 21' engine/foeAbilities.js` prints >= 1
    - `node --test test/unit/combat-scaling.test.js` exits 0 with 12 passing tests
    - `node --test test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js` exits 0 and `git diff --quiet 04eb229 -- test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js test/parity/prototype-master.js.txt test/parity/fixtures` exits 0 and `git diff --quiet bd0ba7c -- test/parity/harness/comparables.js content engine/items.js engine/encounters.js engine/economy.js engine/state.js engine/saveState.js` exits 0
    - `node --test test/unit/foe-damage.test.js` exits 0 (the one-seam invariant scans engine/*.js and finds no new foe-side wp decrement)
    - `node --test "test/parity/**/*.test.js"` exits 0, 30 pass, with no change to any carve-out
    - `npm test` exits 0 with every test passing (923 + 12 = 935; record the exact number in the SUMMARY)
    - `node tools/tune-difficulty.mjs --seeds=3 --json` still runs (smoke; numbers are not compared)
  </acceptance_criteria>
  <done>startCombat, the three melee sites, foeTurn and foeAbilities read the curve exactly as specified with zero new draws; twelve scaling tests pass; the D-15 and FID-02 files are byte-unchanged and green; parity is 30/30 with no new carve-out; the full suite is green and every dial is still at identity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → frozen prototype parity | Every depth-1 fixture and every D-15 tier-2/4/5 determinism seed must stay byte-identical |
| curve → combat consumers | `difficultyCurve` is the only depth-math source; consumers must not inline a second formula |
| save-derived depth → curve | `state.floor.depth` can be tampered/corrupt; `safeDepth` already sanitises it before any new arithmetic |
| per-foe instance → damage seam | Scaling must change only the starting `wp`/`maxWP` and a flat additive term, never bypass `damageFoe` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-06 | Tampering | A rounding or floating-point drift makes `foePower`/`abilityThreat` ≠ 1 at depth ≤ 5, silently shifting D-15 cooldown pins or fixture foe wp | high | mitigate | `over = max(0, d − 5)` argument (exact `exp(0) = 1`), `Object.is(x, 1)` identity pins for d = 1..5, strict `=== 1` fast paths in foeWpFor/foeDmgBonusFor, D-15 + FID-02 files byte-unchanged |
| T-21-07 | Tampering | The count rewrite hoists the d4 ternary out of the wandering branch (or adds a draw), desyncing every combat fixture | high | mitigate | count line pinned by grep (ternary inside `foeCountFor(Math.min(cap, …))` on the non-wandering branch); wandering draw test (test 9); parity 30/30 |
| T-21-08 | Tampering | `dmgBonus` (or any new key) lands on a depth ≤ 5 foe object, breaking byte-identical combat comparables | high | mitigate | conditional spread `dmgBonus > 0` only; test 7 asserts the key is absent at depth 1..5; no comparables edit allowed |
| T-21-09 | Tampering | A consumer decrements foe wp outside `damageFoe` while applying power scaling | medium | mitigate | scaling applied to the object literal at copy time only; `test/unit/foe-damage.test.js` one-seam invariant stays in the verify command |
| T-21-10 | Denial of Service (local) | A corrupted `floor.depth` (NaN/negative) reaching the new arithmetic yields NaN foe counts / cooldowns | low | mitigate | `safeDepth` already clamps before `over` is computed; test 5 asserts finite identity values for NaN/±Infinity/0/−3/1.5 |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet bd0ba7c -- package.json package-lock.json` |
</threat_model>

<verification>
- `node --test test/unit/combat-scaling.test.js` 12/12; `npm test` fully green (expected 935); parity 30/30 with zero new carve-outs; `test/determinism/foe-abilities.test.js` and `test/unit/foe-turn-draw-count.test.js` byte-identical to 04eb229 and green.
- Source gates: eleven constants at identity; `softCapFloat` non-rounding; curve fields in order; four helpers exported; startCombat curve read / clamp / count line / wp / dmgBonus spread; three melee sites; `tickAbilityCooldowns(state, f)` signature + call; four cadence sites through `abilityCadenceFor`; no foeWpFor in foeAbilities.js.
- `difficultyCurve(30)` still reports foeCap 3 / foePower 1 / abilityThreat 1 (this plan is inert everywhere).
</verification>

<success_criteria>
- ROADMAP Phase 21 criterion 1 is met structurally: `engine/difficulty.js` owns foe count (`foeCap`/`foeBonus`), foe level (`foeLvlBias`), foe power and ability threat by depth as one source of truth, consumed by startCombat and foeAbilities.js with zero new draws and a proven ≤ 5 identity band.
- The retune (21-04) can now be a pure constants pass.
</success_criteria>

<output>
Create `.planning/phases/21-consolidated-difficulty-retune/21-02-SUMMARY.md` when done (record: the final constant block, the startCombat count line as landed, the three melee-site line numbers, the four cadence sites, the test-count delta, and confirmation that the D-15/FID-02 files and the parity fixtures are byte-unchanged).
</output>
