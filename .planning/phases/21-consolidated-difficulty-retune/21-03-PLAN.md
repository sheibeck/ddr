---
phase: 21-consolidated-difficulty-retune
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - engine/state.js
  - engine/encounters.js
  - engine/saveState.js
  - test/parity/harness/comparables.js
  - test/unit/newrun.test.js
  - test/unit/save-validation.test.js
  - src/browser/engineAdapter.js
  - test/unit/engineAdapter.test.js
  - mazeworld.html
  - tools/build-www.mjs
autonomous: true
requirements: [TUNE-04]
tags: [engine, state, save, parity-carveout, adapter, settings-ui, dev-toggle, build, node-test]

must_haves:
  truths:
    - "TUNE-04 (D-13): newRun(seed, exclude, { startDepth }) generates floor startDepth, levels the rolled character to THRESHOLDS[min(startDepth, 5) - 1] SP through the ordinary checkLevel path, grants WILMST_CACHE_PER_DEPTH × startDepth wilmst through gainWilmst (why: \"dev start\"), re-captures rngState AFTER those draws, and sets dev = true — e.g. startDepth 20 → floor.depth 20, c.level 5, c.sp 1501, gold = base + 6000 (non-Pickpocket), dev true"
    - "TUNE-04 (D-14, default-identity probe): newRun(seed) and newRun(seed, [], { startDepth: 1 }) are deepStrictEqual for every seed and carry dev === false; newRun.length is still 1; every parity fixture calls newRun(seed) and stays byte-identical"
    - "TUNE-04 (D-13, chargen-untouched probe): a dev run's c.name / race / cls / sub equal the default roll's for the same seed — the character is rolled FIRST with the unchanged draw order; the dev draws (level-up gain dice, purse) happen only afterwards on the dev branch"
    - "TUNE-04 (startDepth sanitisation probe): startDepth 0, -3, NaN, 1.5, Infinity, 'abc' and undefined all produce a state deepStrictEqual to newRun(seed) (dev false, floor 1); startDepth 5000 clamps to DEV_START_DEPTH_MAX = 999 (dev true, floor 999); startDepth 3 → level 3 (sp 501); startDepth 50 → level 5 (sp 1501), floor 50"
    - "TUNE-04 (D-14/D-23, save probe): validateSave and rehydrate emit dev: !!obj.dev — a v1.0-shaped save with no dev key loads with dev === false; dev: true round-trips through serializeRun → JSON → validateSave → rehydrate as true; dev: 'yes' / 1 coerce to true and dev: 0 / null to false; the fresh-run round-trip test (rehydrate(validateSave(serializeRun(newRun(seed)))) deepStrictEqual newRun(seed)) still passes because newRun now always carries dev: false"
    - "TUNE-04 (D-14, carve-out probe): dev is stripped in movementComparable, combatComparable AND economyComparable (the ONLY new Phase 21 carve-out); parity stays 30/30"
    - "TUNE-04 (D-13, exclusion probe): a dev run's death through engineAdapter.dispatch() writes NOTHING to GRAVE_KEY / GRAVE_TOTAL_KEY / RECENT_NAMES_KEY, and startNewRun() after a dev run does NOT record its floor.depth into getBest(); a normal run's death and best-depth recording are unchanged"
    - "TUNE-04 (D-22): the Settings sheet gains a version row whose text reads 'Version dev' in the plain dev loop and 'Version <versionName> (<versionCode>)' in www/index.html (build-www.mjs substitutes the placeholder from android/version.properties and throws if the placeholder is missing); a ≈1.2 s pointer long-press on the version label reveals a hidden row with a numeric depth field (min 1, max 999, default 20) and a Start button; nothing else in normal play reveals it"
    - "TUNE-04 (D-13, UI): window.mzDevStartAtDepth(n) closes the sheet, calls startNewRun(undefined, { startDepth: n }), swaps the state in, clears the log, prints a Floor-N banner in voice, repaints; paint() toggles a small DEV chip in the HUD from S.dev so a dev run is always visibly marked"
    - "npm test fully green; parity 30/30 with exactly one new carve-out (dev); no new event type (leveled/goldGained already narrate); frozen files untouched; mazeworld.html keeps CRLF line endings"
    - statement: "On the device, the long-press feels deliberate (no accidental reveal during a tap), the depth field is usable with the on-screen keyboard, the DEV chip is visible but unobtrusive, and a dev death leaves the graveyard exactly as it was"
      verification: backstop
  artifacts:
    - path: "engine/state.js"
      provides: "newRun(seed, exclude = [], { startDepth = 1 } = {}) with the dev branch; DEV_START_DEPTH_MAX; dev: boolean always present on a fresh state"
      exports: ["newRun", "DEV_START_DEPTH_MAX", "addPartyMember", "PARTY_CAP", "STATE_VERSION"]
      contains: "export const DEV_START_DEPTH_MAX = 999;"
    - path: "engine/encounters.js"
      provides: "WILMST_CACHE_PER_DEPTH exported (was module-private) so the dev purse reuses the existing wilmst-cache row"
      contains: "export const WILMST_CACHE_PER_DEPTH = 300;"
    - path: "engine/saveState.js"
      provides: "dev: !!obj.dev in validateSave's value and in rehydrate's state"
      contains: "dev: !!obj.dev,"
    - path: "test/parity/harness/comparables.js"
      provides: "dev stripped in all three *Comparable() destructures"
      contains: "pendingFind, dev, ...state0"
    - path: "src/browser/engineAdapter.js"
      provides: "initRun(seed, exclude, options); startNewRun(seed, { startDepth }); recordBest and persistGrave gated on !dev"
      contains: "if (diedEvent && !currentState.dev)"
    - path: "mazeworld.html"
      provides: "Settings version row (#mw-settings-version / #mw-app-version), hidden dev row (#mw-dev-row, #mw-dev-start-depth, #mw-dev-start-btn), long-press handler, window.mzDevStartAtDepth, HUD DEV chip (#mw-dev-chip) toggled in paint()"
      contains: "window.mzDevStartAtDepth"
    - path: "tools/build-www.mjs"
      provides: "version placeholder substitution from android/version.properties inside writeIndexHtml"
      contains: "version.properties"
    - path: "test/unit/newrun.test.js"
      provides: "D-13/D-14 newRun tests (default identity, startDepth 20/3/50, sanitisation, chargen untouched, rngState advanced)"
      contains: "startDepth: 20"
    - path: "test/unit/save-validation.test.js"
      provides: "dev coercion + round-trip tests"
      contains: "dev"
    - path: "test/unit/engineAdapter.test.js"
      provides: "dev-run exclusion tests (no grave, no best) + startDepth threading"
      contains: "startDepth"
  key_links:
    - from: "engine/state.js newRun (dev branch)"
      to: "engine/character.js checkLevel, engine/items.js gainWilmst, engine/encounters.js WILMST_CACHE_PER_DEPTH, content THRESHOLDS"
      via: "c.sp = THRESHOLDS[Math.min(startAt, 5) - 1]; checkLevel(state, rng, events); gainWilmst(state, WILMST_CACHE_PER_DEPTH * startAt, \"dev start\", rng, events); state.rngState = rng.getState();"
      pattern: "gainWilmst\\(state, WILMST_CACHE_PER_DEPTH \\* startAt, \"dev start\", rng, events\\);"
    - from: "src/browser/engineAdapter.js startNewRun / initRun"
      to: "engine/state.js newRun options"
      via: "initRun(safeSeed, exclude, options) → newRun(seed, exclude, options)"
      pattern: "newRun\\(seed, exclude, options\\)"
    - from: "mazeworld.html #mw-dev-start-btn click → window.mzDevStartAtDepth"
      to: "engineAdapter.startNewRun(undefined, { startDepth })"
      via: "const state = await startNewRun(undefined, { startDepth: depth });"
      pattern: "startNewRun\\(undefined, \\{ startDepth: depth \\}\\)"
    - from: "engineAdapter.dispatch() death path and startNewRun()"
      to: "storage (GRAVE_KEY / BEST_KEY)"
      via: "both writes gated on !currentState.dev"
      pattern: "!currentState\\.dev"
    - from: "tools/build-www.mjs writeIndexHtml"
      to: "android/version.properties → www/index.html #mw-app-version"
      via: "replace the exact placeholder span text with `${versionName} (${versionCode})`"
      pattern: "mw-app-version"
  prohibitions:
    - requirement_id: TUNE-04
      category: safety
      statement: "MUST NOT add any rng draw before or between rollCharacter → genFloor → reveal on the default path; the dev branch runs only when startAt > 1 and re-captures rngState at its end"
      status: flagged-unverified
      verification: "newrun default-identity test (deepStrictEqual incl. rngState); parity 30/30; test/determinism/same-seed-same-result.test.js green"
    - requirement_id: TUNE-04
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any fixture JSON, test/determinism/foe-abilities.test.js or test/unit/foe-turn-draw-count.test.js; the comparables edit is limited to adding `dev` to the three top-level destructures"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js exits 0; git diff bd0ba7c -- test/parity/harness/comparables.js touches exactly 3 code lines plus comments"
    - requirement_id: TUNE-04
      category: safety
      statement: "MUST NOT make the dev field or the depth row reachable in normal play: no menu entry, no keyboard shortcut, no URL parameter — only the ≈1.2 s long-press on the version label reveals the row, and the row is `hidden` in the markup"
      status: flagged-unverified
      verification: "grep -c 'id=\"mw-dev-row\" hidden' mazeworld.html prints 1; grep -c 'location.search\\|URLSearchParams' mazeworld.html unchanged from bd0ba7c"
    - requirement_id: TUNE-04
      category: values
      statement: "MUST NOT add a new event type (no devRunStarted marker — leveled/goldGained already narrate; the banner is presentation text) and MUST NOT touch engine/difficulty.js, engine/combat.js or engine/foeAbilities.js in this plan"
      status: flagged-unverified
      verification: "test/unit/formatEventsCoverage.test.js green; git diff --quiet $BASE -- engine/difficulty.js engine/combat.js engine/foeAbilities.js exits 0 (BASE = HEAD at the start of this plan = 21-02's final commit, recorded in the SUMMARY)"
    - requirement_id: TUNE-04
      category: safety
      statement: "MUST NOT use DOM, Math.random or Date in engine/state.js / engine/saveState.js; the seed fallback stays in the adapter"
      status: flagged-unverified
      verification: "test/unit/engine-purity.test.js and test/determinism/rng-no-math-random.test.js green"
---

<objective>
Build the dev-only "start at depth N" affordance the TUNE-04 sign-off needs (D-13/D-14/D-22/D-23): `newRun(seed, exclude, { startDepth })` with a `dev` flag that is a first-class serialized boolean (coerced on load, stripped in all three comparables), the adapter threading + graveyard/best-depth exclusion, and the hidden Settings entry — a new version line (useful on its own) whose long-press reveals the depth field. No new screen, no new event type, no reach from normal play.

Purpose: a human cannot reach floor 20–50 inside a 5–10 minute session; without this, the DR round (21-05) cannot happen. It lands BEFORE the retune so the retune's AFTER readout is measured on the final engine (the dev code is inert on every default path).

Output: `engine/state.js`, `engine/encounters.js` (one export), `engine/saveState.js`, `test/parity/harness/comparables.js`, `src/browser/engineAdapter.js`, `mazeworld.html`, `tools/build-www.mjs`, three extended test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md
@.planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md
@.planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md
@engine/state.js
@engine/saveState.js
@test/parity/harness/comparables.js
@src/browser/engineAdapter.js
@tools/build-www.mjs
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| Shared bot module, tool flags, tuning-bot tests, ledger BEFORE half | dev tooling / ledger | 21-01 | landed |
| Curve fields + helpers, startCombat/foeAbilities wiring, combat-scaling tests | engine | 21-02 | landed (identity constants) |
| `newRun(seed, exclude = [], { startDepth = 1 } = {})`; `DEV_START_DEPTH_MAX = 999`; `state.dev` (boolean, always present) | engine | **21-03** | D-13/D-14 |
| `export const WILMST_CACHE_PER_DEPTH` (engine/encounters.js) | engine | **21-03** | reused by the dev purse |
| `dev: !!obj.dev` in `validateSave` + `rehydrate` | engine | **21-03** | D-23 |
| `dev` in the three `*Comparable()` destructures | harness | **21-03** | the phase's ONLY new carve-out |
| `initRun(seed, exclude, options)`, `startNewRun(seed, { startDepth })`, `!dev` gates on `recordBest`/`persistGrave` | client adapter | **21-03** | D-13 |
| Settings version row + hidden dev row + long-press + `window.mzDevStartAtDepth` + HUD DEV chip; `.mw-settings-row[hidden]` / chip CSS | client UI | **21-03** | D-22, Claude's Discretion (gesture, chip) |
| build-www version substitution (`android/version.properties` → `#mw-app-version`) | build | **21-03** | D-22 |
| `test/unit/newrun.test.js` (+6), `test/unit/save-validation.test.js` (+3), `test/unit/engineAdapter.test.js` (+4) | tests | **21-03** | — |
| Retuned constants, rules-change blocks, ledger change table / AFTER / comparison | engine / ledger | 21-04 | — |
| DR checklist + debug-build note; phase gate | ledger / gate | 21-05 | — |

## Exact rule targets (transcribed from CONTEXT.md — cite in code comments)

| Term | Value | Decision |
|---|---|---|
| Signature | `newRun(seed, exclude = [], { startDepth = 1 } = {})` — additive third param (RESEARCH Pattern 4) | D-13 / D-14 |
| Sanitised start | `startAt = Math.min(DEV_START_DEPTH_MAX, difficultyCurve(startDepth).depth)` (integer ≥ 1; the movement.js#descend precedent for reusing the curve's safeDepth) | Security V5 |
| Level grant | `c.sp = THRESHOLDS[Math.min(startAt, 5) - 1]` then `checkLevel(state, rng, events)` | D-13 |
| Purse | `gainWilmst(state, WILMST_CACHE_PER_DEPTH * startAt, "dev start", rng, events)` (300 × depth) | D-13 |
| Flag | `dev: startAt > 1` in the literal; `state.rngState = rng.getState()` re-captured after the dev draws | D-13 / D-14 |
| Save | `dev: !!obj.dev` in both validateSave and rehydrate (absent → false) | D-14 / D-23 |
| Comparables | `dev` added to all three destructures | D-14 |
| Exclusion | `recordBest` skipped when the ENDING run is dev; `persistGrave` skipped when the dying run is dev | D-13 |
| Reveal | long-press ≈ 1200 ms on `#mw-settings-version` → `#mw-dev-row.hidden = false` | D-22 |
| Version text | `Version dev` (dev loop) / `Version <versionName> (<versionCode>)` (built www) | D-22 |

<tasks>

<task type="auto">
  <name>Task 1: newRun(seed, exclude, { startDepth }) + state.dev in the engine, dev coercion in saveState.js, the dev carve-out in all three comparables, with newrun/save-validation tests</name>
  <files>engine/state.js, engine/encounters.js, engine/saveState.js, test/parity/harness/comparables.js, test/unit/newrun.test.js, test/unit/save-validation.test.js</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-13, D-14, D-23 and "Established Patterns" (new serialized field → comparables + save tolerance)
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — "Pattern 4: Dev start-at-depth wiring" (the 3-arg signature, the import-cycle note), Open Question 3 (dev: !!obj.dev), Security Domain (startDepth sanitisation via the safeDepth pattern)
    - .planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md — the `engine/state.js#newRun`, `engine/saveState.js` and `comparables.js` entries
    - engine/state.js (whole file: imports at 9-12, newRun 63-104 — note `rngState: rng.getState()` is captured INSIDE the literal, so the dev branch must re-capture it)
    - engine/encounters.js lines 235-245 (the module-private `const WILMST_CACHE_PER_DEPTH = 300;` and its ECON-09 comment) and 37 (it imports addPartyMember from state.js — the cycle is inert for function declarations and for a const read at call time)
    - engine/character.js lines 261-300 (checkLevel: loops while levelFromSP(c.sp) > c.level, draws gain dice per level) and content/misc-tables.js line 14 (THRESHOLDS = [0, 201, 501, 901, 1501])
    - engine/items.js lines 81-93 (gainWilmst: greed multiplier, Pickpocket extra draws, goldGained event) and engine/movement.js lines 642-652 (descend's `difficultyCurve(state.floor.depth).depth` sanitisation precedent)
    - engine/saveState.js lines 176-200 (validateSave's value whitelist; `dead: !!obj.dead, won: !!obj.won`) and 222-262 (rehydrate's whitelist)
    - test/parity/harness/comparables.js lines 163, 227, 366 (the three destructures) and their PARTY-02/ECON-02 comments
    - test/unit/newrun.test.js (whole file — the arity test at line 12 must keep passing) and test/unit/save-validation.test.js lines 1-35 (the fresh-run round-trip test that forces `dev: false` to be present on a fresh state)
  </read_first>
  <action>
    Record `BASE=$(git rev-parse HEAD)` before any edit (21-02's final commit) — the plan's "untouched" gates diff against it.

    In `engine/encounters.js`: change the module-private wilmst-cache constant to `export const WILMST_CACHE_PER_DEPTH = 300;` (same line, same value, same comment; add one sentence: also read by engine/state.js#newRun's dev start-at-depth purse, D-13).

    In `engine/state.js`: extend the imports — `import { difficultyCurve } from "./difficulty.js";`, add `checkLevel` to the character.js import, `import { gainWilmst } from "./items.js";`, `import { WILMST_CACHE_PER_DEPTH } from "./encounters.js";`, `import { THRESHOLDS } from "../content/index.js";` (comment: the items.js/encounters.js edges close an ESM cycle back to this module — inert, because only hoisted function declarations and a call-time const read cross it; engine.js imports state.js first and every dependency finishes evaluating before newRun is ever called). Add `export const DEV_START_DEPTH_MAX = 999;` (JSDoc: hard ceiling for the dev-only startDepth option — Security V5, mirrors the safeDepth posture). Change the signature to `export function newRun(seed, exclude = [], { startDepth = 1 } = {})` (arity stays 1). Body: `const rng = makeRng(seed); const c = rollCharacter(rng, exclude);` unchanged; then `const startAt = Math.min(DEV_START_DEPTH_MAX, difficultyCurve(startDepth).depth);` (comment: the curve's safeDepth clamps 0 / negative / NaN / non-integer / ±Infinity to 1 — the descend() precedent — so a malformed dev input can never reach genFloor or index THRESHOLDS out of range; 0 rng); `const floor = genFloor(startAt, rng);` (identical to genFloor(1, rng) by default); reveal unchanged; build `const state = { …the existing literal verbatim…, dev: startAt > 1 };` placing `dev` directly after `won: false,` with a comment block: Phase 21 (TUNE-04, D-13/D-14): dev — true only for a start-at-depth run; a plain boolean present on EVERY fresh state exactly like dead/won (so serializeRun/validateSave/rehydrate round-trip it and the fresh-run round-trip test stays deepStrictEqual); the parity harness strips it in all three comparables; a dev run is never written to the graveyard or the best-depth record (src/browser/engineAdapter.js). Then the dev branch: `if (startAt > 1) { … }` with a DEV-ONLY PATH comment (D-14: never reached by a fixture or any default caller, so the rng draws below are safe): `const events = [];` `c.sp = THRESHOLDS[Math.min(startAt, 5) - 1];` `checkLevel(state, rng, events);` (the same level-up path a real climb takes — gain dice, Soldier→Knight, Apprentice reroll, Sorcerer spells) `gainWilmst(state, WILMST_CACHE_PER_DEPTH * startAt, "dev start", rng, events);` (the existing depth-scaled wilmst-cache row — D-13) `state.rngState = rng.getState();` (the literal captured the cursor BEFORE these draws; re-capture so the run resumes from the right place). Return `state`. Update newRun's JSDoc: the options param, the default-identity guarantee, and that `events` from the dev branch are discarded (the UI prints its own banner).

    In `engine/saveState.js`: add `dev: !!obj.dev,` directly after `won: !!obj.won,` in BOTH `validateSave`'s `value` literal and `rehydrate`'s `state` literal, each with a one-line comment: Phase 21 (D-14/D-23): boolean-coerced like dead/won; absent on a pre-Phase-21 save → false.

    In `test/parity/harness/comparables.js`: add `dev` to the three top-level destructures — `const { beats, seed, rngState, version, party, pendingJoiner, pendingFind, dev, ...state0 } = state;` in movementComparable and `const { beats, seed, rngState, version, lastExchange, exchangeN, party, pendingJoiner, pendingFind, dev, ...state0 } = state;` in combatComparable and economyComparable — with a comment beside the ECON-02 one: Phase 21 (TUNE-04, D-14): strip the new top-level `state.dev` too — a fourth analog of party/pendingJoiner/pendingFind; always false on a fixture (every fixture calls newRun(seed)); the prototype master has no such field. No other line changes.

    Extend `test/unit/newrun.test.js` (import `DEV_START_DEPTH_MAX` from ../../engine/state.js, `THRESHOLDS` and `CLASSES` from ../../content/index.js, `eff` from ../../engine/derived.js):
    1. "D-14: newRun(seed) === newRun(seed, [], { startDepth: 1 }) deep-equal, dev false, arity still 1" — seeds [1, 42, 303]: deepStrictEqual (this includes rngState), `dev === false`, `newRun.length === 1`.
    2. "D-13: startDepth 20 → floor 20, level 5, sp 1501, dev true, purse +6000, rngState advanced" — `const base = newRun(42); const dev = newRun(42, [], { startDepth: 20 });` assert `dev.floor.depth === 20`, `dev.c.level === 5`, `dev.c.sp === THRESHOLDS[4]`, `dev.dev === true`, `dev.c.maxWP > base.c.maxWP`, `dev.c.gold >= base.c.gold + 6000`, and exactly `dev.c.gold === base.c.gold + Math.round(6000 * (1 + 0.5 * eff(base.c, "greed")))` when `base.c.sub !== "Pickpocket"` (import `eff` from ../../engine/derived.js — a chargen greed item, if any seed ever rolls one, is folded in rather than guessed), `dev.rngState !== base.rngState`, and `JSON.parse(JSON.stringify(dev))` deepStrictEqual `dev` (still serializable).
    3. "D-13: chargen is untouched — the dev run rolls the SAME adventurer" — for seeds [1, 42, 303]: `name`, `race`, `cls`, `sub` (before any Soldier→Knight promotion: compare against `base.c.sub` only when `base.c.sub !== "Soldier" && base.c.sub !== "Apprentice"`, otherwise assert the promoted sub is a valid CLASSES sub), `temperament`, `motive`, `phobia` equal between `newRun(seed).c` and `newRun(seed, [], { startDepth: 20 }).c`.
    4. "D-13: startDepth 3 → level 3 / sp 501 / floor 3; startDepth 50 → level 5 / sp 1501 / floor 50 / purse +15000".
    5. "sanitisation: 0, -3, NaN, 1.5, Infinity, 'abc', undefined all equal newRun(seed); 5000 clamps to DEV_START_DEPTH_MAX" — for each bad value `assert.deepStrictEqual(newRun(7, [], { startDepth: v }), newRun(7))`; `newRun(7, [], { startDepth: 5000 }).floor.depth === DEV_START_DEPTH_MAX` and `.dev === true`.
    6. "purity: a dev run reads no wall clock and is reproducible" — `assert.deepStrictEqual(newRun(9, [], { startDepth: 12 }), newRun(9, [], { startDepth: 12 }))`.

    Extend `test/unit/save-validation.test.js`:
    7. "D-14/D-23: a pre-Phase-21 save with no dev key loads with dev === false" — take `serializeRun(newRun(5))`, `delete obj.dev`, `validateSave(JSON.stringify(obj))` ok, `.value.dev === false`, `rehydrate(check.value).dev === false`.
    8. "D-14: dev: true survives serializeRun → JSON → validateSave → rehydrate" — `newRun(5, [], { startDepth: 20 })` → round-trip → `dev === true`, `floor.depth === 20`, `c.level === 5`.
    9. "D-23: dev is coerced to a strict boolean" — objects with `dev: "yes"`, `dev: 1` → true; `dev: 0`, `dev: null` → false, via both validateSave and rehydrate.
  </action>
  <verify>
    <automated>node --test test/unit/newrun.test.js test/unit/save-validation.test.js test/unit/engine-purity.test.js test/unit/new-run-loop.test.js test/unit/parley-carveout.test.js test/unit/foe-ability-carveouts.test.js test/determinism/same-seed-same-result.test.js test/roundtrip/serialize-rehydrate.test.js && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export const WILMST_CACHE_PER_DEPTH = 300;' engine/encounters.js` prints 1
    - `grep -c '^export function newRun(seed, exclude = \[\], { startDepth = 1 } = {})' engine/state.js` prints 1 and `grep -c '^export const DEV_START_DEPTH_MAX = 999;' engine/state.js` prints 1
    - `awk '/^export function newRun/,/^}/' engine/state.js | grep -c 'const startAt = Math.min(DEV_START_DEPTH_MAX, difficultyCurve(startDepth).depth);'` prints 1 and the same block greps 1 for `const floor = genFloor(startAt, rng);`, 1 for `dev: startAt > 1,`, 1 for `if (startAt > 1) {`, 1 for `c.sp = THRESHOLDS\[Math.min(startAt, 5) - 1\];`, 1 for `checkLevel(state, rng, events);`, 1 for `gainWilmst(state, WILMST_CACHE_PER_DEPTH \* startAt, "dev start", rng, events);`, 1 for `state.rngState = rng.getState();`
    - `grep -c 'dev: !!obj.dev,' engine/saveState.js` prints 2
    - `grep -c 'pendingFind, dev, ...state0 } = state;' test/parity/harness/comparables.js` prints 3 and `git diff bd0ba7c --numstat -- test/parity/harness/comparables.js` shows a small additive diff (comment lines + the three changed destructures only)
    - `node -e "import('./engine/engine.js').then(m=>{const a=m.newRun(42),b=m.newRun(42,[],{startDepth:1});if(JSON.stringify(a)!==JSON.stringify(b)||a.dev!==false)process.exit(1);const d=m.newRun(42,[],{startDepth:20});if(d.floor.depth!==20||d.c.level!==5||d.dev!==true)process.exit(2);console.log('newRun ok')})"` prints newRun ok
    - `node --test test/unit/newrun.test.js` exits 0 with the previous count + 6 tests; `node --test test/unit/save-validation.test.js` exits 0 with the previous count + 3 tests (the existing fresh-run round-trip test at line 24 is unchanged and green)
    - `node --test "test/parity/**/*.test.js"` exits 0, 30 pass; `node --test test/determinism/same-seed-same-result.test.js test/roundtrip/serialize-rehydrate.test.js` exits 0
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js` exits 0 and `git diff --quiet $BASE -- engine/difficulty.js engine/combat.js engine/foeAbilities.js` exits 0 at the end of the task (BASE = `git rev-parse HEAD` captured before this plan's first edit; this plan never touches them)
    - `node --test test/unit/engine-purity.test.js test/determinism/rng-no-math-random.test.js` exits 0
  </acceptance_criteria>
  <done>newRun accepts the sanitised startDepth option, the dev branch levels/purses/re-captures rngState, the dev flag is a coerced serialized boolean stripped by all three comparables, and nine new tests plus the parity suite (30/30) and the round-trip suites are green.</done>
</task>

<task type="auto">
  <name>Task 2: Thread startDepth through engineAdapter (initRun / startNewRun) and exclude dev runs from the graveyard and best-depth record, with adapter tests</name>
  <files>src/browser/engineAdapter.js, test/unit/engineAdapter.test.js</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-13 (excluded from graveyard AND high score; "never reachable in normal play")
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — Architectural Responsibility Map (graveyard/high-score exclusion is client-adapter tier), Security Domain (clamp startDepth at the adapter too)
    - .planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md — the `src/browser/engineAdapter.js` entry
    - src/browser/engineAdapter.js lines 130-141 (initRun), 181-197 (recordBest — "best" is the only high-score feature: BEST_KEY), 199-259 (persistGrave — the three keys it writes), 262-282 (startNewRun), 330-380 (dispatch's `diedEvent` gate)
    - test/unit/engineAdapter.test.js lines 1-65 (withFakeLocalStorage / flushStorage / getState helpers), 173-200 (startNewRun + getBest tests), 229-285 (GRAVE_KEY constants, firstOpenPlainDir, the starvation-death graveyard test to mirror)
  </read_first>
  <action>
    In `src/browser/engineAdapter.js`:
    1. `export function initRun(seed, exclude = [], options = {})` → `currentState = newRun(seed, exclude, options);` (JSDoc: Phase 21 D-13 — `options.startDepth` is the dev-only start-at-depth; omitted by every existing caller so their rolls stay byte-identical).
    2. `export async function startNewRun(seed, options = {})`: the best-depth write becomes `if (currentState && !currentState.dev) { await recordBest(currentState.floor.depth); }` (comment: Phase 21 D-13 — a dev run's ending depth is never a "best"); after `exclude`, build `const startDepth = Number.isInteger(options.startDepth) && options.startDepth >= 1 ? options.startDepth : 1;` (Security V5 — the engine clamps again; this keeps a non-integer from ever reaching it) and call `initRun(safeSeed, exclude, { startDepth })`. JSDoc: add the options param and the exclusion sentence.
    3. In `dispatch()`: `if (diedEvent && !currentState.dev) track(persistGrave(currentState, diedEvent.cause));` with a comment: Phase 21 (TUNE-04, D-13) — a dev start-at-depth run is a testing run; its death must not enter the graveyard, the all-time total, or the recent-names window. Nothing else changes (the fail-closed catch path still calls `initRun(seed)`).

    Extend `test/unit/engineAdapter.test.js`:
    1. "D-13: startNewRun(seed, { startDepth: 20 }) starts a dev run on floor 20" — `await startNewRun(4242, { startDepth: 20 })` → `state.floor.depth === 20`, `state.dev === true`, `state.c.level === 5`, `getState() === state`; and `await startNewRun(4243, { startDepth: "20" })` → floor 1, dev false (non-integer ignored).
    2. "D-13: a dev run's death through dispatch() writes NO graveyard entry" — `initRun(55, [], { startDepth: 20 })`, starve exactly like the CR-01 test (rations 0, wp 1, steps 99, move via firstOpenPlainDir), assert `after.dead === true`, `await flushStorage()`, then `store.getItem(GRAVE_KEY) === null`, `store.getItem(GRAVE_TOTAL_KEY) === null`, `store.getItem(RECENT_NAMES_KEY) === null`.
    3. "D-13: ending a dev run via startNewRun does not record its depth as best" — `initRun(11, [], { startDepth: 30 })`; `getState().floor.depth = 31` (walked one deeper); `await startNewRun(99)`; `await getBest() === 0`; then `getState().floor.depth = 4; await startNewRun(100); await getBest() === 4` (a normal run still records).
    4. "D-14: a normal run's death after a dev run still buries normally" — `initRun(55, [], { startDepth: 20 })` then `await startNewRun(56)` (normal), starve it → GRAVE_KEY has exactly one tombstone whose name is the normal run's character.
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js test/unit/new-run-loop.test.js test/unit/permadeath.test.js test/unit/death.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function initRun(seed, exclude = \[\], options = {})' src/browser/engineAdapter.js` prints 1 and `grep -c 'currentState = newRun(seed, exclude, options);' src/browser/engineAdapter.js` prints 1
    - `grep -c '^export async function startNewRun(seed, options = {})' src/browser/engineAdapter.js` prints 1 and `grep -c 'if (currentState && !currentState.dev) {' src/browser/engineAdapter.js` prints 1 and `grep -c 'initRun(safeSeed, exclude, { startDepth })' src/browser/engineAdapter.js` prints 1
    - `grep -c 'if (diedEvent && !currentState.dev) track(persistGrave(currentState, diedEvent.cause));' src/browser/engineAdapter.js` prints 1
    - `grep -c 'startDepth' test/unit/engineAdapter.test.js` prints >= 4
    - `node --test test/unit/engineAdapter.test.js` exits 0 with the previous count + 4 tests (every pre-existing test, including "startNewRun(seed) records the ending run's floor.depth into getBest()", unchanged and green)
    - `node --test test/unit/new-run-loop.test.js test/unit/permadeath.test.js test/unit/death.test.js` exits 0
  </acceptance_criteria>
  <done>The adapter threads the dev option to the engine, a dev run never touches the graveyard keys or the best-depth key, normal runs are unaffected, and four adapter tests prove it.</done>
</task>

<task type="auto">
  <name>Task 3: Settings version row + long-press dev field + window.mzDevStartAtDepth + HUD DEV chip in mazeworld.html (CRLF-preserving), and build-www version substitution</name>
  <files>mazeworld.html, tools/build-www.mjs</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-13 (hidden affordance, no new screen), D-22 (version line + ≈1.2 s long-press, version from the build), "Claude's Discretion" (gesture, DEV chip)
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — Pitfall 6 (no version line / no reusable long-press exists; write a local pointerdown/setTimeout/pointerup handler; do NOT reuse controls.js#classifyPointerGesture), Don't Hand-Roll row 3
    - .planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md — the `mazeworld.html` entry (row markup + the local long-press sketch) and the `tools/build-www.mjs` note
    - mazeworld.html lines 1184-1231 (#mw-settings-sheet markup: the five rows; add the new rows after Haptics), 1237-1245 (.mw-hud-top / .mw-hud-row — where the DEV chip goes), 2829-2846 (paint(): add the chip toggle beside the m-floor write), 5537-5590 (settings open/close + the delegated click handler — add the long-press + Start wiring right after `mw-settings-close`), 5838-5870 (window.newGame = engineNewRun — the module function to mirror for window.mzDevStartAtDepth: clear log, center map, banner, paint, draw), 827-840 (.mw-hud-item CSS to base the chip on), 850-860 (chip treatment comment for the legend sheet)
    - tools/build-www.mjs lines 244-266 (writeIndexHtml + main) and tools/bump-version.mjs lines 13-40 (how android/version.properties is read/parsed — mirror its `versionName=`/`versionCode=` line parsing)
    - android/version.properties (current: versionCode=2, versionName=1.0.1)
    - .planning/phases/20-parley-balance-language-system/20-02-PLAN.md Task 3's CRLF gate (`node -e` mixed-line-endings check) — mazeworld.html is CRLF; edit in place, never rewrite the whole file
  </read_first>
  <action>
    In `mazeworld.html` (edit in place; preserve CRLF):
    1. Markup — after the Haptics `.mw-settings-row` inside `#mw-settings-rows`, add two rows:
       `<div class="mw-settings-row" id="mw-settings-version-row"><span class="mw-settings-label mw-settings-version" id="mw-settings-version">Version <span id="mw-app-version">dev</span></span></div>` and
       `<div class="mw-settings-row" id="mw-dev-row" hidden><span class="mw-settings-label">Start at depth (dev)</span><div class="mw-settings-options"><input type="number" id="mw-dev-start-depth" min="1" max="999" value="20" inputmode="numeric" aria-label="Start depth"><button type="button" class="mw-settings-opt" id="mw-dev-start-btn">Start</button></div></div>`,
       preceded by an HTML comment: Phase 21 (TUNE-04, D-13/D-22) — the version line is a real setting-sheet row (build-www.mjs replaces the `dev` placeholder with versionName (versionCode) from android/version.properties); the dev row is hidden and revealed ONLY by a ≈1.2 s long-press on the version label — a dev run is flagged state.dev, marked by the HUD DEV chip, and never buried or recorded as a best depth.
    2. CSS (next to the `.mw-settings-row` rules): `.mw-settings-row[hidden]{display:none}` (the row is a flex container, so the hidden attribute alone would not hide it), `.mw-settings-version{user-select:none;-webkit-user-select:none;-webkit-touch-callout:none;opacity:.75}` (no text-selection/callout during the long-press), `#mw-dev-start-depth{width:5.5em;font:inherit;padding:4px 6px;background:transparent;color:inherit;border:1px solid currentColor;border-radius:4px}`, and a `.mw-dev-chip{font:700 10px/1 inherit;letter-spacing:.08em;padding:3px 6px;border-radius:4px;background:var(--ditto);color:#000;margin-left:8px}` chip (reuse the `--ditto` accent the floor number already uses).
    3. HUD — inside `.mw-hud-top`, right after the first `.mw-hud-row`, add `<span class="mw-dev-chip" id="mw-dev-chip" hidden>DEV</span>`. In `paint()`, immediately after the `m-floor` write, add `const devChip = document.getElementById("mw-dev-chip"); if (devChip) devChip.hidden = !S.dev;` (presentation-only read of the serialized flag).
    4. Module script — after the `mw-settings-close` listener: a local long-press handler (Claude's Discretion — deliberately NOT controls.js#classifyPointerGesture, which is maze-viewport-specific): `const DEV_LONG_PRESS_MS = 1200; let devPressTimer = null;` on `#mw-settings-version` `pointerdown` → `devPressTimer = setTimeout(() => { const row = document.getElementById("mw-dev-row"); if (row) row.hidden = false; }, DEV_LONG_PRESS_MS)`; on `pointerup`, `pointercancel`, `pointerleave` → `clearTimeout(devPressTimer)`; `contextmenu` on the label → `e.preventDefault()`. Then `#mw-dev-start-btn` `click` → `const raw = parseInt(document.getElementById("mw-dev-start-depth")?.value, 10); const depth = Number.isInteger(raw) ? Math.min(999, Math.max(1, raw)) : 20; window.mzDevStartAtDepth?.(depth);`.
    5. Next to `window.newGame = async function engineNewRun()`, add `window.mzDevStartAtDepth = async function devStartAtDepth(depth) { … }` that: hides the settings sheet (`closeSettingsSheet()` is in the same module scope — call it directly); `const state = await startNewRun(undefined, { startDepth: depth });` `window.__mzState.set(state);` clears `#log` exactly as engineNewRun does; `window.mzCenterMap?.();` logs a voice banner `<span class="banner">Floor ${state.floor.depth}.</span> A dev run. The graveyard has agreed to look the other way.` (family-friendly, deadpan); `window.paint(); window.draw();`. Comment: Phase 21 (TUNE-04, D-13) — the only entry point for a dev run; reached solely from the hidden Settings row.

    In `tools/build-www.mjs` `writeIndexHtml(importMap)`: after the import-map injection, read `android/version.properties` (path.join(ROOT, "android", "version.properties")), parse `versionName=` and `versionCode=` lines (trim; throw with a clear message if either is missing); define `const placeholder = '<span id="mw-app-version">dev</span>';` and throw `mazeworld.html is missing the #mw-app-version placeholder — cannot stamp the version` if `html.includes(placeholder)` is false; replace it (first occurrence) with `<span id="mw-app-version">${versionName} (${versionCode})</span>`; `step(\`stamped version ${versionName} (${versionCode}) into www/index.html\`)`. Update the header comment's numbered list with a step 5 describing the stamp (Phase 21, D-22).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');process.exit((s.match(/(?<!\r)\n/g)||[]).length===0?0:1)" && grep -c 'id="mw-dev-row" hidden' mazeworld.html | grep -qx 1 && grep -c 'window.mzDevStartAtDepth = async function devStartAtDepth(depth)' mazeworld.html | grep -qx 1 && npm run build:www && grep -c 'id="mw-app-version">1.0.1 (2)</span>' www/index.html | grep -qx 1 && node --test test/unit/settings.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js && npm test</automated>
    <human-check>On the Pixel 7 debug build (npm run android:debug + adb install -r, per docs/RELEASING.md): open Settings from the HUD gear — the last row reads "Version 1.0.1 (2)"; a quick tap on it does nothing; holding it ~1.2 s reveals "Start at depth (dev)" with a numeric field defaulting to 20 and a Start button; Start at 20 closes the sheet, shows "Floor 20." with the dev banner, the HUD shows a small DEV chip, the hero is level 5 with a 6000-wilmst purse; die on that run, return to the title — the graveyard and best depth are exactly as before. (Deferred to the end-of-phase DR round, 21-05.)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-settings-version"' mazeworld.html` prints 1 and `grep -c '<span id="mw-app-version">dev</span>' mazeworld.html` prints 1 and `grep -c 'id="mw-dev-row" hidden' mazeworld.html` prints 1 and `grep -c 'id="mw-dev-start-depth" min="1" max="999" value="20"' mazeworld.html` prints 1 and `grep -c 'id="mw-dev-start-btn"' mazeworld.html` prints 1
    - `grep -c '\.mw-settings-row\[hidden\]{display:none}' mazeworld.html` prints 1 and `grep -c '\.mw-dev-chip{' mazeworld.html` prints 1 and `grep -c 'id="mw-dev-chip" hidden' mazeworld.html` prints 1
    - `awk '/^function paint\(\) \{/,/^}/' mazeworld.html | grep -c 'devChip.hidden = !S.dev'` prints 1
    - `grep -c 'const DEV_LONG_PRESS_MS = 1200;' mazeworld.html` prints 1 and `grep -c '"pointerdown"' mazeworld.html` prints >= 1 (the new listener) and `grep -c 'clearTimeout(devPressTimer)' mazeworld.html` prints >= 1 and `grep -c 'classifyPointerGesture' mazeworld.html` equals its bd0ba7c count (`git show bd0ba7c:mazeworld.html | grep -c 'classifyPointerGesture'`) — the maze gesture helper is not repurposed
    - `grep -c 'window.mzDevStartAtDepth = async function devStartAtDepth(depth)' mazeworld.html` prints 1 and `grep -c 'startNewRun(undefined, { startDepth: depth })' mazeworld.html` prints 1 and `grep -c 'A dev run. The graveyard has agreed to look the other way.' mazeworld.html` prints 1
    - `grep -c 'mzDevStartAtDepth' mazeworld.html` prints exactly 2 (the definition and the single Start-button call) — no other entry point
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');process.exit((s.match(/(?<!\r)\n/g)||[]).length===0?0:1)"` exits 0 (CRLF preserved, no mixed endings)
    - `grep -c 'version.properties' tools/build-www.mjs` prints >= 2 (header + read) and `grep -c "const placeholder = '<span id=\"mw-app-version\">dev</span>';" tools/build-www.mjs` prints 1
    - `npm run build:www` exits 0, prints `stamped version 1.0.1 (2) into www/index.html`, and `grep -c 'id="mw-app-version">1.0.1 (2)</span>' www/index.html` prints 1 while `grep -c '<span id="mw-app-version">dev</span>' www/index.html` prints 0 (www/ is gitignored build output — do not commit it)
    - `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/settings.test.js test/unit/parley-button-mirror.test.js` exits 0
    - `npm test` exits 0 with every test passing (935 + 6 + 3 + 4 = 948; record the exact number) and `node --test "test/parity/**/*.test.js"` reports 30 pass
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js package.json package-lock.json` exits 0
  </acceptance_criteria>
  <done>The Settings sheet shows a version line (stamped by the build), a ≈1.2 s long-press on it reveals the dev depth row, Start launches a flagged dev run marked by a HUD chip through the one adapter entry point, CRLF is intact, the build stamps the version, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev input field → adapter → engine | A typed `startDepth` is untrusted (non-integer, negative, huge); clamped at both the adapter and `newRun` |
| save file → state | A tampered save may carry `dev` as any value; coerced to a strict boolean on load |
| dev run → persistent records | A dev run must never reach the graveyard, the all-time total, the recent-names window, or the best-depth key |
| normal play → hidden affordance | The dev row must be unreachable without the deliberate long-press |
| engine → frozen prototype parity | Every fixture calls `newRun(seed)`; the default path must stay byte-identical |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-11 | Tampering | Malformed `startDepth` (0, −3, NaN, 1.5, ∞, string, 5000) reaching `genFloor`/`THRESHOLDS` indexing | medium | mitigate | adapter integer check + `Math.min(DEV_START_DEPTH_MAX, difficultyCurve(startDepth).depth)` in newRun; sanitisation test 5 |
| T-21-12 | Tampering | A save carrying `dev: "yes"`/`1` bypassing the strict-boolean contract (or a dev save "un-flagging" itself) | low | mitigate | `dev: !!obj.dev` in validateSave AND rehydrate; coercion test 9; only this player's own records are affected (offline, single-player) |
| T-21-13 | Repudiation | A dev death or dev best-depth silently entering the graveyard / best record and polluting the player's real history | high | mitigate | `!currentState.dev` gates on both `persistGrave` and `recordBest`; adapter tests 2 and 3 assert all four storage keys untouched |
| T-21-14 | Elevation of Privilege | The dev row reachable by a tap, a key, a URL parameter, or shown by default | medium | mitigate | `hidden` in markup + `[hidden]{display:none}` CSS; only a 1200 ms pointer hold reveals it; `mzDevStartAtDepth` referenced exactly twice; no URL parsing added |
| T-21-15 | Tampering | The default `newRun(seed)` path gains a draw or a key (breaking every fixture) | high | mitigate | dev branch gated on `startAt > 1`; `dev: false` present on every fresh state and stripped in all three comparables; default-identity test incl. rngState; parity 30/30 |
| T-21-16 | Denial of Service (local) | build-www silently ships a "dev" version string or the placeholder drifts | low | mitigate | build throws if the placeholder or version keys are missing; `npm run build:www` output gate |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet bd0ba7c -- package.json package-lock.json` |
</threat_model>

<verification>
- `npm test` fully green (expected 948); parity 30/30 with `dev` as the only new carve-out; frozen files untouched; CRLF intact.
- Source gates: newRun signature/branch lines; `dev: !!obj.dev` ×2; three destructures; adapter `initRun`/`startNewRun`/`dispatch` gates; markup ids; `mzDevStartAtDepth` referenced exactly twice; build stamp present in www/index.html.
- Human backstop (end-of-phase DR round): long-press reveal, dev run start, DEV chip, graveyard untouched.
</verification>

<success_criteria>
- A tester can reach floor 20/35/50 in seconds through a hidden, deliberate Settings gesture; the run is flagged, visibly marked, and excluded from the graveyard and best depth; nothing about normal play, saves or parity changed.
</success_criteria>

<output>
Create `.planning/phases/21-consolidated-difficulty-retune/21-03-SUMMARY.md` when done (record: the final newRun signature and dev branch as landed, the comparables diff, the adapter gates, the exact markup ids and the long-press constant, the build stamp line, the test-count delta, and the parity/frozen-file confirmations).
</output>
