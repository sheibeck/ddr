---
phase: 65-run-record-personal-bests
plan: 05
type: execute
wave: 3
depends_on: ["65-03", "65-04"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/shell-new-best.test.js
autonomous: true
requirements: [RUN-04]
user_setup: []

must_haves:
  truths:
    - "When a death beats the player's #1 on any board, the THAT IS THAT panel shows a visually distinct, gold-accented NEW PERSONAL BEST block, placed after the panel's line and before the REVIEW THE ORACLE / BURY THEM buttons. It lists each beaten board with its value (for example 'DEEPEST DESCENT · floor 9') and ends with exactly one quip"
    - "The first-ever death shows the block with a single first-death line and no board list"
    - "A death that sets no new #1 (a lesser top-ten finish included) shows no block at all"
    - "Idempotency: the block is identical on every re-render of the dead panel (after REVIEW THE ORACLE and back, for example), because the view is parked once per died event in window.__mzDeathRecord and never re-drawn from a new rng"
    - "The parcel resets to null whenever the title screen shows (BURY THEM, abandon, save and quit), so a later run's death never shows a stale block"
    - "The death screen owns its own space: the rail stays hidden while S.dead (the railEl.hidden line is unchanged), and no retired transient-notification surface is reintroduced (the stale-terms sweep stays green)"
    - "No GameState field is added: the report reaches the shell only through takeDeathRecord() and the presentation-only window.__mzDeathRecord bridge, which is registered in src/browser/bridge.js and the generated docs/SHELL-MODULES.md table"
    - "The block is built with createElement and textContent only, never innerHTML, and all of its copy comes from the view (content/boards.js); the shell holds no copy of its own"
  prohibitions:
    - statement: "Must not reintroduce the retired v1.4 transient-notification surface or show the rail while the death panel is up; the announcement lives only inside THAT IS THAT"
      status: unresolved
      verification: judgment
    - statement: "Must not claim a new personal best the report does not contain; no block for lesser finishes or for GRAVEYARD"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "mazeworld.html"
      provides: "renderNewBestBlock (classic), the renderCombatOver dead-branch call, the .cb-over-best CSS, and the module-side parcel wiring"
      contains: "renderNewBestBlock"
    - path: "src/browser/bridge.js"
      provides: "the __mzDeathRecord registry entry"
      contains: "__mzDeathRecord"
    - path: "docs/SHELL-MODULES.md"
      provides: "regenerated bridge table including __mzDeathRecord"
      contains: "__mzDeathRecord"
    - path: "test/unit/shell-new-best.test.js"
      provides: "source-region and fake-DOM behaviour tests for the block and its wiring"
      min_lines: 90
  key_links:
    - from: "mazeworld.html (module: dispatchWithNarration)"
      to: "src/browser/engineAdapter.js#takeDeathRecord"
      via: "on a died event: window.__mzDeathRecord = newBestView(takeDeathRecord())"
      pattern: "newBestView\\(takeDeathRecord\\(\\)\\)"
    - from: "mazeworld.html (classic: renderCombatOver)"
      to: "window.__mzDeathRecord"
      via: "kind === 'dead' calls renderNewBestBlock(over, window.__mzDeathRecord) before the actions row"
      pattern: "renderNewBestBlock\\(over, window\\.__mzDeathRecord\\)"
---

<objective>
Announce a new personal best inside the THAT IS THAT death panel (RUN-04, CONTEXT "New-best announcement"). The module script turns the adapter's one-shot death report into a view once per died event and parks it on a presentation-only bridge. The classic `renderCombatOver` renders it as a gold-accented block before the buttons.

The roadmap's "lesser result" line has no surface: the rail is hidden while dead, and the v1.4 ruling retired transient notifications. That line is deliberately dropped, per CONTEXT.

Purpose: the death screen, the most emotional moment in the loop, tells the player when they beat their own record.
Output: the mazeworld.html wiring, the block and its CSS, a bridge registry entry with the regenerated doc table, and test/unit/shell-new-best.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-run-record-personal-bests/65-CONTEXT.md
@.planning/phases/65-run-record-personal-bests/65-03-SUMMARY.md
@.planning/phases/65-run-record-personal-bests/65-04-SUMMARY.md

<interfaces>
- `takeDeathRecord()` (src/browser/engineAdapter.js, Plan 65-04) returns `{ first, newBests, summary } | null`. It is one-shot and is set synchronously inside `dispatch()` on a non-dev died event.
- `newBestView(report)` (src/browser/newBest.js, Plan 65-03) returns `null | { head: string|null, rows: string[], quip: string }`.
- The dead over-panel colours are the existing gold `#e8c97a` (the .cb-over-title / .cb-epitaph accent), body text `#c9bda0`, and the dark panel.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the module-side parcel: import, set on a died event, reset on the title screen, register the bridge</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - mazeworld.html module-script imports (lines ~5370-5372: the pinned engineAdapter import line must stay byte-identical)
    - mazeworld.html module bridge resting values (lines ~5640-5660: window.__mzRail = emptyRail(); window.__mzStair = null;)
    - mazeworld.html dispatchWithNarration (lines ~6236-6283)
    - mazeworld.html showTitleScreen (lines ~6631-6637), window.mzReturnToTitle (~6933) and window.mzAbandonCharacter (~6670)
    - src/browser/bridge.js (the __mzFightEnd and __mzLootReport entries: the presentation-parcel precedent; the file is alphabetically sorted)
    - tools/bridge-doc.mjs (the header: `node tools/bridge-doc.mjs --write` regenerates the docs table)
    - test/unit/bridge-registry.test.js (set equality between live names and BRIDGE, alphabetical order, doc-sync)
  </read_first>
  <action>
1. In the module script, directly after the existing line `import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from "./src/browser/engineAdapter.js";` (leave that line byte-identical, per the repo's separate-import-line precedent), add two lines:
   - `import { takeDeathRecord } from "./src/browser/engineAdapter.js";`
   - `import { newBestView } from "./src/browser/newBest.js";`
   Both carry a "Phase 65 (RUN-04)" comment.

2. After `window.__mzStair = null;` (module resting values, ~5653), add `window.__mzDeathRecord = null;` with a comment: "Phase 65 (RUN-04): the just-died run's NEW PERSONAL BEST block view (src/browser/newBest.js#newBestView over engineAdapter#takeDeathRecord), parked once per died event and read by the classic renderCombatOver on every dead-panel render. Never a field on S/state, same rule as window.__mzFightEnd."

3. In dispatchWithNarration, directly after `const result = dispatch(action);`, add `if (result.events.some((e) => e.type === "died")) window.__mzDeathRecord = newBestView(takeDeathRecord());`. Comment it: taken exactly once, right after the synchronous fold in the adapter. It covers every death path (move, combat including the beat path, abandon), since this is the shell's only dispatch call site.

4. In showTitleScreen, as its FIRST statement, add `window.__mzDeathRecord = null;`. Comment: the title shows after BURY THEM, abandon and save-and-quit, so a later death never renders a stale block.

5. src/browser/bridge.js: add a frozen entry `__mzDeathRecord` in alphabetical position, between `__mzDarkness` and `__mzDescend`:
   - owner: `"mazeworld.html (module)"`
   - consumers: `Object.freeze(["mazeworld.html (classic: renderCombatOver — renders the NEW PERSONAL BEST block on the THAT IS THAT panel)", "mazeworld.html (module: dispatchWithNarration — sets it on a died event; showTitleScreen — resets it to null)"])`
   - purpose: `"Presentation-only parcel of the just-died run's new-personal-best block view; never a field on state."`

6. Run `node tools/bridge-doc.mjs --write` to regenerate the docs/SHELL-MODULES.md bridge table. Do not hand-edit the table.

Keep every new comment free of the retired-surface word that tools/stale-terms.mjs enforces (term id "toast"). Refer to "the rail" or "the death panel" instead.
  </action>
  <verify>
    <automated>node --test test/unit/bridge-registry.test.js test/unit/stale-terms.test.js test/unit/shell-no-content-copies.test.js && node tools/bridge-doc.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from \"./src/browser/engineAdapter.js\";" mazeworld.html` equals 1 (the pinned line is unchanged)
    - `grep -c "import { takeDeathRecord } from \"./src/browser/engineAdapter.js\";" mazeworld.html` equals 1, and `grep -c "import { newBestView } from \"./src/browser/newBest.js\";" mazeworld.html` equals 1
    - `grep -c "window.__mzDeathRecord = newBestView(takeDeathRecord());" mazeworld.html` equals 1
    - `grep -c "__mzDeathRecord" src/browser/bridge.js docs/SHELL-MODULES.md` reports at least 1 for each file, and `node tools/bridge-doc.mjs --check` exits 0
    - `node --test test/unit/bridge-registry.test.js test/unit/stale-terms.test.js` passes
  </acceptance_criteria>
  <done>Each non-dev death parks exactly one view for the dead panel, the title screen clears it, and the new bridge is registered and documented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the NEW PERSONAL BEST block in THAT IS THAT: renderNewBestBlock, its call site, its CSS, and the shell test</name>
  <files>mazeworld.html, test/unit/shell-new-best.test.js</files>
  <read_first>
    - mazeworld.html renderCombatOver (lines ~3994-4069: the `kind === "dead"` lines block, then the `over` section with title, line and actions)
    - mazeworld.html over-panel CSS (lines ~780-790: .cb-over, .cb-over-title, .cb-over-line, .cb-over-lines, .cb-epitaph #e8c97a, .cb-over-actions, .cb-over-btn)
    - mazeworld.html renderEncounter's S.dead branch (lines ~4169-4189) and the rail predicate `railEl.hidden = !!(S.combat || S.dead) || idle;` (~4543)
    - mazeworld.html classic graveyard block comments (lines ~3211-3216, ~3287-3303) that still say the adapter stores 5 stones
    - test/unit/shell-combat-over.test.js (lines 1-130: the CODE/comment-stripping and fnRegion helpers to mirror; its "no innerHTML in renderCombatOver" pin must stay green)
  </read_first>
  <behavior>
    - A fake-DOM test: extract the classic `function renderNewBestBlock(host, view)` source from mazeworld.html, evaluate it with `new Function("document", src + "; return renderNewBestBlock;")` against a minimal fake document (createElement returning { tagName, className, id, textContent, children: [], appendChild }), and check:
      - view null -> host gets no child;
      - view { head: "NEW PERSONAL BEST", rows: ["DEEPEST DESCENT · floor 9", "DEEPEST, FEWEST STEPS · floor 9 · 312 sq"], quip: "Q" } -> host gets one child with className "cb-over-best", whose children are a .cb-over-best-head (textContent "NEW PERSONAL BEST"), two .cb-over-best-row, and one .cb-over-best-quip (textContent "Q"), in that order;
      - view { head: null, rows: [], quip: "First corpse ..." } -> the block holds only the quip
    - Region pins: renderCombatOver contains `if (kind === "dead") renderNewBestBlock(over, window.__mzDeathRecord);`, positioned after `over.appendChild(line);` and before the `cb-over-actions` creation; renderCombatOver and renderNewBestBlock contain no innerHTML
    - The CSS has `.cb-over-best{` with `#e8c97a`, plus `.cb-over-best-head`, `.cb-over-best-row` and `.cb-over-best-quip` rules
    - The rail predicate line `railEl.hidden = !!(S.combat || S.dead) || idle;` is still present verbatim
    - No `S.` assignment introduces a bests or death-record field (the comment-stripped shell has no match for /\bS\.(bests|deathRecord|newBest)\b/)
  </behavior>
  <action>
1. In the classic script, directly AFTER renderCombatOver's closing brace and before `function renderEncounter`, add `function renderNewBestBlock(host, view)`:
   - If `!view || typeof view !== "object"`, return.
   - Create `div.cb-over-best` with id `"cb-over-best"`.
   - If `view.head`, append `div.cb-over-best-head` with textContent view.head.
   - For each string in `Array.isArray(view.rows) ? view.rows : []`, append `p.cb-over-best-row` with textContent = the row.
   - If `view.quip`, append `p.cb-over-best-quip` with textContent view.quip.
   - `host.appendChild(block)`.
   Only createElement, className, id, textContent and appendChild: no innerHTML, and no copy literals (all words come from the view). Comment: "Phase 65 (RUN-04): the gold NEW PERSONAL BEST block on THAT IS THAT (CONTEXT: only a new #1 is announced; a lesser finish stays silent; the rail stays hidden while dead)."

2. In renderCombatOver, directly after `over.appendChild(line);` and before the `if (Array.isArray(opts.buttons) && opts.buttons.length)` actions block, add `if (kind === "dead") renderNewBestBlock(over, window.__mzDeathRecord);`. The block therefore sits under THAT IS THAT / "The maze keeps the rest." and above the buttons (CONTEXT: "before the buttons").

3. CSS, next to the .cb-over-* rules (~line 790), add:
   - `.cb-over-best{margin-top:12px;padding:10px 12px;border:2px solid #e8c97a;background:#1d180f}`
   - `.cb-over-best-head{font-family:var(--disp);font-size:9px;line-height:1.6;color:#e8c97a}`
   - `.cb-over-best-row{font-family:var(--mono);font-weight:700;font-size:14px;line-height:1.4;color:#e8c97a;margin:6px 0 0}`
   - `.cb-over-best-quip{font-family:var(--mono);font-weight:700;font-size:14px;line-height:1.45;color:#c9bda0;margin:8px 0 0}`

4. Comment-only touch-ups in the classic graveyard block. They still say the adapter stores or keeps only 5 stones (near GRAVE_TOTAL_KEY ~3213-3215, the renderGraves count comment ~3287-3289, and "Defensive: the adapter already caps storage at 5" ~3302). Reword them: the adapter now stores up to 60 (Phase 65), and this Dead tab still renders the newest 5 until Phase 66 replaces it with the Leaderboards panel. Do NOT change renderGraves' behaviour or copy: CONTEXT says this phase only keeps the Dead tab from breaking, and it already renders a bounded list.

5. Create test/unit/shell-new-best.test.js covering every `<behavior>` bullet. Reuse the comment-stripping and region-extraction approach from test/unit/shell-combat-over.test.js, reading mazeworld.html with `\r\n` normalised. Also pin Task 1's wiring:
   - the dispatchWithNarration region contains `newBestView(takeDeathRecord())` inside an `e.type === "died"` check;
   - the showTitleScreen region contains `window.__mzDeathRecord = null`.
   The test file must not contain the retired-surface word that stale-terms enforces.
  </action>
  <verify>
    <automated>node --test test/unit/shell-new-best.test.js test/unit/shell-combat-over.test.js test/unit/shell-tab-snapshots.test.js test/unit/stale-terms.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function renderNewBestBlock(host, view)" mazeworld.html` equals 1
    - `grep -c "if (kind === \"dead\") renderNewBestBlock(over, window.__mzDeathRecord);" mazeworld.html` equals 1
    - `grep -c "\.cb-over-best{" mazeworld.html` equals 1, and that rule line contains `#e8c97a`
    - `grep -c "railEl.hidden = !!(S.combat || S.dead) || idle;" mazeworld.html` equals 1 (the rail stays hidden while dead)
    - `node --test test/unit/shell-new-best.test.js` passes with at least 8 tests, including the fake-DOM behaviour tests; `node --test test/unit/shell-combat-over.test.js` passes unedited
    - `npm test` reports 0 failures
  </acceptance_criteria>
  <done>THAT IS THAT shows a gold NEW PERSONAL BEST block (or the first-death line) exactly when the report calls for it, stable across re-renders, rendered with textContent from view copy, with the rail still hidden.</done>
</task>

</tasks>

<verification>
- `npm test` fully green (bridge registry, stale-terms, shell-combat-over, safety scan, parity)
- `node tools/bridge-doc.mjs --check` exits 0
- No Android build, APK install or device step in this plan (deferred-UAT protocol)
</verification>

<success_criteria>
- RUN-04: a new #1 on any board is announced in voice, inside the death panel's own space, as a distinct gold block
- Silent for anything short of a new #1; first-ever death gets its own line
- No GameState field; one registered presentation bridge
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `window.__mzDeathRecord` | presentation bridge (module-owned) | registered in src/browser/bridge.js, documented in docs/SHELL-MODULES.md |
| `renderNewBestBlock(host, view)` | classic shell function | mazeworld.html, after renderCombatOver |
| `.cb-over-best`, `.cb-over-best-head`, `.cb-over-best-row`, `.cb-over-best-quip` | CSS classes | gold #e8c97a accent on the dark over-panel |
| module imports `takeDeathRecord`, `newBestView` | shell wiring | separate import lines |
| test/unit/shell-new-best.test.js | new test file | |

<output>
Create `.planning/phases/65-run-record-personal-bests/65-05-SUMMARY.md` when done. Include a "Human verification (deferred to end of run)" heading listing these checks for the Phase 69 Pixel 7 batch (docs/UAT-v2.0.md), not run now:
(1) on a fresh install, the first death shows the gold block with one first-death line;
(2) a later death deeper than any before shows NEW PERSONAL BEST with DEEPEST DESCENT and DEEPEST, FEWEST STEPS rows plus one quip, sitting above REVIEW THE ORACLE / BURY THEM without pushing the buttons off-screen at the largest text scale;
(3) a shallower death shows no block;
(4) REVIEW THE ORACLE then back to the map shows the same block and the same quip;
(5) BURY THEM, then a new run, then an early death shows no stale block;
(6) the rail never appears over the death panel.
</output>
