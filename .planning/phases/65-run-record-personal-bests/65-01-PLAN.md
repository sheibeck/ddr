---
phase: 65-run-record-personal-bests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/state.js
  - engine/engine.js
  - engine/saveState.js
  - test/unit/acts-counter.test.js
  - test/parity/harness/comparables.js
  - test/parity/movement-parity.test.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements: [RUN-01]
user_setup: []

must_haves:
  truths:
    - "A fresh newRun(seed) GameState carries acts: 0, for a default run, a dev start-at-depth run and a forced-chargen run alike"
    - "Each call to applyAction whose action passes validateAction raises state.acts by exactly 1, including actions the handler then refuses or no-ops (a move into a wall, a camp with no rations)"
    - "An action that fails validateAction (null, {type:'bogus'}, {type:'move', dir:'X'}) returns the SAME state object with acts unchanged"
    - "Boundary: acts is 0 before any action and 1 after the first validated action; a state that lacks acts, or carries a negative, fractional, NaN or string acts, is treated as 0 before the increment"
    - "An old save with no acts key loads through validateSave and rehydrate as acts 0; a negative, fractional, NaN or string acts loads as 0; acts 57 survives serializeRun -> JSON -> validateSave -> rehydrate unchanged"
    - "The counter adds zero rng draws: the same seed and action list give an identical rngState and an identical state apart from acts, whether or not the starting state carried acts"
    - "acts is carved out of all six parity comparables (movementComparable, combatComparable, economyComparable in test/parity/harness/comparables.js plus the test-local comparables in movement-, combat- and magic-parity.test.js), so a state with acts 999 compares equal to the same state with acts 0"
    - "Parity stays green with zero fixture edits: git diff --stat -- test/parity/fixtures/ is empty and test/parity/prototype-master.js.txt keeps hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
    - "FIXTURE-INVENTORY.md declares Phase 65 as a measured zero (the counter moves no fixture), with the measurements that prove it"
  prohibitions:
    - statement: "Recording a run must never change how the run plays: the action counter must not add, remove or reorder any rng draw, or alter any seeded outcome"
      status: unresolved
      verification: judgment
    - statement: "The parity master test/parity/prototype-master.js.txt must never be edited, and no parity fixture may be regenerated in bulk to absorb the new field"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "engine/state.js"
      provides: "acts: 0 on every fresh GameState"
      contains: "acts: 0"
    - path: "engine/engine.js"
      provides: "the once-per-validated-action increment inside applyAction"
      contains: "next.acts"
    - path: "engine/saveState.js"
      provides: "acts whitelisted at both sites (validateSave value, rehydrate state), defaulting to 0"
      contains: "acts:"
    - path: "test/unit/acts-counter.test.js"
      provides: "counter, tolerant-load, zero-rng and comparable carve-out guards"
      min_lines: 80
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "Phase 65 measured-zero declaration"
      contains: "Phase 65"
  key_links:
    - from: "engine/engine.js#applyAction"
      to: "engine/actions.js#validateAction"
      via: "the increment sits after the validateAction early return and right after structuredClone"
      pattern: "next\\.acts"
    - from: "test/parity/harness/comparables.js"
      to: "engine/state.js"
      via: "acts destructured out beside pendingHazard in all three harness comparables"
      pattern: "pendingHazard, acts, \\.\\.\\."
---

<objective>
Give every GameState an action counter, `state.acts`, for the run record (RUN-01, CONTEXT "Run summary fields (RUN-01)", Action count). It starts at 0 in `newRun` and goes up once per validated action inside `applyAction`. It is whitelisted at both `validateSave`/`rehydrate` sites, and old saves load it as 0. It is carved out of all six parity comparables, so the engine gate holds with zero fixture movement, and FIXTURE-INVENTORY.md records that as a measured zero.

Purpose: Plan 65-04's `buildRunSummary` reads `state.acts` into the summary and the integrity hash. It is also the action count later plausibility checks will use.
Output: the engine field, the save whitelist, six comparable carve-outs, the new test/unit/acts-counter.test.js, and a Phase 65 FIXTURE-INVENTORY section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-run-record-personal-bests/65-CONTEXT.md
@engine/engine.js
@engine/state.js

<interfaces>
Downstream contract (Plan 65-04 reads it):
- `state.acts` is a non-negative integer on every GameState that `newRun`, `applyAction`, `validateSave(...).value` or `rehydrate` produces.
- `serializeRun` already spreads the whole state, so `acts` is persisted with no change to serializeRun.

The existing precedent to mirror is `storeRoll`/`pendingHazard`: a new top-level field, set in the newRun literal, whitelisted at BOTH saveState sites and destructured out of every comparable. See the Phase 33/39 comments at each site.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: state.acts — the fresh-run field, the per-validated-action increment, and the tolerant save whitelist</name>
  <files>engine/state.js, engine/engine.js, engine/saveState.js, test/unit/acts-counter.test.js</files>
  <read_first>
    - engine/state.js (the newRun literal, lines 146-259, and its storeRoll/pendingHazard field comments)
    - engine/engine.js (applyAction, lines 35-181: the validateAction early return, the structuredClone, and the rng rehydrate)
    - engine/saveState.js (validateSave's `value` literal at lines 605-661 and rehydrate's `state` literal at lines 703-763; these are the two whitelist sites)
    - test/unit/save-validation.test.js (lines 400-430: the STORE-01 storeRoll tolerant-load tests, the pattern to mirror)
    - engine/actions.js (validateAction, lines 87-130, for invalid-action examples)
    - .planning/phases/65-run-record-personal-bests/65-CONTEXT.md (the "Action count" bullet)
  </read_first>
  <behavior>
    - newRun(1).acts === 0; newRun(1, [], { startDepth: 20 }).acts === 0; newRun(1, [], { force: { cls: "Thief" } }).acts === 0
    - After applyAction(newRun(7), { type: "camp" }), the new state's acts === 1. Three validated actions in a row give 3, even when the handler refuses (a move toward a wall cell)
    - applyAction(s, null), applyAction(s, { type: "bogus" }) and applyAction(s, { type: "move", dir: "X" }) each return `state === s` with s.acts unchanged
    - A hand-built state with no acts key (delete s.acts) gives acts 1 after one validated action. acts -3, 2.5, NaN or "4" also give 1 after one action (coerced to 0 first)
    - The zero-rng pin: run seed 424242 through a fixed 12-action list (moves in every direction, camp, a refused move) twice, once from newRun and once from a copy with acts deleted. The final rngState values are equal, and the final states are deepStrictEqual once acts is removed from both
    - validateSave on a serialized fresh run with the acts key deleted gives value.acts === 0, and rehydrate(value).acts === 0. acts values -1, 1.5, NaN, "9" and null each load as 0. acts 57 round-trips serializeRun -> JSON.stringify -> validateSave -> rehydrate as 57
  </behavior>
  <action>
Implements the CONTEXT decision "Action count", with no dual path (greenfield ruling). Old saves get a tolerant load only.

1. engine/state.js, newRun literal: add `acts: 0,` directly after `steps: 0,`. Give it a short comment block in the file's existing style: "Phase 65 (RUN-01): the run's validated-action count, incremented once per validated action by engine/engine.js#applyAction. Plain assignment, NO rng draw. Read into the run summary and its integrity hash (engine/death.js#buildRunSummary, Phase 65 Plan 04). The parity harness strips it in all six comparables, the same way it strips storeRoll."

2. engine/engine.js, applyAction: directly after `const next = structuredClone(state);` and before `makeRng(next.rngState)`, add `next.acts = (Number.isInteger(next.acts) && next.acts >= 0 ? next.acts : 0) + 1;` with a comment. The comment must say: counted after validateAction passes, whatever the handler then does; no rng draw; the same non-negative-integer coercion validateSave applies, so a hand-built or pre-Phase-65 in-memory state counts from 0. The early-return path for an invalid action is unchanged: it still returns the SAME state object.

3. engine/saveState.js, both whitelist sites:
   - validateSave's `value` literal: add `acts: Number.isInteger(obj.acts) && obj.acts >= 0 ? obj.acts : 0,` directly after `steps,`.
   - rehydrate's `state` literal: add the same expression directly after `steps: obj.steps ?? 0,`.
   Comment both: "Phase 65 (RUN-01): absent on a pre-Phase-65 save, so it loads as 0 and counts from the load point. A tampered value (negative, fractional, NaN, non-number) is never trusted and also loads as 0."

4. Create test/unit/acts-counter.test.js (node:test + node:assert/strict, the same import style as test/unit/save-validation.test.js). It covers every bullet in `<behavior>`. Import newRun and applyAction from engine/engine.js, and validateSave, rehydrate and serializeRun from engine/saveState.js. For the "refused move" case, pick a wall-facing direction from the start cell, mirroring engineAdapter.test.js#firstOpenPlainDir's neighbour scan but looking for a `wall` cell.

Do not add `acts` anywhere else: no event, no narration, no shell reference.
  </action>
  <verify>
    <automated>node --test test/unit/acts-counter.test.js test/unit/save-validation.test.js test/unit/newrun.test.js test/roundtrip/serialize-rehydrate.test.js test/determinism/forced-chargen.test.js test/unit/chargen-rng-pin.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "acts: 0," engine/state.js` shows the field inside the newRun literal, directly after `steps: 0,`
    - `grep -c "next.acts" engine/engine.js` is at least 1, and the increment line appears after the `structuredClone(state)` line (check with grep -n)
    - `grep -c "Number.isInteger(obj.acts)" engine/saveState.js` equals 2 (validateSave value + rehydrate state)
    - `node --test test/unit/acts-counter.test.js` passes with at least 8 tests, covering every `<behavior>` bullet
    - `node --test test/unit/chargen-rng-pin.test.js` passes unedited (it is not in files_modified). That proves newRun's rngState did not move
  </acceptance_criteria>
  <done>Every GameState carries a non-negative integer `acts`. It goes up once per validated action, loads tolerantly from old or tampered saves, and adds no rng draw. The new unit test pins all of this.</done>
</task>

<task type="auto">
  <name>Task 2: carve acts out of all six parity comparables and declare the measured zero</name>
  <files>test/parity/harness/comparables.js, test/parity/movement-parity.test.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/FIXTURE-INVENTORY.md, test/unit/acts-counter.test.js</files>
  <read_first>
    - test/parity/harness/comparables.js (movementComparable 473-530, combatComparable around 584-615, economyComparable around 1111-1140: the destructure lines and their Phase 33/39 comment blocks)
    - test/parity/movement-parity.test.js (line ~63-72), test/parity/combat-parity.test.js (line ~125-133), test/parity/magic-parity.test.js (line ~103-111): the three test-local comparables
    - test/parity/FIXTURE-INVENTORY.md (lines 2530-2610: the "Phase 61 ... Plan 01 — measured zero" section, the exact format to mirror)
    - test/unit/acts-counter.test.js (from Task 1)
  </read_first>
  <action>
Engine gate, per CONTEXT "Established Patterns" and the milestone gates. The CONTEXT names three comparables, but the parity suite has SIX destructure sites that compare an engine state against the frozen prototype: three in the harness and three test-local. `acts` must be stripped at all six, or the three parity test files fail.

1. At each of the six `const { beats, seed, rngState, version, won, ... pendingHazard, ...X } = state;` destructures (comparables.js lines ~519, ~610, ~1137; movement-parity.test.js ~72; combat-parity.test.js ~133; magic-parity.test.js ~111), insert `acts, ` immediately after `pendingHazard, `. Every site then reads `pendingHazard, acts, ...`. Add one comment line above each destructure, mirroring the Phase 39 pendingHazard line: "Phase 65 (RUN-01): strip `state.acts` too, the run's validated-action counter (engine/engine.js#applyAction). Engine-only bookkeeping with no prototype-side equivalent, and a plain strip (no reconcile)."

2. Add to test/unit/acts-counter.test.js a guard test that imports movementComparable, combatComparable and economyComparable from ../parity/harness/comparables.js. For a newRun(31) state s, it asserts that each comparable of `{ ...structuredClone(s), acts: 999 }` deepStrictEquals the same comparable of `{ ...structuredClone(s), acts: 0 }`. If a comparable needs a combat or store sub-state to run, build it the way the existing parity tests do, or document in the test why a fresh state suffices.

3. Measure first, then declare. Before editing (on the Task 1 commit), run `node --test test/parity/*.test.js` and record the pass count. After the carve-outs, re-run and confirm the same pass count, with all parity tests green. Then confirm:
   - `git diff --stat -- test/parity/fixtures/` is empty
   - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
   - `node tools/worn-fixture-scan.mjs | diff --strip-trailing-cr - tools/worn-fixture-scan-output.txt` and `node tools/initiative-fixture-scan.mjs | diff --strip-trailing-cr - tools/initiative-fixture-scan-output.txt` are both empty
   - `node tools/tune-difficulty.mjs --seeds=20 --json` output is byte-identical before and after (save both to the scratchpad, then `cmp`)

4. Append a new top-level section to test/parity/FIXTURE-INVENTORY.md: "## Phase 65: the run record's action counter (RUN-01) — measured zero". Mirror the Phase 61 Plan 01 prose. It needs:
   - **The rule:** `state.acts`, +1 per validated action, zero rng.
   - **The predictor:** exposure is 0 because acts is carved out of all six comparables and changes no handler's behaviour.
   - **The live-scan results:** the four measurements above, with the actual parity pass count.
   - **Byte-identical elsewhere:** fixtures, master and the bot output all unchanged.
   Put it AFTER every existing section. Never inside the generated block that test/parity/fixture-inventory.test.js compares.
  </action>
  <verify>
    <automated>node --test test/parity/ test/unit/acts-counter.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pendingHazard, acts, \.\.\." test/parity/harness/comparables.js` equals 3
    - `grep -c "pendingHazard, acts, \.\.\." test/parity/movement-parity.test.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js` reports 1 for each file
    - `git diff --stat -- test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing
    - `grep -n "## Phase 65" test/parity/FIXTURE-INVENTORY.md` finds the new section, and `node --test test/parity/fixture-inventory.test.js` passes
    - `npm test` reports 0 failures, and the total test count is at least 4197 plus the tests this plan added
  </acceptance_criteria>
  <done>No parity comparable sees `acts`. Parity is green with zero fixture edits and an untouched master. The Phase 65 measured-zero declaration is in FIXTURE-INVENTORY.md with real numbers.</done>
</task>

</tasks>

<verification>
- `npm test` fully green (baseline 4197/4197 at a3b8a6e, plus the new tests)
- `git diff --stat -- test/parity/fixtures/ test/parity/prototype-master.js.txt` empty
- The comparable guard test proves acts 999 ≡ acts 0 for all three harness comparables
</verification>

<success_criteria>
- RUN-01's action count exists on GameState, costs zero rng, and is invisible to parity
- Old saves load with acts 0. Tampered values are neutralised
- The measured zero is declared in FIXTURE-INVENTORY.md
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Where |
|---|---|---|
| `state.acts` | GameState field (non-negative integer, serialized) | engine/state.js#newRun literal |
| once-per-validated-action increment | engine behaviour | engine/engine.js#applyAction |
| `acts` whitelist (x2) | save field | engine/saveState.js#validateSave `value`, #rehydrate `state` |
| `acts` carve-out (x6) | parity harness | test/parity/harness/comparables.js (3), movement/combat/magic-parity.test.js (1 each) |
| test/unit/acts-counter.test.js | new test file | test/unit/ |
| "Phase 65 ... measured zero" | inventory section | test/parity/FIXTURE-INVENTORY.md |

<output>
Create `.planning/phases/65-run-record-personal-bests/65-01-SUMMARY.md` when done. Include the measured numbers (parity pass count before and after, the master hash, scan diffs, bot cmp). Include a "Human verification (deferred to end of run)" heading that reads "None: engine-only, no player-visible change" (the deferred-UAT protocol; the milestone batch is Phase 69's docs/UAT-v2.0.md).
</output>

## Must haves: decision coverage (65-CONTEXT.md)

- D-02 (state.acts counter, carved out of the comparables, whitelisted) and D-11 (old saves load acts as 0) are implemented by this plan.
