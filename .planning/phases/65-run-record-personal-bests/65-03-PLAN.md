---
phase: 65-run-record-personal-bests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - content/boards.js
  - src/browser/newBest.js
  - test/unit/newBest.test.js
  - test/voice/safety-scan.test.js
autonomous: true
requirements: [RUN-04]
user_setup: []

must_haves:
  truths:
    - "content/boards.js holds the voice half of the shared board table: BOARD_COPY keyed by deep, lean, combo, days, kills, purse, yard (the mock's tab order), each with tab, title, rule and unit, and unitOne where a singular exists"
    - "The new-best voice bank has at least 6 NEW_BEST_LINES and at least 3 FIRST_DEATH_LINES, plus NEW_BEST_HEAD 'NEW PERSONAL BEST', all deadpan and family-friendly"
    - "Every new string (BOARD_COPY leaves, NEW_BEST_HEAD, NEW_BEST_LINES, FIRST_DEATH_LINES) is scanned by test/voice/safety-scan.test.js and passes"
    - "newBestView(report) returns null for a null report and for a report with no new best (first false, newBests empty). That keeps a lesser top-ten finish silent"
    - "newBestView({ first: true, ... }) returns { head: null, rows: [], quip } with quip drawn from FIRST_DEATH_LINES: no per-board list on the first-ever death"
    - "newBestView with several boards returns head 'NEW PERSONAL BEST', one row per beaten board in mock tab order formatted TITLE · value (for example 'DEEPEST DESCENT · floor 9'), and exactly one quip from NEW_BEST_LINES"
    - "Idempotency: the quip is chosen deterministically from the run's hash (the same report always yields the same view), with no Math.random, so re-rendering the death panel never reshuffles it"
    - "No string the view produces contains 'WP'; player-facing text says HP, never WP"
  prohibitions:
    - statement: "New-best and first-death copy must stay family-friendly (no profanity, gore or adult content) and stay within the established deadpan voice; it must pass the safety scan"
      status: unresolved
      verification: judgment
    - statement: "The view must not announce a board the report did not list as a strictly-new #1: no rows invented for lesser top-ten finishes or for GRAVEYARD"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "content/boards.js"
      provides: "BOARD_COPY, NEW_BEST_HEAD, NEW_BEST_LINES, FIRST_DEATH_LINES (pure data)"
      exports: ["BOARD_COPY", "NEW_BEST_HEAD", "NEW_BEST_LINES", "FIRST_DEATH_LINES"]
    - path: "src/browser/newBest.js"
      provides: "the pure new-best block view model for the THAT IS THAT death panel"
      exports: ["newBestView", "newBestValueText"]
    - path: "test/unit/newBest.test.js"
      provides: "view-model formatting, silence, first-death and determinism tests"
      min_lines: 80
  key_links:
    - from: "src/browser/newBest.js"
      to: "content/boards.js"
      via: "direct import of the board copy and quip banks"
      pattern: "content/boards\\.js"
    - from: "test/voice/safety-scan.test.js"
      to: "content/boards.js"
      via: "collectAuthoredStrings pushes every new string"
      pattern: "NEW_BEST_LINES"
---

<objective>
Write the voice for the new-best announcement (RUN-04, CONTEXT "New-best announcement"):
- the board copy half of the shared board table (titles, rule lines, units), per CONTEXT "One shared board table": voice copy lives in content/;
- the new-best and first-death quip banks;
- a pure view model, `newBestView(report)`, that turns the adapter's death report into the exact block the THAT IS THAT panel renders.

No shell or DOM code here. Plan 65-05 renders the view.

Purpose: the announcement's words and formatting are tested and safety-scanned before any shell wiring exists.
Output: content/boards.js, src/browser/newBest.js, test/unit/newBest.test.js, and the safety-scan wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/65-run-record-personal-bests/65-CONTEXT.md
@content/epitaphs.js

<interfaces>
The report shape this view consumes (Plan 65-04's `takeDeathRecord()` returns it; build to this contract, you do not need 65-04 to exist):
`{ first: boolean, newBests: string[], summary: RunSummary }`
- `newBests`: board ids from `["deep","lean","combo","days","kills","purse"]`. "yard" never appears.
- `summary`: `{ name, race, sub, cls, level, sp, floor, day, steps, gold, kills, cause, note, epitaph, when, season, seed, acts, hash }`. `hash` is 8 lowercase hex.

The board ids are identical to engine/records.js BOARD_IDS (Plan 65-02, same wave). Do NOT import engine/records.js here. Plan 65-04 adds the cross-check test that BOARD_COPY's keys equal BOARD_IDS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: content/boards.js, the board copy and quip banks, wired into the safety scan</name>
  <files>content/boards.js, test/voice/safety-scan.test.js</files>
  <read_first>
    - content/epitaphs.js (header-comment and pure-data export style)
    - content/safety-wordlist.js (lines 187-240: GORE list and ALLOWLIST, to keep every new word off them)
    - test/voice/safety-scan.test.js (the imports at lines 49-90 and collectAuthoredStrings at lines 292-377; new banks are NOT auto-discovered and must be pushed explicitly)
    - test/determinism/content-is-pure-data.test.js (content modules must export pure JSON data: no functions)
    - .planning/phases/65-run-record-personal-bests/65-CONTEXT.md (the Specifics board table and the quip-voice example)
  </read_first>
  <action>
Create content/boards.js as pure data (no functions, no imports). Header comment: "Phase 65 (RUN-04; the shared board table's voice half, per CONTEXT). The orderings and values live in engine/records.js; this file holds only player-facing copy. Phase 66's panel adds each board's `mark` here from the imported mock (BOARD-03) and may re-word a `rule` line to the mock's exact copy. Boards map to canon fields: squares -> steps, wilmst -> gold, EXP -> sp."

Do NOT add it to content/index.js (Plan 65-04 owns that file this phase). Consumers import it directly, following the precedent of the direct content imports in src/browser/*.js and engine/difficulty.js.

Exports, in exactly this form and key order (the mock's tab order):
- `BOARD_COPY = {`
  - `deep: { tab: "DEEPEST", title: "DEEPEST DESCENT", rule: "Deepest floor reached. Ties go to whoever wasted fewer squares getting there.", unit: "floor" },`
  - `lean: { tab: "LEANEST", title: "DEEPEST, FEWEST STEPS", rule: "Deepest floor for the fewest squares walked. Efficient, right up to the end.", unit: "sq" },`
  - `combo: { tab: "LINEAGE", title: "BY RACE & CLASS", rule: "Your best run for each race and class. Every lineage ends; some end lower.", unit: "floor" },`
  - `days: { tab: "LONGEST", title: "LONGEST HELD OUT", rule: "Most days survived. Endurance is just losing slowly.", unit: "days", unitOne: "day" },`
  - `kills: { tab: "BUTCHERY", title: "MOST KILLS", rule: "Most monsters put down before the dungeon returned the favor.", unit: "kills", unitOne: "kill" },`
  - `purse: { tab: "PURSE", title: "RICHEST CORPSE", rule: "Most wilmst on the body at the end. You cannot take it with you. You tried.", unit: "wilmst" },`
  - `yard: { tab: "GRAVEYARD", title: "YOUR GRAVEYARD", rule: "Everyone you have rolled and lost, deepest first. Not ranked. Nobody here won.", unit: "floor" },`
- `};`
- `NEW_BEST_HEAD = "NEW PERSONAL BEST"`
- `NEW_BEST_LINES` (8 lines, verbatim):
  1. "New personal best. The dungeon has adjusted its expectations of you, slightly."
  2. "A record. It goes on the stone, just under the part where you died."
  3. "Your finest failure yet. The bar was low, and you cleared it lying down."
  4. "Personal best. The previous record holder was also you, and also dead."
  5. "Congratulations. You have never lost this well before."
  6. "The ledger notes an improvement. It is not impressed, but it notes it."
  7. "A new high in a long career of lows. Frame it."
  8. "Best run yet. Your next adventurer now has something to fall short of."
- `FIRST_DEATH_LINES` (3 lines, verbatim):
  1. "First corpse on the books. Every record is yours, for now."
  2. "The ledger opens with you. Someone had to go first."
  3. "One death, one entry, every record. Enjoy the top of a very short list."

Then wire test/voice/safety-scan.test.js:
- Add one import line: `import { BOARD_COPY, NEW_BEST_HEAD, NEW_BEST_LINES, FIRST_DEATH_LINES } from "../../content/boards.js";`, with a "Phase 65 (RUN-04)" comment in the style of the Phase 61/62/63 import comments.
- In collectAuthoredStrings, before `return out;`, push every string leaf of BOARD_COPY with a recursive walk (the walkGearCopy pattern, labelled "BOARD_COPY"), then NEW_BEST_HEAD, then each NEW_BEST_LINES[i] and FIRST_DEATH_LINES[i], with index labels.

The allowlist meta-tests must stay green. None of these words is on the BANNED list; if the scan unexpectedly flags one, reword the line rather than extend ALLOWLIST.
  </action>
  <verify>
    <automated>node --test test/voice/safety-scan.test.js test/determinism/content-is-pure-data.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --input-type=module -e "import('./content/boards.js').then(m=>{if(Object.keys(m.BOARD_COPY).join()!=='deep,lean,combo,days,kills,purse,yard')process.exit(1);if(m.NEW_BEST_LINES.length<6||m.FIRST_DEATH_LINES.length<3)process.exit(1);if(m.NEW_BEST_HEAD!=='NEW PERSONAL BEST')process.exit(1)})"` exits 0
    - `grep -c "content/boards.js" test/voice/safety-scan.test.js` equals 1, and `grep -c "NEW_BEST_LINES" test/voice/safety-scan.test.js` is at least 2 (the import plus the push)
    - `node --test test/voice/safety-scan.test.js` passes, including the "Allowlist is load-bearing" and "Allowlist completeness" meta-tests
    - `node --test test/determinism/content-is-pure-data.test.js` passes
    - `git diff --quiet -- content/index.js` exits 0 (this plan leaves the content barrel untouched)
  </acceptance_criteria>
  <done>The board copy and both quip banks exist as pure content, in voice, and are safety-scanned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/browser/newBest.js, the pure new-best block view model</name>
  <files>src/browser/newBest.js, test/unit/newBest.test.js</files>
  <read_first>
    - content/boards.js (Task 1)
    - src/browser/missLines.js (the precedent for a pure presentation module with a deterministic rotation, never the engine rng)
    - src/browser/upgradeWhy.js (a small pure src/browser formatter module: header and export style)
    - .planning/phases/65-run-record-personal-bests/65-CONTEXT.md ("Several boards at once" and "First-ever death" bullets)
  </read_first>
  <behavior>
    - newBestView(null), newBestView(undefined), newBestView("x") -> null
    - newBestView({ first: false, newBests: [], summary }) -> null (a lesser finish stays silent)
    - newBestView({ first: true, newBests: [], summary }) -> { head: null, rows: [], quip } with quip in FIRST_DEATH_LINES
    - newBestView({ first: false, newBests: ["kills","deep","lean"], summary: { floor: 9, steps: 312, kills: 23, hash: "0000000a", ... } }) -> head "NEW PERSONAL BEST", rows exactly ["DEEPEST DESCENT · floor 9", "DEEPEST, FEWEST STEPS · floor 9 · 312 sq", "MOST KILLS · 23 kills"] (mock tab order, not input order), quip NEW_BEST_LINES[0x0a % NEW_BEST_LINES.length]
    - newBestValueText: "combo" with race "Dwarf", cls "Fighter", floor 4 -> "Dwarf Fighter · floor 4"; "days" with day 1 -> "1 day", day 14 -> "14 days"; "kills" with kills 1 -> "1 kill"; "purse" with gold 1240 -> "1,240 wilmst" (en-US grouping pinned)
    - Unknown ids ("yard", "bogus") in newBests are skipped. If only unknown ids are present and first is false -> null
    - A missing or malformed summary.hash -> index 0 of the bank (no throw)
    - Determinism: calling newBestView twice with the same report returns deepStrictEqual views
    - No produced string matches /\bWP\b/
  </behavior>
  <action>
Create src/browser/newBest.js, a pure ES module. It imports only `{ BOARD_COPY, NEW_BEST_HEAD, NEW_BEST_LINES, FIRST_DEATH_LINES }` from "../../content/boards.js". Header comment: "Phase 65 (RUN-04): the THAT IS THAT death panel's new-personal-best block, as a view model. The shell (mazeworld.html renderCombatOver, Plan 05) renders it with textContent; this module owns the formatting. Pure: no DOM, no storage, no randomness. The quip rotates by the run hash, so re-renders are stable."

Exports:
- `newBestValueText(board, summary)`: the value half of a row. Numbers go through a module-private n(x) (x for a finite number, else 0).
  - deep: `floor ${n(floor)}`
  - lean: `floor ${n(floor)} · ${n(steps)} ${BOARD_COPY.lean.unit}`
  - combo: `${race} ${cls} · floor ${n(floor)}`
  - days: `${d} ${d === 1 ? BOARD_COPY.days.unitOne : BOARD_COPY.days.unit}`
  - kills: the same singular/plural rule with BOARD_COPY.kills
  - purse: `${n(gold).toLocaleString("en-US")} ${BOARD_COPY.purse.unit}`
  - any other id: ""
  The separator is " · " (U+00B7 with a space each side), the same dot the shell's floor/day line uses.
- `newBestView(report)`:
  - If report is not a non-null object, return null.
  - Compute the pick index as `(parseInt(summary.hash, 16) >>> 0) % bank.length` when summary.hash matches /^[0-9a-f]{8}$/, else 0.
  - If report.first === true, return `{ head: null, rows: [], quip: FIRST_DEATH_LINES[pick] }` ("no per-board list, just one line from the bank", per CONTEXT).
  - Otherwise, filter report.newBests (if it is an array) to ids that are own keys of BOARD_COPY, excluding "yard" (GRAVEYARD never announces), and order them by `Object.keys(BOARD_COPY)`. If none remain, return null.
  - Else return `{ head: NEW_BEST_HEAD, rows: ids.map((id) => \`${BOARD_COPY[id].title} · ${newBestValueText(id, summary)}\`), quip: NEW_BEST_LINES[pick] }`.
  - Never throw: guard a missing summary as {}.

Write test/unit/newBest.test.js first (RED) with every `<behavior>` bullet, then implement (GREEN).
  </action>
  <verify>
    <automated>node --test test/unit/newBest.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/newBest.test.js` passes, with at least 10 test() calls
    - newBest.test.js includes a source-scan test: the comment-stripped text of src/browser/newBest.js contains no call to the global random-number API and no reference to the DOM document object (it uses a comment stripper like test/unit/engineAdapter.test.js#stripComments)
    - `grep -n "from \"../../content/boards.js\"" src/browser/newBest.js` finds exactly one import line
    - `node --test test/unit/bridge-registry.test.js` still passes (the new module introduces no window bridge name)
  </acceptance_criteria>
  <done>A tested, deterministic view model turns any death report into either nothing, a first-death line, or a NEW PERSONAL BEST block with ordered board rows and one quip.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/newBest.test.js test/voice/safety-scan.test.js test/determinism/content-is-pure-data.test.js` green
- `npm test` green
</verification>

<success_criteria>
- Every word of the announcement lives in content/ and passes the safety scan
- The block's rows and quip are a pure, deterministic function of the report
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| content/boards.js | new content module | not in the content/index.js barrel (direct import) |
| `BOARD_COPY` | content table | per board: tab, title, rule, unit, unitOne; keys deep/lean/combo/days/kills/purse/yard |
| `NEW_BEST_HEAD` | content string | "NEW PERSONAL BEST" |
| `NEW_BEST_LINES` (8), `FIRST_DEATH_LINES` (3) | content quip banks | safety-scanned |
| src/browser/newBest.js | new presentation module | pure view model |
| `newBestView(report)`, `newBestValueText(board, summary)` | functions | consumed by Plan 65-05 via the module script |
| test/unit/newBest.test.js | new test file | |

<output>
Create `.planning/phases/65-run-record-personal-bests/65-03-SUMMARY.md` when done. List the final copy verbatim, so the user can review the voice in one place. Include a "Human verification (deferred to end of run)" heading: "Voice read-through of BOARD_COPY rule lines and both quip banks (user taste call); Phase 66 reconciles rule lines and adds marks from the mock".
</output>

## Must haves: decision coverage (65-CONTEXT.md)

- D-06 (voice copy in content/boards.js) and D-13 (one NEW PERSONAL BEST block + one quip; first-death line) are implemented by this plan.
