---
phase: 58-motion-pacing
verified: 2026-09-22T23:10:00Z
status: passed
score: 5/5 success criteria verified on automated evidence (orchestrator re-run at HEAD 5fe5d76); device feel deferred to the Phase 60 batch
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - "MOTION-01 (Pixel 7): walking toward a map edge scrolls smoothly, not in jumps; rapid taps never stutter; a drag mid-glide takes over instantly; the ring stays glued to the party; the resting map stays crisp"
  - "MOTION-02 (Pixel 7): MARKS / camp / Settings sheets slide up and down; the ☰ menu drops in and fades; the encounter overlay fades; tabs cross-fade without the map appearing to scroll; the rail slides up and back down with its own card; nothing feels sluggish (~180 ms open / ~120 ms close)"
  - "MOTION-04 (Pixel 7): rail text types on fast (a long card finishes in under a second); tapping a typing card finishes it without dismissing; stair and encounter cards type their line the same way"
  - "MOTION-04 accessibility (Pixel 7, TalkBack): a new rail card is announced in full the moment it appears, and the encounter card is read in full when focused, even mid-typing"
  - "MOTION-03 (Pixel 7): a multi-exchange round plays one exchange at a time at a readable pace; each line types; the foe's HP drops with the line that hit it; buttons return only after the last line; a tap on the panel skips to the end; a killing blow and a death read line by line before the end card"
  - "MOTION-03 audio (Pixel 7): each strike / miss / hurt / kill clip plays as its line appears, not at the start of the round; hurrying plays the rest at once; Sound Off stays silent; a new fight still opens with the family cry"
  - "MOTION-05 (Pixel 7, Android 'Remove animations' on): the map moves in single jumps; every surface appears and disappears instantly with nothing half-visible; a round lands all at once; a whole session (edge walk, every sheet + the ☰ menu, tab switches, rail cards, a full fight) shows no motion and loses no text, sound or card"
gaps: []
---

# Phase 58 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, standing 2026-09-14 ruling)

Goal-backward check of the phase goal: *screens move instead of snapping — the map pans, panels open, combat rounds breathe, and text types itself on — with a reduced-motion path that drops nothing.*

Seven plans. Wave 1 (58-01 motion/cameraGlide/typewriter cores, 58-02 combat-beat core + sfx cue split) ran **in parallel git worktrees**, the first parallel wave of the milestone, after worktrees were re-enabled with `worktree.baseRef: head`. It merged with one trivial add/add conflict on a shared `deferred-items.md` note. Waves 2–6 (58-03..07) ran sequentially on the main tree, because each edits `mazeworld.html`. All 16 user decisions in `58-CONTEXT.md` were accepted as recommended during smart discuss.

## Evidence (orchestrator re-run at HEAD)

| # | ROADMAP success criterion | Result |
|---|---------------------------|--------|
| 1 | The map pans smoothly instead of jumping | `src/browser/cameraGlide.js` has `PAN_MS` 200 and an ease-out, retargetable, cancellable tween (58-01). 58-03 wires one shared instance behind `__mzCameraGlide`. `keepPartyInView` and the ☰ menu's CENTRE MAP (`glideCenterMap`) glide. `centerMap`/`mzCenterMap` stay the instant snap for stairs, teleport, a new run and boot. Drag and pointer-down cancel an in-flight glide first. `positionCanvas` writes a device-pixel-rounded `translate3d`, and the pulse ring moves in lockstep. Tests: `camera-glide.test.js` and `map-pan.test.js`. The stationary-camera ruling is unchanged: this changes how the camera moves, never when. |
| 2 | Menus, panels and tabs open and close with visible motion | 58-04 adds `__mzMotion` (`createPanelMotion`), a timer-driven close (not completion events, so reduced motion can't strand a panel) set to the close duration + 34 ms slack, pinned below `DISMISS_SETTLE_MS`. It covers the rail slide, the sheets, the ☰ menu (160 ms, its own motion, `mwrise` untouched), the encounter overlay and a tab cross-fade. It also fixes the Phase 57 finding: `.mw-rail[hidden]{display:block!important;…}` now beats the global `[hidden]{display:none!important}`, so the rail really slides (verified at HEAD, line 742). Tests: `panel-motion.test.js`. |
| 3 | A combat round plays out with a readable beat between exchanges | `combatBeat.js` (58-02) provides the reveal schedule, per-line HP frames, cue ownership and a lossless `hurry()`. 58-06 wires `__mzBeat` into `engineCombatAction`: the engine still resolves the round at once, and the shell renders the combat body from the beat's frame. Rows reveal about 600–700 ms apart start-to-start and type on. HP and the strike pop move with their line. Buttons and keys arm only after the settle render. A capture-phase `beatHurryTap` (the one sanctioned tap-anywhere exception) and a tab switch both hurry. A killing round reads before the end card. 58-07 moves a combat round's clips onto their lines (`cuesForDispatch` / `playClipIds`); the family cry still fires on the joining dispatch, and the per-dispatch clip SET is unchanged. Tests: `combat-beat.test.js`, `combat-beat-shell.test.js`, `sfx-cues.test.js` and `beat-audio.test.js`; Phase 56's audio tests pass, with its shell-wiring test re-pinned exactly. |
| 4 | Rail and encounter text types on quickly; TalkBack gets the complete text at once | `typewriter.js` (58-01) types at `TYPE_MS_PER_CHAR` 12, capped at `TYPE_MAX_MS` 700 per block. Layout is reserved through `.mw-type-rest`, and `adopt()` keeps a re-render from restarting a block. 58-05 wires `__mzTypewriter`. Rail cards type as one keyed run, and the hold starts from the typing's `onDone`. A tap on typing text completes it; the tap does not dismiss the card. The encounter overlay's line types once per content, with a hidden `#mw-major-desc` holding the complete `spec.line` (`aria-describedby`). The typing element is `aria-hidden` while it types, and `#mw-rail-live` still receives the full text at once. Tests: `typewriter.test.js` and `typed-text.test.js`. |
| 5 | With reduced motion at the OS level, every effect resolves to its end state with nothing lost | One predicate reads the OS setting, `motion.js#prefersReducedMotion` (`REDUCED_MOTION_QUERY`), and it fails to instant. The only `matchMedia` token left in the shell is a comment. One `onReducedMotionChange` subscription drives `settleAllMotion()`, which finishes any glide, close, typing block and beat on a mid-session flip. The CSS blanket rule is byte-identical. `reduced-motion.test.js` grew a section per plan, and 58-07's cross-effect audit proves all four timed effects land synchronously under reduced motion, including the mid-session flip. The shell sandbox defaults to reduced motion, so the whole pre-existing suite exercises the MOTION-05 path. |
| — | Gates | `npm test` **3778 / 0** (3633 at phase start, +145 across seven plans). `npm run build:www` exit 0. `bridge-registry.test.js` 10/10 with four new rows (`__mzCameraGlide`, `__mzMotion`, `__mzTypewriter`, `__mzBeat`). `git diff --stat a01b38e..HEAD -- engine/ content/ test/parity/` **empty**. Master hash `a1f4d0dc…` unchanged. `package.json`/lock untouched. `grep -c Math.random src/browser/sfx.js` = 0. Every touched test file's `^test(` count is ≥ its count at `a01b38e` (orchestrator re-check). |

## Notes the reader should have

- **Two executors went quiet after finishing** (58-04, 58-07). All commits and the SUMMARY were present, the tree was clean, and the transcript sat idle 30 min. The orchestrator verified completion by spot-check (the workflow's completion-signal fallback) and stopped them. No work was lost.
- **Teeth-check reverts wiped uncommitted work twice** (57-04, 58-03). In both cases the executor caught it and redid the edit before committing, so no bad state was ever committed. From 58-04 on, every brief requires `git diff --quiet -- <file>` before any teeth mutation.
- **The worktree line-ending artifact.** Fresh worktrees check out a set of LF fixtures with CRLF (`core.autocrlf=true`, no `.gitattributes` pin), producing 16 false failures in four untouched test files. Master is unaffected (`i/lf w/lf`). This is recorded in `deferred-items.md`. A `.gitattributes` `eol=lf` pin would remove it, and is left as a follow-up.
- **MOTION-05 was checked off early** in REQUIREMENTS.md by 58-03's `requirements.mark-complete`, before 58-07's audit existed. It now stands on that audit, so the checkbox is correct at close.
- **Deviations were all Rule 1/3 and recorded in each SUMMARY:** PERF-01's `performance.now(` census names the two always-live motion clocks as exceptions; `recordingDom.js` gained an additive innerHTML→element fallback, with every pinned snapshot byte-identical; and nine exact-string source anchors were re-pinned after 58-06's `bv` frame threading.
