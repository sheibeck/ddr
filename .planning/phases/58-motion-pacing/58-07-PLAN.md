---
phase: 58-motion-pacing
plan: 07
type: execute
wave: 6
depends_on: ["58-02", "58-06"]
files_modified:
  - mazeworld.html
  - test/unit/beat-audio.test.js
  - test/unit/sfx-settings.test.js
  - test/unit/reduced-motion.test.js
autonomous: true
requirements: [MOTION-03, MOTION-05]

must_haves:
  truths:
    - "Sound fires with its line (D-12). In a combat round, `dispatchWithNarration` still resolves the dispatch's clips exactly once and in the same way (same order, de-duplication, 3-voice cap, counter-driven variation and family cry) through `cuesForDispatch`, which is `clipsForDispatch` with source indexes. Instead of playing them at dispatch, it hands them to `engineCombatAction`, which passes them into `planBeat`. The beat runner then plays each line's clips when that line appears, so the audio no longer gives the outcome away before the text."
    - "Nothing is lost and nothing is added. Across one beat, the clips started are exactly the clips `playForDispatch` would have started for that dispatch, in the same order. Hurrying starts the remaining clips at once. Under reduced motion, or when a round has nothing to reveal, `engineCombatAction` plays the whole list immediately, which is identical to the pre-phase `playForDispatch`."
    - "The Phase 56 contract is preserved (AUD-01..06). With Sound Off, a whole beat opens no audio device and starts no voice, because `playClipIds` goes through the same guarded `playClips`. Clip variation rotates across beats exactly as across dispatches. The family cry on `combatJoined` still plays at dispatch time, because the joining dispatch is a `move`, not a combat round. Every out-of-combat action (move, camp, store, joiner, inventory) still plays at dispatch time; `engineCombatAction` is the only caller that defers. Phase 56's tests are re-pinned to the new exact signature, never loosened."
    - "MOTION-05 holds across the whole phase, proven in one ledger (`reduced-motion.test.js`). There is ONE reduced-motion predicate: the query string exists in JS only in `src/browser/motion.js`. Every JS-timed controller (camera glide, panel close, typewriter, combat beat) is constructed with `reduced: () => prefersReducedMotion(window)`. The pinned blanket CSS rule covers every transition and animation. `settleAllMotion()` lands all four effects, and `onReducedMotionChange` calls it exactly once per change to reduced. Under reduced motion every effect reaches its end state in the same tick with nothing lost, and a mid-session switch to reduced lands every in-flight effect at once."
    - "The Modularity gate holds. `paint()` and `draw()` bodies are byte-identical (comment-stripped) to BASE_58's; every Phase 58 effect lives in a `src/browser/` module behind one bridge (`__mzCameraGlide`, `__mzMotion`, `__mzTypewriter`, `__mzBeat`); `bridge-registry.test.js` is green."
    - statement: "Phase gate: `npm test` reports fail 0; `npm run build:www` exits 0; `git diff --stat \"$BASE_58\"..HEAD -- engine/ content/ test/parity/` is empty; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff \"$BASE_58\"..HEAD -- package.json package-lock.json` is empty (no new dependency, offline gate)."
      verification: "run by Task 3 of this plan"
    - statement: "On the Pixel 7, in a fight each strike, miss, hurt and kill sound plays as its line appears (not all at the start of the round); hurrying plays the rest at once; Sound Off stays silent; a new fight still opens with the monster family's cry."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "With Android's Remove animations on, a whole session (walking to an edge, opening each sheet and the ☰ menu, switching tabs, reading rail cards, a full fight) shows no motion and loses no text, sound or card."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
  artifacts:
    - test/unit/beat-audio.test.js
    - test/unit/reduced-motion.test.js
  key_links:
    - "`dispatchWithNarration(action, opts = {})`: when `opts.cues` is an array, it receives `cuesForDispatch(...)` and nothing plays; otherwise `playForDispatch(...)` plays as before. `playForDispatch(` and `cuesForDispatch(` each occur exactly once in its body, after `dispatch(action)`."
    - "`engineCombatAction` is the only caller passing `{ cues }`. It forwards them to `planBeat({ cues })` when a beat starts, or plays them all through `playClipIds` before its paint/render tail when none does."
  prohibitions:
    - "MUST NOT change any Phase 56 clip mapping, cap, variation, family table or the Settings Sound gate; MUST NOT loosen a Phase 56 assertion (re-pin to exact new source only)."
    - "MUST NOT defer audio for any dispatch other than `engineCombatAction`'s."
    - "MUST NOT fire haptics per line (a Deferred Idea)."
    - "MUST NOT modify engine/, content/, test/parity/, package.json or package-lock.json; MUST NOT edit REQUIREMENTS.md, ROADMAP.md or STATE.md (the orchestrator owns them)."
    - "MUST NOT delete or loosen any test."
---

<objective>
Move each combat clip onto the line that caused it, re-pin Phase 56's shell-wiring tests to the new exact seam, close the phase with a single cross-effect MOTION-05 audit, and run the phase gate.

Per D-12 (sound fires with its line; the Phase 56 contract preserved and re-pinned), D-17 (reduced motion = the OS preference, every effect instant, one predicate) and the Modularity and Offline gates (D-NN numbered in 58-CONTEXT.md's decision-bullet order: map pan D-01..D-04, panels D-05..D-08, combat beat D-09..D-12, typed text and reduced motion D-13..D-17).

Purpose: MOTION-03's audio half (the Phase 56 pointer: "note whether any fight resolves too fast for its own audio to read"), and the phase-level proof of MOTION-05 across all four effects.

Output: the deferred-audio seam, `beat-audio.test.js`, the re-pinned `sfx-settings.test.js`, the audit section of `reduced-motion.test.js`, and the phase gate recorded in the SUMMARY with the batched Pixel 7 checklist for Phase 60.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-motion-pacing/58-CONTEXT.md
@.planning/phases/58-motion-pacing/58-02-SUMMARY.md
@.planning/phases/58-motion-pacing/58-06-SUMMARY.md
@.planning/phases/56-sound-effects-audio-settings/56-04-SUMMARY.md
</context>

<discovery>
## Read before Task 1

**Record the base.** BASE_58 = `git log --diff-filter=A --format=%H -- .planning/phases/58-motion-pacing/58-CONTEXT.md | tail -1`. Re-read the landed `dispatchWithNarration`, `engineCombatAction`, `settleAllMotion` and every Phase 58 bridge by name.

**Why only engineCombatAction defers.** `dispatchWithNarration` has nine callers (the move path, camp, store buy/leave, joiner resolve/dismiss, abandon, the inventory actions, and `engineCombatAction`). Only `engineCombatAction` produces a combat round the beat reveals. The family cry comes from `combatJoined`, which the engine emits on the `move` (or `camp`) dispatch that starts a fight. That dispatch stays on `playForDispatch`, so the cry still plays the instant the encounter appears. A gear change made from the Gear tab mid-fight goes through the inventory path and also keeps dispatch-time audio.

**Phase 56's shell-wiring pin.** `test/unit/sfx-settings.test.js` Half B extracts `dispatchWithNarration`'s body with the regex `function dispatchWithNarration\(action\)\s*\{`, asserts `playForDispatch(` appears exactly once inside it after `dispatch(action)`, and has a TEETH test on a synthetic source. The new signature `dispatchWithNarration(action, opts = {})` requires re-pinning that regex to the exact new signature. Keep "exactly once" and "after `dispatch(action)`", and add the `cuesForDispatch(` twin. Never widen it to accept any signature.
</discovery>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Name | Notes |
|---|---|---|
| Changed module-script function | `dispatchWithNarration(action, opts = {})` | Defers clips into `opts.cues` when asked; otherwise unchanged. |
| Changed module-script function | `engineCombatAction` | Collects cues, forwards them to the beat, or plays them at once. |
| New test | `test/unit/beat-audio.test.js` | Per-line playback, parity with `playForDispatch`, Sound Off, variation, cry. |
| Re-pinned test | `test/unit/sfx-settings.test.js` Half B | Exact new signature; `playForDispatch(` and `cuesForDispatch(` each exactly once. |
| Audit | `test/unit/reduced-motion.test.js` "audit" section | One predicate, four controllers, settle-all, mid-session flip, `paint`/`draw` untouched. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Defer a combat round's clips from dispatchWithNarration into the beat</name>
  <files>mazeworld.html</files>
  <read_first>
    - `mazeworld.html` module script `dispatchWithNarration(action)` in full: the `audioCtx` block and its Phase 56 comment, and the one `playForDispatch(action.type, result.events, audioCtx);` call.
    - `mazeworld.html` `engineCombatAction(type, extra)` as left by 58-06: `before`/`beforeLog`, the `planBeat({...})` call, `beatRunner.start(plan)` plus `return`, and the paint/render tail.
    - `src/browser/sfx.js#cuesForDispatch` and `#playClipIds` (58-02), and the module script's sfx.js import line.
  </read_first>
  <action>
Per D-12.

Add `cuesForDispatch` to the module script's sfx.js import. Change the signature to `function dispatchWithNarration(action, opts = {})`. Replace the single `playForDispatch(action.type, result.events, audioCtx);` statement with an if/else. When `Array.isArray(opts.cues)`, push every entry of `cuesForDispatch(action.type, result.events, audioCtx)` into `opts.cues`. Otherwise call `playForDispatch(action.type, result.events, audioCtx)`. Extend the Phase 56 comment above it with a Phase 58 paragraph (D-12): a combat round's clips are resolved here exactly as before (same order, de-dupe, cap, variation and cry) and handed to `engineCombatAction` to fire with their lines, and every other dispatch still plays at once. Keep the `audioCtx` block and every other statement byte-identical. The function still returns the dispatch result unchanged.

In `engineCombatAction`: declare `const cues = [];` before the dispatch and call `dispatchWithNarration({ type, ...extra }, { cues })`. Add `cues` to the `planBeat({...})` argument. On the path where no beat starts (reduced motion, no plan, or `start` returned false), call `playClipIds(cues.map((c) => c.clip));` immediately before the existing `window.paint();`. Comment: this is identical to the pre-phase `playForDispatch`, with the same clips in the same order. Change no other caller of `dispatchWithNarration`.
  </action>
  <acceptance_criteria>
    - Node checks over `stripHtml(mazeworld.html)`: exactly one `function dispatchWithNarration(action, opts = {})`; inside its body (sliced to its matching close brace, the way sfx-settings.test.js's `extractFunctionBody` does), `playForDispatch(` and `cuesForDispatch(` each occur exactly once, both after `dispatch(action)`, and inside one `if (Array.isArray(opts.cues))` / `else` pair.
    - Exactly one `dispatchWithNarration(` call site passes a second argument, and it is inside `engineCombatAction` and passes `{ cues }`.
    - The `engineCombatAction` slice's `planBeat({` argument contains `cues`, and `playClipIds(cues.map((c) => c.clip));` precedes `window.paint();`.
    - `npm run build:www` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www</automated>
  </verify>
  <done>A combat round's clips are resolved once as before and fire with their lines; every other dispatch, and the no-beat path, sounds exactly as in Phase 56.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: beat-audio.test.js and the exact re-pin of Phase 56's shell-wiring tests</name>
  <files>test/unit/beat-audio.test.js, test/unit/sfx-settings.test.js</files>
  <read_first>
    - `test/unit/sfx-settings.test.js` in full: Half A's fake-backend pattern, and Half B's `extractFunctionBody`, the `playForDispatch(` exactly-once test, the `hapticForEvents` call-site test and the TEETH test.
    - `test/unit/sfx.test.js` `makeFakeBackend` and how it isolates module state between tests.
    - `test/unit/combat-beat.test.js` (58-02) and `test/unit/harness/fakeClock.js` (58-03).
  </read_first>
  <behavior>
    - Using real sfx.js with a fake backend unlocked under Sound On, a real createBeat/createBeatRunner on a fake clock, and a plan built by planBeat from a REAL multi-line combat round with cues from cuesForDispatch: at start only line 0's clips start; each later offset starts exactly that line's clips; the full sequence of started clips across the beat equals clipsForDispatch(sameArgs) computed on a parallel fresh variation.
    - runner.hurry() after line 0 starts every remaining clip at once, in order, and the total equals the no-beat total.
    - Two consecutive beats of single-hit rounds start hit1 then hit2, the same rotation two consecutive playForDispatch calls produce.
    - With Sound Off (applySfxSettings({ sound: false })), unlock plus a full beat plus a hurry record zero open() and zero start() calls.
    - The family cry: cuesForDispatch("move", [{ type: "combatJoined" }], { combatType: "Beasts" }) yields [{ clip: "enemy-beast", idx: 0 }], and the stripped module script shows the move path calls dispatchWithNarration with one argument (dispatch-time audio).
  </behavior>
  <action>
Per D-12. Create `test/unit/beat-audio.test.js` with one named test per `<behavior>` bullet, reusing the fake-backend shape and state isolation from `test/unit/sfx.test.js` and a real combat round from the helper 58-06 used.

Re-pin `test/unit/sfx-settings.test.js` Half B, never loosening:
- Change the `extractFunctionBody` regex for `dispatchWithNarration` to the exact new signature `function dispatchWithNarration\(action, opts = \{\}\)\s*\{`.
- Keep the assertions that `playForDispatch(` appears exactly once, inside that body, after `dispatch(action)`.
- Add that `cuesForDispatch(` also appears exactly once there, after `dispatch(action)`, and that the two sit in one `Array.isArray(opts.cues)` if/else.
- Update that test's name to mention the Phase 58 deferral, and add a comment naming Phase 58 (D-12).
- Leave the other Half B tests, the TEETH test and all of Half A byte-identical.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/beat-audio.test.js test/unit/sfx-settings.test.js test/unit/sfx.test.js test/unit/sfx-map.test.js test/unit/sfx-cues.test.js` exits 0.
    - `git show "$BASE_58":test/unit/sfx-settings.test.js | grep -c '^test('` is less than or equal to `grep -c '^test(' test/unit/sfx-settings.test.js`, and the diff of that file against BASE_58 touches only the re-pinned test.
    - Teeth check, recorded in the SUMMARY: temporarily make the runner's `onLine` skip `playClips` when `info.hurried` is set, and confirm beat-audio's hurry test FAILS. Revert with `git checkout -- src/browser/combatBeat.js`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/beat-audio.test.js test/unit/sfx-settings.test.js</automated>
  </verify>
  <done>Per-line playback, parity with Phase 56's clip set, hurry, variation, Sound Off and the family cry are proven; Phase 56's wiring pin is re-pinned exactly.</done>
</task>

<task type="auto">
  <name>Task 3: The cross-effect MOTION-05 audit, the modularity proof, and the phase gate</name>
  <files>test/unit/reduced-motion.test.js</files>
  <read_first>
    - `test/unit/reduced-motion.test.js` as left by 58-03..58-06 (the pan, panels, typing and beat sections).
    - `mazeworld.html` module script: `settleAllMotion`, the `onReducedMotionChange(window,` subscription, and the four controller constructions (`createCameraGlide(`, `createPanelMotion(`, `createTypewriter(`, `createBeat(`).
    - `tools/ident-sweep.mjs` (`stripJs`, `stripHtml`) and `tools/shell-sweep.mjs` (the `orphans` subcommand).
  </read_first>
  <action>
Per D-17 and the Modularity gate. Append an "audit" section to `test/unit/reduced-motion.test.js`, one named test per claim:
1. One predicate: across the stripped script regions of `mazeworld.html` and every stripped `src/browser/*.js`, the text `prefers-reduced-motion` occurs only in `src/browser/motion.js` (as `REDUCED_MOTION_QUERY`). In the style blocks, the blanket rule `@media (prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}` occurs exactly once.
2. Four controllers, one predicate: each of the four constructor call slices in the module script contains `reduced: () => prefersReducedMotion(window)`, and `prefersReducedMotion` is imported exactly once, from `./src/browser/motion.js`.
3. Settle-all: `settleAllMotion`'s stripped body calls the camera glide's `finish`, `panelMotion.finishAll()`, `typewriter.completeAll()` and `beatRunner.hurry()`. `onReducedMotionChange(window,` occurs exactly once, and its callback calls `settleAllMotion()` only when the new value is true.
4. Mid-session flip (behaviour): build REAL instances of all four controllers on one fake clock sharing one switchable `reduced` flag. Start a glide, a panel close, a typing block (spy `onDone`) and a beat (spy `onLine`/`onEnd`) with the flag false. Flip it to true and run the same four settle calls `settleAllMotion` makes. Assert that the glide applied its exact target, the element is hidden, the text is full with `onDone` fired once, and every line plus `onEnd` fired once. Advancing the clock afterwards changes nothing.
5. Nothing lost under reduced (smoke): in the default (reduced) sandbox with `stubRail: false`, one test performs a pan nudge, a sheet close, a tab switch, a new rail card and a beat start, and asserts every end state in the same tick: `cam` on target, the sheet hidden, the old screen hidden, the card's full text with its hold scheduled, and every beat line visible and settled.
6. Modularity: the comment-stripped bodies of `function paint(` and `function draw(` are unchanged by Phase 58. BASE_58 (a01b38e) predates 57-05's HUD work, which legitimately changed the shell, so do NOT compare against it here. Use PRE58, the commit just before Phase 58's first code landed: `git rev-parse "$(git log --diff-filter=A --format=%H -- src/browser/motion.js | tail -1)^"` (equivalently, the Phase 57 closing commit the orchestrator recorded in STATE.md). Before writing this test, compute the SHA-256 of each stripped body from `git show "$PRE58":mazeworld.html` (a one-off node script) and pin the two hex digests as literals in the test, with a comment naming PRE58's hash and the command used. The test hashes the working tree's stripped bodies and compares. This keeps `npm test` independent of git history while still proving no Phase 58 statement grew either body.

Then run the phase gate and record every result in the SUMMARY:
- `npm test` reports fail 0.
- `npm run build:www` exits 0.
- `git diff --stat "$BASE_58"..HEAD -- engine/ content/ test/parity/` is empty.
- `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
- `git diff "$BASE_58"..HEAD -- package.json package-lock.json` is empty.
- `node --test test/unit/bridge-registry.test.js` is green.
- `node tools/shell-sweep.mjs orphans` does not list `applyCam`, `glideCenterMap`, `showPanel`, `hidePanel` or `beatHurryTap`.

Finally, write into the SUMMARY a "Phase 60 batched Pixel 7 checklist" that collects every deferred device check from plans 58-03..58-07 verbatim (the orchestrator lifts it into VERIFICATION.md's `human_verification`), plus one TalkBack item and one Remove-animations item.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/reduced-motion.test.js` exits 0 with the pan, panels, typing, beat and audit sections.
    - Teeth check, recorded in the SUMMARY: temporarily delete `typewriter.completeAll();` from `settleAllMotion` and confirm audit test 3 FAILS. Revert with `git checkout -- mazeworld.html`.
    - Phase gate: every command listed in the action passes as stated.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>One ledger proves MOTION-05 for every effect, mid-session included; paint and draw are untouched; the phase gate is green and the Phase 60 device checklist is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings Sound gate → audio device | Sound Off must open nothing, whenever the clip fires. |
| OS reduced-motion setting → every effect | One predicate governs all timed effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-22 | Denial of Service (AUD-05) | A deferred clip opening the audio device under Sound Off | high | mitigate | Deferred clips play only through `playClipIds` then `playClips`, which is guarded; beat-audio test 4 records zero open/start. |
| T-58-23 | Repudiation (audio parity) | The beat dropping or duplicating a clip | medium | mitigate | `clipsForDispatch` is the projection of `cuesForDispatch`; beat-audio tests 1-3 compare started clips to Phase 56's list. |
| T-58-24 | Information Disclosure (accessibility) | A reduced-motion user losing a card, line or sound | high | mitigate | Audit tests 1-5: one predicate, four controllers, settle-all, the flip, and the same-tick smoke test. |
| T-58-SC | Tampering | npm installs | high | mitigate | `package.json`/`package-lock.json` diff against BASE_58 is empty (gate). |
</threat_model>

<verification>
- `npm test` reports fail 0; `npm run build:www` exits 0.
- `git diff --stat "$BASE_58"..HEAD -- engine/ content/ test/parity/` is empty; the prototype master hash is unchanged; `package.json`/`package-lock.json` are unchanged.
- `bridge-registry.test.js` is green.
</verification>

<success_criteria>
- Each combat clip plays when its line appears; the per-dispatch clip set, variation, cry and Sound Off gate are exactly Phase 56's.
- MOTION-05 is proven phase-wide: one predicate, four controllers, settle-all, mid-session flip, nothing lost.
- `paint()`/`draw()` are untouched; the engine and parity gates hold; the Pixel 7 checklist is recorded for Phase 60.
</success_criteria>

<output>
Create `.planning/phases/58-motion-pacing/58-07-SUMMARY.md` when done.
</output>
