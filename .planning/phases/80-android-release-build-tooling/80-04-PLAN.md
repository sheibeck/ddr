---
phase: 80-android-release-build-tooling
plan: 04
type: execute
wave: 2
depends_on:
  - 80-01
  - 80-02
  - 80-03
  - 80-06
files_modified:
  - docs/ANDROID-DISPLAY.md
  - docs/RELEASING.md
  - store-listing/LISTING.md
  - tools/android-api-scan.mjs
  - test/unit/android-api-scan.test.js
autonomous: true
requirements:
  - DROID-01
  - DROID-02
  - DROID-03
must_haves:
  truths:
    - "A precondition task STOPS the plan (no build, no file change) unless Phase 79.1's VERIFICATION.md exists without gaps_found, the four code-part SUMMARYs (80-01, 80-02, 80-03, 80-06) exist, the working tree is clean, and no android/keystore.properties is present (so the build can never be upload-key-signed)"
    - "Phase 80 makes exactly ONE release build, here: one `node tools/gradle.mjs bundleRelease assembleRelease` invocation after a cap sync that regenerates the tracked Capacitor files byte-identically; it is unsigned, and a scratch copy of the APK is signed with the Android debug keystore for the emulator; the upload key is never used"
    - "The build's outputs are persisted outside the repo (C:/projects/mazeworld-build/phase80-<short-sha>/: the AAB, the unsigned and debug-signed APKs, mapping.txt and R8's other reports, the generated capacitor.plugins.json, BUILD-INFO.txt, SHA256SUMS.txt), and the SUMMARY records BUILD_COMMIT and ARTIFACT_DIR as key lines, so 80-05 installs this same build"
    - "R8 is proven from that build: every capacitor.plugins.json classpath, com.getcapacitor.plugin.SystemBars and com.getcapacitor.MessageHandler (postMessage unrenamed) keep their names in mapping.txt; androidx classes are renamed; no com.capacitorjs.plugins.statusbar class survives; the AAB embeds BUNDLE-METADATA/com.android.tools.build.obfuscation/proguard.map; aapt2 still lists drawable/splash_screen and string/game_services_project_id; the APK carries assets/public/index.html and assets/capacitor.config.json"
    - "The deprecated-API scan of that build with --fail-on com.capacitorjs.plugins.statusbar,com.darktierstudios.delvedierepeat exits 0 with zero unresolved callers; every remaining call site is listed with owner and reachability, and the splash-screen legacy-method question is answered from the build"
    - "The merged manifest in that build carries android:appCategory with the game value on <application> and the portrait screenOrientation on MainActivity"
    - "The letterbox check passes on the finished milestone code (overlays added by Phases 76-79 stay in the column)"
    - "No check failure is patched and rebuilt inside this plan: a failed build or a failed app-side check stops the plan and becomes a gap for a code-part gap-closure plan followed by a fresh single build; the full npm test is not run here (build/verify-only)"
    - "docs/ANDROID-DISPLAY.md gains the edge-to-edge and audit sections, docs/RELEASING.md records the measured sizes and the build commit, and store-listing/LISTING.md gains a dated package addendum"
  artifacts:
    - path: docs/ANDROID-DISPLAY.md
      provides: "the edge-to-edge and deprecated-API audit sections, from the single build"
      contains: "## Deprecated window API audit"
    - path: docs/RELEASING.md
      provides: "the measured sizes and build commit of the first R8 release"
      contains: "Measured on the Phase 80 build"
    - path: store-listing/LISTING.md
      provides: "the Phase 80 package addendum"
      contains: "Phase 80"
  key_links:
    - from: "the single build's persisted artifacts (ARTIFACT_DIR)"
      to: ".planning/phases/80-android-release-build-tooling/80-05-PLAN.md"
      via: "BUILD_COMMIT and ARTIFACT_DIR key lines plus SHA256SUMS.txt; 80-05 installs the debug-signed APK from here and never rebuilds"
      pattern: "ARTIFACT_DIR:"
    - from: docs/RELEASING.md "Verifying a release build" (80-01)
      to: "this plan's check task"
      via: "the recipe is executed step by step against the single build"
      pattern: "Verifying a release build"
    - from: tools/android-api-scan.mjs (80-01)
      to: "ARTIFACT_DIR/app-release.aab + mapping.txt"
      via: "dex extraction, dexdump, de-obfuscation"
      pattern: "android-api-scan"
  prohibitions:
    - statement: "MUST NOT run more than one release build, rebuild after a failed check, or edit app code or build configuration in this plan; any fix goes through a code-part gap-closure plan and a fresh single build (user ruling 2026-09-26)"
      requirement_id: DROID-01
      category: consent
      status: resolved
      verification: judgment
    - statement: "MUST NOT sign any artifact with the Play upload key, copy or read android/keystore.properties, push to Play, or bump android/version.properties"
      requirement_id: DROID-01
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT report the deprecated-API or display-configuration warnings as resolved without evidence, or narrow the scanned API list or --fail-on prefixes to make a finding disappear; residual call sites are listed with owners and the final verdict waits on the next Play pre-launch report"
      requirement_id: DROID-02
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
BUILD PART (milestone end, after Phase 79.1): make Phase 80's ONE release build and run every build-dependent check against it (R8 keeps, the embedded mapping, shrink survivors, the status-bar plugin gone, the deprecated-API scan, the merged manifest), persist the build for 80-05's emulator pass, and document edge-to-edge handling and the audit.

Purpose: DROID-01 (R8 proven from real output, the mapping ships), DROID-02 (no deprecated call from the app or the removed plugin in the shipped bytecode) and DROID-03 (the game category really ships). User rulings 2026-09-26: no release build while code is still changing; every code and config change landed first (80-01, 80-02, 80-03, 80-06, and the gameplay phases through 79.1); now there is exactly one build, and every build-dependent check runs against it. This plan changes no app code; the full test suite does not run here.

Output: the persisted build in C:/projects/mazeworld-build/phase80-<short-sha>/, the check results, the edge-to-edge and audit sections of docs/ANDROID-DISPLAY.md, the measured sizes in docs/RELEASING.md, and the LISTING.md addendum.
</objective>

## Artifacts this phase produces

Phase 80 is split in two (user rulings 2026-09-26): a **CODE part** (wave 1, no builds, running in parallel with Phases 75.3-79.1) and a **BUILD part** at the very end of the milestone (gated on Phase 79.1's VERIFICATION.md) with exactly one release build.

CODE part (wave 1):
- 80-01: R8 config for release (`android/app/build.gradle`), the defensive `android/app/proguard-rules.pro`, `android/app/src/main/res/raw/keep.xml`, `test/unit/android-r8.test.js`, the scanner `tools/android-api-scan.mjs` + `test/unit/android-api-scan.test.js`, the R8 / mapping / verification sections of `docs/RELEASING.md`
- 80-02: `@capacitor/status-bar` uninstalled (package files, `tools/build-www.mjs`), the cap-sync-regenerated Capacitor Gradle files, `SystemBars` in `src/browser/nativeChrome.js`, the `capacitor.config.json` SystemBars config, `styles.xml` theme colours, `test/unit/android-system-bars.test.js`, the migrated `test/persistence/lifecycle.test.js`
- 80-03: `android:appCategory="game"`, the delimited `<style id="mw-letterbox">` block in `mazeworld.html`, `tools/letterbox-check.mjs`, `test/unit/android-large-screen.test.js`, `docs/ANDROID-DISPLAY.md` (created), browser screenshots in `80-screens/`
- 80-06: the fit tool's `--transcript` and `--force-infeasible` flags, `appendTranscript`, `test/difficulty/fit-difficulty-cli.test.js`, the closed TOOL-01 todo

BUILD part (milestone end):
- **This plan (80-04):** the ONE release build (unsigned, plus a debug-signed APK copy, persisted with SHA-256 sums in `C:/projects/mazeworld-build/phase80-<short-sha>/`, outside the repo), every build-dependent check (R8 keeps, the embedded mapping, shrink survivors, no status-bar class, the deprecated-API scan, the merged manifest), the end-state letterbox re-check, the edge-to-edge and audit sections of `docs/ANDROID-DISPLAY.md`, measured sizes in `docs/RELEASING.md`, the `store-listing/LISTING.md` addendum
- 80-05: the emulator pass on that same build (no rebuild) with screenshots in `80-screens/`, the verification section of `docs/ANDROID-DISPLAY.md`, the milestone-close device checklist and the ask-first Play push offer

## Flagged assumptions (surfaced, not silently decided)

- **DROID-01, DROID-02 and DROID-03, edge probe: unclassified, reviewed manually.** The build-side edges are what R8 keeps or strips: reflective entry points (plugin classes, annotated methods, JavascriptInterface methods), name-loaded resources, and library back-compat paths (androidx.core's system-UI code for API 20-29, the splash-screen plugin's config-gated immersive and fullscreen paths) that survive shrinking even when unreachable here. Each has an explicit check; whether Play's pre-launch analysis flags the residual library calls is only known after the next upload (milestone-close item).
- **First R8 run.** R8 may stop on missing classes. Under the one-build ruling this plan does not add rules and rebuild: it stops, records R8's missing_rules.txt, and hands the gap to a code-part gap-closure plan, after which this plan runs again as the single build.
- **Research assumption A2** (R8 may keep the splash-screen plugin's legacy methods) is answered from this build, not assumed.
- **Scanner fixes are not app changes.** If the scanner misreads the real build output, fixing tools/android-api-scan.mjs (with a matching test case, run on its own) does not touch the build and is allowed; nothing else in the repo's code changes here.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-android-release-build-tooling/80-RESEARCH.md
@.planning/phases/80-android-release-build-tooling/80-01-SUMMARY.md
@.planning/phases/80-android-release-build-tooling/80-02-SUMMARY.md
@.planning/phases/80-android-release-build-tooling/80-03-SUMMARY.md
@docs/RELEASING.md
@docs/ANDROID-DISPLAY.md

<environment>
Run in a git worktree (the precondition refuses a tree that holds android/keystore.properties). In the worktree: `npm ci`, then create android/local.properties with only the sdk.dir line from C:/projects/mazeworld/android/local.properties (not a secret). Never copy, read, create or commit android/keystore.properties or a .jks. Tools: SDK = C:/Users/Dell/AppData/Local/Android/Sdk (build-tools/36.0.0: aapt2.exe, apksigner.bat, zipalign.exe, dexdump.exe; cmdline-tools/latest/bin/apkanalyzer.bat); JDK = the path in android/gradle.properties org.gradle.java.home. Debug keystore for the emulator copy: %USERPROFILE%/.android/debug.keystore, store and key password "android", alias "androiddebugkey". ARTIFACT_DIR = C:/projects/mazeworld-build/phase80-<first 8 chars of BUILD_COMMIT>/ (outside the repo, never committed). Never run npm run play:release or version:bump, and never run the full npm test here.
</environment>

<interfaces>
- docs/RELEASING.md "### Verifying a release build" (written by 80-01): the exact check recipe this plan executes.
- tools/android-api-scan.mjs (80-01): flags --aab, --apk, --mapping, --fail-on, --json; exit 0 clean, 1 a --fail-on match, 2 tooling failure; reports an unresolved count for callers that stay obfuscated.
- tools/letterbox-check.mjs (80-03): exit 0 when all five viewports pass.
- R8 outputs: android/app/build/outputs/mapping/release/{mapping.txt, configuration.txt, resources.txt, missing_rules.txt on missing classes, seeds.txt / usage.txt if produced}. Outputs: android/app/build/outputs/bundle/release/app-release.aab and android/app/build/outputs/apk/release/app-release-unsigned.apk (an app-release.apk would mean something signed it: stop).
- `aapt2 dump xmltree --file AndroidManifest.xml <apk>` prints attributes with numeric values: appCategory game is 0, screenOrientation portrait is 1.
- Zip central-directory entry names are stored uncompressed, so a byte search of the archive for an entry name is valid.
- The pre-R8 baseline: C:/projects/mazeworld/android/app/build/outputs/bundle/release/app-release.aab, if still present (read its size at execution; do not assume a number).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondition gate: STOP unless Phase 79.1 is verified and the code part has landed</name>
  <files>none (read-only gate; no repository file changes)</files>
  <read_first>
    - .planning/phases/ (find the 79.1-* directory and its 79.1-VERIFICATION.md)
    - .planning/phases/80-android-release-build-tooling/ (the 80-01, 80-02, 80-03 and 80-06 SUMMARY files)
  </read_first>
  <action>
1. Run the verify command below from the repository root of this working tree, and also `git status --porcelain` (it must print nothing).
2. If anything fails, STOP the whole plan: make no file change, run no npm or Gradle command, and report to the orchestrator exactly: "BLOCKED (80-04 precondition): <the failing condition>". The conditions are: Phase 79.1's VERIFICATION.md is missing or says gaps_found; a code-part SUMMARY (80-01, 80-02, 80-03 or 80-06) is missing; the working tree is dirty; android/keystore.properties exists here (run this plan in a worktree; never move, rename or read that file).
3. If everything passes, record the gate output in the SUMMARY and continue. This task edits no file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const P='.planning/phases/';const g=fs.readdirSync(P).find(d=>d.startsWith('79.1-'));const v=g&&P+g+'/79.1-VERIFICATION.md';if(!v||!fs.existsSync(v))throw 'BLOCKED: Phase 79.1 has no VERIFICATION.md';if(/^status:\s*gaps_found/m.test(fs.readFileSync(v,'utf8')))throw 'BLOCKED: Phase 79.1 verification has gaps';for(const p of ['01','02','03','06'])if(!fs.existsSync(P+'80-android-release-build-tooling/80-'+p+'-SUMMARY.md'))throw 'BLOCKED: 80-'+p+' has no SUMMARY';if(fs.existsSync('android/keystore.properties'))throw 'BLOCKED: android/keystore.properties present; run 80-04 in a worktree';console.log('gate ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "gate ok" and `git status --porcelain` prints nothing; otherwise the plan stopped with the BLOCKED report and made no change.
  </acceptance_criteria>
  <done>The single build is allowed to run: the milestone code is finished and verified, the code part has landed, and no upload-key config is reachable.</done>
</task>

<task type="auto">
  <name>Task 2: The ONE release build, persisted with hashes</name>
  <files>C:/projects/mazeworld-build/phase80-&lt;short-sha&gt;/ (outside the repository; no tracked file changes)</files>
  <read_first>
    - package.json (the cap:sync script), tools/gradle.mjs, tools/pin-jdk.mjs
    - docs/RELEASING.md ("Android toolchain pin" and the R8 section)
  </read_first>
  <action>
1. Record BUILD_COMMIT = `git rev-parse HEAD`. Set up the worktree per the environment note (`npm ci`, android/local.properties).
2. `npm run cap:sync`, then `node tools/pin-jdk.mjs`. Then `git diff --exit-code -- android/app/capacitor.build.gradle android/capacitor.settings.gradle android/gradle.properties android/version.properties`: the sync must regenerate what the code part committed. If it differs, STOP without building (the code part is incomplete) and report it.
3. Run the build once: `node tools/gradle.mjs bundleRelease assembleRelease` (one Gradle invocation; R8 runs once for the release variant). If it fails for any reason, including R8's "Missing classes detected", STOP: put the error and the full content of android/app/build/outputs/mapping/release/missing_rules.txt (if any) in the SUMMARY as the gap. Do not edit proguard-rules.pro or anything else, and do not build again.
4. Confirm the outputs are unsigned (app-release-unsigned.apk exists; no app-release.apk). If a signed app-release.apk appears, STOP and report.
5. Create ARTIFACT_DIR and copy in: app-release.aab, app-release-unsigned.apk, mapping.txt, configuration.txt, resources.txt (and seeds.txt / usage.txt if produced), and android/app/src/main/assets/capacitor.plugins.json.
6. Make the emulator copy: `zipalign -c -p 4` on the unsigned APK (align into ARTIFACT_DIR first with `zipalign -p 4` only if the check fails), then `apksigner sign` with the debug keystore to ARTIFACT_DIR/app-release-debugsigned.apk, then `apksigner verify --print-certs` and quote the certificate DN (it must be the Android Debug certificate).
7. Write ARTIFACT_DIR/BUILD-INFO.txt (BUILD_COMMIT, date, the exact commands from steps 2-6, the JDK path, AGP 8.13.0, Gradle 8.14.3, "unsigned release; emulator copy debug-signed") and ARTIFACT_DIR/SHA256SUMS.txt (SHA-256 of every file, computed with node:crypto).
8. In the SUMMARY, write these two key lines verbatim at the start of a line, which 80-05's gate parses: `BUILD_COMMIT: <40-hex sha>` and `ARTIFACT_DIR: <absolute path with forward slashes>`, then the hash list.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const out='android/app/build/outputs/';if(!fs.existsSync(out+'bundle/release/app-release.aab'))throw 'no aab';if(!fs.existsSync(out+'apk/release/app-release-unsigned.apk'))throw 'no unsigned apk';if(fs.existsSync(out+'apk/release/app-release.apk'))throw 'signed apk present';if(!fs.statSync(out+'mapping/release/mapping.txt').size)throw 'empty mapping';console.log('single build present')"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one Gradle invocation ran in this plan (the SUMMARY's command log shows it), and it ended BUILD SUCCESSFUL; or the plan stopped with the recorded gap and no second build.
    - ARTIFACT_DIR holds the AAB, both APKs, mapping.txt, capacitor.plugins.json, BUILD-INFO.txt and SHA256SUMS.txt; `apksigner verify --print-certs` on the debug-signed copy names the Android Debug certificate.
    - The SUMMARY carries the `BUILD_COMMIT:` and `ARTIFACT_DIR:` key lines.
    - `git status --porcelain` shows no tracked-file change (the build left the tree clean).
  </acceptance_criteria>
  <done>Phase 80's single release build exists, unsigned, persisted with hashes, with an emulator-installable debug-signed copy.</done>
</task>

<task type="auto">
  <name>Task 3: Every build-dependent check against the single build, plus the end-state letterbox check</name>
  <files>tools/android-api-scan.mjs, test/unit/android-api-scan.test.js</files>
  <read_first>
    - docs/RELEASING.md ("### Verifying a release build")
    - ARTIFACT_DIR (from Task 2)
    - capacitor.config.json (SplashScreen and SystemBars config)
  </read_first>
  <action>
Run each check against the files in ARTIFACT_DIR (never against a new build) and record every result in the SUMMARY:
1. R8 keeps: every classpath in ARTIFACT_DIR/capacitor.plugins.json has an identity line `X -> X:` in mapping.txt (fall back to `apkanalyzer dex packages --defined-only` on the APK, and say which was used); com.getcapacitor.plugin.SystemBars has one; the com.getcapacitor.MessageHandler block contains a member line ending in `-> postMessage`; at least one androidx.* class maps to a different name.
2. The status-bar plugin is gone: no mapping.txt line starts with com.capacitorjs.plugins.statusbar, and capacitor.plugins.json lists no statusbar classpath.
3. The mapping ships: the AAB's bytes contain `BUNDLE-METADATA/com.android.tools.build.obfuscation/proguard.map`.
4. Shrink survivors: `aapt2 dump resources` on the unsigned APK lists drawable/splash_screen and string/game_services_project_id; the APK's bytes contain the entry names assets/public/index.html and assets/capacitor.config.json.
5. Deprecated-API scan: `node tools/android-api-scan.mjs --aab <ARTIFACT_DIR>/app-release.aab --mapping <ARTIFACT_DIR>/mapping.txt --fail-on com.capacitorjs.plugins.statusbar,com.darktierstudios.delvedierepeat` exits 0 and reports zero unresolved callers; run it on the unsigned APK too and confirm the same findings. Paste the AAB table. For every finding record owner and reachability in this app. Answer the splash-screen question: whether legacyImmersive and legacyFullscreen survive in the com.capacitorjs.plugins.splashscreen.SplashScreen block of mapping.txt, and which calls the scan attributes to that class (their gates read splash settings this app does not set).
6. Merged manifest: `aapt2 dump xmltree --file AndroidManifest.xml` on the unsigned APK shows android:appCategory with value 0 (game) on application and android:screenOrientation with value 1 (portrait) on MainActivity; quote the lines.
7. Sizes: the AAB and APK sizes, against the pre-R8 baseline AAB in the main checkout if it is still there.
8. End-state web check: `node tools/letterbox-check.mjs --json` on this tree (the finished milestone code) exits 0; list any fixed overlays it measured that 80-03 did not.
Failures: if a check on the app side fails (a kept class missing, a status-bar class present, the mapping absent, a resource stripped, a --fail-on match, a manifest attribute missing, the letterbox check failing), STOP. Record the failure as a gap for a code-part gap-closure plan followed by a fresh single build; change no code or config and do not rebuild. The one exception: if the scanner itself misreads the real output (a tooling bug), fix tools/android-api-scan.mjs, add a matching case to test/unit/android-api-scan.test.js, run only `node --test test/unit/android-api-scan.test.js`, and re-run the scan on the same artifacts.
  </action>
  <verify>
    <automated>node tools/letterbox-check.mjs --json</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records a pass for each of checks 1-8 with its evidence (identity lines or apkanalyzer lines, the proguard.map check, the aapt2 lines, the scan table with zero unresolved callers, the manifest lines, the sizes, the letterbox table); or the plan stopped with a recorded gap and nothing rebuilt.
    - The scan exited 0 against ARTIFACT_DIR's AAB with the stated --fail-on list; the API list and prefixes are unchanged from 80-01 (`git diff` of tools/android-api-scan.mjs, if any, touches parsing only, and the SUMMARY explains it).
    - The full `npm test` was not run (build/verify-only plan).
  </acceptance_criteria>
  <done>Every build-dependent claim of DROID-01..03 is checked against the single build, and the letterbox holds on the finished code.</done>
</task>

<task type="auto">
  <name>Task 4: Document edge-to-edge handling, the audit, the measured build and the package addendum</name>
  <files>docs/ANDROID-DISPLAY.md, docs/RELEASING.md, store-listing/LISTING.md</files>
  <read_first>
    - docs/ANDROID-DISPLAY.md (created by 80-03; keep its intro and large-screen section unchanged except intro cross-references)
    - docs/RELEASING.md (the R8 section from 80-01; do not touch the release-notes section)
    - store-listing/LISTING.md ("Source-level audit (2026-09-24, commit ebe4b05)")
    - .planning/phases/80-android-release-build-tooling/80-02-SUMMARY.md and this plan's Task 3 results
    - node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java (insets listener)
  </read_first>
  <action>
1. docs/ANDROID-DISPLAY.md, add "## Edge-to-edge and system bars (DROID-02)": how insets reach the CSS (targetSdk 36 draws edge-to-edge on Android 15+; Capacitor 8's core SystemBars plugin either passes env(safe-area-inset-*) through on WebView 140+ with viewport-fit=cover, or pads the WebView and injects zero insets; either way it sets --safe-area-inset-top/right/bottom/left, which mazeworld.html's fallbacks read, so no CSS change); why the shell stays a plain BridgeActivity (no EdgeToEdge.enable: edge-to-edge is the default here and that helper's pre-35 paths call the bar-colour APIs Play flags); the DARK style in capacitor.config.json and nativeChrome.js, with no bar colour ever set; the status-bar plugin removal and its reason (its native load() set the deprecated colour on every launch), crediting the research correction; Android 7-14 (colorPrimaryDark #1b170f and windowBackground #14110c, non-deprecated attributes); Play warnings 1 and 2 verbatim with verdicts (warning 1 addressed, device and emulator evidence in the verification section; warning 2 addressed for the app's code and the removed plugin, residual library call sites in the audit, final verdict at the next Play pre-launch report).
2. Add "## Deprecated window API audit": BUILD_COMMIT, the date, the scan command, the Task 3 table (API, owner, caller, count), a verdict per row (removed / unreachable by config / library back-compat path for older OS versions / other), the splash-screen answer, the manifest confirmation, and "re-run the scan after every release build; a new caller means a dependency change to review before upload".
3. docs/RELEASING.md: inside the R8 section, add "### Measured on the Phase 80 build": BUILD_COMMIT, the AAB and APK sizes against the pre-R8 baseline, and a pointer to docs/ANDROID-DISPLAY.md for edge-to-edge and large-screen decisions. Touch nothing outside the R8 section.
4. store-listing/LISTING.md: under "Source-level audit (2026-09-24, commit ebe4b05)", append a dated "Phase 80 addendum": @capacitor/status-bar removed (bar styling now SystemBars inside @capacitor/core); re-run the same case-insensitive grep of the lockfile's non-dev package names for the ad, analytics and crash-reporting terms the audit lists and record the actual count and the new package list. Leave the original audit text unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/ANDROID-DISPLAY.md','utf8');const r=fs.readFileSync('docs/RELEASING.md','utf8');const l=fs.readFileSync('store-listing/LISTING.md','utf8');for(const k of ['## Large screens (DROID-03)','## Edge-to-edge and system bars (DROID-02)','## Deprecated window API audit','SystemBars','--safe-area-inset-'])if(!d.includes(k)){console.error('doc missing '+k);process.exit(1)}if(!r.includes('Measured on the Phase 80 build')){console.error('releasing');process.exit(1)}if(!l.includes('Phase 80')){console.error('listing');process.exit(1)}console.log('docs ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "docs ok".
    - The audit section names BUILD_COMMIT and has one row per Task 3 finding, each with a verdict.
    - `git diff` shows no removed line inside the 80-03 large-screen section or the RELEASING.md release-notes section.
    - The LISTING.md addendum records an actual re-run grep count.
  </acceptance_criteria>
  <done>Edge-to-edge handling, the audit of the single build, its measured sizes and the package record are documented.</done>
</task>

</tasks>

<verification>
- The precondition passed (Phase 79.1 verified, the code part landed, no upload-key config); exactly one release build ran, unsigned, persisted with hashes.
- Checks 1-8 passed against that build, or the plan stopped with a recorded gap and no rebuild.
- No app code or config changed; no full npm test; no Play upload; no version bump.
- Human verification (milestone-close checklist): after the next Play upload, the pre-launch report's warnings 1, 2 and 3 are gone, or the classes Play names match rows in the audit table and the verdict is recorded in docs/ANDROID-DISPLAY.md.
</verification>

<success_criteria>
- Phase 80's single release build exists and every build-dependent claim is proven against it.
- 80-05 has a persisted, hash-checked build to install without rebuilding.
- The display doc covers edge-to-edge and the audit; the runbook records the measured build.
</success_criteria>

<output>
Create `.planning/phases/80-android-release-build-tooling/80-04-SUMMARY.md` when done. It must contain the lines `BUILD_COMMIT: <sha>` and `ARTIFACT_DIR: <path>` at the start of a line, the hash list, the command log (one Gradle invocation), each check's evidence, and a "Human verification (milestone close)" list with the Play pre-launch report check.
</output>
