---
phase: 80-android-release-build-tooling
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/nativeChrome.js
  - test/persistence/lifecycle.test.js
  - tools/build-www.mjs
  - capacitor.config.json
  - test/unit/android-system-bars.test.js
  - package.json
  - package-lock.json
  - android/app/capacitor.build.gradle
  - android/capacitor.settings.gradle
  - android/app/src/main/res/values/styles.xml
autonomous: true
requirements:
  - DROID-02
must_haves:
  truths:
    - "@capacitor/status-bar is uninstalled: absent from package.json dependencies and package-lock.json, absent from tools/build-www.mjs CAPACITOR_PACKAGES, and `npx cap sync android` (code generation, not a build) has regenerated android/app/capacitor.build.gradle, android/capacitor.settings.gradle and the git-ignored capacitor.plugins.json without it, so its native load() path, which set the deprecated status-bar colour on every launch, will not ship"
    - "src/browser/nativeChrome.js#registerNativeChrome styles the bars through Capacitor 8's core SystemBars plugin (injectable as `SystemBars`, else reached with a guarded dynamic import of @capacitor/core) with setStyle({ style: \"DARK\" }), and sets no bar colour anywhere; the splash hide still runs first and the orientation lock and the @capacitor/app wiring are unchanged"
    - "capacitor.config.json configures plugins.SystemBars with insetsHandling \"css\" and style \"DARK\", so the light status-bar icons are right from the first native frame"
    - "The safe-area contract needs no CSS change: SystemBars injects --safe-area-inset-top/right/bottom/left (and on WebView 140+ with viewport-fit=cover passes env() through), the exact variables mazeworld.html's var(--safe-area-inset-*, env(safe-area-inset-*)) fallbacks read"
    - "MainActivity stays a plain BridgeActivity with no hand-rolled edge-to-edge call; Android 15+ (targetSdk 36) is edge-to-edge by default and SystemBars owns the insets"
    - "AppTheme.NoActionBar sets colorPrimaryDark #1b170f (the Android 7-14 status bar) and android:windowBackground #14110c (no light strip behind the WebView), and styles.xml sets none of the bar-colour theme attributes Android 15 deprecates"
    - "No Gradle task and no release build runs here (user ruling 2026-09-26); the R8 build that proves the plugin is gone from the output is 80-04's single build"
  artifacts:
    - path: src/browser/nativeChrome.js
      provides: "SystemBars-based bar styling in registerNativeChrome"
      contains: "SystemBars"
    - path: capacitor.config.json
      provides: "the SystemBars plugin config (css insets, DARK style)"
      contains: "SystemBars"
    - path: test/unit/android-system-bars.test.js
      provides: "source pins: no status-bar package or import, SystemBars used, config, theme, regenerated Gradle files, plain BridgeActivity"
    - path: test/persistence/lifecycle.test.js
      provides: "registerNativeChrome behaviour tests migrated from StatusBar to SystemBars"
      contains: "SystemBars"
    - path: android/app/src/main/res/values/styles.xml
      provides: "dark window background and pre-Android-15 status-bar colour through non-deprecated attributes"
      contains: "colorPrimaryDark"
  key_links:
    - from: src/browser/nativeChrome.js#registerNativeChrome
      to: "@capacitor/core SystemBars (vendored by tools/build-www.mjs, import-mapped in www/index.html)"
      via: "guarded dynamic import, then SystemBars.setStyle({ style: \"DARK\" })"
      pattern: "import\\(\"@capacitor/core\"\\)"
    - from: node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java#injectSafeAreaCSS
      to: "mazeworld.html var(--safe-area-inset-*, env(safe-area-inset-*)) consumers"
      via: "document.documentElement.style.setProperty(\"--safe-area-inset-top\", ...)"
      pattern: "--safe-area-inset-top"
    - from: package.json dependencies
      to: android/capacitor.settings.gradle + android/app/capacitor.build.gradle
      via: "npx cap sync android regenerates the native plugin list from the installed packages"
      pattern: "include ':capacitor-"
  prohibitions:
    - statement: "MUST NOT add a hand-rolled edge-to-edge or bar-colour call to the native shell (an EdgeToEdge.enable call, a decor-fits-system-windows call, or a bar-colour theme attribute); on older API levels those paths call the APIs Play flags, and SystemBars already owns the insets"
      requirement_id: DROID-02
      category: compliance
      status: resolved
      verification: test
    - statement: "MUST NOT change the phone layout or the shell's safe-area CSS to compensate for the migration; the SystemBars variables match the existing fallbacks, and any visual difference is recorded for the device round rather than patched blind"
      requirement_id: DROID-02
      category: fidelity
      status: resolved
      verification: judgment
    - statement: "MUST NOT run a Gradle task or release build, run npm ci in the main checkout, or edit .claude/CLAUDE.md in this plan; the build is 80-04's, and the main-checkout npm ci and the CLAUDE.md stack-table edit are orchestrator steps after this plan merges"
      requirement_id: DROID-02
      category: consent
      status: resolved
      verification: judgment
---

<objective>
CODE PART, no build: uninstall @capacitor/status-bar and style the bars through Capacitor 8's core SystemBars plugin, regenerating the native plugin list with cap sync, with source and behaviour tests; the release build that proves the plugin is gone is 80-04's.

Purpose: DROID-02 / ROADMAP Phase 80 success criterion 2 (edge-to-edge on Android 15+, no deprecated window or status-bar APIs Play flags). This is the code half; 80-04 builds once and audits the output, and 80-05 checks gesture nav, 3-button nav and a cutout on the emulator. It runs now, in parallel with the gameplay phases (user ruling 2026-09-26).

Output: SystemBars wiring in nativeChrome.js, the plugin removed and the Capacitor Gradle files regenerated, the SystemBars config, dark theme colours for the pre-Android-15 path, and source and behaviour tests.
</objective>

## Research correction (accepted by the orchestrator)

CONTEXT.md said to drop the JS call in `src/browser/nativeChrome.js` that sets the status-bar background colour. 80-RESEARCH.md ("Edge-to-Edge / DROID-02", source-verified) showed that is necessary but not sufficient: `@capacitor/status-bar` 8.0.3's `StatusBarPlugin.load()` constructs `StatusBar`, whose constructor calls the deprecated `Window.setStatusBarColor` on every launch whether JS calls it or not, because the plugin is registered in `capacitor.plugins.json`. The accepted fix removes the package and moves the bar style to the `SystemBars` plugin inside `@capacitor/core` (auto-registered by `Bridge.registerAllPlugins()`). `SystemBars` uses only `WindowInsetsControllerCompat`, and in its default `css` insets mode injects the `--safe-area-inset-*` variables `mazeworld.html` already reads, so no CSS changes.

## Artifacts this phase produces

Phase 80 is split in two (user rulings 2026-09-26): a **CODE part** (wave 1, no builds, running in parallel with Phases 75.3-79.1) and a **BUILD part** at the very end of the milestone (gated on Phase 79.1's VERIFICATION.md) with exactly one release build.

CODE part (wave 1):
- 80-01: R8 config for release (`android/app/build.gradle`), the defensive `android/app/proguard-rules.pro`, `android/app/src/main/res/raw/keep.xml`, `test/unit/android-r8.test.js`, the scanner `tools/android-api-scan.mjs` + `test/unit/android-api-scan.test.js`, the R8 / mapping / verification sections of `docs/RELEASING.md`
- **This plan (80-02):** `@capacitor/status-bar` uninstalled (`package.json`, `package-lock.json`, `tools/build-www.mjs`), the cap-sync-regenerated `android/app/capacitor.build.gradle` and `android/capacitor.settings.gradle`, `SystemBars` in `src/browser/nativeChrome.js`, the `capacitor.config.json` SystemBars config, `styles.xml` theme colours, `test/unit/android-system-bars.test.js`, the migrated `test/persistence/lifecycle.test.js`
- 80-03: `android:appCategory="game"`, the delimited `<style id="mw-letterbox">` block in `mazeworld.html`, `tools/letterbox-check.mjs`, `test/unit/android-large-screen.test.js`, `docs/ANDROID-DISPLAY.md` (created), browser screenshots in `80-screens/`
- 80-06: the fit tool's `--transcript` and `--force-infeasible` flags, `appendTranscript`, `test/difficulty/fit-difficulty-cli.test.js`, the closed TOOL-01 todo

BUILD part (milestone end):
- 80-04: the ONE release build (unsigned, plus a debug-signed APK copy, persisted with SHA-256 sums outside the repo), every build-dependent check (R8 keeps, the embedded mapping, shrink survivors, no status-bar class, the deprecated-API scan, the merged manifest), the end-state letterbox re-check, the edge-to-edge and audit sections of `docs/ANDROID-DISPLAY.md`, measured sizes in `docs/RELEASING.md`, the `store-listing/LISTING.md` addendum
- 80-05: the emulator pass on that same build (no rebuild) with screenshots in `80-screens/`, the verification section of `docs/ANDROID-DISPLAY.md`, the milestone-close device checklist and the ask-first Play push offer

## Flagged assumptions (surfaced, not silently decided)

- **DROID-02, edge probe: unclassified, reviewed manually.** The edges are runtime display states: gesture vs 3-button navigation, a display cutout, system light vs dark theme, WebView older than 140 (SystemBars pads the WebView and injects zero insets instead of passing env() through), and Android 7-14 (not edge-to-edge; the theme colours cover it). The tests here pin the configuration; the display states are checked on the emulator (80-05) and the Pixel 7 at milestone close.
- **Research assumption A1:** Play's static analysis flags a deprecated call's presence in shipped bytecode, not only calls the app's own JS reaches. If wrong, the uninstall was more than needed, which is harmless. 80-04 audits the built release either way.
- **Parallel with gameplay phases.** Other phases may edit nativeChrome.js or test/persistence/ while this runs. Keep the nativeChrome.js edit to the status-bar block, the parameter and the comments that name the plugin, and the lifecycle.test.js edit to the three StatusBar tests plus one new test.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-android-release-build-tooling/80-CONTEXT.md
@.planning/phases/80-android-release-build-tooling/80-RESEARCH.md

<environment>
No Gradle task, no `npm run android:*`, no `npm run play:release`. Allowed: `npm ci` in the worktree, `npm uninstall`, and `npm run cap:sync` (build:www plus `npx cap sync android`: web bundling and code generation, not a release build). `npx cap sync` wipes org.gradle.java.home from the tracked android/gradle.properties; run `node tools/pin-jdk.mjs` afterwards to restore it (it runs no build). This plan is EXPECTED to change android/app/capacitor.build.gradle and android/capacitor.settings.gradle (tracked, regenerated by cap sync, never hand-edited); android/gradle.properties and android/version.properties must end clean. Never copy, create or commit android/keystore.properties or a .jks.
</environment>

<interfaces>
- src/browser/nativeChrome.js#registerNativeChrome({ App, SplashScreen, StatusBar, ScreenOrientation, storage, waitForPending, getGameContext, onBackground, onForeground }): runs the splash hide, then the status-bar block (`StatusBar.setStyle({ style: "DARK" })` then a background-colour call), then `ScreenOrientation.lock({ orientation: "portrait" })`, each in its own try/catch, before the guarded @capacitor/app wiring. Every plugin is injectable; when omitted it is reached with `(await import("<pkg>")).<Export>`. Keep that shape (loadApp's comment explains never returning a plugin proxy from an async function; destructuring from the module namespace is safe).
- mazeworld.html calls registerNativeChrome without a StatusBar argument, so no shell change is needed.
- @capacitor/core dist/index.js exports SystemBars and SystemBarsStyle ("DARK" | "LIGHT" | "DEFAULT"); it is already in CAPACITOR_PACKAGES and import-mapped in www/index.html.
- node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java: initSystemBars() reads config keys `style` (default DEFAULT follows the system theme), `hidden`, `insetsHandling` ("css" default or "disable"); setStyle uses WindowInsetsControllerCompat and paints the decor view with the theme's windowBackground.
- test/persistence/lifecycle.test.js: "registerNativeChrome never throws when SplashScreen/StatusBar/ScreenOrientation are omitted", "CR-03: ... even with no SplashScreen/StatusBar/ScreenOrientation injected either", "CR-03: StatusBar/ScreenOrientation are still attempted ..." (a fake StatusBar with setStyle and a background-colour method).
- test/unit/play-games-intake.test.js: T-67-03 fails if any bare dynamic import in src/browser is missing from CAPACITOR_PACKAGES; reuse its vendoredPackages() approach and stripJs (tools/ident-sweep.mjs).
- android/app/src/main/res/values/styles.xml: AppTheme, AppTheme.NoActionBar (parent Theme.AppCompat.DayNight.NoActionBar; BridgeActivity sets it in onCreate), AppTheme.NoActionBarLaunch (parent Theme.SplashScreen). Colours: --paper #14110c, --paper-2 #1b170f.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the bar styling to SystemBars in the web shell, with config and tests</name>
  <files>src/browser/nativeChrome.js, test/persistence/lifecycle.test.js, tools/build-www.mjs, capacitor.config.json, test/unit/android-system-bars.test.js</files>
  <read_first>
    - src/browser/nativeChrome.js (whole file: header, loadApp, registerNativeChrome)
    - test/persistence/lifecycle.test.js (the StatusBar-related tests and the withFailingAppImport helper)
    - tools/build-www.mjs (CAPACITOR_PACKAGES and the comment above it)
    - capacitor.config.json
    - test/unit/play-games-intake.test.js (vendoredPackages, stripJs, REPO_ROOT)
    - node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java (config keys, setStyle, injectSafeAreaCSS)
    - mazeworld.html (the viewport meta tag and a few var(--safe-area-inset-*, env(...)) rules; do not edit)
  </read_first>
  <behavior>
    - registerNativeChrome({ SystemBars: fake }) calls fake.setStyle exactly once with { style: "DARK" }, and never calls the fake's background-colour spy.
    - With the @capacitor/app import forced to fail, SystemBars styling and the orientation lock are still attempted (CR-03, now for SystemBars).
    - registerNativeChrome resolves without throwing when SystemBars is omitted and @capacitor/core cannot be imported.
    - Source pins (android-system-bars.test.js, comment-stripped with stripJs): nativeChrome.js dynamically imports @capacitor/core and reads .SystemBars; it has no bare import of the removed status-bar package and no background-colour call; CAPACITOR_PACKAGES excludes the status-bar package and keeps @capacitor/core; capacitor.config.json has plugins.SystemBars.insetsHandling === "css" and style === "DARK"; mazeworld.html keeps viewport-fit=cover and still reads var(--safe-area-inset-top, env(safe-area-inset-top and var(--safe-area-inset-bottom, env(safe-area-inset-bottom.
  </behavior>
  <action>
1. Tests first: in test/persistence/lifecycle.test.js rename the StatusBar wording in the three existing test names and comments to SystemBars, change the "still attempted" test to inject `SystemBars` (a fake whose setStyle records its argument, plus a background-colour spy that must stay uncalled) and assert the DARK style; add one DROID-02 test for the exactly-once DARK call with the spy uncalled. Create test/unit/android-system-bars.test.js with the source pins. Confirm both fail.
2. src/browser/nativeChrome.js (edit only the parameter, the status-bar block and the comments that name the plugin):
   - replace the StatusBar injection parameter with `SystemBars: injectedSystemBars`;
   - replace the status-bar try block with a SystemBars block: resolve `injectedSystemBars || (await import("@capacitor/core")).SystemBars`, then `await SystemBars?.setStyle?.({ style: "DARK" })`; no colour call of any kind; same try/catch and position (after the splash hide, before the orientation lock);
   - replace the old 04-05 comment in that block with a DROID-02 comment: the core SystemBars plugin draws the app edge-to-edge under transparent bars and injects --safe-area-inset-* for mazeworld.html's fallbacks; DARK means light icons for the dark parchment; Android 15 deprecated bar colours, so none is set; capacitor.config.json sets the same style for the first native frame; the retired status-bar plugin set a colour from its own native load(), which is why it was uninstalled, not just not called;
   - update the module header's plugin list, the loadApp comment and the registerNativeChrome JSDoc (signature and the CR-03 "splash-hide/status-bar/orientation" wording) to name SystemBars.
3. tools/build-www.mjs: remove the status-bar entry from CAPACITOR_PACKAGES and fix the comment above the list that names StatusBar.
4. capacitor.config.json: add `"SystemBars": { "insetsHandling": "css", "style": "DARK" }` inside plugins, next to SplashScreen, keeping two-space JSON formatting.
5. Run the tests until green.
  </action>
  <verify>
    <automated>node --test test/persistence/lifecycle.test.js test/unit/android-system-bars.test.js test/unit/play-games-intake.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The three test files pass, and the new tests failed before the source changes (RED noted in the SUMMARY).
    - `grep -c "SystemBars" src/browser/nativeChrome.js` is at least 3 and `grep -c "\"SystemBars\"" capacitor.config.json` equals 1.
    - `node -e "const c=require('./capacitor.config.json');if(c.plugins.SystemBars.style!=='DARK'||c.plugins.SystemBars.insetsHandling!=='css')process.exit(1)"` exits 0.
    - `git diff --quiet -- mazeworld.html` succeeds (no shell CSS change).
  </acceptance_criteria>
  <done>The web shell styles the bars through SystemBars with no colour call, the status-bar package is off the vendoring list, and the config sets the style natively.</done>
</task>

<task type="auto">
  <name>Task 2: Uninstall the plugin, regenerate the native project with cap sync, set the theme colours, run the web and test gates</name>
  <files>package.json, package-lock.json, android/app/capacitor.build.gradle, android/capacitor.settings.gradle, android/app/src/main/res/values/styles.xml, test/unit/android-system-bars.test.js</files>
  <read_first>
    - package.json and package-lock.json (the status-bar entries)
    - android/app/capacitor.build.gradle and android/capacitor.settings.gradle (generated; "DO NOT EDIT" headers)
    - android/app/src/main/res/values/styles.xml
    - android/app/src/main/java/com/darktierstudios/delvedierepeat/MainActivity.java
    - node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/BridgeActivity.java (setTheme(R.style.AppTheme_NoActionBar))
    - test/unit/android-system-bars.test.js (from Task 1)
  </read_first>
  <action>
1. Extend test/unit/android-system-bars.test.js first with the native pins, and confirm they fail: package.json dependencies has no status-bar key; package-lock.json has no `node_modules/@capacitor/status-bar` entry and its root package's dependencies lacks it; android/app/capacitor.build.gradle and android/capacitor.settings.gradle reference no status-bar Gradle project; the AppTheme.NoActionBar block in styles.xml sets colorPrimaryDark #1b170f and android:windowBackground #14110c, and styles.xml sets neither Android-15-deprecated bar-colour theme attribute (status-bar colour, navigation-bar colour); MainActivity.java is a class extending BridgeActivity with an empty body.
2. In the worktree: `npm ci` if node_modules is absent, then `npm uninstall @capacitor/status-bar`.
3. `npm run cap:sync` (build:www then `npx cap sync android`; no Gradle). Confirm the regenerated android/app/capacitor.build.gradle and android/capacitor.settings.gradle dropped only the status-bar project, and that the git-ignored android/app/src/main/assets/capacitor.plugins.json no longer lists com.capacitorjs.plugins.statusbar.StatusBarPlugin. Do not hand-edit generated files. Then `node tools/pin-jdk.mjs` and confirm `git diff --exit-code -- android/gradle.properties android/version.properties`.
4. styles.xml, AppTheme.NoActionBar only: add `<item name="colorPrimaryDark">#1b170f</item>` (AppCompat maps it to the status bar on Android 7-14, where edge-to-edge is not forced; the retired plugin used to paint this colour there) and `<item name="android:windowBackground">#14110c</item>` (what SystemBars paints behind the WebView; the DayNight default is light in light mode), with a comment saying so and that bar-colour theme attributes are deliberately not used. Leave AppTheme and AppTheme.NoActionBarLaunch untouched.
5. Web gate: `npm run boot:check` (www was just built by cap:sync). It is environment-flaky on this machine (STATE.md blocker): on the known 0-byte dump, rerun once; if it fails the same way again, record the block verbatim and rely on the node tests plus 80-05's emulator boot. A real page error (an uncaught exception naming nativeChrome or a missing import-map entry) is a failure to fix.
6. Run `npm test`.
7. In the SUMMARY's output notes, state the two ORCHESTRATOR steps this plan must not do: after this plan merges, run `npm ci` in the main checkout so its node_modules drops the status-bar package; and update .claude/CLAUDE.md's stack table (the @capacitor/status-bar row becomes SystemBars inside @capacitor/core).
  </action>
  <verify>
    <automated>node --test test/unit/android-system-bars.test.js && node -e "const p=JSON.parse(require('fs').readFileSync('android/app/src/main/assets/capacitor.plugins.json','utf8'));if(p.some(x=>/statusbar/i.test(x.classpath)))process.exit(1);console.log('status-bar plugin gone from the generated plugin list')" && npm test</automated>
    <human-check>Milestone-close Pixel 7 (release-signed): status-bar icons light over the dark parchment from the first frame, with no light strip or flash; the HUD identity band fully below the status bar and the punch-hole camera; the bottom dock clear of the gesture handle (gesture navigation) and of the 3-button bar (Settings, System, Navigation mode); the same in the system light and dark themes; back still closes sheets and asks before quitting a live run. If an Android 14-or-older device is at hand, its status bar is dark (#1b170f), not grey or blue.</human-check>
  </verify>
  <acceptance_criteria>
    - The verify command passes: the pins are green, the generated plugin list has no status-bar entry, and `npm test` exits 0; the Task 2 pins failed before the uninstall (RED noted).
    - `git diff --stat -- android/app/capacitor.build.gradle android/capacitor.settings.gradle` shows only removed lines.
    - `git diff --exit-code -- android/gradle.properties android/version.properties` exits 0.
    - The boot:check result (pass, or the recorded environment block after one rerun) is in the SUMMARY, which also states that no Gradle task ran and carries the two orchestrator steps.
  </acceptance_criteria>
  <done>The status-bar plugin is gone from the dependency tree and the generated native project, the theme covers the pre-Android-15 path, and the web and test gates pass, all without a build.</done>
</task>

</tasks>

<verification>
- `npm test` green, including test/unit/android-system-bars.test.js, test/persistence/lifecycle.test.js and T-67-03 in test/unit/play-games-intake.test.js.
- No Gradle task ran; the generated plugin list and Capacitor Gradle files carry no status-bar plugin; android/gradle.properties and android/version.properties are clean.
- `git diff --quiet -- mazeworld.html android/app/src/main/java` succeeds.
- Build-output proof (R8 keeps SystemBars, no status-bar class in the dex) is 80-04's, on the single build.
- Human verification (milestone-close checklist): the Pixel 7 items in Task 2's human-check.
</verification>

<success_criteria>
- The deprecated status-bar colour path is removed from the dependency tree and the native project, not just from JS.
- Bars are styled by SystemBars (config and JS), insets reach the existing CSS variables, and the pre-Android-15 path keeps a dark bar through a non-deprecated theme attribute.
- Nothing was built; the orchestrator has its two follow-up steps.
</success_criteria>

<output>
Create `.planning/phases/80-android-release-build-tooling/80-02-SUMMARY.md` when done. Include the RED/GREEN runs, the regenerated-file diffs (removed lines only), the boot:check result, the statement that no build ran, "## Orchestrator steps after merge" (main-checkout `npm ci`; the CLAUDE.md stack-table edit), and a "Human verification (milestone close)" list.
</output>
