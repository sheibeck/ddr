---
phase: 80-android-release-build-tooling
plan: 02
type: execute
wave: 2
depends_on:
  - 80-01
files_modified:
  - src/browser/nativeChrome.js
  - test/persistence/lifecycle.test.js
  - tools/build-www.mjs
  - capacitor.config.json
  - test/unit/android-system-bars.test.js
  - package.json
  - package-lock.json
  - android/app/capacitor.build.gradle
  - android/capacitor.settings.gradle
  - android/app/src/main/res/values/styles.xml
autonomous: true
requirements:
  - DROID-02
must_haves:
  truths:
    - "@capacitor/status-bar is uninstalled: absent from package.json dependencies and package-lock.json, absent from tools/build-www.mjs CAPACITOR_PACKAGES, and `npx cap sync android` has regenerated android/app/capacitor.build.gradle, android/capacitor.settings.gradle and the (git-ignored) capacitor.plugins.json without it, so its native load() path, which set the deprecated status-bar colour on every launch, no longer ships"
    - "src/browser/nativeChrome.js#registerNativeChrome styles the bars through Capacitor 8's core SystemBars plugin (injectable as `SystemBars`, else reached with a guarded dynamic import of @capacitor/core) with setStyle({ style: \"DARK\" }), and sets no bar colour anywhere; the splash hide still runs first and the orientation lock and the @capacitor/app wiring are unchanged"
    - "capacitor.config.json configures plugins.SystemBars with insetsHandling \"css\" and style \"DARK\", so the light status-bar icons are right from the first native frame, before any JS runs"
    - "The safe-area contract needs no CSS change: SystemBars injects --safe-area-inset-top/right/bottom/left on document.documentElement (and, on WebView 140+ with viewport-fit=cover, passes real env() insets through), which are the exact variables mazeworld.html's var(--safe-area-inset-*, env(safe-area-inset-*)) fallbacks already read"
    - "MainActivity stays a plain BridgeActivity with no hand-rolled edge-to-edge call; Android 15+ (targetSdk 36) is edge-to-edge by default and SystemBars owns the insets"
    - "AppTheme.NoActionBar sets colorPrimaryDark #1b170f (the Android 7-14 status bar, where edge-to-edge is not forced) and android:windowBackground #14110c (no light strip or flash behind the WebView), and styles.xml sets none of the bar-colour theme attributes Android 15 deprecates"
    - "The release still builds with R8 after the plugin removal (bundleRelease + assembleRelease succeed) and mapping.txt contains no com.capacitorjs.plugins.statusbar class"
  artifacts:
    - path: src/browser/nativeChrome.js
      provides: "SystemBars-based bar styling in registerNativeChrome"
      contains: "SystemBars"
    - path: capacitor.config.json
      provides: "the SystemBars plugin config (css insets, DARK style)"
      contains: "SystemBars"
    - path: test/unit/android-system-bars.test.js
      provides: "source pins: no status-bar package or import, SystemBars used, config, theme, regenerated Gradle files, plain BridgeActivity"
    - path: test/persistence/lifecycle.test.js
      provides: "registerNativeChrome behaviour tests migrated from StatusBar to SystemBars"
      contains: "SystemBars"
    - path: android/app/src/main/res/values/styles.xml
      provides: "dark window background and pre-Android-15 status-bar colour through non-deprecated attributes"
      contains: "colorPrimaryDark"
  key_links:
    - from: src/browser/nativeChrome.js#registerNativeChrome
      to: "@capacitor/core SystemBars (vendored by tools/build-www.mjs, import-mapped in www/index.html)"
      via: "guarded dynamic import, then SystemBars.setStyle({ style: \"DARK\" })"
      pattern: "import\\(\"@capacitor/core\"\\)"
    - from: node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java#injectSafeAreaCSS
      to: "mazeworld.html var(--safe-area-inset-*, env(safe-area-inset-*)) consumers"
      via: "document.documentElement.style.setProperty(\"--safe-area-inset-top\", ...)"
      pattern: "--safe-area-inset-top"
    - from: package.json dependencies
      to: android/capacitor.settings.gradle + android/app/capacitor.build.gradle
      via: "npx cap sync android regenerates the native plugin list from the installed packages"
      pattern: "include ':capacitor-"
  prohibitions:
    - statement: "MUST NOT add a hand-rolled edge-to-edge or bar-colour call to the native shell (an EdgeToEdge.enable call, a decor-fits-system-windows call, or a bar-colour theme attribute); on older API levels those paths call the very APIs Play flags, and SystemBars already owns the insets"
      requirement_id: DROID-02
      category: compliance
      status: resolved
      verification: test
    - statement: "MUST NOT change the phone layout or the shell's safe-area CSS to compensate for the migration; the SystemBars variables match the existing fallbacks, and any visual difference is recorded for the device round rather than patched blind"
      requirement_id: DROID-02
      category: fidelity
      status: resolved
      verification: judgment
---

<objective>
Remove the deprecated status-bar colour path from the shipped app by uninstalling @capacitor/status-bar and styling the bars through Capacitor 8's core SystemBars plugin, then prove the native plugin list and the R8 release still build.

Purpose: DROID-02 / ROADMAP Phase 80 success criterion 2 (edge-to-edge on Android 15+, no deprecated window or status-bar APIs Play flags). This is the native half; 80-04 audits the built release for any remaining deprecated call and documents it, and 80-05 checks gesture nav, 3-button nav and a cutout on the emulator.

Output: SystemBars wiring in nativeChrome.js, the plugin removed and the Capacitor Gradle files regenerated, the SystemBars config, dark theme colours for the pre-Android-15 path, and source and behaviour tests.
</objective>

## Research correction (accepted by the orchestrator)

CONTEXT.md said to drop the JS call at `src/browser/nativeChrome.js` that sets the status-bar background colour. 80-RESEARCH.md ("Edge-to-Edge / DROID-02", source-verified) showed that is necessary but not sufficient: `@capacitor/status-bar` 8.0.3's `StatusBarPlugin.load()` constructs `StatusBar`, whose constructor calls the deprecated `Window.setStatusBarColor` on every launch whether JS calls it or not, because the plugin is registered in `capacitor.plugins.json`. The accepted fix removes the package entirely and moves the one remaining need (the bar style) to the `SystemBars` plugin that ships inside `@capacitor/core` and is auto-registered by `Bridge.registerAllPlugins()`. `SystemBars` uses only `WindowInsetsControllerCompat`, and in its default `css` insets mode injects the same `--safe-area-inset-*` variables `mazeworld.html` already reads, so no CSS changes.

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 80-02 creates or changes.)

- 80-01: R8 on for release (`android/app/build.gradle`), the defensive `android/app/proguard-rules.pro`, `android/app/src/main/res/raw/keep.xml`, `test/unit/android-r8.test.js`, the R8 + mapping section of `docs/RELEASING.md`
- **This plan (80-02):** `@capacitor/status-bar` uninstalled (`package.json`, `package-lock.json`, `tools/build-www.mjs`), the regenerated `android/app/capacitor.build.gradle` and `android/capacitor.settings.gradle`, `SystemBars` in `src/browser/nativeChrome.js`, `capacitor.config.json` SystemBars config, `styles.xml` theme colours, `test/unit/android-system-bars.test.js`, the migrated `test/persistence/lifecycle.test.js`
- 80-03: `android:appCategory="game"`, the letterboxed portrait column in `mazeworld.html`, `tools/letterbox-check.mjs`, `test/unit/android-large-screen.test.js`, `docs/ANDROID-DISPLAY.md` (created), browser screenshots in `80-screens/`
- 80-04: `tools/android-api-scan.mjs` + `test/unit/android-api-scan.test.js`, the deprecated-window-API audit of the R8 release and the merged-manifest check, the edge-to-edge sections of `docs/ANDROID-DISPLAY.md`, the release-runbook scan step, the `store-listing/LISTING.md` package addendum
- 80-05: the emulator pass (R8 release smoke, gesture/3-button/cutout, tablet and foldable) with screenshots in `80-screens/`, the verification section of `docs/ANDROID-DISPLAY.md`, the consolidated milestone-close device checklist
- 80-06: the fit tool's `--transcript` and `--force-infeasible` flags, `appendTranscript`, `test/difficulty/fit-difficulty-cli.test.js`, the closed TOOL-01 todo

## Flagged assumptions (surfaced, not silently decided)

- **DROID-02, edge probe: unclassified, reviewed manually.** The edges are runtime display states: gesture vs 3-button navigation, a display cutout, system light vs dark theme, WebView older than 140 (SystemBars pads the WebView and injects zero insets instead of passing env() through), and Android 7-14 (not edge-to-edge; the theme colours cover it). The source tests here pin the configuration; the display states are checked on the emulator (80-05) and the Pixel 7 at milestone close.
- **Research assumption A1:** Play's static analysis flags a deprecated call's presence in shipped bytecode, not only calls the app's own JS reaches. If that is wrong, the uninstall was more than needed, which is harmless. 80-04 audits the built release either way.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-android-release-build-tooling/80-CONTEXT.md
@.planning/phases/80-android-release-build-tooling/80-RESEARCH.md
@.planning/phases/80-android-release-build-tooling/80-01-SUMMARY.md

<environment>
Same build environment as 80-01: in a fresh worktree run `npm ci` and create android/local.properties with only the sdk.dir line from C:/projects/mazeworld/android/local.properties; never copy, create or commit android/keystore.properties or a .jks (the release builds unsigned, which is fine here). `npx cap sync` wipes org.gradle.java.home; run `node tools/pin-jdk.mjs` before Gradle (npm run android:release chains it). This plan is EXPECTED to change android/app/capacitor.build.gradle and android/capacitor.settings.gradle (both tracked, both regenerated by cap sync, never hand-edited); android/gradle.properties and android/version.properties must stay clean. Never run npm run play:release or version:bump.
</environment>

<interfaces>
- src/browser/nativeChrome.js#registerNativeChrome({ App, SplashScreen, StatusBar, ScreenOrientation, storage, waitForPending, getGameContext, onBackground, onForeground }): runs the splash hide, then the status-bar block (`StatusBar.setStyle({ style: "DARK" })` then a background-colour call), then `ScreenOrientation.lock({ orientation: "portrait" })`, each in its own try/catch, before the guarded @capacitor/app wiring. Every plugin is injectable; when omitted it is reached with `(await import("<pkg>")).<Export>`. Keep that shape (read loadApp's comment on never returning a plugin proxy from an async function: the destructure-from-module-namespace pattern is safe).
- mazeworld.html calls registerNativeChrome without a StatusBar argument (only App-level context), so no shell change is needed.
- @capacitor/core dist/index.js exports SystemBars, SystemBarsStyle ("DARK" | "LIGHT" | "DEFAULT"), SystemBarType. It is already in tools/build-www.mjs CAPACITOR_PACKAGES and import-mapped in www/index.html.
- node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java: initSystemBars() reads config keys `style` (default DEFAULT, which follows the system theme), `hidden`, `insetsHandling` ("css" default, or "disable"); setStyle uses WindowInsetsControllerCompat.setAppearanceLightStatusBars / LightNavigationBars and paints the decor view with the theme's windowBackground; the insets listener either passes insets through (WebView 140+ with viewport-fit=cover) or pads the WebView and injects the CSS variables.
- test/persistence/lifecycle.test.js: the tests "registerNativeChrome never throws when SplashScreen/StatusBar/ScreenOrientation are omitted", "CR-03: ... even with no SplashScreen/StatusBar/ScreenOrientation injected either" and "CR-03: StatusBar/ScreenOrientation are still attempted ..." (a fake StatusBar with setStyle and a background-colour method).
- test/unit/play-games-intake.test.js: T-67-03 already fails if any bare dynamic import in src/browser is missing from CAPACITOR_PACKAGES; its vendoredPackages() and stripJs (from tools/ident-sweep.mjs) are the helpers to reuse.
- android/app/src/main/res/values/styles.xml: AppTheme (parent Theme.AppCompat.Light.DarkActionBar), AppTheme.NoActionBar (parent Theme.AppCompat.DayNight.NoActionBar; BridgeActivity sets this theme in onCreate), AppTheme.NoActionBarLaunch (parent Theme.SplashScreen). Colours: mazeworld.html --paper #14110c, --paper-2 #1b170f.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the bar styling to SystemBars in the web shell, with config and tests</name>
  <files>src/browser/nativeChrome.js, test/persistence/lifecycle.test.js, tools/build-www.mjs, capacitor.config.json, test/unit/android-system-bars.test.js</files>
  <read_first>
    - src/browser/nativeChrome.js (whole file: header, loadApp, registerNativeChrome)
    - test/persistence/lifecycle.test.js (the StatusBar-related tests and the withFailingAppImport helper)
    - tools/build-www.mjs (CAPACITOR_PACKAGES and the comment above it)
    - capacitor.config.json
    - test/unit/play-games-intake.test.js (vendoredPackages, stripJs usage, REPO_ROOT)
    - node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/plugin/SystemBars.java (config keys, setStyle, injectSafeAreaCSS)
    - mazeworld.html (the viewport meta tag and a few of the var(--safe-area-inset-*, env(...)) rules; do not edit)
  </read_first>
  <behavior>
    - registerNativeChrome({ SystemBars: fake }) calls fake.setStyle exactly once with { style: "DARK" }.
    - The same run never calls a background-colour method on the fake (the fake exposes one as a spy).
    - With the @capacitor/app import forced to fail, SystemBars styling and the orientation lock are still attempted (the CR-03 guarantee, now for SystemBars).
    - registerNativeChrome resolves without throwing when SystemBars is omitted and @capacitor/core cannot be imported.
    - Source pins (android-system-bars.test.js, comment-stripped with stripJs): nativeChrome.js dynamically imports @capacitor/core and reads .SystemBars; it has no bare import of the removed status-bar package and no background-colour call; CAPACITOR_PACKAGES excludes the status-bar package and keeps @capacitor/core; capacitor.config.json has plugins.SystemBars.insetsHandling === "css" and plugins.SystemBars.style === "DARK"; mazeworld.html's viewport meta keeps viewport-fit=cover and its CSS still reads var(--safe-area-inset-top, env(safe-area-inset-top and var(--safe-area-inset-bottom, env(safe-area-inset-bottom.
  </behavior>
  <action>
1. Write the behaviour tests first: in test/persistence/lifecycle.test.js rename the StatusBar wording in the three existing test names and comments to SystemBars, change the "still attempted" test to inject `SystemBars` (fake with setStyle recording its argument, plus a background-colour spy that must stay uncalled) and assert the DARK style; add one DROID-02 test for the exactly-once DARK call with the spy uncalled. Create test/unit/android-system-bars.test.js with the source pins listed in the behaviour block (reuse the CAPACITOR_PACKAGES extraction approach and stripJs). Run both and confirm they fail.
2. src/browser/nativeChrome.js:
   - In registerNativeChrome's parameters, replace the StatusBar injection with `SystemBars: injectedSystemBars`.
   - Replace the status-bar try block with a SystemBars block: resolve `injectedSystemBars || (await import("@capacitor/core")).SystemBars`, then `await SystemBars?.setStyle?.({ style: "DARK" })`. No colour call of any kind. Keep its own try/catch and its position (after the splash hide, before the orientation lock).
   - Replace the old 04-05 comment in that block with a DROID-02 comment: Capacitor 8's core SystemBars plugin (inside @capacitor/core, auto-registered by the Bridge) draws the app edge-to-edge under transparent bars and injects --safe-area-inset-* for mazeworld.html's fallbacks; DARK means light icons for the dark parchment; Android 15 deprecated bar colours and ignores them edge-to-edge, so none is set; capacitor.config.json sets the same style so the first native frame is already right; the retired status-bar plugin set a bar colour from its own native load(), which is why it was uninstalled rather than just not called.
   - Update the module header's plugin list, the loadApp comment and the registerNativeChrome JSDoc (signature and the CR-03 "splash-hide/status-bar/orientation" wording) to name SystemBars.
3. tools/build-www.mjs: remove the status-bar entry from CAPACITOR_PACKAGES and fix the comment above the list that names StatusBar (SystemBars comes from @capacitor/core, already vendored).
4. capacitor.config.json: add `"SystemBars": { "insetsHandling": "css", "style": "DARK" }` inside plugins, next to SplashScreen. Keep the file's two-space JSON formatting.
5. Run the tests until green, then `npm run test:quick`.
  </action>
  <verify>
    <automated>node --test test/persistence/lifecycle.test.js test/unit/android-system-bars.test.js test/unit/play-games-intake.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/persistence/lifecycle.test.js test/unit/android-system-bars.test.js test/unit/play-games-intake.test.js` passes, and the new tests failed before the source changes (RED run noted in the SUMMARY).
    - `grep -c "SystemBars" src/browser/nativeChrome.js` is at least 3 and `grep -c "\"SystemBars\"" capacitor.config.json` equals 1.
    - `node -e "const c=require('./capacitor.config.json');if(c.plugins.SystemBars.style!=='DARK'||c.plugins.SystemBars.insetsHandling!=='css')process.exit(1)"` exits 0.
    - `git diff --quiet -- mazeworld.html` succeeds (no shell CSS change).
    - `npm run test:quick` exits 0.
  </acceptance_criteria>
  <done>The web shell styles the bars through SystemBars with no colour call, the status-bar package is off the vendoring list, and the config sets the style natively.</done>
</task>

<task type="auto">
  <name>Task 2: Uninstall the plugin, regenerate the native project, set the theme colours, and rebuild the R8 release</name>
  <files>package.json, package-lock.json, android/app/capacitor.build.gradle, android/capacitor.settings.gradle, android/app/src/main/res/values/styles.xml, test/unit/android-system-bars.test.js</files>
  <read_first>
    - package.json and package-lock.json (the status-bar entries)
    - android/app/capacitor.build.gradle and android/capacitor.settings.gradle (generated; "DO NOT EDIT" headers)
    - android/app/src/main/res/values/styles.xml
    - android/app/src/main/java/com/darktierstudios/delvedierepeat/MainActivity.java
    - node_modules/@capacitor/android/capacitor/src/main/java/com/getcapacitor/BridgeActivity.java (setTheme(R.style.AppTheme_NoActionBar))
    - test/unit/android-system-bars.test.js (from Task 1)
  </read_first>
  <action>
1. Extend test/unit/android-system-bars.test.js first with the native pins, and confirm they fail:
   - package.json dependencies has no status-bar key; package-lock.json has no `node_modules/@capacitor/status-bar` entry and its root package's dependencies lacks it;
   - android/app/capacitor.build.gradle and android/capacitor.settings.gradle reference no status-bar Gradle project;
   - the AppTheme.NoActionBar style block in styles.xml sets colorPrimaryDark to #1b170f and android:windowBackground to #14110c, and the whole styles.xml sets neither of the Android-15-deprecated bar-colour theme attributes (status-bar colour, navigation-bar colour);
   - MainActivity.java is exactly a class extending BridgeActivity with an empty body (no edge-to-edge or decor-fits calls).
2. Run `npm uninstall @capacitor/status-bar`.
3. Run `npm run cap:sync` (build:www then `npx cap sync android`). Confirm the regenerated android/app/capacitor.build.gradle and android/capacitor.settings.gradle dropped only the status-bar project, and that the git-ignored android/app/src/main/assets/capacitor.plugins.json no longer lists com.capacitorjs.plugins.statusbar.StatusBarPlugin. Do not hand-edit the generated files.
4. styles.xml, AppTheme.NoActionBar only: add `<item name="colorPrimaryDark">#1b170f</item>` (AppCompat maps it to the status bar on Android 7-14, where edge-to-edge is not forced; the retired plugin used to paint this colour there) and `<item name="android:windowBackground">#14110c</item>` (what SystemBars paints behind the WebView; the DayNight default is light in light mode). Add a comment saying so and that bar-colour theme attributes are deliberately not used (deprecated in Android 15). Leave AppTheme and AppTheme.NoActionBarLaunch untouched.
5. Run `node tools/pin-jdk.mjs`, then `node tools/gradle.mjs bundleRelease assembleRelease`. If Gradle reports a stale reference to the removed project, run `node tools/gradle.mjs clean` once and rebuild. Confirm android/app/build/outputs/mapping/release/mapping.txt has no line starting with `com.capacitorjs.plugins.statusbar`.
6. Run `npm run build:www` and `npm run boot:check`. The boot check is environment-flaky on this machine (STATE.md blocker): if it fails with the known 0-byte dump, rerun once; if it fails the same way again, record the environment block verbatim and rely on the node tests plus the 80-05 emulator boot. A real page error (an uncaught exception naming nativeChrome or a missing import-map entry) is a failure to fix.
7. Confirm `git diff --exit-code -- android/gradle.properties android/version.properties` is clean. Run `npm test`.
8. In the SUMMARY, add an orchestrator note: after this plan merges, run `npm ci` in the main checkout so its node_modules matches the lockfile without the status-bar package.
  </action>
  <verify>
    <automated>node --test test/unit/android-system-bars.test.js && node -e "const fs=require('fs');const m=fs.readFileSync('android/app/build/outputs/mapping/release/mapping.txt','utf8');if(/^com\.capacitorjs\.plugins\.statusbar/m.test(m))process.exit(1);const p=JSON.parse(fs.readFileSync('android/app/src/main/assets/capacitor.plugins.json','utf8'));if(p.some(x=>/statusbar/i.test(x.classpath)))process.exit(1);console.log('status-bar plugin gone from the build')"</automated>
    <human-check>Milestone-close Pixel 7 (release-signed): the status-bar icons are light over the dark parchment from the first frame, with no light strip or flash; the HUD identity band sits fully below the status bar and the punch-hole camera; the bottom dock clears the gesture handle (gesture navigation) and the 3-button bar (Settings, System, Navigation mode); the same in the system light and dark themes; back button still closes sheets and asks before quitting a live run. If an Android 14-or-older device is at hand, its status bar is dark (#1b170f), not grey or blue.</human-check>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/android-system-bars.test.js` passes, and the Task 2 pins failed before the uninstall (RED run noted).
    - `node tools/gradle.mjs bundleRelease assembleRelease` exits 0 (BUILD SUCCESSFUL quoted in the SUMMARY) and the verify command prints "status-bar plugin gone from the build".
    - `git diff --stat -- android/app/capacitor.build.gradle android/capacitor.settings.gradle` shows only removed lines.
    - `git diff --exit-code -- android/gradle.properties android/version.properties` exits 0.
    - `npm test` exits 0; the boot:check result (pass, or the recorded environment block after one rerun) is in the SUMMARY.
  </acceptance_criteria>
  <done>The status-bar plugin no longer ships, the native project and theme are consistent with SystemBars, and the R8 release builds.</done>
</task>

</tasks>

<verification>
- `npm test` green, including test/unit/android-system-bars.test.js, test/persistence/lifecycle.test.js and T-67-03 in test/unit/play-games-intake.test.js.
- A real R8 bundleRelease + assembleRelease succeeds after the plugin removal; mapping.txt and capacitor.plugins.json carry no status-bar plugin.
- `git diff --quiet -- mazeworld.html android/app/src/main/java` succeeds (no shell CSS change, no MainActivity change).
- Human verification (milestone-close checklist): the Pixel 7 items in Task 2's human-check.
</verification>

<success_criteria>
- The deprecated status-bar colour path is gone from the shipped app, not just from JS.
- Bars are styled by SystemBars (config + JS), insets reach the existing CSS variables, and the pre-Android-15 path keeps a dark bar through a non-deprecated theme attribute.
- The release still builds with R8.
</success_criteria>

<output>
Create `.planning/phases/80-android-release-build-tooling/80-02-SUMMARY.md` when done. Include the RED/GREEN runs, the regenerated-file diffs (removed lines only), the build result, the boot:check result, the `npm ci` orchestrator note, and a "Human verification (milestone close)" list.
</output>
