---
phase: 80-android-release-build-tooling
plan: 05
type: execute
wave: 3
depends_on:
  - 80-04
files_modified:
  - docs/ANDROID-DISPLAY.md
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-title.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-map.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-resumed.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-backconfirm.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-gesture.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-3button.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-cutout.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-tablet-portrait.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-tablet-rotated.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-fold-unfolded.png
  - .planning/phases/80-android-release-build-tooling/80-screens/emu-fold-folded.png
  - .planning/phases/80-android-release-build-tooling/80-screens/EMULATOR-UNAVAILABLE.md
autonomous: true
requirements:
  - DROID-01
  - DROID-02
  - DROID-03
must_haves:
  truths:
    - "A precondition task STOPS the plan unless Phase 79.1's VERIFICATION.md exists without gaps_found, 80-04's SUMMARY carries BUILD_COMMIT and ARTIFACT_DIR, the debug-signed APK's SHA-256 matches ARTIFACT_DIR/SHA256SUMS.txt, and no app-affecting path (engine, content, src, fonts, mazeworld.html, android, capacitor.config.json, package files, tools/build-www.mjs) changed since BUILD_COMMIT"
    - "This plan installs and exercises 80-04's single build (ARTIFACT_DIR/app-release-debugsigned.apk); it runs no Gradle task, no npm build script and no full test suite"
    - "The R8 release installs and boots on a Pixel 7-profile emulator to the title screen, and logcat shows no FATAL EXCEPTION and no R8-symptom error (ClassNotFoundException, NoSuchMethodException, NoSuchFieldException, or 'not implemented on android' for Preferences, App, SplashScreen, ScreenOrientation, Haptics or SystemBars)"
    - "On the phone emulator the run flow is exercised as far as UI automation reaches: ENTER, roll, the map; force-stop and relaunch resumes the same hero; the back key on the map shows the confirm-quit prompt; each step has a screenshot or a recorded reason"
    - "In gesture navigation, 3-button navigation and with an emulated display cutout, the HUD band sits below the status bar and cutout and the bottom dock clears the navigation bar or gesture handle, judged from screenshots the executor opens"
    - "On a Pixel Tablet-profile emulator the game stays portrait when the device is rotated and shows the centred column with dark gutters; on a Pixel Fold-profile emulator the unfolded screen shows the column and the folded outer screen the full-width phone layout"
    - "Every adb command names an emulator serial, so the user's paired Pixel 7 is never touched"
    - "A runtime regression found here is not fixed and rebuilt in this plan: it stops the pass and becomes a gap for a code-part gap-closure plan followed by a fresh single build"
    - "docs/ANDROID-DISPLAY.md gains a verification section with results and screenshot links; the SUMMARY carries the consolidated milestone-close Pixel 7 checklist and the ask-first Play push offer"
    - statement: "If the emulator cannot boot on this machine within the timebox, EMULATOR-UNAVAILABLE.md records the exact failure and the tablet/foldable and nav-mode checks move to the milestone-close checklist; the browser letterbox checks (80-03, and 80-04's end-state re-run) remain the automated large-screen evidence"
      verification: backstop
  artifacts:
    - path: docs/ANDROID-DISPLAY.md
      provides: "the verification section: emulator results, screenshots, per-release device checks"
      contains: "## Verification"
    - path: .planning/phases/80-android-release-build-tooling/80-screens/emu-tablet-rotated.png
      provides: "evidence that the portrait lock holds on a large screen"
    - path: .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-3button.png
      provides: "evidence of edge-to-edge insets with 3-button navigation"
  key_links:
    - from: "80-04's ARTIFACT_DIR/app-release-debugsigned.apk (hash-checked)"
      to: "the three emulator AVDs"
      via: "adb -s emulator-NNNN install -r; no rebuild"
      pattern: "app-release-debugsigned.apk"
    - from: "the R8 release at boot"
      to: "Capacitor PluginManager (Class.forName over capacitor.plugins.json)"
      via: "a clean boot with no ClassNotFoundException proves 80-01's keep rules at runtime"
      pattern: "ClassNotFoundException"
    - from: "android:appCategory=\"game\" (80-03)"
      to: "the tablet emulator's rotation behaviour"
      via: "rotating the device leaves the app portrait"
      pattern: "appCategory"
  prohibitions:
    - statement: "MUST NOT build, rebuild or re-sign the app in this plan; the pass exercises 80-04's single build, hash-checked (user ruling 2026-09-26)"
      requirement_id: DROID-01
      category: consent
      status: resolved
      verification: test
    - statement: "MUST NOT install on, reconfigure (navigation mode, display size, overlays, rotation) or pair with the user's Pixel 7; no device pauses, and every adb command names an emulator serial"
      requirement_id: DROID-02
      category: consent
      status: resolved
      verification: test
---

<objective>
BUILD PART (milestone end, after Phase 79.1): run the emulator pass on 80-04's single release build without rebuilding: an R8 release smoke, edge-to-edge in gesture and 3-button navigation and with a display cutout, and the tablet and foldable large-screen behaviour, with screenshots; then write the verification record, the consolidated milestone-close device checklist and the ask-first Play push offer.

Purpose: runtime evidence for DROID-01 (the R8 build boots and its bridge works), DROID-02 (insets in both navigation modes and with a cutout) and DROID-03 (the portrait lock holds on a large screen and the column shows), without pausing for the user's device. User rulings 2026-09-26: this pass installs the exact build 80-04 made; it is build/verify-only, with no full test suite. Sound, haptics, Play Games sign-in and the global boards need real hardware and an account; they go to the milestone-close checklist.

Output: emulator screenshots in 80-screens/, the verification section of docs/ANDROID-DISPLAY.md, and the checklist in the SUMMARY.
</objective>

## Artifacts this phase produces

Phase 80 is split in two (user rulings 2026-09-26): a **CODE part** (wave 1, no builds, running in parallel with Phases 75.3-79.1) and a **BUILD part** at the very end of the milestone (gated on Phase 79.1's VERIFICATION.md) with exactly one release build.

CODE part (wave 1):
- 80-01: R8 config for release (`android/app/build.gradle`), the defensive `android/app/proguard-rules.pro`, `android/app/src/main/res/raw/keep.xml`, `test/unit/android-r8.test.js`, the scanner `tools/android-api-scan.mjs` + `test/unit/android-api-scan.test.js`, the R8 / mapping / verification sections of `docs/RELEASING.md`
- 80-02: `@capacitor/status-bar` uninstalled (package files, `tools/build-www.mjs`), the cap-sync-regenerated Capacitor Gradle files, `SystemBars` in `src/browser/nativeChrome.js`, the `capacitor.config.json` SystemBars config, `styles.xml` theme colours, `test/unit/android-system-bars.test.js`, the migrated `test/persistence/lifecycle.test.js`
- 80-03: `android:appCategory="game"`, the delimited `<style id="mw-letterbox">` block in `mazeworld.html`, `tools/letterbox-check.mjs`, `test/unit/android-large-screen.test.js`, `docs/ANDROID-DISPLAY.md` (created), browser screenshots in `80-screens/`
- 80-06: the fit tool's `--transcript` and `--force-infeasible` flags, `appendTranscript`, `test/difficulty/fit-difficulty-cli.test.js`, the closed TOOL-01 todo

BUILD part (milestone end):
- 80-04: the ONE release build (unsigned, plus a debug-signed APK copy, persisted with SHA-256 sums outside the repo), every build-dependent check (R8 keeps, the embedded mapping, shrink survivors, no status-bar class, the deprecated-API scan, the merged manifest), the end-state letterbox re-check, the edge-to-edge and audit sections of `docs/ANDROID-DISPLAY.md`, measured sizes in `docs/RELEASING.md`, the `store-listing/LISTING.md` addendum
- **This plan (80-05):** the emulator pass on that same build (no rebuild): eleven screenshots in `80-screens/` (or `EMULATOR-UNAVAILABLE.md` on the backstop path), the verification section of `docs/ANDROID-DISPLAY.md`, the consolidated milestone-close device checklist and the ask-first Play push offer in the SUMMARY

## Flagged assumptions (surfaced, not silently decided)

- **DROID-01, DROID-02, DROID-03, edge probe: unclassified, reviewed manually.** This plan exercises the runtime edges: the reflective plugin load at boot, both navigation modes, a cutout, device rotation on a large screen, and a fold posture change. Sound, haptics, Play Games sign-in and the global boards cannot be judged on a headless emulator without audio or a signed-in account; they stay on the Pixel 7 checklist.
- **Emulator availability.** This machine has the SDK emulator, avdmanager, the `system-images;android-37.1;google_apis_playstore_ps16k;x86_64` image and a detected hypervisor, but no AVD yet and no confirmed headless boot. The backstop truth covers a failed boot.
- **AVD choice** (Claude's discretion per CONTEXT): three profiles from the installed image (`pixel_7`, `pixel_tablet`, `pixel_fold`) rather than one Resizable AVD, because a fold posture and a rotation are both scriptable from the command line on these profiles. An API 37 image runs this targetSdk 36 app under its targetSdk 36 behaviour rules.
- **Debug-signed install.** The emulator gets the debug-signed copy of the unsigned release build (same dex, same resources, same manifest; only the signature differs), which keeps the upload key out of the pass entirely.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/80-android-release-build-tooling/80-04-SUMMARY.md
@docs/ANDROID-DISPLAY.md

<environment>
No Gradle task, no npm build or sync script, no npm ci, no full test suite, no signing. Emulator tooling under SDK = C:/Users/Dell/AppData/Local/Android/Sdk: platform-tools/adb.exe, emulator/emulator.exe, cmdline-tools/latest/bin/avdmanager.bat (set JAVA_HOME to the JDK in android/gradle.properties org.gradle.java.home). The user's Pixel 7 may be paired over wireless adb and appear in `adb devices`: pass `-s emulator-NNNN` on EVERY adb call. The Bash tool blocks a bare foreground sleep: poll with bounded loops (or the Monitor tool) when waiting for a boot.
</environment>

<interfaces>
- 80-04's SUMMARY: lines `BUILD_COMMIT: <sha>` and `ARTIFACT_DIR: <path>`; ARTIFACT_DIR holds app-release-debugsigned.apk and SHA256SUMS.txt.
- Package and activity: com.darktierstudios.delvedierepeat / .MainActivity. Launch with `adb -s <serial> shell monkey -p com.darktierstudios.delvedierepeat -c android.intent.category.LAUNCHER 1`.
- Boot readiness: `adb -s <serial> wait-for-device`, then poll `adb -s <serial> shell getprop sys.boot_completed` for "1". Dismiss the keyguard with `input keyevent KEYCODE_WAKEUP` and `wm dismiss-keyguard`.
- Screenshots: `adb -s <serial> shell screencap -p /sdcard/s.png` then `adb -s <serial> pull /sdcard/s.png <dest>`. The executor opens each PNG with the Read tool to judge it.
- UI driving: `adb -s <serial> shell uiautomator dump /sdcard/ui.xml`, pull it; WebView content appears as nodes with text and bounds; tap a node's centre with `input tap x y`. The title button reads ENTER; the roller's primary button is #mw-roller-cta (read its label in mazeworld.html). A release build has WebView debugging off, so CDP is not available on the emulator.
- Navigation overlays: `cmd overlay enable-exclusive --category com.android.internal.systemui.navbar.gestural` and `... .navbar.threebutton`. Cutout emulation: `cmd overlay enable-exclusive --category com.android.internal.display.cutout.emulation.tall` (disable it afterwards).
- Rotation: `settings put system accelerometer_rotation 0` then `settings put system user_rotation 1` (landscape) and back to 0. Fold: `adb -s <serial> emu fold` / `emu unfold` on a foldable AVD.
- Logcat: `adb -s <serial> logcat -c` before launch, `logcat -d` after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Precondition gate: STOP unless Phase 79.1 is verified and 80-04's build is intact and current</name>
  <files>none (read-only gate; no repository file changes)</files>
  <read_first>
    - .planning/phases/80-android-release-build-tooling/80-04-SUMMARY.md (BUILD_COMMIT, ARTIFACT_DIR, hashes)
    - .planning/phases/ (the 79.1-* directory and its 79.1-VERIFICATION.md)
  </read_first>
  <action>
1. Run the verify command below from the repository root.
2. If it fails, STOP the whole plan: install nothing, create no AVD, change no file, and report to the orchestrator exactly: "BLOCKED (80-05 precondition): <the failing condition>". The conditions are: Phase 79.1 unverified or with gaps; 80-04's SUMMARY missing its key lines; ARTIFACT_DIR or its debug-signed APK missing; the APK's SHA-256 not in SHA256SUMS.txt; app-affecting files changed since BUILD_COMMIT (the build is stale, so 80-04 must run again after the change).
3. If it passes, record the gate output (including the APK hash) in the SUMMARY.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'),cp=require('child_process'),crypto=require('crypto');const P='.planning/phases/';const g=fs.readdirSync(P).find(d=>d.startsWith('79.1-'));const v=g&&P+g+'/79.1-VERIFICATION.md';if(!v||!fs.existsSync(v))throw 'BLOCKED: Phase 79.1 has no VERIFICATION.md';if(/^status:\s*gaps_found/m.test(fs.readFileSync(v,'utf8')))throw 'BLOCKED: Phase 79.1 verification has gaps';const s=fs.readFileSync(P+'80-android-release-build-tooling/80-04-SUMMARY.md','utf8');const c=(s.match(/^BUILD_COMMIT: ([0-9a-f]{40})\s*$/m)||[])[1];const d=((s.match(/^ARTIFACT_DIR: (.+)$/m)||[])[1]||'').trim();if(!c||!d)throw 'BLOCKED: 80-04 SUMMARY lacks BUILD_COMMIT/ARTIFACT_DIR';const apk=d+'/app-release-debugsigned.apk';if(!fs.existsSync(apk))throw 'BLOCKED: no debug-signed APK in '+d;const h=crypto.createHash('sha256').update(fs.readFileSync(apk)).digest('hex');if(!fs.readFileSync(d+'/SHA256SUMS.txt','utf8').includes(h))throw 'BLOCKED: APK hash not in SHA256SUMS.txt';try{cp.execSync('git diff --quiet '+c+' HEAD -- engine content src fonts mazeworld.html android capacitor.config.json package.json package-lock.json tools/build-www.mjs')}catch(e){throw 'BLOCKED: app files changed since the build; 80-04 must run again'}console.log('gate ok '+h)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "gate ok" followed by the APK hash; otherwise the plan stopped with the BLOCKED report and made no change.
  </acceptance_criteria>
  <done>The pass is cleared to run on 80-04's build, which is intact and matches the finished code.</done>
</task>

<task type="auto">
  <name>Task 2: Boot the phone emulator and run the R8 release smoke on the single build</name>
  <files>.planning/phases/80-android-release-build-tooling/80-screens/emu-phone-title.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-map.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-resumed.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-backconfirm.png, .planning/phases/80-android-release-build-tooling/80-screens/EMULATOR-UNAVAILABLE.md</files>
  <read_first>
    - .planning/phases/80-android-release-build-tooling/80-04-SUMMARY.md (ARTIFACT_DIR, the check results)
    - mazeworld.html (the title ENTER button, #mw-roller-cta's label, the confirm-quit prompt text shown by showConfirmQuit)
    - src/browser/nativeChrome.js (decideBackAction, the confirm-quit flow)
  </read_first>
  <action>
1. Create the three AVDs from the installed image (answer "no" to the custom-hardware prompt): ddr80-phone (device pixel_7), ddr80-tablet (pixel_tablet), ddr80-fold (pixel_fold). If the image is missing, install `system-images;android-36;google_apis;x86_64` with sdkmanager and use it; record which.
2. Boot ddr80-phone headless in the background (`-no-window -no-audio -no-snapshot -no-boot-anim -gpu swiftshader_indirect`). Timebox 8 minutes to sys.boot_completed; on failure retry once with the default GPU mode. If it still fails, kill the process, write .planning/phases/80-android-release-build-tooling/80-screens/EMULATOR-UNAVAILABLE.md (date, AVD, both command lines, the verbatim emulator output, and the checks that move to the milestone-close checklist), and jump to Task 4. That file exists only on this path.
3. With the serial pinned: clear logcat, `install -r` ARTIFACT_DIR/app-release-debugsigned.apk (the single build; never a new one), dismiss the keyguard, launch, wait until `dumpsys window` shows MainActivity focused and about 15 s more for the WebView. Screenshot to emu-phone-title.png and open it.
4. `logcat -d` and search for FATAL EXCEPTION, ClassNotFoundException, NoSuchMethodException, NoSuchFieldException and "not implemented on android". Any hit in a com.getcapacitor, com.capacitorjs or com.idleflowgames frame, or naming Preferences, App, SplashScreen, ScreenOrientation, Haptics or SystemBars, is an R8 regression: capture the stack in the SUMMARY as a gap for a code-part gap-closure plan and a fresh single build, and continue with the display checks. Do not edit any keep rule and do not rebuild. Play Games messages about a missing signed-in account are expected on an emulator; list them without failing.
5. Drive the run flow with uiautomator dumps: tap ENTER, wait for the roller, tap #mw-roller-cta once it activates, wait for the map, screenshot emu-phone-map.png and note the hero name from the dump. Force-stop, relaunch, tap ENTER and confirm the same hero name: screenshot emu-phone-resumed.png. On the map, send KEYCODE_BACK and screenshot emu-phone-backconfirm.png. If a step cannot be driven, record the dump excerpt and the reason and keep going.
6. Leave ddr80-phone running for Task 3.
  </action>
  <verify>
    <automated>test -s .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-title.png || test -s .planning/phases/80-android-release-build-tooling/80-screens/EMULATOR-UNAVAILABLE.md</automated>
    <human-check>Milestone-close Pixel 7, release-signed build (sideloaded, or from the Play internal track after the user's ask-first push): boot, SAVE and QUIT then relaunch and resume, back closes a sheet and asks before quitting a live run, sound and the title music, haptics on a hit, Play Games sign-in, and the global boards ME / ALL / FRIENDS.</human-check>
  </verify>
  <acceptance_criteria>
    - emu-phone-title.png exists and shows the title screen (described in the SUMMARY), or EMULATOR-UNAVAILABLE.md records the boot failure after the retry.
    - The SUMMARY has the logcat verdict (patterns searched, zero R8-symptom hits, or the recorded gap) and states the APK installed was ARTIFACT_DIR's debug-signed build with its hash.
    - The run-flow steps (map, resumed, back-confirm) each have a screenshot or a recorded reason.
    - Every adb invocation in the SUMMARY's command log carries `-s emulator-`; no Gradle or npm build command appears in it.
  </acceptance_criteria>
  <done>The single R8 build boots cleanly on a phone emulator, and the save, resume and back paths are exercised as far as automation reaches.</done>
</task>

<task type="auto">
  <name>Task 3: Navigation modes and cutout on the phone; rotation on the tablet; fold postures on the foldable</name>
  <files>.planning/phases/80-android-release-build-tooling/80-screens/emu-phone-gesture.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-3button.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-phone-cutout.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-tablet-portrait.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-tablet-rotated.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-fold-unfolded.png, .planning/phases/80-android-release-build-tooling/80-screens/emu-fold-folded.png, .planning/phases/80-android-release-build-tooling/80-screens/EMULATOR-UNAVAILABLE.md</files>
  <read_first>
    - docs/ANDROID-DISPLAY.md (the large-screen and edge-to-edge sections, to know what each screenshot must show)
    - .planning/phases/80-android-release-build-tooling/80-screens/browser-tablet-app.png (the expected column look)
  </read_first>
  <action>
1. Phone (ddr80-phone, the map on screen): enable gestural navigation, relaunch to the map, screenshot emu-phone-gesture.png; enable 3-button navigation, screenshot emu-phone-3button.png; enable the tall cutout emulation, relaunch, screenshot emu-phone-cutout.png, then disable the cutout overlay. Open each PNG and record: the HUD band text fully below the status bar and cutout; the bottom dock above the gesture handle or the 3-button bar; no light strip in either bar area. Kill the phone emulator.
2. Tablet (ddr80-tablet): boot per Task 2's timebox, install the same APK, launch, screenshot emu-tablet-portrait.png. Lock rotation to landscape (accelerometer off, user_rotation 1), wait for the configuration change, screenshot emu-tablet-rotated.png, restore user_rotation 0. Record `dumpsys activity activities` lines for the app's bounds and any letterbox state. Verdict: the app never lays out in landscape, and the centred column with dark gutters shows whenever the app window is wider than 480 CSS px. Kill the emulator.
3. Foldable (ddr80-fold): boot, install the same APK, launch. `emu unfold`, screenshot emu-fold-unfolded.png; `emu fold`, screenshot emu-fold-folded.png. If the fold commands are unsupported, use `wm size` with the inner display dimensions from `dumpsys display` for the unfolded shot, reset with `wm size reset`, and record the substitution. Verdict: unfolded shows the column; folded shows the full-width phone layout. Kill the emulator.
4. Where an AVD cannot boot or a posture cannot be set, append the verbatim reason and the replacing milestone-close item to EMULATOR-UNAVAILABLE.md (create it if Task 2 did not).
5. Keep the three AVDs for re-checks; note in the SUMMARY how to delete them (`avdmanager delete avd -n <name>`).
  </action>
  <verify>
    <automated>test -s .planning/phases/80-android-release-build-tooling/80-screens/emu-tablet-rotated.png || test -s .planning/phases/80-android-release-build-tooling/80-screens/EMULATOR-UNAVAILABLE.md</automated>
    <human-check>Milestone-close Pixel 7: switch between gesture and 3-button navigation (Settings, System, Navigation mode) and confirm the HUD and the bottom dock clear the bars in both, around the punch-hole camera, in the system light and dark themes.</human-check>
  </verify>
  <acceptance_criteria>
    - The seven screenshots named in this task exist, each with a one-line verdict in the SUMMARY; or EMULATOR-UNAVAILABLE.md records the verbatim reason and the replacing milestone-close item for each missing one.
    - The tablet verdict states whether the app stayed portrait after rotation, with the dumpsys lines quoted.
    - No command in the SUMMARY's log targets a device other than an emulator serial, and none builds or signs anything.
  </acceptance_criteria>
  <done>Insets are judged in both navigation modes and with a cutout, and the large-screen portrait lock and column are judged on tablet and foldable profiles, all on the single build.</done>
</task>

<task type="auto">
  <name>Task 4: Record the verification, the per-release device checks, and the milestone-close checklist</name>
  <files>docs/ANDROID-DISPLAY.md</files>
  <read_first>
    - docs/ANDROID-DISPLAY.md (sections from 80-03 and 80-04; keep them unchanged)
    - the Task 2 and Task 3 results
    - the human-verification lists in the 80-01 to 80-04 SUMMARYs
  </read_first>
  <action>
1. Append "## Verification" to docs/ANDROID-DISPLAY.md: BUILD_COMMIT and the APK hash of the build exercised; the automated checks and where they live (tools/letterbox-check.mjs, tools/android-api-scan.mjs, the node tests, 80-04's build checks); a results table for the emulator pass (check, AVD profile, result, screenshot link) covering the R8 boot and logcat verdict, the run-flow steps, gesture, 3-button, cutout, tablet portrait and rotated, fold unfolded and folded; and, if the emulator could not run, a link to EMULATOR-UNAVAILABLE.md and the checks that moved to the device list.
2. Append "### Device checks before each Play release": a short reusable list (Pixel 7: both navigation modes, the punch-hole, light and dark system theme, the release-signed smoke; large screen: any tablet or foldable on hand, or re-run the three AVDs; Play Console: the pre-launch report's edge-to-edge, deprecated-API and display-configuration warnings compared with the audit table).
3. In the SUMMARY write "## Milestone-close device checklist (Phase 80)", consolidating every human-check from 80-01 to 80-05 (DROID-01 release smoke; DROID-02 navigation modes, cutout and themes; DROID-03 phone unchanged plus any real large screen; the Play pre-launch report re-check for warnings 1-3), and "## Play push offer (ask-first)": the orchestrator asks the user whether to push a versionCode-bumped signed AAB to the internal-testing track; that push is a new, upload-key-signed build made by `npm run play:release` only after the user says yes; this plan never runs it.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/ANDROID-DISPLAY.md','utf8');for(const k of ['## Large screens (DROID-03)','## Edge-to-edge and system bars (DROID-02)','## Deprecated window API audit','## Verification','### Device checks before each Play release'])if(!s.includes(k)){console.error('missing '+k);process.exit(1)}console.log('verification recorded')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "verification recorded".
    - Every screenshot link in the Verification section resolves to a file in 80-screens/, or the row says why none exists.
    - The SUMMARY has both the consolidated checklist and the ask-first Play push offer; `git diff --exit-code -- android/version.properties` exits 0; the full test suite was not run (verify-only plan).
  </acceptance_criteria>
  <done>The phase's runtime evidence is recorded against the single build, and the orchestrator has one consolidated device checklist and the Play push question.</done>
</task>

</tasks>

<verification>
- The precondition passed on a hash-checked, current build; nothing was built, rebuilt or re-signed.
- Emulator screenshots in 80-screens/ with verdicts, or EMULATOR-UNAVAILABLE.md plus the moved checklist items.
- Every adb command named an emulator serial; no Play upload; no version bump; no full test suite.
- Human verification (milestone-close checklist): the consolidated list in the SUMMARY.
</verification>

<success_criteria>
- The single R8 release is shown to boot with its plugin bridge intact.
- Edge-to-edge is judged in both navigation modes and with a cutout; the large-screen lock and column are judged on tablet and foldable profiles.
- The display doc carries the verification record; the orchestrator gets one device checklist and the ask-first Play push question.
</success_criteria>

<output>
Create `.planning/phases/80-android-release-build-tooling/80-05-SUMMARY.md` when done. Include the gate output with the APK hash, the adb command log (every line with its emulator serial), each screenshot's verdict, the logcat verdict, any recorded gap, "## Milestone-close device checklist (Phase 80)" and "## Play push offer (ask-first)".
</output>
