---
phase: 35-map-screen-rebuild
verified: 2026-09-17T03:10:00Z
status: passed
score: 9/10 requirements verified on automated evidence (MAP-01..09); MAP-10 (the Pixel 7 DR round) is the device round itself — deferred to the end-of-run UAT batch per the user's "defer uat to end" instruction
behavior_unverified: 0
overrides_applied: 0
human_verification: ["Pixel 7 (MAP-03, rail): walk onto a trap / teleport / one-way door — a toned card appears in the bottom rail with title, sentence and (trap) dice line, then clears itself after its hold; no toast anywhere", "Pixel 7 (MAP-03, gear): equip a rejected item on the GEAR tab — the rail (below the Gear list) explains the refusal", "Pixel 7 (MAP-04, joiner): meet a Joiner — COMPANY card with narration, who-walks line, TAKE THEM ALONG / LEAVE THEM; stepping or pressing an arrow only pulses the card; answering releases movement", "Pixel 7 (MAP-04, find): find a chest item — SOMETHING WORTH TAKING with TAKE IT / LEAVE IT; with a full bag the drop shelf + TAKE IT NOW appears instead", "Pixel 7 (MAP-04, climb): fall at a wall/crevice — FELL (bad tone) with the hp line and CLIMB IT; every other direction is refused (card pulses) until CLIMB IT eventually succeeds", "Pixel 7 (MAP-04, auto cards): new floor / level up — FLOOR N (odd) / SKILL LEVEL N (good, longer hold) appears with no tap and clears itself", "Pixel 7 (MAP-05, death): die on a move outside combat — THAT IS THAT at once with the cause lines above the epitaph, no Move-on card first", "Pixel 7 (MAP-08, announcer): with TalkBack on, a new rail card is announced once; a pulse or same-card repaint does not repeat it", "Pixel 7 (MAP-01/03, layout): the tab bar sits below the rail on every tab; the Oracle still opens at the newest line", "Pixel 7 (MAP-01, HUD): FLOOR (gold) · DAY · SQUARES · RATIONS left, x/y WP over a thin bar right; RATIONS red at 2; bar gold under half, red under a quarter; WP text red at/under a quarter; no name/class/hp line; gear icon still opens Settings", "Pixel 7 (MAP-01, chips): pick up a tracked condition (e.g. haste potion) — a tone-coloured chip strip appears under the HUD with label + remaining count; tapping a chip shows its explanation in the rail; the strip disappears when the condition ends", "Pixel 7 (MAP-07, palette): dark fog on unexplored squares, bevelled dark-brown walls, warm-stone floor with inset line, thick border; coloured glyph marks — red ● encounter, purple ◆ teleport, green ▲ door (pointing its direction), red ✕ trap, tan ▪ chest, tan ⧗ crevice, tan ▼ stairs — none as an empty box; gold party dot with pulsing ring; ring stops under reduced motion", "Pixel 7 (MAP-06, marks): tap MARKS — WHAT THE MARKS MEAN sheet with glyph · name · description rows; scrim tap closes; the next step within ~250 ms is swallowed", "Pixel 7 (MAP-06, centre): tap CENTRE after panning/zooming away — the party recenters", "Pixel 7 (MAP-06, camp): tap MAKE CAMP — sheet with the Eight-hours copy, SLEEP / 1 RATION (gold) and WALK ON; with rations SLEEP → CAMP MADE (good) rail card; without → NOTHING TO EAT (bad); SLEEP within ~250 ms of opening does nothing; WALK ON closes with no dispatch; with a rail decision pending MAKE CAMP only pulses the card", "Pixel 7 (MAP-02, tap-to-step): no D-pad anywhere; a tap two squares east steps one east; up-and-right with rock east steps north (fallback); both blocked → NO WAY THAT DIRECTION (dull), no move; tap on own square → YOU ARE HERE", "Pixel 7 (MAP-02, hold): hold ~half a second without travel — UNWALKED / SOLID ROCK / the mark's legend line / EMPTY CORRIDOR in the rail, no move; release after a hold does not step", "Pixel 7 (MAP-02, pan/pinch): drag pans, party marker stays put over its corridor; pinch zooms smoothly between fully out and 2x and recenters on release; a step after panning recenters", "Pixel 7 (MAP-04, lock): walk toward a trap/wall/crevice while a Joiner or find card is pending — the card pulses, nothing happens", "Pixel 7 (MAP-05, stair): step toward ▼ — near-black overlay with large ▼, THE STAIR DOWN, the colder-longer line, GO DOWN / NOT YET; NOT YET returns unchanged and the next tap within ~250 ms is swallowed; GO DOWN descends and FLOOR N+1 appears in the rail; a tap within 250 ms of the overlay appearing is swallowed", "Pixel 7 (MAP-02, keys): with a keyboard, arrows still move; Enter on the stair overlay = GO DOWN; Escape = NOT YET", "Pixel 7 (MAP-08, back button): Android back closes the stair overlay (as NOT YET) and an open MARKS/MAKE CAMP sheet, but never a pending Joiner/find/climb rail card", "Pixel 7 (MAP-08, reduced motion): the stair overlay appears without a fade and both buttons respond once the arm window has elapsed", "Pixel 7 (MAP-01/02/03, cold start): the app cold-starts to the map with the new HUD, the idle rail (FLOOR 1 · NOTHING IS HAPPENING, TAP TO STEP) and no D-pad", "Pixel 7 (MAP-03, full loop): step, trap, chest Take, Joiner accept, wall fall + CLIMB IT, stair GO DOWN, camp — rail cards only, never a toast", "Pixel 7 (CSCR/MAP-05, handoff): one full fight from FIGHT IT OUT to THEY ARE DOWN still works exactly as Phase 34's 27-item list describes (same APK)", "Pixel 7 (MAP-07, glyph fonts): ⧗ and ▪ render as glyphs, not tofu, on the Pixel 7 system fonts — if ⧗ is a box, the follow-up is a one-line glyph swap in mapMarks.js"]
gaps: []
---

# Phase 35 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-17)

Goal-backward check of the phase goal: *the map tab is rebuilt to the imported Claude Design spec — HUD strip + condition chips, a tap-to-step viewport (no D-pad), the bottom RAIL replacing every toast and carrying every decision with movement locked until it is answered, the MAJOR OVERLAY for encounters / descents / out-of-combat death, MARKS / CENTRE / MAKE CAMP chips with sheets, the canvas on the mock's palette, Phase 32 guards intact, engine/parity untouched.*

## Automated evidence (re-run by the orchestrator after 35-05)

- `npm test`: **2170/2170, 0 failures** (2047 at phase start → 2086 after 35-01 → 2090 after 35-02 → 2109 after 35-03 → 2133 after 35-04 → 2170 after 35-05). `npm run build:www` exit 0; `www/src/browser/{rail,tapStep,mapMarks}.js` present; the retired-literal set is absent from `www/index.html`.
- Engine gate: `git diff --stat babcb53..HEAD -- engine content test/parity` **empty** across the whole phase; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (unchanged); `src/browser/controls.js`, `src/browser/icons.js` and the `icons/` assets untouched. No new packages; `fonts.googleapis`/`fonts.gstatic` = 0/0.
- Phase invariants (`test/unit/shell-map-invariants.test.js`, 37 tests): zero `mzToast` / `mw-toast` / `__mzToastLifetime` / `MAX_TOASTS` / `dpad` / `data-dir` / `mazefoot` / `mw-flash` / character-line / PNG-draw / Move-on literals in `mazeworld.html` (comments included); every new tap target wired through `guardTap`; no presentation state on `S`; rail-or-log dispatch partition; `id="mw-rail"` = 1, `renderMajorOverlay(body` = 2, `window.mzCenterMap?.()` = 10, zoom default 0.8.
- Five plans, five SUMMARY.md files, all `## Self-Check: PASSED`; working tree clean at HEAD `0047051`.
- Debug APK built once, after the last `mazeworld.html` edit of the milestone: `android/app/build/outputs/apk/debug/app-debug.apk`, 9,474,974 bytes, 2026-09-17T02:44:49Z, versionName 1.2.0 (versionCode 3). Not installed by the executor (orchestrator step).
- Known pre-existing note: raw `/*` vs `*/` counts in `mazeworld.html` moved 152/148 → 141/138 only because Phase 35's retirements deleted lines carrying some of the `@capacitor/*` comment mentions Phase 34 root-caused; the project's `stripComments()` result is unchanged in shape (0 leftover `/*`, 1 harmless `*/`).

## Success criteria → evidence

| # | Success criterion (ROADMAP) | Evidence |
|---|-----------------------------|----------|
| 1 | No D-pad and no toast anywhere; tap steps one square toward the tap; hold inspects; drag pans; pinch zooms | D-pad markup/CSS/listener deleted (zero-grep pinned); toast host/builder/CSS/lifetime retired (zero-grep pinned); `tapStep(clientX, clientY)` → `screenToCell` → `resolveStep` (dominant axis, fallback, blocked → NO WAY THAT DIRECTION, own square → YOU ARE HERE; `tapStep.test.js` 16) → guarded `window.move`; `inspectAt` after `HOLD_MS` 450; Phase 33 pan/pinch code untouched, `ZOOM_MAX` 2.0, recenter count 10 (`shell-map-viewport.test.js` 25, `shell-map-store-polish` re-pinned). |
| 2 | Every out-of-combat event in the rail with its dice, auto-clearing; every decision answered in the rail with tap-to-step locked until then; level-up and floor arrival need no tap | `dispatchWithToasts` out-of-combat branch → `railCardFor(…, {withIdx:true})` → `railPush` (one stacked card per dispatch, tone/hold per family, generic `rollLineFor` from any `roll`/`need` payload; `rail.test.js` 15); `renderRail` precedence joiner → find → climb → card → idle; `railLocked()` = pendingJoiner ∥ pendingFind ∥ `__mzRail.pending` checked in `window.move` after the settle clause, refusal pulses the card; CLIMB IT re-dispatches the same direction, `climbedOver`/`leaptOver`/death clear it; `S.beats` Move-on cards, `a-next`, `stepping()` retired — FLOOR N / SKILL LEVEL N are rail cards (`shell-map-rail.test.js` 16). Oracle path untouched. |
| 3 | Encounters, descents and out-of-combat death use the major overlay; FIGHT IT OUT opens the combat screen at round 1 | Encounter overlay unchanged from Phase 34; `stepTargetsExit(dir)` pre-dispatch interception → `window.__mzStair` → `renderMajorOverlay` THE STAIR DOWN with GO DOWN (`window.__mzDescend` → the identical `stepNow(dir)`) / NOT YET (dismissal transition, settle); `renderMajorOverlay` body byte-identical to Phase 34; out-of-combat death folds pre-death lines into `renderCombatOver(body, "dead", {lines})` (`shell-map-viewport`, `shell-combat-over` re-pinned). |
| 4 | HUD + condition chips match the mock; MARKS / CENTRE / MAKE CAMP work from the top chips with their sheets | `.mw-hud` FLOOR/DAY/SQUARES/RATIONS + x/y WP bar with the spec thresholds; `#mm-conditions` chip strip from `conditionsOf` with `CONDITION_TONE`/`CONDITION_EXPLAIN` → rail; `.mw-map-chips` MARKS (glyph legend sheet from `MARKS_LEGEND`) / CENTRE (`mzCenterMap`) / MAKE CAMP (`#mw-camp-sheet`, SLEEP → `mzMakeCamp`, refusal/result as rail cards, refused while locked); canvas `draw()` on `MAP_PALETTE` + `MARK_GLYPHS` (deliberate reversal of the Phase-4 PNG decision) + `#mw-party-pulse` (`shell-map-hud.test.js` 19, `mapMarks.test.js` 8, `shell-gear-toolbar` re-pinned). |
| 5 | Guards hold on every new button; `npm test` green with every shell test re-pinned; `build:www` 0; engine/content/parity diff empty; Pixel 7 DR round | Guarded-target sweep in `shell-map-invariants` (every rail/overlay/sheet/chip button through `guardTap`; both sheet closes stamp `lastDismissAt`); automated half verified above (2170/2170, build 0, diff empty, hash pinned). **The DR round is MAP-10 — deferred** (27 checks in the frontmatter list, plus Phase 34's 27 against the same APK). |

## Requirements

MAP-01 ✓ · MAP-02 ✓ · MAP-03 ✓ · MAP-04 ✓ · MAP-05 ✓ · MAP-06 ✓ · MAP-07 ✓ · MAP-08 ✓ · MAP-09 ✓ · MAP-10 ⏭ **Deferred — UAT pending** (the requirement IS the device round; satisfied only by the end-of-run Pixel 7 batch)

## Decisions recorded by the plans

- Climb dice: no engine change this phase — the four climb events carry no `roll`/`need`; the rail's `rollLineFor` renders any payload generically, so an additive engine payload later lights climb dice up with zero shell change. **Deferred follow-up: "climb dice payload — quick task after UAT".**
- Movement lock is GLOBAL (user ruling 3): joiner/find/failed-climb pending → every step path refuses and pulses the card; no walk-away decline anywhere.
- Marks are coloured text glyphs (user-imported mock, reversing Phase 4's PNG icons); `icons/*.png` and `icons.js` stay on disk, only `draw()` and the legend stopped referencing them. ⧗ tofu risk is a UAT item with a one-line swap as the fallback.
- "PICK THE LOCK" does not exist — chest locks auto-resolve; the only find decision is the existing `S.pendingFind` Take/Leave.
- Descent gate is a shell pre-dispatch interception (identical engine `move`); no `combat.pending`-style engine gate was added.
- The rail is a direct child of `#app` between `<main>` and the static tab bar, visible on every tab (hidden during combat/death/win) so Gear/Hero-tab feedback that used to toast is not lost.

## Deferred human verification

The 27 Pixel 7 checks in the frontmatter `human_verification` list (aggregated by 35-05 from 35-02/03/04/05) join Phase 34's 27 (in `34-VERIFICATION.md`) as the end-of-run UAT batch against the single debug APK built by 35-05. Install (`adb install -r` + `am force-stop` + relaunch) is the orchestrator's next step now that the phone is online.
