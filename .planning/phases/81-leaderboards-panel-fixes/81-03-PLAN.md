---
phase: 81-leaderboards-panel-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLAY-GAMES-SETUP.md
  - docs/RELEASING.md
  - store-listing/LISTING.md
  - docs/SHELL-MODULES.md
  - test/unit/play-games-runbook.test.js
  - test/unit/store-listing.test.js
autonomous: true
requirements:
  - BOARD-17
must_haves:
  truths:
    - "docs/PLAY-GAMES-SETUP.md §7 describes four boards per season (DEEPEST, LONGEST, BUTCHERY, PURSE). Its leaderboards table has no LEANEST row, and every row is Larger is better"
    - "docs/PLAY-GAMES-SETUP.md has a new numbered section '## 13. Retired boards' naming LEANEST, Season 1, its id CgkIlvbN0YYPEAIQAw, why it was retired (BOARD-17), and the Play Console path to delete it AFTER the update that stops submitting to it has shipped. It also says what to do if the console refuses to delete a published leaderboard: leave it; nothing submits to it again, and it still counts toward the 70-board cap"
    - "The 70-board cap arithmetic is restated for four boards per season, counting Season 1's five as spent: 65 left, which is 16 more seasons of four"
    - "The runbook no longer claims a global LINEAGE: LINEAGE is ME-only (BOARD-13) and GRAVEYARD is folded into ME (BOARD-14). Neither ever had or will have a Play Games board"
    - "docs/RELEASING.md has '## Next Play push: release notes' with tester-facing text in the game's family-friendly deadpan voice, and '## After the push: console checklist', whose first item is deleting LEANEST, Season 1 per PLAY-GAMES-SETUP.md §13"
    - "store-listing/LISTING.md's Data safety Other actions row lists the four submitted scores (DEEPEST, LONGEST, BUTCHERY, PURSE). The data types, purposes and Collected/Shared answers are unchanged"
    - "test/unit/play-games-runbook.test.js pins the four-board table (no LEANEST row), the §13 heading with the retired id, and the numbered-section sequence 1..13 with no gap"
  artifacts:
    - path: docs/PLAY-GAMES-SETUP.md
      provides: "Four-board runbook; §13 Retired boards"
      contains: "## 13. Retired boards"
    - path: docs/RELEASING.md
      provides: "Release notes text and the post-push console checklist"
      contains: "## After the push: console checklist"
  key_links:
    - from: docs/RELEASING.md
      to: docs/PLAY-GAMES-SETUP.md §13
      via: "the checklist's LEANEST deletion item cites section 13"
      pattern: "PLAY-GAMES-SETUP.md"
  prohibitions:
    - statement: "MUST NOT tell the user to delete any board other than LEANEST, Season 1, and MUST NOT tell anyone to edit an older season's content/leaderboards.js entry beyond the lean removal already ruled"
      category: safety
      status: resolved
      verification: test
---

<objective>
Write the docs side of BOARD-17 (user ruling 2026-09-24):

- the Play Games runbook's §7 and the sections that count boards;
- a new §13 for the retired Season-1 LEANEST board (`CgkIlvbN0YYPEAIQAw`), with the delete-after-ship step;
- the release notes and console checklist for the next Play push;
- the Data safety text.

CONTEXT: "After the update ships, the user deletes the Season-1 LEANEST board (`CgkIlvbN0YYPEAIQAw`) in Play Console. Add it to the release notes and checklist."

Purpose: the user has one place to learn what to delete and when, and the next push goes out with tester-facing notes. The push itself is the orchestrator's (ask-first rule), not this plan's.

Output: the four doc edits and their doc-pinning tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 81-03 creates or changes.)

- 81-01 creates `81-DEBUG.md`, `test/unit/board-death-paths.test.js` and `test/unit/board-global-trace.test.js`.
- 81-02 changes these:
  - `engine/records.js`: `RANKED_BOARDS` has four boards; `leanRate` is deleted.
  - `src/browser/boardScores.js`: `RETIRED_BOARDS` added; `SUBMIT_BOARDS` has four boards.
  - `src/browser/pgsQueue.js#sanitizeEntry` loads old entries tolerantly.
  - `content/leaderboards.js` has four ids; the `lean` copy is gone from `content/boards.js`.
- **This plan:** `docs/PLAY-GAMES-SETUP.md`: §6/§7/§9/§10/§11 describe four boards, plus the new `## 13. Retired boards`.
- **This plan:** `docs/RELEASING.md`: `## Next Play push: release notes` and `## After the push: console checklist`.
- **This plan:** `store-listing/LISTING.md` (four scores in the Other actions row) and the `docs/SHELL-MODULES.md` Phase 68 encodings paragraph.
- 81-04 restructures the panel:
  - `BOARD_IDS` = deep, days, kills, purse, combo; `sortGraveyard` deleted.
  - `chips.me`; ME/ALL/FRIENDS scopes; `buildRail(board, scope)`.
  - The global LINEAGE view is removed; `GLOBAL_BOARDS` has four boards; `defaultScope()` and `scopePicked` are added.
- 81-05 adds `reconcileBests(record, graves)` and the boot reconciliation.
- 81-06 fixes the global boards: `isOwnRecord`, `toGlobalEntry({ meId, mine, scope })`, `invalidate()`, `loadTopScores({ forceReload })`, the pin rule, `global.hiddenYou`, and the `onOpen` seam.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/81-leaderboards-panel-fixes/81-CONTEXT.md
@docs/PLAY-GAMES-SETUP.md
@docs/RELEASING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The Play Games runbook describes four boards and adds §13 Retired boards</name>
  <files>docs/PLAY-GAMES-SETUP.md, test/unit/play-games-runbook.test.js</files>
  <read_first>
    - docs/PLAY-GAMES-SETUP.md (whole file: §6 steps 5, 6, 8 and 11; the "Two facts" bullets; §7 names, table and "The LEANEST limit" paragraph; §9; §10's "LINEAGE global (D-19)" bullet; §11's check table)
    - test/unit/play-games-runbook.test.js (REQUIRED list lines 27-66; the table test lines 114-126; the numbered-sections test lines 172-178)
    - content/leaderboards.js (the Season-1 ids, for the table cross-check)
  </read_first>
  <action>
Edit docs/PLAY-GAMES-SETUP.md.

**§6 (Order of operations)**

- Step 5 creates the four Season-1 leaderboards: DEEPEST, LONGEST, BUTCHERY, PURSE.
- Step 6 sends "the four leaderboard IDs".
- Step 8 checks "one death on all four boards".
- Step 11 creates "the next four boards".
- Keep every section citation the step-order test relies on (sections 2, 3, 4, 5, 7, content/leaderboards.js, docs/RELEASING.md, 11, 12, store-listing/LISTING.md, 9).
- Rewrite the "Two facts" bullets:
  - LINEAGE and GRAVEYARD get no PGS board. Since v2.1, LINEAGE is ME-only (BOARD-13), and GRAVEYARD was folded into the ME scope (BOARD-14).
  - The cap: four boards per season. Season 1 spent five, since LEANEST was created and later retired, so 65 remain, which is 16 more seasons of four.

**§7**

- The heading becomes "one set of four per season".
- The names list drops LEANEST.
- The table drops the LEANEST row.
- Delete "The LEANEST limit" paragraph.
- Add one sentence pointing at §13 for the retired board.

**§9:** "four new leaderboards"; the season arithmetic is restated for four.

**§10:** replace the "LINEAGE global (D-19)" bullet. LINEAGE is ME-only since v2.1: there is no global lineage view and no per-combo board (per-sub-class global boards are backlog 999.11, not this release).

**§11:** "all four boards", and the example table drops LEANEST.

**Add `## 13. Retired boards`**, containing:

- A small table whose columns are Season | Board | Internal key | ID | Retired in, with one row: 1 | LEANEST | lean | `CgkIlvbN0YYPEAIQAw` | v2.1 (BOARD-17). The row starts with the season number, so no table row anywhere in the file begins with the board name.
- Why it was retired (v2.1 BOARD-17): steps per floor let a 1-step death top it, and "deepest floor, then fewest steps" is exactly DEEPEST's ordering.
- The instruction: delete it only AFTER the update that stops submitting to it is live on the track the testers use. Path: Play Console → Delve, Die, Repeat → Grow users → Play Games Services → Setup and management → Leaderboards → LEANEST, Season 1 → delete.
- The caveat: if Play Console will not delete a published leaderboard, leave it. Nothing submits to it again, and it still counts toward the 70.
- A note that the code carries no id for it any more (content/leaderboards.js).

Edit test/unit/play-games-runbook.test.js:

- Keep "LEANEST" in REQUIRED; §13 still names it.
- Replace the ordering test with a four-row check. DEEPEST, LONGEST, BUTCHERY and PURSE are each Larger is better, and no row of the §7 table is the retired board's.
- Add a test that the `## 13. Retired boards` section contains `CgkIlvbN0YYPEAIQAw`, the word "after", and "70".
- The numbered-sections test now expects at least 13 sections.
- Remove "Smaller is better" from REQUIRED only if no section still says it; §13 may explain the retired ordering, so the executor decides from the final text.
  </action>
  <verify>
    <automated>node --test test/unit/play-games-runbook.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## 13. Retired boards" docs/PLAY-GAMES-SETUP.md` equals 1.
    - The §13 section contains `CgkIlvbN0YYPEAIQAw` (`grep -c "CgkIlvbN0YYPEAIQAw" docs/PLAY-GAMES-SETUP.md` is at least 1).
    - No table row in the runbook begins with the retired board's name: `node --input-type=module -e "import fs from 'node:fs'; const d=fs.readFileSync('docs/PLAY-GAMES-SETUP.md','utf8'); if (d.split(/\r?\n/).some((l)=>/^\| LEANEST\b/.test(l))) process.exit(1)"` exits 0.
    - `node --test test/unit/play-games-runbook.test.js` exits 0.
  </acceptance_criteria>
  <done>The runbook describes the four live boards and tells the user exactly when and how to delete LEANEST.</done>
</task>

<task type="auto">
  <name>Task 2: Release notes, the post-push console checklist, the Data safety text and the module doc</name>
  <files>docs/RELEASING.md, store-listing/LISTING.md, docs/SHELL-MODULES.md, test/unit/store-listing.test.js</files>
  <read_first>
    - docs/RELEASING.md (whole file)
    - store-listing/LISTING.md (the Data safety section around lines 70-95)
    - test/unit/store-listing.test.js (whole file, to confirm no pin names five scores; update any that does)
    - docs/SHELL-MODULES.md lines 295-310 (the Phase 68 encodings paragraph)
    - .planning/REQUIREMENTS.md lines 46-55 (BOARD-09..17, the source for the release-note bullets)
  </read_first>
  <action>
In docs/RELEASING.md, add `## Next Play push: release notes` after "Every update".

It holds the tester-facing text to paste into Play Console's release notes: a short plain list in the game's family-friendly deadpan voice, with no internal ids. It covers:

- The Leaderboards panel now has ME | ALL | FRIENDS; signed in, it opens on ALL.
- Your own score is tagged YOU and appears once.
- The boards refresh when you open them.
- LEANEST and GRAVEYARD are gone; your own dead are all still under ME, epitaphs included.
- Every finished run lands on your own boards.

Then add `## After the push: console checklist`:

1. Delete LEANEST, Season 1 (`CgkIlvbN0YYPEAIQAw`) in Play Console once the new build is live (docs/PLAY-GAMES-SETUP.md section 13).
2. With two signed-in accounts (you and a friend), confirm a new DEEPEST score from one shows on the other's ALL board after reopening the Leaderboards panel (the milestone-close device check).

State that the push itself follows the standing ask-first rule.

In store-listing/LISTING.md's Data safety table, the Other actions row says "the four leaderboard scores (DEEPEST, LONGEST, BUTCHERY, PURSE)". Add one sentence under the table: since v2.1, LEANEST is no longer submitted; the Data safety answers themselves are unchanged. Do not change any Yes/No cell. Update store-listing.test.js only if a pin names the five-score list.

In docs/SHELL-MODULES.md's Phase 68 encodings paragraph, the submitted scores are the four (DEEPEST, LONGEST, BUTCHERY, PURSE). Add a clause: LEANEST was retired in v2.1 (BOARD-17); LINEAGE is ME-only and GRAVEYARD is folded into ME. Do not touch the bridge-table markers that test/unit/bridge-registry.test.js reads.
  </action>
  <verify>
    <automated>node --test test/unit/store-listing.test.js test/unit/bridge-registry.test.js test/unit/play-games-runbook.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Next Play push: release notes" docs/RELEASING.md` equals 1, and `grep -c "^## After the push: console checklist" docs/RELEASING.md` equals 1.
    - The checklist names the retired id: `grep -c "CgkIlvbN0YYPEAIQAw" docs/RELEASING.md` is at least 1.
    - `grep -c "the four leaderboard scores (DEEPEST, LONGEST, BUTCHERY, PURSE)" store-listing/LISTING.md` equals 1.
    - `node --test test/unit/store-listing.test.js test/unit/bridge-registry.test.js test/unit/play-games-runbook.test.js` exits 0.
  </acceptance_criteria>
  <done>The next push has its release notes and its after-push checklist, and the listing and module docs match the four submitted boards.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/play-games-runbook.test.js test/unit/store-listing.test.js test/unit/bridge-registry.test.js` is green.
- No code file is modified by this plan.
</verification>

<success_criteria>
- BOARD-17's docs, release-note and checklist half is complete, and the LEANEST deletion step has a single source of truth (§13) that the checklist points at.
</success_criteria>

<output>
Create `.planning/phases/81-leaderboards-panel-fixes/81-03-SUMMARY.md` when done.
</output>
