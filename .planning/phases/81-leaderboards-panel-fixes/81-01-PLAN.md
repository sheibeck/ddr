---
phase: 81-leaderboards-panel-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md
  - test/unit/board-death-paths.test.js
  - test/unit/board-global-trace.test.js
autonomous: true
requirements:
  - BOARD-15
  - BOARD-16
  - BOARD-09
must_haves:
  truths:
    - "Debug first, fix after (CONTEXT, BOARD-15/16): no commit of this plan touches a path under engine/, src/, content/ or mazeworld.html. The only files written are 81-DEBUG.md and the two new test files"
    - "81-DEBUG.md has a '## Root causes' table with exactly one row for each of R-09 (YOU tag), R-10 (row shown twice), R-15 (run missing from ME DEEPEST), R-16a (submission), R-16b (fetch freshness) and R-16c (public-collection visibility). Each row carries a verdict of CONFIRMED, RULED OUT or DEVICE-ONLY, file:line evidence, the test name that pins it, and the plan that fixes it (81-05 or 81-06)"
    - "test/unit/board-death-paths.test.js drives six death paths through the real src/browser/engineAdapter.js dispatch(): combat, trap, starvation, abandon, a resumed save, and a relaunch after death. Each path seeds a depth-9 run and then records a depth-10 run, asserting ME DEEPEST order, persistence to ddr.bests.v1, and agreement with the graveyard stone's hash"
    - "Every confirmed defect is pinned by a node:test test carrying a todo option that names its R-id, so it reports without failing the suite. Every ruled-out hypothesis is pinned by a passing test"
    - "board-global-trace.test.js holds the invariant 'the signed-in player's own submitted score row always resolves to YOU' as a todo test (R-09) over two fake-plugin payload shapes: rows with no scoreHolder, and rows whose holder id differs from the account id"
    - "81-DEBUG.md '## Assumption delta' answers the question 'is the signed-in id from the same API family as the score rows' ids?' It cites the plugin sources (Pgs.kt Player.toJsObject, LeaderboardsModule.kt LeaderboardScore.toJsObject putIfPresent scoreHolder, SignInModule.kt resolveWithPlayer) and states CONFIRMED or REVISED against 81-06's assumption_delta_decision (primary noun: the player's own leaderboard score record)"
    - "81-DEBUG.md '## Device session' records NOT NEEDED with its reason, or raises exactly ONE dynamic checkpoint:human-action with the prepared recipe. No second device pause exists anywhere in Phase 81"
    - "npm test passes with the two new files included. The only failures allowed are the 7 known worktree-only CRLF doc-ledger tests (docs/CLASS-PASS.md, docs/FLEE.md)"
  artifacts:
    - path: .planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md
      provides: "Root causes table, per-R sections, fix routing, assumption delta, device-session decision"
      contains: "## Root causes"
    - path: test/unit/board-death-paths.test.js
      provides: "BOARD-15 death-path pins through the real adapter"
      min_lines: 80
    - path: test/unit/board-global-trace.test.js
      provides: "BOARD-16/09/10 submit and fetch trace pins, the YOU invariant (todo)"
      min_lines: 80
  key_links:
    - from: test/unit/board-death-paths.test.js
      to: src/browser/engineAdapter.js
      via: "boot/dispatch/getBests/getGraveyard/waitForPending imports"
      pattern: "from \"../../src/browser/engineAdapter.js\""
    - from: test/unit/board-global-trace.test.js
      to: src/browser/globalBoards.js and src/browser/playGames.js
      via: "createGlobalBoards, toGlobalEntry, createPlayGames({ loadPlugin }) over a fake plugin"
      pattern: "createGlobalBoards|createPlayGames"
  prohibitions:
    - statement: "MUST NOT ask the user for more than ONE device session in Phase 81, and MUST NOT ask for any unless no single code change is correct under every surviving hypothesis. A debug build costs the player their local runs, because the Play build has to be uninstalled"
      category: values
      status: resolved
      verification: judgment
---

<objective>
Find the root causes of the two live Play bugs before anything is fixed (CONTEXT: "Plan 1 is a root-cause session (debug first, fix after)"):

- **BOARD-15:** runs lost locally. A depth-10 run showed in the Graveyard but not on the player's own DEEPEST board, above their depth-9 run.
- **BOARD-16:** scores lost globally. A friend's depth-11 DEEPEST never showed on the user's ALL board, while the friend saw the user's depth-9 entry.

The same session confirms the BOARD-09 identity question the assumption-delta detector raised, and re-checks the BOARD-10 "shown twice" row against the device report.

Purpose: 81-05 (local) and 81-06 (global) fix only what this plan confirms. Each confirmed defect is left behind as a pinned `todo` test that the fix plan turns green.

Output: `81-DEBUG.md`, `test/unit/board-death-paths.test.js` and `test/unit/board-global-trace.test.js`. No production code changes.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 81-01 creates.)

- **This plan:** `.planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md`. It holds the Root causes table (R-09, R-10, R-15, R-16a, R-16b, R-16c), Fix routing, Assumption delta and the Device session decision.
- **This plan:** `test/unit/board-death-paths.test.js` (BOARD-15 pins) and `test/unit/board-global-trace.test.js` (BOARD-16/09/10 pins, including the YOU invariant).
- 81-02 changes these:
  - `engine/records.js`: `BOARD_IDS` without `lean`; `RANKED_BOARDS` = deep, days, kills, purse; `leanRate` deleted.
  - `src/browser/boardScores.js`: `RETIRED_BOARDS` added (frozen, holding the one retired id `lean`); `SUBMIT_BOARDS` filters retired boards out; `SCORE_ORDER` has four keys.
  - `src/browser/pgsQueue.js#sanitizeEntry` drops retired-board scores and acks.
  - `content/leaderboards.js` `LEADERBOARD_IDS[1]` has four keys, and `BOARD_COPY.lean` is gone.
- 81-03 changes the docs: `docs/PLAY-GAMES-SETUP.md` gets §13 "Retired boards"; `docs/RELEASING.md` gets "Next Play push: release notes" and "After the push: console checklist"; the `store-listing/LISTING.md` Data safety row lists four scores.
- 81-04 restructures the panel:
  - `engine/records.js`: `BOARD_IDS` = deep, days, kills, purse, combo; `sortGraveyard` deleted.
  - `content/boards.js`: `BOARDS_PANEL_COPY.chips.me`.
  - `boardsView.js`: ME/ALL/FRIENDS strip scopes; `buildRail(board, scope)`; the global LINEAGE view is removed.
  - `globalBoards.js`: `GLOBAL_BOARDS` has four boards; `LINEAGE_SAMPLE_N` and `sampled` are removed.
  - `boardsPanel.js`: `defaultScope()`, and `scopePicked` in `state()`.
- 81-05 fixes local recording: `engine/records.js#reconcileBests(record, graves)`, the `loadBests()` reconciliation, and every R-15 defect this plan routes to it.
- 81-06 fixes the global boards:
  - `globalBoards.js`: `isOwnRecord(score, mine)`, `toGlobalEntry(score, index, { meId, mine, scope })`, and `createGlobalBoards(...).invalidate()`.
  - `playGames.js#loadTopScores({ forceReload })`.
  - `boardsView.js`: the pin rule.
  - `content/boards.js`: `global.hiddenYou`.
  - `boardsPanel.js`: the `onOpen` seam, with its `mazeworld.html` wiring.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-leaderboards-panel-fixes/81-CONTEXT.md
@.planning/todos/pending/2026-09-25-leaderboards-lose-runs-locally-and-globally.md
@engine/records.js
@src/browser/engineAdapter.js
@src/browser/globalBoards.js
@src/browser/pgsQueue.js
@src/browser/playGames.js
</context>

<parallel_wave_notice>
81-02 (LEANEST removal) and 81-03 (docs) run in this same wave, and 81-04/81-06 later reshape the panel and the global controller. So the two new test files must follow three rules:

- Never name the `lean`, `yard` or `combo` board ids and never use LEANEST/GRAVEYARD/LINEAGE copy. Take board lists from the `RANKED_BOARDS`/`SUBMIT_BOARDS` imports and use `deep` (plus `days`/`kills`/`purse` only where a test needs them).
- Never assert on a global snapshot's `sampled` field (81-04 removes it). Assert only on `status`, `entries`, `you`, `total` and `stale`.
- Never drive `createBoardsPanel`. The panel's scope behaviour is traced in 81-DEBUG.md prose with line refs, because 81-04 rewrites that controller.
</parallel_wave_notice>

<tasks>

<task type="auto">
  <name>Task 1: BOARD-15 local trace: every death path through the real adapter, plus the hidden-scope and stale-view trace</name>
  <files>test/unit/board-death-paths.test.js, .planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md</files>
  <read_first>
    - src/browser/engineAdapter.js (whole file: dispatch 630-694, recordDeath 398-426, persistGrave 448-519, loadBests 181-212, loadGraveyard 239-267, boot 570-602)
    - engine/records.js (updateBests 315-362, sanitizeBests 263-301, prune 216-236, backfillBests 398-412)
    - engine/death.js (buildRunSummary 70-94, die 120-155, bury 157-160)
    - engine/engine.js lines 110-135 (the abandon action)
    - engine/encounters.js lines 55-105 (trap damage then die "trap")
    - engine/movement.js lines 620-775 (the day tick that starves: die "starve")
    - engine/combat.js lines 2140-2370 (the foe-turn hero damage branch: die "combat")
    - src/browser/storage.js (getItem is not queued behind in-flight writes; the native-to-localStorage degrade path)
    - test/unit/bests-adapter.test.js (the harness to copy: withFakeLocalStorage, boot/loadBests/waitForPending, the storage flush import)
    - src/browser/boardsPanel.js lines 777-851 (onScope toggles back to local only when the active chip is re-tapped; openFromTab resets the scope to local) and src/browser/globalBoards.js lines 260-279 (the 5-minute TTL; no invalidation hook)
    - mazeworld.html lines 6320-6330 (readBoardsData) and 7060-7110 (onRunRecorded, handlePgsFlush)
    - .planning/todos/pending/2026-09-25-leaderboards-lose-runs-locally-and-globally.md
  </read_first>
  <action>
Create `test/unit/board-death-paths.test.js` on the bests-adapter.test.js harness:

- a fake `globalThis.localStorage` per test;
- `boot(seed)` to start;
- `await waitForPending()` plus the storage.js `flush()` before reading storage back.

Import `RANKED_BOARDS`, `compareRuns` and `runHash` from engine/records.js. Never hard-code a board list.

Seed a depth-9 run in every test the same way: an abandon death on a run whose `getState().floor.depth` was set to 9 before dispatch. Then record the depth-10 run through the path under test on a fresh run whose depth is 10. The live state is reached through `getState()`. Set the minimum state the path needs, then dispatch the real action, so every death flows through `dispatch()`'s `died` choke point.

The six paths:

1. **Combat.** Put the hero in a fight they lose: `wp` 1 and a foe whose turn lands. Dispatch the combat action that hands the foe its turn, repeating until `getState().dead` is true (the seeded rng is deterministic). The engine anchor is the foe-turn hero damage branch in engine/combat.js.
2. **Trap.** Place a trap feature on a cell next to the hero, with `wp` low enough, and move onto it. Anchors: engine/movement.js feature dispatch and encounters.js `die(state, "trap")`.
3. **Starvation.** Leave no food and `wp` 1, then dispatch the action that ticks the day. Anchor: `die(state, "starve")` in engine/movement.js.
4. **Abandon.** `dispatch({ type: "abandon" })`.
5. **Resumed save.** Write `serializeRun(state)` of a depth-10 run to `ddr.delve.v1`, call `boot()` again (the rehydrate path), then abandon.
6. **Relaunch after death.** Die at depth 10, drain the writes, call `boot()` again (the relaunch), and assert on the reloaded record.

Each path test asserts all of:

- `getBests().boards.deep[0]` is the depth-10 run's hash and `[1]` the depth-9 hash;
- the depth-10 run is in `getBests().runs`;
- for every board in `RANKED_BOARDS` where `compareRuns(board, depth10, depth9) < 0`, the depth-10 hash is ranked above the depth-9 hash;
- `JSON.parse` of stored `ddr.bests.v1` deep-equals `getBests()`;
- `getGraveyard().graves[0].hash` equals `getBests().boards.deep[0]`.

Add two more tests:

- **Same run twice.** Replaying `updateBests` with the same summary leaves one entry.
- **Device sequence.** Depth 9 then depth 10 by abandon, then a relaunch via `boot()`.

If a path cannot be reached with a small, honest state setup, write the test through the closest real engine action. Record the reason in 81-DEBUG.md. Never call `die()` directly and never bypass `dispatch()`.

Then work the BOARD-15 hypotheses. For each one, write a test or cite code that proves or rules it out:

- **H-15a:** a death that never reaches `dispatch()`'s `died` branch.
- **H-15b:** `recordDeath` throwing, so the summary is null. Then `bury()` still writes the grave, but the bests record and the run-recorded listener are skipped.
- **H-15c:** `bests` still null at the death, which takes the lazy `persistGrave` path.
- **H-15d:** a hash dedupe collision on `rec.last`/`rec.runs`.
- **H-15e:** `prune` dropping the new run.
- **H-15f:** the ddr.bests.v1 write being lost while the ddr.graveyard.v1 write persisted: write order, the storage degrade path, and `getItem` not queued behind writes.
- **H-15g:** `sanitizeBests` on reload dropping the run.
- **H-15-view:** the player was looking at ALL while believing it was their own board. Trace the hidden local scope: no chip returns to it except re-tapping the active one, and the scope survives board switches within a panel session. Also trace the ALL snapshot, which is cached for `GLOBAL_TTL_MS` and never invalidated after the player's own submission, plus Play Games' `forceReload: false`. Cite the lines; this one is prose only, no panel test.

A test that exposes a genuine defect gets `{ todo: "R-15: <one-line defect>" }` and stays failing. All other tests pass.

In 81-DEBUG.md, write a `## R-15` section: each hypothesis, its evidence (file:line), its test name and a verdict of CONFIRMED or RULED OUT. Close with a one-line R-15 verdict naming the root cause(s). This task writes no production code.
  </action>
  <verify>
    <automated>node --test test/unit/board-death-paths.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/board-death-paths.test.js` exists and `node --test test/unit/board-death-paths.test.js` exits 0 (todo tests report without failing).
    - The file names each path in a test title: `grep -c -E "combat|trap|starv|abandon|resumed|relaunch" test/unit/board-death-paths.test.js` is at least 6.
    - The file imports `RANKED_BOARDS` from engine/records.js and never names a retired or reshaped board id. `node --input-type=module -e "import fs from 'node:fs'; const s=fs.readFileSync('test/unit/board-death-paths.test.js','utf8'); if(/['\"](lean|yard|combo)['\"]/.test(s)) process.exit(1)"` exits 0.
    - 81-DEBUG.md contains a `## R-15` heading, one line per hypothesis H-15a..H-15g plus H-15-view, each ending in `CONFIRMED` or `RULED OUT`, and a final `R-15 verdict:` line.
    - `git show --name-only` for this task's commit lists only `test/unit/board-death-paths.test.js` and `.planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md`.
  </acceptance_criteria>
  <done>Six death paths plus the device sequence are pinned through the real adapter. Every BOARD-15 hypothesis has a verdict with evidence. Any real defect is a todo test naming R-15.</done>
</task>

<task type="auto">
  <name>Task 2: BOARD-16 and BOARD-09/10 global trace: submit, fetch, visibility and the YOU identity</name>
  <files>test/unit/board-global-trace.test.js, .planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md</files>
  <read_first>
    - src/browser/pgsQueue.js (sanitizeEntry 66-84, queueEntryFor 119-128, run() 355-442: the `break outer` on the first failure)
    - src/browser/playGames.js (normalizeScore 169-180, submitScore 344-354, loadTopScores 362-382 forceReload false, loadPlayerScore 389-401, createFakePlayGames 489-654)
    - src/browser/globalBoards.js (toGlobalEntry 76-91, load 197-215, view 260-279)
    - src/browser/scoreTag.js (encodeTag, TAG_MAX_LENGTH, tagName, fitName, decodeTag)
    - src/browser/boardScores.js (boardScore deep = floor x 1,000,000 - steps; leaderboardId)
    - content/leaderboards.js (Season-1 ids)
    - src/browser/boardsView.js lines 550-561 (buildGlobalRows pins snap.you under the divider when no listed entry is you) and 647-669 (buildGlobalStanding)
    - src/browser/accountChip.js lines 314-327 (apply: the identity player comes from the sign-in result) and src/browser/account.js lines 55-79
    - mazeworld.html lines 7060-7140 (createSubmissionQueue and createGlobalBoards wiring; playerId getter reads account.identity().player?.id)
    - node_modules/@modbender/capacitor-play-games/android/src/main/java/com/idleflowgames/playgames/LeaderboardsModule.kt (loadScores passes forceReload; LeaderboardScore.toJsObject uses putIfPresent for scoreHolder)
    - node_modules/@modbender/capacitor-play-games/android/src/main/java/com/idleflowgames/playgames/Pgs.kt (Player.toJsObject puts playerId; forceReload defaults false) and SignInModule.kt (resolveWithPlayer)
    - test/unit/playGames.test.js lines 100-160 (the fakePlugin/loaderFor pattern) and test/unit/globalBoards.test.js lines 340-430 (fake provider fetch tests)
    - docs/PLAY-GAMES-SETUP.md §7 and §10
  </read_first>
  <action>
Create `test/unit/board-global-trace.test.js`. It uses `createPlayGames({ loadPlugin })` over a recording fake plugin whose payloads mirror the Kotlin serializers exactly:

- `scores[]` items carry `rank`, `rawScore`, `scoreTag` and `scoreHolderDisplayName`, and carry `scoreHolder` ONLY when the case provides one (putIfPresent);
- `leaderboard.variants[]` items carry `timeSpan`, `collection` and `numScores`.

It also uses `createGlobalBoards` and `createSubmissionQueue` over `createFakePlayGames` where a pure best-score store is enough. Only the `deep` board is used (days/kills/purse only if needed); see the parallel-wave notice.

Pins:

**(R-16a, submit side)**

- `queueEntryFor` on depth 9, 10 and 11 summaries (steps 0, 431 and 999,999) gives safe-integer DEEPEST scores in strict floor order.
- The depth-11 score is above 10,999,000.
- Every tag passes both the queue's tag regex and playGames' tag check. Cover worst-case names: 40 characters, apostrophes, accented letters, spaces, and every numeric field at its cap.
- `submitScore` receives the Season-1 DEEPEST id from content/leaderboards.js.
- **Queue wedge.** A provider that returns `{ ok: false }` for the first queued entry forever means every later entry is never submitted across repeated flushes. Record this as CONFIRMED (todo `R-16a: one permanently rejected entry blocks every later submission`) or RULED OUT with the reason.

**(R-16b, freshness)**

- The native provider's `loadTopScores` passes `forceReload: false` to the plugin. Read the fake plugin's recorded args; todo `R-16b: fetch never forces a reload`.
- After the player's own successful flush, `createGlobalBoards(...).view()` within `GLOBAL_TTL_MS` still returns the pre-submission snapshot, and no controller method exists to drop it; todo `R-16b: no invalidation after the player's own submission or on reopening the panel`.

**(R-16c, public visibility)**

- A fake where `loadPlayerScore` returns the player's record with rank null, while `loadTopScores` (public) omits it.
- The current view pins that record under the "NOT IN THE TOP TEN · YOUR BEST RUN" divider as if it were ranked; todo `R-16c: a record Play Games withholds from the public list is shown as ranked-but-off-list instead of an honest note`.
- Quote Google's rule in 81-DEBUG.md: "If your player has not chosen to share their gameplay activity publicly, they won't appear in this leaderboard." (https://developer.android.com/games/pgs/leaderboards, read 2026-09-24).

**(R-09, YOU identity)**

Two payload shapes:

- (i) score rows with no `scoreHolder`: `normalizeScore` gives `playerId` "", so `toGlobalEntry` marks no row `you`.
- (ii) rows whose `scoreHolder.playerId` differs from the account id (`account.identity().player.id` comes from the sign-in `Player`), while `loadCurrentPlayerScore` returns the identical record: same rank, rawScore and scoreTag.

For both, write the todo invariant test `the signed-in player's own submitted score row always resolves to YOU`. It asserts exactly one row has `you === true` and that it is the player's own record.

**(R-10)**

- Feed the R-09 snapshot into `boardsView` with `board: "deep"`, `scope: "all"`, `signedIn: true`.
- The player's row renders twice: once listed, once pinned under the divider. Todo `R-10: the player's sole #1 entry is shown twice`.

Also trace these in 81-DEBUG.md with line refs:

- the collection (public for ALL, friends for FRIENDS);
- the time span (allTime);
- the Season-1 id map;
- the encoding `scoreOrder` (largerIsBetter for DEEPEST);
- the sign-in and Compete gates on submission (`isCompeting`/`isSignedIn`; a signed-out death waits in the queue).

Note which of these the device report can and cannot distinguish.

Write `## R-09`, `## R-10`, `## R-16a`, `## R-16b` and `## R-16c` sections in 81-DEBUG.md. Each gives the hypothesis, evidence (file:line and plugin source), test name and verdict.

In `## Assumption delta`, answer "is the signed-in id taken from the same API or field family as the score rows' ids?":

- the account id comes from `PlayersClient.getCurrentPlayer()` via the sign-in result (`Player.getPlayerId()`);
- the row ids come from `LeaderboardScore.getScoreHolder().getPlayerId()`, which the plugin emits only when present;
- `globalBoards.js:211` prefers the account id over the player's own record's id.

State whether 81-06's decision holds: primary noun is the player's own leaderboard score record, decision promote. Mark it CONFIRMED or REVISED with the reason. This task writes no production code.
  </action>
  <verify>
    <automated>node --test test/unit/board-global-trace.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/board-global-trace.test.js` exits 0.
    - The file contains the invariant title verbatim: `grep -c "the signed-in player's own submitted score row always resolves to YOU" test/unit/board-global-trace.test.js` is at least 1.
    - Every todo option names an R-id: `grep -c -E "todo: \"R-(09|10|16a|16b|16c)" test/unit/board-global-trace.test.js` equals the file's total count of `todo:` occurrences (`grep -c "todo:"`).
    - The file never names a retired or reshaped board id and never reads `sampled`. `node --input-type=module -e "import fs from 'node:fs'; const s=fs.readFileSync('test/unit/board-global-trace.test.js','utf8'); if(/['\"](lean|yard|combo)['\"]/.test(s)||/\.sampled/.test(s)) process.exit(1)"` exits 0.
    - 81-DEBUG.md contains the headings `## R-09`, `## R-10`, `## R-16a`, `## R-16b`, `## R-16c` and `## Assumption delta`, and the quoted Google sentence about sharing gameplay activity publicly.
    - `git show --name-only` for this task's commit lists only `test/unit/board-global-trace.test.js` and `.planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md`.
  </acceptance_criteria>
  <done>Submit, fetch, visibility and identity are each traced end to end, with a pinned test and a verdict. The assumption-delta question has an evidenced answer.</done>
</task>

<task type="auto">
  <name>Task 3: Root causes table, fix routing and the device-session gate</name>
  <files>.planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md</files>
  <read_first>
    - .planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md (the sections Tasks 1-2 wrote)
    - .planning/phases/81-leaderboards-panel-fixes/81-CONTEXT.md (the device-session rule: ONE short adb session only if ESSENTIAL)
    - .planning/phases/81-leaderboards-panel-fixes/81-05-PLAN.md and 81-06-PLAN.md (objective and files_modified, so each routed defect lands in a plan that owns its file)
    - docs/RELEASING.md (Play build vs debug build signers; uninstalling loses Preferences data)
  </read_first>
  <action>
Top 81-DEBUG.md with a `## Root causes` table. Columns: R-id | symptom (device words) | verdict (CONFIRMED / RULED OUT / DEVICE-ONLY) | root cause, one sentence | evidence (file:line) | pinning test | fix owner. Rows in this order: R-09, R-10, R-15, R-16a, R-16b, R-16c.

Then a `## Fix routing` section with the ownership rule the fix plans read:

- a defect in engine/records.js or src/browser/engineAdapter.js goes to 81-05;
- a defect in src/browser/globalBoards.js, playGames.js, boardsView.js, boardsPanel.js, pgsQueue.js, scoreTag.js, content/boards.js or mazeworld.html goes to 81-06;
- a defect outside both lists is named explicitly with the file, and flagged for the orchestrator.

Also list the device-report sentences each root cause explains, so every symptom in the todo file is accounted for (the three symptoms and the "shown twice" report).

Then the `## Device session` decision. A device session is ESSENTIAL only if both hold:

- some root cause keeps two or more surviving explanations whose fixes are mutually exclusive, so no single change is correct under all of them; and
- no code, plugin-source or Google-doc evidence separates them.

Write down the cost. The shipped release build logs nothing from the Capacitor bridge (release logging is off). A debug build that can sign in needs:

- the debug keystore's SHA-1 as a Play Games credential (docs/PLAY-GAMES-SETUP.md §3, optional today);
- uninstalling the Play build first, which wipes the player's local runs and graveyard.

If NOT ESSENTIAL, record: `Device session: NOT NEEDED`, the reason, and where confirmation happens instead:

- the post-phase Play internal-testing push the orchestrator offers (ask-first rule);
- the milestone-close two-device checklist (user plus friend) that 81-06 adds as a human check.

If ESSENTIAL, stop here and return a dynamic `checkpoint:human-action`, the same mechanism as an auth gate. It asks for ONE short session and carries a recipe written into 81-DEBUG.md:

1. Re-pair adb: `adb mdns services`, then `adb connect <ip:port>` (the port rotates).
2. The exact build and install steps, with the data-loss warning stated first.
3. The exact screens to open.
4. The one `adb logcat -d` capture command with its tag filter.
5. What to send back.

This must be the only device pause in Phase 81.
  </action>
  <verify>
    <automated>node --input-type=module -e "import fs from 'node:fs'; const s=fs.readFileSync('.planning/phases/81-leaderboards-panel-fixes/81-DEBUG.md','utf8'); for (const h of ['## Root causes','## Fix routing','## Device session','## Assumption delta']) if(!s.includes(h)) { console.error('missing '+h); process.exit(1); } for (const r of ['R-09','R-10','R-15','R-16a','R-16b','R-16c']) if(!s.includes('| '+r)) { console.error('missing row '+r); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - 81-DEBUG.md contains `## Root causes` with a table row starting `| R-09`, `| R-10`, `| R-15`, `| R-16a`, `| R-16b` and `| R-16c`, and each row contains one of `CONFIRMED`, `RULED OUT` or `DEVICE-ONLY`.
    - Each row's fix-owner cell names `81-05` or `81-06` (or `none` when RULED OUT).
    - 81-DEBUG.md contains `## Fix routing` naming both owner plans and their files.
    - 81-DEBUG.md contains `## Device session` with either the line `Device session: NOT NEEDED` plus a reason, or a numbered recipe for exactly one session.
    - `npm test` passes, except the 7 known worktree-only CRLF doc-ledger failures (docs/CLASS-PASS.md, docs/FLEE.md) when run inside a worktree.
  </acceptance_criteria>
  <done>Every symptom has a root cause verdict and a fix owner. The fix plans can read 81-DEBUG.md and act without re-investigating. The device-session question is decided and recorded.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/board-death-paths.test.js test/unit/board-global-trace.test.js` exits 0.
- `npm test` passes, except the 7 known worktree-only CRLF doc-ledger tests.
- This plan's commits touch no production file: `git show --name-only` on each lists only the three files in files_modified.
- 81-DEBUG.md answers each of the three device symptoms from the todo file.
</verification>

<success_criteria>
- BOARD-15 and BOARD-16 have root causes found before any fix, as CONTEXT requires, each pinned by a test.
- The BOARD-09 identity question is answered with plugin-source evidence.
- 81-05 and 81-06 each have a precise, file-routed list of what to fix.
</success_criteria>

<output>
Create `.planning/phases/81-leaderboards-panel-fixes/81-01-SUMMARY.md` when done. It repeats the Root causes table and the Device session decision.
</output>
