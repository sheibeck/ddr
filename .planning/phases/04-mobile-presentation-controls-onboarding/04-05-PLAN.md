---
phase: 04-mobile-presentation-controls-onboarding
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-11]
files_modified:
  - mazeworld.html
  - fonts/press-start-2p-400.woff2
  - fonts/courier-prime-400.woff2
  - fonts/courier-prime-700.woff2
  - src/browser/gameName.js
  - src/browser/nativeChrome.js
  - capacitor.config.json
autonomous: true
requirements: [UX-03, UX-08]
must_haves:
  truths:
    - "The app boots past splash into the dark torch-lit ledger theme (not the light parchment look), with Press Start 2P + Courier Prime self-hosted from local fonts/ (zero Google Fonts CDN request)"
    - "A fixed 5-tab bottom nav (MAZE / HERO / GEAR / ORACLE / DEAD) switches between screen containers; MAZE is the default landing screen"
    - "The top HUD shows real engine state (FLOOR n / DAY n / SQUARES n / RATIONS n with a depletion bar) read from the live GameState, padded for the status-bar safe-area"
    - "The display name renders from a single centralized GAME_NAME constant whose value is \"Delve, Die, Repeat\" (matching the already-renamed native manifest), never hardcoded per-file"
    - "Top HUD pads for var(--safe-area-inset-top, env(safe-area-inset-top, 0px)); tab bar/controls pad for the bottom inset; the viewport meta includes viewport-fit=cover"
    - "The native status bar is recolored to the dark theme (light icons on a dark ground) and no longer the parchment #EFE7D6"
  artifacts:
    - src/browser/gameName.js
    - fonts/press-start-2p-400.woff2
    - fonts/courier-prime-400.woff2
    - fonts/courier-prime-700.woff2
  key_links:
    - "The shell's #app root sets --mw-text-scale from settings.js textScaleForSize(readSettings().textSize) so every rem token scales together (UX-08); the maze canvas element is preserved untouched for 04-06 to frame"
  backstops:
    - statement: "Graveyard tab shows the zero-grave empty state NOBODY'S DOWN THERE YET when no graves exist"
      verification: backstop
    - statement: "Gear tab CARRIED empty state shows NOTHING LEFT TO CARRY when there are zero consumables"
      verification: backstop
    - statement: "A storage read/write failure surfaces the generic THE LEDGER WON'T OPEN fallback rather than a blank/broken screen"
      verification: backstop
    - statement: "Graveyard list renders a brief loading placeholder while mzStorage resolves, before ranked stones or the empty state"
      verification: backstop
---

<objective>
Realize slice (a): rebuild mazeworld.html's DOM/CSS chrome as the dark
torch-lit ledger shell — self-hosted fonts, the design's color/typography tokens,
the fixed 5-tab bottom nav, the real-state top HUD, and the safe-area /
dark-status-bar platform correctness — around the UNTOUCHED `<canvas id="maze">`.
After this slice the app boots into the new mobile look, shows real FLOOR/DAY/
SQUARES/RATIONS, and the five tabs switch between screen containers (MAZE live;
HERO/GEAR/ORACLE/DEAD as shells the later slices fill). The 372 tests stay green
and the app stays bootable.

Purpose: The visual + platform foundation every later slice renders inside.
Output: dark-theme mazeworld.html shell, self-hosted fonts, GAME_NAME constant,
dark status bar, safe-area-correct layout.
</objective>

## Phase Goal (user story)

**As a** phone player new to Mazeworld's rules, **I want to** move, fight, and
descend with native touch controls plus a readable character sheet, message log,
settings, and a first-run tutorial, **so that** the game feels native and
approachable and teaches me the loop instead of assuming I already know the
tabletop rules.

> Planner note: ROADMAP Phase 4's `**Goal:**` line is outcome-shaped, not in
> user-story form. This story's three slots are derived faithfully from that goal
> + 04-CONTEXT.md's phase boundary (role = "phone player"). Surfaced in the
> planner return summary; not invented from nothing.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-UI-SPEC.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-CONTEXT.md
@design/Mazeworld Mobile.dc.html
@src/browser/settings.js
@src/browser/nativeChrome.js
@capacitor.config.json
</context>

<branding_note>
The product name is LOCKED to "Delve, Die, Repeat" (04-CONTEXT.md "Game name (branding) — UPDATED 2026-09-08", superseding the earlier "Mazeworld placeholder" note). A parallel /gsd-quick already renamed the NATIVE manifest and committed it (f81942f): appId com.darktierstudios.delvedierepeat, launcher/appName "Delve, Die, Repeat", package.json name delve-die-repeat, mazeworld.html chrome updated. This plan's job is therefore NOT to reconcile a discrepancy (native already matches) but to establish the single source of truth: create src/browser/gameName.js exporting GAME_NAME = "Delve, Die, Repeat" and route the UI title/home/splash text through it so it matches the native manifest from one place. "Mazeworld" survives ONLY as the in-fiction world/setting name (Maze Master, Wilmsry, wilmst currency, lore) — do NOT strip those. The persistence-key rename (mazeworld.*.v1 → ddr.*.v1) is a SEPARATE, earlier concern owned by 04-11 (Wave 1) so all later UI/storage waves already read the renamed keys.
</branding_note>

<tasks>

<task type="auto">
  <name>Task 1: Self-hosted fonts + dark theme tokens + centralized GAME_NAME</name>
  <files>fonts/press-start-2p-400.woff2, fonts/courier-prime-400.woff2, fonts/courier-prime-700.woff2, src/browser/gameName.js, mazeworld.html</files>
  <read_first>
    - mazeworld.html lines 1-62 (existing self-hosted @font-face pattern; the ./fonts/*.woff2 relative refs build-www copies verbatim)
    - 04-UI-SPEC.md Design System (fonts) + Color (dark palette) + Typography (four roles, Press Start 2P + Courier Prime 400/700, --mw-text-scale multiplier)
    - 04-CONTEXT.md "Visual theme & fidelity" + "Game name (branding) — UPDATED 2026-09-08" (name LOCKED to "Delve, Die, Repeat"; native manifest already renamed in f81942f); this plan's branding_note
  </read_first>
  <action>
    Obtain Press Start 2P (400) and Courier Prime (400 + 700) as latin-subset .woff2 via a one-time fetch (both OFL-licensed Google Fonts per 04-RESEARCH.md A2/Open-Question-1; e.g. gwfh.mranftl.com latin-only, or extract from @fontsource then discard the npm package). Commit them into fonts/. Add local @font-face rules in mazeworld.html referencing ./fonts/*.woff2 (matching the Phase-2 pattern); the design's Google Fonts CDN link must NOT ship. Define the dark torch-lit palette (grounds #14110c/#080705/#1b170f, frames #3a3226/#6b5c3c, ink #e6ddc6, gold #e8c97a/#f6e0a2, muted #a89c82/#c9bda0, danger #e07260/#a63a2c/#6b2c22/#2a1512) as :root custom properties, and the four typography roles as rem tokens multiplied by a --mw-text-scale property (default 1.0). Retire the light-theme fonts/colors from the LIVE theme (leave old woff2 on disk, unreferenced). Create src/browser/gameName.js exporting a single GAME_NAME constant with value "Delve, Die, Repeat" (LOCKED product name); route every UI display of the game name (title/home/splash text) through it (never a duplicated hardcoded title) so the visible chrome matches the already-renamed native manifest from one source. Keep "Mazeworld" only where it denotes the in-fiction world/setting (Maze Master, Wilmsry, wilmst, lore) — do not strip those. Do NOT edit capacitor.config.json appId/appName (already renamed in f81942f; native identity is correct).
  </action>
  <verify>
    <automated>node tools/build-www.mjs && ! grep -Eq "fonts\.(googleapis|gstatic)\.com" www/index.html && ls www/fonts/press-start-2p-400.woff2 www/fonts/courier-prime-400.woff2 www/fonts/courier-prime-700.woff2</automated>
  </verify>
  <acceptance_criteria>
    - After `node tools/build-www.mjs`, `www/index.html` has no fonts.googleapis.com / fonts.gstatic.com reference and the three new woff2 exist under `www/fonts/` (assert via a small check script or `grep -L`).
    - `src/browser/gameName.js` exports GAME_NAME === "Delve, Die, Repeat"; the UI title/home/splash text is wired from it (no duplicated hardcoded title literal across files), matching the native appName.
    - `npm test` stays green (372).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 2: 5-tab bottom nav + real-state HUD + screen-container shells</name>
  <files>mazeworld.html</files>
  <read_first>
    - 04-UI-SPEC.md Screens 1-6 (Maze crawl HUD; the 5 canonical tab labels; active-tab treatment) + Component Contracts (tab bar) + 48dp Corrections (tab min-height:48px)
    - mazeworld.html globals: window.__mzState, window.paint, window.draw, window.logLine; existing HUD/panels; adapter dispatch
    - engine/state.js GameState (floor.depth, day, steps, c.rations) for HUD bindings
    - 04-UI-SPEC.md "Copywriting Contract" + "UI Considerations" empty/loading/error backstops for Graveyard + Gear
    - src/browser/settings.js readSettings + textScaleForSize
  </read_first>
  <action>
    Build the fixed 5-tab bottom nav (MAZE / HERO / GEAR / ORACLE / DEAD — exact canonical copy) as an equal-flex row with the active-tab treatment (gold label + 3px top indicator + #241d12 fill; inactive #8f856f/transparent), each tab min-height:48px (expand hit area via padding, keep the chip visually compact). Wire tab switching to show/hide five screen containers; MAZE is the default landing screen and must contain the existing <canvas id="maze"> element UNCHANGED (preserve id — 04-06 frames it). Rebuild the top HUD to read live GameState via the adapter (FLOOR floor.depth / DAY day / SQUARES steps; RATIONS c.rations + 78x5px depletion bar) and re-render from window.paint(). Render HERO/GEAR/ORACLE/DEAD as shell containers with correct headers, and for GEAR CARRIED empty + DEAD graveyard empty/loading/error, use the Copywriting Contract copy (NOTHING LEFT TO CARRY / NOBODY'S DOWN THERE YET / THE LEDGER WON'T OPEN) so no shell is blank or off-tone. On boot, set #app root --mw-text-scale from textScaleForSize(readSettings().textSize) (UX-08). Keep the app bootable; do not break existing dispatch/paint/draw wiring.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/engineAdapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `www/index.html` after build contains the five tab labels MAZE/HERO/GEAR/ORACLE/DEAD and still contains `id="maze"` (canvas preserved).
    - The HUD binds FLOOR/DAY/SQUARES/RATIONS to GameState fields (floor.depth/day/steps/c.rations), not static text.
    - Graveyard + Gear shells render their empty/error copy strings from the Copywriting Contract.
    - `npm test` stays green (372).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 3: Safe-area layout + dark native status bar</name>
  <files>mazeworld.html, src/browser/nativeChrome.js, capacitor.config.json</files>
  <read_first>
    - 04-RESEARCH.md §2 + Pitfall 2 + Pitfall 3 + Code Example #2 (var(--safe-area-inset-*, env(...)) fallback chain; viewport-fit=cover; Android 15+ edge-to-edge; status-bar setStyle unaffected but setBackgroundColor fragile)
    - 04-UI-SPEC.md "Safe Areas & Layout" (top HUD calc(12px + inset); bottom tab/controls calc(8px + inset); portrait already locked)
    - src/browser/nativeChrome.js lines 207-216 (current StatusBar.setStyle LIGHT + setBackgroundColor #EFE7D6 to replace)
    - mazeworld.html <meta name="viewport"> (currently width=device-width, initial-scale=1 — missing viewport-fit=cover)
  </read_first>
  <action>
    Update mazeworld.html's viewport meta to include viewport-fit=cover (required for non-zero insets). Apply the fallback-chained safe-area padding EVERYWHERE UI-SPEC calls for it: top HUD padding-top: calc(12px + var(--safe-area-inset-top, env(safe-area-inset-top, 0px))); tab bar + maze bottom-controls padding-bottom: calc(8px + var(--safe-area-inset-bottom, env(safe-area-inset-bottom, 0px))). Do NOT use bare env() (Android WebView <140 bug, Pitfall 2). Ensure the top HUD band's own dark background (#1b170f) extends to y=0 so the transparent edge-to-edge status-bar area shows the app's dark ground (Pitfall 3), with the safe-area padding on the CONTENT inside it. In nativeChrome.js, recolor the status bar for the dark theme: keep StatusBar.setStyle to the value that yields LIGHT icons/text on a dark ground, and set the background color to a dark ground (#1b170f or #14110c) instead of #EFE7D6 — but treat setBackgroundColor as best-effort (may be ignored under Android 15+ edge-to-edge; the DOM dark band is the real guarantee). Do not otherwise change decideBackAction/flushOnBackground logic. capacitor.config.json: the splash backgroundColor may stay as the branded asset; if a StatusBar plugin config block is needed for style, add it minimally — do not touch appId/appName.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/persistence/back-button-logic.test.js test/persistence/lifecycle.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `www/index.html` viewport meta includes viewport-fit=cover; safe-area paddings use the var(--safe-area-inset-*, env(...)) fallback chain, never bare env().
    - nativeChrome.js status-bar background is a dark-theme color, not #EFE7D6; setStyle yields light-on-dark; decideBackAction/flushOnBackground behavior unchanged (their persistence tests stay green).
    - `npm test` stays green (372).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build tooling → shipped bundle | Fonts are committed source copied at build time; no runtime fetch (offline correctness) |
| native shell → WebView chrome | Status-bar recolor + safe-area insets are delivered by the platform/CSS, consumed by the app |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | shipping a Google Fonts CDN reference (network request in an offline/no-collection app) | high | mitigate | @font-face references only local ./fonts/*.woff2; a build-time check asserts www/index.html has zero fonts.googleapis/gstatic reference (STR-01 no-collection posture). |
| T-04-11 | Information Disclosure | edge-to-edge status bar rendering content under the system bar / wrong safe-area | medium | mitigate | var(--safe-area-inset-*, env(...)) fallback + viewport-fit=cover + top dark band to y=0 (04-RESEARCH.md Pitfalls 2-3); device-UAT confirms real insets. |
| T-04-12 | Tampering | fonts/icons committed assets altered at build time | low | accept | Assets are version-controlled source, no runtime fetch — matches the existing fonts/ posture. |
</threat_model>

<verification>
- `node tools/build-www.mjs` succeeds; www/index.html is dark-themed, CDN-free, canvas preserved, viewport-fit=cover present.
- `npm test` full suite green (372) — persistence/back-button tests confirm nativeChrome logic unchanged.
- Deferred device-UAT (Pixel 7): real safe-area insets, dark status bar appearance, boot-into-dark-theme feel (04-VALIDATION.md).
</verification>

<success_criteria>
- App boots into the dark torch-lit shell with self-hosted fonts, the 5-tab nav, and a real-state HUD; every later slice renders inside this shell.
- Safe-area + dark status bar are platform-correct per 04-RESEARCH.md; the canvas element is untouched and ready for 04-06.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-05-SUMMARY.md` when done.
</output>
