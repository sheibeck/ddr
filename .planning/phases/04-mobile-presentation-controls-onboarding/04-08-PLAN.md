---
phase: 04-mobile-presentation-controls-onboarding
plan: 08
type: execute
wave: 6
depends_on: [04-07, 04-03]
files_modified:
  - mazeworld.html
autonomous: true
requirements: [UX-04, UX-05, UX-02]
must_haves:
  truths:
    - "The HERO tab renders the character sheet (THE DOOMED) from characterSheetViewModel(state): name, LVL, class/race/sub, WIN POTENTIAL bar, the 8 stat rows, quirk box, SKILLS blocks — all bound to the real GameState"
    - "The ORACLE tab renders the scrollable message log from oracleLogViewModel(entries, diceMode): reverse-chronological tappable lines with dice-reveal gated by the diceMode setting"
    - "The full-screen combat/encounter overlay shows the foe card + scrollable combat log + action bar (1 STRIKE, 2 POTION (n), 3 RUN AWAY) driven by the engine-routed combat dispatch and formatEvents"
    - "RUN AWAY is full-width on its own row and >=48dp (isolation IS the mis-tap protection, no confirm dialog); POTION mutes when count is 0"
    - "Dice transparency ('TAP A LINE TO SEE THE DICE THAT DID IT') applies uniformly to the Oracle AND combat logs"
  artifacts: []
  key_links:
    - "Screens are thin renderers over the Wave-1 view-models (04-03) fed by engine-routed events (04-07); the sheet render must never advance rngState (view-model already guarantees this)"
  backstops:
    - statement: "Oracle/combat narration lines wrap (not truncate) at Body role 1.5 line-height, including longer Phase-5-style sentences"
      verification: backstop
    - statement: "A long rolled name / class-race-sub line (e.g. WILMSRY CLOAKER) wraps or scales within the header across all three text-size settings, not overflow the screen edge"
      verification: backstop
---

<objective>
Realize slice (c): build the engine-bound screens on top of the now-fully-routed
adapter — the HERO character sheet (UX-04), the ORACLE scrollable log with dice
transparency (UX-05), and the full-screen combat/encounter overlay (UX-05 combat
log + UX-02 destructive-action isolation). Each is a thin renderer over the
Wave-1 view-models, fed by real engine events. After this slice a player can
inspect their doomed adventurer, read the transparent dice log, and fight in the
full mobile combat screen.

Purpose: The readable/inspectable UI (UX-04/UX-05) that replaces the prototype's
assumption of rules knowledge, with the signature dice-transparency feature.
Output: HERO sheet, ORACLE log, full-screen combat overlay in mazeworld.html.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-UI-SPEC.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@design/Mazeworld Mobile.dc.html
@src/browser/viewModels.js
@src/browser/settings.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: HERO character sheet (THE DOOMED) from the view-model</name>
  <files>mazeworld.html</files>
  <read_first>
    - src/browser/viewModels.js characterSheetViewModel(state) shape (name, level, classLabel/raceLabel/subLabel, snarkLine, stats[], winPotential, quirk, skills[])
    - 04-UI-SPEC.md Screen 2 "THE DOOMED" (header + LVL badge; name Display; class/race/sub; italic snark; WIN POTENTIAL label+value+12px bar; 8 stat rows with 2px dividers; quirk box; SKILLS blocks with left accent; CUT LOSSES button at bottom)
    - 04-UI-SPEC.md 48dp Corrections (CUT LOSSES min-height:48px) + Copywriting Contract (CUT LOSSES confirmation gated by confirm-before-quit — the gate itself is wired in 04-09; here render the button and route its click through a shouldConfirmQuit check placeholder that 04-09 completes)
  </read_first>
  <action>
    Render the HERO tab from characterSheetViewModel(getState()): header THE DOOMED + LVL badge, name (Display role), class/race/sub line, italic snark line, WIN POTENTIAL label+value+12px bar, the 8 stat rows (TO STRIKE/TO HIT/DAMAGE/ARMOR/INTELLIGENCE/SKILL POINTS/NEXT LEVEL/UPKEEP) as label/value pairs with a 2px divider each, the quirk box (bordered, moss decoration), and the SKILLS section (per-skill name/description with left accent border). Re-render the sheet on tab open and after each dispatch (it reads live state). Add the CUT LOSSES · ROLL ANOTHER destructive button (min-height:48px, red treatment) at the bottom; wire its click to check shouldConfirmQuit(readSettings()) and, when confirmation is required, show the confirm copy ("Cut it loose? [name] is still alive down there." -> CUT LOSSES / KEEP GOING) before calling startNewRun — the full confirm dialog + setting wiring is finalized in 04-09, so here route through the settings-gated path (do not hardcode confirm on/off). Ensure long names/class-race lines wrap or scale within the header across S/M/L (long-text backstop). The sheet render must not advance rngState (the view-model already guarantees this — do not call any rng-drawing engine helper from the render path).
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/characterSheetViewModel.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The HERO tab shows all 8 stat rows + WIN POTENTIAL + name/LVL/class-race-sub + quirk + skills bound to live GameState via the view-model.
    - CUT LOSSES is >=48dp, red, and routes through the confirm-before-quit gate (settings-driven, not hardcoded).
    - `npm test` green (roundtrip/determinism confirm the sheet render never touched rngState).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 2: ORACLE scrollable log with dice transparency</name>
  <files>mazeworld.html</files>
  <read_first>
    - src/browser/viewModels.js oracleLogViewModel(entries, diceMode) (row shape + reveal flags)
    - 04-UI-SPEC.md Screen 5 "THE ORACLE" + Component Contracts "Tappable log line" (chevron + narration Body + revealed roll Data/gold below) + "Dice transparency (diceMode)"
    - 04-UI-SPEC.md 48dp Corrections (Oracle log tappable line wrapped in min-height:48px tap target) + "UI Considerations" overflow (scrollable) + long-text (wrap not truncate)
    - src/browser/settings.js readSettings().diceMode; mazeworld.html logLine (how log entries are currently accumulated) so the log entry store can feed the view-model
  </read_first>
  <action>
    Render the ORACLE tab as a scrollable (overflow:auto) reverse-chronological list from oracleLogViewModel(logEntries, readSettings().diceMode): header THE ORACLE + FLOOR badge, hint line "TAP A LINE TO SEE THE DICE THAT DID IT", each entry a tappable row (chevron + narration Body role) with the roll detail (Data role, gold) revealed per diceMode — 'on tap' toggles on tap, 'always' shown inline, 'never' omitted. Wrap each row in a min-height:48px tap target (padding, not visual bulk). Ensure lines wrap (not truncate) at Body role 1.5 line-height for long sentences (Phase-5 copy backstop). Accumulate log entries (narration + optional roll detail) from the html/events returned by dispatch() so both the Oracle and the combat log share one entry model and the SAME view-model/gating.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/oracleLogViewModel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - ORACLE tab renders newest-first, scrollable, with per-line dice reveal gated by diceMode; each tappable line is >=48dp.
    - The combat log (Task 3) reuses the same view-model + gating.
    - Long narration lines wrap, not truncate.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 3: Full-screen combat/encounter overlay</name>
  <files>mazeworld.html</files>
  <read_first>
    - 04-UI-SPEC.md Screen 9 (ENCOUNTER header red + ROUND n; foe card name + wp/maxWp + 8px WP bar + meta + flavor, red border; scrollable combat log same tappable/reveal pattern; action bar 1 STRIKE accent, 2 POTION (n) muted when 0, 3 RUN AWAY full-width isolated row; party rail hidden/OFF for v1)
    - 04-UI-SPEC.md Color colorblind-safe (foe WP bar + numeric n/max label always co-rendered; STRIKE/RUN labels textual)
    - engine state.combat shape (foe fields) + the engine-routed combat handlers from 04-07 (attack/flee/drinkPotion dispatch)
    - 04-UI-SPEC.md 48dp Corrections (3 RUN AWAY min-height:48px non-negotiable)
  </read_first>
  <action>
    Build the full-screen combat overlay (replaces the whole screen, not a tab) that appears while state.combat is active: header ENCOUNTER (red) + ROUND n; foe card (name + wp/maxWp numeric label + 8px WP bar with color AND the numeric label co-rendered for colorblind-safety + meta line size/intelligence/strike-die/to-hit/special + italic flavor, bordered destructive red); a scrollable combat log using the SAME oracleLogViewModel + diceMode gating as the Oracle; and the bottom action bar: 1 · STRIKE (accent primary) -> engine-routed attack dispatch; 2 · POTION (n) (outline, muted/disabled when n===0) -> drinkPotion dispatch; 3 · RUN AWAY full-width on its OWN row (destructive red, min-height:48px) -> flee dispatch. RUN AWAY is NOT gated by a confirm dialog — its isolation/sizing/color IS the mis-tap protection (UX-02). Keep the party rail hidden/disabled (solo v1 default). All actions go through the 04-07 engine-routed dispatch; re-render foe/log after each. After this screen is proven to call the routed handlers, remove any remaining dead classic combat rendering left from 04-07 Task 2's cleanup.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/combat.test.js test/unit/engineAdapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The combat overlay shows the foe card (WP bar + numeric label), a dice-transparent combat log, and the action bar; RUN AWAY is full-width, isolated, >=48dp, ungated.
    - POTION mutes at count 0; STRIKE/POTION/RUN AWAY all dispatch engine-routed actions (no classic combat path).
    - `npm test` full suite green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| render path → GameState | Screens read state to render and must never mutate it (especially rngState) off-band |
| combat action buttons → engine | STRIKE/POTION/RUN AWAY resolve through the engine-routed dispatch |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-18 | Tampering | sheet/combat render advancing rngState or mutating state | high | mitigate | Screens render from view-models that read-only the state; DAMAGE/derived values use non-rng descriptors (04-03); roundtrip/determinism suites stay green as the guard. |
| T-04-19 | Elevation of intent / mis-tap | RUN AWAY (or CUT LOSSES) triggered by adjacent mis-tap losing a run | high | mitigate | RUN AWAY isolated full-width >=48dp row (isolation is the protection per UX-02); CUT LOSSES additionally gated by confirm-before-quit (04-09). |
</threat_model>

<verification>
- `npm test` full suite green; view-model tests + roundtrip/determinism confirm read-only rendering.
- Deferred device-UAT: sheet readability, dice-transparency feel, combat-screen mis-tap safety on the Pixel 7 (04-VALIDATION.md UX-02/04/05).
</verification>

<success_criteria>
- A player can open a real character sheet, read a dice-transparent scrollable log, and fight in the full mobile combat screen — all engine-driven.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-08-SUMMARY.md` when done.
</output>
