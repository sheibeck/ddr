---
phase: 04-mobile-presentation-controls-onboarding
plan: 09
type: execute
wave: 6
depends_on: [04-08, 04-02]
files_modified:
  - mazeworld.html
  - src/browser/nativeChrome.js
  - package.json
  - tools/build-www.mjs
  - test/unit/settings.test.js
autonomous: true
requirements: [UX-07, UX-08, UX-02]
must_haves:
  truths:
    - "A settings screen lets the player adjust all six fields — sound, haptics, text size (S/M/L), control scheme (tap/D-pad), confirm-before-quit, and diceMode (on tap/always/never) — each persisted via settings.js/window.mzStorage"
    - "Changing text size updates --mw-text-scale live so all rem UI text + the maze cell size scale together (UX-08)"
    - "@capacitor/haptics is installed and vendored; Haptics.impact() fires on the intended beats only when the haptics setting is ON, wired via nativeChrome.js's injectable-plugin pattern"
    - "The Sheet's CUT LOSSES action is gated by confirm-before-quit; Clear save is ALWAYS confirmed regardless of that setting (permanent data loss)"
    - "Settings render with in-code defaults immediately, then reconcile silently once mzStorage resolves the persisted values"
  artifacts: []
  key_links:
    - "The settings screen is a thin renderer/writer over settings.js (04-02); haptics is guarded behind the same dynamic-import/injectable pattern nativeChrome.js already uses so node:test never resolves the bare specifier"
  backstops:
    - statement: "Settings render with in-code defaults immediately, then reconcile silently after mzStorage resolves (no visible flash after real values load)"
      verification: backstop
    - statement: "A storage read/write failure in Settings surfaces the generic THE LEDGER WON'T OPEN fallback, not a blank/broken screen"
      verification: backstop
---

<objective>
Realize slice (d): the settings screen and its wiring — all six UX-07 fields over
settings.js, live text-scaling (UX-08), the @capacitor/haptics install + gated
Haptics.impact() wiring, and the confirm-before-quit / always-confirm-clear-save
gates. After this slice a player can tune sound, haptics, text size, control
scheme, confirm-before-quit, and dice transparency, and destructive actions are
mis-tap-safe.

Purpose: Player control over accessibility + feel (UX-07/UX-08) and the
confirm-before-quit safety completion (UX-02).
Output: settings screen, haptics plugin + wiring, confirm gates, live text-scale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-UI-SPEC.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@src/browser/settings.js
@src/browser/nativeChrome.js
@tools/build-www.mjs
</context>

<package_legitimacy>
Package to install: @capacitor/haptics. 04-RESEARCH.md "Package Legitimacy Audit" verdict = OK / Approved (published under ionic-team, same @capacitor/* scope + publisher as the already-trusted @capacitor/app/status-bar/screen-orientation/splash-screen; ~1.2M weekly downloads; repo github.com/ionic-team/capacitor-haptics). Treated as [VERIFIED: npm registry], NOT [ASSUMED]/[SUS] — so NO blocking human-verify checkpoint is required before install (workflow.auto_advance unaffected). The supply-chain threat is still tracked as T-04-SC below.
</package_legitimacy>

<tasks>

<task type="auto">
  <name>Task 1: Install @capacitor/haptics + vendor it + gated Haptics wiring</name>
  <files>package.json, tools/build-www.mjs, src/browser/nativeChrome.js</files>
  <read_first>
    - 04-RESEARCH.md Standard Stack (Supporting) + "Don't Hand-Roll" haptics row (Haptics.impact({style}); guard behind the dynamic-import/injectable pattern) + Package Legitimacy Audit
    - tools/build-www.mjs CAPACITOR_PACKAGES array lines 57-64 (add @capacitor/haptics so it vendors + import-maps like the others)
    - src/browser/nativeChrome.js loadApp pattern 121-136 + registerNativeChrome injectable-plugin pattern (mirror it for Haptics; production reaches it via guarded dynamic import, tests inject a fake)
    - STATE.md note: after any npx cap sync run node tools/pin-jdk.mjs (sync wipes the JDK pin)
  </read_first>
  <action>
    Run npm install @capacitor/haptics (^8.0.2, peer @capacitor/core >=8.0.0 satisfied by 8.5.1). Add "@capacitor/haptics" to tools/build-www.mjs CAPACITOR_PACKAGES so it is vendored + added to the import map exactly like the other plugins (do not special-case it). In src/browser/nativeChrome.js, add a haptics wiring surface mirroring the existing injectable/dynamic-import pattern: an impact(style) helper that, ONLY when the haptics setting is ON, resolves Haptics via an injected object or a guarded dynamic import("@capacitor/haptics") and calls Haptics.impact({style}); a missing/absent plugin or setting-OFF is a silent no-op (never throws). Expose the Haptics object as an injectable param (like App/StatusBar) so node:test can assert impact() is CALLED without resolving the bare specifier. Wire impact() to fire on the intended beats (hit/trap/level-up) from the live page, gated by readSettings().haptics. Note in build-www header that Phase 5 also edits this file. Reminder: this task changes the vendored plugin set — the executor must re-run the build (node tools/build-www.mjs) and, for any device build, npx cap sync then node tools/pin-jdk.mjs.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node -e "process.exit(require('./package.json').dependencies['@capacitor/haptics']?0:1)" && node --test test/unit/settings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - package.json lists @capacitor/haptics; build-www vendors it into www/vendor/@capacitor/haptics and adds it to the import map.
    - nativeChrome.js exposes a gated, injectable Haptics impact() that no-ops when the setting is OFF or the plugin is absent, and never throws.
    - `npm test` stays green (haptics reached only via guarded dynamic import/injection — node:test never resolves the bare specifier).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 2: Settings screen + live text-scale + haptics call-assertion test</name>
  <files>mazeworld.html, test/unit/settings.test.js</files>
  <read_first>
    - src/browser/settings.js (readSettings/writeSetting/SETTINGS_DEFAULTS/textScaleForSize/effectiveTextScale)
    - 04-UI-SPEC.md Typography (S/M/L -> --mw-text-scale; maze cell size folds in) + Accessibility Contract (rem scaling; OS font-scale honoring) + 04-CONTEXT.md Settings decision (5 required + diceMode; fog-of-war NOT user-facing; cellSize folded into text-size)
    - 04-VALIDATION.md UX-07 haptics call-assertion row (injectable/mocked plugin, assert Haptics.impact CALLED)
  </read_first>
  <action>
    Build the settings screen exposing all six fields as controls: sound (toggle), haptics (toggle), text size (S/M/L), control scheme (tap/D-pad), confirm-before-quit (toggle), diceMode (on tap/always/never). Each control writes via settings.js writeSetting and reads current values from readSettings. On text-size change, update the #app root --mw-text-scale to effectiveTextScale(size, OS-scale) live so all rem tokens AND the maze cell size (cellSizeForTextScale) update immediately. Render with in-code SETTINGS_DEFAULTS first, then reconcile silently once readSettings resolves (loading backstop — no flash). Surface the THE LEDGER WON'T OPEN fallback on a storage read/write failure (error backstop). Do NOT expose fog-of-war or a raw cellSize slider (folded into text-size per CONTEXT). Extend test/unit/settings.test.js with a haptics call-assertion: inject a fake Haptics into the nativeChrome impact() path, set haptics ON, trigger a beat, assert impact() was called; set haptics OFF, assert it was NOT called.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/settings.test.js test/unit/textScale.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The settings screen adjusts all six fields, each persisted via settings.js/window.mzStorage; fog-of-war and raw cellSize are NOT exposed.
    - Changing text size updates --mw-text-scale live (rem text + maze cell size scale together).
    - settings.test.js asserts Haptics.impact() is called when haptics ON and not called when OFF (injected fake).
    - `npm test` stays green (372 + extensions).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 3: Confirm-before-quit + always-confirm Clear save</name>
  <files>mazeworld.html</files>
  <read_first>
    - src/browser/settings.js shouldConfirmQuit
    - 04-UI-SPEC.md Copywriting Contract "Destructive confirmation" (CUT LOSSES gated by confirm-before-quit with "Cut it loose? [name] is still alive down there." -> CUT LOSSES/KEEP GOING; Clear save ALWAYS confirmed with "Clear save? This erases the ledger for good." -> CLEAR SAVE/KEEP SAVE regardless of the setting; RUN AWAY NOT gated) + "Controls & Interaction" confirm-before-quit
    - the CUT LOSSES button wired in 04-08 (finalize its gate here)
  </read_first>
  <action>
    Finalize the confirm-before-quit gate: the Sheet's CUT LOSSES · ROLL ANOTHER runs through shouldConfirmQuit(readSettings()) — when ON, show the confirm dialog (Cut it loose? [character name] is still alive down there. -> CUT LOSSES destructive / KEEP GOING default-focused) before startNewRun; when OFF, execute immediately. Add a Clear save action in Settings that is ALWAYS confirmed (Clear save? This erases the ledger for good. -> CLEAR SAVE / KEEP SAVE) regardless of the confirm-before-quit setting, because it is permanent data loss (removes the save/graveyard via window.mzStorage). Leave RUN AWAY (combat) explicitly ungated (its isolation/sizing/color is its protection). Do NOT change nativeChrome.decideBackAction (Phase-2 behavior stays; the hardware back button's confirm is already handled there).
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/confirmQuit.test.js test/persistence/back-button-logic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - CUT LOSSES honors confirm-before-quit (confirm when ON, immediate when OFF); Clear save is ALWAYS confirmed; RUN AWAY is never gated.
    - The hardware back-button confirm logic (decideBackAction) is unchanged; its persistence tests stay green.
    - `npm test` full suite green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| npm install → shipped build | A new dependency enters the offline bundle |
| destructive action → run/save deletion | Cut Losses / Clear save can discard a run or erase all data |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | npm install @capacitor/haptics (supply chain) | high | mitigate | Package legitimacy verdict OK/Approved (ionic-team, same publisher/scope as installed plugins, ~1.2M/wk, official repo) — [VERIFIED], no [SLOP]/[SUS]. Vendored as committed source, reached only via guarded dynamic import; no runtime network fetch (offline/STR-01/STR-04 no-extra-SDK posture — haptics is a first-party Capacitor plugin, not an ad/analytics SDK). |
| T-04-20 | Elevation / data loss | Clear save or Cut Losses triggered without intent | high | mitigate | Clear save is ALWAYS confirmed (permanent loss); Cut Losses gated by confirm-before-quit; both use isolated destructive-red buttons. |
| T-04-21 | Tampering | settings written outside window.mzStorage | medium | mitigate | The settings screen writes only through settings.js -> window.mzStorage. |
</threat_model>

<verification>
- `npm test` full suite green (372 + settings/haptics/textScale/confirmQuit extensions).
- Deferred device-UAT: real haptic feel on the intended beats, live text-scale legibility, mis-tap safety (04-VALIDATION.md UX-07/08).
</verification>

<success_criteria>
- A player can tune all six settings with live text-scaling and gated destructive actions; haptics fires only when enabled; the build stays offline (haptics vendored, no network SDK).
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-09-SUMMARY.md` when done.
</output>
