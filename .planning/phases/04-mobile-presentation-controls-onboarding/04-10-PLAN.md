---
phase: 04-mobile-presentation-controls-onboarding
plan: 10
type: execute
wave: 8
depends_on: [04-09, 04-06, 04-03]
files_modified:
  - mazeworld.html
autonomous: true
requirements: [UX-06]
must_haves:
  truths:
    - "On the first run only, an in-context coach-mark overlay teaches the core loop (move here / that mark is a trap / descend there / you're starving) on the REAL first floor, driven by tutorial.js's step sequencer"
    - "Each coach-mark is a position:fixed dimmed overlay with a spotlight cutout over the target element (no onboarding library), dismissible, capped at 1-2 short lines (no wall of text)"
    - "The tutorial shows once and never again — gated by the persisted mazeworld.tutorialSeen flag via window.mzStorage"
  artifacts: []
  key_links:
    - "The overlay renders tutorial.js's COACH_MARK_STEPS (04-03) and points at the real maze/HUD/tab elements built in 04-05/04-06; tutorialSeen persistence uses the same window.mzStorage seam"
  backstops:
    - statement: "Coach-mark copy stays capped at 1-2 short lines per step (enforced by contract + the tutorial.js length cap, not by truncation)"
      verification: backstop
---

<objective>
Realize slice (e): the first-run, in-context tutorial (UX-06). A lightweight
coach-mark overlay teaches move/fight/descend/survive on the real first floor,
using tutorial.js's tested step sequencer, then never shows again. No wall of
text, no onboarding library — a hand-rolled position:fixed spotlight overlay.

Purpose: Replace the prototype's assumption of rules knowledge with in-context
teaching (UX-06) — the last slice of the phase.
Output: first-run coach-mark tutorial overlay in mazeworld.html.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-UI-SPEC.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-CONTEXT.md
@src/browser/tutorial.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: First-run coach-mark overlay driven by tutorial.js</name>
  <files>mazeworld.html</files>
  <read_first>
    - src/browser/tutorial.js (COACH_MARK_STEPS {id,target,copy}; makeTutorialSequencer current/next/dismiss/isComplete; tutorialSeen async read + setSeen)
    - 04-RESEARCH.md "Don't Hand-Roll" coach-mark row (position:fixed semi-opaque overlay div + CSS clip-path/box-shadow cutout sized to the target's getBoundingClientRect(); ~4 fixed steps)
    - 04-CONTEXT.md tutorial decision (coach-marks on the real first floor: move here / that mark is a trap / descend there / you're starving; dismissible; shown once; persisted mazeworld.tutorialSeen) + 04-UI-SPEC.md Accessibility (animations skippable/instant-legible) + "no wall of text" cap
    - the real target elements from 04-05/04-06: the maze viewport, an adjacent seen cell region, the RATIONS HUD cluster, the descent icon, the tab bar
  </read_first>
  <action>
    On boot, after the shell + maze are rendered, await tutorial.js's tutorialSeen read; if unseen AND a live run exists, start the coach-mark sequence via makeTutorialSequencer(COACH_MARK_STEPS). Render each step as a position:fixed dimmed overlay with a spotlight cutout (box-shadow/clip-path) sized to the current step target's getBoundingClientRect(), plus a small caption box with that step's copy (1-2 short lines) and a dismiss/next affordance. Advance on tap of next / the highlighted target; allow full dismissal at any step. On completion or dismissal, call setSeen() so the flag persists via window.mzStorage and the tutorial never shows again. Keep it non-blocking of gameplay input beyond the highlighted action, and keep any reveal/flicker animation skippable/instant-legible (accessibility). Use the deadpan-but-family-friendly register (tone reference only) for the step copy — but the authoritative copy + length cap already live in tutorial.js COACH_MARK_STEPS; this task renders them, it does not lengthen them.
  </action>
  <verify>
    <automated>node tools/build-www.mjs && node --test test/unit/tutorial.test.js</automated>
  </verify>
  <acceptance_criteria>
    - On a first run (tutorialSeen false), the coach-mark overlay appears on the real first floor, steps through the loop, and is dismissible; on later runs (tutorialSeen true) it does not appear.
    - Each step is a position:fixed spotlight overlay pointing at a real element; copy is 1-2 short lines (from tutorial.js, within the cap); no onboarding library is added.
    - Completion/dismissal persists mazeworld.tutorialSeen via window.mzStorage; `npm test` stays green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| tutorialSeen flag → store | The once-only flag must persist through the single mzStorage seam |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Tampering | tutorialSeen written outside window.mzStorage / re-showing every boot | low | mitigate | setSeen persists only through window.mzStorage; tutorial.js's sequencer + seen-flag are unit-tested (04-03). |
| T-04-23 | Denial of Service | coach-mark overlay trapping input / blocking the game | medium | mitigate | Overlay is dismissible at every step and only highlights the current action; completion/dismissal always clears it. |
</threat_model>

<verification>
- `npm test` full suite green (tutorial sequencer + tutorialSeen unit-tested).
- Deferred device-UAT: does the tutorial actually teach the loop without feeling wordy (a feel judgment) on the Pixel 7 (04-VALIDATION.md UX-06).
</verification>

<success_criteria>
- A first-run player is taught move/fight/descend/survive in-context, once, with no wall of text — completing UX-06 and the phase's approachability goal.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-10-SUMMARY.md` when done.
</output>
