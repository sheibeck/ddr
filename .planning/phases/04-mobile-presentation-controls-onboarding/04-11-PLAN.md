---
phase: 04-mobile-presentation-controls-onboarding
plan: 11
type: execute
wave: 2
depends_on: [04-04]
files_modified:
  - src/browser/storage.js
  - src/browser/engineAdapter.js
  - mazeworld.html
  - test/persistence/dual-write-convergence.test.js
  - test/persistence/flush-drain.test.js
  - test/persistence/storage-migration.test.js
  - test/persistence/storage.test.js
  - test/persistence/harness/sandboxClassicPersistence.js
  - test/unit/engineAdapter.test.js
autonomous: true
requirements: []
must_haves:
  truths:
    - "All three persistence keys are renamed mazeworld.*.v1 -> ddr.*.v1 (ddr.delve.v1, ddr.best.v1, ddr.graveyard.v1), consistently across storage.js, engineAdapter.js, and the two mazeworld.html classic-script key literals, with no migration shim (greenfield/pre-launch, user-confirmed)"
    - "The dual-write-convergence test still passes — the classic script and the adapter use MATCHING renamed keys"
    - "The localStorage->Preferences migration (migrateLegacyKeys) uses the NEW ddr.*.v1 keys as its in-scope set"
    - "Residual PRODUCT-name 'Mazeworld' is removed from user-facing modular src/ strings; 'Mazeworld' is KEPT wherever it denotes the in-fiction world/setting"
    - "The frozen parity golden master (test/parity/prototype-master.js.txt) is left untouched (its key literals are the prototype's own constants; parity asserts on RNG-driven game state, not key strings)"
  artifacts: []
  key_links:
    - "This runs in Wave 2 (before the shell wave) so every later UI/storage wave (04-05..04-10) reads/writes the renamed ddr.*.v1 keys from the start — no key-drift between the rename and the UI rebuild"
---

<objective>
Realize the folded-in rename deep-work the user moved INTO Phase 4 (04-CONTEXT.md
"Folded-in rename scope", 2026-09-08): rename the three persistence keys
mazeworld.*.v1 -> ddr.*.v1 across the modular sources + the classic-script key
literals, update the ~8 persistence/adapter tests that assert those keys, and
strip residual PRODUCT-name "Mazeworld" from user-facing modular strings (keeping
the in-fiction WORLD name). Greenfield/pre-launch, so NO migration shim (user
confirmed no save-orphaning concern). This lands in Wave 2 — before the shell —
so all later UI/storage waves use the renamed keys from the first edit.

Purpose: Consistent, product-correct persistence keys + naming under the LOCKED
"Delve, Die, Repeat" product name, done once on the real sources this phase's UI
rebuilds onto.
Output: renamed keys everywhere (except the frozen parity master), green tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-CONTEXT.md
@src/browser/storage.js
@src/browser/engineAdapter.js
</context>

<rename_note>
Product name is LOCKED to "Delve, Die, Repeat" (04-CONTEXT.md, native manifest already renamed in f81942f). "Mazeworld" stays ONLY as the in-fiction world/setting name (Maze Master, Wilmsry, wilmst currency, lore). The content/*.js "mazeworld" occurrences are header comments referencing the source FILENAME mazeworld.html (a real file path) — those are NOT product-name strings and must NOT be changed. test:quick was already fixed in c5dcaf7 (Node 22 glob form) — do NOT re-touch it.
</rename_note>

<tasks>

<task type="auto">
  <name>Task 1: Rename the three persistence keys mazeworld.*.v1 -> ddr.*.v1 across sources + tests</name>
  <files>src/browser/storage.js, src/browser/engineAdapter.js, mazeworld.html, test/persistence/dual-write-convergence.test.js, test/persistence/flush-drain.test.js, test/persistence/storage-migration.test.js, test/persistence/storage.test.js, test/persistence/harness/sandboxClassicPersistence.js, test/unit/engineAdapter.test.js</files>
  <read_first>
    - src/browser/storage.js lines 55-60 (RUN_SAVE_KEY "mazeworld.delve.v1", BEST_KEY "mazeworld.best.v1", GRAVE_KEY "mazeworld.graveyard.v1", LEGACY_KEYS array) + migrateLegacyKeys() lines 271-315 (the migration must operate on the NEW keys)
    - src/browser/engineAdapter.js SAVE_KEY line ~36, BEST_KEY ~44, GRAVE_KEY ~54, and the mirror-comment references ~31/46 that document the matching mazeworld.html literals
    - mazeworld.html SAVE_KEY line ~537 (classic script) + GRAVE_KEY line ~3007 (classic graveyard)
    - The asserting tests: test/persistence/{dual-write-convergence,flush-drain,storage-migration,storage}.test.js, test/persistence/harness/sandboxClassicPersistence.js, test/unit/engineAdapter.test.js (grep each for the mazeworld.*.v1 literals before editing)
    - test/parity/prototype-master.js.txt lines ~43/~2494 — the FROZEN golden master; its SAVE_KEY/GRAVE_KEY are the prototype's own constants and the parity harness stubs storage, so parity does NOT assert on these key strings. LEAVE IT UNTOUCHED (changing the frozen master would risk the golden-master diff). Document this in the SUMMARY.
  </read_first>
  <action>
    Rename all three keys everywhere they are DEFINED or ASSERTED, keeping every in-sync copy identical: mazeworld.delve.v1 -> ddr.delve.v1, mazeworld.best.v1 -> ddr.best.v1, mazeworld.graveyard.v1 -> ddr.graveyard.v1. In storage.js: update RUN_SAVE_KEY/BEST_KEY/GRAVE_KEY constants; LEGACY_KEYS (the migrateLegacyKeys in-scope set) must reference the NEW ddr.*.v1 keys so the localStorage->Preferences migration operates on the renamed keys (there is NO cross-name shim from mazeworld.* to ddr.* — greenfield, user-confirmed no save-orphaning). In engineAdapter.js: update SAVE_KEY/BEST_KEY/GRAVE_KEY and the mirror comments that describe the matching mazeworld.html literals. In mazeworld.html: update the classic-script SAVE_KEY (~537) and GRAVE_KEY (~3007) literals so the classic path and the adapter stay key-consistent (the dual-write-convergence test asserts this). Update the ~6 test files that assert the literal keys to the new values. Do NOT touch test/parity/prototype-master.js.txt (frozen master). Do NOT add any migration/aliasing from the old names.
  </action>
  <verify>
    <automated>! grep -rEl "mazeworld\.(delve|best|graveyard)\.v1" src/ mazeworld.html test/ --include=*.js --include=*.html | grep -v "prototype-master.js.txt" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "mazeworld\.(delve|best|graveyard)\.v1"` over src/ + mazeworld.html + test/ returns ZERO matches EXCEPT possibly test/parity/prototype-master.js.txt (frozen master, documented).
    - storage.js LEGACY_KEYS / migrateLegacyKeys operate on the ddr.*.v1 keys; no mazeworld.*->ddr.* shim exists.
    - `npm test` = 372 green; `npm run test:quick` green; the dual-write-convergence test proves the classic + adapter keys match.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 2: Strip residual PRODUCT-name "Mazeworld" from user-facing modular sources</name>
  <files>src/browser/engineAdapter.js, mazeworld.html</files>
  <read_first>
    - 04-CONTEXT.md "Folded-in rename scope" (remove product-name "Mazeworld" from user-facing modular sources where it means the PRODUCT; keep it where it means the in-fiction WORLD)
    - grep result: content/*.js "mazeworld" hits are header comments referencing the FILENAME mazeworld.html — those are source-path references, NOT product strings; do NOT change them
    - Any user-facing product-name string in src/browser/* or a modular-owned mazeworld.html UI string (NOT the frozen prototype tables; NOT lore/world usages)
  </read_first>
  <action>
    Grep src/ (and the modular-owned UI strings in mazeworld.html) for user-facing occurrences of the PRODUCT name "Mazeworld" and replace them with the centralized name concept (route through GAME_NAME where a display string, or simply remove/reword a stale comment). KEEP every in-fiction WORLD usage ("Mazeworld" as the setting: Maze Master, Wilmsry, wilmst, lore) unchanged. Do NOT rewrite the prototype's inline content tables (the prototype is being superseded by the modular UI this phase builds) and do NOT change the content/*.js header comments that reference the mazeworld.html filename (those are accurate file-path references). This is a light cleanup — scope it to genuine product-name display/comment strings in the modular layer, not a blanket search-replace.
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js && npm run test:quick</automated>
  </verify>
  <acceptance_criteria>
    - No user-facing PRODUCT-name "Mazeworld" string remains in the modular src/ UI/display layer; in-fiction WORLD usages and the content/*.js filename-reference comments are preserved.
    - `npm test` stays green (372); `npm run test:quick` green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| persisted keys → classic script + adapter | The run-save/best/graveyard keys must stay identical across both writers or a run silently splits/loses data |
| frozen golden master → parity comparison | The prototype master must not be edited or parity determinism regresses |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-24 | Tampering | key drift between the classic script and the adapter after rename | high | mitigate | Rename every in-sync copy identically; the dual-write-convergence test asserts the classic + adapter keys match and stays green. |
| T-04-25 | Denial of Service / data loss | renaming keys orphaning an existing save | medium | accept | Greenfield/pre-launch — user explicitly confirmed no save-orphaning concern, so NO migration shim is added (a shim would be dead complexity). Accepted risk, documented. |
| T-04-26 | Tampering | editing the frozen parity master and breaking golden-master determinism | high | mitigate | test/parity/prototype-master.js.txt is left untouched; the grep acceptance explicitly excludes it; parity asserts on RNG-driven state, not key strings. |
</threat_model>

<verification>
- `npm test` = 372 green; `npm run test:quick` green; dual-write-convergence + storage-migration + adapter tests all pass with the renamed keys.
- `grep -rE "mazeworld\.(delve|best|graveyard)\.v1"` returns nothing outside the frozen master.
</verification>

<success_criteria>
- The three persistence keys are consistently ddr.*.v1 across all live sources + tests; residual product-name "Mazeworld" is gone from the modular UI layer (world name preserved); the frozen parity master is untouched and the full suite is green — all later Phase-4 waves build on the renamed keys.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-11-SUMMARY.md` when done.
</output>
