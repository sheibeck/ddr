---
phase: 04-mobile-presentation-controls-onboarding
plan: 07
type: execute
wave: 4
depends_on: [04-06, 04-04]
files_modified:
  - mazeworld.html
  - test/unit/engineAdapter.test.js
autonomous: true
requirements: [UX-05]
must_haves:
  truths:
    - "The live page's combat inputs (strike, flee, parley, sing, drink potion, read scroll, cast spell) resolve through dispatch()->applyAction, not the classic S-state combat code"
    - "The store/economy inputs (buy item, leave store) resolve through dispatch()->applyAction"
    - "Make Camp and New-Delve/Wipe resolve through the engine (camp via dispatch({type:'camp'}); new-run via the adapter's startNewRun)"
    - "Every routed action's events flow through the completed formatEvents so the Oracle/combat log shows real narration for combat/economy/camp"
    - "The classic S-state duplicate code paths are retired only AFTER the engine-routed call sites are proven (no half-migrated risky commit)"
  artifacts:
    - test/unit/engineAdapter.test.js
  key_links:
    - "This closes the Phase-1/3 deferred item: after this plan the mobile UI is fully engine-driven (applyAction + events), unblocking the engine-driven combat/log screens in 04-08"
---

<objective>
Realize slice (f): finish routing ALL remaining game domains through the engine
adapter in the live page — combat, economy/store, camp, and new-run — replacing
the classic S-state prototype code paths that 01-07 deliberately left un-migrated
(documented in STATE.md's deferred-verification table). Combined with 04-04's
completed formatEvents, this makes the mobile UI fully driven by
`applyAction(state, action) -> {state, events}`, deterministic, and log-complete.

Purpose: Close the "route ALL domains through the engine" deferred item so the
Wave-5 combat/log/sheet screens (04-08) bind to real engine events (UX-05), not
prototype duplicates.
Output: engine-routed combat/store/camp/new-run call sites; extended adapter test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@src/browser/engineAdapter.js
@engine/engine.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route combat + magic inputs through dispatch()</name>
  <files>mazeworld.html</files>
  <read_first>
    - 04-RESEARCH.md §Pattern 3 (window.move at 3415 is the reference; replicate for playerStrike/flee/parley/sing/drinkPotion/readScroll/castSpell; keep classic bodies until the new call sites are proven, then remove as a cleanup — mirrors 01-07)
    - engine/engine.js dispatch switch lines 50-89 (action types: attack, flee, parley, sing, castSpell{idx}, drinkPotion, readScroll already implemented)
    - mazeworld.html startCombat 2306, playerStrike 2380 (classic combat code paths); the combat DOM buttons/handlers
    - src/browser/engineAdapter.js dispatch() (returns {state,events,html}); the trailing module 3362-3497 (existing window.move/newGame override pattern)
  </read_first>
  <action>
    In mazeworld.html's trailing module, add engine-routed handlers for each combat/magic input mirroring window.move: attack -> dispatch({type:'attack'}); flee -> {type:'flee'}; parley -> {type:'parley'}; sing -> {type:'sing'}; drink potion -> {type:'drinkPotion'}; read scroll -> {type:'readScroll'}; cast spell -> {type:'castSpell', idx}. Each sets window.__mzState, re-renders (paint/draw), and pushes the returned html lines to the log. Redirect the existing combat DOM handlers to these routed functions (overwrite the global functions the buttons already call, exactly as window.move overrides the movement handlers — do NOT rewrite the button listeners). Per 04-RESEARCH.md Pattern 3, do NOT delete the classic playerStrike/startCombat bodies in this same commit — leave them until 04-08's mobile combat screen is proven to call the routed versions, then remove the dead classic combat code as a follow-up cleanup within this plan's final task. Do not construct raw state mutations — everything through dispatch().
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js test/unit/combat.test.js test/unit/magic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The live combat/magic inputs call dispatch()->applyAction (attack/flee/parley/sing/drinkPotion/readScroll/castSpell), not the classic S-state functions.
    - Each routed handler re-renders and logs the returned html; `npm test` stays green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto">
  <name>Task 2: Route economy/store + camp + new-run; extend adapter coverage test</name>
  <files>mazeworld.html, test/unit/engineAdapter.test.js</files>
  <read_first>
    - engine/engine.js (buyItem{idx}, leaveStore, camp action types)
    - mazeworld.html openStore 2054, buyFrom 2100 (classic store), makeCamp 1830, btn-camp onclick 3304, newGame 3279 + the existing window.newGame->startNewRun override 3435
    - src/browser/engineAdapter.js startNewRun() (the one-tap new-run entry) + dispatch()
    - test/unit/engineAdapter.test.js (existing patterns to extend — dispatch each action type, assert engine state changed and classic globals not touched)
    - 04-VALIDATION.md "Engine routing" row (integration coverage)
  </read_first>
  <action>
    Route the store inputs (buy -> dispatch({type:'buyItem',idx}); leave -> {type:'leaveStore'}), Make Camp (the btn-camp onclick -> dispatch({type:'camp'}) instead of the classic makeCamp()), and confirm New-Delve/Wipe already routes through startNewRun (extend to the Wipe/New-Delve buttons if any still call classic newGame). Then remove the now-dead classic combat/store/camp function bodies that the routed call sites replaced (playerStrike/startCombat/openStore/buyFrom/makeCamp classic paths), keeping only what the engine-routed handlers and rendering need — a clean-up commit AFTER the routed paths work, never a half-migration. Extend test/unit/engineAdapter.test.js with an integration block that dispatches each newly-routed action type through the adapter (attack, flee, parley, sing, castSpell, drinkPotion, readScroll, buyItem, leaveStore, camp) and asserts engine state advanced via applyAction and formatEvents produced narration (no null). Keep the 372 existing tests green.
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js test/unit/economy.test.js test/unit/movement.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Store buy/leave, Make Camp, and New-Delve all resolve through the engine (dispatch/startNewRun); the classic duplicate combat/store/camp code is removed after the routed paths are proven.
    - The extended engineAdapter test dispatches all newly-routed action types and asserts state advanced + non-null narration.
    - `npm test` full suite green (372 + extensions).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| live-page input → engine action | Combat/store/camp/new-run inputs must resolve through the single applyAction chokepoint |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Tampering | a routed input constructing a raw S-state/GameState mutation bypassing applyAction | high | mitigate | Every routed handler calls dispatch()->applyAction (validated at engine/actions.js validateAction); the adapter test asserts state advanced via applyAction and classic globals aren't the mutation path. |
| T-04-17 | Denial of Service | half-migrated combat leaving a dead-end input (button that neither classic nor engine handles) | medium | mitigate | Classic bodies are retained until routed call sites are proven, then removed (04-RESEARCH.md Pattern 3); combat/economy/movement regression suites stay green throughout. |
</threat_model>

<verification>
- `npm test` full suite green — combat/magic/economy/movement regression + formatEvents coverage all pass.
- The adapter integration test proves every domain now dispatches through applyAction.
- Deferred device-UAT: full engine-driven playthrough on the Pixel 7 (04-VALIDATION.md).
</verification>

<success_criteria>
- The Phase-1/3 deferred "route ALL domains through the engine + complete event narration" item is closed: the live page is fully engine-driven and log-complete.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-07-SUMMARY.md` when done.
</output>
