---
phase: 46-honest-names-dead-exports-the-tutorial-decision
plan: 04
type: execute
wave: 4
depends_on: ["46-03"]
files_modified:
  - tools/ident-sweep.mjs
  - .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md
autonomous: true
requirements: [NAME-02, NAME-01, DEAD-04, DEAD-05]

must_haves:
  truths:
    - "NAME-02 empty: node tools/ident-sweep.mjs \"toast\" \"dpad|d-pad\" \"flightLeft|flightCooldown\" \"c\\.ether\" \"wornSlots\" over src/, engine/, content/, tools/ and mazeworld.html (comments stripped) reports exactly five hits, all inside engine/saveState.js#foldLegacyCounters (the tolerant-load reads of the retired flightLeft/flightCooldown save keys), and zero for every other regex — the recorded grep list in the SUMMARY lists each survivor with its line and reason"
    - "NAME-02 adjacency: grep -rn wornSlots engine/ src/ mazeworld.html tools/ test/ (case-sensitive) prints nothing while grep -rn __mzWornSlots mazeworld.html src/ prints at least one line — the kept slot-taxonomy bridge and the retired option name are distinguished by case, not by proximity"
    - "NAME-02 ordering: git log --oneline 39c5a0f..HEAD lists the renames one per commit in dependency order — module rename, shell-function rename, won (shell), won (engine), controlScheme, tutorial.js, ident-sweep — each body carrying its grep"
    - "The tool proves itself: node tools/ident-sweep.mjs --self-test prints self-test: PASS (a token inside a // comment, inside a /* */ comment and inside an HTML comment is NOT reported; the same token on a code line and inside a string literal IS; a line comment containing the @capacitor/* glob does not swallow the code after it)"
    - "ROADMAP Phase 46 success criteria 1-5 are verified verbatim at the final commit and quoted with their outputs in the SUMMARY: criterion 1 (module + exports + old-token grep 0 + guards), 2 (won grep 0 outside the two fight-outcome survivors, tolerant stale save, abandon guard, one documented retired-field strip), 3 (the ident-sweep record), 4 (tutorial deleted + notes), 5 (npm test fail 0 with the test-delta ledger, engine diff = the six won files, zero fixture moves, master hash, build:www + boot:check green)"
  artifacts:
    - path: "tools/ident-sweep.mjs"
      provides: "comment-stripped identifier grep over the shipped surface (src/, engine/, content/, tools/, mazeworld.html) with --self-test; the NAME-02 gate, reusable by Phase 48"
      min_lines: 60
    - path: ".planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md"
      provides: "the phase closing record: criteria 1-5 verbatim, the NAME-02 recorded grep + survivors, the consolidated test ledger, the commit list, the deferred human-verification batch"
  key_links:
    - from: "tools/ident-sweep.mjs"
      to: "engine/saveState.js#foldLegacyCounters"
      via: "the only surviving hits — reported, listed, explained"
      pattern: "flightLeft|flightCooldown"
  prohibitions:
    - statement: "The recorded NAME-02 grep is not made to pass by rewording, indirecting or fragment-building any identifier in the shipped surface — the tolerant-load reads in foldLegacyCounters stay as they are and are LISTED as survivors, never hidden"
      status: unresolved
    - statement: "No Phase 48 work: comment prose, docs/*.md, .claude/CLAUDE.md, test() / describe() strings and docs/class-pass readouts are not swept here; the tool strips comments precisely so those are out of its report"
      status: unresolved
    - statement: "engine/, content/, fixtures and the master are untouched by this plan (the tool lives in tools/); the closing gates are read-only checks"
      status: unresolved
---

<!-- planner-discipline-allow: toast -->
<!-- planner-discipline-allow: dpad -->
<!-- planner-discipline-allow: d-pad -->
<!-- planner-discipline-allow: flightLeft -->
<!-- planner-discipline-allow: flightCooldown -->
<!-- planner-discipline-allow: c.ether -->
<!-- planner-discipline-allow: wornSlots -->
<!-- planner-discipline-allow: winGame -->
<!-- planner-discipline-allow: toasts.js -->
<!-- planner-discipline-allow: TOAST_FOR -->
<!-- planner-discipline-allow: toastsForAction -->
<!-- planner-discipline-allow: dispatchWithToasts -->
<!-- planner-discipline-allow: toastLifetime -->
<!-- planner-discipline-allow: MAX_TOASTS -->
<!-- planner-discipline-allow: controlScheme -->
<!-- planner-discipline-allow: tutorial -->

<objective>
Close the phase: build the NAME-02 gate — `tools/ident-sweep.mjs`, a comment-stripped identifier grep over the shipped surface (`src/`, `engine/`, `content/`, `tools/`, `mazeworld.html`) in the `tools/shell-sweep.mjs refs` mould — record its run over the criterion-3 regex list (`toast`, `dpad|d-pad`, `flightLeft|flightCooldown`, `c\.ether`, `wornSlots`) with the only allowed survivors enumerated (the five tolerant-load reads inside `engine/saveState.js#foldLegacyCounters`), then verify ROADMAP Phase 46 success criteria 1-5 verbatim and write the phase closing SUMMARY with the consolidated test ledger, the commit list, and the batched Pixel 7 checks (NAME-02 close; criteria 1-5; CONTEXT "Specific Ideas" closing gates).

Purpose: NAME-02's acceptance is a RECORDED grep with zero identifier hits and every survivor explained. A plain `grep` over the shipped surface returns hundreds of "toast" comment mentions (Phase 48's DOCS-01), so the record needs a stripper that leaves string literals and identifiers in and takes comments out — and proves it does with a self-test.

Output: one commit (the tool); `46-04-SUMMARY.md` (the phase closing record).

Probe rows carried by this plan: NAME-02 empty, NAME-02 adjacency, NAME-02 ordering (all authored as plain truths in `must_haves.truths`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-CONTEXT.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-01-SUMMARY.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-02-SUMMARY.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-03-SUMMARY.md
@tools/shell-sweep.mjs

**Phase facts (verified 2026-09-19):**
- Expected NAME-02 survivors after 46-01..46-03: `engine/saveState.js` L517 (`if (typeof c.flightLeft === "number" && c.flightLeft > 0) {`), L519 (`squares: c.flightLeft`), L520 (`else if (typeof c.flightCooldown === "number" && c.flightCooldown > 0) {`), L521 (`squares: c.flightCooldown`), L524 (`for (const k of ["haste", "invis", "ether", "acute", "flightLeft", "flightCooldown"]) delete c[k];`) — all inside `foldLegacyCounters`, the tolerant-load fold of pre-Phase-39 save keys (they sit above the `won` lines 46-02 deleted, so their numbers do not shift). `c\.ether` matches none of them (the fold reads `c[meta.kind]` and the string `"ether"`). Everything else on the list is 0 after 46-01 (toast identifiers), 46-03 (`dpad`), Phase 45 (`wornSlots`).
- The stripper pitfall (documented in `test/unit/shell-narration-wiring.test.js` and `test/unit/narrationLinesCoverage.test.js`): `mazeworld.html` has `//` line comments that contain `@capacitor/*` — a block-comment pass run BEFORE the line-comment pass treats that `/*` as an opener and swallows real code. Line comments must be stripped first. `tools/shell-sweep.mjs` carries a character-level tokenizer (`stripCommentsAndStrings`) that also blanks STRING literals — not reusable as-is, because NAME-02 must still see a retired name inside a string (`"dpad"` was one).
- Windows executor: write the tool with the Write tool; `node:fs`/`node:path` only; no packages.
- Baseline entering this plan: `npm test` 3,231 / fail 0; six commits since 39c5a0f.
</context>

<tasks>

<task type="auto">
  <name>Task 1: tools/ident-sweep.mjs — comment-stripped identifier grep over the shipped surface, with --self-test (one commit)</name>
  <files>tools/ident-sweep.mjs</files>
  <read_first>
    - tools/shell-sweep.mjs L1-60 (the CLI shape and the `refs NAME: <n>` output contract; its tokenizer as a reference for line-count-preserving stripping — do NOT blank strings)
    - test/unit/narrationLinesCoverage.test.js `stripComments` helper (line-comments-first, block-comment interiors blanked to preserve line breaks)
    - tools/shell-boot-check.mjs L1-30 and L240-260 (the `--self-test` precedent)
  </read_first>
  <action>
    Write `tools/ident-sweep.mjs` (node:fs + node:path only; `#!/usr/bin/env node` first line; header comment stating: Phase 46 (NAME-02) gate — comment-stripped identifier grep over the shipped surface; usage; exit codes). Behaviour:

    - Usage `node tools/ident-sweep.mjs [-i] <regex> [<regex> ...]` and `node tools/ident-sweep.mjs --self-test`. Regexes are JavaScript regex sources, case-sensitive unless `-i`.
    - Surface: every `.js`, `.mjs`, `.cjs` file under `src/`, `engine/`, `content/`, `tools/` (recursive; skip `node_modules`, skip non-JS such as `.txt`, `.json`, `.ps1`) plus `mazeworld.html`. Paths are printed relative to the repo root with forward slashes.
    - Stripping (line count preserved so reported numbers match the original file): for JS — remove `//` line comments FIRST (from the `//` to end of line, but not inside a string literal on that line — a simple state machine over quotes `'`, `"`, and backticks suffices; template `${}` bodies may be treated as string text), THEN blank `/* ... */` interiors keeping their newlines. For `mazeworld.html` — first blank `<!-- ... -->` interiors (keeping newlines), then apply the JS stripper to the whole file (it covers both the CSS `/* */` comments and the script `//` comments; because the line-comment pass runs first, the `@capacitor/*` pitfall cannot fire). Strings are KEPT.
    - Report: for each regex, every stripped line that matches prints `file:line: <original line trimmed>`; then `ident-sweep <regex>: <n>`; exit 1 if any n > 0, else 0.
    - `--self-test`: runs the stripper over an in-memory JS sample containing (a) a line comment with the token `needle`, (b) a block comment spanning two lines with the token, (c) a line comment containing the text `@capacitor/*` followed by a later code line with the token, (d) a code line `const needle = 1;`, (e) a string literal `"needle"` on a code line, (f) a code line whose string literal contains `//` followed by a trailing `// needle` comment — and over an HTML sample with `<!-- needle -->`, a CSS `/* needle */` and a script `const needle = 2;`; asserts that exactly (c-code), (d), (e), (f-code, matched only if the token appears outside the comment — it does not, so NOT reported) and the HTML script line are reported and nothing else; prints `self-test: PASS` and exits 0, or the mismatch and exits 1.

    Then run the record (paste every command and its full output into the SUMMARY): `node tools/ident-sweep.mjs --self-test`; `node tools/ident-sweep.mjs "toast" "dpad|d-pad" "flightLeft|flightCooldown" "c\.ether" "wornSlots"` (expect five `engine/saveState.js` hits for the flight regex, 0 for the rest, exit 1 — the survivors); `node tools/ident-sweep.mjs -i "toast"` (0 — no `Toast`/`TOAST` identifier survives either); the positive control `grep -c "toast" src/browser/narrationLines.js` (a large number — comment prose the tool correctly ignores); `node tools/shell-sweep.mjs refs dispatchWithToasts toastsForAction TOAST_FOR winGame` (all 0). Gates: `npm test` fail 0 (3,231 — no test change); `npm run build:www` (tools/ is not shipped, but the gate runs at every commit). Commit as `feat(46-04): tools/ident-sweep.mjs — comment-stripped identifier grep over the shipped surface, with --self-test (the NAME-02 gate)` with the five-survivor record in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/ident-sweep.mjs --self-test | grep -q "self-test: PASS" && node tools/ident-sweep.mjs "toast" "dpad|d-pad" "flightLeft|flightCooldown" "c\.ether" "wornSlots" > "${TEMP:-/tmp}/46-04-sweep.txt"; grep -E "^ident-sweep" "${TEMP:-/tmp}/46-04-sweep.txt"; grep -cE "^engine/saveState\.js:[0-9]+: " "${TEMP:-/tmp}/46-04-sweep.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/ident-sweep.mjs --self-test` prints `self-test: PASS` and exits 0.
    - `node tools/ident-sweep.mjs "toast" "dpad|d-pad" "flightLeft|flightCooldown" "c\.ether" "wornSlots"` exits 1 and its output contains exactly the lines `ident-sweep toast: 0`, `ident-sweep dpad|d-pad: 0`, `ident-sweep flightLeft|flightCooldown: 5`, `ident-sweep c\.ether: 0`, `ident-sweep wornSlots: 0`; every `file:line:` hit line starts with `engine/saveState.js:` and `grep -cE "^engine/saveState\.js:" <output>` prints 5; `grep -vE "^(engine/saveState\.js:|ident-sweep )" <output>` prints nothing.
    - `node tools/ident-sweep.mjs -i "toast"` prints `ident-sweep toast: 0` and exits 0; `grep -c "toast" src/browser/narrationLines.js` prints a number greater than 50 (the positive control for comment stripping).
    - `node tools/ident-sweep.mjs "narrationLines\.js"` exits 1 with at least 4 hits (a sanity positive over live import lines in src/ and the shell).
    - `head -1 tools/ident-sweep.mjs` prints `#!/usr/bin/env node`; `grep -cE "^import .* from \"node:(fs|path)\";" tools/ident-sweep.mjs` prints 2 and `grep -cE "^import " tools/ident-sweep.mjs` prints 2 (no packages).
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3231` and `# fail 0`; `npm run build:www` exits 0.
    - `git status --porcelain` prints nothing after the commit; `git log --oneline 39c5a0f..HEAD | grep -vcE "^[0-9a-f]+ docs\("` prints 7 (SUMMARY docs commits excluded by the filter).
  </acceptance_criteria>
  <done>The NAME-02 gate exists, proves itself, and its recorded run over the criterion-3 list reports exactly the five foldLegacyCounters survivors and nothing else; one green commit.</done>
</task>

<task type="auto">
  <name>Task 2: closing gates — ROADMAP Phase 46 success criteria 1-5 verbatim, the consolidated ledgers, and 46-04-SUMMARY.md as the phase closing record</name>
  <files>.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md, the `### Phase 46` section (the five criteria — quote each verbatim)
    - .planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md (the closing-record shape: "Success criteria" verified in ROADMAP order with command + output, "Gate outputs" per commit, "Tests re-pinned or deleted", "Human verification (deferred to end of run)")
    - the three prior SUMMARYs of this phase (their grep outputs, survivors, test deltas — consolidate, do not re-derive)
  </read_first>
  <action>
    Run and paste each of these verbatim under a `## Success criteria` heading, in ROADMAP order, with the criterion text quoted and the command + output beneath it:

    1. `test -f src/browser/toasts.js` (exit 1); `grep -nE "^export (const|function) (LINE_FOR|linesForAction)\b" src/browser/narrationLines.js` (2 lines); `grep -cE "^export .*(MAX_TOASTS|TOAST_[A-Z_]*_MS|toastLifetime)" src/browser/narrationLines.js` (0); `grep -cE "function dispatchWithNarration\(action\)" mazeworld.html` (1); `grep -rnE "toasts\.js|TOAST_FOR|toastsForAction|dispatchWithToasts|toastLifetime|MAX_TOASTS" src/ mazeworld.html test/ tools/` (empty); `node --test test/unit/narrationLinesCoverage.test.js test/unit/formatEventsCoverage.test.js` (fail 0); `git log --oneline 39c5a0f..HEAD` (seven commits — one rename per commit, in dependency order; this is also the NAME-02 ordering row).
    2. `grep -rnE "winGame|state\.won|\.won\b" engine/ src/ mazeworld.html test/ tools/ test/parity/harness/ | grep -vE "prototype-master\.js\.txt"` (exactly the two `COMBAT_COPY.over.won` fight-outcome lines — quote them with the reason); `grep -cE "won" engine/saveState.js` (0); `node --test test/unit/save-validation.test.js` (fail 0 — the stale-save pin); `grep -cE "if \(!next\.dead\) die\(next, \"abandon\"" engine/engine.js` (1); `grep -cE "version, won," test/parity/harness/comparables.js` (3 — the one documented retired-field strip; restate the 46-02 premise correction in two sentences).
    3. The `node tools/ident-sweep.mjs "toast" "dpad|d-pad" "flightLeft|flightCooldown" "c\.ether" "wornSlots"` output from Task 1, followed by a `## NAME-02 allowed survivors` table with the five `engine/saveState.js` lines (line number, text, reason: tolerant-load read of a pre-Phase-39 save key inside `foldLegacyCounters`, deleted after folding) and the NAME-02 adjacency row: `grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/` (empty) beside `grep -rn "__mzWornSlots" mazeworld.html src/ | wc -l` (at least 1).
    4. `test -f src/browser/tutorial.js` (exit 1); `test -f test/unit/tutorial.test.js` (exit 1); `grep -rn "tutorial" src/ engine/ content/ tools/ mazeworld.html test/` (empty); `grep -c "Phase 46 deleted the 04-era sequencer" .planning/PROJECT.md .planning/REQUIREMENTS.md` (PROJECT at least 1, REQUIREMENTS 1); `node --test test/unit/icons.test.js` (`# tests 15`, fail 0).
    5. `npm test` (`# pass 3231`, `# fail 0`) with a `## Test ledger` table consolidating every delta since 3,242: −2 (46-01 lifetime pins), −1 +1 (46-02 dormant winGame test / stale-save pin), +1 (46-03 settings tolerant-load pin), −10 (46-03 tutorial tests) = 3,231 — no test FILE deleted (tutorial.test.js was moved to icons.test.js; five toast-named files were moved); `git diff --stat 39c5a0f -- engine/` (exactly the six won files); `git diff --stat 39c5a0f -- content/` (empty); `git status --porcelain test/parity/fixtures` (empty); `git hash-object test/parity/prototype-master.js.txt` (`a1f4d0dc29782218d8e5aab65bc5989c33f917f0`); `npm run build:www` (exit 0); `npm run boot:check` (4 PASS).

    Also include: `## Gate outputs` (per commit, chronological — copy the lines from the three prior SUMMARYs plus this plan's); `## Artifacts this phase produced` (narrationLines.js + its 11 exports; dispatchWithNarration; the five renamed test files; icons.test.js; ident-sweep.mjs; the deleted exports/modules/settings); `## Human verification (deferred to end of run)` — the consolidated Pixel 7 batch: (1) a save from the current Play build resumes and plays on (the dropped won key); (2) RAIL card and fight-log lines for a step, a fight round, a camp read exactly as before (pure rename); (3) THEY ARE DOWN still shows after a won fight; (4) Settings shows the four fields and each persists across relaunch; (5) nothing else is user-visible. And a one-line `## Next phase readiness` note for Phase 47: new modules import `narrationLines.js`; `__mzWornSlots` is in the bridge set SHELL-04 must list; `tools/ident-sweep.mjs` is available for Phase 48's grep list (with `-i`, comments excluded — Phase 48 wants comments included, so it uses plain grep for that half).

    `requirements-completed: [NAME-01, NAME-02, DEAD-04, DEAD-05]`. Commit the SUMMARY as `docs(46-04): phase 46 closing summary — success criteria 1-5 verified, NAME-02 recorded grep + survivors, test ledger, deferred Pixel 7 items`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test -f .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md && grep -cE "^## (Success criteria|NAME-02 allowed survivors|Test ledger|Gate outputs|Artifacts this phase produced|Human verification|Next phase readiness)" .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## (Success criteria|NAME-02 allowed survivors|Test ledger|Gate outputs|Artifacts this phase produced|Human verification|Next phase readiness)" 46-04-SUMMARY.md` prints 7.
    - The SUMMARY contains `# pass 3231`, `# fail 0`, `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `ident-sweep flightLeft|flightCooldown: 5`, `ident-sweep toast: 0`, `ident-sweep wornSlots: 0`, `COMBAT_COPY.over.won`, and `self-test: PASS`.
    - `grep -c "engine/saveState.js:" 46-04-SUMMARY.md` is at least 5 (the survivors listed).
    - `git log --oneline 39c5a0f..HEAD | grep -vcE "^[0-9a-f]+ docs\("` prints 7 (the seven code/tool commits, one rename per commit; the SUMMARY docs commits are listed separately in Gate outputs with the exact total).
    - `git status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>46-04-SUMMARY.md is the phase closing record: criteria 1-5 verified verbatim, the NAME-02 recorded grep with its five explained survivors, the consolidated test ledger and gate outputs, the artifacts list, the batched Pixel 7 checks, and the Phase 47/48 readiness note.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share, and the phase total)

- New tool: `tools/ident-sweep.mjs` (comment-stripped identifier grep, `--self-test`).
- Phase total: `src/browser/narrationLines.js` (exports `LINE_FOR`, `linesForAction`, `narrativeLineText`, `oracleDetailText`, `ORACLE_ONLY`, `FEATURE_EVENTS`, `CARD_EVENTS`, `NARRATIVE_ACTIONS`, `TONES`, `PRIORITY`, `slotWord`); shell `dispatchWithNarration(action)`; renamed tests `narrationLinesTable.test.js`, `linesForAction.test.js`, `narrationLinesCoverage.test.js`, `narrativeLines.test.js`, `shell-narration-wiring.test.js`, `icons.test.js`; new pins (DEAD-04 stale save; NAME-02 settings tolerant load); deleted `src/browser/toasts.js`, `src/browser/tutorial.js`, `winGame`/`won`, the lifetime exports, the dead re-exports, the `controlScheme` setting.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → ident-sweep | the tool reads the repo's own source; untrusted input is limited to the regex arguments a developer types |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-08 | Tampering | ident-sweep stripping correctness (a false negative would hide a straggler) | medium | mitigate | `--self-test` covers line/block/HTML comments, strings, and the `@capacitor/*` pitfall; the positive control (`grep -c toast` large vs `ident-sweep toast: 0`) and the five expected survivors prove the run |
| T-46-09 | Denial of service | a catastrophic regex argument | low | accept | developer-typed argument on a local tool; not shipped |
| T-46-SC | Tampering | npm installs | low | accept | no package installed or upgraded; the tool uses node:fs/node:path only |
</threat_model>

<verification>
- ROADMAP Phase 46 criteria 1-5 hold at the final commit (each quoted with its command and output in the SUMMARY).
- NAME-02 rows: empty (recorded sweep with five explained survivors), adjacency (`wornSlots` 0 vs `__mzWornSlots` present, case-sensitive), ordering (`git log` one rename per commit in dependency order).
- Engine-gate fence at phase close: `git diff --stat 39c5a0f -- engine/` = the six won files only; `content/` untouched; zero fixture moves; master hash unchanged; `build:www` + `boot:check` green.
</verification>

<success_criteria>
- `tools/ident-sweep.mjs` committed, self-test PASS, recorded run = five foldLegacyCounters survivors, 0 elsewhere.
- 46-04-SUMMARY.md carries criteria 1-5 verbatim with outputs, the survivors table, the test ledger (3,242 → 3,231 with every delta explained), the artifacts list and the deferred Pixel 7 batch.
</success_criteria>

<output>
Create `.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md` when done (sections per Task 2).
</output>
