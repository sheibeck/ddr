---
phase: 46-honest-names-dead-exports-the-tutorial-decision
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/toasts.js
  - src/browser/narrationLines.js
  - src/browser/eventNarration.js
  - src/browser/fightLog.js
  - src/browser/rail.js
  - src/browser/viewModels.js
  - mazeworld.html
  - test/unit/ability-pool.test.js
  - test/unit/armorDisplay.test.js
  - test/unit/clarity-cause-lines.test.js
  - test/unit/cutthroat-joiner.test.js
  - test/unit/dismiss-joiner.test.js
  - test/unit/fightLog.test.js
  - test/unit/flee-retune.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/lootCompare.test.js
  - test/unit/loot-narration.test.js
  - test/unit/map-reveal.test.js
  - test/unit/movement.test.js
  - test/unit/narrativeToasts.test.js
  - test/unit/narrativeLines.test.js
  - test/unit/party-combat.test.js
  - test/unit/rail.test.js
  - test/unit/rations-audit.test.js
  - test/unit/round-card-worst-case.test.js
  - test/unit/shell-company-panel.test.js
  - test/unit/shell-fight-log.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/shell-map-invariants.test.js
  - test/unit/shell-map-rail.test.js
  - test/unit/shell-toast-wiring.test.js
  - test/unit/shell-narration-wiring.test.js
  - test/unit/spell-mechanics.test.js
  - test/unit/spell-utility.test.js
  - test/unit/toastsCoverage.test.js
  - test/unit/narrationLinesCoverage.test.js
  - test/unit/toastsForAction.test.js
  - test/unit/linesForAction.test.js
  - test/unit/toastTable.test.js
  - test/unit/narrationLinesTable.test.js
  - test/unit/usable-features-audit.test.js
  - test/unit/worn-slots.test.js
  - test/voice/safety-scan.test.js
autonomous: true
requirements: [NAME-01, NAME-02]

must_haves:
  truths:
    - "src/browser/toasts.js does not exist and src/browser/narrationLines.js exports LINE_FOR, linesForAction, narrativeLineText, oracleDetailText, TONES, PRIORITY, CARD_EVENTS, NARRATIVE_ACTIONS, ORACLE_ONLY, FEATURE_EVENTS, slotWord — and nothing named MAX_TOASTS, TOAST_*_MS or toastLifetime (NAME-01)"
    - "NAME-01 empty: LINE_FOR has exactly 250 keys (the count TOAST_FOR had at phase start — the earlier '189' figure is stale) and ORACLE_ONLY exactly 21 entries after this plan; the partition guards (every EVENT_NARRATION type is in LINE_FOR xor ORACLE_ONLY; no dead entries) pass under the new names"
    - "NAME-01 adjacency/ordering: a JSON dump of toastsForAction over a fixed 9-event attack mix and a fixed 3-event narrated move, captured at HEAD before the rename, is byte-identical to the same dump from linesForAction after the rename (diff empty, recorded in the SUMMARY) — same lines, same order, same text"
    - "Every production caller of linesForAction passes limit: Infinity (fightLog.js, rail path in mazeworld.html); the retired toast-host cap is gone, so the default limit is Infinity and the three tests that pinned a default cap of 4 now pin 'the default call returns every folded line'"
    - "The shell function is named dispatchWithNarration and is defined exactly once; every dispatch() call site routes through it; grep for the old function name over src/ mazeworld.html test/ tools/ returns 0"
    - "The dead re-exports are gone: eventNarration.js and rail.js no longer re-export the narration-lines surface (zero consumers existed); narrationLines.js still never imports eventNarration.js"
    - "The rename lands as exactly two commits — the module rename (git mv, history follows) and the shell-function rename — each green (npm test fail 0) and each carrying its zero-straggler grep in the commit message"
  artifacts:
    - path: "src/browser/narrationLines.js"
      provides: "the per-event narration-line table and fold pipeline the rail and fight log read"
      exports: ["LINE_FOR", "linesForAction", "narrativeLineText", "oracleDetailText", "ORACLE_ONLY", "FEATURE_EVENTS", "CARD_EVENTS", "NARRATIVE_ACTIONS", "TONES", "PRIORITY", "slotWord"]
    - path: "test/unit/narrationLinesCoverage.test.js"
      provides: "the partition guard (LINE_FOR ⊎ ORACLE_ONLY = EVENT_NARRATION universe) + purity tripwire under the new names"
    - path: "test/unit/linesForAction.test.js"
      provides: "the fold/priority/ordering pins, re-pinned to the uncapped default"
    - path: "test/unit/narrationLinesTable.test.js"
      provides: "the builder-shape and locked-wording pins"
    - path: "test/unit/narrativeLines.test.js"
      provides: "CARD_EVENTS / NARRATIVE_ACTIONS / narrativeLineText / ctx.narrate pins (lifetime tests deleted with the dead exports)"
    - path: "test/unit/shell-narration-wiring.test.js"
      provides: "the single dispatchWithNarration seam source-pins"
  key_links:
    - from: "mazeworld.html"
      to: "src/browser/narrationLines.js"
      via: "module-script import of linesForAction, NARRATIVE_ACTIONS, PRIORITY, narrativeLineText"
      pattern: "from \"\\./src/browser/narrationLines\\.js\""
    - from: "src/browser/fightLog.js"
      to: "src/browser/narrationLines.js"
      via: "linesForAction(type, events, ctx, { limit: Infinity, withIdx: true })"
      pattern: "linesForAction\\("
    - from: "src/browser/rail.js"
      to: "src/browser/narrationLines.js"
      via: "PRIORITY, ORACLE_ONLY, LINE_FOR, narrativeLineText, oracleDetailText"
      pattern: "from \"\\./narrationLines\\.js\""
  prohibitions:
    - statement: "No alias, re-export or compatibility shim for any old name (the old module path, the old table name, the old fold-function name, the old shell-function name, the lifetime constants) survives a commit boundary"
      status: unresolved
    - statement: "No narration text, tone, priority or ordering changes — every rail card and fight-log line is byte-identical before and after (pure rename; the only behavioural edit is the default limit, which no production caller exercises)"
      status: unresolved
    - statement: "content/ and engine/ are untouched by this plan; no parity fixture moves; the master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0 is unchanged"
      status: unresolved
    - statement: "No test file is deleted; the only deleted test CASES are the two that pinned the dead lifetime exports (toastLifetime boundary table, MAX_TOASTS is 4), each listed in the SUMMARY"
      status: unresolved
---

<!-- planner-discipline-allow: toasts.js -->
<!-- planner-discipline-allow: TOAST_FOR -->
<!-- planner-discipline-allow: toastsForAction -->
<!-- planner-discipline-allow: narrativeToastText -->
<!-- planner-discipline-allow: MAX_TOASTS -->
<!-- planner-discipline-allow: toastLifetime -->
<!-- planner-discipline-allow: TOAST_BASE_MS -->
<!-- planner-discipline-allow: TOAST_PER_CHAR_MS -->
<!-- planner-discipline-allow: TOAST_CAP_MS -->
<!-- planner-discipline-allow: TOAST_STACK_BONUS_MS -->
<!-- planner-discipline-allow: dispatchWithToasts -->

<objective>
Rename `src/browser/toasts.js` to `src/browser/narrationLines.js` with its exports named for what they do today — `TOAST_FOR → LINE_FOR`, `toastsForAction → linesForAction`, `narrativeToastText → narrativeLineText` — delete the dead toast-lifetime exports (`MAX_TOASTS`, `TOAST_BASE_MS`, `TOAST_PER_CHAR_MS`, `TOAST_CAP_MS`, `TOAST_STACK_BONUS_MS`, `toastLifetime()`), delete the two consumer-less re-export lines (eventNarration.js, rail.js), move every one of the 35 importers (5 `src/`, the shell, 28 tests, the voice scan) to the new names, and rename the shell's `dispatchWithToasts(action)` to `dispatchWithNarration(action)` — as two commits, each with the grep that proves zero stragglers (NAME-01; the rename half of NAME-02's toast list; CONTEXT "Names" decisions, user-accepted 2026-09-19).

Purpose: the module holds the per-event narration lines the RAIL and the fight log read — there has been no toast UI since Phase 35. NAME-02's zero-straggler grep is the gate, so every identifier with "toast" in it goes, not just the NAME-01 list.

Output: `src/browser/narrationLines.js` (git-moved, history intact); five `*toast*.test.js` files git-moved to honest names (recorded); two commits; `46-01-SUMMARY.md` carrying both grep outputs, the before/after fold diff, and the test-delta table.

Probe rows carried by this plan: NAME-01 adjacency, NAME-01 empty, NAME-01 ordering (all authored as plain truths in `must_haves.truths`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-CONTEXT.md
@src/browser/toasts.js
@src/browser/eventNarration.js
@src/browser/fightLog.js
@src/browser/rail.js
@test/unit/toastsCoverage.test.js
@test/unit/formatEventsCoverage.test.js
@test/unit/shell-toast-wiring.test.js

**Phase facts (verified 2026-09-19 at HEAD 39c5a0f):**
- `toasts.js` is 1,657 lines. Exports: `TONES` L59, `PRIORITY` L66, `MAX_TOASTS` L69, `CARD_EVENTS` L84, `NARRATIVE_ACTIONS` L94, `TOAST_BASE_MS`/`TOAST_PER_CHAR_MS`/`TOAST_CAP_MS`/`TOAST_STACK_BONUS_MS` L106-109, `toastLifetime` L118-122, `narrativeToastText` L158, `oracleDetailText` L177, `ORACLE_ONLY` L193 (21 entries), `FEATURE_EVENTS` L224 (72 entries), `slotWord` L383, `toastsForAction` L931 (its `limit` default reads `MAX_TOASTS` at L933), `TOAST_FOR` L969 (250 keys). `EVENT_NARRATION` has 270 keys.
- Production callers of `toastsForAction`: `src/browser/fightLog.js:45` and `mazeworld.html:5517` — both pass `{ limit: Infinity, withIdx: true }`. The default cap of 4 is exercised only by three tests (`toastsForAction.test.js:68`, `shell-fight-log.test.js:224-235`, `round-card-worst-case.test.js:155-159`).
- The other consumers of the dead exports are tests only: `narrativeToasts.test.js` (imports the four `TOAST_*_MS` + `toastLifetime`, one test at L84-112), `toastTable.test.js:21-25` ("MAX_TOASTS is 4"), `shell-toast-wiring.test.js:155-162` (a source regex that spells the literal), `shell-map-invariants.test.js:76-78,102` (concatenated literals under map KEYS that spell `toastLifetime`).
- Re-exports with zero consumers: `src/browser/eventNarration.js:1069` (`export { TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, CARD_EVENTS, NARRATIVE_ACTIONS, toastsForAction } from "./toasts.js";`) and `src/browser/rail.js:557-561` (`export { TOAST_FOR, ORACLE_ONLY };` + its comment). The only thing that reads them is the identity test in `toastsCoverage.test.js:249-261`.
- The shell imports from `toasts.js` on three lines (`mazeworld.html:4861`, `4866`, `4871`) and defines `dispatchWithToasts(action)` once at L5505 with 9 call sites (L5609, 5954, 5972, 5979, 5999, 6013, 6031, 6102, 6235). Seven test files spell `dispatchWithToasts` (round-card-worst-case, shell-company-panel, shell-fight-log, shell-gear-39, shell-map-invariants, shell-map-rail, shell-toast-wiring).
- `src/browser/viewModels.js:154` mentions the old path in a comment only. `eventNarration.js:50` imports `slotWord` from the module (kept, path follows). `fightLog.js:45-46` has a local `const toasts = ...` (rename to `lines`).
- Working tree is LF; Git Bash GNU `sed -i` and `git mv` are available; no `jq`; multi-line scripts go through the Write tool, never a heredoc.
- Baseline: `npm test` 3,242 pass / 0 fail; `npm run boot:check` 4 PASS; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv toasts.js → narrationLines.js, rename the exports, delete the dead lifetime exports and the consumer-less re-exports, move all 35 importers (one commit)</name>
  <files>src/browser/toasts.js, src/browser/narrationLines.js, src/browser/eventNarration.js, src/browser/fightLog.js, src/browser/rail.js, src/browser/viewModels.js, mazeworld.html, test/unit/ability-pool.test.js, test/unit/armorDisplay.test.js, test/unit/clarity-cause-lines.test.js, test/unit/cutthroat-joiner.test.js, test/unit/dismiss-joiner.test.js, test/unit/fightLog.test.js, test/unit/flee-retune.test.js, test/unit/hp-not-wp.test.js, test/unit/lootCompare.test.js, test/unit/loot-narration.test.js, test/unit/map-reveal.test.js, test/unit/movement.test.js, test/unit/narrativeToasts.test.js, test/unit/narrativeLines.test.js, test/unit/party-combat.test.js, test/unit/rail.test.js, test/unit/rations-audit.test.js, test/unit/round-card-worst-case.test.js, test/unit/shell-company-panel.test.js, test/unit/shell-fight-log.test.js, test/unit/shell-map-invariants.test.js, test/unit/shell-map-rail.test.js, test/unit/shell-toast-wiring.test.js, test/unit/shell-narration-wiring.test.js, test/unit/spell-mechanics.test.js, test/unit/spell-utility.test.js, test/unit/toastsCoverage.test.js, test/unit/narrationLinesCoverage.test.js, test/unit/toastsForAction.test.js, test/unit/linesForAction.test.js, test/unit/toastTable.test.js, test/unit/narrationLinesTable.test.js, test/unit/usable-features-audit.test.js, test/unit/worn-slots.test.js, test/voice/safety-scan.test.js</files>
  <read_first>
    - src/browser/toasts.js L1-125 (header + the exports being deleted), L925-970 (toastsForAction + its limit default), L969 (TOAST_FOR)
    - src/browser/eventNarration.js L44-52 (the slotWord import + its re-export rationale comment), L1060-1069 (the re-export line)
    - src/browser/fightLog.js L20-50; src/browser/rail.js L30-36, L552-561
    - mazeworld.html L4855-4872 (the three import lines and their placement note about the `@capacitor/*` comment-stripper pitfall — keep the first import ABOVE that comment)
    - test/unit/toastsCoverage.test.js L228-262 (purity tripwire + the re-export identity test), test/unit/shell-toast-wiring.test.js L75-162, test/unit/shell-map-invariants.test.js L73-105, test/unit/narrativeToasts.test.js L1-30 + L82-113, test/unit/toastTable.test.js L10-26, test/unit/toastsForAction.test.js L55-70, test/unit/shell-fight-log.test.js L30-36 + L224-236, test/unit/round-card-worst-case.test.js L10-25 + L150-162, test/unit/fightLog.test.js L210-218
    - .planning/phases/44-retire-the-classic-engine-from-the-shell/44-04-SUMMARY.md (the per-layer commit + zero-reference grep precedent)
  </read_first>
  <action>
    Order matters — the before-dump comes first, on the untouched tree.

    1. **Before-dump (NAME-01 adjacency/ordering pin).** With the Write tool, create `<scratchpad>/46-01-fold-dump.mjs`: an ESM script that takes `argv[2]` = module file name and `argv[3]` = fold export name, dynamic-imports `pathToFileURL("C:/projects/mazeworld/src/browser/" + argv[2]).href` and `pathToFileURL("C:/projects/mazeworld/src/browser/eventNarration.js").href`, then prints `JSON.stringify` of two calls: (a) the fold over the exact 9-event attack mix from `test/unit/toastsForAction.test.js` L56-66 (`wardFaded`, `strikeRefused` reason wizard spell Freeze, `struck` target Dante dmg 8, `struckByFoe` name Rat dmg 3, `backstab`, `frenzy`, `leveled` level 2 wpGain 6, `mirrorFaded`, `trapPoisoned`) with ctx `{}` and opts `{ limit: Infinity, withIdx: true }`; (b) the fold over a `move` action with events `[{ type: "trapPoisoned" }, { type: "leveled", level: 2, wpGain: 6 }, { type: "floorChanged", depth: 3 }]`, ctx `{ narrate: narrateEvent }` and the same opts. Run `node <scratchpad>/46-01-fold-dump.mjs toasts.js toastsForAction > <scratchpad>/46-01-before.json` and confirm it is non-empty. (Scratch files never enter the repo.)

    2. **git mv** `src/browser/toasts.js` → `src/browser/narrationLines.js`.

    3. **Uniform token rename** across `src/`, `mazeworld.html`, `test/`, `tools/` (word-boundary, comments included — the criterion-1 grep is comment-inclusive), in this order: `\bTOAST_FOR\b → LINE_FOR`, `\btoastsForAction\b → linesForAction`, `\bnarrativeToastText\b → narrativeLineText`, then the path string `toasts\.js → narrationLines.js` (covers `./toasts.js`, `src/browser/toasts.js`, and the module's own first-line header). Use `git grep -lE <token> -- src mazeworld.html test tools | xargs sed -i -E 's/.../.../g'`. Do NOT touch `docs/`, `.planning/` or `.claude/` — those are Phase 48's.

    4. **Delete the dead exports** in `narrationLines.js`: `MAX_TOASTS` (L69 + its one-line JSDoc), the lifetime-constants block (the JSDoc L96-105 and the four `TOAST_*_MS` consts L106-109), `toastLifetime` (JSDoc L111-117 + function L118-122). In `linesForAction` change the destructure default to `limit = Infinity` and reword its JSDoc sentence about the default so it says the default is uncapped (every production caller passes `limit: Infinity`; the capped toast host was retired in Phase 35). Rewrite the module header comment (L1-45) so it describes what the file is now: the per-event narration LINE table (`LINE_FOR`) and fold pipeline (`linesForAction`) that `rail.js` (out of combat) and `fightLog.js` (in combat) read, beside `eventNarration.js`'s `EVENT_NARRATION` (the Oracle); keep the purity contract paragraph verbatim in meaning (no DOM, no `engine/` import, no `Math.random`/`Date.now` — the T-25-23 tripwire in the coverage test), keep the ORACLE_ONLY / FEATURE_EVENTS partition paragraph and the 260918-wy1 `slotWord` note; drop every sentence about a toast host, cap or lifetime. Comment prose elsewhere in the file that uses "toast" as a concept word is Phase 48's — leave it.

    5. **src importers.** `fightLog.js`: import from `./narrationLines.js` (done by step 3) and rename the local `const toasts` at L45-46 to `lines`. `rail.js`: delete the re-export block L556-561 (the comment + `export { LINE_FOR, ORACLE_ONLY };`). `eventNarration.js`: delete the re-export line (L1069 after step 3) and the comment lines L1060-1068 that introduce it; reword the L44-49 comment above the `slotWord` import so it no longer cites a re-export precedent (say: the import is one-directional — this file imports from narrationLines.js, never the reverse — the no-cycle rule the coverage test pins). `viewModels.js:154` is a comment mention of the path (already rewritten by step 3).

    6. **Shell.** The three import lines (L4861, 4866, 4871) now read from `./src/browser/narrationLines.js` with `linesForAction` / `narrativeLineText` (step 3); keep the first import ABOVE the `@capacitor/*` doc comment (the placement note at L4849-4860 explains why). No other shell edit in this task — `dispatchWithToasts` is Task 2's commit.

    7. **Test files — rename with git mv (recorded in the SUMMARY):** `toastTable.test.js → narrationLinesTable.test.js`, `toastsForAction.test.js → linesForAction.test.js`, `toastsCoverage.test.js → narrationLinesCoverage.test.js`, `narrativeToasts.test.js → narrativeLines.test.js`, `shell-toast-wiring.test.js → shell-narration-wiring.test.js`. Update the first-line `// test/unit/<name>` header of each. `test()`/`describe()` strings are Phase 48's — leave them unless the line is otherwise rewritten.

    8. **Test re-pins (the only behaviour-visible edits):**
       - `narrativeLines.test.js`: delete the `TOAST_BASE_MS`…`toastLifetime` names from the import (L15-19) and delete the whole lifetime test (L84-112, the DFB-02 boundary table); reword the header line L4 that lists them.
       - `narrationLinesTable.test.js`: drop `MAX_TOASTS` from the import (L13) and delete the "MAX_TOASTS is 4" test (L21-25).
       - `linesForAction.test.js` L68: the assertion becomes `out.length === 9` with the message "no default cap — every folded line is returned (the capped toast host is retired)"; the ordering loop below stays.
       - `shell-fight-log.test.js`: drop `MAX_TOASTS` from the import (L34); in the L224-236 test rename `cappedDefault` to `defaultCall`, assert `defaultCall.length === 6` (equal to `lines.length`) and retitle the test to say the default call is uncapped too.
       - `round-card-worst-case.test.js`: drop `MAX_TOASTS` from the import (L23); at L155-159 rename `hostCapped` to `defaultCall` and assert its length equals the length of the existing explicit `{ limit: Infinity }` fold in that test (the default is the uncapped fold); reword the header comment L13-15 accordingly.
       - `shell-map-invariants.test.js`: rename the two RETIRED map keys that spell the deleted name — `toastLifetimeBridge → lifetimeBridge`, `toastLifetime → lifetimeFn` (L76, L78, and the key list at L102); the concatenated VALUES stay untouched.
       - `shell-narration-wiring.test.js`: re-pin the three import-line regexes (L82, L133-135) to `narrationLines.js` / `linesForAction`; in the L155-162 test rename `toastsSrc → linesSrc`, point the path at `narrationLines.js`, pin the signature `export function linesForAction(type, events, ctx = {}, opts = {})` exactly once, keep the `deduped.slice(0, limit)` once-pin, and REPLACE the regex that spells the deleted constant with two assertions: the source matches `/limit = Infinity/` exactly once and does not match `/limit = [A-Z_]{2,}/` (no constant-backed default). The `dispatchWithToasts` pins in this file are Task 2's.
       - `narrationLinesCoverage.test.js`: `PURE_MODULE_FILES` path (L230, done by step 3); rewrite the L249-261 identity test into "the narration table and the rail do NOT re-export the narration-lines surface, and narrationLines.js never imports eventNarration.js": dynamic-import `eventNarration.js` and `rail.js`, assert `LINE_FOR`, `ORACLE_ONLY`, `FEATURE_EVENTS`, `linesForAction` are `undefined` on both module namespaces, keep the no-cycle `stripComments(...).includes('from "./eventNarration.js"')` assertion against `narrationLines.js`.
       - `fightLog.test.js` L216: the expected import-line list becomes `["./eventNarration.js", "./narrationLines.js"]`.
       - The deleted names also appear in test TITLES and comments that the token replace does not touch (deletions, not renames) — reword each so the criterion grep is clean: `shell-map-rail.test.js:171` title ("…the lifetime bridge, the cap constant, the lifetime function — literals concatenated…"); `shell-narration-wiring.test.js` header L19-20 and the L132-135 test (retitle to "the module imports NARRATIVE_ACTIONS on its own line and never imports CARD_EVENTS or a lifetime helper"; replace the `doesNotMatch` regex that spells the deleted function with an assertion that exactly three `from "./src/browser/narrationLines.js";` import lines exist and none of them contains `CARD_EVENTS`); `round-card-worst-case.test.js:13-15` header comment. Run `grep -rnE "MAX_TOASTS|toastLifetime|TOAST_[A-Z_]*_MS" src/ mazeworld.html test/ tools/` — it must print nothing before the commit; anything it still prints is a comment or title to reword the same way.
       - Every other test file: import path/name tokens only (step 3).

    9. **After-dump:** `node <scratchpad>/46-01-fold-dump.mjs narrationLines.js linesForAction > <scratchpad>/46-01-after.json`; `diff` the two files — must be empty; paste the diff command + "(empty)" into the SUMMARY.

    10. Run `npm test` (expect 3,240 pass — the two deleted lifetime tests — fail 0), `npm run build:www`, `npm run boot:check` (4 PASS), then the straggler grep below; stage with `git add -A src mazeworld.html test` (git mv renames are already staged) and commit as `refactor(46-01): rename src/browser/toasts.js -> narrationLines.js, TOAST_FOR -> LINE_FOR, toastsForAction -> linesForAction, narrativeToastText -> narrativeLineText; delete MAX_TOASTS/TOAST_*_MS/toastLifetime and the dead re-exports; 35 importers + five test files moved (NAME-01)` with the grep line `grep -rnE "toasts\.js|TOAST_FOR|toastsForAction|narrativeToastText|MAX_TOASTS|TOAST_[A-Z_]*_MS|toastLifetime" src/ engine/ content/ tools/ mazeworld.html test/ -> 0` in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && ! test -f src/browser/toasts.js && test -f src/browser/narrationLines.js && ! grep -rqE "toasts\.js|TOAST_FOR|toastsForAction|narrativeToastText|MAX_TOASTS|TOAST_[A-Z_]*_MS|toastLifetime" src/ engine/ content/ tools/ mazeworld.html test/ && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/toasts.js` exits 1; `test -f src/browser/narrationLines.js` exits 0; `git log --follow --oneline -- src/browser/narrationLines.js | wc -l` is greater than 1 (history followed the git mv).
    - `grep -rnE "toasts\.js|TOAST_FOR|toastsForAction|narrativeToastText|MAX_TOASTS|TOAST_[A-Z_]*_MS|toastLifetime" src/ engine/ content/ tools/ mazeworld.html test/` prints nothing (comments included).
    - `grep -cE "^export (const|function) (LINE_FOR|linesForAction|narrativeLineText|oracleDetailText|ORACLE_ONLY|FEATURE_EVENTS|CARD_EVENTS|NARRATIVE_ACTIONS|TONES|PRIORITY|slotWord)\b" src/browser/narrationLines.js` prints 11.
    - `grep -cE "limit = Infinity" src/browser/narrationLines.js` prints 1; `grep -cE "limit = [A-Z_]{2,}" src/browser/narrationLines.js` prints 0.
    - `grep -cE "^export \{" src/browser/eventNarration.js src/browser/rail.js` prints `0` for both files (no re-export lines).
    - `grep -cE "^import .* from \"\./narrationLines\.js\";" src/browser/eventNarration.js src/browser/fightLog.js src/browser/rail.js` prints 1 for each; `grep -cE "from \"\./src/browser/narrationLines\.js\";" mazeworld.html` prints 3.
    - `ls test/unit | grep -E "^(toastTable|toastsForAction|toastsCoverage|narrativeToasts|shell-toast-wiring)\.test\.js$"` prints nothing; `ls test/unit | grep -E "^(narrationLinesTable|linesForAction|narrationLinesCoverage|narrativeLines|shell-narration-wiring)\.test\.js$" | wc -l` prints 5.
    - `diff <scratchpad>/46-01-before.json <scratchpad>/46-01-after.json` prints nothing and both files are non-empty (`wc -c` greater than 100).
    - `node --test test/unit/narrationLinesCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/linesForAction.test.js test/unit/narrationLinesTable.test.js test/unit/narrativeLines.test.js test/voice/safety-scan.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3240` and `# fail 0` (the delta from 3,242 is exactly the two deleted lifetime tests; any other delta is listed with its reason in the SUMMARY).
    - `npm run build:www` exits 0; `npm run boot:check 2>&1 | grep -c PASS` prints 4.
    - `git status --porcelain` prints nothing after the commit; `git diff --stat 39c5a0f -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `git log -1 --format=%B | grep -c "toasts\.js|TOAST_FOR"` is at least 1 (the commit body carries the grep line).
  </acceptance_criteria>
  <done>The module lives at src/browser/narrationLines.js with honest export names, the dead lifetime exports and the two dead re-exports are gone, all 35 importers and the five renamed test files compile and pass, the before/after fold dump is byte-identical, and one commit carries the zero-straggler grep.</done>
</task>

<task type="auto">
  <name>Task 2: rename the shell's dispatchWithToasts(action) to dispatchWithNarration(action) — its own commit with its grep</name>
  <files>mazeworld.html, test/unit/round-card-worst-case.test.js, test/unit/shell-company-panel.test.js, test/unit/shell-fight-log.test.js, test/unit/shell-gear-39.test.js, test/unit/shell-map-invariants.test.js, test/unit/shell-map-rail.test.js, test/unit/shell-narration-wiring.test.js</files>
  <read_first>
    - mazeworld.html L5488-5533 (the function, its doc comment and the routing rule), L5603-5615 and L6025-6035 (two representative call sites)
    - test/unit/shell-narration-wiring.test.js L85-100 (defined-exactly-once + every-dispatch-routed pins)
    - tools/shell-sweep.mjs header (the `refs NAME` zero-reference gate)
  </read_first>
  <action>
    One token, one commit (per D "Commit discipline" in 46-CONTEXT.md): replace `\bdispatchWithToasts\b` with `dispatchWithNarration` everywhere in `mazeworld.html` and `test/` (definition, the 9 call sites, and every comment that names the function — the criterion-1 grep is comment-inclusive), via `git grep -lE "dispatchWithToasts" -- mazeworld.html test | xargs sed -i -E 's/\bdispatchWithToasts\b/dispatchWithNarration/g'`. Then reword ONLY the first sentence of the function's own doc comment (the paragraph directly above the definition, L5488-5504) so it says what the function does today: dispatches one action and routes its folded narration lines to the fight log (in or leaving combat) or to one RAIL card (out of combat); the rest of that paragraph (the `wasCombat`/`inCombat` routing rule, the `window.__mz*` presentation-only note) stays. The `window.__mzFightLog` / `__mzFightEnd` / `__mzCombatMenu` / `__mzRail` bridge names the function exposes are already honest — no bridge rename.

    Gates: `node tools/shell-sweep.mjs refs dispatchWithToasts` → 0; `node tools/shell-sweep.mjs refs dispatchWithNarration` → at least 10 (definition + 9 call sites); `npm test` fail 0 (count unchanged at 3,240); `npm run build:www`; `npm run boot:check` 4 PASS. Commit as `refactor(46-01): rename the shell's dispatchWithToasts(action) -> dispatchWithNarration(action) (NAME-01)` with the grep line `grep -rnE "dispatchWithToasts" src/ engine/ content/ tools/ mazeworld.html test/ -> 0` in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && ! grep -rqE "dispatchWithToasts" src/ engine/ content/ tools/ mazeworld.html test/ && [ "$(grep -cE 'function dispatchWithNarration\(action\)' mazeworld.html)" = "1" ] && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "dispatchWithToasts" src/ engine/ content/ tools/ mazeworld.html test/` prints nothing.
    - `grep -cE "function dispatchWithNarration\(action\)" mazeworld.html` prints 1; `grep -cE "dispatchWithNarration\(" mazeworld.html` is at least 10.
    - `node tools/shell-sweep.mjs refs dispatchWithToasts` reports `refs dispatchWithToasts: 0`.
    - `node --test test/unit/shell-narration-wiring.test.js test/unit/shell-fight-log.test.js test/unit/shell-map-rail.test.js test/unit/shell-company-panel.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3240` and `# fail 0`; `npm run build:www` exits 0; `npm run boot:check 2>&1 | grep -c PASS` prints 4.
    - `git log --oneline 39c5a0f..HEAD | grep -vcE "^[0-9a-f]+ docs\("` prints 2 (the two rename commits; SUMMARY docs commits are excluded by the filter) and `git status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>The shell's single dispatch seam is named dispatchWithNarration, defined once, routed through by every dispatch call site, pinned by the renamed wiring test, with its own green commit and zero-straggler grep.</done>
</task>

<task type="auto">
  <name>Task 3: plan gates and 46-01-SUMMARY.md — grep outputs per rename, fold diff, test-delta table, renamed files, deferred human checks</name>
  <files>.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md (the "Gate outputs" / "Tests re-pinned or deleted" / "Human verification (deferred to end of run)" section shapes to mirror)
    - $HOME/.claude/gsd-core/templates/summary.md
  </read_first>
  <action>
    Re-run and paste verbatim into the SUMMARY: (1) the Task 1 grep and the Task 2 grep, each with "(empty)"; (2) `git log --oneline 39c5a0f..HEAD` (exactly two commits, in order); (3) the fold-dump diff command and "(empty)" plus the byte size of the dump; (4) `npm test` pass/fail lines, `npm run build:www` exit, `npm run boot:check` PASS count — at each of the two commits (record the per-commit lines like 45-03's "Gate outputs"); (5) `git hash-object test/parity/prototype-master.js.txt` and `git status --porcelain test/parity/fixtures` (empty) and `git diff --stat 39c5a0f -- engine/ content/` (empty).

    Sections the SUMMARY must carry, by name: `## Renamed files` (module + the five test files, old → new, `git mv`); `## Deleted exports` (six names, with the reason "dead since Phase 35 — the toast host is retired; only tests read them"); `## Deleted re-exports` (eventNarration.js L1069, rail.js L561 — zero consumers, identity test rewritten); `## Tests re-pinned or deleted` (a table: the two deleted lifetime tests, the three default-cap re-pins with their new assertion, the identity-test rewrite, the RETIRED-map key rename, the wiring-test regex re-pins, the fightLog import-line pin); `## NAME-02 allowed survivors (this plan)` — "none: every identifier with toast in it under src/, the shell and tools/ is renamed; comment prose using the word is Phase 48's (DOCS-01)"; `## Human verification (deferred to end of run)` — one item: on the Pixel 7, the RAIL card lines and the fight-log lines for a step, a fight round and a camp read exactly as before this build (pure rename; the fold dump diff is the desk proof).

    The `requirements-completed` frontmatter field lists `NAME-01` only (NAME-02 closes in 46-04 with the recorded grep). Commit the SUMMARY as `docs(46-01): complete narrationLines rename plan` per the execute-plan workflow.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test -f .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-01-SUMMARY.md && grep -cE "^## (Renamed files|Deleted exports|Deleted re-exports|Tests re-pinned or deleted|NAME-02 allowed survivors|Human verification)" .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## (Renamed files|Deleted exports|Deleted re-exports|Tests re-pinned or deleted|NAME-02 allowed survivors|Human verification)" 46-01-SUMMARY.md` prints 6.
    - The SUMMARY contains the literal strings `# pass 3240`, `# fail 0`, `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, and both rename grep commands each followed by `(empty)`.
    - `grep -c "git mv" 46-01-SUMMARY.md` is at least 6 (module + five test files).
  </acceptance_criteria>
  <done>46-01-SUMMARY.md is the plan's closing record: both grep outputs, the commit list, the fold diff, per-commit gate lines, the renamed/deleted/re-pinned tables and the deferred device check.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- `src/browser/narrationLines.js` — the renamed module (git mv from `src/browser/toasts.js`); exports `LINE_FOR`, `linesForAction`, `narrativeLineText`, `oracleDetailText`, `ORACLE_ONLY`, `FEATURE_EVENTS`, `CARD_EVENTS`, `NARRATIVE_ACTIONS`, `TONES`, `PRIORITY`, `slotWord` (11); no lifetime exports.
- Shell function `dispatchWithNarration(action)` in `mazeworld.html` (was `dispatchWithToasts`).
- Renamed test files (git mv): `test/unit/narrationLinesTable.test.js`, `test/unit/linesForAction.test.js`, `test/unit/narrationLinesCoverage.test.js`, `test/unit/narrativeLines.test.js`, `test/unit/shell-narration-wiring.test.js`.
- Deleted: exports `MAX_TOASTS`, `TOAST_BASE_MS`, `TOAST_PER_CHAR_MS`, `TOAST_CAP_MS`, `TOAST_STACK_BONUS_MS`, `toastLifetime`; the re-export lines in `eventNarration.js` and `rail.js`; two test cases (lifetime table, cap-is-4).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events → narration lines | untrusted-shaped event objects (bare `{ type }` in guards) enter the builders; every builder defends fields with `??`/`?.` (T-25-08, unchanged) |
| narration text → DOM (rail/fight log) | `narrativeLineText` strips markup/decodes entities before the shell inserts text; behaviour unchanged by the rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-01 | Tampering | narrationLines.js purity (no DOM / engine import / Math.random) | medium | mitigate | the T-25-23 purity tripwire in narrationLinesCoverage.test.js keeps scanning the renamed path (PURE_MODULE_FILES updated) |
| T-46-02 | Information disclosure | narrativeLineText HTML stripping | low | accept | pure rename — the before/after fold dump and the existing strip/entity tests prove identical output |
| T-46-SC | Tampering | npm installs | low | accept | no package is installed or upgraded in this phase (no install task exists; package.json untouched) |
</threat_model>

<verification>
- Criterion 1 of ROADMAP Phase 46 holds at the end of this plan: `src/browser/toasts.js` absent; `narrationLines.js` exports `LINE_FOR`, `linesForAction`; the shell's `dispatchWithNarration`; the comment-inclusive grep for the old tokens over `src/ mazeworld.html test/ tools/` returns 0; the coverage guards hold under the new names; each rename is its own commit.
- Engine-gate fence: `git diff --stat 39c5a0f -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` empty; master hash unchanged.
- `npm test` 3,240 / fail 0; `npm run build:www` exit 0; `npm run boot:check` 4 PASS at both commits.
</verification>

<success_criteria>
- Two green commits on master: the module rename (git mv + exports + 35 importers + five test-file moves) and the shell-function rename, each with its zero-straggler grep in the body.
- The before/after fold dump diff is empty (NAME-01 adjacency/ordering pinned); `LINE_FOR` has 250 keys and `ORACLE_ONLY` 21 (NAME-01 empty pinned by the partition guards).
- 46-01-SUMMARY.md carries every section named in Task 3.
</success_criteria>

<output>
Create `.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-01-SUMMARY.md` when done (sections per Task 3).
</output>
