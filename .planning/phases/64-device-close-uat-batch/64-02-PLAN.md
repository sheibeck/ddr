---
phase: 64-device-close-uat-batch
plan: 02
type: execute
wave: 2
depends_on: [64-01]
files_modified:
  - docs/UAT-v1.9.md
  - .planning/REQUIREMENTS.md
  - .planning/todos/pending/
autonomous: false
requirements: [GSCR-12]
must_haves:
  truths:
    - "The v1.9 debug APK is installed on the Pixel 7 with adb install -r, force-stopped and relaunched"
    - "Every checklist row walked with the user has its Result recorded as the user stated it; unwalked rows read 'not reached'"
    - "Every failed row has a todo in .planning/todos/pending/ naming the check, the observed behaviour and the file anchors"
    - "GSCR-12 is Complete when the walk is recorded, or 'Partial — N of M walked' if cut short"
  artifacts:
    - path: docs/UAT-v1.9.md
      provides: recorded results for the v1.9 device batch
---

<objective>
The device walk (GSCR-12, the device half). It is run INLINE by the orchestrator with the user: executors can't talk to the user. Install over wireless adb, relay the checks in the suggested order in small batches, record each result as the user states it, and turn findings into todos. No product code edits during the walk.
</objective>

<tasks>
<task type="checkpoint:human-verify">
  <name>Task 1: Install and relaunch</name>
  <read_first>docs/UAT-v1.9.md, STATE.md Ground Truth (device notes)</read_first>
  <action>`adb -s <device> install -r <apk>`, then `adb shell am force-stop com.darktierstudios.delvedierepeat`, then `adb shell monkey -p com.darktierstudios.delvedierepeat -c android.intent.category.LAUNCHER 1`. Confirm with `dumpsys package` (lastUpdateTime moved).</action>
  <acceptance_criteria>
    - `adb install -r` prints Success
    - `dumpsys package com.darktierstudios.delvedierepeat` shows a lastUpdateTime after the build
  </acceptance_criteria>
</task>
<task type="checkpoint:human-verify">
  <name>Task 2: Walk the checklist with the user</name>
  <read_first>docs/UAT-v1.9.md</read_first>
  <action>Present the rows section by section: a few at a time, in plain words, with what to do and what to expect. Record each answer in the Result cell ("pass", "fail: <what they saw>", "not reached"). For each fail, write a todo (area, observed vs expected, file anchors from the phase SUMMARYs). Commit the checklist after each section.</action>
  <acceptance_criteria>
    - no empty Result cell remains (each is pass / fail: … / not reached)
    - every "fail" row has a matching todo file
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 3: Close the traceability</name>
  <read_first>.planning/REQUIREMENTS.md</read_first>
  <action>Mark GSCR-12 Complete (or "Partial — N of M walked"). Write 64-02-SUMMARY.md with the tallies and the todo list.</action>
  <acceptance_criteria>
    - the REQUIREMENTS.md GSCR-12 row reflects the walk outcome
    - 64-02-SUMMARY.md exists with pass / fail / not-reached counts
  </acceptance_criteria>
</task>
</tasks>

## Artifacts this phase produces
- Recorded results in `docs/UAT-v1.9.md`
- Todos for any failed checks
- `64-01-SUMMARY.md`, `64-02-SUMMARY.md`
