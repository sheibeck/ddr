---
phase: 04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mazeworld.html
  - content/flavor.js
  - content/epitaphs.js
  - src/browser/eventNarration.js
  - src/browser/viewModels.js
  - engine/economy.js
  - test/unit/economy.test.js
  - test/unit/characterSheetViewModel.test.js
autonomous: true
requirements: [TERM-01, TERM-02]
must_haves:
  truths:
    - "No player-facing screen shows the phrase 'skill point(s)' — every such string now reads 'experience point(s)' (or 'XP' where space is tight: HUD footer, kit rows) — TERM-01"
    - "No player-facing screen shows 'Win Potential' or the unit 'wp' for hit points — the HERO sheet label, flavor blurbs, event-narration unit, and the engine-generated store-row labels all read 'Hit Points'/'hp', matching the HUD's existing 'HP' — TERM-02"
    - "The engine state fields c.sp, c.wp, c.maxWP and every event-type identifier (spGained/healed/struck/rested/...) are byte-for-byte unchanged — this is a text-only pass, not a save-migration"
    - "The engine-generated store-row label at engine/economy.js openStore (the (+N wp) food label and the AR/wp armour sub-strings) reads 'hp', and test/unit/economy.test.js fixtures assert the new string in the same commit"
  artifacts:
    - test/unit/characterSheetViewModel.test.js
    - test/unit/economy.test.js
  key_links:
    - "characterSheetViewModel.test.js and formatEventsCoverage.test.js are the regression guards — updating their asserted label strings to the new terms is what proves the rename landed and stays landed"
    - "This is the lowest-risk item and the Wave-1 foundation; it renames the store-label string that plan 04.1-03 (RATION-01) later rewrites, so it must land first"
---

<objective>
Resolve TERM-01 and TERM-02: a pure player-facing TEXT rename pass. Replace every
user-visible "skill point(s)" with "experience point(s)"/"XP" (TERM-01), and every
user-visible "Win Potential" and "wp" hit-point unit with "Hit Points"/"hp"
(TERM-02) — across the live HTML, the flavor/epitaph content, the event narration,
the character-sheet view model, AND the engine-generated store-row labels. This
also fixes a live split-brain bug: the HUD already says "HP" while the HERO sheet
still says "Win Potential" — after this plan every screen agrees.

Purpose: The character sheet must not show the player two different names for one
stat, and the currency the player levels with must read like what it is
(experience), per the phase end goal.
Output: renamed presentation strings + engine-generated store labels; updated
economy + characterSheetViewModel fixtures; determinism/save-shape untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-CONTEXT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-RESEARCH.md
@src/browser/viewModels.js
@src/browser/eventNarration.js
@content/flavor.js
@content/epitaphs.js
@engine/economy.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: TERM-01 — rename "skill points" to "experience points"/"XP"</name>
  <files>mazeworld.html, content/flavor.js, content/epitaphs.js, src/browser/eventNarration.js, src/browser/viewModels.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - 04.1-RESEARCH.md Terminology Audit, "skill point(s)" occurrence table: mazeworld.html (10 sites incl. HUD footer ~4373 and the s-sp sheet label ~1298), content/flavor.js SUB_NOTE.Barbarian + SUB_NOTE.Apprentice (the "skill points" lines), content/epitaphs.js templates using "{sp} skill points", src/browser/eventNarration.js spGained builder (~line 79-82 uses plural(..., "skill point")), src/browser/viewModels.js the SKILL POINTS stat label (line 99)
    - src/browser/viewModels.js characterSheetViewModel stats array — label at line 99 is "SKILL POINTS"; rename to "EXPERIENCE" (use the full word on the sheet where it fits; the tight HUD footer/kit rows use "XP")
    - test/unit/characterSheetViewModel.test.js — the assertion(s) on the SKILL POINTS label string; these must change to the new label in this commit
  </read_first>
  <action>
    Rename every PLAYER-FACING "skill point"/"skill points" occurrence to "experience point"/"experience points", using the abbreviation "XP" only where horizontal space is tight (the HUD footer and any kit/summary rows), exactly as the CONTEXT's TERM-01 decision specifies. Sites: the mazeworld.html static markup (HUD footer, the s-sp sheet-row label, any toast/log strings); content/flavor.js SUB_NOTE.Barbarian and SUB_NOTE.Apprentice; content/epitaphs.js templates (the "{sp} skill points" strings become "{sp} experience points" — keep the {sp} token, only the words change); src/browser/eventNarration.js's spGained narration (the plural noun becomes "experience point"); and src/browser/viewModels.js's stats label at line 99 ("SKILL POINTS" to "EXPERIENCE"). Do NOT touch the state field `c.sp`, the event type `spGained`, or any object key — this is TERM-01 and it is player-visible strings ONLY. Update test/unit/characterSheetViewModel.test.js to assert the new "EXPERIENCE" label instead of "SKILL POINTS" in the same commit so the regression guard tracks the rename. Preserve the sarcastic tone of the flavor/epitaph strings — reword only the two words, nothing else.
  </action>
  <verify>
    <automated>node --test test/unit/characterSheetViewModel.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - No player-facing string in the six edited files contains "skill point" (singular or plural); each now reads "experience point(s)" or "XP" where space is tight.
    - characterSheetViewModel.test.js asserts the new EXPERIENCE label and passes; formatEventsCoverage stays green (no event type added).
    - `c.sp` and the `spGained` event type are unchanged; `npm run test:quick` stays green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: TERM-02 — rename "Win Potential"/"wp" to "Hit Points"/"hp" (incl. engine store labels)</name>
  <files>mazeworld.html, content/flavor.js, src/browser/eventNarration.js, src/browser/viewModels.js, engine/economy.js, test/unit/economy.test.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - 04.1-RESEARCH.md Terminology Audit: "Win Potential" (13 in mazeworld.html incl. the sheet label ~1288 + race/class blurbs; 5 in content/flavor.js RACE_NOTE.Elven/Troll + CLASS_NOTE.Fighter/Thief/Magic User) and "wp" unit (271 in mazeworld.html; 26 narration lines in src/browser/eventNarration.js)
    - 04.1-RESEARCH.md "Provenance/risk split" — the RISKIER site is engine/economy.js openStore where the stock label is engine-generated into serialized state.store.stock[i].n; renaming the text is still a string change, not a schema change, but test/unit/economy.test.js must update in the SAME commit
    - engine/economy.js openStore: the food label template `(+${f.wp} wp)` (line ~85), the armour sub-string `AR ${a.ar}, ${a.wp} wp` (~line 117), and the repair sub-string wording — rename the player-facing "wp" unit to "hp"; the `f.wp`/`a.wp` value reads stay identical (data field, not renamed)
    - src/browser/viewModels.js: the UPKEEP value "wp/day" (line 101) becomes "hp/day"; the winPotential view-model key/value block (lines 104-108) reads c.wp/c.maxWP — the object KEY `winPotential` may stay (internal), only the on-screen sheet LABEL in mazeworld.html:1288 changes to "Hit Points"
    - src/browser/eventNarration.js: the ~26 narration lines ending in " wp" (struck/struckByFoe/healed/rested/wentHungry/foodFound/potionDrunk/backfireSelfDamage/... ) — rename the unit text to " hp"
    - test/unit/economy.test.js + test/unit/characterSheetViewModel.test.js — any asserted store-label or sheet-label string containing "wp"/"Win Potential"
  </read_first>
  <action>
    Rename every PLAYER-FACING "Win Potential" to "Hit Points" and every player-facing hit-point unit "wp" to "hp", matching the HUD's existing "HP" (this closes the HUD-says-HP-but-sheet-says-Win-Potential split the CONTEXT flags under TERM-02). Cover: mazeworld.html (the HERO sheet label at ~1288, the race/class blurbs duplicated inline, and every "wp" unit string across HUD/sheet/kit/log); content/flavor.js RACE_NOTE.Elven, RACE_NOTE.Troll, CLASS_NOTE.Fighter, CLASS_NOTE.Thief, CLASS_NOTE.Magic User (the "Win Potential" phrasings — reword to "Hit Points" while preserving the deadpan tone); src/browser/eventNarration.js (rename the trailing " wp" unit to " hp" on all ~26 lines); src/browser/viewModels.js (the UPKEEP "wp/day" value to "hp/day"; leave the internal `winPotential` object key untouched, it is not shown to the player); and engine/economy.js's openStore stock/sub label strings (the (+N wp) food label and the AR/wp armour sub-string become hp). CRITICAL: do NOT rename the state fields `c.wp`/`c.maxWP`/`c.armorWP`, the data keys `f.wp`/`a.wp`/`p.wp`, or any event-type/object-field identifier — the value reads are identical, only the visible unit text changes (per CONTEXT: renames are player-facing TEXT ONLY; c.wp/c.maxWP are a save-migration boundary). Update test/unit/economy.test.js and test/unit/characterSheetViewModel.test.js fixtures that assert the old label strings to the new "hp"/"Hit Points" text in the same commit.
  </action>
  <verify>
    <automated>node --test test/unit/economy.test.js test/unit/characterSheetViewModel.test.js test/unit/formatEventsCoverage.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - No player-facing string across the edited files contains "Win Potential" or the hit-point unit "wp"; the HERO sheet, flavor blurbs, narration, and engine store labels all read "Hit Points"/"hp".
    - engine/economy.js's engine-generated store-row labels read "hp"; economy.test.js asserts the new label string and passes.
    - State fields c.wp/c.maxWP/c.armorWP and all event-type identifiers are unchanged; the roundtrip serialize/rehydrate test stays green (save shape intact).
    - `npm test` full suite green (determinism/parity/roundtrip intact).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| presentation string ↔ serialized state | Renames must stay in display text; a stray edit to a state-field/event-type key is a save-migration-class change |
| engine-generated label ↔ save file | engine/economy.js's store-row label string is serialized into state.store.stock[i].n — a text change is safe, a key/shape change is not |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-01 | Tampering | a rename accidentally touching c.sp/c.wp/c.maxWP or an event-type key | high | mitigate | Renames are text-only per CONTEXT; the roundtrip serialize/rehydrate + parity + determinism suites (run in `npm test`) prove save shape and RNG order are unchanged. |
| T-04.1-02 | Information Disclosure | inconsistent naming leaving one screen on the old term | low | mitigate | characterSheetViewModel + formatEventsCoverage tests assert the new label strings; the executor sweeps all six presentation files for residual old terms. |
| T-04.1-SC | Tampering | supply chain | n/a | accept | No npm/package installs in this phase — no supply-chain surface introduced. |
</threat_model>

<verification>
- `npm test` full suite green — characterSheetViewModel/economy/formatEventsCoverage/roundtrip all pass with the new label strings.
- No residual "skill point", "Win Potential", or hit-point-unit "wp" strings remain in mazeworld.html, content/flavor.js, content/epitaphs.js, src/browser/eventNarration.js, src/browser/viewModels.js, or engine/economy.js's player-facing labels.
- Deferred device-UAT: on the Pixel 7, confirm the HERO sheet and HUD agree ("HP"/"experience"/"XP") and no screen shows the old terms.
</verification>

<success_criteria>
- CONTEXT success criterion 1 is met: no player-facing screen shows "skill points" or "Win Potential"/"wp" for HP; the HUD and HERO sheet agree, with the engine state fields and save shape untouched.
</success_criteria>

<output>
Create `.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-01-SUMMARY.md` when done.
</output>
