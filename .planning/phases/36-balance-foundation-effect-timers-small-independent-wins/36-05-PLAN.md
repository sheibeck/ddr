---
phase: 36-balance-foundation-effect-timers-small-independent-wins
plan: 05
type: execute
wave: 4
depends_on: ["36-04"]
files_modified:
  - engine/encounters.js
  - engine/actions.js
  - engine/engine.js
  - content/flavor.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/unit/narrativeToasts.test.js
  - test/voice/safety-scan.test.js
  - test/unit/dismiss-joiner.test.js
autonomous: true
requirements: [JOIN-01]
tags: [joiner, dismiss, engine-action, new-event, no-rng, voice, rail, deferred-uat]
notes:
  wave_rationale: "wave 4 because Plan 04 edits engine/encounters.js, content/flavor.js, eventNarration.js, toasts.js, rail.js and safety-scan.test.js — this plan extends the same files (dismissJoiner beside resolveJoiner; the parting-line bank beside the murder bank)"
  rng_draw_impact: "adds zero draws — dismissJoiner is a pure data mutation (party splice) with no rng parameter; applyAction persists an unchanged rngState"
  serialized_field_impact: "none — splices state.party (existing field); joinerDismissed/dismissRefused are events, not state"

must_haves:
  truths:
    - "applyAction(state, { type: 'dismissJoiner' }) on a state with one party member returns a state whose party is [] and events containing joinerDismissed { name, sub } of that member; rngState is unchanged; the input state is not mutated (JOIN-01)"
    - "dismissJoiner refuses with a named reason and NO mutation when the party is empty (noParty), when state.combat is set (inCombat), or when i points past the roster (badIndex) — each pushes dismissRefused { reason } (JOIN-01 'refuses with a named reason')"
    - "validateAction accepts { type: 'dismissJoiner' } and { type: 'dismissJoiner', i: 0 } and rejects i: -1 / i: '0' / i: 1.5 with the reason string dismissJoiner.i must be a non-negative integer when present"
    - "EVENT_NARRATION.joinerDismissed picks one of five JOINER_PARTING_LINES deterministically (name length, no rng), html-escapes the name; EVENT_NARRATION.dismissRefused names each reason; TOAST_FOR carries both; RAIL_FAMILY.joinerDismissed is COMPANY / dull; dismissRefused falls through to the block fallback (NOTHING DOING); NARRATIVE_ACTIONS includes dismissJoiner so the rail line IS the parting sentence; both coverage guards and the voice scan are green (JOIN-01 'sarcastic parting line of their own (a new event type, no rng)')"
    - "railCardFor('dismissJoiner', events, folded, { narrate }) for a joinerDismissed event yields title COMPANY, tone dull, first line = the parting sentence (test/unit/dismiss-joiner.test.js)"
    - "npm test prints '# fail 0'; fixtures untouched; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - path: "engine/encounters.js"
      provides: "export function dismissJoiner(state, i = 0, events = []) directly after resolveJoiner"
    - path: "engine/actions.js"
      provides: "'dismissJoiner' in ACTION_TYPES + its validate case"
    - path: "engine/engine.js"
      provides: "case 'dismissJoiner' beside resolveJoiner"
    - path: "content/flavor.js"
      provides: "JOINER_PARTING_LINES (5 lines, {name} token)"
    - path: "src/browser/eventNarration.js"
      provides: "EVENT_NARRATION.joinerDismissed + .dismissRefused"
    - path: "src/browser/toasts.js"
      provides: "TOAST_FOR.joinerDismissed + .dismissRefused; FEATURE_EVENTS += dismissRefused; NARRATIVE_ACTIONS += dismissJoiner"
    - path: "src/browser/rail.js"
      provides: "RAIL_FAMILY.joinerDismissed { icon ◇, title COMPANY, tone dull }"
    - path: "test/unit/dismiss-joiner.test.js"
      provides: "engine, validation, purity, narration/toast/rail proofs"
  key_links:
    - from: "engine/engine.js applyAction switch"
      to: "engine/encounters.js dismissJoiner"
      via: "case 'dismissJoiner': dismissJoiner(next, action.i ?? 0, events)"
      pattern: "dismissJoiner\\(next, action\\.i \\?\\? 0, events\\)"
    - from: "joinerDismissed event"
      to: "rail COMPANY/dull card carrying the parting line"
      via: "NARRATIVE_ACTIONS has dismissJoiner → toastsForAction narrate ctx → railCardFor → RAIL_FAMILY.joinerDismissed"
      pattern: "joinerDismissed"
  prohibitions:
    - "No rng parameter on dismissJoiner; no draw anywhere in this plan"
    - "No edit to mazeworld.html (the Company panel, DISMISS confirm and bridge are Plan 06)"
    - "The parting line is never a toast-only or inline surface — rail card + Oracle line only (v1.4 ruling)"
    - "No checkpoint:human-verify, no device step, no APK build"
---

<objective>
The engine half of JOIN-01: a new no-rng engine action `dismissJoiner` (registered in `engine/actions.js` + `engine/engine.js` beside `resolveJoiner`) that removes the member (by index, default 0) and pushes `joinerDismissed { name, sub }`, refusing with a named reason when the party is empty or in combat; plus the presentation-table half — the sarcastic parting line as its own event with `EVENT_NARRATION` + toast-table + rail entries, so Plan 06's Company-panel DISMISS control has a complete seam to dispatch into and the coverage guards stay green at every commit.

Per CONTEXT §"Cutthroat & Joiner Dismissal" (JOIN-01, locked): new no-rng engine action `dismissJoiner`, registered beside `resolveJoiner`; removes the member (by index, default 0); pushes `joinerDismissed { name, sub }`; refuses with a named reason when the party is empty or in combat; the parting line surfaces on the rail (COMPANY, dull) — never a toast, never inline text.

Assumption surfaced (not in CONTEXT verbatim): "refuses with a named reason" is implemented as a third new event type `dismissRefused { reason: "noParty" | "inCombat" | "badIndex" }` — in this codebase every refusal is an event with a toast-table `block()` entry (FEED-02, Phase 25: nothing happens silently), so a silent return would regress that bar. `dismissJoiner` is also added to `NARRATIVE_ACTIONS` (mirroring `resolveJoiner`, whose `joinerLeft` snark reaches the rail the same way), which requires updating the exact-set pin in `test/unit/narrativeToasts.test.js`.

Purpose: ROADMAP SC-5's engine/narration half; Plan 06 completes the shell.
Output: the action + handler + validation, `JOINER_PARTING_LINES`, the narration/toast/rail entries, `test/unit/dismiss-joiner.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-CONTEXT.md
@engine/encounters.js
@engine/actions.js
@engine/engine.js
@content/flavor.js
@src/browser/eventNarration.js
@src/browser/toasts.js
@src/browser/rail.js
@test/unit/joiner-acquisition.test.js
</context>

<artifacts_this_phase_produces>
Phase 36 as a whole creates the following symbols/files (this plan creates the items marked **[05]**):

- [01] `docs/class-pass/v15-before.json`, `docs/class-pass/v15-before-depth20.json`; `docs/CLASS-PASS.md` `## v1.5 BEFORE — commit … (Phase 36 — BAL-01)` section; additive v1.5 block in `test/unit/class-pass-ledger.test.js`.
- [02] `engine/effects.js`: `startEffect`, `startCooldown`, `tickRounds`, `tickSquares`, `remaining`, `isReady`, `clearRoundTimers`; serialized field `c.timers`; tick-site calls; `clearStaleTimers`; `stripTimersField`; `test/unit/effects.test.js`.
- [03] `engine/combat.js` `normalizeTarget(combat)`; refactored call sites; shell post-dispatch call, dead-card `aria-disabled`, sweep exclusion; `test/unit/normalizeTarget.test.js`, `test/unit/shell-dead-foe-target.test.js`.
- [04] `engine/movement.js` `cutthroatMurderCheck`; event `joinerMurdered { name, sub, depth }`; `meetJoiner` refusal reduced to the Wilmsry clause; `SUB_NOTE.Cutthroat` rewrite + `JOINER_MURDER_LINES`; narration/toast/rail entries; `FEATURE_EVENTS += joinerMurdered`; ledger row; `test/unit/cutthroat-joiner.test.js`.
- **[05]** engine action `dismissJoiner { i? }` (`engine/actions.js` ACTION_TYPES + validate case, `engine/engine.js` dispatch case, `engine/encounters.js#dismissJoiner`); new events `joinerDismissed { name, sub }` and `dismissRefused { reason: "noParty" | "inCombat" | "badIndex" }`; `content/flavor.js` `JOINER_PARTING_LINES`; `EVENT_NARRATION.joinerDismissed` / `.dismissRefused`, `TOAST_FOR.joinerDismissed` / `.dismissRefused`, `RAIL_FAMILY.joinerDismissed`, `FEATURE_EVENTS += dismissRefused`, `NARRATIVE_ACTIONS += dismissJoiner`; `test/unit/dismiss-joiner.test.js`.
- [06] `mazeworld.html`: classic `SUB_NOTE.Cutthroat` synced; `DISMISS_CONFIRM_MS`, `dismissConfirmRevert`, `revertDismissConfirm()`; Company sheet + two-tap `DISMISS` confirm in `renderPartyRoster()`; CSS `.mw-party-line`, `.mw-party-dismiss`; bridge `window.mzDismissJoiner(i)`; `test/unit/shell-company-panel.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The dismissJoiner engine action — handler in encounters.js, ACTION_TYPES + validation in actions.js, dispatch case in engine.js — with the parting/refusal narration, toast, rail and flavor entries landed in the same commit (coverage guards)</name>
  <files>engine/encounters.js, engine/actions.js, engine/engine.js, content/flavor.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/narrativeToasts.test.js, test/voice/safety-scan.test.js, test/unit/dismiss-joiner.test.js</files>
  <read_first>
    - engine/encounters.js lines 493-522 (`resolveJoiner` — the pure no-rng shape, JSDoc style, `state.party` handling via swapPartyMember; the new export goes DIRECTLY after it)
    - engine/actions.js lines 10-58 (ACTION_TYPES with per-entry comments — add `"dismissJoiner",` directly after `"resolveJoiner",`), lines 103-110 (the resolveJoiner validate case — add the dismissJoiner case directly after it; `isInt` helper already exists in the file)
    - engine/engine.js line 24 (`import { resolveJoiner } from "./encounters.js";` — extend), lines 109-114 (the resolveJoiner case — add the new case directly after it)
    - engine/state.js lines 45-96 (PARTY_CAP, party is a plain array; members are full sheets with name/sub/race/cls/weapon)
    - content/flavor.js lines 69-84 (JOINER_EXIT_LINES bank + JSDoc) and the JOINER_MURDER_LINES bank Plan 04 added after it — add JOINER_PARTING_LINES after that
    - src/browser/eventNarration.js lines 36-47 (flavor import + escapeHtml), the joinerLeft/joinerMurdered entries (deterministic index + replaceAll) and lines 586-591 (joinerRefused — the reason-branch shape)
    - src/browser/toasts.js line 47 (PRIORITY), line 75 (`NARRATIVE_ACTIONS = new Set(["move", "camp", "resolveJoiner"])`), lines 196-262 (FEATURE_EVENTS — the `// Refusals` group), line 285 (`block()`), lines 1309-1321 (joiner toast entries + joinerRefused's reason map)
    - src/browser/rail.js lines 134-137 (the joiner RAIL_FAMILY rows incl. Plan 04's joinerMurdered), lines 200-229 (railFamilyFor — a PRIORITY.block entry always falls to NOTHING DOING / dull, so dismissRefused needs NO family row)
    - test/unit/narrativeToasts.test.js lines 55-60 (the exact-set pin on NARRATIVE_ACTIONS — extend to four)
    - test/voice/safety-scan.test.js lines 63, 135-150 (BRANCH_TOGGLES — add the three new reasons), lines 270-280 (the bank push loop)
    - test/unit/joiner-acquisition.test.js lines 130-216 (resolveJoiner test shapes: rng-free assertion via rngState, party assertions) and test/unit/engine-purity.test.js lines 84-108 (validateAction rejection test shape)
    - test/unit/toastsCoverage.test.js lines 139-160 (every engine event type must be toasted or Oracle-only) and test/unit/formatEventsCoverage.test.js (every event narrated; no narration for a never-emitted type)
  </read_first>
  <behavior>
    - dismissJoiner(stateWithOneMember, 0, []) → events [ { type: "joinerDismissed", name, sub } ] with the member's name/sub; state.party deep-equals []; returns the events array
    - dismissJoiner(stateWithOneMember) (i omitted) → same as i = 0
    - dismissJoiner({ party: [] }) → [ { type: "dismissRefused", reason: "noParty" } ], no mutation; party undefined / null / non-array → noParty, no throw, no key injected
    - dismissJoiner(stateWithOneMember with combat set) → [ { type: "dismissRefused", reason: "inCombat" } ], party untouched (the combat check comes first)
    - dismissJoiner(stateWithOneMember, 3) → [ { type: "dismissRefused", reason: "badIndex" } ], party untouched
    - validateAction: { type: "dismissJoiner" } ok; { type: "dismissJoiner", i: 0 } ok; i: -1, i: "0", i: 1.5 → { ok: false, reason: "dismissJoiner.i must be a non-negative integer when present" }
    - applyAction(state, { type: "dismissJoiner" }): returned state.party is [], events carry joinerDismissed, result.state.rngState === state.rngState (zero draws), the input object is untouched (deepStrictEqual to its pre-call clone), and the returned state JSON round-trips
    - applyAction with an empty party returns dismissRefused noParty and an otherwise deepStrictEqual state
    - EVENT_NARRATION.joinerDismissed({ name: "Ada Brook", sub: "Guard" }) contains "Ada Brook"; same input → same line; five distinct names of lengths 1..5 → five distinct lines (every line reachable); "<b>" in a name renders escaped; bare { type } does not throw
    - EVENT_NARRATION.dismissRefused({ reason }) yields three distinct non-empty strings for noParty / inCombat / badIndex and a non-empty fallback for an unknown reason
    - TOAST_FOR.joinerDismissed(e) → priority PRIORITY.feature, tone "beat", text contains the name; TOAST_FOR.dismissRefused(e) → priority PRIORITY.block for every reason; FEATURE_EVENTS includes "dismissRefused"; NARRATIVE_ACTIONS deep-equals the set of camp/dismissJoiner/move/resolveJoiner
    - RAIL_FAMILY.joinerDismissed deep-equals { icon: "◇", title: "COMPANY", tone: "dull" }; RAIL_FAMILY has no dismissRefused key; railCardFor("dismissJoiner", [joinerDismissed], toastsForAction("dismissJoiner", events, { narrate: narrateEvent }, { limit: Infinity, withIdx: true }), { narrate: narrateEvent }) → title "COMPANY", tone "dull", lines[0].text equals the parting sentence with markup stripped; the same for a dismissRefused event → title "NOTHING DOING", tone "dull"
  </behavior>
  <action>
    <!-- planner-discipline-allow: dismissRefused -->
    1. Write `test/unit/dismiss-joiner.test.js` first with the behaviour bullets above (at least 18 tests; a member planted via `swapPartyMember(state, { ...rollCharacter(makeRng(99)), lvl: 2, level: 2, wp: 40, maxWP: 40 })` on `newRun(seed)`). Run — fails.
    2. `engine/encounters.js`: directly after `resolveJoiner`, add `export function dismissJoiner(state, i = 0, events = [])` with a JSDoc: Phase 36 (JOIN-01) — the player sends a party member away from the Hero tab's Company panel (the shell asks for a confirmation first; the engine never assumes one). Pure, NO rng (a plain roster splice, like resolveJoiner/swapPartyMember) so it never shifts the seeded cursor; not part of any parity fixture. Order of checks: `state.combat` truthy → push `dismissRefused` with reason `"inCombat"` and return (a fight in progress keeps its allies — endCombat syncs them); `!Array.isArray(state.party) || state.party.length === 0` → reason `"noParty"`; `!state.party[i]` → reason `"badIndex"`; otherwise `const [gone] = state.party.splice(i, 1);` and push `joinerDismissed` with `name: gone.name`, `sub: gone.sub`. Returns events. Never adds a key to state.
    3. `engine/actions.js`: add `"dismissJoiner",` to ACTION_TYPES directly after `"resolveJoiner",` with a comment (Phase 36 JOIN-01: send a party member away from the Company panel; pure, no rng; optional non-negative integer `i`, default 0). Add a validate case directly after the resolveJoiner case: when `action.i !== undefined` and (`!isInt(action.i) || action.i < 0`) → `{ ok: false, reason: "dismissJoiner.i must be a non-negative integer when present" }`; otherwise fall through to ok.
    4. `engine/engine.js`: extend the import to `import { resolveJoiner, dismissJoiner } from "./encounters.js";` and add `case "dismissJoiner": dismissJoiner(next, action.i ?? 0, events); break;` directly after the resolveJoiner case, with a two-line comment mirroring resolveJoiner's (Phase 36 JOIN-01; pure data mutation, no rng; refuses in combat / with no party / on a bad index).
    5. `content/flavor.js`: add `export const JOINER_PARTING_LINES` after JOINER_MURDER_LINES with a mirroring JSDoc (Phase 36 JOIN-01; `{name}` = the member sent away; picked by eventNarration.js WITHOUT rng from the name length; the Joiner gets the last word; family-friendly; scanned by safety-scan). Exactly these five lines, verbatim:
       - "{name} takes the news well, by which we mean they were already walking away."
       - "{name} thanks you for the opportunity and means none of it."
       - "{name} leaves at once, which is the first quick decision anyone has made down here."
       - "{name} wishes you the best. {name} has met the best. The two are not expected to overlap."
       - "{name} goes without a word, having saved several for the tavern."
    6. `src/browser/eventNarration.js`: extend the flavor import with `JOINER_PARTING_LINES`; add `joinerDismissed: (e) => { … }` after `joinerMurdered`: `rawName = e.name ?? "Your companion"`, `idx = String(rawName).length % JOINER_PARTING_LINES.length`, template `{name}` → `escapeHtml(rawName)` via replaceAll, inside `<span class="beat">…</span>`. Add `dismissRefused: (e) => …` after it: reason `noParty` → `<span class="beat">There is nobody to send away. You checked twice.</span>`; `inCombat` → `<span class="beat">Not in the middle of a fight. There are manners, even here.</span>`; anything else (badIndex / unknown) → `<span class="beat">Nobody answers to that number.</span>`.
    7. `src/browser/toasts.js`: `NARRATIVE_ACTIONS = new Set(["move", "camp", "resolveJoiner", "dismissJoiner"])` (update its JSDoc list); add `joinerDismissed: (e) => ({ text: `${e?.name ?? "Your companion"} walks. The slot is yours again.`, tone: "beat", priority: PRIORITY.feature })` after joinerMurdered (comment: fallback/coverage text — on the dismissJoiner action the narrative ctx replaces it with the EVENT_NARRATION parting line); add `dismissRefused: (e) => { const map = { noParty: "Nobody is travelling with you.", inCombat: "Not in the middle of a fight.", badIndex: "Nobody by that count." }; return block(map[e?.reason] ?? "Nobody to dismiss."); }` after it; add `"dismissRefused",` to FEATURE_EVENTS in the `// Refusals` group (after `"campFailed",`).
    8. `src/browser/rail.js`: add `joinerDismissed: { icon: "◇", title: "COMPANY", tone: "dull" },` after the joinerMurdered row (no dismissRefused row — block fallback).
    9. `test/unit/narrativeToasts.test.js` lines 55-60: title "NARRATIVE_ACTIONS is exactly move + camp + resolveJoiner + dismissJoiner" and expected `["camp", "dismissJoiner", "move", "resolveJoiner"]`. `test/voice/safety-scan.test.js`: import + push `JOINER_PARTING_LINES` (beside the murder bank) and add `{ reason: "noParty" }, { reason: "inCombat" }, { reason: "badIndex" },` to BRANCH_TOGGLES.
    10. `node --test test/unit/dismiss-joiner.test.js test/unit/narrativeToasts.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/rail.test.js test/unit/engine-purity.test.js test/unit/joiner-acquisition.test.js` → green; `npm test` → `# fail 0`.
  </action>
  <verify>
    <automated>node --test test/unit/dismiss-joiner.test.js test/unit/narrativeToasts.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/rail.test.js test/unit/engine-purity.test.js && test "$(grep -c '^export function dismissJoiner(' engine/encounters.js)" = "1" && test "$(grep -c 'dismissJoiner(next, action.i ?? 0, events);' engine/engine.js)" = "1" && test "$(grep -c '"dismissJoiner",' engine/actions.js)" = "1" && test "$(grep -c '^export const JOINER_PARTING_LINES' content/flavor.js)" = "1" && npm test 2>&1 | grep -q "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function dismissJoiner(" engine/encounters.js` prints 1 and (via `grep -n`) its line is greater than `export function resolveJoiner`'s; `grep -c 'reason: "inCombat"' engine/encounters.js` >= 1; `grep -c 'reason: "noParty"' engine/encounters.js` >= 1; `grep -c 'reason: "badIndex"' engine/encounters.js` >= 1; `grep -c "state.party.splice(i, 1)" engine/encounters.js` >= 1; `grep -c "rng" engine/encounters.js` unchanged vs before the edit (dismissJoiner takes no rng).
    - `grep -c '"dismissJoiner",' engine/actions.js` prints 1; `grep -c 'dismissJoiner.i must be a non-negative integer when present' engine/actions.js` prints 1.
    - `grep -c 'import { resolveJoiner, dismissJoiner } from "./encounters.js";' engine/engine.js` prints 1; `grep -c 'case "dismissJoiner":' engine/engine.js` prints 1.
    - `node -e "import('./content/flavor.js').then(m=>console.log(m.JOINER_PARTING_LINES.length, m.JOINER_PARTING_LINES.every(s=>s.includes('{name}'))))"` prints `5 true`.
    - `node -e "import('./src/browser/toasts.js').then(m=>console.log([...m.NARRATIVE_ACTIONS].sort().join(','), m.FEATURE_EVENTS.includes('dismissRefused'), typeof m.TOAST_FOR.joinerDismissed, typeof m.TOAST_FOR.dismissRefused))"` prints `camp,dismissJoiner,move,resolveJoiner true function function`.
    - `grep -c 'joinerDismissed: { icon: "◇", title: "COMPANY", tone: "dull" }' src/browser/rail.js` prints 1; `node -e "import('./src/browser/rail.js').then(m=>console.log(Object.prototype.hasOwnProperty.call(m.RAIL_FAMILY,'dismissRefused')))"` prints `false` (refusals fall to the block fallback; no family row).
    - `grep -c "JOINER_PARTING_LINES" test/voice/safety-scan.test.js` prints 2; `grep -c 'reason: "noParty"' test/voice/safety-scan.test.js` prints 1.
    - `node --test test/unit/dismiss-joiner.test.js` exits 0 with at least 18 tests; `npm test` prints `# fail 0`; `git status --porcelain test/parity/fixtures` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat -- mazeworld.html package.json package-lock.json` prints nothing.
  </acceptance_criteria>
  <done>dismissJoiner is a validated, dispatched, pure engine action with named refusals, and its parting line reaches the Oracle, the toast table and the rail as its own event — every guard green.</done>
</task>

<task type="auto">
  <name>Task 2: Gate + SUMMARY (rng/serialized-field statements, the surfaced assumption, deferred on-device checks)</name>
  <files>content/flavor.js</files>
  <read_first>
    - .planning/ROADMAP.md Phase 36 Success Criterion 5
    - .planning/milestones/v1.4-phases/35-map-screen-rebuild/35-01-SUMMARY.md (deferred-verification section shape)
  </read_first>
  <action>
    1. Record: `npm test` totals; `git diff --stat -- test/parity mazeworld.html package.json package-lock.json` (empty); master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat v1.4.0 -- engine content src` (cumulative Plans 02–05 files).
    2. State: rng-draw impact "adds zero draws"; serialized-field impact "none (party splice on an existing field)".
    3. Record under `## Assumptions surfaced`: the third event type `dismissRefused { reason }` (named-reason refusals are events in this codebase — FEED-02) and the `NARRATIVE_ACTIONS` extension with its pin update.
    4. `## Human verification (deferred to end of run)` — these checks need Plan 06's panel; list them here as engine-side expectations the Plan 06 checklist exercises: (1) after a DISMISS confirm the rail shows a COMPANY card in the dull tone with one of the five parting lines and the Oracle log carries the same line; (2) the Company panel is empty afterwards and the next Joiner met can be accepted without a swap line; (3) no toast or inline text appears anywhere for the parting line.
    5. No file changes expected in this task; if a gate item fails, fix within this plan and re-run.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "^# fail 0" && test -z "$(git diff --stat -- test/parity mazeworld.html package.json package-lock.json)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; `git diff --stat -- test/parity mazeworld.html package.json package-lock.json` prints nothing; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    - 36-05-SUMMARY.md contains `## Assumptions surfaced`, `## Human verification (deferred to end of run)` (at least 3 numbered checks), the phrase `adds zero draws`, and the string `dismissRefused`.
  </acceptance_criteria>
  <done>Gate green and recorded; the assumption is on the record; the device checks are queued.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presentation/peer → applyAction | untrusted `dismissJoiner` payload (`i`) |
| engine event → presentation markup | member name interpolated into narration HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-12 | Tampering | actions.js dismissJoiner.i | medium | mitigate | validateAction rejects non-integer/negative i; handler refuses badIndex; applyAction no-ops on invalid input (V5) |
| T-36-13 | Denial of Service | dismissJoiner in combat | low | mitigate | inCombat refusal first — a live fight's allies are never spliced under it |
| T-36-14 | Information Disclosure / XSS | EVENT_NARRATION.joinerDismissed | medium | mitigate | escapeHtml on the name; tag-in-name test |
| T-36-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `npm test` `# fail 0`; fixtures untouched; master hash unchanged; no shell/package change from this plan.
- rng-draw impact: zero. Serialized-field impact: none.
</verification>

<success_criteria>
- ROADMAP SC-5 engine/narration half: on confirm the Joiner leaves with a sarcastic parting line of their own (new event type, no rng) and the party slot is free; refusals are named.
</success_criteria>

<output>
Create `.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-05-SUMMARY.md` when done — gate record, `## Assumptions surfaced`, rng/serialized-field statements, `## Human verification (deferred to end of run)`.
</output>
