---
phase: 36-balance-foundation-effect-timers-small-independent-wins
verified: 2026-09-17T17:40:00Z
status: passed
score: 6/6 success criteria verified on automated evidence; every player-facing check (targeting, Cutthroat offer/murder, Company sheet + DISMISS) is deferred to the end-of-run Pixel 7 batch per the user's "defer uat to end" instruction
behavior_unverified: 0
overrides_applied: 0
human_verification: ["Pixel 7 (TGT-01): start a fight with two or more foes and kill the currently-targeted one (own blow, Joiner blow, item/spell kill) — the TARGET tag jumps to the next living foe's card before anything else can be tapped", "Pixel 7 (TGT-02): tap the downed (greyed) card repeatedly — nothing happens: no target change, no log line, no armed feedback", "Pixel 7 (TGT-02, race): immediately after a kill, spam-tap a live non-target card within ~250 ms — either the tap is swallowed or the tapped card becomes the target; a strike never lands on a foe you did not choose", "Pixel 7 (TGT-01, sources): a Joiner's blow or a Freeze that kills the current target also moves the TARGET tag to a survivor", "Pixel 7 (TGT-02, a11y, optional): with TalkBack on, the downed card reads as disabled", "Pixel 7 (CUT-01): roll a Cutthroat and walk until a Joiner is met — the rail shows the COMPANY offer card (TAKE THEM ALONG / LEAVE THEM), not a refusal", "Pixel 7 (CUT-01): accept the Joiner, open the Hero tab — the Company panel lists them", "Pixel 7 (CUT-02): descend floor after floor until the murder line fires (1-in-20 per descent; the dev start-depth harness can speed floors) — a COMPANY card in the bad tone with one of the six lines, the same line in the Oracle log, and the Company panel is empty afterwards", "Pixel 7 (CUT-01, regression): as a Wilmsry meeting a Magic User Joiner, the refusal still reads exactly as before", "Pixel 7 (CUT-01, blurb): the Cutthroat dossier blurb on the Hero tab states 'one descent in twenty'", "Pixel 7 (JOIN-01, rail): after a DISMISS confirm, the rail shows a COMPANY card (dull tone) with one of the five parting lines and the Oracle log carries the same line — never a toast, never inline text", "Pixel 7 (JOIN-01, roster): the Company panel is empty afterwards and the next Joiner met can be accepted without a swap line", "Pixel 7 (JOIN-01, sheet): accept any Joiner, open the Hero tab — the card shows name, sub / race, class · level, an HP bar labelled HP, 'Weapon: <name>', 'Eats N a rest', and a DISMISS button", "Pixel 7 (JOIN-01, confirm): tap DISMISS — it becomes 'Send them off? [Yes] [No]'; No reverts; waiting ~3 s reverts; tapping anywhere else reverts", "Pixel 7 (JOIN-01, confirm → Yes): the card disappears, the panel hides if it was the last member, the rail shows the COMPANY card and no text appears inside the panel", "Pixel 7 (JOIN-01, combat): during a fight, open the Hero tab — no DISMISS button on the card", "Pixel 7 (JOIN-01, rations): a Troll Joiner's card says 'Eats 2 a rest'", "Pixel 7 (SC-6, regression): a normal run and a pre-Phase-36 save look, sound and play identically to 1.4.0 outside the four features above (effects.js has no player-visible behaviour)"]
gaps: []
---

# Phase 36 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-17)

Goal-backward check of the phase goal: *before any new player power lands in v1.5, the class-matrix BEFORE snapshot is pinned; `engine/effects.js` exists as the one timer shape for Phases 38–40 with no player-visible behaviour yet; a dead foe can never be targeted; a Cutthroat can accept a Joiner with a small, stated murder risk on descent; any hero can dismiss a Joiner from the Company panel with a confirmation and a parting line.*

## Automated evidence (re-run by the orchestrator after 36-06)

- `npm test`: **2276/2276, 0 failures** (2179 at phase start → 2182 after 36-01 → 2206 after 36-02 → 2226 after 36-03 → 2244 after 36-04 → 2264 after 36-05 → 2276 after 36-06). `npm run build:www` exit 0 (36-03 and 36-06, the two plans that touched `mazeworld.html`).
- Engine gate: `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (unchanged); `git status --porcelain test/parity/fixtures` empty — no fixture regenerated, no new declared divergence. Cumulative `git diff --stat v1.4.0 -- engine content src mazeworld.html` = exactly 13 files (`engine/{effects,movement,combat,saveState,magic,encounters,actions,engine}.js`, `content/flavor.js`, `src/browser/{eventNarration,toasts,rail}.js`, `mazeworld.html`), 588+/38−.
- BEFORE pin taken at `e69ff07` with `git diff --quiet v1.4.0 -- engine content src mazeworld.html` exiting 0 before, during and after both matrix runs — the yardstick is byte-identical to the shipped 1.4.0 engine.
- New rng draws: exactly one — the d20 in `cutthroatMurderCheck`, the last statement of `descend()`, gated on `c.sub === "Cutthroat" && party.length > 0` (false for every fixture incl. lose-plain seed 1119, every bot run, every pre-Phase-36 save). `dismissJoiner` and `normalizeTarget` draw nothing. New serialized field: `c.timers` only, lazily created, never created by any caller this phase (`startEffect`/`startCooldown` grep hits only `engine/effects.js`), carved out of all three `*Comparable()` fns + the three local duplicates, load-tolerant.
- Coverage guards: `toastsCoverage.test.js` / `formatEventsCoverage.test.js` green with the three new event types (`joinerMurdered`, `joinerDismissed`, `dismissRefused`); `test/voice/safety-scan.test.js` green over the 6 murder lines, 5 parting lines and the new blurb.
- Six plans, six SUMMARY.md files, none carrying `## Self-Check: FAILED`; working tree clean at HEAD `d7ae15c` apart from two untracked user directories outside the phase (`store-listing/`, `tools/store-screenshots/`).

## Success criteria → evidence

| # | Success criterion (ROADMAP) | Evidence |
|---|-----------------------------|----------|
| 1 | Committed BEFORE class-matrix pin, timestamped before any other v1.5 change; `tune-classes.mjs` runs cleanly | `docs/class-pass/v15-before.json` (143×40) + `v15-before-depth20.json` (143×10), `meta.commit e69ff07`, `Bot:` lines byte-identical to the v1.2 pair, 0 stuck, cannot-act gate 0/143; `## v1.5 BEFORE` section in `docs/CLASS-PASS.md`; `class-pass-ledger.test.js` extended 8→9 headings (36-01, commit `b274df6`, before any engine byte moved). |
| 2 | Killing a foe immediately makes the next living foe the target; a downed card does nothing; no race into a mis-target | `normalizeTarget(combat)` exported from `engine/combat.js`, `playerStrike`/`castSpell` refactored onto it (draw-count identity test); shell `engineCombatAction` calls it after the state swap, before `renderEncounter()`; dead cards `aria-disabled="true"`, no role/tabIndex/handler, excluded from the 250 ms arm sweep; `normalizeTarget.test.js` 10 + `shell-dead-foe-target.test.js` 10 (DOM-free arm-window race simulation). `combat.target` byte-identical on every engine path. |
| 3 | A Cutthroat sees an acceptance offer; the blurb matches | Refusal clause removed from `meetJoiner`'s ternary (DELIBERATE RULES CHANGE comment; Wilmsry refusal kept); `SUB_NOTE.Cutthroat` in `content/flavor.js` rewritten and the classic-script copy in `mazeworld.html` synced byte-for-byte; identity-contract BAD re-pointed at the murder risk; `docs/CLASS-PASS.md` row updated; dead `cutthroat` refusal copy removed (`cutthroat-joiner.test.js` 18). |
| 4 | Each descent with a Joiner carries a small, stated murder chance with its own event type and several lines | `cutthroatMurderCheck` (natural 1 on d20, last in `descend()`), `joinerMurdered {name, sub, depth}`, `JOINER_MURDER_LINES` ×6 chosen deterministically, narration/toast/rail (COMPANY, bad) entries; blurb states "one descent in twenty". |
| 5 | Company panel shows the Joiner's sheet + DISMISS with confirmation and a parting line; slot freed | `dismissJoiner` action (actions.js validate + engine.js dispatch; refusals `inCombat`/`noParty`/`badIndex` as `dismissRefused`), `joinerDismissed {name, sub}`, `JOINER_PARTING_LINES` ×5, rail (COMPANY, dull); `renderPartyRoster()` sheet (name · sub/race · class · level, HP track, `Weapon: X`, `Eats N a rest`), two-tap `Send them off? [Yes] [No]` with `DISMISS_CONFIRM_MS`, `window.mzDismissJoiner` via `dispatchWithToasts`; `dismiss-joiner.test.js` 20 + `shell-company-panel.test.js` 12. |
| 6 | `npm test` green, engine/content/parity diff empty of divergences; `engine/effects.js` has no player-visible behaviour | 2276/2276; master hash unchanged; fixtures untouched; `effects.js` seven pure functions + three guarded tick sites (`tickSquares` per step, `tickRounds` in `foeTurn`'s tail, `clearRoundTimers` at `endCombat`) + `clearStaleTimers` on load; zero `start*` callers (`effects.test.js` 24). |

## Requirements

BAL-01 ✓ (pin half; "teach the bot the new abilities" is Phase 42's AFTER-run prep per CONTEXT) · TGT-01 ✓ · TGT-02 ✓ · CUT-01 ✓ · CUT-02 ✓ · JOIN-01 ✓ — automated halves verified; on-device halves in the frontmatter `human_verification` list (18 items, aggregated from the six SUMMARYs' "Human verification (deferred to end of run)" sections).

## Follow-ups noted by the executors (not gaps)

- 13 other classic-script `SUB_NOTE` rows in `mazeworld.html` are drifted from `content/flavor.js` (pre-existing since Phases 23/24) — Phase 43 Clarity candidate or a quick task.
- The `.planning/STATE.md` "execution started" marker was committed separately (`e69ff07`) so Plan 01's literal clean-tree preflight could pass.
