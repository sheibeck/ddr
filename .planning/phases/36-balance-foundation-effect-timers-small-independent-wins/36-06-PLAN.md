---
phase: 36-balance-foundation-effect-timers-small-independent-wins
plan: 06
type: execute
wave: 5
depends_on: ["36-03", "36-05"]
files_modified:
  - mazeworld.html
  - test/unit/shell-company-panel.test.js
autonomous: true
requirements: [JOIN-01, CUT-01]
tags: [shell, hero-tab, company-panel, two-tap-confirm, bridge, sub-note-sync, phase-close, deferred-uat]
notes:
  wave_rationale: "wave 5: Plan 03 edits mazeworld.html (same file) and Plan 05 lands the dismissJoiner action this plan's bridge dispatches"
  rng_draw_impact: "adds zero draws — shell only; the bridge dispatches the pure dismissJoiner action"
  serialized_field_impact: "none — DISMISS_CONFIRM_MS/dismissConfirmRevert are DOM-local presentation state, never on S (serializeRun spreads S)"

must_haves:
  truths:
    - "renderPartyRoster() renders, per member, in this order: name · sub / race · class · level, then the HP label + track (the word HP, never the two-letter W-P token as player-facing text), then 'Weapon: <m.weapon>', then 'Eats <RACES[m.race]?.eats || 1> a rest', then (outside combat) a DISMISS button (ROADMAP SC-5, JOIN-01)"
    - "Tapping DISMISS arms an inline 'Send them off?' [Yes] [No] confirm (the Phase 33 .mw-drop-confirm pattern: DISMISS_CONFIRM_MS = 3000 timeout, outside pointerdown, No → revert; only one row armed at a time); Yes reverts then calls window.mzDismissJoiner(idx) (JOIN-01 'needs a confirmation tap')"
    - "window.mzDismissJoiner(i) dispatches { type: 'dismissJoiner', i } through the same dispatchWithToasts seam as mzResolveJoiner, then sets state, logs, paints and re-renders — so the parting line reaches the rail as a COMPANY/dull card and the Oracle log, never a toast, never inline text (JOIN-01)"
    - "The classic script's SUB_NOTE.Cutthroat (the string the Hero-tab dossier actually renders) is byte-identical to content/flavor.js SUB_NOTE.Cutthroat and the prototype-era sentence about a party member dying by your hand is gone from mazeworld.html (CUT-01 'blurb rewritten to match', ROADMAP SC-3)"
    - "Every existing renderPartyRoster pin still holds: exactly 2 getElementById calls in the region, panel.hidden = party.length === 0, mw-map-hptrack, mw-map-hpfill, Downed; the <style> block still never references aria-disabled; npm run build:www exits 0; npm test '# fail 0'"
    - "36-06-SUMMARY.md aggregates every plan's deferred on-device checks (36-01..36-06) into one grouped, numbered Pixel 7 checklist (the phase's deliverable for the milestone-close UAT batch)"
  artifacts:
    - path: "mazeworld.html"
      provides: "synced SUB_NOTE.Cutthroat; DISMISS_CONFIRM_MS / dismissConfirmRevert / revertDismissConfirm(); the Company sheet + DISMISS confirm in renderPartyRoster(); .mw-party-line / .mw-party-dismiss CSS; window.mzDismissJoiner bridge"
    - path: "test/unit/shell-company-panel.test.js"
      provides: "source pins for the sheet order, HP wording, confirm mechanics, bridge shape, SUB_NOTE sync, existing-pin preservation"
  key_links:
    - from: "renderPartyRoster DISMISS → Yes"
      to: "window.mzDismissJoiner(idx)"
      via: "revertDismissConfirm(); window.mzDismissJoiner?.(idx);"
      pattern: "window\\.mzDismissJoiner\\?\\.\\("
    - from: "window.mzDismissJoiner"
      to: "engine dismissJoiner action"
      via: "dispatchWithToasts({ type: \"dismissJoiner\", i })"
      pattern: "dispatchWithToasts\\(\\{ type: \"dismissJoiner\""
  prohibitions:
    - "No engine/content/src edit in this plan; no new package; no Google Fonts"
    - "The parting line must never be rendered inside the Company panel (no reference to JOINER_PARTING_LINES / joinerDismissed inside renderPartyRoster) and never as a toast"
    - "No CSS rule referencing aria-disabled; the confirm revert is a setTimeout, never a CSS transition"
    - "No new getElementById call inside renderPartyRoster (the shell-party-camp pin counts exactly two)"
    - "No checkpoint:human-verify, no device step, no APK build (the milestone's APK is built at milestone close, not here)"
---

<objective>
The shell half of JOIN-01 and the last CUT-01 sync: the Hero tab's Company panel shows the Joiner's sheet (class/sub/race, HP, weapon, "eats N a rest") and a DISMISS control that asks for confirmation with the Gear tab's two-tap inline pattern; on confirm the bridge dispatches `dismissJoiner` through the same `dispatchWithToasts` seam as `mzResolveJoiner`, and the parting line surfaces on the rail (COMPANY, dull) — never a toast, never inline text. The classic script's own `SUB_NOTE` table (the copy the dossier actually renders — `mazeworld.html:2386-2410`, which has drifted from `content/flavor.js` since Phase 24) gets the Cutthroat blurb synced byte-for-byte.

Per CONTEXT §"Cutthroat & Joiner Dismissal" (JOIN-01 shell, locked): `renderPartyRoster()` Company card gains the sheet — class/sub/race, HP (never "WP"), weapon, "eats N a rest" (`RACES[m.race]?.eats || 1`) — and a DISMISS button using the Gear tab's two-tap inline confirm (Phase 33 `mw-drop-confirm` precedent: arm → YES/NO → timeout/outside-tap revert, one row armed at a time); on confirm the bridge dispatches `dismissJoiner` through `dispatchWithToasts`; the parting line is rail-only. CONTEXT §Specifics: Company sheet line order name · sub/race · class · level, then HP track, then "Weapon: X" and "Eats N a rest", then DISMISS.

Discretion resolved: the DISMISS control is rendered only when `!S.combat` (the engine's inCombat refusal stays as the backstop — a stale panel can never splice a fighting ally); the classic-script `RACES` const (mazeworld.html:1970, the prototype's own table) carries the same `eats` field as content/races.js and is what `renderPartyRoster` (classic scope) reads.

Purpose: ROADMAP SC-5 complete; SC-3's blurb visible on the device.
Output: the shell edits, `test/unit/shell-company-panel.test.js`, the phase's aggregated deferred checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-CONTEXT.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-05-SUMMARY.md
@content/flavor.js
@src/browser/rail.js
@test/unit/shell-party-camp.test.js
@test/unit/shell-gear-toolbar.test.js
</context>

<artifacts_this_phase_produces>
Phase 36 as a whole creates the following symbols/files (this plan creates the items marked **[06]**):

- [01] `docs/class-pass/v15-before.json`, `docs/class-pass/v15-before-depth20.json`; `docs/CLASS-PASS.md` `## v1.5 BEFORE — commit … (Phase 36 — BAL-01)` section; additive v1.5 block in `test/unit/class-pass-ledger.test.js`.
- [02] `engine/effects.js`: `startEffect`, `startCooldown`, `tickRounds`, `tickSquares`, `remaining`, `isReady`, `clearRoundTimers`; serialized field `c.timers`; tick-site calls; `clearStaleTimers`; `stripTimersField`; `test/unit/effects.test.js`.
- [03] `engine/combat.js` `normalizeTarget(combat)`; refactored call sites; shell post-dispatch call, dead-card `aria-disabled`, sweep exclusion; `test/unit/normalizeTarget.test.js`, `test/unit/shell-dead-foe-target.test.js`.
- [04] `engine/movement.js` `cutthroatMurderCheck`; event `joinerMurdered { name, sub, depth }`; `meetJoiner` refusal reduced to the Wilmsry clause; `SUB_NOTE.Cutthroat` rewrite + `JOINER_MURDER_LINES`; narration/toast/rail entries; `FEATURE_EVENTS += joinerMurdered`; ledger row; `test/unit/cutthroat-joiner.test.js`.
- [05] engine action `dismissJoiner { i? }`; events `joinerDismissed { name, sub }`, `dismissRefused { reason }`; `JOINER_PARTING_LINES`; narration/toast/rail entries; `FEATURE_EVENTS += dismissRefused`; `NARRATIVE_ACTIONS += dismissJoiner`; `test/unit/dismiss-joiner.test.js`.
- **[06]** `mazeworld.html`: classic-script `SUB_NOTE.Cutthroat` synced to `content/flavor.js`; `DISMISS_CONFIRM_MS`, `dismissConfirmRevert`, `revertDismissConfirm()`, `escText()` helper; the Company sheet (name · sub / race · class · level, HP track, `Weapon: X`, `Eats N a rest`) and the two-tap `DISMISS` confirm inside `renderPartyRoster()`; CSS `.mw-party-line`, `.mw-party-dismiss`; bridge `window.mzDismissJoiner(i)`; `test/unit/shell-company-panel.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: mazeworld.html — SUB_NOTE sync, the Company sheet, the two-tap DISMISS confirm, the CSS, and the mzDismissJoiner bridge</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html lines 2386-2410 (the classic `const SUB_NOTE = {` table — the Cutthroat row still carries the prototype-era sentence; line 3248 is the dossier read `SUB_NOTE[c.sub]`), content/flavor.js line 52 (the Plan 04 blurb — copy it byte-for-byte)
    - mazeworld.html lines 5140-5172 (`renderPartyRoster()` — the current card template; keep `panel.hidden = party.length === 0;`, the `mw-map-hptrack`/`mw-map-hpfill` track, the `Downed` chip and EXACTLY two `getElementById(` calls; every closing brace inside the function stays indented — the shell-party-camp pin's region ends at the first column-0 `}` after the function head)
    - mazeworld.html lines 3662-3673 (the Drop confirm's module-level trio `DROP_CONFIRM_MS` / `dropConfirmRevert` / `revertDropConfirm()` and its comment) and lines 3714-3742 (`mkDropConfirm`: arm → `span.mw-drop-confirm` + label + Yes/No → replaceWith → setTimeout revert → capture-phase pointerdown outside → revert closure) — mirror with the DISMISS names; note `mkBtn` is local to renderCarriedList, so build buttons with document.createElement here
    - mazeworld.html lines 902-909 (the `.mw-party-*` CSS rules — add the two new rules after `.mw-party-hp-lab b`), line 234 (`.mw-drop-confirm` rule — reused as-is)
    - mazeworld.html lines 1970-1985 (the classic `RACES` table with `eats`), line 1744 (`ROMAN`)
    - mazeworld.html lines 7566-7585 (`window.mzResolveJoiner` — the bridge shape to mirror: dispatchWithToasts → `window.__mzState.set(state)` → logLine loop → `window.paint()` → `window.renderEncounter()`), lines 7108-7134 (dispatchWithToasts: out of combat the folded lines become a rail card via railCardFor/railPush — nothing else to wire)
    - test/unit/shell-party-camp.test.js lines 112-146 (the pins this edit must keep green) and test/unit/shell-gear-toolbar.test.js lines 78-100 (the Drop-confirm pins are region-scoped to renderCarriedList — the DISMISS confirm must live in renderPartyRoster's region, not there)
    - src/browser/rail.js RAIL_FAMILY.joinerDismissed (Plan 05) — nothing to change; the card renders through the existing renderRail path
  </read_first>
  <action>
    1. SUB_NOTE sync: replace the classic table's `"Cutthroat":` value (mazeworld.html ~line 2399) with the exact string of `content/flavor.js` `SUB_NOTE.Cutthroat` (Plan 04's text beginning "Double damage on your first landed blow, and Joiners will walk beside you all the same"). Only that row changes (the other rows' drift from flavor.js is out of scope — note it in the SUMMARY as a follow-up candidate).
    2. CSS: after the `.mw-party-hp-lab b{…}` rule add `.mw-party-line{font-family:var(--mono);font-size:9px;letter-spacing:.04em;text-transform:uppercase;color:var(--ditto)}` and `.mw-party-dismiss{margin-top:4px;align-self:flex-start}`. No aria-disabled selector, no transition.
    3. Directly above `function renderPartyRoster()` add the module-level trio with a comment mirroring the Drop confirm's (Phase 36 JOIN-01 — DOM-local presentation state, never on S; one row armed at a time; setTimeout revert, never a CSS transition; not an encounter button, so no guardTap by design): `const DISMISS_CONFIRM_MS = 3000;`, `let dismissConfirmRevert = null;`, `function revertDismissConfirm() { if (!dismissConfirmRevert) return; const r = dismissConfirmRevert; dismissConfirmRevert = null; r(); }`, plus `const escText = (s) => String(s ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");` (names/subs/weapons are interpolated into innerHTML — escape them; the module's eventNarration.js escapes the same way).
    4. `renderPartyRoster()` — keep its head (panel/host lookups, `party`, `panel.hidden = party.length === 0;`, `host.innerHTML = ""`, the early return). Change `party.forEach((m) =>` to `party.forEach((m, idx) =>`. The card template becomes, in this exact order:
       - `<div class="mw-party-head"><span class="mw-party-name">${escText(m.name || "Companion")}</span><span class="mw-party-sub">${escText(m.sub || "")}${m.sub && m.race ? " / " : ""}${escText(m.race || "")}</span></div>`
       - `<div class="mw-party-line">${escText(m.cls || "—")} · ${ROMAN[lvl - 1] || lvl}</div>`
       - the existing HP label line (`HP <b>${wp}</b>/<b>${maxWP}</b>`) and the existing `mw-map-hptrack` / `mw-map-hpfill` track, unchanged
       - the existing Downed chip, unchanged
       - `<div class="mw-party-line">Weapon: ${escText(m.weapon || "—")}</div>`
       - `<div class="mw-party-line">Eats ${RACES[m.race]?.eats || 1} a rest</div>`
       Then, after `card.innerHTML = …` and only when `!S.combat`: build `const dismiss = document.createElement("button"); dismiss.className = "small mw-party-dismiss"; dismiss.textContent = "DISMISS";` and `dismiss.onclick = arm;` where `arm` (a const arrow defined inside the forEach callback): `revertDismissConfirm();` → `const wrap = document.createElement("span"); wrap.className = "mw-drop-confirm";` → a label span with textContent `Send them off?` → `yes`/`no` buttons via document.createElement("button") with className "small" and textContent "Yes"/"No"; `yes.onclick = () => { revertDismissConfirm(); window.mzDismissJoiner?.(idx); };` `no.onclick = () => revertDismissConfirm();` → append label/yes/no → `dismiss.replaceWith(wrap);` → `const timer = setTimeout(revertDismissConfirm, DISMISS_CONFIRM_MS);` → `const onAnyTap = (e) => { if (!wrap.contains(e.target)) revertDismissConfirm(); }; document.addEventListener("pointerdown", onAnyTap, true);` → `dismissConfirmRevert = () => { clearTimeout(timer); document.removeEventListener("pointerdown", onAnyTap, true); if (wrap.isConnected) wrap.replaceWith(dismiss); };`. Append `dismiss` to the card. Player-facing text in this function says HP; the two-letter W-P token must not appear as visible text (field names like maxWP are fine).
    5. Bridge — in the module script directly after `window.mzResolveJoiner = … };` add `window.mzDismissJoiner = function dismissJoinerBridge(i) { … }` mirroring it exactly: `const { state, html } = dispatchWithToasts({ type: "dismissJoiner", i: Number.isInteger(i) && i >= 0 ? i : 0 });` → `window.__mzState.set(state);` → `for (const line of html) window.logLine(line);` → `window.paint();` → `window.renderEncounter();`. Comment: Phase 36 (JOIN-01) — the Company panel's DISMISS confirm routes through the SAME dispatch()->applyAction() seam as mzResolveJoiner; the engine owns the roster splice and the named refusals (dismissJoiner draws zero rng); dispatchWithToasts already turns the joinerDismissed line into the rail's COMPANY card (dull) out of combat; paint() re-runs renderPartyRoster() so the panel empties.
    6. `npm run build:www` → exit 0; `node --test test/unit/shell-party-camp.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-invariants.test.js test/unit/shell-input-guards.test.js test/unit/shell-dead-foe-target.test.js` → green.
  </action>
  <verify>
    <automated>npm run build:www && node --test test/unit/shell-party-camp.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-invariants.test.js test/unit/shell-input-guards.test.js test/unit/shell-dead-foe-target.test.js && test "$(grep -c 'const DISMISS_CONFIRM_MS = 3000;' mazeworld.html)" = "1" && test "$(grep -c 'function revertDismissConfirm()' mazeworld.html)" = "1" && test "$(grep -c 'window.mzDismissJoiner = function dismissJoinerBridge(' mazeworld.html)" = "1" && test "$(grep -c 'dispatchWithToasts({ type: "dismissJoiner"' mazeworld.html)" = "1" && test "$(grep -c 'one member of every party dies by your hand' mazeworld.html)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const h=fs.readFileSync('mazeworld.html','utf8');const m=h.match(/\"Cutthroat\":\s*\"([^\"]*)\"/);import('./content/flavor.js').then(f=>console.log(m&&m[1]===f.SUB_NOTE.Cutthroat))"` prints `true`; `grep -c "one member of every party dies by your hand" mazeworld.html` prints 0 (the rules-notes `<section class="notes" hidden>` sentence at line ~1602 uses different wording and is untouched).
    - `grep -c 'const DISMISS_CONFIRM_MS = 3000;' mazeworld.html` = 1; `grep -c 'let dismissConfirmRevert = null;' mazeworld.html` = 1; `grep -c 'function revertDismissConfirm()' mazeworld.html` = 1; all three appear at line numbers smaller than `function renderPartyRoster()`'s.
    - Inside the renderPartyRoster region (from `function renderPartyRoster() {` to the next column-0 `}`): `grep`-visible strings `Weapon: `, ` a rest`, `RACES[m.race]?.eats || 1`, `DISMISS`, `Send them off?`, `className = "mw-drop-confirm"`, `setTimeout(revertDismissConfirm, DISMISS_CONFIRM_MS)`, `document.addEventListener("pointerdown", onAnyTap, true)`, `window.mzDismissJoiner?.(idx)`, `!S.combat`, `panel.hidden = party.length === 0;`, `mw-map-hptrack`, `mw-map-hpfill`, `Downed` each occur at least once; `getElementById(` occurs exactly twice.
    - `grep -c 'window.mzDismissJoiner = function dismissJoinerBridge(' mazeworld.html` = 1; `grep -c 'dispatchWithToasts({ type: "dismissJoiner"' mazeworld.html` = 1.
    - `grep -c '^\.mw-party-line{' mazeworld.html` = 1; `grep -c '^\.mw-party-dismiss{' mazeworld.html` = 1.
    - `npm run build:www` exits 0; the five listed shell suites exit 0.
  </acceptance_criteria>
  <done>The Company panel is a real sheet with a confirmed DISMISS wired to the engine action through the standard bridge; the dossier blurb the player sees matches content/flavor.js; every prior shell pin still holds.</done>
</task>

<task type="auto">
  <name>Task 2: test/unit/shell-company-panel.test.js — source pins for the sheet, the confirm, the bridge, the SUB_NOTE sync and the rail-only rule</name>
  <files>test/unit/shell-company-panel.test.js</files>
  <read_first>
    - test/unit/shell-input-guards.test.js lines 30-70 (RAW/CODE/stripComments/sliceBetween helpers — copy), test/unit/shell-party-camp.test.js lines 112-146 (the renderPartyRoster region technique `CODE.indexOf("function renderPartyRoster() {")` → next `"\n}\n"`)
    - test/unit/shell-gear-toolbar.test.js lines 80-100, 138-145 (confirm-pin style; CSS rule extraction with `HTML.match(/^\.mw-drop-confirm[^{]*\{[^}]*\}/gm)`)
    - content/flavor.js (import SUB_NOTE for the sync assertion), src/browser/toasts.js (import NARRATIVE_ACTIONS to assert the bridge's action type is narrative)
  </read_first>
  <action>
    Write `test/unit/shell-company-panel.test.js` (header: Phase 36 JOIN-01/CUT-01, Plan 06). Tests (at least 12):
    1. SUB_NOTE sync: the classic `const SUB_NOTE = {` region's `"Cutthroat":` string === `SUB_NOTE.Cutthroat` from content/flavor.js and matches `/one descent in twenty/i`; the raw file no longer contains the prototype-era sentence (build the needle by concatenating `"one member of every party dies"` + `" by your hand"` so this test never spells it whole).
    2. Module-level trio: `const DISMISS_CONFIRM_MS = 3000;` once, `function revertDismissConfirm()` once, `let dismissConfirmRevert = null;` once, all before `function renderPartyRoster() {`; `const DROP_CONFIRM_MS = 3000;` still once (the gear trio untouched).
    3. Region order: within the renderPartyRoster region the indices of `mw-party-name`, `mw-party-sub`, `mw-party-line`, `HP <b>`, `mw-map-hptrack`, `Weapon: `, ` a rest`, `"DISMISS"` are strictly increasing.
    4. HP wording: the region contains `HP <b>` and does not match `/\bWP\b/` (the two-letter token as a standalone word; field names like maxWP are not words).
    5. Sheet fields: the region contains `escText(m.cls`, `escText(m.race`, `escText(m.sub`, `escText(m.weapon`, `RACES[m.race]?.eats || 1`, `ROMAN[lvl - 1]`.
    6. Confirm mechanics: the region contains `textContent = "DISMISS"`, `"Send them off?"`, `className = "mw-drop-confirm"`, `setTimeout(revertDismissConfirm, DISMISS_CONFIRM_MS)` exactly once, `document.addEventListener("pointerdown", onAnyTap, true)` exactly once, `document.removeEventListener("pointerdown", onAnyTap, true)` exactly once, `revertDismissConfirm(); window.mzDismissJoiner?.(idx);` exactly once, and the DISMISS control is gated on `!S.combat`.
    7. Existing pins preserved: exactly two `getElementById(` in the region; `panel.hidden = party.length === 0;`; `mw-map-hpfill`; `Downed`.
    8. Rail-only: the region contains neither `JOINER_PARTING_LINES` nor `joinerDismissed` nor any toast host token (build `"mzTo" + "ast"` by concatenation); the whole CODE has no toast builder reference for the parting line.
    9. Bridge: `window.mzDismissJoiner = function dismissJoinerBridge(` once; inside its region (to the next `\n  };`): `dispatchWithToasts({ type: "dismissJoiner"` once, then `window.__mzState.set(state);`, `window.logLine(line)`, `window.paint();`, `window.renderEncounter();` in that order; and `NARRATIVE_ACTIONS.has("dismissJoiner") === true` (so the rail line is the parting sentence).
    10. CSS: `^\.mw-party-line\{` and `^\.mw-party-dismiss\{` rules exist once each; the `<style>` block does not match `/aria-disabled/`; neither new rule contains `transition` or `animation`.
    11. Voice: the literal strings `Send them off?` and `DISMISS` are clear of content/safety-wordlist.js BANNED (import BANNED, word-boundary match).
    12. Build artefact sanity (skip if www/index.html is absent): `www/index.html` contains `mzDismissJoiner` — proves build:www carried the shell change.
    Run `node --test test/unit/shell-company-panel.test.js` → green; `npm test` → `# fail 0`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-company-panel.test.js && npm test 2>&1 | grep -q "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-company-panel.test.js` exits 0 with at least 12 tests, including names containing `SUB_NOTE`, `HP`, `DISMISS`, `mzDismissJoiner`, `rail-only`.
    - `npm test` prints `# fail 0` with `# pass` >= 2260 (baseline 2179 + Plans 01–05's new tests + this file).
    - `grep -c "one member of every party dies by your hand" test/unit/shell-company-panel.test.js` prints 0 (built by concatenation).
  </acceptance_criteria>
  <done>The Company panel, its confirm, its bridge and the dossier sync are pinned by a source suite, and the whole suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 36 closing gate + SUMMARY with the aggregated deferred Pixel 7 checklist</name>
  <files>mazeworld.html</files>
  <read_first>
    - .planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-01-SUMMARY.md, 36-02-SUMMARY.md, 36-03-SUMMARY.md, 36-04-SUMMARY.md, 36-05-SUMMARY.md (their `## Human verification (deferred to end of run)` sections — aggregate, grouped by plan, numbered continuously)
    - .planning/milestones/v1.4-phases/35-map-screen-rebuild/35-05-SUMMARY.md (the aggregated-checklist shape; this phase builds NO APK — v1.5 has seven more phases)
    - .planning/ROADMAP.md Phase 36 Success Criteria 1-6
  </read_first>
  <action>
    1. Full gate, every output recorded in the SUMMARY: `npm test` (`# fail 0`, pass count); `npm run build:www` exit 0 and `grep -c "mzDismissJoiner" www/index.html` >= 1; `git diff --stat -- test/parity package.json package-lock.json` empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git status --porcelain test/parity/fixtures` empty; `grep -c "fonts.googleapis" mazeworld.html` prints 0; `git diff --stat v1.4.0 -- engine content src mazeworld.html` — the cumulative Phase 36 footprint (record the file list: engine/effects.js, engine/movement.js, engine/combat.js, engine/saveState.js, engine/magic.js, engine/encounters.js, engine/actions.js, engine/engine.js, content/flavor.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, mazeworld.html — and nothing else); `grep -rc "startEffect\|startCooldown" engine content src mazeworld.html | grep -v ":0$"` lists only engine/effects.js (SC-6: no timer record is created anywhere).
    2. Map each ROADMAP success criterion 1-6 to the plan/test that proves it (1 → 36-01 ledger + class-pass-ledger test; 2 → 36-03 tests; 3 → 36-04 + this plan's SUB_NOTE sync; 4 → 36-04; 5 → 36-05 + this plan; 6 → 36-02 + the grep above).
    3. `## Human verification (deferred to end of run)` — the AGGREGATED Pixel 7 checklist: `### Plan 01` … `### Plan 06`, numbered continuously, each item copied from the plan SUMMARYs; Plan 06's own items: (1) accept any Joiner, open the Hero tab — the Company card shows name, sub / race, class · level, an HP bar (the label reads HP), `Weapon: <name>`, `Eats N a rest` (2 for a Troll Joiner, else 1), and a DISMISS button; (2) tap DISMISS — it becomes `Send them off? [Yes] [No]`; tap No — reverts; tap DISMISS, wait 3 s — reverts; tap DISMISS then tap anywhere else — reverts; (3) tap DISMISS → Yes — the card disappears, the panel hides, the rail shows a COMPANY card (dull) with one of the five parting lines, the Oracle log has the same line, no toast, no text inside the panel; (4) during a fight, open the Hero tab — no DISMISS button on the card; (5) the Cutthroat dossier blurb on the Hero tab reads the new text with "one descent in twenty"; (6) a Troll Joiner's card says `Eats 2 a rest`. Add the standing note that `adb install -r` + `am force-stop` + relaunch and the APK build happen at milestone close, not in this phase.
    4. `## Follow-up candidates` (not this phase): the other classic-script SUB_NOTE rows that drifted from content/flavor.js since Phase 23/24 (list them by key after a quick diff) — a Clarity-phase (43) or quick-task candidate.
    5. No file changes expected in this task; if a gate item fails, fix within this plan and re-run.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "^# fail 0" && npm run build:www && test "$(grep -c 'mzDismissJoiner' www/index.html)" -ge 1 && test -z "$(git diff --stat -- test/parity package.json package-lock.json)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && test "$(grep -c 'fonts.googleapis' mazeworld.html)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; `npm run build:www` exits 0; `grep -c "mzDismissJoiner" www/index.html` >= 1.
    - `git diff --stat -- test/parity package.json package-lock.json` prints nothing; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git status --porcelain test/parity/fixtures` prints nothing; `grep -c "fonts.googleapis" mazeworld.html` prints 0.
    - `grep -rc "startEffect\|startCooldown" engine content src mazeworld.html | grep -v ":0$"` prints exactly one line, for engine/effects.js.
    - 36-06-SUMMARY.md contains `## Human verification (deferred to end of run)` with sub-headings `### Plan 01` through `### Plan 06` and at least 20 numbered items in total, a `## Follow-up candidates` section, and a success-criteria map naming SC-1..SC-6.
  </acceptance_criteria>
  <done>Phase 36 is gated and recorded end-to-end; the on-device round is queued as one grouped checklist for the milestone-close UAT batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member sheet → innerHTML | name/sub/race/cls/weapon strings interpolated into the Company card |
| tap → engine action | a confirmed tap dispatches dismissJoiner with an index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-15 | Information Disclosure / XSS | renderPartyRoster innerHTML | medium | mitigate | escText() on every interpolated member string; DISMISS/confirm built via createElement + textContent |
| T-36-16 | Tampering | mzDismissJoiner index | low | mitigate | non-integer/negative i coerced to 0 in the bridge; validateAction + badIndex refusal in the engine |
| T-36-17 | Elevation of Privilege | DISMISS during combat | low | mitigate | control hidden when S.combat; engine inCombat refusal as backstop |
| T-36-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `npm test` `# fail 0`; `npm run build:www` exit 0; fixtures untouched; master hash unchanged; no package/font change; no timer record created anywhere (SC-6 grep).
- Shell pins: SUB_NOTE sync, trio above renderPartyRoster, sheet order, HP wording, confirm mechanics, bridge shape, rail-only rule, CSS rules, existing pins preserved.
- rng-draw impact: zero. Serialized-field impact: none.
</verification>

<success_criteria>
- ROADMAP SC-5: the Hero tab's Company panel shows the Joiner's sheet (class/sub/race, HP, weapon, "eats N a rest") and a DISMISS control that asks for confirmation; on confirm the Joiner leaves with a sarcastic parting line of their own and the party slot is free.
- ROADMAP SC-3 (blurb visible on device): the dossier's Cutthroat text matches content/flavor.js.
- ROADMAP SC-6 (phase-wide): `npm test` green, engine/content/parity diff limited to the declared Phase 36 footprint, `engine/effects.js` has no player-visible behaviour.
</success_criteria>

<output>
Create `.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-06-SUMMARY.md` when done — the full gate record, the SC-1..6 map, `## Follow-up candidates`, and the aggregated `## Human verification (deferred to end of run)` checklist (Plans 01–06).
</output>
