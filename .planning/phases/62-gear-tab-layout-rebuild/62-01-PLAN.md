---
phase: 62-gear-tab-layout-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/gearTab.js
  - test/unit/gear-view-models.test.js
  - test/unit/gear-panels.test.js
  - test/unit/gearTab.test.js
  - test/voice/safety-scan.test.js
autonomous: true
requirements: [GSCR-01, GSCR-02, GSCR-03, GSCR-04, GSCR-05, GSCR-06]

must_haves:
  truths:
    - "gearHeaderModel(state) returns exactly two stats in order: ARMOR RATING (value armorDisplay(c).ar, so an active Cloak of Armor is included) then WILMST (c.gold, text via toLocaleString). It carries no name, race, class or floor line (GSCR-01 as reworded; the global HUD names the hero)"
    - "gearWornModel(state) returns five rows in the fixed order weapon, armor, cloak, jewelry1, jewelry2 (GEAR_WORN_ORDER), labelled WEAPON, ARMOR, CLOAK, JEWELRY 1, JEWELRY 2, with countText '<filled> / 5' where filled = 5 - emptySlotRows(c).length"
    - "An empty worn row reads name 'empty', note equal to its GEAR_COPY.empty line, value '—', no use cell and no unequip. The one empty-slot rule is emptySlotRows(c), which now also yields a weapon row (GEAR_COPY.empty.weapon, starting 'fists — ') when c.weapon is bare: 'Fists', empty, or not a WEAPONS entry"
    - "The value column carries a number only for the weapon ('<WEAPONS[c.weapon].lab>' plus ' +<c.magicWpn>' when magic) and for worn armor ('AR <c.ar>'). Cloak and jewelry rows read '—'. The armor row's note is the wear '<armorWP>/<armorMax> hp' or 'destroyed'; with an active Cloak of Armor it is armorDisplay(c).under, and the Cloak of Armor itself sits in the CLOAK row"
    - "gearUseCell(state, it) is derived only from itemRowState(state, it). It returns null for kind 'none'. ready, charges and consumable read USE; effect reads ACTIVE with sub st.text; cooldown reads COOLING with sub '<n> SQ' ('1 SQ' when n is 1); a staff's charges sub equals st.text; phase always equals st.kind"
    - "gearBagMeterModel(state) reads bagUsage(c) alone. countText equals usage.text; there is one pip per bag slot (4/6/8/10) with the first usage.have pips lit; full equals usage.full; fullLine is 'BAG FULL · DROP OR USE SOMETHING' only when full; the free-ride line appears only for a capped bag; a bag-less character gets no pips. Potion items and c.scrolls never change the count"
    - "gearBagCardsModel(state) returns one card per dropShelfItems(c) entry, keeping its true c.items index. A slot-family item (weapon, armor, cloak, jewel) carries tag WEAPON/ARMOR/CLOAK/JEWELRY, plus ' · SWAP' when every slot of that family is occupied, and no use cell. A bag-only item carries no tag and gets a use cell exactly when gearUseCell is non-null. desc is it.txt (bagArmorText(it) for armor) plus the usableBy(it, c) suffix"
    - "gearConsumablesModel(state) lists HEALING POTION first, always shown as '×<c.potions>', and enabled exactly when potions > 0 and wp < maxWP (the combat ITEMS submenu's own rule). Next comes one row per buff-potion name, in first-appearance order, as '×N' with useItem on the group's first c.items index. Then SCROLLS when c.scrolls > 0: READ, enabled equal to canRead(state), and otherwise a reason equal to the engine's LINE_FOR.scrollRefused text. heldText is '<potions + buff items + scrolls> HELD'"
    - "gearKitRows(state) returns, in this order: Rations, Spell charges (Magic User only), every running effect the old kit list showed (the Shield ward's pool and rounds, Strength, Regeneration, Mirror Self, Sense Presence, Sense Danger, Map the Floor), and Kills. It returns no Potions, Scrolls or Wilmst row"
    - "Every new model is pure. Two calls deep-equal each other, the state's JSON is unchanged, there are no rng draws, and a legacy c with no worn, items or timers never throws"
    - "Today's renderer still renders byte-identically after this plan: every shell-tab snapshot fixture is unchanged and npm test is green"
    - "Edge GSCR-01/adjacency: when armor rating and wilmst are equal (both 0 on a bare character), the header still returns two separate labelled stats, in order"
    - "Edge GSCR-01/empty: a bare character (no armor, no Cloak of Armor, 0 gold) reads ARMOR RATING '0' and WILMST '0'. Neither is ever blank, 'undefined' or 'NaN'"
    - "Edge GSCR-01/encoding: gold 1234 reads '1,234' (toLocaleString, the format the old m-gold write used), and the AR text is the integer as a plain string"
    - "Edge GSCR-01/ordering: stats[0] is always ARMOR RATING and stats[1] is always WILMST"
    - "Edge GSCR-02/boundary: a bare-handed, unarmoured, unadorned character reads '0 / 5'. A character with a real weapon, worn armor, a cloak and both jewelry keys filled reads '5 / 5'"
    - "Edge GSCR-02/adjacency: two jewels with the same name, worn in jewelry1 and jewelry2, are two separate filled rows"
    - "Edge GSCR-02/empty: Fists yields the weapon empty row with GEAR_COPY.empty.weapon, and each of the five rows falls back to its own in-voice line"
    - "Edge GSCR-02/ordering: the row keys are weapon, armor, cloak, jewelry1, jewelry2 whatever is worn, including when only jewelry2 is filled"
    - "Edge GSCR-02/precision: the armor value is 'AR <c.ar>' as an integer. Wear reads '<armorWP>/<armorMax> hp', or 'destroyed' at armorWP <= 0. A +2 Broadsword reads 'd10+2 +2', the same text the engine writes on a magic weapon item's txt"
    - "Edge GSCR-03/adjacency: exactly 1 square remaining reads '1 SQ'. An item with no timer record reads USE with an empty sub"
    - "Edge GSCR-03/empty: weapon and armor rows never carry a use cell, and an item with no activation (rope, ladder, picks) gets null"
    - "Edge GSCR-03/encoding: a use cell's label and sub come only from GEAR_COPY.use and ITEM_STATE_COPY/itemRowState text, never from an item name"
    - "Edge GSCR-04/boundary: at have === cap the model is full (fullLine set, every pip lit); at have === cap - 1 it is not full and fullLine is ''"
    - "Edge GSCR-04/precision: pips.length equals the bag cap exactly (4, 6, 8, 10), and the number of lit pips equals usage.have"
    - "Edge GSCR-05/adjacency: a bagged jewel with only jewelry1 worn reads 'JEWELRY'; with both jewelry keys worn it reads 'JEWELRY · SWAP'"
    - "Edge GSCR-05/empty: a bag holding only potion items, or nothing, yields zero cards"
    - "Edge GSCR-05/ordering: card indices follow c.items order and equal dropShelfItems(c).map(e => e.i). Potion items are skipped without shifting the index that Equip, Drop and USE dispatch"
    - "Edge GSCR-06/boundary: potions 0 still yields HEALING POTION '×0' with enabled false, and c.scrolls 0 yields no SCROLLS row"
    - "Edge GSCR-06/adjacency: two buff potions with the same name group into one '×2' row; different names stay separate rows"
    - "Edge GSCR-06/empty: nothing held yields only the '×0' healing row and '0 HELD'"
    - "Edge GSCR-06/ordering: HEALING POTION comes first, then the buff groups in first-appearance order, then SCROLLS. Kit rows keep their fixed order with Kills last"
    - "Edge GSCR-06/precision: heldText counts potions + buff items + scrolls exactly, and Spell charges reads '<max - spellsUsed> / <max>' with max === maxCharges(c)"
  artifacts:
    - path: "src/browser/gearTab.js"
      provides: "GEAR_COPY (extended), GEAR_WORN_ORDER, emptySlotRows (weapon row), gearHeaderModel, gearUseCell, gearWornModel, gearBagMeterModel, gearBagCardsModel, gearConsumablesModel, gearKitRows"
      contains: "export function gearWornModel"
    - path: "test/unit/gear-view-models.test.js"
      provides: "behavior, edge, purity and copy tests for every new model"
      min_lines: 250
  key_links:
    - from: "src/browser/gearTab.js#gearWornModel"
      to: "src/browser/gearTab.js#emptySlotRows"
      via: "the one empty-slot read decides filled vs empty for all five rows"
      pattern: "emptySlotRows\\(c\\)"
    - from: "src/browser/gearTab.js#gearUseCell"
      to: "src/browser/gearTab.js#itemRowState"
      via: "the one row-state rule, never a restated timer read"
      pattern: "itemRowState\\(state, it\\)"
    - from: "src/browser/gearTab.js#gearBagMeterModel"
      to: "src/browser/gearTab.js#bagUsage"
      via: "count, pips and full come from bagUsage alone"
      pattern: "bagUsage\\(c\\)"
    - from: "src/browser/gearTab.js#gearBagCardsModel"
      to: "src/browser/viewModels.js#dropShelfItems"
      via: "bag-slot items with their true c.items index"
      pattern: "dropShelfItems\\(c\\)"
    - from: "src/browser/gearTab.js#gearHeaderModel"
      to: "src/browser/viewModels.js#armorDisplay"
      via: "effective AR, Cloak of Armor included"
      pattern: "armorDisplay\\(c\\)"
    - from: "src/browser/gearTab.js#gearConsumablesModel"
      to: "src/browser/narrationLines.js#LINE_FOR.scrollRefused"
      via: "the READ reason is the engine refusal's own line"
      pattern: "LINE_FOR\\.scrollRefused"
  prohibitions:
    - requirement_id: GSCR-06
      category: player-harm
      flagged: true
      statement: "MUST NOT let a Gear-tab tap silently waste a healing potion: the HEALING POTION USE is unavailable at ×0 and at full HP, the same rule the combat ITEMS submenu applies"
    - requirement_id: GSCR-02
      category: values
      flagged: true
      statement: "MUST NOT ship Gear-tab copy outside the family-friendly sarcastic voice: every new GEAR_COPY leaf clears the BANNED wordlist and says HP, never WP"
---

<objective>
Build every number, label and state the new Gear tab shows as pure, DOM-free view models in `src/browser/gearTab.js`, test-first. This covers the slim stat header, the five-row WORN list (with the value column, the in-voice empty lines and the USE cell), the bag meter and pips, the bag cards (slot tag with `· SWAP`), the CONSUMABLES block and the ALSO ON YOU rows.

Purpose: Plan 02's renderer then only turns these models into DOM. Every model reads the shared ones (`armorDisplay`, `emptySlotRows`, `itemRowState`, `bagUsage`, `dropShelfItems`, `usableBy`, `bagArmorText`, `canRead`), so no rule is restated.

Implements CONTEXT (all locked):
- "Header = a slim stat header": ARMOR RATING and WILMST only.
- "Section order" / ALSO ON YOU contents.
- "Five fixed rows in the mock's order", including the Cloak of Armor handling.
- "Value column only where there's a clean number".
- "Empty slots": the one `emptySlotRows` read, plus the new weapon line.
- "USE cell".
- "Bag meter".
- "Bag cards".
- "Consumables".

Output: the extended `GEAR_COPY`, `GEAR_WORN_ORDER`, eight exported model functions, a new unit suite, and updated copy scans. The old renderer is untouched, so no snapshot moves in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/62-gear-tab-layout-rebuild/62-CONTEXT.md
@.planning/phases/61-gear-rules-store-purchase-fix/61-04-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
Existing, in src/browser/gearTab.js (keep every existing export's body and behaviour unless this plan says otherwise):
- `bagUsage(c)` returns `{ have, slots, full, text }`. `slots` is null for a bag-less character. `text` is `"have / slots"`; for a bag-less character it is `"have"`, or `""` when empty.
- `GEAR_COPY`: a frozen object with onYou, wielded, worn, alsoOnYou, bag, freeRide and a nested frozen `empty` { armor, jewelry1, jewelry2, cloak }. onYou and wielded are still read by today's renderer, so they stay in this plan; Plan 02 retires them.
- `emptySlotRows(c)` returns `[{ slot, text }]`: armor (only when `!armorDisplay(c).worn && !armorDisplay(c).magic`), then each empty `WORN_SLOTS` key (jewelry1, jewelry2, cloak).
- `ITEM_STATE_COPY` has ready "READY", squares "{n} SQ", square "1 SQ", cooling "cd {n} SQ", charges "{k}/{max} · {n} SQ".
- `itemRowState(state, it)` returns `{ text, kind: "none"|"consumable"|"ready"|"effect"|"cooldown"|"charges", remaining? }`.
- The line `import { WORN_SLOTS, WORN_KEYS_OF, activationFor, itemTimerId, chargesTimerId, slotFor } from "../../engine/derived.js";` is pinned byte-for-byte by test/unit/shell-worn-slots.test.js:210. Do not edit it; put any new engine/derived.js name on a separate import line.
- Test pins slice `export function renderCarriedList(` → `export function renderGearTab(`, and require `const DROP_CONFIRM_MS = 3000;` / `const SWAP_CONFIRM_MS = 3000;` to sit above renderCarriedList. Place every new export directly after `itemRowState` (before the DROP_CONFIRM_MS comment block), never inside or after renderCarriedList.
- GEAR_SRC-wide count pins that must survive this plan:
  - test/unit/shell-clarity-43.test.js counts `for (const slot of WORN_SLOTS)` as exactly 2, so the new models iterate GEAR_WORN_ORDER and must not add a third loop of that exact shape.
  - `gearRow: true` must stay at 1, `const DROP_CONFIRM_MS = 3000;` at 1, and `^export function itemRowState\(` at 1.
  - gearTab.js must not read window or document (test/unit/gearTab.test.js).

src/browser/viewModels.js:
- `armorDisplay(c)` returns `{ label, ar, current, max, worn, destroyed, magic, sub, wornSub, under, line }`.
- `bagArmorText(it)`.
- `usableBy(it, c)` returns "" or "(usable by …)".
- `dropShelfItems(c)` returns `[{ it, i }]` for the bag-slot items, with the true c.items index.

engine: `WEAPONS[name].lab` (content/weapons.js; "Fists" is not an entry); `canRead(state)` (engine/magic.js); `maxCharges(c)` (engine/movement.js); bag caps small 4 / medium 6 / large 8 / exlarge 10 (content/bags.js); `BAG_FREE_KINDS` (potion, scroll, bag) behind `takesBagSlot`.

src/browser/narrationLines.js: `LINE_FOR.scrollRefused({ reason })` returns `{ text, tone: "block", priority }`. The text is "A Pilfer's hands know locks, not letters." for pilfer and "The runes mean nothing to you." for noRunes. narrationLines.js does not import gearTab.js, so the new import is cycle-free.

src/browser/combatMenu.js: `combatMenuViewModel(state).submenus.items.rows`. The row with `id: "potion"` has `enabled: (c.potions || 0) > 0 && c.wp < c.maxWP`. The item rows are `item-<i>` / `worn-<slot>` with `cost: itemRowState(state, it).text`.

Timer record shapes (from test/unit/itemRowState.test.js): `c.timers["item:<name>"] = { cadence: "squares", left: N, phase: "effect"|"cooldown" }`. A staff has `it.charges` below the activation's max, plus `c.timers["charges:<name>"]`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GEAR_COPY extension, the weapon empty line, and the header / WORN / USE-cell models (test-first)</name>
  <files>src/browser/gearTab.js, test/unit/gear-view-models.test.js, test/unit/gear-panels.test.js</files>
  <read_first>
    - src/browser/gearTab.js (the whole file, 566 lines: GEAR_COPY :53-66, emptySlotRows :82-93, itemRowState :132-159, and today's renderGearTab :399-565 for the kit rows and worn-row sub text being replaced)
    - src/browser/viewModels.js (armorDisplay :28-52, bagArmorText :60-63, usableBy :162-182, dropShelfItems near :284)
    - engine/derived.js (:115-160 BAG_FREE_KINDS/takesBagSlot/slotItems; :182-240 WORN_KEYS_OF/WORN_SLOTS/slotFor; :405-440 activationFor/itemTimerId/chargesTimerId)
    - engine/effects.js (remaining :202-206, isReady :213-216)
    - content/weapons.js (WEAPONS labels)
    - test/unit/gear-panels.test.js (the GEAR_COPY deepEqual pin :38-56 and the emptySlotRows fixtures :119-186)
    - test/unit/itemRowState.test.js (timer and staff-charge fixture shapes)
    - design/Mazeworld Gear.dc.html (renderVals slots/useCell :384-427, layout reference only; its toy rules are not canon)
  </read_first>
  <behavior>
    - GEAR_COPY is frozen and every nested group is frozen. `GEAR_COPY.empty` has exactly the keys weapon, armor, cloak, jewelry1, jewelry2; `empty.weapon` starts "fists — ", and the four existing empty lines are byte-identical.
    - emptySlotRows: `c.weapon` of "Fists", "", undefined or "NotAWeapon" gives a first row `{ slot: "weapon", text: GEAR_COPY.empty.weapon }`. With `c.weapon: "Axe"` there is no weapon row. All seven existing emptySlotRows tests in gear-panels.test.js pass unchanged; their fixtures carry weapon "Axe".
    - GEAR_WORN_ORDER deep-equals ["weapon","armor","cloak","jewelry1","jewelry2"] and is frozen.
    - gearHeaderModel, bare Fighter (armor "Nothing", ar 0, gold 0): `{ stats: [{ key: "ar", label: "ARMOR RATING", value: 0, text: "0" }, { key: "gold", label: "WILMST", value: 0, text: "0" }] }`.
    - gearHeaderModel: gold 1234 gives gold.text "1,234".
    - gearHeaderModel: Mail (ar 12, armorWP 30, armorMax 30) gives ar.value === armorDisplay(c).ar.
    - gearHeaderModel: Cloak of Armor worn with an active "item:Cloak of Armor" effect record over Mail gives ar.value === armorDisplay(c).ar, which is the magic value.
    - gearUseCell: a weapon item, rope, ladder or picks gives null. A kind:"potion" item and the torch give `{ phase: "consumable", label: "USE", sub: "", remaining: 0 }`. A worn Cloak of Speed with no record gives `{ phase: "ready", label: "USE", sub: "", remaining: 0 }`.
    - gearUseCell, effect: left 23 gives phase "effect", label "ACTIVE", sub "23 SQ"; left 1 gives sub "1 SQ".
    - gearUseCell, cooldown: left 12 gives phase "cooldown", label "COOLING", sub "12 SQ", remaining 12; left 1 gives sub "1 SQ".
    - gearUseCell, staff below max with a charges record: phase "charges", label "USE", sub === itemRowState(state, it).text.
    - gearUseCell: for every case above, phase === itemRowState(state, it).kind.
    - gearWornModel, bare Fighter (weapon "Fists", armor "Nothing", worn {}): countText "0 / 5", filled 0, row keys in GEAR_WORN_ORDER. Every row has filled false, name "empty", note === GEAR_COPY.empty[key], value "—", use null, useRef null, unequip null.
    - gearWornModel: weapon "Broadsword" with magicWpn 2 gives value "d10+2 +2" and note === GEAR_COPY.weaponMagic with {n} = 2. With magicWpn 0 it gives value "d10+2" and note === GEAR_COPY.weaponMundane. unequip is `{ slot: "weapon", blocked: bagUsage(c).full }`.
    - gearWornModel: Mail (ar 12, armorWP 22, armorMax 40) gives armor name "Mail", value "AR 12", note "22/40 hp". armorWP 0 gives note "destroyed", and unequip.blocked stays false even on a full bag (a destroyed piece is discarded, not stowed).
    - gearWornModel: Cloak of Armor active over Mail gives cloak row name "Cloak of Armor", armor row name "Mail", value "AR <c.ar>", note === armorDisplay(c).under.
    - gearWornModel: Cloak of Armor active with no plate gives an armor row with filled true, name GEAR_COPY.magicPlate, value "AR <armorDisplay(c).ar>", note === armorDisplay(c).under, unequip null.
    - gearWornModel, worn jewel in jewelry1: value "—", note it.txt, use === gearUseCell(state, it), useRef `{ slot: "jewelry1" }`, unequip `{ slot: "jewelry1", blocked: bagUsage(c).full }`.
    - gearWornModel: two jewels with the same name in jewelry1 and jewelry2 give two filled rows. With all five filled the count reads "5 / 5". With only jewelry2 filled, the key order is unchanged.
    - gearWornModel, encoding: a jewel named `Thief's "Lucky" <Ring> 🎲` gives row.name equal to that exact string.
    - Purity: every model called twice deep-equals; JSON.stringify(state) is unchanged; a `{ c: { weapon: "Fists", armor: "Nothing" } }` state (no worn, items or timers) never throws.
  </behavior>
  <action>
In `src/browser/gearTab.js`:

**1. Extend GEAR_COPY.** Keep it one frozen object. Keep every existing leaf; onYou and wielded stay until Plan 02 deletes today's renderer. Add these leaves, with every nested object `Object.freeze`d:
- `empty.weapon`: "fists — nothing in hand. Free, always with you, and not very good."
- `armorRating`: "ARMOR RATING"
- `wilmst`: "WILMST"
- `consumables`: "CONSUMABLES"
- `count`: "{n} / {max}"
- `held`: "{n} HELD"
- `slot`: { weapon "WEAPON", armor "ARMOR", cloak "CLOAK", jewelry1 "JEWELRY 1", jewelry2 "JEWELRY 2" }
- `family`: { weapon "WEAPON", armor "ARMOR", cloak "CLOAK", jewelry "JEWELRY" }
- `swap`: " · SWAP"
- `emptyName`: "empty"
- `noValue`: "—"
- `chevron`: "›"
- `weaponMagic`: "+{n} magic. Somebody cared, once."
- `weaponMundane`: "no enchantment. Just you and the swing."
- `magicPlate`: "magic plate"
- `armorWear`: "{current}/{max} hp"
- `armorDestroyed`: "destroyed"
- `bagFull`: "BAG FULL · DROP OR USE SOMETHING"
- `bagEmptyHead`: "NOTHING LEFT TO CARRY"
- `bagEmptyBody`: "You used it all. That was the plan, technically."

These last two are moved verbatim from today's emptyHtml literal. Plan 02 switches the renderer over to them.

- `use`: { use "USE", active "ACTIVE", cooling "COOLING", read "READ" }
- `healingPotion`: "HEALING POTION"
- `healingDesc`: "Heals. Wasted at full health."
- `scrolls`: "SCROLLS"
- `scrollDesc`: "A random spell, read aloud. No refunds."
- `qty`: "×{n}"
- `kit`: { rations "Rations", rationsValue "{n} days", spellCharges "Spell charges", spellChargesValue "{k} / {max}", wardValue "{pool} hp left · {rounds} rds", strength "Strength", strengthValue "+{n} damage", regen "Regeneration", regenValue "d8 a round", mirror "Mirror Self", mirrorValue "{n} rds", senses "Sense Presence", sensesValue "till the fight ends", foresight "Sense Danger", foresightValue "armed", reveal "Map the Floor", revealValue "{n} sq", kills "Kills" }. The kit labels and values are today's renderGearTab kit literals, moved verbatim.
- `act`: { unequip "Unequip", bagFull "Bag full" }

Update the GEAR_COPY doc comment with a Phase 62 (GSCR-01..06) note: every Gear-tab string lives here so the voice scan and the hp-not-wp guard walk it.

**2. emptySlotRows.** Prepend one row, `{ slot: "weapon", text: GEAR_COPY.empty.weapon }`, when `!(c.weapon && WEAPONS[c.weapon])`, before the armor row. This implements the CONTEXT rule that bare Fists counts as an empty weapon slot. Leave the rest of the body unchanged. Update its doc comment to state the order: weapon, armor, then the WORN_SLOTS keys.

**3. New exports.** Place them directly after itemRowState and before the DROP_CONFIRM_MS comment block. Each is pure and null-safe (`c.worn && c.worn[k]`, `c.items || []`) and gets a JSDoc naming the Phase 62 requirement it serves:
- `GEAR_WORN_ORDER = Object.freeze(["weapon","armor","cloak","jewelry1","jewelry2"])`. This is the mock's display order; the engine's WORN_SLOTS order is not changed.
- `gearHeaderModel(state)` returns `{ stats: [ar, gold] }` exactly as the behavior block specifies. ar reads `armorDisplay(c).ar`; gold reads `c.gold || 0` with `.toLocaleString()`.
- `gearUseCell(state, it)`:
  - `st = itemRowState(state, it)`; return null when `st.kind === "none"`.
  - label: GEAR_COPY.use.active for effect, GEAR_COPY.use.cooling for cooldown, GEAR_COPY.use.use otherwise.
  - sub: "" for ready and consumable; `st.text` for effect and charges; for cooldown, ITEM_STATE_COPY.square when `st.remaining === 1`, else ITEM_STATE_COPY.squares with `{n}` replaced by `st.remaining`.
  - Return `{ phase: st.kind, label, sub, remaining: st.remaining ?? 0 }`.
  - Charges reads USE because a staff with charges left still works. A tap at 0/max gets the engine's own "recharging" rail refusal (the Phase 31 never-disable-silently rule).
- `gearWornModel(state)` returns `{ filled, max: 5, countText, rows }`.
  - `empties = new Set(emptySlotRows(c).map((r) => r.slot))`, `armorD = armorDisplay(c)`, `usage = bagUsage(c)`.
  - Build one row per GEAR_WORN_ORDER key: `{ key, label: GEAR_COPY.slot[key], filled, name, note, value, use, useRef, unequip }`.
  - Empty row: name GEAR_COPY.emptyName, note from the emptySlotRows row's text, value GEAR_COPY.noValue, use/useRef/unequip null.
  - weapon: name c.weapon; value `WEAPONS[c.weapon].lab + (c.magicWpn ? " +" + c.magicWpn : "")`; note weaponMagic or weaponMundane; unequip `{ slot: "weapon", blocked: usage.full }`.
  - armor:
    - When armorD.worn: name c.armor, value "AR " + c.ar, and note armorD.under when armorD.magic, else armorDestroyed when armorD.destroyed, else armorWear with current/max. unequip is `{ slot: "armor", blocked: usage.full && !armorD.destroyed }`.
    - When only armorD.magic: name magicPlate, value "AR " + armorD.ar, note armorD.under, unequip null.
  - cloak/jewelry1/jewelry2 (it = c.worn[key]): name it.n, note it.txt ?? "", value noValue, use gearUseCell(state, it), useRef `{ slot: key }` when use is non-null, unequip `{ slot: key, blocked: usage.full }`.
  - filled = count of filled rows; countText = GEAR_COPY.count with {n} = filled and {max} = 5.

Do not touch today's renderGearTab or renderCarriedList in this task.

**Tests.** Create `test/unit/gear-view-models.test.js` covering every Task 1 behavior bullet. Build characters as plain objects, the way gear-panels.test.js's fixedChar does, and states as `{ c }`. For the Cloak of Armor, reuse gear-panels.test.js :147-155's exact shapes. Mirror the edge-case bullets as separately named tests prefixed with the edge id (for example "Edge GSCR-02/boundary: …") so the SUMMARY can cite them.

In `test/unit/gear-panels.test.js`:
- Update the GEAR_COPY deepEqual pin to the new full literal shape, keeping the frozen assertions on GEAR_COPY, GEAR_COPY.empty and every other nested group.
- Keys-sorted assertion for `empty`: ["armor","cloak","jewelry1","jewelry2","weapon"].
- Add one emptySlotRows test for the weapon row (Fists first, and no row for "Axe").
  </action>
  <verify>
    <automated>node --test test/unit/gear-view-models.test.js test/unit/gear-panels.test.js test/unit/itemRowState.test.js test/unit/shell-tab-snapshots.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-view-models.test.js` exits 0 with at least 20 passing tests, including test names beginning "Edge GSCR-01/", "Edge GSCR-02/" and "Edge GSCR-03/"
    - `grep -c "export const GEAR_WORN_ORDER" src/browser/gearTab.js` = 1, and each of `export function gearHeaderModel(`, `export function gearUseCell(`, `export function gearWornModel(` has count 1
    - `grep -c "itemRowState(state, it)" src/browser/gearTab.js` >= 4 (the itemRowState declaration line, its two existing callers in renderCarriedList and today's wornSlotRow, plus gearUseCell)
    - `grep -c 'import { WORN_SLOTS, WORN_KEYS_OF, activationFor, itemTimerId, chargesTimerId, slotFor } from "../../engine/derived.js";' src/browser/gearTab.js` = 1 (the pinned line is unchanged)
    - `node --test test/unit/gear-panels.test.js test/unit/itemRowState.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-tab-snapshots.test.js` exits 0; no snapshot fixture changes (`git diff --stat -- test/unit/fixtures/` prints nothing)
  </acceptance_criteria>
  <done>The header, the five WORN rows and the USE cell exist as pure models built on armorDisplay, emptySlotRows and itemRowState. The copy lives in GEAR_COPY, and every edge above has a named test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bag meter, bag cards, consumables and ALSO ON YOU models, copy scans, full check (test-first)</name>
  <files>src/browser/gearTab.js, test/unit/gear-view-models.test.js, test/unit/gearTab.test.js, test/voice/safety-scan.test.js</files>
  <read_first>
    - src/browser/gearTab.js (after Task 1)
    - src/browser/combatMenu.js (:233-300, the ITEMS rows and the potion row's enabled rule)
    - src/browser/narrationLines.js (:286-291 block(), :1478-1488 LINE_FOR.scrollRefused)
    - engine/magic.js (canRead :582-586, readScroll's refusal branch :615-630)
    - engine/movement.js (maxCharges), content/bags.js (the slot caps)
    - src/browser/viewModels.js (dropShelfItems, usableBy, bagArmorText)
    - test/unit/gearTab.test.js (the exports test :75-86 and the voice test :215-224)
    - test/voice/safety-scan.test.js (:330-350 collectAuthoredStrings)
    - test/unit/hp-not-wp.test.js (:130-146; GEAR_COPY is already in the walked banks)
  </read_first>
  <behavior>
    - gearBagMeterModel, small bag (cap 4) holding 3 slot items, 2 kind:"potion" items and c.scrolls 3: have 3, slots 4, countText "3 / 4", pips [true,true,true,false], full false, fullLine "", freeRide === GEAR_COPY.freeRide.
    - gearBagMeterModel: at 4 slot items, full is true, every pip is lit and fullLine === GEAR_COPY.bagFull.
    - gearBagMeterModel: medium, large and exlarge bags give pips.length 6, 8 and 10.
    - gearBagMeterModel, bag-less (no c.bag): slots null, pips [], freeRide "", and countText === bagUsage(c).text ("" when empty, "2" with two items).
    - gearBagMeterModel: countText === bagUsage(c).text in every case.
    - gearBagCardsModel, order and index: items [jewel(0), kind:"potion"(1), rope tool(2), staff(3)] give card indices [0,2,3], deep-equal to dropShelfItems(c).map(e => e.i).
    - gearBagCardsModel, weapon card: a Club item in the bag reads tag "WEAPON" when c.weapon is "Fists" and "WEAPON · SWAP" when a real weapon is wielded.
    - gearBagCardsModel, armor card: reads "ARMOR · SWAP" when armorDisplay(c).worn is true, else "ARMOR"; desc starts with bagArmorText(it).
    - gearBagCardsModel, jewel card: reads "JEWELRY" with only jewelry1 worn and "JEWELRY · SWAP" with both keys worn. A cloak card with a cloak worn reads "CLOAK · SWAP".
    - gearBagCardsModel, bag-only items: rope gives family null, tag "", use null, useRef null. A staff gives tag "", use === gearUseCell(state, staff), useRef === its index. The torch gives a use with phase "consumable".
    - gearBagCardsModel: a bagged jewel or cloak has use null even though itemRowState reads it ready, because equippable gear only works from its slot.
    - gearBagCardsModel: a Fighter-only weapon card for a Magic User has a desc ending with usableBy(it, c), which contains "not you". An unrestricted jewel's desc === it.txt.
    - gearBagCardsModel: a bag of only potion items yields []; a name with an apostrophe, quotes, angle brackets and an emoji comes through verbatim.
    - gearConsumablesModel, nothing held: rows are exactly [heal] with qty 0, qtyText "×0", enabled false, reason "", dispatch { type: "drinkPotion" }; heldText "0 HELD".
    - gearConsumablesModel: potions 3 with wp < maxWP gives heal enabled true. With wp === maxWP heal is enabled false, and heal.enabled === the combat ITEMS potion row's enabled (combatMenuViewModel(state).submenus.items.rows id "potion") across both cases.
    - gearConsumablesModel, buff items: "Acuteness potion (blue)" at indices 1 and 4 plus "Strength potion" at index 2 give rows [heal, Acuteness ×2 with dispatch { type: "useItem", i: 1 }, Strength ×1 with i: 2]. desc is the first item's txt.
    - gearConsumablesModel, scrolls: a Magic User with scrolls 2 gets SCROLLS ×2 with verb "READ", enabled true, reason "", dispatch { type: "readScroll" }.
    - gearConsumablesModel: a Thief with sub "Pilfer" and scrolls 1 gets enabled false and reason === LINE_FOR.scrollRefused({ reason: "pilfer" }).text. A Fighter without Runes/Signs gets reason === LINE_FOR.scrollRefused({ reason: "noRunes" }).text.
    - gearConsumablesModel: potions 3 + 3 buff items + scrolls 2 gives heldText "8 HELD".
    - gearKitRows, Fighter with rations 4 and kills 0: [{ label: "Rations", value: "4 days" }, { label: "Kills", value: "0" }].
    - gearKitRows: a Magic User with spellsUsed 1 gets the second row { label: "Spell charges", value: `${maxCharges(c) - 1} / ${maxCharges(c)}` }.
    - gearKitRows, running effects: with ward { name: "Shield", pool: 9, rounds: 3 }, might 2, regen true, mirror 3, senses true, foresight true and timers["spell:reveal"] { phase: "effect", left: 12 }, the rows come in order Rations, [Spell charges], "Shield" "9 hp left · 3 rds", "Strength" "+2 damage", "Regeneration" "d8 a round", "Mirror Self" "3 rds", "Sense Presence" "till the fight ends", "Sense Danger" "armed", "Map the Floor" "12 sq", Kills.
    - gearKitRows: no row labelled Potions, Scrolls or Wilmst ever appears.
    - Purity (all four): two calls deep-equal; state JSON unchanged; a sparse legacy c never throws.
  </behavior>
  <action>
In `src/browser/gearTab.js`, add these after Task 1's exports (still before the DROP_CONFIRM_MS block):
- Import `LINE_FOR` from "./narrationLines.js" on its own new line.
- Extend the existing `./viewModels.js` import to also bring in `usableBy` and `dropShelfItems`.

New exports:

- `gearBagMeterModel(state)`: `usage = bagUsage(c)`. Return `{ have: usage.have, slots: usage.slots, full: usage.full, countText: usage.text, pips, fullLine, freeRide }`, where:
  - `pips = usage.slots !== null ? Array.from({ length: usage.slots }, (_, i) => i < usage.have) : []`
  - `fullLine = usage.full ? GEAR_COPY.bagFull : ""`
  - `freeRide = usage.slots !== null ? GEAR_COPY.freeRide : ""`

  Keep the `usage.slots !== null` / `GEAR_COPY.freeRide` pairing literal, because Plan 02 re-points the shell-loot-screen pin at this function's region. Never call bagCap, canStow or slotItems here: bagUsage is the one readout.

- `gearBagCardsModel(state)` maps `dropShelfItems(c)` to `{ i, name: it.n, desc, family, tag, swap, use, useRef }`:
  - family is "weapon" for kind weapon, "armor" for kind armor, else `slotFor(it)` ("jewelry", "cloak" or null).
  - swap (only when family is set):
    - weapon: `!!(c.weapon && WEAPONS[c.weapon])`
    - armor: `armorDisplay(c).worn`
    - jewelry/cloak: `(WORN_KEYS_OF[family] || []).every((k) => c.worn && c.worn[k])`
    
    This is the same "every key taken" test renderCarriedList uses to choose the swap confirm.
  - tag: family ? `GEAR_COPY.family[family] + (swap ? GEAR_COPY.swap : "")` : "".
  - use: family ? null : gearUseCell(state, it). useRef is i when use is set, else null.
  - desc: `(it.kind === "armor" ? bagArmorText(it) : (it.txt ?? ""))`, then, when `usableBy(it, c)` is non-empty, a single space and that suffix. Armor reads bagArmorText because the Phase 28 ARMOR-03 rule says a stowed piece shows its live durability, never its frozen txt.

- `gearConsumablesModel(state)`:
  - rows[0], the heal row: `{ key: "heal", name: GEAR_COPY.healingPotion, qty: c.potions || 0, qtyText, desc: GEAR_COPY.healingDesc, verb: GEAR_COPY.use.use, enabled: (c.potions || 0) > 0 && c.wp < c.maxWP, reason: "", dispatch: { type: "drinkPotion" } }`. This is the combat ITEMS potion row's exact enabled rule, so a Gear tap can never waste a potion at full HP.
  - Then group `(c.items || [])` entries with kind "potion" by `it.n`, in first-appearance order, into `{ key: "potion:" + it.n, name: it.n, qty: count, qtyText, desc: first.txt ?? "", verb: GEAR_COPY.use.use, enabled: true, reason: "", dispatch: { type: "useItem", i: firstIndex } }`. These stay enabled: the engine's own useRefused/pilfer line explains any refusal on the rail.
  - When `c.scrolls > 0`, push `{ key: "scroll", name: GEAR_COPY.scrolls, qty: c.scrolls, qtyText, desc: GEAR_COPY.scrollDesc, verb: GEAR_COPY.use.read, enabled: canRead(state), reason, dispatch: { type: "readScroll" } }`.
    - reason is "" when readable. Otherwise it is `LINE_FOR.scrollRefused({ reason: c.sub === "Pilfer" ? "pilfer" : "noRunes" }).text`.
    - The reason-code choice mirrors engine/magic.js#readScroll's own ternary. Name that in a comment.
  - qtyText = GEAR_COPY.qty with {n}.
  - held = potions + buff item count + scrolls; heldText = GEAR_COPY.held with {n}.
  - Return `{ held, heldText, rows }`.

- `gearKitRows(state)` returns `[{ label, value }]`, with every value a string, in the fixed order:
  - Rations (rationsValue with c.rations)
  - Spell charges, only when c.cls === "Magic User": spellChargesValue with {k} = maxCharges(c) - (c.spellsUsed || 0) and {max} = maxCharges(c)
  - c.ward: label c.ward.name, value wardValue with pool/rounds
  - c.might: strength / strengthValue
  - c.regen: regen / regenValue
  - c.mirror > 0: mirror / mirrorValue
  - c.senses: senses / sensesValue
  - c.foresight: foresight / foresightValue
  - timers["spell:reveal"] with phase "effect" and left > 0: reveal / revealValue
  - Kills: String(c.kills || 0)

  The conditions are copied from today's renderGearTab kit block. Potions, Scrolls and Wilmst are gone from this list; they now live in CONSUMABLES and the header.

**Tests.**
- Extend `test/unit/gear-view-models.test.js` with every Task 2 behavior bullet, and with edge-named tests for "Edge GSCR-04/…", "Edge GSCR-05/…" and "Edge GSCR-06/…".
- Build full-bag states with plain objects: `bag: "small"` plus slot items until `slotItems(c).length === 4`.
- Import `combatMenuViewModel` from "../../src/browser/combatMenu.js" for the heal-enabled agreement case. Use the whole state returned by `newRun(seed, [], { force: { cls: "Fighter" } })` (import newRun from "../../engine/state.js"), then set c.potions, c.wp and c.maxWP on it, so characterSheetViewModel has every field it reads.
- In `test/unit/gearTab.test.js`, add GEAR_WORN_ORDER and the eight model functions to the exports test, with typeof assertions.
- In `test/voice/safety-scan.test.js`, import GEAR_COPY from "../../src/browser/gearTab.js" and push every string leaf of GEAR_COPY (walk the nested groups) in collectAuthoredStrings, with a `// Phase 62 (GSCR-01..06)` comment.

**Full check.** Run `npm test`; it must be green with fail 0. Run `git diff --stat -- engine/ content/ test/parity/ mazeworld.html test/unit/fixtures/`; it must print nothing.
  </action>
  <verify>
    <automated>node --test test/unit/gear-view-models.test.js test/unit/gearTab.test.js test/unit/gear-panels.test.js test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js test/unit/shell-tab-snapshots.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-view-models.test.js` exits 0 with at least 40 passing tests, including names beginning "Edge GSCR-04/", "Edge GSCR-05/" and "Edge GSCR-06/"
    - each of `export function gearBagMeterModel(`, `export function gearBagCardsModel(`, `export function gearConsumablesModel(`, `export function gearKitRows(` has `grep -c` = 1 in src/browser/gearTab.js
    - `grep -c "dropShelfItems(c)" src/browser/gearTab.js` >= 1, `grep -c "LINE_FOR.scrollRefused" src/browser/gearTab.js` >= 1, `grep -c "usage.slots !== null ? GEAR_COPY.freeRide" src/browser/gearTab.js` >= 1
    - `grep -c "GEAR_COPY" test/voice/safety-scan.test.js` >= 1
    - `npm test` exits 0 (fail 0)
    - `git diff --stat -- engine/ content/ test/parity/ mazeworld.html test/unit/fixtures/` prints nothing
  </acceptance_criteria>
  <done>Every section of the new tab has a pure, tested model built only on the shared view models and the engine's own predicates. The copy is scanned. Today's renderer and every snapshot are unchanged, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file → view model | item names, txt and timer records come from a deserialized (tamperable) save and flow into model strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-01 | Tampering | gearTab.js models | low | mitigate | models pass item strings through as plain data (never HTML). Plan 02 renders them only via textContent, and every model is null-safe on a sparse or legacy c (purity tests cover it) |
| T-62-02 | Denial of service | gearWornModel/gearUseCell on a malformed timers map | low | accept | itemRowState and the engine effects helpers already tolerate a missing or odd timers map (Phase 39 pins); the models add no new timer reads |
</threat_model>

<verification>
- `npm test` green (fail 0); `node --test test/unit/gear-view-models.test.js` green
- No snapshot fixture, engine, content, parity or mazeworld.html byte changes
- The derived.js import line in gearTab.js is byte-identical; every new export sits between itemRowState and the DROP_CONFIRM_MS block
</verification>

<success_criteria>
- GSCR-01..06 have a pure model each, and every value on the future tab reads from a shared model (armorDisplay, emptySlotRows, itemRowState, bagUsage, dropShelfItems, usableBy, canRead, LINE_FOR.scrollRefused)
- All 22 model-level edge truths above have a named passing test
</success_criteria>

## Source Coverage Audit (phase-wide)

| Source | Item | Plan |
|---|---|---|
| GOAL | Tab reads like the mock: header, five-slot WORN, BAG meter, CONSUMABLES; every number from shared models | 01 (models), 02 (DOM), 03 (agreement) |
| REQ | GSCR-01 slim stat header (ARMOR RATING, WILMST) | 01 gearHeaderModel, 02 #gear-stats |
| REQ | GSCR-02 five fixed WORN rows, N / 5, value column, italic empty lines | 01 gearWornModel + emptySlotRows, 02 #gear-worn |
| REQ | GSCR-03 USE/ACTIVE/COOLING cell from itemRowState, taps deps.useItem | 01 gearUseCell, 02 .mw-gear-use |
| REQ | GSCR-04 bag used/cap + pips, red + BAG FULL line at capacity, bagUsage alone | 01 gearBagMeterModel, 02 #gear-bag-meter |
| REQ | GSCR-05 bag cards (name, desc, slot tag, · SWAP; bag-only USE) | 01 gearBagCardsModel, 02 #gear-bag |
| REQ | GSCR-06 CONSUMABLES (N HELD, ×N, USE/READ, READ says why) + rations/spell charges readout | 01 gearConsumablesModel/gearKitRows, 02 #gear-cons/#gear-kit |
| REQ | GSCR-11 ITEMS submenu / loot / store read the same itemRowState/bagUsage/lootCompare | 02 (renderCarriedList untouched, store/loot snapshots unchanged), 03 (agreement suite + no-fork guard) |
| CONTEXT | Header: slim stat header, no identity block | 01, 02 |
| CONTEXT | Section order WORN → BAG → CONSUMABLES → ALSO ON YOU; ALSO ON YOU content; potions/scrolls → CONSUMABLES, wilmst → header | 01 gearKitRows, 02 markup order |
| CONTEXT | Styling on existing tokens, PS2P heads with 3px rule, gold/red/fills, --mw-text-scale, no new fonts, HP not WP | 02 CSS |
| CONTEXT | Interim actions: › chevron, Equip/Unequip/Drop/Use in-row with the existing two-tap confirms | 02 |
| CONTEXT | WORN: five fixed rows, N / 5, Fists = empty, Cloak of Armor in CLOAK, ARMOR row keeps the plate (armorDisplay.under) | 01 |
| CONTEXT | Value column only for weapon die+magic and armor AR; jewelry/cloak —; note = txt; wear in the armor note | 01 |
| CONTEXT | Empty slots via the one emptySlotRows read + new weapon line, italics | 01 (copy/rule), 02 (italics) |
| CONTEXT | USE cell mapping from itemRowState, only for activatable items, tap = today's useItem path | 01, 02 |
| CONTEXT | Bag meter: used/cap, pips 4/6/8/10, red + line at capacity, freeRide sub-line, bag-less count only | 01, 02 |
| CONTEXT | Bag cards: slot tag family + · SWAP, desc txt + usableBy, bag-only no tag + USE, NOTHING LEFT TO CARRY | 01, 02 |
| CONTEXT | Consumables: heal row (×0 disabled), buff types grouped, SCROLLS READ greyed with engine reason, N HELD | 01, 02 |
| CONTEXT | Shared lists: own renderer in gearTab.js; renderCarriedList unchanged for store/combat/loot; no forked rule | 02, 03 |
| CONTEXT | Standing: presentation only; rail only (no toasts/inline refusal); greenfield replacement + declared snapshot regen | 01/02/03 (engine gate), 02 (snapshots) |
| CONTEXT (deferred, excluded) | Bottom action sheet, combat-greyed rows, back button, TalkBack sheet → Phase 63; Hero tab redo | not planned |
| RESEARCH | none (roadmap: no research) | n/a |

## Probe fallback record (spec-less, --auto)

The edge report (30 applicable) is resolved as follows; all are covered, none are backstopped or dismissed:
- This plan authors 22: GSCR-01 adjacency/empty/encoding/ordering; GSCR-02 boundary/adjacency/empty/ordering/precision; GSCR-03 adjacency/empty/encoding; GSCR-04 boundary/precision; GSCR-05 adjacency/empty/ordering; GSCR-06 boundary/adjacency/empty/ordering/precision.
- Plan 02 authors 3: GSCR-02 encoding, GSCR-03 ordering and GSCR-05 encoding, all DOM textContent/child-order checks.
- Plan 03 authors 5: GSCR-11 adjacency/empty/ordering/idempotency/concurrency.

Totals: 22 + 3 + 5 = 30 authored, 0 flagged-unresolved.

Prohibitions: 6 were kept across the three plans (2 here), all flagged-unverified with no check descriptor. Canon referral: innerHTML injection is covered by /gsd-secure-phase and the T-38-11 createElement/textContent convention, so it is not minted here (Plan 02 keeps it as a tested truth).

## Artifacts this phase produces

This plan's share. Plans 02 and 03 list theirs.

| Artifact | Kind | Where |
|---|---|---|
| GEAR_COPY extended (header, WORN, bag, consumables, kit, act, use, weapon empty line) | frozen copy object (hp-not-wp + voice scanned) | src/browser/gearTab.js |
| emptySlotRows(c) with the weapon row | shared view model (extended) | src/browser/gearTab.js |
| GEAR_WORN_ORDER | frozen display-order constant | src/browser/gearTab.js |
| gearHeaderModel / gearUseCell / gearWornModel | pure view models | src/browser/gearTab.js |
| gearBagMeterModel / gearBagCardsModel / gearConsumablesModel / gearKitRows | pure view models | src/browser/gearTab.js |
| test/unit/gear-view-models.test.js | new unit suite (behavior, edges, purity) | test/unit/ |

<output>
Create `.planning/phases/62-gear-tab-layout-rebuild/62-01-SUMMARY.md` when done. Record the exact return shapes of every new export (Plan 02 reads them), the test count, the `npm test` result, and confirmation that no snapshot fixture moved.
</output>
