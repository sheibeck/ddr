---
phase: 62-gear-tab-layout-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["62-02"]
files_modified:
  - test/unit/gear-agreement.test.js
  - docs/SHELL-MODULES.md
  - docs/CLARITY.md
  - docs/SPELLS.md
  - docs/GEAR-SLOTS.md
autonomous: true
requirements: [GSCR-11]

must_haves:
  truths:
    - "For every item the Gear tab shows a USE cell for, the cell's data-phase equals itemRowState(state, it).kind, and the combat ITEMS submenu row for the same item (combatMenuViewModel(state).submenus.items, id item-<i> or worn-<slot>) carries cost === itemRowState(state, it).text for the same state at the same moment"
    - "The Gear tab's HEALING POTION availability equals the combat ITEMS potion row's enabled flag, and its quantity equals c.potions (the combat row's '<n> LEFT'), across several states"
    - "The Gear BAG head count equals bagUsage(c).text, the same readout the loot card and find card reach through window.__mzBagUsage and the store reads through its direct gearTab.js import. A bag the Gear tab shows as full is a bag the store reports full"
    - "A no-fork source guard holds. Gear-tab code (the Plan 01 model region and the renderGearTab region of gearTab.js, comment-stripped) never calls bagCap/canStow/slotItems (capacity only via bagUsage), isReady/remaining/activationFor/itemTimerId/chargesTimerId (state only via itemRowState), armorSoak (AR only via armorDisplay) or weaponUpgradeDelta/expectedStrike (verdicts only via lootCompare). combatMenu.js still imports itemRowState from ./gearTab.js, storeScreen.js still imports bagUsage and storeRowState, the module script still bridges window.__mzBagUsage = bagUsage and window.__mzLootCompare = lootCompare, and renderCarriedList still reads itemRowState(state, it) and lootCompare(state.c, it).equipNow"
    - "docs/SHELL-MODULES.md lists all 14 tabDeps keys (drinkPotion and readScroll included) and names the Gear-tab view models. docs/CLARITY.md, docs/SPELLS.md and docs/GEAR-SLOTS.md carry dated Phase 62 notes that supersede the ON YOU / BAG / #s-kit descriptions without rewriting the history"
    - "Edge GSCR-11/adjacency: at the same moment the same worn jewel reads, for example, COOLING '12 SQ' on the Gear tab and 'cd 12 SQ' on the ITEMS submenu, both from one itemRowState result. After a squares tick in the state (timer left - 1), both move together"
    - "Edge GSCR-11/empty: the empty-bag Magic User fixture shows the Gear 'NOTHING LEFT TO CARRY' state and the store's sell list with its sell-head hidden, both from the same empty c.items"
    - "Edge GSCR-11/ordering: the Gear bag cards' data-i sequence equals dropShelfItems(c).map(e => e.i) (the loot and find drop-shelf list) and ascends through the takesBagSlot items of c.items"
    - "Edge GSCR-11/idempotency: painting the same state twice through the shell sandbox serializes SNAPSHOT_IDS.gear byte-identically, with the same li.mw-gear-card count"
    - "Edge GSCR-11/concurrency: arming Drop on one bag card and then on another leaves exactly one .mw-drop-confirm under #gear-bag and restores the first card's Drop button. A paint() while a confirm is armed leaves #gear-bag with zero armed confirms"
  artifacts:
    - path: "test/unit/gear-agreement.test.js"
      provides: "cross-screen agreement sweep, no-fork source guard, idempotency and one-armed-confirm checks"
      min_lines: 180
    - path: "docs/SHELL-MODULES.md"
      provides: "14-key deps contract + Gear-tab view models in 'What stays shared'"
      contains: "drinkPotion"
  key_links:
    - from: "test/unit/gear-agreement.test.js"
      to: "src/browser/combatMenu.js#combatMenuViewModel"
      via: "same-state comparison of the ITEMS rows against the rendered Gear USE cells"
      pattern: "combatMenuViewModel\\("
    - from: "test/unit/gear-agreement.test.js"
      to: "test/unit/harness/shellSandbox.js#loadShellSandbox"
      via: "real paint()/renderEncounter() of the Gear tab and the store from the same fixed states"
      pattern: "loadShellSandbox\\("
  prohibitions:
    - requirement_id: GSCR-11
      category: transparency
      flagged: true
      statement: "MUST NOT let the Gear tab show a use state, bag count or potion availability that the player would see differently on the ITEMS submenu, the loot screen or the store at the same moment"
---

<objective>
Prove GSCR-11: the Gear tab can never disagree with the ITEMS combat submenu, the loot screen or the store, because all of them read the same `itemRowState` / `bagUsage` / `lootCompare`. This plan adds a cross-screen agreement sweep over real painted states, a source guard that no Gear-tab code forks a shared rule, and the idempotency and one-armed-confirm checks. It then brings the docs in line with the new layout and runs the phase's final gates.

Purpose: GSCR-11 is an invariant, not a feature, so it gets its own suite rather than riding on Plan 02's DOM tests. The docs catch up to the new tab and the two new deps keys.

Implements CONTEXT "Shared lists (GSCR-11) … all of them read the same itemRowState / bagUsage / lootCompare view models, so a state shown on the Gear tab can never disagree with another screen. Do not fork a copy of any of those rules."

Known, deliberate difference (not an agreement failure): mid-fight, the combat ITEMS submenu keeps its SCROLL row tappable even when the hero cannot read, so the engine's refusal lands in the fight log (the Phase 34 row ruling). The Gear tab greys READ and states that same engine reason (the locked CONTEXT consumables decision). The sweep compares scroll COUNTS only. Phase 63 revisits combat-time greying.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/62-gear-tab-layout-rebuild/62-CONTEXT.md
@.planning/phases/62-gear-tab-layout-rebuild/62-01-SUMMARY.md
@.planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
- test/unit/harness/shellSandbox.js: `loadShellSandbox({ doc })` returns `{ setState, paint, renderEncounter, doc, context }`; `fixedStates()` returns `{ thief, mu, thiefStore, muStore }`; `SNAPSHOT_IDS.gear` holds the ten gear ids (Plan 02). The sandbox's setTimeout never fires, so an armed confirm stays armed. Deps inside the sandbox are the real `tabDeps()` closures (the window.mz* engine bridges are absent, so taps are no-ops). For spy-level checks, call `renderGearTab(host, state, spyDeps)` into `createRecordingDocument()` directly.
- src/browser/combatMenu.js: `combatMenuViewModel(state)` returns `{ submenus: { items: { rows } } }`, whose rows include:
  - `{ id: "potion", cost: "<n> LEFT", enabled }`, present whenever at least one usable exists;
  - `{ id: "scroll", cost: "<n> LEFT" }`;
  - `{ id: "item-<i>", cost: itemRowState(...).text }` for bag rows (potion items or anything with an activation);
  - `{ id: "worn-<slot>", cost }` for worn activatables.

  It never throws with `state.combat === null`.
- src/browser/gearTab.js (after Plan 02): the rendered `.mw-gear-use[data-phase]` sits inside `li.mw-gear-row[data-slot]` (worn) and `li.mw-gear-card[data-i]` (bag). `#gear-bag-head .mw-gear-head-count`; the `li.mw-empty` empty state. The models are exported (see 62-01-SUMMARY.md).
- Timer shapes: `c.timers["item:<name>"] = { cadence: "squares", left, phase }`; staff `it.charges` plus `c.timers["charges:<name>"]` (test/unit/itemRowState.test.js).
- Store surface: `sandbox.renderEncounter()` on thiefStore/muStore renders `#sell-head` / `#sell-list` / the bag-full line; the thief-store.store snapshot contains "Bag full (".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-screen agreement sweep, no-fork source guard, idempotency and one-armed-confirm checks</name>
  <files>test/unit/gear-agreement.test.js</files>
  <read_first>
    - src/browser/gearTab.js (after Plan 02: the model region between itemRowState and the DROP_CONFIRM_MS block, renderCarriedList and renderGearTab)
    - src/browser/combatMenu.js :233-300 (the ITEMS rows)
    - src/browser/storeScreen.js (the imports and the bag-full line)
    - mazeworld.html :4088-4125 (the loot card's __mzBagUsage/__mzLootCompare reads) and :5400-5430 (the module bridge assignments)
    - test/unit/harness/shellSandbox.js :468-567 (SNAPSHOT_IDS, fixedStates)
    - test/unit/shell-tab-snapshots.test.js (the paintFresh pattern)
    - test/unit/itemRowState.test.js (timer/staff fixture shapes)
    - .planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md
  </read_first>
  <behavior>
    - The state sweep covers at least 6 states:
      - fixedStates().thief (full small bag, two worn jewels, a bagged jewel, rope, a potion item);
      - fixedStates().mu (empty bag);
      - a thief clone with jewelry1's item timer in effect (left 7) and jewelry2's cooling (left 3);
      - a Magic User clone with a staff in the bag below max charges plus a "charges:<name>" record, and a torch;
      - a clone at full HP with potions 2;
      - a clone at wp < maxWP with potions 0.
    - USE-cell agreement (Edge GSCR-11/adjacency), for every rendered .mw-gear-use:
      - resolve the item: a worn row maps data-slot to c.worn[slot]; a bag card maps data-i to c.items[i];
      - data-phase must equal itemRowState(state, it).kind;
      - the combatMenuViewModel ITEMS row with id `worn-<slot>` / `item-<i>` exists and has cost === itemRowState(state, it).text.
    - Advancing each timer's left by -1 in a clone moves the Gear sub and the combat cost together.
    - Heal agreement: in every state with c.potions > 0, the Gear heal button's disabled flag === !(combat potion row).enabled and the Gear heal qty text === "×" + c.potions. At potions 0 the Gear button is disabled.
    - bagUsage agreement: the Gear bag head count === bagUsage(c).text in every state. For thiefStore the Gear head count carries mw-gear-full, and the store render (sandbox.renderEncounter()) contains "Bag full (".
    - Empty (Edge GSCR-11/empty): for mu/muStore, #gear-bag holds li.mw-empty with GEAR_COPY.bagEmptyHead, and after renderEncounter() on muStore, #sell-head is hidden.
    - Ordering (Edge GSCR-11/ordering): the Gear bag cards' data-i sequence deep-equals dropShelfItems(c).map((e) => e.i), which ascends and skips kind "potion" items.
    - Idempotency (Edge GSCR-11/idempotency): paint() twice on the same sandbox gives byte-identical serializeElements(SNAPSHOT_IDS.gear) and the same li.mw-gear-card count.
    - Concurrency (Edge GSCR-11/concurrency):
      - In the thief sandbox, click the Drop button of bag card A, then of card B. Exactly one .mw-drop-confirm is left under #gear-bag, and card A again holds a "Drop" button.
      - Then paint() again: #gear-bag holds zero .mw-drop-confirm and zero .mw-swap-confirm.
    - No-fork source guard, on the comment-stripped gearTab.js:
      - the model region (from `export const GEAR_WORN_ORDER` to `const DROP_CONFIRM_MS`) and the renderGearTab region (from `export function renderGearTab(` to EOF) contain none of `bagCap(`, `canStow(`, `slotItems(`, `isReady(`, `remaining(`, `activationFor(`, `itemTimerId(`, `chargesTimerId(`, `armorSoak(`, `weaponUpgradeDelta`, `expectedStrike`, `usedAt`;
      - combatMenu.js contains `import { itemRowState } from "./gearTab.js";`;
      - storeScreen.js imports bagUsage from "./gearTab.js" and storeRowState from "./viewModels.js";
      - mazeworld.html contains `window.__mzBagUsage = bagUsage;` and `window.__mzLootCompare = lootCompare;`;
      - the renderCarriedList region still contains `itemRowState(state, it)` and `lootCompare(state.c, it).equipNow`.
  </behavior>
  <action>
Create `test/unit/gear-agreement.test.js`, covering every behavior bullet. Name each edge test with its id prefix ("Edge GSCR-11/adjacency: …" and so on).

Setup:
- Call `setIdentityDials()` from ./harness/identityDials.js first, as shell-tab-snapshots.test.js does, so the sandbox numbers match the fixtures.
- Build the sweep states with `structuredClone` of the fixedStates plus direct field edits for the timer/staff/HP variants. These are test-only states that never reach a fixture. Use the timer shapes from test/unit/itemRowState.test.js; for a real staff, take one from content/treasure-tables.js STAVES with `kind: "staff"`, so activationFor resolves.
- Render the Gear tab with `loadShellSandbox({ doc: createRecordingDocument() })` + `setState` + `paint()` for the painted checks.
- Render the store with `renderEncounter()` on thiefStore/muStore.
- For the spy-free agreement checks, read the rendered DOM by walking `doc.elementsById.get(id).children` (write a small `findAll(root, cls)` helper, as gear-tab-dom.test.js does).

For the source guard:
- Read gearTab.js, combatMenu.js, storeScreen.js and mazeworld.html CRLF-normalized.
- Strip comments with the same stripComments helper the shell-*.test.js suites use (line comments first, then block comments).
- Slice the regions by the literal markers named in the behavior block, asserting that each marker is found.

This is a test-only plan: if any agreement assertion fails, the fix belongs in the Gear model or renderer (src/browser/gearTab.js, Plans 01-02 territory). Make that fix in the smallest possible edit, record it in the SUMMARY, and never loosen the assertion.
  </action>
  <verify>
    <automated>node --test test/unit/gear-agreement.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-agreement.test.js` exits 0 with at least 12 passing tests, including names beginning "Edge GSCR-11/adjacency", "Edge GSCR-11/empty", "Edge GSCR-11/ordering", "Edge GSCR-11/idempotency" and "Edge GSCR-11/concurrency"
    - the sweep covers at least 6 states, and the USE-cell agreement loop asserts at least 10 (item, state) pairs. Record the count in the SUMMARY
    - `grep -c "combatMenuViewModel(" test/unit/gear-agreement.test.js` >= 1 and `grep -c "loadShellSandbox(" test/unit/gear-agreement.test.js` >= 1
  </acceptance_criteria>
  <done>A sweep over real painted states proves the Gear tab, the ITEMS submenu, the loot drop shelf and the store show the same state, count and order. A source guard proves the Gear-tab code forks none of the shared rules, and the idempotency and one-armed-confirm invariants are pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Docs catch up to the new tab; phase-final gates</name>
  <files>docs/SHELL-MODULES.md, docs/CLARITY.md, docs/SPELLS.md, docs/GEAR-SLOTS.md</files>
  <read_first>
    - docs/SHELL-MODULES.md :1-30 (the Contract deps list) and :90-100 ("What stays shared")
    - docs/CLARITY.md :112-125 (the "Gear screen (CLAR-04)" section) and :135 (the traceability row)
    - docs/SPELLS.md :680-700 ("The Hero-tab kit rows")
    - docs/GEAR-SLOTS.md :355-365 (the Phase 43 bullet) and the end of the file
    - .planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md
  </read_first>
  <action>
**docs/SHELL-MODULES.md**
- In the Contract's `deps` bullet, add `drinkPotion` and `readScroll` to the key list (now 14 keys). Add a sentence saying the Gear tab's consumables dispatch through them.
- In "What stays shared", add a paragraph:
  - src/browser/gearTab.js also exports the Gear-tab view models (GEAR_WORN_ORDER, gearHeaderModel, gearUseCell, gearWornModel, gearBagMeterModel, gearBagCardsModel, gearConsumablesModel, gearKitRows).
  - itemRowState and bagUsage stay the shared rules that combatMenu.js, storeScreen.js and the loot card read.
  - The Gear tab's interim in-row Equip / swap / Drop confirms are renderCarriedList's own, harvested per card, until the Phase 63 sheet.
- Do not touch the generated bridge table between the bridge-table markers.

**docs/CLARITY.md**, "Gear screen (CLAR-04)" section: add a dated "> **Phase 62 update (2026-09-23, v1.9):**" note saying:
- the two-panel ON YOU / BAG layout is superseded by the mock layout: a slim ARMOR RATING / WILMST header, then WORN (five fixed rows), BAG (meter + cards), CONSUMABLES and ALSO ON YOU;
- emptySlotRows now also yields the weapon row;
- the bag-only drop shelf (dropShelfItems) is unchanged;
- see src/browser/gearTab.js.

Leave the historical text in place.

**docs/SPELLS.md**, "The Hero-tab kit rows": add a dated Phase 62 note. The rows now live in the Gear tab's ALSO ON YOU list (`#gear-kit`), built from `gearTab.js#gearKitRows` with copy in `GEAR_COPY.kit`, with the same fields, gates and values.

**docs/GEAR-SLOTS.md**: after the Phase 43 bullet, add a dated Phase 62 note:
- the Gear tab displays the worn keys as WEAPON, ARMOR, CLOAK, JEWELRY 1, JEWELRY 2 (GEAR_WORN_ORDER, a display order only; WORN_SLOTS is unchanged);
- bare Fists reads as an empty weapon slot;
- an active Cloak of Armor sits in the CLOAK row, while the ARMOR row shows the plate underneath;
- a bagged cloak or jewel offers no USE, since it must be worn to work.

Every doc edit is voice-neutral and uses HP, never the old two-letter token.

**Phase gates.**
- `npm test` must be green (fail 0).
- `npm run build:www` must exit 0.
- Run `npm run boot:check`. Record exit 0, or the documented environment block from STATE.md verbatim.
- `git diff --stat -- engine/ content/ test/parity/` must print nothing across the whole phase: `git diff --stat <phase-62 base commit>..HEAD -- engine/ content/ test/parity/`, where the base is the commit before 62-01's first commit.
- The prototype master hash is unchanged: `git hash-object test/parity/prototype-master.js.txt` equals a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
  </action>
  <verify>
    <automated>node --test test/unit/bridge-registry.test.js test/unit/gear-agreement.test.js && npm test && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "drinkPotion" docs/SHELL-MODULES.md` >= 1, `grep -c "readScroll" docs/SHELL-MODULES.md` >= 1 and `grep -c "gearWornModel" docs/SHELL-MODULES.md` >= 1
    - `grep -c "Phase 62" docs/CLARITY.md` >= 1, `grep -c "gearKitRows" docs/SPELLS.md` >= 1 and `grep -c "GEAR_WORN_ORDER" docs/GEAR-SLOTS.md` >= 1
    - `node --test test/unit/bridge-registry.test.js` exits 0 (the generated bridge table is untouched)
    - `npm test` exits 0 (fail 0); `npm run build:www` exits 0; `npm run boot:check` exits 0, or the SUMMARY records the documented environment block verbatim
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0, and the phase-wide `git diff --stat` over engine/, content/ and test/parity/ prints nothing
  </acceptance_criteria>
  <done>The docs describe the tab as built and the deps contract names all 14 keys. The phase closes green, with no engine, content or parity bytes moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | test-only and docs-only plan; no runtime surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-05 | Spoofing | Gear tab state vs other screens | medium | mitigate | the agreement sweep (same state, same moment) plus the no-fork source guard make any future drift a red test |
</threat_model>

<verification>
- `node --test test/unit/gear-agreement.test.js` green; `npm test` green; `npm run build:www` exits 0; boot:check passes or records the documented block
- The prototype master hash is unchanged; engine/, content/ and test/parity/ are untouched across the phase
</verification>

<success_criteria>
- GSCR-11: a red test would catch any disagreement between the Gear tab and the ITEMS submenu, the loot drop shelf or the store, and any Gear-tab fork of itemRowState/bagUsage/lootCompare/armorDisplay
- All five GSCR-11 edge truths have named passing tests
</success_criteria>

## Artifacts this phase produces

This plan's share. Plans 01 and 02 list theirs.

| Artifact | Kind | Where |
|---|---|---|
| test/unit/gear-agreement.test.js | new cross-screen agreement + no-fork guard suite | test/unit/ |
| 14-key deps contract + Gear view models under "What stays shared" | docs | docs/SHELL-MODULES.md |
| dated Phase 62 notes (layout, kit rows, display order) | docs | docs/CLARITY.md, docs/SPELLS.md, docs/GEAR-SLOTS.md |

<output>
Create `.planning/phases/62-gear-tab-layout-rebuild/62-03-SUMMARY.md` when done. Record the sweep's state count and pair count, any source fix the sweep forced, the phase-wide engine/content/parity diff (empty), the prototype master hash, and the npm test / build:www / boot:check results.
</output>
