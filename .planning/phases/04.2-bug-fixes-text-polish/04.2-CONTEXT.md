# Phase 04.2: Bug Fixes & Text Polish — Context

**Created:** 2026-09-09 (DR14 device-review batch). Consolidates the rules-text audit + live-play bug reports into one GSD phase, per user direction ("batch and phase all the little fixes under one new bug fix phase").

## Fix inventory (source of truth)
The detailed, evidence-backed inventory lives in the quick-task dir (not duplicated here):
- `.planning/quick/20260909-rules-text-audit-pass/FINDINGS.md` — the audit: A1 (affliction narration bug), A2 (flying), A3 (rest cure-roll); Part-B P1/P2/P3 text/rule findings; **LOCKED decisions**.
- `.planning/quick/20260909-rules-text-audit-pass/EXTRA-SCOPE.md` — E1–E9 (live bugs + terminology), each root-caused with file:line.
- `.planning/quick/20260909-rules-text-audit-pass/SUMMARY-batch1.md` — the already-landed rules work.

## Locked decisions (from FINDINGS)
- **A1 bucket:** ENCOUNTER_TABLES `"Disease"` → kind-neutral **"Ailment"**.
- **A2 flying:** built the **Cloak charge/cooldown** (20-sq / 50-sq) + Bracelet always-on. ✅ landed.
- **A3 cure roll:** rest = **50% base + Hardiness** (`rng.d(20) <= 10 + (Hardiness?4:0)`), `afflictionLingers` on fail. ✅ landed.
- **E1 Teleport:** remove the duplicate.
- **E2 (combat-item use in the combat bar):** **DEFERRED** to the Economy & Item Balancing milestone (needs the same "use item" plumbing that milestone builds).

## Execution batches (executor subagents; sequential where files overlap)
1. **Batch 1 (rules)** — A2 flying charge/cooldown, A3 cure-roll, A1 engine `kind`. ✅ **DONE, 545/545.**
2. **Bugs A** — E5 poison-at-1hp auto-clear (`engine/movement.js:214`), E4 foe HP current/max (`encounterStarted` drops `maxWP`), E6 clear Oracle on new character. ⏳ **running.**
3. **Bugs B** — E7 backstab "Critical" fires on a miss (`engine/combat.js:312-323`), E8 Cloak-of-Armor inert `eff.cloakArmor` → act as Plate (mirror the `eff.fly` wiring), E9 dead character missing from Dead/graveyard screen (likely the Phase-5 "re-fetch-on-open" class of bug). Touches combat.js/derived.js/adapter/mazeworld.html → run AFTER Bugs A.
4. **Text** — A1 narration + Disease→Ailment, E1 dup Teleport, E3 SP→XP/WP→HP (combat toasts `mazeworld.html:2545/2552` + `content/encounters.js:12` + `content/treasure-tables.js:47` with their **dual-purpose engine parsers**), P1 tableFour/meetFaerie jargon→prose + `stripRollDetail` scoping, P2 `wm`→`wilmst` (`eventNarration.js:148,229,230`), P3 cosmetics. Run AFTER Bugs A/B (shares mazeworld.html + eventNarration.js).

## Hard constraints (every batch)
Engine PURE/DETERMINISTIC; new rng only in the qualifying non-chargen situation; NO state-field/event-type renames (player-facing TEXT + matching parser keys only); dual-purpose table strings change with their parser atomically; new event types need `EVENT_NARRATION` (formatEventsCoverage guard); parity carve-outs (mirror 04.1's `stripDarkForField`) for any new serialized `c.*` field; `npm test` full suite stays green (currently 545/545). No git commits (working tree staged for on-device review).

## Post-compact plan
Execute Bugs B → Text via GSD, `npm test` green, build+deploy to Pixel 7, then this phase is done. THEN start the milestone planning (the "something new").
