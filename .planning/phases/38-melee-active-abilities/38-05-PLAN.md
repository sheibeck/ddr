---
phase: 38-melee-active-abilities
plan: 05
type: execute
wave: 5
depends_on: ["38-03", "38-04"]
files_modified:
  - src/browser/combatMenu.js
  - src/browser/viewModels.js
  - src/browser/rail.js
  - mazeworld.html
  - test/unit/combatMenu.test.js
  - test/unit/shell-combat-actions.test.js
  - test/unit/shell-abilities.test.js
  - test/unit/rail.test.js
  - test/unit/viewModels.test.js
  - docs/ABILITIES.md
autonomous: true
requirements: [ABIL-01, ABIL-03, ABIL-04]
tags: [shell, combat-menu, hero-tab, rail-card, first-paint, docs-ledger, phase-close, deferred-uat]
notes:
  rng_draw_impact: "zero — the shell only dispatches the existing useAbility action through the same dispatch() -> applyAction() seam every combat button uses; every view-model read (isReady/abilityRoundsLeft/ABILITY_BY_ID) is pure; the first-paint pool card reads c.abilities already rolled by newRun."
  serialized_field_impact: "none — window.__mzAbilities is a read-only bridge; the pool card is built from state.c at commit time and pushed to the shell-local rail; no field on S/state."
  bard_ruling: "The Bard keeps Sing as the FIRST row of its ABILITIES submenu (CONTEXT: 'Bard's Sing — keep it'); because a Bard is a Fighter it also rolls abilities, so its ability rows follow the Sing row in the same submenu — nothing a Bard rolled is unreachable."

must_haves:
  truths:
    - "combatMenuViewModel: a Fighter/Thief with c.abilities = ['kata', 'brace'] gets actions[1] = { key: 'abilities', num: 2, label: '2 · ABILITIES', sub: '2/2 READY', enabled: true, accent: false, opens: 'abilities' } and submenus.abilities.rows = [{ id: 'ability-kata', label: 'KATA', cost: 'READY', desc: <catalog txt>, enabled: true, dispatch: { type: 'useAbility', key: 'kata' } }, { id: 'ability-brace', ... }]; with kata on a 3-round cooldown the row's cost is '3 ROUNDS' (1 -> '1 ROUND'), enabled stays true, and the sub reads '1/2 READY'; a once-a-fight ability already used reads 'ONCE A FIGHT · USED'; a Fighter with abilities: [] (or no key) still gets today's disabled NOTHING UP YOUR SLEEVE fallback; a Magic User is unchanged; a Bard's submenu is [sing row (byte-identical), ...ability rows] with the sing sub-line unchanged"
    - "mazeworld.html: COMBAT_DISPATCH.useAbility === (d) => window.mzUseAbility?.(d.key); window.mzUseAbility = (key) => engineCombatAction('useAbility', { key }); test/unit/shell-combat-actions.test.js re-pinned to nine dispatch types"
    - "Hero tab: a new #s-abilities list beside #s-skills renders one row per c.abilities entry — <b>{name}</b> + a provenance tag ('special skill · active' for source table, 'trick' for pool) + <i>{txt}</i> + a state suffix: in combat 'READY' / 'N rounds' / 'once a fight · used', out of combat 'cd N rounds' / 'once a fight'; the #s-skills list still renders only c.skills (kept passives, byte-identical); a Magic User shows the abilities list's none row 'Spells are the trick.'; window.__mzAbilities = { byId, roundsLeft, isReady } is bridged from content/abilities.js + engine/abilities.js + engine/effects.js in the module block"
    - "First paint: commitRolledState(state) calls surfaceAbilityPool(state), which pushes rail.js#abilityPoolCard(state.c) (title RAIL_COPY.abilityPool.title 'UP YOUR SLEEVE', line 'New trick: {name} — {txt}' for the level-1 pool pick, tone good, hold RAIL_HOLD.level, icon ★) via window.mzRailLine and logs the same line to the Oracle; a Magic User (no pool pick) pushes nothing; the dev start-at-depth path reaches the same call"
    - "mzCombatReport(d) appends 'New trick: {name}.' for each d.learned entry (names of abilityLearned events in the fight); noteCombat passes learned: [...]"
    - "characterSheetViewModel(state).abilities is an array of { id, name, description, source, state } mirroring the Hero-tab rows (pure, tested)"
    - "npm run build:www exits 0; npm test prints '# fail 0'; fixtures untouched; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; grep -c fonts.googleapis mazeworld.html == 0; store-listing/ and tools/store-screenshots/ never staged in any Phase 38 commit"
    - "docs/ABILITIES.md carries the UI section and the requirement -> plan map; the SUMMARY aggregates every plan's deferred Pixel 7 items into one numbered, plan-grouped checklist"
  artifacts:
    - path: "src/browser/combatMenu.js"
      provides: "abilityRows(state), the melee ABILITIES branch, the Bard Sing+rows branch, COMBAT_MENU_COPY additions"
    - path: "mazeworld.html"
      provides: "COMBAT_DISPATCH.useAbility, window.mzUseAbility, #s-abilities Hero-tab list, window.__mzAbilities bridge, surfaceAbilityPool(), mzCombatReport learned line"
    - path: "src/browser/rail.js"
      provides: "RAIL_COPY.abilityPool, abilityPoolCard(c)"
    - path: "src/browser/viewModels.js"
      provides: "abilities[] on characterSheetViewModel"
    - path: "test/unit/shell-abilities.test.js"
      provides: "source pins for every mazeworld.html change + build artefact sanity"
  key_links:
    - from: "src/browser/combatMenu.js"
      to: "engine/abilities.js#abilityRoundsLeft + engine/effects.js#isReady"
      via: "cost: isReady ? READY : (once && !ready ? USED : `${n} ROUND(S)`)"
      pattern: "abilityRoundsLeft\\("
    - from: "mazeworld.html COMBAT_DISPATCH"
      to: "engine useAbility action"
      via: "useAbility: (d) => window.mzUseAbility?.(d.key) -> engineCombatAction('useAbility', { key })"
      pattern: "useAbility: \\(d\\) => window\\.mzUseAbility\\?\\.\\(d\\.key\\)"
    - from: "mazeworld.html commitRolledState"
      to: "src/browser/rail.js#abilityPoolCard"
      via: "surfaceAbilityPool(state) -> window.mzRailLine(card.title, card.line, card.tone, card.hold, card.icon)"
      pattern: "surfaceAbilityPool\\("
  prohibitions:
    - "No engine/ or content/ edit in this plan; no fixture or master edit"
    - "No new toast host, no innerHTML-built rows from untrusted strings other than catalog literals (rows via textContent/createElement like the Phase 34 cbRow); no timers/setTimeout for the pool card beyond mzRailLine's own hold"
    - "No checkpoint:human-verify, no device step, no adb, no APK build — the aggregated checklist is written, not executed; explicit-path commits only"
---

<objective>
Put abilities on the screen and close the phase: the combat ABILITIES submenu's third branch (rows: label, cost "READY"/"N ROUNDS"/"ONCE A FIGHT · USED", one-line effect, `dispatch: { type: "useAbility", key }`, tappable on cooldown — D: "Submenu rows"), the shell dispatch bridge, the Hero-tab abilities list beside Special skills with converted entries shown in their active form (D: "Hero tab"), the level-1 pool roll narrated once on the first paint as a rail card and the level-up card's second line (D: "Events" level-up folding, "first paint"), the end-of-fight report's learned line, the `docs/ABILITIES.md` UI/requirement sections, the phase gate, and the ONE aggregated Pixel 7 checklist for the milestone-close UAT batch (deferred-UAT protocol).

Purpose: SC-1 (a Fighter/Thief opens the submenu mid-combat and sees at least one usable ability with a clear ready/N-rounds state) and the legibility half of ABIL-04 are shell work; everything underneath is already proven.
Output: `combatMenu.js`/`viewModels.js`/`rail.js`/`mazeworld.html` edits, `test/unit/shell-abilities.test.js` + extended menu/rail/viewModel tests, the re-pinned dispatch test, the ledger's final sections, the phase-close SUMMARY with the aggregated checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-melee-active-abilities/38-CONTEXT.md
@.planning/phases/38-melee-active-abilities/38-01-SUMMARY.md
@.planning/phases/38-melee-active-abilities/38-02-SUMMARY.md
@.planning/phases/38-melee-active-abilities/38-03-SUMMARY.md
@.planning/phases/38-melee-active-abilities/38-04-SUMMARY.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-04-SUMMARY.md
@src/browser/combatMenu.js
@src/browser/rail.js
@src/browser/viewModels.js
</context>

<artifacts_this_phase_produces>
See 38-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list. This plan creates the **[05]** items: `src/browser/combatMenu.js#abilityRows(state)` (module-private), the `c.abilities.length` branch + the Bard branch extension, `COMBAT_MENU_COPY.abilityReady` ("READY"), `.abilityUsedUp` ("ONCE A FIGHT · USED"), `.abilityRound` ("1 ROUND"), `.abilityRounds` ("{n} ROUNDS"), `.abilitiesSub` ("{ready}/{n} READY"); `src/browser/viewModels.js#characterSheetViewModel(...).abilities` (`{ id, name, description, source, state }`), `VIEW_COPY`-style literals `abilityStateReady` ("READY"), `abilityStateRounds` ("{n} rounds"), `abilityStateUsed` ("once a fight · used"), `abilityStateCd` ("cd {n} rounds"), `abilityStateOnce` ("once a fight"), `abilityTagTable` ("special skill · active"), `abilityTagPool` ("trick"), `noAbilitiesCaster` ("Spells are the trick."); `src/browser/rail.js#RAIL_COPY.abilityPool` (`{ title: "UP YOUR SLEEVE", line: "New trick: {name} — {txt}" }`) + `export function abilityPoolCard(c)`; `mazeworld.html`: `COMBAT_DISPATCH.useAbility`, `window.mzUseAbility(key)`, `#s-abilities` `<ul>` + its `<h3>`/label in the Hero tab markup, the `renderAbilityRows(c)` paint-local helper, `window.__mzAbilities`, `surfaceAbilityPool(state)`, `mzCombatReport`'s `d.learned` line and `noteCombat`'s `learned` field; `test/unit/shell-abilities.test.js`; re-pins in `test/unit/shell-combat-actions.test.js` (nine dispatch types); `docs/ABILITIES.md` "UI" + "Requirements map" sections; the aggregated Pixel 7 checklist (SUMMARY).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: combatMenu.js ABILITIES branch (+ Bard rows) and viewModels.js abilities[] + rail.js abilityPoolCard — pure modules, tests first</name>
  <files>src/browser/combatMenu.js, src/browser/viewModels.js, src/browser/rail.js, test/unit/combatMenu.test.js, test/unit/viewModels.test.js, test/unit/rail.test.js</files>
  <read_first>
    - src/browser/combatMenu.js (all — the isCaster/isBard/else chain at 82-130, COMBAT_MENU_COPY, the pure-module header rules)
    - src/browser/viewModels.js lines 1-30 (imports), 262-295 (characterSheetViewModel's skills[] and return shape)
    - src/browser/rail.js lines 55-115 (RAIL_COPY), 370-386 (wornReconcileCard — the direct-card shape `{ title, line, tone, hold, icon }`), 45-54 (RAIL_HOLD)
    - engine/abilities.js (abilityRoundsLeft), engine/effects.js (isReady), content/abilities.js (ABILITY_BY_ID, ONCE_A_FIGHT, ABILITY_POOL)
    - test/unit/combatMenu.test.js (all — fixed* helpers, the Fighter default-grid test at 73-107, the Bard test at 108-123, the COPY voice test at 309+), test/unit/rail.test.js (the wornReconcileCard tests — grep `wornReconcileCard`), test/unit/viewModels.test.js (the characterSheetViewModel skills tests — grep `skills`)
    - .planning/phases/38-melee-active-abilities/38-RESEARCH.md §"Research Question 7"
  </read_first>
  <behavior>
    - combatMenu: the must_haves rows/sub/cost shapes for a Fighter with ['kata', 'brace'] (plant `c.timers` records via startEffect/startCooldown to drive READY/3 ROUNDS/1 ROUND/ONCE A FIGHT · USED — a secondWind record with left 999 reads USED; a sidestep effect record with left 2 + cd 4 reads '6 ROUNDS'); rows follow c.abilities order; every row `enabled: true`; the id prefix is `ability-`; a c with abilities [] falls to today's fallback (the existing Fighter default-grid test passes unchanged); a Bard with ['kata'] has rows[0] deepStrictEqual today's sing row and rows[1] the kata row; a Magic User's slot 2 is unchanged (existing tests pass); state.combat === null never throws
    - viewModels: characterSheetViewModel(state).abilities for a Thief with ['feint', 'mark'] out of combat -> [{ id: 'feint', name: 'Feint', description: <txt>, source: 'table', state: 'cd 3 rounds' }, { id: 'mark', ..., source: 'pool', state: 'once a fight' }]; in combat with feint on cooldown 2 -> state '2 rounds'; ready in combat -> 'READY'; a used once-a-fight in combat -> 'once a fight · used'; a Magic User -> []; skills[] unchanged
    - rail: abilityPoolCard({ cls: 'Fighter', abilities: ['kata', 'brace'] }) -> { title: 'UP YOUR SLEEVE', line: 'New trick: Brace — halve the next blow that lands on you', tone: 'good', hold: RAIL_HOLD.level, icon: '★' } (the FIRST pool-source id in c.abilities); abilityPoolCard({ cls: 'Magic User', abilities: [] }) and abilityPoolCard(null) -> null; a c with only table ids -> null; RAIL_COPY.abilityPool is frozen and voice-clean
  </behavior>
  <action>
    Extend the three test files FIRST (RED). `src/browser/combatMenu.js`: import `ABILITY_BY_ID`, `ONCE_A_FIGHT` from ../../content/index.js, `isReady` from ../../engine/effects.js, `abilityRoundsLeft` from ../../engine/abilities.js; add the five COPY literals; add a module-private `abilityRows(c)` building rows per must_haves (cost rule: `isReady(c, id) ? abilityReady : (meta.cd === "fight" ? abilityUsedUp : left === 1 ? abilityRound : abilityRounds.replace("{n}", left))` where `left = abilityRoundsLeft(c, key)`); insert `else if (Array.isArray(c.abilities) && c.abilities.length)` BEFORE the current `else` (sub = abilitiesSub with ready/total counts, enabled true, opens "abilities"); in the Bard branch append `...abilityRows(c)` after the sing row (sing row + sub-line byte-identical). Update the header comment (CONTEXT: rows stay tappable on cooldown — the engine's abilityRefused explains; a deliberate departure from the SPELLS rows' enabled gate).

    `src/browser/viewModels.js`: import the same three symbols; add `abilities` to characterSheetViewModel's return (after `skills`) built from `c.abilities || []` with the state rule (in combat: READY / "{n} rounds" / "once a fight · used"; out of combat: "cd {n} rounds" / "once a fight") and the provenance `source`. `src/browser/rail.js`: add `RAIL_COPY.abilityPool` and `export function abilityPoolCard(c)` beside wornReconcileCard (find the first id in `c.abilities` whose catalog entry has `source === "pool"`; null otherwise). Keep all three modules pure (no DOM, no rng, no mutation). Run the three test files (GREEN) + the pure-module guard in toastsCoverage.
  </action>
  <verify>
    <automated>node --test test/unit/combatMenu.test.js test/unit/viewModels.test.js test/unit/rail.test.js test/unit/toastsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "abilityRoundsLeft" src/browser/combatMenu.js src/browser/viewModels.js` >= 1 for each; `grep -c 'dispatch: { type: "useAbility", key }' src/browser/combatMenu.js` prints 1; `grep -c "ONCE A FIGHT · USED" src/browser/combatMenu.js` prints 1
    - `grep -c "^export function abilityPoolCard" src/browser/rail.js` prints 1; `grep -c "UP YOUR SLEEVE" src/browser/rail.js` prints 1
    - `grep -c "abilities," src/browser/viewModels.js` >= 1 (the return shape)
    - the three test files pass; the existing Fighter default-grid, Bard and Magic User combatMenu tests pass UNCHANGED (only additions in test/unit/combatMenu.test.js — `git diff -- test/unit/combatMenu.test.js | grep -c '^-[^-]'` prints 0)
  </acceptance_criteria>
  <done>The view-models render ability rows with READY/N ROUNDS/USED legibility, the Bard keeps Sing first, the Hero-tab model and the pool card exist — all pure and tested.</done>
</task>

<task type="auto">
  <name>Task 2: mazeworld.html — COMBAT_DISPATCH.useAbility + mzUseAbility, Hero-tab #s-abilities list + __mzAbilities bridge, surfaceAbilityPool on commitRolledState, mzCombatReport learned line; source pins</name>
  <files>mazeworld.html, test/unit/shell-abilities.test.js, test/unit/shell-combat-actions.test.js</files>
  <read_first>
    - mazeworld.html lines 5547-5560 (COMBAT_DISPATCH), 7860-7880 (the window.mz* combat bridges; engineCombatAction at 7824), 3170-3190 (the #s-skills render loop in paint()), the `<ul id="s-skills">` markup (grep `id="s-skills"`) and its heading, 6790-6810 + 6829-6840 (the module bridge block — `window.__mzEff` etc.), 7458-7474 (commitRolledState), 7436-7445 (the dev start-at-depth entry — confirm it routes through commitRolledState or has its own commit), 2784-2802 (mzCombatReport) and 7178-7200 (noteCombat's `data` literal)
    - test/unit/shell-combat-actions.test.js lines 144-168 (the COMBAT_DISPATCH pin — extend to nine), test/unit/shell-worn-slots.test.js (Phase 37's source-pin file — copy its CODE/region helper shape)
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-04-SUMMARY.md §Accomplishments (the bridge/paint-local helper conventions; createElement/textContent rows)
  </read_first>
  <action>
    `mazeworld.html`: (1) `COMBAT_DISPATCH.useAbility: (d) => window.mzUseAbility?.(d.key)` after `readScroll`; `window.mzUseAbility = (key) => engineCombatAction("useAbility", { key });` beside `window.mzSing`. (2) Hero tab: add `<ul id="s-abilities">` with a heading/label "Abilities" directly after the `#s-skills` block in the markup; in paint(), after the skills loop, a paint-local `renderAbilityRows(c)` that clears `#s-abilities` and, using `window.__mzAbilities` (`byId`, `roundsLeft`, `isReady`) and `characterSheetViewModel`-equivalent logic (or simply `window.__mzAbilities.rows(state)` — prefer bridging `characterSheetViewModel(state).abilities` through `window.__mzSheetVM` to avoid duplicating the state rule), appends one `<li>` per entry built with createElement/textContent: `<b>{name}</b> <small>{tag}</small> <i>{description}</i> <span>{state}</span>`; a Magic User gets the `none` row "Spells are the trick."; the `#s-skills` loop is untouched. (3) Module block: `window.__mzAbilities = { byId: ABILITY_BY_ID, roundsLeft: abilityRoundsLeft, isReady, sheet: characterSheetViewModel }` (imports from content/index.js, engine/abilities.js, engine/effects.js, viewModels.js — characterSheetViewModel is already imported). (4) `surfaceAbilityPool(state)` directly above `commitRolledState`: `const card = abilityPoolCard(state.c); if (!card) return; window.mzRailLine?.(card.title, card.line, card.tone, card.hold, card.icon); window.logLine?.(card.line);` and call it as the LAST statement of `commitRolledState` (after `mzCenterMap`); if the dev start-at-depth path commits elsewhere, call it there too. Import `abilityPoolCard` from rail.js. (5) `mzCombatReport`: after the leveled line, `for (const n of d.learned || []) lines.push(`<span class="hit">New trick: ${n}.</span>`)`; `noteCombat`'s `data` gains `learned: events.filter((e) => e.type === "abilityLearned").map((e) => e.name)`.

    Tests: `test/unit/shell-abilities.test.js` (new; source pins in the shell-worn-slots style): the dispatch entry regex, the mzUseAbility bridge, the `#s-abilities` markup + render helper + createElement/textContent-only construction (no innerHTML in the region), the bridge object keys, surfaceAbilityPool's body + its commitRolledState call site being the last statement, the report/noteCombat learned lines, voice safety of the new literals, and `npm run build:www` artefact sanity (mirror shell-worn-slots' build check). Re-pin `test/unit/shell-combat-actions.test.js`'s COMBAT_DISPATCH test to nine types (add the useAbility regex, `keys.length === 9`, rename the title). Run the shell test files and `npm run build:www`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-abilities.test.js test/unit/shell-combat-actions.test.js test/unit/shell-worn-slots.test.js && npm run build:www 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useAbility: (d) => window.mzUseAbility?.(d.key)" mazeworld.html` prints 1; `grep -c 'window.mzUseAbility = (key) => engineCombatAction("useAbility", { key })' mazeworld.html` prints 1
    - `grep -c 'id="s-abilities"' mazeworld.html` prints 1; `grep -c "function renderAbilityRows" mazeworld.html` prints 1; `grep -c "window.__mzAbilities = " mazeworld.html` prints 1
    - `grep -c "function surfaceAbilityPool" mazeworld.html` prints 1; `grep -c "surfaceAbilityPool(state)" mazeworld.html` >= 1; `grep -c "abilityPoolCard" mazeworld.html` >= 2 (import + call)
    - `grep -c "New trick: " mazeworld.html` >= 1; `grep -c "learned:" mazeworld.html` >= 1
    - `grep -c "keys.length, 9" test/unit/shell-combat-actions.test.js` prints 1
    - `npm run build:www` exits 0; `grep -c fonts.googleapis mazeworld.html` prints 0; the three shell test files pass
  </acceptance_criteria>
  <done>The submenu dispatches useAbility through the shell bridge, the Hero tab lists abilities with their state, the first paint narrates the level-1 trick on the rail, the fight report names new tricks; source pins and the build are green.</done>
</task>

<task type="auto">
  <name>Task 3: docs/ABILITIES.md UI + requirements map, Phase 38 closing gate, aggregated Pixel 7 checklist</name>
  <files>docs/ABILITIES.md</files>
  <read_first>
    - docs/ABILITIES.md (Plans 01-04's sections — append two sections, never rewrite)
    - .planning/phases/38-melee-active-abilities/38-01-SUMMARY.md, 38-02-SUMMARY.md, 38-03-SUMMARY.md, 38-04-SUMMARY.md (each `## Human verification (deferred to end of run)` section — the items to aggregate)
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-04-SUMMARY.md §"Human verification (deferred to end of run)" (the aggregated, plan-grouped, continuously-numbered checklist format) and §"Success Criteria Map"
  </read_first>
  <action>
    Append to docs/ABILITIES.md: `## UI (Plan 05)` (the submenu row shape and cost vocabulary, the Bard ruling, the Hero-tab list and its state strings, the first-paint pool card copy, the fight-report line) and `## Requirements map` (ABIL-01 -> Plans 03/05; ABIL-02 -> Plans 01/02; ABIL-03 -> Plans 01/05; ABIL-04 -> Plans 03/05; ABIL-05 -> Plan 04; SC-1..5 -> the proving tests by file name).

    Phase gate (record in the SUMMARY with the actual numbers): `npm test` `# fail 0` (total > 2388); `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty AND `git diff --stat 56d51e3 -- test/parity/fixtures` lists exactly the six fixture files Plan 01 added records to (no other fixture change in the phase); `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat -- package.json package-lock.json tools` empty; `grep -c fonts.googleapis mazeworld.html` = 0;
<!-- planner-discipline-allow: fonts.googleapis --> `git diff --stat 56d51e3 -- engine content src mazeworld.html docs` = the union of every plan's declared engine/content/src/mazeworld.html/docs footprint (list it); `git log --stat 56d51e3..HEAD -- store-listing tools/store-screenshots` is empty; both coverage guards + voice scan green; `node --test test/unit/chargen-rng-pin.test.js` green with the file unchanged since 56d51e3.

    Write the SUMMARY's `## Human verification (deferred to end of run)` as the phase's full aggregated Pixel 7 checklist — one numbered list, continuous numbering, grouped by plan (`### Plan 01` … `### Plan 05`), copying Plan 01's level-up item and Plan 04's three Joiner items verbatim, "Nothing to add" notes for Plans 02/03 with their reasons, and Plan 05's own items: (a) open ABILITIES mid-fight as a fresh Fighter/Thief — at least one row reads READY with its effect line; (b) use it — the fight log shows "You call {name}." then the effect line, the foes take their turn once, the row now reads "N ROUNDS"; (c) tap the same row on cooldown — the fight log shows "{Name}: N rounds. Your arm has opinions." and nothing else happens; (d) a once-a-fight ability after use reads "ONCE A FIGHT · USED" and is READY again next fight; (e) Bard: Sing is still the first row, ability rows follow; (f) Hero tab: Special skills list shows only kept passives; the Abilities list shows table actives tagged "special skill · active" and pool tricks tagged "trick", with "cd N rounds" / "once a fight" out of combat; (g) a brand-new run's first paint shows the UP YOUR SLEEVE rail card "New trick: {name} — {txt}" once, mirrored in the Oracle; (h) level up in a fight — the fight log carries "New trick: …" and the victory report lists it; (i) TalkBack reads each ability row's label/cost/desc; (j) a pre-Phase-38 save resumes with its old skills renamed/dropped and abilities present (Hero tab) with no card or crash. Then the standard SUMMARY sections (Success Criteria Map SC-1..5 -> plan -> proof). Explicit-path commits only; never stage store-listing/ or tools/store-screenshots/.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (pass|fail)" && npm run build:www 2>&1 | tail -1 && git hash-object test/parity/prototype-master.js.txt && git diff --stat -- package.json package-lock.json tools && git log --stat 56d51e3..HEAD -- store-listing tools/store-screenshots | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## UI" docs/ABILITIES.md` prints 1; `grep -c "## Requirements map" docs/ABILITIES.md` prints 1; `grep -c "ABIL-0[1-5]" docs/ABILITIES.md` >= 5
    - `npm test` ends `# fail 0` with `# tests` > 2388; `npm run build:www` exits 0
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git status --porcelain test/parity/fixtures` is empty; `git diff --stat -- package.json package-lock.json tools` is empty; `git log --stat 56d51e3..HEAD -- store-listing tools/store-screenshots | wc -l` prints 0
    - `git diff --stat 56d51e3 -- test/unit/chargen-rng-pin.test.js` is empty and the file's tests pass
    - the SUMMARY's `## Human verification (deferred to end of run)` contains the headings `### Plan 01` through `### Plan 05`, at least 14 numbered items in one continuous sequence, the literal "Your arm has opinions", and no `adb`/`install` instruction
  </acceptance_criteria>
  <done>Ledger complete; the whole-phase gate is green with the master untouched and only the six declared fixtures changed since 56d51e3; the aggregated Pixel 7 checklist is queued for the milestone-close UAT batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog strings -> DOM | ability names/txt are content literals rendered via textContent/createElement (never innerHTML from state) |
| shell row payload -> engine | `dispatch.key` comes from the view-model's catalog-derived rows; the engine validates and refuses unknown keys anyway |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-11 | Tampering | mazeworld.html renderAbilityRows | low | mitigate | createElement/textContent only (source-pinned: no innerHTML in the region); a tampered save's `abilities` entries not in the catalog render nothing (byId lookup guard) |
| T-38-12 | Information Disclosure | window.__mzAbilities bridge | low | accept | read-only catalog/pure helpers, no state mutation surface |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | no package change (gate) |
</threat_model>

<verification>
- `npm test` -> `# fail 0`; `npm run build:www` exit 0; parity/master/fixture gates as in Task 3
- `node --test test/unit/combatMenu.test.js test/unit/viewModels.test.js test/unit/rail.test.js test/unit/shell-abilities.test.js test/unit/shell-combat-actions.test.js` green
- The aggregated checklist exists in the SUMMARY (never executed in this run)
</verification>

<success_criteria>
- ROADMAP SC-1: a Fighter/Thief opens the ABILITIES submenu mid-combat and sees at least one usable ability with a clear READY / N ROUNDS state (combatMenu tests + the level-1 guarantee from Plan 01)
- ROADMAP SC-3's narration half: the level-1 pool roll is narrated once on the first paint (rail card) and each level-up's new trick is a second line on the SKILL LEVEL card / fight log / victory report
- ABIL-04 legibility: effect, cooldown and readiness legible per row; the on-cooldown tap is a named refusal (Plan 03) surfaced as a dull fight-log entry
- CONTEXT "Submenu rows", "Hero tab", "first paint" and "Bard keeps Sing" implemented as locked; all five requirement IDs mapped in docs/ABILITIES.md
</success_criteria>

<output>
Create `.planning/phases/38-melee-active-abilities/38-05-SUMMARY.md` when done (include `## Human verification (deferred to end of run)` as the phase's full aggregated, plan-grouped, continuously numbered Pixel 7 checklist, the Success Criteria Map, and the whole-phase gate numbers).
</output>