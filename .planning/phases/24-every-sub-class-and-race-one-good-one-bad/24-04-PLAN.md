---
phase: 24-every-sub-class-and-race-one-good-one-bad
plan: 04
type: execute
wave: 2
depends_on: ["24-02"]
files_modified:
  - engine/economy.js
  - mazeworld.html
  - test/unit/economy.test.js
  - test/parity/fixtures/action-script.economy.json
autonomous: true
requirements: [IDENT-05, FID-07]

must_haves:
  truths:
    - "For a Pickpocket, every priceFor-routed store line (weapons, armour, premium, rations, armour repair) costs Math.max(1, Math.round(racePrice * 1.25)) and a sale pays Math.max(1, Math.round(racePrice * 0.5 * 0.75)); food, potions, lockpicks and the scroll stay flat-priced exactly as they are for every race; a non-Pickpocket's prices are byte-identical to today (D: Pickpocket bad, IDENT-05)"
    - "Dwarven armor wear uses Math.ceil(dmg / 2) (a 1-point hit still costs 1); Pickpocket store prices apply x1.25 buy / x0.75 sell with Math.round after the race multiplier, floored at 1 like every other price; no floating-point value is serialized (probe FID-07 precision)"
    - "The economy fixture (seed 3, whose hero IS a Pickpocket) carries an action-path divergence record with fromAction 0 and stockCostMul 1.25: the store roll (names/order/subs) is proven byte-identical to the frozen prototype at the openStore action with the declared markup on routed lines, and the measured end state (prototype gold 55 / weapon Katana / 3 items; engine gold 79 / weapon Axe / 2 items) is asserted on both sides at both replay sites"
    - "The classic sell-button label bridge (window.__mzSellPrice) passes the hero's sub so a Pickpocket sees the price the engine will actually pay"
    - "storeOpened carries pickpocket: true only for a Pickpocket hero (additive event field, narration unchanged this phase)"
  artifacts:
    - "engine/economy.js — priceFor(base, race, sub = null), sellPriceFor(item, race, sub = null), sellItem passes c.sub, all openStore priceFor call sites pass c.sub, storeOpened.pickpocket"
    - "mazeworld.html — window.__mzSellPrice passes `?.c?.sub` as the third argument (the only mazeworld.html edit in this plan)"
    - "test/unit/economy.test.js — priceFor/sellPriceFor Pickpocket cases; the seed-3 stock pin (Katana 656, Axe 63, Studded 938, Casket 3750, Rations 38; flat lines unchanged)"
    - "test/parity/fixtures/action-script.economy.json — top-level `divergence` record (kind action-path, fromAction 0, stockCostMul 1.25, fields gold/weapon/items, measured before/after) + `_note` sentence"
  key_links:
    - "priceFor(..., c.sub) at every openStore call site -> stock costs -> buyFrom affordability (the fixture's action path diverges at its 6th buy because gold runs out sooner)"
    - "sellPriceFor(item, race, sub) -> sellItem -> itemSold.price; and -> window.__mzSellPrice label"
    - "action-script.economy.json divergence -> actionPathDivergenceOf/stockMarkupDiff/declaredEndDiffs (Plan 24-02) at economy-parity.test.js and full-suite.test.js"
  prohibitions:
    - "Never edit test/parity/prototype-master.js.txt; never rewrite the economy fixture's seed/actions or the harness's 5000 gold bump — declare the divergence"
    - "No new rng draw (pricing is pure arithmetic after the store roll); no new serialized field (the markup is recomputed from c.sub; state.store keeps stock/haggle/race only)"
    - "The sell markdown is applied to the ordinary sell price, never to the marked-up buy price (x1.25 x0.75 would be a 6% penalty, not the decided 25%)"
    - "No other mazeworld.html edit (the canParley mirror belongs to Plan 24-01)"
    - "Human, Elven, Dwarven, Troll, Wilmsry pricing numbers unchanged for every non-Pickpocket hero"
---

<objective>
Give the Pickpocket its bad — shopkeepers know your face: buy prices x1.25 and sell-back x0.75 via the existing race-price hooks (priceFor/sellPriceFor) — and declare the ONE parity fixture this measurably changes (the economy fixture, seed 3, whose hero is a Human Pickpocket) with an action-path record that still proves the store roll is byte-identical to the frozen prototype.

Purpose: IDENT-05 (Pickpocket had no bad); FID-07 discipline for a divergence whose consequence is a changed purchase path.
Output: economy.js pricing hooks; the sell-label bridge; unit tests incl. the seed-3 stock pin; the declared economy divergence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md
@engine/economy.js
@test/unit/economy.test.js
@test/parity/fixtures/action-script.economy.json
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- Changed signatures: `priceFor(base, race, sub = null)`, `sellPriceFor(item, race, sub = null)` (engine/economy.js; third argument optional, non-Pickpocket path value-identical).
- New event field: `storeOpened.pickpocket` (boolean, additive).
- New declared divergence: economy fixture top-level `divergence` record, kind `action-path` (second consumer of Plan 24-02's helpers).
- No new RACES flag, no new serialized field, no new test file (economy.test.js extended).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pickpocket pricing through priceFor/sellPriceFor + sell-label bridge + unit tests (economy parity red by design until Task 2)</name>
  <files>engine/economy.js, mazeworld.html, test/unit/economy.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (Pickpocket bad: "store buy prices x1.25 and sell-back x0.75 ... applied where race pricing is applied (priceFor/sellPriceFor — extend the signature to take the character or sub)")
    - engine/economy.js: priceFor (~L27-31), SELL_SPREAD, sellPriceFor (~L133-136), sellItem (~L146-160), openStore (~L222-308: the repair line, the two weapon lines with the Troll x2 factor, the armour line, the two premium branches, the Rations line — every priceFor call — the Wilmsry haggle, the storeOpened event), `add()` already does Math.max(1, Math.round(cost))
    - mazeworld.html ~L5479-5494: `window.__mzSellPrice = (item) => sellPriceFor(item, (window.__mzState?.get?.() || {})?.c?.race);`
    - test/unit/economy.test.js (~L56-62 the priceFor test; ~L150-156 the Rations line test; fixedFighter/fixedState helpers)
    - test/unit/store-sell.test.js and test/unit/item-wiring.test.js (existing sellPriceFor consumers with two arguments — they must stay green unchanged)
  </read_first>
  <action>
    Per D: Pickpocket (IDENT-05). Add a `// DELIBERATE RULES CHANGE (Phase 24, 2026-09-14, IDENT-05): ...` comment on priceFor and sellPriceFor.

    1. `priceFor(base, race, sub = null)`: compute the race price exactly as today (Troll x3; Elven/Dwarven Math.round(base / 2); else base), then, ONLY when `sub === "Pickpocket"`, return `Math.max(1, Math.round(p * 1.25))`; otherwise return the race price unchanged (a non-Pickpocket must get a value-identical result, including the un-rounded fractional repair arithmetic that follows some call sites).
    2. `sellPriceFor(item, race, sub = null)`: `buy = priceFor(baseValueFor(item), race)` (NO sub — the markdown rides the ordinary sell price), then `Math.max(1, Math.round(buy * SELL_SPREAD * (sub === "Pickpocket" ? 0.75 : 1)))`.
    3. `sellItem`: call `sellPriceFor(it, c.race, c.sub)`.
    4. openStore: pass `c.sub` as the third argument at every priceFor call (repair, both weapon lines, armour, both premium branches, Rations — 6 textual call sites; the Troll x2 weapon factor and the premium multipliers stay outside priceFor exactly as today). Add `pickpocket: c.sub === "Pickpocket"` to the storeOpened event (additive; no narration change required this phase — Phase 25 owns the toast). Nothing else in openStore moves: rng.shuffle / rng.d(2) / rollBlade / rollMailPiece order and the stock index positions are untouched.
    5. mazeworld.html: change `__mzSellPrice` to pass `?.c?.sub` as the third argument (read the state once into a local). Touch nothing else.
    6. test/unit/economy.test.js additions: `priceFor(100, "Human", "Pickpocket") === 125`; `priceFor(101, "Elven", "Pickpocket") === 64` (round(51 x 1.25 = 63.75)); `priceFor(100, "Troll", "Pickpocket") === 375`; `priceFor(1, "Elven", "Pickpocket") === 1` (floor); `priceFor(100, "Human", "Cutthroat") === 100` and `priceFor(100, "Human")` unchanged; `sellPriceFor({ kind: "cloak", n: "Cloak of Armor" }, "Human", "Pickpocket") === 938` (2500 x 0.5 x 0.75 = 937.5 -> 938) vs 1250 for a Human non-Pickpocket; a seed-3 stock pin: `newRun(3)` (a Human Pickpocket — assert `c.sub === "Pickpocket"`), `openStore` with `makeRng(state.rngState)`, then assert by name: Katana 656, Axe 63, Studded 938, "Casket, a broadsword" 3750, "Rations (+1 ration)" 38, and flat lines Chicken 20, Healing potion 150, Set of lockpicks 450 — and that a Human Cutthroat forced via `newRun(3, [], { force: { sub: "Cutthroat" } })` sees Katana 525 / Axe 50 / Studded 750 / Rations 30 with the same stock names in the same order (the roll is price-independent); `storeOpened.pickpocket` true for the Pickpocket, false for the Cutthroat; `sellItem` on a Pickpocket credits 938 for the Cloak of Armor.
    Expect economy parity RED after this task (the seed-3 fixture hero is a Pickpocket) — Task 2 declares it; commit both tasks together.
  </action>
  <verify>
    <automated>node --test test/unit/economy.test.js test/unit/store-sell.test.js test/unit/item-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function priceFor(base, race, sub = null)" engine/economy.js` == 1
    - `grep -c "export function sellPriceFor(item, race, sub = null)" engine/economy.js` == 1
    - `grep -c 'sub === "Pickpocket"' engine/economy.js` >= 3 (priceFor, sellPriceFor, storeOpened)
    - `grep -c ", race, c.sub)" engine/economy.js` == 6 (every openStore priceFor call site: repair, weapon, armour, premium weapon, premium armour, rations; the JSDoc must not repeat the literal)
    - `grep -c "sellPriceFor(it, c.race, c.sub)" engine/economy.js` == 1
    - `grep -c "priceFor(baseValueFor(item), race)" engine/economy.js` == 1 (the sell markdown rides the ordinary sell price)
    - `grep -c "pickpocket: c.sub" engine/economy.js` == 1
    - `grep -c "__mzSellPrice" mazeworld.html` == 1 and the `__mzSellPrice` line passes a `sub` third argument (`grep -c "c?.sub" mazeworld.html` >= 1)
    - `grep -c "Pickpocket" test/unit/economy.test.js` >= 6
    - `node --test test/unit/economy.test.js test/unit/store-sell.test.js test/unit/item-wiring.test.js` exits 0
  </acceptance_criteria>
  <done>Pickpocket buy x1.25 / sell x0.75 flow through the race-price hooks with floor 1, non-Pickpocket prices are value-identical, the sell label bridge is correct, and the seed-3 stock pin proves the exact numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Measure and declare the economy fixture (seed 3) action-path divergence; parity green</name>
  <files>test/parity/fixtures/action-script.economy.json</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md (record shape; stockMarkupDiff semantics; PRICEFOR_ROUTED_EFFECTS)
    - test/parity/fixtures/action-script.economy.json (`_note`, seed 3, the 10 actions: openStore, buyItem 0/3/9/10/11/7/8/6, leaveStore)
    - test/parity/economy-parity.test.js and the economy block of test/parity/full-suite.test.js (the gold bump to 5000 on both sides; how a top-level record is consulted)
    - test/parity/fixtures/action-script.schema.md (the action-path record documentation from Plan 24-02)
  </read_first>
  <action>
    Planner pre-measurement (2026-09-14, scratch engine copy with Task 1's pricing; executor MUST re-measure live against `loadPrototypeSandbox({ seed: 3 })` and write the measured values):

    Stock at action 0 (prototype -> engine; the engine's extra Rations line is the pre-existing RATION-01 divergence stripStoreClosures already drops): 0 Chicken 20 -> 20 | 1 Bread 15 -> 15 | 2 Meat 25 -> 25 | 3 Healing potion 150 -> 150 | 4 Xtra Healing 500 -> 500 | 5 Strength 100 -> 100 | 6 Speed 500 -> 500 | 7 Set of lockpicks 450 -> 450 | 8 Katana 525 -> 656 | 9 Axe 50 -> 63 | 10 Studded 750 -> 938 | 11 Casket, a broadsword 3000 -> 3750 | (engine-only) 12 Rations 30 -> 38. Names, order and subs identical; only routed lines (buyWeapon/buyArmor/buyPremium/buyRations) move, each to Math.max(1, Math.round(cost x 1.25)).
    Purchase path (gold 5000 on both sides): buys 0, 3, 9, 10, 11 succeed on both sides (prototype gold 4980/4830/4780/4030/1030; engine 4980/4830/4767/3829/79; the Casket is bought-then-rejected on both). Action 6 (buyItem 7, lockpicks 450): prototype bought (gold 580) — engine buyFailed (short 371). Action 7 (buyItem 8, Katana): prototype bought + equipped (gold 55) — engine buyFailed. Action 8 (buyItem 6, Speed): buyFailed on both. leaveStore: store null on both.
    End pins: prototype `c.gold 55, c.weapon "Katana", c.items [Cloak of Ether, Healing potion, Lockpicks]`; engine `c.gold 79, c.weapon "Axe", c.items [Cloak of Ether, Healing potion]`; `c.armor "Studded"` and `store null` on both (not declared — they do not differ).

    1. Measure live with a scratch script replaying the fixture through runEconomyAction on both sides (the same gold bump), printing both stocks at action 0 and both sides' gold/weapon/items after every action; confirm the first divergent action is 0 (stock costs) and the first path split is action 6.
    2. Declare: add a top-level `divergence` record to action-script.economy.json: `{ "phase": "24", "requirements": ["IDENT-05", "FID-07"], "kind": "action-path", "fromAction": 0, "stockCostMul": 1.25, "fields": ["gold", "weapon", "items"], "before": {<measured prototype end values, items array verbatim>}, "after": {<measured engine end values>}, "rationale": "<one paragraph: the seed-3 hero is a Human Pickpocket; Phase 24 marks up every priceFor-routed line x1.25 for a Pickpocket, so the store ROLL is byte-identical (asserted via stockMarkupDiff at the openStore action) while gold runs out two purchases earlier (lockpicks and the Katana become unaffordable); seed/actions/gold bump deliberately untouched; purchase mechanics parity for a non-Pickpocket is unchanged code (buyFrom/takeItem untouched) and covered by test/unit/economy.test.js>" }`. Append one sentence to `_note` naming the record.
    3. Run `node --test test/parity/economy-parity.test.js test/parity/full-suite.test.js` — both green via the record (stockMarkupDiff null at action 0; end pins null on both sides; storeOpened/bought/buyFailed/storeLeft still asserted). Then the whole suite.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const f=require('./test/parity/fixtures/action-script.economy.json');const d=f.divergence;if(f.seed!==3||f.actions.length!==10)process.exit(1);if(d.kind!=='action-path'||d.fromAction!==0||d.stockCostMul!==1.25)process.exit(2);for(const k of d.fields){if(JSON.stringify(d.before[k])===JSON.stringify(d.after[k]))process.exit(3)}"` exits 0
    - `node -e "const f=require('./test/parity/fixtures/action-script.economy.json');if(f.divergence.before.gold!==55||f.divergence.after.gold!==79)process.exit(1)"` exits 0 (or the live-measured values, which the SUMMARY must quote if they differ from the planner's prediction)
    - `node --test test/parity/economy-parity.test.js test/parity/full-suite.test.js` exits 0
    - `node --test "test/parity/**/*.test.js"` exits 0
    - `npm test` ends with `# fail 0`
    - `git diff --stat -- test/parity/prototype-master.js.txt test/parity/fixtures/action-script.combat.json test/parity/fixtures/action-script.magic.json test/parity/fixtures/action-script.encounters.json test/parity/fixtures/action-script.movement.json test/parity/fixtures/action-script.chargen.json test/parity/harness/comparables.js` prints nothing
  </acceptance_criteria>
  <done>The economy fixture's Pickpocket divergence is declared with a live-measured record; the store roll is still proven identical to the prototype; the full suite is green with no other parity file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine pricing -> browser label | window.__mzSellPrice displays what the engine will pay; must use the same inputs |
| fixture record -> parity gate | Declared markup relation narrows the byte diff for the economy script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-09 | Tampering | priceFor floor/rounding | low | mitigate | Math.max(1, Math.round(...)) on every marked-up value; unit tests at 1/100/101 bases and all races |
| T-24-10 | Repudiation | economy divergence record | medium | mitigate | stockMarkupDiff asserts the store roll; end pins asserted both sides; rationale names the mechanic |
| T-24-11 | Information Disclosure | sell label mismatch | low | mitigate | __mzSellPrice passes c.sub; the label and sellItem share one function |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase |
</threat_model>

<verification>
- `node --test test/unit/economy.test.js test/unit/store-sell.test.js test/unit/item-wiring.test.js` green.
- `node --test "test/parity/**/*.test.js"` green with the declared economy record.
- `npm test` ends `# fail 0`.
</verification>

<success_criteria>
- A Pickpocket pays x1.25 on routed store lines and receives x0.75 on sales, floor 1; everyone else is unchanged; the sell label matches.
- The seed-3 economy fixture is declared with a measured, machine-checked action-path record that still proves store-roll parity.
</success_criteria>

<output>
Create `.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-04-SUMMARY.md` when done, quoting verbatim the measured stock table (all 12 prototype lines + the engine's Rations line), the per-action gold trajectory on both sides, and the end pins written into the record — Plan 24-07 transcribes them into FIXTURE-INVENTORY.md's Phase 24 section.
</output>
