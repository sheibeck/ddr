---
phase: 24-every-sub-class-and-race-one-good-one-bad
plan: 07
type: execute
wave: 4
depends_on: ["24-03", "24-04", "24-05"]
files_modified:
  - docs/CLASS-PASS.md
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements: [IDENT-10, IDENT-05, IDENT-06, IDENT-07, IDENT-09, FID-07]

must_haves:
  truths:
    - "No new serialized field is introduced (every mechanic reads c.sub, c.race, or existing combat flags such as C.opened2), so no *Comparable() carve-out is needed and a v1.0/v1.1 save loads unchanged; the planner asserts this with git diff on test/parity/harness/comparables.js (only a declared-divergence helper may change) plus the existing round-trip tests (probe FID-07 empty)"
    - "docs/CLASS-PASS.md's 'Rulings (Phase 24 — PLAY-03)' section records every Phase 24 design decision (14 sub-class mechanics, 3 race changes, the Bard party-clause reinterpretation, the Pilfer heal-kind reading, the MoA withdrawal reading) with a one-line rationale each, plus the Freeze-pays-out entry drafted in 23-04-SUMMARY.md and the per-sub/race good+bad table transcribed from the identity-contract test (D: ledger)"
    - "PLANNER ASSUMPTION (flagged, IDENT-10 — a recorded ruling, not a mechanic; probe unresolved): the level-1 Thief dagger is KEPT as-is and the ruling is written into the Rulings section under a heading that begins 'Ruling: level-1 Thief dagger' naming IDENT-10 and the three rationale points (Thieves lead the BEFORE matrix — Ninja 4.22 / Con Artist 4.08 / Acrobat 3.71 top three; the opener backstab doubles the dagger's 2–4; a better weapon is one find away) — grep-able, no engine change"
    - "test/parity/FIXTURE-INVENTORY.md gains a 'Phase 24 identity-pass divergences (IDENT-05..10 / FID-07)' section transcribing the MEASURED combat/lose (seed 14) and economy (seed 3) before/after tables, the lose-apprentice addition, the seed-17 byte-identical note, the FID-02 pin changes, and a 'byte-identical elsewhere' paragraph; the generated roster block is untouched by this plan"
    - "A Phase 24 smoke readout (tools/tune-classes.mjs --sub/--race, 20 seeds) is appended to the Rulings section as a directional signal only, with the explicit caveat that it is NOT the AFTER matrix (Phase 26); the AFTER section still reads 'Nothing recorded yet.'"
  artifacts:
    - "docs/CLASS-PASS.md — Rulings section filled (decisions + rationale, dagger ruling, Freeze pays out, good/bad table, Phase 24 smoke note); AFTER section untouched"
    - "test/parity/FIXTURE-INVENTORY.md — Phase 24 divergences section (prose only)"
  key_links:
    - "24-03-SUMMARY.md / 24-04-SUMMARY.md measured tables -> FIXTURE-INVENTORY.md Phase 24 section (transcribed verbatim, never re-derived by hand)"
    - "24-06-SUMMARY.md 30-row good/bad table -> docs/CLASS-PASS.md Rulings good/bad table"
    - "23-04-SUMMARY.md 'Draft Rulings entry' -> docs/CLASS-PASS.md Rulings (pasted)"
  prohibitions:
    - "The AFTER section of docs/CLASS-PASS.md is not touched (Phase 26 captures the AFTER matrix); no 143-cell matrix run in this plan"
    - "No engine, content, fixture, harness or test change in this plan (docs only); never edit test/parity/prototype-master.js.txt"
    - "The dagger ruling is KEEP — no dagger number changes anywhere"
    - "Ledger prose stays factual and family-friendly; tools/tune-classes.mjs output is quoted, never edited"
    - "The FIXTURE-INVENTORY generated roster block (between the begin/end markers) is not edited here (Plan 24-03 regenerated it)"
---

<objective>
Close the phase's paperwork: fill docs/CLASS-PASS.md's "Rulings (Phase 24 — PLAY-03)" section with every design decision and its rationale (including the IDENT-10 dagger ruling, the Freeze-pays-out entry drafted by Phase 23, and the good/bad table sourced from the identity-contract test), write FIXTURE-INVENTORY.md's Phase 24 divergences section from the measured tables in 24-03/24-04's SUMMARYs, confirm the FID-07 "no new serialized field" gate with a git diff on comparables.js, and record a small tune-classes smoke readout as a directional signal (explicitly not the AFTER matrix).

Purpose: IDENT-10 (a deliberate, recorded ruling), PLAY-03's Rulings section (owned by this phase), FID-07's paper trail.
Output: two documentation files updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-03-SUMMARY.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-04-SUMMARY.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-06-SUMMARY.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-04-SUMMARY.md
@docs/CLASS-PASS.md
@test/parity/FIXTURE-INVENTORY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- Ledger sections: docs/CLASS-PASS.md "Rulings (Phase 24 — PLAY-03)" filled (decision list, dagger ruling, Freeze pays out, good/bad table, Phase 24 smoke note).
- Inventory section: test/parity/FIXTURE-INVENTORY.md "Phase 24 identity-pass divergences".
- No code, content, fixture, harness or test artifact.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Fill the Rulings section — every decision with rationale, the IDENT-10 dagger ruling, the Freeze-pays-out entry, the good/bad table</name>
  <files>docs/CLASS-PASS.md</files>
  <read_first>
    - docs/CLASS-PASS.md (~L637-664: the Rulings placeholder paragraph ending "Nothing recorded yet." and the AFTER placeholder that must stay untouched)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (every decision + the dagger ruling rationale text; `<specifics>`: "every design decision above with a one-line rationale, plus the dagger ruling")
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-04-SUMMARY.md ("Draft Rulings entry (for Phase 24): Freeze pays out" — paste verbatim; and the Petrify/Turn/Gate v1.3 note)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-01-SUMMARY.md, 24-03-SUMMARY.md, 24-04-SUMMARY.md, 24-05-SUMMARY.md (decisions actually landed: Bard reinterpretation, Pilfer heal-kind = heal + full, MoA withdrawal = ordinary roll, event names) and 24-06-SUMMARY.md (the 30-row good/bad table)
    - .planning/research/CLASS-AUDIT-2026-09-14.md (the BEFORE top-three Thief numbers quoted by the dagger ruling come from docs/CLASS-PASS.md's BEFORE section — cross-check Ninja/Wilmsry 5.38 etc. are cell means; the 4.22/4.08/3.71 figures are the BY SUBCLASS roll-up means — quote whichever the ledger's BEFORE section actually shows for Ninja/Con Artist/Acrobat, verbatim)
  </read_first>
  <action>
    Replace ONLY the Rulings placeholder paragraph (keep the `## Rulings (Phase 24 — PLAY-03)` heading; do not touch anything from `## AFTER` onward) with, in this order:
    1. A short preamble (this phase's commit range; "every rule change carries a DELIBERATE RULES CHANGE (Phase 24, ...) comment in the engine").
    2. `### Sub-class rulings` — one bullet per sub-class touched, each `**Sub — GOOD/BAD name.** mechanic in one sentence; rationale in one sentence; requirement id.`: Knight (bad: never-first vs live maxWP >= 20 — "everything over 20 comes straight at you"; IDENT-05), Ninja (bad: cannot parley, mirror updated; IDENT-05), Bard (bad: camp wake on 1-2; party clause — dumb foes target the Bard, reinterpretation rationale: any biased zero-draw pick would exclude one member; IDENT-05), Master of Arms (bad: cannot parley + tracked round-1 clean exit denied — falls to the ordinary roll, the CONTEXT's "flee like any Fighter"; IDENT-05), Court Mage (bad: foes act first in round one; good: boredom 1-in-6 + always parley Humans; IDENT-05/06), Pickpocket (bad: buy x1.25 / sell x0.75 via the race-price hook, so flat-priced food/potions/picks stay flat; IDENT-05), Cutthroat (bad: Joiners refuse, rolled first; IDENT-05), Guard (good: -1 to be hit, stacks with Agility; IDENT-06), Woodsman (enforced: no armour over ar 10 — Mail/Plate and any Warded piece heavier than plain Studded — in take/equip/store; IDENT-07), Pilfer (enforced: useItem heal-kind only = Healing `heal` and Xtra Healing `full`; cures/buffs/staves/cloaks refused; drinkPotion untouched; IDENT-07), Cloaker (bad made real: free vanish only before the first landed blow; IDENT-07). Then `### Race rulings`: Human (neutral control, no change — user decision 2026-09-14), Elven (no change; blurb true), Dwarven (good: armour wears at ceil(dmg/2)), Wilmsry (Magic User Joiners refuse; heal/parley/haggle numbers unchanged — trim deferred to after AFTER), Fridgian (frenzy never wasted on a corpse — the d10 whiff draw removed, the one rng-cursor change of the phase; hide soaks 2, floor 1, stacks with Hardiness), Troll (no change).
    3. `### Ruling: level-1 Thief dagger (IDENT-10) — KEEP` — a paragraph with exactly the CONTEXT rationale: Thieves already lead the BEFORE matrix (quote the ledger's own BEFORE numbers for Ninja, Con Artist, Acrobat verbatim), the opener backstab doubles the dagger's 2–4, and a better weapon is one find away; the dagger is the price of the class's escape/stealth kit, not an accident. State explicitly: no engine change; revisit only if Phase 26's AFTER matrix shows Thieves sliding below the class median.
    4. `### Ruling: Freeze pays out (landed Phase 23)` — paste 23-04-SUMMARY.md's drafted entry verbatim, followed by its Petrify/Turn/Gate v1.3 candidate note in one sentence.
    5. `### Good / bad table (source: test/unit/identity-contract.test.js)` — a 30-row table `| Sub / Race | GOOD | BAD |` transcribed from 24-06-SUMMARY.md (Human row: `neutral control — no race modifier anywhere`).
    6. `### Fidelity posture (FID-07)` — three sentences: zero new serialized fields (comparables.js changed only by Plan 24-02's declared-divergence helpers — quote the `git diff --stat` line); every new event type narrated (list them: withdrawalDenied, vanishDenied, joinerRefused, useRefused; removed: the Fridgian wasted-swing event); two declared action-path fixture divergences (combat/lose seed 14, economy seed 3) + lose-apprentice added — details in FIXTURE-INVENTORY.md's Phase 24 section.
    The placeholder sentence that today closes both the Rulings and the AFTER paragraphs must survive ONLY in the AFTER section (the Rulings copy is replaced by the content above).
  </action>
  <verify>
    <automated>grep -c "Ruling: level-1 Thief dagger" docs/CLASS-PASS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Ruling: level-1 Thief dagger" docs/CLASS-PASS.md` == 1 and `grep -c "IDENT-10" docs/CLASS-PASS.md` >= 1 and `grep -ci "one find away" docs/CLASS-PASS.md` >= 1
    - `grep -c "^### Ruling: Freeze pays out" docs/CLASS-PASS.md` == 1
    - `grep -c "^### Good / bad table" docs/CLASS-PASS.md` == 1 and the table has 30 data rows (`awk '/^### Good \/ bad table/{f=1} f&&/^\| /{n++} END{print n}' docs/CLASS-PASS.md` >= 32 including header + separator)
    - every one of these names appears in the Rulings section: Knight, Ninja, Bard, Master of Arms, Court Mage, Pickpocket, Cutthroat, Guard, Woodsman, Pilfer, Cloaker, Human, Elven, Dwarven, Wilmsry, Fridgian, Troll (`for n in Knight Ninja Bard "Master of Arms" "Court Mage" Pickpocket Cutthroat Guard Woodsman Pilfer Cloaker Human Elven Dwarven Wilmsry Fridgian Troll; do sed -n '/^## Rulings/,/^## AFTER/p' docs/CLASS-PASS.md | grep -q "$n" || echo MISSING $n; done` prints nothing)
    - `sed -n '/^## Rulings/,/^## AFTER/p' docs/CLASS-PASS.md | grep -c "Nothing recorded yet."` == 0
    - `sed -n '/^## AFTER/,$p' docs/CLASS-PASS.md | grep -c "Nothing recorded yet."` == 1 (the AFTER placeholder survives)
    - `git diff --numstat docs/CLASS-PASS.md` shows the AFTER block unchanged: `sed -n '/^## AFTER/,$p' docs/CLASS-PASS.md | md5sum` equals the same command run against `git show HEAD:docs/CLASS-PASS.md`
    - `node --test test/voice/safety-scan.test.js` exits 0 (the ledger is not scanned, but the run confirms nothing else moved)
  </acceptance_criteria>
  <done>The Rulings section carries every Phase 24 decision with rationale, the IDENT-10 KEEP ruling under a grep-able heading, the Freeze entry, the 30-row good/bad table and the FID-07 posture; the AFTER section is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: FIXTURE-INVENTORY.md "Phase 24" divergences section + FID-07 comparables diff check</name>
  <files>test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 20 and Phase 23 sections as the format to mirror; the generated block Plan 24-03 regenerated — do not edit it)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-03-SUMMARY.md (seed-14 per-action table, end pins, seed-17 confirmation, lose-apprentice measurement, FID-02 before/after pins)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-04-SUMMARY.md (seed-3 stock table, gold trajectory, end pins)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md (helper signatures to name)
  </read_first>
  <action>
    Append, after the Phase 23 section, `## Phase 24 identity-pass divergences (IDENT-05..10 / FID-07)` with:
    1. An intro paragraph: of the 14 sub-class and 3 race mechanics, exactly two touch a parity fixture's action path — the Fridgian race pass on combat/lose (seed 14: hide lowers every landed blow; the removed corpse-whiff d10 shifts the cursor from action 7) and the Pickpocket markup on the economy script (seed 3) — both declared with the new `kind: "action-path"` record (Plan 24-02's actionPathDivergenceOf / skipsByteDiffAt / declaredEndDiffs / stockMarkupDiff), never a rewrite of a seed or action list; plus one ADDED scenario (lose-apprentice, seed 127) restoring death-path parity coverage.
    2. `### Combat: lose scenario, seed 14` — the per-action before/after table and end pins transcribed verbatim from 24-03-SUMMARY.md, the draw accounting (identical cursor through action 6; one d10 fewer at action 7), and the FID-02 pin changes (seed 14: 101/10/died → measured; seed 17: 111/11/won → measured; new 127 row).
    3. `### Combat: lose-apprentice scenario, seed 127 (added)` — hero, roster (Bat/Rat + Shriek — the same as seed 14), attack count to death, "byte-identical on both sides".
    4. `### Economy: store visit, seed 3` — the 12-line prototype stock vs engine costs (routed lines x1.25; the engine-only Rations line noted), the purchase-path split at action 6, end pins, and the note that stockMarkupDiff asserts the store roll is still identical.
    5. `### Byte-identical elsewhere` — every other combat/magic/movement/chargen/encounters scenario, test/parity/prototype-master.js.txt (NEVER edited), the generated roster block (regenerated only for the added row); quote the FID-07 gate check: `git diff --stat <phase-start-commit>..HEAD -- test/parity/harness/comparables.js` touches only the declared-divergence helpers, and `git diff --stat <phase-start-commit>..HEAD -- test/parity/prototype-master.js.txt` is empty — run both and paste the output.
    Also list the checks that were measured and found NOT to change any fixture: Knight never-first (fixture Knights seeds 1/6 never fight), Court Mage boredom (no fixture Court Mage), Bard wake (no fixture Bard), Guard -1 (no fixture Guard), Cloaker gate (no fixture Cloaker), Dwarven wear (Dwarven seed 4 is chargen-only), Wilmsry Magic User joiner refusal (no fixture meets a Joiner), Woodsman/Pilfer gates (seed 13 Woodsman is chargen-only; fixture Pilfers only springTrap/encounterDot), Fridgian hide on combat/flee seed 17 (the flee never reaches a landed pursuit blow — byte-identical).
  </action>
  <verify>
    <automated>node --test test/parity/fixture-inventory.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 24 identity-pass divergences" test/parity/FIXTURE-INVENTORY.md` == 1
    - `grep -c "^### Combat: lose scenario, seed 14" test/parity/FIXTURE-INVENTORY.md` == 1 and `grep -c "^### Economy: store visit, seed 3" test/parity/FIXTURE-INVENTORY.md` == 1 and `grep -c "lose-apprentice" test/parity/FIXTURE-INVENTORY.md` >= 2 (generated row + prose)
    - `grep -c "action-path" test/parity/FIXTURE-INVENTORY.md` >= 2
    - `node tools/fixture-inventory.mjs | diff - <(sed -n '/fixture-inventory:generated:begin/,/fixture-inventory:generated:end/p' test/parity/FIXTURE-INVENTORY.md | sed '1d;$d')` prints nothing (generated block untouched)
    - `git diff --stat $(git log --format=%H --reverse -- .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-01-PLAN.md | head -1)..HEAD -- test/parity/prototype-master.js.txt` prints nothing
    - `node --test test/parity/fixture-inventory.test.js` exits 0
  </acceptance_criteria>
  <done>The Phase 24 divergence ledger is complete, transcribed from measured SUMMARY tables, with the FID-07 comparables/prototype-master diff evidence pasted.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 24 smoke readout (directional only) appended to the Rulings section; final suite run</name>
  <files>docs/CLASS-PASS.md</files>
  <read_first>
    - docs/CLASS-PASS.md "Phase 23 note" paragraph (~L590-636) — the exact caveat wording and table shape to mirror
    - tools/tune-classes.mjs usage block (~L82-100: --sub, --race, --seeds)
    - docs/CLASS-PASS.md BEFORE BY SUBCLASS / BY RACE roll-ups (the means to quote beside the smoke numbers)
  </read_first>
  <action>
    1. Run, one at a time (each ~120 runs, expect well under a minute each): `node tools/tune-classes.mjs --sub Knight --seeds 20`, `--sub "Court Mage" --seeds 20`, `--sub Guard --seeds 20`, `--sub Pickpocket --seeds 20`, `--race Fridgian --seeds 20`, `--race Dwarven --seeds 20`. Quote each run's aggregate mean depth and stuck count verbatim (the tool never emits editorial language; do not add any).
    2. Append `### Phase 24 note (2026-09-14, smoke readout — NOT the AFTER matrix)` at the END of the Rulings section (before `## AFTER`): a table `| Sub / Race | BEFORE mean depth (roll-up) | Phase 24 smoke (20 seeds) | Stuck |` with the six rows, quoting the BEFORE roll-up means from this ledger's own BEFORE section, followed by the same explicit caveat paragraph the Phase 23 note uses (different sample size/flags; directional only; the paired AFTER matrix is Phase 26 / PLAY-02). If any run reports stuck > 0, record it as a finding for Phase 25/26 — do not change a dial or the bot.
    3. Run `npm test` and `node --test "test/parity/**/*.test.js"` one final time and quote the pass counts in the SUMMARY (the phase baseline was 1036 / 32).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Phase 24 note" docs/CLASS-PASS.md` == 1 and the note sits before `## AFTER` (`awk '/^### Phase 24 note/{a=NR} /^## AFTER/{b=NR} END{exit !(a<b)}' docs/CLASS-PASS.md` exits 0)
    - `sed -n '/^### Phase 24 note/,/^## AFTER/p' docs/CLASS-PASS.md | grep -c "NOT the AFTER matrix"` >= 1
    - `sed -n '/^### Phase 24 note/,/^## AFTER/p' docs/CLASS-PASS.md | grep -c "^| "` >= 8 (header + separator + 6 rows)
    - `sed -n '/^## AFTER/,$p' docs/CLASS-PASS.md | grep -c "Nothing recorded yet."` == 1
    - `npm test` ends with `# fail 0`; `node --test "test/parity/**/*.test.js"` exits 0
    - `git status --porcelain engine content src test` prints nothing (docs-only plan)
  </acceptance_criteria>
  <done>Six smoke runs are recorded as a caveated directional signal in the Rulings section, the AFTER placeholder is intact, and the full suite is green at phase close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tool output -> ledger | tune-classes stdout is quoted into a doc; no code path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-16 | Repudiation | ledger numbers | low | mitigate | Every number is transcribed from a SUMMARY or tool output, never hand-derived; acceptance criteria pin headings and the untouched AFTER section |
| T-24-17 | Tampering | prototype-master.js.txt | high | mitigate | git diff over the phase range asserted empty |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase |
</threat_model>

<verification>
- Rulings section complete (grep-able headings: dagger ruling, Freeze pays out, good/bad table, Phase 24 note); AFTER section byte-identical.
- FIXTURE-INVENTORY Phase 24 section present; generated block unchanged; prototype master untouched over the phase range.
- `npm test` ends `# fail 0`; parity green.
</verification>

<success_criteria>
- IDENT-10 recorded as a deliberate KEEP ruling with rationale; every Phase 24 decision has a one-line rationale in the ledger; the fixture-divergence paper trail is complete; the phase closes green.
</success_criteria>

<output>
Create `.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-07-SUMMARY.md` when done, quoting the six smoke means, the final npm/parity pass counts, and the two git diff lines that prove the FID-07 gate.
</output>
