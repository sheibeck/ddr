---
phase: 71-device-round-polish-ii
plan: 05
type: execute
wave: 4
depends_on: ["71-03", "71-04"]
files_modified:
  - src/browser/fightLog.js
  - test/unit/fightLog.test.js
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/round-summary-band.test.js
  - test/unit/shell-combat-screen.test.js
  - test/unit/shell-fight-log.test.js
  - test/unit/combat-beat-shell.test.js
  - test/unit/combat-lock-shell.test.js
  - test/unit/foe-inspect-shell.test.js
  - test/unit/reduced-motion.test.js
  - test/unit/hp-not-wp.test.js
autonomous: true
requirements: [POLISH-07]
tags: [vanilla-js, presentation-only, combat, round-summary, mock, milestone-v2.0]

must_haves:
  truths:
    - "The combat screen follows the user's combat v2 mock: a fixed header, a scrolling middle holding only the foes and the party (flex:1, overflow auto), the fixed 'what happened' strip directly above the actions, then the fixed action area; foe count never pushes the strip or the actions off-screen, at text size M and L with 3 or more foes (D-07)"
    - "The strip is in the normal flex flow (flex:none, never absolute or fixed), so it never covers a foe card or an action; its body is a fixed 78px, bottom-aligned box with a top fade mask showing at most the last 3 lines of the latest round, the newest bright and the older ones dim (D-07)"
    - "The strip label reads 'ROUND n · WHAT HAPPENED'; while a round resolves it reads 'RESOLVING' in the mock's red and pulses, with no pulse under reduced motion; the right chip reads 'FULL LOG · count ›' (D-07)"
    - "The strip never spoils a playing round: during a beat it shows only lines the beat has revealed, the newest revealed line types in the strip through Phase 58's shared 'fightlog' typewriter and rises in, a skip completes it, and reduced motion shows it complete at once; #enc-round-live still announces the whole round once (D-07, D-06)"
    - "Content comes from a pure roundSummary(log, beatView): the entries of the newest round in append order, the last 3 kept whole (never cut mid-text), gated on the beat's maxId; an empty, null or malformed log gives no lines and never throws; a new fight never shows the previous fight's lines (D-07)"
    - "The in-panel fight log leaves the middle (the mock's middle holds foes and party only); the full log opens from the strip in 71-06's THE FIGHT SO FAR sheet, and the Oracle stays the complete per-swing log (D-07)"
    - "A tap on the strip while a round resolves is caught by Phase 58's single beatHurryTap (the strip is inside #enc-panel) and skips; it never opens anything (D-06)"
    - "71-04's foe details card lifts to the strip's top edge, so it covers neither the strip nor the actions (D-07, D-10)"
  artifacts:
    - path: "src/browser/fightLog.js"
      provides: "roundSummary(log, beatView) -> { round, lines, newestId, total }, ROUND_STRIP_COPY"
      exports: ["roundSummary", "ROUND_STRIP_COPY"]
    - path: "mazeworld.html"
      provides: "the #cb-summary strip between #cb-mid and #cb-act (label, chip, 78px body), the beat reveal and typing moved into it, the in-panel log removed from #cb-mid, the rail lift re-pointed, the strip CSS from the mock"
      contains: "cb-summary"
    - path: "test/unit/round-summary-band.test.js"
      provides: "sandbox and source pins for the strip's placement, content, beat gating, label and no-spoil rule"
  key_links:
    - from: "mazeworld.html renderEncounter combat branch"
      to: "window.__mzFightLogVM.summary(log, bv)"
      via: "renderRoundStrip(body) between #cb-mid and #cb-act"
      pattern: "cb-summary"
    - from: "the strip's newest line"
      to: "window.__mzTypewriter.type('fightlog', …)"
      via: "the Phase 58 D-14 reveal, re-hosted from the old log row"
      pattern: "fightlog"
---

<objective>
Keep the latest round in view with the user's "what happened" strip, built to the combat v2 mock (the round-summary half of POLISH-07; D-07, with D-06's skip preserved).

Purpose: in a multi-foe fight the foe list pushes the round text below the fold. The user's updated mock answers D-07 with:
- a fixed strip, "ROUND n · WHAT HAPPENED", directly above the actions, showing the last three lines of the latest round;
- a middle that holds only foes and party and scrolls on its own;
- the full log moved into a sheet the strip opens. That sheet is 71-06's.

This plan closes item 2 (round-summary visibility) of `.planning/todos/pending/2026-09-24-combat-lock-actions-while-a-round-plays-and-keep-the-round-summary-visible.md`. Item 1 landed in 71-03, so the SUMMARY names the todo as fully closed.

DESIGN SOURCE: read both files first, before any markup or CSS:
- `design/Mazeworld Combat Panel v2.dc.html`, the verbatim markup;
- `design/COMBAT-V2-NOTES.md`, the behaviour summary.
The strip's structure, sizes, type and colours come from them:
- border-top 3px #3a3226, background #16120c;
- label #a89c82, RESOLVING #e07260 with mwtorch .9s steps(2);
- chip #8f856f at 6px;
- body 78px with the 22px top mask;
- lines 14px bold, 1.4 line-height, newest #e6ddc6, older #8f856f, the newest rising in with mwrise .22s.
The mock's sample content and its "wp" field name are illustrative: the game keeps its real text, and HP never WP.

Planner rulings (record in the SUMMARY):
- R-18, D-06 with the strip (the mock's notes ask for this ruling): the strip sits inside #enc-panel, so during a live beat Phase 58's capture-phase beatHurryTap catches a tap on it, stops propagation and skips. The strip's own tap (which opens the log, in 71-06) can never fire mid-round. No new listener is needed.
- R-19, the in-panel log leaves the middle: the mock's middle holds foes and party only. D-07's "the full round text or log stays scrollable above it" is met by:
  - the strip (the latest round, always visible);
  - 71-06's THE FIGHT SO FAR sheet (the full log, scrollable, grouped by round, opened from the strip);
  - the Oracle, which stays the complete per-swing log.
  Until 71-06 lands in the next wave, the full log is reachable through the Oracle tab. The revert is to put renderFightLog(mid) back.
- R-20, the typing moves with the reveal: Phase 58's line-by-line beat reveal and its "fightlog" typewriter now target the strip's newest line. The hurry/complete/adopt rules, the aria-hidden-while-typing rule and the whole-round announcer (#enc-round-live, D-16) are unchanged. The mock's rise-in is added to the newest line, and under reduced motion both are dropped (the typewriter already resolves at once).
- R-21, "latest round": the strip shows the entries whose `round` equals the newest entry's round, falling back to the newest batch when round is null. A refusal line appended in the same round belongs to it.
- Mock deltas outside D-05..D-07 are not built here. The SUMMARY lists every one under "Mock deltas outside D-07" for the orchestrator. Examples: the "{NAME}'S TURN" party prompt, the STRIKE/SPELL/GEAR/RUN grid labels, the submenu max-height and rise, and the HP bar steps(6).

Backlog 999.5 (Combat screen & Oracle readability) overlap, folded here as notes and recorded in the SUMMARY:
- (1) submenu rows clipping, spell order: no overlap. It stays in 999.5, with 999.6.
- (2) foe family after the name on the card: related to D-09 (71-04's card shows the family). The on-card label stays in 999.5; the mock does not add it.
- (3) Oracle lines in event order: the strip keeps the fight log's own fold order, the same order the sheet shows, so the two never disagree. When 999.5 moves the fold to event order, both follow with no change here.
- (4) status chit in combat shows nothing: 71-04's combat-legal rail path is the mechanism that item needs. It stays in 999.5.
- (5) the scroll-read refusal narration: no overlap.

Output: roundSummary, the strip, the relocated beat reveal, the re-pointed lift, and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-device-round-polish-ii/71-CONTEXT.md
@design/COMBAT-V2-NOTES.md
@design/Mazeworld Combat Panel v2.dc.html
@.planning/todos/pending/2026-09-24-combat-lock-actions-while-a-round-plays-and-keep-the-round-summary-visible.md
@.planning/phases/71-device-round-polish-ii/71-03-SUMMARY.md
@.planning/phases/71-device-round-polish-ii/71-04-SUMMARY.md
@.claude/CLAUDE.md
@src/browser/fightLog.js

<interfaces>
Post-71-04 master. Line numbers are approximate; grep each anchor.

src/browser/fightLog.js:
- the log shape is { seq, nextId, entries: [{ id, seq, text, tone, roll, round, show }] };
- appendFightLog adds one batch per dispatch, sharing one seq and its round;
- fightLogRows returns newest-first; toggleFightLogEntry; fightLogAnnouncement.
It is bridged as `window.__mzFightLogVM = { rows, toggle, announcement, append, dull }` (module ~6600, mirrored in test/unit/harness/shellSandbox.js ~255).

The beat view (window.__mzBeat.view()) returns { state, log, maxId, typeId, hitFoe }.

mazeworld.html:
- renderFightLog(host) (~3659) builds section#cb-log with .cb-log-entry rows:
  - only ids <= bv.maxId while a beat is live;
  - revealable rows toggle the roll on tap;
  - it calls syncFightLogLive(log);
  - it types the bv.typeId row through window.__mzTypewriter "fightlog" (type/adopt/cancel, lastBeatTypedId).
- The combat branch of renderEncounter (~4495-4540):
  - header → `mid.id = "cb-mid"` → renderFoeCards(mid) → renderYourLot(mid) → renderFightLog(mid) → body.appendChild(mid);
  - then the lastLogSeqShown scrollIntoView gate → `act.id = "cb-act"` → renderActionArea(act).
- CSS: .cb-mid{flex:1;overflow:auto;min-height:0}, .cb-act{flex:none…}, and the .cb-log* rules.
- 71-03 added #cb-act[data-locked] and the HOLD prompt. 71-04 added the combat-legal rail card lifted to #cb-act's top through a CSS custom property.
- @keyframes mwrise exists (the rail uses it); check whether mwtorch exists, and add it from the mock if not.

Pins this plan must deliberately re-point, never weaken:
- combat-beat-shell (1)(2)(3)(5)(6): the rows revealed one by one, the typed row aria-hidden, the whole-round announcer after the first render, hurry revealing everything with none typing. They move from .cb-log-entry in #enc-body to the strip's lines.
- shell-fight-log.test.js: the renderFightLog row pins, reveal toggles and scroll gate. Each moves to the strip or to 71-06's sheet; a pin whose surface moves to the sheet is marked for 71-06 in a comment rather than deleted silently.
- shell-combat-screen CSCR-01 (order), CSCR-04, and CSCR-08 (no card/body/panel listeners in the region; exactly one capture click on #enc-panel; no aria-disabled token in <style>; no transitionend/animationend).
- reduced-motion.test.js: the beat section and the paint()/draw() SHA-256 pins. Never edit paint() or draw().
- combat-lock-shell (71-03) and foe-inspect-shell (71-04, the lift).
- bridge-registry, then bridge-doc --write/--check.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: roundSummary — the strip's pure, spoiler-safe content (D-07)</name>
  <files>src/browser/fightLog.js, test/unit/fightLog.test.js, test/unit/hp-not-wp.test.js</files>
  <behavior>
    - roundSummary(log, beatView = null) returns frozen { round, lines, newestId, total }:
      - `round` is the newest entry's round, or null;
      - `lines` holds at most the LAST 3 entries of that round (R-21), in append order (oldest → newest), each { id, text, tone, newest } with text whole and exactly one entry marked newest;
      - `newestId` is that entry's id;
      - `total` is the whole log's entry count (the FULL LOG chip).
    - With a beatView, it reads beatView.log and keeps only entries with id <= beatView.maxId, so a playing round shows only revealed lines. `total` counts the full log after the round (the chip is not a spoiler).
    - A null or undefined log, empty entries, a malformed entry list, or a beat that has revealed nothing yet give { round: null or the round, lines: [], newestId: null, total } with no throw.
    - Boundaries: a round of 1 entry gives 1 line; 3 give 3 lines; 4 give the last 3 (the first is dropped); a refusal appended with the same round joins that round's lines; a round of null falls back to the newest batch.
    - It is pure: two calls deep-equal, and it never mutates log.
  </behavior>
  <action>
RED first: add a "roundSummary (Phase 71 D-07)" section to test/unit/fightLog.test.js covering every behavior bullet, using appendFightLog and dullFightLogLine to build real logs. Add ROUND_STRIP_COPY's voice scan against content/safety-wordlist.js BANNED/ALLOWLIST, and register it in hp-not-wp.test.js's banks. Run and see it fail. Commit as test(71-05).
GREEN: add roundSummary and the frozen ROUND_STRIP_COPY (the strip's label, busy label and chip templates, as quoted in Task 2) to src/browser/fightLog.js. Its JSDoc says:
- D-07's strip content (the mock's last 3 lines of the latest round);
- R-21;
- the fold order, deliberately the log's own (999.5 note (3));
- the no-spoil beat gating.
Run fightLog, shell-fight-log and combat-beat green. Commit as feat(71-05): roundSummary for the what-happened strip.
  </action>
  <verify>
    <automated>node --test test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/combat-beat.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <done>roundSummary returns the last three whole lines of the latest round, never an unrevealed beat line, is safe on empty or malformed logs, and all listed tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The what-happened strip above the actions, the beat reveal moved into it, the log out of the middle (D-07, D-06)</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/round-summary-band.test.js, test/unit/shell-combat-screen.test.js, test/unit/shell-fight-log.test.js, test/unit/combat-beat-shell.test.js, test/unit/combat-lock-shell.test.js, test/unit/foe-inspect-shell.test.js, test/unit/reduced-motion.test.js</files>
  <behavior>
    - The combat branch, per the mock:
      - header → #cb-mid (foes, then YOUR LOT; renderFightLog is no longer called there, R-19) → body.appendChild(mid) → a new renderRoundStrip(body) → #cb-act;
      - the lastLogSeqShown scroll gate goes (nothing in the middle scrolls to a log any more);
      - the pending overlay, the over-panels, the loot, store, stair and death branches never build a strip.
    - renderRoundStrip(host) builds div#cb-summary (flex:none) through createElement/textContent from `window.__mzFightLogVM.summary(bv ? bv.log : window.__mzFightLog, bv)`:
      - a head row with the label (`ROUND n · WHAT HAPPENED`, or `RESOLVING` with data-busy="1" while window.__mzBeat.active()) and the chip `FULL LOG · total ›`;
      - the 78px body with one row per line, the newest carrying a newest marker;
      - the label and chip text come from fightLog.js's frozen ROUND_STRIP_COPY (roundHappened "ROUND {n} · WHAT HAPPENED", resolving "RESOLVING", fullLog "FULL LOG · {n} ›") through the __mzFightLogVM bridge (for example `copy: ROUND_STRIP_COPY`), never as literals declared in the shell (shell-no-content-copies).
      - It calls syncFightLogLive with the WHOLE log (the announcer is unchanged).
      - When bv.typeId equals the newest line's id, it types that row through window.__mzTypewriter "fightlog" with the same type/adopt/cancel and lastBeatTypedId rules renderFightLog used (R-20). Otherwise it cancels any stray run.
      - It adds no listener to the strip in this plan (71-06 adds the open-the-sheet tap).
    - renderFightLog stays defined but is no longer called from the combat branch; 71-06 reuses its row building for the sheet. Its reveal-toggle, roll and announcer logic stay intact.
    - CSS from the mock:
      - #cb-summary: flex:none, border-top 3px #3a3226, background #16120c, padding 9px 12px 10px;
      - the label and chip in the mock's fonts and colours, with `[data-busy="1"]` in #e07260 running mwtorch .9s steps(2) infinite (add the keyframes if missing);
      - a body of height 78px, a column justified flex-end, overflow hidden, and the -webkit-mask-image/mask-image top fade;
      - rows 14px bold 1.4, older #8f856f, newest #e6ddc6 with mwrise .22s;
      - under prefers-reduced-motion the pulse and rise are off (the blanket rule, confirmed by a pin);
      - no position:absolute or fixed on the strip; #cb-mid keeps flex:1, overflow:auto, min-height:0.
    - 71-04's rail lift reads #cb-summary's top when the strip exists, else #cb-act's.
    - window.__mzFightLogVM gains `summary: roundSummary` (module and sandbox mirror), and the BRIDGE purpose names it.
    - Sandbox proof (round-summary-band.test.js, reducedMotion false, fake clock; midFightRound/driveHandoff copied from combat-beat-shell):
      - (a) after a settled round, #cb-summary is the element between #cb-mid and #cb-act, its rows equal roundSummary's texts, #cb-mid holds no .cb-log-entry, and the label reads the round;
      - (b) mid-beat, the rows are exactly the revealed lines, the label is RESOLVING with data-busy "1", and the typed row is aria-hidden while typing;
      - (c) beatHurryTap with a target inside #cb-summary stops propagation, ends the beat (R-18) and opens nothing;
      - (d) a 4-foe fight still renders the strip as the sibling directly before #cb-act;
      - (e) the pending overlay has no strip, and a fresh fight after a finished one shows no previous lines;
      - (f) #enc-round-live holds the whole round after the first beat render.
    - Re-pointed pins:
      - combat-beat-shell (1)(2)(3)(5)(6) read the strip's rows with the same meaning;
      - shell-fight-log's renderFightLog pins: row building and reveal stay pinned on the function; the call-site pin now says it is not called from the combat branch (R-19, 71-06 re-hosts it);
      - CSCR-01's order becomes header → cb-mid → foes → lot → summary strip → cb-act;
      - reduced-motion's beat section pins that the strip resolves at once under reduced motion.
  </behavior>
  <action>
0. Read design/COMBAT-V2-NOTES.md and design/Mazeworld Combat Panel v2.dc.html in full. Note every mock element outside D-05..D-07 for the SUMMARY's "Mock deltas outside D-07".
RED first:
- Create test/unit/round-summary-band.test.js with (a)-(f) plus source pins: the strip's CSS flex:none with no absolute/fixed; #cb-mid's flex:1/overflow:auto; no aria-live on #cb-summary; the lift reads #cb-summary first; renderRoundStrip sits outside nothing new (no listener).
- Re-point the pins listed in behavior (combat-beat-shell, shell-fight-log, shell-combat-screen, reduced-motion, foe-inspect-shell), keeping every assertion's meaning.
Run and see them fail. Commit as test(71-05).
GREEN in mazeworld.html per D-07 and R-18..R-21:
- Add renderRoundStrip beside renderFightLog. Re-host the beat typing in it, with a comment naming R-20 and Phase 58 D-14/D-16.
- Change the combat branch (R-19), with a comment naming the mock and the revert path.
- Add the strip CSS from the mock under a Phase 71 D-07 comment.
- Re-point the lift.
- Extend window.__mzFightLogVM with summary.
- Never edit paint(), draw(), beatHurryTap or its listener, and add no click listener to #enc-panel.
- test/unit/harness/shellSandbox.js: add summary to the __mzFightLogVM mirror.
- src/browser/bridge.js: update __mzFightLogVM's purpose/consumers. Run bridge-doc --write/--check, saving with LF if --check complains only about CRLF.
- docs/SHELL-MODULES.md: a D-07 paragraph (the strip, R-19/R-20, the mock).
Run the Task 2 files plus shell-input-guards, bridge-registry, shell-no-content-copies, hp-not-wp and rail-overlay green, then `npm test` (worktree: only the 7 known CRLF failures). Commit as feat(71-05): the what-happened strip above the combat actions.
  </action>
  <verify>
    <automated>node --test test/unit/round-summary-band.test.js test/unit/shell-combat-screen.test.js test/unit/shell-fight-log.test.js test/unit/combat-beat-shell.test.js test/unit/combat-lock-shell.test.js test/unit/foe-inspect-shell.test.js test/unit/reduced-motion.test.js test/unit/shell-input-guards.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/hp-not-wp.test.js test/unit/rail-overlay.test.js && node tools/bridge-doc.mjs --check</automated>
    <human-check>Deferred to the Phase 71 device round (folded into section M by 71-06):
      1. Fight 3 or more foes at text size M and then L. The strip "ROUND n · WHAT HAPPENED" sits directly above the buttons and shows the last three lines of the round (the newest bright). The foes and your lot scroll above it, and nothing covers a foe card or a button.
      2. While a round plays, the strip's label pulses RESOLVING and only lines that have already appeared show there. Tap the strip mid-round: the round lands at once and nothing opens.
      3. The strip, label, chip and fade match the combat v2 mock.
      4. With a foe details card up (long press), the card sits above the strip and the buttons.
      5. Reduced motion on: the strip shows each round complete with no pulse and no rise.</human-check>
  </verify>
  <done>The combat screen matches the mock's layout: foes and party scroll in the middle, and the strip sits in the flow above the actions showing the latest round's last three lines without spoiling a playing round. The beat typing lives in the strip, a mid-round strip tap skips, and the foe card sits above the strip. Every listed test passes, and bridge-doc --check exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| beat playback → strip | the strip must never reveal a result the playing round has not reached |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-09 | Information disclosure (a spoiled round) | the strip during a beat | low | mitigate | roundSummary gates on beatView.maxId; round-summary-band (b) proves it |
| T-71-10 | Denial of service (a covered control) | the strip's layout | low | mitigate | in-flow flex:none strip between the scrolling #cb-mid and #cb-act; source pins forbid absolute/fixed positioning |
</threat_model>

<verification>
- Both tasks' verify commands pass. `npm test` has 0 failures (in a worktree, only the 7 known CRLF failures). bridge-doc --check exits 0.
- There is no engine/, content/ or test/parity/ change, and paint()/draw() are byte-identical. #enc-panel still has exactly one capture click listener.
</verification>

<success_criteria>
- D-07: the latest round's summary is always visible in the mock's fixed strip above the actions, never covering the foe cards or the actions, holding with 3+ foes at text size L. The full log stays reachable, through the Oracle now and 71-06's sheet next.
- D-06: a tap on the strip mid-round skips and opens nothing.
</success_criteria>

<output>
Create `.planning/phases/71-device-round-polish-ii/71-05-SUMMARY.md` when done. It names the combat-lock todo as fully closed (items 1 and 2), rulings R-18..R-21, the 999.5 notes (1)-(5), "Mock deltas outside D-07", and a "Human verification (deferred)" section listing the five checks above.
</output>
