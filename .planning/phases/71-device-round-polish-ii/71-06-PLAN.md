---
phase: 71-device-round-polish-ii
plan: 06
type: execute
wave: 5
depends_on: ["71-05"]
files_modified:
  - src/browser/fightLog.js
  - test/unit/fightLog.test.js
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/fight-log-sheet.test.js
  - test/unit/shell-fight-log.test.js
  - test/unit/shell-combat-screen.test.js
  - test/unit/hp-not-wp.test.js
  - docs/UAT-v2.0.md
autonomous: true
requirements: [POLISH-07]
tags: [vanilla-js, presentation-only, combat, full-log, sheet, uat, milestone-v2.0]

must_haves:
  truths:
    - "Tapping the what-happened strip (when no round is resolving) opens THE FIGHT SO FAR, a bottom sheet with the whole fight's log, newest first and grouped under ROUND n headers, scrollable inside the sheet (max-height 74%, the mock's scrim), so D-07's full log stays reachable from the combat screen (D-07)"
    - "A line whose fight-log entry carries roll text reveals it in gold on tap and hides it on a second tap; a line with no roll has no reveal affordance and nothing is invented; the 'TAP A LINE FOR ITS DICE' hint shows only when at least one entry has a roll (D-07)"
    - "CLOSE, a scrim tap or Android back closes the sheet (back closes only the sheet on that press); taps inside the panel do not close it; the sheet closes itself if the encounter ends (D-07)"
    - "A tap on the strip while a round resolves still only skips (beatHurryTap, R-18) and never opens the sheet; the strip's open handler is guardTap-wired, so encArmed() also refuses it mid-beat (D-06)"
    - "The sheet is not a live region (#enc-round-live stays the one announcer); it opens and closes through the shared panel-motion helper, instantly under reduced motion (D-07)"
    - "docs/UAT-v2.0.md has a new section M holding every Phase 71 device check (71-01..71-06), source-mapped in the house format, with the rows Phase 71 changed re-worded or superseded (D-13)"
  artifacts:
    - path: "src/browser/fightLog.js"
      provides: "fightLogByRound(log) and the sheet copy"
      exports: ["fightLogByRound"]
    - path: "mazeworld.html"
      provides: "#mw-fightlog-sheet (the legend-sheet family), openFightLogSheet/closeFightLogSheet, the strip's guarded open tap, the closeModal early return, the encounter-end close"
      contains: "mw-fightlog-sheet"
    - path: "test/unit/fight-log-sheet.test.js"
      provides: "sandbox and source pins for the sheet's grouping, reveal, open/close paths and the mid-round refusal"
    - path: "docs/UAT-v2.0.md"
      provides: "section M (Phase 71 device checks) and its Source map entries"
      contains: "Phase 71"
  key_links:
    - from: "mazeworld.html renderRoundStrip (#cb-summary)"
      to: "openFightLogSheet()"
      via: "guardTap(strip, openFightLogSheet)"
      pattern: "openFightLogSheet"
    - from: "mazeworld.html registerNativeChrome closeModal"
      to: "closeFightLogSheet() then return"
      via: "a sheet-first early return after the ☰ return"
      pattern: "closeFightLogSheet"
---

<objective>
Finish the user's combat v2 mock with THE FIGHT SO FAR sheet, the full log that the what-happened strip opens (the full-log half of D-07). Then fold every Phase 71 device check into the v2.0 UAT batch (D-13).

Purpose: 71-05 moved the fight log out of the combat middle into the fixed strip (the latest round). The mock puts the whole fight's log in a bottom sheet opened by tapping the strip, grouped by round, with a tap-a-line dice reveal. That keeps D-07's "the full round text or log stays scrollable" on the combat screen itself. D-13: the phase's device checks go into docs/UAT-v2.0.md section M for the batched Pixel 7 walk.

DESIGN SOURCE: read `design/COMBAT-V2-NOTES.md` (section 5, the full-log sheet) and `design/Mazeworld Combat Panel v2.dc.html` (the showLog block) before any markup or CSS:
- scrim rgba(10,8,6,.72);
- a panel with max-height 74%, background #1b170f and border-top 3px #6b5c3c;
- the title "THE FIGHT SO FAR" (7px, #e8c97a) with the hint (6px, #8f856f) and CLOSE (the 7px bordered chip);
- entries newest first under "ROUND n" headers;
- the "›" mark, text, and a gold roll line on reveal.
The mock's sample content is illustrative; real fight-log text only, and HP never WP.

Planner rulings (record in the SUMMARY):
- R-22, where the sheet lives: in the mock it is an absolute layer inside the combat panel. Here it is a body-level member of the existing .mw-legend-sheet family (#mw-fightlog-sheet, z 45, like the gear, settings and MARKS sheets), styled to the mock.
  - It gets the family's panel-motion open/close, reduced-motion handling, scrim and back-button patterns.
  - It adds no listener inside the renderEncounter render region (CSCR-08).
  - It is visually the mock's bottom sheet.
- R-23, dice: fight-log entries already carry `roll`, set from the engine event's own Oracle detail text by fightLogLinesFor. A line reveals exactly that and nothing else. When `roll` is null the line is plain (the mock notes' "never invent dice"). There is no dice-mode setting (DR18 retired it), so the behaviour is the mock's "on tap" mode.
- R-18 (from 71-05) holds: the strip's tap opens the sheet only when no round is resolving. Mid-round the tap is a skip.

D-13: the orchestrator rebuilds the debug APK and installs it on the Pixel 7 after this plan merges. This plan does NOT build, install or touch adb. The orchestrator moves each source todo to `.planning/todos/done/`. This plan closes no new todo; the combat todo closed with 71-05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-device-round-polish-ii/71-CONTEXT.md
@design/COMBAT-V2-NOTES.md
@design/Mazeworld Combat Panel v2.dc.html
@.planning/phases/71-device-round-polish-ii/71-01-SUMMARY.md
@.planning/phases/71-device-round-polish-ii/71-02-SUMMARY.md
@.planning/phases/71-device-round-polish-ii/71-03-SUMMARY.md
@.planning/phases/71-device-round-polish-ii/71-04-SUMMARY.md
@.planning/phases/71-device-round-polish-ii/71-05-SUMMARY.md
@.claude/CLAUDE.md
@src/browser/fightLog.js
@docs/UAT-v2.0.md

<interfaces>
Post-71-05 master. Line numbers are approximate; grep each anchor.

src/browser/fightLog.js:
- entries are { id, seq, text, tone, roll, round, show };
- toggleFightLogEntry(log, id) flips show;
- roundSummary and ROUND_STRIP_COPY (71-05).
It is bridged as `window.__mzFightLogVM = { rows, toggle, announcement, append, dull, summary, copy }`.

mazeworld.html:
- renderRoundStrip(host) builds #cb-summary (71-05) with no listener yet;
- renderFightLog(host) is still defined but not called from the combat branch; its row DOM (.cb-log-entry, .cb-log-mark, .cb-log-body, .cb-log-text, .cb-log-roll, revealable toggle) is reusable for the sheet;
- guardTap / encArmed / armEncounterButtons, whose sweep includes `.mw-legend-sheet [aria-disabled="true"]:not([data-off])`;
- the .mw-legend-sheet family markup and CSS (~1533-1549);
- the gear sheet's open/close (~5066-5103) and its scrim listener wired once, as the pattern;
- showPanel/hidePanel wrap window.__mzMotion (panelMotion);
- registerNativeChrome getGameContext (~7810):
  - hasOpenModal lists the open sheets;
  - closeModal's order is: the account sheet, the title panel, the ☰ menu-first return (70-04), the gear sheet, stair, camp/marks, then beats/store.
- renderEncounter's inactive branch clears __mzFightEnd/__mzCombatMenu.

Pins to respect:
- CSCR-08: no card/body/panel listeners in the render region; exactly one capture click on #enc-panel; no aria-disabled token in <style>; no transitionend/animationend.
- shell-fight-log.test.js: the pins 71-05 marked for 71-06 move to the sheet.
- reduced-motion: paint()/draw() SHA-256 and the panel-motion rules.
- hud-menu-layout (5)(20): the closeModal ☰-first return must stay first after the account/title returns.
- bridge-registry, then bridge-doc --write/--check.
- shell-no-content-copies.
- test/unit/harness/shellSandbox.js: recordingDom; invoke handlers through .onclick.

docs/UAT-v2.0.md house format (see sections L and Z and the Source map):
- `## L. Device-round polish — Phase 70 (29)`, then a one-paragraph lead, then a `| # | Step | Who | Result |` table, with Who `user` and Result `open`;
- the markers *(edge)*, *(after console setup; …)* and *(if available: …)*;
- the **Sources:** paragraph, **Suggested order**, the Source map `| Source | Item | Row | Disposition |` with `[NN-PP-Sk]` ids, and the closing **Tally** line.
- Row L1 says the theme is "clearly quieter than a UI tap or a footstep", and D-02 raised MUSIC_GAIN to 0.9. L22 (the ☰ tap lands a typing round) still stands.
- The **Build:** line is the orchestrator's.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: THE FIGHT SO FAR — the full-log sheet the strip opens (D-07, D-06)</name>
  <files>src/browser/fightLog.js, test/unit/fightLog.test.js, mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/fight-log-sheet.test.js, test/unit/shell-fight-log.test.js, test/unit/shell-combat-screen.test.js, test/unit/hp-not-wp.test.js</files>
  <behavior>
    - fightLogByRound(log) is pure and returns a frozen array of { round, entries }:
      - rounds newest first, entries within a round newest first, each entry { id, text, tone, roll, show };
      - null-round entries form one group labelled by the copy's fallback;
      - a null, empty or malformed log gives [];
      - two calls deep-equal, and it never mutates log.
    - The sheet copy is added to fightLog.js's frozen copy: title "THE FIGHT SO FAR", diceHint "TAP A LINE FOR ITS DICE", close "CLOSE", round "ROUND {n}", and a no-round fallback. It is voice-scanned and walked by hp-not-wp.
    - Markup: `#mw-fightlog-sheet.mw-legend-sheet[hidden]` with a scrim, a panel (role dialog, aria-label "The fight so far"), a head (title, hint, CLOSE) and a rows container, next to the other legend sheets.
    - CSS to the mock: scrim rgba(10,8,6,.72), panel max-height 74% (overriding the family's 78vh), the head and chip styles, ROUND n headers, rows reusing the .cb-log-* look, and the roll line in gold. No new transition beyond the family's own; no aria-disabled token in <style>.
    - openFightLogSheet():
      - a no-op while window.__mzBeat?.active?.() (R-18);
      - otherwise renders the groups from window.__mzFightLogVM, shows the hint only when some entry has a roll, and opens through showPanel;
      - each row with a roll is a tabbable toggle whose tap flips its show state through window.__mzFightLogVM.toggle on window.__mzFightLog and re-renders that row; a row without a roll gets no handler (R-23).
    - closeFightLogSheet() closes through hidePanel. CLOSE and the scrim are wired once, outside the render region.
    - renderRoundStrip wires `guardTap(strip, openFightLogSheet)` and gives the strip role="button", tabIndex 0 and an accessible name from the copy.
    - closeModal: `if (fightLogSheetOpen()) { closeFightLogSheet(); return; }` sits after the ☰ early return and before the gear sheet branch. hasOpenModal includes it.
    - renderEncounter's inactive branch closes the sheet if it is open.
    - Sandbox proof (fight-log-sheet.test.js):
      - (a) after two settled rounds, calling the strip's onclick after the arm window opens the sheet, and the headers read ROUND n newest first with entries newest first;
      - (b) a row with a roll toggles its roll line visible and hidden, and a row without one has no onclick;
      - (c) mid-beat, the strip's onclick does nothing (encArmed false) and openFightLogSheet() is a no-op;
      - (d) CLOSE and the scrim close the sheet;
      - (e) the closeModal source pin has the sheet return after the ☰ return and before the gear sheet;
      - (f) ending the encounter closes an open sheet.
    - Re-pointed pins: shell-fight-log's reveal/roll pins (marked by 71-05) now pin the sheet rows. CSCR-08 still holds: the strip's guardTap is not a body/panel/card listener, and #enc-panel still has one capture click.
  </behavior>
  <action>
0. Read design/COMBAT-V2-NOTES.md section 5 and the mock's showLog block.
RED first:
- fightLog.test.js: fightLogByRound cases and the copy scans; register the new copy in hp-not-wp.test.js.
- Create test/unit/fight-log-sheet.test.js with (a)-(f).
- Re-point the marked shell-fight-log pins, and extend shell-combat-screen with the strip's guardTap pin.
Run and see them fail. Commit as test(71-06).
GREEN per D-07, D-06, R-22 and R-23:
- src/browser/fightLog.js: add fightLogByRound and the copy.
- mazeworld.html:
  - add the sheet markup and CSS;
  - add openFightLogSheet, closeFightLogSheet and fightLogSheetOpen beside the gear sheet's open/close, reusing renderFightLog's row building. Either refactor renderFightLog into a row builder the sheet calls, or retire it if nothing else calls it, and record which;
  - wire CLOSE and the scrim once;
  - add the strip's guardTap, the closeModal and hasOpenModal lines, and the encounter-end close;
  - extend window.__mzFightLogVM with byRound;
  - never edit paint(), draw() or beatHurryTap.
- test/unit/harness/shellSandbox.js: add byRound to the mirror.
- src/browser/bridge.js: update __mzFightLogVM's purpose/consumers. Run bridge-doc --write/--check, saving with LF if --check complains only about CRLF.
- docs/SHELL-MODULES.md: a paragraph on the sheet (R-22, R-23).
Run the Task 1 files plus combat-beat-shell, combat-lock-shell, round-summary-band, foe-inspect-shell, shell-input-guards, hud-menu-layout, reduced-motion, bridge-registry and shell-no-content-copies green. Commit as feat(71-06): THE FIGHT SO FAR, the full log the strip opens.
  </action>
  <verify>
    <automated>node --test test/unit/fightLog.test.js test/unit/fight-log-sheet.test.js test/unit/shell-fight-log.test.js test/unit/shell-combat-screen.test.js test/unit/hp-not-wp.test.js test/unit/combat-beat-shell.test.js test/unit/combat-lock-shell.test.js test/unit/round-summary-band.test.js test/unit/foe-inspect-shell.test.js test/unit/shell-input-guards.test.js test/unit/hud-menu-layout.test.js test/unit/reduced-motion.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js && node tools/bridge-doc.mjs --check</automated>
    <human-check>Deferred to the Phase 71 device round (folded into section M below):
      1. After a few rounds, tap the what-happened strip. THE FIGHT SO FAR rises with every line of the fight, newest first, under ROUND n headers, and scrolls.
      2. Tap a line that shows dice: its roll appears in gold, and tapping again hides it. Lines without dice do nothing. CLOSE, a tap on the dark area, or Android back closes it, and the fight is exactly as it was.
      3. Tap the strip while a round is still resolving: the round lands, and the sheet does not open.</human-check>
  </verify>
  <done>The strip opens a mock-faithful full-log sheet grouped by round with honest dice reveals. It closes by CLOSE, scrim or back, and never opens mid-round. Every listed test passes, and bridge-doc --check exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Fold every Phase 71 device check into docs/UAT-v2.0.md section M, and the phase gates (D-13)</name>
  <files>docs/UAT-v2.0.md</files>
  <action>
1. Fold, per D-13, in the house format:
   - Add "## M. Device-round polish II — Phase 71 (N)" after section L and before Z. Give it a lead paragraph: walk on the Phase 71 debug APK the orchestrator installs with `adb install -r` (same debug signer, saves kept), and the section's order.
   - Rows M1..Mn: `| # | Step | Who | Result |`, Who `user`, Result `open`, using *(edge)* where it fits. The sources are the "Human verification (deferred)" sections of 71-01 (6 checks), 71-02 (4), 71-03 (5), 71-04 (7), 71-05 (5) and this plan's Task 1 (3). Order them for one pass:
     1. sound and sliders;
     2. the gear sheet;
     3. the combat lock and skip;
     4. the condition chips;
     5. long press;
     6. the what-happened strip;
     7. the full-log sheet.
     Merge duplicates into one row. For example, 71-03's "☰ mid-round" duplicates L22, so it becomes a "still holds" clause, and the mid-round strip tap in 71-05 and this plan merges into one row.
   - Rows Phase 71 changed:
     - L1 ("clearly quieter than a UI tap or a footstep") is re-worded for Phase 71: at MUSIC_GAIN 0.9 the theme is audible and still sits under a footstep or UI tap, and the MUSIC slider scales it (see M#).
     - Check sections A-L for any other row whose claim a Phase 71 change moved: the fight log's place on the combat screen, the foe card meta (HP on Bat/Rat and Viper), combat prompts, store row text and sheet contents. Re-word each to the Phase 71 behaviour, or mark it "Superseded by Phase 71 — see M#." with Result `superseded`.
   - Update the **Sources:** paragraph (add the six Phase 71 plan SUMMARYs with their item counts, and the re-worded/superseded rows), **Suggested order** (section M after L; none of its rows needs the console setup), and the **Tally** line (0 walked, dated 2026-09-24, naming section M's count and the re-worded or superseded rows).
   - In the Source map, add one line per folded item, `[71-PP-Sk]` with k in each SUMMARY's list order, each appearing exactly once with its row and disposition (own row / merged / split). Set the disposition of any existing source line whose row was re-worded or superseded to "re-worded for Phase 71 (M#)" or "superseded by Phase 71 (M#)".
   - Leave the **Build:** line for the orchestrator. Do not build, install or touch adb.
2. Phase gates:
   - Run `npm test`: 0 failures on master; in a worktree only the 7 known CRLF doc-ledger failures.
   - Run `node tools/bridge-doc.mjs --check`.
   - Over the whole phase, `git diff` from the parent of the first 71-01 commit must show:
     - engine/: 0 files;
     - test/parity/, including prototype-master.js.txt: byte-identical;
     - content/: 0 files, because the new copy lives in src/browser modules;
     - sfx/: byte-identical;
     - under test/unit/fixtures/: only the four snapshots 71-02 declared.
   Record the counts.
3. Write .planning/phases/71-device-round-polish-ii/71-06-SUMMARY.md with:
   - rulings R-22 and R-23 and the renderFightLog refactor-or-retire choice;
   - the UAT fold tally (rows added, re-worded, superseded);
   - the phase gate results;
   - the deferred device checks.
   Commit as docs(71-06): fold the Phase 71 device checks into UAT-v2.0, and docs(71-06) for the SUMMARY.
  </action>
  <verify>
    <automated>grep -q "^## M\. Device-round polish II — Phase 71" docs/UAT-v2.0.md && grep -q "\[71-06-S" docs/UAT-v2.0.md && npm test && node tools/bridge-doc.mjs --check && git diff --quiet HEAD -- engine test/parity content sfx</automated>
  </verify>
  <done>docs/UAT-v2.0.md has section M with every Phase 71 device check source-mapped once, and the changed rows are re-worded or superseded. The full suite is green (worktree: the 7 CRLF failures only). The phase-wide diff touches no engine/, content/, test/parity/ or sfx/ file and only the four declared fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player tap → combat state | the strip's open tap must never act mid-round, and the sheet must never act at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-11 | Tampering (a stray act) | the strip tap / the sheet | low | mitigate | the strip's open is guardTap-wired (encArmed false mid-beat) and beatHurryTap stops the tap first; the sheet only reads the log and flips a presentation-only show flag; fight-log-sheet (c) proves the mid-round refusal |
| T-71-12 | Information disclosure (invented dice) | the sheet's reveal | low | mitigate | a reveal shows only the entry's own engine-derived roll; rows with no roll get no handler (R-23), pinned by fight-log-sheet (b) |
</threat_model>

<verification>
- Both tasks' verify commands pass. `npm test` has 0 failures (in a worktree, only the 7 known CRLF failures). bridge-doc --check exits 0.
- Phase-wide: engine/, content/, test/parity/ and sfx/ are untouched, and only 71-02's four declared fixtures moved.
- docs/UAT-v2.0.md section M exists and every folded item appears once in the Source map.
</verification>

<success_criteria>
- D-07: the full log stays reachable and scrollable from the combat screen through THE FIGHT SO FAR, grouped by round, with honest dice reveals, never opened mid-round.
- D-13: every Phase 71 device check is in docs/UAT-v2.0.md section M, ready for the orchestrator's debug-APK install and the batched Pixel 7 walk.
</success_criteria>

<output>
Create `.planning/phases/71-device-round-polish-ii/71-06-SUMMARY.md` when done (contents listed in Task 2).
</output>
