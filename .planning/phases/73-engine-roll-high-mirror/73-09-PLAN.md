---
phase: 73-engine-roll-high-mirror
plan: 09
type: execute
wave: 7
depends_on:
  - 73-08
files_modified:
  - engine/encounters.js
  - engine/movement.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/roll-high-guard.test.js
  - test/parity/roll-high-invariant.test.js
  - test/unit/encounters.test.js
  - test/unit/movement.test.js
  - test/unit/linesForAction.test.js
  - test/unit/cutthroat-joiner.test.js
  - test/unit/one-and-done-lines.test.js
  - test/unit/identity-world.test.js
  - test/unit/narrationLinesTable.test.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "The trap dodge, both chest-lock rolls, every climb segment, the leap and the affliction cure are read roll-high through rollCheck, with the climb and leap penalties folded into the threshold (roll >= atLeastFor(success - penalty, 10)); the chest scroll and the climb fall-avoid stay already-high and are tagged"
    - "The wandering-monster wake stays oriented as it is (bad news at the bottom): the eight raw d20s are the hero's quiet checks, quiet at roll >= wakeOn + 1, and wanderingMonster carries rolls, atLeast and dieN"
    - "The Cutthroat's joiner murder stays a mishap on the natural 1, and joinerMurdered carries roll 1, atLeast 2 and dieN 20"
    - "trapAvoided, trapSprung and trapDisarmed carry the dodge triple (trapSprung's old roll was the trap-kind pick; its result is the trap name); chestLockRolled carries the lock triple beside opened; climbedOver and fellClimbing carry roll (the deciding segment) and rolls; leaptOver, fellInGorge, afflictionCured and afflictionLingers carry the triple; the trap and lock lines and the chest chain use the final shape"
    - "Every engine/*.js file is enforced (ALL_ENFORCED is true), so the guard fails the build on any roll-under shape, untagged draw or stray mirror anywhere in engine/; the invariant is COMPLETE, so every roll-carrying event carries atLeast unless it is a named table pick, and none carries need"
    - "Outcomes, draw counts and every baseline are unchanged"
  artifacts:
    - path: engine/encounters.js
      provides: "the trap dodge and chest locks on rollCheck; every draw tagged"
      contains: "atLeastFor(nimble, 20)"
    - path: engine/movement.js
      provides: "climb segments, leap and cure on rollCheck; the wake and the murder check tagged and reported"
      contains: "rollCheck(rng, 10"
    - path: test/unit/roll-high-guard.test.js
      provides: "ALL_ENFORCED = true over every engine file"
      contains: "ALL_ENFORCED = true"
    - path: test/parity/roll-high-invariant.test.js
      provides: "COMPLETE = true"
      contains: "COMPLETE = true"
  key_links:
    - from: test/unit/roll-high-guard.test.js
      to: engine/*.js (all 24 files)
      via: "ENFORCED equals the full engine listing"
      pattern: "ALL_ENFORCED"
    - from: src/browser/narrationLines.js#chestChain
      to: src/browser/rollRange.js#rollVsText
      via: "(roll vs lo–hi) appended to the outcome line"
      pattern: "rollVsText"
  prohibitions:
    - statement: "MUST NOT show a mirrored roll-high roll next to a roll-under need or threshold on any surface: the trap and lock lines and the chest chain use 'roll vs lo–hi'"
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Convert the dungeon checks (traps, chest locks, climbs, leaps, affliction cures), report the wake and the Cutthroat murder in their existing orientation, tag every remaining draw in encounters.js and movement.js, and close the guard and the invariant over the whole engine.

Purpose: CONTEXT Area 1 (verdict rows 30-33 and 40-45): "A wanderer wakes on a LOW roll: the bad news sits at the bottom, so there's no mirror"; "Mishap gates that fire on a natural 1 stay on the 1"; the rare d20 travel event is the Cutthroat murder check. CONTEXT Area 3: "A guard (fails the build)" over all of `engine/`, and every roll-carrying event natively carries the triple, "including soak, climb/leap, cure and wake" (REQUIREMENTS ROLL-05).

Output: the converted sites and their event fields, the trap and lock lines and the chest chain, the tags, ALL_ENFORCED and COMPLETE.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-09 creates or changes.)

- The helper and guard (73-01); baselines and the invariant (73-02); rollRange.js and the verdicts (73-03); the hero strike and foe soak (73-04); member/ally/thrown strikes and the isBestFace flip (73-05); resistance and mishaps (73-06); foe swings and the hero soak (73-07); flee, parley, gates and drops, with combat.js enforced (73-08)
- **This plan:** springTrap and openChest on rollCheck; climb segments, leap and cure on rollCheck; the wake and the murder check reported in their existing orientation
- **This plan:** trap/lock/climb/leap/cure/wake/murder event triples; the trapAvoided and chestLockRolled lines and the chest chain
- **This plan:** `ALL_ENFORCED = true` (the guard covers every engine file) and `COMPLETE = true` (the invariant's completeness rule is on)
- The proofs, the ledger close-out, the readout and the comment sweep (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/73-engine-roll-high-mirror/73-08-SUMMARY.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/encounters.js at d274925:
- springTrap (~L66-L100): `nimble = 5 + (Acrobat ? 3 : 0) + classTrapAvoidFor(c)`; `dodge = rng.d(20)`; `if (dodge <= nimble) { trapAvoided { roll: dodge, need: nimble }; return }`; Pilfer `trapDisarmed { reason: "pilfer" }`; `r = rng.d(8)` (the trap-kind pick, selection); damage (rollDice); `trapSprung { roll: r, name: tr.n, dmg }`.
- openChest (~L112-L150): tiered `need = [0, 5, 7, 8][Math.min(tier, 3)] + intelBonus(c); roll = rng.d(10); opened = roll <= need; chestLockRolled { roll, need, dieN: 10, picks, opened }`; bare `need = 8 + intelBonus(c); roll = rng.d(20); …dieN: 20…`; gold `(rng.d(10) + 6)…` (amount); scroll `rng.d(6) >= 3` (already-high).
- Other draws (tag): encounterDot table/roll (~L186-L187 selection), newDay food pick (~L357 selection), learnable count (~L377 amount), findMisc picks (~L411, L419, L430, L437 selection), meetFaerie gift pick (~L449 selection) and amounts (~L457, L462, L476), learnSpells level pick (~L532 selection) and amounts (~L534, L536), catchAffliction kind/phobia picks (~L633, L637, L678 selection) and duration (~L643 amount), goInsane pick (~L657 selection), might (~L668 amount).
engine/movement.js at d274925:
- move climb (~L258-L277): `hPenalty = heightsPenalty(c)`; `kind = rng.pick(...)`; `feet = 10 * (1 + rng.d(2))` (selection: the height pick); per segment `r = rng.d(10) + hPenalty + armorBulk(c); if (r <= tbl.success) continue; ok = false; for (g…) if (rng.d(20) > 2) hurt += rollDice(rng, tbl.fall)` (fall-avoid: already-high).
- move leap (~L278-L291): `row = LEAP_TABLE[rng.d(4) - 1]` (selection); `need = row.F|T|M`; `r = rng.d(10) + wPenalty + armorBulk(c); if (r > need) { ok = false; hurt = rng.d(6) + rng.d(6) }` (amount).
- the outcome pushes (~L300-L315): on failure `{ type: climbing ? "fellClimbing" : "fellInGorge", hurt }`, then die or `draggedOver`; on success `{ type: climbing ? "climbedOver" : "leaptOver" }`.
- newDay: camp heal (~L687 amount); cure `if (rng.d(20) <= 10 + (skill(c, "Hardiness") ? 4 : 0))` -> afflictionCured / afflictionLingers `{ kind }` (~L708-L713); armor patch amounts (~L737, L742); wake `wakeOn = wanderWakeFacesFor(c.sub); for (h < 8) if (rng.d(20) <= wakeOn) woke++;` then `wanderingMonster { hours: woke, bard }` (~L783-L793).
- teleport direction picks (~L839-L840 selection), distance (~L844 amount); cutthroatMurderCheck `if (rng.d(20) === 1)` -> joinerMurdered `{ name, sub, depth }` (~L950-L952).
src/browser/eventNarration.js: trapAvoided (~L826, "…half-step early. <roll> vs <need>."), chestLockRolled (~L834, "Lock: <roll> vs <need>."). src/browser/narrationLines.js: LINE_FOR.trapAvoided (~L1542), LINE_FOR.chestLockRolled (~L1554), chestChain (~L787-L809, appends `(${e.roll} vs ${e.need})`). Target: `<span class="roll">18 vs 16–20.</span>`, `Lock: <span class="roll">7</span> vs 6–10.` (keep the existing span layout of each line; change only the roll-vs part), the chest chain appends `(7 vs 6–10)` via rollVsText. The climb, leap, cure, wake and murder lines print no roll today; the rail's generic dice line (73-04) now reveals climbs and leaps as "roll N vs lo–hi".
test/unit/roll-high-guard.test.js: add encounters.js and movement.js to ENFORCED, then set `ALL_ENFORCED = true` (ENFORCED must equal every engine/*.js file). test/parity/roll-high-invariant.test.js: set `COMPLETE = true`.

Proof gates and test-update rules: exactly as in 73-04-PLAN.md `<interfaces>`. Wake and murder rolls are NOT mirrored (already oriented); trap, lock, climb, leap and cure rolls are (N+1−old).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Traps and chest locks, their events, lines and chain</name>
  <files>engine/encounters.js, src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/encounters.test.js, test/unit/linesForAction.test.js, test/unit/narrationLinesTable.test.js</files>
  <read_first>
    - engine/encounters.js (springTrap, openChest, and every `.d(` line)
    - docs/ROLL-LEDGER.md (verdict rows 30-33; Event fields)
    - src/browser/eventNarration.js (trapAvoided, chestLockRolled) and src/browser/narrationLines.js (LINE_FOR.trapAvoided, LINE_FOR.chestLockRolled, chestChain)
    - test/unit/encounters.test.js and test/unit/linesForAction.test.js (~L515-L525)
  </read_first>
  <action>
Per verdict rows 30-33:
1. springTrap: `check = rollCheck(rng, 20, atLeastFor(nimble, 20))` at the old draw position. On `.ok`, push trapAvoided `{ type, ...rollFields }`. A Pilfer's trapDisarmed gains the triple (the dodge failed). trapSprung becomes `{ type, ...rollFields(check), name, dmg }`: its old `roll` was the trap-kind pick, which stays a tagged selection draw whose result is `name`.
2. openChest: in both paths, `faces` (the old need) goes to `rollCheck(rng, 10 or 20, atLeastFor(faces, N))`; `opened = check.ok`; chestLockRolled `{ type, ...rollFields, picks, opened }`. Tag the gold amount and the scroll (already-high).
3. Tag every other raw draw in encounters.js per `<interfaces>`.
4. UI: eventNarration trapAvoided and chestLockRolled, LINE_FOR.trapAvoided and LINE_FOR.chestLockRolled, and chestChain move to the target shapes via rangeText and rollVsText.
5. Update the trap and lock assertions per the test-update rules.
  </action>
  <verify>
    <automated>node --test test/unit/encounters.test.js test/unit/linesForAction.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollCheck(rng, 20, atLeastFor(nimble, 20))" engine/encounters.js` equals 1, and `grep -c "rollCheck(" engine/encounters.js` equals 3.
    - `[trap:*]` and `[lock:*]` rows in rollDirection-checks.test.js pass unchanged; the state pins pass.
  </acceptance_criteria>
  <done>Traps and locks read roll-high end to end.</done>
</task>

<task type="auto">
  <name>Task 2: Climbs, leaps, the cure, the wake and the murder check</name>
  <files>engine/movement.js, test/unit/movement.test.js, test/unit/cutthroat-joiner.test.js, test/unit/one-and-done-lines.test.js, test/unit/identity-world.test.js</files>
  <read_first>
    - engine/movement.js (move ~L240-L320, newDay ~L680-L800, teleport ~L835-L850, cutthroatMurderCheck ~L940-L955, and every `.d(` line)
    - engine/difficulty.js (wanderWakeFacesFor ~L852)
    - docs/ROLL-LEDGER.md (verdict rows 40-45)
    - test/unit/movement.test.js, test/unit/cutthroat-joiner.test.js, test/unit/rollDirection-checks.test.js (the climb, leap, cure and wake rows, which must run unchanged)
  </read_first>
  <action>
Per verdict rows 40-45:
1. Climb: before the segment loop, compute `climbFaces = tbl.success - hPenalty - armorBulk(state.c)` (pure; the penalties are constant across segments). Each segment draws `rollCheck(rng, 10, atLeastFor(climbFaces, 10))` at the old position and records its roll in a `rolls` array; a segment passes on `.ok`. The fall-avoid line stays as it is and is tagged `already-high`. Tag the height pick (selection).
2. Leap: `check = rollCheck(rng, 10, atLeastFor(need - wPenalty - armorBulk(state.c), 10))`; failure is `!check.ok`. Tag the table pick (selection) and the fall damage (amount).
3. Outcome events: fellClimbing and climbedOver carry `roll` (the last segment's roll), `rolls`, `atLeast` and `dieN: 10`; fellInGorge and leaptOver carry the leap triple. draggedOver is unchanged.
4. Cure: `rollCheck(rng, 20, atLeastFor(10 + (skill(c, "Hardiness") ? 4 : 0), 20))`, drawn only when an affliction exists (as before); afflictionCured and afflictionLingers carry the triple.
5. Wake: the eight raw d20s stay unmirrored (already oriented), each on a line tagged `already-high`. The hero stays quiet on `roll >= wakeOn + 1`; count the hours that fail that check. wanderingMonster carries `{ hours, bard, rolls, atLeast: wakeOn + 1, dieN: 20 }`. Write the comparison in the roll-high form (roll >= atLeast); the guard forbids a raw draw compared at-or-under.
6. Cutthroat murder: the d20 stays a mishap on the natural 1 (tag `mishap-on-1`), drawn only under the old condition; joinerMurdered gains `roll`, `atLeast: 2`, `dieN: 20`.
7. Tag every other raw draw in movement.js. Rewrite the region comments to the faces reading.
8. Update the assertions per the test-update rules (climb, leap and cure rolls mirror; wake and murder rolls do not).
  </action>
  <verify>
    <automated>node --test test/unit/movement.test.js test/unit/cutthroat-joiner.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollCheck(rng, 10, atLeastFor(climbFaces, 10))" engine/movement.js` equals 1, and `grep -c "rollCheck(" engine/movement.js` equals 3 (climb, leap, cure).
    - `grep -c "wakeOn + 1" engine/movement.js` is at least 1 (the wake's quiet threshold carried as atLeast).
    - `grep -c "roll:mishap-on-1" engine/movement.js` equals 1.
    - `[climb:*]`, `[leap:*]`, `[cure:*]` and `[wake:*]` rows pass unchanged; the pins and the pre-switch save pass.
  </acceptance_criteria>
  <done>Every movement check reads from the right side of the die and reports its roll.</done>
</task>

<task type="auto">
  <name>Task 3: Close the guard and the invariant over the whole engine, and the proof gates</name>
  <files>test/unit/roll-high-guard.test.js, test/parity/roll-high-invariant.test.js</files>
  <read_first>
    - test/unit/roll-high-guard.test.js and test/parity/roll-high-invariant.test.js
    - `ls engine/*.js`
  </read_first>
  <action>
1. Add engine/encounters.js and engine/movement.js to ENFORCED with live DRAW_INVENTORY counts, then set `ALL_ENFORCED = true`. The guard must pass over all 24 engine files. A failure means an unconverted or untagged site: fix the engine, never loosen the guard.
2. Add the OUTCOME rows: trapAvoided (success), trapSprung and trapDisarmed (failure, only when they carry atLeast), chestLockRolled (success iff `opened`), climbedOver (success: every entry of `rolls` is at or above atLeast), fellClimbing (failure: the last roll is below atLeast and every earlier one is at or above it), leaptOver (success), fellInGorge (failure), afflictionCured (success), afflictionLingers (failure), wanderingMonster (`hours` equals the count of `rolls` below atLeast), joinerMurdered (failure).
3. Set `COMPLETE = true`. If I6 fails, an event still carries a roll without atLeast: convert its site (it is a check) or, only if it is a genuine table pick, add it to SELECTION_ROLL_EVENTS with a one-line reason and record it in the SUMMARY.
4. Run the proof gates.
  </action>
  <verify>
    <automated>node --test test/unit/roll-high-guard.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ALL_ENFORCED = true" test/unit/roll-high-guard.test.js` equals 1, and `grep -c "COMPLETE = true" test/parity/roll-high-invariant.test.js` equals 1.
    - `node --test test/unit/roll-high-guard.test.js` passes with every engine/*.js file enforced.
    - `grep -cE "^\s+(trapAvoided|trapSprung|trapDisarmed|chestLockRolled|climbedOver|fellClimbing|leaptOver|fellInGorge|afflictionCured|afflictionLingers|wanderingMonster|joinerMurdered)\s*[:(]" test/parity/roll-high-invariant.test.js` is at least 12.
    - All proof gates in 73-04's `<interfaces>` hold.
  </acceptance_criteria>
  <done>The whole engine is roll-high, guarded, and every roll-carrying event reports its triple.</done>
</task>

</tasks>

<verification>
- The proof gates hold; the guard enforces every engine file; the invariant is complete; the direction tests, pins and pre-switch save are unchanged and green.
</verification>

<success_criteria>
ROADMAP criteria 1, 3 and 5 are fully met across `engine/`: every check reads roll-high, the guard fails on any roll-under shape anywhere in the engine, and every roll-carrying event carries roll, atLeast and dieN. Criterion 2 holds for this plan's sites.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-09-SUMMARY.md` when done. Include the event shapes, the final DRAW_INVENTORY for all 24 files, any event added to SELECTION_ROLL_EVENTS (with its reason), the test counts, and a "Human verification (deferred to end of run)" note: a trap dodge and a chest lock read "<roll> vs lo–hi" in the Oracle; a climb's rail card reveals "roll N vs lo–hi".
</output>
