---
phase: 73-engine-roll-high-mirror
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/rollRange.js
  - test/unit/rollRange.test.js
  - docs/ROLL-LEDGER.md
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "src/browser/rollRange.js#rangeText(atLeast, dieN) writes a winning range the one way every surface uses: 'lo–hi' with an en dash (U+2013), a single winning face as just 'N', every face as '1–N', no winning face as 'nothing', and a missing or non-numeric field as '?'"
    - "rollVsText(roll, atLeast, dieN) writes Phase 74's final roll line core, '17 vs 18–20', with '?' for a missing roll"
    - "docs/ROLL-LEDGER.md records a verdict for every one of the 48 inventory sites plus the gate-shaped appendix draws: mirror, already high, mishap on 1, or not a check (selection or amount), with each mirrored site's new check, the event that carries its roll, and the plan that converts it"
    - "The ledger documents the event-field contract every conversion plan implements: roll, atLeast, dieN (plus rolls, mods, critAtLeast, auto, nested soak and bag), need removed from every roll-carrying event, and each event's outcome rule for the runtime invariant"
    - "The ledger records the two inventory corrections: items.js L250 is a treasure-kind selection (there is no lockpick-wear mechanic), and the startCombat tier bleed is a hero-side check that mirrors"
    - "test/unit/roll-ledger-sync.test.js stays green and unchanged (the new section adds no bracketed site:source ids)"
  artifacts:
    - path: src/browser/rollRange.js
      provides: "rangeText and rollVsText, pure, no DOM"
      exports: ["rangeText", "rollVsText"]
    - path: test/unit/rollRange.test.js
      provides: "the range-format contract"
    - path: docs/ROLL-LEDGER.md
      provides: "## Phase 73 mirror verdicts (ROLL-05), with ### Verdict per site, ### Event fields (Phase 73) and ### Corrections"
      contains: "## Phase 73 mirror verdicts (ROLL-05)"
  key_links:
    - from: src/browser/eventNarration.js, src/browser/narrationLines.js, src/browser/rail.js (from 73-04 on)
      to: src/browser/rollRange.js
      via: "import { rangeText, rollVsText }"
      pattern: "rollRange.js"
  prohibitions:
    - statement: "MUST NOT show a mirrored roll-high roll next to a roll-under need or threshold on any surface: every line that prints an event roll uses the 'roll vs lo–hi' shape once its event carries atLeast"
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Build the one range formatter every event-driven roll line uses, and write the per-site mirror verdicts into docs/ROLL-LEDGER.md as the checklist for plans 73-04 to 73-09.

Purpose: CONTEXT Area 1 ("The planner classifies every one of the 48 ledger sites plus the appendix... The per-site verdict... is recorded in the ledger") and Area 3 ("Use Phase 74's final line shape... The winning range is always written lo–hi (a single face reads 20)"). The formatter is shared so no two surfaces ever format a range differently. Phase 74 extends it with the signed-modifier formatter.

Output: src/browser/rollRange.js, test/unit/rollRange.test.js, and the new ledger section.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-03 creates or changes.)

- engine/dice.js `rollCheck`, `atLeastFor`, `rollFields`; the helper and guard tests; tags in the conversion-free files (73-01)
- The baseline harness, generator, pins, pre-switch save, readout baseline, readout-compare tool, and the runtime invariant (73-02)
- **This plan:** src/browser/rollRange.js (`rangeText`, `rollVsText`) and test/unit/rollRange.test.js
- **This plan:** docs/ROLL-LEDGER.md `## Phase 73 mirror verdicts (ROLL-05)` (73-10 fills in the status column and the handoffs)
- The event-field contract on every roll-carrying event (73-04 to 73-09); `BAG_DROP_FACES` and the FLEE.md payload (73-08); the Phase 73 DIFFICULTY-RETUNE H2, FIXTURE-INVENTORY section, voice-sample tool and comment sweep (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md
@docs/ROLL-LEDGER.md

<interfaces>
The line shape (74-CONTEXT, user-accepted 2026-09-25): "**17** vs 18–20 (light blade +1). You miss it." In Oracle HTML the bold roll is `<span class="roll">17</span>`. The range uses U+2013 (the repo already writes ranges like "1–2" with it; its minus sign is U+2212, never a hyphen).

rangeText rules: not a finite number for either argument -> "?"; atLeast > dieN -> "nothing"; atLeast === dieN -> String(dieN); otherwise `${Math.max(1, atLeast)}–${dieN}` (so atLeast <= 1 reads "1–N"). Examples: (16, 20) "16–20"; (18, 20) "18–20"; (20, 20) "20"; (21, 20) "nothing"; (1, 20) and (-3, 20) "1–20"; (2, 6) "2–6"; (2, 2) "2"; (1, 2) "1–2".

THE VERDICT TABLE for the new ledger section (transcribe it; the `#` column matches `## Site inventory`). N is the die; faces is today's count of winning faces; `atLeastFor(faces, N) = N + 1 - faces`.

| # | Site | Verdict | Roll-high check | Event(s) carrying roll/atLeast/dieN | Plan |
|---|---|---|---|---|---|
| 1 | combat.js#resolveInitiative 2×d20 | ALREADY HIGH (contested, `mine >= theirs`, the hero wins ties) | both draws tagged already-high | combatJoined keeps mine/theirs (contested, no atLeast) | 73-08 |
| 2 | Tracking | REMOVED (Phase 38) | — | — | — |
| 3 | combat.js#startCombat foe count d4 + difficulty.js#foeCountFor | NOT A CHECK (selection: the count table) | draws and the `firstRoll <= 2` dispatch tagged selection | — | 73-01 / 73-08 |
| 3b | combat.js#startCombat tier bleed `rng.d(4) <= tierSpreadFor()` | MIRROR (hero-side: a lower-tier foe is good news) | rollCheck(rng, 4, atLeastFor(tierSpreadFor(), 4)) | none | 73-08 |
| 4 | Con Artist weak-foe flee d6 `<= 4` | MIRROR | rollCheck(rng, 6, atLeastFor(4, 6)) = 3–6 | foeFled {reason: "conArtist"} | 73-08 |
| 5 | Court Mage boredom d12 `<= 2` | MIRROR | rollCheck(rng, 12, atLeastFor(2, 12)) = 11–12 | foeBored | 73-08 |
| 6 | fight phobia Hardiness shrug d2 `=== 1` | MIRROR | rollCheck(rng, 2, atLeastFor(1, 2)) = 2 | phobiaAfraid (when the shrug die was drawn and failed) | 73-08 |
| 7 | playerStrike frenzy trigger d8 `<= 5` | MIRROR | rollCheck(rng, 8, atLeastFor(5, 8)) = 4–8 | frenzy | 73-04 |
| 8 | playerStrike strike die (Philly two dice) | MIRROR | rollCheck(rng, dieN, atLeastFor(faces, dieN)); Philly keeps Math.max of two mirrored faces | struck / strikeMissed | 73-04 |
| 9 | Stealth crit `<= 2` | MIRROR | roll >= atLeastFor(2, dieN) | struck.critAtLeast | 73-04 |
| 10 | Ninja crit `<= 2` | MIRROR | roll >= atLeastFor(2, dieN) | struck.critAtLeast | 73-04 |
| 11 | weapon crit `<= weaponCrit(c)` | MIRROR | roll >= atLeastFor(weaponCrit(c), dieN) | struck.critAtLeast | 73-04 |
| 12 | killFoe loot drop d20 `<= 2 + f.lvl` | MIRROR (hero-side good news) | rollCheck(rng, 20, atLeastFor(2 + f.lvl, 20)) | lootDropped (kill drop) | 73-08 |
| 13 | killFoe bag drop d20 `<= BAG_DROP_UNDER` | MIRROR; the constant is renamed BAG_DROP_FACES | rollCheck(rng, 20, atLeastFor(BAG_DROP_FACES, 20)) | lootDropped.bag (nested) | 73-08 |
| 14 | killFoe cooking d6 `>= 4` | ALREADY HIGH | tagged already-high | — | 73-08 |
| 15 | pursuitStrike | MIRROR | rollCheck(rng, foeDie, atLeastFor(faces, dieN)) | foeMissed / struckByFoe | 73-07 |
| 16 | pursuit Soldier crit-taken | MIRROR | roll >= atLeastFor(Soldier ? 2 : 1, dieN) | struckByFoe.critAtLeast | 73-07 |
| 17 | flee d20 `roll + bonus >= need` | ALREADY HIGH; the bonus folds into the threshold: roll >= need - bonus | raw draw tagged already-high | fleeRolled {roll, atLeast, dieN, mods} (total removed) | 73-08 |
| 18 | parley d20 `<= min(9+bonus,17) - dial` | MIRROR | rollCheck(rng, 20, atLeastFor(faces, 20)) | parleyRolled | 73-08 |
| 19 | parley Humans payout d6 `=== 6` | ALREADY HIGH | tagged already-high | — | 73-08 |
| 20 | allyTurn summon strike | MIRROR | rollCheck on STRIKE_DICE[lvl-1]; Philly Math.max | allyStruck / allyMissed | 73-05 |
| 21 | alliesTurn legacy strike | MIRROR | same | allyStruck / allyMissed | 73-05 |
| 22 | memberStrike (+ member crit on the best face) | MIRROR | rollCheck(rng, dieN, atLeastFor(faces, dieN)); crit isBestFace | allyStruck / allyMissed | 73-05 |
| 23 | allyCast thrown `roll - bonus <= need` | MIRROR, bonus folded | rollCheck(rng, dieN, atLeastFor(need + bonus, dieN)) | allyCast | 73-05 |
| 24 | applyFoeDamageToPlayer hero armor soak d20 `<= soakAr` | MIRROR | rollCheck(rng, 20, atLeastFor(soakAr, 20)) | armorSoaked (success); struckByFoe.soak (failure) | 73-07 |
| 25 | foeTurn ability gate d6 `<= 4` | MIRROR (the foe is the roller; using its ability is its success) | rollCheck(rng, 6, atLeastFor(4, 6)) = 3–6 | foeCast | 73-08 |
| 26 | foeTurn member branch | MIRROR | rollCheck(rng, mDieN, atLeastFor(faces, mDieN)) | foeMissed {member} / memberStruck | 73-07 |
| 27 | member crit natural best | MIRROR | roll === dieN (isBestFace) | memberStruck.critAtLeast | 73-07 |
| 28 | foeTurn hero branch | MIRROR | rollCheck(rng, dieN, atLeastFor(faces, dieN)) | foeMissed / struckByFoe | 73-07 |
| 29 | hero Soldier crit-taken | MIRROR | roll >= atLeastFor(Soldier ? 2 : 1, dieN) | struckByFoe.critAtLeast | 73-07 |
| 30 | springTrap dodge d20 `<= nimble` | MIRROR | rollCheck(rng, 20, atLeastFor(nimble, 20)) | trapAvoided (success); trapSprung, trapDisarmed (failure) | 73-09 |
| 31 | openChest scroll d6 `>= 3` | ALREADY HIGH | tagged already-high | — | 73-09 |
| 32 | openChest tiered lock d10 | MIRROR | rollCheck(rng, 10, atLeastFor(need, 10)) | chestLockRolled | 73-09 |
| 33 | openChest bare lock d20 | MIRROR | rollCheck(rng, 20, atLeastFor(need, 20)) | chestLockRolled | 73-09 |
| 34 | foeDamage.js natural-armor soak d20 `<= sp.ar` | MIRROR | rollCheck(rng, 20, atLeastFor(foe.sp.ar, 20)) | foeArmorSoaked (success); struck.soak, allyStruck.soak (failure) | 73-04 |
| 35 | castSpell Apprentice backfire d8 `=== 1` | MISHAP ON 1 | tagged mishap-on-1 | spellBackfired {roll: 1, atLeast: 2, dieN: 8} | 73-06 |
| 36 | castSpell doubled backfire d8 `=== 1` | MISHAP ON 1 | tagged mishap-on-1 | summonBackfired {roll: 1, atLeast: 2, dieN: 8} | 73-06 |
| 37 | castSpell thrown `roll - bonus <= target` | MIRROR, bonus folded | rollCheck(rng, dieN, atLeastFor(target + bonus, dieN)) | spellThrown | 73-05 |
| 38 | castSpell vapor roll d6 | NOT A CHECK (selection: the outcome table) | tagged selection | vaporRolled keeps its table roll | 73-06 |
| 39 | castSpell vapor kill-save d10 `!== 1` | MISHAP ON 1 (the foe's save; a 1 fails) | tagged mishap-on-1 | none | 73-06 |
| 40 | movement climb d10 + penalties `<= success` | MIRROR, penalties folded | rollCheck(rng, 10, atLeastFor(success - penalty, 10)) per segment | climbedOver / fellClimbing (roll and rolls) | 73-09 |
| 41 | climb fall-avoid d20 `> 2` | ALREADY HIGH | tagged already-high | — (hurt) | 73-09 |
| 42 | movement leap d10 + penalties, fail `> need` | MIRROR, penalties folded | rollCheck(rng, 10, atLeastFor(need - penalty, 10)) | leaptOver / fellInGorge | 73-09 |
| 43 | newDay affliction cure d20 `<= 10 (+4)` | MIRROR | rollCheck(rng, 20, atLeastFor(10 + hardiness, 20)) | afflictionCured / afflictionLingers | 73-09 |
| 44 | newDay wandering wake d20 `<= wakeOn`, ×8 | ALREADY ORIENTED (bad news sits at the bottom): the hero's quiet check is roll >= wakeOn + 1, no mirror | raw draws tagged already-high | wanderingMonster {rolls, atLeast, dieN} | 73-09 |
| 45 | cutthroatMurderCheck d20 `=== 1` (the rare d20 travel event) | MISHAP ON 1 (bad news for the hero on the 1) | tagged mishap-on-1 | joinerMurdered {roll: 1, atLeast: 2, dieN: 20} | 73-09 |
| 46 | derived.js#resistRoll d20 `< intel` | MIRROR | rollCheck(rng, 20, atLeastFor(intel - 1, 20)) = 22 - intel | spellResisted, resistFailed, heroResisted, heroResistFailed, allySpellMissed {resisted: true} | 73-06 |
| 47 | items.js#rollTreasureItem `rng.d(12) === 1` | NOT A CHECK (selection: the treasure is Lockpicks; correction below) | tagged selection | — | 73-01 |
| 48 | character.js#checkLevel Sorcerer spell loss d8 `=== 1` | MISHAP ON 1 | tagged mishap-on-1 | none (no event today) | 73-01 |
| A1 | items.js tool-loot gate `toolRng.d(8) === 1`, weighted tool pick, and the r<=3/5/7/9 category ladder | NOT A CHECK (selection) | every draw and ladder line tagged selection | — | 73-01 |
| A2 | economy.js premium `rng.d(2) === 1` | NOT A CHECK (selection) | tagged selection | — | 73-01 |
| A3 | every other appendix draw | NOT A CHECK (selection or amount, per the appendix) | tagged | selection events keep a bare table roll | per file |

THE EVENT FIELDS table (`### Event fields (Phase 73)`), including each event's OUTCOME rule for test/parity/roll-high-invariant.test.js:
- Every roll-carrying event carries `roll` (the high-is-good face), `atLeast` (the lowest winning face; the threshold gets its own name because `target` already names the victim on about 48 event pushes) and `dieN`. `need` is removed from every roll-carrying event. Events that are not rolls keep their own `need` (hunger's ration count, the spell-level gates).
- `mods` replaces `needMods`: `{ name, delta }` with the same values, signed for the ROLLER (+1 face = +1 to hit). Phase 74 re-signs them from the player's side.
- `rolls`: an array of faces when one event reports several draws of the same check (climb segments, the eight wake hours).
- `critAtLeast`: present when a critical came from the die (weapon crit, Stealth, Ninja, a foe's natural best, the Soldier's extra face).
- `auto: true`: the to-hit comparison was bypassed (an auto-hit opener or ability); the roll is still reported.
- Nested `soak` / `bag`: a second check reported on the same event, `{ roll, atLeast, dieN }` (a failed armor soak on a landed blow; the bag-upgrade gate on a loot drop).
- Outcome rules: struck, allyStruck, memberStruck, struckByFoe, frenzy, foeShattered, foeArmorSoaked, armorSoaked, trapAvoided, leaptOver, climbedOver, afflictionCured, foeFled, foeBored, lootDropped, foeCast, spellResisted, heroResisted, allySpellMissed{resisted} report SUCCESS; strikeMissed, allyMissed, foeMissed, trapSprung, trapDisarmed, fellInGorge, fellClimbing, afflictionLingers, resistFailed, heroResistFailed, phobiaAfraid, spellBackfired, summonBackfired, joinerMurdered report FAILURE; spellThrown, allyCast, fleeRolled and parleyRolled succeed unless their failure event (spellMissed for the same target, allySpellMissed{resisted: false}, fleeFailed, parleyFailed) follows in the same action before the next event of the same type; chestLockRolled succeeds when `opened`; wanderingMonster's `hours` equals the count of `rolls` below `atLeast`; a nested soak on a landed blow is a failure; a nested bag succeeds when the dropped item is a bag.

THE CORRECTIONS (`### Corrections to the Phase 72 inventory`):
- Site 47 (items.js L250) is not "lockpick wear". `rollTreasureItem`'s `!hasPicks(c) && rng.d(12) === 1` makes the treasure a set of Lockpicks, so it is a selection. No lockpick-wear mechanic exists. CONTEXT's outcome for it (stays on the 1) holds either way.
- The appendix's "tier-drop gate" (startCombat) is a binary good-news gate for the hero, so it is classified as a check and mirrored (3b); the foe COUNT stays a selection.
- A player-facing roll-under string lives in engine/items.js (the Lockpicks item text "1–5 on d10 against any lock"). It is authored prose, so it goes to Phase 79 (ROLL-04), not here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The shared range formatter</name>
  <files>src/browser/rollRange.js, test/unit/rollRange.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (L60-L90, the existing helper style and the U+2212 convention)
    - src/browser/upgradeWhy.js (a small pure src/browser module and its test, for file conventions)
    - .planning/phases/74-roll-display-modifier-honesty/74-CONTEXT.md (the range format decision)
  </read_first>
  <behavior>
    - rangeText(16, 20) is "16–20"; (18, 20) "18–20"; (20, 20) "20"; (21, 20) "nothing"; (1, 20) and (-3, 20) "1–20"; (2, 6) "2–6"; (2, 2) "2"; (1, 2) "1–2".
    - rangeText(undefined, 20), (16, undefined) and (NaN, 20) are "?".
    - Every range separator is U+2013; no hyphen-minus appears in any output.
    - rollVsText(17, 18, 20) is "17 vs 18–20"; rollVsText(undefined, 16, 20) is "? vs 16–20".
  </behavior>
  <action>
Write test/unit/rollRange.test.js from the behaviour list first and see it fail.

Then create src/browser/rollRange.js, a pure ES module (no DOM, no imports) exporting `rangeText(atLeast, dieN)` and `rollVsText(roll, atLeast, dieN)` with the rules in `<interfaces>`. Header comment: Phase 73 (ROLL-05); the ONE place a winning range is written; the event-driven roll lines in eventNarration.js, narrationLines.js and rail.js use it from 73-04 on; Phase 74 extends it with the player-side modifier formatter; the line shape is "**17** vs 18–20 (mods)".

tools/build-www.mjs copies all of src/ into www/, so the new module ships with no build change; confirm that in the file listing.
  </action>
  <verify>
    <automated>node --test test/unit/rollRange.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function rangeText" src/browser/rollRange.js` equals 1, and `grep -c "export function rollVsText" src/browser/rollRange.js` equals 1.
    - `node --test test/unit/rollRange.test.js` exits 0 and covers every example in the behaviour list, including the U+2013 check.
  </acceptance_criteria>
  <done>One formatter owns the range text.</done>
</task>

<task type="auto">
  <name>Task 2: The mirror verdicts, event-field contract and corrections in docs/ROLL-LEDGER.md</name>
  <files>docs/ROLL-LEDGER.md</files>
  <read_first>
    - docs/ROLL-LEDGER.md (the whole file; mind `## Site inventory`, `## Not-a-check draws`, `## Handoffs → Phase 74`, `## Bot readout`)
    - test/unit/roll-ledger-sync.test.js (which section it parses, and how)
    - engine/items.js (L245-L265, to confirm correction 1 yourself)
    - this plan's `<interfaces>` (the verdict table, the event fields, the corrections)
  </read_first>
  <action>
Append a new H2 at the END of docs/ROLL-LEDGER.md: `## Phase 73 mirror verdicts (ROLL-05)`. It holds:
- An intro paragraph: the rule (CONTEXT Area 1: high is good for whoever rolls, and a 1 is always the worst face; only checks whose winning faces sit at the bottom today mirror; already-high rolls and mishap-on-1 gates stay; selection and amount draws are untouched; rolls with no obvious roller are read from the hero's side). The mechanism: `engine/dice.js#rollCheck` draws the same r and reads `roll = N + 1 - r`; content keeps its face counts; `atLeastFor(faces, N) = N + 1 - faces`; every seed resolves identically.
- `### Verdict per site`: the verdict table from `<interfaces>`, with an added last column `Status` holding "planned" on every row (73-10 fills it in).
- `### Event fields (Phase 73)`: the event-field contract and the outcome rules from `<interfaces>`, as prose bullets plus a compact table (Event | Roller | Outcome rule | Plan).
- `### Corrections to the Phase 72 inventory`: the three corrections from `<interfaces>`. Confirm correction 1 against engine/items.js before writing it.

Do not edit any existing section; `## Modifier ledger` and its ids stay exactly as they are. Do not write bracketed `[site:source]` ids in the new section. The sync test must see no change.
  </action>
  <verify>
    <automated>node --test test/unit/roll-ledger-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 73 mirror verdicts (ROLL-05)" docs/ROLL-LEDGER.md` equals 1, and `grep -c "^### Event fields (Phase 73)" docs/ROLL-LEDGER.md` equals 1.
    - The verdict table has a row for every site number 1-48 plus 3b and A1-A3: `awk '/^## Phase 73 mirror verdicts/,0' docs/ROLL-LEDGER.md | grep -cE '^\| (3b|A[123]|[0-9]+) \|'` is at least 52.
    - `node --test test/unit/roll-ledger-sync.test.js` exits 0, and `git diff --quiet d274925 -- test/unit/roll-ledger-sync.test.js` exits 0.
    - `git diff d274925 -- docs/ROLL-LEDGER.md` shows only added lines after the old last line (no existing line changed).
  </acceptance_criteria>
  <done>Every site has a recorded verdict and every conversion plan has its checklist.</done>
</task>

</tasks>

<verification>
- The formatter passes its contract, and the ledger holds the verdicts, the event contract and the corrections.
- The ledger-sync guard is untouched and green; no engine file changes.
</verification>

<success_criteria>
CONTEXT Area 1's classification is recorded for all 48 sites plus the appendix, and the one range format the event lines will use exists and is pinned.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-03-SUMMARY.md` when done. Include the rangeText examples, the verdict counts by class (mirror / already high / mishap on 1 / not a check), and confirmation of correction 1 with the items.js line.
</output>
