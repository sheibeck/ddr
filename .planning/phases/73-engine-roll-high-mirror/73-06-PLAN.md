---
phase: 73-engine-roll-high-mirror
plan: 06
type: execute
wave: 4
depends_on:
  - 73-05
files_modified:
  - engine/derived.js
  - engine/magic.js
  - engine/foeAbilities.js
  - engine/combat.js
  - src/browser/eventNarration.js
  - test/unit/roll-high-guard.test.js
  - test/parity/roll-high-invariant.test.js
  - test/unit/resist-roll.test.js
  - test/unit/magic.test.js
  - test/unit/foe-abilities.test.js
  - test/unit/linesForAction.test.js
  - test/unit/spell-mechanics.test.js
  - test/unit/casters-can-act.test.js
  - test/unit/identity-contract.test.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "engine/derived.js#resistRoll reads its d20 roll-high: rollCheck(rng, 20, atLeastFor(intel - 1, 20)), so a resistor with intel I resists on 22 - I to 20, byte-identical to the old roll < intel; the intel >= 12 gate still draws nothing below it, and the return keeps { rolled, resisted, roll } and adds atLeast and dieN"
    - "spellResisted, resistFailed, heroResisted, heroResistFailed and allySpellMissed {resisted: true} carry roll, atLeast and dieN; the Oracle prints '<roll> vs lo–hi (intel N)'"
    - "The Apprentice and doubled-summon backfires stay on the natural 1 (mishap-on-1): the draw happens under exactly the old condition, and spellBackfired and summonBackfired carry roll 1, atLeast 2 and dieN 8"
    - "The vapor outcome roll stays a table pick (selection) and the vapor kill-save stays the foe's save that fails on a 1 (mishap-on-1); neither changes"
    - "Every raw draw in engine/magic.js is tagged; engine/magic.js and engine/derived.js are enforced by the guard; derived.js's JSDoc for toHit, foeToHitVs, foeToHitBreakdown, memberToHit, afraidNeed, classNeed, weaponNeedMod, fleeBreakdown and resistRoll describe winning faces and the roll-high reading, while every derived function still returns the same numbers"
    - "Outcomes, draw counts and every baseline are unchanged"
  artifacts:
    - path: engine/derived.js
      provides: "resistRoll on rollCheck; faces-reading JSDoc"
      contains: "rollCheck(rng, 20, atLeastFor(intel - 1, 20))"
    - path: engine/magic.js
      provides: "resist events with the triple; tagged mishaps, selections and amounts"
      contains: "roll:mishap-on-1"
    - path: engine/foeAbilities.js
      provides: "heroResisted and heroResistFailed with the triple"
  key_links:
    - from: engine/magic.js, engine/foeAbilities.js, engine/combat.js#allyCast
      to: engine/derived.js#resistRoll
      via: "res.roll, res.atLeast, res.dieN"
      pattern: "resistRoll\\("
  prohibitions:
    - statement: "MUST NOT regenerate, edit or re-declare a parity fixture, divergence record, comparable carve-out, state pin, the pre-switch save or the Phase 72 direction tests, nor loosen an expected test value, to absorb a mirror mistake"
      category: integrity
      status: resolved
      verification: judgment
---

<objective>
Convert resistance (the p.25 intel save, for both the hero and foes) to roll-high, carry the triple on every resist event and backfire, tag every remaining draw in engine/magic.js, and bring magic.js and derived.js under the guard.

Purpose: CONTEXT Area 1 (verdict rows 35, 36, 38, 39 and 46: resistance mirrors; "Mishap gates that fire on a natural 1 stay on the 1: Apprentice/doubled backfire... the foe's save vs the vapor kill"). CONTEXT Area 2: "Only names, JSDoc and comments that say 'roll under'/'need N or less' are rewritten to the faces/roll-high reading", and derived functions keep returning faces for Phase 74.

Output: resistRoll on rollCheck; resist and backfire event triples; the resist Oracle lines; magic.js fully tagged; derived.js JSDoc in the faces reading; the guard enforcing magic.js and derived.js.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-06 creates or changes.)

- The helper and guard (73-01); baselines and the invariant (73-02); rollRange.js and the ledger verdicts (73-03); the hero strike, crits, frenzy and foe soak (73-04); member/ally/thrown strikes and the isBestFace flip (73-05)
- **This plan:** engine/derived.js `resistRoll` on rollCheck, returning `{ rolled, resisted, roll, atLeast, dieN }`; the faces-reading JSDoc on the derived to-hit, soak and flee functions
- **This plan:** spellResisted, resistFailed, heroResisted, heroResistFailed, allySpellMissed{resisted}, spellBackfired and summonBackfired triples; their Oracle lines where a roll is printed
- **This plan:** every raw draw in engine/magic.js tagged; magic.js and derived.js enforced by the guard
- Foe swings and the hero soak (73-07); flee, parley, gates and drops (73-08); traps, locks, climbs, cures and wake (73-09); the proofs and docs (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/73-engine-roll-high-mirror/73-05-SUMMARY.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/derived.js#resistRoll (L1433): `if ((intel ?? 0) < 12) return { rolled: false, resisted: false, roll: undefined }; const roll = rng.d(20); return { rolled: true, resisted: roll < intel, roll };`. roll < intel means r <= intel - 1, which is (intel - 1) winning faces, so atLeast = 22 - intel. test/unit/rollDirection-checks.test.js calls `resistRoll(rng, 18).resisted` and `resistRoll(rng, 11)` (it expects NO draw); both must keep working unchanged.
Callers: engine/magic.js castSpell (~L150-L163) pushes `spellResisted { target, spell, roll: res.roll, intel }` or `resistFailed { target, roll: res.roll }`; engine/foeAbilities.js heroResist (~L104-L112) pushes `heroResisted`/`heroResistFailed { name, ability, roll, intel }`; engine/combat.js allyCast's non-thrown branch (~L2034) pushes `allySpellMissed { …, resisted: true, roll: res.roll }`.
engine/magic.js draws (live grep; tag each): L128 `c.sub === "Apprentice" && rng.d(8) === 1` (mishap-on-1; spellBackfired); L131 backfire self-damage (rollDice, no tag); L176 `doubled && rng.d(8) === 1` (mishap-on-1; summonBackfired); L177 `lvl * lvl + rng.d(6)` (amount); L192 rounds (amount, two draws on one line); L207 damage multiplier draw (amount); L210, L227, L252, L263, L279, L317, L321, L363, L402, L431, L445, L475, L571 (amounts); L310 `c.level >= 5 ? 4 : rng.d(6)` vapor outcome (selection); L313 `r === 4 && rng.d(10) !== 1` vapor kill-save (mishap-on-1); L423 insane outcome pick (selection). The thrown branch (73-05) already uses rollCheck.
Mishap events keep the natural-1 reading: the raw draw is the hero's roll (1 is the worst face), and the hero avoids the mishap at 2 or more, so the event carries `{ roll: <the raw draw>, atLeast: 2, dieN: 8 }`. Keep the `&&` short-circuit: the d8 is drawn only for an Apprentice, or only when `doubled`.
src/browser/eventNarration.js: heroResisted (~L613) and heroResistFailed (~L615) print `${roll} vs intel ${intel}`; spellResisted (~L703) prints `${roll}</span> vs intel ${intel}`; resistFailed (~L704) prints the roll alone; allySpellMissed (~L538) prints `${roll} against its wits`. Target shape: `<span class="roll">15 vs 8–20 (intel 14).</span>` (keep each line's existing sentence and voice; replace only the roll-vs part).
engine/derived.js JSDoc to rewrite (numbers unchanged): toHit, classNeed, weaponNeedMod, afraidNeed, memberToHit, foeToHitVs, foeToHitBreakdown, fleeBreakdown, resistRoll, and any other comment in derived.js stating a roll-under rule ("roll <= need", "need N or less", "low = hit"). The functions keep returning counts of winning faces (fleeBreakdown keeps its d20 need and bonus); Phase 74 reads them.

Proof gates and test-update rules: exactly as in 73-04-PLAN.md `<interfaces>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: resistRoll on rollCheck, the resist events, and the backfire mishaps</name>
  <files>engine/derived.js, engine/magic.js, engine/foeAbilities.js, engine/combat.js, test/unit/resist-roll.test.js, test/unit/magic.test.js, test/unit/foe-abilities.test.js, test/unit/spell-mechanics.test.js, test/unit/casters-can-act.test.js, test/unit/identity-contract.test.js</files>
  <read_first>
    - engine/derived.js (imports; resistRoll L1410-L1440)
    - engine/magic.js (L120-L200 backfires, L145-L165 resistance, L300-L320 vapor)
    - engine/foeAbilities.js (L95-L115 heroResist)
    - engine/combat.js (allyCast's non-thrown branch)
    - test/unit/resist-roll.test.js, test/unit/rollDirection-checks.test.js (L335-L350, which must run unchanged)
  </read_first>
  <action>
Per verdict row 46 and rows 35-36:
1. engine/derived.js: import `rollCheck, atLeastFor` from ./dice.js. resistRoll keeps the gate and its early return, then draws `rollCheck(rng, 20, atLeastFor(intel - 1, 20))` and returns `{ rolled: true, resisted: check.ok, roll: check.roll, atLeast: check.atLeast, dieN: 20 }`. JSDoc: p.25 canon, a resistor succeeds on 22 − intel to 20, byte-identical to the old strict "under intel" reading.
2. The resist events carry the triple beside their existing fields: spellResisted and resistFailed (magic.js), heroResisted and heroResistFailed (foeAbilities.js), and allySpellMissed with `resisted: true` (combat.js allyCast). Use `roll: res.roll, atLeast: res.atLeast, dieN: res.dieN`.
3. engine/magic.js backfires: restructure each gate so the d8 is drawn under exactly the old condition, on its own line tagged `// roll:mishap-on-1`, and push spellBackfired / summonBackfired with `roll`, `atLeast: 2`, `dieN: 8` added.
4. Update the resist and backfire assertions per the test-update rules (resist-roll.test.js's scripted draws stay; its expected `roll` values mirror to 21 − old).
  </action>
  <verify>
    <automated>node --test test/unit/resist-roll.test.js test/unit/rollDirection-checks.test.js test/unit/magic.test.js test/unit/foe-abilities.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollCheck(rng, 20, atLeastFor(intel - 1, 20))" engine/derived.js` equals 1.
    - `grep -c "atLeast: 2" engine/magic.js` equals 2 (spellBackfired and summonBackfired), and both pushes also carry `dieN: 8`.
    - `[resist:intel]` and `[resist:intel-gate]` in test/unit/rollDirection-checks.test.js pass unchanged; the state pins pass.
  </acceptance_criteria>
  <done>Resistance reads roll-high, and every resist and backfire event reports its roll.</done>
</task>

<task type="auto">
  <name>Task 2: Tag magic.js, rewrite derived.js's JSDoc, and enforce both files</name>
  <files>engine/magic.js, engine/derived.js, test/unit/roll-high-guard.test.js</files>
  <read_first>
    - engine/magic.js (every raw draw; `grep -n "\.d(" engine/magic.js`)
    - engine/derived.js (the JSDoc of every function named in `<interfaces>`)
    - test/unit/roll-high-guard.test.js
  </read_first>
  <action>
1. engine/magic.js: tag every remaining raw draw per `<interfaces>` (amount, selection, mishap-on-1). Vapor keeps its outcome roll as a selection and its kill-save as a mishap-on-1: code unchanged, tags only.
2. engine/derived.js: rewrite the JSDoc and comments that describe the old bottom-of-the-die rule (the phrasings listed in `<interfaces>`) to the faces reading (for example, toHit returns the number of winning faces on the strike die; a bigger count is better; the engine reads a check roll-high with atLeastFor). Change no code and no returned value.
3. test/unit/roll-high-guard.test.js: add engine/magic.js and engine/derived.js to ENFORCED with live DRAW_INVENTORY counts (magic.js: rollCheck from the thrown branch plus its tagged draws; derived.js: `{ rollCheck: 1 }`). If a shape fires in either file, the site is unconverted or untagged: fix the engine, not the guard.
4. `node tools/comment-only-diff.mjs <plan-base> engine/derived.js` is expected to FAIL only because of resistRoll's code change from Task 1. Confirm by inspection that every other derived.js hunk is comment-only.
  </action>
  <verify>
    <automated>node --test test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"engine/magic.js"' test/unit/roll-high-guard.test.js` is at least 1, and the same for `"engine/derived.js"`; the guard passes.
    - With engine/magic.js in ENFORCED, the guard's tag rule passes (every raw draw line in magic.js carries a valid tag) and its inventory for magic.js matches a live count.
    - `grep -ciE "roll[- ]under|or less|low = hit" engine/derived.js` equals 0.
  </acceptance_criteria>
  <done>magic.js and derived.js are guarded, and derived.js documents faces.</done>
</task>

<task type="auto">
  <name>Task 3: The resist Oracle lines, the invariant rows, and the proof gates</name>
  <files>src/browser/eventNarration.js, test/parity/roll-high-invariant.test.js, test/unit/linesForAction.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (heroResisted, heroResistFailed, spellResisted, resistFailed, allySpellMissed)
    - test/parity/roll-high-invariant.test.js (OUTCOME)
    - test/unit/linesForAction.test.js (~L395-L415, the resist groups)
  </read_first>
  <action>
1. src/browser/eventNarration.js: in each line that prints a resist roll, replace the part that sets the roll against the intel stat (or prints the bare roll, or the "against its wits" roll) with `<span class="roll">${roll} vs ${rangeText(e.atLeast, e.dieN)} (intel ${intel}).</span>` where the event carries intel, and without the parenthetical where it does not. Keep each line's sentence and voice. Backfire lines print no roll today; leave them.
2. test/parity/roll-high-invariant.test.js OUTCOME: spellResisted and heroResisted (success); resistFailed and heroResistFailed (failure); allySpellMissed (success when `resisted` is true, the resistor's roll); spellBackfired and summonBackfired (failure).
3. Update the resist-line expectations per the test-update rules, then run the proof gates.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vs intel" src/browser/eventNarration.js` equals 0.
    - `grep -cE "^\s+(spellResisted|resistFailed|heroResisted|heroResistFailed|allySpellMissed|spellBackfired|summonBackfired)\s*[:(]" test/parity/roll-high-invariant.test.js` is at least 7.
    - All proof gates in 73-04's `<interfaces>` hold.
  </acceptance_criteria>
  <done>Resistance and magic mishaps are roll-high or correctly oriented, reported and guarded.</done>
</task>

</tasks>

<verification>
- The proof gates hold; the resist direction rows are unchanged and green; magic.js and derived.js are enforced.
</verification>

<success_criteria>
ROADMAP criteria 1, 2, 4 and 5 hold for resistance and the magic mishaps: roll-high, byte-identical, faces kept, and reported on their events.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-06-SUMMARY.md` when done. Include the resistRoll return shape, the event shapes, the magic.js inventory, the JSDoc functions rewritten, the test counts, and a "Human verification (deferred to end of run)" note: a foe's spell against a hero with intel 14 or more reads "<roll> vs 8–20 (intel 14)" in the Oracle.
</output>
