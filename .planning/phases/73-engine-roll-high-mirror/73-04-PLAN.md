---
phase: 73-engine-roll-high-mirror
plan: 04
type: execute
wave: 2
depends_on:
  - 73-01
  - 73-02
  - 73-03
files_modified:
  - engine/combat.js
  - engine/foeDamage.js
  - src/browser/eventNarration.js
  - src/browser/rail.js
  - test/unit/roll-high-guard.test.js
  - test/parity/roll-high-invariant.test.js
  - test/unit/combat.test.js
  - test/unit/afraid.test.js
  - test/unit/ability-strike.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/linesForAction.test.js
  - test/unit/fightLog.test.js
  - test/unit/rail.test.js
  - test/unit/missLines.test.js
  - test/unit/combat-beat.test.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "The hero's strike die, the Philly second die, the frenzy trigger and the foe's natural-armor soak are read roll-high through rollCheck: the same draw r in the same position reads as N + 1 - r and succeeds at or above atLeastFor(faces, N)"
    - "Philly's 'two dice, keep the better' keeps Math.max of the two mirrored faces, byte-identical to the old keep-the-lower"
    - "The weapon crit, Stealth crit and Ninja crit sit on the top faces: roll >= atLeastFor(weaponCrit(c), dieN) and roll >= atLeastFor(2, dieN)"
    - "struck and strikeMissed carry roll, atLeast and dieN with no need key; needMods is renamed mods with unchanged values; struck adds critAtLeast for a die-driven crit, auto: true for a bypassed comparison, and a nested soak triple when the foe's armor die was drawn and failed; frenzy and foeArmorSoaked carry their own triple"
    - "The Oracle's strike lines read '<roll> vs <lo–hi> (mods). You miss it.' via src/browser/rollRange.js, never a mirrored roll beside a roll-under number, and rail.js's generic dice line reads 'roll N vs lo–hi'"
    - "Draw count and order are unchanged at every converted site; parity, the Phase 72 direction tests, the state pins, the pre-switch save and the invariant all pass without being edited, and foeDamage.js is enforced by the guard"
  artifacts:
    - path: engine/combat.js
      provides: "playerStrike and the frenzy gate on rollCheck; the one transitional shatter call"
      contains: "rollCheck("
    - path: engine/foeDamage.js
      provides: "damageFoe's soak on rollCheck, returning the failed soak triple"
      contains: "rollCheck("
    - path: src/browser/eventNarration.js
      provides: "strikeMissed and struck in the final line shape; modsText and modsClause"
      contains: "rollRange.js"
  key_links:
    - from: engine/combat.js#playerStrike
      to: engine/dice.js#rollCheck
      via: "import { rollCheck, atLeastFor, rollFields }"
      pattern: "rollCheck\\(rng, dieN"
    - from: engine/combat.js#playerStrike (struck)
      to: engine/foeDamage.js#damageFoe
      via: "landed.soak spread onto struck"
      pattern: "landed.soak"
    - from: src/browser/eventNarration.js
      to: src/browser/rollRange.js
      via: "rangeText(e.atLeast, e.dieN)"
      pattern: "rangeText"
  prohibitions:
    - statement: "MUST NOT regenerate, edit or re-declare a parity fixture, divergence record, comparable carve-out, state pin, the pre-switch save or the Phase 72 direction tests, nor loosen an expected test value, to absorb a mirror mistake; a moved result means the site is fixed, never the evidence"
      category: integrity
      status: resolved
      verification: judgment
---

<objective>
Convert the hero's own offense to roll-high: the strike die (with Philly's second die), the weapon, Stealth and Ninja crits, the Fridgian frenzy trigger, and the foe's natural-armor soak of the hero's blow. Carry the new triple on struck, strikeMissed, frenzy and foeArmorSoaked, and update the Oracle lines that print them.

Purpose: CONTEXT Area 1 (sites 7-11 and 34 of the verdict table) and Area 3 ("Where Phase 73 stops on displays"). This is the first vertical slice: engine sites, their event fields, the lines that print them, and the tests that assert them, all in one plan so the suite stays green.

Output: playerStrike and damageFoe on rollCheck; the new event fields; strikeMissed/struck lines in the final shape; rail's generic dice line on atLeast/dieN; the guard enforcing foeDamage.js; invariant OUTCOME rows for the new events.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-04 creates or changes.)

- engine/dice.js `rollCheck`/`atLeastFor`/`rollFields`, the guard and helper tests, tags in the conversion-free files (73-01)
- The baselines, readout-compare tool, pins, pre-switch save and the invariant (73-02); src/browser/rollRange.js and the ledger verdicts (73-03)
- **This plan:** playerStrike's strike die, Philly, crits and frenzy gate on rollCheck; foeDamage.js's soak on rollCheck (the file is enforced by the guard)
- **This plan:** struck/strikeMissed `{ roll, atLeast, dieN, mods?, critAtLeast?, auto?, soak? }`, frenzy and foeArmorSoaked triples; `modsText`/`modsClause` in eventNarration.js; rail.js `rollLineFor` on atLeast/dieN
- The member/ally/thrown strikes and the isBestFace flip (73-05); resistance and magic mishaps (73-06); foe swings and the hero soak (73-07); flee, parley, gates and drops (73-08); traps, locks, climbs, cures and wake (73-09); the proofs and docs (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/dice.js (73-01): `rollCheck(rng, dieN, atLeast)` -> `{ roll, atLeast, dieN, ok }` (one draw, roll = dieN + 1 - r); `atLeastFor(faces, dieN)` = dieN + 1 - faces; `rollFields(chk)` -> `{ roll, atLeast, dieN }`; `isBestFace(roll, dieN)` is STILL `roll === 1` (73-05 flips it).
src/browser/rollRange.js (73-03): `rangeText(atLeast, dieN)` ("16–20", "20", "1–20", "nothing", "?"), `rollVsText(roll, atLeast, dieN)`.

engine/combat.js at d274925 (line numbers drift; key by function):
- playerStrike (~L546-L845). Frenzy gate L601-L606: `if (R.frenzy && rng.d(8) <= 5) { attacks = …; frenzyFired = true; events.push({ type: "frenzy" }) }`. Per attack (L608-L838): `dieN = strikeDie(c)`; `let roll = rng.d(dieN)` (L610); Philly `if (t.sp && t.sp.slow) roll = Math.min(roll, rng.d(dieN))` (L616); then a PURE need chain L628-L660 (frenzy-narrowed toHit, dozing/stupid floor 5, sp.toHit cap, sp.fast, magicOnly/daggerOnly zero, AS.needShift with abilityMods, afraidNeed with afraidMods, `needMods = [...abilityMods, ...afraidMods]`); `subAuto`/`auto` L666-L668 (subAuto writes C.opened); `hit = auto || (need > 0 && roll <= need)` L669; strikeMissed push L671-L681 `{ type, target, roll, need, dieN, untouchable: need === 0, needMods?, via? }`; shatter call L690 `shatterIfBest(state, t, roll, dieN, "you", rng, events)`; `crit = roll <= weaponCrit(c) && !noCrit` L714 with `critBy = "roll"`; Stealth `roll <= 2` L755 (critBy "stealth"); Ninja `roll <= 2` L765 (critBy "ninja"); `landed = damageFoe(...)` L823; struck push L824-L836 `{ type, target, roll, need, dmg, critical, critBy?, needMods?, afraid?, via? }`.
- The need chain draws nothing, so it may be computed BEFORE the strike draw; the draw order (strike die, then Philly's second die) must not change.
engine/foeDamage.js damageFoe (L72-L109): the soak gate (L96) `if (physical && !source.crit && foe.sp && foe.sp.ar > 0) { const roll = rng.d(20); if (roll <= foe.sp.ar) { events.push({ type: "foeArmorSoaked", name, amount: dmg }); return { applied: 0, soaked: true, mult } } }`, then `foe.wp -= dmg; return { applied: dmg, soaked: false, mult }`. It is the module's only draw.

src/browser/eventNarration.js: `needModsText(mods)` L82 ("Guard −1, insulted +1"), `needModsClause(mods, need)` L87 (" (needs 4: Guard −1)"), strikeMissed L384-L387, struck L397-L411, fleeRolled L450-L456 (uses needModsText). src/browser/rail.js `rollLineFor(event, narrate)` L452-L463: (a) the Oracle sentence when it carries a roll span; (b) else "roll N[ · M to clear][ · −K hp]" from a numeric event.roll and event.need.

Target line shapes (74-CONTEXT, user-accepted): strikeMissed `<span class="roll">7</span> vs 16–20 (afraid −3). <span class="miss">You miss Dante.</span>` plus the quip; struck `<span class="roll">17</span> vs 16–20. <span class="hit">…Critical!</span> You hit Dante for <span class="roll">4</span> hp.` The old "(needs N: …)" repeat is dropped; the mods list follows the range. Mod signs stay as the engine gives them (signed for the roller); Phase 74 re-signs them from the player's side. The untouchable strikeMissed line is unchanged. rail's generic line becomes "roll N vs lo–hi[ · −K hp]".

Proof gates (end of plan; all must hold):
1. `node --test "test/parity/**/*.test.js"` exits 0 (it includes the runtime invariant).
2. `git diff --quiet d274925 -- test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-ledger-sync.test.js test/unit/harness/rollOdds.js test/parity/fixtures test/parity/prototype-master.js.txt test/parity/harness/comparables.js` exits 0.
3. `git diff --stat <plan-base>..HEAD -- test/unit/roll-high-state-pins.test.js test/unit/harness/rollHighBaseline.js test/unit/roll-high-save-compat.test.js test/unit/fixtures/roll-high/` is empty, and `node --test test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-high-guard.test.js` exits 0. (`<plan-base>` is `git rev-parse HEAD` recorded before the first edit.)
4. `node tools/initiative-fixture-scan.mjs | diff --strip-trailing-cr - tools/initiative-fixture-scan-output.txt` and `node tools/worn-fixture-scan.mjs | diff --strip-trailing-cr - tools/worn-fixture-scan-output.txt` are both empty.
5. `npm test` shows 0 failures other than the 7 known worktree-only CRLF doc-ledger failures (docs/CLASS-PASS.md, docs/FLEE.md), and its `# tests` count is at least the plan-base count.

Test-update rules (CONTEXT Area 3: "Expected values are mirrored (N+1−old), never loosened"):
- A scripted rng face in a test never changes; the draws are identical. Only expectations about event fields and printed text change.
- An asserted event roll v on a dN becomes N+1−v; an asserted `need: k` becomes `atLeast: N+1−k` plus `dieN: N`; `needMods` becomes `mods` with the same values.
- A hand-built event in a narration test is rewritten in the new shape with an explicit dieN (20 unless the scenario implies another die), mirrored values, and the expected string in the new line shape.
- A test title stating the roll-under reading is reworded to the roll-high reading.
- Never delete an assertion, loosen an exact value, or change a scripted draw to make a test pass. A behaviour assertion that flips (a hit becomes a miss) means the engine site is wrong: fix the site.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: playerStrike, the frenzy gate and the crits on rollCheck</name>
  <files>engine/combat.js, test/unit/combat.test.js, test/unit/afraid.test.js, test/unit/ability-strike.test.js</files>
  <read_first>
    - engine/combat.js (imports L55-L75; playerStrike L546-L845; shatterIfBest L943-L950)
    - engine/dice.js
    - docs/ROLL-LEDGER.md (`## Phase 73 mirror verdicts`, rows 7-11, and `### Event fields (Phase 73)`)
    - test/unit/combat.test.js (the playerStrike tests, including the Philly `slow` tests near L1369-L1395 and any strikeMissed need assertions near L1631)
    - test/unit/afraid.test.js (L130-L150 strike assertions)
  </read_first>
  <action>
Per CONTEXT Area 1 and verdict rows 7-11:
1. Import `rollCheck, atLeastFor, rollFields` from ./dice.js.
2. Frenzy gate: `if (R.frenzy)` draws `rollCheck(rng, 8, atLeastFor(5, 8))` and, when `.ok`, does what the old branch did, pushing `{ type: "frenzy", ...rollFields(check) }`. The draw happens only for a frenzy race, exactly as before.
3. Per attack: keep `dieN = strikeDie(c)`. Move the pure need chain above the draw and rename its locals: `need` becomes `faces`, `needBeforeAfraid` becomes `facesBeforeAfraid`, and the combined list becomes `mods` (the entries `{ name: "overhead" | "afraid", delta }` keep their values). Then draw `let check = rollCheck(rng, dieN, atLeastFor(faces, dieN))`. Philly: when `t.sp && t.sp.slow`, draw a second `rollCheck(rng, dieN, check.atLeast)` and keep `Math.max(check.roll, second.roll)` as the roll (recompute ok from it). Keep `subAuto`/`auto` exactly where they were relative to the rest.
4. `hit = auto || roll >= atLeast` (faces 0 gives atLeast = dieN + 1, which never hits). strikeMissed pushes `{ type, target, ...rollFields, untouchable: faces === 0, mods? (when non-empty), via? }`; `need` is gone.
5. Crits on the top faces: `crit = roll >= atLeastFor(weaponCrit(c), dieN) && !noCrit`; Stealth and Ninja use `roll >= atLeastFor(2, dieN)`. Track `critAtLeast` for the crit that wins: weaponCrit's for critBy "roll", `atLeastFor(2, dieN)` for "stealth" and "ninja", none for backstab, cutthroat or a forced crit.
6. struck pushes `{ type, target, ...rollFields, dmg, critical, critBy?, critAtLeast? (only when critBy is roll, stealth or ninja), mods?, afraid?, auto: true (only when auto), soak? (Task 2 wires this), via? }`; `need` is gone.
7. The shatter call cannot switch yet (isBestFace still reads roll === 1, and five other callers are still roll-under). Pass the old face: `shatterIfBest(state, t, dieN + 1 - roll, dieN, "you", rng, events)`, with a trailing comment containing the exact phrase `Phase 73 transitional: 73-05 flips isBestFace and passes roll`. This is the only such shim; 73-05 removes it.
8. Update the comments in the converted region: they describe faces and the roll-high reading, not "roll under". Keep every DELIBERATE RULES CHANGE rationale.
9. Tests: apply the test-update rules to the playerStrike/frenzy assertions in combat.test.js, afraid.test.js and ability-strike.test.js (for example the Philly test "a 7 then a 2 hits with struck.roll === 2" on a d20 becomes struck.roll === 19, retitled "the better mirrored face is kept"). Run the direction tests unchanged.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/afraid.test.js test/unit/ability-strike.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/skeleton-shatter.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollCheck(rng, dieN" engine/combat.js` is at least 2 (the strike die and Philly's second die), and `grep -c "rollCheck(rng, 8, atLeastFor(5, 8))" engine/combat.js` equals 1.
    - `grep -c "Phase 73 transitional" engine/combat.js` equals 1.
    - Inside playerStrike (`awk '/^export function playerStrike/,/^}/' engine/combat.js`), no line matches `roll <=` or `Math.min(roll`.
    - test/unit/rollDirection.test.js, rollDirection-checks.test.js and skeleton-shatter.test.js pass unchanged; the state pins pass.
  </acceptance_criteria>
  <done>The hero's strike resolves roll-high with identical outcomes.</done>
</task>

<task type="auto">
  <name>Task 2: The foe's natural-armor soak on rollCheck, and the guard for foeDamage.js</name>
  <files>engine/foeDamage.js, engine/combat.js, test/unit/roll-high-guard.test.js, test/unit/feedback-payload.test.js</files>
  <read_first>
    - engine/foeDamage.js (the whole file)
    - engine/combat.js (playerStrike's struck push; grep `damageFoe(` for the other callers, which stay untouched here)
    - test/unit/roll-high-guard.test.js (ENFORCED, DRAW_INVENTORY)
    - tests that assert foeArmorSoaked's shape: `grep -rln "foeArmorSoaked" test/`
  </read_first>
  <action>
Per verdict row 34:
1. engine/foeDamage.js: import `rollCheck, atLeastFor, rollFields` from ./dice.js. Replace the soak draw with `rollCheck(rng, 20, atLeastFor(foe.sp.ar, 20))` under the unchanged gate. On `.ok`, push `{ type: "foeArmorSoaked", name, amount: dmg, ...rollFields(check) }` and return as before. When the soak die was drawn and failed, return `{ applied, soaked: false, mult, soak: rollFields(check) }`; otherwise return the old shape with no soak key. Update the DETERMINISM GATE comment to describe the roll-high read (same single draw, same gate).
2. engine/combat.js playerStrike: spread `...(landed.soak ? { soak: landed.soak } : {})` onto struck. The other damageFoe callers ignore the new key until their own plans.
3. test/unit/roll-high-guard.test.js: add engine/foeDamage.js to ENFORCED with DRAW_INVENTORY `{ rollCheck: 1 }` (all other counts 0). The guard must pass over it.
4. Update the foeArmorSoaked shape assertions per the test-update rules (the new triple is additive).
  </action>
  <verify>
    <automated>node --test test/unit/roll-high-guard.test.js test/unit/rollDirection-checks.test.js test/unit/feedback-payload.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollCheck(rng, 20, atLeastFor(foe.sp.ar, 20))" engine/foeDamage.js` equals 1.
    - With engine/foeDamage.js in ENFORCED, the guard's inventory for it is exactly one rollCheck call and zero raw draws.
    - `grep -c '"engine/foeDamage.js"' test/unit/roll-high-guard.test.js` is at least 1, and the guard passes.
    - `[foe-soak:natural-ar]` and `[foe-soak:crit-bypass]` in rollDirection-checks.test.js pass unchanged.
  </acceptance_criteria>
  <done>The foe's armor soak is roll-high, reports its roll both ways, and foeDamage.js is guarded.</done>
</task>

<task type="auto">
  <name>Task 3: The strike lines, rail's dice line, the invariant rows, and the remaining tests</name>
  <files>src/browser/eventNarration.js, src/browser/rail.js, test/parity/roll-high-invariant.test.js, test/unit/feedback-payload.test.js, test/unit/linesForAction.test.js, test/unit/fightLog.test.js, test/unit/rail.test.js, test/unit/missLines.test.js, test/unit/combat-beat.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (L60-L90 helpers, L380-L412 strikeMissed/struck, L444-L456 fleeRolled)
    - src/browser/rail.js (L440-L465 rollLineFor)
    - src/browser/rollRange.js
    - test/parity/roll-high-invariant.test.js (OUTCOME, NESTED_CHECKS)
    - test/unit/fightLog.test.js (L80-L105), test/unit/rail.test.js (L205-L240), test/unit/linesForAction.test.js (L320-L340), test/unit/feedback-payload.test.js (L650-L715)
  </read_first>
  <action>
Per CONTEXT Area 3 ("Phase 73 updates ONLY the consumers that print a roll or need from an EVENT... Use Phase 74's final line shape"):
1. src/browser/eventNarration.js: import `rangeText` from ./rollRange.js. Rename needModsText to `modsText` (update its fleeRolled caller). Add `modsClause(mods)`, which returns " (Guard −1, insulted +1)" or "". Rewrite strikeMissed (non-untouchable branch) and struck to `<span class="roll">${roll}</span> vs ${rangeText(e.atLeast, e.dieN)}${modsClause(e.mods)}. …`, keeping every other clause (quip, crit text, dmg, afraid) as it is. Keep the old need-clause helper for now: the foe-side and thrown lines still call it until 73-05 and 73-07 convert them. Voice rules hold: family-friendly, U+2212 minus.
2. src/browser/rail.js rollLineFor (b): when event.roll is a number, build `roll ${event.roll}`, add ` vs ${rangeText(event.atLeast, event.dieN)}` when both are numbers, and keep the hurt suffix. The need branch goes (no converted event carries need, and every unconverted roll event has an Oracle roll span, so branch (a) handles it). Update the JSDoc.
3. test/parity/roll-high-invariant.test.js OUTCOME: add `struck` (success; the invariant already exempts `auto`), `strikeMissed` (failure), `frenzy` (success), `foeArmorSoaked` (success); NESTED_CHECKS `soak` on struck is a failure (a landed blow means the soak failed).
4. Update the narration, fight-log, rail, linesForAction, missLines and combat-beat expectations per the test-update rules (for example fightLog.test.js's "7 vs 12. You hit Giant Rat for 5 hp." is rebuilt from a new-shape event with an explicit dieN).
5. Run the proof gates.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollRange.js" src/browser/eventNarration.js` equals 1, and `grep -c "rollRange.js" src/browser/rail.js` equals 1.
    - `grep -cE "^\s+(struck|strikeMissed|frenzy|foeArmorSoaked)\s*[:(]" test/parity/roll-high-invariant.test.js` is at least 4 (their OUTCOME rows).
    - All five proof gates in `<interfaces>` hold.
  </acceptance_criteria>
  <done>The hero's offense is roll-high end to end: engine, events, the Oracle, the fight log and the rail.</done>
</task>

</tasks>

<verification>
- Proof gates 1-5 hold.
- The direction tests, pins, pre-switch save and parity are untouched and green; the invariant checks the new events on every fixture replay and the bot sweep.
</verification>

<success_criteria>
ROADMAP criteria 1, 2 and 5 hold for the hero's strike, crits, frenzy and the foe soak: roll-high in the engine, byte-identical outcomes, and the triple on every event that reports them.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-04-SUMMARY.md` when done. Include the new event shapes, the transitional shatter call, every test file touched with a one-line reason, the plan-base and final test counts, and a "Human verification (deferred to end of run)" note: on the Pixel 7, swing at a foe and confirm the Oracle reads "<roll> vs 16–20" for a level-1 Fighter (18–20 for a caster, 17–20 for a thief).
</output>
