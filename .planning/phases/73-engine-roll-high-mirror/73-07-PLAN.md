---
phase: 73-engine-roll-high-mirror
plan: 07
type: execute
wave: 5
depends_on:
  - 73-06
files_modified:
  - engine/combat.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/parity/roll-high-invariant.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/combat.test.js
  - test/unit/parley.test.js
  - test/unit/armor-durability.test.js
  - test/unit/identity-race.test.js
  - test/unit/identity-contract.test.js
  - test/unit/party-combat.test.js
  - test/unit/party-abilities.test.js
  - test/unit/linesForAction.test.js
  - test/unit/narrationLinesTable.test.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "A foe's swing at the hero, at a party member, and its parting pursuit strike are read roll-high through rollCheck; the need chain (blind override, Weaken cap, member Sidestep/Smoke, the insult applied last) folds into the face count in its existing order, so the foe finds a smoked-and-insulted target on 19–20 of a d20"
    - "A foe's crit sits on the top face (roll === dieN), widened to the top two faces against a Soldier: roll >= atLeastFor(Soldier ? 2 : 1, dieN); struckByFoe keeps critical for the top face and soldierCrit for the Soldier's extra face"
    - "The hero's armor soak is read roll-high: rollCheck(rng, 20, atLeastFor(soakAr, 20)) with Taunt's doubled AR folded in; armorSoaked carries the soak triple, and a landed struckByFoe carries a nested soak triple when the soak die was drawn and failed"
    - "foeMissed, struckByFoe and memberStruck carry roll, atLeast and dieN with no need key; needMods is renamed mods with unchanged values; a die-driven crit adds critAtLeast; applyFoeDamageToPlayer's parameter object carries { dmg, roll, atLeast, dieN, mods } instead of the old need"
    - "The Oracle's foe-swing lines use the final 'roll vs lo–hi (mods)' shape; the old need-clause helper is gone; narrationLines' foeMissed 'would have hit' reason reads roll >= atLeast + (sum of negative deltas), byte-identical to its old meaning"
    - "Outcomes, draw counts and every baseline are unchanged"
  artifacts:
    - path: engine/combat.js
      provides: "foeTurn's hero and member branches, pursuitStrike and applyFoeDamageToPlayer's soak on rollCheck"
      contains: "atLeastFor(soakAr, 20)"
    - path: src/browser/eventNarration.js
      provides: "foeMissed, struckByFoe and memberStruck in the final line shape"
      contains: "modsClause(e.mods)"
    - path: src/browser/narrationLines.js
      provides: "foeMissed's would-have-hit reason on the roll-high fields"
  key_links:
    - from: engine/combat.js#foeTurn and #pursuitStrike
      to: engine/combat.js#applyFoeDamageToPlayer
      via: "{ dmg, roll, atLeast, dieN, mods }"
      pattern: "applyFoeDamageToPlayer\\(state"
    - from: src/browser/narrationLines.js#LINE_FOR.foeMissed
      to: "event fields roll, atLeast, mods"
      via: "the would-have-hit test"
      pattern: "atLeast"
  prohibitions:
    - statement: "MUST NOT show a mirrored roll-high roll next to a roll-under need or threshold on any surface: every foe-swing line uses 'roll vs lo–hi' once its event carries atLeast"
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Convert every foe to-hit and the hero's armor soak: foeTurn's hero and member branches, pursuitStrike, the foe crits including the Soldier's weakness, and applyFoeDamageToPlayer's soak die. Carry the triple on foeMissed, struckByFoe, memberStruck and armorSoaked, and finish the combat lines by retiring the old need-clause helper.

Purpose: CONTEXT Area 1 (verdict rows 15, 16, 24, 26-29): "the foe's natural-best crit on members", "the Soldier's crit-taken weakness (attacker's top face, or top 2 for a Soldier)", "the 'only a perfect roll finds you' overrides... the foe needs its die's top face, and the insult adds one more face (19–20 on a d20)". The foe is the roller here, so its roll is high-is-good for the FOE; its mods stay signed for the foe (Phase 74 re-signs them for the player).

Output: the foe-side sites on rollCheck, the new event fields and parameter object, the three Oracle lines, the rewritten would-have-hit reason, the invariant rows and the mirrored tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-07 creates or changes.)

- The helper and guard (73-01); baselines and the invariant (73-02); rollRange.js and the ledger verdicts (73-03); the hero strike and foe soak (73-04); member/ally/thrown strikes and the isBestFace flip (73-05); resistance and magic mishaps (73-06)
- **This plan:** foeTurn's hero and member branches, pursuitStrike, the foe crits and applyFoeDamageToPlayer's soak on rollCheck; applyFoeDamageToPlayer's `{ dmg, roll, atLeast, dieN, mods }` parameters
- **This plan:** foeMissed/struckByFoe/memberStruck `{ roll, atLeast, dieN, mods?, critAtLeast?, soak? }`; the armorSoaked triple; the foe-swing Oracle lines; the old need-clause helper removed; LINE_FOR.foeMissed on the new fields
- Flee, parley, gates, drops and the foe ability gate (73-08; combat.js is enforced there); traps, locks, climbs, cures and wake (73-09); the proofs and docs (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/73-engine-roll-high-mirror/73-06-SUMMARY.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/combat.js at d274925 (key by function; lines drift):
- pursuitStrike (~L986-L1042): `dieN = foeDie(c, pursuer)`; `roll = rng.d(dieN)`; `need = foeToHitVs(state)`; `needMods = foeToHitBreakdown(state).mods.slice()`; blind -> need 1; `C.foeToHitPenalty` -> Math.min; `C.parleyInsulted` -> need + 1 (LAST); `if (roll > need) { foeMissed {name, roll, need, needMods?}; return }`; `crit = roll === 1 || (roll <= 2 && c.sub === "Soldier")`; the damage dice draw; `applyFoeDamageToPlayer(state, pursuer, rng, events, { dmg, roll, need, needMods })`.
- foeTurn member branch (~L2551-L2680): `mDieN`, `mRoll = rng.d(mDieN)`, `mNeed` chain (blind, penalty, the member's own Sidestep `Math.max(1, mNeed - 2)` and Smoke `= 1`, insulted LAST), `mNeedMods`; `if (mRoll > mNeed)` pushes foeMissed `{ name, roll, need, member, needMods? }` (then the member Riposte); `mCrit = mRoll === 1`; memberStruck `{ name, member, dmg, roll, need, critical: mRoll === 1, needMods? }`.
- foeTurn hero branch (~L2682-L2755): `dieN = foeDie(c, f)`; `roll = rng.d(dieN)`; the need chain like pursuit; `if (roll > need)` pushes foeMissed (then the hero Riposte); `crit = roll === 1 || (roll <= 2 && c.sub === "Soldier")`; `applyFoeDamageToPlayer(state, f, rng, events, { dmg, roll, need, needMods })`.
- applyFoeDamageToPlayer (~L2190-L2371): signature `(state, foe, rng, events, { dmg, roll, need, needMods, ignoresArmor, ability })`. Soak: `soakAr = taunt ? Math.min(20, av.ar * 2) : av.ar`; `if (av.wp > 0 && av.ar > 0 && !ignores) { const soak = rng.d(20); if (soak <= soakAr) { … armorSoaked push … return } }`. struckByFoe push `{ type, name, roll, need, dmg, ignoresArmor, critical: roll === 1, soaked?, needMods?, soldierCrit (roll === 2 && Soldier)? }`. foeBolted (the ability path) carries no to-hit roll. External callers also exist (engine/foeAbilities.js bolts, `grep -rn "applyFoeDamageToPlayer(" engine/`); update every caller's parameter object.
The draws in each branch come BEFORE the pure need chain; either keep the draw first and compute atLeast after it (rollCheck then needs the threshold, so compute the face count first; it is pure), or keep the order the way 73-04 did in playerStrike. The draw order must not change: to-hit die, then the damage dice, then the soak die.

Mirrored crit rule: `critAtLeast = atLeastFor(c.sub === "Soldier" ? 2 : 1, dieN)`; `crit = roll >= critAtLeast`. In applyFoeDamageToPlayer: `critical = isBestFace(roll, dieN)` and `soldierCrit = roll === dieN - 1 && c.sub === "Soldier"`; struckByFoe carries `critAtLeast` when critical or soldierCrit is true. Member crit: `isBestFace(mRoll, mDieN)`, critAtLeast = mDieN.

src/browser/eventNarration.js: memberStruck (~L542), foeMissed (~L550) and struckByFoe (~L578) still call the old need clause; 73-04 added `modsClause(mods)`. Target lines: foeMissed `Zit swings at Bram, <span class="roll">12</span> vs 19–20 (insulted +1), and misses.`; struckByFoe `<span class="roll">20</span> vs 16–20 (Guard −1). <span class="hurt">Critical!</span> Zit hits you for <span class="hurt">7 hp</span> (hide soaked 2).`; memberStruck in the same shape. The mods are the engine's, signed for the foe; Phase 74 negates them for the player.
src/browser/narrationLines.js LINE_FOR.foeMissed (~L1266-L1274): `wouldHaveHit = negative.length > 0 && e.roll != null && e.need != null && e.roll <= e.need - negSum`. The same meaning on the new fields: without the negative deltas the foe's lowest winning face would be `atLeast + negSum` (negSum is negative), so `wouldHaveHit = negative.length > 0 && e.roll != null && e.atLeast != null && e.roll >= e.atLeast + negSum`.

Proof gates and test-update rules: exactly as in 73-04-PLAN.md `<interfaces>`. For tests that call applyFoeDamageToPlayer directly with `{ dmg, roll: 3, need: 5 }` and no die: use dieN 20 and mirror (roll 18, atLeast 16); the function never compares them, so only the pushed event fields change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The hero branch, pursuit, the foe crits and the hero soak on rollCheck</name>
  <files>engine/combat.js, test/unit/combat.test.js, test/unit/feedback-payload.test.js, test/unit/parley.test.js, test/unit/armor-durability.test.js</files>
  <read_first>
    - engine/combat.js (pursuitStrike, applyFoeDamageToPlayer, foeTurn's hero branch; grep every applyFoeDamageToPlayer caller across engine/)
    - docs/ROLL-LEDGER.md (verdict rows 15, 16, 24, 28, 29; Event fields)
    - test/unit/combat.test.js (every applyFoeDamageToPlayer call ~L830-L980 and ~L1530-L1590; the parley insult tests ~L1160-L1175)
    - test/unit/parley.test.js (L370-L415, L450-L460, L725-L750 the insult need assertions)
  </read_first>
  <action>
Per verdict rows 15, 16, 24, 28 and 29:
1. foeTurn hero branch and pursuitStrike: rename `need` to `faces` and `needMods` to `mods`; keep every term and its order (blind override, Weaken cap, insult LAST); read the to-hit die through `rollCheck(rng, dieN, atLeastFor(faces, dieN))` at the old draw position. A miss is `!check.ok`, pushing foeMissed `{ type, name, ...rollFields, mods? }`. The crit is `roll >= atLeastFor(Soldier ? 2 : 1, dieN)`. Pass `{ dmg, roll, atLeast, dieN, mods }` to applyFoeDamageToPlayer.
2. applyFoeDamageToPlayer: its parameter destructuring becomes exactly `{ dmg, roll, atLeast, dieN, mods, ignoresArmor, ability }`; the soak die becomes `rollCheck(rng, 20, atLeastFor(soakAr, 20))` under the unchanged gate (Taunt's doubling is already inside soakAr). armorSoaked gains `...rollFields(soakCheck)`. When the soak die was drawn and failed, struckByFoe gains `soak: rollFields(soakCheck)`. struckByFoe carries `...{ roll, atLeast, dieN }`, `critical: isBestFace(roll, dieN)`, `soldierCrit` for `roll === dieN - 1 && Soldier`, `critAtLeast` when either is true, and `mods` when non-empty; `need` is gone. Update every other caller's parameter object (the ability path passes no roll).
3. Rewrite the comments in the converted regions to the faces/roll-high reading (for example "p.44: roll d20; at or under your AR" becomes the soak reading on the top AR faces). Keep every DELIBERATE RULES CHANGE rationale.
4. Update the hero-branch, pursuit, applyFoeDamageToPlayer, armor and insult assertions per the test-update rules. An insult test that asserted `struck.need === 6` on a d20 now asserts `atLeast === 15`, and its "N + 1" wording becomes "one more top face".
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/parley.test.js test/unit/armor-durability.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "atLeastFor(soakAr, 20)" engine/combat.js` equals 1, and `grep -c "atLeastFor(c.sub === \"Soldier\" ? 2 : 1, dieN)" engine/combat.js` is at least 2 (hero branch and pursuit).
    - `grep -c "{ dmg, roll, atLeast, dieN, mods, ignoresArmor, ability }" engine/combat.js` equals 1 (applyFoeDamageToPlayer's new parameter destructuring).
    - The foe-vs-hero, pursuit, foe-crit and hero-soak rows in both direction files pass unchanged; the state pins pass.
  </acceptance_criteria>
  <done>A foe's swing at the hero and the hero's soak read roll-high.</done>
</task>

<task type="auto">
  <name>Task 2: The member branch and the member crit on rollCheck</name>
  <files>engine/combat.js, test/unit/party-combat.test.js, test/unit/party-abilities.test.js, test/unit/feedback-payload.test.js</files>
  <read_first>
    - engine/combat.js (foeTurn's member branch; pickFoeTarget's JSDoc for the draw-order rule)
    - docs/ROLL-LEDGER.md (verdict rows 26, 27)
    - test/unit/party-combat.test.js and test/unit/feedback-payload.test.js (L286-L305, the member-branch needMods test)
  </read_first>
  <action>
Per verdict rows 26-27:
1. Rename `mNeed` to `mFaces` and `mNeedMods` to `mMods`; keep the chain and its order (blind, penalty, member Sidestep, member Smoke, insult LAST). Read the to-hit die through `rollCheck(rng, mDieN, atLeastFor(mFaces, mDieN))` at the old position.
2. A miss pushes foeMissed `{ type, name, ...rollFields, member, mods? }` and the member Riposte follows unchanged. The member crit is `isBestFace(roll, mDieN)`. memberStruck pushes `{ type, name, member, dmg, ...rollFields, critical, critAtLeast: mDieN (only when critical), mods? }`; `need` is gone.
3. Rewrite the region's comments to the faces reading, and update the member-branch assertions per the test-update rules.
  </action>
  <verify>
    <automated>node --test test/unit/party-combat.test.js test/unit/party-abilities.test.js test/unit/feedback-payload.test.js test/unit/rollDirection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rollCheck(rng, mDieN, atLeastFor(mFaces, mDieN))" engine/combat.js` equals 1.
    - Every `[foe-vs-member:*]` and `[foe-crit-vs-member:*]` row in test/unit/rollDirection.test.js passes unchanged.
  </acceptance_criteria>
  <done>Every foe to-hit reads roll-high.</done>
</task>

<task type="auto">
  <name>Task 3: The foe-swing lines, the would-have-hit reason, the invariant rows, and the proof gates</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js, test/parity/roll-high-invariant.test.js, test/unit/linesForAction.test.js, test/unit/narrationLinesTable.test.js, test/unit/identity-race.test.js, test/unit/identity-contract.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (the helpers ~L65-L95; memberStruck, foeMissed, struckByFoe)
    - src/browser/narrationLines.js (LINE_FOR.foeMissed ~L1266)
    - test/parity/roll-high-invariant.test.js
    - test/unit/linesForAction.test.js (~L350-L370, the foe-line payloads)
  </read_first>
  <action>
1. src/browser/eventNarration.js: memberStruck, foeMissed and struckByFoe print `<span class="roll">${roll}</span> vs ${rangeText(e.atLeast, e.dieN)}${modsClause(e.mods)}`, keeping every other clause (member name, Critical!, dmg, soakedText). Then delete the old need-clause helper: after this edit nothing calls it (confirm with grep). Keep modsText and modsClause.
2. src/browser/narrationLines.js LINE_FOR.foeMissed: read `e.mods` and compute wouldHaveHit on the new fields exactly as `<interfaces>` gives it.
3. test/parity/roll-high-invariant.test.js OUTCOME: foeMissed (failure), struckByFoe (success), memberStruck (success), armorSoaked (success). The nested soak on struckByFoe is a failure.
4. Update the remaining foe-line expectations per the test-update rules, then run the proof gates.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "needs \${" src/browser/eventNarration.js` equals 0 (the old "(needs N: …)" clause is gone).
    - `grep -c "e.atLeast + negSum" src/browser/narrationLines.js` equals 1.
    - `grep -c "e?.roll <= e" src/browser/narrationLines.js` equals 0.
    - `grep -cE "^\s+(foeMissed|struckByFoe|memberStruck|armorSoaked)\s*[:(]" test/parity/roll-high-invariant.test.js` is at least 4.
    - All proof gates in 73-04's `<interfaces>` hold.
  </acceptance_criteria>
  <done>Both sides of every swing are roll-high end to end.</done>
</task>

</tasks>

<verification>
- The proof gates hold; every foe-side direction row is unchanged and green; the invariant covers the new events.
</verification>

<success_criteria>
ROADMAP criteria 1, 2 and 5 hold for foe swings, pursuit, foe crits and the hero soak, including the Phase 72 ruling expressed high (a smoked-and-insulted target is found on 19–20 of a d20).
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-07-SUMMARY.md` when done. Include the applyFoeDamageToPlayer parameter shape, the event shapes, every caller updated, the test files touched, the test counts, and a "Human verification (deferred to end of run)" note: a foe's miss and hit both read "<roll> vs lo–hi" in the Oracle; a smoked and insulted hero shows "vs 19–20".
</output>
