---
phase: 73-engine-roll-high-mirror
plan: 05
type: execute
wave: 3
depends_on:
  - 73-04
files_modified:
  - engine/combat.js
  - engine/magic.js
  - engine/dice.js
  - src/browser/eventNarration.js
  - test/parity/roll-high-invariant.test.js
  - test/unit/skeleton-shatter.test.js
  - test/unit/party-combat.test.js
  - test/unit/party-abilities.test.js
  - test/unit/combat.test.js
  - test/unit/afraid.test.js
  - test/unit/spell-mechanics.test.js
  - test/unit/linesForAction.test.js
  - test/unit/freeze-pays-out.test.js
  - test/unit/cast-refusals.test.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "Party-member, legacy-ally and summoned-ally strikes are read roll-high through rollCheck with their Phase 72 per-target chain folded into the face count, and Philly keeps the better mirrored face"
    - "Hero and member thrown attack spells fold the school and throw bonus into the threshold: rollCheck(rng, dieN, atLeastFor(target + bonus, dieN)), byte-identical to the old roll - bonus <= target"
    - "engine/dice.js#isBestFace(roll, dieN) is roll === dieN; the Skeleton shatters on the die's top face for all six strikers, the transitional shatter call from 73-04 is gone, and foeShattered carries roll, atLeast (= dieN) and dieN"
    - "A member's natural-best crit is isBestFace(roll, dieN) and reports critAtLeast = dieN"
    - "allyStruck, allyMissed, allyCast and spellThrown carry roll, atLeast and dieN with no need key; the separate bonus field is folded into mods as school/throw entries; allyStruck carries auto and a nested soak where they apply"
    - "The Oracle's allyMissed, allyCast and spellThrown lines use the final 'roll vs lo–hi (mods)' shape; outcomes, draw counts and every baseline are unchanged"
  artifacts:
    - path: engine/dice.js
      provides: "isBestFace(roll, dieN) === (roll === dieN)"
      contains: "roll === dieN"
    - path: engine/combat.js
      provides: "memberStrike, alliesTurn's legacy branch, allyTurn and allyCast's thrown branch on rollCheck"
      contains: "rollCheck("
    - path: engine/magic.js
      provides: "castSpell's thrown branch on rollCheck"
      contains: "rollCheck("
    - path: test/unit/skeleton-shatter.test.js
      provides: "the isBestFace contract in the roll-high reading"
  key_links:
    - from: engine/combat.js#shatterIfBest
      to: engine/dice.js#isBestFace
      via: "the mirrored roll from every striker"
      pattern: "isBestFace\\(roll, dieN\\)"
    - from: engine/magic.js#castSpell (thrown)
      to: engine/dice.js#rollCheck
      via: "import from ./dice.js"
      pattern: "rollCheck\\("
  prohibitions:
    - statement: "MUST NOT regenerate, edit or re-declare a parity fixture, divergence record, comparable carve-out, state pin, the pre-switch save or the Phase 72 direction tests, nor loosen an expected test value, to absorb a mirror mistake"
      category: integrity
      status: resolved
      verification: judgment
---

<objective>
Convert every other to-hit on the player's side: party-member, legacy and summoned-ally strikes, and hero and member thrown attack spells. Flip isBestFace, so the Skeleton shatters on the top face everywhere, and remove 73-04's transitional shatter call.

Purpose: CONTEXT Area 1 (verdict rows 20-23 and 37; "Crits and best-face effects move to the top faces... the Skeleton shatter via isBestFace (becomes roll === dieN)"; "Thrown spells' roll − bonus <= target folds the school bonus into the threshold"). isBestFace flips here because this plan converts the last five shatter callers.

Output: the four strikers and both thrown branches on rollCheck; isBestFace flipped; the new event fields; the three Oracle lines; invariant rows; mirrored tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-05 creates or changes.)

- engine/dice.js helper, the guard and helper tests, tags in the conversion-free files (73-01); baselines and the invariant (73-02); rollRange.js and the ledger verdicts (73-03); the hero strike, crits, frenzy and foe soak (73-04)
- **This plan:** engine/dice.js `isBestFace` = `roll === dieN`; the transitional shatter call removed; foeShattered `{ …, roll, atLeast: dieN, dieN }`
- **This plan:** memberStrike, alliesTurn legacy, allyTurn and allyCast (thrown) in combat.js, and castSpell (thrown) in magic.js on rollCheck
- **This plan:** allyStruck/allyMissed/allyCast/spellThrown triples; `mods` entries `school`/`throw` replace the thrown `bonus` field; the allyMissed/allyCast/spellThrown Oracle lines
- Resistance and magic mishaps (73-06); foe swings and the hero soak (73-07); flee, parley, gates and drops (73-08); traps, locks, climbs, cures and wake (73-09); the proofs and docs (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/73-engine-roll-high-mirror/73-04-SUMMARY.md
@docs/ROLL-LEDGER.md

<interfaces>
From 73-01/73-04: `rollCheck(rng, dieN, atLeast)` -> `{ roll, atLeast, dieN, ok }`; `atLeastFor(faces, dieN)`; `rollFields(chk)`. playerStrike already reads roll-high and calls `shatterIfBest(state, t, dieN + 1 - roll, …)` behind a comment carrying the phrase "Phase 73 transitional". damageFoe returns `soak` (the failed soak triple) when its die was drawn and failed. eventNarration.js has `modsText`/`modsClause` and imports `rangeText`.

engine/combat.js at d274925 (line numbers drift; key by function):
- allyTurn (~L1521-L1560): `dieN = STRIKE_DICE[C.ally.lvl - 1]`; `let roll = rng.d(dieN)`; `let need = 5`; Philly `roll = Math.min(roll, rng.d(dieN))`; then the per-target chain (dozing/stupid floor, sp.toHit, sp.fast, magicOnly/daggerOnly zero); `if (roll <= need) { if (!shatterIfBest(...)) { damage; allyStruck {name, target, dmg} } } else allyMissed {name}`; the `--C.ally.rounds` countdown always runs after.
- alliesTurn legacy branch (~L1600-L1640): same shape with `legacyDieN = STRIKE_DICE[clamp(ally.lvl,1,5)-1]`; `if (shatterIfBest(...)) continue`.
- memberStrike (~L1901-L1975): `let need = memberToHit(view)`; `dieN = strikeDie(view)`; `let roll = rng.d(dieN)`; Philly; per-target chain; `mod.needShift`; `auto = !!(mod && mod.autoHit)`; `if (!auto && roll > need) { allyMissed {name, target, roll, need, weapon?, via?}; return }`; shatter; `crit = roll === 1 && !noCrit`; forceCrit and the Thief backstab set crit too; `hit = damageFoe(...)`; allyStruck `{ name, target, dmg, weapon?, crit?, backstab?, via? }`.
- allyCast thrown (~L2003-L2030): `freeze`; `dieN = freeze ? 10 : 8`; `need = freeze ? 6 : 4`; `bonus = schoolBonus(view.sub, sp.s) + eff(view, "throw")`; `roll = rng.d(dieN)`; push allyCast `{ name, spell, target, roll, need, bonus }`; `if (roll - bonus <= need) { shatter; damage; allySpellHit } else allySpellMissed { resisted: false }`.
engine/magic.js castSpell thrown (~L479-L552): `bonus = schoolBonus(c.sub, sp.s) + eff(c, "throw")`; per target `dieN = freeze ? 10 : 8`; `baseTarget = freeze ? 6 : 4`; `target = afraidNeed(state, baseTarget)`; afraidMods; `roll = rng.d(dieN)`; push spellThrown `{ spell, target: t.name, roll, need: target, bonus, needMods? }`; `if (roll - bonus <= target) { shatter → continue; damage; spellHit … } else spellMissed`. magic.js imports from ./combat.js; importing ./dice.js is cycle-free.
engine/dice.js `isBestFace(roll, dieN)` returns `roll === 1` today; test/unit/skeleton-shatter.test.js L56-L62 pins that reading.

Mods naming for thrown spells: keep afraidMods, and add `{ name: "school", delta: schoolBonus(...) }` and `{ name: "throw", delta: eff(…, "throw") }` when each is non-zero. They sum to today's `bonus`, so the threshold is `atLeastFor(target + bonus, dieN)`.

Target lines: allyMissed (targeted form) `Bram swings a Sword at Dante and misses. <span class="roll">9 vs 16–20.</span>`; allyCast `Bram casts <span class="hit">Fireball</span> at Dante. <span class="roll">7 vs 4–8 (school +1).</span>`; spellThrown `Fireball at Dante: <span class="roll">7</span> vs 4–8 (school +1).` Each uses rangeText and modsClause.

Proof gates and test-update rules: exactly as in 73-04-PLAN.md `<interfaces>` (parity glob green; the direction tests, ledger-sync, rollOdds harness, parity fixtures, master and comparables unchanged since d274925; the pins, save and baseline harness untouched since the plan base, and they pass with the guard; both fixture scans empty; npm test green except the 7 known worktree CRLF doc failures, with the test count not lower than the plan base). Mirror expectations N+1−old, never loosen, never change a scripted draw.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Member, legacy and summoned-ally strikes on rollCheck</name>
  <files>engine/combat.js, test/unit/party-combat.test.js, test/unit/party-abilities.test.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/combat.js (allyTurn, alliesTurn's legacy branch, memberStrike, shatterIfBest)
    - docs/ROLL-LEDGER.md (verdict rows 20-22; Event fields)
    - test/unit/party-combat.test.js, test/unit/party-abilities.test.js (their allyMissed/allyStruck roll and need assertions), test/unit/combat.test.js (the Phase 72 F1 legacy/summon regression tests)
  </read_first>
  <action>
Per verdict rows 20-22, in each of allyTurn, the legacy branch and memberStrike:
1. Compute the face count first (it is pure): rename `need` to `faces` and apply the existing per-target chain (and `mod.needShift` in memberStrike) in its existing order. Then draw `rollCheck(rng, dieN, atLeastFor(faces, dieN))`. For Philly (`t.sp && t.sp.slow`), draw a second rollCheck and keep `Math.max` of the two rolls. The draw order is unchanged: the strike die, then Philly's.
2. A hit is `roll >= atLeast` (memberStrike: `auto ||` it). Events: allyMissed carries `...rollFields` (the targeted memberStrike form keeps target/weapon/via; the legacy and summon forms gain target and the triple); allyStruck carries `...rollFields` plus `auto: true` when auto, `critAtLeast: dieN` when memberStrike's crit came from the natural-best face, and `soak` from damageFoe's return when present. `need` is gone.
3. memberStrike's natural-best crit becomes `isBestFace(roll, dieN) && !noCrit`. It reads correctly once Task 2 flips isBestFace; this task and Task 2 land in the same plan, so run the full suite only after Task 2.
4. Their shatter calls pass the mirrored roll: `shatterIfBest(state, t, roll, dieN, …)`.
5. Keep allyTurn's `--C.ally.rounds` countdown running after a hit, a miss and a shatter.
6. Update the comments in the converted regions to the faces/roll-high reading. Update the member/ally roll and need assertions per the test-update rules.
  </action>
  <verify>
    <automated>node --test test/unit/party-combat.test.js test/unit/party-abilities.test.js test/unit/rollDirection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - In each of allyTurn, the legacy branch and memberStrike, the strike draw is a rollCheck: `grep -c "rollCheck(rng, legacyDieN" engine/combat.js` is at least 1, and `grep -c "rollCheck(rng, dieN" engine/combat.js` is at least 6.
    - `awk '/^function memberStrike/,/^}/' engine/combat.js | grep -c "isBestFace(roll, dieN)"` equals 1.
    - test/unit/rollDirection.test.js passes unchanged (its member-strike and ally-strike rows included).
  </acceptance_criteria>
  <done>Every party-side melee strike reads roll-high.</done>
</task>

<task type="auto">
  <name>Task 2: Thrown attack spells on rollCheck, and the isBestFace flip</name>
  <files>engine/magic.js, engine/combat.js, engine/dice.js, test/unit/skeleton-shatter.test.js, test/unit/afraid.test.js, test/unit/spell-mechanics.test.js, test/unit/freeze-pays-out.test.js, test/unit/cast-refusals.test.js</files>
  <read_first>
    - engine/magic.js (imports, castSpell's thrown branch ~L470-L555)
    - engine/combat.js (allyCast's thrown branch; playerStrike's transitional shatter call; shatterIfBest)
    - engine/dice.js (isBestFace)
    - test/unit/skeleton-shatter.test.js (L50-L62 and every foeShattered assertion)
    - test/unit/afraid.test.js (L395-L440 thrown assertions)
  </read_first>
  <action>
Per verdict row 37 and row 23, and CONTEXT Area 1's crit ruling:
1. engine/magic.js thrown: import `rollCheck, atLeastFor, rollFields` from ./dice.js. Keep `bonus`, `baseTarget`, `target = afraidNeed(...)` and `afraidMods`. Build `mods` = afraidMods plus the school and throw entries (non-zero only). Draw `rollCheck(rng, dieN, atLeastFor(target + bonus, dieN))`. Push spellThrown `{ type, spell, target: t.name, ...rollFields, mods? }` (drop `need` and `bonus`); a hit is `.ok`. Pass the mirrored roll to shatterIfBest. Rewrite the Afraid comment (it said the target shrinks, never the roll) to the faces reading: Afraid removes winning faces from the top range.
2. engine/combat.js allyCast thrown: the same fold with the member's view (`schoolBonus(view.sub, sp.s)`, `eff(view, "throw")`, `need` renamed `faces`). allyCast pushes `{ type, name, spell, target, ...rollFields, mods? }` (drop `need` and `bonus`); the resist branch below is 73-06's.
3. engine/dice.js: isBestFace returns `roll === dieN`. Its JSDoc: the top face of an N-sided die is the roller's best; Phase 73 flipped it with every caller now passing the mirrored roll.
4. engine/combat.js: playerStrike's shatter call passes `roll` (remove the `dieN + 1 -` and the transitional comment). shatterIfBest pushes foeShattered as `{ type, target, by, roll, atLeast: dieN, dieN, ...extra }`.
5. test/unit/skeleton-shatter.test.js: rewrite the isBestFace contract test to the roll-high reading, mirrored: `isBestFace(20, 20)` and `isBestFace(6, 6)` true; `isBestFace(1, 20)`, `isBestFace(19, 20)` and `isBestFace(1, 6)` false; retitle it. Every odds-based shatter row must pass unchanged. Adjust a foeShattered shape assertion only if it deep-equals the old shape (the triple is additive).
6. Update the thrown roll/need/bonus assertions per the test-update rules.
  </action>
  <verify>
    <automated>node --test test/unit/skeleton-shatter.test.js test/unit/afraid.test.js test/unit/spell-mechanics.test.js test/unit/freeze-pays-out.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "return roll === dieN" engine/dice.js` equals 1.
    - `grep -c "Phase 73 transitional" engine/combat.js` equals 0, and the guard's transitional rule is ready to enforce combat.js later.
    - `grep -c "atLeastFor(target + bonus, dieN)" engine/magic.js` equals 1, and `grep -c "rollCheck(" engine/magic.js` is at least 1.
    - Every `[shatter:*]` row in test/unit/rollDirection.test.js and every shatter test in skeleton-shatter.test.js (except the rewritten contract test) passes unchanged; the state pins pass.
  </acceptance_criteria>
  <done>Thrown spells read roll-high, and the best face is the top face everywhere.</done>
</task>

<task type="auto">
  <name>Task 3: The ally and thrown Oracle lines, the invariant rows, and the proof gates</name>
  <files>src/browser/eventNarration.js, test/parity/roll-high-invariant.test.js, test/unit/linesForAction.test.js, test/unit/combat.test.js, test/unit/party-combat.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (allyMissed ~L517, allyCast ~L525, spellThrown ~L770, and the helpers)
    - test/parity/roll-high-invariant.test.js (OUTCOME, NESTED_CHECKS)
    - test/unit/linesForAction.test.js (the spellThrown groups ~L370-L435)
  </read_first>
  <action>
1. src/browser/eventNarration.js (CONTEXT Area 3, the final line shape): allyMissed's targeted form prints `<span class="roll">${roll} vs ${rangeText(e.atLeast, e.dieN)}${modsClause(e.mods)}.</span>` when roll is present; allyCast prints `<span class="roll">${roll} vs ${range}${modsClause}.</span>` (the "(+bonus)" suffix goes; the bonus is now a mod); spellThrown prints `<span class="roll">${roll}</span> vs ${range}${modsClause}.`. Keep every other clause, the voice and the bare-payload safety.
2. test/parity/roll-high-invariant.test.js OUTCOME: `allyStruck` (success), `allyMissed` (failure), `foeShattered` (success; atLeast = dieN), `spellThrown` (success unless a `spellMissed` for the same target follows before the next spellThrown in the same action), `allyCast` (only when it carries atLeast: success unless an `allySpellMissed` with `resisted: false` follows before the next allyCast). The nested soak on allyStruck is a failure.
3. Update the remaining expectations per the test-update rules.
4. Run the proof gates.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s+(allyStruck|allyMissed|foeShattered|spellThrown|allyCast)\s*[:(]" test/parity/roll-high-invariant.test.js` is at least 5.
    - All proof gates in 73-04's `<interfaces>` hold.
  </acceptance_criteria>
  <done>Every player-side to-hit is roll-high end to end, and the Skeleton shatters on the top face.</done>
</task>

</tasks>

<verification>
- The proof gates hold; the shatter rows and the direction tests are unchanged and green; the invariant covers the new events.
</verification>

<success_criteria>
ROADMAP criteria 1, 2 and 5 hold for member, ally and thrown to-hit rolls and the best-face shatter; CONTEXT's "the Skeleton shatter via isBestFace (becomes roll === dieN)" is done.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-05-SUMMARY.md` when done. Include the new event shapes, the isBestFace flip, the shim removal, the test files touched, the test counts, and a "Human verification (deferred to end of run)" note: a party member's miss reads "<roll> vs 16–20" in the Oracle; a thrown attack spell reads "<roll> vs lo–hi" on its d8 (with "(school +N)" for a sub that has an offense bonus); a Skeleton shatters on the top face of the striking die.
</output>
