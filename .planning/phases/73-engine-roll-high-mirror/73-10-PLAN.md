---
phase: 73-engine-roll-high-mirror
plan: 10
type: execute
wave: 8
depends_on:
  - 73-09
files_modified:
  - docs/DIFFICULTY-RETUNE.md
  - docs/ROLL-LEDGER.md
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/roll-high-invariant.test.js
  - tools/voice-sample.mjs
  - tools/voice-sample-output.txt
  - engine/combat.js
  - engine/derived.js
  - engine/encounters.js
  - engine/movement.js
  - engine/magic.js
  - engine/foeDamage.js
  - engine/dice.js
  - engine/items.js
  - engine/foeAbilities.js
  - engine/difficulty.js
  - content/weapons.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "Every die check in engine/ reads the same draw r as (N+1) - r through engine/dice.js#rollCheck and succeeds at or above atLeast, with every modifier folded into the threshold as a signed bonus to the roller; flee and initiative stay roll-high with their modifiers aligned"
    - "Every seeded run resolves identically: the parity suite passes with zero regenerated fixtures, zero new divergence records and zero new comparable carve-outs (git diff against d274925 is empty for test/parity/fixtures, prototype-master.js.txt and comparables.js, and no divergence record names Phase 73); the Phase 72 direction tests, the ledger-sync test, the state pins and the pre-switch save pass unedited"
    - "The 200-seed readout (node tools/tune-difficulty.mjs --seeds=200) at the end of the phase equals the Phase 73 base readout byte for byte (after CRLF normalisation) and contains every recorded line of docs/DIFFICULTY-RETUNE.md's Phase 72 AFTER block (commit d2adfd6), in order; it is recorded as the third proof in a new H2"
    - "The guard fails the build on any roll-under comparison, untagged raw draw or stray mirror anywhere in engine/, and the runtime invariant holds over every parity replay and the bot sweep"
    - "Content numbers keep their values as counts of winning faces and are documented that way; no persisted field was renamed; a pre-switch save loads and resolves identically with no conversion step"
    - "Every roll-carrying event carries roll, atLeast and dieN, and no roll-carrying event carries need; every event-driven Oracle, fight-log and rail roll line reads 'roll vs lo–hi (mods)'"
    - "A roll exactly equal to atLeast succeeds, so every boundary face resolves exactly as the old roll <= need did at r = need; an initiative tie still goes to the hero"
    - "Degenerate thresholds are exact, never clamped: zero winning faces never succeeds and prints 'nothing', every face always succeeds and prints '1–N', a single face prints 'N', and a missing field prints '?'"
    - "Draw count and order are unchanged at every site: rollCheck draws exactly one die where the old draw sat, Philly's two dice keep their order and keep the better mirrored face, and event order is unchanged"
    - "The mirror is applied exactly once (only inside rollCheck; the guard forbids mirror arithmetic anywhere else), and a pre-switch save's load-save-load round trip is stable with no conversion step"
    - "A run interrupted mid-run (saved before the switch, reloaded after it) resumes with identical outcomes; the engine keeps no roll state outside the serializable state, and each wave left master green"
    - "Engine and content comments describe the faces and roll-high reading; any remaining mention of the old reading is marked as history (it names Phase 73); authored player-facing prose is untouched and handed to Phase 79"
  artifacts:
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "## v2.1 roll-high mirror (Phase 73) — bot readout, before the v1.2 H2"
      contains: "(Phase 73)"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "## Phase 73: engine roll-high mirror (ROLL-05) — measured zero"
      contains: "## Phase 73:"
    - path: docs/ROLL-LEDGER.md
      provides: "the finalized verdict statuses, the Phase 73 draw inventory table, and the Phase 74/79 handoffs"
    - path: test/parity/roll-high-invariant.test.js
      provides: "the standing ROLL-05 zero-declaration check"
  key_links:
    - from: docs/DIFFICULTY-RETUNE.md Phase 73 H2
      to: tools/roll-high-baseline-readout.txt
      via: "node tools/readout-compare.mjs --exact"
      pattern: "readout-compare"
    - from: docs/ROLL-LEDGER.md handoffs
      to: .planning/phases/74-roll-display-modifier-honesty
      via: "the event field names and the faces-returning derived functions"
      pattern: "atLeast"
  prohibitions:
    - statement: "MUST NOT show a mirrored roll-high roll next to a roll-under need or threshold on any surface"
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT regenerate, edit or re-declare a parity fixture, divergence record, comparable carve-out, state pin, the pre-switch save or the Phase 72 direction tests, nor loosen an expected test value, to absorb a mirror mistake; a moved result means the site is fixed, never the evidence"
      category: integrity
      status: resolved
      verification: judgment
    - statement: "MUST NOT change the meaning of any number stored in an existing save, rename a persisted field, or require a save reset"
      category: player-data
      status: resolved
      verification: judgment
---

<objective>
Close Phase 73: record the three proofs (byte-identical parity, the unchanged Phase 72 direction tests, and the matching 200-seed readout), finalize the ledger and hand off to Phases 74 and 79, and sweep the engine and content comments to the faces and roll-high reading.

Purpose: CONTEXT Area 3 ("Proof it changed nothing": zero regenerated fixtures and carve-outs; the direction tests unchanged; the readout matching Phase 72's AFTER readout exactly, recorded as a third proof) and Area 2 ("names, JSDoc and comments that say 'roll under'... are rewritten").

Output: the Phase 73 H2 in docs/DIFFICULTY-RETUNE.md, the Phase 73 section in test/parity/FIXTURE-INVENTORY.md, the finalized ledger, a standing zero-declaration check, the voice-sample tool on the new fields, and comment-only engine/content edits.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-10 creates or changes.)

- The helper, the guard and its ALL_ENFORCED closure (73-01, 73-09); the baselines, readout baseline and the invariant with COMPLETE (73-02, 73-09); rollRange.js and the ledger verdicts (73-03); every check converted and every event carrying the triple (73-04 to 73-09)
- **This plan:** docs/DIFFICULTY-RETUNE.md `## v2.1 roll-high mirror (Phase 73) — bot readout`; test/parity/FIXTURE-INVENTORY.md `## Phase 73: …`; docs/ROLL-LEDGER.md statuses, the Phase 73 draw inventory and the handoffs
- **This plan:** the ROLL-05 zero-declaration check in test/parity/roll-high-invariant.test.js; tools/voice-sample.mjs (and its output) on the new fields
- **This plan:** comment-only sweeps of engine/ and content/weapons.js

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/73-engine-roll-high-mirror/73-09-SUMMARY.md
@docs/ROLL-LEDGER.md

<interfaces>
tools/readout-compare.mjs (73-02): `--recorded <doc> "<heading prefix>" <file>` (subsequence of the fenced block) and `--exact <fileA> <fileB>`. tools/roll-high-baseline-readout.txt holds the full 200-seed readout at the Phase 73 base, ending with `EXIT=0`.
docs/DIFFICULTY-RETUNE.md: the Phase 72 H2 `## v2.1 roll-direction pass (Phase 72) — bot readouts` (~L5453) holds `### AFTER — commit d2adfd6 (all Phase 72 fixes)`; `## v1.2 retune (Phase 27) — TUNE-05..07` must stay the LAST H2 (test/unit/difficulty-retune-ledger.test.js). Insert the new H2 between them.
test/parity/FIXTURE-INVENTORY.md: the last H2 is `## Phase 72: roll-direction sign fixes (ROLL-01) — measured per plan` (~L2771); test/parity/fixture-inventory.test.js pins the generated roster block at the top.
Divergence records live in the six fixture JSON files (`divergences`, `scenario.divergence`, `scenario.chargenDivergence`, top-level `divergence`), each with a `phase` field (see test/parity/divergence-records.test.js#collectRecords, L61-L95).
tools/voice-sample.mjs `sampleEvent(type)` (~L66-L90) fabricates a representative event with `roll`, `need`, `total` and more for every builder, and writes tools/voice-sample-output.txt.
The comment sweep's target phrasings (the old bottom-of-the-die reading) in engine/ and content/*.js comments: "roll under", "roll-under", "low = hit", "keep the lower", "at or under your AR", "N or less", "a 1 is best", "the lower (low = hit)". A comment about a mishap that fires on a natural 1 is CORRECT and stays (those gates kept their natural 1). Authored player-facing strings are NOT comments and stay exactly as they are (Phase 79 owns them), for example engine/items.js's Lockpicks text "1–5 on d10 against any lock", the Smoke/Mirror Self/Weaken/Crystal Staff texts, and the bestiary "hittable only on a 4" notes.
content/weapons.js L16-L22 is the one content comment block that explains the old reading ("a strike lands on roll <= need... a bigger number to roll under is a bonus").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the final readout, then sweep the comments and update the voice-sample tool</name>
  <files>engine/combat.js, engine/derived.js, engine/encounters.js, engine/movement.js, engine/magic.js, engine/foeDamage.js, engine/dice.js, content/weapons.js, tools/voice-sample.mjs, tools/voice-sample-output.txt</files>
  <read_first>
    - `grep -rniE "roll.under|low = hit|keep the lower|or less|at or under" engine/ content/weapons.js` (the sweep list)
    - content/weapons.js (L10-L30)
    - tools/voice-sample.mjs (L60-L140)
    - docs/ROLL-LEDGER.md (`## Handoffs → Phase 79`)
  </read_first>
  <action>
First, start the readout in the background (it outlives the 10-minute foreground cap), writing to the scratchpad, not the repo: `node tools/tune-difficulty.mjs --seeds=200 > <scratchpad>/readout73.txt 2>&1; echo "EXIT=$?" >> <scratchpad>/readout73.txt`. Task 3 uses it after the completion notification.

Comment sweep (per CONTEXT Area 2; comments only, no code token changes): rewrite every engine/ and content/weapons.js comment that states the old bottom-of-the-die reading (the phrasings in `<interfaces>`) as a CURRENT rule, so it describes faces and the roll-high reading. A sentence that has to mention the old reading for history names Phase 73 in the same line (for example "before Phase 73 this read as a low range"). Leave natural-1 mishap comments as they are, and never touch a string literal. If a player-facing string still encodes the old direction, add it to the Phase 79 handoff list you record in Task 2 (do not edit it).

tools/voice-sample.mjs: sampleEvent carries the new roll fields (`roll` in 1..20, `atLeast` in 1..21, `dieN: 20`, `rolls`, `mods` with one or two `{ name, delta }` entries, `critAtLeast`) and drops `need` and `total`. Then run it to regenerate tools/voice-sample-output.txt. The output is a sampling artifact for voice review; skim it for any "?" range where a roll line should read "lo–hi".
  </action>
  <verify>
    <automated>node tools/comment-only-diff.mjs HEAD engine content && node tools/voice-sample.mjs && node --test test/unit/roll-high-guard.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/comment-only-diff.mjs <plan-base> engine content` exits 0 (engine and content changed on comments only in this plan).
    - `grep -rniE "roll[- ]under|low = hit|keep the lower" engine/ content/weapons.js | grep -v "Phase 73"` prints nothing.
    - `grep -c "atLeast" tools/voice-sample.mjs` is at least 1.
    - `grep -c "need:" tools/voice-sample.mjs` equals 0.
    - The readout is running in the background (or finished) into the scratchpad file.
  </acceptance_criteria>
  <done>The engine explains itself in the roll-high reading, and the voice sampler exercises the new lines.</done>
</task>

<task type="auto">
  <name>Task 2: Measure zero moved fixtures, add the standing zero-declaration check, and finalize the ledger</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/roll-high-invariant.test.js, docs/ROLL-LEDGER.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 72 H2 as the model)
    - test/parity/divergence-records.test.js (L61-L95 collectRecords)
    - docs/ROLL-LEDGER.md (header, `## How to read this ledger`, `## Phase 73 mirror verdicts (ROLL-05)`, both Handoffs sections)
    - every 73-0N-SUMMARY.md (the plan that converted each site, and the event shapes)
    - test/unit/roll-high-guard.test.js (the final DRAW_INVENTORY)
  </read_first>
  <action>
1. Measure and record (per CONTEXT Area 3, "Proof it changed nothing"):
   - `node --test "test/parity/**/*.test.js"` is green.
   - `git diff --stat d274925 -- test/parity/fixtures test/parity/prototype-master.js.txt test/parity/harness/comparables.js` is empty.
   - `git hash-object test/parity/prototype-master.js.txt` is a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
   - Both fixture scans are empty.
   - `git diff --stat d274925 -- test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-ledger-sync.test.js test/unit/harness/rollOdds.js` is empty.
   - The state pins, the pre-switch save and the invariant pass, and their files are unchanged since 73-02 except the invariant's OUTCOME rows and COMPLETE flag (`git log --oneline -- test/unit/roll-high-state-pins.test.js test/unit/fixtures/roll-high/ test/unit/harness/rollHighBaseline.js` shows only 73-02's commits).
2. test/parity/roll-high-invariant.test.js: add a standing test that walks every divergence record in the six fixture files (collectRecords' shape, re-implemented locally) and asserts that none declares phase 73 (a `phase` whose `+`-split parts include "73").
3. test/parity/FIXTURE-INVENTORY.md: append `## Phase 73: engine roll-high mirror (ROLL-05) — measured zero` with the rule (the same draw, read N+1−r; content keeps its face counts), the predictor (a representation change cannot move state), and every live result from step 1, plus the three wider nets (the pins, the save and the invariant) and the readout (filled in by Task 3).
4. docs/ROLL-LEDGER.md:
   - Add a header line: Phase 73 (ROLL-05) switched the engine to roll-high, pointing to `## Phase 73 mirror verdicts (ROLL-05)`.
   - In `## How to read this ledger`, mark the first paragraph's roll-under description as the pre-Phase-73 reading, in one added sentence (the Modifier ledger's "Phase 73 reading" column is now the live reading).
   - Fill every Status cell in `### Verdict per site` with the converting plan ("done 73-0N").
   - Add `### Phase 73 draw inventory` (per file: rollCheck, amount, selection, mishap-on-1, already-high), copied from the guard's DRAW_INVENTORY.
   - Extend `## Handoffs → Phase 74` with the event field names (roll, atLeast, dieN, rolls, mods, critAtLeast, auto, nested soak and bag), the note that mods stay signed for the roller (Phase 74's one formatter negates them on foe-roll lines), the faces-returning derived functions (range on a dN = (N+1−faces)–N), and src/browser/rollRange.js.
   - Extend `## Handoffs → Phase 79` with every player-facing string Task 1 found (at least the Lockpicks text in engine/items.js).
   - Leave `## Modifier ledger` untouched; test/unit/roll-ledger-sync.test.js must pass unchanged.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" test/unit/roll-ledger-sync.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 73: engine roll-high mirror (ROLL-05)" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `grep -c "### Phase 73 draw inventory" docs/ROLL-LEDGER.md` equals 1.
    - Every Status cell in the verdict table is filled: `awk '/^### Verdict per site/,/^### Event fields/' docs/ROLL-LEDGER.md | grep -c "planned"` equals 0.
    - `git diff --stat d274925 -- test/parity/fixtures test/parity/prototype-master.js.txt test/parity/harness/comparables.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-ledger-sync.test.js test/unit/harness/rollOdds.js` prints nothing.
    - The zero-declaration test exists and passes.
  </acceptance_criteria>
  <done>The byte-identical proof is measured and recorded, and the ledger hands off cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: The readout proof, its record, and the final gate</name>
  <files>docs/DIFFICULTY-RETUNE.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md (~L5453-L5750, the Phase 72 H2, as the model)
    - test/unit/difficulty-retune-ledger.test.js (the H2 ordering rules)
    - the scratchpad readout from Task 1, once its completion notification has arrived
  </read_first>
  <action>
1. When the background readout has finished, confirm it ends with `EXIT=0`, then run both comparisons: `node tools/readout-compare.mjs --exact tools/roll-high-baseline-readout.txt <scratchpad>/readout73.txt` and `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "### AFTER — commit d2adfd6" <scratchpad>/readout73.txt`. If either fails, a site was flipped wrong somewhere the pins did not reach. STOP and report it; never re-baseline.
2. docs/DIFFICULTY-RETUNE.md: insert `## v2.1 roll-high mirror (Phase 73) — bot readout` immediately before `## v1.2 retune (Phase 27)` (keep v1.2 the last H2), with `### Change under measurement` (the engine reads every check roll-high; representation only), `### Parameters` (`node tools/tune-difficulty.mjs --seeds=200`, solo, start depth 1), `### AFTER — commit <sha>` holding the readout in a fenced block trimmed exactly like the Phase 72 AFTER block (the same trim note), and `### Reading`: identical to the Phase 73 base byte for byte and to every recorded line of the Phase 72 AFTER block; no difficulty change and no retune owed.
3. Add the readout result to FIXTURE-INVENTORY.md's Phase 73 section.
4. Final gate: `npm test` green (0 failures other than the 7 known worktree-only CRLF doc-ledger failures), the parity glob green, and the guard, invariant, pins, save and direction tests green.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Both readout-compare runs exit 0.
    - `grep -c "^## v2.1 roll-high mirror (Phase 73)" docs/DIFFICULTY-RETUNE.md` equals 1, and test/unit/difficulty-retune-ledger.test.js passes.
    - `npm test` shows 0 failures other than the 7 known worktree-only CRLF doc-ledger failures.
  </acceptance_criteria>
  <done>The third proof is recorded: the whole game plays exactly as before, now read roll-high.</done>
</task>

</tasks>

<verification>
- All three proofs hold and are recorded: parity byte-identical with zero moved fixtures, records or carve-outs; the Phase 72 direction tests unchanged and green; the 200-seed readout identical.
- The guard covers every engine file; the invariant is complete; the ledger is final; comments read roll-high.
</verification>

<success_criteria>
Every ROADMAP Phase 73 success criterion is met: (1) every check reads (N+1)−r at or above a high target; (2) every seeded run resolves identically, proven three ways; (3) the guard fails on any roll-under comparison in engine/; (4) content numbers are expressed as winning faces, and saves need no conversion (the pre-switch save proves it); (5) every roll-carrying event carries roll, atLeast (the roadmap's "target", renamed because target already names the victim) and dieN.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-10-SUMMARY.md` when done. Include the three proofs with their commands and results, the readout comparison output, the Phase 79 handoff strings, the final test count, and a "Human verification (deferred to end of run)" roll-up for the milestone's Pixel 7 batch, collected from 73-04 to 73-09.
</output>
