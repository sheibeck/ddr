---
phase: 73-engine-roll-high-mirror
plan: 08
type: execute
wave: 6
depends_on:
  - 73-07
files_modified:
  - engine/combat.js
  - engine/foeAbilities.js
  - engine/items.js
  - content/bags.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - docs/FLEE.md
  - test/unit/roll-high-guard.test.js
  - test/parity/roll-high-invariant.test.js
  - test/unit/flee-retune.test.js
  - test/unit/parley.test.js
  - test/unit/linesForAction.test.js
  - test/unit/loot-pile.test.js
  - test/unit/bag-cap-gate.test.js
  - test/unit/combat.test.js
  - test/unit/foe-abilities.test.js
  - test/unit/phobia-triggers.test.js
  - test/unit/identity-combat.test.js
autonomous: true
requirements:
  - ROLL-05
must_haves:
  truths:
    - "Flee stays roll-high with its modifiers folded into the threshold: the raw d20 is the roll, and it succeeds when roll >= need - bonus; fleeRolled carries { roll, atLeast, dieN: 20, mods } and no longer carries total or need, and the Oracle reads 'Flee: rolled 12 vs 11–20 (Thief +5)'"
    - "Parley is read roll-high: rollCheck(rng, 20, atLeastFor(min(9 + bonus, 17) - dial, 20)); parleyRolled carries the triple and fluency; the Oracle and fight-log lines use the 'roll vs lo–hi' shape"
    - "The combat gates mirror: Con Artist weak-foe flee (3–6 on a d6), Court Mage boredom (11–12 on a d12), the Hardiness phobia shrug (2 on a d2), the tier bleed (hero-side good news), and the foe ability gate (3–6 on a d6, the foe as roller); each die is drawn under exactly the old condition, and foeFled, foeBored, phobiaAfraid and foeCast carry the triple when their die was drawn"
    - "Loot drops mirror from the hero's side: the kill drop is rollCheck(rng, 20, atLeastFor(2 + f.lvl, 20)), and the bag upgrade uses the renamed BAG_DROP_FACES (same value); lootDropped for a kill drop carries the triple and a nested bag triple when the bag die was drawn"
    - "Initiative, cooking and the Humans parley payout stay as they are (already high) and are tagged; the foe count stays a selection"
    - "Every raw draw in engine/combat.js and engine/foeAbilities.js is tagged or goes through rollCheck, and both files are enforced by the guard; outcomes, draw counts and every baseline are unchanged"
  artifacts:
    - path: engine/combat.js
      provides: "flee, parley, the startCombat and fight gates, the kill drops and the foe ability gate converted; every draw tagged"
      contains: "atLeastFor(BAG_DROP_FACES, 20)"
    - path: engine/foeAbilities.js
      provides: "resolveFoeAbility's optional gate on foeCast; tagged amounts"
    - path: content/bags.js
      provides: "BAG_DROP_FACES (renamed, same value)"
      contains: "export const BAG_DROP_FACES"
    - path: docs/FLEE.md
      provides: "the fleeRolled payload in its new shape"
  key_links:
    - from: engine/combat.js#killFoe
      to: engine/items.js#offerLoot
      via: "an optional roll-check argument spread onto lootDropped"
      pattern: "offerLoot\\(state, drop, events"
    - from: engine/combat.js#foeTurn
      to: engine/foeAbilities.js#resolveFoeAbility
      via: "the ability-gate check passed through to foeCast"
      pattern: "resolveFoeAbility\\("
  prohibitions:
    - statement: "MUST NOT change any rule, odds, draw count or draw order: each converted gate draws its die under exactly the old short-circuit condition, and flee's fold of the bonus into the threshold is arithmetic only"
      category: integrity
      status: resolved
      verification: judgment
---

<objective>
Finish engine/combat.js: flee (already high, bonus folded into the threshold), parley, the startCombat and fight() gates, the kill drops, and the foe ability gate. Tag every remaining draw in combat.js and foeAbilities.js and bring both files under the guard.

Purpose: CONTEXT Area 1: flee and initiative stay high ("today's d20 + bonus >= 14 becomes roll >= 14 − bonus... the line reads '12 vs 11–20'"); rolls with no obvious roller are read from the hero's side ("Drops land on the top faces"); the per-site verdicts are rows 1, 3, 3b, 4-6, 12-14, 17-19 and 25. CONTEXT Area 2: names that say "roll under" are rewritten (BAG_DROP_UNDER -> BAG_DROP_FACES, a content export that is not persisted).

Output: the converted sites, their event fields, the flee and parley lines and chains, the FLEE.md payload, the renamed bag constant, the tags, and the guard enforcing combat.js and foeAbilities.js.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 73-08 creates or changes.)

- The helper and guard (73-01); baselines and the invariant (73-02); rollRange.js and the verdicts (73-03); the hero strike and foe soak (73-04); member/ally/thrown strikes and the isBestFace flip (73-05); resistance and magic mishaps (73-06); foe swings and the hero soak (73-07)
- **This plan:** flee (`fleeRolled { roll, atLeast, dieN, mods }`), parley (`parleyRolled { roll, atLeast, dieN, fluency }`), the Con Artist, Court Mage, Hardiness, tier-bleed and foe-ability gates, the kill and bag drops (`lootDropped { …, roll, atLeast, dieN, bag? }`)
- **This plan:** content/bags.js `BAG_DROP_FACES`; docs/FLEE.md `## Event payload and narration (FLEE-02)` updated; engine/items.js `offerLoot` gains an optional roll-check argument
- **This plan:** every draw in combat.js and foeAbilities.js tagged or converted; both files enforced
- Traps, locks, climbs, cures and wake, ALL_ENFORCED and COMPLETE (73-09); the proofs and docs (73-10)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/73-engine-roll-high-mirror/73-CONTEXT.md
@.planning/phases/73-engine-roll-high-mirror/73-07-SUMMARY.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/combat.js at d274925 (key by function; lines drift):
- resolveInitiative (~L185-L186): `mine = rng.d(20)`, `theirs = rng.d(20)`; `mine >= theirs` (the hero wins ties). Already high: tag both lines; combatJoined keeps mine/theirs.
- startCombat: foe count `foeCountFor(rng.d(4), () => rng.d(4))` (~L251, selection: tag); tier bleed `clamp(maxLvl - (rng.d(4) <= tierSpreadFor() ? 1 : 0), 1, 5)` (~L254); the Knight/Con Artist/Court Mage else-if chain (~L362-L380): `else if (c.sub === "Con Artist" && f.lvl <= 1 && rng.d(6) <= 4) { … foeFled {name, reason: "conArtist"} }` then `else if (c.sub === "Court Mage" && rng.d(12) <= 2) { … foeBored {name} … }`.
- fight() phobia (~L481-L491): `if ((c.phobiaType === type || … || armed) && !(skill(c, "Hardiness") && rng.d(2) === 1)) { … phobiaAfraid {rounds, trigger?} }`. The d2 is drawn only when the phobia condition holds AND the hero has Hardiness.
- killFoe (~L866-L925): XP d6 (amount), coin d10 (amount), `if (rng.d(20) <= 2 + f.lvl) { drop = rollTreasureItem(…); tier = bagUpgradeTier(state); if (tier && rng.d(20) <= BAG_DROP_UNDER) drop = bagItemFor(tier); offerLoot(state, drop, events) }`, cooking `else if (rng.d(6) >= 4)` (already high).
- flee (~L1128-L1139): `const { need, mods, bonus } = fleeBreakdown(c); const roll = rng.d(20); const total = roll + bonus; events.push({ type: "fleeRolled", roll, mods, total, need }); if (total >= need) …`.
- parley (~L1279-L1310): `bonus = …`; `roll = rng.d(20)`; `need = Math.min(9 + bonus, 17) - parleyNeedModFor()`; parleyRolled `{ roll, need, fluency: flu }`; `if (roll <= need) { … XP d6 (amount) …; if (C.type === "Humans" && rng.d(6) === 6) { wm = rng.d(6) * 100 * depth … } }` (payout already high, amount).
- foeTurn ability gate (~L2523-L2530): `if (ready && (neverMelee || rng.d(6) <= 4)) { if (resolveFoeAbility(state, f, ready, rng, events).died) return events; continue; }`. The d6 is drawn only when `ready && !neverMelee`.
- The other combat.js draws to tag (live grep): sing (lullaby n, thunder n and r: amount), regen amount (~L2424), summon/legacy damage adds, member ability amounts, allyCast status amounts, pickFoeTarget's pool pick (selection), and the damage fallbacks `rng.d(6)` in pursuit and both foeTurn branches (amount).
engine/foeAbilities.js: `resolveFoeAbility(state, f, a, rng, events)` (~L159) pushes foeCast `{ name, ability, kind, txt }` first; amounts at ~L173 (rollDice), L209 `rounds: rng.d(4)` (amount), L217/L237 rollDice.
engine/items.js#offerLoot (~L956): `offerLoot(state, it, events = [])` pushes `lootDropped { name, kind }`; other callers pass three arguments.
content/bags.js L40-L43: `// BAG_DROP_UNDER — on a successful treasure drop … one extra d20 at or under this value swaps the rolled item for the bag` and `export const BAG_DROP_UNDER = 3;`. References: engine/combat.js, test/unit/bag-cap-gate.test.js, test/unit/loot-pile.test.js, docs/ROLL-LEDGER.md (historical mentions stay).
UI: eventNarration fleeRolled (~L450, "Flee: rolled 8 (Thief +5, Mail −1) — 12 against 14.") and parleyRolled (~L476, "Talk it down: 12 vs 13 (+2 for the tongue)."); narrationLines LINE_FOR.fleeRolled (~L1188, "Flee: 8 (Thief +5) = 12 vs 14"), LINE_FOR.parleyRolled (~L1209), fleeChain (~L741, reuses LINE_FOR.fleeRolled's text), parleyChain (~L767-L783, appends `(${e.roll} vs ${e.need})`). Target lines: Oracle `Flee: rolled <span class="roll">8</span> vs 10–20 (Thief +5, Mail −1).`; rail `Flee: 8 vs 10–20 (Thief +5, Mail −1)`; Oracle `Talk it down: <span class="roll">12</span> vs 8–20 (+2 for the tongue).`; the parley chain appends `(12 vs 8–20)` via rollVsText.
docs/FLEE.md `## Event payload and narration (FLEE-02)` (~L107-L130) documents the old payload and the old lines; test/unit/flee-ledger.test.js pins its headings (keep them).

Proof gates and test-update rules: exactly as in 73-04-PLAN.md `<interfaces>`. For flee, an asserted `total`/`need` pair becomes `atLeast = need - bonus` with `dieN: 20`; the roll is NOT mirrored (flee is already high).
</interfaces>
</context>

<!-- planner-discipline-allow: BAG_DROP_UNDER -->

<tasks>

<task type="auto">
  <name>Task 1: Flee and parley, their events, lines and chains</name>
  <files>engine/combat.js, src/browser/eventNarration.js, src/browser/narrationLines.js, docs/FLEE.md, test/unit/flee-retune.test.js, test/unit/parley.test.js, test/unit/linesForAction.test.js</files>
  <read_first>
    - engine/combat.js (flee ~L1068-L1153, parley ~L1234-L1320)
    - engine/derived.js (fleeBreakdown: it keeps returning { need, mods, bonus })
    - src/browser/eventNarration.js (fleeRolled, parleyRolled) and src/browser/narrationLines.js (LINE_FOR.fleeRolled, LINE_FOR.parleyRolled, fleeChain, parleyChain)
    - docs/FLEE.md (the Event payload section) and test/unit/flee-ledger.test.js
    - test/unit/flee-retune.test.js (~L120-L305) and test/unit/parley.test.js (~L465-L530, L675-L685)
  </read_first>
  <action>
Per verdict rows 17-19:
1. flee: keep `fleeBreakdown(c)`. Draw the raw d20 on a line tagged `// roll:already-high`; `atLeast = need - bonus`; push `{ type: "fleeRolled", roll, atLeast, dieN: 20, mods }`; success is `roll >= atLeast`. `total` and `need` are gone. Rewrite the JSDoc and comments to say flee is already roll-high with the modifiers folded into the threshold.
2. parley: `faces = Math.min(9 + bonus, 17) - parleyNeedModFor()`; `check = rollCheck(rng, 20, atLeastFor(faces, 20))` at the old draw position; push `{ type: "parleyRolled", ...rollFields(check), fluency: flu }`; success is `check.ok`. Tag the XP d6 (amount), the Humans payout gate (already-high) and the payout amount line (amount). Rewrite the Phase 72 F5 comment to the faces reading (a positive dial removes faces).
3. UI: eventNarration fleeRolled and parleyRolled, LINE_FOR.fleeRolled and LINE_FOR.parleyRolled, and parleyChain move to the target lines in `<interfaces>`, via rangeText and rollVsText. fleeChain keeps reusing LINE_FOR.fleeRolled's text.
4. docs/FLEE.md: update the payload code line and the three example lines in `## Event payload and narration (FLEE-02)` to the new shape, adding one sentence: Phase 73 (ROLL-05) folds the bonus into the threshold; the roll is the raw d20, already high. Keep every heading.
5. Update the flee and parley assertions per the test-update rules and `<interfaces>` (flee rolls are not mirrored; parley rolls are: 21 − old).
  </action>
  <verify>
    <automated>node --test test/unit/flee-retune.test.js test/unit/parley.test.js test/unit/linesForAction.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type: "fleeRolled", roll, atLeast, dieN: 20, mods' engine/combat.js` equals 1.
    - `grep -c "rollCheck(rng, 20, atLeastFor(faces, 20))" engine/combat.js` is at least 1.
    - test/unit/flee-retune.test.js asserts the exact new Oracle line for `{ type: "fleeRolled", roll: 8, atLeast: 10, dieN: 20, mods: [Thief +5, Mail −1] }` (`Flee: rolled <span class="roll">8</span> vs 10–20 (Thief +5, Mail −1).`) and the matching rail line, and passes.
    - `[flee:*]` and `[parley:*]` rows in rollDirection-checks.test.js pass unchanged; the state pins pass.
  </acceptance_criteria>
  <done>Flee and parley read roll-high on screen and in the engine.</done>
</task>

<task type="auto">
  <name>Task 2: The combat gates, the kill drops and the foe ability gate</name>
  <files>engine/combat.js, engine/foeAbilities.js, engine/items.js, content/bags.js, test/unit/loot-pile.test.js, test/unit/bag-cap-gate.test.js, test/unit/combat.test.js, test/unit/foe-abilities.test.js, test/unit/phobia-triggers.test.js, test/unit/identity-combat.test.js</files>
  <read_first>
    - engine/combat.js (startCombat ~L240-L385, fight() ~L440-L495, killFoe ~L855-L925, foeTurn ~L2515-L2535)
    - engine/foeAbilities.js (resolveFoeAbility ~L150-L240)
    - engine/items.js (offerLoot ~L950-L975)
    - content/bags.js (L35-L50) and its two tests
    - docs/ROLL-LEDGER.md (verdict rows 3, 3b, 4-6, 12-14, 25)
  </read_first>
  <action>
Per verdict rows 3b, 4-6, 12, 13 and 25, drawing every die under exactly the old short-circuit condition (the order of draws must not move):
1. Tier bleed: `rollCheck(rng, 4, atLeastFor(tierSpreadFor(), 4)).ok ? 1 : 0`. No event.
2. Con Artist and Court Mage: keep the else-if chain's semantics. The die is drawn only when the preceding conditions hold; an assignment inside the condition, or an equivalent restructure, is fine. Con Artist `rollCheck(rng, 6, atLeastFor(4, 6))`; Court Mage `rollCheck(rng, 12, atLeastFor(2, 12))`. foeFled {reason: "conArtist"} and foeBored carry `...rollFields`.
3. Hardiness shrug: compute the phobia condition once (pure); draw `rollCheck(rng, 2, atLeastFor(1, 2))` only when it holds and the hero has Hardiness; the fear lands unless the shrug is `.ok`; phobiaAfraid carries the triple when the die was drawn.
4. Kill drops: the loot gate is `rollCheck(rng, 20, atLeastFor(2 + f.lvl, 20))`. Inside, the bag gate is `rollCheck(rng, 20, atLeastFor(BAG_DROP_FACES, 20))`, drawn only when `tier` is truthy. Call `offerLoot(state, drop, events, { ...rollFields(lootCheck), ...(bagCheck ? { bag: rollFields(bagCheck) } : {}) })`. engine/items.js offerLoot takes an optional fourth argument and spreads it onto lootDropped; its other callers are unchanged. Tag the XP d6 and coin d10 (amount) and cooking (already-high).
5. content/bags.js: rename BAG_DROP_UNDER to BAG_DROP_FACES (the same value, 3). Its comment reads: the number of top faces of the extra d20 that swap the drop for the bag (Phase 73 renamed it; the name used to describe the old bottom-of-the-die reading). Update engine/combat.js's import and the two tests. The content index re-exports it; check `grep -rn "BAG_DROP" content/ engine/ src/ test/ tools/`.
6. Foe ability gate: `gate = ready && !neverMelee ? rollCheck(rng, 6, atLeastFor(4, 6)) : null`; take the ability when `ready && (neverMelee || gate.ok)`; call `resolveFoeAbility(state, f, ready, rng, events, gate)`. resolveFoeAbility takes an optional sixth argument `gate = null` and spreads `rollFields(gate)` onto foeCast when present.
7. Update the gate, drop, bag and foeCast assertions per the test-update rules.
  </action>
  <verify>
    <automated>node --test test/unit/loot-pile.test.js test/unit/bag-cap-gate.test.js test/unit/combat.test.js test/unit/foe-abilities.test.js test/unit/phobia-triggers.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const BAG_DROP_FACES = 3" content/bags.js` equals 1.
    - `git grep -c "BAG_DROP_UNDER" -- content engine src test tools` prints nothing.
    - `grep -c "atLeastFor(2 + f.lvl, 20)" engine/combat.js` equals 1; `grep -c "atLeastFor(tierSpreadFor(), 4)" engine/combat.js` equals 1; `grep -c "rollCheck(rng, 6, atLeastFor(4, 6))" engine/combat.js` equals 2 (the Con Artist and the ability gate).
    - `[drop:foe-level]` and every combat direction row pass unchanged; the pins and the pre-switch save pass.
  </acceptance_criteria>
  <done>Every combat gate and drop reads from the right side of the die.</done>
</task>

<task type="auto">
  <name>Task 3: Tag the rest of combat.js and foeAbilities.js, enforce both, the invariant rows, and the proof gates</name>
  <files>engine/combat.js, engine/foeAbilities.js, test/unit/roll-high-guard.test.js, test/parity/roll-high-invariant.test.js</files>
  <read_first>
    - engine/combat.js (`grep -n "\.d(" engine/combat.js`)
    - engine/foeAbilities.js (`grep -n "\.d(" engine/foeAbilities.js`)
    - test/unit/roll-high-guard.test.js and test/parity/roll-high-invariant.test.js
  </read_first>
  <action>
1. Tag every remaining raw draw in engine/combat.js and engine/foeAbilities.js per `<interfaces>` (initiative already-high, the foe count and pickFoeTarget selection, every damage/count/duration amount).
2. Add engine/combat.js and engine/foeAbilities.js to ENFORCED with live DRAW_INVENTORY counts. The guard's shape, mirror and transitional rules must pass over combat.js: any hit means an unconverted site; fix the engine, never the guard.
3. test/parity/roll-high-invariant.test.js OUTCOME: fleeRolled (success unless a fleeFailed follows before the next fleeRolled in the same action), parleyRolled (success unless a parleyFailed follows before the next parleyRolled), foeFled (success, only when it carries atLeast), foeBored (success), phobiaAfraid (failure, only when it carries atLeast), lootDropped (success, only when it carries atLeast), foeCast (success, only when it carries atLeast). NESTED_CHECKS bag: success iff the event's kind is "bag".
4. Run the proof gates.
  </action>
  <verify>
    <automated>node --test test/unit/roll-high-guard.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"engine/combat.js"' test/unit/roll-high-guard.test.js` and `grep -c '"engine/foeAbilities.js"' test/unit/roll-high-guard.test.js` are each at least 1, and the guard passes.
    - `grep -cE "^\s+(fleeRolled|parleyRolled|foeFled|foeBored|phobiaAfraid|lootDropped|foeCast)\s*[:(]" test/parity/roll-high-invariant.test.js` is at least 7.
    - All proof gates in 73-04's `<interfaces>` hold.
  </acceptance_criteria>
  <done>engine/combat.js is fully roll-high and guarded.</done>
</task>

</tasks>

<verification>
- The proof gates hold; combat.js and foeAbilities.js are enforced; the direction tests, pins and pre-switch save are unchanged and green.
</verification>

<success_criteria>
ROADMAP criteria 1, 2, 3 (for combat.js), 4 (the BAG_DROP rename; no persisted field renamed) and 5 hold for flee, parley, the gates and the drops; CONTEXT's "Flee and initiative keep roll-high with their modifier conventions aligned" is met.
</success_criteria>

<output>
Create `.planning/phases/73-engine-roll-high-mirror/73-08-SUMMARY.md` when done. Include the event shapes, how each short-circuit was preserved, the combat.js and foeAbilities.js inventories, the test files touched, the test counts, and a "Human verification (deferred to end of run)" note: a Human Thief's flee in light armor reads "Flee: rolled N vs 9–20 (Thief +5)"; a parley reads "Talk it down: N vs lo–hi".
</output>
