---
phase: 72-roll-direction-sign-audit-fixes
plan: 07
type: execute
wave: 5
depends_on:
  - 72-01
  - 72-03
  - 72-06
files_modified:
  - engine/combat.js
  - engine/difficulty.js
  - content/skills.js
  - content/bestiary.js
  - test/unit/rollDirection.test.js
  - test/unit/rollDirection-checks.test.js
  - test/unit/parley.test.js
  - test/unit/roll-ledger-sync.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - docs/ROLL-LEDGER.md
  - docs/DIFFICULTY-RETUNE.md
autonomous: true
requirements:
  - ROLL-01
must_haves:
  truths:
    - "F5 is fixed: a positive PARLEY_NEED_MOD makes parley harder, as documented (it is subtracted from parley's roll-under need). It is 0 at identity, so zero fixtures move, and [parley:parley-need-mod] is green"
    - "Every ledger finding classed SIGN BUG (text-backed, local), and every finding the user ruled 'fix' or 'text' that is local, is applied with its own direction row and a measured fixture declaration. Any ruled fix that is not local is recorded as a follow-up for /gsd-plan-phase 72 --gaps, never dropped"
    - "docs/ROLL-LEDGER.md is final: every known fix and finding carries its outcome (FIXED with plan and commit, RULED text, DEFERRED, OUT OF SCOPE), the fixture outcome of each fix links its FIXTURE-INVENTORY section, and the Phase 74 and Phase 79 handoffs are complete"
    - "Every ledger modifier id (verdict not N/A) has a direction-test row with the same [site:source] id and vice versa; test/unit/roll-ledger-sync.test.js pins this, and neither direction test file keeps a node:test todo option"
    - "The direction tests assert on ODDS (the fraction of die faces that succeed for the roller), never on raw need arithmetic, so Phase 73 can run them unchanged. Both files pass on the final engine"
    - "An AFTER bot readout (node tools/tune-difficulty.mjs --seeds=200) on the final engine is recorded verbatim beside the BEFORE readout, with a BEFORE → AFTER reading and notes on the evasion and parley-dial sign changes against the historical Phase 54 dial table"
    - "The full suite is green, the prototype master hash is unchanged, and only fixtures declared in FIXTURE-INVENTORY.md's Phase 72 H2 moved during the phase"
  artifacts:
    - path: test/unit/roll-ledger-sync.test.js
      provides: "a standing guard that ledger ids equal direction-test ids, with no pending rows"
    - path: docs/ROLL-LEDGER.md
      provides: "the final audited ledger"
      contains: "FIXED"
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 72 AFTER readout"
      contains: "### AFTER — commit"
  key_links:
    - from: test/unit/roll-ledger-sync.test.js
      to: docs/ROLL-LEDGER.md
      via: "the [site:source] ids parsed from the Modifier ledger"
      pattern: "ROLL-LEDGER.md"
    - from: engine/combat.js#parley
      to: engine/difficulty.js#parleyNeedModFor
      via: "the subtracted dial term"
      pattern: "- parleyNeedModFor\\(\\)"
  prohibitions:
    - statement: "MUST NOT apply a finding the user ruled 'defer', or any NEEDS-RULING finding that has no ruling"
      status: resolved
      verification: judgment
    - statement: "MUST NOT change difficulty without a declaration: every applied fix is measured, and the AFTER readout records the net effect"
      status: resolved
      verification: judgment
---

<objective>
Close Phase 72. Apply the text-backed local findings and the user's rulings from the 72-01 checkpoint (F5, the parley dial's latent inversion, is always one of them). Take the AFTER bot readout. Finalize the ledger. Add a standing guard that keeps the ledger and the direction tests in sync.

Purpose: this finishes ROLL-01. Success criterion 1's "each bonus widens / each penalty narrows" holds with no pending rows, success criterion 5 is honoured for any late fix, and the engine gate's AFTER readout is on record. Phase 73 then builds on a verified-correct, fully pinned modifier set.

Output: the late fixes, test/unit/roll-ledger-sync.test.js, the final docs/ROLL-LEDGER.md, and the AFTER section in docs/DIFFICULTY-RETUNE.md.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 72-07 creates or changes.)

- `docs/ROLL-LEDGER.md`: **this plan** writes the final verdicts
- `docs/DIFFICULTY-RETUNE.md` Phase 72 H2: **this plan** writes `### AFTER — commit …` and `### Reading (BEFORE → AFTER)`
- `test/unit/harness/rollOdds.js`, `test/unit/rollDirection.test.js` (72-02), `test/unit/rollDirection-checks.test.js` (72-03): **this plan** clears the last pending row and adds rows for any late ids
- **This plan:** `test/unit/roll-ledger-sync.test.js`
- **This plan:** F5, `engine/combat.js#parley` SUBTRACTS `parleyNeedModFor()`, and the `parleyNeedModFor` JSDoc states the direction; plus any other applied finding
- `classEvasionFor` subtracted; member-branch insult last; Smoke text (72-04). Frenzy (72-05). `isBestFace`, `shatterIfBest`, `foeShattered`, `sp.shatterOnBest` (72-06)
- `test/parity/FIXTURE-INVENTORY.md` Phase 72 H2 and the ROLL-01 guard (**this plan** appends `### Plan 07 — …`)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md
@docs/ROLL-LEDGER.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-01-SUMMARY.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-02-SUMMARY.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-03-SUMMARY.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-04-SUMMARY.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-05-SUMMARY.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-06-SUMMARY.md

<interfaces>
engine/combat.js#parley (~L1226-1231 at eee9e10): `const need = Math.min(9 + bonus, 17) + parleyNeedModFor();`, and success is `roll <= need`. This is the F5 site. engine/difficulty.js#parleyNeedModFor (~L679-685): its JSDoc says "Direction: up = harder".
The docs/DIFFICULTY-RETUNE.md historical Phase 54 dial table (~L4914-4916) documents `FLEE_NEED_MOD / PARLEY_NEED_MOD … harder`, and Thief `evasion` "first candidate … -1". Leave those rows as historical record; note the sign changes in the Phase 72 H2 instead.
test/unit/difficulty-retune-ledger.test.js pins `## v1.2 retune (Phase 27) — TUNE-05..07` as the last H2.
The direction test ids are the bracketed prefixes of test titles in test/unit/rollDirection.test.js and test/unit/rollDirection-checks.test.js. The harness self-test id `[harness:…]` is not a ledger modifier.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply F5 and every other applicable finding and ruling, each with a direction row and a measured declaration</name>
  <files>engine/combat.js, engine/difficulty.js, content/skills.js, content/bestiary.js, test/unit/rollDirection.test.js, test/unit/rollDirection-checks.test.js, test/unit/parley.test.js, test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js</files>
  <read_first>
    - docs/ROLL-LEDGER.md (`## New findings` and `## Rulings`; this is the work list)
    - engine/combat.js (parley ~L1173-1260), engine/difficulty.js (parleyNeedModFor ~L679-685)
    - test/unit/rollDirection-checks.test.js (the `[parley:parley-need-mod]` pending row), test/unit/parley.test.js (any PARLEY_NEED_MOD tests; grep for them)
    - for each other finding being applied: the engine and content lines the ledger's Evidence column cites
    - test/parity/FIXTURE-INVENTORY.md (the Phase 72 H2) and test/parity/divergence-records.test.js (the ROLL-01 test)
  </read_first>
  <behavior>
    - At identity (PARLEY_NEED_MOD 0), parley's success odds are unchanged. With PARLEY_NEED_MOD +1, parley wins on exactly one face fewer (harder, as documented).
    - Each other applied finding makes its claimed direction true, proven by a new or flipped odds row.
    - No NEEDS-RULING finding without a "fix"/"text" ruling is touched.
  </behavior>
  <action>
Build the work list from docs/ROLL-LEDGER.md. It is F5, plus every row whose Class is SIGN BUG (text-backed, local), plus every row whose `## Rulings` entry is "fix" or "text" and whose "Applied by" is 72-07. F4 is not on this list: 72-05 owns it.

F5 (always applied; CONTEXT: "New sign bugs found mid-audit: fix them in this phase when the text backs the fix and the change is local"):
- In engine/combat.js#parley, change the dial term so a positive PARLEY_NEED_MOD is SUBTRACTED from the capped need, i.e. `Math.min(9 + bonus, 17) - parleyNeedModFor()`. Rewrite the adjacent comment: "Phase 72 (ROLL-01, finding F5): parley succeeds on roll ≤ need, so the dial is subtracted — a positive value is harder, as documented; identity 0 is a structural no-op".
- Rewrite parleyNeedModFor's JSDoc in engine/difficulty.js: "subtracted from … parley's need … Direction: up = harder (fewer faces succeed)".
- Delete the todo option on `[parley:parley-need-mod]`.
- If test/unit/parley.test.js or another test pins the old behaviour at a non-zero dial value, update it to the documented direction with a one-line F5 rationale.

Every other item on the list: make the smallest local change that makes the text's claimed direction true (or, for a "text" ruling, the smallest content-text change that makes the text match the engine; content txt edits follow 72-04's pattern of pins, snapshot regeneration and the serialized-txt check). Add or flip the matching `[site:source]` odds row in the right direction test file. Each such row follows 72-02's mirror-proof rules: no need numbers, no roll-convention literals in state.

Budget valve: if the applicable items beyond F5 add up to more than three local edits, or any single item needs a new mechanic or a change across more than one function family, apply the local ones. Record the rest in the ledger's `## Rulings` "Applied by" column as "follow-up: /gsd-plan-phase 72 --gaps" with a one-line scope, and list them in the SUMMARY. Never drop one silently.

Measurement for every engine edit in this task: `node --test test/parity/*.test.js`, both scan diffs (`--strip-trailing-cr`), `git diff --stat <plan-base>..HEAD -- test/parity/fixtures/` and the master hash. Declare the result under the Phase 72 H2 of test/parity/FIXTURE-INVENTORY.md as `### Plan 07 — late findings (F5 and rulings): measured …`. F5 is 0 at identity, so its predicted moved set is zero. If a ruled fix moves a fixture, follow the 72-05 pattern: append `+72` to the phase, add a rationale paragraph, re-measure `after` from the scan line verbatim, regenerate only that holder, and extend `ROLL01_EXPECTED_HOLDERS`.
  </action>
  <verify>
    <automated>node --test test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/parley.test.js && node --test test/parity/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\- parleyNeedModFor()" engine/combat.js` equals 1, and `grep -cE "\+ parleyNeedModFor\(\)" engine/combat.js` equals 0.
    - `grep -c "harder" engine/difficulty.js` is at least 1 within parleyNeedModFor's JSDoc, and that JSDoc says "subtracted".
    - `[parley:parley-need-mod]` carries no todo option and passes.
    - Every applied finding has a direction row whose id appears in docs/ROLL-LEDGER.md; every unapplied finding carries "defer", "follow-up" or "OUT OF SCOPE" in the ledger.
    - `grep -c "^### Plan 07 — late findings" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `node --test test/parity/*.test.js` exits 0; both scan diffs are empty; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
  </acceptance_criteria>
  <done>F5 and every applicable finding are fixed, pinned by odds rows and measured. Nothing ruled "defer" was touched, and nothing is silently dropped.</done>
</task>

<task type="auto">
  <name>Task 2: The AFTER bot readout and the final ledger</name>
  <files>docs/DIFFICULTY-RETUNE.md, docs/ROLL-LEDGER.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md (the `## v2.1 roll-direction pass (Phase 72) — bot readouts` H2 that 72-01 wrote, with its BEFORE block; find it with Grep)
    - docs/ROLL-LEDGER.md (the whole file)
    - test/parity/FIXTURE-INVENTORY.md (the Phase 72 H2's Plan 04/05/06/07 section headings, for links)
    - the 72-01..72-06 SUMMARYs (commit shas, measured outcomes, pins moved)
  </read_first>
  <action>
AFTER readout. Record `git rev-parse HEAD` (the final Phase 72 engine). Run `node tools/tune-difficulty.mjs --seeds=200` in the background with an `EXIT=` sentinel, and poll with a bounded Monitor or until-loop, never a foreground sleep. Use the same parameters as the BEFORE run.

Then, inside the Phase 72 H2 of docs/DIFFICULTY-RETUNE.md (use Edit, never Write, and keep it before the v1.2 H2), append:
- `### AFTER — commit <sha> (all Phase 72 fixes)`, with the stdout verbatim in a fenced block.
- `### Reading (BEFORE → AFTER)`: a table of death-depth min/p50/p90/max and the action-count p50, plus every per-class or per-race line the tool prints, and a one-paragraph reading. The frenzy change (Fridgian heroes) is the expected mover; the evasion and parley dial flips are no-ops at identity; the insult reorder only touches party members with their own Smoke or Sidestep.
- `### Sign notes against the historical dial table`: the Phase 54 table's "first candidate Thief evasion −1" now reads +1 (a positive evasion is harder to hit), and `PARLEY_NEED_MOD`'s documented "harder" is now true (F5). Leave the historical rows unedited.

Final ledger (docs/ROLL-LEDGER.md):
- Set every modifier row's Verdict to its final state: known fixes → `FIXED (72-0N, <sha>)`; applied findings → `FIXED (72-07, <sha>)`; ruled text → `RULED: text (<sha>)`; deferred → `DEFERRED (backlog)`; follow-ups → `FOLLOW-UP (gaps)`.
- Add a `Fixture outcome` line under each known fix and each applied finding that links its FIXTURE-INVENTORY section and states the measured moved set (or zero).
- Move every id from `### Ids not yet in a direction test` into the main table once Task 3 has rows for them.
- Complete the `## Handoffs → Phase 74` and `## Handoffs → Phase 79` lists. Mark the Skeleton note's digit-free rewording (72-06) as done.
- Fill `## Bot readout` with the BEFORE and AFTER headline numbers and a pointer.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js && node -e "const t=require('fs').readFileSync('docs/DIFFICULTY-RETUNE.md','utf8').replace(/\r\n/g,'\n');const a=t.indexOf('## v2.1 roll-direction pass (Phase 72)');const b=t.indexOf('## v1.2 retune (Phase 27)');const s=t.slice(a,b);for(const h of ['### BEFORE — commit','### AFTER — commit','### Reading (BEFORE → AFTER)','### Sign notes']){if(a<0||b<a||!s.includes(h)){console.error('missing '+h);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok`, and the retune-ledger test passes (v1.2 is still the last H2).
    - `grep -c "BUG → 72-0" docs/ROLL-LEDGER.md` equals 0 (no known fix is left in the pre-fix state).
    - The count of rows whose Verdict starts with FIXED and names a 72-0N plan is at least 5 (the fixes (a)–(d) plus F5): `grep -cE "FIXED \(72-0[4-7]" docs/ROLL-LEDGER.md` is at least 5.
    - `grep -c "Fixture outcome" docs/ROLL-LEDGER.md` is at least 5.
  </acceptance_criteria>
  <done>The AFTER readout and the reading are recorded, and the ledger states every outcome.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The ledger↔test sync guard and the final phase gates</name>
  <files>test/unit/roll-ledger-sync.test.js, test/unit/rollDirection.test.js, test/unit/rollDirection-checks.test.js</files>
  <read_first>
    - docs/ROLL-LEDGER.md (`## Modifier ledger`)
    - test/unit/rollDirection.test.js and test/unit/rollDirection-checks.test.js (their title ids)
    - test/unit/difficulty-retune-ledger.test.js (its CRLF normalisation and section-parsing helpers, as a pattern)
  </read_first>
  <behavior>
    - Every `[site:source]` id in the ledger's `## Modifier ledger` table whose Verdict is not `N/A…` appears as a title prefix in one of the two direction test files.
    - Every direction-test title id except `harness:*` appears in the ledger's Modifier ledger.
    - Neither direction test file passes node:test's todo option anywhere.
  </behavior>
  <action>
Create test/unit/roll-ledger-sync.test.js. It reads docs/ROLL-LEDGER.md (normalize CRLF to LF first) and slices the `## Modifier ledger` section up to the next `## ` heading. For each table row, it collects the bracketed id when the row's Verdict cell does not start with `N/A`. It reads both direction test sources and collects the bracketed id at the start of each `test(` title string, skipping `harness:` ids. It asserts:
- (1) the ledger ids minus the test ids is empty, and the failure message lists the missing ids;
- (2) the test ids minus the ledger ids is empty;
- (3) neither test source passes node:test's pending option. Match an options-object key named for it inside a `test(` call, and read only the two direction files, so the guard's own source never self-trips.
Its header comment says it is a Phase 72 standing guard that keeps the ROLL-01 ledger and its odds contract in lockstep, and that Phase 73 must keep both green without editing the direction test bodies.

If (1) fails for an id that the ledger lists and 72-02/72-03 did not cover, add the missing odds row to the right direction test file under the mirror-proof rules. If no content row allows a mirror-proof probe, set that ledger row's Verdict to `N/A (no mirror-proof probe: <reason>)`. Never loosen the guard.

Final phase gates:
- `npm test` is green. In a worktree, only STATE.md's known pre-existing CRLF doc-ledger failures may appear, identical on the base commit.
- The master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
- `git diff --name-only <phase-base eee9e10>..HEAD -- test/parity/fixtures/` lists only holders declared in the Phase 72 H2.
- The Skeleton's old roll-under crit key is absent from engine, content and src (the acceptance grep below).
- The mirror-proof token gates from 72-02 and 72-03 still print 0 on both direction files.
  </action>
  <verify>
    <automated>node --test test/unit/roll-ledger-sync.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/roll-ledger-sync.test.js` exits 0.
    - `grep -cE "\{\s*todo|todo:" test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js` reports 0 for each file.
    - The 72-02 token gate prints 0 on both files: `grep -vE '^\s*(//|\*)' test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js | grep -cE '\.need\b|\.roll\b|\.dieN\b|toHit\(|foeToHitVs\(|foeToHitBreakdown\(|fleeBreakdown\(|afraidNeed\(|classNeed\(|memberToHit\(|strikeDie\(|foeDie\('`.
    - `npm test` exits 0 (full suite green).
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    - `git diff --name-only eee9e10..HEAD -- test/parity/fixtures/` lists only files named in a `#### Moved set — declared records` table under FIXTURE-INVENTORY.md's Phase 72 H2 (possibly none).
    - `git grep -n "critOn" -- engine content src` prints nothing.
  </acceptance_criteria>
  <done>The ledger and the odds contract are locked together by a standing test, no pending rows remain, and the phase gates are green.</done>
</task>

</tasks>

<verification>
- `npm test` is green; the retune-ledger and ledger-sync tests pass; there are no todo rows; the token gates print 0; the master hash is unchanged; only declared fixtures moved.
</verification>

<success_criteria>
All five ROADMAP Phase 72 success criteria hold. The ledger is audited and final, the insult order is right on both branches, the frenzy honours the dark cap, the Skeleton claim is wired and M&M's is removed, and every moved fixture is measured, declared and regenerated. The direction tests are the odds contract Phase 73 must run unchanged.
</success_criteria>

<output>
Create `.planning/phases/72-roll-direction-sign-audit-fixes/72-07-SUMMARY.md` when done. Include the applied findings, the follow-ups (if any) for `/gsd-plan-phase 72 --gaps`, the BEFORE → AFTER reading, the final fixture moved set across the phase, and a "Human verification (deferred to end of run)" section that rolls up the 72-04/05/06 device checks (Smoke text after an insult, the Fridgian frenzy in the dark, the Skeleton shatter) for the milestone's batched Pixel 7 checklist.
</output>
