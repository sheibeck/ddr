---
phase: 72-roll-direction-sign-audit-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/harness/rollOdds.js
  - test/unit/rollDirection.test.js
autonomous: true
requirements:
  - ROLL-01
must_haves:
  truths:
    - "The direction test asserts on ODDS: the count of the probed die's faces (draw values 1..N) for which the engine reports a success for the roller, divided by N, found by driving the real engine entry point once per face. It never reads a need, target, roll or dieN field, and never calls a need or die arithmetic helper, so Phase 73's roll-high mirror (which keeps every draw's outcome byte-identical) can run it unchanged"
    - "The direction test never writes a roll-convention number (need, target, AR, toHit, foeToHit, Weaken penalty, dodge or lock threshold) into state or content. Heroes come from newRun(seed, [], { force }), foes are copied from BESTIARY rows, gear is copied from WEAPONS/ARMORS rows, timed effects come from engine entry points or engine/effects.js#startEffect, and only flags, counts, levels and stats are set directly"
    - "Every combat modifier source in the catalogue has a row keyed [site:source] whose title carries that id, asserting a bonus raises and a penalty lowers the roller's odds (weakly), and at least one strict probe per source proves the direction is really exercised"
    - "A modifier whose effect is absorbed by a clamp (need already floored at 1, an untouchable need-0 foe, the dark cap at 2) is asserted by the direction test as non-worsening (never flagged as a sign bug), and a clamp never turns a bonus into a penalty"
    - "Identity-valued dials (e.g. Thief evasion = 0, FOE_ACCURACY = 0) are asserted to change nothing at identity, and their DIRECTION is asserted by probing a non-zero value inside the test"
    - "Where application order changes the outcome (the natural-1 overrides vs the parley insult), the test pins the order-dependent result: the insult is the LAST term in BOTH the hero branch and the party-member branch (Smoked + insulted → need 2 in both, i.e. exactly two foe-winning faces)"
    - "Rows for the four known bugs (member-branch insult order, Thief evasion sign, frenzy second swing, Skeleton shatter) are present and carry node:test's todo option naming the fixing plan, so they run RED today without failing the suite; 72-04/05/06 remove the option"
    - "Every harness probe fails loudly (throws) when the probed draw never happens or its die size varies between faces, so no row can pass vacuously"
  artifacts:
    - path: test/unit/harness/rollOdds.js
      provides: "probeRng, faceOdds, jointOdds, compareOdds, assertBonus, assertPenalty, assertSame, heroState, foeFrom, inCombat, withMember"
      exports: ["probeRng", "faceOdds", "jointOdds", "compareOdds", "assertBonus", "assertPenalty", "assertSame", "heroState", "foeFrom", "inCombat", "withMember"]
    - path: test/unit/rollDirection.test.js
      provides: "Combat to-hit, crit and shatter direction rows"
      contains: "[hero-strike:weapon-heavy]"
  key_links:
    - from: test/unit/rollDirection.test.js
      to: engine/combat.js
      via: "playerStrike / foeTurn / flee (pursuit) / alliesTurn / allyTurn driven with probeRng"
      pattern: "playerStrike\\(|foeTurn\\("
    - from: test/unit/rollDirection.test.js
      to: docs/ROLL-LEDGER.md
      via: "[site:source] ids in test titles"
      pattern: "\\[foe-vs-member:"
  prohibitions:
    - statement: "MUST NOT encode today's roll-under arithmetic in the direction test (no need/target numbers in assertions or state), because the test is the contract Phase 73 must pass unchanged"
      status: resolved
      verification: judgment
---

<objective>
Build the odds harness and the COMBAT half of the ROLL-01 direction test, `test/unit/rollDirection.test.js`. For every combat modifier source (hero to-hit, hero crit, foe to-hit against the hero, foe crit, foe to-hit against a party member, pursuit, member and ally strikes), compute the roller's success odds with and without the modifier and assert that the change goes the way the modifier's text or name claims.

Purpose: the direction test is the CONTEXT-locked form of the ledger ("a direction test plus a short ledger doc"). It is written in success ODDS, never raw need numbers, so Phase 73's roll-high mirror must be able to run it UNCHANGED. The four known bugs get RED rows marked with node:test's `todo` option. The fix plans turn them green.

Output: `test/unit/harness/rollOdds.js` (the shared harness, also used by 72-03) and `test/unit/rollDirection.test.js`.
</objective>

<assumption_delta_decision>
decision: no-change
noun: n/a
rationale: lexical false positive. "second" refers to Fridgian frenzy's second attack swing, not to a second identity, platform or source of truth.
</assumption_delta_decision>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 72-02 creates.)

- `docs/ROLL-LEDGER.md` (72-01; finalized by 72-07)
- `docs/DIFFICULTY-RETUNE.md` Phase 72 H2 (72-01 BEFORE, 72-07 AFTER)
- **This plan:** `test/unit/harness/rollOdds.js`, which exports `probeRng`, `faceOdds`, `jointOdds`, `compareOdds`, `assertBonus`, `assertPenalty`, `assertSame`, `heroState`, `foeFrom`, `inCombat`, `withMember`
- **This plan:** `test/unit/rollDirection.test.js`, the combat direction rows (pending rows flipped by 72-04/05/06)
- `test/unit/rollDirection-checks.test.js` (72-03)
- `test/unit/roll-ledger-sync.test.js` (72-07)
- `engine/dice.js#isBestFace`, `engine/combat.js#shatterIfBest`, event `foeShattered`, `sp.shatterOnBest` (72-06)
- `classEvasionFor` subtracted in `foeToHitVs`/`foeToHitBreakdown` (72-04)
- `test/parity/FIXTURE-INVENTORY.md` Phase 72 H2, and the `divergence-records.test.js` ROLL-01 guard (72-04, extended by 72-05/06/07)
- Smoke txt and smokeThrown narration, the Skeleton and M&M notes (72-04, 72-06)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md
@.planning/milestones/v1.3-phases/31-combat-start-gating-effect-hygiene/31-ROLL-DIRECTION-AUDIT.md

<interfaces>
Engine entry points the rows drive (all take `(state, rng, events)` unless noted; `state` is mutated in place, so clone per face):
- engine/combat.js: `playerStrike`, `foeTurn`, `flee`, `alliesTurn`, `allyTurn`, `startCombat(state, wandering, forcedType, rng, events)`, `pickFoeTarget` (not called directly), `liveFoes`, `killFoe`.
- engine/abilities.js: `useAbility(state, key, rng, events)` (the hero must own the ability id in `c.abilities`).
- engine/magic.js: `castSpell(state, idx, rng, events, now)`, where idx is the SPELLS index.
- engine/effects.js: `startEffect(c, id, { rounds })` sets up a timed effect record (for example id `ability:smoke`, `ability:sidestep`, `ability:battleRoar`, `ability:taunt`). engine/derived.js#abilityEffectActive reads `c.timers["ability:<key>"]` with phase "effect" and left > 0.
- engine/state.js: `newRun(seed, [], { force: { cls, sub, race } })` returns a real chargen GameState; `addPartyMember(state, member)`.
- engine/character.js: `rollCharacter(rng, [], force)` for a member sheet.
- engine/difficulty.js: `setDialsForTuning(overrides)` returns a restore function (it restores to the SHIPPED DIALS). `DIALS` is the shipped value set (FOE_ACCURACY 0, CLASS_MITIGATION.Thief.evasion 0).
- content: `BESTIARY` (content/bestiary.js), `WEAPONS` (content/weapons.js, each row has a `need` of −2..+1 and `crit` 1|2), `ARMORS`, `SPELLS`.

Site facts the probes rely on:
- playerStrike: with no frenzy (non-Fridgian), no haste, no Barbarian or Ambidextrous, and no Philly-slow target, draw 0 is the strike die. Outcome signals: `struck` or `foeArmorSoaked` = the hit landed; `strikeMissed` = a miss. `struck.critical` is the crit outcome. For a Fridgian, draw 0 is the frenzy d8 (a draw value of 1 triggers frenzy), draw 1 is swing 1 and draw 2 is swing 2 when swing 1 missed (a missed swing draws no damage dice). playerStrike ends with afterPlayerAction, which may run foeTurn; the fill value keeps drawing, so read only the first strike event(s). Cat Burglar and Ninja auto-hit their first strike unless `C.opened` is already true.
- foeTurn hero branch: for a plain foe (no `abilities`, no dot/acid, awake) with no live party member and no hero regen, draw 0 is the foe's to-hit die. Outcome: a `foeMissed` event for that swing = a miss; any other outcome = a landed swing.
- foeTurn member branch: `pickFoeTarget` returns the member with ZERO draws when that member's own sheet has a live `ability:taunt` timer. Use this to target the member without depending on a pick-draw value. Then draw 0 is the member swing's to-hit die. Outcome: `foeMissed` carrying a `member` field = a miss; `memberStruck` = a hit (`critical` on it is the crit outcome).
- pursuitStrike runs inside `flee` on a successful flee against a live `sp.pursues` foe (Spectre, content). Draw 0 is flee's d20; a draw value that succeeds today succeeds after Phase 73 too (the mirror preserves every draw's outcome). The next draw is the pursuer's to-hit die.
- memberStrike and allyCast run from `alliesTurn` for a classed member; the legacy strike needs a `C.allies` entry without a sheet; `allyTurn` needs `C.ally` (a summon) with `lvl` and `rounds`.
- The Fridgian frenzy need is at engine/combat.js ~L611 (the canon hard-set that 72-05 replaces). Skeleton and M&M are in content/bestiary.js at ~L151 and ~L124.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The odds harness and the hero-side rows (hero-strike, hero-crit)</name>
  <files>test/unit/harness/rollOdds.js, test/unit/rollDirection.test.js</files>
  <read_first>
    - engine/combat.js (playerStrike ~L546-811, killFoe ~L821-890, afterPlayerAction ~L1415)
    - engine/derived.js (toHit ~L1125, afraidNeed ~L1149, strikeDie ~L766, weaponCrit ~L916, noCritFor ~L1031; read them to understand the rules only, because the tests must not call them)
    - engine/state.js (newRun ~L146, the `force` option), engine/effects.js (startEffect ~L140), engine/abilities.js (useAbility ~L128 and the overheadBlow/kata/feint descriptor cases)
    - content/weapons.js, content/bestiary.js, content/classes.js, content/races.js, content/skills.js, content/abilities.js, content/flavor.js
    - test/unit/combat.test.js lines 1-130 (the fakeRng/fixedState helper patterns to mirror)
    - test/unit/harness/identityDials.js (a harness-module precedent)
  </read_first>
  <action>
Create `test/unit/harness/rollOdds.js`, an ES module and NOT a `*.test.js` file, exporting:
- `probeRng({ face, isProbe = (i) => i === 0, fill = () => 1, pick })` returns an rng with `d(sides)`, `pick(arr)` (default `arr[0]`), `shuffle(a)` (identity) and `getState()` (a constant). For draw index i: if `isProbe(i, sides)`, record `{ i, sides }` in `.probed` and return `face`, throwing if `face > sides`. Otherwise return `Math.min(sides, Math.max(1, fill(i, sides)))`. It never throws on running out of draws.
- `faceOdds(run, opts)` does the following. `run(rng)` clones its own base state, drives one engine entry point with that rng, and returns a boolean: did the ROLLER succeed. Run once with face 1 to learn N from the first probed draw's `sides`. Then run faces 1..N and return `{ wins, n }`. Throw when a run probed nothing or probed a different `sides`. This is the vacuous-row guard.
- `jointOdds(run, { isProbeA, isProbeB, fill })` enumerates two probed draw positions (for Philly's two dice and for initiative's two d20s) and returns `{ wins, total }`, with the same guards.
- `compareOdds(a, b)` returns the sign of a.wins/a.n − b.wins/b.n, computed by cross-multiplication with no floats. `assertBonus(withMod, without, { strict, label })`, `assertPenalty(...)` and `assertSame(...)` assert through compareOdds, with a message naming the row id.
- Mirror-proof builders:
  - `heroState({ cls, sub, race, seed = 1, level })` calls `newRun(seed, [], { force: { cls, sub, race } })`. It makes the party's cell lit (the tile dark flag false, darkFor 0) and sets `c.level` if given.
  - `foeFrom(type, tier, name)` copies a BESTIARY row exactly the way startCombat builds a foe: name, type, lvl = tier, size, intel, wp, maxWP, alive, asleep 0, `sp` = the row's own `sp` object, lives 2 when `sp.twice` else 1, and abilities when present. There is an optional `wp` override so a strike never kills during a probe.
  - `inCombat(state, foes, extra)` sets `state.combat = { foes, type, round: 1, target: 0, spellOpen: false, tracked: false, pending: false, ...extra }`.
  - `withMember(state, { cls, sub, race, seed })` adds a real rollCharacter sheet with addPartyMember and returns its index. The member rows sync `C.allies` by calling `startCombat` with a fill rng and then replacing `C.foes` and clearing `pending`.
- A header comment states the two contracts. (1) Odds only: success is decided by event types and outcome booleans, never by need, target, roll or dieN fields. (2) No roll-convention number is ever written into state or content: values come from content rows or engine entry points; flags, counts, levels and stats are set directly. It also states that Phase 73 must run these tests unchanged.

Create `test/unit/rollDirection.test.js`, which imports only the harness, engine action entry points, content tables and the dial hook. Each row is one `test()` whose title starts with its id in brackets, e.g. `[hero-strike:weapon-heavy] a heavy weapon narrows the hero's hit odds`, and whose body carries a comment quoting the claim text and its file:line. Each row asserts its precondition (the modifier is live, for example the timer is in the effect phase or the flag is set) before probing, and restores any dial it sets. Rows run under the shipped DIALS.

Hero-side row catalogue. Each line reads id — probe — claim, where bonus means the with-modifier odds are at least the without-modifier odds, and strict means at least one state shows a strict difference:
- `[hero-strike:class-fighter]`: Fighter vs Magic User, same race and a plain foe. Bonus, strict.
- `[hero-strike:elven]`: Elven MU vs Human MU ("hits on 5 whatever the class"). Bonus, strict.
- `[hero-strike:acrobat]`: Acrobat vs Pickpocket. Bonus, strict. `[hero-strike:cleric]`: Cleric vs a plain MU sub. Bonus, strict.
- `[hero-strike:inspired]`: a Bard's song, set up through the engine (`sing`) or with C.inspired as the engine sets it. Bonus, strict in the light. `OK (clamp)`, non-worsening, in the dark.
- `[hero-strike:weapon-light]` and `[hero-strike:weapon-heavy]`: pick a WEAPONS row whose need is +1 and one whose need is −2 or −1, and compare each with a need-0 weapon. Bonus strict and penalty strict. Add a comment that this is the device trigger: "−2 to hit" is really worse.
- `[hero-strike:dazed]`: `c.foeEffect` kind "dazed" with rounds >= 1. Penalty, strict.
- `[hero-strike:dark-cap]`: a dark tile. Penalty, strict for a Fighter. `[hero-strike:night-vision]` and `[hero-strike:senses]`: in the dark, with the skill or `c.senses` vs without. Bonus, strict.
- `[hero-strike:dozing]` and `[hero-strike:stupid]`: `t.asleep` / `t.stupid` on an MU. Bonus, strict. On a Fighter it is `OK (clamp)`, non-worsening.
- `[hero-strike:hard-to-hit]`: a Zit copied from content, for a Fighter. Penalty, strict. For an MU it is a clamp and assertSame is allowed.
- `[hero-strike:fast]`: a Pogo copied from content. Penalty, strict.
- `[hero-strike:magic-only]`: a Ghost from content, with no magic weapon: wins === 0 (untouchable). With a magic weapon (`c.magicWpn` > 0, a count): wins > 0.
- `[hero-strike:overhead-blow]`: set up the Overhead Blow descriptor through useAbility (the hero owns it) or through the transient descriptor exactly as useAbility sets it. Penalty, strict.
- `[hero-strike:afraid]`: C.afraid rounds > 0. Penalty, strict. `[hero-strike:afraid-vs-untouchable]`: a Ghost without a magic weapon stays at wins === 0 when Afraid ("never revives an untouchable").
- `[hero-strike:floor-clamp]`: Overhead Blow plus Afraid on a need that is already floored: non-worsening and wins >= 1, so a penalty never goes below one face while the target is touchable.
- `[hero-strike:philly-slow]`: a Philly from content (keep the better of two dice). Use jointOdds over both dice. Bonus, strict.
- `[hero-strike:auto-hit]`: a Cat Burglar's first strike (C.opened false) wins on every face.
- `[hero-strike:level-die]`: a level-2 hero vs a level-1 hero of the same build (a smaller die). Bonus, strict. `[hero-strike:elven-strike-step]`: Bonus, strict. `[hero-strike:illusionist-d20]`: an Illusionist at level 2 vs a Wizard at level 2. Penalty, strict. `[hero-strike:acuteness]`: a live Potion of Acuteness effect set up through the engine's item path or startEffect with the carried item. Bonus, strict.
- Crit rows (success = `struck.critical`): `[hero-crit:precise-blade]` a crit-2 weapon vs a crit-1 weapon, bonus, strict. `[hero-crit:no-crit-sub]` a Guard/Soldier crits on zero faces. `[hero-crit:dark]` the dark without Night Vision crits on zero faces. `[hero-crit:stealth]` a Stealth opener vs no Stealth, bonus, strict. `[hero-crit:ninja]` a Ninja's non-opening strike, bonus, strict.

Use no raw need numbers anywhere. Where a row must compare two die sizes, compare fractions through compareOdds.
  </action>
  <verify>
    <automated>node --test test/unit/rollDirection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/rollDirection.test.js` exits 0.
    - `grep -c "^\s*test(\"\[hero-strike:" test/unit/rollDirection.test.js` is at least 24, and `grep -c "^\s*test(\"\[hero-crit:" test/unit/rollDirection.test.js` is at least 5 (or the equivalent count if titles use single quotes or template literals; every catalogue id appears as a bracketed title prefix).
    - Mirror-proof token gate: `grep -vE '^\s*(//|\*)' test/unit/rollDirection.test.js test/unit/harness/rollOdds.js | grep -cE '\.need\b|\.roll\b|\.dieN\b|\.target\b.*[<>]=?|toHit\(|foeToHitVs\(|foeToHitBreakdown\(|fleeBreakdown\(|afraidNeed\(|classNeed\(|memberToHit\(|strikeDie\(|foeDie\(|weaponNeedMod\(|weaponCrit\('` prints 0.
    - `grep -c "export function faceOdds\|export function jointOdds\|export function probeRng\|export function heroState\|export function foeFrom" test/unit/harness/rollOdds.js` equals 5.
    - The harness throws on a vacuous probe. The test file contains one self-test row, `[harness:vacuous-guard]`, asserting `faceOdds` throws when `run` never draws.
    - `git status --porcelain -- engine/ content/ src/` prints nothing.
  </acceptance_criteria>
  <done>The harness exists with vacuous-probe guards, and every hero-side combat modifier has a passing odds row keyed by its id.</done>
</task>

<task type="auto">
  <name>Task 2: The foe-side, member and ally rows, the ordering and identity rows, and the RED pending rows for the four known bugs</name>
  <files>test/unit/rollDirection.test.js</files>
  <read_first>
    - test/unit/rollDirection.test.js and test/unit/harness/rollOdds.js (from Task 1)
    - engine/combat.js (foeTurn ~L2270-2640 with both branches, pursuitStrike ~L927, flee ~L1008, pickFoeTarget ~L1987, alliesTurn ~L1508, memberStrike ~L1796, allyCast ~L1873, allyTurn ~L1455, the startCombat ally sync ~L337)
    - engine/derived.js (foeToHitVs ~L1217 and foeDie ~L1185, to understand the rules only)
    - engine/magic.js (the castSpell Weaken and Mirror Self branches, for setting those effects up through the engine), engine/items.js (useItem, for the invisibility and Anklet effects)
    - content/abilities.js L43-56, content/skills.js, content/spells.js L93-95, content/treasure-tables.js (Anklet of Invisibility, Crystal Staff), content/races.js (Elven, Dwarven, Fridgian), content/flavor.js L41/56
    - engine/difficulty.js (classEvasionFor ~L591, foeAccuracyFor ~L579)
  </read_first>
  <action>
Add these rows. The roller is the FOE unless noted, so a hero-defense source is a PENALTY to the roller.

Foe against the hero (foeTurn hero branch, a plain content foe such as Ned copied from content, draw 0 = the foe to-hit die, success = no foeMissed):
- `[foe-vs-hero:elven]`: Elven hero vs Human. Bonus to the foe, strict. Deliberate Phase 31: "easy to hit".
- `[foe-vs-hero:acrobat]`, `[foe-vs-hero:guard]`: penalty, strict.
- `[foe-vs-hero:gear-foe-to-hit]`: a live Anklet of Invisibility effect ("foes need two better to land"). Penalty, strict.
- `[foe-vs-hero:foe-accuracy]`: at the shipped identity, assertSame. With `setDialsForTuning({ FOE_ACCURACY: 1 })`, bonus, strict. Restore afterwards.
- `[foe-vs-hero:battle-roar]` (hero's own), `[foe-vs-hero:party-battle-roar]` (a member's own Battle Roar covers the hero), `[foe-vs-hero:sidestep]`: penalty, strict.
- `[foe-vs-hero:smoke]`, `[foe-vs-hero:mirror-self]` (cast through castSpell or with `c.mirror` set to a count), `[foe-vs-hero:invisibility]` (a live invis item effect), `[foe-vs-hero:blind]` (`f.blind` flag): each gives exactly wins === 1, the single best face.
- `[foe-vs-hero:weaken]`: cast Weaken with castSpell on a low-intel foe using a fill rng, then assert `C.foeToHitPenalty` is truthy (the effect is live, with no number compared), then probe a fresh foeTurn. Penalty, strict for a base-5 hero.
- `[foe-vs-hero:insult]`: `C.parleyInsulted = true`. Bonus to the foe, exactly one face more on the same die.
- Ordering rows (hero branch, passing today): `[foe-vs-hero:insult-after-smoke]`, `[foe-vs-hero:insult-after-mirror]`, `[foe-vs-hero:insult-after-invisibility]`, `[foe-vs-hero:insult-after-blind]`: override plus insulted gives exactly wins === 2.
- `[foe-vs-hero:dwarven-foe-strike-step]`: Dwarven vs Human with the same need ("foes strike at a better die"). Bonus to the foe, strict. `[foe-vs-hero:foe-level-die]`: a higher-tier foe of a same-need build. Bonus to the foe (a smaller die, floored at d8), non-worsening.
- `[foe-vs-hero:thief-evasion]` is a PENDING row, 72-04, because it is RED today. At identity, assertSame. With `setDialsForTuning({ CLASS_MITIGATION: { Thief: { ...DIALS.CLASS_MITIGATION.Thief, evasion: 1 } } })` against a Thief hero: penalty to the foe, strict (a positive evasion makes the Thief harder to hit). A non-Thief hero under the same dial: assertSame.

Foe crit against the hero (success = the landed swing is a crit; read `critical` on the resulting event, whether struck-by-foe or soaked, the way the engine reports it): `[foe-crit-vs-hero:soldier]`: a Soldier hero vs a Knight. Bonus to the foe crit odds, strict ("a BAD trait").

Foe against a member (the member branch; the member's own live `ability:taunt` timer forces the target with zero draws; success = `memberStruck`):
- `[foe-vs-member:battle-roar]` (the member's own or the hero's, party-wide): penalty, strict.
- `[foe-vs-member:member-sidestep]`: penalty, strict. `[foe-vs-member:member-smoke]`: wins === 1. `[foe-vs-member:blind]`: wins === 1. `[foe-vs-member:weaken]`: penalty, strict. `[foe-vs-member:insult]`: exactly one face more.
- `[foe-vs-member:foe-accuracy]`: identity assertSame, then a non-zero probe is a bonus, strict.
- `[foe-vs-member:hero-only-terms]`: the HERO's own Sidestep or Smoke timer leaves the member's odds unchanged (assertSame). This is scope, per foeToHitVs's `vs` rule.
- `[foe-vs-member:insult-after-member-smoke]` is a PENDING row, 72-04, RED today. The member's own Smoke plus insulted gives exactly wins === 2, the same as the hero branch. Name the paired hero row in a comment. This row and `[foe-vs-hero:insult-after-smoke]` together are the ordering pin.
`[foe-crit-vs-member:natural-best]`: the member crit window is exactly one face (wins === 1 on a landed-swing crit enumeration).

Pursuit (the flee success exit against a content Spectre): `[pursuit:blind]` wins === 1, `[pursuit:weaken]` penalty strict, `[pursuit:insult]` exactly one face more, `[pursuit:insult-after-blind]` exactly 2.

Member and ally strikes (roller = the member or ally, success = `allyStruck` or the equivalent hit event):
- `[member-strike:class-fighter]` (a Fighter member vs an MU member), `[member-strike:elven]`, `[member-strike:acrobat]`, `[member-strike:cleric]`: bonus, strict. `[member-strike:level-die]`: bonus, strict.
- `[ally-strike:ally-level-die]` (a summon `C.ally` at a higher level): bonus, non-worsening.

Frenzy (PENDING, 72-05, RED today):
- `[hero-strike:frenzy-second-swing]`: a Fridgian Fighter in the lit. Draw 0 is the frenzy trigger value 1, draw 1 is a swing-1 draw value that misses (confirm it with the event), and draw 2 is the probe. The swing-2 wins equal the normal swing's wins minus exactly 1 on the same die, and they are never below 1.
- `[hero-strike:frenzy-dark-cap]`: the same in the dark. The swing-2 odds are at or below the dark-capped normal swing's odds, never above them.

Skeleton shatter (PENDING, 72-06, RED today; take the Skeleton from BESTIARY by name, wp from content, twice-lived). Success = the Skeleton is no longer alive after exactly one strike event, in that one call:
- `[shatter:hero-strike]` wins === 1 (only the best face).
- `[shatter:member-strike]` wins === 1.
- `[shatter:ally-strike]` wins === 1.
- `[shatter:thrown]` (the hero's thrown attack spell through castSpell; an MU with the spell and charges) wins === 1.
- `[shatter:ally-thrown]` wins === 1.
Choose wp and the other fills so an ordinary max-damage hit can NOT kill both lives in one call. That way wins counts only the shatter.

Each pending row passes node:test's `todo` option as a string naming the fixing plan (for example "fixed by 72-04"). The row body asserts the POST-FIX behaviour. The fixing plan deletes the option and nothing else.
  </action>
  <verify>
    <automated>node --test test/unit/rollDirection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/rollDirection.test.js` exits 0, with the pending rows reported as todo (at least 10 todo rows: thief-evasion, insult-after-member-smoke, frenzy-second-swing, frenzy-dark-cap and the five shatter rows, plus any the executor splits further).
    - Every id listed in Task 2's catalogue appears as a bracketed title prefix. `grep -cE '\[(foe-vs-hero|foe-crit-vs-hero|foe-vs-member|foe-crit-vs-member|pursuit|member-strike|ally-strike|shatter):' test/unit/rollDirection.test.js` is at least 40.
    - `grep -c "insult-after-smoke\]" test/unit/rollDirection.test.js` is at least 1, and `grep -c "insult-after-member-smoke\]" test/unit/rollDirection.test.js` is at least 1 (the ordering pin on both branches).
    - The mirror-proof token gate from Task 1 still prints 0 on both files, and additionally `grep -vE '^\s*(//|\*)' test/unit/rollDirection.test.js | grep -cE 'foeToHitPenalty\s*=|\bar\s*:\s*[0-9]|toHit\s*:\s*[0-9]'` prints 0 (no roll-convention number written into state).
    - Run the todo rows' bodies once without the option in a scratch copy and record in the SUMMARY that each FAILS on today's engine (RED confirmed) and every non-pending row PASSES. Do not commit the scratch copy.
    - `npm test` exits 0 (full suite green). In a worktree, any failure must be one of STATE.md's known pre-existing CRLF doc-ledger failures and identical on the base commit.
    - `git status --porcelain -- engine/ content/ src/` prints nothing.
  </acceptance_criteria>
  <done>The combat half of the direction test covers every combat modifier source in odds, and the four known bugs are pinned as RED pending rows for 72-04/05/06 to flip.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/rollDirection.test.js` exits 0; the pending rows show as todo, and the SUMMARY records them as RED on today's engine.
- The token gates print 0 (no need, roll or target reads, and no need-helper calls or roll-convention literals).
- `npm test` is green.
</verification>

<success_criteria>
Every combat modifier source has an odds-based direction row that Phase 73 can run unchanged, and the four known sign and order bugs are pinned RED for their fix plans.
</success_criteria>

<output>
Create `.planning/phases/72-roll-direction-sign-audit-fixes/72-02-SUMMARY.md` when done. List every row id with its verdict (pass or pending→plan), the RED confirmation for each pending row, any catalogue id dropped with the reason, and a "Human verification (deferred to end of run)" section that reads "none".
</output>
