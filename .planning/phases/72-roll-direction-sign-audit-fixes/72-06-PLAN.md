---
phase: 72-roll-direction-sign-audit-fixes
plan: 06
type: execute
wave: 4
depends_on:
  - 72-05
files_modified:
  - engine/dice.js
  - engine/combat.js
  - engine/magic.js
  - content/bestiary.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/skeleton-shatter.test.js
  - test/unit/rollDirection.test.js
  - test/unit/foe-conditions.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - test/determinism/foe-abilities.test.js
autonomous: true
requirements:
  - ROLL-01
must_haves:
  truths:
    - "Any to-hit roll aimed at a Skeleton that LANDS and shows its die's best face (a natural 1 in today's roll-under engine) destroys it outright, including its second life. This covers hero weapon strikes (ability strikes and auto-hit openers included), party-member strikes, summoned and legacy ally strikes, and hero and member thrown attack spells. A Con Artist's no-injury opening warning blow is the one exception"
    - "The shatter is wired through one engine field, sp.shatterOnBest, one helper, engine/combat.js#shatterIfBest, one best-face predicate, engine/dice.js#isBestFace(roll, dieN) (Phase 73 flips only that predicate), one new event type foeShattered with an EVENT_NARRATION entry and a narration-lines entry, and zero new rng draws (a shatter skips the damage roll and routes through killFoe)"
    - "M&M's dead 'criticals on a 1' claim is gone: its note reads 'deaf' and the roll-under crit key is removed. The Skeleton's note is true in both conventions ('your best roll shatters it'), and no engine, content or src file still names the old key"
    - "The five [shatter:*] direction rows show exactly one shattering face out of N for each striker, with no todo option"
    - "Parity is measured: no replay site meets a Skeleton or M&M, so zero fixtures move. Any determinism pin that meets a summoned Skeleton and moves is re-measured and declared, and a ROLL-01 guard asserts no replay site emits foeShattered"
  artifacts:
    - path: engine/dice.js
      provides: "isBestFace(roll, dieN)"
      exports: ["rollDice", "isBestFace"]
    - path: engine/combat.js
      provides: "shatterIfBest export and its call sites"
      contains: "export function shatterIfBest"
    - path: content/bestiary.js
      provides: "the Skeleton's shatterOnBest flag"
      contains: "shatterOnBest: true"
    - path: test/unit/skeleton-shatter.test.js
      provides: "shatter behaviour tests"
  key_links:
    - from: engine/combat.js#playerStrike, #memberStrike, #alliesTurn, #allyTurn, #allyCast
      to: engine/combat.js#shatterIfBest
      via: "a call after the to-hit lands, before the damage roll"
      pattern: "shatterIfBest\\("
    - from: engine/magic.js#castSpell (thrown)
      to: engine/combat.js#shatterIfBest
      via: "import from ./combat.js"
      pattern: "shatterIfBest"
    - from: src/browser/eventNarration.js
      to: engine event foeShattered
      via: "EVENT_NARRATION.foeShattered"
      pattern: "foeShattered"
  prohibitions:
    - statement: "MUST NOT leave a bestiary promise the engine does not keep: the Skeleton's shatter is wired, and M&M's crit clause and field are removed"
      status: resolved
      verification: judgment
---

<objective>
Land ROLL-01 fix (c): "Rolling max on your dice triggers the shatter" (the user, 2026-09-24). A Skeleton struck on its die's best face is destroyed outright, both lives. Wire the engine field, one helper, one event and one narration line. Drop M&M's dead claim. Then measure the parity footprint.

Purpose: ROADMAP success criterion 4. The bestiary's roll-under crit claim is either wired or removed, and CONTEXT rules wire it for the Skeleton and drop it for M&M. The scope of "your dice" is the planner's discretion under CONTEXT, recorded in docs/ROLL-LEDGER.md `## Skeleton shatter scope`.

Output: `isBestFace`, `shatterIfBest`, the `foeShattered` event with its narration, the content edits, `test/unit/skeleton-shatter.test.js`, green shatter rows, and the measured declaration.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 72-06 creates or changes.)

- `docs/ROLL-LEDGER.md` (72-01; 72-07 finalizes it), `docs/DIFFICULTY-RETUNE.md` Phase 72 H2 (72-01 and 72-07)
- `test/unit/harness/rollOdds.js`, `test/unit/rollDirection.test.js` (72-02), `test/unit/rollDirection-checks.test.js` (72-03), `test/unit/roll-ledger-sync.test.js` (72-07)
- `classEvasionFor` subtracted; member-branch insult last; Smoke text (72-04). Frenzy second swing = normal − 1 (72-05)
- **This plan:** `engine/dice.js#isBestFace(roll, dieN)`, which returns `roll === 1` today and becomes `roll === dieN` after Phase 73
- **This plan:** `engine/combat.js#shatterIfBest(state, t, roll, dieN, by, rng, events, extra = {})`, exported
- **This plan:** event type `foeShattered` `{ type, target, by, spell? }`, `EVENT_NARRATION.foeShattered`, and `LINE_FOR.foeShattered` (or an `ORACLE_ONLY` entry with a reason)
- **This plan:** the content field `sp.shatterOnBest: true` on the Skeleton (it replaces the roll-under crit key); M&M's key is removed and its note becomes "deaf"; the Skeleton's note becomes "kill it twice; your best roll shatters it"
- **This plan:** `test/unit/skeleton-shatter.test.js`; `### Plan 06 — …` in FIXTURE-INVENTORY.md; the ROLL-01 guard extended
- `parleyNeedModFor` subtracted in `parley` (72-07, F5)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md
@docs/ROLL-LEDGER.md

<interfaces>
content/bestiary.js at eee9e10:
- ~L124: `{ n: "M&M", sz: "S", i: 6, wp: 3, sp: { <roll-under crit key>: 1, note: "deaf; criticals on a 1" } }`
- ~L151: `{ n: "Skeleton", sz: "H", i: 2, wp: 6, sp: { twice: true, toHit: 4, <roll-under crit key>: 1, note: "kill it twice; a 1 shatters it" } }`
The Skeleton is also summoned by the Vampire's foe ability (engine/foeAbilities.js ~L180-203; the `sp` is taken by reference from the content row, and `lives` is 2 for `twice`).
Foes are built with `sp: picked.sp || {}` and `lives: picked.sp && picked.sp.twice ? 2 : 1` (combat.js startCombat ~L258-276). `state.combat` is nulled on every save load (engine/saveState.js), so no saved foe carries the old key.
killFoe (combat.js ~L821): `if (f.lives > 1) { f.lives--; f.wp = f.maxWP; events.push({ type: "foeRevived", … }); return }`, and otherwise the death, sp, coin, loot and cooking draws.
The striker sites (each is `hit = …` then a damage roll):
- playerStrike (~L602-803): the miss branch at `if (!hit) { … continue; }` (~L647-659); then `let dmg = weaponDamage(c, rng)` (~L661). `opening = !C.opened2` and `C.opened2 = true` are set AFTER the damage roll (~L685-686). The Con Artist opener bail (~L736-739) `continue`s with no damage.
- memberStrike (~L1796): `if (!auto && roll > need) { … return; }`, then `weaponDamage(view, rng)`. dieN = strikeDie(view).
- alliesTurn legacy (~L1522-1533): `if (roll <= 5) { d = … rng.d(6) … }`. dieN = STRIKE_DICE[clamp(ally.lvl,1,5)-1].
- allyTurn summon (~L1460-1475): `if (roll <= 5) { … }`, followed by the `--C.ally.rounds` countdown, which must still run after a shatter. dieN = STRIKE_DICE[C.ally.lvl-1].
- allyCast thrown (~L1884-1901): `if (roll - bonus <= need) { … rollDice(rng, sp.dmg) … }`. dieN is the local.
- engine/magic.js castSpell thrown (~L506-545): `if (roll - bonus <= target) { … rollDice … }` inside `for (const t of targets)`. dieN is the local. magic.js already imports liveFoes, killFoe and others from ./combat.js.
The combat.js header comment (~L25-35) lists the roll-under crit key among the flavor-only sp flags; update that list.
Narration coverage guards: test/unit/formatEventsCoverage.test.js (every engine `type:` literal needs an EVENT_NARRATION entry) and test/unit/narrationLinesCoverage.test.js (each type is in LINE_FOR or in ORACLE_ONLY with a reason). test/unit/foe-conditions.test.js has an engine-scan guard over foe-field assignments.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: isBestFace, shatterIfBest, the six call sites, the content edits and the narration</name>
  <files>engine/dice.js, engine/combat.js, engine/magic.js, content/bestiary.js, src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/skeleton-shatter.test.js, test/unit/rollDirection.test.js, test/unit/foe-conditions.test.js</files>
  <read_first>
    - engine/dice.js (the whole file)
    - engine/combat.js (header ~L1-60, playerStrike ~L546-811, killFoe ~L821-830, allyTurn ~L1455-1476, alliesTurn ~L1508-1535, memberStrike ~L1796-1856, allyCast ~L1873-1902)
    - engine/magic.js (imports ~L20-40, the castSpell thrown branch ~L470-560)
    - content/bestiary.js (~L120-155)
    - src/browser/eventNarration.js (the foeKilled and foeRevived entries, for voice), src/browser/narrationLines.js (ORACLE_ONLY ~L170 and the LINE_FOR shape ~L992)
    - test/unit/foe-conditions.test.js (~L265-380, the engine-scan coverage guard)
    - docs/ROLL-LEDGER.md (`## Skeleton shatter scope`)
    - test/unit/rollDirection.test.js (the five `[shatter:*]` pending rows) and test/unit/harness/rollOdds.js
  </read_first>
  <behavior>
    - isBestFace(1, 20) is true; isBestFace(2, 20) and isBestFace(20, 20) are false (today's roll-under engine).
    - A hero strike on a full-HP content Skeleton destroys it (alive false, a foeShattered event, then foeKilled, never foeRevived) on exactly one face of the strike die. Every other landing face damages it normally, and a normal killing blow still triggers foeRevived first (the kill-twice rule is intact).
    - The same one-face shatter holds for a member strike, a legacy ally strike, a summoned ally strike (whose rounds countdown still ticks), a hero thrown attack spell and a member thrown attack spell.
    - A Con Artist's opening warning blow on its best face does NOT shatter (no foeShattered, no damage), and the strike after it can.
    - A shatter draws no weapon-damage or spell-damage dice. The draws after the to-hit die are exactly killFoe's own.
    - A non-Skeleton foe (no flag) never emits foeShattered.
  </behavior>
  <action>
Per CONTEXT fix (c) and the ledger's `## Skeleton shatter scope`:

1. engine/dice.js: export `isBestFace(roll, dieN)`, a pure function that returns `roll === 1`. Its JSDoc: "the best face of an N-sided die for the ROLLER. Today's roll-under engine: a natural 1. Phase 73 (the roll-high mirror) redefines it as `roll === dieN`; `dieN` is taken now so that stays a one-line change. No rng."

2. engine/combat.js: export `shatterIfBest(state, t, roll, dieN, by, rng, events, extra = {})`. It returns false unless `t && t.alive && t.sp && t.sp.shatterOnBest && isBestFace(roll, dieN)`. Otherwise it pushes `{ type: "foeShattered", target: t.name, by, ...extra }`, sets `t.lives = 1` and `t.wp = 0`, calls `killFoe(state, t, rng, events)` and returns true. Its JSDoc cites Phase 72 ROLL-01 (c) and the scope. Import isBestFace from ./dice.js.

3. Call sites. Each calls shatterIfBest only once the to-hit has LANDED and BEFORE the damage roll, and skips the rest of that strike when it returns true.
   - playerStrike: right after the miss branch. Guard it with `!(c.sub === "Con Artist" && !C.opened2)` so the warning blow is excluded. `by` is "you". On a shatter, set `C.opened2 = true` (the opening strike happened) and `continue`.
   - memberStrike: `by` is ally.name; return on a shatter.
   - alliesTurn legacy: inside the landed branch, before the damage die; `by` is ally.name; continue on a shatter.
   - allyTurn: inside the landed branch, before the damage die; `by` is C.ally.name. Structure it so the `--C.ally.rounds` countdown still runs after a shatter.
   - allyCast thrown: inside the landed branch, before the damage roll; `by` is ally.name and `extra` is `{ spell: sp.n }`; return on a shatter.
   - engine/magic.js castSpell thrown: import shatterIfBest from ./combat.js; inside the landed branch, before the damage roll; `by` is "you" and `extra` is `{ spell: sp.n }`; `continue` to the next target on a shatter.

4. The combat.js header comment: remove the roll-under crit key from the flavor-only sp list and add `sp.shatterOnBest` (Phase 72, ROLL-01 (c)) to the mechanical list.

5. content/bestiary.js:
   - Skeleton: replace its roll-under crit key and its value `1` with `shatterOnBest: true`, and set its note to "kill it twice; your best roll shatters it". It has no digit, so it is true before and after the Phase 73 mirror; record it in the ledger's Phase 79 handoff as done.
   - M&M: delete that same key, and set its note to "deaf".
   - Add a one-line `// DELIBERATE RULES CHANGE (Phase 72, ROLL-01 (c), user ruling 2026-09-24): …` comment above each edited row. Do not name the old key in the comment.

6. Narration. `EVENT_NARRATION.foeShattered` in src/browser/eventNarration.js is a family-friendly deadpan line in the house voice, naming the striker (`by`, where "you" reads as "Your") and the target, and saying both lives went at once. For example: "Your best roll lands clean. The Skeleton comes apart, both lives at once, and nobody is sweeping up." Add a `LINE_FOR.foeShattered` in src/browser/narrationLines.js with the matching tone and the feature priority. Both must be safe on a bare payload (the coverage guards call them with `{ type }` only).

7. If test/unit/foe-conditions.test.js's engine-scan guard flags a foe-field assignment the helper adds (for example `lives`), add a NOT_A_CONDITION entry with the reason "kill-twice counter; the Skeleton shatter zeroes it (Phase 72)". Add no chip.

8. Tests: create test/unit/skeleton-shatter.test.js covering every behaviour bullet above. Use the harness (heroState, foeFrom("Walking Dead", 2, "Skeleton"), inCombat, faceOdds) and a draw-recording rng for the zero-extra-draws check. Delete the todo option on all five `[shatter:*]` rows in test/unit/rollDirection.test.js.
  </action>
  <verify>
    <automated>node --test test/unit/skeleton-shatter.test.js test/unit/rollDirection.test.js test/unit/formatEventsCoverage.test.js test/unit/narrationLinesCoverage.test.js test/unit/foe-conditions.test.js test/unit/combat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "critOn" engine/ content/ src/` prints nothing (exit 1).
    - `grep -c "shatterOnBest: true" content/bestiary.js` equals 1; `grep -c "export function isBestFace" engine/dice.js` equals 1; `grep -c "export function shatterIfBest" engine/combat.js` equals 1.
    - `grep -c "shatterIfBest(" engine/combat.js` is at least 6 (the definition plus five call sites), and `grep -c "shatterIfBest" engine/magic.js` is at least 2 (the import plus one call).
    - `grep -c "criticals on a 1" content/bestiary.js` equals 0, and `grep -c 'note: "deaf"' content/bestiary.js` equals 1.
    - `grep -c "foeShattered" src/browser/eventNarration.js src/browser/narrationLines.js` reports at least 1 in each file.
    - The five `[shatter:*]` rows in test/unit/rollDirection.test.js carry no todo option and pass.
    - The verify command exits 0.
  </acceptance_criteria>
  <done>The Skeleton shatters on its best face for every striker in scope, M&M's dead claim is gone, and the new event is narrated.</done>
</task>

<task type="auto">
  <name>Task 2: Measure and declare the shatter's parity footprint; extend the ROLL-01 guard</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js, test/determinism/foe-abilities.test.js</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 72 H2 with its Plan 04 and Plan 05 sections; the generated roster block at the top)
    - test/parity/divergence-records.test.js (the ROLL-01 (Phase 72) test, replaySiteEvents)
    - test/determinism/foe-abilities.test.js (its header rule on re-measuring pins, and the `walking-dead-t5` Vampire rows)
  </read_first>
  <action>
Predictor. The generated roster in FIXTURE-INVENTORY.md shows every replay site rolls only Bat/Rat, Shriek, Viper or Ned, and none dispatches a Vampire summon. The bestiary edit touches only the Skeleton and M&M rows. So the predicted parity moved set is EMPTY.

Measure:
- `node --test test/parity/*.test.js`: green.
- Both scan diffs (`--strip-trailing-cr`): empty.
- `node --test test/parity/fixture-inventory.test.js`: green (the roster is unchanged).
- `git diff --stat <plan-base>..HEAD -- test/parity/fixtures/ test/parity/harness/comparables.js test/parity/prototype-master.js.txt`: empty.
- The master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0.

Determinism pins. Run `npm test`. If a determinism or draw-count pin moved, it moved because a fight met a summoned Skeleton that rolled its best face; test/determinism/foe-abilities.test.js's Vampire rows are the only candidates. Re-measure it LIVE through that file's own helper and update it with a one-line "Phase 72 ROLL-01 (c): a summoned Skeleton shattered on its best face" rationale. Any other failure is a regression, so stop and report it.

Add `### Plan 06 — the Skeleton shatters on its best face (ROLL-01 (c)): measured` under the Phase 72 H2, with:
- the rule and the scope (copied from the ledger);
- the predictor;
- the live results;
- `#### Draw-count pins` (each re-measured pin old → new, or "none moved");
- `#### Byte-identical elsewhere`. The content key rename is not serialized in any fixture, and `state.combat` is nulled on load, so no save carries the old key.

Extend the ROLL-01 (Phase 72) test in test/parity/divergence-records.test.js with part (c): across every replay site, zero `foeShattered` events. Leave `ROLL01_EXPECTED_HOLDERS` unchanged (the parity moved set is empty).
  </action>
  <verify>
    <automated>node --test test/parity/*.test.js && node tools/initiative-fixture-scan.mjs | diff --strip-trailing-cr - tools/initiative-fixture-scan-output.txt && node tools/worn-fixture-scan.mjs | diff --strip-trailing-cr - tools/worn-fixture-scan-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Plan 06 — the Skeleton shatters on its best face" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `grep -c "foeShattered" test/parity/divergence-records.test.js` is at least 1.
    - `node --test test/parity/*.test.js` exits 0, and both scan diffs are empty.
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0, and `git diff --stat <plan-base>..HEAD -- test/parity/fixtures/ test/parity/harness/comparables.js` is empty.
    - `npm test` exits 0 (full suite green; in a worktree, only STATE.md's known pre-existing CRLF doc-ledger failures may appear, identical on the base commit).
  </acceptance_criteria>
  <done>The shatter's footprint is measured (zero parity moves predicted and confirmed), and any moved determinism pin is declared. A standing guard proves no replay site shatters anything.</done>
</task>

</tasks>

<verification>
- The critOn grep is empty; the helper, predicate and event exist; every shatter row passes; the coverage guards are green; parity and the scans are clean; `npm test` is green.
</verification>

<success_criteria>
ROADMAP Phase 72 success criterion 4 is met (the claim is wired for the Skeleton and removed for M&M), and success criterion 5 is honoured for this fix.
</success_criteria>

<output>
Create `.planning/phases/72-roll-direction-sign-audit-fixes/72-06-SUMMARY.md` when done. Include the six call sites, the narration line text, the measured results, any pin re-measured, and a "Human verification (deferred to end of run)" section: on the Pixel 7, find a Skeleton (Walking Dead, tier 2), land a best-face strike, and confirm the Oracle shatter line and that it does not come back.
</output>
