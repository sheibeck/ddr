---
phase: 72-roll-direction-sign-audit-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 72-01
  - 72-02
files_modified:
  - engine/derived.js
  - engine/difficulty.js
  - engine/combat.js
  - content/abilities.js
  - content/skills.js
  - docs/ABILITIES.md
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/class-mitigation.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/abilities-catalog.test.js
  - test/unit/parley.test.js
  - test/unit/fixtures/shell-snapshots/thief.hero.txt
  - test/unit/rollDirection.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
autonomous: true
requirements:
  - ROLL-01
must_haves:
  truths:
    - "Where application order changes the outcome (the natural-1 overrides vs the parley insult), the test pins the order-dependent result: the insult is the LAST term in BOTH the hero branch and the party-member branch (Smoked + insulted → need 2 in both)"
    - "In engine/combat.js#foeTurn's member branch the insulted +1 is applied after the member's own Sidestep and Smoke, so the member's Smoke no longer swallows the insult. The hero branch and pursuitStrike already apply it last and are unchanged in behaviour"
    - "A positive CLASS_MITIGATION.Thief.evasion SUBTRACTS from the foe's need in foeToHitVs and foeToHitBreakdown (the Thief is harder to hit), and foeToHitBreakdown's recorded delta equals foeToHitVs's effect"
    - "Identity-valued dials (e.g. Thief evasion = 0, FOE_ACCURACY = 0) are asserted to change nothing at identity, and their DIRECTION is asserted by probing a non-zero value inside the test"
    - "The Smoke description (content/abilities.js, content/skills.js, docs/ABILITIES.md) and both smokeThrown narration lines stay true under the insult: foes need a natural 1 to find you, or a 1–2 if you insulted them"
    - "Both fixes move ZERO parity fixtures (evasion is 0 at identity, and no replay site carries a party). That is measured, declared as a measured zero in test/parity/FIXTURE-INVENTORY.md, and pinned by a new ROLL-01 (Phase 72) guard in test/parity/divergence-records.test.js"
    - "The direction rows [foe-vs-hero:thief-evasion] and [foe-vs-member:insult-after-member-smoke] no longer carry node:test's todo option and pass"
  artifacts:
    - path: engine/derived.js
      provides: "foeToHitVs / foeToHitBreakdown subtract classEvasionFor"
      contains: "h -= classEvasionFor(c)"
    - path: engine/combat.js
      provides: "member-branch insult applied last"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "Phase 72 H2 plus the Plan 04 measured-zero section"
      contains: "## Phase 72: roll-direction sign fixes (ROLL-01)"
    - path: test/parity/divergence-records.test.js
      provides: "ROLL-01 (Phase 72) standing guard"
      contains: "ROLL-01 (Phase 72)"
  key_links:
    - from: engine/derived.js#foeToHitVs
      to: engine/difficulty.js#classEvasionFor
      via: "the subtracted evasion term (vs hero, Thief only)"
      pattern: "h -= classEvasionFor"
    - from: engine/combat.js#foeTurn member branch
      to: C.parleyInsulted
      via: "the insulted block placed after the member Smoke block"
      pattern: "name: \"insulted\""
  prohibitions:
    - statement: "MUST NOT leave a player-facing Smoke promise that the engine breaks after the fix (the text must state the insult exception)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT change any display or narration SIGN convention in this plan (needModsClause and the like are Phase 74's); only the Smoke wording the rules fix directly requires changes"
      status: resolved
      verification: judgment
---

<objective>
Land two of ROLL-01's known fixes, both on the foe's to-hit side: (d) flip the Thief `evasion` dial's sign, and (a) make the parley insult the LAST term on the party-member branch, as it already is on the hero branch. Then show that neither moves a parity fixture.

Purpose: ROADMAP success criterion 2 (the insult lands in the right order on both branches) and CONTEXT fix (d). The pending direction rows 72-02 pinned for these go green.

Output: the engine edits, the Smoke text and narration, updated unit tests, the new Phase 72 section in FIXTURE-INVENTORY.md and the new ROLL-01 guard in divergence-records.test.js.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 72-04 creates or changes.)

- `docs/ROLL-LEDGER.md` (72-01; 72-07 finalizes it)
- `docs/DIFFICULTY-RETUNE.md` Phase 72 H2 (72-01 BEFORE, 72-07 AFTER)
- `test/unit/harness/rollOdds.js`, `test/unit/rollDirection.test.js` (72-02), `test/unit/rollDirection-checks.test.js` (72-03), `test/unit/roll-ledger-sync.test.js` (72-07)
- **This plan:** the semantic change that `classEvasionFor(c)` is SUBTRACTED in `engine/derived.js#foeToHitVs` and `#foeToHitBreakdown`, and the `classEvasionFor` JSDoc direction is rewritten
- **This plan:** the member-branch insult reorder in `engine/combat.js#foeTurn`
- **This plan:** Smoke txt ("… (a 1–2 if you insulted them) …") in content/abilities.js, content/skills.js and docs/ABILITIES.md; `EVENT_NARRATION.smokeThrown` and `LINE_FOR.smokeThrown` reworded
- **This plan:** the `test/parity/FIXTURE-INVENTORY.md` H2 `## Phase 72: roll-direction sign fixes (ROLL-01) — measured per plan`, and the `test/parity/divergence-records.test.js` test `ROLL-01 (Phase 72): …` with an expected-holders constant `ROLL01_EXPECTED_HOLDERS`
- `engine/dice.js#isBestFace`, `engine/combat.js#shatterIfBest`, event `foeShattered`, `sp.shatterOnBest` (72-06)
- `parleyNeedModFor` subtracted in `parley` (72-07, F5)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/derived.js#foeToHitVs (~L1217-1238, at eee9e10). The evasion term today:
- the line after `h += foeAccuracyFor();` adds `classEvasionFor(c)` into `h` for `vs === "hero" && c.cls === "Thief"`.
engine/derived.js#foeToHitBreakdown (~L1256-1327): the matching block (~L1290-1297) computes `const evasion = classEvasionFor(c)`, adds it into `h`, and pushes `{ name: "evasion", delta: h - before }`.
engine/difficulty.js#classEvasionFor (~L583-595): its JSDoc currently says "Direction: negative = easier to dodge (the first candidate manual notch is -1…)".

engine/combat.js#foeTurn member branch (~L2422-2546). The order today:
1. `let mNeed = foeToHitVs(state, "member")` plus `mNeedMods` from `foeToHitBreakdown(state, "member")`
2. `if (f.blind)` → mNeed = 1
3. `if (C.foeToHitPenalty)` → min
4. `if (C.parleyInsulted)` → mNeed += 1, push `{ name: "insulted", … }`   ← move this
5. the member's own Sidestep (`abilityEffectActive(mSheet, "sidestep")`) → max(1, mNeed − 2)
6. the member's own Smoke (`abilityEffectActive(mSheet, "smoke")`) → mNeed = 1
7. `if (mRoll > mNeed)` → the miss branch
The hero branch (~L2548-2570) is: foeToHitVs(state) (which includes Sidestep, Smoke, Mirror and invis) → blind → penalty → insulted. That is already correct. pursuitStrike (~L927-956) has the same shape as the hero branch and is already correct.

Tests that pin evasion today:
- test/unit/class-mitigation.test.js ~L161-190: probes `evasion: -1`, expects foeToHitVs lowered by 1 and a breakdown `{ name: "evasion", delta: -1 }`; a Fighter is unaffected.
- test/unit/feedback-payload.test.js ~L204-230: loops `evasion of [-1, 0]` and asserts `m.delta === evasion`.
Smoke text pins: test/unit/abilities-catalog.test.js ~L139; test/unit/fixtures/shell-snapshots/thief.hero.txt ~L78 (regenerate with `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`); docs/ABILITIES.md ~L59.
Narration: src/browser/eventNarration.js `smokeThrown` (~L651, "Gone. For two rounds they need a natural 1 to find you."); src/browser/narrationLines.js `smokeThrown` (~L1361, "Gone. They need a natural 1 to find you.").
Parity: the prototype master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0. `test/parity/divergence-records.test.js` has `collectRecords()`, `RECORDS` and `replaySiteEvents(seed, actions)`, plus prior per-phase guards (DMG-02, JOIN-02, GRULE-01, STORE-02) to copy the shape from.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: (d) Thief evasion subtracts from the foe's need</name>
  <files>engine/derived.js, engine/difficulty.js, test/unit/class-mitigation.test.js, test/unit/feedback-payload.test.js, test/unit/rollDirection.test.js</files>
  <read_first>
    - engine/derived.js (foeToHitVs ~L1191-1238 and foeToHitBreakdown ~L1240-1327, including their JSDoc)
    - engine/difficulty.js (classEvasionFor ~L583-595, and the CLASS_MITIGATION comment ~L256-273)
    - test/unit/class-mitigation.test.js (~L150-200) and test/unit/feedback-payload.test.js (~L195-235)
    - test/unit/rollDirection.test.js (the `[foe-vs-hero:thief-evasion]` pending row)
    - .planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md (fix (d))
  </read_first>
  <behavior>
    - With `CLASS_MITIGATION.Thief.evasion: 1`, a Thief hero's foeToHitVs(state, "hero") is exactly one lower than at identity, and foeToHitBreakdown records `{ name: "evasion", delta: -1 }`, with its `need` equal to foeToHitVs's.
    - At identity (evasion 0), a Thief's foeToHitVs and its breakdown are unchanged, with no evasion entry.
    - `vs === "member"` and non-Thief heroes are unaffected by the dial.
    - The `[foe-vs-hero:thief-evasion]` odds row passes without the todo option.
  </behavior>
  <action>
Per CONTEXT fix (d) (user ruling 2026-09-24): in engine/derived.js#foeToHitVs, change the Thief evasion term from an add into a SUBTRACT, `h -= classEvasionFor(c)`. Keep the same guard (`vs === "hero" && c.cls === "Thief"`) and the same position, right after the FOE_ACCURACY term. In foeToHitBreakdown's matching block, subtract the same way (`h -= evasion`). The pushed delta stays `h - before`, which now records −evasion. Rewrite the comment above both sites: "CLASS_MITIGATION.Thief.evasion (vs hero only) is SUBTRACTED from the foe's need: a positive evasion makes the Thief harder to hit. Phase 72, ROLL-01 (d), user ruling 2026-09-24; identity 0 is a structural no-op".

In engine/difficulty.js, rewrite classEvasionFor's JSDoc. It is "subtracted from the foe's to-hit need… Identity: 0 (canon). Direction: up = the Thief is harder to hit (the foe lands on fewer faces); the first candidate manual notch is +1". Change nothing else in difficulty.js; DIALS stays evasion 0. Do NOT edit the historical Phase 54 dial table in docs/DIFFICULTY-RETUNE.md; 72-07 records the sign change in the Phase 72 H2.

Tests. In class-mitigation.test.js, change the probed dial value from −1 to +1 in the evasion test and its non-Thief sibling. Retitle: "Thief evasion +1 lowers foeToHitVs by 1 for vs hero only …". Keep the breakdown expectation `delta: -1`. In feedback-payload.test.js, iterate `evasion of [1, 0]` and assert the recorded delta equals −evasion. Delete the todo option on `[foe-vs-hero:thief-evasion]` in test/unit/rollDirection.test.js. Change nothing else in that row.
  </action>
  <verify>
    <automated>node --test test/unit/class-mitigation.test.js test/unit/feedback-payload.test.js test/unit/rollDirection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "h -= classEvasionFor(c)" engine/derived.js` equals 1 and `grep -c "h -= evasion" engine/derived.js` equals 1.
    - `grep -cE "h \+= (classEvasionFor|evasion)" engine/derived.js` equals 0.
    - `grep -c "harder to hit" engine/difficulty.js` is at least 1 in classEvasionFor's JSDoc.
    - `grep -n "thief-evasion" test/unit/rollDirection.test.js` shows that row with no todo option (the three words "fixed by 72-04" no longer appear on the evasion row).
    - `node --test test/unit/class-mitigation.test.js test/unit/feedback-payload.test.js test/unit/rollDirection.test.js` exits 0, and the evasion row is reported as a pass, not a todo.
  </acceptance_criteria>
  <done>A positive evasion now makes Thieves harder to hit. Identity is untouched, and the row is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: (a) The insult is the last term on the member branch; the Smoke text stays true</name>
  <files>engine/combat.js, content/abilities.js, content/skills.js, docs/ABILITIES.md, src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/abilities-catalog.test.js, test/unit/parley.test.js, test/unit/fixtures/shell-snapshots/thief.hero.txt, test/unit/rollDirection.test.js</files>
  <read_first>
    - engine/combat.js (foeTurn ~L2400-2570 for both branches, and pursuitStrike ~L927-960)
    - test/unit/parley.test.js (~L355-410, "Test 5", the member branch mNeed widening; and ~L673-700, the pursuit test)
    - content/abilities.js ~L56, content/skills.js ~L53, docs/ABILITIES.md ~L59, test/unit/abilities-catalog.test.js ~L139
    - src/browser/eventNarration.js (smokeThrown ~L651), src/browser/narrationLines.js (smokeThrown ~L1361)
    - test/unit/shell-tab-snapshots.test.js (its header, for the MZ_SNAPSHOT_UPDATE procedure)
    - .planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md (fix (a), the user's words)
  </read_first>
  <behavior>
    - A member with its own live Smoke, facing an insulted foe (C.parleyInsulted), is found on exactly two faces of the foe's die (it was one). A Smoked, un-insulted member is found on one face.
    - The member branch's needMods list ends with the `insulted` entry, after any `Sidestep`/`Smoke` entries.
    - The hero branch and pursuitStrike behave as before (the insult is already last there).
    - No rng draw is added, removed or reordered.
  </behavior>
  <action>
Per CONTEXT fix (a) (user ruling 2026-09-24): "a smoked hero is found on 1–2 … The bonus then is a literal +1 when insulted".

In engine/combat.js#foeTurn's member branch, MOVE the `if (C.parleyInsulted) { … }` block, unchanged, from its current spot (between the Weaken-penalty block and the member Sidestep block) to immediately after the member Smoke block, so it becomes the last need term before the `mRoll > mNeed` miss test. Rewrite its comment: "PARLEY-02 / D-06 / D-20 + Phase 72 ROLL-01 (a), user ruling 2026-09-24: the insult is the LAST need term on every foe swing (hero branch, member branch and pursuitStrike), so an override (Smoke / Mirror / invisible / blind) resets the need first and the insult then adds its one face on top. Post-draw arithmetic, zero draws". Add the same one-line "insult is the last term (Phase 72 ROLL-01 (a))" note to the existing insult comments in the hero branch and pursuitStrike, with no code change there.

Text, changed only as far as the rules fix requires (CONTEXT: "Update the Smoke description … only as far as needed to stay true"):
- content/abilities.js and content/skills.js Smoke `txt` → "gone: for two rounds foes need a natural 1 to find you (a 1–2 if you insulted them), and a flee during it just works". Make the docs/ABILITIES.md table row identical, and the test/unit/abilities-catalog.test.js pin identical.
- src/browser/eventNarration.js smokeThrown → "Gone. For two rounds they need a natural 1 to find you — a 1–2 if you insulted them." src/browser/narrationLines.js smokeThrown → "Gone. They need a natural 1 to find you (a 1–2 if you insulted them)."
- Keep the `${member}: ` prefix logic in both narration lines exactly as it is.
- Before editing, confirm that ability and skill `txt` is not serialized into state: grep engine/character.js and engine/saveState.js for where abilities and skills are stored. If it were, it would need the REWORDED_TXT_ITEMS-style carve-out in test/parity/harness/comparables.js. Record the finding in the SUMMARY. If a carve-out is needed, STOP and report it rather than touching comparables.js, because this plan does not own that file.
- Regenerate the shell snapshot with `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`, then run the suite without the variable. `git diff test/unit/fixtures/shell-snapshots/` must show ONLY the Smoke text line or lines changing.

Tests. In test/unit/parley.test.js, add "Phase 72 ROLL-01 (a): the member branch applies the insult after the member's own Smoke". Build a member with a live `ability:smoke` timer (startEffect) and a live `ability:taunt` timer (a zero-draw target force), a plain foe, and C.parleyInsulted. Assert the resulting member swing event's needMods has `insulted` LAST and `Smoke` before it. Also assert, through `faceOdds` from test/unit/harness/rollOdds.js, that the foe wins on exactly 2 faces. Delete the todo option on `[foe-vs-member:insult-after-member-smoke]` in test/unit/rollDirection.test.js.
  </action>
  <verify>
    <automated>node --test test/unit/parley.test.js test/unit/abilities-catalog.test.js test/unit/shell-tab-snapshots.test.js test/unit/rollDirection.test.js test/unit/feedback-payload.test.js test/unit/formatEventsCoverage.test.js test/unit/narrationLinesCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Member-branch order check prints `ok`: `node -e "const s=require('fs').readFileSync('engine/combat.js','utf8');const a=s.indexOf('const member = pickFoeTarget(');const b=s.indexOf('const dieN = foeDie(c, f);',a);const r=s.slice(a,b);const i=r.indexOf('name: \"insulted\"');const k=r.lastIndexOf('name: \"Smoke\"');if(a<0||b<0||i<0||k<0||i<k){console.error('order wrong',i,k);process.exit(1)}console.log('ok')"`.
    - `grep -c "a 1–2 if you insulted them" content/abilities.js content/skills.js docs/ABILITIES.md test/unit/abilities-catalog.test.js` reports 1 in each of the four files.
    - `grep -c "insulted them" src/browser/eventNarration.js src/browser/narrationLines.js` reports at least 1 in each file.
    - The `[foe-vs-member:insult-after-member-smoke]` row carries no todo option and passes.
    - `git diff --stat -- test/unit/fixtures/shell-snapshots/` lists only thief.hero.txt (or also any other snapshot that renders the Smoke text), and every changed line contains "insulted them".
    - The verify command exits 0.
  </acceptance_criteria>
  <done>A member's own Smoke no longer swallows the insult. Both branches apply it last, and every Smoke text is true.</done>
</task>

<task type="auto">
  <name>Task 3: Measure and declare zero fixture moves; open the Phase 72 inventory section and the ROLL-01 guard</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the tail sections, Phase 61 `### Plan 01 … measured zero` and Phase 65, as the shape to copy; use Grep to find `^## Phase 6`, then read from there to EOF)
    - test/parity/divergence-records.test.js (collectRecords ~L61, replaySiteEvents ~L237, and the GRULE-01 and STORE-02 tests ~L487-620 as the shape to copy)
    - tools/initiative-fixture-scan.mjs (its header, for how to run it and diff it)
  </read_first>
  <action>
Measure; do not assume. On the Task 1+2 engine, run each of these and keep its output for the section below:
1. `node --test test/parity/*.test.js`: every test passes.
2. `node tools/initiative-fixture-scan.mjs | diff --strip-trailing-cr - tools/initiative-fixture-scan-output.txt`: empty.
3. `node tools/worn-fixture-scan.mjs | diff --strip-trailing-cr - tools/worn-fixture-scan-output.txt`: empty.
4. `git diff --stat -- test/parity/fixtures/ test/parity/harness/comparables.js test/parity/prototype-master.js.txt`: empty.
5. `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
6. `node --test test/parity/fixture-inventory.test.js`: passes (the roster is unchanged).

If any of 1–6 shows a move, STOP. Report the moved site in the SUMMARY and do not regenerate anything, because this plan predicts zero.

Append a new H2 at the END of test/parity/FIXTURE-INVENTORY.md: `## Phase 72: roll-direction sign fixes (ROLL-01) — measured per plan`. Open it with one paragraph that names the four known fixes and their plans: (a) and (d) here, (b) 72-05, (c) 72-06, plus the F5 parley-dial sign in 72-07. Then add `### Plan 04 — Thief evasion sign + the insult is the last term on the member branch: measured zero`, with:
- The rule, for each fix.
- The predictor. Evasion is 0 at identity, so it changes nothing anywhere. The member branch runs only against a live party member; no replay site carries a party (the JOIN-02 guard already proves no site meets a Joiner), and the reorder changes mNeed only when the member's own Sidestep or Smoke is live. So the predicted moved set is empty.
- The live-scan results: the six measurements above, stated with their outputs.
- Byte-identical elsewhere: comparables.js is untouched (no new serialized field); the Smoke txt is not serialized state (per Task 2's check).

In test/parity/divergence-records.test.js, add a module-level `const ROLL01_EXPECTED_HOLDERS = [];`, with a comment that it holds the measured holders whose divergence record's `phase` (split on "+") includes "72", and that 72-05/06/07 extend it with their measured sets. Then add `test("ROLL-01 (Phase 72): the holders declaring Phase 72 are exactly the measured moved set; no replay site reaches the member branch", …)`. It asserts:
- (a) The sorted holder ids from RECORDS whose phase includes "72" deep-equal `ROLL01_EXPECTED_HOLDERS`.
- (b) Replaying every combat/magic site with the existing replaySiteEvents shape emits zero `memberStruck` events and zero `foeMissed` events carrying a `member` field.
  </action>
  <verify>
    <automated>node --test test/parity/divergence-records.test.js test/parity/fixture-inventory.test.js && node --test test/parity/*.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 72: roll-direction sign fixes (ROLL-01) — measured per plan" test/parity/FIXTURE-INVENTORY.md` equals 1, and `grep -c "^### Plan 04 — Thief evasion sign" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `grep -c "ROLL01_EXPECTED_HOLDERS" test/parity/divergence-records.test.js` is at least 2, and `grep -c "ROLL-01 (Phase 72)" test/parity/divergence-records.test.js` is at least 1.
    - `node --test test/parity/*.test.js` exits 0.
    - `git diff --stat <plan-base>..HEAD -- test/parity/fixtures/ test/parity/harness/comparables.js test/parity/prototype-master.js.txt` is empty (<plan-base> = the commit this plan started from), and `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    - `npm test` exits 0 (full suite green; in a worktree, only STATE.md's known pre-existing CRLF doc-ledger failures may appear, identical on the base commit).
  </acceptance_criteria>
  <done>Both fixes are measured at zero fixture moves, declared in the inventory, and guarded by a standing ROLL-01 test that the later fix plans extend.</done>
</task>

</tasks>

<verification>
- The member-branch order check prints ok; the evasion greps pass; both formerly pending rows pass.
- `node --test test/parity/*.test.js` is green; fixtures, comparables and master are untouched; `npm test` is green.
</verification>

<success_criteria>
ROADMAP Phase 72 success criterion 2 is met (the insult order is right on both branches), fix (d) is landed, and success criterion 5 is honoured for this plan (the measured zero is declared).
</success_criteria>

<output>
Create `.planning/phases/72-roll-direction-sign-audit-fixes/72-04-SUMMARY.md` when done. Include the six measurement outputs, the txt-serialization check result, the tests changed, and a "Human verification (deferred to end of run)" section: on the Pixel 7, use Smoke in a fight after a failed parley and confirm the Smoke card and Oracle line read "(a 1–2 if you insulted them)".
</output>
