---
phase: 72-roll-direction-sign-audit-fixes
plan: 05
type: execute
wave: 3
depends_on:
  - 72-01
  - 72-04
files_modified:
  - engine/combat.js
  - test/unit/combat.test.js
  - test/unit/rollDirection.test.js
  - test/parity/fixtures/action-script.combat.json
  - tools/initiative-fixture-scan-output.txt
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - test/determinism/foe-abilities.test.js
  - test/unit/foe-turn-draw-count.test.js
autonomous: true
requirements:
  - ROLL-01
must_haves:
  truths:
    - "Fridgian frenzy's second swing uses the hero's normal to-hit narrowed by exactly one face (floor one face), so it honours the dark cap and dazed via toHit(state), and every later need rule (dozing/stupid, hard-to-hit, fast, magicOnly, Overhead Blow, Afraid) still applies after it. The canon hard-set need is gone"
    - "The change is a DECLARED divergence from the 1994 rules (canon need 3: a fighter's frenzy swing improves 3→4 and a magic user's drops 3→2), stated in a DELIBERATE RULES CHANGE code comment, in the ledger and in FIXTURE-INVENTORY.md"
    - "A modifier whose effect is absorbed by a clamp (need already floored at 1, an untouchable need-0 foe, the dark cap at 2) is asserted by the direction test as non-worsening (never flagged as a sign bug), and a clamp never turns a bonus into a penalty"
    - "Every parity fixture and every determinism or draw-count pin the change moves is MEASURED (never hand-computed), declared with before/after and a rationale, and regenerated. Nothing else moves, and the prototype master hash is unchanged"
    - "The direction rows [hero-strike:frenzy-second-swing] and [hero-strike:frenzy-dark-cap] carry no todo option and pass"
  artifacts:
    - path: engine/combat.js
      provides: "the frenzy second swing = normal to-hit narrowed by one"
      contains: "Math.max(1, toHit(state) - 1)"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "Plan 05 measured-set section"
      contains: "### Plan 05 — Fridgian frenzy's second swing"
  key_links:
    - from: engine/combat.js#playerStrike
      to: engine/derived.js#toHit
      via: "the frenzy swing need derived from toHit(state) (dark cap and dazed included)"
      pattern: "toHit\\(state\\) - 1"
    - from: test/parity/divergence-records.test.js
      to: test/parity/fixtures/action-script.combat.json
      via: "ROLL01_EXPECTED_HOLDERS lists the measured frenzy holders"
      pattern: "ROLL01_EXPECTED_HOLDERS"
  prohibitions:
    - statement: "MUST NOT hand-type a regenerated fixture value, or loosen a draw-count pin without a measured rationale; every moved value is copied from a live scan or helper output"
      status: resolved
      verification: judgment
---

<objective>
Land ROLL-01 fix (b). Fridgian frenzy's second swing becomes the hero's normal to-hit narrowed by one face, instead of the canon hard-set need that skipped the dark cap. Then measure, declare and regenerate exactly the fixtures and pins it moves.

Purpose: ROADMAP success criterion 3 ("frenzy's second swing honours the dark-cap penalty instead of hard-setting a fixed need"), done as the user ruled on 2026-09-24. This is the one fix CONTEXT expects to move difficulty (Fridgian heroes only) and possibly a fixture: combat#lose, seed 14, is a Fridgian Soldier.

Output: the engine edit, updated and new unit tests, green frenzy rows, and the measured and declared moved set.
</objective>

<assumption_delta_decision>
decision: no-change
noun: n/a
rationale: lexical false positive. "second" refers to Fridgian frenzy's second attack swing, not to a second identity, platform or source of truth.
</assumption_delta_decision>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 72-05 creates or changes.)

- `docs/ROLL-LEDGER.md` (72-01; 72-07 finalizes it)
- `docs/DIFFICULTY-RETUNE.md` Phase 72 H2 (72-01 BEFORE, 72-07 AFTER, which is where the frenzy's difficulty effect shows)
- `test/unit/harness/rollOdds.js`, `test/unit/rollDirection.test.js` (72-02), `test/unit/rollDirection-checks.test.js` (72-03), `test/unit/roll-ledger-sync.test.js` (72-07)
- `classEvasionFor` subtracted; member-branch insult last; Smoke text (72-04)
- **This plan:** the `engine/combat.js#playerStrike` frenzy second-swing need = `Math.max(1, toHit(state) - 1)` (a DELIBERATE RULES CHANGE, ROLL-01 (b)); plus the F4 gating if the user ruled it
- **This plan:** `### Plan 05 — Fridgian frenzy's second swing …` in FIXTURE-INVENTORY.md; `ROLL01_EXPECTED_HOLDERS` extended; any measured fixture, scan-output or pin regeneration
- `engine/dice.js#isBestFace`, `engine/combat.js#shatterIfBest`, event `foeShattered`, `sp.shatterOnBest` (72-06)
- `parleyNeedModFor` subtracted in `parley` (72-07, F5)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md
@docs/ROLL-LEDGER.md

<interfaces>
engine/combat.js#playerStrike (~L546-811 at eee9e10):
- ~L597: `if (R.frenzy && rng.d(8) <= 5) { attacks = Math.max(attacks, 2); events.push({ type: "frenzy" }); }`
- ~L602: `for (let a = 0; a < attacks && t.alive; a++) {`, then `const dieN = strikeDie(c); let roll = rng.d(dieN);` and the Philly slow second die.
- ~L611: the swing's need line. For `a === 1 && R.frenzy` it hard-sets the canon constant (3); otherwise `toHit(state)`. This is the line to change.
- ~L614-636: the rules applied after it, in order: dozing/stupid `Math.max(need, 5)`; `sp.toHit` `Math.min`; `sp.fast` `Math.max(1, need - 1)`; `sp.magicOnly && !c.magicWpn` → 0; `AS.needShift` (floor 1, only if need > 0); `afraidNeed`.
engine/derived.js#toHit (~L1125): classNeed + inspired + eff(toHit) + weaponNeedMod, floor 1, dazed −2 (floor 1), then the dark cap `Math.min(h, 2)` unless Night Vision or senses. It is always >= 1.
Existing frenzy tests: test/unit/combat.test.js ~L551-580 (the comment says "need 3 for the frenzy swing"; the draws are [5, 20, 20, 20] and it asserts 2 misses); test/unit/ability-strike.test.js ~L319-327 (Knight Fridgian with 3 attacks; asserts 3 attack events).
The fixture exposure known at planning time: `action-script.combat.json#lose`, seed 14, Fighter Soldier Fridgian, with divergence phase `24+31+51+52+54` (tools/initiative-fixture-scan-output.txt). The other Fridgian replay sites (flee seed 17, and chargen/potion/trap seed 1) never reach a second strike.
F4 (docs/ROLL-LEDGER.md `## Rulings`): if the user ruled F4 "fix", the narrowed need applies only when the frenzy fired on THIS strike, not to every Fridgian second attack.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The frenzy second swing = the normal to-hit narrowed by one face</name>
  <files>engine/combat.js, test/unit/combat.test.js, test/unit/rollDirection.test.js</files>
  <read_first>
    - engine/combat.js (playerStrike ~L546-660, the header comment ~L1-50)
    - engine/derived.js (toHit ~L1114-1137)
    - test/unit/combat.test.js (~L540-590) and test/unit/ability-strike.test.js (~L300-330)
    - test/unit/rollDirection.test.js (the two `[hero-strike:frenzy-…]` pending rows) and test/unit/harness/rollOdds.js
    - docs/ROLL-LEDGER.md (`## Known fixes` (b) and `## Rulings`, to see whether F4 is ruled "fix")
    - content/races.js (the Fridgian row) and content/flavor.js ~L29 (the Fridgian race note)
  </read_first>
  <behavior>
    - Lit, a Fridgian Fighter: the frenzy swing wins on exactly one face fewer than a normal swing, on the same die.
    - Lit, a Fridgian Magic User: the frenzy swing wins on exactly one face fewer than a normal swing (canon had it EQUAL to the normal swing).
    - In the dark without Night Vision: the frenzy swing wins on at most as many faces as the dark-capped normal swing, never more (canon let it beat the cap), and never on fewer than one face against a touchable foe.
    - Dazed, Afraid, a hard-to-hit foe and a magicOnly foe still apply after it (a magicOnly foe without a magic weapon: zero faces).
    - The draw count per strike is unchanged; only which draw values hit changes.
  </behavior>
  <action>
Per CONTEXT fix (b) (user ruling 2026-09-24: "Fridgian frenzy's second swing = your normal to-hit, one worse"): in engine/combat.js#playerStrike, replace the frenzy swing's canon hard-set constant need with `Math.max(1, toHit(state) - 1)`. The non-frenzy path stays `toHit(state)`. Leave every rule after it (dozing/stupid, `sp.toHit`, `fast`, `magicOnly`, `AS.needShift`, `afraidNeed`) in its current order and untouched.

Add a comment block above the line: "DELIBERATE RULES CHANGE (Phase 72, ROLL-01 (b), user ruling 2026-09-24): the 1994 rules hard-set the frenzy swing's need to a constant that ignored the dark cap and dazed. The swing is now the hero's normal to-hit narrowed by one face (floor 1): a fighter's frenzy swing improves on canon, a magic user's worsens. Zero rng change. Declared in test/parity/FIXTURE-INVENTORY.md (Phase 72, Plan 05)". Do not repeat the old canon expression in the comment.

F4. If docs/ROLL-LEDGER.md `## Rulings` records F4 as "fix", capture the frenzy trigger's result in a local boolean at the trigger line (the same draw, the same branch) and key the narrowed need on that boolean instead of the race flag. That way a Fridgian's non-frenzy second attack (Barbarian, haste, Ambidextrous, Last Stand) uses the normal need. Name F4 in the comment and in the SUMMARY. If F4 is "text", "defer" or absent, keep the race-keyed condition exactly as it is.

Update the combat header comment only if it describes the frenzy need. Content: content/races.js and content/flavor.js do not state the swing's odds ("frenzies into a second wild swing", "swing twice at whatever is nearest"). Confirm this and change nothing (CONTEXT: "Update the Fridgian race note if it states the swing's odds"); record the check in the SUMMARY.

Tests. In test/unit/combat.test.js ~L551, reword the stale comment that names the canon frenzy need to say "the frenzy swing's need is the normal need narrowed by one (Phase 72 ROLL-01 (b))"; keep its assertions. Add three outcome-based tests next to it, using the harness faceOdds and heroState:
- lit Fighter: frenzy wins = normal wins − 1;
- lit Magic User: the same;
- dark without Night Vision: frenzy wins ≤ dark-capped normal wins, and ≥ 1 against a touchable foe.
Delete the todo option on `[hero-strike:frenzy-second-swing]` and `[hero-strike:frenzy-dark-cap]` in test/unit/rollDirection.test.js.

Then run `npm test`. For every OTHER test that fails, decide whether the frenzy change moved it: it did if a Fridgian second-swing draw now lands or misses differently. For each one it moved, re-measure the new value LIVE through that file's own helper (never hand-compute) and update the pin with a one-line "Phase 72 ROLL-01 (b)" rationale, as each file's header rule allows. Candidates: test/determinism/foe-abilities.test.js FULL_FIGHTS, test/unit/foe-turn-draw-count.test.js, the fight-log tests and identity-contract. List each old → new in the SUMMARY; Task 2 copies the list into the inventory. Any failure NOT caused by the frenzy change is a regression, so stop and report it.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/ability-strike.test.js test/unit/rollDirection.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Math.max(1, toHit(state) - 1)" engine/combat.js` equals 1.
    - `grep -cE "R\.frenzy \? 3" engine/combat.js` equals 0.
    - `grep -c "ROLL-01 (b)" engine/combat.js` is at least 1.
    - `grep -n "frenzy-second-swing\]\|frenzy-dark-cap\]" test/unit/rollDirection.test.js` shows both rows with no todo option, and `node --test test/unit/rollDirection.test.js` reports them as passes.
    - The three new combat.test.js frenzy tests exist (`grep -c "ROLL-01 (b)" test/unit/combat.test.js` is at least 3) and pass.
    - `npm test` exits 0 after the measured pin updates (in a worktree, only STATE.md's known pre-existing CRLF doc-ledger failures may appear, identical on the base commit), EXCLUDING the parity suite files Task 2 regenerates. Record any parity failures for Task 2.
  </acceptance_criteria>
  <done>The frenzy swing is one face worse than a normal swing everywhere, including in the dark. The unit and direction tests prove it by odds, and every moved pin is re-measured with a rationale.</done>
</task>

<task type="auto">
  <name>Task 2: Measure, declare and regenerate the frenzy's moved set</name>
  <files>test/parity/fixtures/action-script.combat.json, tools/initiative-fixture-scan-output.txt, test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js, test/determinism/foe-abilities.test.js, test/unit/foe-turn-draw-count.test.js</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md: the Phase 52 section (`## Phase 52: crit doubles the dice…`, the full measured-set pattern) and the Phase 72 H2 that 72-04 added. Use Grep to find both, then read those ranges.
    - test/parity/fixtures/action-script.combat.json (the `lose` scenario's `divergence` record only: phase, rationale, fields, before/after)
    - tools/initiative-fixture-scan.mjs (its header) and tools/initiative-fixture-scan-output.txt (the `#lose` rows and its Record values section)
    - test/parity/divergence-records.test.js (the DMG-02 test ~L187 and the ROLL-01 test 72-04 added)
  </read_first>
  <action>
Predict, then measure.

Predictor. With a scratch replay (newRun → applyStartCombat/applyAction over each combat/magic replay site, the same dispatch full-suite.test.js uses, NOT a committed tool), count, per site, the `frenzy` events and the second-swing strike events. For each second swing, write down its draw value, the old need (the canon constant) and the new need. The swing flips outcome only when the draw lies between the two. Put the table in FIXTURE-INVENTORY.md.

Live scan. `node tools/initiative-fixture-scan.mjs > <scratchpad>/scan-after.txt`, then `diff --strip-trailing-cr <scratchpad>/scan-after.txt tools/initiative-fixture-scan-output.txt`. Also run `node --test test/parity/*.test.js` and the worn scan diff.

For EACH moved site (predicted and confirmed; the expected candidate is `action-script.combat.json#lose`), follow the Phase 52 pattern exactly:
- Append `+72` to the scenario's `divergence.phase`.
- Append a "Phase 72 (ROLL-01 (b), 2026-09-24): …" paragraph to its `divergence.rationale`. It gives the second-swing draw, old need → new need, hit↔miss, and the downstream field change.
- Re-measure `after` by copying the scan's `fields.after (engine @end)` line verbatim. Never hand-type it.
- Regenerate `tools/initiative-fixture-scan-output.txt` from the edited engine (`node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt`).
Regenerate ONLY moved holders. If the scan shows zero moves (the seed-14 second swing's draw is outside the flip window), record a measured zero and change no fixture.

Add `### Plan 05 — Fridgian frenzy's second swing (ROLL-01 (b), declared canon divergence): measured moved set (N)` under the Phase 72 H2, with these parts:
- the rule (canon constant → normal to-hit narrowed by one, with the fighter and magic-user consequences);
- the predictor table;
- the live-scan diff summary;
- `#### Moved set — declared records`, a table `Holder | Site / seed | Hero | record | fromAction | fields before → after | rationale pointer`;
- `#### Draw-count pins`, listing every pin Task 1 re-measured (old → new, which helper measured it), or "byte-identical" for each pin file checked;
- `#### Byte-identical elsewhere`: the master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0 is unchanged and comparables.js is untouched.

In test/parity/divergence-records.test.js, add the measured holder ids to `ROLL01_EXPECTED_HOLDERS` (sorted). The ROLL-01 test then pins the moved set.
  </action>
  <verify>
    <automated>node --test test/parity/*.test.js && node tools/initiative-fixture-scan.mjs | diff --strip-trailing-cr - tools/initiative-fixture-scan-output.txt && node tools/worn-fixture-scan.mjs | diff --strip-trailing-cr - tools/worn-fixture-scan-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/parity/*.test.js` exits 0, and both scan diffs are empty after regeneration.
    - `grep -c "^### Plan 05 — Fridgian frenzy's second swing" test/parity/FIXTURE-INVENTORY.md` equals 1, and that section contains `#### Moved set — declared records` and `#### Draw-count pins`.
    - `git diff --name-only <plan-base>..HEAD -- test/parity/fixtures/` lists only holders named in the Moved set table (possibly none).
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0, and `git diff --stat <plan-base>..HEAD -- test/parity/harness/comparables.js` is empty.
    - Every holder whose divergence phase includes "72" appears in `ROLL01_EXPECTED_HOLDERS`, and the ROLL-01 (Phase 72) test passes.
    - `npm test` exits 0 (full suite green; in a worktree, only STATE.md's known pre-existing CRLF doc-ledger failures may appear, identical on the base commit).
  </acceptance_criteria>
  <done>The frenzy's parity footprint is measured, declared per site and regenerated. Nothing undeclared moved.</done>
</task>

</tasks>

<verification>
- The frenzy greps pass; the frenzy odds rows are green; the parity suite and both scan diffs are clean; `npm test` is green; the master hash is unchanged.
</verification>

<success_criteria>
ROADMAP Phase 72 success criterion 3 is met, and success criterion 5 is honoured for the frenzy (the moved set is measured, declared and regenerated, and nothing else moves).
</success_criteria>

<output>
Create `.planning/phases/72-roll-direction-sign-audit-fixes/72-05-SUMMARY.md` when done. Include the predictor table, the moved set (or measured zero), every re-measured pin old → new, the F4 handling, the Fridgian-text check, and a "Human verification (deferred to end of run)" section: on the Pixel 7, play a Fridgian Fighter into a dark square, trigger a frenzy, and confirm the Oracle's second-swing need reads one worse than the first swing's.
</output>
