---
phase: 72-roll-direction-sign-audit-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ROLL-LEDGER.md
  - docs/DIFFICULTY-RETUNE.md
autonomous: false
requirements:
  - ROLL-01
must_haves:
  truths:
    - "docs/ROLL-LEDGER.md inventories every die check in engine/ (extending Phase 31's 34 sites): to-hit both ways, soak both ways, thrown spells, resistance, initiative, flee, parley, traps, locks, climbs/leaps, cures, wake, drops, gates, summons and crits, each with its die, who rolls, what success means and a verdict"
    - "Every non-comment rng.d( call in engine/*.js is accounted for in the ledger: listed as a CHECK site row, or listed in the not-a-check appendix as a SELECTION or AMOUNT draw, and the per-file counts in the ledger equal the live grep counts"
    - "docs/ROLL-LEDGER.md has one modifier row per modifier source (weapon, armor, spell, ability, item, race, sub-class, condition, terrain/darkness, foe trait, tuning dial), keyed by the same [site:source] id the direction tests use, with the claimed direction (quoted player-facing text plus file:line), the verified direction today, the Phase 73 roll-high reading and a verdict"
    - "The ledger says in plain words that the dropped heavy weapon's '−2 to hit' device report was a correct penalty (heavy weapons narrow the success range) and that only the presentation was ambiguous"
    - "The four known fixes are listed with the user's 2026-09-24 rulings and the plan that lands each: (a) insult last on both branches → 72-04, (b) frenzy second swing one worse → 72-05, (c) Skeleton best-face shatter → 72-06, (d) Thief evasion sign → 72-04"
    - "Every NEW finding is classified in the ledger as one of: text-backed local sign bug (fixed in 72-07), needs a user ruling (batched checkpoint in this plan), or out of ROLL-01 scope (recorded, not fixed). Nothing is fixed silently and nothing is dropped silently"
    - "Display-sign inconsistencies (needModsClause printing deltas in opposite senses on hero-need and foe-need lines, and any others found) are recorded as Phase 74 handoffs, and roll-direction phrasing is recorded as Phase 79 handoffs. Neither is fixed here"
    - "A BEFORE bot readout (node tools/tune-difficulty.mjs --seeds=200, solo) on the pre-fix engine is recorded verbatim in docs/DIFFICULTY-RETUNE.md under a new H2 placed immediately before '## v1.2 retune (Phase 27) — TUNE-05..07', which stays the ledger's last H2"
  artifacts:
    - path: docs/ROLL-LEDGER.md
      provides: "The ROLL-01 audited ledger: site inventory, modifier ledger, known fixes, new findings, rulings, Phase 74/79 handoffs"
      contains: "## Modifier ledger"
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "Phase 72 BEFORE bot readout"
      contains: "## v2.1 roll-direction pass (Phase 72)"
  key_links:
    - from: docs/ROLL-LEDGER.md
      to: test/unit/rollDirection.test.js
      via: "shared [site:source] ids (catalogue in 72-02-PLAN.md and 72-03-PLAN.md)"
      pattern: "\\[hero-strike:"
    - from: docs/ROLL-LEDGER.md
      to: .planning/milestones/v1.3-phases/31-combat-start-gating-effect-hygiene/31-ROLL-DIRECTION-AUDIT.md
      via: "Phase 31 site # column (extend, don't redo)"
      pattern: "Phase 31"
  prohibitions:
    - statement: "MUST NOT resolve a text-vs-canon conflict, or a finding whose fix would noticeably swing difficulty, without a user ruling. Every such finding goes to the batched checkpoint:decision and its ruling is written into the ledger before any plan acts on it"
      status: resolved
      verification: judgment
    - statement: "MUST NOT change any engine, content or display byte in this plan. The audit records; later plans fix"
      status: resolved
      verification: judgment
---

<objective>
Audit every modifier that feeds a die check in today's roll-under engine and write the ROLL-01 ledger, `docs/ROLL-LEDGER.md`. Take the phase's BEFORE bot readout on the untouched engine. If the audit finds anything that needs a design ruling, batch those findings into one checkpoint:decision for the user.

Purpose: ROLL-01's success criterion 1. The ledger is the inventory the direction tests (72-02/72-03) mirror, the fix plans (72-04..72-06) cite, Phase 73's mirror is built on and Phase 74 takes its display-sign handoffs from. The ledger extends Phase 31's 34-site audit (commit b92ce09). It does not redo it.

Output: `docs/ROLL-LEDGER.md` (new), a BEFORE section in `docs/DIFFICULTY-RETUNE.md`, and a Rulings section filled in from the user's answer if the checkpoint fires.

Decisions honoured: CONTEXT "Ledger & method": a direction test plus a short ledger doc; extend the Phase 31 audit; the text wins; new sign bugs are fixed when the text backs the fix and the change is local; a checkpoint:decision when a design ruling is needed; display-sign issues are recorded for Phase 74. Known fixes (a)–(d) are recorded here and landed by 72-04/05/06. The engine gate bot readout is taken BEFORE.
</objective>

<assumption_delta_decision>
decision: no-change
noun: n/a
rationale: lexical false positive. "second" refers to Fridgian frenzy's second attack swing, not to a second identity, platform or source of truth.
</assumption_delta_decision>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 72-01 creates.)

- `docs/ROLL-LEDGER.md`: the ROLL-01 ledger. **This plan** creates it; 72-07 finalizes the verdicts.
- `docs/DIFFICULTY-RETUNE.md`: new H2 `## v2.1 roll-direction pass (Phase 72) — bot readouts`. **This plan** writes BEFORE; 72-07 writes AFTER.
- `test/unit/harness/rollOdds.js`: odds harness `probeRng`, `faceOdds`, `jointOdds`, `compareOdds`, `assertBonus`, `assertPenalty`, `assertSame`, and the mirror-proof state builders `heroState`, `foeFrom`, `inCombat`, `withMember` (72-02).
- `test/unit/rollDirection.test.js`: the combat to-hit/crit/shatter direction test (72-02). Its pending rows are flipped by 72-04/05/06.
- `test/unit/rollDirection-checks.test.js`: the soak/spell/resist/initiative/flee/parley/trap/lock/climb/leap/cure/wake/drop direction test (72-03).
- `test/unit/roll-ledger-sync.test.js`: a standing guard that ledger ids equal direction-test ids (72-07).
- `engine/dice.js#isBestFace(roll, dieN)`: the best face of a die, today `roll === 1` (72-06).
- `engine/combat.js#shatterIfBest(state, t, roll, dieN, by, rng, events)`: exported (72-06).
- Event type `foeShattered`, with `EVENT_NARRATION.foeShattered` and a `LINE_FOR`/`ORACLE_ONLY` entry (72-06).
- Content field renamed: the Skeleton's roll-under crit key becomes `sp.shatterOnBest: true`; M&M's copy is removed (72-06).
- Semantic change: `classEvasionFor(c)` is SUBTRACTED from the foe's need in `foeToHitVs`/`foeToHitBreakdown` (72-04).
- `test/parity/FIXTURE-INVENTORY.md`: new H2 `## Phase 72: roll-direction sign fixes (ROLL-01) — measured per plan` (72-04 creates it; 72-05/06/07 append).
- `test/parity/divergence-records.test.js`: new test `ROLL-01 (Phase 72): ...` (72-04 creates it; 72-05/06/07 extend it).
- Text: Smoke txt (content/abilities.js, content/skills.js, docs/ABILITIES.md), the smokeThrown narration lines, the Skeleton and M&M notes.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md
@.planning/milestones/v1.3-phases/31-combat-start-gating-effect-hygiene/31-ROLL-DIRECTION-AUDIT.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-02-PLAN.md
@.planning/phases/72-roll-direction-sign-audit-fixes/72-03-PLAN.md

<interfaces>
Die-check starter list (planner's pre-scan at commit eee9e10; line numbers drift, so the executor re-derives them). Every non-comment `rng.d(` per engine file at eee9e10: abilities 2, character 14, combat 44, derived 1, dice 1, economy 1, encounters 27, foeAbilities 1, foeDamage 1, items 14, magic 23, maze 1, movement 17. Recount live with the command in Task 1's acceptance criteria.

CHECK sites already found (roll vs threshold):
- engine/combat.js#resolveInitiative (~L176): d20 mine vs d20 theirs, `mine >= theirs` (roll-high). Forced-foe overrides: Samurai, Fridgian `slow`, Knight vs a big foe, Court Mage round 1. Forced-you: foresight, Acute Hearing. `c.senses` waives forced-foe.
- combat.js startCombat gates (~L365 Con Artist weak-foe flee d6<=4, ~L372 Court Mage boredom d12<=2); fight's phobia Hardiness shrug d2===1 (~L481).
- combat.js#playerStrike (~L546): frenzy trigger d8<=5 (~L597); strike `rng.d(strikeDie(c))` (~L604); Philly `slow` keeps the lower of two dice (~L610); need = frenzy special (~L611) else `toHit(state)`; dozing/stupid max 5; `t.sp.toHit` min; `fast` −1; `magicOnly` 0; Overhead Blow `AS.needShift`; `afraidNeed`; auto-hit (Cat Burglar/Ninja opener, `AS.autoHit`); crit `roll <= weaponCrit(c)` unless noCrit (Guard/Soldier/dark without Night Vision/Cloak of Strength); Stealth opener roll<=2; Ninja roll<=2.
- combat.js#killFoe (~L821): drop d20 <= 2+f.lvl; bag drop d20 <= BAG_DROP_UNDER; cooking d6>=4.
- combat.js#pursuitStrike (~L927): foe die vs foeToHitVs, then blind→1, Weaken min, insulted +1.
- combat.js#flee (~L1008): Smoke = automatic; d20 + fleeBreakdown bonus >= need (roll-high); FLEE_NEED_MOD.
- combat.js#parley (~L1173): d20 <= min(9+bonus,17) + PARLEY_NEED_MOD; bonus = Con Artist 4, Woodsman 3, Wilmsry 4, Elven-vs-Humans 3, 2×fluency, +level, −top foe level; Humans payout d6===6.
- combat.js#allyTurn (~L1455) summon strike d(STRIKE_DICE[lvl-1]) <= 5; #alliesTurn legacy strike (~L1522) <= 5; #memberStrike (~L1796) d(strikeDie(view)) vs memberToHit(view) (+needShift), member crit roll===1; #allyCast (~L1873) thrown `roll - bonus <= need` (d8 vs 4, Freeze d10 vs 6), resistRoll.
- combat.js#applyFoeDamageToPlayer (~L2061) hero soak d20 <= soakAr (armorSoak ar × Fighter armorMul, Cloak of Armor plate, Taunt ×2 cap 20, noArmor foes bypass).
- combat.js#foeTurn (~L2270): ability gate d6<=4; member branch (~L2422-2546): foeToHitVs(state,"member") → blind → Weaken min → insulted → member Sidestep → member Smoke; member crit mRoll===1. Hero branch (~L2548-2620): foeToHitVs(state) → blind → Weaken min → insulted; crit roll===1 or (<=2 and Soldier).
- engine/derived.js: #toHit (~L1125) classNeed, inspired, eff toHit, weaponNeedMod, floor 1, dazed −2, dark cap 2 waived by Night Vision/senses. #foeToHitVs (~L1217) base 5, race foeToHit (Elven +1, deliberate Phase 31), Acrobat 3, Guard −1, eff foeToHit, FOE_ACCURACY, Thief evasion (added today, the (d) bug), Battle Roar −2 (hero or party), Sidestep −2 (hero only), Smoke/Mirror/invis → 1, floor 1. #foeToHitBreakdown (~L1256) is the narration mirror. #resistRoll (~L1427) d20 < intel, gated intel >= 12. #strikeDie (~L766) level die, Elven strikeStep, Illusionist d20 until level 3, Acuteness d6. #foeDie (~L1185) max(d8, level die + Dwarven foeStrikeStep).
- engine/foeDamage.js (~L97) foe natural soak d20 <= foe.sp.ar (a crit bypasses it).
- engine/magic.js#castSpell thrown (~L480-540): d8 vs 4 (Freeze d10 vs 6), `roll - bonus <= target` where bonus = schoolBonus + eff(throw), target through afraidNeed; Apprentice backfire d8===1 (~L128); doubled backfire d8===1 (~L176); the `r === 4 && d10 !== 1` branch (~L313, classify it).
- engine/encounters.js#springTrap (~L66) d20 <= 5 + Acrobat 3 + Thief trapAvoid; #openChest (~L113) Locks tier d10 <= [0,5,7,8][tier] + intelBonus, bare d20 <= 8 + intelBonus, Pilfer auto; chest scroll d6>=3.
- engine/movement.js: climb (~L270) d10 + heightsPenalty + armorBulk <= success per 10 ft, fall d20>2 per segment; leap (~L285) d10 + waterPenalty + armorBulk > need = fail (class column F/T/M); affliction cure (~L708) d20 <= 10 + Hardiness 4; wandering wake (~L785) d20 <= wakeOn per hour (Bard +1 face, WANDER_RATE); ~L950 d20 === 1 (classify it).
- engine/items.js (~L250) `!hasPicks && d12 === 1` (classify it).

Likely NEW findings to verify (planner's pre-scan; keep, reword or drop each with a reason):
- F1: member, legacy-ally and summoned-ally strikes (`memberStrike`, `alliesTurn` legacy, `allyTurn`) apply none of the per-target to-hit rules playerStrike applies (dozing/stupid, `sp.toHit` "hittable only on a 4", `sp.fast` "strike one higher", `sp.magicOnly` "only magic touches it", Philly `slow`). The text claims these penalties for anyone; members ignore them. It is text-backed, but it swings party balance against Ghosts/Spectres, so it NEEDS A RULING.
- F2: Acute Hearing's "3 to hit the unseen" (content/skills.js ~L51) has no engine effect, since no unseen-foe mechanic exists (Herman's `sp.invis` is inert). This is a dead bonus claim. Wiring it or dropping the clause is a canon question, so it NEEDS A RULING.
- F3: Shadow's "only a dagger or magic touches it" (content/bestiary.js ~L137, `daggerOnly`) is inert. This is a dead penalty claim, and it NEEDS A RULING.
- F4: the frenzy-odds second swing is keyed on `a === 1 && R.frenzy` (race), not on the frenzy having fired, so a Fridgian's non-frenzy second attack (Barbarian, haste, Ambidextrous, Last Stand) also takes the frenzy odds. That is canon, but no text claims it, so it NEEDS A RULING. If the user rules "fix", 72-05 applies it in the same edit.
- F5: the `PARLEY_NEED_MOD` dial is documented "up = harder" (engine/difficulty.js#parleyNeedModFor JSDoc, and the docs/DIFFICULTY-RETUNE.md dial table row `FLEE_NEED_MOD / PARLEY_NEED_MOD ... harder`). But engine/combat.js#parley ADDS it to a roll-under need (`roll <= need` succeeds), so a positive value makes parley EASIER. This is the same latent inversion as known fix (d): 0 at identity, so zero fixtures move. It is text-backed and local, so it is a SIGN BUG that 72-07 fixes without a ruling. The fix makes a positive value subtract from the parley need, so the documented "up = harder" becomes true. The direction row `[parley:parley-need-mod]` (72-03) pins it RED until then.
- Dead hooks with no content source (N/A, not findings): `eff(c,"toHit")` and `eff(c,"throw")` / `eff(c,"spellDmg")`. No content row sets them, so there is no claim to break (Phase 31 Finding 3).
- O1 (out of scope, not a sign bug): the hero's Mirror Self and invisibility item effects also shield party members (`foeToHitVs(state,"member")` reads the hero's `c.mirror` and invis). Mirror Self's text says "you". The Crystal Staff's text says "party invisible".
- O2 (out of scope, magnitude/order): the member branch applies the member's own Sidestep AFTER the Weaken cap, while the hero branch applies Sidestep BEFORE it (inside foeToHitVs). Sidestep plus Weaken therefore gives a member 1 face and the hero 3.
- O3 (Phase 74): needModsClause prints need deltas in opposite senses on hero-need and foe-need lines.
- O4 (Phase 79): every "need a 1" / "hit on a 3" phrase becomes imprecise under the insult: Mirror Self "foes need a 1 to hit", the Crystal Staff "enemies need a 1", Weaken "they hit on a 3".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start the BEFORE bot readout, then write the ledger skeleton and the full die-check site inventory</name>
  <files>docs/ROLL-LEDGER.md</files>
  <read_first>
    - .planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md (all decisions, the code_context line anchors and specifics)
    - .planning/milestones/v1.3-phases/31-combat-start-gating-effect-hygiene/31-ROLL-DIRECTION-AUDIT.md (the 34-site table to extend; reuse its # numbering in a "Phase 31 #" column)
    - .planning/phases/72-roll-direction-sign-audit-fixes/72-02-PLAN.md and 72-03-PLAN.md (the `[site:source]` id catalogues; the ledger must use the same ids)
    - engine/combat.js, engine/derived.js, engine/magic.js, engine/encounters.js, engine/movement.js, engine/foeDamage.js, engine/foeAbilities.js, engine/items.js, engine/abilities.js, engine/economy.js, engine/character.js, engine/maze.js, engine/phobias.js, engine/death.js (only the regions holding a non-comment rng.d( call; find them with Grep, then read the ranges)
  </read_first>
  <action>
Step 1, the BEFORE readout. Confirm the engine is untouched: `git status --porcelain -- engine/ content/` must be empty. Record `git rev-parse HEAD` as the BEFORE commit. Start `node tools/tune-difficulty.mjs --seeds=200` in the BACKGROUND, capturing stdout to a scratchpad file and appending an `EXIT=<code>` sentinel line. The solo run takes a few minutes, so do not wait on it. Task 2 writes the transcript into the retune ledger when it finishes. Per CONTEXT's engine gate, this one readout is the only difficulty readout the phase needs BEFORE the fixes. Do not run `--party`.

Step 2, create `docs/ROLL-LEDGER.md` with these H2 sections in this order. The later tasks and plans fill some of them:
1. `## How to read this ledger`. Explain today's roll-under convention: a hero hit is roll ≤ need, a foe miss is roll > need, and flee/initiative are the roll-high exceptions. Explain the Phase 73 roll-high reading: each draw r is read as (N+1)−r against a high target. Give one worked dual-view example for a user who reasons in roll-high terms: "insult: need 1 → 2 today = 19–20 on a d20 after Phase 73". Define the verdict vocabulary: `OK`, `OK (clamp)`, `OK (deliberate, Phase N)`, `N/A (no modifier)`, `BUG → 72-0N`, `FINDING Fn`.
2. `## The device trigger`. The dropped heavy weapon read "−2 to hit". State plainly that the audit confirms heavy weapons narrow the success range (`weaponNeedMod` −1/−2 is subtracted from the hero's need), so the modifier was correct and only its presentation was ambiguous. That presentation fix belongs to Phase 74.
3. `## Site inventory`. Columns: `# | Site (file#function) | Die | Who rolls | Success means | Roll-high today? | Modifier sources ([site:source] ids) | Phase 31 # | Verdict`. One row per CHECK site (a roll compared to a threshold that decides success or failure). Cover every site in the interfaces starter list plus any the Grep sweep finds. Mark each Phase 31 site with its Phase 31 number. New sites get `—`.
4. `## Not-a-check draws (completeness appendix)`. List every remaining non-comment rng.d( call as `file#function — SELECTION (table/target pick)` or `AMOUNT (damage/heal/duration/count)`. Close the section with a per-file count table: `| File | CHECK draws | SELECTION | AMOUNT | Total |`, where Total equals the live grep count.
5. `## Modifier ledger` (Task 2 fills this).
6. `## Known fixes (a)–(d)` (Task 2 fills this).
7. `## New findings` (Task 2 fills this).
8. `## Rulings` (Task 3 fills this, or it records "No rulings needed").
9. `## Skeleton shatter scope` (Task 2 fills this).
10. `## Handoffs → Phase 74 (display signs)` and `## Handoffs → Phase 79 (roll-direction phrasing)` (Task 2 fills these).
11. `## Bot readout`: a pointer to docs/DIFFICULTY-RETUNE.md's Phase 72 H2.

Record each site by function name, and add a line number "at commit <BEFORE sha>" only where it helps, because later plans shift lines. The inventory must cover every category CONTEXT names: to-hit both ways, soak both ways, thrown spells, resistance, initiative, flee, parley, traps, locks, climbs/leaps, cures, wake, drops, gates, summons and crits. Change no engine, content or test file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('docs/ROLL-LEDGER.md','utf8');for(const h of ['## How to read this ledger','## The device trigger','## Site inventory','## Not-a-check draws','## Modifier ledger','## Known fixes','## New findings','## Rulings','## Skeleton shatter scope','## Handoffs → Phase 74','## Handoffs → Phase 79','## Bot readout']){if(!t.includes(h)){console.error('missing '+h);process.exit(1)}}console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/ROLL-LEDGER.md` exists and contains all twelve H2 headings listed in the action (the verify command passes).
    - The per-file count table in `## Not-a-check draws` matches the live counts from `for f in engine/*.js; do n=$(grep -v '^\s*//\|^\s*\*' "$f" | grep -o 'rng\.d(' | wc -l); [ "$n" -gt 0 ] && echo "$f $n"; done` for every file (compare by eye or with a small node script; every file with a non-zero count appears in the table).
    - `grep -c "Phase 31 #" docs/ROLL-LEDGER.md` is at least 1, and at least 34 site rows carry a non-dash Phase 31 number (every Phase 31 site is carried forward).
    - The site inventory has at least one row for each category named in CONTEXT: initiative, flee, parley, trap, lock (chest), climb, leap, cure, wake, drop, soak, thrown, resist, crit.
    - This plan's commits touch only docs/ROLL-LEDGER.md and docs/DIFFICULTY-RETUNE.md: `git diff --name-only <BEFORE sha>..HEAD -- engine/ content/ src/` is empty. 72-02 runs in parallel and touches only test/, so do not gate on test/.
    - A background tune-difficulty run was started, and its output file path and the BEFORE commit sha are recorded in the task's commit message or the SUMMARY.
  </acceptance_criteria>
  <done>The ledger skeleton and a complete, count-reconciled site inventory are committed, and the BEFORE bot readout is running on the untouched engine.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the modifier ledger, the known fixes, the new findings and the handoffs; record the BEFORE readout</name>
  <files>docs/ROLL-LEDGER.md, docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - docs/ROLL-LEDGER.md (from Task 1)
    - .planning/phases/72-roll-direction-sign-audit-fixes/72-02-PLAN.md and 72-03-PLAN.md (the `[site:source]` id catalogues)
    - content/abilities.js, content/skills.js, content/spells.js, content/treasure-tables.js, content/bestiary.js, content/flavor.js, content/races.js, content/classes.js, content/weapons.js, content/armors.js, content/flee.js, content/mu-chart.js (the player-facing direction claims; CONTEXT lists the known lines: abilities L43/45/51/56, skills L28/30/40/47/51/53, spells L93/95, treasure-tables L129/244, bestiary L48/63/124/151, flavor L34/36/41/43/56/63, races L23)
    - src/browser/eventNarration.js and src/browser/narrationLines.js (grep for needModsClause, "natural 1", "need a", "to hit" to find display-sign and phrasing handoffs)
    - docs/DIFFICULTY-RETUNE.md: only the `## v1.7 tuning pass` H2's opening paragraph and the `## v1.2 retune (Phase 27)` heading line (use Grep to locate them, then read those ranges)
    - test/unit/difficulty-retune-ledger.test.js (its last-H2 guard)
  </read_first>
  <action>
`## Modifier ledger`. Write one row per modifier source per site, with columns `Id | Site | Source | Kind | Claimed direction (quote + file:line) | Verified today | Phase 73 reading | Verdict`.
- Id is the `[site:source]` id from the 72-02/72-03 catalogues. Where the audit finds a source the catalogues lack, mint a new id in the same kebab-case style and list it under a sub-heading `### Ids not yet in a direction test`. 72-07 adds the test rows for these.
- Kind is one of weapon, armor, spell, ability, item, race, sub-class, class, condition, terrain, foe-trait, dial.
- "Claimed direction" quotes the player-facing text that makes the promise (bonus or penalty, for whom). The text wins (CONTEXT). A source with no player-facing text states its claim from its name or its JSDoc and marks it `(no text)`.
- "Verified today" states which way the source moves the ROLLER's success odds in today's engine (widens, narrows, overrides to one face, overrides to zero, or no effect), with the function and the term.
- "Phase 73 reading" restates the effect in roll-high words, e.g. "Guard: foe hits on one face fewer: 17–20 → 18–20 on a d20".
- Clamp-absorbed cases get `OK (clamp)`, and the note says the clamp never turns a bonus into a penalty. Deliberate past changes get `OK (deliberate, Phase N)` (for example Elven foeToHit +1, Phase 31). Flat gates with no modifier get `N/A (no modifier)` on the site row only; they get no modifier row.

`## Known fixes (a)–(d)`. Quote each user ruling from CONTEXT verbatim. Give the current wrong behaviour with its function and term, the target behaviour in both views (today's need arithmetic and the roll-high face count), and the plan that lands it:
- (a) The insult is the LAST term on the member branch as on the hero branch: Smoked + insulted → need 2 = 19–20 on a d20 after Phase 73. Plan 72-04.
- (b) Frenzy's second swing = the normal to-hit narrowed by one face, floor one face, and it keeps every later need rule. This is a declared canon divergence: canon need 3 → fighters' frenzy improves 3→4, magic users' drops 3→2. Plan 72-05.
- (c) The Skeleton's best face shatters it, both lives, on every landed to-hit roll aimed at it. M&M's dead "criticals on a 1" clause and field go. Plan 72-06.
- (d) Thief `evasion`: a positive value subtracts from the foe's need. It is 0 at identity, so no fixture moves. Plan 72-04.

`## New findings`. Write a table `Fn | Finding | Evidence (function + text quote) | Class | Proposed disposition`. Class is one of `SIGN BUG (text-backed, local)` (72-07 fixes it without asking), `NEEDS RULING` (text vs canon, or a noticeable difficulty swing, per CONTEXT) or `OUT OF SCOPE` (not a sign or direction issue; recorded only). Verify each of the planner's pre-scan items F1–F5 and O1–O4 from the interfaces block against the code and text. Keep, reword or drop each with a one-line reason. Add every other finding the audit turns up. A dead claim, where text promises a modifier the engine never applies, is a direction finding (the bonus never widens, or the penalty never narrows), so it gets Class SIGN BUG or NEEDS RULING, never OUT OF SCOPE.

`## Skeleton shatter scope`. This is the planner's call under CONTEXT's Claude's Discretion for (c). Record that "your dice" means every drawn to-hit die aimed at a shatter-flagged foe whose strike LANDS: the hero's weapon strikes (including ability strikes and auto-hit openers, because the die is still drawn), party-member strikes, summoned and legacy ally strikes, and hero and member thrown attack spells. One exception: a Con Artist's opening warning blow never shatters, because by rule it deals no injury. An untouchable (need 0) target never shatters, because the strike never lands. The shatter pre-empts the damage roll (no weaponDamage draw on a shatter) and routes through killFoe with the foe's lives set to 1.

`## Handoffs → Phase 74 (display signs)`. List needModsClause's opposite-sense deltas (the function, both line kinds, and one example of each) and any other surface where a modifier's sign reads differently on two surfaces. `## Handoffs → Phase 79 (roll-direction phrasing)`. List every player-facing string the audit saw that encodes roll direction and becomes wrong after the Phase 73 mirror, or is imprecise under the insult (at least the O4 strings).

The BEFORE readout. When the background run's `EXIT=` sentinel appears (poll it with a bounded Monitor or until-loop, never a foreground sleep), insert a new H2 into docs/DIFFICULTY-RETUNE.md IMMEDIATELY BEFORE the line `## v1.2 retune (Phase 27) — TUNE-05..07`: `## v2.1 roll-direction pass (Phase 72) — bot readouts`. Under it add:
- `### Rule changes under measurement`: the four fixes (a)–(d) in one line each.
- `### Parameters`: the exact command.
- `### BEFORE — commit <sha> (phase start, no engine edit)`, with the full stdout verbatim in a fenced code block.

The v1.2 H2 must stay the ledger's last H2. Use Edit (a scoped insertion), never Write, on docs/DIFFICULTY-RETUNE.md. If the run exits non-zero, record the failure and its output instead, and flag it in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js && node -e "const t=require('fs').readFileSync('docs/ROLL-LEDGER.md','utf8');const ids=(t.match(/\[[a-z-]+:[a-z0-9-]+\]/g)||[]);if(ids.length<60){console.error('only '+ids.length+' modifier ids');process.exit(1)}for(const s of ['(a)','(b)','(c)','(d)','NEEDS RULING','72-04','72-05','72-06','needModsClause','19–20']){if(!t.includes(s)){console.error('missing '+s);process.exit(1)}}console.log('ok '+ids.length)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/difficulty-retune-ledger.test.js` passes (the v1.2 H2 is still the last H2).
    - `grep -c "^## v2.1 roll-direction pass (Phase 72) — bot readouts" docs/DIFFICULTY-RETUNE.md` equals 1, and the next H2 after it is `## v1.2 retune (Phase 27) — TUNE-05..07`.
    - The Phase 72 H2 contains `### BEFORE — commit` followed by a fenced block holding the tool's own `Death-depth distribution:` line (or a recorded failure with its output).
    - `docs/ROLL-LEDGER.md` carries at least 60 `[site:source]` ids (the verify command counts them). Every id in the 72-02/72-03 catalogues appears, except ids the audit shows do not exist; those are listed with a reason under `### Ids not yet in a direction test` or in a "Catalogue ids dropped" note.
    - The `## Known fixes (a)–(d)` section names plans 72-04, 72-05 and 72-06 and contains the dual-view string `19–20`.
    - Every row of the `## New findings` table has one of the three Class values, and F1–F5 and O1–O4 each appear, either kept or explicitly dropped with a reason.
    - `## Handoffs → Phase 74` names `needModsClause`.
    - This plan's commits touch only docs/ROLL-LEDGER.md and docs/DIFFICULTY-RETUNE.md: `git diff --name-only <BEFORE sha>..HEAD -- engine/ content/ src/` is empty. 72-02 runs in parallel and touches only test/, so do not gate on test/.
  </acceptance_criteria>
  <done>The ledger is complete for today's engine, every finding is classified and the BEFORE readout is recorded. Nothing outside docs/ changed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Batched rulings for the audit's NEEDS-RULING findings (fires only if there are any)</name>
  <files>docs/ROLL-LEDGER.md</files>
  <action>Count the `## New findings` rows whose Class is NEEDS RULING. If there are none, write "No rulings needed (audit found no text-vs-canon or difficulty-swing findings)." under `## Rulings`, commit, and finish without stopping. Otherwise stop and present every NEEDS-RULING row at once, as described in the context block below. When the user replies, write the rulings table into `## Rulings`, update each finding's Proposed disposition to match, and commit. Change no engine, content, src or test file.</action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/ROLL-LEDGER.md','utf8');const i=t.indexOf('## Rulings');const j=t.indexOf('\n## ',i+5);const s=t.slice(i,j<0?undefined:j);if(i<0||!(s.includes('No rulings needed')||/\|\s*F\d+\s*\|/.test(s))){console.error('Rulings section not filled');process.exit(1)}console.log('ok')"</automated>
  </verify>
  <done>`## Rulings` records either "No rulings needed" or one ruling per NEEDS-RULING finding, with the plan that applies it.</done>
  <read_first>
    - docs/ROLL-LEDGER.md (`## New findings`, the rows whose Class is NEEDS RULING)
    - .planning/phases/72-roll-direction-sign-audit-fixes/72-CONTEXT.md (the "When code and text disagree, the text wins" and "Stop with a checkpoint:decision" decisions)
  </read_first>
  <decision>How should each NEEDS-RULING finding in docs/ROLL-LEDGER.md be handled: fix the engine to match the text, change the text to match the engine or canon, or defer to the backlog?</decision>
  <context>
Precondition: if the `## New findings` table has ZERO rows with Class `NEEDS RULING`, do not stop. Write "No rulings needed (audit found no text-vs-canon or difficulty-swing findings)." under `## Rulings`, commit, and finish the plan.

Otherwise present every NEEDS-RULING row at once, as one batch, never one stop per finding. For each row show its id, a one-sentence finding, the text quote, today's engine behaviour, the proposed disposition, and the difficulty exposure (which heroes and foes it touches). Show each finding in both views where it has a number. CONTEXT locks that a text-vs-canon conflict is never fixed silently. Findings the user rules "fix" are applied by 72-07 Task 1 (bounded to local fixes). The exception is F4 (the frenzy condition): 72-05 applies it in the same edit if ruled "fix". A ruled fix that is not local is recorded as a follow-up for `/gsd-plan-phase 72 --gaps`.
  </context>
  <options>
    <option id="apply-proposed">
      <name>Accept every proposed disposition as written in the ledger</name>
      <pros>One reply; the proposals already follow "the text wins" where the fix is local</pros>
      <cons>Accepts each proposal's difficulty exposure without a per-item look</cons>
    </option>
    <option id="per-finding">
      <name>Rule each finding separately (reply "F1 fix, F2 text, F3 defer, ...")</name>
      <pros>Full control per item</pros>
      <cons>Longer reply</cons>
    </option>
    <option id="defer-all">
      <name>Defer every NEEDS-RULING finding to the backlog; fix nothing new this phase</name>
      <pros>Phase 72 stays exactly the four known fixes</pros>
      <cons>The ledger ships with known, unfixed promise gaps (recorded, not silent)</cons>
    </option>
  </options>
  <resume-signal>Reply "apply-proposed", "defer-all", or per finding: "F1 fix|text|defer, F2 ...". On resume, write each ruling into docs/ROLL-LEDGER.md `## Rulings` as a table `Fn | Ruling (fix/text/defer) | Applied by (72-05/72-07/backlog) | Date | User's words`, update each finding's Proposed disposition to match, and commit.</resume-signal>
  <acceptance_criteria>
    - `docs/ROLL-LEDGER.md` `## Rulings` either says "No rulings needed" or has one table row per NEEDS-RULING finding, each with a ruling of fix, text or defer and the plan that applies it.
    - This plan's commits touch only docs/ROLL-LEDGER.md and docs/DIFFICULTY-RETUNE.md: `git diff --name-only <BEFORE sha>..HEAD -- engine/ content/ src/` is empty. 72-02 runs in parallel and touches only test/, so do not gate on test/.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `node --test test/unit/difficulty-retune-ledger.test.js` passes.
- `docs/ROLL-LEDGER.md` has all twelve H2 sections, at least 60 `[site:source]` ids, the four known fixes with their plans, classified new findings, and a filled `## Rulings`.
- No file under engine/, content/, src/ or test/ changed in this plan.
</verification>

<success_criteria>
ROLL-01 success criterion 1 is satisfied for the inventory half: the ledger covers every die check and every modifier source and confirms each direction or flags it. The BEFORE readout is on record, and every finding that needs the user has been put to the user once.
</success_criteria>

<output>
Create `.planning/phases/72-roll-direction-sign-audit-fixes/72-01-SUMMARY.md` when done. Include the BEFORE commit sha, the tune-difficulty headline numbers (death-depth p50/p90), the count of sites and modifier ids, the New findings table (Class column), the Rulings outcome, and a "Human verification (deferred to end of run)" section. It will read "none" for this plan.
</output>
