---
phase: 79-content-narrative-pass
plan: 13
type: execute
wave: 6
depends_on:
  - 79-12
files_modified:
  - tools/narrative-review.mjs
  - docs/NARRATIVE-PASS.md
  - docs/narrative-pass/review.html
  - test/unit/narrative-review.test.js
  - tools/voice-sample.mjs
  - tools/voice-sample-output.txt
autonomous: true
requirements:
  - VOX-05
  - VOX-04
  - ROLL-04
must_haves:
  truths:
    - "The review page (CONTEXT: 'a before/after REVIEW PAGE grouped by surface, with each changed line, the event it describes and why it changed'): docs/NARRATIVE-PASS.md lists every changed line of Phase 79, grouped by surface in the corpus's fixed surface order, each row giving the trigger (the event and the engine function, the bank or the screen), the line before (from the committed phase-base snapshot), the line after, the rubric reasons and the one-sentence why"
    - "The same content is published as a self-contained HTML page, docs/narrative-pass/review.html (inline CSS, no scripts, no external requests, every string HTML-escaped, readable on a phone and in light or dark), ready for the orchestrator to publish as an artifact at milestone close"
    - "A key changed by more than one plan shows once: the base line, the final line, and every plan's why in order"
    - "The page opens with the pass in numbers (per surface: lines judged, lines changed, lines kept word for word), the four-point rubric, the standing rulings, and how to ask for tone changes (one quick follow-up task, per CONTEXT's review model)"
    - "Doc-synced: test/unit/narrative-review.test.js regenerates both files from the committed ledgers and the base snapshot and fails on any difference (CRLF normalised), and fails on a ledger row with no page row"
    - "VOX-05 ordering: generation is deterministic (surface order, then key, then plan number); two runs are byte-identical"
    - "Coverage at the phase head (one-shot): `node tools/voice-inventory.mjs --check-ledgers --after --coverage` exits 0, so every line that changed between the phase base and the head has a ledger row and every ledger row's after-line exists in the live corpus"
    - "The phase closes with green gates: npm test 0 failures, the parity suite green, test/parity/prototype-master.js.txt and test/parity/fixtures byte-identical to the phase base, and the engine diff since the phase base limited to 79-02's additive gain and Table 4 fields and the string literals 79-05 and 79-11 reworded"
    - statement: "The user reads the review page at milestone close and finds the rewritten lines state what happened, to whom and why, read naturally aloud and keep the family-friendly deadpan voice"
      verification: backstop
  artifacts:
    - path: tools/narrative-review.mjs
      provides: "the page generator (--md, --html, --check)"
      contains: "--check"
    - path: docs/NARRATIVE-PASS.md
      provides: "the before/after review page, grouped by surface"
    - path: docs/narrative-pass/review.html
      provides: "the publishable HTML page"
    - path: test/unit/narrative-review.test.js
      provides: "the doc-synced page guard"
    - path: tools/voice-sample-output.txt
      provides: "the regenerated voice sample"
  key_links:
    - from: tools/narrative-review.mjs
      to: docs/narrative-pass/why/*.json and docs/narrative-pass/corpus-base.json
      via: "the page rows"
      pattern: "corpus-base"
    - from: test/unit/narrative-review.test.js
      to: tools/narrative-review.mjs
      via: "regenerate and compare"
      pattern: "narrative-review"
  prohibitions:
    - statement: "MUST NOT show a 'before' or 'after' line on the review page that the game never actually printed (every before comes from the base snapshot, every after from the live corpus)"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: test
---

<objective>
Produce the review page the user reads at milestone close, prove every changed line is on it, refresh the voice sample, and close the phase with the gates and one Pixel 7 checklist (VOX-05's review model; VOX-04 and ROLL-04's changes are on the same page).

Purpose: CONTEXT's review model is "Claude drafts; the user reviews at milestone close", with no mid-run pauses. The page is the draft, grouped by surface so the user can read the Oracle, the rail, the blurbs and the panels one at a time, and the ledgers make every row provable.

Output: the generator, the Markdown and HTML pages, the doc-sync test, the voice sample, the gate results, the compiled device checklist.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 79-13 creates or changes.)

- 79-01: the voice corpus, checks and CLI, the method README, the base snapshot
- 79-02: honest gain and Table 4 numbers (engine fields and lines)
- 79-03: the generated sub-class and race footers and the audited blurbs
- 79-04: the fight-domain lines
- 79-05: the rules text in content and `test/unit/authored-ranges.test.js`
- 79-06: epitaphs, death, boards and quip copy
- 79-07: combat screen and chip copy
- 79-08: the powers-domain lines
- 79-09: hero, gear, store, boards and final-sheet panels
- 79-10: map, legend, title, roller copy, settings, menus and remaining shell copy
- 79-11: the world-domain lines and the rail cards
- 79-12: the doc-synced ROLL-04 guard, the extended voice guards, the handed-on closure
- **This plan (79-13):** `tools/narrative-review.mjs`, `docs/NARRATIVE-PASS.md`, `docs/narrative-pass/review.html`, `test/unit/narrative-review.test.js`, the regenerated `tools/voice-sample-output.txt` (and `tools/voice-sample.mjs` if it needs the new fields), the gate results and the Pixel 7 checklist

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-content-narrative-pass/79-CONTEXT.md
@docs/narrative-pass/README.md
@docs/CLARITY.md

<interfaces>
**Run order:** every other Phase 79 plan is merged. This plan runs alone. The phase base SHA is `docs/narrative-pass/corpus-base.json`'s `base` field.

Inputs: docs/narrative-pass/corpus-base.json (entries with key, surface, trigger, texts; the SURFACES order from tools/lib/voice-corpus.mjs), docs/narrative-pass/why/79-02.json through 79-12.json (rows `{ key, surface, trigger, before, after, reasons, why }`), the live corpus (buildCorpus) for the per-surface counts. tools/lib/voice-checks.mjs#validateLedgers and the CLI's `--check-ledgers --after --coverage`. docs/CLARITY.md's before/after table is the house precedent for a ledger page. tools/voice-sample.mjs: `SAMPLE_OVERRIDES`, `SAMPLES_PER_TYPE`; it writes tools/voice-sample-output.txt deterministically.

Docs on this machine are CRLF (core.autocrlf=true): compare with CRLF normalised, and write the generated files so a regeneration is byte-identical after checkout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The review page generator and its doc-sync guard</name>
  <files>tools/narrative-review.mjs, test/unit/narrative-review.test.js, docs/NARRATIVE-PASS.md, docs/narrative-pass/review.html</files>
  <read_first>
    - docs/narrative-pass/README.md (surfaces, key scheme, ledger schema, reasons)
    - every docs/narrative-pass/why/*.json and the base snapshot's shape
    - tools/lib/voice-corpus.mjs (SURFACES) and tools/lib/voice-checks.mjs (validateLedgers)
    - docs/CLARITY.md (the before/after table format)
  </read_first>
  <behavior>
    - Given a synthetic base and two synthetic ledgers (one key changed twice), the generator emits one row for that key with the base before, the final after and both whys in plan order; rows are grouped by surface in SURFACES order, then key.
    - The Markdown and the HTML carry the same rows; the HTML escapes <, > and & in every cell and loads nothing external.
    - `--check` exits 0 when the committed files match a fresh generation (CRLF normalised) and 1 with a diff summary otherwise.
    - A ledger row missing from the page, or a page row with no ledger row, fails the test.
    - Two generations are byte-identical.
  </behavior>
  <action>
1. Tests first: test/unit/narrative-review.test.js with the synthetic cases above plus a live case (regenerate from the committed inputs and compare with the committed pages). Watch them fail.
2. tools/narrative-review.mjs (Node built-ins only): read the inputs; collapse chains per key; write `docs/NARRATIVE-PASS.md` with a header (what the pass did; per-surface judged, changed and kept counts from the live corpus and the ledgers; the rubric quoted from CONTEXT; the standing rulings; "how to ask for changes": list the line and the wanted tone, and it goes into one quick follow-up task) and one section per surface with a table: Trigger, Before, After, Why (reasons as tags). Write `docs/narrative-pass/review.html` from the same rows: a self-contained page (inline CSS using the game's parchment palette in light and a dark variant under prefers-color-scheme, a surface index at the top, one table per surface that reflows to stacked cards on a narrow screen, no scripts). Add `--check`.
3. Generate both pages and commit them; make the tests pass.
  </action>
  <verify>
    <automated>node --test test/unit/narrative-review.test.js && node tools/narrative-review.mjs --check</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/narrative-review.test.js` passes, including the chain, escape, determinism and live doc-sync cases.
    - `node tools/narrative-review.mjs --check` exits 0.
    - `grep -c "<script" docs/narrative-pass/review.html` equals 0 and `grep -c -E "(src|href)=\"https?:" docs/narrative-pass/review.html` equals 0.
    - docs/NARRATIVE-PASS.md has one `## ` section per surface that has at least one changed line, in SURFACES order.
  </acceptance_criteria>
  <done>The user has one page, grouped by surface, showing every line Phase 79 changed, what it described and why.</done>
</task>

<task type="auto">
  <name>Task 2: Coverage, the voice sample, the gates and the device checklist</name>
  <files>tools/voice-sample.mjs, tools/voice-sample-output.txt</files>
  <read_first>
    - every .planning/phases/79-content-narrative-pass/79-0N-SUMMARY.md and 79-1N-SUMMARY.md (their human-check lines, open items and gate notes)
    - tools/voice-sample.mjs
    - the four routed todos (.planning/todos/pending/2026-09-21-sub-class-descriptions-..., 2026-09-23-narrative-pass-..., 2026-09-25-heal-lines-..., 2026-09-26-table-4-roll-line-...)
  </read_first>
  <action>
1. Run `node tools/voice-inventory.mjs --check-ledgers --after --coverage` at the phase head; it must exit 0 (a gap means a changed line with no ledger row: add the row in the owning plan's ledger file with its real before and after, regenerate the pages, and say so in the SUMMARY).
2. If tools/voice-sample.mjs never exercises 79-02's `gained` or Table 4 `amount` branches, add one SAMPLE_OVERRIDES entry per gain type and for tableFour (the capped case and the loss case). Run `node tools/voice-sample.mjs`; commit tools/voice-sample-output.txt only if it changed.
3. Gates, each result recorded in the SUMMARY: `npm test` (0 failures); `node --test "test/parity/**/*.test.js"`; `git diff --quiet <phase-base> -- test/parity/prototype-master.js.txt test/parity/fixtures` (exits 0); `git diff --name-only <phase-base> -- engine` (expected: 79-02's gain and Table 4 files, plus string-literal-only hunks from 79-05 and 79-11; show each hunk kind); `git diff <phase-base> -- test/parity/harness/comparables.js` (only 79-05's named carve-outs, if any); `npm run boot:check` (environment-blocked on this machine per STATE; record the result either way); `node tools/voice-inventory.mjs --roll-under --hygiene --twins --safety --count` (0).
4. Compile the Phase 79 Pixel 7 checklist into the SUMMARY: every human-check line from 79-02 through 79-12, numbered, one line each, grouped by requirement (VOX-04, ROLL-04, VOX-05 and the two number-honesty todos), plus "read docs/narrative-pass/review.html (published by the orchestrator) and list any tone changes for one quick follow-up". No device pause now.
5. List the four routed todos as resolved by this phase (the orchestrator moves them), and the review page path for the orchestrator to publish as an HTML artifact at milestone close.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --check-ledgers --after --coverage && npm test && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --check-ledgers --after --coverage` exits 0.
    - `npm test` reports 0 failures and the parity suite passes.
    - `git diff --quiet <phase-base> -- test/parity/prototype-master.js.txt test/parity/fixtures` exits 0.
    - The SUMMARY holds the compiled, numbered Pixel 7 checklist covering VOX-04, ROLL-04, VOX-05 and both number-honesty todos, the review page path, and the four todos marked resolved.
  </acceptance_criteria>
  <done>Phase 79 is closed with every changed line on the review page, green gates and one device checklist for the milestone batch.</done>
</task>

</tasks>

<verification>
- The review page exists in Markdown and HTML, grouped by surface, provably complete (coverage) and in sync (doc-sync test).
- Every gate is green and recorded; the device checks are compiled for the milestone-close batch.
  <human-check>
  At milestone close: the user reads docs/narrative-pass/review.html surface by surface alongside the Pixel 7 checklist and lists any tone changes for one quick follow-up.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 79's three criteria are each met and evidenced: the footers (VOX-04), the doc-synced ROLL-04 guard, and the narrative pass on the review page (VOX-05), with the engine gate intact.
</success_criteria>

<output>
Create `.planning/phases/79-content-narrative-pass/79-13-SUMMARY.md` when done. Include the phase base and head SHAs, the per-surface counts, the coverage result, every gate result, the compiled Pixel 7 checklist, the review page path and the resolved todos.
</output>
