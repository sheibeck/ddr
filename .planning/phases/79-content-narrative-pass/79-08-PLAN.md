---
phase: 79-content-narrative-pass
plan: 08
type: execute
wave: 3
depends_on:
  - 79-04
files_modified:
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/narrationLinesTable.test.js
  - test/unit/spell-utility.test.js
  - test/unit/spell-mechanics.test.js
  - test/unit/foe-abilities.test.js
  - test/unit/ability-pool.test.js
  - test/unit/cast-refusals.test.js
  - test/unit/scroll-cast-copy-note.test.js
  - test/unit/party-abilities.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/rail.test.js
  - docs/narrative-pass/why/79-08.json
autonomous: true
requirements:
  - VOX-05
  - ROLL-04
must_haves:
  truths:
    - "VOX-05, the powers domain (every EVENT_NARRATION and LINE_FOR builder whose type engine/abilities.js, engine/magic.js, engine/scrollFumble.js or an engine/foe*.js file emits and engine/combat.js does not, as `--domain powers` lists it at execution time): every line is judged against the four-point rubric and only failures are rewritten; a passing line is byte-identical to the base"
    - "Spell, ability, foe-ability, scroll-read and scroll-fumble lines name the caster, the target and the result (damage, healing, the effect and its duration) before any joke; refusal lines in this domain say what was refused and why in the player's terms"
    - "ROLL-04 in this domain: every roll-under hit the scan assigns to 79-08 (for example Battle Roar's 'need two better', Mirror Self, Weaken or Smoke narration if they land here) is rewritten to the Phase 74 range or to faces, and `--owner 79-08 --roll-under --count` prints 0"
    - "VOX-05 adjacency: the Oracle line and the rail twin for each event state the same numbers (`--owner 79-08 --twins --count` prints 0), and no rewrite collapses two different events into identical text"
    - "VOX-05 empty and encoding: every builder renders a readable line for a bare `{ type }` event and missing fields, with no leaked undefined, NaN, null, [object or {token}; negatives use U+2212 and ranges U+2013 (`--owner 79-08 --hygiene --count` prints 0)"
    - "Phase 75.1's scroll-read and fumble lines stay consistent with the read bands and the fumble table (a fumble names what went wrong and to whom, never a diagnosis); 77-05's copy-limit note still reads after a successful cast, never as a refusal"
    - "The honest gain numbers 79-02 pinned (healed, potionDrunk and any other powers-domain gain) and the Phase 74 sign guard stay green without edits; every changed key has a ledger row `--check-ledgers --plan 79-08 --after` accepts"
    - "Presentation only: engine/, content/ and test/parity are byte-identical to the plan base"
  artifacts:
    - path: src/browser/eventNarration.js
      provides: "the rewritten powers-domain Oracle lines"
    - path: src/browser/narrationLines.js
      provides: "the rewritten powers-domain rail twins"
    - path: docs/narrative-pass/why/79-08.json
      provides: "one why-ledger row per changed key"
  key_links:
    - from: src/browser/eventNarration.js and src/browser/narrationLines.js
      to: src/browser/rollRange.js
      via: "every roll, range and signed number"
      pattern: "rollRange"
  prohibitions:
    - statement: "MUST NOT let a joke replace the fact of who cast what on whom and what it did"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT print a number, duration or range that differs from the event, or change a rule to match a line"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT introduce profanity, gore or adult content, or name a diagnosis in a fumble, insanity or backfire line"
      requirement_id: VOX-05
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite a line that already passes the rubric"
      requirement_id: VOX-05
      category: values
      status: resolved
      verification: judgment
---

<objective>
Run the narrative pass over the powers: every Oracle and rail line a spell, ability, foe ability or scroll event produces (VOX-05), fixing the roll-under phrases the scan assigns here (ROLL-04).

Purpose: the powers grew the most text in this milestone (Phase 75.1's scroll reading and fumbles, 75.3's resist and hold lines, 77's onset lines). The rubric holds them to the same standard as the fight: the fact first, the joke after, the numbers the engine used.

Output: rewritten lines in the two narration files, re-pinned tests, and the ledger.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 79-08 creates or changes.)

- 79-01: the voice corpus, checks and CLI, the method README, the base snapshot
- 79-02: honest gain and Table 4 numbers (engine fields and lines)
- 79-03: the generated sub-class and race footers and the audited blurbs
- 79-04: the fight-domain lines
- 79-05: the rules text in content and `test/unit/authored-ranges.test.js`
- 79-06: epitaphs, death, boards and quip copy
- 79-07: combat screen and chip copy
- **This plan (79-08):** the powers-domain Oracle and rail lines (`eventNarration.js`, `narrationLines.js`), `docs/narrative-pass/why/79-08.json`
- 79-09: hero, gear, store, boards and final-sheet panels
- 79-10: map, legend, title, roller copy, settings, menus and remaining shell copy
- 79-11: the world-domain lines and the rail cards
- 79-12: the doc-synced ROLL-04 guard, the extended voice guards and the handed-on closure
- 79-13: the review page, the ledger coverage check, the voice sample, the gates and the Pixel 7 checklist

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-content-narrative-pass/79-CONTEXT.md
@docs/narrative-pass/README.md
@docs/CLARITY.md
@.planning/phases/79-content-narrative-pass/79-04-SUMMARY.md

<interfaces>
**Run order:** 79-04 (the fight domain, same two files) is merged; read its SUMMARY for the choices it made so the powers lines match. Read the 75.1-03 to 75.1-06, 75.3-04/05 and 77-05/77-07 SUMMARYs for the lines they added. Wave 3 siblings: 79-07 owns mazeworld.html's combat and condition copy, combatMenu.js, combatPanel.js, conditionEffects.js, heroConditions.js, foeConditions.js, foeDetails.js and their tests; 79-09 owns the hero, gear, store, boards and final-sheet modules, their tests and the shell snapshots. Hand on anything outside this plan's files.

The worklist: `node tools/voice-inventory.mjs --owner 79-08 --table`, plus `--roll-under`, `--hygiene`, `--twins`, `--safety`, `--count`, `--check-ledgers --plan 79-08 --after`. Narration shapes, ORACLE_ONLY and the roller rule for rollRange.js calls as in 79-04's interfaces. 79-02's gain builders (healed, potionDrunk and any other gain type magic.js emits) keep their `gained`-first shape and test/unit/honest-gains.test.js is not edited. test/unit/roll-sign-consistency.test.js must pass unedited.

Standing rulings: HP not WP; U+2212 minus; U+2013 ranges; the house cost shape; family-friendly deadpan; a Pilfer's text never names a diagnosis; card versus rail-line split.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Judge the powers domain and rewrite what fails</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js</files>
  <read_first>
    - docs/narrative-pass/README.md and 79-04's SUMMARY (the choices already made for the fight lines)
    - `node tools/voice-inventory.mjs --owner 79-08 --table` output
    - every builder in scope in both files
    - the engine push site for any line whose accuracy you doubt (the trigger column), read only
  </read_first>
  <action>
1. Judge every key in the worklist against the four rubric points; keep a pass/fail table for the SUMMARY.
2. Rewrite each failing Oracle line and its rail twin together: caster, target and result first, then the joke; costs in the house shape; every number and range through rollRange.js; roll-under hits in the roll-high reading. Keep builders defensive for missing fields. Leave passing lines byte-identical; hand on what you cannot fix here.
3. `node tools/voice-inventory.mjs --owner 79-08 --roll-under --hygiene --twins --safety --count` until it prints 0.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --owner 79-08 --roll-under --hygiene --twins --safety --count && node --test test/unit/formatEventsCoverage.test.js test/unit/narrationLinesCoverage.test.js test/unit/roll-sign-consistency.test.js test/unit/honest-gains.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --owner 79-08 --roll-under --hygiene --twins --safety --count` prints 0.
    - The coverage guards, the sign guard and test/unit/honest-gains.test.js pass with none of those files edited.
    - `git diff --quiet <plan-base> -- engine content test/parity` exits 0.
  </acceptance_criteria>
  <done>Every powers line either passed untouched or now says who did what to whom, and what it did, before the joke.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin and record</name>
  <files>test/unit/narrationLinesTable.test.js, test/unit/spell-utility.test.js, test/unit/spell-mechanics.test.js, test/unit/foe-abilities.test.js, test/unit/ability-pool.test.js, test/unit/cast-refusals.test.js, test/unit/scroll-cast-copy-note.test.js, test/unit/party-abilities.test.js, test/unit/feedback-payload.test.js, test/unit/rail.test.js, docs/narrative-pass/why/79-08.json</files>
  <read_first>
    - `npm test` failures after Task 1
    - docs/narrative-pass/README.md (ledger schema, handed-on rule)
  </read_first>
  <action>
1. Re-pin each failing test whose only difference is a wording this plan changed (a "VOX-05 (79-08)" comment at the pin); fix the line, not the test, for any other failure. Hand on pins outside this list (the SUMMARY names file and test).
2. Write docs/narrative-pass/why/79-08.json and run `node tools/voice-inventory.mjs --check-ledgers --plan 79-08 --after` until it passes.
3. Run `npm test`.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --check-ledgers --plan 79-08 --after && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --check-ledgers --plan 79-08 --after` exits 0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The powers lines are on the record and the suite is green.</done>
</task>

</tasks>

<verification>
- Powers-domain lines pass the rubric; roll-under, hygiene, twins and safety read 0 for this owner; the guards pass unedited.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol): cast a damage spell, a heal, Weaken and a summon; read a scroll as a non-caster until one fumbles; let a foe cast. Each Oracle and rail line names who, whom and the result before any joke.
  </human-check>
</verification>

<success_criteria>
VOX-05 holds for the powers: spell, ability, foe-ability and scroll lines state clearly what happened, to whom and why.
</success_criteria>

<output>
Create `.planning/phases/79-content-narrative-pass/79-08-SUMMARY.md` when done. Include the plan base SHA, the pass/fail table, every changed line (before and after), every re-pinned test, the check counts and "Handed on".
</output>
