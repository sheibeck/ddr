---
phase: 79-content-narrative-pass
plan: 11
type: execute
wave: 4
depends_on:
  - 79-05
  - 79-08
files_modified:
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - src/browser/rail.js
  - src/browser/hazardCard.js
  - engine/movement.js
  - engine/encounters.js
  - test/unit/narrationLinesTable.test.js
  - test/unit/narrativeLines.test.js
  - test/unit/linesForAction.test.js
  - test/unit/oracleLogViewModel.test.js
  - test/unit/rail.test.js
  - test/unit/rail-dismiss.test.js
  - test/unit/hazard-card.test.js
  - test/unit/shell-map-rail.test.js
  - test/unit/one-and-done-lines.test.js
  - test/unit/rations-audit.test.js
  - test/unit/rations-books.test.js
  - test/unit/book-refill-lines.test.js
  - test/unit/loot-narration.test.js
  - test/unit/store-delivery.test.js
  - test/unit/destroyed-armor-swap.test.js
  - test/unit/ailment-narration.test.js
  - test/unit/clarity-cause-lines.test.js
  - test/unit/map-reveal.test.js
  - test/unit/movement.test.js
  - test/unit/dismiss-joiner.test.js
  - test/unit/cutthroat-joiner.test.js
  - test/unit/joiner-level-cap.test.js
  - test/unit/shell-resume-line.test.js
  - test/unit/size-voice.test.js
  - test/unit/worn-slots.test.js
  - docs/narrative-pass/why/79-11.json
autonomous: true
requirements:
  - VOX-05
  - ROLL-04
must_haves:
  truths:
    - "VOX-05, the world domain (every EVENT_NARRATION and LINE_FOR builder `--domain world` lists at execution time: movement, encounters, traps, hazards, the day cycle, rations, items and gear, the store, level-ups, Joiners, darkness, the resume lines, and every shell-stamped or indirect type), plus the rail cards (RAIL_COPY, COMBAT_CARD_KINDS and the hazard decision card): every line is judged against the four-point rubric and only failures are rewritten"
    - "Refusal lines (every refusals-surface key in this domain) say what was refused and why in the player's terms, never just 'You can't'"
    - "ROLL-04 in this domain: the Anklet and Cloak of Invisibility 'unseen' lines and every other roll-under hit the scan assigns to 79-11 read roll-high, and `--owner 79-11 --roll-under --count` prints 0"
    - "VOX-05 adjacency: the Oracle line and the rail twin for each event state the same numbers (`--owner 79-11 --twins --count` prints 0), and no rewrite merges two different events into identical text (the event-order fold keeps folding only true repeats)"
    - "VOX-05 empty and encoding: every builder and card renders a readable line for a bare `{ type }` event and missing fields, with no leaked undefined, NaN, null, [object or {token}; negatives use U+2212 and ranges U+2013 (`--owner 79-11 --hygiene --count` prints 0)"
    - "Card versus rail-line split holds: a decision stays a card (the hazard card, a Joiner offer, a find), a minor event stays a line; no rewrite moves an event between the two"
    - "79-02's honest gain and Table 4 lines (rested, floorRegen, foodFound, faerieBoon, cloakRegenerated, leveled, tableFour, encounterRolled and any other world-domain gain) keep their numbers; test/unit/honest-gains.test.js and test/unit/roll-sign-consistency.test.js pass unedited"
    - "Engine string literals (engine/movement.js and engine/encounters.js prose that reaches the player) change only as strings; the parity suite passes and test/parity is byte-identical to the plan base"
  artifacts:
    - path: src/browser/eventNarration.js
      provides: "the rewritten world-domain Oracle lines"
    - path: src/browser/narrationLines.js
      provides: "the rewritten world-domain rail twins"
    - path: src/browser/rail.js
      provides: "the audited RAIL_COPY and card kinds"
    - path: src/browser/hazardCard.js
      provides: "the audited hazard decision card copy"
    - path: docs/narrative-pass/why/79-11.json
      provides: "one why-ledger row per changed key"
  key_links:
    - from: src/browser/eventNarration.js and src/browser/narrationLines.js
      to: src/browser/rollRange.js
      via: "every roll, range and signed number"
      pattern: "rollRange"
  prohibitions:
    - statement: "MUST NOT let a joke replace the fact of what happened on the step, the floor or the day, and to whom"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT print a number or range that differs from the event, or change a rule, a draw or state to match a line"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT introduce profanity, gore or adult content, or name a diagnosis in an ailment, phobia or insanity line"
      requirement_id: VOX-05
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite a line that already passes the rubric"
      requirement_id: VOX-05
      category: values
      status: resolved
      verification: judgment
---

<objective>
Run the narrative pass over the world: every Oracle and rail line from movement, encounters, traps, hazards, the day, rations, items, the store, Joiners and the relaunch, plus the rail cards themselves (VOX-05), fixing the roll-under phrases the scan assigns here (ROLL-04).

Purpose: this is the largest and most varied domain, and it holds most refusal lines. It runs last among the narration plans so it can match the choices the fight and powers plans already made.

Output: rewritten lines in the two narration files, the rail and hazard card copy, any engine prose strings, re-pinned tests, and the ledger.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 79-11 creates or changes.)

- 79-01: the voice corpus, checks and CLI, the method README, the base snapshot
- 79-02: honest gain and Table 4 numbers (engine fields and lines)
- 79-03: the generated sub-class and race footers and the audited blurbs
- 79-04: the fight-domain lines
- 79-05: the rules text in content and `test/unit/authored-ranges.test.js`
- 79-06: epitaphs, death, boards and quip copy
- 79-07: combat screen and chip copy
- 79-08: the powers-domain lines
- 79-09: hero, gear, store, boards and final-sheet panels
- 79-10: map, legend, title, roller copy, settings, menus and remaining shell copy
- **This plan (79-11):** the world-domain Oracle and rail lines (`eventNarration.js`, `narrationLines.js`), the rail cards (`rail.js`, `hazardCard.js`), any player-facing prose strings in `engine/movement.js` and `engine/encounters.js`, `docs/narrative-pass/why/79-11.json`
- 79-12: the doc-synced ROLL-04 guard, the extended voice guards and the handed-on closure
- 79-13: the review page, the ledger coverage check, the voice sample, the gates and the Pixel 7 checklist

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-content-narrative-pass/79-CONTEXT.md
@docs/narrative-pass/README.md
@docs/CLARITY.md
@.planning/phases/79-content-narrative-pass/79-04-SUMMARY.md
@.planning/phases/79-content-narrative-pass/79-08-SUMMARY.md

<interfaces>
**Run order:** 79-04 and 79-08 (the other two narration domains, same files) and 79-05 (content text and engine/items.js strings) are merged; read their SUMMARYs so the world lines match the choices made. Read the 75-04 (ailments, destroyed armour), 75.2-04 (size lines), 76-03/76-05 (resume lines), 78-01/78-03 (hazard decision) and 78-05 (book refill, spell charge) SUMMARYs. Wave 4 sibling: 79-10 owns mazeworld.html, mapMarks.js, darknessView.js, hudBands.js, roller.js, settings.js, hudMenu.js, arrowPad.js, accountChip.js, playGames.js and their tests. Hand on anything that needs its files.

The worklist: `node tools/voice-inventory.mjs --owner 79-11 --table`, plus `--roll-under`, `--hygiene`, `--twins`, `--safety`, `--count`, `--check-ledgers --plan 79-11 --after`. rail.js: `RAIL_COPY`, `COMBAT_CARD_KINDS`, railCardFor, the direct rail events; hazardCard.js: the pure card view model and its copy (78-03). Narration shapes, ORACLE_ONLY and the roller rule for rollRange.js calls as in 79-04's interfaces. 79-02's world-domain gain and Table 4 builders keep their numbers.

Engine strings: a player-facing literal in engine/movement.js or engine/encounters.js (a `result` or reason string pushed on an event) may be reworded as a string only; engine/encounters.js#tableFour's prose is already number-free (79-02) and changes only if it fails the rubric. Events are not serialized, so an event string needs no carve-out; if a reworded string can reach serialized state, measure and carve out by the stripCloakArmorTxt precedent (test/parity/harness/comparables.js is otherwise not touched here; if a carve-out is needed, stop and hand it to 79-12 with the measurement).

Standing rulings: HP not WP; U+2212 minus; U+2013 ranges; the house cost shape; Dungeon and Game Master naming; family-friendly deadpan; a Pilfer's text never names a diagnosis; card versus rail-line split.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Judge the world domain and the rail cards, rewrite what fails</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js, src/browser/rail.js, src/browser/hazardCard.js, engine/movement.js, engine/encounters.js</files>
  <read_first>
    - docs/narrative-pass/README.md and the 79-04 and 79-08 SUMMARYs
    - `node tools/voice-inventory.mjs --owner 79-11 --table` output
    - every builder in scope in both narration files; rail.js (RAIL_COPY, COMBAT_CARD_KINDS, railCardFor); hazardCard.js
    - the engine push site for any line whose accuracy you doubt, read only except for its own string literal
  </read_first>
  <action>
1. Judge every key in the worklist against the four rubric points (refusals: is the reason in the player's terms?); keep a pass/fail table for the SUMMARY.
2. Rewrite each failing Oracle line and its rail twin together (the fact first, the joke after; costs in the house shape; numbers and ranges through rollRange.js; roll-under hits in the roll-high reading); rewrite failing rail-card and hazard-card copy without changing which events are cards. Engine prose: strings only. Leave passing lines byte-identical; hand on what you cannot fix here.
3. `node tools/voice-inventory.mjs --owner 79-11 --roll-under --hygiene --twins --safety --count` until it prints 0.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --owner 79-11 --roll-under --hygiene --twins --safety --count && node --test test/unit/formatEventsCoverage.test.js test/unit/narrationLinesCoverage.test.js test/unit/roll-sign-consistency.test.js test/unit/honest-gains.test.js && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --owner 79-11 --roll-under --hygiene --twins --safety --count` prints 0.
    - The coverage guards, the sign guard and test/unit/honest-gains.test.js pass with none of those files edited; the parity suite passes and `git diff --quiet <plan-base> -- test/parity content` exits 0.
    - `git diff <plan-base> -- engine` changes string literals only (the SUMMARY shows each hunk).
  </acceptance_criteria>
  <done>Every world line and rail card either passed untouched or now says what happened, to whom and why before the joke.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin and record</name>
  <files>test/unit/narrationLinesTable.test.js, test/unit/narrativeLines.test.js, test/unit/linesForAction.test.js, test/unit/oracleLogViewModel.test.js, test/unit/rail.test.js, test/unit/rail-dismiss.test.js, test/unit/hazard-card.test.js, test/unit/shell-map-rail.test.js, test/unit/one-and-done-lines.test.js, test/unit/rations-audit.test.js, test/unit/rations-books.test.js, test/unit/book-refill-lines.test.js, test/unit/loot-narration.test.js, test/unit/store-delivery.test.js, test/unit/destroyed-armor-swap.test.js, test/unit/ailment-narration.test.js, test/unit/clarity-cause-lines.test.js, test/unit/map-reveal.test.js, test/unit/movement.test.js, test/unit/dismiss-joiner.test.js, test/unit/cutthroat-joiner.test.js, test/unit/joiner-level-cap.test.js, test/unit/shell-resume-line.test.js, test/unit/size-voice.test.js, test/unit/worn-slots.test.js, docs/narrative-pass/why/79-11.json</files>
  <read_first>
    - `npm test` failures after Task 1
    - docs/narrative-pass/README.md (ledger schema, handed-on rule)
  </read_first>
  <action>
1. Re-pin each failing test whose only difference is a wording this plan changed (a "VOX-05 (79-11)" comment at the pin); fix the line for any other failure; hand on pins outside this list.
2. Write docs/narrative-pass/why/79-11.json and run `node tools/voice-inventory.mjs --check-ledgers --plan 79-11 --after` until it passes.
3. Run `npm test`.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --check-ledgers --plan 79-11 --after && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --check-ledgers --plan 79-11 --after` exits 0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The world lines are on the record and the suite is green.</done>
</task>

</tasks>

<verification>
- World-domain lines and rail cards pass the rubric; roll-under, hygiene, twins and safety read 0 for this owner; the guards pass unedited; parity untouched.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol): walk a floor with a trap, a crevice decision, a store, a Joiner offer, a hungry night and a relaunch mid-fight; read the rail and the Oracle: each line says what happened, to whom and why before any joke, and each refusal says why.
  </human-check>
</verification>

<success_criteria>
VOX-05 holds for the world: every Oracle line, rail line and rail card outside the fight and the powers states clearly what happened, to whom and why.
</success_criteria>

<output>
Create `.planning/phases/79-content-narrative-pass/79-11-SUMMARY.md` when done. Include the plan base SHA, the pass/fail table, every changed line (before and after), every re-pinned test, the check counts and "Handed on".
</output>
