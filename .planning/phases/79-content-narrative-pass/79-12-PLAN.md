---
phase: 79-content-narrative-pass
plan: 12
type: execute
wave: 5
depends_on:
  - 79-01
  - 79-02
  - 79-03
  - 79-04
  - 79-05
  - 79-06
  - 79-07
  - 79-08
  - 79-09
  - 79-10
  - 79-11
files_modified:
  - tools/lib/voice-checks.mjs
  - test/unit/roll-phrasing.test.js
  - docs/ROLL-LEDGER.md
  - test/unit/authored-ranges.test.js
  - test/unit/roll-sign-consistency.test.js
  - tools/stale-terms.mjs
  - test/unit/stale-terms.test.js
  - test/voice/safety-scan.test.js
  - test/unit/hp-not-wp.test.js
  - test/voice/narrative-hygiene.test.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - mazeworld.html
  - docs/narrative-pass/why/79-12.json
autonomous: true
requirements:
  - ROLL-04
  - VOX-05
  - VOX-04
must_haves:
  truths:
    - "ROLL-04 (CONTEXT: 'A DOC-SYNCED TEST scans every player-facing string (content txt/note, EVENT_NARRATION templates, line banks, panel copy) for roll-under patterns ... and fails on any'): test/unit/roll-phrasing.test.js runs every ROLL_UNDER_PATTERNS row over the whole live corpus and fails on any hit outside the exceptions list"
    - "Doc-synced: docs/ROLL-LEDGER.md's new '## Phase 79 roll phrasing closure (ROLL-04)' section lists every pattern (id, what it catches, a violation example, the roll-high replacement shape) and every exception (key, phrase, reason); the test fails when the doc's pattern ids or exceptions differ from tools/lib/voice-checks.mjs, or when a doc example does not trip its own pattern"
    - "No rot and no vacuity: every exception matches a live corpus string (a stale exception fails the test), each pattern trips its violation fixture and passes its clean one, and the corpus meets its per-surface floors"
    - "Every '## Handoffs → Phase 79' item in docs/ROLL-LEDGER.md and every '(g) What stays for Phase 79' item is closed, each named in the new section with the plan that closed it"
    - "Authored ranges stay true: test/unit/authored-ranges.test.js covers every stated fixed-die range and face count outside content too (the narration lines, chip sentences, blurbs and legend lines Phase 79 rewrote), each computed from the engine; test/unit/roll-sign-consistency.test.js gains scenarios proving the authored Smoke, Mirror Self, Weaken and invisibility ranges equal the measured ranges on the foe card and the chips under the same state"
    - "The voice guards are extended, not loosened: test/voice/safety-scan.test.js and test/unit/hp-not-wp.test.js each gain a corpus-wide test (every string the corpus holds, including the new footers and every bank Phases 75.1-78 added), their existing tests and allowlist meta-tests stay; the stale-terms tripwire gains an enforced term for the retired roll-under phrases in comments and test titles"
    - "test/voice/narrative-hygiene.test.js holds the whole corpus at zero hygiene hits (VOX-05 empty and encoding: no leaked undefined, NaN, null, [object or {token}; U+2212 minus and U+2013 ranges; no WP, old working title or 'Maze Master'; clean spacing) and zero twin-number disagreements (VOX-05 adjacency), except listed exceptions with reasons"
    - "Every 'Handed on' item in the 79-02 to 79-11 SUMMARYs is closed here (fixed with a ledger row in docs/narrative-pass/why/79-12.json, or recorded as not a failure with the reason)"
    - "Engine gate at the phase head: test/parity/prototype-master.js.txt and test/parity/fixtures are byte-identical to the phase base; this plan changes no engine file"
  artifacts:
    - path: test/unit/roll-phrasing.test.js
      provides: "the doc-synced ROLL-04 guard"
      contains: "ROLL_UNDER_PATTERNS"
    - path: docs/ROLL-LEDGER.md
      provides: "## Phase 79 roll phrasing closure (ROLL-04)"
      contains: "## Phase 79 roll phrasing closure"
    - path: test/voice/narrative-hygiene.test.js
      provides: "corpus-wide hygiene and twin guards"
    - path: test/voice/safety-scan.test.js
      provides: "the corpus-wide family-friendly scan"
    - path: test/unit/hp-not-wp.test.js
      provides: "the corpus-wide HP-not-WP scan"
  key_links:
    - from: test/unit/roll-phrasing.test.js
      to: docs/ROLL-LEDGER.md (the Phase 79 section's tables)
      via: "the doc-sync parse"
      pattern: "Phase 79 roll phrasing closure"
    - from: test/unit/roll-phrasing.test.js, test/voice/narrative-hygiene.test.js, test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js
      to: tools/lib/voice-corpus.mjs#buildCorpus
      via: "the live corpus"
      pattern: "buildCorpus\\("
  prohibitions:
    - statement: "MUST NOT loosen any existing voice guard (no removed test, no widened allowlist without a real in-corpus collision, no deleted pattern) to make the phase pass"
      requirement_id: ROLL-04
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT excuse a genuine roll-under phrase with an exception; an exception is only for a phrase that is not about a roll"
      requirement_id: ROLL-04
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Lock the pass in: the doc-synced ROLL-04 guard, the extended voice guards (safety, HP-not-WP, stale terms, the sign guard), a corpus-wide hygiene and twin guard, and the closure of every item the rewrite plans handed on (ROLL-04, VOX-05, and VOX-04 through the corpus scans over the new footers).

Purpose: ROADMAP criterion 2 asks for a test that pins that no roll-under phrasing remains, and the orchestrator's guardrail says the existing voice guards must be extended to the new text, not loosened. Running them over the same corpus the rewrite plans used means the guard sees exactly what the player sees.

Output: two new tests, four extended tests and tools, the ROLL-LEDGER closure section, the handed-on fixes and their ledger.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 79-12 creates or changes.)

- 79-01: the voice corpus, checks and CLI, the method README, the base snapshot
- 79-02: honest gain and Table 4 numbers (engine fields and lines)
- 79-03: the generated sub-class and race footers and the audited blurbs
- 79-04: the fight-domain lines
- 79-05: the rules text in content and `test/unit/authored-ranges.test.js`
- 79-06: epitaphs, death, boards and quip copy
- 79-07: combat screen and chip copy
- 79-08: the powers-domain lines
- 79-09: hero, gear, store, boards and final-sheet panels
- 79-10: map, legend, title, roller copy, settings, menus and remaining shell copy
- 79-11: the world-domain lines and the rail cards
- **This plan (79-12):** `test/unit/roll-phrasing.test.js`, the `## Phase 79 roll phrasing closure (ROLL-04)` section of `docs/ROLL-LEDGER.md`, the finalized exceptions in `tools/lib/voice-checks.mjs`, the extended `authored-ranges.test.js`, `roll-sign-consistency.test.js`, `stale-terms.mjs`, `safety-scan.test.js` and `hp-not-wp.test.js`, `test/voice/narrative-hygiene.test.js`, the handed-on fixes and `docs/narrative-pass/why/79-12.json`
- 79-13: the review page (`docs/NARRATIVE-PASS.md`, `docs/narrative-pass/review.html`), the ledger coverage check, the voice sample, the gates and the Pixel 7 checklist

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-content-narrative-pass/79-CONTEXT.md
@docs/narrative-pass/README.md
@.planning/phases/74-roll-display-modifier-honesty/74-08-SUMMARY.md

<interfaces>
**Run order:** every other Phase 79 plan except 79-13 is merged. Read every 79-0N SUMMARY's "Handed on" list and check counts. This plan runs alone in its wave.

tools/lib/voice-checks.mjs (79-01): `ROLL_UNDER_PATTERNS`, `ROLL_PHRASING_EXCEPTIONS`, `HYGIENE_RULES`, `HYGIENE_EXCEPTIONS`, `scanRollUnder`, `scanHygiene`, `scanTwins`, `scanSafety`, `validateLedgers`; tools/lib/voice-corpus.mjs `buildCorpus`. test/unit/roll-ledger-sync.test.js is the doc-sync precedent (parse a named section, normalise CRLF, compare with code). docs/ROLL-LEDGER.md is CRLF; 74-08 appended its section append-only with a small node script so the diff has zero deletion lines; do the same. test/voice/safety-scan.test.js: `collectAuthoredStrings`, `findBannedTerms`, the allowlist completeness and load-bearing meta-tests. test/unit/hp-not-wp.test.js: `PLAYER_WP` and its self-check. tools/stale-terms.mjs: `TERMS`, `ALLOWED` (survivor classes A to D), the rot rule; test/unit/stale-terms.test.js pins zero unlisted and zero rot. test/unit/roll-sign-consistency.test.js drives the real engine (`playerStrike`, `foeTurn`, `flee`) and harvests signed tokens and ranges from each surface. test/unit/authored-ranges.test.js (79-05) has one row per authored range in content.

Handed-on fixes may touch any file; the likeliest are the two narration files and mazeworld.html (listed). Any other file touched is named in the SUMMARY. Engine files are not touched here; a handed-on engine string goes back to the orchestrator as an open item.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The doc-synced ROLL-04 guard and the range pins</name>
  <files>tools/lib/voice-checks.mjs, test/unit/roll-phrasing.test.js, docs/ROLL-LEDGER.md, test/unit/authored-ranges.test.js, test/unit/roll-sign-consistency.test.js, tools/stale-terms.mjs, test/unit/stale-terms.test.js</files>
  <read_first>
    - tools/lib/voice-checks.mjs and tools/lib/voice-corpus.mjs
    - test/unit/roll-ledger-sync.test.js (the doc-sync pattern)
    - docs/ROLL-LEDGER.md ("Handoffs → Phase 79", "(g) What stays for Phase 79", the last section's CRLF append convention)
    - test/unit/authored-ranges.test.js and test/unit/roll-sign-consistency.test.js
    - tools/stale-terms.mjs and test/unit/stale-terms.test.js
    - every 79-0N SUMMARY (which plan closed which handoff)
  </read_first>
  <behavior>
    - roll-phrasing.test.js: zero corpus hits outside ROLL_PHRASING_EXCEPTIONS; every exception matches a live string; every pattern trips its violation and passes its clean fixture; the doc's pattern ids and exceptions equal the code's; every doc example trips its pattern; floors met.
    - authored-ranges.test.js: a row for every fixed-die range or face count Phase 79 wrote outside content (narration, chips, blurbs, legend, epitaphs), computed from the engine.
    - roll-sign-consistency.test.js: under Smoke, Mirror Self, a Weakened foe and an Anklet of Invisibility, the authored text's range equals the foe card's odds line and the chip's measured clause.
    - stale-terms: a new enforced term for the retired roll-under phrase families; zero unlisted and zero rot.
  </behavior>
  <action>
1. Tests first: create test/unit/roll-phrasing.test.js (behaviour above, reading the doc section with CRLF normalised). Watch it fail (the doc section does not exist yet).
2. Prune ROLL_PHRASING_EXCEPTIONS and HYGIENE_EXCEPTIONS to the live, genuinely non-roll entries, each with its reason.
3. Append `## Phase 79 roll phrasing closure (ROLL-04)` to docs/ROLL-LEDGER.md (append-only, CRLF, by node script): the phrasing rule (fixed die: the Phase 74 range; scaling die: faces; a mishap on a 1 is canon), the patterns table, the exceptions table, and the closure list naming each handoff and the plan that closed it.
4. Extend test/unit/authored-ranges.test.js and test/unit/roll-sign-consistency.test.js per the behaviour; add the stale-terms term with its allowed survivors (class A: tests asserting the retirement; class B: prose naming the retirement) and keep its test green.
  </action>
  <verify>
    <automated>node --test test/unit/roll-phrasing.test.js test/unit/authored-ranges.test.js test/unit/roll-sign-consistency.test.js test/unit/stale-terms.test.js test/unit/roll-ledger-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/roll-phrasing.test.js test/unit/authored-ranges.test.js test/unit/roll-sign-consistency.test.js test/unit/stale-terms.test.js test/unit/roll-ledger-sync.test.js` passes.
    - `grep -c "^## Phase 79 roll phrasing closure" docs/ROLL-LEDGER.md` equals 1 and `git diff <phase-base> -- docs/ROLL-LEDGER.md` shows no deletion lines from Phase 79 commits.
    - `node tools/voice-inventory.mjs --roll-under --count` prints 0.
  </acceptance_criteria>
  <done>A test that reads the ledger fails the day any player-facing string reads roll-under again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The voice guards cover the whole corpus; close what was handed on</name>
  <files>test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js, test/voice/narrative-hygiene.test.js, src/browser/eventNarration.js, src/browser/narrationLines.js, mazeworld.html, docs/narrative-pass/why/79-12.json</files>
  <read_first>
    - test/voice/safety-scan.test.js and test/unit/hp-not-wp.test.js (every existing test, the meta-tests)
    - tools/lib/voice-checks.mjs (HYGIENE_RULES, scanTwins)
    - every 79-0N SUMMARY's "Handed on" list
    - docs/narrative-pass/README.md (rubric, ledger)
  </read_first>
  <behavior>
    - safety-scan.test.js gains a test: every corpus string, templates filled with the closed vocabularies, has zero banned terms; if the corpus surfaces a genuine new collision on game vocabulary, the corpus joins collectAuthoredStrings so the allowlist meta-tests still hold.
    - hp-not-wp.test.js gains a test: no corpus string has a standalone wp/WP, using the same PLAYER_WP (with a sameness assertion if the corpus module keeps its own copy).
    - narrative-hygiene.test.js: zero hygiene hits and zero twin disagreements over the whole corpus outside the listed exceptions; a stale exception fails.
    - Every handed-on item is either fixed (a why row in 79-12.json) or recorded as a non-failure with its reason.
  </behavior>
  <action>
1. Tests first: add the corpus-wide tests to safety-scan.test.js and hp-not-wp.test.js (keeping every existing test) and create test/voice/narrative-hygiene.test.js. Run them.
2. Work through every "Handed on" item and every hit the new tests find: fix the line by the rubric (the owning plan's reasoning in its SUMMARY is the brief), add its why row, re-pin its test. Engine strings go back to the orchestrator as an open item instead.
3. `node tools/voice-inventory.mjs --roll-under --hygiene --twins --safety --count` prints 0 for the whole corpus; `node tools/voice-inventory.mjs --check-ledgers --plan 79-12 --after` passes.
4. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js test/voice/narrative-hygiene.test.js && node tools/voice-inventory.mjs --roll-under --hygiene --twins --safety --count && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js test/voice/narrative-hygiene.test.js` passes; `git diff <plan-base> -- test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js` shows only added lines apart from import lists.
    - `node tools/voice-inventory.mjs --roll-under --hygiene --twins --safety --count` prints 0.
    - `git diff --quiet <phase-base> -- test/parity/prototype-master.js.txt test/parity/fixtures` exits 0 and `git diff --quiet <plan-base> -- engine` exits 0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every voice guard sees every string the player sees, and nothing handed on is left open.</done>
</task>

</tasks>

<verification>
- ROLL-04's doc-synced guard is live with no rot; the range pins bind authored text to the engine on every surface.
- The safety, HP-not-WP, stale-terms and sign guards are extended to the full corpus; hygiene and twins read 0; nothing handed on is left open.
- Engine, parity fixtures and the prototype master are unchanged.
</verification>

<success_criteria>
ROADMAP Phase 79 criterion 2 holds with its test: no player-facing string encodes roll-under phrasing, and a doc-synced test pins that none remains. The voice guards cover every new line.
</success_criteria>

<output>
Create `.planning/phases/79-content-narrative-pass/79-12-SUMMARY.md` when done. Include the plan base SHA, the final pattern and exception tables, the new guard tests and their counts, every handed-on item and how it closed, and any open item returned to the orchestrator.
</output>
