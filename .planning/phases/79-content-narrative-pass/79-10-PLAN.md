---
phase: 79-content-narrative-pass
plan: 10
type: execute
wave: 4
depends_on:
  - 79-03
  - 79-07
  - 79-09
files_modified:
  - mazeworld.html
  - src/browser/mapMarks.js
  - src/browser/darknessView.js
  - src/browser/hudBands.js
  - src/browser/roller.js
  - src/browser/settings.js
  - src/browser/hudMenu.js
  - src/browser/arrowPad.js
  - src/browser/accountChip.js
  - src/browser/playGames.js
  - test/unit/mapMarks.test.js
  - test/unit/dark-surfaces.test.js
  - test/unit/hudBands.test.js
  - test/unit/roller.test.js
  - test/unit/settings.test.js
  - test/unit/hudMenu.test.js
  - test/unit/hud-menu-layout.test.js
  - test/unit/arrow-pad.test.js
  - test/unit/shell-arrow-pad.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/shell-hearing.test.js
  - test/unit/settings-volume-shell.test.js
  - test/unit/dead-lockdown.test.js
  - docs/narrative-pass/why/79-10.json
autonomous: true
requirements:
  - VOX-05
  - ROLL-04
must_haves:
  truths:
    - "VOX-05 for the map and the shell (CONTEXT scope: 'Panel copy (hero, gear, store, camp, title, settings, boards)'): the MARKS legend and hold-inspect copy, the darkness chip and its tap card, the HUD bands' fixed copy, the title screen, the roller's copy, camp, Settings, the ☰ menu, the arrow pad, the account chip and every other mazeworld.html literal and markup text not owned by 79-07, 79-09 or 79-11 is judged against the four-point rubric; only failures change"
    - "ROLL-04 on these surfaces: the LOCKED BOX legend and any other stated roll read roll-high, pinned to the engine's lock check in test/unit/mapMarks.test.js, and `--owner 79-10 --roll-under --count` prints 0"
    - "Static panel headings in mazeworld.html that 79-09 handed on (if any) are judged here"
    - "VOX-05 empty and encoding: every string renders without leaked undefined, NaN, null or {token}; U+2212 and U+2013 where signs and ranges appear (`--owner 79-10 --hygiene --count` prints 0); settings and menu labels stay short enough for their rows (no new clipping at text size L)"
    - "Naming: no player-facing string uses the old working title or 'WP'; 'Dungeon' and 'Game Master' stand"
    - "Presentation only: engine/, content/ and test/parity are byte-identical to the plan base; the roller's identity footer from 79-03 is not rewritten here"
    - statement: "On the Pixel 7 at text size L, every rewritten menu, settings and legend label still fits its row"
      verification: backstop
  artifacts:
    - path: src/browser/mapMarks.js
      provides: "the audited legend and hold-inspect copy with the roll-high LOCKED BOX line"
    - path: mazeworld.html
      provides: "audited title, camp, settings, map and static panel copy"
    - path: docs/narrative-pass/why/79-10.json
      provides: "one why-ledger row per changed string"
  key_links:
    - from: test/unit/mapMarks.test.js
      to: the engine's lock check faces and src/browser/rollRange.js#facesRangeText
      via: "the LOCKED BOX legend's pinned range"
      pattern: "facesRangeText"
  prohibitions:
    - statement: "MUST NOT change a label so it no longer names what the control does (a Settings row, a menu action, an arrow-pad mode)"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT reintroduce the old working title, 'WP' or 'Maze Master' in any player-facing string"
      requirement_id: VOX-05
      category: values
      status: resolved
      verification: test
---

<objective>
Run the narrative pass over the map, the title and roller, Settings, the ☰ menu and everything else left in mazeworld.html and the small shell modules (VOX-05), including the legend's roll-under lock line (ROLL-04).

Purpose: this is the last panel sweep, so it also closes the static markup other panel plans handed on. Labels here are short; the rubric's first point is often simply "does this label say what the control does".

Output: audited copy in mazeworld.html and nine modules, a pinned legend range, re-pinned tests, and the ledger.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 79-10 creates or changes.)

- 79-01: the voice corpus, checks and CLI, the method README, the base snapshot
- 79-02: honest gain and Table 4 numbers (engine fields and lines)
- 79-03: the generated sub-class and race footers and the audited blurbs
- 79-04: the fight-domain lines
- 79-05: the rules text in content and `test/unit/authored-ranges.test.js`
- 79-06: epitaphs, death, boards and quip copy
- 79-07: combat screen and chip copy
- 79-08: the powers-domain lines
- 79-09: hero, gear, store, boards and final-sheet panels
- **This plan (79-10):** audited copy in mazeworld.html (map, title, camp, settings, static panel markup), `mapMarks.js`, `darknessView.js`, `hudBands.js`, `roller.js` (ROLLER_COPY), `settings.js`, `hudMenu.js`, `arrowPad.js`, `accountChip.js`, `playGames.js`; the pinned LOCKED BOX range; `docs/narrative-pass/why/79-10.json`
- 79-11: the world-domain lines and the rail cards
- 79-12: the doc-synced ROLL-04 guard, the extended voice guards and the handed-on closure
- 79-13: the review page, the ledger coverage check, the voice sample, the gates and the Pixel 7 checklist

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-content-narrative-pass/79-CONTEXT.md
@docs/narrative-pass/README.md
@.planning/phases/79-content-narrative-pass/79-07-SUMMARY.md
@.planning/phases/79-content-narrative-pass/79-09-SUMMARY.md

<interfaces>
**Run order:** 79-03 (roller footer, mazeworld.html CSS) and 79-07 (mazeworld.html's combat and condition copy) are merged; read 79-09's SUMMARY for any static panel heading it handed on. Read the 76-02 (darkness chip copy), 78-02 (text size), 78-03 (MARKS copy), 78-06 (dead state), 78-07 (arrow pad) and 78-09 (hearing marks) SUMMARYs. Wave 4 sibling: 79-11 owns src/browser/eventNarration.js, narrationLines.js, rail.js, hazardCard.js and the world-domain narration, rail and hazard tests (rail.test.js, hazard-card.test.js, shell-map-rail.test.js and the others it lists). Hand on anything that needs its files.

The worklist: `node tools/voice-inventory.mjs --owner 79-10 --table`, plus `--roll-under`, `--hygiene`, `--safety`, `--count`, `--check-ledgers --plan 79-10 --after`. mazeworld.html raw entries are keyed by their enclosing const or function (for example `raw:mazeworld.html#MAP_COPY`), plus markup text. The lock check the LOCKED BOX legend describes: engine/encounters.js#openChest (its d10 faces constant; read only).

Standing rulings: HP not WP; U+2212 minus; U+2013 ranges; Dungeon and Game Master naming (never the old working title); family-friendly deadpan; tap-to-move is the default and the arrow pad is opt-in (copy must not imply otherwise); card versus rail-line split.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The map legend and the lock line</name>
  <files>src/browser/mapMarks.js, src/browser/darknessView.js, test/unit/mapMarks.test.js, test/unit/dark-surfaces.test.js</files>
  <read_first>
    - src/browser/mapMarks.js (the legend rows, hold-inspect copy) and test/unit/mapMarks.test.js
    - src/browser/darknessView.js and test/unit/dark-surfaces.test.js
    - engine/encounters.js#openChest (the lock faces), src/browser/rollRange.js (facesRangeText)
    - `node tools/voice-inventory.mjs --owner 79-10 --surface map --table` output
  </read_first>
  <behavior>
    - The LOCKED BOX legend row states the roll-high opening range, equal to facesRangeText(the engine's lock faces, 10), and names what a failure costs.
    - Every legend row and hold-inspect line states what the mark means before any flavour.
  </behavior>
  <action>
1. Test first: pin the LOCKED BOX range to the engine in test/unit/mapMarks.test.js. Watch it fail.
2. Judge every legend, mark and darkness string against the rubric; rewrite failures (the lock line in the roll-high reading).
3. Re-pin mapMarks.test.js and dark-surfaces.test.js where they pinned a changed line.
  </action>
  <verify>
    <automated>node --test test/unit/mapMarks.test.js test/unit/dark-surfaces.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/mapMarks.test.js test/unit/dark-surfaces.test.js` passes, including the pinned lock range.
    - `node tools/voice-inventory.mjs --owner 79-10 --surface map --roll-under --count` prints 0.
  </acceptance_criteria>
  <done>The map's legend reads roll-high and says what each mark means.</done>
</task>

<task type="auto">
  <name>Task 2: Title, roller, settings, menus and the rest of the shell; record</name>
  <files>mazeworld.html, src/browser/hudBands.js, src/browser/roller.js, src/browser/settings.js, src/browser/hudMenu.js, src/browser/arrowPad.js, src/browser/accountChip.js, src/browser/playGames.js, test/unit/hudBands.test.js, test/unit/roller.test.js, test/unit/settings.test.js, test/unit/hudMenu.test.js, test/unit/hud-menu-layout.test.js, test/unit/arrow-pad.test.js, test/unit/shell-arrow-pad.test.js, test/unit/shell-map-hud.test.js, test/unit/shell-hearing.test.js, test/unit/settings-volume-shell.test.js, test/unit/dead-lockdown.test.js, docs/narrative-pass/why/79-10.json</files>
  <read_first>
    - `node tools/voice-inventory.mjs --owner 79-10 --table` output
    - docs/narrative-pass/README.md
    - each module in the files list and the mazeworld.html raw entries in scope
  </read_first>
  <action>
1. Judge every string in scope against the rubric; rewrite failures (labels say what the control does; notices say what happened and what to do next; keep labels short enough for their rows).
2. `node tools/voice-inventory.mjs --owner 79-10 --roll-under --hygiene --safety --count` until it prints 0.
3. Re-pin each test whose only difference is a wording this plan changed (a "VOX-05 (79-10)" comment); fix the copy for any other failure; hand on pins outside this list.
4. Write docs/narrative-pass/why/79-10.json and run `node tools/voice-inventory.mjs --check-ledgers --plan 79-10 --after` until it passes.
5. Run `npm test`.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --owner 79-10 --roll-under --hygiene --safety --count && node tools/voice-inventory.mjs --check-ledgers --plan 79-10 --after && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --owner 79-10 --roll-under --hygiene --safety --count` prints 0.
    - `node tools/voice-inventory.mjs --check-ledgers --plan 79-10 --after` exits 0.
    - `git diff --quiet <plan-base> -- engine content test/parity` exits 0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The last panel copy is swept and on the record.</done>
</task>

</tasks>

<verification>
- Map, title, roller, settings, menu and remaining shell copy passes the rubric; the checks read 0 for this owner; the lock line is pinned to the engine.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol): open MARKS and hold a locked box; open Settings and the ☰ menu at text size L; start a new run from the title through the roller. Every label says what it does and nothing clips.
  </human-check>
</verification>

<success_criteria>
VOX-05 and ROLL-04 hold for the map, the title, Settings, the menus and the rest of the shell copy.
</success_criteria>

<output>
Create `.planning/phases/79-content-narrative-pass/79-10-SUMMARY.md` when done. Include the plan base SHA, every changed string (before and after), the re-pinned tests, the headings closed for 79-09 and "Handed on".
</output>
