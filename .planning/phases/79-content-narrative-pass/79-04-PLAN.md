---
phase: 79-content-narrative-pass
plan: 04
type: execute
wave: 2
depends_on:
  - 79-01
  - 79-02
files_modified:
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - src/browser/fightLog.js
  - test/unit/narrationLinesTable.test.js
  - test/unit/fightLog.test.js
  - test/unit/fight-log-worst-case.test.js
  - test/unit/fight-log-oldest-row.test.js
  - test/unit/fight-log-sheet.test.js
  - test/unit/initiative-line.test.js
  - test/unit/flee-retune.test.js
  - test/unit/party-combat.test.js
  - test/unit/roll-display-lines.test.js
  - test/unit/event-order-fold.test.js
  - test/unit/fixtures/event-order/default-fold-corpus.json
  - test/unit/round-summary-band.test.js
  - test/unit/shell-fight-log.test.js
  - test/unit/rail.test.js
  - docs/narrative-pass/why/79-04.json
autonomous: true
requirements:
  - VOX-05
  - ROLL-04
must_haves:
  truths:
    - "VOX-05, the fight domain (every EVENT_NARRATION and LINE_FOR builder whose type engine/combat.js emits, as `--domain fight` lists it at execution time, plus THE FIGHT SO FAR and the round strip in fightLog.js): every line is judged against the four-point rubric, and every line that fails is rewritten; a line that passes is byte-identical to the base"
    - "The rubric (CONTEXT): (1) states what happened, to whom and with what result, with numbers where the player needs them; (2) reads naturally aloud; (3) the deadpan, family-friendly joke comes after the fact, never instead of it; (4) accurate to what the engine did"
    - "Strike and blow lines keep the Phase 34 fight-log dice-first shape (docs/CLARITY.md) and every roll, range and modifier still comes from rollRange.js with the explicit roller, so the Phase 74 sign guard stays green unchanged"
    - "ROLL-04 in the fight domain: every roll-under hit the 79-01 scan assigns to 79-04 is rewritten (a fixed die in the Phase 74 range format, a scaling die in faces), and `--owner 79-04 --roll-under --count` prints 0"
    - "VOX-05 adjacency: the Oracle line and the rail twin for the same event state the same numbers (`--owner 79-04 --twins --count` prints 0), and no rewrite makes two different events render the same text, so the event-order fold still folds only genuinely identical back-to-back lines; the fold corpus fixture is regenerated deliberately and named"
    - "VOX-05 empty: every builder in the domain still renders a readable line for a bare `{ type }` event and for missing fields, with no leaked undefined, NaN, null, [object or {token} (`--owner 79-04 --hygiene --count` prints 0)"
    - "VOX-05 encoding: negative numbers use U+2212 and ranges U+2013 (never a hyphen-minus before a digit); names interpolated into markup stay escaped"
    - "The honest gain and Table 4 numbers 79-02 pinned stay pinned (its tests unchanged); every changed key has a why-ledger row that `--check-ledgers --plan 79-04 --after` accepts"
    - "Presentation only: engine/, content/ and test/parity are byte-identical to the plan base"
  artifacts:
    - path: src/browser/eventNarration.js
      provides: "the rewritten fight-domain Oracle lines"
    - path: src/browser/narrationLines.js
      provides: "the rewritten fight-domain rail twins"
    - path: src/browser/fightLog.js
      provides: "the audited THE FIGHT SO FAR and round-strip copy"
    - path: docs/narrative-pass/why/79-04.json
      provides: "one why-ledger row per changed key"
  key_links:
    - from: src/browser/eventNarration.js and src/browser/narrationLines.js
      to: src/browser/rollRange.js
      via: "every roll, range, modifier and signed number"
      pattern: "rollRange"
    - from: docs/narrative-pass/why/79-04.json
      to: docs/narrative-pass/corpus-base.json
      via: "each row's before is a base rendering (validated)"
      pattern: "oracle:"
  prohibitions:
    - statement: "MUST NOT let a joke replace or blur the fact: who acted, on whom, and what the result was come first in every rewritten line"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT print a number, range or modifier that differs from the event the engine emitted, or change any rule to make a line true"
      requirement_id: VOX-05
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT introduce profanity, gore or adult content; the darkness is in the wit"
      requirement_id: VOX-05
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT rewrite a line that already passes the rubric (the deferred full voice rewrite stays out)"
      requirement_id: VOX-05
      category: values
      status: resolved
      verification: judgment
---

<objective>
Run the narrative pass over the fight: every Oracle line, rail line and fight-log line an engine/combat.js event produces, plus the fight log's own copy (VOX-05), fixing the roll-under phrases the scan assigns here (ROLL-04).

Purpose: the fight is where the player most needs to know what happened, to whom and why, and where most lines were written quickly across many milestones. The user's rule: "a joke that hides the fact reads as a bug". Only failing lines change, so the churn and the re-pins stay proportional.

Output: rewritten lines in the two narration files and fightLog.js, re-pinned tests, a regenerated fold corpus, and the ledger.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 79-04 creates or changes.)

- 79-01: the voice corpus, checks and CLI, the method README, the base snapshot
- 79-02: honest gain and Table 4 numbers (engine fields and lines)
- 79-03: the generated sub-class and race footers and the audited blurbs
- **This plan (79-04):** the fight-domain Oracle and rail lines (`eventNarration.js`, `narrationLines.js`), THE FIGHT SO FAR and round-strip copy (`fightLog.js`), the regenerated fold corpus, `docs/narrative-pass/why/79-04.json`
- 79-05: the rules text in content and `test/unit/authored-ranges.test.js`
- 79-06: epitaphs, death, boards and quip copy
- 79-07: combat screen and chip copy
- 79-08: the powers-domain lines
- 79-09: hero, gear, store, boards and final-sheet panels
- 79-10: map, legend, title, roller copy, settings, menus and remaining shell copy
- 79-11: the world-domain lines and the rail cards
- 79-12: the doc-synced ROLL-04 guard, the extended voice guards and the handed-on closure
- 79-13: the review page, the ledger coverage check, the voice sample, the gates and the Pixel 7 checklist

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/79-content-narrative-pass/79-CONTEXT.md
@docs/narrative-pass/README.md
@docs/CLARITY.md
@.planning/phases/79-content-narrative-pass/79-01-SUMMARY.md
@.planning/phases/79-content-narrative-pass/79-02-SUMMARY.md

<interfaces>
**Run order:** 79-01 (the corpus and CLI) and 79-02 (honest gains, Table 4) are merged. Wave 2 siblings: 79-05 owns the content/*.js rules text, engine/items.js, engine/economy.js, test/parity/harness/comparables.js and the gear, store and hero snapshots; 79-06 owns content/epitaphs.js, content/boards.js, content/placement.js, content/account.js, src/browser/missLines.js and src/browser/scoreTag.js. Do not edit their files; a failing line that only they can fix, or whose only pin is in their files, goes in the SUMMARY's "Handed on" list (docs/narrative-pass/README.md).

The worklist: `node tools/voice-inventory.mjs --owner 79-04 --table` (every fight-domain key, its trigger and its current renderings), `--roll-under`, `--hygiene`, `--twins`, `--safety` (add `--count` for the number), `--check-ledgers --plan 79-04 --after` for the ledger. 79-01's SUMMARY has the base worklist.

Narration shapes: EVENT_NARRATION builders `(e) => html` (span classes hit/miss/hurt/roll/beat/banner); LINE_FOR builders `(e, ctx) => { text, tone, priority }`; `linesForAction` feeds the rail (rail.js#railCardFor) and THE FIGHT SO FAR (fightLog.js#fightLogLinesFor, event order since 77-02, adjacent-only folding); `ORACLE_ONLY`. fightLog.js: `ROUND_STRIP_COPY`, fightLogByRound. The roll lines use rollRange.js's rangeText, rollVsText, modsClause with an explicit ROLLERS value (the one-formatter guard in test/unit/roll-sign-consistency.test.js must stay green without edits). 79-02's gain builders (regenerated, secondWindHealed, cooked and any other gain type combat.js emits) keep their `gained`-first shape; test/unit/honest-gains.test.js is not edited here.

The why-ledger row: `{ key, surface, trigger, before, after, reasons, why }` (docs/narrative-pass/README.md); one row per changed key, `before` a base rendering (or an earlier plan's `after` for a key 79-02 already changed).

Standing rulings: HP not WP; U+2212 minus; U+2013 ranges; the house line shape "Cause: plain why. −N unit." for costs; Dungeon and Game Master naming; family-friendly deadpan; a Pilfer's text never names a diagnosis; card versus rail-line split (a line never becomes a card here).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Judge the fight domain and rewrite what fails</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js, src/browser/fightLog.js</files>
  <read_first>
    - docs/narrative-pass/README.md (the rubric, the house shape, the roll phrasing rule, the ledger)
    - `node tools/voice-inventory.mjs --owner 79-04 --table` output (every key and rendering in scope)
    - src/browser/eventNarration.js and src/browser/narrationLines.js (every builder in scope, and the shared helpers they call)
    - src/browser/fightLog.js (ROUND_STRIP_COPY and the sheet copy)
    - engine/combat.js at the push site of any event whose line you are unsure is accurate (the trigger column names it)
  </read_first>
  <action>
1. For every key in the worklist, read its renderings against the four rubric points and decide pass or fail; keep a working table (key, verdict, failed points, note) for the SUMMARY.
2. Rewrite each failing Oracle line and its rail twin together, in the game's deadpan voice: the fact first (who, to whom, the result, the numbers the player needs), the joke after; strike lines keep the dice-first shape; costs keep "Cause: plain why. −N unit."; every number through rollRange.js. Fix every roll-under hit the scan lists here (fixed die: the Phase 74 range; scaling die: faces). Keep every builder defensive for missing fields.
3. Leave every passing line byte-identical. Record any failing line you cannot fix here in "Handed on".
4. Re-check with `node tools/voice-inventory.mjs --owner 79-04 --roll-under --hygiene --twins --safety --count` until it prints 0.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --owner 79-04 --roll-under --hygiene --twins --safety --count</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --owner 79-04 --roll-under --hygiene --twins --safety --count` prints 0.
    - `node --test test/unit/formatEventsCoverage.test.js test/unit/narrationLinesCoverage.test.js test/unit/roll-sign-consistency.test.js test/unit/honest-gains.test.js` passes with none of those four files edited.
    - `git diff --quiet <plan-base> -- engine content test/parity` exits 0.
  </acceptance_criteria>
  <done>Every fight line either passed the rubric untouched or now states what happened before it jokes about it.</done>
</task>

<task type="auto">
  <name>Task 2: Re-pin deliberately, regenerate the fold corpus, write the ledger</name>
  <files>test/unit/narrationLinesTable.test.js, test/unit/fightLog.test.js, test/unit/fight-log-worst-case.test.js, test/unit/fight-log-oldest-row.test.js, test/unit/fight-log-sheet.test.js, test/unit/initiative-line.test.js, test/unit/flee-retune.test.js, test/unit/party-combat.test.js, test/unit/roll-display-lines.test.js, test/unit/event-order-fold.test.js, test/unit/fixtures/event-order/default-fold-corpus.json, test/unit/round-summary-band.test.js, test/unit/shell-fight-log.test.js, test/unit/rail.test.js, docs/narrative-pass/why/79-04.json</files>
  <read_first>
    - `npm test` failures after Task 1 (each names a pinned old line)
    - test/unit/event-order-fold.test.js and its fixture (how the corpus is regenerated and why it is pinned)
    - docs/narrative-pass/README.md (ledger schema, reasons, handed-on rule)
  </read_first>
  <action>
1. For each failing test, confirm the failure is only a pinned wording this plan changed, then re-pin it to the new wording (a one-line "VOX-05 (79-04)" comment where the pin lives). A failure that is not a wording change is a bug in Task 1: fix the line, not the test. Only re-pin files in this plan's list; a pin that lives in a sibling's file is handed on.
2. Regenerate the event-order fold corpus with its own documented command, confirm the only differences are rewritten wordings (no order change, no new or lost fold), and name it in the SUMMARY.
3. Write docs/narrative-pass/why/79-04.json: one row per changed key with before, after, reasons and a one-sentence why; run `node tools/voice-inventory.mjs --check-ledgers --plan 79-04 --after` until it passes.
4. Run `npm test`.
  </action>
  <verify>
    <automated>node tools/voice-inventory.mjs --check-ledgers --plan 79-04 --after && node --test test/unit/voice-corpus.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/voice-inventory.mjs --check-ledgers --plan 79-04 --after` exits 0.
    - `node --test test/unit/voice-corpus.test.js` passes (the standing ledger validation).
    - `npm test` reports 0 failures.
    - The SUMMARY lists every re-pinned test, the fold-corpus regeneration, the pass/fail table and any "Handed on" items.
  </acceptance_criteria>
  <done>The fight reads clearly, every changed line is on the record with its reason, and the suite is green.</done>
</task>

</tasks>

<verification>
- Fight-domain lines pass the rubric; roll-under, hygiene, twin and safety checks read 0 for this owner.
- The sign guard, the coverage guards and 79-02's honesty tests pass unedited; engine and content untouched.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol): read a full fight in THE FIGHT SO FAR and the Oracle, with a flee attempt, a party member's blow and an initiative line; every line says who did what to whom and the result before any joke. The user reads the review page's Oracle and rail sections alongside.
  </human-check>
</verification>

<success_criteria>
VOX-05 holds for the fight: every Oracle, rail and fight-log line from a combat event states clearly what happened, to whom and why, in the game's voice.
</success_criteria>

<output>
Create `.planning/phases/79-content-narrative-pass/79-04-SUMMARY.md` when done. Include the plan base SHA, the pass/fail table (key, verdict, failed rubric points), every changed line (before and after), every re-pinned test, the fold-corpus regeneration, the check counts, and "Handed on".
</output>
