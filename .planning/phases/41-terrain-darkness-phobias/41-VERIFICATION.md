---
phase: 41-terrain-darkness-phobias
verified: 2026-09-18T21:00:00Z
status: passed
score: 4/4 success criteria verified on automated evidence (SC-1 as amended by the greenfield ruling: no run flag; zero main-rng draws moved; zero fixtures moved by measurement); every player-facing check (water paint, HUD +2, wading line, phobia lines, Rattled chip, 3×3 dark filter) is deferred to the end-of-run Pixel 7 batch per the user's "defer uat to end" instruction
behavior_unverified: 0
overrides_applied: 0
human_verification: ["Pixel 7 (TERR-01): a fresh floor shows blue water pools on the explored map; lit pools in a distinct blue, a pool inside a dark blob in a visibly darker shade", "Pixel 7 (TERR-01, tolerant load): a pre-Phase-41 save resumes with a waterless floor and no card; the next descent shows water", "Pixel 7 (TERR-02): HUD SQUARES jumps by 2 stepping onto a water cell and by 1 stepping off it", "Pixel 7 (TERR-02): the 'Wading…' line fires exactly once on entering a pool, not once per wet square", "Pixel 7 (TERR-02, timers): a lit torch / MAPPED chip counts down 2 per water step", "Pixel 7 (TERR-02, day): a water step that crosses square 100 rolls over exactly one day (not zero, not two)", "Pixel 7 (TERR-02, exemption): a flying (Bracelet / active Cloak of Flying window) or ethereal character pays 1 on water — no wading line, no double tick", "Pixel 7 (TERR-04/05, water): a Bodies-of-water hero entering a pool sees 'Water. You knew this was coming. Your knees did too.' once; silent while wading on; again only after stepping fully out and back in", "Pixel 7 (TERR-04/05, dark): a Darkness-phobic hero sees 'The dark. It was always going to be the dark.' once per fresh entry into an unlit square", "Pixel 7 (TERR-04/05, trapped): a dead end shows the existing 'Four walls and one door you already used. −N hp.' AND the new 'A dead end. The walls lean in a little, just to be sure.' in that order; hp loss unchanged", "Pixel 7 (TERR-04/05, heights): a climb/gorge attempt shows 'That is a long way down. Your stomach has already left.' BEFORE the roll; a retry of the same tile is silent; walking away and back shows it again", "Pixel 7 (TERR-04/05, death): at ≤ 25% hp a Death-phobic hero sees 'You can hear your own pulse. It sounds unimpressed.' once, not again until hp climbs above half", "Pixel 7 (TERR-05, arm): the next fight after any trigger opens with the Afraid chip and the phobiaAfraid line ends '… Still rattled from the water / the dark / the drop / the dead end / your own pulse.'", "Pixel 7 (TERR-05, chip): the Rattled chip shows between the trigger and that next fight, distinct from the in-fight Afraid chip; tap explanation reads 'Something out there got to you. The next fight opens Afraid — harder to hit, softer blows — until it passes.'", "Pixel 7 (TERR-01, inspect): a hold on a water tile shows the WATER card — 'Two squares a step, and your boots never dry. Wade, or go around.'", "Pixel 7 (TERR-03): standing on a dark square without Night Vision / a lit torch / the Amulet of Light shows only the 3×3 squares around the party — every other explored cell disappears", "Pixel 7 (TERR-03): stepping off the dark square restores the full explored view instantly, nothing re-walked", "Pixel 7 (TERR-03, waivers): with a lit torch, Night Vision, or the Amulet of Light on a dark tile the full map stays visible — confirm all three independently", "Pixel 7 (TERR-03 × SPELL-05): Map the Floor's window keeps counting while the 3×3 filter is active on the same square — the filter hides, never pauses", "Pixel 7 (a11y): TalkBack reads the Rattled chip's label/explanation and the WATER card's title/line", "Pixel 7 (voice): spot-check the WATER card, the Rattled explanation, the five phobia lines and the wading line for the family-friendly deadpan tone in real play"]
gaps: []
---

# Phase 41 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-18)

Goal-backward check of the phase goal: *water and darkness become real, felt terrain — multi-square water pools; stepping onto water costs 2 moves; a dark square without Night Vision or a light effect shows only the 3×3 area; every phobia fires once on fresh entry as the existing Afraid penalty, never a lost action.*

Fourth phase under the greenfield amendment. The roadmap's `state.terrainRoll` run-flag wording was superseded by the ruling (the user named "water in `genFloor`" as the case where fixtures follow): water exists for every run, fixture and bot; the fixtures a change moves are measured and declared. Two ratified Key Decisions: water cost "one tap, two squares of time"; terrain phobias "arm Afraid for the next fight".

## Automated evidence (re-run by the orchestrator after 41-04)

- `npm test`: **2915/2915, 0 failures** (2813 at phase start → 2826 after 41-01 → 2859 after 41-02 → 2891 after 41-03 → 2915 after 41-04). `npm run build:www` exit 0; `grep -c fonts.googleapis mazeworld.html` = 0; `package.json`/lock unchanged.
- Engine gate: `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (unchanged); `git status --porcelain test/parity/fixtures` empty — **zero fixture regenerations in the whole phase**, by measurement not assumption: `test/unit/floor-gen-rng-pin.test.js` (written BEFORE water landed) pins the main-rng draw count + cursor per (seed, depth) over 98 entries and stays green — `placeWater` runs last in `genFloor` on `derivedRng(rng.getState(), "terrain", depth)`; the live roster scan `tools/terrain-fixture-scan.mjs` reports `WATER HITS: 0` (no scripted fixture move lands on a water cell — `action-script.movement.json` seed 256 is the only fixture with moves) and `TERRAIN TRIGGER EXPOSURE: 0` (no fixture hero with a terrain phobia ever moves). `cell.water`, `c.phobiaState`, `c.fearArmed` are structural carve-outs (`stripWaterField`, `stripPhobiaFields`) at all three comparables + the three local duplicates. `chargen-rng-pin.test.js` green; `docs/class-pass/v15-before*.json` byte-identical to `562811f`.
- Cumulative `git diff --stat 562811f -- engine content src mazeworld.html docs tools test/parity/fixtures` = 15 files, 1470+/37−; `mazeworld.html` touched by 41-04 only.
- Zero new rng draws anywhere: pools from the derived stream only; every phobia trigger a pure state check; `moveCost` a pure derived read.
- New events (`waded`, `phobiaTriggered`, additive `trigger` on `phobiaAfraid`) on all three narration surfaces; coverage guards and voice scan green.
- Four plans, four SUMMARY.md files, all `## Self-Check: PASSED`; working tree clean at HEAD `0655c22` apart from the two untracked user directories outside the phase.

## Success criteria → evidence

| # | Success criterion (ROADMAP, as amended by CONTEXT) | Evidence |
|---|-----------------------------|----------|
| 1 | New floors can contain a multi-square blue water pool; no existing main-rng draw moves and the fixtures that would diverge are declared | `engine/maze.js#placeWater` (called LAST in `genFloor`, derived stream, zero main draws — 98-entry rng pin green), `difficultyCurve().waterPools` (1 → 3 by depth 9, breather floors 1, matching the real `isBreather` cadence), pools 3–8 cells on `!wall && !feat` cells, never near spawn (`dist > 4`), never exit-adjacent, always passable; `MAP_PALETTE.water`/`waterDark`; `sanitizeWaterCells` tolerant load; `terrain.test.js` (7); scan `WATER HITS: 0` → nothing to declare. |
| 2 | Stepping onto a water square costs 2 moves, and every squares-based counter or timer reflects that cost consistently | `moveCost(state, cell)` / `WATER_MOVE_COST = 2` at the one step site: `state.steps += cost`, `crossings(n)` cadence helper (a +2 step never skips a `% 20`/`% 100` boundary; `per: 1` afflictions tick twice), `tickSquares(c, cost)` once, `c.darkFor` by cost, flying/ether exempt (a READY-but-unstarted Cloak of Flying is not spent on a puddle); `waded` on entry only; `water-cost.test.js` (32); bot paths across water without stalling (tuning-bot test). |
| 3 | Standing on a dark square without Night Vision or a light effect shows only the 3×3 area on the map; leaving restores the explored view | `DARK_VIEW_RADIUS`, `mapViewRadius(state)`, `inViewWindow(state, x, y)` pure reads (same waiver set as `revealRadius`: Night Vision, `eff("light")`, torch `lit`); `draw()`'s `visible(x, y)` filter at both continue sites via `window.__mzMapView`; engine `seen`/`spellSeen` untouched, Map the Floor's window keeps counting; `darkness-filter.test.js` (13), `shell-terrain-41.test.js` (8). |
| 4 | Entering water / a climb / a dead end / near-death triggers the matching phobia, named in the rail line, once on fresh entry | `engine/phobias.js` (region model `c.phobiaState`, `armFear`, `checkTerrainPhobias`, `checkDeathPhobia`, `noteHeightsAttempt`, `resetFloorPhobiaRegions`, `isDeadEnd`); triggers: water cell / `inDark` / climb-gorge attempt before the roll / dead end / hp ≤ 25% re-arming above 50% (in and out of combat); `c.fearArmed` is `fight()`'s fourth OR-condition, consumed at `fight()`; `phobiaAfraid.trigger` names it ("… Still rattled from the water"); existing `heightsPenalty`/`waterPenalty`/`trappedPanic` untouched (no double count); `fearArmed` "Rattled" chip; `phobia-triggers.test.js` (24). |

## Requirements

TERR-01 ✓ · TERR-02 ✓ · TERR-03 ✓ · TERR-04 ✓ · TERR-05 ✓ — automated halves verified; on-device halves in the frontmatter `human_verification` list (21 items, from the 22-item aggregated checklist in 41-04-SUMMARY.md; items 1 and 16 folded).

## Accepted planner/executor deviations (recorded in plan/SUMMARY frontmatter)

- Planner clarifications: only `state.steps`, `c.darkFor` and `c.timers` are per-square systems (no separate encounter clock / member timers); `fearArmed` is consumed at `fight()` rather than cleared on leaving the region (single-tile Heights would otherwise be a no-op); the dead-end line differs from `trappedPanic`'s since both fire on the same step; `waded` fires on entry only.
- 41-01: `waterPools` follows the real breather cadence (`isBreather`, depth 6/11/16…) rather than the plan's assumed 5/10/15; measured values recorded.
- 41-02: bot-test `maxActions` raised 600 → 1000 after a false "stuck" (seed 2 dies naturally at action 623).
- 41-03/41-04: literal grep-count criteria undercounted legitimate second occurrences (`sanitizePhobiaFields` wired into both `validateSave` and `rehydrate`, matching every sibling helper; `### Plan NN` headings collide across phases in FIXTURE-INVENTORY.md by the file's own convention); two pre-existing import-line pins re-measured.

## Follow-ups noted (not gaps)

- Phase 42's tuning bot: water's 2-square cost and armed fear will shift hunger/depth outcomes — expected, measured in the ONE AFTER matrix against the v1.5 BEFORE pin.
- Phase 43 (clarity pass) may want the phobia trigger lines on the Hero-tab phobia row ("fires on: entering water").
