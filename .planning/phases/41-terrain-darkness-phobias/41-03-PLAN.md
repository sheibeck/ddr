---
phase: 41-terrain-darkness-phobias
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - engine/phobias.js
  - engine/movement.js
  - engine/combat.js
  - engine/derived.js
  - engine/saveState.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/parity/harness/comparables.js
  - test/parity/movement-parity.test.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/unit/phobia-triggers.test.js
  - test/unit/conditions.test.js
  - test/unit/save-validation.test.js
  - tools/terrain-fixture-scan.mjs
  - docs/TERRAIN.md
autonomous: true
requirements: [TERR-04, TERR-05]
tags: [phobias, afraid, region-model, fear-armed, terrain-triggers, narration, key-decision]

must_haves:
  truths:
    - "Every phobia has a real trigger: Bodies of water on entering a water cell, Darkness on entering an unlit square (as well as the existing dark-combat check), Heights on a crevice/gorge climb attempt (before the roll), Being trapped on entering a dead end, Death on HP crossing to 25% of max (in or out of combat), and the five type-matched combat phobias exactly as today"
    - "A terrain phobia fires ONCE on fresh entry into its region (`c.phobiaState` region model) — never every step inside — and re-arms only when the region is left (out of the water / out of the dark / off the dead end / more than one square from the feared tile / HP back above 50%)"
    - "A fresh trigger narrates immediately on the rail with the trigger named (`phobiaTriggered { phobia, trigger }`) and arms `c.fearArmed = { phobia, trigger }`, which `fight()` reads as one more OR-condition so the NEXT fight opens Afraid through the real Phase 31 mechanism (`afraidNeed`/`afraidDamage`) — never a lost action; the arm is consumed by that fight (whether or not Hardiness shrugs it off)"
    - "Existing mechanics are not double-counted: `heightsPenalty`/`waterPenalty` still land on the climb/leap roll, `trappedPanic` keeps its hp loss and per-tile debounce — the Afraid penalty lands on a different roll (the next fight's strikes)"
    - "`c.phobiaState` and `c.fearArmed` are structural harness carve-outs (six sites) and tolerant-load sanitized; every parity fixture replays byte-identical (measured: no fixture hero with a terrain phobia ever moves; the scan's phobia column proves it)"
    - "Zero rng on every trigger path; the only new draw is `fight()`'s existing Hardiness d2, now also reachable when armed (never for a fixture)"
  artifacts:
    - "engine/phobias.js (new leaf module): TERRAIN_PHOBIAS, PHOBIA_TRIGGER_KEY, DEATH_REARM_FRACTION, isDeadEnd, tileKey, armFear, checkTerrainPhobias, checkDeathPhobia, noteHeightsAttempt, resetFloorPhobiaRegions"
    - "engine/combat.js#fight — the fourth OR-condition + consumption + additive `trigger` on phobiaAfraid; foeTurn tail checkDeathPhobia"
    - "engine/derived.js#conditionsOf — the `fearArmed` chip"
    - "test/unit/phobia-triggers.test.js"
    - "src/browser/{eventNarration,toasts,rail}.js — phobiaTriggered entries, PHOBIA_TRIGGER_PHRASE, phobiaAfraid trigger clause"
    - "docs/TERRAIN.md ## Phobia triggers — Key Decision (Plan 03) filled"
  key_links:
    - "move(): after the per-square ticks and newDay, before feature dispatch -> checkTerrainPhobias(state, events); climb block roll branch -> noteHeightsAttempt(state, nx, ny, events); teleport() landing -> checkTerrainPhobias; descend() -> resetFloorPhobiaRegions(c)"
    - "fight(): armed = c.fearArmed && c.fearArmed.phobia === c.phobia -> OR-condition -> combat.afraid = AFRAID_ROUNDS -> afraidNeed/afraidDamage (unchanged)"
    - "foeTurn tail (after the allies loop) -> checkDeathPhobia(state, events)"
    - "toastsForAction('move') narrative -> EVENT_NARRATION.phobiaTriggered (trigger named) -> RAIL_FAMILY.phobiaTriggered"
---

<objective>
Land TERR-04/05 — the user-chosen "Arm Afraid for the next fight" shape (CONTEXT Area 3, ratified Key Decision): a region model (`c.phobiaState`) that fires each terrain phobia exactly once on fresh entry, an immediate rail line naming the trigger, and a zero-rng `c.fearArmed` flag that `fight()` reads as one more OR-condition so the next fight opens Afraid through the real Phase 31 penalty — never a lost action. Five triggers: water cell, unlit square, climb/gorge attempt, dead end, HP at or below 25% (re-arming above 50%). Existing roll penalties and the trapped hp loss stay exactly as they are.

Purpose: Before this plan three phobias (Bodies of water, Heights, Being trapped) were silent roll modifiers or a bare hp loss, Darkness fired only in combat, and Death only at fight start. After it, every phobia is a felt, named, once-per-entry event with a real consequence in the next fight.

Output: `engine/phobias.js`, the wiring in movement/combat/teleport/descend, the `fearArmed` chip, harness carve-outs, tolerant load, narration on three surfaces, `test/unit/phobia-triggers.test.js`, docs/TERRAIN.md Plan 03 section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-terrain-darkness-phobias/41-CONTEXT.md
@.planning/phases/41-terrain-darkness-phobias/41-RESEARCH.md
@.planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md
@engine/movement.js
@engine/combat.js
@engine/derived.js
@engine/phobias.js
@content/flavor.js
@test/unit/afraid.test.js
@test/parity/harness/comparables.js
</context>

<standing_rules>
- Engine Gate + AMENDMENT: zero rng on trigger paths; new serialized fields (`c.phobiaState`, `c.fearArmed`) carved out in the three exported comparables AND the three local `comparable()` duplicates; master never edited; no run-flag gating; old saves tolerant-load (missing phobia state defaulted — i.e. simply absent).
- Phase 31 ruling (user, 2026-09-16): "Phobia should be penalties, never a no actions state" — nothing in this plan refuses an action.
- Rail is the one feedback surface: a phobia trigger is a narrative line (rail card via the move dispatch's fold), not a decision card. Every new event type gets `EVENT_NARRATION` + `TOAST_FOR` + `RAIL_FAMILY` and must pass `test/voice/safety-scan.test.js` (family-friendly, deadpan).
- Deferred UAT; verification agents OFF: the LAST task runs the gate (`npm test` `# fail 0`, master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0) and records the numbers.
- Commits per task: `feat(41-03): …` / `test(41-03): …` / `docs(41-03): …` with the two trailer lines (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`). Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
- `mazeworld.html` is NOT edited in this plan (the `fearArmed` chip's copy rows land in Plan 04; the chip DATA lands here).
- Planner interpretation of CONTEXT Area 3 (binding for the executor): ALL FIVE terrain phobias arm Afraid for the next fight (TERR-05's literal text); "Being-trapped keeps its existing flat hp loss and gains only the fresh-entry debounce + narration" means the hp loss is retained and NOT replaced — the arm applies to it like the other four. Recorded in docs/TERRAIN.md as the CONTEXT clarification.
</standing_rules>

<artifacts_this_phase_produces>
See 41-01-PLAN.md's phase-wide list. This plan creates: `engine/phobias.js` (all exports listed in must_haves), the `move()`/`teleport()`/`descend()` wiring and the Heights attempt hook in `engine/movement.js` (with `isDeadEnd` relocated to phobias.js), `fight()`'s fourth OR-condition + consumption + additive `trigger`, the foeTurn-tail `checkDeathPhobia` in `engine/combat.js`, `conditionsOf`'s `fearArmed` chip in `engine/derived.js`, `sanitizePhobiaFields` in `engine/saveState.js`, `stripPhobiaFields` in the harness (+ 3 locals), the `phobiaTriggered` event with `EVENT_NARRATION`/`TOAST_FOR`/`RAIL_FAMILY` entries and the exported `PHOBIA_TRIGGER_PHRASE` map in `src/browser/eventNarration.js`, the phobia column in `tools/terrain-fixture-scan.mjs`, `test/unit/phobia-triggers.test.js`, and docs/TERRAIN.md `## Phobia triggers — Key Decision (Plan 03)`.
</artifacts_this_phase_produces>

<tasks>
<!-- planner-discipline-allow: (appended by Plan 03) -->

<task type="auto" tdd="true">
  <name>Task 1: engine/phobias.js region model + movement wiring (water / darkness / dead end / heights attempt / teleport / descend)</name>
  <files>engine/phobias.js, engine/movement.js, test/unit/phobia-triggers.test.js</files>
  <read_first>
    - engine/movement.js lines 41-95 (`isDeadEnd`, `heightsPenalty`/`waterPenalty`, `TRAPPED_PHOBIA_PANIC`), 136-265 (the climb/gorge block: `flyOver`, the tool branch, the ether branch, the `hazardChoice` pending pre-check and its early `return`, then `let ok = true;` — the roll branch), 265-300 (position update, `trappedPanic` with `there.trapPanicked`), 400-440 (after newDay: `if (state.dead) return events;` then feature dispatch), 681-745 (`teleport()` landing), 819-842 (`descend()`)
    - engine/derived.js lines 14-45 (`DEATH_PANIC_THRESHOLD = 0.25`, the Afraid knobs), 677-705 (`inDark`)
    - engine/encounters.js lines 619-671 (`newPhobia`, `fallDark`/`DARKNESS_DURATION` — how `c.darkFor` is raised)
    - content/flavor.js lines 17-22 (the ten `PHOBIAS` — five `t: null` terrain phobias, five typed)
    - test/unit/movement.test.js lines 40-100 (helpers to copy) and its `trappedPanic`/`heightsFear` tests (search "trappedPanic" / "heightsFear") — the existing behaviours that MUST stay green
    - .planning/phases/41-terrain-darkness-phobias/41-CONTEXT.md Area 3 (trigger list, region model, re-arm rules)
  </read_first>
  <behavior>
    - Bodies of water: a Bodies-of-water hero stepping from dry onto a water cell emits exactly one `phobiaTriggered { phobia: "Bodies of water", trigger: "water" }`, sets `c.fearArmed` to `{ phobia: "Bodies of water", trigger: "water" }` and `c.phobiaState["Bodies of water"] === true`; a second step inside the pool emits nothing; stepping onto a dry cell sets the region `false` and leaves `c.fearArmed` set; re-entering fires again
    - Darkness: stepping onto a `.dark` cell fires `trigger: "dark"` once; staying silent inside; leaving re-arms; with `c.darkFor > 0` (fallDark) the next step fires even on a lit tile (region = `inDark(state)`); Night Vision does NOT suppress the trigger (consistent with `fight()`'s dark check)
    - Being trapped: entering a dead end (<= 1 open orthogonal neighbour) fires `trigger: "deadEnd"` AND the existing `trappedPanic` (hp loss unchanged: 4, Hardiness 2, clamped to leave 1 hp); the event order is `trappedPanic` then `phobiaTriggered`; stepping out and back into the SAME dead end: `trappedPanic` does not re-fire (per-tile marker, unchanged) but `phobiaTriggered` does (region re-entry)
    - Heights: a climb or gorge attempt on the roll branch emits `phobiaTriggered { trigger: "heights" }` BEFORE `heightsFear`/`waterFear` and before any roll (event order asserted); a retry of the same tile after a fall is silent; stepping so the hero is more than one square (Manhattan) from that tile and coming back fires again; the `hazardChoice` pause (ladder/rope carried) does NOT fire — the second "CLIMB IT" move does; the flyOver / ether / tool branches never fire
    - Teleport: landing on water / a dark cell / a dead end fires the matching trigger once
    - Descend: `c.phobiaState` loses the four floor-bound keys (water/dark/heights/trapped) and keeps `Death`; `c.fearArmed` survives descend
    - A hero whose phobia is a combat type (e.g. "Fire") walking water/dark/dead-end/climbing never gains `c.phobiaState` or `c.fearArmed` (no key creation)
    - Every trigger path runs with `fakeRng([])` (throws on any draw) — zero rng — except the climb roll itself in the Heights cases (scripted)
  </behavior>
  <action>
    A. Create `engine/phobias.js` — a cycle-free leaf importing ONLY `inDark`, `DEATH_PANIC_THRESHOLD` from `./derived.js`. Header comment: Phase 41 (TERR-04/05), user-chosen Key Decision "Arm Afraid for the next fight"; the region model; zero rng; every function mutates only `state.c.phobiaState` / `state.c.fearArmed` and pushes `phobiaTriggered`. Exports:
       - `TERRAIN_PHOBIAS = Object.freeze(["Bodies of water", "Darkness", "Heights", "Being trapped", "Death"])`.
       - `PHOBIA_TRIGGER_KEY = Object.freeze({ "Bodies of water": "water", Darkness: "dark", Heights: "heights", "Being trapped": "deadEnd", Death: "nearDeath" })` — the `trigger` payload vocabulary (short keys; the shell maps them to phrases).
       - `DEATH_REARM_FRACTION = 0.5`.
       - `isDeadEnd(f, x, y)` — MOVED verbatim from engine/movement.js (delete it there; movement.js imports it from here); use a local `DIRV`-equivalent list of the four cardinal vectors (do not import movement.js).
       - `tileKey(x, y)` returning the string `x + "," + y`.
       - `armFear(state, trigger, events)`: sets `state.c.fearArmed = { phobia: state.c.phobia, trigger }` and pushes `{ type: "phobiaTriggered", phobia: state.c.phobia, trigger }`.
       - `regionActive(state, phobia)` (pure): `"Bodies of water"` -> the hero's current cell has `water === true`; `"Darkness"` -> `inDark(state)`; `"Being trapped"` -> `isDeadEnd(state.floor, px, py)`; anything else -> `false`.
       - `checkDeathPhobia(state, events)`: no-op unless `state.c.phobia === "Death"` and `!state.dead`; `was = !!(c.phobiaState && c.phobiaState.Death)`; `enter = c.wp <= c.maxWP * DEATH_PANIC_THRESHOLD`; `stay = !(c.wp > c.maxWP * DEATH_REARM_FRACTION)`; `now = was ? stay : enter`; on `now && !was` lazily create `c.phobiaState = {}` if absent, set `Death = true`, `armFear(state, "nearDeath", events)`; on `!now && was` set `Death = false`.
       - `checkTerrainPhobias(state, events)`: hero only; returns immediately when `state.dead` or `state.c.phobia` is not in `TERRAIN_PHOBIAS`; for `"Death"` delegate to `checkDeathPhobia`; for `"Heights"` run ONLY the leave-check: if `typeof c.phobiaState?.Heights === "string"`, parse the stored tile key and set `c.phobiaState.Heights = false` when the Manhattan distance from `(f.px, f.py)` to that tile is greater than 1; for the other three: `was = c.phobiaState?.[phobia] === true`, `now = regionActive(state, phobia)`; on `now && !was` lazily create the map, set `true`, `armFear(state, PHOBIA_TRIGGER_KEY[phobia], events)`; on `!now && was` set `false`. The map is created ONLY on a first trigger (a character that never triggers never gains the key).
       - `noteHeightsAttempt(state, x, y, events)`: no-op unless `state.c.phobia === "Heights"`; `key = tileKey(x, y)`; if `c.phobiaState?.Heights !== key` then lazily create the map, set `Heights = key`, `armFear(state, "heights", events)`.
       - `resetFloorPhobiaRegions(c)`: when `c.phobiaState` is a plain object, delete the `"Bodies of water"`, `"Darkness"`, `"Heights"`, `"Being trapped"` keys (keep `Death`; keep `c.fearArmed`).

    B. `engine/movement.js` wiring — import `{ isDeadEnd, checkTerrainPhobias, noteHeightsAttempt, resetFloorPhobiaRegions }` from `./phobias.js`; delete the local `isDeadEnd` (keep its explanatory comment, pointing at phobias.js):
       1. In the climb/gorge block's ROLL branch, immediately after the pending pre-check (after the `return events;` of the `hazardChoice` branch) and before `let ok = true;`, call `noteHeightsAttempt(state, nx, ny, events)` with a DELIBERATE RULES CHANGE comment: Phase 41 (TERR-04/05) — Heights fires on the attempt, before the roll; `heightsPenalty`/`waterPenalty` still land on the roll itself (different roll from the next fight's Afraid — no double count).
       2. After `if (state.dead) return events;` (post-newDay) and BEFORE `const cell = f.g[ny][nx];`, call `checkTerrainPhobias(state, events)` with a comment: runs after every per-square hp/dark tick of this step so the Death crossing and the darkFor-driven region read the settled state; runs before feature dispatch so a pending combat started by newDay or by the dot below already sees `c.fearArmed`.
       3. In `teleport()`, after the `teleported` event push and before the dot/trap dispatch, call `checkTerrainPhobias(state, events)` (a landing is a fresh entry).
       4. In `descend()`, after the new floor's `reveal(...)` and `floorChanged`, call `resetFloorPhobiaRegions(state.c)` (comment: floor-bound regions die with the floor; Death and the armed fear survive).
       The `trappedPanic` block is untouched (its per-tile marker and hp loss stay).

    C. `test/unit/phobia-triggers.test.js` (new) — every `<behavior>` bullet as a `node:test` case, using copies of movement.test.js's helpers (`fakeRng`, `wallGrid`, `open`, `fixedFighter`, `fixedState`; set `c.phobia`/`c.phobiaType` per case; corridors built with `open(g, x, y, { water: true })` / `{ dark: true }` / `{ feat: "climb" }`). For the Heights cases script the climb rolls through `fakeRng` exactly as the existing climb tests do (pick -> "rope", d2, d10...). For teleport use `teleport(state, makeRng(seed), [])` on a grid where every landing candidate in the rolled direction is water/dark. Assert event ORDER with `events.map((e) => e.type)`.

    Run `node --test test/unit/phobia-triggers.test.js test/unit/movement.test.js test/unit/tools.test.js test/unit/item-wiring.test.js` — green. Commit `feat(41-03): terrain phobia region model — engine/phobias.js, movement/teleport/descend wiring, Heights attempt trigger`.
  </action>
  <acceptance_criteria>
    - `test -f engine/phobias.js`; `grep -cE "^export (function|const) (TERRAIN_PHOBIAS|PHOBIA_TRIGGER_KEY|DEATH_REARM_FRACTION|isDeadEnd|tileKey|armFear|regionActive|checkDeathPhobia|checkTerrainPhobias|noteHeightsAttempt|resetFloorPhobiaRegions)\b" engine/phobias.js` prints 11
    - `grep -c "^import" engine/phobias.js` is 1 and that import is from `./derived.js`
    - `grep -c "^function isDeadEnd" engine/movement.js` is 0 and `grep -c "isDeadEnd" engine/movement.js` is at least 2 (import + the trappedPanic use)
    - `grep -c "noteHeightsAttempt(state, nx, ny, events)" engine/movement.js` is 1; `grep -c "checkTerrainPhobias(state, events)" engine/movement.js` is 2 (move + teleport); `grep -c "resetFloorPhobiaRegions(state.c)" engine/movement.js` is 1
    - `grep -c 'type: "phobiaTriggered"' engine/phobias.js` is 1 and `grep -rc 'type: "phobiaTriggered"' engine/ | grep -v ":0" | wc -l` is 1 (one push site)
    - `node --test test/unit/phobia-triggers.test.js` exits 0 with at least 16 tests
    - `node --test test/unit/movement.test.js test/unit/tools.test.js test/unit/item-wiring.test.js test/unit/water-cost.test.js test/unit/engine-purity.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/phobia-triggers.test.js test/unit/movement.test.js test/unit/tools.test.js test/unit/water-cost.test.js test/unit/engine-purity.test.js</automated>
  </verify>
  <done>The five terrain phobias each fire once on fresh entry through the region model, re-arm on leaving, narrate with the trigger key, arm `c.fearArmed`, draw no rng, and never touch a character without that phobia.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fight() reads the armed fear (fourth OR-condition), Death crossing in combat, the fearArmed chip</name>
  <files>engine/combat.js, engine/derived.js, test/unit/phobia-triggers.test.js, test/unit/conditions.test.js</files>
  <read_first>
    - engine/combat.js lines 365-432 (`fight()` — the three-condition trigger, the Hardiness d2, `phobiaAfraid { rounds }`), 1220-1280 (`endCombat`), 2484-2542 (the foeTurn tail: ward/mirror/foeEffect/afraid/tickRounds/allies loop, then `return events`)
    - engine/derived.js lines 470-630 (`conditionsOf` — the bad block: affliction, darkness, afraid; the STABLE order contract in its JSDoc)
    - test/unit/afraid.test.js lines 30-115 (helpers: `fakeRng`, `countingRng`, the pending-combat builders) and tests (vi)/(vii)/(viii)/(ix)
    - test/unit/conditions.test.js (existing chip-order pins that must stay green)
    - engine/phobias.js (Task 1's `checkDeathPhobia`, `armFear`)
  </read_first>
  <behavior>
    - fight(): a hero with `c.fearArmed = { phobia: c.phobia, trigger: "water" }` whose phobia is NOT type-matched, not Darkness-in-dark, not near death, opens the fight with `combat.afraid === AFRAID_ROUNDS` and a `phobiaAfraid` event equal to `{ type: "phobiaAfraid", rounds: AFRAID_ROUNDS, trigger: "water" }`; `c.fearArmed` is absent after `fight()` (consumed) — `"fearArmed" in c` is false
    - Control: the same hero without `fearArmed` is not afraid and the `phobiaAfraid` event shape for an ordinary type-matched trigger is unchanged: `deepStrictEqual` to `{ type: "phobiaAfraid", rounds: AFRAID_ROUNDS }` (no `trigger` key)
    - Hardiness: an armed hero with Hardiness draws exactly one extra d2 (countingRng: initiative pair + 1); on a 1 the fear is shrugged off (no afraid) but `fearArmed` is still consumed
    - Stale arm: `fearArmed.phobia !== c.phobia` (the phobia changed via newPhobia) is ignored AND consumed
    - Death in combat: a Death-phobic hero at 30/55 hp takes foe damage in `foeTurn` to 13/55 — that foeTurn's events contain one `phobiaTriggered { trigger: "nearDeath" }`, `c.fearArmed` is set, `c.phobiaState.Death === true`; the arm survives `endCombat` and the NEXT `fight()` opens Afraid with `trigger: "nearDeath"`; healing to 30 (above 50%) then clears the region (`Death === false`) on the next check
    - `nearDeathPanic` (the existing at-fight-start check) still fires independently for a Death-phobic hero at/below 25% with no arm
    - conditionsOf: with `c.fearArmed = { phobia: "Heights", trigger: "heights" }` the returned list contains exactly one `{ key: "fearArmed", polarity: "bad", phobia: "Heights", trigger: "heights" }` placed after any `darkness` chip and before any `afraid` chip; absent when `fearArmed` is absent; a fresh `newRun` character has no such chip
    - "Never a lost action": with `combat.afraid > 0` from an armed trigger, strike/cast/drink/read/use/flee/parley are all accepted (re-run afraid.test.js (iv)'s action list through `applyAction` on an armed-then-fought state)
  </behavior>
  <action>
    A. `engine/combat.js#fight()`: before the trigger `if`, compute `const armed = !!(c.fearArmed && typeof c.fearArmed === "object" && c.fearArmed.phobia === c.phobia); const armedTrigger = armed ? c.fearArmed.trigger : null;` then CONSUME: `if ("fearArmed" in c) delete c.fearArmed;` (the arm is spent by this fight's opening whether or not Hardiness shrugs it off; a stale arm for a replaced phobia is dropped here too). Add `|| armed` as the FOURTH OR-condition inside the existing parenthesised trigger condition (left of the Hardiness `&&`, so the d2 is drawn only when some condition is already true — unchanged for every fixture, none of which ever carries `fearArmed`). Push `{ type: "phobiaAfraid", rounds: AFRAID_ROUNDS, ...(armedTrigger ? { trigger: armedTrigger } : {}) }` — additive spread, so the existing shape is byte-identical when not armed. Extend the DELIBERATE RULES CHANGE comment: Phase 41 (TERR-05) — the fourth condition, the Key Decision, the consume-at-fight rule (it deliberately survives endCombat/descend so a Death crossing mid-fight or a wade with no fight on this floor still lands on the NEXT fight), the stale-phobia guard.
       In the foeTurn tail, after the allies `tickRounds` loop and before `return events`, add `checkDeathPhobia(state, events)` (import from `./phobias.js`) with a comment: Phase 41 (TERR-04) — the in-combat half of the Death trigger; zero draws; a no-op for every non-Death-phobic hero (every fixture).
       `endCombat` is NOT changed (no fearArmed clear there — see the consume rule).

    B. `engine/derived.js#conditionsOf`: in the bad block, after the `darkness` chip and before the `afraid` chip, push `{ key: "fearArmed", polarity: "bad", phobia: c.fearArmed.phobia, trigger: c.fearArmed.trigger }` when `c.fearArmed` is a non-null object with a string `phobia`. Update the JSDoc's chip list with the new row (`fearArmed {polarity:"bad", phobia, trigger} — Phase 41 (TERR-05): an ARMED terrain phobia waiting for the next fight; cleared when fight() consumes it`). Pure read; no key creation.

    C. Tests: append the `<behavior>` cases to `test/unit/phobia-triggers.test.js` (fight/foeTurn/endCombat cases built the way afraid.test.js builds a pending combat — `startCombat(state, false, "Beasts", rng, [])` then `fight(state, rng, [])`), and add the chip cases to `test/unit/conditions.test.js` (keep every existing order pin green — extend the "stable order" test's expected array where the chip now sits).

    Run `node --test test/unit/phobia-triggers.test.js test/unit/afraid.test.js test/unit/conditions.test.js test/unit/combat.test.js test/unit/fight-gate.test.js test/unit/foe-turn-draw-count.test.js` — green (foe-turn-draw-count's pins must be unchanged: `checkDeathPhobia` draws nothing). Commit `feat(41-03): fight() opens Afraid from an armed terrain phobia; Death crossing in combat; fearArmed chip`.
  </action>
  <acceptance_criteria>
    - `grep -c "|| armed" engine/combat.js` is 1; `grep -c 'if ("fearArmed" in c) delete c.fearArmed;' engine/combat.js` is 1; `grep -c "armedTrigger ? { trigger: armedTrigger } : {}" engine/combat.js` is 1
    - `grep -c "checkDeathPhobia(state, events)" engine/combat.js` is 1 and `grep -c "from \"./phobias.js\"" engine/combat.js` is 1
    - `grep -c 'key: "fearArmed"' engine/derived.js` is 1
    - `grep -c "fearArmed" engine/combat.js` — the endCombat function body contains no `fearArmed` reference: `awk '/^export function endCombat/,/^}/' engine/combat.js | grep -c fearArmed` prints 0
    - `node --test test/unit/phobia-triggers.test.js` exits 0 with at least 24 tests (Task 1's 16 + 8 here)
    - `node --test test/unit/afraid.test.js test/unit/conditions.test.js test/unit/combat.test.js test/unit/fight-gate.test.js test/unit/foe-turn-draw-count.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/phobia-triggers.test.js test/unit/afraid.test.js test/unit/conditions.test.js test/unit/combat.test.js test/unit/fight-gate.test.js test/unit/foe-turn-draw-count.test.js</automated>
  </verify>
  <done>An armed terrain fear opens the next fight Afraid through the real Phase 31 mechanism and is consumed there; a Death crossing mid-fight arms the fight after; the chip data exists; every existing Afraid/draw-count pin is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: narration on three surfaces, harness carve-out (six sites), tolerant load, phobia scan column, TERRAIN ledger, plan gate</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, engine/saveState.js, test/parity/harness/comparables.js, test/parity/movement-parity.test.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/unit/save-validation.test.js, test/unit/phobia-triggers.test.js, tools/terrain-fixture-scan.mjs, test/parity/FIXTURE-INVENTORY.md, docs/TERRAIN.md</files>
  <read_first>
    - src/browser/eventNarration.js lines 120-135 (`heightsFear`/`waterFear`/`trappedPanic`) and 230-240 (`phobiaAfraid`)
    - src/browser/toasts.js lines 930-940 and 1005-1010 (`TOAST_FOR.heightsFear`, `TOAST_FOR.phobiaAfraid` — `PRIORITY.feature`)
    - src/browser/rail.js lines 205-215 (the three `AFRAID` rows)
    - test/voice/safety-scan.test.js lines 185-235 (builders are rendered with token values)
    - test/parity/harness/comparables.js lines 185-245 (`stripFoeEffectField`/`stripTimersField`/`stripWornField`/`stripAbilitiesField` — the c-level helper shape and where they sit in the three chains) and the three chain lines (~435, ~518, ~1026)
    - test/parity/movement-parity.test.js lines 85-100, combat-parity.test.js and magic-parity.test.js local `comparable()` — the `{ name, darkFor, ..., ...cRest } = rest.c` destructures to extend
    - engine/saveState.js lines 165-225 (`clearStaleTimers`/`sanitizeWorn` — the "present-but-tampered is neutralised, absent is never injected" discipline) and the `migratedC` chain in `validateSave`
    - tools/terrain-fixture-scan.mjs (Plan 01's tool — extend, do not rewrite)
    - test/parity/FIXTURE-INVENTORY.md "## Phase 41" (append a Plan 03 subsection)
    - docs/TERRAIN.md (fill the `(appended by Plan 03)` placeholder only)
  </read_first>
  <action>
    A. Narration (voice: sarcastic, deadpan, family-friendly; the trigger NAMED in the line — TERR-05):
       - `src/browser/eventNarration.js`: export `PHOBIA_TRIGGER_PHRASE = Object.freeze({ water: "the water", dark: "the dark", heights: "the drop", deadEnd: "the dead end", nearDeath: "your own pulse" })`. Add `phobiaTriggered: (e) => ...` selecting by `e?.trigger`: `water` -> `<span class="hurt">Water. You knew this was coming. Your knees did too.</span> It will show in the next fight.`; `dark` -> `<span class="hurt">The dark. It was always going to be the dark.</span> It will show in the next fight.`; `heights` -> `<span class="hurt">That is a long way down. Your stomach has already left.</span> It will show in the next fight.`; `deadEnd` -> `<span class="hurt">A dead end. The walls lean in a little, just to be sure.</span> It will show in the next fight.`; `nearDeath` -> `<span class="hurt">You can hear your own pulse. It sounds unimpressed.</span> It will show in the next fight.`; any other/missing trigger -> `<span class="hurt">Your phobia has noticed where you are.</span> It will show in the next fight.` (The dead-end line is deliberately DIFFERENT from `trappedPanic`'s existing "Four walls…" line because both fire on the same step.) Extend `phobiaAfraid` to append ` Still rattled from ${PHOBIA_TRIGGER_PHRASE[e.trigger]}.` when `e?.trigger` maps to a phrase; unchanged otherwise.
       - `src/browser/toasts.js`: `TOAST_FOR.phobiaTriggered = (e) => ({ text: <the same headline sentence per trigger, without the trailing clause>, tone: "hurt", priority: PRIORITY.feature })`.
       - `src/browser/rail.js`: `RAIL_FAMILY.phobiaTriggered = { icon: "·", title: "A PHOBIA", tone: "bad" }`.
       Run `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/rail.test.js test/voice/` — green.

    B. Harness carve-out: add `stripPhobiaFields(c)` to `test/parity/harness/comparables.js` (drops `phobiaState` and `fearArmed` when present; JSDoc: structural carve-out, Phase 41 (TERR-04/05), no prototype-side equivalent; no fixture hero with a terrain phobia ever moves — measured by the scan — so this is a tripwire like stripTimersField), wired as a wrapper in all three exported `rest.c` chains, and add `phobiaState, fearArmed` to the destructure lists of the three local `comparable()` functions (movement/combat/magic parity tests) with a Phase 41 comment each.

    C. Tolerant load: `engine/saveState.js` `sanitizePhobiaFields(c)`: when `"phobiaState" in c` and the value is not a plain object -> delete the key; when it is, delete any entry whose value is neither a boolean nor a string; when `"fearArmed" in c` and the value is not a plain object with string `phobia` and string `trigger` -> delete the key. Never injects. Wire into `validateSave`'s `migratedC` chain right after `clearFoeEffect`. Tests in `test/unit/save-validation.test.js`: a pre-Phase-41 save (no keys) loads with neither key; tampered `phobiaState: "x"` / `phobiaState: { Death: 7 }` / `fearArmed: 5` / `fearArmed: { phobia: 1 }` are dropped; valid `{ Death: true, Heights: "3,4" }` and `{ phobia: "Death", trigger: "nearDeath" }` survive a serializeRun -> JSON -> validateSave -> rehydrate round trip.

    D. Scan: extend `tools/terrain-fixture-scan.mjs` — for the movement fixture also flag, per move, whether the destination is `.dark`, a dead end (import `isDeadEnd` from engine/phobias.js), or a climb/gorge attempt, and print a final line `TERRAIN TRIGGER EXPOSURE: <n>` = the number of fixtures whose hero has a phobia in `TERRAIN_PHOBIAS` AND whose script contains at least one `move` action (the only fixture with moves is seed 256; its hero's phobia is printed in the existing column — the executor reads the measured value, never assumes). Run it; paste the phobia column + the exposure line into a new `### Plan 03 — c.phobiaState / c.fearArmed: structural carve-out, measured zero fixture moves` subsection in FIXTURE-INVENTORY.md, listing which fixture heroes DO carry terrain phobias (seeds with Being trapped / Heights / Bodies of water) and why they cannot trigger (no `move` actions; `checkDeathPhobia` needs the Death phobia, which no fixture hero has — state the measured list).

    E. `docs/TERRAIN.md` — replace `(appended by Plan 03)` under `## Phobia triggers — Key Decision (Plan 03)` with: the Key Decision verbatim (CONTEXT Area 3, user-chosen); the trigger table (phobia | trigger key | region | re-arm rule | existing mechanic kept); the `fearArmed` lifecycle (set by armFear; read + consumed at `fight()`; survives endCombat and descend; stale-phobia guard; Hardiness d2 still applies at fight time); the Heights tile-key model and the one-square leave rule; the dead-end definition (<= 1 open orthogonal neighbour); the Death hysteresis (enter at 25%, re-arm above 50%; in-combat check at the foeTurn tail); the CONTEXT clarification (all five arm; Being trapped keeps its hp loss); the harness/tolerant-load rules; "never a lost action" pointer to afraid.test.js (iv).

    F. GATE: `npm test` (`# fail 0`; record `# tests`/`# pass`), master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0, `git status --porcelain test/parity/fixtures` unchanged from Plan 02's state (no new fixture edits). Commit `docs(41-03): phobia narration, fearArmed/phobiaState carve-out + tolerant load, scan phobia column, TERRAIN Key Decision`. Write `41-03-SUMMARY.md` (40-05 format) with the gate numbers, `## Key Decisions for PROJECT.md` (the arm-Afraid shape, verbatim), and `## Human verification (deferred to end of run)`: a Bodies-of-water hero entering a pool sees "Water. You knew this was coming…" exactly once, silence inside, again on re-entry; a Darkness hero sees "The dark…" on entering unlit squares; a dead end shows both the hp-loss line and the phobia line; a climb attempt shows the Heights line before the roll result; at <= 25% hp the Death line appears once and not again until healed above half; the next fight after any of these opens with the Afraid chip and "Still rattled from the water/the dark/the drop/the dead end/your own pulse"; the Rattled chip (Plan 04 copy) shows between the trigger and that fight.
  </action>
  <acceptance_criteria>
    - `grep -c "export const PHOBIA_TRIGGER_PHRASE" src/browser/eventNarration.js` is 1; `grep -c "phobiaTriggered" src/browser/eventNarration.js` is at least 1; `grep -c "Still rattled from" src/browser/eventNarration.js` is 1
    - `grep -c "phobiaTriggered" src/browser/toasts.js` is at least 1; `grep -c "phobiaTriggered:" src/browser/rail.js` is 1
    - `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/rail.test.js test/voice/` exits 0
    - `grep -c "function stripPhobiaFields" test/parity/harness/comparables.js` is 1; `grep -c "stripPhobiaFields(" test/parity/harness/comparables.js` is at least 4; `grep -c "phobiaState, fearArmed" test/parity/movement-parity.test.js` is 1; same for test/parity/combat-parity.test.js and test/parity/magic-parity.test.js
    - `grep -c "function sanitizePhobiaFields" engine/saveState.js` is 1 and `grep -c "sanitizePhobiaFields(" engine/saveState.js` is 2 (definition + chain)
    - `grep -c "phobiaState\|fearArmed" test/unit/save-validation.test.js` is at least 4
    - `node tools/terrain-fixture-scan.mjs | grep -cE "^TERRAIN TRIGGER EXPOSURE: [0-9]+"` is 1; `grep -c "### Plan 03" test/parity/FIXTURE-INVENTORY.md` is 1
    - `grep -c "(appended by Plan 03)" docs/TERRAIN.md` is 0 and `grep -c "Arm Afraid" docs/TERRAIN.md` is at least 1
    - `node --test test/parity/` exits 0 and `git status --porcelain test/parity/fixtures` shows no file beyond what Plan 02 committed (empty after commit)
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0
    - `test -f .planning/phases/41-terrain-darkness-phobias/41-03-SUMMARY.md`; `grep -c "## Human verification (deferred to end of run)" .planning/phases/41-terrain-darkness-phobias/41-03-SUMMARY.md` is 1; `grep -c "Key Decisions for PROJECT.md" .planning/phases/41-terrain-darkness-phobias/41-03-SUMMARY.md` is 1
  </acceptance_criteria>
  <verify>
    <automated>node tools/terrain-fixture-scan.mjs | grep -E "^TERRAIN TRIGGER EXPOSURE" && npm test 2>&1 | grep -E "^# (tests|pass|fail) [0-9]+" && git hash-object test/parity/prototype-master.js.txt && npm run build:www</automated>
    <human-check>Deferred to the milestone-close Pixel 7 batch (listed in the SUMMARY): each of the five trigger lines fires once per fresh entry; the next fight opens Afraid with the "Still rattled from …" clause.</human-check>
  </verify>
  <done>Every new event is narrated on all three surfaces and voice-clean; the two new character fields are carved out at six sites and tolerant-loaded; the scan proves no fixture hero with a terrain phobia ever moves; docs/TERRAIN.md carries the Key Decision; the gate is green and recorded.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/phobia-triggers.test.js` — every trigger fires once on fresh entry, re-arms on leaving, draws no rng, arms fearArmed; fight() consumes it and opens Afraid; Death crossing works in and out of combat.
- `node --test test/unit/afraid.test.js test/unit/foe-turn-draw-count.test.js` — Phase 31 pins and draw counts untouched.
- `node --test test/parity/` — byte-identical with only `stripPhobiaFields` added.
- `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0.
</verification>

<success_criteria>
- ROADMAP SC-4 (as amended by CONTEXT): entering water triggers Bodies of water; a climb/gorge attempt triggers Heights; a dead end triggers Being trapped; near-death triggers Death; each named in the rail line, once per fresh entry, applying the Phase 31 Afraid penalty to the next fight — never a lost action.
- TERR-04 and TERR-05 engine + narration complete; only the chip's shell copy remains for Plan 04.
</success_criteria>

<output>
Create `.planning/phases/41-terrain-darkness-phobias/41-03-SUMMARY.md` when done (40-05 format; include `## Key Decisions for PROJECT.md` with the arm-Afraid decision and `## Human verification (deferred to end of run)`).
</output>
