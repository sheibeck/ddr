---
phase: 41-terrain-darkness-phobias
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - engine/derived.js
  - engine/movement.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/unit/water-cost.test.js
  - test/unit/tuning-bot.test.js
  - test/parity/movement-parity.test.js
  - test/parity/full-suite.test.js
  - test/roundtrip/serialize-rehydrate.test.js
  - test/parity/fixtures/action-script.movement.json
  - test/parity/FIXTURE-INVENTORY.md
  - docs/TERRAIN.md
autonomous: true
requirements: [TERR-02]
tags: [terrain, water, move-cost, timers, cadence, key-decision, bot, narration]

must_haves:
  truths:
    - "One tap onto a water cell advances `state.steps` by exactly 2 (the HUD Squares counter, which reads `S.steps`, jumps by 2); stepping off water onto a dry cell costs 1 (CONTEXT Area 1, ratified Key Decision)"
    - "Every squares-based system sees the full cost on that one dispatch: `tickSquares(c, 2)` called ONCE (item/spell/ability/torch timers, the Map the Floor window), the affliction cadence, the cloak 20-square ticks, the Magic User 20-square charge, the 100-square newDay, `c.darkFor` — and a cadence boundary crossed by a +2 step fires exactly once, never skipped (99 -> 101 still rolls the day)"
    - "Flying (Bracelet of Flight / a LIVE Cloak of Flying window) and ethereal characters pay 1 on water; a ready-but-unstarted Cloak of Flying is not spent on a puddle"
    - "A normal (cost-1) step is byte-identical to before this plan: every parity fixture replays unchanged EXCEPT the one(s) the Plan 01 scan measured stepping onto water, which are declared as `action-path` records with measured before/after values"
    - "The tuning bot paths across water and never stalls (water is passable; the bot simply pays the 2 — cost-aware pathing is Phase 42's)"
    - "Entering water narrates one 'Wading…' line (event `waded`) with EVENT_NARRATION + TOAST_FOR + RAIL_FAMILY entries, voice-scan clean"
  artifacts:
    - "engine/derived.js#moveCost(state, cell) + WATER_MOVE_COST = 2"
    - "engine/movement.js#move — cost-aware step with the `crossings(n)` cadence helper; `waded` event; tickSquares(c, cost)"
    - "test/unit/water-cost.test.js"
    - "src/browser/{eventNarration,toasts,rail}.js waded entries"
    - "docs/TERRAIN.md ## Water cost — Key Decision (Plan 02) filled"
    - "test/parity/fixtures/action-script.movement.json action-path record (ONLY if the scan measured a hit) + action-path support in its three consumers"
  key_links:
    - "move(): const cost = moveCost(state, there) -> state.steps += cost -> crossings(n) for every steps-cadence site -> tickSquares(c, cost) once"
    - "HUD: mazeworld.html paint() writes S.steps into #m-steps — no shell edit needed; the engine's +2 is the counter's +2"
    - "toastsForAction('move') -> narrativeToastText(EVENT_NARRATION.waded) -> railCardFor -> RAIL_FAMILY.waded"
---

<objective>
Land TERR-02 — the ratified Key Decision "one tap, two squares of time": a single `move` onto a water cell advances `state.steps` by 2 and every per-square system by the same 2 in one dispatch, through a `moveCost(state, cell)` derived read consumed at the one step-cost site in `engine/movement.js#move`. Includes the cadence-crossing rule so a +2 step never skips a 20/100-square boundary, the flight/ether exemption, the `waded` narration, the bot smoke proof, and — if Plan 01's scan measured a hit — the declared `action-path` divergence on the movement fixture with measured before/after values.

Purpose: Water has to be FELT as time: hunger, torch burn, the Map the Floor window, spell charges and the day all move twice as fast while wading. This is the second of the two Key Decisions the phase hands to PROJECT.md.

Output: `moveCost`/`WATER_MOVE_COST`, the rewired step site, `waded`, `test/unit/water-cost.test.js`, the bot test, docs/TERRAIN.md Plan 02 section, the declared fixture record if measured.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-terrain-darkness-phobias/41-CONTEXT.md
@.planning/phases/41-terrain-darkness-phobias/41-RESEARCH.md
@.planning/phases/41-terrain-darkness-phobias/41-01-SUMMARY.md
@engine/movement.js
@engine/derived.js
@engine/effects.js
@test/unit/movement.test.js
@test/parity/movement-parity.test.js
@test/parity/fixtures/action-script.schema.md
</context>

<standing_rules>
- Engine Gate + AMENDMENT: no run-flag gating; a cost-1 step must be byte-identical to before (the `crossings` helper equals the old modulo test when cost is 1); the master file is never edited; a fixture the cost genuinely moves is DECLARED with measured values and only that fixture's consumers change.
- Pitfall 3 (research): `tickSquares(c, cost)` is called ONCE per step with `n = cost`, never twice.
- Deferred UAT; verification agents OFF: the LAST task runs the gate (`npm test` `# fail 0`, master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0 because `src/browser` is touched) and records the numbers in the SUMMARY.
- Commits per task: `feat(41-02): …` / `test(41-02): …` / `docs(41-02): …`, each ending with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
- `mazeworld.html` is NOT edited in this plan (the HUD counter already reads `S.steps`; Plan 04 owns every shell paint change). `tools/lib/tuning-bot.mjs` MAY be edited only if the smoke test below shows a stall; `docs/class-pass/v15-before*.json` pins are never regenerated (Phase 42 owns the AFTER).
- No scope reduction language ("for now", "v1", "simplified"): every per-square system listed below gets the cost in this plan.
</standing_rules>

<artifacts_this_phase_produces>
See 41-01-PLAN.md's phase-wide list. This plan creates: `engine/derived.js#moveCost` + `WATER_MOVE_COST`; the cost-aware `move()` (local `crossings` helper, `waded { cost }` event, `tickSquares(c, cost)`); `test/unit/water-cost.test.js`; `waded` entries in `src/browser/eventNarration.js`, `src/browser/toasts.js` (`TOAST_FOR.waded`), `src/browser/rail.js` (`RAIL_FAMILY.waded`); the bot water-pathing test in `test/unit/tuning-bot.test.js`; docs/TERRAIN.md `## Water cost — Key Decision (Plan 02)`; and, ONLY if measured, the `action-path` record on `action-script.movement.json` plus action-path support in `test/parity/movement-parity.test.js`, `test/parity/full-suite.test.js`, `test/roundtrip/serialize-rehydrate.test.js`.
</artifacts_this_phase_produces>

<tasks>
<!-- planner-discipline-allow: (appended by Plan 02) -->

<task type="auto" tdd="true">
  <name>Task 1: moveCost derived read + the cost-aware step site (crossings cadence, tickSquares once, darkFor, waded)</name>
  <files>engine/derived.js, engine/movement.js, test/unit/water-cost.test.js</files>
  <read_first>
    - engine/movement.js lines 116-440 (`move()` end to end: the climb/gorge block, `f.px = nx`, `state.steps++`, the affliction `state.steps % af.per` tick, the `c.darkFor--` tick, the two cloak `% CLOAK_TICK_SQUARES` sites, the `c.timers` block with `tickSquares(c, 1)` + `narrateTimerTransitions` + the `spell:reveal` refog reaction, the `% 20` spell-charge site, the `% 100` newDay site, the feature dispatch)
    - engine/effects.js (`tickSquares(c, n)` — `n` is already accepted; `tick()` floors at 0 and flips effect->cooldown once)
    - engine/derived.js lines 375-470 (`itemEffectActive`, `isFlying`, `hasItemNamed`) and 677-705 (`inDark`, `revealRadius` — the pure-derived-read style to match)
    - test/unit/movement.test.js lines 40-100 (the `fakeRng`/`wallGrid`/`open`/`fixedFighter`/`fixedState` helpers — copy them into the new test file; they are module-local) and lines 640-660 (the timers/spell-charge cadence tests — the shapes to extend)
    - test/unit/item-wiring.test.js lines 165-200 (the cloak 20-square tick tests, `steps: 19` setup)
    - content/afflictions.js (cadences: `per: 1`, `2`, `10`, `20` exist — a `per: 1` affliction must tick TWICE on a water step)
    - .planning/phases/41-terrain-darkness-phobias/41-CONTEXT.md Area 1 (the Key Decision text, verbatim)
  </read_first>
  <behavior>
    - moveCost(state, cell): 1 for null/undefined/dry cell; 2 for `cell.water === true`; 1 on water when the hero carries the Bracelet of Flight, has a LIVE `fly` item effect, or a LIVE `ether` item effect; 2 on water for a Cloak-of-Flying carrier with NO live record (ready is not spent on a puddle) and no effect record is started by the step
    - Step onto water from dry: steps +2, exactly one `moved`, exactly one `waded { cost: 2 }`; water -> water: +2, no `waded`; water -> dry: +1, no `waded`; a flying hero over water: +1, no `waded`
    - Cadence crossing fires ONCE, never skips: steps 98 -> water -> 100 emits one `dayBegan`; 99 -> 101 emits one `dayBegan`; 97 -> 99 emits none; Magic User spellsUsed 1 at 19 -> 21 emits one `spellChargeRecovered`; Cloak of Healing at 19 -> 21 emits one `cloakHealed` (zero rng draws)
    - Affliction: `per: 1` at steps 0 -> water step -> exactly two `afflictionTick` events and two loss draws; `per: 10` at 9 -> 11 one tick; at 8 -> 10 one tick; at 10 -> 12 zero ticks; an affliction that clears on the first of two ticks (loss 0 -> afflictionPassed) does not tick a second time
    - darkFor 2 -> water step -> 0 with exactly one `darknessLifted`; darkFor 1 -> 0 one `darknessLifted`; darkFor 3 -> 1 no event; the Amulet of Light branch (darknessDispelled) is unchanged
    - Timers on a water step: a squares record `left: 3` becomes `left: 1` (no transition); `left: 1, cd: 50` flips to `phase: "cooldown", left: 50` with EXACTLY one transition/one `itemEffectFaded`; `left: 2` with no cd is deleted with one transition; a `rounds` record is untouched; `spell:reveal` at `left: 1` triggers exactly one `revealFaded`
    - A cost-1 step on a dry cell produces exactly the same state and events as before this plan (compare a dry-floor walk against a snapshot built by the same test with a cost of 1 — use `structuredClone` before/after and the existing movement.test.js expectations)
  </behavior>
  <action>
    A. `engine/derived.js` — add `export const WATER_MOVE_COST = 2;` beside the Afraid knobs (a TUNING KNOB comment citing CONTEXT Area 1: one tap, two squares of time; Phase 42 measures the hunger drift) and `export function moveCost(state, cell)`: returns 1 unless `cell && cell.water === true`; returns 1 when `hasItemNamed(state.c, "Bracelet of Flight")`, or `itemEffectActive(state.c, "fly")`, or `itemEffectActive(state.c, "ether")` (JSDoc: flight/ether skip the surcharge — "walls and crevices are nothing", water too — but a READY, not-yet-started Cloak of Flying window is deliberately NOT started for a puddle, unlike the climb block's flyOver; the cloak is spent on walls); otherwise `WATER_MOVE_COST`. Pure, zero rng, reads only `state.c` and the passed cell.

    B. `engine/movement.js` — import `moveCost` from `./derived.js`. In `move()`:
       1. After the climb/gorge block and before `f.px = nx;`, compute `const cost = moveCost(state, there);` (`there` is the destination cell object already in scope).
       2. Replace the single increment with `const stepsBefore = state.steps; state.steps += cost;` and define, immediately after, a local `const crossings = (n) => Math.floor(state.steps / n) - Math.floor(stepsBefore / n);` with a comment: for cost 1 this equals the old modulo-equals-zero cadence test exactly (0 or 1), so every cost-1 step is byte-identical; for cost 2 a crossed boundary fires once and is never skipped (99 -> 101 still rolls the day — the once-a-day rule).
       3. After `events.push(moved(...))`, push `{ type: "waded", cost }` ONLY when `cost > 1 && !here.water` (`here` is the departure cell captured before the position update) — one line per wade, not per step (planner discretion: keeps an 8-cell pool from stacking eight toasts; the HUD counter carries the per-step cost).
       4. Affliction: replace the modulo-cadence gate on `af.per` with a countdown loop over `crossings(af.per)` that runs the EXISTING tick body (roll, clamp, loss-0 clear, `--af.left` clear) once per crossing and stops early when `c.affliction` is cleared inside the body. Do not change the body.
       5. `c.darkFor`: in the non-Amulet branch replace the decrement with `c.darkFor = Math.max(0, c.darkFor - cost);` keeping the `=== 0` darknessLifted push. The Amulet branch is untouched.
       6. Both cloak sites: replace the modulo-cadence test on `CLOAK_TICK_SQUARES` with `crossings(CLOAK_TICK_SQUARES) > 0`.
       7. Timers: `tickSquares(c, cost)` — ONE call, `n = cost`. Rewrite the Phase 36 comment above it to state Phase 41 (TERR-02) now passes the step's cost here and that it is called once per step (research Pitfall 3), never twice.
       8. Spell charge: `crossings(20) > 0`. newDay: `crossings(100) > 0`.
       9. Add a DELIBERATE RULES CHANGE comment block above the cost line: Phase 41 (TERR-02, user-ratified Key Decision 2026-09-18): one tap onto water = two squares of time; every per-square system below reads `cost`; list the systems (state.steps and its cadences: affliction/cloaks/spell charge/newDay/Bard songAt; c.darkFor; c.timers) and note that CONTEXT's phrases "encounter clock"/"member timers" have no separate per-square counterpart in the engine (verified: the wandering-monster check is newDay's own; party members carry no squares-cadence state) — so the list above IS the complete set.

    C. `test/unit/water-cost.test.js` (new) — implement every bullet in `<behavior>` as a `node:test` case, using copies of movement.test.js's `fakeRng`/`wallGrid`/`open`/`fixedFighter`/`fixedState` helpers (open the destination cell with `{ water: true }` via `open(g, x, y, { water: true })`). For the newDay cases seed a real `makeRng(1)` (newDay draws d10 + 8 d20s and possibly the affliction cure) and assert exactly one `dayBegan` in the returned events. For the timers case use `startEffect(c, "item:Cloak of Speed", { squares: 3 })` etc. from engine/effects.js and count transitions via the emitted `itemEffectFaded`/`itemCooled` events (import `narrateTimerTransitions`'s event names from the existing effect-expiry tests). For the byte-identical case, run one dry step on a fresh fixedState and `assert.deepStrictEqual` the resulting `{ steps, c.darkFor, c.timers, events }` against the values the existing movement.test.js cases already expect (steps 1, one `moved`, no `waded`).

    Run `node --test test/unit/water-cost.test.js test/unit/movement.test.js test/unit/item-wiring.test.js test/unit/effect-expiry.test.js test/unit/map-reveal.test.js` — all green. Commit: `feat(41-02): one tap, two squares — moveCost derived read, cost-aware step site, waded event` (explicit paths: engine/derived.js engine/movement.js test/unit/water-cost.test.js).
  </action>
  <acceptance_criteria>
    - `grep -c "export const WATER_MOVE_COST = 2" engine/derived.js` is 1; `grep -c "export function moveCost" engine/derived.js` is 1
    - `grep -c "const cost = moveCost(state, there)" engine/movement.js` is 1; `grep -c "state.steps += cost" engine/movement.js` is 1; `grep -c "const crossings = " engine/movement.js` is 1
    - `grep -c "tickSquares(c, cost)" engine/movement.js` is 1 and `grep -v '^\s*//' engine/movement.js | grep -c "tickSquares(c, 1)"` prints 0
    - `grep -c "crossings(100) > 0" engine/movement.js` is 1; `grep -c "crossings(20) > 0" engine/movement.js` is 1; `grep -c "crossings(CLOAK_TICK_SQUARES) > 0" engine/movement.js` is 2; `grep -c "crossings(af.per)" engine/movement.js` is 1
    - `grep -v '^\s*//' engine/movement.js | grep -c "state.steps % "` prints 0 (every modulo cadence site now reads crossings)
    <!-- planner-discipline-allow: state.steps %  -->
    <!-- planner-discipline-allow: tickSquares(c, 1) -->
    - `grep -c 'type: "waded"' engine/movement.js` is 1
    - `node --test test/unit/water-cost.test.js` exits 0 with at least 14 tests
    - `node --test test/unit/movement.test.js test/unit/item-wiring.test.js test/unit/effect-expiry.test.js test/unit/map-reveal.test.js test/unit/afraid.test.js` exits 0
    - `node -e "import('./engine/derived.js').then(m=>{const s={c:{items:[],skills:{}}};process.exit(m.moveCost(s,{water:true})===2&&m.moveCost(s,{})===1&&m.moveCost(s,null)===1?0:1)})"` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/water-cost.test.js test/unit/movement.test.js test/unit/item-wiring.test.js test/unit/effect-expiry.test.js test/unit/map-reveal.test.js</automated>
  </verify>
  <done>A water step costs 2 across every per-square system in one dispatch with cadence boundaries never skipped; flying/ether pay 1; a dry step is byte-identical; `waded` fires on entry only; the new test file covers every behavior bullet.</done>
</task>

<task type="auto">
  <name>Task 2: waded narration, bot water-pathing proof, declared fixture divergence (measured), TERRAIN ledger Key Decision, plan gate</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/tuning-bot.test.js, test/parity/movement-parity.test.js, test/parity/full-suite.test.js, test/roundtrip/serialize-rehydrate.test.js, test/parity/fixtures/action-script.movement.json, test/parity/FIXTURE-INVENTORY.md, docs/TERRAIN.md</files>
  <read_first>
    - src/browser/eventNarration.js lines 95-135 and 590-600 (the `flownOver`/`heightsFear`/`revealFaded` builders — the `<span class="beat">` shape) 
    - src/browser/toasts.js lines 46-100 (TONES, PRIORITY, CARD_EVENTS, NARRATIVE_ACTIONS) and 925-940, 1305-1310 (the `flownOver`/`revealFaded` TOAST_FOR builders)
    - src/browser/rail.js lines 140-215 (RAIL_FAMILY rows — `flownOver`, `revealFaded`, the three `AFRAID` rows) and 296-330 (`railFamilyFor`)
    - test/unit/toastsCoverage.test.js and test/unit/formatEventsCoverage.test.js headers (how canonical event types are derived from `type: "..."` literals in engine/*.js)
    - test/voice/safety-scan.test.js lines 185-235 (every builder is rendered with token values — builders must tolerate a missing payload)
    - tools/lib/tuning-bot.mjs lines 60-200 (BOT_DEFAULTS `maxActions: 20000`, `canStep`, `bfsFirstStep`, `nearestUnseenDir`, `dirTowardExit`) and lines 780-840 (`playRun(seed, opts, onStep)` return shape: `stuck`, `outcome`, `deathDepth`)
    - test/unit/tuning-bot.test.js lines 700-750 (the `playRun` smoke tests and `BOT_DEFAULTS` import)
    - test/parity/FIXTURE-INVENTORY.md "## Phase 41: water terrain" (Plan 01's measured scan — the `WATER HITS` line decides step C below) and lines 326-400 (the Phase 24 action-path declaration format)
    - test/parity/combat-parity.test.js lines 200-260 (how `actionPathDivergenceOf`/`skipsByteDiffAt`/`declaredEndDiffs` are consumed in a per-action loop — the pattern to port to the movement consumers if needed)
    - test/parity/movement-parity.test.js lines 105-160, test/parity/full-suite.test.js (its movement leg), test/roundtrip/serialize-rehydrate.test.js lines 90-112 (the `steps === 100` / `day === 2` end assertions)
    - docs/TERRAIN.md (Plan 01's skeleton — fill the `(appended by Plan 02)` placeholder only)
  </read_first>
  <action>
    A. Narration for `waded` (voice: sarcastic, deadpan, family-friendly):
       - `src/browser/eventNarration.js`: `waded: () => "<span class=\"beat\">Wading. Everything takes twice as long and smells worse.</span>"` (placed beside the climb/flight movement lines, with a Phase 41 (TERR-02) comment).
       - `src/browser/toasts.js`: `TOAST_FOR.waded = () => ({ text: "Wading. Twice as long, and it smells worse.", tone: "beat", priority: PRIORITY.other })`.
       - `src/browser/rail.js`: `RAIL_FAMILY.waded = { icon: "·", title: "WADING", tone: "odd" }`.
       Run `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/rail.test.js test/voice/` — green.

    B. Bot proof — add to `test/unit/tuning-bot.test.js`: test "Phase 41 (TERR-02): the bot paths across water and never stalls": (1) for seeds 1..20, `newRun(seed)` then assert `dirTowardExit(state)` is a non-null direction and `nearestUnseenDir(state)` is non-null (water is passable; BFS is `.wall`/feat-based so a pool never blocks); (2) for seeds `[1, 2, 3]`, `playRun(seed, { ...BOT_DEFAULTS, maxActions: 600 })` returns `stuck === false` and `outcome !== "stuck"`, and at least one of the three runs' event tallies (or a manual `onStep` counter over the emitted events) contains a `waded` event — proving the bot actually crossed water and kept running. If (2) ever reports `stuck`, THEN and only then edit `tools/lib/tuning-bot.mjs` to add water to whatever avoidance broke it — and record the change in the SUMMARY (expected: no edit needed).

    C. Fixture divergence — re-run `node tools/terrain-fixture-scan.mjs` and read `WATER HITS`:
       - If `WATER HITS: 0`: nothing is declared. Run `node --test test/parity/ test/roundtrip/` — green unchanged. Add a `### Plan 02 — the move cost: measured, zero fixture moves` subsection to FIXTURE-INVENTORY.md's Phase 41 section stating the scan was re-run after the cost landed with zero hits.
       - If `WATER HITS` names seed 256 action indices: (1) MEASURE the record values, never hand-type them — write a scratch script (scratchpad, not committed) that replays the fixture through `applyAction` and prints the engine's final `c.wp`, `steps`, `day`, and replays the frozen prototype via `loadPrototypeSandbox({ seed })` + `ctx.move(dir)` for the prototype's final values (the harness in test/parity/harness/sandboxPrototype.js); (2) add to `action-script.movement.json`'s top level a `divergence` record: `kind: "action-path"`, `phase: 41`, `requirements: ["TERR-02"]`, `fromAction: <first hit index>`, `fields: ["wp"]`, `before: { wp: <proto> }`, `after: { wp: <engine> }`, `stateFields: ["steps", "day"]`, `stateBefore: { steps: 100, day: 2 }`, `stateAfter: { steps: <engine>, day: <engine> }`, `rationale` naming the water cells the script crosses and that water costs two squares (TERR-02) while the frozen prototype has no water; (3) port action-path support into `test/parity/movement-parity.test.js` (import `actionPathDivergenceOf`, `skipsByteDiffAt`, `declaredEndDiffs`; in the per-action loop skip the byte diff at/after `fromAction`; after the loop assert `declaredEndDiffs(ctx.S, engineState, pathDiv)` returns `before: null` and `after: null`; make the trailing `steps`/`day` assertions read `pathDiv.stateAfter` when a record exists and keep the literal 100/2 otherwise), the movement leg of `test/parity/full-suite.test.js` (same three helpers, same skip), and `test/roundtrip/serialize-rehydrate.test.js` (its `steps === 100` sanity assertion reads the record's `stateAfter.steps` when present); (4) update the fixture's `_note` with one sentence pointing at the record; (5) write the FIXTURE-INVENTORY.md `### Plan 02 — movement fixture seed 256: declared action-path divergence (TERR-02)` subsection with a before/after table (`steps`, `day`, `wp`, the hit indices and cells) and the "measured, not assumed" sentence; (6) `node --test test/parity/ test/roundtrip/ test/unit/parity-action-path-divergence.test.js` — green; `git status --porcelain test/parity/fixtures` now shows ONLY `action-script.movement.json` (the one declared fixture) — record that in the SUMMARY.

    D. `docs/TERRAIN.md` — replace the `(appended by Plan 02)` line under `## Water cost — Key Decision (Plan 02)` with: the Key Decision verbatim from CONTEXT Area 1 (user-chosen, ratified — "One tap, two squares of time"); the mechanism (`moveCost`, `crossings`, `tickSquares(c, cost)` once, `waded` entry-only); the exemption table (Bracelet of Flight / live fly window / live ether -> 1; ready cloak -> 2, not spent); the per-square systems table with the CONTEXT-vs-code correction ("encounter clock"/"member timers" have no separate engine counterpart — newDay's wandering check and the hero's timers are the whole set); the declared divergence (or "measured zero"); a "Phase 42 note" that hunger/depth drift from water time is expected and measured there against the v1.5 BEFORE pin.

    E. GATE: `npm test` (`# fail 0` — record `# tests`/`# pass`), `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0 (src/browser touched). Commit `docs(41-02): waded narration, bot water proof, measured fixture declaration, TERRAIN Key Decision` (explicit paths). Write `41-02-SUMMARY.md` (40-05 format) with the gate numbers, the scan result and declaration status, `## Key Decisions for PROJECT.md` (the water-cost decision, verbatim), and `## Human verification (deferred to end of run)`: HUD Squares jumps by 2 on a water step and by 1 stepping off; the "Wading…" line appears once on entering a pool; a lit torch / MAPPED chip counts down 2 per water step; a day rolls over when a water step crosses square 100.
  </action>
  <acceptance_criteria>
    - `grep -c "waded" src/browser/eventNarration.js` is at least 1; `grep -c "waded" src/browser/toasts.js` is at least 1; `grep -c "waded:" src/browser/rail.js` is 1
    - `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/rail.test.js test/voice/` exits 0
    - `grep -c "paths across water" test/unit/tuning-bot.test.js` is 1 and `node --test test/unit/tuning-bot.test.js` exits 0
    - `node tools/terrain-fixture-scan.mjs | grep -E "^WATER HITS: [0-9]+"` prints one line, and FIXTURE-INVENTORY.md contains a `### Plan 02` subsection whose content matches it: if the count is 0 the subsection says "zero fixture moves" and `git status --porcelain test/parity/fixtures` is empty; if greater than 0 then `grep -c '"kind": "action-path"' test/parity/fixtures/action-script.movement.json` is 1, `grep -c "actionPathDivergenceOf" test/parity/movement-parity.test.js` is at least 1, `grep -c "actionPathDivergenceOf" test/parity/full-suite.test.js` is at least 1, `grep -c "stateAfter" test/roundtrip/serialize-rehydrate.test.js` is at least 1, and `git status --porcelain test/parity/fixtures` lists only `action-script.movement.json` before the commit
    - `grep -c "(appended by Plan 02)" docs/TERRAIN.md` is 0 and `grep -c "One tap, two squares" docs/TERRAIN.md` is at least 1
    - `grep -c 'getElementById("m-steps").textContent = S.steps' mazeworld.html` is 1 (read-only confirmation the HUD counter is fed by the engine's steps; no shell edit in this plan: `git diff --quiet HEAD -- mazeworld.html` at commit time)
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `npm run build:www` exits 0
    - `test -f .planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md`; `grep -c "## Human verification (deferred to end of run)" .planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md` is 1; `grep -c "Key Decisions for PROJECT.md" .planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md` is 1
  </acceptance_criteria>
  <verify>
    <automated>node tools/terrain-fixture-scan.mjs | grep -E "^WATER HITS" && npm test 2>&1 | grep -E "^# (tests|pass|fail) [0-9]+" && git hash-object test/parity/prototype-master.js.txt && npm run build:www</automated>
    <human-check>Deferred to the milestone-close Pixel 7 batch (listed in the SUMMARY): Squares +2 on a water step, the Wading line, timers burning 2 per water step.</human-check>
  </verify>
  <done>`waded` is narrated on all three surfaces and voice-clean; the bot crosses water without stalling; the movement fixture is either measured-unmoved or declared with measured values and its three consumers understand the record; docs/TERRAIN.md carries the Key Decision; the gate is green and recorded.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/water-cost.test.js` — every behavior bullet green, including the cadence-crossing and once-only timer transition cases.
- `node --test test/unit/tuning-bot.test.js` — the bot crosses water and never reports `stuck`.
- `node tools/terrain-fixture-scan.mjs` — the measured hit count matches the declaration status in FIXTURE-INVENTORY.md.
- `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0.
</verification>

<success_criteria>
- ROADMAP SC-2 (as amended): stepping onto a water square costs 2 moves and every squares-based counter/timer reflects it consistently — the HUD counter (fed by `S.steps`), item/spell/torch timers, cadences and the day.
- The Key Decision is recorded in docs/TERRAIN.md and handed to PROJECT.md via the SUMMARY.
- Only a measured fixture divergence is declared; nothing else moved.
</success_criteria>

<output>
Create `.planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md` when done (40-05 format; include `## Key Decisions for PROJECT.md` with the water-cost decision and `## Human verification (deferred to end of run)`).
</output>
