---
phase: 75.2-hero-size-matters
plan: 05
type: execute
wave: 4
depends_on:
  - 75.2-01
  - 75.2-02
  - 75.2-03
  - 75.2-04
files_modified:
  - docs/ROLL-LEDGER.md
  - test/unit/hero-size-rules.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - docs/DIFFICULTY-RETUNE.md
  - tools/readouts/75.2-05-final.txt
  - tools/voice-sample.mjs
  - tools/voice-sample-output.txt
autonomous: true
requirements:
  - RULES-11
must_haves:
  truths:
    - "docs/ROLL-LEDGER.md gains `## Phase 75.2 hero size (RULES-11)`, appended with CRLF and zero deleted lines: the size modifier sites (the hero's damage, the foe's faces against the hero and against a Joiner, each with its direction-test id) and the size-rule audit table S1–S14 copied from 75.2-01, each row's verdict intact (75.2-CONTEXT: rules that care about big or small, each recorded in a table)"
    - "A standing guard, test/unit/hero-size-rules.test.js, fails if a creature-size read appears outside the audited set: across comment-stripped engine/*.js the only `eff(.., \"size\")` call is inside derived.js#itemSizeStep, the only SIZE_STEP_OF read inside #raceSizeStep, and the only `sizeAxes` read inside #sizeAxisStep; sizeAxisStep is read by sizeDamage (weaponDamage, expectedStrike), foeToHitVs, foeToHitBreakdown and combat.js's member branch; no engine file names a race to apply the signature rule; the only foe-size reads are the two `size: picked.sz` record builders; and the ROLL-LEDGER section lists S1–S14. Its failure message says a new size rule must read the hero's and Joiners' size through sizeAxisStep (so the signature masks apply) and join the audit"
    - "test/parity/FIXTURE-INVENTORY.md's Phase 75.2 section closes with a consolidated summary (each plan's predictor, measurement and declared moves, the prototype master hash), and a Phase 75.2 exposure guard in divergence-records.test.js asserts, across every replay site, that no site that fights starts with a hero whose damage or face axis step is non-zero, no event is an itemEffectStarted of kind giant or enlarge, and no event carries a `size` mod, unless a Phase 75.2 record declares the holder; a doctored event list carrying a size mod fails it"
    - "docs/DIFFICULTY-RETUNE.md has one `## v2.1 hero size (Phase 75.2) — bot readouts` H2 placed immediately before `## v1.2 retune (Phase 27)` (which stays the last H2), recording 75.2-01's BEFORE/AFTER and per-race BEFORE/AFTER, 75.2-02's BEFORE/AFTER and the phase FINAL, each fenced block proven with readout-compare --recorded; a per-race effect table (Elven, Dwarven, Troll: mean depth, p50, reach5 before and after); and a reading against the depth-20 unicorn and the floor 5–7 average with a 'Flag for the user' paragraph where a band or a race moved, and no retune (75.2-CONTEXT: report the per-race effect, don't compensate)"
    - "tools/voice-sample.mjs renders the giant and enlarge start lines with the size fields (constant values, no new random draws in the tool) and `size` joins its modifier names; the regenerated sample shows 'one size larger'"
    - "The whole-phase gates hold at the head: npm test 0 failures; the parity suite green; the prototype master hash unchanged since the phase base; the roll-high guard, direction tests, ledger sync, state pins, save compat and sign-consistency tests green"
  artifacts:
    - path: docs/ROLL-LEDGER.md
      provides: "the Phase 75.2 size sites and the S1–S14 audit"
      contains: "Phase 75.2 hero size"
    - path: test/unit/hero-size-rules.test.js
      provides: "the standing size-read guard"
      contains: "RULES-11"
    - path: test/parity/divergence-records.test.js
      provides: "the Phase 75.2 exposure guard"
      contains: "Phase 75.2"
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 75.2 readout record with the per-race effect"
      contains: "Phase 75.2"
    - path: tools/readouts/75.2-05-final.txt
      provides: "the phase FINAL readout"
  key_links:
    - from: docs/DIFFICULTY-RETUNE.md (Phase 75.2 blocks)
      to: tools/readouts/75.2-0{1,2}-*.txt and 75.2-05-final.txt
      via: "readout-compare --recorded"
      pattern: "75.2-0"
    - from: test/unit/hero-size-rules.test.js
      to: engine/derived.js#sizeAxisStep
      via: "the one allowed size read, masks included"
      pattern: "sizeAxisStep"
  prohibitions:
    - statement: "MUST NOT retune anything in this plan: a reading outside the target is written as a 'Flag for the user' paragraph, never acted on (the checkpointed fit protocol: engine changes go to the user)"
      requirement_id: RULES-11
      category: transparency
      status: unverified
      flagged: true
---

<objective>
Close Phase 75.2's engine gate: write the size-rule audit into the roll ledger and guard it, finish the fixture story with a standing exposure guard, record every balance readout (overall and per race) in the difficulty ledger, refresh the voice sample, and prove every gate at the head.

Purpose: ROADMAP criteria 3 and 5 ("rules that care about big or small read the hero's size as well as the foe's"; "moved fixtures are measured, declared and regenerated, and a bot readout before and after is recorded") and the standing engine gate. Each balance plan committed its own readouts so the waves never collided on the shared ledgers; this plan writes them once.

Output: the ROLL-LEDGER section and its guard, the FIXTURE-INVENTORY close and exposure guard, the DIFFICULTY-RETUNE.md record and final readout, the voice sample, and the gate results with the phase's human checks rolled up.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75.2-05 creates or changes.)

- 75.2-01: the size helpers and the size term in damage and the foe's odds (hero and Joiners); `test/unit/hero-size.test.js`; the size direction and ledger rows; identity and CLASS-PASS rows; the FIXTURE-INVENTORY Plan 01 subsection; readouts (overall and per race)
- 75.2-02: the Gauntlet and Enlarge as one step each, their texts, the event and chip size fields; `test/unit/size-items.test.js`; the FIXTURE-INVENTORY Plan 02 subsection; readouts
- 75.2-03: the hero sheet's SIZE row and `#s-size`; the sheet's damage reading `sizeDamage`; `test/unit/hero-size-display.test.js`; the declared hero snapshots
- 75.2-04: the stepped-size chips, the `giant` / `enlarge` Oracle and rail lines, the race notes, the size sign scenario; `test/unit/size-voice.test.js`
- **This plan (75.2-05):** `## Phase 75.2 hero size (RULES-11)` in docs/ROLL-LEDGER.md; `test/unit/hero-size-rules.test.js`; the FIXTURE-INVENTORY Phase 75.2 summary and the exposure guard in `test/parity/divergence-records.test.js`; `## v2.1 hero size (Phase 75.2) — bot readouts` in docs/DIFFICULTY-RETUNE.md and `tools/readouts/75.2-05-final.txt`; the voice-sample tool update and regenerated output; the whole-phase gates

## Flagged assumptions (surfaced, not silently decided)

- **Readings are recorded, not acted on.** If the average run ending leaves floors 5–7, or one race's mean depth moves by more than a floor, this plan writes a "Flag for the user" paragraph and does not retune.
- **The bot rarely meets the items.** The bot drinks Enlarge only as a round-1 hard-fight buff and uses the Gauntlet as a worn buff, so 75.2-02's readout moves little; the reading says so. The race rule (75.2-01) is the phase's real balance shift.
- **The audit's "no change" rows (S3, S4, S5, S6, S7, S11, S12, S13) are ruled by the user (2026-09-25, after planning):** no current rule keys on creature size, so criterion 3 is met by the audit and its guard, with no new creature-size rule; S8–S10 record the signature ruling.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75.2-hero-size-matters/75.2-CONTEXT.md
@.planning/phases/75.2-hero-size-matters/75.2-01-SUMMARY.md
@.planning/phases/75.2-hero-size-matters/75.2-02-SUMMARY.md
@.planning/phases/75.2-hero-size-matters/75.2-03-SUMMARY.md
@.planning/phases/75.2-hero-size-matters/75.2-04-SUMMARY.md
@.planning/phases/75.2-hero-size-matters/75.2-01-PLAN.md

<interfaces>
- 75.2-01-PLAN.md's "The size-rule audit" section is the S1–S14 table to copy.
- docs/ROLL-LEDGER.md is CRLF; append with a small Node script that keeps CRLF and deletes nothing (74-08's method); test/unit/roll-ledger-sync.test.js reads only its `## Modifier ledger` section, which 75.2-01 already extended.
- docs/DIFFICULTY-RETUNE.md: the Phase 72, 73, 75 and 75.1 bot-readout H2s are the format; `## v1.2 retune (Phase 27)` must stay the last H2 (test/unit/difficulty-retune-ledger.test.js). `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<file>` proves a fenced block against its file.
- test/parity/divergence-records.test.js: `replaySiteEvents(seed, actions, opts)` returns `{ state, events, ... }` for a replay site; the JOIN-02, GRULE-01, Phase 75 and Phase 75.1 exposure guards show how to walk every site and count; `collectRecords()` gathers declared divergence records.
- tools/voice-sample.mjs: `sampleEvent(type)` builds one random event per line (its `kind` is an affliction pick, so itemEffectStarted samples fall to the generic line); MOD_NAMES lists the sampled mod names. Adding a random draw to sampleEvent reshuffles the whole sample, so add constant fields and a separate deterministic block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The size-rule audit in the roll ledger, its standing guard, and the fixture close</name>
  <files>docs/ROLL-LEDGER.md, test/unit/hero-size-rules.test.js, test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js</files>
  <read_first>
    - .planning/phases/75.2-hero-size-matters/75.2-01-PLAN.md ("The size-rule audit")
    - docs/ROLL-LEDGER.md (its closing sections and the Modifier-ledger rows 75.2-01 added)
    - .planning/phases/74-roll-display-modifier-honesty/74-08-SUMMARY.md (the CRLF append method)
    - engine/derived.js (raceSizeStep, itemSizeStep, sizeStepOf, sizeAxisStep and their readers) and engine/combat.js (the member branch; `size: picked.sz`) and engine/foeAbilities.js (the summon builder)
    - test/parity/FIXTURE-INVENTORY.md (the Phase 75.2 section 75.2-01 and 75.2-02 wrote)
    - test/parity/divergence-records.test.js (replaySiteEvents, collectRecords, the Phase 75 and 75.1 exposure guards)
    - every Phase 75.2 SUMMARY's fixture measurement
  </read_first>
  <behavior>
    - hero-size-rules: on comment-stripped engine sources, exactly one `eff(<name>, "size")` call exists and it is inside itemSizeStep; SIZE_STEP_OF appears in engine only inside raceSizeStep; `sizeAxes` appears in engine only inside sizeAxisStep; `sizeAxisStep(` is called in derived.js (sizeDamage, foeToHitVs, foeToHitBreakdown) and combat.js (the member branch); no engine file compares a race name to decide a size axis; the only `.sz` reads are the two `picked.sz` builders; no engine file reads `.size` off a foe-like binding (f, foe, t, target, pursuer, picked excluded as the builder); docs/ROLL-LEDGER.md's Phase 75.2 section lists S1 through S14.
    - Teeth: the scan helper, fed a doctored source string with a second size read (for example a foe `f.size` comparison, or a second `sizeAxes` read), reports it.
    - Exposure guard: across every replay site (the test asserts the current site count), zero sites that fight start with a hero whose sizeAxisStep(c, "dmg") or sizeAxisStep(c, "face") is non-zero (c = newRun(seed).c), zero itemEffectStarted events of kind giant or enlarge, and zero events whose mods include a `size` entry; the declared Phase 75.2 holder set equals the measured set (empty if nothing moved); a doctored event list with one size mod fails.
  </behavior>
  <action>
1. docs/ROLL-LEDGER.md: append `## Phase 75.2 hero size (RULES-11)` with a Node script that keeps CRLF and deletes nothing. It holds: the rule in one paragraph (race base plus items; ±2 damage and ±1 foe face per step; the signature rule — a race's base step never cancels its own trait, recorded as `sizeAxes` masks on the Dwarven (damage) and Elven (face) rows, while item steps always apply in full; `size` mod signed for the foe; Joiners by their own race and mask; no draws); a "Size modifier sites" table (hero damage: weaponDamage/expectedStrike; foe vs hero: foeToHitVs/foeToHitBreakdown/foeSwingVsHero with `[foe-vs-hero:size-large]`, `[foe-vs-hero:size-small]`, `[foe-vs-hero:size-item]`, `[foe-vs-hero:size-signature]`; foe vs member: the member branch with `[foe-vs-member:size]`); and the S1–S14 audit table copied from 75.2-01-PLAN.md with its verdicts.
2. Create test/unit/hero-size-rules.test.js with the behaviour above (a local comment stripper, as test/unit/roll-sign-consistency.test.js does). Its failure message states the RULES-11 rule for any new size read.
3. test/parity/FIXTURE-INVENTORY.md: close the Phase 75.2 section with `### Phase 75.2 — summary`: each plan's predictor, measurement and declared moves (from the SUMMARYs), the prototype master hash, and the guard's name.
4. test/parity/divergence-records.test.js: add the Phase 75.2 exposure guard in the behaviour list, walking every replay site with replaySiteEvents, with the teeth test.
  </action>
  <verify>
    <automated>node --test test/unit/hero-size-rules.test.js test/parity/divergence-records.test.js test/unit/roll-ledger-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 75.2 hero size (RULES-11)" docs/ROLL-LEDGER.md` equals 1, and `git diff --numstat <plan-base> -- docs/ROLL-LEDGER.md` shows 0 deleted lines.
    - `grep -c "| S14 |" docs/ROLL-LEDGER.md` equals 1.
    - `grep -c "### Phase 75.2 — summary" test/parity/FIXTURE-INVENTORY.md` equals 1, and `grep -c "Phase 75.2" test/parity/divergence-records.test.js` is at least 1.
    - `node --test test/unit/hero-size-rules.test.js` passes with at least 5 tests, and the new divergence guard passes.
  </acceptance_criteria>
  <done>The audit of every size-flavoured rule is in the ledger and guarded against a silent new size read, and the phase's fixture story is complete and guarded.</done>
</task>

<task type="auto">
  <name>Task 2: The final readout, the DIFFICULTY-RETUNE.md record, the voice sample and the gates</name>
  <files>docs/DIFFICULTY-RETUNE.md, tools/readouts/75.2-05-final.txt, tools/voice-sample.mjs, tools/voice-sample-output.txt</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md (the Phase 75 and Phase 75.1 H2s in full)
    - tools/readout-compare.mjs
    - test/unit/difficulty-retune-ledger.test.js
    - tools/readouts/75.2-01-*.txt and 75.2-02-*.txt, and those plans' SUMMARY readout notes
    - tools/voice-sample.mjs
  </read_first>
  <action>
1. Start `node tools/tune-difficulty.mjs --seeds=200` at the phase head with run_in_background, stdout to tools/readouts/75.2-05-final.txt, and wait for it.
2. Insert `## v2.1 hero size (Phase 75.2) — bot readouts` immediately before the v1.2 H2 (after the Phase 75.1 H2). It holds: a "Change under measurement" list (75.2-01: race sets size, ±2 damage and ±1 foe face per step, with each race's signature surviving — Dwarves keep +2 damage and gain a face of protection, Elves keep thin bones and lose 2 damage, Trolls stack both — and Joiners by their own race; 75.2-02: Enlarge becomes a +1 step and loses its separate damage); the parameters (200 seeds, start depth 1, the shipped dials; per-race: tune-classes --race, 10 seeds, 4 workers); `### Plan 01 — BEFORE`, `### Plan 01 — AFTER`, `### Plan 01 — per race BEFORE`, `### Plan 01 — per race AFTER`, `### Plan 02 — BEFORE`, `### Plan 02 — AFTER` and `### Phase 75.2 — FINAL`, each followed by a fenced block of headline lines copied verbatim from its file and a short reading; a per-race effect table (Elven, Dwarven, Troll: mean depth, p50, reach5, before → after) with one sentence per race; and a reading against the depth-20 unicorn and the floor 5–7 average, with a "Flag for the user" paragraph if the average left floors 5–7 or a race moved by more than a floor. No retune.
3. Prove every block with `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<file>` and run the ledger test.
4. tools/voice-sample.mjs: add constant `size`, `step` and `sizeDmg` fields to sampleEvent (no new random draw) and "size" to MOD_NAMES; append a short deterministic block rendering EVENT_NARRATION.itemEffectStarted for kinds giant and enlarge with those fields. Run `node tools/voice-sample.mjs` and commit the regenerated output.
5. Gates: `npm test` 0 failures; `node --test "test/parity/**/*.test.js"` passes; `git hash-object test/parity/prototype-master.js.txt` is unchanged from the phase base; `node --test test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js test/unit/roll-sign-consistency.test.js` passes.
6. Roll every Phase 75.2 plan's human checks into one list in the SUMMARY for the milestone's batched Pixel 7 round.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js "test/parity/**/*.test.js" test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js test/unit/roll-sign-consistency.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## v2.1 hero size (Phase 75.2) — bot readouts" docs/DIFFICULTY-RETUNE.md` equals 1 and test/unit/difficulty-retune-ledger.test.js passes.
    - For each of the seven headings, `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<matching file>` exits 0 (the seven commands listed in the SUMMARY).
    - tools/readouts/75.2-05-final.txt exists and is non-empty.
    - `grep -c "one size larger" tools/voice-sample-output.txt` is at least 1.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every Phase 75.2 balance change is recorded against machine-checked readouts, overall and per race, the voice sample shows the size lines, and every gate is green at the head.</done>
</task>

</tasks>

<verification>
- The size-rule audit is in the ledger and guarded; the fixture story is complete with an exposure guard; every readout is recorded and proven; the voice sample is current; npm test is green.
</verification>

<success_criteria>
ROADMAP Phase 75.2 criteria 3 and 5 hold: every rule that cares about a creature being big or small is audited and the hero's (and Joiners') size reads where the rule implies it, guarded against drift; moved fixtures are measured, declared and regenerated; and bot readouts before and after, overall and per race, are recorded with the phase's human checks batched for the milestone close.
</success_criteria>

<output>
Create `.planning/phases/75.2-hero-size-matters/75.2-05-SUMMARY.md` when done. Include the seven readout-compare commands, the per-race table, the gate results and the rolled-up human-check list.
</output>
