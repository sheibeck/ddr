---
phase: 75.2-hero-size-matters
status: passed
verified: 2026-09-26
verifier: orchestrator (deferred-UAT protocol; gsd-verifier disabled in config)
score: 1/1 requirements
human_verification:
  - "Hero tab SIZE row per race: a Troll reads 'Large · +2 damage, −1 vs their swings'; an Elf reads 'Small · −2 damage' and is still thin-boned; a Dwarf reads 'Small · +1 vs their swings' and keeps +2 damage; a Human has no size effect. Wearing the Gauntlet of the Giant adds '· <race size> by birth', and the Damage line shifts by +2."
  - "Foe details and the Oracle foe-swing line show a 'size' modifier for a Troll (size −1) and a Dwarf (size +1). An Elf shows only 'Elven −1', with no size token."
  - "The Gauntlet of the Giant and an Enlarge potion each read 'one size larger for fifty squares: +2 damage, and one face easier for foes to hit'. Enlarge no longer adds a separate +4. Its chip names the resulting size ('Large'/'Huge') and counts down, and both stacked read as two steps."
  - "Joiners: a Troll Joiner hits harder and is easier to hit; a Dwarven Joiner is harder to hit."
---

# Phase 75.2: Hero Size Matters — Verification

**Verdict:** passed on automated evidence. The device checks above are batched into the milestone-close Pixel 7 checklist. The balance verdicts are deferred to Phase 79.1 by user ruling (2026-09-26: bots run only at the milestone end).

## Requirement coverage

| Req | Evidence | Status |
|-----|----------|--------|
| RULES-11 | 75.2-01 adds the size seam in `engine/derived.js` (`raceSizeStep` / `itemSizeStep` / `sizeAxisStep` / `sizeName` / `heroSize` / `sizeDamage`): ±2 damage and ±1 foe face per step, for the hero and for Joiners. The race-signature masks are DATA in `content/races.js` (Dwarven `{dmg:false}`, Elven `{face:false}`), with a guard that derives the masks from the trait fields. 75.2-02: the Gauntlet of the Giant and Enlarge are exactly +1 step each, Enlarge's +4 is removed, the text is honest, and the size fields ride on the event and the chip. 75.2-03: the hero sheet SIZE row, and the damage bracket / `#s-dmg` include size. 75.2-04: the stepped-size chips, the Oracle/rail size lines, the race notes stating the net truth, and a 5-surface size scenario in the sign guard. 75.2-05: the S1–S14 audit in ROLL-LEDGER, a standing engine source-scan guard (no race-name checks), and the fixture close with an exposure guard | ✓ |

## User rulings honoured
- **Race signatures survive size.** Dwarves are +2 damage and one face harder to hit. Elves are −2 damage and still thin-boned. Trolls are +11 damage and one face easier to hit. Item steps always apply in full.
- **Joiners get size** under the same rule.

## Automated gates
- Full `npm test` on master: **6501/6501 pass**. Parity **60/60**. The prototype master hash is unchanged. `boot:check` 4/4.
- **Zero parity fixtures moved.** Declared state-pin re-pins, with traced causes:
  - `solo-2` and `party-fighter-knight` (the Dwarven face step)
  - `party-fighter-knight` and `deep-8` (the size items' rewritten text in the unstripped state hash)
  - one bot-tactics seed swap (a Troll)
  - the pre-switch save's `expected.hash` was re-recorded because of item-text changes; the save data is untouched.
- **Orchestrator fix 85826e0f:** after 75.2-02 gave Enlarge its own kind, placeholder chip copy was added to keep master green, and 75.2-04 then wrote the real copy.

## Balance
The BEFORE readouts are committed (`tools/readouts/75.2-01-before.txt` and `75.2-01-races-before.txt`). The AFTER readout and the per-race verdicts are **deferred to Phase 79.1** by user ruling. `docs/DIFFICULTY-RETUNE.md` carries a Phase 75.2 stub.
