---
phase: 75.2-hero-size-matters
plan: 04
type: execute
wave: 3
depends_on:
  - 75.2-01
  - 75.2-02
  - 75.2-03
files_modified:
  - mazeworld.html
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - content/races.js
  - content/flavor.js
  - test/unit/size-voice.test.js
  - test/unit/roll-sign-consistency.test.js
  - test/unit/status-chit-combat.test.js
  - test/unit/narrationLinesTable.test.js
autonomous: true
requirements:
  - RULES-11
must_haves:
  truths:
    - "A live size-stepping item's condition chip is labelled with the hero's resulting size (75.2-CONTEXT: 'condition chips show a stepped size while it lasts'): a Human using the Gauntlet shows a 'Large' chip with its squares left; Gauntlet and Enlarge both live show two 'Huge' chips, each counting its own item down"
    - "The chip's tap card leads with conditionEffectText's measured '−1 vs their swings', then names the item and its squares, then says exactly what a step does ('One size larger while it lasts: +2 damage, and one face easier for foes to hit.'); no chip copy promises overhead clearance"
    - "`enlarge` has CONDITION_COPY, CONDITION_TONE and CONDITION_EXPLAIN entries alongside `giant`; a chip with no `size` field (any other item) renders exactly as before"
    - "The Oracle and the rail narrate a size item's start from the event's own fields: 'N squares one size larger: you are <size>. <sizeDmg> damage, and one face easier for foes to hit.' in voice, the number printed through rollRange.js#signedText; an event without size fields falls back to a plain 'one size larger for N squares' line; the old corridor line is gone"
    - "The Elven, Dwarven and Troll notes (content/races.js `note` and content/flavor.js RACE_NOTE) state the net truth under the size rule and the user's signature ruling (race signatures survive size): an Elf strikes a die better and hits on 5, is still thin-boned and easy to hit, carries 0.6x hp, and being small costs it 2 damage; a Dwarf keeps its +2 damage and half-rate armour wear, being small makes it one face harder to hit, and foes strike at a better die; a Troll has 75 hp and +11 damage (being large adds 2), is one face easier to hit, pays triple and eats two rations"
    - "The cross-surface sign guard covers size and the signature mask: a real foeTurn against a Troll reads 'size −1' on the Oracle line, the fight-log reveal and the foe details odds line, and the Troll's hero-sheet SIZE detail reads '−1 vs their swings'; a Dwarf reads 'size +1' on the Oracle line and the foe details odds line; an Elf reads 'Elven −1' there and no size token at all; a Human's live Gauntlet chip effect reads '−1 vs their swings' (encoding: every minus is U+2212)"
  artifacts:
    - path: mazeworld.html
      provides: "the size chip label rule and the giant/enlarge chip copy, tone and tap text"
      contains: "cn.size"
    - path: src/browser/eventNarration.js
      provides: "the Oracle lines for giant and enlarge"
      contains: "enlarge:"
    - path: src/browser/narrationLines.js
      provides: "the rail lines for giant and enlarge"
      contains: "enlarge:"
    - path: content/flavor.js
      provides: "the rewritten Elven, Dwarven and Troll RACE_NOTE"
      contains: "RACE_NOTE"
    - path: test/unit/size-voice.test.js
      provides: "the chip, narration and race-note checks and the voice scan"
    - path: test/unit/roll-sign-consistency.test.js
      provides: "the size scenario"
      contains: "size"
  key_links:
    - from: mazeworld.html#paintConditions
      to: engine/derived.js#conditionsOf (chip.size)
      via: "the chip label reads the chip's own size name"
      pattern: "cn\\.size"
    - from: src/browser/eventNarration.js#itemEffectStarted
      to: engine/items.js#applyActivation (size, sizeDmg)
      via: "the Oracle line reads the event's own size fields"
      pattern: "sizeDmg"
  prohibitions:
    - statement: "MUST NOT make size copy mock a body type: chip text, Oracle and rail lines and race notes stay deadpan and family-friendly, poking fun at doorways and odds, never at a player's weight or build as an insult"
      requirement_id: RULES-11
      category: values
      status: unverified
      flagged: true
---

<objective>
Make every surface say what size does (RULES-11): the chip for a live Gauntlet or Enlarge is labelled with the hero's resulting size and explains exactly what a step does; the Oracle and the rail narrate the start of a size effect from the event's own fields; the Elven, Dwarven and Troll notes state their net truth; and the cross-surface sign guard proves the size modifier reads the same everywhere.

Purpose: 75.2-CONTEXT's display rule ("condition chips show a stepped size while it lasts") and its item-text rule ("their text states exactly what a step does ... the 'mind the ceilings' promise is dropped"), plus Phase 74's standing rule that the same modifier never shows two signs. After 75.2-01 every sized race's note is incomplete: none mentions its size, the Elf's lost damage, the Dwarf's extra face of protection, or the Troll's extra damage and exposure.

Output: the chip copy and label rule, the narration lines, the race notes, the size voice test and the sign scenario.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75.2-04 creates or changes.)

- 75.2-01: the size helpers and the size term in damage and the foe's odds (hero and Joiners); `test/unit/hero-size.test.js`; the size direction and ledger rows; identity and CLASS-PASS rows; readouts
- 75.2-02: the Gauntlet and Enlarge as one step each, their texts, the `itemEffectStarted` and `conditionsOf` size fields; `test/unit/size-items.test.js`; readouts
- 75.2-03: the hero sheet's SIZE row and `#s-size`; the sheet's damage reading `sizeDamage`; `test/unit/hero-size-display.test.js`; the declared hero snapshots
- **This plan (75.2-04):** the `cn.size` chip label rule and the `giant` / `enlarge` CONDITION_COPY, CONDITION_TONE and CONDITION_EXPLAIN entries in mazeworld.html; the `giant` / `enlarge` lines in `EVENT_NARRATION.itemEffectStarted` and `LINE_FOR.itemEffectStarted`; the Elven, Dwarven and Troll notes in content/races.js and content/flavor.js; `test/unit/size-voice.test.js`; the size scenario in `test/unit/roll-sign-consistency.test.js`
- 75.2-05: the size-rule audit and its guard; the consolidated fixture section and exposure guard; the DIFFICULTY-RETUNE.md record and final readout; the voice sample; the gates

## Flagged assumptions (surfaced, not silently decided)

- **One chip per item, labelled with the resulting size.** Two live size items show two chips with the same size name, each counting its own item down; the tap text names which item. Chip wording and tone (`good` for both, like the Gauntlet today) are Claude's discretion.
- **Race-note wording** is Claude's discretion within the net facts above; Phase 79's blurb sweep may polish it further.
- **Edge probe (RULES-11):** encoding (U+2212 on every surface) is covered by the sign scenario above.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75.2-hero-size-matters/75.2-CONTEXT.md
@.planning/phases/75.2-hero-size-matters/75.2-02-SUMMARY.md
@.planning/phases/75.2-hero-size-matters/75.2-03-SUMMARY.md
@.planning/phases/74-roll-display-modifier-honesty/74-08-SUMMARY.md

<interfaces>
- 75.2-02 (read its SUMMARY): `conditionsOf` chips for a live size-stepping item carry `step` and `size` (the hero's current size name), `key` "giant" (Gauntlet) or "enlarge" (Enlarge), plus remaining, cadence and source. `itemEffectStarted` for them carries `size`, `step`, `sizeDmg`.
- 75.2-03 (read its SUMMARY): characterSheetViewModel's `size` row `{ key, label, value, step, base, detail, text }`; the "vs their swings" phrasing.
- mazeworld.html: CONDITION_COPY (the `giant` row, label "Giant-handed", unit "sq"), CONDITION_TONE (`giant: "good"`), CONDITION_EXPLAIN (`giant` holds a sentence with an overhead-clearance promise), `explainCondition(cn, label)` (prefixes the item name and the lower-cased label), and `paintConditions`'s label chain (affliction, foeEffect, itemCooldown/staffCharges, ward, then `CONDITION_COPY[cn.key]?.label || cn.key`). A chip tap's card leads with src/browser/conditionEffects.js#conditionEffectText (bridged as window.__mzConditionEffect), then explainCondition's sentence; the effect text measures the item-source what-if generically (so the size chip's "−1 vs their swings" needs no new WHAT_IF key).
- src/browser/eventNarration.js and narrationLines.js: `itemEffectStarted` builds a per-kind `map` (haste, invis, ether, acute, might, fly, lit, power, giant, glow, unseen, tongue, brace, plate) with a generic fallback; the `giant` entries hold the old corridor line. Both modules may import from ./rollRange.js.
- test/unit/roll-sign-consistency.test.js: fullHeroState(foe, over), plainFoe, fakeRng, signedTokens/assertModifier, EVENT_NARRATION, oracleDetailText, foeDetailsCard, characterSheetViewModel, conditionsOf, conditionEffectText, startEffect is importable from engine/effects.js.
- The voice rules: HP not WP; U+2212 for a minus; deadpan and family-friendly (test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The stepped-size chips and the size lines in the Oracle and the rail</name>
  <files>mazeworld.html, src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/size-voice.test.js, test/unit/status-chit-combat.test.js, test/unit/narrationLinesTable.test.js</files>
  <read_first>
    - mazeworld.html (CONDITION_COPY, CONDITION_TONE, CONDITION_EXPLAIN, explainCondition, paintConditions — grep for each name; read only those regions)
    - src/browser/conditionEffects.js (conditionEffectText, itemSourceWhatIf)
    - src/browser/eventNarration.js and src/browser/narrationLines.js (itemEffectStarted in each)
    - src/browser/rollRange.js (signedText)
    - test/unit/status-chit-combat.test.js (its shell-sandbox chip harness: r.chip, r.explain, r.descriptor)
    - test/unit/narrationLinesTable.test.js
  </read_first>
  <behavior>
    - A Human with a live Gauntlet record: the painted chip's label is "Large", its detail "<left> sq", its tone good; its tap card reads "−1 vs their swings. Gauntlet of the Giant — large, <left> squares. One size larger while it lasts: +2 damage, and one face easier for foes to hit." (the measured lead, then explainCondition).
    - Gauntlet and Enlarge both live on a Human: two chips labelled "Huge", in c.timers insertion order.
    - Enlarge alone: chip "Large", tone good; tap text names "Enlarge".
    - A chip without a `size` field (a Ring of Power's power chip, a Strength might chip) renders its old label and detail.
    - EVENT_NARRATION.itemEffectStarted({ kind: "giant", left: 50, size: "Large", step: 1, sizeDmg: 2 }) and the enlarge equivalent name "Large", "+2 damage" and "one face easier for foes to hit"; LINE_FOR.itemEffectStarted does the same with tone "magic"; an event without size fields gives the plain fallback line; no line mentions overhead clearance or the corridor.
    - Voice: every new string uses hp, not wp; no ASCII hyphen-minus before a digit; it passes the safety scan.
  </behavior>
  <action>
1. mazeworld.html, per 75.2-CONTEXT ("condition chips show a stepped size while it lasts"; "their text states exactly what a step does"):
   - CONDITION_COPY: the `giant` row's fallback label becomes "Enlarged"; add `enlarge: { label: "Enlarged", unit: "sq" }`.
   - CONDITION_TONE: add `enlarge: "good"`.
   - CONDITION_EXPLAIN: `giant` and `enlarge` both become "One size larger while it lasts: +2 damage, and one face easier for foes to hit." (the old sentence's overhead-clearance clause is gone).
   - paintConditions: in the label chain, before the generic CONDITION_COPY fallback, a chip carrying a non-empty string `cn.size` is labelled with it. A RULES-11 (Phase 75.2) comment.
2. src/browser/eventNarration.js and narrationLines.js: replace the `giant` entry and add `enlarge` in both `itemEffectStarted` maps. When the event carries `size`, the line reads "<left> squares one size larger: you are <size>. <signedText(sizeDmg)> damage, and one face easier for foes to hit." with a short deadpan tail if it fits (about being a bigger target, never about weight); without `size`, "One size larger for <left> squares." The Oracle wraps it in the existing `hit` span; the rail keeps tone "magic" and PRIORITY.you. Import signedText from ./rollRange.js if the module lacks it.
3. Create test/unit/size-voice.test.js with the behaviour above: drive chips through the shell sandbox the status-chit tests use (a state with `startEffect` records for the items), and the narration builders directly with events shaped like 75.2-02's. Update narrationLinesTable.test.js or status-chit-combat.test.js only where a pinned `giant` string moved, with a RULES-11 comment.
  </action>
  <verify>
    <automated>node --test test/unit/size-voice.test.js test/unit/status-chit-combat.test.js test/unit/narrationLinesTable.test.js test/unit/narrationLinesCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ci "ceilings" mazeworld.html` equals 0.
    - `grep -c "too large for the corridor" src/browser/eventNarration.js src/browser/narrationLines.js` prints 0 for each file.
    - `grep -c "enlarge:" src/browser/eventNarration.js` and `grep -c "enlarge:" src/browser/narrationLines.js` are each at least 1.
    - `grep -c "cn.size" mazeworld.html` is at least 1, and `grep -c "enlarge:" mazeworld.html` is at least 3 (copy, tone, explain).
    - `node --test test/unit/size-voice.test.js` passes with at least 6 tests.
  </acceptance_criteria>
  <done>A live size item shows a chip named for the size it makes you, its tap text says exactly what that means, and the Oracle and rail announce it the same way.</done>
</task>

<task type="auto">
  <name>Task 2: The race notes and the size scenario in the cross-surface sign guard</name>
  <files>content/races.js, content/flavor.js, test/unit/size-voice.test.js, test/unit/roll-sign-consistency.test.js</files>
  <read_first>
    - content/races.js (the Elven, Dwarven and Troll `note` strings and their comments)
    - content/flavor.js (RACE_NOTE)
    - test/unit/identity-contract.test.js (75.2-01's renamed race entries, for the net facts)
    - test/unit/roll-sign-consistency.test.js (in full)
    - src/browser/foeDetails.js (foeDetailsCard's "it hits you on" line)
  </read_first>
  <action>
1. content/races.js `note` and content/flavor.js RACE_NOTE for Elven, Dwarven and Troll, extended to the net facts in this plan's truths (the same facts 75.2-01 wrote into the identity contract and the user's signature ruling), in the existing deadpan voice, HP not WP. Keep every existing claim that is still true (thin bones, the elven plan to kill it first, the dwarven +2 and the fair trade, the Troll starving first and paying triple) and add the size clause each race now has: the Elf's size costs it 2 damage and nothing else; the Dwarf's size makes it one face harder to hit and never touches its +2; the Troll's size adds 2 damage (+11 in all) and one face for foes. Add a RULES-11 (Phase 75.2) comment on each changed row. Human, Wilmsry and Fridgian are untouched.
2. Add to test/unit/size-voice.test.js: each rewritten note names its race's size ("small" or "large"); the Elven note still calls elves thin-boned and mentions the 2-damage cost; the Dwarven note still offers its +2 damage and mentions being harder to hit; the Troll note says +11 damage and easier to hit; every note passes the voice rules.
3. test/unit/roll-sign-consistency.test.js: add a scenario "size: Large and Small read the same player-signed size token on every surface", driven through the real engine like its neighbours:
   - a Troll hero: a real foeTurn event's Oracle line, its fight-log reveal (oracleDetailText) and foeDetailsCard's "it hits you on" line each carry "size −1"; characterSheetViewModel's size row detail contains "−1 vs their swings";
   - a Dwarven hero: the Oracle line and the foe details odds line carry "size +1";
   - an Elven hero: the Oracle line and the foe details odds line carry "Elven −1" and signedTokens(text, ["size"]) is empty on both (the signature mask keeps size off the Elf's foe roll);
   - a Human with a live Gauntlet record: conditionsOf's giant chip's conditionEffectText contains "−1 vs their swings".
4. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/size-voice.test.js test/unit/roll-sign-consistency.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 75.2" content/flavor.js` and `grep -c "Phase 75.2" content/races.js` are each at least 1.
    - `grep -ci "small" content/flavor.js` is at least 2 and `grep -c "+11" content/flavor.js` is at least 1 (the Elven and Dwarven RACE_NOTE name their size; the Troll's states its total).
    - `grep -c "size −1" test/unit/roll-sign-consistency.test.js` is at least 1 and `grep -c "size +1" test/unit/roll-sign-consistency.test.js` is at least 1.
    - `node --test test/unit/roll-sign-consistency.test.js` passes with at least 12 tests.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Every race's note tells its true size story, and the sign guard proves the size modifier reads the same on the Oracle, the fight log, foe details, the hero sheet and the chip.</done>
</task>

</tasks>

<verification>
- A live size item shows a chip named for the resulting size with an exact explanation; the Oracle and rail announce size effects from the event's own fields; no surface promises overhead clearance.
- The Elven, Dwarven and Troll notes state their net truth; the sign guard covers size on five surfaces; npm test is green.
  <human-check>
  Pixel 7, at milestone close: use a Gauntlet of the Giant; the chip strip shows "Large" with its squares, a tap explains "+2 damage, and one face easier for foes to hit" and "−1 vs their swings", and the Oracle and rail lines say the same; drink an Enlarge too and both chips read "Huge". Read the Elven, Dwarven and Troll race notes on the roll screen or the dossier: each states its size and net effect (the Elf still thin-boned, the Dwarf still +2 damage). In a fight, a Dwarf's foe odds show "size +1" and an Elf's show no size entry.
  </human-check>
</verification>

<success_criteria>
ROADMAP criteria 2 and 4 are visible everywhere: the size effect shows in the roll breakdown, the chips and the narration, and item and race text states exactly what size does.
</success_criteria>

<output>
Create `.planning/phases/75.2-hero-size-matters/75.2-04-SUMMARY.md` when done. Include the final chip, Oracle, rail and race-note strings.
</output>
