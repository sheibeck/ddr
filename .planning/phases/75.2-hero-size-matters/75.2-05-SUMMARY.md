---
phase: 75.2-hero-size-matters
plan: 05
subsystem: engine-rules
tags: [size, roll-ledger, fixture-inventory, divergence-guard, difficulty-retune, voice-sample, engine-gate]

# Dependency graph
requires:
  - phase: 75.2-hero-size-matters (Plans 01-04)
    provides: "the size-rule seam (engine/derived.js's sizeAxisStep/heroSize/sizeDamage), the item event/chip fields, the hero-sheet SIZE row, and every voice surface — this plan audits, guards, and closes the phase's fixture/ledger/readout/voice-sample story over that landed work"
provides:
  - "docs/ROLL-LEDGER.md's `## Phase 75.2 hero size (RULES-11)`: the rule paragraph, the size-modifier-sites table, and the S1-S14 audit table copied verbatim from 75.2-01-PLAN.md"
  - "test/unit/hero-size-rules.test.js (9 tests): a comment-stripped engine/*.js scan proving the only eff(sheet,\"size\")/SIZE_STEP_OF/sizeAxes reads sit inside itemSizeStep/raceSizeStep/sizeAxisStep, every sizeAxisStep( call site is in the audited set, no engine file names a race to decide a mask, the only foe-size reads are the two picked.sz builders, and the ledger lists S1-S14 — with a teeth test"
  - "test/parity/FIXTURE-INVENTORY.md's `### Phase 75.2 — summary`: the consolidated per-plan predictor/measurement/declared-moves table and the unchanged prototype master hash"
  - "test/parity/divergence-records.test.js's RULES-11 exposure guard: no fight-starting replay site's hero carries a non-zero size axis, no itemEffectStarted of kind giant/enlarge fires anywhere, and no event carries a size mod, across all 31 replay sites — with its own teeth test"
  - "docs/DIFFICULTY-RETUNE.md's `## v2.1 hero size (Phase 75.2) — bot readouts`: the rule under measurement, the already-committed BEFORE readout, and an explicit deferral of the FINAL/AFTER/per-race verdicts to Phase 79.1"
  - "tools/voice-sample.mjs's size-aware sampleEvent fields and its new giant/enlarge deterministic block; the regenerated tools/voice-sample-output.txt"
affects: ["79.1-milestone-close-balance-step"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A source-scanning seam guard (test/unit/hero-size-rules.test.js) built from one shared primitive — functionBodySpan + violationsFor — that checks every real match of a pattern falls inside an allow-listed function's own body; the SAME primitive is fed a doctored source string in a \"has teeth\" test, proving the guard fails on a genuine violation rather than merely passing on today's code"
    - "An exposure guard (divergence-records.test.js's RULES-11 test) checks a fight-starting hero's OWN pre-action size (`sizeAxisStep(newRun(seed).c, axis)`) directly against the engine's own exported primitive, never re-deriving race/mask logic — the guard can never silently drift from what the engine itself would compute"

key-files:
  created:
    - test/unit/hero-size-rules.test.js
  modified:
    - docs/ROLL-LEDGER.md
    - test/parity/FIXTURE-INVENTORY.md
    - test/parity/divergence-records.test.js
    - docs/DIFFICULTY-RETUNE.md
    - tools/voice-sample.mjs
    - tools/voice-sample-output.txt

key-decisions:
  - "ORCHESTRATOR RULING (2026-09-26, carried from Plans 01/02): bot balance readouts across v2.1 now run ONCE at the milestone's own close (Phase 79.1), not per-phase. This plan's own Task 2 skipped running `node tools/tune-difficulty.mjs --seeds=200` and any `tune-classes --race` pass, and did NOT write the full 7-heading readout-compare-proven `## v2.1 hero size (Phase 75.2) — bot readouts` section the plan's own must_haves originally specified. Instead, docs/DIFFICULTY-RETUNE.md gets a short stub: the rule shipped, the BEFORE readout already committed by 75.2-01 (`tools/readouts/75.2-01-before.txt`, `tools/readouts/75.2-01-races-before.txt`), and an explicit \"final readout + per-race verdicts deferred to Phase 79.1 (user ruling 2026-09-26)\" note. No `tools/readouts/75.2-05-final.txt` was created. Every plan acceptance criterion naming an AFTER/FINAL/per-race readout is satisfied by this deferral, not by a readout-compare proof — see Deviations below."
  - "The size-rule audit's guard (hero-size-rules.test.js) is built on function-body containment (brace-depth counting from a matched declaration) rather than a simple occurrence count, because the plan's own audited primitives (`sizeAxes`, `sizeAxisStep(`) are legitimately read MORE than once inside their one audited function — the guard's job is proving every occurrence stays inside the allowed function, not counting to exactly one."
  - "The RULES-11 exposure guard in divergence-records.test.js checks the fight-starting hero's OWN pre-action size directly via `sizeAxisStep(newRun(seed).c, axis)` (imported straight from engine/derived.js) rather than re-deriving 'is this race sized' from a race-name list — the guard can never drift out of sync with what the engine itself computes for a future race."

requirements-completed: [RULES-11]

coverage:
  - id: D1
    description: "The size-rule audit (S1-S14) is written into docs/ROLL-LEDGER.md verbatim from 75.2-01-PLAN.md, and a standing guard (test/unit/hero-size-rules.test.js) proves the audited seam (eff(sheet,\"size\")/SIZE_STEP_OF/sizeAxes/sizeAxisStep( calls, the two picked.sz foe-record builders) holds across engine/*.js, with a doctored-source teeth test proving the guard would catch a future violation"
    requirement: RULES-11
    verification:
      - kind: unit
        ref: "test/unit/hero-size-rules.test.js (9 tests, all pass)"
        status: pass
      - kind: unit
        ref: "test/unit/roll-ledger-sync.test.js (unaffected — still green after the ledger append)"
        status: pass
    human_judgment: false
  - id: D2
    description: "test/parity/FIXTURE-INVENTORY.md's Phase 75.2 section closes with a consolidated per-plan summary (predictor/measurement/declared moves, the unchanged prototype master hash), and test/parity/divergence-records.test.js's RULES-11 exposure guard proves zero size exposure across all 31 replay sites (no fight-starting sized hero, no giant/enlarge item start, no size mod anywhere), with its own teeth test"
    requirement: RULES-11
    verification:
      - kind: unit
        ref: "test/parity/divergence-records.test.js (16 tests including the 2 new RULES-11 tests, all pass)"
        status: pass
      - kind: other
        ref: "node --test \"test/parity/**/*.test.js\": 60/60, 0 fail; git hash-object test/parity/prototype-master.js.txt unchanged (a1f4d0dc29782218d8e5aab65bc5989c33f917f0)"
        status: pass
    human_judgment: false
  - id: D3
    description: "docs/DIFFICULTY-RETUNE.md records the Phase 75.2 rule and its already-committed BEFORE readout with an explicit deferral of the FINAL/AFTER/per-race verdicts to Phase 79.1 (2026-09-26 orchestrator ruling); tools/voice-sample.mjs's giant/enlarge lines render their size fields deterministically (\"one size larger\" present); every whole-phase gate (npm test, parity suite, prototype master hash, roll-high guard, direction tests, ledger sync, state pins, save compat, sign-consistency) is green at the head"
    requirement: RULES-11
    verification:
      - kind: unit
        ref: "test/unit/difficulty-retune-ledger.test.js (13/13, v1.2 H2 confirmed still last); node --test test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js test/unit/roll-sign-consistency.test.js (266/266, 0 fail)"
        status: pass
      - kind: other
        ref: "npm test: 6501/6501, 0 failures"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every Phase 75.2 plan's own deferred Pixel-7 human-check item, rolled up into one batched list for the milestone-close UAT round"
    verification: []
    human_judgment: true
    rationale: "Requires a physical device (Pixel 7) per the project's deferred-UAT protocol; see 'Human verification (deferred to end of run)' below for the rolled-up list."

duration: ~55min
completed: 2026-09-26
status: complete
---

# Phase 75.2 Plan 05: The size-rule audit, the fixture close, the DIFFICULTY-RETUNE stub, and the whole-phase gates Summary

**The size-rule audit (S1-S14) is now written into docs/ROLL-LEDGER.md and guarded by a standing engine-wide source scan; the parity fixture story closes with a consolidated per-plan summary and a teeth-tested 31-site exposure guard proving zero size drift; docs/DIFFICULTY-RETUNE.md records the shipped rule and its already-committed BEFORE readout with the FINAL/AFTER/per-race verdicts explicitly deferred to the milestone-end balance step (Phase 79.1, per the 2026-09-26 orchestrator ruling); the voice sample now renders the Gauntlet/Enlarge size lines deterministically; and every whole-phase gate is green (npm test 6501/6501).**

## Performance

- **Duration:** ~55 min
- **Started:** 2026-09-26T16:05:00Z (session start, plan base e2f6eec8)
- **Completed:** 2026-09-26T17:01:28Z
- **Tasks:** 2
- **Files modified:** 7 (1 created, 6 modified)

## Accomplishments

- **Task 1 — the size-rule audit in the roll ledger, its standing guard, and the fixture close:** `docs/ROLL-LEDGER.md` gains `## Phase 75.2 hero size (RULES-11)` (appended via a Node script, CRLF preserved, zero deleted lines — `git diff --numstat` confirms 66 insertions/0 deletions): the rule paragraph (race base plus items, ±2 damage / ±1 foe face per step, the signature rule, `size` mod signed for the foe, Joiners by their own race and mask, no draws), a "Size modifier sites" table, and the S1-S14 audit table copied verbatim from `75.2-01-PLAN.md`. `test/unit/hero-size-rules.test.js` (new, 9 tests) builds a comment-stripped, import-stripped scan of every `engine/*.js` file and proves: the only `eff(sheet, "size")` call sits inside `itemSizeStep`; the only `SIZE_STEP_OF` read sits inside `raceSizeStep`; the only `sizeAxes` read sits inside `sizeAxisStep`; every `sizeAxisStep(` call site is one of `derived.js`'s own `heroSize`/`sizeDamage`/`foeToHitVs`/`foeToHitBreakdown` or `combat.js#foeTurn`'s member branch; `sizeAxisStep`'s own body never names a race to decide its mask; the only `.sz` reads are the two `picked.sz` foe-record builders (`engine/combat.js`, `engine/foeAbilities.js`); no engine file reads `.size` off a foe-like binding; and the ledger lists S1 through S14. A "has teeth" test feeds the SAME scanning primitive (`functionBodySpan`/`violationsFor`) a doctored source string carrying a second, unaudited `eff(f, "size")`/`sizeAxes` read and confirms it is reported. `test/parity/FIXTURE-INVENTORY.md`'s Phase 75.2 section closes with `### Phase 75.2 — summary` — a consolidated table of every plan's own predictor/measurement/declared-moves (Plans 01/02 verbatim from their own subsections; Plans 03/04 noted as UI/content-only, touching no `engine/*.js` file, so zero parity exposure was the only possible outcome) and the unchanged prototype master hash. `test/parity/divergence-records.test.js` gains the RULES-11 exposure guard: across all 31 replay sites, the seven sites whose own action script ever dispatches `startCombat` (`combat.json`'s win/lose/lose-apprentice/lose-plain/flee/parley and `magic.json`'s cast-damage) each start with a hero whose `sizeAxisStep(c, "dmg")` and `sizeAxisStep(c, "face")` are both 0 (all seven confirmed Human/Fridgian/Wilmsry); zero `itemEffectStarted` events of kind `giant`/`enlarge` fire anywhere; and zero events anywhere carry a `size` mod — with its own teeth test. `node --test "test/parity/**/*.test.js"`: 60/60 (up from 58, the two new tests); `git hash-object test/parity/prototype-master.js.txt` unchanged (`a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **Task 2 — the DIFFICULTY-RETUNE stub, the voice sample, and the whole-phase gates:** Per the 2026-09-26 orchestrator ruling (bot balance readouts now run once, at the Phase 79.1 milestone close, not per-phase), this task did NOT run `tools/tune-difficulty.mjs` or any `tune-classes --race` pass, and did NOT write the full 7-heading readout-compare-proven section the plan's own must_haves originally specified. Instead, `docs/DIFFICULTY-RETUNE.md` gains `## v2.1 hero size (Phase 75.2) — bot readouts`, inserted immediately before the standing `## v1.2 retune (Phase 27)` H2 (confirmed still the ledger's LAST H2 — `test/unit/difficulty-retune-ledger.test.js` test (1) passes), via the same CRLF-preserving Node-script append method (78 insertions/0 deletions): the rule under measurement (race base + item steps, ±2 dmg / ±1 face per step, the race-signature masks), the BEFORE readout already committed by 75.2-01 (`tools/readouts/75.2-01-before.txt`'s overall headline numbers and `tools/readouts/75.2-01-races-before.txt`'s per-race pooled numbers, quoted verbatim), and an explicit deferral: "the phase FINAL readout, the AFTER readout for both plans, and every per-race verdict... are deferred to Phase 79.1 (user ruling 2026-09-26)." `tools/voice-sample.mjs`: `sampleEvent`'s constant fields gain `size: "Large"`, `step: 1`, `sizeDmg: 2` (no new `rng()` draw — inserting one would reshuffle every sample after it), `"size"` joins `MOD_NAMES`, and a new deterministic block renders `EVENT_NARRATION.itemEffectStarted` for both `giant` and `enlarge` kinds with those same fields. Regenerated `tools/voice-sample-output.txt` (984 lines, up from 982 — the two new size-item lines; every other line-count-preserving diff is the expected MOD_NAMES reshuffle of which mod name gets picked at each existing `rng()` draw); `grep -c "one size larger" tools/voice-sample-output.txt` is 2. Gates: `node --test test/unit/difficulty-retune-ledger.test.js "test/parity/**/*.test.js" test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js test/unit/roll-sign-consistency.test.js` — 266/266, 0 fail. **`npm test`: 6501/6501, 0 failures** (up from the dispatch-time baseline of 6490 — exactly the 11 new tests this plan added: 9 in `hero-size-rules.test.js`, 2 in `divergence-records.test.js`).

## Task Commits

Each task was committed atomically:

1. **Task 1: The size-rule audit in the roll ledger, its standing guard, and the fixture close** — `58a16b25` (test)
2. **Task 2: The DIFFICULTY-RETUNE stub, the voice sample, and the whole-phase gates** — `78b0c112` (docs)

**Plan metadata:** this SUMMARY.md's own commit (pending, by the orchestrator's convention — this plan ran as a sequential executor on the main working tree, worktree isolation unavailable on this machine).

## Files Created/Modified

- `docs/ROLL-LEDGER.md` — `## Phase 75.2 hero size (RULES-11)`: the rule, the modifier-sites table, the S1-S14 audit table
- `test/unit/hero-size-rules.test.js` — new, 9 tests, the standing size-read seam guard (with a teeth test)
- `test/parity/FIXTURE-INVENTORY.md` — `### Phase 75.2 — summary`: the consolidated per-plan table and hash
- `test/parity/divergence-records.test.js` — the RULES-11 exposure guard (with a teeth test)
- `docs/DIFFICULTY-RETUNE.md` — `## v2.1 hero size (Phase 75.2) — bot readouts`: the rule, the committed BEFORE readout, the deferral note
- `tools/voice-sample.mjs` — `sampleEvent`'s size fields, `MOD_NAMES` gains `"size"`, the new giant/enlarge deterministic block
- `tools/voice-sample-output.txt` — regenerated (984 lines)

## Decisions Made

- Bot balance readouts deferred to Phase 79.1 across this whole plan (the 2026-09-26 orchestrator ruling, already established by Plans 01/02) — see `key-decisions` above and Deviations below.
- The size-rule audit guard uses function-body containment (brace-depth counting), not a bare occurrence count, since the audited primitives are legitimately read more than once inside their one allowed function.
- The RULES-11 exposure guard reads the engine's own `sizeAxisStep` directly rather than re-deriving "which races are sized," so it can never drift from the engine's own computation.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 4 — surfaced, not auto-applied without basis] Task 2 restructured per a standing orchestrator ruling already established by prior plans**

- **Found during:** Task 2 planning (before any file edit)
- **Issue:** The plan's own must_haves and Task 2 `<action>` specify running `tools/tune-difficulty.mjs --seeds=200` for a phase FINAL readout, writing `tools/readouts/75.2-05-final.txt`, and recording a full 7-heading `docs/DIFFICULTY-RETUNE.md` section (BEFORE/AFTER for both plans plus the phase FINAL) each proven via `node tools/readout-compare.mjs --recorded`.
- **Resolution:** This orchestrator's own dispatch notes explicitly override that instruction, citing the SAME 2026-09-26 user ruling Plans 01 and 02's SUMMARYs already document verbatim ("bot balance runs happen ONCE at the milestone end, new Phase 79.1... SKIP the final readout and any per-race/tune-classes run in this plan"). This is not a new decision this executor made unilaterally — it is following an explicit, already-corroborated instruction from the dispatching orchestrator, consistent with three prior plans' own committed SUMMARYs. Per that instruction, `docs/DIFFICULTY-RETUNE.md` records a short stub instead (the rule, the already-committed BEFORE readout, and an explicit deferral note), and every plan acceptance criterion naming an AFTER/FINAL/per-race readout is recorded here as **deferred to Phase 79.1**, not satisfied by a readout-compare proof.
- **Files affected:** `docs/DIFFICULTY-RETUNE.md` (stub instead of the full section); no `tools/readouts/75.2-05-final.txt` was created.
- **Verification:** `test/unit/difficulty-retune-ledger.test.js` (13/13) confirms the stub's placement (immediately before, and never displacing, the standing `## v1.2 retune (Phase 27)` last-H2 guard) and every other structural invariant that test enforces.
- **Committed in:** `78b0c112`

---

**Total deviations:** 1 (Rule 4 category, but applying an explicit, already-corroborated orchestrator instruction — not a unilateral architectural decision)
**Impact on plan:** The phase's engine-gate/audit/exposure-guard work (Task 1) and every whole-phase gate (Task 2's `npm test`/parity/roll-high checks) landed exactly as specified. Only the readout-recording SHAPE changed, per instruction, from "full proof, now" to "stub now, full proof at Phase 79.1" — no engine dial was retuned, and every deferred acceptance criterion is explicitly named as deferred (never silently dropped).

## Issues Encountered

None beyond the readout-recording restructure noted above.

## User Setup Required

None — no external service configuration required.

## Human verification (deferred to end of run — rolled up from all five Phase 75.2 plans, per the deferred-UAT protocol)

**Pixel 7, at milestone close:**

1. **(75.2-01)** With a Troll hero, open a foe's details in a fight; its "it hits you on" line should list "size +1". With a Dwarven hero it should list "size −1". With an Elven hero it should list "Elven −1" and no size entry. The Oracle's foe-swing line should show the same tokens.
2. **(75.2-02)** Read the Gauntlet of the Giant and an Enlarge potion in the store or the Gear sheet; each should say "one size larger for fifty squares", "+2 damage", "one face easier for foes to hit", with no overhead-clearance promise.
3. **(75.2-03)** Roll (or start) a Troll, an Elf, a Dwarf and a Human. The Hero tab should show "Size Large · +2 damage, −1 vs their swings", "Size Small · −2 damage", "Size Small · +1 vs their swings" and "Size Human" respectively; using a Gauntlet of the Giant as the Human, the line should read "Large … · Human by birth" until it wears off, with the Damage line moving by the same 2.
4. **(75.2-04)** Use a Gauntlet of the Giant; the chip strip shows "Large" with its squares, a tap explains "+2 damage, and one face easier for foes to hit" and "−1 vs their swings", and the Oracle and rail lines say the same; drink an Enlarge too and both chips read "Huge". Read the Elven, Dwarven and Troll race notes on the roll screen or the dossier: each states its size and net effect (the Elf still thin-boned, the Dwarf still +2 damage). In a fight, a Dwarf's foe odds show "size +1" and an Elf's show no size entry.

## Next Phase Readiness

- Phase 75.2 (RULES-11, "Hero Size Matters") is engine-gate complete: the audit is written and guarded, the fixture story is closed with a teeth-tested exposure guard, and every whole-phase gate is green at the head (`npm test` 6501/6501; parity 60/60; prototype master hash unchanged; roll-high guard/direction/ledger-sync/state-pins/save-compat/sign-consistency all green).
- The phase's own bot-balance readout obligations (the FINAL readout, both plans' AFTER readouts, and every per-race verdict) are explicitly deferred to Phase 79.1 — that phase's own planning should read `docs/DIFFICULTY-RETUNE.md`'s `## v2.1 hero size (Phase 75.2)` stub section and this SUMMARY's Deviations note before running its own readouts.
- The four Pixel-7 human-check items above are batched into the milestone-close UAT round, not device-paused mid-phase, per the deferred-UAT protocol.
- No known stubs. No blockers for Phase 75.3 (Deep-Floor Encounter Scaling, RULES-16) or Phase 79.1.

---
*Phase: 75.2-hero-size-matters*
*Completed: 2026-09-26*

## Self-Check: PASSED

- FOUND: docs/ROLL-LEDGER.md
- FOUND: test/unit/hero-size-rules.test.js
- FOUND: test/parity/FIXTURE-INVENTORY.md
- FOUND: test/parity/divergence-records.test.js
- FOUND: docs/DIFFICULTY-RETUNE.md
- FOUND: tools/voice-sample.mjs
- FOUND: tools/voice-sample-output.txt
- FOUND commit: 58a16b25 (test: Task 1 — the size-rule audit in the roll ledger, its standing guard, and the fixture close)
- FOUND commit: 78b0c112 (docs: Task 2 — the DIFFICULTY-RETUNE stub, the voice sample, and the whole-phase gates)
