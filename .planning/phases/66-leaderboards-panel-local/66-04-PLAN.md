---
phase: 66-leaderboards-panel-local
plan: 04
type: execute
wave: 2
depends_on: ["66-01", "66-02"]
files_modified:
  - src/browser/boardsView.js
  - test/unit/boardsView.test.js
autonomous: true
requirements: [BOARD-02, BOARD-03, BOARD-04, BOARD-05, BOARD-06, BOARD-07, BOARD-08]
user_setup: []

must_haves:
  truths:
    - "boardsView({ bests, graves, total, board, scope, open, entry, hasHero, signedIn, recentHash }) is pure and deterministic: no DOM, no storage, no clock, no randomness, no input mutation, and two calls on the same input return deepStrictEqual views (D-15)"
    - "DEEPEST, LEANEST, LONGEST, BUTCHERY and PURSE list the bests record's own top ten in record order (LEANEST in squares-per-floor order after 66-01), each row showing rank, the adventurer's name as the headline, 'RACE SUB · LVL n' in Roman numerals, a value bar and value + unit (PURSE digit-grouped, LEANEST 'floor · sq') from canon fields steps/gold/sp/level (D-05, D-13)"
    - "LINEAGE groups by race + class from the stored aggregates: best floor then fewer steps, each row naming the best adventurer, the combination, and 'N INTERRED' / 'N INTERRED, NO SURVIVORS', with the mock's 'N rolled, N dead. Deepest was ...' detail"
    - "GRAVEYARD lists every stored stone (up to 60) deepest first then fewer steps, unranked (no rank numbers, no winner highlight), with the yard name/line/detail order: name, 'RACE SUB · LVL n', cause, and the epitaph when opened (D-10)"
    - "An open row carries its cause and epitaph as the detail and exactly six chips FLOOR / DAYS / SQUARES / KILLS / EXP / WILMST (BOARD-06)"
    - "Offline no row has a handle, a YOU or FRIEND tag, or a divider; cutTopTen still pins a player's best below 'NOT IN THE TOP TEN · YOUR BEST RUN' whenever an input list has a 'you' entry outside the first ten (built now, reachable with Phase 68's global rows) (D-13, BOARD-05)"
    - "The header shows LEADERBOARDS, the signed-out scope line ('Your dead only. The world has not been told.', or GRAVEYARD's 'Your own dead. Nobody else's business.'), and INTERRED = the lifetime total, never fewer than the stones held (D-08)"
    - "Signed out, the identity strip shows the nobody glyph, 'PLAY GAMES · SIGNED OUT' and 'Your dead only', with ALL/FRIENDS dimmed; selecting either scope replaces the rows with that scope's in-panel note; GRAVEYARD has no strip and ignores scope (D-06)"
    - "The standing card places the most recent run (the newest stone, else bests.last) among your own dead on the active board: ordinal place, 'of N of yours.' plus one quip; LINEAGE reads 'of N combinations.'; GRAVEYARD reads INTERRED + lifetime total + 'rolled, delved, and buried. Deepest was X on floor F.' (D-07)"
    - "Ranked footnote and GRAVEYARD footnote come from content/boards.js verbatim (D-11)"
    - "Title entry adds the chevron and the dock: BACK TO THE DUNGEON alone with a live hero, else BACK TO TITLE + ROLL A NEW HERO; tab entry has neither (D-01, D-02)"
    - "Switching from a ranked board to the adjacent GRAVEYARD swaps the scope line and removes the strip, and switching back restores both, with nothing carried over between the two"
    - "With no data at all, the header reads LEADERBOARDS, the signed-out scope line and INTERRED 0, every board shows 'Nobody of yours has qualified for this board yet.', and the standing card reads NO ENTRY; a single run fills every ranked board with one row at a full bar (D-12)"
    - "The rail always lists the seven boards in BOARD_IDS order with each board's tab and colour, whatever board is active or empty"
    - "Runs tied on every ordering key keep the record's stored order; the scope chips are always ALL then FRIENDS"
    - "Two runs with the same adventurer name get distinct row keys (hash-based; GRAVEYARD keys add the stone index) and the same avatar colour and initials"
    - "The most recent run's place is 1 + the number of your runs strictly better on that board, so a run tied with others takes the best shared place"
    - "The standing quip is picked deterministically from the run's hash and the board, so the same data always shows the same line"
    - "The view model references no network-capable API; with a null bests record and no graves it still returns a complete, renderable view"
    - statement: "Avatar initials and colour operate on UTF-16 code units exactly like the mock's INITIALS/AVATAR helpers; generated adventurer names are ASCII, so non-ASCII or astral-plane names are only guaranteed not to throw"
      verification: backstop
  prohibitions:
    - statement: "Must not present local rows as a global or friends ranking: no handle, no YOU/FRIEND tag, and no 'worldwide' or 'among friends' wording while signed out"
      status: unresolved
      verification: judgment
    - statement: "Must not make any network call or reference any network-capable API from the view model"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/boardsView.js"
      provides: "boardsView, cutTopTen, runPool, avatarColour, initialsOf, ordinal, AVATAR_PALETTE"
      contains: "export function boardsView"
    - path: "test/unit/boardsView.test.js"
      provides: "every board's ordering, LEANEST, GRAVEYARD, empty states, standing card, signed-out strip, dock, purity"
      min_lines: 250
  key_links:
    - from: "src/browser/boardsView.js"
      to: "engine/records.js (compareRuns, leanRate, normalizeStone, sortGraveyard, sanitizeBests, lineageKey, BOARD_IDS)"
      via: "direct import; the view model never redefines an ordering"
      pattern: "from \"../../engine/records.js\""
    - from: "src/browser/boardsView.js"
      to: "content/boards.js (BOARD_COPY, BOARD_FOOTNOTES, BOARDS_PANEL_COPY, STANDING_LINES)"
      via: "direct import; the view model holds no copy of its own"
      pattern: "from \"../../content/boards.js\""
---

<objective>
Build the Leaderboards panel's pure data seam, src/browser/boardsView.js: one function that turns the bests record, the stored graveyard and the lifetime total into the exact view object 66-03's renderer draws. Implements D-15 (the pure view model the renderer is fed through), D-05 (the adventurer as the offline identity), D-06 (signed-out strip and notes), D-07 (standing card on the most recent run, with the quip bank), D-08 (INTERRED is the lifetime total), D-10 (GRAVEYARD rows), D-11 (marks, colours, rules, footnotes), D-12 (empty states), D-13 (rows and the built-in divider) and the view half of D-01/D-02, with D-16's unit tests.

Purpose: every board is correct and honest from local data alone, provable without a DOM.
Output: src/browser/boardsView.js and test/unit/boardsView.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/66-leaderboards-panel-local/66-01-SUMMARY.md
@.planning/phases/66-leaderboards-panel-local/66-02-SUMMARY.md

<interfaces>
Output: the view contract pinned in 66-03-PLAN.md's interfaces (header, strip, rail, board, body, row, standing, footnote, dock). Match it field for field.

Inputs:
- bests: a BestsRecord { v, runs: { hash: RunSummary }, boards: { deep, lean, days, kills, purse: hash[] }, lineage: { "race cls": { count, best } }, last } or null
- graves: stored stones, newest first (RunSummary shape; pre-Phase-65 stones lack season/seed/acts/hash)
- total: the adapter's lifetime count; board: a BOARD_IDS id; scope: "local" | "all" | "friends"; open: a row key or null; entry: "tab" | "title"; hasHero: boolean; signedIn: boolean (always false this phase); recentHash: optional override of the most recent run's hash
- RunSummary fields used: name, race, sub, cls, level, sp, floor, day, steps, gold, kills, note (death note text, lower-case first letter), epitaph, hash
- From 66-01: compareRuns (LEANEST by squares per floor), leanRate, normalizeStone, sortGraveyard, sanitizeBests, lineageKey, BOARD_IDS
- From 66-02: BOARD_COPY[id].{tab,title,rule,mark,col,unitLabel}, BOARD_FOOTNOTES, BOARDS_PANEL_COPY (its `{token}` templates), STANDING_LINES { first, ten, rest }
- content/index.js exports ROMAN (I..V); a level past V renders as its number (`ROMAN[level - 1] || level`, the heroTab.js precedent)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the ported helpers and the row builders: avatar, initials, ordinal, pool, top-ten cut, per-board entries</name>
  <files>src/browser/boardsView.js, test/unit/boardsView.test.js</files>
  <read_first>
    - design/Mazeworld Boards Panel.dc.html (AV/AVATAR/INITIALS/ORD at ~204-216, cut() at ~250-259, entries() at ~261-279, the rows mapping at ~335-370)
    - engine/records.js (as changed by 66-01: compareRuns, leanRate, normalizeStone, sortGraveyard, sanitizeBests, lineageKey)
    - content/boards.js (as extended by 66-02)
    - src/browser/newBest.js (the pure view-model module precedent and its hash-keyed pick)
  </read_first>
  <behavior>
    - avatarColour: "Hilda Ferrow" gives "#5c4a6b", "Pip Alderwey" "#4a5c6b", "Odo" "#4a6b52", "Marta Quen" "#5c5c4a" (vectors computed from the mock's AVATAR formula); a non-string key never throws
    - initialsOf: "Hilda Ferrow" gives "HF", "Odo" gives "OD", "" and null give "?"
    - ordinal: 1ST 2ND 3RD 4TH 11TH 12TH 13TH 21ST 22ND 23RD 24TH 101ST 111TH 112TH
    - runPool(bests, graves): the union of bests.runs and every normalized stone, deduped by hash (a legacy stone and its backfilled entry count once), graves in their stored order first
    - cutTopTen(entries): 9 entries come back unchanged; 12 entries with a "you" entry at index 4 give the first ten; 12 entries with the only "you" entry at index 11 give the first ten plus that entry marked divider; the input array and its entries are never mutated
    - Ranked board rows (deep/lean/days/kills/purse): one per hash in bests.boards[board], in order, keyed by hash; headline = name; name slot ""; line "HUMAN SOLDIER · LVL II"-style (race and sub upper-cased, Roman level; level 7 renders "LVL 7"); detail = the death note with its first letter capitalised, then ". ", then the epitaph; val: DEEPEST "9", LEANEST "9 · 312", LONGEST day, BUTCHERY kills, PURSE gold as "4,688" (en-US grouping); unit from unitLabel
    - Stats: exactly FLOOR, DAYS, SQUARES, KILLS, EXP, WILMST with floor, day, steps, kills, sp and en-US-grouped gold as strings; missing numbers render "0"
    - LEANEST order in the rows follows squares per floor (a 22-step floor-1 run sits above a 900-step floor-9 run)
    - LINEAGE rows: one per lineage entry whose best run exists, ordered by compareRuns("combo") on the best runs, then by key ascending; key "combo:" + the lineage key; headline = the best run's name; name slot = the key upper-cased; line "1 INTERRED" or "3 INTERRED, NO SURVIVORS"; detail = "3 rolled, 3 dead. Deepest was NAME, floor F in S squares. EPITAPH"; val = the best floor; at most ten rows
    - GRAVEYARD rows: all normalized stones (up to 60) in sortGraveyard order, keys hash + ":" + index, rank "", top false, podium false, headline = name, name slot = the level line, line = the capitalised death note, detail = the epitaph, val "floor · steps"
    - Value bars: min-max over the listed rows' metric (floor for deep/combo/yard, day, kills, gold; for LEANEST the negated leanRate so the best rate is fullest, an unplaced run gets the minimum 3); pct = max(3, round((v - min) / (max - min) * 100)); when every metric is equal (including a single row) every bar is 100
  </behavior>
  <action>
Create src/browser/boardsView.js (implements D-05, D-10, D-13 row content). Header comment in the newBest.js style: Phase 66 (BOARD-02..08, D-15), the Leaderboards panel's pure view model; no DOM, storage, clock or randomness; every ordering comes from engine/records.js and every word from content/boards.js; no network. Imports: engine/records.js (compareRuns, leanRate, normalizeStone, sortGraveyard, sanitizeBests, lineageKey, BOARD_IDS), content/boards.js (BOARD_COPY, BOARD_FOOTNOTES, BOARDS_PANEL_COPY, STANDING_LINES) and ROMAN from content/index.js.

1. Port the mock helpers (D-05: port, don't reinvent): `export const AVATAR_PALETTE` (the six mock colours in mock order); `export function avatarColour(key)` (the mock's hash: start at 7, for each UTF-16 code unit a = (a * 31 + code) >>> 0, then palette[hash % 6], over String(key ?? "")); `export function initialsOf(key)` (the mock's INITIALS: strip "@", turn "." and "_" into spaces, split on spaces, drop empties; first letter of word one plus first letter of word two, or the second letter of word one when there is no second word; upper-cased; "?" when there are no words, where the mock would throw); `export function ordinal(n)` (the mock's ORD, verbatim logic).

2. Module-private helpers: num(x) (finite number else 0); fill(template, vars) replacing each {token} with String(vars[token]) or "" when absent; levelLine(run) building RACE SUB · LVL n from BOARDS_PANEL_COPY.level and .sep; causeLine(run) (the note with its first letter capitalised, "" when missing); statChips(run) (the six chips in D-13's order with en-US grouping for WILMST); valueText(board, run).

3. `export function runPool(bests, graves)`: sanitized bests runs plus normalizeStone(stone) for each stored stone, deduped by hash, stored stones first in their stored order.

4. `export function cutTopTen(entries)` (BOARD-05, the mock's cut() generalised): return a new array; when there are ten or fewer entries return copies of them all; otherwise the first ten, and when none of those has you === true but a later entry does, append a copy of the first such entry with divider set to BOARDS_PANEL_COPY.divider. Never mutate the input. Rows built offline never set `you`, so the divider is unreachable until Phase 68 supplies global rows (D-13), but the path is live and tested here.

5. Module-private per-board entry builders producing, for each listed run, { key, run, rank, top, podium, you: false, divider: "", headline, tag: "", name, line, detail, val, unit, stats, metric } for ranked boards, LINEAGE and GRAVEYARD exactly as the behaviour list states, plus a private barPercents(entries) implementing the bar rule. Ranked and LINEAGE rows: rank String(i + 1), top i === 0, podium i < 3. GRAVEYARD rows: rank "", top false, podium false (D-10: not ranked). Avatar = { initials: initialsOf(headline name), bg: avatarColour(headline name) } using the adventurer's name (for LINEAGE, the best run's name).

6. Tests (test/unit/boardsView.test.js, Task 1 section): every behaviour bullet, with a local makeSummary(overrides) fixture that builds a hashed RunSummary via engine/records.js#runHash and a record built with emptyBests/updateBests.
  </action>
  <verify>
    <automated>node --test test/unit/boardsView.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function cutTopTen" src/browser/boardsView.js` equals 1; `grep -c "export function runPool" src/browser/boardsView.js` equals 1; `grep -c "export function avatarColour" src/browser/boardsView.js` equals 1
    - `node --test test/unit/boardsView.test.js` passes with the pinned avatar/initials/ordinal vectors, the cutTopTen divider cases and every board's row content
  </acceptance_criteria>
  <done>Each board's rows are built from canon fields in the right order, the mock's helpers are ported with pinned vectors, and the top-ten cut with its divider is live and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: boardsView: header, strip and scope, rail, body, standing card, footnote and dock</name>
  <files>src/browser/boardsView.js, test/unit/boardsView.test.js</files>
  <read_first>
    - src/browser/boardsView.js (Task 1)
    - .planning/phases/66-leaderboards-panel-local/66-03-PLAN.md (the view contract in its interfaces block)
    - design/Mazeworld Boards Panel.dc.html (renderVals ~281-384: scopeLine, me/strip, scopes, standing, footnote)
    - design/Mazeworld Leaderboards.dc.html (the entry chrome: fromTitle, hasHero, the bottom bar)
    - .planning/phases/66-leaderboards-panel-local/66-CONTEXT.md (D-01, D-02, D-06, D-07, D-08, D-11, D-12)
  </read_first>
  <behavior>
    - Input normalization: an unknown board gives "deep"; an unknown scope gives "local"; any entry other than "title" gives "tab"; a null bests is treated as empty; non-array graves as []
    - header: title "LEADERBOARDS"; scopeLine = BOARDS_PANEL_COPY.scope.ranked on every ranked board and .yard on GRAVEYARD; interred = total when it is a non-negative integer, raised to the stone count when lower, else the stone count; back = entry is "title"; backLabel "Back"
    - strip: null on GRAVEYARD; elsewhere glyph "?", label "PLAY GAMES · SIGNED OUT", source "Your dead only", scopes [ALL, FRIENDS] with on = (scope === id) and dim true
    - body: on a ranked board with scope "all" or "friends", { kind: "note", line: that scope's note } even when there is no data; GRAVEYARD ignores scope; otherwise { kind: "empty", line } when there are no rows, else { kind: "rows", rows } with row.open = (row.key === open)
    - rail: seven entries in BOARD_IDS order with tab, col and on
    - board: { id, mark, col, title, rule } from BOARD_COPY
    - standing, ranked board: recent = the run whose hash is recentHash, else the newest normalized stone, else bests.last; place = ordinal(1 + the number of pool runs strictly better by compareRuns(board)); label = the recent run's name upper-cased + " · " + unitLabel; note = "of N of yours. " + quip, N = the pool size. Example: the most recent of 37 runs ranking 4th reads place "4TH", note beginning "of 37 of yours."
    - standing, a tie: three runs tied on every key with the recent one among them all place "1ST"
    - standing, LINEAGE: the recent run's combo rank among all combos (uncut) with label COMBO + " · FLOOR" and note "of N combinations. " + quip
    - standing, GRAVEYARD: label "INTERRED", place String(the header's interred), note "rolled, delved, and buried. Deepest was NAME on floor F." using the bests DEEPEST #1 run, else the first GRAVEYARD row
    - standing, empty (no runs at all) on every board including GRAVEYARD: label "NO ENTRY", place "—", note BOARDS_PANEL_COPY.standing.noNote
    - quip: bank first when place 1, ten when 2..10, rest beyond; index (parseInt(hash, 16) + BOARD_IDS.indexOf(board)) mod bank length, 0 + board index for an invalid hash; the same input always gives the same quip
    - footnote: BOARD_FOOTNOTES.yard on GRAVEYARD, else .ranked
    - dock: null for tab entry; title entry with hasHero true gives [{ id "dungeon", label "BACK TO THE DUNGEON", primary true }]; without a hero [{ id "title", "BACK TO TITLE", primary false }, { id "roll", "ROLL A NEW HERO", primary true }]
    - A full run of all seven boards on a 12-run fixture: DEEPEST and LEANEST orders differ; GRAVEYARD holds all 12 unranked; LINEAGE groups by race + class
    - Offline no row has a non-empty tag, you true or a divider, and no string in the view contains "worldwide", "among friends" or "@"
    - Purity: boardsView on deep-frozen inputs never throws or mutates; two calls give deepStrictEqual views; the comment-stripped module source contains no document., window., localStorage, Math.random, Date. or network identifier (fetch, XMLHttpRequest, WebSocket, EventSource, sendBeacon)
    - Empty data: boardsView({}) returns a complete view with INTERRED 0, the empty body on every board and NO ENTRY
  </behavior>
  <action>
In src/browser/boardsView.js add `export function boardsView(input = {})` (implements D-15, D-06, D-07, D-08, D-11, D-12, D-01/D-02 view chrome):

1. Normalize the input per the behaviour list: `const bests = sanitizeBests(input.bests)` (the record is already sanitized by the adapter; this keeps the view model safe on any input); graves = normalized stones (normalizeStone, nulls dropped, stored order); pool = runPool(bests, graves); total and interred per D-08; board, scope and entry defaults.

2. Build and return the view object exactly per the 66-03 contract: header, strip (null on GRAVEYARD; dim = signedIn !== true, so ALL/FRIENDS are dimmed while signed out, per D-06), rail, board, body (note / empty / rows via Task 1's builders, with cutTopTen applied to the ranked and LINEAGE row lists and GRAVEYARD never cut), standing (per D-07, with the recent run found by hash in the pool), footnote, dock (per D-01/D-02).

3. Keep every string sourced from content/boards.js; this module holds no player-facing literal except structural separators taken from BOARDS_PANEL_COPY.sep. Signed out is the only identity branch built this phase (Phase 67 adds a signed-in identity input at this same seam; Phase 68 adds global and friends sources).

4. Tests (test/unit/boardsView.test.js, Task 2 section): every behaviour bullet, plus deep-freeze and double-call idempotency checks and the comment-stripped source pins (the comment-stripping approach of test/unit/gear-tab-dom.test.js).
  </action>
  <verify>
    <automated>node --test test/unit/boardsView.test.js test/voice/safety-scan.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function boardsView" src/browser/boardsView.js` equals 1
    - `node --test test/unit/boardsView.test.js` passes with at least 30 tests in total across both tasks, including every board's ordering, the LEANEST-vs-DEEPEST difference, GRAVEYARD's 60-stone unranked list, the empty view, the standing tie case, the signed-out note swap and the purity pins
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (the failing-test set must not grow)
  </acceptance_criteria>
  <done>One pure function produces every word, number and flag the panel shows, for all seven boards, both entries and every empty or signed-out state.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures)
- No Android build, APK or device step
</verification>

<success_criteria>
- D-15: the panel's whole content is a tested pure function of local data
- BOARD-02..BOARD-08 content is correct and honest offline
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| src/browser/boardsView.js | new module | pure view model |
| `boardsView(input)` | export | the D-15 seam |
| `cutTopTen(entries)` | export | BOARD-05's pin, generalised from the mock |
| `runPool(bests, graves)` | export | hash-deduped union for standing ranks |
| `AVATAR_PALETTE`, `avatarColour`, `initialsOf`, `ordinal` | exports | ported mock helpers |
| test/unit/boardsView.test.js | new test file | |

## Flagged assumptions (unresolved edge rows)

- BOARD-05 (edge probe: unclassified): offline every row is the player's own, so the "NOT IN THE TOP TEN" pin cannot trigger; it is built and tested through cutTopTen with synthetic entries and becomes reachable with Phase 68's global rows (D-13).
- The standing card's "of N of yours" counts the runs the device still remembers (the 60-stone graveyard plus every run the bests record keeps). It equals INTERRED until the 61st death; past that, a most-recent run outside its board's top ten is ranked among the remembered runs only.

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-04-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: after a few deaths, each board's order matches expectation (LEANEST rewards short efficient runs); the standing card's place and "of N" read sensibly after a fresh death. Do NOT create `.planning/phases/66-*/deferred-items.md`.
</output>
