---
phase: 66-leaderboards-panel-local
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/records.js
  - test/unit/records.test.js
  - test/unit/bests-adapter.test.js
  - src/browser/engineAdapter.js
  - test/unit/graveyard-adapter.test.js
autonomous: true
requirements: [BOARD-02, BOARD-03, BOARD-08]
user_setup: []

must_haves:
  truths:
    - "LEANEST ranks by squares walked per floor reached (steps / floor), lowest first; an equal rate goes to the deeper floor, then to fewer steps; a full tie compares as 0 (D-09)"
    - "Two runs with the same squares-per-floor rate (for example 40 steps to floor 2 and 80 steps to floor 4) are never merged or reordered by rounding: the rate is compared by cross-multiplication, and the deeper run ranks first"
    - "A run with no valid floor (missing, or below 1) ranks after every run with a floor on LEANEST, and the comparator never divides by zero or returns NaN"
    - "sanitizeBests re-ranks every stored board list with a stable sort by that board's comparator before the top-ten cut: an older ddr.bests.v1 whose LEANEST list was stored in the old depth order loads re-ranked, a record already in order loads byte-identical (idempotent), and full ties keep their stored order"
    - "DEEPEST, LINEAGE, LONGEST, BUTCHERY, PURSE and GRAVEYARD orderings are unchanged; no GameState field is added and the parity suite passes with zero fixture movement (engine gate)"
    - "normalizeStone gives a legacy stone exactly the hash its backfilled bests entry carries, so the panel can dedupe the graveyard against the bests record"
    - "getGraveyard().total is the lifetime count: the stored total when it is a non-negative integer, the stored stone count when the key is missing, and never less than the number of stones held (0, 1, 60 and 61-plus deaths all read correctly) (D-08)"
    - "A stored total that is non-numeric, negative, fractional or non-finite is ignored in favour of the stone count, so INTERRED is always a non-negative integer"
    - "A non-dev death folds into the in-memory graveyard (newest first, capped at 60, total + 1) synchronously inside dispatch(), before the durable write settles, so a panel rendered right after the death shows it without awaiting storage; a dev start-at-depth death never enters it"
    - "loadGraveyard() never rejects and reads only through the storage.js abstraction (no network, no raw localStorage); boot() loads it before resolving; initRun()/startNewRun() never clear it"
  prohibitions:
    - statement: "Must not change any serialized GameState field, any engine rng draw, or any parity fixture to deliver the LEANEST re-rank; it is a comparator change in engine/records.js only"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "engine/records.js"
      provides: "the LEANEST squares-per-floor comparator, leanRate, normalizeStone, and the re-ranking sanitizeBests"
      contains: "export function normalizeStone"
    - path: "src/browser/engineAdapter.js"
      provides: "loadGraveyard()/getGraveyard(): the in-memory 60-stone graveyard and lifetime total, loaded at boot and folded synchronously at every non-dev death"
      contains: "export function getGraveyard"
    - path: "test/unit/graveyard-adapter.test.js"
      provides: "the graveyard read seam's unit tests"
      min_lines: 90
    - path: "test/unit/records.test.js"
      provides: "LEANEST re-rank, sanitize re-rank and normalizeStone tests"
  key_links:
    - from: "src/browser/engineAdapter.js#loadBests"
      to: "engine/records.js#sanitizeBests"
      via: "every load of ddr.bests.v1 passes through sanitizeBests, which now re-ranks each board list"
      pattern: "sanitizeBests\\(JSON\\.parse"
    - from: "src/browser/engineAdapter.js#recordDeath"
      to: "the module-level graveyard snapshot"
      via: "the same summary the bests fold uses is unshifted into the in-memory graveyard"
      pattern: "graveyard = \\{"
    - from: "engine/records.js#backfillBests"
      to: "engine/records.js#normalizeStone"
      via: "backfill normalizes each legacy stone through the one exported helper"
      pattern: "normalizeStone\\("
---

<objective>
Give the Leaderboards panel its two data-layer foundations. Implements D-09 (LEANEST re-ranked by squares per floor, with re-ranking of a stored list on load) and the adapter half of D-08, D-14 and D-15 (the lifetime INTERRED total and a synchronous, in-memory graveyard read seam that the panel and the title gate read instead of storage).

Purpose: every board and the header read canon, local data through one seam, and LEANEST stops duplicating DEEPEST.
Output: engine/records.js (comparator, leanRate, normalizeStone, re-ranking sanitize), src/browser/engineAdapter.js (loadGraveyard/getGraveyard + synchronous fold), and their tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/65-run-record-personal-bests/65-04-SUMMARY.md

<interfaces>
- engine/records.js (Phase 65) today: `compareRuns(board, a, b)` groups deep/lean/combo/yard as floor desc then steps asc; `boardValue(board, run)`; `sanitizeBests(raw)` filters and dedupes each RANKED_BOARDS list then slices to BOARD_TOP_N in stored order; `backfillBests(graves)` normalizes each legacy stone inline (season 0, no seed/acts, fresh runHash) before folding oldest-first; `sortGraveyard(graves)`. The module has no imports and must keep none.
- src/browser/engineAdapter.js (Phase 65-04): `GRAVE_KEY` "ddr.graveyard.v1", `GRAVE_TOTAL_KEY` "ddr.graveyard.total.v1", `GRAVE_CAP = 60`, module state `bests`/`deathRecord`, `loadBests()`, `getBests()`, `recordDeath(state, cause, when)` (synchronous, never throws, called only for non-dev deaths inside `dispatch()`), `persistGrave(...)` (async, tracked), `boot(freshSeed)`, `initRun`, `startNewRun`.
- Consumers built later in this phase: src/browser/boardsView.js (66-04) imports `compareRuns`, `leanRate`, `normalizeStone`, `sortGraveyard`, `sanitizeBests`, `lineageKey`, `BOARD_IDS`; mazeworld.html (66-06/66-07) imports `getBests`, `getGraveyard`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LEANEST by squares per floor, leanRate, normalizeStone, and a re-ranking sanitizeBests</name>
  <files>engine/records.js, test/unit/records.test.js, test/unit/bests-adapter.test.js</files>
  <read_first>
    - engine/records.js (the whole file: compareRuns, boardValue, sanitizeBests, updateBests, backfillBests, sortGraveyard, the no-imports header)
    - test/unit/records.test.js (the compareRuns deep/lean test near line 168, the updateBests tests from ~307, the sanitizeBests tests from ~452, the backfillBests tests from ~519)
    - test/unit/bests-adapter.test.js (the "a deeper run announces new bests on deep and lean" test near line 220)
    - .planning/phases/66-leaderboards-panel-local/66-CONTEXT.md (D-09)
  </read_first>
  <behavior>
    - compareRuns("lean", {floor:4,steps:40}, {floor:2,steps:30}) is negative (10 per floor beats 15 per floor)
    - compareRuns("lean", {floor:9,steps:900}, {floor:1,steps:22}) is positive (100 per floor loses to 22)
    - Equal rate, deeper wins: compareRuns("lean", {floor:4,steps:80}, {floor:2,steps:40}) is negative; compareRuns("lean", {floor:3,steps:1}, {floor:6,steps:2}) is positive (the 1/3 vs 2/6 rounding trap)
    - A placed run beats an unplaced one: compareRuns("lean", {floor:1,steps:500}, {floor:0,steps:0}) is negative; two unplaced runs fall through to fewer steps: compareRuns("lean", {floor:0,steps:5}, {floor:0,steps:9}) is negative
    - compareRuns("lean", {}, {}) === 0, a full tie returns 0, and 200 generated pairs (including missing/NaN/Infinity fields) never return NaN
    - compareRuns("deep", ...) and every other board return exactly what they returned before (the existing deep/days/kills/purse/combo/yard tests pass unedited)
    - leanRate({floor:4,steps:40}) === 10; leanRate({floor:0,steps:5}) === Infinity; leanRate(null) === Infinity
    - sanitizeBests re-ranks: a hand-built record whose boards.lean holds three runs in floor-desc order comes back ordered by rate; sanitizeBests(sanitizeBests(r)) deepStrictEquals sanitizeBests(r); for a record produced by updateBests the output deepStrictEquals the input; two runs tied on every key keep their stored order
    - updateBests after the change: the lean list of a mixed sequence is ordered by rate; newBests contains "lean" only when the new run's rate is strictly better (or equal rate and deeper)
    - normalizeStone(legacyStone).hash equals the key backfillBests([legacyStone]).runs holds for it; normalizeStone(null) === null; normalizeStone({floor:"x"}) === null; a stone with a numeric season and a valid hash keeps both; normalizeStone never mutates its input
  </behavior>
  <action>
In engine/records.js (per D-09, keeping the module import-free and pure):

1. compareRuns: take "lean" out of the deep/combo/yard group and give it its own branch. Read floor and steps of both runs through the existing num()/field() helpers. A floor below 1 counts as unplaced. If exactly one run is unplaced, the placed run ranks first. If both are placed, compare the cross-multiplied rates (a.steps times b.floor against b.steps times a.floor; the lower rate ranks first) so no division or floating-point rounding is involved. Rate ties (and the both-unplaced case) fall through to the deeper floor first, then the fewer steps. Update the compareRuns doc comment: "lean: squares per floor asc (cross-multiplied), then floor desc, then steps asc; an unplaced run (floor below 1) ranks last".

2. Add `export function leanRate(run)`: steps divided by floor when the floor is at least 1, else Number.POSITIVE_INFINITY. Doc comment: the LEANEST metric in one place, read by the panel's value bar (Phase 66) and by Phase 68's submission; boardValue("lean") stays the floor because the row still displays "floor · sq".

3. Add `export function normalizeStone(stone)`: returns null unless the stone is a plain object with a finite numeric floor. Otherwise returns a shallow copy; when the copy has no numeric season, set season 0, delete seed and acts, and set hash = runHash(copy); when it has a numeric season but no valid hash, set hash = runHash(copy). Move backfillBests' inline normalization into a call to normalizeStone (skip stones it returns null for) so backfill behaviour is byte-identical.

4. sanitizeBests: after each RANKED_BOARDS list is filtered and deduped (the existing loop), stable-sort it with compareRuns(board, runs[x], runs[y]) and only then slice to BOARD_TOP_N. Doc comment: "Phase 66 (D-09): re-ranks every stored list by its board's current comparator, so an older record whose LEANEST list was stored in the retired depth order loads re-ranked; a list already in order is unchanged (Array.prototype.sort is stable), so this is idempotent and needs no record version bump." Leave `v: 1`.

5. Tests (test/unit/records.test.js): split the existing "compareRuns deep/lean" test into a deep-only test (its deep assertions unchanged) and a new LEANEST test block covering every behaviour bullet; add leanRate, normalizeStone and sanitize re-rank tests. Any other existing test (here or in test/unit/bests-adapter.test.js) that encoded "lean orders exactly like deep" is re-pinned to the new rule, and each re-pin is listed with its before/after in the SUMMARY. Tests that do not depend on the LEANEST order stay unedited.

Engine gate: this is a comparator change only. It touches no GameState field, no rng and no fixture. Prove it by running the parity suite and confirming no parity file moved; record "zero fixtures moved" in the SUMMARY (no FIXTURE-INVENTORY.md entry is needed because nothing moved).
  </action>
  <verify>
    <automated>node --test test/unit/records.test.js test/unit/bests-adapter.test.js test/unit/newBest.test.js test/unit/run-summary.test.js test/unit/death.test.js && node --test test/parity/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function normalizeStone" engine/records.js` equals 1 and `grep -c "export function leanRate" engine/records.js` equals 1
    - `node -e "import('./engine/records.js').then(m=>{if(!(m.compareRuns('lean',{floor:9,steps:900},{floor:1,steps:22})>0))process.exit(1)})"` exits 0 (LEANEST no longer orders like DEEPEST)
    - `node --test test/unit/records.test.js` passes and includes the LEANEST cases from the behaviour list (the 1/3 vs 2/6 rounding case, the unplaced cases and the NaN sweep)
    - `node --test test/parity/` passes, and `git diff --stat -- test/parity/fixtures/ test/parity/prototype-master.js.txt test/parity/FIXTURE-INVENTORY.md` prints nothing
    - engine/records.js still starts no line with an import statement (the existing engine-purity test stays green: `node --test test/unit/engine-purity.test.js` passes)
  </acceptance_criteria>
  <done>LEANEST ranks by squares per floor with deterministic tie-breaks, a stored old-order lean list re-ranks on load, legacy stones normalize to the same hash as their backfilled entries, and nothing in the engine gate moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the adapter's graveyard read seam: loadGraveyard, getGraveyard and the synchronous death fold</name>
  <files>src/browser/engineAdapter.js, test/unit/graveyard-adapter.test.js</files>
  <read_first>
    - src/browser/engineAdapter.js (GRAVE_KEY/GRAVE_TOTAL_KEY/GRAVE_CAP/BESTS_KEY constants, the `bests`/`deathRecord` module state and its comment, loadBests, getBests, recordDeath, persistGrave, boot, initRun, the died-event branch of dispatch)
    - test/unit/bests-adapter.test.js (the withFakeLocalStorage and legacyStone helpers, the dev-exclusion test, the synchronous-fold test) as the harness pattern to mirror
    - mazeworld.html classic graveyard block (search "async function loadGraves") for the total fallback rule this seam inherits (missing key falls back to the stone count; never fewer than the stones)
  </read_first>
  <behavior>
    - Both keys missing: loadGraveyard() resolves { graves: [], total: 0 }, and getGraveyard() returns the same object
    - Three stones and no total key: total 3. Stored total "200" with 60 stones: total 200. Stored total "2" with 5 stones: total 5
    - Stored totals "abc", "-4", "2.5" and "Infinity" each fall back to the stone count
    - Corrupt graves JSON or a non-array value gives []; non-object entries are dropped; 70 stored stones load as the newest 60
    - A storage read that throws resolves { graves: [], total: 0 } and never rejects
    - getGraveyard() is null before any load; after boot() it is non-null
    - After startNewRun() and dispatch({ type: "abandon" }) (not awaited), getGraveyard().graves[0].hash equals that death's summary hash and total rose by exactly 1, before any await
    - A graveyard already holding 60 stones stays at 60 after another death while total keeps counting
    - A dev start-at-depth run's death leaves getGraveyard() unchanged (mirror the existing bests dev-exclusion test)
    - initRun()/startNewRun() never reset getGraveyard()
    - After waitForPending() and a storage flush, the stored ddr.graveyard.v1 newest stone deepStrictEquals getGraveyard().graves[0] and the stored total equals getGraveyard().total
  </behavior>
  <action>
In src/browser/engineAdapter.js (implements the adapter side of D-08, D-14 and D-15):

1. Add module state `let graveyard = null;` beside `bests`/`deathRecord`, with a comment: Phase 66 (BOARD-02, D-08/D-15), the in-memory { graves, total } the Leaderboards panel and the title gate read; adapter-owned cross-run data, never GameState; null until loadGraveyard() runs; replaced (never mutated) on every change so an earlier reference stays a stable snapshot.

2. Add `export async function loadGraveyard()`: read GRAVE_KEY and GRAVE_TOTAL_KEY together through storage.getItem. Parse the graves JSON inside its own try (corrupt or non-array gives []), keep only plain non-array objects, and slice to GRAVE_CAP. The total is the parsed stored value only when the raw value is not null and Number(raw) is a non-negative integer; otherwise it is the stone count. Then take the larger of total and the stone count. Assign graveyard = { graves, total } and return it. Wrap everything in an outer try/catch that assigns and returns { graves: [], total: 0 }, so it never rejects. Doc comment in the loadBests style.

3. Add `export function getGraveyard()` returning the current snapshot (null before the first load). Doc comment: callers treat it as read-only.

4. boot(): add `await loadGraveyard();` directly after the existing `await loadBests();`, with a comment that the panel and the title's VIEW THE DEAD gate read it synchronously from then on.

5. recordDeath(): directly after the summary is built (and before the bests branch, so it runs whether or not bests is loaded), when graveyard is not null set graveyard to a new object whose graves are the summary followed by the previous graves, sliced to GRAVE_CAP, and whose total is the previous total plus 1. It stays inside recordDeath's existing try, so it can never throw into dispatch(). recordDeath is only reached for non-dev deaths, so dev runs stay excluded.

6. persistGrave(): after the writes are enqueued, when graveyard is still null (a death that raced ahead of boot), set it from the graves array and total this function just computed, so the in-memory view never lags storage.

7. initRun()/startNewRun() do not touch graveyard (cross-run data).

8. Create test/unit/graveyard-adapter.test.js covering every behaviour bullet. Reuse bests-adapter.test.js's withFakeLocalStorage/legacyStone helper shapes (copy them; do not import from another test file), and call loadGraveyard() at the start of each test to reset module state.
  </action>
  <verify>
    <automated>node --test test/unit/graveyard-adapter.test.js test/unit/bests-adapter.test.js test/unit/engineAdapter.test.js test/unit/new-run-loop.test.js test/persistence/ && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export async function loadGraveyard" src/browser/engineAdapter.js` equals 1 and `grep -c "export function getGraveyard" src/browser/engineAdapter.js` equals 1
    - `grep -c "await loadGraveyard();" src/browser/engineAdapter.js` equals 1 (inside boot)
    - `node --test test/unit/graveyard-adapter.test.js` passes with at least 11 tests, one per behaviour bullet
    - test/persistence/dual-write-convergence.test.js's "no raw localStorage in engineAdapter.js" guard still passes
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (compare the failing-test names before and after; the set must not grow)
  </acceptance_criteria>
  <done>The adapter serves a synchronous, lifetime-accurate graveyard snapshot loaded at boot and updated at every non-dev death, through storage.js only.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures), including test/parity/ with zero fixture movement
- No Android build, APK or device step (deferred-UAT protocol)
</verification>

<success_criteria>
- D-09: LEANEST is a distinct board (squares per floor), re-ranked on load for any stored record
- D-08/D-15: the lifetime total and the 60 stones are readable synchronously from the adapter, never from storage by the panel
- Engine gate held: no GameState, rng or fixture change
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `compareRuns` "lean" branch | engine/records.js | squares per floor, cross-multiplied; ties to the deeper floor, then fewer steps |
| `leanRate(run)` | engine/records.js export | the LEANEST metric; Infinity when unplaced |
| `normalizeStone(stone)` | engine/records.js export | legacy-stone normalization shared with backfillBests |
| re-ranking `sanitizeBests` | engine/records.js | stable sort per board before the top-ten cut |
| `loadGraveyard()` | src/browser/engineAdapter.js export | async, never rejects |
| `getGraveyard()` | src/browser/engineAdapter.js export | `{ graves, total }` or null |
| test/unit/graveyard-adapter.test.js | new test file | |

## Flagged assumptions

- "The most recent run" (D-07) is keyed by the newest graveyard stone; this plan guarantees that stone is present in memory the instant a death is dispatched.

## Phase source audit (all plans)

| SOURCE | ID | Feature / requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | DEAD tab becomes the mock's Leaderboards panel, fully local | 01-07 | COVERED |
| REQ | BOARD-01 | Opens from DEAD tab and title; back returns to origin | 03, 06, 07 | COVERED |
| REQ | BOARD-02 | Header, scope line, INTERRED, identity strip, ALL/FRIENDS | 01, 02, 03, 04, 05, 06 | COVERED |
| REQ | BOARD-03 | Seven-board rail, centred chip, marks/titles/rules | 01, 02, 03, 04, 05 | COVERED |
| REQ | BOARD-04 | Top-ten rows from canon fields | 03, 04, 05 | COVERED |
| REQ | BOARD-05 | NOT IN THE TOP TEN divider | 03, 04 | COVERED (built, unreachable offline per D-13) |
| REQ | BOARD-06 | Tap-expand with cause, epitaph, six chips | 03, 04, 05 | COVERED |
| REQ | BOARD-07 | Standing card and footnotes | 02, 03, 04 | COVERED |
| REQ | BOARD-08 | Local-only, deliberate signed-out state, zero network | 01, 02, 03, 04, 06, 07 | COVERED |
| CONTEXT | D-01 | Title-opened chevron + bottom bar, tab bar hidden | 03, 04, 05, 07 | COVERED |
| CONTEXT | D-02 | Tab-opened: tab bar visible, no chevron | 03, 04, 06 | COVERED |
| CONTEXT | D-03 | Android back mirrors the chevron | 03, 07 | COVERED |
| CONTEXT | D-04 | Title opens GRAVEYARD; tab opens last viewed, else DEEPEST | 03, 07 | COVERED |
| CONTEXT | D-05 | Adventurer is the offline identity | 04 | COVERED |
| CONTEXT | D-06 | Signed-out strip, dimmed chips, in-panel note | 02, 03, 04 | COVERED |
| CONTEXT | D-07 | Standing card on the most recent run + quip bank | 02, 04 | COVERED |
| CONTEXT | D-08 | INTERRED is the lifetime total | 01, 04 | COVERED |
| CONTEXT | D-09 | LEANEST re-ranked by squares per floor | 01, 02 | COVERED |
| CONTEXT | D-10 | GRAVEYARD rows, up to 60, unranked | 04 | COVERED |
| CONTEXT | D-11 | Marks, colours, rules, footnotes from the mock | 02, 04 | COVERED |
| CONTEXT | D-12 | Empty states | 02, 04 | COVERED |
| CONTEXT | D-13 | Rows, tap-expand, rail centring, reduced motion | 03, 04, 05 | COVERED |
| CONTEXT | D-14 | boardsPanel.js module, bridge, classic renderers deleted | 03, 06, 07 | COVERED |
| CONTEXT | D-15 | Pure view-model data seam | 01, 04 | COVERED |
| CONTEXT | D-16 | View-model unit tests + shell tests | 01-07 | COVERED |

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-01-SUMMARY.md` when done. List every re-pinned test with before/after, state "zero parity fixtures moved", and include a "Human verification (deferred to end of run)" heading: none new from this plan (data layer only). Do NOT create `.planning/phases/66-*/deferred-items.md`; out-of-scope findings go in the SUMMARY.
</output>
