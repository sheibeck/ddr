---
phase: 56-sound-effects-audio-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/build-www.mjs
  - test/unit/sfx-assets.test.js
autonomous: true
requirements: [AUD-06]
user_setup: []

must_haves:
  truths:
    - "A build that has the 30 delivered clips in sfx/ produces www/sfx/ containing those same 30 .mp3 files, byte-identical, with no re-encode."
    - "A build run with sfx/ absent fails loud: tools/build-www.mjs throws and the process exits non-zero, so a bundle with missing audio can never be produced."
    - "The 30 delivered clip filenames are pinned by a test, so a silently added, renamed or deleted clip fails npm test rather than shipping."
    - "All 30 clips ship and all 30 are mapped — the copy is whole-directory, with no per-file skip, filter or exception anywhere in it."
    - "EDGE empty (AUD-06): the asset pipeline copies a whole directory with no per-file allowlist — a build with zero extra files and zero missing files is the only passing state; nothing in the copy path throws on an empty event list because it never sees events at all."
    - "EDGE missing asset (AUD-06): build is where a missing clip fails loud (copySfx() throws when sfx/ is absent); runtime degradation to silence for one clip is owned by plan 56-03."
    - statement: "On a real Pixel 7, first launch after install with the phone in airplane mode plays every clip with nothing fetched over the network."
      verification: backstop
  artifacts:
    - "tools/build-www.mjs contains a copySfx() function and calls it from main()'s copy sequence."
    - "test/unit/sfx-assets.test.js exists and pins the 30-filename manifest."
    - "www/sfx/ (build output, gitignored) contains 30 .mp3 files after npm run build:www."
  key_links:
    - "main()'s copy sequence in tools/build-www.mjs -> copySfx() — if the call is not added, the function exists and does nothing and www/ ships silent."
    - "sfx/ (repo source of truth, 30 MP3s) -> www/sfx/ (Capacitor webDir) -> the Android bundle — this whole-directory copy is the only thing that makes AUD-06's offline guarantee true."
  prohibitions:
    - statement: "MUST NOT modify any file under engine/ or content/."
      status: flagged-unverified
      verification: "git diff --stat v1.7..HEAD -- engine/ content/ prints nothing"
    - statement: "MUST NOT add, remove or reorder any rng draw; MUST NOT call Math.random() anywhere in the audio path (variation is a counter-driven shuffle-bag per CONTEXT)."
      status: flagged-unverified
      verification: "comment-filtered grep over src/browser/sfx.js finds zero pseudo-random calls; test/determinism and test/parity suites pass unchanged"
    - statement: "MUST NOT move any parity fixture, edit test/parity/prototype-master.js.txt, or touch test/parity/harness/comparables.js."
      status: flagged-unverified
      verification: "git hash-object test/parity/prototype-master.js.txt equals a1f4d0dc29782218d8e5aab65bc5989c33f917f0; git diff --stat v1.7..HEAD -- test/parity/ prints nothing"
    - statement: "MUST NOT fetch any asset over the network at runtime, and MUST NOT add a third-party dependency or SDK."
      status: flagged-unverified
      verification: "no absolute URL scheme appears in src/browser/sfx.js; git diff v1.7..HEAD -- package.json shows no dependency change"
    - statement: "MUST NOT construct an AudioContext while the Sound setting is Off."
      status: flagged-unverified
      verification: "test/unit/sfx-settings.test.js asserts the injected backend's open() call count is 0 for sound:false"
    - statement: "MUST NOT alter the return value or observable side effects of dispatchWithNarration() beyond playing audio — its callers keep their render tail byte-identical."
      status: flagged-unverified
      verification: "the mazeworld.html diff inside dispatchWithNarration adds exactly one statement and no return-path change; the hapticForEvents call-site count stays 2"
    - statement: "MUST NOT add new bodies to mazeworld.html's paint()/draw(); the audio layer lives in src/browser/sfx.js."
      status: flagged-unverified
      verification: "git diff v1.7..HEAD -- mazeworld.html shows no hunk inside the paint() or draw() bodies"
    - statement: "MUST NOT change combat, rail or encounter timing to make room for sounds — pacing is Phase 58 (MOTION-03)."
      status: flagged-unverified
      verification: "git diff v1.7..HEAD -- mazeworld.html src/browser/ introduces no new timing constant or delay"
---

<objective>
Get the 30 delivered sound clips into the shipped Android bundle, and make a build that is missing them fail loud instead of shipping silent.

Purpose: AUD-06 — audio must work on first launch after install with the phone in airplane mode. That is only true if every clip is copied into `www/` at build time and nothing is fetched at runtime. `icons/optimized/`, `fonts/` and the splash already copy; `sfx/` is the only gap.
Output: `copySfx()` in `tools/build-www.mjs` wired into the build's copy sequence, and `test/unit/sfx-assets.test.js` pinning the 30-clip manifest.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-sound-effects-audio-settings/56-CONTEXT.md
@tools/build-www.mjs
</context>

<!-- planner-discipline-allow: Math.random -->

<tasks>

<task type="auto">
  <name>Task 1: Add copySfx() to the build's copy sequence</name>
  <files>tools/build-www.mjs</files>
  <read_first>
    - tools/build-www.mjs — read `copyIcons()` (the whole-directory-copy-with-loud-throw precedent), `copyFonts()`, `copySplash()`, `copySourceDirs()`, and `main()`'s copy sequence. `copyIcons()` is the shape to mirror exactly.
    - .planning/phases/56-sound-effects-audio-settings/56-CONTEXT.md — the "Settings, packaging & tests" decision block: mirror `copyIcons()` exactly, whole-directory copy, throw loudly if the source is missing, MP3s ship as delivered with no re-encoding.
  </read_first>
  <action>
    Define `copySfx()` in tools/build-www.mjs immediately after `copySplash()`, mirroring `copyIcons()` line for line in structure: resolve `path.join(ROOT, "sfx")`; when `existsSync()` is false, throw an Error whose message names the missing absolute path and states that the 30 delivered one-shot clips are expected there; otherwise `cpSync(src, path.join(WWW, "sfx"), { recursive: true })` followed by a `step()` line reporting the copy.

    Add the `copySfx();` call to `main()`'s copy sequence, positioned directly after `copySplash();` and before `vendorCapacitorPackages()`, so it runs in the same phase as the other asset copies.

    It is a whole-directory copy: no per-file allowlist, no extension filtering, no transcoding, no bitrate change, no renaming. The MP3s land in `www/sfx/` exactly as they sit in `sfx/`. Do not touch `copySourceDirs()`, `copyFonts()`, `copyIcons()`, `copySplash()`, `writeIndexHtml()` or `vendorCapacitorPackages()`.

    All 30 clips copy. Do not add a skip, filter or exception for any file — the copy stays a plain whole-directory copy, so a clip added to `sfx/` later ships without a build change.

    Do not add `www/` to git — it is gitignored build output and stays that way.
  </action>
  <verify>
    <automated>npm run build:www && test "$(find www/sfx -name '*.mp3' | wc -l)" -eq 30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "copySfx(" tools/build-www.mjs` prints exactly two lines: the `function copySfx()` definition and the `copySfx();` call inside `main()`.
    - The `copySfx();` call line number is greater than the `copySplash();` call line number and less than the `vendorCapacitorPackages()` line number.
    - `npm run build:www` exits 0.
    - `find www/sfx -name '*.mp3' | wc -l` prints `30`.
    - `ls www/sfx | wc -l` prints `30` (no non-mp3 stowaways).
    - Byte-identity spot check: `cmp sfx/walk1.mp3 www/sfx/walk1.mp3` exits 0 and `cmp sfx/enemy-undead.mp3 www/sfx/enemy-undead.mp3` exits 0.
    - Fail-loud check: with `sfx/` temporarily renamed, `node tools/build-www.mjs` exits non-zero and its stderr names the missing directory; the directory is restored and `npm run build:www` exits 0 again before the task is considered done.
    - `git diff --stat -- engine/ content/` prints nothing.
  </acceptance_criteria>
  <done>The build copies `sfx/` into `www/sfx/` as part of its normal sequence, and a build with `sfx/` absent fails with a named error instead of producing a silent bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the 30-clip manifest and the build wiring in a unit test</name>
  <files>test/unit/sfx-assets.test.js</files>
  <read_first>
    - test/unit/bridge-registry.test.js — the precedent for a unit test that reads repo source/asset files off disk (`fs`, `path`, `url.fileURLToPath` for the repo root) and asserts by set equality. Reuse its repo-root resolution shape.
    - tools/build-www.mjs — the `copySfx()` written in Task 1, so the source-scan assertion matches the real identifier and call position.
    - .planning/phases/56-sound-effects-audio-settings/56-CONTEXT.md — the `sfx/` inventory line listing the 30 verified filenames.
  </read_first>
  <action>
    Create `test/unit/sfx-assets.test.js` as a `node:test` + `node:assert/strict` file with a frozen `EXPECTED_CLIPS` array of the 30 delivered basenames (no extension): `walk1`, `walk2`, `walk3`, `walk-water1`, `walk-water2`, `walk-water3`, `hit1`, `hit2`, `miss1`, `miss2`, `hurt1`, `hurt2`, `hurt3`, `foe-die`, `enemy-beast`, `enemy-demon`, `enemy-human`, `enemy-undead`, `spell`, `resist`, `heal`, `drink`, `chest`, `gold`, `trap`, `jump`, `stairs`, `levelup`, `death`, `ui-tap`.

    Assert, as separate named tests:
    1. `EXPECTED_CLIPS.length` is 30 and contains no duplicates.
    2. `readdirSync(sfx/)` sorted equals `EXPECTED_CLIPS.map(id => id + ".mp3")` sorted — set equality both ways, so a new, renamed or deleted clip fails here (this is the totality gate for the asset side).
    3. Every file in `sfx/` has a non-zero `statSync().size`.
    4. Source scan of `tools/build-www.mjs`: it contains a `function copySfx(` definition, contains a `copySfx();` call, the call's index is after the `copySplash();` call's index, and the file references `path.join(ROOT, "sfx")` and `path.join(WWW, "sfx")`.
    5. Source scan of `tools/build-www.mjs`: the `copySfx` body contains an `existsSync` guard that throws, proving the fail-loud contract is present and not just documented.

    This test reads only; it must not write, copy or build anything. It must not import `src/browser/sfx.js` (which does not exist yet in this wave) and must not require `www/` to exist.

    This manifest is an asset-side check only: it asserts the 30 files EXIST. Whether each one is reachable from the event map is plan 56-02's assertion, not this file's business.
  </action>
  <verify>
    <automated>node --test test/unit/sfx-assets.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/sfx-assets.test.js` exits 0 with 5 or more passing tests and 0 failing.
    - Teeth check: temporarily appending a stray `zz.mp3` to `sfx/` makes `node --test test/unit/sfx-assets.test.js` exit non-zero; the stray file is deleted and the test passes again before the task is done.
    - `grep -c "EXPECTED_CLIPS" test/unit/sfx-assets.test.js` is at least 3.
    - `grep -c "import .*src/browser/sfx" test/unit/sfx-assets.test.js` is 0 (no dependency on wave-2 work).
    - `npm test` reports 0 failing tests.
  </acceptance_criteria>
  <done>The 30-clip set and the `copySfx()` build wiring are both pinned by an automated test that fails if either drifts.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

Phase-wide symbol inventory (every plan repeats this list; the **Plan** column says which plan creates it).

| Artifact | Kind | Plan |
|---|---|---|
| `tools/build-www.mjs` -> `copySfx()` | build function (new) | 56-01 |
| `www/sfx/` (30 `.mp3` files) | build output, gitignored | 56-01 |
| `test/unit/sfx-assets.test.js` | test file (new) | 56-01 |
| `src/browser/sfx.js` | module (new) | 56-02 creates, 56-03 extends |
| `CLIP_IDS` | exported frozen array (30 ids) | 56-02 |
| `CLIP_GROUPS` | exported frozen map: group id -> clip id array | 56-02 |
| `EVENT_CLIP_GROUP` | exported frozen map: engine event type -> group id | 56-02 |
| `FAMILY_CRY` | exported frozen map: 6 BESTIARY families -> 4 used `enemy-*` clip ids | 56-02 |
| `STEP_SUPPRESSING_EVENTS` | exported frozen Set | 56-02 |
| `DISPATCH_CLIP_CAP` | exported constant (3) | 56-02 |
| `groupsForDispatch(actionType, events, ctx)` | exported pure function | 56-02 |
| `createVariation()` | exported factory (counter-driven round-robin) | 56-02 |
| `clipsForDispatch(actionType, events, ctx, variation)` | exported pure function | 56-02 |
| `test/unit/sfx-map.test.js` | test file (new) | 56-02 |
| `VOICE_CAP` | exported constant (8) | 56-03 |
| `unlockSfx()` | exported function | 56-03 |
| `applySfxSettings(settings)` | exported function | 56-03 |
| `playForDispatch(actionType, events, ctx)` | exported function | 56-03 |
| `playUiTap()` | exported function | 56-03 |
| `stopAllSfx()` | exported function | 56-03 |
| `__mzSfxBackendOverride` | bridge registry name (test-only injection hook) | 56-03 |
| `src/browser/bridge.js` BRIDGE entry for `__mzSfxBackendOverride` | registry row | 56-03 |
| `docs/SHELL-MODULES.md` bridge table row | generated doc row | 56-03 |
| `test/unit/sfx.test.js` | test file (new) | 56-03 |
| `test/unit/sfx-settings.test.js` | test file (new) | 56-04 |
| `mazeworld.html` sfx import + 4 call sites (dispatch seam, settings apply, first-gesture unlock, delegated ui-tap) | shell wiring | 56-04 |

No new config keys. No new dependency. `SETTINGS_DEFAULTS.sound` already exists in `src/browser/settings.js` and is not redefined.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo `sfx/` -> build output `www/sfx/` | Whatever sits in `sfx/` at build time ships inside the signed AAB. |
| build script -> Android bundle | A silent copy failure produces a shippable-but-broken artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-56-01 | Tampering | `sfx/` contents | medium | mitigate | `test/unit/sfx-assets.test.js` pins the 30-filename manifest by set equality — an added, renamed or swapped clip fails `npm test` before it can ship. |
| T-56-02 | Denial of Service | `copySfx()` | medium | mitigate | Missing `sfx/` throws at build time (`existsSync` guard), so a silent-audio bundle cannot be produced; this is the loud half of the AUD-06 missing-asset edge. |
| T-56-03 | Information Disclosure | build output | low | accept | `www/` is gitignored regenerated output containing only already-public game assets; no secret enters the copy path. |
| T-56-SC | Tampering | npm/pip/cargo installs | high | accept | This phase installs no packages — `package.json` is not modified. No legitimacy checkpoint is required because there is no install task; the prohibition "MUST NOT add a third-party dependency or SDK" is asserted in the phase gate (56-04). |
</threat_model>

<verification>
- `npm run build:www` exits 0 and `find www/sfx -name '*.mp3' | wc -l` prints 30.
- `node --test test/unit/sfx-assets.test.js` exits 0.
- `npm test` reports 0 failing.
- `git diff --stat v1.7..HEAD -- engine/ content/` prints nothing.
- `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
</verification>

<success_criteria>
The 30 delivered clips are copied whole into `www/sfx/` by the normal build, a build missing `sfx/` fails loud, and both facts are pinned by an automated test.
</success_criteria>

<output>
Create `.planning/phases/56-sound-effects-audio-settings/56-01-SUMMARY.md` when done.
</output>
