---
phase: 39-gear-magic-items-one-shot-tools
plan: 05
type: execute
wave: 5
depends_on: ["39-04"]
files_modified:
  - src/browser/viewModels.js
  - src/browser/combatMenu.js
  - src/browser/rail.js
  - mazeworld.html
  - test/unit/itemRowState.test.js
  - test/unit/combatMenu.test.js
  - test/unit/rail.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-combat-actions.test.js
  - docs/GEAR-BALANCE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GEAR-02, GEAR-05]

must_haves:
  truths:
    - "Stepping onto a wall with a ladder / a crevice with a rope shows a rail decision card BEFORE any roll: USE LADDER · CLIMB IT (wall) or USE ROPE · LEAP IT (crevice); the tool button dispatches `useTool`, the other dispatches the same `move(dir)` the player would have made"
    - "The post-fall retry card reads CLIMB IT (wall) or LEAP IT (crevice) and, when the matching tool is carried, also offers USE LADDER / USE ROPE"
    - "The Darkness card offers USE TORCH when a torch is carried; a torch is also usable from the Gear tab and the ITEMS submenu while in the dark"
    - "Gear-tab item rows and the ITEMS submenu rows show ONE row-state rule (`itemRowState`): READY / `N SQ` (effect running) / `cd N SQ` (cooling) / `k/n · N SQ` (staff charges + recharge) — always tappable, a tap on cooldown reaches the engine's named refusal"
    - "The condition-chip strip shows one chip per live item effect with its countdown (Hasted 23 sq · Invisible 40 sq · Lit 33 sq · Strong 20 sq …), one per cooling item ('Cloak of Speed · cd 41 sq'), one per recharging staff ('Pine Staff · 0/1 · 17 sq'); tapping a chip explains it in one line naming the item and what is counting"
    - "The Hero tab's TO STRIKE / TO HIT read the engine's strikeDie/toHit (weapon need mod and Acuteness included), not the stale classic duplicates"
    - "docs/GEAR-BALANCE.md carries the UI section and the full requirements map; REQUIREMENTS.md marks GEAR-02 and GEAR-05 complete; the whole-phase gate is green; the SUMMARY aggregates the phase's Pixel 7 checklist grouped by plan and numbered continuously"
  artifacts:
    - "src/browser/viewModels.js — itemRowState(state, it), ITEM_STATE_COPY"
    - "src/browser/rail.js — RAIL_COPY.hazard, RAIL_COPY.dark"
    - "mazeworld.html — hazard decision card, retry-card tool button, dark-card USE TORCH, Gear-tab row states, chip copy tables, window.mzUseTool + four bridges"
    - "test/unit/shell-gear-39.test.js — source pins"
    - "docs/GEAR-BALANCE.md — 'Chips and row states — Plan 05' + 'Requirements map'"
  key_links:
    - "mazeworld.html renderRail -> S.pendingHazard (decision card) and window.__mzRail.pending { kind: 'climb'|'dark', dir, feat } (retry / dark cards) -> window.mzUseTool / window.move / window.mzUseItem"
    - "combatMenu.js ITEMS rows and mazeworld.html Gear-tab rows -> viewModels.js#itemRowState -> engine/effects.js#remaining + derived.js#activationFor (the ONE row-state rule)"
    - "paintConditions -> conditionsOf descriptors (Plan 03's itemCooldown/staffCharges/lit keys) -> CONDITION_COPY/TONE/EXPLAIN rows"
---

<objective>
Put Phase 39 on the screen and close the phase: the hazard decision cards (pre-roll and retry) with USE LADDER / USE ROPE, the Darkness card's USE TORCH, Gear-tab and ITEMS row states on one view-model rule, the condition chips for item effects / cooldowns / staff charges with tap explanations, the Hero tab's to-hit routed through the engine, the ledger's UI + requirements-map sections, REQUIREMENTS.md, the whole-phase gate, and the aggregated Pixel 7 checklist for the milestone-close UAT batch.

Purpose: SC-2 ("shows its effect-remaining, then its cooldown-remaining, as a condition chip until it's usable again") and SC-3 ("consume it at that decision point (e.g. the CLIMB IT rail card)") are player-visible truths; this plan is where they become visible. It is also the ONLY plan that edits `mazeworld.html` (the standing merge hazard).

Output: pure-module row-state/copy changes + tests, the shell surfaces + source pins, ledger close, phase gate, checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-CONTEXT.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-03-SUMMARY.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-04-SUMMARY.md
@.planning/phases/38-melee-active-abilities/38-05-SUMMARY.md
@docs/ABILITIES.md
</context>

<standing_rules>
- Rail is the ONE feedback surface; decision cards only for decisions (the hazard choice, the torch offer); every other new event stays a toast + Oracle line.
- Voice: sarcastic, deadpan, family-friendly; every new player-facing literal lives in an object-literal table the voice scan walks (`RAIL_COPY`, `ITEM_STATE_COPY`, `COMBAT_MENU_COPY`, the shell's `CONDITION_*` tables) — run `node --test test/voice/safety-scan.test.js` and `test/unit/rail.test.js`.
- DOM: createElement/textContent for every new row/button (T-38-11 precedent) — no innerHTML built from item names in the new regions.
- Commit convention `feat(39-05): …` / `test(39-05): …` / `docs(39-05): …` + the two attribution lines; explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT: no device step, no `adb`, no APK build; the SUMMARY's "## Human verification (deferred to end of run)" is the PHASE's aggregated Pixel 7 checklist (grouped by plan, numbered continuously, copying 38-05-SUMMARY.md's format and folding in Plans 01–04's deferred items).
- `npm run build:www` must exit 0 (mazeworld.html + src/browser change).
</standing_rules>

<artifacts_this_phase_produces>
See 39-01-PLAN.md "artifacts_this_phase_produces". This plan creates every P05 entry: `viewModels.js#itemRowState` + `ITEM_STATE_COPY`, `combatMenu.js` ITEMS rows on `itemRowState` (+ `COMBAT_MENU_COPY` additions if any), `rail.js` `RAIL_COPY.hazard`/`RAIL_COPY.dark`, `mazeworld.html` hazard decision card / retry-card tool button / dark-card USE TORCH / Gear-tab row states / `CONDITION_COPY|TONE|EXPLAIN` rows for `lit`, `itemCooldown`, `staffCharges` / `explainCondition(cn, label)` / `window.mzUseTool` / `window.__mzHasTool`, `__mzToHit`, `__mzStrikeDie`, `__mzItemRowState` bridges, `test/unit/itemRowState.test.js`, `test/unit/shell-gear-39.test.js`, the ledger's UI + requirements-map sections.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: itemRowState view-model, ITEMS submenu rows, rail copy — pure modules, tests first</name>
  <files>src/browser/viewModels.js, src/browser/combatMenu.js, src/browser/rail.js, test/unit/itemRowState.test.js, test/unit/combatMenu.test.js, test/unit/rail.test.js</files>
  <read_first>
    - src/browser/combatMenu.js lines 218-262 (the ITEMS slot: carriedRows/wornRows computing `cd` from the retired usedAt/every, `enabled: cd === 0`) and the Phase 38 ABILITIES branch (abilityRows — the always-enabled READY / N ROUNDS precedent and COMBAT_MENU_COPY literals)
    - src/browser/viewModels.js (module imports; `characterSheetViewModel`'s ABILITY_VIEW_COPY as the copy-table precedent)
    - src/browser/rail.js lines 70-110 (RAIL_COPY; `climb: { retry: "CLIMB IT" }`)
    - engine/derived.js (Plan 03's activationFor/itemTimerId/chargesTimerId, Plan 04's hasTool), engine/effects.js#remaining
    - test/unit/combatMenu.test.js lines 20-60 and 190-290 (the fixedWornStaff helper with `every/usedAt`, the ITEMS cd tests to rewrite), test/unit/rail.test.js (how RAIL_COPY is walked by the voice scan)
    - .planning/phases/38-melee-active-abilities/38-05-SUMMARY.md "key-decisions" (the state-suffix rule lives in ONE place; rows on cooldown stay tappable)
  </read_first>
  <behavior>
    - `itemRowState(state, it)` returns `{ text, kind, remaining }`: a non-activatable (weapon/armor/rope/ladder/passive jewel) → `{ text: "", kind: "none" }`; a consumable (potion, torch) → `{ text: "", kind: "consumable" }`; a cd item with no record → `{ text: "READY", kind: "ready" }`; during its effect → `{ text: "{n} SQ", kind: "effect", remaining: n }`; during its cooldown → `{ text: "cd {n} SQ", kind: "cooldown", remaining: n }`; a staff with charges and no recharge record → `{ text: "{k}/{max}", kind: "charges" }` hmm — no: READY when `charges === max`; `{k}/{max} · {n} SQ` while recharging (k may be 0); singular "1 SQ" handled like Phase 38's "1 ROUND". The rule reads `state.c.timers` via `remaining` and the activation via `activationFor` — never `it.usedAt`/`it.every`.
    - `ITEM_STATE_COPY` object literal holds every literal (`ready: "READY"`, `squares: "{n} SQ"`, `square: "1 SQ"`, `cooling: "cd {n} SQ"`, `charges: "{k}/{max} · {n} SQ"`).
    - combatMenu ITEMS rows: `cost: itemRowState(state, it).text`, `enabled: true` for every activatable row (a tap on cooldown reaches the engine's `useRefused` line — the Phase 38 ruling); a torch in the bag appears as a row (it has `use`) with an empty cost; rope/ladder do NOT appear (no `use`); the `usableCount` and the "none" fallback are unchanged in shape; a legacy `c` with no `timers` gets READY for its cd items.
    - `RAIL_COPY.hazard` = `{ title: "A CHOICE", ladder: "USE LADDER", rope: "USE ROPE", climb: "CLIMB IT", leap: "LEAP IT", wall: "A wall. You could climb it. You could also not.", crevice: "A crevice. Leaping is traditional. Rope is smarter." }`; `RAIL_COPY.dark = { torch: "USE TORCH" }`; `RAIL_COPY.climb.retry` stays "CLIMB IT" (existing pin); rail.test.js's recursive voice walk passes.
  </behavior>
  <action>
    Write `test/unit/itemRowState.test.js` FIRST and rewrite `test/unit/combatMenu.test.js`'s ITEMS tests (the `fixedWornStaff`/`every/usedAt` fixtures become `charges` + planted `c.timers` records) — RED. Then: `viewModels.js` — export `itemRowState(state, it)` and `ITEM_STATE_COPY` (imports: `activationFor`, `itemTimerId`, `chargesTimerId` from engine/derived.js; `remaining` from engine/effects.js; read the record's `phase` from `state.c.timers[id]`). `combatMenu.js` — both ITEMS row builders call `itemRowState`; delete the two `it.every ? …` lines; `enabled: true`; JSDoc: the ONE row-state rule is viewModels.js#itemRowState (the Gear tab reads it through `window.__mzItemRowState`). `rail.js` — add `hazard` and `dark` to `RAIL_COPY` (frozen literal). Run the voice scan.
  </action>
  <verify>
    <automated>node --test test/unit/itemRowState.test.js test/unit/combatMenu.test.js test/unit/rail.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function itemRowState" src/browser/viewModels.js` prints 1 and `grep -c "export const ITEM_STATE_COPY" src/browser/viewModels.js` prints 1.
    - `grep -c "usedAt" src/browser/combatMenu.js src/browser/viewModels.js` prints 0 for both. <!-- planner-discipline-allow: usedAt -->
    - `grep -c "itemRowState(" src/browser/combatMenu.js` is at least 2.
    - `grep -c 'ladder: "USE LADDER"\|rope: "USE ROPE"\|torch: "USE TORCH"\|leap: "LEAP IT"' src/browser/rail.js` prints 4.
    - `node --test test/unit/itemRowState.test.js test/unit/combatMenu.test.js test/unit/rail.test.js test/voice/safety-scan.test.js` `# fail 0`.
  </acceptance_criteria>
  <done>One view-model rule renders every item's READY / effect / cooldown / charges state; the ITEMS submenu uses it with always-tappable rows; the rail copy for the hazard/dark buttons exists and passes the voice scan.</done>
</task>

<task type="auto">
  <name>Task 2: mazeworld.html — hazard decision + retry + dark cards, Gear-tab row states, chip copy, engine-routed Hero-tab to-hit, bridges, source pins</name>
  <files>mazeworld.html, test/unit/shell-gear-39.test.js, test/unit/shell-worn-slots.test.js, test/unit/shell-combat-actions.test.js</files>
  <read_first>
    - mazeworld.html lines 2886-2910 (CONDITION_COPY), 2975-2995 (CONDITION_TONE / CONDITION_EXPLAIN), 3005-3080 (paintConditions), 3140-3155 (paint's s-die/s-hit lines using the classic strikeDie()/toHit()), 3290-3305 (wornSlotRow's `it.every ? …` cooldown text), 3870-3900 (renderCarriedList rows' `it.every ? …` line and the Use button condition), 5605-5620 (COMBAT_DISPATCH), 6165-6260 (renderRail's joiner/find/climb branches and railButtons), 6940-6970 (the module-script bridge block: `window.__mzLootCompare = lootCompare` etc.), 7385-7470 (window.move guards, stepNow, the `pending` assignment at 7428, the `__mzPendingNarration` stash), 7855-7900 (inventoryAction bridges, window.mzUseItem)
    - src/browser/rail.js (Task 1's RAIL_COPY.hazard/dark) and src/browser/viewModels.js#itemRowState
    - engine/derived.js exports `hasTool`, `toHit`, `strikeDie`, `toolIndex` (items.js)
    - test/unit/shell-worn-slots.test.js line 154 (the regex pin on the old cooldown expression — must be re-pinned), test/unit/shell-abilities.test.js (the source-pin test style to copy), test/unit/shell-combat-actions.test.js (COMBAT_DISPATCH pin)
    - .planning/phases/38-melee-active-abilities/38-05-SUMMARY.md (how the Phase 38 shell plan structured bridges and pins)
  </read_first>
  <action>
    Bridges (module script block): import `hasTool`, `toHit`, `strikeDie` from `./engine/derived.js`, `toolIndex` from `./engine/items.js`, `itemRowState` from `./src/browser/viewModels.js`; set `window.__mzHasTool = hasTool`, `window.__mzToolIndex = toolIndex`, `window.__mzToHit = toHit`, `window.__mzStrikeDie = strikeDie`, `window.__mzItemRowState = itemRowState`. Add `window.mzUseTool = (tool, dir) => { if (railLocked()) { window.mzRailPulse?.(); return; } stepWith({ type: "useTool", tool, dir }); }` — refactor `stepNow(dir)` into `stepWith(action)` (the body is action-agnostic: dispatchWithToasts, noteCombat, haptics, pending computation, narration stash, beats, paint/draw/log, recenter) with `stepNow = (dir) => stepWith({ type: "move", dir })`; `dir` for the pending record comes from `action.dir`.

    Pending computation in `stepWith`: `const fellClimb = events.some(e => e.type === "fellClimbing"); const fellGorge = events.some(e => e.type === "fellInGorge"); const darkFell = events.some(e => e.type === "darknessFell"); const torch = window.__mzHasTool(state.c, "torch"); window.__mzRail = { …, pending: state.dead ? null : (fellClimb || fellGorge) ? { kind: "climb", dir: action.dir, feat: fellGorge ? "gorge" : "climb" } : (darkFell && torch) ? { kind: "dark" } : null };`. Extend the `__mzPendingNarration` stash condition to `(state.pendingJoiner || state.pendingFind || (state.pendingHazard && !state.pendingHazard.declined))`.

    renderRail: insert a branch BEFORE the joiner branch (a hazard decision is a movement decision and must win over an older card): `if (S.pendingHazard && !S.pendingHazard.declined && !S.combat && !S.store)` → `key = "hazard:" + feat + ":" + dir`, tone "info", icon "⧗", title `copy.hazard.title`, lines = pending narration (or `copy.hazard.wall`/`copy.hazard.crevice` when empty), buttons `[ { id: "a-hazard-tool", label: tool === "ladder" ? copy.hazard.ladder : copy.hazard.rope, onTap: () => window.mzUseTool(tool, dir) }, { id: "a-hazard-roll", label: feat === "gorge" ? copy.hazard.leap : copy.hazard.climb, cls: "secondary", onTap: () => window.move(dir) } ]`. Retry branch (`rail.pending.kind === "climb"`): the retry label becomes `pend.feat === "gorge" ? copy.hazard.leap : copy.hazard.climb` (falls back to `copy.climb.retry` when `feat` is absent), and when `window.__mzHasTool(S.c, pend.feat === "gorge" ? "rope" : "ladder")` prepend a `{ id: "mw-rail-tool", label: …, onTap: () => { window.__mzRail = { ...window.__mzRail, pending: null }; window.mzUseTool(tool, pend.dir); } }` button. New dark branch after the climb branch: `else if (rail.pending && rail.pending.kind === "dark" && S.c.darkFor > 0 && window.__mzHasTool(S.c, "torch"))` → the darknessFell card's lines (`rail.card` or `vm.lineCard("IN THE DARK", …)`), one button `{ id: "mw-rail-torch", label: copy.dark.torch, onTap: () => { window.__mzRail = { ...window.__mzRail, pending: null }; window.mzUseItem(window.__mzToolIndex(S.c, "torch")); } }`. Buttons through `railButtons` (guardTap wiring).

    Gear tab: `wornSlotRow` and `renderCarriedList`'s row title → `const st = window.__mzItemRowState(S, it); … ${st.text ? " · " + st.text : ""}` — build the `<b>`/`<i>`/tag via createElement/textContent instead of the innerHTML template in the two changed rows (keep every other row untouched); the Use button condition stays `it.kind === "potion" || it.use` (a torch has `use`, rope/ladder do not).

    Chips: CONDITION_COPY adds `lit: { label: "Lit", unit: "sq" }`, `itemCooldown: { label: "", unit: "sq" }`, `staffCharges: { label: "", unit: "sq" }` and gives `might` `unit: "sq"` (a spell might chip carries no `remaining`, so the generic branch prints no detail for it — unchanged). CONDITION_TONE adds `lit: "good", itemCooldown: "odd", staffCharges: "odd"`. paintConditions: `label = cn.item ?? (CONDITION_COPY[cn.key]?.label || cn.key)` for the two item-keyed chips; detail: `itemCooldown` → `cd ${cn.remaining} sq`, `staffCharges` → `${cn.charges}/${cn.max} · ${cn.remaining} sq`, `acute` keeps its rds/sq branch (now read from `cn.cadence === "rounds"`). Add `explainCondition(cn, label)`: for an item-sourced chip (`cn.source` or `cn.item`) returns `"{Item} — {what}, {n} {squares|rounds}."` (the unit spelled out, `cn.cadence === "rounds"` → rounds, else squares) where what = `cooling` (itemCooldown — CONTEXT's example line is exactly "Cloak of Speed — cooling, 41 squares"), `{charges} of {max} charges, next in` (staffCharges), else the chip label lower-cased (e.g. "Cloak of Speed — hasted, 23 squares"), followed by the CONDITION_EXPLAIN sentence; CONDITION_EXPLAIN adds `lit: "You are carrying a light. Darkness will have to wait its turn."`, `itemCooldown: "Used, and not ready to be used again. Squares fix that."`, `staffCharges: "The staff is remembering how to do that. Walk it off."`. The chip tap passes `explainCondition(cn, label)` to `mzRailLine`.

    Hero tab: `document.getElementById("s-die").textContent = "d" + window.__mzStrikeDie(S.c); document.getElementById("s-hit").textContent = "1–" + window.__mzToHit(S);` (the classic duplicates stay in place as dead code for the cleanup milestone — do not delete them here).

    COMBAT_DISPATCH: no new entry (the torch in combat goes through the existing `useItem` dispatch); re-pin `shell-combat-actions.test.js` only if its count changed (it should not).

    Source pins: `test/unit/shell-gear-39.test.js` (new) asserting the source contains: `window.__mzHasTool = hasTool`, `window.__mzItemRowState = itemRowState`, `window.__mzToHit = toHit`, `window.__mzStrikeDie = strikeDie`, `window.mzUseTool =`, `function stepWith(`, `S.pendingHazard && !S.pendingHazard.declined`, `copy.hazard.ladder`, `copy.hazard.leap`, `copy.dark.torch`, `rail.pending.kind === "dark"`, `explainCondition(`, `itemCooldown:` in CONDITION_TONE, `"s-hit").textContent = "1–" + window.__mzToHit(S)`; and that the two Gear-tab row builders contain `__mzItemRowState(S, it)` and no longer contain the old `it.every ?` expression (region-scoped grep between `const wornSlotRow` and `renderCarriedList(carry` and inside `rows.forEach`). Re-pin `shell-worn-slots.test.js:154` to the new row expression. Run `npm run build:www`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-gear-39.test.js test/unit/shell-worn-slots.test.js test/unit/shell-combat-actions.test.js test/unit/shell-abilities.test.js test/voice/safety-scan.test.js && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "window.mzUseTool = " mazeworld.html` prints 1; `grep -c "function stepWith(" mazeworld.html` prints 1; `grep -c "S.pendingHazard && !S.pendingHazard.declined" mazeworld.html` is at least 1.
    - `grep -c 'rail.pending.kind === "dark"' mazeworld.html` prints 1; `grep -c "copy.dark.torch" mazeworld.html` prints 1; `grep -c "copy.hazard.ladder\|copy.hazard.rope" mazeworld.html` is at least 2.
    - `sed -n '/const wornSlotRow = /,/renderCarriedList(carry/p' mazeworld.html | grep -c "it.every ?" ` prints 0 and `sed -n '/rows.forEach(({ it, i }) => {/,/for (const a of (opts.actions/p' mazeworld.html | grep -c "it.every ?"` prints 0 (region-scoped: the classic dead-code duplicates elsewhere in the file are out of scope). <!-- planner-discipline-allow: it.every ? -->
    - `grep -c '__mzItemRowState(S, it)' mazeworld.html` is at least 2.
    - `grep -c 'window.__mzToHit(S)' mazeworld.html` prints 1 and `grep -c 'window.__mzStrikeDie(S.c)' mazeworld.html` prints 1.
    - `grep -c "itemCooldown\|staffCharges\|lit:" mazeworld.html` is at least 6 (COPY + TONE + EXPLAIN rows + paintConditions branches).
    - `npm run build:www` exits 0; `node --test test/unit/shell-gear-39.test.js` `# fail 0` with at least 10 pins.
  </acceptance_criteria>
  <done>Every GEAR-02/GEAR-05 surface is on screen through the rail (decision, retry, dark cards), the Gear tab and ITEMS rows share one row-state rule, chips show and explain effects/cooldowns/charges, and the Hero tab's to-hit is the engine's.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger close (UI + requirements map), REQUIREMENTS.md, whole-phase gate, aggregated Pixel 7 checklist</name>
  <files>docs/GEAR-BALANCE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/GEAR-BALANCE.md (every section Plans 02–04 wrote; the two remaining placeholders "## Chips and row states — Plan 05" and "## Requirements map — Plan 05")
    - docs/ABILITIES.md lines 470-560 ("## UI (Plan 05)" and "## Requirements map" — the shape to mirror)
    - .planning/REQUIREMENTS.md lines 29-36 and 113-115
    - .planning/phases/39-gear-magic-items-one-shot-tools/39-01-SUMMARY.md … 39-04-SUMMARY.md — each "## Human verification (deferred to end of run)" section (the items to aggregate)
    - .planning/phases/38-melee-active-abilities/38-05-SUMMARY.md "## Success Criteria Map" and "## Human verification (deferred to end of run)" (the exact aggregated-checklist format: grouped by plan, numbered continuously, "Nothing to add" notes for plans without device-testable surfaces)
  </read_first>
  <action>
    1. docs/GEAR-BALANCE.md: fill "## Chips and row states — Plan 05" (the `itemRowState` rule and its five literals; always-tappable rows and the Phase 38 ruling; the chip keys/labels/details/explanations table; the three rail cards — pre-roll decision, retry with tool, dark with torch — with their button labels and dispatches; the Hero-tab to-hit routing; what stays for the cleanup milestone: the classic `strikeDie()/toHit()/useItem()` duplicates). Fill "## Requirements map — Plan 05": `GEAR-01 → Plans 01/02`, `GEAR-02 → Plans 03/05`, `GEAR-05 → Plans 04/05`; ROADMAP SC-1 → `test/unit/gear-axes.test.js` (band role coverage) + the ledger's per-class table; SC-2 → `test/unit/item-activation.test.js` (effect → cooldown records) + `test/unit/itemRowState.test.js` + `test/unit/conditions.test.js` (chips); SC-3 → `test/unit/tools.test.js` (hazardChoice/useTool/torch) + `test/unit/shell-gear-39.test.js` (the card wiring); SC-4 → `test/unit/tools.test.js` (loot row + store tiers). Add a closing "## Out of scope / next" list: Phase 40 spells/scrolls on the same timers; Phase 41 torch vs the reveal/timed-light model; Phase 42 bot tactics for items + `wornSlots`; the cleanup milestone's classic-duplicate deletion.

    2. .planning/REQUIREMENTS.md: `[x]` GEAR-02 and GEAR-05; traceability rows → Complete.

    3. WHOLE-PHASE GATE (this is the phase's verification — agents are off): `npm test` → `# tests N`, `# fail 0` (record N; expect > 2563); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` empty (every fixture change was committed inside Plans 01/04 with its declaration — list those files from the SUMMARIES); `npm run build:www` exit 0; `git diff --stat e5b7be0 -- engine content src mazeworld.html docs tools package.json package-lock.json` — record the file list and confirm `package.json`/`package-lock.json` are absent from it (zero new dependencies) and `fonts.googleapis` count in mazeworld.html is 0; `node --test test/unit/chargen-rng-pin.test.js` green (the chargen cursor never moved); confirm `store-listing/` and `tools/store-screenshots/` were never staged (`git log --stat e5b7be0..HEAD -- store-listing tools/store-screenshots` prints nothing).

    4. SUMMARY: the standard shape PLUS "## Success Criteria Map (ROADMAP SC-1..4)" (SC → text → plan → proof, like 38-05) and "## Human verification (deferred to end of run)" = the PHASE's aggregated Pixel 7 checklist: `### Plan 01` (TO HIT 1–6 Rapier / 1–4 Flail on a Fighter; Thief in Plate refused the backstab, in Studded not; a store shows visibly different weapon lines with "+N a swing"/"not an upgrade"), `### Plan 02` ("Nothing to add" beyond the store readout if already covered — say so), `### Plan 03` (Speed potion → "You use Speed potion." + "Double attacks for 50 squares." + Hasted chip counting down; a worn Cloak of Speed used twice → the vending-machine line and a "cd N sq" chip; Pine Staff twice → the patience line and a "0/1 · N sq" chip; the chip tap explanation naming the item; a pre-Phase-39 save resumes with no card), `### Plan 04`/`### Plan 05` (the decision card before a wall with a ladder; decline at a crevice, fall, the retry card offering LEAP IT + USE ROPE; Darkness with a torch → USE TORCH, later Darkness resisted; torch refused in a lit corridor; store lines by depth; Gear-tab rows READY / N SQ / cd N SQ; ITEMS rows tappable on cooldown reaching the refusal; TalkBack reads the new buttons and chips; a voice spot-check of the new lines in real play). Number continuously across plans; fold in every item the four earlier SUMMARIES listed.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && git status --porcelain test/parity/fixtures && npm run build:www && grep -c "fonts.googleapis" mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0` and the `# tests` count is greater than 2563.
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `git status --porcelain test/parity/fixtures` prints nothing; `git diff --stat e5b7be0 -- package.json package-lock.json` prints nothing.
    - `grep -c "fonts.googleapis" mazeworld.html` prints 0.
    - `grep -c "\[x\] \*\*GEAR-02\*\*\|\[x\] \*\*GEAR-05\*\*" .planning/REQUIREMENTS.md` prints 2; `grep -c "| GEAR-0[125] | Phase 39 | Complete |" .planning/REQUIREMENTS.md` prints 3.
    - `grep -c "## Requirements map" docs/GEAR-BALANCE.md` prints 1 and `grep -c "appended by Plan" docs/GEAR-BALANCE.md` prints 0 (every placeholder filled). <!-- planner-discipline-allow: appended by Plan -->
    - The SUMMARY contains "## Human verification (deferred to end of run)" with `### Plan 01` … `### Plan 05` headings and a continuously numbered list.
  </acceptance_criteria>
  <done>Phase 39 is closed: ledger complete, all three GEAR requirements marked complete, the whole-phase gate recorded, and the milestone-close UAT batch has its aggregated checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state → DOM | item names / tool names rendered into rows, chips and rail buttons |
| rail buttons → engine | `mzUseTool`/`mzUseItem` dispatch validated actions; a stale card must not dispatch a wrong index |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-14 | Tampering | item/tool names into the DOM (Gear rows, chip labels, explanations) | medium | mitigate | createElement/textContent in every new/changed region (T-38-11 precedent); no innerHTML template carries a name in the two rewritten row builders — pinned by shell-gear-39.test.js |
| T-39-15 | Elevation of privilege | the dark card's USE TORCH dispatching a stale bag index | low | mitigate | the index is resolved at tap time via `__mzToolIndex(S.c, "torch")`; the engine's `useItem` re-validates the item and refuses `notDark` if darkness already lifted |
| T-39-16 | Denial of service | a stuck decision card (pendingHazard shown while combat/store open) | low | mitigate | the branch is gated on `!S.combat && !S.store`; any step clears `pendingHazard` engine-side |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan (verified by the gate's `package.json` diff check) |
</threat_model>

<verification>
- Tasks 1–2 test files green; `npm run build:www` exit 0; whole-phase gate numbers recorded in the SUMMARY.
- The aggregated Pixel 7 checklist exists, grouped by plan and numbered continuously, and is never executed in this run.
</verification>

<success_criteria>
- ROADMAP SC-2 and SC-3 are player-visible (chips with countdowns; the decision-point cards); SC-1 and SC-4 were closed by Plans 02 and 04; GEAR-01/02/05 all complete.
</success_criteria>

<output>
Create `.planning/phases/39-gear-magic-items-one-shot-tools/39-05-SUMMARY.md` when done — the phase-closing SUMMARY (38-05-SUMMARY.md shape) with the Success Criteria Map and the aggregated "## Human verification (deferred to end of run)" checklist.
</output>
