---
phase: 39-gear-magic-items-one-shot-tools
plan: 04
type: execute
wave: 4
depends_on: ["39-03"]
files_modified:
  - content/tools.js
  - content/activations.js
  - content/index.js
  - engine/derived.js
  - engine/items.js
  - engine/economy.js
  - engine/encounters.js
  - engine/movement.js
  - engine/actions.js
  - engine/engine.js
  - engine/state.js
  - engine/saveState.js
  - tools/lib/tuning-bot.mjs
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/unit/tools.test.js
  - test/unit/bot-buy-policy.test.js
  - test/unit/actions.test.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/GEAR-BALANCE.md
autonomous: true
requirements: [GEAR-05]

must_haves:
  truths:
    - "Rope, Ladder and Torch exist as `kind: \"tool\"` bag items (one bag slot each, one of each at most); they appear as loot (a low-odds row in rollTreasureItem drawn from a derived rng stream — zero main-rng draws, so every fixture's draw sequence is untouched) and as store stock at depth-appropriate tiers (Torch 25 and Rope 60 from tier 0, Ladder 150 from tier 1), each line offered only when the tool is not already carried"
    - "Stepping onto a climbable wall with a ladder, or a gorge/crevice with a rope, PAUSES before any roll: the engine stashes `state.pendingHazard { feat, dir, tool, declined: false }` and emits `hazardChoice`; `useTool { tool, dir }` consumes the tool and passes the tile with no roll and no fall damage; a second `move(dir)` declines (sets `declined: true`) and runs today's synchronous roll in today's order; a character without the matching tool sees byte-identical movement (zero new draws, no pending state)"
    - "After a failed roll the pending record stays `declined: true` on the same square so the retry card can offer both CLIMB IT and USE LADDER/USE ROPE without a second prompt; any successful step, tool use, teleport or descent clears it"
    - "Using a torch while `inDark(state)` clears `c.darkFor`, emits `torchLit`, starts a 40-square `lit` effect (`item:Torch`) during which `fallDark` is suppressed with `darknessResisted`, and consumes the torch; a torch used while not dark is refused (`useRefused { reason: \"notDark\" }`) and not consumed"
    - "Every new event (`hazardChoice`, `toolUsed`, `toolRefused`, `torchLit`, `darknessResisted`, `useRefused notDark`, `itemRejected haveOne`) is narrated in EVENT_NARRATION + TOAST_FOR (+ RAIL_FAMILY) in voice and passes the safety scan"
    - "Only the fixtures the derived tool roll actually fires on move (expected: none; the chest scenario, seed 2, is the one exposed call) — declared if so; the master hash is unchanged"
  artifacts:
    - "content/tools.js — TOOLS, TOOL_ORDER, TOOL_LOOT_WEIGHTS, TOOL_ACTIVATION_OF"
    - "engine/movement.js — pendingHazard pre-check, move(…, opts.tool), useTool"
    - "engine/items.js — toolItem, toolIndex, pickLootTool, the tool loot row, takeItem haveOne refusal"
    - "engine/economy.js — giveTool store effect + tool lines"
    - "test/unit/tools.test.js"
  key_links:
    - "engine/movement.js#move pre-check -> derived.js#hasTool (the ONLY gate; false for every fixture and the bot, so the roll path is byte-identical)"
    - "engine/items.js#rollTreasureItem -> engine/rng.js#derivedRng(rng.getState(), 'tool', depth) (no main draw)"
    - "engine/items.js#useItem case light -> encounters.js#fallDark reads derived.js#itemEffectActive(c, 'lit')"
    - "test/parity/harness/comparables.js top-level strip of pendingHazard + ENGINE_ONLY_STORE_EFFECTS filter (buyRations, giveTool)"
---

<objective>
Land GEAR-05's engine half: the three one-shot tools as bag items in loot and store, the pre-roll hazard decision (`pendingHazard` + `hazardChoice` + `useTool`) that lets a player spend a ladder or rope instead of rolling, and the torch that lights `c.darkFor` darkness and holds it off for 40 squares — all with zero new rng draws on the no-tool path so every fixture and the bot keep today's movement byte-for-byte.

Purpose: SC-3/SC-4 — "Facing a pit, an unclimbable wall, or a dark region, a player carrying rope/ladder/torch respectively can consume it at that decision point"; "Rope, ladder, and torch appear as loot and store stock at depth-appropriate tiers".

Output: content + engine + actions + narration + tests; the shell cards (USE LADDER / USE ROPE / USE TORCH buttons) are Plan 05's.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-CONTEXT.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-03-SUMMARY.md
@engine/rng.js
</context>

<standing_rules>
- Greenfield; fixture protocol per 39-01-PLAN.md `<fixture_divergence_protocol>`. Prefer the derived rng stream for the new loot roll (STATE.md engine-gate amendment) so no fixture's draw order moves.
- Commit convention `feat(39-04): …` / `test(39-04): …` / `docs(39-04): …` + the two attribution lines; explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT; SUMMARY "## Human verification (deferred to end of run)".
- No `mazeworld.html` edits. `src/browser` edits are narration-table entries only (eventNarration/toasts/rail).
- The torch interacts ONLY with `c.darkFor` (not tile `.dark`, not the reveal radius — Phase 41). Bot TACTICS stay Phase 42; the one bot line this plan adds (answer a pending hazard with the tool it is already carrying) is a pending-state handler, not a timing tactic — record it in the SUMMARY.
</standing_rules>

<artifacts_this_phase_produces>
See 39-01-PLAN.md "artifacts_this_phase_produces". This plan creates every P04 entry: `content/tools.js` (`TOOLS`, `TOOL_ORDER`, `TOOL_LOOT_WEIGHTS`, `TOOL_ACTIVATION_OF`), `derived.js#hasTool`, `items.js#toolItem/toolIndex/pickLootTool`, the `rollTreasureItem` tool row, `takeItem`'s `haveOne` refusal, `economy.js` `STORE_EFFECTS.giveTool` + tool lines + `baseValueFor` tool case, `movement.js` `state.pendingHazard`/`move(…, opts)`/`useTool`, `useItem` `case "light"` + `notDark` refusal, `encounters.js#fallDark` suppression, `actions.js`/`engine.js` `useTool`, `state.js`/`saveState.js` `pendingHazard: null`, harness `pendingHazard` strip + `ENGINE_ONLY_STORE_EFFECTS`, events `hazardChoice`/`toolUsed`/`toolRefused`/`torchLit`/`darknessResisted`, `test/unit/tools.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tools content; loot row on a derived stream; store lines; sell value; duplicate gate</name>
  <files>content/tools.js, content/activations.js, content/index.js, engine/derived.js, engine/items.js, engine/economy.js, test/unit/tools.test.js, test/parity/harness/comparables.js</files>
  <read_first>
    - engine/items.js lines 95-176 (hasPicks, rollTreasureItem's lockpick gate — the 1:1 precedent, rollJewel/rollCloak/rollStaff) and 319-400 (takeItem, giveItem/stowItem, bagCap)
    - engine/rng.js (derivedRng(...parts), makeRng, getState)
    - engine/economy.js lines 44-160 (TREASURE_BASE_VALUES/baseValueFor/sellPriceFor), 188-235 (STORE_EFFECTS incl. giveLockpicks), 235-260 (storeTier), 306-430 (openStore — the lockpick `add(...)` line, the Rations `add(...)` at the end, the haggle pass), 435-478 (STOWING_EFFECTS, buyFrom)
    - content/activations.js and content/index.js (Plan 03)
    - engine/derived.js (Plan 03's activationKeyFor — tools key by `it.n`)
    - test/parity/harness/comparables.js lines 433-455 (stripStoreClosures's buyRations filter) and Plan 01's declaredStockDiffs
    - test/unit/economy.test.js and test/unit/store-roll.test.js (existing stock-line pins that count lines — the appended tool lines must not break "flat lines unchanged" pins; read them and re-pin if they assert a total line count)
  </read_first>
  <behavior>
    - `TOOLS.torch` = `{ n: "Torch", cost: 25, fromTier: 0, use: "light", txt: "lights the dark once, and keeps it off for forty squares" }`; `TOOLS.rope` = `{ n: "Rope", cost: 60, fromTier: 0, feat: "gorge", txt: "the honest way across a crevice. Once." }`; `TOOLS.ladder` = `{ n: "Ladder", cost: 150, fromTier: 1, feat: "climb", txt: "one climbable wall, no climbing. Once." }`; `TOOL_ORDER = ["torch","rope","ladder"]`; `TOOL_LOOT_WEIGHTS = { torch: 4, rope: 3, ladder: 1 }`; `TOOL_ACTIVATION_OF = { Torch: { kind: "lit", effect: 40 } }`; `ACTIVATION_OF.Torch` deep-equals that; content stays pure data.
    - `toolItem("torch")` → `{ kind: "tool", tool: "torch", n: "Torch", use: "light", txt }`; `toolItem("rope")` → `{ kind: "tool", tool: "rope", n: "Rope", txt }` (no `use`); never carries `cost`/`fromTier`/`feat`/`act`.
    - `hasTool(c, "rope")` true iff a bag item has `kind: "tool"` and `tool: "rope"`; `toolIndex(c, tool)` → index or -1; `slotItems(c)` counts a tool (bag cap applies).
    - `rollTreasureItem(rng, depth, c)`: the main rng advances by EXACTLY the same number of draws as before this plan for every (seed, depth, c) where the derived roll does not fire (pin: replay 200 seeds at depth 1 and 3 with `c = {}` and compare `rng.getState()` before/after against a pre-plan copy of the function's draw count — the lockpick d12 + the d10 + the sub-roll); when the derived roll fires (`derivedRng(rng.getState(), "tool", depth).d(8) === 1`) it returns a tool item and the main rng has advanced by exactly one draw (the lockpick d12); at depth 1 the ladder is never rolled; a carried tool is never rolled again (`hasTool` gate — falls through to the normal table with no extra main draw); find at least one seed where it fires by scanning and pin it.
    - `openStore` at depth 1 (tier 0) appends `Torch` (25) and `Rope` (60) lines AFTER the Rations line with `effectId: "giveTool"`, no Ladder; at depth 2 (tier 1) also `Ladder` (150); a hero already carrying a rope gets no Rope line; a Wilmsry's haggle (×0.7) applies to the tool lines like every other line; the flag-on (`storeRoll`) store appends the same lines after its reroll block; the pre-tool stock indices 0..N are unchanged (pin: the seed-3 economy stock's first 13 names are identical to before this task).
    - `buyFrom` on a Torch line → `bought` + the torch in the bag (`stowItem` — a full bag refuses with `bagFull`, no gold spent); `sellPriceFor(toolItem("rope"), "Human")` = 30 (half of 60); a `Ladder` sells for 75.
    - `takeItem(state, toolItem("rope"))` with a rope already carried → `itemRejected { item, reason: "haveOne" }`, nothing stowed.
    - `stripStoreClosures` drops `giveTool` lines like `buyRations` (both sides' compare still equal for the economy fixture), and Plan 01's `declaredStockDiffs` reads names/costs through `stripStoreClosures`.
  </behavior>
  <action>
    Write `test/unit/tools.test.js` FIRST (RED). Create `content/tools.js` per the behaviours (header: tools are the Lockpicks precedent — a bag item that answers one check; `feat` names the hazard tile a tool answers; `use: "light"` makes the torch a `useItem` activatable while rope/ladder are spent through `useTool`), spread `TOOL_ACTIVATION_OF` into `content/activations.js`'s `ACTIVATION_OF`, add the module to `content/index.js`.

    engine/derived.js: export `hasTool(c, tool)`. engine/items.js: export `toolItem(key)`, `toolIndex(c, tool)`, module-private `pickLootTool(toolRng, depth, c)` (candidates = `TOOL_ORDER` filtered by `depth >= 2 || key !== "ladder"` and `!hasTool(c, key)`; weighted pick: `r = toolRng.d(totalWeight)`, walk cumulative weights; `null` when no candidate). In `rollTreasureItem`, immediately after the lockpick gate and BEFORE `const r = rng.d(10)`: `const toolRng = derivedRng(rng.getState(), "tool", depth); if (toolRng.d(8) === 1) { const key = pickLootTool(toolRng, depth, c || {}); if (key) return toolItem(key); }` — document that this is the STATE.md engine-gate amendment's derived-stream pattern: keyed by the main cursor so it is deterministic per replay and adds zero main draws. In `takeItem`, before the staff class gate: `if (it.kind === "tool" && hasTool(c, it.tool)) { events.push({ type: "itemRejected", item: it, reason: "haveOne" }); return events; }`.

    engine/economy.js: `STORE_EFFECTS.giveTool(state, params, events) { stowItem(state, params.item, events, false); }`; `STOWING_EFFECTS = new Set(["giveLockpicks", "giveTool"])`; after the Rations `add(...)` and before the haggle pass: `for (const key of TOOL_ORDER) if (TOOLS[key].fromTier <= storeTier(d) && !hasTool(c, key)) add(TOOLS[key].n, TOOLS[key].cost, "giveTool", { item: toolItem(key) }, TOOLS[key].txt);` (flat prices like the lockpick line — no priceFor — zero rng, appended last so every existing index is stable). `baseValueFor`: `if (item.kind === "tool") return TOOLS[item.tool]?.cost ?? TREASURE_FALLBACK_VALUE;`.

    test/parity/harness/comparables.js: introduce `const ENGINE_ONLY_STORE_EFFECTS = new Set(["buyRations", "giveTool"])` and use it in `stripStoreClosures`'s filter and `stockMarkupDiff`'s `engineRouted` filter; make sure `declaredStockDiffs` compares `stripStoreClosures(store).stock` names/costs on both sides (extend its JSDoc: engine-only lines are dropped before comparing). Re-pin any unit test asserting a total store line count.
  </action>
  <verify>
    <automated>node --test test/unit/tools.test.js test/unit/economy.test.js test/unit/store-roll.test.js test/unit/items.test.js test/determinism/content-is-pure-data.test.js test/parity/economy-parity.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./content/index.js').then(m=>console.log(m.TOOL_ORDER.join(','), m.TOOLS.ladder.fromTier, JSON.stringify(m.ACTIVATION_OF.Torch)))"` prints `torch,rope,ladder 1 {"kind":"lit","effect":40}`.
    - `grep -c 'derivedRng(rng.getState(), "tool", depth)' engine/items.js` prints 1.
    - `grep -c '"giveTool"' engine/economy.js` is at least 2 (effect key + STOWING_EFFECTS) and `grep -c 'reason: "haveOne"' engine/items.js` prints 1.
    - `grep -c 'ENGINE_ONLY_STORE_EFFECTS' test/parity/harness/comparables.js` is at least 3.
    - `node --test test/unit/tools.test.js` `# fail 0` with at least 12 tests; `node --test test/parity/economy-parity.test.js` `# fail 0`.
  </acceptance_criteria>
  <done>Tools exist as content and items, roll as loot without touching the main rng, stock at depth tiers, sell for half price, never duplicate, and the parity store compare ignores the engine-only lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hazard pre-roll pending state + useTool action; torch lights darkness; narration; bot pending handler</name>
  <files>engine/movement.js, engine/items.js, engine/encounters.js, engine/actions.js, engine/engine.js, engine/state.js, engine/saveState.js, tools/lib/tuning-bot.mjs, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/tools.test.js, test/unit/actions.test.js, test/unit/bot-buy-policy.test.js</files>
  <read_first>
    - engine/movement.js lines 105-215 (move's signature, the one-way door checks, the flying/ether/roll block, `f.px = nx`) and the `teleport`/`descend` functions (grep `f.px =` / `state.floor =` for every position/floor assignment)
    - engine/items.js useItem (Plan 03's refusal ladder + applyActivation + the consumption branch) and derived.js#inDark
    - engine/encounters.js lines 626-660 (DARKNESS_DURATION, fallDark)
    - engine/actions.js (ACTION_TYPES, DIRS, validateAction cases) and engine/engine.js applyAction's switch
    - engine/state.js#newRun lines 200-245 (pendingFind/pendingLoot/storeRoll initialisers) and engine/saveState.js lines 373-400 (rehydrate's `pendingFind: null`) + validateSave's result object
    - tools/lib/tuning-bot.mjs decideAction's out-of-combat chain (Plan 02's store step)
    - src/browser/toasts.js lines 190-205 (SILENT list: "the dedicated … prompt IS the UI" precedent — `findOffered`) and 268-285 (FEATURE_EVENTS refusal list); src/browser/eventNarration.js lines 95-110 (flownOver/phasedThrough), 690-745; src/browser/rail.js lines 145-225 (RAIL_FAMILY + the feat-noun map for climb events)
    - test/unit/movement.test.js (how a climb/gorge tile is planted on a floor for a test — reuse that fixture builder) and test/unit/actions.test.js (validateAction pins)
  </read_first>
  <behavior>
    - `newRun(1).pendingHazard === null`; `rehydrate`/`validateSave` always yield `pendingHazard: null` (transient like pendingFind).
    - Ladder at a wall: a hero with a ladder in the bag, standing west of a `climb` tile: `move(E)` → events contain exactly one `hazardChoice { feat: "climb", dir: "E", tool: "ladder" }`, position unchanged, `state.pendingHazard` deep-equals `{ feat: "climb", dir: "E", tool: "ladder", declined: false }`, `rngState` unchanged, `steps` unchanged; a second `move(E)` → no `hazardChoice`, `pendingHazard.declined === true`, and the roll runs (rngState advanced; either `climbedOver` or `fellClimbing`); after a `fellClimbing`, `pendingHazard` is still present with `declined: true`; a third `move(E)` rolls again with no new `hazardChoice`; `move(N)` to a free tile clears `pendingHazard` to null.
    - Rope at a gorge: the same with `feat: "gorge"`, `tool: "rope"`; a hero carrying only a ladder at a gorge gets NO pending state and rolls immediately (and vice versa).
    - No tool: a hero with an empty bag stepping onto the same climb tile with the same seed produces byte-identical events and `rngState` to the pre-plan engine (pin by asserting no `hazardChoice` in events, `pendingHazard === null`, and that the move consumed draws).
    - `useTool(state, "ladder", "E", …)` at the wall: the ladder leaves the bag, events contain `toolUsed { tool: "ladder", feat: "climb", item }` and `moved`, position is the wall tile, `there.feat === null`, `pendingHazard === null`, `steps` incremented, no `climbedOver`/`fellClimbing`, and NO climb-roll draws (rngState only advances by whatever the step's normal ticks draw — assert equal to a plain step onto an empty tile with the same seed); the rest of the step (reveal, per-step ticks, cell dispatch) runs as for any step.
    - `useTool` refusals: no such tool carried → `toolRefused { tool, reason: "noTool" }`; the target tile is not the matching hazard (or a wall/out of bounds) → `toolRefused { reason: "noHazard" }`; in combat/store/dead → no-op; a flying hero uses the ladder anyway? No — `isFlying` wins: the tool is NOT consumed and `flownOver` fires (order: tool branch checks `isFlying` first).
    - `validateAction({ type: "useTool", tool: "rope", dir: "N" })` ok; `tool: "torch"` → not ok (`useTool.tool must be ladder or rope`); bad dir → not ok; `applyAction` dispatches to `useTool`.
    - Torch: `useItem` on a bag torch with `c.darkFor = 12` → `itemUsed`, `torchLit { left: 40 }`, `itemEffectStarted { kind: "lit", left: 40 }`, `itemConsumed`, `c.darkFor === 0`, torch gone, `c.timers["item:Torch"]` effect record 40 squares; with `darkFor = 0` and a lit tile → same (the tile stays `.dark` — Phase 41 owns tile darkness); with `darkFor = 0` on a lit tile → `useRefused { reason: "notDark" }`, torch still in the bag, no timers key; `fallDark` while `itemEffectActive(c, "lit")` → `darknessResisted { by: "torch" }`, no tile painted `.dark`, `darkFor` unchanged (0); after 40 steps the lit effect fades (`itemEffectFaded { kind: "lit" }`) and `fallDark` works again.
    - Bot: `decideAction` with `state.pendingHazard = { …, declined: false }` returns `{ type: "useTool", tool, dir }`; with `declined: true` it falls through to the normal chain.
    - Narration: every new event type resolves in `formatEvents` and `TOAST_FOR`; `hazardChoice` is on the toasts SILENT list (the rail decision card IS the UI, like `findOffered`); `toolRefused` is in FEATURE_EVENTS.
  </behavior>
  <action>
    Extend `test/unit/tools.test.js` (+ `test/unit/actions.test.js`, `test/unit/bot-buy-policy.test.js`) FIRST (RED).

    engine/movement.js: `export function move(state, dir, rng, events = [], now = Date.now, opts = {})`. Inside the `climb || gorge` block, restructure as: `if (opts.tool) { … } else if (isFlying(state)) { … } else if (itemEffectActive(c, "ether")) { … } else { pre-check; roll }` where the tool branch (reached only from `useTool`, which has already validated the tile and the bag) does `const i = toolIndex(state.c, opts.tool); const [used] = state.c.items.splice(i, 1); events.push({ type: "toolUsed", tool: opts.tool, feat: there.feat, item: used }); there.feat = null; state.pendingHazard = null;` and falls through to the normal step tail. The pre-check at the top of the roll branch: `const toolFor = climbing ? "ladder" : "rope"; const pend = state.pendingHazard; if (pend && pend.dir === dir && pend.feat === there.feat) { pend.declined = true; } else if (hasTool(state.c, toolFor)) { state.pendingHazard = { feat: there.feat, dir, tool: toolFor, declined: false }; events.push({ type: "hazardChoice", feat: there.feat, dir, tool: toolFor }); return events; }` — zero draws, zero mutation besides the pending record on the no-decline path; a character without the tool never enters either branch. After `f.px = nx; f.py = ny;` add `state.pendingHazard = null;` (a genuine step anywhere resolves the decision); also null it in `teleport` and `descend` beside their position/floor assignment. Export `useTool(state, tool, dir, rng, events = [], now = Date.now)`: the same combat/store/dead/won early return as move; `TOOLS[tool]?.feat` missing → `toolRefused { tool, reason: "unknown" }`; `!hasTool(state.c, tool)` → `noTool`; the tile in `dir` missing/wall/`feat !== TOOLS[tool].feat` → `noHazard`; else `return move(state, dir, rng, events, now, { tool })`. Import `TOOLS`, `hasTool`, `toolIndex`, `itemEffectActive` as needed. Header comment: the decision point design (CONTEXT Area 1), the "zero new draws on the no-tool path" invariant, and why `declined` persists on the square (the post-fall retry card offers both buttons without a second prompt).

    engine/items.js useItem: in the refusal ladder (after `combatOnly`, before `itemReady`): `if (kind === "light" && !inDark(state)) { events.push({ type: "useRefused", item: it, reason: "notDark" }); return events; }`; switch `case "light": { const wasDark = c.darkFor > 0; c.darkFor = 0; events.push({ type: "torchLit", left: ACTIVATION_OF.Torch.effect, wasDark }); break; }` (the lit effect record + `itemEffectStarted` come from applyActivation); consumption branch → `if (it.kind === "potion" || it.uses === 1 || it.kind === "tool")`.

    engine/encounters.js fallDark: first line `if (itemEffectActive(state.c, "lit")) { events.push({ type: "darknessResisted", by: "torch" }); return events; }` (import itemEffectActive); document that the torch only ever touches `c.darkFor` (Phase 41 owns the tile model).

    engine/actions.js: add `"useTool"` to ACTION_TYPES (comment: the hazard decision's spend action; the torch goes through useItem) and a validate case: `tool` must be `"ladder"` or `"rope"`, `dir` in DIRS. engine/engine.js: `case "useTool": useTool(next, action.tool, action.dir, rng, events); break;`. engine/state.js newRun: `pendingHazard: null` beside `pendingFind` (comment: transient decision state, carved out of every comparable like pendingFind). engine/saveState.js: `pendingHazard: null` in both `validateSave`'s result and `rehydrate`.

    tools/lib/tuning-bot.mjs: after the pendingJoiner/pendingFind lines: `if (state.pendingHazard && !state.pendingHazard.declined) return { type: "useTool", tool: state.pendingHazard.tool, dir: state.pendingHazard.dir };` (comment: a pending-state handler so the harness never spends two dispatches per hazard; item TIMING tactics are Phase 42).

    Narration (all three tables): `hazardChoice` — Oracle line by tool: ladder "A wall. Also: a ladder. Someone thought of everything, and it was you." / rope "A crevice, and you happen to have rope. The honest way across." (toasts: SILENT-list entry with the findOffered rationale; RAIL_FAMILY `{ icon: "⧗", title: "A CHOICE", tone: "info" }`); `toolUsed` — ladder "Up the ladder, over the wall. It did not need to be dramatic." / rope "Rope across the gap. Boring, safe, gone." (toast same text, tone hit; RAIL_FAMILY `{ icon: "⧗", title: "OVER IT", tone: "good" }`; add `toolUsed` to rail.js's feat-noun map reading `e.feat` — "wall" for climb, "crevice" for gorge — if the map is keyed by type only, add both spellings the consumer expects and note it); `toolRefused` — noTool "No {tool} on you. Wishing is not a tool." / noHazard "Nothing here for a {tool}." / unknown "That is not a tool." (block tone; FEATURE_EVENTS entry); `torchLit` — "You light the torch. The dark files a complaint." (toast tone hit; RAIL_FAMILY `{ icon: "◇", title: "LIT", tone: "good" }`); `darknessResisted` — "Darkness tries. Your torch declines." (RAIL_FAMILY same family as torchLit); `useRefused` `notDark` — "It is not dark. Save the torch for when it is."; `itemRejected` `haveOne` — "You already carry one {item}. One is the limit; two is a hobby."; `itemEffectStarted` kind `lit` → "Forty squares of carrying a light." (extend Plan 03's kind map). Run the voice scan and both coverage guards.
  </action>
  <verify>
    <automated>node --test test/unit/tools.test.js test/unit/actions.test.js test/unit/movement.test.js test/unit/bot-buy-policy.test.js test/unit/save-validation.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useTool" engine/movement.js` prints 1; `grep -c 'type: "hazardChoice"' engine/movement.js` prints 1; `grep -c "state.pendingHazard = null" engine/movement.js` is at least 3 (step tail, tool branch, teleport/descend).
    - `grep -c '"useTool"' engine/actions.js engine/engine.js` prints at least 1 for each file.
    - `grep -c "pendingHazard: null" engine/state.js engine/saveState.js` prints 1 for state.js and 2 for saveState.js.
    - `grep -c 'reason: "notDark"' engine/items.js` prints 1; `grep -c 'type: "darknessResisted"' engine/encounters.js` prints 1.
    - `grep -c 'type: "useTool"' tools/lib/tuning-bot.mjs` prints 1.
    - `grep -c "hazardChoice\|toolUsed\|toolRefused\|torchLit\|darknessResisted" src/browser/eventNarration.js` is at least 5 and the same grep on src/browser/toasts.js is at least 5.
    - `node --test test/unit/tools.test.js test/unit/actions.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js` `# fail 0`.
  </acceptance_criteria>
  <done>The hazard decision is a pure pending state with zero draws on the no-tool path, `useTool` spends the tool and passes the tile, the torch lights and holds off `c.darkFor` darkness, every event is narrated, and the bot answers a pending hazard.</done>
</task>

<task type="auto">
  <name>Task 3: Parity strip for pendingHazard, fixture check (chest seed 2), FIXTURE-INVENTORY + ledger sections, plan gate</name>
  <files>test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, test/parity/FIXTURE-INVENTORY.md, docs/GEAR-BALANCE.md</files>
  <read_first>
    - test/parity/harness/comparables.js — the three top-level destructures (`const { beats, seed, rngState, version, …, storeRoll, ...state0 } = state;` at ~320, ~400, ~850)
    - test/parity/*.test.js — `grep -n "pendingFind" test/parity/*.test.js` for any local top-level destructure that must gain `pendingHazard`
    - test/parity/FIXTURE-INVENTORY.md (Plan 03's section and the `<!-- 39-04 -->` anchor)
    - docs/GEAR-BALANCE.md ("## One-shot tools (GEAR-05) — Plan 04" placeholder)
    - test/parity/fixtures/action-script.encounters.json (the `chest` scenario, seed 2 — the one fixture call into rollTreasureItem)
  </read_first>
  <action>
    1. comparables.js: add `pendingHazard` to the three top-level destructures beside `pendingFind` (comment: a seventh analog of party/pendingJoiner/pendingFind/pendingLoot/dev/storeRoll — transient decision state, `null` on every fixture and the bot, no prototype equivalent). Mirror in any local test destructure the grep finds.

    2. Run `npm test`. Expected: every fixture byte-identical — in particular the `chest` scenario (seed 2) still offers the Cloak of Speed (the derived tool roll did not fire for that cursor). If it DID fire (the find is a tool), follow the fixture protocol: declare a `divergence` record on the chest scenario (measured before/after of `items` via the reconciled pending find — read how `reconcilePendingFind` folds it), update the FIXTURE-INVENTORY roster if the tool changed the "Foes rolled" row (it cannot — no combat), and record it. Any other failing fixture is a bug in this plan, not a divergence — fix it.

    3. FIXTURE-INVENTORY.md: replace `<!-- 39-04 -->` with `## Phase 39: one-shot tools (GEAR-05) — derived-stream loot row, pre-roll hazard state`: the derived-stream rationale (`derivedRng(rng.getState(), "tool", depth)`, zero main draws, the one exposed call = chest seed 2, measured result), the `pendingHazard` carve-out, the `giveTool` engine-only store lines (`ENGINE_ONLY_STORE_EFFECTS`), and the "byte-identical elsewhere" paragraph (no fixture hero carries a tool, so the movement pre-check never fires; the economy fixture's appended tool lines are filtered like Rations).

    4. docs/GEAR-BALANCE.md: fill "## One-shot tools (GEAR-05) — Plan 04": the three tools (name, price, tier, loot weight, what it answers, consumed when), the decision-point design (pre-roll card + retry card + dark card — the shell half is Plan 05), the `pendingHazard` shape and lifecycle (created / declined / cleared), the zero-draw invariant and the derived-stream loot row, the torch's exact scope (`c.darkFor` only; 40-square lit effect suppresses `fallDark`; tile `.dark` untouched — Phase 41), the refusal vocabulary (`toolRefused` noTool/noHazard/unknown, `useRefused` notDark, `itemRejected` haveOne), the events, the bot's pending-hazard handler and what is deferred to Phase 42.

    5. PLAN GATE: `npm test` → `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` empty unless step 2 declared the chest divergence (then exactly that one file); `git diff --stat -- mazeworld.html` empty; `npm run build:www` exit 0. Record every number in the SUMMARY.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && git status --porcelain test/parity/fixtures && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; the master hash is `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `grep -c "pendingHazard" test/parity/harness/comparables.js` is at least 3.
    - `grep -c "## Phase 39: one-shot tools" test/parity/FIXTURE-INVENTORY.md` prints 1; `grep -c "pendingHazard" docs/GEAR-BALANCE.md` is at least 1.
    - `git status --porcelain test/parity/fixtures` is empty, OR lists only `action-script.encounters.json` with a declared chest record whose `before` is byte-identical to the prior file (documented in the SUMMARY).
    - `git diff --stat -- mazeworld.html` empty; `npm run build:www` exits 0.
  </acceptance_criteria>
  <done>The harness ignores the transient pending state, the one exposed loot call is measured, the inventory and ledger record GEAR-05's engine half, and the gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action payload → engine | `useTool { tool, dir }` is untrusted wire input |
| save JSON → engine | a saved `pendingHazard` (or a tampered one) must never resume a stale decision |
| derived rng → loot | keyed by the main cursor; must not be influenceable by the shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-10 | Tampering | `useTool` with an arbitrary `tool`/`dir` | medium | mitigate | `validateAction` allows only ladder/rope and N/S/E/W; `useTool` re-checks the bag (`hasTool`) and the target tile's feat before any mutation; refusals are events, never throws |
| T-39-11 | Tampering | a tampered `pendingHazard` in a save (wrong dir, tool not carried) | low | mitigate | never rehydrated — both load chains force `pendingHazard: null`; the engine re-derives the decision on the next step |
| T-39-12 | Elevation of privilege | spending a tool the character does not own (bag index race) | low | mitigate | the tool branch resolves the index at spend time via `toolIndex`; `useTool` refuses `noTool` first |
| T-39-13 | Denial of service | a pending hazard that blocks every action | low | mitigate | `pendingHazard` blocks nothing; any successful step, teleport or descent clears it; the bot handles it in one dispatch |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan |
</threat_model>

<verification>
- `node --test test/unit/tools.test.js` green; `npm test` `# fail 0`; master hash unchanged; fixtures unchanged unless the chest record is declared; build:www ok.
- SUMMARY "Human verification (deferred to end of run)": (1) buy a Ladder at a depth-2 store, walk into a climbable wall — a decision card appears BEFORE any roll (visible after Plan 05) with USE LADDER / CLIMB IT; USE LADDER passes with no damage and the ladder is gone; (2) with a Rope, decline at a crevice (LEAP IT), fall, and confirm the retry card now offers USE ROPE beside LEAP IT; (3) get Darkness (table result) while carrying a Torch — the card offers USE TORCH; using it lights the maze and a later Darkness within 40 squares is resisted; (4) try USE from the Gear tab on a Torch in a lit corridor — the refusal line reads "It is not dark. Save the torch for when it is."; (5) a Torch/Rope line appears in a depth-1 store, Ladder only from depth 2, and none once carried.
</verification>

<success_criteria>
- ROADMAP SC-3 (engine half) and SC-4: tools in loot and store at depth tiers; a carried tool can be consumed at the hazard's decision point (pre-roll pending state / useTool / torch use) exactly once.
</success_criteria>

<output>
Create `.planning/phases/39-gear-magic-items-one-shot-tools/39-04-SUMMARY.md` when done (38-05-SUMMARY.md shape; "## Human verification (deferred to end of run)" required; record whether the chest fixture's derived roll fired).
</output>
