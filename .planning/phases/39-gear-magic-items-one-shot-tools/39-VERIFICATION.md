---
phase: 39-gear-magic-items-one-shot-tools
verified: 2026-09-18T15:20:00Z
status: passed
score: 4/4 success criteria verified on automated evidence; every player-facing check (row states, chips, hazard/dark cards, to-hit readout, store mix, old-save resume) is deferred to the end-of-run Pixel 7 batch per the user's "defer uat to end" instruction
behavior_unverified: 0
overrides_applied: 0
human_verification: ["Pixel 7 (GEAR-01, to-hit): Hero tab TO HIT / TO STRIKE show the real weapon-adjusted numbers — Rapier 1–6, Flail 1–4 on a Fighter (need +1 / need −1 axes visible)", "Pixel 7 (GEAR-01, bulk): a Thief in Plate armor is refused the backstab ('Heavy armor negates…'); a Thief in Studded armor is not", "Pixel 7 (GEAR-01, store): the store's weapon offerings at any depth show a genuine heavy/neutral/light mix, not 'highest dice you can afford'", "Pixel 7 (GEAR-02, potion): drink a Speed potion from the Gear tab — the Oracle shows 'You use Speed potion.' then 'Double attacks for 50 squares.' and a Hasted chip counts down in the condition strip", "Pixel 7 (GEAR-02, cooldown): use a worn Cloak of Speed twice in a row — the second tap reads 'Cloak of Speed: 50 squares. It is not a vending machine.', the Gear-tab row shows 'cd 50 SQ' and a matching itemCooldown chip counts down", "Pixel 7 (GEAR-02, charges): a Magic User uses a Pine Staff in a fight, then again — the second use reads 'Pine Staff: 100 squares to the next charge. Patience is also a spell.' and the ITEMS submenu / staffCharges chip shows '0/1 · N SQ'", "Pixel 7 (GEAR-02, tolerant load): resume a pre-Phase-39 save mid-run — no card, no crash, worn cloaks/staves usable, any previously-active haste/invis/ether/acute effect reappears as a timed chip", "Pixel 7 (GEAR-05, ladder): buy a Ladder at a depth-2 store, walk into a climbable wall — a decision card appears BEFORE any roll with USE LADDER / CLIMB IT; USE LADDER passes with no roll, no damage, and the ladder is gone", "Pixel 7 (GEAR-05, rope): with a Rope, decline at a crevice (LEAP IT), fall, and confirm the retry card now offers USE ROPE beside LEAP IT; USE ROPE passes with no roll and no fall damage", "Pixel 7 (GEAR-05, torch): get Darkness while carrying a Torch — the card offers USE TORCH; using it lights the maze and a later Darkness within 40 squares is resisted", "Pixel 7 (GEAR-05, torch refusal): USE from the Gear tab on a Torch in a lit corridor reads 'It is not dark. Save the torch for when it is.' and the torch is not consumed", "Pixel 7 (GEAR-05, stock): a Torch/Rope line appears in a depth-1 store, Ladder only from depth 2, and none once carried", "Pixel 7 (GEAR-02, rows): Gear tab — a cloak/staff/potion each show READY / N SQ / cd N SQ / k/max · N SQ matching what the item claims", "Pixel 7 (GEAR-02, submenu): ITEMS submenu — a row on cooldown/recharging stays tappable and tapping it shows the engine's own named refusal in the fight log, never a silent no-op", "Pixel 7 (GEAR-02, chips): a live item effect, a cooling item and a recharging staff each show their own chip; tapping each names the item and what's counting (e.g. 'Cloak of Speed — cooling, 41 squares. Used, and not ready to be used again. Squares fix that.')", "Pixel 7 (a11y): TalkBack reads the new rail buttons (USE LADDER / USE ROPE / USE TORCH / CLIMB IT / LEAP IT) and the condition chips' labels/details", "Pixel 7 (voice): spot-check the hazard/dark card copy, the cooldown/recharging refusal lines and the chip explanations for the family-friendly deadpan tone in real play"]
gaps: []
---

# Phase 39 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-18)

Goal-backward check of the phase goal: *weapons and armor are reworked so store and loot present meaningful trade-offs instead of one best pick per class; every activatable magic item follows one model — use → effect for X → cooldown for Y; new one-shot tools (rope, ladder, torch) each answer exactly one hazard and are consumed on use.*

Second phase under the greenfield amendment; also carries the user's **once-a-day ruling** (2026-09-18: 100 squares = one day; every item usable at least once a day → effect + cd ≤ 100, staff recharge ≤ 100 per charge).

## Automated evidence (re-run by the orchestrator after 39-05)

- `npm test`: **2686/2686, 0 failures** (2563 at phase start → 2585 after 39-01 → 2594 after 39-02 → 2617 after 39-03 → 2664 after 39-04 → 2686 after 39-05). `npm run build:www` exit 0; `grep -c fonts.googleapis mazeworld.html` = 0; `package.json`/lock untouched (zero new deps).
- Engine gate: `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (unchanged); `git status --porcelain test/parity/fixtures` empty. Declared divergences: the economy fixture's Pickpocket-markup record re-declared with `declaredStockDiffs` (re-priced store, 39-01), its `every: 100 → 80` literal regenerated (once-a-day Cloak of Ether, 39-03) and its `stockAfter` extended with the two engine-only tool lines (39-04); three new harness carve-outs (`stripRetiredCounterFields`, `stripReauthoredEveryField`, `pendingHazard` strip) mirror the Phase 28/37 `stripCloakArmorTxt`/`stripWornField` precedents because the affected comparisons are live prototype-vs-engine runs, not stored literals. The one exposed `rollTreasureItem` fixture (chest, seed 2) was measured — the derived tool roll does not fire, no divergence. `chargen-rng-pin.test.js` green; `docs/class-pass/v15-before*.json` byte-identical to `8ab7ef6`.
- Cumulative `git diff --stat 8ab7ef6 -- engine content src mazeworld.html docs tools test/parity/fixtures` = 30 files, 8872+/335− (the bulk is `docs/class-pass/v15-gear-smoke.json`, the 429-run smoke pin). `mazeworld.html` touched by 39-05 only.
- Retired engine state: `c.haste`, `c.invis`, `c.ether`, `c.acute`, `c.flightLeft`, `c.flightCooldown`, potion writes to `c.might`, `it.usedAt`, staff `every: 250` — no engine read remains (grep-pinned); `foldLegacyCounters` gives old saves a tolerant load with no card.
- New event types (`itemEffectStarted/Faded`, `itemCooled`, `staffRecharged`, `hazardChoice`, tool/torch events, `useRefused` reasons `recharging`/`notDark`, `itemRejected.haveOne`) all have `EVENT_NARRATION` + `TOAST_FOR` + rail entries — coverage guards and voice scan green.
- Five plans, five SUMMARY.md files, all `## Self-Check: PASSED`; working tree clean at HEAD `f336ad9` apart from the two untracked user directories outside the phase.

## Success criteria → evidence

| # | Success criterion (ROADMAP) | Evidence |
|---|-----------------------------|----------|
| 1 | Store and loot tables show differentiated weapon/armor choices per class rather than a single best pick, recorded in a before/after ledger | `content/weapons.js` 24 rows with `need` (−2..+1, applied to the NEED — low-range convention) and `crit` (1\|2, five precise blades), re-diced/re-priced with key order + `cls` pinned so every `rng.pick`/`shuffle` is unchanged; `content/armors.js` `bulk` 0/0/1/1/2; `gear-axes.test.js` proves every store tier offers heavy/neutral/light for F and T; `expectedStrike` replaces max-damage in `weaponUpgradeDelta`/`lootCompare`/the bot; `docs/GEAR-BALANCE.md` before/after tables, per-class picks with live-computed expected strike, need-vs-die equivalence, 429-run smoke within noise of the v1.5 BEFORE pin. |
| 2 | Using an activatable magic item shows its effect-remaining, then its cooldown-remaining, as a condition chip until it's usable again | `content/activations.js` `ACTIVATION_OF` (20 entries: 6 cd rows, 8 staves with charges, 5 potions, Torch); `useItem` → `startEffect`/`startCooldown` on `c.timers['item:<key>']` / `'charges:<name>'`; `conditionsOf` emits effect chips with `remaining`/`source`, `itemCooldown` and `staffCharges` chips; `itemRowState` (READY / N SQ / cd N SQ / k/max · N SQ) drives Gear tab + ITEMS rows; chip tap explanations; `item-activation.test.js` (24) + `itemRowState.test.js` + `shell-gear-39.test.js`. Once-a-day rule applied (Amulet of Stone 100, Cloak of Invisibility 50/50, Cloak of Ether 20+80, Oak/Pine/Cedar recharge 100). |
| 3 | Facing a pit, an unclimbable wall, or a dark region, a player carrying rope/ladder/torch can consume it at that decision point to bypass or ease the hazard; each tool consumed exactly once | `state.pendingHazard` pre-roll pause (`hazardChoice`, zero draws, zero movement) when the matching tool is carried; `useTool { tool, dir }` consumes it and passes with no roll/no fall damage; a second `move` declines in place and today's roll runs; retry card carries the tool button; torch `light` clears `c.darkFor`, 40-square `lit` effect suppresses `fallDark`, `notDark` refusal keeps the torch; `tools.test.js`; shell cards USE LADDER/USE ROPE vs CLIMB IT/LEAP IT and USE TORCH on the dark card (`RAIL_COPY.hazard/dark`). |
| 4 | Rope, ladder, and torch appear as loot and store stock at depth-appropriate tiers | `content/tools.js` (Torch 25 / tier 0, Rope 60 / tier 0, Ladder 150 / tier 1); store lines appended after Rations as `giveTool` (`ENGINE_ONLY_STORE_EFFECTS`, offered only when not carried); loot row in `rollTreasureItem` on a derived rng stream (`derivedRng(rng.getState(), "tool", depth)`, zero main draws — 200 seeds × 2 depths proven byte-identical on the no-fire path); duplicates refused `itemRejected { reason: "haveOne" }` per the Lockpicks precedent. |

## Requirements

GEAR-01 ✓ · GEAR-02 ✓ · GEAR-05 ✓ — automated halves verified; on-device halves in the frontmatter `human_verification` list (17 items, de-duplicated from the 20-item aggregated checklist in 39-05-SUMMARY.md — Plan 05's items 17/18 restate Plan 04's 8/9 and item 16 restates Plan 01's 1).

## Accepted planner/executor deviations (recorded in plan/SUMMARY frontmatter)

- Planner corrections to CONTEXT, verified in code: Soldier's 1–2 crit line is the FOE's crit against a Soldier (hero Soldier never crits) — weapon crit implemented as `roll <= weaponCrit(c) && !noCrit`; rolled staves already carried `every: 250` (charge pools are a mild buff, not a fix for "infinite"); members never activate items (no member item timers).
- 39-03 committed Tasks 1+2 as one `feat` commit — no green intermediate state existed without a dual-path bridge the ruling forbids.
- 39-04 guarded the derived tool roll on `typeof rng.getState === "function"` so pre-existing bare test-double rngs are unaffected (production callers always thread a real `makeRng`); re-measured one `FULL_FIGHTS` draw-count pin (a real kill now drops a Torch).
- 39-01/39-05 re-pinned a dozen out-of-scope test source pins that broke as direct fallout (shared import lines, retired `it.every`/`usedAt` expressions, the `stepWith` pending shape).

## Follow-ups noted (not gaps)

- Every Magic-User-legal weapon costs ≤ 250, so a Magic User's store roll never reaches tier 3's cost floor (falls back to the tier-0 list) — recorded in `docs/GEAR-BALANCE.md`; revisit with Phase 42's tuning pass or a later rebanding.
- Phase 42's bot must learn *when* to pop items (`useItem` timing) before the ONE AFTER matrix; it already buys/equips under the new axes and answers a `pendingHazard` with a carried tool.
- 39-01 human item 3 (store mix visible on the store screen) assumes need/crit are legible in the store rows — check on device; if not, a small store-row copy pass belongs to the shell cleanup milestone.
