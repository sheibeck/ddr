---
phase: 39-gear-magic-items-one-shot-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/weapons.js
  - content/armors.js
  - engine/derived.js
  - engine/combat.js
  - engine/movement.js
  - engine/items.js
  - src/browser/viewModels.js
  - test/unit/gear-axes.test.js
  - test/unit/economy.test.js
  - test/unit/store-roll.test.js
  - test/unit/items.test.js
  - test/unit/characterSheetViewModel.test.js
  - test/parity/harness/comparables.js
  - test/parity/economy-parity.test.js
  - test/parity/fixtures/action-script.economy.json
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixtures/action-script.schema.md
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements: [GEAR-01]

must_haves:
  truths:
    - "Every WEAPONS row carries `need` (-2|-1|0|1) and `crit` (1|2); every ARMORS row carries `bulk` (0|1|2); the 24 weapon keys keep their pre-phase order and `cls` strings byte-for-byte so every rng.pick/rng.shuffle over WEAPONS keys is unchanged"
    - "A hero's strike need is `toHit(state)` = class/race/sub need + weapon `need` (floored at 1) — a light weapon raises the number a hit lands on, a heavy one lowers it; no (class, legal weapon) pair yields a base need below 2"
    - "A precise blade (Rapier, Katana, Wakazashi, Ninja-to, Dagger) crits on a roll of 1 or 2; every other weapon crits on 1 only; the Guard/Soldier/dark/Cloak-of-Strength noCrit rule is unchanged"
    - "Armor bulk is added to the climb and leap roll comparison, subtracted from the flee roll, and replaces the hard-coded Plate name checks in the Thief Stealth/backstab gates (`armorBulk(c) >= 2`)"
    - "`weaponUpgradeDelta`/`lootCompare` compare weapons by expected damage per swing under the new need/crit axes, not by max damage"
    - "For every store tier and every class letter F/T, `storeWeaponPool` contains at least one heavy (need < 0), one neutral (need 0) and one light (need > 0) weapon; for M it does at tiers 0, 1 and 3 (tier 2 is the Magic User's two-pick ceiling)"
    - "Every parity fixture the table change moves is DECLARED (before/after measured live) and regenerated; every other fixture is byte-identical; `prototype-master.js.txt` is untouched"
  artifacts:
    - "content/weapons.js — 24 rows with need/crit, re-priced, WEAPON_MAX recomputed"
    - "content/armors.js — 5 rows with bulk"
    - "engine/derived.js — classNeed(c), weaponNeedMod(c|base), weaponCrit(c), armorBulk(c), expectedStrike(c, base, bonus, prof)"
    - "test/unit/gear-axes.test.js — the axes contract suite"
    - "test/parity/FIXTURE-INVENTORY.md — '## Phase 39: gear axes — declared divergences' section"
  key_links:
    - "engine/combat.js#playerStrike reads toHit(state) (now including weaponNeedMod) and `roll <= weaponCrit(c) && !noCrit` for the crit"
    - "engine/items.js#weaponUpgradeDelta -> engine/derived.js#expectedStrike (the ONE 'is this weapon better' rule takeItem, lootCompare and the bot read)"
    - "test/parity/economy-parity.test.js -> comparables.js#declaredStockDiffs (the new store-roll pin replacing stockCostMul)"
---

<objective>
Land GEAR-01's mechanical half: weapons get a to-hit axis (`need`) and a crit axis (`crit`), armor gets a `bulk` axis, the 24 weapon rows are re-diced and re-priced so each store band offers a heavy / neutral / precise pick per class, the engine reads the new axes everywhere the strike, climb, leap, flee and Thief-stealth rules live, and the "is this weapon better" heuristic becomes expected-damage-per-swing. Every parity fixture the change moves is measured, declared and regenerated — nothing else is touched.

Purpose: "highest dice you can afford" stops being the only decision. This plan is the engine truth Plan 02's bot policy, ledger and smoke measure against.

Output: new content axes + engine reads + a contract test suite + declared fixture divergences. No shell (`mazeworld.html`) edits in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-CONTEXT.md
@docs/GEAR-SLOTS.md
@test/parity/FIXTURE-INVENTORY.md
@test/parity/fixtures/action-script.schema.md
</context>

<standing_rules>
- Greenfield ruling (CONTEXT "Standing rulings"): no dual paths, no fixture-motivated hedges. A fixture the change moves is DECLARED and regenerated; `test/parity/prototype-master.js.txt` is never edited (hash must stay `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- Commit convention: `feat(39-01): …` / `test(39-01): …` / `docs(39-01): …`; end every commit message with the two attribution lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
- Stage with explicit paths only (`git add <file> …`); never stage `store-listing/` or `tools/store-screenshots/` (untracked user directories, not part of any phase).
- Deferred UAT: no device step, no `adb`, no APK build, no pause. The SUMMARY carries a "## Human verification (deferred to end of run)" section listing what a Pixel 7 tester would check for this plan.
- No `mazeworld.html` edits in this plan (the shell is Plan 05's).
</standing_rules>

<artifacts_this_phase_produces>
Phase-wide symbol inventory (P = plan that creates it). This plan creates the P01 entries.

Content: `WEAPONS[*].need`, `WEAPONS[*].crit` (P01) · `ARMORS[*].bulk` (P01) · `content/treasure-tables.js` `act` on every JEWELRY/CLOAKS/STAVES row + `TREASURE_ACTIVATION_OF` (P03) · `content/potions.js` `act` on Speed/Strength/Enlarge/Acuteness/Invisible + `POTION_ACTIVATION_OF` (P03) · `content/activations.js` `ACTIVATION_OF` (P03) · `content/tools.js` `TOOLS`, `TOOL_ORDER` (P04).
Engine derived (`engine/derived.js`): `classNeed(c)`, `weaponNeedMod(baseOrC)`, `weaponCrit(c)`, `armorBulk(c)`, `expectedStrike(c, base, bonus, prof)` (P01) · `itemEffectActive(c, kind)`, `potionMight(c)`, `itemTimerId(it)`, `activationFor(it)`, `hasTool(c, tool)`, `conditionsOf` descriptors `itemCooldown`/`staffCharges`/`lit` (P03/P04).
Engine items (`engine/items.js`): `itemReady(state, it)` timer-backed (P03) · `useItem` activation apply (P03) · `narrateTimerTransitions(state, transitions, events)` (P03) · tool loot row in `rollTreasureItem` via `derivedRng` (P04) · `takeItem` duplicate-tool refusal (P04).
Engine movement (`engine/movement.js`): `state.pendingHazard` pre-roll pending state, `move(state, dir, rng, events, now, opts)` with `opts.tool`, `useTool(state, tool, dir, rng, events, now)` (P04).
Engine actions: `useTool { tool, dir }` in `ACTION_TYPES`/`validateAction`/`applyAction` (P04).
Engine save: `foldLegacyCounters(c, steps)` in `engine/saveState.js` (P03).
Events: `itemEffectStarted { item, kind, left, cadence }`, `itemEffectFaded { item, kind }`, `itemCooled { item }`, `staffRecharged { item, charges, max }`, `useRefused` reasons `recharging`/`notDark` (P03) · `hazardChoice { feat, dir, tool }`, `toolUsed { tool, feat }`, `toolRefused { tool, reason }`, `torchLit { left }`, `darknessResisted { by }` (P04).
Parity harness: `stripRetiredCounterFields` (P03), `pendingHazard` top-level strip (P04), `declaredStockDiffs` + fixture record keys `stockAfter`/`stockNames` (P01).
Shell/browser: `src/browser/viewModels.js#itemRowState(state, it)` + `ITEM_STATE_COPY` (P05), `combatMenu.js` ITEMS rows on `itemRowState` (P05), `rail.js` `RAIL_COPY.hazard`/`RAIL_COPY.dark` + `RAIL_FAMILY` entries (P05), `mazeworld.html` hazard decision card, retry-card tool button, dark-card USE TORCH, Gear-tab row states, `window.__mzToHit`/`__mzStrikeDie`/`__mzHasTool`/`__mzItemRowState` bridges (P05).
Docs: `docs/GEAR-BALANCE.md` (P02 creates; P03/P04/P05 append), `docs/class-pass/v15-gear-smoke.json` (P02).
Tests (new files): `test/unit/gear-axes.test.js` (P01), `test/unit/bot-buy-policy.test.js` (P02), `test/unit/item-activation.test.js` (P03), `test/unit/tools.test.js` (P04), `test/unit/shell-gear-39.test.js` (P05).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Weapon need/crit axes, armor bulk, re-diced/re-priced tables, contract tests</name>
  <files>content/weapons.js, content/armors.js, test/unit/gear-axes.test.js</files>
  <read_first>
    - content/weapons.js (the 24 rows, WEAPON_MAX, the halve flag convention)
    - content/armors.js (5 rows, MAGIC_ARMOR_TABLE)
    - content/store-stock.js (STORE_WEAPON_BANDS 0-250 / 100-500 / 200-700 / 400-900; STORE_ARMOR_CAP)
    - content/kit.js (KIT starting weapons — every KIT weapon name must remain a WEAPONS key)
    - content/classes.js (CLASSES[*].toHit: Magic User 3, Thief 4, Fighter 5)
    - engine/economy.js lines 249-256 (storeWeaponPool — the band/fallback rule the coverage test exercises)
    - test/unit/store-roll.test.js lines 148-175 (the existing pool pins, including "every M weapon costs <= 250")
    - test/determinism/content-is-pure-data.test.js (content must stay function-free)
  </read_first>
  <behavior>
    - Test 1 (key-order pin): `Object.keys(WEAPONS)` deep-equals the exact pre-phase 24-key list in order: Axe, Bastard Sword, Battle Axe, Broadsword, Claymore, Dagger, Katana, Kopesh Sword, Long Sword, Ninja-to, Rapier, Short Sword, Wakazashi, Club, Flail, Mace, Morning Star, Quarter Staff, Spiked Staff, Whip, Awl Pike, Bardiche, Naganita, Spear; and each row's `cls` equals its pre-phase string (Axe FTM, Bastard Sword F, Battle Axe F, Broadsword F, Claymore F, Dagger FTM, Katana FT, Kopesh Sword F, Long Sword FT, Ninja-to FT, Rapier FTM, Short Sword FTM, Wakazashi FT, Club FTM, Flail FT, Mace FT, Morning Star FT, Quarter Staff FTM, Spiked Staff FTM, Whip FT, Awl Pike FT, Bardiche F, Naganita F, Spear FTM).
    - Test 2 (axes present): every row has integer `need` in {-2,-1,0,1} and `crit` in {1,2}; exactly Rapier, Katana, Wakazashi, Ninja-to, Dagger have `crit: 2`; exactly Bardiche has `need: -2` and its `cls` is "F".
    - Test 3 (floor guarantee): for every class in CLASSES and every weapon whose `cls` includes that class's letter (F/T/M), `CLASSES[cls].toHit + WEAPONS[w].need >= 2`.
    - Test 4 (WEAPON_MAX consistency): for every weapon, `WEAPON_MAX[w] === (halve ? Math.ceil((n*sides+bonus)/2) : n*sides+bonus)`.
    - Test 5 (band role coverage): for tier 0..3 and letter F and T, `storeWeaponPool(letter, tier)` has ≥1 weapon with need<0, ≥1 with need 0, ≥1 with need>0; for M at tiers 0, 1, 3 the same; at M tier 2 at least two distinct need values.
    - Test 6 (every KIT weapon exists): every `KIT[sub][0]` is a WEAPONS key.
    - Test 7 (armor bulk): ARMORS names/order unchanged (Cloth, Leather, Studded, Mail, Plate) with `bulk` 0,0,1,1,2 respectively; `ar`/`wp`/`min`/`cls`/`cost` byte-identical to pre-phase values (Cloth 300/12/3/FTM/1; Leather 500/15/6/FT/1; Studded 750/18/10/FT/2; Mail 1000/30/12/F/3; Plate 2000/45/15/F/4).
    - Test 8 (Magic User ceiling): every M-legal weapon still costs <= 250 (keeps store-roll.test.js's fallback pin true).
  </behavior>
  <action>
    Write `test/unit/gear-axes.test.js` FIRST with the eight behaviours above (RED), then rewrite `content/weapons.js` and `content/armors.js`.

    WEAPONS — keep every key, key ORDER and `cls` string exactly as today (this is load-bearing: `engine/economy.js#openStore` shuffles `Object.keys(WEAPONS).filter(cls)` and `engine/items.js#rollBlade` does `rng.pick(Object.keys(WEAPONS))`, so any reorder/rename/add/remove would move the store roll and the premium blade on every fixture — the axes and prices are the change, not the roster). Set each row's `dice`/`lab`/`cost`/`need`/`crit` to exactly (mean in brackets is documentation for the ledger, not a field):
    - Cutting: Axe d6 [3.5] cost 50 need 0 crit 1 · Bastard Sword 2d8+1 [10] 650 need -1 crit 1 · Battle Axe 2d6+1 [8] 325 need -1 crit 1 · Broadsword d10+2 [7.5] 550 need 0 crit 1 · Claymore d12+2 [8.5] 800 need 0 crit 1 · Dagger d6 halve [2] 75 need +1 crit 2 · Katana d10+1 [6.5] 650 need +1 crit 2 · Kopesh Sword d12+3 [9.5] 500 need -1 crit 1 · Long Sword d8+2 [6.5] 400 need 0 crit 1 · Ninja-to d8+1 [5.5] 475 need +1 crit 2 · Rapier d6 [3.5] 200 need +1 crit 2 · Short Sword d6+2 [5.5] 250 need 0 crit 1 · Wakazashi d6+1 [4.5] 350 need +1 crit 2.
    - Bludgeoning: Club d6 25 need 0 crit 1 · Flail d10+2 [7.5] 250 need -1 crit 1 · Mace d8+1 [5.5] 125 need -1 crit 1 · Morning Star d8+2 [6.5] 175 need -1 crit 1 · Quarter Staff d6 25 need 0 crit 1 · Spiked Staff d8 [4.5] 100 need -1 crit 1 · Whip d6 halve [2] 35 need +1 crit 1 (the one light weapon without the precise crit).
    - Pole arms: Awl Pike 2d6+2 [9] 400 need -1 crit 1 · Bardiche 2d10+2 [13] 900 need -2 crit 1 (the single -2 weapon, Fighter-only) · Naganita 2d8+2 [11] 750 need -1 crit 1 · Spear d8 [4.5] 150 need 0 crit 1.
    `lab` strings follow the existing convention ("d6", "2d6+1", "d10+2", "d6/2" for halve). Recompute `WEAPON_MAX` from the new dice (halve → ceil(max/2): Dagger 3, Whip 3). Update the file's header comment to name the two new axes and the Phase 39 ruling: to-hit is a LOW range (a strike lands on `roll <= need`), so a light weapon's bonus is `need + 1` and a heavy weapon's penalty is `need - 1` — the user's "+ to hit as a penalty" is the same arithmetic read from the die's side; the Phase 31 `afraidNeed` convention (penalties shrink the need) is the precedent.

    ARMORS — add `bulk` to each row (Cloth 0, Leather 0, Studded 1, Mail 1, Plate 2); change nothing else (AR/WP are the soak axis per CONTEXT Area 3; costs stay so the faerie fixture's Warded leather and the economy fixture's Studded line keep their values). Header comment: bulk is added to the climb/leap roll comparison, subtracted from the flee roll, and `bulk >= 2` replaces the old hard-coded Plate name checks in the Thief stealth gates (Plan 01 Task 2).

    Run the new test file (GREEN), then `node --test test/unit/store-roll.test.js` — its pool/fallback pins must still pass with the new prices (if the "M tier 3 fallback equals every M-legal weapon" pin fails, the prices are wrong, not the pin).
  </action>
  <verify>
    <automated>node --test test/unit/gear-axes.test.js test/unit/store-roll.test.js test/determinism/content-is-pure-data.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-axes.test.js` prints `# fail 0` and `# pass 8` or more.
    - `node -e "import('./content/weapons.js').then(m=>console.log(Object.keys(m.WEAPONS).length, Object.values(m.WEAPONS).filter(w=>w.crit===2).length, Object.values(m.WEAPONS).filter(w=>w.need===-2).length))"` prints `24 5 1`.
    - `grep -c "bulk:" content/armors.js` prints 5.
    - `node --test test/determinism/content-is-pure-data.test.js` passes (no functions in content).
    - `git diff --stat -- content/store-stock.js content/kit.js` is empty (bands and kits untouched).
  </acceptance_criteria>
  <done>The two content tables carry the new axes with the exact numbers above, WEAPON_MAX matches the dice, the key order/cls strings are pinned by test, and the store-roll pool pins still hold.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine reads — toHit need mod, crit range, armor bulk in climb/leap/flee/stealth, expected-strike upgrade heuristic</name>
  <files>engine/derived.js, engine/combat.js, engine/movement.js, engine/items.js, src/browser/viewModels.js, test/unit/gear-axes.test.js, test/unit/items.test.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - engine/derived.js lines 455-560 (strikeDie, inDark, toHit, afraidNeed — the need-modifier convention) and 693-726 (weaponDamage's flat terms: level², prof, magicWpn, race dmg/wpnBonus, might, Heft, Master of Arms, eff dmg, 2*size, Guard, Sorcerer cap)
    - engine/combat.js lines 525-680 (playerStrike: `roll <= need`, the crit line `roll === 1 && !noCrit`, the `heavy` list and the Stealth `c.armor !== "Plate"` gate) and 1655-1680 (memberStrike's copy of the heavy list), 912-925 (flee's `roll + bonus >= 11`)
    - engine/movement.js lines 135-210 (climb/leap block: `r = rng.d(10) + hPenalty` / `+ wPenalty`)
    - engine/items.js lines 186-270 (canEquipWeapon, weaponUpgradeDelta, armorUpgradeDelta) and 319-345 (takeItem's weapon branch reading weaponUpgradeDelta)
    - src/browser/viewModels.js lines 36-60 (damageBracket) and 175-200 (lootCompare's weapon branch and its `+${delta} damage` line)
    - test/unit/items.test.js (existing weaponUpgradeDelta/takeItem pins — some assert max-damage semantics and must be re-pinned to expected-strike)
    - test/unit/characterSheetViewModel.test.js and test/unit/lootCompare*.test.js if present (`ls test/unit | grep -i loot`) for the lootCompare line pins
  </read_first>
  <behavior>
    - `classNeed(c)`: Fighter 5, Thief 4, Magic User 3; Elven max(…,5); Acrobat 5; Cleric max(…,4) — pure, no state.
    - `weaponNeedMod(x)`: accepts a weapon base name or a character (`x.weapon`); returns `WEAPONS[base].need` or 0 for Fists/unknown.
    - `weaponCrit(c)`: `WEAPONS[c.weapon].crit` or 1.
    - `armorBulk(c)`: `ARMORS.find(a => a.name === c.armor)?.bulk ?? 0` — "Nothing" and a warded piece resolve by `c.armor` name (a Warded leather sets `c.armor = "Leather"` via takeItem, bulk 0).
    - `toHit(state)`: for a Fighter with a Rapier the value is 6; with a Flail 4; with a Bardiche 3; a Magic User with a Spiked Staff 2 (never below 1 — `Math.max(1, …)` after the weapon term); the dark clamp `Math.min(h, 2)` still applies after; Afraid still applies last in playerStrike.
    - Crit: with a stubbed rng making the strike roll 2, a Thief with a Dagger (not opening, not Ninja/Cutthroat) gets `struck.crit === true`; the same roll with an Axe gets `crit === false`; a Guard with a Dagger never crits (noCrit); a roll of 1 crits with any weapon except under noCrit.
    - Stealth/backstab gates: a Thief in Studded (bulk 1) still backstabs (identical to today, since the old list's "Studded Leather"/"Chain Mail" names never matched any ARMORS row); a Thief in Plate (bulk 2) is denied (`backstabDenied`), same as today.
    - Flee: `fleeRolled` carries `bulk`; the success test is `roll + bonus - bulk >= 11` (a Fighter in Plate needs a natural 13).
    - Climb/leap: `r = rng.d(10) + hPenalty + armorBulk(c)` / `rng.d(10) + wPenalty + armorBulk(c)` — with a stubbed d10 a Plate wearer fails a comparison a Leather wearer passes.
    - `expectedStrike(c, base, bonus, prof)`: `(hitP + critP) * Math.max(1, flat + avg + bonus + prof)` where need = clamp(classNeed(c) + WEAPONS[base].need, 1, ∞), dieN = strikeDie(c), hitP = min(1, need/dieN), critP = (c.sub is Guard or Soldier or eff(c,"noCrit")>0) ? 0 : min(hitP, WEAPONS[base].crit/dieN), avg = mean of the weapon dice (halve → mean of ceil(d/2) over d=1..sides, i.e. 2 for d6), flat = weaponDamage's additive terms minus the dice, prof and might (level², race dmg/wpnBonus, Heft +2, Master of Arms +2, eff dmg, 2*eff size, Guard `-(4-level)` under level 4). Sample pin: a level-1 Human Fighter (flat = 1, no race mods, prof 0, magicWpn 0, d20 strike die): Rapier (need 6, crit 2) = (6/20 + 2/20) × (1 + 3.5) = 1.80; Flail (need 4) = (4/20 + 1/20) × (1 + 7.5) = 2.125; Short Sword (need 5) = (5/20 + 1/20) × (1 + 5.5) = 1.95 (assert each ±0.01) — the three tier-0 picks land within 20% of each other at level 1, which is the whole point.
    - `weaponUpgradeDelta(c, it)` = `expectedStrike(c, it.base, it.bonus||0, 0) - expectedStrike(c, c.weapon, c.magicWpn||0, c.prof||0)`, rounded to 2 decimals; `takeItem` still refuses `notBetter` on `<= 0`.
    - `lootCompare(c, weaponItem).line` reads `+{delta} a swing` (one decimal) for an upgrade and `not an upgrade` otherwise.
  </behavior>
  <action>
    Append the engine/derived/items behaviours above to `test/unit/gear-axes.test.js` (plus re-pins in `test/unit/items.test.js` for weaponUpgradeDelta/takeItem where they assert max-damage arithmetic, and in the lootCompare test file for the line copy) — RED first.

    engine/derived.js: factor the class/race/sub part of `toHit` into an exported `classNeed(c)` (byte-identical arithmetic to today's first four lines); add exported `weaponNeedMod`, `weaponCrit`, `armorBulk`, `expectedStrike` as specified. `toHit(state)` becomes: `let h = classNeed(c); if (inspired) h += …; h += eff(c,"toHit"); h += weaponNeedMod(c); h = Math.max(1, h); dazed clamp; dark clamp; return h` — keep the dazed/dark lines exactly where they are. Add a `weaponDiceMean(w)` module-private helper (halve-aware) for expectedStrike. Document in the JSDoc: penalties shrink the need (Phase 31 `afraidNeed` precedent); the ±1 magnitude ruling; the -2 Bardiche is Fighter-only so no class's base need drops below 2 (proved by gear-axes.test.js).

    engine/combat.js playerStrike: replace the crit line with `let crit = roll <= weaponCrit(c) && !noCrit;` (import weaponCrit/armorBulk from derived). Replace the hero `heavy` list expression with `c.cls === "Thief" && armorBulk(c) >= 2` and the Stealth clause's `c.armor !== "Plate"` with `armorBulk(c) < 2`; replace memberStrike's copied `heavy` list with `view.cls === "Thief" && armorBulk(view) >= 2` (note in the comment that the old list's two non-Plate names never matched an ARMORS row, so behaviour is identical for every armor that exists). Flee: compute `const bulk = armorBulk(c);`, push `fleeRolled { roll, bonus, bulk, need: 11 }`, test `roll + bonus - bulk >= 11`. Do NOT touch the foe-side `roll <= 2 && c.sub === "Soldier"` crit lines (841/2374) — those are the foe's crit against a Soldier (the sub's bad), not the hero's crit; record this correction of CONTEXT's "Soldier 1–2 stacks" note in the SUMMARY (the hero's crit for a Soldier stays suppressed by noCrit, unchanged).

    engine/movement.js: add `+ armorBulk(state.c)` to both `r` computations (climb per-10ft loop and the leap), import armorBulk. Zero new draws.

    engine/items.js: rewrite `weaponUpgradeDelta` on `expectedStrike` (import from derived), JSDoc naming it the ONE rule takeItem, lootCompare and the tuning bot share; `armorUpgradeDelta` unchanged.

    src/browser/viewModels.js lootCompare weapon branch: `line = upgrade ? \`+${delta.toFixed(1)} a swing\` : "not an upgrade"` (keep the refusal branch). Re-pin its tests.

    Do not edit `foeToHitVs`/`foeToHitBreakdown` (foe-side needs are not a weapon axis).
  </action>
  <verify>
    <automated>node --test test/unit/gear-axes.test.js test/unit/items.test.js test/unit/combat.test.js test/unit/movement.test.js test/unit/characterSheetViewModel.test.js test/unit/identity-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function classNeed\|export function weaponNeedMod\|export function weaponCrit\|export function armorBulk\|export function expectedStrike" engine/derived.js` prints 5.
    - `grep -c 'roll <= weaponCrit(c) && !noCrit' engine/combat.js` prints 1; `grep -c 'armorBulk(' engine/combat.js` prints at least 4 (hero heavy, Stealth clause, member heavy, flee).
    - `grep -c '"Studded Leather"' engine/combat.js` prints 0. <!-- planner-discipline-allow: "Studded Leather" -->
    - `grep -c 'armorBulk(state.c)' engine/movement.js` prints 2.
    - `grep -c 'expectedStrike' engine/items.js` is at least 2 (import + use).
    - `node --test test/unit/identity-contract.test.js` passes (Soldier's foe-crit bad and Guard's never-crits bad unchanged).
    - `node --test test/unit/gear-axes.test.js` `# fail 0`.
  </acceptance_criteria>
  <done>The engine reads need/crit/bulk at every site CONTEXT names, toHit floors at 1, the crit range is weapon-driven, the two Thief gates read bulk, flee/climb/leap pay bulk, and the upgrade heuristic + loot compare read expected strike.</done>
</task>

<task type="auto">
  <name>Task 3: Measure, declare and regenerate the fixtures the axes move; harness stock pin; FIXTURE-INVENTORY; plan gate</name>
  <files>test/parity/harness/comparables.js, test/parity/economy-parity.test.js, test/parity/fixtures/action-script.economy.json, test/parity/fixtures/action-script.combat.json, test/parity/fixtures/action-script.schema.md, test/parity/FIXTURE-INVENTORY.md, test/unit/economy.test.js, test/unit/store-roll.test.js</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md sections "Phase 31" (lines 659-724) and "Phase 38" (725-800) — the declared-divergence table format to copy
    - test/parity/fixtures/action-script.schema.md (the `divergence` / `kind: "action-path"` record shape, `stockCostMul`)
    - test/parity/harness/comparables.js lines 595-745 (actionPathDivergenceOf, skipsByteDiffAt, declaredEndDiffs, stockMarkupDiff) and 829-900 (economyComparable, runEconomyAction)
    - test/parity/economy-parity.test.js lines 55-125 (how `stockCostMul` gates checkStockMarkup and the `buyFailed` assertion)
    - test/parity/fixtures/action-script.economy.json (the existing Phase 24 action-path record: fromAction 0, stockCostMul 1.25, before/after gold/weapon/items)
    - test/parity/fixtures/action-script.combat.json (scenarios win/lose/lose-apprentice/lose-plain/flee/parley and their existing records)
    - test/unit/economy.test.js lines 260-295 (the seed-3 stock pin: Katana 656 / Axe 63 / Studded 938 / Casket 3750 and the flat-store 525/50 pins)
    - test/unit/store-roll.test.js lines 200-215 (Casket 3750 pin)
    - .planning/phases/38-melee-active-abilities/38-05-SUMMARY.md "## Human verification (deferred to end of run)" (the SUMMARY section format)
  </read_first>
  <action>
    1. MEASURE. Run `npm test` and list every failing parity test. Expected: the economy fixture (seed 3) fails at action 0 (`checkStockMarkup`: Katana's engine cost is 650×1.25 = 813, not 656) — names and order of the store lines are byte-identical to the prototype because Task 1 pinned the WEAPONS key order and cls strings. Possibly `lose` (seed 14, Soldier with a Long Sword whose dice moved d8→d8+2) via its end-state `after` pins, and `win` (seed 3, Thief Dagger: need 4→5, crit 2 — its opening backstab already crits and hits, so it is expected to stay byte-identical; confirm). `lose-plain` (seed 1119, Short Sword d6+1→d6+2) only ever misses, so it is expected byte-identical; confirm. `flee`/`parley`/magic/encounters/chargen/movement must be untouched. Write a scratch replay script in the session scratchpad (NOT committed) that, for each moved scenario, prints the record's `fields`/`stateFields` from `loadPrototypeSandbox` (before) and the engine (after) — the same replay shape `test/parity/harness/comparables.js`'s runners use — so every declared value is measured, never hand-typed.

    2. DECLARE the economy fixture. In `action-script.economy.json`'s top-level `divergence` record: keep `kind: "action-path"`, `fromAction: 0`, `fields: ["gold","weapon","items"]`, keep `before` byte-identical (prototype); REMOVE `stockCostMul`; ADD `stockNames` (the prototype's action-0 stock `n` list in order, measured) and `stockAfter` (the engine's action-0 `[n, cost]` pairs in the same order, measured); set `phase: "24+39"`, append GEAR-01 to `requirements`, regenerate `after` from the measured engine end state, and extend `rationale` with one paragraph: the store ROLL (names/order/subs) is byte-identical because the WEAPONS key order and class letters are pinned; every weapon line's cost and label now follow the re-priced table (Katana 650, Axe 50, Broadsword 550 for the Casket premium) on top of the Pickpocket ×1.25; the purchase path from action 1 changes accordingly. Add to comparables.js an exported `declaredStockDiffs(protoStore, engineStore, record)` returning `{ names, after }` — `names`: diffState of the prototype's stock names vs `record.stockNames` AND of the engine's stock names vs the same list (both must be null); `after`: diffState of the engine's `[n, cost]` pairs vs `record.stockAfter`. In economy-parity.test.js replace the `stockCostMul` branch with: `if (pathDiv?.stockAfter && action.type === "openStore") { const d = declaredStockDiffs(ctx.S.store, engineState.store, pathDiv); assert.equal(d.names, null, …); assert.equal(d.after, null, …); }` (keep `checkStockMarkup` exported and its branch for a record that still carries `stockCostMul`, so the helper's contract is unchanged for any future record). If the engine path no longer produces a `buyFailed` event, make that assertion conditional on `!pathDiv?.stockAfter` and say why in a comment (the actions array is never edited). Document the two new record keys in `action-script.schema.md` beside `stockCostMul`.

    3. DECLARE any combat scenario that moved. `lose`/`lose-apprentice`/`parley` already carry `fromAction: 0` action-path records — if their measured `after` values changed, update ONLY `after`/`stateAfter` and append a Phase 39 sentence to `rationale` (weapon dice/need change named). If `win` or `lose-plain` moved (not expected), add a new `kind: "action-path"` record with `fromAction: 0`, measured before/after, `phase: 39`, `requirements: ["GEAR-01"]`. A scenario that did not move gets NO record and NO edit.

    4. RE-PIN unit tests: `test/unit/economy.test.js`'s seed-3 stock pin (Katana 813 / Axe 63 / Studded 938 / Casket price from Broadsword 550 / flat Katana 650, Axe 50 — measured, then written) and `test/unit/store-roll.test.js`'s Casket pin. Run `node tools/fixture-inventory.mjs` and confirm the generated roster block is unchanged (no encounter draw moved); if it differs, stop and investigate — a roster change means a draw moved that should not have.

    5. FIXTURE-INVENTORY.md: append `## Phase 39: gear axes (GEAR-01) — declared divergences` with (a) the design statement (need/crit/bulk, key order pinned, prices/dice moved), (b) a table `| Fixture | Scenario / seed | Hero | Weapon | What moved | before → after |` with one row per declared record, (c) a "### Byte-identical elsewhere (Phase 39, GEAR-01)" paragraph naming every scenario that did not move and why (chargen never reads a weapon's dice; movement seed 256 never reaches a climb/leap tile; flee/parley never strike; faerie's `rng.pick(ARMORS)` is unchanged because ARMORS keeps 5 rows in order). Leave placeholder anchors `<!-- 39-03 -->` and `<!-- 39-04 -->` at the end for the later plans' sections.

    6. PLAN GATE (verification agents are off — this task is the gate): `npm test` → `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` → `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` lists ONLY the fixture files this task deliberately edited; `git diff --stat -- mazeworld.html` empty; `npm run build:www` exit 0 (src/browser/viewModels.js changed in Task 2). Record every number in the SUMMARY.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`.
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `grep -c '"stockAfter"' test/parity/fixtures/action-script.economy.json` prints 1 and `grep -c '"stockCostMul"' test/parity/fixtures/action-script.economy.json` prints 0. <!-- planner-discipline-allow: "stockCostMul" -->
    - `grep -c "export function declaredStockDiffs" test/parity/harness/comparables.js` prints 1.
    - `grep -c "## Phase 39: gear axes" test/parity/FIXTURE-INVENTORY.md` prints 1.
    - `node tools/fixture-inventory.mjs | diff - <(sed -n '/fixture-inventory:generated:begin/,/fixture-inventory:generated:end/p' test/parity/FIXTURE-INVENTORY.md | sed '1d;$d')` is empty (roster unchanged) — or the executor records the exact diff and the reason in the SUMMARY if the tool's output framing differs; the ROSTER rows must be identical.
    - `git diff --stat -- mazeworld.html` is empty.
    - `npm run build:www` exits 0.
    - Every edited fixture record has `before` values byte-identical to the pre-task file (only `after`/`stateAfter`/`stockNames`/`stockAfter`/`phase`/`requirements`/`rationale` change).
  </acceptance_criteria>
  <done>The suite is green with every moved fixture declared and measured, the economy fixture's store pin is the stronger `stockNames`/`stockAfter` check, FIXTURE-INVENTORY carries the Phase 39 GEAR-01 section, and the master file/hash is untouched.</done>
</task>

</tasks>

<fixture_divergence_protocol>
Applies to Task 3 (and is referenced by Plans 03/04): (1) run the suite and let the harness tell you which scenarios move — never reason from seeds; (2) for each moved scenario, measure `before` from `loadPrototypeSandbox({seed})` and `after` from the engine replay with a scratch script, never hand-type; (3) edit only that scenario's record (`after`/`stateAfter`, new record if none existed), never `seed`/`actions`/`scenarios` arrays, never `before`; (4) one FIXTURE-INVENTORY section per plan with a rationale row per record and a "byte-identical elsewhere" paragraph; (5) `prototype-master.js.txt` is never touched.
</fixture_divergence_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content tables → engine | authored data (need/crit/bulk) consumed by arithmetic; a bad value could push a need below 1 or a crit above the die |
| save JSON → engine | `c.weapon`/`c.armor` names from a save resolve through WEAPONS/ARMORS lookups |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-01 | Tampering | `toHit` need arithmetic | medium | mitigate | `Math.max(1, …)` floor after the weapon term; gear-axes.test.js proves no legal (class, weapon) base need < 2 and every `need` ∈ {-2,-1,0,1} |
| T-39-02 | Tampering | `weaponCrit`/`armorBulk` on an unknown `c.weapon`/`c.armor` from a tampered save | low | mitigate | both default (crit 1, bulk 0) when the lookup misses — never throw |
| T-39-03 | Denial of service | fixture harness accepting a hand-typed `stockAfter` | low | mitigate | `declaredStockDiffs` compares measured stock against the record on BOTH sides (names) — a typo fails the suite rather than passing silently |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan (no `package.json` change); the SC checkpoint is not triggered |
</threat_model>

<verification>
- `node --test test/unit/gear-axes.test.js` green (all eight content behaviours + the engine behaviours).
- `npm test` `# fail 0`; parity master hash unchanged; only the declared fixture files changed.
- `npm run build:www` exit 0.
- SUMMARY records: test count, master hash, the list of fixture records edited (with measured before/after), the Soldier-crit correction note, and the "Human verification (deferred)" items: (1) Hero tab TO HIT reads 1–6 with a Rapier / 1–4 with a Flail on a Fighter — note this is only visible after Plan 05 routes the Hero-tab number through the engine bridge; (2) a Thief in Plate is refused the backstab, in Studded is not.
</verification>

<success_criteria>
- ROADMAP SC-1 (mechanical half): store weapon pools contain a heavy/neutral/light pick per class per tier, proven by test; the ledger (Plan 02) records before/after.
- GEAR-01 axes are live in every rule site CONTEXT Area 3 names; no fixture moves undeclared.
</success_criteria>

<output>
Create `.planning/phases/39-gear-magic-items-one-shot-tools/39-01-SUMMARY.md` when done (copy the 38-05-SUMMARY.md frontmatter/section shape; include "## Human verification (deferred to end of run)").
</output>
