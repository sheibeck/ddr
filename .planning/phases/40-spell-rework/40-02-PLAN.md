---
phase: 40-spell-rework
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - engine/magic.js
  - engine/combat.js
  - tools/lib/tuning-bot.mjs
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/unit/spell-mechanics.test.js
  - test/unit/magic.test.js
  - test/unit/tuning-bot.test.js
  - test/unit/identity-contract.test.js
  - test/unit/casters-can-act.test.js
  - test/unit/party-abilities.test.js
  - test/unit/party-combat.test.js
  - test/unit/freeze-pays-out.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/SPELLS.md
autonomous: true
requirements: [SPELL-01, SPELL-03]

must_haves:
  truths:
    - "No engine or bot rule keys on a spell's display name: Lightning's every-foe case reads `sp.aoe === 'all'`, Freeze's frozen-solid kill reads `sp.onHit === 'freeze'` in both `castSpell` and the bot's kill tier — a rename can never silently break either"
    - "Ice does what its text says: a `dot` cast puts `f.dot = { left: d4+1, dmg: d6, by: 'ice' }` on the target (resistable by an intelligent foe, like Acid), ticks d6 a round in foeTurn's existing dot block, and when the last tick leaves the foe standing it is frozen solid and dies through killFoe (pays like any kill)"
    - "Lesser Summon conjures a weaker ally than Summon: level clamp(c.level − 1, 1, 3), d4 rounds, its own four-name table, and NEVER the Summoner's doubling or backfire (draws: d4 + pick, no d8)"
    - "Control spells carry an explicit scope × duration: Weaken is every foe for d4+1 rounds on a `c.timers['spell:weaken']` rounds record whose expiry clears `C.weakened`/`C.foeToHitPenalty` and narrates `weakenFaded`; Stupidity is one foe for the rest of the fight (`t.stupid` — it loses every turn, `foeStupefied`, and is hit as if asleep); Shrink's 'half damage' is real (a shrunk foe's melee damage is halved at every foe-damage line)"
    - "Every new event type has EVENT_NARRATION + TOAST_FOR + RAIL_FAMILY entries and passes the voice scan; `dotTick` narrates 'from the ice' when `by === 'ice'`"
    - "The tuning bot still casts every reshaped kind (`dot` scores in the damage tier); every existing parity fixture is byte-identical (no fixture casts Ice/Weaken/Stupidity/Shrink/Lesser Summon) — confirmed by the suite, not asserted"
  artifacts:
    - "engine/magic.js — data-flag thrown branch, `dot` branch, lesser summon branch, Weaken timer, Stupidity flag-only, ALLY_NAMES/LESSER_ALLY_NAMES"
    - "engine/combat.js — ice dot payoff, spell:weaken expiry, foeStupefied skip + need rule, shrunk halving, allyCast weaken timer"
    - "tools/lib/tuning-bot.mjs — onHit/aoe/dot repoints"
    - "test/unit/spell-mechanics.test.js"
  key_links:
    - "content/spells.js flags (onHit/aoe/lesser/kind dot) -> engine/magic.js#castSpell branches -> engine/combat.js#foeTurn (dot tick + ice payoff, spell:weaken expiry, stupid skip)"
    - "engine/magic.js weaken -> engine/effects.js#startEffect('spell:weaken') -> engine/combat.js#foeTurn tail tickRounds transition -> C.weakened cleared + weakenFaded"
    - "tools/lib/tuning-bot.mjs#chooseSpell -> the same flags (no name literals)"
---

<objective>
Land the offense-school MECHANICS the Plan 01 table promised: the engine reads the new data flags instead of spell names (research Pitfall 2), Ice becomes the real damage-over-time spell its text has always claimed (CONTEXT: "Ice gets a real DOT"), Lesser Summon gets its own small-and-safe cast branch (CONTEXT Area 3), and the control spells get their explicit scope × duration axis (Weaken d4+1 rounds on a timer, Stupidity for the fight, Shrink's half damage made true).

Purpose: SPELL-01 is only true if the mechanics match the one-line niche text — a table that says "damage over time" over a burst branch is the exact lie this phase exists to remove.

Output: the four engine branches, the combat.js hooks, the bot repoints, narration for `iceApplied`/`weakenFaded`/`foeStupefied` (+ the `dotTick` ice branch), `test/unit/spell-mechanics.test.js`, updated pins, the ledger's "Offense mechanics" section. No `mazeworld.html` edits (Plan 05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-spell-rework/40-CONTEXT.md
@.planning/phases/40-spell-rework/40-01-SUMMARY.md
@.planning/phases/38-melee-active-abilities/38-03-SUMMARY.md
@engine/magic.js
@engine/effects.js
</context>

<standing_rules>
- Greenfield (CONTEXT "Standing rulings"): no dual paths; the new mechanics are the only mechanics. Fixture protocol: 40-01-PLAN.md `<standing_rules>` item "Fixture divergence protocol". Expected fixture regenerations this plan: NONE — no fixture casts Ice, Weaken, Stupidity, Shrink, Lightning or Lesser Summon, and Freeze's `onHit` path is draw-for-draw identical to the name check it replaces (the `cast-damage` scenario proves it). If the suite says otherwise, STOP, measure, declare per the protocol.
- `test/parity/prototype-master.js.txt` never edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- Commit convention `feat(40-02): …` / `test(40-02): …` / `docs(40-02): …` + the two attribution lines (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`); explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT: no device steps; SUMMARY has "## Human verification (deferred to end of run)".
- Every new event type: `EVENT_NARRATION` (src/browser/eventNarration.js) + `TOAST_FOR` (src/browser/toasts.js) + `RAIL_FAMILY` (src/browser/rail.js); `toastsCoverage.test.js`/`formatEventsCoverage.test.js`/`test/voice/safety-scan.test.js` green.
- Bot: `chooseSpell` stays kind-generic; only the two name-keyed checks are repointed and `dot` joins the damage tier — no new TACTICS (Phase 42). `docs/class-pass/v15-before*.json` is never regenerated.
- The one-tick-already-spent invariant (38-03 SUMMARY): a cast IS the round's action, so the same dispatch's `afterPlayerAction → foeTurn` tail ticks a freshly-started rounds record once before `castSpell` returns — state this in the Weaken JSDoc and account for it in the tests.
</standing_rules>

<artifacts_this_phase_produces>
See 40-01-PLAN.md `<artifacts_this_phase_produces>` for the phase-wide inventory. This plan creates every P02 entry: magic.js's flag-driven thrown branch, the `dot` branch (+ `iceApplied`), the lesser summon branch (+ `ALLY_NAMES`/`LESSER_ALLY_NAMES`, additive `lesser: true` on `allySummoned`/`allyPending`), the Weaken timer (`spell:weaken`, additive `weakened.rounds`), Stupidity flag-only; combat.js's ice payoff, `spell:weaken` expiry (+ `weakenFaded`), `foeStupefied` skip + the `t.stupid` need-5 rule, shrunk halving at three lines, `allyCast` weaken timer (+ additive `allySpellHit.rounds`); the bot's `onHit`/`aoe`/`dot` repoints; `test/unit/spell-mechanics.test.js`.
</artifacts_this_phase_produces>

<mechanics_spec>
Numbers (CONTEXT rulings + Claude's discretion; every one goes into docs/SPELLS.md "Offense mechanics"):

- Thrown branch: `targets = sp.aoe === "all" ? liveFoes(state) : [C.foes[C.target]]`; `freeze = sp.onHit === "freeze"`; everything else in the branch (d10 vs d8, need 6 vs 4, afraid, mult, damageFoe, killFoe, kill-twice revive) byte-identical.
- `dot` branch (Ice): target = `C.foes[C.target]` (normalizeTarget already ran); if alive: `t.dot = { left: rng.d(4) + 1, dmg: sp.dmg, by: "ice" }`; event `iceApplied { target, rounds }`. One draw. No to-hit roll (like Acid); resistable (not in RESIST_IMMUNE_KINDS — `dot` is absent from that set, so an intel ≥ 12 foe gets the d20). No level multiplier, no `spellDmg` eff (like Acid). Recasting on a foe already carrying an ice dot REFRESHES `left` (overwrite), never stacks.
- Ice payoff (combat.js#foeTurn dot block): after the tick's damage and kill check, if the record just ran out (`left` reached 0), `by === "ice"` and the foe is still alive → `f.frozen = true`, event `frozenSolid { target }`, `killFoe(...)`, revive-unfreeze on the kill-twice rule exactly as the Freeze branch does (`if (f.alive) f.frozen = false`), then `continue`. Zero extra draws before killFoe's own.
- Lesser summon (magic.js summon branch): `lesser = sp.lesser === true`; `doubled = c.sub === "Summoner" && !lesser`; `lvl = lesser ? Math.max(1, Math.min(3, c.level - 1)) : Math.min(5, c.level + (doubled ? 1 : 0))`; the backfire check (`doubled && rng.d(8) === 1`) is skipped for lesser (no d8 draw); `rounds = lesser ? rng.d(4) : (doubled ? 2 : 1) * rng.d(4) + 2`; `name = rng.pick(lesser ? LESSER_ALLY_NAMES : ALLY_NAMES)`; `ALLY_NAMES` = the existing four literals in their existing order (moved to a module const, byte-identical strings); `LESSER_ALLY_NAMES` = ["A thing with one horn, mostly", "Something with nearly enough arms", "A small grey sulk", "A shape that is mildly upsetting to look at"]; events `allySummoned`/`allyPending` gain `lesser: true` only on the lesser branch (conditionally spread — the Summon event shape stays byte-identical). The ally object itself gains NO new field.
- Weaken: `C.weakened = true; C.foeToHitPenalty = 3;` as today, PLUS `const rounds = rng.d(4) + 1; startEffect(c, "spell:weaken", { rounds });` (import `startEffect` from ./effects.js — a leaf, no cycle); event `weakened { rounds }`. Re-casting overwrites the record (refresh). `allyCast`'s weaken branch does the same on `state.c` with its own `rng.d(4) + 1` draw; `allySpellHit { effect: "weakened", rounds }`.
- Weaken expiry (combat.js#foeTurn tail): capture `const trans = tickRounds(c)` inside the existing `if (c.timers)` guard, pass it to `narrateTimerTransitions` as today, then `if (C && trans.some(t => t.id === "spell:weaken" && t.from === "effect")) { C.weakened = false; C.foeToHitPenalty = 0; events.push({ type: "weakenFaded" }); }`. `clearRoundTimers` at endCombat already drops the record; `state.combat` is nulled there anyway.
- Stupidity: `t.stupid = true`; the `t.asleep = Math.max(t.asleep, rng.d(10))` line and its draw are REMOVED (the rest-of-the-fight rule replaces the nap); event `stupefied` unchanged. combat.js#foeTurn: directly after the `f.asleep` block, `if (f.stupid) { events.push({ type: "foeStupefied", name: f.name }); continue; }` (a stupid foe never reaches the flee/ability/melee code). combat.js#playerStrike line ~536: `if (t.asleep > 0 || t.stupid) need = Math.max(need, 5);`.
- Shrink: at the three foe-melee damage lines — foeTurn hero-target `let dmg = …` (~2381), foeTurn member-target `let mDmg = …` (~2305), pursuitStrike (~853, variable `pursuer`) — add `if (f.shrunk) dmg = Math.ceil(dmg / 2);` (resp. `mDmg`, `pursuer.shrunk`) immediately after the existing `C.weakened` halving. Zero draws; false on every fixture.
- Bot (`chooseSpell`): `if (sp.onHit === "freeze") { score = 410; tier = "kill"; }` replaces the name check; the damage tier's condition becomes `thrown || volley || acid || dot`; inside it `if (sp.aoe === "all") expected *= Math.max(1, nFoes);` replaces the name check; `else if (sp.kind === "dot") { if (target && target.dot) continue; expected *= 3; }` (mean d4+1 = 3.5 ticks, a documented constant like acid's ×2). Update the JSDoc table (KILL: `onHit: "freeze"` 410; DAMAGE: `aoe: "all"` multiplier, `dot` ×3).
</mechanics_spec>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data flags in the thrown branch, the dot branch + ice payoff, the lesser summon branch, bot repoints, narration</name>
  <files>engine/magic.js, engine/combat.js, tools/lib/tuning-bot.mjs, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/spell-mechanics.test.js, test/unit/tuning-bot.test.js, test/unit/identity-contract.test.js, test/unit/casters-can-act.test.js, test/unit/freeze-pays-out.test.js</files>
  <read_first>
    - engine/magic.js lines 1-60 (imports, RESIST_IMMUNE_KINDS), 138-160 (the summon branch — the doubled/backfire rules and the four inline ally names), 330-400 (the thrown branch — the two name-keyed reads, the Freeze kill routing and its comment block)
    - engine/combat.js lines 2130-2170 (foeTurn's acid and dot tick blocks — the `f.dot = { left, dmg, by }` template and its kill check), 1308-1332 (allyTurn — how an ally's lvl drives STRIKE_DICE and damage)
    - engine/abilities.js lines 200-215 (the Poisoned Edge `applyPoison` + `poisonedEdgeApplied` shape this task mirrors for Ice)
    - tools/lib/tuning-bot.mjs lines 195-340 (bestCastableSummonIdx, lowestCastableUtilitySpellIdx, chooseSpell + its JSDoc table)
    - src/browser/eventNarration.js lines 495-505 (dotTick), 540-570 (the spell-effect lines), 575-585 (frozenSolid); src/browser/toasts.js lines 570-600 (the FEED-06 fold list), 1215-1225 (dotTick), 1265-1310 (the same spell lines); src/browser/rail.js lines 140-220 (RAIL_FAMILY)
    - test/unit/magic.test.js lines 1-60 (fixedState/fixedFoe/fixedCombat/fakeRng helpers to reuse), test/unit/freeze-pays-out.test.js (the Freeze kill pins — must stay green unchanged), test/unit/tuning-bot.test.js lines 225-260 and 425-455 (the Freeze/Lightning/Ice ordering pins), test/unit/identity-contract.test.js lines 300-315 (the Summoner GOOD from Plan 01 — re-pin to the lesser numbers), test/unit/casters-can-act.test.js lines 175-200 (IDENT-03 doubled-formula tests at level 2)
    - test/unit/toastsCoverage.test.js and test/unit/formatEventsCoverage.test.js headers (how the guards derive the event set — a retired name-check must not leave a dead narration entry)
  </read_first>
  <behavior>
    - Lightning (aoe all) in a 3-foe combat pushes three `spellThrown` events and hits/misses each foe independently; a Fireball cast in the same combat targets only `C.foes[C.target]`. The engine source contains no `sp.n ===` comparison against any spell name.
    - Freeze: `test/unit/freeze-pays-out.test.js` passes byte-unchanged; a thrown spell without `onHit` never emits `frozenSolid`.
    - Ice (`SPELLS[13]`) in combat vs an intel-1 foe with `fakeRng([3])`: one draw, `foe.dot` deep-equals `{ left: 4, dmg: { n: 1, sides: 6, bonus: 0 }, by: "ice" }`, event `iceApplied { target, rounds: 4 }`, no `spellThrown`; vs an intel-12 foe the first draw is the d20 resist (`spellResisted` on ≤ 11, otherwise the dot lands).
    - Ice payoff: a foe with `dot: { left: 1, dmg: d6, by: "ice" }` and 200 wp, after one `foeTurn`: takes one d6 tick (`dotTick { by: "ice", left: 0 }`), then `frozenSolid { target }`, then `foeKilled` (via killFoe, sp/gold/kills paid); a foe whose tick itself kills it pays through the existing dot kill path with NO `frozenSolid`; a `by: "poisonedEdge"` dot running out never freezes; a kill-twice (`lives`) foe survives the freeze once and is unfrozen (`f.frozen === false`, still alive).
    - Lesser Summon by a level-1 Summoner in combat with `fakeRng([3, 0.5])`-style values (d4 → 3, then the name pick): exactly TWO draws, `C.ally` deep-equals `{ lvl: 1, rounds: 3, name: <LESSER_ALLY_NAMES entry> }`, event `allySummoned { …, lvl: 1, rounds: 3, lesser: true }`; no `summonBackfired` is ever possible (a fakeRng whose first value is 1 still summons). A level-5 Summoner's lesser ally is lvl 3 (cap); a level-1 Wizard's is lvl 1. Summon by a level-2 Summoner still draws d8 first and doubles (casters-can-act's IDENT-03 pins hold at level 2); `allySummoned` for Summon carries NO `lesser` key.
    - Bot: `chooseSpell` on a Sorcerer with [Freeze, Fireball] picks Freeze (410) — the existing test; with [Mangle, Lightning, Fireball, Acid, Ice] vs 1 foe the order is Mangle > Fireball > Acid(318) > Ice(313.5 → ties broken as documented) > … — re-measure and pin the measured order; Lightning vs 3 foes outranks Fireball; Ice is skipped when the target already carries `dot`.
    - Narration: `iceApplied`, `dotTick` with `by: "ice"` ("from the ice"), `allySummoned`/`allyPending` with `lesser` (optional small-ally wording) render through EVENT_NARRATION and TOAST_FOR; RAIL_FAMILY has an `iceApplied` row; the voice scan is green.
  </behavior>
  <action>
    Write `test/unit/spell-mechanics.test.js` FIRST (RED) with the behaviours above (reuse magic.test.js's fixture helpers by copying them — do not import a test file).

    engine/magic.js: per `<mechanics_spec>` — thrown branch reads `sp.aoe`/`sp.onHit` (delete both name comparisons; update the Freeze comment block to say the flag is the switch); engine/combat.js#allyCast (~line 1733) has a THIRD name-keyed Freeze check for member casts — repoint it to `sp.onHit === "freeze"` in the same commit; add the `dot` branch before the `else` thrown fallback (an `else if (sp.kind === "dot")` beside `acid`); rewrite the summon branch per the lesser rules with `ALLY_NAMES`/`LESSER_ALLY_NAMES` module consts (the four existing strings byte-identical, same order). Header comment: list `dot` among the kinds and name the three flags.

    engine/combat.js#foeTurn dot block: capture `by`/`ranOut` and add the ice payoff after the existing kill check, mirroring the Freeze branch's frozen/killFoe/revive lines; comment it as Phase 40 (SPELL-01, Ice) and note `by` is the switch so Poisoned Edge is unaffected.

    tools/lib/tuning-bot.mjs#chooseSpell: the three repoints per `<mechanics_spec>` + JSDoc.

    src/browser: EVENT_NARRATION — `iceApplied: (e) => "Ice climbs {target}: d6 a round for {rounds} rounds, then it stops moving."` (hit span); `dotTick` branches on `e.by === "ice"` → "{target} takes {dmg} from the ice." else the existing poison line; `allySummoned`/`allyPending` → when `e.lesser` say "{name} answers the call, sort of." / "{name} is coming, in a small way." else the existing lines. TOAST_FOR — the same three (short forms: "Ice climbs {target} ({rounds})." magic tone; dotTick ice → "{target} takes {dmg} from the ice." hurt tone). RAIL_FAMILY — `iceApplied: { icon: "·", title: "ICE", tone: "odd" }`. Run the coverage guards + voice scan.

    Pins: tuning-bot.test.js (Ice ordering → measured), identity-contract.test.js Summoner GOOD (re-pin the cast to the lesser numbers: `fakeRng([3, …])` two draws, `allyPending { lvl: 1, rounds: 3, lesser: true }`), casters-can-act.test.js (IDENT-03 tests are level-2 Summon casts after Plan 01 — confirm green; add one lesser-vs-full contrast if absent), freeze-pays-out.test.js (must pass unchanged — do not edit unless a helper import moved).
  </action>
  <verify>
    <automated>node --test test/unit/spell-mechanics.test.js test/unit/magic.test.js test/unit/freeze-pays-out.test.js test/unit/tuning-bot.test.js test/unit/identity-contract.test.js test/unit/casters-can-act.test.js test/unit/usable-features-audit.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sp\.n === "Lightning"\|sp\.n === "Freeze"' engine/magic.js tools/lib/tuning-bot.mjs` prints 0 for both files; `grep -rn 'sp\.n === "' engine/ tools/lib/ | wc -l` prints 0. <!-- planner-discipline-allow: sp.n === "Lightning" --> <!-- planner-discipline-allow: sp.n === "Freeze" -->
    - `grep -c 'sp\.aoe === "all"' engine/magic.js tools/lib/tuning-bot.mjs` prints 1 for each; `grep -c 'sp\.onHit === "freeze"' engine/magic.js engine/combat.js tools/lib/tuning-bot.mjs` prints 1 for each of the three files.
    - `grep -c 'sp\.kind === "dot"' engine/magic.js` is at least 1; `grep -c 'type: "iceApplied"' engine/magic.js` prints 1; `grep -c 'by === "ice"' engine/combat.js` is at least 1.
    - `grep -c "LESSER_ALLY_NAMES\|ALLY_NAMES" engine/magic.js` is at least 3 (two const declarations + the pick line).
    - `grep -c "iceApplied" src/browser/eventNarration.js src/browser/toasts.js src/browser/rail.js` is at least 1 for each file.
    - `node --test test/unit/spell-mechanics.test.js` `# fail 0` with at least 12 tests; every listed suite `# fail 0`.
  </acceptance_criteria>
  <done>No spell is name-keyed anywhere in engine or bot; Ice is a real DOT with the promised freeze; Lesser Summon is small, safe and its own thing; the bot casts all of it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The control axis — Weaken on a rounds timer with expiry, Stupidity for the fight, Shrink's half damage; member weaken; narration</name>
  <files>engine/magic.js, engine/combat.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/spell-mechanics.test.js, test/unit/magic.test.js, test/unit/party-abilities.test.js, test/unit/party-combat.test.js</files>
  <read_first>
    - engine/magic.js lines 160-185 (stun / weaken / stupid branches)
    - engine/combat.js lines 530-540 (playerStrike's asleep need-5 line), 845-856 (pursuitStrike's damage line), 1755-1775 (allyCast's weaken branch), 2165-2185 (foeTurn's asleep/stunned skip blocks), 2296-2310 and 2372-2392 (the two foeTurn damage lines with the C.weakened halving), 2400-2445 (the tail: ward/mirror/foeEffect/afraid/tickRounds/narrateTimerTransitions)
    - engine/effects.js (startEffect/tickRounds/clearRoundTimers semantics — an effect record without `cd` is deleted at 0 and reported `{ id, from: "effect", to: null }`)
    - engine/items.js lines 1060-1100 (narrateTimerTransitions ignores non-item ids — `spell:` ids fall through untouched)
    - test/unit/magic.test.js lines 310-360 (the three Weaken tests whose fakeRng sequences gain a d4), test/unit/party-abilities.test.js and test/unit/party-combat.test.js (grep `weaken` — member-cast pins whose draw sequences gain a d4)
    - .planning/phases/38-melee-active-abilities/38-03-SUMMARY.md (the one-tick-already-spent invariant)
  </read_first>
  <behavior>
    - Weaken by a Wizard vs an intel-1 foe with `fakeRng([2, …foe-turn draws])`: `C.weakened === true`, `C.foeToHitPenalty === 3`, `c.timers["spell:weaken"]` is `{ cadence: "rounds", left: 2, phase: "effect" }` immediately after the cast (d4 = 2 → 3 rounds, one already ticked by the same dispatch's foeTurn tail); event `weakened { rounds: 3 }`; two more foeTurn calls later the record is gone, `C.weakened === false`, `C.foeToHitPenalty === 0`, and that turn's events include `weakenFaded`; the foes' damage on the expiry turn is still halved (the swing precedes the tail tick).
    - Re-casting Weaken mid-window overwrites `left` (refresh) and pushes a second `weakened` — never two records.
    - `allyCast` weaken (a Magic User party member) draws one extra d4 after the resist roll, starts the same `spell:weaken` record on `state.c`, and `allySpellHit` carries `rounds`.
    - Stupidity vs an intel-1 foe: ZERO draws (no d10), `t.stupid === true`, `t.asleep` unchanged (0); the next `foeTurn` emits `foeStupefied { name }` for it and the foe neither swings nor casts; `playerStrike` vs a stupid foe with need 3 uses need 5 (`strikeRolled.need === 5`, or the equivalent field the existing asleep test asserts); the flag survives every round until the foe dies or combat ends.
    - Shrink: a shrunk foe (`shrunk: true`, lvl 1) whose melee would deal `1 + d6` deals `ceil((1 + d6)/2)` to the hero, to a party member, and in `pursuitStrike`; a foe that is both shrunk and under Weaken is quartered (ceil twice).
    - Narration: `weakened` renders the rounds ("They hit softer now, for 3 rounds."), `weakenFaded` ("Their arms remember how to swing."), `foeStupefied` ("{name} stands there, thinking about nothing."); RAIL_FAMILY rows exist for `weakenFaded` and `foeStupefied`; voice scan green.
  </behavior>
  <action>
    Extend `test/unit/spell-mechanics.test.js` FIRST (RED) with the behaviours above (drive full `foeTurn` calls with a `makeRng(seed)` where exact draw sequences are not the point; use fakeRng for the draw-count assertions).

    engine/magic.js: weaken branch per `<mechanics_spec>` (import `startEffect`; JSDoc names the one-tick-already-spent invariant); stupid branch drops the asleep line (comment: DELIBERATE RULES CHANGE, Phase 40, SPELL-01 — CONTEXT "Stupidity (single, the fight)"; the nap is replaced by a rest-of-the-fight skip in combat.js).

    engine/combat.js: (a) foeTurn tail — capture the tickRounds transitions and clear the weaken flags on the `spell:weaken` effect→null transition with `weakenFaded` (keep `narrateTimerTransitions` receiving the same list); (b) foeTurn — the `f.stupid` skip block after the asleep block; (c) playerStrike — `|| t.stupid` on the need-5 line, comment updated; (d) the three shrunk halvings; (e) allyCast — the d4 draw + `startEffect(state.c, "spell:weaken", { rounds })` + `rounds` on `allySpellHit`. Update the module header's list of magic-set fields (`spell:weaken` record; `t.stupid` now read).

    src/browser: EVENT_NARRATION/TOAST_FOR — `weakened` reads `e.rounds` (fallback wording without it), `weakenFaded`, `foeStupefied` (TOAST tone "magic"/"beat" as the fold rules require — check the FEED-06 fold list for `foeSlept`'s treatment and mirror it for `foeStupefied`); RAIL_FAMILY — `weakenFaded: { icon: "·", title: "WEAKEN FADES", tone: "dull" }`, `foeStupefied: { icon: "·", title: "STUPEFIED", tone: "odd" }`. Coverage guards + voice scan.

    Pins: magic.test.js's three Weaken tests (add the d4 value to each landing sequence; the resisted case draws none); party-abilities/party-combat member-weaken pins (add the d4); any test asserting Stupidity's asleep value is rewritten to the flag (grep `stupid` across test/unit first).
  </action>
  <verify>
    <automated>node --test test/unit/spell-mechanics.test.js test/unit/magic.test.js test/unit/combat.test.js test/unit/party-abilities.test.js test/unit/party-combat.test.js test/unit/effects.test.js test/unit/effect-expiry.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"spell:weaken"' engine/magic.js` prints 1 and `grep -c '"spell:weaken"' engine/combat.js` is at least 2 (allyCast + the expiry).
    - `grep -c 'type: "weakenFaded"' engine/combat.js` prints 1; `grep -c 'type: "foeStupefied"' engine/combat.js` prints 1.
    - `grep -c "t.asleep = Math.max(t.asleep, rng.d(10))" engine/magic.js` prints 0. <!-- planner-discipline-allow: t.asleep = Math.max(t.asleep, rng.d(10)) -->
    - `grep -c "\.shrunk) dmg = Math.ceil\|\.shrunk) mDmg = Math.ceil" engine/combat.js` prints 3.
    - `grep -c "t.asleep > 0 || t.stupid" engine/combat.js` prints 1.
    - `grep -c "weakenFaded\|foeStupefied" src/browser/rail.js` prints 2.
    - Every listed suite `# fail 0`; `node --test test/unit/spell-mechanics.test.js` now has at least 22 tests.
  </acceptance_criteria>
  <done>Weaken has a visible, expiring duration; Stupidity is a fight-long single-target disable; Shrink's text is true; members' weaken follows the same rule.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger "Offense mechanics" section, fixture check, plan gate</name>
  <files>docs/SPELLS.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - docs/SPELLS.md (Plan 01's sections; the "## Offense mechanics (Plan 02)" placeholder)
    - docs/ABILITIES.md lines 248-370 ("## Play rules (Plan 03)" — the per-mechanic write-up + draw statement shape to mirror)
    - test/parity/FIXTURE-INVENTORY.md (Plan 01's Phase 40 section — append a short Plan 02 paragraph under it)
  </read_first>
  <action>
    1. docs/SPELLS.md: replace the "(appended by Plan 02)" line under "## Offense mechanics (Plan 02)" with: the data flags and the sites that read them (with the retired name checks named); the `dot` branch and the ice payoff (draw statement: cast d4; per tick d6 + damageFoe's own; payoff killFoe's own draws only); Lesser Summon's numbers and name table vs Summon's (draw statement: d4 + pick vs d8 + d4 + pick for a Summoner); the control axis table `| Spell | Scope | Duration | Mechanism |` for Doze/Stun/Weaken/Stupidity/Blind/Shrink/Petrify; the Weaken timer lifecycle incl. the one-tick-already-spent invariant; the bot repoints and the `dot` ×3 constant; "Byte-identical elsewhere" — why no fixture moved.

    2. FIXTURE-INVENTORY.md: under the Phase 40 section add "### Plan 02 — engine mechanics: zero fixture moves" with the one-paragraph reason (no fixture casts the changed kinds; Freeze's flag path is draw-identical, proven by `cast-damage`).

    3. PLAN GATE (record in the SUMMARY): `npm test` `# fail 0` (record `# tests`); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0 (src/browser changed); `git status --porcelain test/parity/fixtures` empty; `git log --stat <plan-base>..HEAD -- store-listing tools/store-screenshots` prints nothing.

    4. SUMMARY: standard shape + "## Human verification (deferred to end of run)" — Plan 02 items: (1) cast Ice on a tough foe — the fight log shows "Ice climbs …" then a d6 tick each round and "freezes solid" at the end with the kill paid; (2) cast Weaken — the Weakened badge on each foe and the rounds count, and "Their arms remember how to swing." when it fades; (3) Stupidity — the foe "stands there, thinking about nothing" every round; (4) a level-1 Summoner's Lesser Summon brings a small ally for at most 4 rounds and never backfires; (5) Lightning still hits every foe.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && npm run build:www && git status --porcelain test/parity/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0; `git status --porcelain test/parity/fixtures` prints nothing.
    - `grep -c "(appended by Plan 02)" docs/SPELLS.md` prints 0 and `grep -c "## Offense mechanics (Plan 02)" docs/SPELLS.md` prints 1. <!-- planner-discipline-allow: (appended by Plan 02) -->
    - `grep -c "### Plan 02" test/parity/FIXTURE-INVENTORY.md` prints 1.
  </acceptance_criteria>
  <done>The ledger records every offense mechanic with its draw statement; the gate is green and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content flags → engine | a tampered/unknown `kind` or a missing `dmg` on a `dot` row |
| timer records → combat | a `spell:weaken` record on `c.timers` outliving its combat (load path) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-03 | Tampering | `dot` row without `dmg` | low | mitigate | `rollDice` on a missing notation is the existing engine V5 no-op path (0 damage); the dot branch guards `sp.dmg` before writing the record |
| T-40-04 | Denial of service | a stale `spell:weaken` record on load | low | mitigate | rounds-cadence records are cleared by `clearStaleTimers` (Phase 36) and `combat` is nulled on load — `C.weakened` cannot outlive its fight |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan |
</threat_model>

<verification>
- Tasks 1–2 suites green; Task 3 gate recorded; zero fixture moves confirmed by the suite.
</verification>

<success_criteria>
- ROADMAP SC-1's mechanics half: two same-level offense spells now behave differently (burst vs DOT vs control-with-duration vs chaos), and the engine reads data flags, never names.
</success_criteria>

<output>
Create `.planning/phases/40-spell-rework/40-02-SUMMARY.md` when done.
</output>
