---
phase: 40-spell-rework
plan: 04
type: execute
wave: 4
depends_on: ["40-03"]
files_modified:
  - content/spells.js
  - engine/magic.js
  - engine/maze.js
  - engine/movement.js
  - engine/derived.js
  - engine/saveState.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/unit/map-reveal.test.js
  - test/unit/spell-table.test.js
  - test/unit/magic.test.js
  - test/unit/save-validation.test.js
  - test/unit/conditions.test.js
  - test/unit/movement.test.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/SPELLS.md
autonomous: true
requirements: [SPELL-05]

must_haves:
  truths:
    - "Casting Map the Floor marks every not-yet-seen non-wall cell `seen = true` AND `spellSeen = true` (cells already seen are left alone), starts ONE `c.timers['spell:reveal']` squares record of 40 (the row's own `squares` field, stated in its txt, ≤ 100 per the once-a-day rule), and narrates `floorMapped { squares, cells }`; recasting refreshes the record and never double-marks"
    - "Normal exploration during the window graduates cells: every cell `reveal()` touches loses its `spellSeen` flag and stays seen for good (the ratified Key Decision — only what the spell ALONE showed re-fogs)"
    - "At expiry exactly ONE sweep runs (on the effect→null transition the per-step tick returns, never per step): every still-flagged cell becomes `seen = false` with the flag removed, and `revealFaded { cells }` narrates 'The map forgets what it was told.'; descending deletes the record silently (the mapped floor is gone)"
    - "`spellSeen` is carved out of all three `*Comparable()` functions and the three local duplicates (a structural tripwire — no fixture ever casts the spell); a fresh `newRun` floor carries no `spellSeen` key on any cell; the master hash is unchanged"
    - "Old saves load tolerantly: a floor carrying stale `spellSeen` flags with no live `spell:reveal` record has the flags cleared (seen left as saved); a grimoire holding the retired name 'Detect Magic' is renamed to 'Map the Floor' — no card, no narration, no rng"
    - "`conditionsOf` emits a `reveal` chip with the squares left while the window is open; the `detectMagic` event and its narration/toast entries are retired (no dead entries)"
  artifacts:
    - "engine/maze.js — reveal() graduation, refogSpellSeen(floor)"
    - "engine/magic.js — the provenance-marking reveal branch (+ floorMapped)"
    - "engine/movement.js — spell:reveal transition handling (+ revealFaded), descend clears the record"
    - "engine/saveState.js — clearStaleSpellSeen(floor, c), migrateSpellNames(c)"
    - "engine/derived.js — conditionsOf key reveal"
    - "test/parity/harness/comparables.js — stripSpellSeen"
    - "test/unit/map-reveal.test.js"
  key_links:
    - "engine/magic.js reveal branch -> engine/effects.js#startEffect('spell:reveal', { squares: 40 }) -> engine/movement.js per-step tickSquares transition -> engine/maze.js#refogSpellSeen (the ONE sweep) -> revealFaded"
    - "engine/maze.js#reveal (move/teleport/descend/newRun) -> deletes cell.spellSeen (graduation) BEFORE the same step's tick"
    - "engine/saveState.js#validateSave -> clearStaleSpellSeen + migrateSpellNames"
---

<objective>
Turn the renamed Map the Floor into the time-boxed reveal SPELL-05 asks for, on the ratified Key Decision (CONTEXT Area 1, user-chosen): re-fog ONLY what the spell alone showed — a per-cell provenance flag, normal walking graduates cells to permanent memory, one sweep at expiry, recast refreshes. The window rides Phase 36's timer model exactly like Phase 39's item effects (`spell:` id prefix, squares cadence).

Purpose: the prototype's Detect Magic was a permanent one-way whole-floor reveal; the roadmap wants a stated window and honest re-fogging without ever taking away what the player earned by walking.

Output: the engine model (magic/maze/movement/derived/saveState), the harness carve-out, tolerant load (stale flags + the old spell name), narration for `floorMapped`/`revealFaded`, `test/unit/map-reveal.test.js`, the ledger's Key Decision section. No `mazeworld.html` edits (Plan 05 paints spell-seen cells in a distinct tint and adds the chip copy).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-spell-rework/40-CONTEXT.md
@.planning/phases/40-spell-rework/40-03-SUMMARY.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-02-SUMMARY.md
@engine/effects.js
@engine/maze.js
</context>

<standing_rules>
- Greenfield (CONTEXT "Standing rulings"): the timed, re-fogging reveal is the ONLY reveal; no permanent-reveal option. Fixture protocol: 40-01-PLAN.md `<standing_rules>`. Expected fixture regenerations: NONE (no fixture casts the reveal spell; `reveal()`'s graduation `delete` is a no-op on cells that never carried the flag — every fixture floor is byte-identical; the suite confirms).
- `test/parity/prototype-master.js.txt` never edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- Commit convention `feat(40-04): …` / `test(40-04): …` / `docs(40-04): …` + the two attribution lines (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`); explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT: no device steps; SUMMARY has "## Human verification (deferred to end of run)".
- Every new event type: `EVENT_NARRATION` + `TOAST_FOR` + `RAIL_FAMILY`; the retired `detectMagic` entries are DELETED from both tables (the coverage guards forbid dead entries); voice scan green.
- Research Pitfall 4 is binding: the sweep reacts to the ONE `{ id: "spell:reveal", from: "effect", to: null }` transition — never a per-step poll of the record.
- Once-a-day rule: the window is 40 squares (≤ 100), from the row's `squares` field — the txt already states 40.
- Phase 41 note (research): the darkness rework is a render-time filter over `seen`; this plan changes what `seen` contains. The shell's `draw()` reads `cell.seen` fresh every paint (verified: mazeworld.html draw loop) — no coupling.
</standing_rules>

<artifacts_this_phase_produces>
See 40-01-PLAN.md `<artifacts_this_phase_produces>`. This plan creates every P04 entry: `SPELLS[5].squares = 40`; magic.js's provenance-marking reveal branch + `floorMapped { squares, cells }`; `engine/maze.js#reveal` graduation + `refogSpellSeen(floor)`; movement.js's `spell:reveal` transition handling + `revealFaded { cells }` + `descend`'s record delete; `conditionsOf` key `reveal { remaining, cadence: "squares" }`; `engine/saveState.js#clearStaleSpellSeen(floor, c)` + `migrateSpellNames(c)`; harness `stripSpellSeen(floor)` in three comparables + three local duplicates; `test/unit/map-reveal.test.js`; the retired `detectMagic` event.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The provenance model — cast marks + timer, reveal() graduation, the one expiry sweep, recast refresh, descend clears, the reveal chip</name>
  <files>content/spells.js, engine/magic.js, engine/maze.js, engine/movement.js, engine/derived.js, test/unit/map-reveal.test.js, test/unit/spell-table.test.js, test/unit/magic.test.js, test/unit/conditions.test.js, test/unit/movement.test.js</files>
  <read_first>
    - engine/maze.js lines 100-208 (genFloor's cell literal `{ wall, seen, feat, … }` — the flag is LAZY, never added to the literal; reveal())
    - engine/magic.js lines 286-296 (the reveal branch to rewrite) and the effects.js import added by Plan 02
    - engine/movement.js lines 255-275 (move(): `f.px/f.py` set, `state.steps++`, `reveal(f, revealRadius(state))` — BEFORE the per-step block), 380-395 (the `if (c.timers) narrateTimerTransitions(state, tickSquares(c, 1), events)` site), the `descend()` function (grep `export function descend`) and `teleport()` (their own `reveal` calls)
    - engine/effects.js (startEffect overwrite semantics = refresh; tickSquares returns the transition list; an effect record with no `cd` is deleted at 0 and reported `from: "effect", to: null`)
    - engine/derived.js#conditionsOf (Plan 03's four chips — the `reveal` chip goes after `foresight`)
    - engine/items.js lines 1060-1100 (narrateTimerTransitions ignores `spell:` ids — the movement site keeps calling it and handles `spell:reveal` itself)
    - test/unit/magic.test.js lines 285-300 (the reveal test to rewrite), test/unit/movement.test.js (the Phase 36 30-move draw-count invariance test — must stay green with a reveal record planted), test/unit/conditions.test.js (order pins)
    - .planning/phases/40-spell-rework/40-RESEARCH.md "Detect Magic / Map Reveal (SPELL-05)" Option A and Pitfall 4
  </read_first>
  <behavior>
    - `SPELLS[5].squares === 40` (test/unit/spell-table.test.js gains the pin; the txt already says 40).
    - Cast on a fresh floor (only the start radius seen): every non-wall cell that was `seen: false` becomes `seen: true, spellSeen: true`; cells already seen carry NO `spellSeen` key; wall cells untouched; `c.timers["spell:reveal"]` deep-equals `{ cadence: "squares", left: 40, phase: "effect" }`; the events contain `floorMapped { squares: 40, cells: N }` where N equals the count of newly-marked cells; zero rng draws.
    - Walking: after `move()` steps into new territory, every cell within `revealRadius` of the new position has `seen: true` and NO `spellSeen` key (graduated), whether it was spell-marked or not; the count of flagged cells never increases during the window.
    - Steps 1..39 after the cast: no cell ever goes `seen: true → false`; no `revealFaded`. The 40th step: exactly one `revealFaded { cells: M }` where M is the count of cells still flagged before the sweep; afterwards every cell is either (walked → `seen: true`, no flag) or (spell-only → `seen: false`, no flag); the record is gone; the floor holds ZERO `spellSeen` keys. A 41st step produces no second sweep.
    - Recast at step 20: `left` is 40 again, `floorMapped` fires again with `cells` = only the cells newly marked (0 if the floor is still fully lit), no flag is set twice (the flagged count is unchanged by a recast on an unchanged floor); expiry then occurs 40 squares after the recast.
    - `descend()` with a live record: the record is deleted, no `revealFaded`, the new floor has no flags; `teleport()` does not touch the record.
    - `conditionsOf` during the window → `{ key: "reveal", polarity: "good", remaining: <left>, cadence: "squares" }` after `foresight`; absent otherwise.
    - The Phase 36 movement draw-count invariance test still passes with a reveal record planted (the sweep draws nothing).
    - `newRun(seed).floor` for the 20 pin seeds: no cell has a `spellSeen` key; `c` has no `timers` key.
  </behavior>
  <action>
    Write `test/unit/map-reveal.test.js` FIRST (RED) covering the behaviours above (use `newRun(seed)` + `applyAction` moves along a known corridor, or a hand-built open floor via the magic.test.js fixture helpers for the cast/sweep counts; `makeRng` where draws do not matter).

    content/spells.js row 5: add `squares: 40` (header comment: the reveal window the engine reads — the txt states the same number). spell-table.test.js: pin it.

    engine/maze.js: `reveal()` — in the inner loop, after `seen = true`, `if (cell.spellSeen) delete cell.spellSeen;` (comment: Phase 40 graduation — a cell the player actually sees leaves the spell's provenance; a no-op on cells that never carried the flag, so every fixture floor is byte-identical). Add `export function refogSpellSeen(floor)`: walks `floor.g`, for each cell with `spellSeen` sets `seen = false` and deletes the flag, returns the count; pure, no rng; JSDoc names the Key Decision.

    engine/magic.js reveal branch: per the behaviours — mark only `!wall && !seen` cells (both fields), count them, `startEffect(c, "spell:reveal", { squares: sp.squares })`, push `floorMapped { squares: sp.squares, cells }`; delete the `detectMagic` push. Update the header comment's kind list.

    engine/movement.js: import `refogSpellSeen` from ./maze.js; at the per-step site capture `const trans = tickSquares(c, 1)`, keep `narrateTimerTransitions(state, trans, events)`, then `if (trans.some(t => t.id === "spell:reveal" && t.from === "effect")) { const cells = refogSpellSeen(f); events.push({ type: "revealFaded", cells }); }` — all inside the existing `if (c.timers)` guard; comment: Pitfall 4 — one sweep on the transition, and the step's own `reveal()` above has already graduated this step's cells. `descend()`: `if (c.timers && c.timers["spell:reveal"]) delete c.timers["spell:reveal"];` before the new floor is generated (comment: the mapped floor is behind you; silent by design — the chip disappears with the floor).

    engine/derived.js#conditionsOf: after `foresight`, `const rev = c.timers && c.timers["spell:reveal"]; if (rev && rev.phase === "effect" && rev.left > 0) out.push({ key: "reveal", polarity: "good", remaining: rev.left, cadence: "squares" });` + JSDoc line. Update conditions.test.js's order pins; rewrite magic.test.js's reveal test to the new event/flags.
  </action>
  <verify>
    <automated>node --test test/unit/map-reveal.test.js test/unit/magic.test.js test/unit/maze.test.js test/unit/movement.test.js test/unit/conditions.test.js test/unit/effects.test.js test/unit/effect-expiry.test.js test/unit/chargen-rng-pin.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function refogSpellSeen" engine/maze.js` prints 1; `grep -c "spellSeen" engine/maze.js` is at least 3.
    - `grep -c 'type: "floorMapped"' engine/magic.js` prints 1 and `grep -c 'type: "detectMagic"' engine/magic.js` prints 0. <!-- planner-discipline-allow: type: "detectMagic" -->
    - `grep -c '"spell:reveal"' engine/magic.js` prints 1; `grep -c '"spell:reveal"' engine/movement.js` is at least 2 (the transition + descend); `grep -c 'type: "revealFaded"' engine/movement.js` prints 1.
    - `grep -c 'key: "reveal"' engine/derived.js` prints 1.
    - `node -e "import('./engine/engine.js').then(m=>{let n=0;for(const s of [1,7,8,15,24,256]){const f=m.newRun(s).floor;for(const row of f.g)for(const c of row)if('spellSeen' in c)n++;}console.log(n)})"` prints `0`.
    - `node --test test/unit/map-reveal.test.js` `# fail 0` with at least 12 tests; every listed suite `# fail 0`.
  </acceptance_criteria>
  <done>Map the Floor reveals for 40 squares, walking keeps what it earns, one sweep re-fogs the rest, and the window is a chip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harness carve-out (three comparables + three local duplicates), tolerant load (stale flags + old spell name), narration</name>
  <files>test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, engine/saveState.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/map-reveal.test.js, test/unit/save-validation.test.js</files>
  <read_first>
    - test/parity/harness/comparables.js lines 195-260 (stripTimersField/stripWornField/stripAbilitiesField — the structural-tripwire shape) and the three `*Comparable()` bodies (the `rest.c = …` chains; add a `rest.floor` line beside them)
    - test/parity/combat-parity.test.js, magic-parity.test.js, movement-parity.test.js — each file's local `function comparable(state)` (the destructure lists; the three files keep their own copies, per Phase 21's Rule-1 lesson in STATE.md)
    - engine/saveState.js lines 160-215 (clearFoeEffect/clearStaleTimers/sanitizeWorn — the additive-with-default tolerant-load helpers) and 354-430 (validateSave's `c:`/`floor:` lines)
    - test/unit/save-validation.test.js (the shape of the existing tolerant-load tests)
    - src/browser/eventNarration.js line 562 and toasts.js line 1285 (the `detectMagic` entries to DELETE); rail.js RAIL_FAMILY (teleported's "◆" glyph)
    - test/unit/toastsCoverage.test.js / formatEventsCoverage.test.js (no dead entries)
  </read_first>
  <behavior>
    - `stripSpellSeen(floor)` returns a floor whose every cell lacks `spellSeen`; a floor with no flags anywhere is returned as the SAME object (cheap no-op); it never mutates its input.
    - All three `*Comparable()` outputs and the three local `comparable()` outputs of a state with a flagged floor carry no `spellSeen` key; `node --test "test/parity/**/*.test.js"` is fully green.
    - `validateSave` on a save whose floor has `spellSeen: true` cells and NO `spell:reveal` record: every flag is removed, every `seen` value is as saved, no `timers` key is injected, no event; with a LIVE record (`phase: "effect", left > 0`) the flags are kept.
    - `validateSave` on a save whose `c.grimoire` contains "Detect Magic": it reads "Map the Floor" afterwards (position preserved; no duplicate if both names were present — dedupe keeping the first); every other name untouched.
    - `floorMapped` renders "The floor lays itself out in your head — every corridor on this level, for 40 squares." (reads `e.squares`), `revealFaded` renders "The map forgets what it was told." (beat); toasts likewise (short forms); RAIL_FAMILY `floorMapped: { icon: "◆", title: "MAPPED", tone: "odd" }`, `revealFaded: { icon: "◆", title: "THE MAP FORGETS", tone: "dull" }`; no `detectMagic` entry remains in either table; voice scan green.
  </behavior>
  <action>
    Extend `test/unit/map-reveal.test.js` and `test/unit/save-validation.test.js` FIRST (RED).

    comparables.js: add `export function stripSpellSeen(floor)` (JSDoc: Phase 40 SPELL-05 — a brand-new engine-only lazily-set per-cell provenance flag with no prototype-side equivalent; no fixture ever casts the reveal spell, so this is a structural tripwire exactly like stripTimersField) and wire `if (rest.floor) rest.floor = stripSpellSeen(rest.floor);` into movementComparable, combatComparable and economyComparable directly before their `rest.c` chains. Mirror the same line into the three local `comparable()` duplicates (combat-parity/magic-parity/movement-parity), importing the helper from the harness.

    engine/saveState.js: add `clearStaleSpellSeen(floor, c)` (walks `floor.g` when it is an array of arrays; deletes `spellSeen` from every cell unless `c.timers?.["spell:reveal"]` is a live effect record; never adds a key; returns the floor) and `migrateSpellNames(c)` (a frozen `RETIRED_SPELL_NAMES = { "Detect Magic": "Map the Floor" }` map applied over `c.grimoire` with first-occurrence dedupe; a no-op when nothing matches) — wire `migrateSpellNames` into the `c:` chain (before `foldLegacyCounters`) and `floor: clearStaleSpellSeen(obj.floor, <the migrated c>)` (compute `c` first into a local so the floor helper reads the final timers). JSDocs follow clearStaleTimers's additive-with-default wording.

    src/browser: delete the two `detectMagic` entries; add `floorMapped`/`revealFaded` to EVENT_NARRATION, TOAST_FOR (magic / beat tones, PRIORITY.you / PRIORITY.other) and RAIL_FAMILY. Run the coverage guards + voice scan.
  </action>
  <verify>
    <automated>node --test test/unit/map-reveal.test.js test/unit/save-validation.test.js test/unit/engineAdapter.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js && node --test "test/parity/**/*.test.js" 2>&1 | grep -E "^# (tests|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stripSpellSeen" test/parity/harness/comparables.js` is at least 4 (definition + three call sites); `grep -c "stripSpellSeen" test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js` is at least 2 for each file (import + call).
    - `grep -c "function clearStaleSpellSeen\|function migrateSpellNames" engine/saveState.js` prints 2; `grep -rl '"Detect Magic"' engine/` prints exactly `engine/saveState.js` (the migration map is the ONLY place the retired name survives in engine/).
    - `grep -rc "detectMagic" src/browser/eventNarration.js src/browser/toasts.js` prints 0 for both. <!-- planner-discipline-allow: detectMagic -->
    - `grep -c "floorMapped\|revealFaded" src/browser/rail.js` prints 2.
    - `node --test "test/parity/**/*.test.js"` prints `# fail 0`; `git status --porcelain test/parity/fixtures` prints nothing.
    - `node --test test/unit/map-reveal.test.js` now has at least 18 tests, `# fail 0`.
  </acceptance_criteria>
  <done>The flag can never reach a parity diff, old saves load clean with the new name, and both reveal events narrate.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger Key Decision section, FIXTURE-INVENTORY carve-out note, plan gate</name>
  <files>docs/SPELLS.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - docs/SPELLS.md (the "## Map the Floor — Key Decision: re-fog provenance (Plan 04)" placeholder)
    - test/parity/FIXTURE-INVENTORY.md lines 881-960 (Phase 39's "retired-field carve-out, zero fixture moves" section — the shape for a structural carve-out note)
    - .planning/phases/40-spell-rework/40-CONTEXT.md "Area 1" (the ratified Key Decision text)
  </read_first>
  <action>
    1. docs/SPELLS.md: replace the "(appended by Plan 04)" line with: the Key Decision stated verbatim from CONTEXT Area 1 ("Only what the spell alone showed" — user-chosen, ratified; to be recorded in PROJECT.md's Key Decisions at phase close), the two research options and why A won, the mechanism (flag semantics; graduation; the single sweep on the transition; recast refresh; descend; the 40-square window and the once-a-day rule), the chip, the tolerant-load rules (stale flags; the retired-name migration), the harness carve-out, "Byte-identical elsewhere", and a Phase 41 note (render-time darkness filter over `seen` — orthogonal).

    2. FIXTURE-INVENTORY.md: "### Plan 04 — `spellSeen` provenance flag: structural carve-out, zero fixture moves" (why: lazy flag, no fixture casts the spell, `reveal()`'s delete is a no-op on unflagged cells).

    3. PLAN GATE (record in the SUMMARY): `npm test` `# fail 0` (record `# tests`); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty; the store-listing/screenshots log check prints nothing.

    4. SUMMARY: standard shape + a "## Key Decision (for PROJECT.md)" paragraph restating the ratified re-fog provenance decision in one sentence for the orchestrator + "## Human verification (deferred to end of run)" — Plan 04 items: (1) cast Map the Floor — the whole floor appears, the rail says "The floor lays itself out…", a Mapped chip shows 40 sq counting down (Plan 05 paints spell-only cells in a distinct tint); (2) walk 40 squares — the corridors you walked stay, the rest fog back with "The map forgets what it was told."; (3) recast at 20 squares — the chip resets to 40 and nothing flickers; (4) descend mid-window — the chip vanishes, the new floor is normal; (5) resume a pre-Phase-40 save that had Detect Magic — the grimoire row reads Map the Floor, no card.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && npm run build:www && git status --porcelain test/parity/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; the master hash prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0; no fixture file is modified.
    - `grep -c "(appended by Plan 04)" docs/SPELLS.md` prints 0 and `grep -c "Only what the spell alone showed" docs/SPELLS.md` is at least 1. <!-- planner-discipline-allow: (appended by Plan 04) -->
    - `grep -c "### Plan 04" test/parity/FIXTURE-INVENTORY.md` prints 1.
    - The SUMMARY contains a "## Key Decision (for PROJECT.md)" heading.
  </acceptance_criteria>
  <done>The Key Decision is written where the orchestrator will read it, the carve-out is documented, and the gate is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save → engine | tampered `spellSeen` flags / a stale `spell:reveal` record on a floor that no longer matches |
| engine → shell | `seen` flipping back to false under the map/tap-to-step surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-07 | Tampering | stale/tampered `spellSeen` on load | low | mitigate | `clearStaleSpellSeen` strips flags with no live record; a live record simply re-fogs at its expiry (worst case: cells the player never walked re-fog — acceptable, by design) |
| T-40-08 | Denial of service | tap-to-step onto a cell that just re-fogged | low | mitigate | `tapStep.js` already refuses an unseen cell with the "unwalked" rail line; `draw()` reads `seen` fresh each paint (verified) |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan |
</threat_model>

<verification>
- Tasks 1–2 suites green including the full parity glob; Task 3 gate recorded; zero fixture moves.
</verification>

<success_criteria>
- ROADMAP SC-4: the renamed reveal shows the floor for a stated number of squares, then re-fogs only what it alone revealed; walked cells stay seen; the decision is written as a Key Decision.
</success_criteria>

<output>
Create `.planning/phases/40-spell-rework/40-04-SUMMARY.md` when done (with the "## Key Decision (for PROJECT.md)" paragraph).
</output>
