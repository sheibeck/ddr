---
phase: 40-spell-rework
plan: 03
type: execute
wave: 3
depends_on: ["40-02"]
files_modified:
  - engine/derived.js
  - engine/combat.js
  - engine/magic.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - test/unit/spell-utility.test.js
  - test/unit/conditions.test.js
  - test/unit/magic.test.js
  - test/unit/combat.test.js
  - test/unit/cast-refusals.test.js
  - test/unit/usable-features-audit.test.js
  - docs/USABLE-FEATURES-AUDIT.md
  - test/parity/FIXTURE-INVENTORY.md
  - docs/SPELLS.md
autonomous: true
requirements: [SPELL-02, SPELL-07]

must_haves:
  truths:
    - "Every utility spell has an observable effect the player can point to: Mirror Self, Sense Presence, Regeneration and an armed Sense Danger each surface a `conditionsOf` chip (mirror with rounds; senses; regen; foresight) in the fixed order after `ward`; none of the ten utility kinds is a dead pick"
    - "Sense Presence means something in the engine: while `c.senses` is up no forced foe-first initiative rule (Samurai/Fridgian slow/Knight-vs-big/Court Mage round 1) applies, in addition to the existing dark to-hit waiver; it lasts until the next fight ends and its expiry narrates `sensesFaded` (Regeneration likewise narrates `regenFaded`; a still-running mirror narrates `mirrorFaded` when the fight ends)"
    - "A scroll is scribed into a Magic User's grimoire ONLY when `canCast`'s two gates (`spellLevelFor` + `schoolGate`) already pass; otherwise `scrollTooAdvanced { spell, need, have, school }` names the level needed and the scroll is cast once for free as today — a scribed spell is therefore always castable immediately"
    - "Old saves with a scribed-but-uncastable spell keep it; `canCast` refuses with the existing `spellAboveLevel`/`spellSchoolLocked` naming the level (no migration needed — verified by test)"
    - "Every new event type has EVENT_NARRATION + TOAST_FOR + RAIL_FAMILY entries and passes the voice scan; every parity fixture is byte-identical (the `scroll` scenario's Wizard has no gated school — measured)"
  artifacts:
    - "engine/derived.js — conditionsOf keys mirror/senses/regen/foresight"
    - "engine/combat.js — rollInitiative senses clause; endCombat expiry narration; combatJoined.senses"
    - "engine/magic.js — readScroll aligned with canCast + scrollTooAdvanced"
    - "test/unit/spell-utility.test.js"
  key_links:
    - "engine/magic.js utility branches (c.mirror/c.senses/c.regen/c.foresight) -> engine/derived.js#conditionsOf -> mazeworld.html paintConditions (Plan 05 adds the copy rows)"
    - "engine/combat.js#rollInitiative reads c.senses -> combatJoined { first, senses } -> narration"
    - "engine/magic.js#readScroll -> engine/derived.js#spellLevelFor + schoolGate (the same two checks canCast uses)"
---

<objective>
Make every utility spell felt (SPELL-02) and fix scroll scribing (SPELL-07): the three utility spells with real effects but zero surface (Mirror Self, Sense Presence, Regeneration — research "Utility Spell Audit") plus an armed Sense Danger get condition chips; Sense Presence's "never surprised" gets a real initiative meaning and a stated expiry; expiries narrate; `readScroll` uses `canCast`'s own two gates so a scroll can never scribe a spell the caster cannot cast, and refuses by naming the level needed.

Purpose: CONTEXT's SPELL-02 ruling ("every utility cast narrates its effect … add `conditionsOf` descriptors … `c.senses` expiry defined") and the routine SPELL-07 decision ("align it with `canCast`'s two checks … the refusal names the level").

Output: `conditionsOf` chips, the senses initiative clause, endCombat expiry events, the readScroll fix, narration for `sensesFaded`/`regenFaded`/`scrollTooAdvanced`, `test/unit/spell-utility.test.js`, the ledger's "Utility visibility and scrolls" section. No `mazeworld.html` edits (Plan 05 renders the chip copy and the Hero-tab rows).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-spell-rework/40-CONTEXT.md
@.planning/phases/40-spell-rework/40-02-SUMMARY.md
@engine/derived.js
@engine/magic.js
</context>

<standing_rules>
- Greenfield (CONTEXT "Standing rulings"): no dual paths. Fixture protocol: 40-01-PLAN.md `<standing_rules>`. Expected fixture regenerations this plan: NONE (no fixture casts a utility spell; the `scroll` scenario's seed-7 Wizard has every school at gate 1 and `spellLevelFor === sp.lvl` for every row, so the new condition is arithmetically identical for it — the suite confirms). If the suite says otherwise, STOP, measure, declare.
- `test/parity/prototype-master.js.txt` never edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- Commit convention `feat(40-03): …` / `test(40-03): …` / `docs(40-03): …` + the two attribution lines (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`); explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT: no device steps; SUMMARY has "## Human verification (deferred to end of run)".
- Every new event type: `EVENT_NARRATION` + `TOAST_FOR` + `RAIL_FAMILY`; coverage guards + voice scan green.
- `conditionsOf` stays a PURE read (no rng, no mutation, no new serialized field) — the chips read fields the engine already writes.
</standing_rules>

<artifacts_this_phase_produces>
See 40-01-PLAN.md `<artifacts_this_phase_produces>`. This plan creates every P03 entry: `conditionsOf` descriptors `mirror { remaining }` / `senses` / `regen` / `foresight`; `rollInitiative`'s `c.senses` clause; `combatJoined.senses` (additive); events `sensesFaded`, `regenFaded` (endCombat; `mirrorFaded` reused there), `scrollTooAdvanced { spell, need, have, school }`; `test/unit/spell-utility.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Utility chips (mirror/senses/regen/foresight), Sense Presence's initiative meaning, expiry narration</name>
  <files>engine/derived.js, engine/combat.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/spell-utility.test.js, test/unit/conditions.test.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/derived.js lines 464-600 (conditionsOf — the good block's fixed order: live item effects, might, ward, flight, itemCooldown, staffCharges, then bad; the JSDoc descriptor list)
    - engine/combat.js lines 125-165 (rollInitiative — the forced-foe-first clause), 372-380 (fight's `combatJoined { first }` push), 1199-1245 (endCombat — the unconditional `regen=false / mirror=0 / senses=0` resets and their fidelity comment), 2404-2410 (foeTurn's ward/mirror countdowns)
    - engine/magic.js lines 286-300 (senses/foresee/mirror/regen branches — the fields each sets)
    - test/unit/conditions.test.js (every order/shape pin — the "ward after might, before flight" and "fully-loaded stable order" tests gain the four new chips), test/unit/combat.test.js (grep `rollInitiative`/`first` for the Samurai/Fridgian/Court Mage initiative pins that the senses clause must NOT change when `c.senses` is 0)
    - src/browser/eventNarration.js lines 205-215 (combatJoined), 420-430 (wardFaded/mirrorFaded), 555-570 (sensesGained/senseDanger/mirrorSelf/regenerationCast); toasts.js 1150-1160 and 1280-1295; rail.js RAIL_FAMILY
  </read_first>
  <behavior>
    - `conditionsOf` with `c.mirror = 4` → contains `{ key: "mirror", polarity: "good", remaining: 4 }`; `c.senses = 1` → `{ key: "senses", polarity: "good" }`; `c.regen = true` → `{ key: "regen", polarity: "good" }`; `c.foresight = true` → `{ key: "foresight", polarity: "good" }`; all four sit AFTER `ward` and BEFORE `flight` in the order mirror → senses → regen → foresight; absent when 0/false; `conditionsOf` remains pure (state deep-equal before/after).
    - `rollInitiative`: a Samurai with `c.senses = 1` is no longer forced to "foe" (the two d20s are still drawn; the higher wins as for anyone); the same Samurai with `c.senses = 0` is still "foe" (existing pins byte-identical); a foreseen character is still "you"; a Court Mage round 1 with senses up rolls normally; `fight()`'s `combatJoined` carries `senses: true` only when `c.senses` is up and `first === "you"` (conditionally spread — the plain shape is byte-identical otherwise).
    - `endCombat` on a character with `regen: true` pushes `regenFaded`; with `senses: 1` pushes `sensesFaded`; with `mirror > 0` pushes `mirrorFaded`; on a bare character (all zero/false) pushes NONE of the three and every existing endCombat event pin holds.
    - Narration renders `sensesFaded` ("Your senses dull back to normal."), `regenFaded` ("The wounds stop closing on their own."), `combatJoined { senses: true }` ("You move first. Nothing gets the jump on you."); RAIL_FAMILY rows for `sensesFaded`/`regenFaded`; voice scan green.
  </behavior>
  <action>
    Write `test/unit/spell-utility.test.js` FIRST (RED) with the behaviours above (copy magic.test.js's fixture helpers; for initiative use `fakeRng([mine, theirs])` with explicit d20 values).

    engine/derived.js#conditionsOf: after the `ward` push add, in order, `if (c.mirror > 0) out.push({ key: "mirror", polarity: "good", remaining: c.mirror });`, `if (c.senses) out.push({ key: "senses", polarity: "good" });`, `if (c.regen) out.push({ key: "regen", polarity: "good" });`, `if (c.foresight) out.push({ key: "foresight", polarity: "good" });` — extend the JSDoc descriptor list (Phase 40, SPELL-02: the three invisible utility effects + the armed foresight; `mirror` counts down in foeTurn; `senses`/`regen`/`foresight` are cleared at endCombat / Fight!).

    engine/combat.js#rollInitiative: the forced clause becomes `(samurai || slow || knightBig || courtMage) && !foreseen && !c.senses ? "foe" : …` with a DELIBERATE RULES CHANGE comment (Phase 40, SPELL-02, CONTEXT "c.senses expiry defined … state it in txt": Sense Presence's canon "never surprised" had no engine read; it now waives every forced foe-first rule until the fight ends, the dark to-hit waiver in derived.js#toHit is unchanged, and the two d20s are still always drawn). fight(): `events.push({ type: "combatJoined", first, ...(c.senses && first === "you" ? { senses: true } : {}) })`. endCombat: before the three resets push the three conditional expiry events (order: regenFaded, sensesFaded, mirrorFaded) — keep the unconditional resets byte-identical (the fidelity comment stays).

    src/browser: EVENT_NARRATION/TOAST_FOR for `sensesFaded`, `regenFaded`, the `combatJoined` senses branch; RAIL_FAMILY `sensesFaded: { icon: "·", title: "SENSES FADE", tone: "dull" }`, `regenFaded: { icon: "·", title: "REGENERATION ENDS", tone: "dull" }`. Update conditions.test.js's order pins to include the four chips; combat.test.js gains one senses-initiative case beside the Samurai pin.
  </action>
  <verify>
    <automated>node --test test/unit/spell-utility.test.js test/unit/conditions.test.js test/unit/combat.test.js test/unit/identity-contract.test.js test/unit/identity-combat.test.js test/unit/afraid.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'key: "mirror"\|key: "senses"\|key: "regen"\|key: "foresight"' engine/derived.js` prints 4.
    - `grep -c "!foreseen && !c.senses" engine/combat.js` prints 1; `grep -c 'type: "sensesFaded"\|type: "regenFaded"' engine/combat.js` prints 2.
    - `grep -c "sensesFaded\|regenFaded" src/browser/rail.js` prints 2; both names appear in src/browser/eventNarration.js and src/browser/toasts.js.
    - `node -e "import('./engine/derived.js').then(m=>console.log(m.conditionsOf({c:{mirror:2,senses:1,regen:true,foresight:true,ward:{pool:10,rounds:3,name:'Shield'}}}).map(x=>x.key).join(',')))"` prints `ward,mirror,senses,regen,foresight`.
    - `node --test test/unit/spell-utility.test.js` `# fail 0` with at least 10 tests; every listed suite `# fail 0`.
  </acceptance_criteria>
  <done>Mirror Self, Sense Presence, Regeneration and an armed Sense Danger are visible as chips; Sense Presence's initiative meaning is real and its expiry is narrated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scroll scribing aligned with canCast; scrollTooAdvanced names the level; tolerant old-grimoire proof</name>
  <files>engine/magic.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/unit/spell-utility.test.js, test/unit/magic.test.js, test/unit/cast-refusals.test.js, test/unit/usable-features-audit.test.js, docs/USABLE-FEATURES-AUDIT.md</files>
  <read_first>
    - engine/magic.js lines 468-514 (readScroll — the options filter, the pick, the copy-to-grimoire condition, the free-cast path with `scrollCast`)
    - engine/derived.js lines 1154-1250 (schoolAllowed/schoolGate/canLearn/spellLevelFor/canCast)
    - .planning/phases/40-spell-rework/40-RESEARCH.md "Scroll Scribing Bug (SPELL-07)" (the five reachable level-1 cases: Warlock+Heal, Court Mage+Map the Floor, Apprentice+Map the Floor, Illusionist+Shield, Summoner+Freeze)
    - test/unit/magic.test.js (grep `readScroll` — existing scroll pins), test/unit/cast-refusals.test.js (the refusal vocabulary tests — add the scroll refusal beside `scrollRefused`), test/unit/usable-features-audit.test.js lines 400-470 (the scroll rows in CASES) and docs/USABLE-FEATURES-AUDIT.md's scroll section (grep `scrollCopiedToGrimoire`)
    - test/parity/fixtures/action-script.magic.json `scroll` scenario (seed 7 — measure it stays byte-identical)
  </read_first>
  <behavior>
    - A level-1 Warlock reading a scroll that picks Heal (healing gate 3): `scrollRead`, then `scrollTooAdvanced { spell: "Heal", need: 3, have: 1, school: "healing" }`, then `scrollCast` + the Heal effect (`healed`) — and "Heal" is NOT in the grimoire afterwards.
    - A level-1 Wizard reading a scroll that picks Heal: `scrollCopiedToGrimoire` and "Heal" in the grimoire, castable immediately (`canCast` true) — unchanged behaviour.
    - A level-1 Wizard reading a scroll that picks Fireball (lvl 3): `scrollTooAdvanced { need: 3, have: 1 }` then the free cast (in combat: the throw; out of combat: `castRefused combatOnly` as today).
    - A Fighter with Runes/Signs: no scribing, no `scrollTooAdvanced`, free cast as today. A Magic User whose sub cannot learn the school (`canLearn` false): free cast as today, no `scrollTooAdvanced`.
    - `need` is `Math.max(spellLevelFor(c.sub, sp), schoolGate(c.sub, sp.s))`; a spell already in the grimoire is never re-scribed and never `scrollTooAdvanced` (free cast).
    - Tolerant proof: a state whose grimoire already holds "Heal" on a level-1 Warlock (an old save) loads and `castSpell` refuses with `spellSchoolLocked { need: 3, have: 1 }` — no crash, no migration.
    - Every `readScroll` outcome above draws exactly one pick plus the free cast's own draws (the new branch adds ZERO draws).
  </behavior>
  <action>
    Extend `test/unit/spell-utility.test.js` FIRST (RED) — force the pick with a `fakeRng` whose first value selects the wanted option index (compute the index from `SPELLS.filter(lvl <= depth+1)` at depth 1, 16 rows after Plan 01).

    engine/magic.js#readScroll: replace the copy condition with `c.cls === "Magic User" && canLearn(c.sub, sp) && !c.grimoire.includes(sp.n)` as the outer gate; inside it, if `spellLevelFor(c.sub, sp) <= c.level && c.level >= schoolGate(c.sub, sp.s)` → scribe as today; else push `scrollTooAdvanced { spell, need: Math.max(spellLevelFor, schoolGate), have: c.level, school: sp.s }` and fall through to the existing free-cast path. Rewrite the JSDoc: DELIBERATE RULES CHANGE (Phase 40, SPELL-07): the prototype scribed on raw `sp.lvl <= level` and never checked the school gate, so five level-1 (sub, spell) pairs were scribed uncastable; the scribe gate is now exactly `canCast`'s two checks; old saves keep such entries and `canCast` names the level.

    src/browser: EVENT_NARRATION `scrollTooAdvanced: (e) => "<miss>{spell} needs level {need}; you are {have}. The scroll reads itself once and crumbles.</miss>"`, TOAST_FOR the short form (miss tone, PRIORITY.you), RAIL_FAMILY `scrollTooAdvanced: { icon: "▪", title: "TOO ADVANCED", tone: "dull" }`. docs/USABLE-FEATURES-AUDIT.md scroll section: add the `scrollTooAdvanced` line and the rule; keep the doc-sync tests green. cast-refusals.test.js: add one scroll case naming the event.
  </action>
  <verify>
    <automated>node --test test/unit/spell-utility.test.js test/unit/magic.test.js test/unit/cast-refusals.test.js test/unit/usable-features-audit.test.js test/parity/magic-parity.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "sp.lvl <= c.level" engine/magic.js` prints 0 (readScroll no longer reads the printed level) and `grep -c 'type: "scrollTooAdvanced"' engine/magic.js` prints 1. <!-- planner-discipline-allow: sp.lvl <= c.level -->
    - `grep -c "spellLevelFor(c.sub, sp) <= c.level && c.level >= schoolGate(c.sub, sp.s)" engine/magic.js` prints 1.
    - `grep -c "scrollTooAdvanced" src/browser/eventNarration.js src/browser/toasts.js src/browser/rail.js docs/USABLE-FEATURES-AUDIT.md` is at least 1 for each.
    - `node --test test/parity/magic-parity.test.js` `# fail 0` (the scroll scenario is byte-identical).
    - `node --test test/unit/spell-utility.test.js` now has at least 18 tests, `# fail 0`; every listed suite `# fail 0`.
  </acceptance_criteria>
  <done>A scribed scroll is always castable at once; an unscribable one says which level it needs and still casts itself once.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger "Utility visibility and scrolls" section, fixture check, plan gate</name>
  <files>docs/SPELLS.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - docs/SPELLS.md (the "## Utility visibility and scrolls (Plan 03)" placeholder; Plan 02's section for the shape)
    - .planning/phases/40-spell-rework/40-RESEARCH.md "Utility Spell Audit (SPELL-02)" (the ten-kind table to close out)
  </read_first>
  <action>
    1. docs/SPELLS.md: replace the "(appended by Plan 03)" line with: the ten-kind utility audit table `| Kind | Spell(s) | Effect | Surface (chip / events) | Expiry |` closed out (heal/ward/might/mirror/senses/foresee/regen/summon/turn/gate), the Sense Presence rules change (initiative waiver; expiry "till your next fight ends"; the two d20s always drawn), the endCombat expiry lines, the scroll rule (the exact condition; the five previously-reachable stuck cases; `scrollTooAdvanced`'s payload; the tolerant-load statement), "Byte-identical elsewhere".

    2. FIXTURE-INVENTORY.md: "### Plan 03 — utility visibility + scroll gate: zero fixture moves" paragraph (no fixture casts a utility spell; the scroll scenario's Wizard has every gate at 1 — measured by the suite).

    3. PLAN GATE (record in the SUMMARY): `npm test` `# fail 0` (record `# tests`); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty; the store-listing/screenshots log check prints nothing.

    4. SUMMARY: standard shape + "## Human verification (deferred to end of run)" — Plan 03 items (chips render in Plan 05; list the engine-observable checks): (1) cast Mirror Self / Sense Presence / Regeneration outside a fight — after Plan 05 each shows a chip on the map HUD; (2) with Sense Presence up, a Samurai-race-style forced "They move first." no longer happens — the Fight! line reads "You move first. Nothing gets the jump on you."; (3) after the fight ends, "Your senses dull back to normal." / "The wounds stop closing on their own." appear once; (4) a level-1 Warlock reading a Heal scroll sees "Heal needs level 3; you are 1." and is healed once; Heal is not in the grimoire.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && npm run build:www && git status --porcelain test/parity/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; the master hash prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0; no fixture file is modified.
    - `grep -c "(appended by Plan 03)" docs/SPELLS.md` prints 0 and `grep -c "## Utility visibility and scrolls (Plan 03)" docs/SPELLS.md` prints 1. <!-- planner-discipline-allow: (appended by Plan 03) -->
    - `grep -c "### Plan 03" test/parity/FIXTURE-INVENTORY.md` prints 1.
  </acceptance_criteria>
  <done>The utility audit is closed in the ledger, the scroll rule is documented, and the gate is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save → engine | an old grimoire entry that fails `canCast` forever |
| engine → shell | new chip keys the shell has no copy row for until Plan 05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-05 | Denial of service | an old save's uncastable grimoire entry | low | accept | `canCast` refuses with a named level; the entry stays (tolerant); no migration — the refusal IS the explanation |
| T-40-06 | Information disclosure | a chip key with no CONDITION_COPY row (between Plans 03 and 05) | low | mitigate | paintConditions already falls back to the raw key as the label and the `default` explanation — never throws; Plan 05 adds the rows |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan |
</threat_model>

<verification>
- Tasks 1–2 suites green; Task 3 gate recorded; zero fixture moves.
</verification>

<success_criteria>
- ROADMAP SC-2 (every utility spell observable, none dead) and SC-5 (scroll castable immediately or refused by naming the level) hold in the engine; SC-6's chip is already emitted outside combat (Plan 05 adds the Hero-tab rounds).
</success_criteria>

<output>
Create `.planning/phases/40-spell-rework/40-03-SUMMARY.md` when done.
</output>
