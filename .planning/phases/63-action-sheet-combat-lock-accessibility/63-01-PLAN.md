---
phase: 63-action-sheet-combat-lock-accessibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/gearSheet.js
  - test/unit/gear-sheet-model.test.js
  - test/voice/safety-scan.test.js
  - test/unit/hp-not-wp.test.js
autonomous: true
requirements: [GSCR-07, GSCR-08, GSCR-09, GRULE-02]

must_haves:
  truths:
    - "gearSheetModel(state, { from: 'worn', slot }) for a FILLED slot returns label '<GEAR_COPY.slot[slot]> · WORN', title = the worn piece's name, note = that slot's gearWornModel row note, why '' and actions in the fixed order USE (only when the row carries a gearUseCell) → UNEQUIP (DISCARD instead for destroyed armor) → one 'SWAP FOR <name>' per bag card whose family fits the slot, in ascending c.items index"
    - "An EMPTY worn slot returns label '<SLOT> · EMPTY', title 'NOTHING WORN', note = GEAR_COPY.empty[slot], and one 'EQUIP <name>' per fitting bag card, or exactly one greyed 'NOTHING TO EQUIP' action whose sub is the in-voice nothing line and whose run is null"
    - "A bag target { from: 'bag', i, n } returns label 'BAG · <GEAR_COPY.family[family]>' or 'BAG · USED FROM THE BAG', title = the item name, note = gearBagCardsModel's desc; a LEGAL weapon or armor card also carries why = lootCompare(c, it).line. Actions: USE (bag-only activatables only) → one 'EQUIP TO <SLOT>' (free key) or 'SWAP INTO <SLOT>' (occupied key) per key the family owns, JEWELRY 1 before JEWELRY 2 → DROP (confirm: true)"
    - "Acceptance case: newRun(7)'s Magic User (level 3, Quarter Staff, prof 0, magicWpn 0) with a Spiked Staff at c.items[0] gets a bag sheet whose why is exactly 'd8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade' and whose action labels are exactly ['SWAP INTO WEAPON', 'DROP']. With state.combat set, SWAP INTO WEAPON is enabled:false with the Phase 61 combat line as its reason, and DROP stays enabled"
    - "While gearLockReason(state) === 'combat' (the pending Fight! preview included), every EQUIP / SWAP FOR / EQUIP TO / SWAP INTO / UNEQUIP / DISCARD action is enabled:false with reason === LINE_FOR.gearRefused({ type: 'gearRefused', verb, reason: 'combat' }).text (verb 'equipItem' for equips and swaps, 'unequipSlot' for UNEQUIP and DISCARD). USE and DROP stay enabled. The same state with combat null enables them again"
    - "UNEQUIP is enabled:false with reason 'Bag is full — free a slot first.' exactly when the WORN row's unequip.blocked is true (bagUsage(c).full); DISCARD on destroyed armor is enabled on a full bag"
    - "An illegal weapon or armor candidate (class, race, Woodsman or weight rule) is LISTED, never hidden, enabled:false, with reason === lootCompare(c, it).line (the engine's weaponRefusalReason/armorRefusalReason through refusalText, never restated)"
    - "Every action except NOTHING TO EQUIP carries run = the exact engine action object the shell bridge dispatches: { type: 'useItem', slot } or { type: 'useItem', i }; { type: 'unequipSlot', slot }; { type: 'equipItem', i } for weapon/armor and { type: 'equipItem', i, slot } for cloak/jewelry1/jewelry2; { type: 'dropItem', i }. Greyed actions keep their run so a test can prove the engine refuses it the same way"
    - "USE is never greyed (the Phase 31 never-disable-silently ruling and the row's own USE cell agree); its sub reads the gearUseCell phase: ready, one-use consumable, 'Already running — n squares left.', 'Cooling down — n more squares.' (singular forms at 1) or 'Charges <k/max · n SQ>.'"
    - "gearSheetModel returns null for a malformed target, an unknown worn slot, a bag index with no card, a bag card whose name no longer matches target.n, or a bag-free item (potion/scroll/bag), so the shell can close a sheet whose target vanished. It is pure: two calls deep-equal and JSON.stringify(state) is unchanged"
    - "Edge GSCR-07/adjacency: the thief fixture's bag is exactly full, so UNEQUIP on jewelry1 is greyed with the bag-full line; the same state with one bag item removed (one free slot) enables it"
    - "Edge GSCR-07/empty: the mu fixture's empty armor slot with an empty bag yields exactly one action, NOTHING TO EQUIP (enabled false, run null); a filled slot with no fitting bag item yields zero SWAP FOR actions"
    - "Edge GSCR-07/ordering: for a filled activatable jewel with two fitting bag jewels the keys read ['use', 'unequip', 'swap:<lower i>', 'swap:<higher i>'], and a second call returns the identical order"
    - "Edge GSCR-08/adjacency: a bag jewel with jewelry1 worn and jewelry2 free yields ['SWAP INTO JEWELRY 1', 'EQUIP TO JEWELRY 2', 'DROP']; both keys worn yields two SWAP INTO; both free yields two EQUIP TO; each run targets its own key"
    - "Edge GSCR-08/empty: a bag-only item with no activation (a rope) yields exactly one action, DROP; a target index past the end of c.items yields null"
    - "Edge GSCR-08/ordering: a bag-only activatable (a Magic User's staff) yields ['use', 'drop'], USE first and DROP always last"
    - "Edge GSCR-09/adjacency: an action that is both combat-locked and illegal (or both combat-locked and bag-full) shows the COMBAT reason, because the engine checks the lock first"
    - "Edge GSCR-09/empty: every enabled action has reason '' and every greyed action has a non-empty reason equal to its sub"
  artifacts:
    - path: "src/browser/gearSheet.js"
      provides: "GEAR_SHEET_COPY (frozen, nested frozen) and the pure gearSheetModel(state, target) view model"
      contains: "export function gearSheetModel("
    - path: "test/unit/gear-sheet-model.test.js"
      provides: "behavior, acceptance, combat-lock, edge and purity suite for the sheet model"
      min_lines: 250
  key_links:
    - from: "src/browser/gearSheet.js#gearSheetModel"
      to: "src/browser/gearTab.js#gearWornModel / gearBagCardsModel"
      via: "the WORN rows (name, note, use, unequip.blocked) and bag cards (i, family, desc, use) are read, never re-derived"
      pattern: "gearWornModel\\(state\\)"
    - from: "src/browser/gearSheet.js#gearSheetModel"
      to: "engine/items.js#gearLockReason"
      via: "the combat lock predicate read on every call"
      pattern: "gearLockReason\\(state\\)"
    - from: "src/browser/gearSheet.js#gearSheetModel"
      to: "src/browser/narrationLines.js#LINE_FOR.gearRefused"
      via: "the combat reason text is the rail line's own text, never a second literal"
      pattern: "LINE_FOR\\.gearRefused\\("
    - from: "src/browser/gearSheet.js#gearSheetModel"
      to: "src/browser/viewModels.js#lootCompare"
      via: "legality, the illegal reason and the explained upgrade line"
      pattern: "lootCompare\\("
  prohibitions:
    - requirement_id: GSCR-08
      category: honesty
      flagged: true
      statement: "MUST NOT hide an illegal candidate: a weapon or armor the class, race or weight rules refuse is listed and greyed with the engine's own reason, so the player learns why rather than wondering where it went"
    - requirement_id: GRULE-02
      category: player-agency
      flagged: true
      statement: "MUST NOT grey USE or DROP because a fight is up: using an item (the torch included) and dropping one are not gear changes and stay live mid-fight"
    - requirement_id: GSCR-09
      category: honesty
      flagged: true
      statement: "MUST NOT invent a UI-only refusal reason: every greyed reason is the engine's own (the gearRefused line, lootCompare's refusal line, or the bag-full rule), and the combat line is never a second literal"
---

<objective>
Build the pure, DOM-free half of the Gear action sheet in a new module, `src/browser/gearSheet.js`:
- `GEAR_SHEET_COPY`: every new player-facing string the sheet shows, as one frozen object.
- `gearSheetModel(state, target)`: the sheet's header and ordered actions for a WORN slot or a BAG card. Each action says whether it is live, the engine's own reason when it is greyed, and the exact engine action it dispatches.

Purpose: GSCR-07, GSCR-08, GSCR-09 and GRULE-02 are decided here as data. The renderer (Plan 02), the engine-agreement sweep (Plan 03) and the shell (Plans 04–05) all read this one model, so no screen restates a rule.

Implements CONTEXT "Sheet actions and reasons" (header shape, filled/empty slot actions, DISCARD for destroyed armor, NOTHING TO EQUIP, illegal candidates listed but greyed, bag-card upgrade line, one action per named slot with both jewelry keys, DROP) and "Combat lock" (EQUIP/SWAP/UNEQUIP/DISCARD greyed with the Phase 61 line single-sourced, USE and DROP live). Module boundary and the model shape are Claude's discretion: a new `gearSheet.js` beside `gearTab.js`, returning `{ target, label, title, note, why, actions: [{ key, label, sub, reason, enabled, run, confirm }] }`.

Output: `src/browser/gearSheet.js` (copy + model), `test/unit/gear-sheet-model.test.js`, and GEAR_SHEET_COPY walked by the voice and hp-not-wp scans.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-CONTEXT.md
@.planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md
@.planning/phases/61-gear-rules-store-purchase-fix/61-01-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
From src/browser/gearTab.js (Phase 62, read-only for this plan):
- `GEAR_COPY.slot` = { weapon "WEAPON", armor "ARMOR", cloak "CLOAK", jewelry1 "JEWELRY 1", jewelry2 "JEWELRY 2" }; `GEAR_COPY.family` = { weapon, armor, cloak, jewelry } (uppercase words); `GEAR_COPY.empty[slot]` (in-voice empty lines); `GEAR_COPY.chevron` "›".
- `GEAR_WORN_ORDER` = ["weapon","armor","cloak","jewelry1","jewelry2"].
- `gearWornModel(state)` → `{ filled, max, countText, rows: [{ key, label, filled, name, note, value, use, useRef, unequip: null | { slot, blocked } }] }`. Fists = weapon row filled:false. A Cloak-of-Armor-only armor row is filled:true with name GEAR_COPY.magicPlate and unequip null. A destroyed armor row has unequip.blocked false even on a full bag.
- `gearUseCell(state, it)` → null | `{ phase: "ready"|"consumable"|"effect"|"cooldown"|"charges", label, sub, remaining }` (already embedded as row.use / card.use).
- `gearBagCardsModel(state)` → `[{ i, name, desc, family: "weapon"|"armor"|"cloak"|"jewelry"|null, tag, swap, use, useRef }]`, one per dropShelfItems(c) entry (true c.items index, bag-free kinds excluded).
From src/browser/viewModels.js: `lootCompare(c, it)` → `{ kind, legal, reason, line, why, ... }` (weapon/armor: `line` is "can't use (…)" when illegal, else the explained line ending " · not an upgrade" / " · upgrade"); `armorDisplay(c)` → `{ worn, destroyed, magic, under, ar, ... }`.
From engine/items.js: `gearLockReason(state)` → "combat" | null.
From engine/derived.js: `WORN_FAMILY_OF` (jewelry1/jewelry2 → "jewelry", cloak → "cloak"), `WORN_KEYS_OF` (jewelry → ["jewelry1","jewelry2"], cloak → ["cloak"]).
From src/browser/narrationLines.js: `LINE_FOR.gearRefused(e)` → `{ text, tone, priority }`; for verb equipItem/unequipSlot it is the Phase 61 combat line.
Engine action shapes (engine/actions.js validateAction): equipItem `{ i }` plus optional `slot` that must be jewelry1/jewelry2/cloak; unequipSlot `{ slot }`; dropItem `{ i }`; useItem `{ i }` or `{ slot }` (worn keys only).
</interfaces>
</context>

<assumption_delta_decision>
The assumption-delta scan flagged the word "optional" in ROADMAP's Phase 63 line "Discuss recommended: optional". That word describes whether a discuss pass was needed, not an optional feature. The discuss pass ran, and 63-CONTEXT.md locks every behaviour. No scope change and no plan change.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GEAR_SHEET_COPY and gearSheetModel(state, target), test-first</name>
  <files>src/browser/gearSheet.js, test/unit/gear-sheet-model.test.js</files>
  <read_first>
    - src/browser/gearTab.js (GEAR_COPY :66-143, GEAR_WORN_ORDER :249, gearUseCell :277-287, gearWornModel :298-354, gearBagCardsModel :389-405)
    - src/browser/viewModels.js (armorDisplay :28-52, refusalText :82-100, lootCompare :197-245)
    - engine/items.js (weaponRefusalReason :310-315, armorRefusalReason :329-334, gearLockReason :409-411, dropItem :742-749, equipItem :775-869, unequipSlot :883-944)
    - engine/derived.js (:182-240 WORN_KEYS_OF / WORN_FAMILY_OF / WORN_SLOTS / slotFor)
    - engine/actions.js (validateAction cases useItem / dropItem / equipItem / unequipSlot, :113-175)
    - src/browser/narrationLines.js (block() :290, GEAR_LOCK_LOOT_VERBS :301, LINE_FOR.gearRefused :1702)
    - test/unit/gear-tab-dom.test.js (fixedChar/richChar builders :194-247, a pattern to copy)
    - test/unit/upgrade-why.test.js (:24-31 and :88-96, the Spiked Staff hero built from newRun(7))
    - test/unit/harness/shellSandbox.js (fixedStates() :506-571, the thief and mu fixtures)
    - design/Mazeworld Gear.dc.html (sheetFor :325-381, layout and copy reference only; its toy rules are not canon)
  </read_first>
  <behavior>
    - GEAR_SHEET_COPY is frozen and every nested group is frozen; every leaf is a string. It carries the exact strings "NOTHING WORN", "NOTHING TO EQUIP", "USE", "UNEQUIP", "DISCARD", "DROP", "DROP IT? · tap again", "CANCEL", "Bag is full — free a slot first.", "USED FROM THE BAG", and the templates "SWAP FOR {name}", "EQUIP {name}", "EQUIP TO {slot}", "SWAP INTO {slot}".
    - Filled worn jewel (richChar-style jewelry1 with Ring of Power, bag not full, one bag jewel): label "JEWELRY 1 · WORN", title "Ring of Power", note === gearWornModel row note, why "", action keys ["use","unequip","swap:<i>"]. USE run { type: "useItem", slot: "jewelry1" }, UNEQUIP run { type: "unequipSlot", slot: "jewelry1" } with sub "Moves to the bag and takes a slot.", SWAP FOR label "SWAP FOR <bag jewel name>" with run { type: "equipItem", i, slot: "jewelry1" } and sub === that card's desc.
    - Worn weapon (fixedChar Axe, bag holding a Short Sword): no USE; UNEQUIP run { type: "unequipSlot", slot: "weapon" }; SWAP FOR run is exactly { type: "equipItem", i } with no slot key; its sub === lootCompare(c, it).line.
    - Destroyed armor (armor "Mail", ar 12, armorWP 0, armorMax 40) on a FULL bag: the second action is DISCARD, enabled, run { type: "unequipSlot", slot: "armor" }; there is no UNEQUIP.
    - Cloak-of-Armor-only armor slot (reuse gear-view-models.test.js's magic-plate fixture): label "ARMOR · EMPTY", title === GEAR_COPY.magicPlate, EQUIP actions for bag armor (none → NOTHING TO EQUIP).
    - Empty weapon slot (Fists) with a bagged weapon: label "WEAPON · EMPTY", title "NOTHING WORN", note === GEAR_COPY.empty.weapon, one action "EQUIP <name>" run { type: "equipItem", i }.
    - Bag weapon on a hero wielding one: label "BAG · WEAPON", "SWAP INTO WEAPON" sub "<worn weapon name> comes off and goes to the bag."; on a Fists hero it is "EQUIP TO WEAPON" with sub "Fills the slot and frees a bag slot.". A bag armor over destroyed worn armor reads SWAP INTO ARMOR with sub "<armor name> is scrap. It stays behind.".
    - Acceptance (Spiked Staff): newRun(7) with c.level 3, weapon "Quarter Staff", prof 0, magicWpn 0 and c.items = [{ kind: "weapon", base: "Spiked Staff", bonus: 0, n: "Spiked Staff", txt: "d8" }]; target { from: "bag", i: 0, n: "Spiked Staff" }: why === "d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade"; labels ["SWAP INTO WEAPON","DROP"]; DROP confirm true, run { type: "dropItem", i: 0 }.
    - Combat: the same state with combat { pending: true }: SWAP INTO WEAPON enabled false, reason === LINE_FOR.gearRefused({ type: "gearRefused", verb: "equipItem", reason: "combat" }).text and sub === reason; DROP enabled. On a worn jewel sheet in combat: USE enabled, UNEQUIP reason === the unequipSlot-verb gearRefused text, every SWAP FOR greyed with the equipItem-verb text. Clearing combat enables them again.
    - Illegal: a Magic User's bagged Long Sword and bagged Mail are both listed (in the weapon/armor worn sheets and on their own bag sheets), enabled false, reason === lootCompare(c, it).line, and that reason starts with "can't use".
    - USE sub by phase: a ready worn jewel → the ready line; a cooling record left 12 → "Cooling down — 12 more squares."; left 1 → "Cooling down — 1 more square."; an effect record left 23 → "Already running — 23 squares left."; left 1 → "Already running — 1 square left."; a Magic User's bag staff below max charges → "Charges " + gearUseCell(state, it).sub + "."; the torch → the one-use line. USE is enabled in every case.
    - Edge GSCR-07/adjacency, GSCR-07/empty, GSCR-07/ordering, GSCR-08/adjacency, GSCR-08/empty, GSCR-08/ordering, GSCR-09/adjacency and GSCR-09/empty each get one named test with exactly the must_haves wording (the "Edge GSCR-xx/yy:" prefix is the test-name prefix).
    - Null cases: target null, {}, { from: "worn", slot: "helm" }, { from: "bag", i: 99, n: "x" }, a name mismatch, and a bag index pointing at a kind:"potion" item all return null; a state with no c returns null.
    - Purity: every case above called twice deep-equals; JSON.stringify(state) is unchanged after the calls; a sparse `{ c: { weapon: "Fists", armor: "Nothing" } }` state never throws for every GEAR_WORN_ORDER slot.
  </behavior>
  <action>
Create `src/browser/gearSheet.js`. Keep this plan's code DOM-free; Plan 02 adds the renderer to the same file.

1. Header comment. Say what the module is: Phase 63 (GSCR-07..10, GRULE-02), the GEAR tab's bottom action sheet. Say it follows the docs/SHELL-MODULES.md contract: no window or document globals, only `host.ownerDocument` plus deps. Say this plan adds the pure copy and model and Plan 02 adds the renderer. Do not quote the Phase 61 combat line's words anywhere in this file, comments included. Refer to it only as "the Phase 61 gearRefused line". Do not name the element HTML-setter properties in any comment either, because Plan 02 gates on their absence.

2. Imports:
- `WORN_FAMILY_OF` and `WORN_KEYS_OF` from `../../engine/derived.js`
- `gearLockReason` from `../../engine/items.js`
- `GEAR_COPY`, `GEAR_WORN_ORDER`, `gearWornModel` and `gearBagCardsModel` from `./gearTab.js`
- `lootCompare` and `armorDisplay` from `./viewModels.js`
- `LINE_FOR` from `./narrationLines.js`

Import nothing else from the engine. Legality, the illegal reason and the bag-full state come only through lootCompare, gearWornModel and gearBagCardsModel (GSCR-11's no-fork rule).

3. `export const GEAR_SHEET_COPY = Object.freeze({...})`. Freeze every nested group. Groups and leaves:
- `head`:
  - `worn` "WORN"
  - `empty` "EMPTY"
  - `bag` "BAG"
  - `fromBag` "USED FROM THE BAG"
  - `sep` " · "
  - `nothingWorn` "NOTHING WORN"
- `act`:
  - `use` "USE"
  - `unequip` "UNEQUIP"
  - `discard` "DISCARD"
  - `swapFor` "SWAP FOR {name}"
  - `equip` "EQUIP {name}"
  - `equipTo` "EQUIP TO {slot}"
  - `swapInto` "SWAP INTO {slot}"
  - `drop` "DROP"
  - `dropConfirm` "DROP IT? · tap again"
  - `nothing` "NOTHING TO EQUIP"
- `sub`:
  - `unequip` "Moves to the bag and takes a slot."
  - `bagFull` "Bag is full — free a slot first."
  - `discard` "It is scrap now. Off it comes, and nothing goes in the bag."
  - `fills` "Fills the slot and frees a bag slot."
  - `comesOff` "{name} comes off and goes to the bag."
  - `scrap` "{name} is scrap. It stays behind."
  - `drop` "Gone for good. Frees a slot immediately."
  - `nothing` "Nothing in the bag fits this slot. Find something, or live without."
- `use`:
  - `ready` "Ready when you are."
  - `consumable` "One use. Then it is a memory."
  - `effect` "Already running — {n} squares left."
  - `effectOne` "Already running — 1 square left."
  - `cooldown` "Cooling down — {n} more squares."
  - `cooldownOne` "Cooling down — 1 more square."
  - `charges` "Charges {state}."
- `cancel` "CANCEL"

The house voice is Claude's discretion (deadpan, family-friendly, HP never WP). These exact strings are the ones CONTEXT and GSCR-07 lock: NOTHING WORN, NOTHING TO EQUIP, the bag-full line and the DROP confirm.

4. `export function gearSheetModel(state, target)`. Pure, null-safe, never mutates. It returns `null` or `{ target, label, title, note, why, actions }`. Each action is `{ key, label, sub, reason, enabled, run, confirm }`, and `confirm` is true only for DROP.

Shared locals:
- `lock = gearLockReason(state)`.
- `lockLine(verb)` returns `LINE_FOR.gearRefused({ type: "gearRefused", verb, reason: lock }).text` when lock is set, else "".
- `cards = gearBagCardsModel(state)`.
- `rows = gearWornModel(state).rows`, indexed by key.
- `armorD = armorDisplay(c)`.
- `equipRun(i, key)` returns `{ type: "equipItem", i }`. Add `slot: key` only when `key` is cloak, jewelry1 or jewelry2; the engine's validator rejects a slot on weapon or armor.
- `candidate(card, slotKey, kind)`: `it = c.items[card.i]`; `cmp = lootCompare(c, it)` only for weapon or armor kind; `reason = lockLine("equipItem") || (cmp && !cmp.legal ? cmp.line : "")`. Checking the lock first is the engine's own order (refuseGear runs before the legality check in equipItem). `enabled = !reason`. `sub` is the reason when greyed; otherwise `cmp.line` for weapon or armor, else `card.desc`. `key` is `${kind}:${card.i}`. The label is the swapFor or equip template with `{name}` = card.name. `run = equipRun(card.i, slotKey)`.
- `useAction(cell, run)`: key "use", label act.use, enabled true, reason "". Pick `sub` by `cell.phase`:
  - ready → use.ready
  - consumable → use.consumable
  - effect → effectOne when `cell.remaining === 1`, else effect with `{n}`
  - cooldown → cooldownOne or cooldown, the same way
  - charges → use.charges with `{state}` = cell.sub

Worn target: `target.from === "worn"` and `GEAR_WORN_ORDER.includes(target.slot)`.
- `family` = the slot itself for weapon and armor, else `WORN_FAMILY_OF[slot]`.
- `fits` = the cards whose `family` equals it, in their existing order.
- `real` = `armorD.worn` for armor, else `row.filled`. This way the Cloak of Armor's magic plate reads as an empty armor slot.
- Header:
  - label: `GEAR_COPY.slot[slot]` + sep + (worn or empty)
  - title: `row.name` when real or when row.filled, else nothingWorn
  - note: `row.note`
  - why: ""
- When real:
  - USE first, if `row.use` (run `{ type: "useItem", slot }`).
  - Then DISCARD when `slot === "armor" && armorD.destroyed`: reason `lockLine("unequipSlot")`, sub is sub.discard when enabled. Otherwise UNEQUIP: reason `lockLine("unequipSlot") || (row.unequip && row.unequip.blocked ? sub.bagFull : "")`, sub is sub.unequip when enabled. Both use run `{ type: "unequipSlot", slot }`.
  - Then one `candidate(card, slot, "swap")` per fit.
- When not real: one `candidate(card, slot, "equip")` per fit. If there are none, add a single NOTHING TO EQUIP action: key "nothing", enabled false, reason and sub both sub.nothing, run null.

Bag target: `target.from === "bag"`.
- `card = cards.find(k => k.i === target.i)`. Return null if there is no card or `card.name !== target.n`.
- `it = c.items[card.i]`; `cmp` is lootCompare only for weapon or armor.
- Header:
  - label: head.bag + sep + (family ? `GEAR_COPY.family[family]` : head.fromBag)
  - title: card.name
  - note: card.desc
  - why: `cmp.line` when `cmp && cmp.legal`, else ""
- Actions:
  - USE first, if `card.use` (run `{ type: "useItem", i: card.i }`).
  - Then one action per key when there is a family: `[family]` for weapon and armor, else `WORN_KEYS_OF[family]`.
    - `occupied`: `armorD.worn` for armor, else `rows[key].filled`.
    - label: swapInto or equipTo with `{slot}` = `GEAR_COPY.slot[key]`.
    - reason: `lockLine("equipItem") || (cmp && !cmp.legal ? cmp.line : "")`.
    - enabled sub: when occupied, use sub.scrap if `key === "armor" && armorD.destroyed`, otherwise sub.comesOff, with `{name}` = `rows[key].name`. When not occupied, sub.fills.
    - key `slot:${key}`, run `equipRun(card.i, key)`.
  - Last, DROP: key "drop", enabled true, reason "", sub sub.drop, run `{ type: "dropItem", i: card.i }`, confirm true.

Any other target returns null.

5. Write `test/unit/gear-sheet-model.test.js` first (RED), then implement (GREEN). Cover every `<behavior>` bullet with named tests. Import `newRun` from `../../engine/engine.js` for the Spiked Staff hero. Copy the fixedChar/richChar builders locally from gear-tab-dom.test.js (never import from another test file). Use `fixedStates()` from `./harness/shellSandbox.js` for the thief and mu fixtures. Assert reasons against `LINE_FOR.gearRefused(...)` and `lootCompare(...)` output rather than typed literals, except the strings the behavior list pins exactly.
  </action>
  <verify>
    <automated>node --test test/unit/gear-sheet-model.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-sheet-model.test.js` exits 0 with at least 30 passing tests, including test names that begin "Edge GSCR-07/adjacency", "Edge GSCR-07/empty", "Edge GSCR-07/ordering", "Edge GSCR-08/adjacency", "Edge GSCR-08/empty", "Edge GSCR-08/ordering", "Edge GSCR-09/adjacency" and "Edge GSCR-09/empty"
    - `grep -c "export function gearSheetModel(" src/browser/gearSheet.js` = 1 and `grep -c "export const GEAR_SHEET_COPY" src/browser/gearSheet.js` = 1
    - `grep -c "LINE_FOR.gearRefused(" src/browser/gearSheet.js` >= 1, `grep -c "gearLockReason(state)" src/browser/gearSheet.js` >= 1, `grep -c "lootCompare(" src/browser/gearSheet.js` >= 1, `grep -c "gearBagCardsModel(state)" src/browser/gearSheet.js` >= 1
    - `grep -c "change outfits" src/browser/gearSheet.js` = 0 (the combat line is single-sourced)
    - `grep -c "Bag is full — free a slot first." src/browser/gearSheet.js` = 1
    - `git diff --stat -- engine/ content/ test/parity/ mazeworld.html src/browser/gearTab.js src/browser/viewModels.js src/browser/narrationLines.js test/unit/fixtures/` prints nothing
  </acceptance_criteria>
  <done>gearSheetModel returns the locked header and action set for every WORN slot and BAG card: the combat, bag-full and illegal reasons come from the engine's own lines, runs are exact engine actions, and every edge has a named passing test.</done>
</task>

<task type="auto">
  <name>Task 2: Scan the new copy, pin the module contract, full suite</name>
  <files>test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js, test/unit/gear-sheet-model.test.js</files>
  <read_first>
    - test/voice/safety-scan.test.js (the GEAR_COPY import :66 and the recursive walkGearCopy block :350-358)
    - test/unit/hp-not-wp.test.js (imports :26-32, the walked copy-object list :131-134, the gearTab.js literal scan :256-266)
    - test/unit/gear-agreement.test.js (the no-fork source guard's comment-stripping helper and forbidden-name list, the pattern to mirror)
    - test/unit/gearTab.test.js (:113-119, the no window/document global pin, the pattern to mirror)
    - src/browser/gearSheet.js (as written by Task 1)
  </read_first>
  <action>
1. test/voice/safety-scan.test.js:
- Import `GEAR_SHEET_COPY` from `../../src/browser/gearSheet.js`.
- Right after the GEAR_COPY walk, walk every string leaf recursively. Reuse the walkGearCopy pattern: either generalise the IIFE into a named `walkCopy(obj, label)` called for both objects, or add a second IIFE. The label is "GEAR_SHEET_COPY".
- Add a comment tagged "Phase 63 (GSCR-07..10)".

2. test/unit/hp-not-wp.test.js:
- Import `GEAR_SHEET_COPY` and add it to the walked copy-object list.
- Add a sibling of the gearTab.js literal scan, named "src/browser/gearSheet.js: no string literal contains a standalone wp/WP token". It uses the same extraction and allowlist.

3. Append a "module contract" section to test/unit/gear-sheet-model.test.js. Read gearSheet.js CRLF-normalised and comments stripped, with the same order-sensitive stripper gear-agreement.test.js uses. Pin:
- (a) no `window.` and no `document.` global read;
- (b) the no-fork list: none of `bagCap(`, `canStow(`, `slotItems(`, `takesBagSlot(`, `isReady(`, `remaining(`, `activationFor(`, `itemTimerId(`, `chargesTimerId(`, `armorSoak(`, `weaponUpgradeDelta(`, `armorUpgradeDelta(`, `expectedStrike(`, `weaponRefusalReason(`, `armorRefusalReason(` is called;
- (c) the Phase 61 combat line's text, read at test time from `LINE_FOR.gearRefused({ type: "gearRefused", verb: "equipItem", reason: "combat" }).text`, does not appear as a literal in the stripped source;
- (d) GEAR_SHEET_COPY and every nested object are frozen.

4. Run `npm run build:www` and then `npm test`. The suite must be green. In a fresh worktree, only the known CRLF false-fails in class-pass-ledger.test.js and flee-ledger.test.js are tolerated; master is the source of truth.
  </action>
  <verify>
    <automated>node --test test/unit/gear-sheet-model.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js && npm run build:www && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "GEAR_SHEET_COPY" test/voice/safety-scan.test.js` >= 2 and `grep -c "GEAR_SHEET_COPY" test/unit/hp-not-wp.test.js` >= 2
    - `grep -c "gearSheet.js" test/unit/hp-not-wp.test.js` >= 1
    - `node --test test/unit/gear-sheet-model.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js` exits 0
    - `npm run build:www` exits 0, and then `npm test` reports fail 0 (worktree CRLF false-fails in class-pass-ledger/flee-ledger excepted)
    - `git diff --stat -- engine/ content/ test/parity/ mazeworld.html test/unit/fixtures/` prints nothing
  </acceptance_criteria>
  <done>The new copy passes the family-friendly and HP-not-WP scans. The module is pinned to its contract (no globals, no forked rule, no second combat literal). The full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file → view model | item names, txt and timer records come from a deserialized, tamperable save and flow into the model's strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-01 | Tampering | gearSheetModel | low | mitigate | Item strings pass through as plain data, never markup. Plan 02 renders them only via textContent. The model is null-safe on a sparse or legacy c (purity tests) and returns null for any unresolvable target |
| T-63-02 | Elevation of privilege | a greyed action's run descriptor | low | mitigate | The model only describes. The engine re-checks every gate on dispatch (refuseGear, equipRejected, stowItem), so a shell that wired a greyed action anyway still cannot change gear mid-fight. Plan 03 proves the engine refuses each greyed run with the same reason |
</threat_model>

<verification>
- `node --test test/unit/gear-sheet-model.test.js` green, with every Edge GSCR-07/08/09 name present
- `npm run build:www && npm test` green (fail 0)
- No byte changes to engine/, content/, test/parity/, mazeworld.html, gearTab.js, viewModels.js, narrationLines.js or any snapshot fixture
</verification>

<success_criteria>
- One pure model decides every sheet header, action, greyed reason and dispatch; every reason is the engine's own text
- The acceptance example (Spiked Staff, mid-fight greying, DROP live) is a passing test
</success_criteria>

## Source Coverage Audit (phase-wide)

| Source | Item | Plan |
|---|---|---|
| GOAL | One bottom sheet drives every equip/swap/unequip/use/drop decision on the Gear tab | 01 model, 02 renderer, 04 shell, 05 rows |
| GOAL | Engine's real reason on every greyed row | 01 (reasons from LINE_FOR/lootCompare/bag rule), 03 (engine agreement sweep) |
| GOAL | Combat lock reflected live | 01 (gearLockReason per call), 04 (paint re-renders the open sheet) |
| GOAL | Reduced motion + TalkBack usable | 04 (showPanel/hidePanel, dialog ARIA, focus), 02 (button semantics) |
| REQ | GSCR-07 worn-slot sheet: SLOT · WORN/EMPTY, USE with state, UNEQUIP bag-full line, SWAP FOR, EQUIP, NOTHING TO EQUIP | 01 model, 02 render, 05 row opens it |
| REQ | GSCR-08 bag-card sheet: USE for never-worn items, EQUIP TO/SWAP INTO per named slot (both jewelry), DROP; replaces the inline confirms | 01 model, 02 render + tap-again DROP, 05 interim removal |
| REQ | GSCR-09 engine reason on every greyed action; outcomes on the rail only; no toasts or inline text | 01 reasons, 02 closes before dispatch with no result text, 03 itemDropped/itemUnequipped reach the rail + agreement sweep |
| REQ | GSCR-10 CANCEL / backdrop / Android back close; reduced-motion rise and fade; TalkBack title and actions | 02 CANCEL + a11y attributes, 04 scrim + back handler + motion + dialog focus |
| REQ | GRULE-02 EQUIP/SWAP/UNEQUIP greyed in a fight, USE live, work again after | 01 model, 03 engine sweep (after-fight), 04 live re-render |
| CONTEXT | What opens a sheet: WORN rows (all five, empty ones too) and BAG cards; inline USE stays direct (stopPropagation); consumables get no sheet | 05 |
| CONTEXT | Camp-sheet pattern: scrim, showPanel/hidePanel, armEncounterButtons + guardTap, lastDismissAt, back-handler modal list; module under src/browser following the Phase 47 contract | 02 (guardTap via deps), 04 (lifecycle) |
| CONTEXT | Interim row buttons removed; renderCarriedList byte-identical; store/loot/hero snapshots unmoved | 05 |
| CONTEXT | DROP arms a tap-again confirm ("DROP IT? · tap again", 3 s, DOM-local), second tap dispatches and closes | 01 copy/confirm flag, 02 behaviour |
| CONTEXT | Header: SLOT · WORN / SLOT · EMPTY / BAG · FAMILY or USED FROM THE BAG, title, note | 01 |
| CONTEXT | Filled slot: USE (state sub), UNEQUIP (bag-full greyed), SWAP FOR per fitting item; destroyed armor → DISCARD | 01 |
| CONTEXT | Empty slot: EQUIP per fitting item or greyed NOTHING TO EQUIP in voice | 01 |
| CONTEXT | Illegal candidates listed but greyed with refusalText via weaponRefusalReason/armorRefusalReason | 01 (through lootCompare), 03 (agreement) |
| CONTEXT | Bag card: explained upgrade line (lootCompare.line) in the header note area; EQUIP TO/SWAP INTO per named slot; DROP; bag-only items USE (if activatable) + DROP | 01, 02 (why element) |
| CONTEXT | Outcome to the rail only through tabDeps bridges; sheet closes; no toast, no in-sheet result; greyed tap does nothing | 02, 03 |
| CONTEXT | Combat: greyed with the single-sourced Phase 61 line; USE/DROP live; re-renders greyed in place if a fight starts; works after | 01, 03, 04 |
| CONTEXT | Closing: CANCEL, scrim, back; focus returns to the opening row | 02 (CANCEL), 04 (scrim, back, focus), 05 (row ids) |
| CONTEXT | TalkBack: role=dialog + aria-modal + aria-labelledby, focus to title, real buttons, aria-disabled with the reason as description, rows announced as buttons with an "opens actions" hint | 02, 04, 05 |
| CONTEXT | Motion via Phase 58 showPanel/hidePanel with the reduced-motion snap; every font size calc(rem * --mw-text-scale) | 04 |
| CONTEXT | Standing: presentation only; rail only; HP not WP; createElement/textContent only; greenfield with declared snapshot regeneration | every plan; 04/05 snapshots |
| CONTEXT (deferred, excluded) | Mid-fight READ on the Gear tab vs the combat submenu; the --mw-font-* token bug | not planned |
| RESEARCH | none (roadmap: no research) | n/a |

## Probe fallback record (spec-less, --auto)

The edge report lists 13 applicable rows. 11 are covered with defensible criteria, 0 are backstopped, 0 are dismissed, and 2 are unclassified and carried as flagged assumptions:
- This plan (8): GSCR-07 adjacency/empty/ordering, GSCR-08 adjacency/empty/ordering, GSCR-09 adjacency/empty.
- Plan 02 (2): GSCR-08/encoding (hostile names render as text in the title, labels and subs) and GSCR-09/ordering (the sheet closes before the dispatch, so the rail speaks from the post-action state and the sheet never re-renders stale).
- Plan 03 (1): GSCR-09/encoding (every greyed reason is byte-equal to the engine's own refusal text, with no markup).
- GSCR-10 and GRULE-02 are `unclassified`. Both stay UNRESOLVED and are surfaced as flagged assumptions in Plan 04's must_haves, never auto-backstopped. The sandbox and source-pin proofs there cover the logic. The on-device TalkBack reading, the hardware back button and the in-hand feel are Phase 64 (GSCR-12) device checks.

Totals: 8 + 2 + 1 = 11 authored, plus 2 flagged-unresolved, for 13 surfaced. No silent drop.

Prohibitions: 10 are kept across the five plans (3 here, 2 in Plan 02, 1 in Plan 03, 2 in Plan 04, 2 in Plan 05), all flagged-unverified with no check descriptor. Canon referral: HTML injection through item names is covered by /gsd-secure-phase and the T-38-11 createElement/textContent convention, so it is not minted here. Plan 02 keeps it as a tested truth.

## Artifacts this phase produces

This plan's share. Plans 02–05 list theirs.

| Artifact | Kind | Where |
|---|---|---|
| GEAR_SHEET_COPY | frozen copy object (voice + hp-not-wp scanned) | src/browser/gearSheet.js |
| gearSheetModel(state, target) | pure view model: header + ordered actions with enabled/reason/run/confirm | src/browser/gearSheet.js |
| test/unit/gear-sheet-model.test.js | new unit suite (behavior, acceptance, combat lock, edges, purity, module contract) | test/unit/ |
| voice + hp-not-wp scan coverage of GEAR_SHEET_COPY and gearSheet.js literals | test extension | test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js |

<output>
Create `.planning/phases/63-action-sheet-combat-lock-accessibility/63-01-SUMMARY.md` when done. Record the exact return shape of gearSheetModel and every GEAR_SHEET_COPY leaf, because Plans 02–05 read them. Also record the test count, the `npm test` result, and confirmation that no fixture, engine, content or parity byte moved.
</output>
