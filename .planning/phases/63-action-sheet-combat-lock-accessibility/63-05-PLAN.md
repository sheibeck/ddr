---
phase: 63-action-sheet-combat-lock-accessibility
plan: 05
type: execute
wave: 4
depends_on: ["63-03", "63-04"]
files_modified:
  - src/browser/gearTab.js
  - mazeworld.html
  - test/unit/gear-tab-dom.test.js
  - test/unit/gear-agreement.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/gear-panels.test.js
  - test/unit/gear-sheet-shell.test.js
  - test/unit/shell-tab-snapshots.test.js
  - test/unit/fixtures/shell-snapshots/thief.gear.txt
  - test/unit/fixtures/shell-snapshots/mu.gear.txt
  - test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt
  - docs/SHELL-MODULES.md
  - docs/GEAR-SLOTS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GSCR-07, GSCR-08, GSCR-09, GSCR-10]

must_haves:
  truths:
    - "Tapping any of the five WORN rows, filled or empty, calls deps.openGearSheet({ from: 'worn', slot }, 'gear-open-<slot>'); tapping a BAG card calls deps.openGearSheet({ from: 'bag', i, n: <name> }, 'gear-open-bag-<i>'); CONSUMABLES cards open nothing and keep their own USE/READ buttons"
    - "Each openable row's main block (.mw-gear-main / .mw-gear-card-main) is the accessible opener: id gear-open-<slot> or gear-open-bag-<i>, role='button', tabindex='0', aria-haspopup='dialog', an sr-only 'opens actions' hint (GEAR_COPY.opensHint), and Enter or Space opens the sheet; the openable li carries mw-gear-tap (cursor pointer, a focus-visible outline on the opener)"
    - "The inline USE / ACTIVE / COOLING button still acts directly (useItem({ slot }) on a WORN row, useItem(i) on a bag card) and calls event.stopPropagation(), so the same tap never also opens the sheet"
    - "Greenfield: no in-row Unequip or 'Bag full' button, no harvested Equip / swap confirm / Drop, and no .mw-gear-actions row under any of the ten gear ids; renderGearTab no longer calls the shared carried-item list; GEAR_COPY's act group is deleted"
    - "renderCarriedList's function body is byte-identical to the Phase 62 close (commit 4adc5e4), and the thief.hero, mu.hero, thief-store.store and mu-store.store fixtures are byte-identical; thief.gear and mu.gear are regenerated deliberately, and thief.gear-confirms is deleted with its test because the Plan 04 sheet snapshots replace it"
    - "End to end in the sandbox: tapping the thief's bagged-jewel card opens the sheet (SWAP INTO JEWELRY 1, SWAP INTO JEWELRY 2, DROP); after the arm window, SWAP INTO JEWELRY 1 closes the sheet and then calls window.mzEquipItem(i, 'jewelry1'); focus returns to gear-open-bag-<i> on the next tick"
    - "Phase gates hold: npm test is green, build:www and boot:check pass, engine/ content/ test/parity/ are byte-identical to 4adc5e4, the prototype master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0, and REQUIREMENTS.md marks GSCR-07, GSCR-08, GSCR-09, GSCR-10 and GRULE-02 complete"
  artifacts:
    - path: "src/browser/gearTab.js"
      provides: "renderGearTab rows and cards open the action sheet; interim in-row actions removed; GEAR_COPY.opensHint"
      contains: "deps.openGearSheet"
    - path: "test/unit/fixtures/shell-snapshots/thief.gear.txt"
      provides: "regenerated gear snapshot (declared)"
      contains: "gear-open-jewelry1"
  key_links:
    - from: "src/browser/gearTab.js#renderGearTab"
      to: "mazeworld.html#openGearSheet"
      via: "deps.openGearSheet(target, openerId), the tabDeps closure Plan 04 added"
      pattern: "deps\\.openGearSheet\\?\\.\\("
    - from: "src/browser/gearTab.js#useCellEl"
      to: "deps.useItem"
      via: "a direct tap that stops propagation"
      pattern: "stopPropagation"
  prohibitions:
    - requirement_id: GSCR-08
      category: consistency
      flagged: true
      statement: "MUST NOT keep a second equip / swap / unequip / drop path on the Gear tab beside the sheet: greenfield, one decision surface, no hidden in-row fallbacks"
    - requirement_id: GSCR-07
      category: values
      flagged: true
      statement: "MUST NOT show the token WP anywhere on the Gear tab or in the sheet: player text says HP"
---

<objective>
Finish the phase:
- Make every WORN row and BAG card open the action sheet, with accessible opener semantics and the inline USE button still direct.
- Delete the Phase 62 interim in-row actions (greenfield), leaving `renderCarriedList` byte-identical for the store, combat and loot hosts.
- Migrate the tests written against the interim actions.
- Regenerate the two Gear snapshots, and delete the confirms snapshot the sheet snapshots replace.
- Catch the docs up, mark the requirements, and run the phase gates.

Purpose: GSCR-07/08 become real player flows (tap a slot or a card, get the sheet). GSCR-08's "replaces the inline two-tap Drop and swap confirms" is literally true. GSCR-09's "no inline refusal text on rows" holds: the interim "Bag full" button is gone. GSCR-10's focus return and the "opens actions" hint land on the rows.

Implements CONTEXT: "What opens a sheet" (WORN rows, all five including empty ones, and BAG cards; the whole row is the target with the › chevron; the inline USE acts directly with stopPropagation; consumables get no sheet), "Interim row buttons are removed" (renderCarriedList byte-identical; store/loot snapshots must not move), "TalkBack … Openable rows are announced as buttons with an 'opens actions' hint", and "Standing rulings" (greenfield; the gear snapshots regenerated and named, with thief.gear-confirms becoming a sheet snapshot).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-CONTEXT.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-03-SUMMARY.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-04-SUMMARY.md
@.planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
From Plan 04: tabDeps() now has `openGearSheet(target, openerId)` and `closeGearSheet()`; `context.openGearSheet` / `closeGearSheet` / `refreshGearSheet` exist in the sandbox; SNAPSHOT_IDS.gearSheet; fixtures thief.gear-sheet-bag.txt / thief.gear-sheet-worn.txt.
Target shapes: `{ from: "worn", slot }` (slot in GEAR_WORN_ORDER), `{ from: "bag", i, n }` (i = gearBagCardsModel card.i, n = card.name).
src/browser/gearTab.js today (Phase 62):
- GEAR_COPY :66-143, including the `act` group { unequip, bagFull }.
- renderGearTab :773-938: the el/head/useCellEl/chevron closures; WORN rows :820-847 with the in-row unequip actions block :835-844; bag cards :863-894 with the per-card harvest through the shared carried-item list :887-890; CONSUMABLES :896-926.
- renderCarriedList :570-756 (must stay byte-identical).
Phase base for the byte-identity gates: commit 4adc5e4 (Phase 62 closed; the Phase 63 plans are docs-only on top).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rows and cards open the sheet; interim in-row actions removed; row CSS and the #screen-gear comment</name>
  <files>src/browser/gearTab.js, mazeworld.html</files>
  <read_first>
    - src/browser/gearTab.js (GEAR_COPY :44-143 and its doc comment; renderGearTab :758-938 in full; renderCarriedList :555-756, read only, to confirm nothing there is touched)
    - mazeworld.html (:250-305 the Phase 62 Gear CSS block; :1885-1927 the #screen-gear markup and its comment)
    - src/browser/gearSheet.js (target shapes the model resolves)
    - test/unit/gear-agreement.test.js (the no-fork guard's renderGearTab-region forbidden list, which the new code must respect)
  </read_first>
  <action>
In `src/browser/gearTab.js`:

1. GEAR_COPY. Delete the `act` leaf group (unequip, bagFull). Its only consumer was the interim in-row button. Add `opensHint: "opens actions"`. Extend the doc comment with a "Phase 63 (GSCR-07/08/10)" note: the act group is gone with the in-row actions, and opensHint is the rows' TalkBack hint. In that note, name the deleted group in words, never with its dotted property path; the acceptance grep counts comments. The same goes for comments about the removed harvest call and its flag.

2. renderGearTab. Add one more local closure beside el/head/useCellEl/chevron:
- `opener(main, target, openerId)`:
  - sets `main.id = openerId`;
  - sets the attributes role "button", tabindex "0" and aria-haspopup "dialog";
  - appends `el("span", "sr-only", GEAR_COPY.opensHint)` to main;
  - defines `open = () => deps.openGearSheet?.(target, openerId)`;
  - sets `main.onkeydown = (e) => { if (e && (e.key === "Enter" || e.key === " ")) { e.preventDefault?.(); open(); } }`;
  - returns `open`.

3. WORN rows:
- Give every row (filled and empty) the className `"mw-gear-row mw-gear-tap"` plus `" mw-gear-row-empty"` when empty.
- After building `main`, set `li.onclick = opener(main, { from: "worn", slot: row.key }, "gear-open-" + row.key)`.
- DELETE the whole `if (row.unequip) { … }` in-row actions block. The children stay slot, main, value, optional USE cell, chevron.

4. useCellEl. The button handler becomes `(e) => { e?.stopPropagation?.(); deps.useItem?.(ref); }`, so a USE tap never bubbles to the row's open handler.

5. Bag cards:
- className `"mw-gear-card mw-gear-tap"`.
- After building the card's `.mw-gear-card-main`, set `li.onclick = opener(main, { from: "bag", i: card.i, n: card.name }, "gear-open-bag-" + card.i)`.
- DELETE the per-card temporary list and the shared carried-item list call that harvested the Equip / swap-confirm / Drop row. After this, renderGearTab calls that shared renderer nowhere.
- Remove the local `c` binding if nothing else in renderGearTab still reads it.

6. Leave CONSUMABLES and ALSO ON YOU exactly as they are: no opener, no mw-gear-tap.

7. Update renderGearTab's doc comment: Phase 63, rows and cards open the bottom action sheet through deps.openGearSheet; the inline USE acts directly; the in-row actions and the per-card harvest are gone; the shared carried-item list now serves only the store sell list, the combat ITEMS list and the loot card. Do not edit anything between `export function renderCarriedList(` and its closing brace, nor the DROP/SWAP confirm trios above it. They stay byte-identical for the other hosts.

In `mazeworld.html`:

8. CSS. After the Phase 62 Gear block's last rule (`.mw-gear-kit li{…}`), add:
- `.mw-gear-tap{cursor:pointer;touch-action:manipulation}`
- `.mw-gear-tap [role="button"]:focus-visible{outline:2px solid var(--ditto);outline-offset:2px}`

Add no transition, no animation and no ARIA-state selector.

9. The #screen-gear markup comment. Replace the sentence that says renderCarriedList supplies each bag card's actions "until Phase 63's bottom action sheet makes the rows tappable themselves". The new text says: since Phase 63 every WORN row and BAG card opens the bottom action sheet (#mw-gear-sheet); the inline USE button acts directly; consumables keep their own USE/READ buttons.
  </action>
  <verify>
    <automated>node --test test/unit/gear-sheet-model.test.js test/unit/gear-sheet-dom.test.js test/unit/gear-view-models.test.js test/unit/gearTab.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deps.openGearSheet?.(" src/browser/gearTab.js` >= 1, `grep -c "stopPropagation" src/browser/gearTab.js` >= 1, `grep -c 'opensHint: "opens actions"' src/browser/gearTab.js` = 1
    - `grep -c "renderCarriedList(tmp" src/browser/gearTab.js` = 0 and `grep -c "gearRow: true" src/browser/gearTab.js` = 0
    - `grep -c "GEAR_COPY.act" src/browser/gearTab.js` = 0
    - `grep -c "^\.mw-gear-tap{" mazeworld.html` = 1
    - `git show 4adc5e4:src/browser/gearTab.js | node -e "const fs=require('fs');const body=t=>{t=t.replace(/\r\n/g,'\n');const i=t.indexOf('export function renderCarriedList(');return t.slice(i,t.indexOf('\n}\n',i))};process.exit(body(fs.readFileSync(0,'utf8'))===body(fs.readFileSync('src/browser/gearTab.js','utf8'))?0:1)"` exits 0 (renderCarriedList byte-identical)
    - the four named suites exit 0
  </acceptance_criteria>
  <done>Every WORN row and BAG card is an accessible opener for the sheet, USE still acts in place, and not one interim in-row action remains. The shared carried-item list is untouched for the store, combat and loot.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the interim-action tests, end-to-end row → sheet tests, declared snapshot regeneration</name>
  <files>test/unit/gear-tab-dom.test.js, test/unit/gear-agreement.test.js, test/unit/shell-worn-slots.test.js, test/unit/gear-panels.test.js, test/unit/gear-sheet-shell.test.js, test/unit/shell-tab-snapshots.test.js, test/unit/fixtures/shell-snapshots/thief.gear.txt, test/unit/fixtures/shell-snapshots/mu.gear.txt, test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt</files>
  <read_first>
    - test/unit/gear-tab-dom.test.js (:150-190 helpers; :306-354 the ordering, USE-cell and unequip tests; :397-463 the bag-card tests including the harvested-confirms test)
    - test/unit/gear-agreement.test.js (:1-30 the header; :344-380 the Edge GSCR-11/concurrency test)
    - test/unit/shell-worn-slots.test.js (:1-30 the header list; :142-190 the "gearRow: true appears exactly once" and Unequip tests)
    - test/unit/gear-panels.test.js (:38-125 the GEAR_COPY deep-equal pin)
    - test/unit/shell-tab-snapshots.test.js (the whole file)
    - test/unit/gear-sheet-shell.test.js (Plan 04's sandbox helpers)
  </read_first>
  <action>
1. test/unit/gear-tab-dom.test.js:
- Add `openGearSheet: spy()` to gearDeps().
- Re-point "Edge GSCR-03/ordering" to the children classes ["mw-gear-slot","mw-gear-main","mw-gear-val","mw-gear-use","mw-gear-chev"].
- Replace the "WORN unequip" test with "WORN rows open the sheet". For all five rows of richChar and of the mu fixture: li.onclick() calls openGearSheet with ({ from: "worn", slot }, "gear-open-<slot>"); the main block carries id, role "button", tabindex "0", aria-haspopup "dialog" and an sr-only child whose text is GEAR_COPY.opensHint; `main.onkeydown({ key: "Enter", preventDefault(){} })` and `{ key: " " }` each open it once; `{ key: "a" }` does not.
- Extend the WORN USE-cell test: `btn.onclick({ stopPropagation: s })` calls s once, calls useItem({ slot }), and leaves openGearSheet uncalled.
- Replace the harvested-confirms test with "BAG cards open the sheet": each card's onclick opens ({ from: "bag", i: card.i, n: card.name }, "gear-open-bag-<i>").
- Add "greenfield": no node under SNAPSHOT_IDS.gear has class mw-gear-actions, mw-gear-act, mw-gear-drop, mw-drop-confirm or mw-swap-confirm; the comment-stripped renderGearTab region has no call to the shared carried-item list; CONSUMABLES li elements carry no mw-gear-tap and no onclick.

2. test/unit/gear-agreement.test.js. Replace the Edge GSCR-11/concurrency test with a Phase 63 sheet-level one, keeping the "Edge GSCR-11/concurrency" name prefix: paint the thief; open the bag-jewel card's sheet, then the rope card's (programmatically through the rows' onclick). Exactly one sheet is shown with the rope's title. A fresh paint() keeps it rendered with the same title. No in-row confirm exists under #gear-bag or #gear-worn. Update the header comment's "Phase 63 revisits combat-time greying" line to point at gear-sheet-model / gear-sheet-agreement.

3. test/unit/shell-worn-slots.test.js:
- Re-point the :142 test, whose title says the gearRow flag literal appears exactly once in gearTab.js inside renderGearTab's region. It should now assert zero occurrences in gearTab.js, because Phase 63 removed the per-card harvest. Keep the GEAR_WORN_ORDER half.
- Replace the Unequip test with "Worn rows: tapping a worn jewel's row opens the sheet on that slot".
- Update the header list item.

4. test/unit/gear-panels.test.js. The GEAR_COPY deep-equal drops the act group and adds `opensHint: "opens actions"`.

5. test/unit/gear-sheet-shell.test.js. Add the end-to-end sandbox tests:
- Paint the thief with a fake clock, and stub `context.window.mzEquipItem` with a spy that records whether the sheet was already closing or hidden.
- Click the bagged-jewel card's li: the sheet opens with SWAP INTO JEWELRY 1 / SWAP INTO JEWELRY 2 / DROP.
- `clock.advance(ARM_DELAY_MS)`, then click SWAP INTO JEWELRY 1: the spy got (i, "jewelry1") and saw the sheet closing.
- A focus spy on `doc.getElementById("gear-open-bag-" + i)` fires after `clock.advance(1)`.
- A second test: clicking a WORN row's USE button (with a stopPropagation stub) never opens the sheet.

6. Snapshots:
- Delete the "thief.gear-confirms" test and `git rm test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt`.
- Update the guard list and its title and count (nine → eight), and the file header: Phase 63 removed the interim confirms, and the sheet snapshots (thief.gear-sheet-bag/-worn) replace them.
- Regenerate ONLY the two Gear fixtures: `MZ_SNAPSHOT_UPDATE=1 node --test --test-name-pattern="(thief|mu)\.gear —" test/unit/shell-tab-snapshots.test.js`. If the name pattern needs adjusting, adjust it; never capture the whole file.
- Confirm `git status --short test/unit/fixtures/shell-snapshots/` shows exactly thief.gear.txt and mu.gear.txt modified and thief.gear-confirms.txt deleted.
- Re-run the file without the env var: it must be green.

7. Run `npm run build:www && npm test`. Other suites may still pin the interim actions (for example shell-clarity-43, shell-gear-39, shell-armor-display, shell-loot-screen, tools/stale-terms.mjs allow-list entries, or anything grepping "Unequip" / "mw-gear-actions" in the Gear region). Re-point each to the sheet behaviour as a Rule 1 fix and name it in the SUMMARY. Never re-point a store, loot, combat or hero assertion; those surfaces must not move.
  </action>
  <verify>
    <automated>npm run build:www && node --test test/unit/gear-tab-dom.test.js test/unit/gear-agreement.test.js test/unit/shell-worn-slots.test.js test/unit/gear-panels.test.js test/unit/gear-sheet-shell.test.js test/unit/shell-tab-snapshots.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - the six named suites exit 0, and `npm test` reports fail 0 (worktree CRLF false-fails in class-pass-ledger/flee-ledger excepted)
    - `test -f test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt` fails (file deleted), and `ls test/unit/fixtures/shell-snapshots/ | wc -l` prints 8 (six survivors plus Plan 04's two sheet fixtures)
    - `grep -c "gear-open-jewelry1" test/unit/fixtures/shell-snapshots/thief.gear.txt` >= 1 and `grep -c "opens actions" test/unit/fixtures/shell-snapshots/thief.gear.txt` >= 5 (one hint per WORN row)
    - `git diff --stat 4adc5e4 -- test/unit/fixtures/shell-snapshots/thief.hero.txt test/unit/fixtures/shell-snapshots/mu.hero.txt test/unit/fixtures/shell-snapshots/thief-store.store.txt test/unit/fixtures/shell-snapshots/mu-store.store.txt` prints nothing
  </acceptance_criteria>
  <done>Every test that assumed the interim in-row actions now proves the sheet flow instead, the row-to-sheet-to-dispatch path is covered end to end, and only the two declared Gear fixtures moved (plus the one retired confirms fixture). Hero, store and loot are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Docs, requirements and the phase gates</name>
  <files>docs/SHELL-MODULES.md, docs/GEAR-SLOTS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/SHELL-MODULES.md ("What stays shared", the interim in-row sentence at the end of the Phase 62 paragraph, and Plan 04's Gear action sheet subsection)
    - docs/GEAR-SLOTS.md (§4 combat gear lock :205-225 and the dated Phase 62 update note)
    - .planning/REQUIREMENTS.md (:20-30 the GSCR/GRULE checkboxes; :53-75 Traceability)
    - .planning/phases/63-action-sheet-combat-lock-accessibility/63-03-SUMMARY.md (the new rail lines to document)
  </read_first>
  <action>
1. docs/SHELL-MODULES.md. Replace the last sentence of the Phase 62 paragraph under "What stays shared", the one saying the Gear tab's interim in-row confirms are renderCarriedList's own, harvested per bag card, pending this phase. The new text says: since Phase 63, every WORN row and BAG card opens the action sheet (gearSheet.js) and the Gear tab no longer calls renderCarriedList. The shared list now serves the store sell list, the combat ITEMS list and the loot card, and its gearRow branch has no live caller; it is kept byte-identical by the standing ruling, a cleanup candidate for a later quick task.

2. docs/GEAR-SLOTS.md. After the dated Phase 62 note, add a dated "Phase 63 update (2026-09-23, v1.9)" note covering:
- every equip, swap, unequip, use and drop decision on the Gear tab goes through the bottom action sheet;
- EQUIP / SWAP / UNEQUIP / DISCARD are greyed mid-fight with the engine's own gearRefused line, read through gearLockReason;
- USE and DROP stay live;
- illegal candidates are listed and greyed with lootCompare's refusal line;
- DROP needs a second tap;
- itemDropped and itemUnequipped now narrate on the rail.

3. .planning/REQUIREMENTS.md. Tick GSCR-07, GSCR-08, GSCR-09, GSCR-10 and GRULE-02 (`- [x]`) and set their Traceability rows to "Complete". The same precedent 62-03 set for GSCR-11: this plan's proofs are the evidence.

4. Phase gates. Run each and record the output in the SUMMARY:
- `npm run build:www`
- `npm test` (fail 0; master is the source of truth for the known worktree CRLF noise)
- `npm run boot:check` (four checks; re-run once on the documented graves-timing flake)
- `git diff --stat 4adc5e4 -- engine/ content/ test/parity/` (must print nothing)
- `git hash-object test/parity/prototype-master.js.txt` (must print a1f4d0dc29782218d8e5aab65bc5989c33f917f0)
- the Task 1 renderCarriedList byte-identity command

In the SUMMARY:
- List every snapshot fixture that moved (thief.gear, mu.gear regenerated; thief.gear-confirms deleted; thief.gear-sheet-bag and thief.gear-sheet-worn added in Plan 04) with its rationale.
- List every test re-pointed as a Rule 1 fix.
- Carry forward for Phase 64's device batch: TalkBack reading of the sheet, the hardware back button, reduced motion on device, and the mid-fight greying.
  </action>
  <verify>
    <automated>npm run build:www && npm test && npm run boot:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\[x\] \*\*GSCR-07\*\*" .planning/REQUIREMENTS.md` = 1, and likewise for GSCR-08, GSCR-09, GSCR-10 and GRULE-02; `grep -c "| GSCR-10 | Phase 63 | Complete |" .planning/REQUIREMENTS.md` = 1
    - `grep -c "Phase 63 update" docs/GEAR-SLOTS.md` >= 1
    - `grep -c "until the Phase 63 action sheet replaces them" docs/SHELL-MODULES.md` = 0
    - `npm run build:www` exits 0, `npm test` reports fail 0, and `npm run boot:check` passes its four checks
    - `git diff --stat 4adc5e4 -- engine/ content/ test/parity/` prints nothing, and `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The docs describe the sheet as shipped, the five requirements are marked complete, and every phase gate is green with engine, content and parity untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer → row | a tap on a row's USE button must not also open the sheet underneath it |
| save file → row DOM | item names become opener ids' neighbours and sr-only text; ids come from slot keys and integer indices, never from names |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-11 | Tampering | row opener ids | low | mitigate | ids are built only from GEAR_WORN_ORDER keys and integer card indices, never item text. Names stay in textContent (the existing Phase 62 encoding tests plus the sheet's) |
| T-63-12 | Spoofing (double action) | USE button inside an openable row | low | mitigate | useCellEl stops propagation; a test proves a USE tap never calls openGearSheet |
| T-63-13 | Tampering (regression of other screens) | renderCarriedList | medium | mitigate | a byte-identity gate against 4adc5e4, plus the untouched store/loot/hero snapshots |
</threat_model>

<verification>
- `npm run build:www && npm test` green; `npm run boot:check` passes
- renderCarriedList byte-identical to 4adc5e4; the hero/store fixtures byte-identical; only thief.gear/mu.gear regenerated and thief.gear-confirms deleted
- engine/, content/, test/parity/ byte-identical to 4adc5e4; the prototype master hash unchanged
</verification>

<success_criteria>
- Every Gear-tab decision goes through the sheet; no interim in-row path survives
- Rows are announced as buttons that open actions; USE still acts in place; focus returns to the row
- GSCR-07..10 and GRULE-02 are complete with every gate green
</success_criteria>

## Probe fallback record

This plan authors no edge rows (see 63-01-PLAN.md for the phase-wide ledger of 13: 11 authored, 2 flagged-unresolved). It keeps 2 prohibitions, flagged-unverified with no check descriptor.

## Artifacts this phase produces

This plan's share.

| Artifact | Kind | Where |
|---|---|---|
| renderGearTab rows/cards as sheet openers (role=button, tabindex, aria-haspopup, sr-only hint, Enter/Space), USE stopPropagation, interim actions deleted, GEAR_COPY.opensHint (act group deleted) | Gear tab module | src/browser/gearTab.js |
| .mw-gear-tap CSS + updated #screen-gear comment | shell CSS/markup comment | mazeworld.html |
| migrated gear tests + end-to-end row → sheet → dispatch tests | tests | test/unit/gear-tab-dom, gear-agreement, shell-worn-slots, gear-panels, gear-sheet-shell |
| thief.gear.txt / mu.gear.txt regenerated; thief.gear-confirms.txt deleted | declared snapshot changes | test/unit/fixtures/shell-snapshots/ |
| Phase 63 notes in SHELL-MODULES.md / GEAR-SLOTS.md; GSCR-07..10 + GRULE-02 complete | docs / requirements | docs/, .planning/REQUIREMENTS.md |

<output>
Create `.planning/phases/63-action-sheet-combat-lock-accessibility/63-05-SUMMARY.md` when done. Include the declared fixture ledger for the whole phase, every Rule 1 test re-point, every gate's output, the note that renderCarriedList's gearRow branch and its .mw-drop-confirm / .mw-swap-confirm / .mw-gear-actions CSS are now caller-less (kept byte-identical by ruling, flagged as a follow-up quick task), and the Phase 64 device carry-forward list.
</output>
