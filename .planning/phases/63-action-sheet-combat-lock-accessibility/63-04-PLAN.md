---
phase: 63-action-sheet-combat-lock-accessibility
plan: 04
type: execute
wave: 3
depends_on: ["63-02"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/gear-sheet-shell.test.js
  - test/unit/gearTab.test.js
  - test/unit/shell-tab-snapshots.test.js
  - test/unit/fixtures/shell-snapshots/thief.gear-sheet-bag.txt
  - test/unit/fixtures/shell-snapshots/thief.gear-sheet-worn.txt
autonomous: true
requirements: [GSCR-07, GSCR-08, GSCR-10, GRULE-02]

must_haves:
  truths:
    - "mazeworld.html declares #mw-gear-sheet as a hidden-by-default .mw-legend-sheet (the camp-sheet chrome) holding #mw-gear-sheet-scrim (.mw-legend-scrim) and one .mw-legend-panel with role='dialog', aria-modal='true' and aria-labelledby='mw-gear-sheet-title', which contains every GEAR_SHEET_IDS id; the title is an h2 with tabindex='-1' and CANCEL is a <button type='button'>"
    - "openGearSheet(target, openerId) renders through window.__mzGearSheet into #mw-gear-sheet; when that returns false (or there is no live hero) nothing opens; otherwise it records the target and opener id, runs showPanel, armEncounterButtons() (the 250 ms ghost-tap guard) and moves focus to #mw-gear-sheet-title"
    - "closeGearSheet() clears the target, closes through hidePanel (an animated close, an instant snap under reduced motion), stamps lastDismissAt at call time, and restores focus to the opener id on a 0 ms timer, after any dispatch-driven repaint has rebuilt the row"
    - "While the sheet is open, every paint() re-renders it from S and the stored target (refreshGearSheet): a fight that starts greys EQUIP/SWAP/UNEQUIP/DISCARD in place with the Phase 61 line while the sheet stays open, the same inventory after the fight is live again, and a target that vanished closes the sheet"
    - "The sheet closes on CANCEL (module-wired through guardTap), on a scrim click, and on the Android back button: hasOpenModal ORs in #mw-gear-sheet's visibility, and closeModal closes the Gear sheet first and returns, one layer per press, before the stair, camp, legend, beats and store handling"
    - "armEncounterButtons' arm sweep skips greyed actions (`:not([data-off])`), so their disabled state and reason stay announced, while live actions lose the arm marker after ARM_DELAY_MS"
    - "tabDeps() gains openGearSheet and closeGearSheet (16 keys); the module script imports renderGearSheet and assigns window.__mzGearSheet = renderGearSheet before the first await boot(; src/browser/bridge.js lists __mzGearSheet and the generated docs/SHELL-MODULES.md table matches"
    - "Every new .mw-gsheet-* rule sizes text with calc(<rem> * var(--mw-text-scale)) and carries no transition or animation of its own (the rise and fade are the shared legend chrome, OPEN_MS/CLOSE_MS, killed by the blanket reduced-motion rule); the block holds no ARIA-state selector; greyed rows key off [data-off] (opacity .45, grey label, hidden chevron)"
    - "Two new declared snapshot fixtures capture the sheet (thief.gear-sheet-bag: the bagged third jewel with SWAP INTO JEWELRY 1 / SWAP INTO JEWELRY 2 / DROP; thief.gear-sheet-worn: jewelry1 with UNEQUIP greyed by the full bag); every pre-existing fixture is byte-identical"
  assumptions:
    - requirement_id: GSCR-10
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The edge probe could not classify GSCR-10. The close paths, motion paths and dialog semantics are proven here by sandbox behaviour plus source pins (the back handler only runs under Capacitor). How TalkBack actually reads the sheet and how the hardware back button feels on a Pixel 7 are carried to Phase 64's GSCR-12 device batch, not auto-backstopped"
    - requirement_id: GRULE-02
      category: unclassified
      status: unresolved
      flagged: true
      statement: "The edge probe could not classify GRULE-02. The model (Plan 01), the engine sweep (Plan 03) and the live re-render here prove the logic. The in-hand check (open the Gear tab mid-fight, see the greyed rows, then see them live after) is carried to Phase 64's device batch"
  artifacts:
    - path: "mazeworld.html"
      provides: "#mw-gear-sheet markup, the Phase 63 sheet CSS block, openGearSheet/refreshGearSheet/closeGearSheet, the paint() refresh, tabDeps keys, the arm-sweep exclusion, the back-handler wiring and the __mzGearSheet bridge"
      contains: "id=\"mw-gear-sheet\""
    - path: "test/unit/gear-sheet-shell.test.js"
      provides: "markup, CSS, source-pin and sandbox lifecycle suite (open, close, CANCEL, focus, reduced motion, combat re-render, vanish-close)"
      min_lines: 220
    - path: "test/unit/fixtures/shell-snapshots/thief.gear-sheet-bag.txt"
      provides: "declared new sheet snapshot"
      contains: "## #mw-gear-sheet-title"
  key_links:
    - from: "mazeworld.html#openGearSheet / refreshGearSheet"
      to: "src/browser/gearSheet.js#renderGearSheet"
      via: "window.__mzGearSheet(sheet, S, target, tabDeps())"
      pattern: "window\\.__mzGearSheet\\("
    - from: "mazeworld.html#paint"
      to: "mazeworld.html#refreshGearSheet"
      via: "one guarded call right after the gear tab mount"
      pattern: "refreshGearSheet\\(\\)"
    - from: "mazeworld.html#getGameContext.closeModal"
      to: "mazeworld.html#closeGearSheet"
      via: "the Android back button's close-modal branch"
      pattern: "closeGearSheet\\(\\);"
    - from: "mazeworld.html#tabDeps"
      to: "openGearSheet / closeGearSheet"
      via: "deps closures the Gear tab (Plan 05) and the sheet renderer call"
      pattern: "openGearSheet: \\(target, openerId\\) =>"
  prohibitions:
    - requirement_id: GSCR-10
      category: player-agency
      flagged: true
      statement: "MUST NOT trap the player in the sheet: CANCEL, a scrim tap and the back button each close it, including mid-fight and mid-close, and a greyed or empty sheet is never a dead end"
    - requirement_id: GSCR-10
      category: accessibility
      flagged: true
      statement: "MUST NOT animate for a player who asked for reduced motion: the close snaps and the open keyframes are removed by the blanket prefers-reduced-motion rule"
---

<objective>
Mount the sheet in the shell on the existing camp-sheet pattern:
- The `#mw-gear-sheet` markup and its CSS.
- The classic lifecycle: `openGearSheet` / `refreshGearSheet` / `closeGearSheet` (showPanel/hidePanel motion, armEncounterButtons + guardTap, a lastDismissAt stamp, focus to the title and back to the opener).
- A paint() hook so a fight starting while the sheet is open greys it in place.
- The scrim and Android-back close paths.
- Two tabDeps keys and a `window.__mzGearSheet` bridge (registered and documented).
- Sandbox tests and two new declared sheet snapshots.

Purpose: GSCR-10 (close paths, reduced motion, TalkBack dialog semantics) and GRULE-02's live re-render. After this plan the sheet opens programmatically. Plan 05 makes the Gear rows open it and removes the interim in-row actions.

Implements CONTEXT: "Built on the existing camp-sheet pattern" (scrim, showPanel/hidePanel, armEncounterButtons + guardTap, lastDismissAt, hasOpenModal / decideBackAction close-modal); "If a fight starts while the sheet is open, the sheet re-renders greyed in place rather than closing"; "Closing: CANCEL, a backdrop (scrim) tap, and the Android back button. Focus returns to the row that opened the sheet"; "TalkBack: role=dialog + aria-modal + aria-labelledby, focus moves to the title on open"; "Motion … through Phase 58's showPanel / hidePanel path, reduced motion snaps instantly. Every new font size is calc(<rem> * var(--mw-text-scale))".

Discretion: the sheet reuses the legend chrome's shared OPEN_MS (0.18 s) rise and fade rather than new one-off durations for the mock's ≈0.16 s and ≈0.2 s. This keeps panel-motion.test.js's CSS/JS agreement and gives one reduced-motion path for every sheet.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-CONTEXT.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-02-SUMMARY.md
@docs/SHELL-MODULES.md
@.claude/CLAUDE.md

<interfaces>
From Plan 02 (src/browser/gearSheet.js): `GEAR_SHEET_IDS` = { label "mw-gear-sheet-label", title "mw-gear-sheet-title", note "mw-gear-sheet-note", why "mw-gear-sheet-why", actions "mw-gear-sheet-actions", cancel "mw-gear-sheet-cancel" }. `renderGearSheet(host, state, target, deps)` → true | false. It calls deps.guardTap, deps.closeGearSheet, deps.equipItem/unequip/useItem/dropItem. Action buttons are `button.mw-gsheet-act[data-key]`; greyed ones carry data-off="1" and aria-disabled="true". Sub spans use `mw-gear-sheet-sub-<n>`. Classes: mw-gsheet-act, -act-main, -act-label, -act-sub, -chev.
Target shapes: `{ from: "worn", slot }`, `{ from: "bag", i, n }`.
mazeworld.html anchors (read before editing; lines drift):
- camp-sheet markup :1604-1624
- camp-sheet CSS :1416-1422
- generic button rules :515-563
- `function tabDeps()` :2930
- paint()'s gear mount :3064
- showPanel/hidePanel :3290-3301
- `let lastDismissAt` :3327
- armEncounterButtons :3360-3367
- guardTap :3377
- open/closeCampSheet :4796-4821
- module import of gearTab.js :5338
- the __mzTabs / __mzCarriedList bridge block :5494-5501
- getGameContext hasOpenModal / closeModal :6870-6892
test/unit/harness/shellSandbox.js: `loadShellSandbox({ doc, reducedMotion = true, clock = null })` returns { context, setState, paint, renderEncounter, doc }. Classic functions are callable as `context.openCampSheet()`. `wireBridges` mirrors the module bridge block. Pass `clock` (test/unit/harness/fakeClock.js createFakeClock) to fake setTimeout and Date.now; ARM_DELAY_MS comes from src/browser/inputGuards.js.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: #mw-gear-sheet markup and the Phase 63 sheet CSS block</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html (:1330-1422 the .mw-legend-* chrome, the closing-state rules and the camp-sheet CSS; :515-563 the generic button rule, its sticky-hover note and :active transform; :990 the blanket reduced-motion rule; :1600-1625 the camp-sheet markup and its comment)
    - src/browser/gearSheet.js (GEAR_SHEET_IDS and the class names renderGearSheet emits)
    - design/Mazeworld Gear.dc.html (:117-137 the sheet's look: 3px #6b5c3c top border, 18/18/26 padding, the 3px #3a3226 header rule, the label/title/note sizes and colours, 2px #241f16 action rules, the gold label, 45% opacity greyed rows with a hidden chevron, the bordered CANCEL)
    - test/unit/gear-tab-dom.test.js (:98-134 the CSS pin style: font-size calc and no transition/animation)
    - .planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md (deviation 2: three sibling suites scan the WHOLE style block for the ARIA disabled-state token, comments included)
  </read_first>
  <action>
1. Markup. Insert directly after the camp sheet's closing `</div>` (and before `<div id="app"`) an HTML comment headed "GEAR ACTION SHEET (Phase 63, GSCR-07..10)". It says the sheet reuses the legend chrome, is filled by src/browser/gearSheet.js#renderGearSheet through window.__mzGearSheet, is opened and closed by openGearSheet/closeGearSheet in the classic script, and that outcomes reach the rail, never this sheet. Then add:
- `<div id="mw-gear-sheet" class="mw-legend-sheet" hidden>`, containing:
  - `<div class="mw-legend-scrim" id="mw-gear-sheet-scrim"></div>`
  - `<div class="mw-legend-panel" id="mw-gear-sheet-panel" role="dialog" aria-modal="true" aria-labelledby="mw-gear-sheet-title">`, containing:
    - `<div class="mw-gsheet-head">` with, in this order:
      - `<p class="mw-gsheet-label" id="mw-gear-sheet-label"></p>`
      - `<h2 class="mw-gsheet-title" id="mw-gear-sheet-title" tabindex="-1"></h2>`
      - `<p class="mw-gsheet-note" id="mw-gear-sheet-note"></p>`
      - `<p class="mw-gsheet-why" id="mw-gear-sheet-why" hidden></p>`
    - then `<div class="mw-gsheet-actions" id="mw-gear-sheet-actions"></div>`
    - and `<button type="button" class="mw-gsheet-cancel" id="mw-gear-sheet-cancel"></button>`.

2. CSS. Insert a block directly after the `.mw-camp-btn.secondary{…}` rule. Head it with a comment "GEAR action sheet (Phase 63, GSCR-07..10)" that says: it reuses the legend chrome for the rise, the fade and the closing state; greyed rows key off data-off; every font-size scales with --mw-text-scale. The comment must not name the ARIA disabled-state attribute. Three sibling suites scan the whole style block for that token, comments included (the 62-02 deviation). Use one-line rules in the file's style, with these values (px converted at 16 px per rem):
- `.mw-gsheet-head` — border-bottom 3px solid #3a3226; padding-bottom 13px.
- `.mw-gsheet-label` — margin 0; font-family var(--disp); font-size calc(0.40625rem * var(--mw-text-scale)); line-height 1.5; color #9a8f76.
- `.mw-gsheet-title` — margin 9px 0 0; font-family var(--disp); font-weight 400; font-size calc(0.625rem * var(--mw-text-scale)); line-height 1.5; color #e6ddc6; text-transform uppercase; plus a separate rule `.mw-gsheet-title:focus` with outline none (it is a programmatic focus target only).
- `.mw-gsheet-note` — margin 8px 0 0; font-family var(--mono); font-weight 700; font-size calc(0.90625rem * var(--mw-text-scale)); line-height 1.45; color #a89c82.
- `.mw-gsheet-why` — margin 8px 0 0; font-family var(--mono); font-weight 700; font-size calc(0.8125rem * var(--mw-text-scale)); line-height 1.4; color var(--ditto).
- `.mw-gsheet-act` — display flex; align-items center; gap 12px; width 100%; min-height 48px; margin 0; padding 14px 2px; border 0; border-bottom 2px solid #241f16; border-radius 0; background transparent; box-shadow none; letter-spacing 0; text-align left; color inherit; cursor pointer.
- `.mw-gsheet-act-main` — flex 1; min-width 0; display flex; flex-direction column; gap 5px.
- `.mw-gsheet-act-label` — font-family var(--disp); font-size calc(0.5rem * var(--mw-text-scale)); line-height 1.5; color #e8c97a; text-transform uppercase.
- `.mw-gsheet-act-sub` — font-family var(--mono); font-weight 700; font-size calc(0.78125rem * var(--mw-text-scale)); line-height 1.4; color #8e8369.
- `.mw-gsheet-chev` — flex none; font-family var(--mono); font-weight 700; font-size calc(0.9375rem * var(--mw-text-scale)); color #5c5342.
- `.mw-gsheet-act[data-off]` — opacity .45; cursor default.
- `.mw-gsheet-act[data-off] .mw-gsheet-act-label` — color #8f856f.
- `.mw-gsheet-act[data-off] .mw-gsheet-chev` — visibility hidden.
- `.mw-gsheet-act[data-armed] .mw-gsheet-act-label` — color var(--stamp).
- `.mw-gsheet-act:hover:not(:disabled),.mw-gsheet-act:active:not(:disabled)` — background transparent; transform none; box-shadow none. This beats the generic button hover and the press transform.
- `.mw-gsheet-cancel` — display block; width 100%; min-height 48px; margin-top 16px; padding 15px 8px; text-align center; font-family var(--disp); font-size calc(0.5rem * var(--mw-text-scale)); letter-spacing 0; background transparent; border 2px solid #4a4032; border-radius 0; color #c9bda0; box-shadow 0 4px 0 #0d0b08.
- `.mw-gsheet-cancel:hover:not(:disabled),.mw-gsheet-cancel:active:not(:disabled)` — background transparent; color #c9bda0.

No transition or animation property in any of these rules.

3. Run `npm run build:www`, then `node --test test/unit/panel-motion.test.js test/unit/reduced-motion.test.js test/unit/shell-map-hud.test.js test/unit/shell-combat-screen.test.js test/unit/shell-map-rail.test.js test/unit/gear-tab-dom.test.js`. All must be green; the last three siblings prove the style block stayed free of the ARIA-state token.
  </action>
  <verify>
    <automated>npm run build:www && node --test test/unit/panel-motion.test.js test/unit/reduced-motion.test.js test/unit/shell-map-hud.test.js test/unit/shell-combat-screen.test.js test/unit/shell-map-rail.test.js test/unit/gear-tab-dom.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-gear-sheet"' mazeworld.html` = 1, `grep -c 'id="mw-gear-sheet-scrim"' mazeworld.html` = 1, `grep -c 'aria-labelledby="mw-gear-sheet-title"' mazeworld.html` = 1, `grep -c 'id="mw-gear-sheet-title" tabindex="-1"' mazeworld.html` = 1
    - each of `mw-gear-sheet-label`, `mw-gear-sheet-note`, `mw-gear-sheet-why`, `mw-gear-sheet-actions`, `mw-gear-sheet-cancel` appears as `id="…"` exactly once in mazeworld.html
    - `grep -c "^\.mw-gsheet-act{" mazeworld.html` = 1 and `grep -c "^\.mw-gsheet-act\[data-off\]{" mazeworld.html` = 1
    - `npm run build:www` exits 0 and the six named suites exit 0
  </acceptance_criteria>
  <done>The sheet's static shell exists in mazeworld.html, hidden by default, as an accessible dialog on the shared sheet chrome. Its CSS follows the mock on the existing tokens, scales with text size, and has no motion of its own.</done>
</task>

<task type="auto">
  <name>Task 2: Classic lifecycle, paint() refresh, tabDeps, arm-sweep, back button, bridge, registry and harness</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js</files>
  <read_first>
    - mazeworld.html (:2925-2950 tabDeps; :3055-3066 paint()'s hero/gear mounts; :3280-3381 showPanel/hidePanel, the guard lets, encArmed/encounterSettled, armEncounterButtons, guardTap; :4796-4821 openCampSheet/closeCampSheet and the scrim listener; :5330-5345 the module imports; :5494-5501 the __mzTabs/__mzCarriedList bridge block; :6862-6905 registerNativeChrome's getGameContext)
    - src/browser/bridge.js (the __mzCarriedList and __mzTabs entries, the entry shape and alphabetical order)
    - test/unit/bridge-registry.test.js (:109-214, what the registry and doc-sync tests require)
    - tools/bridge-doc.mjs (the `--write` mode)
    - test/unit/harness/shellSandbox.js (:132-180 wireBridges, :466-480 SNAPSHOT_IDS)
    - test/unit/shell-map-viewport.test.js (:320-340, the hasOpenModal / closeModal ordering pins that must stay green)
    - test/unit/gearTab.test.js (:160-168 the paint() region pin; :186-199 the module-bridge pin)
  </read_first>
  <action>
In mazeworld.html's classic script:

1. Declare the lets directly ABOVE `function tabDeps()`, so they precede paint() in source order and cannot hit a TDZ: `let gearSheetTarget = null;` and `let gearSheetOpenerId = null;`. Add a comment saying these are presentation-only and never on S, because serializeRun spreads S.

2. tabDeps(). Add two keys after `castSpell`:
- `openGearSheet: (target, openerId) => openGearSheet(target, openerId),`
- `closeGearSheet: () => closeGearSheet(),`

3. paint(). Directly after the `window.__mzTabs.gear(...)` mount line, add `if (gearSheetTarget !== null) refreshGearSheet();` with a comment: Phase 63 (GRULE-02), an open sheet re-reads S every paint, so a fight that starts greys it in place and a vanished target closes it. Keep the gear mount line byte-identical. Add no id literal inside paint().

4. armEncounterButtons. In its querySelectorAll selector, change `.mw-legend-sheet [aria-disabled="true"]` to `.mw-legend-sheet [aria-disabled="true"]:not([data-off])`. Extend the comment: a greyed Gear-sheet action keeps its disabled state and reason for TalkBack; only the live buttons' arm marker is tidied.

5. Directly after the camp sheet's `document.getElementById("btn-camp").onclick = openCampSheet;` line, add a commented block "Phase 63 (GSCR-07..10, GRULE-02): the GEAR action sheet, on the camp-sheet pattern" with three functions:
- `function openGearSheet(target, openerId)`:
  - Return early unless `S && S.c && !S.dead && window.__mzGearSheet`.
  - `const sheet = document.getElementById("mw-gear-sheet"); if (!sheet) return;`
  - If `!window.__mzGearSheet(sheet, S, target, tabDeps())`, return (an unresolvable target opens nothing).
  - Otherwise set `gearSheetTarget = target; gearSheetOpenerId = openerId || null;`, then `showPanel(sheet); armEncounterButtons();`, then `document.getElementById("mw-gear-sheet-title")?.focus?.();`.
- `function refreshGearSheet()`:
  - Return if `gearSheetTarget === null`.
  - Look up the sheet. If `!S || !S.c || S.dead || !window.__mzGearSheet || !sheet || !window.__mzGearSheet(sheet, S, gearSheetTarget, tabDeps())`, call `closeGearSheet()` and return.
  - Otherwise `armEncounterButtons()`.
- `function closeGearSheet()`:
  - `const openerId = gearSheetTarget !== null ? gearSheetOpenerId : null;`
  - Set both lets to null.
  - `const sheet = document.getElementById("mw-gear-sheet"); if (sheet) hidePanel(sheet);`
  - `lastDismissAt = Date.now();` (stamped at call time, as closeCampSheet does).
  - `if (openerId) setTimeout(() => document.getElementById(openerId)?.focus?.(), 0);` This runs after a dispatch-driven repaint has rebuilt the opener row.

After the three functions add `document.getElementById("mw-gear-sheet-scrim").addEventListener("click", closeGearSheet);`.

6. registerNativeChrome's getGameContext (module script):
- Append `|| !document.getElementById("mw-gear-sheet")?.hidden` to the hasOpenModal expression, right after the mw-legend-sheet term.
- In closeModal, directly after `hudMenuEvent("escape");` and before `window.__mzStair = null;`, add `if (gearSheetTarget !== null) { closeGearSheet(); return; }`. Comment: the Gear sheet is the top-most layer, so one back press closes only it. Classic top-level lets and functions resolve from the module script exactly as `S` and `closeCampSheet` already do.

7. Module script:
- Next to the gearTab.js import, add `import { renderGearSheet } from "./src/browser/gearSheet.js";`.
- Directly after `window.__mzCarriedList = renderCarriedList;`, add `window.__mzGearSheet = renderGearSheet;` with a one-line Phase 63 comment. It must stay before the first `await boot(`.

8. src/browser/bridge.js. Add a frozen `__mzGearSheet` entry in alphabetical position:
- owner "mazeworld.html (module)"
- consumers ["mazeworld.html (classic: openGearSheet / refreshGearSheet — the Gear action sheet's render)"]
- purpose "Bridges src/browser/gearSheet.js's renderGearSheet so the classic sheet lifecycle (open, repaint refresh, close, back button, ghost-tap arm) renders the ONE pure sheet model, never a second copy."

Then run `node tools/bridge-doc.mjs --write` to regenerate docs/SHELL-MODULES.md's bridge table.

9. test/unit/harness/shellSandbox.js:
- Import `renderGearSheet` from `../../../src/browser/gearSheet.js`.
- In wireBridges, add `w.__mzGearSheet = renderGearSheet;` after `w.__mzCarriedList`.
- Add `gearSheet: ["mw-gear-sheet-label", "mw-gear-sheet-title", "mw-gear-sheet-note", "mw-gear-sheet-why", "mw-gear-sheet-actions", "mw-gear-sheet-cancel"]` to SNAPSHOT_IDS, with a Phase 63 comment.

10. Run `npm run build:www`, then `node --test test/unit/bridge-registry.test.js test/unit/gearTab.test.js test/unit/shell-map-viewport.test.js test/unit/shell-tab-snapshots.test.js test/unit/panel-motion.test.js test/unit/reduced-motion.test.js test/unit/shell-input-guards.test.js`. Every existing snapshot must stay byte-identical, because the sheet is hidden and outside every snapshot id.
  </action>
  <verify>
    <automated>npm run build:www && node --test test/unit/bridge-registry.test.js test/unit/gearTab.test.js test/unit/shell-map-viewport.test.js test/unit/shell-tab-snapshots.test.js test/unit/panel-motion.test.js test/unit/reduced-motion.test.js test/unit/shell-input-guards.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function openGearSheet(target, openerId)" mazeworld.html` = 1, `grep -c "function refreshGearSheet()" mazeworld.html` = 1, `grep -c "function closeGearSheet()" mazeworld.html` = 1
    - `grep -c 'getElementById("mw-gear-sheet-scrim").addEventListener("click", closeGearSheet)' mazeworld.html` = 1
    - `grep -c 'aria-disabled="true"\]:not(\[data-off\])' mazeworld.html` = 1
    - `grep -c "if (gearSheetTarget !== null) refreshGearSheet();" mazeworld.html` = 1 and `grep -c "if (gearSheetTarget !== null) { closeGearSheet(); return; }" mazeworld.html` = 1
    - `grep -c "window.__mzGearSheet = renderGearSheet;" mazeworld.html` = 1 and `grep -c "__mzGearSheet" src/browser/bridge.js` >= 1 and `grep -c "__mzGearSheet" docs/SHELL-MODULES.md` >= 1
    - `grep -c "openGearSheet: (target, openerId) => openGearSheet(target, openerId)" mazeworld.html` = 1 and `grep -c "closeGearSheet: () => closeGearSheet()" mazeworld.html` = 1
    - `grep -c "w.__mzGearSheet = renderGearSheet;" test/unit/harness/shellSandbox.js` = 1
    - the seven named suites exit 0, and `git status --short test/unit/fixtures/` prints nothing
  </acceptance_criteria>
  <done>The shell can open, refresh and close the sheet with the camp sheet's motion, ghost-tap guard and settle stamp. Back and scrim close it, and a repaint keeps it honest mid-fight. The bridge is registered and documented, and the harness mirrors it with no existing snapshot moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shell suite, two declared sheet snapshots, tabDeps pin and the SHELL-MODULES contract</name>
  <files>test/unit/gear-sheet-shell.test.js, test/unit/shell-tab-snapshots.test.js, test/unit/fixtures/shell-snapshots/thief.gear-sheet-bag.txt, test/unit/fixtures/shell-snapshots/thief.gear-sheet-worn.txt, test/unit/gearTab.test.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - test/unit/panel-motion.test.js (:36-60 imports and HTML/CODE reads; :140-215 buildScenario and the open/close/cancel pattern with a fake clock)
    - test/unit/reduced-motion.test.js (:170-195 the reduced-motion sheet assertions)
    - test/unit/shell-tab-snapshots.test.js (the whole file: check(), paintFresh(), the gear tests and the guard list)
    - test/unit/combat-gear-lock.test.js (:31-94 the seed-6 Demons pending-fight fixture)
    - test/unit/gearTab.test.js (:200-230 the tabDeps key test)
    - docs/SHELL-MODULES.md (the Contract section and "What stays shared")
    - src/browser/gearSheet.js (GEAR_SHEET_IDS, gearSheetModel)
  </read_first>
  <behavior>
    - Markup (the region sliced from `<div id="mw-gear-sheet"` to the next `<div id="app"`): class="mw-legend-sheet" hidden; the scrim's class and id; the panel's role="dialog", aria-modal="true" and aria-labelledby="mw-gear-sheet-title"; the title h2 has tabindex="-1"; every Object.values(GEAR_SHEET_IDS) id is declared exactly once inside; CANCEL is `<button type="button"`.
    - CSS (the style block): every rule whose selector contains ".mw-gsheet" declares any font-size as calc(<rem> * var(--mw-text-scale)); none carries "transition" or "animation"; the Phase 63 block, sliced from its header comment to the next "/* ----------" header, contains no "aria-" token; the [data-off] rules set opacity .45 and hide the chevron.
    - Source pins (comment-stripped classic and module script):
      - tabDeps names openGearSheet and closeGearSheet;
      - paint()'s region contains exactly one `refreshGearSheet()`, after the gear mount;
      - the gear-sheet lets are declared before `function tabDeps()`;
      - the armEncounterButtons selector carries `:not([data-off])`;
      - the scrim wiring exists;
      - the hasOpenModal expression mentions mw-gear-sheet;
      - in closeModal, `closeGearSheet();` sits after `hudMenuEvent("escape");` and before `window.__mzStair = null;`;
      - `window.__mzGearSheet = renderGearSheet;` precedes `await boot(`.
    - Sandbox, open: on fixedStates().thief (reduced motion), set the sheet's `hidden = true` and replace the title's focus with a spy. `context.openGearSheet({ from: "bag", i, n }, "gear-open-bag-" + i)` for the bagged jewel: sheet.hidden false, the title text equals the jewel's name, data-key values equal gearSheetModel's action keys, and the focus spy was called once.
    - Sandbox, unresolvable: `openGearSheet({ from: "bag", i: 99, n: "x" })` leaves the sheet hidden and the focus spy uncalled.
    - Sandbox, close under reduced motion: `closeGearSheet()` hides it at once and `context.encounterSettled()` is false right after (the settle stamp).
    - Sandbox, close under motion: with a fake clock, reducedMotion false, close gives data-motion "closing"; after CLOSE_MS + CLOSE_SLACK_MS it is hidden. A focus spy on `doc.getElementById("gear-open-bag-<i>")` is not called before `clock.advance(1)` and is called after.
    - Sandbox, CANCEL: fake clock; open; `clock.advance(ARM_DELAY_MS)`; the #mw-gear-sheet-cancel onclick closes the sheet.
    - Sandbox, DROP: fake clock; stub `context.window.mzDropItem` with a spy that records whether the sheet was hidden at call time. Open the rope's bag sheet, advance ARM_DELAY_MS, click DROP twice. The spy got the rope's index and saw the sheet already closing or hidden.
    - Sandbox, combat re-render (GRULE-02): build the seed-6 base state and its Demons pending clone (the combat-gear-lock builders, copied locally; clear pendingLoot and pendingFind on the base before startCombat, so no loot or find card competes with the sheet). Open `{ from: "worn", slot: "jewelry1" }` on the base: its swap action is enabled. setState(pending) + paint(): the sheet is still shown, the swap button has data-off and its sub text === LINE_FOR.gearRefused({ type: "gearRefused", verb: "equipItem", reason: "combat" }).text. setState(base) + paint(): it is live again. If sandbox paint() cannot render the pending encounter, call `context.refreshGearSheet()` directly (paint's call is source-pinned above) and record that in the SUMMARY.
    - Sandbox, vanish: open the rope's bag sheet; setState(a structuredClone with the rope spliced out) + paint(); the sheet is hidden.
  </behavior>
  <action>
1. Create `test/unit/gear-sheet-shell.test.js` covering every `<behavior>` bullet. Mirror panel-motion.test.js's reads: RAW_HTML CRLF-normalised, CODE from `stripHtml`, and loadShellSandbox + createFakeClock scenarios. Build targets from `dropShelfItems(state.c)` and `slotFor` so the tests never hard-code the thief fixture's indices.

2. test/unit/shell-tab-snapshots.test.js. Add two tests after thief.gear-confirms:
- "SHELL-01 (Phase 63): thief.gear-sheet-bag" — paintFresh(thief), then `sandbox.context.openGearSheet({ from: "bag", i, n }, "gear-open-bag-" + i)` for the bagged jewel, then `check("thief.gear-sheet-bag", doc.serializeElements(SNAPSHOT_IDS.gearSheet))`, asserting the text includes "SWAP INTO JEWELRY 1", "SWAP INTO JEWELRY 2" and "DROP".
- "SHELL-01 (Phase 63): thief.gear-sheet-worn" — the same for `{ from: "worn", slot: "jewelry1" }`, asserting "Bag is full — free a slot first." and "UNEQUIP".

Add both names to the guard test's list and update its title and comment count (seven → nine). Extend the file header comment: Phase 63 adds two declared sheet fixtures.

Capture with `MZ_SNAPSHOT_UPDATE=1 node --test --test-name-pattern="gear-sheet" test/unit/shell-tab-snapshots.test.js`. Then confirm `git status --short test/unit/fixtures/shell-snapshots/` lists ONLY the two new files as untracked. Then run the file without the env var: it must be green.

3. test/unit/gearTab.test.js. In the tabDeps test, add "openGearSheet" and "closeGearSheet" to the key list. Retitle it "…names all 16 deps keys" and add a Phase 63 comment.

4. docs/SHELL-MODULES.md:
- Add `gearSheet.js` (Phase 63) to the intro's module list.
- Extend the Contract `deps` bullet to 16 keys, naming `openGearSheet` / `closeGearSheet`: closures over the classic lifecycle; the Gear rows call openGearSheet(target, openerId), and the sheet's own actions call closeGearSheet before dispatching.
- Add a short "### Gear action sheet (Phase 63)" subsection:
  - gearSheet.js exports GEAR_SHEET_COPY, gearSheetModel, GEAR_SHEET_IDS and renderGearSheet(host, state, target, deps);
  - the classic script owns openGearSheet / refreshGearSheet (called by paint()) / closeGearSheet, the scrim and the back-button close;
  - window.__mzGearSheet is the one bridge.
- Leave the generated bridge table to tools/bridge-doc.mjs.

5. Run `npm run build:www && npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/gear-sheet-shell.test.js test/unit/shell-tab-snapshots.test.js test/unit/gearTab.test.js test/unit/bridge-registry.test.js && npm run build:www && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-sheet-shell.test.js` exits 0 with at least 16 passing tests, including a name containing "GRULE-02" and one containing "reduced motion"
    - test/unit/fixtures/shell-snapshots/thief.gear-sheet-bag.txt and thief.gear-sheet-worn.txt exist and are non-empty; `grep -c "SWAP INTO JEWELRY 2" test/unit/fixtures/shell-snapshots/thief.gear-sheet-bag.txt` >= 1
    - `git diff --stat -- test/unit/fixtures/shell-snapshots/` prints nothing (every pre-existing fixture byte-identical; the two new files are additions)
    - `grep -c "openGearSheet" docs/SHELL-MODULES.md` >= 1 and `grep -c "gearSheet.js" docs/SHELL-MODULES.md` >= 1
    - `npm run build:www` exits 0, `npm test` reports fail 0 (worktree CRLF false-fails in class-pass-ledger/flee-ledger excepted), and `npm run boot:check` passes its four checks (re-run once if the documented graves-timing flake appears)
    - `git diff --stat -- engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The sheet's shell behaviour (open, focus, close paths, motion and reduced motion, the live combat re-render, the vanish-close) is proven in the sandbox and by source pins. Its look is locked by two declared snapshots, with no old fixture moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer → sheet | the opening tap and a closing tap can race a freshly rendered or closing panel |
| Android back → modal stack | a hardware back press must close exactly the top layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-08 | Spoofing (ghost tap) | openGearSheet / refreshGearSheet | low | mitigate | armEncounterButtons() on every open and refresh restarts the 250 ms arm window that guardTap checks via Date.now(), never a transition event. The closing panel is pointer-inert and the scrim absorbs taps (the Phase 58 chrome) |
| T-63-09 | Denial of service (stuck modal) | back handler / scrim | low | mitigate | closeGearSheet is idempotent. hasOpenModal and closeModal both know the sheet, and closeModal closes it first and returns. Source pins lock the order |
| T-63-10 | Information disclosure | none | low | accept | offline, local-only presentation; no data leaves the device |
</threat_model>

<verification>
- `node --test test/unit/gear-sheet-shell.test.js test/unit/shell-tab-snapshots.test.js test/unit/bridge-registry.test.js` green
- `npm run build:www && npm test` green; `npm run boot:check` passes
- Only the two new sheet fixtures appear under test/unit/fixtures/; engine/content/parity untouched
</verification>

<success_criteria>
- The sheet opens, refreshes and closes on the camp-sheet pattern, with CANCEL, scrim and back
- Reduced motion snaps; TalkBack gets a labelled modal dialog with focus on its title and back on the row
- A fight that starts while the sheet is open greys it in place; after the fight it is live again
</success_criteria>

## Probe fallback record

This plan carries the phase's 2 `unclassified` edge rows (GSCR-10, GRULE-02) as flagged, unresolved assumptions in must_haves. They are never auto-backstopped and are routed to Phase 64's device batch (see 63-01-PLAN.md for the phase-wide ledger). It keeps 2 prohibitions, flagged-unverified with no check descriptor.

## Artifacts this phase produces

This plan's share.

| Artifact | Kind | Where |
|---|---|---|
| #mw-gear-sheet markup (dialog on the legend chrome) + the Phase 63 .mw-gsheet-* CSS block | shell markup/CSS | mazeworld.html |
| openGearSheet / refreshGearSheet / closeGearSheet, the paint() refresh, the arm-sweep exclusion, the back-handler wiring, tabDeps openGearSheet/closeGearSheet | classic/module shell wiring | mazeworld.html |
| window.__mzGearSheet | module bridge (registered + documented) | mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md |
| harness wiring + SNAPSHOT_IDS.gearSheet | test harness | test/unit/harness/shellSandbox.js |
| test/unit/gear-sheet-shell.test.js | new shell suite (markup, CSS, pins, sandbox lifecycle) | test/unit/ |
| thief.gear-sheet-bag.txt / thief.gear-sheet-worn.txt | two new declared snapshot fixtures | test/unit/fixtures/shell-snapshots/ |

<output>
Create `.planning/phases/63-action-sheet-combat-lock-accessibility/63-04-SUMMARY.md` when done. Name the two new fixtures as declared additions, confirm every pre-existing fixture is byte-identical, list the tabDeps keys (16), and record whether the combat re-render test ran through paint() or refreshGearSheet(), plus the `npm test` / boot:check results.
</output>
