---
phase: 32-combat-narrative-input-ui-build
plan: 03
type: execute
wave: 3
depends_on: ["32-01", "32-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-input-guards.test.js
  - test/unit/round-card-worst-case.test.js
autonomous: true
requirements: [CMBUI-04, CMBUI-05, CMBUI-06]

must_haves:
  truths:
    - "CMBUI-04: every decision button in the §6.3 list — the action bar (Strike/Potion/Flee/Spells/Parley/Sing/Scroll) and the combat use-list Use rows ('Items'), Fight!, joiner Take them along/Leave them, loot per-row Equip now/Stow/Take/Leave and Take all/Leave all, find Take it/Leave it (both branches), death Review the Oracle AND Confirm, and the #enc-dismiss-slot Move on/Next — is built through ONE classic helper guardTap(btn, fn): the handler no-ops while !isArmed(encRenderedAt, Date.now()); encRenderedAt is stamped once per visible renderEncounter() build; the button keeps its normal look (no disabled flicker) but carries aria-disabled='true' for the arm window and loses it after ARM_DELAY_MS; a tap inside the window is swallowed silently (no toast)"
    - "Keys go through the same check: the keydown handler's S.combat branch starts with the arm check (Enter/Space/1 on the Fight! gate and 1-7 in a fight are swallowed while unarmed); the beats-branch Enter/Space still clicks a-next, whose handler is guarded"
    - "CMBUI-05: window.move (engineMove) gains exactly one second clause — refuse while !isSettled(lastDismissAt, Date.now()) — right after its hasActiveEncounter() return; lastDismissAt is stamped in renderEncounter when hasActiveEncounter() transitions true → false; D-pad, viewport tap and WASD/arrow moves all funnel through window.move so the settle window covers every movement path; no surface gains a tap-anywhere-to-dismiss and the round card has no dismiss gesture"
    - "Both guards are Date.now() comparisons through window.__mzInputGuards — no transition/animation event, no CSS timing — so prefers-reduced-motion users get identical protection"
    - "Exactly the §6.3 set is guarded: store rows/Leave, the drop-to-make-room shelf, the feature-action a-evt button, the won card's Roll another delver and the individual spell-menu buttons are deliberately NOT guarded (outside the ratified list; noted as a Phase 33 candidate)"
    - "A worst-case round measurement test exists (4 foes; one sp.atk:2 + frenzied (4 swings); ability kits; a Fridgian hero for the player-side frenzy; seeds swept deterministically) for both the fight action and an attack, prints the max events/lines/characters, asserts the folded line count never exceeds MAX_TOASTS (toastsForAction's own cap is the card's ceiling by construction), and the SUMMARY records the numbers"
    - "Full gate: npm test '# fail 0' (grown from 1855), npm run build:www exit 0, git diff --stat 9174e6e..HEAD -- engine content test/parity empty; CMBUI-06's on-device DR round is recorded as deferred human verification (the end-of-run UAT batch), never a mid-run pause"
  artifacts:
    - "mazeworld.html: encRenderedAt/armTimer/lastDismissAt/encWasActive classic state; encArmed(), encounterSettled(), guardTap(), armEncounterButtons(); renderCarriedList opts.guard; every §6.3 button wrapped; keydown arm check; engineMove settle clause; renderEncounter transition stamp"
    - "test/unit/shell-input-guards.test.js (new): wiring pins + Date.now() comparison pins + no-animation pins + no-dismiss-gesture pins"
    - "test/unit/round-card-worst-case.test.js (new): the measurement harness"
  key_links:
    - "renderEncounter() visible build → armEncounterButtons() → encRenderedAt = Date.now() → guardTap(btn, fn) → window.__mzInputGuards.isArmed(encRenderedAt, Date.now()) → fn() or silent no-op"
    - "renderEncounter() hasActiveEncounter() true→false → lastDismissAt = Date.now() → engineMove → encounterSettled() → window.__mzInputGuards.isSettled(lastDismissAt, Date.now()) → move or refuse"
    - "keydown (S.combat branch) → encArmed() → mzFight/mzAttack/… ; keydown (beats) → a-next.click() → guarded handler"
---

<objective>
Wire the 32-01 guards onto the 32-02 DOM: one classic `guardTap(btn, fn)` helper puts the 250 ms `ARM_DELAY_MS` arm window (handler no-op + `aria-disabled`, no visual flicker, silent swallow) on exactly the §6.3 decision-button list, the keyboard paths go through the same check, `window.move` gains its one `DISMISS_SETTLE_MS` clause stamped from the `hasActiveEncounter()` true→false transition, and the worst-case round is measured against the engine with both frenzy mechanics combined. Ends with the full gate (`build:www`, `npm test`, parity diff empty) and the hand-off: Phase 33 notes plus the CMBUI-06 DR-round checklist for the end-of-run UAT.

Purpose: CMBUI-04 (decision buttons never fire under a D-pad thumb after a rapid re-render) and CMBUI-05 (nothing important dismisses on a movement tap; a just-dismissed overlay does not become a step) — at zero extra taps, reduced-motion safe.
Output: `mazeworld.html` guard wiring, two new test files, the SUMMARY with the measured line count, the Phase 33 hand-off and the CMBUI-06 checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Commit trailers (every commit in this phase, no exceptions — task commits and the metadata commit alike): end every commit message with these two lines, in this order:

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS

Hard scope rule for the whole phase: shell/presentation/tests only. Do not edit anything under `engine/`, `content/`, or `test/parity/` (including `test/parity/prototype-master.js.txt`); `src/browser/toasts.js` stays unmodified. Haptics are out of scope (Phase 33 note only). UAT is deferred to the end of the autonomous run — never pause for a device check; record Pixel 7 items in the SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-combat-narrative-input-ui-build/32-CONTEXT.md
@.planning/phases/32-combat-narrative-input-ui-build/32-RESEARCH.md
@.planning/phases/32-combat-narrative-input-ui-build/32-01-SUMMARY.md
@.planning/phases/32-combat-narrative-input-ui-build/32-02-SUMMARY.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-03-SUMMARY.md
@docs/COMBAT-NARRATIVE-DESIGN.md
@src/browser/inputGuards.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard every §6.3 decision button through guardTap, route keys through the arm check, add the window.move settle clause and the dismissal stamp</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html ~L4761-4778 (hasActiveEncounter) and ~L4798-4804 (wireDeathConfirm — the Confirm handler to wrap), ~L4869-4886 (renderEncounter's top: the early-return + body rebuild — the transition stamp and the per-render arm stamp go here), the death branch (`btn-death-oracle` onclick + wireDeathConfirm() call), the beats branch (`a-next` onclick assignment in #enc-dismiss-slot), the loot branch (`a-loot-take-all`/`a-loot-leave-all` onclicks + the renderCarriedList call with actions lootEquip/lootTake/lootLeave), the joiner branch (`a-join-yes`/`a-join-no`), the find branch (`a-find-take`/`a-find-leave` in BOTH the not-full and bag-full sub-branches), the Fight! gate (`a-fight`), the action bar handler block at the end of the function (`a-strike`, `a-potion` with its full-health pre-check, `a-flee`, `a-spell` toggle, `a-talk`, `a-sing`, `a-scroll`), and the combat use-list renderCarriedList call (actions: ["use"])
    - mazeworld.html ~L3521-3580 (renderCarriedList: the `mkBtn(label, onClick, disabled)` closure that assigns `bt.onclick = onClick` — the ONE place to add opts.guard; the Gear tab and store sell list call sites must NOT pass guard), ~L5552-5597 (the keydown handler: the `if (S.combat) {` branch and the beats Enter/Space `n.click()` fallback), ~L6117-6124 (engineMove's `if (hasActiveEncounter()) return;` — the settle clause goes immediately after it), ~L5734-5745 (the window.__mzInputGuards bridge from 32-01)
    - docs/COMBAT-NARRATIVE-DESIGN.md §6.3 (the exact guarded list; rules 1-4; the Date.now()-only paragraph), §6.2 (tap budget — no new gate), 32-CONTEXT.md Area 2 (arm-delay mechanism: handler no-op, aria-disabled during the window, no flicker, silent swallow; keys through the same check; one guard in window.move; lastDismissAt on the true→false transition)
  </read_first>
  <action>
    <!-- planner-discipline-allow: aria-disabled -->
    <!-- planner-discipline-allow: transitionend -->
    <!-- planner-discipline-allow: animationend -->
    1. Classic guard helpers, placed directly after hasActiveEncounter() (before vitalsStrip), under one comment block (Phase 32, CMBUI-04/05, design §6.3): declare `let encRenderedAt = 0;`, `let armTimer = null;`, `let lastDismissAt = 0;`, `let encWasActive = false;`. Define `function encArmed()` returning true when the bridge is absent, else `window.__mzInputGuards.isArmed(encRenderedAt, Date.now())`; `function encounterSettled()` likewise with `isSettled(lastDismissAt, Date.now())`; `function armEncounterButtons()` which sets `encRenderedAt = Date.now()`, clears any pending `armTimer`, and schedules `armTimer = setTimeout(...)` for `ARM_DELAY_MS` (read from the bridge, default 250) that removes the `aria-disabled` attribute from every `#enc-panel [aria-disabled="true"]` element — the timer only tidies the accessibility attribute; the decision itself is ALWAYS the Date.now() comparison, never the timer; and `function guardTap(btn, fn)`: return if `!btn`; set `btn.setAttribute("aria-disabled", "true")`; assign `btn.onclick = () => { if (!encArmed()) return; fn(); }`. The comment must state: the handler no-ops inside the window (a tap that lands while the panel is still re-laying-out is swallowed silently — no toast, no disabled styling, the button looks normal); both guards are Date.now() checks and must never be re-expressed as a CSS transition/animation or a transitionend/animationend listener because the reduced-motion rule zeroes those; and that every guarded button renders INSIDE #enc-panel (hit-zone rule 3). Do not add any CSS for the aria-disabled state.
    2. renderEncounter() top: compute `const active = hasActiveEncounter();` once; if `encWasActive && !active` set `lastDismissAt = Date.now();` (the true→false transition — CMBUI-05: the overlay just closed under the thumb); set `encWasActive = active;`; keep the existing early return on `!active` (panel hidden, syncRoundCardLive(null)); immediately after `panel.hidden = false` call `armEncounterButtons();` so every button the rest of the function builds shares one fresh stamp.
    3. Wrap the §6.3 list — replace each direct `.onclick = () => …` assignment with `guardTap(document.getElementById("<id>"), () => …)` keeping the inner function body byte-identical: `a-fight` (mzFight), `a-strike` (mzAttack), `a-potion` (keep its full-health pre-check + hand-written block toast INSIDE the wrapped fn, unchanged text), `a-flee`, `a-spell` (the spellOpen toggle + re-render + scrollIntoView body), `a-talk`, `a-sing`, `a-scroll`, `a-join-yes`, `a-join-no`, `a-loot-take-all`, `a-loot-leave-all`, `a-find-take` and `a-find-leave` in BOTH find sub-branches, `btn-death-oracle` (the __mzShowTab("oracle") jump), and `a-next` (the beats Move on/Next handler). In wireDeathConfirm() wrap the Confirm handler the same way (`guardTap(btn, () => { … })`) — Confirm stays "armed" in the DR4 sense (no read-it-first lock), only the 250 ms re-render window is added. Keep every `if (x) …` null-guard pattern for optional buttons (a-spell/a-talk/a-sing/a-scroll may not exist) — guardTap itself tolerates null.
    4. renderCarriedList: add `opts.guard` — in `mkBtn`, when not disabled, use `guardTap(bt, onClick)` if `opts.guard` else the existing `bt.onclick = onClick`. Pass `guard: true` in exactly two call sites: the loot card's renderCarriedList (lootEquip/lootTake/lootLeave — "loot per-row") and the combat use-list's renderCarriedList (actions ["use"] — the "Items" entry of the action-bar list). The GEAR tab, the store sell list and renderDropShelf are NOT guarded (outside the ratified list). Leave the store `a-leave`/goods rows, the feature `a-evt` button, the won card's `btn-again` and the spell-menu buttons unguarded, and say so in the helper comment (Phase 33 candidate).
    5. Keys: in the keydown handler, make the FIRST statement inside `if (S.combat) {` be `if (!encArmed()) return;` with a comment (Phase 32 CMBUI-04: Enter/Space/1 on the Fight! gate and keys 1-7 in a fight share the buttons' arm window — a key that lands mid re-render is swallowed exactly like a tap). The beats-branch Enter/Space `n.click()` needs no change: a-next's onclick is now the guarded handler. Leave the key-2 full-health toast line byte-identical.
    6. window.move: directly after `if (hasActiveEncounter()) return;` add `if (!encounterSettled()) return;` with a comment (Phase 32 CMBUI-05, design §6.3 rule 2: the ONE dismiss-settle guard — a step that arrives within DISMISS_SETTLE_MS of the overlay closing was aimed at the card, not the corridor; covers D-pad, viewport tap and WASD/arrows because all of them funnel through here). No other movement site changes.
    7. Confirm no surface gained a tap-anywhere-to-dismiss: the round card, #enc-panel and #enc-body get no click/pointer listeners in this plan. Run `node --test test/unit/shell-fight-gate.test.js test/unit/shell-loot-screen.test.js test/unit/shell-round-card.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-armor-display.test.js test/unit/shell-party-camp.test.js` (existing region anchors — e.g. shell-armor-display's `a-leave` onclick anchor — must still hold) and `npm run build:www`. Commit: `feat(32-03): guard every encounter decision button with the arm window; settle window on window.move`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-fight-gate.test.js test/unit/shell-loot-screen.test.js test/unit/shell-round-card.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-armor-display.test.js test/unit/shell-party-camp.test.js 2>&1 | tail -5 && npm run build:www 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'function guardTap(btn, fn)' mazeworld.html` == 1 and `grep -c 'guardTap(' mazeworld.html` >= 21 (definition + 20 wiring sites: 7 action-bar, a-fight, 2 joiner, 2 loot-all, 4 find, btn-death-oracle, wireDeathConfirm's btn, a-next, mkBtn)
    - `grep -c 'isArmed(encRenderedAt, Date.now())' mazeworld.html` == 1 and `grep -c 'isSettled(lastDismissAt, Date.now())' mazeworld.html` == 1
    - `grep -c 'encRenderedAt = Date.now()' mazeworld.html` == 1 and `grep -c 'lastDismissAt = Date.now()' mazeworld.html` == 1
    - `grep -c 'if (!encounterSettled()) return;' mazeworld.html` == 1 and the line number of `if (!encounterSettled()) return;` is exactly one greater than the line number of `if (hasActiveEncounter()) return;` inside engineMove (the `hasActiveEncounter()) return;` occurrence with the greatest line number)
    - `grep -c 'if (!encArmed()) return;' mazeworld.html` == 1
    - `grep -c 'guard: true' mazeworld.html` == 2
    - `grep -c 'opts.guard ? guardTap(bt, onClick)' mazeworld.html` == 1
    - `grep -c 'removeAttribute("aria-disabled")' mazeworld.html` == 1 and `grep -c 'setAttribute("aria-disabled", "true")' mazeworld.html` == 1
    - `sed -n '/<style>/,/<\/style>/p' mazeworld.html | grep -c 'aria-disabled'` == 0 (no visual disabled state for the arm window)
    - `grep -c '\.onclick = () => window\.mz\(Attack\|Flee\|Parley\|Sing\|ReadScroll\|Fight\|TakeAllLoot\|LeaveAllLoot\|TakeFind\|LeaveFind\)' mazeworld.html` == 0 and `grep -c '\.onclick = () => window\.mzResolveJoiner' mazeworld.html` == 0 (every listed bridge is reached only through guardTap)
    - `grep -c 'Already at full health.", "block")' mazeworld.html` == 2
    - `grep -ci 'transitionend\|animationend' mazeworld.html` == 0
    - `npm run build:www` exit code 0; node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>Every ratified decision button ignores taps and keys for 250 ms after the panel is built (no visual flicker, no toast), a step within 250 ms of the overlay closing is refused, and nothing gained a dismiss gesture.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the guard wiring (shell-input-guards.test.js) and measure the worst-case round (round-card-worst-case.test.js)</name>
  <files>test/unit/shell-input-guards.test.js, test/unit/round-card-worst-case.test.js</files>
  <read_first>
    - test/unit/shell-loot-screen.test.js L24-60 (source-assertion helpers) and test/unit/shell-round-card.test.js (32-02: region helpers to mirror — renderEncounterRegion/dispatchRegion)
    - test/unit/foe-abilities.test.js L15-80 (fakeRng / fixedFighter / fixedFloor / fixedState / fixedFoe / fixedCombat — copy the fixed* helpers verbatim into the new measurement test; they are module-local, not exported) and L98-160 (how a foe gets an `abilities` kit)
    - engine/combat.js L226 (startCombat's combat shape: round 1, target 0, spellOpen false, tracked, pending true), L343-349 (fight: only acts on a pending combat), L435-500 (playerStrike; R.frenzy second swing for a Fridgian, `frenzy` event), L1696-1780 (foeTurn: `const swings = (f.frenzied ? 2 : 1) * ((f.sp && f.sp.atk) || 1)` — the foe-side frenzy; the abilities gate), engine/engine.js L34-70 (applyAction: structuredClone + makeRng(next.rngState); cases fight/attack), content/races.js "Fridgian" (frenzy: true), content/bestiary.js L58/L65/L75/L100 (Drake/Stalka Beast/Djinni/Krupke rows with sp.atk / abilities kits), content/foe-abilities.js L44-60 (kit ids)
    - src/browser/toasts.js L47-50 (PRIORITY, MAX_TOASTS), L826-858 (toastsForAction: deduped, priority-sorted, capped at MAX_TOASTS — the card's ceiling by construction), L139 (narrativeToastText)
    - 32-RESEARCH.md Pitfall 4 (combine BOTH frenzy mechanics) and Open Question 3 (a new narrowly-scoped test file using the foe-abilities harness)
  </read_first>
  <action>
    <!-- planner-discipline-allow: transitionend -->
    <!-- planner-discipline-allow: animationend -->
    1. test/unit/shell-input-guards.test.js (the owed guard-constant/wiring assertions), shell-loot-screen style: (a) a `guardHelpersRegion()` sliced from `let encRenderedAt = 0;` to `function vitalsStrip()` — assert it defines `function encArmed()`, `function encounterSettled()`, `function guardTap(btn, fn)` and `function armEncounterButtons()`; contains `isArmed(encRenderedAt, Date.now())` and `isSettled(lastDismissAt, Date.now())` (both predicates compared against the live clock at the call site — the module itself is clock-free, pinned in 32-01); contains `setAttribute("aria-disabled", "true")` and `removeAttribute("aria-disabled")`; contains NO transitionend / animationend / `.animate(` / `transition` / `animation` token (case-insensitive); (b) the window.__mzInputGuards bridge exists and src/browser/inputGuards.js exports both constants equal to 250 (import them); (c) a `renderEncounterRegion()` (same anchors as shell-round-card) — for each id in the exact list [a-fight, a-strike, a-potion, a-flee, a-spell, a-talk, a-sing, a-scroll, a-join-yes, a-join-no, a-loot-take-all, a-loot-leave-all, a-find-take, a-find-leave, btn-death-oracle, a-next] assert `guardTap(document.getElementById("<id>")` occurs (a-find-take/a-find-leave twice each), and assert the region contains no `getElementById("<id>").onclick =` for any of them; `function wireDeathConfirm` region contains `guardTap(btn,`; the renderCarriedList region (from `function renderCarriedList(` to `function renderDropShelf(`) contains `opts.guard ? guardTap(bt, onClick)`; CODE contains `guard: true` exactly twice, once inside the loot branch (between `if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {` and `if (S.pendingJoiner`) and once in the combat use-list block (after `id="combat-use-list"`); the store region (from `if (S.store) {` to `document.getElementById("a-leave").onclick`) contains no `guardTap(` (the store is outside the ratified list); (d) the renderEncounter region contains `lastDismissAt = Date.now()` inside a transition check that references `encWasActive`, and calls `armEncounterButtons()`; (e) the engineMove region (from `window.move = function engineMove` to `window.newGame = `) contains `if (hasActiveEncounter()) return;` immediately followed (next non-blank, non-comment line) by `if (!encounterSettled()) return;` and no other `isSettled`/`encounterSettled` call anywhere else in CODE (one guard, CONTEXT Area 2); (f) the keydown region (from `addEventListener("keydown"` to `addEventListener("resize"`) has `if (S.combat) {` followed by `if (!encArmed()) return;` as its first statement, and still contains `window.mzFight?.()` under `S.combat.pending`; (g) dismissal rule: the renderEncounter region contains no `panel.onclick`, `panel.addEventListener`, `body.onclick`, `body.addEventListener`, `card.onclick` or `card.addEventListener` (no tap-anywhere-to-dismiss; the round card has no gesture); (h) the `<style>` block (slice RAW HTML between `<style>` and `</style>`) contains no `aria-disabled` selector.
    2. test/unit/round-card-worst-case.test.js (the owed worst-case measurement, RESEARCH Pitfall 4 / design §6.7): import `applyAction` from ../../engine/engine.js, `toastsForAction, MAX_TOASTS, narrativeToastText, PRIORITY` from ../../src/browser/toasts.js, and copy the fixed* helpers from foe-abilities.test.js. Build the hero as a Fridgian Fighter (race "Fridgian" — player-side frenzy second swing) with `maxWP: 400, wp: 400` (survives every seed), Club, potions 0. Build FOUR live foes: (1) a Stalka Beast-shaped foe `{ name: "Stalka Beast", type: "Beasts", lvl: 4, size: "XL", intel: 15, wp: 94, maxWP: 94, sp: { atk: 2, dmg: { n: 1, sides: 4, bonus: 0 } }, frenzied: true, abilities: ["stalkaHeal", "stalkaLightning", "stalkaFireball", "stalkaFreeze"] }` (foe-side frenzy × multi-attack = 4 swings + a kit); (2) a Djinni-shaped caster with its four-spell kit and `frenzied: true`; (3) a Krupke-shaped foe with its kit; (4) a plain Giant Rat with `sp: { atk: 2 }, frenzied: true`. Two scenarios: SCENARIO fight — `combat: fixedCombat(foes, { pending: true })` and `applyAction(state, { type: "fight" })`; SCENARIO attack — `combat: fixedCombat(foes, { first: "foe" })` and `applyAction(state, { type: "attack" })`. For each scenario sweep `rngState` over seeds 1..400 (deterministic), run `applyAction({ ...state, rngState: seed }, action)`, take `events`, run `toastsForAction(action.type, events, {})`, map to lines through `narrativeToastText`, and track max events, max card lines (items with priority !== PRIORITY.block), max total characters, and the seed that produced each max; also count how many seeds produced ≥1 ability event (`foeCast`/bolt/drain/heal-style types — detect by checking any event carrying an `ability` field or whose type starts with "foe" and is not struckByFoe/foeMissed/foeKilled) to prove the kits fired. Assertions: `maxLines >= 2`, `maxLines <= MAX_TOASTS` (the pipeline's cap is the card's ceiling — no card-side cap exists), every line non-empty, at least one seed fired an ability, and for the attack scenario at least one seed produced a `frenzy` event (Fridgian second swing) AND at least one seed produced ≥ 4 foe swing events from the Stalka Beast (struckByFoe+foeMissed with its name) — proving BOTH frenzy mechanics were exercised. Print ONE line per scenario with `console.log` in the exact form `round-card worst case (<scenario>): seed=<n> events=<n> lines=<n> chars=<n> abilitySeeds=<n>` so the SUMMARY can quote it.
    3. Run `node --test test/unit/shell-input-guards.test.js test/unit/round-card-worst-case.test.js test/unit/inputGuards.test.js` → `# fail 0`; copy the two printed measurement lines into the SUMMARY (Task 3). Commit: `test(32-03): pin guard wiring; measure the worst-case round line count`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-input-guards.test.js test/unit/round-card-worst-case.test.js test/unit/inputGuards.test.js 2>&1 | grep -E 'round-card worst case|^# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/shell-input-guards.test.js && test -f test/unit/round-card-worst-case.test.js`
    - `node --test test/unit/shell-input-guards.test.js` → `# fail 0` and `# pass` >= 8
    - `node --test test/unit/round-card-worst-case.test.js 2>&1 | grep -c 'round-card worst case ('` == 2 and the run reports `# fail 0`
    - `grep -c 'frenzied: true' test/unit/round-card-worst-case.test.js` >= 3 and `grep -c '"Fridgian"' test/unit/round-card-worst-case.test.js` >= 1 and `grep -c 'abilities: \[' test/unit/round-card-worst-case.test.js` >= 3
    - `grep -c 'MAX_TOASTS' test/unit/round-card-worst-case.test.js` >= 2
    - `git diff --stat 9174e6e..HEAD -- engine content test/parity` prints nothing
  </acceptance_criteria>
  <done>The guard wiring is pinned button by button (and by absence on the unguarded set), the Date.now()-only rule is pinned, and the worst-case round is measured deterministically with both frenzy mechanics and ability kits in play, with the numbers ready for the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate — build:www, npm test, parity diff empty — and the hand-off SUMMARY</name>
  <files>mazeworld.html</files>
  <read_first>
    - tools/build-www.mjs (what build:www validates), package.json scripts
    - .planning/phases/32-combat-narrative-input-ui-build/32-01-SUMMARY.md and 32-02-SUMMARY.md ("Human verification (deferred to end of run)" blocks — merge them into this plan's list so the end-of-run device pass has one Phase 32 list)
    - .planning/phases/31-combat-start-gating-effect-hygiene/31-03-SUMMARY.md "Phase 33 hand-off"-style section (the hand-off shape to mirror)
    - .planning/ROADMAP.md Phase 33 entry (UIF-01..05, STORE-01) — what the UI Feel & Store Polish phase should know about the new combat surface
  </read_first>
  <action>
    1. Run `npm run build:www` — must exit 0 (fix any shell syntax slip from Tasks 1-2 rather than skipping the build). Run `npm test` — `# fail 0`, pass count above the 1855 baseline (record the exact number). Run `git diff --stat 9174e6e..HEAD -- engine content test/parity` and `git status --porcelain test/parity/prototype-master.js.txt` — both empty.
    2. In the SUMMARY record: the exact `npm test` pass count; the two `round-card worst case (…)` measurement lines verbatim (seed/events/lines/chars/abilitySeeds), with the note that the folded line count is bounded by toastsForAction's MAX_TOASTS (4) because the card consumes the pipeline's output unmodified (CONTEXT Area 1) while the character count is what the 40 % max-height/inner-scroll actually absorbs; the guarded-button list as wired; and the deliberately-unguarded set (store rows/Leave, drop shelf, a-evt, btn-again, spell-menu buttons).
    3. Write `## Phase 33 hand-off` in the SUMMARY: (a) haptics candidate — `src/browser/haptics.js` + the Settings toggle already exist; hapticForEvents still fires on struck/struckByFoe/crit/trap/leveled and is unchanged by Phase 32; §6.7 names a light tap on hit/kill as an enhancer, never a decision factor — a Phase 33 polish item if the user wants it; (b) the unguarded set above as a Phase 33 candidate should the DR round show a mis-tap on store rows or the spell menu; (c) the combat surface Phase 33's toolbar/gear-panel layout must be laid out against: head → roster → Round Card (max-height 40 %, inner scroll) → Fight!/action bar → use-list → spell menu, with #enc-dismiss-slot Move on in the topbar; the card has no animation (a Phase 33 entrance cue, if added, must not become a guard); (d) the round label is the round the player acted in while the topbar counter is the round now in progress — if the DR round finds that confusing, the copy lives in ROUND_CARD_COPY only; (e) `window.__mzRoundCard` is the only new presentation state; `#enc-round-live` is the persistent announcer.
    4. Write the CMBUI-06 DR-round checklist as this plan's `## Human verification (deferred to end of run)` (merge 32-01/32-02's bullets in front of it), Pixel 7, never a mid-run pause: (1) walk into an encounter with a D-pad double-tap — the second tap must NOT press Fight! (arm window); Fight! looks normal, not greyed; (2) after Fight!, mash Strike twice fast — exactly one strike resolves per deliberate tap after the panel settles, no "stuck" button; (3) tap a foe row to retarget then immediately Strike — the strike lands on the new target (a swallowed tap within 250 ms is acceptable, a wrong-target strike is not); (4) Move on from a floor-change/level-up card and immediately tap the D-pad — the party does not step for the settle window, then steps normally; the same after Take all / Leave all on a loot card and after Leave them on a joiner; (5) die — Review the Oracle and Confirm both work after the panel settles, Confirm needs no read-first lock; (6) keyboard (if a BT keyboard is handy) or the on-screen keys: Enter on the Fight! gate right after the panel appears is swallowed, a second Enter fights; (7) reduced motion ON in Android settings: every check above behaves identically (guards are clock checks); (8) TalkBack: every guarded button is still announced as a button and becomes tappable after the arm window; (9) the whole DR round feel: a combat round reads in one place, moving on never needs a dismiss-then-continue, nothing important vanished under a movement tap — the CMBUI-06 sign-off question for the user.
    5. Update the STATE.md / ROADMAP.md plan checkboxes per the execute-plan workflow's metadata step (the orchestrator's `phase.complete` step handles VERIFICATION; note in the SUMMARY that CMBUI-06 is a `human_verification` item that does not gate phase completion, per CONTEXT Area 3).
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && npm run build:www 2>&1 | tail -3 && npm test 2>&1 | grep -E '^# (pass|fail)' && git diff --stat 9174e6e..HEAD -- engine content test/parity && git status --porcelain test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:www` exit code 0
    - `npm test` → `# fail 0` and `# pass` >= 1880
    - `git diff --stat 9174e6e..HEAD -- engine content test/parity` prints nothing and `git status --porcelain test/parity/prototype-master.js.txt` prints nothing
    - `grep -c 'round-card worst case (' .planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md` >= 2
    - `grep -c '## Phase 33 hand-off' .planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md` == 1 and `grep -c '## Human verification (deferred to end of run)' .planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md` == 1
  </acceptance_criteria>
  <done>The bundle builds, the suite is green, the engine/content/parity tree is byte-identical to the phase start, and the SUMMARY carries the measured line count, the Phase 33 hand-off and the CMBUI-06 checklist for the end-of-run device pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM taps/keys → guarded handlers → window.mz* bridges | untrusted input timing becomes a validated action only after the arm window; the engine still refuses out-of-turn actions itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-05 | Denial of Service | armEncounterButtons per render | low | mitigate | a re-render restarts a 250 ms window only; the decision is a Date.now() comparison, so a stuck timer can never lock a button (the timer only tidies aria-disabled) — pinned |
| T-32-06 | Tampering | reduced-motion zeroing a guard | low | mitigate | no transition/animation token in the guard helpers or the module — pinned by shell-input-guards + inputGuards tests |
| T-32-07 | Elevation of Privilege | bypassing the arm window via keyboard | low | mitigate | the keydown S.combat branch runs the same encArmed() check; the beats Enter/Space path clicks the guarded a-next handler |
</threat_model>

<verification>
- `npm run build:www` exit 0; `npm test` `# fail 0` (> 1855); `git diff --stat 9174e6e..HEAD -- engine content test/parity` empty; `test/parity/prototype-master.js.txt` untouched.
- shell-input-guards + round-card-worst-case + inputGuards green; every 32-02 pin still green.
- SUMMARY carries the measurement lines, the Phase 33 hand-off and the CMBUI-06 checklist.
</verification>

<success_criteria>
- ROADMAP success criteria 3 and 4 are code-complete and pinned: no listed decision button can fire from a tap that lands during a re-render, and no step can register within the settle window after an overlay closes; nothing dismisses on a movement tap.
- Criterion 5 (CMBUI-06, the on-device DR round) is queued as deferred human verification with an exact checklist; it does not gate phase completion.
</success_criteria>

<output>
Create `.planning/phases/32-combat-narrative-input-ui-build/32-03-SUMMARY.md` when done. The SUMMARY MUST carry: `## Self-Check` (files exist, commit hashes found, exact `npm test` counts, build exit code, the empty engine/content/test/parity diff), `## Deviations` (or "None"), `## Human verification (deferred to end of run)` (32-01 + 32-02 bullets merged in front of the CMBUI-06 DR-round checklist from Task 3 step 4 — Pixel 7, never a mid-run pause), and `## Phase 33 hand-off` (Task 3 step 3: haptics candidate, the unguarded set, the final combat surface layout, the round-label note, the new presentation state). Also quote the two `round-card worst case (…)` measurement lines verbatim.
</output>
