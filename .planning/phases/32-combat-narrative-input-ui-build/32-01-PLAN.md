---
phase: 32-combat-narrative-input-ui-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/inputGuards.js
  - test/unit/inputGuards.test.js
  - mazeworld.html
autonomous: true
requirements: [CMBUI-04, CMBUI-05]

must_haves:
  truths:
    - "CMBUI-04/05 (foundation): src/browser/inputGuards.js is a zero-import pure module exporting ARM_DELAY_MS = 250, DISMISS_SETTLE_MS = 250, isArmed(renderedAt, now) and isSettled(lastDismissAt, now); both predicates are plain millisecond comparisons on their ARGUMENTS (the clock is passed in, never read inside the module), true at exactly the threshold and false one millisecond under, and a non-finite/undefined timestamp counts as 0 (armed/settled) so a never-rendered button or a fresh page can never be locked out"
    - "The module is bridged onto the classic script as window.__mzInputGuards = { ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled } from the trailing module script, in the same bridge block and style as window.__mzCanCast / window.__mzConditionsOf; nothing in this plan wires a button yet (that is 32-03)"
    - "test/unit/inputGuards.test.js pins the two constants, the boundary behaviour of both predicates, the module's purity (no clock read, no DOM, no transition/animation event names, no engine import), and the bridge line in mazeworld.html"
    - "RESEARCH finding 3 verified empirically: test/unit/feedback-payload.test.js needs zero changes (37 pass, untouched); the full suite stays green from the 1855 baseline"
    - "No engine change: git diff --stat 9174e6e..HEAD -- engine content test/parity is empty"
  artifacts:
    - "src/browser/inputGuards.js (new): ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled"
    - "test/unit/inputGuards.test.js (new): constants + boundaries + purity + bridge pins"
    - "mazeworld.html: one import line for ./src/browser/inputGuards.js beside the controls.js import; one window.__mzInputGuards bridge assignment beside window.__mzCanCast"
  key_links:
    - "mazeworld.html module script import → window.__mzInputGuards → (32-03) classic encArmed()/encounterSettled() helpers compare isArmed(encRenderedAt, Date.now()) / isSettled(lastDismissAt, Date.now())"
---

<objective>
Create the pure timing-guard module the Round Card's button guards run on: `src/browser/inputGuards.js` (`ARM_DELAY_MS`/`DISMISS_SETTLE_MS` = 250, `isArmed`/`isSettled` predicates — CONTEXT Area 2), unit-test it without a DOM, and bridge it onto the classic script as `window.__mzInputGuards` exactly like the other `__mz*` bridges. This plan wires no button and changes no behaviour — it is the foundation 32-03 wires against, kept separate so the guard math is proven before it touches `renderEncounter`.

Purpose: the guards are the CMBUI-04/05 mechanism (arm delay after a re-render; settle window after a dismissal). Both MUST be `Date.now()` comparisons, never a CSS transition/animation (the blanket `prefers-reduced-motion` rule at mazeworld.html ~L773 zeroes every transition/animation) — a pure module with the clock passed in makes that structurally testable.
Output: `src/browser/inputGuards.js`, `test/unit/inputGuards.test.js`, the import + bridge lines in `mazeworld.html`, `npm test` `# fail 0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Commit trailers (every commit in this phase, no exceptions — task commits and the metadata commit alike): end every commit message with these two lines, in this order:

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS

Hard scope rule for the whole phase: shell/presentation/tests only. Do not edit anything under `engine/`, `content/`, or `test/parity/` (including `test/parity/prototype-master.js.txt`); `src/browser/toasts.js`'s groupers are reused unmodified (32-02 adds only an optional `limit` argument on toastsForAction's pipeline tail so the Round Card is uncapped; this plan does not touch the file). UAT is deferred to the end of the autonomous run — never pause for a device check; record Pixel 7 items in the SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-combat-narrative-input-ui-build/32-CONTEXT.md
@.planning/phases/32-combat-narrative-input-ui-build/32-RESEARCH.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-03-SUMMARY.md
@docs/COMBAT-NARRATIVE-DESIGN.md
@src/browser/controls.js
@test/unit/controls.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure inputGuards module and its unit test (RED then GREEN)</name>
  <files>src/browser/inputGuards.js, test/unit/inputGuards.test.js</files>
  <read_first>
    - src/browser/controls.js L1-23 (the zero-import, DOM-free sibling-constants precedent: header comment style, `export const TAP_MOVE_THRESHOLD_PX = 10` / `TAP_MAX_DURATION_MS = 350`, JSDoc per export)
    - test/unit/controls.test.js L1-45 (the "exports pure constants" test shape and the node:test/assert imports)
    - test/unit/toastsCoverage.test.js L230-248 (PURE_MODULE_FILES / IMPURITY_PATTERNS — the purity-scan style to mirror for the new module; do NOT add inputGuards.js to that list, write the same kind of scan inside the new test)
    - docs/COMBAT-NARRATIVE-DESIGN.md §6.3 rules 1-2 and the bold "Both guards are Date.now() timestamp checks" paragraph; 32-CONTEXT.md Area 2 "Module" bullet
  </read_first>
  <behavior>
    - ARM_DELAY_MS === 250 and DISMISS_SETTLE_MS === 250 (both exported, both plain numbers)
    - isArmed(1000, 1249) === false; isArmed(1000, 1250) === true; isArmed(1000, 5000) === true (armed at exactly the threshold, not before)
    - isSettled(1000, 1249) === false; isSettled(1000, 1250) === true; isSettled(0, 249) === false; isSettled(0, 250) === true
    - isArmed(undefined, 100) === true and isArmed(NaN, 100) === true and isSettled(undefined, 100) === true — a missing/non-finite stamp counts as 0, so a fresh page or a never-rendered button is armed/settled (fail-open, never a locked UI); isArmed(0, 0) === false (a stamp of 0 at time 0 is inside the window)
    - Purity (source scan of src/browser/inputGuards.js after stripping comments): zero occurrences of `Math.random`, the clock-read call (the global Date object's now method), `document.`, `window.`, `globalThis.`, `localStorage`, `setTimeout`, `requestAnimationFrame`, any `from "…engine/…"` import, and zero case-insensitive occurrences of the words `transition` and `animation` (the module must not even mention a transition-end or animation-end event)
    - The module has no import statements at all (mirrors controls.js)
  </behavior>
  <action>
    <!-- planner-discipline-allow: Date.now -->
    1. Write test/unit/inputGuards.test.js FIRST (RED — the import fails until the module exists). Import `ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled` from "../../src/browser/inputGuards.js" and pin every line of the behavior block above: the two constants; the isArmed/isSettled boundary table (249 → false, 250 → true, plus the 0/0 case); the non-finite-stamp cases; and a purity scan that readFileSync's the module, strips `//` line comments then `/* */` block comments (same order as toastsCoverage.test.js), and asserts the forbidden patterns listed in the behavior block are absent — build the clock-read pattern from fragments (e.g. join "Date." and "now") so the test file itself does not carry the literal, the same fragment trick shell-toast-wiring.test.js uses for dead names. Also assert the stripped source contains no `import ` statement. Leave one test (the bridge pin, Task 2) out for now.
    2. Run `node --test test/unit/inputGuards.test.js` and confirm it FAILS on the missing module (RED). Commit: `test(32-01): add failing inputGuards module test`.
    3. Create src/browser/inputGuards.js (GREEN): a header comment in the controls.js style stating the module is the single home of the two Phase 32 tap-safety timing guards (CMBUI-04/05, design §6.3), that it is DOM-free and clock-free — callers pass the current millisecond reading as `now` so the predicates stay pure and unit-testable — and that neither guard may ever be re-implemented as a CSS transition/animation because the app's reduced-motion rule zeroes those. Export `ARM_DELAY_MS = 250` (JSDoc: milliseconds a freshly rendered decision button ignores taps after renderEncounter builds it) and `DISMISS_SETTLE_MS = 250` (JSDoc: milliseconds window.move refuses input after the encounter overlay dismisses). Export `isArmed(renderedAt, now)` returning whether `now - renderedAt >= ARM_DELAY_MS`, and `isSettled(lastDismissAt, now)` returning whether `now - lastDismissAt >= DISMISS_SETTLE_MS`; both coerce a non-finite first argument to 0 through one small internal `stampOf(v)` helper (Number.isFinite ? v : 0) and coerce `now` the same way. No other exports, no imports.
    4. Run the test again — GREEN. Run `node --test test/unit/controls.test.js test/unit/toastsCoverage.test.js` to confirm the sibling/purity suites are untouched. Commit: `feat(32-01): add pure inputGuards module (ARM_DELAY_MS/DISMISS_SETTLE_MS, isArmed/isSettled)`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/inputGuards.test.js test/unit/controls.test.js test/unit/toastsCoverage.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/inputGuards.js && test -f test/unit/inputGuards.test.js`
    - `grep -c '^export const ARM_DELAY_MS = 250;' src/browser/inputGuards.js` == 1 and `grep -c '^export const DISMISS_SETTLE_MS = 250;' src/browser/inputGuards.js` == 1
    - `grep -c '^export function isArmed(renderedAt, now)' src/browser/inputGuards.js` == 1 and `grep -c '^export function isSettled(lastDismissAt, now)' src/browser/inputGuards.js` == 1
    - `grep -v '^\s*//' src/browser/inputGuards.js | grep -v '^\s*\*' | grep -c 'Date.now'` == 0
    - `grep -v '^\s*//' src/browser/inputGuards.js | grep -v '^\s*\*' | grep -ci 'transition\|animation\|setTimeout\|document\.\|window\.'` == 0
    - `grep -c '^import ' src/browser/inputGuards.js` == 0
    - node --test test/unit/inputGuards.test.js → `# fail 0` and `# pass` >= 5
  </acceptance_criteria>
  <done>The two guard constants and two predicates exist as a pure, clock-free module with boundary-exact unit coverage and a purity scan that would catch any future transition/animation or clock read inside it.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge the module onto the classic script as window.__mzInputGuards and confirm the re-pin baseline</name>
  <files>mazeworld.html, test/unit/inputGuards.test.js</files>
  <read_first>
    - mazeworld.html ~L5683 (the `import { screenToCell, resolveTapDirection, classifyPointerGesture } from "./src/browser/controls.js";` line — the new import goes directly beneath it) and ~L5734-5752 (the `window.__mzConditionsOf = conditionsOf;` / `window.__mzCanCast = canCast;` / `window.__mzArmorDisplay = { armorDisplay, bagArmorText };` bridge block with its comment style — the new bridge goes right after `window.__mzCanCast = canCast;`)
    - test/unit/shell-loot-screen.test.js L24-45 (readFileSync + CRLF normalize + stripComments source-assertion helpers to copy into the new test's bridge pin)
    - 32-RESEARCH.md "Pitfall 6" and "Assumptions Log" (feedback-payload.test.js needs no re-pin; verify empirically here)
  </read_first>
  <action>
    1. In the trailing module script add, immediately below the controls.js import, `import { ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled } from "./src/browser/inputGuards.js";` with a one-line comment: Phase 32 (CMBUI-04/05) — the pure tap-safety timing guards, bridged below for renderEncounter/window.move.
    2. In the bridge block, right after `window.__mzCanCast = canCast;`, add `window.__mzInputGuards = { ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled };` preceded by a short comment in the same voice as the surrounding bridges: Phase 32 (CMBUI-04/05) — the classic renderEncounter()/keydown/window.move guards (32-03) read the arm-delay and dismiss-settle predicates through this ONE bridge; the module never reads the clock itself, the call sites pass Date.now(). Read-only, no rng, no DOM.
    3. Append the bridge pin to test/unit/inputGuards.test.js: readFileSync mazeworld.html (normalize CRLF, strip comments in the shell-loot-screen order), assert the exact import line exists once and the exact bridge assignment exists once, and assert the bridge assignment sits inside the module script (its index is greater than the index of `<script type="module">`).
    4. Verify RESEARCH finding 3 rather than trusting it: `node --test test/unit/feedback-payload.test.js` → 37 pass with the file untouched (`git status --porcelain test/unit/feedback-payload.test.js` empty). Record the number in the SUMMARY under Self-Check. Do not budget any change to that file in this phase.
    5. Run `node --test test/unit/inputGuards.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-fight-gate.test.js test/unit/shell-loot-screen.test.js` (the shell source-assertion suites must not notice the two new lines), then `npm run build:www` (exit 0 — build-www.mjs copies src/ wholesale, no registration needed) and `npm test` (`# fail 0`; pass count >= 1855 + the new tests). Commit: `feat(32-01): bridge inputGuards onto window.__mzInputGuards`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/inputGuards.test.js test/unit/feedback-payload.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-fight-gate.test.js test/unit/shell-loot-screen.test.js 2>&1 | tail -5 && npm run build:www 2>&1 | tail -2 && git status --porcelain test/unit/feedback-payload.test.js test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled } from "./src/browser/inputGuards.js";' mazeworld.html` == 1
    - `grep -c 'window.__mzInputGuards = { ARM_DELAY_MS, DISMISS_SETTLE_MS, isArmed, isSettled };' mazeworld.html` == 1
    - `git status --porcelain test/unit/feedback-payload.test.js` prints nothing and `node --test test/unit/feedback-payload.test.js` → `# pass 37`
    - `npm run build:www` exit code 0
    - `npm test` → `# fail 0` and `# pass` >= 1860
    - `git diff --stat 9174e6e..HEAD -- engine content test/parity` prints nothing
  </acceptance_criteria>
  <done>The classic script can reach the guards through one bridge; the suite is green with the new module tests added; feedback-payload is confirmed untouched; no engine/parity file moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module script → classic script (window.__mzInputGuards) | pure predicates exposed as globals; no state mutation, no rng |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-01 | Denial of Service | isArmed/isSettled with a bad stamp | low | mitigate | non-finite stamps coerce to 0 (fail-open) so a bug can never lock every decision button; pinned by the unit test |
| T-32-02 | Tampering | reduced-motion zeroing a timing guard | low | mitigate | the module is clock-free and transition-free by construction; the purity scan fails the build if either word or a clock read ever enters it |
</threat_model>

<verification>
- `node --test test/unit/inputGuards.test.js` green; `npm test` `# fail 0`; `npm run build:www` exit 0.
- `git diff --stat 9174e6e..HEAD -- engine content test/parity` empty; `test/parity/prototype-master.js.txt` untouched.
- feedback-payload.test.js: 37 pass, file unchanged (RESEARCH finding 3 confirmed, recorded in the SUMMARY).
</verification>

<success_criteria>
- The guard math exists as a pure, tested module and is reachable from the classic script — the CMBUI-04/05 mechanism 32-03 wires is proven before any button is touched.
- Nothing player-visible changes in this plan.
</success_criteria>

<output>
Create `.planning/phases/32-combat-narrative-input-ui-build/32-01-SUMMARY.md` when done. The SUMMARY MUST carry these sections: `## Self-Check` (files exist, commit hashes found, `npm test` counts, build exit code, the feedback-payload 37/unchanged confirmation, the empty engine/content/test/parity diff), `## Deviations` (or "None"), and `## Human verification (deferred to end of run)` with Pixel 7 bullets — for this plan a single bullet stating there is no player-visible change yet and that the guard behaviour is verified on device under 32-03's list. Never instruct a pause for device checks.
</output>
