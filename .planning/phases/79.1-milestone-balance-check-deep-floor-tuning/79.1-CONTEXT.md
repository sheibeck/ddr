# Phase 79.1: Milestone Balance Check & Deep-Floor Tuning - Context

**Gathered:** 2026-09-26 (user ruling, mid-milestone)
**Status:** Ready for planning, once Phase 79 is complete

<domain>
## Phase Boundary

The ONE place in milestone v2.1 where the balance bots run, once all milestone code is complete. It is the final check of the whole milestone's balance, and it is where the Phase 75.3 deep-floor dials get tuned.

**User ruling (2026-09-26):**
- "we should wait until all the fixes are in for a milestone before we run bots to check for our depth scaling. That's expensive, and takes time, and should always be after milestone code is complete."
- "bots always at the end only as a final check of all the code."
</domain>

<decisions>
## Implementation Decisions

- **Inputs moved here:**
  - The checkpointed tail sweep, formerly plan 75.3-06. Its full plan is kept verbatim in `SOURCE-75.3-06-sweep-plan.md` in this directory.
  - Every per-plan BEFORE/AFTER readout, per-race run and deep slice that was deferred from 75.2-01/02/05, 75.3-01/02/03/04/05/07, 76-01 and 78-01.
  - The balance verdicts those plans would have written.
- **Baseline:** the last pre-milestone readout already committed. Use the Phase 75 phase-BEFORE (`tools/readouts/75-02-before.txt`) and the Phase 73/74 roll-high baseline where relevant, plus any BEFORE files already written: 75.2-01's before and races-before, and 75.1's. Take no new mid-milestone baselines.
- **Final readouts (all against the finished code):**
  - the fair bot at 200 and 1,000 seeds;
  - per class (`tune-classes`) and per race (Elven, Dwarven, Troll; hero size, Phase 75.2);
  - the Freeze/Weaken/Doze rotation Sorcerer (75.3-02's `--control-rotation`);
  - deep-start slices from 12, 20, 30 and 40;
  - a Troll Summoner.
- **Tail sweep:** the checkpointed fit protocol from project memory: blocks of 10, stop on a failure pattern, the orchestrator adjusts search parameters, and engine-shape changes go to the user. It runs against the RULED tail targets in 75.3-CONTEXT ("Design rulings after planning"). Floors 1–12 are measured, never refitted.
- **Verdicts:** a PASS or MISS table against every ruled target, recorded in `docs/DIFFICULTY-RETUNE.md`. Every miss, including Phase 75.1's floor-11 survival miss, is reported to the user with 1,000-seed evidence. There is no silent compensation.
- **Order:** this phase runs BEFORE Phase 80. The tuned dial values are code, and the release build has to package them.
- **Cheap checks are unaffected:** per-wave `npm test` (including the bot state-pin tests), parity and boot:check stay in every phase. Only the expensive bot balance runs moved here.

### Claude's Discretion
- The plan split, block sizing for the sweep, and the readout-file naming.
</decisions>

<deferred>
## Deferred Ideas
- None.
</deferred>
