---
phase: 67-play-games-integration-account-chip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/settings.js
  - test/unit/settings.test.js
  - src/browser/playGames.js
  - test/unit/playGames.test.js
  - docs/PLAY-GAMES-SETUP.md
  - test/unit/play-games-runbook.test.js
autonomous: true
requirements: [PGS-02, PGS-01]
user_setup: []

must_haves:
  truths:
    - "settings.js persists three new fields in the existing ddr.settings.v1 blob: compete (default true, D-01), pgsWelcomed (default false, D-04) and pgsDevSignedIn (default false, D-12); an old blob without them reads the defaults with no migration, and invalid values are rejected like every other field (D-02)"
    - "src/browser/playGames.js is the only module that names the plugin: createPlayGames({ loadPlugin }) wraps it behind init/isAuthenticated/signIn/getPlayer, and createFakePlayGames(...) exposes the identical four methods in memory for tests and the browser dev loop (D-12)"
    - "Constructing either provider never loads the plugin; the plugin module is loaded lazily on the first method call only, so a caller that never calls (Compete OFF) never loads it (D-02)"
    - "init() runs initialize() then signIn({ silent: true }); signIn() runs initialize() then signIn({ silent: false }) explicitly, because the plugin's silent option defaults to true (67-01 research correction); both resolve { signedIn, player } and never throw or reject: a load failure, a rejection, a decline or no profile all resolve signed out (D-01, D-11)"
    - "The player identity is normalized to exactly { id, displayName }; avatar, hi-res and banner image URLs are dropped (D-07, no remote profile image)"
    - "Neither provider has a sign-out method (D-03), and neither exposes the Phase 68 leaderboard methods yet; RESERVED_LEADERBOARD_METHODS names them for Phase 68 (D-14, D-18, D-19 carried to Phase 68)"
    - "The Capacitor plugin proxy is never returned from an async function or used as a resolution value (nativeChrome.js#loadApp's thenable trap): a plugin whose 'then' property throws still works through every method"
    - "docs/PLAY-GAMES-SETUP.md is the console runbook: enable PGS, link the Play App Signing key's SHA-1 (not the upload key's), obtain the APP_ID and where to paste it, the tester allow-list, what Phase 69 completes, and the engineering notes carried to Phase 68 (D-16, D-14, D-18, D-19)"
  prohibitions:
    - statement: "Must not call the plugin's telemetry-shaped surface (game stats and events recording, recall access, server-side access, player stats); this phase uses sign-in and player identity only"
      status: unresolved
      verification: judgment
    - statement: "Must not launch the interactive Play Games sign-in flow except when the player taps the Sign in row; launch and Compete-ON attempts are silent"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/playGames.js"
      provides: "PROVIDER_METHODS, RESERVED_LEADERBOARD_METHODS, FAKE_PLAYER, createPlayGames, createFakePlayGames"
      contains: "export function createPlayGames"
    - path: "test/unit/playGames.test.js"
      provides: "provider contract, laziness, normalization, never-throw, no-sign-out, thenable-proxy and source pins"
      min_lines: 150
    - path: "src/browser/settings.js"
      provides: "compete, pgsWelcomed, pgsDevSignedIn defaults and allowed values"
      contains: "compete: true"
    - path: "docs/PLAY-GAMES-SETUP.md"
      provides: "the D-16 Play Console runbook and the notes carried to Phase 68"
      contains: "Play App Signing"
  key_links:
    - from: "src/browser/playGames.js#createPlayGames"
      to: "the plugin package (vendored and import-mapped by 67-06)"
      via: "the default loadPlugin's dynamic import, reached only on a provider method call"
      pattern: "import\\(\"@modbender/capacitor-play-games\"\\)"
    - from: "src/browser/settings.js SETTINGS_DEFAULTS"
      to: "the account controller (67-07) and the shell boot (67-08)"
      via: "readSettings() / writeSetting('compete' | 'pgsWelcomed', ...)"
      pattern: "compete"
---

<objective>
Lay the identity foundation every later plan builds on: the persisted Compete setting and first-sign-in flag, the D-12 provider seam with its native and fake implementations, and the D-16 console runbook. Implements D-01 (Compete defaults ON), D-02 (the provider never loads the plugin until called; the caller decides), D-03 (no sign-out method), D-04 (the persisted first-sign-in flag), D-07 (identity carries no image URL), D-11 (every non-success resolves signed out, no retry here), D-12 (the provider seam, fake, and the dev simulate-signed-in setting), D-15 (the runbook documents the APP_ID placeholder and its graceful failure) and D-16 (the runbook). D-14, D-18 and D-19 are carried to Phase 68: encoded in docs/PLAY-GAMES-SETUP.md's engineering notes and in the provider interface's RESERVED_LEADERBOARD_METHODS; no submission is implemented here.

Purpose: one seam that Phase 68's leaderboard calls extend, testable without a device.
Output: settings.js fields, src/browser/playGames.js, docs/PLAY-GAMES-SETUP.md, and their tests.
</objective>

<assumption_delta_decision>
noun: the player identity returned by playGames.getPlayer() (and carried in init()/signIn() results) as { id, displayName }
detector: fired on the roadmap's plugin-selection wording ("alternative", "chosen"), a lexical false positive
decision: no-change
rationale: Phase 67 introduces the first and only identity provider (Google Play Games Services v2). There is no second provider to reconcile. The D-12 provider seam keeps any future second provider an add-behind-the-interface change: a new create*() factory exposing the same PROVIDER_METHODS, with no caller edits.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-RESEARCH.md

<interfaces>
The plugin's JS surface (from the planner's read-only pre-read of the 0.5.0 tarball; 67-01 confirms it):
- The module exports `PlayGames`, a Capacitor registerPlugin proxy (every property access, including `then`, is forwarded to native).
- `PlayGames.initialize(): Promise<void>`
- `PlayGames.signIn(opts?: { silent?: boolean }): Promise<{ signedIn: boolean, player?: PlayerInfo }>`: silent DEFAULTS TO TRUE; failures resolve { signedIn: false }
- `PlayGames.isSignedIn(): Promise<{ signedIn: boolean }>`
- `PlayGames.getPlayer(): Promise<PlayerInfo>`: rejects when signed out
- `PlayerInfo`: { playerId: string, displayName: string, avatarUrl?, hiResImageUrl?, bannerImageUrl..., title?, level/XP, friend status }
- Phase 68 names (not bound here): submitScore({ leaderboardId, score, scoreTag? }), loadTopScores, loadPlayerCenteredScores, loadCurrentPlayerScore, loadFriends

Provider contract this plan defines (67-07's controller and 67-08's shell consume it):
- `PROVIDER_METHODS` = ["init", "isAuthenticated", "signIn", "getPlayer"]
- `init()` resolves { signedIn: boolean, player: { id, displayName } | null }, the silent launch attempt
- `signIn()` resolves the same shape, the interactive attempt (only ever from the Sign in row)
- `isAuthenticated()` resolves a boolean
- `getPlayer()` resolves { id, displayName } or null
- Every method never throws and never rejects.

settings.js today: SETTINGS_DEFAULTS { sound, haptics, textSize, confirmBeforeQuit, dressing } frozen; ALLOWED_VALUES per key; readSettings() merges only known keys with allowed values; writeSetting(key, value) rejects unknown keys and invalid values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: settings.js gains compete, pgsWelcomed and pgsDevSignedIn</name>
  <files>src/browser/settings.js, test/unit/settings.test.js</files>
  <read_first>
    - src/browser/settings.js (the whole file: header, SETTINGS_DEFAULTS, ALLOWED_VALUES, readSettings, writeSetting)
    - test/unit/settings.test.js (the defaults pin at ~49, the key-order/length pin at ~139-142, the independence and tolerant-load tests)
    - test/unit/sfx-settings.test.js and test/unit/shell-gear-toolbar.test.js (other SETTINGS_DEFAULTS readers)
  </read_first>
  <behavior>
    - SETTINGS_DEFAULTS keys in order: sound, haptics, textSize, confirmBeforeQuit, dressing, compete, pgsWelcomed, pgsDevSignedIn; compete true, pgsWelcomed false, pgsDevSignedIn false
    - readSettings() on an old blob holding only the five original keys returns compete true, pgsWelcomed false, pgsDevSignedIn false
    - writeSetting("compete", false) persists false and changes no other field; writeSetting("pgsWelcomed", true) changes only pgsWelcomed
    - writeSetting("compete", "no") and writeSetting("compete", 0) are no-ops returning the current settings
    - A persisted compete of "off" (invalid) reads back as the default true
  </behavior>
  <action>
Per D-01, D-02, D-04 and D-12 (Claude's discretion: the storage keys live in the existing settings blob, so one storage write per change and the same fail-open posture):

1. In src/browser/settings.js append three fields to SETTINGS_DEFAULTS, after dressing and in this order: `compete: true` (D-01: Compete is ON on a fresh install; D-02: OFF means the provider is never called), `pgsWelcomed: false` (D-04: the first-sign-in card has been shown), `pgsDevSignedIn: false` (D-12: dev-only; seeds the in-memory fake provider as signed in, in the browser dev loop only, and is ignored on native). Add each to ALLOWED_VALUES as [true, false]. Update the header comment: eight fields now, with one sentence per new field naming its phase and decision. Keep the tolerant-load wording (an old blob without these keys reads their defaults with no migration).

2. Update test/unit/settings.test.js: the defaults test covers the three new defaults; the key-order pin lists all eight keys and length 8 (keeping the retired handedness-key assertion); add one test per behaviour bullet.
  </action>
  <verify>
    <automated>node --test test/unit/settings.test.js test/unit/sfx-settings.test.js test/unit/shell-gear-toolbar.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compete: true" src/browser/settings.js` equals 1, and `grep -c "pgsWelcomed: false" src/browser/settings.js` and `grep -c "pgsDevSignedIn: false" src/browser/settings.js` each equal 1
    - `node --test test/unit/settings.test.js` passes, including the eight-key order pin and the old-blob tolerant-load test
  </acceptance_criteria>
  <done>Compete, the first-sign-in flag and the dev flag persist through the existing settings path with defaults and validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/browser/playGames.js, the provider seam (native + fake)</name>
  <files>src/browser/playGames.js, test/unit/playGames.test.js</files>
  <read_first>
    - src/browser/nativeChrome.js (loadApp and its comment: the Capacitor proxy must be wrapped in a plain object and never returned from an async function; the globalThis override hook pattern, which this module replaces with an injected loader)
    - src/browser/haptics.js (a small native-plugin wrapper with a guarded dynamic import and a never-throw posture)
    - src/browser/newBest.js (header-comment style of a small src/browser module)
    - test/unit/bridge-registry.test.js (any new __mz name in src/browser must be registered; this module must add none)
    - tools/ident-sweep.mjs (stripJs, for the source pins)
  </read_first>
  <behavior>
    - PROVIDER_METHODS deep-equals ["init", "isAuthenticated", "signIn", "getPlayer"]; createPlayGames() and createFakePlayGames() each return a frozen object whose function-valued keys are exactly those four (the fake adds a calls() inspector and both carry a string kind "native"/"fake")
    - Neither provider has signOut, signout, disconnect or any RESERVED_LEADERBOARD_METHODS key; RESERVED_LEADERBOARD_METHODS deep-equals ["submitScore", "loadTopScores", "loadPlayerCenteredScores", "loadCurrentPlayerScore", "loadFriends"]
    - createPlayGames({ loadPlugin: spy }) calls the spy zero times until a method runs, then exactly once across any number of calls (memoized)
    - init() on a recording fake plugin calls initialize() then signIn({ silent: true }) and resolves { signedIn: true, player: { id: "p1", displayName: "Lanternjaw" } } from { signedIn: true, player: { playerId: "p1", displayName: "Lanternjaw", avatarUrl: "https://x" } }; the resolved player has exactly the keys id and displayName
    - init() when signIn resolves { signedIn: true } without a player calls getPlayer(); when getPlayer rejects it resolves { signedIn: true, player: null }
    - init() when signIn resolves { signedIn: false } resolves { signedIn: false, player: null } and never calls getPlayer
    - init() when the loader rejects, when initialize() rejects, or when signIn rejects resolves { signedIn: false, player: null }; after a loader rejection a later call tries the loader again
    - signIn() calls initialize() (once per provider, memoized across init/signIn/isAuthenticated) and then signIn({ silent: false }) with the silent key present and false
    - isAuthenticated() resolves true/false from isSignedIn(); a rejection resolves false
    - getPlayer() resolves the normalized identity or null when the plugin rejects; whitespace-only display names normalize to ""
    - Across every method, the plugin receives only initialize, signIn, isSignedIn and getPlayer calls (no telemetry method is ever touched)
    - A plugin object built as a Proxy whose get trap throws for the key "then" works through all four methods
    - Fake: default init() resolves signed out; createFakePlayGames({ signedIn: true }).init() resolves FAKE_PLAYER; signIn() with interactive "accept" (default) signs in, with "decline" stays signed out; calls() returns the ordered method names called so far as a frozen array
    - Source pins (comment-stripped via stripJs): exactly one dynamic import in the module, whose specifier is the plugin package; no static import of the plugin; no network identifier (fetch, XMLHttpRequest, WebSocket, EventSource, sendBeacon); no window., document. or __mz reference
  </behavior>
  <action>
Create src/browser/playGames.js (implements D-12's seam, D-02's laziness, D-03's absence of sign-out, D-07's image-free identity and D-11's never-reject posture). Header comment in the house style: Phase 67 (PGS-02, D-12), the one module that names the Play Games plugin; the shell and every other module reach Play Games only through a provider made here; the caller (the account controller) decides whether to call at all (Compete OFF means no call, so the plugin module never loads); both providers share PROVIDER_METHODS; there is no sign-out, because PGS v2 has none (D-03, confirmed by 67-RESEARCH.md); only initialize/signIn/isSignedIn/getPlayer are ever called on the plugin; the identity carries no image URL (D-07).

1. `export const PROVIDER_METHODS` (frozen, the four names) and `export const RESERVED_LEADERBOARD_METHODS` (frozen, the five Phase 68 plugin method names), commented: carried to Phase 68 per D-14 (the 64-char URL-safe score tag), D-18 (no epitaph in the tag) and D-19 (five boards per season, LINEAGE from a DEEPEST sample); Phase 68 binds them behind this seam and this phase exposes none of them.

2. `export const FAKE_PLAYER = Object.freeze({ id: "fake-player", displayName: "Dev Delver" })`.

3. A module-private `toPlayer(info)`: null unless info is an object; id = the trimmed playerId string (else ""), displayName = the trimmed displayName string (else ""); returns a frozen { id, displayName } and copies nothing else.

4. `export function createPlayGames({ loadPlugin } = {})`. The default loader is an arrow function returning the dynamic import of the plugin package by its exact npm name (this is the only place that name appears in src/ or the shell). Private state: a memoized load promise and a memoized initialize promise.
   - Private `load()`: when not memoized, call the loader and map its module namespace to a plain wrapper object holding only the PlayGames property. Never return the PlayGames proxy itself from any async function or pass it to resolve (the thenable trap documented in nativeChrome.js#loadApp). On rejection, clear the memo so a later call can retry, and reject for the caller to catch.
   - Private `ready()`: memoized; loads and then awaits the plugin's initialize(); on failure clears its memo and rejects.
   - `init()`: try ready(), then signIn with an options object whose silent key is true; when signedIn and the result has a player, return { signedIn: true, player: toPlayer(player) }; when signedIn without a player, try getPlayer() once (null on rejection); when not signedIn return { signedIn: false, player: null }; catch everything to { signedIn: false, player: null }.
   - `signIn()`: the same flow with silent explicitly false (the plugin's default is true, per 67-01).
   - `isAuthenticated()`: ready() then isSignedIn(); returns result.signedIn === true; false on any failure.
   - `getPlayer()`: ready() then getPlayer() mapped through toPlayer; null on any failure.
   Return Object.freeze({ kind: "native", init, isAuthenticated, signIn, getPlayer }).

5. `export function createFakePlayGames({ signedIn = false, player = FAKE_PLAYER, interactive = "accept" } = {})`: in-memory state; init() and isAuthenticated() reflect the current state; signIn() signs in when interactive is "accept" and stays out when "decline"; getPlayer() returns the player or null; each call appends its name to a private log; calls() returns a frozen copy. Return Object.freeze({ kind: "fake", init, isAuthenticated, signIn, getPlayer, calls }).

6. The module references no window, document or __mz global and no network API; it adds no bridge.

7. Create test/unit/playGames.test.js covering every behaviour bullet, with a local recording fake plugin factory (options: signIn result, getPlayer result or rejection, initialize rejection) and a Proxy-wrapped variant for the thenable test.
  </action>
  <verify>
    <automated>node --test test/unit/playGames.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function createPlayGames" src/browser/playGames.js` equals 1 and `grep -c "export function createFakePlayGames" src/browser/playGames.js` equals 1
    - `grep -c "RESERVED_LEADERBOARD_METHODS" src/browser/playGames.js` is at least 1
    - `node --test test/unit/playGames.test.js` passes with at least 18 tests, including laziness, the silent-false interactive call, never-reject, no-sign-out, telemetry-free, thenable-proxy and the source pins
    - `node --test test/unit/bridge-registry.test.js test/unit/stale-terms.test.js` passes (no new bridge name, no retired term)
  </acceptance_criteria>
  <done>One provider seam with a lazy native implementation and a fake, proven never to load the plugin unless called, never to reject, and never to reach beyond sign-in and identity.</done>
</task>

<task type="auto">
  <name>Task 3: docs/PLAY-GAMES-SETUP.md, the console runbook, and its content pin</name>
  <files>docs/PLAY-GAMES-SETUP.md, test/unit/play-games-runbook.test.js</files>
  <read_first>
    - .planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md (D-14..D-19)
    - .planning/phases/67-play-games-integration-account-chip/67-RESEARCH.md ("Score-Tag Encoding", "Board → Leaderboard Mapping", "LINEAGE Global Form", "Rate/Quotas and Offline", "Code Examples")
    - docs/RELEASING.md (the house runbook tone, and how the upload keystore and Play App Signing are described today)
    - .planning/STATE.md "Ground Truth" (app identity "Delve, Die, Repeat", appId com.darktierstudios.delvedierepeat, internal-testing track)
  </read_first>
  <action>
Write docs/PLAY-GAMES-SETUP.md (D-16), in the docs/RELEASING.md tone: numbered steps, exact paths, no marketing. Use the shipping name "Delve, Die, Repeat" and the appId com.darktierstudios.delvedierepeat. Never use the old working title anywhere in the doc. Sections:

1. What this is: the Play Console steps only the user can do, which can run in parallel with development. Until they are done, sign-in fails gracefully: the account chip stays the "nobody" glyph, one rail card says Play Games did not answer, and the game is fully playable (D-15).
2. Enable Play Games Services: Play Console → the app → Play Games Services → Setup and management → Configuration; create the PGS project (or link the existing Google Cloud project). Add a note that console labels move over time and the reader should follow the current equivalents.
3. Credentials: add an Android credential for package com.darktierstudios.delvedierepeat using the SHA-1 of the **Play App Signing key** (Play Console → Test and release → Setup → App signing → "App signing key certificate"), not the upload key's SHA-1. Explain why: Play re-signs every installed build with the app signing key, so a credential on the upload key never matches a Play-installed build. Optional testing credential: to try sign-in on a locally built debug APK, add a second credential with the debug keystore's SHA-1 (the keytool command for %USERPROFILE%\.android\debug.keystore, alias androiddebugkey, password android).
4. The APP_ID: where the console shows the numeric application ID, and where it goes: replace the 12-zero placeholder in android/app/src/main/res/values/games-ids.xml (string resource game_services_project_id, created by 67-06 and referenced by the com.google.android.gms.games.APP_ID meta-data in AndroidManifest.xml). Then rebuild with `npm run android:debug` or `npm run play:release`.
5. Testers: while the PGS configuration is unpublished, only accounts on its Testers list can sign in; add the internal-testing track's tester accounts there.
6. What Phase 69 completes: the leaderboards (D-19: five per season, namely DEEPEST, LEANEST, LONGEST, BUTCHERY and PURSE; LINEAGE and GRAVEYARD get no PGS board), each board's sort direction and score format from the research's mapping table, publishing the PGS configuration, and the Data Safety answers. State the 70-leaderboards-per-game cap and that five boards per season lasts 14 seasons.
7. Engineering notes carried to Phase 68 (D-14, D-18, D-19): the score tag is at most 64 characters from the unreserved set A-Z a-z 0-9 - . _ ~ ; the versioned, dot-delimited plain-text layout from 67-RESEARCH.md; the name truncation rule (full name → "First L." → hard-truncated first name); no epitaph in the tag, and global rows expand to a short cause line plus the stat chips (D-18); LINEAGE global is client-side grouping of a top-N DEEPEST fetch by race + class with an honest "sampled" footnote (D-19); submissions resolve only after the server write (0.5.0 uses the *Immediate variants), so Phase 68's durable queue stays required.
8. How to check it worked: with a tester account on a Play-installed build, the chip shows the player's initials and the Leaderboards identity strip shows PLAY GAMES · SIGNED IN.

Then create test/unit/play-games-runbook.test.js, which reads the doc (CRLF-normalized) and asserts it contains: "Play App Signing", "SHA-1", "upload key", "game_services_project_id", "android/app/src/main/res/values/games-ids.xml", "com.darktierstudios.delvedierepeat", "Testers", "Phase 69", "Phase 68", "64", each of DEEPEST, LEANEST, LONGEST, BUTCHERY and PURSE, and "70"; and that it contains no case-insensitive match of the old working title (build that word in the test from two string halves so the test file itself stays clean for other scans).
  </action>
  <verify>
    <automated>node --test test/unit/play-games-runbook.test.js</automated>
  </verify>
  <acceptance_criteria>
    - docs/PLAY-GAMES-SETUP.md exists and `node --test test/unit/play-games-runbook.test.js` passes
    - `grep -c "Play App Signing" docs/PLAY-GAMES-SETUP.md` is at least 1 and `grep -c "game_services_project_id" docs/PLAY-GAMES-SETUP.md` is at least 1
    - `npm test` reports 0 failures, or only failures pre-existing on master before this plan (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The user can complete the Play Console side from one document, and Phase 68 has the tag and LINEAGE decisions in the repo.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step in this plan
</verification>

<success_criteria>
- D-12: a provider seam with native and fake implementations that the shell and tests use interchangeably
- D-01/D-02/D-04: Compete, its default and the first-sign-in flag persist through settings.js
- D-16: the runbook exists; D-14/D-18/D-19 are recorded for Phase 68
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `SETTINGS_DEFAULTS.compete` / `.pgsWelcomed` / `.pgsDevSignedIn` | settings fields | true / false / false, in ddr.settings.v1 |
| src/browser/playGames.js | new module | the only file that names the plugin |
| `PROVIDER_METHODS` | export | ["init", "isAuthenticated", "signIn", "getPlayer"] |
| `RESERVED_LEADERBOARD_METHODS` | export | the Phase 68 plugin methods (carried D-14/D-18/D-19) |
| `FAKE_PLAYER` | export | { id: "fake-player", displayName: "Dev Delver" } |
| `createPlayGames({ loadPlugin })` | export | the native provider, lazy, never rejects |
| `createFakePlayGames({ signedIn, player, interactive })` | export | the in-memory provider for tests and the browser dev loop |
| docs/PLAY-GAMES-SETUP.md | new doc | the D-16 runbook |
| test/unit/playGames.test.js, test/unit/play-games-runbook.test.js | new tests | |

## Flagged assumptions (unresolved edge rows)

- PGS-02 (edge probe: unclassified): "non-blocking" is guaranteed at two levels. The provider never rejects (this plan), and the shell never awaits the launch attempt (67-08). A native attempt that never settles is bounded by the controller's silent-attempt timeout (67-07).

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-02-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: the user walks docs/PLAY-GAMES-SETUP.md end to end in Play Console and confirms each menu path still exists. Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>
