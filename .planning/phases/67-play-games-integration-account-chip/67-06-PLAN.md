---
phase: 67-play-games-integration-account-chip
plan: 06
type: execute
wave: 2
depends_on: ["67-01"]
files_modified:
  - package.json
  - package-lock.json
  - tools/patch-play-games.mjs
  - test/unit/play-games-patch.test.js
  - test/unit/play-games-intake.test.js
  - tools/build-www.mjs
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/res/values/games-ids.xml
  - android/app/capacitor.build.gradle
  - android/capacitor.settings.gradle
autonomous: true
requirements: [PGS-01]
user_setup:
  - service: google-play-games-services
    why: "Real sign-in needs a PGS project, a Play App Signing SHA-1 credential and the APP_ID; until then sign-in fails gracefully (D-15)"
    dashboard_config:
      - task: "Follow docs/PLAY-GAMES-SETUP.md (enable PGS, link the Play App Signing SHA-1, paste the APP_ID into games-ids.xml, add testers)"
        location: "Google Play Console -> Delve, Die, Repeat -> Play Games Services"

must_haves:
  truths:
    - "This plan runs only when 67-01-PLUGIN-REVIEW.md records `Ruling: as-is` or `Ruling: patch`; on `Ruling: stop` it makes no change and reports that the phase needs a replan (D-17)"
    - "package.json pins @modbender/capacitor-play-games at exactly 0.5.0 (no caret or tilde), and package-lock.json's node_modules/@modbender/capacitor-play-games entry carries integrity sha512-GJ7gzDQICQxzPIzKejqYnG9HXnL2/lq5CenHPeDaedkbaYf2hZndj1Xx+F4TtE0PLHp1Da2Ku5W/AMTw5Znb3w== (D-17)"
    - "tools/build-www.mjs vendors and import-maps the plugin like the @capacitor/* plugins, and a test proves every bare-specifier dynamic import in src/browser/*.js is on the vendoring list, so the WebView can resolve playGames.js's import (the stuck-on-splash bug class)"
    - "The manifest carries the com.google.android.gms.games.APP_ID meta-data pointing at @string/game_services_project_id, and games-ids.xml defines that string as a non-translatable 12-zero placeholder until the user's console setup (D-15)"
    - "Patch ruling only: tools/patch-play-games.mjs, run by `npm run cap:sync` before `npx cap sync`, moves PlayGamesSdk.initialize(context) from the plugin's load() into its JS-called initialize() method; it runs only when PlayGamesPlugin.kt's sha256 matches the 67-01-reviewed file or the already-patched file, and otherwise exits non-zero naming D-17 (D-02 holds as written)"
    - "`npm run android:debug` builds a debug APK on the pinned toolchain (AGP 8.13.0 / Gradle 8.14.3 / JDK 21 / compileSdk 36) with the plugin wired in; nothing in android/build.gradle, android/variables.gradle, the Gradle wrapper or gradle.properties' JDK pin is upgraded or left changed (D-13)"
    - "The release runtime classpath contains no ads, analytics, measurement or crash-reporting artifact (firebase, admob, play-services-ads*, play-services-measurement*, crashlytics and similar), audited with Gradle's dependencies task and recorded in the SUMMARY (D-13, PGS-01)"
  prohibitions:
    - statement: "Must not upgrade AGP, Gradle, Kotlin tooling pins, compileSdk/targetSdk or the JDK pin to make the plugin build; an incompatibility stops the plan with a report instead"
      status: unresolved
      verification: judgment
    - statement: "Must not install a device build, deploy, or build a release/AAB in this phase"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "package.json"
      provides: "the exact 0.5.0 dependency (and, patch ruling, the cap:sync patch step)"
      contains: "\"@modbender/capacitor-play-games\": \"0.5.0\""
    - path: "android/app/src/main/res/values/games-ids.xml"
      provides: "the game_services_project_id placeholder"
      contains: "game_services_project_id"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "the PGS APP_ID meta-data"
      contains: "com.google.android.gms.games.APP_ID"
    - path: "test/unit/play-games-intake.test.js"
      provides: "exact-pin, lockfile-integrity, vendoring, dynamic-import coverage and manifest/resource pins"
      min_lines: 80
  key_links:
    - from: "src/browser/playGames.js default loader (67-02)"
      to: "www/vendor/@modbender/capacitor-play-games/index.js"
      via: "the import map build-www.mjs injects into www/index.html"
      pattern: "@modbender/capacitor-play-games"
    - from: "android/app/src/main/AndroidManifest.xml meta-data"
      to: "android/app/src/main/res/values/games-ids.xml game_services_project_id"
      via: "@string reference"
      pattern: "@string/game_services_project_id"
---

<objective>
Install the chosen plugin exactly as the user ruled and wire it into the WebView and the Android build. Implements D-17 (the exact 0.5.0 pin with the lockfile integrity committed, and the ruling's branch), D-13 (compatibility with the pinned AGP 8.13 / Gradle 8.14.3 toolchain proven by a build, and a transitive Gradle audit for ads and analytics), D-15 (the APP_ID string resource placeholder referenced from the manifest) and, under the patch ruling, D-02 as written (the native SDK stays uninitialized until a Compete-ON provider calls initialize()). Completes PGS-01's "wired into the Android build, adding no ads or analytics SDK".

Purpose: the plugin is present, reviewed, pinned, resolvable from the WebView and compiled, with nothing extra riding along.
Output: the pinned dependency, the vendoring entry, the manifest/resource wiring, the cap-sync-generated Gradle wiring, the optional patch tool, tests and a recorded dependency audit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md
@.planning/phases/67-play-games-integration-account-chip/67-01-SUMMARY.md

<interfaces>
- 67-01-PLUGIN-REVIEW.md has one line `Ruling: as-is`, `Ruling: patch` or `Ruling: stop`, and the reviewed sha256 of the plugin's android/src/main/java/com/idleflowgames/playgames/PlayGamesPlugin.kt.
- The plugin's load() (0.5.0): `override fun load() { super.load(); runCatching { PlayGamesSdk.initialize(context) }; ... }`; its JS-called method: `@PluginMethod fun initialize(call: PluginCall) = call.resolve()`.
- The plugin's package.json: module dist/esm/index.js (it imports @capacitor/core by bare specifier and ./definitions and ./web extensionless, which build-www's rewriter already handles); capacitor.android.src "android".
- tools/build-www.mjs: CAPACITOR_PACKAGES (an explicit list), vendorCapacitorPackages() copies each package's ESM dir to www/vendor/<pkg>/ and maps the bare name in the injected import map.
- package.json scripts: cap:sync = "npm run build:www && npx cap sync android"; android:debug = cap:sync, then node tools/pin-jdk.mjs, then node tools/gradle.mjs assembleDebug.
- `npx cap sync android` regenerates the tracked android/app/capacitor.build.gradle and android/capacitor.settings.gradle (adding the plugin's Gradle project) and rewrites android/gradle.properties (tools/pin-jdk.mjs re-adds the JDK line).
- test/unit/shell-map-invariants.test.js "MAP-09" compares package.json's dependency keys with HEAD: commit package.json and package-lock.json BEFORE running the full suite, or that test fails on the uncommitted working tree.
- Worktree facts (executors run in git worktrees on Windows): node_modules is untracked, so a worktree has none until npm installs; android/local.properties (sdk.dir) is untracked and machine-local, so copy it from C:/projects/mazeworld/android/local.properties into the worktree's android/ when missing (it is gitignored; never commit it). JAVA_HOME must point at the pinned JDK 21 (STATE.md Ground Truth).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: exact-pin install, the lockfile integrity pin, and (patch ruling) the guarded build-time patch</name>
  <files>package.json, package-lock.json, tools/patch-play-games.mjs, test/unit/play-games-patch.test.js, test/unit/play-games-intake.test.js</files>
  <read_first>
    - .planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md (the Ruling line, the reviewed sha256, the load()/initialize() citations)
    - package.json (scripts and dependencies)
    - tools/pin-jdk.mjs (the house style of a small idempotent tool that edits one file and fails loudly)
    - test/unit/shell-map-invariants.test.js (MAP-09's HEAD comparison)
  </read_first>
  <behavior>
    - play-games-intake: package.json's dependencies["@modbender/capacitor-play-games"] is exactly "0.5.0"; package-lock.json's packages["node_modules/@modbender/capacitor-play-games"] has version "0.5.0" and the pinned integrity string
    - patch (patch ruling only): patchPlayGamesSource(text) on a minimal Kotlin text holding the load() line and the one-line initialize method returns { changed: true, text } in which load() no longer calls PlayGamesSdk.initialize and initialize() runs it (inside runCatching) before call.resolve(); applying it again returns { changed: false } with identical text; CRLF input keeps CRLF line endings; a text missing either anchor throws an error naming D-17
    - patch CLI: exits 0 and writes nothing when the file's sha256 already equals the patched hash; patches when it equals the reviewed original; exits non-zero with a D-17 message on any other hash; when node_modules holds the plugin, the test asserts the live file is in the patched state
  </behavior>
  <action>
Read the `Ruling:` line in 67-01-PLUGIN-REVIEW.md first.
- `stop`: make no change, write the SUMMARY stating that the phase needs a replan of the plugin intake, and end the plan.
- `as-is` or `patch`: continue.

1. Install per D-17: `npm install --save-exact @modbender/capacitor-play-games@0.5.0` (in a worktree this also creates node_modules). Confirm package.json now reads exactly "0.5.0" and that the lock entry's integrity equals the pinned string. If npm resolved anything else, stop and report. Commit package.json and package-lock.json together before running the full suite (MAP-09).

2. Patch ruling only (D-02 as written, the D-17 amendment the user approved). Create tools/patch-play-games.mjs in the tools/pin-jdk.mjs style. The header states the ruling, D-02/D-17 and the exact edit. It exports `patchPlayGamesSource(text)`, a pure function that removes the `runCatching { PlayGamesSdk.initialize(context) }` statement from load() and replaces the one-line `initialize` @PluginMethod with a block body that runs `runCatching { PlayGamesSdk.initialize(context) }` and then `call.resolve()`. It preserves the file's line endings and throws when either anchor is missing. It also exports `ORIGINAL_SHA256` (the value recorded in 67-01's review) and `PATCHED_SHA256` (computed once from the patched output and pasted in as a literal). The CLI main resolves node_modules/@modbender/capacitor-play-games/android/src/main/java/com/idleflowgames/playgames/PlayGamesPlugin.kt from the repo root, hashes it, no-ops on PATCHED_SHA256, patches on ORIGINAL_SHA256, and otherwise prints a D-17 message ("the plugin changed; review the new version before patching") and exits 1. A missing file also exits 1. Wire it into package.json's cap:sync script as "npm run build:www && node tools/patch-play-games.mjs && npx cap sync android". Create test/unit/play-games-patch.test.js per the behaviour list.

3. As-is ruling only: do not create the patch tool. Record in the SUMMARY that D-02 is amended per the ruling and that the Compete-OFF network capture is a release-blocking device check.

4. Create test/unit/play-games-intake.test.js with the exact-pin and lockfile-integrity tests (Task 2 extends it).
  </action>
  <verify>
    <automated>node --test test/unit/play-games-intake.test.js test/unit/play-games-patch.test.js test/unit/shell-map-invariants.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"@modbender/capacitor-play-games": "0.5.0"' package.json` equals 1
    - `grep -c "sha512-GJ7gzDQICQxzPIzKejqYnG9HXnL2/lq5CenHPeDaedkbaYf2hZndj1Xx+F4TtE0PLHp1Da2Ku5W/AMTw5Znb3w==" package-lock.json` is at least 1
    - Patch ruling: `grep -c "patch-play-games.mjs" package.json` equals 1 and `node --test test/unit/play-games-patch.test.js` passes; as-is ruling: tools/patch-play-games.mjs does not exist and the SUMMARY records the amended D-02
    - `node --test test/unit/play-games-intake.test.js test/unit/shell-map-invariants.test.js` passes after package.json and package-lock.json are committed
  </acceptance_criteria>
  <done>The plugin is installed at exactly the reviewed version with its integrity committed, and, under the patch ruling, the ambient SDK init is deferred by a guarded, idempotent build step.</done>
</task>

<task type="auto">
  <name>Task 2: WebView vendoring, APP_ID resource and manifest, cap sync, debug build and the Gradle dependency audit</name>
  <files>tools/build-www.mjs, android/app/src/main/AndroidManifest.xml, android/app/src/main/res/values/games-ids.xml, android/app/capacitor.build.gradle, android/capacitor.settings.gradle, test/unit/play-games-intake.test.js</files>
  <read_first>
    - tools/build-www.mjs (CAPACITOR_PACKAGES and its comment block, vendorCapacitorPackages, the step() log wording)
    - tools/ident-sweep.mjs (stripJs, for the dynamic-import scan)
    - android/app/src/main/AndroidManifest.xml and android/app/src/main/res/values/strings.xml
    - android/app/build.gradle (the conditional google-services block, if any; release minify is off)
    - docs/RELEASING.md and .planning/STATE.md "Build/env" (npm run android:debug, pin-jdk, gradle.mjs)
  </read_first>
  <action>
1. tools/build-www.mjs: append "@modbender/capacitor-play-games" to CAPACITOR_PACKAGES with a comment: Phase 67 (D-12/D-17), vendored and import-mapped so src/browser/playGames.js's guarded dynamic import resolves in the Android WebView; reached only on native with Compete ON; the exact 0.5.0 pin was reviewed in 67-01. Reword the vendoring step log from "@capacitor/* packages" to "native plugin packages". No other behaviour change.

2. D-15: create android/app/src/main/res/values/games-ids.xml as a `<resources>` file defining `<string name="game_services_project_id" translatable="false">000000000000</string>`, with an XML comment: the Play Games APP_ID placeholder; replace it per docs/PLAY-GAMES-SETUP.md; with the placeholder, sign-in fails gracefully (nobody chip, one rail card, fully playable). In AndroidManifest.xml, inside `<application>` after the FileProvider, add `<meta-data android:name="com.google.android.gms.games.APP_ID" android:value="@string/game_services_project_id" />` with a one-line comment pointing at the runbook. Add no permissions.

3. Extend test/unit/play-games-intake.test.js. Assert that build-www's CAPACITOR_PACKAGES literal includes the plugin. Scan every src/browser/*.js file (comment-stripped with stripJs) for dynamic `import("…")` calls whose specifier is bare (not starting with "." or "/") and assert each is on that list, so a missing vendoring fails a node test instead of sticking the device on the splash. Assert that the manifest has the APP_ID meta-data with that exact @string value, that games-ids.xml defines game_services_project_id with translatable="false" and a 12-digit numeric placeholder, and that capacitor.build.gradle and capacitor.settings.gradle include the plugin's Gradle project (after step 4 regenerates them).

4. Build (D-13). In a worktree, copy android/local.properties from C:/projects/mazeworld/android/ when missing (never commit it). Run `npm run android:debug`. Under the patch ruling this runs the patch tool, so confirm its output reports "patched" or "already patched". The build must succeed on the pinned toolchain. If it fails for a plugin/toolchain incompatibility (for example the plugin's Kotlin Gradle Plugin 2.4.10 or its AGP 9.3.1 buildscript classpath against Gradle 8.14.3 + AGP 8.13.0), do NOT upgrade anything: stop, record the error excerpt and the plugin's buildscript lines in the SUMMARY, and return a checkpoint for the user. After a successful build, `git diff android/gradle.properties android/build.gradle android/variables.gradle android/gradle/wrapper` must be empty; restore gradle.properties with git checkout if cap sync reordered it. Commit the regenerated capacitor.build.gradle and capacitor.settings.gradle.

5. Audit (D-13, PGS-01). Run `node tools/gradle.mjs :app:dependencies --configuration releaseRuntimeClasspath` with output redirected to a temp file outside the repo, and search it case-insensitively for firebase, admob, play-services-ads, ads-identifier, analytics, measurement, crashlytics, appsflyer, adjust, facebook, appcenter, sentry and bugsnag. Record in the SUMMARY: the command, every com.google.android.gms and com.google.firebase artifact with its resolved version, the Kotlin stdlib version pulled in, and the search result. Also record that the root buildscript's google-services classpath is never applied (no google-services.json, no applied plugin). Any hit means stop: record the hit in the SUMMARY and return a checkpoint for the user without running later plans (D-13 forbids ads/analytics). Zero hits is the expected result.

6. Run `npm test` (package.json and the lock are already committed).
  </action>
  <verify>
    <automated>node --test test/unit/play-games-intake.test.js test/unit/sfx-assets.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@modbender/capacitor-play-games" tools/build-www.mjs` is at least 1; `grep -c "com.google.android.gms.games.APP_ID" android/app/src/main/AndroidManifest.xml` equals 1; `grep -c "game_services_project_id" android/app/src/main/res/values/games-ids.xml` equals 1
    - `grep -ci "play-games" android/capacitor.settings.gradle` is at least 1
    - `npm run android:debug` exited 0 (its tail is recorded in the SUMMARY), and `git diff --quiet HEAD -- android/gradle.properties android/build.gradle android/variables.gradle android/gradle/wrapper` exits 0 afterwards
    - The SUMMARY's dependency audit lists the command, the Google artifacts with versions, and zero forbidden matches
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The plugin resolves in the WebView, the APP_ID placeholder is wired, the debug APK builds on the pinned toolchain, and the audit proves no ads or analytics SDK came along.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | a third-party native plugin is installed |
| Gradle repositories → release runtime classpath | transitive Android artifacts enter the shipped APK/AAB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-SC | Tampering | npm install of the plugin | high | mitigate | exact 0.5.0 pin, lockfile integrity pinned by test, the 67-01 review and blocking-human ruling; the patch tool refuses any file whose sha256 is not the reviewed one |
| T-67-02 | Information disclosure | transitive Gradle dependencies | high | mitigate | releaseRuntimeClasspath audit for ads/analytics/measurement/crash SDKs; any hit stops the plan |
| T-67-03 | Denial of service | the WebView module resolution | medium | mitigate | the vendoring list plus the dynamic-import coverage test prevent a stuck-on-splash boot |
</threat_model>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- `npm run android:debug` succeeds on the pinned toolchain; no device install, no release/AAB build
- The Gradle audit is recorded with zero forbidden artifacts
</verification>

<success_criteria>
- PGS-01: the chosen plugin is wired into the Android build and adds no ads or analytics SDK
- D-17: exact pin, integrity committed, ruling honoured
- D-15: the APP_ID placeholder is wired and documented
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `"@modbender/capacitor-play-games": "0.5.0"` | dependency | exact pin; lock integrity sha512-GJ7g… |
| tools/patch-play-games.mjs (`patchPlayGamesSource`, `ORIGINAL_SHA256`, `PATCHED_SHA256`) | new tool | patch ruling only; run by `npm run cap:sync` |
| CAPACITOR_PACKAGES entry | build-www change | vendored to www/vendor/@modbender/capacitor-play-games/ |
| android/app/src/main/res/values/games-ids.xml `game_services_project_id` | new resource | 000000000000 placeholder (D-15) |
| AndroidManifest.xml APP_ID meta-data | manifest | `@string/game_services_project_id` |
| capacitor.build.gradle / capacitor.settings.gradle | regenerated by cap sync | the plugin's Gradle project |
| test/unit/play-games-intake.test.js, test/unit/play-games-patch.test.js | new tests | |

## Flagged assumptions (unresolved edge rows)

- PGS-01 (edge probe: unclassified): "adds no ads or analytics SDK" is proven for the release runtime classpath at 0.5.0 / play-services-games-v2 22.0.0. A future plugin or SDK bump re-runs this audit (D-17: an upgrade is a deliberate, reviewed change).

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-06-SUMMARY.md` when done, with the ruling followed, the build tail, and the dependency audit, plus a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: (1) with the placeholder APP_ID, a cold boot with Compete ON shows the nobody chip and one failure rail card, and the game plays normally with no crash (D-15); (2) with Compete OFF, a cold boot shows no Play Games Logcat lines and no Play Games network traffic (release-blocking under the as-is ruling); (3) after the user's console setup, a tester account signs in. Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>
