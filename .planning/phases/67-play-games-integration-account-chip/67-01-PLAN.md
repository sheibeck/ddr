---
phase: 67-play-games-integration-account-chip
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md
autonomous: false
requirements: [PGS-01]
user_setup: []

must_haves:
  truths:
    - "The published @modbender/capacitor-play-games@0.5.0 tarball is fetched with npm pack into a directory OUTSIDE the repo and reviewed before anything is installed; this plan changes no package.json, package-lock.json, node_modules or android/ file (D-17)"
    - "The tarball's integrity equals the registry's sha512-GJ7gzDQICQxzPIzKejqYnG9HXnL2/lq5CenHPeDaedkbaYf2hZndj1Xx+F4TtE0PLHp1Da2Ku5W/AMTw5Znb3w== and the review records the sha256 of android/src/main/java/com/idleflowgames/playgames/PlayGamesPlugin.kt (the file a patch ruling would touch)"
    - "67-01-PLUGIN-REVIEW.md records every D-17 review item: native entry points (every @PluginMethod), Gradle dependencies and the buildscript classpath against the pinned toolchain, any network, analytics or ads code, whether the plugin initializes the PGS SDK at app start, the sign-in option default, the APP_ID resource convention and the install-lifecycle scripts"
    - "The review states the blocker plainly: PlayGamesPlugin.load() runs PlayGamesSdk.initialize(context) at every app launch, before any JS runs, so the JS-side Compete gate cannot keep the SDK uninitialized when Compete is OFF (D-02 conflict); research Assumption A1 is falsified"
    - "The plan stops at a blocking human checkpoint that also serves as the [SUS]-package legitimacy check, and records the user's ruling (as-is, patch or stop) under '## Ruling' before any later plan installs the plugin (D-17, D-13)"
  prohibitions:
    - statement: "Must not install, vendor, copy into the repo, or wire the plugin into the Android build during the review; the review is read-only against a temp directory"
      status: unresolved
      verification: judgment
  artifacts:
    - path: ".planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md"
      provides: "the D-17 tarball review notes, the blocker, and the user's ruling"
      contains: "## Ruling"
  key_links:
    - from: "67-01-PLUGIN-REVIEW.md ## Ruling"
      to: "67-06-PLAN.md (plugin install and Android wiring)"
      via: "67-06 reads the ruling and runs only its matching branch (as-is or patch); a stop ruling halts the phase for a replan"
      pattern: "## Ruling"
---

<objective>
Review the published plugin tarball before install and get the user's intake ruling. Implements D-17 (exact-version intake, one review of the published tarball before install, review notes recorded, a blocker stops at a checkpoint rather than proceeding), D-13 (the plugin choice is re-checked against the pinned toolchain and for ads/analytics code) and the D-02 conflict check (does the plugin initialize the SDK where Compete cannot stop it). Addresses PGS-01's "chosen and adds no ads or analytics SDK" half; 67-06 does the wiring.

The planner already opened this tarball read-only while planning and found a D-17 blocker: `PlayGamesPlugin.load()` calls `PlayGamesSdk.initialize(context)` at plugin registration (every launch), and the plugin README confirms it ("initialize() — No-op. PlayGamesSdk.initialize runs automatically when the plugin loads"). The executor confirms every pre-read fact against the actual tarball, records the review, and stops at the blocking checkpoint. The same checkpoint discharges the package-legitimacy gate (67-RESEARCH.md rates the package [SUS]: 11 days old, 41 downloads a week).

Purpose: nothing lands in package.json until the user has seen what the plugin does at app start.
Output: 67-01-PLUGIN-REVIEW.md with the review and the ruling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-RESEARCH.md

<interfaces>
Planner pre-read of the 0.5.0 tarball (read-only, 2026-09-23). Confirm each fact; record any mismatch as a finding.

- package.json: name @modbender/capacitor-play-games, version 0.5.0, MIT, module dist/esm/index.js, main dist/plugin.cjs.js, types dist/esm/index.d.ts, peerDependencies { "@capacitor/core": "^8.0.0" }, no dependencies, capacitor.android.src "android". Scripts are build/clean/docgen/verify/typecheck/check-docs/prepublishOnly only (no preinstall/install/postinstall).
- Registry integrity: sha512-GJ7gzDQICQxzPIzKejqYnG9HXnL2/lq5CenHPeDaedkbaYf2hZndj1Xx+F4TtE0PLHp1Da2Ku5W/AMTw5Znb3w==
- dist/esm/index.js: registerPlugin("PlayGames", { web: lazy import of ./web }); exports PlayGames and the definitions.
- dist/esm/definitions.d.ts: initialize(): Promise<void>; signIn(opts?: { silent?: boolean }): Promise<SignInResult>, where silent DEFAULTS TO TRUE (the research said a bare call is interactive; that is wrong); SignInResult { signedIn: boolean; player?: PlayerInfo }; isSignedIn(): Promise<{ signedIn: boolean }>; getPlayer(): Promise<PlayerInfo>, rejecting when signed out; PlayerInfo { playerId, displayName, avatarUrl?, hi-res and banner URLs, title, level/XP, friend status }; addListener("signInStateChanged"). Phase 68 surface: submitScore({ leaderboardId, score, scoreTag? }) (resolves after the server write), loadTopScores, loadPlayerCenteredScores, loadCurrentPlayerScore, loadFriends({ pageSize?, forceReload?, resolve? }) with { friends, resolutionRequired }.
- android/src/main/java/com/idleflowgames/playgames/PlayGamesPlugin.kt: `@CapacitorPlugin(name = "PlayGames")`; `override fun load()` runs `runCatching { PlayGamesSdk.initialize(context) }` and registers an IntentSender launcher; `@PluginMethod fun initialize(call: PluginCall) = call.resolve()`. Telemetry-shaped surfaces exist but run only when called: recordGameEvent, recordGameEvents, recordProgressUpdate, requestGameEventsUpload, incrementEvent, loadEvents, loadEventsByIds, requestRecallAccess, requestServerSideAccess, loadPlayerStats.
- SignInModule.kt: silent sign-in uses isAuthenticated; interactive sign-in pre-flights GoogleApiAvailability, then signIn(); every failure resolves { signedIn: false } (no rejection).
- android/build.gradle: a buildscript classpath of com.android.tools.build:gradle:9.3.1 and org.jetbrains.kotlin:kotlin-gradle-plugin:2.4.10; applies com.android.library and org.jetbrains.kotlin.android; JVM target 21; compileSdk/minSdk/targetSdk from rootProject ext (36/24/36); dependencies play-services-games-v2:22.0.0, androidx.appcompat, project(':capacitor-android'). The research's "matches AGP 8.13" claim is contradicted by this classpath.
- android/src/main/AndroidManifest.xml: empty manifest (no permissions). consumer-rules.pro keeps com.idleflowgames.playgames.**.
- README: the APP_ID meta-data is com.google.android.gms.games.APP_ID pointing at @string/game_services_project_id.

The project's pinned toolchain (STATE.md Ground Truth; android/build.gradle, android/variables.gradle): AGP 8.13.0, Gradle 8.14.3, JDK 21, compileSdk/targetSdk 36, minSdk 24. Do not upgrade any of them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review the published 0.5.0 tarball read-only and write 67-01-PLUGIN-REVIEW.md</name>
  <files>.planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md</files>
  <read_first>
    - .planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md (D-02, D-12, D-13, D-15, D-17)
    - .planning/phases/67-play-games-integration-account-chip/67-RESEARCH.md ("Package Legitimacy Audit", "Sign-in Mechanics", "Assumptions Log" A1 and A5, "Code Examples")
    - src/browser/nativeChrome.js (loadApp's comment on why a Capacitor plugin proxy must never be returned from an async function)
    - tools/build-www.mjs (vendorCapacitorPackages: how a plugin's ESM entry is vendored and import-mapped)
    - android/build.gradle and android/variables.gradle (the pinned AGP, SDK levels and the root buildscript classpath)
  </read_first>
  <action>
Per D-17 and D-13, review the published tarball read-only. Nothing in this task touches package.json, package-lock.json, node_modules or android/.

1. Make a temp directory under the OS temp dir (never inside the repo). Run `npm view @modbender/capacitor-play-games@0.5.0 dist.integrity dist.tarball time.modified license` and `npm pack @modbender/capacitor-play-games@0.5.0 --pack-destination <tmp>`, then extract the .tgz there with tar. Compute the sha512 of the .tgz as base64 and compare it with the registry integrity in the interfaces block. Compute the sha256 (hex) of `package/android/src/main/java/com/idleflowgames/playgames/PlayGamesPlugin.kt` and record it (67-06's patch branch guards on it).

2. Read, in the extracted package: package.json (scripts, dependencies, peerDependencies, module/main/types, capacitor field), dist/esm/index.js, dist/esm/definitions.d.ts, dist/esm/web.js, android/build.gradle, android/src/main/AndroidManifest.xml, consumer-rules.pro, every Kotlin file under android/src/main/java, README.md and CHANGELOG.md. In the Kotlin sources, search for: code that runs in load(), init blocks and companion objects; any HTTP/URL/socket use; any firebase, analytics, measurement, crashlytics or ads reference; and the telemetry-shaped client surfaces (game stats, events, recall, server-side access, player stats).

3. Write 67-01-PLUGIN-REVIEW.md with these sections, each a short table or list with file:line citations into the extracted package:
   - Identity and integrity: name, version, license, publish time, registry integrity, computed integrity (match yes/no), the PlayGamesPlugin.kt sha256, the tarball file list.
   - Install surface: lifecycle scripts (none expected), runtime dependencies (none expected), peer dependency.
   - JS surface: the registered plugin name, the web fallback, and the exact method signatures this phase uses (initialize, signIn with the silent default, isSignedIn, getPlayer and the PlayerInfo fields), plus the Phase 68 methods by name (submitScore with scoreTag, loadTopScores, loadPlayerCenteredScores, loadCurrentPlayerScore, loadFriends).
   - Native surface: every @PluginMethod, grouped by module; which are telemetry-shaped (this project never calls them).
   - Gradle: the buildscript classpath (AGP 9.3.1 and Kotlin Gradle Plugin 2.4.10), plugins applied, SDK levels, JVM target, and the runtime dependencies, each compared with the pinned toolchain; state that the root project's AGP 8.13.0 is already on the parent classloader, and that only 67-06's debug build proves the Kotlin plugin works on Gradle 8.14.3 + AGP 8.13.0 (no toolchain upgrade is allowed).
   - Network, analytics and ads scan: what was found (expected: no HTTP code of its own, no ads/analytics libraries; play-services-games-v2 is the only Google dependency, audited transitively in 67-06).
   - Ambient initialization (THE BLOCKER): quote the load() line and the README's initialize() note; explain that Capacitor instantiates every registered plugin and calls load() while the bridge starts, before any JS runs, so the SDK initializes at every launch even with Compete OFF; state that research Assumption A1 is falsified and that D-02 ("the PGS SDK is never initialized at launch") cannot hold with the tarball as published.
   - Research corrections: signIn's silent option defaults to true (the Sign in row must pass silent false explicitly); the APP_ID resource is named game_services_project_id; the plugin's own Gradle classpath is AGP 9.3.1, not 8.13.
   - Options for the ruling (verbatim from Task 2's checkpoint) and an empty "## Ruling" heading.
   Write the file in plain prose and tables. Keep product naming as "Delve, Die, Repeat"; never use the old working title.

4. Delete the temp directory. Commit the review file alone.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md','utf8');for(const k of ['## Ruling','PlayGamesSdk.initialize','game_services_project_id','9.3.1','2.4.10','sha512-GJ7gzDQICQxzPIzKejqYnG9HXnL2','silent'])if(!t.includes(k)){console.error('missing '+k);process.exit(1)}" && git diff --quiet HEAD -- package.json package-lock.json android</automated>
  </verify>
  <acceptance_criteria>
    - 67-01-PLUGIN-REVIEW.md exists and contains "## Ruling", "PlayGamesSdk.initialize", "game_services_project_id", "9.3.1", "2.4.10", the registry integrity string, and a recorded sha256 for PlayGamesPlugin.kt (a 64-hex-digit value)
    - `git diff --quiet HEAD -- package.json package-lock.json android` exits 0 (nothing installed or wired)
    - No file under the repo root was created by npm pack (the tarball lives only in the temp directory, which is deleted)
  </acceptance_criteria>
  <done>The review is committed, every D-17 item is recorded with citations, and the ambient-initialization blocker is stated with its evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Blocking intake ruling (package legitimacy + the ambient SDK initialization blocker)</name>
  <files>.planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md (read-only here)</files>
  <action>Stop and present this checkpoint to the user, per D-17 (a review blocker stops at a checkpoint rather than proceeding) and the package-legitimacy gate (the research rates the package [SUS]). Quote the review's "Ambient initialization" and "Gradle" sections and the three options below. Install nothing, edit no file, and do not continue to Task 3 until the user answers. This checkpoint is never auto-approved, whatever the workflow's auto-advance setting.</action>
  <verify>
    <human-check>The user has answered with one of as-is, patch or stop after checking the npm and GitHub pages.</human-check>
  </verify>
  <done>The user's ruling keyword (and any notes) is in hand for Task 3.</done>
  <what-built>A read-only review of @modbender/capacitor-play-games@0.5.0 (67-01-PLUGIN-REVIEW.md). The package is flagged [SUS] by the research (11 days old, 41 downloads a week), and the review found a D-17 blocker: the plugin's native load() runs PlayGamesSdk.initialize(context) on every app launch, before any JavaScript runs, so turning Compete OFF cannot keep the Play Games SDK uninitialized (D-02 says it must never be initialized at launch with Compete OFF). The plugin's own Gradle file also declares AGP 9.3.1 and Kotlin Gradle Plugin 2.4.10 on its buildscript classpath (the project stays on AGP 8.13.0 / Gradle 8.14.3; 67-06's debug build proves or disproves compatibility, and stops rather than upgrading).</what-built>
  <how-to-verify>
    1. Legitimacy: open https://www.npmjs.com/package/@modbender/capacitor-play-games and https://github.com/modbender/capacitor-play-games. Confirm it is a genuine Play Games Services v2 wrapper (a fork of @idleflowgames/capacitor-play-games), with a real commit history and an MIT license.
    2. Read the "Ambient initialization" and "Gradle" sections of .planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md.
    3. Choose one ruling:
       - as-is: install 0.5.0 unmodified. D-02 is amended to "the JS layer never calls sign-in, isSignedIn or getPlayer while Compete is OFF". The native SDK still initializes at launch. The Compete-OFF network capture on the Pixel 7 becomes a release-blocking device check in the Phase 69 batch.
       - patch (recommended): install 0.5.0 with the exact pin. 67-06 adds tools/patch-play-games.mjs, run by `npm run cap:sync` before `npx cap sync`. It moves the PlayGamesSdk.initialize(context) call out of load() and into the JS-called initialize() method, and runs only when PlayGamesPlugin.kt's sha256 matches the reviewed 0.5.0 file (any other bytes fail the build loudly). D-02 then holds as written: the SDK stays untouched until a Compete-ON provider calls initialize(). The package is still installed from npm and not vendored. Only a guarded two-line build-time patch is added, and D-17 is amended to allow it.
       - stop: do not install. The phase halts for a replan (for example a vendored fork, the research's fallback).
  </how-to-verify>
  <resume-signal>Type "as-is", "patch" or "stop" (optionally followed by notes). If the legitimacy check fails, type "stop".</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the ruling in the review file</name>
  <files>.planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md</files>
  <read_first>
    - .planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md (Task 1's review)
  </read_first>
  <action>
Under "## Ruling" in 67-01-PLUGIN-REVIEW.md, write: the ruling keyword exactly as given (as-is, patch or stop) on a line of its own in the form `Ruling: <keyword>`, the date, the user's notes verbatim, the legitimacy confirmation, and which D-decision the ruling amends (as-is amends D-02's wording; patch amends D-17's "not vendored" with the guarded build-time patch; stop halts the phase). Add one line naming what 67-06 must do next for that ruling. Commit.

Per D-17, if the ruling is stop, also state in the plan SUMMARY that 67-06 must not run and that the orchestrator must replan the intake before 67-06/67-08 execute.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('.planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md','utf8');if(!/^Ruling: (as-is|patch|stop)\s*$/m.test(t)){console.error('no ruling line');process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - 67-01-PLUGIN-REVIEW.md has exactly one line matching `Ruling: as-is`, `Ruling: patch` or `Ruling: stop`
    - The SUMMARY copies the review's findings table (integrity, blocker, Gradle classpath, telemetry surfaces, research corrections) and the ruling, as D-17 requires
  </acceptance_criteria>
  <done>The user's ruling is recorded in a machine-readable line that 67-06 branches on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | a third-party native plugin enters the build |
| plugin native code → Android app process | the plugin's load() runs at every launch with the app's privileges |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-67-SC | Tampering | npm install of @modbender/capacitor-play-games | high | mitigate | read-only tarball review before install, integrity compared with the registry, blocking-human legitimacy checkpoint, exact pin plus lockfile integrity in 67-06 |
| T-67-01 | Information disclosure | PlayGamesPlugin.load() ambient SDK init | high | mitigate | surfaced to the user as a blocker; the patch ruling defers the init to a Compete-ON JS call |
</threat_model>

<verification>
- The review file exists with every D-17 item and a machine-readable ruling line
- package.json, package-lock.json and android/ are untouched by this plan
- No Android build, APK or device step
</verification>

<success_criteria>
- D-17: the published tarball was reviewed once before install, the notes are recorded, and the blocker stopped at a checkpoint
- D-13: the Gradle surface and the ads/analytics question are answered from the source, not the README
- The user's ruling gates 67-06
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| .planning/phases/67-play-games-integration-account-chip/67-01-PLUGIN-REVIEW.md | review record | D-17 notes, the blocker, `Ruling: <keyword>` |
| PlayGamesPlugin.kt sha256 | recorded value | the patch branch's guard in 67-06 |

## Flagged assumptions (unresolved edge rows)

- PGS-01 (edge probe: unclassified): the "adds no ads or analytics SDK" half is answered here from the plugin's own sources; the transitive Gradle audit is 67-06's, after the ruling.

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-01-SUMMARY.md` when done, including the review's findings table and the ruling (D-17 requires the notes in the SUMMARY), and a "Human verification (deferred to end of run)" heading that lists, for the Phase 69 batch: with Compete OFF on a cold boot, Logcat and a network capture show no Play Games activity (release-blocking if the ruling was as-is). Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>

## Must haves: post-review rulings (67-CONTEXT D-20, D-21)

- D-20 resolves this plan's checkpoint: the user chose **as-is** (install 0.5.0 unmodified; D-02 amended — Compete OFF never calls sign-in/submit/fetch; the SDK still initializes at launch). Record that as the checkpoint outcome in the SUMMARY and do not stop to ask again. The Sign in row passes `silent: false`.
- D-21: the AGP path is decided by the orchestrator's AGP 9.3.1 spike (`67-AGP9-SPIKE.md`, VERDICT line) before 67-06 runs.
