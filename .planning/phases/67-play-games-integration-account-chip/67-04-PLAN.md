---
phase: 67-play-games-integration-account-chip
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - content/boards.js
  - test/unit/boards-copy.test.js
  - src/browser/boardsView.js
  - test/unit/boardsView.test.js
  - src/browser/boardsPanel.js
  - test/unit/boardsPanel-dom.test.js
  - test/unit/boardsPanel.test.js
autonomous: true
requirements: [ACCT-01]
user_setup: []

must_haves:
  truths:
    - "Signed in (Compete ON), the Leaderboards identity strip shows the Play Games display name, its initials avatar (the mock's AVATAR/INITIALS over the display name, with the gold 'on' ring) and PLAY GAMES · SIGNED IN (D-08)"
    - "Signed out or Compete OFF, the Phase 66 strip is unchanged: the nobody glyph, PLAY GAMES · SIGNED OUT and 'Your dead only' (D-08)"
    - "Signed in, the ALL and FRIENDS chips are no longer dimmed, and tapping either replaces the rows with an in-panel note in voice saying the global boards are coming online; the rows themselves stay local until Phase 68, and nothing claims a global or friends ranking (D-08)"
    - "createBoardsPanel takes an injected identity() seam: its answer reaches buildView as signedIn and player on every render; a missing or throwing identity() renders signed out; refresh() re-reads it (the Phase 66 hardcoded signedIn:false is gone)"
    - "The renderer draws the signed-in avatar as a .mw-bd-av with the initials, an inline background and the ring, and data-on '1', with no new class name (BOARDS_CLASSES is unchanged); a strip without an avatar still draws the nobody glyph"
    - "GRAVEYARD still has no strip in either state, and the signed-in change adds no network call or network-capable reference to the view model or the panel"
  prohibitions:
    - statement: "Must not present the local rows as global or friends rankings while signed in; the 'coming online' note replaces the rows rather than relabelling them"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "content/boards.js"
      provides: "BOARDS_PANEL_COPY.strip.live and BOARDS_PANEL_COPY.note.live"
      contains: "live"
    - path: "src/browser/boardsView.js"
      provides: "boardsView's player input and the signed-in strip/notes"
      contains: "player"
    - path: "src/browser/boardsPanel.js"
      provides: "the avatar strip branch and createBoardsPanel's identity seam"
      contains: "identity"
  key_links:
    - from: "src/browser/boardsPanel.js#createBoardsPanel render()"
      to: "src/browser/boardsView.js#boardsView"
      via: "buildView({ ...data, board, scope, open, entry, hasHero, signedIn, player }) with signedIn/player from identity()"
      pattern: "identity\\("
    - from: "src/browser/boardsView.js strip (signed in)"
      to: "initialsOf / avatarColour"
      via: "the avatar is computed from the display name"
      pattern: "avatarColour\\("
---

<objective>
Bring the Phase 66 identity strip to life behind the same seam. Implements D-08: signed in, the strip shows the display name, the initials avatar and PLAY GAMES · SIGNED IN; the Phase 66 signed-out strip remains for signed out or Compete OFF; the ALL/FRIENDS chips' tap note says the global boards are coming online, in voice, until Phase 68's rows arrive. The shell hands the account identity in through a new injected identity() seam (67-08 wires it).

Purpose: the panel reflects a real sign-in the moment one exists, with no change to its local rows.
Output: content/boards.js live copy, boardsView's signed-in branch, boardsPanel's avatar branch and identity seam, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md

<interfaces>
Phase 66 (merged before this phase executes; read the live files, which are authoritative over this summary):
- content/boards.js BOARDS_PANEL_COPY.strip = { glyph: "?", label: "PLAY GAMES · SIGNED OUT", source: "Your dead only" }; BOARDS_PANEL_COPY.note = { all, friends } (the signed-out notes); chips { all: "ALL", friends: "FRIENDS" }.
- src/browser/boardsView.js: `boardsView({ bests, graves, total, board, scope, open, entry, hasHero, signedIn, recentHash })`; strip null on GRAVEYARD, else { glyph, label, source, scopes: [{ id, label, on, dim }] } with dim = signedIn !== true; a ranked board with scope "all"/"friends" gives body { kind: "note", line: note[scope] }; exports initialsOf, avatarColour, AVATAR_PALETTE.
- src/browser/boardsPanel.js: buildStrip draws `div.mw-bd-av mw-bd-av-nobody` with strip.glyph, then the label/source text and the scope chips; `createBoardsPanel({ host, buildView, readData, prefs, reducedMotion, onRoute })`; render() calls buildView({ ...data, board, scope, open, entry, hasHero, signedIn: false }); refresh() re-renders only while open; BOARDS_CLASSES lists every emitted class.
- The mock (design/Mazeworld Boards Panel.dc.html ~36-39, ~205-214, ~297): the signed-in strip is the AVATAR(handle, 30, true) square with initials, the handle, and the source "PLAY GAMES · SIGNED IN"; AVATAR's on-ring is box-shadow "inset 0 0 0 1px rgba(0,0,0,.5), 0 0 0 1px #e8c97a".

The account identity (67-03's accountIdentity; this plan only needs its shape): { signedIn: boolean, player: { id, displayName } | null }.

Strip view contract after this plan (both states):
- signed out: { glyph: "?", avatar: null, label: "PLAY GAMES · SIGNED OUT", source: "Your dead only", scopes (dim true) }
- signed in: { glyph: "", avatar: { initials, bg }, label: <display name or strip.live.unnamed>, source: "PLAY GAMES · SIGNED IN", scopes (dim false) }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the live copy and boardsView's signed-in strip and notes</name>
  <files>content/boards.js, test/unit/boards-copy.test.js, src/browser/boardsView.js, test/unit/boardsView.test.js</files>
  <read_first>
    - content/boards.js (BOARDS_PANEL_COPY: strip, note, chips)
    - test/unit/boards-copy.test.js (the verbatim copy pins)
    - src/browser/boardsView.js (input normalization, the strip and body builders, the purity pins)
    - test/unit/boardsView.test.js (the signed-out strip tests and the offline "no worldwide / among friends / @" pin)
    - design/Mazeworld Boards Panel.dc.html (the strip markup ~33-47 and the me block ~292-297)
  </read_first>
  <behavior>
    - BOARDS_PANEL_COPY.strip.live is { source: "PLAY GAMES · SIGNED IN", unnamed: <a deadpan fallback name> } and BOARDS_PANEL_COPY.note.live is { all, friends }, each a single in-voice line saying the global boards are not open yet (e.g. all: "The world's ledger is still being bound. Your own dead will have to do."); both walked by the existing safety and HP scans with no registration change
    - boardsView({ ..., signedIn: true, player: { id: "p1", displayName: "Lanternjaw" } }) on DEEPEST gives strip { glyph: "", avatar: { initials: initialsOf("Lanternjaw"), bg: avatarColour("Lanternjaw") }, label: "Lanternjaw", source: "PLAY GAMES · SIGNED IN" } with both scope chips dim false
    - Signed in with player null or an empty display name: label is strip.live.unnamed and the avatar is computed from that fallback
    - Signed out (signedIn false, or signedIn missing, even with a player passed) gives the unchanged Phase 66 strip plus avatar: null
    - Signed in on a ranked board with scope "all" gives body { kind: "note", line: note.live.all }; "friends" gives note.live.friends; scope "local" still lists the local rows
    - GRAVEYARD: strip null and scope ignored in both states
    - Signed in: the header scope line, rows, standing card and footnote are identical to the signed-out view for the same data (only the strip and the notes change); no string in the signed-in view contains "worldwide" or "among friends"
    - Purity and determinism still hold with the player input (deep-frozen player never mutated; two calls deepStrictEqual)
  </behavior>
  <action>
Per D-08:

1. content/boards.js: add `live` to BOARDS_PANEL_COPY.strip ({ source: "PLAY GAMES · SIGNED IN", unnamed: a deadpan fallback such as "A player with no name" }) and to BOARDS_PANEL_COPY.note ({ all, friends }). The live notes say, in voice, that the global boards are coming online and the local dead will have to do for now. They must not claim a rank or a count. Keep deep-freezing. Add a Phase 67 (D-08) line to the block comment. Extend test/unit/boards-copy.test.js's verbatim pins to the new strings.

2. src/browser/boardsView.js: accept a `player` input ({ id, displayName } or null; anything else is treated as null). Treat the player as signed in only when signedIn is exactly true. In the strip builder (not GRAVEYARD): signed in gives glyph "", avatar { initials: initialsOf(name), bg: avatarColour(name) } where name is the trimmed display name or strip.live.unnamed, label name, source strip.live.source, and chips with dim false; signed out keeps the Phase 66 strip and adds avatar: null. In the body builder: a ranked board with scope all/friends uses note.live[scope] when signed in and note[scope] otherwise. Change nothing else (scope line, rows, standing card, footnote, dock). Update the header comment: Phase 67 supplies signedIn and player at this seam; Phase 68 adds global sources.

3. test/unit/boardsView.test.js: add a test per behaviour bullet; keep the offline "no worldwide / among friends / @" pin scoped to signed-out views and add the signed-in "no worldwide / among friends" pin.
  </action>
  <verify>
    <automated>node --test test/unit/boardsView.test.js test/unit/boards-copy.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PLAY GAMES · SIGNED IN" content/boards.js` equals 1
    - `node --test test/unit/boardsView.test.js test/unit/boards-copy.test.js` passes, including the signed-in strip, the fallback name, the live notes and the unchanged-rows comparison
    - `node --test test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js` passes
  </acceptance_criteria>
  <done>The view model produces a live, honest strip and live notes for a signed-in player and leaves everything else identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: boardsPanel.js draws the avatar and takes an injected identity() seam</name>
  <files>src/browser/boardsPanel.js, test/unit/boardsPanel-dom.test.js, test/unit/boardsPanel.test.js</files>
  <read_first>
    - src/browser/boardsPanel.js (buildStrip ~168-195, createBoardsPanel's parameters and render(), the header comment's "signedIn is always false this phase" note ~397)
    - test/unit/boardsPanel-dom.test.js (makeView fixture, the strip tests, the BOARDS_CLASSES completeness walk)
    - test/unit/boardsPanel.test.js (the recording fake buildView; the "Every open passes ... signedIn false" test)
  </read_first>
  <behavior>
    - A strip with avatar { initials: "LJ", bg: "#4a5c6b" } renders one .mw-bd-av without the mw-bd-av-nobody class, text "LJ", data-on "1", inline background "#4a5c6b" and the mock's on-ring box-shadow; a strip with avatar null (or no avatar key) renders the Phase 66 nobody glyph unchanged
    - The BOARDS_CLASSES completeness walk still passes with a signed-in view (no new class name)
    - createBoardsPanel without identity passes signedIn false and player null to buildView on every open
    - With identity returning { signedIn: true, player: { id: "p", displayName: "Lanternjaw" } }, every render passes signedIn true and that player; returning { signedIn: false, player: X } passes player null
    - An identity() that throws renders with signedIn false and player null and the panel still renders
    - refresh() after the identity answer changes re-renders with the new identity (buildView sees it) while the panel is open, and does nothing while closed
  </behavior>
  <action>
Per D-08 (the strip goes live behind the same seam):

1. buildStrip in src/browser/boardsPanel.js: when strip.avatar is an object, create `div.mw-bd-av` (no nobody class) with textContent = avatar.initials, dataset.on "1", aria-hidden "true", inline style background = avatar.bg and boxShadow = "inset 0 0 0 1px rgba(0,0,0,.5), 0 0 0 1px #e8c97a" (the mock's AVATAR on-ring); otherwise keep the existing nobody-glyph element. The label/source text and scope chips are unchanged.

2. createBoardsPanel gains an optional `identity` parameter (a function; default returns a frozen signed-out { signedIn: false, player: null }). In render(), read it inside try/catch (a throw or a non-object means signed out) and pass `signedIn: id.signedIn === true` and `player: id.signedIn === true ? (id.player ?? null) : null` to buildView in place of the hardcoded signedIn:false. Replace the header comment's "signedIn is always false this phase" note with: Phase 67 (D-08) supplies the account identity through the injected identity() seam; Phase 68 adds global and friends sources behind the same view.

3. Tests: add the DOM cases to test/unit/boardsPanel-dom.test.js (extend makeView with a signed-in variant) and the controller cases to test/unit/boardsPanel.test.js (a mutable fake identity). Update the existing "signedIn false" controller test to also assert player null.
  </action>
  <verify>
    <automated>node --test test/unit/boardsPanel-dom.test.js test/unit/boardsPanel.test.js test/unit/boardsView.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "identity" src/browser/boardsPanel.js` is at least 2, and render()'s buildView call takes signedIn and player from identity() (pinned by the updated controller test's signed-in case)
    - `node --test test/unit/boardsPanel-dom.test.js test/unit/boardsPanel.test.js` passes, including the avatar branch, the unchanged BOARDS_CLASSES walk, the default, signed-in, throwing and refresh identity cases
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The panel renders the signed-in avatar and reads the account identity through one injected seam, with every Phase 66 behaviour intact.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step
</verification>

<success_criteria>
- D-08: the identity strip goes live signed in and stays the Phase 66 strip signed out or Compete OFF; the chips' note says the boards are coming online
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `BOARDS_PANEL_COPY.strip.live`, `BOARDS_PANEL_COPY.note.live` | content | the signed-in source line, fallback name and coming-online notes |
| `boardsView({ ..., signedIn, player })` | extended export | the signed-in strip (avatar, name, source) and live notes |
| `createBoardsPanel({ ..., identity })` | extended export | injected account identity, default signed out |
| strip `avatar` field | view contract | { initials, bg } signed in, null signed out |

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-04-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: signed in on the device, the Leaderboards strip shows the display name, the initials avatar with its gold ring and PLAY GAMES · SIGNED IN; ALL and FRIENDS are lit and show the coming-online note; with Compete OFF the strip returns to SIGNED OUT. Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>
