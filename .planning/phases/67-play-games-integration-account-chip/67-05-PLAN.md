---
phase: 67-play-games-integration-account-chip
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - mazeworld.html
  - test/unit/account-layout.test.js
  - test/unit/hud-menu-layout.test.js
  - test/unit/shell-map-hud.test.js
autonomous: true
requirements: [ACCT-01, ACCT-02]
user_setup: []

must_haves:
  truths:
    - "HUD band 2 carries a square account chip (#mw-acct-chip) immediately left of the ☰ button, inside a new .mw-hud-actions group; the ☰ button, its menu, its four legacy row ids and its SETTINGS row are unchanged (D-05)"
    - "The title screen carries the same chip (#mw-title-acct-chip) in its top-right corner, clear of the safe-area inset (D-06)"
    - "Both chips have a hit box of at least 44×44 CSS px, and their static first paint is the deliberate 'nobody' face (a hollow square with a dim ?) until the module renders, so the chip never reads as broken (D-07)"
    - "#mw-acct-sheet is a bottom sheet of the mw-legend-sheet family (scrim, role=dialog panel, head with a title slot and a Close button, a rows host), hidden by default (D-09)"
    - "The account sheet and the settings sheet stack above the title screen and the roller (z-index 55 > 50 and 51), so the sheet and its Settings row work from the title chip (D-06, D-10)"
    - "Band 2 still fits a 411px Pixel 7 at text size M with the chip added: counters + one band gap + the chip's footprint + the ☰ footprint, computed by the extended width-budget test (D-05)"
    - "The hidden dev row gains a Play Games (dev) option group bound to the pgsDevSignedIn setting through the existing delegated settings handler (D-12)"
    - "The chip and sheet sit outside the maze viewport's hit path, and the new group declares no z-index, transform or position, so the ☰ menu's z-ladder and scrim behaviour are unchanged"
  prohibitions:
    - statement: "Must not rename, reorder or re-route any ☰ menu row, or move SETTINGS out of the ☰ menu"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "mazeworld.html"
      provides: "#mw-acct-chip in .mw-hud-actions, #mw-title-acct-chip, #mw-acct-sheet, the pgsDevSignedIn dev group, and the .mw-acct-* / .mw-hud-actions / .mw-title-acct CSS"
      contains: "id=\"mw-acct-sheet\""
    - path: "test/unit/account-layout.test.js"
      provides: "markup, geometry, z-order and class-rule pins for the chip and sheet"
      min_lines: 120
  key_links:
    - from: "mazeworld.html #mw-acct-chip / #mw-title-acct-chip / #mw-acct-rows"
      to: "src/browser/accountChip.js (67-07) renderAccountChip / renderAccountSheet, wired in 67-08"
      via: "the pinned class contract (ACCOUNT_CLASSES) and element ids"
      pattern: "mw-acct-rows"
---

<objective>
Give the account chip and its sheet a place in the shell: the band-2 slot beside ☰ (D-05), the title screen's top-right corner (D-06), the bottom sheet in the mw-legend-sheet family (D-09), the look of the avatar and "nobody" faces with 44×44 targets (D-07), and the hidden dev option that seeds the fake provider (D-12). Markup and CSS only; 67-07 renders into these elements and 67-08 wires them.

Purpose: the chip fits band 2 on a Pixel 7 and the sheet works from both the HUD and the title screen.
Output: mazeworld.html markup and CSS, test/unit/account-layout.test.js, and updated HUD layout pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md

<interfaces>
Element ids (67-07 renders into them, 67-08 wires them): #mw-acct-chip (band 2), #mw-title-acct-chip (title), #mw-acct-sheet, #mw-acct-scrim, #mw-acct-title, #mw-acct-close, #mw-acct-rows.

Class contract. 67-07's renderer emits exactly these (it exports them as ACCOUNT_CLASSES; this plan writes a CSS rule for each and pins the list literally):
mw-acct-face, mw-acct-initials, mw-acct-glyph, mw-acct-id, mw-acct-id-text, mw-acct-name, mw-acct-status, mw-acct-action, mw-acct-help, mw-acct-row, mw-acct-label, mw-acct-options, mw-acct-opt, mw-acct-settings.
Static-markup classes owned by this plan: mw-hud-actions, mw-acct-chip, mw-title-acct, mw-acct-sheet, mw-acct-title, mw-acct-rows.
Face states (data-state on .mw-acct-face): "avatar" (initials on an inline background plus the gold on-ring), "nobody" (hollow square, dim ?), "pending" (the nobody face, dimmed).

Band-2 width budget today (test/unit/hud-menu-layout.test.js test 14, text size M): labels 22 glyphs × 6.5 = 143; numerals (3+3+5+2)ch × 9.6 = 124.8; label-number gaps 4 × 2 = 8; counter gaps 3 × 8 = 24; band padding 28; one band gap 10; ☰ footprint 48 − 8 = 40; total 377.8 of 411. A 44px chip that overlaps the empty band gap with margin-left −10px costs 34px, so 411.8 overflows by 0.8px. Reducing .mw-hud-counters' gap from 8px to 7px (3px back, invisible) brings the total to 408.8. The ☰ geometry stays untouched, because it is a ≥48dp UX-02 target.

Existing pins this plan must update deliberately (same commit, Phase 67 comment): test/unit/shell-map-hud.test.js (c) pins `.mw-hud-counters{...gap:8px...}` verbatim; test/unit/hud-menu-layout.test.js (2) slices counters → menu wrap → ☰ and requires the counters-only slice to carry no "mw-hud-menu" token (the chip's markup must not use that token); (4) lists ids outside the viewport; (5) lists HUD rules that declare no z-index/transform; (14) is the width budget. test/unit/shell-gear-toolbar.test.js requires .mw-hud/.mw-hud-band2/.mw-hud-counters to declare no position (unchanged).

z-index facts: .mw-title-screen 50; the roller screen 51; .mw-legend-sheet 45; .mw-hud-menu-scrim 5; .mw-hud-menu-wrap 6.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: markup: the band-2 chip group, the title chip, the account sheet and the dev option</name>
  <files>mazeworld.html, test/unit/account-layout.test.js, test/unit/hud-menu-layout.test.js</files>
  <read_first>
    - mazeworld.html HUD header markup (search `<div class="mw-hud-band2">` through `</header>`, including the Phase 57 comments)
    - mazeworld.html title screen markup (search `id="mw-title-screen"`)
    - mazeworld.html settings sheet markup (search `id="mw-settings-sheet"`, including #mw-dev-row)
    - test/unit/hud-menu-layout.test.js (tests 2, 4 and 12, and its sliceBetween/ruleFor helpers)
    - test/unit/shell-no-content-copies.test.js (the shell must not duplicate content/ strings)
  </read_first>
  <action>
Per D-05, D-06, D-09 and D-12:

1. Band 2: wrap a new account chip and the existing `.mw-hud-menu-wrap` (moved unchanged, children and ids intact) in `<div class="mw-hud-actions">`, placed after `.mw-hud-counters`. The chip comes first: `<button type="button" class="mw-acct-chip" id="mw-acct-chip" aria-haspopup="dialog" aria-controls="mw-acct-sheet" aria-label="Play Games account">` holding `<span class="mw-acct-face" data-state="nobody" aria-hidden="true"><span class="mw-acct-glyph">?</span></span>`. Precede it with an HTML comment: Phase 67 (ACCT-01, D-05/D-07), the account chip sits immediately left of ☰; the ☰ menu and its four legacy row ids are unchanged; rendered by src/browser/accountChip.js through the module script. Do not use the token "mw-hud-menu" anywhere in the chip's markup or comment (test 2's counters slice ends at the menu wrap).

2. Title screen: inside #mw-title-screen, after the .mw-title-scrim div and before .mw-title-body, add the same chip with id "mw-title-acct-chip" and class "mw-acct-chip mw-title-acct" (same aria attributes and the same static nobody face), with a one-line Phase 67 (D-06) comment.

3. Account sheet: directly after the settings sheet's closing tag, add a comment block (Phase 67, ACCT-02, D-09/D-10: the account bottom sheet, the legend-sheet family chrome; rows rendered by src/browser/accountChip.js into #mw-acct-rows; opened by either account chip) and `<div id="mw-acct-sheet" class="mw-legend-sheet mw-acct-sheet" hidden>` containing `<div class="mw-legend-scrim" id="mw-acct-scrim"></div>` and `<div class="mw-legend-panel" role="dialog" aria-labelledby="mw-acct-title">`. The panel holds `<div class="mw-legend-head"><span class="mw-acct-title" id="mw-acct-title"></span><button type="button" class="mw-legend-close" id="mw-acct-close" aria-label="Close">Close</button></div>` and `<div class="mw-acct-rows" id="mw-acct-rows"></div>`. The title text is rendered from content by the module, so the markup carries none.

4. Dev option (D-12): inside the existing #mw-dev-row, after the start-depth options, add `<span class="mw-settings-label">Play Games (dev, next launch)</span>` and `<div class="mw-settings-options" data-setting="pgsDevSignedIn">` with two `.mw-settings-opt` buttons, data-value "false" (text "Signed out") and data-value "true" (text "Signed in"). The existing delegated #mw-settings-rows click handler persists it through writeSetting with no new code; 67-08 reads it once at boot to seed the browser fake. Add a one-line comment: browser dev loop only, native ignores it.

5. Tests: create test/unit/account-layout.test.js (its markup half) pinning: the band-2 order counters → .mw-hud-actions → #mw-acct-chip → .mw-hud-menu-wrap → #mw-hud-menu-btn; both chips' attributes and static nobody face; the title chip inside #mw-title-screen before .mw-title-body; the account sheet's classes, ids, hidden attribute, role/aria-labelledby and rows host; the dev group's data-setting and two values inside #mw-dev-row; both chips and the sheet outside the .mw-maze-viewport slice. In test/unit/hud-menu-layout.test.js extend test (4)'s two id lists with "mw-acct-chip", and extend test (2) with an assertion that #mw-acct-chip sits between the counters and the menu wrap. Read mazeworld.html with CRLF normalized, as the existing layout tests do.
  </action>
  <verify>
    <automated>node --test test/unit/account-layout.test.js test/unit/hud-menu-layout.test.js test/unit/shell-no-content-copies.test.js test/unit/shell-tab-snapshots.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-acct-chip"' mazeworld.html` equals 1, `grep -c 'id="mw-title-acct-chip"' mazeworld.html` equals 1, `grep -c 'id="mw-acct-sheet"' mazeworld.html` equals 1 and `grep -c 'data-setting="pgsDevSignedIn"' mazeworld.html` equals 1
    - `grep -c 'class="mw-hud-actions"' mazeworld.html` equals 1
    - `node --test test/unit/account-layout.test.js test/unit/hud-menu-layout.test.js` passes (the ☰ row pins, test 3, are untouched and still pass)
  </acceptance_criteria>
  <done>The shell has both chip slots, the sheet and the dev option, with the ☰ menu byte-identical in behaviour.</done>
</task>

<task type="auto">
  <name>Task 2: CSS: chip faces, 44×44 geometry, title placement, sheet rows, z-order, and the width budget</name>
  <files>mazeworld.html, test/unit/account-layout.test.js, test/unit/hud-menu-layout.test.js, test/unit/shell-map-hud.test.js</files>
  <read_first>
    - mazeworld.html CSS: .mw-hud-band2, .mw-hud-counters, .mw-hud-menu-wrap, .mw-hud-menu-btn, .mw-hud-menu-face (~1060-1120); the sticky-hover and button:active rules (~552-600); .mw-title-screen and its children (~1248-1310); .mw-legend-sheet family (~1348-1383); .mw-settings-row/-label/-options/-opt (~1392-1410); the roller screen rule (~1461)
    - design/Mazeworld Boards Panel.dc.html (AVATAR ~205-210: the on-ring and initials font)
    - test/unit/hud-menu-layout.test.js tests 5 and 14
    - test/unit/shell-map-hud.test.js test (c)
  </read_first>
  <action>
Per D-05, D-06, D-07, D-09 and D-10 (Claude's discretion over the exact drawing of the glyph):

1. Band 2 (D-05): add `.mw-hud-actions{display:flex;align-items:center;flex:none}`, with no gap, z-index, transform or position, so the ☰ wrap's z-index 6 still competes at header level. Change `.mw-hud-counters`' gap from 8px to 7px and nothing else in that rule (the width budget in the interfaces block). Update test/unit/shell-map-hud.test.js (c)'s verbatim pin to gap:7px with a one-line Phase 67 comment giving the reason.

2. The chip (D-07): `.mw-acct-chip{width:44px;height:44px;margin:-5px 0 -5px -10px;padding:0;border:0;background:transparent;box-shadow:none;border-radius:0;display:flex;align-items:center;justify-content:center;flex:none}`. The −10px left margin overlaps only the empty band gap, and the −5px vertical margins keep band 2's height equal to the ☰'s outer 34px. Add `.mw-acct-chip:active:not(:disabled){transform:none;box-shadow:none}` so the generic button press does not jump the HUD.
   Faces: `.mw-acct-face` 34×34, box-sizing border-box, flex-centred. `[data-state="avatar"]`: no border, box-shadow "inset 0 0 0 1px rgba(0,0,0,.5),0 0 0 1px #e8c97a" (the mock's on-ring), background set inline by the renderer. `[data-state="nobody"]`: background transparent, border 2px solid #6b5c3c, no shadow (the hollow square). `[data-state="pending"]`: the nobody look at opacity .55. `.mw-acct-initials`: var(--disp), 10px, line-height 1, #e6ddc6. `.mw-acct-glyph`: var(--mono), 700, 15px, line-height 1, #6b5c3c (the dim ?).

3. Title chip (D-06): `.mw-title-acct{position:absolute;top:calc(12px + var(--safe-area-inset-top, env(safe-area-inset-top, 0px)));right:12px;z-index:2;margin:0}`.

4. Sheet (D-09/D-10): `#mw-acct-sheet,#mw-settings-sheet{z-index:55}`, commented: above the title screen (50) and the roller (51), because the title chip opens the account sheet and its Settings row opens the settings sheet from the title. Row rules in the settings-sheet look, none shorter than 44px of tap height, and buttons at min-height 48px (UX-02): .mw-acct-id (flex row, gap 12px, padding 14px 0, bottom border 2px #241f16), .mw-acct-id-text (flex column, min-width 0, gap 4px), .mw-acct-name (var(--mono) 700 13.5px #e6ddc6, single line with ellipsis), .mw-acct-status (var(--disp) 6.5px #9a8f76), .mw-acct-action (full width, min-height 48px, var(--disp) 8px uppercase, #e8c97a on #1a1510, 2px #6b5c3c border, 0 4px 0 #0d0b08 shadow, top margin 14px), .mw-acct-action:disabled (the dim look), .mw-acct-help (var(--mono) 700 12.5px #a89c82, line-height 1.35, top margin 10px), .mw-acct-row / .mw-acct-row:last-child, .mw-acct-label, .mw-acct-options and .mw-acct-opt (+ .mw-acct-opt.active) copying the .mw-settings-* declarations as separate rules (do not edit the settings selectors), and .mw-acct-settings (full-width row button, min-height 48px, left-aligned, the .mw-hud-menu-item typography). .mw-acct-sheet, .mw-acct-title and .mw-acct-rows each get a rule (they may be minimal).

5. Tests. In test/unit/hud-menu-layout.test.js: test (5) adds `.mw-hud-actions` to the no-z-index/no-transform list. Test (14) parses `.mw-acct-chip`'s width and its margin shorthand (fourth value = margin-left, second = margin-right), asserts width and height are at least 44, asserts `.mw-hud-actions` declares no gap, and adds chip footprint = width + margin-left + margin-right to the total with the band gap still counted once. The total must stay ≤ 411 at M, and the L total is still logged only. In test/unit/account-layout.test.js (CSS half): the chip's 44×44; the title chip's position:absolute with top/right; both sheets' z-index greater than .mw-title-screen's and the roller screen's (parsed, not hard-coded); the nobody and pending face rules; `.mw-hud-actions` declares no position/z-index/transform/gap; and every class in the literal list from the interfaces block (both the renderer list and the static list) has at least one rule in the style block.
  </action>
  <verify>
    <automated>node --test test/unit/account-layout.test.js test/unit/hud-menu-layout.test.js test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^.mw-acct-chip{width:44px;height:44px" mazeworld.html` equals 1 and `grep -c "#mw-acct-sheet,#mw-settings-sheet{z-index:55}" mazeworld.html` equals 1
    - `node --test test/unit/hud-menu-layout.test.js` passes with test (14) including the chip and reporting a total ≤ 411 at M
    - `node --test test/unit/account-layout.test.js test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js` passes
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The chip reads as an avatar or a deliberate nobody, fits band 2 on a Pixel 7, sits in the title's corner, and its sheet rows are styled and stack above the title.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step
</verification>

<success_criteria>
- D-05/D-06: both chip slots exist; the ☰ menu is unchanged
- D-07/D-09: the faces, targets and sheet chrome match the house sheet family
- The band-2 width budget is recomputed and passes
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `#mw-acct-chip` in `.mw-hud-actions` | markup | band 2, left of ☰ |
| `#mw-title-acct-chip` (`.mw-title-acct`) | markup | title screen top-right |
| `#mw-acct-sheet` / `#mw-acct-scrim` / `#mw-acct-title` / `#mw-acct-close` / `#mw-acct-rows` | markup | the account bottom sheet |
| `data-setting="pgsDevSignedIn"` group in #mw-dev-row | markup | dev-only fake seed |
| `.mw-acct-*`, `.mw-hud-actions`, `.mw-title-acct` rules; sheets at z-index 55; counters gap 7px | CSS | |
| test/unit/account-layout.test.js | new test | |

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-05-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: on the Pixel 7 at text sizes S, M and L, band 2 shows every counter unclipped with the chip beside ☰; the title chip sits clear of the status bar; the "nobody" face reads as deliberate, not broken. Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>
