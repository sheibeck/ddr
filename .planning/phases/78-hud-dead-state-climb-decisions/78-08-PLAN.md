---
phase: 78-hud-dead-state-climb-decisions
plan: 08
type: execute
wave: 5
depends_on:
  - 78-07
files_modified:
  - src/browser/stairsFade.js
  - test/unit/stairs-fade.test.js
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/shell-stairs-fade.test.js
  - src/browser/viewModels.js
  - test/unit/find-card-full-bag.test.js
  - test/unit/shell-map-rail.test.js
  - test/unit/shell-loot-screen.test.js
autonomous: true
requirements:
  - HUD-06
  - HUD-09
must_haves:
  truths:
    - "HUD-06 (CONTEXT: 'a stairs descent fades to black in about 0.6s under the stairs sound, then fades in on the new floor in about 0.4s. With reduced motion there is an instant cut, and the sound still plays'): src/browser/stairsFade.js is a pure, timer-injected controller with STAIRS_FADE_MS { out: 600, in: 400 }; start(onDark, onDone) runs out, then onDark, then in, then onDone; under reduced motion it calls onDark and onDone at once and never shows black"
    - "HUD-06 wiring: every dispatch whose events carry `floorChanged` (the GO DOWN step, and any other path such as Phase 75's resumed exit tile after a fight) starts the fade in dispatchWithNarration; the stairs clip plays at dispatch as today (the cue map's floorChanged -> stairs), so it plays under the fade-out; the map canvas keeps showing the old floor while the screen darkens (draw() holds while the controller is holding) and the new floor is drawn and centred (mzCenterMap) at the dark point"
    - "HUD-06 input lock: while the fade runs, the overlay swallows every tap and drag (map, HUD, tabs, the arrow pad) and window.move refuses (so the keyboard and any queued step do nothing); nothing can walk on the new floor in the dark. hasActiveEncounter() is left unchanged, because it also decides whether the encounter overlay shows"
    - "HUD-06 cards after the fade: the FLOOR n rail card (and any rail card from the same dispatch) becomes visible only when the fade-in has finished, with its full hold counted from then; a combat that starts on arrival is revealed by the fade-in, never shown under black"
    - "HUD-06 scope: teleports keep today's snap (the user asked about stairs); a dev start-at-depth run and a save resume never fade (they do not dispatch floorChanged); the blanket reduced-motion CSS rule is respected (the controller's reduced flag, not a CSS transition, decides the instant cut)"
    - "HUD-09 (CONTEXT: 'the found item (name, stats line, description) and TAKE / LEAVE stay pinned; the drop list is a bounded, scrollable region inside the card, with each row showing name plus the Gear-tab stat line; the same rule applies to any other card listing N bag items'): on the rail's find card with a full bag, the found item's lines come first, the drop list sits in its own region with a max height and vertical scrolling, and TAKE IT NOW / LEAVE IT stay in the card's action row outside that region; the rail card as a whole never grows past the stage"
    - "HUD-09 rows: src/browser/viewModels.js#dropShelfRows(c) returns one row per dropShelfItems(c) entry, in bag order, with the true c.items index, the item name and the Gear tab's stat line (itemStatLines, the one formatter); each row still drops its own item"
    - "HUD-09 adjacency: the drop list appears only when the bag is exactly full and the found item takes a slot; one slot short, the card is the plain TAKE IT / LEAVE IT card"
    - "HUD-09 empty: a potion or scroll found on a full bag gets the plain card and no drop list (quick task 260918-vvt's rule); a one-slot bag that is full gives exactly one row"
    - "HUD-09 ordering: rows are in bag order; after a drop the card re-renders with the found item still first and the remaining rows in order"
    - statement: "HUD-09 encoding and fit: with the largest bag full at text size L on the Pixel 7, the found item's name and TAKE / LEAVE stay on screen, long names and stat lines wrap inside their rows, and the drop list scrolls inside the card"
      verification: backstop
    - "HUD-09 audit: every other card or panel that lists N bag items (the loot screen's bag-full drop shelf and any rail card) uses the same bounded region; the SUMMARY lists what was audited and changed"
  artifacts:
    - path: src/browser/stairsFade.js
      provides: "STAIRS_FADE_MS and the pure fade controller"
      contains: "export function createStairsFade"
    - path: src/browser/viewModels.js
      provides: "dropShelfRows(c)"
      contains: "export function dropShelfRows"
    - path: test/unit/stairs-fade.test.js
      provides: "timing, reduced motion, holding, restart and cancel cases on a fake clock"
    - path: test/unit/find-card-full-bag.test.js
      provides: "rows, the bounded region and the pinned item, adjacency and empty cases"
  key_links:
    - from: mazeworld.html (module: dispatchWithNarration)
      to: src/browser/stairsFade.js (window.__mzStairsFade)
      via: "a floorChanged dispatch starts the fade"
      pattern: "floorChanged"
    - from: mazeworld.html (classic draw and renderRail)
      to: window.__mzStairsFade
      via: "the old floor held and the rail kept hidden while dark"
      pattern: "__mzStairsFade"
    - from: mazeworld.html (classic renderRail's find branch)
      to: src/browser/viewModels.js#dropShelfRows
      via: "the bounded drop list rows"
      pattern: "dropShelfRows|__mzDropShelfItems"
  prohibitions:
    - statement: "MUST NOT accept a move, tap or pad press while the screen is dark, and MUST NOT animate the fade for a reduced-motion player"
      requirement_id: HUD-06
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT hide the item being decided on, or TAKE / LEAVE, behind the drop list"
      requirement_id: HUD-09
      category: transparency
      status: resolved
      verification: test
---

<objective>
Two presentation fixes (HUD-06, HUD-09): a stairs descent fades to black under the stairs sound and fades in on the new floor (an instant cut with reduced motion), and a full-bag find card keeps the found item and TAKE / LEAVE in view while the drop list scrolls inside the card.

Purpose: the user's 2026-09-24 request ("taking the stairs down should feel like a descent, not a hard cut") and 2026-09-25 report ("if you have a larger bag, you only see what's in your bag to drop, and you can't scroll the rail back up to see the item you are trying to loot").

Output: a pure fade controller and its wiring; a drop-row view model and the bounded find card.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 78-08 creates or changes.)

- 78-01: the engine pre-roll decision (`resolveHazard`, `hazardOdds`), its narration, the bot's answer, re-pins and readouts
- 78-02: every shell text size follows S/M/L; the slider gesture module and wiring
- 78-03: the pre-roll hazard card on the rail, the retry card retired, the one-and-done copy
- 78-04: the parity exposure guard, the relaunch probe, the Phase 78 declarations
- 78-05: band 1's identity line; the spell-charge and new-day book rail lines
- 78-06: the dead-state lockdown and the FINAL SHEET
- 78-07: the Movement setting and the arrow pad
- **This plan (78-08):** `src/browser/stairsFade.js`; the `#mw-fade` overlay, the floorChanged hook, draw's hold, the input lock and the post-fade rail reveal (`window.__mzStairsFade`, registered in `bridge.js` and `docs/SHELL-MODULES.md`); `viewModels.js#dropShelfRows`; the find card's bounded drop list; `test/unit/stairs-fade.test.js`; `test/unit/shell-stairs-fade.test.js`; `test/unit/find-card-full-bag.test.js`
- 78-09: Hear the next room; the phase close

## Flagged assumptions (surfaced, not silently decided)

- **HUD-06 edge probe: unclassified, reviewed manually.** Resolved as the truths above: the fade covers every floorChanged dispatch (not only GO DOWN), the old floor is held while darkening, input is locked, cards appear after the fade-in, teleports are unchanged, and reduced motion cuts instantly with the sound.
- **The fade hooks dispatchWithNarration, not stepWith**, because Phase 75's RULES-12 can resolve an exit tile after a fight (a floorChanged from a combat dispatch); one hook covers both. The executor confirms every floorChanged-producing path goes through it.
- **The clip length.** CONTEXT asks for about 0.6s out and 0.4s in; if sfx/stairs.mp3 is much longer, keep the timings (the sound simply continues into the fade-in) and record its measured length.
- **HUD-09 fit** is a layout property a node test cannot measure; the structure (item first, bounded region, actions outside it, a capped rail) is pinned, and the fit at L on the largest bag is measured in scratch where possible and confirmed on the device.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-hud-dead-state-climb-decisions/78-CONTEXT.md
@.planning/todos/pending/2026-09-24-stairs-descent-fades-to-black-with-the-stairs-sound-then-fades-in.md
@.planning/todos/pending/2026-09-25-full-bag-find-card-hides-the-item-being-looted.md

<interfaces>
mazeworld.html (read the file):
- module `dispatchWithNarration(action, opts)`: dispatch, the death record, audio (`playForDispatch` / `cuesForDispatch`; src/browser/sfx.js maps `floorChanged` to the `stairs` clip), the fight log or the rail card (`railCardFor` then `railPush` and `window.renderRail()`), returns the result. `stepWith(action)` then paints, logs, and centres on `floorChanged`/`teleported` (`window.mzCenterMap`) or keeps the party in view, then glides the marker. The dev start-at-depth path (`window.mzDevStartAtDepth`) and boot set state and paint directly.
- classic `hasActiveEncounter()` (the single "something is happening" predicate: dead, combat, store, pending loot, beats, the stair overlay, the combat beat); `draw()` repaints the canvas from S; `renderRail()` decides `hidden`/`data-shown` and starts the hold timer for a new card (`isNew`).
- the blanket reduced-motion CSS rule; `window.__mzMotion.reduced()` (src/browser/motion.js) is the runtime predicate every motion controller is built with. test/unit/harness/fakeClock.js (createFakeClock) drives timer-based controllers in tests (src/browser/cameraGlide.js and typewriter.js are the precedent).
- renderRail's find branch: `usage = window.__mzBagUsage(c)`, `full = usage.full && window.__mzTakesBagSlot(it)`; lines (pending narration, the item line with its sub and usable note, the loot compare line), then when full the bag-full line and `shelfItems = window.__mzDropShelfItems(c)`; after the lines loop a `.shelf#find-drop-shelf` is appended inside `#mw-rail-lines` and filled by classic `renderDropShelf(shelf, entries)` (a `.goods` button per entry: name, the armor or txt sub, "Drop", `window.mzDropItem(i)`); the actions row `#mw-rail-actions` is a sibling after the lines. CSS: `#mw-rail` has no max-height except `#mw-rail[data-over="combat"]` (max-height 45%, overflow-y auto); `.shelf` and `.goods` rules.
- src/browser/viewModels.js: `dropShelfItems(c)` (the one bag-slot list with true indexes), `itemStatLines(item, c)` (the Gear tab's stat formatter, also used by gearSheet.js and storeScreen.js).
- src/browser/bridge.js BRIDGE (alphabetical; owner, consumers, purpose) with test/unit/bridge-registry.test.js; `node tools/bridge-doc.mjs --write` regenerates docs/SHELL-MODULES.md's table. test/unit/harness/shellSandbox.js wires real bridges for shell tests.
- 78-07's arrow pad presses call window.move (so the lock covers them); 78-02's root text scale.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The fade controller</name>
  <files>src/browser/stairsFade.js, test/unit/stairs-fade.test.js</files>
  <read_first>
    - src/browser/cameraGlide.js and src/browser/motion.js (the timer-injected controller pattern and the reduced predicate)
    - test/unit/harness/fakeClock.js and a test that drives cameraGlide with it
  </read_first>
  <behavior>
    - With motion allowed: start(onDark, onDone) sets phase "out" (holding true, active true); at 600ms onDark runs once and phase is "in" (holding false, active true); at 1000ms onDone runs once and phase is "idle" (active false). setPhase is called with "out", "in", "idle" in that order.
    - Reduced motion: start calls onDark then onDone synchronously, setPhase is only ever "idle", holding and active are never true.
    - A start while a fade is active restarts cleanly: the pending callbacks of the first run are cancelled except its onDark, which runs at once if it had not yet run, so the new floor is always drawn; exactly one onDone fires, for the last start.
    - cancel() jumps to idle, runs any pending onDark then onDone once, and clears timers.
    - Purity: no DOM or window reads; timers come from the injected schedule.
  </behavior>
  <action>
1. Tests first on the fake clock. Watch them fail.
2. Create src/browser/stairsFade.js (house style, header naming HUD-06 and the user's request): `STAIRS_FADE_MS` frozen { out: 600, in: 400 }, `createStairsFade({ setPhase, reduced, schedule, cancel })` returning `{ start, cancel, active, holding }`.
  </action>
  <verify>
    <automated>node --test test/unit/stairs-fade.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/stairs-fade.test.js` passes every behaviour.
    - `grep -c "export function createStairsFade" src/browser/stairsFade.js` equals 1.
  </acceptance_criteria>
  <done>A tested controller owns the fade's timing and its reduced-motion cut.</done>
</task>

<task type="auto">
  <name>Task 2: The descent fades under the stairs sound</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/shell-stairs-fade.test.js</files>
  <read_first>
    - mazeworld.html (dispatchWithNarration, stepWith's jumped/centre block, window.mzDevStartAtDepth, classic hasActiveEncounter, draw, renderRail's show and hold logic, the reduced-motion CSS rule, where other module controllers are created and bridged)
    - src/browser/bridge.js and test/unit/bridge-registry.test.js; tools/bridge-doc.mjs
    - test/unit/harness/shellSandbox.js
  </read_first>
  <action>
1. Markup and CSS: a `#mw-fade` overlay (full stage and HUD, black, `aria-hidden="true"`, `pointer-events:none` at rest and `auto` while a phase is active, opacity driven by a `data-phase` attribute with transitions of 600ms out and 400ms in).
2. Module: create the controller with `reduced: () => window.__mzMotion?.reduced?.()` and a setPhase that writes `data-phase`; bridge it as `window.__mzStairsFade` (register it in src/browser/bridge.js with owner, consumers and purpose, then `node tools/bridge-doc.mjs --write`).
3. dispatchWithNarration: when the result's events contain `floorChanged`, start the fade with onDark = draw the canvas, `window.mzCenterMap()`, and paint the HUD; onDone = reveal the rail (re-run renderRail so the FLOOR card's show and hold begin now). stepWith: skip its own centre for a floorChanged (the fade's onDark does it); teleports keep today's centre. Leave the audio exactly where it is (the stairs clip plays at dispatch, under the fade-out).
4. Input lock: the module `window.move` returns at once while the fade is active (the keyboard and the arrow pad both funnel through it), and the overlay's `pointer-events:auto` while active swallows taps and drags everywhere it covers. Leave hasActiveEncounter() unchanged (it also decides whether renderEncounter shows its overlay). Classic draw() returns early (keeping the old floor on the canvas) while `window.__mzStairsFade?.holding?.()`; renderRail keeps the rail hidden while the fade is active. Comments: HUD-06.
5. test/unit/shell-stairs-fade.test.js (source and sandbox): the overlay markup; a floorChanged dispatch starts the fade; window.move refuses during it; draw holds while darkening; hasActiveEncounter's body is unchanged; mzDevStartAtDepth and boot never start it; a teleport does not start it; the reduced path cuts at once. Wire the bridge in shellSandbox.js if the classic script needs it there.
  </action>
  <verify>
    <automated>node --test test/unit/shell-stairs-fade.test.js test/unit/stairs-fade.test.js test/unit/bridge-registry.test.js test/unit/reduced-motion.test.js test/unit/sfx-map.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mw-fade" mazeworld.html` is at least 2 and `grep -c "__mzStairsFade" mazeworld.html` is at least 3 (the bridge, draw, renderRail).
    - `grep -c "__mzStairsFade" src/browser/bridge.js` equals 1 and `node --test test/unit/bridge-registry.test.js` passes (the docs table regenerated).
    - `node --test test/unit/shell-stairs-fade.test.js test/unit/reduced-motion.test.js test/unit/sfx-map.test.js` passes.
  </acceptance_criteria>
  <done>Going down the stairs darkens the old floor under the stairs sound, swaps in the dark, and fades in on the new floor; nothing moves meanwhile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The full-bag find card keeps the loot in view</name>
  <files>src/browser/viewModels.js, test/unit/find-card-full-bag.test.js, mazeworld.html, test/unit/shell-map-rail.test.js, test/unit/shell-loot-screen.test.js</files>
  <read_first>
    - src/browser/viewModels.js (dropShelfItems, itemStatLines)
    - mazeworld.html (renderRail's find branch and the lines loop, renderDropShelf, the `#mw-rail`, `.mw-rail-lines`, `.shelf`, `.goods` CSS, the module bridge for `__mzDropShelfItems`; the loot screen's bag-full shelf in renderEncounter)
    - content/bags.js (the largest bag's slot count)
    - test/unit/shell-map-rail.test.js and test/unit/shell-loot-screen.test.js (their find and shelf pins)
  </read_first>
  <behavior>
    - dropShelfRows(c): one row per dropShelfItems(c) entry in the same order with `{ i, name, stats }` where `stats` joins that item's itemStatLines texts; bag-free items (potions, scrolls) never appear; an empty bag gives [].
    - With the largest bag full and a weapon found (a real engine state): the find card's DOM order is the found item's lines, then the bounded drop region (one row per bag item, each row a name and its stat line and a Drop control calling mzDropItem with the true index), then the actions row with TAKE IT NOW and LEAVE IT outside the region.
    - One slot short of full: no drop region and the plain TAKE IT / LEAVE IT; a potion found on a full bag: no drop region.
    - CSS: the drop region's rule declares a max-height tied to the viewport and the text scale and `overflow-y:auto`; the map-side rail card has its own max-height so it never exceeds the stage; long names wrap (no nowrap or ellipsis on the rows).
  </behavior>
  <action>
1. Tests first in test/unit/find-card-full-bag.test.js (the view model through real engine states; the shell structure through the sandbox). Watch them fail.
2. viewModels.js: export `dropShelfRows(c)` (JSDoc: HUD-09, the one bag-slot list plus the one stat formatter). Point the existing `window.__mzDropShelfItems` bridge at the rows (update its bridge.js purpose text only if the registry test requires it) or add the rows to an existing bridge object; add no new `__mz` name.
3. mazeworld.html: in renderRail's find branch, render the drop rows into a dedicated bounded region (a class such as `mw-find-drop`) placed after the item's lines and before the actions row, with a short header line; renderDropShelf renders a row's name and stat line; add the CSS for the region (max-height from the viewport and the scale, `overflow-y:auto`, `overscroll-behavior:contain`, wrapping rows) and a max-height for the map-side rail card. Audit renderEncounter's loot screen and any other card that lists bag items; apply the same bounded region where one lists N bag items; record the audit.
4. Re-pin shell-map-rail.test.js and shell-loot-screen.test.js where their find or shelf pins moved (HUD-09 comments), then run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/find-card-full-bag.test.js test/unit/shell-map-rail.test.js test/unit/shell-loot-screen.test.js test/unit/text-scale.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/find-card-full-bag.test.js` passes every behaviour, including adjacency, empty and order.
    - `grep -c "export function dropShelfRows" src/browser/viewModels.js` equals 1 and `grep -c "mw-find-drop" mazeworld.html` is at least 2.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>With a full bag, the found item and TAKE / LEAVE stay in view and the drop list scrolls inside the card.</done>
</task>

</tasks>

<verification>
- The fade controller is proven on a fake clock; every floorChanged dispatch fades, holds the old floor, locks input and shows cards after; reduced motion cuts instantly with the sound.
- The find card pins the loot and its buttons and bounds the drop list.
- `npm test` is green; `npm run boot:check` passes (rerun once if it flakes).
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause):
  - Take the stairs down: the screen darkens under the stairs sound, then the new floor fades in; the FLOOR card appears after. Tap and press arrows during the fade: nothing moves.
  - With Android's remove-animations setting on: an instant cut, and the stairs sound still plays.
  - Teleport: unchanged (a snap).
  - With the biggest bag, full, at text size L, find a weapon: its name, stats and TAKE IT NOW / LEAVE IT stay visible; the drop list scrolls inside the card; each row shows the item's stats.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 78 criteria 4 and 8 hold: a stairs descent fades to black under the stairs sound, then fades in on the new floor, honouring reduced motion; with a full bag, the find card keeps the found item and TAKE / LEAVE in view while the drop list scrolls inside the card.
</success_criteria>

<output>
Create `.planning/phases/78-hud-dead-state-climb-decisions/78-08-SUMMARY.md` when done. Include the plan base SHA, the measured stairs clip length, the list of floorChanged-producing paths confirmed through the hook, the bag-list audit, and every re-pinned test.
</output>
