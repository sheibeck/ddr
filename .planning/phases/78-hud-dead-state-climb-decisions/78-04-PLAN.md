---
phase: 78-hud-dead-state-climb-decisions
plan: 04
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - test/parity/hazard-exposure.test.js
  - test/roundtrip/resume-roundtrip.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/DIFFICULTY-RETUNE.md
  - docs/TERRAIN.md
  - docs/ROLL-LEDGER.md
autonomous: true
requirements:
  - CLIMB-01
  - CLIMB-02
must_haves:
  truths:
    - "A standing parity guard (test/parity/hazard-exposure.test.js) replays every fixture script and scenario on the engine, the same way the parity tests do, and asserts the measured hazard exposure 78-01 recorded: with zero exposure (expected), no replay site ever emits `hazardChoice`, so a future fixture that reaches a wall or crevice fails loudly and points at the reconcile decision; if 78-01 measured exposure, the guard pins exactly that site set"
    - "CLIMB-02 concurrency, proven through the relaunch corpus: test/roundtrip/resume-roundtrip.test.js (Phase 76's bot corpus) probes every sampled state with a pending hazard with `resolveHazard` cross true on both the live and the reloaded state and asserts the next states and events are deep-equal (a relaunch can never change the climb or leap roll), probes a TURN BACK the same way at least once, and asserts the corpus actually sampled pending-hazard states (a count above zero)"
    - "The Phase 78 H2 in test/parity/FIXTURE-INVENTORY.md declares the pre-roll decision as a canon divergence in movement timing (the prototype rolls on the step; the engine pauses and rolls on the commit, draw-identical), with the rule, the predictor (78-01's exposure table), the live scan (parity pass count; `git diff --stat <phase-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` empty unless 78-01 reconciled a site), the draw-identity proof (the golden commit test), the Heights timing change, and the moved bot pins and seeds from 78-01 in a table (label, before, after, cause)"
    - "docs/DIFFICULTY-RETUNE.md gets a `## v2.1 HUD & climb decisions (Phase 78) — bot readout` section recording 78-01's 200-seed before and after readouts, the per-seed comparison and its explained differences, and that no compensating change was made"
    - "docs/TERRAIN.md's Heights tile-key section states the new timing (noteHeightsAttempt runs only on a committed climb or leap; the pause and TURN BACK never arm fear) and drops the retry-record sentence"
    - "docs/ROLL-LEDGER.md appends (append-only, the file's CRLF line endings kept, zero deletion lines) a short Phase 78 note closing Phase 74's climb-card handoff: the card's odds come from rollOdds.js#hazardOddsText over the engine's own faces (78-03)"
    - "Nothing here changes behaviour: engine/ and src/ are byte-identical to this plan's base"
  artifacts:
    - path: test/parity/hazard-exposure.test.js
      provides: "the standing no-hazard-at-any-replay-site guard"
      contains: "hazardChoice"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the Phase 78 H2 (the pre-roll decision, measured)"
      contains: "## Phase 78"
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 78 readout record"
      contains: "(Phase 78)"
  key_links:
    - from: test/roundtrip/resume-roundtrip.test.js (the pending-hazard probe)
      to: the resolveHazard action
      via: "the same commit on the live and the reloaded state"
      pattern: "resolveHazard"
    - from: test/parity/hazard-exposure.test.js
      to: every fixture in test/parity/fixtures
      via: "an engine replay per site"
      pattern: "action-script"
  prohibitions:
    - statement: "MUST NOT regenerate or edit any parity fixture the measurement did not move, and MUST NOT edit test/parity/prototype-master.js.txt"
      requirement_id: CLIMB-01
      category: transparency
      status: resolved
      verification: test
---

<objective>
Declare and guard the pre-roll decision (CLIMB-01, CLIMB-02) the way the engine gate requires: a standing parity exposure guard, a relaunch proof that a resumed decision rolls exactly the same, and the written records (fixture inventory, readouts, terrain rules, roll ledger).

Purpose: the engine gate ("measure, declare, regenerate only what moved; bot readout before and after") and CONTEXT's relaunch integration point ("Phase 76's relaunch persistence must carry pendingHazard").

Output: one new guard test, the relaunch probe, and four documentation sections.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 78-04 creates or changes.)

- 78-01: the engine pre-roll decision (`resolveHazard`, `hazardOdds`), its narration, the bot's answer, re-pins and readouts
- 78-02: every shell text size follows S/M/L; the slider gesture module and wiring
- 78-03: the pre-roll hazard card on the rail, the retry card retired, the one-and-done copy
- **This plan (78-04):** `test/parity/hazard-exposure.test.js`; the `resolveHazard` probe in `test/roundtrip/resume-roundtrip.test.js`; the Phase 78 H2 in `test/parity/FIXTURE-INVENTORY.md`; the Phase 78 readout section in `docs/DIFFICULTY-RETUNE.md`; the Heights timing in `docs/TERRAIN.md`; the Phase 78 note in `docs/ROLL-LEDGER.md`
- 78-05: band 1's identity line; the spell-charge and new-day book rail lines
- 78-06: the dead-state lockdown and the FINAL SHEET
- 78-07: the Movement setting and the arrow pad
- 78-08: the stairs fade; the full-bag find card
- 78-09: Hear the next room; the phase close (it appends its own measured-zero note under this plan's H2)

## Flagged assumptions (surfaced, not silently decided)

- **The guard is written for the exposure 78-01 measured.** Zero is expected; if 78-01 measured and reconciled sites, the guard pins that exact set instead and the FIXTURE-INVENTORY section names them.
- **The relaunch corpus now meets many more pending hazards** (every bot crossing pauses), so its runtime may grow; if it exceeds the file's own budget, sample pending-hazard states with the per-run cap the file already uses and say so.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-hud-dead-state-climb-decisions/78-CONTEXT.md
@.planning/phases/78-hud-dead-state-climb-decisions/78-01-SUMMARY.md

<interfaces>
From 78-01's SUMMARY: the plan base SHA (the phase base for the parity diff), the parity exposure table, the bot exposure list, the golden scenario counts, the moved pins and seeds (label, before, after, cause), the readout files tools/readouts/78-01-before.txt and -after.txt and the per-seed comparison.

test/parity/divergence-records.test.js shows the engine-only exposure-replay pattern (imports newRun/applyAction, applyStartCombat from ./harness/comparables.js; reads every fixture JSON; replays scripts and scenarios); reuse it rather than inventing another replay.

test/roundtrip/resume-roundtrip.test.js (76-04): a bot corpus over `playRun` with an onStep hook; for a state with a live combat, an open store, a pendingFind, a pendingHazard, a pendingTile or a pendingJoiner it saves, loads, asserts `diffPaths` is empty, then applies a probe action to the live and the loaded state and asserts equal next states and events. The pending-hazard probe was `move` in the hazard's `dir`, which since 78-01 re-pauses.

Docs: test/parity/FIXTURE-INVENTORY.md's Phase 75/76 H2s are the section template (rule, predictor, live scan, moved set, draw-count pins, byte-identical elsewhere). docs/DIFFICULTY-RETUNE.md's `## v2.1 engine rules (Phase 75) — bot readouts` is the readout template. docs/TERRAIN.md's `### The Heights tile-key model and the one-square leave rule` describes the retry-era timing. docs/ROLL-LEDGER.md's last H2 is `## Phase 74 display closure (ROLL-02/03)`, which names the Phase 78 climb-card handoff; the file uses CRLF line endings and test/unit/roll-ledger-sync.test.js guards it (74-08 appended with a small Node script to keep CRLF).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The parity exposure guard and the relaunch probe</name>
  <files>test/parity/hazard-exposure.test.js, test/roundtrip/resume-roundtrip.test.js</files>
  <read_first>
    - .planning/phases/78-hud-dead-state-climb-decisions/78-01-SUMMARY.md (the exposure table)
    - test/parity/divergence-records.test.js (the exposure-replay pattern and its imports)
    - test/parity/movement-parity.test.js (how the movement script is replayed)
    - test/roundtrip/resume-roundtrip.test.js (the corpus, the probe table, the counts it asserts)
  </read_first>
  <action>
1. Create test/parity/hazard-exposure.test.js (header: Phase 78, CLIMB-01, why it exists): replay every fixture file's script or scenarios on the engine exactly as divergence-records.test.js does and collect every `hazardChoice` event with its site and action index. Assert the collected set equals 78-01's measured set (empty expected), with a failure message that says a fixture now reaches a wall or crevice and names 78-01's reconcile decision. Add a teeth case: a synthetic engine replay that steps a hero toward a hand-set climb tile does emit `hazardChoice` through the same collector.
2. test/roundtrip/resume-roundtrip.test.js: change the pending-hazard probe to `{ type: "resolveHazard", cross: true }`; add one TURN BACK probe (`cross: false`) on the first sampled pending-hazard state; assert the corpus sampled at least one pending-hazard state (and report the count in a test title or message). Comment with CLIMB-02 and why the commit is the meaningful probe (no die was drawn before the relaunch, so the same die must come after it).
  </action>
  <verify>
    <automated>node --test test/parity/hazard-exposure.test.js test/roundtrip/resume-roundtrip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/parity/hazard-exposure.test.js` passes, including its teeth case.
    - `node --test test/roundtrip/resume-roundtrip.test.js` passes and its pending-hazard sample count is above zero.
    - `grep -c "resolveHazard" test/roundtrip/resume-roundtrip.test.js` is at least 1.
  </acceptance_criteria>
  <done>A future fixture that reaches a hazard fails loudly, and a relaunch is proven not to change a pending climb or leap.</done>
</task>

<task type="auto">
  <name>Task 2: The written records (inventory, readout, terrain, ledger)</name>
  <files>test/parity/FIXTURE-INVENTORY.md, docs/DIFFICULTY-RETUNE.md, docs/TERRAIN.md, docs/ROLL-LEDGER.md</files>
  <read_first>
    - .planning/phases/78-hud-dead-state-climb-decisions/78-01-SUMMARY.md
    - test/parity/FIXTURE-INVENTORY.md (the Phase 75 and 76 H2s)
    - docs/DIFFICULTY-RETUNE.md (the Phase 75 readout section)
    - docs/TERRAIN.md (the Heights tile-key section)
    - docs/ROLL-LEDGER.md (the Phase 74 display closure, its Phase 78 handoff line) and test/unit/roll-ledger-sync.test.js
    - tools/readouts/78-01-before.txt and tools/readouts/78-01-after.txt
  </read_first>
  <action>
1. test/parity/FIXTURE-INVENTORY.md: append `## Phase 78: the pre-roll hazard decision (CLIMB-01/02) — measured` with `### The rule`, `### The predictor` (78-01's exposure table), `### The live scan` (parity pass count; the phase-base diff of fixtures and the prototype master, empty unless a site was reconciled; `git hash-object test/parity/prototype-master.js.txt`), `### Draw identity` (the golden commit test and its scenario counts), `### Heights timing (declared)`, `### Bot pins and seeds` (78-01's table) and `### Byte-identical elsewhere`. State it as a declared canon divergence in movement timing.
2. docs/DIFFICULTY-RETUNE.md: add `## v2.1 HUD & climb decisions (Phase 78) — bot readout` next to the other v2.1 sections, with both readouts' headline numbers (death-depth distribution, reach counts), the per-seed comparison and every explained difference, and "no compensating change".
3. docs/TERRAIN.md: rewrite the Heights paragraph's timing sentences to the Phase 78 rule (noteHeightsAttempt runs on the commit; the pause and TURN BACK never arm fear; flight, ether and a tool still never reach it) and remove the retry-record sentence.
4. docs/ROLL-LEDGER.md: append (CRLF kept, zero deletions, as 74-08 did with a small Node script) a short `## Phase 78 note (CLIMB-01)` saying the climb card's odds read rollOdds.js#hazardOddsText over engine/movement.js#hazardOdds, which shares the roll's faces helpers, closing Phase 74's handoff.
5. Confirm `git diff --quiet <plan-base> -- engine src` exits 0, then run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/roll-ledger-sync.test.js test/parity/fixture-inventory.test.js test/unit/stale-terms.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 78" test/parity/FIXTURE-INVENTORY.md` equals 1.
    - `grep -c "(Phase 78)" docs/DIFFICULTY-RETUNE.md` is at least 1.
    - `grep -c "Phase 78" docs/TERRAIN.md` is at least 1.
    - `git diff <plan-base> --numstat -- docs/ROLL-LEDGER.md` shows zero deleted lines and `node --test test/unit/roll-ledger-sync.test.js` passes.
    - `git diff --quiet <plan-base> -- engine src` exits 0 and `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The pre-roll decision is declared, measured and written down where the engine gate expects it.</done>
</task>

</tasks>

<verification>
- The exposure guard and the relaunch probe pass; the corpus samples pending hazards.
- The inventory, readout, terrain and ledger records exist; no behaviour changed in this plan.
- `npm test` is green.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause): with the climb card up, force-close the app and relaunch; the same card returns, and CLIMB IT rolls as it would have (compare the Oracle line).
  </human-check>
</verification>

<success_criteria>
The pre-roll decision's engine change is measured, declared and guarded per the engine gate, and a relaunch is proven not to change a pending climb or leap (CLIMB-02's relaunch half).
</success_criteria>

<output>
Create `.planning/phases/78-hud-dead-state-climb-decisions/78-04-SUMMARY.md` when done. Include the plan base SHA, the guard's measured set, the corpus's pending-hazard sample count, and links to the four written sections.
</output>
