---
phase: 78-hud-dead-state-climb-decisions
plan: 05
type: execute
wave: 2
depends_on:
  - 78-01
files_modified:
  - src/browser/hudBands.js
  - test/unit/hudBands.test.js
  - test/unit/hud-bands-layout.test.js
  - src/browser/narrationLines.js
  - src/browser/eventNarration.js
  - src/browser/engineAdapter.js
  - test/unit/book-refill-lines.test.js
  - test/unit/narrationLinesTable.test.js
  - test/unit/engineAdapter.test.js
autonomous: true
requirements:
  - HUD-01
must_haves:
  truths:
    - "HUD-01 (CONTEXT: 'band 1 reads \"Race Sub-class · Lvl N\" (e.g. \"Dwarf Pickpocket · Lvl 3\"), with no parent class and no parentheses'): src/browser/hudBands.js#identityParts(c).line is the race and the sub-class joined by one space, then ' · Lvl N'; identityLine(c) is 'Name — ' plus that line"
    - "HUD-01 fallbacks: a character with no sub-class shows its class in the sub-class's place (today's omission rule for a falsy sub); a missing race leaves no leading space; with neither race nor class nor sub the line is 'Lvl N'; a falsy level reads Lvl 1; a missing or null character keeps the 'Nameless' placeholder; the functions never throw"
    - "Spell charges (CONTEXT, user report 2026-09-25: a regained spell charge gets its rail line back): `spellChargeRecovered` leaves ORACLE_ONLY and gets a LINE_FOR rail line with the count (have/max), a minor event shown as a line, never a decision card"
    - "ORACLE_ONLY audit (CONTEXT: 'audit ORACLE_ONLY for other stale \"the HUD shows this\" reasons'): every remaining entry's reason is re-checked against today's HUD; `dayBegan` (the HUD Day counter) and `floorChanged` (the HUD Depth counter) stay with reasons that are true; any other stale reason is fixed; the SUMMARY lists the audit"
    - "The new-day book line (CONTEXT, second report): on a fed day that refilled a spent book, the rail line and the Oracle line name the refill with the count, for example 'A new day. Your book is full again (12/12).', and name each party member whose book refilled; the line sits with the day's rations line, which comes before any wandering monster in the same step's events, so when a fight follows it shows in the fight's lead-in (THE FIGHT SO FAR, event order per 77-02) and otherwise on the rail"
    - "Agreement with Phase 75's RULES-15: the refill is named ONLY when the engine's own `rationsEaten.refilled` is set and a sheet's spent charges actually went to zero; an unfed day keeps today's wentHungry `booksKept` wording (the book stayed empty) and never claims a refill; a fed day with no spent book says nothing about books"
    - "Presentation only (CONTEXT: 'Presentation-only'): the counts come from a pure stamp, eventNarration.js#stampBookRefill(events, before, after), applied in engineAdapter.js#dispatch after 77-05's stampScrollCopyNotes; the engine's events, state and rng are untouched"
  artifacts:
    - path: src/browser/hudBands.js
      provides: "identityParts/identityLine in the Race Sub-class · Lvl N form"
      contains: "identityParts"
    - path: src/browser/eventNarration.js
      provides: "stampBookRefill and the refill clause on the rationsEaten Oracle line"
      contains: "export function stampBookRefill"
    - path: src/browser/narrationLines.js
      provides: "spellChargeRecovered's rail line; the refill clause on rationsEaten's line; the audited ORACLE_ONLY"
      contains: "spellChargeRecovered"
    - path: test/unit/book-refill-lines.test.js
      provides: "fed, unfed, full-book, member, trickle and same-step-wanderer cases through the real engine"
  key_links:
    - from: src/browser/engineAdapter.js#dispatch
      to: src/browser/eventNarration.js#stampBookRefill
      via: "the stamp on the returned events before narration"
      pattern: "stampBookRefill\\("
    - from: src/browser/narrationLines.js (LINE_FOR.rationsEaten)
      to: the stamped `books` payload
      via: "the refill clause"
      pattern: "books"
  prohibitions:
    - statement: "MUST NOT claim a refill that did not happen: an unfed day, a book that was already full, or a sheet whose charges did not reach zero gets no 'full again' line"
      requirement_id: HUD-01
      category: transparency
      status: resolved
      verification: test
---

<objective>
Make the HUD and the rail tell the truth at a glance (HUD-01 plus two CONTEXT decisions): band 1 reads "Race Sub-class · Lvl N"; a regained spell charge gets a rail line again; and a new day that refills the spell book says so with the count, before any fight the same step brings.

Purpose: the 2026-09-22 ruling ("Only show the sub class on top rail ... keep the race, drop the parent class"), and the 2026-09-25 reports ("we're no longer getting a rail update when a spell charge is regained"; "I had 1 charge left ... then when combat started I had 12 charges"). The second is not an engine bug: a fed new day refills the book (Phase 75 RULES-15) and nothing on screen said so.

Output: the band-1 formatter, the charge line, the refill stamp and lines, and tests through the real engine.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 78-05 creates or changes.)

- 78-01: the engine pre-roll decision (`resolveHazard`, `hazardOdds`), its narration, the bot's answer, re-pins and readouts
- 78-02: every shell text size follows S/M/L; the slider gesture module and wiring
- 78-03: the pre-roll hazard card on the rail, the retry card retired, the one-and-done copy
- 78-04: the parity exposure guard, the relaunch probe, the Phase 78 declarations
- **This plan (78-05):** band 1 "Race Sub-class · Lvl N" (`hudBands.js`); `spellChargeRecovered`'s rail line and the audited ORACLE_ONLY (`narrationLines.js`); `eventNarration.js#stampBookRefill` and the refill clause on the rations lines; the stamp in `engineAdapter.js#dispatch`; `test/unit/book-refill-lines.test.js`
- 78-06: the dead-state lockdown and the FINAL SHEET
- 78-07: the Movement setting and the arrow pad
- 78-08: the stairs fade; the full-bag find card
- 78-09: Hear the next room; the phase close

## Flagged assumptions (surfaced, not silently decided)

- **HUD-01 edge probe: unclassified, reviewed manually.** Resolved as the fallback truths above. The sub-class alone is not a unique class marker, but the ruling asks for exactly this; the Hero tab still shows the full "Race Sub · Class" tag.
- **Where the refill line lives.** `dayBegan` is a direct rail event (the Day card) but ORACLE_ONLY on the line side, so it never reaches THE FIGHT SO FAR. The refill therefore rides the fed day's `rationsEaten` line (already a LINE_FOR line, already before the wandering check in event order), which is what makes it show in a same-step fight's lead-in. The Day counter reason for `dayBegan` stays true, so it stays ORACLE_ONLY.
- **The spell-charge line fires every 20 squares for a Magic User with a spent charge.** It is a minor rail line (auto-clearing), per the card-vs-rail-line ruling.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-hud-dead-state-climb-decisions/78-CONTEXT.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-12-SUMMARY.md
@.planning/todos/pending/2026-09-22-hud-band-1-identity-line-should-show-sub-class-not-full-class.md
@.planning/todos/pending/2026-09-25-rail-line-when-a-spell-charge-is-regained.md

<interfaces>
**Run order:** 77-02 (event-order fold for the fight log), 77-05 (stampScrollCopyNotes in eventNarration.js, applied in engineAdapter.js#dispatch after decorateMisses) and 78-01 (hazard narration entries) are merged first. Read their SUMMARYs; reference functions by name.

src/browser/hudBands.js: `IDENTITY_PLACEHOLDER` ("Nameless"), `identityParts(c)` -> frozen `{ name, line }` where today `line` is `${raceClass}${subGroup} · Lvl ${level}` (race and class joined, then " (Sub)"), and `identityLine(c)` -> `${name} — ${line}`. test/unit/hudBands.test.js pins seven identityLine cases (they expect "Race Class (Sub)"). mazeworld.html's paint writes identityParts into `#mw-hud-name`/`#mw-hud-line` and identityLine into the line's title (no shell edit needed).

engine/movement.js (read only): every 20 squares a Magic User with a spent charge pushes `spellChargeRecovered { charges, max }` (hero only). newDay pushes `dayBegan`, then on a fed day `rationsEaten { eats, left, eaters, refilled? }` (refilled when any book, hero's or a live member's, had spent charges) and resets `c.spellsUsed` and each member's `spellsUsed` to 0; on an unfed day `wentHungry { cost, need, have, mouths, heft?, booksKept? }`; the wandering-monster check comes after. `maxCharges(c)` is exported.

src/browser/narrationLines.js: `ORACLE_ONLY` (a Set with a reason comment per entry: `dayBegan` "the HUD's day counter already shows this", `floorChanged` "the HUD's depth banner already shows this", `spellChargeRecovered` "the grimoire/HUD charge display already shows this", ...); `LINE_FOR.rationsEaten` ("Rations: −N (M left).") and `LINE_FOR.wentHungry` (with " Book stays empty." when booksKept); `linesForAction(type, events, ctx, opts)` feeds both the rail (railCardFor) and the fight log (fightLogLinesFor). src/browser/eventNarration.js: `EVENT_NARRATION.spellChargeRecovered`, `dayBegan`, `rationsEaten`, `wentHungry` (the booksKept clause). src/browser/rail.js: `RAIL_DIRECT` includes `dayBegan`.

src/browser/engineAdapter.js#dispatch: `applyAction(currentState, action)`, persist, the death fold, the miss decoration and 77-05's stamp; `currentState` before applyAction is the `before` state.

The stale-terms tripwire bans the retired UI-surface words in comments and test titles: say "rail line".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Band 1 reads "Race Sub-class · Lvl N" (HUD-01)</name>
  <files>src/browser/hudBands.js, test/unit/hudBands.test.js, test/unit/hud-bands-layout.test.js</files>
  <read_first>
    - src/browser/hudBands.js (identityParts, identityLine and their JSDoc)
    - test/unit/hudBands.test.js and test/unit/hud-bands-layout.test.js
    - .planning/todos/pending/2026-09-22-hud-band-1-identity-line-should-show-sub-class-not-full-class.md
  </read_first>
  <behavior>
    - { name "Ardwin", race "Dwarf", cls "Thief", sub "Pickpocket", level 3 } -> identityParts line "Dwarf Pickpocket · Lvl 3", identityLine "Ardwin — Dwarf Pickpocket · Lvl 3".
    - sub null -> "Human Thief · Lvl 3" (class stands in); race empty -> "Pickpocket · Lvl 3"; race, cls and sub all empty -> "Lvl 3"; level 0 or missing -> "Lvl 1"; name empty -> "Nameless — ..."; null or undefined -> "Nameless" (identityLine) and { name: "Nameless", line: "" } (identityParts), never throwing.
    - No output contains "(" or ")" or the parent class when a sub-class exists; no standalone "WP".
  </behavior>
  <action>
1. Rewrite the seven identityLine tests and add the fallback cases above in test/unit/hudBands.test.js (HUD-01 comment, the user's ruling quoted). Watch them fail.
2. hudBands.js, per CONTEXT HUD-01: build `line` from the race and `c.sub || c.cls` (falsy parts dropped, one space between) followed by " · Lvl N", or just "Lvl N" when neither exists; update both JSDoc blocks (the format, the fallbacks, the ruling).
3. Update test/unit/hud-bands-layout.test.js only if it pins the old string, with a HUD-01 comment.
  </action>
  <verify>
    <automated>node --test test/unit/hudBands.test.js test/unit/hud-bands-layout.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/hudBands.test.js` passes, including "Dwarf Pickpocket · Lvl 3" and every fallback case.
    - `grep -c "HUD-01" src/browser/hudBands.js` is at least 1.
    - `node --test test/unit/hud-bands-layout.test.js test/unit/hp-not-wp.test.js` passes.
  </acceptance_criteria>
  <done>Band 1 names the race and the sub-class, never the parent class in parentheses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A regained charge and a refilled book get rail lines</name>
  <files>src/browser/narrationLines.js, src/browser/eventNarration.js, src/browser/engineAdapter.js, test/unit/book-refill-lines.test.js, test/unit/narrationLinesTable.test.js, test/unit/engineAdapter.test.js</files>
  <read_first>
    - src/browser/narrationLines.js (ORACLE_ONLY and its reasons, LINE_FOR.rationsEaten/wentHungry, linesForAction)
    - src/browser/eventNarration.js (spellChargeRecovered, dayBegan, rationsEaten, wentHungry; 77-05's stampScrollCopyNotes as the stamp pattern)
    - src/browser/engineAdapter.js (dispatch and where 77-05's stamp is applied)
    - engine/movement.js (the 20-square trickle, newDay's fed and unfed branches, maxCharges) - read only
    - src/browser/fightLog.js#fightLogLinesFor and src/browser/rail.js#railCardFor (the two consumers of linesForAction)
    - test/unit/narrationLinesTable.test.js and test/unit/engineAdapter.test.js
  </read_first>
  <behavior>
    - stampBookRefill(events, before, after): returns a new array; a `rationsEaten` with `refilled` gets `books: [{ who, name, have, max }]` for the hero (who "you") and then each party member (by index, same name in before and after) whose spent charges were above 0 before and are 0 after, with have = max = maxCharges of that sheet; every other element is the same object; input never mutated; non-array input gives [].
    - A Magic User at 11 spent of 12 steps across the 100-square boundary with rations: the rail line reads the day's refill with "(12/12)" and the rations clause; the Oracle line carries the same refill clause.
    - The same hero with no rations: no refill claim anywhere; the hunger line keeps "Book stays empty."
    - A Fighter hero with a Magic User member whose book was spent: the line names the member's book and count, not the hero's.
    - A Magic User whose book was already full on a fed day: the rations line has no book clause.
    - Walking 20 squares with a spent charge: a rail line reports the charge with have/max (the spellChargeRecovered line), and spellChargeRecovered is no longer in ORACLE_ONLY.
    - Same-step wanderer: a fed refill day whose wandering check starts a fight: THE FIGHT SO FAR's lines (fightLogLinesFor, event order) show the refill line before the encounter line.
    - Through engineAdapter.dispatch (booted with a spent Magic User one square short of the day boundary): the returned events carry the stamp and the returned html contains the refill clause.
  </behavior>
  <action>
1. Tests first: test/unit/book-refill-lines.test.js with every behaviour above, driven by real engine states (newRun, a forced Magic User or party member, `c.spellsUsed` and `state.steps` set to the boundary, rations set as needed, the move dispatched with applyAction); add the dispatch case to test/unit/engineAdapter.test.js. Watch them fail.
2. eventNarration.js: export pure `stampBookRefill(events, before, after)` (JSDoc: CONTEXT's second report, RULES-15 agreement, the stampScrollCopyNotes precedent). Give `rationsEaten`'s Oracle line a refill clause from `e.books` (the hero's "Your book is full again (12/12)", each member by name), keeping today's wording otherwise.
3. narrationLines.js: remove `spellChargeRecovered` from ORACLE_ONLY; add `LINE_FOR.spellChargeRecovered` (the count as have/max, tone magic, priority other, deadpan); give `LINE_FOR.rationsEaten` the day's refill wording when `e.books` is non-empty (for example "A new day. Your book is full again (12/12).", members named after the hero), followed by the rations clause; leave wentHungry's line as it is. Re-read every ORACLE_ONLY reason against today's HUD and fix any that is stale (dayBegan and floorChanged stay, with accurate reasons).
4. engineAdapter.js#dispatch: capture the state before applyAction and apply stampBookRefill after 77-05's stamp, before narration; update the JSDoc.
5. Re-pin test/unit/narrationLinesTable.test.js where it listed spellChargeRecovered as ORACLE_ONLY (HUD-01/CONTEXT comment), then run `npm test` (the coverage guards, the voice scan and the stale-terms tripwire included).
  </action>
  <verify>
    <automated>node --test test/unit/book-refill-lines.test.js test/unit/engineAdapter.test.js test/unit/narrationLinesTable.test.js test/unit/formatEventsCoverage.test.js test/unit/stale-terms.test.js test/voice/safety-scan.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/book-refill-lines.test.js` passes every behaviour, including the unfed, full-book, member-only and same-step-wanderer cases.
    - `grep -c "export function stampBookRefill" src/browser/eventNarration.js` equals 1 and `grep -c "stampBookRefill(" src/browser/engineAdapter.js` is at least 1.
    - `grep -c "spellChargeRecovered:" src/browser/narrationLines.js` is at least 1 (the LINE_FOR entry).
    - `git diff --quiet <plan-base> -- engine content` exits 0 (presentation only).
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>A regained charge and a refilled book each say so on the rail (and in a fight's lead-in), and nothing claims a refill that did not happen.</done>
</task>

</tasks>

<verification>
- Band 1 reads "Race Sub-class · Lvl N" with every fallback pinned.
- The charge and refill lines appear only when the engine's own events say so, with counts from a pure stamp.
- engine/ and content/ are byte-identical; `npm test` is green.
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause):
  - Band 1 reads, for example, "Dwarf Pickpocket · Lvl 3", with no parent class and no parentheses.
  - As a Magic User with a spent charge, walk 20 squares: a rail line reports the charge coming back with the count.
  - Cross a day boundary with rations and a spent book: the rail (or, if a wandering monster attacks, THE FIGHT SO FAR's first lines) says the book is full again with the count; without rations, it says the book stayed empty.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 78 criterion 1 holds (band 1 reads "Race Sub-class · Lvl N"), and CONTEXT's two added decisions hold: a regained spell charge gets a rail line again, and a new day that refills the book names it before any same-step fight.
</success_criteria>

<output>
Create `.planning/phases/78-hud-dead-state-climb-decisions/78-05-SUMMARY.md` when done. Include the plan base SHA, the ORACLE_ONLY audit (each entry, its reason, kept or changed), the new strings for the voice record, and every re-pinned test.
</output>
