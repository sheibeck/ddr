---
phase: 78-hud-dead-state-climb-decisions
plan: 03
type: execute
wave: 2
depends_on:
  - 78-01
  - 78-02
files_modified:
  - src/browser/hazardCard.js
  - src/browser/rollOdds.js
  - src/browser/rail.js
  - src/browser/mapMarks.js
  - mazeworld.html
  - test/unit/hazard-card.test.js
  - test/unit/rollOdds.test.js
  - test/unit/rail.test.js
  - test/unit/mapMarks.test.js
  - test/unit/shell-map-rail.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/harness/shellSandbox.js
autonomous: true
requirements:
  - CLIMB-01
  - CLIMB-02
must_haves:
  truths:
    - "CLIMB-01: while `S.pendingHazard` is set (out of combat and store), the rail shows ONE decision card for every hero: CLIMB IT (a wall) or LEAP IT (a crevice), USE LADDER / USE ROPE only while the matching tool is carried (read live with hasTool), and TURN BACK, always last; it never auto-clears and a body tap never dismisses it (a decision card, per the card-vs-rail-line ruling)"
    - "The card shows each option's odds in Phase 74's range format (CONTEXT): the cross line comes from src/browser/rollOdds.js#hazardOddsText, which reads engine/movement.js#hazardOdds (78-01, the roll's own faces helpers) and formats only through src/browser/rollRange.js (facesRangeText, dieText, signedText, modsText); a climb reads its per-10-ft range on a d10 with the rock case named when it differs and the 2-or-3-roll note, a leap reads its range from the narrowest to the widest gap for the hero's class, and a live Heights, Bodies-of-water or armour-bulk penalty is named with a U+2212 minus; a case with no winning face reads 'nothing'"
    - "The card says plainly that a fall hurts and that a failed crossing still gets you across (Phase 54's one-and-done), and a carried tool's line says what it does (crosses with no roll, and the tool stays behind)"
    - "The buttons dispatch the engine's own actions through the step path (stepWith: narration, haptics, paint, keep-in-view, the marker glide): CLIMB IT / LEAP IT sends `resolveHazard` cross true, TURN BACK sends `resolveHazard` cross false, USE LADDER / USE ROPE sends the existing `useTool` with the record's tool and dir"
    - "railLocked() covers a pending hazard: a map tap, a keyboard arrow or any other move while the card is up pulses the card instead of stepping (the Joiner/find card rule)"
    - "CLIMB-02: the post-fall retry card is gone: stepWith no longer sets a climb/gorge `window.__mzRail.pending` on fellClimbing/fellInGorge and renderRail has no retry branch, so no card can offer a retry for a square already crossed; the dark card's USE TORCH offer is untouched"
    - "CLIMB-02 idempotency: a second tap on a card button after the first dispatched finds no pending record and dispatches nothing (the card is gone after the repaint, the arm window guards the tap, and the engine no-ops an empty resolveHazard)"
    - "CLIMB-02 concurrency: a relaunch with the card up shows the same card, because the record survives the load (Phase 76 plus 78-01) and the card is derived from S.pendingHazard alone"
    - "The crevice and wall copy in src/browser/mapMarks.js (the MARKS legend and the hold-inspect card that reads it) describes the pre-roll choice and one-and-done: you choose before any roll (climb or leap, a carried tool, or turn back), and a failed roll still gets you across, hurt"
    - "TURN BACK's rail line (78-01's LINE_FOR turnedBack) shows under its own rail family (TURNED BACK) with the wall or crevice icon read from the event's `feat`, like toolUsed and draggedOver"
    - "Every new copy bank (the card's copy) is walked by the standing HP-not-WP and voice scans; deadpan, family-friendly, U+2212 minus, U+2013 ranges"
  artifacts:
    - path: src/browser/hazardCard.js
      provides: "HAZARD_CARD_COPY and hazardCardViewModel(state, deps): title, lines, odds and ordered buttons for the pending hazard"
      contains: "export function hazardCardViewModel"
    - path: src/browser/rollOdds.js
      provides: "hazardOddsText(state, feat): the card's odds strings from the engine's hazardOdds"
      contains: "export function hazardOddsText"
    - path: test/unit/hazard-card.test.js
      provides: "card shape per feat and tool, odds agreement with the engine, no retry card, stale-tap and relaunch cases"
  key_links:
    - from: src/browser/rollOdds.js#hazardOddsText
      to: engine/movement.js#hazardOdds
      via: "the roll's own faces, formatted only through rollRange.js"
      pattern: "hazardOdds\\("
    - from: mazeworld.html (classic: renderRail)
      to: src/browser/hazardCard.js#hazardCardViewModel (through window.__mzRailVM)
      via: "the pending-hazard branch"
      pattern: "hazardCard"
    - from: mazeworld.html (module: window.mzResolveHazard)
      to: engine resolveHazard (78-01)
      via: "stepWith({ type: \"resolveHazard\", cross })"
      pattern: "type: \"resolveHazard\""
  prohibitions:
    - statement: "MUST NOT show odds computed any other way than the roll computes them, or leave out a live penalty"
      requirement_id: CLIMB-01
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT show a retry card, or any card, for a wall or crevice already crossed"
      requirement_id: CLIMB-02
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT offer USE LADDER / USE ROPE when the tool is not carried, or roll any die from the card before a commit"
      requirement_id: CLIMB-01
      category: fairness
      status: resolved
      verification: test
---

<objective>
Put the pre-roll decision on the rail (CLIMB-01, CLIMB-02): stepping toward a wall or crevice shows CLIMB IT / LEAP IT with honest odds, USE LADDER / USE ROPE when carried, and TURN BACK; the old post-fall retry card is gone; the MARKS copy describes the choice and one-and-done.

Purpose: the user's 2026-09-22 report ("it made the roll for me as I entered, but still left a button that showed: Climb It!") and the option-B ruling. The engine half (the pause, `resolveHazard`, `hazardOdds`) landed in 78-01.

Output: a pure card view model, the odds text, the rail wiring, the retired retry card, and the new legend copy.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 78-03 creates or changes.)

- 78-01: the engine pre-roll decision (`resolveHazard`, `hazardOdds`), its narration, the bot's answer, re-pins and readouts
- 78-02: every shell text size follows S/M/L; the slider gesture module and wiring
- **This plan (78-03):** `src/browser/hazardCard.js`; `src/browser/rollOdds.js#hazardOddsText`; the rail's pending-hazard card, `window.mzResolveHazard`, railLocked covering it; the retry card and its `rail.pending` record retired; `RAIL_FAMILY.turnedBack`; the one-and-done wall/crevice copy in `mapMarks.js`; `test/unit/hazard-card.test.js`
- 78-04: the parity exposure guard, the relaunch probe, the Phase 78 declarations
- 78-05: band 1's identity line; the spell-charge and new-day book rail lines
- 78-06: the dead-state lockdown and the FINAL SHEET
- 78-07: the Movement setting and the arrow pad
- 78-08: the stairs fade; the full-bag find card
- 78-09: Hear the next room; the phase close

## Flagged assumptions (surfaced, not silently decided)

- **How the odds read (Claude's discretion on layout and voice, CONTEXT).** The engine rolls the wall kind and the climb height at commit time, and the gap width for a leap, so the card cannot name one exact number without peeking at the dice (forbidden). It shows the honest spread instead: the climb's per-10-ft range (and rock's, when different) with "2 or 3 rolls", and the leap's range from the narrowest to the widest gap for the hero's class.
- **The card's buttons bypass railLocked for their own record** (railLocked now covers the hazard, so the old `mzUseTool` guard would refuse its own button). They dispatch only while `S.pendingHazard` is set; a stale tap does nothing.
- **Three buttons on the rail** (CLIMB IT, USE LADDER, TURN BACK) must fit the Pixel 7 at L; the executor checks the row wraps cleanly (78-02's scale is in by now) and the device check confirms.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-hud-dead-state-climb-decisions/78-CONTEXT.md
@.planning/phases/78-hud-dead-state-climb-decisions/78-01-SUMMARY.md
@.planning/todos/pending/2026-09-22-climb-leap-retry-card-is-stale-under-one-and-done.md

<interfaces>
From 78-01 (engine/movement.js): `state.pendingHazard = { feat, dir, tool }` (tool: `ladder` for climb, `rope` for gorge); `hazardChoice { feat, dir, tool, carried }`; the action `{ type: "resolveHazard", cross: boolean }`; `turnedBack { feat, dir }` with an Oracle line and a LINE_FOR rail line; pure `hazardOdds(state, feat)` (dieN 10; climb cases by wall kind with faces and fall dice and the 2-or-3-roll note; leap rows with `ft` and faces for the hero's class; the named penalties). Read 78-01's SUMMARY for the exact return shape.

mazeworld.html (read the file):
- classic `renderRail()`: the first branch is `if (S.pendingHazard && <retry flag check> && !S.combat && !S.store)`, building `lines` from `window.__mzPendingNarration` or `copy.hazard.wall/crevice` and two buttons (`a-hazard-tool` -> `window.mzUseTool(tool, dir)`, `a-hazard-roll` -> `window.move(dir)`). A later branch `rail.pending && rail.pending.kind === ...` is the post-fall retry card (`mw-rail-climb`, plus `mw-rail-tool` when the tool is carried). `railButtons(actionsEl, buttons)` builds the row; `armEncounterButtons()` arms fresh buttons.
- classic `railLocked()` returns true for a pending Joiner or find, or any `window.__mzRail.pending`.
- module `window.move` (engineMove: hasActiveEncounter, encounterSettled, railLocked pulse, the stair gate, stepNow), `window.mzUseTool` (railLocked guard, then `stepWith({ type: "useTool", tool, dir })`), `stepWith(action)` (dispatchWithNarration, then the `window.__mzRail.pending` computation from fellClimbing/fellInGorge/darknessFell, `window.__mzPendingNarration` from pendingJoiner/pendingFind/an un-retried pendingHazard, paint, keep-in-view, glide).
- module: `window.__mzRailVM = { card, push, clear, lineCard, announcement, copy, holdForCard, dismissKind, isCombatCard }` (extend this object; the bridge NAME set is pinned by test/unit/bridge-registry.test.js, so add no new `__mz` name).
- src/browser/rail.js: `RAIL_COPY.climb.retry`, `RAIL_COPY.hazard` (title, ladder, rope, climb, leap, wall, crevice), `RAIL_FAMILY` (hazardChoice, toolUsed, the climb/leap outcomes), `RAIL_FEATURE_ICON`, and railCardFor's `.feat`-based icon exception for toolUsed and draggedOver.
- src/browser/rollOdds.js: the one module for non-event "right now" odds (74-04); src/browser/rollRange.js: `facesRangeText`, `dieText`, `signedText`, `modsText`. test/unit/roll-sign-consistency.test.js's one-formatter scan fails any src/browser file that defines its own sign formatter.
- src/browser/mapMarks.js#MARKS_LEGEND: `crevice` ("Climb it, or fall, and be grateful for half the fall.") and `wall` ("Climb it on a d10 under your class's number, or fall and eat the difference.") both predate one-and-done and roll-high. 76-02 edited this file (darkness); read it fresh.
- test/unit/hp-not-wp.test.js walks registered copy banks (74-08 pattern); test/voice/safety-scan.test.js scans the voice banks. test/unit/harness/shellSandbox.js wires the real `__mzRailVM` for shell tests.
- The stale-terms tripwire bans retired UI-surface words in comments and test titles.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The card's view model and its honest odds</name>
  <files>src/browser/hazardCard.js, src/browser/rollOdds.js, src/browser/rail.js, test/unit/hazard-card.test.js, test/unit/rollOdds.test.js, test/unit/rail.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - engine/movement.js (hazardOdds and the faces helpers, from 78-01) and .planning/phases/78-hud-dead-state-climb-decisions/78-01-SUMMARY.md
    - src/browser/rollOdds.js and src/browser/rollRange.js
    - src/browser/rail.js (RAIL_COPY.hazard, RAIL_COPY.climb, RAIL_FAMILY, RAIL_FEATURE_ICON, railCardFor's feat-icon exception)
    - test/unit/rollOdds.test.js and test/unit/roll-sign-consistency.test.js (the one-formatter rule)
    - test/unit/hp-not-wp.test.js (how copy banks are registered)
  </read_first>
  <behavior>
    - A Fighter paused at a wall, no ladder, no penalties: title names the wall; the cross button reads CLIMB IT; its odds line reads the climb's range on a d10 per 10 ft (rope/wood 4–10, rock 5–10) with the 2-or-3-roll note; the fall line and the one-and-done line are present; buttons are [CLIMB IT, TURN BACK].
    - The same hero carrying a ladder: buttons are [CLIMB IT, USE LADDER, TURN BACK] in the order the executor fixes and pins (TURN BACK last); the tool line says the ladder crosses with no roll and stays behind.
    - A Magic User paused at a crevice with a rope: LEAP IT reads the range from the narrowest gap (M column 9 faces, 2–10) to the widest (1 face, 10 only), USE ROPE is offered.
    - A Heights-phobic climber in heavy armour: the faces drop by both penalties, the penalties are named with U+2212 (for example "Heights −2, armour −1"), and a case whose faces reach 0 reads "nothing".
    - Agreement: for a matrix of heroes and feats, every range in the card equals `facesRangeText` of the same case's faces from `hazardOdds`, and every penalty equals the engine's own.
    - No pending record, a combat or an open store: the view model is null.
    - TURN BACK's rail family: `railFamilyFor("turnedBack", ...)` returns the TURNED BACK family; railCardFor gives it the wall icon for a climb and the crevice icon for a gorge.
  </behavior>
  <action>
1. Tests first (test/unit/hazard-card.test.js, test/unit/rollOdds.test.js, test/unit/rail.test.js), driven by real engine states (newRun plus a hand-set climb/gorge cell and a `move` through applyAction, so the record is the engine's own). Watch them fail.
2. src/browser/rollOdds.js: add `hazardOddsText(state, feat)` returning the cross line's pieces (range text, die, the roll-count note, the named penalties) built only from `hazardOdds` and rollRange.js; extend the module header's consumer list (78-03).
3. Create src/browser/hazardCard.js (pure, house style, header naming CLIMB-01/02 and the user's ruling): frozen `HAZARD_CARD_COPY` (titles per feat, the button labels CLIMB IT / LEAP IT / USE LADDER / USE ROPE / TURN BACK, the fall line, the one-and-done line, the tool lines; deadpan, family-friendly, HP never WP) and `hazardCardViewModel(state, deps)` returning null or `{ key, feat, dir, title, lines, buttons }` where each button is `{ id, label, act }` with `act` one of cross, tool or back (plus `tool` for the tool button). `deps` carries `hasTool` so the module stays engine-import-light, or import it from engine/derived.js if the module's purity scan allows.
4. src/browser/rail.js: retire `RAIL_COPY.climb` and move the hazard copy the card needs into HAZARD_CARD_COPY (delete `RAIL_COPY.hazard` once nothing reads it, updating rail.test.js); add `RAIL_FAMILY.turnedBack` (icon ⧗, title TURNED BACK, a quiet tone) and give turnedBack the same `.feat`-based icon treatment as toolUsed and draggedOver at railCardFor's lookup site, with comments.
5. Register HAZARD_CARD_COPY in test/unit/hp-not-wp.test.js's walk and voice scan.
  </action>
  <verify>
    <automated>node --test test/unit/hazard-card.test.js test/unit/rollOdds.test.js test/unit/rail.test.js test/unit/hp-not-wp.test.js test/unit/roll-sign-consistency.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/hazard-card.test.js test/unit/rollOdds.test.js test/unit/rail.test.js` passes, including the engine-agreement matrix.
    - `grep -c "export function hazardCardViewModel" src/browser/hazardCard.js` equals 1 and `grep -c "export function hazardOddsText" src/browser/rollOdds.js` equals 1.
    - `grep -c "hazardOdds(" src/browser/rollOdds.js` is at least 1.
    - `node --test test/unit/roll-sign-consistency.test.js test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js` passes (the one-formatter rule holds for the new module).
  </acceptance_criteria>
  <done>A pure view model builds the whole card from the engine's own record and odds.</done>
</task>

<task type="auto">
  <name>Task 2: The card on the rail, the lock, and the retry card retired</name>
  <files>mazeworld.html, test/unit/shell-map-rail.test.js, test/unit/shell-gear-39.test.js, test/unit/harness/shellSandbox.js, test/unit/hazard-card.test.js</files>
  <read_first>
    - mazeworld.html (classic renderRail's hazard branch and the post-fall retry branch, railLocked, railButtons, armEncounterButtons; module window.move, window.mzUseTool, stepWith's pending and narration computations, the `window.__mzRailVM` assignment)
    - src/browser/hazardCard.js (Task 1)
    - test/unit/shell-map-rail.test.js and test/unit/shell-gear-39.test.js (their hazard and retry pins)
    - test/unit/harness/shellSandbox.js (how `__mzRailVM` is wired for shell tests)
  </read_first>
  <action>
1. Module script: import hazardCardViewModel and add it to the `window.__mzRailVM` object as `hazardCard`; add `window.mzResolveHazard(cross)` that does nothing unless `S.pendingHazard` is set and no encounter covers the map, then calls `stepWith({ type: "resolveHazard", cross: cross === true })`; change `window.mzUseTool` to allow its own card's tap (it dispatches only while a pending record for that tool and dir exists) instead of refusing on railLocked. Comments name CLIMB-01 and why.
2. Classic renderRail: the pending-hazard branch now builds title, lines and buttons from `window.__mzRailVM.hazardCard(S)` (with `__mzPendingNarration` lines first, as today); button acts map to `window.mzResolveHazard(true)`, `window.mzUseTool(tool, dir)` and `window.mzResolveHazard(false)`, each through railButtons (guardTap and the arm window keep a double tap safe). Delete the post-fall retry branch.
3. Classic railLocked: add a pending hazard (out of combat and store). stepWith: stop setting a climb/gorge `window.__mzRail.pending` on fellClimbing/fellInGorge (keep the dark torch pending exactly as it is); the pending-narration stash reads `state.pendingHazard` alone. Rewrite the comments that describe the retry card, the tool-only card and "a failed climb holds the party on its square" to the new rule.
4. test/unit/harness/shellSandbox.js: wire the real hazardCard into its `__mzRailVM`. Update test/unit/shell-map-rail.test.js and test/unit/shell-gear-39.test.js pins (with CLIMB-01/02 comments) and add shell pins: renderRail's hazard branch reads `hazardCard`; railLocked covers `pendingHazard`; the post-fall retry branch and its retry button's id no longer exist anywhere in the shell; stepWith sets no climb/gorge pending; mzResolveHazard dispatches `resolveHazard`.
5. Add to test/unit/hazard-card.test.js, through the sandbox or the engine: after a commit that crosses (success or fail-and-cross), no pending record exists and the view model is null (no stale card); a second resolveHazard dispatch is a no-op; a validateSave/rehydrate round trip of a paused state yields the same card (same key, lines and buttons).
  </action>
  <verify>
    <automated>node --test test/unit/hazard-card.test.js test/unit/shell-map-rail.test.js test/unit/shell-gear-39.test.js test/unit/bridge-registry.test.js test/unit/shell-input-guards.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hazardCard" mazeworld.html` is at least 2 (the bridge extension and renderRail's read) and `grep -c 'type: "resolveHazard"' mazeworld.html` is at least 1.
    - test/unit/shell-map-rail.test.js carries a CLIMB-02 pin proving the post-fall retry branch and its retry button id are absent from mazeworld.html, and it passes.
    - `node --test test/unit/hazard-card.test.js test/unit/shell-map-rail.test.js test/unit/shell-gear-39.test.js test/unit/bridge-registry.test.js` passes.
  </acceptance_criteria>
  <done>The decision card shows for every wall and crevice, locks movement until answered, and no retry card survives a crossing.</done>
</task>

<task type="auto">
  <name>Task 3: The MARKS copy describes the choice and one-and-done</name>
  <files>src/browser/mapMarks.js, test/unit/mapMarks.test.js</files>
  <read_first>
    - src/browser/mapMarks.js (MARKS_LEGEND, legendFor; the 76-02 darkness edits)
    - test/unit/mapMarks.test.js (the legend pins)
    - src/browser/tapStep.js#inspectCell (the hold-inspect card reads legendFor)
  </read_first>
  <action>
1. Rewrite MARKS_LEGEND's `crevice` and `wall` descriptions per CONTEXT ("The crevice/wall copy in src/browser/mapMarks.js describes one-and-done"): each says you choose before any roll (climb it or leap it, a carried ladder or rope, or turn back) and that a failed roll still gets you across, hurt. Roll-high phrasing only (no "under your number"), deadpan, family-friendly, HP never WP. Update the table's comment (Phase 78, CLIMB-02).
2. Update test/unit/mapMarks.test.js's legend pins with a CLIMB-02 comment, and add a pin that neither description contains the old retry wording ("or fall" as the whole outcome) or roll-under phrasing ("under your").
3. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/mapMarks.test.js test/unit/tapStep.test.js test/voice/safety-scan.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/mapMarks.test.js` passes with the new copy pins.
    - `grep -c "CLIMB-02" src/browser/mapMarks.js` is at least 1.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The MARKS legend and the hold-inspect card describe the pre-roll choice and one-and-done.</done>
</task>

</tasks>

<verification>
- The card is built from the engine's record and odds; buttons dispatch the engine's own actions; the map is locked while it is up.
- No retry card exists; the MARKS copy matches the rule.
- `npm test` is green; `npm run boot:check` passes (rerun once if it flakes).
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause):
  - Walk into a wall with no ladder: the card shows CLIMB IT with a d10 range and TURN BACK; no dice until CLIMB IT.
  - TURN BACK: the card goes, you are on the same square, and stepping at the wall again shows the card again.
  - Commit and fail: you land on the far side hurt, and no CLIMB IT button is left behind.
  - Carry a ladder (then a rope at a crevice): USE LADDER / USE ROPE appears and crosses with no roll.
  - With the card up, tap the map and press an arrow key: the card pulses; you do not move.
  - Relaunch with the card up: the same card comes back.
  - Hold on a wall and a crevice mark, and open MARKS: the copy describes the choice and one-and-done.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 78 criterion 5 holds end to end: stepping toward a wall or crevice square first shows a decision card (CLIMB IT / LEAP IT, USE LADDER / USE ROPE when carried, TURN BACK) with no dice rolled until the player commits; TURN BACK costs nothing; no stale retry card ever appears after a crossing; the MARKS copy describes one-and-done.
</success_criteria>

<output>
Create `.planning/phases/78-hud-dead-state-climb-decisions/78-03-SUMMARY.md` when done. Include the plan base SHA, the final card copy and odds strings for the voice record, the button order, and every re-pinned test with before and after.
</output>
