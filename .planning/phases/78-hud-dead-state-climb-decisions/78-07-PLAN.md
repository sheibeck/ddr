---
phase: 78-hud-dead-state-climb-decisions
plan: 07
type: execute
wave: 4
depends_on:
  - 78-06
  - 78-02
files_modified:
  - src/browser/settings.js
  - test/unit/settings.test.js
  - src/browser/controls.js
  - test/unit/controls.test.js
  - src/browser/arrowPad.js
  - test/unit/arrow-pad.test.js
  - mazeworld.html
  - test/unit/shell-arrow-pad.test.js
  - test/unit/shell-map-viewport.test.js
  - test/unit/shell-map-invariants.test.js
  - tools/stale-terms.mjs
autonomous: true
requirements:
  - HUD-08
must_haves:
  truths:
    - "HUD-08 (CONTEXT, user 2026-09-25): Settings has a Movement choice, TAP TO MOVE (the default) or ARROWS, and, only while ARROWS is chosen, a Pad choice, BOTTOM LEFT or BOTTOM RIGHT; both live in the one settings record as `movement` ('tap' | 'arrows', default 'tap') and `padSide` ('left' | 'right', default 'right'), validated on write and tolerant on read: a missing, unknown or tampered stored value reads its default, so an unknown movement falls back to tap-to-move"
    - "Arrow mode shows a 4-way pad over the map in the chosen bottom corner; each press calls window.move(dir), the same single choke point a tap step and a keyboard arrow use (the encounter gate, the settle window, the rail lock and the stair gate, then stepNow), so there is no second movement path; one press is one step (no hold-to-repeat)"
    - "In arrow mode a map tap never moves the party: tapStep resolves no step; hold-inspect, drag-to-pan and pinch-to-zoom keep working. Hardware arrow keys and WASD step in both modes, as today"
    - "keepPartyInView treats the pad's on-screen rect as a visible edge (CONTEXT): src/browser/controls.js#keepInViewRect nudges the camera the minimum needed so the party's cell stays at least EDGE_TRIGGER_CELLS from the viewport edges AND from the pad's exposed edges (its top edge and its inner side edge) whenever the party is in the pad's column or row band, so the map scrolls before the party slips under the pad; elsewhere the pad may overlap the map"
    - "HUD-08 adjacency: the pad edge uses keepInViewAxis's own strictness (a party exactly EDGE_TRIGGER_CELLS from an edge does not scroll, closer does); BOTTOM LEFT and BOTTOM RIGHT are mirror images (a pinned mirrored pair of cases)"
    - "HUD-08 empty: with no pad (tap mode, or the pad hidden) keepInViewRect returns exactly what keepInViewAxis gives on each axis (a regression pin), so tap-to-move's camera is unchanged"
    - "HUD-08 ordering: two quick presses are two window.move calls in order, each gated on its own; a press while a rail decision card is up pulses the card (railLocked) and does not step; a press during a marker glide re-aims the glide as a tap step does"
    - "The pad scales with the S/M/L text size (78-02's root scale) with every button at least 48 CSS px at S; each button has a TalkBack label ('Step north', 'Step east', 'Step south', 'Step west'); any press animation honours reduced motion"
    - "The pad is hidden or inert exactly when map taps are: while dead (78-06's lockdown), while combat, a store, the stair overlay or any other over-map encounter is up (hasActiveEncounter), and while a sheet (Settings, MARKS, camp, gear, FINAL SHEET) covers the map; it lives inside the map screen, so other tabs never show it"
    - "The pad never sits under a rail card: while a rail card is shown the pad lifts above the card's top edge (and keepPartyInView reads the lifted rect), and it never covers the card's text or buttons"
    - "Nothing else depends on the pad (the standing ruling): tap-to-move stays the default and complete; switching modes live takes effect at once without a reload"
  artifacts:
    - path: src/browser/settings.js
      provides: "the `movement` and `padSide` fields with allowed values and defaults"
      contains: "padSide"
    - path: src/browser/controls.js
      provides: "keepInViewRect: the stationary-camera rule with the pad as an edge"
      contains: "export function keepInViewRect"
    - path: src/browser/arrowPad.js
      provides: "ARROW_PAD_COPY and the pure pad view model (visibility, side, buttons, labels)"
      contains: "export function arrowPadModel"
    - path: test/unit/shell-arrow-pad.test.js
      provides: "the shell wiring pins: settings rows, the pad markup, window.move presses, tap mode off in arrow mode, the keep-in-view read"
  key_links:
    - from: mazeworld.html (the pad's buttons)
      to: window.move
      via: "the one movement choke point"
      pattern: "window\\.move\\("
    - from: mazeworld.html (classic keepPartyInView)
      to: src/browser/controls.js#keepInViewRect (through window.__mzControls)
      via: "the pad's rect as an edge"
      pattern: "keepInViewRect"
    - from: mazeworld.html (classic tapStep)
      to: window.__mzSettings.movement
      via: "arrow mode turns tap-to-move off"
      pattern: "movement"
  prohibitions:
    - statement: "MUST NOT create a second movement path: every pad press goes through window.move and its gates"
      requirement_id: HUD-08
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT move the party on a map tap in arrow mode"
      requirement_id: HUD-08
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT make any other feature depend on the pad: tap-to-move stays the default and complete"
      requirement_id: HUD-08
      category: values
      status: resolved
      verification: test
---

<objective>
Add the opt-in arrow pad (HUD-08): a Movement setting (TAP TO MOVE by default, or ARROWS in the bottom-left or bottom-right corner), a pad whose every press is one step through the same path a tap step uses, tap-to-move fully off in arrow mode, and a camera that scrolls before the party walks under the pad.

Purpose: the user's 2026-09-25 request ("a setting to choose between click to move and arrow key movement ... as the party approaches the arrow keys we include their position when determining to auto scroll the map"), a deliberate, opt-in reversal of the v1.4/v1.5 tap-only ruling; tap-to-move stays the default.

Output: two settings fields, a pure camera rule, a pure pad view model, the settings rows, the pad overlay and its wiring.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 78-07 creates or changes.)

- 78-01: the engine pre-roll decision (`resolveHazard`, `hazardOdds`), its narration, the bot's answer, re-pins and readouts
- 78-02: every shell text size follows S/M/L; the slider gesture module and wiring
- 78-03: the pre-roll hazard card on the rail, the retry card retired, the one-and-done copy
- 78-04: the parity exposure guard, the relaunch probe, the Phase 78 declarations
- 78-05: band 1's identity line; the spell-charge and new-day book rail lines
- 78-06: the dead-state lockdown and the FINAL SHEET
- **This plan (78-07):** `movement` and `padSide` in `settings.js`; `controls.js#keepInViewRect`; `src/browser/arrowPad.js`; the Movement and Pad settings rows, the `#mw-arrow-pad` overlay, arrow-mode tap handling and the pad-aware keep-in-view in `mazeworld.html`; `test/unit/arrow-pad.test.js`; `test/unit/shell-arrow-pad.test.js`
- 78-08: the stairs fade (it will block the pad with every other input while dark); the full-bag find card
- 78-09: Hear the next room; the phase close

## Flagged assumptions (surfaced, not silently decided)

- **HUD-08 edge probe: adjacency, empty and ordering** are the truths above.
- **Default pad side: BOTTOM RIGHT** (CONTEXT names both choices but no default; right suits a right thumb). One constant to change.
- **The pad lifts above a shown rail card** so a minor rail line never blocks walking; the alternative (the card covering the pad until dismissed) would stop the player after every event. Layout detail at the executor's discretion within that truth.
- **The retired control bar's literals stay retired.** test/unit/shell-map-viewport.test.js and shell-map-invariants.test.js pin that three literals of the old v1.4 control bar never appear in mazeworld.html; the new pad uses new names (`mw-arrow-pad`, a `data-step` attribute), and those tests' titles are reworded to say the OLD control bar is gone while the opt-in pad (HUD-08) exists. The Phase 46 settings pin that the old movement-scheme key never returns stays as it is; the new keys are different names.
- **Canon referral:** a tampered stored settings value is canon input validation, already covered by writeSetting's allowlist and readSettings' tolerant merge (T-04-04); not minted here.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-hud-dead-state-climb-decisions/78-CONTEXT.md
@.planning/todos/pending/2026-09-25-setting-tap-to-move-or-on-screen-arrow-pad.md

<interfaces>
src/browser/settings.js: `SETTINGS_DEFAULTS` (eleven fields today), `ALLOWED_VALUES` (arrays or predicates), `isValidSettingValue`, `readSettings` (merges only known keys with valid values), `writeSetting` (rejects unknown keys and invalid values). test/unit/settings.test.js pins the field list and the Phase 33/46 retired-key pins (built from string fragments; do not spell the retired key or value whole).

src/browser/controls.js: `EDGE_TRIGGER_CELLS` (2) and `keepInViewAxis(camAxis, partyAxis, spanCells)` (rest margin `max(EDGE_TRIGGER_CELLS + 1, span / 3)`, strict `<` edge test, centres when the span is too small). test/unit/controls.test.js pins it.

mazeworld.html (read the file):
- classic `keepPartyInView()` reads `#mw-maze-viewport`'s rect, `partyCentre()`, the camera glide's target, and calls `window.__mzControls.keepInViewAxis` per axis; `window.mzKeepPartyInView` is called after a step, a tab return, an overlay dismiss, a pinch end, a Settings close, a textSize change and a resize.
- classic `tapStep(clientX, clientY)` (hasActiveEncounter, railLocked pulse, screenToCell, resolveStep, then `window.move(dir)`); its comment says tap-to-move is the only movement surface (that sentence carries an allowlisted retired word in tools/stale-terms.mjs ALLOWED; if you rewrite the sentence, delete that ALLOWED entry in the same commit or the rot test fails). `inspectAt` (hold) and the pan/pinch pipeline in `initMazeViewportControls`.
- classic keyboard handler: arrows and WASD call `window.move` (gated on hasActiveEncounter).
- module `window.move` (engineMove) is the single movement choke point; `window.__mzSettings` is the resolved settings bridge; `applySettings`, `renderSettingsSheet` (the `.mw-settings-options[data-setting]` groups and `.mw-settings-opt[data-value]` buttons; the volume group shows only while Sound is On via `hidden`), the settings click handler (`writeSetting`, `applySettings`, `renderSettingsSheet`).
- `window.__mzControls = { screenToCell, resolveTapDirection, classifyPointerGesture, keepInViewAxis }` (extend the object; add no new `__mz` name).
- The rail `#mw-rail` is an absolute overlay at the bottom of `#mw-stage`; renderRail sets `hidden`/`data-shown` and, over combat, a `--mw-rail-lift` custom property.
- test/unit/shell-map-viewport.test.js and test/unit/shell-map-invariants.test.js zero-grep three retired control-bar literals (read them before naming anything).
- 78-06's dead lockdown (hasActiveEncounter counts S.dead; the tab rule) and 78-02's root `--mw-text-scale`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The settings fields, the pad-aware camera rule and the pad model</name>
  <files>src/browser/settings.js, test/unit/settings.test.js, src/browser/controls.js, test/unit/controls.test.js, src/browser/arrowPad.js, test/unit/arrow-pad.test.js</files>
  <read_first>
    - src/browser/settings.js and test/unit/settings.test.js (field list, validation, the retired-key pins)
    - src/browser/controls.js and test/unit/controls.test.js (keepInViewAxis and EDGE_TRIGGER_CELLS)
    - src/browser/tapStep.js (the house style for a pure shell module: frozen constants, no DOM)
  </read_first>
  <behavior>
    - settings: defaults `movement: "tap"`, `padSide: "right"`; writeSetting accepts "tap"/"arrows" and "left"/"right" and rejects anything else (the stored value is unchanged); readSettings of a blob with `movement: "hover"` or `padSide: 7` returns the defaults for those keys; an old blob with neither key reads the defaults; the retired-key pins still pass.
    - keepInViewRect(cam, party, span, obstacle) with obstacle null equals `{ x: keepInViewAxis(cam.x, party.x, span.x), y: keepInViewAxis(cam.y, party.y, span.y) }` for a grid of inputs.
    - With a bottom-right pad obstacle (in cells, relative to the viewport's top-left) and the party walking down inside the pad's column: the camera moves once the party is closer than EDGE_TRIGGER_CELLS to the pad's top edge, not at exactly EDGE_TRIGGER_CELLS; walking down OUTSIDE the pad's column only reacts to the viewport's own bottom edge; walking right inside the pad's row band reacts to the pad's left edge; the bottom-left case mirrors every one of these.
    - The rule never snaps to centre when a minimal nudge suffices and returns the input camera unchanged when no edge is near.
    - arrowPadModel(settings, ctx): visible only when `settings.movement === "arrows"` and ctx says the map accepts input (not dead, no encounter, no sheet); `side` follows padSide; four buttons N, E, S, W each with its TalkBack label ("Step north" and so on) from frozen ARROW_PAD_COPY; unknown settings give the tap-mode model (hidden).
  </behavior>
  <action>
1. Tests first in test/unit/settings.test.js, test/unit/controls.test.js and a new test/unit/arrow-pad.test.js. Watch them fail.
2. settings.js (per CONTEXT HUD-08): add `movement` and `padSide` to SETTINGS_DEFAULTS and ALLOWED_VALUES, update the header's field count and the defaults comment (Phase 78, HUD-08, the opt-in reversal of the tap-only ruling). Do not reuse the retired Phase 46 key or its value.
3. controls.js: export `keepInViewRect(cam, party, span, obstacle)` (all in cells; obstacle `{ left, top, right, bottom }` or null) built on keepInViewAxis's own rest and strictness; JSDoc names HUD-08 and "the pad is an edge".
4. Create src/browser/arrowPad.js (pure, house style): `ARROW_PAD_COPY` (the four labels and the pad's own accessible name), `ARROW_PAD_DIRS` (N, E, S, W with their grid placement), `arrowPadModel(settings, ctx)`. Name nothing with the retired control-bar literals.
  </action>
  <verify>
    <automated>node --test test/unit/settings.test.js test/unit/controls.test.js test/unit/arrow-pad.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/settings.test.js test/unit/controls.test.js test/unit/arrow-pad.test.js` passes, including the tolerant-load, null-obstacle regression, strict-edge and mirrored cases.
    - `grep -c "padSide" src/browser/settings.js` is at least 2 and `grep -c "export function keepInViewRect" src/browser/controls.js` equals 1.
    - `grep -c "export function arrowPadModel" src/browser/arrowPad.js` equals 1.
  </acceptance_criteria>
  <done>The setting, the camera rule and the pad model exist and are proven in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: The Movement rows, the pad on the map, and tap-to-move off in arrow mode</name>
  <files>mazeworld.html, test/unit/shell-arrow-pad.test.js, test/unit/shell-map-viewport.test.js, test/unit/shell-map-invariants.test.js, tools/stale-terms.mjs</files>
  <read_first>
    - mazeworld.html (the settings sheet rows and renderSettingsSheet, the settings click handler, applySettings; the map screen markup around `#mw-maze-viewport`; tapStep, inspectAt, initMazeViewportControls; the keyboard handler; renderRail's show/hide; the module `window.move`)
    - test/unit/shell-map-viewport.test.js and test/unit/shell-map-invariants.test.js (the retired literals)
    - tools/stale-terms.mjs (the ALLOWED entry that matches tapStep's comment)
    - src/browser/arrowPad.js (Task 1)
  </read_first>
  <action>
1. Settings sheet: add a Movement row (`data-setting="movement"`: TAP TO MOVE, ARROWS) and a Pad row (`data-setting="padSide"`: BOTTOM LEFT, BOTTOM RIGHT) shown only while ARROWS is chosen (the volume rows' `hidden` pattern in renderSettingsSheet). The existing click handler already writes, applies and re-renders; after a movement or padSide write, refresh the pad and call `window.mzKeepPartyInView`.
2. Markup and CSS: a `#mw-arrow-pad` container inside the map screen over `#mw-maze-viewport`, four `<button type="button">` children (a `data-step` attribute carrying N/E/S/W, an aria-label from ARROW_PAD_COPY), a cross layout in the chosen corner (`data-side="left|right"`), sized with 78-02's scale (each button `max(48px, calc(<rem> * var(--mw-text-scale)))`), clear of the safe-area inset, above the canvas and below the rail and sheets in z-order; no animation unless motion is allowed.
3. Wiring (module or classic, following where the neighbouring code lives): render the pad from `arrowPadModel(window.__mzSettings, ctx)` on applySettings, on paint (so a death, a fight, the stair overlay or an open sheet hides or disables it) and on tab switches; each button's click calls `window.move(dir)`; pointer events on the pad never reach the viewport's pan/tap pipeline. While the rail is shown, lift the pad above it (a custom property set where renderRail writes `data-shown`).
4. tapStep: in arrow mode, a tap resolves no step (return before resolveStep's dispatch) and says nothing; hold-inspect, pan and pinch are unchanged. Rewrite the comment that calls tap-to-move the only movement surface to name both modes (HUD-08); since that line is matched by a tools/stale-terms.mjs ALLOWED entry, delete that entry in the same change (the rot test would fail otherwise) and do not write the retired word again.
5. Tests: create test/unit/shell-arrow-pad.test.js (source and sandbox pins: the two rows, the pad markup with `data-step` and aria-labels, each press calls window.move, the pad is hidden while dead or in combat, tapStep does not dispatch in arrow mode but still does in tap mode, keyboard arrows still move in both modes); reword the retired-control-bar test titles in shell-map-viewport.test.js and shell-map-invariants.test.js to say the OLD control bar stays gone while the opt-in pad (HUD-08) exists, keeping their zero-greps.
  </action>
  <verify>
    <automated>node --test test/unit/shell-arrow-pad.test.js test/unit/shell-map-viewport.test.js test/unit/shell-map-invariants.test.js test/unit/stale-terms.test.js test/unit/bridge-registry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mw-arrow-pad" mazeworld.html` is at least 2 and `grep -c 'data-setting="movement"' mazeworld.html` equals 1 and `grep -c 'data-setting="padSide"' mazeworld.html` equals 1.
    - `node --test test/unit/shell-arrow-pad.test.js test/unit/shell-map-viewport.test.js test/unit/shell-map-invariants.test.js test/unit/stale-terms.test.js test/unit/bridge-registry.test.js` passes (the retired literals still count zero).
  </acceptance_criteria>
  <done>ARROWS shows a pad whose presses walk the party through the normal step path, and map taps no longer move in arrow mode.</done>
</task>

<task type="auto">
  <name>Task 3: The camera scrolls before the party walks under the pad</name>
  <files>mazeworld.html, test/unit/shell-arrow-pad.test.js</files>
  <read_first>
    - mazeworld.html (classic keepPartyInView, partyCentre, positionCanvas, CELL; where `window.__mzControls` is assigned)
    - src/browser/controls.js#keepInViewRect (Task 1)
  </read_first>
  <action>
1. Add keepInViewRect to the `window.__mzControls` object (no new bridge name).
2. keepPartyInView: in arrow mode with the pad shown, read `#mw-arrow-pad`'s bounding rect, convert it to cells relative to `#mw-maze-viewport`'s rect (divide by CELL), and use keepInViewRect with that obstacle; otherwise keep calling keepInViewAxis per axis exactly as today (the tap-mode path is unchanged). Keep the glide-target and positionCanvas behaviour. Comment: HUD-08, the pad is an edge.
3. Call mzKeepPartyInView after the pad's side or visibility changes.
4. Add sandbox pins to test/unit/shell-arrow-pad.test.js: in arrow mode keepPartyInView calls keepInViewRect with an obstacle derived from the pad's rect; in tap mode it calls keepInViewAxis and never keepInViewRect.
5. Run `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-arrow-pad.test.js test/unit/controls.test.js test/unit/shell-map-viewport.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "keepInViewRect" mazeworld.html` is at least 2 (the bridge extension and keepPartyInView's call).
    - `node --test test/unit/shell-arrow-pad.test.js` passes, including the arrow-mode and tap-mode camera pins.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>Walking toward the pad scrolls the map before the party slips under it; tap mode's camera is unchanged.</done>
</task>

</tasks>

<verification>
- The two settings fields validate and load tolerantly; ARROWS shows the pad in the chosen corner; each press is one window.move.
- Map taps do not move in arrow mode; hold, pan, pinch and keyboard arrows still work.
- The camera treats the pad as an edge; tap mode's camera is byte-identical in behaviour.
- The retired control bar's literals stay at zero; `npm test` is green; `npm run boot:check` passes (rerun once if it flakes).
  <human-check>
  Pixel 7, at milestone close (deferred-UAT protocol, no mid-run pause):
  - Settings: switch Movement to ARROWS and back live; the Pad row appears only for ARROWS.
  - The pad in BOTTOM LEFT and in BOTTOM RIGHT: each press takes one step; tapping the map does nothing (holding still inspects, dragging still pans).
  - Walk toward the pad: the map scrolls before the party slips under it, in both corners.
  - A rail line appears: the pad stays above it and keeps working.
  - The pad is gone or inert while dead, in a fight, at the stair prompt and while Settings or MARKS is open.
  - At text size S the pad buttons are still easy to hit; at L they grow; TalkBack reads "Step north" and so on.
  </human-check>
</verification>

<success_criteria>
ROADMAP Phase 78 criterion 7 holds: a Movement setting switches between tap-to-move (default) and an on-screen arrow pad in the chosen bottom corner; in arrow mode map taps never move the party, and the map auto-scrolls so the party never walks under the pad.
</success_criteria>

<output>
Create `.planning/phases/78-hud-dead-state-climb-decisions/78-07-SUMMARY.md` when done. Include the plan base SHA, the pad's size at S/M/L, the ALLOWED entry removed from tools/stale-terms.mjs, and every re-pinned test.
</output>
