---
phase: 42-flee-retune-consolidated-balance-close
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - tools/lib/tuning-bot.mjs
  - test/unit/bot-tactics.test.js
  - test/unit/tuning-bot.test.js
autonomous: true
requirements: [BAL-01, BAL-02]
tags: [tuning-bot, abilities, items, loot, harness, no-stall, deferred-uat]

must_haves:
  truths:
    - "In combat a Fighter/Thief bot uses a READY ability by the SAME class-driven policy the engine's Joiners follow (`pickMemberAbility`: round-1 opener; damage-tagged while the target is above half hp, Last Stand only under the death-panic line; defensive-tagged while the hero is below half hp), and a once-a-fight foe-targeted ability is aimed at the hardest live foe (BAL-01 second half, CONTEXT 'Abilities (Phase 38)')"
    - "The bot pops a Speed/Strength/Enlarge potion (or a ready worn Cloak of Speed) at round 1 of a hard fight (a kit-bearing foe or any live foe of level ≥ 3), quaffs a bag Healing/Xtra Healing potion below the flee threshold BEFORE it decides to flee, fires a worn staff with charges in combat (freeze/weaken/stone/fire/gas on ≥ 2 live foes; dome/heal when hurt), lights a torch when in the dark, and answers a pending hazard with the matching tool (CONTEXT 'Items (Phase 39)')"
    - "The bot never dispatches an action the engine would REFUSE without a state change (an unowned/cooling ability, a Death potion, a non-heal item as a Pilfer, a bagged activatable under the worn model, a torch while lit, a staff with no charges) — every pick is guarded by the engine's own read-only predicates AND `observe` blocks a refused key for the rest of the encounter/floor as a Rule-1 safety net (no stall, ever)"
    - "The bot takes the victory loot pile it has ignored since v1.3 (`takeAllLoot`; `leaveAllLoot` once the bag is full) and plays the shipped game's run rules (`newRun` with `storeRoll: true` and `wornSlots: true`) so the AFTER matrix measures the game players get — recorded as `RUN_FLAGS` and, from Plan 03, in every report's `meta.runFlags`"
    - "The `Bot:` parameter line and `BOT_DEFAULTS` keys it prints are byte-identical to the BEFORE pin's (`docs/class-pass/v15-before.json` meta.bot) — new tactics constants live in `BOT_TACTICS`, never in the line — so Plan 04's parameter-parity gate against the BEFORE pins passes"
    - "`decideAction` stays pure (the existing purity test still passes); the ONE new write-path bypass — `playRun` setting `state.combat.target` before a `useAbility` dispatch — mirrors the shell's own foe-card tap (`S.combat.target = i`) and is documented as harness-only"
  artifacts:
    - "tools/lib/tuning-bot.mjs: BOT_TACTICS, RUN_FLAGS, hardFight(state), hardestFoeIndex(state), chooseAbility(state, ctx), chooseCombatItem(state, ctx), chooseFieldItem(state, ctx), decideAction steps (a0)/(b2)/(b3)/(h2)/(loot)/(torch), makeBotContext abilityBlocked/itemBlocked, observe's abilityRefused/useRefused/lootTaken/lootLeft handling, playRun's newRun RUN_FLAGS + target write"
    - "test/unit/bot-tactics.test.js (new): the policy matrix, the guards, the no-stall proof"
  key_links:
    - "ABILITY_BY_ID (content) + isReady(c, `ability:${key}`) (engine/effects.js) + DEATH_PANIC_THRESHOLD -> chooseAbility -> { type: 'useAbility', key, target? } -> playRun writes state.combat.target -> applyAction useAbility"
    - "itemReady/activationFor/itemEffectActive/inDark/slotFor (engine, read-only) -> chooseCombatItem/chooseFieldItem -> { type: 'useItem', i } or { type: 'useItem', slot } (never a bag index for a slot item once c.worn exists)"
    - "observe: abilityRefused -> ctx.abilityBlocked.add(key); useRefused -> ctx.itemBlocked.add(label); bagFull -> ctx.findFull; encounterStarted/floorChanged clear the blocks"
---

<objective>
Teach the tuning bot (`tools/lib/tuning-bot.mjs`) the Phase 38 abilities and the Phase 39 items — by the same class-driven policies the engine's Joiners already follow — plus the two things it needs to have items at all: taking the victory loot pile (ignored since v1.3) and playing the shipped game's run rules (`storeRoll`/`wornSlots`). Every new pick is guarded so the engine never refuses it without a state change, with `observe`-side blocks as the Rule-1 safety net, and a forced-cell no-stall proof.

Purpose: BAL-01's second half ("the tuning bot is taught to use the new abilities, spells, items and tools before the AFTER run") — without this, Plan 04's consolidated AFTER matrix would measure abilities and gear nobody uses.

Output: the extended bot module, `test/unit/bot-tactics.test.js`, an extended `test/unit/tuning-bot.test.js`, 42-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-CONTEXT.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-01-SUMMARY.md
@.planning/phases/38-melee-active-abilities/38-04-SUMMARY.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-02-SUMMARY.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-04-SUMMARY.md
@tools/lib/tuning-bot.mjs
@engine/abilities.js
@engine/items.js
@engine/combat.js
@engine/derived.js
@engine/effects.js
@engine/state.js
@content/abilities.js
@content/activations.js
@content/potions.js
@content/tools.js
@test/unit/tuning-bot.test.js
@test/unit/bot-buy-policy.test.js
@docs/ABILITIES.md
@docs/GEAR-BALANCE.md
@docs/GEAR-SLOTS.md
</context>

<standing_rules>
- The bot talks to the engine through `applyAction`/`newRun`/`makeRng` plus READ-ONLY helpers with an existing precedent. New read-only imports allowed here: `isReady` (`engine/effects.js`), `itemReady`/`toolIndex` (`engine/items.js`), `inDark`/`itemEffectActive`/`slotFor`/`activationFor`/`DEATH_PANIC_THRESHOLD` (`engine/derived.js`), `ABILITY_BY_ID`/`TARGETED_KINDS`-equivalent kinds (`content`/`engine/items.js`). The ONE new write-path bypass (the `state.combat.target` assignment in `playRun`) is documented next to `forceParty` as the second harness-only bypass, mirroring `mazeworld.html`'s foe-card tap handler; `decideAction` itself never mutates.
- `botLine(opts)` output and the `BOT_DEFAULTS` keys it reads are FROZEN: `docs/class-pass/v15-before.json` meta.bot must equal the AFTER's (Plan 04's parity gate, `tools/class-pass-diff.mjs` PARITY_FIELDS includes `bot`). New numbers go in `export const BOT_TACTICS` (documented in the ledger by Plan 03).
- Free actions loop: `useItem` in combat never triggers a foe turn, and every refusal (`useRefused`, `abilityRefused`) returns with NO state change — a bot that re-picks a refused action loops to `maxActions`. Every pick must be guarded by the engine's own predicates AND blocked on refusal in `observe` (the HARN-02 Rule-1 pattern: `fleeBlocked`/`strikeBlocked`).
- Deferred UAT: no device steps. Verification agents OFF: the LAST task runs this plan's gate (`npm test` `# fail 0`, master hash) and records the numbers. `src/browser` is not touched — no `npm run build:www` needed.
- Commits per task: `feat(42-02): …` / `test(42-02): …`, trailers `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
See 42-01-PLAN.md's phase-wide list. This plan creates: `BOT_TACTICS`, `RUN_FLAGS`, `hardFight`, `hardestFoeIndex`, `chooseAbility`, `chooseCombatItem`, `chooseFieldItem`, the decideAction steps (a0) heal-before-flee, (b2) round-1 buff, (b3) staff, (h2) ability, the loot-pile and torch field steps, `ctx.abilityBlocked`/`ctx.itemBlocked`, the `observe` handlers, the `playRun` target write and `RUN_FLAGS` pass-through; `test/unit/bot-tactics.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Abilities — chooseAbility mirrors pickMemberAbility, hardest-foe targeting for once-a-fight abilities, refusal blocks, purity kept</name>
  <files>tools/lib/tuning-bot.mjs, test/unit/bot-tactics.test.js</files>
  <read_first>
    - engine/combat.js lines 1501-1536 (`pickMemberAbility(sheet, ally, round, target)` — the policy to MIRROR: ready = owned ∩ same class ∩ isReady; round-1 opener; damage when `target.wp > target.maxWP / 2` with Last Stand's `ally.wp <= ally.maxWP * DEATH_PANIC_THRESHOLD` gate; defensive when `ally.wp < ally.maxWP / 2`; first match in `sheet.abilities` order) and 87-110 (`liveFoes`, `normalizeTarget`)
    - engine/abilities.js lines 120-175 (`useAbility`'s refusal ladder: notFought/unknown/notInCombat/cooldown/noTarget/notLowEnough — the exact conditions the bot must pre-empt) and content/abilities.js (the 20-row catalog: `cls`, `cd` ("fight" = once-a-fight), `target`, `tag`)
    - engine/effects.js lines 200-224 (`remaining`, `isReady(c, id)`), engine/derived.js line 24 (`DEATH_PANIC_THRESHOLD = 0.25`)
    - tools/lib/tuning-bot.mjs lines 476-620 (`decideAction`'s in-combat chain (a)-(i) and the pending-state chain), 400-420 (`makeBotContext`), 720-745 (`observe`), 776-825 (`playRun` loop — where the target write goes), 24-35 (imports)
    - test/unit/tuning-bot.test.js lines 84-155 (`mkState`/`mu`/`fight` helpers — copy the pattern into the new file), 663-675 (the observe-block test shape), 751-775 (the purity test — must still pass)
    - test/unit/party-abilities.test.js (the 9-test policy matrix for pickMemberAbility — mirror its cases for the hero)
    - mazeworld.html line 6257 (`S.combat.target = i` — the shell's own target write the harness bypass mirrors)
  </read_first>
  <behavior>
    - chooseAbility(state, ctx) returns null for a Magic User, out of combat, or with `c.abilities` empty/absent; returns null when every owned ability is on cooldown (a `c.timers["ability:kata"]` cooldown record) or in `ctx.abilityBlocked`
    - Round 1 with a ready opener-tagged ability (e.g. `pommelStrike`) → `{ key: "pommelStrike" }` even when a damage ability is also ready; round 2, target above half hp, `kata` ready → `{ key: "kata" }`; target below half hp and hero below half hp with `brace` ready → `{ key: "brace" }`; target below half and hero above half with only damage/defensive abilities → null (falls through to attack); `lastStand` is skipped while `c.wp > c.maxWP * 0.25` and picked at/below it when the target is above half
    - A once-a-fight (`cd === "fight"`) FOE-targeted ability (`mark`, `hamstring`, `cutpurse`, `lastStand`) carries `target` = `hardestFoeIndex(state)`: the live foe with the highest `lvl`, ties broken by higher `wp`, then the lowest index; self-targeted or cooldown-only abilities carry no `target`
    - decideAction in combat (Fighter, round 1, opener ready, full hp, no potions) returns `{ type: "useAbility", key: "pommelStrike" }`; with the target field present when the pick is once-a-fight foe-targeted; the ability step sits AFTER the sing step and BEFORE the attack fallback, and never fires for a Magic User (a Sorcerer with charges still gets its castSpell)
    - observe: an `abilityRefused { key }` event adds `key` to `ctx.abilityBlocked`; `encounterStarted` clears the set; the next decideAction skips that key
    - playRun applies `action.target` to `state.combat.target` before dispatching `useAbility` and strips the field from the dispatched action (validateAction only requires `key`); a playRun over a forced Fighter cell (`force: { cls: "Fighter", sub: "Knight", race: "Human" }`, seed 1, maxActions 1000) records at least one `abilityUsed` event and is not stuck
    - decideAction never mutates its state argument on any new branch (deepStrictEqual before/after, extending the existing purity test's coverage to a state with abilities and timers)
  </behavior>
  <action>
    A. `tools/lib/tuning-bot.mjs` imports: add `isReady` from `../../engine/effects.js`, `DEATH_PANIC_THRESHOLD` from `../../engine/derived.js`, `ABILITY_BY_ID` from `../../content/index.js`. Add `export const BOT_TACTICS = Object.freeze({ hardFoeLvl: 3, staffMinFoes: 2, dotToughMargin: 1, mapBankRatio: 0.5 })` (JSDoc: Phase 42 (BAL-01 second half) — the tactics constants, Claude's discretion, NOT part of the `Bot:` line so the BEFORE/AFTER parameter parity holds; transcribed into docs/CLASS-PASS.md by Plan 03; `dotToughMargin`/`mapBankRatio` are consumed by Plan 03's spell rules).

    B. `export function hardestFoeIndex(state)`: over `state.combat.foes`, among `alive` entries pick the highest `lvl`, tie → highest `wp`, tie → lowest index; returns `null` with no live foe. `export function chooseAbility(state, ctx)`: null unless `state.combat` and `c.cls` is Fighter or Thief; `owned = Array.isArray(c.abilities) ? c.abilities : []`; `ready = owned.filter(id => ABILITY_BY_ID[id] && ABILITY_BY_ID[id].cls === c.cls && isReady(c, \`ability:${id}\`) && !ctx.abilityBlocked.has(id)).map(id => ABILITY_BY_ID[id])`; target = `C.foes[C.target]` if alive else `liveFoes(state)[0]`; then EXACTLY pickMemberAbility's three branches with `sheet = ally = c` (opener at round 1; damage when `target.wp > target.maxWP / 2`, Last Stand only when `c.wp <= c.maxWP * DEATH_PANIC_THRESHOLD`; defensive when `c.wp < c.maxWP / 2`); result `{ key: meta.id }` plus `target: hardestFoeIndex(state)` when `meta.cd === "fight" && meta.target === "foe"`. JSDoc cites engine/combat.js#pickMemberAbility as the mirrored policy and CONTEXT "once-a-fight abilities on the hardest foe".

    C. `decideAction`: insert step (h2) between the scoring table (h) and the attack fallback (i): `const ab = chooseAbility(state, ctx); if (ab) return ab.target === undefined ? { type: "useAbility", key: ab.key } : { type: "useAbility", key: ab.key, target: ab.target };`. Update the JSDoc chain list.

    D. `makeBotContext`: add `abilityBlocked: new Set()`, `itemBlocked: new Set()` (Task 2 fills the second). `observe`: `abilityRefused` → `ctx.abilityBlocked.add(e.key)`; `encounterStarted` also clears both sets (`.clear()`).

    E. `playRun`: before `applyAction`, `if (action.type === "useAbility" && Number.isInteger(action.target) && state.combat) { state.combat.target = action.target; }` then dispatch `{ type: "useAbility", key: action.key }`. Comment: the SECOND harness-only write-path bypass (after forceParty) — mirrors mazeworld.html's foe-card tap (`S.combat.target = i`), a presentation-layer selection, never an rng-bearing mutation; `decideAction` stays pure.

    F. `test/unit/bot-tactics.test.js` (new; copy `mkState`/`fight` helpers, add `fighter(over)`/`thief(over)` sheet helpers with `abilities`, `timers: {}`): every `<behavior>` bullet; the no-stall bullet uses `playRun(1, { ...BOT_DEFAULTS, maxActions: 1000, force: { cls: "Fighter", sub: "Knight", race: "Human" } }, onStep)` and counts `abilityUsed` events (if seed 1 never fights before dying, try seeds 1..5 and assert at least one run used an ability — record which seed in the test comment, measured not guessed). Extend the purity test in `test/unit/tuning-bot.test.js` (line ~751) with a Fighter state carrying `abilities: ["kata", "brace"]` and `timers: {}` in round 2.

    Run `node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js test/unit/bot-buy-policy.test.js` — green. Commit `feat(42-02): bot uses abilities by the Joiner policy (opener/damage/defensive), once-a-fight on the hardest foe, refusal blocks` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "export function chooseAbility" tools/lib/tuning-bot.mjs` is 1; `grep -c "export function hardestFoeIndex" tools/lib/tuning-bot.mjs` is 1; `grep -c "export const BOT_TACTICS" tools/lib/tuning-bot.mjs` is 1
    - `grep -c "abilityBlocked: new Set()" tools/lib/tuning-bot.mjs` is 1; `grep -c 'e.type === "abilityRefused"' tools/lib/tuning-bot.mjs` is 1
    - `grep -c "state.combat.target = action.target" tools/lib/tuning-bot.mjs` is 1
    - `node -e "import('./tools/lib/tuning-bot.mjs').then(m=>{const l=m.botLine({...m.BOT_DEFAULTS,seeds:40,workers:4,maxActions:5000});const b=require('./docs/class-pass/v15-before.json').meta.bot;process.exit(l===b?0:1)})"` exits 0 (the Bot: line is byte-identical to the BEFORE pin)
    - `node --test test/unit/bot-tactics.test.js` exits 0 with at least 10 tests; `node --test test/unit/tuning-bot.test.js test/unit/bot-buy-policy.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js test/unit/bot-buy-policy.test.js</automated>
  </verify>
  <done>A melee bot uses its kit exactly like a Joiner would, aims its once-a-fight tricks at the hardest foe, can never loop on a refused ability, and the Bot: line is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Items — heal before flee, buffs before a hard fight, worn staves in combat, torch in the dark, the loot pile, and the shipped run rules</name>
  <files>tools/lib/tuning-bot.mjs, test/unit/bot-tactics.test.js</files>
  <read_first>
    - engine/items.js lines 1006-1019 (`itemReady`, `TARGETED_KINDS`), 1111-1200 (`useItem`'s ref forms `{ slot }` vs bag index and its refusal ladder: pending → wrongClass (staff, non-MU) → notWorn (a slot item addressed by bag index while `c.worn` exists) → pilfer (non heal/full) → combatOnly (targeted kind outside combat) → notDark (torch while `!inDark`) → cooldown/recharging), 1240-1372 (the kind switch: heal/full/speed/strength/enlarge/light/dome/freeze/weaken/stone/fire/gas/heal-staff; potions/tools consumed; items are FREE actions — no foe turn), 949-1000 (`takeAllLoot` — refused items stay in the pile with ONE `bagFull`; `leaveAllLoot`), 182-195 (`toolItem`/`toolIndex`; a torch is `{ kind: "tool", tool: "torch", use: "light" }`)
    - engine/derived.js lines 141-160 (`slotFor`), 330-395 (`activationFor`, `itemEffectActive(c, kind)` — kinds `haste`/`might`/`lit`/`invis`), 722-726 (`inDark`)
    - content/potions.js (potion rows: `n`, `eff` → the bag item's `eff2`: heal/poison/speed/full/strength/disease/enlarge/acute/death…; a found potion is `{ kind: "potion", n: "Speed potion (yellow)", eff2: "speed", uses: 1 }`), content/treasure-tables.js lines 73-140 (cloaks with `use: "haste"` = Cloak of Speed; staves `use` kinds and `act.charges`)
    - engine/state.js lines 132-171 (`newRun`'s `storeRoll`/`wornSlots` options — both `false` for every bot call today; `wornSlots: true` creates `c.worn` and wears the Thief's starting cloak; `storeRoll: true` enables the depth-rolled store stock), docs/GEAR-SLOTS.md lines 228-232 and 273-275 (the deferred "Phase 42 bot extension: `{ wornSlots: true }`")
    - engine/items.js lines 380-390 (`autoWearSlot` — an empty-slot item auto-wears on take; otherwise it stows), engine/death.js lines 85-100 (`forfeitLoot` — the pile is forfeited on flee/death; it never blocks movement, which is why the bot has ignored it)
    - tools/lib/tuning-bot.mjs decideAction steps (a)/(b) and the out-of-combat chain (pendingJoiner → pendingFind → pendingHazard → store → potion → camp → summon → scroll → move)
    - test/unit/tuning-bot.test.js lines 346-364 (pending-prompt test shape), lines 210-224 (potion/flee ordering test)
  </read_first>
  <behavior>
    - hardFight(state): true when `liveFoesHaveAbilities(state)` or any live foe has `lvl >= BOT_TACTICS.hardFoeLvl`; false for two level-1 plain foes
    - chooseCombatItem(state, ctx) (combat only, after Fight!) returns `{ action, reason }` or null, where `action` is the useItem dispatch and `reason` is one of `"heal"` / `"buff"` / `"staff"`: (1) below the flee line (`ratio < fleeAt`) with a bag potion whose `eff2` is `heal` or `full` → `{ action: { type: "useItem", i }, reason: "heal" }` (Xtra Healing preferred over Healing); (2) at round 1 of a hard fight, a bag potion with `eff2` in speed/strength/enlarge whose activation kind (`activationFor(it).kind`: haste/might) is not already active per `itemEffectActive` → useItem by bag index, reason `"buff"` (speed first, then strength, then enlarge); a worn `c.worn.cloak` with `use === "haste"` that `itemReady` accepts → `{ action: { type: "useItem", slot: "cloak" }, reason: "buff" }`; (3) a Magic User with a worn `c.worn.staff` that `itemReady` accepts: a targeted kind (freeze/weaken/stone/fire/gas) when `liveFoes(state).length >= BOT_TACTICS.staffMinFoes`, `dome` when `ratio < potionThreshold` and `!c.ward`, staff `heal` when `ratio < potionThreshold` → `{ action: { type: "useItem", slot: "staff" }, reason: "staff" }`; null otherwise
    - Guards: a `death` potion is never chosen; a Pilfer never gets a non-heal/full item; a slot item is never addressed by bag index while `c.worn` exists (a legacy state without `c.worn` may use a bagged staff by index — keep both paths, the run flags make the worn path the live one); a torch/staff/cloak in `ctx.itemBlocked` (by label) is skipped; a staff with `charges === 0` is skipped
    - decideAction ordering: (a0) the heal-potion pick precedes the flee/parley check (a) — a hero at 20% hp with a Healing potion in the bag drinks instead of fleeing; (b2)/(b3) sit after the generic `drinkPotion` step (b) and before talk-first (c)
    - chooseFieldItem(state, ctx) (out of combat): a bag torch (`kind === "tool" && use === "light"`) while `inDark(state)` and `!itemEffectActive(c, "lit")` → `{ type: "useItem", i }`; null otherwise (rope/ladder stay on the existing `pendingHazard` handler)
    - Loot: `state.pendingLoot` non-empty → `{ type: "takeAllLoot" }` unless `ctx.findFull` → `{ type: "leaveAllLoot" }`; this step precedes `pendingFind`; observe: `lootTaken`/`lootLeft` clear `findFull` the same way `findTaken`/`findLeft` do; `bagFull` sets it (so a refused pile is left on the next decision, never re-taken forever)
    - observe: `useRefused { item }` adds the item's label (`potion:${eff2}` / `tool:${tool}` / `n`) to `ctx.itemBlocked`; `encounterStarted` AND `floorChanged` clear it
    - `RUN_FLAGS` deep-equals `{ storeRoll: true, wornSlots: true }`; `playRun(seed, opts).state.c.worn` is an object (worn model live) and `state.storeRoll === true`; a Thief run starts with its cloak in `c.worn.cloak`
    - No-stall: for each of the forced cells `{ cls: "Thief", sub: "Pilfer", race: "Human" }` and `{ cls: "Magic User", sub: "Sorcerer", race: "Human" }` and `{ cls: "Fighter", sub: "Knight", race: "Troll" }`, `playRun(seed, { ...BOT_DEFAULTS, maxActions: 1000, force })` for seeds 1..3 is never `stuck`; across those nine runs at least one `itemUsed` or `lootTaken` event occurs
  </behavior>
  <action>
    A. Imports: `itemReady`, `toolIndex` from `../../engine/items.js`; `inDark`, `itemEffectActive`, `slotFor`, `activationFor` from `../../engine/derived.js`. Add `export const RUN_FLAGS = Object.freeze({ storeRoll: true, wornSlots: true })` (JSDoc: Phase 42 (BAL-01/02) — the bot plays the SHIPPED game's run rules, exactly what `engineAdapter#startNewRun` passes; the BEFORE pin (e69ff07) predates `wornSlots` and ran with the legacy bag-summed `eff()` and the fixed store, so the AFTER records these flags in `meta.runFlags` (Plan 03) rather than pretending the harness is unchanged; greenfield ruling 2026-09-17: the bot always plays the new rules).

    B. `export function hardFight(state)`, `export function chooseCombatItem(state, ctx)`, `export function chooseFieldItem(state, ctx)` per `<behavior>`, each a pure read. Item label helper `itemLabel(it)` (module-private, exported for Plan 03's tallies): `it.kind === "potion" ? \`potion:${it.eff2}\` : it.kind === "tool" ? \`tool:${it.tool}\` : it.n`. Address rule: an item with `slotFor(it)` non-null is used ONLY via `{ slot }` from `c.worn` when `"worn" in c`; potions/tools by bag index. Every candidate passes `itemReady(state, it)` and is not in `ctx.itemBlocked`.

    C. `decideAction` in-combat chain: compute `const pick = chooseCombatItem(state, ctx);` once, right after the Fight! check; (a0) `if (pick && pick.reason === "heal") return pick.action;` BEFORE the flee/parley check (a); (b2)/(b3) `if (pick && (pick.reason === "buff" || pick.reason === "staff")) return pick.action;` AFTER the generic `drinkPotion` step (b) and before talk-first (c). Out-of-combat chain: insert the loot step FIRST (before `pendingJoiner`): `if (Array.isArray(state.pendingLoot) && state.pendingLoot.length) return ctx.findFull ? { type: "leaveAllLoot" } : { type: "takeAllLoot" };` and the torch step after the camp step: `const field = chooseFieldItem(state, ctx); if (field) return field;`. Update the JSDoc chain letters.

    D. `observe`: `useRefused` → `ctx.itemBlocked.add(itemLabel(e.item))`; `lootTaken`/`lootLeft` → `ctx.findFull = false`; `floorChanged` also clears `itemBlocked`. `playRun`: `newRun(seed, [], { startDepth: opts.startDepth, force: opts.force, ...RUN_FLAGS })`.

    E. `test/unit/bot-tactics.test.js`: every `<behavior>` bullet (build worn-model states with `c.worn = { staff: { kind: "staff", n: "Birch Staff", use: "freeze", charges: 2 } }` etc.; potions as `{ kind: "potion", n: "Speed potion (yellow)", eff2: "speed", uses: 1 }`; a torch via `toolItem("torch")`-shaped literal; timers `{}`), the RUN_FLAGS/playRun assertions, the nine-run no-stall proof (measure the action budget like the Phase 41 test did — if 1000 is too small for a run to END naturally, raise it and say why in the comment; never let an undersized budget report a false stuck).

    Run `node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js test/unit/bot-buy-policy.test.js test/unit/class-matrix.test.js` — green. Commit `feat(42-02): bot pops heal/buff potions, fires worn staves, lights torches, takes loot, plays storeRoll+wornSlots run rules` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "export const RUN_FLAGS = Object.freeze({ storeRoll: true, wornSlots: true })" tools/lib/tuning-bot.mjs` is 1; `grep -c "\.\.\.RUN_FLAGS" tools/lib/tuning-bot.mjs` is 1
    - `grep -c "export function chooseCombatItem" tools/lib/tuning-bot.mjs` is 1; `grep -c "export function chooseFieldItem" tools/lib/tuning-bot.mjs` is 1; `grep -c "export function hardFight" tools/lib/tuning-bot.mjs` is 1
    - `grep -c 'type: "takeAllLoot"' tools/lib/tuning-bot.mjs` is 1; `grep -c 'type: "leaveAllLoot"' tools/lib/tuning-bot.mjs` is 1; `grep -c 'e.type === "useRefused"' tools/lib/tuning-bot.mjs` is 1
    - `node -e "import('./tools/lib/tuning-bot.mjs').then(m=>{const r=m.playRun(1,{...m.BOT_DEFAULTS,maxActions:50});process.exit(r.state.storeRoll===true&&r.state.c.worn&&typeof r.state.c.worn==='object'?0:1)})"` exits 0
    - `node -e "import('./tools/lib/tuning-bot.mjs').then(m=>{const l=m.botLine({...m.BOT_DEFAULTS,seeds:40,workers:4,maxActions:5000});const b=require('./docs/class-pass/v15-before.json').meta.bot;process.exit(l===b?0:1)})"` exits 0
    - `node --test test/unit/bot-tactics.test.js` exits 0 with at least 22 tests; `node --test test/unit/tuning-bot.test.js test/unit/bot-buy-policy.test.js test/unit/class-matrix.test.js` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js test/unit/bot-buy-policy.test.js test/unit/class-matrix.test.js</automated>
  </verify>
  <done>The bot heals before it runs, buffs before a hard fight, fires the staff it wears, lights torches in the dark, picks up what it wins, and plays the shipped run rules — every pick guarded, every refusal blocked, nine forced runs never stuck.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gate, doc notes for the item/ability policy, SUMMARY</name>
  <files>tools/lib/tuning-bot.mjs</files>
  <read_first>
    - tools/lib/tuning-bot.mjs (the header comment listing the read-only helpers with precedent and the ONE write-path bypass — update both statements)
    - .planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md (the `maxActions` lesson and the human-verification section format)
  </read_first>
  <action>
    A. Update `tools/lib/tuning-bot.mjs`'s header comment: the read-only helper list gains `isReady` (effects.js), `itemReady`/`toolIndex` (items.js), `inDark`/`itemEffectActive`/`slotFor`/`activationFor`/`DEATH_PANIC_THRESHOLD` (derived.js), `ABILITY_BY_ID` (content); the "exactly ONE write-path bypass" sentence becomes "exactly TWO — forceParty and playRun's combat-target selection before a useAbility dispatch (mirrors the shell's foe-card tap)". Commit `docs(42-02): tuning-bot header — new read-only helpers, the second harness-only bypass` (explicit path).

    B. PLAN GATE (verification agents are off): `npm test` (`# fail 0`; record `# tests`/`# pass`), `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `git status --porcelain test/parity/fixtures` empty. No `build:www` (no `src/browser` change).

    C. Write `42-02-SUMMARY.md` (standard sections) with: the policy summary in one paragraph per area (abilities / items / loot / run flags) for Plan 03 to transcribe into the ledger; `## Human verification (deferred to end of run)` — this plan is harness-only (no device-visible change); list ONE item: "none — dev harness only; the Pixel 7 checklist gains nothing from this plan" so Plan 04's aggregation has an explicit entry; `## Corrections to CONTEXT`: (1) the bot never took the victory loot pile (`state.pendingLoot`) before this plan — items came only from encounter-dot finds; (2) the bot ran `newRun` without `storeRoll`/`wornSlots` (legacy bag-summed `eff()`, fixed store) — now `RUN_FLAGS`; (3) any measured `maxActions` budget adjustment in the no-stall tests; `## Next Phase Readiness`: Plan 03 adds spells-by-niche, the usage tallies, the smoke and the ledger addendum.
  </action>
  <acceptance_criteria>
    - `grep -c "exactly TWO" tools/lib/tuning-bot.mjs` is 1
    - `npm test` output ends with `# fail 0` and `# pass` is at least the Plan 01 gate count + 30
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` is empty
    - `.planning/phases/42-flee-retune-consolidated-balance-close/42-02-SUMMARY.md` exists and contains `## Human verification (deferred to end of run)` and `## Corrections to CONTEXT`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <done>The suite is green with the master untouched; the header tells the truth about the harness's read surface and its two bypasses; the SUMMARY hands Plan 03 the policy text.</done>
</task>

</tasks>

<verification>
- `npm test` `# fail 0`; master hash unchanged; no fixture edits.
- `botLine` equals the BEFORE pin's `meta.bot`.
- Nine forced-cell runs (Thief/MU/Fighter × seeds 1-3) never stuck; abilities and items observed in use.
</verification>

<success_criteria>
- BAL-01 second half (abilities + items + tools): the bot uses them by the Joiner policy and the CONTEXT item rules, with guards and observe-blocks so it can never stall.
- The loot pile and the shipped run rules are on; `Bot:` line frozen.
</success_criteria>

<output>
Create `.planning/phases/42-flee-retune-consolidated-balance-close/42-02-SUMMARY.md` when done.
</output>
