---
phase: 69-compliance-device-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PLAY-GAMES-SETUP.md
  - test/unit/play-games-runbook.test.js
autonomous: true
requirements: [COMPLY-03]
user_setup:
  - service: Google Play Console (Play Games Services)
    why: "Enabling PGS, credentials, the APP_ID, testers, the five Season-1 leaderboards and publishing are console actions only the account owner can take (D-06); they are deferred items and never block the milestone"
    dashboard_config:
      - task: "Walk docs/PLAY-GAMES-SETUP.md section 6 (Order of operations) top to bottom, then hand Claude the APP_ID and the five leaderboard IDs for the rebuild"
        location: "Play Console -> Delve, Die, Repeat -> Grow users -> Play Games Services -> Setup and management"

must_haves:
  truths:
    - "docs/PLAY-GAMES-SETUP.md is a complete, walkable console runbook: one ordered checklist (section 6) runs from enabling PGS through credentials, the APP_ID, testers, the five Season-1 leaderboards, pasting their IDs into content/leaderboards.js, the rebuild, the tester check and publishing, and points at the Data safety answers and the two privacy URLs (D-06)"
    - "The five Season-1 leaderboards are specified with name, ordering (LEANEST Smaller is better, the other four Larger is better, per 68 D-16), score format (Numeric, no decimals) and limits (empty), and tamper protection on (D-06)"
    - "The runbook covers pasting the IDs into the content/ leaderboard-ID map (68 D-14), the APP_ID string resource game_services_project_id, linking the Play App Signing SHA-1 plus the optional debug-keystore SHA-1, the tester allow-list, publishing the PGS config, and the season-bump procedure (68 D-15) (D-06)"
    - "A new publishing section gives the current menu path (Grow users, Play Games Services, Setup and management, Publishing), what publishing does (every user with the game gets PGS, not only Testers), the up-to-2-hours delay, and its source URL and date (D-06)"
    - "The runbook states that these are the user's Play Console actions, recorded as deferred human items in docs/UAT-v2.0.md, and that they never block the milestone close (D-06)"
    - "Every existing runbook pin in test/unit/play-games-runbook.test.js still passes; new pins cover the order of operations, publishing, the ordering of each board row and the consecutive section numbering"
  artifacts:
    - path: "docs/PLAY-GAMES-SETUP.md"
      provides: "the finished PGS console runbook"
      contains: "Order of operations"
    - path: "test/unit/play-games-runbook.test.js"
      provides: "the extended runbook pins"
      contains: "Publishing"
  key_links:
    - from: "docs/PLAY-GAMES-SETUP.md section 6"
      to: "sections 2, 3, 4, 5, 7, 8, 11 and 12"
      via: "each checklist step names the section that details it"
      pattern: "Order of operations"
    - from: "docs/PLAY-GAMES-SETUP.md"
      to: "store-listing/LISTING.md Data safety"
      via: "the Data safety pointer in the order of operations"
      pattern: "store-listing/LISTING.md"
---

<objective>
Finish the Play Console runbook Phase 67 started. Implements D-06: the five Season-1 leaderboards (names, ordering, score format, limits), the IDs into content/leaderboards.js, the APP_ID string resource, the Play App Signing and debug-keystore SHA-1 credentials, the tester allow-list, publishing the PGS config, and the season-bump procedure. These are all recorded as the user's deferred console actions, never as a blocking checkpoint.

The runbook already covers sections 1 to 5 and 7 to 11 (67-02 and 68-01). This plan verifies them rather than rewriting them. It adds what D-06 still lacks: a single ordered checklist (section 6 is rewritten from its future-tense "what Phase 69 adds" list), a publishing section, the tester-check extension for leaderboards, and the edge statements the probe asked for.

Purpose: COMPLY-03. The user can walk the console once, in order, and hand Claude the IDs.
Output: docs/PLAY-GAMES-SETUP.md and test/unit/play-games-runbook.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-compliance-device-close/69-CONTEXT.md
@docs/PLAY-GAMES-SETUP.md
@test/unit/play-games-runbook.test.js

<interfaces>
- The current runbook's sections: 1 What this is; 2 Enable Play Games Services; 3 Credentials (the SHA-1 that matters; includes the optional debug-keystore keytool command); 4 The APP_ID (game_services_project_id in android/app/src/main/res/values/games-ids.xml, 12-zero stand-in); 5 Testers; 6 What Phase 69 completes (future tense; rewritten here); 7 Leaderboards (the ordering table, Numeric, limits empty, tamper protection on, the LEANEST limit); 8 Where the IDs go (content/leaderboards.js LEADERBOARD_IDS[season]); 9 Starting a new season (content/season.js SEASON bump, the 70-board / 14-season cap); 10 How the score tag is built; 11 How to check it worked.
- The existing REQUIRED pins include "Phase 69", "Phase 68", "70", "64", the five board names, "Smaller is better", "Larger is better", "PLACEHOLDER", "tamper", "First L.", "content/leaderboards.js", "LEADERBOARD_IDS", "content/season.js", "Testers", "Play App Signing", "upload key", "game_services_project_id", and the old-title guard (the doc must never contain the old working title). All must keep passing.
- Google's publishing page, read by the planner: developer.android.com/games/pgs/console/publish ("Test and publish your game", last updated 2026-06-16). Path: Grow users > Play Games Services > Setup and management > Publishing. The Publishing section lists anything missing or misconfigured. Publishing makes the configured PGS available to all users with the game, without being added as testers, and is separate from publishing the app. Changes can take up to 2 hours; publish at least 2 hours before the game goes live. An unpublished game must allowlist tester accounts. Tester data is not deleted at publish.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the runbook pins (RED)</name>
  <files>test/unit/play-games-runbook.test.js</files>
  <read_first>
    - test/unit/play-games-runbook.test.js
    - docs/PLAY-GAMES-SETUP.md
  </read_first>
  <behavior>
    - The REQUIRED list also needs: "Order of operations", "Publishing", "Grow users", "2 hours", "store-listing/LISTING.md", "privacy/apps", "privacy/delete-data", "debug keystore", "Numeric", "docs/UAT-v2.0.md"
    - Ordering test: in the section 7 table, the LEANEST row contains "Smaller is better", and the DEEPEST, LONGEST, BUTCHERY and PURSE rows each contain "Larger is better"
    - Order-of-operations test: take only the numbered checklist lines (lines that start with a number and a period) inside the section headed "Order of operations", and find the first line that mentions each step. The steps must appear in this order: enabling PGS (section 2), the SHA-1 credential (section 3), the APP_ID (section 4), Testers (section 5), the leaderboards (section 7), content/leaderboards.js (section 8), the rebuild (docs/RELEASING.md), the tester check (section 11), publishing (section 12). Matching each step by the section number it cites is the least brittle way
    - Numbering test: the "## N." headings run 1, 2, 3 ... with no gap and no repeat
  </behavior>
  <action>
    Per D-06. Add the new needles to REQUIRED and the three structural tests above. Keep every existing needle and the old-title guard exactly as they are. Slice sections with a helper that takes the text from a "## " heading to the next "## " heading, with CRLF already normalized as the file does now. For the table rows, match the markdown lines that begin with "| DEEPEST", "| LEANEST", and so on. Update the file's header comment so it says Phase 69 extends the pins (order of operations, publishing, the per-row ordering, the numbering). Do not use tools/stale-terms.mjs's retired words. Run the file and confirm the new tests fail against the current doc.
  </action>
  <verify>
    <automated>node --test test/unit/play-games-runbook.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - The new tests fail against the unmodified runbook (RED, recorded in the SUMMARY with the failing test names)
    - All pre-existing tests in the file still pass
  </acceptance_criteria>
  <done>The runbook's missing pieces are pinned before they are written.</done>
</task>

<task type="auto">
  <name>Task 2: Write the order of operations, the publishing section and the leaderboard checks (GREEN)</name>
  <files>docs/PLAY-GAMES-SETUP.md</files>
  <read_first>
    - docs/PLAY-GAMES-SETUP.md
    - content/leaderboards.js
    - content/season.js
    - docs/RELEASING.md (the rebuild and upload wording)
  </read_first>
  <action>
    Per D-06. Verify sections 2 to 5 and 7 to 11 against content/leaderboards.js, content/season.js and games-ids.xml. Fix only statements that are wrong, and list any fix in the SUMMARY. Then:
    1. Rewrite section 6 as "## 6. Order of operations (Phase 69)". Replace its future-tense opening sentence and bullet list with a short paragraph and a numbered checklist. The paragraph says these are your Play Console steps, recorded as deferred items in docs/UAT-v2.0.md, never blocking a milestone close, and that the 2.0.0 build works without them (sign-in fails gracefully and boards are skipped until the IDs are in). The checklist, one line each with the section that details it:
       (1) enable PGS (section 2);
       (2) add the Android credential with the Play App Signing key's SHA-1, and optionally a second one with the debug keystore's SHA-1 for a local debug APK (section 3);
       (3) copy the APP_ID (section 4);
       (4) add every closed-testing tester and yourself to Testers (section 5);
       (5) create the five Season-1 leaderboards (section 7);
       (6) send Claude the APP_ID and the five IDs, or paste them yourself into games-ids.xml and content/leaderboards.js LEADERBOARD_IDS[1] (sections 4 and 8);
       (7) rebuild and upload with a versionCode bump (docs/RELEASING.md);
       (8) check with a tester account (section 11);
       (9) publish the configuration (section 12);
       (10) enter the Data safety answers from store-listing/LISTING.md, with the privacy URL https://darktierstudios.com/privacy/apps and the delete-data URL https://darktierstudios.com/privacy/delete-data (Play Console, App content);
       (11) later, per season, section 9.
       Keep the two facts the old section held: LINEAGE and GRAVEYARD get no PGS board, and the 70-board lifetime cap (five per season, fourteen seasons).
    2. Section 7: state each board's name as it is typed (for example "DEEPEST, Season 1"). Add that the console may label Ordering as "Sort order". Keep the table, the Numeric format with no decimals, the empty limits and tamper protection as they are.
    3. Add "## 12. Publish the Play Games configuration" after section 11:
       - The path: Grow users, Play Games Services, Setup and management, Publishing. The page lists anything missing or misconfigured; fix it and come back.
       - What publishing does: every player with the game can use Play Games sign-in and the leaderboards, not only the Testers list, and it is separate from publishing the app itself.
       - Timing: changes take up to 2 hours; publish at least 2 hours before a production rollout.
       - Tester data is not deleted at publish.
       - Each leaderboard's ordering cannot be changed once published (section 7).
       - When: after the section 11 check passes. While the app is only on closed testing, staying unpublished is fine as long as every closed-track tester is on the Testers list; anyone else simply stays signed out.
       - Source: developer.android.com/games/pgs/console/publish, updated 2026-06-16, read 2026-09-24. Re-fetch it before writing; if its path or wording changed, use the current one and record that in the SUMMARY.
    4. Section 11: add the leaderboard check. After the IDs are in and the build is rebuilt, one tester death appears on all five boards. The console's score view shows its v1 tag (for example v1.2.8.3.0.7.22.431.19.4688.1180.Hilda_Ferrow), and the LEANEST score is the squares-per-floor rate times 1,000.
    5. Edge statements (probe coverage69.json, COMPLY-03):
       - Adjacency: in section 3, both SHA-1 credentials can exist side by side on the same package, and each build matches only its own. In section 9, the fourteenth season takes boards 66 to 70, the last five, and there is no fifteenth set of five.
       - Empty: in section 5, an unpublished configuration with nobody on the Testers list lets nobody sign in, you included. Section 8 already says a board whose ID was never replaced is skipped; keep it.
       - Ordering: section 7's LEANEST limit, where equal rates tie on the global board while the local board breaks the tie by depth; keep it.
    Keep the shipping name only. The old working title must never appear, including in paths. The doc must end with the file ending the test expects (LF, as now).
  </action>
  <verify>
    <automated>node --test test/unit/play-games-runbook.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/unit/play-games-runbook.test.js passes, both the new and the pre-existing tests
    - grep -c "^## " docs/PLAY-GAMES-SETUP.md prints 12 (sections 1 to 12)
    - The section 6 checklist names sections 2, 3, 4, 5, 7, 8, 9, 11 and 12, docs/RELEASING.md and store-listing/LISTING.md
    - Section 12 cites developer.android.com/games/pgs/console/publish with a date
    - npm test shows no failures beyond the known set (on master: AUD-06; in a worktree: the 7 CRLF doc-ledger tests)
  </acceptance_criteria>
  <done>The runbook can be walked once, in order, from an empty console to published leaderboards, and every D-06 item is in it and pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook text → the user's Play Console actions | A wrong step (wrong SHA-1, wrong ordering) is costly: orderings are fixed after publishing and the 70-board cap is lifetime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-05 | Tampering (integrity of config) | section 7 ordering table | medium | mitigate | Per-row ordering pinned by a test (LEANEST Smaller is better, the rest Larger is better); publishing section repeats that ordering is fixed once published |
| T-69-06 | Spoofing | section 3 credentials | medium | mitigate | "Not the upload key's SHA-1" stays pinned; the debug credential is described as a separate, optional second credential |
| T-69-07 | Information disclosure | runbook contents | low | accept | The runbook holds no secrets: keystore passwords live only in the git-ignored android/keystore.properties, and the debug keystore's password is Android's public default |
</threat_model>

<verification>
- node --test test/unit/play-games-runbook.test.js passes
- The doc has sections 1 to 12, numbered consecutively, and section 6 is the ordered checklist
- npm test: no failures beyond the known set
</verification>

<success_criteria>
- COMPLY-03: the runbook covers enabling PGS, linking the Play App Signing SHA-1, creating the leaderboard IDs per board per season, publishing the config and the tester allow-list, and says where the IDs land in the build's config. The console steps are the user's deferred items (D-06)
</success_criteria>

## Flagged assumptions (edge probe coverage69.json)

- COMPLY-03 adjacency: two credentials on one package (debug and Play App Signing) coexist and never match each other's build. At exactly the cap, the fourteenth season uses boards 66 to 70 and a fifteenth set cannot be created.
- COMPLY-03 empty: with no IDs pasted, boards are skipped silently. With the 12-zero APP_ID, sign-in fails gracefully. With no Testers while unpublished, nobody signs in.
- COMPLY-03 ordering: equal LEANEST rates tie on the global board, since one integer cannot also break ties by depth, and the local board breaks the tie by depth. The checklist order is the order to act in; publishing comes after the tester check.

<output>
Create `.planning/phases/69-compliance-device-close/69-02-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing: walk docs/PLAY-GAMES-SETUP.md section 6 in Play Console, confirm each menu path, and send Claude the APP_ID and five leaderboard IDs for the rebuild.
</output>
