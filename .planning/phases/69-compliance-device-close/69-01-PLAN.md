---
phase: 69-compliance-device-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - store-listing/LISTING.md
  - test/unit/store-listing.test.js
  - store-listing/screenshots/phone/08-dead.png
  - store-listing/screenshots/tablet-7in/08-dead.png
  - store-listing/screenshots/tablet-10in/08-dead.png
  - C:/projects/darktier-studio/src/pages/privacy/apps.astro
  - C:/projects/darktier-studio/src/pages/privacy/delete-data.astro
autonomous: true
requirements: [COMPLY-01, COMPLY-02]
user_setup:
  - service: darktierstudios.com (the darktier-studio Astro site)
    why: "The reconciled privacy and delete-data pages are committed locally only; the user reviews and deploys them (D-02)"
    dashboard_config:
      - task: "Review the local darktier-studio commit recorded in store-listing/LISTING.md, then deploy the site (npm run deploy in C:/projects/darktier-studio)"
        location: "C:/projects/darktier-studio (Firebase Hosting)"
  - service: Google Play Console
    why: "Data safety answers and the listing copy are drafted in the repo; only the account owner can enter them (D-04, D-05)"
    dashboard_config:
      - task: "Enter the Data safety answers from store-listing/LISTING.md (Data safety section), with privacy URL https://darktierstudios.com/privacy/apps and delete-data URL https://darktierstudios.com/privacy/delete-data"
        location: "Play Console -> Delve, Die, Repeat -> Policy and programs -> App content -> Data safety"
      - task: "Paste the re-voiced full description"
        location: "Play Console -> Grow users -> Store presence -> Main store listing"

must_haves:
  truths:
    - "The live apps policy source (C:/projects/darktier-studio/src/pages/privacy/apps.astro) is the one reconciled; no new policy page is drafted in this repo (D-01)"
    - "The policy's what-is-sent list mirrors the v1 score tag literally: the Play Games player ID; per run, the five leaderboard scores (DEEPEST, LEANEST, LONGEST, BUTCHERY, PURSE); and a short tag with race, sub-class, level, cause of death, floor, days, steps, kills, wilmst, experience and the game-rolled adventurer name (possibly shortened). It names what is not sent: the epitaph, saves, settings, the local graveyard and personal bests (D-02 gap 1, CONTEXT specifics, 67 D-18)"
    - "The policy no longer lists a death count, no longer promises a sign-out from the account menu (it says turn Compete off with STOP COMPETING, and use the Play Games app to disconnect entirely), and no longer describes the tombstone share card (the section is removed) (D-02 gaps 2, 3, 4)"
    - "The policy says plainly that Compete is on by default and the game may sign the player in automatically at launch unless Compete is off; with Compete off the game sends no leaderboard data and makes no sign-in, submit or fetch call, while Google's own Play Games software may still start with the app and show its own banner (D-02 gap 5, 67 D-01, 67 D-20)"
    - "The policy's effective date is the execution date and its Data safety summary matches store-listing/LISTING.md's answers (D-02 gap 6, D-04)"
    - "delete-data.astro tells the player to turn Compete off instead of signing out; its Play Games deletion steps are checked against Google's current help and changed only if wrong (D-02)"
    - "Both website edits are one local commit in C:/projects/darktier-studio, never pushed; store-listing/LISTING.md's Privacy policy section records the effective date and that commit hash (D-02, D-03)"
    - "store-listing/LISTING.md's Data safety section answers: User IDs (Play Games player ID) and App activity (scores plus the tag's gameplay details) collected; encrypted in transit; optional (Compete off); purpose App functionality; not sold; not shared, with the finding and its sources; deletion through the Play Games app and the delete-data page (D-04)"
    - "The answers are backed by a fresh source-level audit recorded in the listing: package.json and the lockfile's runtime packages, the AndroidManifest.xml permissions, and a grep of a freshly built www/ for network APIs, every hit classified, with no ads, analytics or crash SDKs (D-04). The build-level half (Gradle releaseRuntimeClasspath, merged release manifest) is 69-04's, on the actual 2.0.0 build"
    - "The store description's offline bullet is re-voiced to name optional Google Play Games leaderboards; no other description claim contradicts v2.0; the graveyard screenshot is regenerated only if the capture tool runs with no new install, otherwise it is recorded as a deferred human item (D-05)"
  artifacts:
    - path: "C:/projects/darktier-studio/src/pages/privacy/apps.astro"
      provides: "the reconciled Google Play apps privacy policy"
      contains: "Compete"
    - path: "C:/projects/darktier-studio/src/pages/privacy/delete-data.astro"
      provides: "the Play Console Delete data URL page, with the Compete-off step"
      contains: "Compete"
    - path: "store-listing/LISTING.md"
      provides: "Data safety answers + source-level audit, the privacy record (date + website commit), the re-voiced description"
      contains: "App functionality"
    - path: "test/unit/store-listing.test.js"
      provides: "pins for the Data safety answers, the privacy record and the description limits"
      min_lines: 40
  key_links:
    - from: "store-listing/LISTING.md (Privacy policy URL section)"
      to: "the darktier-studio commit that reconciled apps.astro"
      via: "the recorded commit hash and effective date"
      pattern: "darktier-studio commit [0-9a-f]{7,40}"
    - from: "apps.astro what-is-sent list"
      to: "src/browser/scoreTag.js encodeTag field order"
      via: "one listed value per tag field, in the tag's order"
      pattern: "experience"
    - from: "apps.astro Data safety summary"
      to: "store-listing/LISTING.md Data safety answers"
      via: "same data types, same shared/optional/purpose answers"
      pattern: "App functionality"
---

<objective>
Bring the privacy policy, the Data Safety answers and the store listing into line with what v2.0 actually sends. Implements D-01 (the live darktier-studio apps policy is the source of truth), D-02 (reconcile apps.astro's six known gaps and re-check delete-data.astro, committed locally in the website repo), D-03 (LISTING.md records the reconciled page's effective date and website commit hash), D-04 (the Data safety answers, the shared/not-shared finding with its sources, backed by a fresh source-level SDK and dependency audit) and D-05 (the re-voiced offline bullet, and the graveyard screenshot regenerated or flagged).

Purpose: COMPLY-01 and COMPLY-02. The public disclosure has to match the shipped code exactly, including the uncomfortable part (Google's SDK starts at launch even with Compete off, 67 D-20).
Output: the two reconciled .astro pages (one local commit in C:/projects/darktier-studio, not pushed), the updated store-listing/LISTING.md, and a new test/unit/store-listing.test.js. No Android build here: the build-level audit and the AAB are 69-04's.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-compliance-device-close/69-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-01-SUMMARY.md
@store-listing/LISTING.md

<interfaces>
Facts the planner verified on 2026-09-24 (re-check each before writing a claim that depends on it):
- Score tag v1 (src/browser/scoreTag.js): twelve dot-separated fields v1.race.sub.lvl.cause.floor.day.steps.kills.gold.sp.name, max 64 chars, URL-safe. gold is the wilmst carried; sp is experience points (src/browser/heroTab.js labels c.sp EXPERIENCE). The name is fitted: full, else "First L.", else the first name cut short. No epitaph, note, seed, hash or acts ever ride in it.
- The adventurer's name is rolled by the game (content/names.js); there is no name-entry field (grep found no rename/name input in mazeworld.html or src/browser/).
- Five submitted boards: DEEPEST, LEANEST, LONGEST, BUTCHERY, PURSE (LINEAGE and GRAVEYARD are never submitted). The submitted number is an ordering key per docs/PLAY-GAMES-SETUP.md section 7.
- Account menu copy (content/account.js sheet): SIGN IN, STOP COMPETING, COMPETE ON/OFF, SETTINGS; stopHelp says to disconnect in the Play Games app. The menu opens from the account chip: top-right of the title screen, and on the dungeon HUD directly left of the ☰ menu.
- 67 D-01: auto sign-in at launch, Compete ON by default. 67 D-03: no programmatic sign-out; STOP COMPETING turns Compete off. 67 D-20: the plugin's native load() calls PlayGamesSdk.initialize at every app start, even with Compete OFF; Google may show its own "Welcome back" banner; the game itself never calls sign-in, submit or fetch with Compete OFF.
- The offline submission queue is stored on the device under ddr.pgsqueue.v1 (src/browser/pgsQueue.js); turning Compete OFF purges it (68-07). Global board results are cached in memory only (src/browser/globalBoards.js has no storage).
- FRIENDS: the Play Games friends consent screen appears only after the player taps SHOW MY FRIENDS (68-05, 68-06). No profile image URL leaves src/browser/playGames.js (initials avatars only).
- package.json runtime dependencies: 8 @capacitor/* packages plus @modbender/capacitor-play-games pinned at exactly 0.5.0; one devDependency (@capacitor/cli). capacitor.config.json does not enable CapacitorHttp.
- android/app/src/main/AndroidManifest.xml: the only uses-permission is INTERNET; the games APP_ID meta-data references @string/game_services_project_id.
- A grep of the current (stale) www/ found network-API text only in www/src/browser/sfx.js (a same-origin fetch of the bundled ./sfx/<clip>.mp3 files) and the vendored @capacitor/core files. Rebuild www/ before auditing.
- 67-06's Gradle audit (2026-09-24, releaseRuntimeClasspath): play-services-games-v2 22.0.0, play-services-base 18.5.0, play-services-basement 18.9.0, play-services-tasks 18.2.0, no firebase, 0 forbidden hits, google-services never applied. 69-04 re-runs this on the 2.0.0 build.
- Google sources read by the planner: (1) Play Console Help answer 10787469 (Data safety form): "sharing" excludes transfers to a service provider processing on the developer's behalf, and transfers from a user-initiated action or prominent disclosure with consent where users reasonably expect it; "collect" includes data sent by SDKs in the app; optional only if all users can opt in or opt out; App activity lists "Other actions", described with gameplay as an example; App info and performance covers crash logs and diagnostics. (2) developer.android.com/games/pgs/data-collection (the old developers.google.com/games/services/data-collection URL 301-redirects here), "Prepare for Google Play's data disclosure requirements", last updated 2026-06-16: collected automatically are Gamer Identity (gamertag, avatar), Analytics and Diagnostics "to improve the stability of our SDKs"; collected by usage include game scores; a PGS game "can only read/write the authenticated player's data for that game"; data encrypted in transit over HTTPS; users delete through their Play Games profile (play.google.com/games/profile) or Google My Account; "as the app developer, you are solely responsible for deciding how to respond" to the Data safety form.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Source-level SDK and dependency audit, and the Data safety answers in LISTING.md</name>
  <files>store-listing/LISTING.md, test/unit/store-listing.test.js</files>
  <read_first>
    - store-listing/LISTING.md (whole file; the Data safety section is rewritten)
    - package.json and package-lock.json (grep only; do not read the lockfile in full)
    - android/app/src/main/AndroidManifest.xml
    - src/browser/playGames.js (header comment, lines 1-40)
    - .planning/phases/67-play-games-integration-account-chip/67-06-SUMMARY.md (the "Dependency audit" section)
    - test/unit/play-games-runbook.test.js (the house pattern for doc pins)
  </read_first>
  <behavior>
    - The Data safety section answers "collect or share" with Yes, and lists User IDs and App activity as collected, optional, for App functionality, encrypted in transit, deletable
    - The section says the data is not shared and not sold, and cites both Google sources (answer/10787469 and developer.android.com/games/pgs/data-collection)
    - The section records the source-level audit with its date, and the claim that INTERNET is an unused default is gone
    - The section names the delete-data URL
  </behavior>
  <action>
    Per D-04. Step 1, the audit (write raw outputs to the session scratchpad, never into the repo):
    (a) If node_modules is missing (a worktree), run npm ci. It installs only what package-lock.json already pins, with integrity checks. Never run npm install with a package name. Then run npm run build:www so www/ reflects the current source.
    (b) Runtime packages: list package.json dependencies and devDependencies. From package-lock.json, list every package entry that is not marked dev, and run a case-insensitive grep over those names for firebase, admob, play-services-ads, ads-identifier, analytics, measurement, crashlytics, appsflyer, adjust, facebook, appcenter, sentry and bugsnag. Expect 0 hits.
    (c) Permissions: list the uses-permission lines in android/app/src/main/AndroidManifest.xml. Expect INTERNET only, and no AD_ID.
    (d) Network APIs: grep -rlE over www/ for fetch(, XMLHttpRequest, WebSocket, sendBeacon and EventSource. Classify every file hit. Expected: www/src/browser/sfx.js (a same-origin fetch of a bundled ./sfx clip, no network); the vendored @capacitor/core files (the bridge's own HTTP patch, inert because capacitor.config.json does not enable CapacitorHttp); and the vendored Play Games plugin's web code, if it has any hits (never loaded on native, and src/browser/playGames.js loads the plugin lazily and only with Compete on). Any other hit outside src/browser/playGames.js is a defect finding. Record it in the SUMMARY under "Defect findings (fix plan needed)" and do not word the listing around it. Per the 69 CONTEXT domain, a real defect becomes a small fix plan in this phase.
    Step 2, test first (RED): create test/unit/store-listing.test.js in the style of test/unit/play-games-runbook.test.js. Read store-listing/LISTING.md with CRLF normalized, and slice the "## Data safety" section up to the next "## " heading. Assert that the section contains: User IDs, App activity, App functionality, encrypted in transit, optional, not sold, the word Shared, developer.android.com/games/pgs/data-collection, answer/10787469, privacy/delete-data, and Source-level audit. Also assert that the whole file no longer contains the old collect answer or the unused-INTERNET claim; express those two checks as regexes in the test, not as grep gates. Run it and confirm it fails.
    Step 3 (GREEN): rewrite the "## Data safety" section of store-listing/LISTING.md in plain reference prose plus a table, covering:
    - The top question: Yes, the app collects user data.
    - User IDs: the Google Play Games player ID. Collected: yes. Shared: no. Optional: yes. Purpose: App functionality.
    - App activity, Other actions: the five leaderboard scores per run plus the score tag's gameplay details (race, sub-class, level, cause, floor, days, steps, kills, wilmst, experience, and the game-rolled adventurer name). Collected: yes. Shared: no. Optional: yes. Purpose: App functionality. Confirm the label on answer/10787469; if Play files game scores under a different App activity type, use that label and note why.
    - Processed ephemerally: no (Google stores the scores).
    - Security: encrypted in transit (Google Play services over HTTPS, per the PGS page). Users can request deletion: yes, through the Play Games app, play.google.com/games/profile, and https://darktierstudios.com/privacy/delete-data.
    - Not sold. Nothing else is collected. No Advertising ID.
    - "Optional" is justified because every user can turn Compete off (opt-out qualifies per answer/10787469).
    - The shared finding and its sources. Not shared: the transfer goes to Google Play Games Services, which processes it to run the leaderboards (the service-provider exemption). Posting to a public leaderboard is also the player's own choice, made by leaving Compete on (the user-initiated exemption). The PGS page says the game can read and write only the authenticated player's data for this game. Cite both URLs, with the PGS page's 2026-06-16 update date and today's fetch date.
    - An "Open decision for the console step (user)" note. Google's PGS page lists automatic Analytics and Diagnostics collection by its SDK, and per 67 D-20 the SDK initializes at launch even with Compete off. If the release-blocking Compete-off capture in docs/UAT-v2.0.md shows the SDK sending anything at launch, the conservative answer is to also declare App info and performance, Diagnostics (collected, not optional). The D-04 answers above stay as locked; this note records the finding and does not change them.
    - A "Source-level audit (2026-09-24, commit HEAD short hash)" subsection with the results of (b), (c) and (d), each hit and its classification. The INTERNET permission is now described as used by Google Play services for Play Games while Compete is on.
    - A one-line pointer that the build-level audit (Gradle releaseRuntimeClasspath and the merged release manifest) is recorded by 69-04 on the 2.0.0 build.
    Delete the old answer paragraph (the 2026-09-17 verification) entirely; git history keeps it. Run the test again (GREEN).
  </action>
  <verify>
    <automated>node --test test/unit/store-listing.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/unit/store-listing.test.js passes, and it failed before the section was rewritten (RED recorded in the SUMMARY)
    - The Data safety section cites both answer/10787469 and developer.android.com/games/pgs/data-collection
    - The audit subsection lists every www/ network-API file hit with a classification, and the forbidden-name grep result (expected 0)
    - The SUMMARY lists the raw audit commands and their exit codes, and either "Defect findings: none" or each finding
  </acceptance_criteria>
  <done>LISTING.md carries the D-04 answers, the not-shared finding with sources, the open diagnostics decision for the user, and a dated source-level audit, all pinned by a test.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile apps.astro and delete-data.astro in darktier-studio and commit them locally</name>
  <files>C:/projects/darktier-studio/src/pages/privacy/apps.astro, C:/projects/darktier-studio/src/pages/privacy/delete-data.astro</files>
  <read_first>
    - C:/projects/darktier-studio/src/pages/privacy/apps.astro (whole file)
    - C:/projects/darktier-studio/src/pages/privacy/delete-data.astro (whole file)
    - src/browser/scoreTag.js (lines 1-60: field order, name fitting, what never rides)
    - content/account.js (the sheet labels)
    - src/browser/pgsQueue.js (header comment: the queue key and the Compete-off purge)
    - store-listing/LISTING.md (the Data safety section written in Task 1)
  </read_first>
  <action>
    Per D-01 and D-02. Before editing, run git -C C:/projects/darktier-studio status --short and record the output. If unrelated files are already modified there, leave them alone and stage only the two page paths. Website voice: plain, accurate and friendly, never the game's sarcasm (CONTEXT discretion). Every factual sentence must trace to code or a decision. Put a claim table in the SUMMARY (sentence, then source file/line or decision id).

    apps.astro:
    1. Update the frontmatter comment. It describes Delve, Die, Repeat 2.0 (Leaderboards) and points at store-listing/LISTING.md in the game repo as the Data safety record. Drop the note about a future build shipping. Set const effective to the execution date (expected "September 24, 2026"). Keep const contact unchanged. Refresh the Layout description attribute so it no longer calls sign-in something you choose.
    2. Lede: we collect nothing ourselves and run no servers. The one exception is Google Play Games leaderboards through the in-game Compete setting. Compete is on by default and you can turn it off any time; with it off, the game sends no leaderboard data.
    3. Leaderboards section (gaps 1, 2, 3, 5):
       - Name the five boards in plain words (deepest descent, leanest run, longest-lived, most kills, fattest purse). Drop the old example list, including its deaths item.
       - Automatic sign-in: with Compete on, the game may sign you in to Google Play Games automatically when it starts, using the Play Games profile on the phone. Declining, or having no profile, leaves the game fully playable.
       - Compete off: the game makes no sign-in, submission or leaderboard request, and sends no leaderboard data. Google's own Play Games software, part of Google Play services, still starts with the app and may show its own "Welcome back" banner. That is governed by Google's privacy policy and your Play Games settings (67 D-20).
       - What is sent, as a list mirroring the tag in its field order: your Play Games player ID; for each run that ends, the five leaderboard scores; and a short run summary with race, sub-class, level, cause of death, floor reached, days survived, steps walked, kills, wilmst carried, experience points, and your adventurer's name. The name is the one the game rolled, not yours, and may be shortened. Then a not-sent sentence: the epitaph, your saves, your settings, your personal bests and your local graveyard stay on the phone.
       - Friends: only if you tap SHOW MY FRIENDS and allow it on Google's screen does the game show your Play Games friends' scores for this game. The global and friends results are shown on the phone and not saved.
       - Keep "Where it goes" and "Google's rules apply". Add play.google.com/games/profile to the deletion bullet.
       - Rewrite the deletion bullet's stop-sending sentence and the old sign-out sentence as follows. To stop, open the account menu (the small face at the top right of the title screen, or beside the ☰ menu in the dungeon) and choose STOP COMPETING, or set COMPETE to OFF. To disconnect the game from Play Games entirely, use the Play Games app. Turning Compete off also discards any runs still waiting to be sent. Confirm this purge in src/browser/pgsQueue.js and the 68-07 wiring before writing it; if the code disagrees, leave the sentence out and note why in the SUMMARY.
    4. Gap 4: remove the tombstone share-card section, both its h2 and its paragraph. The removal is the CONTEXT's preferred option and this plan's ruling. Remove any other mention of sharing a card.
    5. "Data stored on your device": the current run, the graveyard, personal bests, settings (including Compete), and while offline the short list of runs waiting to be sent to the leaderboards. None of it is sent to us, and it is removed on uninstall.
    6. "Permissions": internet is used only for Google Play Games while Compete is on, plus Google's own start-up described above.
    7. Gap 6, "Data safety summary": match LISTING.md exactly. User IDs (the Play Games player ID) and App activity (scores and the run summary) are collected. They are optional (turn Compete off), used for app functionality, encrypted in transit, deletable, not shared with third parties and not sold. Replace the old "shared with Google Play Games Services" wording.
    8. Keep "Who this covers", "Children", "Your rights", "Changes" and "Contact", adjusting any wording that implies signing in is required or opt-in only.

    delete-data.astro:
    - Lede: add that data is sent only while Compete is on, and what it is (player ID, run scores and the short run summary listed in the policy).
    - "Stop sending new data": the Compete-off instructions from item 3 above, with the Play Games app for a full disconnect.
    - Deletion steps: WebFetch Google's current Play Games help. Start with https://support.google.com/googleplay/answer/3129346; if that page does not cover per-game deletion, search for Google's "delete Play Games data" help. Compare the four steps (profile picture, Settings, the delete account and data item, per-game Delete). Change them only if Google's current wording differs, and record the URL, fetch date and verdict in the SUMMARY. If no page is reachable, leave the steps as they are and say so (docs/UAT-v2.0.md carries a device check of these steps).

    Build check: if C:/projects/darktier-studio/node_modules exists, run npm run build there. It writes only the git-ignored dist/ and public/build-info.json. A failure caused by the two pages must be fixed. A failure from environment or network must be recorded, not worked around.

    Commit: git -C C:/projects/darktier-studio add src/pages/privacy/apps.astro src/pages/privacy/delete-data.astro, then commit with the message "docs(privacy): reconcile the apps policy with Delve, Die, Repeat 2.0 leaderboards" and the session attribution trailers. Never push. Confirm with git -C C:/projects/darktier-studio show --stat HEAD that exactly those two files changed, and record the full and short hash.
  </action>
  <verify>
    <automated>cd C:/projects/darktier-studio && git show --stat --format=%h HEAD | grep -c "src/pages/privacy/" && grep -c "Compete" src/pages/privacy/apps.astro && grep -c "Compete" src/pages/privacy/delete-data.astro</automated>
  </verify>
  <acceptance_criteria>
    - grep -ci "total death count" C:/projects/darktier-studio/src/pages/privacy/apps.astro returns 0
    - grep -ci "most deaths" C:/projects/darktier-studio/src/pages/privacy/apps.astro returns 0
    - grep -ci "sign out at any time" C:/projects/darktier-studio/src/pages/privacy/apps.astro returns 0
    - grep -c "Sharing your results" C:/projects/darktier-studio/src/pages/privacy/apps.astro returns 0
    - grep -c ">Sign out<" C:/projects/darktier-studio/src/pages/privacy/delete-data.astro returns 0
    - apps.astro contains "STOP COMPETING", "experience", "wilmst", "epitaph", "Welcome back", "App functionality" and "play.google.com/games/profile"
    - The effective constant is the execution date, not "September 17, 2026"
    - git -C C:/projects/darktier-studio show --stat HEAD lists exactly the two page files, and git -C C:/projects/darktier-studio status shows the branch ahead of origin (not pushed)
    - The SUMMARY has the claim table, the delete-steps check (URL, date, verdict) and the darktier-studio build result
  </acceptance_criteria>
  <done>The live policy source says exactly what 2.0 sends and does, including Compete-on-by-default and Google's own start-up, and is committed locally in the website repo for the user to deploy.</done>
</task>

<task type="auto">
  <name>Task 3: LISTING.md privacy record, re-voiced description, screenshot ruling, and the extended pins</name>
  <files>store-listing/LISTING.md, test/unit/store-listing.test.js, store-listing/screenshots/phone/08-dead.png, store-listing/screenshots/tablet-7in/08-dead.png, store-listing/screenshots/tablet-10in/08-dead.png</files>
  <read_first>
    - store-listing/LISTING.md (the Privacy policy URL, Full description and Screenshots sections)
    - tools/store-screenshots/README.md
    - test/unit/store-listing.test.js (from Task 1)
  </read_first>
  <action>
    Per D-03: in "## Privacy policy URL", keep https://darktierstudios.com/privacy/apps and its note. Add the delete-data URL (https://darktierstudios.com/privacy/delete-data, for Play Console's "Delete data URL" field). Add a record line of the form: "Reconciled for 2.0.0 (Leaderboards): effective <date>; darktier-studio commit <short hash> (local, not yet deployed; the user deploys the site)", using Task 2's values.
    Per D-05: in the Full description block, replace the offline bullet (the one that claims no account, no sign-in and nothing collected) with: "Plays fully offline. Optional Google Play Games leaderboards, if you want the whole world to see how you died. No ads, no in-app purchases." Re-read every other sentence of the description and the short descriptions against v2.0. Change a sentence only if v2.0 makes it false, and list any such change in the SUMMARY. The description must stay within Google's metadata policy (no calls to action, no ranking claims) and within 4000 characters.
    Screenshot ruling (D-05): the graveyard shot (screenshots/*/08-dead.png) predates the Leaderboards panel. Check whether playwright-core resolves from tools/store-screenshots, for example with node -e and require.resolve using a paths option pointing at tools/store-screenshots. On 2026-09-24 it is not installed anywhere in the repo. Installing it would be a new package, which this plan must not do: the package-legitimacy gate needs an audit, and research was skipped. If it resolves, start node serve.js and run node capture.js per the README, time-boxed to about 10 minutes. Replace 08-dead.png at all three sizes only if the new capture shows the Leaderboards panel, and add the PNGs to this task's commit. Otherwise add an "Owed (deferred human item)" note under "## Screenshots". It says 08-dead shows the pre-2.0 graveyard and must be regenerated showing the Leaderboards panel on DEEPEST at 1080x1920, 1350x2400 and 1620x2880. Either install playwright-core per the README and run capture.js (whose bot predates later UI changes and may need label updates), or capture by hand in Chrome device mode at 432x768 @2.5, 675x1200 @2 and 810x1440 @2 from the served www/.
    Extend test/unit/store-listing.test.js:
    - The fenced Full description block is at most 4000 characters.
    - The preferred short description (the first backticked line under "## Short description") is at most 80 characters.
    - The description contains "Optional Google Play Games leaderboards".
    - A case-insensitive regex finds no claim in the file that nothing is collected.
    - The Privacy section contains privacy/apps, privacy/delete-data, a date matching a "Month D, 2026" regex, and "darktier-studio commit" followed by 7 to 40 hex characters.
    Avoid tools/stale-terms.mjs's retired words in the test (for example the old transient-message UI name).
  </action>
  <verify>
    <automated>node --test test/unit/store-listing.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/unit/store-listing.test.js test/unit/stale-terms.test.js passes
    - LISTING.md's Privacy section names the darktier-studio short hash that git -C C:/projects/darktier-studio rev-parse --short HEAD prints
    - Either the three 08-dead.png files are regenerated and show the Leaderboards panel, or the Screenshots section has the "Owed (deferred human item)" note, and the SUMMARY states which and why
    - npm test shows no failures beyond the known set (on master: AUD-06, the user's untracked sfx/theme.mp3; in a worktree: the 7 CRLF doc-ledger tests)
  </acceptance_criteria>
  <done>The listing records the reconciled policy (date and commit), carries the re-voiced description, and states the screenshot's status honestly, all pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo text → public website / Play Console | What the policy and Data safety form say becomes a public, regulated disclosure |
| this repo → the darktier-studio repo | The executor writes and commits in a second git repository |
| npm lockfile → worktree node_modules | npm ci materializes already-pinned packages for the www/ build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-01 | Repudiation / Information disclosure | apps.astro, LISTING.md Data safety | high | mitigate | Every policy sentence is traced to code or a decision in the SUMMARY's claim table; the what-is-sent list mirrors scoreTag.js field for field; the Compete-off paragraph states Google's own SDK start-up (67 D-20) rather than claiming zero traffic; the diagnostics question is surfaced to the user, not silently answered |
| T-69-02 | Tampering | darktier-studio commit | medium | mitigate | Stage only the two page paths by name, check the commit with git show --stat, never push; the user reviews and deploys |
| T-69-03 | Tampering (supply chain) | screenshot tool dependency | medium | mitigate | No package install: playwright-core is used only if it already resolves; otherwise the screenshot is a deferred human item |
| T-69-04 | Tampering | npm ci in a worktree | low | accept | Lockfile-only install with integrity hashes, the same packages 67-06 audited; no new package names |
</threat_model>

<verification>
- node --test test/unit/store-listing.test.js test/unit/stale-terms.test.js passes
- The darktier-studio HEAD commit touches exactly src/pages/privacy/apps.astro and src/pages/privacy/delete-data.astro, and is not pushed
- LISTING.md's Data safety answers and apps.astro's Data safety summary name the same data types and the same shared, optional and purpose answers
- npm test: no failures beyond the known set
</verification>

<success_criteria>
- COMPLY-01: the live apps policy source describes the Play Games player ID and score collection (Compete on by default, turn it off any time) and says nothing else leaves the device from the game
- COMPLY-02: LISTING.md drafts the Data safety answers (User IDs + App activity, App functionality, not shared, optional) and backs them with a dated source-level audit; the build-level half follows in 69-04
- D-01, D-02, D-03, D-04 and D-05 are each implemented as stated
</success_criteria>

## Flagged assumptions (edge probe coverage69.json)

- COMPLY-01 (unclassified): the list of what is sent has exactly the tag's eleven values plus the player ID and the five scores, no more and no fewer. A run under Compete off contributes nothing to the list (the empty case), and a run whose name is cut to "First L." or a shortened first name is covered by "may be shortened".
- COMPLY-02 (unclassified): the "optional" answer rests on Compete being opt-out for every user. The one thing that is not optional, Google's SDK start-up at launch, is surfaced as the user's console-time diagnostics decision and tied to the release-blocking capture, not hidden.

<output>
Create `.planning/phases/69-compliance-device-close/69-01-SUMMARY.md` when done. Include: the audit commands with exit codes and results; any defect findings; the claim table; the delete-steps check; the darktier-studio commit hash (full and short) and build result; the screenshot outcome; and a "Human verification (deferred to end of run)" heading listing: (1) deploy the darktier-studio site after reviewing the commit; (2) enter the Data safety answers and both URLs in Play Console; (3) paste the full description; (4) the diagnostics decision after the release-blocking capture; (5) the owed screenshot, if it was not regenerated.
</output>
