---
phase: 69-compliance-device-close
plan: 04
type: execute
wave: 2
depends_on: ["69-01", "69-02", "69-03"]
files_modified:
  - android/version.properties
  - store-listing/LISTING.md
  - docs/UAT-v2.0.md
autonomous: true
requirements: [COMPLY-04, COMPLY-02]
user_setup:
  - service: Google Play Console
    why: "The signed 2.0.0 (versionCode 9) AAB is built here; the user uploads it to the closed-testing track by hand (standing rule: never upload for them) (D-07)"
    dashboard_config:
      - task: "Upload android/app/build/outputs/bundle/release/app-release.aab (2.0.0, versionCode 9) to closed testing"
        location: "Play Console -> Delve, Die, Repeat -> Test and release -> Testing -> Closed testing -> Create new release"

must_haves:
  truths:
    - "android/version.properties reads versionCode=9 and versionName=2.0.0, set through the repo's bump flow (npm run version:bump with --name 2.0.0) from exactly versionCode 8 / 1.9.0 (D-07)"
    - "A signed release AAB is built by the existing npm run android:release flow at android/app/build/outputs/bundle/release/app-release.aab; keytool -printcert shows its upload-key certificate, and its merged release manifest has versionCode 9 and versionName 2.0.0 (D-07)"
    - "The AAB contains the 30 delivered sfx clips and not the user's untracked sfx/theme.mp3, which is held outside the repo during the build and restored byte-identical afterwards"
    - "Keystore credentials are never printed, echoed, logged or committed; android/keystore.properties stays git-ignored and is only checked for existence and key names (D-07)"
    - "The build-level SDK and dependency audit runs on this build: Gradle releaseRuntimeClasspath (0 forbidden hits, the play-services artifact versions recorded), the merged release manifest permissions (no AD_ID), google-services not applied, and the freshly built www/ network-API grep matching 69-01's classification. It is recorded in store-listing/LISTING.md (D-04)"
    - "docs/UAT-v2.0.md's header records the Play build: 2.0.0 / versionCode 9, the AAB path, byte size, sha256, the signing certificate fingerprint, the source commit, the tag, and that the user uploads it to closed testing (D-07)"
    - "The release is one commit, chore(release): 2.0.0 (versionCode 9), and the lightweight tag v2.0.0-play9 points at it, created only after a successful, verified build. Nothing is uploaded to Play and nothing is pushed (D-07)"
    - "The debug APK is not built here: D-08's Pixel 7 debug build and install offer belong to the orchestrator at the milestone close, after this plan (D-08)"
  artifacts:
    - path: "android/version.properties"
      provides: "versionCode 9 / versionName 2.0.0"
      contains: "versionCode=9"
    - path: "store-listing/LISTING.md"
      provides: "the build-level audit on the 2.0.0 build"
      contains: "releaseRuntimeClasspath"
    - path: "docs/UAT-v2.0.md"
      provides: "the Play build record in the header"
      contains: "v2.0.0-play9"
  key_links:
    - from: "git tag v2.0.0-play9"
      to: "the chore(release) 2.0.0 commit"
      via: "a lightweight tag on the release commit, matching v1.9.0-play8's convention"
      pattern: "v2.0.0-play9"
    - from: "docs/UAT-v2.0.md Play build line"
      to: "android/app/build/outputs/bundle/release/app-release.aab"
      via: "the recorded sha256 of the file"
      pattern: "sha256"
---

<objective>
Ship the milestone's signed Play build and close the audit loop on it. Implements D-07: bump to 2.0.0 / versionCode 9 with the repo's version-bump flow, build the signed AAB with the existing android:release flow, record the AAB path and versionCode, and tag v2.0.0-play9 after a successful build. The user uploads it to closed testing by hand; this plan never uploads and never pushes. It also implements the build-level half of D-04's audit on the shipped build. D-08's debug APK is explicitly not built here: the orchestrator builds it at the milestone close, after this plan, and makes the Pixel 7 install offer with the save-data warning already written into docs/UAT-v2.0.md.

Purpose: COMPLY-04 (a signed AAB with PGS, ready for the testing track) and the shipped-build evidence for COMPLY-02.
Output: android/version.properties (2.0.0 / 9), the signed AAB (git-ignored build output), the build-level audit in store-listing/LISTING.md, the Play build line in docs/UAT-v2.0.md, one release commit and the local tag v2.0.0-play9.

EXECUTION LOCATION: this plan must run in the main checkout, C:/projects/mazeworld, not in a git worktree. android/keystore.properties is git-ignored and exists only there, and a worktree build would produce an unsigned bundle at a path that is deleted with the worktree.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-compliance-device-close/69-CONTEXT.md
@docs/RELEASING.md
@.planning/phases/69-compliance-device-close/69-01-SUMMARY.md

<interfaces>
- package.json scripts: "version:bump": "node tools/bump-version.mjs"; "android:release": "npm run cap:sync && node tools/pin-jdk.mjs && node tools/gradle.mjs bundleRelease" (no bump); "play:release": bump then android:release. Do not run play:release after the manual bump, because it would bump a second time, to 10. tools/bump-version.mjs takes --name X and --dry-run and always adds 1 to versionCode.
- android/version.properties on 2026-09-24: versionCode=8, versionName=1.9.0.
- The previous release: commit 0c440bd "chore(release): 1.9.0 (versionCode 8) — v1.9 The Gear Screen for Play closed testing", whose body carries the AAB path, byte size and sha256. The lightweight tag v1.9.0-play8 points at it.
- tools/build-www.mjs copySfx() copies the whole sfx/ directory recursively into www/sfx/, so any untracked file in sfx/ ships in the AAB. On master the user's untracked sfx/theme.mp3 (about 2.3 MB) is there, and it is the cause of the known AUD-06 test failure. It must be held outside the repo for the build and never committed or deleted.
- 67-06's Gradle audit (releaseRuntimeClasspath): play-services-games-v2 22.0.0, play-services-base 18.5.0, play-services-basement 18.9.0 (18.4.0 requested), play-services-tasks 18.2.0, kotlin-stdlib 2.4.10, no firebase, forbidden grep 0 hits; google-services is on the buildscript classpath but applied only if android/app/google-services.json exists (it does not). The merged debug manifest permissions were INTERNET, VIBRATE and the androidx DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION.
- The toolchain is pinned at AGP 8.13.0 / Gradle 8.14.3 / JDK 21 (docs/RELEASING.md): never let anything upgrade it. npx cap sync wipes org.gradle.java.home and pin-jdk re-applies it, so android/gradle.properties must end with no diff.
- content/leaderboards.js LEADERBOARD_IDS[1] and android/app/src/main/res/values/games-ids.xml still hold their pre-console values unless the user has sent real ones. This build then ships with Play Games inert (graceful sign-in failure, boards skipped), and that is expected and recorded, not fixed here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight: main checkout, keystore presence, clean tree, hold the theme clip aside, green suite</name>
  <files>android/version.properties</files>
  <read_first>
    - docs/RELEASING.md
    - tools/bump-version.mjs (lines 1-60)
    - android/version.properties
  </read_first>
  <action>
    Per D-07. Every check below must pass before anything is bumped. On any failure, restore anything already moved, revert android/version.properties if it changed (git checkout -- android/version.properties), stop, and report the failing check in the SUMMARY. This is a stop-and-report, not a pause for the user.
    1. Location: git rev-parse --show-toplevel is C:/projects/mazeworld and git rev-parse --git-dir prints .git (not a worktree path). If this plan was started in a worktree, stop and report that it must run in the main checkout.
    2. Secrets: test -f android/keystore.properties. Check that git check-ignore -q android/keystore.properties exits 0. Count lines matching the key names storeFile, storePassword and keyAlias with a non-empty value, using grep -cE on the key names only. Expect 3. Never cat, echo, print, copy or log the file or any value in it, and never pass it to git add.
    3. Tree: git status --porcelain must show nothing staged or modified. The only untracked entries allowed are sfx/theme.mp3 and .claude/worktrees/. All 69-01, 69-02 and 69-03 work must already be merged on master (check with git log for their SUMMARY commits).
    4. Hold the clip aside: if sfx/theme.mp3 exists, record its sha256 and byte size, then move it to the session scratchpad (outside the repo). Record the exact scratchpad path in the SUMMARY. Then git status --porcelain --untracked-files=all over sfx, src, engine, content, assets, fonts, icons, vendor and mazeworld.html must print nothing.
    5. Suite: npm test. With the clip held aside, AUD-06 passes too, so expect 0 failures on master. Any failure stops the plan (restore the clip first).
    6. Bump check: node tools/bump-version.mjs --dry-run --name 2.0.0 must report versionCode 8 -> 9 and versionName 1.9.0 -> 2.0.0. Any other starting value means someone already bumped, and the plan stops.
    7. Bump: npm run version:bump -- --name 2.0.0. Confirm android/version.properties now reads versionCode=9 and versionName=2.0.0 and nothing else in the file changed.
  </action>
  <verify>
    <automated>grep -E "^version(Code|Name)=" android/version.properties && git check-ignore -q android/keystore.properties && echo keystore-ignored</automated>
  </verify>
  <acceptance_criteria>
    - android/version.properties has versionCode=9 and versionName=2.0.0
    - The SUMMARY records the clip's sha256, byte size and scratchpad path, and "npm test: 0 failures (clip held aside)"
    - No keystore value appears anywhere in the transcript-visible outputs, the SUMMARY or git
  </acceptance_criteria>
  <done>The main checkout is clean, the clip is out of the build's reach, the suite is green and the version reads 2.0.0 (9).</done>
</task>

<task type="auto">
  <name>Task 2: Build the signed AAB, verify it, restore the clip, and run the build-level audit</name>
  <files>store-listing/LISTING.md</files>
  <read_first>
    - store-listing/LISTING.md (the Data safety section and 69-01's source-level audit)
    - .planning/phases/67-play-games-integration-account-chip/67-06-SUMMARY.md (the "Dependency audit" section, for comparison)
  </read_first>
  <action>
    Per D-07 and D-04. Record the build start time, then run npm run android:release. It runs cap sync, pin-jdk and the signed bundleRelease, and does not bump. As soon as the command returns, pass or fail, move the held clip back to sfx/theme.mp3 and confirm its sha256 matches the recorded value. If the build failed, revert android/version.properties, stop and report the error (the last 40 lines of output, with no secrets).
    Verify the AAB at android/app/build/outputs/bundle/release/app-release.aab:
    (a) it exists and is newer than the build start;
    (b) keytool -printcert -jarfile on it prints a certificate. Record its SHA-256 fingerprint and owner line, which are public. An unsigned bundle prints no certificate, and that fails the plan;
    (c) byte size and sha256;
    (d) its entry listing (unzip -l, or jar tf) has 30 .mp3 entries under the bundled public sfx folder and no entry for the held-aside clip. Record the count, and record the entry path of the bundled Play Games plugin vendor folder;
    (e) the merged release manifest, found under android/app/build/intermediates with a path containing both "merged" and "release", has android:versionCode="9", android:versionName="2.0.0", the com.google.android.gms.games.APP_ID meta-data, and its uses-permission list, with no AD_ID. Record the list;
    (f) git diff --quiet -- android/gradle.properties android/build.gradle android/variables.gradle android/gradle/wrapper exits 0 (the toolchain pin is untouched).
    Build-level audit (D-04):
    - node tools/gradle.mjs :app:dependencies --configuration releaseRuntimeClasspath, with output to the scratchpad.
    - A case-insensitive grep of that report for firebase, admob, play-services-ads, ads-identifier, analytics, measurement, crashlytics, appsflyer, adjust, facebook, appcenter, sentry and bugsnag must find 0 hits.
    - Record the com.google.android.gms artifact versions and compare them with 67-06's table, noting any change.
    - Confirm android/app/google-services.json does not exist, so the google-services plugin is not applied.
    - Re-run 69-01's www/ network-API grep on the www/ this build produced, and compare the classified file list with the one in LISTING.md.
    If the audit finds a forbidden artifact, or a network-API hit 69-01 did not classify: do not tag. Revert android/version.properties, leave the AAB uncommitted and unrecorded, record a "Defect findings (fix plan needed)" entry in the SUMMARY, and stop.
    Otherwise, append to store-listing/LISTING.md's Data safety section a "Build-level audit (2.0.0, versionCode 9, <date>)" subsection with: the command, the forbidden-grep result, the play-services artifact table, google-services not applied, the merged release manifest permissions, the bundled www/ network-API files (same as the source-level list, or the differences), and the AAB sha256 it was run against. Run node --test test/unit/store-listing.test.js; it must still pass.
  </action>
  <verify>
    <automated>test -f android/app/build/outputs/bundle/release/app-release.aab && keytool -printcert -jarfile android/app/build/outputs/bundle/release/app-release.aab | grep -c "SHA256:" && node --test test/unit/store-listing.test.js</automated>
  </verify>
  <acceptance_criteria>
    - keytool -printcert on the AAB prints at least one SHA256 fingerprint line (signed)
    - The AAB entry listing has 30 sfx .mp3 entries; the held-aside clip is absent from the listing (recorded as a count of 0)
    - The merged release manifest shows versionCode 9 / versionName 2.0.0
    - sfx/theme.mp3 is back in place with its recorded sha256, and still untracked
    - LISTING.md contains "releaseRuntimeClasspath" and the AAB sha256; node --test test/unit/store-listing.test.js passes
  </acceptance_criteria>
  <done>A signed 2.0.0 (9) AAB exists, is verified from the inside, carries nothing the user did not ship, and its dependency tree is audited and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Record the Play build, commit the release, tag v2.0.0-play9</name>
  <files>docs/UAT-v2.0.md, android/version.properties, store-listing/LISTING.md</files>
  <read_first>
    - docs/UAT-v2.0.md (the header's Build paragraph)
  </read_first>
  <action>
    Per D-07 and D-08. In docs/UAT-v2.0.md, replace the Build paragraph's sentence about the Play build with a filled "Play build:" line. It gives 2.0.0 (versionCode 9), android/app/build/outputs/bundle/release/app-release.aab, the byte size, the sha256, the upload-key certificate SHA-256 fingerprint, "built from commit <the release commit, filled after committing, see below>", the tag v2.0.0-play9, and "the user uploads it to closed testing by hand". Add one sentence on the Play Games state of this build. If the IDs were still the pre-console values, sign-in fails gracefully and boards are skipped until the console setup and a rebuild (section 0). If the user had already supplied them, say so. Keep the sentence saying the orchestrator adds the debug APK line at the close (D-08).
    Commit android/version.properties, docs/UAT-v2.0.md and store-listing/LISTING.md in one commit. Subject: "chore(release): 2.0.0 (versionCode 9) — v2.0 Leaderboards for Play closed testing". The body gives the signed AAB path, byte size and sha256, the certificate fingerprint, and one audit line (releaseRuntimeClasspath 0 forbidden hits; permissions list), followed by the session attribution trailers. Stage the three paths by name only. Never git add -A, never stage sfx/theme.mp3 or anything under android/app/build, and never stage android/keystore.properties.
    Because the UAT line names the release commit, which only exists after committing, write "the release commit tagged v2.0.0-play9" in the doc rather than a hash. The tag is the stable pointer, so no amend is needed.
    Tag: git tag v2.0.0-play9 (lightweight, on HEAD, matching v1.9.0-play8). Check that git rev-parse v2.0.0-play9 equals git rev-parse HEAD. Do not push the commit or the tag; the orchestrator pushes at the milestone close. Do not upload anything to Play.
    Final state check: git status --porcelain shows only the untracked sfx/theme.mp3 and .claude/worktrees/ entries.
  </action>
  <verify>
    <automated>git rev-parse v2.0.0-play9 && test "$(git rev-parse v2.0.0-play9)" = "$(git rev-parse HEAD)" && git show --stat HEAD --format=%s | head -5 && grep -c "v2.0.0-play9" docs/UAT-v2.0.md</automated>
  </verify>
  <acceptance_criteria>
    - HEAD's subject is "chore(release): 2.0.0 (versionCode 9) — v2.0 Leaderboards for Play closed testing", and it changes exactly android/version.properties, docs/UAT-v2.0.md and store-listing/LISTING.md
    - The tag v2.0.0-play9 exists locally and resolves to HEAD; git ls-remote is not run, and nothing is pushed
    - docs/UAT-v2.0.md's header has the Play build line with the sha256 and "v2.0.0-play9"
    - git status --porcelain lists only sfx/theme.mp3 and .claude/worktrees/ as untracked, with nothing staged or modified
  </acceptance_criteria>
  <done>The milestone's signed AAB is recorded, committed and tagged locally, waiting for the user's upload; the debug APK is left to the orchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git-ignored keystore.properties → the build | Upload-key credentials enter the Gradle signing config |
| the working tree → the AAB | Every file build-www copies ships to players |
| local repo → Play / origin | Upload and push are the user's and the orchestrator's, not this plan's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-10 | Information disclosure | android/keystore.properties | critical | mitigate | Existence and key-name checks only (grep -cE on names), git check-ignore confirmed, three paths staged by name, never cat or echo; SUMMARY states that no secret was printed |
| T-69-11 | Tampering | AAB contents | high | mitigate | The untracked clip is held outside the repo during build-www and restored byte-identical (sha256); an untracked-file scan of every copied directory must be empty; the AAB listing is checked for 30 sfx clips and no extra |
| T-69-12 | Spoofing | unsigned or wrongly signed bundle | high | mitigate | keytool -printcert must show the upload-key certificate before tagging; its fingerprint is recorded |
| T-69-13 | Tampering (supply chain) | release runtime classpath | high | mitigate | Build-level releaseRuntimeClasspath audit with the forbidden-name grep; any hit blocks the tag and becomes a fix plan |
| T-69-14 | Elevation of privilege | Play upload / git push | medium | mitigate | No upload command, no push; the tag is local; the user uploads by hand (standing rule) |
| T-69-15 | Denial of service (release) | versionCode reuse | medium | mitigate | Dry-run must show exactly 8 -> 9 before bumping; play:release is not run after the manual bump |
</threat_model>

<verification>
- android/version.properties: versionCode=9, versionName=2.0.0
- The AAB is signed (keytool -printcert), holds 30 sfx clips and not the untracked clip, and its merged manifest says 9 / 2.0.0
- The releaseRuntimeClasspath audit found 0 forbidden hits and is recorded in LISTING.md; node --test test/unit/store-listing.test.js passes
- The v2.0.0-play9 tag is on the release commit; nothing pushed or uploaded; sfx/theme.mp3 restored and untracked
</verification>

<success_criteria>
- COMPLY-04 (build half): a signed AAB with PGS for the testing track exists as 2.0.0 / versionCode 9, recorded and tagged; the user uploads it (D-07)
- COMPLY-02 (shipped-build evidence): the Data safety answers are backed by the build-level audit of this exact AAB (D-04)
- D-08 is left to the orchestrator's milestone close, with the install warning already in docs/UAT-v2.0.md
</success_criteria>

## Flagged assumptions (edge probe coverage69.json)

- COMPLY-04 adjacency: versionCode 9 sits directly after the 8 already on Play; the dry-run guard refuses any other start, so a reused or skipped code cannot slip through. The Play build and the debug APK share a package but not a signer (the D-08 warning).
- COMPLY-04 empty: a missing or incomplete keystore.properties stops the plan before the bump; an unsigned bundle (no certificate) fails before the tag; an empty untracked-file scan is required before build-www.
- COMPLY-04 ordering: bump, then build, then verify, then audit, then record, then commit, then tag. The tag never precedes a verified build, and the clip is restored right after the build whatever the outcome.

<output>
Create `.planning/phases/69-compliance-device-close/69-04-SUMMARY.md` when done. Include: the pre-flight results (with no secret values), the clip's sha256 before and after, the build duration, the AAB path, size, sha256 and certificate fingerprint, the entry counts, the merged-manifest facts, the audit table, the commit hash and the tag. Add a "Human verification (deferred to end of run)" heading listing: upload the AAB to closed testing (user, by hand); and the orchestrator's D-08 debug APK build and Pixel 7 install offer, noting that the install requires an uninstall that deletes save data.
</output>
