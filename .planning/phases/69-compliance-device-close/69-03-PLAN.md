---
phase: 69-compliance-device-close
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/UAT-v2.0.md
autonomous: true
requirements: [COMPLY-04]
user_setup: []

must_haves:
  truths:
    - "docs/UAT-v2.0.md is one batched Pixel 7 checklist in the house format of docs/UAT-v1.9.md: a header (Build, Protocol, Sources, Suggested order), lettered sections with | # | Step | Who | Result | tables, and a closing Tally line (D-09)"
    - "It merges every human_verification item from the Phase 65, 66, 67 and 68 VERIFICATION files and every extra item from those phases' SUMMARY 'Human verification (deferred to end of run)' sections; a Source map appendix accounts for each source item as its own row, merged into a row, or superseded, with the reason (D-09)"
    - "Sections follow D-09's grouping, in its order: the new-best block; the Leaderboards panel on every board; title vs tab entry and back; sign-in (auto, decline, no profile); the account chip and menu; Compete off with a network capture; the offline queue and flush; 'you placed X' (live and deferred rail card); seasons; airplane mode (D-09)"
    - "The Phase 67 Compete-OFF cold-boot network capture is marked RELEASE-BLOCKING, is the first row of its section and is first in the Suggested order after section 0 (D-09, 67 D-20, 67 research Assumption A1)"
    - "Section 0 lists the user's deferred non-device items: the Play Console runbook walk (D-06), sending the IDs for the rebuild, uploading the 2.0.0 (9) AAB to closed testing by hand (D-07), deploying the website (D-02), entering the Data safety answers and URLs (D-04), pasting the listing copy (D-05), the diagnostics decision after the capture, and the delete-data steps check"
    - "The Protocol says the batch is written and committed, not run mid-milestone, and carries the D-08 install warning: the Play build and the debug APK have different signers, so installing one over the other needs an uninstall, which deletes the run, graveyard, bests, settings and any queued submissions (D-08)"
    - "Rows that need the user's console setup, or a signed-in tester, are marked in the step text, and a debug-APK sign-in test is noted as needing the optional debug-keystore credential"
  artifacts:
    - path: "docs/UAT-v2.0.md"
      provides: "the v2.0 batched device checklist"
      contains: "RELEASE-BLOCKING"
  key_links:
    - from: "docs/UAT-v2.0.md Source map"
      to: ".planning/phases/65..68/*-VERIFICATION.md human_verification entries"
      via: "one [NN-Vk] token per source item, k in frontmatter order"
      pattern: "\\[68-V16\\]"
---

<objective>
Write the milestone's one batched Pixel 7 checklist, docs/UAT-v2.0.md. Implements D-09: it merges every human_verification item from the Phase 65 to 68 VERIFICATION files and their SUMMARY extras, is grouped by D-09's areas, uses the docs/UAT-v1.9.md house format, and marks the Compete-OFF capture RELEASE-BLOCKING. It carries D-08's install warning in its Protocol. Its section 0 lists the deferred user actions from D-02, D-04, D-05, D-06 and D-07 (website deploy, Data safety entry, listing copy, console runbook, AAB upload), so nothing waiting on the user is scattered across files.

Purpose: COMPLY-04's checklist half. It is written and committed, not run: the deferred-UAT protocol batches the walk at the milestone close.
Output: docs/UAT-v2.0.md. The Play build line is added by 69-04, and the debug APK line by the orchestrator at the close (D-08).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/69-compliance-device-close/69-CONTEXT.md
@docs/UAT-v1.9.md

<interfaces>
Source counts the planner read on 2026-09-24 (human_verification entries in each VERIFICATION frontmatter): 65 has 10, 66 has 18, 67 has 15 and 68 has 16. The verify command recounts them from the files, so the doc must use the live counts.
SUMMARY files with a "Human verification (deferred to end of run)" section that lists items: 65-03, 65-04, 65-05, 66-02, 66-03, 66-04, 66-05, 66-06, 66-07, 67-01, 67-02, 67-03, 67-04, 67-05, 67-06, 67-07, 67-08, 68-01, 68-02, 68-03, 68-04, 68-05, 68-06, 68-07. The sections in 65-01, 65-02 and 66-01 say None.
Known merges and supersessions to apply (check each against the files):
- 65-04's three SUMMARY items repeat 65-VERIFICATION items 2 to 4 and merge into them.
- 67-VERIFICATION item 1 (the release-blocking Compete-OFF cold boot), 67-01's and 67-06's release-blocking items, and 67-08 item 5's capture clause are the same check and become one RELEASE-BLOCKING row. 68-VERIFICATION's "Compete OFF, die: no rank line ... (capture)" is a different moment (a death, not a cold boot), so it stays its own row in the same section.
- 67-04 item 2 and 67-VERIFICATION's "ALL/FRIENDS ... show the coming-online notes" were superseded by Phase 68's live ALL/FRIENDS views. Map them to the 68 rows with the note "superseded by Phase 68 live boards". Keep 67's Compete-OFF restores the dimmed chips and the GRAVEYARD has no identity strip as rows.
- 68-01's console items (create the five boards, paste the IDs, confirm the tag in the console) merge into section 0 and the post-console rows.
- 68-02 item 3 (the optional rank-after-submit lag check) and 68-06 item 6 (season chips wrap once a second season exists) are extras with no VERIFICATION twin, and each gets its own row.
- 68-07 item 6 (the browser dev loop with pgsDevSignedIn) is a desk check, not a device check. It goes in a final desk section.
House format (docs/UAT-v1.9.md): an H1 naming the version and phase; bold **Build:**, **Protocol:**, **Sources:** and **Suggested order:** paragraphs; "## A. Title — Phase NN (count)" sections; 4-column tables with a Result column; "---" then a **Tally** line.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the merged item inventory and write docs/UAT-v2.0.md</name>
  <files>docs/UAT-v2.0.md</files>
  <read_first>
    - docs/UAT-v1.9.md (house format)
    - .planning/phases/65-run-record-personal-bests/65-VERIFICATION.md (frontmatter)
    - .planning/phases/66-leaderboards-panel-local/66-VERIFICATION.md (frontmatter)
    - .planning/phases/67-play-games-integration-account-chip/67-VERIFICATION.md (frontmatter)
    - .planning/phases/68-global-boards-submissions-you-placed-x/68-VERIFICATION.md (frontmatter)
    - The "Human verification (deferred to end of run)" section of each SUMMARY listed in the interfaces block (extract with awk between that heading and the next "## " heading; do not read whole SUMMARYs)
  </read_first>
  <action>
    Per D-09. Step 1: in the session scratchpad, build an inventory. Number each VERIFICATION item [NN-Vk], with k in frontmatter order starting at 1. Number each SUMMARY extra [NN-PP-Sk]. Mark each one own-row, merged into another item, or superseded (with the reason), applying the merges and supersessions in the interfaces block and any other exact duplicate you find. Items that say the same check in different words are one row. Items at different moments (for example a cold boot and a death) are two rows.
    Step 2: write docs/UAT-v2.0.md in the house format.
    - H1: "v2.0 Pixel 7 device round — Phase 69 (Compliance & Device Close)".
    - **Build:** "the Play build and the debug APK are recorded here when built: the 2.0.0 (versionCode 9) AAB by 69-04, the milestone's debug APK by the orchestrator at the close (D-08)."
    - **Protocol:** the deferred-UAT protocol. The batch is written and committed in Phase 69, not run mid-milestone. Results are recorded as pass, "fail: what you saw", or not reached, and a skipped "if available" row is not reached, never a pass. Findings become todos, never mid-walk edits. Include the D-08 install warning (different signers; an uninstall deletes the run, graveyard, bests, settings and any queued submissions). Sign-in and leaderboard rows need the console setup; on a local debug APK they also need the optional debug-keystore credential (docs/PLAY-GAMES-SETUP.md section 3), otherwise use the Play build from closed testing.
    - **Sources:** the four VERIFICATION files with their counts, plus the SUMMARY files that contributed extras.
    - **Suggested order:** section 0 first; then F1, the RELEASE-BLOCKING capture, on a fresh install with Compete OFF before any sign-in; then the no-console sections; then the after-console sections; then airplane mode last. Adjust if the inventory suggests a better walk, and keep F1 first after 0.
    - "## 0. Before the walk — console, website and upload (user)": numbered rows with Who "user", covering: walk docs/PLAY-GAMES-SETUP.md section 6 and confirm each menu path (D-06); send Claude the APP_ID and five leaderboard IDs, after which Claude rebuilds with a versionCode bump, because the 2.0.0 (9) AAB still carries the 12-zero APP_ID and unfilled board IDs, so sign-in fails gracefully until then; upload the 2.0.0 (9) AAB to closed testing by hand (D-07; never uploaded by Claude); review the darktier-studio commit recorded in store-listing/LISTING.md and deploy the site (D-02); enter the Data safety answers and both URLs from store-listing/LISTING.md (D-04); paste the full description (D-05); if store-listing/LISTING.md's Screenshots section marks the graveyard shot as owed, regenerate it (D-05); after F1, make the diagnostics decision recorded in LISTING.md's Data safety section, and confirm the privacy page's Compete-off paragraph matches what F1 showed (edit and redeploy if not); on the Pixel 7, open the Play Games app and confirm the delete-data page's steps match its menus; only if testing sign-in on a debug APK, run the runbook's debug-keystore keytool command and confirm it prints a SHA-1.
    - Sections A to J, in D-09's order: A new personal best block and run record (Phase 65); B the Leaderboards panel on every board (Phases 66 and 68); C title vs tab entry and back (Phases 66 and 67); D sign-in: auto, decline, no profile (Phase 67); E the account chip and menu (Phase 67); F Compete OFF and the network capture (Phases 67 and 68); G the offline queue and flush (Phase 68); H "you placed X", live line and deferred rail card (Phase 68); I seasons (Phase 68); J airplane mode (Phases 66, 67 and 68). Voice read-throughs sit in the area they read. If a voice item spans areas, add "K. Voice read-through at default and largest text sizes". End with "Z. Desk check — browser dev loop" for the dev-loop item. Put each section's item count in its heading, as in v1.9.
    - Section B names every board: DEEPEST, LEANEST, LONGEST, BUTCHERY, PURSE, LINEAGE and GRAVEYARD. Add three edge rows, each marked "(edge)": a fresh install with no deaths shows every board's empty state; a device with exactly one death shows it on every board; and two local runs with the same LEANEST rate list the deeper run first.
    - F1: bold RELEASE-BLOCKING at the start of the step. Capture adb logcat plus a per-app network capture for the game's package on a Compete OFF cold boot. Expected: no Play Games sign-in, submit or fetch from the game. Google's own SDK start-up and a possible "Welcome back" banner are expected (67 D-20); record exactly what they do, for the privacy text and the diagnostics decision.
    - Mark prerequisites in italics at the start of a step, like v1.9's "if available" notes: "(after console setup)", "(signed in, Compete ON)", "(no Play Games profile)".
    - Who is "user" on every device row. Result is "open" on every row.
    - End with "---" and "**Tally (written 2026-09-24):** 0 walked. The batch is run at the milestone close per the deferred-UAT protocol; RELEASE-BLOCKING row F1 must pass before any production rollout."
  </action>
  <verify>
    <automated>grep -c "RELEASE-BLOCKING" docs/UAT-v2.0.md && grep -cE "^## [0A-Z]\. " docs/UAT-v2.0.md && grep -c "| # | Step | Who | Result |" docs/UAT-v2.0.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/UAT-v2.0.md has sections 0 and A to J, in D-09's order, and Z (K only if used)
    - Every section table has the header row "| # | Step | Who | Result |"
    - F1 starts with **RELEASE-BLOCKING**, and the Suggested order names F1 first after section 0
    - The Protocol contains the D-08 install warning, and section 0 names the AAB upload by the user, the website deploy, the Data safety entry and the runbook walk
  </acceptance_criteria>
  <done>The v2.0 checklist exists in the house format, with the release-blocking capture first and every deferred user action in section 0.</done>
</task>

<task type="auto">
  <name>Task 2: Source map appendix and the completeness check</name>
  <files>docs/UAT-v2.0.md</files>
  <read_first>
    - docs/UAT-v2.0.md (from Task 1)
    - the scratchpad inventory from Task 1
  </read_first>
  <action>
    Per D-09. Between the last section and the "---" Tally line, append "## Source map". It is a table with the columns Source, Item (short paraphrase), Row and Disposition, and one line per inventory entry. Source uses the bracketed token ([65-V1] ... [68-V16], [67-04-S2], and so on). Row is the UAT row id, for example B3. Disposition is own row, merged, or superseded, with the reason. Every VERIFICATION item must appear exactly once in the Source column. Then run the verify command and fix any unmapped item before finishing. Finally run npm test (the doc is not under test; this confirms nothing else moved).
  </action>
  <verify>
    <automated>node -e 'const fs=require("fs");const d=fs.readFileSync("docs/UAT-v2.0.md","utf8");const P={65:"65-run-record-personal-bests",66:"66-leaderboards-panel-local",67:"67-play-games-integration-account-chip",68:"68-global-boards-submissions-you-placed-x"};const miss=[];let total=0;for(const n of Object.keys(P)){const fm=fs.readFileSync(".planning/phases/"+P[n]+"/"+n+"-VERIFICATION.md","utf8").split(/^---\r?$/m)[1];const c=(fm.split("human_verification:")[1].match(/^\s+- /gm)||[]).length;total+=c;for(let k=1;k<=c;k++){if(!d.includes("["+n+"-V"+k+"]"))miss.push(n+"-V"+k)}}if(miss.length){console.error("unmapped:",miss.join(" "));process.exit(1)}console.log("all "+total+" VERIFICATION items mapped")'</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints "all N VERIFICATION items mapped" and exits 0
    - Every SUMMARY extra in the inventory has a Source map line
    - npm test shows no failures beyond the known set (on master: AUD-06; in a worktree: the 7 CRLF doc-ledger tests)
  </acceptance_criteria>
  <done>Every deferred check from Phases 65 to 68 is provably in the batch, and every merge or supersession has its reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checklist → release decision | A missing or softened release-blocking check could let a leaking build reach production |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-69-08 | Information disclosure | F1 Compete-OFF capture | high | mitigate | F1 is marked RELEASE-BLOCKING, walked first on a fresh install, and the Tally line repeats that it must pass before any production rollout |
| T-69-09 | Repudiation | merged/superseded items | low | mitigate | The Source map records every source item's disposition and reason; the verify command proves every VERIFICATION item is mapped |
</threat_model>

<verification>
- The Source-map completeness command exits 0
- docs/UAT-v2.0.md has the house header, sections 0 and A to J in D-09 order, and the Tally line
- npm test: no failures beyond the known set
</verification>

<success_criteria>
- COMPLY-04 (checklist half): the milestone's Pixel 7 batch is written as docs/UAT-v2.0.md, covering sign-in, decline, the offline queue and flush, the panel on every board, the "you placed X" card, the account chip and airplane mode, as one batched checklist that is not run mid-milestone (D-09)
</success_criteria>

## Flagged assumptions (edge probe coverage69.json)

- COMPLY-04 adjacency: two items that describe the same check merge into one row that cites both sources. Two items at adjacent but different moments (a Compete-OFF cold boot, and a Compete-OFF death) stay separate rows.
- COMPLY-04 empty: a SUMMARY whose section says None contributes nothing, and the Source map lists only real items. Section B has an explicit empty-state row and a one-death row for every board.
- COMPLY-04 ordering: rows keep source order within a section unless the walk needs otherwise. F1 is first after section 0. Equal local LEANEST rates list the deeper run first (an edge row).

<output>
Create `.planning/phases/69-compliance-device-close/69-03-SUMMARY.md` when done, with the per-phase item counts, the merges and supersessions applied, and the section row counts. No "Human verification" items of its own: the whole file is the deferred batch.
</output>
