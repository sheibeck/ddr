---
phase: 17-fixture-inventory-foe-turn-refactors
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parity/harness/fixtureRoster.js
  - tools/fixture-inventory.mjs
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/fixture-inventory.test.js
autonomous: true
requirements: [FID-01]
tags: [parity, fixtures, bestiary, inventory, determinism]

must_haves:
  truths:
    - "FID-01 boundary: the inventory states and the pinned test asserts that every fixture-rolled foe is level 1 because startCombat computes maxLvl = clamp(min(c.level, depth), 1, 5) with level 1 / depth 1 and the d4 downgrade roll clamps at the floor of 1 — one step either side (a level-2 hero on depth 1, or depth 2 with a level-1 hero) still yields maxLvl 1, so only depth >= 2 AND level >= 2 can expose a level-2 entry"
    - "FID-01 adjacency: when a scenario rolls the same creature twice (seed 303 rolls Dante x2) the generated table lists both instances in C.foes order (not merged), while the exposed-surface summary dedupes to the (type, name) set — the test asserts both the 2-instance row and the 4-name set"
    - "FID-01 empty: every fixture/scenario that never starts combat (magic heal/potion/scroll, all 5 encounters scenarios, economy, movement seed 256, chargen) appears as an explicit trigger=none row with an empty foes list — never omitted — and a startCombat that leaves state.combat null would be recorded as an explicit empty-foes row rather than dropped"
    - "FID-01 ordering: rows are emitted in fixed fixture order (chargen, movement, combat, magic, economy, encounters) then in-file scenario order, and foes within a row in state.combat.foes array order (the order startCombat rolled them); the test's deepStrictEqual pins this exact order"
    - "FID-01 precision: each row's seed is the fixture JSON's integer seed copied verbatim (the test asserts row.seed === scenario.seed with strict equality for every row) — no seed is re-derived, hashed, or coerced"
    - "The committed FIXTURE-INVENTORY.md table between the generated markers is byte-equal (after CRLF normalisation and trim) to rosterToMarkdown(enumerateFixtureRoster()) — the test fails if the document drifts from what the harness actually rolls"
    - "The inventory calls out that combat/magic fixtures only force Beasts and Humans, that the complete parity-exposed bestiary surface is Bat/Rat, Shriek, Viper (Beasts lvl 1) and Dante (Humans lvl 1), and that BESTIARY.Beasts[0] / BESTIARY.Humans[0] array length and order are load-bearing for rng.pick"
  artifacts:
    - path: "test/parity/harness/fixtureRoster.js"
      provides: "enumerateFixtureRoster() replay + rosterToMarkdown() renderer, reusing applyStartCombat/applyAction/runEconomyAction"
      exports: ["enumerateFixtureRoster", "rosterToMarkdown", "FIXTURE_ORDER"]
    - path: "tools/fixture-inventory.mjs"
      provides: "CLI that prints the generated markdown table (or --json rows) for regenerating the document"
      contains: "fixtureRoster.js"
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "The committed FID-01 inventory document with generated table + Phase 18 constraints"
      contains: "fixture-inventory:generated:begin"
      min_lines: 40
    - path: "test/parity/fixture-inventory.test.js"
      provides: "Pinned roster + document-consistency regression test"
      contains: "enumerateFixtureRoster"
  key_links:
    - from: "test/parity/harness/fixtureRoster.js"
      to: "test/parity/harness/comparables.js"
      via: "imports applyStartCombat and runEconomyAction (never re-implements startCombat / rng rehydration)"
      pattern: "import \\{[^}]*applyStartCombat[^}]*\\} from \"./comparables.js\""
    - from: "test/parity/fixture-inventory.test.js"
      to: "test/parity/harness/fixtureRoster.js"
      via: "imports enumerateFixtureRoster + rosterToMarkdown and pins their output"
      pattern: "from \"./harness/fixtureRoster.js\""
    - from: "test/parity/fixture-inventory.test.js"
      to: "test/parity/FIXTURE-INVENTORY.md"
      via: "reads the doc, slices the marker-delimited block, asserts equality with the generated table"
      pattern: "fixture-inventory:generated:begin"
    - from: "tools/fixture-inventory.mjs"
      to: "test/parity/harness/fixtureRoster.js"
      via: "imports the same enumerate/render functions the test pins"
      pattern: "fixtureRoster.js"
  prohibitions:
    - requirement_id: FID-01
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any test/parity/fixtures/*.json, or test/parity/harness/comparables.js — the inventory describes the frozen fixtures, it never changes them"
    - requirement_id: FID-01
      category: values
      statement: "MUST NOT derive any roster row by reasoning about seeds or reading bestiary arrays by hand — every foes cell comes from an actual replay through the harness (applyStartCombat / applyAction / runEconomyAction)"
    - requirement_id: FID-01
      category: safety
      statement: "MUST NOT add a runtime or dev dependency (package.json unchanged); the enumeration uses node:test, node:fs, node:path and the existing engine/harness modules only"
---

<objective>
Produce the FID-01 fixture inventory: a repeatable enumeration (harness module + CLI) that REPLAYS every parity fixture through the existing engine harness and records exactly which `content/bestiary.js` creatures each fixture seed rolls; a committed `test/parity/FIXTURE-INVENTORY.md` whose table is generated by that replay; and a pinned `node:test` regression test that fails if the roster or the document ever drifts. This is the document Phase 18 consults before touching any bestiary number.

Purpose: Phase 18 (BEST-03 / FID-05) may only change fixture-exercised creatures through narrow, named `comparables.js` carve-outs. Without a replay-verified inventory, a rebalance of the wrong creature silently breaks byte-identical parity. Research already executed the enumeration once (17-RESEARCH.md "Fixture Inventory (FID-01) — Verified Results") and the planner re-ran it on 2026-09-13 with identical results; this plan turns that one-off into a committed, self-checking artifact.

Output: `test/parity/harness/fixtureRoster.js`, `tools/fixture-inventory.mjs`, `test/parity/FIXTURE-INVENTORY.md`, `test/parity/fixture-inventory.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-CONTEXT.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-PATTERNS.md
@test/parity/harness/comparables.js
@test/parity/full-suite.test.js
@test/parity/fixtures/README.md
@tools/tune-difficulty.mjs
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `enumerateFixtureRoster()` | exported function, `test/parity/harness/fixtureRoster.js` | 17-01 | replays every fixture; returns ordered rows `{ fixture, scenario, seed, trigger, forced, foes[] }` |
| `rosterToMarkdown(rows)` | exported function, `test/parity/harness/fixtureRoster.js` | 17-01 | renders the rows as the exact markdown table the doc embeds |
| `FIXTURE_ORDER` | exported const, `test/parity/harness/fixtureRoster.js` | 17-01 | `["chargen","movement","combat","magic","economy","encounters"]` |
| `tools/fixture-inventory.mjs` | dev-only CLI | 17-01 | prints the generated table (`--json` for rows) |
| `test/parity/FIXTURE-INVENTORY.md` | committed document | 17-01 (+ 17-03 appends the draw-count baseline section) | FID-01 deliverable |
| `test/parity/fixture-inventory.test.js` | `node:test` file | 17-01 | pins roster + doc consistency |
| `pickFoeTarget(state, rng)` | exported function, `engine/combat.js` | 17-02 | see 17-02 |
| `applyFoeDamageToPlayer(state, foe, rng, events, { dmg, roll, need })` | exported function, `engine/combat.js` | 17-02 | see 17-02 |
| `countingRng(inner)` | test-local helper, `test/unit/foe-turn-draw-count.test.js` | 17-03 | Phase 19 copies it verbatim |
| `test/unit/foe-turn-draw-count.test.js` | `node:test` file | 17-03 | FID-02 pinned baseline |

<tasks>

<task type="auto">
  <name>Task 1: Build the replay-based roster enumeration (harness module + CLI)</name>
  <files>test/parity/harness/fixtureRoster.js, tools/fixture-inventory.mjs</files>
  <read_first>
    - test/parity/harness/comparables.js (applyStartCombat lines 199-206; runEconomyAction lines 312-323 and its INTERNAL_FNS; note runEconomyAction needs the prototype sandbox ctx — do not export or modify anything in this file)
    - test/parity/full-suite.test.js (lines 100-185: how each fixture domain is replayed — movement via applyAction, combat/magic via applyStartCombat + applyAction, economy/encounters via loadPrototypeSandbox + runEconomyAction with gold set to 5000 on BOTH sides before the economy script)
    - test/parity/harness/sandboxPrototype.js (loadPrototypeSandbox({ seed }) signature, line 154)
    - test/parity/fixtures/action-script.{chargen,movement,combat,magic,economy,encounters}.json (shapes: combat/magic/encounters are { _note, scenarios[] } with per-scenario name/seed/actions; movement/economy are { seed, actions[] }; chargen is { seed, seeds[], actions: [] })
    - engine/combat.js lines 98-135 (startCombat: maxLvl = clamp(min(c.level, depth), 1, 5); lvl = clamp(maxLvl - (d4===1 ? 1 : 0), 1, 5); roster = BESTIARY[type][lvl-1]; picked = rng.pick(roster) — this is why array order/length is load-bearing)
    - tools/tune-difficulty.mjs lines 1-30 (dev-only ESM script header convention: shebang, "NOT shipped, NOT a node:test file", Run: examples)
    - .planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md "Fixture Inventory (FID-01) — Verified Results" (the expected output; the method section shows the applyStartCombat + newRun shape to reuse)
  </read_first>
  <action>
    Create `test/parity/harness/fixtureRoster.js` (an ESM harness module beside comparables.js/diffState.js — a library, NOT a `*.test.js` file, so `node --test` never picks it up). Export three things:

    1. `FIXTURE_ORDER` — the const array `["chargen", "movement", "combat", "magic", "economy", "encounters"]`; rows are always emitted in this fixture order, then in-file scenario order.

    2. `enumerateFixtureRoster()` — synchronous, no arguments; reads every `test/parity/fixtures/action-script.<name>.json` in FIXTURE_ORDER (resolve the fixtures dir from `import.meta.url` exactly like full-suite.test.js does) and REPLAYS each script through the existing harness, returning an array of row objects `{ fixture, scenario, seed, trigger, forced, foes }` where `fixture` is the json filename, `scenario` is the scenario name (or the literal string `(script)` for the single-script movement/economy fixtures, or `(<N> seeds, no actions)` for chargen), `seed` is the fixture's integer seed copied verbatim, `trigger` is one of `"startCombat"` / `"wandering"` / `"none"`, `forced` is the startCombat action's `forced` string or `null`, and `foes` is an array of `{ name, type, lvl, wp }` snapshotted from `state.combat.foes` at the moment `state.combat` transitions from null to non-null (in array order). Replay rules, mirroring full-suite.test.js line-for-line: combat + magic scenarios start from `newRun(seed)`; a `startCombat` action goes through `applyStartCombat(state, action.wandering, action.forced)` (imported from `./comparables.js`), every other action through `applyAction(state, action)` from `engine/engine.js`; movement replays its 101 actions through `applyAction` and records any combat that starts as `trigger: "wandering"` (this is the executed proof that seed 256 never rolls a wandering monster — do not rely on the fixture's `_note`); economy and encounters need the prototype sandbox because their internal actions dispatch through `runEconomyAction(ctx, engineState, action)` — call `loadPrototypeSandbox({ seed })`, set `ctx.S.c.gold = 5000` and `engineState.c.gold = 5000` before the economy script exactly as full-suite does, and check `state.combat` after every action; chargen has no actions, so emit one `trigger: "none"` row for the file (scenario `(<seeds.length> seeds, no actions)`, seed = the file's top-level seed). If a `startCombat` action ever leaves `state.combat` null (cleared immediately), still emit the row with `trigger: "startCombat"` and `foes: []` — never drop a row. Only ever snapshot on the null→non-null transition so a fight that persists across several actions is recorded once.

    3. `rosterToMarkdown(rows)` — pure; returns a markdown table string with header `| Fixture | Scenario | Seed | Trigger | Forced type | Foes rolled |` and an alignment row, one line per row; the foes cell joins entries as `Name (Type lvl N, wp W)` separated by `; `, or an em dash `—` when empty; `forced` null renders as `—`. Use `\n` line endings, no trailing newline. This exact string is what the document embeds and the test compares against.

    Then create `tools/fixture-inventory.mjs` (shebang + the tune-difficulty.mjs header convention: dev-only, zero-dependency, NOT shipped, NOT a test file; note explicitly that it imports the parity harness on purpose because the point is to replay the same code path the parity tests exercise). It imports `enumerateFixtureRoster` and `rosterToMarkdown` from `../test/parity/harness/fixtureRoster.js`; with no flags it prints `rosterToMarkdown(rows)` to stdout followed by a newline; with `--json` it prints `JSON.stringify(rows, null, 2)`. Document in the header: `node tools/fixture-inventory.mjs` and how to paste the output between the markers in `test/parity/FIXTURE-INVENTORY.md`.

    Expected replay result (verified by research AND by the planner on 2026-09-13 — if your output differs, something in engine/combat.js startCombat, content/bestiary.js, or a fixture changed; STOP and report rather than "fixing" the inventory): combat/win seed 3 Beasts → Shriek (lvl 1, wp 3); combat/lose seed 14 Beasts → Bat/Rat (lvl 1, wp 1), Shriek (lvl 1, wp 3); combat/flee seed 17 Beasts → Viper (lvl 1, wp 3), Shriek (lvl 1, wp 3); combat/parley seed 303 Humans → Dante (lvl 1, wp 20), Dante (lvl 1, wp 20); magic/cast-damage seed 8 Beasts → Shriek (lvl 1, wp 3); every other row → trigger none, no foes.
  </action>
  <verify>
    <automated>node tools/fixture-inventory.mjs && node tools/fixture-inventory.mjs --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const f=r.filter(x=>x.foes.length);if(f.length!==5)process.exit(1);const names=[...new Set(f.flatMap(x=>x.foes.map(y=>y.type+':'+y.name)))].sort();if(JSON.stringify(names)!==JSON.stringify(['Beasts:Bat/Rat','Beasts:Shriek','Beasts:Viper','Humans:Dante']))process.exit(2);if(f.some(x=>x.foes.some(y=>y.lvl!==1)))process.exit(3);console.log('roster OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/parity/harness/fixtureRoster.js` and `grep -c '^export function enumerateFixtureRoster' test/parity/harness/fixtureRoster.js` prints 1
    - `grep -c '^export function rosterToMarkdown' test/parity/harness/fixtureRoster.js` prints 1 and `grep -c '^export const FIXTURE_ORDER' test/parity/harness/fixtureRoster.js` prints 1
    - `grep -c 'applyStartCombat' test/parity/harness/fixtureRoster.js` prints >= 2 (import + call) and `grep -c 'runEconomyAction' test/parity/harness/fixtureRoster.js` prints >= 2
    - `grep -c 'loadPrototypeSandbox' test/parity/harness/fixtureRoster.js` prints >= 2 (economy + encounters replays use the sandbox)
    - `node tools/fixture-inventory.mjs` exits 0 and its output contains exactly one line matching `action-script.combat.json | parley | 303 | startCombat | Humans | Dante (Humans lvl 1, wp 20); Dante (Humans lvl 1, wp 20)`
    - `node tools/fixture-inventory.mjs --json` exits 0 and parses as a JSON array with exactly 5 rows whose `foes` is non-empty, exactly 4 distinct (type,name) pairs across them, every foe `lvl === 1`, and the movement row has `trigger === "none"`
    - `node tools/fixture-inventory.mjs --json | grep -c '"trigger": "wandering"'` prints 0
    - `git status --porcelain test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt package.json` prints nothing (none of those files changed)
  </acceptance_criteria>
  <done>The CLI prints the six-fixture roster table from a real replay; the JSON rows show 5 fights, 4 creature names, all level 1, zero wandering encounters; no fixture/harness/master file was touched.</done>
</task>

<task type="auto">
  <name>Task 2: Commit FIXTURE-INVENTORY.md and the pinned roster + document-consistency test</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/fixture-inventory.test.js</files>
  <read_first>
    - test/parity/harness/fixtureRoster.js (from Task 1 — the row shape and rosterToMarkdown output you will pin)
    - .planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md sections "Fixture Inventory (FID-01) — Verified Results" and "Document format recommendation" (the analysis prose to carry into the doc; Phase 18 constraints)
    - test/parity/fixtures/README.md and test/parity/fixtures/action-script.schema.md (heading/table style to match)
    - test/parity/combat-parity.test.js lines 1-40 (import/header style for a parity-dir test file)
    - content/bestiary.js lines 1-20 and 48-52 (BESTIARY.Beasts[0] = Bat/Rat, Shriek, Viper; BESTIARY.Humans[0] = Dante only — cite these in the doc)
    - engine/combat.js lines 98-135 (maxLvl clamp + rng.pick(roster) — cite in the "why only level 1" paragraph)
    - .planning/research/PITFALLS.md Pitfall 14 (never silently regenerate expected values without a paired intent artifact — the test's comment cites this)
  </read_first>
  <action>
    Write `test/parity/FIXTURE-INVENTORY.md` with these sections, in order:

    1. Title `# Parity Fixture Inventory — which bestiary creatures the frozen fixtures roll`, then a short preamble: generated on the run date by `node tools/fixture-inventory.mjs` by REPLAYING every fixture through the parity harness (never by reasoning about seeds); Phase 17 / FID-01; regenerate instructions (run the CLI, paste its output between the markers below, run `node --test test/parity/fixture-inventory.test.js`) and the rule that if the pinned test breaks after an intentional fixture/seed/bestiary change you regenerate this document AND update the pinned array together in the same commit with a rationale — never just edit the pinned array (PITFALLS.md Pitfall 14).

    2. `## Roster by fixture (generated)` containing exactly the CLI's table wrapped by the two HTML-comment marker lines `<!-- fixture-inventory:generated:begin -->` and `<!-- fixture-inventory:generated:end -->` on their own lines (the table verbatim between them, nothing else). Produce it by running the CLI, not by typing.

    3. `## Parity-exposed bestiary surface` (hand-written analysis): the complete exposed set is 2 types x 4 names, all level 1 — Beasts lvl 1: `Bat/Rat`, `Shriek`, `Viper` (all three entries of `BESTIARY.Beasts[0]`, content/bestiary.js lines 14-16); Humans lvl 1: `Dante` (the only `BESTIARY.Humans[0]` entry, line 50). Explain WHY only level 1: every fixture starts a level-1 character on floor 1 and startCombat's `maxLvl = clamp(min(c.level, depth), 1, 5)` with the d4 downgrade `clamp(maxLvl - 1, 1, 5)` can only yield 1; state the boundary explicitly (level 2 on depth 1, or depth 2 with level 1, still gives 1; only level >= 2 AND depth >= 2 could expose a level-2 entry). Explain WHY only Beasts/Humans: the combat/magic fixtures force those two types via `startCombat.forced`; heal/potion/scroll scenarios have no startCombat; encounters/economy actions never reference BESTIARY (content/encounters.js has zero matches); the movement fixture's seed 256 draws zero wandering-monster 1s on d20 across its 101 actions — verified by this replay, not just by the fixture's `_note`. State that duplicates (Dante x2 on seed 303) are two rolled instances of the same entry.

    4. `## What this means for Phase 18 (constraints)`: (a) any change to `wp`, `sp.dmg`, `sp.atk`, `sp.toHit`, `sp.ar`, `sp.noArmor`, `sp.twice`, `sz`, `i`, or `n` of Bat/Rat, Shriek, Viper, or Dante changes parity output and requires a narrow, named `combatComparable` carve-out (FID-05 / BEST-03) modelled on `stripFoeDamageClosures` (test/parity/harness/comparables.js:158) with a rationale — never a fixture regeneration; (b) every other creature (all level 2-5 entries of every type; every Demons / Lair Beasts / Magical / Walking Dead entry at any level) is unconstrained by the parity suite and can be rebalanced freely; (c) the LENGTH and ORDER of `BESTIARY.Beasts[0]` and `BESTIARY.Humans[0]` are load-bearing because startCombat picks with `rng.pick(roster)` (index = floor(next * length)) — inserting, removing, or reordering a level-1 Beasts/Humans entry changes which creature every combat/magic seed rolls and would break every combat fixture; (d) Phase 19's ability kits (Djinni, Krupke, Drudge, Vampire, Stalka Beast) touch none of the four exposed creatures.

    Leave a final `## Draw-count baseline (FID-02)` heading placeholder with one line: "Appended by plan 17-03." (17-03 replaces that line with the pinned table.)

    Then write `test/parity/fixture-inventory.test.js` (`node:test` + `node:assert/strict`, ESM, header comment in the combat-parity.test.js style explaining: this pins WHICH creatures the frozen fixtures roll so Phase 18 cannot silently rebalance a fixture-exposed creature; if it fails after an intentional change, regenerate FIXTURE-INVENTORY.md too — do not just update the pinned array). Import `enumerateFixtureRoster`, `rosterToMarkdown`, `FIXTURE_ORDER` from `./harness/fixtureRoster.js`; read the fixture JSONs and the doc with `node:fs` resolved from `import.meta.url`. Call `enumerateFixtureRoster()` ONCE at module top and share the rows. Tests:

    - "FID-01: the five fixture fights roll exactly the pinned creatures, in order" — filter rows with non-empty foes; `assert.deepStrictEqual` against the pinned array of `{ fixture, scenario, seed, forced, foes: [{ name, type, lvl, wp }] }` for win/3/Beasts/[Shriek 1/3], lose/14/Beasts/[Bat/Rat 1/1, Shriek 1/3], flee/17/Beasts/[Viper 1/3, Shriek 1/3], parley/303/Humans/[Dante 1/20, Dante 1/20], cast-damage/8/Beasts/[Shriek 1/3] (project rows to those keys before comparing so `trigger` is asserted separately as `"startCombat"` for all five).
    - "FID-01: the parity-exposed surface is exactly 2 types x 4 names, all level 1" — build the sorted unique `type:name` set from every foe in every row and `deepStrictEqual` to `["Beasts:Bat/Rat", "Beasts:Shriek", "Beasts:Viper", "Humans:Dante"]`; assert every foe has `lvl === 1`; assert the set of `forced` values over fight rows is exactly `["Beasts", "Humans"]`.
    - "FID-01: every non-fight scenario is present as an explicit none row and nothing rolls a wandering monster" — assert rows exist (trigger `"none"`, foes `[]`) for magic heal/potion/scroll, encounters trap/chest/tablefour/faerie/affliction, economy `(script)`, movement `(script)`, and the chargen row; assert no row has trigger `"wandering"`; assert the rows' fixture names appear in FIXTURE_ORDER order (map rows to fixture index and check non-decreasing).
    - "FID-01 precision: every row's seed is the fixture's integer seed, verbatim" — for each row look up its fixture JSON (scenario seed for scenario files, top-level seed otherwise) and `assert.equal(row.seed, expectedSeed)` with `Number.isInteger(row.seed)`.
    - "FID-01: FIXTURE-INVENTORY.md's generated block matches the live replay" — read the doc, take the text strictly between the begin/end marker lines, normalise `\r\n` to `\n` and trim, and `assert.equal` it to `rosterToMarkdown(rows)`; also assert the doc contains the four creature names and the heading `## What this means for Phase 18`.
  </action>
  <verify>
    <automated>node --test test/parity/fixture-inventory.test.js && node --test test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/parity/fixture-inventory.test.js` exits 0 with at least 5 passing tests and 0 failing
    - `grep -c 'fixture-inventory:generated:begin' test/parity/FIXTURE-INVENTORY.md` prints 1 and `grep -c 'fixture-inventory:generated:end' test/parity/FIXTURE-INVENTORY.md` prints 1
    - `grep -c '^## What this means for Phase 18' test/parity/FIXTURE-INVENTORY.md` prints 1 and `grep -c '^## Parity-exposed bestiary surface' test/parity/FIXTURE-INVENTORY.md` prints 1 and `grep -c '^## Draw-count baseline (FID-02)' test/parity/FIXTURE-INVENTORY.md` prints 1
    - `grep -c 'Bat/Rat' test/parity/FIXTURE-INVENTORY.md` prints >= 2, and the doc mentions `Shriek`, `Viper`, `Dante`, `stripFoeDamageClosures`, `rng.pick`, and `maxLvl`
    - `grep -c 'Beasts (lvl 1)\|Beasts lvl 1' test/parity/FIXTURE-INVENTORY.md` prints >= 1 (the table's foes cells render the level)
    - `wc -l < test/parity/FIXTURE-INVENTORY.md` prints >= 40
    - `node --test test/parity` exits 0 (the full parity suite, including full-suite.test.js, is untouched and green)
    - `git status --porcelain test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt` prints nothing
  </acceptance_criteria>
  <done>FIXTURE-INVENTORY.md is committed with a replay-generated table between markers plus the exposed-surface analysis and Phase 18 constraints; fixture-inventory.test.js pins the roster, the seeds, the none rows, and the document block, and is green alongside the untouched parity suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling → engine | `tools/fixture-inventory.mjs` and the harness replay run only on the developer's machine against local fixtures; nothing ships in `www/` or the AAB |
| fixture JSON → harness | Committed, hand-authored fixture files are the only input; no network, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Tampering | `test/parity/prototype-master.js.txt`, `test/parity/fixtures/*.json`, `comparables.js` | high | mitigate | Prohibited in must_haves; acceptance criteria run `git status --porcelain` on those paths and require empty output; the parity suite (`node --test test/parity`) must stay green with zero fixture regeneration |
| T-17-02 | Repudiation | `FIXTURE-INVENTORY.md` drifting from what the harness rolls | medium | mitigate | `fixture-inventory.test.js` compares the marker-delimited doc block byte-for-byte to the live replay, so a stale document fails CI |
| T-17-03 | Denial of Service | replay loops in `enumerateFixtureRoster` | low | accept | Fixtures are finite (max 101 actions); the replay mirrors full-suite.test.js, which already runs under a 15s budget |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase — `package.json`/lockfile unchanged (checked by acceptance criteria); slopcheck + blocking human checkpoint would apply to any [ASSUMED]/[SUS] package, none exist |
</threat_model>

<verification>
- `node tools/fixture-inventory.mjs` prints the roster table; the five fight rows list Shriek / Bat/Rat+Shriek / Viper+Shriek / Dante x2 / Shriek, all `lvl 1`.
- `node --test test/parity/fixture-inventory.test.js` green (>= 5 tests).
- `node --test test/parity` green, byte-identical to the frozen prototype master (no fixture, harness, or master edits — `git status --porcelain` on those paths is empty).
- `npm test` green: 683 existing + the new fixture-inventory tests, 0 failing.
</verification>

<success_criteria>
- ROADMAP Phase 17 success criterion 1 satisfied: a written inventory lists exactly which `content/bestiary.js` creatures each parity fixture seed rolls, generated by replay, and pinned by a test.
- The document names the four exposed creatures, explains the level-1 clamp and the Beasts/Humans-only forcing, and states the Phase 18 carve-out constraints including the array length/order hazard.
- Every edge-probe truth in `must_haves.truths` (boundary, adjacency, empty, ordering, precision) is asserted by the test file or stated in the document.
</success_criteria>

<output>
Create `.planning/phases/17-fixture-inventory-foe-turn-refactors/17-01-SUMMARY.md` when done (include the CLI's table output and the final `node --test test/parity` / `npm test` counts).
</output>
