---
phase: 17-fixture-inventory-foe-turn-refactors
plan: 03
type: execute
wave: 2
depends_on: ["17-01", "17-02"]
files_modified:
  - test/unit/foe-turn-draw-count.test.js
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements: [FID-02]
tags: [determinism, rng, draw-count, regression, foe-turn, parity]

must_haves:
  truths:
    - "FID-02 boundary: for foes with NO abilities field (the field does not exist in content/bestiary.js or on any state.combat.foes entry today — the test asserts Object.hasOwn(foe, 'abilities') === false for every foe it counts) the per-foeTurn draw count is pinned at the exact boundary values 0 (asleep foe), 1 (one swing, miss), 2 (one swing, hit: to-hit + d6), 2 (Bat/Rat shape: two swings with flat damage — zero damage dice), 3 (hit + armour soak d20) and 6 (two Dante-shaped foes, three swings each, all missing) — and Phase 19 must keep every one of these numbers when it adds the abilities gate"
    - "FID-02 boundary (full fights): a complete fixture-seeded fight from startCombat to combatEnded/died consumes exactly the pinned total draws measured against the PRE-refactor engine on 2026-09-13 — seed 3/Beasts 12, seed 14/Beasts 101, seed 17/Beasts 111, seed 303/Humans 66, seed 8/Beasts 32 — proving the 17-02 extraction is draw-for-draw identical and giving Phase 19 the 'before' baseline its zero-draw gate must reproduce"
    - "FID-02 precision: the draw counter cannot silently under- or over-count — for every seeded full fight the test cross-checks the counter against mulberry32's own cursor, asserting ((start + Math.imul(draws, 0x6d2b79f5)) | 0) === (rng.getState() | 0), so a wrapper that missed a method (or counted shuffle wrong) fails loudly; the counter is an integer incremented once per d()/pick()/next() call and by max(0, arr.length - 1) per shuffle(arr), matching engine/rng.js's loop exactly"
    - "The counting wrapper is injected as the ONLY rng object the engine sees (countingRng(makeRng(state.rngState)) for seeded fights, countingRng(fakeRng(seq)) for exact micro cases), so no draw can bypass it; the parity suite's rngState stripping (RESEARCH Pitfall 3) is therefore complemented by a direct numeric count rather than relied on as a substitute"
    - "npm test is green after the whole phase with 683 pre-existing tests plus every new test from plans 17-01, 17-02 and 17-03, zero failing, and git shows no commit in this phase touching test/parity/prototype-master.js.txt, test/parity/fixtures, test/parity/harness/comparables.js, or package.json"
  artifacts:
    - path: "test/unit/foe-turn-draw-count.test.js"
      provides: "countingRng(inner) helper + pinned per-foeTurn and full-fight draw counts for foes without an abilities field (Phase 19's baseline)"
      contains: "countingRng"
      min_lines: 80
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "Draw-count baseline section listing the pinned totals per fixture seed and the Phase 19 contract"
      contains: "## Draw-count baseline (FID-02)"
  key_links:
    - from: "test/unit/foe-turn-draw-count.test.js"
      to: "engine/combat.js"
      via: "drives startCombat + playerStrike (which runs afterPlayerAction -> foeTurn) and foeTurn directly with the counting rng"
      pattern: "import \\{[^}]*(startCombat|playerStrike|foeTurn)[^}]*\\} from \"../../engine/combat.js\""
    - from: "test/unit/foe-turn-draw-count.test.js"
      to: "engine/rng.js"
      via: "wraps makeRng(state.rngState) for the seeded full fights and cross-checks getState against the additive mulberry32 step"
      pattern: "0x6d2b79f5"
    - from: "test/unit/foe-turn-draw-count.test.js"
      to: "engine/engine.js"
      via: "newRun(seed) produces the same starting state and rngState cursor the parity fixtures start from"
      pattern: "import \\{ newRun \\} from \"../../engine/engine.js\""
    - from: "test/parity/FIXTURE-INVENTORY.md"
      to: "test/unit/foe-turn-draw-count.test.js"
      via: "the baseline section names the test file as the source of the pinned numbers"
      pattern: "foe-turn-draw-count.test.js"
  prohibitions:
    - requirement_id: FID-02
      category: values
      statement: "MUST NOT adjust a pinned draw count to make the test pass — the numbers were measured against the pre-refactor engine; a mismatch means engine/combat.js changed draw order/count and the ENGINE must be fixed (or the change escalated as a deliberate divergence with rationale), never the pin"
    - requirement_id: FID-02
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any parity fixture, or comparables.js, and MUST NOT add a dependency — the test uses node:test, node:assert/strict and the existing engine modules only"
    - requirement_id: FID-02
      category: values
      statement: "MUST NOT count draws by reasoning or hand-tracing; every pinned integer is produced by running the engine through the counting wrapper, and the cursor cross-check is mandatory on every seeded fight"
---

<objective>
Write the FID-02 draw-count regression test: a `countingRng(inner)` wrapper plus pinned draw counts — per `foeTurn` (exact micro cases via fakeRng) and for five complete fixture-seeded fights (via `makeRng`) — for foes that have no `abilities` field, cross-checked against mulberry32's cursor arithmetic. Then close the phase: append the baseline table to `test/parity/FIXTURE-INVENTORY.md` and run the full gate (npm test green; no fixture/harness/master/package changes anywhere in the phase).

Purpose: the parity suite proves draw order only indirectly (it strips `rngState`; an extra draw shows up as a diverging value later). Phase 19 will insert an ability-attempt gate into `foeTurn` right after the sleep check; the engine gate demands that a foe without `abilities` draws exactly zero additional RNG. This test is the numeric "before" baseline Phase 19 re-runs unchanged — and, run now against the post-17-02 engine, it also proves the extraction preserved every draw (the pinned integers were measured on the PRE-refactor engine).

Output: `test/unit/foe-turn-draw-count.test.js`; the `## Draw-count baseline (FID-02)` section of `test/parity/FIXTURE-INVENTORY.md`; a green `npm test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-CONTEXT.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-02-SUMMARY.md
@engine/rng.js
@engine/combat.js
@engine/engine.js
@test/unit/party-combat.test.js
@test/unit/rng.test.js
@test/parity/FIXTURE-INVENTORY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `countingRng(inner)` | test-local helper in `test/unit/foe-turn-draw-count.test.js` | 17-03 | wraps ANY rng (fakeRng or makeRng); `.draws` getter; passes `getState`/`setState` through when present; Phase 19 copies it verbatim |
| `test/unit/foe-turn-draw-count.test.js` | `node:test` file | 17-03 | FID-02 pinned baseline: 6 micro cases + 5 full fights + cursor cross-check |
| `## Draw-count baseline (FID-02)` | section of `test/parity/FIXTURE-INVENTORY.md` | 17-03 | pinned totals per fixture seed + Phase 19 contract |
| `pickFoeTarget`, `applyFoeDamageToPlayer` | exports of `engine/combat.js` | 17-02 | see 17-02 |
| `enumerateFixtureRoster`, `rosterToMarkdown`, `FIXTURE_ORDER`, `tools/fixture-inventory.mjs`, `test/parity/fixture-inventory.test.js` | harness exports / CLI / test | 17-01 | see 17-01 |

<tasks>

<task type="auto">
  <name>Task 1: Write test/unit/foe-turn-draw-count.test.js with countingRng, micro pins, and full-fight pins</name>
  <files>test/unit/foe-turn-draw-count.test.js</files>
  <read_first>
    - engine/rng.js (entire file, 77 lines: makeRng's public surface is exactly d/pick/shuffle/next/getState/setState; mulberry32 advances its state by the constant 0x6d2b79f5 with `| 0` wrap on every next() — shuffle(arr) calls next() arr.length-1 times, d()/pick() once each)
    - engine/combat.js as left by 17-02 (foeTurn now calls pickFoeTarget + applyFoeDamageToPlayer; startCombat lines 98-270; playerStrike line 281 — it calls afterPlayerAction which runs foeTurn, so one playerStrike call per "attack" action drives a full round exactly as applyAction's "attack" case does)
    - engine/engine.js lines 40-75 (applyAction rehydrates makeRng(next.rngState) per action and persists rng.getState() afterwards — so continuing ONE rng object across startCombat + N playerStrike calls yields the identical draw stream a fixture replay would)
    - test/unit/party-combat.test.js lines 1-100 (fakeRng throw-on-underflow, fixedFighter/fixedFloor/fixedState/fixedFoe/fixedCombat builders — copy these helpers locally in the same "mirrors ... verbatim" style the file itself uses; do NOT import from a test file)
    - test/unit/rng.test.js (header/import style for a test that exercises makeRng directly)
    - .planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md "Code Examples — Draw-count regression test" (the countingRng shape; adapt it to wrap an inner rng object rather than a seed) and Pitfall 3 (why the parity suite is not a draw counter)
  </read_first>
  <action>
    Create `test/unit/foe-turn-draw-count.test.js` (`node:test` + `node:assert/strict`, ESM). Header comment: this is FID-02's pinned RNG-draw baseline for foes WITHOUT an `abilities` field (the field does not exist yet; Phase 19 adds it and must re-run this file unchanged to prove its ability-attempt gate draws nothing for ability-less foes); explain that the parity suite strips `rngState` and only catches an extra draw indirectly (RESEARCH Pitfall 3), so this file counts draws directly; state that the full-fight integers were measured against the PRE-Phase-17 foeTurn on 2026-09-13 and therefore also prove 17-02's extraction is draw-for-draw identical; and state the rule that a mismatch is an ENGINE bug, never a reason to edit a pin.

    Define `countingRng(inner)` — takes an inner rng OBJECT (a fakeRng or a makeRng) and returns an object with `d(n)` (draws++ then inner.d), `pick(a)` (draws++ then inner.pick), `next()` (draws++ then inner.next — guard for inner lacking next), `shuffle(a)` (draws += Math.max(0, a.length - 1) then inner.shuffle), pass-through `getState`/`setState` when the inner provides them, and a `draws` getter. Define local `fakeRng(seq)` (throws on underflow, `pick` returns arr[0]), `fixedFighter`, `fixedFloor`, `fixedState`, `fixedFoe`, `fixedCombat` exactly as party-combat.test.js defines them. Define a `assertNoAbilities(foes)` helper that asserts `Object.hasOwn(f, "abilities") === false` for every foe (this is the precondition every pin documents).

    Section 1 — per-foeTurn micro pins (exact, via `countingRng(fakeRng(seq))`; each test calls `assertNoAbilities` first, runs `foeTurn(state, rng, [])`, then asserts `rng.draws` AND the expected event types):
    1. "asleep foe: 0 draws" — `fixedFoe({ asleep: 2 })`, `fakeRng([])` → draws 0, events `["foeSlept"]`.
    2. "one swing that misses: 1 draw (to-hit only)" — `fakeRng([7])` → draws 1, events `["foeMissed"]`.
    3. "one swing that hits an unarmoured hero: 2 draws (to-hit + d6 damage)" — `fakeRng([3, 4])` → draws 2, events `["struckByFoe"]`, `c.wp` 50.
    4. "Bat/Rat shape (atk 2, flat damage {n:0,sides:0,bonus:1}): 2 draws — two to-hit rolls, ZERO damage dice" — `fixedFoe({ name: "Bat/Rat", wp: 1, maxWP: 1, sp: { atk: 2, dmg: { n: 0, sides: 0, bonus: 1 } } })`, `fakeRng([3, 3])` → draws 2, events `["struckByFoe", "struckByFoe"]` each with dmg 2 (lvl squared 1 + flat 1).
    5. "hit on an armoured hero: 3 draws (to-hit + d6 + d20 soak)" — c `{ ar: 15, armorWP: 20, armorMax: 20, armorMin: 0, armor: "Studded" }`, `fakeRng([3, 4, 10])` → draws 3, events `["armorSoaked"]`.
    6. "two Dante-shaped foes (atk 3 each), all six swings miss: 6 draws" — two `fixedFoe({ name: "Dante", type: "Humans", wp: 20, maxWP: 20, sp: { atk: 3 } })`, `fakeRng([7, 7, 7, 7, 7, 7])` → draws 6, six `foeMissed` events.

    Section 2 — full fixture-seeded fights (via `countingRng(makeRng(state.rngState))`). Write a `runFullFight(seed, forced)` helper: `const state = newRun(seed); const start = state.rngState; const rng = countingRng(makeRng(start)); const events = [];` then `startCombat(state, false, forced, rng, events)`; `assertNoAbilities(state.combat.foes)`; record `foeNames`; then loop `while (state.combat && !state.dead && attacks < 200) { playerStrike(state, rng, events); attacks++; }`; return `{ state, rng, start, attacks, foeNames }`. Pin with a table-driven test (one `test(...)` per row so a failure names the seed) using these PRE-refactor measurements — seed 3 / "Beasts" / foes ["Shriek"] / total draws 12 / outcome won (state.dead false, state.combat null, 1 attack); seed 14 / "Beasts" / ["Bat/Rat", "Shriek"] / 101 / died (state.dead true, 10 attacks); seed 17 / "Beasts" / ["Viper", "Shriek"] / 111 / won (11 attacks); seed 303 / "Humans" / ["Dante", "Dante"] / 66 / won (6 attacks); seed 8 / "Beasts" / ["Shriek"] / 32 / won (4 attacks). Each row asserts: `foeNames` deepEqual, `rng.draws` equal to the pinned total, `attacks` equal to the pinned count and `< 200`, the outcome flags, and the mulberry32 cursor cross-check `assert.equal((start + Math.imul(rng.draws, 0x6d2b79f5)) | 0, rng.getState() | 0, "counter disagrees with the rng cursor")`. Name the section comment "Phase 19 contract" and spell out: add `abilities: []` or a non-empty kit only behind a gate; ability-less foes must reproduce every number in this file exactly.

    Also add one test "countingRng: counts shuffle as arr.length-1 draws and agrees with the cursor" — wrap `makeRng(42)`, call d(20) 137 times, shuffle a 5-element array, pick once; assert draws 142 and the cursor cross-check (this pins the wrapper's own arithmetic independent of combat).
  </action>
  <verify>
    <automated>node --test test/unit/foe-turn-draw-count.test.js && npm run test:quick</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/foe-turn-draw-count.test.js` and `grep -c 'function countingRng(inner)' test/unit/foe-turn-draw-count.test.js` prints 1
    - `grep -c '0x6d2b79f5' test/unit/foe-turn-draw-count.test.js` prints >= 2 (cursor cross-check used in the fight rows and the wrapper self-test)
    - `grep -c 'Object.hasOwn(f, "abilities")' test/unit/foe-turn-draw-count.test.js` prints >= 1
    - `grep -c 'import { newRun } from "../../engine/engine.js"' test/unit/foe-turn-draw-count.test.js` prints 1 and `grep -c 'from "../../engine/combat.js"' test/unit/foe-turn-draw-count.test.js` prints 1
    - `node --test test/unit/foe-turn-draw-count.test.js` exits 0 reporting >= 12 tests passed (6 micro + 5 fights + 1 wrapper self-test) and 0 failed
    - `grep -c 'seed: 303' test/unit/foe-turn-draw-count.test.js` prints >= 1 and `grep -c '101' test/unit/foe-turn-draw-count.test.js` prints >= 1 (the seed-14 fight's pinned total is present as a literal, not computed)
    - `grep -c 'Phase 19' test/unit/foe-turn-draw-count.test.js` prints >= 1
    - `npm run test:quick` exits 0
    - `git status --porcelain test/parity package.json` prints nothing
  </acceptance_criteria>
  <done>The draw-count file pins 0/1/2/2/3/6 per-foeTurn draws and 12/101/111/66/32 full-fight draws for ability-less foes, every seeded fight agrees with the mulberry32 cursor, and the quick suite is green.</done>
</task>

<task type="auto">
  <name>Task 2: Append the draw-count baseline to FIXTURE-INVENTORY.md and run the phase gate</name>
  <files>test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md as written by 17-01 (the trailing `## Draw-count baseline (FID-02)` heading with its one-line "Appended by plan 17-03." placeholder; the generated block between the markers must NOT be touched)
    - test/unit/foe-turn-draw-count.test.js (Task 1 — the pinned numbers and helper name to reference)
    - test/parity/fixture-inventory.test.js (17-01 — confirms it only compares the marker-delimited block, so appending below the markers is safe)
    - .planning/ROADMAP.md Phase 17 success criteria (all four must be true at this gate)
  </read_first>
  <action>
    Replace the placeholder line under `## Draw-count baseline (FID-02)` in `test/parity/FIXTURE-INVENTORY.md` (leave the heading; do not touch anything between the generated markers) with: one sentence saying these totals are pinned by `test/unit/foe-turn-draw-count.test.js` using a counting wrapper around `makeRng`, measured against the pre-Phase-17 engine on 2026-09-13 and re-asserted after the 17-02 extraction; a table with columns `Fixture scenario | Seed | Forced | Foes | Attacks to resolve | Total RNG draws | Outcome` and the five rows (combat/win 3 Beasts Shriek 1 12 won; combat/lose 14 Beasts Bat/Rat + Shriek 10 101 died; combat/flee 17 Beasts Viper + Shriek 11 111 won; combat/parley 303 Humans Dante x2 6 66 won; magic/cast-damage 8 Beasts Shriek 4 32 won) — note that "attacks" here means the test's attack-until-resolved loop, not the fixture's action list; a second small table of the per-foeTurn micro pins (asleep 0; one miss 1; one hit 2; Bat/Rat two flat-damage swings 2; hit + armour soak 3; two Dantes all-miss 6); and a `### Phase 19 contract` paragraph: a foe without an `abilities` field must reproduce every number above unchanged after the ability gate lands; `abilities: []` must behave identically to an absent field; only a non-empty kit may add draws, and those must be carved out per FID-04.

    Then run the phase gate and record the results in the SUMMARY: `npm test` (expect 683 + the new tests from 17-01, 17-02 and 17-03, 0 failing); `node --test test/parity` (byte-identical, untouched); `git log --oneline c5fc219..HEAD -- test/parity/prototype-master.js.txt test/parity/fixtures test/parity/harness/comparables.js package.json package-lock.json` must print nothing (no commit in this phase touched the frozen master, any fixture, the comparables carve-outs, or the dependency manifest); `node tools/fixture-inventory.mjs` still prints the same table the doc embeds.
  </action>
  <verify>
    <automated>node --test test/parity/fixture-inventory.test.js test/unit/foe-turn-draw-count.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Draw-count baseline (FID-02)' test/parity/FIXTURE-INVENTORY.md` prints 1 and `grep -c 'Appended by plan 17-03' test/parity/FIXTURE-INVENTORY.md` prints 0
    - `grep -c 'foe-turn-draw-count.test.js' test/parity/FIXTURE-INVENTORY.md` prints >= 1 and `grep -c '^### Phase 19 contract' test/parity/FIXTURE-INVENTORY.md` prints 1
    - `grep -c '| 303 |' test/parity/FIXTURE-INVENTORY.md` prints >= 2 (the seed appears in both the generated roster table and the baseline table) and the baseline table row for seed 14 contains `101`
    - `node --test test/parity/fixture-inventory.test.js` exits 0 (the generated block was not disturbed)
    - `npm test` exits 0 with `# fail 0` and `# pass` >= 683 + 5 + 23 + 12 = 723
    - `git log --oneline c5fc219..HEAD -- test/parity/prototype-master.js.txt test/parity/fixtures test/parity/harness/comparables.js package.json package-lock.json | wc -l` prints 0
    - `git status --porcelain` shows no unexpected modified files outside the phase's declared files_modified lists
  </acceptance_criteria>
  <done>FIXTURE-INVENTORY.md carries the pinned draw-count baseline and the Phase 19 contract beneath the untouched generated block; the full suite is green with all Phase 17 tests; git history proves the frozen master, fixtures, comparables and package manifest were never touched during the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → engine | The counting wrapper is the only rng object the engine sees during the test; no external input, no network, nothing shipped |
| doc → future phases | `FIXTURE-INVENTORY.md`'s baseline is advisory text; the enforceable contract is the test file itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-08 | Tampering | pinned draw counts edited to hide an engine draw-order change | high | mitigate | Prohibited in must_haves; the cursor cross-check ties every count to mulberry32's actual state so a wrong pin cannot be made to pass by counter tweaks; the parity suite independently guards order |
| T-17-09 | Repudiation | a future ability gate adding draws for ability-less foes without anyone noticing | high | mitigate | This file is the numeric baseline Phase 19 must re-run unchanged (documented in the file header and the doc's Phase 19 contract) |
| T-17-10 | Denial of Service | a full-fight loop that never resolves | low | mitigate | Hard cap of 200 attacks per fight with an assertion that the cap was not hit |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `package.json`/lockfile unchanged and verified by the phase-gate git log check; slopcheck + blocking human checkpoint would gate any [ASSUMED]/[SUS] package, none exist |
</threat_model>

<verification>
- `node --test test/unit/foe-turn-draw-count.test.js` green with >= 12 tests; every seeded fight passes the cursor cross-check.
- `npm test` green: >= 723 passing (683 baseline + 17-01's >= 5 + 17-02's 23 + 17-03's >= 12), 0 failing.
- `node --test test/parity` green and byte-identical; `git log c5fc219..HEAD` on the frozen master, fixtures, comparables and package manifest is empty.
- `FIXTURE-INVENTORY.md` contains the generated roster block (unchanged), the exposed-surface analysis, the Phase 18 constraints, and the FID-02 baseline with the Phase 19 contract.
</verification>

<success_criteria>
- ROADMAP Phase 17 success criterion 3 satisfied: a draw-count regression test proves a foe without the new `abilities` field draws exactly the pinned RNG — the numeric baseline Phase 19's gate must preserve (zero additional draws).
- ROADMAP success criteria 1, 2 and 4 remain true at the phase gate (inventory committed, helpers extracted and tested, parity byte-identical).
- Every FID-02 edge-probe truth (boundary values, precision/cursor cross-check) is asserted by the test file.
</success_criteria>

<output>
Create `.planning/phases/17-fixture-inventory-foe-turn-refactors/17-03-SUMMARY.md` when done (include the final `npm test` totals, the five pinned fight totals as measured, and the empty git-log gate output).
</output>
