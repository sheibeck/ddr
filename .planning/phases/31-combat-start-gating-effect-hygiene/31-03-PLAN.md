---
phase: 31-combat-start-gating-effect-hygiene
plan: 03
type: execute
wave: 3
depends_on: ["31-02"]
files_modified:
  - mazeworld.html
  - src/browser/viewModels.js
  - test/unit/shell-toast-wiring.test.js
  - test/unit/shell-fight-gate.test.js
  - test/unit/spell-menu-mirror.test.js
  - test/unit/grimoireViewModel.test.js
  - test/unit/foe-effect-chip.test.js
  - test/unit/characterSheetViewModel.test.js
autonomous: true
requirements: [CMB-01, CMB-02, CMB-03, CMB-04]

must_haves:
  truths:
    - "CMB-01 (shell): the Fight! button and the Enter/Space/1 keys dispatch the engine's fight action through engineCombatAction; the encounter preview is gated on the engine's combat.pending, not a presentation flag; the AMBUSH pre-death special case is gone because a pre-emptive kill now happens inside the fight dispatch and renders through the ordinary death card"
    - "CMB-02 (shell): the in-combat SPELLS menu filters through the engine's spellLevelFor-aware canCast via one bridge (window.__mzCanCast) — a level-1 Summoner sees Summon and a level-1 Illusionist sees Phantom Host (the Phase 23 regression, closed); the Hero-tab Grimoire names 'Needs level N' or '<school> opens at level N' instead of 'Not ready yet'; every Use/Sing/Scroll button stays visible when blocked and a tap produces the engine's refusal toast; cooldown rows keep showing the countdown"
    - "CMB-02 probe (empty): a caster whose grimoire has nothing castable at this level sees the existing 'Nothing in the grimoire you can work at this level.' empty state, never a blank menu"
    - "CMB-03 (shell): the Gear tab's Use button appears for every potion/use item outside combat and dispatches useItem; buffs apply and their chip appears immediately; a targeted staff's tap produces the combatOnly toast"
    - "CMB-04 (shell): the condition tracker renders the ward as 'Shield <pool> hp · <rounds> rds' from conditionsOf's ward entry (both numbers on one chip, mirroring the flight special case); the Acute chip reads rds in combat and sq outside; the Afraid chip renders 'Afraid · N rds' from conditionsOf's afraid entry (CONDITION_COPY.afraid replaces the old phobia row — D 'Phobia = penalty, never a lost action') and no 'Phobia: …' chip exists any more"
    - "Sheet TO HIT reads the LOW range (31-ROLL-DIRECTION-AUDIT Finding 2, port regression, display only): characterSheetViewModel's toHit stat renders `1–N` (en dash, exactly the prototype's own `\"1–\" + toHit()` sheet text at prototype-master.js.txt:1076 / mazeworld.html:2950) instead of the inverted `N+`; the roll screen's reels and the Hero tab therefore agree; no template anywhere in src/browser appends a plus sign after a toHit value; zero gameplay change (toHit(state) itself was verified correct)"
    - "Prohibition: refusals stay toasts (no card, no Round Card work); no new engine surface; combat never serialized; npm run build:www exits 0; the full suite is green"
  artifacts:
    - "mazeworld.html: mzFight → engineCombatAction('fight'); pending gate in renderEncounter + key handler; AMBUSH special case removed; classic canCast bridged; renderCarriedList Use always visible; combat use-list unfiltered by readiness; Sing/Scroll buttons visible with countdown/count; CONDITION_COPY.ward + the two-number branch; CONDITION_COPY.afraid (label 'Afraid', unit 'rds') replacing the phobia row and its label-chain special case; window.__mzCanCast bridge"
    - "src/browser/viewModels.js: grimoireViewModel level/school reasons; characterSheetViewModel toHit stat value `1–${toHit(state)}`"
    - "test/unit/shell-fight-gate.test.js, test/unit/spell-menu-mirror.test.js (new); shell-toast-wiring + grimoireViewModel + foe-effect-chip + characterSheetViewModel re-pins"
  key_links:
    - "a-fight click / Enter / Space / 1 → window.mzFight → engineCombatAction('fight') → dispatchWithToasts → noteCombat (victory report if the opener clears) → renderEncounter"
    - "renderEncounter spell menu → classic canCast(sp) → window.__mzCanCast(S, sp) → engine/derived.js#canCast → spellLevelFor"
    - "renderCarriedList 'use' → window.mzUseItem → inventoryAction/engineCombatAction useItem → useRefused toast (PRIORITY.block)"
    - "paintConditions → window.__mzConditionsOf(S) → ward {pool, remaining} → 'Shield N hp · M rds'; afraid {remaining} → CONDITION_COPY.afraid → 'Afraid N rds'"
---

<objective>
Wire the shell to the Plan 01/02 engine: Fight! dispatches `fight` and the preview is gated on `combat.pending` (the AMBUSH pre-death special case collapses); the in-combat spell menu filters through the engine's override-aware `canCast` via one bridge (the Phase 23 regression fix); the Hero Grimoire names the real reason; Use/Sing/Scroll buttons stay visible when blocked so the engine's refusal toasts explain; the Shield chip shows pool + rounds; the phobia chip becomes the Afraid countdown chip; the roll screen's TO HIT stat stops saying `N+` and says `1–N` like the prototype's sheet (roll-direction audit Finding 2). Ends with `npm run build:www` and the full suite.

Purpose: the player experiences CMB-01..04 on the device: no strike before Fight!, no silent "not ready", potions from Gear, a Shield chip, an Afraid chip that counts down instead of a lost turn — with refusals as toasts Phase 32 will not move.
Output: targeted mazeworld.html + viewModels.js edits, shell source-assertion tests, the classic-vs-engine spell gate mirror test, `build:www` exit 0, `npm test` `# fail 0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-CONTEXT.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-RESEARCH.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-01-SUMMARY.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-02-SUMMARY.md
@docs/COMBAT-NARRATIVE-DESIGN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fight! dispatches `fight`; the preview gates on combat.pending; the AMBUSH pre-death special case collapses</name>
  <files>mazeworld.html, test/unit/shell-toast-wiring.test.js, test/unit/shell-fight-gate.test.js</files>
  <read_first>
    - mazeworld.html ~L4870-4930 (renderEncounter's early-return order: S.dead → S.won → the beats block, whose first branch is the DR17 AMBUSH-death roster + Fight! special case), ~L5140-5180 (the foe roster list, then the DR17 Fight! gate branch that renders a single Fight! button and returns), ~L5520-5545 (the keydown handler: the Fight!-gate branch that swallows keys 1-7 and releases on Enter/Space/1), ~L5975-6000 (noteCombat's fresh-combat branch that sets the presentation flag), ~L6090-6112 (engineMove's diedOnMove/ambush beats synthesis), ~L6552-6590 (engineCombatAction + mzAttack/mzFight/mzFlee…), ~L6665-6680 (mzMakeCamp's flag set)
    - test/unit/shell-toast-wiring.test.js L210-224 (the DFB-01 pin asserting the ambush flag line exists — to be re-pinned) and test/unit/shell-loot-screen.test.js (the readFileSync + stripComments source-assertion style to copy for the new test)
    - docs/COMBAT-NARRATIVE-DESIGN.md §6.1 "State: Fight! gate" and §6.3 (Phase 32 adds ARM_DELAY_MS to this button later — do NOT add guards here)
  </read_first>
  <action>
    <!-- planner-discipline-allow: awaitingFight -->
    <!-- planner-discipline-allow: state.beats.foes -->
    1. `window.mzFight = () => engineCombatAction("fight");` — replace the whole presentation-only mzFight body (and its "no dispatch, no rng" comment) with the one-liner beside mzAttack; update the comment to say the engine's fight action rolls initiative and the pre-emptive turn NOW (Phase 31, CMB-01). engineCombatAction already handles the aftermath: noteCombat's `wasCombat && !has` branch builds the victory report if the opener clears the room, the S.dead branch renders the death card if the opener kills, hapticForEvents/logLine/paint/renderEncounter run as for any action.
    2. renderEncounter's Fight! gate: change its condition from the presentation flag to `if (C.pending)`; keep the branch body (roster above, a single `a-fight` button, return) and rewrite its comment: the ENGINE owns the gate — while `combat.pending` nothing has rolled or struck; mzFight dispatches `fight`. The keydown handler's gate likewise reads `S.combat.pending` (same keys release it: Enter/Space/1; other action keys swallowed).
    3. Remove every remaining `awaitingFight` write/read: noteCombat's fresh-combat branch no longer sets a flag (keep the combatSnap capture and the `__mzLootReport = null` reset); mzMakeCamp no longer sets one (a wandering ambush during camp now arrives from the engine already pending); the renderEncounter beats-block AMBUSH special case (the `b.awaitingFight` roster branch) is deleted; engineMove's diedOnMove block drops the `ambush` sub-branch and the `state.beats.foes/combatType` carry — a move can no longer kill via an encounter (the opener runs inside `fight`), so the title is always "The maze has the last word" and `preDeath: true` stays exactly once. Grep the file for the old flag name afterwards: zero non-comment occurrences.
    4. test/unit/shell-toast-wiring.test.js: re-pin "the preDeath and ambush branches survive byte-identical" → `preDeath: true` appears exactly once AND the old ambush flag assignment is absent; rename the test accordingly (DFB-01 preDeath survives; the ambush gate collapsed in Phase 31).
    5. Create test/unit/shell-fight-gate.test.js (readFileSync mazeworld.html, normalize CRLF, strip comments like shell-loot-screen.test.js): assert `window.mzFight = () => engineCombatAction("fight")` is present exactly once; `if (C.pending)` appears in the renderEncounter region (between `function renderEncounter` and `function noteCombat`); `S.combat.pending` appears in the keydown region; the old presentation flag name has zero non-comment occurrences; `preDeath: true` exactly once; `state.beats.foes` absent; `case "fight"` is NOT needed anywhere in the shell (the engine owns it) — i.e. no classic `function fight(` was added.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-fight-gate.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-loot-screen.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'window.mzFight = () => engineCombatAction("fight")' mazeworld.html` == 1
    - `grep -v '^\s*//' mazeworld.html | grep -c 'awaitingFight'` == 0
    - `grep -c 'if (C.pending)' mazeworld.html` >= 1 and `grep -c 'S.combat.pending' mazeworld.html` >= 1
    - `grep -c 'preDeath: true' mazeworld.html` == 1
    - `grep -c 'state.beats.foes' mazeworld.html` == 0
    - node --test test/unit/shell-fight-gate.test.js test/unit/shell-toast-wiring.test.js → `# fail 0`
  </acceptance_criteria>
  <done>Fight! is a real dispatch; the preview is engine-gated; the ambush pre-death hack is gone; the shell pins prove it.</done>
</task>

<task type="auto">
  <name>Task 2: Bridge the in-combat spell gate to the engine, split the grimoire reasons, keep blocked Use/Sing/Scroll buttons visible, paint the Shield chip</name>
  <files>mazeworld.html, src/browser/viewModels.js, test/unit/spell-menu-mirror.test.js, test/unit/grimoireViewModel.test.js, test/unit/shell-fight-gate.test.js, test/unit/foe-effect-chip.test.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - mazeworld.html L2042-2050 (the classic MU_CHART helpers and the stale classic canCast — the direct printed-level comparison RESEARCH 3.2 identified; called from the combat spell menu ~L5210 and the dead classic castSpell ~L4095), L3445-3449 (classic itemReady, kept — the row countdown still uses it), L3500-3545 (renderCarriedList: the `cd` countdown in the row title and the `use` action branch that only appends Use when ready), ~L5180-5188 (the action bar: a-sing only when songReady(), a-scroll only when scrolls && canRead()), ~L5190-5205 (the combat use-list `useable`/`filter` clauses that gate on readiness), ~L5205-5225 (the spell menu loop + the empty-state text), ~L5541-5542 (keys 6/7), L2815-2830 (CONDITION_COPY — its `phobia` row is replaced), L2837-2870 (paintConditions: the flight special case is the two-value precedent; the label chain's `cn.key === "phobia"` branch is removed — the engine no longer emits that key after Plan 01), test/unit/foe-effect-chip.test.js L62-77 (anchors the label chain on the phobia branch — re-pin to the `affliction` branch), L5684-5705 (the __mzConditionsOf/__mzArmorDisplay/__mzLootCompare bridge block; module imports at L5648-5653)
    - src/browser/viewModels.js L295-343 (grimoireViewModel's disabledReason ladder) and its imports (canCast, maxCharges — add spellLevelFor/schoolGate from ../../engine/derived.js), L242-247 (characterSheetViewModel's `stats` array — the `toHit` row's value template appends a plus sign after the number, which reads as HIGH-roll-good; the game's to-hit is LOW-roll-good: hit on roll ≤ toHit); mazeworld.html L2950 (the classic Hero-tab write `"1–" + toHit()` — the prototype's own sheet text, and the shape to match) vs L6287 (the roll screen's reels read characterSheetViewModel, so today the roll screen and the Hero tab disagree); test/unit/characterSheetViewModel.test.js L43 (the pin that currently asserts the plus-suffix form); .planning/phases/31-combat-start-gating-effect-hygiene/31-ROLL-DIRECTION-AUDIT.md Finding 2
    - engine/derived.js L648-651 (spellLevelFor), L690-699 (canCast), content/spell-level-overrides.js
    - test/unit/parley-button-mirror.test.js (extraction + replay pattern for a classic function), test/unit/spell-level-overrides.test.js L88-125 (the canCast diff-walk to reuse for the mirror walk), test/unit/grimoireViewModel.test.js L100-120
  </read_first>
  <action>
    <!-- planner-discipline-allow: sp.lvl > S.c.level -->
    <!-- planner-discipline-allow: itemReady(it)) li.appendChild -->
    <!-- planner-discipline-allow: Not ready yet -->
    1. Spell gate (orchestrator resolution 5; RESEARCH 3.2): in the module script's bridge block add `window.__mzCanCast = canCast;` (import canCast from ./engine/derived.js next to conditionsOf) with the same read-only/no-rng comment style. Replace the classic `canCast(sp)` BODY with the ONE rule `return !!(window.__mzCanCast && window.__mzCanCast(S, sp));` and a comment: Phase 31 (CMB-02) — the classic copy checked `sp.lvl` directly and never learned Phase 23's spellLevelFor override table, which hid a level-1 Summoner's Summon and a level-1 Illusionist's Phantom Host from the combat menu; it now delegates to the engine's predicate (the shell never re-implements the gate). The dead classic castSpell keeps calling it unchanged. The spell menu loop and its empty-state text are unchanged.
    2. src/browser/viewModels.js grimoireViewModel: replace the single `!canCast` branch with the engine's own two-way split — `else if (spellLevelFor(c.sub, sp) > c.level) disabledReason = \`Needs level ${spellLevelFor(c.sub, sp)}\`; else if (c.level < schoolGate(c.sub, sp.s)) disabledReason = \`${sp.s} opens at level ${schoolGate(c.sub, sp.s)}\`;` (grimoire membership is already guaranteed by the row source); keep the inCombat / combatOnly / charges branches and their strings byte-identical. test/unit/grimoireViewModel.test.js: re-pin the "Not ready yet" test to the level string and add a school-locked case and a Summoner-L1/Summon castable case.
    3. Never disable silently (D CMB-02 shell; Phase 25.1 DFB-06): renderCarriedList's `use` branch appends the Use button for every `it.kind === "potion" || it.use` row regardless of readiness (the row title already shows `· N sq`); the combat use-list's `useable` and `filter` clauses drop their readiness term (keep the potion-or-use term). The action bar renders `6 · Sing` for every Bard, appending ` (${left} sq)` when `!songReady()` where left mirrors the engine (`100 - (S.steps - (S.c.songAt ?? -999))`), and `7 · Scroll (n)` whenever `S.c.scrolls > 0` (canRead no longer hides it — a Pilfer's tap gets the engine's pilfer/noRunes toast); keys 6/7 follow the same conditions (Bard; scrolls > 0). A tap on any blocked button dispatches as today and the engine's refusal toast explains.
    4. Shield chip (D CMB-04): add `ward: { label: "Shield" }` to CONDITION_COPY and, in paintConditions before the generic `remaining` branch, `else if (cn.key === "ward") detail = \`${cn.pool} hp · ${cn.remaining} rds\`;` (the flight precedent). For the acute chip use the unit `S.combat ? "rds" : "sq"` (Plan 02 ticks it per step outside combat) — a one-line special case beside the ward branch, leaving CONDITION_COPY.acute's default unit for the fallback.
    4b. Afraid chip (D "Phobia = penalty, never a lost action"): REPLACE the CONDITION_COPY row keyed `phobia` (the DR17 row whose label is the word Phobia) with `afraid: { label: "Afraid", unit: "rds" }` (comment: Phase 31 — the Afraid penalty countdown; the engine's conditionsOf emits `afraid {remaining}` only while combat.afraid > 0) and DELETE the label chain's phobia special case (the ternary branch that built "Phobia: <fear>") so the generic `CONDITION_COPY[cn.key]?.label` + `remaining`/unit path renders "Afraid 2 rds" through the existing detail branch — no new special case; keep the affliction and foeEffect branches byte-identical. test/unit/foe-effect-chip.test.js L69-73: re-anchor the ordering assertion on the `cn.key === "affliction"` branch (foeEffect must come after affliction and before the generic fallback) and add one assertion that `afraid:\s*\{\s*label:\s*"Afraid",\s*unit:\s*"rds"\s*\}` exists and that the file has zero non-comment occurrences of the old phobia label-chain branch. Do NOT touch the classic dead combat code further down the file (its own freeze lines belong to the retained prototype copy that no shell path calls).
    4c. Sheet TO HIT label (31-ROLL-DIRECTION-AUDIT Finding 2 — a port regression in the display template, zero gameplay change): in src/browser/viewModels.js characterSheetViewModel change the `toHit` stat's value template so it renders the range `1–${toHit(state)}` (an EN DASH U+2013, the same character the DAMAGE row and the prototype's sheet use) instead of the number followed by a plus sign; add a one-line comment: "LOW-roll-good — a strike lands on d ≤ toHit, so the sheet shows the hitting range 1–N exactly as the prototype's Hero tab (mazeworld.html: \"1–\" + toHit()); the old plus suffix implied the opposite polarity (roll-direction audit Finding 2, Phase 31)." test/unit/characterSheetViewModel.test.js L43: re-pin to `` `1–${toHit(state)}` `` and add one assertion that the value starts with "1–" and ends with the numeric toHit. Nothing else in the stats array changes; toHit(state) itself is untouched. extract the classic `function canCast(sp) {…}` source; assert it contains `__mzCanCast` and does NOT contain the direct printed-level comparison; evaluate it with `new Function("window", "S", body-wrapper)` binding `window = { __mzCanCast: engineCanCast }`; pin: level-1 Summoner with Summon in grimoire → true; level-1 Illusionist with Phantom Host → true; level-1 Wizard with Fireball → false; then walk every (sub in MU subs × spell × level 1..5) with a grimoire containing that spell and assert classic === engine canCast for every cell. Also assert the module script contains `window.__mzCanCast = canCast`.
    6. Extend test/unit/shell-fight-gate.test.js with the Task 2 source pins: CONDITION_COPY has a `ward` row and an `afraid` row and no `phobia` row; the `cn.key === "ward"` branch renders both `hp` and `rds`; the renderCarriedList use branch no longer gates on readiness (the old `itemReady(it)) li.appendChild` form is absent); the combat use-list filter has no readiness term; `6 · Sing` renders for `S.c.sub === "Bard"`; the scroll button condition no longer includes `canRead()`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/spell-menu-mirror.test.js test/unit/grimoireViewModel.test.js test/unit/shell-fight-gate.test.js test/unit/spell-level-overrides.test.js test/unit/conditions.test.js test/unit/foe-effect-chip.test.js test/unit/characterSheetViewModel.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'window.__mzCanCast = canCast' mazeworld.html` == 1 and `grep -c '__mzCanCast(S, sp)' mazeworld.html` == 1
    - `grep -v '^\s*//' mazeworld.html | grep -c 'sp.lvl > S.c.level'` == 0
    - `grep -c 'Needs level' src/browser/viewModels.js` == 1 and `grep -c 'opens at level' src/browser/viewModels.js` == 1
    - `grep -c 'Not ready yet' src/browser/viewModels.js` == 0
    - `grep -v '^\s*//' mazeworld.html | grep -c 'itemReady(it)) li.appendChild'` == 0
    - `grep -c 'ward: { label: "Shield"' mazeworld.html` == 1 and `grep -c 'cn.key === "ward"' mazeworld.html` == 1
    - `grep -c 'afraid: { label: "Afraid", unit: "rds" }' mazeworld.html` == 1
    - `grep -v '^\s*//' mazeworld.html | grep -c 'phobia: { label: "Phobia"'` == 0
    - `grep -v '^\s*//' mazeworld.html | grep -c 'cn.key === "phobia"'` == 0
    - `grep -c '6 · Sing' mazeworld.html` >= 1
    - `grep -c '1–${toHit(state)}' src/browser/viewModels.js` == 1
    - `grep -c 'toHit(state)}+' src/browser/viewModels.js` == 0 and `grep -rc 'toHit(state)}+' src/browser/ | grep -v ':0' | wc -l` == 0 (no plus-suffixed to-hit template remains anywhere in the browser layer)
    - `grep -c '1–${toHit(state)}' test/unit/characterSheetViewModel.test.js` >= 1
    - `grep -c 'songReady() ? `<button id="a-sing"' mazeworld.html` == 0 (the Bard's button is no longer hidden while cooling)
    - node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>The combat spell menu can never disagree with the engine's gate (pinned cell-by-cell), the grimoire says why, blocked Use/Sing/Scroll buttons are visible and explained by the engine's toast, the Shield chip shows both numbers, the tracker shows "Afraid N rds" instead of a phobia name, and the roll screen's TO HIT reads the hitting range 1–N like the Hero tab.</done>
</task>

<task type="auto">
  <name>Task 3: Build the www bundle and run the full gate</name>
  <files>mazeworld.html</files>
  <read_first>
    - tools/build-www.mjs (what build:www copies/validates), package.json scripts
    - .planning/phases/31-combat-start-gating-effect-hygiene/31-01-SUMMARY.md and 31-02-SUMMARY.md (their "Human verification (deferred to end of run)" blocks — copy them forward into this plan's SUMMARY with this plan's items so the end-of-run device pass has one list)
  </read_first>
  <action>
    1. Run `npm run build:www` — must exit 0 (fix any shell syntax slip from Tasks 1-2 rather than skipping the build).
    2. Run `npm test` — `# fail 0`; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` empty.
    3. In the SUMMARY, consolidate the three plans' human-verification items under "## Human verification (deferred to end of run)" (the Pixel 7 pass happens at the end of the autonomous run per STATE.md), and note for Phase 32: the Fight! gate is now `combat.pending` (design doc §6.1 wrote the DR17 presentation flag), `combatJoined`/`phobiaAfraid`/`combatInDark` arrive in the `fight` action's events (§6.4's "Encounter start" fold row — the freeze row is gone: a phobia is now the Afraid penalty, `phobiaAfraid {rounds}` at Fight! and `fearPassed` when it ends, with `needMods: [{name:'afraid'}]` on strikeMissed/struck/spellThrown and `afraid: true` on struck/spellHit for the Round Card to render as a penalty line, never a lost turn), and refusals (`castRefused`/`useRefused`/`actionRefused` + the new reasons) are PRIORITY.block toasts that must stay toasts (§6.4 "Refusals and blocks").
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && npm run build:www 2>&1 | tail -3 && npm test 2>&1 | grep -E '^# (pass|fail)' && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:www` exit code 0
    - `npm test` → `# fail 0` and `# pass` >= 1650
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing
  </acceptance_criteria>
  <done>The bundle builds, the suite is green, fixtures/master untouched, and the deferred device checklist plus Phase 32 hand-off notes are in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM taps/keys → engineCombatAction/inventoryAction | untrusted UI input becomes validated actions; the engine refuses out-of-turn actions itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-07 | Tampering | keydown while pending | low | mitigate | the key gate reads the ENGINE's combat.pending; even a bypass only reaches refuseIfPending (notFought) |
| T-31-08 | Spoofing | classic spell gate drift | low | mitigate | one bridge to engine canCast + the cell-by-cell mirror test |
</threat_model>

<verification>
- `npm run build:www` exit 0; `npm test` `# fail 0`; fixtures/master untouched.
- Shell source pins (shell-fight-gate, spell-menu-mirror) green; grimoireViewModel re-pinned.
</verification>

<success_criteria>
- ROADMAP success criteria 1-4 are player-visible: preview then Fight!; refusals explained; potions from Gear; Shield chip with pool + rounds. Criteria 5-6 (expiry, Amulet) are engine-complete from Plan 02 and surface through existing chips/loot card.
</success_criteria>

<human_verification>
Deferred to the end of the autonomous run (copy into SUMMARY.md under "## Human verification (deferred to end of run)", merged with Plans 01/02's items):
- Pixel 7: encounter → roster + Fight! only; the Oracle has no initiative/strike line until Fight! is tapped; tap Fight! → "You/They move first." toast, then the opener; a phobia (matching foe type) shows the "Afraid. Your aim wobbles for 2 rounds." toast after Fight! and the tracker's "Afraid 2 rds" chip — tapping Strike SWINGS at once (roll vs the shrunk need, e.g. "4 vs 2 (needs 2: afraid −3)"; a hit shows the halved damage and "Fear pulls the blow."), tapping Spells → a spell CASTS (no refusal toast for fear), Items/Flee/Parley all respond; the chip counts 2 → 1 → gone and "The fear passes." prints — your to-hit range shrinks by 3 while afraid, never a lost turn, never a "frozen" line.
- Roll (or dev-start) a level-1 Summoner and a level-1 Illusionist: the combat SPELLS menu lists Summon / Phantom Host and casting works.
- Hero tab Grimoire: a spell above level reads "Needs level N"; a school-locked one reads "<school> opens at level N".
- Gear tab: every potion/use item shows Use; a staff on cooldown shows the countdown and its tap toasts the squares left; Acuteness drunk in a corridor shows the Acute chip in sq, then rds in a fight.
- Cast Shield (or use a Rowan Staff): the chip reads "Shield <pool> hp · <rounds> rds" and both numbers fall as expected.
- Bard: "6 · Sing (N sq)" shows while cooling and its tap toasts the wait.
- Roll a new character: the roll screen's TO HIT reel reads "1–5" (a Fighter) / "1–3" (a Magic User) — a range, never "5+" — and matches the Hero tab's "To hit 1–N" line after the roll.
</human_verification>

<output>
Create `.planning/phases/31-combat-start-gating-effect-hygiene/31-03-SUMMARY.md` when done (consolidated human-verification list + the Phase 32 hand-off notes).
</output>
