---
phase: 31-combat-start-gating-effect-hygiene
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - engine/magic.js
  - engine/items.js
  - engine/combat.js
  - engine/movement.js
  - engine/derived.js
  - src/browser/toasts.js
  - src/browser/eventNarration.js
  - test/parity/magic-parity.test.js
  - test/unit/magic.test.js
  - test/unit/items.test.js
  - test/unit/conditions.test.js
  - test/unit/afraid.test.js
  - test/unit/cast-refusals.test.js
  - test/unit/item-combat-gate.test.js
  - test/unit/effect-expiry.test.js
  - test/unit/usable-features-audit.test.js
  - docs/USABLE-FEATURES-AUDIT.md
autonomous: true
requirements: [CMB-02, CMB-03, CMB-04, CMB-05, CMB-06]

must_haves:
  truths:
    - "CMB-02: every refusal path in castSpell/readScroll/useItem/sing emits exactly one explaining event before any side effect (no charge spent, no cooldown burned, no item consumed, zero rng — fakeRng([]) proves it): castRefused {spell, reason} for combatOnly/notFought; noChargesLeft/spellNotKnown/spellAboveLevel/spellSchoolLocked keep their own types (they already self-explain — RESEARCH 'extend, don't replace'); useRefused {item, reason} for cooldown{left}/wrongClass/pilfer/combatOnly/notFought; scrollRefused for noScrolls/pilfer/noRunes/notFought; actionRefused {action:'sing'} for cooldown{left}/wrongClass/notFought — and NO refusal of any type carries a fear reason (D: phobia is a penalty, never a lost action; the refusal vocabulary has no 'frozen')"
    - "CMB-02 probe (ordering): castSpell's guard ladder is notFought → noChargesLeft → spellNotKnown → spellAboveLevel → spellSchoolLocked → combatOnly, then the cast; a spell both unknown and above level reports spellNotKnown; a targeted cast whose C.target points at a corpse retargets to the first live foe exactly like playerStrike (noTarget is unreachable in combat; outside combat combatOnly fires first)"
    - "Afraid, spell/item side (D 'Phobia = penalty, never a lost action', user correction: to-hit is a LOW range): while combat.afraid > 0 the thrown-spell target number SHRINKS by AFRAID_TO_HIT_PENALTY via afraidNeed (4 → 1, Freeze 6 → 3; floor 1) — the same helper playerStrike uses, so a d8 roll of 4 lands unafraid and misses afraid — and every point of damage the HERO deals is halved through afraidDamage (thrown, Earthquake, Volley, and the Pine Staff's fire; Math.ceil, min 1); spellThrown carries the effective need plus needMods [{name:'afraid', delta}], spellHit carries afraid:true; casting, scroll-reading, item use, flight and parley stay fully available while afraid (afraid.test.js proves no *Refused event ever names fear); zero new rng draws"
    - "CMB-02 probe (empty): a caster with an empty grimoire gets spellNotKnown for every idx; a Bard with songAt fresh gets actionRefused cooldown with a positive integer left; a non-Bard singing gets actionRefused wrongClass"
    - "CMB-03: every content/potions.js kind (heal/poison/speed/full/strength/disease/enlarge/acute/death/invis) resolves identically with state.combat null and non-null; the five targeted staff kinds (freeze/weaken/stone/fire/gas) outside combat return useRefused combatOnly with usedAt untouched, the item still carried, and zero draws; a non-Magic-User using a staff gets useRefused wrongClass"
    - "CMB-04: conditionsOf on c.ward {pool:34, rounds:3, name:'Shield'} yields {key:'ward', polarity:'good', pool:34, remaining:3, name:'Shield'} placed after might and before flight; pool 0 or ward null yields no ward entry; conditionsOf stays a pure read"
    - "CMB-05: c.acute decrements by exactly 1 per foeTurn tail (after the ward/mirror ticks) and by exactly 1 per exploration step in move(); acute 1 → 0 emits acuteFaded once; acute 2 → 1 emits nothing; acute 0 stays 0 with no event and no key added; endCombat clears a positive acute to exactly 0 with one acuteFaded; counters are integers, never negative; ward/mirror keep their per-foeTurn tick + endCombat clear; regen/senses clear at endCombat; haste/invis/ether tick per step; might clears at newDay; halfNext is one-shot"
    - "CMB-06: an item kill that removes the last live foe (Amulet of Stone/Oak Staff stone, Pine Staff fire) pushes encounterCleared + combatEnded and nulls state.combat inside the same useItem call; stoning 4 of 5 foes leaves combat open with one live foe; every stoned foe pays through killFoe (c.sp/c.gold/c.kills change exactly as a melee kill of the same foe would); foeStoned {names} lists the stoned names in target order before the per-foe foeKilled lines"
    - "Parity: the three phobia fixtures were declared in Plan 01 (lose, lose-apprentice, cast-damage — action-path, fromAction 0); this plan adds NO new divergence: the thrown-spell penalty leaves cast-damage's measured end state exactly as declared (seed 8's d10 roll 1 still lands at the shrunk need 3 and Freeze kills through killFoe regardless of the halved hp) — the record is re-verified, not re-measured; the combatOnly guard, the retarget line, the useItem gate, the item-kill close and the expiry ticks are all guarded on state no fixture carries; every other fixture is byte-identical; prototype-master.js.txt untouched; no fixture regenerated"
    - "Prohibition: no new rng draws (every new branch is a refusal/clear/decrement guarded on state the fixtures never carry, and afraidNeed/afraidDamage are pure arithmetic); refusals are toasts at PRIORITY.block, never ORACLE_ONLY; no Round Card work"
  artifacts:
    - "engine/magic.js: combatOnly guard and the retarget line in castSpell; afraidNeed on the thrown target + needMods on spellThrown; afraidDamage on the thrown/quake/volley damage + afraid:true on spellHit; NO fear guard in castSpell or readScroll"
    - "engine/items.js: TARGETED_KINDS, useRefused cooldown/wrongClass/combatOnly, foeStoned, afraidDamage on the fire staff's damage, the narrow cleared-check after the switch"
    - "engine/combat.js: sing refusals (actionRefused), foeTurn tail acute tick (between mirror and foeEffect; Plan 01's afraid tick stays last), endCombat acute clear"
    - "engine/movement.js: per-step acute tick; engine/derived.js: conditionsOf ward entry (beside Plan 01's afraid entry)"
    - "src/browser/toasts.js + eventNarration.js: foeStoned, acuteFaded; spellThrown narration renders needModsClause and spellHit's afraid flag"
    - "docs/USABLE-FEATURES-AUDIT.md (fear is a PENALTY row, never a refusal row) + test/unit/usable-features-audit.test.js (table-driven, doc-synced)"
    - "test/unit/cast-refusals.test.js, item-combat-gate.test.js, effect-expiry.test.js; afraid.test.js extended with the spell/item side; conditions/items/magic re-pins"
  key_links:
    - "useItem stone/fire → killFoe (payout) → narrow cleared-check → encounterCleared + endCombat → Phase 29's loot card (shell)"
    - "foeTurn tail / move() per-step / endCombat → c.acute → conditionsOf acute chip"
    - "c.ward (magic.js ward kind, items.js dome) → conditionsOf ward {pool, remaining} → Plan 03's two-number chip"
    - "combat.afraid > 0 → magic.js thrown branch: target = afraidNeed(state, base) → spellThrown {need, needMods} → roll - bonus <= target; dmg = afraidDamage(state, dmg) → spellHit {dmg, afraid:true}"
    - "usable-features-audit.test.js CASES ↔ docs/USABLE-FEATURES-AUDIT.md (every reason string and feature name in CASES must appear in the doc; 'Afraid' appears as a penalty, and no refusal reason named after the old freeze appears anywhere)"
---

<objective>
Land the refusal vocabulary and the four effect/kill fixes on the Plan 01 engine: `castSpell` refuses combat-only spells outside combat (and retargets a dead target like Strike) — and is NEVER refused for fear: instead the Afraid penalty Plan 01 put on strikes is extended to the spell/item side (thrown-spell target shrinks by 3 via `afraidNeed`, all hero-dealt spell/item damage halves via `afraidDamage`); `useItem` refuses cooldown/wrong-class/targeted-outside-combat instead of silently fizzling, narrates `foeStoned`, and closes the encounter after any item kill (CMB-03/CMB-06); Acuteness finally counts down (per round in combat, per step outside) and clears at `endCombat` (CMB-05); `conditionsOf` surfaces the ward as `{pool, rounds}` (CMB-04). No new fixture divergence (the three phobia records were declared in Plan 01; cast-damage's is re-verified here). `docs/USABLE-FEATURES-AUDIT.md` + a table-driven, doc-synced test complete the CMB-02 audit, recording fear as a penalty row.

Purpose: every refusal to act explains itself, fear penalises but never blocks, buff potions work from Gear, the Shield has chip data, no round-based effect lingers, and the Amulet of Stone ends a fight with a full payout — all engine-side, so Plan 03 only wires the shell.
Output: engine refusals/expiry/kill-close + spell/item-side Afraid + toast/Oracle copy + audit ledger + tests; `npm test` `# fail 0`, master untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-CONTEXT.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-RESEARCH.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spell/scroll/song refusals (combatOnly, cooldown — never fear), the dead-target retarget, and the spell/item side of the Afraid penalty</name>
  <files>engine/magic.js, engine/combat.js, engine/items.js, src/browser/toasts.js, src/browser/eventNarration.js, test/parity/magic-parity.test.js, test/unit/magic.test.js, test/unit/cast-refusals.test.js, test/unit/afraid.test.js</files>
  <read_first>
    - engine/magic.js L48-75 (castSpell's guard ladder as left by Plan 01: the refuseIfPending line, then charges, then canCast's three diagnostics), L92-105 (the resist block — `C && !RESIST_IMMUNE_KINDS`), L160/175/227/348 (the four `C.foes[C.target]` readers: blind/acid/petrify/thrown), L180-190 (Earthquake: `d = rollDice(rng, sp.dmg) * mult` then damageFoe per live foe), L215-225 (Volley: per-target `d = rollDice(rng, sp.dmg)` then damageFoe), L344-372 (the thrown branch: `const target = freeze ? 6 : 4;` `roll = rng.d(dieN)`, the `spellThrown {roll, need: target, bonus}` push, `if (roll - bonus <= target)`, `dmg = rollDice(...) * mult + eff(c,"spellDmg")`, damageFoe, `spellHit {dmg, mult}`, the Freeze kill through killFoe — hp-independent), L437-473 (readScroll)
    - engine/derived.js: Plan 01's AFRAID_* constants and afraidNeed/afraidDamage (pure; need-side, floor 1; Math.ceil, min 1) — import both helpers into magic.js and items.js
    - engine/combat.js L416-418 (the retarget line to mirror), L927-940 (songReady/sing), Plan 01's playerStrike afraid site (the needMods/afraid payload convention to mirror)
    - engine/items.js L915-925 (the `fire` case: `dmg` then `damageFoe(state, t, dmg, { kind: "item" … })`)
    - content/spells.js (every row's combatOnly flag: Mirror Self/Shield/Bubble/Regeneration/Sense Presence/Summon/Phantom Host/heals/Strength/Detect Magic/Sense Danger are castable outside combat; the 20 others are combatOnly)
    - test/parity/fixtures/action-script.magic.json (cast-damage's Phase 31 action-path record from Plan 01: fromAction 0, fields sp/gold/kills/rations, after sp 5/gold 51/kills 1/rations 5 — this task must leave it TRUE) and test/parity/magic-parity.test.js (already action-path-aware after Plan 01)
    - src/browser/eventNarration.js (spellThrown/spellHit narrations; needModsClause helper) and src/browser/toasts.js (the spellCast folder that groups spellThrown/spellHit/frozenSolid — add nothing to the folder, only the standalone builders may mention fear)
    - test/unit/magic.test.js L168-173 (nothingToThrowAt pin), L226-234 (Earthquake warded with combat null), L320-345 (the 04-DR10 "documents WHY combatOnly" block: Earthquake/Death cast with combat null) and any other test casting a combatOnly spell with `combat: null`
    - test/unit/freeze-pays-out.test.js (the FID-06 unit pin), test/unit/afraid.test.js (Plan 01's strike-side pins and helpers — extend, do not duplicate)
    - Probe facts (verified at plan time): cast-damage seed 8 = Illusionist, phobia Beasts, no Hardiness → afraid 2 at Fight!, first "you"; the scripted castSpell idx 4 (Freeze) rolls d10 = 1 vs need 6 today; under the penalty need becomes 3 and 1 ≤ 3 still lands; dmg 3 → 2 but Freeze kills through killFoe regardless, so sp/gold/kills/rations end exactly as the Phase 23/31 record declares. The scroll scenario (seed 7, Wizard) reads Stupidity (combatOnly) outside combat: today a silent no-op; after the combatOnly guard a castRefused with byte-identical state (no draw, spellsUsed restored by readScroll).
  </read_first>
  <action>
    ENGINE (D "not ready yet" audit, CMB-02; D "Phobia = penalty, never a lost action" — there is NO fear refusal: a caster who is afraid casts, at a penalty):
    1. engine/magic.js castSpell: after the canCast diagnostics block and BEFORE `c.spellsUsed++`: `if (sp.combatOnly && !state.combat) { events.push({ type: "castRefused", spell: sp.n, reason: "combatOnly" }); return events; }` — applies to scroll casts too (a scroll of Fireball read in a corridor is still spent by readScroll, but nothing fizzles, no Apprentice backfire d8 is drawn, and Earthquake can no longer self-damage a caster with no foes — RESEARCH 4.4). Then, still before `c.spellsUsed++`: `if (C) { const cur = C.foes[C.target]; if (!cur || !cur.alive) C.target = C.foes.findIndex((f) => f.alive); }` — the playerStrike retarget, so blind/acid/petrify/thrown never silently no-op on a corpse while burning a charge (noTarget is thereby unreachable in combat; document that in the ledger instead of adding a dead reason emitter). Do NOT add any guard on `combat.afraid` anywhere in castSpell or readScroll — RESEARCH §2/open question 2 proposed a fear refusal for casting; the user ruled it out (2026-09-16): fear is a penalty, never a refusal.
    2. engine/magic.js thrown branch (the ONLY spell to-hit roll in the engine — Earthquake/Volley/Death/etc. roll no attack die): replace `const target = freeze ? 6 : 4;` with `const baseTarget = freeze ? 6 : 4; const target = afraidNeed(state, baseTarget); const afraidMods = target !== baseTarget ? [{ name: "afraid", delta: target - baseTarget }] : [];` and spread `...(afraidMods.length ? { needMods: afraidMods } : {})` into the `spellThrown` push (its `need: target` is then the effective, shrunk need — the Oracle's roll-vs-need line stays honest). The compare `roll - bonus <= target` is unchanged in form. Comment: "Phase 31 Afraid — to-hit is a LOW range, so the target SHRINKS (4 → 1, Freeze 6 → 3), never the roll; pure arithmetic, zero rng; fixture-visible only on the declared cast-damage record, where d10 = 1 still lands."
    3. engine/magic.js damage halving (the hero's spell damage — Claude's discretion on scope: every point the HERO deals directly, not companions/wards/acid ticks): in the thrown branch `const dmg = afraidDamage(state, rollDice(rng, sp.dmg) * mult + eff(c, "spellDmg"));` and spread `...(afraidMods.length ? { afraid: true } : {})` into the `spellHit` push; in Earthquake `const d = afraidDamage(state, rollDice(rng, sp.dmg) * mult);`; in Volley `const d = afraidDamage(state, rollDice(rng, sp.dmg));`. engine/items.js `fire` case: `dmg = afraidDamage(state, dmg)` before its damageFoe. Every site is post-roll arithmetic (the dice are drawn exactly as before — zero rng change) and a no-op whenever `combat.afraid` is falsy.
    4. engine/combat.js sing: after the refuseIfPending line replace the silent `if (!C || !songReady(state)) return events;` with: `if (!C) return events;` then `if (!songReady(state)) { events.push({ type: "actionRefused", action: "sing", reason: c.sub === "Bard" ? "cooldown" : "wrongClass", ...(c.sub === "Bard" ? { left: 100 - (state.steps - (c.songAt ?? -999)) } : {}) }); return events; }` (left is a positive integer; songReady's own arithmetic, mirrored).
    5. Presentation: src/browser/eventNarration.js `spellThrown` appends `needModsClause(e.needMods, e.need)` after its need (absent needMods renders byte-identical); `spellHit` appends ` <span class="miss">Fear pulls the spell.</span>` when `e.afraid`. toasts.js: no new type (spellThrown/spellHit already have builders and a folder); leave the folder's text alone.

    PARITY (no new divergence — re-verify Plan 01's record):
    6. Run `node --test test/parity/magic-parity.test.js test/parity/full-suite.test.js`: cast-damage must pass against the record Plan 01 declared (`declaredEndDiffs` null both ways with the same `after`). If it fails on `after`, the thrown penalty is wrong-way (a shrunk need of 3 must still accept roll 1) — fix the engine, never the record. Nothing in any fixture JSON changes in this task; `git status --porcelain test/parity/fixtures` must stay empty. test/parity/magic-parity.test.js is touched only if its cast-damage post-assertions need the spellThrown `need`/`needMods` payload pinned (optional: assert the recorded spellThrown event has `need: 3` and `needMods[0].name === "afraid"`).

    TESTS:
    7. test/unit/magic.test.js: re-pin every test that casts a combatOnly spell with `combat: null` (the nothingToThrowAt pin at ~L168, the warded Earthquake at ~L226, the 04-DR10 block at ~L320-345) to: one `castRefused {reason:"combatOnly"}`, `c.wp` and `c.spellsUsed` unchanged, and `fakeRng([])` (zero draws). Keep every in-combat test as is.
    8. Create test/unit/cast-refusals.test.js (copy magic.test.js's helpers): the ladder order (a caster with zero charges reports noChargesLeft; a known spell above level reports spellAboveLevel not combatOnly; an unknown combatOnly spell outside combat reports spellNotKnown); retarget (C.target on a dead foe, cast Acid → acidApplied on the first live foe and C.target updated); sing: non-Bard → actionRefused wrongClass; Bard with songAt = steps → actionRefused cooldown with left 100; Bard ready → sang; scroll of a combatOnly spell read outside combat → scrollRead + scrollCast + castRefused combatOnly with the scroll consumed and spellsUsed restored; and the negative pin: with `combat.afraid = 2`, castSpell (a thrown spell, fakeRng sized for the roll + damage) and readScroll each proceed — `castRefused`/`scrollRefused` ABSENT, `spellThrown` present.
    9. Extend test/unit/afraid.test.js with the spell/item side: (x) THROWN DIRECTION: same caster/foe/fakeRng, roll 4 with bonus 0 — unafraid `spellThrown.need === 4` and `spellHit`; with `combat.afraid = 2` `spellThrown.need === 1`, `needMods` deepEqual `[{ name: "afraid", delta: -3 }]`, `spellMissed`, and the foe's wp untouched; Freeze: base 6 → 3, a d10 roll of 3 lands afraid, a 4 misses; (xi) THROWN DAMAGE: a landing thrown spell with rollDice forced to 3 → `spellHit.dmg === 2` and `spellHit.afraid === true`; forced to 1 → 1; (xii) Earthquake and Volley halve per hit (compare `foe.wp` deltas unafraid vs afraid with identical fakeRng) — and draw the SAME number of rng values (counting rng); (xiii) Pine Staff fire while afraid halves (`itemBurned`/foe wp delta), min 1; (xiv) a scroll read while afraid casts (no `scrollRefused`).
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/cast-refusals.test.js test/unit/afraid.test.js test/unit/magic.test.js test/unit/freeze-pays-out.test.js test/parity/magic-parity.test.js test/parity/full-suite.test.js test/parity/fixture-inventory.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js 2>&1 | tail -5 && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' engine/magic.js | grep -c 'reason: "combatOnly"'` == 1
    - `grep -v '^\s*//' engine/magic.js engine/combat.js engine/items.js | grep -c 'reason: "frozen"\|reason: "afraid"'` == 0 (no refusal is ever named after fear)
    - `grep -v '^\s*//' engine/magic.js | grep -c 'C.foes.findIndex((f) => f.alive)'` == 1
    - `grep -v '^\s*//' engine/magic.js | grep -c 'afraidNeed(state, baseTarget)'` == 1 and `grep -v '^\s*//' engine/magic.js | grep -c 'afraidDamage(state,'` == 3 (thrown, quake, volley) and `grep -v '^\s*//' engine/items.js | grep -c 'afraidDamage(state,'` == 1
    - `grep -v '^\s*//' engine/combat.js | grep -c 'action: "sing"'` >= 1 (Plan 01's notFought site plus this cooldown/wrongClass site)
    - `git status --porcelain test/parity/fixtures` prints nothing (the record declared in Plan 01 still holds; nothing re-measured here)
    - `grep -c 'needModsClause' src/browser/eventNarration.js` >= 6 (Plan 01's five plus spellThrown)
    - node --test over the files in verify → `# fail 0`; `git status --porcelain test/parity/prototype-master.js.txt` empty
  </acceptance_criteria>
  <done>A combat-only spell in a corridor, a cooling Bard, and a dead target all produce one explaining event and no side effect; an afraid caster casts at a shrunk target and half damage instead of being refused; the cast-damage record still holds with no re-measurement; every refusal reason is in the fixed vocabulary and none is fear.</done>
</task>

<task type="auto">
  <name>Task 2: useItem gate + item-kill close + foeStoned (CMB-03/06), Acuteness/effect expiry (CMB-05), ward chip data (CMB-04)</name>
  <files>engine/items.js, engine/combat.js, engine/movement.js, engine/derived.js, src/browser/toasts.js, src/browser/eventNarration.js, test/unit/items.test.js, test/unit/conditions.test.js, test/unit/item-combat-gate.test.js, test/unit/effect-expiry.test.js</files>
  <read_first>
    - engine/items.js L18-40 (imports — `killFoe` already comes from ./combat.js; add liveFoes/endCombat to that same import), L775-787 (itemReady), L797-939 (useItem as left by Plan 01: refuseIfPending, the silent `!itemReady` return, pilfer, usedAt/itemUsed, the kind switch — heal/full/poison/disease/strength/enlarge/speed/haste/acute/invis/ether/half/death/dome/freeze/weaken/stone/fire/gas — and the consumption block)
    - engine/combat.js L370-374 (the narrow cleared-check precedent to copy verbatim), L560-625 (killFoe — the payout is already correct; do not batch or re-derive), L986-1023 (endCombat's clear block), L1784-1800 (foeTurn's tail as left by Plan 01: ward → mirror → foeEffect → afraid ticks — add acute between mirror and foeEffect; Plan 01's afraid tick stays LAST)
    - engine/movement.js L293-295 (the per-step haste/invis/ether decrements — add acute beside them)
    - engine/derived.js L179-238 (conditionsOf — insert ward after the might line, before the flight block)
    - content/potions.js, content/treasure-tables.js L17-45 (jewelry/cloak/staff `use` kinds; the Amulet of Stone aoe:4; rollStaff adds every:250)
    - test/unit/items.test.js L204-235 (the potion/cooldown/death tests — the cooldown test asserts `events` deepEqual [] and must be re-pinned), test/unit/conditions.test.js L16-60 (cleanChar + the GOOD-counter pins), test/unit/combat.test.js's fixedCombat/fixedFoe helpers (copy for the new files)
    - src/browser/toasts.js (TOAST_FOR near the Phase 29 lootDropped entries; ORACLE_ONLY), src/browser/eventNarration.js (the matching region)
  </read_first>
  <action>
    useItem (engine/items.js) — D "Potions from Gear" + D "Amulet of Stone":
    1. Add `const TARGETED_KINDS = new Set(["freeze", "weaken", "stone", "fire", "gas"]);` (module scope, exported for the audit test). Rewrite useItem's head so the refusal ladder is, in order, all BEFORE `it.usedAt`/`itemUsed`: (a) `if (!it) return events;` (b) Plan 01's refuseIfPending; (c) `if (it.kind === "staff" && c.cls !== "Magic User") → useRefused {item: it, reason: "wrongClass"}`; (d) the existing pilfer refusal; (e) `const kind = …` then `if (!state.combat && TARGETED_KINDS.has(kind)) → useRefused {item: it, reason: "combatOnly"}`; (f) replace the silent `!itemReady` return with `if (!itemReady(state, it)) { events.push({ type: "useRefused", item: it, reason: "cooldown", left: it.every - (state.steps - (it.usedAt ?? -99999)) }); return events; }` (left is a positive integer). Every refusal leaves usedAt, the bag, and the rng untouched. Buff/heal kinds keep their combat-agnostic behaviour (RESEARCH 4.1 — CMB-03's "drinkable from Gear" is the engine's existing behaviour, now provably so).
    2. `stone` case: compute `const targets = foes.slice(0, it.aoe ?? 2);` push `{ type: "foeStoned", names: targets.map((f) => f.name) }` BEFORE the loop, then the existing per-foe `f.wp = 0; killFoe(...)` loop over `targets` (RESEARCH Pitfall 5 — do not batch the kills).
    3. After the consumption block, before `return events;`: the narrow cleared-check copied from combat.js L370-374 (`if (state.combat && !liveFoes(state).length) { events.push({ type: "encounterCleared" }); endCombat(state, events); }`). Import liveFoes/endCombat from ./combat.js. Do NOT call afterPlayerAction (RESEARCH 8.2 — items stay free actions).

    Effect expiry (D "effect expiry", CMB-05; orchestrator resolution 4 — applied to the DRUNK round-based effect, Acuteness; ward/mirror keep CONTEXT's stated per-round tick + endCombat clear, see the ledger row):
    4. engine/combat.js foeTurn tail, right after the mirror tick: `if (c.acute > 0 && --c.acute <= 0) events.push({ type: "acuteFaded" });`. endCombat, after `state.c.senses = 0;`: `if (state.c.acute > 0) { state.c.acute = 0; events.push({ type: "acuteFaded" }); }`. engine/movement.js per-step block, after the ether decrement: the same guarded decrement + acuteFaded. All three fire only when acute > 0 (no fixture ever sets it — useItem is fixture-free), never add a key, never draw.
    5. engine/derived.js conditionsOf: after the might line add `if (c.ward && c.ward.pool > 0) out.push({ key: "ward", polarity: "good", pool: c.ward.pool, remaining: c.ward.rounds, name: c.ward.name });` and extend the JSDoc's condition list.

    Presentation:
    6. TOAST_FOR + EVENT_NARRATION: `foeStoned` (toast: `${names.join(", ")} turn to stone. Statues don't hit back.`, tone hit, PRIORITY.you; Oracle: same idea with the names in a hit span) and `acuteFaded` (toast: "Your edge dulls.", tone beat, PRIORITY.other; Oracle: `<span class="beat">The Acuteness wears off.</span>`). Neither in ORACLE_ONLY.

    Tests:
    7. test/unit/items.test.js: re-pin the cooldown test to expect exactly one `useRefused {reason:"cooldown", left: 240}` (every 250, usedAt 10, steps 20) with `state2.c.ward` still null and usedAt unchanged.
    8. Create test/unit/item-combat-gate.test.js: for each TARGETED_KINDS staff outside combat → useRefused combatOnly, usedAt undefined, item still in the bag, fakeRng([]) (zero draws); a non-MU with an Oak Staff → wrongClass; each potion eff (all 10) outside combat vs inside combat — same wp/might/haste/acute/invis/ether/halfNext/affliction outcome given the same fakeRng; Amulet of Stone (aoe 4) on 1 foe → foeStoned [name], foeKilled, encounterCleared, combatEnded, combat null, and c.sp/c.gold/c.kills equal a control state where the same foe was killed by `killFoe` directly with the same fakeRng; Amulet on 5 foes → 4 stoned, combat open with exactly 1 live foe, foeStoned.names length 4 in roster order; Oak Staff (aoe default 2) on 2 foes → cleared; Pine Staff fire that kills the last foe → cleared; a freeze/gas use (no kill) leaves combat open; a heal potion in combat does NOT trigger a foe turn (no struckByFoe/foeMissed).
    9. Create test/unit/effect-expiry.test.js: acute 2 → foeTurn (1 foe, fakeRng sized so the foe misses) → acute 1, no acuteFaded; acute 1 → foeTurn → 0 + one acuteFaded; acute 0 → foeTurn → 0, no event, `"acute" in c` unchanged; endCombat with acute 5 → 0 + one acuteFaded, with acute 0 → no event; move() one step with acute 3 → 2 (use a fixedFloor with an open neighbour, or newRun + a legal dir), with acute 1 → 0 + acuteFaded; the tick order in foeTurn's tail is ward → mirror → acute → foeEffect → afraid (assert event order wardFaded, mirrorFaded, acuteFaded, foeEffectFaded, fearPassed when all five expire on the same tail — extend Plan 01's afraid.test.js order pin or assert it here once, not both); ward/mirror/regen/senses still clear at endCombat; haste/invis/ether still tick per step; might still clears at newDay (a counting rng proves zero extra draws for every case).
    10. test/unit/conditions.test.js: add `ward: null` to cleanChar; new tests: ward {pool:34, rounds:3, name:"Shield"} → the exact entry after might/before flight; pool 0 → absent; purity round-trip unchanged.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/items.test.js test/unit/item-combat-gate.test.js test/unit/effect-expiry.test.js test/unit/conditions.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/parity/full-suite.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' engine/items.js | grep -c 'TARGETED_KINDS'` >= 2 (definition + gate) and `grep -v '^\s*//' engine/items.js | grep -c 'reason: "cooldown"'` == 1 and `grep -c 'reason: "wrongClass"' engine/items.js` >= 2 (the existing itemRejected + the new useRefused)
    - `grep -v '^\s*//' engine/items.js | grep -c 'type: "foeStoned"'` == 1 and `grep -v '^\s*//' engine/items.js | grep -c 'endCombat(state, events)'` == 1
    - `grep -v '^\s*//' engine/combat.js | grep -c 'type: "acuteFaded"'` == 2 (foeTurn tail + endCombat) and `grep -v '^\s*//' engine/movement.js | grep -c 'type: "acuteFaded"'` == 1
    - `grep -v '^\s*//' engine/derived.js | grep -c 'key: "ward"'` == 1
    - `grep -c 'foeStoned\|acuteFaded' src/browser/toasts.js` >= 2 and `grep -c 'foeStoned\|acuteFaded' src/browser/eventNarration.js` >= 2
    - node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>Targeted items refuse outside combat without burning anything, staves refuse non-casters, cooldowns explain themselves with the squares left, an item kill closes the encounter through the normal cleared path with a full payout and a foeStoned line, Acuteness ticks per round and per step and clears at endCombat, and the ward is enumerable with pool + rounds.</done>
</task>

<task type="auto">
  <name>Task 3: docs/USABLE-FEATURES-AUDIT.md + the table-driven, doc-synced audit test; full suite</name>
  <files>docs/USABLE-FEATURES-AUDIT.md, test/unit/usable-features-audit.test.js</files>
  <read_first>
    - docs/PARLEY-REBALANCE.md and docs/CLASS-PASS.md (ledger style: front matter, tables, a "verified, not a bug" register)
    - content/spells.js (32 rows), content/potions.js (10), content/treasure-tables.js (jewelry/cloaks/staves `use` fields), content/skills.js (skill names for the passive/active split), content/spell-level-overrides.js
    - engine/magic.js (castSpell's final ladder), engine/items.js (useItem's final ladder + TARGETED_KINDS), engine/combat.js (playerStrike wizard refusal + the Afraid penalty site, flee samurai/cloaker, parley ninja/masterOfArms/wilmsry/exhausted, sing, refuseIfPending), engine/movement.js#makeCamp (campFailed — an active feature with a refusal), engine/derived.js#canCast/spellLevelFor/schoolGate
    - RESEARCH.md §3.3 (itemReady steps-keyed cooldown — verified correct, not a bug), §5 (inventory seed), §6 (the full effect table), §2 (existing refusal vocabulary)
    - test/unit/identity-contract.test.js (the Phase 24 ledger-test precedent: a JS mirror of a doc table, cross-checked against the doc text)
  </read_first>
  <action>
    1. Write docs/USABLE-FEATURES-AUDIT.md (D CMB-02 deliverable; layout is Claude's discretion): header (phase, date, the one-line rule "a blocked button stays visible; tapping it dispatches; the engine's refusal event names the why; refusals are toasts — and stay toasts after Phase 32"); §1 Refusal vocabulary — a table mapping CONTEXT's reason list (cooldown{left}, wrongClass, exploreOnly, combatOnly, noTarget, noCharges, notFought, pilfer — there is deliberately NO fear reason; add a one-line note under the table: "Fear is a penalty, not a refusal — see §5b") to the ACTUAL event type(s) that carry it (noCharges → noChargesLeft; noTarget → nothingToThrowAt/insaneNoTarget/nothingToTurn/gateRefused plus "unreachable in combat: castSpell retargets like Strike"; exploreOnly → reserved, no engine emitter today) with the toast text and Oracle line for each; §2 Spells — all 32 rows: name, level (with the two overrides marked), school, kind, combatOnly, and columns for explore / combat / Fight!-pending / afraid (always: casts at the penalty — thrown kinds at need −3, all hero damage halved) / no charge / unknown / above level / school-locked → outcome or refusal reason; §3 Potions (10) × explore/combat/pending/Pilfer; §4 Items with `use` — staves (8; note wrongClass for non-casters and the every:250 cooldown), cloaks (invis/haste/ether + Cloak of Flying passive), jewelry (Pendant half, Amulet stone aoe 4; the rest passive), scrolls (noScrolls/pilfer/noRunes/notFought; afraid → reads normally), lockpicks (passive); §5 Class/race/sub-class ACTIVE features (Strike incl. the Wizard rule, Flee incl. Samurai/Cloaker, Parley incl. Ninja/Master of Arms/Wilmsry/exhausted/never-eligible-hidden, Sing incl. cooldown/wrongClass, Withdraw (Tracking), Make Camp (campFailed), Abandon) and an explicit "passive skills carry no refusal" list; §5b PENALTIES (not refusals) — the Afraid row (D "Phobia = penalty, never a lost action", user ruling 2026-09-16): trigger (type-matched phobia / Darkness in the dark / Death near death; Hardiness d2 halves the odds), effect (combat.afraid = AFRAID_ROUNDS 2; strike and thrown-spell to-hit RANGE shrinks by AFRAID_TO_HIT_PENALTY 3 — hit on d ≤ need, so need − 3, floor 1; all hero-dealt damage ÷ AFRAID_DMG_DIV 2, Math.ceil, min 1), what stays allowed (everything: strike, cast, drink, read, use, flee, parley, sing), countdown (per foeTurn tail, fearPassed at 0, clears with the combat), chip ("Afraid · N rds"), events (phobiaAfraid {rounds} / fearPassed), and the three declared fixtures; §6 Effect expiry — every timed field from RESEARCH §6 with unit / ticks-where / clears-where / Phase 31 change, including the Key Decision: Acuteness ticks per foeTurn in combat AND per step outside (timer starts the moment it is drunk from Gear) and clears at endCombat; ward/mirror tick per foeTurn and clear at endCombat (unchanged — a Shield cast before a fight still waits for the fight); regen/senses are booleans cleared at endCombat; haste/invis/ether per step; might until the next day; halfNext one-shot; afraid (combat.afraid — rounds; ticks per foeTurn tail; clears at endCombat with the combat object; Phase 31 NEW); §7 Verified-not-bugs (itemReady steps-keyed cooldown; the Hero-tab "On the combat screen" is routing, not a refusal); §8 Declared divergences (the three phobia fixtures — combat/lose, combat/lose-apprentice, magic/cast-damage — as action-path records from action 0 under the Afraid ruling, with their before/after; foresight consumed at Fight! time; encounterStarted no longer carries first / combatJoined does); §9 Shell obligations for Plan 03 (Use button always visible with countdown; combat spell menu bridged to engine canCast; grimoire reasons split; Sing button with countdown; ward chip).
    2. Create test/unit/usable-features-audit.test.js — a JS mirror, table-driven: a `CASES` array of `{ feature, circumstance, run: () => events, expect }` entries built with the magic/items/combat test helpers, where `expect` is either `{ refused: { type, reason } }` or `{ ok: <event type> }`. Generate rows programmatically: for EVERY spell in SPELLS — outside combat (known, level-legal, charges) → combatOnly ? castRefused combatOnly : its success event (or at least "not a *Refused/noChargesLeft/spellNotKnown" event — use a per-kind success map: heal→healed, ward→wardRaised, might→strengthCast, mirror→mirrorSelf, reveal→detectMagic, foresee→senseDanger, senses→sensesGained, regen→regenerationCast, summon→allyPending); in combat with a live target → the same per-kind success map for combatOnly kinds (thrown→spellThrown, stun→stunned, weaken→weakened, stupid→stupefied, blind→blinded, shrink→shrunk, acid→acidApplied, quake→earthquake, vapor→vaporRolled, volley→volley, petrify→petrified, insane→insaneRolled, status→dozed, death→deathCast, turn/gate against a matching foe type→walkingDeadTurned/planeGated); pending → castRefused notFought; afraid (combat.afraid = 2, a live target) → the SAME success event as in combat (never a *Refused — this row proves fear refuses nothing); spellsUsed = maxCharges → noChargesLeft; unknown → spellNotKnown; level too high → spellAboveLevel; school-locked → spellSchoolLocked. For EVERY potion: outside and inside combat → its state effect (assert the field, e.g. acute > 0 after Acuteness, might +8 after Strength); pending → useRefused notFought; Pilfer with a non-heal → useRefused pilfer. For EVERY staff: MU in combat → success (dome→ward set, freeze/gas→asleep, weaken→combat.weakened, stone→foeStoned, fire→itemBurned, invis→invis 100, heal→healed); MU outside combat → TARGETED_KINDS ? combatOnly : success; non-MU → wrongClass; on cooldown → cooldown with `left` a positive integer. Cloak/jewelry `use` kinds (half/stone/invis/haste/ether): the same pattern. Scrolls: noScrolls/pilfer/noRunes/notFought and afraid → scrollRead (no refusal). Strike/Flee/Parley/Sing: wizard/samurai/ninja/masterOfArms/cooldown/wrongClass/notFought and the success path, plus an afraid row for each (Strike → struck/strikeMissed with needMods afraid; Flee/Parley/Sing → their ordinary outcome, never a fear refusal). One `test()` per CASES row (name = feature + circumstance) asserting: exactly one event of the refused type with that reason for refusals (and, for refusals, that `fakeRng([])` was enough — zero draws — and `c.spellsUsed`/`it.usedAt`/bag length unchanged), or the success event present with NO `*Refused`/`noChargesLeft`/`spellNotKnown` event. Then the doc-sync tests: read docs/USABLE-FEATURES-AUDIT.md and assert every distinct `reason` string used by CASES, every SPELLS name, every POTIONS name, every STAVES/JEWELRY/CLOAKS name that carries a `use`, and every timed field name from the expiry section (acute, ward, mirror, regen, senses, haste, invis, ether, might, halfNext, foeEffect, flightLeft, afraid) appears verbatim in the doc, the strings "Afraid", "AFRAID_TO_HIT_PENALTY" and "penalty" appear in the doc, and the doc's refusal-vocabulary table contains NO row for a fear reason (assert the §1 table region has no cell reading `frozen` or `afraid`); and that every refused `{type, reason}` pair in CASES produces DISTINCT non-empty `TOAST_FOR[type](e).text` and `EVENT_NARRATION[type](e)` strings across reasons of the same type (no two reasons share a line).
    3. Full gate: `npm test` → `# fail 0`; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing (this plan declares no divergence; the three phobia records were committed by Plan 01).
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/usable-features-audit.test.js 2>&1 | tail -5 && npm test 2>&1 | grep -E '^# (pass|fail)' && git status --porcelain test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - docs/USABLE-FEATURES-AUDIT.md exists; `grep -c '^## ' docs/USABLE-FEATURES-AUDIT.md` >= 9; `grep -c 'Acuteness' docs/USABLE-FEATURES-AUDIT.md` >= 2; `grep -c 'notFought\|combatOnly\|wrongClass\|cooldown\|pilfer' docs/USABLE-FEATURES-AUDIT.md` >= 5; `grep -c 'Afraid' docs/USABLE-FEATURES-AUDIT.md` >= 3; `grep -c 'AFRAID_TO_HIT_PENALTY' docs/USABLE-FEATURES-AUDIT.md` >= 1
    - test/unit/usable-features-audit.test.js exists; `node --test test/unit/usable-features-audit.test.js 2>&1 | grep -E '^# pass'` reports >= 120 tests and `# fail 0`
    - `npm test` → `# fail 0`; `git status --porcelain test/parity/prototype-master.js.txt` empty; `git status --porcelain test/parity/fixtures` empty
  </acceptance_criteria>
  <done>The ledger lists every usable spell/item/gear piece/active feature × circumstance with its refusal reason and every timed effect's expiry; the table-driven test exercises each blocked and each allowed circumstance and keeps the doc honest; the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → applyAction useItem/castSpell `i`/`idx` | untrusted indices into the bag/SPELLS (already validated as non-negative integers; a bad index stays a no-op) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-04 | Tampering | useItem targeted kinds outside combat | low | mitigate | combatOnly refusal before usedAt/itemUsed — no cooldown burn, no consumption, no draw |
| T-31-05 | Elevation of Privilege | staff use by a non-caster via the loot pile | low | mitigate | useRefused wrongClass (stowItem never gated staves) |
| T-31-06 | Denial of Service | stranded combat after an item kill | medium | mitigate | narrow cleared-check → endCombat (pinned for stone/fire, 1-of-1 and 4-of-5) |
</threat_model>

<verification>
- `npm test` `# fail 0`; master untouched; no fixture diff at all in this plan (the three phobia records landed in Plan 01).
- Coverage guards + voice scan green with foeStoned/acuteFaded added.
- No `afterPlayerAction` call in useItem (grep) — items remain free actions.
</verification>

<success_criteria>
- ROADMAP criteria 2-6 hold at the engine/data level: every refusal explains itself (audit + test), buff potions work outside combat, ward chip data exists, Acuteness and every audited effect expire, Amulet of Stone ends the fight with a kill payout.
</success_criteria>

<human_verification>
Deferred to the end of the autonomous run (copy into SUMMARY.md under "## Human verification (deferred to end of run)"; needs Plan 03's shell):
- Pixel 7: drink Acuteness from Gear in a corridor — the "Acute" chip appears and counts down as you walk; enter a fight — it keeps counting per round; when the fight ends the chip is gone and the Oracle shows "The Acuteness wears off."
- Use the Amulet of Stone on the last foe — "… turn to stone. Statues don't hit back.", the kill payout lines, and Phase 29's loot card (if a drop rolled) — no stranded combat screen.
- Tap a staff's Use in a corridor — a toast names why (wants a target); tap it on cooldown — the toast says how many squares are left and the row shows the countdown.
- In an afraid fight (the "Afraid · N rds" chip up), open Spells and cast a thrown spell (Fireball/Freeze): the cast goes ahead — never a refusal toast for fear — and the Oracle's line reads roll vs the shrunk need, e.g. "3 vs 1 (needs 1: afraid −3)"; a landed spell reads "… for N hp. Fear pulls the spell." with the halved number; your to-hit range shrinks by 3 while afraid, then the chip clears and "The fear passes." prints.
</human_verification>

<output>
Create `.planning/phases/31-combat-start-gating-effect-hygiene/31-02-SUMMARY.md` when done (record the Acuteness step-tick Key Decision, the damage-halving scope decision — hero-dealt weapon/spell/item damage only — and confirm the cast-damage record re-verified unchanged).
</output>
