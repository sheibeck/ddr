---
phase: 31-combat-start-gating-effect-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/combat.js
  - engine/derived.js
  - engine/magic.js
  - engine/items.js
  - engine/actions.js
  - engine/engine.js
  - tools/lib/tuning-bot.mjs
  - src/browser/toasts.js
  - src/browser/eventNarration.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/parity/full-suite.test.js
  - test/parity/pending-fight-audit.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixtures/action-script.magic.json
  - test/parity/FIXTURE-INVENTORY.md
  - test/unit/fight-gate.test.js
  - test/unit/afraid.test.js
  - test/unit/combat.test.js
  - test/unit/conditions.test.js
  - test/unit/combat-scaling.test.js
  - test/unit/foe-turn-draw-count.test.js
  - test/unit/toastTable.test.js
  - test/unit/tuning-bot.test.js
autonomous: true
requirements: [CMB-01]

must_haves:
  truths:
    - "CMB-01: after the encounter step (move into a dot tile, encounterDot, or a wandering-monster newDay), state.combat exists with pending:true, c.foresight is untouched, state.combat.first is undefined, and the events carry encounterStarted (without first) but no combatJoined/phobiaAfraid/combatInDark/foe-strike event — nothing rolls or strikes before fight"
    - "CMB-01 probe (rng): startCombat + fight, driven by ONE counting rng, consumes exactly the OPENER draw total the pre-split single call consumed for every draw-count table seed (3/14/17/303/8/127 — an opener-only pin added to test/unit/foe-turn-draw-count.test.js), and the full-fight totalDraws pins of the three NON-phobic seeds (3 → 12, 17 → 101, 303 → 66) stay byte-identical with fight chained after startCombat; the three phobic seeds' full-fight rows (14, 8, 127) are re-measured once under the ruling with a dated comment; a fight on a non-pending or null combat draws nothing and changes nothing"
    - "CMB-01 probe (order): fight draws in today's exact order — the two initiative d20s, then the conditional Hardiness rng.d(2) (only when a phobia condition is true AND the character has Hardiness), then combatInDark (0 draws), then the pre-emptive foeTurn only when first === 'foe' — and clears pending before any of them can be observed by a player action"
    - "While combat.pending, every player combat action other than fight is refused with exactly one explaining event carrying reason 'notFought' (strikeRefused / fleeRefused / parleyRefused / castRefused / useRefused / scrollRefused / actionRefused) and mutates nothing, draws nothing"
    - "Phobia = penalty, never a lost action (USER RULING 2026-09-16): a triggered phobia sets combat.afraid = AFRAID_ROUNDS (2) and emits phobiaAfraid {rounds:2} — with the SAME trigger conditions and the SAME conditional Hardiness rng.d(2) at the same site inside fight, and ZERO new rng draws (no shake-off roll); an afraid character CAN strike, cast, drink, read, use, flee, parley and sing — no refusal event of any type is ever emitted for fear, and no refusal reason named after the old freeze exists anywhere in engine/, src/browser/, tools/ or the tests"
    - "Afraid penalty math (to-hit is a LOW range — hit on d ≤ need): while combat.afraid > 0 the player's strike target number SHRINKS by AFRAID_TO_HIT_PENALTY (3), floor 1, applied as the LAST modifier after every existing need rule and never reviving an untouchable (need 0) foe — so the afraid need is strictly lower than the unafraid need for the same character, and a roll that hits unafraid can miss afraid; the player's weapon damage is halved (Math.max(1, Math.ceil(dmg / AFRAID_DMG_DIV))) after crit/weakened arithmetic; strikeMissed/struck carry the effective need plus an additive needMods [{name:'afraid', delta}] so the Oracle's roll-vs-need line stays honest"
    - "Afraid countdown: combat.afraid ticks down by exactly 1 at the foeTurn tail (the same site ward/mirror tick, so a foes-first opener inside fight ticks it 2 → 1 before the player's first action, exactly like a pre-cast ward), reaching 0 emits fearPassed exactly once, a combat with afraid 0/absent never emits it, and endCombat clears it implicitly (combat null, no chip, no fearPassed); conditionsOf yields {key:'afraid', polarity:'bad', remaining:N, phobia:<fear name>} iff state.combat.afraid > 0 (a Hardiness shrug-off shows no chip)"
    - "Parity: every non-phobia fixture is byte-identical; the THREE fixtures whose hero triggers a phobia — combat/lose (seed 14, Fridgian Soldier, Beasts), combat/lose-apprentice (seed 127, Human Apprentice, Beasts) and magic/cast-damage (seed 8, Wilmsry Illusionist, Beasts) — each carry a declared Phase 24-style action-path divergence record (kind action-path, fromAction 0, before = the prototype's measured end values, after = the engine's measured end values, rationale citing the user ruling 2026-09-16) consumed by actionPathDivergenceOf/skipsByteDiffAt/declaredEndDiffs; no seed, action list, or prototype-master.js.txt byte is edited; applyStartCombat chains a real fight so the four scripted startCombat scenarios advance exactly as far as the prototype's single call; reconcilePendingFight (compare-only clone) is wired into all three shared comparables AND the three local comparable() copies"
    - "Prohibition: no new rng draws (draw-count pins), master never edited (git status clean), refusals never silent, refusals never routed away from toasts (every new refusal type/reason has a TOAST_FOR builder at PRIORITY.block and is absent from ORACLE_ONLY), combat never serialized (rehydrate nulls combat; pending/afraid need no save-shape work)"
  artifacts:
    - "engine/combat.js: export function fight(state, rng, events) (initiative onward, the phobia trigger now sets combat.afraid), export function refuseIfPending(state, events, type, extra) (the ONE notFought guard), startCombat ends at the roster + pending:true + encounterStarted; playerStrike applies afraidNeed/afraidDamage and has NO fear branch that ends the turn; foeTurn's tail ticks combat.afraid and emits fearPassed"
    - "engine/derived.js: export const AFRAID_ROUNDS = 2, AFRAID_TO_HIT_PENALTY = 3, AFRAID_DMG_DIV = 2 (documented tuning knobs beside DEATH_PANIC_THRESHOLD); export function afraidNeed(state, need) and afraidDamage(state, dmg) (pure, zero rng); conditionsOf's afraid entry replaces the old phobia chip"
    - "engine/actions.js: 'fight' in ACTION_TYPES; engine/engine.js: case 'fight'"
    - "tools/lib/tuning-bot.mjs: decideAction returns {type:'fight'} first whenever state.combat.pending"
    - "src/browser/toasts.js + eventNarration.js: combatJoined, castRefused, actionRefused, phobiaAfraid, fearPassed entries (the two old phobia-freeze entries are gone); notFought/combatOnly/exploreOnly/noTarget/cooldown/wrongClass reason copy on every refusal builder; strikeMissed/struck Oracle lines render needModsClause"
    - "test/parity/harness/comparables.js: reconcilePendingFight (exported), applyStartCombat chains fight"
    - "test/parity/fixtures/action-script.combat.json: lose (extended) + lose-apprentice (new) action-path records; action-script.magic.json: cast-damage action-path record (replaces the Phase 23 field-only record); FIXTURE-INVENTORY.md Phase 31 section; magic-parity.test.js + full-suite.test.js magic sub-test understand action-path records"
    - "test/parity/pending-fight-audit.test.js: the standing A3 walk"
    - "test/unit/fight-gate.test.js: the CMB-01 pins; test/unit/afraid.test.js: the Afraid penalty/countdown/chip/never-refused pins (strike side; Plan 02 extends it with the spell/item side)"
  key_links:
    - "startCombat → state.combat.pending:true → applyAction({type:'fight'}) → combat.js#fight → rollInitiative/phobia trigger (combat.afraid = AFRAID_ROUNDS + phobiaAfraid)/combatInDark/foeTurn → delete pending"
    - "combat.afraid > 0 → playerStrike: need = afraidNeed(state, need) (last modifier, floor 1) and dmg = afraidDamage(state, dmg) → strikeMissed/struck {need, needMods:[{name:'afraid', delta}]} → eventNarration needModsClause; foeTurn tail: --combat.afraid → 0 → fearPassed; conditionsOf → {key:'afraid', remaining}"
    - "comparables.js#applyStartCombat → startCombat THEN fight on the same rng (real advance) ; *Comparable()/local comparable() → reconcilePendingFight FIRST (clone + fight via applyAction, never mutating the compared state)"
    - "actionPathDivergenceOf(scenario) → skipsByteDiffAt(pathDiv, i) from action 0 → declaredEndDiffs(ctx.S, engineState, pathDiv) both null, for lose / lose-apprentice (combat-parity + full-suite combat sub-test) and cast-damage (magic-parity + full-suite magic sub-test)"
    - "every player combat action → refuseIfPending as its first check → {type:'<own refusal type>', reason:'notFought'}"
---

<objective>
Split `startCombat` at the Fight! cut line (D: Fight! gating, CMB-01): the ENCOUNTER step keeps everything up to and including the roster/`encounterStarted` (minus `first`) and marks `state.combat.pending = true`; a NEW validated engine action `fight` performs everything from `rollInitiative` onward in today's exact draw order and emits `combatJoined {first}`. Every other combat action refuses with `notFought` while pending. In the same pass the phobia trigger that now lives inside `fight` stops freezing the hero and instead applies the "Afraid" PENALTY (D "Phobia = penalty, never a lost action", USER RULING 2026-09-16): `combat.afraid = 2` rounds of a −3 to-hit range and half damage on the player's strikes, counting down at the foeTurn tail — the old lost-first-strike shake-off branch is deleted, the two old events are renamed (`phobiaAfraid {rounds}` / `fearPassed`), and the chip becomes "Afraid · N rds". Parity stays byte-identical for every non-phobia fixture through a real `fight` chained inside the harness's `applyStartCombat` plus a compare-only `reconcilePendingFight` in every comparable; the three phobia-triggering fixtures (combat/lose, combat/lose-apprentice, magic/cast-damage) are declared Phase 24-style action-path divergences. The tuning bot presses Fight!. The A3 assumption (no fixture chains a non-fight action onto a pending combat) becomes a standing test.

Purpose: nothing rolls or strikes before the player presses Fight! — the root fix for the reported "enemy attacks before Fight!" bug — and a phobia never again costs the player an action, landed as engine groundwork Phase 32 builds against.
Output: `fight` action + `pending` flag + `combatJoined` event + `refuseIfPending` guard + the Afraid mechanic (constants, penalty, countdown, chip data, events) + harness reconcile/chain + three declared divergence records + pins; `npm test` fully green (`# fail 0`), no fixture seed/action edited, master untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-CONTEXT.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-RESEARCH.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-02-SUMMARY.md
</context>

<assumption_delta_decision>
noun: combat encounter lifecycle (encounter → fight)
decision: no-change
rationale: a new action (`fight`) splits an existing sequence in two; `state.combat` keeps its shape plus a transient `pending` flag (present only between the encounter step and `fight`, deleted — not set false — by `fight` so the joined combat object's key set is byte-identical to the prototype's) and, ONLY for a phobia-triggered fight, an `afraid` counter where the prototype had its boolean freeze flag (the three fixtures that reach this branch are declared divergences, so the key rename is never silently compared); no identity/primary-key change; combat is still never serialized (engine/saveState.js#rehydrate nulls `combat` unconditionally — confirmed, RESEARCH A1 closed); no invariant test warranted beyond the parity reconcile + the draw-count pins.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Split startCombat at the Fight! cut line, add the `fight` action, the `refuseIfPending` guard, the Afraid penalty (replacing the phobia freeze), the bot's Fight!, and the presentation entries</name>
  <files>engine/combat.js, engine/derived.js, engine/magic.js, engine/items.js, engine/actions.js, engine/engine.js, tools/lib/tuning-bot.mjs, src/browser/toasts.js, src/browser/eventNarration.js, test/unit/toastTable.test.js</files>
  <read_first>
    - engine/combat.js L113-135 (rollInitiative), L153-376 (startCombat — the cut line is `state.combat = {…}` at L218 / `const first = rollInitiative(state, rng)` at L219; the phobia block L349-356 — the trigger condition and its conditional Hardiness rng.d(2) stay byte-identical, only the two lines inside the braces change; combatInDark L357; the pre-emptive foeTurn + cleared check L358-374), L388-415 (playerStrike's refusal head — the five-line fear branch at L410-414 that ends the turn is DELETED), L440-460 (the per-attack need chain: strikeDie → frenzy 3 / toHit → asleep → hard-to-hit → fast → magicOnly → auto/hit — the afraid penalty goes AFTER the magicOnly line), L505-545 (crit ×2, the weakened halving, damageFoe, the struck push — the afraid halving goes right after the weakened line), L705-711 (flee head), L843-870 (parley head), L927-940 (songReady/sing head), L1025-1075 (afterPlayerAction — unchanged, it still rerolls initiative on round 2+; note foeTurn can run TWICE per player action when the foes win the reroll, so every foeTurn-tail counter — ward, mirror, and now afraid — ticks per foe turn), L1784-1797 (foeTurn's tail: ward → mirror → foeEffect ticks — the afraid tick goes AFTER the foeEffect block, last in the tail; Plan 02 later inserts acute between mirror and foeEffect)
    - engine/derived.js L23 (DEATH_PANIC_THRESHOLD — the constants block to extend), L325-345 (toHit — READ THIS FIRST: the game's to-hit is a LOW range, a strike lands when the die roll is ≤ need, so "harder to hit" means a SMALLER need; dazed already does `h = Math.max(1, h - 2)`, the afraid penalty follows that exact convention), L179-238 (conditionsOf — the phobia block at L214-235 is REPLACED by the afraid entry)
    - engine/magic.js L48-75 (castSpell's guard ladder), L403-416 (drinkPotion), L437-473 (readScroll)
    - engine/items.js L797-815 (useItem's head: the `!it || !itemReady` return and the pilfer refusal)
    - engine/actions.js (ACTION_TYPES + validateAction), engine/engine.js L52-150 (the dispatch switch)
    - tools/lib/tuning-bot.mjs L432-486 (decideAction's in-combat branch)
    - src/browser/toasts.js L47 (PRIORITY), L155-183 (ORACLE_ONLY), L185-258 (FEATURE_EVENTS incl. the "Refusals" block — L216-217 list the two old phobia-freeze types to rename), L262-270 (block helper), L723-780 (encounterStart folder — its L769 case for the old freeze type), L904-935 (encounterStarted/old-freeze/combatInDark/strikeRefused/scrollRefused builders — L927 and L934 are the two old freeze builders), L938-960 (strikeMissed/struck builders), L1025-1045 (the foeMissed needMods convention), L1246 (useRefused builder); src/browser/eventNarration.js L64-72 (needModsText/needModsClause helpers), L178-210 (encounterStarted/old-freeze/combatInDark/strikeRefused — L200 and L210 are the two old freeze narrations), L216-240 (strikeMissed/struck narrations), L464 (scrollRefused), L586 (useRefused). NOTE: `frozenSolid`, the foe-side `t.frozen` flag and `effect: "frozen"` belong to the FREEZE SPELL (a foe frozen solid) and to `allySpellHit` — they are NOT the phobia freeze and must not be touched; "frozen prototype" in comments means the master file
    - test/unit/toastTable.test.js L40-46 (REFUSAL_TYPES) and L48-58 (the block/priority invariant every refusal type must satisfy)
    - test/unit/formatEventsCoverage.test.js + test/unit/toastsCoverage.test.js headers (the coverage guards derive event types from `type: "…"` literals in engine/*.js — every new type needs BOTH an EVENT_NARRATION and a TOAST_FOR entry, and must not also sit in ORACLE_ONLY; once the two old types are no longer emitted, their table entries are dead copy and must be REMOVED, not left behind)
  </read_first>
  <action>
    <!-- planner-discipline-allow: phobiaFrozen -->
    <!-- planner-discipline-allow: shookOffFrozen -->
    <!-- planner-discipline-allow: combat.frozen -->
    <!-- planner-discipline-allow: C.frozen -->
    ENGINE SPLIT (engine/combat.js), per D "Fight! gating (CMB-01)":
    1. In `startCombat`, change the combat literal to `{ foes, type, round: 1, target: 0, spellOpen: false, tracked, pending: true }` and DELETE the `const first = rollInitiative(state, rng);` line that follows it. Keep the `encounterStarted` push exactly where it is (so trackable/allyJoined/warlockBoost/foeFled/foeBored/encounterCleared keep today's order) but REMOVE its `first` field; keep the five zero-draw flags (samuraiNeverFirst, fridgianSlow, acuteHearing, knightBigFoe, courtMageTalksFirst). Everything from the `nearDeathPanic` line through the pre-emptive `foeTurn` + its cleared check MOVES (comments included) into the new function below; `startCombat` now ends right after the Knight/Con Artist/Court Mage loop's `if (!liveFoes(state).length) { encounterCleared; state.combat = null; return events; }` with a plain `return events;`. The Tracking roll stays at the encounter step (D: Claude's Discretion — only draws from rollInitiative onward move).
    2. Add `export function fight(state, rng, events = [])`: `const C = state.combat; if (!C || !C.pending) return events;` (idempotent, zero draws — a fight on a joined or absent combat is a no-op); `const c = state.c; const type = C.type;`; `const first = rollInitiative(state, rng);`; `delete C.pending;` (delete, never assign false — the joined combat object must have the prototype's exact key set); `events.push({ type: "combatJoined", first });`; then the moved phobia block with its two inner lines REPLACED per D "Phobia = penalty, never a lost action": the `if ((c.phobiaType === type || Darkness-in-dark || nearDeathPanic) && !(skill(c, "Hardiness") && rng.d(2) === 1))` condition stays byte-identical (same conditions, same conditional Hardiness draw, same site), and its body becomes `state.combat.afraid = AFRAID_ROUNDS; events.push({ type: "phobiaAfraid", rounds: AFRAID_ROUNDS });` — no boolean freeze flag is set anywhere any more and the old phobia-freeze event type is never emitted; rewrite the block's comment (keep the Darkness/Death mutual-exclusion and single-Hardiness-roll reasoning, replace every "freeze/root" sentence with the Afraid penalty: "DELIBERATE RULES CHANGE (Phase 31, user ruling 2026-09-16): a phobia is a PENALTY, never a lost action — the prototype froze the hero and spent the first strike shaking it off; the engine now sets combat.afraid (AFRAID_ROUNDS rounds of a shrunk to-hit range and halved damage, see engine/derived.js) with zero new draws; the three fixtures that reach this branch are declared action-path divergences"); then the moved `combatInDark` push, the moved `if (first === "foe") { foeTurn(...); if (state.combat && !liveFoes(state).length) { encounterCleared; endCombat } }`; `return events;`. JSDoc: name the cut line, that this is the CMB-01 split, that the draw ORDER is unchanged (initiative d20 ×2 → conditional Hardiness d2 → foeTurn's own draws), that `c.foresight` is now consumed at Fight! time (RESEARCH Pitfall 3 — a documented zero-fixture timing shift), and that a foes-first opener ticks `afraid` 2 → 1 inside this call (the foeTurn tail runs), exactly as it ticks a pre-cast ward.

    THE AFRAID PENALTY (engine/derived.js + engine/combat.js), per D "Phobia = penalty, never a lost action" (USER RULING 2026-09-16; user correction: the to-hit range is LOW, so the penalty SHRINKS the target number — never subtract from the roll):
    2b. engine/derived.js, directly under DEATH_PANIC_THRESHOLD: `export const AFRAID_ROUNDS = 2;`, `export const AFRAID_TO_HIT_PENALTY = 3;`, `export const AFRAID_DMG_DIV = 2;` with a doc block headed "TUNING KNOBS — Afraid (Phase 31, user ruling 2026-09-16: phobia is a penalty, never a lost action)": rounds the penalty lasts (ticks at the foeTurn tail, clears with the combat), how many points the player's to-hit RANGE shrinks by (hit on d ≤ need, so need − 3, floor 1 — a smaller need is harder), and the damage divisor (Math.ceil, min 1). Then two pure helpers beside toHit: `export function afraidNeed(state, need)` → returns `need` unchanged unless `state.combat && state.combat.afraid > 0 && need > 0`, else `Math.max(1, need - AFRAID_TO_HIT_PENALTY)` (the `need > 0` guard keeps an untouchable magic-only foe at 0 — fear cannot make it hittable); `export function afraidDamage(state, dmg)` → `state.combat && state.combat.afraid > 0 ? Math.max(1, Math.ceil(dmg / AFRAID_DMG_DIV)) : dmg`. Both read-only, zero rng; JSDoc both with the LOW-range sentence so no future reader flips the sign. Import all five names into engine/combat.js next to DEATH_PANIC_THRESHOLD.
    2c. engine/combat.js playerStrike: DELETE the five-line branch that tested the combat's fear flag, cleared it, pushed the old shake-off event, called afterPlayerAction and returned (the "frozen-round skip" — update the function JSDoc's list accordingly: it is now "the Afraid to-hit/damage penalty"). In the attack loop, immediately after the `magicOnly` line and before `const auto = …`: `const needBefore = need; need = afraidNeed(state, need); const afraidMods = need !== needBefore ? [{ name: "afraid", delta: need - needBefore }] : [];` — the LAST modifier, so it composes with the frenzy 3, asleep 5, hard-to-hit, fast and darkness rules and can never revive a need-0 foe. Spread `...(afraidMods.length ? { needMods: afraidMods } : {})` into BOTH the `strikeMissed` push and the `struck` push (additive payload, absent for every unafraid strike — the Phase 25 FEED-01 foeMissed convention). Right after the weakened-halving line (`if (c.foeEffect && … "weakened" …) dmg = Math.ceil(dmg / 2);`) add `dmg = afraidDamage(state, dmg);` and spread `...(afraidMods.length ? { afraid: true } : {})` into the `struck` push so the Oracle can say the blow was pulled. Comment both sites: "Phase 31 Afraid — pure arithmetic on already-rolled values, zero rng; false for every non-phobia fixture".
    2d. engine/combat.js foeTurn tail: AFTER the foeEffect tick block (last statement before `return events;`): `if (C.afraid > 0 && --C.afraid <= 0) events.push({ type: "fearPassed" });` (C is the local `state.combat` reference the tail already uses; never adds the key to a combat that lacks it). endCombat needs NO change — `state.combat = null` clears the counter with the combat and no fearPassed fires at the end of a fight (the fear ends with the fight, not with a line); say so in a one-line comment beside `state.combat = null`.
    2e. engine/derived.js conditionsOf: REPLACE the whole active-phobia block (the `if (state && state.combat && c.phobia) { const phobiaActive = … }` block and its comment) with `if (state && state.combat && state.combat.afraid > 0) out.push({ key: "afraid", polarity: "bad", remaining: state.combat.afraid, phobia: c.phobia });` and update the JSDoc's condition list line (`phobia {…}` → `afraid {polarity:"bad", remaining:<rounds>, phobia:<fear name>} — Phase 31: the Afraid penalty in the CURRENT combat, only while combat.afraid > 0 (a Hardiness shrug-off shows nothing)`). Pure read, zero rng.
    3. Add `export function refuseIfPending(state, events, type, extra = {})`: returns false unless `state.combat && state.combat.pending`; otherwise pushes `{ type, ...extra, reason: "notFought" }` and returns true. Wire it as the FIRST check (before any other refusal or `!C` return) in: `playerStrike` → `"strikeRefused"`; `flee` → `"fleeRefused"`; `parley` → `"parleyRefused"`; `sing` → `"actionRefused", { action: "sing" }`; engine/magic.js `castSpell` (right after the `if (!sp) return events;` line) → `"castRefused", { spell: sp.n }`; `drinkPotion` → `"actionRefused", { action: "drinkPotion" }`; `readScroll` (before the `!c.scrolls` check) → `"scrollRefused"`; engine/items.js `useItem` (right after the `!it` check, BEFORE the itemReady check — Plan 02 replaces that silent itemReady return) → `"useRefused", { item: it }`. magic.js/items.js already import from ./combat.js — extend those import lists.
    4. engine/actions.js: add `"fight"` to ACTION_TYPES (no payload; no validate case needed, like `camp`). engine/engine.js: add `case "fight": fight(next, rng, events); break;` and import `fight` from ./combat.js.
    5. tools/lib/tuning-bot.mjs `decideAction`: as the very first statement inside `if (state.combat) {`, add `if (state.combat.pending) return { type: "fight" };` with a one-line comment (Phase 31 CMB-01: the bot presses Fight! like a player). Nothing else in the bot changes.

    PRESENTATION ENTRIES (src/browser/toasts.js, src/browser/eventNarration.js) — family-friendly deadpan, voice-safe; these are the ONLY copy sites, Phase 32 must not move them (refusals stay toasts, D user clarification):
    6. New TOAST_FOR builders: `combatJoined: (e) => ({ text: e?.first === "you" ? "You move first." : "They move first.", tone: e?.first === "you" ? "hit" : "hurt", priority: PRIORITY.feature })`; `castRefused: (e) => block(map[e?.reason] ?? "The spell refuses you.")` with a reason map covering notFought ("Fight! first. The spell keeps."), combatOnly (names e.spell: "… wants a target. Save it for a fight."), exploreOnly ("… needs quieter surroundings."), noTarget ("Nothing left to aim at.") — there is NO fear reason in any refusal map (D: nothing is refused for fear); `actionRefused: (e) => block(...)` keyed by reason: notFought ("Fight! first."), cooldown (`Your voice needs ${e.left} more squares.`), wrongClass ("Only a Bard sings here."), fallback "Not now."
    7. Extend the existing reason maps: `useRefused` adds cooldown (`${item} needs ${e.left} more squares.`), wrongClass (`${item} is a stick to anyone who is not a Magic User.`), combatOnly (`${item} wants a target. Save it for a fight.`), exploreOnly, noTarget, notFought ("Fight! first. It will keep."), keeping the pilfer text and the generic fallback byte-identical; `strikeRefused` adds notFought ("Fight! first, then swing."); `fleeRefused` adds notFought ("Running comes after Fight!, not instead of it."); `parleyRefused` adds notFought ("They are not listening yet. Fight! first."); `scrollRefused` adds notFought ("Fight! first. The scroll will keep."). Every one of these stays a `block(...)` (tone block, PRIORITY.block).
    7b. The Afraid events (rename, never add beside): REPLACE the toasts.js builder at L927 with `phobiaAfraid: (e) => ({ text: \`Afraid. Your aim wobbles for ${e?.rounds ?? 2} rounds.\`, tone: "hurt", priority: PRIORITY.feature })` (the fallback 2 is copy-layer only — the browser layer does not import engine constants; the engine always sends `rounds`) and the builder at L934 with `fearPassed: () => ({ text: "The fear passes. Your hands remember what they are for.", tone: "hit", priority: PRIORITY.feature })`; rename the two FEATURE_EVENTS entries (L216-217) to `phobiaAfraid`/`fearPassed`; in the encounterStart folder (L769) rename the case to `phobiaAfraid` with the suffix " · afraid" (it will rarely fold now that the event arrives from the `fight` dispatch, but the case must name the live type). eventNarration.js: REPLACE L200 with `phobiaAfraid: (e) => \`<span class="hurt">Your phobia has you shaking.</span> Harder to hit and softer blows for ${e.rounds ?? 2} rounds. You can still swing — you just will not enjoy it.\`` and L210 with `fearPassed: () => \`<span class="hit">The fear passes.</span> Your hands remember what they are for.\``; extend `strikeMissed` (the non-untouchable branch) and `struck` to append `needModsClause(e.needMods, e.need)` right after `vs ${e.need ?? "?"}` (renders " (needs 2: afraid −3)"; absent needMods renders byte-identical to today), and in `struck` append ` <span class="miss">Fear pulls the blow.</span>` when `e.afraid` is true. Family-friendly deadpan; no profanity.
    8. Add `castRefused` and `actionRefused` to FEATURE_EVENTS' Refusals block; add `combatJoined` nowhere in ORACLE_ONLY. Mirror every builder above in EVENT_NARRATION (Oracle sentences in the same voice, `<span class="miss">…</span>` for refusals, `<span class="beat">`/`<span class="hurt">` for combatJoined). The encounterStart folder's combatInDark case stays as it is (the folded events simply no longer share an array with encounterStarted; both have standalone builders — RESEARCH Pitfall 1); do not build a new folder. After the renames, `grep` both browser files: the two old phobia-freeze type names must have zero occurrences (the coverage guards would otherwise carry dead entries).
    9. test/unit/toastTable.test.js: add `"castRefused"` and `"actionRefused"` to REFUSAL_TYPES; in the "reason-specific refusal text differs from the generic fallback" test add a castRefused notFought-vs-fallback pair and a useRefused cooldown-vs-fallback pair.
    10. tools/lib/tuning-bot.mjs and test/parity/harness/*.js carry no reference to the phobia freeze today (verified at plan time — their only "frozen" words are the Freeze spell's `frozenSolid` and "frozen prototype" comments); keep it that way — the bot needs no Afraid logic (it strikes/casts as usual while afraid).
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/actions.test.js 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' engine/combat.js | grep -c 'export function fight('` == 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'export function refuseIfPending('` == 1
    - `grep -v '^\s*//' engine/combat.js | grep -c 'pending: true'` >= 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'delete C.pending'` >= 1
    - `grep -c 'refuseIfPending(state, events, "' engine/combat.js engine/magic.js engine/items.js` totals 8 call sites (strikeRefused, fleeRefused, parleyRefused, actionRefused ×2, castRefused, scrollRefused, useRefused)
    - `grep -c '"fight"' engine/actions.js` >= 1 and `grep -c 'case "fight":' engine/engine.js` == 1 and `grep -c 'return { type: "fight" }' tools/lib/tuning-bot.mjs` == 1
    - `grep -v '^\s*//' engine/derived.js | grep -c 'export const AFRAID_ROUNDS = 2'` == 1, `grep -v '^\s*//' engine/derived.js | grep -c 'export const AFRAID_TO_HIT_PENALTY = 3'` == 1, `grep -v '^\s*//' engine/derived.js | grep -c 'export const AFRAID_DMG_DIV = 2'` == 1, `grep -v '^\s*//' engine/derived.js | grep -c 'export function afraidNeed('` == 1, `grep -v '^\s*//' engine/derived.js | grep -c 'export function afraidDamage('` == 1
    - `grep -v '^\s*//' engine/derived.js | grep -c 'need - AFRAID_TO_HIT_PENALTY'` == 1 (the penalty subtracts from the NEED, never from the roll)
    - `grep -v '^\s*//' engine/derived.js | grep -c 'roll - AFRAID_TO_HIT_PENALTY\|roll + AFRAID_TO_HIT_PENALTY'` == 0
    - `grep -v '^\s*//' engine/combat.js | grep -c 'afraidNeed(state, need)'` == 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'afraidDamage(state, dmg)'` == 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'type: "phobiaAfraid"'` == 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'type: "fearPassed"'` == 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'combat.afraid = AFRAID_ROUNDS'` == 1
    - `grep -v '^\s*//' engine/derived.js | grep -c 'key: "afraid"'` == 1
    - `grep -v '^\s*//' engine/derived.js | grep -c 'key: "phobia"'` == 0
    - `cat engine/*.js src/browser/*.js tools/lib/*.mjs test/parity/harness/*.js | grep -v '^\s*//' | grep -c 'phobiaFrozen\|shookOffFrozen\|combat\.frozen\|C\.frozen'` == 0 (the phobia freeze is gone from every live engine/browser/tool/harness line; the Freeze spell's `frozenSolid` and the foe-side `t.frozen`/`effect: "frozen"` are untouched and not matched by this pattern)
    - `grep -c 'combatJoined' src/browser/toasts.js src/browser/eventNarration.js` >= 1 each; `grep -c 'castRefused' src/browser/toasts.js` >= 2 (builder + FEATURE_EVENTS) and >= 1 in eventNarration.js; same for `actionRefused`; `grep -c 'phobiaAfraid\|fearPassed' src/browser/toasts.js` >= 4 (two builders + two FEATURE_EVENTS entries) and `grep -c 'phobiaAfraid\|fearPassed' src/browser/eventNarration.js` >= 2; `grep -c 'needModsClause(e.needMods, e.need)' src/browser/eventNarration.js` >= 5 (the three foe-side sites plus strikeMissed and struck)
    - node --test test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js → `# fail 0`
  </acceptance_criteria>
  <done>startCombat ends at the roster with pending:true and an encounterStarted that no longer carries first; fight is a validated, dispatched action that performs initiative→phobia trigger→combatInDark→pre-emptive foeTurn in the old order and deletes pending; a triggered phobia sets combat.afraid = 2 (zero new draws) instead of freezing, playerStrike applies the need −3 (floor 1, need-side) and half-damage penalty and never ends the turn for fear, the foeTurn tail counts it down to fearPassed, conditionsOf exposes the afraid chip; all eight player combat actions refuse with notFought while pending; the bot presses Fight!; every new/renamed type has toast + Oracle copy, the old freeze types are gone, and the coverage/voice guards are green.</done>
</task>

<task type="auto">
  <name>Task 2: Parity — chain a real `fight` inside applyStartCombat, add the compare-only reconcilePendingFight to every comparable, declare the three phobia-fixture action-path divergences, and make the A3 fixture walk a standing test</name>
  <files>test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, test/parity/full-suite.test.js, test/parity/fixtures/action-script.combat.json, test/parity/fixtures/action-script.magic.json, test/parity/FIXTURE-INVENTORY.md, test/parity/pending-fight-audit.test.js</files>
  <read_first>
    - test/parity/harness/comparables.js L28-82 (reconcilePendingFind/reconcilePendingLoot — the clone-and-apply pattern), L214-256 (movementComparable), L299-326 (combatComparable), L328-340 (applyStartCombat), L470-570 (stripScenarioDivergence, actionPathDivergenceOf, skipsByteDiffAt, declaredEndDiffs — `before` = the PROTOTYPE's measured end values, `after` = the ENGINE's; `fields` are c-fields, `stateFields` top-level), L677-705 (economyComparable), L710-738 (applyInternal/INTERNAL_FNS/runEconomyAction)
    - test/parity/combat-parity.test.js L100-150 (the local comparable() + local applyStartCombat), L150-245 (the loop — it ALREADY consumes action-path records generically via actionPathDivergenceOf/skipsByteDiffAt/declaredEndDiffs and skips the per-name outcome checks for record-bearing scenarios: no loop change is needed for lose/lose-apprentice, only the records), test/parity/magic-parity.test.js L60-105 (local comparable()/applyStartCombat), L107-213 (the loop still uses the Phase 23 kind-less `stripScenarioDivergence` selection and hard-codes cast-damage post-assertions; the "narrow and well-formed" test at L195-213), test/parity/movement-parity.test.js (its local comparable()), test/parity/full-suite.test.js L140-178 (the combat sub-test's action-path handling — the model to mirror) vs L181-225 (the magic sub-test, still Phase 23-shaped), test/parity/harness/fixtureRoster.js L52-84 (also calls the harness applyStartCombat — no edit needed)
    - test/parity/fixtures/action-script.combat.json: `lose` (seed 14) carries the Phase 24 action-path record (fromAction 1, fields wp/sp/kills/rations, stateFields dead; before wp 0/sp 5/kills 1/rations 7/dead true = the PROTOTYPE; after wp 22/sp 30/kills 2/rations 8/dead false = today's engine) — EXTEND it; `lose-apprentice` (seed 127) has none — ADD one; `parley` (seed 303) shows the Phase 27 shape. action-script.magic.json: `cast-damage` (seed 8) carries the Phase 23 kind-less record (fields sp/gold/kills/rations; before sp 0/gold 50/kills 0/rations 4; after sp 5/gold 51/kills 1/rations 5) — REPLACE it with an action-path record
    - Plan-time probe facts (a replay of every fixture scenario through today's engine, checked for the phobia trigger): EXACTLY THREE scenarios trigger a phobia, all at action 0 against a forced Beasts encounter and all without Hardiness — combat/lose (seed 14: Fighter/Soldier/Fridgian, phobia "Bats and rats" = Beasts; foes went first, the old freeze was shaken off at action 1), combat/lose-apprentice (seed 127: Magic User/Apprentice/Human, phobia "Bats and rats"; hero first, the old freeze ate action 1's strike), magic/cast-damage (seed 8: Magic User/Illusionist/Wilmsry, phobia "Bats and rats"; hero first; the prototype's castSpell had no fear check so its Freeze cast went ahead: d10 roll 1 vs need 6 hit, dmg 3, Shriek frozen). Every chargen seed, every encounters seed, movement (seed 256), economy (seed 3), combat/win (3), combat/flee (17), combat/parley (303), magic/heal (7), magic/potion (1) and magic/scroll (7) trigger NO phobia and stay byte-identical. Today's engine end states (= the prototype's for the two byte-identical ones): lose-apprentice wp 0/sp 0/gold 50/kills 0/rations 4/dead true; cast-damage wp 31/sp 5/gold 51/kills 1/rations 5/dead false/combat null
    - test/parity/fixtures/*.json scripts: combat (startCombat → attack/flee/parley), magic (cast-damage: startCombat → castSpell; heal/potion/scroll: no combat), encounters (springTrap/openChest/encounterDot — none of the three encounterDot seeds starts a combat: tablefour/faerie/affliction), movement (101 moves, BFS avoids dot tiles, step 100's newDay draws no wandering monster at seed 256), economy (store only)
    - test/parity/FIXTURE-INVENTORY.md L30-50 (the generated roster block — foes rolled at the ENCOUNTER step, unchanged by anything in this phase; it must not change) and the Phase 23/24/27 hand-written sections below it (the shape to follow)
    - 29-02-SUMMARY.md "Deviations" (why the three local comparable() copies need the same reconcile as the shared harness)
  </read_first>
  <action>
    1. In comparables.js add `export function reconcilePendingFight(state)`: if `state?.combat?.pending` is truthy, return `applyAction(state, { type: "fight" }).state` (applyAction is already imported; it structuredClones, rebuilds the rng from `state.rngState`, so the clone advances by exactly the draws the prototype's single-call startCombat already consumed); otherwise return `state` unchanged. JSDoc it as the Phase 31 (CMB-01) analog of reconcilePendingLoot: COMPARE-ONLY, never mutates the compared state, proves "the state WOULD match once Fight! is pressed". It must run BEFORE the destructure that strips `rngState` (it needs the cursor): in movementComparable, combatComparable and economyComparable, make the first statement `state = reconcilePendingFight(state);` (rename the parameter or shadow it) ahead of the existing `const { beats, seed, rngState, … } = state;` line.
    2. Change the harness `applyStartCombat` so it mirrors the prototype's single-call startCombat: after `startCombat(next, wandering, forced, rng, events)`, add `fight(next, rng, events);` on the SAME rng before persisting `next.rngState` (import `fight` from engine/combat.js). This is the REAL state advance every scripted `startCombat` fixture action needs (RESEARCH 1.4 mode 1) — it covers full-suite.test.js's two sites and fixtureRoster.js automatically. Apply the identical two-line change to the LOCAL `applyStartCombat` copies in combat-parity.test.js and magic-parity.test.js.
    3. In the three local `comparable()` functions (combat-parity, magic-parity, movement-parity): import `reconcilePendingFight` from ./harness/comparables.js and call it first, exactly as step 1 does for the shared ones. Do NOT touch any fixture `seed`, `actions`, `name`, `_note`, FIXTURE-INVENTORY.md's generated block, or prototype-master.js.txt — the ONLY fixture edits in this phase are the three divergence RECORDS below.

    THE THREE DECLARED DIVERGENCES (D "Phobia = penalty, never a lost action": a deliberate rules change; Phase 24 action-path mechanism, never a fixture regeneration). All three diverge at action 0 because the compared combat object itself differs the moment Fight! resolves — the prototype's boolean freeze flag vs the engine's `afraid: 2` counter — so `fromAction` is 0 for all three; the per-action byte diff is skipped from there and the end state is machine-checked instead:
    4. Measure first, then declare. Run the full suite once with Task 1's engine in place and read the engine's END values from the failing declaredEndDiffs/diff messages (or a throwaway replay via the harness `applyStartCombat` + `applyAction`) — `after` must be the MEASURED engine end state, never a guess; `before` is the PROTOTYPE's end state, which is already known (it is the current record's `before` for `lose`, and today's byte-identical engine end state for `lose-apprentice` and `cast-damage`, listed in read_first).
       a. action-script.combat.json `lose` (seed 14): keep the record's key set exactly as the harness reads it (`kind`, `fromAction`, `fields`, `before`, `after`, `stateFields`, `stateBefore`, `stateAfter`, `rationale`; `phase`/`requirements` are documentation strings — the parity tests only assert `phase` is truthy) and set `"phase": "24+31"`, `"requirements": ["FID-07", "IDENT-08", "CMB-01"]`, `"fromAction": 0`; keep `fields`/`stateFields`/`before`/`stateBefore` byte-identical; re-measure `after`/`stateAfter`; EXTEND `rationale` with a second paragraph: "Phase 31 (user ruling 2026-09-16: phobia is a penalty, not a lost action): this Fridgian Soldier's Beasts phobia triggers at Fight!. The prototype froze him and spent action 1 shaking it off (no strike drawn); the engine sets combat.afraid = 2 instead and action 1 is a real strike at need − 3 and half damage, so the rng draw sequence diverges from action 1 onward — on top of the Phase 24 frenzy/hide divergence. fromAction moves from 1 to 0 because the fight-step combat object already differs (afraid counter vs freeze flag). After re-measured live on <date>."
       b. action-script.combat.json `lose-apprentice` (seed 127): ADD `"divergence": { "phase": "31", "requirements": ["CMB-01"], "kind": "action-path", "fromAction": 0, "fields": ["wp", "sp", "gold", "kills", "rations"], "before": { "wp": 0, "sp": 0, "gold": 50, "kills": 0, "rations": 4 }, "after": { <measured> }, "stateFields": ["dead"], "stateBefore": { "dead": true }, "stateAfter": { "dead": <measured> }, "rationale": "<Phase 31, user ruling 2026-09-16: phobia is a penalty, not a lost action. The seed-127 Human Apprentice fears Beasts; the prototype froze him at Fight! and spent action 1 (the first attack) shaking it off — no strike die drawn. The engine sets combat.afraid = 2 and action 1 is a real strike (need − 3, floor 1, half damage), so every draw from action 1 onward shifts and the outcome is re-measured here. fromAction is 0 because the fight-step combat object differs (afraid counter vs freeze flag). NOTE: this scenario was added in Phase 24 to restore byte-identical death-path parity; after this ruling NO combat fixture reaches a death byte-identically — death-path coverage lives in test/unit/combat.test.js and the tuning bot's matrices (see 31-01-SUMMARY).>" }` — if `after.wp` is 0 and `stateAfter.dead` is true the hero still dies, write that in the rationale; if not, write the actual outcome.
       c. action-script.magic.json `cast-damage` (seed 8): REPLACE the Phase 23 kind-less record with `"divergence": { "phase": "23+31", "requirements": ["FID-06", "CMB-01"], "kind": "action-path", "fromAction": 0, "fields": ["sp", "gold", "kills", "rations"], "before": { "sp": 0, "gold": 50, "kills": 0, "rations": 4 }, "after": { "sp": 5, "gold": 51, "kills": 1, "rations": 5 }, "stateFields": ["dead"], "stateBefore": { "dead": false }, "stateAfter": { "dead": false }, "rationale": "<keep the Phase 23 Freeze-pays-out paragraph verbatim, then: Phase 31 (user ruling 2026-09-16: phobia is a penalty, not a lost action): the seed-8 Illusionist fears Beasts, so at Fight! the prototype set its freeze flag while the engine sets combat.afraid = 2 — the compared combat objects differ from action 0, hence the action-path shape. The prototype's castSpell had no fear check, so both sides cast Freeze on the same draws: d10 roll 1 vs need 6 (prototype) / need 3 (engine, afraid) — still a hit; the halved damage (3 → 2) is irrelevant because Freeze kills through killFoe regardless of hp. No draw position moves, so the declared end fields are exactly the Phase 23 payout fields.>" }`. The expected `after` is unchanged from Phase 23 (verified at plan time: roll 1 ≤ 3 still lands and the kill path is hp-independent) — confirm by measurement; if it differs, the Afraid math is wrong (re-check the need-side direction), do NOT paper over it in the record.
    5. Teach the magic loops the action-path shape (mirror the combat loop): in test/parity/magic-parity.test.js `const pathDiv = actionPathDivergenceOf(scenario);` use the bare comparable when `pathDiv` is set (keep the `stripScenarioDivergence` selection ONLY for a kind-less record, so both shapes remain supported), skip the per-action byte diff with `skipsByteDiffAt(pathDiv, i)`, and after the loop replace the hand-rolled fields loop with `declaredEndDiffs(ctx.S, engineState, pathDiv)` asserted null both ways (keep the hand-rolled loop for a kind-less record). Replace the cast-damage post-assertions with: `phobiaAfraid` IS in allEventTypes, `spellThrown`/`frozenSolid`/`foeKilled` ARE in allEventTypes (FID-06 still exercised — the afraid caster still casts and still pays out), NO `castRefused` and NO `strikeRefused` in allEventTypes (nothing was refused for fear), `engineState.combat === null`. In the "narrow and well-formed" test accept `record.kind === "action-path"` (require `Number.isInteger(record.fromAction)` and non-empty `fields`) alongside the kind-less shape. Apply the same selection/skip/declaredEndDiffs change to full-suite.test.js's magic sub-test (L181-225), mirroring its own combat sub-test.
    6. test/parity/FIXTURE-INVENTORY.md: add a `### Phase 31: phobia is a penalty (Afraid) — three declared action-path divergences` section AFTER the Phase 27 section and OUTSIDE the generated block: one row per scenario (fixture, scenario, seed, hero, fear, fromAction 0, before → after, what the prototype lost vs what the engine does), the user ruling, and the coverage note from 4b.
    7. Create test/parity/pending-fight-audit.test.js — the standing A3 walk (RESEARCH A3; orchestrator resolution 3). Engine-only replay (no sandbox): for every scenario in the combat/magic fixtures, replay with the harness `applyStartCombat` for startCombat entries and `applyAction` for the rest; for the encounters fixture use a local applyInternal over springTrap/openChest/encounterDot (import from engine/encounters.js, mirror comparables.js#applyInternal); for movement and economy use applyAction (economy: bump `state.c.gold = 5000` first like full-suite does; openStore/leaveStore are validated? — no: openStore is internal, so route economy through the same local applyInternal with `openStore` from engine/economy.js for that entry, applyAction for buyItem/leaveStore). Assert, for every fixture/scenario/action index i: (a) after `applyStartCombat` the state never has `combat.pending` (the chain proof); (b) whenever `state.combat?.pending` is truthy after action i, there is NO scripted action i+1 (a pending encounter only ever survives at scenario end, where the compare-only reconcile is sufficient); (c) the movement fixture's 101 actions never produce a non-null `state.combat` (documents that seed 256's step-100 newDay draws no wandering monster) and the three encounterDot seeds never produce a combat. Print nothing on success; failures name fixture/scenario/index.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js test/parity/economy-parity.test.js test/parity/full-suite.test.js test/parity/chargen-parity.test.js test/parity/fixture-inventory.test.js test/parity/pending-fight-audit.test.js 2>&1 | tail -5 && git status --porcelain test/parity/prototype-master.js.txt && git diff --stat test/parity/fixtures</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export function reconcilePendingFight' test/parity/harness/comparables.js` == 1 and `grep -c 'reconcilePendingFight(' test/parity/harness/comparables.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js` totals >= 7 (definition + 3 shared comparables + 3 local copies)
    - `grep -c 'fight(next, rng, events)' test/parity/harness/comparables.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js` == 3
    - `node -e "const j=require('./test/parity/fixtures/action-script.combat.json');for(const n of ['lose','lose-apprentice']){const d=j.scenarios.find(s=>s.name===n).divergence;if(!d||d.kind!=='action-path'||d.fromAction!==0||!d.fields.length||!d.rationale.includes('2026-09-16'))process.exit(1)}"` exits 0
    - `node -e "const j=require('./test/parity/fixtures/action-script.magic.json');const d=j.scenarios.find(s=>s.name==='cast-damage').divergence;if(d.kind!=='action-path'||d.fromAction!==0||d.fields.join()!=='sp,gold,kills,rations'||!d.rationale.includes('2026-09-16'))process.exit(1)"` exits 0
    - `git diff test/parity/fixtures/action-script.combat.json test/parity/fixtures/action-script.magic.json | grep '^[-+]' | grep -v '^[-+][-+]' | grep -c '"seed"\|"actions"\|"type": "\|"idx"\|"forced"\|"wandering"\|"_note"'` == 0 (only divergence records changed — no seed, action, or note line)
    - `git diff --stat test/parity/fixtures` lists ONLY action-script.combat.json and action-script.magic.json; `git status --porcelain test/parity/prototype-master.js.txt` prints nothing
    - `grep -c 'actionPathDivergenceOf' test/parity/magic-parity.test.js` >= 1 and `grep -c 'declaredEndDiffs' test/parity/magic-parity.test.js test/parity/full-suite.test.js` totals >= 3 (magic-parity + full-suite's combat and magic sub-tests)
    - `grep -c 'Phase 31' test/parity/FIXTURE-INVENTORY.md` >= 1
    - `git diff test/parity/FIXTURE-INVENTORY.md | grep -c 'fixture-inventory:generated'` == 0 (the generated block is untouched)
    - test/parity/pending-fight-audit.test.js exists and node --test on it → `# fail 0`
    - node --test over the eight parity files listed in verify → `# fail 0`
  </acceptance_criteria>
  <done>Every scripted startCombat scenario advances through a real fight on the same rng; every non-phobia scenario compares byte-identical to the prototype after every action; the three phobia scenarios carry measured, dated action-path records the loops machine-check; the compare-only reconcile is in all six comparable() functions; the A3 walk is a standing test; no seed/action/master edit.</done>
</task>

<task type="auto">
  <name>Task 3: Pin CMB-01 and the Afraid penalty, re-point the unit tests that assumed the single-call startCombat and the phobia freeze; full suite green</name>
  <files>test/unit/fight-gate.test.js, test/unit/afraid.test.js, test/unit/combat.test.js, test/unit/conditions.test.js, test/unit/combat-scaling.test.js, test/unit/foe-turn-draw-count.test.js, test/unit/tuning-bot.test.js</files>
  <read_first>
    - test/unit/combat.test.js L1-60 (fakeRng/fixedState/fixedCombat helpers), L160-270 (the phobia block — every test calls startCombat with a fakeRng and asserts the old freeze flag/event; these are re-pinned to `afraid`/`phobiaAfraid` AND get `fight` chained), L1405-1432 (the reflect-kill opener test reading `started.first`), and every other `startCombat(` call site (19 total)
    - test/unit/conditions.test.js L16-60 (cleanChar + helpers), L122-175 (the DR17 phobia-chip block: type-match / freeze-flag / Darkness / Death / no-match / outside-combat / purity — re-pinned to the afraid chip)
    - test/unit/combat-scaling.test.js (9 startCombat sites), test/unit/foe-turn-draw-count.test.js L244-300 (runFullFight + the totalDraws table — these totals are the no-new-draw proof), test/unit/tuning-bot.test.js L150-180 (decideAction combat cases)
    - engine/derived.js toHit (LOW range: hit on roll ≤ need) and the AFRAID_* constants + afraidNeed/afraidDamage from Task 1; engine/combat.js playerStrike's need chain and the struck/strikeMissed payloads
    - Other files with startCombat sites that MAY need a chained fight (only where an assertion depends on first/afraid/pre-emptive outcomes): test/unit/identity-combat.test.js, identity-contract.test.js, party-combat.test.js, parley.test.js, foe-abilities.test.js, joiner-acquisition.test.js, encounters.test.js, movement.test.js, engineAdapter.test.js, feedback-payload.test.js, test/determinism/foe-abilities.test.js, test/roundtrip/serialize-rehydrate.test.js
  </read_first>
  <action>
    1. Create test/unit/fight-gate.test.js (import startCombat, fight, refuseIfPending, playerStrike, flee, parley, sing from engine/combat.js; castSpell, drinkPotion, readScroll from engine/magic.js; useItem from engine/items.js; applyAction/newRun from engine/engine.js; makeRng; copy the fakeRng/fixedState/fixedFloor helpers from combat.test.js). Pins:
       a. Encounter step draws nothing past the roster: `startCombat(state, true, "Beasts", fakeRng([1]), events)` (wandering → one d4 lvl roll, pick consumes nothing) leaves `state.combat.pending === true`, `state.combat.first === undefined`, `c.foresight` still true when it started true, events contain `encounterStarted` WITHOUT a `first` key and contain none of combatJoined/phobiaAfraid/combatInDark/foeMissed/struckByFoe.
       b. `fight` with fakeRng([15, 5]) on that state: exactly two draws (fakeRng throws on a third), `combatJoined {first:"you"}` pushed, `pending` key ABSENT (`"pending" in state.combat === false`), `c.foresight === false` afterwards.
       c. Phobia order: a Darkness-phobic character in the dark WITH Hardiness — startCombat with fakeRng([1]) then fight with fakeRng([15, 5, 2]) → `state.combat.afraid === 2`, `phobiaAfraid {rounds: 2}` after combatJoined; the same without Hardiness needs exactly [15, 5] (no d2) — mirrors combat.test.js's phobia pins, now split across the two calls and asserting the counter, not a flag.
       d. Foes-first pre-emptive strike happens only inside fight: startCombat with a seeded makeRng on a fixture-representative seed, assert no foe-strike event before fight and at least one foeMissed/struckByFoe (or a kill) after when combatJoined.first === "foe" (seed-scan up to 200 seeds for a first:"foe" case, like the reflect-kill test does).
       e. Idempotence: `fight` on `state.combat === null` and on a joined combat (no pending) with fakeRng([]) returns the events untouched (zero draws).
       f. notFought gate: with a pending combat, each of attack/flee/parley/sing/castSpell/drinkPotion/readScroll/useItem dispatched via applyAction returns exactly one event `{type: <its refusal type>, reason:"notFought"}` and the returned state equals the input state byte-for-byte except rngState (unchanged too: assert `diffState`/deepEqual on the whole state); `refuseIfPending` returns false and pushes nothing when combat is null or joined.
       g. Draw-count split: for seeds 3, 14, 17, 303 (forced Beasts/Beasts/Beasts/Humans as the combat fixture does), wrap makeRng(seed) in a counting rng; assert `startCombat` alone draws strictly fewer than `startCombat` + `fight`, that `fight` adds at least 2, and that `state.combat.first` is set only after `fight`. The byte-identical TOTAL is proven by step 3 below (foe-turn-draw-count's table), not re-derived here.
       h. tuning bot: `decideAction({...combatState, combat:{...pending:true}}, rng, ctx)` returns `{type:"fight"}` (add to test/unit/tuning-bot.test.js instead if the helpers live there).
    1b. Create test/unit/afraid.test.js — the strike-side Afraid pins (D "Phobia = penalty, never a lost action"; Plan 02 appends the spell/item side to this same file). Helpers: copy fakeRng/fixedState/fixedCombat/fixedFoe from combat.test.js; import AFRAID_ROUNDS, AFRAID_TO_HIT_PENALTY, AFRAID_DMG_DIV, afraidNeed, afraidDamage, toHit, conditionsOf from engine/derived.js; playerStrike, foeTurn, endCombat, fight, startCombat from engine/combat.js; applyAction from engine/engine.js. Pins (one `test(` each):
       i.   DIRECTION: for a fixedState hero (say a Fighter, toHit 5) `afraidNeed({...state, combat:{afraid:2}}, toHit(state))` is STRICTLY LESS than `toHit(state)` and equals `Math.max(1, toHit(state) - AFRAID_TO_HIT_PENALTY)`; with `afraid: 0` or `combat: null` it returns `need` unchanged; `afraidNeed(afraidState, 0) === 0` (an untouchable foe stays untouchable); `afraidNeed(afraidState, 2) === 1` (floor 1, never 0 or negative).
       ii.  A ROLL THAT HITS UNAFRAID MISSES AFRAID: the same fixedState/fixedCombat, target foe with no asleep/fast/toHit/magicOnly specials; roll a fakeRng that yields a strike die equal to the unafraid need (e.g. need 5, roll 5) — unafraid: `struck` with need 5; then the same hero/foe/rng with `combat.afraid = 2` → `strikeMissed` with `need === 2`, `roll === 5`, `needMods` deepEqual `[{ name: "afraid", delta: -3 }]`, and the foe's wp untouched. Zero rng difference: both runs consume the same draws up to the miss (the afraid run draws no damage die — assert via a counting rng that the afraid run draws exactly one fewer die, i.e. the weapon-damage draw the hit would have made).
       iii. DAMAGE HALVED, MIN 1: a hit while afraid with weaponDamage forced to 3 (fakeRng) → `struck.dmg === 2` (Math.ceil(3/2)) and `struck.afraid === true`; a hit with weapon damage 1 → `dmg === 1` (never 0); a Guard/Soldier (noCrit) rolling a 1 still deals `Math.ceil(weapon/2)`; a crit (roll 1, non-noCrit) doubles first then halves (`Math.ceil(2·w / 2) === w`) — pin the order crit → weakened → afraid by composing weakened + afraid (`Math.ceil(Math.ceil(w/2)/2)`).
       iv.  NEVER A LOST ACTION / NEVER REFUSED: with `combat.afraid = 2`, `applyAction(state, {type:"attack"})` emits a `struck` or `strikeMissed` (a real swing — never the old shake-off, never a strikeRefused) and afterPlayerAction ran (a foe event or a round advance is present); `castSpell` of a known, charged spell while afraid emits its normal outcome and NO `castRefused`; `drinkPotion`, `readScroll` (with scrolls), `useItem` (a potion), `flee`, `parley` and `sing` (a Bard) while afraid each emit their normal outcome or their ordinary non-fear refusal and NEVER a refusal whose reason mentions fear (assert every `*Refused` event's `reason` is in the fixed vocabulary {notFought, cooldown, wrongClass, combatOnly, exploreOnly, noTarget, pilfer, wizard, samurai, ninja, masterOfArms, exhausted, noScrolls, noRunes} and none is "frozen" or "afraid").
       v.   COUNTDOWN + fearPassed: `combat.afraid = 2`, one `foeTurn` (one foe, fakeRng sized so it misses) → `afraid === 1`, no `fearPassed`; a second foeTurn → `afraid === 0` and exactly one `fearPassed`; a third → stays 0, no event; a combat WITHOUT the key never gains it (`"afraid" in state.combat === false` after foeTurn); the tail order is ward → mirror → foeEffect → afraid (assert event order `wardFaded`, `mirrorFaded`, `foeEffectFaded`, `fearPassed` when all expire on the same tail; Plan 02's acute tick slots in after mirror).
       vi.  FOES-FIRST OPENER TICKS ONCE INSIDE fight: a phobic hero without Hardiness, startCombat + fight with a fakeRng that makes `first === "foe"` and the foe miss → after `fight`, `afraid === 1`, `phobiaAfraid {rounds: 2}` precedes the foe's swing events, no `fearPassed` yet.
       vii. endCombat: `combat.afraid = 2` then `endCombat(state, events)` → `state.combat === null`, no `fearPassed` in events, and `conditionsOf(state)` has no afraid entry.
       viii. CHIP: `conditionsOf({ c: cleanChar-like hero with phobia "Bats and rats", combat: { type: "Beasts", afraid: 2 } })` contains exactly one `{ key: "afraid", polarity: "bad", remaining: 2, phobia: "Bats and rats" }`; `afraid: 0` or a matching phobiaType WITHOUT the counter (the Hardiness shrug-off case) → no afraid entry and no `phobia` entry; outside combat → none; the read is pure (deepEqual the state before/after).
       ix.  ZERO NEW DRAWS: a counting rng around startCombat + fight for a phobic hero without Hardiness draws exactly the initiative pair (+ foeTurn's own draws when foes go first) — the same count as the pre-ruling freeze path; with Hardiness exactly one more (the d2). Both mirror the old totals, i.e. the ruling added no draw.
    2. test/unit/combat.test.js: in every test whose assertion depends on initiative/afraid/pre-emptive outcomes (the whole phobia block L176-270, the reflect-kill opener test at L1415, and any other of the 19 sites that reads state.combat.first/afraid or foe hp after an opener), chain `fight(state, rng, events)` with the SAME rng object and events array immediately after `startCombat(...)`. The fakeRng sequences do not change (the draws only split across two calls). In the phobia block, swap every freeze assertion for the counter: positive cases assert `state.combat.afraid === 2` and a `phobiaAfraid` event with `rounds: 2`; negative cases assert `!state.combat.afraid` and no `phobiaAfraid` (rename the test titles from "freezes"/"freeze" to "is afraid"/"the Afraid penalty"); the "exactly N fakeRng entries" draw-count comments stay true and stay asserted. The reflect-kill test reads `combatJoined.first` instead of `started.first`. Tests that only assert roster/count/allies/warlock/knight/conArtist/courtMage behaviour need no change.
    2b. test/unit/conditions.test.js L122-175: re-pin the DR17 phobia-chip block to the afraid chip — `combat: { type: "Humans", afraid: 2 }` with a Crowds/Humans hero → exactly `[{ key: "afraid", polarity: "bad", remaining: 2, phobia: "Crowds" }]`; a type-matched phobia WITHOUT the counter → `[]` (the chip follows the penalty, not the fear); Darkness-in-the-dark without the counter → only the `darkness` chip (no afraid); Death near death without the counter → `[]`; outside combat → `[]`; purity: `conditionsOf` on `{ c, combat: { type: "Humans", afraid: 1 } }` leaves the state deepEqual to its clone. Delete the old freeze-flag test; keep the block's header comment but rewrite it for the Afraid chip.
    3. test/unit/foe-turn-draw-count.test.js `runFullFight`: insert `fight(state, rng, events);` after `startCombat(...)` (import fight). Plan-time probe: the table's six seeds split into THREE non-phobic rows — seed 3 (12 draws), 17 (101), 303 (66) — whose `totalDraws`/`attacks`/`outcome` MUST remain byte-identical (do not re-measure; a change there means a draw moved out of order or a new draw crept in — the CMB-01 rng prohibition, enforced here) — and THREE rows whose hero fears Beasts and fights Beasts — seed 14 (87/8/won), 8 (32/4/won), 127 (119/14/died) — whose FULL-FIGHT totals legitimately change under the Afraid ruling (the first strike is now a real swing, so draws shift from action 1 onward). For those three: re-measure `totalDraws`/`attacks`/`outcome` ONCE with the finished Task 1 engine and annotate each row with a comment naming the cause ("Phase 31 (user ruling 2026-09-16): phobia is a penalty — the old shake-off no longer eats the first strike; was 87/8/won" etc.). To keep the SPLIT's own no-new-draw proof independent of the ruling, add a second table/test in the same file: for each of the six seeds, count draws of the OPENER ONLY — `startCombat` + `fight` on one counting rng — and pin the number today's single-call `startCombat` consumed (measure on the pre-change tree with `git stash` before editing, or read the plan-time reference: counting `d()` + `pick()` calls, seed 3 → 6, 14 → 11, 17 → 10, 303 → 10, 8 → 5, 127 → 8; use the file's own counting rng for the committed numbers). This opener pin covers the phobic seeds too, because the trigger's draw (none without Hardiness) and the pre-emptive foeTurn are unchanged by the ruling.
    4. test/unit/combat-scaling.test.js and the other files listed in read_first: run `npm test`, and for each failure caused by a missing initiative/opener, chain `fight` the same way; never weaken an assertion. Record in the SUMMARY which files needed the chain and why.
    5. Full gate: `npm test` → `# fail 0` (count grows from 1593); `git status --porcelain test/parity/prototype-master.js.txt` empty; `git diff --stat test/parity/fixtures` shows only the two record-bearing JSON files from Task 2.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/fight-gate.test.js test/unit/afraid.test.js test/unit/combat.test.js test/unit/conditions.test.js test/unit/combat-scaling.test.js test/unit/foe-turn-draw-count.test.js test/unit/tuning-bot.test.js 2>&1 | tail -5 && npm test 2>&1 | grep -E '^# (pass|fail)' && git status --porcelain test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/fight-gate.test.js exists with >= 8 `test(` blocks covering (a)-(h) above; `node --test test/unit/fight-gate.test.js` → `# fail 0`
    - test/unit/afraid.test.js exists with >= 9 `test(` blocks covering (i)-(ix) above; `grep -c 'afraidNeed' test/unit/afraid.test.js` >= 3; `grep -c 'fearPassed' test/unit/afraid.test.js` >= 3; `grep -c 'needMods' test/unit/afraid.test.js` >= 1; `node --test test/unit/afraid.test.js` → `# fail 0`
    - `grep -c 'fight(state, rng, events)' test/unit/foe-turn-draw-count.test.js` >= 1; the seed 3/17/303 rows (12/101/66) are byte-identical (`git diff test/unit/foe-turn-draw-count.test.js` shows no change on those three rows); the seed 14/8/127 rows carry a "Phase 31" comment naming the ruling; the file has a second opener-only draw table with six rows and `grep -c 'opener' test/unit/foe-turn-draw-count.test.js` >= 2
    - `grep -c 'combatJoined' test/unit/combat.test.js` >= 1 (the reflect-kill test re-pointed); `grep -c 'phobiaAfraid' test/unit/combat.test.js` >= 4
    - `cat test/unit/*.test.js test/parity/*.test.js test/parity/harness/*.js | grep -v '^\s*//' | grep -c 'phobiaFrozen\|shookOffFrozen\|combat\.frozen\|frozen: true'` == 0
    - `grep -c 'key: "afraid"' test/unit/conditions.test.js` >= 2
    - `grep -c 'key: "phobia"' test/unit/conditions.test.js` == 0
    - `npm test` → `# fail 0` and `# pass` >= 1600
    - `git status --porcelain test/parity/prototype-master.js.txt` prints nothing
  </acceptance_criteria>
  <done>CMB-01 is pinned at engine level (encounter draws nothing past the roster; fight draws in the old order; pending gates every action; the bot fights); the Afraid ruling is pinned (need shrinks by 3 on the need side, floor 1; a hit-unafraid roll misses afraid; damage halves to a floor of 1; 2-round countdown with one fearPassed; endCombat clears; chip shape; no action is ever refused or lost for fear; zero new draws); every pre-split/pre-ruling unit test is re-pointed without weakening; the draw-count totals prove no new rng; the full suite is green with the master untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell/bot → applyAction | untrusted `{type:"fight"}` (no payload) and every combat action while pending |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-01 | Tampering | engine/actions.js validateAction | low | mitigate | `fight` added to ACTION_TYPES with no payload; unknown types still no-op |
| T-31-02 | Denial of Service | fight idempotence | low | mitigate | `fight` on null/joined combat is a zero-draw no-op (pinned) — a replayed Fight! can never reroll initiative |
| T-31-03 | Information Disclosure | pending combat in saves | low | accept | rehydrate already nulls `combat`; nothing new is serialized |
</threat_model>

<verification>
- `npm test` → `# fail 0`; `git status --porcelain test/parity/prototype-master.js.txt` empty; `git diff --stat test/parity/fixtures` shows only the two record-bearing JSON files (divergence records only).
- Draw-count pins in test/unit/foe-turn-draw-count.test.js: the opener-only table (six seeds) and the three non-phobic full-fight rows are byte-identical to the pre-split numbers; the three phobic rows are re-measured with a dated Phase 31 comment.
- Coverage guards (formatEventsCoverage, toastsCoverage) and the voice scan green with combatJoined/castRefused/actionRefused/phobiaAfraid/fearPassed added and the two old phobia-freeze types removed.
- `cat engine/*.js src/browser/*.js tools/lib/*.mjs test/parity/harness/*.js test/unit/*.test.js test/parity/*.test.js | grep -v '^\s*//' | grep -c 'phobiaFrozen\|shookOffFrozen\|combat\.frozen\|C\.frozen'` == 0.
</verification>

<success_criteria>
- ROADMAP success criterion 1 at the engine level: an encounter leaves `state.combat.pending` with no initiative and no enemy strike until `fight` is dispatched (shell wiring lands in 31-03).
- Phobia is a penalty, never a lost action: a triggered phobia costs −3 of to-hit range and half damage for 2 rounds, and every action stays available (user ruling 2026-09-16).
- Parity byte-identical for every non-phobia fixture; the three phobia fixtures are declared, measured action-path divergences; zero seed/action edits, master untouched; A3 is a standing test.
</success_criteria>

<human_verification>
Deferred to the end of the autonomous run (copy into SUMMARY.md under "## Human verification (deferred to end of run)"; needs Plan 03's shell):
- Pixel 7: walk into an encounter — the Oracle shows only the encounter line (foe names) and NO "they move first"/strike lines until Fight! is tapped; after the tap the initiative line and any pre-emptive strike appear together.
- Pixel 7, an afraid fight (a hero whose phobia matches the foe type — e.g. "Bats and rats" vs Beasts): after Fight! the Oracle reads "Your phobia has you shaking. Harder to hit and softer blows for 2 rounds…", the tracker shows the "Afraid · 2 rds" chip, and the very first Strike tap SWINGS (a roll-vs-need line such as "4 vs 2 (needs 2: afraid −3). You miss…" or "… You hit … for N hp. Fear pulls the blow.") — never a lost turn, never a "frozen"/"shake it off" line; Spells/Items/Flee/Parley all respond normally while the chip is up; the chip counts 2 → 1 → gone and the Oracle prints "The fear passes." — your to-hit range shrinks by 3 while afraid, then recovers.
</human_verification>

<output>
Create `.planning/phases/31-combat-start-gating-effect-hygiene/31-01-SUMMARY.md` when done (list which unit-test files needed the chained `fight`; confirm the opener-only draw totals and the three non-phobic full-fight totals were untouched; record the measured `after` values of the three declared phobia records and note that no combat fixture reaches a death byte-identically any more).
</output>
