---
phase: 47-shell-modularisation
plan: 05
type: execute
wave: 5
depends_on: ["47-04"]
files_modified:
  - src/browser/storeScreen.js
  - src/browser/bridge.js
  - mazeworld.html
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/storeScreen.test.js
  - test/unit/gearTab.test.js
  - test/unit/heroTab.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/shell-map-store-polish.test.js
  - test/unit/shell-armor-display.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-clarity-43.test.js
  - test/unit/shell-combat-over.test.js
  - test/unit/shell-no-content-copies.test.js
  - .planning/STATE.md
autonomous: true
requirements: [SHELL-03, SHELL-04, SHELL-01, SHELL-02]

must_haves:
  truths:
    - "SHELL-03 (idempotency): rendering the store twice from the same state yields byte-equal DOM with no duplicate rows — the Plan 01 snapshot test's idempotency case stays green against the module-rendered store"
    - "SHELL-03: the Store fixtures (thief-store.store, mu-store.store) AND every other fixture compare byte-equal after the carve; no fixture file changed since the Plan 01 capture commit"
    - "renderEncounter()'s S.store branch is exactly one `window.__mzTabs.store(body, S, tabDeps())` call; the classic script declares no STORE_ROLL_COPY and builds no shelf/sell-list/a-leave rows"
    - "SHELL-04 (adjacency): `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen })` — the CONTEXT's exact final shape — appears once in the module script before the first paint; `__mzTabs` is one registry name; no `__mzGearTab`/`__mzHeroTab`/`__mzStoreScreen` name exists"
    - "SHELL-04 (empty/ordering): the registry test (set-equality, sorted keys, fail-first teeth) is green at the phase's final commit with the final registry count recorded"
    - "ROADMAP criterion 3: shell-no-content-copies.test.js is widened to every src/browser/ export name (derived from the modules' export statements at test time) and names every tab module; the criterion's grep over the nine content tables prints 0"
    - "ROADMAP criterion 2: `wc -l < mazeworld.html` is measured at the final commit; if it is still >= 5,000 after the CONTEXT's fallback (move the tabs' remaining private helpers), the shortfall is recorded in the SUMMARY, docs/SHELL-MODULES.md and a STATE.md blocker with the classified remaining bulk — never a silent pass"
    - "ROADMAP criteria 1, 4, 5 verified verbatim at the final commit with command + output in the SUMMARY"
  artifacts:
    - "src/browser/storeScreen.js — renderStoreScreen(host, state, deps), STORE_ROLL_COPY"
    - "test/unit/storeScreen.test.js — module + mount pins"
    - "test/unit/shell-no-content-copies.test.js — widened to every src/browser export"
    - "docs/SHELL-MODULES.md — ## Line budget filled with the final count and per-plan deltas"
  key_links:
    - "renderEncounter() -> window.__mzTabs.store(body, S, tabDeps()) -> storeScreen.js#renderStoreScreen -> gearTab.js#renderCarriedList (sell rows) + gearTab.js#bagUsage + viewModels.js#armorDisplay/usableBy"
    - "storeScreen.js -> deps.buyItem / deps.leaveStore (the classic tabDeps() closures over window.mzBuyItem / window.mzLeaveStore)"
    - "test/unit/shell-no-content-copies.test.js -> every src/browser/*.js export statement (the widened criterion-3 pin)"
  prohibitions:
    - statement: "MUST NOT change any rendered DOM or text of the Store — the Plan 01 fixtures are the lock; MZ_SNAPSHOT_UPDATE is never set in this plan"
      requirement_id: SHELL-03
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT read window/document globals inside storeScreen.js — host, host.ownerDocument and deps only"
      requirement_id: SHELL-03
      category: values
      status: unverified
      verification: test
    - statement: "MUST NOT report criterion 2 as met when `wc -l` is >= 5,000, and MUST NOT purge comments outside the moved lines to reach it (Phase 48's job) — record the measured shortfall instead"
      requirement_id: SHELL-04
      category: transparency
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete a source pin — re-home every failing assertion to storeScreen.js or the new mount call"
      requirement_id: SHELL-03
      category: values
      status: unverified
      verification: judgment
---

<objective>
Carve the Store screen out of `renderEncounter()` into `src/browser/storeScreen.js` (`renderStoreScreen(host, state, deps)` owning the whole `S.store` branch — header, stock shelf with repair-row math and usable-by suffixes, the "Your gear" sell list through gearTab.js's `renderCarriedList`, the Leave button, `STORE_ROLL_COPY`), give `window.__mzTabs` its final locked shape, then close the phase: widen the no-duplicate pin to every `src/browser/` export, measure the line budget with the CONTEXT's fallback, and verify ROADMAP criteria 1-5 verbatim.

Purpose: SHELL-03 (the Store renders from a named module with its own source-pin suite) and the closing half of SHELL-04 (the registry at its final state, the widened criterion-3 pin, the line count recorded honestly).

Output: `src/browser/storeScreen.js`, `test/unit/storeScreen.test.js`, the shell's store mount, the final `__mzTabs`, widened `shell-no-content-copies.test.js`, the filled `## Line budget`, the phase closing SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-shell-modularisation/47-CONTEXT.md
@.planning/phases/47-shell-modularisation/47-03-SUMMARY.md
@.planning/phases/47-shell-modularisation/47-04-SUMMARY.md
@docs/SHELL-MODULES.md
@src/browser/gearTab.js
@src/browser/heroTab.js
@src/browser/bridge.js
@test/unit/harness/shellSandbox.js
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->
<!-- planner-discipline-allow: STORE_ROLL_COPY -->
<!-- planner-discipline-allow: __mzTabs.store( -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt` (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **No behaviour change:** the committed BEFORE fixtures are the lock; `MZ_SNAPSHOT_UPDATE` is never set.
- **Greenfield ruling:** no shims; one mount call per surface; old helpers deleted in the same commit; `shell-sweep refs` = 0.
- **Line-budget honesty (planner projection, measured at 0129ca3):** the three bodies + attached comments + the classic wrappers total about 820 lines out and about 45 lines in (mount calls, tabDeps, imports) — a landing near 5,550 lines, ABOVE criterion 2's 5,000. The CONTEXT's own fallback (move the tabs' remaining private helpers) is the only in-scope lever; comment purges outside moved lines are Phase 48 (DOCS-01..03; the two scripts hold ~1,840 comment-only lines and the markup ~630 HTML-comment lines). Task 2 measures, applies the fallback, and records the result either way. Do NOT widen scope to reach the number; do NOT report the criterion met if it is not.
- **Windows executor environment:** Git Bash, no `jq`; multi-line scripts in files. Anchor by names, not line numbers.
- **Verification agents are off:** the acceptance criteria ARE the verification.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: storeScreen.js owns the S.store branch; renderEncounter() mounts through window.__mzTabs.store; __mzTabs takes its final shape; pins re-homed; storeScreen.test.js</name>
  <files>src/browser/storeScreen.js, src/browser/bridge.js, mazeworld.html, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/storeScreen.test.js, test/unit/gearTab.test.js, test/unit/heroTab.test.js, test/unit/hp-not-wp.test.js, test/unit/shell-map-store-polish.test.js, test/unit/shell-armor-display.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-clarity-43.test.js, test/unit/shell-combat-over.test.js</files>
  <read_first>
    - mazeworld.html `renderEncounter()`: the `if (S.store) {` branch through its `return;` — `const st = S.store;`, `const usage = window.__mzBagUsage(S.c);`, the `body.innerHTML += ...` template (header, purse line with `st.markup > 1`, the `S.storeRoll === true ? STORE_ROLL_COPY` line, `#shelf`, the `usage.full` Bag-full line, `#sell-head`, `#sell-list`, `#a-leave`), `const ad = window.__mzArmorDisplay.armorDisplay(S.c);`, the `st.stock.forEach` row builder (repair-row `sub`, `window.__mzUsableBy(item.effectParams.item, S.c)`, `window.mzBuyItem?.(i)`), the sell section (`window.__mzCarriedList(document.getElementById("sell-list"), S, S.c.items, {...}, tabDeps())` from Plan 03, `sellHead.style.display = "none"`), `document.getElementById("a-leave").onclick = () => window.mzLeaveStore?.();`
    - mazeworld.html: `const STORE_ROLL_COPY = "...";` with its comment block (grep `^const STORE_ROLL_COPY`)
    - mazeworld.html module script: `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab });`, the gearTab.js/heroTab.js import lines, `window.__mzUsableBy = usableBy;`, `window.__mzBagUsage = bagUsage;`, `window.__mzArmorDisplay = ...` (all three STAY — the rail's find card and the loot card / drop shelf still read them; verify with ident-sweep after the carve)
    - src/browser/gearTab.js `renderCarriedList(container, state, items, opts, deps)` and `bagUsage`; src/browser/viewModels.js `armorDisplay`, `usableBy`
    - test/unit/shell-map-store-polish.test.js (the `const STORE_ROLL_COPY = "..."` extraction + safety-wordlist scan), shell-armor-display.test.js (6 store pins: the repair-row `ad.wornSub` line), shell-loot-screen.test.js (6 store pins: `usage.full`/`usage.have`/`usage.slots` in the store branch), shell-clarity-43.test.js (4: the `__mzUsableBy` store read + the potions-ride-free note), shell-combat-over.test.js (`/Bag full \(/`), gearTab.test.js pin (7) `__mzCarriedList(` count and pin (8) the `__mzTabs` literal, heroTab.test.js pin (6)
  </read_first>
  <action>
    1. Create `src/browser/storeScreen.js` (head comment in the gearTab.js style: "the STORE screen — the whole S.store branch of the encounter overlay: header, stock shelf (repair-row math, usable-by suffixes), the Your gear sell list, Leave. Contract: renderStoreScreen(host, state, deps); host is #enc-body, already cleared by the shell's renderEncounter() frame."). Move `STORE_ROLL_COPY` VERBATIM as an export with its comment. `export function renderStoreScreen(host, state, deps = {})`: `const doc = host.ownerDocument; const st = state.store; const c = state.c;` then the branch body verbatim with only these substitutions: `host.innerHTML += <the same template>` (with `c.gold`, `state.storeRoll`), `bagUsage(c)` (import from ./gearTab.js), `armorDisplay(c)` and `usableBy(item.effectParams.item, c)` (import from ./viewModels.js), `doc.getElementById("shelf"|"sell-head"|"sell-list"|"a-leave")`, `deps.buyItem?.(i)`, `renderCarriedList(doc.getElementById("sell-list"), state, c.items, { actions: usage.full ? ["sell", "drop"] : ["sell"] }, deps)` (import from ./gearTab.js), `deps.leaveStore?.()`. Byte-identical template text, class names and row order.
    2. mazeworld.html: the store branch becomes `if (S.store) { window.__mzTabs.store(body, S, tabDeps()); return; }` (keep the branch's position and the `return;`). Delete `const STORE_ROLL_COPY` and its comment block. Module script: `import { renderStoreScreen } from "./src/browser/storeScreen.js";` and the final `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen });`. Verify with `node tools/ident-sweep.mjs "__mzUsableBy\b"`, `"__mzBagUsage\b"`, `"__mzArmorDisplay\b"` that each still has a classic reader (rail find card / loot card / drop shelf) — they stay; if any turns out to have only its assignment left, delete it and its registry entry (record which).
    3. `src/browser/bridge.js`: `__mzTabs` purpose/consumers final ("gear + hero + store; read by paint() and renderEncounter()"); `__mzCarriedList` consumers now the loot card only. `node tools/bridge-doc.mjs --write`. Harness `wireBridges`: `__mzTabs` = the full frozen triple from the real modules.
    4. Pins (same commit, re-home never delete): shell-map-store-polish (the `STORE_ROLL_COPY` extraction reads storeScreen.js; the `storeRoll` gating pin reads storeScreen.js's template), shell-armor-display (6 -> `STORE_SRC`), shell-loot-screen (6), shell-clarity-43 (4), shell-combat-over (`Bag full (` -> storeScreen.js; its `if (S.store) {` pin still matches the shell), gearTab.test.js (7) tightens to `window.__mzCarriedList(` count 1 and (8) to the final `__mzTabs` literal, heroTab.test.js (6) likewise; append `src/browser/storeScreen.js` to hp-not-wp.test.js's module scan. Record the ledger.
    5. `test/unit/storeScreen.test.js`: (1) exports `renderStoreScreen`, `STORE_ROLL_COPY`; (2) comment-stripped storeScreen.js has no `window.` / `document.` token and uses `ownerDocument`; (3) every `getElementById("<id>")` id in storeScreen.js also appears as `id="<id>"` inside the module's own template string (`shelf`, `sell-head`, `sell-list`, `a-leave`); (4) the shell's `renderEncounter()` region contains `window.__mzTabs.store(body, S, tabDeps());` exactly once, inside an `if (S.store) {` line's block, and no `"shelf"`, `"sell-list"`, `"a-leave"` literal and no `STORE_ROLL_COPY`; (5) the module script assigns `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen })` before `await boot(`; (6) storeScreen.js imports `renderCarriedList` and `bagUsage` from `./gearTab.js` and `armorDisplay`/`usableBy` from `./viewModels.js`; (7) voice: `STORE_ROLL_COPY` and the template's player-facing strings ("A store", "Your gear", "Leave", "Bag full", "Potions and scrolls still ride free.") clear content/safety-wordlist.js; (8) SHELL-03 idempotency is covered by test/unit/shell-tab-snapshots.test.js's store case — assert that file contains its idempotency test title (a cross-pin so the truth cannot be silently dropped).
    6. Gates: `npm run build:www`; `npm test` fail 0; snapshot test fail 0 with NO fixture change; `npm run boot:check` 4 PASS; sweeps; fence. Commit: `refactor(47-05): src/browser/storeScreen.js owns the Store — renderEncounter() mounts it through window.__mzTabs.store; __mzTabs final shape`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-tab-snapshots.test.js test/unit/storeScreen.test.js test/unit/gearTab.test.js test/unit/heroTab.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)" && grep -v '^\s*//' mazeworld.html | grep -c "window.__mzTabs.store(body, S, tabDeps());" && grep -c "window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen });" mazeworld.html && node tools/shell-sweep.mjs refs STORE_ROLL_COPY; echo "sweep-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export (function renderStoreScreen\(|const STORE_ROLL_COPY)" src/browser/storeScreen.js` prints 2
    - `node tools/ident-sweep.mjs "\bwindow\." | grep -c "src/browser/storeScreen.js"` prints 0; `node tools/ident-sweep.mjs "\bdocument\." | grep -c "src/browser/storeScreen.js"` prints 0; `grep -c "ownerDocument" src/browser/storeScreen.js` prints a number >= 1
    - `grep -v '^\s*//' mazeworld.html | grep -c "window.__mzTabs.store(body, S, tabDeps());"` prints 1; `grep -cE "^const STORE_ROLL_COPY" mazeworld.html` prints 0; `node tools/shell-sweep.mjs refs STORE_ROLL_COPY` prints `refs STORE_ROLL_COPY: 0` and exits 0
    - `awk '/^function renderEncounter\(\)/,/^}/' mazeworld.html | grep -cE '"(shelf|sell-list|sell-head|a-leave)"'` prints 0; `awk '/^function renderEncounter\(\)/,/^}/' mazeworld.html | grep -c "if (S.store) {"` prints 1
    - `grep -c "window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen });" mazeworld.html` prints 1
    - `grep -v '^\s*//' mazeworld.html | grep -c "window.__mzCarriedList("` prints 1 (the loot card only)
    - `grep -cE "__mz(GearTab|HeroTab|StoreScreen)\b" mazeworld.html src/browser/*.js` prints 0 for every file (adjacency: one `__mzTabs` name, not three)
    - `node tools/bridge-doc.mjs --check` exits 0; `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git diff --stat HEAD~1 -- test/unit/fixtures/` prints nothing; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1
    - `node --test test/unit/storeScreen.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `grep -E "^# pass"` prints a number >= 8
    - `git diff --stat HEAD~1 --diff-filter=D -- test/` prints nothing; for every re-pointed pin file F, `grep -c "^test(" F` equals `git show HEAD~1:F | grep -c "^test("`
    - `grep -c "storeScreen.js" test/unit/hp-not-wp.test.js` prints a number >= 1
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm run boot:check` prints four PASS lines
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The Store renders from storeScreen.js behind one mount call inside the encounter frame; `__mzTabs` has its final locked shape; the Store, Gear and Hero fixtures all compare byte-equal; pins re-homed; gates green.</done>
</task>

<task type="auto">
  <name>Task 2: closing gates — widen the no-duplicate pin to every src/browser export, measure the line budget (with the CONTEXT fallback), verify ROADMAP criteria 1-5 verbatim, write the phase closing SUMMARY</name>
  <files>test/unit/shell-no-content-copies.test.js, docs/SHELL-MODULES.md, mazeworld.html, .planning/STATE.md</files>
  <read_first>
    - test/unit/shell-no-content-copies.test.js (all tests; the `CLASSIC`/`MOD` regions and the declaration regex of test 1 — the widened test reuses them)
    - .planning/ROADMAP.md `### Phase 47` success criteria 1-5 (the verbatim commands)
    - tools/shell-sweep.mjs header (the `orphans` subcommand — advisory reachability listing; `refs` is the gate)
    - .planning/phases/47-shell-modularisation/47-03-SUMMARY.md and 47-04-SUMMARY.md (`wc -l` before/after per plan, the bridge deltas, the test ledgers — consolidated here)
    - .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md (the closing-record shape: criteria verbatim with outputs, test ledger, consolidated ledgers, Human verification section)
  </read_first>
  <action>
    1. **Widen criterion 3** in `test/unit/shell-no-content-copies.test.js`: add test 6 — read every `src/browser/*.js` at test time, derive the export-name set from `export (async )?(const|let|var|function|class) NAME` declarations and `export { a, b as c }` lists (a helper `exportNamesOf(source)`), assert the set has >= 100 names (measured 130 at planning, before the new modules), and assert that neither `CLASSIC` nor `MOD` contains a `^\s*(?:const|let|var|function|async function)\s+NAME\b` declaration for any of them; the test title reads "criterion 3: mazeworld.html declares no duplicate of any src/browser export (gearTab.js, heroTab.js, storeScreen.js, viewModels.js, bridge.js, ... every module)" and the failure message names the offending module and name. Keep tests 1-5 (re-homed in Plan 04) untouched.
    2. **Line budget:** `wc -l < mazeworld.html`. If < 5000, record it. If >= 5000: run `node tools/shell-sweep.mjs orphans`; for every orphan whose only former callers were the Gear/Hero/Store bodies (verify each with `refs`), move it into its tab module or delete it (refs 0), re-run the full gates, re-measure. Whatever the outcome, fill `docs/SHELL-MODULES.md`'s `## Line budget` with: the phase-start count (6,339 at 0129ca3), each plan's delta (from the SUMMARYs), the final count, and — if still >= 5,000 — a classified breakdown of the remaining bulk measured with: comment-only script lines (`awk 'NR>=<classic start> && NR<=<classic end>' mazeworld.html | grep -cE "^\s*(//|/\*|\*)"` and the same over the module script), markup+CSS lines before `<body>`, and the largest remaining classic function groups by name (map/camera/tap controls, rail, combat renderers, graves, oracle). In that case ALSO append to `.planning/STATE.md` under `### Blockers/Concerns (open)` a line `[SHELL-04 line budget]: Phase 47 landed mazeworld.html at N lines (criterion 2 says < 5,000) after carving the three surfaces and their private helpers; the remaining bulk is <breakdown>; candidate levers for a user ruling: Phase 48's comment purge (DOCS-01..03), module-owned screen markup, the inventory/store action bridges. Not a scope reduction — a measured shortfall.` and make the closing SUMMARY's criterion-2 line say NOT MET with the number.
    3. **ROADMAP criteria 1-5 verbatim** (run every command, paste outputs into the SUMMARY): (1) `test -f src/browser/gearTab.js src/browser/heroTab.js src/browser/storeScreen.js`; `test -f test/unit/gearTab.test.js test/unit/heroTab.test.js test/unit/storeScreen.test.js`; `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -cE "__mzTabs\.(hero|gear)\("` = 2; `awk '/^function renderEncounter\(\)/,/^}/' mazeworld.html | grep -c "__mzTabs.store("` = 1; the re-pointed suites green (`node --test test/unit/shell-company-panel.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-39.test.js test/unit/shell-map-store-polish.test.js`); (2) `wc -l < mazeworld.html` (the number, met or not); (3) `grep -cE "^const (WEAPONS|ARMOR|SPELLS|JEWELRY|CLOAKS|STAVES|SUB_NOTE|RACES|SUBS)\b" mazeworld.html` = 0 and `node --test test/unit/shell-no-content-copies.test.js` green; (4) `node --test test/unit/bridge-registry.test.js` green + the final registry count via `node -e "import('./src/browser/bridge.js').then(m => console.log(m.bridgeNames().length))"` + `node tools/bridge-doc.mjs --check`; (5) `node --test test/unit/shell-tab-snapshots.test.js` green, `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` = 1, `git diff --stat <phase-start-commit> -- engine/ content/ test/parity/fixtures/` empty, master hash. Plus `npm run build:www`, `npm test`, `npm run boot:check`.
    4. **Ledgers for the closing SUMMARY:** test count 3,231 (phase start, measured) -> final, every delta explained per plan; bridge count 55 (measured, comment-stripped) -> final, every addition/removal listed with its plan; deletions ledger (classic helpers + module-script functions + bridges) with the `refs`/ident-sweep proofs; the consolidated per-file pin re-point ledger across Plans 03-05 (`grep -c "^test("` per file unchanged); the snapshot outputs (7 compared, 0 diffs, fixtures untouched since Plan 01); `docs/SHELL-MODULES.md` regenerated (`--check` exit 0).
    5. Commit: `docs(47-05): phase 47 closing gates — widened no-duplicate pin, line budget measured, criteria 1-5 verified`. If the fallback moved code, commit that separately first (`refactor(47-05): move the tabs' remaining private helpers (line budget fallback)`).
  </action>
  <verify>
    <automated>node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)" && grep -cE "^const (WEAPONS|ARMOR|SPELLS|JEWELRY|CLOAKS|STAVES|SUB_NOTE|RACES|SUBS)\b" mazeworld.html; wc -l < mazeworld.html; grep -c "## Line budget" docs/SHELL-MODULES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "criterion 3: mazeworld.html declares no duplicate of any src/browser export" test/unit/shell-no-content-copies.test.js` prints 1; `grep -cE "gearTab\.js.*heroTab\.js.*storeScreen\.js" test/unit/shell-no-content-copies.test.js` prints a number >= 1; `node --test test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# pass"` prints a number >= 6 and `grep -E "^# fail"` prints `# fail 0`
    - `grep -cE "^const (WEAPONS|ARMOR|SPELLS|JEWELRY|CLOAKS|STAVES|SUB_NOTE|RACES|SUBS)\b" mazeworld.html` prints 0
    - `wc -l < mazeworld.html` printed and recorded; `awk '/^## Line budget/,0' docs/SHELL-MODULES.md | grep -cE "[0-9]{4}"` prints a number >= 3 (start, deltas, final); if the final count is >= 5000 then `grep -c "SHELL-04 line budget" .planning/STATE.md` prints 1 and the SUMMARY's criterion-2 line contains "NOT MET"; if < 5000 the SUMMARY's criterion-2 line contains "Holds"
    - `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -cE "__mzTabs\.(hero|gear)\("` prints 2; `awk '/^function renderEncounter\(\)/,/^}/' mazeworld.html | grep -c "__mzTabs.store("` prints 1
    - `node -e "import('./src/browser/bridge.js').then(m => console.log(m.bridgeNames().length))"` prints the final count (recorded); `node tools/bridge-doc.mjs --check` exits 0
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass"` prints a number >= 3231 (recorded with the delta ledger)
    - `npm run boot:check` prints four PASS lines
    - `git diff --stat 0129ca3 -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `test -f .planning/phases/47-shell-modularisation/47-05-SUMMARY.md` exit 0 after the SUMMARY is written, and it contains the sections `## Success criteria`, `## Test ledger`, `## Bridge ledger`, `## Pin re-point ledger`, `## Line budget`, `## Human verification (deferred to end of run)`
  </acceptance_criteria>
  <done>Criteria 1, 3, 4, 5 hold with recorded outputs; criterion 2 is measured and either holds or is recorded as NOT MET with the classified shortfall and a STATE blocker; the closing SUMMARY consolidates every ledger.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-level list (the plan that creates each item in brackets):

- `src/browser/gearTab.js` [03] — `renderGearTab(host, state, deps)`, `renderCarriedList(container, state, items, opts, deps)`, `bagUsage`, `GEAR_COPY`, `emptySlotRows`, `ITEM_STATE_COPY`, `itemRowState`
- `src/browser/heroTab.js` [04] — `renderHeroTab(host, state, deps)`, `characterSheetViewModel`, `ABILITY_VIEW_COPY`, `RATIONS_COPY`, `eatsLineFor`, `rationsViewModel`, `grimoireViewModel`
- `src/browser/storeScreen.js` [05] — `renderStoreScreen(host, state, deps)`, `STORE_ROLL_COPY`
- `src/browser/bridge.js` [02] — `BRIDGE`, `bridgeNames()`; `src/browser/viewModels.js` [03/04] — the seven shared exports only
- `mazeworld.html` [02-05] — `tabDeps()`; `paint()` mounts hero then gear through `window.__mzTabs`; `renderEncounter()` mounts the store; the module script assigns `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen })` and `window.__mzCarriedList`; dead helpers and bridges gone
- `test/unit/harness/recordingDom.js`, `test/unit/harness/shellSandbox.js`, `test/unit/shell-tab-snapshots.test.js`, `test/unit/fixtures/shell-snapshots/*.txt` (7) [01]
- `test/unit/bridge-registry.test.js`, `tools/bridge-doc.mjs`, `tools/ident-sweep.mjs` (exports) [02]
- `test/unit/gearTab.test.js` [03], `test/unit/heroTab.test.js` [04], `test/unit/storeScreen.test.js` [05]
- `test/unit/shell-no-content-copies.test.js` — widened to every src/browser export [05]
- `docs/SHELL-MODULES.md` [02, 03-05 regenerated, 05 line budget]
- `.planning/STATE.md` — a `[SHELL-04 line budget]` blocker ONLY if the final count is >= 5,000 [05]
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine store stock -> #enc-body innerHTML | stock names/sub/usable are engine + content data (never user input); `usableBy` returns static USABLE_COPY strings |
| deps closures -> window.mzBuyItem / mzLeaveStore | the module dispatches only through the closures the classic script hands it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-09 | Tampering | storeScreen.js template | medium | mitigate | the template moves byte-identical (the two store fixtures prove it); the re-homed shell-clarity-43 pin asserts `usableBy` output (class names only) is the sole non-literal text inside the row innerHTML |
| T-47-10 | Repudiation | criterion 2 reporting | medium | mitigate | the count is measured by command at the final commit and recorded in three places (SUMMARY, docs, STATE blocker when unmet); the prohibition forbids reporting it met when it is not |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 47 |
</threat_model>

<verification>
- All five ROADMAP criteria commands run and recorded; snapshot test green with fixtures untouched since Plan 01; registry test green; widened no-duplicate pin green
- `npm test` fail 0; `build:www`; `boot:check` four PASS; fence diff against `0129ca3` empty for engine/content/parity
</verification>

<success_criteria>
- SHELL-03: the Store renders from `src/browser/storeScreen.js` with its own source-pin suite; render idempotency proven
- SHELL-04: `__mzTabs` final shape; registry + doc at their final state; the widened no-duplicate pin names every module; the line count measured and reported honestly (met, or NOT MET with the breakdown + blocker)
- SHELL-01/02: fixtures still byte-equal at the phase's final commit; every pin re-homed, none deleted
</success_criteria>

<output>
Create `.planning/phases/47-shell-modularisation/47-05-SUMMARY.md` when done — the phase closing record in the 46-04-SUMMARY.md shape: `## Success criteria` (1-5 verbatim with outputs; criterion 2 "Holds" or "NOT MET — N lines" with the breakdown), `## Test ledger` (3,231 -> final, per plan), `## Bridge ledger` (55 -> final, every +/- with plan and proof), `## Deletions ledger`, `## Pin re-point ledger` (consolidated 03-05, `grep -c "^test("` unchanged per file), `## Snapshot lock` (7 fixtures, 0 diffs, one fixture commit), `## Line budget` (start, per-plan deltas, fallback actions, final), `## Gate outputs` per commit, and `## Human verification (deferred to end of run)`: Gear tab (ON YOU / BAG, equip/use/drop/swap confirms), Hero tab (sheet, dossier, Company, Grimoire, RATIONS, abilities), Store (buy/sell/repair rows, usable-by suffixes, Bag-full line, Leave) all look and behave exactly as before; tab switching shows no flash or missing panel — the milestone-close Pixel 7 batch.
</output>
