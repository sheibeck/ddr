---
phase: 75.3-deep-floor-encounter-scaling
plan: 07
type: execute
wave: 5
depends_on:
  - 75.3-01
  - 75.3-02
  - 75.3-03
  - 75.3-04
  - 75.3-05
files_modified:
  - docs/DIFFICULTY-RETUNE.md
  - docs/ROLL-LEDGER.md
  - test/unit/control-at-depth-rules.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - tools/voice-sample.mjs
  - tools/voice-sample-output.txt
  - tools/readouts/75.3-07-deep-final.txt
  - tools/readouts/75.3-07-rotation-final.txt
  - .planning/phases/75.3-deep-floor-encounter-scaling/fit/tail-final.jsonl
autonomous: true
requirements:
  - RULES-16
  - RULES-17
  - RULES-18
must_haves:
  truths:
    - "docs/DIFFICULTY-RETUNE.md has one `## v2.1 deep-floor encounter scaling (Phase 75.3) — bot readouts` H2 immediately before `## v1.2 retune (Phase 27)` (which stays the last H2): the change under measurement (RULES-16/17/18), the parameters, every BEFORE and AFTER block (75.3-01 natural; 75.3-02 deep and rotation; 75.3-03 deep; 75.3-05 rotation), the `#### Fit` record from 75.3-06 (decision, evaluation table, ruling-F rows, stop reason, locked values), the FINAL blocks, each fenced block proven against its file with readout-compare --recorded"
    - "The record gives a PASS / MISS verdict, BEFORE and FINAL, for every target the user ruled on 2026-09-25 (75.3-CONTEXT 'Design rulings after planning'): fresh start at 1,000 seeds reaching floor 20 at about 1% or less, floor 21 or deeper under 0.5%, floor 30 in 0 or 1 run of 1,000; the floor-12 start reaching floor 20 at 5% or less; the floor-20 start gaining a median of 2 floors or fewer and reaching floor 30 at 1% or less; the floor-30 start gaining a median of 0 floors with p90 of 1 or fewer; the rotation Sorcerer never outlasting the fair bot from the same depth; the average run still ending on floors 5-7 (the 1,000-seed median death depth), with floors 1-12 not refitted"
    - "The record also reads the phase against the wider target (75.3-CONTEXT: 'report per-band reach (5+, 10+, 20+)'): a per-band reach table BEFORE → after RULES-16 → FINAL, the natural floors 1-12 verdict, and the 1,000-seed tail rows 13-20 (measured, not fitted); every missed ruled target or band shift gets a 'Flag for the user' paragraph, and nothing is retuned"
    - "The control-rotation verdict is explicit (ROADMAP criterion 5): the Human Sorcerer on the rotation from depths 20, 30 and 40, BEFORE and FINAL, against the fair bot from the same depths and the plain Sorcerer, with a one-line verdict per depth (outlasts the fair bot, or no longer does); depth 40 is reported beside it; the Troll Summoner from 20 is reported"
    - "docs/ROLL-LEDGER.md gains `## Phase 75.3 control at depth (RULES-18)`, appended with CRLF and zero deleted lines: the depth-resist roll site (a roll-high d20 on a derived stream, faces by depth), the `[resist:depth]` row, the hold rule, and the control audit C1-C19 / X1-X7 copied from 75.3-04 with each row's verdict"
    - "A standing guard, test/unit/control-at-depth-rules.test.js, fails when a foe-control assignment (asleep, stupid, frozen, blind, weakened, shrunk or held set on a foe or the combat) appears in an engine function that does not also call resistControl, outside the audited exemptions (X5's abilities, holdFoe itself); controlResistCheck is called only by resistControl; the only 'controlResist' derived stream is in derived.js; the ledger section lists every audit id; its failure message says a new control must go through resistControl and join the audit"
    - "test/parity/FIXTURE-INVENTORY.md's Phase 75.3 section closes with a consolidated summary, and a Phase 75.3 exposure guard in divergence-records.test.js asserts that no replay site starts a fight on floor 5 or deeper, no foe carries `elite`, `held` or `resisted`, and no event is controlResisted, controlHeld, foeStillHeld or foeHoldBroken unless a Phase 75.3 record declares it; a doctored event list fails it"
    - "tools/voice-sample.mjs renders the four new events from constant fields (no new random draws) and the regenerated sample shows a resist line with a roll and a hold line"
    - "The whole-phase gates hold at the head: npm test 0 failures; the parity suite green; the prototype master hash unchanged since the phase base; the roll-high guard, direction tests, ledger sync, state pins, save compat and sign-consistency tests green"
  artifacts:
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 75.3 readout record, the fit record and the rotation verdict"
      contains: "Phase 75.3"
    - path: docs/ROLL-LEDGER.md
      provides: "the depth-resist site and the control audit"
      contains: "Phase 75.3 control at depth"
    - path: test/unit/control-at-depth-rules.test.js
      provides: "the standing control-site guard"
      contains: "RULES-18"
    - path: test/parity/divergence-records.test.js
      provides: "the Phase 75.3 exposure guard"
      contains: "Phase 75.3"
    - path: tools/readouts/75.3-07-final-1000.txt
      provides: "the 1,000-seed FINAL natural readout"
  key_links:
    - from: docs/DIFFICULTY-RETUNE.md (Phase 75.3 blocks)
      to: tools/readouts/75.3-0*.txt
      via: "readout-compare --recorded"
      pattern: "75.3-0"
    - from: test/unit/control-at-depth-rules.test.js
      to: engine/combat.js#resistControl
      via: "the one allowed door for a control"
      pattern: "resistControl"
  prohibitions:
    - statement: "MUST NOT retune anything in this plan: a reading outside the target becomes a 'Flag for the user' paragraph, never an edit (75.3-CONTEXT: don't auto-compensate; engine changes go to the user)"
      requirement_id: RULES-16
      category: transparency
      status: unverified
      flagged: true
    - statement: "MUST NOT report a ruled target (or the control-rotation verdict) as met when the FINAL readout says otherwise; every verdict is read from the numbers, and a miss is flagged"
      requirement_id: RULES-18
      category: transparency
      status: unverified
      flagged: true

> **ORCHESTRATOR NOTE (user ruling 2026-09-26):** bots run only at the milestone end. This plan does NOT run any readout, 1,000-seed slice or verdict against the tail targets. Those, and the checkpointed sweep (formerly 75.3-06), move to Phase 79.1. Keep only the non-bot close-out: the ROLL-LEDGER audit and its standing guard, the FIXTURE-INVENTORY close and exposure guard, the voice sample, and the gates. In DIFFICULTY-RETUNE.md, write only a Phase 75.3 stub that points to 79.1.

---

<objective>
Close Phase 75.3: take the FINAL readouts (a 200- and a 1,000-seed natural run, the deep-start slices, the rotation Sorcerer, one tail row), write the whole phase into docs/DIFFICULTY-RETUNE.md with the fit record and a PASS / MISS verdict on every target the user ruled (the unicorn at 20, depth 30 basically never, the rotation contained, the average run on floors 5-7), put the control audit into the roll ledger with a standing guard, finish the fixture story with an exposure guard, refresh the voice sample, and prove every gate at the head.

Purpose: ROADMAP criteria 3-5 ("a 200-seed bot readout before and after is recorded against the depth-20 unicorn / floor 5-7 average target"; "slopes set by bot readouts"; "a control-lock rotation no longer carries a caster to depth 40") and the standing engine gate. Each plan committed its own readouts so the waves never collided on the shared ledgers; this plan writes them once.

Output: the final readouts, the DIFFICULTY-RETUNE.md H2, the ROLL-LEDGER section and its guard, the FIXTURE-INVENTORY close and exposure guard, the voice sample, and the gates with the phase's human checks rolled up.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75.3-07 creates or changes.)

- 75.3-01: FOE_COUNT_DEPTH and the depth-aware count; `test/unit/foe-count-depth.test.js`; the 75.3 overlay; readouts
- 75.3-02: the `controlRotation` bot option and flags; `tools/lib/tail-score.mjs`; `fit-difficulty.mjs --objective=tail`; the phase BEFORE deep readouts
- 75.3-03: the knee dials and elites; `test/unit/deep-curve.test.js`; readouts
- 75.3-04: CONTROL_AT_DEPTH, the resist check, held foes, combat.js sites, four events, two chips; the control audit table
- 75.3-05: hero spells and items at depth, honest texts, the bot's Freeze at depth; rotation readouts
- 75.3-06: the checkpointed tail sweep and locked values; `fit/tail-log.jsonl`, `fit/tail-best.json`
- **This plan (75.3-07):** `## v2.1 deep-floor encounter scaling (Phase 75.3) — bot readouts` in docs/DIFFICULTY-RETUNE.md; `## Phase 75.3 control at depth (RULES-18)` in docs/ROLL-LEDGER.md; `test/unit/control-at-depth-rules.test.js`; the FIXTURE-INVENTORY Phase 75.3 summary and the exposure guard in `test/parity/divergence-records.test.js`; the voice-sample update and output; `tools/readouts/75.3-07-{final-200,final-1000,deep-final,rotation-final}.txt` and `fit/tail-final.jsonl`; the gates and the human-check rollup

## Flagged assumptions (surfaced, not silently decided)

- **Readings are recorded, not acted on.** If the average run leaves floors 5-7, a band moves, or the rotation still outlasts the field, this plan writes a "Flag for the user" paragraph and changes nothing.
- **The 1,000-seed tail is measured, not fitted** (floors 13-20 and reach-20 from the natural run), matching the Phase 54 record's convention.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-CONTEXT.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-01-SUMMARY.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-02-SUMMARY.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-03-SUMMARY.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-04-SUMMARY.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-05-SUMMARY.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-06-SUMMARY.md
@.planning/phases/75.3-deep-floor-encounter-scaling/75.3-04-PLAN.md

<interfaces>
- docs/DIFFICULTY-RETUNE.md: the Phase 72, 73, 75, 75.1 and 75.2 bot-readout H2s are the format (the 75.2 H2 sits immediately before `## v1.2 retune (Phase 27)`, which must stay the last H2: test/unit/difficulty-retune-ledger.test.js). `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading line>" tools/readouts/<file>` proves the first fenced block under a heading is an in-order subset of the file (heading text matched from its `#`s).
- docs/ROLL-LEDGER.md is CRLF; append with a small Node script that keeps CRLF and deletes nothing (74-08's method). The control audit table is in 75.3-04-PLAN.md ("The control audit").
- Engine sites that assign foe control (read them): engine/magic.js#castSpell, engine/combat.js (allyCast, foeTurn's Ice payoff, sing, holdFoe), engine/items.js#useItem, engine/abilities.js (applyPommel sets `stunned`, applyDirtyTrick sets `blind` and `blindFor`: X5, exempt). Phase 75.1 added scroll-fumble paths that act on the READER (hero-side `combat.heroBlind`, `heroShrunk`, `heroOut`, `selfDot`) and a fumbled foe `regen`; none is a foe control. If the scan nevertheless finds a foe-control assignment in a 75.1 fumble path, add it to the audit as an X row (a fumble effect, not the hero's control) and to the guard's exemption list with that reason, and name it in the SUMMARY.
- test/parity/divergence-records.test.js: `replaySiteEvents(seed, actions, opts)` returns `{ state, events, ... }` per replay site; the Phase 75 / 75.1 / 75.2 exposure guards show how to walk every site and assert the site count; `collectRecords()` gathers declared records.
- tools/voice-sample.mjs: sampleEvent(type) draws one random event per line; adding a draw to it reshuffles the sample, so render the new events in a separate deterministic block with constant fields (75.2-05's method).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The FINAL readouts and the DIFFICULTY-RETUNE.md record</name>
  <files>docs/DIFFICULTY-RETUNE.md, tools/readouts/75.3-07-final-200.txt, tools/readouts/75.3-07-final-1000.txt, tools/readouts/75.3-07-deep-final.txt, tools/readouts/75.3-07-rotation-final.txt, .planning/phases/75.3-deep-floor-encounter-scaling/fit/tail-final.jsonl</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md (the Phase 75, 75.1 and 75.2 H2s in full; Phase 54's `#### Fit` and `#### Tail` sections for the fit-record and tail-table format)
    - tools/readout-compare.mjs and test/unit/difficulty-retune-ledger.test.js
    - every tools/readouts/75.3-0*.txt file and the 75.3 SUMMARYs' readout notes; fit/tail-before.jsonl, fit/tail-log.jsonl
  </read_first>
  <action>
1. Record the phase base (the commit before 75.3-01's first edit, from 75.3-01's SUMMARY) and the plan base.
2. With run_in_background, one at a time, each watched by a Monitor: `node tools/tune-difficulty.mjs --seeds=200` to tools/readouts/75.3-07-final-200.txt; `node tools/tune-difficulty.mjs --seeds=1000` to tools/readouts/75.3-07-final-1000.txt; the four `=== fair --start-depth=N ===` blocks (12, 20, 30, 40, 200 seeds) to tools/readouts/75.3-07-deep-final.txt; the five 75.3-02 build blocks (rotation 20 / 30 / 40, plain Sorcerer 20, Troll Summoner 20) to tools/readouts/75.3-07-rotation-final.txt; and `node tools/fit-difficulty.mjs --objective=tail --fresh=always --dials='{}' --workers=4 --log=.planning/phases/75.3-deep-floor-encounter-scaling/fit/tail-final.jsonl` (the scored FINAL row against the ruled targets).
3. Insert `## v2.1 deep-floor encounter scaling (Phase 75.3) — bot readouts` immediately before `## v1.2 retune (Phase 27)` (after the Phase 75.2 H2). It holds:
   - "Change under measurement": RULES-16 (the count rule table), RULES-17 (the knee after floor 12, elites from floor 16 with their titles), RULES-18 (the resist faces by floor, the hold of holdRounds, the audit summary), with the locked dial values.
   - "Parameters": 200 / 1,000 seeds, start depths, the rotation and build slices, the shipped dials.
   - `### Plan 01 — BEFORE`, `### Plan 01 — AFTER`, `### Phase BEFORE — deep slices`, `### Phase BEFORE — builds`, `### Plan 03 — BEFORE`, `### Plan 03 — AFTER`, `### Plan 05 — rotation BEFORE`, `### Plan 05 — rotation AFTER`, `### Phase 75.3 — FINAL (200)`, `### Phase 75.3 — FINAL (1,000)`, `### Phase 75.3 — FINAL deep slices`, `### Phase 75.3 — FINAL builds`: each followed by a fenced block of headline lines copied verbatim from its file and a short reading.
   - `#### Fit`: 75.3-06's decision, the evaluation table, every ruling-F row, the stop reason, the locked values and the final tail row.
   - `### Verdicts against the ruled targets`: one row per target the user ruled on 2026-09-25 (quoted), with its BEFORE value (fit/tail-before.jsonl, the 75.3-02 readouts), its FINAL value (fit/tail-final.jsonl and the FINAL readouts), the target and PASS / MISS; the fresh-start rows read from tools/readouts/75.3-07-final-1000.txt (reach 20, reach 21+, the count reaching 30, the median death depth for the floor 5-7 average).
   - A per-band reach table (reach 5+, 10+, 20+, median, p90) BEFORE → after RULES-16 → FINAL; the natural floors 1-12 verdict; the 1,000-seed tail rows 13-20 (measured, not fitted).
   - A rotation table (rotation Sorcerer vs the fair slice vs the plain Sorcerer, from 20 / 30 / 40, BEFORE and FINAL: median and p90 floors gained) with a one-line verdict per depth, and the Troll Summoner from 20.
   - A dial change table for FOE_COUNT_DEPTH, FOE_HP_SCALE and FOE_HIT_SCALE (knee fields), FOE_ELITE and CONTROL_AT_DEPTH (before, locked, searched or held, direction, record).
   - "Reading against the target": the user's words ("Living past floor 20 should be exceedingly rare. 20 is the unicorn run. Player getting to depth 30 should basically never happen") and the floor 5-7 average, with a "Flag for the user" paragraph for every MISS in the verdict table. No retune.
4. Prove every fenced block with `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading line>" tools/readouts/<file>` and run the ledger test. List the commands in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js && node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "### Phase 75.3 — FINAL (200)" tools/readouts/75.3-07-final-200.txt && node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "### Phase 75.3 — FINAL builds" tools/readouts/75.3-07-rotation-final.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## v2.1 deep-floor encounter scaling (Phase 75.3) — bot readouts" docs/DIFFICULTY-RETUNE.md` equals 1, and the last `^## ` line of the file is still `## v1.2 retune (Phase 27) — TUNE-05..07`.
    - For each of the twelve `### ` headings listed in step 3, `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<matching file>` exits 0 (the twelve commands in the SUMMARY).
    - The four final readout files and fit/tail-final.jsonl exist and are non-empty; fit/tail-final.jsonl's row has `freshMeasured` true.
    - `grep -c "### Verdicts against the ruled targets" docs/DIFFICULTY-RETUNE.md` equals 1, and that table has a PASS or MISS in every row (the SUMMARY lists each).
  </acceptance_criteria>
  <done>The whole phase is on record against machine-checked readouts, with the fit record, a PASS / MISS verdict for every ruled target, a per-band reach table and an explicit rotation verdict, and every miss flagged rather than fixed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The control audit in the roll ledger and its standing guard</name>
  <files>docs/ROLL-LEDGER.md, test/unit/control-at-depth-rules.test.js</files>
  <read_first>
    - .planning/phases/75.3-deep-floor-encounter-scaling/75.3-04-PLAN.md ("The control audit")
    - docs/ROLL-LEDGER.md (its closing sections, the `[resist:depth]` row 75.3-04 added, and the Phase 75.2 hero-size section for the section format)
    - engine/magic.js, engine/combat.js, engine/items.js, engine/abilities.js, engine/derived.js (every control assignment and the resist helpers)
    - test/unit/hero-size-rules.test.js (75.2-05's scan-guard pattern and comment stripper)
  </read_first>
  <behavior>
    - On comment-stripped engine sources, every function body that assigns a control to a foe or the combat (`.asleep =` with a value, `.stupid = true`, `.frozen = true`, `.blind = true`, `weakened = true`, `.shrunk = true`, `.held =`) also calls `resistControl(`, except applyPommel and applyDirtyTrick (X5) and holdFoe itself.
    - `controlResistCheck(` appears in engine code only inside combat.js#resistControl; the string "controlResist" as a derived-stream purpose appears only inside derived.js#controlResistCheck.
    - docs/ROLL-LEDGER.md's Phase 75.3 section lists C1 through C19 and X1 through X7.
    - Teeth: the scan helper, fed a doctored function body that sets `f.asleep = 5` without calling resistControl, reports it; fed one that calls resistControl, passes.
  </behavior>
  <action>
1. docs/ROLL-LEDGER.md: append `## Phase 75.3 control at depth (RULES-18)` with a Node script that keeps CRLF and deletes nothing: the rule in one paragraph (the knee, faces by floor and the cap, roll-high on the derived stream, the hold of holdRounds, indefinite controls only capped); a "Depth-resist site" table (derived.js#controlResistCheck: d20, the resistor is the foe, resisted on roll >= 21 - faces, `[resist:depth]`, events controlResisted); the audit table C1-C19 / X1-X7 copied from 75.3-04-PLAN.md with each row's verdict and landed plan.
2. Create test/unit/control-at-depth-rules.test.js with the behaviour above (a local comment stripper and a function-body splitter over the engine files; the failure message states the RULES-18 rule: every new foe control goes through resistControl and joins the audit).
  </action>
  <verify>
    <automated>node --test test/unit/control-at-depth-rules.test.js test/unit/roll-ledger-sync.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Phase 75.3 control at depth (RULES-18)" docs/ROLL-LEDGER.md` equals 1, `grep -c "| C19 |" docs/ROLL-LEDGER.md` equals 1, and `git diff --numstat <plan-base> -- docs/ROLL-LEDGER.md` shows 0 deleted lines.
    - `node --test test/unit/control-at-depth-rules.test.js` passes with at least 5 tests.
  </acceptance_criteria>
  <done>Every control in the game is audited in the ledger and guarded: a future control that skips the depth rule fails the build.</done>
</task>

<task type="auto">
  <name>Task 3: The fixture close, the exposure guard, the voice sample and the gates</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js, tools/voice-sample.mjs, tools/voice-sample-output.txt</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 75.3 section's plan subsections)
    - test/parity/divergence-records.test.js (replaySiteEvents, collectRecords, the Phase 75 / 75.1 / 75.2 exposure guards)
    - tools/voice-sample.mjs (sampleEvent, MOD_NAMES, 75.2-05's deterministic block)
    - every Phase 75.3 SUMMARY's fixture and pin measurement
  </read_first>
  <action>
1. test/parity/FIXTURE-INVENTORY.md: close the Phase 75.3 section with `### Phase 75.3 — summary`: each plan's predictor, measurement and declared moves (from the SUMMARYs), the carve-outs (`elite`, `held`, `resisted`), the prototype master hash, and the guard's name.
2. test/parity/divergence-records.test.js: add the Phase 75.3 exposure guard from the truths, walking every replay site with replaySiteEvents (assert the current site count), with a teeth test on a doctored event list carrying a controlResisted event.
3. tools/voice-sample.mjs: append a deterministic block rendering EVENT_NARRATION for controlResisted (a Freeze shaken off: roll 17, atLeast 13, dieN 20, depth 20), controlHeld (frozen, 3 rounds), foeStillHeld and foeHoldBroken (stone), with constant fields and no new random draw. Run `node tools/voice-sample.mjs` and commit the regenerated output.
4. Gates: `npm test` 0 failures; `node --test "test/parity/**/*.test.js"`; `git hash-object test/parity/prototype-master.js.txt` unchanged since the phase base; `node --test test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js test/unit/roll-sign-consistency.test.js`.
5. Roll every Phase 75.3 plan's human checks into one list in the SUMMARY for the milestone's batched Pixel 7 round (deferred UAT): on a start-at-depth 20 run, fights bring three foes with titled elites; foe details' "hits for" range matches the blows; a Freeze past floor 12 either prints a "shrugs off" line with its roll or holds the foe with a "Frozen · 3" chip that counts down and breaks; an Unmoved chip explains itself on long-press; on floors 5-9 roughly one fight in four is solo; the changed spell and staff texts read well on the grimoire and the gear sheet.
  </action>
  <verify>
    <automated>node --test test/parity/divergence-records.test.js "test/parity/**/*.test.js" test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js test/unit/roll-sign-consistency.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Phase 75.3 — summary" test/parity/FIXTURE-INVENTORY.md` equals 1 and `grep -c "Phase 75.3" test/parity/divergence-records.test.js` is at least 1.
    - `grep -c "shrugs off" tools/voice-sample-output.txt` is at least 1.
    - `git hash-object test/parity/prototype-master.js.txt` equals the phase-base hash recorded in 75.3-01's SUMMARY.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The phase's fixture story is complete and guarded, the voice sample shows the new lines, every gate is green, and the device checks are batched for the milestone close.</done>
</task>

</tasks>

<verification>
- The DIFFICULTY-RETUNE.md record, the ROLL-LEDGER audit and its guard, the FIXTURE-INVENTORY close and exposure guard, and the voice sample are in place; every readout block is machine-checked; npm test is green.
- Every ruled target's verdict and the rotation verdict are read from the FINAL numbers; every miss is flagged, nothing is retuned.
</verification>

<success_criteria>
ROADMAP Phase 75.3 criteria 3-5 are recorded and guarded: before/after readouts against the depth-20 unicorn / floor 5-7 target with declared fixtures, slopes set by readouts, and an explicit verdict on whether a control-lock rotation still carries a caster deep.
</success_criteria>

<output>
Create `.planning/phases/75.3-deep-floor-encounter-scaling/75.3-07-SUMMARY.md` when done. Include the readout-compare commands, the ruled-target verdict table, the per-band and rotation tables, the gate results, and the rolled-up human-check list.
</output>
