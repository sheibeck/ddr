---
phase: 18-bestiary-rebalance-canon-combat-fixes
plan: 04
type: execute
wave: 2
depends_on: ["18-02"]
files_modified:
  - engine/magic.js
  - engine/items.js
  - test/unit/magic.test.js
  - test/unit/item-wiring.test.js
autonomous: true
requirements: [CANON-01, CANON-03, CANON-04, FID-05]
tags: [engine, magic, items, damage-seam, determinism, parity, node-test]

must_haves:
  truths:
    - "CANON-04 (D-09, D-11): every spell damage site in engine/magic.js — quake (per foe), volley (per ball), thrown (Fireball / Lightning / Freeze etc.), and the insane r=2 foe-on-foe blow — calls damageFoe with a typed source ({ kind: 'spell', school: sp.kind, casterSub: c.sub } for cast spells; { kind: 'foe', crit: false } for the foe-on-foe blow) and keeps its own kill check where it already is; no foe-side direct wp decrement remains on a code line in magic.js (the caster's own wp decrements for quake self-damage, Apprentice backfire and the death spell are untouched)"
    - "CANON-04 covered: a Cleric's Fireball deals exactly double to a Demons foe (14 -> 28) and a Wizard's does not (14); any caster's Fireball deals double to a Walking Dead foe; Earthquake is applied PER FOE (a Walking Dead foe takes 2x the rolled amount while a Humans foe in the same cast takes 1x) and the earthquake event still reports the single rolled amount"
    - "CANON-03 covered: volley (Fireballs) totals APPLIED damage — each ball on a halfDmg foe is ceil-halved and volley.totalDamage is the sum of what landed"
    - "CANON-01 (D-06): spell damage never draws the soak d20 — a Fireball against an sp.ar 15 foe lands in full with zero extra draws; the insane r=2 foe-on-foe blow IS physical and can be soaked by the victim's own natural armor (one gated d20), in which case insaneStruckAlly is not pushed"
    - "CANON-01 (A3): the fire item effect (Pine Staff, items.js 'fire') routes each ball through damageFoe as { kind: 'item', crit: false } — soakable by sp.ar (one gated d20 per soakable ball), never multiplied; itemBurned.total sums applied damage"
    - "FID-05: the magic-parity fixture (seed 8, Shriek, cast-damage) and every other parity fixture stay byte-identical — no foe any fixture rolls has ar/halfDmg or a multiplier-matching type against the fixture's caster, so the routed sites are pure pass-throughs there; zero fixture regeneration"
    - "Event shapes: spellHit.dmg, acid handling (unchanged — acid is applied at cast time and ticks in combat.js), volley.totalDamage, insaneStruckAlly.dmg and itemBurned.total report APPLIED values; earthquake.amount keeps the rolled base (no shape change on any event)"
  artifacts:
    - path: "engine/magic.js"
      provides: "damageFoe import + four routed sites (quake, volley, insane r=2, thrown)"
      contains: "import { damageFoe } from \"./foeDamage.js\""
    - path: "engine/items.js"
      provides: "damageFoe import + routed 'fire' effect"
      contains: "kind: \"item\""
    - path: "test/unit/magic.test.js"
      provides: "Cleric-vs-Demons x2 / Wizard control; Wizard-vs-Walking-Dead x2 / vs Demons control; per-foe Earthquake; halfDmg volley total; spell vs sp.ar zero-draw; insane r=2 soak + control"
      contains: "CANON-04"
    - path: "test/unit/item-wiring.test.js"
      provides: "fire item soaked by sp.ar (one extra d20, total 0) + unarmoured control (total 10)"
      contains: "foeArmorSoaked"
  key_links:
    - from: "engine/magic.js castSpell thrown branch"
      to: "engine/foeDamage.js damageFoe"
      via: "const hit = damageFoe(state, t, dmg, { kind: \"spell\", school: sp.kind, casterSub: c.sub }, rng, events); spellHit dmg: hit.applied; existing freeze / killFoe handling follows"
      pattern: "kind: \"spell\", school: sp.kind, casterSub: c.sub"
    - from: "engine/magic.js castSpell insane r=2"
      to: "engine/foeDamage.js damageFoe"
      via: "damageFoe(state, o, d, { kind: \"foe\", crit: false }, rng, events)"
      pattern: "kind: \"foe\", crit: false"
    - from: "engine/items.js useItem fire"
      to: "engine/foeDamage.js damageFoe"
      via: "damageFoe(state, t, dmg, { kind: \"item\", crit: false }, rng, events); tot += hit.applied"
      pattern: "kind: \"item\", crit: false"
  prohibitions:
    - requirement_id: FID-05
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, regenerate any test/parity/fixtures/*.json (including action-script.magic.json), or change test/parity/harness/comparables.js"
    - requirement_id: CANON-01
      category: safety
      statement: "MUST NOT introduce any rng draw on a spell path — spells bypass armor (D-06), so no damageFoe call with kind 'spell' may ever reach the d20; the only new draws in this plan are the gated d20 for the insane foe-on-foe blow and the item fire balls, both fired only against a foe with sp.ar"
    - requirement_id: CANON-04
      category: values
      statement: "MUST NOT treat direct wp SETS as damage — shrink (ceil-halve of wp/maxWP), petrify/turn/gate/insane r=1,3,6 and Freeze's wp = 0, vapor's wp = 0, and stone's wp = 0 are status effects or instant removals and stay outside the seam (18-RESEARCH.md diagram: 'Excluded from the seam')"
    - requirement_id: CANON-01
      category: values
      statement: "MUST NOT move killFoe into the seam or reorder any kill check / continue / return relative to its damage line"
    - requirement_id: CANON-01
      category: safety
      statement: "MUST NOT create an import cycle: magic.js and items.js import the seam; engine/foeDamage.js must keep importing only from ../content/index.js"
---

<objective>
Route the remaining five damage-to-foe sites through the 18-02 seam: `engine/magic.js`'s quake (per foe), volley (per ball), thrown (Fireball/Lightning/Freeze and the other thrown kinds) and the insane r=2 foe-on-foe blow, plus `engine/items.js`'s fire item effect — with typed sources, applied-damage event payloads, and every kill check exactly where it already is. Prove the CANON-04 rows through real casts (Cleric vs Demons, any spell vs Walking Dead, per-foe Earthquake), the CANON-03 halving through a volley total, and the D-06 spell-bypass plus the physical soakability of item fire and the foe-on-foe blow with `fakeRng` sequences.

Purpose: CANON-04's "one small table in the magic damage path" is only real once the spell sites consult it; this plan is the magic-side half of the routing (18-03 is the combat-side half; the two are disjoint files in the same wave). It also closes the two non-hero, non-spell physical sources (item fire, foe-on-foe) so no direct foe-wp decrement is left anywhere for 18-06's invariant test.

Output: modified `engine/magic.js`, `engine/items.js`; extended `test/unit/magic.test.js`, `test/unit/item-wiring.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-PATTERNS.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-02-SUMMARY.md
@engine/magic.js
@engine/items.js
@engine/foeDamage.js
@content/spells.js
@test/unit/magic.test.js
@test/unit/item-wiring.test.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| routed `engine/magic.js` sites: quake (spell), volley (spell), insane r=2 (foe), thrown (spell) | engine edits | **18-04** | kill checks unchanged; earthquake.amount keeps the rolled base |
| routed `engine/items.js` fire effect (item) | engine edit | **18-04** | soakable, never multiplied |
| `engine/foeDamage.js` seam, `content/damage-multipliers.js`, `foeArmorSoaked` | seam + content + event | 18-02 | consumed here |
| routed `engine/combat.js` sites + slow | engine edits | 18-03 | parallel wave-2 plan |
| rebalanced `content/bestiary.js` + pins | content + test | 18-05 | parallel wave-2 plan |
| `tools/bestiary-yardstick.mjs`, `content/BESTIARY-REBALANCE.md`, invariant test | tooling / doc / test | 18-01, 18-06 | — |

## Assumptions locked by this plan

- Item fire damage is PHYSICAL (18-RESEARCH A3): a thrown flask/staff bolt is not a cast spell; D-06 names spells as the only bypass. Never multiplied (not caster-typed).
- The insane r=2 blow is `kind: "foe"` (a foe hitting a foe): physical, soakable by the VICTIM's `sp.ar`, never multiplied (no player caster identity applies).
- `earthquake.amount` stays the single rolled base `d` (the spell's own roll), because the event describes the quake, not a per-foe result; per-foe applied values are visible on each foe's wp and via `foeKilled`. `volley.totalDamage`, `spellHit.dmg`, `insaneStruckAlly.dmg`, `itemBurned.total` switch to applied values (equal to raw for every foe without ar/halfDmg/multiplier, so no existing assertion changes).
- Acid's damage is applied at TICK time in `engine/combat.js` (18-03); `magic.js`'s acid branch only stores `{ rounds, dmg }` on the foe and is untouched here.
- Freeze: the thrown branch calls the seam first (so a Freeze bolt still reports applied damage in `spellHit`), then the existing `alive = false / frozen / wp = 0` instant-removal block runs unchanged.

<tasks>

<task type="auto">
  <name>Task 1: Route quake / volley / insane r=2 / thrown in magic.js and the fire effect in items.js through damageFoe</name>
  <files>engine/magic.js, engine/items.js</files>
  <read_first>
    - engine/foeDamage.js (18-02 output — signature, source vocabulary, return shape, caller contract in the JSDoc)
    - engine/magic.js lines 22-28 (import block; `killFoe`/`liveFoes`/`afterPlayerAction` come from ./combat.js), 165-179 (quake: `mult`, `d`, the per-foe forEach with the direct decrement + kill check, the earthquake event, the caster self-damage block which is NOT foe damage), 194-204 (volley: `n`, the per-ball loop with the direct decrement, `tot += d`, kill check), 269-297 (insane: r=2 branch with `o`, `d = t.lvl*t.lvl + rng.d(6)`, the direct decrement, insaneStruckAlly, kill check; r=1/3/6 are wp SETS — leave them), 325-357 (thrown: `bonus`, `targets`, per-target roll, `dmg = rollDice * mult + eff(c,"spellDmg")`, the direct decrement, spellHit, the freeze instant-removal block, the kill check + continue)
    - engine/items.js lines 18-31 (import block and the circular-import note — damageFoe's import is NOT circular since foeDamage.js imports only content), 586-598 (fire: `n = rng.d(6)`, per-ball `dmg = rng.d(10) + 4`, the direct decrement, `tot += dmg`, kill check, itemBurned)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md "Pattern 2" (magic.js and items.js insertion notes) and the architecture diagram's "Excluded from the seam" list
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md D-06, D-09, D-10, D-11
  </read_first>
  <action>
    In `engine/magic.js` add `import { damageFoe } from "./foeDamage.js";` beside the other `./` imports and route four sites, each with a one-line comment naming the source kind and the canon rule it enables:
    - quake: inside the `liveFoes(state).forEach((f) => ...)` replace the direct decrement of `f`'s wp with `damageFoe(state, f, d, { kind: "spell", school: sp.kind, casterSub: c.sub }, rng, events);` and keep `if (f.wp <= 0) killFoe(state, f, rng, events);` right after it. Leave `events.push({ type: "earthquake", amount: d })` and the caster self-damage block untouched (comment: the event reports the quake's single roll; per-foe multipliers/halving show on each foe).
    - volley: replace the direct decrement of `t`'s wp with `const hit = damageFoe(state, t, d, { kind: "spell", school: sp.kind, casterSub: c.sub }, rng, events);` and change the accumulator to `tot += hit.applied;`; keep the kill check.
    - insane r=2: replace the direct decrement of `o`'s wp with `const hit = damageFoe(state, o, d, { kind: "foe", crit: false }, rng, events);`, push `insaneStruckAlly { target: o.name, dmg: hit.applied }` only when `!hit.soaked`, keep `if (o.wp <= 0) killFoe(state, o, rng, events);`. Comment: a maddened foe's blow on its neighbour is physical — the victim's own natural armor may soak it (gated d20, D-05), no multiplier row applies (no player caster).
    - thrown: replace the direct decrement of `t`'s wp with `const hit = damageFoe(state, t, dmg, { kind: "spell", school: sp.kind, casterSub: c.sub }, rng, events);` and set `spellHit`'s `dmg` to `hit.applied` (keep `mult` in the event as the level multiplier it already is); the freeze block and the `if (t.wp <= 0) { killFoe(...); continue; }` stay exactly where they are.
    Leave every direct wp SET alone (shrink, petrify, turn, gate, insane r=1/3/6, vapor, freeze) and every caster-side `c.wp` decrement alone.

    In `engine/items.js` add `import { damageFoe } from "./foeDamage.js";` next to the `killFoe` import (extend the circular-import comment with one sentence: foeDamage.js imports only content, so this edge is acyclic). In the `"fire"` case replace the direct decrement of `t`'s wp with `const hit = damageFoe(state, t, dmg, { kind: "item", crit: false }, rng, events);` and `tot += hit.applied;`; keep the kill check and the `itemBurned { total: tot }` event. Comment: item damage is physical (soakable by sp.ar, 18-RESEARCH A3), never multiplied.
  </action>
  <verify>
    <automated>node --test test/unit/magic.test.js test/unit/items.test.js test/unit/item-wiring.test.js test/unit/engine-purity.test.js test/unit/formatEventsCoverage.test.js && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^import { damageFoe } from "./foeDamage.js";' engine/magic.js` prints 1 and `grep -c '^import { damageFoe } from "./foeDamage.js";' engine/items.js` prints 1
    - `grep -v '^\s*//\|^\s*\*' engine/magic.js | grep -Ec '\b(t|f|o|foe)\.wp -= '` prints 0 and `grep -v '^\s*//\|^\s*\*' engine/items.js | grep -Ec '\b(t|f|o|foe)\.wp -= '` prints 0
    - `grep -v '^\s*//\|^\s*\*' engine/magic.js | grep -c 'c\.wp -= '` prints 3 (quake self-damage, backfire, death spell — untouched)
    - `grep -c 'kind: "spell", school: sp.kind, casterSub: c.sub' engine/magic.js` prints 3 (quake, volley, thrown) and `grep -c 'kind: "foe", crit: false' engine/magic.js` prints 1
    - `grep -c 'kind: "item", crit: false' engine/items.js` prints 1 and `grep -c 'tot += hit.applied' engine/items.js` prints 1 and `grep -c 'tot += hit.applied' engine/magic.js` prints 1
    - `grep -c 'events.push({ type: "earthquake", amount: d })' engine/magic.js` prints 1 (unchanged) and `grep -c 'f.wp = Math.ceil(f.wp / 2)' engine/magic.js` prints 1 (shrink untouched)
    - `grep -c 'killFoe(state, f, rng, events)' engine/magic.js` prints at least 2 and `grep -c 'if (o.wp <= 0) killFoe(state, o, rng, events);' engine/magic.js` prints 1 and `grep -c 'if (t.wp <= 0) killFoe(state, t, rng, events);' engine/items.js` prints 1
    - `grep -v '^\s*//\|^\s*\*' engine/foeDamage.js | grep -c '^import'` prints 1 and `grep -c 'from "../content/index.js"' engine/foeDamage.js` prints 1 (seam still imports only content — no cycle)
    - `node --test test/unit/magic.test.js test/unit/items.test.js test/unit/item-wiring.test.js` exits 0 with every existing test passing unmodified
    - `node --test "test/parity/**/*.test.js"` exits 0 (30 pass) and `git status --porcelain test/parity` prints nothing
  </acceptance_criteria>
  <done>All four magic.js damage sites and the items.js fire effect call damageFoe with typed sources; wp SETS and caster self-damage are untouched; kill checks are in place; every existing magic/items test and the byte-identical parity suite pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add CANON-04 / CANON-03 / D-06 / A3 behavioral tests for the routed spell and item sites</name>
  <files>test/unit/magic.test.js, test/unit/item-wiring.test.js</files>
  <read_first>
    - test/unit/magic.test.js lines 1-110 (imports, `fakeRng`, `fixedWizard` (Magic User / Wizard, L1, Dagger), full-size `fixedFloor`, `fixedState`, `fixedFoe`, `fixedCombat`, `SPELL_IDX` lookup) and lines 152-166 (Fireball test — the exact 6-draw sequence `[1, 5, 5, 1, 1, 20]` and its comment), 208-227 (Earthquake test — `[10, 10, 10, 1, 1, 20]`, mult = max(1, 4-4) = 1 → d = 38)
    - engine/magic.js lines 45-100 after Task 1 (charge check, canCast/grimoire gate, Apprentice backfire only for sub Apprentice, the intelligent-target resistance roll — fires only when the first live foe's intel >= 12 and the kind is not thrown/ward/... — so keep foe intel at 1)
    - engine/derived.js lines 413-437 (schoolBonus: Wizard offense 3, Cleric offense 0; canCast requires grimoire + level >= sp.lvl + level >= schoolGate) and content/mu-chart.js Cleric row (offense 0 — Clerics CAN cast offense spells)
    - content/spells.js lines 40-60 (Fireball lvl 3 thrown 2d10+4; Fireballs lvl 4 volley 1d10+2; Earthquake lvl 4 quake 3d10+8; Insane lvl 2 insane; Lightning lvl 4 thrown d10+6)
    - test/unit/item-wiring.test.js lines 20-100 (`fakeRng`, `fixedFighter`, `fixedState`, `fixedFoe`, `fixedCombat`) and 210-222 (useItem with a combat fixture); content/treasure-tables.js line 42 (Pine Staff `{ n: "Pine Staff", use: "fire", txt: ... }`)
    - engine/combat.js afterPlayerAction (lines 674-724) — after a non-lethal cast: allyTurn (no ally), alliesTurn (no allies), foeTurn (one d20 per live foe swing; 7 = miss vs need 5), then rollInitiative (two d20; 15 vs 10 → "you", no second foe turn)
  </read_first>
  <action>
    In `test/unit/magic.test.js` add a section `// --- Phase 18: damageFoe routing (CANON-04 / CANON-03 / D-06) ---`. Every foe uses `intel: 1` (no resistance roll) and the trailing afterPlayerAction draws are: one d20 per live foe (7 → miss), then initiative 15, 10. Sequences traced by the planner against the post-Task-1 code:
    1. "castSpell: a Cleric's Fireball deals double to a Demons foe (CANON-04, D-11)" — c `{ cls: "Magic User", sub: "Cleric", grimoire: ["Fireball"], level: 3 }`, foe `{ type: "Demons", wp: 40, maxWP: 40, intel: 1 }`; Cleric offense bonus is 0 so the d8 roll 1 hits (1 - 0 <= 4); dmg (5+5+4) x max(1, 3-3) = 14 → x2 = 28. `fakeRng([1, 5, 5, 7, 15, 10])` → `spellHit.dmg === 28`, `foe.wp === 12`, no foeArmorSoaked, exactly 6 draws.
    2. "castSpell: a Wizard's Fireball does NOT double against Demons (Cleric-only row)" — same but sub Wizard (bonus 3; roll 1 - 3 <= 4 hits): spellHit dmg 14, wp 26.
    3. "castSpell: any caster's Fireball doubles against Walking Dead (magic x2, D-11)" — Wizard, foe `{ type: "Walking Dead", wp: 40 }` → spellHit dmg 28, wp 12.
    4. "castSpell: a spell never draws the armor soak (D-06) — Fireball vs sp.ar 15 lands in full" — Wizard, foe `{ type: "Humans", sp: { ar: 15 }, wp: 40 }`, same 6-draw sequence → spellHit dmg 14, wp 26, no foeArmorSoaked; a 7th draw would have thrown.
    5. "castSpell: Earthquake is applied per foe — Walking Dead takes 2x, Humans 1x; earthquake.amount stays the rolled 38" — c `{ sub: "Wizard", grimoire: ["Earthquake"], level: 4, wp: 50, maxWP: 50, ward: null }`, foes `[ { type: "Walking Dead", wp: 100, maxWP: 100 }, { type: "Humans", wp: 100, maxWP: 100 } ]` (both intel 1); `fakeRng([10, 10, 10, 7, 7, 15, 10])` → Walking Dead wp 24, Humans wp 62, `earthquake.amount === 38`, `earthquakeSelfDamage.amount === 19`, `c.wp === 31`.
    6. "castSpell: Fireballs (volley) totals APPLIED damage — each ball on a halfDmg foe is ceil-halved (CANON-03)" — c `{ sub: "Wizard", grimoire: ["Fireballs"], level: 4 }`, foe `{ sp: { halfDmg: true }, wp: 50, maxWP: 50, intel: 1, type: "Humans" }`; `fakeRng([2, 5, 3, 7, 15, 10])` → balls (5+2)=7 → 4 and (3+2)=5 → 3; `volley` event `{ rolls: 2, totalDamage: 7 }`, wp 43.
    7. "castSpell: Insanity r=2 — the foe-on-foe blow is physical and can be soaked by the victim's natural armor (one gated d20, no insaneStruckAlly)" — c `{ sub: "Wizard", grimoire: ["Insane"], level: 2 }`, foes `[ { name: "A", type: "Beasts", lvl: 1, wp: 10, intel: 1 }, { name: "B", type: "Beasts", sp: { ar: 12 }, wp: 10, intel: 1 } ]`, `C.target = 0`; `fakeRng([2, 4, 5, 7, 7, 15, 10])` → d6 = 2 (r=2), d = 1 + 4 = 5, soak 5 <= 12 → events include `{ type: "foeArmorSoaked", name: "B", amount: 5 }`, no insaneStruckAlly, B wp 10; control without ar: `fakeRng([2, 4, 7, 7, 15, 10])` → insaneStruckAlly dmg 5, B wp 5.
    In `test/unit/item-wiring.test.js` add `// --- 9. Phase 18: fire item routed through damageFoe (CANON-01 A3) ---`: c `{ items: [{ n: "Pine Staff", use: "fire" }] }`, combat `fixedCombat([fixedFoe({ sp: { ar: 12 }, wp: 30, maxWP: 30 })])`; `useItem(state, 0, fakeRng([1, 6, 3]), [])` → one ball (d6 = 1), dmg 6 + 4 = 10, soak 3 <= 12 → events include foeArmorSoaked amount 10 and `itemBurned.total === 0`, foe wp 30, exactly 3 draws; control foe without ar: `fakeRng([1, 6])` → itemBurned total 10, wp 20.
  </action>
  <verify>
    <automated>node --test test/unit/magic.test.js test/unit/item-wiring.test.js test/unit/items.test.js && node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CANON-04' test/unit/magic.test.js` prints at least 1 and `grep -c 'foeArmorSoaked' test/unit/magic.test.js` prints at least 2 and `grep -c 'foeArmorSoaked' test/unit/item-wiring.test.js` prints at least 1
    - `node --test test/unit/magic.test.js` exits 0 reporting >= 7 more tests than before this plan, 0 failed, with every pre-existing test unmodified (`git diff -U0 e01ac46 -- test/unit/magic.test.js | grep -c '^-.*assert'` prints 0)
    - `node --test test/unit/item-wiring.test.js` exits 0 with >= 1 new test and `git diff -U0 e01ac46 -- test/unit/item-wiring.test.js | grep -c '^-.*assert'` prints 0
    - `node --test "test/parity/**/*.test.js"` exits 0 (30 pass) and `git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt package.json package-lock.json` exits 0
    - `npm test` exits 0 with 0 failing tests
  </acceptance_criteria>
  <done>Real casts prove Cleric-vs-Demons and spell-vs-Walking-Dead doubling, per-foe Earthquake application, halfDmg volley totals, the D-06 spell bypass, and the soakable foe-on-foe and item-fire blows; the parity suite is byte-identical and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| castSpell / useItem → `damageFoe` | Internal boundary; `idx`/`i` are validated upstream by `validateAction` (LO-01) before these handlers run |
| save file → state | Unchanged; acid keeps `{ rounds, dmg }`, no caster identity stashed (D-17) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-02 | Tampering | A spell path reaching the soak d20 (unconditional extra draw) would desync the magic-parity fixture | high | mitigate | Seam gates on `kind !== "spell"`; test 4 proves zero extra draws for a spell vs an ar foe; `node --test "test/parity/**/*.test.js"` byte-identical |
| T-18-01 | Tampering | Frozen master / fixtures / comparables edited | high | mitigate | Prohibited; `git diff --quiet e01ac46 -- test/parity/...` in acceptance |
| T-18-03 | Denial of Service | Re-ordered kill checks leaving a dead foe alive (or double-killing) in AoE loops | medium | mitigate | Kill checks stay adjacent to each seam call (acceptance greps); per-foe Earthquake and volley tests exercise the loops |
| T-18-04 | Elevation of Privilege | Import cycle or DOM/Math.random creeping into magic.js/items.js | low | mitigate | `engine-purity.test.js`; acceptance asserts foeDamage.js imports only content |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; manifests unchanged (acceptance) |
</threat_model>

<verification>
- `node --test test/unit/magic.test.js test/unit/item-wiring.test.js test/unit/items.test.js` green with all pre-existing tests unmodified plus >= 8 new.
- No foe-side direct wp decrement remains on a code line in `engine/magic.js` or `engine/items.js`; three `c.wp -=` caster decrements remain in magic.js.
- `node --test "test/parity/**/*.test.js"` byte-identical; `npm test` green; frozen files unchanged since e01ac46.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 3's magic half holds through real casts: Cleric spells deal 2x to Demons, magic deals 2x to Walking Dead, Sterling-style halving applies to spell damage — each by a dedicated test — and spells bypass foe armor per D-06.
- Every non-hero physical source (item fire, foe-on-foe) is routed, leaving zero direct foe-wp decrements outside the seam for 18-06's invariant test.
</success_criteria>

<output>
Create `.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-04-SUMMARY.md` when done (record the five routed sites with source kinds and the event payload decisions).
</output>
