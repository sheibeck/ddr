---
phase: 18-bestiary-rebalance-canon-combat-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/bestiary-yardstick.mjs
  - test/unit/bestiary-yardstick.test.js
  - content/BESTIARY-REBALANCE.md
autonomous: true
requirements: [BEST-01, BEST-02, FID-05]
tags: [content, bestiary, balance, tooling, yardstick, node-test]

must_haves:
  truths:
    - "BEST-01 (D-01): tools/bestiary-yardstick.mjs computes, for every one of the 53 bestiary rows, the composite level-N hero's expected damage per swing (E), time-to-kill (TTK = wp / E, x2 for sp.twice), the foe's expected damage per round (foeDPR = atk x (tier^2 + avg(sp.dmg) or 3.5) x 6/foeDie) and rounds-to-die (RTD = heroHP[tier] / foeDPR), plus per-tier medians and ratios — the same formulas 18-RESEARCH.md's Rebalance Yardstick section specifies, reproduced to within 0.01 on every row"
    - "BEST-01 boundary (edge probe): the outlier flag is STRICT — ttkRatio > 2.0 or lethalityRatio (medianRTD / RTD) > 2.0 flags 'over-tier'; a row at exactly 2.00 is NOT flagged (Werebeast's TTK 4.67 / 2.34 = 2.00 is unflagged on the TTK axis and flagged only on lethality 2.60)"
    - "BEST-01 adjacency (edge probe): rows touching the threshold — Cave Bear TTK 2.08x, Philly TTK 2.00x (prototype mode), Herman T4 lethality 2.10x, Sterling TTK 2.19x — appear in content/BESTIARY-REBALANCE.md's Review Verdicts table each with an explicit disposition (not silently absorbed into 'unchanged')"
    - "BEST-01 empty (edge probe): a bestiary entry with NO sp object at all (Dread Lock) and entries with sp but no sp.dmg (Shriek, Goblin, Djinni ...) compute without throwing, using atk 1 and the engine's d6 fallback average 3.5 — exactly what engine/combat.js foeTurn does at runtime"
    - "BEST-01 ordering (edge probe): rows are emitted in BESTIARY iteration order (Object.entries type order, then tier index 0..4, then array order) so two runs on the same content are byte-identical and the markdown block is diffable across commits"
    - "BEST-01 precision (edge probe): TTK, RTD, E, foeDPR and every ratio are printed with exactly 2 decimals via toFixed(2); medians are computed on the unrounded values; Infinity (magicOnly foes vs an unenchanted hero) prints as 'inf' and is EXCLUDED from the tier median"
    - "BEST-01 / D-16: content/BESTIARY-REBALANCE.md records the tune-difficulty readout (death-depth min/p50/p90/max, action-count distribution, outcome line, top-5 death causes; --seeds=200) captured against the UNMODIFIED phase-start tree (e01ac46) as the BEFORE signal, labelled informational and explicitly 'not a gate'"
    - "BEST-01 / D-02 / D-18 / D-19 / D-14 / D-03: the doc's Review Verdicts table lists every row whose over-tier flag fires in prototype mode (Dante, Cave Bear, Sterling, Werebeast, Drake, Djinni x2, Stalka Beast, Vampire, Herman T4) plus the D-03 discount targets (Krupke, Drudge x2) with the planned disposition each will receive in 18-05, and lists every under-tier row (ratio < 0.5) with the disposition 'under-tier — no buff in a conservative pass, Phase 21' (D-02 direction: this phase corrects unfairly hard fights; making trivial creatures harder is a buff, not a conservative fix)"
    - statement: "BEST-02: the doc's pre-ability discount section states the rounding rule Math.round(wp x 0.75) and the dice-step ladder d12 -> d10 -> d8 -> d6 -> d4 (a creature with no sp.dmg sits on the engine's d6 fallback, so one step down = an authored d4) and lists the exact planned numbers for Djinni x2 / Krupke / Drudge x2 / Vampire / Stalka Beast"
      verification: backstop
    - "FID-05: the doc is the phase's required before/after artifact — after 18-01 it carries the complete BEFORE table (mechanics=prototype, 53 rows) and states that ZERO parity fixtures are regenerated because the four fixture-exposed rows (Bat/Rat, Shriek, Viper, Dante — test/parity/FIXTURE-INVENTORY.md) are untouched (D-14/D-15)"
  artifacts:
    - path: "tools/bestiary-yardstick.mjs"
      provides: "Zero-dependency ESM CLI + exported pure functions computeYardstick(BESTIARY, { mechanics }) and toMarkdown(result); flags --mechanics=prototype|canon (default canon), --json, --bestiary=<path>"
      exports: ["computeYardstick", "toMarkdown"]
      min_lines: 120
    - path: "test/unit/bestiary-yardstick.test.js"
      provides: "node:test coverage of the yardstick: synthetic mini-bestiary exact pins (formula, strict >2.0 flag, twice, toHit/fast/magicOnly, empty sp, canon-mode ar/slow/halfDmg), live-bestiary smoke (53 rows, prototype-mode tier medians pinned +-0.01), ordering + precision contracts"
      contains: "computeYardstick"
    - path: "content/BESTIARY-REBALANCE.md"
      provides: "D-04 stat-table document: method, D-02 rule + direction, tune-difficulty BEFORE readout (D-16), generated BEFORE table between yardstick:before markers, Review Verdicts (planned dispositions), pre-ability discount numbers (D-03), parity carve-out statement (zero), and placeholder headings for the AFTER sections 18-06 fills"
      contains: "yardstick:before:begin"
  key_links:
    - from: "tools/bestiary-yardstick.mjs"
      to: "content/bestiary.js"
      via: "dynamic import(pathToFileURL(resolved --bestiary path or content/bestiary.js)).BESTIARY — the script reads the LIVE table, never a hand-copied one"
      pattern: "pathToFileURL"
    - from: "content/BESTIARY-REBALANCE.md"
      to: "tools/bestiary-yardstick.mjs"
      via: "the BEFORE block is the script's --mechanics=prototype markdown output pasted verbatim between the yardstick:before:begin / :end marker comments; the doc's How-to-regenerate section names the exact command"
      pattern: "bestiary-yardstick.mjs --mechanics=prototype"
    - from: "test/unit/bestiary-yardstick.test.js"
      to: "tools/bestiary-yardstick.mjs"
      via: "imports computeYardstick/toMarkdown (the CLI main is guarded so importing does not print)"
      pattern: "from \"../../tools/bestiary-yardstick.mjs\""
  prohibitions:
    - requirement_id: FID-05
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any test/parity/fixtures/*.json, or test/parity/harness/comparables.js — this plan is tooling + documentation only"
    - requirement_id: BEST-01
      category: values
      statement: "MUST NOT change any number in content/bestiary.js in this plan — the BEFORE snapshot must be captured against the unmodified tree; number changes belong to 18-05"
    - requirement_id: BEST-01
      category: transparency
      statement: "MUST NOT present the yardstick as a pass/fail gate or the tune-difficulty readout as a balance proof — both are informational (D-16); the doc labels them so"
    - requirement_id: BEST-01
      category: safety
      statement: "MUST NOT add npm dependencies; the script and test use only node: builtins and ESM imports from content/"
---

<objective>
Build the committed yardstick — `tools/bestiary-yardstick.mjs` — that reproduces 18-RESEARCH.md's 53-row TTK/RTD table from the live `content/bestiary.js` (per D-01), pin its formulas with a unit test, and start `content/BESTIARY-REBALANCE.md` (D-04) with the methodology, the D-02 outlier rule and its direction, the tune-difficulty BEFORE readout (D-16), the generated BEFORE table, and the Review Verdicts table that names every flagged row and the disposition it will receive.

Purpose: BEST-01 requires every creature reviewed against its tier with outliers fixed and a before/after table committed. The review has to be reproducible (Phase 21 re-runs it for the consolidated retune), and the BEFORE half must be captured before 18-05 changes any number. The script also models the canon mechanics this phase lands (foe armor soak, slow, halfDmg) so 18-06's AFTER table shows their real effect — including the CANON-01 tankiness jump on the five `sp.ar` creatures, which the table must record as canon-intended (D-19 precedent) rather than hide.

Output: `tools/bestiary-yardstick.mjs`, `test/unit/bestiary-yardstick.test.js`, `content/BESTIARY-REBALANCE.md` (BEFORE half + verdicts; AFTER headings left for 18-06).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-PATTERNS.md
@content/bestiary.js
@content/misc-tables.js
@tools/tune-difficulty.mjs
@tools/fixture-inventory.mjs
@test/parity/FIXTURE-INVENTORY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `tools/bestiary-yardstick.mjs` — `computeYardstick(BESTIARY, { mechanics })`, `toMarkdown(result)` | CLI script + exported pure functions | **18-01** | zero rng, zero deps; `--mechanics=prototype\|canon`, `--json`, `--bestiary=<path>` |
| `test/unit/bestiary-yardstick.test.js` | test file | **18-01** (extended by 18-06 with the doc-consistency check) | synthetic pins + live smoke |
| `content/BESTIARY-REBALANCE.md` | committed stat-table doc (D-04) | **18-01** (BEFORE + verdicts), 18-06 (AFTER + ledger) | referenced from `content/bestiary.js` header (18-05) |
| `content/damage-multipliers.js` — `DAMAGE_MULTIPLIERS` | pure content table | 18-02 | 3 rows, re-exported from `content/index.js` |
| `engine/foeDamage.js` — `damageFoe(state, foe, rawDmg, source, rng, events)`, `multiplierFor(source, foe)` | engine module (the seam) | 18-02 | returns `{ applied, soaked, mult }` |
| `foeArmorSoaked { name, amount }` event + `EVENT_NARRATION.foeArmorSoaked` | event type + narration | 18-02 | D-08 |
| `test/unit/foe-damage.test.js` | test file | 18-02 (+18-06 invariant test) | dedicated fakeRng tests per D-13 |
| routed call sites in `engine/combat.js` (5), `engine/magic.js` (4), `engine/items.js` (1) + Philly `slow` re-roll | engine edits | 18-03, 18-04 | CANON-01/03/04/05 |
| rebalanced `content/bestiary.js` rows + `content-tables.test.js` pins | content + test | 18-05 | D-03, D-17, D-18, D-19 |

<tasks>

<task type="auto">
  <name>Task 1: Write tools/bestiary-yardstick.mjs (pure TTK/RTD calculator with prototype/canon mechanics modes) and its unit test</name>
  <files>tools/bestiary-yardstick.mjs, test/unit/bestiary-yardstick.test.js</files>
  <read_first>
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md sections "Rebalance Yardstick (BEST-01, BEST-02) — Methodology and Full Table" (hero-side formulas, foe-side formulas, the 53-row table with tier medians) and "Pre-Ability Discount (D-03) — Concrete New Numbers" (rounding + dice ladder)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md D-01, D-02, D-05, D-07, D-10, D-12, D-16, D-18, D-19 and "Claude's Discretion" (committed script preferred)
    - content/bestiary.js (whole file — the row shapes: `n`, `sz`, `i`, `wp`, optional `sp` with `atk`, `dmg {n,sides,bonus}`, `toHit`, `fast`, `magicOnly`, `twice`, `ar`, `slow`, `halfDmg`; note Dread Lock has NO `sp` at all)
    - content/misc-tables.js line 13 (`STRIKE_DICE = [20, 12, 10, 8, 6]`)
    - engine/derived.js lines 303-307 (`foeDie` = max(8, STRIKE_DICE[lvl-1]) → [20,12,10,8,8] for tiers 1..5) and engine/combat.js line 1039 (foe damage = lvl^2 + rollDice(sp.dmg) or d6 fallback — the 3.5 average)
    - tools/tune-difficulty.mjs lines 1-30 and 230-260 (header convention: "dev-only, zero-dependency, NOT a node:test file"; argv parsing style; `--json` flag) and tools/fixture-inventory.mjs (the shorter sibling; import style)
    - test/unit/foe-turn-draw-count.test.js lines 1-40 (header prose style for a pinned-numbers test) and test/unit/content-tables.test.js lines 1-30 (import + assertion style)
  </read_first>
  <action>
    Create `tools/bestiary-yardstick.mjs` as a dev-only, zero-dependency Node ESM script in the exact header style of `tools/tune-difficulty.mjs` (state: not shipped, not a node:test file, informational per D-16 — not a gate; cite D-01/D-02 and 18-RESEARCH.md as the formula source). It must import nothing from `engine/` and nothing third-party: only `node:path`/`node:url`/`node:process` plus a dynamic `import(pathToFileURL(...).href)` of the bestiary module so `--bestiary=<path>` can point at an alternate copy (used for historical BEFORE runs, e.g. a file produced by `git show e01ac46:content/bestiary.js`). Default module: `content/bestiary.js` resolved relative to the script's own location, never relative to cwd.

    Export two pure functions and guard the CLI main behind `if (import.meta.url === pathToFileURL(process.argv[1]).href)` so tests can import without printing:

    `computeYardstick(BESTIARY, { mechanics = "canon" } = {})` returns `{ mechanics, rows, tiers }`. Constants (document each with its 18-RESEARCH.md derivation): `STRIKE_DICE = [20,12,10,8,6]`; `FOE_DIE = [20,12,10,8,8]`; `HERO_HP = [41.7, 46.2, 50.0, 54.5, 60.0]`; `GENERIC_NEED = 4`; `heroAvgDmg(L) = L*L + 4.69`; `FOE_NEED = 5`. Per row (iterate `Object.entries(BESTIARY)` in insertion order, then tier index 0..4, then array order — this ordering is a contract): tier `L = tierIndex + 1`; `sp = entry.sp || {}`; effective hero need: start at GENERIC_NEED, `Math.min(need, sp.toHit)` when `sp.toHit !== undefined`, `Math.max(1, need - 1)` when `sp.fast`, and `sp.magicOnly` → need 0 (the composite hero carries no magic weapon). Hit probabilities on die `d = STRIKE_DICE[L-1]`: plain `pHit = need/d`, `pCrit = 1/d`; when `mechanics === "canon"` and `sp.slow`, use two-dice-keep-lower: `pHit = 1 - ((d-need)/d)^2`, `pCrit = 1 - ((d-1)/d)^2` (D-12). Armor factor: `arBlock = mechanics === "canon" && sp.ar > 0 ? sp.ar/20 : 0` (D-05). Expected hero damage per swing: `E = heroAvgDmg(L) * ((pHit - pCrit) * (1 - arBlock) + 2 * pCrit)` — non-crit hits are soaked with probability arBlock, the natural-1 crit is doubled and bypasses the soak (D-07); when `need === 0`, E = 0. If `mechanics === "canon"` and `sp.halfDmg`, `E = E / 2` (D-10; the ceil is ignored in expectation — state this). `ttk = E > 0 ? (entry.wp / E) * (sp.twice ? 2 : 1) : Infinity`. Foe side: `atk = sp.atk || 1`; `avgDice = sp.dmg ? sp.dmg.n * (sp.dmg.sides + 1) / 2 + (sp.dmg.bonus || 0) : 3.5`; `foeDPR = atk * (L*L + avgDice) * (FOE_NEED + 1) / FOE_DIE[L-1]`; `rtd = HERO_HP[L-1] / foeDPR`. Row object: `{ tier, type, name, wp, atk, dmg (the notation or null), toHit, ar, flags (array of the sp keys among twice/slow/halfDmg/magicOnly/fast/caster/never_melee that are truthy), E, ttk, foeDPR, rtd }`. Per tier: `medianTTK` over FINITE ttk values only and `medianRTD` over all rows (standard median: middle value or mean of the two middle values); then per row `ttkRatio = ttk / medianTTK` (Infinity stays Infinity), `lethality = medianRTD / rtd`, `flag`: `"over-tier"` when `ttkRatio > 2.0 || lethality > 2.0` (STRICT greater-than — 2.00 exactly is not flagged), `"under-tier"` when `ttkRatio < 0.5 || lethality < 0.5`, `"infinite"` when ttk is Infinity, else `""`. `tiers` is `{ [tier]: { medianTTK, medianRTD, rows: n } }`.

    `toMarkdown(result)` returns one markdown table (no surrounding headings) with a header line naming the mechanics mode and the per-tier medians, then columns `Tier | Type | Creature | wp | atk | dmg | toHit | ar | flags | heroE | TTK | xTTKmed | foeDPR | RTD | xLethal | Flag`, every number via `toFixed(2)`, Infinity printed as `inf`, dmg printed as `NdS+B` (e.g. `1d8+2`, `0d0+25` for a flat 25, `d6*` for the fallback) and blank cells for absent toHit/ar. Deterministic: same input → byte-identical output.

    CLI main: parse `--mechanics=prototype|canon` (default `canon`; anything else → print usage to stderr and exit 1), `--json` (print `JSON.stringify(result, null, 2)`; Infinity must survive — replace with the string `"inf"` in a replacer), `--bestiary=<path>`. Default output is `toMarkdown(result)` followed by a trailing newline. Usage examples in the header: `node tools/bestiary-yardstick.mjs --mechanics=prototype` (the BEFORE table), `node tools/bestiary-yardstick.mjs` (AFTER, canon), `node tools/bestiary-yardstick.mjs --json`.

    Create `test/unit/bestiary-yardstick.test.js` (`node:test` + `node:assert/strict`, importing `computeYardstick`/`toMarkdown` from `../../tools/bestiary-yardstick.mjs` and `BESTIARY` from `../../content/index.js`). Use a SYNTHETIC mini-bestiary for exact pins (so 18-05's number changes cannot break this file) plus a live smoke section:
    1. "formula: a plain tier-1 foe (wp 10, no sp)" — one type, tier 1 only (pad tiers 2-5 with empty arrays): E = 5.69 * 5/20 = 1.4225, TTK 7.03 (+-0.01), foeDPR = 1 * (1 + 3.5) * 6/20 = 1.35, RTD = 41.7/1.35 = 30.89 (+-0.01); atk defaults to 1; dmg null.
    2. "twice doubles TTK; toHit lowers the effective need; fast lowers it by one; magicOnly gives Infinity and 'infinite' flag and is excluded from the tier median" — four synthetic rows in one tier; assert each contract, and that `tiers[1].medianTTK` is finite.
    3. "flag is strict: a row at exactly 2.00x the tier median is NOT flagged; 2.01x is" — construct three rows whose wp are chosen so one lands exactly at 2.00 x median (e.g. wp 10, 10, 20 with identical sp → medians from the two 10s → the 20 row is exactly 2.00) and assert `flag === ""` for it; then a fourth mini-bestiary with wp 10, 10, 21 → the 21 row flags `"over-tier"`.
    4. "under-tier: ttkRatio < 0.5 flags 'under-tier'" — wp 10, 10, 4 → the 4 row is `"under-tier"`.
    5. "canon mechanics: ar 12 scales non-crit damage by 0.4 while the crit share is untouched" — tier-3 row (d10, need 4): prototype E = 13.69 * 5/10 = 6.845; canon with ar 12: E = 13.69 * ((0.4 - 0.1) * 0.4 + 0.2) = 13.69 * 0.32 = 4.3808 (+-0.001); `mechanics: "prototype"` ignores ar entirely (E 6.845).
    6. "canon mechanics: halfDmg halves E; slow uses two-dice-keep-lower" — tier-1 halfDmg row: canon E = 0.71125; tier-1 slow row (d20, need 4): pHit = 1 - (16/20)^2 = 0.36, pCrit = 1 - (19/20)^2 = 0.0975 → E = 5.69 * (0.2625 + 0.195) = 2.603 (+-0.01); in prototype mode both rows equal the plain 1.4225.
    7. "ordering: rows follow Object.entries type order, then tier, then array order" — two types with two tiers each; assert `rows.map(r => r.name)` deepEqual the expected sequence; and `toMarkdown` of the same input twice is byte-identical.
    8. "precision: toMarkdown prints two decimals and 'inf'" — regex-check a rendered synthetic table: every numeric cell matches `/^-?\d+\.\d{2}$/` or `inf`, and the flat-25 dmg renders as `0d0+25`.
    9. "live bestiary smoke (prototype mode): 53 rows; only Ghost and Spectre are infinite; prototype tier medians are T1 3.51/30.89, T2 3.31/10.87, T3 2.34/5.56, T4 1.43/3.73, T5 1.62/2.81 (+-0.01 each)" — these medians are stable across this phase's rebalance (verified by the planner: none of the 18-05 changes moves a tier's middle two TTK or RTD values), so this pin is safe to run before and after 18-05; also assert the four fixture-exposed rows exist with wp 1/3/3/20 (Bat/Rat, Shriek, Viper, Dante).
    10. "live bestiary smoke (canon mode): Philly's TTK drops below 4.0 (slow), Sterling's TTK exceeds 9.0 (halfDmg), and every sp.ar row (Drat, Krupke, Craig, Herman x2, Google) has a canon TTK strictly greater than its prototype TTK".
  </action>
  <verify>
    <automated>node --test test/unit/bestiary-yardstick.test.js && node tools/bestiary-yardstick.mjs --mechanics=prototype | head -5 && node tools/bestiary-yardstick.mjs --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r.rows.length!==53)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function computeYardstick' tools/bestiary-yardstick.mjs` prints 1 and `grep -c '^export function toMarkdown' tools/bestiary-yardstick.mjs` prints 1
    - `grep -v '^\s*//' tools/bestiary-yardstick.mjs | grep -c 'Math.random\|from "../engine'` prints 0 (pure content-only computation, no engine import)
    - `grep -c 'pathToFileURL' tools/bestiary-yardstick.mjs` prints at least 2 (module resolution + CLI main guard)
    - `grep -c '> 2.0\|> 2)' tools/bestiary-yardstick.mjs` prints at least 1 and `grep -c '>= 2.0' tools/bestiary-yardstick.mjs` prints 0 (strict threshold — the flag comparison is greater-than, never greater-or-equal)
    - `node tools/bestiary-yardstick.mjs --mechanics=prototype | grep -c '^| '` prints 55 (1 header row + 1 separator + 53 data rows) — the header/medians line above the table is not a `| ` line
    - `node tools/bestiary-yardstick.mjs --mechanics=prototype | grep 'Drake' | grep -c '7.30'` prints 1 (Drake's prototype xTTKmed) and `node tools/bestiary-yardstick.mjs --mechanics=prototype | grep 'Werebeast' | grep -c '2.60'` prints 1 (Werebeast's lethality ratio)
    - `node tools/bestiary-yardstick.mjs | grep 'Sterling' | grep -c '10.23'` prints 1 (canon halfDmg TTK) and `node tools/bestiary-yardstick.mjs | grep 'Philly' | grep -c '3.84'` prints 1 (canon slow TTK)
    - `node tools/bestiary-yardstick.mjs --mechanics=bogus; echo $?` prints a non-zero exit code
    - `node --test test/unit/bestiary-yardstick.test.js` exits 0 reporting >= 10 tests passed, 0 failed
    - `npm test` exits 0 (the new test file is auto-discovered; no other file changed)
    - `git status --porcelain content/bestiary.js test/parity package.json` prints nothing
  </acceptance_criteria>
  <done>The yardstick script reproduces 18-RESEARCH.md's table from the live bestiary in both mechanics modes, exposes pure functions, and its unit test pins formulas, the strict flag threshold, canon-mode modelling, ordering, precision, and the stable live tier medians.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the BEFORE snapshot (tune-difficulty + prototype-mode yardstick) and author content/BESTIARY-REBALANCE.md with method, verdicts, and the AFTER placeholders</name>
  <files>content/BESTIARY-REBALANCE.md</files>
  <read_first>
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md D-02, D-03, D-04, D-14, D-16, D-17, D-18, D-19 and "Specific Ideas" (required table columns: tier, type, before → after for wp / dmg / toHit / ar / atk, TTK / RTD, one-line reason)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md sections "Full 53-row table", "Sterling / CANON-03 interaction", "Cross-check against the rulebook", "Pre-Ability Discount (D-03)", Assumptions A1/A5/A6 and Open Question 1
    - test/parity/FIXTURE-INVENTORY.md (whole file — the doc style to mirror: purpose, how-to-regenerate, marker-delimited generated block, "what this means" constraints) and its "Parity-exposed bestiary surface" section (the four names)
    - tools/tune-difficulty.mjs lines 195-228 (the exact readout lines to transcribe: death-depth distribution, action-count distribution, death-cause breakdown, outcome line)
    - tools/bestiary-yardstick.mjs (Task 1 output — the markdown block shape)
  </read_first>
  <action>
    First confirm the tree is the unmodified phase start for everything that feeds the bot and the yardstick: `git diff --quiet e01ac46 -- content/bestiary.js engine/` must exit 0 (only this plan's tooling/test files may differ). Then capture the two BEFORE readouts: run `node tools/tune-difficulty.mjs --seeds=200` (takes ~5-6 minutes; capture stdout to the scratchpad) and `node tools/bestiary-yardstick.mjs --mechanics=prototype` (capture stdout).

    Create `content/BESTIARY-REBALANCE.md` (D-04) in the FIXTURE-INVENTORY.md house style with these sections, in order:
    1. Title + purpose paragraph: this is the committed before/after stat table BEST-01 requires; it is the artifact Phase 21's consolidated retune starts from; it is referenced from `content/bestiary.js`'s header (18-05 adds the reference).
    2. "Method (D-01)": tier N = fair fight for a level-N hero (foe level = clamp(min(hero level, depth), 1, 5)); the composite hero (average of MU/Fighter/Thief at chargen + average level-ups, Human, no store purchases, no skills; heroHP [41.7, 46.2, 50.0, 54.5, 60.0]; avgDmg L^2 + 4.69; need 4 on STRIKE_DICE) and the foe side (atk x (tier^2 + avg dice or 3.5) x 6/foeDie); TTK and RTD definitions; the simplifications (no player armor, no skills; `never_melee` is inert until Phase 19 so Drudge is scored as it actually fights today). Name the exact regeneration commands and the marker convention.
    3. "The outlier rule (D-02) and its direction": strict > 2.0x on TTK (too tanky) or lethality (medianRTD/RTD > 2.0, too deadly) flags over-tier; medians are per tier over finite TTKs; state explicitly that this conservative pass fixes OVER-tier outliers only — under-tier rows (ratio < 0.5, e.g. Bat/Rat, Zit, Shadow, Stink Bug, Wolf, Floater, Flube, Drarl T5) are recorded, not buffed, because D-02's intent ("keep prototype numbers everywhere else so Phase 21 retunes from a stable baseline") and every locked fix in this phase (D-03, D-18) move numbers DOWN; a buff pass is Phase 21's call.
    4. "tune-difficulty readout (D-16) — informational, not a gate": a BEFORE subsection transcribing the death-depth line, the action-count line, the outcome line and the top 5 death causes from the captured run (200 seeds, run against e01ac46), and an AFTER subsection containing only the placeholder line `_Filled by 18-06 after the rebalance and the canon modifiers land._`.
    5. "BEFORE — full table (mechanics=prototype, 53 rows)": the captured yardstick markdown pasted VERBATIM between `<!-- yardstick:before:begin -->` and `<!-- yardstick:before:end -->` marker lines, preceded by one sentence noting it was generated from `content/bestiary.js` at commit e01ac46 (prototype-actual mechanics: ar/slow/halfDmg inert).
    6. "Review verdicts (BEST-01)": a table with columns `Creature | Tier / Type | Prototype TTK (xmed) | Prototype RTD (xlethal) | Verdict | Planned change (18-05) | Reason` containing at least these rows, with these verdicts:
       - Dante (T1 Humans): 14.06 (4.00x) / 10.30 (3.00x) — DEFERRED to Phase 21 — fixture-exposed (D-14), no carve-out this phase.
       - Cave Bear (T2 Beasts): 6.90 (2.08x) / 10.87 (1.00x) — UNCHANGED, borderline — within the composite-hero approximation error (18-RESEARCH A1: single-digit %); noted for Phase 21; the research-resolved follow-ups (D-18) added only Drake and Werebeast.
       - Philly (T1 Walking Dead): 7.03 (2.00x) / 25.27 — UNCHANGED — exactly at the strict threshold; CANON-05 slow (18-03) lowers its canon TTK to 3.84 (1.09x).
       - Sterling (T3 Beasts): 5.11 (2.19x) / 5.38 — UNCHANGED wp 35 (D-19) — halfDmg (CANON-03) roughly doubles TTK to ~10.23 (4.38x): canon-intended two hearts — revisit in Phase 21.
       - Werebeast (T3 Magical): 4.67 (2.00x) / 2.14 (2.60x lethal) — FIX (D-18): sp.dmg 1d10+5 → 1d10+0, keep atk 2; expected RTD 2.87 (1.94x lethal); note text "two attacks at d10+5" → "two attacks at d10" so the note stays truthful (D-02's own contradiction clause).
       - Drake (T4 Beasts): 10.44 (7.30x) / 2.34 — FIX (D-18): wp 135 → 38; expected TTK 2.94 (2.05x — the ~2x band D-18 targets, not the median); stays the single tankiest tier-4 body (Herman 36); fire breath cooldown is CANON-02 / Phase 19.
       - Herman (T4 Humans): 2.78 (1.95x) / 1.77 (2.10x lethal) — UNCHANGED — flat 25 base damage is explicit rulebook text ("strikes as a level five"); its own TTK 1.95x makes the fight self-consistent (2-3 rounds either way); canon-mode note below.
       - Djinni (T4 and T5 Demons): 6.65 (4.65x) and 3.48 (2.15x) — FIX (D-03): wp 86 → 65, add sp.dmg 1d4+0; "pre-ability discount — revisit Phase 21".
       - Krupke (T2 Humans): 6.35 (1.92x) / 8.80 — FIX (D-03, not itself an outlier): wp 23 → 17, sp.dmg 1d8+2 → 1d6+2; pre-ability discount.
       - Drudge (T4 and T5 Magical): 0.93 / 0.49 — FIX (D-03, HP only): wp 12 → 9; never melees per canon so no dice step.
       - Vampire (T5 Walking Dead): 3.84 (2.38x) / 1.40 (2.00x) — FIX (D-03): wp 95 → 71, add sp.dmg 1d4+0.
       - Stalka Beast (T5 Beasts): 5.05 (3.13x) / 1.40 (2.00x) — FIX (D-03): wp 125 → 94, add sp.dmg 1d4+0 (still 2.35x after — the D-03 discount is the locked treatment; Phase 21 owns the rest).
       - Ghost, Spectre (T4 Demons): infinite TTK — UNCHANGED, by design (magicOnly canon gate).
       - Under-tier rows: one row each for Bat/Rat (0.20x, also fixture-exposed), Zit (0.33x), Shadow (0.33x), Stink Bug (0.36x), Wolf (0.38x), Floater (0.43x), Flube (0.44x), Drarl T5 (0.47x), Drudge T5 (0.30x → 0.22x after D-03) — "under-tier — no buff in a conservative pass; Phase 21".
       - Every other row: a single collapsed line "all other rows: within band — unchanged".
    7. "Canon modifiers and the AFTER table (CANON-01/03/04/05)": explain that 18-06 will paste the `--mechanics=canon` output between `<!-- yardstick:after:begin -->` / `<!-- yardstick:after:end -->` markers (insert both marker lines now with the placeholder `_Filled by 18-06._` between them), and pre-state the known consequence the planner computed: foe natural armor (D-05, no durability) raises the melee-only TTK of the five `sp.ar` creatures — Drat 3.80 → 5.93 (2.54x), Krupke (after discount) 4.70 → 7.34 (2.22x), Craig 1.86 → 3.37 (2.36x), Herman T4 2.78 → 5.06 (3.54x), Google 5.25 → 9.54 (2.88x) — recorded as "canon natural armor (D-05) — melee yardstick only, spells bypass (D-06); revisit Phase 21", the same treatment D-19 gives Sterling. State that no wp is lowered to compensate (D-02 conservative pass; the mechanic, not a prototype number, is what is new).
    8. "Pre-ability discount (D-03, BEST-02)": rounding rule `Math.round(wp * 0.75)`, dice ladder d12 → d10 → d8 → d6 → d4 (no sp.dmg = engine d6 fallback, so one step down is an authored 1d4+0), and the seven-row before/after table (Djinni x2 86→65 + 1d4, Krupke 23→17 + 1d8+2→1d6+2, Drudge x2 12→9 HP-only, Vampire 95→71 + 1d4, Stalka Beast 125→94 + 1d4), each flagged "pre-ability discount — revisit Phase 21".
    9. "Parity carve-outs (BEST-03 / FID-05)": ZERO — the four fixture-exposed rows (Bat/Rat, Shriek, Viper, Dante; FIXTURE-INVENTORY.md) are untouched, so no `comparables.js` carve-out and no fixture regeneration; `test/parity/fixture-inventory.test.js` and `test/unit/foe-turn-draw-count.test.js`'s FULL_FIGHTS pins are the proof; every new mechanic is gated on a flag (`sp.ar`, `sp.slow`, `sp.halfDmg`, foe type/name multipliers) that none of the four carries (D-15).
    10. "Change ledger (filled by 18-06)": heading + placeholder line.
    Keep the family-friendly deadpan voice in prose where it fits (this is a developer doc; clarity first).
  </action>
  <verify>
    <automated>node --test test/unit/bestiary-yardstick.test.js && grep -c 'yardstick:before:begin' content/BESTIARY-REBALANCE.md && grep -c 'yardstick:after:begin' content/BESTIARY-REBALANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f content/BESTIARY-REBALANCE.md` succeeds and `wc -l < content/BESTIARY-REBALANCE.md` prints at least 150
    - `grep -c 'yardstick:before:begin\|yardstick:before:end\|yardstick:after:begin\|yardstick:after:end' content/BESTIARY-REBALANCE.md` prints 4
    - `awk '/yardstick:before:begin/{p=1;next} /yardstick:before:end/{p=0} p' content/BESTIARY-REBALANCE.md | grep -c '^| '` prints 55 (the pasted 53-row table + header + separator)
    - `awk '/yardstick:before:begin/{p=1;next} /yardstick:before:end/{p=0} p' content/BESTIARY-REBALANCE.md | diff - <(node tools/bestiary-yardstick.mjs --mechanics=prototype) | grep -c '^[<>] |'` prints 0 (the pasted block matches the live prototype-mode output line-for-line on table rows; this holds until 18-05 changes numbers)
    - `grep -c 'Death-depth distribution\|p50=' content/BESTIARY-REBALANCE.md` prints at least 2 (tune-difficulty BEFORE readout transcribed) and `grep -ci 'not a gate' content/BESTIARY-REBALANCE.md` prints at least 1
    - `grep -c 'D-14\|D-18\|D-19\|D-03\|D-16\|D-02' content/BESTIARY-REBALANCE.md` prints at least 6
    - `grep -c 'Drake' content/BESTIARY-REBALANCE.md` prints at least 3 and `grep -c '135 → 38\|135 -> 38' content/BESTIARY-REBALANCE.md` prints at least 1
    - `grep -c '1d10+5 → 1d10+0\|1d10+5 -> 1d10+0' content/BESTIARY-REBALANCE.md` prints at least 1
    - `grep -c 'Math.round(wp \* 0.75)\|Math.round(wp x 0.75)' content/BESTIARY-REBALANCE.md` prints at least 1
    - `grep -c 'Filled by 18-06' content/BESTIARY-REBALANCE.md` prints at least 3 (tune AFTER, yardstick AFTER, change ledger)
    - `grep -c 'under-tier' content/BESTIARY-REBALANCE.md` prints at least 3
    - `git diff --quiet e01ac46 -- content/bestiary.js engine/ test/parity` exits 0 (no numbers, engine, or parity files changed by this plan)
    - `npm test` exits 0
  </acceptance_criteria>
  <done>content/BESTIARY-REBALANCE.md exists with the methodology, the D-02 rule and direction, the tune-difficulty BEFORE readout, the verbatim prototype-mode BEFORE table, a Review Verdicts table naming every flagged/under-tier row with its disposition and planned change, the D-03 numbers, the zero-carve-out statement, and marker placeholders for 18-06's AFTER content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content tables → yardstick script | Trusted in-repo data read by a dev-only script; no user or network input |
| `--bestiary=<path>` CLI flag → dynamic import | Developer-supplied local path on the developer's own machine (dev tooling, never shipped); an arbitrary path executes as an ES module — acceptable for a dev script, documented in the header |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | `test/parity/prototype-master.js.txt`, fixtures, `comparables.js` edited to make a changed behavior pass | high | mitigate | Prohibited in must_haves; acceptance criteria run `git diff --quiet e01ac46 -- test/parity` and `git status --porcelain test/parity` |
| T-18-05 | Repudiation | Stat table drifting from the code it describes (hand-edited numbers) | medium | mitigate | The BEFORE block is generated by the committed script and checked line-for-line against live output in this plan; 18-06 adds the permanent AFTER-block consistency test |
| T-18-04 | Elevation of Privilege | Dev script importing engine/DOM or using Math.random | low | mitigate | Script imports only content + node builtins (acceptance grep); `tools/` is not shipped |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase; `package.json`/lockfile unchanged (acceptance criterion); slopcheck + blocking human checkpoint would gate any [ASSUMED]/[SUS] package, none exist |
</threat_model>

<verification>
- `node --test test/unit/bestiary-yardstick.test.js` green (>= 10 tests).
- `node tools/bestiary-yardstick.mjs --mechanics=prototype` reproduces 18-RESEARCH.md's key numbers (Drake 7.30x, Werebeast lethality 2.60x, Djinni T4 4.65x, Stalka 3.13x, Vampire 2.38x) and `node tools/bestiary-yardstick.mjs` (canon) shows Sterling 10.23 and Philly 3.84.
- `content/BESTIARY-REBALANCE.md` carries the BEFORE table verbatim, the tune-difficulty BEFORE readout, the Review Verdicts, and the AFTER placeholders.
- `npm test` green (724 baseline + this plan's new tests); `git diff --quiet e01ac46 -- content/bestiary.js engine/ test/parity` exits 0.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 1 is half-satisfied: every bestiary row is reviewed against its tier with a reproducible yardstick and the BEFORE half of the committed table exists with explicit verdicts; 18-06 completes the AFTER half.
- Phase 21 can re-run the exact review with one command.
</success_criteria>

<output>
Create `.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-01-SUMMARY.md` when done (record the tune-difficulty BEFORE readout lines, the prototype-mode tier medians, and the script's CLI contract).
</output>
