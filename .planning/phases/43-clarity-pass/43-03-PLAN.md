---
phase: 43-clarity-pass
plan: 03
type: execute
wave: 3
depends_on: [43-02]
files_modified:
  - src/browser/viewModels.js
  - test/unit/usableBy.test.js
  - test/unit/gear-panels.test.js
  - test/unit/lootCompare.test.js
  - test/unit/hp-not-wp.test.js
  - docs/CLARITY.md
autonomous: true
requirements: [CLAR-02, CLAR-04]
tags: [view-model, loot-legibility, gear-tab, pure, deferred-uat]

must_haves:
  truths:
    - "One pure helper `usableBy(it, c)` in `src/browser/viewModels.js` reads the weapon/armor `cls` string and the staff Magic-User gate and returns `(usable by Fighters, Thieves)` for a class-restricted item, `''` for an unrestricted one, and `(usable by Fighters — not you)` when the given hero cannot use it (ROADMAP SC-2, CLAR-02, CONTEXT §CLAR-02); a Thief with Heft offered Mail reads `(usable by Fighters — and a Thief with Heft)` because the engine's `armorRefusalReason` says so"
    - "`lootCompare(c, it)` gains an additive `usable` field equal to `usableBy(it, c)` so the LOOT screen appends it from the same source it already reads (no second class-gate derivation in the shell)"
    - "`dropShelfItems(c)` returns only slot-consuming BAG items with their true `c.items` index (`[{ it, i }]`) — never a worn slot item, never the wielded weapon or worn armor, never a slot-exempt potion (dropping a potion frees no slot) — so Plan 04's bag-full drop prompt lists bag items only (ROADMAP SC-4, CLAR-04)"
    - "`emptySlotRows(c)` returns one in-voice row per EMPTY worn slot (the six `WORN_SLOTS` plus armor when nothing is worn) from a frozen `GEAR_COPY` (ON YOU / WIELDED / WORN / BAG headings + empty states), with the staff row telling a non-Magic-User the truth (`staff — nothing, and nothing you could hold`), so Plan 04's ON YOU panel reads as slots (CONTEXT §CLAR-04)"
    - "Every new string clears the voice scan and `hp-not-wp`; nothing here touches the engine, `mazeworld.html` or parity"
  artifacts:
    - "src/browser/viewModels.js#usableBy(it, c = null), USABLE_COPY, lootCompare(...).usable, #dropShelfItems(c), #emptySlotRows(c), GEAR_COPY"
    - "test/unit/usableBy.test.js, test/unit/gear-panels.test.js (new); test/unit/lootCompare.test.js extended"
    - "docs/CLARITY.md `## Loot legibility (CLAR-02)` and `## Gear screen (CLAR-04)` sections"
  key_links:
    - "content/weapons.js `cls` + content/armors.js `cls` + the staff Magic-User gate (engine/items.js#takeItem/autoWearSlot) -> usableBy -> FIND card / LOOT screen (via lootCompare.usable) / store rows (Plan 04 wiring)"
    - "engine/items.js#weaponRefusalReason/armorRefusalReason (the ONE legality rule) -> usableBy's `not you` / Heft clause — never a restated rule"
    - "engine/derived.js#slotItems (the ONE capacity count) -> dropShelfItems (same potion exemption) -> the drop shelf"
    - "engine/derived.js#WORN_SLOTS -> emptySlotRows order -> Plan 04's ON YOU rows"
---

<objective>
Build the pure, DOM-free view models Plan 04's shell wiring will read: `usableBy(it, c)` (the "(usable by …)" text for every loot offer, with the deadpan "— not you" when the hero cannot use it), the additive `lootCompare(...).usable` field, `dropShelfItems(c)` (bag-only, slot-consuming, true indices), and `emptySlotRows(c)` + `GEAR_COPY` (the ON YOU panel's slot vocabulary and in-voice empty states). Ledger the display rules in `docs/CLARITY.md`.

Purpose: ROADMAP SC-2 and SC-4 (CLAR-02, CLAR-04) — the logic lives in a testable module so the last plan (the only one allowed to edit `mazeworld.html`) is pure wiring.

Output: the four exports + two frozen COPY objects, three test files, two ledger sections, 43-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-clarity-pass/43-CONTEXT.md
@.planning/phases/43-clarity-pass/43-02-SUMMARY.md
@src/browser/viewModels.js
@engine/items.js
@engine/derived.js
@content/weapons.js
@content/armors.js
@content/treasure-tables.js
@docs/GEAR-SLOTS.md
@docs/CLARITY.md
</context>

<standing_rules>
- No engine edits, no `mazeworld.html` edits, no content edits in this plan — `src/browser/viewModels.js` + tests + docs only. If a rule turns out to need an engine read that does not exist, STOP and record it (do not restate a rule in the view model — import the engine predicate).
- Presentation purity: viewModels.js may import from `engine/` (it already does) but never touches DOM/Math.random/Date; every new export is a pure function of its arguments (the existing purity guard in test/unit/toastsCoverage.test.js covers toasts/narration; add an explicit no-mutation assertion in the new tests).
- Deferred UAT; verification agents OFF (last task runs the gate: `npm test` `# fail 0`, master hash, `npm run build:www` exit 0 because `src/browser` is touched); HP not WP; voice scan on every COPY leaf.
- Commits per task: `feat(43-03): …` / `test(43-03): …` / `docs(43-03): …` with the two trailer lines. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
See 43-01-PLAN.md `<artifacts_this_phase_produces>` for the phase-wide list. This plan produces: `src/browser/viewModels.js#usableBy`, `USABLE_COPY`, `lootCompare(...).usable`, `#dropShelfItems`, `#emptySlotRows`, `GEAR_COPY`; `test/unit/usableBy.test.js`; `test/unit/gear-panels.test.js`; `docs/CLARITY.md` `## Loot legibility (CLAR-02)` + `## Gear screen (CLAR-04)`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usableBy(it, c) + USABLE_COPY + lootCompare.usable</name>
  <files>src/browser/viewModels.js, test/unit/usableBy.test.js, test/unit/lootCompare.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - src/browser/viewModels.js lines 144-181 (`classNames(letters)` — "Fighter or Thief" singular join; `refusalText(reason, cls)` — the loot screen's "can't use (…)" fragment; both module-private — REUSE `classNames`' NAME map idea but the plural form is new), 183-226 (`lootCompare` — the weapon/armor/bag/other branches; add `usable` to EVERY returned object so the shape is uniform)
    - engine/items.js lines 270-340 (`classLetter`, `canEquipWeapon`, `weaponRefusalReason` -> `null | "acrobat" | "wrongClass"`, `armorRefusalReason` -> `null | "noArmor" | "woodsman" | "tooHeavy"` — a Thief with Heft may wear `ar <= 12` armor regardless of `cls`), 440-470 and 380-389 (a staff is `wrongClass` for anyone but a Magic User — `takeItem`'s staff gate and `autoWearSlot`'s `it.kind === "staff" && c.cls !== "Magic User"`)
    - content/weapons.js (every `cls` is one of `FTM`, `FT`, `F`), content/armors.js (`Cloth` FTM, `Leather`/`Studded` FT, `Mail`/`Plate` F), content/treasure-tables.js STAVES (kind `staff`, Magic User only), JEWELRY/CLOAKS (unrestricted)
    - engine/items.js lines 145-176 (`rollBlade` magic weapon items carry `base`; `rollMailPiece` armor items carry `armor` + `cls`), engine/economy.js lines 336-360 (store `weaponLine`/`armorLine`/`premiumLine` items — `effectParams.item` shapes Plan 04 will pass in)
    - test/unit/lootCompare.test.js (the fixedChar helper + the exact-field tests — add a `usable` assertion to each existing case rather than a deepStrictEqual that would break them)
  </read_first>
  <behavior>
    - `USABLE_COPY` is exported, frozen: `{ F: "Fighters", T: "Thieves", M: "Magic Users", usable: "(usable by {who})", notYou: "(usable by {who} — not you)", heft: "(usable by {who} — and a Thief with Heft)" }`
    - `usableBy({ kind: "weapon", base: "Bardiche" })` is `(usable by Fighters)`; `({ kind: "weapon", base: "Katana" })` is `(usable by Fighters, Thieves)`; `({ kind: "weapon", base: "Axe" })` is `""` (FTM); `({ kind: "armor", armor: "Mail", cls: "F", ar: 12 })` is `(usable by Fighters)`; `({ kind: "armor", cls: "FTM" })` is `""`; `({ kind: "staff", n: "Rowan Staff" })` is `(usable by Magic Users)`; `({ kind: "cloak" })`, `({ kind: "jewel" })`, `({ kind: "potion" })`, `({ kind: "tool" })`, `({ kind: "bag" })`, `null`, `{}` are all `""`; a weapon whose `base` is not in WEAPONS is `""`
    - With a hero: `usableBy(bardiche, thief)` is `(usable by Fighters — not you)`; `usableBy(katana, fighter)` is `(usable by Fighters, Thieves)`; `usableBy(longSword, acrobatThief)` is `(usable by Fighters, Thieves — not you)` (Acrobat dagger-only via `weaponRefusalReason`); `usableBy(mail, thiefWithHeft)` is `(usable by Fighters — and a Thief with Heft)`; `usableBy(mail, thiefNoHeft)` is `(usable by Fighters — not you)`; `usableBy(rowanStaff, fighter)` is `(usable by Magic Users — not you)`; `usableBy(rowanStaff, magicUser)` is `(usable by Magic Users)`; `usableBy(leather, fridgianFighter)` is `(usable by Fighters, Thieves — not you)` (noArmor race); `usableBy(plate, woodsman)` is `(usable by Fighters — not you)`; `usableBy(axe, anyone)` is `""` even when the hero cannot wield it for a sub-class reason (an Acrobat with an Axe) — an unrestricted item's refusal is the loot screen's `can't use (…)` line's job, not this suffix
    - `lootCompare(c, it).usable === usableBy(it, c)` for weapon, armor, bag and other kinds; every previously-asserted `lootCompare` field is unchanged
    - Pure: `usableBy` never mutates `it` or `c` (deepStrictEqual before/after) and never throws on a sparse item
  </behavior>
  <action>
    A. `src/browser/viewModels.js`: add `export const USABLE_COPY = Object.freeze({ F: "Fighters", T: "Thieves", M: "Magic Users", usable: "(usable by {who})", notYou: "(usable by {who} — not you)", heft: "(usable by {who} — and a Thief with Heft)" });` beside `ITEM_STATE_COPY`. Add a module-private `restrictedClasses(it)` -> the `cls` letters string for a weapon (`WEAPONS[it.base]?.cls`), an armor (`it.cls ?? ARMORS.find(a => a.name === it.armor)?.cls` — import `ARMORS` from `../../content/index.js`), `"M"` for `kind === "staff"`, else `null`; returns `null` when the letters are `"FTM"` (unrestricted). Add `export function usableBy(it, c = null)`: `letters = restrictedClasses(it)`; if `!letters` return `""`; `who` = letters split, mapped through `USABLE_COPY[letter]`, joined `", "`; if `!c` return `usable`; legality: weapon -> `weaponRefusalReason(c, it) === null`; armor -> `armorRefusalReason(c, it) === null`; staff -> `c.cls === "Magic User"`; if legal and the hero's class letter (`c.cls === "Fighter" ? "F" : c.cls === "Thief" ? "T" : "M"`) is NOT in `letters` (the Heft case) return `heft`; if legal return `usable`; else `notYou`. JSDoc: Phase 43 (CLAR-02) — the ONE "(usable by …)" rule for FIND card / LOOT screen / store rows (Plan 04 bridges it as `window.__mzUsableBy`); legality is the engine's own `weaponRefusalReason`/`armorRefusalReason` (never restated), the staff gate mirrors `engine/items.js#takeItem`; pure, no rng, no DOM. Extend `lootCompare`: add `usable: usableBy(it, c)` to all four return objects (weapon, armor, bag, fallthrough) with a one-line `Phase 43 (CLAR-02, additive field)` comment.

    B. Tests: `test/unit/usableBy.test.js` (new) — every `<behavior>` bullet (build heroes as plain objects: `{ cls, sub, race, skills: {…}, weapon, armor, ar }`; Heft is `skills: { Heft: 1 }`; Fridgian for noArmor; Woodsman sub for the mail/plate refusal), a `USABLE_COPY` deepEqual pin, a BANNED-wordlist walk of its leaves, and a purity test. Extend `test/unit/lootCompare.test.js`: in each existing case assert `cmp.usable` (the Broadsword +2 case is `(usable by Fighters)`; the class-illegal case ends `— not you)`; the bag case `""`). Add `USABLE_COPY` to `test/unit/hp-not-wp.test.js`'s COPY walk.

    Run `node --test test/unit/usableBy.test.js test/unit/lootCompare.test.js test/unit/hp-not-wp.test.js test/unit/gear-axes.test.js test/unit/loot-narration.test.js test/voice/` — green. Commit `feat(43-03): usableBy(it, c) + USABLE_COPY; lootCompare.usable (additive)` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "export const USABLE_COPY" src/browser/viewModels.js` is 1; `grep -c "export function usableBy(it, c = null)" src/browser/viewModels.js` is 1; `grep -c "usable: usableBy(it, c)" src/browser/viewModels.js` is 4
    - `node -e "import('./src/browser/viewModels.js').then(m=>{const thief={cls:'Thief',sub:'Pilfer',race:'Human',skills:{},weapon:'Dagger',armor:'Leather',ar:6};const heft={...thief,skills:{Heft:1}};const mu={cls:'Magic User',sub:'Wizard',race:'Human',skills:{},weapon:'Dagger',armor:'Cloth',ar:3};const ok=m.usableBy({kind:'weapon',base:'Bardiche'})==='(usable by Fighters)'&&m.usableBy({kind:'weapon',base:'Katana'})==='(usable by Fighters, Thieves)'&&m.usableBy({kind:'weapon',base:'Axe'})===''&&m.usableBy({kind:'weapon',base:'Bardiche'},thief)==='(usable by Fighters — not you)'&&m.usableBy({kind:'armor',armor:'Mail',cls:'F',ar:12,wp:30,min:3},heft)==='(usable by Fighters — and a Thief with Heft)'&&m.usableBy({kind:'armor',armor:'Mail',cls:'F',ar:12,wp:30,min:3},thief)==='(usable by Fighters — not you)'&&m.usableBy({kind:'staff',n:'Rowan Staff'},thief)==='(usable by Magic Users — not you)'&&m.usableBy({kind:'staff',n:'Rowan Staff'},mu)==='(usable by Magic Users)'&&m.usableBy({kind:'cloak'})===''&&m.usableBy(null)==='';process.exit(ok?0:1)})"` exits 0
    - `node --test test/unit/usableBy.test.js` exits 0 with at least 16 tests; `node --test test/unit/lootCompare.test.js test/unit/hp-not-wp.test.js test/unit/gear-axes.test.js test/unit/loot-narration.test.js test/voice/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/usableBy.test.js test/unit/lootCompare.test.js test/unit/hp-not-wp.test.js test/unit/gear-axes.test.js test/unit/loot-narration.test.js test/voice/</automated>
  </verify>
  <done>Every class-restricted item can say who can use it — and whether that is you — from one pure helper that defers to the engine's legality rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dropShelfItems(c), emptySlotRows(c), GEAR_COPY + tests</name>
  <files>src/browser/viewModels.js, test/unit/gear-panels.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - engine/derived.js lines 109-120 (`slotItems(c)` — potions are slot-exempt; THE capacity count), 122-127 (`WORN_SLOTS` frozen order ring/bracelet/amulet/helm/cloak/staff), 129-150 (`slotFor`), 151-170 (`carriedItems` — bag ∪ worn; NOT what the drop shelf wants)
    - src/browser/viewModels.js lines 103-133 (`armorDisplay(c)` — `worn`/`magic`/`destroyed` flags; the armor empty state is `!worn && !magic`), 228-238 (`bagUsage`)
    - mazeworld.html lines 4076-4088 (`renderDropShelf(shelf, items)` — currently `forEach((bi, i) => … mzDropItem(i))` over `c.items`, so a potion row would dispatch a drop that frees no slot; Plan 04 re-points it at `dropShelfItems`), 3383-3425 (`wornRow`/`wornSlotRow` — the ON YOU rows Plan 04 keeps; `emptySlotRows` fills the gaps between them)
    - docs/GEAR-SLOTS.md §2 (slot taxonomy: staff is a WORN slot; one item per slot; no shields exist)
  </read_first>
  <behavior>
    - `dropShelfItems({ items: [gear a, potion p, gear b] })` returns `[{ it: a, i: 0 }, { it: b, i: 2 }]` (true `c.items` indices, potion skipped); `dropShelfItems({ items: [], worn: { ring: jewel } , weapon: "Axe", armor: "Mail" })` returns `[]`; `dropShelfItems({})`/`(null)` return `[]`; the returned `it` objects are the SAME references as `c.items[i]` (no clone) and `c` is not mutated; `dropShelfItems(c).length === slotItems(c).length` for every input
    - `GEAR_COPY` is exported, frozen: `{ onYou: "ON YOU", wielded: "WIELDED", worn: "WORN", alsoOnYou: "ALSO ON YOU", bag: "BAG", empty: { armor: "armor — nothing. The wind is your armor, and the wind is not on your side.", ring: "ring — nothing. Ten fingers, zero commitments.", bracelet: "bracelet — nothing. A bare wrist, ready for bad decisions.", amulet: "amulet — nothing. Your neck has never been less interesting.", helm: "helm — nothing. Hair, technically, counts for zero.", cloak: "cloak — nothing. Cold and unmagical, in that order.", staff: "staff — nothing. Wave your hands and see how far that gets you.", staffNotYou: "staff — nothing, and nothing you could hold. Magic Users only." } }`
    - `emptySlotRows(c)` returns `[{ slot, text }]` in the order armor -> WORN_SLOTS: armor first ONLY when `armorDisplay(c)` is neither `worn` nor `magic`; then one row per WORN_SLOTS key whose `c.worn?.[slot]` is falsy (a legacy `c` with no `worn` map yields all six); the staff row's text is `GEAR_COPY.empty.staffNotYou` when `c.cls !== "Magic User"`, else `GEAR_COPY.empty.staff`; a fully-equipped Magic User (armor worn, six slots filled) returns `[]`; pure
    - Every `GEAR_COPY` leaf clears the safety wordlist and `hp-not-wp`
  </behavior>
  <action>
    A. `src/browser/viewModels.js`: import `WORN_SLOTS` (extend the `engine/derived.js` import — note test/unit/shell-*.test.js pin the SHELL's derived import line, not this module's). Add `export const GEAR_COPY = Object.freeze({ … })` exactly as in `<behavior>` (nested `empty` frozen too). Add `export function dropShelfItems(c)`: `(c && Array.isArray(c.items) ? c.items : []).map((it, i) => ({ it, i })).filter(({ it }) => it && it.kind !== "potion")` — JSDoc: Phase 43 (CLAR-04) — the bag-full drop prompt's ONE source list: slot-consuming BAG items only (the same potion exemption as `engine/derived.js#slotItems`, kept in lock-step by test), with the true `c.items` index so `dropItem(i)` addresses the right entry; never a worn slot item, never the wielded weapon or worn armor (those are not in `c.items`). Add `export function emptySlotRows(c)` per `<behavior>` (use `armorDisplay(c)` for the armor test; `c.worn && c.worn[slot]` for each WORN_SLOTS key). JSDoc: Phase 43 (CLAR-04) — the ON YOU panel's empty states read as SLOTS in voice (CONTEXT §CLAR-04: "ring — nothing"); Plan 04 renders these between the existing wornRow/wornSlotRow rows; pure.

    B. `test/unit/gear-panels.test.js` (new): every `<behavior>` bullet, the `GEAR_COPY` deepEqual pin, the `dropShelfItems(c).length === slotItems(c).length` lock-step over a few shapes (import `slotItems` from engine/derived.js), the BANNED walk, purity. Add `GEAR_COPY` to `test/unit/hp-not-wp.test.js`'s COPY walk.

    Run `node --test test/unit/gear-panels.test.js test/unit/hp-not-wp.test.js test/unit/lootCompare.test.js test/unit/itemRowState.test.js test/unit/worn-slots.test.js test/voice/` — green. Commit `feat(43-03): dropShelfItems (bag-only, true indices) + emptySlotRows + GEAR_COPY` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "export const GEAR_COPY" src/browser/viewModels.js` is 1; `grep -c "export function dropShelfItems(c)" src/browser/viewModels.js` is 1; `grep -c "export function emptySlotRows(c)" src/browser/viewModels.js` is 1
    - `node -e "import('./src/browser/viewModels.js').then(async m=>{const {slotItems}=await import('./engine/derived.js');const a={kind:'jewel',n:'a'},p={kind:'potion',n:'p'},b={kind:'tool',n:'b'};const c={items:[a,p,b],worn:{ring:{kind:'jewel',n:'Ring'}},weapon:'Axe',armor:'Mail',ar:12,armorWP:30,armorMax:30,cls:'Fighter'};const d=m.dropShelfItems(c);const e=m.emptySlotRows(c);process.exit(d.length===2&&d[0].it===a&&d[0].i===0&&d[1].it===b&&d[1].i===2&&d.length===slotItems(c).length&&e.length===5&&e[0].slot==='bracelet'&&e[4].slot==='staff'&&e[4].text===m.GEAR_COPY.empty.staffNotYou&&m.dropShelfItems(null).length===0?0:1)})"` exits 0
    - `node --test test/unit/gear-panels.test.js` exits 0 with at least 12 tests; `node --test test/unit/hp-not-wp.test.js test/unit/lootCompare.test.js test/unit/itemRowState.test.js test/unit/worn-slots.test.js test/voice/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/gear-panels.test.js test/unit/hp-not-wp.test.js test/unit/lootCompare.test.js test/unit/itemRowState.test.js test/unit/worn-slots.test.js test/voice/</automated>
  </verify>
  <done>The drop prompt has a bag-only source list with honest indices, and every empty worn slot has an in-voice row waiting for the ON YOU panel.</done>
</task>

<task type="auto">
  <name>Task 3: docs/CLARITY.md display-rule sections + plan gate + SUMMARY</name>
  <files>docs/CLARITY.md, .planning/phases/43-clarity-pass/43-03-SUMMARY.md</files>
  <read_first>
    - docs/CLARITY.md (Plan 01's inventory + HP sweep sections; insert the two new sections BEFORE `## Requirements map`)
    - docs/GEAR-SLOTS.md §4 (play rules — cross-link the ON YOU grouping to it), docs/TERRAIN.md lines 403-412 ("What stays for the cleanup milestone" style)
  </read_first>
  <action>
    A. `docs/CLARITY.md`: add `## Loot legibility (CLAR-02)` — the `usableBy` rule (restricted = `cls !== "FTM"` for weapons/armor, staff = Magic Users; the three text forms; legality via the engine's refusal reasons; the Heft clause; where it shows: FIND card, LOOT screen (via `lootCompare.usable`), store rows; the Joiner offer carries NO item, so the suffix has no site there — CONTEXT's "Company/Joiner offer where an item is involved" resolves to "none today"). Add `## Gear screen (CLAR-04)` — the two panels: ON YOU = WIELDED (the weapon row) + WORN (armor + the six `WORN_SLOTS`, empty states from `GEAR_COPY.empty`, staff-not-you for non-Magic-Users) + ALSO ON YOU (the existing kit summary rows — potions, scrolls, rations, wilmst, spell charges, running effects, kills — minus the weapon/armor duplicates, because they need no bag slot and are literally on your person); BAG = the slot-consuming items with the `n / slots` readout; the roadmap's "Carried: weapon, staff, shield" is superseded (staff is a worn slot per Phase 37; there are no shields — user ruling in CONTEXT); the drop prompt reads `dropShelfItems` (bag-only, potions excluded because dropping one frees no slot). Cross-link docs/GEAR-SLOTS.md.

    B. PLAN GATE: `npm test` (record `# tests/# pass/# fail`, fail 0); master hash; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty.

    C. Commit `docs(43-03): CLARITY ledger — loot legibility + gear screen rules` (explicit paths). Write `43-03-SUMMARY.md` (standard sections, gate numbers, `## Human verification (deferred to end of run)`: `None — pure view models and docs only (no mazeworld.html/src/browser render change); the device checks land with Plan 04's wiring.` Plus `## Corrections to CONTEXT` if any (e.g. the Joiner offer has no item).
  </action>
  <acceptance_criteria>
    - `grep -c "^## Loot legibility (CLAR-02)" docs/CLARITY.md` is 1; `grep -c "^## Gear screen (CLAR-04)" docs/CLARITY.md` is 1; `grep -c "docs/GEAR-SLOTS.md" docs/CLARITY.md` is at least 1; the `## Requirements map` heading is still the LAST `## ` heading in the file (`grep "^## " docs/CLARITY.md | tail -1` prints `## Requirements map`)
    - `npm test` output ends with `# fail 0`; `# pass` is at least the Plan 02 SUMMARY's recorded pass count + 28
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0; `git status --porcelain test/parity/fixtures` is empty
    - `.planning/phases/43-clarity-pass/43-03-SUMMARY.md` exists and contains `## Human verification (deferred to end of run)`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && grep "^## " docs/CLARITY.md | tail -1</automated>
  </verify>
  <done>The display rules are ledgered; the suite is green; Plan 04 has everything it needs to wire the shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| item objects (saves, loot rolls, store lines) -> view models | sparse or legacy item shapes reach `usableBy`/`dropShelfItems` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-07 | Denial of service | `usableBy` on a sparse/legacy item (no `base`, unknown `armor`) | low | mitigate | every lookup optional-chained; unknown -> `""`; pinned by the sparse-item tests |
| T-43-08 | Tampering | a restated class rule drifting from the engine | medium | mitigate | legality is `weaponRefusalReason`/`armorRefusalReason` imported from engine/items.js; only the display text is local |
| T-43-09 | Tampering | drop shelf index mismatch (dropping the wrong item) | medium | mitigate | `dropShelfItems` returns the true `c.items` index; lock-step test with `slotItems` |
| T-43-SC | Tampering | npm/pip/cargo installs | low | accept | no installs |
</threat_model>

<verification>
- `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0.
- `usableBy` truth table (16+ cases) pinned; `lootCompare.usable` additive; `dropShelfItems` lock-step with `slotItems`; `emptySlotRows` order and staff-not-you pinned; COPY objects voice/hp clean.
</verification>

<success_criteria>
- CLAR-02 logic complete and testable before any shell edit.
- CLAR-04 logic (bag-only drop source, in-voice empty slots) complete and testable before any shell edit.
- No engine/content/shell/parity change.
</success_criteria>

<output>
Create `.planning/phases/43-clarity-pass/43-03-SUMMARY.md` when done (standard sections + `## Human verification (deferred to end of run)` + the gate numbers).
</output>
