---
phase: 34-combat-screen-rebuild
plan: 05
type: execute
wave: 5
depends_on: ["34-01", "34-02", "34-03", "34-04"]
files_modified:
  - mazeworld.html
  - test/unit/shell-combat-over.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-input-guards.test.js
autonomous: true
requirements: [CSCR-07, CSCR-08, CSCR-09, CSCR-10]
tags: [vanilla-js, fight-end, over-panel, loot, death, voice-scan, executor-gate, deferred-uat]

must_haves:
  truths:
    - "Killing the last foe renders THEY ARE DOWN (or THEY STAND DOWN after a successful parley) inside the dark panel: the victory report lines (rounds, felled, hp, gold, level) and the ending action's own lines as plain text, the Phase 29 loot rows (Equip now / Stow / Take / Leave with lootCompare, guard: true), the drop shelf when the bag is full, and TAKE ALL / LEAVE ALL; the panel closes exactly as the loot card does today (pile empty → hasActiveEncounter false → map)"
    - "A win with no drops (or a soothed parley) renders the same over-panel with a single BACK TO THE MAZE button that clears S.beats; fleeing renders YOU GOT OUT + the escape lines + BACK TO THE MAZE"
    - "Death renders THAT IS THAT (#e07260) with the death note, floor/day/XP line, the epitaph, last words and the killing-blow lines, and the two existing buttons — REVIEW THE ORACLE (#btn-death-oracle) and BURY THEM (#btn-death-confirm, armed, no read-first lock, wired by wireDeathConfirm)"
    - "The joiner offer and the find card keep their exact behaviour, ids and textContent wiring and render in the dark vocabulary (data-mode=\"dark\")"
    - "Every ending/decision button (cb-over-btn, a-loot-take-all, a-loot-leave-all, loot rows, btn-death-oracle, btn-death-confirm, a-join-yes/no, a-find-take/leave) is wired through guardTap; nothing dismisses on a map tap; DISMISS_SETTLE_MS still gates window.move; window.__mzFightEnd and window.__mzCombatMenu are cleared on the panel's dismissal transition"
    - "Every new player-facing literal in COMBAT_COPY, COMBAT_MENU_COPY and COMBAT_PANEL_COPY passes the BANNED voice scan; the S.won 'Through the Gate' card, the beats card for floor/level-up narration and the store are untouched (Phase 35 owns them)"
    - "Final gate: npm test green, npm run build:www exit 0, git diff -- engine content test/parity empty, master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - "mazeworld.html: renderCombatOver(host, kind, opts), htmlToPlain(html), COMBAT_COPY.over/headerOver/takeAll/leaveAll/lootHead, the rewritten death / loot / beats-over branches, data-mode=\"dark\" on joiner + find, the Enter/Space cb-over-btn key path"
    - "test/unit/shell-combat-over.test.js"
    - "Re-pinned: shell-loot-screen (dark loot branch), shell-input-guards (cb-over-btn added)"
  key_links:
    - "noteCombat (Plan 02: rep.over, beats.over:'fled', window.__mzFightEnd) -> renderEncounter death/loot/beats branches -> renderCombatOver -> COMBAT_COPY.over[kind]"
    - "renderEncounter dismissal transition (encWasActive && !active) -> lastDismissAt + mzCenterMap + window.__mzFightEnd = null + window.__mzCombatMenu = null"
    - "keydown beats branch Enter/Space -> #a-next || #cb-over-btn"
  prohibitions:
    - "No edits under engine/, content/, test/parity/; no new field on S (beats.over was added in Plan 02 on the shell-owned transient beats object only)"
    - "No innerHTML for any line that carries a player name, an epitaph or an engine narration string in the over-panel — htmlToPlain + textContent"
    - "No toast on any ending path; the toast host is not deleted"
    - "No tap-anywhere-to-dismiss; no read-first lock on BURY THEM; every decision button through guardTap"
    - "The debug APK build / adb install is NOT part of this plan — it happens once after Phase 35's last plan (mid-run builds of mazeworld.html would be stale)"
---

<objective>
Fold the fight endings into the combat screen (CSCR-07): the win (loot rows inside THEY ARE DOWN), the flee (YOU GOT OUT), and the death (THAT IS THAT with REVIEW THE ORACLE / BURY THEM) all render through one `renderCombatOver(host, kind, opts)` in the mock's over-panel vocabulary, the joiner offer and find card keep their behaviour in the dark styling, the dismissal transition clears the phase's transient globals, the last guard re-pins land (CSCR-08), and the phase closes with the full executor gate (CSCR-09) plus the deferred on-device checklist that stands in for CSCR-10 during this autonomous run.

**CSCR-10 in this run:** verification agents are off and UAT is deferred to the end of the run, so the plan-side deliverable for CSCR-10 is (a) the automated gate in Task 3 and (b) the `## Human verification (deferred to end of run)` section of this plan's SUMMARY, which aggregates the Pixel 7 checklist for the whole phase (Plans 02-05). The debug APK build + `adb install -r` + the user's DR round happen once, after Phase 35's last plan, because executors keep editing `mazeworld.html` and a mid-wave build would be stale.

Discretion decisions (copy and behaviour): over kinds are `won` (THEY ARE DOWN), `soothed` (THEY STAND DOWN — the existing "They stand down" report title, upper-cased, so a talked-down fight is not mislabelled), `fled` (YOU GOT OUT), `dead` (THAT IS THAT); header right-hand text on endings reads NONE STANDING / YOU LEFT / YOU FELL; the over-panel prints the ending action's parked lines (`window.__mzFightEnd.lines`) above the report so the killing blow / escape roll is never lost; death in or out of combat both use this branch this phase (Phase 35 may re-route out-of-combat death to the major overlay). The `S.won` Through-the-Gate card is out of scope and untouched.

Purpose: the screen's exits; the phase's gate and hand-off.
Output: `mazeworld.html` (renderCombatOver, htmlToPlain, COMBAT_COPY endings, three rewritten branches, joiner/find dark mode, dismissal clears, keydown), `test/unit/shell-combat-over.test.js`, re-pinned `shell-loot-screen.test.js` / `shell-input-guards.test.js`, the phase gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-combat-screen-rebuild/34-CONTEXT.md
@.planning/phases/34-combat-screen-rebuild/34-RESEARCH.md
@.planning/phases/34-combat-screen-rebuild/34-02-SUMMARY.md
@.planning/phases/34-combat-screen-rebuild/34-03-SUMMARY.md
@.planning/phases/34-combat-screen-rebuild/34-04-SUMMARY.md
@design/Mazeworld Combat Panel.dc.html
</context>

<artifacts_this_phase_produces>
See 34-01-PLAN.md "Artifacts this phase produces" for the full phase list. This plan creates: `renderCombatOver(host, kind, opts)`, `htmlToPlain(html)`, `COMBAT_COPY.over.{won,soothed,fled,dead}`, `COMBAT_COPY.headerOver`, `COMBAT_COPY.takeAll/leaveAll/lootHead`, DOM ids `cb-over`, `cb-over-btn`, classes `cb-over-lines cb-epitaph cb-loot cb-sub` (markup; CSS from Plan 03), `data-mode="dark"` on the death/loot/beats-over/joiner/find branches, and `test/unit/shell-combat-over.test.js`. It retires the `.deathcard` markup on the death branch (the S.won card keeps the class).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: renderCombatOver + htmlToPlain; death, loot and beats-over branches; joiner/find dark mode; dismissal clears; keydown</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html: renderEncounter lines from the top through the store branch as they stand after Plan 04 — the dismissal block `if (encWasActive && !active) {` (add the two clears INSIDE it, after `window.mzCenterMap?.();`, no nested braces — test/unit/shell-map-store-polish.test.js slices to the first `}`), the death branch (`if (S.dead && !preDeathBeat) {` … `wireDeathConfirm(); return; }` with the `.deathcard` markup and the `btn-death-oracle` guardTap), the won branch (UNTOUCHED), the beats branch (`if (!S.combat && S.beats && …) { const b = S.beats; …`), the loot branch (`if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {` — keep every pinned literal listed in test/unit/shell-loot-screen.test.js lines 170-186 and the `Bag full (` text), the joiner branch and the find branch (add one `panel.dataset.mode = "dark";` line at the top of each; change nothing else), lines 4953-4961 (wireDeathConfirm — unchanged), 2734-2755 (mzCombatReport: rep.lines are HTML with span.roll/hit/hurt — hence htmlToPlain), the Plan 04 helper block (add the two new helpers after renderActionArea), the keydown beats-branch Enter/Space line (`const n = document.getElementById("a-fight") || document.getElementById("a-next"); if (n) n.click();`)
    - noteCombat as left by Plan 02 (rep.over, the over:"fled" beat, window.__mzFightEnd)
    - design/Mazeworld Combat Panel.dc.html lines 120-126 (over panel template) and comment lines 151-153 (over panel style + titles)
    - test/unit/shell-loot-screen.test.js lines 170-215 (loot pins + branch order + joiner region untouched), test/unit/shell-party-camp.test.js lines 62-74 (joiner names who walks via textContent — unchanged), test/unit/shell-input-guards.test.js lines 189-193 (wireDeathConfirm pin) and 219-226 (dismissal stamp pin)
  </read_first>
  <action>
    1. Extend `COMBAT_COPY` (keep it one object literal) with: `takeAll: "TAKE ALL"`, `leaveAll: "LEAVE ALL"`, `lootHead: "WHAT THEY LEFT BEHIND"`, `back: "BACK"` (already there), `headerOver: { won: "NONE STANDING", soothed: "NONE STANDING", fled: "YOU LEFT", dead: "YOU FELL" }`, `over: { won: { title: "THEY ARE DOWN", line: "Every one of them. Try not to look smug.", btn: "BACK TO THE MAZE" }, soothed: { title: "THEY STAND DOWN", line: "Words. Who knew.", btn: "BACK TO THE MAZE" }, fled: { title: "YOU GOT OUT", line: "Not proud. Not dead either.", btn: "BACK TO THE MAZE" }, dead: { title: "THAT IS THAT", line: "The maze keeps the rest.", oracle: "REVIEW THE ORACLE", bury: "BURY THEM" } }`.
    2. Helpers (after renderActionArea, before renderEncounter):
       - `function htmlToPlain(html)`: `const t = document.createElement("template"); t.innerHTML = String(html ?? ""); return (t.content.textContent || "").replace(/\s+/g, " ").trim();` (comment: the report/beat/last-words lines are engine HTML with span.roll/hit/hurt; the over-panel prints them as plain text — dice kept — through textContent only. This is the one innerHTML in the helper and it never touches the live DOM).
       - `function renderCombatOver(host, kind, opts = {})`: `const copy = COMBAT_COPY.over[kind] || COMBAT_COPY.over.won;` (a) `renderCombatHeader(host, { label: "ENCOUNTER", round: "", standing: COMBAT_COPY.headerOver[kind] || "" })`; (b) `const mid = document.createElement("div"); mid.className = "cb-mid"; mid.id = "cb-mid";` then a `div.cb-over-lines` holding one `<p>` per line of `[...(window.__mzFightEnd && window.__mzFightEnd.lines ? window.__mzFightEnd.lines : []), ...(opts.lines || [])]` each via `p.textContent = htmlToPlain(line)`; for `kind === "dead"` add the death lines: `${S.c.name}, ${S.c.race} ${S.c.sub}, ${S.deathNote}.`, `Floor ${S.floor.depth} · day ${S.day} · ${Math.round(S.c.sp)} XP`, the epitaph as `p.cb-epitaph` (textContent S.epitaph || ""), and each `S.lastWords` entry (htmlToPlain); if `typeof opts.fillMid === "function"` call `opts.fillMid(mid)` (the loot branch injects its rows here); `host.appendChild(mid)`; (c) `const over = document.createElement("div"); over.className = "cb-over"; over.id = "cb-over";` with div `.cb-over-title` (+ class "dead" when kind === "dead") textContent copy.title, div `.cb-over-line` textContent copy.line, then buttons: if `opts.buttons` is an array of `{ id, label, cls, onTap }` create each as `<button class="cb-over-btn {cls}" id="{id}">` inside a `.cb-over-actions` div, append, and wire `guardTap(document.getElementById(b.id), b.onTap)` for those with an onTap (the death Confirm passes no onTap and is wired by wireDeathConfirm afterwards); `host.appendChild(over)`. No innerHTML anywhere in this function; element variables named `el`/`p`/`over`/`mid`.
    3. Death branch: replace the `.deathcard` innerHTML build with `panel.dataset.mode = "dark"; renderCombatOver(body, "dead", { buttons: [ { id: "btn-death-oracle", label: COMBAT_COPY.over.dead.oracle, cls: "secondary", onTap: () => window.__mzShowTab?.("oracle") }, { id: "btn-death-confirm", label: COMBAT_COPY.over.dead.bury, cls: "dead" } ] }); wireDeathConfirm(); return;` (the oracle button is wired inside renderCombatOver through guardTap — delete the old `const oracleBtn … guardTap(oracleBtn, …)` lines; `wireDeathConfirm` keeps `guardTap(btn, …)` on `btn-death-confirm` exactly as today — armed, no read-first lock, returns to the title screen).
    4. Beats branch: as its FIRST statements (after `const b = S.beats;`) add `if (b.over) { panel.dataset.mode = "dark"; const grp = b.groups && b.groups[0]; renderCombatOver(body, b.over, { lines: grp && grp.lines ? grp.lines : [], buttons: [{ id: "cb-over-btn", label: COMBAT_COPY.over[b.over] ? COMBAT_COPY.over[b.over].btn : COMBAT_COPY.over.won.btn, cls: "", onTap: () => { S.beats = null; renderEncounter(); } }] }); return; }` (comment: Phase 34 CSCR-07 — a won-without-drops / soothed / fled fight ends on the over-panel; every other beat (floor, level-up, feature narration) keeps the legacy card until Phase 35). Leave the rest of the beats branch byte-identical.
    5. Loot branch: keep the guard line, `const c = S.c; const usage = window.__mzBagUsage(c); const rep = window.__mzLootReport;` and replace the card build with `panel.dataset.mode = "dark"; const kind = rep && rep.over === "soothed" ? "soothed" : "won"; renderCombatOver(body, kind, { lines: rep && rep.lines ? rep.lines : [], fillMid: (mid) => { … }, buttons: [ { id: "a-loot-take-all", label: COMBAT_COPY.takeAll, cls: "", onTap: () => window.mzTakeAllLoot?.() }, { id: "a-loot-leave-all", label: COMBAT_COPY.leaveAll, cls: "secondary", onTap: () => window.mzLeaveAllLoot?.() } ] }); return;` where `fillMid` appends, via a wrapper div whose innerHTML is the SAME static markup as today's card minus the report (`<p class="cb-sub">${COMBAT_COPY.lootHead} · bag ${usage.text || "unlimited"}</p><ul class="skills cb-loot" id="loot-list"></ul>` + the existing bag-full paragraph `<p class="cb-sub" style="color:var(--rust)">Bag full (${usage.have}/${usage.slots}). Drop something to make room, or leave it.</p><div class="shelf" id="loot-drop-shelf"></div>` under the same `usage.full && needsSlot` condition), then calls the UNCHANGED `renderCarriedList(document.getElementById("loot-list"), S.pendingLoot, { actions: ["lootEquip", "lootTake", "lootLeave"], guard: true, subFor: (it) => { const cmp = window.__mzLootCompare(c, it); return cmp.sub ? `${cmp.line} · ${cmp.sub}` : cmp.line; } })` and `const dropShelf = document.getElementById("loot-drop-shelf"); if (dropShelf) renderDropShelf(dropShelf, c.items || []);`. The two take-all/leave-all `guardTap` calls now happen inside renderCombatOver via the buttons array — keep the literals `window.mzTakeAllLoot` / `window.mzLeaveAllLoot` / `id: "a-loot-take-all"` / `id: "a-loot-leave-all"` inside the loot branch (the test regexes only need the tokens present in the branch region). `usage.text`, `usage.full`, `usage.have`, `usage.slots` and `Bag full (` stay as literal usages (LOOT-04 pins).
    6. Joiner branch and find branch: add `panel.dataset.mode = "dark";` as the first statement of each (nothing else changes — ids, textContent head, guardTap wiring stay byte-identical).
    7. Dismissal transition: inside `if (encWasActive && !active) {` after `window.mzCenterMap?.();` add `window.__mzFightEnd = null;` and `window.__mzCombatMenu = null;` (no nested block).
    8. keydown beats branch: change the Enter/Space line to `const n = document.getElementById("a-next") || document.getElementById("cb-over-btn"); if (n) n.click();` (the a-fight id no longer exists).
    9. Delete the `.deathcard` CSS rules ONLY if the S.won branch no longer uses the class — it does (`class="deathcard"` in the won card), so leave the CSS; just confirm the death branch region contains no `deathcard`.
  </action>
  <verify>
    <automated>grep -c "function renderCombatOver(host, kind, opts = {})" mazeworld.html && grep -c "function htmlToPlain(html)" mazeworld.html && grep -c 'renderCombatOver(body, "dead"' mazeworld.html && grep -c "if (b.over) {" mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function renderCombatOver(host, kind, opts = {})" mazeworld.html` = 1; `grep -c "function htmlToPlain(html)" mazeworld.html` = 1; `grep -c 'renderCombatOver(body, "dead"' mazeworld.html` = 1; `grep -c "renderCombatOver(body, b.over" mazeworld.html` = 1; `grep -c "renderCombatOver(body, kind" mazeworld.html` = 1.
    - `grep -c 'title: "THEY ARE DOWN"' mazeworld.html` = 1; `grep -c 'title: "THEY STAND DOWN"' mazeworld.html` = 1; `grep -c 'title: "YOU GOT OUT"' mazeworld.html` = 1; `grep -c 'title: "THAT IS THAT"' mazeworld.html` = 1; `grep -c 'oracle: "REVIEW THE ORACLE"' mazeworld.html` = 1; `grep -c 'bury: "BURY THEM"' mazeworld.html` = 1; `grep -c 'takeAll: "TAKE ALL"' mazeworld.html` = 1; `grep -c 'leaveAll: "LEAVE ALL"' mazeworld.html` = 1.
    - `grep -c 'panel.dataset.mode = "dark";' mazeworld.html` = 6 (combat, death, beats-over, loot, joiner, find).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('if (S.dead && !preDeathBeat) {');const b=s.indexOf('if (S.won) {',a);const r=s.slice(a,b);process.exit(!r.includes('deathcard')&&r.includes('wireDeathConfirm();')&&r.includes('\"btn-death-oracle\"')&&r.includes('\"btn-death-confirm\"')?0:1)"` exits 0.
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function renderCombatOver(host, kind, opts = {})');const b=s.indexOf('function renderEncounter()');const r=s.slice(a,b).replace(/\/\/[^\n]*/g,'');process.exit((r.match(/innerHTML/g)||[]).length===0?0:1)"` exits 0 (renderCombatOver has no innerHTML; htmlToPlain sits before it or is the only innerHTML user — place htmlToPlain BEFORE renderCombatOver so this slice excludes it).
    - Loot branch region (from `if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {` to `if (S.pendingJoiner && !S.combat && !S.store) {`) still contains every literal: `window.__mzBagUsage(c)`, `window.__mzLootReport`, `id="loot-list"`, `renderCarriedList(`, `actions: ["lootEquip", "lootTake", "lootLeave"]`, `guard: true`, `subFor`, `window.__mzLootCompare(c, it)`, `id="loot-drop-shelf"`, `renderDropShelf(`, `"a-loot-take-all"`, `"a-loot-leave-all"`, `window.mzTakeAllLoot`, `window.mzLeaveAllLoot`, `Bag full (`.
    - `grep -c "window.__mzFightEnd = null;" mazeworld.html` = 2 (noteCombat fresh fight + the dismissal transition); `grep -c "window.__mzCombatMenu = null;" mazeworld.html` >= 3 (dispatchWithToasts, fightLogRefuse, BACK, dismissal).
    - `grep -c 'document.getElementById("a-next") || document.getElementById("cb-over-btn")' mazeworld.html` = 1; `grep -c '"a-fight"' mazeworld.html` = 0.
    - `grep -c "guard: true" mazeworld.html` = 1; the joiner branch and find branch regions contain `panel.dataset.mode = "dark";` once each.
    - The joiner/find wiring lines are otherwise unchanged: `git diff -U0 -- mazeworld.html | grep -c '^-.*a-join-yes\|^-.*a-find-take'` = 0.
  </acceptance_criteria>
  <done>Win/soothed/flee/death all end on the mock's over-panel with the report, the ending lines, the loot rows where relevant and guarded buttons; joiner/find are dark-styled with identical behaviour; the dismissal transition clears the phase globals.</done>
</task>

<task type="auto">
  <name>Task 2: shell-combat-over.test.js (three variants + voice scan of every new string) and the last re-pins</name>
  <files>test/unit/shell-combat-over.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-input-guards.test.js</files>
  <read_first>
    - test/unit/shell-combat-actions.test.js (Plan 04 — helpers, regions, the COMBAT_COPY leaf extractor and the BANNED matcher to copy)
    - test/unit/shell-loot-screen.test.js lines 170-215 (loot pins to re-run against the new region; the branch-order and joiner-untouched tests)
    - test/unit/shell-input-guards.test.js (GUARDED_IDS after Plan 04; wireDeathConfirm pin; dismissal pin)
    - src/browser/combatMenu.js and src/browser/combatPanel.js (COMBAT_MENU_COPY / COMBAT_PANEL_COPY for the aggregate voice scan)
    - mazeworld.html regions written in Task 1
  </read_first>
  <action>
    1. Create `test/unit/shell-combat-over.test.js` (header: Phase 34 CSCR-07/08/09, Plan 05). Regions (copy `fnRegion(sig)` = signature → next "\nfunction " from the Plan 03/04 tests): `overRegion()` = `fnRegion("function renderCombatOver(host, kind, opts = {})")`; `deathBranch()` = "if (S.dead && !preDeathBeat) {" → "if (S.won) {"; `beatsBranch()` = "if (!S.combat && S.beats && S.beats.groups && S.beats.groups.length) {" → "if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {"; `lootBranch()` = that guard → "if (S.pendingJoiner && !S.combat && !S.store) {"; `joinerBranch()` = that guard → "if (S.pendingFind && !S.combat && !S.store) {"; `findBranch()` = that guard → "if (S.store) {"; `noteCombatRegion()` = "function noteCombat(" → "function hapticForEvents"; `copyLiteral()` = the `const COMBAT_COPY = {` … `\n};` literal. Tests:
       a. Three variants exist in copy: `title: "THEY ARE DOWN"`, `title: "YOU GOT OUT"`, `title: "THAT IS THAT"` (+ `title: "THEY STAND DOWN"`), `bury: "BURY THEM"`, `oracle: "REVIEW THE ORACLE"`, `btn: "BACK TO THE MAZE"` at least 3 times.
       b. renderCombatOver: overRegion contains `cb-over-title`, `cb-over-line`, `cb-over-lines`, `cb-over-actions`, `cb-over-btn`, `htmlToPlain(`, `window.__mzFightEnd`, `guardTap(document.getElementById(b.id), b.onTap)`, `renderCombatHeader(host`, `COMBAT_COPY.headerOver[kind]`, and zero `innerHTML`.
       c. Death (CSCR-07): deathBranch contains `renderCombatOver(body, "dead"`, `"btn-death-oracle"`, `"btn-death-confirm"`, `cls: "dead"`, `wireDeathConfirm();`, `panel.dataset.mode = "dark"`, and no `deathcard`; the wireDeathConfirm region still has `guardTap(btn,` and no `btn.onclick =` (no read-first lock: CODE has zero `AGAIN_LOCK`).
       d. Win/loot (CSCR-07): lootBranch contains `renderCombatOver(body, kind`, `rep.over === "soothed"`, `fillMid`, every Phase 29 literal listed in Task 1's acceptance criteria, `COMBAT_COPY.takeAll`, `COMBAT_COPY.leaveAll`, `COMBAT_COPY.lootHead`, `guard: true` once, `panel.dataset.mode = "dark"`.
       e. Flee / won-without-drops: beatsBranch starts (first statement after `const b = S.beats;`) with `if (b.over) {`, contains `renderCombatOver(body, b.over`, `"cb-over-btn"`, `S.beats = null; renderEncounter();`; noteCombatRegion contains `over: "fled"`, `rep.over =`, `over: rep.over`; the legacy `a-next` dismiss slot code still exists after the over block.
       f. Joiner/find restyle: joinerBranch and findBranch each contain `panel.dataset.mode = "dark";` once, `"a-join-yes"`/`"a-join-no"` resp. `"a-find-take"`/`"a-find-leave"` wired via guardTap, and joinerBranch still contains `head.textContent =` (the DFB-04 textContent rule).
       g. Guards (CSCR-08): the dismissal block `if (encWasActive && !active) {` contains `lastDismissAt = Date.now();`, `window.mzCenterMap?.();`, `window.__mzFightEnd = null;`, `window.__mzCombatMenu = null;` before its first `}`; the engineMove region still has `if (!encounterSettled()) return;` as the next statement after `if (hasActiveEncounter()) return;`; the keydown beats line references `"cb-over-btn"`; no card/body/panel handler in the helpers+renderEncounter region; CODE has zero `transitionend`/`animationend`.
       h. No toasts on endings: the helpers+renderEncounter region contains zero `mzToast`.
       i. Header endings copy: `headerOver: {` has won/soothed/fled/dead keys.
       j. Voice scan of EVERY new string this phase introduced: all double-quoted leaves of COMBAT_COPY (incl. nested objects), every value of COMBAT_MENU_COPY and COMBAT_PANEL_COPY (import them), and the flee beat title "You got out" — each non-empty and clear of BANNED (ALLOWLIST honoured).
       k. Legacy untouched: the won branch still contains `class="deathcard"` and `id="btn-again"`; the store region contains no `guardTap(` and no `data-mode`.
    2. Re-pin `test/unit/shell-loot-screen.test.js`: in "the loot screen card renders the folded report, the shared list, the shelf, and take-all/leave-all" keep every existing assertion and add `assert.match(region, /renderCombatOver\(body, kind/)` and `assert.match(region, /panel\.dataset\.mode = "dark"/)`; if the test's `lootRegion()` end marker is the joiner guard it still works — verify; the `Bag full (` assertion must still pass (the literal is kept).
    3. Re-pin `test/unit/shell-input-guards.test.js`: append `"cb-over-btn"` and `"btn-death-confirm"` is NOT added (it is wired in wireDeathConfirm via a captured `btn` — already covered by its own test) — so GUARDED_IDS gains only `"cb-over-btn"`; note the loot take-all/leave-all ids are now wired inside renderCombatOver through `guardTap(document.getElementById(b.id), b.onTap)` — extend `countGuardedWiring` with a third shape: an id counts as wired when the region contains `id: "<id>"` inside a `buttons: [` array AND `guardTap(document.getElementById(b.id), b.onTap)` exists in the region (comment: Phase 34 — over-panel buttons are wired generically). Keep every other test.
    4. Run `node --test test/unit/shell-combat-over.test.js test/unit/shell-loot-screen.test.js test/unit/shell-input-guards.test.js test/unit/shell-party-camp.test.js test/unit/shell-map-store-polish.test.js`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-combat-over.test.js test/unit/shell-loot-screen.test.js test/unit/shell-input-guards.test.js test/unit/shell-party-camp.test.js test/unit/shell-map-store-polish.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-combat-over.test.js` exits 0 with at least 11 tests.
    - `grep -c '"cb-over-btn"' test/unit/shell-input-guards.test.js` >= 1; `grep -c "renderCombatOver(body, kind" test/unit/shell-loot-screen.test.js` = 1.
    - `grep -c "COMBAT_MENU_COPY" test/unit/shell-combat-over.test.js` >= 1; `grep -c "COMBAT_PANEL_COPY" test/unit/shell-combat-over.test.js` >= 1 (aggregate voice scan).
    - The five suites in the verify command exit 0.
  </acceptance_criteria>
  <done>The three ending variants, the joiner/find restyle, the guard and dismissal invariants, and the whole phase's new copy are pinned and voice-scanned.</done>
</task>

<task type="auto">
  <name>Task 3: Phase 34 executor gate + the deferred Pixel 7 checklist (CSCR-09, CSCR-10)</name>
  <files>mazeworld.html</files>
  <read_first>
    - .planning/phases/34-combat-screen-rebuild/34-02-SUMMARY.md, 34-03-SUMMARY.md, 34-04-SUMMARY.md (their "Human verification (deferred to end of run)" sections — aggregate them into this plan's SUMMARY)
    - .planning/phases/34-combat-screen-rebuild/34-RESEARCH.md "Executor Gate" (the four verified commands) and Pitfall 4 (the `/*` comment-stripper false positive — run the imbalance check)
    - .planning/ROADMAP.md Phase 34 "Success Criteria" 1-6
  </read_first>
  <action>
    Run the full gate and record the exact outputs in the SUMMARY, one item per line:
    1. `npm test` — must print `# fail 0` and a pass count >= 1955 + the new tests − the 13 deleted shell-round-card tests.
    2. `npm run build:www` — exit 0; afterwards the three new modules exist under www/src/browser/ (fightLog.js, combatMenu.js, combatPanel.js).
    3. `git diff --stat -- engine content test/parity` — empty; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` — empty.
    4. `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    5. Comment-stripper hygiene (RESEARCH Pitfall 4): the count of block-comment openers equals the count of closers in mazeworld.html; if unequal, find the glob-like substring in a Phase 34 comment and reword it.
    6. No new packages: `git diff --stat -- package.json package-lock.json` empty; no network font link in mazeworld.html (the Google Fonts host name must not appear).
    7. Toast discipline: exactly one toast call site survives in the module script (the out-of-combat queue in dispatchWithToasts) and the toast host element is still present in the markup (Phase 35 retires it).
    No file changes are expected in this task; if a gate item fails, fix it in mazeworld.html/tests within this plan and re-run. Then write the SUMMARY with the aggregated Pixel 7 checklist (see verification) — this list IS the CSCR-10 plan-side deliverable for this autonomous run; the APK build/install is deliberately not performed here.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "^# fail 0" && npm run build:www && test -z "$(git diff --stat -- engine content test/parity)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` output contains `# fail 0`; `npm run build:www` exits 0; `git diff --stat -- engine content test/parity` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    - `test -f www/src/browser/fightLog.js && test -f www/src/browser/combatMenu.js && test -f www/src/browser/combatPanel.js` exits 0.
    - `grep -o '/\*' mazeworld.html | wc -l` = `grep -o '\*/' mazeworld.html | wc -l`.
    - `git diff --stat -- package.json package-lock.json` prints nothing; `grep -c "fonts.googleapis" mazeworld.html` = 0.
    - `34-05-SUMMARY.md` contains the heading `## Human verification (deferred to end of run)` followed by at least 20 numbered Pixel 7 checks aggregated from Plans 02-05, and an explicit line stating that the debug APK build + adb install happen after Phase 35's last plan.
  </acceptance_criteria>
  <done>The phase-wide gate is green and recorded; CSCR-10's on-device round is queued as a concrete checklist for the milestone-close UAT batch.</done>
</task>

</tasks>

<verification>
- Full gate per Task 3 (npm test `# fail 0`, build:www exit 0, engine/content/parity diff empty, master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0, no new packages, no Google Fonts, toast host intact, one out-of-combat mzToast call).
- SUMMARY contract (deferred UAT — CSCR-10): end `34-05-SUMMARY.md` with `## Human verification (deferred to end of run)` that (a) restates that the debug APK is built and `adb install -r`'d ONCE after Phase 35's last plan lands (mid-wave builds would be stale), (b) aggregates the numbered Pixel 7 checks from 34-02/03/04-SUMMARY.md, and (c) adds this plan's checks: (1) kill the last foe with drops pending — THEY ARE DOWN, the sarcastic line, the report lines (rounds, felled, hp, +N wilmst visible), the killing-blow line, the loot rows with Equip now / Stow / Take / Leave and the bag readout, TAKE ALL / LEAVE ALL; taking or leaving every drop closes the panel and the map is recentred; a step within a quarter second of the close is ignored; (2) kill the last foe with no drops — THEY ARE DOWN with a single BACK TO THE MAZE; (3) talk a group down — THEY STAND DOWN; (4) flee successfully — YOU GOT OUT, the escape roll line, BACK TO THE MAZE; the panel does not close on a map tap; (5) die in combat — THAT IS THAT in red, the death note, floor/day/XP, the epitaph, the last exchanges; REVIEW THE ORACLE opens the Oracle tab and back lands on the same screen; BURY THEM (no second tap needed, but swallowed within 250 ms of render) returns to the title screen; (6) a Joiner offer and a find card appear in the dark styling with the same Take/Leave behaviour; (7) the Through the Gate win card and the store look as before; (8) TalkBack reads the over-panel title and buttons; no toast appears on any ending.
</verification>

<success_criteria>
- ROADMAP success criteria 4 (THEY ARE DOWN with loot rows / YOU GOT OUT / THAT IS THAT with Review the Oracle and Confirm), 5 (every button guarded, settle on dismiss, no map-tap dismiss, announcer once) and 6 (tests re-pinned, build, engine diff, and the Pixel 7 round queued) are source-provable or explicitly deferred per the run's rule.
- Every CONTEXT decision is traceable: Layout/Foes/YOUR LOT (Plan 03), Fight log (Plans 01-02), Actions (Plans 01, 04), Fight! gate (Plan 03), Fight end (this plan), Guards & tests (Plans 02-05), On-device (this plan's SUMMARY).
- Engine/content/parity untouched for the whole phase.
</success_criteria>

<output>
Create `.planning/phases/34-combat-screen-rebuild/34-05-SUMMARY.md` when done — with the aggregated `## Human verification (deferred to end of run)` section (the CSCR-10 deliverable for this run).
</output>
