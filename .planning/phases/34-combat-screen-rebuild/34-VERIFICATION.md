---
phase: 34-combat-screen-rebuild
verified: 2026-09-16T23:30:00Z
status: passed
score: 9/10 requirements verified on automated evidence (CSCR-01..09); CSCR-10 (the Pixel 7 DR round) is the device round itself — deferred to the end-of-run UAT batch per the user's "defer uat to end" instruction
behavior_unverified: 0
overrides_applied: 0
human_verification: ["Pixel 7 (CSCR-04, log): start a fight, tap STRIKE twice — the › log shows the newest line at the TOP, older lines below; no toast appears anywhere during the fight", "Pixel 7 (CSCR-04, dice): tap a 'You hit …' line — a gold dice line appears under it in place (no re-render); tap again — it hides", "Pixel 7 (CSCR-04, refusal): tap a greyed action the engine refuses (e.g. Parley twice) — the refusal appears as a dimmer › line, never a toast", "Pixel 7 (CSCR-08, announcer): with TalkBack on, one new log batch is announced once; opening/closing a submenu does not repeat it", "Pixel 7 (CSCR-04, clear): after a fight ends, the next fight starts with an empty log", "Pixel 7 (CSCR-06, overlay): walk into an encounter — near-black MAJOR OVERLAY with red ●, SOMETHING IS HERE (one foe) / THEY ARE ALREADY HERE (several), the foes named, the d8/d10 roll note, ONE gold FIGHT IT OUT button; nothing resolves before the tap; a tap within 250 ms of the overlay appearing is swallowed", "Pixel 7 (CSCR-01/02/03, round 1): after FIGHT IT OUT — ENCOUNTER header with ROUND 1 and N STANDING, foe cards (glyph, upper-case name, SIZE · INT · note, hp/max, red bar), YOUR LOT strip (hero card gold), › log with 'You move first.' / 'They move first.' on top", "Pixel 7 (CSCR-02, target): tap a second foe card — gold border + TARGET tag; tap a dead card — nothing happens", "Pixel 7 (CSCR-03, joiner): accept a Joiner first — YOUR LOT shows the companion card beside the hero, hint reads YOUR LOT · EACH ROLLS THEIR OWN, strip scrolls sideways when cards exceed the width", "Pixel 7 (CSCR-01): the map and D-pad are never visible beneath the combat screen", "Pixel 7 (CSCR-08, reduced motion): with reduced-motion on, overlay and panel appear without fades and buttons still respond after the 250 ms arm window", "Pixel 7 (CSCR-05, grid): the action band shows PICK YOUR MISTAKE over 1 · STRIKE (die / to-hit range / damage sub-line), 2 · SPELLS or 2 · ABILITIES, 3 · ITEMS, 4 · SOCIAL (red accent), each with its sub-line", "Pixel 7 (CSCR-05, strike): STRIKE resolves at once — a › line appears immediately, no submenu opens", "Pixel 7 (CSCR-05, Magic User): SPELLS lists every known spell with LVL N and blurb, title NAME · SPELLS · N CHARGES; tapping a greyed above-level spell closes the submenu and logs a dull › refusal; BACK returns to the grid", "Pixel 7 (CSCR-05, Bard/Fighter): Bard ABILITIES shows SING READY or N SQ; a Fighter's ABILITIES button is greyed with NOTHING UP YOUR SLEEVE and a tap does nothing", "Pixel 7 (CSCR-05, items): ITEMS lists POTION (N LEFT), SCROLL when carried, every usable carried item with its cooldown; POTION at full health logs the dull 'Already at full health…' line — no toast", "Pixel 7 (CSCR-05, social): SOCIAL lists FLEE (d20, 11+ / WITHDRAW on round 1 when unnoticed) and PARLEY", "Pixel 7 (CSCR-05, keys): with a keyboard connected, 1-4 pick the grid, digits pick submenu rows, Esc/Backspace = BACK; a key within 250 ms of a re-render is swallowed", "Pixel 7 (CSCR-04/08): no toast of any kind appears at any point during a fight", "Pixel 7 (CSCR-07, win+loot): kill the last foe with drops pending — THEY ARE DOWN, sarcastic line, report lines (rounds/felled/hp/+N wilmst), killing-blow line, loot rows with Equip now/Stow/Take/Leave and the bag readout, TAKE ALL / LEAVE ALL; taking or leaving every drop closes the panel and recentres the map; a step within 250 ms of the close is ignored", "Pixel 7 (CSCR-07, win no loot): kill the last foe with no drops — THEY ARE DOWN with a single BACK TO THE MAZE", "Pixel 7 (CSCR-07, parley): talk a group down — THEY STAND DOWN", "Pixel 7 (CSCR-07, flee): flee successfully — YOU GOT OUT, the escape roll line, BACK TO THE MAZE; the panel does not close on a map tap", "Pixel 7 (CSCR-07, death): die in combat — THAT IS THAT in red, death note, floor/day/XP, epitaph, last exchanges; REVIEW THE ORACLE opens the Oracle tab and back lands on the same screen; BURY THEM needs no second tap (but is swallowed within 250 ms of render) and returns to the title screen", "Pixel 7 (CSCR-07, cards): a Joiner offer and a find card appear in the dark styling with the same Take/Leave behaviour", "Pixel 7 (CSCR-07, untouched): the Through the Gate win card and the store look as before", "Pixel 7 (CSCR-08, a11y): TalkBack reads the over-panel title and buttons; no toast appears on any ending"]
gaps: []
---

# Phase 34 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-16)

Goal-backward check of the phase goal: *the combat UX is rebuilt to the imported Claude Design spec — one dark full-screen panel (header, foe cards, YOUR LOT, newest-first › log with tap-to-reveal dice, 2×2 action grid with submenus), the loot/flee/death endings folded into the same screen, the Fight! gate as the map's major overlay, Phase 32 guards intact, engine/parity untouched.*

## Automated evidence (re-run by the orchestrator after 34-05)

- `npm test`: **2047/2047, 0 failures** (1955 at phase start → 1998 after 34-01 → 1999 after 34-02 → 2014 after 34-03 → 2028 after 34-04 → 2047 after 34-05). `npm run build:www` exit 0 (34-05 gate; `www/src/browser/{fightLog,combatMenu,combatPanel}.js` present).
- Engine gate: `git diff --stat 76d7448..HEAD -- engine content test/parity` **empty** across the whole phase; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (unchanged). No new packages (`package.json`/lock diff empty); `grep -c fonts.googleapis mazeworld.html` = 0; `#mw-toast-host` present once; exactly one `window.mzToast?.(` call site (the out-of-combat queue).
- Five plans, five SUMMARY.md files, all `## Self-Check: PASSED`; working tree clean at HEAD `1149c33`.
- Known pre-existing note (34-05 gate D9): raw `/*` vs `*/` counts in `mazeworld.html` are 152/148 both before and after this phase — the three `@capacitor/*` mentions inside `//` comments already documented in Phase 25; the project's `stripComments()` (line comments first) is clean, which is why every source-assertion test passes.

## Success criteria → evidence

| # | Success criterion (ROADMAP) | Evidence |
|---|-----------------------------|----------|
| 1 | Encounter → major overlay (SOMETHING IS HERE / THEY ARE ALREADY HERE, foes named, FIGHT IT OUT); nothing resolves before the tap; tap opens the combat screen at round 1 | `renderMajorOverlay(host, spec)` renders `#mw-major` for `combat.pending` BEFORE any band and returns; spec from `encounterOverlaySpec(S)` (`src/browser/combatPanel.js`); `#mw-major-primary` → `window.mzFight` through `guardTap`; `#a-fight` retired file-wide (`shell-combat-screen.test.js` 15, `shell-fight-gate.test.js` re-pinned). |
| 2 | Every narrative line lands in the › log (newest first, tap reveals dice); every refusal is a dull › entry; no toasts; Oracle complete | `dispatchWithToasts` single `if (wasCombat \|\| inCombat)` writes `window.__mzFightLog` via `toastsForAction(..., {limit: Infinity, withIdx: true})`; `PRIORITY.block` → `dullFightLogLine`; reveal text = `oracleDetailText(narrateEvent(events[idx]))`; DR18 rule scoped to `#enc-body .evt p .roll`; `renderFightLog` newest-first with in-place toggle; `round-card-worst-case` retargeted (log line count = folded count, 400/400 seeds reveal dice); the two shell-side potion refusals moved to `fightLogRefuse`; zero in-combat `mzToast` call sites (`shell-fight-log.test.js` 14, `shell-toast-wiring` re-pinned, `shell-combat-actions.test.js`). |
| 3 | STRIKE resolves at once; SPELLS/ABILITIES, ITEMS, SOCIAL open the right submenu per class and fight; number keys work | `renderActionArea` 2×2 grid `cb-strike/cb-spells/cb-items/cb-social` driven by `combatMenuViewModel(state)` (ABILITIES fallback, NOTHING UP YOUR SLEEVE, per-class rows pinned for Fighter/Bard/Magic User/Thief in `combatMenu.test.js` 11); `COMBAT_DISPATCH` → existing `window.mz*` bridges only; `window.__mzCombatMenu` reset per dispatch; keydown map 1–4 / digits / Esc-Backspace clicks the same guarded buttons (`shell-combat-actions.test.js` 15). |
| 4 | Last foe down → THEY ARE DOWN with loot rows; flee → YOU GOT OUT; death → THAT IS THAT with Review the Oracle / Confirm | `renderCombatOver(host, kind, opts)` for `won` (loot rows via the Phase 29 bridges, TAKE ALL / LEAVE ALL, report + killing-blow lines; pile-empty → map close unchanged), `stand-down`, `fled`, `died` (REVIEW THE ORACLE / BURY THEM via `wireDeathConfirm`, armed, no read-first lock); ending lines carried on `window.__mzFightEnd`; dismissal clears `__mzFightEnd`/`__mzCombatMenu` (`shell-combat-over.test.js` 19, `shell-loot-screen` re-pinned). |
| 5 | Every button guarded (250 ms arm, settle on dismiss); nothing dismisses on a map tap; TalkBack announces a new log line once | Every new tap target (foe cards — previously UNGUARDED, closed here — overlay buttons, grid, submenu rows, BACK, over-panel buttons) goes through `guardTap`; `DISMISS_SETTLE_MS` on `window.move` untouched; no tap-anywhere-to-dismiss; `#enc-round-live` persists across renders and `syncFightLogLive` is seq-gated (`shell-input-guards.test.js` re-pinned to the new ids in 34-03/04/05). |
| 6 | `npm test` green, every shell test re-pinned; `build:www` 0; engine/content/parity diff empty; Pixel 7 DR round | Automated half verified above (2047/2047, build 0, diff empty, hash pinned). `shell-round-card` → `shell-fight-log`; `shell-input-guards`, `shell-fight-gate`, `shell-loot-screen`, `shell-toast-wiring`, `shell-gear-toolbar`, `round-card-worst-case` re-pinned; `foe-effect-chip`, `shell-party-camp` untouched and green. **The DR round is CSCR-10 — deferred** (27 checks in the frontmatter list). |

## Requirements

CSCR-01 ✓ · CSCR-02 ✓ · CSCR-03 ✓ · CSCR-04 ✓ · CSCR-05 ✓ · CSCR-06 ✓ · CSCR-07 ✓ · CSCR-08 ✓ · CSCR-09 ✓ · CSCR-10 ⏭ **Deferred — UAT pending** (the requirement IS the device round; it is satisfied only by the end-of-run Pixel 7 batch, not by this file)

## Decisions recorded by the plans (for Phase 35)

- Fight-log roll detail is sourced through the folding pipeline (`toastsForAction` `withIdx` + `oracleDetailText`) — one folded entry reveals its first constituent event's dice; the Oracle stays the complete record.
- `renderMajorOverlay(host, spec)` is combat-agnostic (`{icon, iconTone, title, line, roll?, primary:{label,onTap}, secondary}`) — Phase 35 calls it for GO DOWN / NOT YET and out-of-combat death without modification.
- There is no engine `retarget` action; targeting stays the shell mutation `S.combat.target = i; renderEncounter()` wrapped in `guardTap`.
- Out-of-combat toasts and `#mw-toast-host` remain until Phase 35's rail replaces them.

## Deferred human verification

The 27 Pixel 7 checks in the frontmatter `human_verification` list (aggregated by 34-05 from the 34-02/03/04/05 SUMMARY sections) join the end-of-run UAT batch per the user's instruction for this autonomous run. The debug APK build + `adb install -r` happen ONCE after Phase 35's last plan — executors keep editing `mazeworld.html` through both phases and a mid-run build would be stale.
