---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 10
type: execute
wave: 4
depends_on:
  - 75-05
  - 75-06
  - 75-07
files_modified:
  - content/mu-chart.js
  - engine/derived.js
  - engine/magic.js
  - engine/combat.js
  - docs/SPELLS.md
  - test/unit/summoner-heal.test.js
  - test/unit/identity-contract.test.js
  - test/unit/magic.test.js
  - tools/readouts/75-10-before.txt
  - tools/readouts/75-10-after.txt
autonomous: true
requirements:
  - RULES-03
must_haves:
  truths:
    - "The Summoner's weakness is a chart data flag, not a name check: MU_CHART.Summoner.healMul is 0.5, and engine/derived.js#healMulFor(sub) reads it (1 for every row without the flag)"
    - "A healing-school spell cast by a Summoner restores floor(amount × 0.5), minimum 1: the Heal and Major Heal branch of castSpell and each per-round Regeneration tick; the rolled dice and their draws are unchanged"
    - "RULES-03 precision: rolled heals of 1, 2, 3, 7, 10 and 30 restore 1, 1, 1, 3, 5 and 15; the halving applies last, after the Cleric bonus and a heal2x race's doubling, so a heal2x Summoner rolling 5 restores 5"
    - "Potions, staff heals and heals cast by anyone else are unaffected; no other sub-class's heal changes"
    - "The healed and regenerated events carry the halved amount (so the Oracle and rail already print it) plus an additive `halved: true` payload when the halving applied; there is no new event type and no new draw"
    - "The identity contract's Summoner 'bad' is re-pinned to the healing weakness"
    - "docs/SPELLS.md's Summoner text states both Phase 75 changes: the offense gate is removed (75-05) and its own healing spells restore half"
    - "No parity fixture moves (magic#heal is cast by a Wizard; no replay site has a Summoner heal), measured; a 200-seed readout is taken before and after"
  artifacts:
    - path: content/mu-chart.js
      provides: "the Summoner row's healMul: 0.5"
      contains: "healMul: 0.5"
    - path: engine/derived.js
      provides: "healMulFor(sub) and applyCasterHealMul(sub, amount)"
      contains: "export function healMulFor"
    - path: test/unit/summoner-heal.test.js
      provides: "the halving, the precision table, the ordering, and the unaffected-heal controls"
  key_links:
    - from: engine/magic.js (the heal branch) and engine/combat.js (the regeneration tick)
      to: engine/derived.js#applyCasterHealMul
      via: "one halving rule for every healing spell the hero casts"
      pattern: "applyCasterHealMul\\("
  prohibitions:
    - statement: "MUST NOT halve potions, staff heals, or heals someone else casts on the Summoner: the weakness is only in the healing the Summoner itself casts"
      requirement_id: RULES-03
      category: fairness
      status: resolved
      verification: test
    - statement: "MUST NOT hide the halving: the amount the Oracle and rail print is the amount actually restored"
      requirement_id: RULES-03
      category: transparency
      status: resolved
      verification: test
---

<objective>
Give the Summoner its new stated weakness (RULES-03, second half): healing spells it casts restore half, floored, with a minimum of 1.

Purpose: with the offense gate removed (75-05), the user ruled on 2026-09-25: "let's make summoner weakness be that healing spells are only half as effective." The summon backfire stays. 75-CONTEXT locks it as a chart data flag, with no new rng, and the Oracle shows the halved amount.

Output: the chart flag, one halving helper used by both heal paths, re-pinned identity, docs, tests, and a before/after readout.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-10 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- **This plan (75-10):** `MU_CHART.Summoner.healMul` 0.5 and `healMulFor`; heal and regeneration halving with the additive `halved` payload; the identity contract's Summoner weakness; the `docs/SPELLS.md` Summoner section; `tools/readouts/75-10-before.txt` / `-after.txt`
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **Scroll reads count.** A Summoner reading a Heal scroll is the caster, so the scroll heals half as well: the rule sits in castSpell's heal branch, which scroll reads use too. Flagged for the user.
- **Regeneration halves per tick.** Its d8 tick is halved each round (floor, minimum 1). Only the hero ever carries c.regen, so only the hero's own casting is halved.
- **Party members never cast heals.** allyCast only casts attack spells, so "any target" in practice means the Summoner itself. A test pins that allyCast has no heal kind, so this cannot drift unnoticed.
- **No voice hint yet.** The halving has no extra line in this plan: the Oracle prints the true, halved number, and the `halved` payload is there for Phase 79's narrative pass (VOX-05) or the Summoner blurb (VOX-04).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/todos/pending/2026-09-25-summoner-weakness-healing-spells-half-as-effective.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-05-SUMMARY.md

<interfaces>
- content/mu-chart.js: the Summoner row after 75-05 (no gate).
- engine/derived.js: `schoolBonus(sub, school)` reads MU_CHART the same way; put the new helpers beside it.
- engine/magic.js castSpell heal branch: `let amt = rollDice(rng, sp.dmg) + (c.sub === "Cleric" ? 3 : 0); if (RACES[c.race].heal2x) amt *= 2; c.wp = Math.min(c.maxWP, c.wp + amt); events.push({ type: "healed", amount: amt, spell: sp.n });`. `drinkPotion` is separate and is not touched.
- engine/combat.js foeTurn head: `if (c.regen) { const r = rng.d(8); if (c.wp < c.maxWP) { c.wp = Math.min(c.maxWP, c.wp + r); events.push({ type: "regenerated", amount: r }); } }`.
- engine/items.js: the Poplar Staff heal is an item effect and is not touched.
- test/unit/identity-contract.test.js: the Summoner entry (75-05 set `bad` to the summon backfire).

**Readout protocol:** record `git rev-parse HEAD` as the plan base before the first edit.
- BEFORE: run `git worktree add --detach <session scratchpad>/75-10-base <plan-base>`. From that directory, start `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, stdout to this worktree's `tools/readouts/75-10-before.txt` (absolute path). Remove the scratch worktree when done.
- AFTER: once every engine edit is committed, run the same command here into `tools/readouts/75-10-after.txt` with run_in_background, and wait for it.
- Commit both. docs/DIFFICULTY-RETUNE.md is 75-13's.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The chart flag and one halving rule on both heal paths</name>
  <files>content/mu-chart.js, engine/derived.js, engine/magic.js, engine/combat.js, test/unit/summoner-heal.test.js, test/unit/magic.test.js</files>
  <read_first>
    - content/mu-chart.js
    - engine/derived.js (schoolGate, schoolBonus and their neighbourhood)
    - engine/magic.js (castSpell's heal branch; readScroll's path into castSpell; drinkPotion)
    - engine/combat.js (the foeTurn regeneration tick; allyCast's spell kinds)
    - test/unit/magic.test.js (the existing heal tests)
  </read_first>
  <behavior>
    - healMulFor("Summoner") is 0.5; healMulFor("Cleric") and healMulFor("Wizard") are 1; healMulFor(undefined) is 1.
    - applyCasterHealMul("Summoner", n) for n = 1, 2, 3, 7, 10, 30 gives 1, 1, 1, 3, 5, 15. For any other sub it returns n unchanged.
    - A Summoner casting Heal with the d10 scripted to 8 restores 4, and healed carries amount 4 and halved true. A Wizard with the same roll restores 8, with no halved key.
    - A heal2x-race Summoner rolling 5 restores 5 (doubled, then halved).
    - A Summoner reading a Heal scroll with an 8 restores 4.
    - A Summoner with c.regen and the regeneration d8 scripted to 7 regains 3 (regenerated amount 3, halved true). A Wizard regains 7.
    - drinkPotion by a Summoner restores its full amount.
    - allyCast's handled kinds include no heal kind.
    - The number of rng draws in each case is identical to a non-Summoner doing the same thing (a counting rng).
  </behavior>
  <action>
1. Record the plan base and start the BEFORE readout.
2. content/mu-chart.js: add `healMul: 0.5` to the Summoner row, with a comment: RULES-03 (Phase 75, user 2026-09-25), the Summoner's weakness is that its own healing spells restore half.
3. engine/derived.js: add and export `healMulFor(sub)` (a finite positive number from the row, else 1) and `applyCasterHealMul(sub, amount)` (the amount unchanged at 1; otherwise `Math.max(1, Math.floor(amount * mul))`), each with a RULES-03 JSDoc.
4. engine/magic.js heal branch: apply it last (after the Cleric bonus and heal2x), then clamp to maxWP exactly as today. `healed` gains `halved: true` only when the amount changed.
5. engine/combat.js regeneration tick: halve the drawn d8 with the same helper, and `regenerated` gains `halved: true` only when it changed. The draw stays where it is.
6. Create test/unit/summoner-heal.test.js with every behaviour above. Re-pin any magic.test.js heal case only if it used a Summoner (it should not).
  </action>
  <verify>
    <automated>node --test test/unit/summoner-heal.test.js test/unit/magic.test.js test/unit/roll-high-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "healMul: 0.5" content/mu-chart.js` equals 1.
    - `grep -v '^\s*//' engine/magic.js engine/combat.js | grep -c "Summoner"` does not rise above its plan-base count (the rule is data-driven: no new sub-class name check).
    - `grep -c "applyCasterHealMul(" engine/magic.js` is at least 1, and `grep -c "applyCasterHealMul(" engine/combat.js` is at least 1.
    - `node --test test/unit/summoner-heal.test.js` passes.
  </acceptance_criteria>
  <done>Every healing spell a Summoner casts restores half, through one data-driven rule, with no new draw.</done>
</task>

<task type="auto">
  <name>Task 2: The identity contract, the docs, fixtures, and the AFTER readout</name>
  <files>test/unit/identity-contract.test.js, docs/SPELLS.md, tools/readouts/75-10-before.txt, tools/readouts/75-10-after.txt</files>
  <read_first>
    - test/unit/identity-contract.test.js (the Summoner entry after 75-05)
    - docs/SPELLS.md (every Summoner mention, including the Phase 40 "offense gate stays 3" line and the school-gate example list)
  </read_first>
  <action>
1. identity-contract.test.js: re-pin the Summoner's `bad` to "healing spells it casts restore half" (a Summoner's Heal on a scripted roll restores floor(n/2), minimum 1). Keep the summon backfire as an extra assertion in the same entry, since the user kept it. Comment: RULES-03 (Phase 75).
2. docs/SPELLS.md: add a "Summoner (Phase 75, RULES-03)" note stating that the offense gate is removed (a level-1 Summoner casts offense) and that its own healing spells restore half (floor, minimum 1); potions and others' heals are unaffected; the summon backfire stays. Mark the earlier "offense gate stays 3" and "Summoner+Freeze (offense gate 3)" lines as superseded by Phase 75, keeping them as history.
3. Measure fixtures: `node --test "test/parity/**/*.test.js"` passes and `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0. A failure is a STOP.
4. Commit, take the AFTER readout and wait. Record both readouts' headline lines (and the Summoner row, if reported) in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/identity-contract.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RULES-03" docs/SPELLS.md` is at least 1.
    - `npm test` reports 0 failures.
    - tools/readouts/75-10-before.txt and tools/readouts/75-10-after.txt both exist and are non-empty.
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
  </acceptance_criteria>
  <done>The Summoner's identity reads "summons may turn on you, and it heals poorly" everywhere the project states it.</done>
</task>

</tasks>

<verification>
- A Summoner's own heals restore half (floor, minimum 1) through a chart flag; potions and others' heals are unchanged; the events carry the true amount.
- The identity contract and docs match; zero fixtures moved; both readouts are committed; npm test is green.
  <human-check>
  Pixel 7, at milestone close: a Summoner casting Heal restores about half what a Cleric's does.
  </human-check>
</verification>

<success_criteria>
The amended RULES-03 holds in full: the Summoner's offense gate is gone (75-05), and its healing spells restore half, which is its stated weakness.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-10-SUMMARY.md` when done. Include the precision table as tested and both readouts' headline lines.
</output>
