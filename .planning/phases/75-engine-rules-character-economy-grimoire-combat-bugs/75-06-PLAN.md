---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 06
type: execute
wave: 2
depends_on:
  - 75-04
files_modified:
  - engine/combat.js
  - engine/magic.js
  - engine/derived.js
  - engine/saveState.js
  - engine/items.js
  - content/spells.js
  - mazeworld.html
  - src/browser/conditionEffects.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - docs/SPELLS.md
  - test/unit/sense-presence.test.js
  - test/unit/bubble-mirror.test.js
  - test/unit/initiative-line.test.js
  - test/unit/magic.test.js
  - test/unit/combat.test.js
  - test/unit/spell-table.test.js
  - test/unit/spell-utility.test.js
  - test/unit/conditions.test.js
  - test/unit/status-chit-combat.test.js
  - test/unit/tuning-bot.test.js
  - test/roundtrip/serialize-rehydrate.test.js
  - tools/readouts/75-06-before.txt
  - tools/readouts/75-06-after.txt
autonomous: true
requirements:
  - RULES-05
  - RULES-14
must_haves:
  truths:
    - "RULES-05: with Sense Presence active (c.senses), resolveInitiative gives the hero the first move outright: senses joins the Foresight / Acute Hearing branch, `why` is 'senses' unless Foresight or Acute Hearing already decided it, and both initiative d20s are still drawn (the draw count is unchanged)"
    - "RULES-05: with senses active on a dark square, combatInDark ('You cannot see what you are fighting') is not emitted, and the hero's weapon crit is allowed in the dark; without senses, both dark rules behave exactly as today"
    - "RULES-05: the initiative verdict for why 'senses' reads in voice ('You felt them coming.'), on both the Oracle and the rail/fight log (the shared initiativeVerdictText)"
    - "RULES-05: the Phase 40 JSDoc on resolveInitiative and docs/SPELLS.md state the new rule"
    - "RULES-14: Bubble is a one-shot mirror: the next blow that reaches the caster's damage pipeline (a foe swing, a pursuit strike, a foe ability bolt or drain) is reflected in full at its attacker through damageFoe with kind 'reflect'; the caster takes none of it; then the bubble pops into a plain 25 hp pool that lasts the rest of that round"
    - "RULES-14: Bubble has no 12-round duration; the armed mirror lasts until a blow lands or the fight ends (endCombat still clears every ward)"
    - "RULES-14: Shield is unchanged (50 hp for 5 rounds, no reflection); the old reflect-the-soaked-share path is removed (greenfield) and no ward carries a reflect key"
    - "RULES-14 boundary: after the pop, a 25-damage blow is fully soaked and shatters the pool, a 26-damage blow lets 1 through, and a 24-damage blow leaves 1 in the pool"
    - "RULES-14 adjacency: the mirrored blow and the popped pool never apply to the same blow; the pool starts on the next blow of that round"
    - "RULES-14 empty: a round in which every swing misses leaves the mirror armed; a Bubble raised out of combat waits for the next fight's first landed blow"
    - "RULES-14 ordering: the mirror is checked before Hardiness, the Fridgian hide, the Pendant's halving and Brace, so the full blow reflects and no single-charge buffer is spent; the popped pool soaks after them, like Shield"
    - "RULES-14 precision: the reflected amount is the blow's full integer damage; the attacker's own natural armor may soak it exactly as any reflect does today (the gated d20 seam)"
    - "RULES-14: the ward condition chip shows an armed Bubble as the ward's own name with 'next hit', and a popped pool as its hp and rounds; an old save's reflecting ward loads as an armed mirror"
    - "No parity fixture moves (no replay site casts a ward or carries senses; the planner's scan found no wardRaised, sensesGained or combatInDark event), measured; a 200-seed bot readout is taken before and after (the bot casts Bubble as a ward opener; it never casts Sense Presence)"
  artifacts:
    - path: engine/combat.js
      provides: "resolveInitiative senses branch; combatInDark and the dark crit ban honour senses; applyFoeDamageToPlayer's mirror and pop; the tail tick skips an armed mirror"
      contains: "mirror"
    - path: content/spells.js
      provides: "the Bubble row: kind ward, mirror true, popPool 25, new txt"
      contains: "popPool: 25"
    - path: engine/magic.js
      provides: "the ward branch raises the armed mirror for a mirror spell and a plain pool otherwise"
    - path: engine/derived.js
      provides: "conditionsOf ward entry for an armed mirror"
    - path: test/unit/sense-presence.test.js
      provides: "initiative, dark line, dark crit and draw-count pins"
    - path: test/unit/bubble-mirror.test.js
      provides: "mirror, pop pool, boundary, ordering, buffers, Shield unchanged, old-save load"
  key_links:
    - from: engine/combat.js#applyFoeDamageToPlayer
      to: engine/foeDamage.js#damageFoe
      via: "the full blow reflected with kind 'reflect'"
      pattern: "kind: \"reflect\""
    - from: engine/derived.js#conditionsOf (ward entry)
      to: mazeworld.html paintConditions ward chip
      via: "the mirror flag picks the 'next hit' detail"
      pattern: "mirror"
    - from: src/browser/narrationLines.js#initiativeVerdictText
      to: src/browser/eventNarration.js combatJoined
      via: "one shared verdict for why 'senses'"
      pattern: "initiativeVerdictText"
  prohibitions:
    - statement: "MUST NOT let Sense Presence decide initiative silently: the verdict names it whenever it is the reason the hero goes first"
      requirement_id: RULES-05
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT change Shield: it stays a 50 hp pool for 5 rounds with no reflection"
      requirement_id: RULES-14
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT let the reflected blow hurt the caster or spend the caster's single-charge protections (the Pendant's halving, Brace)"
      requirement_id: RULES-14
      category: safety
      status: resolved
      verification: test
---

<objective>
Two combat-spell rules:
- RULES-05: Sense Presence delivers what its text promises. The hero always goes first, never sees "You cannot see what you are fighting", and can crit in the dark.
- RULES-14: Bubble stops being a bigger Shield. It reflects the next blow in full at the attacker, then pops into a small pool for the rest of that round.

Purpose:
- A depth-8 Court Mage with Sense Presence up lost initiative 2 vs 17 in the dark and died. Phase 40 read "nothing gets the jump on you" as only waiving the forced foe-first rules.
- The user found Bubble (100 hp for 12 rounds, plus reflection) strictly better than Shield, and chose "reflect the next attack, keep a small pool".

Output: the engine rules, the chip and the lines, docs/SPELLS.md, tests, and a before/after readout.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-06 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- **This plan (75-06):** Sense Presence wins initiative and lifts the dark line and the dark crit ban; Bubble is a one-shot mirror with a 25 hp pop pool; the ward chip and lines; old-save tolerance; `docs/SPELLS.md`; `test/unit/sense-presence.test.js`, `test/unit/bubble-mirror.test.js`; `tools/readouts/75-06-before.txt` / `-after.txt`
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **RULES-05, edge probe: unclassified, reviewed manually.**
  - The reasons still report in their existing order: Foresight, then Acute Hearing, then senses (all three mean "you go first").
  - Sense Presence is a "you" spell, so only the hero benefits; party members' dark rules are untouched.
  - The Darkness phobia still triggers in the dark with senses up: fear is not skill.
  - No replacement line is printed where "cannot see" used to be, because the initiative verdict already names the sense.
- **The pop pool is 25 hp** (the user's recommended value, half of Shield's 50). The readout records its effect; a retune is a separate ruling.
- **What the mirror covers.** It covers exactly the blows Shield soaks today: everything that reaches applyFoeDamageToPlayer. A miss does not pop it. Earthquake's "half to you unless warded" still counts an armed or popped bubble as a ward.
- **Phase 75.1 handoff.** RULES-10's fumbled Bubble scroll lands on a foe. The mirror lives only on the hero's damage pipeline, so 75.1 must add a foe-side mirror in damageFoe.
- **Party members never cast Bubble.** allyCast only casts thrown, weaken and status spells. A test pins this.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/todos/pending/2026-09-23-sense-presence-should-win-initiative-and-lift-the-dark-penal.md
@.planning/todos/pending/2026-09-25-bubble-reflects-the-next-attack-with-a-small-pool.md

<interfaces>
**Phase 74 formatter rule (orchestrator):** Phase 74 has merged. Read its SUMMARYs (74-02 builds src/browser/rollRange.js; 74-03 re-signs the event-driven roll lines in both narration files) before editing a narration file. No line this plan adds prints a check roll, a winning range or a modifier. If one ever must, format it only through src/browser/rollRange.js, roll-high ("17 vs 18–20"), with player-signed modifiers (U+2212 minus); never hand-build one.

Phase 74 has merged before this plan runs. Reference functions by name: 74-01 extracted the to-hit chains into derived.js helpers, 74-03 re-signed the narration roll lines, and 74-07 added src/browser/conditionEffects.js (the hero condition-chip effect text) and its bridge. Read the 74 SUMMARYs for their exact exports.

engine/combat.js:
- `resolveInitiative(state, rng)`: draws `mine` and `theirs` (two d20s, tagged already-high); `forcedFoe = (samurai || slow || knightBig || courtMage) && !foreseen && !c.senses`; `C.first = forcedFoe ? "foe" : foreseen || acuteHearing ? "you" : mine >= theirs ? "you" : "foe"`; `why` chain ends with `else if (c.senses && C.first === "you") why = "senses"`.
- `fight()`: pushes `combatJoined { first, mine, theirs, why?, foe?, senses? }`, then later `if (inDark(state) && !skill(c, "Night Vision")) events.push({ type: "combatInDark" })`.
- `playerStrike`: `const noCrit = c.sub === "Guard" || c.sub === "Soldier" || (inDark(state) && !skill(c, "Night Vision")) || eff(c, "noCrit") > 0;`.
- `applyFoeDamageToPlayer(state, foe, rng, events, { dmg, … })`: in order, Hardiness, hide, `c.halfNext`, Brace, then the ward block. The ward block reflects its soaked share when `c.ward.reflect` (killFoe plus the early return on a reflect kill), pushes wardAbsorbed / wardShattered, then armor soak.
- The foeTurn tail: `if (c.ward && --c.ward.rounds <= 0) { wardFaded; c.ward = null }`.
- `endCombat`: `state.c.ward = null`.

engine/derived.js: `toHit` already waives the dark cap with senses (`inDark && !Night Vision && !c.senses`). `conditionsOf` pushes the ward chip only while `c.ward.pool > 0`.

engine/magic.js:
- ward branch: `c.ward = { pool: sp.pool, rounds: sp.rounds, reflect: !!sp.reflect, name: sp.n }` and `wardRaised { spell, pool, reflect }`;
- Earthquake: `if (!c.ward)` self-damage.

engine/items.js: the Rowan Staff dome sets a ward literal that still carries a false reflect key.

content/spells.js: Shield `{ kind: "ward", pool: 50, rounds: 5 }`; Bubble's row carries the old 100 pool, 12 rounds and a reflect flag.

mazeworld.html: `CONDITION_LABEL.ward = { label: "Shield" }`; paintConditions' ward detail is `${cn.pool} hp · ${cn.remaining} rds`; `CONDITION_EXPLAIN.ward`.

Narration: EVENT_NARRATION and LINE_FOR `wardRaised`, `wardReflected`, `wardAbsorbed`, `wardShattered`, `wardFaded`; `initiativeVerdictText(e)` in narrationLines.js (its "senses" case).

Tests that pin the old Bubble:
- magic.test.js ("castSpell: Bubble sets a reflecting ward pool");
- spell-table.test.js (the Bubble row);
- combat.test.js (a Samurai with a reflecting Bubble ward kills a low-hp foe in the opening foe turn);
- serialize-rehydrate.test.js (the ward sub-state round trip);
- tuning-bot.test.js (HARN-02 ward opener).
test/unit/initiative-line.test.js pins the verdict wording.

**Readout protocol:** record `git rev-parse HEAD` as the plan base before the first edit.
- BEFORE: run `git worktree add --detach <session scratchpad>/75-06-base <plan-base>`. From that directory, start `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, stdout to this worktree's `tools/readouts/75-06-before.txt` (absolute path). Remove the scratch worktree when it finishes.
- AFTER: once every engine edit is committed, run the same command here into `tools/readouts/75-06-after.txt` with run_in_background, and wait for it.
- Commit both. docs/DIFFICULTY-RETUNE.md is 75-13's.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sense Presence wins initiative and lifts both dark rules (RULES-05)</name>
  <files>engine/combat.js, src/browser/narrationLines.js, src/browser/conditionEffects.js, docs/SPELLS.md, test/unit/sense-presence.test.js, test/unit/initiative-line.test.js</files>
  <read_first>
    - engine/combat.js (resolveInitiative and its Phase 40 JSDoc; fight's combatJoined and combatInDark; playerStrike's noCrit line)
    - engine/derived.js (toHit's dark cap; inDark; read only in this task)
    - src/browser/narrationLines.js (initiativeVerdictText)
    - src/browser/conditionEffects.js (the darkness entry, if it states a crit ban)
    - test/unit/initiative-line.test.js
    - docs/SPELLS.md (the Sense Presence rows and "Sense Presence's rules change" section)
  </read_first>
  <behavior>
    - A Court Mage in round 1 with senses wins initiative on the scripted pair (1, 20), with why "senses". Without senses the same pair gives "foe" (the Court Mage rule).
    - A Samurai with senses and a plain hero with senses on (1, 20) both go first with why "senses". Foresight plus senses reports why "foreseen".
    - resolveInitiative draws exactly two dice with and without senses (a counting rng).
    - On a dark tile without Night Vision, fight() emits combatInDark without senses and does not emit it with senses.
    - In the dark with senses, a strike whose roll sits on the weapon's crit face is critical; without senses it is not.
    - initiativeVerdictText({ first: "you", why: "senses" }) contains "You felt them coming." and still says you go first. Every other why case returns exactly today's text.
  </behavior>
  <action>
1. Record the plan base and start the BEFORE readout (see `<interfaces>`).
2. resolveInitiative: add `c.senses` to the "you" branch beside Foresight and Acute Hearing. Keep both draws where they are, and keep the why chain's order. Rewrite the Phase 40 paragraph of the JSDoc: RULES-05, Phase 75 — Sense Presence now wins outright; the draw count is unchanged.
3. fight(): the combatInDark push also requires `!c.senses`. playerStrike: the dark term of noCrit also requires `!c.senses`. Comment both with RULES-05 ("full skill in the dark", matching toHit's existing waiver).
4. narrationLines.js#initiativeVerdictText: the "senses" case becomes "You felt them coming. You go first."
5. conditionEffects.js: if its darkness effect text states a crit ban or a to-hit cap, make it read the same senses waiver (through the engine's own functions, never a re-derived rule). If it states neither, leave it untouched and say so in the SUMMARY.
6. docs/SPELLS.md: update the Sense Presence rule text and the "Sense Presence's rules change" section.
7. Create test/unit/sense-presence.test.js with every behaviour above, and re-pin initiative-line.test.js's senses case.
  </action>
  <verify>
    <automated>node --test test/unit/sense-presence.test.js test/unit/initiative-line.test.js test/unit/roll-high-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "foreseen || acuteHearing || c.senses" engine/combat.js` equals 1.
    - `grep -c "You felt them coming." src/browser/narrationLines.js` equals 1.
    - `node --test test/unit/sense-presence.test.js` passes, including the two-draw count case and the dark-crit case.
    - `node --test test/unit/roll-high-guard.test.js` passes (DRAW_INVENTORY unchanged).
  </acceptance_criteria>
  <done>Sense Presence wins initiative, removes the dark line, allows dark crits, and the Oracle says why.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bubble as a one-shot mirror with a 25 hp pop pool (RULES-14 engine)</name>
  <files>content/spells.js, engine/magic.js, engine/combat.js, engine/items.js, engine/saveState.js, test/unit/bubble-mirror.test.js, test/unit/magic.test.js, test/unit/combat.test.js, test/unit/spell-table.test.js, test/roundtrip/serialize-rehydrate.test.js</files>
  <read_first>
    - content/spells.js (Shield and Bubble rows)
    - engine/magic.js (the ward branch; the Earthquake self-damage check)
    - engine/combat.js (applyFoeDamageToPlayer in full; the foeTurn tail tick; endCombat; allyCast's spell kinds)
    - engine/foeDamage.js (damageFoe with kind "reflect")
    - engine/items.js (the dome case)
    - engine/saveState.js (where c's sub-objects are sanitized on load)
    - the five Bubble-pinning tests named in `<interfaces>`
  </read_first>
  <behavior>
    - Casting Bubble sets c.ward to an armed mirror: { name: "Bubble", mirror: true, pool: 0, popPool: 25, rounds: null }. wardRaised carries mirror true and popPool 25.
    - Casting Shield sets { name: "Shield", pool: 50, rounds: 5 } (no reflect key), exactly the old pool and rounds.
    - A 14-damage foe swing on an armed Bubble: the foe takes 14 through damageFoe(kind "reflect") (a foe with no natural armor), the caster's wp is unchanged, and wardReflected carries mirror true. c.ward is then { name: "Bubble", pool: 25, rounds: 1 }.
    - A reflect that kills the attacker runs killFoe and the swing loop continues to the next foe.
    - After the pop, in the same foe turn: a 25 blow is fully soaked and the pool shatters; a 26 blow lets 1 through; a 24 blow leaves 1. At the tail of that foe turn the pool fades (wardFaded).
    - A foe turn where every swing misses leaves the mirror armed, and the tail tick does not fade it.
    - With c.halfNext true and Brace set, a mirrored blow leaves both still set.
    - A pursuit strike and a foe ability bolt each trigger the mirror.
    - endCombat clears an armed mirror.
    - A Samurai with an armed Bubble facing a low-hp foe in the opening foe turn still kills it by reflection (the re-pinned combat.test.js case).
    - An old save whose c.ward is { pool: 100, rounds: 12, reflect: true, name: "Bubble" } loads as the armed mirror. A saved Shield ward loads without a reflect key. An armed mirror round-trips with rounds null.
  </behavior>
  <action>
1. content/spells.js: rewrite the Bubble row as kind "ward" with `mirror: true` and `popPool: 25`, dropping the old pool, rounds and reflect keys. New txt: "defensive · you · the next blow bounces back at whoever threw it, then a 25 hp film for the rest of that round". Shield's row is untouched.
2. engine/magic.js ward branch: for a mirror spell, raise the armed mirror; otherwise raise `{ pool, rounds, name }`. wardRaised carries `{ spell, pool, ...(mirror ? { mirror: true, popPool } : {}) }`.
3. engine/combat.js#applyFoeDamageToPlayer:
   - At the very top, before Hardiness, if the ward is an armed mirror and dmg > 0: reflect the whole dmg at `foe` through damageFoe with `{ kind: "reflect", crit: false }`, push wardReflected with mirror true and the applied amount when not soaked, and replace c.ward with `{ name, pool: popPool, rounds: 1 }`.
   - On a reflect kill, call killFoe and keep the existing early-return contract. In every case return `{ died: false, onArmour: false, applied: 0 }`.
   - Delete the old reflect-the-soaked-share branch in the ward block; the remaining ward block (absorb, shatter) handles Shield and the popped pool.
4. The foeTurn tail tick decrements rounds only when rounds is a number, so an armed mirror never fades there.
5. engine/items.js: drop the false reflect key from the dome's ward literal.
6. engine/saveState.js: on load, a c.ward with reflect true becomes the armed mirror (popPool from the SPELLS Bubble row); any other ward loses a reflect key. Comment it: tolerant load, RULES-14.
7. Add a RULES-14 (Phase 75, user 2026-09-25) comment at each site. Create test/unit/bubble-mirror.test.js with every behaviour above; also pin that allyCast never casts a ward kind. Re-pin the five old Bubble tests to the new shapes (never delete one).
  </action>
  <verify>
    <automated>node --test test/unit/bubble-mirror.test.js test/unit/magic.test.js test/unit/combat.test.js test/unit/spell-table.test.js test/roundtrip/serialize-rehydrate.test.js test/unit/roll-high-guard.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' engine/combat.js | grep -c "c.ward.reflect"` equals 0.
    - `grep -v '^\s*//' engine/magic.js engine/items.js | grep -c "reflect:"` equals 0.
    - `grep -c "popPool: 25" content/spells.js` equals 1, and the Shield row still reads `pool: 50, rounds: 5`.
    - `node --test test/unit/bubble-mirror.test.js` passes, with the boundary cases 24, 25 and 26 and the untouched-buffer case.
    - `node --test test/unit/roll-high-guard.test.js` passes (the reflect uses the existing damageFoe seam; no untagged draw).
  </acceptance_criteria>
  <done>Bubble reflects one full blow, pops into a 25 hp pool for the round, and Shield is exactly as before.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The ward chip, the lines and the docs; measure fixtures; take the AFTER readout (RULES-14 surfaces)</name>
  <files>engine/derived.js, mazeworld.html, src/browser/conditionEffects.js, src/browser/eventNarration.js, src/browser/narrationLines.js, docs/SPELLS.md, test/unit/conditions.test.js, test/unit/spell-utility.test.js, test/unit/status-chit-combat.test.js, test/unit/tuning-bot.test.js, test/unit/bubble-mirror.test.js, tools/readouts/75-06-before.txt, tools/readouts/75-06-after.txt</files>
  <read_first>
    - engine/derived.js (conditionsOf's ward entry and its JSDoc)
    - mazeworld.html (CONDITION_LABEL.ward, CONDITION_EXPLAIN.ward, paintConditions' ward detail branch)
    - src/browser/conditionEffects.js (its ward entry, if any)
    - src/browser/eventNarration.js and src/browser/narrationLines.js (wardRaised, wardReflected)
    - docs/SPELLS.md (the Bubble row and the kind table's ward row)
    - test/unit/conditions.test.js, test/unit/spell-utility.test.js, test/unit/status-chit-combat.test.js (ward chip pins)
  </read_first>
  <behavior>
    - conditionsOf with an armed mirror returns a ward entry { key: "ward", polarity: "good", pool: 0, name: "Bubble", mirror: true }. A popped pool returns today's shape (pool, remaining, name). No ward, or a shattered one, returns no entry.
    - The shell chip for an armed Bubble reads its name with "next hit". A Shield chip reads its pool and rounds exactly as today.
    - EVENT_NARRATION.wardRaised with mirror reads like "A bubble shimmers around you. The next blow goes back where it came from." A Shield raise reads its points (no "reflecting").
    - wardReflected with mirror reads like "The bubble catches it and sends it back — N to <foe>. Pop." and mentions the film for the rest of the round. Without mirror, it reads today's text.
    - The rail twins say the same in their one-line form.
  </behavior>
  <action>
1. engine/derived.js#conditionsOf: push the ward entry when the pool is positive OR the ward is an armed mirror, adding `mirror: true` for the armed case. Update the JSDoc bullet.
2. mazeworld.html: the ward chip label reads the ward's own name (cn.name), falling back to "Shield". The detail is "next hit" for a mirror, else today's pool/rounds text. Give CONDITION_EXPLAIN.ward (or conditionEffects.js's ward entry, whichever leads the tap card after 74-07) a mirror branch: "The next blow that lands goes back to whoever threw it; then a small pool for the rest of the round."
3. Narration: update wardRaised and wardReflected in both tables as the behaviour list says. Keep the voice family-friendly and deadpan, and keep HP not WP.
4. docs/SPELLS.md: update the Bubble row, the ward kind row, and add a short "Bubble's rules change (RULES-14, Phase 75)" note (the one-shot mirror, the 25 hp pop pool, Shield unchanged, the 75.1 handoff).
5. Re-pin the chip and bot tests that assert the old Bubble. Add the behaviour cases to bubble-mirror.test.js (or conditions.test.js for the conditionsOf case).
6. Measure fixtures: `node --test "test/parity/**/*.test.js"` passes and `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0. If a parity test fails, STOP and report it: 75-05 owns this wave's declarations.
7. Commit, take the AFTER readout and wait. Record both readouts' headline lines in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/bubble-mirror.test.js test/unit/conditions.test.js test/unit/spell-utility.test.js test/unit/status-chit-combat.test.js test/unit/tuning-bot.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "next hit" mazeworld.html` is at least 1.
    - `grep -v '^\s*//' src/browser/eventNarration.js | grep -c "e.reflect"` equals 0.
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
    - tools/readouts/75-06-before.txt and tools/readouts/75-06-after.txt both exist and are non-empty.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The armed bubble is visible and narrated, the docs match the rule, zero fixtures moved, and the nerf is measured.</done>
</task>

</tasks>

<verification>
- Sense Presence: first move, no dark line, dark crits allowed; the draw count is unchanged.
- Bubble: one full reflection, then a 25 hp pool for the round; Shield unchanged; old saves load tolerantly.
- Zero fixtures moved (measured); npm test and parity are green; both readouts are committed.
  <human-check>
  Pixel 7, at milestone close:
  - Cast Sense Presence and walk into a fight on a dark square: "you go first", no "cannot see" line.
  - Cast Bubble: the next hit bounces back and the bubble pops; a small pool soaks the rest of that round, then it is gone.
  </human-check>
</verification>

<success_criteria>
ROADMAP criteria 3 (Sense Presence always wins initiative, never "cannot see", crits in the dark) and 8 (Bubble reflects the next attack and pops, leaving a small pool for the round; no longer a bigger Shield) hold.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-06-SUMMARY.md` when done. Include the new lines' wording, the conditionEffects.js decision, the fixture measurement, both readouts' headline lines, and the 75.1 handoff note.
</output>
