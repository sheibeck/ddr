---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 08
type: execute
wave: 3
depends_on:
  - 75-01
  - 75-02
  - 75-05
  - 75-06
files_modified:
  - src/browser/combatBeat.js
  - src/browser/combatPanel.js
  - mazeworld.html
  - engine/encounters.js
  - engine/movement.js
  - test/unit/trap-death-repro.test.js
  - test/unit/combat-beat.test.js
  - test/unit/combat-beat-shell.test.js
  - test/unit/hp-surface-guard.test.js
  - .planning/debug/trap-death-21hp-oracle-minus1.md
autonomous: true
requirements:
  - RULES-06
must_haves:
  truths:
    - "The fix implements exactly the cause(s) 75-01 documented under '### Fix inputs' in .planning/debug/trap-death-21hp-oracle-minus1.md, and each of 75-01's todo-marked 'RULES-06 cause:' tests passes as an ordinary test (its todo flag removed, its assertion unchanged)"
    - "If 75-01's verdict is 'no reproduction', no production code changes and this plan lands only the two standing guards"
    - "Every visible hero-HP readout (the top HUD #mw-hud-wp, the YOUR LOT hero card, the combat-over panel) equals state.c.wp at every point the player can act, after every beat-ending path: natural settle, hurry, tab switch, flee, a kill, a superseded dispatch, the over-panel dismiss and reduced motion (test/unit/hp-surface-guard.test.js)"
    - "No intermediate beat frame ever shows the hero more hp than the engine's running hp through that frame's events, so the display never overstates survival"
    - "Engine guard: across a seed sweep from a dev start at depth 2, every trapSprung's dmg equals the hp that trap removed, and a hero dies to a trap only when their hp before the trap was at most its dmg; every hp loss on a move step is narrated with its true amount"
    - "RULES-06 boundary: a hero at dmg + 1 hp survives a trap of dmg with exactly 1 hp; a hero at exactly dmg hp dies, and the line prints dmg"
    - "The debug session is closed: status resolved, a Phase 75 resolution naming the fix, the files changed and the verification, and a human check for the milestone device round"
  artifacts:
    - path: test/unit/hp-surface-guard.test.js
      provides: "the shell-level HP-surface guard across all beat-ending paths, plus the intermediate-frame never-overstates guard"
    - path: test/unit/trap-death-repro.test.js
      provides: "75-01's cause tests, now passing; the engine trap/hazard guard; the boundary cases"
    - path: .planning/debug/trap-death-21hp-oracle-minus1.md
      provides: "the closed session with the Phase 75 resolution"
      contains: "status: resolved"
  key_links:
    - from: .planning/debug/trap-death-21hp-oracle-minus1.md (### Fix inputs)
      to: this plan's Task 1
      via: "the category and file select the branch"
      pattern: "### Fix inputs"
    - from: test/unit/hp-surface-guard.test.js
      to: mazeworld.html paint() / src/browser/combatPanel.js#yourLotViewModel / src/browser/combatBeat.js
      via: "every readout compared with state.c.wp"
      pattern: "mw-hud-wp"
  prohibitions:
    - statement: "MUST NOT fix anything 75-01 did not document, and MUST NOT guess: a fix outside the documented cause, or outside this plan's files, is a stop-and-report"
      requirement_id: RULES-06
      category: integrity
      status: resolved
      verification: test
    - statement: "MUST NOT hide or soften the trap's own damage line to make the symptom go away: the Oracle keeps printing the true hp removed"
      requirement_id: RULES-06
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT let any on-screen HP readout show more hp than the hero really has at a moment the player can act"
      requirement_id: RULES-06
      category: safety
      status: resolved
      verification: test
---

<objective>
Fix RULES-06 exactly as 75-01's root-cause session documented, then land two standing guards so that a trap the Oracle reports as "−1 HP" can never again surprise-kill a hero who seemed to have 21.

Purpose: ROADMAP criterion 4 requires the fix to follow the debug session, not a guess. This plan's first task therefore branches on the documented finding. The guards hold whatever the finding was:
- every visible HP readout equals engine truth wherever the player can act;
- every hp loss is narrated with its true amount.

Output: the fix (if any), the passing cause tests, the two guards, and the closed debug session.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-08 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- **This plan (75-08):** the RULES-06 fix from 75-01's Fix inputs; `test/unit/hp-surface-guard.test.js` (every visible HP readout equals state HP wherever the player can act) and the hazard-narration guard
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **Files owned here.** This plan owns the display files the 2026-09-22 session pointed at (combatBeat.js, combatPanel.js, mazeworld.html) and the two engine files a trap step runs through (encounters.js, movement.js). Scheduling guarantees no other plan in this wave touches them. A documented cause in any other file (for example the narration tables, which 75-01's predecessor already cleared) cannot be fixed here safely: the executor stops and reports it, and the orchestrator schedules it.
- **Guard runtime.** The engine guard's seed sweep is sized to run in under 60 seconds inside npm test. The full 500-seed reproduction stays a scratch tool, as in 75-01.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/debug/trap-death-21hp-oracle-minus1.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-01-SUMMARY.md

<interfaces>
75-01's outputs are the inputs here: the debug file's `### Verdict` and `### Fix inputs` sections (category, file#function, assertion, test name for each cause) and test/unit/trap-death-repro.test.js (the `{ todo: true }` "RULES-06 cause:" tests). Read them first. They decide Task 1's branch.

Candidate surfaces:
- src/browser/combatBeat.js: `planBeat`, `heroFrames`, `foeFrames`, `lineIdxsFor`, `viewFor`, `createBeatRunner`. The 2026-09-22 fix pins the last hero-hp frame to after.c.wp. Its recorded blind spots were intermediate-frame under-counts on multi-swing folds and the foeFrames mirror.
- src/browser/combatPanel.js#yourLotViewModel: the YOUR LOT hero card.
- mazeworld.html: `paint()` (#mw-hud-wp), `renderEncounter`, `renderYourLot`, the over-panel dismiss, the window.__mzBeat bridge.
- engine/encounters.js#springTrap and engine/movement.js#move: the trap step and the per-step drains.

Test technique: test/unit/combat-beat-shell.test.js with test/unit/harness/shellSandbox.js and a fake clock (the real beat runner against the classic shell).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the documented root-cause fix (branch on 75-01's finding)</name>
  <files>src/browser/combatBeat.js, src/browser/combatPanel.js, mazeworld.html, engine/encounters.js, engine/movement.js, test/unit/trap-death-repro.test.js, test/unit/combat-beat.test.js</files>
  <read_first>
    - .planning/debug/trap-death-21hp-oracle-minus1.md (### Verdict and ### Fix inputs)
    - .planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-01-SUMMARY.md
    - test/unit/trap-death-repro.test.js (the todo cause tests)
    - every file the Fix inputs name, in full around the named function
  </read_first>
  <behavior>
    - Each "RULES-06 cause:" test from 75-01 passes with its todo flag removed and its assertion unchanged.
    - Boundary: a hero with dmg + 1 hp survives a trap of dmg with exactly 1 hp; a hero with exactly dmg hp dies with cause "trap", and the trapSprung line prints dmg.
  </behavior>
  <action>
1. Record the plan base. Read the Fix inputs, then take the matching branch for each documented cause:
   - **Display** (combatBeat.js, combatPanel.js or mazeworld.html): fix the named function so that the readout it feeds equals engine truth. For example, if the cause is intermediate-frame under-counting on a folded line, compute the hero frame from the cumulative dmg of every event the line represents, not only its first. Keep the Oracle, rail and fight-log text byte-identical: the fold contract is shared with other surfaces.
   - **Engine** (encounters.js or movement.js): fix the named path so that every hp loss is narrated with its true amount and applied once. Then measure fixtures (`node --test "test/parity/**/*.test.js"` with no fixture edits). A moved fixture is a STOP.
   - **Any other file, or narration:** STOP. Commit nothing for that cause and return a checkpoint that quotes the Fix inputs entry. The orchestrator schedules it.
   - **Verdict "no reproduction":** make no production change; go to Task 2.
2. Remove the todo flag from each fixed cause test, leaving its assertion untouched. Add the boundary cases to test/unit/trap-death-repro.test.js. Add a comment at each fix naming RULES-06 (Phase 75) and the debug file.
3. Run the beat, shell and parity suites.
  </action>
  <verify>
    <automated>node --test test/unit/trap-death-repro.test.js test/unit/combat-beat.test.js test/unit/combat-beat-shell.test.js "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "todo: true" test/unit/trap-death-repro.test.js` equals 0 (every cause is fixed; a stopped cause is reported, not left as todo).
    - `node --test test/unit/trap-death-repro.test.js` passes, with the two boundary cases.
    - `git diff --stat <plan-base>` touches no file outside this plan's files_modified list.
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
  </acceptance_criteria>
  <done>Every documented cause is fixed and proven by the test 75-01 wrote before the fix existed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The two standing guards, and close the debug session</name>
  <files>test/unit/hp-surface-guard.test.js, test/unit/trap-death-repro.test.js, test/unit/combat-beat-shell.test.js, .planning/debug/trap-death-21hp-oracle-minus1.md</files>
  <read_first>
    - test/unit/combat-beat-shell.test.js and test/unit/harness/shellSandbox.js
    - .planning/debug/trap-death-21hp-oracle-minus1.md (the Phase 75 section's beat-path audit table)
    - tools/lib/tuning-bot.mjs (the per-action loop, for the engine sweep)
  </read_first>
  <behavior>
    - For each of the eight beat-ending paths, with fights whose last round folds a two-swing foe and a three-foe landing: after the beat, #mw-hud-wp and the YOUR LOT hero hp both equal state.c.wp.
    - For each frame of a multi-line round, the frame's hero hp is never greater than the engine hp after the last event that frame represents.
    - Engine sweep (a small seed set, under 60 s): for every trapSprung, the hero's hp dropped by exactly dmg at that event. A trap death only happens when hp before the trap was at most dmg. The hp change of every action equals its narrated losses minus its narrated gains, counting only events that carry an amount.
  </behavior>
  <action>
1. Create test/unit/hp-surface-guard.test.js with the shell-level behaviours, reusing the combat-beat-shell technique. Copy the small helpers; do not import from another test file.
2. Add the engine sweep to test/unit/trap-death-repro.test.js. Drive the real engine with the bot's policy from `newRun(seed, [], { startDepth: 2 })` over enough seeds and actions to meet at least 20 trap springs while staying under 60 seconds. Assert every engine behaviour above.
3. Close the debug file:
   - set `status: resolved` and bump `updated`;
   - add a `### Resolution (Phase 75)` subsection with the root cause(s), the fix, the files changed, the verification commands and results, and the two guards;
   - add a human check for the milestone device round.
4. Run the whole suite.
  </action>
  <verify>
    <automated>node --test test/unit/hp-surface-guard.test.js test/unit/trap-death-repro.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/hp-surface-guard.test.js` passes, with at least 8 path cases.
    - The engine sweep in test/unit/trap-death-repro.test.js asserts at least 20 trapSprung events (the test fails if fewer occur, so it cannot pass vacuously).
    - `grep -c "^status: resolved" .planning/debug/trap-death-21hp-oracle-minus1.md` equals 1, and `grep -c "### Resolution (Phase 75)" .planning/debug/trap-death-21hp-oracle-minus1.md` equals 1.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>A displayed HP can never overstate survival again, every hp loss is narrated truly, and the session is closed with evidence.</done>
</task>

</tasks>

<verification>
- 75-01's documented causes are fixed and their tests pass; nothing undocumented was changed.
- Both standing guards are green; the debug session is resolved; npm test and parity are green.
  <human-check>
  Pixel 7, at milestone close: a floor-2 trap at about 21 HP never kills on a "−1 HP" line. After a fight in which a foe swings twice, the YOUR LOT card and the top HP bar agree with each other.
  </human-check>
</verification>

<success_criteria>
ROADMAP criterion 4 holds: a trap the Oracle reports as "−1 HP" can never kill the hero, and the fix followed an explicit root-cause session.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-08-SUMMARY.md` when done. Name the branch taken for each cause and the guard results.
</output>
