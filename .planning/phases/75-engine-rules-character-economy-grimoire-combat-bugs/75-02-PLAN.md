---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/encounters.js
  - engine/difficulty.js
  - test/unit/hp-growth-linear.test.js
  - test/unit/encounters.test.js
  - test/unit/class-mitigation.test.js
  - tools/readouts/75-02-before.txt
  - tools/readouts/75-02-after.txt
autonomous: true
requirements:
  - RULES-01
  - RULES-02
must_haves:
  truths:
    - "RULES-01 (verify, then pin): two '+25 HP' Table-4 pulls on one hero grow maxWP by exactly two flat steps of dotHpFor('large'), never ×1.6 each, at identity dials (25 a step) and at the shipped dials (HERO_HP_SCALE 1.25: 31 a step)"
    - "RULES-01: the toll row removes exactly dotHpFor('mid') whatever the hero's maxWP (a 40-hp hero and a 392-hp hero both lose 15 at identity, 19 shipped), so it never takes a share of an inflated pool"
    - "RULES-01 boundary: zero, one, two and three '+25 HP' pulls add exactly 0, 1, 2 and 3 steps"
    - "RULES-01 adjacency: two pulls in consecutive actions sum exactly; a pull made while Strength's temporary max-HP boost is live survives the next day's reset as exactly one step, and casting Strength again while boosted does not double it again"
    - "RULES-01 empty: a hero on 1 hp takes the toll's flat amount, the line prints that amount, and the hero dies through die(state, 'maze'); a pull on a hero at the smallest maxWP adds the same flat step"
    - "RULES-01 ordering: a pull then a toll, and a toll then a pull, leave the same maxWP and the same hp"
    - "RULES-01 precision: dotHpFor is Math.round(DOT_HP_BASE[kind] × HERO_HP_SCALE) (13 / 19 / 31 at 1.25, half-up) and always an integer of at least 1; no fraction of maxWP enters it. The faerie's +d20/−d10 base-HP rows and every level-up gain are flat amounts that never read the hero's own maxWP"
    - "RULES-01: no engine code changes unless one of the pins above fails against master; if one does, the compounding path is fixed and named in the SUMMARY"
    - "RULES-02: WILMST_CACHE_PER_DEPTH is 100, still paid through lootFor(WILMST_CACHE_PER_DEPTH × depth) with no new rng draw"
    - "RULES-02 boundary: at identity dials a depth-1 cache pays 100 and a depth-20 cache pays 2000; at the shipped LOOT_SCALE 0.8 they pay 80 and 1600"
    - "RULES-02 adjacency: a cache and a chest on the same floor pay independently; a Pickpocket's take still rides on top through gainWilmst's own draw, unchanged"
    - "RULES-02 empty: a hero at the bag's wilmst cap is clamped by gainWilmst exactly as today"
    - "RULES-02 ordering and idempotency: two caches on one floor pay the same amount each, and replaying the same action from the same state pays the same amount (the non-Pickpocket path draws no rng: fakeRng([]) passes)"
    - "RULES-02 precision: the amount is lootFor's Math.round of an integer product, always an integer"
    - "RULES-02 concurrency: the payout is a pure function of depth and the loot dial, with no stored state and no rng, so a re-run or interleaved action can never pay a different amount"
    - "The dev start-at-depth purse (engine/state.js) keeps reading WILMST_CACHE_PER_DEPTH and follows the cut (100 × start depth); engine/state.js itself is not edited"
    - "No parity fixture moves (the planner's exposure scan: the encounters#tablefour scenario, seed 3, rolls the '+25 HP' row, not the cache); measured, not assumed"
    - "A 200-seed bot readout is taken before and after the cut and committed as tools/readouts/75-02-before.txt and 75-02-after.txt"
  artifacts:
    - path: test/unit/hp-growth-linear.test.js
      provides: "the RULES-01 pins at identity and shipped dials, plus the faerie, level-up and Strength non-compounding pins"
    - path: engine/encounters.js
      provides: "WILMST_CACHE_PER_DEPTH = 100, with its ECON-09 comment updated for RULES-02"
      contains: "WILMST_CACHE_PER_DEPTH = 100"
    - path: tools/readouts/75-02-before.txt
      provides: "the 200-seed readout at the plan base"
    - path: tools/readouts/75-02-after.txt
      provides: "the 200-seed readout after the cut"
  key_links:
    - from: engine/encounters.js#tableFour ("wilmst cache" row)
      to: engine/difficulty.js#lootFor
      via: "gainWilmst(state, lootFor(WILMST_CACHE_PER_DEPTH × depth), 'tableFour', …)"
      pattern: "lootFor\\(WILMST_CACHE_PER_DEPTH"
    - from: engine/state.js#newRun (dev start purse)
      to: engine/encounters.js#WILMST_CACHE_PER_DEPTH
      via: "the one constant; the dev purse follows the cut"
      pattern: "WILMST_CACHE_PER_DEPTH \\* startAt"
  prohibitions:
    - statement: "MUST NOT cut any other purse: the kill purse, chests, the faerie's gold and LOOT_SCALE keep their values; only the cache row's own constant changes (the user deferred releasing LOOT_SCALE)"
      requirement_id: RULES-02
      category: values
      status: resolved
      verification: test
    - statement: "MUST NOT change max HP by an amount the line does not state: every Table-4 HP line prints the exact amount it added or removed"
      requirement_id: RULES-01
      category: transparency
      status: resolved
      verification: test
---

<objective>
Pin the Table-4 HP dots as linear (RULES-01) and cut the red-dot wilmst cache from 300 × depth to 100 × depth (RULES-02).

Purpose:
- RULES-01: USER RULING G already made the dots a flat canon value × HERO_HP_SCALE (`dotHpFor(kind)`), so the ×1.6 compounding the user saw should be gone. 75-CONTEXT says verify on master, pin it with a regression test at the dials that actually ship, and change code only if a pin fails.
- RULES-02: 75-CONTEXT locks a flat cut (`WILMST_CACHE_PER_DEPTH` 300 → 100), still through `lootFor`, with no new draw.

Output: the RULES-01 regression test, the one-constant cut with its re-pinned tests, and a before/after 200-seed readout.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-02 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- **This plan (75-02):** `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; `tools/readouts/75-02-before.txt` / `-after.txt`
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **The cache vs a chest.** A chest pays (d10+6) × 100 × depth / 10: 70 to 160 × depth, 115 on average. At the user's locked 100 × depth a cache pays a little under an average chest, and a faerie's flat d10 × 100 still beats it on floors 1–3. 75-CONTEXT's "the cache stays the best single find (≈ a good chest)" is therefore only roughly true. The user's number stands. The device check ("a red-dot cache on floors 1–3 pays less than a store-tier reset") holds. Flagged for the device round.
- **The dev start-at-depth purse** is defined as "the cache row" (Phase 21 D-13), so it follows the cut: a dev start at depth N now gets 100 × N. The default readout starts at depth 1 and never reaches that path.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/todos/pending/2026-09-21-table-4-hp-dots-compound-max-hp-geometrically-regression.md
@.planning/todos/pending/2026-09-21-red-dot-wilmst-cache-still-pays-far-too-much.md

<interfaces>
- engine/difficulty.js: `DOT_HP_BASE = { small: 10, mid: 15, large: 25 }`; `dotHpFor(kind)` returns the base at HERO_HP_SCALE 1, else `Math.max(1, Math.round(base × HERO_HP_SCALE))`; `lootFor(coin)`; `setDialsForTuning(overrides)` and the shipped `DIALS` (HERO_HP_SCALE 1.25, LOOT_SCALE 0.8).
- engine/encounters.js#tableFour: "+25 HP" does `c.maxWP += dotHpFor("large"); c.wp += n`; "-15 HP" does `c.wp -= dotHpFor("mid")`; the "wilmst cache" row calls `gainWilmst(state, lootFor(WILMST_CACHE_PER_DEPTH * state.floor.depth), "tableFour", rng, events)`; `die(state, "maze")` at wp ≤ 0.
- engine/encounters.js#meetFaerie: "+d20 Base HP" adds `rng.d(20)`; "-d10 Base HP" subtracts `rng.d(10)` (floor 5).
- engine/character.js#checkLevel: each level adds `heroMaxWpFor(gain)`, a flat rolled amount.
- engine/magic.js (the might branch): Strength sets `c.strengthBoost = c.maxWP` once (guarded by `!c.strengthBoost`) and adds it; engine/movement.js#newDay subtracts it.
- test/unit/harness/identityDials.js: `setIdentityDials` / `withIdentity`. Unit tests run at identity by default; a shipped-dial case must read the shipped DIALS (for example through `setDialsForTuning({})` inside a try/finally that restores identity) and must never leak dial state to other tests.
- The current pins: test/unit/encounters.test.js ("the 'wilmst cache' row pays a depth-scaled amount": 300 at depth 1, 2100 at depth 7) and test/unit/class-mitigation.test.js (its table-four cache case).

**Readout protocol (orchestrator standing rule):** record `git rev-parse HEAD` as the plan base before the first edit.
- BEFORE: run `git worktree add --detach <session scratchpad>/75-02-base <plan-base>`. From that directory, start `node tools/tune-difficulty.mjs --seeds=200` with run_in_background, redirecting stdout to this worktree's `tools/readouts/75-02-before.txt` (absolute path). It takes about 15 minutes. When it finishes, run `git worktree remove --force` on the scratch worktree.
- AFTER: once every engine edit is committed, run the same command in this worktree into `tools/readouts/75-02-after.txt` with run_in_background, and wait for it.
- Never read a readout while it is still running. Commit both files. The ledger entry in docs/DIFFICULTY-RETUNE.md is 75-13's job; this plan does not edit the ledger.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Start the BEFORE readout, then pin Table-4 HP growth as linear (RULES-01)</name>
  <files>test/unit/hp-growth-linear.test.js, engine/encounters.js, engine/difficulty.js</files>
  <read_first>
    - engine/difficulty.js (DOT_HP_BASE, dotHpFor, lootFor, setDialsForTuning, the DIALS HERO_HP_SCALE and LOOT_SCALE entries)
    - engine/encounters.js (tableFour, meetFaerie)
    - engine/character.js (checkLevel's HP gain)
    - engine/magic.js (the might branch) and engine/movement.js (newDay's strengthBoost reset)
    - test/unit/encounters.test.js (its existing tableFour ±HP test and helpers)
    - test/unit/harness/identityDials.js
  </read_first>
  <behavior>
    - At identity: 0/1/2/3 "+25 HP" pulls on a maxWP-40 hero give maxWP 40/65/90/115. At shipped dials they give 40/71/102/133.
    - The toll takes exactly 15 at identity and 19 shipped, from a 40-maxWP hero and from a 392-maxWP hero alike.
    - A pull then a toll, and a toll then a pull, leave the same maxWP and wp.
    - A hero on 1 hp who takes the toll dies with cause "maze", and the toll line prints 15 (identity).
    - A faerie "+d20 Base HP" with a scripted 12 adds exactly 12 to maxWP whatever the hero's maxWP is. A level-up adds the rolled gain, independent of how many pulls came before.
    - Strength cast twice while boosted leaves maxWP unchanged by the second cast. A "+25 HP" pull made during the boost survives newDay's reset as exactly one step.
    - dotHpFor at shipped dials returns 13, 19 and 31, each an integer of at least 1.
  </behavior>
  <action>
1. Record the plan base, then start the BEFORE readout per the protocol in `<interfaces>`. Continue while it runs.
2. Create test/unit/hp-growth-linear.test.js covering every behaviour above. Use the fixedState/fakeRng patterns encounters.test.js uses (copy the small local helpers, do not import them from another test file). Put each shipped-dial case inside a try/finally that restores identity dials.
3. Run it against master. If every pin passes, make no engine change: RULES-01 is verified and pinned. If a pin fails, fix only that compounding path in engine/encounters.js or engine/difficulty.js so it adds a flat amount that never reads the hero's own maxWP, and name the path in the SUMMARY. A compounding path in any other file is a STOP: report it rather than editing a file this plan does not own.
  </action>
  <verify>
    <automated>node --test test/unit/hp-growth-linear.test.js test/unit/encounters.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/hp-growth-linear.test.js` passes, with at least 8 test cases, including at least one shipped-dial case and one identity case.
    - `grep -c "setIdentityDials\|withIdentity\|finally" test/unit/hp-growth-linear.test.js` is at least 1 (dial state is restored).
    - The BEFORE readout was started from a worktree at the plan base (the SUMMARY records the worktree path and the base SHA).
  </acceptance_criteria>
  <done>RULES-01 is verified on master and pinned at both dial settings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut the wilmst cache to 100 × depth (RULES-02), measure the fixtures, take the AFTER readout</name>
  <files>engine/encounters.js, test/unit/encounters.test.js, test/unit/class-mitigation.test.js, tools/readouts/75-02-before.txt, tools/readouts/75-02-after.txt</files>
  <read_first>
    - engine/encounters.js (the ECON-09 comment block above WILMST_CACHE_PER_DEPTH; tableFour's "wilmst cache" case)
    - engine/state.js (the dev start-at-depth purse; read only)
    - engine/items.js (gainWilmst: the Pickpocket draw and the bag cap)
    - test/unit/encounters.test.js (the cache test) and test/unit/class-mitigation.test.js (its table-four cache case)
  </read_first>
  <behavior>
    - At identity, a depth-1 cache pays 100 and a depth-7 cache pays 700, through goldGained with source tableFour and no redundant tableFour beat. fakeRng([]) is enough (no draw).
    - At shipped dials a depth-1 cache pays 80 and a depth-20 cache pays 1600.
    - Two caches on one floor pay the same amount each.
    - A chest's payout at the same depth is unchanged by this plan.
  </behavior>
  <action>
1. In engine/encounters.js set `WILMST_CACHE_PER_DEPTH` to 100. Add a "RULES-02 (Phase 75, user 2026-09-25)" paragraph to the ECON-09 comment: the flat cut; still through lootFor; no new draw; the dev start purse follows. Leave the tableFour case and lootFor untouched.
2. Re-pin the cache cases in test/unit/encounters.test.js (100 and 700) and test/unit/class-mitigation.test.js. Add the shipped-dial and two-caches cases from the behaviour list to encounters.test.js.
3. Measure the fixtures: `node --test "test/parity/**/*.test.js"` must pass, and `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` must exit 0. If a parity test fails, STOP: do not regenerate or declare anything. Report the failing site and its replay evidence (the planner's scan predicts zero: encounters#tablefour rolls "+25 HP").
4. Commit, then run the AFTER readout per the protocol and wait for it. Record in the SUMMARY: both readouts' headline lines (median floor, reach bands, gold on hand if reported), the fixture measurement, and the dev-purse note.
  </action>
  <verify>
    <automated>node --test test/unit/encounters.test.js test/unit/class-mitigation.test.js test/unit/hp-growth-linear.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const WILMST_CACHE_PER_DEPTH = 100;" engine/encounters.js` equals 1.
    - `grep -c "lootFor(WILMST_CACHE_PER_DEPTH \* state.floor.depth)" engine/encounters.js` equals 1 (the call shape is unchanged).
    - `git diff <plan-base> -- engine/difficulty.js engine/state.js engine/items.js` shows no change to lootFor, LOOT_SCALE, the chest formula, gainWilmst or the dev purse line (engine/difficulty.js changes only if Task 1 found compounding).
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
    - tools/readouts/75-02-before.txt and tools/readouts/75-02-after.txt both exist and are non-empty.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The cache pays 100 × depth (through lootFor), no fixture moved, and the readout shows the economy effect.</done>
</task>

</tasks>

<verification>
- RULES-01 is pinned at identity and shipped dials; engine code changed only if a pin failed.
- RULES-02 is one constant with re-pinned tests; zero fixtures moved (measured); both readouts are committed.
- `npm test` and `node --test "test/parity/**/*.test.js"` are green; the prototype master is untouched.
  <human-check>
  Pixel 7, at milestone close:
  - Pull "+25 HP" twice on one hero: max HP grows by two equal steps.
  - A red-dot cache on floors 1–3 pays less than a store-tier reset.
  </human-check>
</verification>

<success_criteria>
ROADMAP criterion 1 (HP dots linear; the toll from the non-inflated pool) holds and is pinned. Criterion 2's cache clause (a bounded ~100 × depth cut) holds, with its economy effect measured.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-02-SUMMARY.md` when done. Include the RULES-01 verdict (verified or fixed, and which path), the fixture measurement, and both readouts' headline lines.
</output>
