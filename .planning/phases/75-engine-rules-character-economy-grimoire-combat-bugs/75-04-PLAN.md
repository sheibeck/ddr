---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/items.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - src/browser/gearSheet.js
  - test/unit/ailment-narration.test.js
  - test/unit/destroyed-armor-swap.test.js
  - test/unit/gear-sheet-model.test.js
autonomous: true
requirements:
  - RULES-07
  - RULES-08
must_haves:
  truths:
    - "RULES-07: an ailment roll of 5 or 6 (AFFLICTIONS rows 5–6: kind 'Disease', phobia: true) narrates a fear of the mind in voice, never a bare 'Disease.', on both the Oracle (EVENT_NARRATION.afflictionRolled) and the rail (LINE_FOR.afflictionRolled); the existing phobiaAcquired line ('A new fear settles in: …') still follows"
    - "RULES-07: the real Disease and Poison rows (1–4, 7, 8) keep naming their kind exactly as today"
    - "RULES-07: the engine and content are unchanged (canon kept): the narration reads the row through AFFLICTIONS[e.roll − 1].phobia"
    - "RULES-08: on every armor-replacing path (equipItem's armor branch, takeLoot's equip-now armor branch, takeItem's store armor branch), when the worn piece being replaced is destroyed (armor set and not 'Nothing', ar > 0, armorWP ≤ 0) the existing event gains an additive payload { discarded: { kind: 'armor', n, armor, ar, left: 0 }, destroyed: true }; no new event type and no rng"
    - "RULES-08: the discarded descriptor is built by one private helper that unequipSlot's destroyed branch also uses, so the unequip event stays byte-identical"
    - "RULES-08: the Oracle and rail lines for a destroyed-replacement swap say the old piece was destroyed and left behind, pairing in voice with the existing itemUnequipped destroyed line; a swap over a live piece reads exactly as today"
    - "RULES-08: on the Gear sheet, a WORN armor slot's SWAP FOR row, when the worn piece is destroyed, says the worn armor is destroyed and will be discarded; the BAG card's SWAP INTO ARMOR row keeps its existing '{name} is scrap. It stays behind.' note"
    - "No parity fixture moves (no fixture dispatches an armor swap over destroyed armor; the harness's legacy takeItem auto-take discards events); measured"
  artifacts:
    - path: src/browser/eventNarration.js
      provides: "afflictionRolled phobia-row wording; itemEquipped/itemTaken destroyed clauses"
      contains: "phobia"
    - path: src/browser/narrationLines.js
      provides: "the rail twins of the same lines"
    - path: engine/items.js
      provides: "the destroyedArmorPiece helper and the additive discarded/destroyed payload on the three paths"
      contains: "destroyed: true"
    - path: src/browser/gearSheet.js
      provides: "the worn-slot SWAP FOR destroyed note"
    - path: test/unit/ailment-narration.test.js
      provides: "rolls 5, 6 and one real Disease row, both tables"
    - path: test/unit/destroyed-armor-swap.test.js
      provides: "one test per armor-replacing path, payload and line"
  key_links:
    - from: src/browser/eventNarration.js#afflictionRolled and narrationLines.js#afflictionRolled
      to: content/afflictions.js#AFFLICTIONS
      via: "the row's own phobia flag, read by roll"
      pattern: "AFFLICTIONS\\["
    - from: engine/items.js#equipItem / #takeLoot / #takeItem
      to: engine/items.js (destroyed-piece helper shared with unequipSlot)
      via: "one descriptor for every destroyed-armor event"
      pattern: "destroyed: true"
  prohibitions:
    - statement: "MUST NOT announce a Disease the hero does not get: rows 5–6 never print 'Disease' as what happened"
      requirement_id: RULES-07
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT make light of mental illness or use frightening or adult framing: the mind-row line stays family-friendly deadpan about the hero's nerve, not a diagnosis"
      requirement_id: RULES-07
      category: values
      status: resolved
      verification: judgment
    - statement: "MUST NOT let a destroyed piece vanish silently on a swap, and MUST NOT claim it went back into the bag"
      requirement_id: RULES-08
      category: transparency
      status: resolved
      verification: test
---

<objective>
Two honesty fixes that tell the player what actually happened:
- RULES-07: an ailment roll of 5–6 stays canon (a disease of the mind that gives a phobia), but the narration now says so instead of printing "Disease.".
- RULES-08: swapping new armor over a destroyed piece now says the old piece was destroyed and left behind.

Purpose: the v1.2 "nothing happens silently" rule. The device reports: "it says disease, I never got disease, but a new phobia instead"; and old armor "dropped instead of going into the bag", which was actually a destroyed piece swapped out silently.

Output: narration changes for both, the additive engine payload on the three armor-replacing paths, the Gear sheet note, and tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-04 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- **This plan (75-04):** honest ailment 5–6 lines; the additive `discarded`/`destroyed` payload on `itemEquipped`/`itemTaken`; the paired destroyed-swap lines; the Gear sheet worn-slot note; `test/unit/ailment-narration.test.js`, `test/unit/destroyed-armor-swap.test.js`
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **RULES-07, edge probe: unclassified, reviewed manually.** The only input is the d8 roll. Rows 5 and 6 are the phobia rows; any other roll keeps its kind. An out-of-range or missing roll falls back to today's wording ("Something has its hooks in you"), so a malformed event never prints a false diagnosis.
- **RULES-08, edge probe: unclassified, reviewed manually.** The three paths are the only places armor replaces armor:
  - takeFind never equips armor (it bags it);
  - the store's strictly-better gate still compares against the destroyed piece's AR (unchanged; not in scope);
  - the Cloak of Armor's magic plate is not a worn piece and never reaches the destroyed branch.
  A swap made in combat is already refused by the Phase 61 gear lock before any payload is built.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/todos/pending/2026-09-23-ailment-roll-5-6-narrates-disease-but-gives-a-phobia.md
@.planning/todos/pending/2026-09-23-narrate-a-destroyed-armor-piece-when-new-armor-replaces-it.md

<interfaces>
**Phase 74 formatter rule (orchestrator):** Phase 74 has merged. Read its SUMMARYs (74-02 builds src/browser/rollRange.js; 74-03 re-signs the event-driven roll lines in both narration files) before editing a narration file. No line this plan adds prints a check roll, a winning range or a modifier. If one ever must, format it only through src/browser/rollRange.js, roll-high ("17 vs 18–20"), with player-signed modifiers (U+2212 minus); never hand-build one.

- content/afflictions.js `AFFLICTIONS` (d8): rows 5 and 6 are `{ dur: "permanent", loss: null, phobia: true, kind: "Disease" }`; rows 2 and 8 are real Disease rows; rows 1, 3, 4 and 7 are Poison.
- engine/encounters.js#rollAffliction emits `afflictionRolled { roll, kind }`, then `phobiaAcquired { name }` for rows 5–6 (no afflictionCaught). Not edited.
- src/browser/eventNarration.js `afflictionRolled`: "Something is wrong with you. <span class=\"roll\">The die turns up N.</span> <kind>." The roll clause lives inside the span so a stripped line still reads cleanly; keep that convention. This file already imports from content/.
- src/browser/narrationLines.js `LINE_FOR.afflictionRolled`: "Something is wrong with you: <kind>."
- engine/items.js:
  - `wornArmorItem(c)` returns null for a destroyed piece (armorWP ≤ 0).
  - `unequipSlot`'s destroyed branch builds `{ kind: "armor", n, armor, ar, left: 0 }` and pushes `itemUnequipped { item, slot: "armor", destroyed: true }`.
  - `equipItem`'s armor branch pushes `itemEquipped { item, slot: "armor" }`.
  - `takeLoot(i, true)`'s armor branch pushes `itemEquipped { item, slot: "armor" }`.
  - `takeItem`'s armor branch pushes `itemTaken { item, ...(replaced ? { replaced } : {}) }`.
  - Every gated path calls `refuseGear` first (Phase 61).
- Narration pairs: EVENT_NARRATION.itemEquipped / itemUnequipped / itemTaken (eventNarration.js) and LINE_FOR.itemEquipped / itemUnequipped / itemTaken (narrationLines.js). The existing destroyed-unequip lines are "You peel off what is left of your <n> and leave it where it falls. The bag declines the honor." (Oracle) and "<n> comes off in pieces. Nothing worth bagging." (rail).
- src/browser/gearSheet.js#gearSheetModel: the WORN-target branch builds SWAP FOR candidates with `candidate(card, slot, "swap")`, whose sub is `cmp.line` (lootCompare). The BAG-target branch already uses `GEAR_SHEET_COPY.sub.scrap` for SWAP INTO ARMOR over a destroyed piece. `armorDisplay(c).destroyed` is the flag.
- test/unit/armor-durability.test.js ("equipItem: swapping onto destroyed armor leaves no bag copy…", "unequipSlot: destroyed armor … is gone") is the template for the new tests. Put the new tests in the new file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Honest ailment 5–6 narration (RULES-07)</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/ailment-narration.test.js</files>
  <read_first>
    - content/afflictions.js
    - engine/encounters.js (rollAffliction; read only)
    - src/browser/eventNarration.js (afflictionRolled and its span convention comment; phobiaAcquired)
    - src/browser/narrationLines.js (LINE_FOR.afflictionRolled, phobiaAcquired, the imports block)
    - test/voice/safety-scan.test.js (how narration strings are scanned)
  </read_first>
  <behavior>
    - EVENT_NARRATION.afflictionRolled({ roll: 5, kind: "Disease" }) keeps the "The die turns up 5." clause inside the roll span, then a mind line (for example "Not your body — your nerve."), and contains no "Disease". Roll 6 behaves the same way.
    - EVENT_NARRATION.afflictionRolled({ roll: 2, kind: "Disease" }) and ({ roll: 8, kind: "Disease" }) still end "Disease."; ({ roll: 1, kind: "Poison" }) still ends "Poison.".
    - LINE_FOR.afflictionRolled for rolls 5 and 6 returns a rail text with the mind wording and no "Disease"; roll 2 still says "Disease".
    - With roll missing or 0, both tables fall back to today's wording and never print a diagnosis the row does not give.
    - Stripping the roll span from the roll-5 Oracle line leaves a clean sentence.
  </behavior>
  <action>
1. In both narration tables, read the row as `AFFLICTIONS[e.roll - 1]` (import AFFLICTIONS from content/afflictions.js, or from content/index.js if that is how the file already imports content). When the row's `phobia` is true, replace the kind clause with one in-voice mind line, for example "Not your body — your nerve.", keeping the family-friendly deadpan voice. Never mock mental illness. Otherwise print the kind exactly as today.
2. Keep the Oracle line's roll clause inside its span exactly as the convention comment requires. Add a comment naming RULES-07 (Phase 75, user kept canon 2026-09-25: rows 5–6 are a disease of the mind that gives a phobia; the words now say so).
3. Create test/unit/ailment-narration.test.js covering every behaviour above.
  </action>
  <verify>
    <automated>node --test test/unit/ailment-narration.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/ailment-narration.test.js` passes, with cases for rolls 5, 6, 2, 8 and 1 in both tables.
    - `git diff --stat <plan-base> -- engine/encounters.js content/afflictions.js` prints nothing.
    - `node --test test/voice/safety-scan.test.js` passes.
  </acceptance_criteria>
  <done>Rows 5–6 read as a fear, and the real Disease and Poison rows are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The destroyed-armor payload on every armor-replacing path (RULES-08 engine)</name>
  <files>engine/items.js, test/unit/destroyed-armor-swap.test.js</files>
  <read_first>
    - engine/items.js (wornArmorItem, equipItem's armor branch, unequipSlot's destroyed branch, takeLoot's equip armor branch, takeItem's armor branch, refuseGear)
    - test/unit/armor-durability.test.js (the destroyed-unequip and destroyed-swap tests: state builders to copy)
  </read_first>
  <behavior>
    - equipItem over a destroyed worn Studded piece: the itemEquipped event carries destroyed true and a discarded descriptor naming Studded with left 0; the bag has no copy of Studded; no rng is drawn.
    - takeLoot(i, true) with armor over a destroyed piece: same payload on itemEquipped; the pile loses the item; no bag copy.
    - takeItem (a store armor delivery) over a destroyed piece: itemTaken carries the payload and no `replaced`.
    - The same three paths over a live (armorWP > 0) worn piece: no discarded or destroyed keys, and today's events stay deep-equal to before.
    - unequipSlot over a destroyed piece: its itemUnequipped event is deep-equal to today's.
    - In combat, each path is refused by the gear lock first and carries no payload.
  </behavior>
  <action>
1. Add a module-private helper, `destroyedArmorPiece(c)`, that returns the `{ kind: "armor", n, armor, ar, left: 0 }` descriptor when the worn piece is destroyed (armor set and not "Nothing", ar > 0, armorWP ≤ 0), else null. Refactor unequipSlot's destroyed branch to build its item with this helper (its event must stay byte-identical).
2. In equipItem's armor branch, takeLoot's equip armor branch and takeItem's armor branch, read the helper BEFORE the equip mutation. When it returns a piece, spread `{ discarded: piece, destroyed: true }` onto the existing event. No new event type, no rng, and no change to any state mutation.
3. Add a RULES-08 comment (Phase 75, Phase 25 additive-payload pattern; pairs with unequipSlot's destroyed flag).
4. Create test/unit/destroyed-armor-swap.test.js covering every behaviour above.
  </action>
  <verify>
    <automated>node --test test/unit/destroyed-armor-swap.test.js test/unit/armor-durability.test.js test/unit/loot-pile.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function destroyedArmorPiece" engine/items.js` equals 1, and `grep -c "destroyedArmorPiece(c)" engine/items.js` is at least 4 (unequipSlot plus the three paths).
    - `node --test test/unit/destroyed-armor-swap.test.js` passes, with at least one test per path and one live-piece control per path.
    - `node --test test/unit/roll-high-guard.test.js` passes (no draw added).
  </acceptance_criteria>
  <done>Every armor-replacing path reports a destroyed outgoing piece through one shared descriptor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The paired lines and the Gear sheet note (RULES-08 surfaces); measure fixtures</name>
  <files>src/browser/eventNarration.js, src/browser/narrationLines.js, src/browser/gearSheet.js, test/unit/destroyed-armor-swap.test.js, test/unit/gear-sheet-model.test.js</files>
  <read_first>
    - src/browser/eventNarration.js (itemEquipped, itemUnequipped, itemTaken)
    - src/browser/narrationLines.js (LINE_FOR.itemEquipped, itemUnequipped, itemTaken)
    - src/browser/gearSheet.js (GEAR_SHEET_COPY.sub, the WORN-target candidate builder, the BAG-target SWAP INTO branch)
    - test/unit/gear-sheet-model.test.js (the "Bag armor card over destroyed worn armor" test and its builders)
  </read_first>
  <behavior>
    - EVENT_NARRATION.itemEquipped with destroyed true and discarded.n "Leather" says the old Leather was already in pieces and is left behind, reusing the unequip line's voice (for example "Your old Leather was already in pieces. You leave it where it fell.").
    - LINE_FOR.itemEquipped with the same payload names the new piece and that the old one is gone.
    - EVENT_NARRATION.itemTaken and LINE_FOR.itemTaken with the payload say the same; the shopkeeper clause does not appear (there is no `replaced`).
    - Every one of these without the payload returns exactly today's string.
    - gearSheetModel for the WORN armor slot with a destroyed worn piece and a fitting bag armor card: the SWAP FOR row's sub states the worn armor is destroyed and will be discarded, followed by the comparison line. With a live worn piece the sub is exactly today's.
  </behavior>
  <action>
1. Add the destroyed clause to itemEquipped and itemTaken in both narration tables, keyed on `e.destroyed` with `e.discarded`. Put the new lines beside the existing itemUnequipped destroyed lines so the two read as a pair. Keep the no-payload strings byte-identical.
2. In gearSheet.js add a frozen `GEAR_SHEET_COPY.sub.discarded` (75-CONTEXT's wording: "Your worn armor is destroyed — it will be discarded."). In the WORN-target branch, when the slot is armor and `armorD.destroyed`, prefix each SWAP FOR candidate's enabled sub with it. A greyed candidate keeps its refusal reason.
3. Extend the tests with every behaviour above.
4. Measure fixtures: `node --test "test/parity/**/*.test.js"` must pass with `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exiting 0. If a parity test fails, STOP and report it; do not declare or regenerate anything.
  </action>
  <verify>
    <automated>node --test test/unit/destroyed-armor-swap.test.js test/unit/gear-sheet-model.test.js "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "discarded" src/browser/gearSheet.js` is at least 2.
    - `node --test test/unit/gear-sheet-model.test.js` passes, including the existing BAG-side scrap test unchanged and a new WORN-side destroyed test.
    - `git diff --quiet <plan-base> -- test/parity/fixtures test/parity/prototype-master.js.txt` exits 0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>A destroyed piece is always named on a swap, in the Oracle, on the rail and on the Gear sheet.</done>
</task>

</tasks>

<verification>
- The ailment 5–6 words match canon; the destroyed-armor swap is narrated on all three paths; the Gear sheet warns before the swap.
- Additive payloads only; no new event type; no rng; zero fixtures moved (measured). npm test is green.
  <human-check>
  Pixel 7, at milestone close:
  - An ailment roll of 5–6 reads as a fear, not a disease.
  - Swapping destroyed armor for a new piece says the old piece is gone.
  </human-check>
</verification>

<success_criteria>
ROADMAP criterion 5 holds: an ailment roll of 5–6 narrates what it gives, and replacing destroyed armor always says the old piece was destroyed and is gone.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-04-SUMMARY.md` when done. Include the final wording of every new line and the fixture measurement.
</output>
