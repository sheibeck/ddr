---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 13
type: execute
wave: 6
depends_on:
  - 75-01
  - 75-02
  - 75-03
  - 75-04
  - 75-05
  - 75-06
  - 75-07
  - 75-08
  - 75-09
  - 75-10
  - 75-11
  - 75-12
files_modified:
  - docs/DIFFICULTY-RETUNE.md
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/divergence-records.test.js
  - tools/readouts/75-13-final.txt
  - tools/voice-sample-output.txt
autonomous: true
requirements:
  - RULES-01
  - RULES-02
  - RULES-03
  - RULES-04
  - RULES-05
  - RULES-06
  - RULES-07
  - RULES-08
  - RULES-12
  - RULES-13
  - RULES-14
  - RULES-15
must_haves:
  truths:
    - "A final 200-seed readout at the phase head is committed as tools/readouts/75-13-final.txt"
    - "docs/DIFFICULTY-RETUNE.md gains '## v2.1 engine rules (Phase 75) — bot readouts', inserted immediately before the still-last '## v1.2 retune (Phase 27)' H2. For each balance-moving plan (75-02 cache, 75-05 Summoner gate and grant legality, 75-06 Bubble, 75-09 wielded staves, 75-10 Summoner healing, 75-12 rations and tiles) it records the change, the BEFORE and AFTER headline lines verbatim from that plan's committed readouts, and a one-paragraph reading; it closes with the phase BEFORE (75-02's before) against the phase FINAL, read against the depth-20 unicorn and floor 5–7 average target"
    - "If the phase moved the average run ending out of floors 5–7, the ledger flags it for a user ruling (engine changes go to the user under the checkpointed fit protocol); this plan does not retune any dial"
    - "The LOOT_SCALE dial-table row no longer claims the wilmst cache rides LOOT_SCALE alone: the cache is its own 100 × depth constant (RULES-02)"
    - "test/parity/FIXTURE-INVENTORY.md's '## Phase 75' section is completed: 75-05's moved set plus a measured-zero subsection for every other plan that touched engine or content (75-02, 04, 06, 07, 08, 09, 10, 12), each citing the plan SUMMARY's measurement commands and results"
    - "test/parity/divergence-records.test.js's Phase 75 guard also proves the measured zeros by replaying all 31 sites: no site casts a ward or Sense Presence, emits combatInDark, meets a wandering monster or tileResumed, wields a staff, has an unfed day with spent charges, or heals as a Summoner, and the declared Phase 75 set is still exactly RULES75_EXPECTED_HOLDERS"
    - "tools/voice-sample-output.txt is regenerated so the new lines (ailment rows 5–6, destroyed-armor swaps, the senses verdict, the bubble, notWielded, booksKept, tileResumed) are in the human voice skim"
    - "The whole-phase gates hold at the head: npm test is 0 failures; the parity suite is green; the prototype master hash is unchanged; the roll-high guard, the Phase 72 direction tests and the Phase 73 state pins pass unedited by this plan"
  artifacts:
    - path: docs/DIFFICULTY-RETUNE.md
      provides: "the Phase 75 readout H2 (per-plan BEFORE → AFTER plus the phase BEFORE → FINAL)"
      contains: "## v2.1 engine rules (Phase 75) — bot readouts"
    - path: test/parity/FIXTURE-INVENTORY.md
      provides: "the completed Phase 75 section"
      contains: "## Phase 75"
    - path: test/parity/divergence-records.test.js
      provides: "the Phase 75 exposure guard"
      contains: "RULES75_EXPECTED_HOLDERS"
    - path: tools/readouts/75-13-final.txt
      provides: "the phase-head 200-seed readout"
  key_links:
    - from: docs/DIFFICULTY-RETUNE.md (the Phase 75 H2's fenced blocks)
      to: tools/readouts/75-*-*.txt
      via: "readout-compare --recorded proves every recorded block is in its source readout"
      pattern: "readout-compare"
    - from: test/parity/divergence-records.test.js (Phase 75 guard)
      to: every Phase 75 rule's trigger event
      via: "an exposure replay over all 31 sites"
      pattern: "Phase 75"
  prohibitions:
    - statement: "MUST NOT hand-type, round or paraphrase a readout number: every recorded block is copied from a committed readout file and machine-checked"
      requirement_id: RULES-02
      category: integrity
      status: resolved
      verification: test
    - statement: "MUST NOT retune a dial or change engine behaviour in the close-out: an off-target readout is reported for a user ruling, never silently fixed"
      requirement_id: RULES-03
      category: values
      status: resolved
      verification: test
---

<objective>
Close Phase 75's engine gate: record every balance change against its readout, complete the fixture inventory with its standing guard, refresh the voice sample, and prove the whole-phase gates at the head.

Purpose: the standing engine gate requires moved fixtures to be declared (75-05 did, the others measured zero) and a before/after readout wherever a rule moves balance. Each balance plan committed its own readouts rather than writing the shared ledger, so parallel waves never collided on it. This plan writes the ledger once, from those files, and takes the final phase readout.

Output: the DIFFICULTY-RETUNE.md Phase 75 section, the completed FIXTURE-INVENTORY section and guard, the final readout and the regenerated voice sample.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-13 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- **This plan (75-13):** the consolidated `## Phase 75` FIXTURE-INVENTORY summary and the phase exposure guard; the `docs/DIFFICULTY-RETUNE.md` Phase 75 readout H2; `tools/readouts/75-13-final.txt`; `tools/voice-sample-output.txt`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-02-SUMMARY.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-05-SUMMARY.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-06-SUMMARY.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-09-SUMMARY.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-10-SUMMARY.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-12-SUMMARY.md

<interfaces>
- docs/DIFFICULTY-RETUNE.md: the last three H2s are "## v2.1 roll-direction pass (Phase 72) — bot readouts", "## v2.1 roll-high mirror (Phase 73) — bot readout" and "## v1.2 retune (Phase 27) — TUNE-05..07", which must stay LAST (test/unit/difficulty-retune-ledger.test.js). Use the Phase 72 and 73 H2s as the format (Change under measurement / Parameters / BEFORE / AFTER / Reading, with fenced readout blocks under "### …" headings). The LOOT_SCALE row of the dial table mentions the wilmst cache.
- tools/readout-compare.mjs: `--recorded <doc> "<heading prefix>" <file>` checks that the first fenced block after a heading is an in-order subsequence of the file; `--exact <a> <b>` checks line-for-line equality.
- Readout files: tools/readouts/75-02-, 75-05-, 75-06-, 75-09-, 75-10- and 75-12-{before,after}.txt.
- test/parity/divergence-records.test.js: `replaySiteEvents`, `collectRecords`, `RULES75_EXPECTED_HOLDERS` (added by 75-05), the JOIN-02 and ROLL-01 exposure tests (the model for a 31-site replay).
- test/parity/FIXTURE-INVENTORY.md: "## Phase 75: engine rules (RULES-01..15) — measured per plan", with 75-05's "### Plan 05" subsection.
- tools/voice-sample.mjs: regenerates tools/voice-sample-output.txt deterministically.
- The 200-seed readout takes about 15 minutes: start it with run_in_background and wait for it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The final readout and the DIFFICULTY-RETUNE.md Phase 75 section</name>
  <files>docs/DIFFICULTY-RETUNE.md, tools/readouts/75-13-final.txt</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md (the Phase 72 and Phase 73 H2s in full; the LOOT_SCALE dial-table row)
    - tools/readout-compare.mjs
    - the six balance plans' SUMMARYs (their readout notes)
    - test/unit/difficulty-retune-ledger.test.js
  </read_first>
  <action>
1. Start `node tools/tune-difficulty.mjs --seeds=200` at the phase head with run_in_background, stdout to tools/readouts/75-13-final.txt, and wait for it.
2. Insert `## v2.1 engine rules (Phase 75) — bot readouts` immediately before the v1.2 H2. It holds:
   - A "Change under measurement" list naming the six balance-moving plans and their rules.
   - Parameters (200 seeds, start depth 1, the shipped dials).
   - For each plan: `### Plan NN — BEFORE` and `### Plan NN — AFTER` headings, each followed by a fenced block of the readout's headline lines copied verbatim from its file, then a short reading.
   - `### Phase 75 — BEFORE (75-02 base)` and `### Phase 75 — FINAL` blocks.
   - A reading against the depth-20 unicorn and the floor 5–7 average. If the average run ending left floors 5–7, write a "Flag for the user" paragraph; do not retune.
3. Update the LOOT_SCALE dial-table row: the wilmst cache is now its own constant (WILMST_CACHE_PER_DEPTH 100, RULES-02) and still passes through lootFor.
4. Prove every recorded block with readout-compare --recorded against its source file, and run the ledger test.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## v2.1 engine rules (Phase 75) — bot readouts" docs/DIFFICULTY-RETUNE.md` equals 1, and it appears before the v1.2 H2 (the ledger test passes).
    - For every `### Plan NN — BEFORE/AFTER` and `### Phase 75 — FINAL` heading, `node tools/readout-compare.mjs --recorded docs/DIFFICULTY-RETUNE.md "<heading>" tools/readouts/<matching file>` exits 0 (all commands listed in the SUMMARY).
    - tools/readouts/75-13-final.txt exists and is non-empty.
  </acceptance_criteria>
  <done>Every Phase 75 balance change is recorded against a machine-checked readout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the fixture inventory and the exposure guard; refresh the voice sample; run the whole-phase gates</name>
  <files>test/parity/FIXTURE-INVENTORY.md, test/parity/divergence-records.test.js, tools/voice-sample-output.txt</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md (the Phase 75 section)
    - test/parity/divergence-records.test.js (the RULES-03 (Phase 75) test, replaySiteEvents, the JOIN-02 and ROLL-01 exposure tests)
    - every Phase 75 SUMMARY's "Fixture measurement" notes
    - tools/voice-sample.mjs
  </read_first>
  <behavior>
    - Across all 31 replay sites: zero wardRaised, wardReflected, sensesGained, combatInDark, wanderingMonster and tileResumed events; no state in which c.staff is set; no wentHungry with booksKept; no healed or regenerated with halved.
    - The declared Phase 75 set equals RULES75_EXPECTED_HOLDERS.
    - The guard fails if any one of those counts becomes non-zero (a test run against a doctored event list proves it has teeth).
  </behavior>
  <action>
1. FIXTURE-INVENTORY.md, Phase 75 section: after 75-05's subsection, add `### Plans 02, 04, 06, 07, 08, 09, 10, 12 — measured zero`. List each plan's rule, its predictor (why no replay site reaches it), and its measurement (commands and results, from its SUMMARY). Close with the prototype master hash and the guard's name.
2. divergence-records.test.js: extend the Phase 75 guard (or add a sibling test) with the exposure replay in the behaviour list, using the existing replaySiteEvents shape. Keep the declared-set assertion.
3. Run `node tools/voice-sample.mjs` and commit the regenerated output. Check that the new lines appear in it.
4. Whole-phase gates:
   - `npm test` is 0 failures.
   - `node --test "test/parity/**/*.test.js"` passes.
   - `git hash-object test/parity/prototype-master.js.txt` is a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
   - `node --test test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js test/unit/roll-ledger-sync.test.js` passes.
5. Roll the phase's human checks (from every plan's verification) into one list in the SUMMARY, for the milestone's batched Pixel 7 round.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" test/unit/roll-high-guard.test.js test/unit/rollDirection.test.js test/unit/rollDirection-checks.test.js test/unit/roll-high-state-pins.test.js test/unit/roll-high-save-compat.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "measured zero" test/parity/FIXTURE-INVENTORY.md` rises by at least 1 relative to the plan base, inside the Phase 75 section.
    - The Phase 75 guard in divergence-records.test.js replays all 31 sites (it asserts the site count is 31) and passes.
    - `git diff --stat <plan-base> -- tools/voice-sample-output.txt` shows a change, and `grep -c "Wield it first." tools/voice-sample-output.txt` is at least 1.
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The phase's fixture story is complete and guarded, the voice sample is current, and every gate is green at the head.</done>
</task>

</tasks>

<verification>
- The ledger records every Phase 75 readout, machine-checked; the fixture inventory and its guard cover every plan; the voice sample is fresh.
- npm test, parity, the roll-high guard, the direction tests and the state pins are all green; the prototype master is unchanged.
</verification>

<success_criteria>
The Phase 75 engine gate is closed with evidence: the moved fixtures are declared and guarded, the measured zeros are proven, and every balance change is recorded against its readout.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-13-SUMMARY.md` when done. Include the phase BEFORE → FINAL headline, any flag for the user, and the rolled-up Pixel 7 checklist.
</output>
