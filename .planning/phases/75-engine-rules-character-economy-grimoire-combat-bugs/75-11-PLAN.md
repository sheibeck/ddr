---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 11
type: execute
wave: 5
depends_on:
  - 75-09
  - 75-03
  - 75-04
files_modified:
  - src/browser/gearTab.js
  - src/browser/gearSheet.js
  - src/browser/viewModels.js
  - src/browser/combatMenu.js
  - src/browser/heroTab.js
  - docs/GEAR-SLOTS.md
  - test/unit/staff-surfaces.test.js
  - test/unit/gear-sheet-model.test.js
  - test/unit/gear-tab-dom.test.js
  - test/unit/gear-view-models.test.js
  - test/unit/gear-panels.test.js
  - test/unit/gear-agreement.test.js
  - test/unit/gear-sheet-dom.test.js
  - test/unit/combatMenu.test.js
  - test/unit/itemRowState.test.js
  - test/unit/usableBy.test.js
  - test/unit/item-stat-lines.test.js
  - test/unit/characterSheetViewModel.test.js
autonomous: true
requirements:
  - RULES-13
must_haves:
  truths:
    - "Gear tab WEAPON row: a wielded staff shows its name, 'd8', its charges and a USE cell that dispatches { type: 'useItem', slot: 'weapon' }; UNEQUIP returns it to the bag; the empty-weapon rule treats a wielded staff as filled"
    - "Gear tab BAG card: for a Magic User a staff is a WEAPON-family card (EQUIP TO WEAPON, or SWAP INTO WEAPON when a weapon is held) with no USE cell; for anyone else it has no equip action, and its text says a Magic User can wield it"
    - "Gear sheet: the WORN weapon sheet for a wielded staff offers USE, UNEQUIP and SWAP FOR each bag weapon or staff; a bag staff's sheet offers EQUIP TO / SWAP INTO WEAPON (Magic User) and DROP, never USE"
    - "The item stat lines for a staff, bag or wielded, state what wielding does ('Wielded: d8 weapon; its power works only in hand') beside its effect and charges; wornItemFor(c, 'weapon') returns the wielded staff, so both views use the same formatter"
    - "The hero sheet's weapon row reads a wielded staff through the engine's weaponRow (d8), never the Club fallback"
    - "Combat ITEMS: a bagged staff row is disabled with the reason 'NOT WIELDED' and is not counted as usable; a wielded staff has its own row marked EQUIPPED that dispatches { type: 'useItem', slot: 'weapon' } and counts as usable"
    - "docs/GEAR-SLOTS.md states the RULES-13 rule and marks the 2026-09-18 staff amendment as reversed"
    - "Presentation only: engine, content and parity fixtures are unchanged; every legality read comes from the engine (wieldedStaff, weaponRow, the Magic User staff gate), never a restated rule"
  artifacts:
    - path: src/browser/gearTab.js
      provides: "the wielded-staff WEAPON row and the staff bag card family"
      contains: "wieldedStaff"
    - path: src/browser/gearSheet.js
      provides: "the staff actions on the worn weapon and bag sheets"
    - path: src/browser/combatMenu.js
      provides: "the greyed bagged-staff row and the EQUIPPED wielded-staff row"
      contains: "notWielded"
    - path: test/unit/staff-surfaces.test.js
      provides: "every surface's staff cases, bag and wielded, Magic User and not"
  key_links:
    - from: src/browser/gearTab.js / gearSheet.js / combatMenu.js
      to: engine/derived.js#wieldedStaff
      via: "one read of the wielded staff"
      pattern: "wieldedStaff\\("
    - from: src/browser/heroTab.js weapon row
      to: engine/derived.js#weaponRow
      via: "a staff's d8 on the sheet"
      pattern: "weaponRow\\("
  prohibitions:
    - statement: "MUST NOT offer USE on a bagged staff anywhere (the Gear tab card, its sheet, combat ITEMS): a bagged staff is inert, and a tap would only be refused"
      requirement_id: RULES-13
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT offer EQUIP of a staff to a hero who cannot wield one"
      requirement_id: RULES-13
      category: transparency
      status: resolved
      verification: test
---

<objective>
Show the wielded staff everywhere the player manages gear (RULES-13, the surfaces): the Gear tab, the gear sheet, the hero sheet and combat ITEMS.

Purpose: 75-CONTEXT says "Update the Gear tab, item text and docs" and "Combat ITEMS greys a bagged staff and shows a wielded one as EQUIPPED". After 75-07 and 75-09 the engine wields staves and refuses bag use. This plan makes that visible and actionable, and never offers an action the engine would refuse.

Output: the surface changes, docs/GEAR-SLOTS.md, and tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-11 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- **This plan (75-11):** the Gear tab, gear sheet, hero sheet and combat ITEMS show and act on a wielded or bagged staff; `docs/GEAR-SLOTS.md`
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **Combat ITEMS scope.** Only the staff part of combat ITEMS is done here: the greyed bagged staff and the EQUIPPED wielded staff. Phase 77's CMBUI-14 extends the same treatment to jewelry and cloaks and marks every worn row EQUIPPED; it should reuse this plan's copy keys.
- **No mazeworld.html change.** The staff surfaces live in the Phase 47 modules (gearTab.js, gearSheet.js, heroTab.js) and the combat view model (combatMenu.js), and the classic renderer already draws disabled rows. If a mazeworld.html edit turns out to be required, stop and report it: 75-12 owns mazeworld.html in this wave.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-07-SUMMARY.md
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-09-SUMMARY.md
@docs/SHELL-MODULES.md

<interfaces>
- From 75-07 and 75-09: `weaponRow(name)` and `wieldedStaff(c)` in engine/derived.js; `STAFF_WEAPON` in content/treasure-tables.js; `{ type: "equipItem", i }` equips a bag staff into the weapon slot; `{ type: "useItem", slot: "weapon" }` uses the wielded staff; a bag-index use of a staff is refused as notWielded.
- src/browser/gearTab.js:
  - `emptySlotRows` treats a weapon as present only when `WEAPONS[c.weapon]` exists;
  - `gearWornModel`'s weapon row reads `WEAPONS[c.weapon].lab`;
  - `gearBagCardsModel` sets `family` to "weapon"/"armor" by kind, else `slotFor(it)`, and gives a family-less card a USE cell (`gearUseCell`);
  - swap detection reads `WEAPONS[c.weapon]`;
  - there is an older inline card action path further down (read it).
- src/browser/gearSheet.js#gearSheetModel: the WORN branch (a `row.use` gives USE; UNEQUIP or DISCARD; SWAP FOR `fits` cards) and the BAG branch (USE when `card.use`; EQUIP TO / SWAP INTO keys by family; DROP). `candidate` calls `lootCompare(c, it)` for weapon/armor-family cards.
- src/browser/viewModels.js: `restrictedClasses` (a staff is "M"), `usableBy`, `lootCompare`, `itemStatLines` (a staff already adds its charges line), `wornItemFor(c, slot)` (the weapon slot reads `WEAPONS[c.weapon]`), `ITEM_STAT_COPY`.
- src/browser/heroTab.js: two `WEAPONS[c.weapon] || WEAPONS["Club"]` reads.
- src/browser/combatMenu.js ITEMS (after 75-03): `carriedRows` (bag items with a potion kind or an activation; every row enabled), `wornRows` (WORN_SLOTS), `usableCount`, COMBAT_MENU_COPY.
- docs/GEAR-SLOTS.md: the 2026-09-18 staff amendment section and the slot table rows for the eight staves.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The Gear tab, the gear sheet, the stat lines and the hero sheet</name>
  <files>src/browser/gearTab.js, src/browser/gearSheet.js, src/browser/viewModels.js, src/browser/heroTab.js, test/unit/staff-surfaces.test.js, test/unit/gear-sheet-model.test.js, test/unit/gear-tab-dom.test.js, test/unit/gear-view-models.test.js, test/unit/gear-panels.test.js, test/unit/gear-agreement.test.js, test/unit/gear-sheet-dom.test.js, test/unit/itemRowState.test.js, test/unit/usableBy.test.js, test/unit/item-stat-lines.test.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - src/browser/gearTab.js (emptySlotRows, gearWornModel, gearBagCardsModel, gearUseCell, the older inline card action path)
    - src/browser/gearSheet.js (gearSheetModel in full)
    - src/browser/viewModels.js (restrictedClasses, usableBy, lootCompare, itemStatLines, wornItemFor, ITEM_STAT_COPY)
    - src/browser/heroTab.js (both weapon reads)
    - engine/derived.js (weaponRow, wieldedStaff; read only)
    - test/unit/gear-sheet-model.test.js and test/unit/gear-view-models.test.js (the builders)
  </read_first>
  <behavior>
    - A Magic User wielding a Birch Staff:
      - the WEAPON row is filled, named Birch Staff, value "d8", with a USE cell whose ref is { slot: "weapon" } and an unequip;
      - its worn sheet offers USE with run { type: "useItem", slot: "weapon" }, UNEQUIP, and SWAP FOR each bag weapon or staff;
      - wornItemFor(c, "weapon") is the staff object;
      - the hero sheet's weapon damage label reads d8.
    - A Magic User with a Rowan Staff in the bag and a Club in hand:
      - the card's family is "weapon" with the SWAP tag and no USE cell;
      - its sheet offers SWAP INTO WEAPON (run { type: "equipItem", i }) and DROP, with no USE.
    - A Fighter with a staff in the bag: the card has no family and no USE cell; its text says a Magic User can wield it; its sheet offers only DROP.
    - itemStatLines for a staff (bag or wielded) include the wield line, the effect and the charges.
    - lootCompare for a staff card never throws: legal for a Magic User, "not you" otherwise.
  </behavior>
  <action>
1. viewModels.js:
   - wornItemFor's weapon slot returns wieldedStaff(c) when present;
   - add a frozen ITEM_STAT_COPY entry for the wield line, and have itemStatLines add it for any staff;
   - lootCompare handles a staff: legality is the Magic User gate; the line is the wield line; no need/crit comparison.
2. gearTab.js:
   - a wielded staff fills the weapon row (name, STAFF_WEAPON's lab via weaponRow, the staff's use cell with useRef { slot: "weapon" }, unequip);
   - a staff card's family is "weapon" for a Magic User and none otherwise, and a staff card never gets a USE cell;
   - swap detection counts a wielded staff;
   - apply the same rules on the older inline card action path.
3. gearSheet.js: the worn weapon sheet uses the row's use cell with the weapon slot run. The BAG branch never adds USE for a staff; EQUIP/SWAP for a staff goes through equipRun unchanged (a weapon-slot equip carries no slot key).
4. heroTab.js: both weapon reads go through weaponRow, falling back to the Club only for Fists or an unknown name.
5. Create test/unit/staff-surfaces.test.js with every behaviour above. Re-pin any existing gear test whose staff expectation (a bagged staff with a USE cell) is now reversed, each with a RULES-13 comment.
  </action>
  <verify>
    <automated>node --test test/unit/staff-surfaces.test.js test/unit/gear-sheet-model.test.js test/unit/gear-tab-dom.test.js test/unit/gear-view-models.test.js test/unit/gear-panels.test.js test/unit/gear-agreement.test.js test/unit/gear-sheet-dom.test.js test/unit/item-stat-lines.test.js test/unit/characterSheetViewModel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wieldedStaff(" src/browser/gearTab.js` is at least 1, and `grep -c "weaponRow(" src/browser/heroTab.js` is at least 1.
    - `grep -v '^\s*//' src/browser/heroTab.js | grep -c 'WEAPONS\[c.weapon\] || WEAPONS\["Club"\]'` equals 0.
    - `node --test test/unit/staff-surfaces.test.js` passes, with the Magic User wielded, Magic User bag and Fighter bag cases.
  </acceptance_criteria>
  <done>The Gear tab and sheets let a Magic User wield, use and swap a staff, and never offer an action the engine refuses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Combat ITEMS for staves, and the docs</name>
  <files>src/browser/combatMenu.js, docs/GEAR-SLOTS.md, test/unit/combatMenu.test.js, test/unit/staff-surfaces.test.js</files>
  <read_first>
    - src/browser/combatMenu.js (COMBAT_MENU_COPY, carriedRows, wornRows, usableCount, the ITEMS submenu)
    - src/browser/viewModels.js (itemRowState)
    - docs/GEAR-SLOTS.md (the staff amendment and the slot table)
  </read_first>
  <behavior>
    - A Magic User in combat with a Birch Staff in the bag: its row has enabled false, cost "NOT WIELDED" and a desc saying to wield it outside the fight; usableCount does not count it.
    - A Magic User in combat wielding a Birch Staff: a row with id "worn-weapon", its cost starting "EQUIPPED · " followed by itemRowState's text, dispatch { type: "useItem", slot: "weapon" }, enabled true, counted in usableCount.
    - With no staff involved, every ITEMS row and count is exactly as before this plan.
  </behavior>
  <action>
1. combatMenu.js:
   - add frozen COMBAT_MENU_COPY keys `notWielded` ("NOT WIELDED"), `notWieldedDesc` (one dry line: wield it outside the fight to use its power) and `equipped` ("EQUIPPED");
   - in carriedRows, a staff row is disabled with those texts;
   - add the wielded-staff row beside wornRows;
   - count usable rows by enabled rows only (the CMBUI-14 counting rule), applied here to staves.
   Comment: RULES-13 (Phase 75); Phase 77 CMBUI-14 extends this to jewelry and cloaks.
2. docs/GEAR-SLOTS.md: add a "RULES-13 (Phase 75): a staff is a Magic User's wielded weapon" section (d8, need 0, crit 1; power only while wielded; charges and recharge unchanged; old saves keep bag staves in the bag) and mark the 2026-09-18 staff amendment as reversed, keeping it as history.
3. Add the behaviour cases to combatMenu.test.js and staff-surfaces.test.js.
4. Confirm presentation only: `git diff --stat <plan-base> -- engine content test/parity mazeworld.html` prints nothing.
  </action>
  <verify>
    <automated>node --test test/unit/combatMenu.test.js test/unit/staff-surfaces.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "notWieldedDesc" src/browser/combatMenu.js` is at least 2.
    - `grep -c "RULES-13" docs/GEAR-SLOTS.md` is at least 1.
    - `git diff --stat <plan-base> -- engine content test/parity mazeworld.html` prints nothing.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>In a fight a wielded staff is usable and marked, and a bagged one says why it is not.</done>
</task>

</tasks>

<verification>
- Every surface shows and acts on a wielded staff and never offers USE on a bagged one or EQUIP to a non-Magic-User.
- Presentation only; docs updated; npm test is green.
  <human-check>
  Pixel 7, at milestone close: a Magic User with a Birch Staff can equip it, strike with it (d8) and freeze with it. Unequipped, it shows as inert, and the combat ITEMS row reads NOT WIELDED.
  </human-check>
</verification>

<success_criteria>
ROADMAP criterion 7 is complete end to end: a Magic User can wield a magic staff as a d8 melee weapon, its power works only while wielded, and the game shows both.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-11-SUMMARY.md` when done.
</output>
