---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/debug/trap-death-21hp-oracle-minus1.md
  - test/unit/trap-death-repro.test.js
autonomous: true
requirements:
  - RULES-06
must_haves:
  truths:
    - "The 2026-09-22 debug session (.planning/debug/trap-death-21hp-oracle-minus1.md) is resumed, not restarted: its eliminated hypotheses are re-checked on current master (after Phases 73 and 74), and its fix (planBeat pins the last hero-hp frame to after.c.wp) is confirmed still present and still tested"
    - "An engine-level reproduction runs the real engine (applyAction, the tuning bot's own policy) from a dev start at depth 2 over at least 500 seeds, including a forced Elven Ninja subset, and records for every action: hp before, every hp-changing event with the amount its line prints, the actual hp change, and death"
    - "The reproduction answers three questions with counts: (i) did any hero die on an action whose narrated losses sum to less than the hp they had; (ii) did any action's narrated losses differ from its actual hp change; (iii) did any fight's last beat frame show more hero hp than the engine state"
    - "A shell-level audit compares every visible hero-HP readout (the top HUD #mw-hud-wp, the YOUR LOT hero card, every intermediate beat frame, the combat-over panel) with state.c.wp after every beat-ending path: natural settle, hurry, tab switch, flee, a kill, a superseded dispatch, the over-panel dismiss and reduced motion, including multi-swing folds and 3+-foe folds"
    - "The debug file ends the session with status root_cause_found (or, if nothing reproduces, an explicit 'no reproduction' verdict) and a Phase 75 findings section naming each confirmed cause, its category (display, engine or narration), the exact file and function, and the assertion the fix must make true"
    - "Every confirmed cause is encoded as a failing case in test/unit/trap-death-repro.test.js marked { todo: true }, so npm test stays green and plan 75-08 turns each case into a passing test"
    - "No production file changes in this plan: engine/, content/, src/ and mazeworld.html are byte-identical to the plan base"
  artifacts:
    - path: .planning/debug/trap-death-21hp-oracle-minus1.md
      provides: "the resumed RULES-06 debug session: re-verified evidence, the new reproduction results, the root-cause verdict and a Fix inputs list for 75-08"
      contains: "## Phase 75 session"
    - path: test/unit/trap-death-repro.test.js
      provides: "one todo-marked failing test per confirmed cause, plus passing tests that pin the re-verified facts (trap line and hp change agree; the last-frame pin holds)"
  key_links:
    - from: .planning/debug/trap-death-21hp-oracle-minus1.md (Fix inputs)
      to: .planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-08-PLAN.md
      via: "75-08's tasks branch on the category and file named here"
      pattern: "### Fix inputs"
    - from: test/unit/trap-death-repro.test.js
      to: src/browser/combatBeat.js#planBeat / engine/encounters.js#springTrap
      via: "the todo cases call the same pure functions the fix will change"
      pattern: "todo: true"
  prohibitions:
    - statement: "MUST NOT land a fix, or any production-code change, before the root cause is documented; this session only reproduces, measures and records (ROADMAP Phase 75 success criterion 4)"
      requirement_id: RULES-06
      category: integrity
      status: resolved
      verification: test
    - statement: "MUST NOT close the session on a guess: every eliminated hypothesis cites the reproduction or code evidence that eliminated it, and a cause that did not reproduce is recorded as unreproduced, never as confirmed"
      requirement_id: RULES-06
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Resume the RULES-06 root-cause session (a floor-2 trap that the Oracle showed as "−1 HP" killed a 21-HP Elven Ninja) and finish it properly before any fix lands.

Purpose: ROADMAP Phase 75 success criterion 4 says the fix must follow an explicit /gsd-debug root-cause session, not a guess. A 2026-09-22 session already exists. It disproved the stale top-HUD hypothesis, disproved "the trap line prints the wrong number", and fixed one real display gap: the YOUR LOT card's last beat frame under-counted multi-swing folds. It never reproduced the death end to end, and it left two blind spots open: the intermediate frames and the foe-frame mirror gap. This plan re-checks all of it on current master, reproduces at scale, audits every HP readout, and writes down the verdict and the fix inputs that plan 75-08 implements.

Output: the updated debug file, and todo-marked failing tests that encode each confirmed cause.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-01 creates or changes.)

- **This plan (75-01):** the resumed RULES-06 debug session (`.planning/debug/trap-death-21hp-oracle-minus1.md`: Phase 75 findings and Fix inputs) and `test/unit/trap-death-repro.test.js` (todo-marked failing cases)
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- 75-03: the combat SPELLS menu lists only castable spells
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- **RULES-06, edge probe: unclassified, reviewed manually.** The requirement has no data-shape edges; its edges are the beat-ending paths and the hp-changing events, and this plan enumerates both exhaustively. If the scale reproduction finds no engine or narration fault, the verdict is "display-only causes", and success criterion 4's "a −1 HP trap can never kill" is then guaranteed by 75-08's two standing guards rather than by an engine change. Flagged for the milestone device round.
- The original report is a single, unreproduced field report. A cause is "confirmed" only if the reproduction or a direct unit reproduction shows it; a plausible mechanism that cannot be shown stays "unreproduced" in the file.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/debug/trap-death-21hp-oracle-minus1.md
@.planning/todos/pending/2026-09-22-trap-death-at-21-hp-oracle-shows-minus-1.md

<interfaces>
Key functions (reference by name; line numbers drift, and Phase 74 has touched combat.js and the narration files):
- engine/encounters.js#springTrap: one dodge rollCheck, then `rng.d(8)` for the trap kind, `dmg` computed once (times, Cat Burglar ×2, Hardiness −3, `scaleHazard`), then `c.wp -= dmg` and `trapSprung { ...rollFields(check), name, dmg }` from the same value; `die(state, "trap")` when wp ≤ 0.
- engine/movement.js#move: per-step drains (afflictionTick, trappedPanic, the darkness counter), the 20-square charge trickle, `newDay` on a 100-square crossing (can start a wandering fight), then the feature dispatch (dot/trap/chest/tele/exit).
- engine/state.js#newRun(seed, exclude, { startDepth, force }): the dev start-at-depth path (levels the hero, grants a purse). `force` accepts `{ race, sub }`.
- tools/lib/tuning-bot.mjs: `playRun` and the per-action decision function the readout uses. Read it to find the per-action loop; a scratch script may wrap applyAction or re-run the loop.
- src/browser/combatBeat.js: `planBeat`, `heroFrames`, `foeFrames`, `lineIdxsFor`, `createBeatRunner` (view/hurry/active), `beatEndMs`. Pure except the runner.
- src/browser/combatPanel.js#yourLotViewModel (the YOUR LOT hero card's `wpLabel`).
- mazeworld.html: `paint()` writes `#mw-hud-wp`; `window.__mzBeat` (active/view/hurry); the renderers read `window.__mzBeat?.view?.()?.state || S` during a beat.
- test/unit/combat-beat-shell.test.js and test/unit/harness/shellSandbox.js: the proven technique for driving the real beat runner against the classic shell with a fake clock.
- node:test marks a known-failing case with `test(name, { todo: true }, fn)`: it is reported but does not fail the run.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resume the session, re-verify on master, and run the engine-level reproduction at scale</name>
  <files>.planning/debug/trap-death-21hp-oracle-minus1.md, test/unit/trap-death-repro.test.js</files>
  <read_first>
    - .planning/debug/trap-death-21hp-oracle-minus1.md (the whole file: hypotheses, evidence, eliminated list, resolution, blind spots)
    - engine/encounters.js (springTrap, tableFour, rollAffliction)
    - engine/movement.js (move: every per-step hp drain and its clamp, the newDay call, the feature dispatch)
    - src/browser/combatBeat.js (planBeat, heroFrames, foeFrames, lineIdxsFor)
    - tools/lib/tuning-bot.mjs (playRun and its per-action loop)
    - test/unit/combat-beat.test.js (the existing last-frame regression test from the 2026-09-22 fix)
  </read_first>
  <action>
1. Record `git rev-parse HEAD` as the plan base. In the debug file set `status: investigating`, bump `updated`, and append a new `## Phase 75 session (2026-09-25)` section at the end. Keep every earlier section unchanged: it is history.
2. Re-verify on master and log each result as an Evidence entry (timestamp, checked, found, implication):
   - planBeat still pins the last hero-hp frame to the real final hp, and its regression test still passes.
   - springTrap still subtracts and narrates the same `dmg` value after the Phase 73 event reshape.
   - trappedPanic and afflictionTick still leave at least 1 hp.
   - Every hp-reducing event on a move step still gets its own Oracle line (formatEvents does not drop it).
3. Write a scratch reproduction in the session scratchpad (never committed). It drives the real engine with the tuning bot's policy from `newRun(seed, [], { startDepth: 2 })` over at least 500 seeds (half unforced, half forced to an Elven Ninja through the `force` option). For every action it records:
   - hp before;
   - each hp-changing event and the amount its narration prints (trapSprung, trappedPanic, afflictionTick, wentHungry, the Table-4 lines, struckByFoe, foeBolted, earthquakeSelfDamage and any other type found by diffing wp across the action);
   - hp after, and whether the hero died.
   For every action that ends a fight, it also runs planBeat on that action's (before, after, events) and records the last hero-hp frame against after.c.wp.
4. Report the three counts from the must-haves: (i) deaths whose narrated losses were smaller than the hp before; (ii) actions whose narrated losses and actual hp change disagree; (iii) fight endings whose last frame overstates hp. For every hit, log the seed, action index and event list as Evidence.
5. Create test/unit/trap-death-repro.test.js with passing tests that pin the re-verified facts from step 2: the trap line's number equals the hp removed, and the last frame equals after.c.wp for a two-swing fold. Build the states directly, the way combat-beat.test.js and encounters.test.js do.
  </action>
  <verify>
    <automated>node --test test/unit/trap-death-repro.test.js test/unit/combat-beat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 75 session" .planning/debug/trap-death-21hp-oracle-minus1.md` equals 1, and the file still contains the pre-existing `## Eliminated` and `## Resolution` headings.
    - The Phase 75 section records the seed count (at least 500), the forced Elven Ninja subset, and all three counts (i), (ii) and (iii) as numbers.
    - `node --test test/unit/trap-death-repro.test.js` passes.
    - `git diff --stat <plan-base> -- engine content src mazeworld.html` prints nothing.
  </acceptance_criteria>
  <done>The earlier findings are re-checked on master and the engine-level reproduction has produced measured counts.</done>
</task>

<task type="auto">
  <name>Task 2: Audit every visible hero-HP readout across every beat-ending path</name>
  <files>.planning/debug/trap-death-21hp-oracle-minus1.md</files>
  <read_first>
    - test/unit/combat-beat-shell.test.js (how the real beat runner is driven against the classic shell with a fake clock)
    - test/unit/harness/shellSandbox.js
    - src/browser/combatPanel.js (yourLotViewModel)
    - mazeworld.html (paint, renderEncounter, renderYourLot, the over-panel dismiss, the window.__mzBeat bridge and its hurry/tab-switch callers)
  </read_first>
  <action>
1. In the scratchpad, reuse combat-beat-shell.test.js's technique to script fights with the real beat runner. Cover:
   - a last round with a single foe swinging twice;
   - a round where 3+ foes land together;
   - an ending round and a mid-fight round.
2. End each fight by every path: natural settle, hurry, tab switch, flee, the kill that ends the fight, a second dispatch that supersedes an active beat, the over-panel dismiss, and the reduced-motion instant path.
3. At every point the player can act (no beat active), read `#mw-hud-wp` and the YOUR LOT card's hp and compare both to state.c.wp. Also sample every intermediate frame: this is blind spot (1) from the earlier session. Compare each frame's hero hp to the engine's running hp through that frame's events.
4. Check foeFrames for the mirror under-count (blind spot 3), but classify it: it cannot mislead the player about their own survival.
5. Log each result as Evidence. For every mismatch, record the exact path, the frame, the shown value and the true value.
  </action>
  <verify>
    <automated>node --test test/unit/combat-beat-shell.test.js test/unit/combat-beat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 75 section lists all eight beat-ending paths by name, each with a result (match, or mismatch with the shown and true values).
    - It records a verdict for intermediate frames (blind spot 1) and for foeFrames (blind spot 3).
    - `git diff --stat <plan-base> -- engine content src mazeworld.html` prints nothing.
  </acceptance_criteria>
  <done>Every visible HP readout has been checked against engine truth on every path that ends a beat.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Record the verdict and the fix inputs, and encode each confirmed cause as a todo test</name>
  <files>.planning/debug/trap-death-21hp-oracle-minus1.md, test/unit/trap-death-repro.test.js</files>
  <read_first>
    - .planning/debug/trap-death-21hp-oracle-minus1.md (after Tasks 1 and 2)
    - test/unit/trap-death-repro.test.js (after Task 1)
  </read_first>
  <behavior>
    - Each confirmed cause has one test that fails against the current code and is marked { todo: true }. Its name starts with "RULES-06 cause:" and names the file and function.
    - If nothing reproduces, the file contains no todo tests and no "RULES-06 cause:" names, and the debug file says so.
  </behavior>
  <action>
1. Add a `### Verdict` subsection to the Phase 75 section. Either set `status: root_cause_found` and state the confirmed cause(s), or keep `status: investigating` and state "no reproduction" with the strongest evidence gathered. Add every newly eliminated hypothesis to the `## Eliminated` list, with its evidence and timestamp.
2. Add a `### Fix inputs` subsection. For each confirmed cause give:
   - the category: display, engine or narration;
   - the file and function;
   - the assertion the fix must make true (for example, "every intermediate hero frame equals the engine's running hp through that line's events");
   - the todo test name.
   Also list the two standing guards 75-08 must add whatever the verdict: every visible HP readout equals state.c.wp wherever the player can act, and no hp loss goes un-narrated or mis-narrated.
3. Add each cause as a todo-marked test to test/unit/trap-death-repro.test.js. The test must fail on the current code (confirm it does), and node:test's todo flag keeps npm test green.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^### (Verdict|Fix inputs)" .planning/debug/trap-death-21hp-oracle-minus1.md` equals 2.
    - `grep -cE "^status: (root_cause_found|investigating)" .planning/debug/trap-death-21hp-oracle-minus1.md` equals 1.
    - For every "RULES-06 cause:" test name in test/unit/trap-death-repro.test.js, the same name appears in the Fix inputs list.
    - `npm test` reports 0 failures (todo cases do not count as failures).
    - `git diff --stat <plan-base> -- engine content src mazeworld.html` prints nothing.
  </acceptance_criteria>
  <done>The root cause (or its absence) is documented with evidence, and 75-08 has exact fix inputs and failing tests to turn green.</done>
</task>

</tasks>

<verification>
- The debug file is resumed with re-verified evidence, a scale reproduction, the shell audit, a verdict and fix inputs.
- No production file changed. npm test is green, and every todo case fails for the documented reason.
</verification>

<success_criteria>
RULES-06's root cause is established by an explicit debug session with measured evidence before any fix lands (ROADMAP criterion 4), and plan 75-08 has exact, testable fix inputs.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-01-SUMMARY.md` when done. Include the verdict, the three reproduction counts, the beat-path audit table, and the Fix inputs list copied from the debug file.
</output>
