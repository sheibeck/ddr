---
phase: 75-engine-rules-character-economy-grimoire-combat-bugs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/combatMenu.js
  - test/unit/combatMenu.test.js
autonomous: true
requirements:
  - RULES-04
must_haves:
  truths:
    - "The combat SPELLS submenu lists a grimoire spell only when engine/derived.js#canCast(state, sp) is true for it; level-locked and school-locked spells are hidden, not greyed (user ruling 2026-09-21)"
    - "A castable spell with no charges left stays listed and disabled: a row is enabled exactly when canCast is true and charges > 0"
    - "RULES-04 adjacency: a spell whose effective level (spellLevelFor) equals the hero's level is listed, and so is a spell whose school gate equals the hero's level; one level above either threshold is hidden"
    - "RULES-04 empty: a grimoire that holds spells but none castable right now shows exactly one disabled row with its own reason copy (never an empty list); an empty grimoire keeps today's no-spells row"
    - "RULES-04 ordering: visible rows keep their relative SPELLS order (Phase 77 CMBUI-08 owns the level-then-name sort); hiding a row never reorders the others"
    - "The rule reads the engine's own canCast; the menu re-derives no level or school formula of its own"
    - "The Hero-tab Grimoire is untouched and still lists every spell the book holds"
    - "Presentation only: no engine, content or parity fixture changes"
  artifacts:
    - path: src/browser/combatMenu.js
      provides: "spell rows filtered by canCast; enabled = charges > 0; COMBAT_MENU_COPY noCastable/noCastableDesc for the all-locked case"
      contains: "noCastableDesc"
    - path: test/unit/combatMenu.test.js
      provides: "the hide/keep/adjacency/empty/ordering pins"
  key_links:
    - from: src/browser/combatMenu.js (the SPELLS submenu builder)
      to: engine/derived.js#canCast
      via: "the one legality read; hidden = !canCast"
      pattern: "canCast\\(state, sp\\)"
  prohibitions:
    - statement: "MUST NOT hide a spell that is only out of charges: the player needs to see it exists but is spent"
      requirement_id: RULES-04
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT remove or filter anything in the Hero-tab Grimoire: it keeps listing every spell the book holds"
      requirement_id: RULES-04
      category: transparency
      status: resolved
      verification: test
---

<objective>
Make the combat SPELLS menu list only what the hero can cast right now: level- and school-locked spells are hidden, while spells that are merely out of charges stay visible and disabled (RULES-04).

Purpose: this is the user's 2026-09-21 ruling. It reverses the earlier "disabled rows stay visible" choice, for combat only. The Hero-tab Grimoire keeps listing everything the book holds.

Output: the filtered spell rows, an in-voice row for the all-locked case, and the tests.
</objective>

## Artifacts this phase produces

(Phase-wide list. **This plan** marks what 75-03 creates or changes.)

- 75-01: the resumed RULES-06 debug session and `test/unit/trap-death-repro.test.js`
- 75-02: `test/unit/hp-growth-linear.test.js`; `WILMST_CACHE_PER_DEPTH` 300 → 100; readouts
- **This plan (75-03):** the combat SPELLS menu lists only castable-by-level/school spells (`src/browser/combatMenu.js`, `test/unit/combatMenu.test.js`)
- 75-04: honest ailment 5–6 lines; destroyed armor named on every armor-replacing path
- 75-05: the Summoner's offense gate removed; grant-time school-gate legality; the declared chargen/combat fixture moves; readouts
- 75-06: Sense Presence initiative and dark rules; Bubble as a one-shot mirror; readouts
- 75-07: the staff wield model (engine)
- 75-08: the RULES-06 fix and its two standing guards
- 75-09: a staff's power works only while wielded; the bot wields staves; readouts
- 75-10: the Summoner's healing weakness; readouts
- 75-11: the staff on the Gear tab, gear sheet, hero sheet and combat ITEMS
- 75-12: the wanderer-interrupted tile; no rations, no book refill; readouts
- 75-13: the Phase 75 FIXTURE-INVENTORY summary, the DIFFICULTY-RETUNE.md record, the final readout, the voice sample

## Flagged assumptions (surfaced, not silently decided)

- The SPELLS action's sub-line keeps "N known" as the whole book count, so the combat sub-line and the Hero-tab Grimoire agree on what the book holds; the submenu title keeps its charges count. Flagged for the device round in case "known" should count only the listed rows.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-CONTEXT.md
@.planning/todos/pending/2026-09-21-summoner-rolls-freeze-it-cannot-cast-hide-uncastable-spells-.md

<interfaces>
src/browser/combatMenu.js (after Phase 74's 74-04, which re-pinned the STRIKE and FLEE lines; the spell rows are unchanged by 74). In `combatMenuViewModelUnlocked`, the caster branch builds `spellRows` from `SPELLS.filter((sp) => (c.grimoire || []).includes(sp.n))`. Each row is `{ id: "spell-<idx>", label, cost: "LVL <n>", desc: sp.txt, niche, nicheLabel, enabled: canCast(state, sp) && charges > 0, dispatch: { type: "castSpell", idx } }`, and the empty case is `[{ id: "none", label: COMBAT_MENU_COPY.noSpells, desc: COMBAT_MENU_COPY.noSpellsDesc, enabled: false }]`.

engine/derived.js#canCast(state, sp): true when the spell is in the grimoire, `spellLevelFor(c.sub, sp) <= c.level`, and `c.level >= schoolGate(c.sub, sp.s)`. Every spell row is a grimoire spell, so for these rows canCast is false exactly when the spell is level- or school-locked.

test/unit/combatMenu.test.js already has "Magic User (Wizard): SPELLS sub-line, submenu title, rows in SPELLS order incl. an above-level spell". That test's above-level expectation is what this plan reverses.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hide level- and school-locked spell rows, keep out-of-charges rows</name>
  <files>src/browser/combatMenu.js, test/unit/combatMenu.test.js</files>
  <read_first>
    - src/browser/combatMenu.js (COMBAT_MENU_COPY, the caster branch of combatMenuViewModelUnlocked)
    - test/unit/combatMenu.test.js (the fixture builders at the top and the Magic User rows test)
    - engine/derived.js (canCast, spellLevelFor, schoolGate)
  </read_first>
  <behavior>
    - A level-1 Wizard with Heal, Freeze and Lightning (level 4) lists Heal and Freeze only; Lightning has no row.
    - A level-1 Sorcerer with Heal (its healing school opens at 4) and Freeze lists Freeze only.
    - A level-2 hero with a level-2 spell lists it (adjacency); a level-3 Illusionist with a protection spell (gate 3) lists it; a level-2 Illusionist does not.
    - With every charge spent (spellsUsed equal to maxCharges), castable rows stay listed with enabled false.
    - Visible rows keep SPELLS order: with Heal, Freeze and Acid in the book at level 2, the rows are exactly those three in SPELLS order.
    - A level-1 Sorcerer whose book is only Heal shows exactly one row with id "none", enabled false, and the new noCastable copy.
    - An empty grimoire still shows today's noSpells row.
  </behavior>
  <action>
1. In the caster branch, filter the grimoire spells by `canCast(state, sp)` before mapping them to rows, and set each row's `enabled` to `charges > 0`. Everything else about a row (id, label, cost, desc, niche, dispatch) stays the same.
2. Add two frozen COMBAT_MENU_COPY keys: `noCastable` (a short label, for example "NOTHING YOU CAN CAST YET") and `noCastableDesc` (one dry, family-friendly line saying the book holds spells above your level or school). When the book is non-empty but no row survives the filter, use them for the single disabled "none" row. The existing noSpells row stays for an empty grimoire.
3. Update the file's comments: RULES-04 (Phase 75, user ruling 2026-09-21) hides locked rows in combat only; the Hero-tab Grimoire lists everything.
4. Replace the old "incl. an above-level spell" expectation in combatMenu.test.js, and add a test for every behaviour above.
  </action>
  <verify>
    <automated>node --test test/unit/combatMenu.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' src/browser/combatMenu.js | grep -c "canCast(state, sp) && charges > 0"` equals 0 (the old combined enable is gone).
    - `grep -c "noCastableDesc" src/browser/combatMenu.js` is at least 2 (declared and used).
    - `node --test test/unit/combatMenu.test.js` passes, including a test asserting an above-level spell has no row and a test asserting an out-of-charges castable spell has a disabled row.
  </acceptance_criteria>
  <done>Combat lists only castable spells; spent spells still show, greyed.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the Hero-tab Grimoire is untouched and the copy passes the voice scans</name>
  <files>test/unit/combatMenu.test.js</files>
  <read_first>
    - src/browser/viewModels.js (grimoireViewModel)
    - test/unit/grimoireViewModel.test.js (how a Hero-tab Grimoire state is built)
    - test/voice/safety-scan.test.js and test/unit/hp-not-wp.test.js (whether they walk COMBAT_MENU_COPY)
  </read_first>
  <action>
1. Add a test to combatMenu.test.js: for a level-1 Wizard holding Lightning, grimoireViewModel still lists Lightning while the combat SPELLS submenu does not.
2. Confirm the standing voice/safety and HP-not-WP scans walk COMBAT_MENU_COPY (read their sources). If they do not, add an assertion in combatMenu.test.js that the two new strings contain no "WP" and no banned word, reusing content/safety-wordlist.js the way the safety scan does.
3. Run the full suite.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - combatMenu.test.js contains a test that calls grimoireViewModel and asserts the above-level spell is still listed.
    - `git diff --stat <plan-base> -- engine content test/parity src/browser/viewModels.js` prints nothing.
    - `npm test` reports 0 failures.
  </acceptance_criteria>
  <done>The combat-only scope of the ruling is pinned and the new copy passes every voice guard.</done>
</task>

</tasks>

<verification>
- Combat lists only castable spells; out-of-charges spells stay visible and disabled; the Hero-tab Grimoire is unchanged.
- Presentation only: no engine/content/fixture change. npm test is green.
  <human-check>
  Pixel 7, at milestone close: in a fight, the SPELLS list shows only spells castable at your level; a spent spell still shows, greyed.
  </human-check>
</verification>

<success_criteria>
ROADMAP criterion 2's menu clause holds: the combat SPELLS menu hides, and never just greys, anything level- or school-locked.
</success_criteria>

<output>
Create `.planning/phases/75-engine-rules-character-economy-grimoire-combat-bugs/75-03-SUMMARY.md` when done.
</output>
