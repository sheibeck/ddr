---
phase: 20-parley-balance-language-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/full-suite.test.js
  - test/unit/parley-carveout.test.js
  - tools/tune-difficulty.mjs
  - docs/PARLEY-REBALANCE.md
autonomous: true
requirements: [PARLEY-01, PARLEY-02, PARLEY-03]
tags: [parity, harness, carve-out, tune-difficulty, readout, baseline, node-test]

must_haves:
  truths:
    - "D-13/D-18: test/parity/harness/comparables.js exports a named stripParleyDivergence(state) that removes EXACTLY c.sp, c.gold, combat.parleyTried and combat.parleyInsulted, and it is applied at BOTH replay sites of the combat fixture's parley scenario (seed 303) — the local comparable() in test/parity/combat-parity.test.js AND the shared combatComparable loop in test/parity/full-suite.test.js — via a scenario-scoped wrapper that is chosen only when scenario.name === \"parley\""
    - "D-13 empty/no-op (edge probe): a state that carries neither parleyTried nor parleyInsulted and has no combat comes back from stripParleyDivergence deep-equal to the input minus c.sp/c.gold — the stripper never ADDS keys, never touches state.combat when it is null, and never touches foes, round, target or any other combat field"
    - "D-13 scoping (edge probe): combatComparable(state) itself STILL exposes c.sp and c.gold (the strip is NOT folded into the shared comparable) — so the win/lose/flee combat scenarios, every magic scenario, movement, chargen, economy and encounters remain compared on sp/gold byte-for-byte"
    - "D-13 pre-divergence (ordering probe): with the engine still at its pre-Phase-20 rules, the carve-out is a no-op on the seed-303 comparison — node --test test/parity/combat-parity.test.js test/parity/full-suite.test.js passes 30/30 across the parity glob both before and after the stripper is wired (the stripper protects the comparison that 20-02 will change; it does not hide a divergence that already exists)"
    - "D-15: tools/tune-difficulty.mjs tallies, per run, parley attempts (parleyRolled events), successes (spGained with reason parley), failures (parleyFailed), refusals (parleyRefused), exhausted retries (parleyExhausted — always 0 before 20-02 lands) and SP-from-parley, prints a 'Parley (D-15 readout' section in the text report, and exposes the same numbers under a parley key in --json output"
    - "D-15 BEFORE: docs/PARLEY-REBALANCE.md holds the verbatim 200-seed tune-difficulty readout measured against the PRE-Phase-20 engine (engine/ and content/ identical to commit 04eb229), the seed-303 BEFORE numbers (need 19, sp 13, gold 250, draws d20=2 d6=5 d6=5 d6=2), the locked-decision change table, and an explicitly empty AFTER section for 20-03 to fill"
    - statement: "The parley carve-out reads as a deliberate, permanent divergence with a stated rationale (like stripRationsField), not a fidelity bug swept under the rug"
      verification: backstop
  artifacts:
    - path: "test/parity/harness/comparables.js"
      provides: "stripParleyDivergence(state) — exported, JSDoc cites PARLEY-01..04 / D-13 / D-18 and names the four stripped fields"
      contains: "export function stripParleyDivergence(state)"
    - path: "test/parity/combat-parity.test.js"
      provides: "scenario-scoped cmp wrapper applied to both diffState calls of the parley scenario only"
      contains: "stripParleyDivergence"
    - path: "test/parity/full-suite.test.js"
      provides: "scenario-scoped cmp wrapper inside the COMBAT_FIXTURE loop only (the MAGIC_FIXTURE loop keeps bare combatComparable)"
      contains: "stripParleyDivergence"
    - path: "test/unit/parley-carveout.test.js"
      provides: "exact-field strip proof, no-op proof, combatComparable-still-exposes-sp/gold proof"
      contains: "stripParleyDivergence"
    - path: "tools/tune-difficulty.mjs"
      provides: "per-run parley tally + 'Parley (D-15 readout' report section + parley key in --json"
      contains: "parleyAttempts"
    - path: "docs/PARLEY-REBALANCE.md"
      provides: "Phase 20 before/after ledger: locked-decision table, seed-303 BEFORE, tune-difficulty BEFORE readout verbatim, empty AFTER section"
      contains: "## tune-difficulty BEFORE"
  key_links:
    - from: "test/parity/combat-parity.test.js"
      to: "test/parity/harness/comparables.js stripParleyDivergence"
      via: "import + `scenario.name === \"parley\" ? (s) => stripParleyDivergence(comparable(s)) : comparable`"
      pattern: "scenario.name === \"parley\" \\? \\(s\\) => stripParleyDivergence"
    - from: "test/parity/full-suite.test.js combat loop"
      to: "test/parity/harness/comparables.js stripParleyDivergence"
      via: "same wrapper shape around combatComparable, chosen per scenario inside the COMBAT_FIXTURE for-loop"
      pattern: "stripParleyDivergence\\(combatComparable"
    - from: "tools/tune-difficulty.mjs autoPlayOnce"
      to: "engine/engine.js applyAction events"
      via: "destructures { state, events } from applyAction and tallies parleyRolled / spGained(reason parley) / parleyFailed / parleyRefused / parleyExhausted"
      pattern: "e.type === \"parleyRolled\""
  prohibitions:
    - requirement_id: PARLEY-01
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt or any test/parity/fixtures/*.json; MUST NOT regenerate any fixture"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures exits 0"
    - requirement_id: PARLEY-01
      category: safety
      statement: "MUST NOT strip c.sp / c.gold / parleyTried / parleyInsulted inside combatComparable, movementComparable or economyComparable themselves (the strip is scenario-scoped, never global) — the magic fixture and the win/lose/flee scenarios stay compared on sp/gold"
      status: flagged-unverified
      verification: "test/unit/parley-carveout.test.js asserts combatComparable(state).c.sp and .c.gold are still present; grep -c 'stripParleyDivergence' inside the MAGIC_FIXTURE loop of full-suite.test.js is 0"
    - requirement_id: PARLEY-02
      category: safety
      statement: "MUST NOT touch engine/, content/, src/browser/ or mazeworld.html in this plan — the engine rules are still pre-Phase-20 when the BEFORE readout runs, and that is the point"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- engine content src mazeworld.html exits 0 at the end of this plan"
    - requirement_id: PARLEY-03
      category: values
      statement: "MUST NOT change any difficulty dial, economy number or the bot's decision policy in tools/tune-difficulty.mjs (readout only — Phase 21 owns the retune); MUST NOT use Math.random or Date anywhere in the tally"
      status: flagged-unverified
      verification: "git diff 04eb229 -- tools/tune-difficulty.mjs touches no line of decideAction; grep -c 'Math.random' tools/tune-difficulty.mjs prints 0"
---

<objective>
Lay the pre-change baseline for Phase 20 BEFORE any parley rule moves: (1) the scenario-scoped parity carve-out `stripParleyDivergence` (D-13), wired at BOTH replay sites of the seed-303 parley scenario (D-18) so 20-02's deliberate payout/odds divergence lands green instead of red; (2) the `tools/tune-difficulty.mjs` parley tally (D-15) so parley attempts / successes / SP share are measurable; (3) the BEFORE half of the D-15 readout, captured against the pre-Phase-20 engine and committed to `docs/PARLEY-REBALANCE.md` beside the locked-decision change table and the seed-303 BEFORE numbers.

Purpose: the carve-out must exist before the engine changes (or 20-02 cannot end `npm test`-green), and the BEFORE readout can only be measured while the engine still has the old rules — so this plan is a hard prerequisite of 20-02, not a parallel sibling.

Output: `stripParleyDivergence` in the harness (+ both call sites + a carve-out unit test), the tune-difficulty parley section, `docs/PARLEY-REBALANCE.md` (new) with the BEFORE readout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-parley-balance-language-system/20-CONTEXT.md
@.planning/phases/20-parley-balance-language-system/20-RESEARCH.md
@.planning/phases/20-parley-balance-language-system/20-PATTERNS.md
@test/parity/harness/comparables.js
@test/parity/combat-parity.test.js
@test/parity/full-suite.test.js
@test/unit/foe-ability-carveouts.test.js
@tools/tune-difficulty.mjs
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `stripParleyDivergence(state)` exported from `test/parity/harness/comparables.js`; scenario-scoped `cmp` wrappers in `test/parity/combat-parity.test.js` and `test/parity/full-suite.test.js` | test harness | **20-01** | D-13 / D-18 — applied ONLY when `scenario.name === "parley"` |
| `test/unit/parley-carveout.test.js` | tests | **20-01** | exact-field / no-op / not-global proofs |
| `tools/tune-difficulty.mjs` parley tally (`parleyAttempts`, `parleySuccesses`, `parleyFailures`, `parleyRefused`, `parleyExhausted`, `parleySp`, `spTotal` per run; `Parley (D-15 readout` section; `parley` key in `--json`) | dev tool | **20-01** | readout only, no policy change |
| `docs/PARLEY-REBALANCE.md` (new) — change table, seed-303 BEFORE, tune-difficulty BEFORE readout verbatim, empty AFTER section | docs | **20-01** (created), 20-03 (AFTER filled) | the 18-06 BESTIARY-REBALANCE.md precedent |
| `fluency(c)` and `killSpFor(c, f, roll)` in `engine/derived.js`; `killFoe` calls `killSpFor` | engine (leaf) | 20-02 | D-01 / D-09 |
| `canParley` / `parley` rewrite, `foeTurn` `need += 1` / `mNeed += 1`, new serialized combat flags `C.parleyTried` / `C.parleyInsulted` (lazily written, absent ≡ false), new event types `parleyExhausted` / `parleyInsulted`, `parleyRolled.fluency` field | engine | 20-02 | D-03..D-08, D-10..D-12, D-20 |
| `EVENT_NARRATION` entries `parleyInsulted`, `parleyExhausted`; `parleyRolled` fluency annotation; `parleyRefused` canon grudge line; `goldGained` parley branch | presentation | 20-02 | D-14 |
| `mazeworld.html` classic `fluency()` + `canParley()` mirror | UI (classic script) | 20-02 | D-17 |
| `test/unit/fluency.test.js`; two new tests + comment fixes in `test/unit/combat.test.js` | tests | 20-02 | LANG-01, D-05, D-06 |
| `test/unit/parley.test.js` (availability matrix, every gate, refusal, exhausted, insulted incl. member branch, odds, clamp, payout property, wilmst odds, draw shape, seed-303 pin, save/load edge); `test/unit/parley-button-mirror.test.js` | tests | 20-03 | D-02, D-04, D-05..D-08, D-10..D-12, D-17, D-19, D-21 |
| `test/parity/FIXTURE-INVENTORY.md` "Phase 20 parley divergence" section (seed-303 before/after table, re-measured); `docs/PARLEY-REBALANCE.md` AFTER readout + comparison; phase gate | docs + gate | 20-03 | D-13, D-15, D-21 |

## Discretion resolved by this plan set

- **Flag initialisation (D-05 discretion):** `C.parleyTried` / `C.parleyInsulted` are written LAZILY (only on an attempt / a failure) and are never initialised in `startCombat`. Reason: an explicit `parleyTried: false` on every `state.combat` would diverge every combat AND magic fixture from the prototype and force a global carve-out, which D-13 forbids (the carve-out is scenario-scoped to seed 303). Absent ≡ false everywhere (engine reads are truthiness checks; the stripper removes the keys whether present or not).
- **Home of the shared kill-SP helper (D-01 discretion):** `engine/derived.js` (the cycle-free leaf; `derived.js` imports nothing from `combat.js`), beside `fluency` and `resistRoll`.
- **Test layout (D-11 discretion):** a new `test/unit/parley.test.js` with its own local fixtures (the `test/unit/foe-abilities.test.js` precedent) for the combinatorial/behavioural suite; `test/unit/fluency.test.js` for the two leaf helpers (the `test/unit/resist-roll.test.js` precedent); the two EXISTING parley tests in `test/unit/combat.test.js` get their stale comments fixed and two small new tests so 20-02 is self-verifying.
- **Readout home (D-15):** `docs/PARLEY-REBALANCE.md` (the `content/BESTIARY-REBALANCE.md` precedent, but parley is an engine rule, not content, so it lives under `docs/`).

<tasks>

<task type="auto">
  <name>Task 1: Export stripParleyDivergence from the harness, wire it scenario-scoped at BOTH parley replay sites, and prove its exact scope with a carve-out test</name>
  <files>test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/full-suite.test.js, test/unit/parley-carveout.test.js</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-13, D-18, D-19 and the "Parity Divergence" decisions block
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — "Pitfall 2: two independent parity test files replay the SAME parley fixture" and "Note on the D-13 stripper for this specific fixture"
    - .planning/phases/20-parley-balance-language-system/20-PATTERNS.md — the three harness/test entries (stripRationsField analog, combat-parity local comparable, full-suite combat loop)
    - test/parity/harness/comparables.js lines 296-316 (stripRationsField JSDoc + body — the deliberate-permanent-divergence precedent), 196-236 (stripFoeAbilityState + combatComparable — the export style)
    - test/parity/combat-parity.test.js lines 26-31 (imports), 89-120 (local comparable()), 135-178 (the per-scenario test: BOTH diffState calls at lines 140 and 173)
    - test/parity/full-suite.test.js lines 35-41 (harness import list), 114-133 (COMBAT_FIXTURE loop — lines 118 and 129 call bare combatComparable), 135-155 (MAGIC_FIXTURE loop — must stay bare)
    - test/unit/foe-ability-carveouts.test.js (whole file — the exact-field / no-over-strip test style to mirror)
  </read_first>
  <action>
    In `test/parity/harness/comparables.js`, directly after `stripRationsField` (before `economyComparable`), add an exported `stripParleyDivergence(state)` with a JSDoc block in the exact shape of stripRationsField's: it is a DELIBERATE, PERMANENT gameplay divergence (Phase 20, PARLEY-01..PARLEY-04, CONTEXT D-13 / D-18): parley's SP payout becomes `round(combat-equivalent x 0.5)` via `killSpFor`, the Humans wilmst check tightens to `d6 === 6`, the Con Artist bonus drops to +4 with `need` clamped to 17, and two combat-scoped flags (`combat.parleyTried`, `combat.parleyInsulted`) are lazily added — so for the ONE parity-exposed parley (action-script.combat.json scenario `parley`, seed 303) `c.sp` and `c.gold` legitimately differ from the frozen prototype (`test/parity/prototype-master.js.txt` — DO NOT EDIT). The JSDoc must state: applied ONLY to that scenario by BOTH `test/parity/combat-parity.test.js` (local comparable) and `test/parity/full-suite.test.js` (shared combatComparable); every other combat scenario (win/lose/flee), the magic fixture, and every other fixture stay byte-identical with no carve-out; the two flags are stripped defensively (a successful parley nulls `state.combat` before comparison, so for seed 303 only `c.sp`/`c.gold` actually diverge). Body: shallow-copy `state`; if `rest.c` exists, destructure `sp` and `gold` out of it and reassign the remainder; if `rest.combat` is truthy, destructure `parleyTried` and `parleyInsulted` out and reassign the remainder; return `rest`. It must NOT be called from inside `combatComparable`, `movementComparable` or `economyComparable`.

    In `test/parity/combat-parity.test.js`: add `import { stripParleyDivergence } from "./harness/comparables.js";` after the `diffState` import (line 31). Inside the per-scenario `test(...)` callback, as its first statement, define `const cmp = scenario.name === "parley" ? (s) => stripParleyDivergence(comparable(s)) : comparable;` with a two-line comment citing D-13/D-18 (scenario-scoped; see the harness JSDoc). Replace `comparable(ctx.S), comparable(engineState)` with `cmp(ctx.S), cmp(engineState)` at BOTH diffState calls (the initial-boot check at line 140 and the per-action check at line 173). Nothing else in this file changes — the local `comparable()` body stays as is.

    In `test/parity/full-suite.test.js`: add `stripParleyDivergence` to the existing `./harness/comparables.js` import list. Inside the `for (const scenario of COMBAT_FIXTURE.scenarios)` loop (the "combat: every combat scenario matches" test), define the same wrapper as the loop body's first statement — `const cmp = scenario.name === "parley" ? (s) => stripParleyDivergence(combatComparable(s)) : combatComparable;` — and use `cmp(...)` for both the initial `newRun` comparison and the per-action comparison in that loop. The MAGIC_FIXTURE loop, the movement/chargen/economy/encounters tests keep their bare comparables untouched.

    Create `test/unit/parley-carveout.test.js` (header comment: Phase 20 D-13/D-18 — the parley carve-out must strip EXACTLY four fields, must be a no-op-shaped identity otherwise, and must NOT be folded into the shared comparables; mirrors test/unit/foe-ability-carveouts.test.js). Import `stripParleyDivergence` and `combatComparable` from `../parity/harness/comparables.js` and `newRun` from `../../engine/engine.js`. Tests:
    1. "D-13: stripParleyDivergence removes exactly c.sp, c.gold, combat.parleyTried, combat.parleyInsulted" — build `newRun(303)`, set `state.combat = { foes: [], type: "Humans", round: 1, target: 0, spellOpen: false, tracked: false, parleyTried: true, parleyInsulted: true }`; the result's `c` lacks `sp` and `gold` but keeps every other key of the input `c` (compare `Object.keys` sets), the result's `combat` lacks both flags and deepStrictEquals the input combat minus those two keys; the input object is not mutated (deep-equal to a structuredClone taken before the call).
    2. "D-13 no-op shape: a state with combat null and no flags comes back identical minus sp/gold" — `newRun(3)` (combat null): the result deepStrictEquals `{ ...state, c: cWithoutSpGold }` and `result.combat === null` (the stripper never fabricates a combat object).
    3. "D-13 scoping: combatComparable still exposes c.sp and c.gold (the strip is scenario-scoped, never global)" — `combatComparable(newRun(303)).c` has own properties `sp` and `gold` with the same values as the input.
    4. "D-13 defensive flags: flags absent in, flags absent out; flags present in, flags absent out" — two calls, both results have neither own property on `combat`.
  </action>
  <verify>
    <automated>node --test test/unit/parley-carveout.test.js test/unit/foe-ability-carveouts.test.js && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function stripParleyDivergence(state)' test/parity/harness/comparables.js` prints 1
    - `grep -c 'const { sp, gold, ...' test/parity/harness/comparables.js` prints 1 and `grep -c 'const { parleyTried, parleyInsulted, ...' test/parity/harness/comparables.js` prints 1
    - `grep -c 'stripParleyDivergence' test/parity/harness/comparables.js` prints at least 2 (JSDoc mention + export) and `awk '/^export function combatComparable/,/^}/' test/parity/harness/comparables.js | grep -c stripParleyDivergence` prints 0 and `awk '/^export function economyComparable/,/^}/' test/parity/harness/comparables.js | grep -c stripParleyDivergence` prints 0 and `awk '/^export function movementComparable/,/^}/' test/parity/harness/comparables.js | grep -c stripParleyDivergence` prints 0
    - `grep -c 'import { stripParleyDivergence } from "./harness/comparables.js";' test/parity/combat-parity.test.js` prints 1 and `grep -c 'scenario.name === "parley" ? (s) => stripParleyDivergence(comparable(s)) : comparable;' test/parity/combat-parity.test.js` prints 1 and `grep -c 'diffState(cmp(ctx.S), cmp(engineState))' test/parity/combat-parity.test.js` prints 2 and `grep -c 'diffState(comparable(ctx.S), comparable(engineState))' test/parity/combat-parity.test.js` prints 0
    - `grep -c 'scenario.name === "parley" ? (s) => stripParleyDivergence(combatComparable(s)) : combatComparable;' test/parity/full-suite.test.js` prints 1 and `grep -c 'stripParleyDivergence' test/parity/full-suite.test.js` prints 2 (import + wrapper) and `awk '/magic: every magic scenario/,/^  }\);/' test/parity/full-suite.test.js | grep -c stripParleyDivergence` prints 0
    - `node --test test/unit/parley-carveout.test.js` exits 0 with 4 passing tests
    - `node --test "test/parity/**/*.test.js"` exits 0 with 30 pass / 0 fail
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures` exits 0
  </acceptance_criteria>
  <done>The named carve-out exists once in the harness, is applied at both parley replay sites and nowhere else, its exact four-field scope is unit-tested, and the parity suite is still 30/30 with zero frozen-file edits.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tools/tune-difficulty.mjs with the D-15 parley tally (attempts / successes / failures / refusals / exhausted / SP share) in both the text report and --json</name>
  <files>tools/tune-difficulty.mjs</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-15 (readout only; NO dial or policy change) and D-16
    - tools/tune-difficulty.mjs lines 1-30 (header + imports), 115-140 (decideAction — DO NOT change its policy), 142-165 (autoPlayOnce — the tally goes here), 170-230 (distribution/causeBreakdown/printReport — the report section goes here), 250-270 (main --json object)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md — the informational-not-a-gate framing and the ~5-6 minute 200-seed runtime warning
    - engine/combat.js lines 630-672 (the CURRENT parley(): event types parleyRolled / spGained{reason:"parley"} / goldGained{why:"parley"} / parleyFailed / parleyRefused — the tally keys off these literals; parleyExhausted arrives with 20-02)
  </read_first>
  <action>
    In `autoPlayOnce(seed)`: declare six per-run counters before the loop — `parleyAttempts`, `parleySuccesses`, `parleyFailures`, `parleyRefused`, `parleyExhausted`, `parleySp` (all 0). Change the loop's `({ state } = applyAction(state, action));` to also capture `events` from applyAction's return, then for each event: `parleyRolled` increments `parleyAttempts`; `spGained` with `e.reason === "parley"` increments `parleySuccesses` and adds `e.amount` to `parleySp`; `parleyFailed` increments `parleyFailures`; `parleyRefused` increments `parleyRefused`; `parleyExhausted` increments `parleyExhausted`. Add all six plus `spTotal: state.c.sp` to the returned result object (after `actions`). Add a comment above the tally naming Phase 20 / D-15: this is a READOUT (informational, like 18-06's readout), never a gate, and `parleyExhausted` is expected to read 0 until the Phase 20 engine rules land.

    Add a `parleySummary(results)` helper next to `causeBreakdown` that sums the six counters and `spTotal` across runs and returns `{ attempts, successes, failures, refused, exhausted, runsWithAttempt, parleySp, spTotal, successRate, spShare }` where `runsWithAttempt` counts runs with `parleyAttempts > 0`, `successRate = attempts ? successes / attempts : 0`, `spShare = spTotal ? parleySp / spTotal : 0`.

    In `printReport`, after the death-cause breakdown and before the Outcome line, print a section headed exactly `Parley (D-15 readout — informational, not a gate):` with three lines: `  attempts=N  successes=N (P%)  failures=N  refused=N  exhausted=N`, `  runs with >=1 attempt: N of R`, `  SP from parley: X of Y total SP (S%)` — percentages via `.toFixed(1)`. In the `--json` branch of `main`, add `parley: parleySummary(results)`.

    Do NOT change `decideAction` (the bot's parley-preferring policy at wp/maxWP < 0.3 is the fixed instrument both readouts share), `MAX_ACTIONS`, the seed stride, or anything in engine/. No Math.random / Date.
  </action>
  <verify>
    <automated>node tools/tune-difficulty.mjs --seeds=3 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const p=j.parley;const keys=['attempts','successes','failures','refused','exhausted','runsWithAttempt','parleySp','spTotal','successRate','spShare'];process.exit(p&&keys.every(k=>typeof p[k]==='number')?0:1)})"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'parleyAttempts' tools/tune-difficulty.mjs` prints at least 3 and `grep -c 'e.type === "parleyRolled"' tools/tune-difficulty.mjs` prints 1 and `grep -c 'e.type === "parleyExhausted"' tools/tune-difficulty.mjs` prints 1 and `grep -c 'e.reason === "parley"' tools/tune-difficulty.mjs` prints 1
    - `grep -c 'function parleySummary(results)' tools/tune-difficulty.mjs` prints 1 and `grep -c 'Parley (D-15 readout' tools/tune-difficulty.mjs` prints 1 and `grep -c 'parley: parleySummary(results)' tools/tune-difficulty.mjs` prints 1
    - `node tools/tune-difficulty.mjs --seeds=3` exits 0 and its output contains the line `Parley (D-15 readout — informational, not a gate):` (check with `node tools/tune-difficulty.mjs --seeds=3 | grep -c 'Parley (D-15 readout'` printing 1)
    - the --json verify command above exits 0
    - `git diff 04eb229 -- tools/tune-difficulty.mjs | grep '^[-+]' | grep -v '^---\|^+++' | grep -c 'canParley\|ratio < 0.3\|MAX_ACTIONS =\|i \* 7919'` prints 0 (policy, safety stop and seed stride untouched)
    - `grep -c 'Math.random\|new Date\|Date.now' tools/tune-difficulty.mjs` prints 0
  </acceptance_criteria>
  <done>The tool reports parley attempts/successes/failures/refusals/exhausted/SP-share per batch in both output modes, with the bot policy byte-for-byte unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the BEFORE readout against the pre-Phase-20 engine and create docs/PARLEY-REBALANCE.md (change table, seed-303 BEFORE, verbatim readout, empty AFTER section)</name>
  <files>docs/PARLEY-REBALANCE.md</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — every decision D-01..D-21 (the change table transcribes them) and the "Grounding facts" paragraph
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — "Seed-303 Fixture: Exact Before/After" (the BEFORE column and the draw table) and "State of the Art" (old vs new rules table)
    - content/BESTIARY-REBALANCE.md — the tune-difficulty BEFORE/AFTER readout sections (the format to mirror: verbatim transcript, seed count, commit, informational framing)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md lines 108-165 (how the previous executor handled the ~5-6 minute run: run it in the background to a file with an EXIT sentinel, poll in bounded checks, never wait in the foreground)
  </read_first>
  <action>
    First confirm the engine is still pre-Phase-20: `git diff --quiet 04eb229 -- engine content src mazeworld.html` must exit 0 (if it does not, STOP and report — the BEFORE readout would be meaningless).

    Launch the readout in the background so you never block on it: run `node tools/tune-difficulty.mjs --seeds=200` with stdout+stderr redirected to a file in your session scratchpad directory (e.g. `<scratchpad>/parley-before.txt`) and append a final `EXIT=<code>` line when it finishes (Bash `run_in_background`; expect roughly 5-6 minutes). While it runs, write the document below; then poll the file in bounded checks (a short loop that sleeps up to 120 s per check via Monitor/until-loop, never a foreground sleep) until the `EXIT=0` sentinel is present, and transcribe the output verbatim.

    Create `docs/PARLEY-REBALANCE.md` with these sections:
    1. `# Parley Rebalance (Phase 20) — before/after ledger` + a two-sentence preamble: what Phase 20 changes (PARLEY-01..04, LANG-01, LANG-02), and that every number here is INFORMATIONAL (D-15 / the 18-06 precedent) — no dial or economy number changes in this phase; Phase 21 owns the consolidated retune.
    2. `## What changes (locked decisions)` — a table with columns `Rule | Before (prototype / pre-Phase-20 engine) | After (Phase 20) | Decision` and one row each for: SP payout (`round(Σ(d6×lvl) × 2.5)` → `round(Σ killSpFor(c, f, d6) × 0.5)`, D-01/D-02); Humans wilmst check (`d6 >= 4`, 50% → `d6 === 6`, ~17%; amount `d6 × 100 × depth` unchanged, D-03); draw shape (unchanged: one d6 per live foe, one wilmst check, one amount only if it fires, D-04); attempts per encounter (unlimited → one, `C.parleyTried`, `parleyExhausted` on a re-send with zero draws, D-05); failure aggro (none → `C.parleyInsulted`, foe `need += 1` in foeTurn for the rest of the fight, D-06/D-20); Con Artist bonus (+6 → +4, target ~60-65% at even level vs a solo foe: need 13, D-07); need ceiling (none → `min(need, 17)`, D-08); fluency (boolean Language-OR-Helm gate → `fluency(c)` 0/1/2, `+2 × fluency` in the bonus, D-09/D-10); encounter types (TALKATIVE at fluency ≥ 1; Magical only at fluency 2; Walking Dead never, D-11); Wilmsry vs Magical (dead branch → reachable refusal that does not consume the attempt, D-12); Con Artist pre-fight talkdown (unchanged, D-16).
    3. `## Seed-303 fixture (the one parity-exposed parley)` — the BEFORE facts measured on the current engine: character Wilmsry / Thief / Con Artist L1, sp 0, gold 50, no Language, no Helm (fluency 0); startCombat forced Humans rolls Dante ×2, the D-16 talkdown removes one, 1 live foe at parley time; draws `d20=2, d6=5, d6=5, d6=2`; bonus 6+4+1−1 = 10, need 19, roll 2 → success, sp 13, gold +200 (final 250). State that the AFTER column (need 17, sp 7, gold unchanged at 50, 3 draws) is COMPUTED from the locked formulas and will be re-measured and pinned by 20-03 in `test/parity/FIXTURE-INVENTORY.md`; link to that section by heading name.
    4. `## tune-difficulty BEFORE (pre-Phase-20 engine)` — one line stating the measurement conditions (`node tools/tune-difficulty.mjs --seeds=200`, engine/content identical to commit `04eb229`, date), then the full transcript verbatim inside a fenced text block (death-depth, action-count, death-cause, Parley section, Outcome).
    5. `## tune-difficulty AFTER (Phase 20 engine)` — exactly the placeholder line `_Pending — filled by 20-03 after the engine rules land._`
    6. `## Comparison` — the placeholder line `_Pending — 20-03._`
    7. `## Deferred to Phase 21` — the Humans wilmst AMOUNT, any difficulty-dial consequence of parley being less lucrative (from CONTEXT "Deferred Ideas").
    Do not commit the scratch output file; only the transcript inside the doc.
  </action>
  <verify>
    <automated>test -f docs/PARLEY-REBALANCE.md && grep -c 'Parley (D-15 readout' docs/PARLEY-REBALANCE.md | grep -qx 1 && grep -c '^## tune-difficulty BEFORE' docs/PARLEY-REBALANCE.md | grep -qx 1 && grep -c '_Pending — filled by 20-03' docs/PARLEY-REBALANCE.md | grep -qx 1 && git diff --quiet 04eb229 -- engine content src mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/PARLEY-REBALANCE.md` exits 0
    - `grep -c '^## What changes (locked decisions)' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c 'D-0[1-9]\|D-1[0-9]\|D-2[01]' docs/PARLEY-REBALANCE.md` prints at least 12
    - `grep -c '^## Seed-303 fixture' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c 'need 19' docs/PARLEY-REBALANCE.md` prints at least 1 and `grep -c 'd20=2, d6=5, d6=5, d6=2' docs/PARLEY-REBALANCE.md` prints 1
    - `grep -c '^## tune-difficulty BEFORE' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c 'Parley (D-15 readout' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c 'attempts=' docs/PARLEY-REBALANCE.md` prints at least 1 and `grep -c '04eb229' docs/PARLEY-REBALANCE.md` prints at least 1
    - `grep -c 'seeded auto-play run(s)' docs/PARLEY-REBALANCE.md` prints 1 (the verbatim transcript header from printReport) and `grep -c '200 seeded auto-play' docs/PARLEY-REBALANCE.md` prints 1
    - `grep -c '^## tune-difficulty AFTER' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c '_Pending — filled by 20-03' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c '^## Deferred to Phase 21' docs/PARLEY-REBALANCE.md` prints 1
    - `git diff --quiet 04eb229 -- engine content src mazeworld.html` exits 0 (the engine was pre-Phase-20 for the whole plan)
    - `npm test` exits 0 with 882 + 4 = 886 passing tests (the four carve-out tests from Task 1; nothing else changed the count)
  </acceptance_criteria>
  <done>The BEFORE half of the D-15 readout is committed verbatim against the pre-Phase-20 engine, the change ledger names every locked decision, and the AFTER/Comparison sections are explicit placeholders for 20-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen prototype -> parity comparison | `test/parity/prototype-master.js.txt` and fixture JSON are read-only oracles; the harness decides what is compared |
| dev tool -> docs | `tools/tune-difficulty.mjs` output is transcribed into a doc humans read for tuning decisions; must be reproducible (seeded) |
| save file -> state | Unchanged by this plan (no engine edits) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Tampering | Over-broad carve-out blinds sp/gold comparison for scenarios that must stay byte-identical (win/lose/flee, magic) | high | mitigate | scenario-scoped wrapper chosen on `scenario.name === "parley"` only; `test/unit/parley-carveout.test.js` asserts `combatComparable` still exposes `c.sp`/`c.gold`; awk gates prove no strip inside the three shared comparables or the magic loop |
| T-20-02 | Tampering | Frozen master / fixtures edited or regenerated to absorb a divergence | high | mitigate | Prohibited; `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures` acceptance gate |
| T-20-03 | Repudiation | BEFORE readout measured against a partially-changed engine, making the before/after comparison unfalsifiable | medium | mitigate | Task 3 pre-check and post-check `git diff --quiet 04eb229 -- engine content src mazeworld.html`; commit hash recorded in the doc; 20-02 depends on this plan |
| T-20-04 | Tampering | Non-deterministic tally (Math.random / Date) makes readouts irreproducible | low | mitigate | grep gate on `Math.random|new Date|Date.now`; seeds are the fixed `i * 7919 + 1` stride |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet 04eb229 -- package.json package-lock.json` |
</threat_model>

<verification>
- `node --test "test/parity/**/*.test.js"` 30 pass; `npm test` 886 pass (882 baseline + 4 carve-out tests); frozen parity files, engine/, content/, src/, mazeworld.html unchanged since 04eb229.
- `stripParleyDivergence` exported once, applied at exactly two call sites (both parley-scoped), absent from all three shared comparables and from the magic loop.
- `node tools/tune-difficulty.mjs --seeds=3 --json` exposes a numeric `parley` object; `docs/PARLEY-REBALANCE.md` holds the verbatim 200-seed BEFORE transcript with the `Parley (D-15 readout` section.
</verification>

<success_criteria>
- ROADMAP Phase 20 criterion 1's parity precondition is in place: the deliberate seed-303 divergence has a named, scenario-scoped, documented carve-out at both replay sites, so 20-02 can land the payout/odds change with the suite green and everything else still byte-identical.
- D-15's BEFORE half exists and is reproducible (seeded tool, pinned commit), ready for the AFTER half in 20-03.
</success_criteria>

<output>
Create `.planning/phases/20-parley-balance-language-system/20-01-SUMMARY.md` when done (record: the four stripped fields, both call sites, the test-count delta 882 -> 886, the BEFORE readout's headline parley numbers, and the wall-clock of the 200-seed run).
</output>
