---
phase: 20-parley-balance-language-system
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - engine/derived.js
  - engine/combat.js
  - test/unit/fluency.test.js
  - test/unit/combat.test.js
  - src/browser/eventNarration.js
  - test/voice/safety-scan.test.js
  - mazeworld.html
autonomous: true
requirements: [PARLEY-01, PARLEY-02, PARLEY-03, PARLEY-04, LANG-01, LANG-02]
tags: [engine, combat, parley, fluency, derived, narration, ui-mirror, deliberate-divergence, node-test]

must_haves:
  truths:
    - "PARLEY-01 (D-01): engine/derived.js exports killSpFor(c, f, roll) = round((roll * f.lvl) * (5 * spMul * Barbarian 0.5 * Apprentice<3 2)) with the SAME two-step raw/mul expression killFoe used inline, and killFoe now calls it — killFoe's own d6 is drawn at the same call site in the same order, so every combat and magic fixture (win/lose/flee/parley/cast-damage) stays byte-identical and the existing killFoe unit test (d6=4, lvl 2 -> 40) still passes untouched"
    - "PARLEY-01/D-02: on a successful parley c.sp gains round(combatEquivalent * 0.5) where combatEquivalent = Σ killSpFor(c, f, d6) over live foes in foe order — the old literal 2.5x multiplier is gone from every code line of engine/combat.js"
    - "PARLEY-01/D-03: the Humans wilmst payout fires only when the check die reads exactly 6; a check of 5 draws NOTHING further (the amount die is never rolled), so seed 303's fourth draw disappears while the first three draws keep their positions (D-04)"
    - "PARLEY-02/D-05 (second-attempt probe): the first attempt (success OR failure) sets C.parleyTried = true; canParley then returns false; a re-sent parley action pushes exactly [{ type: \"parleyExhausted\" }], draws nothing, and leaves state deep-equal to before"
    - "PARLEY-02/D-06/D-20 (insulted-survives-a-round probe): a failed parley pushes parleyFailed then parleyInsulted, sets C.parleyInsulted = true, and from then on BOTH foeTurn to-hit sites (hero: need += 1; member: mNeed += 1) widen the threshold by exactly 1 as post-draw arithmetic — the narrated roll is the literal die, only need changes; pursuitStrike is untouched; zero extra draws; the flag dies with state.combat at endCombat/die"
    - "PARLEY-03/D-07: the Con Artist bonus term is +4 (was +6): at even level vs a solo foe need = 9 + 4 + level − top = 13 (65%, the documented target; ~60% with a typical +1 top-foe gap)"
    - "PARLEY-03/D-08 (need-clamp-at-exactly-17 probe): need = min(9 + bonus, 17) — bonus 8 gives 17 with the clamp inactive, bonus 9+ gives 17 with the clamp active, so no sub/race/fluency stack is an auto-win (roll 18 vs need 17 fails)"
    - "LANG-01/D-09 (fluency 0/1/2 boundary probe): fluency(c) is 0 with neither, 1 with the Language skill alone (tier 1 or tier 2), 1 with a tongue-effect item alone, 2 with both, 0 when the only item's tongue effect is 0 or c.items/c.skills are undefined; it draws nothing and mutates nothing"
    - "LANG-01/D-10: the same fluency read feeds +2 × fluency into parley's bonus (skill +2, Helm +2, both +4, before the D-08 clamp) and parleyRolled carries a fluency field"
    - "LANG-02/D-11: canParley opens the TALKATIVE set (Humans, Demons, Lair Beasts, Beasts) at fluency >= 1 and additionally Magical at fluency 2 for EVERY race/sub (including Con Artist, who cannot parley Magical at fluency < 2 exactly as today); Walking Dead is refused for everyone unconditionally; the old boolean Language-OR-Helm gate line is gone"
    - "PARLEY-04/D-12 (adjacency probe): a Wilmsry with fluency 2 facing Magical passes canParley and parley() answers with parleyRefused{reason:\"wilmsryVsMagical\"} — zero draws, C.parleyTried NOT set (a refusal is not an attempt), so the branch that was dead is now reachable and a second call still refuses rather than exhausting"
    - "D-14: EVENT_NARRATION has entries for parleyInsulted and parleyExhausted (the coverage guard derives the type set from engine/*.js in BOTH directions, so entries and emitters must land together — this plan), parleyRolled appends the fluency contribution when non-zero, parleyRefused narrates the canon grudge line for reason wilmsryVsMagical, and goldGained with why parley reads as the rare payout it now is; the voice safety scan renders every new branch"
    - "D-17: mazeworld.html's classic (non-module) canParley() — the LIVE gate for the '5 · Parley' button and the '5' key — mirrors the engine's rewrite line for line (parleyTried hides the button, Magical opens at fluency 2 via a new classic fluency() helper), while the classic dead parley() beside it is left byte-identical"
    - "D-16 (untouched probe): startCombat's body (including the Con Artist lvl <= 1 pre-fight talkdown and its d6 draws) is byte-identical to 04eb229 — no flag initialisation is added there (lazy flags, absent ≡ false, so no fixture ever gains a key)"
    - "Parity after this plan: npm test fully green; node --test test/parity/**/*.test.js 30/30 (seed 303 covered by the 20-01 carve-out, every other scenario byte-identical with no carve-out); FID-02 draw pins in test/unit/foe-turn-draw-count.test.js unchanged; test/parity/prototype-master.js.txt and fixture JSON untouched"
    - statement: "The three new/changed narration lines read as unmistakable, deadpan, family-friendly Oracle lines — a failed parley and an exhausted retry never happen silently"
      verification: backstop
  artifacts:
    - path: "engine/derived.js"
      provides: "fluency(c) and killSpFor(c, f, roll) — pure, cycle-free leaf helpers with resistRoll-style JSDoc"
      exports: ["fluency", "killSpFor"]
      contains: "export function fluency(c)"
    - path: "engine/combat.js"
      provides: "killFoe via killSpFor; canParley (parleyTried gate, fluency tiers, Magical at 2); parley (exhausted / refusal / +4 / clamp 17 / +2×fluency / killSpFor×0.5 / d6===6 / insulted); foeTurn need += 1 and mNeed += 1"
      contains: "if (C.parleyInsulted) need += 1;"
    - path: "test/unit/fluency.test.js"
      provides: "fluency tier boundaries; killSpFor equivalence matrix vs the inline formula; killFoe uses killSpFor"
      contains: "killSpFor"
    - path: "test/unit/combat.test.js"
      provides: "existing parley tests with corrected comments + need/fluency assertions; D-05 exhausted test; D-06/D-20 hero-branch need+1 test"
      contains: "parleyExhausted"
    - path: "src/browser/eventNarration.js"
      provides: "parleyInsulted, parleyExhausted entries; parleyRolled fluency annotation; parleyRefused canon line; goldGained parley branch"
      contains: "parleyExhausted:"
    - path: "test/voice/safety-scan.test.js"
      provides: "BASE_EVENT fluency/why tokens + branch toggles so every new ternary renders under the scan"
      contains: "fluency: 2"
    - path: "mazeworld.html"
      provides: "classic fluency() + rewritten classic canParley() mirroring engine/combat.js#canParley"
      contains: "function fluency() {"
  key_links:
    - from: "engine/combat.js killFoe / parley"
      to: "engine/derived.js killSpFor"
      via: "import { ..., fluency, killSpFor } from \"./derived.js\"; killFoe: const gained = killSpFor(c, f, roll); parley: reduce((sum, f) => sum + killSpFor(c, f, rng.d(6)), 0)"
      pattern: "killSpFor\\(c, f, (roll|rng\\.d\\(6\\))\\)"
    - from: "engine/combat.js canParley AND parley"
      to: "engine/derived.js fluency"
      via: "const flu = fluency(c); — one read in the gate, one read in the bonus"
      pattern: "const flu = fluency\\(c\\);"
    - from: "engine/combat.js parley (failure path)"
      to: "engine/combat.js foeTurn (both to-hit sites)"
      via: "C.parleyInsulted = true; then if (C.parleyInsulted) need += 1; / if (C.parleyInsulted) mNeed += 1;"
      pattern: "if \\(C\\.parleyInsulted\\) (need|mNeed) \\+= 1;"
    - from: "engine/combat.js parley event literals"
      to: "src/browser/eventNarration.js EVENT_NARRATION"
      via: "type: \"parleyExhausted\" / type: \"parleyInsulted\" pushes ↔ EVENT_NARRATION.parleyExhausted / .parleyInsulted (formatEventsCoverage.test.js bidirectional guard)"
      pattern: "parley(Exhausted|Insulted):"
    - from: "mazeworld.html renderEncounter (#a-talk button) and the '5' key"
      to: "mazeworld.html classic canParley()"
      via: "existing call sites at ~L4981 and ~L5337 (unchanged); the function body now reads S.combat.parleyTried and fluency()"
      pattern: "if \\(C\\.parleyTried\\) return false;"
  prohibitions:
    - requirement_id: PARLEY-01
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any test/parity/fixtures/*.json, test/parity/harness/comparables.js, test/parity/*.test.js or test/unit/foe-turn-draw-count.test.js in this plan; MUST NOT regenerate any fixture"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/unit/foe-turn-draw-count.test.js exits 0; git diff --quiet HEAD~N -- test/parity (20-01's commits excepted) shows no change from this plan"
    - requirement_id: PARLEY-01
      category: values
      statement: "MUST NOT alter killFoe's draw (one d6, drawn first, unconditionally) or its result — killSpFor must keep the exact two-step raw/mul arithmetic so floating-point rounding is identical"
      status: flagged-unverified
      verification: "parity 30/30 with only the 20-01 carve-out; test/unit/fluency.test.js equivalence matrix; existing killFoe test unchanged and green"
    - requirement_id: PARLEY-02
      category: values
      statement: "MUST NOT add parleyTried / parleyInsulted (or any key) to the state.combat literal in startCombat, and MUST NOT touch pursuitStrike — lazy flags only; both blocks stay hash-identical"
      status: flagged-unverified
      verification: "awk '/^export function startCombat/,/^}/' engine/combat.js | tr -d '\\r' | md5sum prints 29ee82224a85976c14ccef8166763427; the pursuitStrike block prints be9895e12352bc80017542871c3e6181"
    - requirement_id: PARLEY-02
      category: safety
      statement: "MUST NOT add any new rng draw on any path: parleyExhausted and parleyRefused draw 0; the insulted +1 is arithmetic on need/mNeed, never on the drawn roll and never a re-roll"
      status: flagged-unverified
      verification: "fakeRng([]) tests for exhausted/refused; grep gates on the two need/mNeed lines; FID-02 pins unchanged"
    - requirement_id: PARLEY-04
      category: values
      statement: "MUST NOT delete the wilmsryVsMagical refusal branch (it is made reachable, not removed) and MUST NOT let it set C.parleyTried"
      status: flagged-unverified
      verification: "grep -c 'reason: \"wilmsryVsMagical\"' engine/combat.js prints 1; 20-03 parley.test.js asserts parleyTried stays undefined after a refusal"
    - requirement_id: LANG-01
      category: values
      statement: "MUST NOT rename the data keys Language (content/skills.js) or tongue (content/treasure-tables.js); MUST NOT read them anywhere in combat.js except through fluency(c)"
      status: flagged-unverified
      verification: "grep -c '\"tongue\"' engine/combat.js prints 0 after the rewrite (the only tongue read lives in derived.js)"
    - requirement_id: LANG-02
      category: safety
      statement: "MUST NOT use DOM, S, Math.random or Date in engine/derived.js or engine/combat.js (engine purity); the mazeworld.html mirror MUST NOT import from the engine (classic script scope)"
      status: flagged-unverified
      verification: "test/unit/engine-purity.test.js and test/determinism/rng-no-math-random.test.js green"
    - requirement_id: PARLEY-03
      category: values
      statement: "MUST NOT change any other bonus term (Woodsman +3, Wilmsry +4, Elven-vs-Humans +3, level − top), the Humans wilmst AMOUNT (d6 × 100 × depth), or any difficulty/economy dial"
      status: flagged-unverified
      verification: "grep gates on the four untouched terms and on the amount expression"
---

<objective>
Land the whole Phase 20 rules change in the engine and its two presentation mirrors: the two pure leaf helpers (`fluency`, `killSpFor`) in `engine/derived.js`; `killFoe` calling `killSpFor` byte-identically; the `canParley` / `parley` rewrite (one attempt per encounter, insulted aggro, Con Artist +4, need ≤ 17, +2 × fluency, combat-equivalent × 0.5 payout, wilmst on a 6, the Wilmsry-vs-Magical refusal made reachable); the two `foeTurn` `need += 1` sites; the narration lines and their safety-scan tokens; and `mazeworld.html`'s classic `canParley()` duplicate kept in lockstep. 20-01's scenario-scoped carve-out is already in place, so the suite ends green.

Purpose: this is the phase's core — every locked decision D-01..D-12, D-14, D-16, D-17, D-20 is implemented here, with the existing test files extended just enough to be self-verifying (the full behavioural suite, the mirror test, the fixture table and the AFTER readout are 20-03).

Output: `engine/derived.js` (+2 exports), `engine/combat.js` (killFoe / canParley / parley / foeTurn), `test/unit/fluency.test.js` (new), `test/unit/combat.test.js` (comments + 2 tests), `src/browser/eventNarration.js` (2 new + 3 changed entries), `test/voice/safety-scan.test.js` (tokens), `mazeworld.html` (classic `fluency()` + `canParley()`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-parley-balance-language-system/20-CONTEXT.md
@.planning/phases/20-parley-balance-language-system/20-RESEARCH.md
@.planning/phases/20-parley-balance-language-system/20-PATTERNS.md
@.planning/phases/20-parley-balance-language-system/20-01-SUMMARY.md
@engine/derived.js
@engine/combat.js
@src/browser/eventNarration.js
@test/unit/combat.test.js
@test/unit/resist-roll.test.js
@test/voice/safety-scan.test.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `stripParleyDivergence(state)` in the harness + both scenario-scoped call sites; `test/unit/parley-carveout.test.js`; tune-difficulty parley tally; `docs/PARLEY-REBALANCE.md` (BEFORE) | harness / tool / docs | 20-01 | landed before this plan |
| `export function fluency(c)` and `export function killSpFor(c, f, roll)` in `engine/derived.js` | engine (leaf) | **20-02** | D-09 / D-01; JSDoc in the resistRoll style |
| `engine/combat.js`: `killFoe` → `killSpFor`; `canParley` rewrite; `parley` rewrite; `foeTurn` `need += 1` / `mNeed += 1` | engine | **20-02** | D-01..D-08, D-10..D-12, D-20 |
| New serialized combat flags `C.parleyTried`, `C.parleyInsulted` (lazy; absent ≡ false; never persisted — D-19) | engine state | **20-02** | carved out by 20-01's stripper |
| New event types `parleyExhausted`, `parleyInsulted`; new field `parleyRolled.fluency` | engine events | **20-02** | coverage guard |
| `EVENT_NARRATION.parleyInsulted`, `.parleyExhausted`; `.parleyRolled` annotation; `.parleyRefused` canon line; `.goldGained` parley branch | presentation | **20-02** | D-14 |
| `test/voice/safety-scan.test.js` BASE_EVENT `fluency: 2`, `why: "parley"`; toggles `{ fluency: 0 }`, `{ why: null }`, `{ reason: "wilmsryVsMagical" }` | tests | **20-02** | every new branch renders |
| `mazeworld.html` classic `function fluency()` + rewritten classic `function canParley()` | UI (classic script) | **20-02** | D-17; dead classic `parley()` untouched |
| `test/unit/fluency.test.js`; `test/unit/combat.test.js` (+2 tests, comment fixes) | tests | **20-02** | LANG-01, D-05, D-06/D-20 |
| `test/unit/parley.test.js`, `test/unit/parley-button-mirror.test.js`, FIXTURE-INVENTORY section, AFTER readout, phase gate | tests / docs | 20-03 | — |

## Exact rule targets (transcribed from CONTEXT.md — cite these in code comments)

| Term | Value | Decision |
|---|---|---|
| Con Artist bonus | +4 | D-07 |
| Woodsman / Wilmsry / Elven-vs-Humans / level − top | +3 / +4 / +3 / unchanged | canon |
| fluency term | +2 × fluency(c) | D-10 |
| need | min(9 + bonus, 17) | D-08 |
| SP on success | round(Σ killSpFor(c, f, d6) × 0.5) | D-01 / D-02 |
| Humans wilmst check | check die === 6, then amount d6 × 100 × depth | D-03 |
| insulted aggro | need += 1 / mNeed += 1 in foeTurn only | D-06 / D-20 |
| attempts | one per encounter via C.parleyTried; re-send → parleyExhausted, 0 draws | D-05 |
| Wilmsry vs Magical | parleyRefused, 0 draws, attempt not consumed | D-12 |
| Magical availability | fluency === 2 only; Walking Dead never | D-11 |

<tasks>

<task type="auto">
  <name>Task 1: Add fluency(c) and killSpFor(c, f, roll) to engine/derived.js, route killFoe through killSpFor byte-identically, and pin both helpers in test/unit/fluency.test.js</name>
  <files>engine/derived.js, engine/combat.js, test/unit/fluency.test.js</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-01, D-09 and "Claude's Discretion" (helper home)
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — "Recommended helper placement" and the two Code Examples for killSpFor / fluency
    - .planning/phases/20-parley-balance-language-system/20-PATTERNS.md — the `engine/derived.js` entry (resistRoll analog + the exact killFoe call-site diff)
    - engine/derived.js lines 1-40 (header, imports — RACES is already imported; skill/eff are local), 409-448 (intelBonus + resistRoll JSDoc: the "why this leaf module" explanation to mirror)
    - engine/combat.js line 52 (the derived.js import list to extend), lines 445-475 (killFoe: the d6 draw at 461 and the three inline formula lines directly under it, plus the DETERMINISM GATE comment that mentions the removed local name)
    - test/unit/resist-roll.test.js (whole file — the dedicated-leaf-helper test style)
    - test/unit/combat.test.js lines 46-56 (fakeRng), 58-96 (fixedFighter/fixedState — copy the minimal shape), 418-431 (the existing killFoe test: d6=4, lvl 2, Human -> 40, coin d10=6, treasure d20=20)
    - content/races.js (spMul: only Wilmsry carries 0.5) and content/classes.js line 44 (Con Artist is a Thief sub; Barbarian / Apprentice are the two SP-modifying subs)
  </read_first>
  <action>
    In `engine/derived.js`, directly after `resistRoll` (before `levelFromSP`), add two exports with resistRoll-style JSDoc blocks:

    `export function killSpFor(c, f, roll)` — PARLEY-01 (Phase 20, D-01): the ONE kill-skill-point formula shared by `engine/combat.js#killFoe` and `#parley`, so parley's "half the skill points" is STRUCTURALLY half of the same number a kill pays (never a second formula that drifts). Homed here because derived.js is the cycle-free leaf (combat.js imports it; it imports nothing from combat.js). Body: read `const R = RACES[c.race];`, compute `const raw = roll * f.lvl;`, compute `const mul = 5 * (R.spMul || 1) * (c.sub === "Barbarian" ? 0.5 : 1) * (c.sub === "Apprentice" && c.level < 3 ? 2 : 1);`, return `Math.round(raw * mul);` — keep this exact two-step raw/mul structure (it is killFoe's current arithmetic moved verbatim; a re-associated single product could round differently in floating point and break parity). Pure: no rng, no mutation; the caller draws the d6 and passes it in.

    `export function fluency(c)` — LANG-01 (Phase 20, D-09): the single source of truth for language fluency, read by BOTH the canParley availability gate and parley's bonus term (never balanced twice). Returns 0 (neither), 1 (the Language skill OR a tongue-effect item such as the Helm of Knowledge), 2 (both). Body: `(skill(c, "Language") ? 1 : 0) + (eff(c, "tongue") > 0 ? 1 : 0)`. Note in the JSDoc that skill tier does not matter (a raised Language is still 1) and that the data keys `Language` / `tongue` are unchanged.

    In `engine/combat.js`: extend the line-52 import to include `fluency` and `killSpFor`. In `killFoe`, keep `const roll = rng.d(6);` exactly where it is and replace the three local formula lines beneath it (the raw product, the race lookup and the multiplier) with the single line `const gained = killSpFor(c, f, roll);` plus a one-line comment: PARLEY-01 / D-01 — same draw, same call site, same arithmetic (now shared with parley via engine/derived.js). Fix the DETERMINISM GATE comment a few lines below so it refers to the `rng.d(6)` that produced `roll` (it currently names the removed local). No other line of killFoe changes.

    Create `test/unit/fluency.test.js` (header: Phase 20 LANG-01 / PARLEY-01 — D-09 fluency tiers and D-01 killSpFor equivalence; mirrors test/unit/resist-roll.test.js). Import `fluency`, `killSpFor` from `../../engine/derived.js`, `killFoe` from `../../engine/combat.js`, `RACES` from `../../content/index.js`; define a local `fakeRng(seq)` and a minimal `fixedFighter`/`fixedState`/`fixedFoe`/`fixedCombat` copied from combat.test.js. Tests:
    1. "D-09: fluency is 0 / 1 / 1 / 2 for neither / skill / Helm / both" — `{ skills: {}, items: [] }` → 0; `{ skills: { Language: 1 } }` → 1; `{ skills: { Language: 2 } }` → 1 (tier is irrelevant); `{ items: [{ n: "Helm of Knowledge", eff: { tongue: 1 } }] }` → 1; both → 2.
    2. "D-09 boundaries: undefined skills/items, a zero tongue effect, and an unrelated skill all read 0" — `{}` → 0; `{ items: [{ n: "x", eff: { tongue: 0 } }] }` → 0; `{ skills: { Tracking: 1 } }` → 0.
    3. "D-09 purity: fluency neither draws nor mutates" — deepStrictEqual of the character before/after (structuredClone), and the function has arity 1 (`fluency.length === 1`).
    4. "D-01: killSpFor equals the inline killFoe formula for every race × sub × level × foe level × d6" — loop `Object.keys(RACES)` × subs `["Soldier", "Barbarian", "Apprentice", "Con Artist"]` × level `[1, 2, 3]` × lvl `1..5` × roll `1..6`; expected = `Math.round((roll * lvl) * (5 * (RACES[race].spMul || 1) * (sub === "Barbarian" ? 0.5 : 1) * (sub === "Apprentice" && level < 3 ? 2 : 1)))`; assert equal for all 6 × 4 × 3 × 5 × 6 = 2160 cases; plus spot pins: Human Soldier lvl 2 roll 4 → 40; Wilmsry Con Artist level 1 lvl 1 roll 5 → 13 (the seed-303 combat-equivalent); Wilmsry Barbarian lvl 1 roll 1 → 1 (the minimum is still ≥ 1); Human Apprentice level 2 lvl 1 roll 6 → 60; Human Apprentice level 3 lvl 1 roll 6 → 30.
    5. "D-01: killFoe pays exactly killSpFor(c, f, roll) for the d6 it draws (solo)" — reuse the existing killFoe test's setup (Humans foe lvl 2, wp 0, fakeRng([4, 6, 20])): `foeKilled.spGained === killSpFor(state.c, foe, 4)` and `state.c.sp === 40`.
  </action>
  <verify>
    <automated>node --test test/unit/fluency.test.js test/unit/combat.test.js test/unit/resist-roll.test.js test/unit/engine-purity.test.js && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function killSpFor(c, f, roll)' engine/derived.js` prints 1 and `grep -c '^export function fluency(c)' engine/derived.js` prints 1
    - `awk '/^export function killSpFor/,/^}/' engine/derived.js | grep -c 'const raw = roll \* f.lvl;'` prints 1 and `awk '/^export function killSpFor/,/^}/' engine/derived.js | grep -c 'return Math.round(raw \* mul);'` prints 1 and `awk '/^export function killSpFor/,/^}/' engine/derived.js | grep -c 'rng'` prints 0
    - `awk '/^export function fluency/,/^}/' engine/derived.js | grep -c 'skill(c, "Language")'` prints 1 and `awk '/^export function fluency/,/^}/' engine/derived.js | grep -c 'eff(c, "tongue") > 0'` prints 1
    - `grep -c 'fluency, killSpFor } from "./derived.js"' engine/combat.js` prints 1 (or an equivalent single import line containing both names: `grep -c 'killSpFor' engine/combat.js` is at least 2 — import + killFoe call)
    - `awk '/^export function killFoe/,/^}/' engine/combat.js | grep -c 'const gained = killSpFor(c, f, roll);'` prints 1 and `awk '/^export function killFoe/,/^}/' engine/combat.js | grep -c 'const roll = rng.d(6);'` prints 1 and `grep -c 'const raw = roll \* f.lvl' engine/combat.js` prints 0 and `awk '/^export function killFoe/,/^}/' engine/combat.js | grep -v '^\s*//' | grep -c 'RACES\['` prints 0
    - `node --test test/unit/fluency.test.js` exits 0 with 5 passing tests
    - `node --test test/unit/combat.test.js` exits 0 (the existing killFoe test at line 418 is unchanged and green)
    - `node --test "test/parity/**/*.test.js"` exits 0, 30 pass (killFoe extraction is byte-identical: every combat/magic fixture pins its d6)
    - `node --test test/unit/engine-purity.test.js test/determinism/rng-no-math-random.test.js` exits 0
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures` exits 0
  </acceptance_criteria>
  <done>Both leaf helpers exist with the exact arithmetic, killFoe routes through killSpFor with its draw untouched (parity 30/30), and fluency's three tiers and killSpFor's 2160-case equivalence are pinned.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite canParley / parley (D-03..D-08, D-10..D-12) and add the insulted need += 1 at both foeTurn sites (D-06/D-20); fix and extend the existing parley tests in combat.test.js</name>
  <files>engine/combat.js, test/unit/combat.test.js</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-03, D-04, D-05, D-06, D-07, D-08, D-10, D-11, D-12, D-16, D-19, D-20 and "Claude's Discretion" (lazy flag init — resolved in 20-01's "Discretion resolved" section: NEVER initialise in startCombat)
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — "System Flow: a parley attempt, start to finish", the canParley BEFORE/AFTER block, "Bonus/need arithmetic", "Payout", "Failure/insult", "foeTurn insulted +1 — TWO call sites", Pitfall 4 (need, not roll)
    - .planning/phases/20-parley-balance-language-system/20-PATTERNS.md — the `canParley / parley rewrite` entry (line-by-line change list) and the `foeTurn insulted +1` entry
    - engine/combat.js lines 64 (TALKATIVE), 598-672 (canParley + parley — the whole rewrite target, including the Phase 15 DELIBERATE RULES CHANGE comment block to replace), 737-770 (endCombat: nulls state.combat — the flags die here, nothing to add), 1199-1201 and 1221-1223 (the two foeTurn to-hit blocks), 521-531 (pursuitStrike — DO NOT touch)
    - test/unit/combat.test.js lines 782-839 (the four existing parley tests) and 46-56 (fakeRng), 88-110 (fixtures)
    - engine/derived.js foeToHitVs (lines 331-343): the fixedFighter (Human Soldier, no Agility, no items, lit floor) needs 5 — the number the new insulted test pins
  </read_first>
  <action>
    Rewrite `canParley(state)` in `engine/combat.js` to this exact decision order (each line a plain `if ... return`): (1) `!state.combat` → false; (2) bind `c = state.c`, `C = state.combat`, `t = C.type`; (3) `C.parleyTried` → false (D-05 — hides the button after the encounter's one attempt); (4) `t === "Walking Dead"` → false (canon, unconditional, for everyone); (5) `const flu = fluency(c);` (6) `t === "Magical" && flu < 2` → false (D-11 — Magical opens ONLY at full fluency, for every race/sub); (7) Con Artist → true; Woodsman and (Beasts or Lair Beasts) → true; Bard and Humans → true (unchanged); (8) `const talkable = flu >= 2 ? [...TALKATIVE, "Magical"] : TALKATIVE;` then `flu >= 1 && talkable.includes(t)` → true (D-10/D-11 — this REPLACES the old boolean skill-or-Helm gate line); (9) Wilmsry and `t !== "Magical"` → true (unchanged — a fluency-2 Wilmsry reaches Magical through step 8, which is exactly why parley()'s refusal below becomes reachable, D-12); Elven and Humans → true; return false. Replace the Phase 15 Helm-of-Knowledge DELIBERATE RULES CHANGE comment block with a Phase 20 block (LANG-01 / LANG-02, D-09 / D-10 / D-11 / D-05): fluency tiers via engine/derived.js#fluency, Magical at fluency 2, Walking Dead never, parleyTried gate; pure read, no rng; and a pointer that mazeworld.html's classic canParley() duplicate (D-17) and test/unit/parley-button-mirror.test.js (20-03) mirror this order line for line — change one, change both.

    Rewrite `parley(state, rng, events)` to this exact order: (1) bind `c`, `C`; `!C` → return events; (2) `C.parleyTried` → push `{ type: "parleyExhausted" }` and return (D-05 — a re-sent action after the encounter's one attempt; zero draws; this check sits BEFORE the canParley re-check because canParley is already false once tried and would return silently); (3) `!canParley(state)` → return events (unchanged silent path for never-eligible cases); (4) the LO-03 zero-live-foes guard, unchanged; (5) `top`, unchanged; (6) the Wilmsry-and-Magical refusal, unchanged text and event (`parleyRefused`, reason `wilmsryVsMagical`) — add a comment: PARLEY-04 / D-12, now REACHABLE (a fluency-2 Wilmsry passes canParley for Magical), zero draws, and it deliberately sits BEFORE the parleyTried write so a refusal never consumes the attempt; (7) `C.parleyTried = true;` (D-05 — the one attempt is spent here, after the refusal, before the roll; lazily written — never initialised in startCombat, absent ≡ false); (8) `const flu = fluency(c);` and the bonus sum: Con Artist term becomes 4 (D-07; comment the documented target: even level vs a solo foe → need 13 → 65%, ~60% with a typical +1 top-foe gap — still the best talker), Woodsman 3, Wilmsry 4, Elven-vs-Humans 3 (all unchanged), plus `2 * flu` (D-10), plus `c.level - top`; (9) `const need = Math.min(9 + bonus, 17);` (D-08 — 85% ceiling so no stack is an auto-win; arithmetic only); (10) `const roll = rng.d(20);` then push `{ type: "parleyRolled", roll, need, fluency: flu }` (D-14 annotation field); (11) success (`roll <= need`): `const combatEquivalent = liveFoes(state).reduce((sum, f) => sum + killSpFor(c, f, rng.d(6)), 0);` — one d6 per live foe in foe order, same draw shape as before (D-04) — then `const sp = Math.round(combatEquivalent * 0.5);` (D-01/D-02 — canon "half the skill points for none of the blood", now structurally half of killFoe's number; the old literal two-and-a-half multiplier is retired), `c.sp += sp`, push `spGained` with reason `parley` (unchanged shape); then the Humans wilmst branch with the check tightened to `rng.d(6) === 6` (D-03 — was the at-least-4 threshold, 50% → ~17%; the amount `rng.d(6) * 100 * state.floor.depth` and its `goldGained { amount, why: "parley" }` event are unchanged, Phase 21 owns the number); `checkLevel`, `endCombat`, return — unchanged; (12) failure: push `parleyFailed` (unchanged), then `C.parleyInsulted = true;` and push `{ type: "parleyInsulted" }` (D-06 — the group takes it personally for the rest of the fight; the flag dies with state.combat at endCombat/die, D-19: never persisted), then `afterPlayerAction(state, rng, events)` and return — unchanged. Put one "DELIBERATE RULES CHANGE (Phase 20, PARLEY-01/02/03, D-01..D-08)" comment block above the function summarising the deviations from the ported prototype lines and pointing at test/parity/FIXTURE-INVENTORY.md's Phase 20 section and the 20-01 carve-out.

    In `foeTurn`, add `if (C.parleyInsulted) mNeed += 1;` immediately after the member branch's `foeToHitPenalty` line (~1201) and `if (C.parleyInsulted) need += 1;` immediately after the hero branch's `foeToHitPenalty` line (~1223), each with a one-line comment: PARLEY-02 / D-06 / D-20 — insulted aggro, post-draw arithmetic on the threshold (never the roll — matches the Agility / foeToHitPenalty precedent), zero extra draws. Do NOT touch pursuitStrike (D-06 scopes the aggro to foeTurn; the flee path stays canon). Do NOT touch startCombat or endCombat.

    In `test/unit/combat.test.js`: (a) in the gate-matrix test (line 782) change the "Magical is never talkative" message to "Magical needs fluency 2 (D-11); fluency 0 here" and append two assertions: `canParley(mk({ sub: "Con Artist" }, "Walking Dead")) === false` and `canParley(mk({ sub: "Con Artist", skills: { Language: 1 }, items: [{ n: "Helm of Knowledge", eff: { tongue: 1 } }] }, "Magical")) === true`; (b) in the Con Artist success test (line 801) rewrite the arithmetic comment: bonus = 4 (Con Artist, D-07) + level 1 − top 1 = 4 → need 13; roll 10 ≤ 13 → success; per-foe d6=3 → killSpFor(Human, lvl 1, 3) = 15 → sp = round(15 × 0.5) = 8 (D-01/D-02 — the same 8 the old formula happened to give); Humans wilmst check d6=2 (not 6, skipped — D-03); the rng sequence `[10, 3, 2]` stays; add `assert.ok(events.some((e) => e.type === "parleyRolled" && e.need === 13 && e.fluency === 0));`; (c) in the failing-roll test (line 814) fix "need(15)" to "need(13)" in the comment and add: `parleyInsulted` is in events and comes AFTER `parleyFailed` (compare indexOf), `state.combat.parleyInsulted === true`, `state.combat.parleyTried === true`; (d) add test "D-05: the second parley in the same encounter is refused with parleyExhausted and draws nothing" — same setup as (c) (Con Artist, Humans foe asleep 5, fakeRng([20, 15, 10]) for the first, failing call); then `assert.equal(canParley(state), false)`; snapshot `structuredClone(state)`; second call `parley(state, fakeRng([]), [])` returns events deepStrictEqual `[{ type: "parleyExhausted" }]` and state deepStrictEqual the snapshot; (e) add test "D-06/D-20: parleyInsulted widens the foe's need by exactly 1 on the hero branch, the roll is untouched" — fixedState + awake fixedFoe (Beasts lvl 1); baseline: `foeTurn(state, fakeRng([6, 3]), [])` → a `foeMissed` event with `need === 5` and `roll === 6`, `state.c.wp === 55`; then a fresh state with `state.combat.parleyInsulted = true`: `foeTurn(state, fakeRng([6, 3]), [])` → a `struckByFoe` event with `need === 6`, `roll === 6`, `dmg === 4` (1 + d6 3), `state.c.wp === 51`; the LO-03 test and all other tests stay untouched.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/fluency.test.js test/unit/item-wiring.test.js test/unit/foe-turn-draw-count.test.js test/unit/party-combat.test.js test/unit/engine-purity.test.js && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^export function canParley/,/^}/' engine/combat.js | grep -c 'if (state.combat.parleyTried) return false;\|if (C.parleyTried) return false;'` prints 1 and `awk '/^export function canParley/,/^}/' engine/combat.js | grep -c 'if (t === "Walking Dead") return false;'` prints 1 and `awk '/^export function canParley/,/^}/' engine/combat.js | grep -c 'if (t === "Magical" && flu < 2) return false;'` prints 1 and `awk '/^export function canParley/,/^}/' engine/combat.js | grep -c 'const talkable = flu >= 2 ? \[...TALKATIVE, "Magical"\] : TALKATIVE;'` prints 1 and `awk '/^export function canParley/,/^}/' engine/combat.js | grep -c 'if (flu >= 1 && talkable.includes(t)) return true;'` prints 1
    - `grep -v '^\s*//' engine/combat.js | grep -c 'if (t === "Walking Dead" || t === "Magical") return false;'` prints 0 and `grep -v '^\s*//' engine/combat.js | grep -c 'skill(c, "Language") || eff(c, "tongue") > 0'` prints 0 and `grep -v '^\s*//' engine/combat.js | grep -c '"tongue"'` prints 0 (code lines only — the only tongue read lives in derived.js#fluency)
    - `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'events.push({ type: "parleyExhausted" });'` prints 1 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'reason: "wilmsryVsMagical"'` prints 1 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'C.parleyTried = true;'` prints 1 and the refusal line number is smaller than the `C.parleyTried = true;` line number (`awk '/^export function parley/,/^}/' engine/combat.js | grep -n 'wilmsryVsMagical\|C.parleyTried = true' | head -2` lists wilmsryVsMagical first)
    - `awk '/^export function parley/,/^}/' engine/combat.js | grep -c '(c.sub === "Con Artist" ? 4 : 0)'` prints 1 and `grep -v '^\s*//' engine/combat.js | grep -c '"Con Artist" ? 6 : 0'` prints 0 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c '2 \* flu'` prints 1 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'const need = Math.min(9 + bonus, 17);'` prints 1 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c '(c.sub === "Woodsman" ? 3 : 0)\|(c.race === "Wilmsry" ? 4 : 0)\|(c.race === "Elven" && C.type === "Humans" ? 3 : 0)'` prints 3
    - `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'events.push({ type: "parleyRolled", roll, need, fluency: flu });'` prints 1
    - `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'killSpFor(c, f, rng.d(6))'` prints 1 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'const sp = Math.round(combatEquivalent \* 0.5);'` prints 1 and `grep -v '^\s*//' engine/combat.js | grep -c '\* 2.5'` prints 0
    - `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'rng.d(6) === 6'` prints 1 and `grep -v '^\s*//' engine/combat.js | grep -c 'rng.d(6) >= 4'` prints 0 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'rng.d(6) \* 100 \* state.floor.depth'` prints 1
    - `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'C.parleyInsulted = true;'` prints 1 and `awk '/^export function parley/,/^}/' engine/combat.js | grep -c 'events.push({ type: "parleyInsulted" });'` prints 1
    - `grep -c 'if (C.parleyInsulted) need += 1;' engine/combat.js` prints 1 and `grep -c 'if (C.parleyInsulted) mNeed += 1;' engine/combat.js` prints 1 and `grep -c 'parleyInsulted' engine/combat.js` counts only lines inside parley and foeTurn (`awk '/^function pursuitStrike/,/^}/' engine/combat.js | grep -c parleyInsulted` prints 0)
    - `awk '/^export function startCombat/,/^}/' engine/combat.js | tr -d '\r' | md5sum | cut -c1-32` prints `29ee82224a85976c14ccef8166763427` and `awk '/^function pursuitStrike/,/^}/' engine/combat.js | tr -d '\r' | md5sum | cut -c1-32` prints `be9895e12352bc80017542871c3e6181` (D-16: startCombat and the flee-pursuit path are byte-identical to 04eb229)
    - `grep -c 'DELIBERATE RULES CHANGE (Phase 20' engine/combat.js` prints at least 1
    - `grep -c 'parleyExhausted' test/unit/combat.test.js` prints at least 2 and `grep -c 'e.need === 13 && e.fluency === 0' test/unit/combat.test.js` prints 1 and `grep -c 'need === 6' test/unit/combat.test.js` prints at least 1
    - `node --test test/unit/combat.test.js` exits 0 with the previous count + 2 passing tests; `node --test test/unit/item-wiring.test.js` exits 0 (the Helm → Humans test at line 101 still holds at fluency 1)
    - `node --test test/unit/foe-turn-draw-count.test.js` exits 0 with ZERO edits to that file (`git diff --quiet 04eb229 -- test/unit/foe-turn-draw-count.test.js` exits 0) — the FID-02 pins (combat/parley seed 303: 6 attacks, 66 draws) are unchanged because that scenario never calls parley
    - `node --test "test/parity/**/*.test.js"` exits 0, 30 pass — seed 303's parley scenario now diverges ONLY in c.sp/c.gold, which 20-01's stripParleyDivergence carves out; `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures` exits 0
  </acceptance_criteria>
  <done>canParley and parley implement every locked term in the stated order, the insulted +1 lands at exactly the two foeTurn thresholds, startCombat/pursuitStrike are hash-identical, and the existing suite (with two new tests) plus parity are green — the coverage guard is the only remaining red until Task 3 adds the narration entries.</done>
</task>

<task type="auto">
  <name>Task 3: Narrate the new events (D-14) with safety-scan tokens, and mirror fluency()/canParley() into mazeworld.html's classic script (D-17)</name>
  <files>src/browser/eventNarration.js, test/voice/safety-scan.test.js, mazeworld.html</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-14, D-17 and "Specific Ideas" ("nothing happens silently"; keep the exhausted/insulted lines unmistakable); D-12 (the canon grudge line)
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — Pitfall 1 (the classic canParley() is LIVE, the classic parley() is dead) and the "mazeworld.html's classic canParley() duplicate" code example
    - .planning/phases/20-parley-balance-language-system/20-PATTERNS.md — the eventNarration entry and the mazeworld.html entry (call sites ~L4981, ~L5337, ~L6261)
    - src/browser/eventNarration.js lines 105-118 (spGained with the parley reason), 180-192 (parleyRefused / parleyRolled / goldGained / parleyFailed — the entries to change/extend)
    - test/voice/safety-scan.test.js lines 106-135 (BASE_EVENT, BRANCH_TOGGLES — the token plumbing every new ternary needs), 148-160 (the scan loop)
    - test/unit/formatEventsCoverage.test.js lines 100-140 (bidirectional guard: every engine literal needs an entry AND every entry needs an engine literal)
    - mazeworld.html lines 1707-1708 (classic `skill`), 3281-3285 (classic `eff(key)`), 3718-3741 (the DR8 comment: which classic functions are live), 4199-4232 (TALKATIVE, classic canParley() — the rewrite target — and the dead classic parley() to leave untouched), 4981 and 5337 (the two live call sites — unchanged)
    - engine/combat.js canParley as rewritten in Task 2 (the order to mirror line for line)
  </read_first>
  <action>
    In `src/browser/eventNarration.js`, in the combat.js block next to the existing parley entries: (1) change `parleyRefused` to a builder that returns the canon grudge line `<span class="miss">Magic Users hate the Wilmsry. There is nothing to discuss.</span>` when `e.reason === "wilmsryVsMagical"` and keeps the current "Not this time, not with them." text otherwise (D-12 / D-14 — canon text preserved); (2) extend `parleyRolled` so that after the `need` value it appends ` (+N for the tongue)` where N is `e.fluency * 2`, only when `e.fluency` is truthy (D-14 — e.g. "Talk it down: 7 vs 15 (+2 for the tongue)."); (3) add `parleyInsulted: () => \`<span class="miss">You have made it personal.</span> They will be aiming with real intent from here on.\`` (D-06 aggro, unmistakable); (4) add `parleyExhausted: () => \`<span class="miss">You already said your piece.</span> They are done listening; try the pointy end.\`` (D-05 retry refused); (5) change `goldGained` so that when `e.why === "parley"` it reads `One of them, against the odds, pays you to forget the whole thing. <span class="hit">+N wilmst.</span>` and otherwise keeps the current `+N wilmst.` line (D-14 — the success line reflects the now-rare wilmst payout). Keep every `<span class="...">` class convention (`miss` for bad news, `hit` for gains, `roll` for dice). Family-friendly, deadpan, no profanity.

    In `test/voice/safety-scan.test.js`: add `fluency: 2` and `why: "parley"` to `BASE_EVENT`, and add `{ fluency: 0 }`, `{ why: null }`, `{ reason: "wilmsryVsMagical" }` to `BRANCH_TOGGLES` so both sides of each new ternary render under the scan.

    In `mazeworld.html`, in the classic (non-module) script, directly after `const TALKATIVE = [...]` (line 4199): add `function fluency()` returning `(skill("Language") ? 1 : 0) + (eff("tongue") > 0 ? 1 : 0)` (the classic `skill` and `eff(key)` read S.c directly), preceded by a comment block: Phase 20 (D-17, LANG-01/LANG-02, PARLEY-02) — this classic canParley()/fluency() pair is LIVE (it gates the "5 · Parley" button in renderEncounter and the "5" key — see the DR8 note above) and is a hand-maintained mirror of engine/combat.js#canParley / engine/derived.js#fluency; test/unit/parley-button-mirror.test.js (20-03) extracts these two functions from this file and replays the engine's availability matrix through them, so any drift fails npm test; the classic parley() below stays dead code and is deliberately untouched. Then rewrite the classic `function canParley()` body to the engine's exact order: `!S.combat` → false; `const C = S.combat;` `C.parleyTried` → false; `const t = C.type;` `t === "Walking Dead"` → false; `const flu = fluency();` `t === "Magical" && flu < 2` → false; Con Artist → true; Woodsman and (Beasts or Lair Beasts) → true; Bard and Humans → true; `const talkable = flu >= 2 ? [...TALKATIVE, "Magical"] : TALKATIVE;` `flu >= 1 && talkable.includes(t)` → true; Wilmsry and `t !== "Magical"` → true; Elven and Humans → true; return false. The old combined Walking-Dead-or-Magical early return and the old skill-only Language gate line are removed from this function (the fluency branch replaces the latter). Preserve the file's CRLF line endings (edit in place; do not rewrite the whole file). Do not touch the dead classic `parley()` (its 20-line block must stay hash-identical), the two call sites, or `window.mzParley`.
  </action>
  <verify>
    <automated>node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/foe-effect-chip.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  parleyInsulted: ' src/browser/eventNarration.js` prints 1 and `grep -c '^  parleyExhausted: ' src/browser/eventNarration.js` prints 1 and `grep -c 'Magic Users hate the Wilmsry. There is nothing to discuss.' src/browser/eventNarration.js` prints 1 and `grep -c 'for the tongue' src/browser/eventNarration.js` prints 1 and `grep -c 'e.why === "parley"' src/browser/eventNarration.js` prints 1 and `grep -c 'e.reason === "wilmsryVsMagical"' src/browser/eventNarration.js` prints 1
    - `grep -c 'fluency: 2' test/voice/safety-scan.test.js` prints 1 and `grep -c '{ fluency: 0 }' test/voice/safety-scan.test.js` prints 1 and `grep -c '{ reason: "wilmsryVsMagical" }' test/voice/safety-scan.test.js` prints 1 and `grep -c 'why: "parley"' test/voice/safety-scan.test.js` prints 1
    - `node --test test/unit/formatEventsCoverage.test.js` exits 0 (both directions: parleyExhausted/parleyInsulted narrated; no orphan entries) and `node --test test/voice/safety-scan.test.js` exits 0
    - `grep -c '^function fluency() {' mazeworld.html` prints 1 and `grep -c '^function canParley() {' mazeworld.html` prints 1 and `awk '/^function canParley\(\) \{/,/^}/' mazeworld.html | grep -c 'if (C.parleyTried) return false;'` prints 1 and `awk '/^function canParley\(\) \{/,/^}/' mazeworld.html | grep -c 'if (t === "Magical" && flu < 2) return false;'` prints 1 and `awk '/^function canParley\(\) \{/,/^}/' mazeworld.html | grep -c 'const talkable = flu >= 2 ? \[...TALKATIVE, "Magical"\] : TALKATIVE;'` prints 1 and `awk '/^function canParley\(\) \{/,/^}/' mazeworld.html | grep -c 'if (t === "Walking Dead") return false;'` prints 1
    - `grep -v '^\s*//' mazeworld.html | grep -c 'if (t === "Walking Dead" || t === "Magical") return false;'` prints 0 and `grep -v '^\s*//' mazeworld.html | grep -c 'if (skill("Language") && TALKATIVE.includes(t)) return true;'` prints 0
    - `awk '/^function parley\(\) \{/,/^}/' mazeworld.html | tr -d '\r' | md5sum | cut -c1-32` prints `da8097f5e4101da9cf971e28009f0cf3` (the dead classic parley() is untouched) and `grep -c 'canParley() ? `<button id="a-talk">5 · Parley</button>` : ""' mazeworld.html` prints 1 and `grep -c 'k === "5" && canParley()' mazeworld.html` prints 1 (call sites unchanged)
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');process.exit((s.match(/(?<!\r)\n/g)||[]).length===0?0:1)"` exits 0 (CRLF preserved — no mixed line endings introduced)
    - `npm test` exits 0 with every test passing (expected 886 + 5 fluency + 2 combat = 893; report the exact number in the SUMMARY)
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/unit/foe-turn-draw-count.test.js package.json package-lock.json` exits 0
  </acceptance_criteria>
  <done>Every new/changed event narrates in voice and passes the safety scan, the coverage guard is green in both directions, the live classic canParley() mirrors the engine's order exactly with the dead classic parley() untouched, and `npm test` is fully green at the end of the plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action dispatch -> engine | A re-sent `parley` action (button hidden, key pressed, or a scripted client) must be rejected without side effects |
| engine -> frozen prototype parity | Every fixture except the carved-out seed-303 parley scenario must stay byte-identical |
| save file -> state | `state.combat` (home of both new flags) is nulled on every load (D-19) — no stale flag can be injected |
| engine events -> player toast | New narration is authored text under the family-friendly scan |
| engine gate -> UI gate | Two hand-maintained copies of `canParley` must agree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Elevation of Privilege | Parley spam — repeating the action for free foe turns / rng probing | medium | mitigate | D-05 `C.parleyTried` gate: `canParley` false + `parleyExhausted` with zero draws and no state change; D-05 test in combat.test.js and 20-03 |
| T-20-06 | Tampering | `killSpFor` extraction changes a kill's draw or rounding, desyncing every combat fixture | high | mitigate | verbatim two-step raw/mul arithmetic; parity 30/30; 2160-case equivalence test; existing killFoe test untouched |
| T-20-07 | Tampering | A stray flag initialisation in `startCombat` adds a key to every fixture's combat object | high | mitigate | lazy flags only; startCombat block md5 pinned (`29ee8222…`); parity gate |
| T-20-08 | Tampering | Insulted +1 applied to the drawn roll (or as a re-roll) instead of the threshold, changing draw counts or narrated dice | medium | mitigate | `need += 1` / `mNeed += 1` only; pursuitStrike md5 pinned; FID-02 pins untouched; D-06 test asserts roll 6 / need 6 |
| T-20-09 | Tampering | Save file carrying `parleyTried`/`parleyInsulted` resurrects mid-fight flags | low | accept | `validateSave`/`rehydrate` null `state.combat` unconditionally (D-19, confirmed by direct read); 20-03 adds a round-trip probe |
| T-20-10 | Information Disclosure | Off-voice or unsafe narration reaches a PEGI-3 toast | medium | mitigate | BASE_EVENT/BRANCH_TOGGLES tokens make every new branch render under `test/voice/safety-scan.test.js` |
| T-20-11 | Spoofing | UI shows a Parley button the engine will refuse (or hides one it would accept) because the classic duplicate drifts | medium | mitigate | D-17 mirror in this plan + `test/unit/parley-button-mirror.test.js` in 20-03 replays the matrix through the extracted classic source |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet 04eb229 -- package.json package-lock.json` |
</threat_model>

<verification>
- `npm test` fully green (expected 893; exact count recorded in the SUMMARY); `node --test "test/parity/**/*.test.js"` 30/30; FID-02 file and frozen parity files unchanged since 04eb229.
- Source gates: both derived.js exports; killFoe via killSpFor; canParley order (parleyTried / Walking Dead / Magical-at-2 / talkable); parley order (exhausted → canParley → LO-03 → refusal → parleyTried → +4 / +2×flu / min 17 → d20 → killSpFor×0.5 → `=== 6` → insulted); two foeTurn `+= 1` lines; startCombat and pursuitStrike md5-identical; mazeworld.html classic canParley mirrored, dead parley() md5-identical.
- Coverage guard green in both directions; safety scan green with the new tokens.
</verification>

<success_criteria>
- ROADMAP Phase 20 criteria 1-4 are implemented in the engine: payout ≤ combat-equivalent by construction (shared `killSpFor`, ×0.5); one attempt per encounter plus insulted aggro; Con Artist +4 with the documented 13/20 target; fluency feeds both the gate and the bonus, Magical opens at fluency 2, Walking Dead never; the Wilmsry-vs-Magical branch is reachable and does not consume the attempt.
- The suite is green at the end of the plan with the deliberate divergence confined to the carved-out seed-303 scenario.
</success_criteria>

<output>
Create `.planning/phases/20-parley-balance-language-system/20-02-SUMMARY.md` when done (record: the final canParley/parley order as landed, the two foeTurn insertion line numbers, the exact narration strings, the test-count delta, and confirmation that startCombat / pursuitStrike / classic parley() hashes matched).
</output>
