---
phase: 20-parley-balance-language-system
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - test/unit/parley.test.js
  - test/unit/parley-button-mirror.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/PARLEY-REBALANCE.md
autonomous: true
requirements: [PARLEY-01, PARLEY-02, PARLEY-03, PARLEY-04, LANG-01, LANG-02]
tags: [tests, parley, availability-matrix, property-test, draw-count, counting-rng, ui-mirror, fixture-inventory, readout, phase-gate, node-test]

must_haves:
  truths:
    - "LANG-02/D-11: test/unit/parley.test.js replays the FULL availability matrix — every race in RACES (6) × sub {Con Artist, Woodsman, Bard, Soldier} × {no Language, Language} × {no Helm, Helm of Knowledge} × every ENC_TYPES entry (6) = 576 cases — against an oracle written from the D-11/D-12 rules (not copied from the implementation), and asserts the aggregates: Magical is available in exactly 24 cases (only at fluency 2), Walking Dead in 0, and a plain Human Soldier at fluency 0 in 0"
    - "PARLEY-04/D-12: every canParley gate has an explicit, individually-messaged assertion (no combat; parleyTried; Walking Dead for Con Artist / Wilmsry / fluency 2; Magical at fluency 0/1/2; Woodsman Beasts + Lair Beasts vs Humans; Bard Humans vs Beasts; fluency 1 via skill and via Helm over all four TALKATIVE types; fluency 2 Magical; Wilmsry per type incl. Magical at fluency 0 and 2; Elven Humans vs Beasts; the no-gate Human Soldier), and the reachable Wilmsry-vs-Magical refusal is proven to draw nothing (fakeRng([])), to leave parleyTried undefined, and to refuse again (not exhaust) on a second call"
    - "PARLEY-02/D-05 (dispatcher-path probe): through the real applyAction pipeline a state whose combat already has parleyTried true yields exactly [{ type: \"parleyExhausted\" }] and an UNCHANGED rngState (zero draws in the real rng, not just a fake); after a SUCCESSFUL parley state.combat is null so there is nothing left to retry"
    - "PARLEY-02/D-06/D-20 (member branch + persistence probes): with C.parleyInsulted the member-branch mNeed reads 6 not 5 for the fixed fighter (a to-hit die of 6 that missed the member now lands: memberStruck need 6, roll 6), the +1 persists across two consecutive foeTurn calls in the same fight, and endCombat nulls the combat and the flag with it"
    - "PARLEY-03/D-07/D-08: parleyRolled.need is 13 for a Con Artist at even level vs a solo foe and 12 vs a foe one level up; need is 17 with the clamp inactive (bonus 8: Con Artist Wilmsry, even level) AND 17 with the clamp active (bonus 12: Con Artist Wilmsry with fluency 2); a roll of 18 vs need 17 fails while 17 succeeds"
    - "LANG-01/D-10: a Bard vs Humans (bonus 0 at even level, need 9) reads need 11 with the skill alone, 11 with the Helm alone, 13 with both, and parleyRolled.fluency is 0/1/1/2 respectively"
    - "PARLEY-01/D-01/D-02: with two live Humans foes (lvl 1 and lvl 2) and per-foe d6 of 3 and 5, the payout is round((15 + 50) × 0.5) = 33 in foe order; a dead foe contributes nothing and draws nothing; the property round(eq × 0.5) ≤ eq holds for every race × sub × foe level 1..5 × d6 1..6 and for every two-foe sum"
    - "PARLEY-01/D-03/D-04: the Humans wilmst check pays only on a 6 (a check of 4 or 5 pays nothing and draws no amount die — proven by fakeRng exhaustion) and pays d6 × 100 × depth on a 6 (depth 3, amount die 4 → +1200); countingRng pins the draw shape: exhausted 0, refused 0, non-Humans success 1 + N, Humans success without payout 1 + N + 1, with payout 1 + N + 2"
    - "D-21 seed-303 pin (re-measured, not computed): newRun(303) → startCombat(forced Humans) → parley draws exactly d20=2, d6=5, d6=5 (three draws; the old fourth draw is gone) and yields parleyRolled { roll 2, need 17, fluency 0 }, spGained { amount 7, reason parley }, no goldGained, combatEnded, c.sp 7, c.gold 50, combat null — for a Wilmsry Con Artist L1 at fluency 0"
    - "D-19 (old-save probe): a mid-fight state carrying parleyTried/parleyInsulted survives JSON.stringify/parse losslessly (booleans), and serializeRun → validateSave → rehydrate loads it with combat null and the character intact — no migration code needed, no data loss"
    - "D-17: test/unit/parley-button-mirror.test.js extracts the classic fluency() and canParley() source from mazeworld.html (CRLF-normalised), builds them with new Function against a stubbed S/skill/eff/TALKATIVE, and replays the same 576-case matrix plus the parleyTried and no-combat cases against engine/combat.js#canParley — zero disagreements"
    - "D-13/D-21: test/parity/FIXTURE-INVENTORY.md gains a '## Phase 20 parley divergence' section AFTER the generated block with the seed-303 before/after table (need 19 → 17, sp 13 → 7, gold 250 → 50, draws 4 → 3, first three dice unchanged), the carve-out description (stripParleyDivergence at both replay sites), the note that the two flags never appear in the compared state for this success-only scenario, and the statement that every other fixture and the FID-02 pins are byte-identical; test/parity/fixture-inventory.test.js stays green (roster pin unchanged, markers untouched)"
    - "D-15: docs/PARLEY-REBALANCE.md's AFTER section holds the verbatim 200-seed readout measured on the landed engine, and the Comparison section states attempts / success rate / SP share / death-depth before vs after in informational (not-a-gate) terms"
    - "Phase gate: npm test fully green (every test), node --test test/parity/**/*.test.js 30/30, FID-02 draw pins untouched, prototype-master.js.txt and fixture JSON untouched, no Math.random/Date in engine or the new tests"
    - statement: "The FIXTURE-INVENTORY table and the PARLEY-REBALANCE ledger read as a self-contained rationale a future maintainer (or Phase 21) can act on without re-deriving anything"
      verification: backstop
  artifacts:
    - path: "test/unit/parley.test.js"
      provides: "the Phase 20 behavioural suite: matrix, gates, refusal, exhausted, insulted (member + persistence), odds, clamp, fluency bonus, payout + property, wilmst odds, draw shape, seed-303 pin, save/load edge"
      contains: "D-21"
    - path: "test/unit/parley-button-mirror.test.js"
      provides: "classic mazeworld.html canParley()/fluency() extracted and replayed against the engine over the full matrix"
      contains: "new Function("
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "## Phase 20 parley divergence section with the re-measured before/after table"
      contains: "## Phase 20 parley divergence"
    - path: "docs/PARLEY-REBALANCE.md"
      provides: "AFTER readout verbatim + Comparison"
      contains: "## tune-difficulty AFTER"
  key_links:
    - from: "test/unit/parley.test.js"
      to: "engine/combat.js canParley / parley / foeTurn / endCombat and engine/derived.js killSpFor / fluency"
      via: "direct imports; countingRng wrapper over fakeRng / makeRng for draw pins; applyAction for the dispatcher-path probe"
      pattern: "import \\{ .*parley.* \\} from \"../../engine/combat.js\""
    - from: "test/unit/parley-button-mirror.test.js"
      to: "mazeworld.html classic fluency() + canParley()"
      via: "fs.readFileSync + regex extraction + new Function(\"S\", \"skill\", \"eff\", \"TALKATIVE\", src)"
      pattern: "function canParley\\(\\) \\{"
    - from: "test/parity/FIXTURE-INVENTORY.md Phase 20 section"
      to: "test/parity/harness/comparables.js stripParleyDivergence"
      via: "names the stripper and both call sites as the carve-out"
      pattern: "stripParleyDivergence"
  prohibitions:
    - requirement_id: PARLEY-01
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, any test/parity/fixtures/*.json, the generated block of FIXTURE-INVENTORY.md (between its markers), test/unit/foe-turn-draw-count.test.js, or any engine/ / src/ / content/ file in this plan"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/unit/foe-turn-draw-count.test.js exits 0; node --test test/parity/fixture-inventory.test.js green; git status shows no engine/src/content change from this plan"
    - requirement_id: PARLEY-02
      category: safety
      statement: "MUST NOT use Math.random, Date, or any non-scripted rng in the new tests (fakeRng / makeRng(seed) only); MUST NOT assert on probability floats (assert exact need values, never a success-rate percentage)"
      status: flagged-unverified
      verification: "grep -c 'Math.random\\|new Date\\|Date.now' on both new test files prints 0"
    - requirement_id: LANG-02
      category: values
      statement: "MUST NOT derive the matrix oracle by calling the engine's canParley (the oracle is an independent restatement of D-11/D-12); MUST NOT skip any race/type combination"
      status: flagged-unverified
      verification: "the oracle function body contains no call to canParley; the matrix loop iterates Object.keys(RACES) and ENC_TYPES (both imported from content), asserting 576 cases"
    - requirement_id: PARLEY-03
      category: values
      statement: "MUST NOT change any dial or number in reaction to the AFTER readout — Phase 21 owns the retune; the readout is transcribed and compared, nothing else"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- engine/difficulty.js content exits 0 at the end of the plan"
---

<objective>
Close Phase 20 with proof: the full behavioural suite for every locked decision (`test/unit/parley.test.js`), the UI-mirror guard that makes D-17 machine-checked (`test/unit/parley-button-mirror.test.js`), the re-measured seed-303 before/after table in `test/parity/FIXTURE-INVENTORY.md` (D-13/D-21), the AFTER half of the D-15 tune-difficulty readout with its comparison in `docs/PARLEY-REBALANCE.md`, and the phase gate (`npm test` fully green, parity 30/30, frozen files untouched).

Purpose: ROADMAP criteria 1-4 each demand a TEST ("verified by a test comparing parley payout to computed combat-equivalent", "every canParley gate has test coverage", "a stated, documented win-rate target", "an availability test per race/class/skill/Helm combination") — this plan is where those tests and documents land against the code 20-02 shipped.

Output: two new test files, one FIXTURE-INVENTORY.md section, the completed PARLEY-REBALANCE.md ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-parley-balance-language-system/20-CONTEXT.md
@.planning/phases/20-parley-balance-language-system/20-RESEARCH.md
@.planning/phases/20-parley-balance-language-system/20-PATTERNS.md
@.planning/phases/20-parley-balance-language-system/20-01-SUMMARY.md
@.planning/phases/20-parley-balance-language-system/20-02-SUMMARY.md
@engine/combat.js
@engine/derived.js
@test/unit/combat.test.js
@test/unit/party-combat.test.js
@test/unit/foe-turn-draw-count.test.js
@test/unit/foe-effect-chip.test.js
@test/unit/save-validation.test.js
@test/parity/FIXTURE-INVENTORY.md
@docs/PARLEY-REBALANCE.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `stripParleyDivergence` + both scenario-scoped call sites; `test/unit/parley-carveout.test.js`; tune-difficulty parley tally; `docs/PARLEY-REBALANCE.md` (BEFORE) | harness / tool / docs | 20-01 | landed |
| `fluency`, `killSpFor` (derived.js); `killFoe` via `killSpFor`; `canParley` / `parley` rewrite; `foeTurn` `need += 1` / `mNeed += 1`; flags `C.parleyTried` / `C.parleyInsulted`; events `parleyExhausted` / `parleyInsulted`; `parleyRolled.fluency`; narration; classic `fluency()` / `canParley()` mirror; `test/unit/fluency.test.js`; combat.test.js additions | engine / presentation / UI / tests | 20-02 | landed |
| `test/unit/parley.test.js` — matrix oracle, every gate, refusal, exhausted (dispatcher path), insulted (member branch + persistence), odds 13/12, clamp 17, fluency +2/+4, payout 33 + property, wilmst on 6 only, countingRng draw shape, seed-303 pin, save/load edge | tests | **20-03** | D-02, D-04, D-05..D-08, D-10..D-12, D-19, D-21 |
| `test/unit/parley-button-mirror.test.js` — classic canParley/fluency extracted from mazeworld.html and replayed over the matrix | tests | **20-03** | D-17 |
| `test/parity/FIXTURE-INVENTORY.md` "## Phase 20 parley divergence" section | docs (parity ledger) | **20-03** | D-13 / D-21 — outside the generated block |
| `docs/PARLEY-REBALANCE.md` AFTER readout + Comparison | docs | **20-03** | D-15 |
| Phase gate: `npm test` green, parity 30/30, frozen files untouched | gate | **20-03** | — |

## Pinned numbers the tests assert (from the fixed fighter: Human Soldier L1, foeToHitVs = 5, lit floor, no items)

| Case | Value |
|---|---|
| Con Artist vs solo lvl-1 foe, level 1 | bonus 4 → need 13 |
| Con Artist vs solo lvl-2 foe, level 1 | bonus 3 → need 12 |
| Con Artist Wilmsry, level 1 vs lvl 1 | bonus 8 → need 17 (clamp inactive) |
| Con Artist Wilmsry + Language + Helm, level 1 vs lvl 1 | bonus 12 → need 17 (clamp active) |
| Bard vs Humans lvl 1, level 1 | bonus 0 → need 9; +2 skill → 11; +2 Helm → 11; both → 13 |
| killSpFor(Human, lvl 1, d6 3) / (Human, lvl 2, d6 5) | 15 / 50 → two-foe payout round(65 × 0.5) = 33 |
| Humans wilmst, depth 3, check 6, amount 4 | +1200 gold |
| Insulted foe vs fixed fighter | need 5 → 6 (hero and member branches) |
| Seed 303 (Wilmsry Con Artist L1, fluency 0, 1 live Dante after the D-16 talkdown) | draws d20=2 d6=5 d6=5; need 17; sp 7; gold 50; 3 draws |

<tasks>

<task type="auto">
  <name>Task 1: Create test/unit/parley.test.js — the availability matrix, every gate, refusal, exhausted, insulted, odds, clamp, fluency bonus, payout property, wilmst odds, draw shape, seed-303 pin and save/load edge</name>
  <files>test/unit/parley.test.js</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-02, D-04, D-05, D-06, D-07, D-08, D-10, D-11, D-12, D-19, D-20, D-21
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — "Seed-303 Fixture: Exact Before/After" (draw table), "Code Examples (tests)", "Validation Architecture" → "Wave 0 Gaps"
    - .planning/phases/20-parley-balance-language-system/20-02-SUMMARY.md — the landed canParley/parley order and narration strings
    - engine/combat.js — canParley, parley, foeTurn (both to-hit sites), endCombat, startCombat, liveFoes as landed by 20-02
    - engine/derived.js — fluency, killSpFor, foeToHitVs
    - test/unit/combat.test.js lines 46-110 (fakeRng, fixedFighter, fixedFloor, fixedState, fixedFoe, fixedCombat — copy these local helpers verbatim; they are not exported) and 782-880 (the existing parley tests — do NOT duplicate them, complement them)
    - test/unit/party-combat.test.js lines 90-97 (fixedAlly / fixedMember) and 152-163 (the member-strike test: pick d(2)=2 → member, to-hit, dmg d6)
    - test/unit/foe-turn-draw-count.test.js lines 41-75 (countingRng wrapper — copy it)
    - test/unit/save-validation.test.js lines 23-33 (serializeRun → JSON → validateSave → rehydrate round-trip pattern)
    - content/bestiary.js line 146 (ENC_TYPES — the six encounter types) and content/races.js (RACES keys)
  </read_first>
  <action>
    Create `test/unit/parley.test.js` with a header comment (Phase 20 — PARLEY-01..04, LANG-01/02; complements test/unit/combat.test.js's four parley tests and test/unit/fluency.test.js; every rng is scripted (fakeRng) or seeded (makeRng); no probability floats are asserted — only exact `need` values, per D-07's documented target). Imports: `test`, `assert/strict`; `canParley`, `parley`, `foeTurn`, `endCombat`, `startCombat`, `liveFoes` from `../../engine/combat.js`; `fluency`, `killSpFor`, `foeToHitVs` from `../../engine/derived.js`; `newRun`, `applyAction` from `../../engine/engine.js`; `makeRng` from `../../engine/rng.js`; `serializeRun`, `validateSave`, `rehydrate` from `../../engine/saveState.js`; `RACES`, `ENC_TYPES` from `../../content/index.js`. Local helpers copied from combat.test.js / party-combat.test.js / foe-turn-draw-count.test.js: `fakeRng(seq)`, `countingRng(inner)` (also record each `d()` result into an exposed `log` array as `d<n>=<value>` strings), `fixedFighter`, `fixedFloor`, `fixedState`, `fixedFoe`, `fixedCombat`, `fixedAlly`, `fixedMember`. Add `const HELM = { n: "Helm of Knowledge", eff: { tongue: 1 } };`, `const TALKATIVE = ["Humans", "Demons", "Lair Beasts", "Beasts"];`, and `mk(cOverrides, type, combatOverrides)` building a fixedState whose combat is `fixedCombat([fixedFoe({ type })], { type, ...combatOverrides })`.

    Write an independent oracle `expectedCanParley(c, t, tried)` as a plain restatement of the rules (NOT a call to the engine): tried → false; Walking Dead → false; compute flu = (has Language skill ? 1 : 0) + (any item with eff.tongue > 0 ? 1 : 0); Magical → flu === 2; Con Artist → true; Woodsman and (Beasts or Lair Beasts) → true; Bard and Humans → true; flu >= 1 and TALKATIVE includes t → true; Wilmsry → true (Magical already handled above); Elven and Humans → true; else false.

    Tests (names exactly as given, one `test(...)` each):
    1. "LANG-02 / D-11: availability matrix — 6 races × 4 subs × skill × Helm × 6 types (576 cases) match the rule oracle" — nested loops over `Object.keys(RACES)`, `["Con Artist", "Woodsman", "Bard", "Soldier"]`, `[false, true]` (Language), `[false, true]` (Helm), `ENC_TYPES`; for each build `mk({ race, sub, skills: lang ? { Language: 1 } : {}, items: helm ? [HELM] : [] }, t)`; assert `canParley(state) === expectedCanParley(state.c, t, false)` with a message naming the case; count cases (assert 576) and aggregates: Magical true count === 24, Walking Dead true count === 0, Human/Soldier/no-skill/no-Helm true count === 0.
    2. "PARLEY-04 / D-12: every canParley gate, explicitly" — the individually-messaged assertions listed in must_haves (no combat → false; `mk({ sub: "Con Artist" }, "Humans", { parleyTried: true })` → false; Walking Dead false for Con Artist, Wilmsry, and a Language+Helm character; Magical false at fluency 0 and 1 (skill only; Helm only) for Con Artist, true at fluency 2; Woodsman: Beasts true, Lair Beasts true, Humans false, Demons false; Bard: Humans true, Beasts false; skill-only fluency 1: Humans/Demons/Lair Beasts/Beasts true, Magical false; Helm-only fluency 1: the same five; fluency 2 (skill + Helm) Magical true for a Human Soldier; Wilmsry: Beasts/Humans/Demons/Lair Beasts true, Magical false at fluency 0, Magical true at fluency 2; Elven: Humans true, Beasts false; Human Soldier fluency 0: all four TALKATIVE types false).
    3. "PARLEY-04 / D-12: a fluency-2 Wilmsry vs Magical is refused with the canon reason, draws nothing, and does NOT spend the attempt" — `mk({ race: "Wilmsry", skills: { Language: 1 }, items: [HELM] }, "Magical")`; `canParley` true; `parley(state, fakeRng([]), [])` deepStrictEqual `[{ type: "parleyRefused", reason: "wilmsryVsMagical" }]`; `state.combat.parleyTried === undefined`; `state.combat` still non-null; a second identical call returns the same single refusal (never parleyExhausted); `state.c.sp` and `state.c.gold` unchanged.
    4. "PARLEY-02 / D-05: through applyAction an exhausted encounter yields only parleyExhausted with an unchanged rngState; a successful parley leaves nothing to retry" — build `newRun(303)`, clone, run `startCombat(next, false, "Humans", makeRng(next.rngState), [])` and persist `rngState`; set `next.combat.parleyTried = true`; `const { state, events } = applyAction(next, { type: "parley" })` → events deepStrictEqual `[{ type: "parleyExhausted" }]`, `state.rngState === next.rngState`, `state.combat` deepStrictEqual `next.combat`. Separately: Con Artist vs Beasts, `parley(state, fakeRng([1, 3]), [])` succeeds → `state.combat === null`; `parley(state, fakeRng([]), [])` returns `[]` (no encounter to retry).
    5. "PARLEY-02 / D-06 / D-20: parleyInsulted widens the MEMBER branch mNeed by exactly 1, roll untouched" — `fixedState({ party: [fixedMember({ wp: 12 })] })`, `state.combat = fixedCombat([fixedFoe({ lvl: 1, wp: 30 })], { allies: [ally] })` with `ally = fixedAlly({ wp: 12, maxWP: 20 })`; baseline `foeTurn(state, fakeRng([2, 6]), [])` → a `foeMissed` event with `member === "Ada"`, `need === 5`, `roll === 6`, `ally.wp === 12`; fresh identical state with `state.combat.parleyInsulted = true`: `foeTurn(state, fakeRng([2, 6, 3]), [])` → `memberStruck` with `member === "Ada"`, `need === 6`, `roll === 6`, `dmg === 4`, `ally.wp === 8`; hero untouched (`state.c.wp === 55`) in both.
    6. "PARLEY-02 / D-06: a failed parley sets the insult, which persists every round and dies with the combat" — Con Artist vs an ASLEEP Beasts foe (asleep 3), `parley(state, fakeRng([20, 15, 10]), [])` (20 > 13 fails; the sleeping foe draws nothing; initiative 15 vs 10); events order: indexOf parleyFailed < indexOf parleyInsulted; `state.combat.parleyInsulted === true` and `parleyTried === true`; wake the foe (`asleep = 0`) and call `foeTurn(state, fakeRng([6, 3]), [])` twice on the same state (resetting `state.c.wp = 55` between) → each produces a `struckByFoe` with `need === 6`; then `endCombat(state, [])` → `state.combat === null`.
    7. "PARLEY-03 / D-07: Con Artist need is 13 at even level vs a solo foe and 12 one level down" — Con Artist Human vs Beasts lvl 1: `parley(state, fakeRng([1, 3]), [])` → parleyRolled `need === 13`, `fluency === 0`; vs Beasts lvl 2: need 12.
    8. "PARLEY-03 / D-08: need clamps at 17 — inactive at bonus 8, active at bonus 12, and 18 still fails" — Con Artist Wilmsry vs Beasts lvl 1: `fakeRng([17, 3])` → need 17 and success; Con Artist Wilmsry + Language + Helm vs Beasts lvl 1: `fakeRng([17, 3])` → need 17 (clamped from 21) and success; same character `fakeRng([18, 15, 10])` (foe asleep 3 so the failure's foe turn draws nothing) → parleyRolled need 17 and `parleyFailed` present (no auto-win).
    9. "LANG-01 / D-10: fluency adds +2 per tier to need and is reported on parleyRolled" — Bard (sub "Bard") vs Humans lvl 1, each with `fakeRng([1, 3, 1])` (roll 1 succeeds; sp d6; wilmst check 1): no skill/Helm → need 9, fluency 0; Language → 11, fluency 1; Helm → 11, fluency 1; both → 13, fluency 2.
    10. "PARLEY-01 / D-01 / D-02: payout is round(Σ killSpFor × 0.5) over live foes in foe order; dead foes draw nothing" — Con Artist Human vs Humans foes [lvl 1, lvl 2] (both alive), `fakeRng([1, 3, 5, 2])` → spGained amount 33 (`Math.round((killSpFor(c, f1, 3) + killSpFor(c, f2, 5)) * 0.5)` — assert both the literal 33 and the formula), no goldGained (check 2), `state.c.sp === 33`, combat null; then foes [lvl 1 alive, lvl 5 dead] with `fakeRng([1, 3, 2])` → amount 8 and no exhaustion error.
    11. "PARLEY-01 / D-02 property: the parley share never exceeds the combat-equivalent for any race × sub × foe level × d6, singly or summed over two foes" — loops over `Object.keys(RACES)` × `["Soldier", "Barbarian", "Apprentice", "Con Artist"]` × lvl 1..5 × roll 1..6: `eq = killSpFor(c, { lvl }, roll)`, assert `Math.round(eq * 0.5) <= eq` and `>= 0`; and for lvl pairs (a, b) with rolls (ra, rb): `Math.round((eqA + eqB) * 0.5) <= eqA + eqB`.
    12. "PARLEY-01 / D-03 / D-04: the Humans wilmst check pays only on a 6 and draws its amount die only then" — Con Artist Human vs Humans lvl 1, floor depth 3: `fakeRng([1, 3, 4])` → no goldGained, gold 50, no throw (a fourth draw would throw "sequence exhausted"); `fakeRng([1, 3, 5])` → same; `fakeRng([1, 3, 6, 4])` → goldGained `amount === 1200`, `why === "parley"`, gold 1250.
    13. "D-04 draw shape pinned with countingRng: exhausted 0, refused 0, success 1 + N (+1 Humans check, +1 payout)" — exhausted: `mk({ sub: "Con Artist" }, "Humans", { parleyTried: true })` → 0 draws; refused (fluency-2 Wilmsry vs Magical) → 0 draws; Con Artist vs two Beasts (lvl 1, 1) `countingRng(fakeRng([1, 2, 2]))` → 3 draws; Con Artist vs two Humans, check 5 → `countingRng(fakeRng([1, 2, 2, 5]))` → 4 draws; check 6 → `countingRng(fakeRng([1, 2, 2, 6, 1]))` → 5 draws.
    14. "D-21 seed-303 pin: the one parity-exposed parley now reads need 17 / sp 7 / gold 50 with the same first three dice and no fourth" — `newRun(303)`: assert `c.race === "Wilmsry"`, `c.sub === "Con Artist"`, `c.level === 1`, `fluency(c) === 0`, `c.sp === 0`, `c.gold === 50`; clone, `startCombat(next, false, "Humans", rng, [])` with `rng = makeRng(next.rngState)`, persist; assert `liveFoes(next).length === 1` and both foes named Dante; then `const counting = countingRng(makeRng(next.rngState)); const events = parley(next, counting, []);` → `counting.log` deepStrictEqual `["d20=2", "d6=5", "d6=5"]`, `counting.draws === 3`; events contain parleyRolled `{ roll: 2, need: 17, fluency: 0 }`, spGained `{ amount: 7, reason: "parley" }`, `combatEnded`, and NO goldGained; `next.c.sp === 7`, `next.c.gold === 50`, `next.combat === null`. Add a comment: BEFORE (pre-Phase-20, measured 2026-09-14) was d20=2 d6=5 d6=5 d6=2 → need 19, sp 13, gold 250 — see test/parity/FIXTURE-INVENTORY.md "Phase 20 parley divergence".
    15. "D-19 old-save probe: mid-fight flags round-trip losslessly through JSON and are dropped with the combat on load" — `state = mk({ sub: "Con Artist" }, "Humans", { parleyTried: true, parleyInsulted: true })`; `JSON.parse(JSON.stringify(state)).combat` has `parleyTried === true` and `parleyInsulted === true`; then `const check = validateSave(JSON.stringify(serializeRun(newRunWithFlags)))` where `newRunWithFlags` is `newRun(303)` after `startCombat` with both flags set on its combat → `check.ok === true`, `rehydrate(check.value).combat === null`, and `rehydrate(check.value).c` deepStrictEqual the pre-save `c` (minus nothing — the character is intact).
  </action>
  <verify>
    <automated>node --test test/unit/parley.test.js test/unit/combat.test.js test/unit/fluency.test.js test/unit/foe-turn-draw-count.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/parley.test.js` exits 0 and `grep -c '^test("' test/unit/parley.test.js` prints 15
    - `grep -c 'function expectedCanParley' test/unit/parley.test.js` prints 1 and `awk '/function expectedCanParley/,/^}/' test/unit/parley.test.js | grep -c 'canParley('` prints 0 (the oracle is independent) and `grep -c 'assert.equal(cases, 576' test/unit/parley.test.js` prints 1 (or an equivalent `=== 576` assertion: `grep -c '576' test/unit/parley.test.js` is at least 1)
    - `grep -c '"wilmsryVsMagical"' test/unit/parley.test.js` prints at least 2 and `grep -c 'parleyTried, undefined\|parleyTried === undefined' test/unit/parley.test.js` prints at least 1
    - `grep -c 'parleyExhausted' test/unit/parley.test.js` prints at least 2 and `grep -c 'rngState' test/unit/parley.test.js` prints at least 2
    - `grep -c 'memberStruck' test/unit/parley.test.js` prints at least 1 and `grep -c 'need === 6\|need, 6' test/unit/parley.test.js` prints at least 2
    - `grep -c 'need === 13\|need, 13' test/unit/parley.test.js` prints at least 2 and `grep -c 'need === 12\|need, 12' test/unit/parley.test.js` prints at least 1 and `grep -c 'need === 17\|need, 17' test/unit/parley.test.js` prints at least 3 and `grep -c 'need === 11\|need, 11' test/unit/parley.test.js` prints at least 2
    - `grep -c 'amount === 33\|amount, 33' test/unit/parley.test.js` prints at least 1 and `grep -c 'amount === 1200\|amount, 1200' test/unit/parley.test.js` prints at least 1 and `grep -c 'Math.round(eq \* 0.5) <= eq\|Math.round(eq \* 0.5), eq' test/unit/parley.test.js` prints at least 1
    - `grep -c 'function countingRng' test/unit/parley.test.js` prints 1 and `grep -c '"d20=2", "d6=5", "d6=5"' test/unit/parley.test.js` prints 1 and `grep -c 'D-21' test/unit/parley.test.js` prints at least 1
    - `grep -c 'rehydrate(' test/unit/parley.test.js` prints at least 2 and `grep -c 'validateSave(' test/unit/parley.test.js` prints at least 1
    - `grep -c 'Math.random\|new Date\|Date.now' test/unit/parley.test.js` prints 0
    - `node --test test/unit/parley.test.js` exits 0 with 15 passing tests; `node --test test/unit/foe-turn-draw-count.test.js` exits 0 with zero edits (`git diff --quiet 04eb229 -- test/unit/foe-turn-draw-count.test.js` exits 0)
  </acceptance_criteria>
  <done>Fifteen deterministic tests pin every locked decision from the user's side — availability, gates, refusal, exhaustion, insult, odds, clamp, fluency bonus, payout, wilmst odds, draw shape, the seed-303 numbers and the old-save edge — all green.</done>
</task>

<task type="auto">
  <name>Task 2: Create test/unit/parley-button-mirror.test.js — extract mazeworld.html's classic fluency()/canParley() and replay the matrix against the engine (D-17)</name>
  <files>test/unit/parley-button-mirror.test.js</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-17
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — Pitfall 1 and Assumption A2 (no automated test exercised the classic block before this file)
    - test/unit/foe-effect-chip.test.js (whole file — the fs.readFileSync + regex extraction precedent for mazeworld.html; note it has no module surface)
    - mazeworld.html lines 1707 (classic `skill`), 3281-3285 (classic `eff(key)`), and the classic `function fluency()` / `function canParley()` pair as landed by 20-02 (directly after `const TALKATIVE = [...]`) — note the file is CRLF
    - engine/combat.js canParley (the authoritative order) and test/unit/parley.test.js Task 1 (reuse the same matrix shape and `mk` helper; copy, do not import — test files are not modules for each other)
  </read_first>
  <action>
    Create `test/unit/parley-button-mirror.test.js` (header: Phase 20 D-17 / LANG-02 / PARLEY-02 — mazeworld.html keeps a hand-maintained classic canParley() that gates the "5 · Parley" button; nothing else can catch it drifting from engine/combat.js#canParley, so this file extracts the real shipped source and replays the engine's matrix through it; mirrors test/unit/foe-effect-chip.test.js's source-read pattern). Read `mazeworld.html` via `fs.readFileSync` from the repo root and normalise `\r\n` to `\n`. Extract with two regexes anchored at line starts: `/\nfunction fluency\(\) \{\n[\s\S]*?\n\}\n/` and `/\nfunction canParley\(\) \{\n[\s\S]*?\n\}\n/`; assert both match exactly once (`match.length` / a global count of 1 each). Build the pair with `new Function("S", "skill", "eff", "TALKATIVE", fluSrc + cpSrc + "\nreturn canParley;")` and a `classicFor(state)` helper that supplies: `S = state`, `skill = (n) => !!(state.c.skills && state.c.skills[n])`, `eff = (key) => (state.c.items || []).reduce((t, it) => t + ((it.eff && it.eff[key]) || 0), 0)`, `TALKATIVE = ["Humans", "Demons", "Lair Beasts", "Beasts"]`, returning the bound classic `canParley`. Local `fixedFighter`/`fixedFloor`/`fixedState`/`fixedFoe`/`fixedCombat`/`mk` copied from parley.test.js; `HELM` as there. Import `canParley` from `../../engine/combat.js`, `RACES`, `ENC_TYPES` from `../../content/index.js`.

    Tests:
    1. "D-17: the classic canParley() agrees with engine/combat.js#canParley on all 576 matrix cases" — same loops as parley.test.js test 1; assert `classicFor(state)() === canParley(state)` per case with a message naming race/sub/lang/helm/type; count 576.
    2. "D-17: the classic gate hides the button after the one attempt and with no combat, exactly like the engine" — `mk({ sub: "Con Artist" }, "Humans", { parleyTried: true })` → both false; `mk({ sub: "Con Artist" }, "Humans")` → both true; a state with `combat: null` → both false; a fluency-2 Wilmsry vs Magical → both true (the refusal is parley()'s job, not the gate's).
    3. "D-17 source pins: the classic canParley reads parleyTried and opens Magical only at fluency 2" — the extracted `cpSrc` contains `if (C.parleyTried) return false;`, `if (t === "Magical" && flu < 2) return false;`, `const talkable = flu >= 2 ? [...TALKATIVE, "Magical"] : TALKATIVE;` and does NOT contain the substring `"Walking Dead" || t === "Magical"`; `fluSrc` contains `skill("Language")` and `eff("tongue")`.
  </action>
  <verify>
    <automated>node --test test/unit/parley-button-mirror.test.js test/unit/foe-effect-chip.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/parley-button-mirror.test.js` exits 0 and `grep -c '^test("' test/unit/parley-button-mirror.test.js` prints 3
    - `grep -c 'new Function("S", "skill", "eff", "TALKATIVE"' test/unit/parley-button-mirror.test.js` prints 1 and `grep -c 'replace(/\\r\\n/g, "\\n")' test/unit/parley-button-mirror.test.js` prints 1
    - `grep -cF 'function canParley\(\) \{' test/unit/parley-button-mirror.test.js` prints at least 1 and `grep -cF 'function fluency\(\) \{' test/unit/parley-button-mirror.test.js` prints at least 1 (the two extraction regexes, matched as fixed strings)
    - `grep -c '576' test/unit/parley-button-mirror.test.js` prints at least 1 and `grep -c 'Math.random\|new Date\|Date.now' test/unit/parley-button-mirror.test.js` prints 0
    - `node --test test/unit/parley-button-mirror.test.js` exits 0 with 3 passing tests
    - Drift tripwire (manual sanity, do not commit): temporarily changing `flu < 2` to `flu < 1` in mazeworld.html's classic canParley makes test 1 fail on the Magical rows, and reverting restores green — record the observation in the SUMMARY
  </acceptance_criteria>
  <done>The classic button gate is machine-checked against the engine over the full matrix, so D-17's "change one, change both" rule is enforced by npm test from now on.</done>
</task>

<task type="auto">
  <name>Task 3: Write the FIXTURE-INVENTORY.md Phase 20 section (re-measured seed-303 before/after), capture the AFTER readout into docs/PARLEY-REBALANCE.md with the comparison, and run the phase gate</name>
  <files>test/parity/FIXTURE-INVENTORY.md, docs/PARLEY-REBALANCE.md</files>
  <read_first>
    - .planning/phases/20-parley-balance-language-system/20-CONTEXT.md — D-13, D-15, D-21 and "Deferred Ideas"
    - .planning/phases/20-parley-balance-language-system/20-RESEARCH.md — "Seed-303 Fixture: Exact Before/After" (the table shape to transcribe, with the AFTER column now MEASURED by Task 1's test 14), Pitfall 5 (4 draws → 3 is correct), "Draw-count baseline (FID-02) is unaffected"
    - test/parity/FIXTURE-INVENTORY.md lines 1-30 (header + generated-block markers — never edit inside), 96-157 (the Phase 18 constraints / FID-02 / Phase 19 contract sections — append after "Phase 19 contract")
    - test/parity/fixture-inventory.test.js lines 174-190 (the generated-block consistency test — proves the markers/rows are untouched)
    - docs/PARLEY-REBALANCE.md (20-01's ledger: the two placeholder lines to replace)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md lines 119-125 and 161 (the AFTER transcription + comparison sentence precedent; background-run handling)
    - test/unit/parley.test.js test 14 (Task 1 — the re-measured numbers this section cites)
  </read_first>
  <action>
    First confirm the engine is post-20-02: `grep -c 'killSpFor' engine/combat.js` must print at least 2 and `git diff --quiet 04eb229 -- engine/combat.js` must exit non-zero. Then launch the AFTER readout in the background exactly as 20-01 Task 3 did (`node tools/tune-difficulty.mjs --seeds=200` → scratchpad file with an `EXIT=<code>` sentinel; ~5-6 minutes; never wait in the foreground; poll in bounded ≤120 s checks) and do the document work while it runs.

    Append to `test/parity/FIXTURE-INVENTORY.md`, after the "### Phase 19 contract" section (outside the generated block), a section `## Phase 20 parley divergence (PARLEY-01..04, LANG-01/02 — D-13 / D-21)` containing: (1) one paragraph: `action-script.combat.json` scenario `parley` (seed 303) is the ONLY fixture that exercises `parley()`; Phase 20 deliberately changes its payout and odds, so its `c.sp` / `c.gold` (and the new `combat.parleyTried` / `combat.parleyInsulted` flags) are carved out by `stripParleyDivergence` (`test/parity/harness/comparables.js`) at BOTH replay sites — `test/parity/combat-parity.test.js` (local `comparable()`) and `test/parity/full-suite.test.js` (shared `combatComparable`) — scoped by `scenario.name === "parley"`; everything else in that scenario (the canParley gate, the encounter, `combatEnded`, the draw positions) is still compared; (2) a "Before / after (measured)" table with rows `character`, `live foes at parley`, `draw 1 (d20)`, `draw 2 (d6, per-foe SP)`, `draw 3 (d6, wilmst check)`, `draw 4 (d6, wilmst amount)`, `bonus`, `need`, `outcome`, `SP formula`, `sp`, `gold`, `draws total`, `events` with the BEFORE column (Wilmsry Con Artist L1 fluency 0; 1 live Dante; 2; 5; 5; 2; 6+4+1−1 = 10; 19; success; round(Σ(d6×lvl) × 2.5) = 13; 13; 250; 4; parleyRolled{2,19}, spGained{13}, goldGained{200}, combatEnded) and the AFTER column re-measured by `test/unit/parley.test.js` test 14 (same character; same 1 live foe; 2; 5; 5; not drawn; 4+4+1−1+0 = 8; 17 (clamp inactive — lands exactly at the ceiling); success; round(killSpFor(c, Dante, 5) × 0.5) = round(13 × 0.5) = 7; 7; 50; 3; parleyRolled{2,17,fluency 0}, spGained{7}, combatEnded) — the executor MUST take the AFTER numbers from the passing test, not from this plan, and stop if they differ; (3) a "Why the draw count changed" note (Pitfall 5: the wilmst check of 5 no longer fires under `=== 6`, so the conditional amount die is never drawn; the first three draws keep their positions — D-04 holds); (4) a "Flags never visible here" note (success nulls `state.combat` before comparison, so the flag strip is defensive for any future mid-fight or failed-parley fixture); (5) a "Byte-identical elsewhere" statement: win / lose / flee, every magic / movement / chargen / economy / encounters fixture, `prototype-master.js.txt`, and the FID-02 pins (the 66-draw `combat/parley` row measures `playerStrike`-only fights and never calls `parley`) are unchanged with no carve-out; the roster table above (generated block) is unchanged because no creature moved.

    In `docs/PARLEY-REBALANCE.md`: replace the `## Seed-303 fixture` section's "computed, will be re-measured" sentence with the measured AFTER values and a pointer to the FIXTURE-INVENTORY section; replace the `_Pending — filled by 20-03 after the engine rules land._` line under `## tune-difficulty AFTER (Phase 20 engine)` with the measurement conditions (command, `--seeds=200`, the git commit of the landed engine, date) and the verbatim transcript in a fenced text block; replace the `_Pending — 20-03._` line under `## Comparison` with 4-6 sentences: parley attempts before vs after (the one-attempt rule and the bot's flee-on-exhausted fallback should drive attempts per run down), success rate before vs after (Con Artist +6 → +4 and the clamp), SP-from-parley share before vs after (×0.5 of the same number a kill pays, plus the rarer wilmst), death-depth/action distributions within noise or not, and the explicit framing: informational only, the bot parleys only below 30% HP so it under-samples deliberate human parley use, no dial moves in this phase, Phase 21 owns the retune (cite the "Deferred to Phase 21" section).

    Phase gate: run `npm test` (full suite) and `node --test "test/parity/**/*.test.js"`; both must be fully green. Record the final test count.
  </action>
  <verify>
    <automated>node --test test/parity/fixture-inventory.test.js && grep -c '^## Phase 20 parley divergence' test/parity/FIXTURE-INVENTORY.md | grep -qx 1 && grep -c '^## tune-difficulty AFTER' docs/PARLEY-REBALANCE.md | grep -qx 1 && grep -c '_Pending' docs/PARLEY-REBALANCE.md | grep -qx 0 && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Phase 20 parley divergence' test/parity/FIXTURE-INVENTORY.md` prints 1 and the section starts AFTER the line `### Phase 19 contract` (`awk '/### Phase 19 contract/{a=NR} /## Phase 20 parley divergence/{b=NR} END{exit !(b>a)}' test/parity/FIXTURE-INVENTORY.md` exits 0)
    - `grep -c 'stripParleyDivergence' test/parity/FIXTURE-INVENTORY.md` prints at least 1 and `grep -c 'combat-parity.test.js' test/parity/FIXTURE-INVENTORY.md` prints at least 1 and `grep -c 'full-suite.test.js' test/parity/FIXTURE-INVENTORY.md` prints at least 1
    - `grep -c '19' test/parity/FIXTURE-INVENTORY.md` is at least 1 more than before and specifically `grep -c 'need.*19.*17\|19 | 17\|19 → 17' test/parity/FIXTURE-INVENTORY.md` prints at least 1; `grep -c '13 | 7\|13 → 7' test/parity/FIXTURE-INVENTORY.md` prints at least 1; `grep -c '250 | 50\|250 → 50' test/parity/FIXTURE-INVENTORY.md` prints at least 1; `grep -c 'not drawn' test/parity/FIXTURE-INVENTORY.md` prints at least 1
    - `grep -c 'Pitfall 5\|first three draws\|draw positions' test/parity/FIXTURE-INVENTORY.md` prints at least 1 and `grep -c 'FID-02' test/parity/FIXTURE-INVENTORY.md` prints at least 2 (the existing section + the Phase 20 statement)
    - `node --test test/parity/fixture-inventory.test.js` exits 0 (generated block and roster pin untouched) and `git diff 04eb229 -- test/parity/FIXTURE-INVENTORY.md | grep '^-' | grep -v '^---' | wc -l` prints 0 (append-only: no existing line removed)
    - `grep -c '^## tune-difficulty AFTER' docs/PARLEY-REBALANCE.md` prints 1 and `grep -c 'Parley (D-15 readout' docs/PARLEY-REBALANCE.md` prints 2 (BEFORE + AFTER transcripts) and `grep -c '200 seeded auto-play' docs/PARLEY-REBALANCE.md` prints 2 and `grep -c '_Pending' docs/PARLEY-REBALANCE.md` prints 0
    - `awk '/^## Comparison/,/^## Deferred/' docs/PARLEY-REBALANCE.md | grep -c -i 'informational\|not a gate'` prints at least 1 and `awk '/^## Comparison/,/^## Deferred/' docs/PARLEY-REBALANCE.md | grep -c 'Phase 21'` prints at least 1
    - `npm test` exits 0 with every test passing (expected 893 + 15 + 3 = 911; record the exact number) and `node --test "test/parity/**/*.test.js"` exits 0 with 30 pass
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/unit/foe-turn-draw-count.test.js engine/difficulty.js content package.json package-lock.json` exits 0
    - `grep -rn 'Math.random\|new Date\|Date.now' engine/ test/unit/parley.test.js test/unit/parley-button-mirror.test.js test/unit/fluency.test.js test/unit/parley-carveout.test.js | grep -v '^\s*//' | grep -v 'engine-purity\|rng-no-math-random' | wc -l` prints 0
  </acceptance_criteria>
  <done>The seed-303 divergence is documented from measured numbers beside its carve-out, the D-15 readout has both halves and a comparison, and the whole suite plus parity are green with every frozen file untouched — Phase 20 is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine gate -> UI gate | Two `canParley` implementations; the mirror test is the only automated agreement check |
| test oracle -> engine | The matrix oracle must be independent of the implementation it checks |
| docs -> future tuning decisions | FIXTURE-INVENTORY and PARLEY-REBALANCE numbers must be measured, not computed |
| frozen prototype -> parity | Only the seed-303 scenario is carved out; everything else byte-identical |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-12 | Spoofing | Parley button shows/hides in disagreement with the engine (classic duplicate drift) | medium | mitigate | `test/unit/parley-button-mirror.test.js` replays 576 cases + parleyTried/no-combat through the extracted classic source; tripwire sanity recorded |
| T-20-13 | Repudiation | Documented before/after numbers that were never measured (computed only) | medium | mitigate | Task 3 takes the AFTER column from Task 1's passing seed-303 test; the AFTER readout is a verbatim transcript with commit + seed count |
| T-20-14 | Tampering | Matrix oracle copied from the implementation, making the availability test tautological | medium | mitigate | oracle is a prose restatement of D-11/D-12; awk gate proves it never calls canParley; aggregate counts (24 / 0 / 0) pin the shape independently |
| T-20-15 | Tampering | Editing inside FIXTURE-INVENTORY.md's generated block or the frozen parity files to make a table "fit" | high | mitigate | append-only gate (no removed lines), fixture-inventory.test.js consistency check, `git diff --quiet 04eb229` on frozen files |
| T-20-16 | Denial of Service | Executor stalls on the ~5-6 minute readout run | low | mitigate | background run with EXIT sentinel + bounded polling (18-06 precedent) |
| T-20-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet 04eb229 -- package.json package-lock.json` |
</threat_model>

<verification>
- `node --test test/unit/parley.test.js` 15 pass; `node --test test/unit/parley-button-mirror.test.js` 3 pass; `node --test test/parity/fixture-inventory.test.js` green; `npm test` fully green (expected 911, exact count recorded); parity 30/30.
- FIXTURE-INVENTORY.md has the appended Phase 20 section with measured 19→17 / 13→7 / 250→50 / 4→3 draws; PARLEY-REBALANCE.md has both readouts and a comparison; no `_Pending` left.
- Frozen parity files, FID-02 test, difficulty dials, content, package manifests unchanged since 04eb229.
</verification>

<success_criteria>
- ROADMAP Phase 20 criteria are each backed by a named test: (1) payout ≤ combat-equivalent (property + the 33-point example + killSpFor sharing); (2) one attempt + insulted aggro with every canParley gate covered including the reachable Wilmsry-vs-Magical refusal; (3) the Con Artist target stated (13/20 at even level) and asserted; (4) fluency in the same bonus term (+2/+4) and the 576-case availability matrix.
- The v1.1 engine gate holds: engine pure/deterministic, parity byte-identical except the documented seed-303 carve-out, no unguarded draws, new fields carved out and load-safe, prototype-master untouched, every new event narrated.
</success_criteria>

<output>
Create `.planning/phases/20-parley-balance-language-system/20-03-SUMMARY.md` when done (record: the 15 + 3 test names, the measured seed-303 AFTER row, the AFTER readout's headline parley numbers next to BEFORE, the final `npm test` count, and the mirror-test tripwire observation).
</output>
