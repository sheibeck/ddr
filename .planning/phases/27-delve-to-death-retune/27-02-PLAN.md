---
phase: 27-delve-to-death-retune
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - content/bestiary.js
  - content/BESTIARY-REBALANCE.md
  - test/unit/content-tables.test.js
  - test/unit/bestiary-yardstick.test.js
  - test/unit/foe-turn-draw-count.test.js
  - test/unit/parley.test.js
  - test/parity/fixture-inventory.test.js
  - test/determinism/foe-abilities.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/FIXTURE-INVENTORY.md
  - engine/difficulty.js
  - test/difficulty/difficulty.test.js
  - test/unit/combat-scaling.test.js
  - test/unit/maze.test.js
  - docs/DIFFICULTY-RETUNE.md
autonomous: true
requirements: [TUNE-06]
tags: [bestiary, dante, canon-deviation, parity-divergence, action-path-record, fixture-inventory, difficulty-curve, shallow-easing, iteration-0, ledger]

must_haves:
  truths:
    - "TUNE-06 (Dante, the ONE canon deviation): content/bestiary.js carries a `DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06)` block with the numbers; the landed form satisfies the recorded decision rule (an attack-only level-1 hero on floor 1 forced into a Humans encounter dies no more often than against the next-deadliest tier-1 type, <= 25 % over 2,000 seeds, measured by the executor's own scratch sim and quoted in the ledger); Dante stays recognisably Dante deeper (its stats and note text are byte-identical wherever it now sits); every test that pinned the old tier-1 Dante row/roster/draw totals — and the D-15 determinism spec whose tier-2 Humans pick moves because the tier-2 roster grew (`humans-t2` seed + its draw pins) — is RE-MEASURED live and updated with a Phase 27 comment — never loosened, never hand-computed; the other four determinism specs stay byte-identical."
    - "TUNE-06 (parity): the combat/parley seed-303 scenario is the ONLY parity divergence — declared on the scenario object in test/parity/fixtures/action-script.combat.json as a `kind: \"action-path\"` record (`fromAction: 0`, non-empty `fields`, measured `before`/`after`, `stateFields: [\"dead\"]`), machine-checked by the existing `actionPathDivergenceOf` / `skipsByteDiffAt` / `declaredEndDiffs` at both replay sites with ZERO edits to test/parity/harness/comparables.js, test/parity/prototype-master.js.txt or any other fixture; `node --test \"test/parity/**/*.test.js\"` is 33/33; test/parity/FIXTURE-INVENTORY.md gets a regenerated roster block and a `## Phase 27 Dante divergence (TUNE-06)` section with the measured per-action table. If the chosen Dante form diverged MORE than that one scenario, the executor picks a smaller change — never declares a second divergence."
    - "TUNE-06 (shallow non-combat easing, depths 3-5 only): engine/difficulty.js gains `DENSITY_CANON_THROUGH_DEPTH = 2` and a restructured encounter-dot formula that reproduces the prototype's 9+depth / depth-1 / 3+depth on floors 1-2 BY CONSTRUCTION (floor 1 = every fixture; floor 2 = the movement fixture's descend, seed 256), then eases `ENCOUNTER_DOT_CAP` 24 -> 15, `DARK_BLOB_CAP` 6 -> 3, `DARK_RADIUS_CAP` 9 -> 7 so darkness arrives smaller and density ramps a floor later; combat knobs (`COMBAT_SCALE_FROM_DEPTH`, `FOE_*`, `ABILITY_THREAT_*`) are NOT moved in this plan; the curve is draw-free (pure function of depth) and `difficultyCurve(1)` / `difficultyCurve(2)` deep-equal their pre-Phase-27 objects."
    - "Tests are restructured once so 27-03 only edits numbers: test/unit/combat-scaling.test.js gets a `PHASE_27_PINS` object (the single place the combat constants are pinned) and identity assertions parametrised on `COMBAT_SCALE_FROM_DEPTH`; test/difficulty/difficulty.test.js splits the PARITY GUARD into floors 1-2 (canon, fixture-exposed) and floors 3-5 (Phase 27 pins); test/unit/maze.test.js's seed-42 dot counts for depths 3-5 are re-measured. No assertion is weakened; no pin is adjusted without being measured first."
    - "Iteration 0 is logged in docs/DIFFICULTY-RETUNE.md (constants, the smoke readout from the harness's own `rollups.pooled` numbers and the 200-seed tune-difficulty cause table, what moved vs the Phase 26 AFTER, what to turn next) and the `### Dante demotion — landed (27-02)` sub-section holds the landed numbers, the sim result, the yardstick rows and the parity record summary."
    - "Engine gate: no new rng draw anywhere (a bestiary stat change and constants add none; the dots/darkness formulas change only how many EXISTING draws floors >= 3 make, which no fixture reaches); `npm test` fully green; `node --test test/voice/safety-scan.test.js` green (a new foe name is scanned); tools/lib/tuning-bot.mjs byte-identical to 5565b22."
  artifacts:
    - path: "content/bestiary.js"
      provides: "Humans tier 1 = the replacement entry; Dante at the END of Humans tier 2 with stats/note byte-identical; DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06) block; header comment no longer claims the four fixture-exposed rows are untouched"
      contains: "DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06)"
    - path: "test/parity/fixtures/action-script.combat.json"
      provides: "parley scenario (seed 303) divergence record, kind action-path, fromAction 0, measured before/after"
      contains: "\"kind\": \"action-path\""
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "regenerated roster block; ## Phase 27 Dante divergence (TUNE-06) — measured per-action table, the record, the FID-07 gate output, byte-identical elsewhere"
      contains: "## Phase 27 Dante divergence (TUNE-06)"
    - path: "engine/difficulty.js"
      provides: "DENSITY_CANON_THROUGH_DEPTH; restructured dots formula (canon on floors 1-2 by construction); ENCOUNTER_DOT_CAP 15, DARK_BLOB_CAP 3, DARK_RADIUS_CAP 7; DELIBERATE RULES CHANGE (Phase 27, TUNE-06) block for the non-combat knobs"
      contains: "export const DENSITY_CANON_THROUGH_DEPTH = 2;"
    - path: "test/unit/combat-scaling.test.js"
      provides: "PHASE_27_PINS object + pin test; identity parametrised on COMBAT_SCALE_FROM_DEPTH; non-combat pins for depths 1-5"
      contains: "PHASE_27_PINS"
    - path: "test/difficulty/difficulty.test.js"
      provides: "PARITY GUARD floors 1-2 (canon) + Phase 27 pins for floors 3-5; named-constants test on the eased values + DENSITY_CANON_THROUGH_DEPTH"
      contains: "DENSITY_CANON_THROUGH_DEPTH"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "### Dante demotion — landed (27-02) filled; ### Iteration log holds Iteration 0"
      contains: "Iteration 0"
  key_links:
    - from: "test/parity/fixtures/action-script.combat.json parley.divergence"
      to: "test/parity/combat-parity.test.js + full-suite.test.js"
      via: "actionPathDivergenceOf(scenario) -> skipsByteDiffAt from action 0 -> declaredEndDiffs both null (no harness edit)"
      pattern: "actionPathDivergenceOf"
    - from: "engine/difficulty.js DENSITY_CANON_THROUGH_DEPTH"
      to: "test/parity/movement-parity.test.js (seed 256 descends to floor 2)"
      via: "dots/darkBlobs/darkRadius at depth <= 2 are the prototype formulas by construction, so genFloor(2) draws and places exactly as before"
      pattern: "DENSITY_CANON_THROUGH_DEPTH"
    - from: "docs/DIFFICULTY-RETUNE.md Iteration 0"
      to: "the iteration-0 smoke JSONs' rollups.pooled"
      via: "numbers copied from `rollups.pooled` of the scratch iter0-nat.json / iter0-d20.json (never hand-derived)"
      pattern: "Iteration 0"
  prohibitions:
    - "MUST NOT edit test/parity/prototype-master.js.txt, test/parity/harness/comparables.js, or any fixture other than adding the one `divergence` record to the parley scenario object; MUST NOT regenerate or re-pin any fixture"
    - "MUST NOT move COMBAT_SCALE_FROM_DEPTH, FOE_CAP_*, FOE_POWER_*, ABILITY_THREAT_* or FOE_LVL_BIAS in this plan (27-03 owns the combat dials); MUST NOT change depth-1 or depth-2 dots/darkness"
    - "MUST NOT change tools/, the bot, any class/race/sub-class feature, engine/character.js, spells, items, upkeep or economy constants"
    - "MUST NOT weaken a test: every changed pin is measured live first (print the value, then write it) and carries a `Phase 27 (2026-09-15, TUNE-06)` comment naming the old value"
    - "MUST NOT overwrite docs/class-pass/*.json or docs/CLASS-PASS.md"
---

<objective>
Land the ONE deliberate canon deviation — the Dante demotion — with its declared, machine-checked parity divergence on the seed-303 parley scenario, then land the shallow-end non-combat easing (floors 3-5: fewer encounter dots, darkness smaller and later) behind a structural floors-1-2 canon gate, restructure the difficulty tests so 27-03 only edits numbers, take the iteration-0 smoke, and log it. Combat dials do not move here.

Purpose: 27-CONTEXT.md lever 2 ("Cut down by a Dante" is the #1 death cause; demote it, the exact form is Claude's discretion bounded by "smallest change that removes the floor-1 spike while keeping Dante recognisably Dante deeper") and lever 1's non-combat clause; the parity discipline is the Phase 23/24 declared-divergence pattern. Iteration 0 isolates the shallow-end effect before 27-03 turns the combat ramp.
Output: bestiary + fixture record + inventory section; `engine/difficulty.js` non-combat restructure and easing; restructured test pins; ledger `### Dante demotion — landed (27-02)` + `Iteration 0`; `npm test` green; parity 33/33; three commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-delve-to-death-retune/27-CONTEXT.md
@.planning/phases/27-delve-to-death-retune/27-01-SUMMARY.md
@docs/DIFFICULTY-RETUNE.md
@content/bestiary.js
@engine/difficulty.js
@engine/combat.js
@engine/maze.js
@test/parity/FIXTURE-INVENTORY.md
@test/parity/fixtures/action-script.combat.json
@test/parity/fixtures/action-script.schema.md
@test/parity/harness/comparables.js
@test/parity/combat-parity.test.js
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md
</context>

<dante_options>
## Dante — forms considered (planner calibration 2026-09-15; the executor re-measures the chosen form with the same sim)

Sim: for seeds 1..2000, `newRun(seed)` → `startCombat(state, false, "Humans", rng, [])` (rng = `makeRng(state.rngState)`) → `playerStrike` until `state.combat` is null or `state.dead` (cap 200 strikes); skip seeds where startCombat already cleared the fight. Reference tier-1 death rates on the same seeds: Beasts 6.8 %, Lair Beasts 13.2 %, Magical 13.3 %, Walking Dead 15.6 %, Demons 25.1 %.

| Form | Change | Hero died (floor 1, level 1) | Blast radius |
|---|---|---|---|
| canon | wp 20, `sp.atk` 3 | 59.8 % (two-Dante fights = 51 % of Humans encounters, won 16.7 %) | — |
| A | `sp.atk` 3 -> 2 | 51.4 % | 303 opener draw count changes -> action-path from 0; the note ("four arms: three strikes") becomes false |
| B | wp 20 -> 12 (10) | 46.8 % (42.8 %) | 303 foe objects differ from action 0; roster wp pins; yardstick wp pin |
| A+B | atk 2, wp 12 | 37.4 % | both of the above |
| **C (recommended, default)** | Dante moved to the END of Humans tier 2, stats + note byte-identical; new plain tier-1 Humans entry `{ n: "Ned", sz: "H", i: 8, wp: 8, sp: { note: "a bandit: one knife, one grudge, no plan" } }` | **14.3 %** (in the band of the other tier-1 types); depth-2 Humans fights for a level-2 hero 40.8 % -> 42.2 % (no new spike) | 303 rolls Ned x2 -> action-path from 0; roster/FID-01 names; 53 -> 54 row pins; Humans tier lengths [1,2,2,2,1] -> [1,3,2,2,1]; yardstick: tier medians UNCHANGED (T1 3.51/30.89, T2 3.31/10.87), Dante@T2 ttk 5.52 (1.67x) rtd 4.11 lethality 2.65 still `over-tier`, Ned@T1 ttk 5.62 (1.60x) unflagged |

Decision rule (recorded in the ledger by 27-01): the landed form must bring the floor-1 Humans death rate to <= 25 % (Demons, the next-deadliest tier-1 type). Only Form C does. A "demotion" in the bestiary's own vocabulary is a tier move; Form C keeps Dante 100 % recognisable ("twins, four arms: three strikes a round") one tier deeper, where its DPR (4.05) sits inside tier 2's 3.8-7.5 range.
</dante_options>

<tasks>

<task type="auto">
  <name>Task 1: Demote Dante (Form C) in content/bestiary.js, re-measure every unit-test pin that named the old row, addendum in BESTIARY-REBALANCE.md, voice scan</name>
  <files>content/bestiary.js, content/BESTIARY-REBALANCE.md, test/unit/content-tables.test.js, test/unit/bestiary-yardstick.test.js, test/unit/foe-turn-draw-count.test.js, test/unit/parley.test.js, test/parity/fixture-inventory.test.js, test/determinism/foe-abilities.test.js</files>
  <read_first>
    - content/bestiary.js: the header comment (Phase 18/19 paragraphs — the sentence claiming the four fixture-exposed creatures "are untouched and must stay so without a named comparables.js carve-out" must be superseded, not deleted), the `"Humans"` block (tier 1 = Dante only; tier 2 = China Wolf, Krupke), the existing DELIBERATE RULES CHANGE comment style on the Krupke row
    - test/unit/content-tables.test.js: the two "four fixture-exposed rows are byte-identical" tests (Phase 18 and Phase 19 copies, each deepStrictEqual on BESTIARY["Humans"][0]), the "tier lengths per type" test (`[1, 2, 2, 2, 1]` for Humans; `flat(2).length === 53`), the "abilities ABSENT ... 45 of 53" test
    - test/unit/bestiary-yardstick.test.js: the "53 rows" live smoke (tier median pins, the tier-1 fixture-rows filter and `wpByName` pins)
    - test/unit/foe-turn-draw-count.test.js: `FULL_FIGHTS` (the seed-303 row: Dante x2, 66 draws, 6 attacks, won — and the Phase 24 comment style for a re-measured row), `runFullFight`, the "FID-02 restated post-Phase-24" test's `FIXTURE_NAMES` list; the two `two Dante-shaped foes (atk 3 each)` micro pins use `fixedFoe` (synthetic) and stay as they are
    - test/unit/parley.test.js Test 14 ("D-21 seed-303 pin": `foes.every(name === "Dante")`, the `counting.log` dice pin `["d20=2","d6=5","d6=5"]`, need/sp/gold) — the seed is kept; the foe name and any dice/need/sp/gold value that changes is re-measured
    - test/parity/fixture-inventory.test.js: the expected roster row for parley/303 (`{ name: "Dante", type: "Humans", lvl: 1, wp: 20 }` x2), FID-01's `["Beasts:Bat/Rat", "Beasts:Shriek", "Beasts:Viper", "Humans:Dante"]`, and the doc-mention loop over the four names
    - test/determinism/foe-abilities.test.js: the `ENCOUNTERS` specs (`humans-t2` forces level/depth 2, type Humans, wants Krupke, `seed: 1`), `firstCasterSeed` (self-deriving: the FIRST seed in 1..300 whose roster contains the wanted caster — test 1 asserts the spec's seed equals it), `FULL_FIGHT_PINS["humans-t2"]` (Krupke x2, 59 draws, 4 attacks, won) and `PER_VISIT_PINS["humans-t2"]`. Appending Dante to Humans tier 2 makes `rng.pick` choose differently: the planner probed it — seed 1 now rolls Dante x2 and the first Krupke seed becomes 3 (roster `["Krupke"]`, one foe). The other four specs (Magical/Demons/Walking Dead/Beasts) are unaffected.
    - tools/bestiary-yardstick.mjs `computeYardstick` (to print the new rows); content/BESTIARY-REBALANCE.md `## Change ledger` (append an addendum there, do not rewrite history)
    - .claude/CLAUDE.md tone rules (family-friendly sarcasm) for the new entry's note; test/voice/safety-scan.test.js scans foe NAMES
  </read_first>
  <action>
    1. Re-measure the decision rule BEFORE editing: write `<scratchpad>/dante-sim.mjs` (ESM; on Windows import the engine via `file:///C:/projects/mazeworld/...` URLs) implementing the sim in `<dante_options>` for the canon bestiary, printing `died %` for forced "Humans" and for "Demons" (the reference). Then apply the change and run the sim again — the Humans figure must be <= the Demons figure. Keep the script in the scratchpad (never commit it); quote both readings in the ledger in Task 3.
    2. `content/bestiary.js` (CRLF file — Edit tool): replace the Humans tier-1 array `[{ n: "Dante", ... }]` with `[{ n: "Ned", sz: "H", i: 8, wp: 8, sp: { note: "a bandit: one knife, one grudge, no plan" } }]`; append the Dante row — byte-identical text `{ n: "Dante", sz: "H", i: 12, wp: 20, sp: { atk: 3, note: "twins, four arms: three strikes a round" } }` — as the LAST entry of Humans tier 2 (after Krupke; array order within a tier is load-bearing for `rng.pick`, so append, never insert before an existing row). Directly above the Humans block add the block comment `// DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06): Dante demoted from tier 1 to tier 2 (stats and note unchanged: sz H, i 12, wp 20, atk 3); tier-1 Humans is now Ned (sz H, i 8, wp 8, one swing a round). Why: "cut down by a Dante" was the #1 death cause at scale (724 of 5,720 Phase 26 runs) — three strikes a round and 20 wp against level-1 characters on floor 1; floor-1 Humans death rate <before> % -> <after> % (attack-only sim, 2,000 seeds). The seed-303 parley parity scenario now rolls Ned and carries a declared action-path divergence (test/parity/FIXTURE-INVENTORY.md, Phase 27 section). Ledger: docs/DIFFICULTY-RETUNE.md, "## v1.2 retune (Phase 27)".` Update the header comment's Phase 18 sentence: after "...must stay so without a named comparables.js carve-out (BEST-03/FID-05)" add "— superseded for Dante by Phase 27 (TUNE-06): Dante moved to tier 2, the tier-1 Humans row is Ned, and the seed-303 scenario carries a declared action-path divergence record instead of a comparables.js carve-out; the other three rows are still untouched".
    3. Tests — run each file, read the failure, confirm the measured value is what the bestiary change implies, then write it with a comment `// Phase 27 (2026-09-15, TUNE-06): was <old> — Dante moved to tier 2, Ned is the tier-1 Humans row (re-measured live, never hand-computed)`:
       - `content-tables.test.js`: both fixture-row tests → `BESTIARY["Humans"][0]` deep-equals the Ned row (rename the tests' "four fixture-exposed rows" wording to say Dante is now tier 2 and Ned is the exposed row); tier lengths Humans `[1, 3, 2, 2, 1]`; `flat(2).length` 53 → 54 in both places; "45 of 53" → 46 of 54 (`withoutAbilities.length` 46; `withAbilities` stays 8); add one assertion that `BESTIARY["Humans"][1].at(-1)` deep-equals the Dante row (recognisably Dante deeper).
       - `bestiary-yardstick.test.js`: `rows.length` 53 → 54; the tier-1 fixture filter list → `["Bat/Rat", "Shriek", "Viper", "Ned"]` with `wpByName.Ned === 8`; add `assert.equal(result.rows.find((r) => r.name === "Dante" && r.tier === 2).wp, 20)`; the tier median pins are expected UNCHANGED (T1 3.51 / 30.89, T2 3.31 / 10.87) — if any median assertion fails, STOP and report (the replacement's numbers are not what this plan specifies).
       - `foe-turn-draw-count.test.js`: `FULL_FIGHTS` seed 303 → `foeNames: ["Ned", "Ned"]` and the measured `totalDraws` / `attacks` / `outcome` (run `runFullFight(303, "Humans")` via a one-off `node -e` or read the assertion diff); `FIXTURE_NAMES` → `["Bat/Rat", "Shriek", "Viper", "Ned"]`; update the restated test's title numbers (12/87/101/<new>/32/119).
       - `parley.test.js` Test 14: `foes.every((f) => f.name === "Ned")`; re-measure the dice log and need/sp/gold with the test's own `countingRng` (print, then pin); keep the test's intent sentence and add the Phase 27 comment. Line ~273 (Test 4) and the D-19 old-save probe only rely on seed 303 rolling a Humans encounter — they should pass unchanged.
       - `test/determinism/foe-abilities.test.js`: run it; test 1 will report the re-derived first Krupke seed for `humans-t2` (expected 3) — set the spec's `seed` to that value, then re-measure `FULL_FIGHT_PINS["humans-t2"]` (foeNames, totalDraws, attacks, outcome) and `PER_VISIT_PINS["humans-t2"]` with the file's own `runFullFight` / per-visit helpers (print, then pin; Phase 27 comment naming the old `seed: 1` / `59 / 4 / won` values and the cause: Dante appended to Humans tier 2 changes the tier-2 pick). The other four specs' pins must be byte-unchanged — if any of them moves, STOP and report.
       - `fixture-inventory.test.js`: parley/303 expected foes → `{ name: "Ned", type: "Humans", lvl: 1, wp: 8 }` x2 (confirm with `node tools/fixture-inventory.mjs --json`); FID-01 names → `["Beasts:Bat/Rat", "Beasts:Shriek", "Beasts:Viper", "Humans:Ned"]`; doc-mention loop → `["Bat/Rat", "Shriek", "Viper", "Ned", "Dante"]` (Task 2 writes the section that mentions both). This test also compares the generated roster block in FIXTURE-INVENTORY.md — it stays RED until Task 2 regenerates the block; that is expected, note it in the commit message.
    Do NOT touch the `const BESTIARY = {` copy inside mazeworld.html (line ~1912) — it belongs to the dead prototype `startCombat` cluster the live app no longer calls (the engine's content/bestiary.js is the only live roster; Phase 18's rebalance left the HTML copy alone for the same reason).
    4. `content/BESTIARY-REBALANCE.md`: append under `## Change ledger` a short `### Phase 27 addendum (TUNE-06, 2026-09-15)` — Dante T1 → T2 unchanged, Ned added at T1 (row, yardstick numbers from `computeYardstick` for Ned@T1 and Dante@T2 in prototype mode), the fixture-exposed set is now Bat/Rat, Shriek, Viper, Ned; "the 53-row tables above are the Phase 18 record and are not regenerated"; pointer to docs/DIFFICULTY-RETUNE.md.
    5. Run `node --test test/voice/safety-scan.test.js` (the name Ned is scanned) and `node --test test/unit/content-tables.test.js test/unit/bestiary-yardstick.test.js test/unit/foe-turn-draw-count.test.js test/unit/parley.test.js test/determinism/foe-abilities.test.js`. Commit: `feat(27-02): Dante demoted to tier 2, Ned is tier-1 Humans — DELIBERATE RULES CHANGE (TUNE-06); pins re-measured (fixture-inventory doc block regenerates in the next commit)`.
  </action>
  <verify>
    <automated>node --test test/unit/content-tables.test.js test/unit/bestiary-yardstick.test.js test/unit/foe-turn-draw-count.test.js test/unit/parley.test.js test/determinism/foe-abilities.test.js test/voice/safety-scan.test.js && node -e "import('./content/bestiary.js').then(({BESTIARY})=>{const t1=BESTIARY.Humans[0],t2=BESTIARY.Humans[1];if(t1.length!==1||t1[0].n!=='Ned'||t1[0].wp!==8)throw new Error('tier1');const d=t2.at(-1);if(d.n!=='Dante'||d.wp!==20||d.sp.atk!==3||d.sp.note!=='twins, four arms: three strikes a round')throw new Error('dante');if(Object.values(BESTIARY).flat(2).length!==54)throw new Error('rows');console.log('bestiary ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `bestiary ok` and every listed test file passes.
    - The scratch sim's Humans death rate for the landed bestiary is <= its Demons death rate (both numbers quoted in the SUMMARY and, in Task 3, the ledger).
    - `git diff --quiet HEAD~1 -- engine src mazeworld.html tools test/parity/prototype-master.js.txt test/parity/harness` exits 0 (only content/ and the listed unit tests changed).
  </acceptance_criteria>
  <done>Dante is a tier-2 creature with byte-identical stats and note, Ned holds tier 1, the decision rule is met by measurement, and every unit pin that named the old row is re-measured and green; the parity-doc test is the only red left, by design, for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the seed-303 action-path divergence on the parley scenario, regenerate the FIXTURE-INVENTORY roster block, write the Phase 27 section, prove parity 33/33 with no harness or master edit</name>
  <files>test/parity/fixtures/action-script.combat.json, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/fixtures/action-script.combat.json: the `lose` scenario's existing `divergence` record (seed 14) — copy its shape exactly (phase, requirements, kind, fromAction, fields, before, after, stateFields, stateBefore, stateAfter, rationale)
    - test/parity/fixtures/action-script.schema.md, the `kind: "action-path"` paragraph (fields are read from `state.c`, taken verbatim from both sides, never hand-typed)
    - test/parity/combat-parity.test.js lines 139-235 (how `pathDiv` skips the byte diff from `fromAction` and how `declaredEndDiffs` is asserted; `loadPrototypeSandbox({ seed })`, `ctx.startCombat(wandering, forced)`, `ctx.parley()`, `applyStartCombat`, `applyAction`) — your scratch replay mirrors this loop
    - test/parity/FIXTURE-INVENTORY.md: the `## Phase 24 identity-pass divergences` section (the measured per-action table format, "Byte-identical elsewhere", the quoted FID-07 gate check) and the generated roster block markers; tools/fixture-inventory.mjs header (regenerate: run with no flags, paste stdout verbatim between the markers)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md (the record contract: declared, measured, asserted — before stripped)
  </read_first>
  <action>
    1. Measure both sides: write `<scratchpad>/seed303-replay.mjs` that imports `loadPrototypeSandbox` from `test/parity/harness/sandboxPrototype.js` and `newRun`/`applyAction` + `startCombat`/`makeRng` exactly as combat-parity.test.js does, replays the `parley` scenario (seed 303: action 0 `startCombat` wandering false forced "Humans"; action 1 `parley`) on the prototype sandbox (`ctx.S`) and the engine, and after EACH action prints, for both sides: the rolled foe names/wp, `c.wp`, `c.sp`, `c.gold`, `c.kills`, `c.rations`, `dead`, and whether `combat` is null. Record the printed table verbatim (it goes into FIXTURE-INVENTORY.md). Expected shape: the prototype still rolls Dante x2 and parleys as before; the engine rolls Ned x2 and its parley draws shift (Ned's opener swings differ from Dante's) — whether the engine's parley succeeds or fails is MEASURED, never assumed.
    2. Add to the `parley` scenario object in `action-script.combat.json` (2-space JSON, keep key order consistent with the `lose` record): `"divergence": { "phase": "27", "requirements": ["TUNE-06"], "kind": "action-path", "fromAction": 0, "fields": ["wp", "sp", "gold", "kills", "rations"], "before": { ...prototype end values... }, "after": { ...engine end values... }, "stateFields": ["dead"], "stateBefore": { "dead": <measured> }, "stateAfter": { "dead": <measured> }, "rationale": "Phase 27 (TUNE-06): Dante was demoted to tier 2 and the tier-1 Humans row is now Ned (wp 8, one swing a round) — the ONE deliberate canon deviation of the v1.2 retune (docs/DIFFICULTY-RETUNE.md, v1.2 section). From action 0 (startCombat) the engine rolls Ned x2 where the frozen prototype rolls Dante x2, so the foe objects and every downstream draw differ; the parley outcome on each side is recorded here as measured. No other scenario in any fixture rolls a Humans encounter, so this is the only divergence." }` — `fromAction` is 0 because the foe objects differ inside the startCombat action itself; the pre-action boot state stays byte-identical and is still asserted.
    3. Run `node --test test/parity/combat-parity.test.js test/parity/full-suite.test.js` — both green with the record and with ZERO changes to comparables.js. If either fails on `declaredEndDiffs`, the record's numbers are wrong: re-measure, never adjust to make it pass. If any OTHER scenario diverges, STOP — the Dante form must be made smaller (return to Task 1), never a second record.
    4. Regenerate the roster block: `node tools/fixture-inventory.mjs` → paste stdout verbatim between `<!-- fixture-inventory:generated:begin -->` / `:end -->` in FIXTURE-INVENTORY.md (only the parley row changes: `Ned (Humans lvl 1, wp 8); Ned (Humans lvl 1, wp 8)`). Next to the Phase 17/18 prose that calls Dante "the only level-1 Humans entry" (the `## Parity-exposed bestiary surface` paragraph and the FID-02 `combat/parley | 303 | Humans | Dante x2 | 6 | 66 | won` row) add a parenthetical "(Phase 27: Dante moved to tier 2 — see the Phase 27 section; the row is kept as the Phase 17 measurement)". Append a new section `## Phase 27 Dante divergence (TUNE-06)` at the END of the file: what changed in the bestiary and why (one paragraph, numbers from Task 1), `### Combat: parley scenario, seed 303` with the measured per-action table (prototype vs engine columns, the same layout as the Phase 24 seed-14 table), the record's before/after as declared, `### Draw-count pin re-measured` (the FULL_FIGHTS 303 row: 66/6/won → the new totals), `### Byte-identical elsewhere` (every other scenario unchanged; the movement fixture's floor-2 descend unaffected — floors 1-2 stay canon by construction in 27-02 Task 3; parity 33/33), and the quoted gate check output of `git diff --stat 4e4b3e7..HEAD -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js` (must print nothing; 4e4b3e7 = the Phase 27 start commit).
    5. `node --test "test/parity/**/*.test.js"` → 33/33 (the fixture-inventory doc test is green again). Commit: `test(27-02): seed-303 parley action-path divergence declared (Dante -> Ned) — FIXTURE-INVENTORY Phase 27 section, roster regenerated, parity 33/33`.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && git diff --quiet 4e4b3e7 -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js && node -e "const f=require('./test/parity/fixtures/action-script.combat.json');const p=f.scenarios.find(s=>s.name==='parley');if(!p.divergence||p.divergence.kind!=='action-path'||p.divergence.fromAction!==0||!p.divergence.fields.length)throw new Error('record');const n=f.scenarios.filter(s=>s.divergence).length;if(n!==2)throw new Error('expected exactly 2 records (lose + parley), got '+n);console.log('record ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "test/parity/**/*.test.js"` reports 33 passing files / 0 failing.
    - Exactly two combat scenarios carry a `divergence` record (`lose` from Phase 24 and `parley` from this plan); no fixture file other than action-script.combat.json changed; prototype-master.js.txt and comparables.js are byte-identical to 4e4b3e7.
    - `node tools/fixture-inventory.mjs` stdout equals the generated block in FIXTURE-INVENTORY.md (the fixture-inventory test asserts it).
    - FIXTURE-INVENTORY.md contains `## Phase 27 Dante divergence (TUNE-06)` as its last H2 and the measured per-action table.
  </acceptance_criteria>
  <done>The one divergence is declared on the scenario, measured on both sides, machine-checked at both replay sites through the existing helpers, documented in the inventory with the gate output, and every other parity scenario is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Shallow non-combat easing behind a floors-1-2 canon gate, test restructure (PHASE_27_PINS, split PARITY GUARD, re-measured maze pins), iteration-0 smoke, ledger entries</name>
  <files>engine/difficulty.js, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/unit/maze.test.js, docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - engine/difficulty.js in full: the constants, `softCap` (rounding) vs `softCapFloat`, `difficultyCurve` (the `over` gate for combat; the dots/darkBlobs/darkRadius expressions), the Phase 21 DELIBERATE RULES CHANGE block
    - engine/maze.js lines 105-150: `nDots = dc.dots` drives the dot-placement loop and `dc.darkBlobs` is the number of `rng.pick(open)` draws — so floors >= 3 will draw differently (allowed: no fixture reaches floor 3) and floors 1-2 must not
    - test/difficulty/difficulty.test.js in full (named constants, caps, PARITY GUARD 1-5, breather, monotone, tolerance); test/difficulty/fairness.test.js (property tests — should stay untouched)
    - test/unit/combat-scaling.test.js: "D-19 identity: difficultyCurve(1..5)" (deepStrictEqual incl. dots/darkBlobs/darkRadius), "21-04 retune values" (the constant pins), "depth-6 first divergence", "cap boundaries", "monotone", "the caps are reached", the draw-shape test at depth 30
    - test/unit/maze.test.js "feature counts match the prototype's formula for seed 42, depths 1-5"
    - docs/DIFFICULTY-RETUNE.md `## v1.2 retune (Phase 27)`: `### Iteration protocol`, the placeholders `### Dante demotion — landed (27-02)` and `### Iteration log`
    - tools/tune-classes.mjs / tools/tune-difficulty.mjs headers (flags); the `rollups.pooled` readout from 27-01
  </read_first>
  <action>
    A. Restructure first, value-neutral through depth 5 (prove it with the UNTOUCHED tests):
       - Add `export const DENSITY_CANON_THROUGH_DEPTH = 2;` with JSDoc: floors 1-2 are parity-fixture-exposed (every fixture is floor 1; the movement fixture, seed 256, descends to floor 2 and compares that floor byte-for-byte), so the three non-combat knobs reproduce the prototype's `9+depth` / `depth-1` / `3+depth` there BY CONSTRUCTION — the easing can only begin at depth 3.
       - Replace the dots expression with the offset form: `shallowDots = ENCOUNTER_DOT_BASE + Math.min(d, DENSITY_CANON_THROUGH_DEPTH)` (10 on floor 1, 11 on floor 2 and beyond), `dOver = Math.max(0, d - DENSITY_CANON_THROUGH_DEPTH)` (0 on floors 1-2), `dots: breather ? ENCOUNTER_DOT_BASE : shallowDots + Math.round(softCapFloat(0, ENCOUNTER_DOT_CAP - (ENCOUNTER_DOT_BASE + DENSITY_CANON_THROUGH_DEPTH), dOver, ENCOUNTER_DOT_SOFT_K))`. At the CURRENT constants this yields 10/11/12/13/14 on floors 1-5 (identical) and the same 17 at depth 10, 21 at depth 20, 24 at depth 50 (depth 7 shifts 16 -> 15 — acceptable, no test pins it). Darkness keeps its two expressions (their caps are the dials). If `softCap` (the rounding helper) is now unreferenced, delete it and fix the JSDoc sentences that cite it; keep `softCapFloat`.
       - Run `node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js` — ALL GREEN with no test edit (this proves the restructure changed nothing on floors 1-5).
    B. Move the non-combat dials (the shallow easing + the deep density this plan sets as iteration 0): `ENCOUNTER_DOT_CAP` 24 -> 15, `DARK_BLOB_CAP` 6 -> 3, `DARK_RADIUS_CAP` 9 -> 7 (`ENCOUNTER_DOT_SOFT_K` 12, `ENCOUNTER_DOT_BASE` 9, `DARK_RADIUS_BASE` 3, `BREATHER_EVERY` 5 unchanged). Print `difficultyCurve(d)` for d = 1..8, 10, 15, 20, 35, 50 and confirm floors 1-2 are unchanged (10/0/4 and 11/1/5) and floors 3-5 read dots 11/12/12, darkBlobs 2/3/3, darkRadius 6/7/7 (deep: dots 13 at 8-10, 14 at 15-20, 15 from ~30; blobs 3, radius 7). Add a `DELIBERATE RULES CHANGE (Phase 27, TUNE-06) — non-combat knobs` block above the ENCOUNTER_DOT_*/DARK_* constants: the whole-curve easing (darkness arrives smaller and stops growing at 3 blobs / radius 7; encounter density ramps a floor later and tops out at 15 dots instead of 24), floors 1-2 canon by construction (`DENSITY_CANON_THROUGH_DEPTH`), values recorded in docs/DIFFICULTY-RETUNE.md v1.2 section (iteration 0; 27-03 may nudge them per the "what to turn" table). Update the file header's Phase 21 paragraph with one Phase 27 sentence. Do NOT touch the combat constants or their block (27-03).
    C. Tests — measure, then pin (every changed pin gets a `Phase 27 (2026-09-15, TUNE-06): was <old>` comment):
       - `test/difficulty/difficulty.test.js`: import `DENSITY_CANON_THROUGH_DEPTH`; "named constants" → `ENCOUNTER_DOT_CAP` 15, `DARK_BLOB_CAP` 3, `DARK_RADIUS_CAP` 7, `DENSITY_CANON_THROUGH_DEPTH` 2 (others unchanged); split the PARITY GUARD into (i) `PARITY GUARD: depths 1-2 reproduce the prototype's exact 9+depth/depth-1/3+depth formula (fixture-exposed: every fixture is floor 1; movement seed 256 descends to floor 2)` looping 1..DENSITY_CANON_THROUGH_DEPTH, and (ii) `Phase 27 deliberate easing (TUNE-06): depths 3-5 are pinned to the retune values` asserting the literal dots/darkBlobs/darkRadius triples you printed; keep the guard's "an intentional retune signal" comment and note Phase 27 is that signal. The caps / breather / monotone / tolerance tests stay as they are (re-run them).
       - `test/unit/combat-scaling.test.js`: (i) add near the top `const PHASE_27_PINS = { COMBAT_SCALE_FROM_DEPTH: 6, FOE_CAP_BASE: 3, FOE_CAP_MAX: 5, FOE_CAP_SOFT_K: 20, FOE_POWER_BASE: 1, FOE_POWER_MAX: 1.6, FOE_POWER_SOFT_K: 35, ABILITY_THREAT_BASE: 1, ABILITY_THREAT_MAX: 2.0, ABILITY_THREAT_SOFT_K: 30, FOE_LVL_BIAS: 0 };` with a comment "the ONE place the combat dials are pinned — 27-03 edits only these numbers per iteration; values recorded in docs/DIFFICULTY-RETUNE.md v1.2 change table" and replace the "21-04 retune values" test with `Phase 27 retune pins (combat dials) match engine/difficulty.js` looping the object against the imported exports (these are still the 21-04 values in this plan — unchanged by design); (ii) add `const NON_COMBAT_PINS = { 1: { dots: 10, darkBlobs: 0, darkRadius: 4 }, 2: { dots: 11, darkBlobs: 1, darkRadius: 5 }, 3: {...}, 4: {...}, 5: {...} }` from the printed values and make the "D-19 identity" test assert, for d = 1..COMBAT_SCALE_FROM_DEPTH-1, that the combat fields equal identity (`foeCap` FOE_CAP_BASE, `foeBonus` 0, `foeLvlBias` FOE_LVL_BIAS, `Object.is(foePower, 1)`, `Object.is(abilityThreat, 1)`, `breather === isBreather(d)`) and, for d <= 5, that `{ dots, darkBlobs, darkRadius }` equals `NON_COMBAT_PINS[d]`; (iii) parametrise "depth-6 first divergence" on `COMBAT_SCALE_FROM_DEPTH`: identity through `COMBAT_SCALE_FROM_DEPTH - 1`, and at `COMBAT_SCALE_FROM_DEPTH` `foePower > 1` when `FOE_POWER_MAX > FOE_POWER_BASE` and `abilityThreat > 1` when `ABILITY_THREAT_MAX > ABILITY_THREAT_BASE` (drop the literal dots/dark expectations from that test — the non-combat pins live in NON_COMBAT_PINS). The draw-shape (depth 1/5/30), cap-boundaries, monotone and caps-reached tests stay.
       - `test/unit/maze.test.js` seed-42 feature counts: run it, read the actual `dot` counts for depths 3-5 from the failure, confirm each equals `difficultyCurve(d).dots`, then pin them (the tele/chest/trap/climb/gorge/one/exit counts must be unchanged — if one moves, stop and investigate; only dots changed).
       - Run `node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js test/determinism` then `node --test "test/parity/**/*.test.js"` (33/33 — floor 2 untouched) then `npm test` (green).
    D. Iteration-0 smoke (Dante + non-combat easing; combat dials untouched). Launch in the background (Bash `run_in_background`), each command's output redirected to a scratchpad log ending in an `EXIT=<code>` line: (1) `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --json --out <scratch>/iter0-nat.json` (~2-3 min), (2) `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --start-depth 20 --json --out <scratch>/iter0-d20.json` (~30 s), (3) `node tools/tune-difficulty.mjs --seeds=200 > <scratch>/iter0-td.txt` (~2 min). Write the ledger prose (step E) while they run; then check each log's last line with bounded polling (at most 10 checks); read the numbers from `rollups.pooled` of the two JSONs (`node -e` printing p50Depth, p90Depth, reach5, reach10, reach20, meanEncountersSurvived, meanFloorsGained, p50FloorsGained, topCauses) and from the tune-difficulty text (Death-depth distribution, Reach table, top 5 causes, Bot line).
    E. Ledger (Edit tool, CRLF): replace `(filled by plan 27-02)` under `### Dante demotion — landed (27-02)` with: the landed form (C) and the two bestiary rows; the executor's sim readings (canon Humans %, landed Humans %, Demons %); the yardstick rows (`computeYardstick` prototype mode: Ned@T1 and Dante@T2 ttk/rtd/lethality/flag; tier medians unchanged); the parity record summary (fields, before, after, stateBefore/After, fromAction 0) and the pointer to FIXTURE-INVENTORY.md's Phase 27 section; the list of re-measured pins (file: old → new). Replace `(filled by plans 27-02 and 27-03)` under `### Iteration log` with `**Iteration 0 (27-02) — Dante demotion + non-combat easing; combat dials untouched.**` followed by: constants (`ENCOUNTER_DOT_CAP` 24→15, `DARK_BLOB_CAP` 6→3, `DARK_RADIUS_CAP` 9→7, `DENSITY_CANON_THROUGH_DEPTH` 2 new; combat at the 21-04 values); the curve table for d = 1..5, 10, 20, 35, 50 (dots/blobs/radius/cap/power/ability); the smoke readout (pooled natural p50 / p90 / reach5 / reach10 / reach20 / top causes; pooled forced-20 encounters survived / floors gained mean + p50; tune-difficulty p50 / reach table / top 5 causes) with each band measure marked in / out; what moved vs the Phase 26 AFTER (is "cut down by a Dante" gone from the top natural causes? what replaced it?); what to turn next: "iteration 1 (27-03) turns the combat ramp — start from `COMBAT_SCALE_FROM_DEPTH` 16, `FOE_CAP_MAX` 4, `FOE_POWER_MAX` 1.3, `ABILITY_THREAT_MAX` 1.5 (planner calibration)"; then a final line `(iterations 1..N are appended below by plan 27-03)`. Sanity: `node --test test/unit/difficulty-retune-ledger.test.js` stays green.
    F. Commit: `feat(27-02): shallow non-combat easing behind DENSITY_CANON_THROUGH_DEPTH (dots cap 15, dark 3/7) + PHASE_27_PINS test restructure; iteration 0 logged`.
  </action>
  <verify>
    <automated>node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js test/unit/difficulty-retune-ledger.test.js && node --test "test/parity/**/*.test.js" && node -e "import('./engine/difficulty.js').then(m=>{const c1=m.difficultyCurve(1),c2=m.difficultyCurve(2);const ok=c1.dots===10&&c1.darkBlobs===0&&c1.darkRadius===4&&c2.dots===11&&c2.darkBlobs===1&&c2.darkRadius===5&&m.DENSITY_CANON_THROUGH_DEPTH===2&&m.ENCOUNTER_DOT_CAP===15&&m.DARK_BLOB_CAP===3&&m.DARK_RADIUS_CAP===7&&m.COMBAT_SCALE_FROM_DEPTH===6&&m.FOE_POWER_MAX===1.6;for(let d=1;d<=5;d++){const c=m.difficultyCurve(d);if(!Object.is(c.foePower,1)||c.foeCap!==3)throw new Error('combat identity broke at '+d)}if(!ok)throw new Error('non-combat gate/dials');console.log('curve ok')})" && npm test 2>&1 | tail -8 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - The verify chain prints `curve ok`, parity is 33/33, and `npm test` ends `# fail 0`.
    - `difficultyCurve(1)` and `difficultyCurve(2)` are byte-identical to their pre-plan objects (dots 10/11, darkBlobs 0/1, darkRadius 4/5, combat identity); depths 3-5 carry the pinned eased values; the combat constants still equal the 21-04 values.
    - `test/unit/combat-scaling.test.js` contains `PHASE_27_PINS` and no test named "21-04 retune values" remains.
    - `docs/DIFFICULTY-RETUNE.md` contains `Iteration 0` and the `### Dante demotion — landed (27-02)` body quotes both sim numbers and the yardstick rows; `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0; `git status --porcelain` is empty after the commit.
  </acceptance_criteria>
  <done>Floors 1-2 are canon by construction, floors 3-5 are eased and pinned, the deep density/darkness dials sit at their iteration-0 values, the combat dials are untouched and pinned in one object for 27-03, iteration 0 is measured and logged, and the tree is green and clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bestiary data -> parity fixtures | the tier-1 Humans row is rolled by the seed-303 scenario; any stat change diverges it and must be declared, never re-pinned |
| difficulty constants -> floor generation draws | dots/darkBlobs change the NUMBER of rng draws genFloor makes; floors 1-2 are fixture-exposed |
| measured pins -> tests | every re-measured number is printed from the engine before it is written into a test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-07 | Tampering | a second scenario diverges (Dante form too large) | high | mitigate | parity 33/33 gate; only ONE record allowed (verify counts exactly 2 records incl. Phase 24's); the instruction is to shrink the change, never add a record |
| T-27-08 | Tampering | prototype-master.js.txt or comparables.js edited to make parity pass | critical | mitigate | `git diff --quiet 4e4b3e7 -- prototype-master.js.txt harness/comparables.js` in Task 2's verify; the record is checked by the existing helpers |
| T-27-09 | Tampering | depth-2 dots/darkness changed (movement fixture floor 2) | high | mitigate | DENSITY_CANON_THROUGH_DEPTH gate by construction; PARITY GUARD 1-2 test; parity run after the constants move |
| T-27-10 | Repudiation | pins adjusted to pass instead of measured | high | mitigate | every changed pin carries a `was <old>` comment and the measured value is printed first; yardstick medians must be unchanged or the task stops |
| T-27-11 | Tampering | combat dials moved early (iteration 0 contaminated) | medium | mitigate | verify asserts COMBAT_SCALE_FROM_DEPTH 6 / FOE_POWER_MAX 1.6 at the end of the plan |
| T-27-12 | Information Disclosure | a new foe name/note outside the family-friendly voice | low | mitigate | voice safety scan runs in Task 1's verify; note text is fixed by this plan |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero deps) |
</threat_model>

<verification>
- `npm test` green; `node --test "test/parity/**/*.test.js"` 33/33 with exactly one new divergence record (parley, seed 303).
- `git diff --quiet 4e4b3e7 -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js` exits 0; `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0.
- `difficultyCurve(1..2)` unchanged, `3..5` eased and pinned, combat constants unchanged; `PHASE_27_PINS` exists.
- Ledger: `### Dante demotion — landed (27-02)` filled; `Iteration 0` logged with pooled numbers; guard test green.
</verification>

<success_criteria>
- The one canon deviation is landed, measured against the recorded rule, declared in parity and documented; the shallow end is eased without touching a fixture-exposed floor; 27-03 can iterate by editing only `engine/difficulty.js` combat constants and `PHASE_27_PINS` (plus the non-combat caps if the "what to turn" table says so).
</success_criteria>

<output>
Create `.planning/phases/27-delve-to-death-retune/27-02-SUMMARY.md` when done. Record: the three commit hashes; the landed Dante form and both sim numbers; the seed-303 record (fields/before/after/stateAfter) and the re-measured FULL_FIGHTS totals; the curve table 1-5/10/20/35/50 after the easing; the iteration-0 pooled readout; the `npm test` pass count and parity 33/33.
</output>
