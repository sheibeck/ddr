---
phase: 27-delve-to-death-retune
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - content/bestiary.js
  - content/BESTIARY-REBALANCE.md
  - test/unit/content-tables.test.js
  - test/unit/bestiary-yardstick.test.js
  - test/unit/foe-turn-draw-count.test.js
  - test/unit/parley.test.js
  - test/parity/fixture-inventory.test.js
  - test/determinism/foe-abilities.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/FIXTURE-INVENTORY.md
  - engine/difficulty.js
  - engine/combat.js
  - engine/movement.js
  - engine/encounters.js
  - test/difficulty/difficulty.test.js
  - test/unit/combat-scaling.test.js
  - test/unit/maze.test.js
  - test/unit/encounters.test.js
  - test/unit/movement.test.js
  - docs/DIFFICULTY-RETUNE.md
autonomous: true
requirements: [TUNE-06]
tags: [bestiary, dante, canon-deviation, parity-divergence, action-path-record, fixture-inventory, difficulty-curve, early-floor-levers, foe-grace, hazard-ramp, darkness-hold, shallow-easing, iteration-0, ledger]

must_haves:
  truths:
    - "TUNE-06 (Dante, the first canon deviation): content/bestiary.js carries a `DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06)` block with the numbers; the landed form satisfies the recorded decision rule (an attack-only level-1 hero on floor 1 forced into a Humans encounter dies no more often than against the next-deadliest tier-1 type, <= 25 % over 2,000 seeds, measured by the executor's own scratch sim and quoted in the ledger); Dante stays recognisably Dante deeper (its stats and note text are byte-identical wherever it now sits); every test that pinned the old tier-1 Dante row/roster/draw totals — and the D-15 determinism spec whose tier-2 Humans pick moves because the tier-2 roster grew (`humans-t2` seed + its draw pins) — is RE-MEASURED live and updated with a Phase 27 comment — never loosened, never hand-computed."
    - "TUNE-06 (parity): after this plan the combat/parley seed-303 scenario is the ONLY parity divergence — declared on the scenario object in test/parity/fixtures/action-script.combat.json as a `kind: \"action-path\"` record (`fromAction: 0`, non-empty `fields`, measured `before`/`after`, `stateFields: [\"dead\"]`), machine-checked by the existing `actionPathDivergenceOf` / `skipsByteDiffAt` / `declaredEndDiffs` at both replay sites with ZERO edits to test/parity/harness/comparables.js, test/parity/prototype-master.js.txt or any other fixture; `node --test \"test/parity/**/*.test.js\"` is 33/33. Every early-floor lever this plan LANDS is parity-clean by construction (foe grace only at depths 2-4 — no fixture fights past floor 1; hazard ramp from floor 2 — no fixture springs a trap or falls past floor 1; darkness hold — floor 2 keeps its single canon blob); test/parity/FIXTURE-INVENTORY.md's Phase 27 section records this and pre-enumerates the records 27-03 must declare if it escalates (encounters trap seed 1, movement seed 256, the 14 chargen seeds, the floor-1 combat/magic scenarios)."
    - "TUNE-06 (early-floor levers, each behind its own draw-free difficulty.js field): `foePower` may sit BELOW 1.0 only at depths 2..FOE_GRACE_CANON_FROM_DEPTH-1 (`FOE_GRACE_AT_2`, linear to exactly 1.0 at FOE_GRACE_CANON_FROM_DEPTH; floor 1 = `FOE_GRACE_AT_1` = 1.0 exactly — last resort only, 27-03); a new `hazardScale` field (1.0 below `HAZARD_FROM_DEPTH` = 2, `HAZARD_SCALE_AT_START` flat through `HAZARD_FLAT_THROUGH_DEPTH`, linear to exactly 1.0 at `HAZARD_CANON_FROM_DEPTH`) is consumed as post-draw arithmetic by engine/movement.js (climb/gorge fall hurt) and engine/encounters.js (springTrap dmg) with a strict `=== 1` fast path so every floor outside the ramp is byte-identical and no draw is added; `darkBlobs` holds at one blob through `DARK_HOLD_THROUGH_DEPTH` = 3 and grows from floor 4 (floor 2 canon); the dots formula is restructured behind `DENSITY_CANON_THROUGH_DEPTH` = 2 (floors 1-2 canon by construction) with `ENCOUNTER_DOT_CAP` 24 -> 15, `DARK_BLOB_CAP` 6 -> 3, `DARK_RADIUS_CAP` 9 -> 7; the deep combat dials (`COMBAT_SCALE_FROM_DEPTH`, `FOE_*_MAX`, `ABILITY_THREAT_*`) are NOT moved in this plan; `difficultyCurve(1)` deep-equals its pre-Phase-27 object plus the two new fields at identity (`hazardScale` 1, `foePower` 1), and `difficultyCurve(2)` keeps dots 11 / darkBlobs 1 / darkRadius 5."
    - "Tests are restructured once so 27-03 only edits numbers: test/unit/combat-scaling.test.js gets a `PHASE_27_PINS` object (every Phase 27 dial incl. the grace/hazard/darkness constants — the single place they are pinned), floor-1 exactness, the grace band, identity from FOE_GRACE_CANON_FROM_DEPTH through COMBAT_SCALE_FROM_DEPTH-1, monotone from depth 2 (floor 1 is the canon anchor the grace dips under, by design), `foeWpFor` never below 1, negative `dmgBonus` on a grace floor, `hazardScale` values and its two consumers at depth 1 (unchanged) vs depth 2 (scaled); test/difficulty/difficulty.test.js splits the PARITY GUARD into floors 1-2 (canon, fixture-exposed) and floors 3-5 (Phase 27 pins); test/unit/maze.test.js's seed-42 dot counts for depths 3-5 are re-measured. No assertion is weakened; no pin is adjusted without being measured first."
    - "Iteration 0 is logged in docs/DIFFICULTY-RETUNE.md (constants, the smoke readout from the harness's own `rollups.pooled` numbers and the 200-seed tune-difficulty cause table, what moved vs the Phase 26 AFTER, the planner's per-lever calibration table, what to turn next in the stated escalation order) and `### Dante demotion — landed (27-02)` holds the landed numbers, the sim result, the yardstick rows and the parity record summary."
    - "Engine gate: no new rng draw anywhere (bestiary stats, constants, and post-draw scaling add none; dots/darkness change only how many EXISTING draws floors >= 3 make, which no fixture reaches); `npm test` fully green; `node --test test/voice/safety-scan.test.js` green (a new foe name is scanned); tools/lib/tuning-bot.mjs byte-identical to 5565b22; no change to engine/character.js, spells, items, upkeep or economy."
  artifacts:
    - path: "content/bestiary.js"
      provides: "Humans tier 1 = Ned; Dante at the END of Humans tier 2 with stats/note byte-identical; DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06) block; header comment no longer claims the four fixture-exposed rows are untouched"
      contains: "DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06)"
    - path: "test/parity/fixtures/action-script.combat.json"
      provides: "parley scenario (seed 303) divergence record, kind action-path, fromAction 0, measured before/after"
      contains: "\"kind\": \"action-path\""
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "regenerated roster block; ## Phase 27 early-floor divergences (TUNE-06): the Dante record (measured per-action table), the parity-clean-by-construction levers, and the pre-enumerated escalation records for 27-03"
      contains: "## Phase 27 early-floor divergences (TUNE-06)"
    - path: "engine/difficulty.js"
      provides: "DENSITY_CANON_THROUGH_DEPTH, FOE_GRACE_AT_1/AT_2/CANON_FROM_DEPTH, HAZARD_FROM_DEPTH/SCALE_AT_START/FLAT_THROUGH_DEPTH/CANON_FROM_DEPTH, DARK_HOLD_THROUGH_DEPTH; restructured dots; graced foePower; new hazardScale field; eased non-combat caps; DELIBERATE RULES CHANGE (Phase 27, TUNE-06) — early-floor levers block"
      contains: "hazardScale"
    - path: "engine/movement.js"
      provides: "fall hurt scaled by difficultyCurve(depth).hazardScale after the canon dice (post-draw, === 1 fast path)"
      contains: "hazardScale"
    - path: "engine/encounters.js"
      provides: "springTrap dmg scaled by difficultyCurve(depth).hazardScale after Hardiness (post-draw, === 1 fast path)"
      contains: "hazardScale"
    - path: "engine/combat.js"
      provides: "startCombat copies a NEGATIVE dmgBonus too (`!== 0`), so a grace floor reduces foe melee; depth 1 still never carries the key"
      contains: "dmgBonus !== 0"
    - path: "test/unit/combat-scaling.test.js"
      provides: "PHASE_27_PINS + pin test; floor-1 exactness; grace band; identity band; monotone-from-2; foeWpFor floor; negative dmgBonus; hazardScale consumers"
      contains: "PHASE_27_PINS"
    - path: "test/difficulty/difficulty.test.js"
      provides: "PARITY GUARD floors 1-2 (canon) + Phase 27 pins for floors 3-5; named-constants test on the eased values + the new constants"
      contains: "DENSITY_CANON_THROUGH_DEPTH"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "### Dante demotion — landed (27-02) filled; ### Iteration log holds Iteration 0 with the per-lever calibration table"
      contains: "Iteration 0"
  key_links:
    - from: "test/parity/fixtures/action-script.combat.json parley.divergence"
      to: "test/parity/combat-parity.test.js + full-suite.test.js"
      via: "actionPathDivergenceOf(scenario) -> skipsByteDiffAt from action 0 -> declaredEndDiffs both null (no harness edit)"
      pattern: "actionPathDivergenceOf"
    - from: "engine/difficulty.js hazardScale"
      to: "engine/movement.js (fall) + engine/encounters.js (springTrap)"
      via: "post-draw multiply with Math.max(1, Math.round(x * hs)) only when hs !== 1 and x > 0"
      pattern: "\\.hazardScale"
    - from: "engine/difficulty.js foePower grace (< 1 at depths 2-4)"
      to: "engine/combat.js startCombat foeWpFor / foeDmgBonusFor"
      via: "wp = max(1, round(baseWp * foePower)); dmgBonus = round((foePower - 1) * lvl^2) copied when !== 0"
      pattern: "dmgBonus !== 0"
    - from: "engine/difficulty.js DENSITY_CANON_THROUGH_DEPTH / DARK_HOLD_THROUGH_DEPTH"
      to: "test/parity/movement-parity.test.js (seed 256 descends to floor 2)"
      via: "dots/darkBlobs/darkRadius at depth <= 2 are the prototype formulas by construction, so genFloor(2) draws and places exactly as before"
      pattern: "DENSITY_CANON_THROUGH_DEPTH"
  prohibitions:
    - "MUST NOT edit test/parity/prototype-master.js.txt, test/parity/harness/comparables.js, or any fixture other than adding the one `divergence` record to the parley scenario object; MUST NOT regenerate or re-pin any fixture"
    - "MUST NOT move COMBAT_SCALE_FROM_DEPTH, FOE_CAP_*, FOE_POWER_MAX/_SOFT_K, ABILITY_THREAT_* or FOE_LVL_BIAS in this plan (27-03 owns the deep dials); MUST NOT change depth-1 or depth-2 dots/darkness; MUST NOT set FOE_GRACE_AT_1 below 1.0 or HAZARD_FROM_DEPTH below 2 (27-03 escalation only, with records)"
    - "MUST NOT change tools/, the bot, any class/race/sub-class feature, engine/character.js (starting rations are a 27-03 escalation with 14 chargen records), spells, items, upkeep or economy constants"
    - "MUST NOT weaken a test: every changed pin is measured live first (print the value, then write it) and carries a `Phase 27 (2026-09-15, TUNE-06)` comment naming the old value"
    - "MUST NOT overwrite docs/class-pass/*.json or docs/CLASS-PASS.md"
---

<objective>
Land the canon deviations the user widened the retune to (27-CONTEXT.md Levers §2-3), each behind its own draw-free `engine/difficulty.js` field, in the parity-clean form first: the Dante demotion with its declared seed-303 divergence, foe grace at floors 2-4 (`foePower` < 1, floor 1 exactly 1.0), a trap/wall-fall damage ramp starting at floor 2 (`hazardScale`, consumed by movement.js and encounters.js), darkness held at one blob through floor 3, and the floors-3-5 density/darkness easing behind a floors-1-2 canon gate. Restructure the difficulty tests so 27-03 only edits numbers, take the iteration-0 smoke, and log it with the planner's per-lever calibration so 27-03 escalates in the stated order (grace deeper → darkness from 4 → hazard from floor 1 → rations → floor-1 grace) only as far as the median needs. Deep combat dials do not move here.

Purpose: the planner's calibration showed dials + Dante alone cannot move the bot median off 3 (floors 2-4 kill 66-85 % of runs: canon tier-2/3 foes, traps, wall-falls, starvation in the dark); the user chose to widen the levers rather than record a known miss. This plan builds every widened lever's seam and lands the set that costs no parity beyond Dante; 27-03 turns the values.
Output: bestiary + fixture record + inventory section; `engine/difficulty.js` fields + three consumers; restructured test pins; ledger `### Dante demotion — landed (27-02)` + `Iteration 0`; `npm test` green; parity 33/33; four commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-delve-to-death-retune/27-CONTEXT.md
@.planning/phases/27-delve-to-death-retune/27-01-SUMMARY.md
@docs/DIFFICULTY-RETUNE.md
@content/bestiary.js
@engine/difficulty.js
@engine/combat.js
@engine/movement.js
@engine/encounters.js
@engine/maze.js
@test/parity/FIXTURE-INVENTORY.md
@test/parity/fixtures/action-script.combat.json
@test/parity/fixtures/action-script.schema.md
@test/parity/harness/comparables.js
@test/parity/combat-parity.test.js
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md
</context>

<dante_options>
## Dante — forms considered (planner calibration 2026-09-15; the executor re-measures the chosen form with the same sim)

Sim: for seeds 1..2000, `newRun(seed)` → `startCombat(state, false, "Humans", rng, [])` (rng = `makeRng(state.rngState)`) → `playerStrike` until `state.combat` is null or `state.dead` (cap 200 strikes); skip seeds where startCombat already cleared the fight. Reference tier-1 death rates on the same seeds: Beasts 6.8 %, Lair Beasts 13.2 %, Magical 13.3 %, Walking Dead 15.6 %, Demons 25.1 %.

| Form | Change | Hero died (floor 1, level 1) | Blast radius |
|---|---|---|---|
| canon | wp 20, `sp.atk` 3 | 59.8 % (two-Dante fights = 51 % of Humans encounters, won 16.7 %) | — |
| A | `sp.atk` 3 -> 2 | 51.4 % | 303 opener draw count changes -> action-path from 0; the note ("four arms: three strikes") becomes false |
| B | wp 20 -> 12 (10) | 46.8 % (42.8 %) | 303 foe objects differ from action 0; roster wp pins; yardstick wp pin |
| A+B | atk 2, wp 12 | 37.4 % | both of the above |
| **C (recommended, default)** | Dante moved to the END of Humans tier 2, stats + note byte-identical; new plain tier-1 Humans entry `{ n: "Ned", sz: "H", i: 8, wp: 8, sp: { note: "a bandit: one knife, one grudge, no plan" } }` | **14.3 %** (in the band of the other tier-1 types); depth-2 Humans fights for a level-2 hero 40.8 % -> 42.2 % (no new spike) | 303 rolls Ned x2 -> action-path from 0; roster/FID-01 names; 53 -> 54 row pins; Humans tier lengths [1,2,2,2,1] -> [1,3,2,2,1]; yardstick: tier medians UNCHANGED (T1 3.51/30.89, T2 3.31/10.87), Dante@T2 ttk 5.52 (1.67x) rtd 4.11 lethality 2.65 still `over-tier`, Ned@T1 ttk 5.62 (1.60x) unflagged; determinism `humans-t2` seed 1 -> 3 |

Decision rule (recorded in the ledger by 27-01): the landed form must bring the floor-1 Humans death rate to <= 25 % (Demons, the next-deadliest tier-1 type). Only Form C does. A "demotion" in the bestiary's own vocabulary is a tier move; Form C keeps Dante 100 % recognisable ("twins, four arms: three strikes a round") one tier deeper, where its DPR (4.05) sits inside tier 2's 3.8-7.5 range.
</dante_options>

<early_floor_levers>
## The widened early-floor levers — field design, parity blast radius, planner calibration

Bot kill table (27-CONTEXT lever 3): 20 % die on floor 1, 66 % by floor 3, 85 % by floor 4. Median 5-6 needs >= 50 % of runs to survive floor 4 (pooled `reach5` >= 50 %). Calibration below: 200-seed `tune-difficulty` natural (td200: p50 / reach >= 5) and the 143 x 3 class matrix (pooled `reach5`, mean), each lever added on top of S0 = Dante Form C + dots cap 15 / dark 3/7 + the 27-03 deep-dial start values (FROM 16, cap 4, power 1.3, cadence 1.5). Directional only — the executor re-smokes.

| Lever | difficulty.js field / constants | Consumers | Parity blast radius (exact) | Calibration (matrix reach5, td200 p50) |
|---|---|---|---|---|
| S0 (Dante + non-combat easing + deep dials) | — | — | seed-303 parley only | reach5 17.0 %, mu 3.24, p50 3 |
| Foe grace, floors 2-4 | `FOE_GRACE_AT_2` (0.75 -> foePower 0.75 / 0.833 / 0.917 at 2 / 3 / 4), `FOE_GRACE_CANON_FROM_DEPTH` 5 (exactly 1.0 from 5), `FOE_GRACE_AT_1` 1.0 | startCombat: `foeWpFor` (wp rounds, floor 1 hp) and `foeDmgBonusFor` (negative bonus now copied when `!== 0`) | ZERO fixtures (every fixture fights on floor 1; the movement fixture never fights on floor 2). Unit/determinism pins: `humans-t2` (depth 2) and `magical-t4` (depth 4) FULL_FIGHT/PER_VISIT pins re-measured; tier-5 specs unchanged (exactly 1.0) | reach5 22.8 %, mu 3.48, p50 3 (0.75); stronger 0.5 / 0.65 / 0.85: see the T-rows below |
| Darkness held through 3 (parity-clean form) | `DARK_HOLD_THROUGH_DEPTH` 3: blobs 0 / 1 / 1 / 2 / 3 on floors 1-5 | maze.js genFloor (fewer `rng.pick` draws on floors 3+) | ZERO fixtures (floor 2 keeps its one canon blob) | ~ +4-5 pts (hold1 rows) |
| Darkness from floor 4 (user's wording; escalation form) | `DARK_FROM_DEPTH` 4: blobs 0 / 0 / 0 / 1 / 2 | same | movement seed 256 diverges from the descend action (floor 2 loses its blob: one fewer draw, a different grid) — action-path record at the fixture top level + wiring `actionPathDivergenceOf`/`skipsByteDiffAt`/`declaredEndDiffs` into movement-parity.test.js and full-suite's movement sub-test (comparables untouched) | reach5 21.7 %, p50 3 (alone); grace+dark4: 25.9 %, p50 4 |
| Trap/wall-fall ramp from floor 2 (parity-clean form) | `HAZARD_FROM_DEPTH` 2, `HAZARD_SCALE_AT_START` 0.5, `HAZARD_FLAT_THROUGH_DEPTH` 3, `HAZARD_CANON_FROM_DEPTH` 5 -> hazardScale 1 / 0.5 / 0.5 / 0.75 / 1 | movement.js fall `hurt`, encounters.js springTrap `dmg` (post-draw; `=== 1` fast path) | ZERO fixtures (no fixture springs a trap or climbs/leaps past floor 1). Unit pins at depth 1 (fixedState) untouched | T1 row below |
| Trap/wall-fall ramp from floor 1 (escalation form) | `HAZARD_FROM_DEPTH` 1 -> hazardScale 0.5 / 0.5 / 0.5 / 0.75 / 1 | same | encounters `trap` scenario, seed 1 (Knight; Darts, roll 4, dmg 3 -> 2; wp 55 -> 56): ONE scenario — a Phase 23 field-strip record `{ fields: ["wp"], before, after }` on the scenario + wiring `stripScenarioDivergence` (assert before/after, then strip) into full-suite's encounters loop; plus ~12 unit pins at fixedState depth 1 (encounters.test.js trap dmg, movement.test.js fall hurt, identity-contract) re-measured | reach5 24.0 %, mu 3.51, p50 4 (alone, floors 1-3) |
| Starting rations +N (escalation) | engine/character.js rollCharacter `rations` (+1 or +2) | newDay upkeep | chargen fixture: all 14 seeds compare `rations` — 14 per-seed `divergences` records (`fields: ["rations"]`; seeds 15 and 24 merge with their existing grimoire records); combat `lose` seed 14 and magic `cast-damage` seed 8 records re-measured (`after.rations` +N); other comparables already strip rations | S4/T3/T4 rows below |
| Floor-1 foe grace (LAST RESORT) | `FOE_GRACE_AT_1` 0.85 | startCombat | combat win 3 / flee 17 / lose-apprentice 127 gain action-path records; lose 14 + parley 303 records re-measured; magic cast-damage 8 record re-measured (convert to action-path if its path flips); FID-02 FULL_FIGHTS (5 rows) + fixture-inventory roster wp values re-measured; ledger note that the fidelity contract now carries broad early-floor exceptions | T5/T6 rows below |

Combined sets — matrix 143 x 3 pooled reach5 / mu; td200 p50, reach5 (copy this table into the ledger's iteration-0 entry as `#### Planner calibration (2026-09-15, directional)`):

| Set | reach5 (matrix) | mu | td200 p50 / reach5 |
|---|---|---|---|
| S0: Dante + dots 15 / dark 3-7 + deep-dial start values | 17.0 % | 3.24 | 3 / 24.0 % |
| S0 + grace 0.75 (2-4) | 22.8 % | 3.48 | 3 / 23.0 % |
| S0 + darkness from 4 | 21.7 % | 3.39 | 3 / 23.5 % |
| S0 + hazard 0.5 on floors 1-3 (0.75 at 4) | 24.0 % | 3.51 | 4 / 31.5 % |
| S0 + grace 0.75 + darkness from 4 | 25.9 % | 3.58 | 4 / 28.5 % |
| S0 + grace 0.75 + darkness held through 3 | 18.4 % | 3.32 | 3 / 20.0 % |
| T1: S0 + hazard from floor 2 (1 / 0.5 / 0.5 / 0.75) | 22.4 % | 3.44 | 4 / 29.5 % |
| T2: S0 + grace 0.5 (0.5 / 0.65 / 0.85) + hold through 3 + hazard from 2 — the strongest parity-clean set | 24.7 % | 3.60 | 4 / 27.5 % |
| T3: T2 + starting rations +2 | 30.8 % | 3.76 | 4 / 31.5 % |
| T4: grace 0.5 + darkness from 4 + hazard from floor 1 + rations +2 (every rung but floor-1 grace) | 35.4 % | 3.96 | 4 / 36.0 % |
| T5: T4 + floor-1 grace 0.85 (last resort) | 34.7 % | 4.00 | 4 / 34.0 % |

Reading: each lever adds ≈ 3-7 points of reach5 and the FULL ladder — every rung including the last resort — tops out near 35 % reach5 (bot median 4, mean ≈ 4.0); floor-1 grace adds nothing measurable once Dante is gone (its spike WAS Dante). The remaining floor 2-4 killers are canon tier-2/3 foes (Werebeast, Poltergeist, Blumble), starvation and falls. The parity-clean set (grace 0.75 + hold + hazard-from-2) is landed here; 27-03 escalates in the user's order only while the smoke median is < 5 and only where the calibration predicts a gain — the median target is expected to be RECORDED as a miss (bot median 4) and handed to the DR round unless the executor's smoke says otherwise; the orchestrator is told this in the SUMMARY.
</early_floor_levers>

<tasks>

<task type="auto">
  <name>Task 1: Demote Dante (Form C) in content/bestiary.js, re-measure every unit/determinism pin that named the old row or the old tier-2 pick, addendum in BESTIARY-REBALANCE.md, voice scan</name>
  <files>content/bestiary.js, content/BESTIARY-REBALANCE.md, test/unit/content-tables.test.js, test/unit/bestiary-yardstick.test.js, test/unit/foe-turn-draw-count.test.js, test/unit/parley.test.js, test/parity/fixture-inventory.test.js, test/determinism/foe-abilities.test.js</files>
  <read_first>
    - content/bestiary.js: the header comment (Phase 18/19 paragraphs — the sentence claiming the four fixture-exposed creatures "are untouched and must stay so without a named comparables.js carve-out" must be superseded, not deleted), the `"Humans"` block (tier 1 = Dante only; tier 2 = China Wolf, Krupke), the existing DELIBERATE RULES CHANGE comment style on the Krupke row
    - test/unit/content-tables.test.js: the two "four fixture-exposed rows are byte-identical" tests (Phase 18 and Phase 19 copies, each deepStrictEqual on BESTIARY["Humans"][0]), the "tier lengths per type" test (`[1, 2, 2, 2, 1]` for Humans; `flat(2).length === 53`), the "abilities ABSENT ... 45 of 53" test
    - test/unit/bestiary-yardstick.test.js: the "53 rows" live smoke (tier median pins, the tier-1 fixture-rows filter and `wpByName` pins)
    - test/unit/foe-turn-draw-count.test.js: `FULL_FIGHTS` (the seed-303 row: Dante x2, 66 draws, 6 attacks, won — and the Phase 24 comment style for a re-measured row), `runFullFight`, the "FID-02 restated post-Phase-24" test's `FIXTURE_NAMES` list; the two `two Dante-shaped foes (atk 3 each)` micro pins use `fixedFoe` (synthetic) and stay as they are
    - test/unit/parley.test.js Test 14 ("D-21 seed-303 pin": `foes.every(name === "Dante")`, the `counting.log` dice pin `["d20=2","d6=5","d6=5"]`, need/sp/gold) — the seed is kept; the foe name and any dice/need/sp/gold value that changes is re-measured
    - test/parity/fixture-inventory.test.js: the expected roster row for parley/303 (`{ name: "Dante", type: "Humans", lvl: 1, wp: 20 }` x2), FID-01's `["Beasts:Bat/Rat", "Beasts:Shriek", "Beasts:Viper", "Humans:Dante"]`, and the doc-mention loop over the four names
    - test/determinism/foe-abilities.test.js: the `ENCOUNTERS` specs (`humans-t2` forces level/depth 2, type Humans, wants Krupke, `seed: 1`), `firstCasterSeed` (self-deriving: the FIRST seed in 1..300 whose roster contains the wanted caster — test 1 asserts the spec's seed equals it), `FULL_FIGHT_PINS["humans-t2"]` (Krupke x2, 59 draws, 4 attacks, won) and `PER_VISIT_PINS["humans-t2"]`. Appending Dante to Humans tier 2 makes `rng.pick` choose differently: the planner probed it — seed 1 now rolls Dante x2 and the first Krupke seed becomes 3 (roster `["Krupke"]`, one foe). NOTE: Task 3's foe grace changes these depth-2 and depth-4 pins AGAIN (wp scaling) — re-measure them in Task 1 for the Dante move and once more in Task 3; both comments name their cause.
    - tools/bestiary-yardstick.mjs `computeYardstick` (to print the new rows); content/BESTIARY-REBALANCE.md `## Change ledger` (append an addendum there, do not rewrite history)
    - .claude/CLAUDE.md tone rules (family-friendly sarcasm) for the new entry's note; test/voice/safety-scan.test.js scans foe NAMES
  </read_first>
  <action>
    1. Re-measure the decision rule BEFORE editing: write `<scratchpad>/dante-sim.mjs` (ESM; on Windows import the engine via `file:///C:/projects/mazeworld/...` URLs) implementing the sim in `<dante_options>` for the canon bestiary, printing `died %` for forced "Humans" and for "Demons" (the reference). Then apply the change and run the sim again — the Humans figure must be <= the Demons figure. Keep the script in the scratchpad (never commit it); quote both readings in the ledger in Task 4.
    2. `content/bestiary.js` (CRLF file — Edit tool): replace the Humans tier-1 array `[{ n: "Dante", ... }]` with `[{ n: "Ned", sz: "H", i: 8, wp: 8, sp: { note: "a bandit: one knife, one grudge, no plan" } }]`; append the Dante row — byte-identical text `{ n: "Dante", sz: "H", i: 12, wp: 20, sp: { atk: 3, note: "twins, four arms: three strikes a round" } }` — as the LAST entry of Humans tier 2 (after Krupke; array order within a tier is load-bearing for `rng.pick`, so append, never insert before an existing row). Directly above the Humans block add the block comment `// DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06): Dante demoted from tier 1 to tier 2 (stats and note unchanged: sz H, i 12, wp 20, atk 3); tier-1 Humans is now Ned (sz H, i 8, wp 8, one swing a round). Why: "cut down by a Dante" was the #1 death cause at scale (724 of 5,720 Phase 26 runs) — three strikes a round and 20 wp against level-1 characters on floor 1; floor-1 Humans death rate <before> % -> <after> % (attack-only sim, 2,000 seeds). The seed-303 parley parity scenario now rolls Ned and carries a declared action-path divergence (test/parity/FIXTURE-INVENTORY.md, Phase 27 section). Ledger: docs/DIFFICULTY-RETUNE.md, "## v1.2 retune (Phase 27)".` Update the header comment's Phase 18 sentence: after "...must stay so without a named comparables.js carve-out (BEST-03/FID-05)" add "— superseded for Dante by Phase 27 (TUNE-06): Dante moved to tier 2, the tier-1 Humans row is Ned, and the seed-303 scenario carries a declared action-path divergence record instead of a comparables.js carve-out; the other three rows are still untouched".
    Do NOT touch the `const BESTIARY = {` copy inside mazeworld.html (line ~1912) — it belongs to the dead prototype `startCombat` cluster the live app no longer calls (the engine's content/bestiary.js is the only live roster; Phase 18's rebalance left the HTML copy alone for the same reason).
    3. Tests — run each file, read the failure, confirm the measured value is what the bestiary change implies, then write it with a comment `// Phase 27 (2026-09-15, TUNE-06): was <old> — Dante moved to tier 2, Ned is the tier-1 Humans row (re-measured live, never hand-computed)`:
       - `content-tables.test.js`: both fixture-row tests → `BESTIARY["Humans"][0]` deep-equals the Ned row (rename the tests' "four fixture-exposed rows" wording to say Dante is now tier 2 and Ned is the exposed row); tier lengths Humans `[1, 3, 2, 2, 1]`; `flat(2).length` 53 → 54 in both places; "45 of 53" → 46 of 54 (`withoutAbilities.length` 46; `withAbilities` stays 8); add one assertion that `BESTIARY["Humans"][1].at(-1)` deep-equals the Dante row (recognisably Dante deeper).
       - `bestiary-yardstick.test.js`: `rows.length` 53 → 54; the tier-1 fixture filter list → `["Bat/Rat", "Shriek", "Viper", "Ned"]` with `wpByName.Ned === 8`; add `assert.equal(result.rows.find((r) => r.name === "Dante" && r.tier === 2).wp, 20)`; the tier median pins are expected UNCHANGED (T1 3.51 / 30.89, T2 3.31 / 10.87) — if any median assertion fails, STOP and report (the replacement's numbers are not what this plan specifies).
       - `foe-turn-draw-count.test.js`: `FULL_FIGHTS` seed 303 → `foeNames: ["Ned", "Ned"]` and the measured `totalDraws` / `attacks` / `outcome` (run `runFullFight(303, "Humans")` via a one-off `node -e` or read the assertion diff); `FIXTURE_NAMES` → `["Bat/Rat", "Shriek", "Viper", "Ned"]`; update the restated test's title numbers (12/87/101/<new>/32/119).
       - `parley.test.js` Test 14: `foes.every((f) => f.name === "Ned")`; re-measure the dice log and need/sp/gold with the test's own `countingRng` (print, then pin); keep the test's intent sentence and add the Phase 27 comment. Line ~273 (Test 4) and the D-19 old-save probe only rely on seed 303 rolling a Humans encounter — they should pass unchanged.
       - `test/determinism/foe-abilities.test.js`: run it; test 1 will report the re-derived first Krupke seed for `humans-t2` (expected 3) — set the spec's `seed` to that value, then re-measure `FULL_FIGHT_PINS["humans-t2"]` (foeNames, totalDraws, attacks, outcome) and `PER_VISIT_PINS["humans-t2"]` with the file's own `runFullFight` / per-visit helpers (print, then pin; Phase 27 comment naming the old `seed: 1` / `59 / 4 / won` values and the cause: Dante appended to Humans tier 2 changes the tier-2 pick). The other four specs' pins must be byte-unchanged in THIS task — if any of them moves, STOP and report.
       - `fixture-inventory.test.js`: parley/303 expected foes → `{ name: "Ned", type: "Humans", lvl: 1, wp: 8 }` x2 (confirm with `node tools/fixture-inventory.mjs --json`); FID-01 names → `["Beasts:Bat/Rat", "Beasts:Shriek", "Beasts:Viper", "Humans:Ned"]`; doc-mention loop → `["Bat/Rat", "Shriek", "Viper", "Ned", "Dante"]` (Task 2 writes the section that mentions both). This test also compares the generated roster block in FIXTURE-INVENTORY.md — it stays RED until Task 2 regenerates the block; that is expected, note it in the commit message.
    4. `content/BESTIARY-REBALANCE.md`: append under `## Change ledger` a short `### Phase 27 addendum (TUNE-06, 2026-09-15)` — Dante T1 → T2 unchanged, Ned added at T1 (row, yardstick numbers from `computeYardstick` for Ned@T1 and Dante@T2 in prototype mode), the fixture-exposed set is now Bat/Rat, Shriek, Viper, Ned; "the 53-row tables above are the Phase 18 record and are not regenerated"; pointer to docs/DIFFICULTY-RETUNE.md.
    5. Run `node --test test/voice/safety-scan.test.js` (the name Ned is scanned) and `node --test test/unit/content-tables.test.js test/unit/bestiary-yardstick.test.js test/unit/foe-turn-draw-count.test.js test/unit/parley.test.js test/determinism/foe-abilities.test.js`. Commit: `feat(27-02): Dante demoted to tier 2, Ned is tier-1 Humans — DELIBERATE RULES CHANGE (TUNE-06); pins re-measured (fixture-inventory doc block regenerates in the next commit)`.
  </action>
  <verify>
    <automated>node --test test/unit/content-tables.test.js test/unit/bestiary-yardstick.test.js test/unit/foe-turn-draw-count.test.js test/unit/parley.test.js test/determinism/foe-abilities.test.js test/voice/safety-scan.test.js && node -e "import('./content/bestiary.js').then(({BESTIARY})=>{const t1=BESTIARY.Humans[0],t2=BESTIARY.Humans[1];if(t1.length!==1||t1[0].n!=='Ned'||t1[0].wp!==8)throw new Error('tier1');const d=t2.at(-1);if(d.n!=='Dante'||d.wp!==20||d.sp.atk!==3||d.sp.note!=='twins, four arms: three strikes a round')throw new Error('dante');if(Object.values(BESTIARY).flat(2).length!==54)throw new Error('rows');console.log('bestiary ok')})"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `bestiary ok` and every listed test file passes.
    - The scratch sim's Humans death rate for the landed bestiary is <= its Demons death rate (both numbers quoted in the SUMMARY and, in Task 4, the ledger).
    - `git diff --quiet HEAD~1 -- engine src mazeworld.html tools test/parity/prototype-master.js.txt test/parity/harness` exits 0 (only content/ and the listed unit/determinism tests changed).
  </acceptance_criteria>
  <done>Dante is a tier-2 creature with byte-identical stats and note, Ned holds tier 1, the decision rule is met by measurement, and every unit/determinism pin that named the old row or the old tier-2 pick is re-measured and green; the parity-doc test is the only red left, by design, for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the seed-303 action-path divergence, regenerate the FIXTURE-INVENTORY roster block, write the Phase 27 section (Dante record, parity-clean levers, pre-enumerated escalation records), prove parity 33/33 with no harness or master edit</name>
  <files>test/parity/fixtures/action-script.combat.json, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/fixtures/action-script.combat.json: the `lose` scenario's existing `divergence` record (seed 14) — copy its shape exactly (phase, requirements, kind, fromAction, fields, before, after, stateFields, stateBefore, stateAfter, rationale)
    - test/parity/fixtures/action-script.schema.md, the `kind: "action-path"` paragraph (fields are read from `state.c`, taken verbatim from both sides, never hand-typed) and the Phase 23 field-strip `divergence`/`divergences` paragraphs
    - test/parity/combat-parity.test.js lines 139-235 (how `pathDiv` skips the byte diff from `fromAction` and how `declaredEndDiffs` is asserted; `loadPrototypeSandbox({ seed })`, `ctx.startCombat(wandering, forced)`, `ctx.parley()`, `applyStartCombat`, `applyAction`) — your scratch replay mirrors this loop
    - test/parity/FIXTURE-INVENTORY.md: the `## Phase 24 identity-pass divergences` section (the measured per-action table format, "Byte-identical elsewhere", the quoted FID-07 gate check) and the generated roster block markers; tools/fixture-inventory.mjs header (regenerate: run with no flags, paste stdout verbatim between the markers)
    - test/parity/full-suite.test.js: the movement sub-test (no divergence wiring today), the economy block (action-path wiring on the fixture top level) and the encounters loop (no divergence wiring today) — you DESCRIBE, in the inventory, the wiring 27-03 would add for the escalation records; you do not add it here
    - The `<early_floor_levers>` table in this plan (blast radius per lever)
  </read_first>
  <action>
    1. Measure both sides: write `<scratchpad>/seed303-replay.mjs` that imports `loadPrototypeSandbox` from `test/parity/harness/sandboxPrototype.js` and `newRun`/`applyAction` + `startCombat`/`makeRng` exactly as combat-parity.test.js does, replays the `parley` scenario (seed 303: action 0 `startCombat` wandering false forced "Humans"; action 1 `parley`) on the prototype sandbox (`ctx.S`) and the engine, and after EACH action prints, for both sides: the rolled foe names/wp, `c.wp`, `c.sp`, `c.gold`, `c.kills`, `c.rations`, `dead`, and whether `combat` is null. Record the printed table verbatim (it goes into FIXTURE-INVENTORY.md). Expected shape: the prototype still rolls Dante x2 and parleys as before; the engine rolls Ned x2 and its parley draws shift (Ned's opener swings differ from Dante's) — whether the engine's parley succeeds or fails is MEASURED, never assumed.
    2. Add to the `parley` scenario object in `action-script.combat.json` (2-space JSON, keep key order consistent with the `lose` record): `"divergence": { "phase": "27", "requirements": ["TUNE-06"], "kind": "action-path", "fromAction": 0, "fields": ["wp", "sp", "gold", "kills", "rations"], "before": { ...prototype end values... }, "after": { ...engine end values... }, "stateFields": ["dead"], "stateBefore": { "dead": <measured> }, "stateAfter": { "dead": <measured> }, "rationale": "Phase 27 (TUNE-06): Dante was demoted to tier 2 and the tier-1 Humans row is now Ned (wp 8, one swing a round) — the first deliberate canon deviation of the v1.2 retune (docs/DIFFICULTY-RETUNE.md, v1.2 section). From action 0 (startCombat) the engine rolls Ned x2 where the frozen prototype rolls Dante x2, so the foe objects and every downstream draw differ; the parley outcome on each side is recorded here as measured. No other scenario in any fixture rolls a Humans encounter, so this is the only divergence the Dante change causes." }` — `fromAction` is 0 because the foe objects differ inside the startCombat action itself; the pre-action boot state stays byte-identical and is still asserted.
    3. Run `node --test test/parity/combat-parity.test.js test/parity/full-suite.test.js` — both green with the record and with ZERO changes to comparables.js. If either fails on `declaredEndDiffs`, the record's numbers are wrong: re-measure, never adjust to make it pass. If any OTHER scenario diverges, STOP — the Dante form must be made smaller (return to Task 1), never a second record.
    4. Regenerate the roster block: `node tools/fixture-inventory.mjs` → paste stdout verbatim between `<!-- fixture-inventory:generated:begin -->` / `:end -->` in FIXTURE-INVENTORY.md (only the parley row changes: `Ned (Humans lvl 1, wp 8); Ned (Humans lvl 1, wp 8)`). Next to the Phase 17/18 prose that calls Dante "the only level-1 Humans entry" (the `## Parity-exposed bestiary surface` paragraph and the FID-02 `combat/parley | 303 | Humans | Dante x2 | 6 | 66 | won` row) add a parenthetical "(Phase 27: Dante moved to tier 2 — see the Phase 27 section; the row is kept as the Phase 17 measurement)". Append a new section `## Phase 27 early-floor divergences (TUNE-06)` at the END of the file with: (a) one paragraph on the widened levers (27-CONTEXT lever 3) and the rule that every lever LANDED by 27-02 is parity-clean by construction (foe grace only at depths 2-4 — no fixture fights past floor 1; hazard ramp from floor 2 — no fixture springs a trap or climbs/leaps past floor 1; darkness hold — floor 2 keeps its single canon blob; dots restructured behind `DENSITY_CANON_THROUGH_DEPTH` = 2), so the seed-303 record is the phase's only divergence unless 27-03 escalates; (b) `### Combat: parley scenario, seed 303 (Dante -> Ned)` with the measured per-action table (prototype vs engine columns, the same layout as the Phase 24 seed-14 table), the record's before/after as declared, and `#### Draw-count pin re-measured` (the FULL_FIGHTS 303 row: 66/6/won → the new totals; the determinism `humans-t2` seed 1 → 3); (c) `### Escalation records (declared only if 27-03 turns the lever)` — one sub-heading per escalation with the EXACT record shape and the test-file wiring it needs, taken from this plan's `<early_floor_levers>` table: `#### Trap/wall-fall ramp from floor 1` (encounters `trap` seed 1: Knight, Darts roll 4, dmg 3 → 2, `c.wp` 55 → 56 — a Phase 23 field-strip record `{ phase, requirements, fields: ["wp"], before: { wp: 55 }, after: { wp: 56 }, rationale }` on the scenario; wiring: full-suite.test.js's encounters loop selects `stripScenarioDivergence(economyComparable(s), scenario.divergence)` when a record is present and asserts the declared before/after with `diffState` before stripping, mirroring magic-parity.test.js), `#### Darkness from floor 4` (movement seed 256: floor 2 loses its blob → one fewer `rng.pick`, a different floor-2 grid from the descend action on; an action-path record at the fixture's top level `{ kind: "action-path", fromAction: <the descend action index, measured — the action whose engine events include floorChanged>, fields: [...measured c fields], stateFields: ["day", "steps", "dead"], ... }`; wiring: movement-parity.test.js and full-suite's movement sub-test add `actionPathDivergenceOf(FIXTURE)` / `skipsByteDiffAt` / `declaredEndDiffs` exactly as combat-parity.test.js does, and gate the hard-coded `floor.depth 2 / day 2 / steps 100` post-assertions on `!pathDiv`), `#### Starting rations +N` (chargen fixture: 14 per-seed `divergences` entries `{ phase: "27", requirements: ["TUNE-06"], fields: ["rations"], before: { rations: <canon> }, after: { rations: <canon + N> }, rationale }` — seeds 15 and 24 MERGE with their existing grimoire records (`fields: ["grimoire", "rations"]`); combat `lose` seed 14 `after.rations` 8 → 8+N and magic `cast-damage` seed 8 `after.rations` 5 → 5+N re-measured; the other comparables already strip `c.rations` via `stripRationsField`), `#### Floor-1 foe grace (last resort)` (combat win 3 / flee 17 / lose-apprentice 127 gain action-path records from action 0; lose 14 and parley 303 records re-measured; magic cast-damage 8's field-strip record re-measured or converted to action-path if its path flips; FID-02 FULL_FIGHTS 5 rows and the generated roster's wp values re-measured; a ledger note that the fidelity contract now carries broad early-floor exceptions); (d) `### Byte-identical elsewhere` (every other scenario unchanged; the movement fixture's floor-2 descend unaffected — floors 1-2 stay canon by construction in Task 3; parity 33/33), and the quoted gate check output of `git diff --stat 4e4b3e7..HEAD -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js` (must print nothing; 4e4b3e7 = the Phase 27 start commit).
    5. `node --test "test/parity/**/*.test.js"` → 33/33 (the fixture-inventory doc test is green again). Commit: `test(27-02): seed-303 parley action-path divergence declared (Dante -> Ned) — FIXTURE-INVENTORY Phase 27 section with the escalation records pre-enumerated, roster regenerated, parity 33/33`.
  </action>
  <verify>
    <automated>node --test "test/parity/**/*.test.js" && git diff --quiet 4e4b3e7 -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js && node -e "const f=require('./test/parity/fixtures/action-script.combat.json');const p=f.scenarios.find(s=>s.name==='parley');if(!p.divergence||p.divergence.kind!=='action-path'||p.divergence.fromAction!==0||!p.divergence.fields.length)throw new Error('record');const n=f.scenarios.filter(s=>s.divergence).length;if(n!==2)throw new Error('expected exactly 2 records (lose + parley), got '+n);const d=require('fs').readFileSync('test/parity/FIXTURE-INVENTORY.md','utf8');for(const h of ['## Phase 27 early-floor divergences (TUNE-06)','#### Trap/wall-fall ramp from floor 1','#### Darkness from floor 4','#### Starting rations +N','#### Floor-1 foe grace (last resort)'])if(!d.includes(h))throw new Error('missing: '+h);console.log('record ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "test/parity/**/*.test.js"` reports 33 passing files / 0 failing.
    - Exactly two combat scenarios carry a `divergence` record (`lose` from Phase 24 and `parley` from this plan); no fixture file other than action-script.combat.json changed; prototype-master.js.txt and comparables.js are byte-identical to 4e4b3e7.
    - `node tools/fixture-inventory.mjs` stdout equals the generated block in FIXTURE-INVENTORY.md (the fixture-inventory test asserts it).
    - FIXTURE-INVENTORY.md ends with the `## Phase 27 early-floor divergences (TUNE-06)` H2 containing the measured seed-303 table and the four pre-enumerated escalation record sub-headings.
  </acceptance_criteria>
  <done>The one divergence is declared on the scenario, measured on both sides, machine-checked at both replay sites through the existing helpers, documented in the inventory with the gate output and the exact records/wiring any 27-03 escalation would need; every other parity scenario is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Land the early-floor lever fields (foe grace 2-4, hazard ramp from floor 2, darkness hold) and the shallow density easing behind a floors-1-2 canon gate; consumers; restructured tests (PHASE_27_PINS, split PARITY GUARD, re-measured pins); parity 33/33</name>
  <files>engine/difficulty.js, engine/combat.js, engine/movement.js, engine/encounters.js, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/unit/maze.test.js, test/unit/encounters.test.js, test/unit/movement.test.js, test/determinism/foe-abilities.test.js</files>
  <read_first>
    - engine/difficulty.js in full: the constants, `softCap` (rounding) vs `softCapFloat`, `difficultyCurve` (the `over` gate for combat; the dots/darkBlobs/darkRadius expressions), `foeWpFor`, `foeDmgBonusFor`, the Phase 21 DELIBERATE RULES CHANGE block
    - engine/combat.js startCombat lines 155-215: `foeWpFor`/`foeDmgBonusFor` use and the `...(dmgBonus > 0 ? { dmgBonus } : {})` copy; the three melee sites `lvl * lvl + (f.dmgBonus || 0) + dice` (lines ~658, ~1691, ~1736 — read-only, unchanged)
    - engine/movement.js lines 136-210 (the climb/gorge branch: `hurt` accumulates from canon dice, the Climbing-skill halving, then `state.c.wp -= hurt`) and its existing `difficultyCurve` import (line 29); engine/encounters.js `springTrap` (lines 63-96: `dmg` from `rollDice`, `times`, Cat Burglar doubling, Hardiness `Math.max(1, dmg - 3)`, then `c.wp -= dmg`) — encounters.js does NOT import difficulty.js yet
    - engine/maze.js lines 105-150: `nDots = dc.dots` drives the dot-placement loop and `dc.darkBlobs` is the number of `rng.pick(open)` draws — floors >= 3 will draw differently (allowed: no fixture reaches floor 3) and floors 1-2 must not
    - test/difficulty/difficulty.test.js in full; test/difficulty/fairness.test.js (property tests — untouched)
    - test/unit/combat-scaling.test.js: "D-19 identity: difficultyCurve(1..5)", "21-04 retune values", "depth-6 first divergence", "cap boundaries", "monotone", "the caps are reached", "synthetic-curve helper pins" (foeWpFor/foeDmgBonusFor), the draw-shape test at depth 1/5/30, `fixedState`/`fixedFoe` helpers
    - test/unit/maze.test.js "feature counts match the prototype's formula for seed 42, depths 1-5"; test/unit/encounters.test.js springTrap tests (fixedState depth 1 — must stay unchanged; add depth-2 siblings); test/unit/movement.test.js climb/gorge fall tests (depth 1 unchanged; add depth-2 siblings)
    - test/determinism/foe-abilities.test.js `humans-t2` (depth 2) and `magical-t4` (depth 4) pins — the grace changes foe wp at those depths (re-measure; tier-5 specs stay byte-identical)
    - The `<early_floor_levers>` table in this plan
  </read_first>
  <action>
    A. Restructure first, value-neutral (prove it with the UNTOUCHED tests before moving anything):
       - `DENSITY_CANON_THROUGH_DEPTH = 2` + the offset dots form: `shallowDots = ENCOUNTER_DOT_BASE + Math.min(d, DENSITY_CANON_THROUGH_DEPTH)`, `dOver = Math.max(0, d - DENSITY_CANON_THROUGH_DEPTH)`, `dots: breather ? ENCOUNTER_DOT_BASE : shallowDots + Math.round(softCapFloat(0, ENCOUNTER_DOT_CAP - (ENCOUNTER_DOT_BASE + DENSITY_CANON_THROUGH_DEPTH), dOver, ENCOUNTER_DOT_SOFT_K))` (10/11/12/13/14 on floors 1-5 at the current constants; delete `softCap` if unreferenced and fix the JSDoc that cites it).
       - `DARK_HOLD_THROUGH_DEPTH = 2` (identity value — canon growth) and the hold form: `darkBlobs: breather ? 0 : (d <= DARK_HOLD_THROUGH_DEPTH ? Math.min(Math.max(0, d - 1), 1) : Math.min(d - DARK_HOLD_THROUGH_DEPTH + 1, DARK_BLOB_CAP))` — at 2 this is exactly the canon `min(d - 1, cap)`.
       - `FOE_GRACE_AT_1 = 1.0`, `FOE_GRACE_AT_2 = 1.0` (identity), `FOE_GRACE_CANON_FROM_DEPTH = 5`, and `graceFor(d)` (not exported): `d === 1 ? FOE_GRACE_AT_1 : d >= FOE_GRACE_CANON_FROM_DEPTH ? 1 : FOE_GRACE_AT_2 + (1 - FOE_GRACE_AT_2) * (d - 2) / (FOE_GRACE_CANON_FROM_DEPTH - 2)`; `foePower: d < COMBAT_SCALE_FROM_DEPTH ? graceFor(d) : softCapFloat(FOE_POWER_BASE, FOE_POWER_MAX, over, FOE_POWER_SOFT_K)` — the `>=` guard returns the literal 1 (no float drift) from FOE_GRACE_CANON_FROM_DEPTH through COMBAT_SCALE_FROM_DEPTH - 1.
       - `HAZARD_FROM_DEPTH = 2`, `HAZARD_SCALE_AT_START = 1.0` (identity), `HAZARD_FLAT_THROUGH_DEPTH = 3`, `HAZARD_CANON_FROM_DEPTH = 5`, and the new curve field `hazardScale: d < HAZARD_FROM_DEPTH || d >= HAZARD_CANON_FROM_DEPTH ? 1 : d <= HAZARD_FLAT_THROUGH_DEPTH ? HAZARD_SCALE_AT_START : HAZARD_SCALE_AT_START + (1 - HAZARD_SCALE_AT_START) * (d - HAZARD_FLAT_THROUGH_DEPTH) / (HAZARD_CANON_FROM_DEPTH - HAZARD_FLAT_THROUGH_DEPTH)` (literal 1 outside the ramp). Export a pure helper `scaleHazard(amount, curve)` = `curve.hazardScale === 1 || amount <= 0 ? amount : Math.max(1, Math.round(amount * curve.hazardScale))`.
       - `foeWpFor`: `Math.max(1, Math.round(baseWp * curve.foePower))` on the non-identity path (a wp-1 foe never becomes 0). `foeDmgBonusFor` unchanged (negative allowed; update its JSDoc). `engine/combat.js` startCombat: `...(dmgBonus !== 0 ? { dmgBonus } : {})` (depth 1 still never carries the key; the comment gains a Phase 27 sentence).
       - Consumers: `engine/movement.js` — in the `if (!ok) {` fall branch replace `state.c.wp -= hurt;` with `hurt = scaleHazard(hurt, difficultyCurve(state.floor.depth)); state.c.wp -= hurt;` (import `scaleHazard` from ./difficulty.js next to the existing `difficultyCurve` import; the `fellClimbing`/`fellInGorge` event then reports the scaled `hurt`); `engine/encounters.js` — add `import { difficultyCurve, scaleHazard } from "./difficulty.js";` and in `springTrap`, immediately before `c.wp -= dmg;`, `dmg = scaleHazard(dmg, difficultyCurve(state.floor.depth));` (after Hardiness; the `trapSprung` event reports the scaled dmg). Each site gets a one-line comment `Phase 27 (TUNE-06): hazardScale — post-draw arithmetic, 0 new draws, identity outside floors HAZARD_FROM_DEPTH..HAZARD_CANON_FROM_DEPTH-1`.
       - Run `node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js test/unit/encounters.test.js test/unit/movement.test.js test/determinism` and `node --test "test/parity/**/*.test.js"` — ALL GREEN with no test edit: the restructure is identity everywhere at these constants.
    B. Move the dials to the landed set (the parity-clean set from the calibration): `ENCOUNTER_DOT_CAP` 24 → 15, `DARK_BLOB_CAP` 6 → 3, `DARK_RADIUS_CAP` 9 → 7, `DARK_HOLD_THROUGH_DEPTH` 2 → 3, `FOE_GRACE_AT_2` 1.0 → 0.75, `HAZARD_SCALE_AT_START` 1.0 → 0.5 (`FOE_GRACE_AT_1` stays 1.0; `HAZARD_FROM_DEPTH` stays 2; the deep combat dials stay at the 21-04 values). Print `difficultyCurve(d)` for d = 1..8, 10, 15, 20, 35, 50 and confirm: floor 1 = dots 10 / blobs 0 / radius 4 / foePower exactly 1 / hazardScale exactly 1; floor 2 = dots 11 / blobs 1 / radius 5 / foePower 0.75 / hazardScale 0.5; floors 3-5 = dots 11 / 12 / 12, blobs 1 / 2 / 3, radius 6 / 7 / 7, foePower 0.8333 / 0.9167 / 1 (exactly 1 at 5), hazardScale 0.5 / 0.75 / 1 (exactly 1 at 5); deep: dots 13 at 8-10, 14 at 15-20, 15 from ~30; blobs 3; radius 7. Add ONE `DELIBERATE RULES CHANGE (Phase 27, TUNE-06) — early-floor levers and non-combat knobs` block above the constants: the widened levers (27-CONTEXT lever 3), why (the bot kill table), floors 1-2 canon by construction (`DENSITY_CANON_THROUGH_DEPTH`; darkness hold keeps floor 2's single blob; grace and hazard start at 2), which constants are escalation-only (`FOE_GRACE_AT_1`, `HAZARD_FROM_DEPTH` → 1, `DARK_HOLD_THROUGH_DEPTH` → a from-4 form) and that each escalation carries a declared divergence record (FIXTURE-INVENTORY.md Phase 27 section); values recorded in docs/DIFFICULTY-RETUNE.md v1.2 (iteration 0; 27-03 turns them). Update the file header's Phase 21 paragraph with one Phase 27 sentence.
    C. Tests — measure, then pin (every changed pin gets a `Phase 27 (2026-09-15, TUNE-06): was <old>` comment):
       - `test/difficulty/difficulty.test.js`: import the new constants; "named constants" → the landed values (`ENCOUNTER_DOT_CAP` 15, `DARK_BLOB_CAP` 3, `DARK_RADIUS_CAP` 7, `DENSITY_CANON_THROUGH_DEPTH` 2, `DARK_HOLD_THROUGH_DEPTH` 3); split the PARITY GUARD into (i) `PARITY GUARD: depths 1-2 reproduce the prototype's exact 9+depth/depth-1/3+depth formula (fixture-exposed: every fixture is floor 1; movement seed 256 descends to floor 2)` looping 1..2, and (ii) `Phase 27 deliberate easing (TUNE-06): depths 3-5 are pinned to the retune values` with the literal triples printed in B; keep the guard's "an intentional retune signal" comment and note Phase 27 is that signal; add `hazardScale is exactly 1 at depths 1, 5, 6, 20 and never below HAZARD_SCALE_AT_START`. The caps / breather / tolerance tests stay; the "non-decreasing dots/darkBlobs/darkRadius" test stays (the hold form is monotone).
       - `test/unit/combat-scaling.test.js`: (i) add `const PHASE_27_PINS = { COMBAT_SCALE_FROM_DEPTH: 6, FOE_CAP_BASE: 3, FOE_CAP_MAX: 5, FOE_CAP_SOFT_K: 20, FOE_POWER_BASE: 1, FOE_POWER_MAX: 1.6, FOE_POWER_SOFT_K: 35, ABILITY_THREAT_BASE: 1, ABILITY_THREAT_MAX: 2.0, ABILITY_THREAT_SOFT_K: 30, FOE_LVL_BIAS: 0, FOE_GRACE_AT_1: 1, FOE_GRACE_AT_2: 0.75, FOE_GRACE_CANON_FROM_DEPTH: 5, HAZARD_FROM_DEPTH: 2, HAZARD_SCALE_AT_START: 0.5, HAZARD_FLAT_THROUGH_DEPTH: 3, HAZARD_CANON_FROM_DEPTH: 5 };` with a comment "the ONE place the Phase 27 dials are pinned — 27-03 edits only these numbers per iteration; values recorded in docs/DIFFICULTY-RETUNE.md v1.2 change table", and replace the "21-04 retune values" test with `Phase 27 retune pins match engine/difficulty.js` looping the object against the exports; (ii) `NON_COMBAT_PINS` for depths 1-5 (dots/darkBlobs/darkRadius from B) asserted by the identity test; (iii) rewrite "D-19 identity" as three tests: `floor 1 is exactly canon` (`Object.is(foePower, 1)`, `Object.is(hazardScale, 1)`, foeCap 3, foeBonus 0, abilityThreat 1, dots 10, blobs 0, radius 4 — unless `FOE_GRACE_AT_1 !== 1`, in which case the test asserts the declared floor-1 value and the SUMMARY says the last resort was used), `grace band: depths 2..FOE_GRACE_CANON_FROM_DEPTH-1 carry foePower = FOE_GRACE_AT_2 + (1 - FOE_GRACE_AT_2) * (d - 2) / (canon - 2) (closeTo 1e-12), never below FOE_GRACE_AT_2, and hazardScale per the flat-then-linear formula`, `identity band: depths FOE_GRACE_CANON_FROM_DEPTH..COMBAT_SCALE_FROM_DEPTH-1 carry foePower exactly 1, abilityThreat exactly 1, foeCap FOE_CAP_BASE, hazardScale exactly 1`; (iv) "monotone" → foeCap/abilityThreat non-decreasing from depth 1; foePower non-decreasing from depth 2 (floor 1 is the canon anchor the grace dips under — say so in the test name); (v) "depth-6 first divergence" parametrised on `COMBAT_SCALE_FROM_DEPTH`; (vi) new helper pins: `foeWpFor(1, { foePower: 0.75 }) === 1`, `foeWpFor(16, { foePower: 0.75 }) === 12`, `foeDmgBonusFor(2, { foePower: 0.75 }) === -1`, `foeDmgBonusFor(1, { foePower: 0.75 }) === 0`, `scaleHazard(3, { hazardScale: 0.5 }) === 2`, `scaleHazard(1, { hazardScale: 0.5 }) === 1`, `scaleHazard(0, { hazardScale: 0.5 }) === 0`, `scaleHazard(7, { hazardScale: 1 }) === 7`; (vii) `startCombat at depth 2 / level 2 copies a negative dmgBonus key on a lvl-2 foe and none on a lvl-1 foe; at depth 1 no foe carries the key` (fixedState + fakeRng; measure the exact roster first). The draw-shape (depth 1/5/30), cap-boundaries and caps-reached tests stay.
       - `test/unit/encounters.test.js`: the existing springTrap tests run at fixedState depth 1 and must pass UNCHANGED; add one sibling at `floor: { depth: 2 }` with the same fakeRng asserting `dmg` is `Math.max(1, Math.round(canon * 0.5))` and the `trapSprung` event reports it, and one at depth 5 asserting canon.
       - `test/unit/movement.test.js`: the existing climb/gorge fall tests (depth 1) pass UNCHANGED; add a depth-2 sibling for one climb fall and one gorge fall asserting the halved `hurt` (and the event value), and a depth-5 canon sibling.
       - `test/unit/maze.test.js` seed-42 feature counts: run it, read the actual `dot` counts for depths 3-5, confirm each equals `difficultyCurve(d).dots`, pin them (tele/chest/trap/climb/gorge/one/exit must be unchanged).
       - `test/determinism/foe-abilities.test.js`: `humans-t2` (depth 2 → foePower 0.75: Krupke wp 17 → 13) and `magical-t4` (depth 4 → 0.9167: Drudge wp scaled) FULL_FIGHT/PER_VISIT pins re-measured with the file's helpers and a comment naming the grace; the three tier-5 specs must be byte-unchanged (exactly 1.0 at 5) — if one moves, STOP.
       - Run `node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js test/unit/encounters.test.js test/unit/movement.test.js test/determinism` then `node --test "test/parity/**/*.test.js"` (33/33 — floor 1 and floor 2 untouched) then `npm test` (green).
    D. Commit: `feat(27-02): early-floor levers — foe grace 2-4 (0.75), hazard ramp from floor 2 (0.5), darkness hold through 3, dots cap 15 / dark 3/7 behind DENSITY_CANON_THROUGH_DEPTH; PHASE_27_PINS test restructure`.
  </action>
  <verify>
    <automated>node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js test/unit/encounters.test.js test/unit/movement.test.js test/determinism && node --test "test/parity/**/*.test.js" && node -e "import('./engine/difficulty.js').then(m=>{const c1=m.difficultyCurve(1),c2=m.difficultyCurve(2),c5=m.difficultyCurve(5);if(!(c1.dots===10&&c1.darkBlobs===0&&c1.darkRadius===4&&Object.is(c1.foePower,1)&&Object.is(c1.hazardScale,1)))throw new Error('floor 1');if(!(c2.dots===11&&c2.darkBlobs===1&&c2.darkRadius===5&&c2.foePower===0.75&&c2.hazardScale===0.5))throw new Error('floor 2');if(!(Object.is(c5.foePower,1)&&Object.is(c5.hazardScale,1)&&c5.darkBlobs===3))throw new Error('floor 5');if(!(m.DENSITY_CANON_THROUGH_DEPTH===2&&m.ENCOUNTER_DOT_CAP===15&&m.DARK_BLOB_CAP===3&&m.DARK_RADIUS_CAP===7&&m.DARK_HOLD_THROUGH_DEPTH===3&&m.FOE_GRACE_AT_1===1&&m.HAZARD_FROM_DEPTH===2&&m.COMBAT_SCALE_FROM_DEPTH===6&&m.FOE_POWER_MAX===1.6))throw new Error('dials');if(m.scaleHazard(3,{hazardScale:0.5})!==2||m.scaleHazard(0,{hazardScale:0.5})!==0||m.foeWpFor(1,{foePower:0.75})!==1)throw new Error('helpers');console.log('curve ok')})" && npm test 2>&1 | tail -8 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - The verify chain prints `curve ok`, parity is 33/33, and `npm test` ends `# fail 0`.
    - `difficultyCurve(1)` deep-equals its pre-plan object plus `hazardScale: 1`; `difficultyCurve(2)` keeps dots 11 / darkBlobs 1 / darkRadius 5; depths 3-5 carry the pinned values; the deep combat constants still equal the 21-04 values; `FOE_GRACE_AT_1` is 1 and `HAZARD_FROM_DEPTH` is 2.
    - `grep -c "dmgBonus !== 0" engine/combat.js` prints 1; `engine/movement.js` and `engine/encounters.js` each call `scaleHazard` exactly once (`grep -c "scaleHazard(" <file>` prints 2 in each — the import line and the call).
    - `test/unit/combat-scaling.test.js` contains `PHASE_27_PINS` and no test named "21-04 retune values" remains; `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0; `git diff --quiet 4e4b3e7 -- engine/character.js content/races.js` exits 0.
  </acceptance_criteria>
  <done>Every widened lever has its own draw-free field and consumer, floors 1-2 are canon by construction, the parity-clean set is landed and pinned in one object, the deep dials are untouched, no fixture beyond seed 303 diverges, and the tree is green.</done>
</task>

<task type="auto">
  <name>Task 4: Iteration-0 smoke and the ledger entries (Dante landed, iteration 0 with the per-lever calibration table and the escalation order)</name>
  <files>docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md `## v1.2 retune (Phase 27)`: `### Levers and the Dante decision`, `### Iteration protocol` (incl. the planner calibration note), the placeholders `### Dante demotion — landed (27-02)` and `### Iteration log`
    - The `<early_floor_levers>` table and `<dante_options>` in this plan; Task 1's sim readings; Task 3's printed curve
    - tools/tune-classes.mjs / tools/tune-difficulty.mjs headers (flags); the `rollups.pooled` readout from 27-01
  </read_first>
  <action>
    1. Smoke (background — Bash `run_in_background`, each command's output redirected to a scratchpad log ending in an `EXIT=<code>` line; poll the logs with bounded checks, at most 10, never a foreground wait): (a) `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --json --out <scratch>/iter0-nat.json` (~2 min), (b) `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --start-depth 20 --json --out <scratch>/iter0-d20.json` (~30 s), (c) `node tools/tune-difficulty.mjs --seeds=200 > <scratch>/iter0-td.txt` (~2 min). Write the ledger prose (step 2) while they run; then read `rollups.pooled` from the two JSONs (`node -e` printing p50Depth, p90Depth, reach5, reach10, reach20, stuck; meanEncountersSurvived, meanFloorsGained, p50FloorsGained, topCauses) and the tune-difficulty text (Death-depth distribution, Reach table, top 5 causes, Bot line).
    2. Ledger (Edit tool, CRLF; the calibration sub-table is the eleven-row table in this plan's `<early_floor_levers>` block — copy it verbatim, it is not re-run here): replace `(filled by plan 27-02)` under `### Dante demotion — landed (27-02)` with: the landed form (C) and the two bestiary rows; the executor's sim readings (canon Humans %, landed Humans %, Demons %); the yardstick rows (`computeYardstick` prototype mode: Ned@T1 and Dante@T2 ttk/rtd/lethality/flag; tier medians unchanged); the parity record summary (fields, before, after, stateBefore/After, fromAction 0) and the pointer to FIXTURE-INVENTORY.md's Phase 27 section; the list of re-measured pins (file: old → new, incl. the determinism `humans-t2` seed and the depth-2/4 grace re-measures). Replace `(filled by plans 27-02 and 27-03)` under `### Iteration log` with `**Iteration 0 (27-02) — Dante demotion + the parity-clean early-floor set; deep combat dials untouched.**` followed by: constants (`ENCOUNTER_DOT_CAP` 24→15, `DARK_BLOB_CAP` 6→3, `DARK_RADIUS_CAP` 9→7, `DARK_HOLD_THROUGH_DEPTH` 3, `DENSITY_CANON_THROUGH_DEPTH` 2, `FOE_GRACE_AT_2` 0.75, `FOE_GRACE_CANON_FROM_DEPTH` 5, `FOE_GRACE_AT_1` 1.0, `HAZARD_FROM_DEPTH` 2, `HAZARD_SCALE_AT_START` 0.5, `HAZARD_FLAT_THROUGH_DEPTH` 3, `HAZARD_CANON_FROM_DEPTH` 5; combat at the 21-04 values); the curve table for d = 1..5, 10, 20, 35, 50 (dots / blobs / radius / foePower / hazardScale / cap / abilityThreat); the smoke readout (pooled natural p50 / p90 / reach5 / reach10 / reach20 / top causes; pooled forced-20 encounters survived / floors gained mean + p50; tune-difficulty p50 / reach table / top 5 causes) with each band measure marked in / out; what moved vs the Phase 26 AFTER (is "cut down by a Dante" gone from the top natural causes? what leads now?); the planner's per-lever calibration table copied verbatim from this plan's `<early_floor_levers>` (the eleven S/T rows) as a `#### Planner calibration (2026-09-15, directional)` sub-table, with its one-paragraph reading (the full ladder tops out near 35 % reach5 / bot median 4; floor-1 grace adds nothing once Dante is gone); what to turn next: "iteration 1 (27-03) confirms this set at smoke scale, then escalates in the user's order only while the smoke median is < 5 — grace deeper (FOE_GRACE_AT_2 0.75 → 0.5) → darkness from 4 (movement record) → hazard from floor 1 (encounters record) → rations +1/+2 (14 chargen records) → floor-1 grace 0.85 (last resort) — and turns the deep ramp (COMBAT_SCALE_FROM_DEPTH 16, FOE_CAP_MAX 4, FOE_POWER_MAX 1.3, ABILITY_THREAT_MAX 1.5) for the forced-20 band in parallel"; then a final line `(iterations 1..N are appended below by plan 27-03)`. Sanity: `node --test test/unit/difficulty-retune-ledger.test.js` stays green.
    3. Commit: `docs(27-02): iteration 0 logged — Dante landed, parity-clean early-floor set, calibration table, escalation order`.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js && node -e "const d=require('fs').readFileSync('docs/DIFFICULTY-RETUNE.md','utf8');for(const s of ['Iteration 0','Planner calibration','FOE_GRACE_AT_2','HAZARD_SCALE_AT_START','DARK_HOLD_THROUGH_DEPTH','iterations 1..N are appended below by plan 27-03'])if(!d.includes(s))throw new Error('missing: '+s);if(d.includes('(filled by plan 27-02)'))throw new Error('27-02 placeholder left');console.log('ledger ok')" && git status --porcelain | grep -c . | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `ledger ok`; the working tree is clean after the commit; `git show --stat --format= HEAD` lists only `docs/DIFFICULTY-RETUNE.md`.
    - The iteration-0 numbers in the ledger equal the `rollups.pooled` values in the scratch JSONs (spot-check p50Depth and reach5 with `node -e`).
  </acceptance_criteria>
  <done>Iteration 0 is measured and logged with the per-lever calibration and the escalation order, the Dante section holds the landed numbers, and 27-03 knows exactly where to append.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bestiary data -> parity fixtures | the tier-1 Humans row is rolled by the seed-303 scenario; any stat change diverges it and must be declared, never re-pinned |
| difficulty constants -> floor generation / combat / hazards | dots/darkBlobs change the NUMBER of draws genFloor makes; foePower/hazardScale change post-draw arithmetic; floors 1-2 are fixture-exposed |
| measured pins -> tests | every re-measured number is printed from the engine before it is written into a test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-07 | Tampering | a second scenario diverges (a lever leaked into floor 1 or floor 2) | high | mitigate | parity 33/33 gate after every constant move; only ONE record allowed in this plan (verify counts exactly 2 incl. Phase 24's); floor-1/2 guard tests; `FOE_GRACE_AT_1 === 1`, `HAZARD_FROM_DEPTH === 2` asserted |
| T-27-08 | Tampering | prototype-master.js.txt or comparables.js edited to make parity pass | critical | mitigate | `git diff --quiet 4e4b3e7 -- prototype-master.js.txt harness/comparables.js` in Task 2's verify; the record is checked by the existing helpers |
| T-27-09 | Tampering | depth-2 dots/darkness changed (movement fixture floor 2) | high | mitigate | DENSITY_CANON_THROUGH_DEPTH gate + the darkness hold form keep floor 2's single blob; PARITY GUARD 1-2 test; parity run after the constants move |
| T-27-10 | Repudiation | pins adjusted to pass instead of measured | high | mitigate | every changed pin carries a `was <old>` comment and the measured value is printed first; yardstick medians and tier-5 determinism specs must be unchanged or the task stops |
| T-27-11 | Tampering | deep combat dials moved early (iteration 0 contaminated) | medium | mitigate | verify asserts COMBAT_SCALE_FROM_DEPTH 6 / FOE_POWER_MAX 1.6 at the end of Task 3 |
| T-27-12 | Denial of Service | a graced foe at 0 wp or a hazard scaled to 0 | medium | mitigate | `foeWpFor` floors at 1; `scaleHazard` floors at 1 for positive amounts and leaves 0 alone; unit pins |
| T-27-13 | Information Disclosure | a new foe name/note outside the family-friendly voice | low | mitigate | voice safety scan runs in Task 1's verify; note text is fixed by this plan |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero deps) |
</threat_model>

<verification>
- `npm test` green; `node --test "test/parity/**/*.test.js"` 33/33 with exactly one new divergence record (parley, seed 303).
- `git diff --quiet 4e4b3e7 -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js engine/character.js` exits 0; `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0.
- `difficultyCurve(1)` canon (+ `hazardScale` 1), `difficultyCurve(2)` keeps its canon non-combat values, floors 2-4 carry the grace/hazard values, floor 5 is exactly identity, deep dials unchanged; `PHASE_27_PINS` exists.
- Ledger: `### Dante demotion — landed (27-02)` filled; `Iteration 0` logged with pooled numbers and the calibration table; guard test green.
</verification>

<success_criteria>
- The Dante deviation and every widened lever's seam are landed with the parity-clean values, measured against the recorded rules, declared where they diverge, and documented; 27-03 can escalate in the user's order by editing `engine/difficulty.js` constants + `PHASE_27_PINS` (plus the pre-enumerated records when an escalation needs one), and turn the deep ramp independently.
</success_criteria>

<output>
Create `.planning/phases/27-delve-to-death-retune/27-02-SUMMARY.md` when done. Record: the four commit hashes; the landed Dante form and both sim numbers; the seed-303 record (fields/before/after/stateAfter) and the re-measured FULL_FIGHTS / determinism totals (Dante move and grace, separately); the curve table 1-5/10/20/35/50 after the landed set; the iteration-0 pooled readout; the `npm test` pass count and parity 33/33.
</output>
