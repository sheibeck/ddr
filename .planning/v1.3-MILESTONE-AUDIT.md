---
milestone: v1.3
name: Feel, Loot & Combat Flow
audited: 2026-09-16
status: passed_with_deferred_uat
phases: 28–33 (6/6 complete)
requirements: 28 complete · 1 dropped (UIF-04) · 1 deferred to device UAT (CMBUI-06)
tests: 1448 → 1955 (0 failures) · parity master untouched · fixtures untouched
---

# v1.3 Milestone Audit — Feel, Loot & Combat Flow

Orchestrator-authored (gsd-integration-checker not spawned — verification agents are off for this project; cross-phase wiring is evidenced by the full suite, which exercises every seam listed below end-to-end).

## Phase verification roll-up

| Phase | Status | Score | Plans | Human checks deferred |
|---|---|---|---|---|
| 28 Armor Integrity & Durability | passed | 5/5 ARMOR | 3 | 5 |
| 29 End-of-Combat Loot & Bag Cap | passed | 6/6 LOOT | 3 | 8 |
| 30 Combat Narrative — Research | passed | 1/1 CMBUI-01 | 1 | 0 |
| 31 Combat Start Gating & Effect Hygiene | passed | 6/6 CMB | 3 | 11 |
| 32 Combat Narrative & Input UI Build | passed | 4/5 CMBUI (06 = device round) | 3 | 14 |
| 33 UI Feel & Store Polish | passed | 5/5 in scope (UIF-04 dropped) | 3 | 12 |

All six `NN-VERIFICATION.md` files carry `status: passed`; 16 plans, 16 SUMMARY.md files, all `## Self-Check: PASSED`.

## Requirements coverage

- **Complete (28):** ARMOR-01..05, LOOT-01..06, CMBUI-01..05, CMB-01..06, UIF-01/02/03/05, STORE-01.
- **Deferred (1):** CMBUI-06 — "validated in an on-device DR round before the milestone closes." The build is on the Pixel 7; the round is the last block of the end-of-run UAT batch. Recorded as *Deferred — UAT pending* in REQUIREMENTS.md (corrected from the auto-flip `phase.complete` applied).
- **Dropped (1):** UIF-04 tutorial on/off — the coach-mark sequencer (`src/browser/tutorial.js`) has zero shell references and UX-06 is parked; a toggle would control nothing. User decision 2026-09-16 → UX-06 backlog.

## Cross-phase integration (seams and the tests that exercise them)

| Seam | Wiring | Evidence |
|---|---|---|
| 28 armor outcomes → 32 Round Card | `armorSoaked {underMin, magic}` / wear events fold through `enemyRound` into the card | `shell-round-card.test.js` routing exclusivity; `toastsCoverage` partition |
| 29 loot pile → 31 item kills → 32 card | `useItem` stone/fire → `killFoe` → `endCombat` → `offerLoot` → loot card; kill line stays a toast over the card | `item-combat-gate.test.js`, `shell-loot-screen.test.js`, `shell-round-card.test.js` |
| 31 `fight` split → 32 preview card + guards | `combat.pending` gates the preview; `encounterStarted` line on the card; Fight! guarded by `ARM_DELAY_MS` | `shell-fight-gate.test.js`, `shell-input-guards.test.js`, `pending-fight-audit.test.js` |
| 31 refusals → 32 routing | `*Refused` at `PRIORITY.block` bypass the card | `cast-refusals.test.js`, `shell-round-card.test.js` |
| 31 Afraid / 31 Elven flip → parity | three declared action-path divergences + `lose-plain` (seed 1119); Elven flip touches no fixture | combat/magic parity suites, `afraid.test.js`, `identity-contract.test.js` |
| 32 dismissal settle → 33 recenter | same `encWasActive && !active` transition stamps `lastDismissAt` and calls `mzCenterMap` | `shell-input-guards.test.js` + `shell-map-store-polish.test.js` (both pin the braced block) |
| 33 `storeRoll` → save/parity | `newRun` option only `startNewRun` passes; six comparable strips; economy fixture byte-identical | `store-roll.test.js`, `save-validation.test.js`, `engineAdapter.test.js`, economy parity |
| 29 bag cap → 33 Drop confirm | Drop dispatches the existing `dropItem`; store Sell/Drop rows byte-identical | `shell-gear-toolbar.test.js` |

`npm test` 1955/1955 at HEAD; `npm run build:www` exit 0; `test/parity/prototype-master.js.txt` hash `a1f4d0dc…` unchanged since v1.2; zero fixture regenerations across the milestone.

## Tech debt / deferred items carried forward

1. **CMBUI-06 device DR round** — pending (UAT batch).
2. **UIF-04 tutorial toggle** → UX-06 backlog (tutorial wiring itself is UX-06).
3. **32-03 unguarded button set** (store rows/Leave, drop shelf, `a-evt`, `btn-again`, spell-menu buttons) + **haptics on hit/kill** — future feel pass.
4. **Round-label asymmetry** (card names the round acted in; topbar shows the round in progress) — revisit only if the DR round finds it confusing.
5. **`storeRoll` as a tuning knob** — tools/ bots run flag-off; a future tuning pass may enable it for ledgers.
6. **Two re-measured pins from the 31-01 rng reorder** (combat/parley seed 303 `sp 7→5`; foe-abilities seed 1) — documented deviations, not debt, but listed for the record.
7. **Character-sheet vs. roll-screen "TO HIT"** — fixed to `1–N` in 31-03; no remaining `N+` template.

## Verdict

Milestone v1.3 is complete on automated evidence. Close it; the 50-item Pixel 7 UAT batch (Phases 28–33 incl. the CMBUI-06 DR round) runs against the installed debug build, and any device findings go to a follow-up quick task or the next milestone.
