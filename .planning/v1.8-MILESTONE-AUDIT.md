---
milestone: v1.8
audited: 2026-09-23T00:10:00Z
status: tech_debt
scores:
  requirements: 26/26
  phases: 5/5
  integration: 5/5
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 60-performance-footprint-close
    items:
      - "docs/UAT-v1.8.md: 30 of 31 checks un-run. Only 56-3 (airplane-mode first launch) passed. The user deferred the rest to their own play sessions ('I'll uat over playing several sessions and report back')."
      - "docs/UAT-v1.7.md, UAT-v1.6.md, UAT-v1.5.md: still un-run from earlier milestones."
  - phase: 58-motion-pacing
    items:
      - "Post-close fix 42f0f8d pins only the LAST hero-hp beat frame to the true after-state. A mid-round frame on a folded multi-hit line can still under-count briefly during the reveal, and foeFrames has the mirror gap (debug session trap-death-21hp-oracle-minus1, blind spots 1 and 3)."
      - "No .gitattributes eol=lf pin, so fresh worktrees show 16 CRLF false failures in four untouched test files. Master is unaffected."
  - phase: 59-party-animation-dungeon-set-dressing
    items:
      - "cameraGlide.js reentrancy: a synchronous cancel()/finish() inside the apply callback throws. No shipped path can reach it; one-line hardening deferred."
  - phase: device-findings-2026-09-22
    items:
      - "todo: the water clip should play on every water step, not only on entering water (sfx.js groupEntriesForDispatch)"
      - "todo: band-1 identity should read Race + Sub-class + Lvl (hudBands.js identityParts). User ruling recorded."
      - "todo: the HUD ☰ menu cannot open while dead (hudMenuEvent passes encounter:true on the death overlay). User said defer."
      - "debug trap-death-21hp-oracle-minus1: awaiting_human_verify. The engine is consistent (trap narration and subtraction share one dmg). The likeliest reading is a true 1/21 HP read as '21 to spare'. The real YOUR LOT under-count found along the way is fixed (42f0f8d)."
nyquist: { compliant_phases: 0, partial_phases: 0, not_validated_phases: 0, missing_phases: 0, overall: "capability inactive — skipped" }
---

# v1.8 Milestone Audit: Sound, Motion & Set Dressing

**Verdict: `tech_debt`.** All 26 requirements are satisfied and nothing blocks the milestone. What remains is the device checklist the user chose to run over their own play sessions, three small follow-ups, and four device findings filed as todos.

## Requirements coverage (3-source cross-reference)

For all 26 requirements, three sources agree: the phase VERIFICATION says `passed`, a SUMMARY lists the requirement in `requirements-completed`, and REQUIREMENTS.md has it ticked `[x]`. So each one is **satisfied**. There are no orphans. 59-VERIFICATION names its requirements by range ("ANIM-01/03", "DRESS-01..03"), and those ranges account for ANIM-03, DRESS-02 and DRESS-03.

| Phase | Requirements | Status |
|---|---|---|
| 56 Sound Effects & Audio Settings | AUD-01..06 | satisfied. Device latency, overlap and airplane-mode checks were deferred to Phase 60; 56-3 passed on the Pixel 7. |
| 57 Map & HUD Layout Band | LAYOUT-01..06 | satisfied. LAYOUT-04 and LAYOUT-05 were amended 2026-09-22 for the user's ☰-menu HUD mock. |
| 58 Motion & Pacing | MOTION-01..05 | satisfied |
| 59 Party Animation & Set Dressing | ANIM-01..03, DRESS-01..05 | satisfied |
| 60 Performance & Footprint Close | PERF-03 | satisfied on device evidence: cold start +44 ms (+5.1 %), step p95 −5.4 ms, AAB +420,608 B (+4.5 %), no regression past any threshold. |

## Phases

5/5 have a `status: passed` VERIFICATION.md. All are orchestrator-authored, following the 2026-09-14 usage ruling that turned verification agents off.

## Cross-phase integration

Checked by the orchestrator directly. No integration-checker agent was spawned, because verification agents are off under the standing usage ruling.

| Link | Evidence |
|---|---|
| 56 sfx → 58 combat beat | `cuesForDispatch` (mazeworld.html:6069) splits a combat dispatch's clips onto the beat lines. The beat runner plays them through `playClipIds` (mazeworld.html:5740). |
| 57 HUD/rail → 58 motion | The ☰ menu and rail use `createPanelMotion` (MENU_OPEN_MS / OPEN_MS / CLOSE_MS). The `.mw-rail[hidden]` override keeps the slide-out visible while it animates. HUD `paint()` is deferred to beat settle. |
| 58 cameraGlide → 59 party sprite | `partySprite.js` drives the marker's step with Phase 58's `createCameraGlide` tween, so the party and the map move as one at an edge. |
| 59 dressing → 60 perf | Cold start with Set dressing On, and the AAB size, were measured on the Pixel 7 in the same session as the v1.7 baseline. |
| Bridge | Every new `window.__mz*` (`__mzCameraGlide`, `__mzMotion`, `__mzTypewriter`, `__mzBeat`, `__mzPartySprite`, `__mzDressing`) goes through `src/browser/bridge.js`. `tools/bridge-doc.mjs --check` is clean. |

Flows: every one of these is exercised by the shell-sandbox suites and the Phase 60 device walk (water, dark, menus and tabs, no jank):
- tap-to-move with camera glide and step sprite
- a fight with the beat, cues and typewriter
- rail and ☰ menu panels opening and closing, including reduced motion
- Settings toggles (Sound, Set dressing)

## Gates at audit HEAD

- `npm test`: **3905 / 3905, fail 0** (HEAD 8facc7a).
- `npm run build:www` exits 0.
- `git diff --quiet v1.7 HEAD -- engine/ content/ test/parity/` passes, so the Engine Gate holds across the **whole** milestone (including the post-close fix 42f0f8d). The `prototype-master.js.txt` blob is still `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`. `package.json` and the lockfile are untouched.

## Post-close work (after Phase 60 closed at bcfc836)

- `9c1b80f`: HUD counter numerals are left-aligned in their reserved slots (a user device finding).
- `42f0f8d`: the combat beat's last hero-hp frame is pinned to the true final HP. It has a regression test that failed before the fix and passes after. Found while debugging the reported trap death.
