---
milestone: v2.0
audited: 2026-09-24T03:30:00Z
status: tech_debt
scores:
  requirements: 26/26
  phases: 5/5
  integration: 5/5
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 69-compliance-device-close
    items:
      - "docs/UAT-v2.0.md (75 rows) is written, not run — the deferred-UAT batch for the whole milestone. F1 (Compete-OFF cold-boot network capture) is RELEASE-BLOCKING under the as-is plugin ruling."
      - "The 2.0.0 (9) AAB carries the placeholder APP_ID 000000000000 and PLACEHOLDER_ leaderboard IDs: sign-in fails gracefully and every board is skipped until the user's console setup and a versionCode 10 rebuild."
      - "User actions owed: website deploy (darktier-studio aaa0f4a, local only), Play Console runbook walk, Data safety entry, Diagnostics declaration decision after F1, listing copy paste, 08-dead.png regenerated by hand (playwright-core not installed), AAB upload."
  - phase: 67-play-games-integration-account-chip
    items:
      - "Google's Play Games SDK initializes at every launch even with Compete OFF (plugin as-is, D-20). The game makes no PGS calls while OFF; the SDK's own behaviour is only characterised by F1."
      - "AGP 9.3.1 is blocked upstream by the plugin's Kotlin (Pgs.kt:144); build stays on AGP 8.13.0 / Gradle 8.14.3 (67-AGP9-SPIKE.md, R8/AGP9 todo)."
      - "Plugin package is young and low-download ([SUS] in research); pinned exactly, reviewed read-only; any bump needs a re-review and re-audit (docs/RELEASING.md)."
  - phase: cross-cutting
    items:
      - "CRLF working-copy artifact (core.autocrlf=true, no .gitattributes): 7 doc-ledger test failures in worktrees and a false bridge-doc --check mismatch on master. The standing `.gitattributes eol=lf` follow-up would remove both."
      - "AUD-06 fails on master while the user's untracked sfx/theme.mp3 sits in sfx/ (theme-music todo must add it as a music track, or build-www will copy it into the AAB — 69-04 set it aside for the release build)."
      - "SUMMARY frontmatter metadata: most Phase 67–69 executor SUMMARYs omit `requirements-completed`; coverage was cross-checked manually against PLAN frontmatter and the orchestrator VERIFICATION tables instead."
      - "Leftover locked worktree folders under .claude/worktrees/ (branches already deleted) — delete at cleanup."
---

# Milestone v2.0 — Leaderboards: Audit

**Status:** tech_debt. All 26 requirements are satisfied on automated evidence, every phase verified `passed`, cross-phase wiring checks out and the four end-to-end flows are connected. What remains is the deferred device batch, the user's console and website actions, and a handful of known non-blocking debts.

## Phases

| Phase | Plans | Verification | Requirements |
|-------|-------|--------------|--------------|
| 65 Run Record & Personal Bests | 5/5 | passed (10 device items) | RUN-01..04 |
| 66 Leaderboards Panel (Local) | 7/7 | passed (18 device items) | BOARD-01..08 |
| 67 Play Games Integration & Account Chip | 8/8 | passed (15 device/console items) | PGS-01, PGS-02, ACCT-01, ACCT-02 |
| 68 Global Boards, Submissions & "You Placed X" | 7/7 | passed (16 device/console items) | PGS-03..06, PLACE-01, PLACE-02 |
| 69 Compliance & Device Close | 4/4 | passed (6 user actions) | COMPLY-01..04 |

## Requirements coverage (3-source cross-reference)

| Source | Result |
|--------|--------|
| REQUIREMENTS.md traceability | 26/26 mapped to Phases 65–69, all checked `[x]` / Complete |
| Phase VERIFICATION.md tables | 26/26 ✓ with plan-level evidence; all five phases `status: passed` |
| SUMMARY frontmatter `requirements-completed` | Present for Phases 65–66 (12/12) and a few 67/69 plans; absent in most 67–69 SUMMARYs (metadata debt). Cross-checked instead against every PLAN's `requirements:` frontmatter — every requirement is claimed by at least one executed plan with a SUMMARY |

No orphaned or unsatisfied requirements. Device-level confirmation for every requirement with a UI or network surface is deferred to `docs/UAT-v2.0.md` under the deferred-UAT protocol.

## Integration (orchestrator check; the integration-checker agent is off under the standing usage ruling)

| Seam | Evidence | ✓ |
|------|----------|---|
| 65 → 68: death choke point → submission queue | `engineAdapter.js` `setRunRecordedListener` (fires after `recordDeath`, before `persistGrave`), wired in `mazeworld.html` to `pgsQueue.enqueue`; dev deaths excluded | ✓ |
| 65 → 68: THAT IS THAT | `__mzDeathRecord` → `renderNewBestBlock` (65), then `__mzPlacement` → `renderRankLine` on the next line (68) | ✓ |
| 66 → 67 → 68: Leaderboards panel | `createBoardsPanel` receives `identity:` (67-08) and `global:` / `seasons` / `onFriendsConsent` (68-07); local views unchanged signed out or Compete OFF | ✓ |
| 67 → 68: Compete OFF | account controller → `pgsQueue.purge()` + `globalBoards.clear()` + rank line cleared (`mazeworld.html` ~6284–6292) | ✓ |
| 67 → 68: provider | one `playGames.js` provider (native + fake) serves identity and all leaderboard calls; `PLUGIN_METHODS_USED` allow-list; background flush waits on adapter + queue | ✓ |

## End-to-end flows

| Flow | Path | ✓ |
|------|------|---|
| Death, Compete ON, signed in | death → local bests + graveyard → NEW PERSONAL BEST block → queue (stored first) → submit ×5 → standing → "You placed Nth of M" rank line | ✓ (device deferred) |
| Death offline / signed out | queued durably → flush on reconnect / sign-in / resume → one THE LEDGER CAUGHT UP rail card at the next map landing; old-season entries dropped with one Oracle line | ✓ (device deferred) |
| Leaderboards panel | title VIEW THE DEAD or DEAD tab → seven local boards → signed in: ALL / FRIENDS / LINEAGE from the 5-minute cache, consent behind a button, stale/unreachable notes, never blocks | ✓ (device deferred) |
| Compete OFF / no Play Games | zero game PGS calls (proven with a recording Proxy), queue purged, panel local-only, full offline play | ✓ (F1 capture deferred, RELEASE-BLOCKING) |

## Automated gates at close

- `npm test` on master: 5045/5045 with the user's untracked `sfx/theme.mp3` set aside (69-04); with it present, only AUD-06 fails.
- Engine gate held across the milestone (`git diff v1.9..HEAD -- engine/ test/parity/`): Phase 65 added `engine/records.js` (run record, bests, `runHash`; 66-01 re-ranked LEANEST there) and the `state.acts` counter plus run-summary fields in `state.js`, `engine.js`, `death.js` and `saveState.js`. `acts` is carved out of the parity comparables (`harness/comparables.js` and three parity tests) and declared as a measured-zero entry in `FIXTURE-INVENTORY.md`. `test/parity/prototype-master.js.txt` is untouched, no fixture was regenerated, and parity passes. Phases 66–69 made no engine change beyond the LEANEST comparator.
- Signed release AAB 2.0.0 / versionCode 9, sha256 `bcaaa1b3…3f97`, tagged `v2.0.0-play9` (local; pushed at close).
- Dependency audits (source level 69-01, build level 69-04): no ads, analytics or crash SDKs; permissions INTERNET, VIBRATE, androidx receiver only; no AD_ID.
