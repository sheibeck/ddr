---
phase: 25-nothing-happens-silently-feature-feedback
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/missLines.js
  - src/browser/toasts.js
  - src/browser/engineAdapter.js
  - test/unit/missLines.test.js
  - test/unit/toastTable.test.js
  - test/unit/engineAdapter.test.js
  - test/voice/safety-scan.test.js
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-05, FEED-06]

must_haves:
  truths:
    - "src/browser/missLines.js exports MISS_LINES (>= 12 distinct, voice-safe fledgling quips, each <= 40 characters), QUIP_MAX_LEVEL = 2, missLineAt(seq) (plain integer modulo the corpus length, never floating point, never Math.random) and decorateMisses(events, level, seq) → { events, seq } which, ONLY when level <= 2, returns a new array whose non-untouchable strikeMissed events are shallow copies carrying `quip: missLineAt(seq++)`; at level 3 the input array is returned unchanged with the same seq (FLAGGED ASSUMPTION FEED-05 — copy deliverable; tests: >= 12 entries, <= 40 chars each, no repeat within one full rotation, every entry passes test/voice/safety-scan.test.js) (D: Miss corpus, FEED-05)"
    - "The quip rotation counter is a plain integer modulo the corpus length (no floating point); missLineAt(N) === missLineAt(0) and missLineAt(-1) never throws (probe FEED-04 precision — the counter half)"
    - "The quip corpus is used at c.level <= 2 and NOT at level 3: decorateMisses(events, 2, s) decorates, decorateMisses(events, 3, s) is the identity (probe FEED-04 boundary — the level-gate half)"
    - "src/browser/engineAdapter.js#dispatch decorates the returned events through decorateMisses with the post-action hero level and a module-level presentation counter, and formats html from the DECORATED events, so the Oracle line and (25-03) the toast carry the same quip from one assignment site (D: presentation-side counter, deterministic, never the engine rng)"
    - "src/browser/toasts.js exports TONES (hit, miss, hurt, dodge, magic, block, beat), PRIORITY ({ block: 0, you: 1, them: 2, feature: 3, other: 4 }), MAX_TOASTS = 4, ORACLE_ONLY (a Set of bookkeeping types with a one-line reason each), FEATURE_EVENTS (the manifest array), and TOAST_FOR — one `(e, ctx) => ({ text, tone, priority })` builder per engine event type NOT in ORACLE_ONLY, including scrollRefused (D: Toast architecture — pure table beside the narration table)"
    - "Every refusal/rejection builder (strikeRefused, fleeRefused, parleyRefused, withdrawalDenied, vanishDenied, itemRejected, equipRejected, useRefused, scrollRefused, noChargesLeft, spellNotKnown, spellAboveLevel, spellSchoolLocked, campFailed, joinerRefused, buyFailed, backstabDenied, bagFull, nothingToThrowAt, nothingToTurn, gateRefused, insaneNoTarget, deathSpellTooWeak, parleyExhausted) returns tone block, priority 0, and a non-empty voiced text even when `reason` is absent or unknown (generic voiced fallback), so an unknown reason can never produce an empty toast (probe FEED-02 empty; D: every refused event toasts amber with the reason in voice)"
    - "Direction is encoded in tone family: builders for struckByFoe/foeMissed/memberStruck/foeBolted/foeDrained/foeDebuffed/foeHealed/foeSummoned/foeCast/foeRevived/foeSlept/foeOutOfSpells/heroResistFailed return tone in {hurt, dodge} with text starting with the foe name (or 'It' fallback); builders for struck/strikeMissed return {hit, miss} with text starting with 'You'; heroResisted is hit and starts with 'You'; spell outcomes are magic/miss family (FLAGGED ASSUMPTION FEED-03 — visual/wording contract; tests here on tone+prefix, CSS distinctness in 25-04, Pixel 7 check non-blocking) (D: Tone palette; FEED-03, FEED-06)"
    - "A feature event missing an optional payload field (goldGained without why, foeFled without reason, encounterStarted without foes) falls back to its base wording and never throws; every builder called with a bare { type } returns null-safe output (probe FEED-01 empty — the builder half)"
    - "The single-event builders carry the exact wording the 25-03 aggregators reuse: struckByFoe → `${name} hits you (${dmg})` + ` · CRIT` when critical/soldierCrit + a soak note ` · ${part}, ${part} soaked` built from `soaked`; foeMissed → `${name} misses you` + ` · ${modNames}` ONLY when the miss is credited to negative needMods (roll <= need − Σ negative deltas); struck → `You hit ${target} (${dmg})` + ` · CRIT`; strikeMissed → `You miss ${target}` + ` — ${quip}` when `quip` present; memberStruck → `${name} hits ${member} (${dmg})` hurt, priority 3 (D: Combat wording, FEED-03/FEED-04)"
    - "test/voice/safety-scan.test.js scans MISS_LINES and renders every TOAST_FOR builder across the same branch toggles and token injections as EVENT_NARRATION, so a new toast string is voice-checked automatically (D: corpus under the voice scan)"
  artifacts:
    - "src/browser/missLines.js — MISS_LINES, QUIP_MAX_LEVEL, missLineAt, decorateMisses"
    - "src/browser/toasts.js — TONES, PRIORITY, MAX_TOASTS, ORACLE_ONLY, FEATURE_EVENTS, TOAST_FOR (25-03 adds toastsForAction to the same file)"
    - "src/browser/engineAdapter.js — dispatch() decorates misses before formatEvents; module-level missSeq counter"
    - "test/unit/missLines.test.js, test/unit/toastTable.test.js — new; test/unit/engineAdapter.test.js — one dispatch-level quip test added; test/voice/safety-scan.test.js — corpus extended"
  key_links:
    - "engineAdapter.dispatch -> decorateMisses(events, currentState.c.level, missSeq) -> formatEvents(decorated) (Oracle) and the shell's toastsForAction(decorated) (25-03/25-04) — one quip, two consumers"
    - "TOAST_FOR builders read the additive fields 25-01 lands (soaked, needMods, soldierCrit, critBy, doubled, by, wear/halved, reason: acrobat, scrollRefused) defensively via `??`/`?.` — the two plans are parallel and must not depend on each other at execution time"
    - "safety-scan.test.js imports { TOAST_FOR } from toasts.js and { MISS_LINES } from missLines.js"
  prohibitions:
    - "No Math.random, Date.now, or any engine rng in missLines.js or toasts.js — the quip rotation is a caller-supplied integer counter"
    - "No DOM access and no import from engine/ inside missLines.js or toasts.js — pure presentation modules like viewModels.js/controls.js"
    - "The engine never emits `quip`; decorateMisses returns copies and never mutates the engine's event objects"
    - "No toast entry for bookkeeping events — they go in ORACLE_ONLY with a reason; the Oracle never loses information a toast shows (toasts are a summary, the Oracle is the record)"
    - "Every toast string stays sarcastic, deadpan, family-friendly and never profane/gory — safety-scan is the gate; no raw engine identifiers (ability ids, reason keys, table keys) in toast text"
    - "No layout or shell work in this plan (25-04 owns mazeworld.html)"
---

<objective>
Build the two pure presentation modules the phase rests on: `src/browser/missLines.js` (the fledgling-miss corpus with a deterministic, caller-supplied rotation counter and the `decorateMisses` decorator that stamps a quip onto level-≤2 miss events) and `src/browser/toasts.js` (tone/priority constants, the `ORACLE_ONLY` allowlist, the `FEATURE_EVENTS` manifest, and the `TOAST_FOR` table — one `(e, ctx) => ({ text, tone, priority })` builder for every engine event type that toasts). Wire `decorateMisses` into `engineAdapter.dispatch` so the Oracle and the toast share one quip. Put the corpus and every toast string under the voice scan.

Purpose: D "Toast architecture — mapping lives in a pure, testable toast table"; D "Miss corpus in src/browser/missLines.js, rotated by a presentation-side counter"; FEED-02 refusal builders with voiced fallbacks; FEED-03 tone families; FEED-05 the corpus; FEED-06 both directions have builders. 25-03 adds `toastsForAction` (aggregation/priority/cap) on top of this table; 25-04 wires the shell; 25-05 adds the coverage/manifest guards.
Output: new src/browser/missLines.js, src/browser/toasts.js; modified src/browser/engineAdapter.js; new test/unit/missLines.test.js, test/unit/toastTable.test.js; extended test/unit/engineAdapter.test.js and test/voice/safety-scan.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md
@src/browser/eventNarration.js
@src/browser/engineAdapter.js
@src/browser/haptics.js
@test/unit/formatEventsCoverage.test.js
@test/voice/safety-scan.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New module `src/browser/missLines.js`: `MISS_LINES` (string[] >= 12, each <= 40 chars), `QUIP_MAX_LEVEL` (2), `missLineAt(seq)`, `decorateMisses(events, level, seq) → { events, seq }`.
- New module `src/browser/toasts.js`: `TONES` (frozen array `["hit","miss","hurt","dodge","magic","block","beat"]`), `PRIORITY` (`{ block: 0, you: 1, them: 2, feature: 3, other: 4 }`), `MAX_TOASTS` (4), `ORACLE_ONLY` (Set), `FEATURE_EVENTS` (string[]), `TOAST_FOR` (object of builders `(e, ctx) => ({ text, tone, priority })`). 25-03 adds `toastsForAction` to this file.
- New tones introduced to the app vocabulary: `dodge` (their miss, muted stamp), `block` (refusal, amber), `beat` (neutral information, ink); `miss` re-specified as muted moss (CSS lands in 25-04).
- Presentation-only event field: `strikeMissed.quip` (added by decorateMisses to a copy of the event).
- engineAdapter.js: module-level `missSeq` counter; `dispatch()` returns decorated events and html formatted from them.
- New test files: test/unit/missLines.test.js, test/unit/toastTable.test.js.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: missLines.js (corpus + deterministic rotation + decorateMisses) with tests, wired into engineAdapter.dispatch</name>
  <files>src/browser/missLines.js, src/browser/engineAdapter.js, test/unit/missLines.test.js, test/unit/engineAdapter.test.js</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: "Miss corpus" — ~12 quips, <= ~40 chars, only while c.level <= 2, presentation-side counter, NEVER Math.random; Oracle keeps the roll and appends the quip; corpus in src/browser/missLines.js under the voice scan)
    - src/browser/engineAdapter.js dispatch (~L351-405: `const { state, events } = applyAction(...)`, the died/bury block, `return { state: currentState, events, html: formatEvents(events) }`) and its header comments; formatEvents/formatEvent (~L408-426)
    - src/browser/haptics.js and src/browser/viewModels.js headers (the pure-presentation-module convention: no DOM, no engine import, unit-tested)
    - test/unit/engineAdapter.test.js ~L370-416 (the E9 pattern: initRun + getState() + a scripted combat + a dispatch loop — reuse it to obtain real strikeMissed events through dispatch)
    - test/determinism/rng-no-math-random.test.js (the comment-stripping purity scan pattern — 25-05 will point it at these modules; write them clean now)
  </read_first>
  <action>
    1. Create src/browser/missLines.js with a header in the house style (what it is, why presentation-side, the determinism rule). Export:
       - `MISS_LINES`: a frozen array of at least 12 distinct fledgling-adventurer quips, each <= 40 characters, deadpan and family-friendly, no exclamation-mark hysteria. Seed corpus (keep, trim or replace, but every constraint holds): "A swing. Technically.", "The air takes the hit for it.", "You attack the general vicinity.", "That was practice. Probably.", "Nowhere near. Good effort though.", "A rehearsal, not a performance.", "Your weapon declines to participate.", "You miss. It looks surprised too.", "Wide. Impressively wide.", "Somewhere, a wall feels threatened.", "The floor was never the target.", "Ambitious. Also nowhere close.", "You swing at yesterday.", "That counts as a warning shot.", "The dungeon rates that a two."
       - `QUIP_MAX_LEVEL = 2`.
       - `missLineAt(seq)`: `MISS_LINES[((seq % n) + n) % n]` with `n = MISS_LINES.length`, integer arithmetic only, negative-safe.
       - `decorateMisses(events, level, seq)`: pure. If `!Array.isArray(events)` or `level > QUIP_MAX_LEVEL` return `{ events, seq }` unchanged (same array reference). Otherwise map: every event with `type === "strikeMissed"` and falsy `untouchable` becomes `{ ...e, quip: missLineAt(seq++) }`; every other event is passed through by reference; return `{ events: mapped, seq }`. Never mutate the input objects.
    2. src/browser/engineAdapter.js: import `{ decorateMisses }` from "./missLines.js"; add a module-level `let missSeq = 0;` (documented: presentation-only rotation counter for the fledgling-miss quips — not serialized, resets on reload by design, never touches rngState). In dispatch, after `currentState = state;` and the died/bury logic, replace the final return with: decorate `events` via `decorateMisses(events, currentState.c?.level ?? 1, missSeq)`, store the returned seq into `missSeq`, and return `{ state: currentState, events: decorated, html: formatEvents(decorated) }`. The catch-branch return is unchanged. Update dispatch's JSDoc (events may carry the presentation-only `quip`).
    3. test/unit/missLines.test.js: MISS_LINES length >= 12; every entry is a string with 1..40 characters (`assert.ok(line.length <= 40, line)`); all entries distinct (`new Set(MISS_LINES).size === MISS_LINES.length`); no entry contains "!" or ALL-CAPS shouting (optional style guard); `missLineAt(0..n-1)` returns every entry once with no repeat within a full rotation; `missLineAt(n) === missLineAt(0)`; `missLineAt(-1) === MISS_LINES[n-1]`; `missLineAt(2 * n + 3) === MISS_LINES[3]`; decorateMisses at level 1 and 2 stamps `quip` on each non-untouchable strikeMissed, advances seq by the number stamped, leaves untouchable misses and other events untouched (same object references), never mutates the input (deepEqual the input before/after); level 3 returns the identical array reference and the same seq; a non-array input is returned as-is; the module source contains no Math.random / Date.now (fs read + regex over comment-stripped source).
    4. test/unit/engineAdapter.test.js: add one test modelled on E9 — initRun a seed, make the hero a level-1 plain Fighter vs an effectively unkillable foe with `sp: { toHit: 1 }` (only a 1 hits, so misses are common) and `wp` huge, loop dispatch({ type: "attack" }) until a strikeMissed appears; assert it carries `quip` ∈ MISS_LINES and that the html line for it contains that quip AFTER the roll span (regex `/<span class="roll">\d+<\/span> vs \d+\..*You miss .*\.<\/span> .+/`); keep looping to a second strikeMissed and assert its quip differs from the first (the counter advanced); then set `getState().c.level = 3` and loop until another strikeMissed and assert `"quip" in e === false` and the html ends at the miss sentence. Bound every loop (e.g. 500 dispatches) and guard the hero's wp so it never dies (set `state.c.wp = state.c.maxWP = 9999` and the foe's `sp.dmg` tiny or `noArmor` false with heavy armour — pick the setup that keeps the run alive; document it).
  </action>
  <verify>
    <automated>node --test test/unit/missLines.test.js test/unit/engineAdapter.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const MISS_LINES" src/browser/missLines.js` == 1 and `grep -c "export const QUIP_MAX_LEVEL = 2" src/browser/missLines.js` == 1 and `grep -c "export function missLineAt" src/browser/missLines.js` == 1 and `grep -c "export function decorateMisses" src/browser/missLines.js` == 1
    - `grep -v '^\s*//' src/browser/missLines.js | grep -c "Math.random\|Date.now"` == 0
    - `grep -v '^\s*//' src/browser/missLines.js | grep -c "document\.\|window\.\|from \"../../engine\|from \"../engine"` == 0
    - `node -e "import('./src/browser/missLines.js').then(m => { if (m.MISS_LINES.length < 12) process.exit(1); if (m.MISS_LINES.some(l => l.length > 40)) process.exit(2); if (new Set(m.MISS_LINES).size !== m.MISS_LINES.length) process.exit(3); })"` exits 0
    - `grep -c 'import { decorateMisses } from "./missLines.js"' src/browser/engineAdapter.js` == 1
    - `grep -c "let missSeq" src/browser/engineAdapter.js` == 1
    - `grep -c "decorateMisses(events" src/browser/engineAdapter.js` == 1
    - `node --test test/unit/missLines.test.js` exits 0 with >= 10 passing tests
    - `node --test test/unit/engineAdapter.test.js` exits 0 (all existing tests + the new quip-through-dispatch test)
  </acceptance_criteria>
  <done>The corpus exists under its constraints, the rotation is an integer counter, decorateMisses is pure and level-gated, and a real dispatch at level 1 yields a quipped strikeMissed whose Oracle line keeps the roll first — while level 3 yields a plain miss.</done>
</task>

<task type="auto">
  <name>Task 2: toasts.js — TONES, PRIORITY, MAX_TOASTS, ORACLE_ONLY, FEATURE_EVENTS and the full TOAST_FOR table</name>
  <files>src/browser/toasts.js</files>
  <read_first>
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (ALL of Decisions: Toast architecture, Combat wording, Feature and refusal feedback incl. the FEATURE_EVENTS list and the refusal list, Spell and item legibility incl. the both-directions list and the ORACLE_ONLY examples; Specifics: "refusals should teach", "keep toasts short")
    - src/browser/eventNarration.js in full — every event type, its payload fields, and its voice (the toast is the SHORT form of the same beat; do not contradict the Oracle)
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-01-PLAN.md `<artifacts_this_phase_produces>` (the exact additive field names this table reads: soaked, needMods, soldierCrit, critBy, need, wear/halved, doubled, by, reason "acrobat", scrollRefused reasons) — 25-01 runs in parallel; read the fields defensively
    - test/unit/formatEventsCoverage.test.js deriveCanonicalEventTypes (the 208 engine types today + 25-01's scrollRefused = the key universe this table must partition with ORACLE_ONLY; run it mentally per engine file: combat.js 67, magic.js 50, encounters.js 31, movement.js 28, items.js 18, foeAbilities.js 8, economy.js 6, foeDamage.js 1, plus the 6 KNOWN_INDIRECT_TYPES)
    - engine/combat.js foeTurn/applyFoeDamageToPlayer, engine/magic.js castSpell/readScroll, engine/foeAbilities.js resolveFoeAbility (payload fields: name/member/dmg/roll/need/critical/spell/target/count/rounds/amount/reason/why/item.n/song/kind)
  </read_first>
  <action>
    Create src/browser/toasts.js (house-style header: pure presentation, no DOM, no engine import, no Math.random; the table is the single source of truth 25-03's toastsForAction and 25-05's coverage guard read). Export, in this order:

    1. `TONES = Object.freeze(["hit", "miss", "hurt", "dodge", "magic", "block", "beat"])` with a doc block defining the families — YOU: hit (your good outcome) / miss (your action produced nothing); THEM: hurt (they damaged/afflicted you or yours) / dodge (their turn produced no damage to you: misses, sleeps, heals, summons, telegraphs); magic (spells/scrolls/song/items you use and their non-damage effects); block (refusals, rejections, limits — "couldn't" never looks like "missed"); beat (neutral information — encounter start, fades, teleports).
    2. `PRIORITY = Object.freeze({ block: 0, you: 1, them: 2, feature: 3, other: 4 })` and `MAX_TOASTS = 4`.
    3. `ORACLE_ONLY`: a Set built from an array literal of `[type, reason]` pairs (or a commented list) covering bookkeeping that already has a dedicated screen, a HUD field, a prompt, or is pure step/roll detail whose outcome sibling always follows: moved, dayBegan, floorChanged, spellChargeRecovered, spGained, combatEnded, died, won, storeLeft, encounterRolled, tableFour, tableFourNoop, findOffered, findTaken, findLeft, itemDropped, itemUnequipped, joinerMet, faerieMet, grimoireSold, itemConsumed. Any addition must carry a reason comment and obey the same principle (never a feature, refusal, outcome, spell or ability event). Roll-detail events that the manifest requires (fleeRolled, chestLockRolled) and their siblings (parleyRolled, trackingRolled, afflictionRolled, insaneRolled, vaporRolled, lullabyRolled, thunderRolled, miscMagicRolled, insanityRolled) are NOT allowlisted — they get short builders at priority `other` and the 25-03 aggregators fold them into their outcome toasts.
    4. `FEATURE_EVENTS`: the manifest array (per D "Feature manifest"): frenzy, warlockBoost, foeFled, foeBored, backstab, silenceStrike, stealthStrike, ninjaFirstStrike, conArtistOpener, deathTouch, goldGained, trapDisarmed, trapDoubled, chestOpened, chestLockRolled, potionDuplicated, rested, potionDrunk, armorPatched, armorSoaked, summonBackfired, spellBackfired, allySummoned, allyPending, phobiaFrozen, shookOffFrozen, encounterStarted, encounterCleared, wanderingMonster, joinerRefused, storeOpened, sang, beastsSoothed, songIgnored, lullabyRolled, thunderRolled, cooked, healed, struck, strikeMissed, struckByFoe, foeMissed, fled, fleeRolled, trapSprung, plus every refusal: strikeRefused, fleeRefused, parleyRefused, withdrawalDenied, vanishDenied, itemRejected, equipRejected, useRefused, scrollRefused, noChargesLeft, spellNotKnown, spellAboveLevel, spellSchoolLocked, campFailed, buyFailed, backstabDenied. (25-05 proves this ⊆ TOAST_FOR ∩ EVENT_NARRATION and ⊇ the identity-contract names — 31 names: allyPending backstabDenied campFailed chestLockRolled chestOpened conArtistOpener encounterCleared fled fleeRefused fleeRolled foeBored foeFled frenzy healed joinerRefused ninjaFirstStrike potionDuplicated rested spellAboveLevel spellBackfired strikeMissed strikeRefused struck struckByFoe trapDisarmed trapSprung useRefused vanishDenied wanderingMonster warlockBoost withdrawalDenied — plus parleyRefused.)
    5. `TOAST_FOR`: one builder per engine event type not in ORACLE_ONLY (209 − 21 = ~188 entries; group them under the same file-section comments as EVENT_NARRATION: movement / combat / foe abilities / magic / economy / encounters / items / inventory). Each builder is `(e, ctx = {}) => ({ text, tone, priority })`, defends every field with `??`/`?.`, and keeps text short (aim <= 48 characters; the Oracle carries the sentence). Locked wordings (25-03's aggregators and tests depend on these exact shapes):
       - struckByFoe: `${name} hits you (${dmg})`; append ` · CRIT` when `critical || soldierCrit`; append a soak note ` · ${parts.join(", ")} soaked` where parts = `hide ${n}` / `Hardiness ${n}` / `ward ${n}` for each key present in `soaked`; tone hurt, priority them. Member-directed struckByFoe never occurs (memberStruck is the member event).
       - foeMissed: `${name} misses you` (or `${name} misses ${member}` when `member`); when `needMods` has negative-delta entries and `roll <= need - Σ(negative deltas)` (the roll would have landed without them) append ` · ${names.join(", ")}`; tone dodge; priority them (hero) / feature (member).
       - memberStruck: `${name} hits ${member} (${dmg})` + ` · CRIT`; tone hurt; priority feature (D: lower priority).
       - struck: `You hit ${target} (${dmg})` + ` · CRIT`; when `critBy` is one of silence/stealth/backstab/ninja/cutthroat append ` (${voiced reason})` e.g. `(backstab)`, `(Cutthroat)`; tone hit; priority you.
       - strikeMissed: untouchable → `You cannot touch ${target}` (tone miss); else `You miss ${target}` + (`quip` ? ` — ${quip}` : ""); tone miss; priority you.
       - Refusals (tone block, priority block, reason → voiced text, unknown/absent reason → a generic voiced fallback such as "Not allowed. The dungeon has rules, apparently."): strikeRefused names the spell (`Wizards don't punch. Cast ${spell}.` when `spell`, else the spell-less wizard line); fleeRefused "A Samurai does not run."; parleyRefused by reason (ninja / masterOfArms / walkingDead / magical / wilmsryVsMagical / tried / generic); withdrawalDenied; vanishDenied; itemRejected/equipRejected by reason (wrongClass, notBetter, noArmor, woodsman, tooHeavy, acrobat, notEquippable — each says WHY: "No mail for a Woodsman.", "Fridgians wear no armour.", "An Acrobat carries a dagger. Only a dagger."); useRefused (pilfer: `${item} does not heal. Pilfers use only healing.`); scrollRefused (pilfer / noRunes / noScrolls); noChargesLeft; spellNotKnown/spellAboveLevel/spellSchoolLocked (name the spell and the level when present); campFailed (noRations); joinerRefused (cutthroat / wilmsry, name the joiner when present); buyFailed (`Short ${short} wilmst`); backstabDenied ("Heavy armour gave you away."); bagFull; nothingToThrowAt; nothingToTurn; gateRefused; insaneNoTarget; deathSpellTooWeak; parleyExhausted.
       - Your spell outcomes (priority you): spellThrown `${spell} at ${target}` (magic — folded by 25-03 when an outcome follows); spellHit `${spell ?? "It"} hits ${target} (${dmg})` + ` ×${mult}` when mult > 1 (magic); spellMissed `${spell ?? "It"} misses ${target}` (miss); spellResisted `${target} resists ${spell}` (miss); resistFailed `${target} fails to resist` (hit); frozenSolid `${target} frozen solid` (magic); dozed/stunned/weakened/stupefied/blinded/shrunk/acidApplied/petrified/walkingDeadTurned/planeGated/insaneRolled/insaneFled/insaneStruckAlly/earthquake/volley/vaporRolled (magic, short effect statements with counts/rounds); healed `+${amount} hp` + ` (${spell})` (hit); potionDrunk `Potion +${amount} hp` + ` (${remaining} left)` + ` · ${doubled}` when doubled (hit); wardRaised/mirrorSelf/strengthCast/regenerationCast/sensesGained/detectMagic/senseDanger (magic); allySummoned/allyPending (magic); summonBackfired/spellBackfired/backfireSelfDamage/earthquakeSelfDamage/deathCast (hurt — it cost you); scrollRead/scrollCopiedToGrimoire/scrollCast (magic); itemUsed/itemBurned/itemFizzled/cured (magic; itemFizzled miss).
       - Their outcomes (priority them): foeCast `${name}: ${txt ?? "something unpleasant"}` (dodge — the telegraph); foeBolted `${name} bolts you (${dmg})` (or `bolts ${member}`) + soak note (hurt); foeDrained `${name} drains you (+${stolen} to it)` (hurt); foeDebuffed `${name}: you are ${kind} (${rounds})` (hurt); foeHealed `${name} heals (+${amount})` (dodge); foeSummoned `${by ?? name} calls for help` / `${name} joins the fight` (dodge); foeRevived `${name} gets back up` (dodge); foeSlept `${name} sleeps through it` (dodge); foeOutOfSpells (dodge); heroResisted `You resist ${name}'s spell` (hit); heroResistFailed `You fail to resist ${name}` (hurt); foePursued `${name} follows you out` (hurt); wardAbsorbed/wardReflected/wardShattered/armorSoaked (`Armour takes ${amount}` + ` · wear ${wear}` + ` (Dwarven, halved)`) / armorDestroyed / damageHalved (you-side mitigations: hit/hurt as fits); foeArmorSoaked `${name}'s armour shrugs it off` (miss); regenerated (hit); acidTick (magic); foeEffectFaded (hit); wardFaded/mirrorFaded (beat); memberDowned (hurt).
       - Features (priority feature, tone per family): frenzy "Frenzy — two wild swings" (hit); warlockBoost `The dead stiffen (+${amount} hp each)` (hurt); foeFled by reason (knight: `${name} flees a Knight`; conArtist: `${name} talked out of it`; lowHp: `${name} runs`) (hit); foeBored `${name} loses interest` (hit); backstab/silenceStrike/stealthStrike/ninjaFirstStrike/deathTouch (hit, name the mechanic); conArtistOpener "Con Artist opener: all flourish, no damage" (miss); goldGained `+${amount} wilmst` + ` (Pickpocket)` when why === "pickpocket", ` (parley)` when why === "parley" (hit); trapDisarmed "Pilfer: trap disarmed" (hit); trapDoubled "Cat Burglar: the trap hits twice as hard" (hurt); trapSprung `${name} (${dmg})` (hurt, priority them); chestOpened (reason pilfer: "Pilfer: box open, no lock roll") (hit); chestLockRolled `Lock: ${roll} vs ${need}` (beat, other); potionDuplicated "Warlock: +1 potion" (magic); rested `Camp +${amount} hp` + ` · ${doubled}, doubled` (hit); armorPatched `${by ?? "Mending"}: +${amount} armour` (hit); allyJoined (hit); phobiaFrozen "Frozen by fear" (hurt); shookOffFrozen "You shake it off" (hit); encounterStarted `${foe list}` with the flag clauses (knightBigFoe: " · big foe, the Knight waits"; samuraiNeverFirst: " · a Samurai never strikes first"; fridgianSlow: " · you strike last"; courtMageTalksFirst: " · you talk first, they act first"; acuteHearing: " · you heard them coming"; wandering: prefix "Wandering: ") (beat, feature); trackable "Unnoticed — you could slip away" (beat); combatInDark (beat); encounterCleared "Nothing left standing" (hit); wanderingMonster `Camp disturbed` + ` · Bard: the singing carried` (hurt); joinerJoined/joinerDeclined (hit/beat); storeOpened "Shop open" + ` · Pickpocket: buys ×1.25, sells ×0.75` / troll / elfOrDwarf notes (block for the pickpocket note, beat otherwise); sang `You sing ${song}` (magic); beastsSoothed `${count} stand down` (hit); songIgnored (miss); lullabyRolled `${n} nod off` (magic); thunderRolled (magic); cooked (hit); foeKilled `${name} falls (+${spGained} XP)` (hit, you); leveled `Skill level ${level} (+${wpGain} hp)` (hit, feature); fled by reason (hit, you); fleeRolled `Flee: ${roll}+${bonus} vs ${need}` (beat, other); fleeFailed (miss, you); parleyRolled (beat, other); parleyFailed (miss); parleyInsulted (hurt); movement/afflictions/traps/finds/items/economy entries as short statements in the matching family (afflictionTick hurt, afflictionPassed/Cured hit, afflictionLingers hurt, wentHungry hurt, cloakHealed/cloakRegenerated hit, teleported beat, oneWayBlocked block? — no: beat (it is world state, not a refusal), climbedOver/leaptOver/flownOver/phasedThrough hit, fellClimbing/fellInGorge hurt, heightsFear/waterFear/trappedPanic hurt, bought/itemSold/rationsBought hit, itemGiven/itemTaken/itemEquipped hit, scrollFound/foodFound/grimoireLearned hit, trapAvoided hit, trapPoisoned hurt, chestLocked miss, faerieBoon hit, faerieBane hurt, phobiaAcquired/afflictionCaught/insanity* hurt, darknessFell hurt, darknessLifted/darknessDispelled hit, miscMagicRolled beat).
    Keep the reason→text maps as small local objects so 25-03/25-05 tests can iterate `Object.keys(TOAST_FOR)`.
  </action>
  <verify>
    <automated>node -e "import('./src/browser/toasts.js').then(m => { const n = Object.keys(m.TOAST_FOR).length; console.log('TOAST_FOR', n, 'ORACLE_ONLY', m.ORACLE_ONLY.size, 'FEATURE', m.FEATURE_EVENTS.length); if (n < 180) process.exit(1); for (const [t, fn] of Object.entries(m.TOAST_FOR)) { const r = fn({ type: t }, {}); if (!r || typeof r.text !== 'string' || !r.text.trim() || !m.TONES.includes(r.tone) || !Number.isInteger(r.priority)) { console.error('bad builder', t, r); process.exit(2); } } })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export const TONES' src/browser/toasts.js` == 1 and `grep -c '"dodge"' src/browser/toasts.js` >= 1 and `grep -c '"block"' src/browser/toasts.js` >= 1 and `grep -c '"beat"' src/browser/toasts.js` >= 1
    - `grep -c 'export const PRIORITY' src/browser/toasts.js` == 1 and `grep -c 'export const MAX_TOASTS = 4' src/browser/toasts.js` == 1
    - `grep -c 'export const ORACLE_ONLY' src/browser/toasts.js` == 1 and `grep -c 'export const FEATURE_EVENTS' src/browser/toasts.js` == 1 and `grep -c 'export const TOAST_FOR' src/browser/toasts.js` == 1
    - `grep -c "^  scrollRefused:" src/browser/toasts.js` == 1 and `grep -c "^  struckByFoe:" src/browser/toasts.js` == 1 and `grep -c "^  foeMissed:" src/browser/toasts.js` == 1 and `grep -c "^  memberStruck:" src/browser/toasts.js` == 1 and `grep -c "^  heroResisted:" src/browser/toasts.js` == 1 and `grep -c "^  nothingToThrowAt:" src/browser/toasts.js` == 1 and `grep -c "^  noChargesLeft:" src/browser/toasts.js` == 1
    - `grep -v '^\s*//' src/browser/toasts.js | grep -c "Math.random\|Date.now\|document\.\|window\.\|from \"../../engine\|from \"../engine"` == 0
    - the `<verify>` node one-liner exits 0 and prints TOAST_FOR >= 180, ORACLE_ONLY >= 15, FEATURE >= 55
    - `node -e "import('./src/browser/toasts.js').then(m => { const bad = m.FEATURE_EVENTS.filter(t => !(t in m.TOAST_FOR)); if (bad.length) { console.error(bad); process.exit(1); } if (m.FEATURE_EVENTS.some(t => m.ORACLE_ONLY.has(t))) process.exit(2); if ([...m.ORACLE_ONLY].some(t => t in m.TOAST_FOR)) process.exit(3); })"` exits 0
  </acceptance_criteria>
  <done>toasts.js exports the tone/priority vocabulary, the allowlist, the manifest and a builder for every toasting engine event type, with the locked combat/refusal wordings and voiced fallbacks, all pure and DOM-free.</done>
</task>

<task type="auto">
  <name>Task 3: toastTable.test.js (builder contracts, refusal fallbacks, tone families, bare-type safety) + safety-scan corpus extension</name>
  <files>test/unit/toastTable.test.js, test/voice/safety-scan.test.js</files>
  <read_first>
    - src/browser/toasts.js (Task 2) and src/browser/missLines.js (Task 1)
    - test/voice/safety-scan.test.js in full: BASE_EVENT/BRANCH_TOGGLES/NAME_FIELDS/renderEventVariants (~L100-160), the EVENT_NARRATION test (~L157-167), collectAuthoredStrings (~L196-236), meta-tests A/B (the allowlist must stay complete AND load-bearing — a new toast string that collides with a banned word either gets reworded or, if a legitimate Scunthorpe word, allowlisted and thereby load-bearing)
    - test/unit/haptics.test.js (events → side-effect mapper test idiom)
  </read_first>
  <action>
    1. Create test/unit/toastTable.test.js:
       - Every TOAST_FOR builder, called with a bare `{ type }` and `{}` ctx, returns `{ text, tone, priority }` with non-empty trimmed text, `tone ∈ TONES`, `priority ∈ Object.values(PRIORITY)` (iterate Object.entries(TOAST_FOR); assert per type with the type in the message).
       - Refusal fallbacks (probe FEED-02 empty): for each of the 24 refusal types listed in the plan's must_haves, `TOAST_FOR[t]({ type: t })`, `({ type: t, reason: undefined })` and `({ type: t, reason: "definitelyNotAReason" })` all return tone "block", priority 0, non-empty text; and a reason-specific text differs from the fallback for at least: parleyRefused ninja, itemRejected woodsman, equipRejected acrobat, useRefused pilfer, scrollRefused pilfer, joinerRefused cutthroat, strikeRefused with spell "Freeze" (text contains "Freeze").
       - Tone families (FEED-03 flagged assumption): with a payload `{ name: "Dante", member: "Bram", dmg: 6, roll: 3, need: 5 }`, each of struckByFoe, foeMissed, memberStruck, foeBolted, foeDrained, foeDebuffed, foeHealed, foeSummoned, foeCast, foeRevived, foeSlept, foeOutOfSpells, heroResistFailed returns tone ∈ {hurt, dodge} and text starting with "Dante" or (for foeSummoned/foeCast fallbacks) a non-"You" prefix; struck/strikeMissed return tone ∈ {hit, miss} and text starting with "You"; heroResisted returns hit and starts with "You".
       - Locked wordings: struckByFoe `{ name: "Dante", dmg: 6 }` → text exactly "Dante hits you (6)"; with `critical: true` → "Dante hits you (6) · CRIT"; with `soldierCrit: true` → contains "· CRIT"; with `soaked: { hide: 2 }` → "Dante hits you (4) · hide 2 soaked" when dmg 4; foeMissed `{ name: "Dante" }` → "Dante misses you"; foeMissed `{ name: "Dante", roll: 5, need: 4, needMods: [{ name: "Guard", delta: -1 }] }` → "Dante misses you · Guard" (roll 5 would have landed at 5); the same with roll 12 → "Dante misses you" (not credited); memberStruck `{ name: "Dante", member: "Bram", dmg: 5 }` → "Dante hits Bram (5)", priority PRIORITY.feature; struck `{ target: "Dante", dmg: 8 }` → "You hit Dante (8)"; `{ target: "Dante", dmg: 8, critical: true, critBy: "backstab" }` → starts "You hit Dante (8) · CRIT"; strikeMissed `{ target: "Dante" }` → "You miss Dante"; with `quip: "Wide. Impressively wide."` → "You miss Dante — Wide. Impressively wide."; untouchable → contains "cannot".
       - Both directions (FEED-06): spellHit `{ spell: "Fireball", target: "Dante", dmg: 12 }` → "Fireball hits Dante (12)" magic; spellMissed → "Fireball misses Dante" miss; spellResisted `{ target: "Dante", spell: "Doze" }` → "Dante resists Doze" miss; frozenSolid `{ target: "Dante" }` → contains "frozen solid"; foeBolted `{ name: "Drudge", dmg: 7 }` → "Drudge bolts you (7)" hurt; heroResisted `{ name: "Drudge" }` → starts "You resist" hit.
       - Missing optional fields never throw (probe FEED-01 empty): goldGained `{ type }` and `{ type, amount: 3 }` (no why) → "+3 wilmst"; foeFled `{ type, name: "Rat" }` (no reason) → a non-empty hit toast; encounterStarted `{ type }` (no foes) → non-empty beat.
       - Feature flags: rested `{ amount: 16, doubled: "Soldier" }` contains "Soldier"; goldGained `{ amount: 3, why: "pickpocket" }` contains "Pickpocket"; storeOpened `{ pickpocket: true }` contains "1.25"; wanderingMonster `{ hours: 2, bard: true }` contains "Bard"; chestOpened `{ reason: "pilfer" }` contains "Pilfer"; trapDisarmed contains "Pilfer"; armorSoaked `{ amount: 5, wear: 3, halved: true }` contains "Dwarven"; foeFled `{ name: "Rat", reason: "knight" }` contains "Knight".
       - Manifest sanity: every FEATURE_EVENTS entry is a TOAST_FOR key; no FEATURE_EVENTS entry is in ORACLE_ONLY; ORACLE_ONLY ∩ keys(TOAST_FOR) = ∅; FEATURE_EVENTS has no duplicates.
       - Purity: toasts.js source (comment-stripped) contains no Math.random / Date.now / document. / window.; calling every builder twice with the same payload gives deepEqual results.
    2. test/voice/safety-scan.test.js: import `{ TOAST_FOR }` from "../../src/browser/toasts.js" and `{ MISS_LINES }` from "../../src/browser/missLines.js". Add BRANCH_TOGGLES entries that exercise the new branches: `{ reason: "pilfer" }`, `{ reason: "acrobat" }`, `{ reason: "woodsman" }`, `{ reason: "noRunes" }`, `{ reason: "knight" }`, `{ reason: "conArtist" }`, `{ reason: "cutthroat" }`, `{ reason: "wilmsry" }`, `{ why: "pickpocket" }`, `{ pickpocket: true }`, `{ bard: true }`, `{ doubled: "Soldier" }`, `{ halved: true, wear: 2 }`, `{ soaked: { hide: 2, hardiness: 3, ward: 1 } }`, `{ needMods: [{ name: "Guard", delta: -1 }] }`, `{ critBy: "cutthroat" }`, `{ soldierCrit: true }`, `{ quip: "X" }`, `{ untouchable: true }`, `{ knightBigFoe: true, courtMageTalksFirst: true, samuraiNeverFirst: true, fridgianSlow: true, acuteHearing: true }`. Add a test "TOAST_FOR: every toast builder renders family-friendly across all branches and tokens" that mirrors the EVENT_NARRATION test but reads `fn(ev, {}).text` (skip null results). Add `MISS_LINES.forEach((s, i) => push(`MISS_LINES[${i}]`, s))` to collectAuthoredStrings so the corpus is scanned AND counted for meta-tests A/B. If a toast/quip string trips a banned term, reword it; only allowlist a genuine Scunthorpe collision.
  </action>
  <verify>
    <automated>node --test test/unit/toastTable.test.js test/voice/safety-scan.test.js test/unit/missLines.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/toastTable.test.js` exits 0 with >= 20 passing tests
    - `grep -c 'definitelyNotAReason' test/unit/toastTable.test.js` >= 1 and `grep -c '"Dante hits you (6) · CRIT"' test/unit/toastTable.test.js` == 1 and `grep -c '"Dante misses you · Guard"' test/unit/toastTable.test.js` == 1 and `grep -c '"You miss Dante — Wide. Impressively wide."' test/unit/toastTable.test.js` == 1 and `grep -c '"Fireball hits Dante (12)"' test/unit/toastTable.test.js` == 1 and `grep -c '"Drudge bolts you (7)"' test/unit/toastTable.test.js` == 1
    - `grep -c 'from "../../src/browser/toasts.js"' test/voice/safety-scan.test.js` == 1 and `grep -c 'from "../../src/browser/missLines.js"' test/voice/safety-scan.test.js` == 1 and `grep -c "MISS_LINES\[" test/voice/safety-scan.test.js` >= 1 and `grep -c 'TOAST_FOR: every toast builder renders family-friendly' test/voice/safety-scan.test.js` == 1
    - `node --test test/voice/safety-scan.test.js` exits 0 (corpus clean; allowlist still complete and load-bearing)
    - `npm test` output ends with `# fail 0`
  </acceptance_criteria>
  <done>The toast table's contracts (locked wordings, refusal fallbacks, tone families, both directions, bare-type safety, manifest sanity, purity) are unit-tested, and every toast string and miss quip is under the family-friendly voice scan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events → toast text | Payload fields (names, reasons, counts) are interpolated into short player-facing strings; raw identifiers or banned words are the failure mode |
| presentation counter → Oracle/toast | A non-deterministic or shared-mutable counter would make the quip differ between the Oracle and the toast or across replays |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-06 | Information Disclosure | TOAST_FOR builders echoing raw reason keys / ability ids | medium | mitigate | reason→text maps with voiced fallbacks; safety-scan renders every builder; toastTable tests assert the fallback text is voiced |
| T-25-07 | Tampering | Math.random / Date.now creeping into missLines.js or toasts.js | medium | mitigate | source-scan assertions in missLines.test.js and toastTable.test.js (25-05 adds the standing guard) |
| T-25-08 | Denial of Service | a builder throwing on a sparse payload and breaking the post-dispatch render | medium | mitigate | bare-`{ type }` test over every builder; `??`/`?.` everywhere |
| T-25-09 | Tampering | decorateMisses mutating engine event objects (shared with noteCombat/haptics/lastStrike readers) | low | mitigate | copy-on-decorate + input-unchanged deepEqual test |
| T-25-10 | Repudiation | a toast showing information the Oracle lacks | low | mitigate | every toast field is also narrated (25-01 clauses); ORACLE_ONLY is one-directional by construction |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/missLines.test.js test/unit/toastTable.test.js test/unit/engineAdapter.test.js test/voice/safety-scan.test.js` green.
- `npm test` ends `# fail 0` (this plan touches no engine file; parity unaffected).
- The `<verify>` one-liner in Task 2 prints TOAST_FOR >= 180 and exits 0.
</verification>

<success_criteria>
- A pure toast table exists for every toasting engine event type with locked combat/refusal wordings, voiced fallbacks and family tones; ORACLE_ONLY and FEATURE_EVENTS are declared beside it.
- The miss corpus (>= 12, <= 40 chars, distinct, voice-safe) rotates on an integer counter and reaches both the Oracle and the toast from one decoration site in engineAdapter.dispatch, only at level <= 2.
- Every string is under the voice scan; no Math.random/Date.now/DOM in either module.
</success_criteria>

<output>
Create `.planning/phases/25-nothing-happens-silently-feature-feedback/25-02-SUMMARY.md` when done (list the final ORACLE_ONLY set, the FEATURE_EVENTS count, the TOAST_FOR entry count, and the final MISS_LINES so 25-03/25-05 executors can assert against them).
</output>
