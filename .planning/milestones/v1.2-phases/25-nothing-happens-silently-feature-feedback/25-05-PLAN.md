---
phase: 25-nothing-happens-silently-feature-feedback
plan: 05
type: execute
wave: 3
depends_on: ["25-03"]
files_modified:
  - test/unit/toastsCoverage.test.js
  - src/browser/eventNarration.js
autonomous: true
requirements: [FEED-01, FEED-02, FEED-06]

must_haves:
  truths:
    - "Every engine event type not in the explicit ORACLE_ONLY allowlist has a TOAST_FOR entry — enforced by a coverage test that enumerates event types the same way test/unit/formatEventsCoverage.test.js does (comment-stripped `type: \"…\"` scan of engine/*.js plus KNOWN_INDIRECT_TYPES); nothingToThrowAt, noChargesLeft, spellResisted and every foe ability event (foeCast, foeBolted, foeDrained, foeDebuffed, foeHealed, foeSummoned, heroResisted, heroResistFailed) are asserted to be in the table, not the allowlist (probe FEED-06 empty; D: Explicit Oracle-only allowlist)"
    - "The partition is exact: keys(TOAST_FOR) ∪ ORACLE_ONLY equals the canonical engine type set, keys(TOAST_FOR) ∩ ORACLE_ONLY is empty, and neither set contains a type the engine never emits (no dead/typo entries — the same guard EVENT_NARRATION has) (D: coverage guard of its own)"
    - "FEATURE_EVENTS ⊆ keys(TOAST_FOR) ∩ keys(EVENT_NARRATION): every manifest entry has BOTH a toast builder and an Oracle line (D: Feature manifest + coverage test, FEED-01)"
    - "FEATURE_EVENTS ⊇ every event type name asserted by test/unit/identity-contract.test.js, derived at runtime by scanning that test's comment-stripped source for expectEvent/findEvent second arguments, `.type === \"…\"` comparisons and `{ type: \"…\" }` literals, intersected with the canonical engine set (>= 30 names today) — never a hand-copied list (D: manifest covers the identity-contract names)"
    - "src/browser/missLines.js and src/browser/toasts.js contain no Math.random, Date.now, DOM access or engine/ import on any non-comment line — a standing purity tripwire in the same shape as test/determinism/rng-no-math-random.test.js (D: presentation modules pure; never the engine rng)"
    - "Every ORACLE_ONLY type still has an EVENT_NARRATION entry (except `moved`, the deliberately silent step) — Oracle-only means the Oracle carries it, not that it vanishes (D: the Oracle is the record)"
    - "src/browser/eventNarration.js re-exports TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS and toastsForAction from ./toasts.js so the narration table and the toast table are reachable from one module as the decision allows (D: TOAST_FOR exported from eventNarration.js or a sibling it re-exports)"
  artifacts:
    - "test/unit/toastsCoverage.test.js — new standing guard (partition, exclusivity, manifest ⊆/⊇, purity, Oracle-only-still-narrated)"
    - "src/browser/eventNarration.js — one re-export line for the toast module's public surface"
  key_links:
    - "deriveCanonicalEventTypes (duplicated locally from formatEventsCoverage.test.js, same regexes) -> TOAST_FOR keys ∪ ORACLE_ONLY (toasts.js)"
    - "identity-contract.test.js source scan -> FEATURE_EVENTS (toasts.js) -> TOAST_FOR + EVENT_NARRATION"
    - "eventNarration.js `export { … } from \"./toasts.js\"` — one-directional (toasts.js never imports eventNarration.js, so no cycle)"
  prohibitions:
    - "No hand-copied event-type or identity-name lists in the test — every expected set is derived from source at runtime"
    - "No change to toasts.js or missLines.js content in this plan (only tests and the re-export); any gap the guard finds is fixed in toasts.js by adding the missing builder or allowlist entry WITH a reason comment, never by weakening the test"
    - "ORACLE_ONLY may not contain a refusal, an outcome, a spell/ability or a FEATURE_EVENTS type — asserted"
    - "The re-export must not create an import cycle (toasts.js has no import from eventNarration.js) — asserted by a source check"
---

<objective>
Lock the phase's two invariants with standing guards: (1) nothing new can slip in silently — every engine event type is either toasted or explicitly Oracle-only, with the partition and the no-dead-entries rule enforced from engine source at runtime, exactly like the narration coverage guard; (2) the feature manifest is honest — every FEATURE_EVENTS entry has both a toast and an Oracle line, and the manifest covers every event name the identity-contract test asserts. Add the purity tripwire for the two new presentation modules and the eventNarration re-export the decision allows.

Purpose: FEED-06 ("every spell/ability/item effect in either direction is legible" — the coverage test is what makes that a guarantee rather than a snapshot), FEED-01 (the manifest proves every class/race feature has both channels), FEED-02 (every refusal type is in the table, never the allowlist).
Output: new test/unit/toastsCoverage.test.js; one re-export line in src/browser/eventNarration.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-02-SUMMARY.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-03-SUMMARY.md
@test/unit/formatEventsCoverage.test.js
@test/determinism/rng-no-math-random.test.js
@src/browser/toasts.js
@src/browser/eventNarration.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New standing guard: test/unit/toastsCoverage.test.js (partition + exclusivity + manifest ⊆/⊇ + purity + Oracle-only-still-narrated + no-cycle).
- src/browser/eventNarration.js: `export { TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, toastsForAction } from "./toasts.js";`
- No new modules, events, fields or tones.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: toastsCoverage.test.js — partition, exclusivity, manifest ⊆ table ∩ narration, manifest ⊇ identity-contract names</name>
  <files>test/unit/toastsCoverage.test.js</files>
  <read_first>
    - test/unit/formatEventsCoverage.test.js in full (stripComments, collectJsFiles, KNOWN_INDIRECT_TYPES, deriveCanonicalEventTypes — copy these helpers verbatim into the new file; the two-guard shape to mirror)
    - test/unit/identity-contract.test.js: findEvent/expectEvent helpers (~L198-210) and the three assertion shapes used across the file — `expectEvent(events, "X", {...})`, `findEvent(events, "X")`, `events.some((e) => e.type === "X")`, `assert.deepEqual(events, [{ type: "X", … }])` — the regexes must catch all four
    - src/browser/toasts.js exports (TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS) and src/browser/eventNarration.js EVENT_NARRATION
    - .planning/phases/25-nothing-happens-silently-feature-feedback/25-CONTEXT.md (Decisions: "Explicit Oracle-only allowlist … The coverage test demands a toast entry for every engine event type NOT in the allowlist"; "Feature manifest + coverage test")
  </read_first>
  <action>
    Create test/unit/toastsCoverage.test.js (node:test, fs/path/url like the narration guard). Local helpers: `stripComments`, `collectJsFiles`, `deriveCanonicalEventTypes` copied from formatEventsCoverage.test.js (same KNOWN_INDIRECT_TYPES, same `type:\s*([^,}]+)` + quoted-string regexes; add a header note that the two files must be kept in sync and why they are not shared — each guard reads standalone). Tests:
    1. "every engine-emitted event type is either toasted or explicitly Oracle-only": canonical.size > 200; `missing = [...canonical].filter(t => !(t in TOAST_FOR) && !ORACLE_ONLY.has(t))` deep-equals [] (message lists them sorted).
    2. "TOAST_FOR and ORACLE_ONLY are disjoint and contain no dead entries": `Object.keys(TOAST_FOR).filter(t => ORACLE_ONLY.has(t))` is []; `Object.keys(TOAST_FOR).filter(t => !canonical.has(t))` is []; `[...ORACLE_ONLY].filter(t => !canonical.has(t))` is [].
    3. "the named legibility events live in the table, never the allowlist" (probe FEED-06 empty): for each of nothingToThrowAt, noChargesLeft, spellResisted, resistFailed, foeCast, foeBolted, foeDrained, foeDebuffed, foeHealed, foeSummoned, heroResisted, heroResistFailed, scrollRefused, and every refusal type (strikeRefused, fleeRefused, parleyRefused, withdrawalDenied, vanishDenied, itemRejected, equipRejected, useRefused, noChargesLeft, spellNotKnown, spellAboveLevel, spellSchoolLocked, campFailed, joinerRefused, buyFailed, backstabDenied): `t in TOAST_FOR` and `!ORACLE_ONLY.has(t)`.
    4. "ORACLE_ONLY holds bookkeeping only": no FEATURE_EVENTS entry is in ORACLE_ONLY; every ORACLE_ONLY type except "moved" has an EVENT_NARRATION entry (the Oracle still carries it); ORACLE_ONLY.size is between 10 and 40 (a sanity band so the allowlist cannot quietly swallow the table).
    5. "FEATURE_EVENTS ⊆ TOAST_FOR ∩ EVENT_NARRATION": for every manifest entry both `TOAST_FOR[t]` and `EVENT_NARRATION[t]` are functions; no duplicates in FEATURE_EVENTS; every manifest builder called with a bare `{ type }` returns non-empty text.
    6. "FEATURE_EVENTS ⊇ the identity-contract event names": read test/unit/identity-contract.test.js, strip comments, collect names via `/(?:expectEvent|findEvent)\(\s*[A-Za-z0-9_]+\s*,\s*"([A-Za-z]+)"/g`, `/\.type === "([A-Za-z]+)"/g` and `/\{\s*type:\s*"([A-Za-z]+)"/g`, intersect with `canonical` (drops encounter-type strings such as "Beasts"/"Humans"), assert the result has >= 30 names, and assert `[...names].filter(n => !FEATURE_EVENTS.includes(n))` deep-equals [] (message lists the gaps).
    If any test fails on first run, fix src/browser/toasts.js (add the missing builder, or add the type to ORACLE_ONLY with a reason comment if — and only if — it is bookkeeping with a dedicated screen/HUD/prompt) — never weaken the assertion. Record any such fix in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/toastsCoverage.test.js` exits 0 with >= 6 passing tests
    - `grep -c "function deriveCanonicalEventTypes" test/unit/toastsCoverage.test.js` == 1 and `grep -c "KNOWN_INDIRECT_TYPES" test/unit/toastsCoverage.test.js` >= 2
    - `grep -c "identity-contract.test.js" test/unit/toastsCoverage.test.js` >= 1 and `grep -c 'expectEvent|findEvent' test/unit/toastsCoverage.test.js` >= 1
    - `grep -c "FEATURE_EVENTS" test/unit/toastsCoverage.test.js` >= 4 and `grep -c "ORACLE_ONLY" test/unit/toastsCoverage.test.js` >= 6
    - `grep -c '"heroResistFailed"' test/unit/toastsCoverage.test.js` >= 1 and `grep -c '"nothingToThrowAt"' test/unit/toastsCoverage.test.js` >= 1 and `grep -c '"scrollRefused"' test/unit/toastsCoverage.test.js` >= 1
    - `grep -v '^\s*//' test/unit/toastsCoverage.test.js | grep -c 'const IDENTITY_NAMES = \['` == 0 (no hand-copied identity-name list)
    - `node --test test/unit/formatEventsCoverage.test.js` still exits 0
  </acceptance_criteria>
  <done>The toast table is provably total over engine event types minus an explicit, bookkeeping-only allowlist; the manifest is provably backed by both channels and covers every identity-contract event name — all derived from source, none hand-copied.</done>
</task>

<task type="auto">
  <name>Task 2: Purity tripwire for the presentation modules, the eventNarration re-export (no cycle), full suite + parity</name>
  <files>test/unit/toastsCoverage.test.js, src/browser/eventNarration.js</files>
  <read_first>
    - test/determinism/rng-no-math-random.test.js (the non-comment-line scan pattern and its failure message shape)
    - src/browser/eventNarration.js header + last export (where a trailing re-export line goes); src/browser/toasts.js import lines (must not import eventNarration.js)
    - src/browser/engineAdapter.js import of EVENT_NARRATION (unaffected by the re-export)
  </read_first>
  <action>
    <!-- planner-discipline-allow: eventNarration -->
    1. Append to test/unit/toastsCoverage.test.js:
       - "presentation toast modules are pure": for src/browser/toasts.js and src/browser/missLines.js, read the source, strip comments, assert no line matches `Math.random`, `Date.now`, `document.`, `window.`, `globalThis.`, `localStorage`, or an import specifier containing `engine/`; list offending file:line pairs in the message.
       - "the narration table re-exports the toast surface without a cycle": dynamic-import src/browser/eventNarration.js and assert `mod.TOAST_FOR === (await import("../../src/browser/toasts.js")).TOAST_FOR` (same object — a re-export, not a copy), and that `mod.toastsForAction` is a function; source-check that toasts.js contains no `from "./eventNarration.js"`.
    2. src/browser/eventNarration.js: add, after the EVENT_NARRATION export, one line `export { TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, toastsForAction } from "./toasts.js";` with a two-line comment (Phase 25: the toast table lives beside this table in ./toasts.js and is re-exported here per the phase decision; toasts.js never imports this module).
    3. Run `npm test` and the parity suite; confirm test/parity is clean and prototype-master.js.txt untouched (this plan touches no engine file; the check is the phase-close proof).
  </action>
  <verify>
    <automated>node --test test/unit/toastsCoverage.test.js && npm test && node --test "test/parity/**/*.test.js"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export { TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, toastsForAction } from "./toasts.js";' src/browser/eventNarration.js` == 1
    - `grep -c 'eventNarration' src/browser/toasts.js` == 0 (no cycle)
    - `grep -c "Math.random" test/unit/toastsCoverage.test.js` >= 1 and `grep -c "missLines.js" test/unit/toastsCoverage.test.js` >= 1
    - `node --test test/unit/toastsCoverage.test.js` exits 0 with >= 8 passing tests
    - `npm test` output ends with `# fail 0` and `# pass` >= 1300 (1188 baseline + the phase's new tests across 25-01..25-05)
    - `node --test "test/parity/**/*.test.js"` shows `# pass 33`; `git status --porcelain test/parity` prints nothing; `git diff --stat -- test/parity/prototype-master.js.txt` prints nothing
    - `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/engineAdapter.test.js` exits 0 (the re-export changes nothing for existing importers)
  </acceptance_criteria>
  <done>The two presentation modules are guarded pure, the narration module re-exports the toast surface without a cycle, and the whole suite plus parity is green with a clean fixture tree — the phase's automated verification is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine source → test derivation | The guards trust the `type: "…"` scan; a new push shape the regex misses would be a silent gap in BOTH coverage guards |
| module graph | A cycle between eventNarration.js and toasts.js would break ESM evaluation order in the WebView |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-20 | Tampering | ORACLE_ONLY growing to hide missing builders | medium | mitigate | size band + no-FEATURE/no-refusal/no-ability membership assertions + Oracle-still-narrates check |
| T-25-21 | Tampering | regex derivation missing a new push shape | low | accept | identical derivation to the narration guard (kept in sync by note); a miss would also miss there and be caught at review |
| T-25-22 | Denial of Service | import cycle via the re-export | medium | mitigate | source assertion that toasts.js never imports eventNarration.js; identity check on the re-exported object |
| T-25-23 | Tampering | Math.random/Date.now/DOM creeping into the pure modules later | medium | mitigate | standing non-comment-line scan |
| T-25-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/toastsCoverage.test.js` green (>= 8 tests).
- `npm test` ends `# fail 0` with >= 1300 passing; parity 33/33; clean test/parity tree; prototype-master.js.txt untouched.
- formatEventsCoverage, safety-scan and engineAdapter tests unaffected by the re-export.
</verification>

<success_criteria>
- A new engine event type cannot ship without either a toast builder or an explicit, reasoned Oracle-only entry.
- Every manifest feature has both channels; every identity-contract event name is in the manifest.
- The presentation modules are guarded pure; the module graph is acyclic; the phase closes with a fully green suite and byte-identical parity.
</success_criteria>

<output>
Create `.planning/phases/25-nothing-happens-silently-feature-feedback/25-05-SUMMARY.md` when done (record the final canonical type count, the ORACLE_ONLY size, the FEATURE_EVENTS size, the identity-name count, and any builder/allowlist fix the guard forced).
</output>
