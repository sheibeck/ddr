---
phase: 26-mass-playtest-class-pass-ledger
plan: 03
type: execute
wave: 3
depends_on: ["26-01", "26-02"]
files_modified:
  - docs/class-pass/verdicts.json
  - docs/CLASS-PASS.md
autonomous: true
requirements: [PLAY-02, PLAY-03]
tags: [class-pass, fun-band, verdicts, editorial, ledger, outliers, handoff]

must_haves:
  truths:
    - "If the revisit list is empty the Outliers section says so explicitly ('No revisit rows — every out-of-band row was accepted') rather than being omitted; every required section heading exists even when its content is a single line; the ledger test asserts presence of BEFORE / Rulings / AFTER / Outliers / Handoff headings."
    - "Every sub-class (24) and race (6, Human included as the control) row carries BEFORE mean, AFTER mean, delta, reach-5 BEFORE/AFTER and a band; every OUT-OF-BAND row additionally carries accept or revisit plus a one-sentence reason, and every revisit row a one-line suggested lever for v1.3 — all in docs/class-pass/verdicts.json, rendered into the ledger by the diff script, never typed into the Markdown (26-CONTEXT.md, 'The fun band', 'Outliers and the handoff')."
    - "Too strong is not bad: a strong-but-fun identity is accepted with its sentence; only rows judged unfun or degenerate (one mechanic dominates every fight, or the bad never bites) go to the revisit list; Wilmsry is judged by its AFTER number like every other row — still far ahead => revisit with the deferred numeric levers (parley +4 -> lower; heal 2x -> 1.5x), gap closed => accepted (26-CONTEXT.md)."
    - "The AFTER section leads with the zero-cannot-act headline, then the by-class BEFORE->AFTER roll-up, the bottom-five/top-five sub-classes, then the race table (26-CONTEXT.md Specific Ideas); the depth-20 slice is reported as Phase 27's yardstick with floors-gained/encounters-survived only and NO fun-band verdict on the deep slice."
    - "The ledger ends with `## Handoff to Phase 27`: AFTER natural roll-ups by class/sub/race, mu and the band edges, the reach table (>=5/>=10 pooled; >=20 stated as not carried per cell by the harness JSON, with the max per-cell p90 bound and the depth-20 slice as the >=20 yardstick), the depth-20 roll-ups, and the accepted-but-strong rows — so the retune knows the player power it is tuning against."
    - "The 30-row good/bad table is NOT duplicated — the AFTER/Outliers/Handoff blocks reference the Rulings section instead; the AFTER placeholder anchors (the heading replaced by 26-02, the provenance sub-heading) stay in place and the script's AFTER block is inserted between the AFTER heading and the provenance sub-heading."
  artifacts:
    - path: "docs/class-pass/verdicts.json"
      provides: "schema class-pass-verdicts/1 — bands for 24 subs + 6 races + out-of-band cells, cannotAct (empty), depth-20 roll-ups, and the editorial verdict/reason/lever on every out-of-band sub/race row"
      contains: "\"schema\": \"class-pass-verdicts/1\""
    - path: "docs/CLASS-PASS.md"
      provides: "AFTER comparison block (script --section after), `## Outliers (Phase 26 — revisit list)`, `## Handoff to Phase 27` — eight H2 sections in the fixed order"
      contains: "## Handoff to Phase 27"
  key_links:
    - from: "docs/class-pass/verdicts.json"
      to: "docs/CLASS-PASS.md"
      via: "node tools/class-pass-diff.mjs --verdicts docs/class-pass/verdicts.json --out-verdicts docs/class-pass/verdicts.json --section after|outliers|handoff — output pasted verbatim"
      pattern: "class-pass-verdicts/1"
    - from: "docs/CLASS-PASS.md (Outliers, Phase 26)"
      to: "docs/class-pass/verdicts.json revisit rows"
      via: "the Outliers table rows are exactly the rows whose verdict is revisit (rendered, not hand-listed)"
      pattern: "## Outliers \\(Phase 26 — revisit list\\)"
    - from: "docs/CLASS-PASS.md (Handoff)"
      to: "docs/class-pass/after.json + after-depth20.json rollups"
      via: "the handoff tables are after.rollups / afterDeep.rollups rendered through rankRollupRows"
      pattern: "### AFTER natural roll-ups — by class"
  prohibitions:
    - "No engine, content, shell or harness change from the revisit list — every lever is written as a v1.3 candidate line in verdicts.json, nothing is implemented"
    - "Never retype a number into the ledger — every table is the diff script's output pasted verbatim; the only hand-written text lives in verdicts.json's verdict/reason/lever fields"
    - "No verdict, band or editorial sentence on any depth-20 row"
    - "No edits to any ledger section above the AFTER heading and no edit to 26-02's provenance paragraph or transcripts"
    - "Do not duplicate the 30-row good/bad table anywhere — reference the Rulings section"
    - "Never judge around a cannot-act cell: if the re-run gate reports any, STOP (Rule 4) — no verdicts are written"
    - "Family-friendly, deadpan, factual sentences only — no profanity, no player-facing copy"
---

<objective>
Judge the AFTER matrix and finish the ledger (PLAY-02 verdicts, PLAY-03 outliers + handoff). Run `tools/class-pass-diff.mjs` on the committed BEFORE/AFTER pairs to produce `docs/class-pass/verdicts.json`, write the one hand-written column — accept/revisit + one-sentence reason on every out-of-band sub-class/race row, plus a one-line v1.3 lever on every revisit row — into that JSON, re-render, and paste the script's three Markdown blocks into `docs/CLASS-PASS.md`: the AFTER comparison block (leading with the zero-cannot-act headline, the by-class roll-up, bottom/top five subs, the race table, all 24 subs, the reach table, the depth-20 yardstick, the out-of-band appendix) between the AFTER heading and 26-02's provenance sub-heading, then `## Outliers (Phase 26 — revisit list)` and `## Handoff to Phase 27` at the end of the file.

Purpose: PLAY-02 asks for a written fun-band verdict per sub-class and race row with every cannot-act row zero; PLAY-03 asks for the remaining outliers as next-milestone candidates and a handoff Phase 27's retune can tune against. This plan writes the judgment; the numbers stay machine-derived.
Output: `docs/class-pass/verdicts.json`, the three new ledger blocks, one commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-01-SUMMARY.md
@.planning/phases/26-mass-playtest-class-pass-ledger/26-02-SUMMARY.md
@tools/class-pass-diff.mjs
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `tools/class-pass-diff.mjs` (`--before/--after/--before-deep/--after-deep/--verdicts/--out-verdicts/--section/--json/--gate`) + `test/unit/class-pass-diff.test.js`. [26-01]
- `docs/class-pass/after.json`, `docs/class-pass/after-depth20.json`; ledger AFTER heading (full pin hash), `### Pin and provenance (Phase 26 capture)`, both verbatim transcripts. [26-02]
- `docs/class-pass/verdicts.json` — schema `class-pass-verdicts/1`; every out-of-band sub/race row carries `verdict` (accept|revisit), `reason` (one sentence), and — for revisit — `lever` (one line, v1.3 candidate) (this plan). [26-03]
- `docs/CLASS-PASS.md`: the AFTER comparison block (`**Zero cannot-act cells.**` headline, parity line, mu line, seven H3 tables) inserted between the AFTER heading and the provenance sub-heading; `## Outliers (Phase 26 — revisit list)`; `## Handoff to Phase 27` (this plan). [26-03]
- `test/unit/class-pass-ledger.test.js`. [26-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Render verdicts.json, re-run the cannot-act gate, write the accept/revisit column (reason + lever) on every out-of-band sub/race row</name>
  <files>docs/class-pass/verdicts.json</files>
  <read_first>
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md, sections "The fun band (PLAY-02)", "Outliers and the handoff", "Claude's Discretion" (wording), "Specific Ideas" (the headline question: which classes over/under-perform now, and did the caster problem go away?)
    - tools/class-pass-diff.mjs header (verdicts.json schema; the mergeEditorial rules: only verdict/reason/lever are copied, verdict must be accept|revisit|null)
    - docs/CLASS-PASS.md `### Good / bad table` (each row's GOOD and BAD, to name the mechanic in every reason and lever) and `### Race rulings` (the Wilmsry paragraph: parley +4 / heal 2x deferred trims)
    - docs/class-pass/after.json: for each out-of-band sub/race row only, the cells under it (`topCauses`, `meanKills`, `reach10`) — read via `node -e` filters, do not dump all 143 cells
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-02-SUMMARY.md (pin, gate result)
  </read_first>
  <action>
    1. Preflight: `git status --porcelain` is empty; `git diff --quiet 5565b22 -- tools/tune-classes.mjs tools/lib` exits 0; `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json; echo rc=$?` prints `cannot-act cells: 0 of 143` and `rc=0`. If rc is 3, STOP (Deviation Rule 4) exactly as Plan 26-02 specifies — write no verdicts.
    2. Render the placeholders: `node tools/class-pass-diff.mjs --out-verdicts docs/class-pass/verdicts.json --json > /dev/null` (defaults resolve the four committed JSONs). Confirm with `node -e` that `schema === "class-pass-verdicts/1"`, `subs.length === 24`, `races.length === 6`, `cannotAct.length === 0`, `meta.parity.natural.ok && meta.parity.deep.ok`, and print the compact list of out-of-band rows: `subs.concat(races).filter(r => r.band !== "fine").map(r => [r.key, r.band, r.before.meanDepth, r.after.meanDepth, r.delta])` plus `meta.mu` and `meta.bands.edges`.
    3. Write the editorial column by editing ONLY the `verdict`, `reason` and `lever` fields of `docs/class-pass/verdicts.json` (scoped Edit calls on the JSON; keep 2-space formatting; never touch any number). Rules (26-CONTEXT.md, locked):
       - Every row whose `band` is `too weak` or `too strong` gets `verdict` = `"accept"` or `"revisit"` and a `reason` of one sentence (at most 200 characters, factual, deadpan is fine, family-friendly; cite the AFTER mean and the mechanic by its Rulings-table name).
       - `"accept"` when the row's edge (or shortfall) IS its identity and the dungeon still bites: for a too-strong row its bad still shows up in outcomes (its top causes are ordinary deaths, its kills/reach10 sit in the same order of magnitude as its class peers, no single mechanic ends every fight); for a too-weak row only when the shortfall is the deliberate price of a kit that still acts (meanKills >= 0.5 and reach5 > 0 in every cell under it) — expect this to be rare.
       - `"revisit"` when the row is degenerate or unfun: one mechanic dominates every fight (e.g. an opener that decides most fights before a foe acts), the bad never bites (its bad's outcome never appears), or a too-weak row whose Phase 23/24 fix was supposed to lift it and it still trails its class — in particular any caster sub-class still too weak means the caster problem did not go away, and the reason must say so plainly.
       - Every `"revisit"` row gets `lever`: one line naming which good or bad to touch and in which direction (for example "Summoner: let the level-1 summon act on the turn it appears (good up)" or "Ninja: opener max damage -> max of one die (good down)"), always framed as a v1.3 candidate — no engine edit now.
       - Wilmsry is judged by its AFTER number like every other row: band `too strong` => `verdict: "revisit"`, reason citing its AFTER mean vs BEFORE 3.93 and the Joiner refusal not closing the gap, `lever: "Wilmsry: parley +4 -> +2; camp heal 2x -> 1.5x (both deferred from Phase 24)"`; band `fine` => `verdict: "accept"` with reason "in band at AFTER <mean> (BEFORE 3.93) — the Joiner refusal closed the gap; deferred numeric trim closed" (an in-band row MAY carry an accept verdict; the renderer shows it; no other in-band row gets a verdict).
       - Every other in-band row keeps `verdict: null, reason: null, lever: null`. Never set `revisit` on an in-band row.
       - Human is the control: if Human is out of band, say what that implies for mu in its reason (it is a race with no modifier, so its band is the class mix talking, not a race mechanic) and accept it.
    4. Re-render through the merge to validate and normalise: `node tools/class-pass-diff.mjs --verdicts docs/class-pass/verdicts.json --out-verdicts docs/class-pass/verdicts.json --section after > "<scratchpad>/after-block.md"`, then `--section outliers > "<scratchpad>/outliers-block.md"` and `--section handoff > "<scratchpad>/handoff-block.md"` (each with the same `--verdicts`/`--out-verdicts` flags; the file is idempotent under re-merge). Assert with `node -e`: no out-of-band sub/race row has `verdict === null` or an empty `reason`; every `revisit` row has a non-empty `lever`; no `fine` row has `verdict === "revisit"`; `grep -c "(verdict pending)" "<scratchpad>/after-block.md"` is 0. Record in the SUMMARY the headline answers in one sentence each, quoting the rendered numbers: which classes over/under-perform now (by-class table), whether the caster problem went away (Magic User's AFTER mean and the caster subs' bands), and Wilmsry's disposition.
  </action>
  <verify>
    <automated>node -e "const v=require('./docs/class-pass/verdicts.json');const rows=v.subs.concat(v.races);const oob=rows.filter(r=>r.band!=='fine');console.log(v.schema,v.subs.length,v.races.length,v.cannotAct.length,oob.every(r=>(r.verdict==='accept'||r.verdict==='revisit')&&typeof r.reason==='string'&&r.reason.length>0),rows.filter(r=>r.verdict==='revisit').every(r=>typeof r.lever==='string'&&r.lever.length>0),rows.filter(r=>r.band==='fine').every(r=>r.verdict!=='revisit'),v.meta.parity.natural.ok&&v.meta.parity.deep.ok)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `class-pass-verdicts/1 24 6 0 true true true true`.
    - `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json; echo rc=$?` ends with `rc=0`.
    - `node tools/class-pass-diff.mjs --verdicts docs/class-pass/verdicts.json --section after | grep -c "(verdict pending)"` prints `0`.
    - `node -e "const v=require('./docs/class-pass/verdicts.json');const w=v.races.find(r=>r.key==='Wilmsry');console.log((w.band==='too strong')===(w.verdict==='revisit'), w.verdict!==null)"` prints `true true` (Wilmsry always carries a verdict, revisit iff too strong).
    - `node -e "const v=require('./docs/class-pass/verdicts.json');const rows=v.subs.concat(v.races);console.log(rows.every(r=>r.reason===null||r.reason.length<=200), rows.filter(r=>r.band==='fine'&&r.key!=='Wilmsry').every(r=>r.verdict===null&&r.reason===null&&r.lever===null))"` prints `true true`.
    - Re-running `node tools/class-pass-diff.mjs --verdicts docs/class-pass/verdicts.json --out-verdicts "<scratchpad>/v2.json" --json > /dev/null && cmp docs/class-pass/verdicts.json "<scratchpad>/v2.json"; echo rc=$?` prints `rc=0` (the committed file is a fixed point of the merge — byte-identical on re-render).
    - `git status --porcelain` prints only `?? docs/class-pass/verdicts.json` (nothing else touched; not committed until Task 2).
    - `node -e "const v=require('./docs/class-pass/verdicts.json');console.log(v.deep.bySub.every(r=>!('band' in r)&&!('verdict' in r)))"` prints `true` (no verdict on the depth-20 slice).
  </acceptance_criteria>
  <done>verdicts.json carries bands for every sub/race row and cell, an accept/revisit call with a one-sentence reason on every out-of-band sub/race row, a v1.3 lever on every revisit row, Wilmsry's disposition by its AFTER number, and nothing on the depth-20 slice — and it is a fixed point of the script's own merge.</done>
</task>

<task type="auto">
  <name>Task 2: Paste the AFTER block, the Outliers (Phase 26) section and the Handoff to Phase 27 into the ledger; commit ledger + verdicts</name>
  <files>docs/CLASS-PASS.md, docs/class-pass/verdicts.json</files>
  <read_first>
    - docs/CLASS-PASS.md: the AFTER heading line and the `### Pin and provenance (Phase 26 capture)` sub-heading directly beneath it (the insertion point is the blank line between them), and the last line of the file (the AFTER section's closing "machine-diffable aggregates" line)
    - The three scratchpad blocks rendered in Task 1 (`after-block.md`, `outliers-block.md`, `handoff-block.md`) — pasted verbatim, byte-for-byte
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-CONTEXT.md "Specific Ideas" (zero cannot-act is the first line of the AFTER section; lead with the by-class roll-up and the bottom/top five subs, then the race table — the script's block already has this order; do not reorder)
  </read_first>
  <action>
    Scoped Edit calls only (never a whole-file Write; nothing above the AFTER heading changes; 26-02's provenance paragraph and transcripts stay byte-identical):
    1. Insert the content of `after-block.md` verbatim between the `## AFTER — commit <hash> (Phase 26 — PLAY-02)` heading and `### Pin and provenance (Phase 26 capture)`: old_string = the heading line + blank line + the provenance sub-heading line; new_string = heading, blank line, the block, blank line, the provenance sub-heading. The first non-blank line under the AFTER heading must now start with `**Zero cannot-act cells.**`.
    2. Append `outliers-block.md` verbatim after the AFTER section's closing line (a blank line, then the `## Outliers (Phase 26 — revisit list)` heading and its content — either the exact sentence `No revisit rows — every out-of-band row was accepted.` or the revisit table plus the fixed v1.3 sentence).
    3. Append `handoff-block.md` verbatim after it (blank line, then `## Handoff to Phase 27` and its sub-sections). The file ends with the handoff's `### Caveats` sentences and a single trailing newline.
    4. Verify the pastes are byte-exact: `node tools/class-pass-diff.mjs --verdicts docs/class-pass/verdicts.json --section after` output must be a substring of the ledger (check with a `node -e` that reads both and calls `includes()` on the trimmed output), likewise `--section outliers` and `--section handoff`. Verify the H2 order and count (eight), the AFTER placeholder anchors (heading, provenance sub-heading, both transcript sub-headings) still present, and that the good/bad table header `| Sub / Race | GOOD | BAD |` appears exactly once in the file.
    5. Commit `docs/CLASS-PASS.md` and `docs/class-pass/verdicts.json` together via the GSD commit helper (message: `docs(26-03): class-pass verdicts — AFTER fun bands, outliers, Phase 27 handoff`). After the commit: `git diff --quiet <pin> -- engine content src mazeworld.html tools` (pin = the hash in the AFTER heading) exits 0; `npm test` still ends `# fail 0`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const {execFileSync}=require('child_process');const doc=fs.readFileSync('docs/CLASS-PASS.md','utf8');const run=(s)=>execFileSync(process.execPath,['tools/class-pass-diff.mjs','--verdicts','docs/class-pass/verdicts.json','--section',s]).toString();const ok=['after','outliers','handoff'].map(s=>doc.includes(run(s).trim()));const h2=doc.split('\n').filter(l=>l.startsWith('## '));console.log(ok.join(','),h2.length,h2[h2.length-1])"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `true,true,true 8 ## Handoff to Phase 27`.
    - `grep -n "^## " docs/CLASS-PASS.md | awk -F: '{print $2}' | paste -sd '|'` equals, in order: `## Bot proxy (HARN-02)|## How to reproduce|## BEFORE — commit 5565b222564f8ee2a944e03c1e4a3c27cae09b40 (pre-identity-pass engine)|## Outliers / Findings (Phase 22)|## Rulings (Phase 24 — PLAY-03)|## AFTER — commit <pin> (Phase 26 — PLAY-02)|## Outliers (Phase 26 — revisit list)|## Handoff to Phase 27` (with `<pin>` the 40-hex hash 26-02 wrote).
    - `awk '/^## AFTER — commit/{f=1;next} f && NF {print; exit}' docs/CLASS-PASS.md` prints a line starting with `**Zero cannot-act cells.**` (the first non-blank line of the AFTER section).
    - `grep -c "^### Pin and provenance (Phase 26 capture)$" docs/CLASS-PASS.md` prints `1`; `grep -c "^### AFTER transcript — tune-classes" docs/CLASS-PASS.md` prints `2`; the AFTER block's seven H3 headings (`### By class — BEFORE → AFTER`, `### Sub-classes — bottom five and top five (AFTER)`, `### Races — BEFORE → AFTER (Human is the control)`, `### Sub-classes — all 24 rows (AFTER rank order)`, `### Reach table (AFTER natural, pooled over completed runs)`, `### Depth-20 slice — Phase 27's yardstick (no verdicts)`, `### Out-of-band cells (appendix)`) each appear exactly once and all precede the provenance sub-heading (`grep -n` line numbers ascending in that order and all < the provenance line number).
    - `grep -c "^## Outliers (Phase 26 — revisit list)$" docs/CLASS-PASS.md` prints `1` and EITHER `grep -c "^No revisit rows — every out-of-band row was accepted.$" docs/CLASS-PASS.md` prints `1` OR `grep -c "Suggested lever (v1.3)" docs/CLASS-PASS.md` prints `1` (exactly one of the two).
    - `grep -c "^## Handoff to Phase 27$" docs/CLASS-PASS.md` prints `1`; `grep -c "^### AFTER natural roll-ups — by " docs/CLASS-PASS.md` prints `3`; `grep -c "^### Depth-20 roll-ups — by " docs/CLASS-PASS.md` prints `3`; `grep -c "^### Accepted-but-strong rows$" docs/CLASS-PASS.md` prints `1`; `grep -c "^### Caveats$" docs/CLASS-PASS.md` prints `1`; `tail -c 1 docs/CLASS-PASS.md | od -An -tx1 | tr -d ' '` prints `0a`.
    - `grep -c "| Sub / Race | GOOD | BAD |" docs/CLASS-PASS.md` prints `1` (the 30-row table is not duplicated); `grep -c "(verdict pending)" docs/CLASS-PASS.md` prints `0`.
    - `git diff HEAD~1 -- docs/CLASS-PASS.md | grep '^-' | grep -v '^---' | wc -l` prints `0` (this plan only added lines to the ledger — nothing removed or rewritten, so 26-02's provenance and transcripts and everything above the AFTER heading are byte-identical).
    - `git status --porcelain` prints nothing; `git log -1 --format=%s` starts with `docs(26-03): class-pass verdicts`; `git show --stat --format= HEAD | grep -c "docs/CLASS-PASS.md\|docs/class-pass/verdicts.json"` prints `2`.
    - `git diff --quiet "$(grep -oE '^## AFTER — commit [0-9a-f]{40}' docs/CLASS-PASS.md | grep -oE '[0-9a-f]{40}')" -- engine content src mazeworld.html tools; echo rc=$?` prints `rc=0`; `npm test 2>&1 | tail -8` shows `# fail 0`.
  </acceptance_criteria>
  <done>The ledger has all eight sections in the fixed order: the AFTER section opens with the zero-cannot-act headline and the machine-rendered comparison tables above 26-02's provenance and transcripts, the Outliers (Phase 26) section lists exactly the revisit rows (or says none), and the Handoff to Phase 27 closes the file — every table byte-identical to the script's rendering, committed with verdicts.json.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdicts.json editorial fields -> ledger | the only hand-written text; it must never carry numbers the reader could mistake for measurements, and it must never silently change a band |
| script output -> ledger paste | a paste that is not byte-exact would break "numbers never retyped" and Plan 26-04's re-render assertion |
| revisit list -> next milestone | a lever phrased as an instruction could be mistaken for landed work; every lever is a v1.3 candidate line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-13 | Tampering | hand-edited numbers or bands in verdicts.json | high | mitigate | only verdict/reason/lever are edited; the re-render fixed-point check (cmp rc=0) proves every other field is machine-derived |
| T-26-14 | Tampering | Markdown pasted with edits or reordered tables | high | mitigate | includes() assertion of each --section output against the ledger; 26-04 re-asserts it as a standing test |
| T-26-15 | Repudiation | judging around a cannot-act cell | high | mitigate | --gate re-run in preflight; rc=3 => Rule 4 STOP before any verdict is written |
| T-26-16 | Elevation of Privilege | a revisit lever landing as an engine change in this phase | medium | mitigate | files_modified is docs-only; git diff --quiet against the pin over engine content src mazeworld.html tools after the commit |
| T-26-17 | Tampering | verdict on the depth-20 slice | medium | mitigate | deep rows carry no band/verdict keys (asserted); the deep tables are floors-gained/encounters-survived only |
| T-26-18 | Information Disclosure | ledger contents | low | accept | aggregate numbers about a fictional dungeon; no personal data |
| T-26-SC | Tampering | npm/pip/cargo installs | low | accept | no installs in this plan |
</threat_model>

<verification>
- `docs/class-pass/verdicts.json`: schema ok, 24 subs / 6 races, cannotAct empty, every out-of-band row has verdict + reason, every revisit row has a lever, no revisit on an in-band row, no verdict key on any deep row, and the file is a fixed point of the script's merge.
- `docs/CLASS-PASS.md`: eight H2 headings in the fixed order ending with Handoff; the AFTER section's first line is the zero-cannot-act headline; the three script sections are byte-exact substrings; the good/bad table header appears once; only lines were added relative to HEAD~1.
- Engine/harness untouched: `git diff --quiet <pin> -- engine content src mazeworld.html tools` exits 0; `npm test` green.
</verification>

<success_criteria>
- Every sub-class and race row has a band and, where out of band, a written accept/revisit verdict with a reason; every revisit row names a v1.3 lever; the ledger's AFTER, Outliers and Handoff sections are machine-rendered from verdicts.json and the two AFTER JSONs.
- Phase 27 can read the Handoff block and know mu, the band edges, the reach table, the depth-20 yardstick, and which strong rows were deliberately accepted.
</success_criteria>

<output>
Create `.planning/phases/26-mass-playtest-class-pass-ledger/26-03-SUMMARY.md` when done. Record: mu BEFORE/AFTER and the band edges; the by-class BEFORE->AFTER means; the list of out-of-band sub/race rows with their band and verdict; the revisit rows with levers (or "none"); Wilmsry's disposition; the one-sentence answers to "which classes over/under-perform now" and "did the caster problem go away"; the in-band cell count; the ledger commit hash.
</output>
