---
phase: 24-every-sub-class-and-race-one-good-one-bad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/combat.js
  - engine/derived.js
  - mazeworld.html
  - src/browser/eventNarration.js
  - test/unit/identity-combat.test.js
  - test/unit/parley-button-mirror.test.js
autonomous: true
requirements: [IDENT-05, IDENT-06, IDENT-07, FID-07]

must_haves:
  truths:
    - "A Knight never wins initiative while any live foe in the encounter has maxWP >= 20 (a foe at 19 does not trigger; 20 does); a foreseen Knight still goes first, exactly as the Samurai does (D: Knight bad, IDENT-05)"
    - "A Court Mage's foes act first in round one only (round 2+ rolls normally), unless foreseen; a Court Mage's boredom kill fires on d12 in {1,2} only (a 3 never fires) on the same single draw; a Court Mage can parley Humans even at fluency 0 (D: Court Mage bad + good, IDENT-05/IDENT-06)"
    - "A Ninja and a Master of Arms return false from canParley for every encounter type regardless of fluency (the sub gate wins), and a direct parley() emits parleyRefused with reason ninja / masterOfArms, consuming zero rng and never setting parleyTried (D: Ninja/MoA bads, IDENT-05)"
    - "Two sub-classes share one mechanism without collision: Ninja and Master of Arms both return false from canParley via separate named reasons; Knight-vs-big-foe and Court Mage both use the never-first initiative structure alongside Samurai/Fridgian, and a character matching two of these (e.g. a Fridgian Court Mage) goes last exactly once (probe IDENT-05 adjacency)"
    - "In flee(), the Master of Arms tracked-withdrawal denial is checked inside the C.tracked && C.round === 1 branch before the clean exit; the Cloaker's !C.opened2 gate is checked on the free-vanish branch; the Samurai never-flee check stays first; the decision order in flee() and canParley() is documented and mirrored in mazeworld.html's classic canParley (parley-button-mirror test) (probe IDENT-05 ordering)"
    - "Court Mage boredom fires on d12 in {1,2} only; a Court Mage can parley Humans even at fluency 0 (additive to the Bard-vs-Humans line); Guard's -1 applies before the floor-1 clamp so a Guard facing a foe already at need 1 stays at 1 (probe IDENT-06 boundary)"
    - "Boredom probability is exactly 2/12 = 1/6 on a single existing draw; Guard's modifier is an integer -1 (probe IDENT-06 precision)"
    - "foeToHitVs is 4 for a Guard (5 - 1), 3 for a Guard with Agility, never below 1 (D: Guard good, IDENT-06)"
    - "A Cloaker vanishes for free only while C.opened2 is falsy; once truthy, flee emits vanishDenied and falls through to the ordinary Thief roll (d20 + 5 vs 11) (D: Cloaker bad, IDENT-07)"
    - "A tracked Master of Arms in round 1 emits withdrawalDenied and rolls the ordinary flee die instead of the clean exit; every other Fighter's tracked round-1 clean exit is byte-identical to today (D: MoA bad, IDENT-05)"
    - "In party play a foe with intel <= 3 swings at a Bard hero instead of any party member; the pickFoeTarget draw still fires exactly once (zero new draws) and a solo Bard, a non-Bard hero, or a foe with intel >= 4 sees today's behaviour (D: Bard bad, party clause, IDENT-05)"
    - "Every new event type (withdrawalDenied, vanishDenied) and every new reason/flag (parleyRefused ninja/masterOfArms, encounterStarted knightBigFoe/courtMageTalksFirst) has an EVENT_NARRATION line; coverage guard and voice scan green (FID-07)"
  artifacts:
    - "engine/combat.js — knightFacesBigFoe(state) exported helper; rollInitiative never-first clause for Knight-vs-big-foe (every round) and Court Mage (round 1); boredom d12 <= 2; canParley Ninja/MoA false + Court Mage-vs-Humans true; parley() Ninja/MoA parleyRefused; flee() Cloaker !C.opened2 gate + vanishDenied, MoA withdrawalDenied; pickFoeTarget(state, rng, foe) Bard clause"
    - "engine/derived.js — foeToHitVs Guard -1 beside Agility"
    - "mazeworld.html — classic canParley() mirror carries the same two new lines"
    - "src/browser/eventNarration.js — entries for withdrawalDenied, vanishDenied; extended parleyRefused and encounterStarted builders"
    - "test/unit/identity-combat.test.js — new focused unit tests for every mechanic above"
    - "test/unit/parley-button-mirror.test.js — 7-sub matrix (1008 cases) + source pins for the two new mirror lines"
  key_links:
    - "rollInitiative -> startCombat first-move -> foeTurn at encounter start (a 'foe' result gives the foes a free round-one swing)"
    - "afterPlayerAction re-rolls initiative every round with C.round already incremented, so the Court Mage clause reads C.round === 1 only at encounter start"
    - "engine canParley <-> mazeworld.html classic canParley (parley-button-mirror.test.js replays both)"
    - "foeTurn melee swing -> pickFoeTarget(state, rng, f) (the only call site that passes the foe; foeAbilities.js's bolt/drain call stays foe-less and unchanged)"
  prohibitions:
    - "No new rng draw behind a sub-class or race guard — every mechanic in this plan is a flat modifier, a gate, or a wider hit on a roll that already happens"
    - "No new serialized field on state, state.c or state.combat (opened2, tracked, round, foresight and c.sub are the only reads)"
    - "Never edit test/parity/prototype-master.js.txt; never trim, rewrite or regenerate any parity fixture in this plan (no fixture hero is a Knight/Court Mage/Ninja/MoA/Guard/Cloaker in combat, so parity stays byte-identical)"
    - "Phase 23's Wizard strike refusal, Summoner/Illusionist level-1 casting and Freeze payout are untouched"
    - "No toast/UI work beyond EVENT_NARRATION lines and the classic canParley mirror (Phase 25 owns feedback)"
    - "Narration text stays sarcastic, deadpan and family-friendly (test/voice/safety-scan.test.js is the gate)"
---

<objective>
Land the combat-side sub-class goods and bads that live in engine/combat.js and engine/derived.js: the Knight's big-foe initiative bad, the Court Mage's talk-first bad plus its widened boredom kill and Humans parley good, the Ninja's and Master of Arms' no-parley bads (with the mazeworld.html classic canParley mirror updated in lockstep), the Master of Arms' denied clean withdrawal, the Guard's -1 to be hit good, the Cloaker's real bad (free vanish only before the first landed blow), and the Bard's party-play low-wit targeting — all zero-draw, all narrated.

Purpose: IDENT-05/IDENT-06/IDENT-07 for the seven sub-classes whose hooks sit in combat.js/derived.js; every change is felt in play (initiative, parley availability, flee odds, to-hit) and none of them touches a parity fixture's action path.
Output: modified engine/combat.js, engine/derived.js, mazeworld.html (mirror only), src/browser/eventNarration.js; new test/unit/identity-combat.test.js; extended test/unit/parley-button-mirror.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md
@.planning/research/CLASS-AUDIT-2026-09-14.md
@engine/combat.js
@engine/derived.js
@test/unit/parley-button-mirror.test.js
@test/unit/combat.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New exported helper: `knightFacesBigFoe(state)` in engine/combat.js (true when `state.c.sub === "Knight"` and any `state.combat.foes` entry is alive with `maxWP >= 20`).
- New event types: `withdrawalDenied` (`{ type, reason: "masterOfArms" }`), `vanishDenied` (`{ type, reason: "seen" }`).
- New reasons/flags on existing events: `parleyRefused` reasons `"ninja"` and `"masterOfArms"`; `encounterStarted` booleans `knightBigFoe` and `courtMageTalksFirst`.
- Changed signature: `pickFoeTarget(state, rng, foe = null)` (third argument optional; only foeTurn's melee swing passes it).
- New test file: test/unit/identity-combat.test.js.
- No new RACES flags, no new serialized fields, no fixture or comparables.js change in this plan.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Initiative (Knight big-foe, Court Mage round 1), boredom 1-in-6, Guard -1, Cloaker gate, Master of Arms withdrawal, Bard party targeting</name>
  <files>engine/combat.js, engine/derived.js, test/unit/identity-combat.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (Decisions: Knight, Court Mage bad/good, Guard, Cloaker, Master of Arms, Bard party clause; Claude's Discretion on event names and the Bard reinterpretation)
    - engine/combat.js: rollInitiative (~L88-108, the `(samurai || slow) && !foreseen` chain), startCombat (~L126-330: `state.combat = {...}`, `const first = rollInitiative(...)`, the encounterStarted event with `samuraiNeverFirst`, the Knight/Con Artist/Court Mage foe loop with the d12 boredom check, the trailing `if (first === "foe") foeTurn(...)`), playerStrike (~L332-470: `C.opened2` is set true on the first LANDED blow), flee (~L592-640: Samurai refusal, Cloaker free exit, `C.tracked && C.round === 1` clean exit, the d20+5 vs 11 roll), afterPlayerAction (~L870-910: `round++` then `rollInitiative` every round), pickFoeTarget (~L1035-1043), foeTurn's melee swing (~L1290: `const member = pickFoeTarget(state, rng);`)
    - engine/derived.js: foeToHitVs (~L331-343: Acrobat 3, Agility -1, `eff(c, "foeToHit")`, dark/mirror/invis 1, `Math.max(1, h)`)
    - test/unit/combat.test.js: fakeRng/fixedFighter/fixedFloor/fixedState/fixedFoe/fixedCombat helpers (~L47-112) and the Samurai initiative test (~L279-287), the Cloaker flee test (~L756), the tracked-round-1 pursuit test (~L1322-1335) — copy the helper shapes, do not import them
    - test/unit/party-combat.test.js: the pickFoeTarget tests (~L253-300) build `state = { combat: {...} }` with NO `c` — the Bard clause must read `state.c?.sub`
  </read_first>
  <action>
    All edits carry a `// DELIBERATE RULES CHANGE (Phase 24, 2026-09-14, IDENT-05|IDENT-06|IDENT-07): ...` comment naming the prototype behaviour, the new rule and the zero-draw guarantee, matching the file's existing convention. engine/combat.js uses CRLF line endings — keep them.

    1. Knight big-foe never-first (per D: Knight, IDENT-05). Export `knightFacesBigFoe(state)` from engine/combat.js: returns `state.c.sub === "Knight" && !!state.combat && state.combat.foes.some((f) => f.alive && f.maxWP >= 20)`. In `rollInitiative`, compute `const knightBig = knightFacesBigFoe(state);` and `const courtMage = c.sub === "Court Mage" && C.round === 1;` and extend the never-first condition to `(samurai || slow || knightBig || courtMage) && !foreseen`. Both d20s are still drawn first (mine/theirs) — the result is overridden, never the draws. Because afterPlayerAction increments `round` BEFORE it re-rolls, the Court Mage clause is true only for the encounter-start roll (per D: Court Mage bad — foes act first in round one); the Knight clause re-evaluates every round and switches off once every foe with maxWP >= 20 is dead or fled ("never wins initiative vs any live foe with maxWP >= 20"). Update the rollInitiative JSDoc.
    2. encounterStarted flags: in startCombat's `events.push({ type: "encounterStarted", ... })` add `knightBigFoe: knightFacesBigFoe(state)` and `courtMageTalksFirst: c.sub === "Court Mage"` beside `samuraiNeverFirst` (additive fields; no test pins the exact key set of this event — confirm with grep before relying on it).
    3. Court Mage boredom (per D: Court Mage good, IDENT-06): in the startCombat foe loop the Court Mage branch currently compares the d12 to exactly one; change the comparison to `rng.d(12) <= 2` (1-in-6, same single draw, nothing else in the branch changes).
    4. Guard good (per D: Guard, IDENT-06): in engine/derived.js foeToHitVs insert `if (c.sub === "Guard") h -= 1;` directly after the Agility line and before the `eff(c, "foeToHit")` line, so the existing `Math.max(1, h)` floor applies and the dark/mirror/invis `h = 1` overrides still win. Update the JSDoc.
    5. Cloaker bad (per D: Cloaker, IDENT-07): change the free-vanish branch guard to `if (c.sub === "Cloaker" && !C.opened2)` (body unchanged). Directly after that branch add `if (c.sub === "Cloaker") events.push({ type: "vanishDenied", reason: "seen" });` — a Cloaker who has already landed a blow narrates the denial and then continues into the tracked/ordinary-roll code exactly like any Thief (d20 + 5 vs 11). Zero draws added; `opened2` already exists.
    6. Master of Arms withdrawal (per D: Master of Arms, IDENT-05): restructure the tracked branch as `if (C.tracked && C.round === 1) { if (c.sub === "Master of Arms") { events.push({ type: "withdrawalDenied", reason: "masterOfArms" }); } else { <the existing three statements, byte-identical: pursuitStrike/died check, fled reason tracked, endCombat, return> } }` so a Master of Arms falls through to the ordinary roll and every other Fighter's path is unchanged. Update flee's JSDoc decision order: Samurai -> Cloaker (free while unseen) -> tracked round 1 (denied for a Master of Arms) -> d20 roll.
    7. Bard party clause (per D: Bard, IDENT-05; discretion documented here): change the signature to `pickFoeTarget(state, rng, foe = null)`. Keep the existing pool/draw code byte-identical, then after `const pick = rng.d(liveMembers.length + 1);` add: `if (foe && foe.intel <= 3 && state.c?.sub === "Bard") return null;` — an intel-3-or-lower foe always swings at the Bard hero when a live party stands beside them. Rationale to write into the JSDoc: any zero-draw reinterpretation that merely BIASES a (n+1)-sided pick toward the hero must take a slot from one specific member (asymmetric), so the only symmetric zero-draw design is "the dumb creature goes for the Bard"; the draw is still consumed so the party-mode rng stream is unchanged. In foeTurn's melee loop change the call to `pickFoeTarget(state, rng, f)`. Leave engine/foeAbilities.js's two-argument call untouched (an ability-casting foe is not "too stupid to know better").
    8. Tests — create test/unit/identity-combat.test.js with local copies of fakeRng/fixedFighter/fixedFloor/fixedState/fixedFoe/fixedCombat (same shapes as combat.test.js) and these cases:
       - Knight: foes `[fixedFoe({ wp: 20, maxWP: 20 })]`, `rollInitiative(state, fakeRng([20, 1]))` → "foe" (mine 20 >= theirs 1 would otherwise be "you"); maxWP 19 → "you"; the 20-maxWP foe with `alive: false` → "you"; `foresight: true` → "you" and `c.foresight` cleared; every call consumes exactly two draws (the fakeRng throws on a third).
       - Court Mage: round 1 with `[20, 1]` → "foe"; `round: 2` → "you"; `foresight: true` → "you".
       - knightFacesBigFoe: true/false for the cases above; false for a non-Knight facing a 25-maxWP foe.
       - Boredom: drive `startCombat(state, false, "Beasts", rng, events)` on a level-1 Court Mage (Magic User, Cloth) with a scripted fakeRng whose d12 slot is 2 → `foeBored` present and `encounterCleared` (the lone foe dies of boredom; `pick` returns the first roster entry); same script with the d12 slot 3 → no `foeBored`. Measure the exact draw sequence with a recording rng (the order is: d4 foe count, d4 level downgrade, pick roster, d20 mine, d20 theirs, d12 boredom, then killFoe's d6/d10/d20/d6) — adjust the script, never the assertion target.
       - Guard: `foeToHitVs(fixedState({ c: { sub: "Guard" } }))` === 4; with `skills: { Agility: 1 }` === 3; with `mirror: 1` === 1 (override wins); a Soldier stays 5.
       - Cloaker: with `opened2` unset, `flee` emits `fled` reason "cloaker" and no `fleeRolled`, drawing only pursuitStrike's dice; with `opened2: true`, `flee(state, fakeRng([20, ...]))` emits `vanishDenied` then `fleeRolled` then `fled` reason "escaped"; with a low roll it emits `vanishDenied`, `fleeRolled`, `fleeFailed`.
       - Master of Arms: `tracked: true, round: 1` → events contain `withdrawalDenied` and `fleeRolled`, never `fled` with reason "tracked"; a Soldier in the identical setup gets `fled` reason "tracked" and no `fleeRolled`.
       - Bard targeting: state `{ c: fixedFighter({ sub: "Bard" }), combat: { foes: [], allies: [ally Ada wp 20] } }`, foe `fixedFoe({ intel: 1 })`: `pickFoeTarget(state, fakeRng([2]), foe)` === null and the rng is exhausted after exactly one draw; `intel: 4` → Ada; a Soldier hero with intel 1 → Ada; no foe argument → Ada (today's behaviour).
  </action>
  <verify>
    <automated>node --test test/unit/identity-combat.test.js test/unit/combat.test.js test/unit/party-combat.test.js test/unit/foe-turn-draw-count.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function knightFacesBigFoe" engine/combat.js` == 1
    - `grep -c "maxWP >= 20" engine/combat.js` >= 1
    - `grep -c 'c.sub === "Court Mage" && C.round === 1' engine/combat.js` == 1
    - `grep -c "rng.d(12) <= 2" engine/combat.js` == 1
    - `grep -v '^\s*//' engine/combat.js | grep -c "rng.d(12) === 1"` == 0 (the old equals-one boredom check is gone)
    - `grep -c 'if (c.sub === "Guard") h -= 1;' engine/derived.js` == 1
    - `grep -c 'c.sub === "Cloaker" && !C.opened2' engine/combat.js` == 1 and `grep -c '"vanishDenied"' engine/combat.js` == 1
    - `grep -c '"withdrawalDenied"' engine/combat.js` == 1 and `grep -c 'reason: "tracked"' engine/combat.js` == 1 (the non-MoA clean exit survives)
    - `grep -c "export function pickFoeTarget(state, rng, foe = null)" engine/combat.js` == 1 and `grep -c "pickFoeTarget(state, rng, f)" engine/combat.js` == 1 and `grep -c "pickFoeTarget(state, rng)" engine/foeAbilities.js` == 1 (unchanged)
    - `grep -c "knightBigFoe:" engine/combat.js` == 1 and `grep -c "courtMageTalksFirst:" engine/combat.js` == 1
    - `grep -c "DELIBERATE RULES CHANGE (Phase 24" engine/combat.js` >= 5 and `grep -c "DELIBERATE RULES CHANGE (Phase 24" engine/derived.js` >= 1
    - `node --test test/unit/identity-combat.test.js` exits 0 with >= 12 passing tests
    - `node --test test/unit/combat.test.js test/unit/party-combat.test.js test/unit/foe-turn-draw-count.test.js` exits 0 (the existing Samurai/Cloaker/tracked/pickFoeTarget tests and the FID-02 pins are unchanged and green)
    - `node --test "test/parity/**/*.test.js"` exits 0 (no fixture hero exercises any branch touched here)
  </acceptance_criteria>
  <done>Knight/Court Mage never-first, 1-in-6 boredom, Guard -1, Cloaker gate + vanishDenied, MoA withdrawalDenied, and the Bard party clause are implemented with zero new draws and covered by identity-combat.test.js; existing unit and parity suites stay green.</done>
</task>

<task type="auto">
  <name>Task 2: canParley gates (Ninja, Master of Arms false; Court Mage vs Humans true) + parley() refusals + the mazeworld.html classic mirror in lockstep</name>
  <files>engine/combat.js, mazeworld.html, test/unit/parley-button-mirror.test.js, test/unit/identity-combat.test.js</files>
  <read_first>
    - engine/combat.js canParley (~L658-682) and parley() (~L701-730: the parleyTried block, `if (!canParley(state)) return events;`, the Wilmsry-vs-Magical refusal that sits BEFORE `C.parleyTried = true`)
    - mazeworld.html classic `function canParley()` (~L4240-4258) and the D-17 lockstep note in engine/combat.js's canParley JSDoc
    - test/unit/parley-button-mirror.test.js (all 153 lines: the regex extraction requires the classic function body to consist of single-line statements — a nested `}` at column 0 would end the match early; the 576-case matrix over 4 subs; the source-pin test)
    - test/unit/parley.test.js (~L1-120 helpers) for the canParley/parley test idioms
  </read_first>
  <action>
    Per D: Ninja, Master of Arms, Court Mage good (IDENT-05/IDENT-06). Engine and mirror change in the SAME commit.

    1. engine/combat.js canParley: directly after `if (C.parleyTried) return false;` insert one line `if (c.sub === "Ninja" || c.sub === "Master of Arms") return false; // Phase 24 IDENT-05: a Ninja never speaks; a Master of Arms attacks without question`. Directly after the Bard-vs-Humans line insert `if (c.sub === "Court Mage" && t === "Humans") return true; // Phase 24 IDENT-06: courtly manners`. Extend the JSDoc's decision-order note and the D-17 lockstep warning.
    2. engine/combat.js parley(): after the parleyTried block and BEFORE `if (!canParley(state)) return events;` add two guards that push `{ type: "parleyRefused", reason: "ninja" }` / `{ type: "parleyRefused", reason: "masterOfArms" }` and return — zero draws, never touching `C.parleyTried` (a refusal never spends the one attempt, same posture as the Wilmsry-vs-Magical refusal).
    3. mazeworld.html classic canParley(): insert the exact mirrored lines using `S.c.sub`: `if (S.c.sub === "Ninja" || S.c.sub === "Master of Arms") return false;` right after `if (C.parleyTried) return false;`, and `if (S.c.sub === "Court Mage" && t === "Humans") return true;` right after the Bard line. Single-line statements only, no block braces, so the mirror test's regex still captures the whole function. Touch nothing else in mazeworld.html in this task.
    4. test/unit/parley-button-mirror.test.js: widen the matrix sub list to `["Con Artist", "Woodsman", "Bard", "Soldier", "Ninja", "Master of Arms", "Court Mage"]` and the case-count assertion from 576 to 1008 (7 subs x 6 races x 2 x 2 x 6 types); add source pins asserting `cpSrc` includes both new classic lines verbatim; add a test that a Court Mage vs Humans is true on both sides with no Language skill and no Helm, and that a Ninja and a Master of Arms are false on both sides for every ENC_TYPES value even with Language + Helm (fluency 2).
    5. test/unit/identity-combat.test.js: add canParley cases (Ninja false for every type at fluency 2; Master of Arms false for every type; Court Mage vs Humans true at fluency 0, vs Beasts false at fluency 0, vs Walking Dead false) and parley() cases (a Ninja's direct parley emits exactly one `parleyRefused` with reason "ninja", `C.parleyTried` stays undefined, and `fakeRng([])` is never drawn; same for masterOfArms).
  </action>
  <verify>
    <automated>node --test test/unit/parley-button-mirror.test.js test/unit/parley.test.js test/unit/identity-combat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'if (c.sub === "Ninja" || c.sub === "Master of Arms") return false;' engine/combat.js` == 1
    - `grep -c 'if (c.sub === "Court Mage" && t === "Humans") return true;' engine/combat.js` == 1
    - `grep -c 'reason: "ninja"' engine/combat.js` == 1 and `grep -c 'reason: "masterOfArms"' engine/combat.js` == 2 (parley refusal + Task 1's withdrawalDenied)
    - `grep -c 'if (S.c.sub === "Ninja" || S.c.sub === "Master of Arms") return false;' mazeworld.html` == 1 and `grep -c 'if (S.c.sub === "Court Mage" && t === "Humans") return true;' mazeworld.html` == 1
    - `grep -c "^function canParley() {" mazeworld.html` == 1 (the classic function still appears exactly once)
    - `grep -c "assert.equal(cases, 1008);" test/unit/parley-button-mirror.test.js` == 1 and `grep -c '"Master of Arms", "Court Mage"' test/unit/parley-button-mirror.test.js` == 1
    - `node --test test/unit/parley-button-mirror.test.js` exits 0 (all 1008 matrix cases agree; new source pins pass)
    - `node --test test/unit/parley.test.js test/unit/identity-combat.test.js` exits 0
  </acceptance_criteria>
  <done>A Ninja and a Master of Arms can never parley (engine + classic mirror + 1008-case mirror proof), a Court Mage can always parley Humans, and a direct Ninja/MoA parley narrates a refusal without spending the attempt or drawing rng.</done>
</task>

<task type="auto">
  <name>Task 3: EVENT_NARRATION entries for the new events, reasons and flags; coverage guard + voice scan + full suite green</name>
  <files>src/browser/eventNarration.js</files>
  <read_first>
    - src/browser/eventNarration.js: header (tone rules), the `encounterStarted`, `foeBored`, `fled`, `fleeRolled`, `parleyRefused`, `fleeRefused` builders (~L129-190)
    - test/unit/formatEventsCoverage.test.js (both guards: every engine-emitted type narrated; no dead entries)
    - test/voice/safety-scan.test.js header (what is scanned; banned-word policy)
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md `<specifics>` (voice: funny and specific)
  </read_first>
  <action>
    Add/extend builders (deadpan, family-friendly; exact sentences are discretion — the lines below are suggestions):
    - `withdrawalDenied`: e.g. `<span class="miss">Slipping away untouched would mean not attacking. You attack. Roll like everyone else.</span>`
    - `vanishDenied`: e.g. `<span class="miss">You can always vanish — as long as nobody has seen your face. They have now seen your face.</span>`
    - `parleyRefused`: extend the existing reason switch with `"ninja"` (e.g. "A Ninja does not speak. Least of all to them.") and `"masterOfArms"` (e.g. "A Master of Arms has one answer to a question like that, and it is not a sentence."), keeping the wilmsryVsMagical text and the generic fallback byte-identical.
    - `encounterStarted`: append a clause when `e.knightBigFoe` (e.g. " Something with real heft has noticed the Knight. It moves first.") and when `e.courtMageTalksFirst` (e.g. " You open with a few words. They open with everything else."); the existing banner/names text stays first and unchanged when neither flag is set.
    Every builder must survive being called with only `{ type }` (the coverage test does exactly that) — guard every field with `??`/`?.`.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "  withdrawalDenied:" src/browser/eventNarration.js` == 1 and `grep -c "  vanishDenied:" src/browser/eventNarration.js` == 1
    - `grep -c '"ninja"' src/browser/eventNarration.js` == 1 and `grep -c '"masterOfArms"' src/browser/eventNarration.js` == 1
    - `grep -c "knightBigFoe" src/browser/eventNarration.js` >= 1 and `grep -c "courtMageTalksFirst" src/browser/eventNarration.js` >= 1
    - `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js` exits 0
    - `npm test` output ends with `# fail 0` and the pass count is >= 1036 + the new tests added by Tasks 1-2
    - `node --test "test/parity/**/*.test.js"` exits 0 with zero changes under test/parity/ (`git status --porcelain test/parity` prints nothing)
  </acceptance_criteria>
  <done>Every event type/reason/flag introduced by this plan has a narration line, the coverage guard and voice scan are green, and the whole suite (unit + parity) passes with no parity file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine -> browser narration | Engine event payloads (names of foes/items) are interpolated into HTML narration strings |
| player-facing copy | Authored narration must stay family-friendly (store rating) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-01 | Tampering | eventNarration.js builders | low | accept | Interpolated values come only from the engine's own content tables (bestiary/subclass names), never from user text; existing builders follow the same pattern |
| T-24-02 | Information Disclosure | narration voice | low | mitigate | test/voice/safety-scan.test.js scans every new builder; family-friendly wording only |
| T-24-03 | Denial of Service | rollInitiative/flee gates | low | mitigate | Pure, zero-draw gates; unit tests prove no refusal loop (MoA/Cloaker fall through to the ordinary roll; the tuning bot checks canParley before parleying) |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase (package.json untouched); nothing to audit |
</threat_model>

<verification>
- `node --test test/unit/identity-combat.test.js` green with >= 15 tests covering every mechanic in this plan.
- `node --test test/unit/parley-button-mirror.test.js` green at 1008 cases.
- `npm test` ends `# fail 0`; `node --test "test/parity/**/*.test.js"` green; `git status --porcelain test/parity` empty.
- Engine gate: `git diff --stat -- test/parity/harness/comparables.js test/parity/prototype-master.js.txt test/parity/fixtures` shows 0 lines.
</verification>

<success_criteria>
- Knight never wins initiative vs a live maxWP >= 20 foe unless foreseen; Court Mage foes act first in round one; boredom is 1-in-6; Court Mage parleys Humans; Ninja/MoA never parley (engine + mirror); MoA has no clean tracked withdrawal; Guard is hit on one better; Cloaker vanishes free only before the first landed blow; dumb foes go for a Bard in party play.
- Zero new rng draws, zero new serialized fields, zero parity changes.
- Every new event/reason/flag narrated; voice scan green.
</success_criteria>

<output>
Create `.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-01-SUMMARY.md` when done, recording: the exact draw script measured for the boredom test, the Bard reinterpretation decision (always-the-Bard for intel <= 3, draw preserved) and any narration sentences chosen.
</output>
