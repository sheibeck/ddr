---
phase: 24-every-sub-class-and-race-one-good-one-bad
plan: 03
type: execute
wave: 2
depends_on: ["24-01", "24-02"]
files_modified:
  - content/races.js
  - engine/combat.js
  - src/browser/eventNarration.js
  - test/unit/combat.test.js
  - test/unit/identity-race.test.js
  - test/unit/foe-turn-draw-count.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixture-inventory.test.js
  - test/parity/FIXTURE-INVENTORY.md
autonomous: true
requirements: [IDENT-08, IDENT-09, FID-07]

must_haves:
  truths:
    - "A Fridgian's hide soaks 2 from every blow that reaches applyFoeDamageToPlayer (foe swing, pursuit strike, ability bolt), floor 1 per the Hardiness convention, stacking with Hardiness's -3; a Human takes the full amount (D: Fridgian (b))"
    - "A Fridgian's frenzy second swing is never spent on a corpse: with a dead foe present and the d8 frenzy roll at or under 5, both swings target the live foe, no wasted-swing event exists anywhere in engine/ or src/, and the corpse-whiff d10 is no longer drawn (D: Fridgian (a))"
    - "A Dwarf's armour loses Math.ceil(dmg / 2) durability on a soaked blow (a 1-point over-min hit still costs 1) while every other race loses dmg; armorWP still floors at 0 (D: Dwarven)"
    - "Every new mechanic is zero-draw (flat modifier, gate, or wider hit on an existing roll), so rng consumption order is unchanged for every seed EXCEPT where the Fridgian corpse-whiff draw is removed; the planner measures whether any parity fixture's action path has a Fridgian hero frenzying with a dead foe present and, if so, declares that scenario as a divergence (never blanket) — MEASURED: combat/lose (seed 14) is that scenario; combat/flee (seed 17) and every other fixture stay byte-identical (probe FID-07 ordering)"
    - "Sub-class and race identity are the exact canonical strings from content/classes.js and content/races.js; new race traits, where implemented as data, live as flags on RACES (hide, armorWear) read by the engine, never string-matched from flavor text (probe IDENT-05 encoding)"
    - "combat/lose (seed 14) carries an action-path divergence record with fromAction 1 whose before (prototype: wp 0, sp 5, kills 1, rations 7, dead true) and after (engine: wp 22, sp 30, kills 2, rations 8, dead false) are measured live and asserted on both sides at every replay site"
    - "A new combat scenario lose-apprentice (seed 127, Apprentice/Human, startCombat Beasts + 14 attacks, roster Bat/Rat + Shriek) dies on BOTH sides byte-identically, restoring death-path parity coverage; the parity-exposed bestiary surface stays exactly 2 types x 4 names"
    - "The FID-02 attack-loop pins are re-measured, not adjusted: seed 14 -> 87 draws / 8 attacks / won, seed 17 -> 101 draws / 9 attacks / won, new row seed 127 -> 119 draws / 14 attacks / died; seeds 3, 303, 8 unchanged (12/66/32); each with a Phase 24 provenance comment"
    - "Human, Elven, Wilmsry and Troll carry no hide/armorWear flag and take today's damage/wear; Fridgian keeps noArmor and slow; Dwarven keeps dmg 2, upkeep 1 and foeStrikeStep 1"
  artifacts:
    - "content/races.js — Fridgian `hide: 2`, Dwarven `armorWear: 0.5`"
    - "engine/combat.js — applyFoeDamageToPlayer hide line beside Hardiness; Dwarven half-wear on the armour-durability line; frenzy block without the corpse-whiff branch"
    - "src/browser/eventNarration.js — the wasted-frenzy entry removed (coverage guard: no dead entries)"
    - "test/unit/identity-race.test.js — hide/wear/frenzy unit tests; races.js flag assertions"
    - "test/unit/combat.test.js — the frenzy test rewritten for the always-live second swing"
    - "test/parity/fixtures/action-script.combat.json — lose scenario `divergence` record (kind action-path) + new `lose-apprentice` scenario"
    - "test/unit/foe-turn-draw-count.test.js — FULL_FIGHTS rows re-measured + seed 127 row"
    - "test/parity/fixture-inventory.test.js + test/parity/FIXTURE-INVENTORY.md — pinned roster and generated block regenerated together (lose-apprentice row)"
  key_links:
    - "RACES[c.race].hide -> applyFoeDamageToPlayer (the ONLY hero-damage seam: foeTurn swing, pursuitStrike, foe ability bolts all route here)"
    - "RACES[c.race].armorWear -> the `dmg > av.min` armour-durability line (soak roll unchanged; only the subtracted amount changes)"
    - "engine/combat.js frenzy block -> the seed-14 rng cursor (one d10 fewer from the first frenzy with a corpse present: action 7 of combat/lose)"
    - "action-script.combat.json lose.divergence -> actionPathDivergenceOf/skipsByteDiffAt/declaredEndDiffs (Plan 24-02) at combat-parity.test.js and full-suite.test.js"
    - "fixtureRoster.js enumeration -> fixture-inventory.test.js pinned array -> FIXTURE-INVENTORY.md generated block (all three change together)"
  prohibitions:
    - "Never edit test/parity/prototype-master.js.txt"
    - "Never trim, rewrite or re-seed the existing lose scenario (seed 14, 10 attacks) — declare it; adding lose-apprentice is additive"
    - "No new rng draw behind a race guard; the whiff removal REMOVES a draw and is the only cursor change, declared on the one fixture it touches"
    - "No new serialized field (hide/armorWear are content data read at damage time)"
    - "The Human race gets NO mechanic; Elven and Troll mechanics unchanged; Fridgian keeps noArmor and always-last; Wilmsry heal/parley/haggle numbers unchanged"
    - "FID-02 pins are measured with countingRng against the patched engine, never hand-computed"
---

<objective>
Land the race pass that touches combat.js — the Fridgian's frenzy never wasting its second swing on a corpse and its hide soaking 2 from every blow, and the Dwarf's armour wearing at half rate — as RACES data flags, and declare the ONE parity fixture whose action path this measurably changes (combat/lose, seed 14, a Fridgian Soldier) with a machine-checked action-path record, re-measure the FID-02 draw pins, and add a phase-24-neutral `lose-apprentice` scenario so the parity suite keeps exercising the death path on both sides.

Purpose: the Fridgian and Dwarven goods promised by the user's race pass, plus FID-07's discipline (declared, measured divergence; no blanket regeneration; prototype master untouched).
Output: races.js flags; combat.js edits; narration entry removed; new identity-race tests; rewritten frenzy test; fixture record + new scenario; re-measured pins; regenerated inventory roster.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-04-SUMMARY.md
@engine/combat.js
@content/races.js
@test/parity/FIXTURE-INVENTORY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New RACES flags: `Fridgian.hide = 2`, `Dwarven.armorWear = 0.5` (content/races.js).
- Removed event type: the Fridgian wasted-swing event (engine emission and its EVENT_NARRATION entry both deleted).
- New parity scenario: `lose-apprentice` (seed 127) in action-script.combat.json.
- New declared divergence: `lose` scenario `divergence` record, kind `action-path` (the first consumer of Plan 24-02's helpers).
- New test file: test/unit/identity-race.test.js.
- Re-measured FID-02 pins (foe-turn-draw-count.test.js FULL_FIGHTS) + regenerated inventory roster (doc + pinned array).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RACES flags + Fridgian hide/whiff + Dwarven half-wear in combat.js; narration entry removal; unit tests (parity temporarily red by design until Task 2)</name>
  <files>content/races.js, engine/combat.js, src/browser/eventNarration.js, test/unit/combat.test.js, test/unit/identity-race.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (Race pass: Dwarven, Fridgian (a)/(b); "prefer data flags")
    - content/races.js (the flag style: frenzy/slow/noArmor/heal2x/spMul)
    - engine/combat.js: playerStrike's frenzy block (~L370-378: the d8 <= 5 frenzy, then the dead-foe lookup and the d10 whiff branch that returns early) and the playerStrike JSDoc (~L325-331) that mentions the wasted swing; applyFoeDamageToPlayer (~L1092-1165: the Hardiness line `dmg = Math.max(1, dmg - 3)`, the ward block, the armour soak block with the durability subtraction on the `dmg > av.min` line)
    - src/browser/eventNarration.js (~L156-157: the frenzy pair of entries)
    - test/unit/combat.test.js (~L358-374: the frenzy-wasted test to rewrite; helpers ~L47-112)
    - test/unit/formatEventsCoverage.test.js (the "no dead entries" guard — why the narration entry must go in the same commit)
  </read_first>
  <action>
    <!-- planner-discipline-allow: frenzyWasted -->
    Per D: Fridgian (a) and (b), D: Dwarven. Each engine edit carries a `// DELIBERATE RULES CHANGE (Phase 24, 2026-09-14, race pass / IDENT-08): ...` comment naming the prototype behaviour and the new rule. combat.js is CRLF — preserve.

    1. content/races.js: add `hide: 2` to Fridgian and `armorWear: 0.5` to Dwarven (keep every existing flag and the `note` strings; add a one-line comment per flag: "hide: flat damage soaked from every blow, read by applyFoeDamageToPlayer"; "armorWear: fraction of a soaked blow charged to armour durability, Math.ceil'd, read by applyFoeDamageToPlayer").
    2. engine/combat.js applyFoeDamageToPlayer: add `const R = RACES[c.race];` after `const c = state.c;` and insert `if (R.hide) dmg = Math.max(1, dmg - R.hide);` directly AFTER the Hardiness line (stacking: Hardiness first, hide second, both floor 1 — the existing Hardiness convention). On the armour-durability line replace the subtracted amount with `(R.armorWear ? Math.ceil(dmg * R.armorWear) : dmg)` so a non-Dwarf's expression is value-identical to today (`c.armorWP = Math.max(0, c.armorWP - <amount>)`; the `!av.magic && dmg > av.min` guard is unchanged).
    3. engine/combat.js playerStrike frenzy block: delete the corpse lookup and the whole early-return branch that rolled a d10 against a dead foe (the "wasted swing" path), leaving `attacks = 2; events.push({ type: "frenzy" });` inside the `R.frenzy && rng.d(8) <= 5` block. The attack loop already targets `t` (the live target), so the second swing now always lands on a live foe. Update the playerStrike JSDoc phrase about the wasted swing. This REMOVES one d10 draw whenever a Fridgian frenzies with a corpse present — the only rng-cursor change in this phase; Task 2 declares its single fixture consequence.
    4. src/browser/eventNarration.js: delete the `frenzyWasted` entry (the engine no longer emits it; the coverage guard rejects dead entries). Keep the `frenzy` entry.
    5. test/unit/combat.test.js: rewrite the frenzy test (~L358) as "Fridgian frenzy grants a second wild swing that always targets a live foe": same Corpse + Target(wp 999) combat with `target: 1`; script the draws as d8 5 (frenzy), two strike rolls that miss (e.g. 20, 20 — the second swing needs 3), then the foe turn / initiative / second foe turn exactly as the old comment described (20, 15, 10, 20). Assert: `frenzy` present; exactly two `strikeMissed` events, both `target === "Target"`; no event type contains "Wasted"; the fakeRng is fully consumed (a further `rng.d(10)` throws "sequence exhausted") — proving the d10 whiff draw is gone. Measure the sequence with a recording rng if it disagrees; never weaken the assertions.
    6. Create test/unit/identity-race.test.js (local helper copies; import RACES, applyFoeDamageToPlayer, playerStrike, makeRng):
       - RACES flags: `RACES.Fridgian.hide === 2`, `RACES.Dwarven.armorWear === 0.5`, and `["Human","Elven","Wilmsry","Troll"].every(r => !("hide" in RACES[r]) && !("armorWear" in RACES[r]))`; Fridgian still has `noArmor` and `slow`; Dwarven still has `dmg 2`, `upkeep 1`, `foeStrikeStep 1`; `RACES.Human` keys are exactly size/upkeep/note.
       - Hide: a Fridgian (armor "Nothing", ar 0, armorWP 0) hit via `applyFoeDamageToPlayer(state, fixedFoe(), fakeRng([]), events, { dmg: 5, roll: 3, need: 5 })` loses exactly 3 wp and the `struckByFoe.dmg` is 3; dmg 2 → 1 (floor); dmg 1 → 1; with `skills: { Hardiness: 1 }` dmg 7 → 2 (7-3-2); the same call on a Human loses 5; zero draws in every case (fakeRng([]) untouched — a Fridgian has no armour so no soak die).
       - Dwarven wear: a Dwarf in Leather (ar 6, armorMin 1, armorWP 15, armorMax 15) with the soak die scripted to 1: dmg 5 → `armorSoaked` and `armorWP === 12`; dmg 4 → 13; dmg 1 → 15 (not over min, unchanged behaviour); a Human in the same Leather with dmg 5 → 10; the events never include struckByFoe on a soaked blow.
       - Frenzy never wasted: reuse the rewritten combat.test.js scenario shape and additionally assert with a counting wrapper that the number of draws equals the script length.
    Expect `node --test "test/parity/**/*.test.js"` to be RED for combat/lose after this task — that is the measured divergence Task 2 declares; commit Tasks 1 and 2 together (Phase 23-04 precedent), run the unit suites now.
  </action>
  <verify>
    <automated>node --test test/unit/identity-race.test.js test/unit/combat.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "hide: 2" content/races.js` == 1 and `grep -c "armorWear: 0.5" content/races.js` == 1
    - `grep -c "if (R.hide) dmg = Math.max(1, dmg - R.hide);" engine/combat.js` == 1
    - `grep -c "Math.ceil(dmg \* R.armorWear)" engine/combat.js` == 1
    - `grep -v '^\s*//' engine/combat.js | grep -c "rng.d(10) <= 5"` == 0
    - `grep -v '^\s*//' engine/combat.js | grep -c "frenzyWasted"` == 0 and `grep -c "frenzyWasted" src/browser/eventNarration.js` == 0
    - `grep -c 'type: "frenzy" ' engine/combat.js` == 1 (the frenzy event itself survives)
    - `grep -c "DELIBERATE RULES CHANGE (Phase 24" engine/combat.js` >= 8 (Plan 24-01's five plus three here)
    - `node --test test/unit/identity-race.test.js` exits 0 with >= 8 passing tests
    - `node --test test/unit/combat.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js` exits 0
  </acceptance_criteria>
  <done>Fridgian hide/whiff and Dwarven half-wear are data-flag-driven engine behaviour with unit proof; the dead narration entry is gone; only the declared combat/lose parity scenario is red, pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Measure and declare the combat/lose (seed 14) action-path divergence; add lose-apprentice (seed 127); re-measure FID-02 pins</name>
  <files>test/parity/fixtures/action-script.combat.json, test/unit/foe-turn-draw-count.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-02-SUMMARY.md (helper signatures: actionPathDivergenceOf/skipsByteDiffAt/declaredEndDiffs; record shape documented in test/parity/fixtures/action-script.schema.md)
    - test/parity/fixtures/action-script.combat.json (the `_note`, the four scenarios; `lose` = seed 14, startCombat Beasts + 10 attacks)
    - test/parity/combat-parity.test.js (how a record is consulted; the `lose` post-assertions now gated on no record)
    - test/unit/foe-turn-draw-count.test.js (~L244-270 runFullFight + FULL_FIGHTS; ~L545 the restated-totals test; the file's "pins are measured, not adjusted" rule and its provenance-comment style)
    - test/parity/harness/sandboxPrototype.js loadPrototypeSandbox (to measure the prototype side live)
  </read_first>
  <action>
    Planner pre-measurement (2026-09-14, scratch copy of the engine with Task 1's three edits, replayed with the parity harness; the executor MUST re-measure live and write the measured values, never these predictions, into the record):

    combat/lose, seed 14 (Fighter / Soldier / Fridgian, no armour, wp 52; foes Bat/Rat wp 1 + Shriek wp 3):
    | action | prototype (frozen master) | engine (Phase 24) | cause |
    |---|---|---|---|
    | 0 startCombat | identical | identical | — |
    | 1-6 attack | hero wp 32 / 16 / 14 / 8 / 5 / 5; Bat/Rat killed at action 5 (sp 5, gold 51, kills 1, rations 7) on both sides | hero wp 38 / 26 / 25 / 22 / 21 / 21; same kill, same rng cursor after every action | hide: every struckByFoe dmg is 2 lower (floor 1); zero draw change |
    | 7 attack | frenzy; d10 corpse-whiff drawn (no whiff); two misses | frenzy; NO d10 drawn; two misses with different rolls | whiff removal: the rng cursors diverge here |
    | 8 attack | frenzy, misses | Shriek killed: sp 30, kills 2, rations 8, wp 22, combat null | diverged stream |
    | 9-10 attack | frenzy wasted twice; struckByFoe 14 -> died, wp 0 | no-op (combat already null) | — |
    End state pins: prototype `c.wp 0, c.sp 5, c.kills 1, c.rations 7, dead true`; engine `c.wp 22, c.sp 30, c.kills 2, c.rations 8, dead false`; `c.gold` 51 and `combat` null on both.

    1. Measure live: write a scratch script (scratchpad, not the repo) that loads `loadPrototypeSandbox({ seed: 14 })` and `newRun(14)`, replays the scenario's actions on both sides exactly as combat-parity.test.js does, prints both sides' `c.wp/sp/kills/rations/gold`, `dead`, `combat === null` after every action plus `rngState`, and confirms the first action whose comparable diverges is index 1 and the first action whose rngState diverges is index 7. Also replay `flee` (seed 17) and confirm it is still byte-identical (the fixture's flee action never reaches a frenzy).
    2. Declare: add to the `lose` scenario object a `divergence` record: `{ "phase": "24", "requirements": ["FID-07", "IDENT-08"], "kind": "action-path", "fromAction": 1, "fields": ["wp", "sp", "kills", "rations"], "before": {<measured prototype end values>}, "after": {<measured engine end values>}, "stateFields": ["dead"], "stateBefore": { "dead": true }, "stateAfter": { "dead": false }, "rationale": "<one paragraph: Fridgian hide -2 lowers every landed blow from action 1 (no draw change); the removed corpse-whiff d10 shifts the rng cursor from action 7; the hero now kills the Shriek at action 8 and survives where the prototype's Fridgian died at action 10; seed/actions deliberately untouched; death-path parity coverage moves to lose-apprentice>" }`. Append one sentence to the fixture's `_note` naming the record and the new scenario.
    3. Add scenario `{ "name": "lose-apprentice", "seed": 127, "actions": [startCombat wandering false forced "Beasts", then exactly 14 attack actions] }` after `lose`. Planner pre-measurement: seed 127 rolls Magic User / Apprentice / Human (Cloth, wp 33), foes Bat/Rat (wp 1) + Shriek (wp 3) — the same roster as seed 14 — and dies on the 14th attack with identical comparable state on both sides after every action (verified against the frozen prototype). Confirm live; if the 14th attack is not the death, adjust the attack count to the measured death action (never the seed).
    4. FID-02 pins (test/unit/foe-turn-draw-count.test.js): re-measure every FULL_FIGHTS row with runFullFight against the patched engine and update ONLY the rows that changed, each with a `// Phase 24 (2026-09-14, race pass): was N/M/outcome — Fridgian corpse-whiff draw removed + hide -2` provenance comment: predicted seed 14 -> `totalDraws 87, attacks 8, outcome "won"`, seed 17 -> `totalDraws 101, attacks 9, outcome "won"`; add a row for seed 127 (`forced "Beasts", foeNames ["Bat/Rat","Shriek"]`, predicted `totalDraws 119, attacks 14, outcome "died"`); seeds 3/303/8 must be unchanged (12/66/32). Update the restated-totals test title from `(12/101/111/66/32)` to the measured six-row list and extend its Section-provenance comment with the Phase 24 rationale (a DELIBERATE rules change to the Fridgian frenzy, not a seam bug).
    5. Run `node --test "test/parity/**/*.test.js"` — combat-parity and full-suite must be green via the record; fixture-inventory.test.js will now FAIL on the new scenario row — that is Task 3's job.
  </action>
  <verify>
    <automated>node --test test/parity/combat-parity.test.js test/parity/full-suite.test.js test/unit/foe-turn-draw-count.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const f=require('./test/parity/fixtures/action-script.combat.json');const l=f.scenarios.find(s=>s.name==='lose');if(l.seed!==14||l.actions.length!==11)process.exit(1);if(l.divergence.kind!=='action-path'||l.divergence.fromAction!==1)process.exit(2);if(!f.scenarios.find(s=>s.name==='lose-apprentice'&&s.seed===127))process.exit(3)"` exits 0
    - `node -e "const f=require('./test/parity/fixtures/action-script.combat.json');const d=f.scenarios.find(s=>s.name==='lose').divergence;for(const k of d.fields){if(d.before[k]===d.after[k])process.exit(1)}if(d.stateBefore.dead!==true||d.stateAfter.dead!==false)process.exit(2)"` exits 0 (every declared field really differs; the outcome flip is pinned)
    - `grep -c '"name": "lose-apprentice"' test/parity/fixtures/action-script.combat.json` == 1
    - `grep -c "seed: 127" test/unit/foe-turn-draw-count.test.js` >= 1 and `grep -c "Phase 24" test/unit/foe-turn-draw-count.test.js` >= 3
    - `node --test test/parity/combat-parity.test.js test/parity/full-suite.test.js` exits 0
    - `node --test test/unit/foe-turn-draw-count.test.js` exits 0 (every pin measured, including the unchanged 12/66/32 rows)
    - `git diff --stat -- test/parity/prototype-master.js.txt test/parity/fixtures/action-script.magic.json test/parity/fixtures/action-script.economy.json test/parity/fixtures/action-script.encounters.json test/parity/fixtures/action-script.movement.json test/parity/fixtures/action-script.chargen.json` prints nothing
  </acceptance_criteria>
  <done>The seed-14 divergence is declared with live-measured before/after pins asserted at both replay sites, lose-apprentice restores death-path coverage byte-identically, and every FID-02 pin is re-measured with provenance.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the fixture-inventory roster (pinned array + generated doc block) for the new scenario; full suite green</name>
  <files>test/parity/fixture-inventory.test.js, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/fixture-inventory.test.js (~L33-100: the pinned fightRows array, `Array(5).fill("startCombat")`, the 2-types-x-4-names surface test, the none-rows test)
    - test/parity/FIXTURE-INVENTORY.md (~L1-45: "How to regenerate" and the generated-block markers; the prose "exactly 2 types × 4 creature names")
    - tools/fixture-inventory.mjs (run it; paste stdout between the markers)
  </read_first>
  <action>
    1. Run `node tools/fixture-inventory.mjs` and paste its stdout verbatim between the `fixture-inventory:generated:begin/end` markers in test/parity/FIXTURE-INVENTORY.md, replacing the old table (expected: one new row `action-script.combat.json | lose-apprentice | 127 | startCombat | Beasts | Bat/Rat (Beasts lvl 1, wp 1); Shriek (Beasts lvl 1, wp 3)` directly after the `lose` row; every other row unchanged).
    2. In test/parity/fixture-inventory.test.js add the matching pinned row object after the `lose` row (fixture action-script.combat.json, scenario "lose-apprentice", seed 127, forced "Beasts", foes Bat/Rat lvl 1 wp 1 and Shriek lvl 1 wp 3), change `Array(5)` to `Array(6)`, and reword "the five fixture fights" to "six" in the test title; the parity-exposed surface assertion (`["Beasts:Bat/Rat", "Beasts:Shriek", "Beasts:Viper", "Humans:Dante"]`) stays exactly as is (the new seed adds no creature). Add a one-line Phase 24 provenance comment beside the new row (why a scenario was added: the declared seed-14 divergence means the engine no longer dies there).
    3. Do NOT write the prose "Phase 24" divergence section of FIXTURE-INVENTORY.md here — Plan 24-07 owns it (it also needs Plan 24-04's economy numbers). Only the generated block changes in this plan.
    4. `npm test` and the parity suite must be fully green; commit Tasks 1-3 together with the SUMMARY recording the measured tables.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "lose-apprentice" test/parity/FIXTURE-INVENTORY.md` == 1 (the generated row only) and `grep -c "lose-apprentice" test/parity/fixture-inventory.test.js` >= 1
    - `grep -c 'Array(6).fill("startCombat")' test/parity/fixture-inventory.test.js` == 1
    - `node tools/fixture-inventory.mjs | diff - <(sed -n '/fixture-inventory:generated:begin/,/fixture-inventory:generated:end/p' test/parity/FIXTURE-INVENTORY.md | sed '1d;$d')` prints nothing (generated block matches the tool output exactly)
    - `node --test test/parity/fixture-inventory.test.js` exits 0
    - `node --test "test/parity/**/*.test.js"` exits 0
    - `npm test` ends with `# fail 0`
    - `git diff --stat -- test/parity/prototype-master.js.txt` prints nothing
  </acceptance_criteria>
  <done>Roster doc and pinned array agree with the new six-fight roster; the whole suite is green with the seed-14 divergence declared and the prototype master untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/races.js -> engine | Data flags read at damage time; values are integers/fractions authored in-repo |
| fixture record -> parity gate | A declared record narrows the byte diff for one scenario from a declared action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-06 | Repudiation | lose.divergence record | medium | mitigate | before/after measured live and asserted on both sides; lose-apprentice keeps the death path compared byte-for-byte; FID-02 pins re-measured with provenance |
| T-24-07 | Tampering | prototype-master.js.txt / other fixtures | high | mitigate | git diff acceptance criteria prove they are untouched |
| T-24-08 | Denial of Service | applyFoeDamageToPlayer floors | low | accept | `Math.max(1, ...)` and `Math.max(0, ...)` floors keep damage/wear non-negative; unit tests cover dmg 1/2 edges |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase |
</threat_model>

<verification>
- `node --test test/unit/identity-race.test.js test/unit/combat.test.js` green.
- `node --test "test/parity/**/*.test.js"` green with the declared seed-14 record and the new lose-apprentice scenario.
- `node --test test/unit/foe-turn-draw-count.test.js` green with re-measured pins.
- `npm test` ends `# fail 0`.
</verification>

<success_criteria>
- Fridgian hide -2 (floor 1, stacks with Hardiness), frenzy second swing never wasted (d10 removed), Dwarven half armour wear — all as RACES data flags.
- Exactly one fixture scenario diverges and it is declared, measured and asserted; death-path parity coverage preserved via lose-apprentice; roster/pins regenerated together with rationale.
</success_criteria>

<output>
Create `.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-03-SUMMARY.md` when done, quoting verbatim: the seed-14 per-action before/after table, the end-state pins written into the record, the seed-17 byte-identical confirmation, the lose-apprentice measurement (hero, roster, death action), and the FID-02 before/after pins — Plan 24-07 transcribes these into FIXTURE-INVENTORY.md's Phase 24 section.
</output>
