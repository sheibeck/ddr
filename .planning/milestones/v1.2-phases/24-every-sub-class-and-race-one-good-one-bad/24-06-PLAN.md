---
phase: 24-every-sub-class-and-race-one-good-one-bad
plan: 06
type: execute
wave: 4
depends_on: ["24-01", "24-03", "24-04", "24-05"]
files_modified:
  - test/unit/identity-contract.test.js
autonomous: true
requirements: [IDENT-08, IDENT-05, IDENT-06, IDENT-07]

must_haves:
  truths:
    - "Knight's big-foe threshold is maxWP >= 20 exactly: a foe at 19 does not trigger, 20 does (test both); Bard wake roll widens to d20 <= 2 exactly (a 3 never wakes); Court Mage boredom is d12 <= 2 exactly (a 3 never fires); Guard/Fridgian/Dwarven modifiers apply at every value with the existing floors (foe to-hit floor 1; damage floor per the Hardiness convention; armorWP floor 0) (probe FID-07 boundary)"
    - "Stacking is additive and documented: Guard -1 stacks with Agility -1 (floor 1) in foeToHitVs; Fridgian hide -2 stacks with Hardiness -3 in applyFoeDamageToPlayer; a Knight who is also foreseen (Sense Danger) still goes first, exactly as the Samurai does; a Master of Arms or Ninja with fluency still cannot parley (the sub gate wins) (probe FID-07 adjacency)"
    - "Applying the same forced scenario twice yields identical results (pure engine); the identity-contract test builds each scenario from newRun(seed, [], {force}) so it is reproducible byte-for-byte; the Joiner refusal consumes exactly the same draws as an accepted offer (roll first, refuse after) (probe FID-07 idempotency)"
    - "An encounter with no foe at maxWP >= 20 leaves the Knight's initiative untouched; a solo Bard never triggers the party-targeting clause; a Cutthroat or Wilmsry who never meets a Joiner sees no change; a Pickpocket who never enters a store sees no change; each is asserted as a negative case in the identity-contract table (probe IDENT-05 empty)"
    - "A Woodsman offered no armor, a Pilfer with an empty items array, and a Cloaker who has not entered combat see no behavior change (negative cases); refusals fire only on the gated action (probe IDENT-07 empty)"
    - "The identity-contract table has exactly one good and one bad entry per sub-class (24) and per non-Human race (5) plus a Human-neutral entry; a sub-class whose good and bad touch the same hook (e.g. Cloaker flee) is asserted with two distinct scenarios (probe IDENT-08 adjacency)"
    - "The table is asserted complete by a meta-test: every key of CLASSES[*].subs and every key of RACES appears in the table (Human as neutral), so adding a sub-class or race later fails the test until an entry exists (probe IDENT-08 empty)"
    - "Table iteration order is the canonical order of CLASSES[*].subs then RACES keys; each entry is an independent node:test case with no shared mutable state, so a failure names the sub-class or race and the good/bad half (probe IDENT-08 ordering)"
  artifacts:
    - "test/unit/identity-contract.test.js — ONE data table (24 subs + 5 races, each { good: {name, scenario, assert}, bad: {name, scenario, assert} }) + a Human-neutral entry + the completeness meta-test + negative cases"
  key_links:
    - "newRun(seed, [], { force: { sub, race } }) (Phase 22-01 seam) -> a canonical forced hero -> synthetic combat/store/camp state -> the engine function under test"
    - "The table's good/bad names are the source for docs/CLASS-PASS.md's Rulings good/bad table (Plan 24-07 transcribes them)"
  prohibitions:
    - "The test never re-implements engine arithmetic as an oracle beyond the literal numbers the decisions fix (20 hp, d20 <= 2, d12 <= 2, ar <= 10, x1.25/x0.75, ceil(dmg/2), -2 hide, -1 Guard)"
    - "No engine, content or fixture change in this plan — a failing entry is reported, never patched around (an engine bug found here goes to the SUMMARY as a blocker)"
    - "The Human race is asserted NEUTRAL only; no Human mechanic is introduced or implied"
    - "Never combine force { sub: 'Samurai', race: 'Fridgian' } (canon-impossible; normalizeForce throws)"
---

<objective>
Write the identity-contract test: one data table covering all 24 sub-classes and the 5 non-Human races, each with a named good and a named bad, every entry built from Phase 22's `newRun(seed, [], { force })` seam plus a synthetic combat/store/camp state, asserting the named mechanic fires — plus Human asserted neutral on every hook, negative cases, and a completeness meta-test against CLASSES/RACES.

Purpose: IDENT-08 — the machine-checked proof that no sub-class or race is penalties-only or perks-only, and the source table for the ledger's good/bad rows.
Output: test/unit/identity-contract.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md
@.planning/research/CLASS-AUDIT-2026-09-14.md
@.planning/phases/22-class-aware-harness-before-matrix/22-01-SUMMARY.md
@engine/combat.js
@engine/derived.js
@engine/items.js
@engine/encounters.js
@engine/economy.js
@engine/movement.js
@engine/magic.js
@content/classes.js
@content/races.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New test file: test/unit/identity-contract.test.js (the IDENT-08 table; no engine/content change).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold + the Fighter and Thief rows (16 subs x good/bad)</name>
  <files>test/unit/identity-contract.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (Contract test decision: one table; scenarios via newRun(seed, [], { force }) + synthetic states; Human neutral)
    - .planning/phases/22-class-aware-harness-before-matrix/22-01-SUMMARY.md and engine/character.js normalizeForce (sub-only force infers cls; Fridgian Samurai refused)
    - .planning/research/CLASS-AUDIT-2026-09-14.md (the existing GOOD/BAD per sub — the hooks to assert) and this phase's SUMMARYs 24-01/24-03/24-04/24-05 (the new hooks and event names)
    - engine/combat.js: rollInitiative, startCombat (Knight small-foe flee, Con Artist d6, Court Mage d12, Warlock boost), playerStrike (Barbarian attacks, Cat Burglar/Ninja auto, Ninja crit <= 2, Cutthroat first-blow crit `C.cut`, Con Artist opener, Guard/Soldier noCrit, Soldier crit-taken <= 2 in foeTurn), flee, canParley, parley bonus (Woodsman +3), songReady, pickFoeTarget
    - engine/derived.js: toHit (Acrobat 5, Cleric 4), foeToHitVs (Acrobat 3, Guard -1, Agility), weaponDamage (Master of Arms +2, Guard -(4-level), Sorcerer cap 9, race dmg), strikeDie (Illusionist d20 until 3, Elven strikeStep), foeDie (Dwarven foeStrikeStep), killSpFor (Barbarian 0.5, Apprentice x2, Wilmsry spMul), schoolBonus/schoolGate/canLearn, spellLevelFor
    - engine/items.js: canEquipWeapon (Acrobat dagger), canEquipArmor/armorRefusalReason (Woodsman, noArmor), gainWilmst (Pickpocket extra take), useItem (Pilfer); engine/encounters.js springTrap (Pilfer disarm, Cat Burglar double, Acrobat +3 dodge), openChest (Pilfer free), meetJoiner; engine/economy.js priceFor/sellPriceFor; engine/movement.js newDay (Soldier/Wilmsry heal x2, Warlock potion dup, Master of Arms patch, Bard wake); engine/magic.js canRead (Pilfer), Apprentice backfire, Summoner doubled, Cleric +3 heal
    - test/unit/combat.test.js and test/unit/parley.test.js helper shapes (fakeRng with `sequence exhausted`, fixedFoe/fixedCombat)
  </read_first>
  <action>
    File layout: local helpers (fakeRng, countingRng, fixedFoe, fixedCombat, `hero(sub, race = "Human", seed = 1)` = `newRun(seed, [], { force: { sub, race } })` returning the full state with `state.combat = null`; `withCombat(state, foes, overrides)` attaches a fixedCombat; small `expectEvent(events, type, fields)` matcher). Then ONE exported-shaped constant `CONTRACT` — an ordered array of `{ key, kind: "sub" | "race", good: { name, run(t) }, bad: { name, run(t) } }` in canonical CLASSES[*].subs order (Magic User, Fighter, Thief as in content/classes.js) followed by RACES order; a Human entry `{ key: "Human", kind: "race", neutral: { name: "no race modifier anywhere", run } }`. A loop registers `test(\`identity-contract ${key} GOOD: ${name}\`)` and `... BAD: ...` per entry (one node:test case per half, no shared mutable state — every run builds its own hero).

    Fighter rows (per D and the audit; each `run` builds the hero, the synthetic state, calls the engine, asserts):
    - Knight — good: startCombat with a forced roster containing a foe of maxWP < 5 → `foeFled` reason "knight" (build via a scripted rng for `startCombat(state, false, "Beasts", ...)` or directly assert on the loop by pre-building `state.combat` and calling the exported helper if one exists — otherwise script the draws as identity-combat.test.js did); bad: `rollInitiative` "foe" vs a live maxWP 20 foe with mine 20 / theirs 1, "you" at 19, "you" when foreseen (boundary + adjacency).
    - Guard — good: `foeToHitVs` 4 (5 for the Human Soldier control), 3 with Agility, 1 when mirror > 0; bad: `weaponDamage` at level 1 is 3 lower than the same hero's formula without the sub (compare a Soldier with identical weapon/race and a fixed rng) and `playerStrike` roll 1 never crits (no `critical`/backstab-style crit event; use the noCrit path).
    - Woodsman — good: `canParley` true vs Beasts and Lair Beasts at fluency 0, false vs Humans; parley bonus +3 visible in `parleyRolled.need` vs a Soldier control; bad: `canEquipArmor` false for Mail (ar 12) and Plate, true for Studded (ar 10) (adjacency at 10/12).
    - Soldier — good: `newDay` camped heal is double a Human Knight control's with the same rng; bad: foeTurn with a foe roll of 2 doubles damage (`struckByFoe.dmg` = 2x the Knight control's for the same script).
    - Barbarian — good: two `strikeMissed`/`struck` events per `playerStrike` (attacks 2); bad: `killSpFor` (or the sp awarded by killFoe) is half the Knight control's.
    - Master of Arms — good: `weaponDamage` +2 vs the Soldier control; bad: `canParley` false for every ENC_TYPES value even with Language + Helm, and a tracked round-1 `flee` emits `withdrawalDenied` + `fleeRolled`.
    - Samurai — good: forced Samurai hero has `armor "Plate"` and `magicWpn 2`; bad: `rollInitiative` "foe" with mine 20 / theirs 1; `flee` → `fleeRefused`.
    - Bard — good: `canParley` true vs Humans at fluency 0; `songReady` true at steps 0 (songAt unset); bad: `newDay` with the eight wake draws `[3,3,3,3,3,3,3,2]` emits `wanderingMonster` (a Soldier control with the same script does not; a Bard with all 3s does not — boundary); party clause: `pickFoeTarget(stateWithAlly, fakeRng([2]), fixedFoe({ intel: 3 }))` is null while intel 4 picks the ally.
    Thief rows:
    - Pickpocket — good: `gainWilmst` (or the take path) yields more gold than a Cutthroat control for the same rng (the Pickpocket extra take; read engine/items.js ~L81-95 for the exact hook and event); bad: `priceFor(100, "Human", "Pickpocket") === 125`, `sellPriceFor(cloakOfArmor, "Human", "Pickpocket") === 938`; negative: a Pickpocket who never opens a store has c.gold unchanged after newRun.
    - Pilfer — good: `springTrap` with a scripted rng emits the Pilfer free-disarm event (read encounters.js ~L71 for its name) and `openChest` opens without a Locks roll; bad: `useItem` on a Strength potion → `useRefused` reason "pilfer"; `canRead` false; negative: a Pilfer with `items: []` calling useItem(0) emits nothing.
    - Cat Burglar — good: first `playerStrike` roll 20 still hits (`struck`, auto opener); bad: `springTrap` damage is double the Pilfer/Con Artist control's for the same script.
    - Cutthroat — good: first landed blow is a crit (event/damage doubled vs a control with the same roll); bad: `meetJoiner` → `joinerRefused` reason "cutthroat", `pendingJoiner === null`, and the rng cursor equals a control hero's (same draws); negative: a Cutthroat who never meets a Joiner has `pendingJoiner` null and `c.joiner` null after newRun.
    - Cloaker — good: `flee` with `opened2` unset → `fled` reason "cloaker" and no `fleeRolled`; bad (distinct scenario): `opened2: true` → `vanishDenied` + `fleeRolled`; negative: a Cloaker with `state.combat === null` calling flee emits nothing.
    - Ninja — good: opener `playerStrike` auto-hits for max weapon damage (compare `struck.dmg` to the weapon's max via WEAPONS) and a later roll of 2 crits; bad: `canParley` false for every ENC_TYPES value at fluency 2; `parley` → `parleyRefused` reason "ninja".
    - Con Artist — good: `canParley` true for every non-Walking-Dead, non-Magical type at fluency 0; a lvl-1 foe leaves on d6 <= 4 at startCombat (`foeFled` reason "conArtist"); bad: the opening landed blow deals 0 damage (`conArtistOpener` event, foe wp unchanged).
    - Acrobat — good: `foeToHitVs` 3 and `toHit` 5; bad: `canEquipWeapon` false for a Long Sword, true for a Dagger.
    Use `hero(sub, "Human")` for every sub row so race modifiers never confound a sub assertion.
  </action>
  <verify>
    <automated>node --test test/unit/identity-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const CONTRACT = \[" test/unit/identity-contract.test.js` == 1
    - `grep -c 'kind: "sub"' test/unit/identity-contract.test.js` >= 16 after this task (24 after Task 2)
    - `grep -c "force: { sub" test/unit/identity-contract.test.js` >= 1 (every hero is built through the Phase 22 seam)
    - `grep -c "maxWP: 19" test/unit/identity-contract.test.js` >= 1 and `grep -c "maxWP: 20" test/unit/identity-contract.test.js` >= 1 (Knight boundary at 19/20)
    - `grep -c "ar: 12" test/unit/identity-contract.test.js` >= 1 and `grep -c "ar: 10" test/unit/identity-contract.test.js` >= 1 (Woodsman boundary)
    - `node --test test/unit/identity-contract.test.js` exits 0 with >= 32 passing tests (16 subs x good/bad)
  </acceptance_criteria>
  <done>All 16 Fighter/Thief sub-classes have a passing good and a passing bad entry built from forced heroes, with the decision thresholds asserted at their boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Magic User rows (8 subs), the 5 non-Human race rows, and the Human-neutral entry</name>
  <files>test/unit/identity-contract.test.js</files>
  <read_first>
    - engine/magic.js: castSpell (Apprentice d8 === 1 backfire ~L78 and its event; Summoner `doubled` ~L115; Cleric +3 heal ~L322; thrown bonus `schoolBonus(c.sub, sp.s) + eff(c, "throw")` ~L347); engine/derived.js schoolBonus/schoolGate/canLearn/spellLevelFor/castableAttackSpells; content/mu-chart.js (Wizard offense 3 all schools; Cleric offense 0; Summoner gate offense 3; Court Mage 2/2/1)
    - engine/combat.js Wizard strikeRefused (~L347), Warlock boost (~L245), Court Mage d12 <= 2 and round-1 initiative; engine/movement.js Warlock potion dup (~L484), heal2x (~L419); engine/character.js rollGrimoire special grants (Sorcerer Freeze+Fireball, Cleric Heal/Major Heal, Illusionist Mirror Self/Phantom Host, Summoner Summon)
    - content/races.js (all flags) and Phase 24 flags hide/armorWear; engine/derived.js strikeDie/foeDie/killSpFor race terms; engine/economy.js priceFor race terms; engine/movement.js eats/upkeep
    - test/unit/freeze-pays-out.test.js and test/unit/guaranteed-attack-spell.test.js (Phase 23 idioms for driving castSpell on a forced caster)
  </read_first>
  <action>
    Magic User rows (hero(sub, "Human")):
    - Wizard — good: `schoolBonus("Wizard", "offense") === 3` and `canLearn("Wizard", sp)` true for every SPELLS entry; bad: with a castable attack spell known and charges left, `playerStrike` → `strikeRefused` reason "wizard".
    - Warlock — good: `newDay` with `potions > 0` → `potionDuplicated`; bad: `startCombat(..., "Walking Dead")` → `warlockBoost` and every foe's maxWP raised by `c.level`.
    - Sorcerer — good: forced grimoire includes Freeze and Fireball; bad: `weaponDamage` never exceeds 9 for a Sorcerer holding a heavy weapon with a max-rolling rng.
    - Summoner — good: `spellLevelFor("Summoner", Summon) === 1` and a level-1 cast of Summon is not refused (`spellAboveLevel` absent) — reuse Phase 23-03's empty-foe-list idiom; bad: `schoolGate("Summoner", "offense") === 3` so `canCast` is false for Freeze at level 1 even when known.
    - Cleric — good: `toHit` 4 and a Heal cast heals 3 more than a Wizard control with the same rng; bad: `schoolBonus("Cleric", "offense") === 0` (the audit's "soft bad", asserted as-is; note in the SUMMARY that the user made no Cleric change).
    - Illusionist — good: `spellLevelFor("Illusionist", Phantom Host) === 1`; bad: `strikeDie` is 20 at level 1 and drops at level 3.
    - Court Mage — good: startCombat with the d12 scripted to 2 → `foeBored` (3 → none; boundary) and `canParley` true vs Humans at fluency 0; bad: `rollInitiative` "foe" in round 1 with mine 20 / theirs 1, "you" in round 2.
    - Apprentice — good: `killSpFor` (or awarded sp) is double a Wizard control's at level 1; bad: castSpell with the d8 scripted to 1 → the backfire event (read its name at magic.js ~L78).
    Race rows (hero of a neutral sub, e.g. "Soldier", with the race forced; Fridgian rows use a non-Samurai Fighter):
    - Elven — good: `strikeDie` one step better and `toHit` 5 vs a Human Soldier control; bad: `c.maxWP` equals round(Human maxWP x 0.6) for the same seed/sub, and `foeToHitVs` is 4.
    - Dwarven — good: `weaponDamage` +2 vs control and a soaked blow costs `Math.ceil(dmg / 2)` armorWP (5 → 3; 1 over-min → 1); bad: `foeDie` returns a better (lower-index) die than the control's.
    - Wilmsry — good: camped `newDay` heals double the control; `canParley` true vs Beasts at fluency 0; bad: `killSpFor` is half; `meetJoiner` with the pinned Magic-User joiner seed from 24-05's SUMMARY → `joinerRefused` reason "wilmsry".
    - Fridgian — good: `playerStrike` with d8 = 5 and a corpse present makes two swings at the live target (no wasted event, no d10 drawn — fakeRng exhaustion) and `applyFoeDamageToPlayer` dmg 5 → 3 (dmg 2 → 1 floor; with Hardiness dmg 7 → 2); bad: `canEquipArmor` false for Leather (noArmor) and `rollInitiative` "foe" with mine 20 / theirs 1.
    - Troll — good: `c.maxWP === 75` and `weaponDamage` +9 (dmg 6 + wpnBonus 3) vs control; bad: `priceFor(100, "Troll") === 300` and `RACES.Troll.eats === 2` (makeCamp with 1 ration → `campFailed`).
    Human neutral entry: `Object.keys(RACES.Human)` is exactly `["size", "upkeep", "note"]`; `foeToHitVs` 5 for a Human Soldier; `priceFor(100, "Human") === 100`; `sellPriceFor(x, "Human")` equals 0.5 x base; `newDay` heal not doubled vs the formula; `strikeDie`/`foeDie` equal the class baseline; `rollInitiative` decided purely by mine >= theirs; `canEquipArmor` unaffected by race; a Human non-Cutthroat meeting a Magic User joiner gets a pendingJoiner; `applyFoeDamageToPlayer` dmg 5 → 5; a soaked blow costs the full dmg of armorWP.
  </action>
  <verify>
    <automated>node --test test/unit/identity-contract.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'kind: "sub"' test/unit/identity-contract.test.js` == 24 and `grep -c 'kind: "race"' test/unit/identity-contract.test.js` == 6
    - `grep -c 'key: "Human"' test/unit/identity-contract.test.js` == 1 and `grep -c "neutral:" test/unit/identity-contract.test.js` == 1
    - `grep -c '\["size", "upkeep", "note"\]' test/unit/identity-contract.test.js` == 1
    - `node --test test/unit/identity-contract.test.js` exits 0 with >= 59 passing tests (29 x good/bad + Human neutral)
  </acceptance_criteria>
  <done>Every Magic User sub and every race has passing good/bad entries; Human is asserted neutral on every hook.</done>
</task>

<task type="auto">
  <name>Task 3: Completeness meta-test, negative cases, idempotency, full suite</name>
  <files>test/unit/identity-contract.test.js</files>
  <read_first>
    - content/classes.js CLASSES[*].subs and content/races.js RACES (the canonical key sets and order)
    - the CONTRACT table written in Tasks 1-2
  </read_first>
  <action>
    1. Meta-test "IDENT-08: the contract table is complete and canonically ordered": the table's `kind: "sub"` keys, in order, deep-equal `[...CLASSES["Magic User"].subs, ...CLASSES.Fighter.subs, ...CLASSES.Thief.subs]` (24) and the `kind: "race"` keys deep-equal `Object.keys(RACES)` (6); every non-Human entry has both `good` and `bad` with non-empty `name` strings; the Human entry has `neutral` and neither good nor bad; no duplicate keys.
    2. Negative cases as their own tests (probe IDENT-05/IDENT-07 empty): a Knight vs foes all under 20 maxWP rolls initiative purely by dice; a solo Bard's `pickFoeTarget` returns null without drawing (no allies); a Cutthroat/Wilmsry that never meets a Joiner has `pendingJoiner` null and `c.joiner` null; a Pickpocket that never opens a store keeps `c.gold` from chargen; a Woodsman offered no armour equips nothing and emits nothing; a Pilfer with `items: []` useItem(0) emits nothing; a Cloaker with no combat flee emits nothing.
    3. Idempotency test (probe FID-07): building `hero("Cutthroat")` twice yields deep-equal states; running the Cutthroat joiner scenario twice yields deep-equal event lists and equal rng cursors; the Cutthroat's meetJoiner cursor equals a Soldier control's (roll first, refuse after).
    4. Run the whole suite; the SUMMARY must include the 30-row good/bad name table (key | good name | bad name) for Plan 24-07 to transcribe into the ledger.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "IDENT-08: the contract table is complete" test/unit/identity-contract.test.js` == 1
    - `grep -c "Object.keys(RACES)" test/unit/identity-contract.test.js` >= 1 and `grep -c 'CLASSES\["Magic User"\].subs' test/unit/identity-contract.test.js` >= 1
    - `node --test test/unit/identity-contract.test.js` exits 0 with >= 68 passing tests
    - `npm test` ends with `# fail 0`
    - `git status --porcelain engine content test/parity` prints nothing (this plan changes only the new test file)
  </acceptance_criteria>
  <done>The table is provably complete against CLASSES/RACES, negative and idempotency cases pass, and the full suite is green with no engine/content/parity change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test -> engine | Test-only; no runtime exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-15 | Repudiation | contract table completeness | low | mitigate | Meta-test derives the expected key set from CLASSES/RACES at runtime, never a hand-copied list |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase |
</threat_model>

<verification>
- `node --test test/unit/identity-contract.test.js` green with >= 68 tests.
- `npm test` ends `# fail 0`; no engine/content/parity file changed.
</verification>

<success_criteria>
- One data table proves a good and a bad for all 24 sub-classes and 5 races, Human neutral, boundaries at the decided thresholds, complete against CLASSES/RACES.
</success_criteria>

<output>
Create `.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-06-SUMMARY.md` when done, including the 30-row (key | good | bad) table and any engine discrepancy found (reported, not patched).
</output>
