---
phase: 24-every-sub-class-and-race-one-good-one-bad
plan: 05
type: execute
wave: 3
depends_on: ["24-01", "24-03", "24-04"]
files_modified:
  - engine/encounters.js
  - engine/items.js
  - engine/movement.js
  - engine/economy.js
  - src/browser/eventNarration.js
  - content/flavor.js
  - test/unit/identity-world.test.js
autonomous: true
requirements: [IDENT-05, IDENT-07, IDENT-09, FID-07]

must_haves:
  truths:
    - "A Cutthroat never gains a Joiner: meetJoiner rolls the joiner exactly as today (d10 level, rollCharacter, d20 wp, discarded d20 — same draws, c.joiner and joinerMet unchanged) and then emits joinerRefused with reason cutthroat and leaves state.pendingJoiner null (D: Cutthroat bad, IDENT-05)"
    - "A Magic User joiner refuses a Wilmsry hero via the same joinerRefused event with reason wilmsry, after the joiner is rolled; a Fighter or Thief joiner still becomes the Wilmsry's pendingJoiner; a Human hero meeting a Magic User joiner gets a pendingJoiner as today (D: Wilmsry; Human neutral)"
    - "Woodsman armor legality is ar <= 10 (Studded ar 10 legal; Mail ar 12 refused), asserted at both values; a Pilfer may use an item exactly when its resolved kind is heal (drinkPotion always allowed; useItem heal allowed; every other kind refused); the Cloaker vanish is free exactly while C.opened2 is falsy and becomes the ordinary Thief flee roll the moment it is truthy (probe IDENT-07 adjacency)"
    - "The Woodsman gate sits inside canEquipArmor (single source of truth for takeItem and equipItem); the Pilfer gate sits at the top of useItem before itemReady; the Cloaker gate precedes the free-vanish branch in flee; each placement is stated in the plan action so the executor cannot put it after a side effect (probe IDENT-07 ordering)"
    - "A Woodsman's store never offers an armour line the Woodsman cannot wear (a Woodsman in Studded sees no Mail line; a Soldier in Studded still does); every other hero's stock is byte-identical (D: Woodsman, IDENT-07)"
    - "A Bard camping wakes on a d20 of 1 OR 2 per hour (same eight draws; a 3 never wakes); every other sub wakes only on a 1 (D: Bard bad, IDENT-05)"
    - "Every blurb that names a number matches the engine's number (Troll +9 = dmg 6 + wpnBonus 3; Elven 0.6x; Court Mage one in six; Fridgian five times in eight and hide soaks 2); every blurb that names a mechanic names the implemented one (Wizard/Summoner/Illusionist already updated in Phase 23) (probe IDENT-09 adjacency)"
    - "No blurb describes a mechanic that does not exist and no implemented good/bad is undescribed: the sweep task lists all 30 SUB_NOTE/RACE_NOTE keys with a checked/changed column in the SUMMARY (probe IDENT-09 empty)"
    - "Blurbs are plain UTF-8 strings in content/flavor.js; the voice safety scan and the flavor-table count tests stay green; no key is renamed (probe IDENT-09 encoding)"
    - "SUB_NOTE / RACE_NOTE key order is unchanged; only string values change (probe IDENT-09 ordering)"
    - "Every new event type (joinerRefused, useRefused) and reason (itemRejected/equipRejected woodsman; wanderingMonster bard flag) has an EVENT_NARRATION line; coverage guard and voice scan green (FID-07)"
  artifacts:
    - "engine/encounters.js — meetJoiner refusal branch (reasons cutthroat / wilmsry) after all draws"
    - "engine/items.js — armorRefusalReason(c, it) exported helper (noArmor | woodsman | tooHeavy | null); canEquipArmor delegates to it; takeItem/equipItem emit the helper's reason; useItem Pilfer gate before itemReady"
    - "engine/movement.js — newDay wake threshold `<= (c.sub === \"Bard\" ? 2 : 1)`; wanderingMonster.bard flag"
    - "engine/economy.js — openStore armour line filtered through canEquipArmor"
    - "src/browser/eventNarration.js — joinerRefused, useRefused entries; itemRejected/equipRejected woodsman text; wanderingMonster bard clause"
    - "content/flavor.js — 14 changed blurbs (Knight, Guard, Woodsman, Master of Arms, Bard, Pickpocket, Pilfer, Cutthroat, Cloaker, Ninja, Court Mage, Dwarven, Wilmsry, Fridgian); 16 checked-unchanged"
    - "test/unit/identity-world.test.js — joiner / armour / useItem / wake / store-filter tests"
  key_links:
    - "meetJoiner -> joinerRefused (no pendingJoiner) -> engine.js resolveJoiner never sees a candidate; the tuning bot's `if (state.pendingJoiner)` branch simply never fires"
    - "armorRefusalReason -> canEquipArmor -> takeItem / equipItem / openStore mails filter (one rule, four readers)"
    - "useItem gate -> before it.usedAt / itemUsed (a refused use leaves cooldown and inventory untouched)"
    - "newDay wake loop -> startCombat(wandering) (same call, wider trigger for a Bard)"
  prohibitions:
    - "No new rng draw behind a sub-class or race guard (the joiner is rolled BEFORE the refusal; the wake loop keeps eight draws; the gates are pure reads)"
    - "No new serialized field (c.joiner stays; pendingJoiner is simply left null)"
    - "Never edit test/parity/prototype-master.js.txt; no fixture change (no fixture meets a Joiner, camps as a Bard, equips heavy armour as a Woodsman, or useItems as a Pilfer — the encounters fixtures' Pilfer heroes only springTrap/encounterDot)"
    - "The Human race gets NO mechanic; Wilmsry heal 2x / parley +4 / haggle 0.7 numbers unchanged; Elven and Troll untouched"
    - "drinkPotion and canRead untouched (a Pilfer still drinks healing potions and still cannot read scrolls)"
    - "Flavor stays sarcastic, deadpan and family-friendly (voice scan); no SUB_NOTE/RACE_NOTE key renamed or reordered"
    - "No toast/UI work beyond EVENT_NARRATION lines (Phase 25 owns feedback)"
---

<objective>
Land the world-side goods and bads outside combat.js: Joiners refuse a Cutthroat (and Magic User Joiners refuse a Wilmsry), a Woodsman cannot wear or be sold Mail/Plate, a Pilfer can use only heal-kind items, a camping Bard draws wandering monsters twice as often — then sweep all 30 SUB_NOTE/RACE_NOTE blurbs so every sentence on the sheet is true of the engine as it now stands.

Purpose: the remaining IDENT-05/IDENT-07 mechanics plus IDENT-09 (truthful flavor), all zero-draw and narrated.
Output: encounters.js, items.js, movement.js, economy.js edits; narration entries; flavor.js sweep; identity-world.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md
@.planning/research/CLASS-AUDIT-2026-09-14.md
@engine/encounters.js
@engine/items.js
@engine/movement.js
@content/flavor.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- New event types: `joinerRefused` (`{ type, reason: "cutthroat" | "wilmsry", name, sub, cls, lvl }`), `useRefused` (`{ type, item, reason: "pilfer" }`).
- New reasons/flags on existing events: `itemRejected` / `equipRejected` reason `"woodsman"`; `wanderingMonster.bard` boolean.
- New exported helper: `armorRefusalReason(c, it)` in engine/items.js.
- Content: 14 rewritten SUB_NOTE/RACE_NOTE strings (keys/order unchanged).
- New test file: test/unit/identity-world.test.js.
- No new RACES flag, no new serialized field, no fixture change.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Joiners refuse a Cutthroat; Magic User Joiners refuse a Wilmsry (after the roll, zero new draws)</name>
  <files>engine/encounters.js, test/unit/identity-world.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (Cutthroat bad; Wilmsry; `<specifics>` — one event type, two reasons, funny and specific)
    - engine/encounters.js meetJoiner (~L442-472: d10 level, rollCharacter, d20 wp, discarded d20, `c.joiner = {...}`, `state.pendingJoiner = {...}`, `joinerMet`) and resolveJoiner (~L484-494)
    - test/unit/joiner-acquisition.test.js (~L29-120: fakeRng/fixedState/fixedPending helpers; the "consumes NO extra rng" control-replay idiom at ~L101-118)
  </read_first>
  <action>
    Per D: Cutthroat (IDENT-05) and D: Wilmsry (race pass). `// DELIBERATE RULES CHANGE (Phase 24, 2026-09-14, IDENT-05): ...` comment.

    1. In meetJoiner, keep every line through `c.joiner = {...}` byte-identical (all four draws, the canon `c.joiner` record). Then compute `const refusal = c.sub === "Cutthroat" ? "cutthroat" : c.race === "Wilmsry" && joinerChar.cls === "Magic User" ? "wilmsry" : null;`. Push `joinerMet` exactly as today in both branches. If `refusal` is null, set `state.pendingJoiner` exactly as today. If set, do NOT touch `state.pendingJoiner` (leave it as it was — null on every path that reaches here) and push `{ type: "joinerRefused", reason: refusal, name: joinerChar.name, sub: joinerChar.sub, cls: joinerChar.cls, lvl }` after `joinerMet`. Update the JSDoc: draw order unchanged; refusal is a pure read after the last draw.
    2. Create test/unit/identity-world.test.js (local helper copies from joiner-acquisition.test.js; import meetJoiner, rollCharacter, makeRng, newRun):
       - Cutthroat hero: `meetJoiner(state, makeRng(555), events)` → `state.pendingJoiner === null`, events contain `joinerMet` then `joinerRefused` with `reason "cutthroat"` and `name === state.c.joiner.name`; `state.c.joiner` is set exactly as for a non-Cutthroat run of the same seed (deep-equal against a Soldier control).
       - Draw parity: the rng cursor after a Cutthroat's meetJoiner equals the cursor after a control hero's meetJoiner from the same seed (replay `makeRng(777)` twice).
       - Wilmsry vs Magic User joiner: scan seeds 1..500 with `rollCharacter(makeRng(seed))` at the meetJoiner draw position (or simply run meetJoiner on a Wilmsry hero for consecutive seeds) until a joiner with `cls === "Magic User"` appears and one with `cls !== "Magic User"` appears; pin both seeds in the test: the Magic User case → `joinerRefused` reason "wilmsry", pendingJoiner null; the non-MU case → pendingJoiner set, no joinerRefused.
       - Human neutral: a Human Soldier meeting the pinned Magic-User-joiner seed gets a pendingJoiner and no joinerRefused.
  </action>
  <verify>
    <automated>node --test test/unit/identity-world.test.js test/unit/joiner-acquisition.test.js test/unit/encounters.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"joinerRefused"' engine/encounters.js` == 1 and `grep -c 'reason: refusal' engine/encounters.js` == 1
    - `grep -c '"cutthroat"' engine/encounters.js` == 1 and `grep -c '"wilmsry"' engine/encounters.js` == 1
    - `grep -c 'joinerChar.cls === "Magic User"' engine/encounters.js` == 1
    - `grep -c "const discardedMaxWP = 20 \* lvl + rng.d(20);" engine/encounters.js` == 1 (the discarded draw survives, before the refusal)
    - `node --test test/unit/identity-world.test.js` exits 0 with >= 5 joiner tests passing
    - `node --test test/unit/joiner-acquisition.test.js test/unit/encounters.test.js` exits 0 unchanged
  </acceptance_criteria>
  <done>Cutthroat and Wilmsry-vs-Magic-User refusals fire after the fully-rolled joiner with identical draws, one event type and two reasons; pendingJoiner stays null; Human/other cases unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Woodsman armour gate (canEquipArmor + store filter) and Pilfer heal-only useItem gate</name>
  <files>engine/items.js, engine/economy.js, test/unit/identity-world.test.js</files>
  <read_first>
    - .planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-CONTEXT.md (Woodsman: refuse any ar > 10; Pilfer: heal-kind only via useItem; drinkPotion untouched; scrolls stay blocked)
    - engine/items.js: canEquipArmor (~L204-207), takeItem armour path (~L241-260: noArmor → tooHeavy → notBetter reasons), equipItem armour path (~L409-430), useItem (~L491-500: `if (!it || !itemReady(state, it)) return events;` then `it.usedAt = state.steps` and `itemUsed`; `const kind = it.kind === "potion" ? it.eff2 : it.use;`)
    - content/potions.js (eff "heal" = Healing, eff "full" = Xtra Healing — both restore wp; poison/disease cures are not heal-kind), content/armors.js (Cloth 3, Leather 6, Studded 10, Mail 12, Plate 15)
    - engine/economy.js openStore (~L255-268: `const mails = ARMORS.filter((a) => a.cls.includes(letter) && a.ar > c.ar); if (mails.length && !RACES[race].noArmor) {...}`) — Plan 24-04 already touched this file; re-read the current text
    - test/unit/items.test.js (~L38-64 helpers; ~L202-235 useItem tests), test/unit/inventory-actions.test.js (canEquipArmor consumers)
  </read_first>
  <action>
    Per D: Woodsman and D: Pilfer (IDENT-07). `// DELIBERATE RULES CHANGE (Phase 24, 2026-09-14, IDENT-07): ...` comments.

    1. engine/items.js: add `export function armorRefusalReason(c, it)` returning `"noArmor"` when `RACES[c.race].noArmor`, `"woodsman"` when `c.sub === "Woodsman" && it.ar > 10` (Mail and Plate — and any Warded piece heavier than plain Studded — the blurb's "no mail, no plate"), `"tooHeavy"` when the existing class/Heft rule fails, else `null`. Rewrite `canEquipArmor(c, it)` as `armorRefusalReason(c, it) === null` (behaviour identical for everyone but a Woodsman). In takeItem's and equipItem's armour paths replace the noArmor/tooHeavy pair with one read of `armorRefusalReason` that pushes `itemRejected` / `equipRejected` with that reason (the `notBetter` check in takeItem stays after it, unchanged).
    2. engine/items.js useItem: directly after the `if (!it || !itemReady(state, it)) return events;` line and BEFORE `it.usedAt = state.steps`, resolve the kind the same way the function already does and add the gate: `if (c.sub === "Pilfer" && kind !== "heal" && kind !== "full") { events.push({ type: "useRefused", item: it, reason: "pilfer" }); return events; }` — a refused use leaves `usedAt`, inventory and rng untouched. Move the existing `const kind = ...` line up so it is computed once. Document the discretion call: heal-kind = the two wp-restoring potion effects (Healing `heal`, Xtra Healing `full`); cures, buffs, staves, cloaks and every `use:` item are refused; drinkPotion (the generic healing draught) and canRead are untouched.
    3. engine/economy.js openStore: change the armour candidate filter to `ARMORS.filter((a) => a.cls.includes(letter) && a.ar > c.ar && canEquipArmor(c, a))` (import canEquipArmor from ./items.js — check for an import cycle first: items.js must not import economy.js; if it does, inline `armorRefusalReason` usage the same way). For every non-Woodsman hero the filter result is unchanged (the class letter already matched; noArmor was already guarded), so every fixture stock is byte-identical; a Woodsman in Studded gets no armour line at all rather than a Mail line whose purchase would be rejected after the gold is spent.
    4. Tests (identity-world.test.js): `canEquipArmor(woodsman, Studded ar 10) === true`, `Mail ar 12 === false`, `Plate === false`, `Leather === true`; `armorRefusalReason` returns "woodsman" for Mail on a Woodsman, "tooHeavy" for Mail on a Thief without Heft, "noArmor" for a Fridgian, null for a Soldier; takeItem of Mail on a Woodsman emits `itemRejected` reason "woodsman" and leaves `c.armor`; equipItem likewise emits `equipRejected` reason "woodsman"; a Soldier takes Mail; openStore for a Woodsman wearing Studded (ar 10, full armorWP) has no `buyArmor` line while a Soldier in Studded has one (drive with `makeRng(1)`, compare `stock.filter(s => s.effectId === "buyArmor").length` 0 vs 1) and the two stocks otherwise list the same weapon names; Pilfer: useItem on a Healing potion heals and consumes it; on an Xtra Healing potion heals to max; on a Strength potion emits `useRefused` reason "pilfer", no `itemUsed`, item still carried, `usedAt` undefined, `fakeRng([])` untouched; a Cat Burglar using the Strength potion gets `itemUsed`; `canRead` for a Pilfer is still false.
  </action>
  <verify>
    <automated>node --test test/unit/identity-world.test.js test/unit/items.test.js test/unit/inventory-actions.test.js test/unit/economy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function armorRefusalReason" engine/items.js` == 1 and `grep -c 'c.sub === "Woodsman" && it.ar > 10' engine/items.js` == 1
    - `grep -c "return armorRefusalReason(c, it) === null;" engine/items.js` == 1
    - `grep -c 'reason: "pilfer"' engine/items.js` == 1 and `grep -c '"useRefused"' engine/items.js` == 1
    - the useRefused push appears on a lower line number than `it.usedAt = state.steps;` (`awk '/"useRefused"/{a=NR} /it.usedAt = state.steps;/{b=NR} END{exit !(a<b)}' engine/items.js` exits 0)
    - `grep -c "canEquipArmor(c, a)" engine/economy.js` == 1
    - `grep -c 'if (c.sub === "Pilfer") return false;' engine/magic.js` == 1 (canRead untouched)
    - `node --test test/unit/identity-world.test.js` exits 0 with >= 12 armour/useItem/store tests passing
    - `node --test test/unit/items.test.js test/unit/inventory-actions.test.js test/unit/economy.test.js` exits 0
    - `node --test "test/parity/**/*.test.js"` exits 0 (no fixture hero is a Woodsman in a store or a Pilfer using an item)
  </acceptance_criteria>
  <done>Woodsman legality lives in one helper read by take/equip/store; a Pilfer's non-heal use is refused before any side effect; all existing item/economy tests and parity stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Bard wake roll; EVENT_NARRATION entries; the 30-blurb flavor sweep; full suite green</name>
  <files>engine/movement.js, src/browser/eventNarration.js, content/flavor.js, test/unit/identity-world.test.js</files>
  <read_first>
    - engine/movement.js newDay (~L379-505: the eight-iteration wake loop comparing each d20 to exactly one, then `wanderingMonster` + `startCombat(state, true, ...)`)
    - src/browser/eventNarration.js (joinerMet/joinerDeclined ~L376-384, itemRejected ~L413, equipRejected ~L440, wanderingMonster ~L106, itemUsed/itemFizzled ~L415-418; Plan 24-01/24-03 already touched this file — re-read the current text)
    - content/flavor.js SUB_NOTE / RACE_NOTE (all 30 strings) and the engine hooks each describes (see .planning/research/CLASS-AUDIT-2026-09-14.md for the audit's per-sub GOOD/BAD columns and the Phase 24 CONTEXT decisions for the new mechanics)
    - test/voice/safety-scan.test.js header (what is scanned) and test/unit/movement.test.js (~L703-720: the newDay wandering-monster test idiom)
  </read_first>
  <action>
    1. Bard wake (per D: Bard, IDENT-05): in newDay, compute `const wakeOn = c.sub === "Bard" ? 2 : 1;` before the loop and change the per-hour test to `rng.d(20) <= wakeOn` (eight draws, unchanged count and order). Add `bard: c.sub === "Bard"` to the `wanderingMonster` event. DELIBERATE RULES CHANGE comment.
    2. Tests (identity-world.test.js): a Bard with a scripted rng whose eight wake draws are `[3,3,3,3,3,3,3,2]` followed by startCombat's draws emits `wanderingMonster` with `hours 1` and `bard true`; a Soldier with the same script emits no `wanderingMonster`; a Bard with all eight draws at 3 emits none; exactly eight wake draws are consumed before startCombat begins (compare the cursor of a `makeRng` seed after the loop against a control that draws d20 eight times — or use the fakeRng exhaustion idiom on the no-wake case).
    3. EVENT_NARRATION (deadpan, family-friendly; sentences are discretion): `joinerRefused` — reason cutthroat e.g. `Word has reached the Joiners. The Joiners have reached the exit.` (name the joiner), reason wilmsry e.g. `${name}, a Magic User, takes one look at a Wilmsry and remembers an appointment elsewhere.`; `useRefused` — e.g. `${item} does not heal, so as far as a Pilfer is concerned it does not work.`; `itemRejected` / `equipRejected` — add a "woodsman" reason branch (e.g. `A Woodsman in mail is a tree in a tin. No.`) ahead of the existing generic text, which stays byte-identical for other reasons; `wanderingMonster` — append a clause when `e.bard` (e.g. ` Something too stupid to know better heard the singing.`). Every builder must tolerate `{ type }` alone.
    4. Flavor sweep (IDENT-09): re-read all 30 SUB_NOTE + RACE_NOTE strings against the implemented mechanics and rewrite the false or incomplete ones — at minimum: Knight (the big-foe rule is now "20 hit points or more come straight at you — and they move first"), Guard (add the good: every creature needs one better to land a blow), Woodsman (the mail/plate refusal is now enforced — keep, sharpen), Master of Arms (cannot parley; no clean withdrawal from a fight you have tracked — "you attack without question" is now literal), Bard (camping draws visitors twice as often; in company the dim ones come for you), Pickpocket (shopkeepers know your face: a quarter more to buy, a quarter less to sell), Pilfer (heal-kind only, enforced), Cutthroat (no Joiner will travel with you), Cloaker (the vanish is free only until someone has seen your face — i.e. until your first landed blow), Ninja (no longer flavor-only), Court Mage (one creature in SIX dies of boredom; you talk first, so they act first), Dwarven (armour wears at half the rate), Wilmsry (Magic Users will not travel with you), Fridgian (the second swing never lands on a corpse; a hide that shrugs off two points of every blow; still no armour, still last). Check-only (expected unchanged unless found false): Soldier, Barbarian, Samurai, Cat Burglar, Con Artist, Acrobat, Wizard, Warlock, Sorcerer, Summoner, Cleric, Illusionist, Apprentice, Human, Elven, Troll. Keys and key order unchanged; sarcastic, deadpan, family-friendly; no profanity/gore. Record every key in the SUMMARY with `checked` or `changed` and one line of why.
    5. Run the coverage guard, the voice scan, and the whole suite.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const wakeOn = c.sub === "Bard" ? 2 : 1;' engine/movement.js` == 1 and `grep -c "rng.d(20) <= wakeOn" engine/movement.js` == 1
    - `grep -v '^\s*//' engine/movement.js | grep -c "rng.d(20) === 1"` == 0 (the old exactly-one wake check is gone)
    - `grep -c 'bard: c.sub === "Bard"' engine/movement.js` == 1
    - `grep -c "  joinerRefused:" src/browser/eventNarration.js` == 1 and `grep -c "  useRefused:" src/browser/eventNarration.js` == 1 and `grep -c '"woodsman"' src/browser/eventNarration.js` >= 1 and `grep -c "e.bard" src/browser/eventNarration.js` >= 1
    - `node -e "const {SUB_NOTE,RACE_NOTE}=await import('./content/flavor.js');const s=Object.keys(SUB_NOTE),r=Object.keys(RACE_NOTE);if(s.length!==24||r.length!==6)process.exit(1);if(s.join()!=='Knight,Guard,Woodsman,Soldier,Barbarian,Master of Arms,Samurai,Bard,Pickpocket,Pilfer,Cat Burglar,Cutthroat,Cloaker,Ninja,Con Artist,Acrobat,Wizard,Warlock,Sorcerer,Court Mage,Illusionist,Cleric,Summoner,Apprentice')process.exit(2);if(r.join()!=='Human,Elven,Dwarven,Wilmsry,Fridgian,Troll')process.exit(3)" --input-type=module` exits 0 (keys and order unchanged)
    - `grep -c "in twelve" content/flavor.js` == 0 and `grep -ci "one creature in six\|one in six" content/flavor.js` >= 1 (Court Mage number matches d12 <= 2)
    - `grep -c "occasionally something already dead" content/flavor.js` == 0 (the Fridgian corpse-swing line is gone)
    - `git diff --numstat content/flavor.js` shows changes on >= 14 lines (the 14 named blurbs) and the SUMMARY lists all 30 keys as checked/changed
    - `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js` exits 0
    - `npm test` ends with `# fail 0` and `node --test "test/parity/**/*.test.js"` exits 0
  </acceptance_criteria>
  <done>A Bard wakes on 1-2, every new event/reason narrated, every one of the 30 blurbs re-read and made true (14 changed, 16 checked), voice scan and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/flavor.js -> player | Authored copy shown on the sheet; store-rating sensitive |
| engine events -> narration | Joiner names/item names interpolated into HTML strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-12 | Information Disclosure | flavor/narration voice | low | mitigate | test/voice/safety-scan.test.js over every changed string and builder |
| T-24-13 | Denial of Service | useItem refusal | low | mitigate | Refusal returns before any side effect; a Pilfer can still drinkPotion; unit test proves no stuck state |
| T-24-14 | Tampering | store armour filter | low | mitigate | Filter only ever removes an illegal line for a Woodsman; unit test proves other heroes' stock is unchanged and parity stays green |
| T-24-SC | Tampering | npm/pip/cargo installs | low | accept | No package install in this phase |
</threat_model>

<verification>
- `node --test test/unit/identity-world.test.js` green with >= 20 tests.
- `node --test test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js` green.
- `npm test` ends `# fail 0`; parity green; `git status --porcelain test/parity` empty.
</verification>

<success_criteria>
- Cutthroat/Wilmsry joiner refusals, Woodsman armour gate (take/equip/store), Pilfer heal-only useItem, Bard double wake — zero new draws, zero new fields, all narrated.
- All 30 blurbs truthful; keys/order unchanged; voice scan green.
</success_criteria>

<output>
Create `.planning/phases/24-every-sub-class-and-race-one-good-one-bad/24-05-SUMMARY.md` when done, including the 30-row checked/changed table (key | checked/changed | why), the pinned Wilmsry joiner seeds, and the narration sentences chosen.
</output>
