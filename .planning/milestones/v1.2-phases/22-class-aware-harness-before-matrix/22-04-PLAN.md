---
phase: 22-class-aware-harness-before-matrix
plan: 04
type: execute
wave: 3
depends_on:
  - 22-03
files_modified:
  - docs/CLASS-PASS.md
  - docs/class-pass/before.json
  - docs/class-pass/before-depth20.json
autonomous: true
requirements:
  - PLAY-01
tags: [ledger, before-snapshot, class-pass, dev-only]

must_haves:
  truths:
    - "BEFORE is commit-pinned: the ledger records the full engine commit hash and states that `git diff --quiet <hash> -- engine content src mazeworld.html` exited 0 both immediately before the capture started and after the capture's final commit; the Bot: parameter line is transcribed verbatim (identical to meta.bot in the JSON) so AFTER (Phase 26) can grep-diff it."
    - "docs/CLASS-PASS.md did not exist before this phase — this plan creates it with a Bot proxy section, a BEFORE section (natural-start 143x40 matrix + depth-20 143x10 slice, verbatim), an Outliers/Findings section, and headed placeholders (Rulings — Phase 24 / PLAY-03; AFTER — Phase 26 / PLAY-02), and creates docs/class-pass/before.json and docs/class-pass/before-depth20.json (per-cell aggregates only, no per-run rows)."
    - "The BEFORE capture is the LAST task of the phase and its commit lands before any Phase 23 engine commit; the ledger names the pinned hash and the v1.1-close baseline hash 1b4daed, and a later reader can verify with git that the only engine/ or content/ change between them is the dev-only force option (engine/character.js, engine/state.js)."
  artifacts:
    - path: "docs/CLASS-PASS.md"
      provides: "the class-pass ledger: Bot proxy, reproduce commands, BEFORE (pinned, verbatim transcripts), Outliers/Findings, Rulings + AFTER placeholders"
      min_lines: 200
    - path: "docs/class-pass/before.json"
      provides: "natural-start BEFORE matrix aggregates (143 cells x 40 seeds)"
      contains: "\"startDepth\": 1"
    - path: "docs/class-pass/before-depth20.json"
      provides: "depth-20 BEFORE slice aggregates (143 cells x 10 seeds)"
      contains: "\"startDepth\": 20"
  key_links:
    - from: "docs/CLASS-PASS.md"
      to: "docs/class-pass/before.json"
      via: "the ledger's Bot: line equals meta.bot in the JSON and names the same pinned commit"
      pattern: "Bot: exploreBudget=50  maxActions=5000"
    - from: "docs/CLASS-PASS.md"
      to: "tools/tune-classes.mjs"
      via: "the reproduce section quotes the exact command lines used"
      pattern: "tools/tune-classes.mjs --seeds 40 --workers 4 --max-actions 5000"
  prohibitions:
    - statement: "The BEFORE capture MUST NOT be taken on an engine tree that differs from the pinned commit (uncommitted engine/content/src edits, or a later engine commit), and the committed docs/class-pass/*.json MUST NOT contain per-run rows — aggregates only."
      status: resolved
      verification: test
---

<objective>
Capture the BEFORE snapshot (PLAY-01) against the commit-pinned pre-identity-pass engine and commit it to the new class-pass ledger — LAST in the phase, before any Phase 23 rule lands. Two matrix runs with the finished harness: the full 143-cell natural-start matrix at 40 seeds/combo (5,720 runs) and the depth-20 slice at 10 seeds/combo (1,430 runs), both with the 5,000-action cap, 4 workers, and the identical `Bot:` parameter line. Write `docs/CLASS-PASS.md` (BEFORE section + Bot proxy + reproduce commands + Outliers/Findings + headed placeholders for Rulings/Phase 24 and AFTER/Phase 26) and `docs/class-pass/before.json` + `docs/class-pass/before-depth20.json` (aggregates only).

Per 22-CONTEXT.md "Matrix tool, volume, and the BEFORE snapshot": the engine/content/src must be proven unchanged from the pinned commit for the whole capture (`git diff --quiet <hash> -- engine content src mazeworld.html`), mirroring docs/DIFFICULTY-RETUNE.md's discipline (commit pin, identical `Bot:` line, background runs with an `EXIT=<code>` sentinel and bounded polling). The pinned hash is HEAD after Plans 22-01..22-03 landed — the dev-only force option changes no behavior on the default path, so pinning after it is correct; the only engine diff versus the v1.1-close baseline `1b4daed` is `engine/character.js` + `engine/state.js`.

Purpose: PLAY-01 — every Phase 23/24 identity change gets a paired baseline that is impossible to recover later without git archaeology.
Output: `docs/CLASS-PASS.md`, `docs/class-pass/before.json`, `docs/class-pass/before-depth20.json`, one commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-class-aware-harness-before-matrix/22-CONTEXT.md
@.planning/phases/22-class-aware-harness-before-matrix/22-02-SUMMARY.md
@.planning/phases/22-class-aware-harness-before-matrix/22-03-SUMMARY.md
@.planning/research/CLASS-AUDIT-2026-09-14.md

@docs/DIFFICULTY-RETUNE.md
@tools/tune-classes.mjs
@tools/lib/tuning-bot.mjs
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `engine/character.js` — `rollCharacter(rng, exclude = [], force = null)`; `force` shape `{ cls?, sub?, race? }` canonical keys; throws on unknown keys or Fridgian+Samurai. [22-01]
- `engine/state.js` — `newRun(seed, exclude = [], { startDepth = 1, force = null } = {})`. [22-01]
- `test/determinism/forced-chargen.test.js`. [22-01]
- `tools/lib/tuning-bot.mjs` — `chooseSpell`, `isTalkFirst`, `botLine`, extended `decideAction`/`observe`/`makeBotContext`/`playRun` (`opts.startDepth`, `opts.force`; result `stuck`, `outcome`, `startDepth`, `floorsGained`, `encountersSurvived`), `BOT_DEFAULTS.startDepth`. [22-02]
- `test/unit/tuning-bot.test.js` extensions. [22-02]
- `tools/lib/class-matrix.mjs` — `enumerateCells`, `EXCLUDED_CELLS`, `resolveName`, `resolveForce`, `selectCells`, `seedList`, `rowFromRun`, `summarizeRows`, `rankCells`, `rollups`, `buildReport`, `formatText`. [22-03]
- `tools/tune-classes.mjs` — the matrix CLI (`--seeds --workers --max-actions --start-depth --explore-budget --cls --sub --race --json --out`). [22-03]
- `tools/tune-difficulty.mjs` — `--start-depth=N`, stuck bucket. [22-03]
- `test/unit/class-matrix.test.js`. [22-03]
- `docs/CLASS-PASS.md` — the class-pass ledger (this plan). [22-04]
- `docs/class-pass/before.json`, `docs/class-pass/before-depth20.json` — aggregate-only BEFORE snapshots (this plan). [22-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Pin the engine, prove it clean, and run the natural-start BEFORE matrix in the background</name>
  <files>docs/class-pass/before.json</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md lines 87-112 (the exact BEFORE-readout discipline paragraph to mirror: commit pin, git diff --quiet before/after, run_in_background + EXIT sentinel + bounded polling, abort threshold)
    - .planning/phases/22-class-aware-harness-before-matrix/22-03-SUMMARY.md (measured wall time of the acceptance runs — size the polling window from it)
    - tools/tune-classes.mjs header (flags, JSON shape, stderr elapsed line)
    - .planning/phases/22-class-aware-harness-before-matrix/22-CONTEXT.md, sections "Matrix tool, volume, and the BEFORE snapshot" and "Specific Ideas"
  </read_first>
  <action>
    Preflight (all must hold before launching; stop and fix if any fails):
    1. `git status --porcelain` prints nothing (clean tree — every 22-01..22-03 change is committed). If it prints anything, commit or discard it first; never capture on an uncommitted tree.
    2. `npm test` ends with `# fail 0`.
    3. `PIN=$(git rev-parse HEAD)`; `git diff --quiet "$PIN" -- engine content src mazeworld.html` exits 0 (trivially true on a clean tree — record the exit code anyway, it is the ledger's proof line). Also capture `git diff --name-only 1b4daed "$PIN" -- engine content src mazeworld.html` — it must list exactly `engine/character.js` and `engine/state.js` (the dev-only force option). Record PIN (full and `--short`) for the ledger.
    4. `mkdir -p docs/class-pass`.

    Launch (background, sentinel, bounded polling — never a foreground wait): run `node tools/tune-classes.mjs --seeds 40 --workers 4 --max-actions 5000 --out docs/class-pass/before.json > "<scratchpad>/before-natural.txt" 2>&1; echo "EXIT=$?" >> "<scratchpad>/before-natural.txt"` with the Bash tool's `run_in_background: true`, where `<scratchpad>` is the session scratchpad directory. Poll by reading the tail of that file in bounded checks (each check is a short `tail -3` call; wait no more than 60 s between checks; no single Bash call longer than 60 s), until a line `EXIT=<code>` appears. Abort threshold: 90 minutes wall (expected ~15-25 min on 4 cores at ~1.5 ms/action; the Samurai/Wizard loop fixes removed most stuck runs). If the threshold passes, kill the process, record the partial progress, and stop — do NOT commit a partial snapshot.

    On `EXIT=0`: verify `docs/class-pass/before.json` parses, has `meta.seeds === 40`, `meta.startDepth === 1`, `meta.maxActions === 5000`, `meta.workers === 4`, `cells.length === 143`, `meta.commit` equals `git rev-parse --short HEAD`, and that no object anywhere in it has a `rows` key (aggregates only). Keep the text transcript file — Task 3 pastes it verbatim. Do not commit yet (Task 3 makes the single ledger commit).
  </action>
  <verify>
    <automated>node -e "const r=require('./docs/class-pass/before.json');const walk=(o)=>o&&typeof o==='object'?Object.keys(o).some(k=>k==='rows')||Object.values(o).some(walk):false;console.log(r.meta.seeds,r.meta.startDepth,r.meta.maxActions,r.meta.workers,r.cells.length,walk(r))"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `40 1 5000 4 143 false`.
    - `test "$(node -e "console.log(require('./docs/class-pass/before.json').meta.commit)")" = "$(git rev-parse --short HEAD)"` succeeds (the JSON names the pinned commit).
    - `git diff --quiet HEAD -- engine content src mazeworld.html; echo rc=$?` prints `rc=0` (engine tree identical to the pin throughout).
    - `git diff --name-only 1b4daed HEAD -- engine content src mazeworld.html | sort | tr '\n' ' '` prints exactly `engine/character.js engine/state.js `.
    - The scratchpad transcript file ends with `EXIT=0`, contains exactly one line starting with `Bot: exploreBudget=50  maxActions=5000  party=off` and that line ends with `seeds=40  workers=4  startDepth=1`.
    - `node -e "const r=require('./docs/class-pass/before.json');console.log(r.cells.every(c=>c.n===40), r.cells.filter(c=>c.completed===0).length)"` prints `true <k>` where `<k>` is the number of all-stuck cells (expected 0; if k > 0 the cells are listed in Task 3's Outliers section, never silently dropped).
  </acceptance_criteria>
  <done>The natural-start BEFORE matrix (143 x 40) completed against the pinned, proven-clean engine and its aggregate JSON is on disk with the correct meta; the text transcript is saved for the ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Run the depth-20 BEFORE slice in the background</name>
  <files>docs/class-pass/before-depth20.json</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md lines 1072-1110 (what a depth-20 dev-start character looks like — level 5, SP 1501, depth-scaled purse — so the slice's numbers are read correctly)
    - tools/tune-classes.mjs header (`--start-depth`; the extra gained/survived columns appear only when startDepth > 1)
    - The Task 1 transcript file (to reuse the exact same discipline and confirm the Bot line differs from Task 1's ONLY in `seeds=` and `startDepth=`)
  </read_first>
  <action>
    Re-check `git status --porcelain` is empty except the untracked `docs/class-pass/before.json` from Task 1 and that `git diff --quiet HEAD -- engine content src mazeworld.html` still exits 0. Launch in the background exactly like Task 1: `node tools/tune-classes.mjs --seeds 10 --workers 4 --max-actions 5000 --start-depth 20 --out docs/class-pass/before-depth20.json > "<scratchpad>/before-depth20.txt" 2>&1; echo "EXIT=$?" >> "<scratchpad>/before-depth20.txt"`. Poll in bounded checks until `EXIT=<code>`; abort threshold 45 minutes (1,430 deep runs; deep deaths are fast). On `EXIT=0` verify the JSON: `meta.seeds === 10`, `meta.startDepth === 20`, `cells.length === 143`, `meta.commit` = short HEAD, no `rows` key anywhere, every cell `n === 10`, and every non-null `meanDepth >= 20`. Confirm the text transcript's table includes the gained/survived columns and its `Bot:` line ends with `seeds=10  workers=4  startDepth=20`. Do not commit yet.
  </action>
  <verify>
    <automated>node -e "const r=require('./docs/class-pass/before-depth20.json');const walk=(o)=>o&&typeof o==='object'?Object.keys(o).some(k=>k==='rows')||Object.values(o).some(walk):false;console.log(r.meta.seeds,r.meta.startDepth,r.cells.length,walk(r),r.cells.every(c=>c.n===10),r.cells.every(c=>c.meanDepth===null||c.meanDepth>=20))"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `10 20 143 false true true`.
    - `test "$(node -e "console.log(require('./docs/class-pass/before-depth20.json').meta.commit)")" = "$(git rev-parse --short HEAD)"` succeeds.
    - The scratchpad transcript ends with `EXIT=0` and its `Bot:` line ends with `seeds=10  workers=4  startDepth=20`.
    - `node -e "const a=require('./docs/class-pass/before.json').meta.bot,b=require('./docs/class-pass/before-depth20.json').meta.bot;console.log(a.replace(/seeds=\d+/,'seeds=X').replace(/startDepth=\d+/,'startDepth=X')===b.replace(/seeds=\d+/,'seeds=X').replace(/startDepth=\d+/,'startDepth=X'))"` prints `true` (identical bot parameters apart from seeds/startDepth).
  </acceptance_criteria>
  <done>The depth-20 BEFORE slice (143 x 10) completed against the same pinned engine with the same bot parameters; its aggregate JSON and transcript are on disk.</done>
</task>

<task type="auto">
  <name>Task 3: Write docs/CLASS-PASS.md (Bot proxy, BEFORE, Outliers/Findings, placeholders) and make the single BEFORE commit</name>
  <files>docs/CLASS-PASS.md, docs/class-pass/before.json, docs/class-pass/before-depth20.json</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md lines 1-112 (ledger voice, the parameter table, the BEFORE paragraph shape — mirror them; this ledger is its sibling)
    - .planning/phases/22-class-aware-harness-before-matrix/22-02-SUMMARY.md (the final chooseSpell table + decideAction order to transcribe into the Bot proxy section; the six planning-time stuck seeds)
    - .planning/research/CLASS-AUDIT-2026-09-14.md (the pre-phase 400-seed baseline this snapshot supersedes; cite it as superseded, do not copy its numbers)
    - Both scratchpad transcripts from Tasks 1 and 2 (pasted verbatim)
    - .planning/ROADMAP.md Phase 23-27 goal lines (to name what each placeholder section will hold)
  </read_first>
  <action>
    Create `docs/CLASS-PASS.md` (new file; family-friendly, deadpan ledger voice like DIFFICULTY-RETUNE.md — no verdict words in the BEFORE tables). Sections, in order:
    1. `# Class Pass (v1.2, Phases 22-27) — before/after ledger` — one paragraph: purpose (PLAY-01 BEFORE, PLAY-02 AFTER + fun-band verdicts in Phase 26, PLAY-03 rulings in Phase 24), the TUNING PROXY / not-a-gate caveat, and the sentence that the matrix is 143 cells because Fridgian Samurai is canon-impossible ("Fridges don't wear any armor" — the prototype rerolls the sub), so ROADMAP's "144-combo" reads as 143 + 1 footnoted exclusion.
    2. `## Bot proxy (HARN-02)` — prose summary of the sub-class-aware policy (openers, talk-first parley, sing, summon in/out of combat, kill/damage/disable/heal tiers, Shield placement, Samurai never flees, Wizard strike-refused fallback, scroll reading, no useItem) transcribed from 22-02-SUMMARY, the `chooseSpell` tier/score table verbatim, and a parameter table mirroring DIFFICULTY-RETUNE.md's: seeds per cell (40 natural / 10 at depth 20; list `i*7919+1`), workers 4, per-run action cap 5,000, exploration budget 50, flee 0.3 / 0.5 vs kit-bearing (v1.1 D-06), potion/camp < 0.5 (v1.1 D-05), start depth 1 / 20, party off. State that BEFORE and AFTER MUST use identical values and that the `Bot:` line inside each transcript records them verbatim for grep-diffing.
    3. `## How to reproduce` — the two exact command lines from Tasks 1 and 2 (with `--out` paths), the preflight (`git status --porcelain` empty, `npm test` green, `git diff --quiet <hash> -- engine content src mazeworld.html`), and the background/sentinel/bounded-poll discipline in two sentences.
    4. `## BEFORE — commit <full PIN hash> (pre-identity-pass engine)` — the pin paragraph: captured date; `git diff --quiet <PIN> -- engine content src mazeworld.html` exited 0 immediately before each run and again after this ledger's commit; the ONLY engine/content/src difference between the v1.1-close baseline `1b4daed` and `<PIN>` is the dev-only `force` option in `engine/character.js` and `engine/state.js` (no rule, number or narration changed; parity byte-identical, no carve-out); wall times of both runs (from the stderr `elapsed:` lines); then `### tune-classes --seeds 40 --workers 4 --max-actions 5000` with the Task 1 stdout transcript pasted verbatim inside a fenced block (including its `Stuck:` and `Bot:` lines), then `### tune-classes --seeds 10 --workers 4 --max-actions 5000 --start-depth 20` with the Task 2 transcript verbatim, then a line naming the two JSON files as the machine-diffable aggregates for Phase 26 (`docs/class-pass/before.json`, `docs/class-pass/before-depth20.json`; aggregates only, no per-run rows).
    5. `## Outliers / Findings (Phase 22)` — (a) the stuck investigation: the v1.1 baseline's 19/400 stuck runs were 12 Samurai `fleeRefused` loops and 7 Wizard `strikeRefused` loops, both bot-side no-progress loops fixed in `tools/lib/tuning-bot.mjs` (Rule 1; list the six seeds 229652, 783982, 894848, 926524, 1195770, 1274960); (b) the ENGINE finding carried to Phase 23 (IDENT-01): a Wizard with charges but no castable attack spell cannot melee (`playerStrike` refuses) — the bot now casts a utility spell or flees, the engine was NOT changed here; (c) no unreachable-exit or other engine dead-end appeared in the 400-seed scan; (d) any cell in either JSON with `stuck > 0` listed by name with its count (or the explicit sentence that no cell had a stuck run); (e) the sentence that the 400-seed audit numbers in `.planning/research/CLASS-AUDIT-2026-09-14.md` are superseded by this BEFORE (they were captured with the thrown-only bot).
    6. `## Rulings (Phase 24 — PLAY-03)` — headed placeholder: one sentence saying every sub-class/race good-bad ruling lands here with its rationale, nothing yet.
    7. `## AFTER — commit <hash> (Phase 26 — PLAY-02)` — headed placeholder: the two reproduce commands with `--out docs/class-pass/after.json` / `after-depth20.json`, the instruction to grep-diff the `Bot:` lines against BEFORE, and "fun-band verdicts per row are written here, not in the tool".

    Then commit ALL THREE files in ONE commit via the GSD commit helper (message: `docs(22): BEFORE class matrix — 143x40 natural + 143x10 depth-20, engine pinned <short PIN>`). After the commit, re-run `git diff --quiet <PIN> -- engine content src mazeworld.html` and record its exit code (must be 0) in the SUMMARY; the ledger's pin paragraph already asserts it.
  </action>
  <verify>
    <automated>test -f docs/CLASS-PASS.md && grep -c "^## " docs/CLASS-PASS.md && grep -c "$(node -e "console.log(require('./docs/class-pass/before.json').meta.bot)")" docs/CLASS-PASS.md && git diff --quiet HEAD -- engine content src mazeworld.html && echo ENGINE_CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## " docs/CLASS-PASS.md` >= 6 (Bot proxy, How to reproduce, BEFORE, Outliers / Findings, Rulings, AFTER).
    - With `PIN=$(node -e "console.log(require('./docs/class-pass/before.json').meta.commit)")` (the pinned short hash — HEAD itself moves with the ledger commit, so never compare against HEAD here): `grep -c "$PIN" docs/CLASS-PASS.md` >= 2 (the hash appears in the BEFORE heading and the pin paragraph) and `git rev-parse --verify "$PIN^{commit}"` succeeds.
    - `grep -c "$(node -e "console.log(require('./docs/class-pass/before.json').meta.bot)")" docs/CLASS-PASS.md` >= 1 and `grep -c "$(node -e "console.log(require('./docs/class-pass/before-depth20.json').meta.bot)")" docs/CLASS-PASS.md` >= 1 (both Bot lines transcribed verbatim).
    - `grep -c "1b4daed" docs/CLASS-PASS.md` >= 1; `grep -c "Samurai" docs/CLASS-PASS.md` >= 3; `grep -c "Wizard" docs/CLASS-PASS.md` >= 2; `grep -c "IDENT-01" docs/CLASS-PASS.md` >= 1.
    - `grep -c "^## Rulings" docs/CLASS-PASS.md` = 1 and `grep -c "^## AFTER" docs/CLASS-PASS.md` = 1 and `grep -c "^## Outliers" docs/CLASS-PASS.md` = 1.
    - `git status --porcelain` prints nothing after the commit; `git log -1 --format=%s` starts with `docs(22): BEFORE class matrix`; `git show --stat HEAD --format= | grep -c "docs/CLASS-PASS.md\|docs/class-pass/before.json\|docs/class-pass/before-depth20.json"` = 3.
    - `git diff --quiet "$PIN" -- engine content src mazeworld.html; echo rc=$?` prints `rc=0` after the ledger commit.
    - `git diff --quiet -- test/parity/prototype-master.js.txt test/parity/harness/comparables.js` exits 0 and `git log --oneline 1b4daed..HEAD -- test/parity/prototype-master.js.txt | wc -l` prints `0` (master never edited this phase).
  </acceptance_criteria>
  <done>docs/CLASS-PASS.md exists with the pinned BEFORE section (both transcripts verbatim, Bot lines identical to the JSON), the Bot proxy and reproduce sections, the stuck-investigation findings including the Phase 23 engine finding, and headed Rulings/AFTER placeholders; the ledger and both aggregate JSONs are committed in one commit on an engine tree proven identical to the pin before and after.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → snapshot | the capture is only meaningful on a committed, clean engine tree matching the pin |
| tool output → ledger | transcripts are pasted verbatim; any hand edit would break the grep-diff contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-14 | Repudiation | BEFORE provenance | critical | mitigate | full hash pinned in the ledger + meta.commit in both JSONs; git diff --quiet proven before and after; 1b4daed diff limited to the force option |
| T-22-15 | Tampering | partial snapshot committed | high | mitigate | never commit on a non-zero EXIT or after the abort threshold; --out is temp-then-rename; all three files land in one commit |
| T-22-16 | Tampering | Bot: line drift between BEFORE and AFTER | high | mitigate | botLine is the single emitter; both lines transcribed verbatim and asserted equal to meta.bot |
| T-22-17 | Information Disclosure | ledger contents | low | accept | aggregate numbers about a fictional dungeon; no personal data |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No installs in this plan |
</threat_model>

<verification>
- Both JSON snapshots parse, carry `meta.commit` = pinned short hash, 143 cells each, no `rows` key, correct seeds/startDepth.
- `docs/CLASS-PASS.md` contains both `Bot:` lines verbatim, the pinned hash, 1b4daed, the Samurai/Wizard findings, and the Rulings/AFTER placeholders.
- `git diff --quiet <PIN> -- engine content src mazeworld.html` exits 0 after the ledger commit; working tree clean.
</verification>

<success_criteria>
- A BEFORE matrix (natural start + depth-20 slice) is committed to the class-pass ledger against a commit-pinned engine whose only difference from the v1.1 close is the dev-only force option — before any Phase 23 change lands.
- Phase 26 can re-run the exact commands, grep-diff the `Bot:` line, and machine-diff the JSON.
</success_criteria>

<output>
Create `.planning/phases/22-class-aware-harness-before-matrix/22-04-SUMMARY.md` when done. Record: the pinned hash, both wall times, the total stuck count per run, the list of any all-stuck cells, and the ledger commit hash. Then note in STATE.md's Ground Truth that `docs/CLASS-PASS.md` now exists and Phase 23 may begin.
</output>
