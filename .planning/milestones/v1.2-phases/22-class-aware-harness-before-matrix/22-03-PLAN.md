---
phase: 22-class-aware-harness-before-matrix
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - tools/lib/class-matrix.mjs
  - tools/tune-classes.mjs
  - tools/tune-difficulty.mjs
  - test/unit/class-matrix.test.js
autonomous: true
requirements:
  - HARN-03
  - HARN-04
  - HARN-01
tags: [harness, matrix, worker-threads, cli, dev-only]

must_haves:
  truths:
    - "Names resolve case-insensitively at the CLI to the exact canonical CLASSES/RACES/subs string keys (e.g. --sub summoner -> 'Summoner', --cls 'magic user' -> 'Magic User'); the engine-side force option accepts canonical keys only and throws on anything else."
    - "Ranking ties break deterministically: mean death depth desc, then p50 desc, then reach>=5 desc, then sub name asc, then race name asc — documented at the top of tools/tune-classes.mjs and asserted by a unit test on a synthetic tie."
    - "--seeds 0 or a negative/NaN value exits non-zero with a usage message; a cell whose every run is stuck reports depth metrics as n/a in text and null in JSON (never NaN) with stuck=N; the 143-cell matrix is complete even if some cells are all-stuck."
    - "Cells are keyed by the canonical { cls, sub, race } strings from content/classes.js and content/races.js; the text table and the JSON use the identical strings; Fridgian+Samurai is omitted with a footnote and a meta.excluded entry."
    - "Output is independent of worker scheduling: per-cell aggregates are computed from the fixed seed list i*7919+1 (i<N) and the cells/rollups JSON is byte-identical across --workers 1 and --workers 4 (meta differs only in workers and the workers= token of the Bot line) — asserted by an acceptance run in this plan."
    - "Workers never share engine state (each worker imports the engine and plays its own runs); the main thread only aggregates; the --out JSON snapshot file is written once, after every worker has returned, via write-to-temp-then-rename, so an interrupted run leaves no partial docs/class-pass/*.json."
  artifacts:
    - path: "tools/lib/class-matrix.mjs"
      provides: "cell enumeration (143), name/force resolution, seed list, per-cell aggregation with stuck bucket, deterministic ranking, roll-ups, report builder and text formatter"
      exports: ["enumerateCells", "EXCLUDED_CELLS", "resolveName", "resolveForce", "selectCells", "seedList", "rowFromRun", "summarizeRows", "rankCells", "rollups", "buildReport", "formatText"]
    - path: "tools/tune-classes.mjs"
      provides: "the matrix CLI with worker_threads parallelism, --json/--out, documented JSON shape and tie-break"
      min_lines: 150
    - path: "tools/tune-difficulty.mjs"
      provides: "--start-depth=N flag and the stuck bucket in text + JSON"
      contains: "start-depth"
    - path: "test/unit/class-matrix.test.js"
      provides: "cells/exclusion/name resolution/seed list/aggregation/tie-break/roll-ups/force pass-through/purity tests"
      min_lines: 120
  key_links:
    - from: "tools/tune-classes.mjs"
      to: "tools/lib/tuning-bot.mjs"
      via: "workers call playRun(seed, { ...opts, force: cell })"
      pattern: "playRun\\("
    - from: "tools/tune-classes.mjs"
      to: "tools/lib/class-matrix.mjs"
      via: "main thread imports enumerate/select/summarize/rank/buildReport/formatText"
      pattern: "class-matrix.mjs"
    - from: "tools/lib/class-matrix.mjs"
      to: "content/classes.js"
      via: "CLASSES/RACES imported from content/index.js as the canonical key source"
      pattern: "CLASSES"
  prohibitions:
    - statement: "The harness (tools/tune-classes.mjs, tools/lib/*.mjs) MUST NOT be shipped: never referenced from tools/build-www.mjs's copy list and never present under www/ after a build."
      status: resolved
      verification: test
    - statement: "The matrix tool MUST NOT print fun-band verdicts, over/under-performer labels, or any editorial judgment per row — numbers only; verdicts are Phase 26 (PLAY-02) editorial work in the ledger."
      status: resolved
      verification: test
    - statement: "Deep-start runs (--start-depth) MUST NOT receive extra kit or gear grants beyond what newRun's existing dev path already produces (SP threshold level-up + depth-scaled purse) — a gear grant is a Deferred Idea."
      status: resolved
      verification: test
    - statement: "No new npm dependency MUST be added for parallelism or anything else in this phase — worker_threads is a Node built-in; package.json dependencies/devDependencies stay byte-identical."
      status: resolved
      verification: test
---

<objective>
Build `tools/tune-classes.mjs` — the class-aware matrix tool (HARN-03) — on a small pure helper module `tools/lib/class-matrix.mjs`, with `worker_threads` parallelism (zero dependencies), the paired seed list `i*7919+1`, a 5,000-action default cap, ranked rows with mean/p50/p90 death depth, reach >=5/>=10 %, mean kills, mean level, mean actions, stuck count and top-3 death causes, roll-ups by class/sub/race, the grep-stable `Bot:` line (extended with seeds/workers/startDepth), `--json` output plus atomic `--out`, and `--start-depth` (HARN-04) on BOTH this tool and `tools/tune-difficulty.mjs`. Expose the force seam as `--cls/--sub/--race` (HARN-01's CLI half) with case-insensitive resolution to canonical keys and a friendly refusal of Fridgian Samurai BEFORE newRun.

Per 22-CONTEXT.md "Matrix tool, volume, and the BEFORE snapshot (HARN-03, PLAY-01)" and "Start-at-depth for the bot (HARN-04)": 40 seeds/combo default (143 x 40 = 5,720 runs), knobs `--seeds --workers --max-actions --start-depth --sub --race --json`, no fun-band verdicts, deep runs also report floorsGained and encountersSurvived, `--start-depth` reuses newRun's option via playRun with no extra gear.

Claude's Discretion choices (documented in the tool header): work is distributed by CELL through a main-thread queue (each worker plays whole cells and returns compact per-run rows; the main thread aggregates, so p50/p90 and roll-ups are true percentiles over runs and the output is scheduling-independent); JSON has NO timing fields (elapsed goes to stderr) so BEFORE/AFTER files diff cleanly; `--out PATH` writes atomically.

Purpose: HARN-03/HARN-04 — the instrument the BEFORE snapshot (Plan 22-04) and Phase 26's AFTER snapshot are taken with.
Output: `tools/lib/class-matrix.mjs`, `tools/tune-classes.mjs`, updated `tools/tune-difficulty.mjs`, `test/unit/class-matrix.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-class-aware-harness-before-matrix/22-CONTEXT.md
@.planning/phases/22-class-aware-harness-before-matrix/22-01-SUMMARY.md
@.planning/phases/22-class-aware-harness-before-matrix/22-02-SUMMARY.md

@tools/lib/tuning-bot.mjs
@tools/tune-difficulty.mjs
@content/classes.js
@content/races.js
@engine/character.js
@test/unit/tuning-bot.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `engine/character.js` — `rollCharacter(rng, exclude = [], force = null)`; `force` shape `{ cls?, sub?, race? }` canonical keys; throws on unknown keys or Fridgian+Samurai. [22-01]
- `engine/state.js` — `newRun(seed, exclude = [], { startDepth = 1, force = null } = {})`. [22-01]
- `test/determinism/forced-chargen.test.js`. [22-01]
- `tools/lib/tuning-bot.mjs` — `chooseSpell`, `isTalkFirst`, `botLine`, extended `decideAction`/`observe`/`makeBotContext`/`playRun` (`opts.startDepth`, `opts.force`; result `stuck`, `outcome`, `startDepth`, `floorsGained`, `encountersSurvived`), `BOT_DEFAULTS.startDepth`. [22-02]
- `test/unit/tuning-bot.test.js` extensions. [22-02]
- `tools/lib/class-matrix.mjs` — `enumerateCells()` (143 canonical `{cls, sub, race}`), `EXCLUDED_CELLS`, `resolveName(kind, raw)`, `resolveForce({cls, sub, race})`, `selectCells(force)`, `seedList(n)`, `rowFromRun(run)`, `summarizeRows(rows)`, `rankCells(cells)`, `rollups(cellRows)`, `buildReport({ cellRows, opts, commit })`, `formatText(report)`. [22-03]
- `tools/tune-classes.mjs` — CLI flags `--seeds N` (default 40), `--workers N` (default os.availableParallelism()), `--max-actions N` (default 5000), `--start-depth N` (default 1), `--explore-budget N` (default 50), `--cls X`, `--sub X`, `--race X`, `--json`, `--out PATH`; both `--flag value` and `--flag=value` forms; same file runs as main thread and as worker (isMainThread branch). [22-03]
- `tools/tune-difficulty.mjs` — `--start-depth=N`; stuck bucket in text (Outcome line) and JSON (`stuck`, `completed`, depth/causes over completed runs). [22-03]
- `test/unit/class-matrix.test.js`. [22-03]
- `docs/CLASS-PASS.md`, `docs/class-pass/before.json`, `docs/class-pass/before-depth20.json`. [22-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/lib/class-matrix.mjs (pure matrix helpers) and its unit tests</name>
  <files>tools/lib/class-matrix.mjs, test/unit/class-matrix.test.js</files>
  <read_first>
    - tools/lib/tuning-bot.mjs (post-22-02: `percentile`, `distribution`, `botLine`, `BOT_DEFAULTS`, playRun's result fields `deathDepth`, `kills` via `state.c.kills`, `state.c.level`, `actions`, `stuck`, `cause`, `floorsGained`, `encountersSurvived`, `won`)
    - content/classes.js (CLASSES key order and each `subs` array — the canonical enumeration order), content/races.js (RACES key order; RACE_D8 is NOT the enumeration source)
    - engine/character.js (post-22-01: the exact Error messages the engine throws — the CLI resolver must reject Fridgian+Samurai BEFORE newRun with its own friendlier message)
    - test/unit/tuning-bot.test.js (test-file conventions; the purity source-scan pattern to replicate for class-matrix.mjs)
    - .planning/phases/22-class-aware-harness-before-matrix/22-CONTEXT.md, section "Matrix tool, volume, and the BEFORE snapshot"
  </read_first>
  <action>
    Create `tools/lib/class-matrix.mjs` — a pure, zero-dependency Node ESM helper module (no Math.random / Date.now / fs / os / worker_threads — the tool file owns I/O and threads). Header comment: dev-only, TUNING PROXY NOT A GATE (copy the tuning-bot.mjs wording), the ranking tie-break rule, and the JSON shape (below). Imports: `CLASSES`, `RACES` from `../../content/index.js`; `percentile`, `botLine` from `./tuning-bot.mjs`.

    Exports:
    - `EXCLUDED_CELLS = Object.freeze([{ cls: "Fighter", sub: "Samurai", race: "Fridgian", reason: "canon-impossible: Fridges don't wear any armor (the prototype rerolls the sub)" }])`.
    - `enumerateCells()` → for each `cls` in `Object.keys(CLASSES)`, each `sub` in `CLASSES[cls].subs`, each `race` in `Object.keys(RACES)` (that nested order), skipping EXCLUDED_CELLS → exactly 143 `{ cls, sub, race }` objects.
    - `resolveName(kind, raw)` with kind in `"cls" | "sub" | "race"`: trims, matches case-insensitively against the canonical keys (`Object.keys(CLASSES)`, the union of all `subs`, `Object.keys(RACES)`), returns the canonical string, else throws an Error listing the valid keys for that kind.
    - `resolveForce({ cls, sub, race })` (all optional, raw CLI strings) → canonical `{ cls?, sub?, race? }` with `cls` inferred from `sub` when only sub is given (find the class whose subs include it); throws when a given cls does not own the given sub; throws a clear message when the pair is Samurai + Fridgian (quote the canon line and say the matrix omits this cell) — this fires BEFORE any newRun. Returns `null` when no key is given.
    - `selectCells(force)` → `enumerateCells()` filtered by whichever of cls/sub/race the (resolved) force sets; `null` → all 143.
    - `seedList(n)` → `Array.from({ length: n }, (_, i) => i * 7919 + 1)` (the exact stride tune-difficulty uses — paired seeds across every cell).
    - `rowFromRun(run)` → `{ seed, deathDepth, floorsGained, kills: run.state.c.kills, level: run.state.c.level, actions, stuck, cause, won, encounters: run.tallies.encounters, encountersSurvived }` (compact; no state object).
    - `summarizeRows(rows)` → sort a copy by `seed` first (order-independence), then `completed = rows.filter(r => !r.stuck)`; returns `{ n, completed: completed.length, stuck: n - completed.length, meanDepth, p50Depth, p90Depth, reach5, reach10, meanKills, meanLevel, meanActions, meanFloorsGained, p50FloorsGained, meanEncountersSurvived, topCauses }` where every depth/kill/level/actions/floors/encounters metric is computed over `completed` only and is `null` when `completed.length === 0` (never NaN); means rounded to 2 decimals; `reach5`/`reach10` = percent of completed runs with `deathDepth >= 5 / >= 10`, one decimal; `p50`/`p90` via `percentile` over the sorted depths; `topCauses` = the three most frequent `cause` strings among completed runs as `[{ cause, count }]` sorted by count desc then cause asc.
    - `rankCells(cells)` → a sorted COPY using exactly: `meanDepth` desc with `null` last, then `p50Depth` desc, then `reach5` desc, then `sub` asc, then `race` asc (plain `<`/`>` string comparison, not localeCompare), assigning `rank` 1..N.
    - `rollups(cellRows)` where `cellRows` is `[{ cell, rows }]` → `{ byClass, bySub, byRace }`, each an array of `{ key, ...summarizeRows(concatenated rows of every member cell) }` ranked with the same comparator (key asc as the final tiebreak).
    - `buildReport({ cellRows, opts, commit })` → `{ meta, cells, rollups }` with `meta = { tool: "tune-classes", commit, seeds: opts.seeds, seedList: "i*7919+1", workers: opts.workers, maxActions: opts.maxActions, startDepth: opts.startDepth, exploreBudget: opts.exploreBudget, bot: botLine(opts), cells: cellRows.length, excluded: EXCLUDED_CELLS, filter: { cls, sub, race } (nulls when unset) }`, `cells = rankCells(cellRows.map(({cell, rows}) => ({ ...cell, ...summarizeRows(rows) })))`. NO timing fields anywhere in the report.
    - `formatText(report)` → the deterministic text block (string): header `tune-classes: <cells> cells x <seeds> seeds (<runs> runs) — start depth <D>`, the TUNING PROXY warning line, a fixed-width ranked table with columns `#  class  sub  race  n  stuck  mean  p50  p90  >=5%  >=10%  kills  lvl  actions  top causes` (plus `gained(mean/p50)  survived` columns when `meta.startDepth > 1`; `n/a` for null metrics), then three roll-up tables (BY CLASS / BY SUBCLASS / BY RACE) with the same columns minus rank, a footnote line for each `meta.excluded` entry, a `Stuck: <total stuck> of <total runs> runs hit maxActions=<cap> (own bucket; excluded from depth stats)` line, and finally `meta.bot` verbatim as the last line. No verdict words anywhere (no "fun", "strong", "weak", "over", "under").

    Create `test/unit/class-matrix.test.js` (node:test, assert/strict): (1) `enumerateCells().length === 143`, every cell has canonical strings, no cell equals the excluded triple, 24 distinct subs and 6 races present; (2) `resolveName("sub", "summoner") === "Summoner"`, `resolveName("cls", "magic user") === "Magic User"`, `resolveName("race", " TROLL ") === "Troll"`, `resolveName("sub", "Paladin")` throws with a message containing "Wizard"; (3) `resolveForce({ sub: "summoner" })` deepStrictEqual `{ cls: "Magic User", sub: "Summoner" }`; `resolveForce({ cls: "fighter", sub: "wizard" })` throws; `resolveForce({ sub: "samurai", race: "fridgian" })` throws with /Fridg/i; `resolveForce({})` is null; (4) `selectCells({ race: "Troll" }).length === 24`, `selectCells({ sub: "Samurai" }).length === 5`, `selectCells({ cls: "Thief" }).length === 48`; (5) `seedList(3)` deepStrictEqual `[1, 7920, 15839]`; (6) `summarizeRows` on synthetic rows: all-stuck rows → `meanDepth === null`, `p50Depth === null`, `reach5 === null`, `stuck === n`; mixed rows exclude the stuck row from every depth metric and count it; topCauses caps at 3 and orders by count then name; (7) `rankCells` on a synthetic tie: equal meanDepth and p50 → higher reach5 first; fully equal metrics → sub asc then race asc; a null-meanDepth cell ranks last; (8) `rollups` on two synthetic cells of the same class → byClass has 1 entry whose `n` is the sum; bySub/byRace keyed correctly; (9) `buildReport` on two synthetic cells has `meta.cells === 2`, `meta.excluded.length === 1`, no key named `elapsed`/`ms`/`time` anywhere (walk the object), `meta.bot` starts with `Bot: `; `formatText` output contains the excluded footnote, the `Stuck:` line, ends with the `Bot:` line, and does not contain the words "fun", "strong" or "weak"; (10) force pass-through smoke (HARN-01 via the bot): `playRun(1, { ...BOT_DEFAULTS, maxActions: 5, force: resolveForce({ sub: "summoner", race: "troll" }) }).state.c` has `sub === "Summoner"`, `race === "Troll"`, `cls === "Magic User"`; (11) purity: the module source has no `Math.random`/`Date.now` line outside comments (replicate the tuning-bot purity scan).
  </action>
  <verify>
    <automated>node --test test/unit/class-matrix.test.js 2>&1 | grep -E '^# (pass|fail)'</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/class-matrix.test.js 2>&1 | grep -E '^# fail 0'` matches and `grep -E '^# pass [0-9]+'` shows >= 11 tests.
    - `node -e "import('./tools/lib/class-matrix.mjs').then(m=>console.log(m.enumerateCells().length, m.EXCLUDED_CELLS.length, m.selectCells({race:'Troll'}).length, JSON.stringify(m.seedList(3))))"` prints `143 1 24 [1,7920,15839]`.
    - `node -e "import('./tools/lib/class-matrix.mjs').then(m=>console.log(JSON.stringify(m.resolveForce({sub:'summoner'}))))"` prints `{"cls":"Magic User","sub":"Summoner"}`.
    - `node -e "import('./tools/lib/class-matrix.mjs').then(m=>{try{m.resolveForce({sub:'Samurai',race:'Fridgian'});console.log('NO THROW')}catch(e){console.log('THREW',/Fridg/i.test(e.message))}})"` prints `THREW true`.
    - `node -e "import('./tools/lib/class-matrix.mjs').then(m=>{const s=m.summarizeRows([{seed:1,deathDepth:3,stuck:true,cause:'maxActionsHit',kills:0,level:1,actions:5000,floorsGained:2,encounters:1,encountersSurvived:1,won:false}]);console.log(s.meanDepth,s.p50Depth,s.reach5,s.stuck,s.completed)})"` prints `null null null 1 0`.
    - `grep -c "Math.random\|Date.now\|worker_threads\|from \"node:fs\"\|from \"fs\"" tools/lib/class-matrix.mjs` prints 0 or matches only inside comment lines (verify with `grep -v '^\s*//'` piped first: `grep -v '^\s*//' tools/lib/class-matrix.mjs | grep -c 'Math.random\|Date.now\|worker_threads\|node:fs'` prints `0`).
  </acceptance_criteria>
  <done>class-matrix.mjs provides deterministic enumeration, resolution, aggregation (stuck-aware, null-safe), ranking with the documented tie-break, roll-ups, report and text formatting, all pinned by unit tests including the force pass-through smoke.</done>
</task>

<task type="auto">
  <name>Task 2: Write tools/tune-classes.mjs — the worker_threads matrix CLI with --json and atomic --out</name>
  <files>tools/tune-classes.mjs</files>
  <read_first>
    - tools/tune-difficulty.mjs (header style, parseArgs shape, `main()` call convention, the `--flag=value` form to keep supporting)
    - tools/lib/class-matrix.mjs (post-Task-1 API), tools/lib/tuning-bot.mjs (post-22-02: `playRun`, `BOT_DEFAULTS`, `botLine`)
    - docs/DIFFICULTY-RETUNE.md lines 54-85 (the ledger's parameter table; the tool's header must name every knob so the ledger can transcribe it)
    - Node docs for `node:worker_threads` (`Worker`, `isMainThread`, `parentPort`, `workerData`; `new Worker(new URL(import.meta.url), { workerData })` for a same-file ESM worker) and `node:os` (`availableParallelism`)
  </read_first>
  <action>
    Create `tools/tune-classes.mjs` (shebang `#!/usr/bin/env node`, ESM). Header comment: dev-only / NOT shipped / NOT a node:test file / TUNING PROXY NOT A GATE (copy the wording), the run examples (`node tools/tune-classes.mjs`, `--seeds 40 --workers 4 --out docs/class-pass/before.json`, `--start-depth 20 --seeds 10`, `--sub Summoner --race Troll --seeds 5`, `--json`), the full JSON shape (copy from class-matrix.mjs's header: `{ meta: { tool, commit, seeds, seedList, workers, maxActions, startDepth, exploreBudget, bot, cells, excluded, filter }, cells: [{ rank, cls, sub, race, n, completed, stuck, meanDepth, p50Depth, p90Depth, reach5, reach10, meanKills, meanLevel, meanActions, meanFloorsGained, p50FloorsGained, meanEncountersSurvived, topCauses }], rollups: { byClass, bySub, byRace } }`), the ranking tie-break sentence, the work-distribution note (by cell via a main-thread queue; the main thread only aggregates; no shared engine state), and the statement that the JSON carries no timing fields.

    `parseArgs(argv)`: accept BOTH `--flag value` and `--flag=value`. Defaults: `{ ...BOT_DEFAULTS, seeds: 40, workers: os.availableParallelism(), maxActions: 5000, startDepth: 1, json: false, out: null, cls: null, sub: null, race: null }` (note `maxActions` 5000 overrides BOT_DEFAULTS.maxActions for matrix runs, per CONTEXT). Validation: `--seeds`, `--workers`, `--max-actions`, `--start-depth` must parse as finite integers >= 1, `--explore-budget` >= 0; any violation or an unknown flag prints a usage block to stderr and exits with code 2. Resolve `--cls/--sub/--race` through `resolveForce` inside a try/catch that prints the error message and exits 2 (this is where Fridgian Samurai is refused, BEFORE any newRun).

    Main thread (`isMainThread`): `cells = selectCells(force)`; `seeds = seedList(opts.seeds)`; `runOpts = { ...opts minus cli-only keys }`. If `opts.workers === 1` or `cells.length === 1`: play inline — `rows = seeds.map(s => rowFromRun(playRun(s, { ...runOpts, force: cell })))` per cell (same function the worker uses; export nothing — keep a local `playCell(cell, seeds, runOpts)`). Otherwise spawn `min(workers, cells.length)` Workers with `new Worker(new URL(import.meta.url), { workerData: { runOpts, seeds } })`; keep a queue of cell indices; on each worker `message` `{ cell, rows }` store rows for that cell and post the next cell or terminate the worker when the queue is empty; on `error` print and `process.exit(1)`; resolve when every cell has rows. Build `cellRows = cells.map(cell => ({ cell, rows }))` in enumeration order (NOT completion order). `commit` = `execSync("git rev-parse --short HEAD").toString().trim()` in a try/catch falling back to `"unknown"` (node:child_process is built-in). `report = buildReport({ cellRows, opts, commit })`. Output: `--json` → `console.log(JSON.stringify(report, null, 2))` only; otherwise `console.log(formatText(report))`. `--out PATH` → `mkdirSync(dirname, { recursive: true })`, `writeFileSync(PATH + ".tmp", JSON)`, `renameSync(PATH + ".tmp", PATH)` — written ONCE after every worker returned. Always print `elapsed: <seconds>s  workers=<n>  runs=<cells*seeds>` to STDERR (never into the JSON or the text report). Exit 0.

    Worker thread (`!isMainThread`): read `{ runOpts, seeds }` from `workerData`; `parentPort.on("message", (cell) => parentPort.postMessage({ cell, rows: playCell(cell, seeds, runOpts) }))`. Each worker imports the engine itself through tuning-bot.mjs — no state crosses threads except plain-object messages.

    Do not print any verdict wording. Do not add `useItem`, gear grants, or any engine call other than through playRun.
  </action>
  <verify>
    <automated>node tools/tune-classes.mjs --sub Summoner --race Troll --seeds 2 --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);console.log(r.cells.length,r.cells[0].cls,r.cells[0].sub,r.cells[0].race,r.meta.seeds,r.meta.maxActions,r.meta.excluded.length,typeof r.meta.bot)})"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `1 Magic User Summoner Troll 2 5000 1 string`.
    - `node tools/tune-classes.mjs --sub samurai --race fridgian --seeds 1; echo "rc=$?"` prints a message matching /Fridg/i and `rc=2`.
    - `node tools/tune-classes.mjs --seeds 0; echo "rc=$?"` prints usage text and `rc=2`; `node tools/tune-classes.mjs --sub Paladin --seeds 1; echo "rc=$?"` prints a message listing valid subs and `rc=2`.
    - `node tools/tune-classes.mjs --sub Summoner --race Troll --seeds 2 2>/dev/null | tail -1 | grep -E '^Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5\(caster\)  potion<0.5  camp<0.5  seeds=2  workers=[0-9]+  startDepth=1$'` prints one line (text mode ends with the extended Bot line).
    - `node tools/tune-classes.mjs --sub Summoner --race Troll --seeds 2 2>/dev/null | grep -c 'Fridgian'` is >= 1 (the excluded-cell footnote prints even for a single-cell run).
    - Scheduling independence (HARN-03 ordering): `node tools/tune-classes.mjs --race Troll --seeds 2 --workers 1 --json 2>/dev/null > "$TMP/a.json"` and the same with `--workers 4` into `"$TMP/b.json"`, then `node -e "const a=require(process.argv[1]),b=require(process.argv[2]);const pick=r=>JSON.stringify({cells:r.cells,rollups:r.rollups});console.log(pick(a)===pick(b), a.meta.workers, b.meta.workers, a.cells.length)" "$TMP/a.json" "$TMP/b.json"` prints `true 1 4 24`.
    - Atomic write: `node tools/tune-classes.mjs --sub Summoner --race Troll --seeds 1 --out "$TMP/x/out.json" 2>/dev/null >/dev/null; ls "$TMP/x"` lists exactly `out.json` (no leftover `.tmp`), and `node -e "const r=require(process.argv[1]);console.log(Object.keys(r).join(','), r.cells[0].topCauses.length<=3, 'elapsed' in r.meta)" "$TMP/x/out.json"` prints `meta,cells,rollups true false`.
    - `node tools/tune-classes.mjs --start-depth 20 --sub Knight --race Troll --seeds 2 --json 2>/dev/null | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);const c=r.cells[0];console.log(r.meta.startDepth, c.meanFloorsGained===null||c.meanFloorsGained>=0, c.meanDepth===null||c.meanDepth>=20)})"` prints `20 true true`.
    - `grep -c "worker_threads" tools/tune-classes.mjs` >= 1; `grep -c "renameSync" tools/tune-classes.mjs` >= 1; `grep -c "isMainThread" tools/tune-classes.mjs` >= 2.
    - `git diff --quiet -- package.json package-lock.json` exits 0 (no dependency added).
    - `grep -c "tune-classes\|tools/lib" tools/build-www.mjs` prints `0` (the harness is not in the shipped copy list).
  </acceptance_criteria>
  <done>tune-classes.mjs runs any subset of the 143 cells in parallel, prints the ranked text matrix or JSON, writes --out atomically, refuses Fridgian Samurai and bad inputs before newRun, and produces byte-identical cells/rollups regardless of worker count.</done>
</task>

<task type="auto">
  <name>Task 3: Add --start-depth and the stuck bucket to tools/tune-difficulty.mjs</name>
  <files>tools/tune-difficulty.mjs</files>
  <read_first>
    - tools/tune-difficulty.mjs (whole file — parseArgs, printReport's Death-depth/Outcome lines, the --json object)
    - tools/lib/tuning-bot.mjs (post-22-02: playRun result fields `stuck`/`outcome`/`startDepth`; `sharedJson` already reports `stuck`/`completed`; `printSharedReadout` already prints the Stuck line and the extended Bot line)
    - docs/DIFFICULTY-RETUNE.md lines 114-195 (the v1.1 transcript shape — keep every existing line's wording; only the Outcome line changes and the depth stats switch to completed runs)
  </read_first>
  <action>
    In `tools/tune-difficulty.mjs`:
    1. `parseArgs`: add `--start-depth=N` → `opts.startDepth = n` when `Number.isFinite(n) && n >= 1` (default comes from BOT_DEFAULTS.startDepth = 1). Add the flag to the header's Run examples (`--seeds=50 --start-depth=20`). Cite HARN-04 and 22-CONTEXT.md "Start-at-depth for the bot": reuses newRun's dev option through playRun; no extra gear.
    2. Stuck bucket: in `printReport` compute `completed = results.filter((r) => !r.stuck)` and `stuckCount = results.length - completed.length`; the "Death-depth distribution" uses `completed`; `causeBreakdown(completed)` (so "maxActionsHit" no longer appears as a death cause); keep the "Action-count distribution" over ALL runs (it is not a depth stat). Replace the final Outcome line with `Outcome: ${dead} dead, ${won} won, ${stuckCount} stuck (hit maxActions=${opts.maxActions}; excluded from depth stats)`. Add `startDepth` to the report header line: `tune-difficulty: N seeded auto-play run(s), start depth D`.
    3. `--json`: add `completed` and `stuck` counts, compute `deathDepth` and `causes` over `completed`, keep everything else; `sharedJson` already contributes `bot.startDepth`.
    4. `autoPlayOnce` needs no change (it spreads `run` minus `state`, so the new `stuck`/`outcome`/`startDepth`/`floorsGained`/`encountersSurvived` fields flow through). Do NOT change the seed stride, MAX default, or any D-05/D-06 parameter.
  </action>
  <verify>
    <automated>node tools/tune-difficulty.mjs --seeds=4 --start-depth=20 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);console.log(r.runs, r.bot.startDepth, typeof r.stuck, typeof r.completed, r.completed===0||r.deathDepth.min>=20)})"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `4 20 number number true`.
    - `node tools/tune-difficulty.mjs --seeds=4 | grep -E '^Outcome: [0-9]+ dead, [0-9]+ won, [0-9]+ stuck \(hit maxActions=20000; excluded from depth stats\)$'` prints one line.
    - `node tools/tune-difficulty.mjs --seeds=4 | grep -E '^tune-difficulty: 4 seeded auto-play run\(s\), start depth 1$'` prints one line.
    - `node tools/tune-difficulty.mjs --seeds=4 --start-depth=20 | grep -E '^Bot: exploreBudget=50  maxActions=20000  party=off  flee=0.3/0.5\(caster\)  potion<0.5  camp<0.5  seeds=4  startDepth=20$'` prints one line.
    - `node tools/tune-difficulty.mjs --seeds=3 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);console.log(r.causes.some(c=>c.cause==='maxActionsHit'))})"` prints `false` (stuck runs are never listed as a death cause).
    - `grep -c "start-depth" tools/tune-difficulty.mjs` >= 2 and `grep -c "stuck" tools/tune-difficulty.mjs` >= 4.
    - `npm test 2>&1 | grep -E '^# fail 0'` matches.
  </acceptance_criteria>
  <done>tune-difficulty accepts --start-depth, reports stuck runs as their own bucket, computes depth stats and death causes over completed runs, and prints the extended Bot line; nothing else in its readout wording changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → tune-classes/tune-difficulty | developer-supplied flags; numeric ranges and names validated before any engine call |
| main thread ↔ workers | plain-object postMessage payloads only (cell, seeds, opts, rows); no shared memory, no engine state crosses |
| tool → filesystem (--out) | writes one JSON file the developer named; temp-then-rename |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-09 | Tampering | CLI name/number parsing | medium | mitigate | resolveForce canonicalizes names or exits 2; integer range checks; Fridgian Samurai refused before newRun |
| T-22-10 | Denial of Service | runaway matrix run | medium | mitigate | 5,000-action default cap; per-cell queue; --workers bounded by cells; elapsed reported |
| T-22-11 | Tampering | partial/corrupt snapshot file | high | mitigate | --out written once after all workers return via write-to-temp-then-rename; no timing fields so diffs are clean |
| T-22-12 | Repudiation | unreproducible matrix | high | mitigate | fixed seed list i*7919+1; scheduling-independent aggregation; meta.commit + meta.bot recorded; --workers 1 vs 4 equality asserted |
| T-22-13 | Information Disclosure | --out path traversal | low | accept | dev-only CLI run by the developer on their own machine; canon path-safety is /gsd-secure-phase territory, not minted here |
| T-22-SC | Tampering | npm/pip/cargo installs | high | accept | No installs — worker_threads/os/fs/child_process are Node built-ins; package.json byte-identical |
</threat_model>

<verification>
- `node --test test/unit/class-matrix.test.js` green (>= 11 tests); `npm test` green.
- `node tools/tune-classes.mjs --race Troll --seeds 2 --json` produces byte-identical `cells`/`rollups` under `--workers 1` and `--workers 4`.
- `node tools/tune-classes.mjs --sub samurai --race fridgian` exits 2 with the canon message; `--seeds 0` exits 2 with usage.
- `node tools/tune-difficulty.mjs --seeds=4 --start-depth=20 --json` reports `bot.startDepth 20`, `stuck`/`completed` counts.
- `git diff --quiet -- package.json` exit 0; `grep -c "tune-classes\|tools/lib" tools/build-www.mjs` is 0.
</verification>

<success_criteria>
- A developer can run every sub-class x race combination (143 cells) for N paired seeds in parallel and read a ranked text matrix or machine-diffable JSON, reproducible from the same seed list regardless of worker count.
- `--cls/--sub/--race` force any cell (case-insensitive), `--start-depth` measures deep-floor lethality on both tools, stuck runs are reported honestly and separately.
</success_criteria>

<output>
Create `.planning/phases/22-class-aware-harness-before-matrix/22-03-SUMMARY.md` when done. Record the measured wall time of the `--race Troll --seeds 2` acceptance runs (workers 1 vs 4) so Plan 22-04 can size its polling window, and the final text-table column layout.
</output>