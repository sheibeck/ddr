---
phase: 25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/toasts.js
  - src/browser/eventNarration.js
  - mazeworld.html
  - test/unit/narrativeToasts.test.js
  - test/unit/shell-toast-wiring.test.js
  - test/unit/shell-oracle-panel.test.js
autonomous: true
requirements: [DFB-01, DFB-02, DFB-03]
tags: [toasts, beats-card, oracle, shell, presentation, device-feedback, dfb-01, dfb-02, dfb-03]

must_haves:
  truths:
    - "DFB-01 adjacency: card vs toast is decided by the exported CARD_EVENTS set (exactly floorChanged and leveled), checked by the shell BEFORE the generic html-to-beats fallback; the existing preDeath / awaitingFight / pendingJoiner / pendingFind branches run first and are byte-identical. A move whose events contain BOTH a card event and toast-only events shows the card AND raises the toasts (toasts are raised inside dispatchWithToasts before the card is built; the card never swallows toasts). Unit test: CARD_EVENTS is disjoint from ORACLE_ONLY, and no toast-only move event (trapSprung, climbedOver, chestOpened, goldGained, teleported, darknessFell, afflictionCured, rested) is in CARD_EVENTS."
    - "DFB-01 empty: a dispatch with no events and no html produces neither a card nor a toast; narrativeToastText('') returns ''; a narration line that strips to whitespace-only yields '' and the pipeline falls back to the builder's table text (never a blank toast); mzToast returns early on empty text. Tested."
    - "DFB-01 encoding: narrativeToastText strips every HTML tag, drops the roll-detail spans (the <span class=\"roll\">...</span> blocks and their trailing whitespace) BEFORE stripping the remaining tags, decodes &amp; &lt; &gt; &quot; &#39; &nbsp; and numeric &#NN; entities to their characters, collapses whitespace, and trims — its output never contains a '<tag>' or an '&xxx;' entity sequence; the shell inserts toast text with textContent, never innerHTML. Tests cover a line with entities, a nested <b>, and a dice span."
    - "DFB-01 ordering: on the narrative actions (move, camp, resolveJoiner — the exported NARRATIVE_ACTIONS set) the direct-mapped toasts of one dispatch keep engine/Oracle order within a priority tier; the Phase 25 priority sort and the MAX_TOASTS cap-4 eviction (lowest priority evicted first) are unchanged. Test: three same-priority toast-only events -> three toasts in event order, each carrying its Oracle sentence."
    - "DFB-01 single decision: the narrative-vs-table choice lives in ONE place — toastsForAction step 7 (direct-mapped events) reads ctx.narrate; when present and the event type is not in CARD_EVENTS, the toast text is narrativeToastText(ctx.narrate(e)) with the table text as fallback; aggregated toasts (encounter start, enemy/your rounds, spell/flee/parley/chest chains) keep their Phase 25 wording. The shell passes ctx.narrate ONLY for action types in NARRATIVE_ACTIONS, so combat/store/inventory actions toast exactly as before (toastsForAction with a bare ctx is byte-identical to Phase 25 — every existing toastsForAction test still passes)."
    - "DFB-02 boundary: toastLifetime(len, visible) = min(TOAST_CAP_MS, TOAST_BASE_MS + TOAST_PER_CHAR_MS * len) + TOAST_STACK_BONUS_MS * clamp(visible, 0, MAX_TOASTS - 1) with TOAST_BASE_MS 3000, TOAST_PER_CHAR_MS 60, TOAST_CAP_MS 9000, TOAST_STACK_BONUS_MS 1200; len 0 -> 3000, len 1 -> 3060, len 99 -> 8940, len 100 -> 9000, len 101 -> 9000; visible 3 -> +3600 (max 12600), visible 4 clamps to 3, visible -1 clamps to 0. Constants and helper exported from toasts.js; unit tests at len 0/1/99/100/101 and visible 0/3/4/-1. Tap-to-dismiss clears the timer and removes the toast; a timer that fires after a tap is a no-op (no double-removal error)."
    - "DFB-02 precision: all lifetime math is integer milliseconds (Number.isInteger asserted on the helper output for integer inputs); reduced-motion only shortens the CSS transition (the prefers-reduced-motion rule survives byte-identical), never the lifetime; the host stays position:fixed with cap 4 (no layout shift)."
    - "DFB-03: #screen-oracle is a flex column filling its screen height, #log-panel gets flex:1 / min-height:0 and #log scrolls inside the panel (overflow-y:auto, max-height:none); opening the Oracle tab sets #log.scrollTop = 0 (newest line is at the top because logLine prepends); a scroll listener on #log shows the '↑ newer' pill when scrollTop > 24 px and hides it at or below 24 px; tapping the pill scrolls to 0 and hides itself; an empty log shows no pill and throws nothing. Source-assertion tests check the CSS rules, the scrollTop reset in the open handler, and the pill element + handler."
    - "Camp results are toast-only too: the mzMakeCamp handler builds a beats card only under the same CARD_EVENTS gate as engineMove (never, in practice), so rested/wentHungry/afflictionCured/armorPatched/potionDuplicated/wanderingMonster arrive as narrative toasts; the 'camp' action is in NARRATIVE_ACTIONS."
    - "tableFour and tableFourNoop leave ORACLE_ONLY and gain TOAST_FOR builders (tone beat, priority other, text = e.result with a voiced fallback) — their old ORACLE_ONLY justification ('the over-map overlay already narrates this') no longer holds once the card is gone; the Phase 25 coverage guards (exact partition, no dead entries, ORACLE_ONLY size band) stay green."
  artifacts:
    - path: "src/browser/toasts.js"
      provides: "CARD_EVENTS, NARRATIVE_ACTIONS, TOAST_BASE_MS, TOAST_PER_CHAR_MS, TOAST_CAP_MS, TOAST_STACK_BONUS_MS, toastLifetime(len, visible), narrativeToastText(html); ctx.narrate hook in toastsForAction step 7; tableFour/tableFourNoop builders"
      exports: ["CARD_EVENTS", "NARRATIVE_ACTIONS", "TOAST_BASE_MS", "TOAST_PER_CHAR_MS", "TOAST_CAP_MS", "TOAST_STACK_BONUS_MS", "toastLifetime", "narrativeToastText"]
    - path: "src/browser/eventNarration.js"
      provides: "narrateEvent(e) — one event's Oracle HTML line via EVENT_NARRATION ('' for unknown/moved); re-exports CARD_EVENTS and NARRATIVE_ACTIONS beside the existing toast surface"
      exports: ["narrateEvent"]
    - path: "mazeworld.html"
      provides: "CARD_EVENTS gate in engineMove and mzMakeCamp; dispatchWithToasts passes ctx.narrate for NARRATIVE_ACTIONS; mzToast lifetime via window.__mzToastLifetime + stack bonus + tap-to-dismiss; .mw-toast pointer-events:auto; Oracle panel fill CSS, #log-newer pill, initOracleScroll, scroll-to-newest on tab open"
    - path: "test/unit/narrativeToasts.test.js"
      provides: "unit tests for CARD_EVENTS, NARRATIVE_ACTIONS, narrativeToastText, toastLifetime, the ctx.narrate hook, ordering, empty, and the tableFour builders"
      min_lines: 120
    - path: "test/unit/shell-oracle-panel.test.js"
      provides: "source assertions for the Oracle fill CSS, the scrollTop reset on open, the newer pill element and its scroll/click handlers"
      min_lines: 40
  key_links:
    - from: "mazeworld.html (module script)"
      to: "src/browser/toasts.js"
      via: "a second import line directly under the existing toastsForAction import: import { CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime } from \"./src/browser/toasts.js\"; plus window.__mzToastLifetime = toastLifetime bridged for the classic script's mzToast"
      pattern: "import \\{ CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime \\} from \"\\./src/browser/toasts\\.js\";"
    - from: "mazeworld.html (module script)"
      to: "src/browser/eventNarration.js"
      via: "import { narrateEvent } from \"./src/browser/eventNarration.js\"; passed as ctx.narrate by dispatchWithToasts for NARRATIVE_ACTIONS"
      pattern: "import \\{ narrateEvent \\} from \"\\./src/browser/eventNarration\\.js\";"
    - from: "mazeworld.html engineMove / mzMakeCamp"
      to: "CARD_EVENTS"
      via: "events.some((e) => CARD_EVENTS.has(e.type)) appended to the html-to-beats condition in both handlers"
      pattern: "events\\.some\\(\\(e\\) => CARD_EVENTS\\.has\\(e\\.type\\)\\)"
    - from: "mazeworld.html initTabs showTab"
      to: "#log"
      via: "if (name === \"oracle\") window.__mzOracleToNewest?.(); — the helper sets scrollTop = 0 and hides the pill"
      pattern: "__mzOracleToNewest"
  prohibitions:
    - "No engine change in this plan: engine/, content/, tools/ and test/parity/ are untouched (git status on those paths prints nothing)"
    - "The existing first-line module import `import { toastsForAction } from \"./src/browser/toasts.js\";` stays byte-identical (test/unit/shell-toast-wiring.test.js pins it) — new names come in on their own import lines directly below it"
    - "dispatchWithToasts(action) stays the single seam: still defined exactly once, still the only bare dispatch( call, still calls toastsForAction(action.type, ...)"
    - "The preDeath, awaitingFight/ambush, pendingJoiner and pendingFind branches of engineMove are not edited; only the final else-if condition gains the CARD_EVENTS clause"
    - "No Math.random / Date.now / DOM access in toasts.js or eventNarration.js (the purity guard in test/unit/toastsCoverage.test.js scans toasts.js); toasts.js never imports eventNarration.js (cycle guard)"
    - "The toast host keeps position:fixed;left:0;right:0; and the prefers-reduced-motion rule byte-identical; host cap stays 4"
---

<objective>
Tune the narrative delivery on device (DFB-01/02/03): the dismissible "Move on" card is reserved for decisions and big updates (the existing preDeath / Fight! / Joiner / find branches, plus a new floor and a level-up via an exported `CARD_EVENTS` whitelist); every other move-path and camp event becomes a toast whose text is the Oracle's own sentence with the dice stripped (`narrativeToastText`, wired through a `ctx.narrate` hook in `toastsForAction` — ONE decision point, tested); toasts linger (`toastLifetime`: 3000 + 60/char, cap 9000, +1200 per toast already visible, integer ms), can be tapped away, and keep the fixed no-CLS host at cap 4; the Oracle panel fills the screen, opens at the newest line, and shows a "↑ newer" pill when scrolled away.

Purpose: 25.1-CONTEXT.md decisions 1–3 and the two Oracle items (fill the screen; open at the most recent line). "Walking rhythm is the point: a trap should be a glance at a toast, not a tap on a card."
Output: `src/browser/toasts.js` (+ exports), `src/browser/eventNarration.js` (`narrateEvent`), `mazeworld.html` (shell + CSS), three test files, three commits, `npm test` green, `npm run build:www` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-CONTEXT.md
@.planning/phases/25-nothing-happens-silently-feature-feedback/25-04-SUMMARY.md
@src/browser/toasts.js
@src/browser/eventNarration.js
@test/unit/shell-toast-wiring.test.js
@test/unit/toastsForAction.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `src/browser/toasts.js`: `CARD_EVENTS`, `NARRATIVE_ACTIONS`, lifetime constants + `toastLifetime`, `narrativeToastText`, `ctx.narrate` hook, `tableFour`/`tableFourNoop` builders [25.1-01]; `joinerLeft` builder, `campFailed` numbers, `FEATURE_EVENTS` + `joinerLeft` [25.1-02]; `allyCast` in ORACLE_ONLY, `allySpellHit`/`allySpellMissed` builders, `allyStruck`/`allyMissed` class flags, `FEATURE_EVENTS` + the four ally events [25.1-03].
- `src/browser/eventNarration.js`: `narrateEvent` + re-exports [25.1-01]; `joinerLeft` (escaped names, deterministic snark line from `content/flavor.js#JOINER_EXIT_LINES`) and `campFailed` with numbers [25.1-02]; `allyCast`/`allySpellHit`/`allySpellMissed` and the enriched `allyStruck`/`allyMissed` [25.1-03].
- `mazeworld.html`: CARD_EVENTS gate, narrative ctx, mzToast lifetime/tap, Oracle fill + pill [25.1-01]; Joiner offer "X walks." head, camp button short-state via `window.__mzNightlyEats`, `window.__mzPartyCap` bridge [25.1-02].
- Engine: `engine/state.js#swapPartyMember`, `engine/encounters.js#resolveJoiner` swap, `engine/movement.js#nightlyEats` + `campFailed {need, have}` [25.1-02]; `engine/derived.js#memberToHit`/`bestAttackSpell`, `engine/combat.js#alliesTurn` class branches + `allyCast`, `engine/movement.js#newDay` member `spellsUsed` reset [25.1-03].
- Tests: `test/unit/narrativeToasts.test.js`, `test/unit/shell-oracle-panel.test.js`, shell-toast-wiring additions [25.1-01]; joiner-acquisition/movement/party-model/voice-scan/shell-party-camp additions [25.1-02]; party-combat additions, joiner-acquisition integration pin [25.1-03].
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: toasts.js — CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime + constants, narrativeToastText, the ctx.narrate hook, tableFour builders; eventNarration.js narrateEvent; unit tests</name>
  <files>src/browser/toasts.js, src/browser/eventNarration.js, test/unit/narrativeToasts.test.js</files>
  <read_first>
    - src/browser/toasts.js lines 1–60 (header + TONES/PRIORITY/MAX_TOASTS), 58–82 (ORACLE_ONLY — note the tableFour/tableFourNoop lines and their reasons), 200–235 (the toastsForAction pipeline header comment, especially the paragraph saying `ctx` is currently unread), 682–735 (dedupeByType + toastsForAction — step 7 is the `events.forEach` block that maps unconsumed events through TOAST_FOR), 737–780 (the first TOAST_FOR entries, to mirror builder style)
    - src/browser/eventNarration.js lines 1–64 (header, helpers), 138–146 (campFailed/teleported/floorChanged/leveled entries — the roll/banner span shapes), 550–556 (the trailing re-export line)
    - src/browser/engineAdapter.js lines 420–450 (formatEvents/formatEvent — html is NOT 1:1 with events because null lines are filtered; that is why narrateEvent(e) builds one event's line directly from EVENT_NARRATION)
    - mazeworld.html lines 5728–5744 (the shell's stripRollDetail: the exact roll-span regex `/<span class="roll">[\s\S]*?<\/span>\s*/g` to mirror) and 2654–2668 (the current mzToast, to understand `text.length` as the `len` argument)
    - test/unit/toastsCoverage.test.js in full (the partition/dead-entry/ORACLE_ONLY-size/purity guards this task must keep green — tableFour leaving ORACLE_ONLY must be matched by a TOAST_FOR builder) and test/unit/toastsForAction.test.js lines 1–60 (test style + the "only ORACLE_ONLY events yields []" fixture)
  </read_first>
  <action>
    In `src/browser/toasts.js` (keep the module pure — no DOM, no engine import, no Math.random/Date.now; every builder defends fields with `??`/`?.`):

    1. Beside `MAX_TOASTS`, add and document (Phase 25.1, DFB-01 decision 1 / DFB-02 decision 3, 25.1-CONTEXT.md):
       - `export const CARD_EVENTS = new Set(["floorChanged", "leveled"]);` — the ONLY move-path events that still earn the dismissible "Move on" card (the encounter Fight! gate, a Joiner offer, a find, death and the end-of-fight report are handled by their own shell branches, not by this set). Comment that the shell checks it BEFORE its generic html-to-beats fallback and that a dispatch carrying both a card event and toast-only events shows the card AND the toasts.
       - `export const NARRATIVE_ACTIONS = new Set(["move", "camp", "resolveJoiner"]);` — the action types whose direct-mapped toasts carry the Oracle sentence (the shell passes `ctx.narrate` only for these; combat/store/inventory keep the short Phase 25 table text).
       - `export const TOAST_BASE_MS = 3000; export const TOAST_PER_CHAR_MS = 60; export const TOAST_CAP_MS = 9000; export const TOAST_STACK_BONUS_MS = 1200;`
       - `export function toastLifetime(len, visible)` → `Math.min(TOAST_CAP_MS, TOAST_BASE_MS + TOAST_PER_CHAR_MS * n) + TOAST_STACK_BONUS_MS * v` where `n = Math.max(0, Math.floor(Number(len) || 0))` and `v = Math.max(0, Math.min(MAX_TOASTS - 1, Math.floor(Number(visible) || 0)))`. Integer in, integer out; no floats, no rounding beyond the floor on the inputs. Doc-comment the table: len 0 → 3000, len 100 → 9000 (cap reached), visible 3 → +3600, so the maximum is 12600 ms; reduced motion never touches this number (CSS only).
       - `export function narrativeToastText(html)` — returns plain text for a toast from one Oracle HTML line: (a) `String(html ?? "")`; (b) remove every `<span class="roll">…</span>` block plus its trailing whitespace with the SAME regex the shell's stripRollDetail uses (non-greedy, `[\s\S]*?`, global); (c) strip every remaining tag with `/<[^>]+>/g`; (d) decode entities: `&amp;` `&lt;` `&gt;` `&quot;` `&#39;` `&apos;` `&nbsp;` and numeric `&#NN;` / `&#xHH;` (decode `&amp;` LAST so `&amp;lt;` becomes `&lt;` text, not `<`); (e) collapse runs of whitespace (including the non-breaking space) to one space and trim; (f) return `""` when nothing is left. Doc-comment why tags are stripped BEFORE entities are decoded (a decoded `&lt;b&gt;` must stay literal text, never become a tag) and that the shell inserts the result via textContent.
    2. `toastsForAction(type, events, ctx = {})` — the ONE narrative-vs-table decision. In step 7 (the `events.forEach` that maps unconsumed, non-ORACLE_ONLY events through `TOAST_FOR`), after `const { text, tone, priority } = builder(e, ctx);` compute `const narrative = typeof ctx.narrate === "function" && !CARD_EVENTS.has(e.type) ? narrativeToastText(ctx.narrate(e)) : "";` and push `{ text: narrative || text, tone, priority, idx, type: e.type }`. Nothing else in the pipeline changes (aggregated toasts in steps 1–6 keep their Phase 25 wording; dedupe/sort/cap unchanged). Update the pipeline header comment: replace the paragraph that says `ctx` is reserved/unread with a description of `ctx.narrate` — `(e) => html string | ""` — supplied by the shell for NARRATIVE_ACTIONS; for those actions each direct-mapped toast-only event carries the Oracle sentence (dice stripped) instead of the terse label, with the table text as the fallback when the narration strips to nothing; CARD_EVENTS types keep their table toast because the card carries their sentence.
    3. Remove `"tableFour"` and `"tableFourNoop"` from `ORACLE_ONLY` (their reason — the over-map overlay narrates them — is no longer true once the card is gated) and add two builders in the encounters.js section of `TOAST_FOR`: `tableFour: (e) => ({ text: e?.result ?? "Something happens.", tone: "beat", priority: PRIORITY.other })` and `tableFourNoop: (e) => ({ text: e?.result ?? "Nothing much happens.", tone: "beat", priority: PRIORITY.other })`. (`e.result` is already a full prose sentence — see engine/encounters.js#tableFour.)

    In `src/browser/eventNarration.js`: add `export function narrateEvent(e)` — returns `""` for a null/undefined event, an event without a string `type`, or a type with no `EVENT_NARRATION` builder (so `moved` is `""`); otherwise `EVENT_NARRATION[e.type](e) || ""`. Doc-comment that it exists because `formatEvents` filters null lines (html is not 1:1 with events) and the toast layer needs ONE event's line. Extend the trailing re-export to `export { TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, CARD_EVENTS, NARRATIVE_ACTIONS, toastsForAction } from "./toasts.js";` (same objects, no copies — the cycle/identity guard in toastsCoverage keeps passing).

    Create `test/unit/narrativeToasts.test.js` (node:test + node:assert/strict; import from `../../src/browser/toasts.js` and `narrateEvent`/`EVENT_NARRATION` from `../../src/browser/eventNarration.js`). Tests, named as below:
    1. "CARD_EVENTS is exactly floorChanged + leveled, disjoint from ORACLE_ONLY, and contains no toast-only move event": deepEqual of `[...CARD_EVENTS].sort()` with `["floorChanged", "leveled"]`; for each of trapSprung, trapAvoided, climbedOver, fellInGorge, chestOpened, goldGained, foodFound, teleported, darknessFell, afflictionCured, rested, wentHungry, tableFour assert `!CARD_EVENTS.has(t)`; `leveled` is in TOAST_FOR (its card and its table toast coexist), `floorChanged` is in ORACLE_ONLY.
    2. "NARRATIVE_ACTIONS is exactly move + camp + resolveJoiner": deepEqual sorted; assert `attack`, `castSpell`, `buyItem`, `equipItem` are NOT members.
    3. "narrativeToastText: strips the roll span, nested tags, decodes entities, collapses whitespace": `narrativeToastText('<span class="hurt">Pit trap finds you first.</span> <span class="roll">5 hp.</span>')` === "Pit trap finds you first."; `narrativeToastText('<span class="hit"><b>Bram &amp; Co</b> say &quot;hi&quot; &lt;quietly&gt;&nbsp;now</span>')` === 'Bram & Co say "hi" <quietly> now'; `narrativeToastText('<span class="roll">7 vs 5.</span>')` === ""; `narrativeToastText("")` === "" and `narrativeToastText(undefined)` === ""; the output of the real `narrateEvent({ type: "trapSprung", name: "Pit trap", dmg: 5 })` through the helper contains no `<` followed by a letter and no `&[a-z#0-9]+;` sequence (regex asserts).
    4. "toastLifetime: the DFB-02 boundary table, integer output, clamped visible": len 0/1/99/100/101 with visible 0 → 3000/3060/8940/9000/9000; len 0 visible 3 → 6600; len 100 visible 3 → 12600; visible 4 → same as visible 3; visible -1 → same as visible 0; `Number.isInteger` on every result; the four exported constants equal 3000/60/9000/1200.
    5. "ctx.narrate: on a narrative action a direct-mapped toast carries the Oracle sentence; without it the table text is unchanged": `toastsForAction("move", [{ type: "trapSprung", name: "Pit trap", dmg: 5 }], { narrate: narrateEvent })[0].text` === "Pit trap finds you first." (tone hurt); the same call with `{}` gives the Phase 25 text "Pit trap (5)." — assert the two differ and the tone/priority are identical between them.
    6. "ctx.narrate never replaces a CARD_EVENTS toast or an aggregated toast": `toastsForAction("move", [{ type: "leveled", level: 2, wpGain: 5 }], { narrate: narrateEvent })[0].text` equals the `{}` result (table text); `toastsForAction("move", [{ type: "struckByFoe", name: "Rat", dmg: 3, roll: 2, need: 5 }, { type: "struckByFoe", name: "Rat", dmg: 4, roll: 1, need: 5 }], { narrate: narrateEvent })` yields the same single aggregated "Rat hits you 2 of 2 (7)"-style toast as with `{}` (deepEqual the two arrays).
    7. "ctx.narrate fallback: a narration that strips to nothing falls back to the table text, never a blank toast": call with `{ narrate: () => '<span class="roll">7 vs 5.</span>' }` on `[{ type: "goldGained", amount: 3 }]` → one toast whose text equals the `{}` text; with `{ narrate: () => "" }` likewise; no toast text is ever "" across both.
    8. "ordering: three same-priority toast-only events keep event order and each carries its own sentence": events climbedOver, foodFound {name:"Bread", wp: 3}, darknessFell (all PRIORITY.other) → 3 toasts whose texts equal `narrativeToastText(narrateEvent(e))` in that order.
    9. "adjacency: a move carrying floorChanged AND a trap yields the trap toast only (the card owns the floor line)": `toastsForAction("move", [{ type: "floorChanged", depth: 2 }, { type: "trapSprung", name: "Pit trap", dmg: 5 }], { narrate: narrateEvent })` → length 1, text "Pit trap finds you first."; and `CARD_EVENTS.has("floorChanged")` is true (documenting that the shell builds the card from the same dispatch).
    10. "empty: no events -> no toasts under either ctx": `toastsForAction("move", [], { narrate: narrateEvent })` deepEquals [].
    11. "tableFour / tableFourNoop toast their prose result and are no longer Oracle-only": `TOAST_FOR.tableFour({ type: "tableFour", result: "Something unseen takes its cut — 10 hp, gone." }).text` equals that sentence, tone beat; bare `{ type: "tableFourNoop" }` gives non-empty text; `ORACLE_ONLY.has("tableFour")` and `ORACLE_ONLY.has("tableFourNoop")` are both false.
    12. "narrateEvent: '' for moved/unknown/null, the EVENT_NARRATION line otherwise": `narrateEvent({ type: "moved" })` === ""; `narrateEvent({ type: "nonsenseType" })` === ""; `narrateEvent(null)` === ""; `narrateEvent({ type: "teleported" })` === `EVENT_NARRATION.teleported({ type: "teleported" })`.
    Run `node --test test/unit/narrativeToasts.test.js test/unit/toastsCoverage.test.js test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/voice/safety-scan.test.js` — all green. Commit: `feat(25.1-01): CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime, narrativeToastText, ctx.narrate hook, tableFour toasts` (message ends with the two attribution lines from the plan footer).
  </action>
  <verify>
    <automated>node --test test/unit/narrativeToasts.test.js test/unit/toastsCoverage.test.js test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/narrativeToasts.test.js` exits 0 with `# pass` >= 12 and `# fail 0`.
    - `node --test test/unit/toastsCoverage.test.js test/unit/toastsForAction.test.js test/unit/toastTable.test.js` exits 0 with `# fail 0` (the Phase 25 partition, dead-entry, ORACLE_ONLY-size, purity and cycle guards all still pass).
    - `node -e "import('./src/browser/toasts.js').then(m=>console.log([...m.CARD_EVENTS].sort().join(','), [...m.NARRATIVE_ACTIONS].sort().join(','), m.toastLifetime(0,0), m.toastLifetime(100,3), m.toastLifetime(101,9), m.TOAST_BASE_MS, m.TOAST_PER_CHAR_MS, m.TOAST_CAP_MS, m.TOAST_STACK_BONUS_MS, JSON.stringify(m.narrativeToastText('<b>a &amp; b</b> <span class=\"roll\">1 vs 2.</span>'))))"` prints `floorChanged,leveled camp,move,resolveJoiner 3000 12600 12600 3000 60 9000 1200 "a & b"`.
    - `node -e "import('./src/browser/eventNarration.js').then(m=>console.log(typeof m.narrateEvent, m.narrateEvent({type:'moved'})==='', m.CARD_EVENTS===undefined))"` prints `function true false`.
    - `grep -v '^\s*//' src/browser/toasts.js | grep -c "ctx.narrate"` prints a number >= 1, and `grep -v '^\s*//' src/browser/toasts.js | grep -c 'from "./eventNarration.js"'` prints `0`.
    - `git status --porcelain engine content tools test/parity mazeworld.html` prints nothing (this task touches only the two modules and the new test).
    - `git log -1 --format=%s` starts with `feat(25.1-01)`.
  </acceptance_criteria>
  <done>The toast module owns the card whitelist, the narrative-action set, the lifetime formula and the plain-text sentence helper; toastsForAction makes the narrative-vs-table choice in exactly one line; tableFour results can toast; every Phase 25 guard is still green.</done>
</task>

<task type="auto">
  <name>Task 2: mazeworld.html — CARD_EVENTS gate (move + camp), narrative ctx in dispatchWithToasts, mzToast lifetime/stack bonus/tap-to-dismiss, .mw-toast pointer-events; shell-toast-wiring additions</name>
  <files>mazeworld.html, test/unit/shell-toast-wiring.test.js</files>
  <read_first>
    - mazeworld.html lines 5389–5400 (the module script opening: the hoisted `import { toastsForAction } ...` line that MUST stay byte-identical, and the comment explaining why it is first), 5452–5470 (the window.__mzBags / __mzSellPrice bridge block — the bridge pattern to copy for window.__mzToastLifetime), 5700–5720 (FEATURE_EVENT_TITLE + beatsTitleFor), 5800–5870 (dispatchWithToasts and engineMove — the final `else if (!state.combat && ... && html.length)` block is the html-to-beats fallback to gate), 6403–6420 (mzMakeCamp — its own beats synthesis), 2648–2668 (window.mzToast in the classic script), 672–702 (the toast CSS: .mw-toast-host, .mw-toast, tones, keyframes, reduced-motion)
    - test/unit/shell-toast-wiring.test.js in full (stripComments order, the pinned import regex, the "exactly one bare dispatch(" and "host cap 4" assertions that must keep passing)
    - src/browser/toasts.js as written in Task 1 (toastLifetime signature, CARD_EVENTS, NARRATIVE_ACTIONS)
  </read_first>
  <action>
    Module script (`<script type="module">`, mazeworld.html ~L5389):
    1. Directly BELOW the existing, untouched line `import { toastsForAction } from "./src/browser/toasts.js";` add exactly two lines: `import { CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime } from "./src/browser/toasts.js";` and `import { narrateEvent } from "./src/browser/eventNarration.js";` (they sit inside the same hoisted-first position, so no comment-stripper span can swallow them). Do not edit the existing import line.
    2. In the bridge block beside `window.__mzBags = BAGS;` add `window.__mzToastLifetime = toastLifetime;` with a short comment (Phase 25.1 DFB-02: the classic script's mzToast reads the lifetime through this read-only bridge so the formula lives in toasts.js and is unit-tested there).
    3. `dispatchWithToasts(action)`: keep the function name, the single `dispatch(action)` call and the `toastsForAction(action.type, result.events, ctx)` shape; build `const ctx = NARRATIVE_ACTIONS.has(action.type) ? { narrate: narrateEvent } : {};` and pass it. Extend the doc comment: for NARRATIVE_ACTIONS (move/camp/resolveJoiner) each direct-mapped toast carries the Oracle sentence via narrateEvent (DFB-01 decision 2); every other action keeps Phase 25's short table text.
    4. `engineMove`: leave the preDeath / ambush / pendingJoiner / pendingFind logic byte-identical. In the FINAL `else if (...)` that synthesizes `state.beats` from `html`, append the clause `&& events.some((e) => CARD_EVENTS.has(e.type))` to the condition (after `html.length`). Update the comment block above FEATURE_EVENT_TITLE: the card is now gated on CARD_EVENTS (floorChanged, leveled); every other feature landing is a narrative toast raised by dispatchWithToasts; the title map is only consulted for card events. Add `leveled: ["Skill level up", "moss"],` to FEATURE_EVENT_TITLE (keep the other entries — harmless, and floorChanged is still used).
    5. `window.mzMakeCamp`: apply the SAME gate — the `if (!state.combat && !state.store && !state.dead && !state.won && html.length)` that builds the "Camp" card gains `&& events.some((e) => CARD_EVENTS.has(e.type))`; destructure `events` from the dispatch result (`const { state, events, html } = dispatchWithToasts({ type: "camp" });`). Comment: camp results are toast-only (DFB-01); the gate is kept identical to engineMove's so there is one rule, not two.

    Classic script `window.mzToast` (~L2654): keep the signature `(text, tone)`, the early return on empty text, the host lookup, `t.className`, `dataset.tone`, `textContent`, the append and the `while (host.children.length > 4)` trim line exactly. Changes: read `const visible = host.children.length;` BEFORE appending; replace the old lifetime line (the one with the 4200 cap and the 45-per-character rate — delete it entirely) with `const life = window.__mzToastLifetime ? window.__mzToastLifetime(text.length, visible) : 4200;`; then `let timer = null; const dismiss = () => { if (timer !== null) { clearTimeout(timer); timer = null; } t.classList.add("out"); setTimeout(() => t.remove(), 300); }; timer = setTimeout(dismiss, life); t.addEventListener("click", dismiss);`. Update the block comment above it: lifetime = toasts.js#toastLifetime via the bridge (3000 + 60/char, cap 9000, +1200 per toast already visible — DFB-02), tap/click dismisses early and clears the timer (a timer firing after a tap is a harmless no-op: classList.add is idempotent and Element.remove on a detached node is a no-op), reduced motion is CSS-only.

    CSS (~L672–702): in `.mw-toast{...}` change `pointer-events:none;` to `pointer-events:auto;cursor:pointer;touch-action:manipulation;` (the HOST keeps `pointer-events:none` so the empty host never blocks the map; a child with pointer-events:auto still receives taps). Do not touch `.mw-toast-host`, the tone rules, the keyframes, or the `prefers-reduced-motion` line.

    Append to `test/unit/shell-toast-wiring.test.js` (reuse its `HTML`/`CODE`/`stripComments` helpers; import `CARD_EVENTS`, `NARRATIVE_ACTIONS` from toasts.js) these tests:
    - "DFB-01: the module imports CARD_EVENTS/NARRATIVE_ACTIONS/toastLifetime and narrateEvent on their own lines, and the original toastsForAction import is untouched": `assert.match(CODE, /import \{ CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime \} from "\.\/src\/browser\/toasts\.js";/)`, `assert.match(CODE, /import \{ narrateEvent \} from "\.\/src\/browser\/eventNarration\.js";/)`, and the pre-existing `/import \{ toastsForAction \} from .../` regex still matches.
    - "DFB-01: dispatchWithToasts passes ctx.narrate only for NARRATIVE_ACTIONS": `assert.match(CODE, /NARRATIVE_ACTIONS\.has\(action\.type\) \? \{ narrate: narrateEvent \} : \{\}/)`.
    - "DFB-01: the html-to-beats fallback in engineMove AND mzMakeCamp is gated on CARD_EVENTS": `(CODE.match(/events\.some\(\(e\) => CARD_EVENTS\.has\(e\.type\)\)/g) || []).length === 2`; and the engineMove gate keeps its pending guards: `assert.match(CODE, /&& !state\.pendingJoiner && !state\.pendingFind && html\.length\s*&& events\.some\(\(e\) => CARD_EVENTS\.has\(e\.type\)\)/)`.
    - "DFB-01: the preDeath and ambush branches survive byte-identical": assert `CODE` contains `preDeath: true` exactly once and `state.beats.awaitingFight = true;` at least once.
    - "DFB-02: mzToast reads the lifetime through window.__mzToastLifetime, measures visible before appending, and dismisses on tap with a cleared timer": slice `CODE` from `window.mzToast = function` to `window.mzSpellCharges` (region scope); in that region assert `/const visible = host\.children\.length;/`, `/window\.__mzToastLifetime\(text\.length, visible\)/`, `/clearTimeout\(timer\)/`, `/t\.addEventListener\("click", dismiss\)/`, and that the region does NOT contain the 45-per-character rate (build the pattern as `"text.length * " + "45"` so the test never spells the dead formula); assert the module bridges it: `/window\.__mzToastLifetime = toastLifetime;/`.
    - "DFB-02: toasts are tappable while the host stays non-blocking": `.mw-toast{` rule (regex `/\.mw-toast\{([^}]*)\}/`) contains `pointer-events:auto` and `cursor:pointer`; the `.mw-toast-host{` rule still contains `pointer-events:none`.
    - "DFB-01: FEATURE_EVENT_TITLE carries a title for both CARD_EVENTS": for each `t` of CARD_EVENTS assert `new RegExp(t + ': \\[').test(CODE)`.
    Run `node --test test/unit/shell-toast-wiring.test.js test/unit/parley-button-mirror.test.js test/unit/foe-effect-chip.test.js test/unit/tutorial.test.js test/unit/save-validation.test.js` (every mazeworld.html source-assertion suite) — green. Commit: `feat(25.1-01): card only for floorChanged/leveled, narrative toasts on move/camp/joiner, toast lifetime + tap-to-dismiss` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/shell-toast-wiring.test.js test/unit/parley-button-mirror.test.js test/unit/foe-effect-chip.test.js test/unit/tutorial.test.js test/unit/save-validation.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-toast-wiring.test.js` exits 0 with `# pass` >= 21 (14 existing + 7 new) and `# fail 0`.
    - `grep -c 'import { toastsForAction } from "./src/browser/toasts.js";' mazeworld.html` prints `1`; `grep -c 'import { CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime } from "./src/browser/toasts.js";' mazeworld.html` prints `1`; `grep -c 'import { narrateEvent } from "./src/browser/eventNarration.js";' mazeworld.html` prints `1`.
    - `grep -c "events.some((e) => CARD_EVENTS.has(e.type))" mazeworld.html` prints `2`.
    - `grep -c "window.__mzToastLifetime = toastLifetime;" mazeworld.html` prints `1` and `grep -c "window.__mzToastLifetime(text.length, visible)" mazeworld.html` prints `1`.
    - `grep -c "text.length \* 45" mazeworld.html` prints `0` and `grep -c "clearTimeout(timer)" mazeworld.html` prints `1`.
    - `grep -c 'preDeath: true' mazeworld.html` prints `1` (the pre-death branch is untouched).
    - `grep -c "function dispatchWithToasts(action)" mazeworld.html` prints `1`.
    - `git status --porcelain engine content tools test/parity src` prints nothing (this task touches only mazeworld.html and the wiring test).
    - `git log -1 --format=%s` starts with `feat(25.1-01)`.
  </acceptance_criteria>
  <done>On device, a trap/climb/chest/coin/teleport/camp result is a lingering, tappable toast carrying the Oracle's sentence and no "Move on" card; a new floor or a level-up still raises the card; the Fight!/Joiner/find/death branches are untouched; every shell source-assertion suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Oracle panel fills the screen, opens at the newest line, "↑ newer" pill; shell-oracle-panel test; full suite + build</name>
  <files>mazeworld.html, test/unit/shell-oracle-panel.test.js</files>
  <read_first>
    - mazeworld.html lines 253 (`#screen-maze{display:flex;flex-direction:column;height:100%;min-height:0}` — the fill precedent to mirror), 614–626 (`.log{...max-height:min(44vh,440px);overflow-y:auto...}` and the .log p rules), 767–774 (why #app has a definite 100vh height), 880–881 (.mw-screens / .mw-screen), 1476–1481 (the Oracle screen markup: `<section class="panel" id="log-panel"><h2>The Oracle ...</h2><div class="log" id="log"></div></section>`), 1560–1600 (initTabs/showTab — where the Oracle-open hook goes), 2637–2645 (logLine PREPENDS: newest line is `logEl.firstChild`, so "most recent" is scrollTop 0)
    - test/unit/shell-toast-wiring.test.js lines 1–60 (the stripComments helper + HTML/CODE setup to copy into the new sibling test)
  </read_first>
  <action>
    CSS (add a small `/* ---------- Phase 25.1 (DFB-03): the Oracle fills the screen ---------- */` block right after the `.log .banner::before{content:""}` rule, ~L626):
    - `#screen-oracle{display:flex;flex-direction:column;height:100%;min-height:0}` (mirrors #screen-maze so the flex chain #app -> .mw-screens -> #screen-oracle has a definite height).
    - `#log-panel{flex:1;min-height:0;display:flex;flex-direction:column;position:relative}` (the panel grows to the available height; position:relative anchors the pill).
    - `#log-panel .log{flex:1;min-height:0;max-height:none;overflow-y:auto}` (the LIST scrolls inside the panel; the page never scrolls).
    - `.mw-log-newer{position:absolute;left:50%;bottom:14px;transform:translateX(-50%);min-height:44px;padding:0 18px;border-radius:22px;font-family:var(--mono);font-size:11px;letter-spacing:.1em;text-transform:uppercase;background:var(--paper-3);color:var(--ditto);border:1px solid var(--ditto);box-shadow:0 6px 18px rgba(0,0,0,.5);z-index:2}` and `.mw-log-newer[hidden]{display:none}`.
    Keep `#log-panel{order:4}` in the max-width media query and the fixed toast host / bottom tab bar untouched.

    Markup (~L1479): after `<div class="log" id="log"></div>` inside `#log-panel` add `<button type="button" class="mw-log-newer" id="log-newer" hidden>↑ newer</button>` (with an HTML comment: DFB-03 — shown only while the reader has scrolled away from the newest line; newest is at the TOP because logLine prepends).

    Classic script — add an IIFE `initOracleScroll` immediately BEFORE the existing `(function initTabs() {` (so `window.__mzOracleToNewest` exists before showTab's first call):
    `(function initOracleScroll() { const log = document.getElementById("log"); const pill = document.getElementById("log-newer"); if (!log || !pill) return; const NEWER_THRESHOLD_PX = 24; const sync = () => { pill.hidden = log.scrollTop <= NEWER_THRESHOLD_PX; }; log.addEventListener("scroll", sync, { passive: true }); pill.addEventListener("click", () => { log.scrollTop = 0; sync(); }); window.__mzOracleToNewest = () => { log.scrollTop = 0; sync(); }; sync(); })();`
    with a doc comment (DFB-03: newest line is at the top; the pill appears past a 24 px threshold and hides at the top; tapping it returns to the newest line; an empty log has scrollTop 0 so the pill stays hidden and nothing throws).
    In `initTabs`'s `showTab(name)`, after the tab/aria loop and before the `dead` re-fetch block, add `if (name === "oracle") window.__mzOracleToNewest?.();` with a one-line comment (DFB-03: every open of the Oracle lands on the most recent line).

    Create `test/unit/shell-oracle-panel.test.js` (copy the `HTML`/`CODE`/`stripComments` setup from shell-toast-wiring.test.js — line comments first, then block comments). Tests:
    1. "DFB-03: #screen-oracle fills its screen as a flex column": `/#screen-oracle\{display:flex;flex-direction:column;height:100%;min-height:0\}/`.
    2. "DFB-03: #log-panel grows (flex:1 / min-height:0) and anchors the pill (position:relative)": regex on the `#log-panel{` rule containing `flex:1`, `min-height:0`, `position:relative`.
    3. "DFB-03: the log list scrolls inside the panel": `#log-panel .log{` rule contains `overflow-y:auto`, `max-height:none`, `min-height:0`.
    4. "DFB-03: the newer pill exists, starts hidden, and has a hidden rule": `/<button type="button" class="mw-log-newer" id="log-newer" hidden>/` in HTML and `/\.mw-log-newer\[hidden\]\{display:none\}/`.
    5. "DFB-03: opening the Oracle tab scrolls to the newest line": `/if \(name === "oracle"\) window\.__mzOracleToNewest\?\.\(\);/` in CODE and `/window\.__mzOracleToNewest = \(\) => \{ log\.scrollTop = 0; sync\(\); \};/`.
    6. "DFB-03: the pill toggles on scroll past 24 px and taps back to the top": in CODE assert `/const NEWER_THRESHOLD_PX = 24;/`, `/log\.addEventListener\("scroll", sync, \{ passive: true \}\)/`, `/pill\.addEventListener\("click"/`, `/pill\.hidden = log\.scrollTop <= NEWER_THRESHOLD_PX;/`.
    7. "DFB-03: logLine still prepends (the newest line is the top)": `/logEl\.insertBefore\(p, logEl\.firstChild\);/` in CODE (documents why scrollTop 0 is "newest").
    8. "the fixed toast host and the tab bar are untouched by the Oracle layout": `.mw-toast-host{` still has `position:fixed` and `.mw-tabbar{` still has `position:fixed;left:0;right:0;bottom:0`.
    Then run the full gate: `npm test 2>&1 | tail -8` (`# fail 0`, `# pass` >= 1330 + 12 + 7 + 8 = 1357), `node --test "test/parity/**/*.test.js" 2>&1 | tail -4` (33 files green, `git status --porcelain test/parity` empty), `npm run build:www` (exit 0). Commit: `feat(25.1-01): Oracle panel fills the screen, opens at the newest line, newer pill` (with the attribution lines).
  </action>
  <verify>
    <automated>node --test test/unit/shell-oracle-panel.test.js test/unit/shell-toast-wiring.test.js && npm test 2>&1 | tail -8 && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-oracle-panel.test.js` exits 0 with `# pass` >= 8 and `# fail 0`.
    - `grep -c '#screen-oracle{display:flex;flex-direction:column;height:100%;min-height:0}' mazeworld.html` prints `1`; `grep -c 'id="log-newer" hidden' mazeworld.html` prints `1`; `grep -c 'if (name === "oracle") window.__mzOracleToNewest?.();' mazeworld.html` prints `1`; `grep -c "const NEWER_THRESHOLD_PX = 24;" mazeworld.html` prints `1`.
    - `npm test 2>&1 | tail -8` shows `# fail 0` and a `# pass` count >= 1357 (1330 measured at phase start + 12 narrativeToasts + 7 wiring + 8 oracle).
    - `node --test "test/parity/**/*.test.js" 2>&1 | grep -E "^# (pass|fail)"` shows `# fail 0`, and `git status --porcelain test/parity` prints nothing; `git diff --quiet HEAD -- test/parity/prototype-master.js.txt; echo rc=$?` prints `rc=0`.
    - `npm run build:www` exits 0.
    - `git status --porcelain engine content tools` prints nothing after the commit (no engine change anywhere in this plan).
    - `git log -1 --format=%s` starts with `feat(25.1-01)`.
  </acceptance_criteria>
  <done>The Oracle tab fills the phone screen, always opens at the newest line, and offers a "↑ newer" pill when the reader scrolls into the past; the whole suite, the parity suite and the www build are green with no engine file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events -> toast text | narration HTML becomes toast TEXT; must never reach the DOM as HTML |
| classic script <-> module script | window.__mzToastLifetime / window.__mzOracleToNewest bridges (read-only helpers, no state) |
| toast host over the map | toasts become tappable (pointer-events:auto) and sit above the map |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25.1-01 | Tampering | narrativeToastText output inserted as HTML | medium | mitigate | tags stripped before entities decoded; the shell only ever uses textContent (existing mzToast line kept); test asserts no tag/entity sequence survives |
| T-25.1-02 | Denial of Service | a tapped toast's timer double-removing / throwing | low | mitigate | dismiss clears the timer; classList.add idempotent; Element.remove on a detached node is a no-op; source test pins clearTimeout |
| T-25.1-03 | Tampering | card gate regression (card swallowed or re-appearing for toast-only events) | high | mitigate | CARD_EVENTS is the single exported set; two-site gate asserted by count; adjacency unit test proves the trap toast still fires beside floorChanged |
| T-25.1-04 | Information Disclosure | dice detail leaking into toasts | low | mitigate | the roll span is removed first; test on a roll-only line yields "" and falls back to the table text |
| T-25.1-05 | Denial of Service | tappable toasts intercepting map taps | low | accept | user-requested tap-to-dismiss; the host itself stays pointer-events:none, only live toasts catch taps, and they self-remove |
| T-25.1-06 | Tampering | engine drift from a presentation phase | high | mitigate | prohibitions + git status gates on engine/content/tools/test/parity; parity 33/33 run in Task 3 |
| T-25.1-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero new deps) |
</threat_model>

<verification>
- `node --test test/unit/narrativeToasts.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-oracle-panel.test.js` green; `npm test` ends `# fail 0` with `# pass` >= 1357.
- `node --test "test/parity/**/*.test.js"` 33 files green, zero fixture changes, `test/parity/prototype-master.js.txt` untouched.
- `npm run build:www` exits 0; `git status --porcelain engine content tools` empty.
- Device acceptance (non-blocking, for the orchestrator's phone-push offer): walk onto a trap → toast with the Oracle sentence, no card; teleport square → toast, map recenters, no card; descend → card; level up → card; four toasts in a row readable one by one; tapping a toast removes it; Oracle tab fills the screen, opens at the newest line, shows "↑ newer" when scrolled down.
</verification>

<success_criteria>
- DFB-01: the card appears only for floorChanged/leveled (plus the untouched Fight!/Joiner/find/death/report branches); every other move/camp event is a narrative toast; the decision is one exported set and one pipeline line, both tested.
- DFB-02: lifetime = toastLifetime (3000 + 60/char, cap 9000, +1200 per visible toast, integer ms), tap-to-dismiss with a cleared timer, reduced-motion CSS-only, host fixed at cap 4.
- DFB-03: Oracle fills the screen, opens at the newest line, "↑ newer" pill toggles at 24 px and taps back to the top.
</success_criteria>

<output>
Create `.planning/phases/25.1-device-feedback-batch-card-vs-toast-joiner-swap-class-based-/25.1-01-SUMMARY.md` when done. Record: the exported names added to toasts.js/eventNarration.js, the exact engineMove/mzMakeCamp gate condition as committed, the mzToast lifetime line as committed, the test counts, and the `npm test` pass count.

Commit message footer for every commit in this plan:
Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS
</output>
