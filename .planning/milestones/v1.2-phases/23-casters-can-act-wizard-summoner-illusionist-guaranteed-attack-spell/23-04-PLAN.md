---
phase: 23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell
plan: 04
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
  - 23-03
files_modified:
  - engine/magic.js
  - test/unit/freeze-pays-out.test.js
  - test/parity/fixtures/action-script.magic.json
  - test/parity/harness/comparables.js
  - test/parity/magic-parity.test.js
  - test/parity/full-suite.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/CLASS-PASS.md
autonomous: true
requirements:
  - IDENT-01
  - IDENT-02
  - IDENT-03
  - IDENT-04
  - FID-06
tags: [engine, magic, freeze, killFoe, parity, fixture-divergence, fid-06, ledger, sanity-readout]

must_haves:
  truths:
    - "If the change turns out to alter ZERO fixture seeds, no fixture file is touched and the summary records 'no regeneration needed' explicitly (never a blanket regeneration); if a magic/full-suite fixture casts Freeze or a level-1 Summon, that fixture is regenerated narrowly with the documented rationale."
    - "Fixture regeneration uses the project's existing regeneration script/method (see test/parity/fixtures/README.md and FIXTURE-INVENTORY.md) so the JSON serialization (key order, number formatting) is byte-compatible with the harness comparators; the before/after diff for each regenerated seed is limited to the grimoire array (and any downstream fields that derive from it) and is quoted in the plan summary."
    - "rollCharacter's rng-consumption order is unchanged for every class: the attack top-up adds no draws (uses the existing shuffled list), the override table is a pure read, and Freeze->killFoe draws only inside an already-successful cast; a test compares rng.getState() after rollCharacter for the 14 chargen seeds before vs after the change and asserts equality."
    - "A successful Freeze routes the kill through killFoe: frozenSolid is emitted first, then foeKilled with spGained via killSpFor, loot (gainWilmst / treasure roll), c.kills incremented, party split applied; t.frozen stays true on a real kill; a kill-twice foe (lives 2) is revived by killFoe's lives rule instead (canon), and is then not flagged frozen."
    - "The magic fixture's cast-damage scenario (seed 8, Illusionist casts Freeze at a Shriek, roll 1 <= 6 hits) is the ONLY parity scenario that casts Freeze; its measured divergence is exactly c.sp 0->5, c.gold 50->51, c.kills 0->1, c.rations 4->5 and is declared on the scenario with before/after/rationale; every other magic scenario and every other fixture stays byte-identical with no strip."
  artifacts:
    - path: "engine/magic.js"
      provides: "thrown branch: Freeze -> frozenSolid then killFoe (DELIBERATE RULES CHANGE, user decision); spellAboveLevel diagnostic via spellLevelFor"
      contains: "Freeze kills awarded nothing in the prototype"
    - path: "test/unit/freeze-pays-out.test.js"
      provides: "Freeze kill awards sp/kills/coin; event order frozenSolid before foeKilled; kill-twice foe revives; miss awards nothing; non-Freeze thrown unchanged"
      contains: "frozenSolid"
    - path: "test/parity/fixtures/action-script.magic.json"
      provides: "cast-damage scenario `divergence` record: fields sp/gold/kills/rations, measured before/after, rationale"
      contains: "\"divergence\""
    - path: "test/parity/harness/comparables.js"
      provides: "stripScenarioDivergence(state, divergence) — scenario-scoped, field-scoped strip over state.c using stripDeclaredFields"
      contains: "export function stripScenarioDivergence"
    - path: "test/parity/magic-parity.test.js"
      provides: "scenario-scoped strip + before/after assertions for declared scenarios; cast-damage asserts foeKilled now fires"
      contains: "stripScenarioDivergence"
    - path: "test/parity/full-suite.test.js"
      provides: "magic sub-test applies the same scenario-scoped strip"
      contains: "stripScenarioDivergence"
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "'Phase 23 caster divergences' section: chargen seeds 15/24 and magic cast-damage before/after tables, draw accounting, byte-identical-elsewhere paragraph"
      contains: "Phase 23"
    - path: "docs/CLASS-PASS.md"
      provides: "'Phase 23 note' paragraph under Outliers / Findings with the three smoke means vs BEFORE (2.48 / 2.18 / 2.20), explicitly not the AFTER matrix"
      contains: "Phase 23 note"
  key_links:
    - from: "engine/magic.js thrown branch (Freeze)"
      to: "engine/combat.js#killFoe"
      via: "killFoe(state, t, rng, events) called right after the frozenSolid push"
      pattern: "frozenSolid"
    - from: "test/parity/magic-parity.test.js"
      to: "test/parity/fixtures/action-script.magic.json#scenarios[cast-damage].divergence"
      via: "scenario.divergence selects stripScenarioDivergence and drives the before/after assertions"
      pattern: "scenario\\.divergence"
    - from: "docs/CLASS-PASS.md Phase 23 note"
      to: "tools/tune-classes.mjs"
      via: "the three quoted commands (--sub Wizard/Summoner/Illusionist --seeds 20) and their bySub meanDepth"
      pattern: "tune-classes.mjs --sub"
  prohibitions:
    - "test/parity/prototype-master.js.txt is NEVER edited."
    - "Never blanket-regenerate: only the cast-damage scenario of the magic fixture gains a `divergence` record; `seed`, `actions`, `spellIndex`, and the other three scenarios are byte-identical; no other fixture file is touched by this plan."
    - "No new serialized engine-state field; no `*Comparable()` FIELD carve-out — only the scenario-scoped strip; `stripScenarioDivergence` is applied only where `scenario.divergence` is declared."
    - "The Freeze change adds rng draws ONLY inside an already-successful Freeze hit (killFoe's d6/d10/d20 and the Beasts cooking d6) — no draw on a miss, no draw outside the thrown branch; test/unit/chargen-rng-pin.test.js and every combat/economy/encounters/movement fixture stay untouched."
    - "Petrify and the other alive=false-without-killFoe kinds (turn/gate) are NOT changed — the user asked for Freeze only; note them in the SUMMARY as a v1.3 spell-audit candidate."
    - "docs/CLASS-PASS.md's AFTER section (Phase 26) and Rulings section (Phase 24) are NOT written; only a short 'Phase 23 note' under Outliers / Findings is added; the Rulings entry for Freeze is DRAFTED in the SUMMARY, not in the doc."
    - "No tools/ bot-policy edits; the sanity readout is a smoke signal quoted in the SUMMARY and the note, never a tuning input."
    - "content/spells.js is not touched."
---

<objective>
Land the user-added rule "Freeze pays out": in `engine/magic.js`'s thrown branch a successful Freeze now routes the kill through `combat.js#killFoe` (experience via `killSpFor`, coin, treasure roll, kill count, party split, cooking, checkLevel) after emitting `frozenSolid`, instead of only flagging the foe dead — the prototype awarded nothing for a Freeze kill, which the user ruled a bug, not a rule. Also make `castSpell`'s `spellAboveLevel` diagnostic read the effective level through `spellLevelFor` (one definition; `need` is correct for override cells).

Then honor FID-06 for the ONE parity scenario that casts Freeze: `action-script.magic.json` `cast-damage` (seed 8, Illusionist, Freeze at a Shriek, roll 1 hits). The planner's simulation on the pre-change engine + the patched thrown branch measured the divergence exactly: `c.sp 0 -> 5`, `c.gold 50 -> 51`, `c.kills 0 -> 1`, `c.rations 4 -> 5` (Beasts cooking d6 >= 4 fired, no Cooking skill), items unchanged (treasure d20 did not fire), combat null on both sides. Declare it on the scenario (before/after/rationale), add the scenario-scoped strip at both magic replay sites modelled on `stripParleyDivergence`, and record the phase's full FID-06 ledger (chargen seeds 15/24 from Plan 02 + this scenario) in `FIXTURE-INVENTORY.md`. Close with the sanity readout (three `tune-classes` runs, 20 seeds each, quoted against BEFORE 2.48 / 2.18 / 2.20 as a smoke signal only) and a short "Phase 23 note" in the class-pass ledger.

Purpose: a caster's kills count like everyone else's (Freeze is the most common level-1 kill spell — the guaranteed attack from Plan 02 is often Freeze, so without this the new day-one attack would pay nothing); the phase's deliberate divergences are declared, measured and machine-checked, never swept under a blanket regeneration.
Output: `engine/magic.js` edit, `test/unit/freeze-pays-out.test.js`, magic fixture `divergence` record, `stripScenarioDivergence`, both magic sites, `FIXTURE-INVENTORY.md` section, `docs/CLASS-PASS.md` note; `npm test` fully green; parity 30/30.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-02-SUMMARY.md
@.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-03-SUMMARY.md

@engine/magic.js
@engine/combat.js
@engine/derived.js
@test/unit/magic.test.js
@test/parity/magic-parity.test.js
@test/parity/full-suite.test.js
@test/parity/harness/comparables.js
@test/parity/fixtures/action-script.magic.json
@test/parity/FIXTURE-INVENTORY.md
@docs/CLASS-PASS.md
@tools/tune-classes.mjs
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `test/unit/chargen-rng-pin.test.js` — 20-seed rng cursors + per-sub rollGrimoire draw pins (pre-change). [23-01]
- `content/spell-level-overrides.js` — `SPELL_LEVEL_OVERRIDES`; barrel export. [23-01]
- `engine/derived.js` — `spellLevelFor`, `ATTACK_SPELL_KINDS`, `isAttackSpell`, `castableAttackSpells`; `canCast` via `spellLevelFor`. [23-01]
- `test/unit/spell-level-overrides.test.js`. [23-01]
- `engine/character.js#rollGrimoire` — zero-draw attack top-up, Summoner exempt; override-aware `usableNow`. [23-02]
- `test/unit/guaranteed-attack-spell.test.js`. [23-02]
- `test/parity/fixtures/action-script.chargen.json` — `divergences` block (seeds 15, 24). [23-02]
- `test/parity/harness/comparables.js` — `chargenDivergenceFor`, `stripDeclaredFields` [23-02]; `stripScenarioDivergence` [23-04].
- `test/parity/chargen-parity.test.js`, `test/parity/full-suite.test.js` (chargen sub-test). [23-02]
- `test/parity/fixtures/action-script.schema.md` — `divergences` / `divergence`. [23-02]
- `engine/combat.js#playerStrike` — Wizard rule; `strikeRefused.spell`. [23-03]
- `src/browser/eventNarration.js` — `strikeRefused` names the spell. [23-03]
- `content/flavor.js` — Wizard/Summoner/Illusionist `SUB_NOTE`. [23-03]
- `test/unit/casters-can-act.test.js`; `test/unit/combat.test.js` update. [23-03]
- `engine/magic.js` — Freeze via `killFoe`; `spellAboveLevel` via `spellLevelFor`. [23-04]
- `test/unit/freeze-pays-out.test.js`. [23-04]
- `test/parity/fixtures/action-script.magic.json` — `cast-damage` `divergence` record. [23-04]
- `test/parity/magic-parity.test.js`, `test/parity/full-suite.test.js` (magic sub-test) — scenario-scoped strip + assertions. [23-04]
- `test/parity/FIXTURE-INVENTORY.md` — "Phase 23 caster divergences" section. [23-04]
- `docs/CLASS-PASS.md` — "Phase 23 note" under Outliers / Findings. [23-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Freeze routes the kill through killFoe; spellAboveLevel reads the effective level; unit tests</name>
  <files>engine/magic.js, test/unit/freeze-pays-out.test.js</files>
  <read_first>
    - engine/magic.js lines 340-385 (thrown branch: `freeze` d10 vs 6, `damageFoe`, `spellHit`, the Freeze special case that sets alive/frozen/wp and `continue`s) and lines 56-70 (refusal diagnostics) and line 22 (derived.js import to extend with `spellLevelFor`)
    - engine/combat.js lines 469-520 (`killFoe`: lives rule -> foeRevived; alive=false, wp=0, kills++, d6 sp via killSpFor, party split, d10 coin via gainWilmst, d20 treasure check `<= 2 + f.lvl`, Beasts cooking d6 >= 4 without Cooking skill, checkLevel)
    - engine/foeDamage.js line 90-97 (damageFoe's only draw is the armor d20 — spell damage bypasses armor, so no draw for a spell hit)
    - test/unit/magic.test.js lines 41-100 (fixedWizard/fixedState/fixedFloor helpers, SPELL_IDX) and lines 155-175 (a thrown-spell test with a documented rng sequence)
    - test/unit/combat.test.js lines 101-112 (fixedFoe with `lives`, fixedCombat)
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md "Freeze pays out (user addition, 2026-09-14)"
    - The Phase-23 orchestrator's exact comment text: `// DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, user decision): Freeze kills awarded nothing in the prototype — a bug, not a rule.`
  </read_first>
  <action>
    1. engine/magic.js thrown branch — replace the Freeze special case so that on a hit it: sets `t.frozen = true`; pushes `{ type: "frozenSolid", target: t.name }`; calls `killFoe(state, t, rng, events)` (already imported); then, if `t.alive` is still true (killFoe's kill-twice `lives` rule revived it — Philly/Skeleton), sets `t.frozen = false` (a standing foe is not frozen); then `continue`. Drop the hand-rolled `t.alive = false` / `t.wp = 0` assignments — killFoe owns the death (it sets both) and the revive. Comment block above it, verbatim first line: `// DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, user decision): Freeze kills awarded nothing in the prototype — a bug, not a rule.` followed by: what the prototype did (marked the foe dead and frozen with wp 0, skipped killFoe — no experience, coin, treasure, kill count or party split; Ice at level 3 has the same "then frozen" flavor but was never special-cased), what changes (same frozenSolid event and t.frozen flag for narration; the kill now pays like any other), the determinism note (the extra draws — killFoe's d6/d10/d20 and the Beasts cooking d6 — happen ONLY after a successful Freeze hit; a miss draws exactly as before; the only parity scenario that casts Freeze is action-script.magic.json cast-damage, seed 8, declared under FID-06 in this plan), and the kill-twice note (a lives-2 creature now shrugs off a Freeze once, per canon "you have to kill it twice" — the prototype let Freeze bypass lives).
    2. engine/magic.js refusal diagnostics — import `spellLevelFor` from ./derived.js; in the `else if (sp.lvl > c.level)` diagnostic use `spellLevelFor(c.sub, sp)` for both the condition and the `need` field (one definition of effective level; a one-line Phase 23 IDENT-03/IDENT-04 comment). Behavior for every non-override cell is identical.
    3. Create `test/unit/freeze-pays-out.test.js` (helpers copied from magic.test.js; a Beasts-type `fixedFoe` with wp 3, maxWP 3, lvl 1, intel 1, `lives: 1`; Wizard L1 with grimoire ["Freeze"]):
       a. Freeze hit pays out: rng sequence d10 roll 1 (hit: 1 - bonus 3 <= 6), d6 dmg 3, then killFoe's d6 sp 4, d10 coin 5, d20 treasure 20 (skips), cooking d6 1 (skips) — assert events contain, in this order, `spellThrown`, `spellHit`, `frozenSolid`, `foeKilled` (with `spGained > 0`), `goldGained`; `state.c.kills === 1`; `state.c.sp > 0`; `state.c.gold > 50`; `foe.alive === false`, `foe.frozen === true`, `foe.wp === 0`; `encounterCleared` present.
       b. Event order: `events.findIndex(frozenSolid) < events.findIndex(foeKilled)`.
       c. Freeze miss pays nothing and draws nothing extra: d10 roll 10 (miss) with `fakeRng([10])` only — no `frozenSolid`, no `foeKilled`, `kills === 0`, `sp === 0`; `spellMissed` present.
       d. Kill-twice foe: same as (a) but `lives: 2` and `maxWP: 5` — events contain `frozenSolid` then `foeRevived` and NO `foeKilled`; `foe.alive === true`, `foe.wp === 5`, `foe.frozen === false`, `kills === 0`; rng sequence is just d10 + d6 (killFoe returns before its own draws).
       e. Party split still applies through the shared routine: `state.combat.allies = [{ wp: 5 }]` (one live member) — `foeKilled.spGained` equals `Math.round(gained / 2)` where `gained = killSpFor(c, foe, 4)` (import killSpFor from derived.js to compute the expectation).
       f. Non-Freeze thrown unchanged: Wizard L3 with ["Fireball"] on a wp-1 foe still reaches killFoe via the `t.wp <= 0` path exactly as before (one `foeKilled`, no `frozenSolid`).
       g. `spellAboveLevel` need via spellLevelFor: Wizard L1 with ["Phantom Host"] -> `need: 3`; Illusionist L1 with ["Phantom Host"] -> no refusal event, `spellsUsed === 1` (confirming the diagnostic is unreachable for the override cell).
    4. Run `node --test test/unit/freeze-pays-out.test.js test/unit/magic.test.js test/unit/formatEventsCoverage.test.js`. Expect `node --test test/parity/magic-parity.test.js` to fail on cast-damage at `c.sp` (or the first of the four fields) until Task 2 lands — do not commit until Task 2 is green (commit Tasks 1+2 together).
  </action>
  <verify>
    <automated>node --test test/unit/freeze-pays-out.test.js test/unit/magic.test.js test/unit/formatEventsCoverage.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `# fail 0` and the freeze file contributes >= 7 passing tests.
    - `grep -c "DELIBERATE RULES CHANGE (Phase 23, 2026-09-14, user decision): Freeze kills awarded nothing in the prototype — a bug, not a rule." engine/magic.js` prints `1`.
    - `awk '/if \(freeze\) \{/{f=1} f&&/killFoe\(state, t, rng, events\)/{print "ok"; exit}' engine/magic.js` prints `ok` (killFoe is called inside the Freeze block).
    - `grep -c 'type: "frozenSolid"' engine/magic.js` prints `1` (event kept, single push site).
    - `grep -c "spellLevelFor(c.sub, sp)" engine/magic.js` prints >= 2 (condition + need).
    - `grep -c "foeRevived" test/unit/freeze-pays-out.test.js` prints >= 1 and `grep -c "killSpFor" test/unit/freeze-pays-out.test.js` prints >= 1.
    - `node --test test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` (no chargen draw touched).
  </acceptance_criteria>
  <done>A Freeze kill pays experience, coin, treasure roll, kill count and party split exactly like a melee kill, frozenSolid still narrates it, kill-twice creatures follow canon, misses are unchanged, and the diagnostics read the effective level.</done>
</task>

<task type="auto">
  <name>Task 2: FID-06 — declare the cast-damage divergence on the magic fixture; scenario-scoped strip at both magic sites; inventory ledger</name>
  <files>test/parity/fixtures/action-script.magic.json, test/parity/harness/comparables.js, test/parity/magic-parity.test.js, test/parity/full-suite.test.js, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/magic-parity.test.js (whole file: local `comparable()` at lines 55-88 which strips name/darkFor/flight/bag but NOT sp/gold/kills/rations; the scenario loop lines 100-160 and the cast-damage event assertion)
    - test/parity/full-suite.test.js lines 140-165 (the magic sub-test uses the shared `combatComparable`, which also keeps sp/gold/kills/rations)
    - test/parity/harness/comparables.js: `stripParleyDivergence` (lines 324-364, the precedent and its JSDoc depth), Plan 02's `stripDeclaredFields`/`chargenDivergenceFor`, and `combatComparable` (223-245)
    - test/parity/fixtures/action-script.magic.json (scenario objects: name/seed/actions; `_note`, `spellIndex`) and action-script.schema.md's `divergence` paragraph from Plan 02
    - test/parity/FIXTURE-INVENTORY.md: the "Phase 20 parley divergence" section (the before/after table + "Why the draw count changed" + "Byte-identical elsewhere" structure to mirror) and the generated roster block markers (do NOT edit inside the markers)
    - test/parity/fixture-inventory.test.js (pins the roster; must stay green — no bestiary creature moves)
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-02-SUMMARY.md (the chargen before/after books to transcribe into the inventory section)
  </read_first>
  <action>
    1. MEASURE first (scratch script, not committed): replay the cast-damage scenario exactly as magic-parity.test.js does (`loadPrototypeSandbox({seed: 8})` + `ctx.startCombat(false, "Beasts")` + `ctx.castSpell(4)` on the prototype; `newRun(8)` + `applyStartCombat` + `applyAction castSpell idx 4` on the engine) and print both sides' `c.sp, c.gold, c.kills, c.rations, c.items.length, c.wp, c.level` and `combat === null`, plus the engine event types. Planner's expectation: prototype `0 50 0 4 0 31 1 true`, engine `5 51 1 5 0 31 1 true`, engine events `... spellThrown, spellHit, frozenSolid, foeKilled, goldGained, cooked, encounterCleared, combatEnded`. The MEASURED values are what you declare; if `items`/`wp`/`level` also differ, add them to `fields` and explain in the SUMMARY.
    2. Fixture: on the `cast-damage` scenario object in `test/parity/fixtures/action-script.magic.json` add `"divergence": { "phase": "23", "requirements": ["FID-06"], "fields": ["sp", "gold", "kills", "rations"], "before": { "sp": 0, "gold": 50, "kills": 0, "rations": 4 }, "after": { "sp": 5, "gold": 51, "kills": 1, "rations": 5 }, "rationale": "<one paragraph>" }` (measured values). Rationale: Freeze pays out (user decision 2026-09-14) — the prototype's thrown branch marked a frozen foe dead without calling killFoe, so a Freeze kill awarded no experience, coin, treasure roll, kill count or ration; the engine now routes the kill through killFoe after frozenSolid; seed 8's Illusionist freezes the Shriek on a d10 roll of 1, so this is the one parity scenario that pays out where the prototype did not; killFoe's four draws (d6 sp, d10 coin, d20 treasure, Beasts cooking d6) happen after the last compared action, so no earlier draw position moves. Keep `seed`, `actions`, `spellIndex`, `_note` (you may append one sentence to `_note` pointing at `divergence`) and the other three scenarios byte-identical; keep 2-space JSON formatting.
    3. comparables.js: export `stripScenarioDivergence(state, divergence)` — returns `state` unchanged when `divergence` is falsy, else a shallow copy whose `c` is `stripDeclaredFields(state.c, divergence.fields)`. JSDoc in the stripParleyDivergence style: FID-06 (Phase 23, "Freeze pays out"), scenario-scoped via the fixture's own declaration, applied at BOTH magic replay sites, every undeclared scenario compares bare.
    4. Both magic sites: in `test/parity/magic-parity.test.js` choose `cmp = scenario.divergence ? (s) => stripScenarioDivergence(comparable(s), scenario.divergence) : comparable` and use `cmp` for the initial and per-action diffState; after the action loop, for a declared scenario assert for every field in `divergence.fields`: `ctx.S.c[field] === divergence.before[field]` and `engineState.c[field] === divergence.after[field]` (so the record is machine-checked on both sides). Tighten the cast-damage event assertion: it must now include `frozenSolid` AND `foeKilled`. Mirror the `cmp` selection and the before/after assertions in `test/parity/full-suite.test.js`'s magic sub-test (using `combatComparable`). Add a test in magic-parity.test.js: "divergence records are narrow and well-formed" — at most one scenario carries `divergence`, its `fields` are non-empty, and `before[f] !== after[f]` for every declared field.
    5. FIXTURE-INVENTORY.md: append a `## Phase 23 caster divergences (IDENT-01..04 / FID-06)` section AFTER the Phase 20 section (outside the generated markers) containing: (a) a chargen table — seeds 15 and 24 (from 23-02-SUMMARY.md): class/sub/race, BEFORE grimoire, AFTER grimoire, cause (seed 24: IDENT-02 attack top-up appended Freeze; seed 15: Summon counts as usable-now via the override, top-up stopped one spell earlier), and the statement that `newRun(seed).rngState` is unchanged for all 20 fixture seeds (pinned by test/unit/chargen-rng-pin.test.js); (b) the magic cast-damage table — field / BEFORE / AFTER for sp, gold, kills, rations plus the event list before vs after and the draw accounting (prototype: d10 + d6 = 2 draws in castSpell; engine: 2 + killFoe's 4 = 6, all after the frozenSolid); (c) a "Byte-identical elsewhere" paragraph: every other chargen seed, the heal/potion/scroll magic scenarios, every combat/movement/economy/encounters fixture, the prototype master, the roster table (regenerate with `node tools/fixture-inventory.mjs` and confirm the generated block is unchanged), and the FID-02 draw-count pins (test/unit/foe-turn-draw-count.test.js measures playerStrike loops, never castSpell). Mark measured numbers `[VERIFIED: engine run]` like the Phase 20 section does.
    6. Run `node --test test/parity` (30/30 + the new well-formedness tests) and `npm test` — fully green. Commit Tasks 1+2: `feat(23-04): Freeze pays out via killFoe + FID-06 magic cast-damage divergence record + inventory ledger`.
  </action>
  <verify>
    <automated>node --test test/parity 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/parity 2>&1 | grep -E "^# fail"` prints `# fail 0`.
    - `node -e "const f=require('./test/parity/fixtures/action-script.magic.json');const s=f.scenarios.find(x=>x.name==='cast-damage');console.log(f.scenarios.filter(x=>x.divergence).length, s.divergence.fields.join(','), s.divergence.before.sp, s.divergence.after.sp, s.divergence.after.kills, s.divergence.phase, s.seed, f.scenarios.length)"` prints `1 sp,gold,kills,rations 0 5 1 23 8 4` (or the measured equivalent recorded in the SUMMARY).
    - `grep -c "export function stripScenarioDivergence" test/parity/harness/comparables.js` prints `1`; `grep -c "stripScenarioDivergence" test/parity/magic-parity.test.js test/parity/full-suite.test.js` prints >= 1 for EACH file.
    - `grep -c "foeKilled" test/parity/magic-parity.test.js` prints >= 2 (the tightened cast-damage assertion).
    - `grep -c "## Phase 23 caster divergences" test/parity/FIXTURE-INVENTORY.md` prints `1`; `grep -c "VERIFIED: engine run" test/parity/FIXTURE-INVENTORY.md` prints >= 3 (Phase 20's existing marks plus this section's).
    - `node tools/fixture-inventory.mjs > "$SCRATCH/roster.txt" && node -e "const fs=require('fs');const doc=fs.readFileSync('test/parity/FIXTURE-INVENTORY.md','utf8');const gen=doc.split('<!-- fixture-inventory:generated:begin -->')[1].split('<!-- fixture-inventory:generated:end -->')[0].trim();const out=fs.readFileSync(process.env.SCRATCH+'/roster.txt','utf8').trim();console.log(gen===out)"` prints `true` (generated roster block unchanged), where `$SCRATCH` is the session scratchpad.
    - `git diff --stat HEAD~1 -- test/parity/prototype-master.js.txt test/parity/fixtures/action-script.combat.json test/parity/fixtures/action-script.movement.json test/parity/fixtures/action-script.economy.json test/parity/fixtures/action-script.encounters.json | wc -l` prints `0`.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# fail 0` with a pass count >= 1028 (Plan 02's >= 1004 + Plan 03's >= 16 + this plan's >= 8).
  </acceptance_criteria>
  <done>The single Freeze-casting parity scenario is declared with measured before/after values and rationale, both replay sites assert and strip only those fields for only that scenario, the inventory ledger records the phase's complete FID-06 story, and the whole suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Sanity readout (smoke signal, not AFTER) and the class-pass ledger note</name>
  <files>docs/CLASS-PASS.md</files>
  <read_first>
    - docs/CLASS-PASS.md lines 539-600 ("Outliers / Findings (Phase 22)" — the IDENT-01 finding paragraph this note answers; the Rulings and AFTER placeholders that must stay untouched) and lines 86-95 (the Bot: parameter line — the smoke runs use the default flags, so the Bot: line will differ in `seeds=`; say so)
    - tools/tune-classes.mjs header lines 15-25 (flags: `--sub`, `--seeds`, `--out`, `--json`) and tools/lib/class-matrix.mjs lines 28-40 (JSON shape: `rollups.bySub[].meanDepth`, `cells[]`)
    - .planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-CONTEXT.md "Specific Ideas" (sanity check only; real AFTER waits for Phase 26; keep the bads visible; draft the Rulings entry for Freeze in the SUMMARY)
  </read_first>
  <action>
    1. Preflight: `git status --porcelain` is empty (Tasks 1-2 committed) and `npm test` is green. Record `git rev-parse --short HEAD`.
    2. Run, one at a time in the foreground with a generous timeout (each is 6 cells x 20 seeds = 120 runs, expected well under a few minutes): `node tools/tune-classes.mjs --sub Wizard --seeds 20 --out "$SCRATCH/p23-wizard.json"`, then the same for `Summoner` and `Illusionist` (`$SCRATCH` = session scratchpad). From each JSON read `rollups.bySub[0].meanDepth` (and `stuck` total) with a one-line `node -e`. Keep the three text transcripts.
    3. In `docs/CLASS-PASS.md`, at the END of the "Outliers / Findings (Phase 22)" section (immediately before `## Rulings (Phase 24 — PLAY-03)`), add a paragraph headed `**Phase 23 note (2026-09-14, smoke readout — NOT the AFTER matrix).**` stating: the three IDENT-01..04 fixes landed at commit <short hash> (Wizard fights when no attack spell is castable and the refusal names the spell; every non-Summoner Magic User rolls a day-one attack spell; Summon is level 1 for Summoners; Phantom Host is level 1 for Illusionists; Freeze kills now pay out); the three quoted commands; a three-row mini table `Sub | BEFORE mean depth (143x40 matrix) | Phase 23 smoke (6 races x 20 seeds) | stuck` with BEFORE values 2.48 (Wizard), 2.18 (Summoner), 2.20 (Illusionist) and the measured smoke means; and an explicit caveat that a 120-run smoke at default flags is a directional signal only (different `seeds=` from the pinned Bot: line, different sample) — the paired AFTER matrix is Phase 26 (PLAY-02) and the Rulings entries are Phase 24 (PLAY-03). Do NOT touch the Rulings or AFTER sections or any BEFORE table.
    4. Commit: `docs(23-04): Phase 23 smoke readout note in the class-pass ledger`. In the SUMMARY, DRAFT (do not write into the doc) the Rulings-section entry for "Freeze pays out" (what changed, why, the parity scenario it moved, the experience-curve implication for casters) so Phase 24 can paste it.
  </action>
  <verify>
    <automated>grep -c "Phase 23 note" docs/CLASS-PASS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 23 note" docs/CLASS-PASS.md` prints `1`.
    - `grep -c "tune-classes.mjs --sub Wizard --seeds 20" docs/CLASS-PASS.md` prints >= 1 and the same for `--sub Summoner --seeds 20` and `--sub Illusionist --seeds 20`.
    - `awk '/^## Rulings \(Phase 24/{f=1} /^## AFTER/{f=0} f' docs/CLASS-PASS.md | grep -c "Phase 23 note"` prints `0` and `awk '/^## AFTER/{f=1} f' docs/CLASS-PASS.md | grep -c "Phase 23"` prints `0` (Rulings/AFTER untouched).
    - `git diff HEAD~1 -- docs/CLASS-PASS.md | grep -E '^-' | grep -vE '^---' | wc -l` prints `0` (the note is purely additive — no existing line removed).
    - The three `$SCRATCH/p23-*.json` files exist and `node -e "for(const s of ['wizard','summoner','illusionist']){const r=require(process.env.SCRATCH+'/p23-'+s+'.json');console.log(s, r.rollups.bySub[0].meanDepth, r.cells.length)}"` prints three lines each ending in `6`.
    - The SUMMARY quotes the three means next to 2.48 / 2.18 / 2.20 and contains a section headed "Draft Rulings entry (for Phase 24): Freeze pays out".
  </acceptance_criteria>
  <done>The ledger carries a clearly-labelled smoke readout answering Phase 22's carried-forward IDENT-01 finding, the AFTER/Rulings placeholders are untouched, and the SUMMARY drafts the Freeze ruling for Phase 24.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | killFoe is the existing, fixture-pinned death routine; Freeze now calls it. No new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-07 | Tampering | engine/magic.js Freeze -> killFoe (rng draw position) | medium | mitigate | Draws happen only after a successful hit; the single affected parity scenario is declared and machine-checked (before/after on both sides); chargen pins stay green |
| T-23-08 | Repudiation | test/parity/fixtures/action-script.magic.json#divergence | medium | mitigate | Record carries phase/requirements/fields/before/after/rationale; the well-formedness test rejects records with no real difference; FIXTURE-INVENTORY.md documents it with [VERIFIED: engine run] marks |
| T-23-09 | Information Disclosure | docs/CLASS-PASS.md smoke note | low | accept | Dev-only ledger, aggregates only, never shipped |
| T-23-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase |
</threat_model>

<verification>
- `node --test test/unit/freeze-pays-out.test.js` green (>= 7 tests).
- `node --test test/parity` 30/30 green; exactly ONE scenario (`cast-damage`) carries a `divergence` record; the prototype master and the other five fixture files are untouched by this plan.
- `node tools/fixture-inventory.mjs` output equals the generated block in FIXTURE-INVENTORY.md (roster unchanged).
- `npm test` green (>= 1028 pass, 0 fail).
- `docs/CLASS-PASS.md` gained exactly one additive "Phase 23 note" paragraph under Outliers / Findings.
</verification>

<success_criteria>
- A Freeze kill awards sp/coin/treasure roll/kill count/party split via killFoe; frozenSolid still narrates; kill-twice creatures follow canon.
- FID-06 is complete for the phase: the chargen seeds 15/24 (Plan 02) and the magic cast-damage scenario are the ONLY declared divergences, each measured, machine-checked and documented in FIXTURE-INVENTORY.md; everything else is byte-identical against the frozen prototype.
- The sanity readout is recorded as a smoke signal only; Phase 26 owns AFTER, Phase 24 owns Rulings.
</success_criteria>

<output>
Create `.planning/phases/23-casters-can-act-wizard-summoner-illusionist-guaranteed-attack-spell/23-04-SUMMARY.md` when done. MUST quote: the measured cast-damage before/after (all fields), the engine event list, the three smoke means vs BEFORE, the draft Rulings entry for Freeze, and the note that Petrify/Turn/Gate still bypass killFoe (v1.3 spell-audit candidate, not changed here).
</output>
