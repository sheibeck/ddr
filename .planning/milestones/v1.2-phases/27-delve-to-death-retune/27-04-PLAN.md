---
phase: 27-delve-to-death-retune
plan: 04
type: execute
wave: 4
depends_on: ["27-03"]
files_modified:
  - docs/DIFFICULTY-RETUNE.md
autonomous: false
requirements: [TUNE-07]
tags: [dr-round, human-checkpoint, debug-build, adb-install, pixel-7, ledger, verdict-left-blank, no-engine-change]

must_haves:
  truths:
    - "TUNE-07 (boundary): the DR checklist covers exactly FOUR runs — Run 1 forced 20, Run 2 forced 35, Run 3 forced 50 (dev start-at-depth toggle), Run 4 natural from floor 1 — each with per-run 'what should be true' lines derived from the band and the retune AFTER numbers, a fill-in table, and free notes; the verdict field has exactly three allowed values (tuned / tune-again / deferred) and is written by the USER or quoted verbatim by the orchestrator — never by the executor; the checklist states that 'tune-again' allows ONE more bounded iteration inside this phase (27-03's protocol + a ledger addendum + a rebuild + a second round of this checklist), that 'deferred' is recorded with the user's reason and closes the milestone on it, and that the phase cannot be marked complete with the verdict blank."
    - "TUNE-07 (precision): the build handed to the user is identified by the commit hash it was built from, the APK path, byte size and file timestamp, the stamped version (`android/version.properties` versionName/versionCode) and the device's post-install `dumpsys package` versionName/lastUpdateTime line; the dev start-at-depth values are stated exactly (20 / 35 / 50); the checklist quotes the band numbers and the retune AFTER numbers so the user judges against the same figures the bot was tuned to, and quotes the user's own v1.1 words as the thing that must no longer be true at 20."
    - "The build on the phone is the 27-03 pin's tree (docs-only commits after it do not change the APK): `git status --porcelain` empty before the build, `npm test` green, `npm run android:debug` succeeds, `adb install -r` → `am force-stop` → `monkey ... LAUNCHER 1` on the Pixel 7 (serial from `adb devices`, base `adb-28051FDH200H0R`), `android/gradle.properties` restored if the build touched it (never committed), no `www/` or APK committed."
    - "This plan changes NO engine, content, src, mazeworld.html, tools or test file — the retune under test is 27-03's pin; the ONLY file committed is docs/DIFFICULTY-RETUNE.md (the DR checklist)."
    - "The plan ENDS on a blocking human checkpoint that hands the user the four runs and the report format; the executor stops there and writes nothing into the Verdict block."
  artifacts:
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "### DR checklist — TUNE-07 filled: build identity, install proof, dev-start how-to with the current Settings labels, four run sections with 'what should be true' + fill-in tables, flag-if-noticed, the blank three-valued Verdict block and the what-happens-next rules"
      contains: "### Run 4 — natural"
  key_links:
    - from: "docs/DIFFICULTY-RETUNE.md DR checklist 'what should be true' lines"
      to: "the band table and the Comparison vs band (27-03)"
      via: "each run quotes the relevant band row(s) and the retune AFTER numbers verbatim"
      pattern: "what should be true"
    - from: "the debug APK on the Pixel 7"
      to: "the 27-03 pin"
      via: "built from a clean tree at or after the pin with no engine/content/src/tools change; hash + APK timestamp + dumpsys line recorded in the checklist"
      pattern: "app-debug.apk"
    - from: "the checkpoint's resume signal"
      to: "the Verdict block"
      via: "the orchestrator writes the user's verdict word (tuned / tune-again / deferred) verbatim; the executor never does"
      pattern: "tune-again"
  prohibitions:
    - "MUST NOT modify engine/, content/, src/, mazeworld.html, tools/ or test/ — `git diff --quiet <27-03 pin> -- engine content src mazeworld.html tools test` exits 0 at the end of the plan"
    - "MUST NOT write the verdict, mark TUNE-07 or the phase complete, or claim the band is met on the basis of the bot numbers — the Verdict block reads '(to be filled by the tester ...)' at the end of the plan"
    - "MUST NOT uninstall the app on the phone (Preferences data would be lost) — on a signer mismatch STOP and report at the checkpoint"
    - "MUST NOT commit android/gradle.properties, www/, or any APK/AAB; MUST NOT bump the version or build a release/Play bundle (the Play internal-build offer is the orchestrator's standing ASK after the verdict)"
---

<objective>
Put the retuned build on the Pixel 7 and hand the user the TUNE-07 DR round: build the debug APK from the 27-03 pin, install and relaunch it over adb, append the DR checklist to `docs/DIFFICULTY-RETUNE.md` (build identity, dev-start how-to, four runs — forced 20 / 35 / 50 and a natural run — each with "what should be true" lines from the band and the AFTER, a fill-in table, the flag-if-noticed list, the three-valued verdict left blank, and the what-happens-next rules), commit the checklist, and STOP on a blocking human checkpoint. The verdict is the user's; the orchestrator records it and handles tune-again, deferral and the Play-build offer.

Purpose: TUNE-07 — a human DR round on the Pixel 7 (start-at-depth 20/35/50 plus a natural run) re-issues the TUNE-04 verdict; the milestone closes only on "tuned" or a user-recorded deferral. The user's v1.1 words — "Level 20, way overtuned. It's instant death on any combat." — are the thing that must no longer be true at 20.
Output: APK installed and running on the phone, the DR checklist committed, one blocking checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-delve-to-death-retune/27-CONTEXT.md
@.planning/phases/27-delve-to-death-retune/27-03-SUMMARY.md
@docs/DIFFICULTY-RETUNE.md
@docs/RELEASING.md
@android/version.properties
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the debug APK from the pin, install + relaunch on the Pixel 7 over adb, append the DR checklist (build identity, how-to, four runs, blank verdict) to the ledger, commit</name>
  <files>docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md: the v1.1 `## DR checklist — TUNE-04 sign-off` (its `### Getting the build on the device`, `### Starting a run at depth N`, per-run tables, `### Flag if noticed`, `### Verdict`, `### What happens next (D-16)` — the shape to mirror) and the v1.2 section's `### Target band`, `### Comparison vs band` (the AFTER numbers per measure, the misses handed to this round), `### Change table` (the final curve at 20 / 35 / 50), and the two placeholders `### DR checklist — TUNE-07` / `### Verdict (TUNE-07)`
    - .planning/STATE.md "Ground Truth": Build/env (JDK pin, `npx cap sync` wipes the gradle java.home pin and pin-jdk re-applies it; AGP/Gradle versions), Device (Pixel 7 wireless adb, serial base `adb-28051FDH200H0R`, port rotates, rediscover via `adb mdns services`; install alone does not reload the WebView; signer mismatch rule; screenshots hit the lock screen — the user reviews and reports), the standing Play-build ASK rule
    - docs/RELEASING.md (debug vs release commands; never bump/build release here)
    - mazeworld.html: the Settings sheet rows `#mw-settings-version-row` ("Version <versionName> (<versionCode>)", stamped by tools/build-www.mjs from android/version.properties — currently 1.2.0 (3)) and `#mw-dev-row` ("Start at depth (dev)", the number input, the "Start" button); the dev banner text "A dev run. The graveyard has agreed to look the other way." and the DEV chip
    - 27-03-SUMMARY.md: the pin hash, the AFTER numbers per band row, the misses, the final curve at 20 / 35 / 50
    - android/version.properties (versionName / versionCode to quote)
  </read_first>
  <action>
    1. Preflight: `git status --porcelain` must be empty; `PIN=$(git rev-parse HEAD)` (this is at or after the 27-03 pin; `git diff --quiet <27-03 pin> -- engine content src mazeworld.html tools` must exit 0); `npm test` green (record the count).
    2. Build in the background with a sentinel: `(npm run android:debug; echo EXIT=$?) > <scratch>/android-debug.log 2>&1` (Bash `run_in_background: true`; ≈ 1-3 min); poll the log's last line with bounded checks until `EXIT=0` (a non-zero exit: quote the last 40 log lines and stop). After the build, if `git status --porcelain` shows `android/gradle.properties` modified, run `git checkout -- android/gradle.properties` (the local JDK pin — never committed). Record the APK identity: `ls -l --time-style=full-iso android/app/build/outputs/apk/debug/app-debug.apk` (bytes + timestamp), and `grep -E "versionName|versionCode" android/version.properties`.
    3. Install and relaunch (ADB="C:/Users/Dell/AppData/Local/Android/Sdk/platform-tools/adb.exe"): `"$ADB" devices` — take the serial that starts with `adb-28051FDH200H0R` (it may carry a `-<suffix>._adb-tls-connect._tcp` tail; if absent run `"$ADB" mdns services` and retry once; if still absent, STOP and report "device not reachable" at the checkpoint with the APK path and the three commands for the user). Then `"$ADB" -s "$SERIAL" install -r android/app/build/outputs/apk/debug/app-debug.apk` (on `INSTALL_FAILED_UPDATE_INCOMPATIBLE` — a differently-signed build is on the phone — do NOT uninstall; report it at the checkpoint), `"$ADB" -s "$SERIAL" shell am force-stop com.darktierstudios.delvedierepeat`, `"$ADB" -s "$SERIAL" shell monkey -p com.darktierstudios.delvedierepeat -c android.intent.category.LAUNCHER 1`, and prove it: `"$ADB" -s "$SERIAL" shell dumpsys package com.darktierstudios.delvedierepeat | grep -E "versionName|lastUpdateTime"` — `lastUpdateTime` must be later than the APK timestamp's minute and `versionName` must equal android/version.properties' versionName. Quote these lines in the checklist.
    4. Ledger (Edit tool, CRLF): replace `(filled by plan 27-04)` under `### DR checklist — TUNE-07` with:
       - A preface: this round is the phase's exit criterion; the constants under test (the final change-table values: `COMBAT_SCALE_FROM_DEPTH`, the three MAX/K pairs, the non-combat caps) and the curve at 20 / 35 / 50 (cap / power / cadence / dots / blobs / radius) copied from the change table; the retune AFTER band verdicts in one line each (in / out with the numbers) and the sentence that the bot is a rarity floor — the tester is the user, on the Pixel 7, and only the tester fills the verdict.
       - `#### Build under test`: commit `<PIN>` (full hash), `android/app/build/outputs/apk/debug/app-debug.apk` — <bytes> bytes, <timestamp>; version <versionName> (<versionCode>); `npm test` <count> green; installed on `<SERIAL>` — the dumpsys `versionName` / `lastUpdateTime` lines verbatim; the three adb commands used (with the full adb path) for reinstalling if needed.
       - `#### Starting a run at depth N`: 1) start or resume a run; 2) HUD gear → Settings; 3) press and HOLD the last row, "Version <versionName> (<versionCode>)", ≈ 1.2 s (a plain tap does nothing); 4) the hidden "Start at depth (dev)" row appears; 5) type 20, 35 or 50; 6) tap Start; 7) the log shows "Floor N." with the dev banner ("A dev run. The graveyard has agreed to look the other way.") and the HUD shows the DEV chip; the hero is level 5 with a 300 × N wilmst purse; the run is never buried and never a best depth. Each forced run is a fresh dev start.
       - `#### Run 1 — forced 20 (the acceptance bar)`: **what should be true:** the v1.1 words "instant death on any combat" must no longer be true — a level-5 hero should get 3-5 fights, not one (band: encounters survived 3.0-5.0; retune AFTER <value>); clearing floor 20 should be possible but not expected (band: floors gained p50 >= 1, mean 1.0-2.0; retune AFTER p50 <value> / mean <value>); the curve here (from the change table): cap <n>, power <x>, cadence <x>, dots <n>, blobs <n>, radius <n>. Fill-in table rows: fights before death (count) | floors cleared from 20 | did any single fight feel like a coin flip or a wall? (name the foe) | session length (start/end clock) | ended for a reason I understood — yes/no + epitaph | free notes.
       - `#### Run 2 — forced 35` and `#### Run 3 — forced 50`: **what should be true:** descriptive only — the run should wrap up naturally (no dial-back, no forced death); death should come from the curve (foe cap / power / cadence at that depth, copied from the change table), not from a cliff; report floors gained and fights survived; the bot's tune-difficulty readout at that depth (floors gained p50 / p90 / max, top causes from the AFTER transcripts) is the yardstick. Same fill-in rows.
       - `#### Run 4 — natural (floor 1 onward)`: **what should be true:** floor 1 still kills careless level-1 characters (traps, starvation, a bad fight) but no longer three strikes a round from one canon foe — Dante is now met from tier 2 (level 2+ / depth 2+) and Ned holds tier 1; darkness arrives smaller from floor 3 and density ramps a floor later; the band's natural median is 5-6 (retune AFTER pooled p50 <value>, reach >= 10 <value> %, reach >= 20 <value> %) — a human should typically outlast the bot. Fill-in rows: death depth | first Humans encounter (who, which floor, how did it go) | darkness / starvation felt (floors 3-5) | first fight that felt like the ramp (floor) | session length | ended for a reason I understood — yes/no + epitaph | free notes.
       - `#### Flag if noticed (observations, not tasks)`: any class that feels wrong at 20 (Phase 26's revisit list is empty — name it and it becomes a v1.3 candidate); Con Artist talk-heavy runs (watch, don't touch); the Oracle voice slipping out of family-friendly sarcasm; Ned's line landing flat.
       - Replace the Verdict placeholder's line under `### Verdict (TUNE-07)` with the block: `**Overall verdict:** (to be filled by the tester — exactly one of: tuned / tune-again / deferred)`; `**Per-run notes above complete:** (to be filled by the tester)`; `**If deferred — the user's reason, verbatim:** (to be filled by the tester)`; `**Gate at hand-off:** npm test <count>, parity 33/33, pin <PIN7>, APK <timestamp>, installed <lastUpdateTime> — <date>.`; and `**What happens next:** tuned → TUNE-07 closes the milestone's deferred TUNE-04 verdict; the phase's VERIFICATION flips to passed on the user's word; per the standing rule the assistant then ASKS whether to push a versionCode-bumped signed AAB to the Play internal-testing track (`node tools/bump-version.mjs` + `npm run android:release`) — never unasked. tune-again → ONE more bounded iteration inside this phase: 27-03's iteration protocol on `engine/difficulty.js` constants only (+ `PHASE_27_PINS`), a `#### Addendum — tune-again iteration` under the change table with before/after values and a rationale, a fresh smoke, a rebuild, and a second round of this same checklist; no re-plan. deferred → the user's reason is recorded verbatim above and the milestone closes on it; the shipping constants stay as pinned. The phase cannot be marked complete while the verdict is blank.`
    5. `node --test test/unit/difficulty-retune-ledger.test.js` (still green — the checklist fills only its own placeholders). Commit ONLY the ledger: `docs(27-04): DR checklist — TUNE-07 round on pin <PIN7>, build <timestamp> installed on the Pixel 7; verdict left to the tester`. Final proof: `git status --porcelain` empty; `git diff --quiet <27-03 pin> -- engine content src mazeworld.html tools test` exits 0.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js && node -e "const d=require('fs').readFileSync('docs/DIFFICULTY-RETUNE.md','utf8');const s=d.slice(d.indexOf('### DR checklist — TUNE-07'));for(const h of ['#### Build under test','#### Starting a run at depth N','#### Run 1 — forced 20','#### Run 2 — forced 35','#### Run 3 — forced 50','#### Run 4 — natural','#### Flag if noticed','app-debug.apk','lastUpdateTime','what should be true','instant death on any combat','exactly one of: tuned / tune-again / deferred','What happens next'])if(!s.includes(h))throw new Error('missing: '+h);if(!/\*\*Overall verdict:\*\* \(to be filled by the tester/.test(s))throw new Error('verdict must be blank');if((s.match(/#### Run [1-4] — /g)||[]).length!==4)throw new Error('exactly four runs');console.log('checklist ok')" && git status --porcelain | grep -c . | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - The verify script prints `checklist ok`; the working tree is clean; the only file in `git show --stat --format= HEAD` is `docs/DIFFICULTY-RETUNE.md`.
    - `"$ADB" -s "$SERIAL" shell dumpsys package com.darktierstudios.delvedierepeat | grep lastUpdateTime` shows a time later than the APK's timestamp, and the same line is quoted in the checklist (or, if the device was unreachable / signer-mismatched, the checklist says so and the checkpoint reports it).
    - `git diff --quiet <27-03 pin> -- engine content src mazeworld.html tools test` exits 0; no `www/`, APK or `android/gradle.properties` change is committed.
  </acceptance_criteria>
  <done>The retuned build is on the phone and running, the checklist tells the user exactly what to play and what should be true at each depth, the verdict block is blank with its three allowed values, and nothing but the ledger changed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (checkpoint): The TUNE-07 DR round — hand the retuned build and the four-run checklist to the user, then STOP</name>
  <action>Do nothing further in this plan. Present the what-built / how-to-verify text below to the user and stop; do not write, infer or pre-fill the verdict (the orchestrator records the user's word verbatim in the ledger's `### Verdict (TUNE-07)` block). This checkpoint is the phase's exit criterion and is never auto-approved.</action>
  <what-built>The v1.2 delve-to-death retune (Phase 27): Dante demoted to tier 2 (Ned holds tier 1), darkness and encounter density eased from floor 3, and the combat ramp moved so depth 20 is no longer "instant death on any combat" — retuned constants pinned in `engine/difficulty.js`, the full bot AFTER recorded against the band in `docs/DIFFICULTY-RETUNE.md` (`## v1.2 retune (Phase 27)`), and the debug build installed and launched on the Pixel 7 (build identity in `#### Build under test`).</what-built>
  <how-to-verify>
    Play the four runs in `docs/DIFFICULTY-RETUNE.md` → `### DR checklist — TUNE-07` (each forced run is a fresh dev start: Settings → hold the "Version …" row ≈ 1.2 s → "Start at depth (dev)" → type the depth → Start; the DEV chip confirms it):
    1. Run 1 — forced 20: the acceptance bar. Count the fights you survive and the floors you clear. The band says 3-5 fights and clearing a floor should be possible; "instant death on any combat" must no longer be true.
    2. Run 2 — forced 35 and 3. Run 3 — forced 50: descriptive — does the run wrap up naturally on the curve (no cliff, no dial-back)? Note floors gained and fights survived.
    4. Run 4 — natural from floor 1: floor 1 still dangerous but no three-strikes-a-round canon foe at level 1; darkness smaller from floor 3; how deep did you get, and what killed you?
    For each run, fill the table (or just tell me the numbers and impressions). Then give the verdict — exactly one word: **tuned** (the milestone closes; I will then ASK about pushing a Play internal-testing build), **tune-again** (one more bounded constants iteration + rebuild + this checklist again), or **deferred** (tell me the reason; it is recorded verbatim and the milestone closes on it).
    If the phone did not get the build (device unreachable / signer mismatch — see the checklist's `#### Build under test`), say so and I will hand you the install commands.
  </how-to-verify>
  <resume-signal>Reply with the verdict word — tuned / tune-again / deferred — plus your per-run notes (and the reason, if deferred). The orchestrator records your words verbatim in the ledger's Verdict block; nothing is written there until you answer.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor -> device | adb install/relaunch on the user's phone; an uninstall would destroy Preferences data |
| bot numbers -> human verdict | the checklist must present the band and the AFTER as the yardstick without pre-judging the verdict |
| executor -> Verdict block | only the user (via the orchestrator) writes the verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-19 | Repudiation | executor fills or implies the verdict | high | mitigate | verify asserts the Verdict line is the blank template; the checkpoint is blocking; the SUMMARY states the phase ends on the human round |
| T-27-20 | Tampering | build not from the pin (stale www, unrestored gradle pin, uncommitted change) | high | mitigate | clean tree before the build; git diff gate vs the 27-03 pin; hash + APK timestamp + dumsys line recorded |
| T-27-21 | Denial of Service | uninstalling the app on a signer mismatch (data loss) | medium | mitigate | prohibition: never uninstall; report at the checkpoint instead |
| T-27-22 | Information Disclosure | committing www/ or the APK | low | mitigate | .gitignore covers both; verify checks the commit's file list |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero deps) |
</threat_model>

<verification>
- The DR checklist section exists with four runs, the build identity, the how-to, and the blank three-valued verdict; the ledger guard test is green; the tree is clean; only the ledger was committed.
- The phone runs the build from the pin (dumpsys lastUpdateTime after the APK timestamp).
- The plan ends on the blocking checkpoint; the executor's SUMMARY says the phase waits on the user's verdict.
</verification>

<success_criteria>
- TUNE-07's hand-off is complete: the user can play the four prescribed runs on the retuned build and answer with one of three words; the orchestrator can record the verdict, run the tune-again loop if asked, or close the milestone on "tuned" / a recorded deferral and then ASK about the Play internal build.
</success_criteria>

<output>
Create `.planning/phases/27-delve-to-death-retune/27-04-SUMMARY.md` when done (before the checkpoint resolves). Record: the pin, the APK bytes/timestamp, the device serial and the dumpsys lines, the `npm test` count, the ledger commit hash, and the statement that the verdict is outstanding by design (the phase ends `human_needed` on the DR round).
</output>
