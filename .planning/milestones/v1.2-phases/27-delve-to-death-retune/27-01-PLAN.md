---
phase: 27-delve-to-death-retune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DIFFICULTY-RETUNE.md
  - tools/lib/class-matrix.mjs
  - tools/tune-classes.mjs
  - test/unit/class-matrix.test.js
  - test/unit/difficulty-retune-ledger.test.js
autonomous: true
requirements: [TUNE-05]
tags: [ledger, target-band, docs-first, harness-readout, pooled-rollup, standing-guard, no-engine-change]

must_haves:
  truths:
    - "TUNE-05 (boundary): band edges are closed intervals stated with their measurement: natural median death depth 5-6 inclusive (p50 over ALL completed natural-start runs, integer floors, so 5 and 6 both pass); pooled reach >= 20 1.0-2.0 % inclusive; forced-20 encounters survived (pooled mean) 3.0-5.0 inclusive; forced-20 floors gained p50 >= 1 AND mean 1.0-2.0 inclusive; a reading exactly on an edge is IN band. The reach >= 10 corridor (~10-20 %) is recorded as consistency-derived and ADVISORY, never a pass/fail edge. All of this is written into docs/DIFFICULTY-RETUNE.md as a table with a 'how measured' column BEFORE any constant or bestiary change (this plan commits before 27-02 starts), and test/unit/difficulty-retune-ledger.test.js asserts the section exists and the numbers appear."
    - "TUNE-05 (empty): the ledger states the evaluation order — the cannot-act gate (`node tools/class-pass-diff.mjs --gate --after docs/class-pass/retune-after.json`) runs FIRST and the band is not evaluated when it fails; pooled numbers are run-weighted (summarizeRows over the concatenation of every cell's rows) so an all-stuck cell contributes nothing rather than NaN; the forced-20 floors-gained p50 over 1,430 runs is read from the harness's own new `rollups.pooled` block, never recomputed by hand."
    - "TUNE-05 (encoding): the new ledger section is plain Markdown appended with the Edit tool so the file's CRLF line endings are preserved (core.autocrlf=true; never rewrite the file), numbers are copied verbatim from harness output, commit pins are short hashes, the user's v1.1 verdict is quoted verbatim in straight quotes, no HTML."
    - "TUNE-05 (precision): every band number is stated to the precision the harness prints (mean depth / floors / encounters 2 dp, reach 1 dp, p50 integer) and comparison uses printed precision with no hidden rounding; the Phase 21 D-09 targets (median 8-15, p90 >= 25, < 2 % past 50, 40-80 actions/floor) are explicitly marked SUPERSEDED in the same section so two bands never coexist."
    - "The band is measurable from the harness JSON alone: tools/lib/class-matrix.mjs gains per-cell `reach20` and a run-weighted `rollups.pooled` block (key 'ALL', the full summarizeRows shape over every row of every cell) — an ADDITIVE readout change; tools/lib/tuning-bot.mjs (the bot) is byte-identical to the Phase 26 pin 5565b22, `botLine` and the `meta.bot` string are untouched, and Phase 26's committed JSON/ledger still pass test/unit/class-pass-ledger.test.js unchanged."
    - "No engine, content, src, mazeworld.html or fixture file changes in this plan: `git diff --quiet HEAD~N -- engine content src mazeworld.html test/parity` holds for every commit of this plan."
  artifacts:
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "## v1.2 retune (Phase 27) — TUNE-05..07: v1.1 verdict verbatim, standing rules, the target band table (measure / Phase 26 AFTER / target / how measured / edge rule), D-09 superseded, bot proxy + Bot lines, BEFORE-by-reference, levers + the Dante decision rule, iteration protocol, and placeholder sub-headings for 27-02/27-03/27-04"
      contains: "## v1.2 retune (Phase 27)"
    - path: "tools/lib/class-matrix.mjs"
      provides: "summarizeRows reach20; pooledSummary(cellRows); rollups().pooled; formatText POOLED block; header JSON-shape comment updated"
      contains: "export function pooledSummary"
    - path: "test/unit/difficulty-retune-ledger.test.js"
      provides: "structural standing guard for the v1.2 section (heading present exactly once and last, band table numbers, superseded marker, verbatim verdict quote, sub-heading order); extended by 27-03 with the retune AFTER JSON provenance checks"
      contains: "v1.2 retune (Phase 27)"
    - path: "test/unit/class-matrix.test.js"
      provides: "reach20 edge/null tests, pooledSummary run-weighting test, formatText POOLED block + Bot-line-last test"
      contains: "pooledSummary"
  key_links:
    - from: "docs/DIFFICULTY-RETUNE.md band table 'how measured' column"
      to: "tools/lib/class-matrix.mjs rollups.pooled"
      via: "each band row names the exact JSON path (rollups.pooled.p50Depth / reach10 / reach20 / meanEncountersSurvived / p50FloorsGained / meanFloorsGained) of docs/class-pass/retune-after.json or retune-after-depth20.json that 27-03 reads"
      pattern: "rollups\\.pooled"
    - from: "test/unit/difficulty-retune-ledger.test.js"
      to: "docs/DIFFICULTY-RETUNE.md"
      via: "reads the ledger, slices the v1.2 H2 section, asserts the band numbers and sub-headings"
      pattern: "DIFFICULTY-RETUNE.md"
  prohibitions:
    - "MUST NOT touch engine/, content/, src/, mazeworld.html, test/parity/ or tools/lib/tuning-bot.mjs — this plan records the band and adds a readout; it tunes nothing"
    - "MUST NOT change tools/class-pass-diff.mjs (test/unit/class-pass-ledger.test.js re-renders Phase 26's ledger through it) or any docs/class-pass/*.json"
    - "MUST NOT change botLine() or the Bot: line format — 27-03's AFTER Bot lines must equal Phase 26's byte-for-byte"
    - "MUST NOT edit the v1.1 sections of docs/DIFFICULTY-RETUNE.md above the new H2 (append only)"
---

<objective>
Record the user-decided delve-to-death target band (TUNE-05) in `docs/DIFFICULTY-RETUNE.md` under a new `## v1.2 retune (Phase 27) — TUNE-05..07` section BEFORE any constant or bestiary moves, mark the v1.1 D-09 targets superseded, quote the v1.1 verdict and the standing rules verbatim, list the levers and the Dante decision rule, and write the bounded iteration protocol the next two plans follow. Make the band measurable from the harness JSON alone by adding an additive, bot-neutral pooled readout (`reach20` per cell + `rollups.pooled`) to `tools/lib/class-matrix.mjs` — Phase 26's own handoff says the natural matrix's >= 20 rate and overall median are not carried by the JSON, so this plan supplies them. Add a small structural guard test so the section and its numbers cannot silently drift.

Purpose: 27-CONTEXT.md locks "the band is recorded BEFORE tuning begins" and the edge probes lock closed edges, printed precision and harness-derived pooled numbers. This plan is the docs-first gate; it is committed before 27-02 starts.
Output: the v1.2 ledger section, `pooledSummary`/`reach20` in the class-matrix readout with tests, `test/unit/difficulty-retune-ledger.test.js`, `npm test` green (>= 1421 + the new tests), three commits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-delve-to-death-retune/27-CONTEXT.md
@docs/DIFFICULTY-RETUNE.md
@tools/lib/class-matrix.mjs
@test/unit/class-matrix.test.js
@test/unit/class-pass-ledger.test.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide list; owner plan in brackets)

- `docs/DIFFICULTY-RETUNE.md` `## v1.2 retune (Phase 27) — TUNE-05..07`: band table, superseded D-09, verdict quote, standing rules, bot proxy, BEFORE-by-reference, levers + Dante decision rule, iteration protocol, placeholders. [27-01] → Dante landed numbers + iteration 0 [27-02] → change table, iteration log 1..N, AFTER transcripts, comparison vs band, counterweight triggers, not-changed-and-why [27-03] → DR checklist (four runs) + blank verdict [27-04].
- `tools/lib/class-matrix.mjs` + `tools/tune-classes.mjs` header: per-cell `reach20`, `rollups.pooled`, `POOLED` text block. Bot untouched. [27-01]
- `test/unit/difficulty-retune-ledger.test.js` — standing guard. [27-01, extended 27-03]
- `content/bestiary.js` Dante demotion + tier-1 Humans replacement, DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06) block; header comment updated. [27-02]
- `test/parity/fixtures/action-script.combat.json` `parley` scenario `divergence: { kind: "action-path", fromAction: 0, ... }` record; `test/parity/FIXTURE-INVENTORY.md` regenerated roster block + `## Phase 27 Dante divergence` section; unit-test pins that named the old Dante row/numbers re-measured. [27-02]
- `engine/difficulty.js`: `DENSITY_CANON_THROUGH_DEPTH = 2` + restructured dots formula (canon on floors 1-2 by construction), eased `ENCOUNTER_DOT_CAP` / `DARK_BLOB_CAP` / `DARK_RADIUS_CAP`, and (27-03) the combat dials `COMBAT_SCALE_FROM_DEPTH` / `FOE_CAP_MAX` / `FOE_POWER_MAX` / `ABILITY_THREAT_MAX` (+ `*_SOFT_K`); one `DELIBERATE RULES CHANGE (Phase 27, TUNE-06)` block. [27-02 non-combat, 27-03 combat]
- `test/difficulty/difficulty.test.js`, `test/unit/combat-scaling.test.js`, `test/unit/maze.test.js`: floors 1-2 canon guard, floors 3-5 Phase 27 pins, `PHASE_27_PINS` constant object, identity through `COMBAT_SCALE_FROM_DEPTH - 1`. [27-02 structure, 27-03 final values]
- `docs/class-pass/retune-after.json`, `docs/class-pass/retune-after-depth20.json` — the retune AFTER (143 x 40 natural, 143 x 10 forced-20), pinned to the final constants commit, Bot lines identical to Phase 26's. Phase 26's `after.json` / `after-depth20.json` / `verdicts.json` untouched. [27-03]
- Debug APK on the Pixel 7 + DR checklist; the phase ends on a blocking human checkpoint. [27-04]
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Append the `## v1.2 retune (Phase 27) — TUNE-05..07` section to docs/DIFFICULTY-RETUNE.md — band table, superseded D-09, verbatim verdict, standing rules, bot proxy, BEFORE by reference, levers + Dante decision rule, iteration protocol, placeholders</name>
  <files>docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - .planning/phases/27-delve-to-death-retune/27-CONTEXT.md in full — the band table, the standing rules, the levers, the method, the DR round, Claude's Discretion, Specific Ideas, Deferred Ideas
    - docs/DIFFICULTY-RETUNE.md: the header (lines 1-60), `## Targets (D-09)`, `## Bot proxy (TUNE-02)`, `## Change table (21-04)` + `### Iteration log`, `## Comparison vs D-09 (21-04)`, `## Not changed, and why (21-04)`, `### Verdict` and `### What happens next (D-16)` (the section shapes to mirror; the verdict block holds the user's exact v1.1 words)
    - docs/CLASS-PASS.md `## Handoff to Phase 27` (the Phase 26 AFTER yardstick: mu 3.08, reach >= 5 17.2 / >= 10 0.3, cannot-act 0, commits before 5565b22 / after d1e3235, the by-class natural and depth-20 roll-ups, the Caveats — especially "the natural matrix's >= 20 rate and its overall median are not carried per cell by the harness JSON")
    - .planning/phases/26-mass-playtest-class-pass-ledger/26-02-SUMMARY.md — the capture procedure (pin proof, background run + EXIT sentinel, gate before ledger, meta.commit auto-stamp hazard)
  </read_first>
  <action>
    Append (Edit tool, after the file's last line; the file is CRLF — do not rewrite it) a new H2 section `## v1.2 retune (Phase 27) — TUNE-05..07` with these H3 sub-sections IN THIS ORDER (the guard test in Task 3 pins the order):

    1. `### Why again — the v1.1 verdict, verbatim`: quote the user's TUNE-04 verdict exactly as the `### Verdict` block above records it — "Level 20, way overtuned. It's instant death on any combat." (user, 2026-09-14; run 1 at depth 20 was enough; runs 2/3 not played; retune deferred until player power moved) — and the D-16 lead ("the depth-20 band (foe cap 4, power ≈1.21×, cadence ≈1.39×) is already lethal for a level-5 hero ... pull `FOE_POWER_MAX`/`ABILITY_THREAT_MAX` down and/or push `*_SOFT_K` out, and consider capping foes-per-encounter growth below 5"). State the acceptance bar at 20 in the user's words: a level-5 hero at 20 should get 3-5 fights, not one.

    2. `### Standing rules (user, 2026-09-14 / 2026-09-15)`: depth 20 is THE tuning target, not infinite depth; reaching 20 is a unicorn run (rare, celebrated, not expected); past 20 nothing is dialed back and no mechanic forces death — the run wraps up naturally on the existing curve; "competent player" cannot be quantified in a game this RNG-heavy, so the bot proxy sets rarity floors and the human DR round (TUNE-07) is the real verdict; keep the opening honest — floor 1 still kills careless level-1 characters (traps, starvation, a bad fight); the change is that ONE canon foe no longer does it three times a round; the tail matters more than the middle (1-2 % reach 20 means the curve keeps steepening past ~10 while the median sits at 5-6).

    3. `### Target band (TUNE-05) — supersedes D-09`: a Markdown table `| Measure | Phase 26 AFTER (pin d1e3235) | Target | How measured | Edge rule |` with exactly these rows:
       - Natural median death depth | 3 (class p50 Thief 3 / Fighter 3 / Magic User 2; pooled mean 3.08) | **5-6** (whole curve: the shallow end is eased too — with the widened early-floor levers of CONTEXT Levers §3; the user chose to widen rather than record a known miss) | `rollups.pooled.p50Depth` of `docs/class-pass/retune-after.json` — `tools/tune-classes.mjs --seeds 40 --workers 4 --max-actions 5000` (143 x 40, seeds `i*7919+1`, Bot line identical to Phase 26) | closed: 5 and 6 pass; integer floors
       - Reach >= 10 (pooled) | 0.3 % | ≈ 10-20 % | `rollups.pooled.reach10` | ADVISORY corridor, consistency-derived from the two user pins on either side — never pass/fail
       - Reach >= 20 (pooled, bot) | n/a in the Phase 26 JSON (0.0 % in every v1.1 200-seed readout) | **1.0-2.0 %** (the unicorn; the bot's number is a rarity floor — humans play better) | `rollups.pooled.reach20` (added by this plan) | closed: 1.0 and 2.0 pass; 1 dp
       - Forced start at 20, level-5 hero: encounters survived | 1.32 (pooled mean; Thief 1.72 / Fighter 1.17 / Magic User 1.07) | **3.0-5.0** ("dangerous, not hopeless") | `rollups.pooled.meanEncountersSurvived` of `docs/class-pass/retune-after-depth20.json` — `tools/tune-classes.mjs --seeds 10 --workers 4 --max-actions 5000 --start-depth 20` (143 x 10) | closed; 2 dp
       - Forced start at 20: floors gained | mean 0.14, p50 0 | **p50 >= 1 AND mean 1.0-2.0** | `rollups.pooled.p50FloorsGained` / `rollups.pooled.meanFloorsGained` (same file) | both conditions; closed; p50 integer, mean 2 dp
       - Past 20 (forced 35 / 50) | — | descriptive only: floors gained (= death depth − start) and causes are REPORTED; no target, no dial-back, no forced death | `node tools/tune-difficulty.mjs --seeds=200 --start-depth=35` and `--start-depth=50` death-depth distribution + cause table | none (reported)
       - Cannot-act cells | 0 of 143 | **0** (hard gate) | `node tools/class-pass-diff.mjs --gate --after docs/class-pass/retune-after.json` exits 0 | evaluated FIRST; a failing gate stops the retune before the band is read
       Write comparison operators in the table as ASCII `>=` (never the `≥` glyph) — the guard test matches `p50 >= 1` and `Reach >= 20` literally. Below the table: (a) "Precision: numbers are compared at the precision the harness prints — means 2 dp, reach 1 dp, p50 integer; no hidden rounding." (b) "Pooling: every pooled number is run-weighted — `summarizeRows` over the concatenation of all 143 cells' rows (`rollups.pooled`, added by 27-01); a cell with zero completed runs contributes nothing, never NaN; the cannot-act gate runs before the band is evaluated." (c) The superseding sentence, verbatim: "The Phase 21 D-09 targets above (median death depth 8-15, p90 >= 25, < 2 % of runs past floor 50, median 40-80 actions per floor) are SUPERSEDED by this table as of 2026-09-15 — they were set for an infinite-depth curve the user has since rejected; only this band is evaluated from here on." (d) Cheap smoke proxy note: during iterations `node tools/tune-difficulty.mjs --seeds=200` (natural) and `tools/tune-classes.mjs --seeds 3` slices are directional readouts only; the band is judged on the full AFTER.

    4. `### Bot proxy and parameters (identical to Phase 26)`: the harness is `tools/tune-classes.mjs` on `tools/lib/tuning-bot.mjs` (Phase 22, byte-identical to pin 5565b22 — no bot policy changes this phase); quote the two Phase 26 Bot lines verbatim (from `docs/class-pass/after.json` / `after-depth20.json` `meta.bot`): `Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5(caster)  potion<0.5  camp<0.5  seeds=40  workers=4  startDepth=1` and `Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5(caster)  potion<0.5  camp<0.5  seeds=10  workers=4  startDepth=20`; state that 27-03's AFTER JSONs must carry these strings byte-for-byte (`metaParity` modulo commit) and that this plan's only harness change is the additive pooled readout (`reach20`, `rollups.pooled`, `POOLED` text block) which changes no run, no draw and no Bot line.

    5. `### BEFORE — by reference (not re-run)`: BEFORE for this retune IS the Phase 26 AFTER: `docs/class-pass/after.json` and `after-depth20.json` (meta.commit d1e3235), their verbatim transcripts under `## AFTER — commit d1e3235...` in `docs/CLASS-PASS.md`, and the `## Handoff to Phase 27` yardstick (mu 3.08; Thief 3.51 > Fighter 3.16 > Magic User 2.55; reach >= 5 17.2 / >= 10 0.3; depth-20 floors gained 0.25 / 0.10 / 0.07, encounters survived 1.72 / 1.17 / 1.07 by class; cannot-act 0). Top death causes at scale (from the Phase 26 AFTER cells' topCauses — quote the Phase 26 handoff/CONTEXT figures): "cut down by a Dante" 724 tallies, "undone by a trap" 333, "starved in the dark" 300.

    6. `### Levers and the Dante decision (TUNE-06)`: transcribe the three levers from CONTEXT.md — (1) global depth dials in `engine/difficulty.js`, whole curve: combat knobs (`COMBAT_SCALE_FROM_DEPTH` may only move UP, never below 6; `FOE_CAP_MAX`/`_SOFT_K`, `FOE_POWER_MAX`/`_SOFT_K`, `ABILITY_THREAT_MAX`/`_SOFT_K`) for the ramp toward 20, identity through at least depth 5; non-combat knobs (`ENCOUNTER_DOT_*`, `DARK_BLOB_CAP`, `DARK_RADIUS_*`) may ease depths 3-5 — NOT depth 2: the movement parity fixture (seed 256) descends to floor 2 and compares that floor byte-for-byte, so floors 1-2 are canon by construction (a `DENSITY_CANON_THROUGH_DEPTH = 2` gate, landed in 27-02); (2) the Dante demotion (a deliberate canon deviation); (3) the WIDENED early-floor levers (user decision 2026-09-15, CONTEXT Levers §3 — after the planner's calibration showed dials + Dante alone leave the bot median at 3: kill table 20 % dead on floor 1, 66 % by floor 3, 85 % by floor 4): as further deliberate canon deviations, each behind its own draw-free `difficultyCurve` field and each with a declared divergence record where it touches a fixture — foe grace at floors 2-4 (`foePower` < 1 only at depths 2-4; floor 1 exactly 1.0), a trap/wall-fall damage ramp (new `hazardScale` field consumed by movement.js and encounters.js; starts at floor 2 unless the median still misses — a floor-1 start diverges the encounters trap scenario, seed 1, and is declared), starvation relief (darkness later — the parity-clean form holds one blob through floor 3; the from-floor-4 form diverges the movement fixture, seed 256, and is declared — then starting rations +1/+2 only if the median still misses, 14 chargen records), and floor-1 foe grace as LAST RESORT only (orchestrator decision) after everything else has been tried and measured; escalation order (smallest set that reaches median >= 5): Dante → foe grace 2-4 → darkness later → trap/fall ramp → rations → floor-1 grace, one rung per iteration, each measured with the smoke and recorded with a `DELIBERATE RULES CHANGE (Phase 27, 2026-09-15, TUNE-06)` comment, a change-table row and its record; (4) nothing else — no class change, no upkeep/economy dial unless a v1.1 counterweight trigger fires (economy: tune-economy `peakGold p50 > 5000`; party: `--party` p50 > 1.5 x solo p50 — both are MEASURED in the AFTER and recorded; if one fires it is reported to the orchestrator as a follow-up, not implemented inside the constants-only pass), no change to `engine/character.js`, spells or items.
       Then a `**Dante — the decision rule (recorded before the change):**` paragraph: Dante is the only tier-1 Humans entry (sz H, i 12, wp 20, `sp.atk 3` — three strikes a round) and the #1 killer; the demotion form is chosen by ONE measurable rule — an attack-only level-1 hero on floor 1 forced into a Humans encounter (2,000 seeds, `newRun(seed)` → `startCombat(state, false, "Humans", rng)` → `playerStrike` until resolved) must die no more often than against the next-deadliest tier-1 type (Demons, ≈ 25 %) — with the planner's calibration table (2026-09-15, same sim): canon wp 20 / atk 3 → hero dies 59.8 % (two-Dante fights are 51 % of Humans encounters and are won 16.7 %); Form A `sp.atk` 3 → 2 → 51.4 %; Form B wp 20 → 12 → 46.8 % (wp 10 → 42.8 %); A+B (atk 2, wp 12) → 37.4 %; Form C — Dante moved to tier 2 with stats and note byte-identical, and a plain new tier-1 Humans entry (wp 8, one swing a round) → 14.3 % (Beasts 6.8 / Lair Beasts 13.2 / Magical 13.3 / Walking Dead 15.6 / Demons 25.1), while depth-2 Humans fights for a level-2 hero move 40.8 % → 42.2 % (Dante fits tier 2 without opening a new spike). Only Form C satisfies the rule; it is the planner's recommendation and 27-02's default; 27-02 records the landed numbers under `### Dante demotion — landed (27-02)`.

    7. `### Iteration protocol (TUNE-06)`: cap of FOUR constant iterations after iteration 0 (27-02: Dante + non-combat easing, combat untouched); each iteration = change constants → update the test pins → `node --test test/difficulty test/unit/combat-scaling.test.js test/unit/maze.test.js` → smoke (background + `EXIT=` sentinel, bounded polling): `tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --json --out <scratch>/iterN-nat.json` (pooled p50 / reach10 / reach20 / causes) and `--start-depth 20 --seeds 3 ... --out <scratch>/iterN-d20.json` (pooled encounters survived / floors gained), plus `tools/tune-difficulty.mjs --seeds=200` for the cause table → read against the band → log entry (constants; readout numbers; what moved; what to turn next, one rationale sentence per knob) → stop when the smoke lands inside the band or at iteration 4, then the FULL AFTER on a proven pin. Missed measures after the cap are RECORDED with the executor's one-line reading and handed to the DR round — never chased with a fifth iteration. Add the planner's calibration note (directional, 143 x 3 + 200-seed natural, 2026-09-15): at full combat identity the level-5 bot at depth 20 survives ≈ 3.0 encounters and gains ≈ 0.4 floors (p50 0) — identity is the ceiling of what the deep combat dials can do; fewer deep encounter dots and less darkness lift floors gained toward ≈ 1 (dots 12, blobs 3, radius 7 → ≈ 0.97 floors, ≈ 3.1 encounters). For the median: with dials + Dante alone the bot median stayed 3 (pooled reach >= 5 ≈ 17 %); the widened early-floor levers each add ≈ 3-7 points of reach >= 5 (foe grace 0.75 at 2-4: 22.8 %; darkness from 4: 21.7 %; trap/fall ramp 0.5: 24.0 %; grace 0.5 + darkness hold + ramp from floor 2: 24.7 %; + rations +2: 30.8 %; + darkness from 4 + ramp from floor 1: 35.4 %; + floor-1 grace 0.85: 34.7 %) — the full ladder tops out near 35 % (bot median 4, mean ≈ 4.0), so the median row is expected to be recorded as a miss with the ladder's ceiling stated unless the executor's smoke says otherwise; the escalation order and the "turn a rung only where a gain is predicted" rule live in 27-03. A median miss after the cap is a recorded outcome, not a reason for a fifth iteration.

    8. Placeholders, each an H3 with exactly one line beneath it: `### Dante demotion — landed (27-02)` → `(filled by plan 27-02)`; `### Change table (27-02 / 27-03)` → `(filled by plan 27-03)`; `### Iteration log` → `(filled by plans 27-02 and 27-03)`; `### AFTER readouts — retuned engine` → `(filled by plan 27-03)`; `### Comparison vs band` → `(filled by plan 27-03)`; `### Counterweight triggers (measured)` → `(filled by plan 27-03)`; `### Not changed, and why` → `(filled by plan 27-03)`; `### DR checklist — TUNE-07` → `(filled by plan 27-04)`; `### Verdict (TUNE-07)` → `**Overall verdict:** (to be filled by the tester — tuned / tune-again / deferred)`.

    Voice: ledger prose is plain and factual (no player-facing copy here). Commit: `docs(27-01): v1.2 retune band recorded before tuning — D-09 superseded, Dante decision rule, iteration protocol`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/DIFFICULTY-RETUNE.md','utf8');const h=[...d.matchAll(/^## .+$/gm)].map(m=>m[0]);const last=h[h.length-1];if(!last.startsWith('## v1.2 retune (Phase 27)'))throw new Error('v1.2 section must be the last H2: '+last);for(const s of ['5-6','1.0-2.0','3.0-5.0','p50 >= 1','SUPERSEDED','Level 20, way overtuned','rollups.pooled','(filled by plan 27-02)','(filled by plan 27-03)','(filled by plan 27-04)','to be filled by the tester'])if(!d.includes(s))throw new Error('missing: '+s);if(!/\r\n/.test(d))throw new Error('CRLF lost');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify script prints `ok`.
    - `git diff --stat HEAD~1 -- docs/DIFFICULTY-RETUNE.md` shows only insertions (no deletions above the new H2); `git diff --quiet HEAD~1 -- engine content src mazeworld.html tools test` exits 0 for this commit.
    - `git log -1 --format=%s` starts with `docs(27-01)`.
  </acceptance_criteria>
  <done>The band, its measurement paths, the superseding of D-09, the verbatim v1.1 verdict, the levers, the Dante decision rule with the calibration numbers, and the iteration protocol are on disk and committed before any constant or bestiary change exists in the tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive pooled readout to the class-matrix harness — per-cell reach20, pooledSummary(cellRows), rollups.pooled, a POOLED text block — with tests; bot and Bot line untouched</name>
  <files>tools/lib/class-matrix.mjs, tools/tune-classes.mjs, test/unit/class-matrix.test.js</files>
  <read_first>
    - tools/lib/class-matrix.mjs in full: the header JSON-shape contract, `summarizeRows` (the empty-completed null branch and the `reachPct` closure), `rollups`/`groupAndSummarize`, `buildReport`, `rowValues`/`BASE_HEADERS`/`DEEP_HEADERS`, `formatText` (the Bot line must stay the LAST line — "the ledger greps this exact line")
    - tools/tune-classes.mjs header comment (JSON SHAPE block) — update it to match; nothing else in that file changes
    - test/unit/class-matrix.test.js: the summarizeRows null-safety test (95), rollups test (153), buildReport/formatText test (172 — `text.trim().endsWith(report.meta.bot)` and the no-"fun"/"strong"/"weak" scan), purity test (217)
    - tools/class-pass-diff.mjs `pooledReach` and `buildVerdicts` — read-only; confirm it ignores unknown cell keys and unknown rollups keys (it reads byClass/bySub/byRace only), so the Phase 26 JSONs and the ledger test are unaffected
  </read_first>
  <action>
    In `tools/lib/class-matrix.mjs`:
    - `summarizeRows(rows)`: add `reach20` — in the zero-completed branch `reach20: null` right after `reach10: null`; in the normal branch `reach20: reachPct(20)` right after `reach10`. Same rounding as reach5/reach10 (one decimal, percentage of completed runs with `deathDepth >= 20`).
    - New export `pooledSummary(cellRows)`: returns `{ key: "ALL", ...summarizeRows(cellRows.flatMap(({ rows }) => rows)) }` — the harness's own summarizer over the concatenation of every cell's rows, so every pooled metric (p50Depth, p90Depth, reach5/10/20, meanEncountersSurvived, p50FloorsGained, meanFloorsGained, topCauses...) is run-weighted by construction and null-not-NaN when nothing completed. JSDoc: this is the band's measurement seam (docs/DIFFICULTY-RETUNE.md `## v1.2 retune (Phase 27)`); Phase 26's caveat that the JSON carried no overall median / reach >= 20 is closed here; additive — no play, no draw, no Bot-line change.
    - `rollups(cellRows)`: add `pooled: pooledSummary(cellRows)` as a fourth key (byClass, bySub, byRace, pooled).
    - `formatText(report)`: after the BY RACE table and its blank line, push a `POOLED (all cells, run-weighted over completed runs):` line followed by a header row `n  stuck  mean  p50  p90  >=5%  >=10%  >=20%  kills  lvl  actions` (+ `gained(mean)  gained(p50)  survived` when `deep`) and one value row for `ru.pooled` (reuse `fmt`, reach values `.toFixed(1)` or `n/a`), then a blank line. Do NOT add a `>=20%` column to `BASE_HEADERS`/`rowValues` — the existing ranked and roll-up tables keep their Phase 26 columns so transcripts stay visually comparable. The Stuck line and `meta.bot` remain the last two lines. The block must not contain the words "fun", "strong" or "weak" (the existing test scans for them).
    - Header comment: document `reach20` in the cells shape and `pooled` in rollups (`rollups: { byClass, bySub, byRace, pooled }`), and add one sentence to the "no timing field" paragraph that `pooled` is Phase 27's band readout.
    In `tools/tune-classes.mjs`: update only the JSON SHAPE header comment (cells gain `reach20`; rollups gain `pooled`). No code change.
    In `test/unit/class-matrix.test.js` add tests: (1) `summarizeRows: reach20 is null when all rows are stuck, 0.0 when no completed run reaches 20, and counts a run at exactly deathDepth 20` (synthetic rows; one at 20, one at 19, one at 25); (2) `pooledSummary: run-weighted over cells — the pooled p50Depth is the median over ALL rows, not the mean of per-cell p50s` (cell A with 1 completed row at depth 9, cell B with 3 completed rows at depths 1, 1, 2 → pooled p50Depth 1 (or the harness's own percentile of [1,1,2,9] — assert against `summarizeRows` applied to the concatenation, and assert it differs from the mean of the two cells' p50s), `key === "ALL"`, `n === 4`, and an all-stuck pair yields `p50Depth === null` with no NaN anywhere (walk the object); (3) `buildReport: rollups.pooled present with key ALL; formatText contains a POOLED block after BY RACE, still ends with the Bot line, still has no verdict words` (extend the existing buildReport test or add a sibling). Keep the purity test green (no new imports).
    Then prove Phase 26 is untouched: `node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json` exits 0 and `node --test test/unit/class-pass-ledger.test.js test/unit/class-pass-diff.test.js` is green; `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0; `node tools/tune-classes.mjs --sub Knight --race Human --seeds 2 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const r=JSON.parse(s);if(r.rollups.pooled.key!=='ALL'||typeof r.cells[0].reach20!=='number')throw new Error('pooled missing');console.log('pooled ok', r.meta.bot)})"` prints `pooled ok` and the Bot line in the Phase 26 format (`Bot: exploreBudget=50  maxActions=5000 ...`). Commit: `feat(27-01): class-matrix pooled readout — reach20 + rollups.pooled for the v1.2 band (bot untouched)`.
  </action>
  <verify>
    <automated>node --test test/unit/class-matrix.test.js test/unit/tuning-bot.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-ledger.test.js && node tools/class-pass-diff.mjs --gate --after docs/class-pass/after.json && git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/class-matrix.test.js` exits 0 with at least 3 more passing tests than before this task.
    - `node tools/tune-classes.mjs --sub Knight --race Human --seeds 2 --json` output parses, has `rollups.pooled.key === "ALL"`, numeric `cells[0].reach20`, and a `meta.bot` string that starts with `Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5(caster)  potion<0.5  camp<0.5`.
    - `node tools/tune-classes.mjs --sub Knight --race Human --seeds 2` (text) ends with the Bot line and contains a `POOLED (all cells, run-weighted over completed runs):` block.
    - `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0; `git diff --quiet HEAD~1 -- tools/class-pass-diff.mjs docs/class-pass engine content src mazeworld.html test/parity` exits 0.
  </acceptance_criteria>
  <done>The band's pooled measures (overall median, reach >= 20, forced-20 pooled encounters/floors incl. p50) are emitted by the harness itself, run-weighted and null-safe, without touching the bot, the Bot line, Phase 26's artifacts or the class-pass ledger guard.</done>
</task>

<task type="auto">
  <name>Task 3: Write test/unit/difficulty-retune-ledger.test.js — the structural standing guard for the v1.2 section</name>
  <files>test/unit/difficulty-retune-ledger.test.js</files>
  <read_first>
    - test/unit/class-pass-ledger.test.js — the `h2s`/`section` doc-parsing helpers and the "structure and provenance, never a balance number" discipline (mirror the style; copy the two helpers)
    - docs/DIFFICULTY-RETUNE.md `## v1.2 retune (Phase 27) — TUNE-05..07` as written in Task 1
  </read_first>
  <action>
    Create `test/unit/difficulty-retune-ledger.test.js` (node:test + node:assert/strict, reads `docs/DIFFICULTY-RETUNE.md` relative to the repo root, normalises CRLF to LF before matching). Tests: (1) `v1.2 section: the H2 "## v1.2 retune (Phase 27)" appears exactly once and is the LAST H2 of the ledger`; (2) `band table: the seven measure rows are present with their target numbers` — assert the section text contains, each at least once: `5-6`, `1.0-2.0`, `3.0-5.0`, `p50 >= 1`, `Reach >= 20`, `10-20`, `hard gate`, `rollups.pooled.p50Depth`, `rollups.pooled.reach20`, `rollups.pooled.meanEncountersSurvived`, `rollups.pooled.p50FloorsGained`, `--start-depth=35`, `--start-depth=50`, `class-pass-diff.mjs --gate`; (3) `D-09 is marked superseded inside the v1.2 section` (contains `SUPERSEDED` and `D-09`); (4) `the v1.1 verdict is quoted verbatim` (contains `"Level 20, way overtuned. It's instant death on any combat."`); (5) `sub-headings present in the fixed order` — the H3 list from Task 1 items 1-8 (Why again → Standing rules → Target band → Bot proxy → BEFORE → Levers → Iteration protocol → Dante demotion — landed → Change table → Iteration log → AFTER readouts → Comparison vs band → Counterweight triggers → Not changed → DR checklist → Verdict), asserted by index order of first occurrence; (6) `the two Phase 26 Bot lines are quoted verbatim` (both strings from Task 1 item 4 present). Do NOT assert the absence of `(filled by plan ...)` placeholders — 27-02/27-03/27-04 fill them and 27-03 extends this file with the AFTER provenance checks. Never assert a balance outcome (no AFTER number). Run `npm test` and record the pass count (expect 1421 + Task 2's tests + these 6). Commit: `test(27-01): difficulty-retune ledger standing guard — v1.2 section, band numbers, D-09 superseded`.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js && npm test 2>&1 | tail -8 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/difficulty-retune-ledger.test.js` exits 0 with 6 passing tests.
    - `npm test` ends `# fail 0` and `# pass` >= 1430.
    - `git status --porcelain` is empty after the commit; `git diff --quiet HEAD~3 -- engine content src mazeworld.html test/parity tools/lib/tuning-bot.mjs tools/class-pass-diff.mjs docs/class-pass` exits 0 (nothing tuned, nothing captured, Phase 26 untouched).
  </acceptance_criteria>
  <done>The band section cannot silently drift: heading placement, the band numbers, the superseded marker, the verbatim verdict, the Bot lines and the sub-heading order are guarded by a named test that runs in milliseconds; the full suite is green and the tree is clean for 27-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ledger prose -> later plans | 27-02/27-03 read the band and the Dante rule from this section; a mistyped edge or a missing "how measured" path would be tuned against |
| harness JSON -> band evaluation | `rollups.pooled` is the single source for the band's pooled numbers; a non-run-weighted or NaN-prone pooled block would mis-evaluate the band |
| readout change -> Phase 26 artifacts | tools/lib/class-matrix.mjs is shared with the Phase 26 ledger guard; a Bot-line or renderer change would break comparability |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | band edges (open vs closed, precision) | high | mitigate | edge rule column per row + precision sentence; guard test pins the numbers; D-09 superseded in the same section so no second band exists |
| T-27-02 | Tampering | pooled numbers derived by hand | high | mitigate | `pooledSummary` reuses `summarizeRows` over concatenated rows; run-weighting + null-not-NaN unit-tested; ledger names the JSON paths |
| T-27-03 | Repudiation | bot parameters drift between Phase 26 and the AFTER | high | mitigate | tuning-bot.mjs byte-identical to 5565b22 (git diff gate); Bot lines quoted verbatim in the ledger and asserted by the guard test; botLine untouched |
| T-27-04 | Tampering | constants or bestiary edited before the band is recorded | medium | mitigate | this plan touches no engine/content file (git diff gate per commit); 27-02 depends_on 27-01 |
| T-27-05 | Denial of Service | Phase 26 ledger guard broken by the readout change | medium | mitigate | class-pass-diff.mjs untouched; class-pass-ledger + class-pass-diff tests run in Task 2's verify |
| T-27-06 | Information Disclosure | tools/test shipping in the www bundle | low | accept | tools/ and test/ are never copied by tools/build-www.mjs (existing project rule) |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero deps) |
</threat_model>

<verification>
- `node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-matrix.test.js test/unit/class-pass-ledger.test.js` green; `npm test` ends `# fail 0` with >= 1430 passing.
- `docs/DIFFICULTY-RETUNE.md`'s last H2 is the v1.2 section; it contains the band table, the superseded sentence, the verbatim verdict, the Dante decision rule and the iteration protocol; CRLF preserved.
- `git diff --quiet 5565b22 -- tools/lib/tuning-bot.mjs` exits 0; no engine/content/src/fixture change in any of this plan's three commits.
</verification>

<success_criteria>
- TUNE-05 satisfied: the band is agreed (CONTEXT.md) and recorded in the ledger with measurement paths and closed edges before any tuning commit exists; D-09 is superseded in the same section.
- 27-02 can start from a committed tree where the Dante decision rule and the iteration protocol are already written, and 27-03 can evaluate every band row directly from `rollups.pooled` of its AFTER JSONs.
</success_criteria>

<output>
Create `.planning/phases/27-delve-to-death-retune/27-01-SUMMARY.md` when done. Record: the three commit hashes, the `npm test` pass count, the exact H3 list as written, and a note that no engine/content/bestiary file changed.
</output>
