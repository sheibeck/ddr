---
phase: 04-mobile-presentation-controls-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/settings.js
  - test/unit/settings.test.js
  - test/unit/textScale.test.js
  - test/unit/confirmQuit.test.js
autonomous: true
requirements: [UX-07, UX-08, UX-01]
must_haves:
  truths:
    - "All 6 settings (sound, haptics, textSize, controlScheme, confirmBeforeQuit, diceMode) round-trip through window.mzStorage and read back with correct defaults when unset"
    - "textScaleForSize maps S/M/L to 0.85/1.0/1.25 and clampTextScale bounds any OS font-scale input to [0.85, 1.25]"
    - "shouldConfirmQuit returns true only when confirmBeforeQuit is ON (gates the Sheet's Cut Losses action)"
    - "controlScheme persists as 'tap'|'dpad' (default 'tap'); diceMode persists as 'on tap'|'always'|'never' (default 'on tap')"
  artifacts:
    - src/browser/settings.js
    - test/unit/settings.test.js
    - test/unit/textScale.test.js
    - test/unit/confirmQuit.test.js
  key_links:
    - "settings.js is the ONLY writer of the settings keys — all persistence goes through window.mzStorage (per-key write queue), never raw localStorage/Preferences (storage.js data-integrity contract)"
---

<objective>
Deliver the DOM-free settings module: the persisted schema for all six UX-07
fields plus the pure helpers for UX-08 text-scaling and the confirm-before-quit
gate. Fully testable now (04-VALIDATION.md Wave 0), independent of any DOM. The
Wave-6 settings screen (04-09) renders/writes through this module; the Wave-2
shell (04-05) reads textSize to set the root `--mw-text-scale` custom property.

Purpose: One source of truth for settings + scaling math, routed exclusively
through window.mzStorage (04-RESEARCH.md Security Domain: no raw localStorage).
Output: `src/browser/settings.js` and its three test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-UI-SPEC.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@src/browser/storage.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings schema + read/write round-trip over window.mzStorage</name>
  <files>src/browser/settings.js, test/unit/settings.test.js</files>
  <read_first>
    - src/browser/storage.js exports (getItem async, setItem, removeItem, flush async) and its header on the per-key write-queue / dual-write-hazard fix
    - test/unit/engineAdapter.test.js lines 1-60 (withFakeLocalStorage pattern — how node:test stubs globalThis.localStorage so window.mzStorage's browser branch works headlessly)
    - 04-UI-SPEC.md "Text-size setting" + "Controls & Interaction" (diceMode 'on tap'|'always'|'never' default 'on tap'; controlScheme 'tap'|'dpad' default 'tap')
    - 04-CONTEXT.md Settings decision (the 5 required + diceMode; fog-of-war NOT user-facing; cellSize folded into text-size)
  </read_first>
  <behavior>
    - SETTINGS_DEFAULTS = { sound:true, haptics:true, textSize:'M', controlScheme:'tap', confirmBeforeQuit:true, diceMode:'on tap' }.
    - readSettings() awaits window.mzStorage.getItem for each key under a single namespaced key (e.g. 'mazeworld.settings.v1' as one JSON blob) and returns defaults merged over any persisted partial; a missing/corrupt blob yields full defaults (never throws).
    - writeSetting(key, value) validates the value against the allowed set for that key (invalid → no-op, keeps prior/default) and persists the merged blob via window.mzStorage.setItem.
    - controlScheme persistence (UX-01) and diceMode both covered here.
    - Test: default read when unset; each field round-trips; invalid value rejected; corrupt JSON → defaults; writes go through window.mzStorage not raw localStorage.
  </behavior>
  <action>
    Create src/browser/settings.js exporting SETTINGS_DEFAULTS, readSettings(), writeSetting(key,value), and a settings storage-key constant. Persist all six fields as ONE JSON object under a single versioned key via window.mzStorage (get/set), matching engineAdapter.js's fail-open posture (a blocked/private store just falls back to defaults; never throw). Validate each field against its allowed set before writing. Do NOT touch raw localStorage or @capacitor/preferences directly — only window.mzStorage (data-integrity threat, 04-RESEARCH.md). Write test/unit/settings.test.js FIRST using the withFakeLocalStorage pattern from engineAdapter.test.js, awaiting flush() before inspecting the backing store; RED then GREEN. Include a haptics-call injection seam note: the settings module exposes the persisted haptics boolean; the actual Haptics.impact() call-assertion test lives with the 04-09 wiring, but assert here that readSettings().haptics round-trips.
  </action>
  <verify>
    <automated>node --test test/unit/settings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/settings.test.js` passes: unset→defaults; all 6 fields round-trip; invalid value rejected; corrupt blob→defaults.
    - `grep -n "localStorage" src/browser/settings.js` returns nothing (persistence only via window.mzStorage).
    - controlScheme and diceMode defaults are 'tap' and 'on tap'.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Text-scale clamping + confirm-quit gate (pure)</name>
  <files>src/browser/settings.js, test/unit/textScale.test.js, test/unit/confirmQuit.test.js</files>
  <read_first>
    - 04-UI-SPEC.md Typography ("S=0.85× M=1.0× L=1.25×", one root multiplier) and Accessibility Contract ("OS font-scale honoring, clamped to the same 0.85×–1.25× bound")
    - 04-UI-SPEC.md "Confirm-before-quit" (gates the Sheet's CUT LOSSES · ROLL ANOTHER; RUN AWAY in combat is explicitly NOT gated)
    - src/browser/nativeChrome.js decideBackAction (the pure-gate pattern to mirror)
  </read_first>
  <behavior>
    - textScaleForSize('S'|'M'|'L') → 0.85|1.0|1.25; unknown → 1.0.
    - clampTextScale(osScale) clamps any finite number to [0.85, 1.25]; non-finite → 1.0.
    - effectiveTextScale(size, osScale) multiplies textScaleForSize(size) by a clamped OS factor but the FINAL product is itself clamped to [0.85, 1.25] so an extreme OS setting can't break the pixel-art layout.
    - shouldConfirmQuit({confirmBeforeQuit}) → true only when confirmBeforeQuit is truthy (used to gate Cut Losses); pure, no side effects.
    - Test: three sizes; OS extremes clamp; non-finite→1.0; confirm gate true/false.
  </behavior>
  <action>
    Extend src/browser/settings.js with pure exports textScaleForSize, clampTextScale, effectiveTextScale, and shouldConfirmQuit — mirroring nativeChrome.js#decideBackAction's pure-function posture (no DOM, no storage). Write test/unit/textScale.test.js (scale mapping + OS-scale clamping) and test/unit/confirmQuit.test.js (gate honors the setting) FIRST (RED), then implement to GREEN. Note in the header that RUN AWAY (combat) is deliberately never routed through shouldConfirmQuit — its mis-tap protection is isolation/sizing/color only (04-UI-SPEC.md).
  </action>
  <verify>
    <automated>node --test test/unit/textScale.test.js test/unit/confirmQuit.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/textScale.test.js` passes: S/M/L→0.85/1.0/1.25; OS extremes clamp to [0.85,1.25]; non-finite→1.0.
    - `node --test test/unit/confirmQuit.test.js` passes: gate returns true only when confirmBeforeQuit is ON.
    - effectiveTextScale never returns a value outside [0.85, 1.25].
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| settings write → persisted store | Settings values are user-controlled and must be validated + routed through the single mzStorage seam |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | settings persistence bypassing window.mzStorage's write-queue | medium | mitigate | settings.js writes ONLY through window.mzStorage.get/setItem (never raw localStorage/Preferences), preserving storage.js's per-key ordering + the dual-write-hazard fix (04-RESEARCH.md data-integrity threat). |
| T-04-04 | Tampering | invalid/adversarial setting value (e.g. textSize='XL') | low | mitigate | writeSetting validates every value against its allowed set; effectiveTextScale clamps to [0.85,1.25] so no setting value can break the fixed pixel-art chrome. |
</threat_model>

<verification>
- `npm run test:quick` green; 372 existing tests stay green.
- Settings persistence exercised via the withFakeLocalStorage harness (headless).
</verification>

<success_criteria>
- settings.js provides the full six-field schema + scaling/confirm helpers, all tested.
- Zero raw localStorage/Preferences access; everything via window.mzStorage.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-02-SUMMARY.md` when done.
</output>
