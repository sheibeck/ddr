---
phase: 04-mobile-presentation-controls-onboarding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/viewModels.js
  - src/browser/tutorial.js
  - src/browser/icons.js
  - test/unit/characterSheetViewModel.test.js
  - test/unit/oracleLogViewModel.test.js
  - test/unit/tutorial.test.js
autonomous: true
requirements: [UX-04, UX-05, UX-06]
must_haves:
  truths:
    - "characterSheetViewModel(state) binds every UI-SPEC HERO row (TO STRIKE, TO HIT, DAMAGE, ARMOR, INTELLIGENCE, SKILL POINTS, NEXT LEVEL, UPKEEP, WIN POTENTIAL, LVL, name, class/race/sub, quirk, skills) to the REAL GameState.c + engine/derived.js, not the mockup's placeholder shape"
    - "oracleLogViewModel(entries, diceMode) returns reverse-chronological rows with dice-reveal gating that matches diceMode ('on tap' hidden-until-tapped, 'always' shown, 'never' omitted)"
    - "the coach-mark sequencer advances/dismisses across the fixed step set and reports tutorialSeen; step copy is capped to the 'no wall of text' length"
    - "icons.js maps every engine feature cell to one of the 9 PNG keys and preloads them fail-open before first paint"
  artifacts:
    - src/browser/viewModels.js
    - src/browser/tutorial.js
    - src/browser/icons.js
    - test/unit/characterSheetViewModel.test.js
    - test/unit/oracleLogViewModel.test.js
    - test/unit/tutorial.test.js
  key_links:
    - "view-models bind to engine/derived.js (strikeDie/toHit/weaponDamage/upkeep/levelFromSP) + GameState.c — a wrong field binding silently shows the mockup's placeholder shape (04-RESEARCH.md Pitfall 1)"
---

<objective>
Deliver the DOM-free view-models and helpers the Wave-5 screens (04-08) and
Wave-7 tutorial (04-10) render: the character-sheet view-model (UX-04), the
Oracle-log view-model with dice-reveal gating (UX-05), the coach-mark sequencer
(UX-06), and the feature-icon loader/mapper (UX-03/UX-08 canvas icons). All pure
or DOM-injectable, tested headlessly now (04-VALIDATION.md Wave 0). Every binding
reads the REAL engine state, never the design mockup's throwaway shape.

Purpose: Lock the engine→screen data contract as tested pure functions before any
DOM exists, so the Wave-5 screens are thin renderers over proven view-models.
Output: `src/browser/viewModels.js`, `src/browser/tutorial.js`, `src/browser/icons.js` + tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-UI-SPEC.md
@.planning/phases/04-mobile-presentation-controls-onboarding/04-RESEARCH.md
@engine/derived.js
@engine/character.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Character-sheet view-model bound to real GameState (UX-04)</name>
  <files>src/browser/viewModels.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - engine/character.js rollCharacter() lines 107-163 (the REAL c fields: cls, sub, race, intel, level, sp, maxWP, wp, skills, weapon, prof, magicWpn, armor, ar, armorMin, armorWP, patches, temperament, motive, phobia, potions, rations, gold, scrolls, items, grimoire, kills, name)
    - engine/derived.js (strikeDie(c), toHit(state), weaponDamage(c,rng) — note rng dependency, upkeep(c), levelFromSP(sp), skill/skillTier/eff, THRESHOLDS via levelFromSP)
    - 04-UI-SPEC.md Screen 2 "THE DOOMED" (rows: TO STRIKE, TO HIT, DAMAGE, ARMOR, INTELLIGENCE, SKILL POINTS, NEXT LEVEL, UPKEEP; WIN POTENTIAL bar; LVL badge; name; class/race/sub line; quirk box; SKILLS blocks)
  </read_first>
  <behavior>
    - characterSheetViewModel(state) returns a flat, render-ready object: name, level, classLabel, raceLabel, subLabel, snarkLine (from temperament/motive/phobia — tone reference only, static assembly), stats[] as {key,label,value} for the 8 rows, winPotential {value,max,pct}, quirk {label,text}, skills[] as {name,description,tier}.
    - Damage row: since weaponDamage needs an rng, the sheet shows a deterministic damage descriptor derived WITHOUT drawing from GameState.rngState (e.g. the weapon's dice notation + flat modifiers, or a min–max range) so rendering the sheet NEVER mutates rngState (parity/determinism must stay green).
    - NEXT LEVEL uses THRESHOLDS/levelFromSP to show sp-to-next; ARMOR from c.armor/ar; TO STRIKE from strikeDie(c); TO HIT from toHit(state); UPKEEP from upkeep(c); INTELLIGENCE from c.intel.
    - Test: build a state via engine newRun(fixedSeed); assert every row binds to the real field/derived value; assert calling the view-model does NOT change state.rngState (deep-equal before/after).
  </behavior>
  <action>
    Create src/browser/viewModels.js exporting characterSheetViewModel(state). Bind each UI-SPEC HERO row to the real c field or engine/derived.js helper — per 04-RESEARCH.md Pitfall 1, NEVER use the mockup's placeholder field names. Critically, the DAMAGE row must be rendered as a static descriptor (weapon dice notation + level²/prof/magicWpn/race modifiers as a range or formula string) that reads only c — do NOT call weaponDamage() with the live rng from GameState, because that would advance rngState off-band and break determinism/parity (UI-only-never-mutate-rngState contract, 04-CONTEXT.md). Write test/unit/characterSheetViewModel.test.js FIRST driving engine newRun(seed), asserting field bindings AND rngState invariance (structuredClone before → deep-equal after), RED then GREEN.
  </action>
  <verify>
    <automated>node --test test/unit/characterSheetViewModel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/characterSheetViewModel.test.js` passes: all 8 stat rows + WIN POTENTIAL + name/level/class/race/sub + quirk + skills bind to real values from a seeded newRun state.
    - Test asserts state.rngState is byte-identical before and after calling the view-model.
    - `grep -n "\.wp\b\|s\.ch\|s\.pos\|s\.feats" src/browser/viewModels.js` shows no mockup-shape field reads (bindings use GameState.c / derived.js).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Oracle-log view-model with dice-reveal gating (UX-05)</name>
  <files>src/browser/viewModels.js, test/unit/oracleLogViewModel.test.js</files>
  <read_first>
    - 04-UI-SPEC.md Screen 5 "THE ORACLE" + "Tappable log line" (chevron + narration + conditionally-revealed roll detail) + "Dice transparency (diceMode)" ('on tap' default hidden-until-tapped; 'always' inline; 'never' hidden)
    - src/browser/engineAdapter.js formatEvents/formatEvent (the narration line + any roll spans like class="roll")
  </read_first>
  <behavior>
    - oracleLogViewModel(entries, diceMode) returns rows newest-first, each {narration, roll, revealable, revealedByDefault}: 'on tap' → revealable:true, revealedByDefault:false; 'always' → revealedByDefault:true; 'never' → roll omitted/null, revealable:false.
    - Reverse-chronological ordering regardless of input order (view-model sorts/reverses by entry sequence).
    - Empty/one/many all render; a roll-less narration line renders with no reveal affordance.
    - Test: three diceMode values × mixed entries (some with roll detail, some without); assert ordering + gating flags.
  </behavior>
  <action>
    Extend src/browser/viewModels.js with oracleLogViewModel(entries, diceMode). Take the log entries the adapter accumulates (narration text + optional roll-detail) and produce reverse-chronological render rows with dice-reveal flags per the diceMode contract. Keep it pure (no DOM). The combat log reuses the SAME function (04-UI-SPEC.md: gating "applies uniformly to the Oracle log AND the combat log"). Write test/unit/oracleLogViewModel.test.js FIRST (RED) covering all three diceMode values and roll-present/absent rows, then GREEN.
  </action>
  <verify>
    <automated>node --test test/unit/oracleLogViewModel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/oracleLogViewModel.test.js` passes: newest-first order; 'on tap'→revealable&hidden; 'always'→revealed; 'never'→no roll; roll-less lines have no reveal affordance.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Coach-mark sequencer + feature-icon loader (UX-06, UX-03 icons)</name>
  <files>src/browser/tutorial.js, src/browser/icons.js, test/unit/tutorial.test.js</files>
  <read_first>
    - 04-UI-SPEC.md "Copywriting Contract" tone reference + "long-text Tutorial coach-mark copy (capped 1–2 short lines)"; 04-CONTEXT.md tutorial decision (first-run coach-marks: move here / that mark is a trap / descend there / you're starving; dismissible; shown once; persisted mazeworld.tutorialSeen)
    - 04-RESEARCH.md §"Don't Hand-Roll" coach-mark row (position:fixed overlay + clip-path; ~4 fixed steps — no library) and Code Example #3 (preloadIcons + drawFeatureIcon + FEATURE_ICONS)
    - STATE.md "Provided Assets" icon→feature mapping (chest→chest, crevice→gorge/climb, descent→exit, encounter→dot, onewaydoor→one-way door, party→player, teleport→tele, trap→trap, wall→climbable wall)
    - mazeworld.html draw() lines 1388-1430 (the engine feat keys actually drawn: "dot","tele","one","trap","chest","climb","gorge","exit"/"gate")
  </read_first>
  <behavior>
    - tutorial.js: COACH_MARK_STEPS is a fixed array (~4) of {id, target, copy} where copy is ≤2 short lines; makeTutorialSequencer(steps) exposes current()/next()/dismiss()/isComplete(); tutorialSeen persistence via window.mzStorage (mazeworld.tutorialSeen) with async read + a setSeen() writer, fail-open.
    - icons.js: FEATURE_ICONS is the 9 keys; featureKeyForCell(cell) maps an engine feat value to a PNG key (dot→encounter, tele→teleport, one→onewaydoor, trap→trap, chest→chest, climb→wall, gorge→crevice, exit/gate→descent) and the player marker→party; preloadIcons(basePath) returns a promise of {key:Image} that fail-opens on error (a missing icon just won't draw, never blocks boot); drawFeatureIcon(ctx,img,dx,dy,size) draws at Math.round(cellSize*0.6) centered, in CSS-px space (DPR already absorbed).
    - Test (tutorial only — icons.js Image/canvas parts are DOM and deferred to device-UAT): step advance/dismiss/complete; tutorialSeen round-trip via withFakeLocalStorage; each step copy length within the cap.
  </behavior>
  <action>
    Create src/browser/tutorial.js (pure sequencer + mzStorage-backed tutorialSeen flag) and src/browser/icons.js (feature→PNG-key map, preloader, draw helper). Map every engine feat key drawn in mazeworld.html draw() to one of the 9 provided PNG icons per the STATE.md provided-assets mapping; the player marker uses party.png. Keep the coach-mark copy within the "no wall of text" cap (assert a max character/line count in the test). icons.js's Image()/drawImage parts are browser-only (guard/inject so node:test never constructs a real Image) — the tutorial sequencer + tutorialSeen and featureKeyForCell mapping ARE unit-tested. Write test/unit/tutorial.test.js FIRST (RED): sequencer transitions, tutorialSeen round-trip (withFakeLocalStorage), and step-copy length cap; then GREEN. Also assert featureKeyForCell mapping for all engine feat keys in this test file (pure, DOM-free).
  </action>
  <verify>
    <automated>node --test test/unit/tutorial.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/tutorial.test.js` passes: sequencer advance/dismiss/isComplete; tutorialSeen persists+reads via window.mzStorage; every COACH_MARK_STEPS copy string is within the length cap; featureKeyForCell maps dot/tele/one/trap/chest/climb/gorge/exit/gate to the correct one of the 9 PNG keys.
    - icons.js does not construct a real Image() at module top level (browser-only paths are lazy/injected) so importing it under node:test does not throw.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green (372 existing + new).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| tutorialSeen / persisted flags → store | Tutorial + icon state must not bypass the mzStorage seam or read the mockup's placeholder state shape |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Tampering | view-model binding to mockup placeholder fields instead of real GameState | high | mitigate | characterSheetViewModel binds strictly to GameState.c + engine/derived.js; a test asserts real-field values from a seeded newRun. Prevents shipping the mockup's throwaway shape (04-RESEARCH.md Pitfall 1). |
| T-04-06 | Tampering | sheet render advancing rngState off-band via weaponDamage(rng) | high | mitigate | DAMAGE row rendered as a static descriptor from c only; test asserts rngState byte-identical before/after. Keeps determinism/parity (372 tests) green. |
| T-04-07 | Tampering | tutorialSeen written outside window.mzStorage | low | mitigate | tutorial.js persists only through window.mzStorage per-key queue. |
</threat_model>

<verification>
- `npm run test:quick` green; 372 existing tests stay green (especially determinism/roundtrip/parity — proving the sheet view-model never touches rngState).
</verification>

<success_criteria>
- Three pure/injectable modules exist with passing tests; every screen data contract is locked and engine-bound before the DOM is built.
</success_criteria>

<output>
Create `.planning/phases/04-mobile-presentation-controls-onboarding/04-03-SUMMARY.md` when done.
</output>
