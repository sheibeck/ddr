---
phase: "04.2"
name: Bug Fixes & Text Polish
status: complete
completed: 2026-09-09
tests: 567/567 (parity 25/25)
deployed: Pixel 7 (com.darktierstudios.delvedierepeat)
executed_as: quick-task batches (not per-plan PLAN/SUMMARY files) — inventory in .planning/quick/20260909-rules-text-audit-pass/
---

# Phase 04.2: Bug Fixes & Text Polish — SUMMARY

**Complete + deployed 2026-09-09.** The DR14 device-review batch (rules-text audit + live-play bugs), consolidated into one GSD phase per user direction and executed as sequential executor batches (file-ownership serialized on `engine/combat.js` / `mazeworld.html`). Full fix inventory + evidence: `.planning/quick/20260909-rules-text-audit-pass/` (`FINDINGS.md`, `EXTRA-SCOPE.md` E1–E12, `SUMMARY-batch1.md`).

## Batches (all built + deployed)
- **Batch 1 (rules)** → 545/545 — A2 flying charge/cooldown, A3 rest cure-roll, A1 engine affliction `kind`.
- **Bugs A** → 549/549 — E5 poison-at-1hp auto-clear, E6 clear Oracle on new character, E4 foe HP current/max (defensive `maxWP` in `encounterStarted`).
- **Bugs B** → 554/554 — E7 Con-Artist opener no longer fakes "Critical" (now a *sarcastic* no-damage beat; user directive: lean into humor when class/race works against the player), E8 Cloak-of-Armor soaks as Plate (`armorSoak(c)`, magical/non-degrading), E9 Dead tab re-fetches storage on open.
- **Graveyard Rework (E12)** → 564/564 — cap graveyard at last 5 shown/stored, non-clearable running total of all dead, clear button removed, no name reuse over last ~25 (determinism-safe: one seeded `rng.pick` + no-rng forward-walk past excluded names; empty exclusion is byte-identical → no parity carve-out). Storage keys `ddr.graveyard.total.v1`, `ddr.graveyard.names.v1`.
- **Text** → 567/567 — A1 narration + Disease→Ailment bucket, E1 dup Teleport→`-10 HP`, E3 SP→XP/WP→HP (dual-purpose strings changed atomically with their `encounterDot`/`tableFour`/`meetFaerie` parsers), E10 `goldGained` `(${why})` source-tag leak removed + reads "wilmst" + redundant `+3000` beat dropped, E11 `Skill pts`→XP gravestone label + epitaph editorial polish (3 lines in `content/epitaphs.js`), P1 jargon→prose + stripRollDetail scoping, P2 wm→wilmst, P3 cosmetics (partial; spellThrown/trapPoisoned/scroll* deliberately skipped, noted).

## Deferred out of this phase
- **E2** (use combat-usable items from the combat bar) → **Economy & Item Balancing** milestone (needs the same "use item" plumbing that milestone builds).
- **+3000 wilmst amount** (too generous — only its DISPLAY was fixed here) → Economy & Item Balancing §5.
- **Parley balance pass** (parley pays ~2.5× kill-XP at zero risk; Con-Artist 75%@L1; dead `parleyRefused` branch) → Monster Balancing candidate #5.

## Constraints honored
Engine stayed pure/deterministic; no state-field or event-type renames; dual-purpose table strings changed atomically with their parsers; no new rng draws in chargen (parity byte-identical); every new event type carries an `EVENT_NARRATION` entry; working tree left uncommitted for on-device review; no git operations run.
