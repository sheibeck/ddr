---
phase: 04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep
plan: 03
type: execute
wave: 2
depends_on: [04.1-01]
files_modified:
  - engine/economy.js
  - engine/encounters.js
  - src/browser/eventNarration.js
  - test/unit/economy.test.js
  - test/unit/encounters.test.js
autonomous: true
requirements: [RATION-01]
must_haves:
  truths:
    - "Buying food at the store heals HP only — the STORE_EFFECTS.eatRation effect no longer increments c.rations as a silent side effect — RATION-01"
    - "Rations are sold as their OWN store line item at a set gold price (race-adjusted via priceFor), with a dedicated effect that increments c.rations and pushes a player-visible ration-purchase event — RATION-01"
    - "The parallel dungeon-tile findFood path heals HP only and no longer silently adds a ration — the 'any food = +1 ration' rule is removed in BOTH economy.js and encounters.js together — RATION-01"
    - "Every ration change is surfaced to the player (the silent +1 was the confusion); the Cooking beast-carcass ration path in combat.js is left intact as an intended, narrated foraging reward"
  artifacts:
    - test/unit/economy.test.js
    - test/unit/encounters.test.js
  key_links:
    - "The store label string this plan rewrites was renamed wp→hp by 04.1-01; this plan depends on 04.1-01 to avoid a same-file merge conflict and to build on the renamed label"
    - "Any new ration-change event type MUST get an EVENT_NARRATION entry or test/unit/formatEventsCoverage.test.js fails the build — that guard is the surfacing guarantee"
---

<objective>
Resolve RATION-01 (user chose Option A): decouple ration acquisition from
HP-restoring food. (1) Store food purchases become PURE HP healing — drop the
silent c.rations++ from STORE_EFFECTS.eatRation. (2) Add Rations as their own store
line item at a set, race-adjusted gold price with a dedicated, narrated
buy-rations effect. (3) Apply the same decoupling to engine/encounters.js findFood
(HP heal only, no silent ration). (4) Surface every ration change to the player.
The Cooking beast-carcass ration path in combat.js stays as-is (intended reward).

Purpose: Rations are conceptually a food-supply resource; today every food
purchase piggybacks a hidden, unscaled +1 ration the player is never told about —
the exact confusion the CONTEXT calls out.
Output: pure-heal food, a dedicated visible Rations store line, decoupled
findFood, new narrated ration events, updated economy/encounters fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-CONTEXT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-RESEARCH.md
@engine/economy.js
@engine/encounters.js
@src/browser/eventNarration.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Food = pure HP heal + a dedicated Rations store line</name>
  <files>engine/economy.js, src/browser/eventNarration.js, test/unit/economy.test.js</files>
  <read_first>
    - engine/economy.js STORE_EFFECTS.eatRation (lines 39-43): currently `c.wp = min(maxWP, wp+params.wp); c.rations++` — the c.rations++ is the silent side effect to remove
    - engine/economy.js openStore stock generation (lines 76-134): the FOODS[0]/[1]/[5] `add(...)` loop (line 85), the priceFor(base, race) helper (lines 26-30) for race-adjusted pricing, and the `add(n, cost, effectId, effectParams, sub)` plain-data stock-entry shape (line 82-83). Note: `add(...)` consumes NO rng, so inserting a new stock entry does NOT shift RNG order (the premium item's rng.d(2)/rollBlade draws stay in the same sequence) — insert the Rations line among the food adds
    - engine/economy.js buyFrom (lines 154-169) — how STORE_EFFECTS[effectId] is dispatched and the `bought` event is pushed; a new effect follows the same (state, params, events) => void contract
    - src/browser/eventNarration.js EVENT_NARRATION table + the existing food/bought lines (foodFound ~242, bought/storeOpened) — add a narration entry for the new ration-purchase event in the deadpan tone; formatEventsCoverage will fail without it
    - test/unit/economy.test.js (lines 98-106): the eatRation test asserts `state.c.rations === 1` after buying food — this assertion INVERTS (food no longer changes rations); add coverage for the new Rations line and its ration gain
    - 04.1-CONTEXT.md RATION-01 (Option A) + 04.1-RESEARCH.md "the food/rations tangle"
  </read_first>
  <action>
    Edit STORE_EFFECTS.eatRation to heal HP only: keep `c.wp = Math.min(c.maxWP, c.wp + params.wp)` and REMOVE `c.rations++` — buying food is now pure healing with no ration side effect. Add a new plain-data store line item "Rations" in openStore at a set base gold price run through priceFor(base, race) for race consistency (choose a sensible fixed base, e.g. one ration for ~30 gold; document the chosen number in a comment as a deliberate value), carrying a new effectId (e.g. "buyRations") whose STORE_EFFECTS entry increments c.rations by the purchased amount and pushes a player-visible event (e.g. `{ type: "rationsBought", amount }`). Insert the Rations `add(...)` among the existing food adds (before the premium-item rng draws) so RNG order is untouched. Add the EVENT_NARRATION entry for the new rationsBought event in the deadpan tone (e.g. state the ration count gained explicitly, since surfacing the change is the whole point). Update test/unit/economy.test.js: assert that buying a food (eatRation) heals wp and leaves c.rations UNCHANGED; assert the store stock includes a Rations line whose purchase increments c.rations and emits rationsBought. Do NOT rename c.rations or any effect params key — only behavior and the new line change.
  </action>
  <verify>
    <automated>node --test test/unit/economy.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Buying food heals HP and does NOT change c.rations; a dedicated Rations store line increments c.rations and emits a narrated rationsBought event.
    - The new event type has EVENT_NARRATION coverage (formatEventsCoverage green).
    - RNG order in openStore is unchanged (premium-item parity intact); `npm run test:quick` stays green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Decouple findFood (dungeon tile) from rations — HP heal only</name>
  <files>engine/encounters.js, src/browser/eventNarration.js, test/unit/encounters.test.js</files>
  <read_first>
    - engine/encounters.js findFood (lines 255-263): currently `c.wp = min(maxWP, wp+f.wp); c.rations++; push foodFound` — remove the c.rations++ to mirror the store decoupling
    - src/browser/eventNarration.js foodFound narration (~line 242): "(+N wp)" (renamed to hp by 04.1-01) — confirm the line no longer implies a ration gain; adjust wording only if it references rations
    - engine/combat.js the Cooking beast-carcass path (lines ~345-354, the `cooked` event "+X wp, +1 ration") — DO NOT touch this; it is the intended, narrated foraging reward the CONTEXT says to keep
    - test/unit/encounters.test.js — the findFood test(s); assert HP heal and no ration change after this decoupling
  </read_first>
  <action>
    In engine/encounters.js findFood, remove `c.rations++` so a found food heals HP only (keep `c.wp = Math.min(c.maxWP, c.wp + f.wp)` and the foodFound event) — this removes the duplicated "any food = +1 ration" rule from the second acquisition path, in lockstep with Task 1's store change, exactly as the CONTEXT requires ("apply the same decoupling to findFood so the rule is removed in both places together"). Leave the Cooking beast-carcass ration path in engine/combat.js completely untouched (it stays an intended, narrated ration reward). If the foodFound narration text references a ration gain, reword it to reflect HP-only; otherwise leave it. Update test/unit/encounters.test.js: assert findFood heals wp (bounded by maxWP) and leaves c.rations UNCHANGED. Do not rename c.rations or any field.
  </action>
  <verify>
    <automated>node --test test/unit/encounters.test.js test/unit/combat.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - findFood heals HP and does NOT change c.rations; the Cooking beast-carcass ration path in combat.js is unchanged and still awards its ration.
    - The "any food = +1 ration" rule is gone from BOTH economy.js and encounters.js.
    - `npm test` full suite green (determinism/parity/roundtrip intact).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| store/tile effect → resource change | Ration changes must be explicit and player-visible, never silent |
| new stock entry → RNG order | Inserting a store line must not shift the seeded-rng consumption order |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-05 | Repudiation | a ration change happening without a visible event (the original silent-+1 confusion) | medium | mitigate | The Rations purchase emits a narrated rationsBought event; formatEventsCoverage forces narration; food paths no longer touch rations silently. |
| T-04.1-06 | Tampering | inserting the Rations add() before/among rng draws shifting openStore RNG order | high | mitigate | add() is pure (no rng); the Rations line is inserted among the food adds, before the premium-item rng.d(2)/rollBlade draws; the full parity/determinism suite runs in `npm test`. |
| T-04.1-SC | Tampering | supply chain | n/a | accept | No npm/package installs in this phase. |
</threat_model>

<verification>
- `npm test` full suite green — economy/encounters/combat/formatEventsCoverage + parity/determinism/roundtrip all pass.
- Store food heals HP only; the dedicated Rations line is the visible ration purchase; findFood heals HP only; Cooking still awards a ration.
- Deferred device-UAT: on the Pixel 7, buying food shows HP up with no surprise ration change, and buying Rations shows the ration counter move with a log line.
</verification>

<success_criteria>
- CONTEXT success criterion 2 is met: buying food heals HP only; rations are a distinct purchasable/foraged resource; and any ration change is shown to the player.
</success_criteria>

<output>
Create `.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-03-SUMMARY.md` when done.
</output>
