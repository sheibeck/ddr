---
phase: 04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep
plan: 04
type: execute
wave: 3
depends_on: [04.1-03, 04.1-02, 04.1-01]
files_modified:
  - engine/derived.js
  - engine/encounters.js
  - src/browser/viewModels.js
  - test/unit/encounters.test.js
autonomous: true
requirements: [RULE-01]
must_haves:
  truths:
    - "Intelligence measurably changes at least one gameplay outcome: a higher c.intel yields a better openChest lock result for the same seed/roll, provable by a test — RULE-01"
    - "The INT-scaled read is a single exported helper in engine/derived.js (e.g. intelBonus(c)) so the engine and the character sheet consume the same value — mirroring the damageBracket↔weaponDamage sync pattern — RULE-01"
    - "The check is self-contained: it depends on NO foe spellcasting and no new subsystem — it augments the existing openChest lock roll's success threshold — RULE-01"
    - "The INT bonus changes the comparison only (the success threshold), adding/removing NO rng draw, so openChest's RNG consumption order is unchanged and parity/determinism stay green"
  artifacts:
    - test/unit/encounters.test.js
  key_links:
    - "openChest is rolled by EVERY class on EVERY chest (even the bare-d20 no-Locks path), so an INT bonus there makes Intelligence matter for the whole roster, not just one subclass"
    - "Adding intelBonus to the success threshold (need), not as a new rng draw, is what keeps RNG order identical — the single most important determinism guardrail for this plan"
    - "Depends on 04.1-03 (encounters.js findFood) and 04.1-02 (derived.js) for file ownership, and 04.1-01 (viewModels.js) for the label pass — same-file waves are serialized"
---

<objective>
Resolve RULE-01 (user chose Option B): wire Intelligence into a small,
self-contained gameplay check with no dependency on foes gaining spellcasting.
Add an exported INT-scaled helper in engine/derived.js and apply it as a bonus to
openChest's lock-roll success threshold in engine/encounters.js, so a smarter
character opens locks more reliably. Surface the derived read on the character
sheet so Intelligence visibly reads-as-and-is a gameplay stat.

Purpose: Intelligence is the single clearest cosmetic orphan on the sheet
(04.1-RESEARCH.md); the phase end goal requires the number to bite.
Output: a derived intelBonus helper, an INT bonus on the openChest lock roll, a
sheet read kept in sync, and a test proving higher INT changes the outcome.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-CONTEXT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-RESEARCH.md
@engine/derived.js
@engine/encounters.js
@src/browser/viewModels.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add intelBonus helper (derived.js) and apply it to the openChest lock roll</name>
  <files>engine/derived.js, engine/encounters.js, test/unit/encounters.test.js</files>
  <read_first>
    - engine/encounters.js openChest (lines 104-133): the tiered path `need = [0,5,7,8][min(tier,3)]; roll = rng.d(10); opened = roll <= need` and the bare-d20 path `roll = rng.d(20); opened = roll <= 8`. The INT bonus raises `need` (a smarter character needs a higher/easier threshold), changing ONLY the comparison — the rng.d(10)/rng.d(20) draw stays exactly where it is, then the subsequent gainWilmst/scroll/treasure rng draws follow in the same order
    - engine/derived.js the exported-helper pattern (skill/skillTier/eff at lines 17-28; strikeDie/toHit) — add a small pure `intelBonus(c)` returning an INT-scaled integer (e.g. clamp of floor((c.intel - baseline) / step)); choose and document a modest curve so a high-INT character gains ~1-2 to the lock threshold, not a runaway bonus
    - engine/character.js chargen — the c.intel roll range, so the curve's baseline/step are sane for the actual distribution
    - src/browser/viewModels.js characterSheetViewModel stats: line 98 INTELLIGENCE already shows raw c.intel; decide whether to surface the derived lock bonus (e.g. append the bonus to the INTELLIGENCE value or add a small derived note) so the sheet reflects the new mechanical read
    - test/unit/encounters.test.js openChest tests for the fixture/rng pattern; the chestLockRolled event carries roll/need so the test can assert need shifted with intel
  </read_first>
  <action>
    Add an exported pure helper `intelBonus(c)` to engine/derived.js that maps c.intel to a small non-negative integer bonus via a documented, modest curve (so a higher Intelligence yields a larger bonus, capped to avoid trivializing locks — pick and comment the baseline/step and cap). In engine/encounters.js openChest, apply intelBonus(c) to the lock-roll SUCCESS THRESHOLD in BOTH the tiered path (add it to `need` before `opened = roll <= need`) and the bare-d20 path (add it to the `need: 8` comparison), and include the resolved need in the existing chestLockRolled event so it is observable. CRITICAL: apply the bonus to the threshold ONLY — do NOT add, remove, or reorder any rng draw (the rng.d(10)/rng.d(20) lock roll and every following gainWilmst/scroll/treasure draw must stay in the exact same sequence) so openChest's RNG consumption order is preserved and parity/determinism stay green. Surface the read on the sheet: in src/browser/viewModels.js, reflect the intelBonus (e.g. show it alongside the INTELLIGENCE row or as a derived value) using the same derived.js helper so the sheet and engine never drift (the damageBracket↔weaponDamage sync precedent). Log the openChest change as a DELIBERATE RULES CHANGE (Intelligence had no mechanical read; RULE-01 gives it one). Add a test/unit/encounters.test.js case: for a fixed rng producing a borderline lock roll, a high-intel character opens the chest while an otherwise-identical low-intel character does not (or the chestLockRolled `need` is provably higher for the high-intel character).
  </action>
  <verify>
    <automated>node --test test/unit/encounters.test.js test/unit/characterSheetViewModel.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - intelBonus(c) is exported from derived.js and consumed by both openChest and the character sheet (single source of truth).
    - A higher c.intel measurably changes the openChest outcome (a borderline roll that fails at low INT succeeds at high INT), proven by a test.
    - No rng draw is added/removed/reordered in openChest; `npm test` full suite green (parity/determinism/roundtrip intact).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| derived stat → resolution + sheet | The same intelBonus helper must feed both the engine check and the sheet so they cannot diverge |
| threshold change → RNG order | The bonus must alter the comparison, never the seeded-rng draw sequence |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-07 | Tampering | adding an rng draw for the INT check and shifting openChest RNG order | high | mitigate | The bonus is applied to the success threshold only; no rng draw changes; the full parity/determinism suite runs in `npm test`. |
| T-04.1-08 | Information Disclosure | the sheet showing an INT value that does not match the engine's actual bonus | low | mitigate | Both the sheet and openChest call the single exported intelBonus helper; characterSheetViewModel.test.js guards the parity. |
| T-04.1-09 | Elevation of Privilege | an oversized INT bonus trivializing locks | low | mitigate | The curve is modest and capped, documented in-code; a test pins the borderline behavior. |
| T-04.1-SC | Tampering | supply chain | n/a | accept | No npm/package installs in this phase. |
</threat_model>

<verification>
- `npm test` full suite green — encounters/characterSheetViewModel/formatEventsCoverage + parity/determinism/roundtrip all pass.
- Higher INT provably opens a borderline-locked chest that lower INT does not; the sheet reflects the derived bonus via the shared helper.
- Deferred device-UAT: on the Pixel 7, a high-Intelligence adventurer visibly opens locked chests more often.
</verification>

<success_criteria>
- CONTEXT success criterion 4 is met: Intelligence measurably changes at least one gameplay outcome (a higher INT yields a different result), provable by a test.
</success_criteria>

<output>
Create `.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-04-SUMMARY.md` when done.
</output>
