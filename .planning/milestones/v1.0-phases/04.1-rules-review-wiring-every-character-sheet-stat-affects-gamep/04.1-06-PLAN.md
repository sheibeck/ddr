---
phase: 04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep
plan: 06
type: execute
wave: 5
depends_on: [04.1-05]
files_modified:
  - engine/combat.js
  - engine/movement.js
  - content/flavor.js
  - src/browser/eventNarration.js
  - test/unit/combat.test.js
  - test/unit/movement.test.js
autonomous: true
requirements: [PHOBIA-01]
must_haves:
  truths:
    - "Death phobia: a Death-phobic character whose own HP is at/below a near-death threshold (~25% of maxWP) freezes/panics when an encounter starts — a genuinely new low-HP trigger reusing the combat frozen seam, Hardiness-halved — PHOBIA-01"
    - "Being trapped phobia: a trapped-phobic character suffers a fear effect in an enclosed situation (a one-way-door block and/or a dead-end tile), Hardiness-halved — PHOBIA-01"
    - "Heights phobia: a heights-phobic character takes a penalty on climb tiles (reusing the existing climb roll math), making falls more likely, Hardiness-halved — PHOBIA-01"
    - "Bodies of water phobia: a water-phobic character takes a penalty on gorge/crevice tiles (the cheapest real hook — reusing the existing leap-the-gorge math), Hardiness-halved; flavor-only fallback used ONLY if no real hook is feasible — PHOBIA-01"
    - "All 10 phobias (the 4 original type-matched + Darkness from 04.1-05 + these 4) now produce a documented mechanical effect; re-rolled phobias (newPhobia / catchAffliction) draw from the same catalog so all behave — PHOBIA-01"
  artifacts:
    - test/unit/combat.test.js
    - test/unit/movement.test.js
  key_links:
    - "Death reuses the combat.frozen / shookOffFrozen seam like Darkness (04.1-05) and the 4 type-matched phobias; the movement phobias fold a deterministic penalty into the existing climb/leap roll comparison and the one-way/dead-end path — no new rng in the hot movement loop, so parity/determinism are trivially safe"
    - "Hardiness 'phobias halved' is honored uniformly: for combat freezes as the existing 50% rng.d(2) shrug (Death), and for movement penalties as a deterministic half-magnitude penalty (Heights/Water/Trapped) to keep the movement path rng-free"
    - "Depends on 04.1-05 for combat.js/movement.js/eventNarration.js ownership and to build on the darkness-generalized phobia hook"
---

<objective>
Resolve the remaining half of PHOBIA-01 (user chose Option C, bespoke per phobia):
give the 4 still-inert phobias distinct, flavor-matched effects, each respecting
Hardiness's phobias-halved mitigation. Death → near-death combat panic; Being
trapped → fear in enclosed (one-way/dead-end) situations; Heights → climb-roll
penalty; Bodies of water → gorge/crevice-leap penalty (the cheapest real hook).
Combined with the Darkness work in 04.1-05 and the 4 original type-matched
phobias, all 10 catalog entries now bite.

Purpose: 6 of 10 phobias were pure flavor; the phase end goal requires every
sheet quirk that reads as gameplay to affect gameplay.
Output: 4 bespoke phobia effects wired through existing combat/movement seams,
Hardiness-halved, with tests asserting each; every new event narrated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-CONTEXT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-RESEARCH.md
@engine/combat.js
@engine/movement.js
@content/flavor.js
@src/browser/eventNarration.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Death phobia — near-death panic at encounter start (combat.js, Hardiness-halved)</name>
  <files>engine/combat.js, content/flavor.js, src/browser/eventNarration.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/combat.js startCombat phobia hook (lines 192-195) and the Darkness-by-name freeze added in 04.1-05 — mirror that structure for Death, keyed by c.phobia === "Death" and a near-death HP condition (c.wp at/below a documented fraction of c.maxWP, ~25%)
    - engine/combat.js frozen/shookOffFrozen flow (playerStrike 219-224) — Death reuses the same frozen flag so the first action shakes off the panic
    - content/flavor.js PHOBIAS (lines 17-22) — Death has t: null; optionally add a light situational tag field to the catalog entries (Death/Being trapped/Heights/Bodies of water) to make the engine branch data-driven, but keep the EFFECT logic in the engine per the Architectural Responsibility Map (content is data only)
    - 04.1-RESEARCH.md Phobias "Death (as a phobia) ... there is no existing near-death state hook" — this task creates that trigger
    - src/browser/eventNarration.js phobiaFrozen (~104) — reuse or add a Death-specific near-death-panic event with its own EVENT_NARRATION entry
    - test/unit/combat.test.js phobia fixtures to mirror
  </read_first>
  <action>
    In engine/combat.js startCombat, add a Death-phobia near-death panic beside the existing type-matched and Darkness checks: when c.phobia === "Death" AND c.wp is at/below a documented near-death threshold (~25% of c.maxWP, comment the exact fraction as a deliberate value), freeze the character via the existing state.combat.frozen seam and push a panic event, gated by the SAME Hardiness 50% shrug (rng.d(2) === 1) the other phobias use. Order the new rng.d(2) so it fires ONLY for a Death-phobic character who is actually near death (never chargen, never for others), preserving RNG order for everyone else; document it as a DELIBERATE RULES CHANGE (the Death phobia was inert). Optionally add a situational tag to the Death entry in content/flavor.js PHOBIAS for clarity, but keep the threshold/freeze logic in the engine. Reuse phobiaFrozen narration or add a Death near-death-panic event with an EVENT_NARRATION entry in the deadpan tone. Extend test/unit/combat.test.js: a Death-phobic character at/below the threshold freezes on encounter start; the same character above the threshold does NOT freeze; Hardiness halves it on the mitigating roll; a non-Death character consumes no extra rng.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - A Death-phobic character at/below the near-death HP threshold freezes on encounter start; above it, no freeze; Hardiness halves it — proven by tests.
    - Any new event type has narration coverage; the new rng.d(2) fires only for near-death Death-phobic characters.
    - `npm test` full suite green (parity/determinism intact).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Heights, Bodies of water, and Being trapped — movement-triggered fears (Hardiness-halved)</name>
  <files>engine/movement.js, content/flavor.js, src/browser/eventNarration.js, test/unit/movement.test.js</files>
  <read_first>
    - engine/movement.js the climb/gorge block (lines 81-114): the climb roll `r = rng.d(10) - climbBonus(c)` compared to tbl.success, and the gorge leap `r = rng.d(10) - leapBonus(c)` compared to `need`. A phobia penalty is added to `r` (higher r = worse) — this modifies the COMPARISON only, adding NO rng draw, so RNG order is preserved
    - engine/movement.js the one-way-door blocks (lines 70-77, oneWayBlocked "approach"/"departure") — the enclosed/"trapped" moment for the Being trapped phobia; optionally also detect a dead-end tile (a newly-entered tile with exactly one non-wall orthogonal neighbor) after the move commits (lines 116-120)
    - engine/movement.js climbBonus/leapBonus (lines 43-44) — the existing skill modifiers the phobia penalty sits beside
    - content/flavor.js PHOBIAS — Heights / Bodies of water / Being trapped entries (all t: null), keyed by name in the engine
    - 04.1-CONTEXT.md PHOBIA-01: Heights (climb/leap/gorge penalty), Bodies of water (cheapest real hook — the gorge/crevice leap is the reuse; flavor-only fallback ONLY if no real hook), Being trapped (penalty/freeze in dead-end/one-way situations)
    - src/browser/eventNarration.js — new fear events (heights/water/trapped) each need an EVENT_NARRATION entry
    - test/unit/movement.test.js climb/leap fixtures (lines 213-241) for the rng pattern that makes a borderline roll pass/fail
  </read_first>
  <action>
    Add three movement-triggered phobia effects in engine/movement.js, each keyed by c.phobia name and each honoring Hardiness by HALVING the penalty magnitude (deterministic — no new rng in the hot movement loop, keeping parity trivially safe):
    (1) Heights: when c.phobia === "Heights", add a documented penalty to the climb roll `r` on a climb-feature tile (raising failure/fall chance); a Climbing skill and Hardiness reduce it per their existing/halved rules. Push a fear event.
    (2) Bodies of water: when c.phobia === "Bodies of water", add the analogous penalty to the gorge/crevice leap `r` (the crevice is the cheapest REAL water-adjacent hook — a chasm you fear may be flooded); Hardiness halves it. Push a fear event. Use flavor-only fallback ONLY if the gorge reuse proves infeasible.
    (3) Being trapped: when c.phobia === "Being trapped", apply a fear effect at the enclosed moment — on a one-way-door block (approach or departure) and/or on entering a dead-end tile — as a modest deterministic panic (e.g. a small wp loss or a shaken penalty), Hardiness-halved. Push a fear event. **DEBOUNCE (plan-check WARNING):** the dead-end/one-way panic MUST be one-shot per tile so a player oscillating on a dead-end tile can't re-trigger the wp loss every step — fire it only on a genuine ENTRY to a newly-entered enclosed tile (guard on a serialized marker, e.g. remember the last tile that already panicked, or only trigger when the tile is freshly entered this move), never on a bump/blocked no-op or on standing still. Add a regression test asserting a second consecutive attempt at the same dead-end does NOT re-apply the penalty.
    Optionally add situational tags to these entries in content/flavor.js PHOBIAS for clarity, but keep the effect logic in the engine. Document each as a DELIBERATE RULES CHANGE (these phobias were inert). Add EVENT_NARRATION entries for every new fear event in the deadpan tone (formatEventsCoverage guard). Extend test/unit/movement.test.js: a Heights-phobic character fails a borderline climb an identical non-phobic character passes; a Bodies-of-water-phobic character fails a borderline gorge leap an identical non-phobic character passes; a Being-trapped-phobic character suffers the panic at a one-way block / dead-end; and in each case Hardiness reduces the penalty. Confirm no rng draw is added in the movement path (the penalties change comparisons/apply deterministic loss only).
  </action>
  <verify>
    <automated>node --test test/unit/movement.test.js test/unit/formatEventsCoverage.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Heights (climb), Bodies of water (gorge), and Being trapped (one-way/dead-end) each produce a documented mechanical effect, Hardiness-halved, proven by tests.
    - Being trapped's penalty is one-shot per enclosed-tile entry (debounced) — a second consecutive attempt at the same dead-end does NOT re-apply it, proven by a regression test.
    - Every new fear event has EVENT_NARRATION coverage; no rng draw is added in the movement path (parity/determinism intact).
    - All 10 phobias now produce a documented effect (4 original + Darkness + these 4, plus Death); `npm test` full suite green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| new combat rng draw → determinism | The Death near-death Hardiness roll must not shift RNG order for characters who never trigger it |
| movement penalty → RNG order | Heights/Water/Trapped effects must modify comparisons / apply deterministic loss, never add an rng draw in the hot movement loop |
| re-rolled phobia → catalog coverage | newPhobia / catchAffliction re-rolls must land on phobias that all now behave |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-13 | Tampering | the Death rng.d(2) or a stray movement rng draw shifting RNG order and breaking parity/determinism | high | mitigate | Death's roll fires only for near-death Death-phobic characters; movement effects add NO rng (deterministic penalties); full parity/determinism suites run in `npm test`; changes documented as deliberate. |
| T-04.1-14 | Denial of Service | a movement phobia penalty so large the character can never climb/leap/pass, softlocking a run | medium | mitigate | Penalties are modest, documented, and Hardiness-halved; tests pin borderline (not always-fail) behavior; the tile features remain passable for a lucky roll. |
| T-04.1-15 | Repudiation | a phobia effect firing with no visible log line | low | mitigate | Each effect pushes a fear event with EVENT_NARRATION coverage enforced by formatEventsCoverage. |
| T-04.1-SC | Tampering | supply chain | n/a | accept | No npm/package installs in this phase. |
</threat_model>

<verification>
- `npm test` full suite green — combat/movement/formatEventsCoverage + parity/determinism/roundtrip all pass.
- Each of Death, Heights, Bodies of water, and Being trapped produces its documented effect, Hardiness-halved, proven by a test; all 10 phobias behave (incl. re-rolled).
- Deferred device-UAT: on the Pixel 7, a heights-phobe fumbles climbs, a water-phobe fears the crevice, a trapped-phobe panics at a one-way door, and a death-phobe locks up when jumped at low HP.
</verification>

<success_criteria>
- The remaining half of CONTEXT success criterion 5 is met: all 10 phobias produce a documented mechanical effect when triggered (the 6 formerly-inert ones now bite), and Hardiness still halves them.
</success_criteria>

<output>
Create `.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-06-SUMMARY.md` when done.
</output>
