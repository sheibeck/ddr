---
phase: 04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep
plan: 05
type: execute
wave: 4
depends_on: [04.1-04, 04.1-02]
files_modified:
  - engine/derived.js
  - engine/encounters.js
  - engine/movement.js
  - engine/combat.js
  - engine/character.js
  - src/browser/eventNarration.js
  - test/unit/movement.test.js
  - test/unit/combat.test.js
autonomous: true
requirements: [PHOBIA-01]
must_haves:
  truths:
    - "A real persistent darkness state exists: the 'Darkness' encounter-table result (fallDark) now sets a duration-based darkness counter on the character (a new field, e.g. c.darkFor, in steps) in addition to painting tiles dark — connecting the previously-thin darkness table effect to a lasting state — PHOBIA-01"
    - "While the persistent darkness state is active the character counts as inDark regardless of tile: inDark(state) returns true, so the fog-of-war reveal radius shrinks (revealRadius) and the existing in-dark to-hit penalty applies; Night Vision still waives the penalties everywhere inDark is consumed — PHOBIA-01"
    - "The persistent darkness counter ticks down as the character moves and clears when it reaches zero (a lantern/step-based recovery), keeping determinism (decrement only, no new chargen rng) — PHOBIA-01"
    - "A Darkness-phobic character (c.phobia === 'Darkness') freezes/panics when an encounter starts while dark, respecting the existing Hardiness 'phobias halved' 50% mitigation (combat.js:193 pattern) — PHOBIA-01"
    - "The new darkness field is initialized at chargen and serialized so the roundtrip save-shape test stays green; no state-field or event-type is renamed"
  artifacts:
    - test/unit/movement.test.js
    - test/unit/combat.test.js
  key_links:
    - "inDark(state) is THE single hook every darkness consumer already reads (toHit cap, revealRadius shrink, combatInDark, no-crit-in-dark); extending inDark to also return true while c.darkFor>0 makes the persistent state flow through all of them for free, and Night Vision waivers stay intact because they already wrap inDark"
    - "The Darkness phobia reuses the combat.frozen / shookOffFrozen seam the 4 working phobias use — generalized from a foe-type match to an inDark-at-encounter-start condition"
    - "This is the highest-risk item: it touches derived/encounters/movement/combat/character; it is gated to Wave 4 after the earlier waves free those shared engine files"
---

<objective>
Resolve the Darkness half of PHOBIA-01 (user's explicit steer, Option C): wire a
REAL persistent darkness state and connect the previously-inert darkness table
effect to it. The 'Darkness' encounter result gains a lasting darkness counter that
makes the character count as inDark (shrinking fog-of-war and applying the existing
in-dark to-hit penalty; Night Vision still waives it), ticking down as they move.
A Darkness-phobic character additionally freezes/panics when an encounter starts
while dark, respecting Hardiness's phobias-halved mitigation.

Purpose: Darkness is one of the 6 inert phobias, and the darkness table effect is
an orphaned mechanic; the CONTEXT directs wiring BOTH through one persistent state.
Output: a persistent darkness field + inDark extension + fallDark wiring + a
Darkness-phobia freeze, all deterministic, with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-CONTEXT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-RESEARCH.md
@engine/derived.js
@engine/encounters.js
@engine/movement.js
@engine/combat.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persistent darkness state — field, inDark extension, fallDark wiring, per-step tick</name>
  <files>engine/character.js, engine/derived.js, engine/encounters.js, engine/movement.js, src/browser/eventNarration.js, test/unit/movement.test.js</files>
  <read_first>
    - engine/derived.js inDark(state) (lines 49-52): reads only the current tile's `.dark`. It is consumed by revealRadius (line 63), toHit (line 81, caps to 2 in the dark unless Night Vision/senses), and combat.js (combatInDark line 196, no-crit line 263). Extending inDark to ALSO return true while the persistent darkness counter is active makes the whole set respond to the persistent state, and every Night Vision waiver already wraps inDark so it keeps working
    - engine/encounters.js fallDark (lines 444-454): today it only paints a 4-radius of tiles `.dark = true` and pushes darknessFell. This is the "darkness table effect" to connect — have it ALSO set the persistent counter
    - engine/movement.js the per-step block after a successful move (lines 122-134, the affliction tick) — the natural place to decrement the persistent darkness counter each step and clear it at zero; mirror the affliction-tick shape (no rng needed — a plain decrement)
    - engine/character.js rollCharacter (lines 142-160): the chargen object literal where fields like acute/invis/haste/affliction are initialized to 0/null — add the new darkness field initialized to 0 here so save-shape/roundtrip is clean and no chargen rng is added
    - engine/derived.js revealRadius (lines 62-64) — confirm it already derives from inDark so the shrink is automatic once inDark honors the persistent state
    - src/browser/eventNarration.js darknessFell (~line 255) + combatInDark (~line 105) — reuse/extend; if a new "darkness deepened/lifted" event is added it needs a narration entry (formatEventsCoverage guard)
    - test/unit/movement.test.js — the move/reveal fixtures to assert the reveal radius shrinks and the counter ticks down
  </read_first>
  <action>
    Add a persistent darkness field to the character (choose a clear name that is NOT an existing key, e.g. c.darkFor, measured in steps) and initialize it to 0 in engine/character.js rollCharacter alongside the other 0/null fields so the save shape stays clean (no new chargen rng draw). Extend engine/derived.js inDark(state) so it returns true when EITHER the current tile is dark OR the persistent counter is active (`state.c.darkFor > 0`) — this single change makes revealRadius shrink, the toHit in-dark cap apply, and combat's in-dark handling fire while the persistent darkness lasts, with Night Vision continuing to waive them (its checks already wrap inDark). In engine/encounters.js fallDark, in addition to painting the tile radius dark, set the persistent counter to a documented duration (a set number of steps) so the 'Darkness' table result now has lasting meaning; keep the darknessFell event (extend its payload if useful). In engine/movement.js's per-step block, decrement the persistent counter each successful move and clear/announce it at zero (a plain decrement — no rng; add a narrated "darkness lifts" event if you emit one, with an EVENT_NARRATION entry). Extend test/unit/movement.test.js: after fallDark sets the counter, inDark is true and revealRadius is the shrunk value even on a non-dark tile; the counter decrements per step and clears at zero; a Night Vision character is unaffected by the penalties. Do not rename any existing field or event type.
  </action>
  <verify>
    <automated>node --test test/unit/movement.test.js test/unit/maze.test.js test/unit/formatEventsCoverage.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - fallDark sets a persistent darkness counter; while it is active inDark(state) is true and revealRadius returns the shrunk radius even off a dark tile; Night Vision still waives the penalties.
    - The counter decrements per step and clears at zero; any new event type has narration coverage.
    - The new field is chargen-initialized and serialized (roundtrip test green); no field/event rename; `npm test` stays green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Darkness-phobia freeze on encounter start while dark (Hardiness-halved)</name>
  <files>engine/combat.js, src/browser/eventNarration.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/combat.js startCombat, the existing phobia hook (lines 192-195): `if (c.phobiaType === type && !(skill(c, "Hardiness") && rng.d(2) === 1)) { state.combat.frozen = true; push phobiaFrozen }`. This is the seam to generalize — the Darkness phobia has phobiaType null (content/flavor.js PHOBIAS), so it is keyed by NAME (c.phobia === "Darkness"), not by foe type
    - engine/derived.js inDark(state) (as extended in Task 1) — the condition for the Darkness freeze
    - engine/combat.js line 196 combatInDark + the frozen/shookOffFrozen flow (playerStrike lines 219-224) — the Darkness freeze reuses the SAME frozen flag so the player's first action shakes it off, exactly like the type-matched phobias
    - src/browser/eventNarration.js phobiaFrozen (~line 104) — reuse it, or add a Darkness-specific frozen narration (new event type needs an EVENT_NARRATION entry)
    - 04.1-CONTEXT.md PHOBIA-01 Darkness bullet + 04.1-RESEARCH.md Phobias "one existing mechanical hook"
    - test/unit/combat.test.js — the existing phobiaFrozen fixtures (type-matched) to mirror for the Darkness-by-name case
  </read_first>
  <action>
    In engine/combat.js startCombat, add a Darkness-phobia freeze alongside the existing type-matched phobia check: when c.phobia === "Darkness" AND inDark(state) is true at encounter start, set state.combat.frozen and push the freeze event, gated by the SAME Hardiness-halved mitigation the existing phobia uses (a Hardiness character has a 50% chance to shrug it off via rng.d(2) === 1). Reuse the existing frozen/shookOffFrozen flow so the player's first action is consumed shaking it off (do not invent a new freeze mechanism). Order the new rng.d(2) Hardiness roll so it only fires for a Darkness-phobic character in the dark (never during chargen and never for non-phobic characters), preserving parity/determinism for everyone else; document it as a DELIBERATE RULES CHANGE (the Darkness phobia was inert). Reuse phobiaFrozen narration or add a Darkness-specific frozen event with its own EVENT_NARRATION entry. Extend test/unit/combat.test.js: a Darkness-phobic character starting an encounter while dark is frozen; the same character in the light is NOT frozen; a Hardiness + Darkness-phobic character shakes it off on the mitigating roll. Ensure a non-Darkness, non-type-matched character consumes no extra rng (RNG order unchanged for them).
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - A Darkness-phobic character freezes on encounter start while dark, does not freeze in the light, and Hardiness halves it — all proven by tests.
    - The freeze reuses the frozen/shookOffFrozen seam; any new event type has narration coverage.
    - The new Hardiness rng.d(2) fires only for Darkness-phobic characters in the dark; `npm test` full suite green (parity/determinism intact).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| new persistent field → save shape | c.darkFor must be chargen-initialized and serialized or a reloaded save behaves differently |
| new combat rng draw → determinism | The Darkness Hardiness roll must not shift RNG order for characters who never trigger it |
| inDark extension → all darkness consumers | Broadening inDark must not accidentally darken characters who are neither on a dark tile nor under persistent darkness |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-10 | Tampering | new Darkness Hardiness rng.d(2) shifting RNG order and breaking parity/determinism | high | mitigate | The roll fires only for c.phobia === "Darkness" in the dark (never chargen, never for others); full parity/determinism suites run in `npm test`; documented deliberate change. |
| T-04.1-11 | Denial of Service | inDark returning true for `darkFor` values that never decrement, permanently blinding the player | medium | mitigate | The per-step tick decrements and clears the counter at zero; a movement test asserts it clears; the value is defaulted to 0 at chargen. |
| T-04.1-12 | Tampering | the new field missing from serialization | medium | mitigate | c.darkFor is initialized in rollCharacter with the other scalar fields; the roundtrip serialize/rehydrate test runs in verify. |
| T-04.1-SC | Tampering | supply chain | n/a | accept | No npm/package installs in this phase. |
</threat_model>

<verification>
- `npm test` full suite green — movement/combat/maze/formatEventsCoverage + parity/determinism/roundtrip all pass.
- Persistent darkness shrinks fog-of-war and applies the in-dark penalty off a dark tile, ticks down, and clears; Night Vision waives it; a Darkness-phobic character freezes in the dark and Hardiness halves it.
- Deferred device-UAT: on the Pixel 7, a 'Darkness' encounter visibly shrinks sight for several steps, and a Darkness-phobic hero locks up when jumped in the dark.
</verification>

<success_criteria>
- The Darkness half of CONTEXT success criterion 5 is met: the Darkness phobia is backed by a real persistent darkness/fog-of-war state that also gives the previously-inert darkness table effect meaning, Hardiness still halves it, and Night Vision waives the penalties.
</success_criteria>

<output>
Create `.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-05-SUMMARY.md` when done.
</output>
