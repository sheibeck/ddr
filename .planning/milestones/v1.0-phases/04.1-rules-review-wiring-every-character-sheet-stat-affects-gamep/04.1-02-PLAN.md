---
phase: 04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/movement.js
  - engine/derived.js
  - test/unit/movement.test.js
  - test/unit/combat.test.js
autonomous: true
requirements: [RULE-02]
must_haves:
  truths:
    - "Thief Sewing repairs armour in play: during newDay, a Sewing-skilled character with damaged armour patches it (d6 back at tier 1, d6+3 six times at tier 2), fired by a live capability gate — not the dead c.dr>0 branch — RULE-02"
    - "Fighter subclass Master of Arms confers a flat '+2 with every weapon' bonus, implemented in engine/derived.js weaponDamage (and/or toHit), where none existed before — RULE-02"
    - "Master of Arms' '+3 repaired-yourself' armour-patch clause (the d6+3 branch in newDay) now fires because the dead c.dr gate is replaced with a real capability check — RULE-02"
    - "The gate replacement is logged in-code as a DELIBERATE RULES CHANGE following the engine/movement.js:239 precedent, and does NOT patch armour for characters lacking Sewing or the Master of Arms subclass"
  artifacts:
    - test/unit/movement.test.js
    - test/unit/combat.test.js
  key_links:
    - "The c.dr field is never set by rollCharacter (confirmed dead code, movement.js:216-220); the fix replaces the gate with skillTier(c,'Sewing')||c.sub==='Master of Arms' so the inner Sewing/Master-of-Arms branches become reachable without regressing the (previously never-taken) bare else branch onto all characters"
    - "Master of Arms +2 belongs in derived.js weaponDamage so viewModels' damageBracket preview and the combat roll stay byte-identical (the damageBracket↔weaponDamage parity guarded by characterSheetViewModel.test.js)"
---

<objective>
Resolve RULE-02: fix the c.dr dead-code bug that silently disables two subclass
abilities. (1) In engine/movement.js newDay, replace the unreachable `c.dr > 0`
gate — a field rollCharacter never sets — with a real capability check so Thief
Sewing's armour self-repair and Master of Arms' "+3 repaired-yourself" armour patch
actually fire. (2) In engine/derived.js, implement the Master of Arms headline "+2
with every weapon" flat bonus, which currently has NO implementation anywhere.

Purpose: Two character-sheet capabilities (a whole Thief skill and a Fighter
subclass's stated identity) read as gameplay but do nothing today; the phase end
goal requires they bite.
Output: a live capability gate for the armour-patch branch + a Master-of-Arms
weapon bonus in derived.js, both logged as deliberate rules changes, with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-CONTEXT.md
@.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-RESEARCH.md
@engine/movement.js
@engine/derived.js
@content/skills.js
@content/flavor.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the dead c.dr gate so Sewing + Master-of-Arms armour patch fires</name>
  <files>engine/movement.js, test/unit/movement.test.js</files>
  <read_first>
    - engine/movement.js newDay, the armour-patch block (lines 214-238): the outer gate `if (c.armorWP < c.armorMax && c.dr > 0)`, the Sewing branch (skillTier + c.patches < maxPatch, d6 / d6+3), the Master of Arms branch (d6+3), and the bare else (d4). Note the header comment (216-220) already documents c.dr as never-set dead code
    - engine/derived.js skillTier(c, name) — the capability read to use in the new gate
    - content/skills.js THIEF_SKILLS.Sewing (tier 1: "d6 back, 4 times"; tier 2 up: "d6+3 back, 6 times") — the tier/maxPatch semantics the branch encodes
    - engine/movement.js:239-248 — the existing "DELIBERATE RULES CHANGE (04-DR12...)" comment block; mirror its style for this change
    - engine/character.js rollCharacter (c.patches:0 is set; c.dr is NOT — confirm) so the new gate never depends on c.dr
    - test/unit/movement.test.js newDay tests (~lines 404-440) for the fixture/rng pattern to extend
  </read_first>
  <action>
    In engine/movement.js newDay, replace the dead outer gate `c.dr > 0` with a real capability check: the armour-patch block runs only when armour is damaged AND the character can actually repair it — `c.armorWP < c.armorMax && (skillTier(c, "Sewing") || c.sub === "Master of Arms")`. Keep the inner branch structure intact (the Sewing d6 / tier-2 d6+3-six-times branch gated by c.patches < maxPatch, then the Master of Arms d6+3 branch); the bare `else { rng.d(4) }` branch was only ever reachable under the dead gate and must NOT now fire for characters without Sewing or the Master of Arms subclass, so fold it away or leave it reachable ONLY for the intended patch-exhausted-Sewing case — do not grant a free d4 armour patch to arbitrary characters. Add a DELIBERATE RULES CHANGE comment (dated, following the movement.js:239 precedent) documenting that the prototype's unreachable c.dr condition is deliberately replaced with the capability check so the ported-verbatim dead branch becomes live, and noting that this introduces a new rng.d draw inside newDay ONLY for Sewing/Master-of-Arms characters with damaged armour (no chargen RNG is touched, so parity is unaffected). Extend test/unit/movement.test.js: a tier-1 Sewing thief with damaged armour heals armorWP by d6 (bounded by armorMax) during newDay and pushes armorPatched; a tier-2 Sewing thief patches d6+3 up to six times; a Master of Arms fighter with damaged armour patches d6+3; and a plain character with damaged armour and neither capability gets NO armour patch.
  </action>
  <verify>
    <automated>node --test test/unit/movement.test.js test/unit/formatEventsCoverage.test.js</automated>
  </verify>
  <acceptance_criteria>
    - The armour-patch block fires for Sewing-skilled characters and Master of Arms (previously dead) and does NOT fire for characters lacking both.
    - The armorPatched event narration coverage stays green (event type already exists).
    - A DELIBERATE RULES CHANGE comment documents the gate replacement; `npm run test:quick` stays green.
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Master of Arms "+2 with every weapon" in derived.js</name>
  <files>engine/derived.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/derived.js weaponDamage(c, rng) (lines 121-135): the additive modifier stack (level^2 + base + prof + magicWpn + race/Kata/Heft/eff/Guard/Sorcerer). This is where a flat weapon-proficiency bonus belongs
    - engine/derived.js toHit(state) (lines 71-83) — the parallel to-hit modifier stack, in case the "+2 with every weapon" is best read partly as accuracy; pick the reading that matches the prototype's "weapon proficiency" intent and document it
    - content/flavor.js SUB_NOTE["Master of Arms"] = "Plus two with every weapon ever forged, plus three with anything you have repaired yourself." — the sheet text this bonus must make true
    - 04.1-RESEARCH.md Special Skills, "Bonus finding — Master of Arms": grep confirms NO existing implementation of the +2 in derived.js weaponDamage/toHit today
    - src/browser/viewModels.js damageBracket (the UI preview that mirrors weaponDamage) — adding the +2 in weaponDamage keeps the sheet's DAMAGE preview and the combat roll in sync (characterSheetViewModel.test.js enforces this)
    - test/unit/combat.test.js — the weaponDamage / playerStrike fixtures to extend
  </read_first>
  <action>
    In engine/derived.js weaponDamage, add a flat "+2 with every weapon" bonus for the Master of Arms subclass: when `c.sub === "Master of Arms"`, add 2 to the accumulated damage (a weapon-proficiency bonus, applied before the final Math.max(1, d) clamp and before the Sorcerer cap, consistent with the existing prof/Heft additive pattern). This is a flat additive, no RNG — it does not change RNG consumption order, so parity/determinism are unaffected. If the prototype's "with every weapon" is better read as accuracy, additionally/instead nudge toHit — but keep it simple and documented: a single flat damage bonus in weaponDamage satisfies the sheet's stated identity and keeps the damageBracket preview in sync. Add a DELIBERATE RULES CHANGE comment noting that Master of Arms' headline "+2 with every weapon" had no implementation and is now wired here per RULE-02. Because damageBracket in viewModels mirrors weaponDamage, confirm the DAMAGE preview reflects the +2 (the characterSheetViewModel parity test guards this). Extend test/unit/combat.test.js: a Master of Arms fighter's weaponDamage (for a fixed weapon/level/rng) is exactly 2 higher than an otherwise-identical non-Master-of-Arms fighter.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/characterSheetViewModel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - weaponDamage returns exactly +2 for a Master of Arms fighter versus an identical non-Master-of-Arms fighter (same weapon/level/rng), provable by a test.
    - The sheet's DAMAGE preview (damageBracket) reflects the +2 (characterSheetViewModel parity test green).
    - No RNG-order change; `npm test` full suite green (parity/determinism intact).
  </acceptance_criteria>
  <done>All acceptance_criteria above hold and npm test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| dead-code gate → live resolution | Enabling a previously-unreachable branch must not grant unintended effects to characters outside the intended capability |
| new RNG draw → determinism | The now-live armour-patch d6 draw runs during newDay for Sewing/Master-of-Arms characters only |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04.1-03 | Elevation of Privilege | the bare `else { d4 }` branch patching armour for all characters if the gate is loosened too far | medium | mitigate | The new gate requires Sewing skill OR Master of Arms; a test asserts a plain character with damaged armour gets NO patch. |
| T-04.1-04 | Tampering | new newDay rng.d draw shifting RNG order and breaking parity | high | mitigate | The draw fires ONLY for Sewing/Master-of-Arms characters (never chargen); parity is chargen-order-bound (04.1-RESEARCH.md) and the full parity/determinism suites run in `npm test`; the deliberate change is documented in-code. |
| T-04.1-SC | Tampering | supply chain | n/a | accept | No npm/package installs in this phase. |
</threat_model>

<verification>
- `npm test` full suite green — movement/combat/characterSheetViewModel + parity/determinism/roundtrip all pass.
- The armour-patch block is proven reachable for Sewing and Master of Arms and proven NOT to fire for characters lacking both.
- weaponDamage delta of +2 for Master of Arms is asserted; the DAMAGE sheet preview matches.
- Deferred device-UAT: on the Pixel 7, a Sewing thief visibly regains armour at camp and a Master of Arms fighter hits harder.
</verification>

<success_criteria>
- CONTEXT success criterion 3 is met: Thief Sewing repairs armour in play and Master of Arms confers its "+2 with every weapon" bonus — both provable by a test.
</success_criteria>

<output>
Create `.planning/phases/04.1-rules-review-wiring-every-character-sheet-stat-affects-gamep/04.1-02-SUMMARY.md` when done.
</output>
