---
phase: 05-voice-content-graveyard
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - content/safety-wordlist.js
  - test/voice/safety-scan.test.js
  - content/voice-combat.js
  - content/voice-magic.js
  - content/voice-explore.js
  - content/voice-economy-item.js
  - src/browser/engineAdapter.js
  - tools/voice-sample.mjs
  - test/voice/adapter-integration.test.js
autonomous: true
requirements:
  - VOX-01
  - VOX-02
must_haves:
  truths:
    - "Every authored variant string, rendered with every representative + boundary token value, contains zero banned terms (word-boundary matched, allowlist-aware, Scunthorpe-safe) — the exhaustive scan is complete, not a sample"
    - "The live Oracle/event log narrates the previously-dropped engine event types (combat/magic/economy/encounter/item) via narrate(), while the 372-test baseline stays green"
    - "A large seeded random sample (~5000 narrate calls) is written to a reviewable artifact for the deferred human tone/funny UAT"
  artifacts:
    - content/safety-wordlist.js
    - test/voice/safety-scan.test.js
    - tools/voice-sample.mjs
    - test/voice/adapter-integration.test.js
  key_links:
    - "src/browser/engineAdapter.js#formatEvent -> presentation/voice.js#narrate (only for explicitly-mapped, currently-dropped types; returns plain text, formatEvent wraps the span)"
    - "test/voice/safety-scan.test.js renders VOICE_BANKS x closed-vocab tokens + content/epitaphs.js banks and scans against content/safety-wordlist.js with \\b word-boundary + allowlist"
---

<objective>
Make the voice real and provably safe: broaden the authored variant corpus for tonal richness, wire narrate() into the live browser Oracle/event log so the previously-dropped combat/magic/economy/encounter/item events finally narrate (VOX-01 integration), and stand up the VOX-02 exhaustive family-friendly safety guardrail plus its vendored zero-dependency wordlist and a human-review sample artifact.

Purpose: The safety scan is the standing guardrail (like the round-trip suite) that prevents procedural combinatorics from ever shipping inappropriate copy — the store-rating-grade risk PITFALLS.md flagged. Integration proves the "one place Events become narrative copy" seam end-to-end without regressing the 372 green tests or the engine-purity / determinism / parity contracts.

Output: content/safety-wordlist.js (vendored + curated, licensed), test/voice/safety-scan.test.js (exhaustive), broadened content/voice-*.js banks, the narrate() wiring in src/browser/engineAdapter.js, tools/voice-sample.mjs (human-review artifact generator), and test/voice/adapter-integration.test.js.

## Slice framing (MVP)
This is slice 2 of 3. It builds directly on 05-01's generator (narrate + category map + banks) and delivers the user-observable increment: the running game now speaks its sarcastic voice in the log for the full event vocabulary, and that voice is machine-verified family-friendly.

## Artifacts this plan produces
- content/safety-wordlist.js — vendored (LDNOOBW en, CC BY 4.0, attributed) + hand-curated slur/gore/adult supplement + Scunthorpe allowlist
- test/voice/safety-scan.test.js — the exhaustive VOX-02 guardrail
- tools/voice-sample.mjs — ~5000-call seeded sample -> reviewable artifact
- src/browser/engineAdapter.js (modified) — narrate() wired into formatEvent
- test/voice/adapter-integration.test.js — integration + rngState-isolation-through-the-adapter regression
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-voice-content-graveyard/05-CONTEXT.md
@.planning/phases/05-voice-content-graveyard/05-RESEARCH.md
@.planning/phases/05-voice-content-graveyard/05-01-SUMMARY.md

# The generator + banks this plan integrates and scans
@presentation/voice.js
@presentation/eventCategories.js
@content/voice-index.js
@content/VOICE-STYLE-GUIDE.md
# The integration point (modify formatEvent's fall-through only) + its test contract
@src/browser/engineAdapter.js
@test/unit/engineAdapter.test.js
# Closed-vocabulary sources the exhaustive scan must render tokens from + allowlist against
@content/names.js
@content/races.js
@content/flavor.js
@content/bestiary.js
@content/epitaphs.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendored safety wordlist + exhaustive family-friendly scan (VOX-02 guardrail)</name>
  <files>content/safety-wordlist.js, test/voice/safety-scan.test.js</files>
  <read_first>
    - content/VOICE-STYLE-GUIDE.md — the family-friendly contract the scan enforces
    - content/voice-index.js — VOICE_BANKS (the corpus to render exhaustively)
    - content/epitaphs.js — EPITAPHS + CAUSE_TEXT (death copy is rendered content too; include it in the scan corpus)
    - content/names.js, content/races.js, content/flavor.js (SUB_NOTE keys = subclasses), content/bestiary.js (creature `n:` names) — the CLOSED token vocabularies to render into templates AND to seed the Scunthorpe allowlist
    - test/determinism/content-is-pure-data.test.js — the guard content/safety-wordlist.js must pass (pure data, no functions)
  </read_first>
  <action>
    Vendor content/safety-wordlist.js as PURE DATA only: exported arrays of banned terms grouped by category (hard profanity/crude, slurs, gore/graphic-violence, sexual/adult) plus an ALLOWLIST array of known-safe game vocabulary that would otherwise collide (Scunthorpe exceptions). Seed the profanity/crude category from the LDNOOBW "List of Dirty, Naughty, Obscene, and Otherwise Bad Words" (en). Before vendoring: VERIFY the source is still CC BY 4.0 and skim the current list (per RESEARCH Assumptions A2 — do not trust the research pass); include a header comment with attribution (source URL + CC BY 4.0). LDNOOBW is profanity-focused only — hand-curate small supplementary arrays for slurs and gore/adult terms this game's dark tone could risk. Keep this file zero-runtime-dependency and function-free (it must pass the content-purity guard). Do NOT put any matching/normalization LOGIC here — that lives in the test.

    Write test/voice/safety-scan.test.js — the EXHAUSTIVE guardrail (not a sample). It must: (1) build a small deterministic set of representative + boundary token values from the closed vocabularies (every race name, every subclass, a couple of character names, every creature `n:` name for {foe}, min/typical/max plausible depth+day+gold+sp, a couple of dmg/roll values); (2) render EVERY variant string in EVERY VOICE_BANKS category (and every EPITAPHS bank + CAUSE_TEXT template) with EVERY token value — this is the full combinatorial space for this CLOSED corpus, complete in milliseconds; (3) scan every rendered string against every banned term using CASE-INSENSITIVE WORD-BOUNDARY regex (\b...\b), skipping any match that is on the allowlist; (4) assert zero banned matches, listing offending (category, variant, token, term) tuples on failure. Include a one-time sanity sub-test that scans ALL closed-vocabulary source strings (names/races/subclasses/creatures/existing epitaphs) against the wordlist and asserts every resulting collision is covered by the allowlist — this proves the allowlist is complete and the scan won't false-positive on legitimate game words.

    NOTE (comment discipline): do NOT paste any actual banned word into this plan-derived source as an inline example or "what not to match" comment — reference categories abstractly; the only place literal banned terms appear is content/safety-wordlist.js's data arrays.
  </action>
  <verify>
    <automated>node --test test/voice/safety-scan.test.js test/determinism/content-is-pure-data.test.js</automated>
  </verify>
  <done>The exhaustive scan renders the entire closed corpus (all voice banks + epitaphs x all closed-vocab tokens) and passes with zero banned matches; the allowlist sanity sub-test confirms no false-positive on existing game vocabulary; content/safety-wordlist.js is pure data, licensed, and attributed.</done>
</task>

<task type="auto">
  <name>Task 2: Broaden the variant banks for tonal richness (guarded by the scan)</name>
  <files>content/voice-combat.js, content/voice-magic.js, content/voice-explore.js, content/voice-economy-item.js</files>
  <read_first>
    - content/VOICE-STYLE-GUIDE.md — tone contract + token vocabulary
    - content/epitaphs.js — the density/quality bar for authored sarcasm to match
    - test/voice/safety-scan.test.js (Task 1) — the guardrail every new line must pass
  </read_first>
  <action>
    Expand each category's variant array for breadth and tonal variety so the log does not read repetitively across a run — target roughly 6-12 variants for the high-frequency combat/magic/explore categories and 3-6 for rarer ones, using the {token} vocabulary (name/foe/race/sub/floor/day/sp/gold/dmg/roll/level). Preserve the pure-data {text, weight} shape (no functions). Every added line MUST satisfy the family-friendly contract — run the safety scan after authoring; it is the acceptance gate. Keep the deadpan, self-aware, trope-poking voice consistent with content/epitaphs.js and the style guide. Do not introduce new categories here (the taxonomy + coverage were locked in 05-01); only enrich existing banks.
  </action>
  <verify>
    <automated>node --test test/voice/safety-scan.test.js test/voice/generator.test.js test/determinism/content-is-pure-data.test.js</automated>
  </verify>
  <done>Banks are broadened for the full taxonomy; the exhaustive safety scan and the generator/purity guards all stay green over the enlarged corpus.</done>
</task>

<task type="auto">
  <name>Task 3: Wire narrate() into the live Oracle log + human-review sample tool</name>
  <files>src/browser/engineAdapter.js, tools/voice-sample.mjs, test/voice/adapter-integration.test.js</files>
  <read_first>
    - src/browser/engineAdapter.js#formatEvents / #formatEvent — the integration point (module-level `currentState` is available inside formatEvent)
    - test/unit/engineAdapter.test.js — the EXACT assertions that must stay green: floorChanged->"Floor 3", died->"died", won->"Gate", leveled includes "2", moved drops, and an UNKNOWN type drops silently (html.length stays 4)
    - presentation/voice.js#narrate + presentation/eventCategories.js#EVENT_CATEGORY — what formatEvent will call
    - engine/rng.js — reminder: pass ONLY currentState.seed / currentState.steps to narrate, NEVER currentState.rngState
  </read_first>
  <action>
    Wire narrate() into formatEvent WITHOUT breaking the 372 baseline. The rule: route through narrate ONLY event types that are explicitly present in EVENT_CATEGORY AND not already handled by an existing explicit `case`. Concretely, change the `default:` branch (currently `return null`) to: if EVENT_CATEGORY[e.type] exists, compute plain text = narrate(e, buildCtx(currentState), currentState?.seed, ordinal) and return it wrapped in a `<span class="...">` whose class is derived from the event's category family (a small CATEGORY_SPAN map: combat.hit/crit/kill + magic.hit + meta.* positives -> "hit"; combat.miss/foeMiss + magic.miss/fail -> "miss"; combat.foeHit/status + explore.affliction/hazard + meta.hunger -> "hurt"; rolls -> "roll"; everything else -> "beat"). If EVENT_CATEGORY[e.type] is absent, still `return null` (so a truly-unknown/synthetic type keeps dropping silently — this is what keeps the engineAdapter.test.js "drops unknown ones" assertion green). LEAVE every existing explicit `case` (moved, floorChanged, died, won, leveled, dayBegan, teleported, spGained, afflictionTick, ... ) UNCHANGED so their exact-string tests keep passing; Phase 4's presentation rewrite unifies them later. Add buildCtx(state): derive the token context from state.c (name, sub, race, cls, sp, gold, level->lvl) and state (floor.depth->floor/depth, day, steps) — and it MUST NOT throw when state is null (return safe empty defaults), because formatEvents is called directly in a test without a live state. Pass the event's index within the batch as `ordinal` (thread it from formatEvents' map callback) so repeated same-type events in one dispatch can vary. engineAdapter must never reference Math.random or the DOM (its existing purity test stays green) and must never read currentState.rngState.

    Write tools/voice-sample.mjs: a Node script that seeds a deterministic run, enumerates a large random-but-seeded set of ~5000 (event, ctx) combinations across the category taxonomy, calls narrate() for each, and writes the rendered lines to a reviewable artifact file (e.g. tools/voice-sample-output.txt) for the DEFERRED human tone/funny UAT. This is a tool, not a pass/fail test — it is a second-layer regression net and the artifact for the human review, NOT the VOX-02 safety guarantee (the exhaustive scan is). It must be re-runnable and produce identical output for a fixed seed.

    Write test/voice/adapter-integration.test.js: install a fake localStorage (mirror engineAdapter.test.js's withFakeLocalStorage), initRun a seed, dispatch a real action, and assert (a) formatEvents now emits a narrated `<span>` line for a previously-dropped mapped type (e.g. a combat/movement-adjacent event present in the batch) — proving integration; (b) capturing currentState.rngState before and after a formatEvents pass over a synthetic events array leaves rngState byte-identical (Pitfall-1 regression through the real adapter path); (c) a synthetic event with a type NOT in EVENT_CATEGORY still drops (returns nothing).
  </action>
  <verify>
    <automated>node --test test/voice/adapter-integration.test.js test/unit/engineAdapter.test.js && node tools/voice-sample.mjs && node --test test/unit test/determinism test/roundtrip test/parity</automated>
  </verify>
  <done>formatEvent narrates every explicitly-mapped, previously-dropped event type via narrate() while all existing engineAdapter.test.js assertions stay green; unknown types still drop; rngState is provably untouched through the adapter; tools/voice-sample.mjs regenerates the ~5000-line human-review artifact deterministically; the full suite (unit/determinism/roundtrip/parity — the 372 baseline plus new tests) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| engine events -> formatEvent -> DOM log | narrate() returns PLAIN TEXT; formatEvent is the only place markup (`<span>`) is applied, keeping banned-substring matching straddle-free and the "one place knows markup" rule intact. |
| authored corpus -> released app | The exhaustive scan gates every variant before it can reach a player (a content-rating / store-compliance concern for Phase 6). |

Offline, single-player, ASVS L1. No new network/account boundary.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Information Disclosure (rating/reputational integrity) | authored variant corpus | high | mitigate | Exhaustive word-boundary, allowlist-aware safety scan over the COMPLETE closed corpus (every variant x every closed-vocab token + epitaphs) on every content change — complete, not sampled (Pitfall 2). |
| T-05-05 | Tampering | src/browser/engineAdapter.js narration path | high | mitigate | formatEvent passes only currentState.seed/steps to narrate; adapter-integration.test.js asserts rngState is byte-identical across a formatEvents pass; engineAdapter purity test keeps Math.random/DOM out. |
| T-05-06 | Tampering (Scunthorpe false-negative/positive) | test/voice/safety-scan.test.js | medium | mitigate | \b word-boundary + case-insensitive match + curated allowlist proven complete against all closed game vocabulary in the scan's sanity sub-test. |
| T-05-07 | Tampering (supply chain, vendored data) | content/safety-wordlist.js | medium | mitigate | Wordlist vendored as DATA (not an npm package — zero-runtime-dep preserved); LDNOOBW CC BY 4.0 license re-verified at implementation time + attributed; no executable code, no injection surface. |

No npm/pip/cargo package installs this phase — the wordlist is vendored data, so no package-legitimacy checkpoint applies.
</threat_model>

<verification>
- node --test test/voice/safety-scan.test.js — exhaustive family-friendly corpus scan green (VOX-02)
- node --test test/voice/adapter-integration.test.js test/unit/engineAdapter.test.js — narrate integration + rngState isolation, existing adapter contract intact
- node tools/voice-sample.mjs — regenerates the deterministic human-review artifact (VOX-02 deferred tone UAT input)
- node --test test/unit test/determinism test/roundtrip test/parity — 372 baseline + new tests all green
- node --test test/determinism/content-is-pure-data.test.js — wordlist + broadened banks stay pure data
</verification>

<success_criteria>
- Exhaustive safety scan is complete (not sampled), word-boundary + allowlist (Scunthorpe-safe), green over the whole authored corpus incl. epitaphs
- narrate() narrates the full previously-dropped event vocabulary in the live log; the 372 tests + engine-purity + parity/determinism/round-trip stay green
- Wordlist vendored zero-dep, license-verified + attributed; human-review sample artifact regenerates deterministically
- engineAdapter never touches currentState.rngState during narration (regression-tested)
</success_criteria>

<output>
Create `.planning/phases/05-voice-content-graveyard/05-02-SUMMARY.md` when done.
</output>
