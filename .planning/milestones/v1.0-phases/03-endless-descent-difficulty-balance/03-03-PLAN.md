---
phase: 03-endless-descent-difficulty-balance
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/browser/engineAdapter.js
  - mazeworld.html
  - test/unit/engineAdapter.test.js
  - test/unit/new-run-loop.test.js
  - test/unit/newrun.test.js
  - test/unit/permadeath.test.js
autonomous: true
requirements: [RUN-01, RUN-04, RUN-05]
must_haves:
  truths:
    - "From a dead run, a single adapter call starts a fresh, valid, dice-rolled run (floor 1, level 1, new seed)"
    - "The browser prototype's existing death-card 'Roll another delver' button routes through the engine adapter, closing the run loop in the dev harness"
    - "The best depth reached persists across runs within the dev loop (localStorage), ready for Phase 2 to relocate to durable native storage"
    - "newRun(seed) produces a 100%-dice-rolled character with zero player choices, on a revealed floor 1"
    - "Character death is permanent — there is no revive/undo/continue; die() is the sole terminator and a new run is a fresh character, not a resurrection"
  artifacts:
    - "test/unit/new-run-loop.test.js"
    - "test/unit/newrun.test.js"
    - "test/unit/permadeath.test.js"
  key_links:
    - "engineAdapter.startNewRun() records the ending run's floor.depth into a best-depth localStorage key, then calls initRun(newSeed) — the dev-loop stand-in for the durable best-depth Phase 2/SAV-04 will persist natively"
    - "mazeworld.html overwrites window.newGame (mirroring the existing window.move override) so the death-card btn-again onclick routes through the engine adapter without touching the DOM inside the adapter module"
---

<objective>
Close the run loop: wire the prototype's EXISTING death card + 'Roll another delver' button
through the engine adapter so a single tap from death starts a fresh dice-rolled run, and
track best-depth in the dev loop (localStorage) as the Phase-2-ready seam for a durable high
score. Plus the RUN-coverage tests proving the dice-rolled character (RUN-01), permadeath as
the sole terminator (RUN-04), and the one-tap new-run loop (RUN-05). This is engine-adapter
GLUE, NOT mobile UI design — the polished death screen / score display is Phase 4's contract.
(RUN-01, RUN-04, RUN-05; honors CONTEXT locked decisions: one-tap newRun(newSeed) from the
death state, score = deepest floor reached, permadeath is the only terminator, best-depth in
serializable local storage for Phase 2 to make durable.)

Purpose: make the endless run loop closable and testable end-to-end in the browser dev harness.
Output: adapter one-tap new-run + best-depth tracking, death-card wiring in mazeworld.html,
and RUN-01/04/05 coverage tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-endless-descent-difficulty-balance/03-CONTEXT.md
@.planning/phases/03-endless-descent-difficulty-balance/03-RESEARCH.md
@.planning/phases/03-endless-descent-difficulty-balance/03-VALIDATION.md

# The adapter, its host page, its tests, and the engine factory it drives:
@src/browser/engineAdapter.js
@test/unit/engineAdapter.test.js
@engine/state.js
@engine/death.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapter one-tap new-run + best-depth tracking; wire mazeworld.html death card</name>
  <read_first>
    - src/browser/engineAdapter.js (whole file: initRun lines 41-44, boot lines 53-68,
      persist lines 73-80, dispatch lines 89-110; the SAVE_KEY constant line 28; note the
      purity test forbids `document` and `Math.random` INSIDE this module)
    - test/unit/engineAdapter.test.js (the withFakeLocalStorage stub lines 23-37 and the
      purity test lines 125-129 — new code must stay DOM-free and Math.random-free)
    - engine/state.js (newRun(seed) — the one-call fresh-run factory the adapter invokes)
    - mazeworld.html lines 3266-3301 (the existing `<script type="module">` that imports
      { boot, dispatch } and overwrites window.move) and lines 3013-3057 (armAgain / the
      death-card btn-again onclick that currently calls the prototype's newGame())
    - 03-RESEARCH.md "one-tap new run" Code Examples (option A: adapter calls the factory
      directly) and Architectural Responsibility Map (best-depth row)
  </read_first>
  <files>src/browser/engineAdapter.js, mazeworld.html, test/unit/engineAdapter.test.js</files>
  <action>
    In src/browser/engineAdapter.js (keep the module DOM-free and Math.random-free — the purity
    test enforces this; localStorage and Date.now are already used here and remain allowed):
    add a BEST_KEY constant "mazeworld.best.v1". Add getBest() returning the stored best depth as
    a number (0 if absent/blocked/corrupt; never throws — same try/catch posture as boot/persist).
    Add a module-internal recordBest(depth) that reads the stored best, writes max(stored, depth)
    back to BEST_KEY, and never throws. Add startNewRun(seed) — the one-tap new-run entry point
    (option A per 03-RESEARCH.md): if a currentState exists, recordBest(currentState.floor.depth)
    to capture the ending run's deepest floor (descent is one-way, so floor.depth IS the deepest
    reached = the run score per 03-CONTEXT.md); then call initRun(typeof seed === "number" ? seed :
    Date.now()) and return the fresh state. Guard the seed as an integer (Date.now() fallback) so a
    malformed seed can't reach newRun unchecked (Security Domain V5; threat T-03-05). Document that
    best-depth lives in localStorage ONLY as the browser dev-loop stand-in; Phase 2 (SAV-04)
    relocates it to durable Capacitor Preferences — the seam is intentionally minimal.

    In mazeworld.html's existing `<script type="module">` (lines 3266-3301): also import
    startNewRun from ./src/browser/engineAdapter.js, and overwrite window.newGame with an
    engineNewRun function that calls startNewRun(), sets window.__mzState with the returned state,
    and repaints (window.paint(); window.draw(); and a logLine banner) — mirroring the existing
    window.move override exactly. Because the classic-script `function newGame(){...}` created a
    mutable window.newGame property and the death-card btn-again onclick calls the bare newGame()
    identifier (armAgain line 3031 and the won-path line 3057), overwriting window.newGame reroutes
    the death-card 'Roll another delver' button through the engine adapter with no change to the
    death-card markup or the DOM code inside the adapter module. Add a short comment documenting
    this is the RUN-05 one-tap new-run wiring and that the polished mobile death screen is Phase 4.

    In test/unit/engineAdapter.test.js: add tests (using withFakeLocalStorage) that
    (a) startNewRun(seed) after a prior run returns a fresh state (floor.depth 1, the new seed) and
    swaps it in as currentState; (b) startNewRun records the ending run's floor.depth into getBest()
    — e.g. initRun, set the current state's floor.depth to a deeper value, call startNewRun, assert
    getBest() reflects that deeper value; (c) getBest() returns 0 when nothing is stored and never
    throws when storage is blocked. Keep the existing purity test — the new adapter code must not
    reference document or Math.random.
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js</automated>
  </verify>
  <done>
    engineAdapter exports startNewRun + getBest; startNewRun records best-depth then returns a fresh
    run; the module stays DOM-free and Math.random-free (purity test green). mazeworld.html's module
    script overwrites window.newGame so the death-card button routes through the adapter.
  </done>
</task>

<task type="auto">
  <name>Task 2: RUN-01/04/05 coverage tests — dice-rolled chargen, permadeath, one-tap loop</name>
  <read_first>
    - engine/state.js (newRun(seed): rollCharacter → genFloor(1) → reveal; the fields it sets)
    - engine/character.js (rollCharacter — 100%-dice-rolled, level 1, no player-choice inputs)
    - engine/death.js (die() sets state.dead; there is no revive/undo/continue anywhere)
    - src/browser/engineAdapter.js (startNewRun / initRun / getBest from Task 1)
    - test/unit/engineAdapter.test.js (the withFakeLocalStorage stub pattern to reuse)
    - 03-VALIDATION.md Per-Requirement Verification Map (RUN-01 newrun, RUN-04 permadeath,
      RUN-05 new-run-loop)
  </read_first>
  <files>test/unit/newrun.test.js, test/unit/permadeath.test.js, test/unit/new-run-loop.test.js</files>
  <action>
    Create test/unit/newrun.test.js (RUN-01): assert newRun(seed) (imported from engine/state.js or
    engine/engine.js) takes ONLY a seed — no player-choice parameters — and produces a valid
    dice-rolled character at level 1 with a non-empty name, cls/sub/race populated from the content
    tables, on a fresh floor (depth 1) that has been revealed (the spawn cell and its neighbors have
    seen === true). Assert two different seeds produce different characters (chargen is seed-driven)
    and the same seed reproduces the same character (determinism), so the roll is 100% dice with zero
    choices. (The character-sheet UI reveal itself is Phase 4; this proves the engine precondition.)

    Create test/unit/permadeath.test.js (RUN-04): starting from newRun(seed), call die() (from
    engine/death.js) with a cause and assert state.dead becomes true; assert there is no engine action
    or field that revives/undoes/continues a dead run — e.g. after death, applyAction with any normal
    action (move/attack) is a no-op that leaves state.dead true (move() short-circuits on state.dead),
    proving death is permanent for that character. Assert die() (not any Gate/winGame) is the terminator.

    Create test/unit/new-run-loop.test.js (RUN-05): using withFakeLocalStorage, initRun(seed), advance
    or force the run to a dead state (e.g. set floor.depth deep and mark it dead, or drive die via the
    engine), then call startNewRun() ONCE and assert the returned/current state is a brand-new valid
    run — floor.depth 1, character level 1, a different seed, dead === false — i.e. a single call from
    the death state yields a fresh dice-rolled run (the one-tap loop). Assert getBest() captured the
    ended run's deepest floor. Reuse the fakeLocalStorage helper (copy the small stub from
    engineAdapter.test.js or import a shared pattern) so the adapter's localStorage calls are stubbed.
  </action>
  <verify>
    <automated>node --test test/unit/newrun.test.js test/unit/permadeath.test.js test/unit/new-run-loop.test.js</automated>
  </verify>
  <done>
    newrun.test.js proves 100%-dice-rolled, choice-free, revealed floor-1 chargen (RUN-01).
    permadeath.test.js proves death is permanent with no revive/undo/continue (RUN-04).
    new-run-loop.test.js proves one adapter call from death yields a fresh run + records best-depth (RUN-05).
    All three are green under `node --test`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → adapter → engine | The death-card button and the new-run seed are the inputs crossing into the engine this plan adds. save-file → engine remains the pre-existing boundary (unchanged; validateSave still governs load). Offline single-player; no network/auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | startNewRun(seed) | low | mitigate | Guard seed as an integer with a Date.now() fallback before it reaches newRun, matching the isInt discipline the engine's action validators already use — a malformed/adversarial seed can't reach newRun unchecked. |
| T-03-06 | Tampering | best-depth localStorage value | low | accept | A self-edited best-depth is trivial single-player self-tampering with no leaderboard to protect (03-RESEARCH Security Domain / PITFALLS P12); getBest() coerces to a number and defaults to 0 on any non-numeric/blocked read so it never crashes the loop. |
| T-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | ZERO runtime deps; no installs this phase. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
- `node --test test/unit/engineAdapter.test.js` green (new startNewRun/getBest + purity).
- `node --test test/unit/newrun.test.js test/unit/permadeath.test.js test/unit/new-run-loop.test.js` green.
- `node --test` (full suite) green — this plan adds adapter exports + tests and one mazeworld.html
  glue block; it does not modify engine rule modules, so all prior tests stay green.
- Manual (dev-loop, deferred to milestone-end UAT per the autonomous run): open mazeworld.html, die,
  tap 'Roll another delver', confirm a fresh dice-rolled run starts through the engine adapter.
</verification>

<success_criteria>
- RUN-01: newRun(seed) yields a 100%-dice-rolled, choice-free character on a revealed floor 1.
- RUN-04: die() is the sole terminator; death is permanent with no revive/undo/continue.
- RUN-05: a single adapter call from the death state starts a fresh run; the death-card button routes through the engine adapter.
- Best-depth persists in the dev loop (localStorage) as the Phase-2-ready high-score seam.
</success_criteria>

<output>
Create `.planning/phases/03-endless-descent-difficulty-balance/03-03-SUMMARY.md` when done.
</output>
