---
phase: 01-engine-extraction-determinism
plan: 10
type: execute
wave: 8
depends_on: ["01-06", "01-07", "01-08", "01-09"]
files_modified:
  - engine/economy.js
  - engine/encounters.js
  - engine/engine.js
  - test/unit/economy.test.js
  - test/unit/encounters.test.js
  - test/parity/fixtures/action-script.economy.json
  - test/parity/fixtures/action-script.encounters.json
  - test/parity/economy-parity.test.js
  - test/parity/full-suite.test.js
autonomous: true
requirements: [ENG-01, ENG-03, ENG-04, ENG-05]
must_haves:
  truths:
    - "The store's buy closures are eliminated: stock entries are plain data {n,sub,cost,effectId,effectParams} and an engine-side STORE_EFFECTS lookup applies the purchase — the store now serializes and survives save/reload"
    - "openStore/buyFrom/leaveStore route through applyAction with plain-data stock, matching the prototype"
    - "The movement feature-trigger stubs (encounterDot/springTrap/openChest) are replaced with the real handlers wiring into combat/economy/magic/faerie/joiner/affliction"
    - "The full test suite is green: every ENG requirement (01-05) plus round-trip and parity across chargen/movement/combat/magic/economy/encounters fixtures"
  artifacts:
    - path: "engine/economy.js"
      provides: "openStore/buyFrom/leaveStore + STORE_EFFECTS lookup (plain-data stock, no closures)"
      exports: ["openStore", "buyFrom", "leaveStore", "STORE_EFFECTS"]
    - path: "engine/encounters.js"
      provides: "encounterDot/springTrap/openChest/tableFour/findFood/findGrimoire/findGear/findMisc/meetFaerie/meetJoiner/catchAffliction/goInsane/newPhobia/fallDark (RNG-injected)"
      exports: ["encounterDot", "springTrap", "openChest"]
    - path: "test/parity/full-suite.test.js"
      provides: "aggregate parity+round-trip run across all fixtures (ENG-05 phase gate)"
  key_links:
    - from: "engine/economy.js"
      to: "engine/items.js + content/index.js"
      via: "STORE_EFFECTS applies plain-data purchases via giveItem/takeItem; stock built from WEAPONS/ARMORS/FOODS/POTIONS"
      pattern: "STORE_EFFECTS|effectId"
    - from: "engine/encounters.js"
      to: "engine/combat.js + engine/economy.js"
      via: "encounterDot dispatches to startCombat / openStore / meetFaerie / catchAffliction per the encounter tables"
      pattern: "startCombat|openStore"
    - from: "engine/movement.js"
      to: "engine/encounters.js"
      via: "movement feature stubs replaced by real encounterDot/springTrap/openChest handlers"
      pattern: "encounterDot|springTrap|openChest"
---

<objective>
Close the extraction: eliminate the store's buy closures (the one non-serializable data the
prototype itself flags) by converting stock to plain `{effectId, effectParams}` descriptors with
an engine-side effect lookup; port the encounter/trap/chest domain and replace the movement
feature-trigger stubs with real handlers; and land the full-suite parity + round-trip gate proving
zero regressions across the entire ruleset.

Purpose: ENG-03/ENG-04 (store becomes plain data that serializes and survives save/reload — the
CONTEXT-flagged anti-pattern fixed), ENG-01 (encounters pure, no DOM), and ENG-05 (the whole run —
chargen, movement, combat, magic, economy, encounters, death, descent — matches the prototype). The
encounter layer is the glue that ties movement to combat/economy, so it comes last.

Output: engine/economy.js, engine/encounters.js, the stub-replacement wiring in movement, unit
tests, economy/encounters parity fixtures, and the aggregate full-suite gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-engine-extraction-determinism/01-RESEARCH.md
@.planning/phases/01-engine-extraction-determinism/01-VALIDATION.md
@.planning/phases/01-engine-extraction-determinism/SKELETON.md
@mazeworld.html
@engine/engine.js
@engine/movement.js
@engine/combat.js
@engine/magic.js
@engine/items.js
@content/index.js
@test/parity/harness/sandboxPrototype.js
@test/parity/harness/diffState.js
</context>

## Phase Goal

**As a** Mazeworld player, **I want to** trigger encounters, shops, traps, and chests as I explore and have them run through the deterministic engine, **so that** the full crawl is preserved exactly — with even the shop now safe to save and resume.

<artifacts>
- `engine/economy.js` — `openStore(state, rng, events)` (plain-data stock), `buyFrom(state, idx, events)`, `leaveStore(state, events)`, `STORE_EFFECTS` (effectId → (state,params,events)=>void lookup)
- `engine/encounters.js` — `encounterDot`, `springTrap`, `openChest`, `tableFour`, `findFood`, `findGrimoire`, `findGear`, `findMisc`, `meetFaerie`, `meetJoiner`, `catchAffliction`, `goInsane`, `newPhobia`, `fallDark` (RNG-injected, event-emitting)
- movement stub replacements (in engine/movement.js) calling the real handlers
- tests: `economy.test.js`, `encounters.test.js`, `economy-parity.test.js`, `full-suite.test.js`, `action-script.economy.json`, `action-script.encounters.json`
- applyAction dispatch cases: `buyItem`, `leaveStore`, `useItem`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Store closures → plain-data stock + STORE_EFFECTS lookup (ENG-03/ENG-04)</name>
  <files>engine/economy.js, engine/engine.js, test/unit/economy.test.js</files>
  <read_first>
    - mazeworld.html (lines 1998-2055 openStore/buyFrom/leaveStore: the `add(n,cost,buy,sub)` closures — FOODS eat, POTIONS give, lockpicks, armor repair, weapon/armor buy via takeItem, MU scroll, premium rollBlade/rollMailPiece; buyFrom calls item.buy(); the line 3182-3184 comment that store stock holds closures and is excluded from save)
    - .planning/research/ARCHITECTURE.md (Anti-Pattern 3 + Pattern 3: callback-bearing descriptors → id + lookup-table); .planning/phases/01-engine-extraction-determinism/01-RESEARCH.md (Pattern 3: {effectId, effectParams} + STORE_EFFECTS)
    - engine/items.js (giveItem/takeItem/hasPicks/rollBlade/rollMailPiece), content/index.js (FOODS/POTIONS/WEAPONS/ARMORS, priceFor)
    - engine/engine.js (add buyItem/leaveStore dispatch)
  </read_first>
  <action>
    Create engine/economy.js. Port openStore(state,rng,events) building state.store={stock,haggle,race}
    where each stock entry is PLAIN DATA: {n, sub, cost, effectId, effectParams} — replace every
    `add(..., () => sideEffect())` closure with a stable effectId and its params (eatRation{wp},
    givePotion{potion}, giveLockpicks, repairArmor, buyWeapon{name}, buyArmor{name}, buyScroll,
    buyPremium{item}). Implement STORE_EFFECTS as an engine-side map effectId → (state,params,events)=>void
    calling the item helpers (giveItem/takeItem/etc.). Port buyFrom(state,idx,events): gold check, mark
    sold, apply STORE_EFFECTS[stock[idx].effectId](state, stock[idx].effectParams, events) — never store
    or call a function held in state. Port leaveStore. Keep priceFor/haggle/Troll/Elf-Dwarf pricing and
    the premium (rollBlade/rollMailPiece) exactly. Wire engine/engine.js dispatch for "buyItem"(idx),
    "leaveStore", and "useItem"(i). The store must now be included in serializeRun (no longer excluded)
    since it is plain data.
  </action>
  <verify>
    <automated>node --test test/unit/economy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - state.store.stock entries contain no function-typed leaves (content-purity-style assertion) and carry effectId/effectParams
    - buyFrom applies the correct STORE_EFFECTS effect, deducts gold, and marks the entry sold — matching the prototype's outcomes
    - A GameState with an open store JSON round-trips deepStrictEqual (the store now survives save/reload — ENG-04)
    - economy.js references no Math.random/document/localStorage
  </acceptance_criteria>
  <done>The store's closures are gone: stock is plain data applied via an engine-side effect lookup, and an open shop now serializes losslessly.</done>
</task>

<task type="auto">
  <name>Task 2: Encounters/traps/chests + replace the movement feature stubs</name>
  <files>engine/encounters.js, engine/movement.js, test/unit/encounters.test.js</files>
  <read_first>
    - mazeworld.html (lines 2058-2249 springTrap (TRAPS via rng, avoid roll, poison affliction, die), openChest (lockpicks, gold via gainWilmst, treasure), encounterDot (ENCOUNTER_TABLES d8 then d10, dispatch: monster types→startCombat, Store→openStore, Food/Misc Magic/Grimoire/Faerie/Joiner/Teleport/Disease/Insanity/Phobia/Darkness/table-four WP-SP-WM, Magic Weapon/Armor), tableFour, findFood/findGrimoire/findGear/findMisc, meetFaerie, meetJoiner, catchAffliction, goInsane, newPhobia, fallDark)
    - engine/movement.js (the encounterDot/springTrap/openChest stubs to replace), engine/combat.js (startCombat), engine/economy.js (openStore), engine/items.js (gainWilmst/takeItem/giveItem/rollTreasureItem), content/index.js (ENCOUNTER_TABLES/ENC_ALIAS/TRAPS/AFFLICTIONS/FAERIE/MISC_MAGIC/PHOBIAS/INSANITY)
  </read_first>
  <action>
    Create engine/encounters.js porting springTrap(state,rng,events) (TRAPS pick via rng with the times
    flag, avoid-on-1-5 d20, poison→catchAffliction, die on lethal), openChest(state,rng,events)
    (lockpicks gate, gold via gainWilmst, treasure via rollTreasureItem/takeItem), encounterDot(state,rng,
    events) (ENCOUNTER_TABLES[d8][d10] resolution dispatching to startCombat for monster types via
    ENC_ALIAS, openStore for Store, and findFood/findGrimoire/findGear/findMisc/meetFaerie/meetJoiner/
    catchAffliction/goInsane/newPhobia/fallDark/teleport/tableFour for the rest), and all those helpers
    with rng-injected rolls and emitted events. Then replace the movement feature-trigger stubs in
    engine/movement.js (from plan 01-07) so a "dot" cell calls encounterDot, "trap" calls springTrap,
    "chest" calls openChest — matching the prototype's move() feature dispatch order exactly. Keep arrays
    as arrays (no Map/Set). No global S; no DOM.
  </action>
  <verify>
    <automated>node --test test/unit/encounters.test.js</automated>
  </verify>
  <acceptance_criteria>
    - springTrap/openChest/encounterDot are pure, RNG-injected, and emit events; encounterDot resolves ENCOUNTER_TABLES[d8][d10] and dispatches to combat/store/faerie/etc. matching the prototype
    - Movement's dot/trap/chest cells now trigger the real handlers (stubs removed); moving onto a dot can start combat or open a store deterministically
    - A trap that reduces wp to 0 routes through die(); a chest awards gold/treasure via the item helpers
    - encounters.js references no Math.random/document/localStorage
  </acceptance_criteria>
  <done>Encounters, traps, and chests are ported and wired into movement, completing the feature-trigger chain.</done>
</task>

<task type="auto">
  <name>Task 3: Economy/encounters parity fixtures + full-suite parity &amp; round-trip gate</name>
  <files>test/parity/fixtures/action-script.economy.json, test/parity/fixtures/action-script.encounters.json, test/parity/economy-parity.test.js, test/parity/full-suite.test.js, test/roundtrip/serialize-rehydrate.test.js</files>
  <read_first>
    - test/parity/harness/sandboxPrototype.js + diffState.js; existing chargen/movement/combat/magic parity tests (the pattern + fixtures to aggregate)
    - .planning/phases/01-engine-extraction-determinism/01-VALIDATION.md (Per-Task Verification Map — every ENG requirement's automated command; the phase-gate expectation of a green full suite)
    - test/roundtrip/serialize-rehydrate.test.js (the standing guardrail to extend across all fixtures)
    - mazeworld.html (2044-2055 buyFrom/leaveStore; 2107-2149 encounterDot dispatch — for fixture authoring)
  </read_first>
  <action>
    Author test/parity/fixtures/action-script.economy.json (walk onto a Store dot, buy several stock
    slots incl. a food/potion/weapon/armor and armor-repair, hit an insufficient-gold case, leave the
    store — mid-store serialize/rehydrate included) and action-script.encounters.json (walk onto dots
    triggering a faerie, a trap, a chest, an affliction, and a table-four result — chosen via seeds that
    make them reachable). Write test/parity/economy-parity.test.js driving both the frozen prototype and
    the engine through the economy + encounters fixtures, asserting diffState null after every action
    (stripped), INCLUDING a save/reload in the middle of an open store to prove the closure fix (the
    prototype could not do this — so for the store-survives-reload assertion, verify the ENGINE state
    round-trips losslessly with an open store, rather than diffing against the prototype's unsaved store).
    Write test/parity/full-suite.test.js aggregating all fixtures (chargen, movement, combat, magic,
    economy, encounters) into one parity+round-trip pass and asserting the whole suite green — the ENG-05
    phase gate. Extend serialize-rehydrate.test.js to iterate every fixture so the round-trip guardrail
    covers the entire ruleset (ENG-04). Confirm the ENG-01/02/03 guards (engine-purity, rng-no-math-random,
    content-is-pure-data) are included in the full `node --test` run.
  </action>
  <verify>
    <automated>node --test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test` (full suite) is green: engine-purity (ENG-01), rng-no-math-random + same-seed determinism (ENG-02), content-is-pure-data (ENG-03), serialize-rehydrate across all fixtures (ENG-04), and prototype parity across chargen/movement/combat/magic/economy/encounters (ENG-05)
    - economy-parity proves the engine's open-store state round-trips losslessly (the store-closure fix — store survives save/reload)
    - full-suite.test.js runs all fixtures and asserts the aggregate parity + round-trip gate, within the ~15s budget target
    - No Date.now-derived field causes flakiness (stripVolatileFields applied everywhere)
  </acceptance_criteria>
  <done>Economy and encounters are parity-verified, the store survives save/reload, and the full-suite gate proves zero regressions across the entire ruleset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store/encounter action input → applyAction | buyItem/leaveStore/useItem actions validated at the chokepoint (plan 01-05); store stock is now plain data. |
| open-store state → save/rehydrate | The store previously held closures and was excluded from saves; it is now plain data included in the serialized run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-10a | Tampering | non-serializable store closures (the flagged anti-pattern) | medium | mitigate | Stock becomes {effectId, effectParams}; effects live in an engine-side STORE_EFFECTS map, never in state. A round-trip test with an open store proves serializability (ENG-03/ENG-04). |
| T-01-10b | Tampering / DoS | buyItem with out-of-range/sold idx | low | mitigate | buyFrom guards idx bounds, sold flag, and gold sufficiency, no-oping invalid buys without throwing. |
| T-01-10c | Tampering | edited save with an open store | low | mitigate | validateSave (plan 01-06) shape-checks the rehydrated store; unknown effectIds are ignored/no-op rather than executed. |

No package installs (T-01-SC N/A). Offline single-player: no network/auth surface.
</threat_model>

<verification>
- Store closures eliminated; stock is plain data via STORE_EFFECTS; open store survives save/reload (ENG-03/ENG-04).
- Encounters/traps/chests ported and wired into movement (ENG-01).
- Full `node --test` green across all ENG requirements and every parity/round-trip fixture (ENG-05).
</verification>

<success_criteria>
- The entire prototype ruleset runs behind the pure, deterministic, serializable applyAction contract with zero regressions.
- No non-serializable data remains in GameState; the round-trip guardrail covers the whole ruleset.
- The phase's five success criteria are all mechanically verified green.
</success_criteria>

<output>
Create `.planning/phases/01-engine-extraction-determinism/01-10-SUMMARY.md` when done.
</output>
