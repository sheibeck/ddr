---
phase: 01-engine-extraction-determinism
plan: 09
type: execute
wave: 7
depends_on: ["01-05", "01-06", "01-08"]
files_modified:
  - engine/magic.js
  - engine/engine.js
  - test/unit/magic.test.js
  - test/parity/fixtures/action-script.magic.json
  - test/parity/magic-parity.test.js
autonomous: true
requirements: [ENG-01, ENG-05]
must_haves:
  truths:
    - "castSpell resolves every spell kind (heal/ward/might/status/thrown/reveal/mirror/stun/weaken/acid/quake/volley/petrify/summon/turn/gate/death and the rest) through applyAction, RNG-injected, matching the prototype"
    - "drinkPotion and readScroll route through applyAction and apply their effects identically to the prototype"
    - "Spell charges (maxCharges/spellsUsed), grimoire/canCast gating, and Magic User interactions are preserved"
    - "The magic parity fixture matches the frozen prototype after every action; the round-trip guardrail stays green through spell/ward sub-state"
  artifacts:
    - path: "engine/magic.js"
      provides: "castSpell/drinkPotion/readScroll/canRead + spell-effect resolution (RNG-injected, event-emitting)"
      exports: ["castSpell", "drinkPotion", "readScroll"]
    - path: "test/parity/magic-parity.test.js"
      provides: "spell/potion/scroll parity vs. the frozen prototype"
  key_links:
    - from: "engine/magic.js"
      to: "engine/combat.js + engine/derived.js + content/index.js"
      via: "offensive spells damage/kill foes (killFoe), read SPELLS/canCast, resolve dmg via rng notation"
      pattern: "killFoe|canCast|SPELLS"
    - from: "engine/engine.js"
      to: "engine/magic.js"
      via: "applyAction dispatches castSpell/drinkPotion/readScroll"
      pattern: "case \"castSpell\""
---

<objective>
Port the magic domain — spellcasting (all 31 spells across their kinds), potions, and scrolls —
behind applyAction, RNG-injected and event-emitting, matching the prototype exactly. Offensive
spells reuse the combat kill/damage path, so magic slots in after combat.

Purpose: ENG-01 (magic pure, no DOM) + ENG-05 (zero regressions across every spell kind, charge
economy, grimoire gating, and Magic User subclass rules). castSpell is the single largest function
in the prototype; faithful porting of its per-kind resolution is the crux of magic parity.

Output: engine/magic.js, a magic unit test, and a spell/potion/scroll parity fixture + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-engine-extraction-determinism/01-RESEARCH.md
@.planning/phases/01-engine-extraction-determinism/SKELETON.md
@mazeworld.html
@engine/engine.js
@engine/combat.js
@engine/derived.js
@engine/items.js
@content/index.js
@test/parity/harness/sandboxPrototype.js
@test/parity/harness/diffState.js
</context>

## Phase Goal

**As a** Mazeworld player casting spells and quaffing potions, **I want to** have magic resolve through the deterministic engine, **so that** the spell system is preserved exactly while running behind the pure applyAction contract.

<artifacts>
- `engine/magic.js` — `castSpell(state, idx, rng, events)`, `drinkPotion(state, rng, events)`, `readScroll(state, rng, events)`, `canRead(state)`, `maxCharges` (shared or re-exported), and the per-kind spell resolvers
- `test/unit/magic.test.js`, `test/parity/fixtures/action-script.magic.json`, `test/parity/magic-parity.test.js`
- applyAction dispatch cases: `castSpell`, `drinkPotion`, `readScroll`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: castSpell — port all spell-kind resolution behind applyAction</name>
  <files>engine/magic.js, engine/engine.js, test/unit/magic.test.js</files>
  <read_first>
    - mazeworld.html (lines 2476-2666 castSpell full body: charge check maxCharges/spellsUsed, canCast/grimoire gating, per-kind switch — heal/ward/might/status(Doze)/thrown/reveal/mirror/stun/weaken/acid/stupid/blind/shrink/quake/vapor/volley(Fireballs)/petrify/insane/summon/turn/gate/senses/regen/death — each with its dmg notation, target selection, foe effects, killFoe calls; 883-914 canCast/schoolGate/maxCharges; 910-913 INSANITY table)
    - content/index.js (SPELLS with dmg dice-notation, MU_CHART, INSANITY), engine/derived.js (canCast/schoolGate/eff), engine/combat.js (killFoe/liveFoes), engine/dice.js (rollDice)
    - engine/engine.js (add castSpell dispatch case)
  </read_first>
  <action>
    Create engine/magic.js. Port castSpell(state,idx,rng,events) faithfully: verify canCast + charge
    availability (maxCharges(state.c) − spellsUsed), increment spellsUsed, then resolve by spell.kind
    exactly as the prototype — heal/major-heal (rollDice on dmg notation, cap at maxWP), ward (Shield/
    Bubble pool+rounds+reflect), might (Strength boost with next-day expiry via strengthBoost),
    status/Doze/stun/weaken/stupid/shrink/blind/insane (foe status + INSANITY pick), thrown/volley/acid/
    quake/lightning/mangle/fireball (dmg to targeted/all foes via rng notation, killFoe on lethal,
    Earthquake also hits the caster), reveal (Detect Magic), mirror, summon/phantom (ally), turn (walking
    dead), gate (Plane Gate), senses/foresee/foresight, regen, petrify, death (one foe dies costs 25 wp).
    Emit structured events; replace S with state and D/pick with rng. Wire engine/engine.js to dispatch
    action.type "castSpell" through the act-wrapper (cast then afterPlayerAction/foeTurn as the prototype
    did). Preserve Apprentice misfire and Sorcerer/other subclass quirks if present in castSpell.
  </action>
  <verify>
    <automated>node --test test/unit/magic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - castSpell consumes a charge and gates on canCast/grimoire; casting with no charges left is a no-op
    - A damage spell (e.g., Fireball) applies rollDice(dmg notation) to the target and calls killFoe on lethal
    - Heal caps at maxWP; Shield/Bubble set a ward pool; Earthquake damages the caster too — all matching the prototype
    - magic.js references no Math.random/document/localStorage
  </acceptance_criteria>
  <done>Every spell kind resolves pure and RNG-injected behind applyAction.</done>
</task>

<task type="auto">
  <name>Task 2: Potions, scrolls, and magic parity vs. the frozen prototype</name>
  <files>engine/magic.js, engine/engine.js, test/parity/fixtures/action-script.magic.json, test/parity/magic-parity.test.js, test/roundtrip/serialize-rehydrate.test.js</files>
  <read_first>
    - mazeworld.html (lines 2667-2676 drinkPotion (uses potions count, effect by potion — this consumes the equipped/known potion effect); 2765-2788 canRead/readScroll (Runes/Signs skill or scroll item, spell from scroll); 1958-1997 itemReady/useItem for potion items with eff2/uses)
    - test/parity/movement-parity.test.js + combat-parity.test.js (the drive-both-and-diff pattern), test/parity/harness/*
    - engine/magic.js (Task 1), engine/items.js (useItem/potion items), engine/engine.js (add drinkPotion/readScroll dispatch)
  </read_first>
  <action>
    Add drinkPotion(state,rng,events) and readScroll(state,rng,events)/canRead(state) to engine/magic.js,
    porting the prototype's potion-effect resolution (heal/full/strength/speed/invis/cure/etc.) and
    scroll casting (Runes/Signs gating, cast a spell from a scroll) with rng-injected rolls and emitted
    events. Wire engine/engine.js dispatch for "drinkPotion" and "readScroll". Author
    test/parity/fixtures/action-script.magic.json with scenarios: a Magic User casting a damage spell in
    a forced encounter, a heal, drinking a healing potion, and reading a scroll (pick seeds/classes that
    make these reachable). Write test/parity/magic-parity.test.js driving both the frozen prototype and
    the engine, asserting diffState null after every action (stripped) and coherent event-type sequences.
    Ensure the standing round-trip guardrail covers a ward/spell sub-state (add a magic round-trip case or
    include the magic fixture in serialize-rehydrate coverage).
  </action>
  <verify>
    <automated>node --test test/parity/magic-parity.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - drinkPotion and readScroll apply the prototype's effects and route through applyAction
    - magic-parity passes for cast-damage, heal, potion, and scroll scenarios (diffState null after every action, stripped)
    - Round-trip guardrail holds with a ward/spell sub-state present (mid-cast serialize/rehydrate lossless)
  </acceptance_criteria>
  <done>Potions and scrolls are ported, and the whole magic domain is proven byte-faithful to the prototype.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| magic action input → applyAction | castSpell/drinkPotion/readScroll actions are validated at the chokepoint (plan 01-05); ward/spell sub-state stays plain data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09a | Tampering / DoS | castSpell with bad idx / no charges / out of combat | low | mitigate | Handlers validate idx range, charge availability, and combat context, no-oping invalidly rather than throwing. |
| T-01-09b | Tampering | non-serializable ward/spell state | low | mitigate | Ward/might/mirror state is plain-data numbers/objects; round-trip through magic sub-state proves serializability (ENG-04). |

No package installs (T-01-SC N/A). Offline single-player: no network/auth surface.
</threat_model>

<verification>
- Magic is pure, RNG-injected, behind applyAction (ENG-01).
- Spells/potions/scrolls match the frozen prototype (ENG-05).
- Round-trip guardrail green through ward/spell sub-state (ENG-04).
</verification>

<success_criteria>
- The full magic domain runs behind the pure applyAction contract with zero regressions.
- All 31 spells, charge economy, and grimoire gating are preserved and parity-verified.
</success_criteria>

<output>
Create `.planning/phases/01-engine-extraction-determinism/01-09-SUMMARY.md` when done.
</output>
