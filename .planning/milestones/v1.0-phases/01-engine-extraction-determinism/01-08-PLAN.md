---
phase: 01-engine-extraction-determinism
plan: 08
type: execute
wave: 6
depends_on: ["01-05", "01-06", "01-07"]
files_modified:
  - engine/combat.js
  - engine/engine.js
  - test/unit/combat.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/combat-parity.test.js
autonomous: true
requirements: [ENG-01, ENG-05]
must_haves:
  truths:
    - "startCombat builds foes from BESTIARY deterministically (type, count, level scaling, subclass/race interactions) matching the prototype"
    - "attack/flee/parley/sing actions route through applyAction and resolve strikes, initiative, foe turns, kills, and skill-point/loot/level gains identically to the prototype"
    - "killFoe drives skill points, loot (via items helpers), cooking, and checkLevel; foeTurn resolves creature attacks, specials, and player death"
    - "The combat parity fixture (win, lose→death, flee, parley) matches the frozen prototype after every action; the round-trip guardrail stays green through combat sub-state"
  artifacts:
    - path: "engine/combat.js"
      provides: "startCombat/rollInitiative/liveFoes/playerStrike/killFoe/flee/parley/canParley/sing/songReady/endCombat/afterPlayerAction/allyTurn/foeTurn (RNG-injected, event-emitting)"
      exports: ["startCombat", "playerStrike", "foeTurn", "flee", "parley", "sing", "endCombat"]
    - path: "test/parity/combat-parity.test.js"
      provides: "win/lose/flee/parley parity vs. the frozen prototype"
  key_links:
    - from: "engine/combat.js"
      to: "engine/character.js + engine/items.js + engine/death.js"
      via: "killFoe calls checkLevel + takeItem/gainWilmst; foe/player kills call die"
      pattern: "checkLevel|takeItem|die\\("
    - from: "engine/engine.js"
      to: "engine/combat.js"
      via: "applyAction dispatches attack/flee/parley/sing to combat handlers"
      pattern: "case \"attack\""
---

<objective>
Port the combat domain (encounter setup, initiative, player strikes, foe turns, kills, flee/
parley/sing, end-of-combat) behind applyAction, RNG-injected and event-emitting, matching the
prototype exactly. This is the heaviest rule domain and the core of the crawl's tension.

Purpose: ENG-01 (combat pure, no DOM) + ENG-05 (zero combat regressions across the full BESTIARY,
all class/race/subclass interactions, criticals, phobias, darkness, allies). killFoe and death tie
into the items/leveling/death helpers already built (plan 01-06/01-05), so combat slots in as the
next slice after movement.

Output: engine/combat.js, a combat unit test, and a win/lose/flee/parley parity fixture + test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-engine-extraction-determinism/01-RESEARCH.md
@.planning/phases/01-engine-extraction-determinism/SKELETON.md
@mazeworld.html
@engine/engine.js
@engine/derived.js
@engine/character.js
@engine/items.js
@engine/death.js
@content/index.js
@test/parity/harness/sandboxPrototype.js
@test/parity/harness/diffState.js
</context>

## Phase Goal

**As a** Mazeworld player, **I want to** fight the creatures the maze throws at me through the deterministic engine, **so that** combat — the heart of the crawl — is preserved exactly while running behind the pure applyAction contract.

<artifacts>
- `engine/combat.js` — `startCombat(state, wandering, forced, rng, events)`, `rollInitiative`, `liveFoes`, `playerStrike`, `killFoe`, `canParley`, `parley`, `flee`, `songReady`, `sing`, `endCombat`, `afterPlayerAction`, `allyTurn`, `foeTurn` (all state/rng-parameterized)
- `test/unit/combat.test.js`, `test/parity/fixtures/action-script.combat.json`, `test/parity/combat-parity.test.js`
- applyAction dispatch cases: `attack`, `flee`, `parley`, `sing`
</artifacts>

<tasks>

<task type="auto">
  <name>Task 1: Encounter setup + player strike resolution</name>
  <files>engine/combat.js, engine/engine.js, test/unit/combat.test.js</files>
  <read_first>
    - mazeworld.html (lines 2250-2308 startCombat: type ENC_TYPES[D(6)-1], Tracking, maxLvl/cap/count, foe build from BESTIARY roster+pick, initiative, Warlock/Knight/Con Artist/Court Mage interactions, phobia freeze, darkness; 2310-2322 rollInitiative/liveFoes; 2324-2402 playerStrike full body: Wizard refusal, frozen, attacks count (Barbarian/Ambidextrous/haste/Fridgian frenzy), toHit/strikeDie, criticals, Stealth/Cat Burglar/Cutthroat/Ninja/Death-touch specials, damage, killFoe, afterPlayerAction)
    - engine/derived.js (toHit/strikeDie/weaponDamage/foeDie/foeToHitVs/inDark/skill/eff), engine/character.js (checkLevel), engine/items.js (takeItem/gainWilmst/rollTreasureItem), engine/death.js (die)
    - engine/engine.js (add the attack dispatch case)
  </read_first>
  <action>
    Create engine/combat.js. Port startCombat(state,wandering,forced,rng,events) building
    state.combat={foes,type,round,target,spellOpen,tracked,...} from BESTIARY via rng (ENC_TYPES pick,
    level scaling maxLvl/cap, foe roster pick, twice→lives:2), preserving Tracking, Warlock walking-dead
    boost, Knight/Con Artist/Court Mage removals, phobia freeze, darkness note, ally join, and
    first-move via rollInitiative (Samurai/slow/foresight/Acute Hearing). Port playerStrike(state,rng,
    events) faithfully: Wizard-won't-melee, frozen skip, attack count (Barbarian/Ambidextrous/haste/
    Fridgian frenzy incl. wasted-swing-on-corpse), per-attack toHit vs strikeDie, critical rules
    (Stealth/Cat Burglar first-strike/Cutthroat/Ninja/Death-touch/Guard/Soldier), weaponDamage,
    applying damage to foe wp, killFoe on lethal, then afterPlayerAction. Emit struck/hit/miss/killed
    events instead of say(). Replace global S with state; replace D/pick/shuffle with rng. Wire
    engine/engine.js to dispatch action.type "attack" → act-wrapper that calls playerStrike then
    foeTurn/afterPlayerAction as the prototype's act(playerStrike) did.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - startCombat(state, ...) with a fixed seed builds a deterministic foe list from BESTIARY (type/count/levels reproducible)
    - playerStrike applies the correct toHit/strikeDie and damage on a crafted state, and killFoe fires on lethal damage
    - Barbarian/Ambidextrous/haste yield 2 attacks; Fridgian frenzy is reproduced; Wizard refuses melee while a charge remains
    - combat.js references no Math.random/document/localStorage
  </acceptance_criteria>
  <done>Encounter setup and player-strike resolution run pure and RNG-injected behind applyAction.</done>
</task>

<task type="auto">
  <name>Task 2: killFoe, foe turns, allies, flee/parley/sing, endCombat</name>
  <files>engine/combat.js, engine/engine.js, test/unit/combat.test.js</files>
  <read_first>
    - mazeworld.html (lines 2403-2436 killFoe: lives, skill points d6×lvl×mul, purse/coin via gainWilmst, treasure drop D(20)<=2+lvl, cooking, checkLevel; 2677-2707 flee; 2694-2735 canParley/parley; 2736-2764 songReady/sing; 2789-2830 endCombat/afterPlayerAction/allyTurn; 2831-2896 foeTurn: per-foe attack, specials (poison/disease/acid/steals/enthrall/awe/caster/grapple/entangle/possess/raise/breath every N), foeDie vs foeToHitVs, player damage, ward/mirror/invis, die on wp<=0)
    - engine/items.js (gainWilmst/takeItem/rollTreasureItem), engine/character.js (checkLevel), engine/death.js (die), engine/derived.js (foeDie/foeToHitVs)
    - engine/engine.js (add flee/parley/sing dispatch cases)
  </read_first>
  <action>
    Add to engine/combat.js: killFoe(state,f,rng,events) with lives handling, skill-point formula (d6 ×
    foe.lvl × mul with spMul/Barbarian/Apprentice modifiers), coin via gainWilmst, treasure via
    rollTreasureItem/takeItem on D(20)<=2+lvl, cooking rations, then checkLevel. Port foeTurn(state,rng,
    events) resolving each live foe's attack: foeDie vs foeToHitVs, criticals, all creature specials
    (poison/disease/acid/steals/enthrall/awe/caster/grapple/entangle/possess/raise/shriek/breath-every-N/
    twice/quills/blind/phobia), applying ward/mirror/invis mitigations, player damage, and die() on wp<=0.
    Port flee (escape chance, tracked clean exit), canParley/parley (Language/Con Artist/Court Mage,
    intel thresholds), songReady/sing (Bard, gifts), endCombat, afterPlayerAction (round advance, foeTurn
    trigger, allyTurn), allyTurn. Emit structured events; replace S with state, D/pick/shuffle with rng.
    Wire engine/engine.js dispatch for "flee"/"parley"/"sing" through the act-wrapper equivalent.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js</automated>
  </verify>
  <acceptance_criteria>
    - killFoe awards the prototype's skill-point amount (d6×lvl×mul) and rolls loot/treasure via the injected rng
    - foeTurn applies creature specials and player damage, and triggers die() when player wp reaches 0
    - flee/parley/sing resolve per the prototype's class/skill rules and end combat cleanly when they succeed
    - Round-trip guardrail stays green with an active combat sub-state in GameState
  </acceptance_criteria>
  <done>Kills, foe turns, allies, and the flee/parley/sing exits are ported pure and RNG-injected.</done>
</task>

<task type="auto">
  <name>Task 3: Combat parity fixtures (win / lose / flee / parley) vs. the frozen prototype</name>
  <files>test/parity/fixtures/action-script.combat.json, test/parity/combat-parity.test.js, test/roundtrip/serialize-rehydrate.test.js</files>
  <read_first>
    - test/parity/harness/sandboxPrototype.js + diffState.js; test/parity/movement-parity.test.js (the pattern to mirror: drive both prototype and engine, diff after each action)
    - mazeworld.html (3231-3239 keydown combat handlers 1=strike/2=potion/3=flee/5=parley/6=sing → the action vocabulary; 2250 startCombat entry — forced type enables a deterministic encounter for fixtures)
    - engine/combat.js (the handlers under test), engine/engine.js (attack/flee/parley dispatch)
  </read_first>
  <action>
    Author test/parity/fixtures/action-script.combat.json with several scenarios (as multiple seed+action
    entries or a scenario list): a forced encounter the player WINS by repeated attack; one the player
    LOSES (death) — verifying die parity; a FLEE; and a PARLEY (choose a seed/class that can parley).
    Use a forced-encounter entry hook so the fixture reliably triggers combat (e.g., start on a dot or
    an action that forces startCombat with a fixed type) so parity is deterministic. Write
    test/parity/combat-parity.test.js driving both the frozen prototype sandbox and the engine through
    each scenario, asserting diffState(protoS, engineState) is null after every action (stripped) and
    that the event `type` sequence is coherent. Confirm the standing round-trip guardrail is also run
    across the combat fixture (import the fixture into serialize-rehydrate coverage or add a combat
    round-trip case).
  </action>
  <verify>
    <automated>node --test test/parity/combat-parity.test.js test/roundtrip/serialize-rehydrate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - combat-parity passes for win, lose (death), flee, and parley scenarios — engine matches the frozen prototype after every action (diffState null, stripped)
    - The round-trip guardrail holds through combat sub-state (mid-fight serialize/rehydrate is lossless)
    - Fixtures trigger combat deterministically (forced type) so the test is not flaky
  </acceptance_criteria>
  <done>Combat is proven byte-faithful to the prototype across win/lose/flee/parley, with the round-trip guardrail green through combat.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| combat action input → applyAction | attack/flee/parley/sing actions are validated at the applyAction chokepoint (plan 01-05); combat sub-state stays plain data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08a | Tampering / DoS | out-of-context combat action (e.g., attack with no combat) | low | mitigate | Handlers guard `if (!state.combat) return` no-op; validateAction rejects malformed shapes — no throw, no corruption. |
| T-01-08b | Tampering | non-serializable data in combat sub-state | low | mitigate | Foes are plain arrays of plain objects (no Map/Set, no closures); round-trip test through combat proves serializability (ENG-04). |

No package installs (T-01-SC N/A). Offline single-player: no network/auth surface.
</threat_model>

<verification>
- Combat is pure, RNG-injected, and behind applyAction (ENG-01).
- Win/lose/flee/parley match the frozen prototype (ENG-05).
- Round-trip guardrail green through combat sub-state (ENG-04).
</verification>

<success_criteria>
- The full combat domain runs behind the pure applyAction contract with zero regressions.
- All class/race/subclass/creature interactions are preserved and parity-verified.
</success_criteria>

<output>
Create `.planning/phases/01-engine-extraction-determinism/01-08-SUMMARY.md` when done.
</output>
