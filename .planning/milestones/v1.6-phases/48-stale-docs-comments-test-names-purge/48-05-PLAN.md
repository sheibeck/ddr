---
phase: 48-stale-docs-comments-test-names-purge
plan: 05
type: execute
wave: 5
depends_on: ["48-01", "48-02", "48-03", "48-04"]
files_modified:
  - .claude/CLAUDE.md
  - docs/CLARITY.md
  - docs/CLASS-PASS.md
  - docs/COMBAT-NARRATIVE-DESIGN.md
  - docs/FLEE.md
  - docs/GEAR-BALANCE.md
  - docs/GEAR-SLOTS.md
  - docs/SHELL-MODULES.md
  - docs/SPELLS.md
  - docs/TERRAIN.md
  - docs/USABLE-FEATURES-AUDIT.md
  - test/unit/stale-terms.test.js
  - tools/stale-terms.mjs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]

must_haves:
  truths:
    - "DOCS-02 (CLAUDE.md): `grep -cE \"iOS|Xcode|Apple|macOS|App Store\" .claude/CLAUDE.md` prints 0 — rows deleted, not annotated; the SCOPE OVERRIDE banner is gone; the stack notes describe tap-to-move + the RAIL (no D-pad, no toast); every `<!-- GSD:… -->` marker and every non-stack section is byte-identical"
    - "DOCS-02 (docs/): every docs/*.md is bucketed — (a) current, swept; (b) design record of a finished pass, kept with a one-line `> **Status (v1.6, Phase 48):**` note where a section is superseded; (c) documented only dead code — deleted (`docs/COMBAT-NARRATIVE-DESIGN.md`, the one deletion) — with the bucket list and reasons in the SUMMARY; every ledger test that reads a doc (clarity-cause-lines, class-pass-ledger, identity-contract, difficulty-retune-ledger, flee-ledger, rations-audit, usable-features-audit, bridge-registry) stays green and `node tools/bridge-doc.mjs --check` exits 0"
    - "DOCS-01/03 (closing gate): `node tools/stale-terms.mjs` over the whole surface exits 0 — every enforced row unlisted 0, allow-list rot 0 — and `test/unit/stale-terms.test.js` pins that state so a future stale comment fails `npm test`"
    - "Criteria 1-4 verbatim: the SUMMARY records the final table with its zeros, every survivor (file:line + reason, grouped by class), the CLAUDE.md grep → 0, the docs bucket list, `ls test/unit | grep -iE \"toast|dpad\"` empty, the describe/test grep → only listed absence-assertions, `npm test` = 3,288 + N (N = the tripwire's tests, recorded), the phase-wide `comment-only-diff` over engine/content/fixtures, and the master hash"
  artifacts:
    - ".claude/CLAUDE.md — Android-only stack section (Capacitor 8 + Android Studio; Google Play prerequisites only; tap-to-move + RAIL in the stack notes)"
    - "docs/ — 15 files remain (COMBAT-NARRATIVE-DESIGN.md deleted); 9 edited (CLARITY, CLASS-PASS, FLEE, GEAR-BALANCE, GEAR-SLOTS, SHELL-MODULES, SPELLS, TERRAIN, USABLE-FEATURES-AUDIT); 6 untouched (ABILITIES, DIFFICULTY-RETUNE, PARLEY-REBALANCE, RATIONS, RELEASING, UAT-v1.5)"
    - "test/unit/stale-terms.test.js — 5 tests: zero unlisted on every enforced row over the real repo; zero allow-list rot; fail-first teeth with synthetic sources; classic-script + legacy-won rows are informational; --self-test passes"
    - "48-05-SUMMARY.md — the phase's closing record (criteria 1-4 verbatim, the two judgment calls + the bounded-body-rule reading and the fixture-hygiene commit flagged for the user to reverse)"
  key_links:
    - "test/unit/stale-terms.test.js -> tools/stale-terms.mjs#scan/defaultSources/ALLOWED: the standing tripwire — a new stale comment anywhere on the surface, or a rotted allow entry, fails npm test"
    - "docs/USABLE-FEATURES-AUDIT.md §6 -> test/unit/usable-features-audit.test.js doc-sync pins: the legacy field names stay as retirement rows because the test asserts the doc names them"
    - "docs/FLEE.md / CLASS-PASS.md / DIFFICULTY-RETUNE.md -> their ledger tests pin H2 order and byte-identical rendered blocks: status notes are blockquotes, never headings; rendered blocks untouched"
  prohibitions:
    - statement: "MUST NOT change any rule in .claude/CLAUDE.md — the Project/Constraints text keeps its meaning (one clause reworded to drop the literal), the GSD-managed sections are byte-identical, only the stack section between the GSD:stack markers is edited"
      requirement_id: DOCS-02
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT add or reorder a `#`/`##`/`###` heading in a test-pinned doc, and MUST NOT edit inside a byte-pinned rendered block (CLASS-PASS AFTER/Outliers/Handoff/v1.5 AFTER; DIFFICULTY-RETUNE v1.2 section; SHELL-MODULES bridge table)"
      requirement_id: DOCS-02
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT touch docs/UAT-v1.5.md (the pending device batch) or anything under .planning/ other than REQUIREMENTS.md/STATE.md/ROADMAP.md plan-close bookkeeping"
      requirement_id: DOCS-02
      category: values
      status: unverified
      verification: judgment
---

<objective>
Close the phase: make `.claude/CLAUDE.md` describe the Android-only, tap-to-move + RAIL game it documents (rows deleted, not annotated); bucket every `docs/*.md` (keep / keep with a status note / delete) and sweep the stale lines in the kept ones; pin the criterion-1 table with `test/unit/stale-terms.test.js`; and write the closing SUMMARY with ROADMAP criteria 1-4 verified verbatim.

Purpose: DOCS-02 in full, plus the standing tripwire and the recorded proof DOCS-01/DOCS-03 require. After this plan nothing in the repo describes a pattern the game no longer employs, and a regression fails `npm test`.

Output: `.claude/CLAUDE.md`; nine edited and one deleted `docs/*.md`; `test/unit/stale-terms.test.js`; `tools/stale-terms.mjs` (final ALLOWED); `48-05-SUMMARY.md`; REQUIREMENTS.md DOCS-01..03 complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-stale-docs-comments-test-names-purge/48-CONTEXT.md
@.planning/phases/48-stale-docs-comments-test-names-purge/48-04-SUMMARY.md
@tools/stale-terms.mjs
@.claude/CLAUDE.md
</context>

<!-- planner-discipline-allow: iOS -->
<!-- planner-discipline-allow: Xcode -->
<!-- planner-discipline-allow: Apple -->
<!-- planner-discipline-allow: macOS -->
<!-- planner-discipline-allow: App Store -->
<!-- planner-discipline-allow: SCOPE OVERRIDE -->
<!-- planner-discipline-allow: toast -->
<!-- planner-discipline-allow: D-pad -->
<!-- planner-discipline-allow: dpad -->
<!-- planner-discipline-allow: TOAST_FOR -->
<!-- planner-discipline-allow: toasts.js -->
<!-- planner-discipline-allow: toastsForAction -->
<!-- planner-discipline-allow: dispatchWithToasts -->
<!-- planner-discipline-allow: COMBAT-NARRATIVE-DESIGN -->
<!-- planner-discipline-allow: cleanup milestone -->
<!-- planner-discipline-allow: landing in Plans -->
<!-- planner-discipline-allow: @capacitor/ios -->
<!-- planner-discipline-allow: 3,260 -->
<!-- planner-discipline-allow: WKWebView -->
<!-- planner-discipline-allow: UserDefaults -->
<!-- planner-discipline-allow: retired -->

<phase_ground_rules>
These rules apply to every task in every Phase 48 plan; they are restated verbatim in each plan.

- **Engine-gate fence (every commit):** `engine/`, `content/` and `test/parity/fixtures/` change on COMMENT LINES ONLY, proven by a comment-stripped diff (`node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` — every changed file reports `comment-only:`; fixtures are JSON and must not appear at all). `test/parity/prototype-master.js.txt` is never edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **Test invariant:** `npm test` pass count stays exactly **3,288** / fail 0 at every commit of Plans 01-04 (renames only; no test added or deleted — a test that must go needs its reason in the SUMMARY). Plan 05 adds the tripwire's own tests and records the new count as "3,288 + N".
- **Phase 47 DOM-snapshot smoke:** `node --test test/unit/shell-tab-snapshots.test.js` stays 10/10 with `git diff --stat -- test/unit/fixtures/` empty — a comment edit cannot move it, so a fixture diff means a code line was touched.
- **Never `git add -A`.** Stage by path. Never touch `icons/` or `sfx/`. `.planning/` is history — out of scope for the sweep (SUMMARY/STATE/REQUIREMENTS updates at plan close are the normal exceptions).
- **No wholesale comment-volume purge** — Phase 47's line-budget ruling closed that. This phase removes stale *meaning*, not comment volume. A comment that explains a retirement by name ("was a toast; the rail owns feedback since Phase 35") stays when it still helps a reader — it must not describe the retired thing as live.
- **Judgment call 1 (CONTEXT, user-reversible):** `classic script` is a LIVE term — it names `mazeworld.html`'s non-module `<script>` block. It stays; the tool reports it as an informational row. A comment that says the classic script *is an engine*, *mirrors the engine*, or names a specific classic `X()` duplicate as if it still existed IS stale and goes.
- **Judgment call 2 (CONTEXT, user-reversible):** allowed survivors are (A) tests/lines that ASSERT a retirement (the Phase 35 invariant suite, `assert.equal(... , 0)`-style pins and their titles), (B) prose retirement statements that still help a reader, (C) tolerant-load legacy-key reads and their pins (`engine/saveState.js#foldLegacyCounters`, the parity comparables' prototype-side strips, `test/unit/item-activation.test.js`'s fold pins, `save-validation.test.js`'s OLD-SAVE inputs), (D) inert legacy keys inside hand-built test-state literals (`flightLeft: 0, flightCooldown: 0` / `won: false` in `fixedState()`-style objects) — Plan 04's last task removes these in one revertable commit (the 46-02 SUMMARY handed the `won: false` list to this phase); until then they are counted, listed, and not edited. Every survivor is an `ALLOWED` entry in `tools/stale-terms.mjs` with file + match + reason — nothing else survives.
- **Windows executor environment:** Git Bash, no `jq`; heredocs and `node -e` one-liners mangle backslashes — write any regex-heavy script to a file with the Write tool, never a heredoc. Delete a tracked file with `git rm`.
- **Line numbers drift:** anchor by the quoted text; numbers below were measured at HEAD `649de2b`.
- **Verification agents are off:** the acceptance criteria ARE the verification — every `<verify>` command is literal, with its expected output.
- **Test bodies: the bounded rule.** The CONTEXT says "test bodies and assertions are not touched (a body that needs changing is a behaviour question — flag it, don't fix it here)". This phase reads that as: no expected value, condition, fixture literal, event type or field name changes. Two edits are NOT behaviour questions and ARE in scope (Plans 03-04): (i) renaming a local identifier whose name contains `toast`/`Toast`/`TOAST` to its `line`/`Line`/`LINE` equivalent, consistently within its scope; (ii) rewording an assertion MESSAGE string. A test TITLE is not a body: rename it. A comment inside a body is a comment: rewrite it. If a rename or comment rewrite trips another test that pins the old text: reword so the pinned substring survives when it is not itself stale; if it IS the stale text, revert and list the line as a survivor with the pinning test's name — never edit an expected value. Plan 04's fixture-hygiene commit is the one further body edit this phase makes.
- **Docs are test-pinned:** `flee-ledger.test.js` pins FLEE.md's eight H2s in order and the `fleeRolled` / `docs/CLASS-PASS.md` mentions; `class-pass-ledger.test.js` + `identity-contract.test.js` pin CLASS-PASS.md's H2 positions and byte-identical rendered blocks; `difficulty-retune-ledger.test.js` pins DIFFICULTY-RETUNE.md's `## v1.2 retune (Phase 27)` as the LAST H2; `clarity-cause-lines.test.js` parses CLARITY.md's Cost-event inventory table; `rations-audit.test.js` parses RATIONS.md's rules table; `usable-features-audit.test.js` asserts USABLE-FEATURES-AUDIT.md contains every refusal reason, spell/potion/item name, and the timed-field names (including the pre-Phase-39 ones); `bridge-registry.test.js` + `tools/bridge-doc.mjs --check` pin SHELL-MODULES.md's bridge table. A status note is a `> **Status (v1.6, Phase 48):** …` blockquote placed directly under the `**Date:**` line (or under the H1 when there is none) — NEVER a new heading.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: .claude/CLAUDE.md — Android-only stack section (rows deleted, not annotated)</name>
  <files>.claude/CLAUDE.md</files>
  <read_first>
    - .claude/CLAUDE.md in full (203 lines at 649de2b): the seven `<!-- GSD:… -->` marker pairs; the stack section runs from `<!-- GSD:stack-start source:research/STACK.md -->` (~24) to `<!-- GSD:stack-end -->` (~159) — everything you edit is between those two markers except ONE clause in `### Constraints` (~15)
    - `grep -nE "iOS|Xcode|Apple|macOS|App Store|Mac\b|WKWebView|UserDefaults|D-pad|dpad|toast|two-store|both stores|3,260" .claude/CLAUDE.md` — the 30 lines (each is handled below)
    - src/browser/storage.js head comment (`@capacitor/preferences`-backed persistence; the localStorage mirror) — the current fact the Sources footnote must state
    - .planning/phases/48-stale-docs-comments-test-names-purge/48-CONTEXT.md `### CLAUDE.md and docs (DOCS-02)` — the ruling: delete, don't annotate; keep Project/Constraints/GSD sections; "do not change any rule in it"
  </read_first>
  <action>
    Edit with the Edit tool, section by section (never a whole-file Write). Keep every `<!-- GSD:… -->` marker line byte-identical.

    1. `### Constraints` (~15): the Platforms bullet's last sentence "iOS is explicitly excluded." → "No other platform is in scope." — the rule is unchanged; the literal goes so criterion 2's grep is a true zero. Nothing else in `## Project`/`### Constraints` changes.
    2. Delete the `> ⚠️ **SCOPE OVERRIDE — ANDROID / GOOGLE PLAY ONLY.** …` blockquote (~28) and the empty heading `## The Central Decision: Path A (wrap) vs. Path B (port/rewrite)` (~30; it has no body).
    3. `### Core Technologies` table: delete the `| ~~Xcode~~ | **OUT OF SCOPE** | …` row. Capacitor row: keep (already Android-only wording); re-read it and drop any "both"/"iOS" fragment if present.
    4. `### Supporting Libraries` table: `@capacitor/preferences` row — "(`UserDefaults` on iOS, `SharedPreferences` on Android)" → "(`SharedPreferences` on Android)" and delete the "; WKWebView migrations have historically dropped `localStorage` entirely" clause; `@capacitor/splash-screen` row — "expected on both stores" → "expected on Play"; `@capacitor/screen-orientation` row — "The prototype's UI (1080px reflow, D-pad) is portrait-first" → "The UI (1080px reflow, tap-to-move map, bottom RAIL card) is portrait-first"; check the `@capacitor/app` row reads Android-only (it does: hardware/gesture back button).
    5. `### Development Tools` table: delete the `| Xcode (macOS only) | …` row; Fastlane row — "a solo dev doing two-store submissions manually the first time" → "a solo dev doing Play submissions manually the first time"; the Android Studio and keytool rows are Android-only already (keytool row mentions Play App Signing — keep).
    6. `## Installation` (~64-74): the section body is five bare `#` comment lines left from a stripped code block whose commands included the second platform — delete the whole section (heading + the five lines). It carries no rule.
    7. `## Alternatives Considered` table: delete the `| Manual keystore + Xcode signing | …` row.
    8. `## What NOT to Use` table: localStorage row — "Evictable under Android storage pressure / iOS WebView migrations" → "Evictable under Android storage pressure"; hand-rolled WebView row — "(raw `WKWebView`/`android.webkit.WebView` project)" → "(raw `android.webkit.WebView` project)"; Electron row — 'irrelevant to "must ship to App Store and Google Play"' → 'irrelevant to "must ship to Google Play"'.
    9. `## Stack Patterns by Variant`: delete the two bullets about a cloud Mac CI and Xcode/Apple's SDK bump; keep the two rules-engine bullets (`S` state / `act()` dispatcher; the multiplayer-ready decoupling) verbatim.
    10. `## Version Compatibility` table: delete the `| Capacitor 8.x (iOS) | …` row.
    11. `## Store-Submission Prerequisites (concrete, for a first-timer)`: delete the entire `### Apple App Store` subsection (heading + its table); in `### Google Play`: Hardware row → "Fully buildable on Windows via Android Studio."; Age rating row — drop "— a separate process from Apple's, answer similarly (mild fantasy violence, no profanity/gore) to get" and reword to "answer honestly (mild fantasy violence, no profanity/gore) to get an appropriately low rating …"; Review row — "Google Play review is typically faster than Apple's for straightforward apps" → "Google Play review is typically fast for straightforward apps".
    12. `## Sources`: delete the four Apple/iOS source lines (developer.apple.com enroll/fee-waivers; the SDK-requirement news + 9to5mac; the age-ratings news + ppc.land; termsfeed ios-apps-privacy-policy + app-privacy-details); first line `https://www.npmjs.com/package/@capacitor/ios — Capacitor 8.5.1 current version` → `https://www.npmjs.com/package/@capacitor/android — Capacitor 8.x current version`; the last line "Direct inspection of `C:\projects\mazeworld\mazeworld.html` (3,260 lines) — confirmed DOM+CSS UI with single `<canvas>` maze renderer, `localStorage`-only persistence (2 keys: graveyard + active save), no `fetch`/`WebSocket`/Worker/ServiceWorker usage" → the current facts: "`mazeworld.html` (5,621 lines at the Phase 47 close) — DOM+CSS UI with a single `<canvas>` maze renderer and tap-to-move; persistence through `src/browser/storage.js` (`@capacitor/preferences`, with a `localStorage` mirror for the browser dev loop); no `fetch`/`WebSocket`/Worker/ServiceWorker usage" — verify each fact before writing it: `wc -l < mazeworld.html`, `grep -c "@capacitor/preferences" src/browser/storage.js` (≥ 1), `grep -cE "fetch\(|WebSocket|new Worker|serviceWorker" mazeworld.html src/browser/*.js | grep -v ":0$" | wc -l` (0).
    13. Leave `## Conventions`, `## Architecture`, `## Project Skills`, `## GSD Workflow Enforcement`, `## Developer Profile` byte-identical (`git diff -U0 -- .claude/CLAUDE.md | grep -E "^@@"` hunks all fall between lines 13-160 of the original).

    Commit: `docs(48-05): CLAUDE.md — Android-only stack notes; iOS/Xcode/Apple rows deleted; tap-to-move + RAIL`.
  </action>
  <verify>
    <automated>grep -cE "iOS|Xcode|Apple|macOS|App Store" .claude/CLAUDE.md; grep -ciE "d-pad|dpad|toast|SCOPE OVERRIDE|WKWebView|UserDefaults" .claude/CLAUDE.md; grep -c "GSD:" .claude/CLAUDE.md; grep -c "tap-to-move" .claude/CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "iOS|Xcode|Apple|macOS|App Store" .claude/CLAUDE.md` prints 0; `grep -ciE "iOS|Xcode|Apple|macOS|App Store" .claude/CLAUDE.md` prints 0 (record both — the case-insensitive run has no false positives at 649de2b)
    - `grep -ciE "d-pad|dpad|toast|SCOPE OVERRIDE|WKWebView|UserDefaults|two-store|both stores|3,260" .claude/CLAUDE.md` prints 0
    - `grep -c "GSD:" .claude/CLAUDE.md` prints 14 (all seven marker pairs intact); `grep -cE "^## (Project|Conventions|Architecture|Project Skills|GSD Workflow Enforcement|Developer Profile)$" .claude/CLAUDE.md` prints 6; `grep -c "^### Constraints" .claude/CLAUDE.md` prints 1
    - `grep -c "No other platform is in scope" .claude/CLAUDE.md` prints 1; `grep -c "Google Play (Android only)" .claude/CLAUDE.md` prints 1
    - `grep -c "tap-to-move" .claude/CLAUDE.md` prints a number >= 2 (the screen-orientation row and the Sources line); `grep -c "RAIL" .claude/CLAUDE.md` prints a number >= 1
    - `grep -c "^### Google Play" .claude/CLAUDE.md` prints 1; `grep -c "^### Apple" .claude/CLAUDE.md` prints 0; `grep -c "^## Installation" .claude/CLAUDE.md` prints 0; `grep -c "The Central Decision" .claude/CLAUDE.md` prints 0
    - `grep -c "@capacitor/android" .claude/CLAUDE.md` prints a number >= 2; `grep -c "@capacitor/preferences" .claude/CLAUDE.md` prints a number >= 2
    - `git diff 649de2b -U0 -- .claude/CLAUDE.md | grep -E "^@@" | awk -F'[ ,]' '{gsub("-","",$2); if ($2+0 < 13 || $2+0 > 160) print "outside"}' | wc -l` prints 0 (every hunk starts inside the Project-constraints clause or the stack section)
    - `git diff 649de2b -- .claude/CLAUDE.md | grep -E "^-.*(decoupled from UI|fully serializable|dispatcher, fully serializable|Paid upfront|Fidelity)" | wc -l` prints 0 (no rule line removed — the Constraints bullets and the two rules-engine bullets survive verbatim except the one reworded platform clause)
  </acceptance_criteria>
  <done>CLAUDE.md's stack section is Android-only with the second platform's rows gone, the stack notes describe tap-to-move + the RAIL, and no rule or GSD-managed section changed.</done>
</task>

<task type="auto">
  <name>Task 2: docs/ buckets — one deletion, nine sweeps/status notes, six untouched; every ledger test green</name>
  <files>docs/CLARITY.md, docs/CLASS-PASS.md, docs/COMBAT-NARRATIVE-DESIGN.md, docs/FLEE.md, docs/GEAR-BALANCE.md, docs/GEAR-SLOTS.md, docs/SHELL-MODULES.md, docs/SPELLS.md, docs/TERRAIN.md, docs/USABLE-FEATURES-AUDIT.md</files>
  <read_first>
    - `for f in docs/*.md; do echo "== $f"; grep -niE "toast|d-pad|dpad|wornSlots|flightLeft|flightCooldown|c\.ether|dead classic|classic engine|cleanup milestone|landing in Plans|TOAST_FOR|toasts\.js|toastsForAction|dispatchWithToasts|Round Card" "$f" | cut -c1-200; done` — every stale line in every doc (the bucket table below was built from this at 649de2b)
    - test/unit/flee-ledger.test.js lines 50-62 (the eight pinned H2s), test/unit/class-pass-ledger.test.js lines 85-115 and 300-310 (H2 positions + byte-identical rendered blocks), test/unit/difficulty-retune-ledger.test.js lines 126-131 (v1.2 section is the LAST H2), test/unit/usable-features-audit.test.js lines 500-535 (doc-sync: reasons, names, and the timed-field list including `flightLeft`/`ether`/`haste`/`invis`), test/unit/clarity-cause-lines.test.js lines 281-300 and 471-490 (the Cost-event inventory parser), tools/bridge-doc.mjs (`--check` compares the table between the markers)
    - src/browser/inputGuards.js head (Plan 02 removed the only code citation of COMBAT-NARRATIVE-DESIGN.md — confirm `git grep -n "COMBAT-NARRATIVE-DESIGN" -- ':!.planning' ':!docs'` prints nothing before deleting)
    - .planning/milestones/v1.3-phases/30-combat-narrative-input-research/ (`ls`) — the archived Phase 30 record the deletion points at
  </read_first>
  <action>
    Apply the bucket table. A status note is one blockquote line `> **Status (v1.6, Phase 48):** …` directly under the `**Date:**` line (or under the H1 when there is none); never a heading. Sweep = rewrite the quoted stale line(s) in place.

    | Doc | Bucket | Action |
    |---|---|---|
    | ABILITIES.md | (b) keep, no edit | zero stale lines; Phase 38's rules are live |
    | CLARITY.md | (a) keep + sweep | ~68 "the Oracle, the toasts, the presentation COPY objects" → "the Oracle, the rail lines, the presentation COPY objects"; ~73 "Every `EVENT_NARRATION`/`TOAST_FOR` builder" → "`EVENT_NARRATION`/`LINE_FOR`"; the Cost-event inventory table untouched (parsed by clarity-cause-lines.test.js) |
    | CLASS-PASS.md | (b) keep + status note | insert directly under the H1 (line 1): `> **Status (v1.6, Phase 48):** measurement record. The `wornSlots` run flag named in the v1.5 sections was retired in Phase 45 — the worn-slot model is the engine's only path and `RUN_FLAGS` is `{ storeRoll: true }`; each readout's `meta.runFlags` records the flags of its own era.` No other line changes (rendered blocks are byte-pinned; H2 positions are pinned) |
    | COMBAT-NARRATIVE-DESIGN.md | (c) DELETE | `git rm docs/COMBAT-NARRATIVE-DESIGN.md`. Reason for the SUMMARY: every surface it documents is retired — the toast system (§1.1; Phase 35 rail), the D-pad overlay (§1.2/§1.5; tap-to-move), the Round Card (§4/§6; Phase 34 fight log), the Guarded Bundled Toast runner-up (§5; never built); its one live rule (§6.3 tap guards) is stated in `src/browser/inputGuards.js`'s own header (Plan 02); the Phase 30 record survives in `.planning/milestones/v1.3-phases/30-combat-narrative-input-research/` and git history (`5c904a6`) |
    | DIFFICULTY-RETUNE.md | (b) keep, no edit | zero stale lines (the adb commands are live); test-pinned |
    | FLEE.md | (a) keep + sweep | ~125 `src/browser/toasts.js#fleeChain` → `src/browser/narrationLines.js#fleeChain`; ~127 `TOAST_FOR.fleeRolled` → `LINE_FOR.fleeRolled`; ~128 "**Toast** (`TOAST_FOR.fleeRolled`)" → "**Rail line** (`LINE_FOR.fleeRolled`)"; ~166 `TOAST_FOR` → `LINE_FOR`; ~171 `test/unit/toastsForAction.test.js` → `test/unit/linesForAction.test.js`; ~182 `src/browser/toasts.js#fleeChain` → `narrationLines.js#fleeChain`. The eight H2s and the `fleeRolled` / `docs/CLASS-PASS.md` mentions untouched |
    | GEAR-BALANCE.md | (b) keep + status note + sweep | status: `> **Status (v1.6, Phase 48):** design record of Phase 39; the rules are live. "What stays for the cleanup milestone" is done — Phases 44/47 deleted the classic duplicates it names; `toasts.js`/`TOAST_FOR` are `narrationLines.js`/`LINE_FOR` since Phase 46, and toasts gave way to the RAIL in Phase 35.` Sweeps: ~654 "the activation toast and the Oracle line" → "the activation rail line and the Oracle line"; ~809-810 "`src/browser/toasts.js` (TOAST_FOR/ORACLE_ONLY/FEATURE_EVENTS)" → "`src/browser/narrationLines.js` (LINE_FOR/ORACLE_ONLY/FEATURE_EVENTS)"; ~928 `dispatchWithToasts` → `dispatchWithNarration`; ~944-950 the "### What stays for the cleanup milestone" paragraph → keep the heading, replace the body with one line: "Done in v1.6 — Phase 44 (DEAD-01) and Phase 47 deleted the classic `strikeDie()`/`toHit()`/`useItem()` duplicates."; ~973-975 the deferred-list bullet → "(done — see above)". The retired-field table (~527-536) and the user-quote / "There is no D-pad" lines (~605, ~641) are retirement records — keep |
    | GEAR-SLOTS.md | (b) keep + status note | status: `> **Status (v1.6, Phase 48):** design record of Phase 37; the worn-slot model is live and unconditional since Phase 45 (the `wornSlots` run option and the reconciliation hedges are gone — old saves fold on load).` ~325 is already annotated by Phase 45; ~254 "never a toast" is a retirement statement — keep both |
    | PARLEY-REBALANCE.md | (b) keep, no edit | zero stale lines |
    | RATIONS.md | (a) keep, no edit | zero stale lines; test-pinned |
    | RELEASING.md | (a) keep, no edit | live ops doc (its only case-insensitive `ios` hit is inside `darktierstudios`) |
    | SHELL-MODULES.md | (a) keep + sweep | intro (~3-8): "(landing in Plans 03–05 of Phase 47). This doc is written first, before those carves, so the module contract is fixed and every … bridge … is listed in one place, with an owner, before a single line moves." → present tense: "(Phase 47). The contract below is what those modules implement, and every `window.__mz*` bridge crossing the classic-script/module-script seam is listed in one place, with an owner." The bridge table between the markers untouched (`node tools/bridge-doc.mjs --check` exit 0); `## Line budget` already records the 5,621 ruling — keep |
    | SPELLS.md | (b) keep + status note + sweep | status: `> **Status (v1.6, Phase 48):** design record of Phase 40; the spell table and mechanics are live. "What stays for the cleanup milestone" is done — Phase 44 deleted the classic `SPELLS`/`castSpell`/`rollGrimoire` duplicates.` Sweeps: ~724-730 the "### What stays for the cleanup milestone" body → one line "Done in v1.6 — Phase 44 (DEAD-01/DEAD-02) deleted the classic `SPELLS` table duplicate, `castSpell()` and `rollGrimoire()`."; ~761-765 the deferred bullet → "(done — see above)" |
    | TERRAIN.md | (a) keep + sweep | ~148 "crossing an 8-cell pool never stacks eight toasts" → "… never stacks eight rail lines" |
    | UAT-v1.5.md | keep untouched | the pending device batch (CONTEXT ruling); its checklist text mentions a toast (~141) and the classic script's eff (~42, live) — recorded, not edited |
    | USABLE-FEATURES-AUDIT.md | (b) keep + status note + sweep | status: `> **Status (v1.6, Phase 48):** audit record of Phase 31; the refusal vocabulary and every row are live. Refusals surface as RAIL lines / dull fight-log entries since Phase 35 (the toasts this doc cites were retired); `toasts.js` is `narrationLines.js` since Phase 46; §6's `c.haste`/`c.invis`/`c.ether`/`c.flightLeft`/`c.flightCooldown` rows name pre-Phase-39 counters that are `c.timers` records now (`foldLegacyCounters` folds a legacy save).` Sweeps: the opening blockquote (~7-9) "refusals are toasts — and stay toasts after Phase 32's Round Card ships (the design doc §4.2 already routes `PRIORITY.block` refusals to toasts as direct replies to a tap)" → "refusals are `PRIORITY.block` narration lines — a dull fight-log entry in combat, a RAIL line outside it — as direct replies to a tap"; ~20 "own toast + Oracle line (`src/browser/toasts.js` / `eventNarration.js`)" → "own narration line + Oracle line (`src/browser/narrationLines.js` / `eventNarration.js`)"; ~342 "explains via toast" → "explains via a rail line"; §6 rows ~294-299: append to each of the `c.haste`, `c.invis`, `c.ether`, `c.flightLeft` / `c.flightCooldown` first cells " (pre-Phase-39 counter — now a `c.timers` record)" — the literal field names STAY (usable-features-audit.test.js's doc-sync test asserts the doc contains `haste`, `invis`, `ether`, `flightLeft`, …) |

    After the edits run the ledger tests: `node --test test/unit/clarity-cause-lines.test.js test/unit/class-pass-ledger.test.js test/unit/identity-contract.test.js test/unit/difficulty-retune-ledger.test.js test/unit/flee-ledger.test.js test/unit/rations-audit.test.js test/unit/usable-features-audit.test.js test/unit/bridge-registry.test.js` (fail 0) and `node tools/bridge-doc.mjs --check` (exit 0). Then `npm test` (3288/0 — Plan 05 Task 3 is where the count grows). Commit: `docs(48-05): docs/ bucketed — COMBAT-NARRATIVE-DESIGN.md deleted (retired surfaces only); status notes + stale-line sweeps in nine ledgers`.
  </action>
  <verify>
    <automated>test ! -f docs/COMBAT-NARRATIVE-DESIGN.md && echo deleted; ls docs/*.md | wc -l; grep -c "Status (v1.6, Phase 48)" docs/CLASS-PASS.md docs/GEAR-BALANCE.md docs/GEAR-SLOTS.md docs/SPELLS.md docs/USABLE-FEATURES-AUDIT.md; node tools/bridge-doc.mjs --check; echo "check-exit=$?"; node --test test/unit/clarity-cause-lines.test.js test/unit/class-pass-ledger.test.js test/unit/identity-contract.test.js test/unit/difficulty-retune-ledger.test.js test/unit/flee-ledger.test.js test/unit/rations-audit.test.js test/unit/usable-features-audit.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f docs/COMBAT-NARRATIVE-DESIGN.md && echo deleted` prints `deleted`; `git log --diff-filter=D --name-only --oneline -1 -- docs/COMBAT-NARRATIVE-DESIGN.md | grep -c "COMBAT-NARRATIVE"` prints 1; `git grep -c "COMBAT-NARRATIVE-DESIGN" -- ':!.planning'` prints nothing
    - `ls docs/*.md | wc -l` prints 15
    - `grep -c "Status (v1.6, Phase 48)" docs/CLASS-PASS.md docs/GEAR-BALANCE.md docs/GEAR-SLOTS.md docs/SPELLS.md docs/USABLE-FEATURES-AUDIT.md` prints `1` for each of the five files; `grep -c "^#" docs/CLASS-PASS.md` equals the same count on `git show 649de2b:docs/CLASS-PASS.md` (no heading added — likewise for FLEE.md, DIFFICULTY-RETUNE.md, USABLE-FEATURES-AUDIT.md)
    - `grep -ciE "toasts\.js|TOAST_FOR|toastsForAction|dispatchWithToasts" docs/*.md | grep -v ":0$" | wc -l` prints 0
    - `grep -ciE "toast" docs/CLARITY.md docs/FLEE.md docs/TERRAIN.md docs/SHELL-MODULES.md | grep -v ":0$" | wc -l` prints 0; `grep -ciE "toast" docs/GEAR-BALANCE.md docs/GEAR-SLOTS.md docs/USABLE-FEATURES-AUDIT.md` prints only lines that are the status note or a retirement statement — list each in the SUMMARY (expected ≤ 5 lines total)
    - `grep -c "landing in Plans" docs/SHELL-MODULES.md` prints 0; `grep -c "cleanup milestone's job" docs/GEAR-BALANCE.md docs/SPELLS.md | grep -v ":0$" | wc -l` prints 0
    - `grep -cE "\| \`c\.(haste|invis|ether|flightLeft)\`.*pre-Phase-39 counter" docs/USABLE-FEATURES-AUDIT.md` prints a number >= 3 (the annotated §6 rows, names kept)
    - `git diff --stat 649de2b -- docs/UAT-v1.5.md docs/ABILITIES.md docs/DIFFICULTY-RETUNE.md docs/PARLEY-REBALANCE.md docs/RATIONS.md docs/RELEASING.md` prints nothing
    - `node tools/bridge-doc.mjs --check; echo "check-exit=$?"` ends with `check-exit=0`
    - `node --test test/unit/clarity-cause-lines.test.js test/unit/class-pass-ledger.test.js test/unit/identity-contract.test.js test/unit/difficulty-retune-ledger.test.js test/unit/flee-ledger.test.js test/unit/rations-audit.test.js test/unit/usable-features-audit.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3288` and `# fail 0`
  </acceptance_criteria>
  <done>Every docs/*.md describes the current game or carries a status note saying what is superseded; the one dead-surface doc is deleted with its reason; every ledger test and the bridge-doc check stay green.</done>
</task>

<task type="auto">
  <name>Task 3: test/unit/stale-terms.test.js (the standing tripwire) + the closing table, criteria 1-4 verbatim, REQUIREMENTS, SUMMARY</name>
  <files>test/unit/stale-terms.test.js, tools/stale-terms.mjs, .planning/REQUIREMENTS.md</files>
  <read_first>
    - tools/stale-terms.mjs (final ALLOWED after Plans 01-04; `scan`, `defaultSources`, `renderTable`, `TERMS`)
    - test/unit/bridge-registry.test.js (the house style for a tool-backed gate test: imports the tool's exports, one set-equality test over the real repo, fail-first teeth with synthetic inputs, sorted-order and shape tests)
    - `node tools/stale-terms.mjs; echo "exit=$?"` — MUST already exit 0 before this task writes the test; if it does not, the remaining unlisted lines are Plans 01-04's leftovers: fix them the same way (rewrite/delete, or ALLOWED with a reason) and record each in the SUMMARY under "Leftovers found at close"
    - .planning/ROADMAP.md Phase 48 `**Success Criteria**` 1-4 (verbatim — each is reproduced with its commands and outputs in the SUMMARY)
    - .planning/phases/48-stale-docs-comments-test-names-purge/48-0{1,2,3,4}-SUMMARY.md — the survivor lists, renamed titles, and fixture-hygiene numbers this SUMMARY consolidates
  </read_first>
  <action>
    1. **test/unit/stale-terms.test.js** — `import test from "node:test"; import assert from "node:assert/strict"; import { TERMS, ALLOWED, scan, defaultSources } from "../../tools/stale-terms.mjs";` Tests (exactly five, named so the SUMMARY can cite them):
       (a) `DOCS-01/03: every enforced stale-term row reports zero unlisted lines over the shipped surface` — `const { rows } = scan(defaultSources()); for (const r of rows.filter(r => r.enforced)) assert.equal(r.unlisted, 0, …)` with the failure message listing the unlisted `file:line: text` entries for that row (read them from `hits`);
       (b) `DOCS-01: no allow-list entry has rotted (every ALLOWED entry still matches a live line)` — `unusedAllow.length === 0` with the entries named;
       (c) `teeth: a synthetic comment that presents a retired surface as live is reported unlisted` — `scan([{ path: "x/a.js", text: "// the D-pad moves the party\nconst a = 1;\n" }], { allowed: [] })` → the `dpad` row has `unlisted === 1`; and the same with an ALLOWED entry `{ term: "dpad", file: "x/a.js", match: "moves the party", reason: "t" }` → `unlisted === 0`, `allowed === 1`;
       (d) `informational rows never fail: classic-script and legacy-won are enforced:false` — assert both ids exist and `enforced === false`, and that a synthetic `classic script` line lands on that row without affecting any enforced row;
       (e) `shape: TERMS ids are unique and every ALLOWED entry names a known term with a non-empty file, match and reason` — plus `new RegExp(entry.match)` compiles for every entry.
       Head comment: Phase 48's standing tripwire; how to add a survivor (an ALLOWED entry with a reason — never by weakening a regex); that the scanner reads RAW lines (comments are the subject) unlike ident-sweep/bridge-registry's comment-stripped scans.
    2. Run `node --test test/unit/stale-terms.test.js` (5/5) then `npm test` — record the new total as **3,288 + 5 = 3,293** (or whatever N the file has; state N).
    3. **Closing gates (run all, paste outputs verbatim into the SUMMARY under "## Success criteria (ROADMAP Phase 48, verbatim)"):**
       - Criterion 1: `node tools/stale-terms.mjs` (the full table; every enforced row `Unlisted` 0; the `## Allowed survivors` block is the listed-survivors record, grouped by class A/B/C with reasons; the `classic-script` row's count with the note "live term, kept — judgment call 1"; the `legacy-won` row's count after Plan 04 Task 3). Also record the recentre row (`recent(er|re).*(every|each) step`) and `dead classic|classic engine` at 0.
       - Criterion 2: `grep -cE "iOS|Xcode|Apple|macOS|App Store" .claude/CLAUDE.md` → 0; `grep -ciE "d-pad|dpad|toast" .claude/CLAUDE.md` → 0; the docs bucket table from Task 2 with the deletion reason.
       - Criterion 3: `ls test/unit | grep -iE "toast|dpad"` → empty; `grep -rniE "^\s*(test|describe|it)\(.*(toast|d-pad|dpad)" test/` → the list of absence-assertion titles only (each with its class-A reason from Plan 04's ALLOWED); `test/unit/fight-log-worst-case.test.js` rename evidence.
       - Criterion 4: `npm test 2>&1 | grep -E "^# (pass|fail)"` → `# pass 3293` (3,288 + 5, explained) / `# fail 0`; `node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` (every changed engine/content file `comment-only:`; zero fixture entries); `git diff --stat 649de2b -- test/parity/fixtures test/parity/prototype-master.js.txt` (empty); `git hash-object test/parity/prototype-master.js.txt` → `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `node --test test/parity/*.test.js` fail 0; `npm run build:www` exit 0; `npm run boot:check` 4 PASS; `node --test test/unit/shell-tab-snapshots.test.js` 10/10 with `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` = 1; `node tools/ident-sweep.mjs --self-test` PASS; `node tools/shell-sweep.mjs orphans` runs (exit code recorded); `node tools/bridge-doc.mjs --check` exit 0.
    4. **REQUIREMENTS.md:** mark DOCS-01, DOCS-02, DOCS-03 complete (checkbox + the traceability table rows → `Complete`).
    5. **SUMMARY sections the phase needs (beyond the template):** `## Judgment calls for the user to reverse` — (1) `classic script` kept as a live term (count); (2) the survivor classes A-D as applied; (3) the bounded body rule (local `toast*` identifiers and assertion messages renamed — list the files); (4) Plan 04 Task 3's fixture-hygiene commit (hash; `git revert <hash>` restores the literals). `## Docs bucket list` (the Task 2 table with the final per-file outcome). `## Survivors` (consolidated from Plans 01-04, grouped by class, file:line + reason). `## Test ledger` (3,288 → 3,293 with the five tripwire tests named). `## Human verification (deferred to end of run)` — none required: no user-visible change (record as such for the VERIFICATION).
    6. Commit: `test(48-05): stale-terms tripwire pinned (5 tests); Phase 48 closing gates — criteria 1-4 verified; DOCS-01..03 complete`.
  </action>
  <verify>
    <automated>node --test test/unit/stale-terms.test.js 2>&1 | grep -E "^# (pass|fail)"; node tools/stale-terms.mjs > /dev/null; echo "tool-exit=$?"; npm test 2>&1 | grep -E "^# (pass|fail)"; node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures; git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/stale-terms.test.js` exits 0; `grep -cE "^test\(" test/unit/stale-terms.test.js` prints 5; `node --test test/unit/stale-terms.test.js 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 5` and `# fail 0`
    - `node tools/stale-terms.mjs > /dev/null; echo "tool-exit=$?"` prints `tool-exit=0`; `node tools/stale-terms.mjs --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);console.log(j.rows.filter(r=>r.enforced).every(r=>r.unlisted===0), j.unusedAllow.length)})"` prints `true 0`
    - `node tools/stale-terms.mjs --self-test` prints `self-test: PASS`; `node tools/ident-sweep.mjs --self-test` prints `self-test: PASS`
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3293` and `# fail 0` (3,288 + the five tripwire tests; if N differs, the SUMMARY states N and the arithmetic)
    - `node tools/comment-only-diff.mjs 649de2b engine content test/parity/fixtures` prints only `comment-only:` lines and the summary line with `0 code-changed`, exits 0; `git diff --stat 649de2b -- test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `ls test/unit | grep -iE "toast|dpad"; echo "exit=$?"` prints only `exit=1`
    - `grep -rniE "^\s*(test|describe|it)\(.*(toast|d-pad|dpad)" test/ | wc -l` prints the same number as the count of class-A title entries recorded in the SUMMARY's criterion-3 list (expected 12-13; the 13 absence-assertion titles measured at 649de2b), and every listed title contains one of `retired|deleted|gone|zero|no toast|no D-pad|never toasts|survives|absent`
    - `grep -cE "iOS|Xcode|Apple|macOS|App Store" .claude/CLAUDE.md` prints 0; `test ! -f docs/COMBAT-NARRATIVE-DESIGN.md` exits 0
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines; `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1
    - `grep -cE "DOCS-0[123].*(Complete|complete)" .planning/REQUIREMENTS.md` prints 3
    - `git status --porcelain icons/ sfx/` prints nothing
  </acceptance_criteria>
  <done>The tripwire is a real test; criteria 1-4 hold verbatim with their outputs recorded; DOCS-01..03 complete; the SUMMARY names every survivor and every judgment call the user can reverse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md -> every future agent session | the file is loaded as project instructions; a wrong edit here misdirects later work — hence "delete rows, never change a rule" and the GSD-marker byte-identity check |
| docs/ -> ledger tests | eight tests parse or pin doc content; the ledger-test run is the gate for every doc edit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-10 | Tampering | .claude/CLAUDE.md rules | high | mitigate | hunks confined to lines 13-160; rule-line removal grep = 0; GSD markers = 14; six section headings intact |
| T-48-11 | Tampering | test-pinned docs | medium | mitigate | blockquote-only status notes; heading counts pinned; ledger tests + bridge-doc --check in the acceptance criteria |
| T-48-12 | Repudiation | doc deletion | low | mitigate | `git rm` with the reason and the archived Phase 30 pointer in the SUMMARY; `git grep` proves no live citation |
| T-48-13 | Denial of service | tripwire false positives on future work | low | mitigate | ALLOWED entries with reasons are the documented escape hatch; the test names the offending lines |
| T-48-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 48 |
</threat_model>

<verification>
- `node tools/stale-terms.mjs` exit 0; `node --test test/unit/stale-terms.test.js` 5/5
- CLAUDE.md grep → 0; docs bucket list recorded; ledger tests + bridge-doc --check green
- `npm test` 3293/0; comment-only-diff over engine/content/fixtures clean; master hash unchanged; build:www + boot:check + snapshot green
</verification>

<success_criteria>
- ROADMAP Phase 48 criteria 1-4 verified verbatim with recorded outputs
- The tripwire pins the state; DOCS-01, DOCS-02, DOCS-03 complete
- Every survivor and every judgment call is in the SUMMARY for the user to reverse
</success_criteria>

<output>
Create `.planning/phases/48-stale-docs-comments-test-names-purge/48-05-SUMMARY.md` when done — the phase's closing record. It MUST carry: `## Success criteria (ROADMAP Phase 48, verbatim)` with each criterion's commands and outputs; the final `node tools/stale-terms.mjs` table; `## Survivors` grouped by class (A absence assertions / B retirement statements / C tolerant-load reads and pins) with file:line + reason, consolidated from Plans 01-04; `## Docs bucket list`; `## Judgment calls for the user to reverse` (classic script; the survivor classes; the bounded body rule; the fixture-hygiene commit hash); `## Test ledger` (3,288 → 3,293, the five tripwire tests named); the comment-only-diff output; the master hash; and `## Human verification (deferred to end of run)` stating none is required (no user-visible change).
</output>
