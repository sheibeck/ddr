---
phase: 49-measure-first-perf-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/perfMarks.js
  - test/unit/perfMarks.test.js
  - mazeworld.html
  - docs/PERF-BASELINE.md
autonomous: true
requirements: [PERF-01]

must_haves:
  truths:
    - "PERF-01 (measurement half): a dev (start-at-depth) run records four timing rows per step — step (the whole stepWith body), dispatch (dispatchWithNarration), paint (one paint() call), draw (the trailing draw() call) — into a 100-sample ring and shows them as median / p95 / max on the #mw-dev-perf line under the Settings sheet's dev row, plus a `[mzperf]` console line every 10 steps"
    - "A normal (non-dev) run takes no timing reading and records nothing: every performance.now() call in the shell sits on a line guarded by the `perf` handle, and `perf` is null unless the live state's dev flag is true — pinned by source tests, not assumed"
    - "The pure module src/browser/perfMarks.js reads no window/document/performance global; its record/summary/formatReadout behaviour is unit-tested in node with injected samples (ring cap, nearest-rank median/p95, max, total vs n, reset, readout wording)"
    - "After `npm run build:www`, `grep -rn \"performance.now\" www/` lists only the guarded module-script lines inside stepWith — recorded verbatim (criterion 4 is met by construction; the instrumentation stays dev-gated rather than being removed)"
    - "The Phase 47 DOM snapshots (10/10, no fixture change), boot:check 4/4, npm test fail 0 (3,293 + the new suite), stale-terms exit 0, bridge-registry + bridge-doc --check green, engine fence untouched — at every commit"
    - "docs/PERF-BASELINE.md exists with the Method, planned Protocol (depth 5, computed from difficultyCurve) and How-to-re-measure sections filled; the Device / BEFORE / Jank / Decision / AFTER sections are named and explicitly reserved for Plan 49-02 (they carry the user's numbers, which do not exist yet)"
    - "The 49-01 SUMMARY carries a `## User checklist` the orchestrator pastes into chat verbatim and an empty `## Device report` section the orchestrator appends the user's numbers to"
  artifacts:
    - "src/browser/perfMarks.js — createPerfMarks(size), perfMarks (shared instance), formatReadout(summary), PERF_ROWS, PERF_RING_SIZE (100), PERF_LOG_EVERY (10)"
    - "test/unit/perfMarks.test.js — module behaviour tests + the shell dev-gate source pins"
    - "mazeworld.html — #mw-dev-perf inside #mw-dev-row (+ its CSS), the perfMarks import, the seven guarded timing lines in stepWith, perfReadout(perf), perfMarks.reset() in mzDevStartAtDepth"
    - "docs/PERF-BASELINE.md — Method / Protocol / How to re-measure filled; the report sections reserved for 49-02"
  key_links:
    - "stepWith(action) [module script] -> perf = state.dev ? perfMarks : null -> perf.record(row, ms) x4 -> perfReadout(perf) -> #mw-dev-perf textContent + console.log('[mzperf] ' + JSON)"
    - "mzDevStartAtDepth -> perfMarks.reset() + perfReadout(perfMarks) (a fresh dev run starts at n=0 with an empty, collapsed readout)"
    - "tools/build-www.mjs copies src/ into www/ -> the module import resolves in the APK; boot:check's headless Chrome proves the import parses"
  prohibitions:
    - statement: "MUST NOT change a single byte under engine/, content/, test/parity/ (master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0) — the marks are shell-only"
      requirement_id: PERF-01
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT record, read the clock, or write the readout when the live state's dev flag is falsy — no new setting, no persistence, nothing on the HUD"
      requirement_id: PERF-01
      category: values
      status: unverified
      verification: test
    - statement: "MUST NOT add a window.__mz* bridge (the brackets live in the module script, which imports perfMarks.js directly) — bridge.js and docs/SHELL-MODULES.md stay untouched"
      requirement_id: PERF-01
      category: values
      status: unverified
      verification: test
    - statement: "MUST NOT change any rendered DOM inside the snapshotted roots — the Phase 47 fixtures compare byte-equal; MZ_SNAPSHOT_UPDATE is never set"
      requirement_id: PERF-01
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT build or install the APK from this plan — the orchestrator builds it once after 49-01 lands (memory: build the debug APK only after the last wave)"
      requirement_id: PERF-01
      category: values
      status: unverified
      verification: judgment
---

<objective>
Land the phase's measurement method (CONTEXT `## Measurement (PERF-01)` — user ruling 2026-09-20): a pure ring-buffer module `src/browser/perfMarks.js`, seven `performance.now()` lines inside the module script's `stepWith(action)` bracketing four rows — `step` (the whole per-step body), `dispatch` (`dispatchWithNarration`), `paint` (the `window.paint()` call) and `draw` (the trailing `window.draw()` call) — every one behind the existing dev gate (`state.dev`), a `#mw-dev-perf` readout line inside the hidden `#mw-dev-row` (CONTEXT `Readout`), a `[mzperf]` console line every 10 steps for `adb logcat -s chromium`, the `www/` grep proof (CONTEXT `Ship proof`, ROADMAP criterion 4), the computed start depth D (CONTEXT `Device protocol`), the doc's method/protocol sections, and the user checklist the orchestrator hands over with the APK.

Purpose: PERF-01 — the shell's per-step cost becomes a number the user reads off the phone. Nothing is fixed here (PERF-02 is Plan 49-02's, after the numbers exist).

Output: `src/browser/perfMarks.js`, `test/unit/perfMarks.test.js`, the shell call sites + readout, `docs/PERF-BASELINE.md` (method half), a SUMMARY with the verbatim checklist and an empty Device report section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-measure-first-perf-pass/49-CONTEXT.md
@docs/SHELL-MODULES.md
@src/browser/bridge.js
@engine/difficulty.js
</context>

<!-- planner-discipline-allow: performance.now -->
<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->
<!-- planner-discipline-allow: performance -->
<!-- planner-discipline-allow: __mz -->
<!-- planner-discipline-allow: perf.record( -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/` (master `test/parity/prototype-master.js.txt` hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`). Presentation-only: `mazeworld.html`, `src/browser/`, `test/unit/`, `docs/`.
- **No `git add -A`.** Stage the named files only. Never touch `icons/`, `sfx/`, or `.planning/` beyond this phase's own artifacts (`49-01-SUMMARY.md`).
- **No behaviour change:** the Phase 47 DOM-snapshot smoke (`node --test test/unit/shell-tab-snapshots.test.js` → 10/10, `git diff --stat -- test/unit/fixtures/` empty) and `npm run boot:check` (4/4 PASS) hold at every commit. `#mw-dev-perf` sits in the Settings sheet, outside the snapshotted roots — prove it by RUNNING the smoke, never by assumption.
- **Dev gate only:** the marks record NOTHING when the live state's `dev` flag is falsy (`engine/state.js` sets `dev: startAt > 1` — true only for a start-at-depth run). No new setting, no persistence, no HUD change.
- **Retired vocabulary:** `tools/stale-terms.mjs` (Phase 48 tripwire) scans every new comment and test title — do not write the retired terms it lists (the old directional-pad control, the retired pop-up feedback surface, the deleted classic-engine mirror, the Phase 37 slot-option hedge, the pre-Phase-39 item counters, the retired per-step camera recentre). `node tools/stale-terms.mjs` must exit 0 at every commit.
- **Gate set at every commit:** `npm test` fail 0 (record the count: 3,293 at phase start + this plan's suite), `npm run build:www` exit 0, `npm run boot:check` 4/4, DOM snapshots 10/10 with no fixture change, `node --test test/unit/bridge-registry.test.js` fail 0 + `node tools/bridge-doc.mjs --check` exit 0 (nothing bridged — both stay green unchanged), `node tools/stale-terms.mjs` exit 0, fence empty.
- **Windows executor environment:** Git Bash, no `jq`. Write regex-heavy files (`perfMarks.test.js`) with the Write tool, never a heredoc. Edit `mazeworld.html` with the Edit tool only (the working copy is CRLF via autocrlf — never rewrite the whole file; `git diff --numstat HEAD -- mazeworld.html` must show a small hunk count, proving no whole-file line-ending rewrite). Line numbers below were measured at `9c9a755` (5,580 lines) and may drift — anchor by the marker strings given.
- **Verification agents are off:** the acceptance criteria ARE the verification; the orchestrator authors VERIFICATION.md from the `<verify>` blocks, so record every command's actual output in the SUMMARY.
</phase_ground_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/browser/perfMarks.js — the pure ring-buffer timing module, with its unit suite</name>
  <files>src/browser/perfMarks.js, test/unit/perfMarks.test.js</files>
  <read_first>
    - src/browser/tapStep.js head comment (the pure-module header style: what it is, who calls it, the no-globals rule) and test/unit/tapStep.test.js (a pure module's node:test suite shape — `import test from "node:test"; import assert from "node:assert/strict";`)
    - tools/ident-sweep.mjs `stripJs` export (imported by test/unit/bridge-registry.test.js line 37 — reuse it for the purity scan, never a second stripper)
    - test/unit/shell-input-guards.test.js lines 1-60: the `RAW_HTML`/`HTML`/`stripComments`/`CODE`/`sliceBetween` pattern the Task 2 shell pins will reuse in this same file
    - 49-CONTEXT.md `## Measurement (PERF-01)`: the contract — ring of the last N=100 samples per row; `record(row, ms)`; `summary()` → `{ row: { n, median, p95, max } }`; `formatReadout(summary)`; unit-tested with injected samples
  </read_first>
  <behavior>
    - exports: `createPerfMarks`, `perfMarks`, `formatReadout`, `PERF_ROWS` (frozen `["step","dispatch","paint","draw"]`), `PERF_RING_SIZE` (100), `PERF_LOG_EVERY` (10) — every one present and of the expected type/value
    - record 1..100 into "step" → summary().step deep-equals `{ n: 100, total: 100, median: 50, p95: 95, max: 100 }` (nearest-rank: median = sorted[ceil(0.5·n)−1], p95 = sorted[ceil(0.95·n)−1])
    - record 1..150 into "step" (ring cap 100) → `{ n: 100, total: 150, median: 100, p95: 145, max: 150 }` — the oldest 50 were dropped, total still counts them
    - a single sample 7.25 → `{ n: 1, total: 1, median: 7.25, p95: 7.25, max: 7.25 }`
    - `record("step", NaN)`, `record("step", Infinity)`, `record("step", -1)`, `record("step", undefined)`, `record("", 3)`, `record(42, 3)` → nothing recorded (summary stays `{}`)
    - rows recorded in the order draw, paint, dispatch, step come back from summary() with keys in PERF_ROWS order (step, dispatch, paint, draw); a row never recorded is absent (no zero-filled rows); an unknown row name ("oracle") is accepted and listed after the known rows
    - `createPerfMarks(3)`: after 5 records n is 3 and total is 5; two `createPerfMarks()` instances are independent (recording into one leaves the other's summary `{}`)
    - `reset()` → summary() is `{}` and a following record starts total at 1 again
    - `formatReadout({ step:{n:57,total:57,median:7.4,p95:12.1,max:30.2}, dispatch:{n:57,total:57,median:1.1,p95:2.0,max:4.0}, paint:{n:57,total:57,median:3.2,p95:6.0,max:9.9}, draw:{n:57,total:57,median:1.9,p95:4.3,max:7.7} })` equals exactly `step 7.4 / 12.1 / 30.2 · dispatch 1.1 / 2.0 / 4.0 · paint 3.2 / 6.0 / 9.9 · draw 1.9 / 4.3 / 7.7 ms (med / p95 / max, n=57)`; numbers are rendered with `toFixed(1)` (7 → `7.0`, 7.25 → `7.3`); `formatReadout({})` equals `""`; `n=` is the step row's n (or the first listed row's n when step is absent)
    - purity: `stripJs(readFileSync("src/browser/perfMarks.js"))` contains no `window`, `document`, `performance`, `globalThis`, `console` token and no `import` statement — the module is dependency-free
  </behavior>
  <action>
    1. Create `src/browser/perfMarks.js` with a head comment in the tapStep.js style: "src/browser/perfMarks.js — Phase 49 (PERF-01): pure per-row timing rings for the dev-gated step marks. The shell (mazeworld.html's module script, stepWith) takes the clock readings and calls record(row, ms); this module never reads a clock or a global, so it is unit-tested in node with injected samples. Rows: step (the whole stepWith body), dispatch (dispatchWithNarration), paint (one paint() call — which itself ends by calling draw(), so paint ≥ draw), draw (the trailing draw() call). Nothing here runs in a normal run: the shell's `perf` handle is null unless state.dev is true (docs/PERF-BASELINE.md, Method)." Do NOT put the literal clock-function name (`performance` + `.now`) anywhere in this file, comments included — the criterion-4 grep must list only the shell's guarded lines.
    2. Implement exactly the API in `<behavior>`: `PERF_ROWS`, `PERF_RING_SIZE = 100`, `PERF_LOG_EVERY = 10`; `createPerfMarks(size = PERF_RING_SIZE)` returning a frozen object `{ record, summary, reset, size }` over a private `Map` of row → `{ ring: number[], total: number }`. `record(row, ms)`: return early unless `typeof row === "string" && row` and `Number.isFinite(ms) && ms >= 0`; push, then `shift()` while `ring.length > size`; `total += 1`. `summary()`: build a plain object in PERF_ROWS order first, then any other recorded rows in first-recorded order; for each row with `ring.length > 0`: `n = ring.length`, `total`, a sorted copy (`[...ring].sort((a, b) => a - b)`), `median = sorted[Math.ceil(0.5 * n) - 1]`, `p95 = sorted[Math.ceil(0.95 * n) - 1]`, `max = sorted[n - 1]`. `reset()` clears the Map. `formatReadout(summary)`: `Object.keys(summary)` in the order summary() produced them, each rendered `${row} ${median.toFixed(1)} / ${p95.toFixed(1)} / ${max.toFixed(1)}`, joined with ` · ` (space, U+00B7 middle dot, space), then ` ms (med / p95 / max, n=${(summary.step || summary[firstKey]).n})`; an empty summary returns `""`. Export `perfMarks = createPerfMarks()` as the shell's one shared instance (a plain export, no bridge).
    3. Create `test/unit/perfMarks.test.js` (Write tool) with one `test(...)` per `<behavior>` bullet (10 module tests — exactly one test per bullet, no more, so the counts below hold), importing `stripJs` from `../../tools/ident-sweep.mjs` for the purity test. Title every test with the phase tag (`PERF-01: ...`). Task 2 appends the shell source pins to this same file — leave a `// ─── shell pins (Task 2) ───` section marker at the end.
    4. Run `node --test test/unit/perfMarks.test.js` (fail 0), then the full gate set. `npm test` count = 3,293 + 10 = 3,303; record it. Commit exactly these two files: `feat(49-01): src/browser/perfMarks.js — pure per-row timing rings (record/summary/formatReadout) + unit suite (PERF-01)`.
  </action>
  <verify>
    <automated>node --test test/unit/perfMarks.test.js 2>&1 | grep -E "^# (pass|fail)" && node -e "import('./src/browser/perfMarks.js').then(m => { const p = m.createPerfMarks(); for (let i = 1; i <= 150; i++) p.record('step', i); console.log(JSON.stringify(p.summary().step), m.formatReadout({ step: { n: 57, total: 57, median: 7.4, p95: 12.1, max: 30.2 } })); })" && node -e "import('./tools/ident-sweep.mjs').then(t => { const s = t.stripJs(require('fs').readFileSync('src/browser/perfMarks.js','utf8')); console.log(/\b(window|document|performance|globalThis|console)\b/.test(s) ? 'IMPURE' : 'pure'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/perfMarks.js && test -f test/unit/perfMarks.test.js` exit 0
    - `node --test test/unit/perfMarks.test.js 2>&1 | grep -E "^# pass"` prints `# pass 10`; `grep -E "^# fail"` prints `# fail 0`
    - the `node -e` above prints `{"n":100,"total":150,"median":100,"p95":145,"max":150} step 7.4 / 12.1 / 30.2 ms (med / p95 / max, n=57)`
    - the purity `node -e` prints `pure`; `grep -c "performance" src/browser/perfMarks.js` prints 0 (comments included)
    - `grep -cE "^export (const|function) (PERF_ROWS|PERF_RING_SIZE|PERF_LOG_EVERY|createPerfMarks|formatReadout|perfMarks)\b" src/browser/perfMarks.js` prints 6
    - `grep -c "^import " src/browser/perfMarks.js` prints 0
    - `node tools/stale-terms.mjs; echo "exit=$?"` prints `exit=0`
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3303` and `# fail 0`
    - `node --test test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` (the module script declares none of the six new export names — nothing imports them yet)
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `git show --stat HEAD --format= | grep -E "^ " | wc -l` prints 2 (only the two named files in the commit)
  </acceptance_criteria>
  <done>perfMarks.js exists as a dependency-free module whose ring/percentile/readout behaviour is pinned by 10 passing node tests; nothing in the shell references it yet; all gates green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the dev-gated call sites in stepWith, the #mw-dev-perf readout, the logcat line, the shell source pins, the www/ grep proof</name>
  <files>mazeworld.html, test/unit/perfMarks.test.js</files>
  <read_first>
    - mazeworld.html module script `function stepWith(action) {` (≈L4911) through `function stepNow(dir) {` (≈L4989): the exact statement order — `const wasCombat = ...`, `const { state, events, html } = dispatchWithNarration(action);`, `window.__mzState.set(state);`, `noteCombat(...)`, `hapticForEvents(events);`, the pending-rail block, `window.__mzPendingNarration = ...`, the diedOnMove beats block, `window.paint();`, `window.draw();`, `for (const line of html) window.logLine(line);`, the `jumped` / `mzCenterMap` / `mzKeepPartyInView` tail. NOTE: `paint()` (classic, ≈L2371-2449) ends with `renderEncounter(); renderRail(); draw();` — so the paint row includes one draw and stepWith then draws a second time; the draw row measures that second, standalone call. Record this fact in the Task 3 doc (it is a finding, not something to fix here).
    - mazeworld.html `window.mzDevStartAtDepth = async function devStartAtDepth(depth) {` (≈L4999-5011) — its first statement is `closeSettingsSheet();`; test/unit/shell-abilities.test.js line 144 slices this function to `\n  };` and asserts `surfaceAbilityPool(state);` is inside — keep that call where it is
    - mazeworld.html the dev-row markup line `<div class="mw-settings-row" id="mw-dev-row" hidden>...` (≈L1326, ONE line) and the comment block above it (≈L1319-1324); the dev-row CSS block `.mw-settings-row[hidden]{display:none}` … `.mw-dev-chip{...}` (≈L1154-1157) and its comment (≈L1147-1153)
    - mazeworld.html module-script import block (≈L4199-4319) — put the new import beside `import { resolveStep, inspectCell, HOLD_MS, TAP_MAX_TRAVEL_PX, DIR_VECTORS } from "./src/browser/tapStep.js";` (≈L4263)
    - test/unit/shell-gear-39.test.js lines 104-113 (the existing stepWith region pin: `sliceBetween(CODE, "function stepWith(action) {", "function stepNow(dir) {")` must still find `dispatchWithNarration(action)` and `window.__mzHasTool(state.c, "torch")`) and test/unit/shell-no-content-copies.test.js test 6 (the module script must not DECLARE — const/let/function — any perfMarks.js export name; an `import { … }` binding is fine)
    - test/unit/harness/shellSandbox.js line ~164 (`performance: { now: () => 0 }`) — the snapshot sandbox runs the CLASSIC script only; stepWith is module-script code and never runs there, so the snapshot smoke is unaffected by construction — still run it
    - 49-CONTEXT.md `## Measurement` + `Readout`: "Every call site is guarded by S.dev", the readout wording, the `[mzperf]` line every 10 steps, "No new setting, no persistence, nothing on the HUD"
  </read_first>
  <behavior>
    - (shell pin) the module script contains exactly one `import { perfMarks, formatReadout, PERF_LOG_EVERY } from "./src/browser/perfMarks.js";` line
    - (shell pin) in the comment-stripped shell, EVERY line containing `performance.now(` also contains `perf ? ` or `if (perf)`; the count of such lines is 7; and the RAW (comment-included) count of lines containing `performance.now` is also 7 — no comment anywhere in mazeworld.html spells the clock call
    - (shell pin) the `const perf = ` line inside the stepWith region contains `.dev ? perfMarks : null` — the dev gate is the only way `perf` is non-null
    - (shell pin) the stepWith region contains `perf.record("dispatch",`, `perf.record("paint",`, `perf.record("draw",`, `perf.record("step",` exactly once each and `perfReadout(perf)` once; file-wide, `perf.record(` occurs exactly 4 times (no recording site outside stepWith)
    - (shell pin) in the stepWith region the marks bracket the right calls, by index order: `const tStep` < `dispatchWithNarration(action)` < `perf.record("dispatch"` < `window.__mzState.set(state)`; and `const tPaint` < `window.paint();` < `perf.record("paint"` < `const tDraw` < `window.draw();` < `perf.record("draw"` < `window.logLine(line)` < `perf.record("step"`
    - (shell pin) `function perfReadout(perf) {` is declared exactly once in the module script and its body contains `"mw-dev-perf"`, `formatReadout(`, `PERF_LOG_EVERY`, `"[mzperf] "` and `JSON.stringify(`; it is never called from outside stepWith / mzDevStartAtDepth (file-wide `perfReadout(` count is 3: the declaration + two calls)
    - (shell pin) the mzDevStartAtDepth region (sliced to `\n  };`) contains `perfMarks.reset();` followed (later in the region) by `perfReadout(perfMarks);`, and still contains `surfaceAbilityPool(state);`
    - (shell pin) the markup contains `id="mw-dev-perf"` exactly once, positioned after `id="mw-dev-row"` and before the next `class="mw-settings-row"` or the closing `</div>` of `#mw-settings-rows` (i.e. inside the dev row), and the element carries no text content in the shipped markup (empty until the first recorded step)
    - (shell pin) the `<style>` block contains both `#mw-dev-perf{` and `#mw-dev-perf:empty{display:none}`; `getElementById("mw-dev-perf")` occurs exactly once in the whole file (inside perfReadout) — the readout has one writer and it is not paint()
    - (shell pin) the classic script (everything before `<script type="module">`) contains no `perfMarks`, `perfReadout` or `performance.now` token — the classic draw()/paint() bodies are untouched
  </behavior>
  <action>
    **A. Markup + CSS (same commit).**
    1. Inside the `#mw-dev-row` line, immediately before its final closing `</div>`, insert `<div id="mw-dev-perf"></div>` (empty; no class needed — the id rule below styles it). Extend the comment block above the row with two lines: "Phase 49 (PERF-01): #mw-dev-perf is the dev-run timing readout (med / p95 / max ms per row, written by the module script's perfReadout after every recorded step). It stays empty — and collapsed by the :empty rule — in a normal run, because the marks record nothing unless state.dev is true."
    2. After the `#mw-dev-start-depth{...}` CSS line add: `#mw-dev-perf{margin-top:8px;font-family:var(--mono);font-size:10px;letter-spacing:.04em;line-height:1.5;color:#c9bda0;opacity:.85;overflow-wrap:anywhere}` and `#mw-dev-perf:empty{display:none}` (two lines).

    **B. Module script.**
    3. Add the import line from `<behavior>` beside the tapStep.js import.
    4. In `stepWith(action)`, directly after the existing `const wasCombat = ...;` line, add a 4-line comment ("Phase 49 (PERF-01): dev-gated timing marks — `perf` is null unless this is a start-at-depth dev run (state.dev), so a normal run reads no clock and records nothing. Four rows, docs/PERF-BASELINE.md#Method.") — the comment must NOT contain the clock-call literal — then `const perf = window.__mzState.get()?.dev ? perfMarks : null;` and `const tStep = perf ? performance.now() : 0;`. Directly after the `dispatchWithNarration(action)` destructuring line add `if (perf) perf.record("dispatch", performance.now() - tStep);`. Replace the adjacent pair `window.paint();` / `window.draw();` with: `const tPaint = perf ? performance.now() : 0;` / `window.paint();` / `if (perf) perf.record("paint", performance.now() - tPaint);` / `const tDraw = perf ? performance.now() : 0;` / `window.draw();` / `if (perf) perf.record("draw", performance.now() - tDraw);`. As the LAST statement of stepWith (after the `mzKeepPartyInView` line) add `if (perf) { perf.record("step", performance.now() - tStep); perfReadout(perf); }`. That is seven clock lines total; every one carries `perf ? ` or `if (perf)`.
    5. Declare `function perfReadout(perf) { ... }` in the module script immediately above `function stepWith(action) {` (after the stepWith doc comment's neighbour, keeping the existing comments attached to stepWith). Body: `const s = perf.summary();` / `const el = document.getElementById("mw-dev-perf");` / `if (el) el.textContent = formatReadout(s);` / `if (s.step && s.step.total % PERF_LOG_EVERY === 0) console.log("[mzperf] " + JSON.stringify(s));`. Head comment (3 lines): "Phase 49 (PERF-01): the dev-row readout + the logcat line (`adb logcat -s chromium | grep mzperf`). Reached only from stepWith's `if (perf)` tail and mzDevStartAtDepth's reset, so it never runs in a normal run; the element lives in the Settings sheet, not the HUD."
    6. In `mzDevStartAtDepth`, directly after `closeSettingsSheet();`, add `perfMarks.reset();` and `perfReadout(perfMarks);` (an empty summary → `formatReadout` returns `""` → the readout collapses; no log line) with a one-line comment "Phase 49: a fresh dev run starts its timing rings at n=0."

    **C. Pins (same commit).** Append the `<behavior>` shell pins (10 tests — one per bullet) to `test/unit/perfMarks.test.js` under the Task 1 section marker, reusing the shell-input-guards `stripComments`/`sliceBetween` helpers (copy them — the file must stay self-contained like every other shell pin file) and slicing `MODULE = HTML.slice(HTML.indexOf('<script type="module">'))`, `CLASSIC = HTML.slice(0, that index)`. Region for stepWith: `sliceBetween(CODE, "function stepWith(action) {", "function stepNow(dir) {")`. Region for the dev start: `sliceBetween(CODE, "window.mzDevStartAtDepth = async function devStartAtDepth(depth) {", "\n  };")`.

    **D. Gates + the ship proof.** `npm run build:www` then run `grep -rn "performance.now" www/` and `grep -rn "performance.now" www/ | grep -vc "perf ? \|if (perf)"` — the first lists exactly 7 lines, all `www/index.html`, all inside stepWith; the second prints 0. Paste BOTH outputs verbatim into the SUMMARY under `## Ship proof (criterion 4)`. Then the full gate set (snapshots 10/10 + fixtures diff empty, boot:check 4/4, `npm test` = 3,293 + 20 = 3,313 fail 0, bridge-registry fail 0 + bridge-doc --check exit 0, stale-terms exit 0, fence). Also `git diff --numstat HEAD -- mazeworld.html` — expect roughly 25-35 added / 2-4 removed lines, proving no whole-file line-ending rewrite. Commit exactly `mazeworld.html` and `test/unit/perfMarks.test.js`: `feat(49-01): dev-gated step timing marks in stepWith, #mw-dev-perf readout, [mzperf] logcat line (PERF-01)`.
  </action>
  <verify>
    <automated>node --test test/unit/perfMarks.test.js test/unit/shell-tab-snapshots.test.js test/unit/shell-gear-39.test.js test/unit/shell-abilities.test.js test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)" && grep -c "performance.now" mazeworld.html && grep "performance.now" mazeworld.html | grep -vc "perf ? \|if (perf)"; npm run build:www >/dev/null 2>&1; grep -rn "performance.now" www/ | grep -vc "perf ? \|if (perf)"; grep -rc "performance.now" www/ | grep -v ":0$"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { perfMarks, formatReadout, PERF_LOG_EVERY } from "./src/browser/perfMarks.js";' mazeworld.html` prints 1
    - `grep -c "performance.now" mazeworld.html` prints 7; `grep "performance.now" mazeworld.html | grep -vc "perf ? \|if (perf)"` prints 0
    - `grep -c 'perf.record("' mazeworld.html` prints 4; `grep -c "function perfReadout(perf) {" mazeworld.html` prints 1; `grep -c "perfReadout(" mazeworld.html` prints 3
    - `grep -c "\.dev ? perfMarks : null" mazeworld.html` prints 1
    - `grep -c 'id="mw-dev-perf"' mazeworld.html` prints 1; `grep -c "#mw-dev-perf:empty{display:none}" mazeworld.html` prints 1
    - `awk '/<script type="module">/{exit} {print}' mazeworld.html | grep -c "perfMarks\|perfReadout\|performance.now"` prints 0 (classic script untouched)
    - `node --test test/unit/perfMarks.test.js 2>&1 | grep -E "^# pass"` prints `# pass 20`; `grep -E "^# fail"` prints `# fail 0`
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 10` and `# fail 0`; `git diff --stat HEAD -- test/unit/fixtures/` prints nothing; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1
    - `npm run build:www` exits 0; `grep -rn "performance.now" www/ | wc -l` prints 7; `grep -rn "performance.now" www/ | grep -vc "perf ? \|if (perf)"` prints 0; `grep -rl "performance.now" www/` prints exactly `www/index.html`
    - `npm run boot:check` prints four PASS lines (the perfMarks.js import resolved and parsed in headless Chrome)
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `node tools/bridge-doc.mjs --check; echo $?` prints 0; `git status --porcelain src/browser/bridge.js docs/SHELL-MODULES.md` prints nothing (no bridge added)
    - `node tools/stale-terms.mjs; echo "exit=$?"` prints `exit=0`
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3313` and `# fail 0`
    - `git diff --numstat HEAD~1 HEAD -- mazeworld.html` shows added ≤ 40 and removed ≤ 6 (no whole-file rewrite); `git ls-files --eol mazeworld.html` still reports `i/lf`
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>A dev run records four rows per step and shows them on #mw-dev-perf with a [mzperf] console line every 10 steps; a normal run reads no clock (pinned); the www/ grep lists only the 7 guarded lines; every gate green with no fixture, bridge or engine change.</done>
</task>

<task type="auto">
  <name>Task 3: docs/PERF-BASELINE.md (method + protocol + re-measure recipe, depth D computed) and the SUMMARY with the verbatim user checklist</name>
  <files>docs/PERF-BASELINE.md</files>
  <read_first>
    - engine/difficulty.js `difficultyCurve(depth)` return shape (`breather`, `darkBlobs`, `waterPools`, ...) and `isBreather` (every BREATHER_EVERY-th floor after 1 — depths 6, 11, 16, ...)
    - docs/DIFFICULTY-RETUNE.md lines 1064-1070 (the adb wireless install/relaunch recipe — quote it, do not paraphrase the commands)
    - docs/UAT-v1.6.md header (it points the user at docs/PERF-BASELINE.md for the protocol and says the perf protocol runs first — this task's doc is what they will read)
    - 49-CONTEXT.md `## Device protocol`, `## docs/PERF-BASELINE.md (criterion 1)` (the section list), and `## Deferred Ideas` (the Chrome remote-profiling cross-check — mention only as a future re-measure option)
    - .planning/STATE.md `Ground Truth` → `Device` (Pixel 7 `adb-28051FDH200H0R`, force-stop + monkey relaunch, Play-vs-local signer note)
  </read_first>
  <action>
    1. Compute D and record the command + output verbatim in the SUMMARY: `node -e "import('./engine/difficulty.js').then(m => { for (let d = 5; d <= 999; d++) { const c = m.difficultyCurve(d); if (!c.breather && c.darkBlobs >= 1) { console.log(JSON.stringify({ D: d, breather: c.breather, darkBlobs: c.darkBlobs, waterPools: c.waterPools })); break; } } })"` — the planner measured `{"D":5,"breather":false,"darkBlobs":3,"waterPools":2}` at `9c9a755`; if the executor's output differs, the executor's number wins and the checklist says so.
    2. Write `docs/PERF-BASELINE.md` (Write tool) with exactly these H2 sections in this order — **Method**, **Device**, **Protocol**, **BEFORE**, **Jank report**, **Decision**, **AFTER**, **How to re-measure**. Fill now: *Method* — the four rows and what each brackets (step = the whole stepWith body: dispatch, state set, paint(), the trailing draw(), the Oracle log append, the camera nudge; dispatch = dispatchWithNarration = engine action + narration fold + rail card push; paint = one paint() call, which ends by calling draw() so paint ≥ draw and DOM-only ≈ paint − draw; draw = the trailing standalone draw() — note that stepWith therefore draws the canvas twice per step, recorded here as a finding subject to the same ≥ 16 ms rule, not as a fix), the ring (last 100 samples per row), nearest-rank median/p95, the dev gate (`state.dev`, true only for a start-at-depth run — the marks read no clock otherwise), the readout wording, the `[mzperf]` JSON line every 10 steps, and the criterion-4 grep with its 7-line result pasted from Task 2. *Protocol* (planned) — depth D from step 1 with the curve values, ≥ 50 tapped steps, water + dark region entered and left, ≥ 1 encounter card, ≥ 1 tab switch, "representative, not a synthetic best case", what to read and how to report (rows med / p95 / max, n; device build number; APK commit; jank or "none"). *How to re-measure* — `npm run android:debug`, the quoted adb recipe, the long-press reveal, Start at D, walk, read `#mw-dev-perf` and/or `adb logcat -s chromium | grep mzperf`, and one line noting Chrome remote profiling (chrome://inspect over adb) as a declined-this-pass cross-check for a future pass. The four report sections (*Device*, *BEFORE*, *Jank report*, *Decision*, *AFTER*) each carry exactly one line: "Filled by Plan 49-02 from the user's Pixel 7 report (quoted as given, never rounded or estimated)." — they are not empty and not invented.
    3. Write the SUMMARY (`.planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md`) per the summary template PLUS three phase-specific sections, in this order at the end: `## Ship proof (criterion 4)` (the two grep outputs from Task 2 verbatim), `## User checklist` (the block below, verbatim — the orchestrator pastes it into chat; substitute D only if step 1 measured a different depth), and `## Device report` containing only the line "(appended by the orchestrator after the Pixel 7 session — Plan 49-02 stops if this section has no numbers)".

    The checklist text (copy exactly, keep the numbering):

    Phase 49 device checklist — Pixel 7 perf baseline (PERF-01)

    Build: debug APK at android/app/build/outputs/apk/debug/app-debug.apk from commit APK_COMMIT (the orchestrator fills this in after `npm run android:debug`).

    1. If a Play-installed build is on the phone, uninstall it first — a Play build and a local debug build have different signers.
    2. `adb devices` (or `adb mdns services` if the port rotated — the Pixel 7 is adb-28051FDH200H0R).
    3. `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`
    4. `adb shell am force-stop com.darktierstudios.delvedierepeat` then `adb shell monkey -p com.darktierstudios.delvedierepeat 1` (install alone does not reload the WebView).
    5. Title → ENTER → take any roll → on the map open Settings (the gear chip right of MAKE CAMP) and long-press the "Version …" label for about 1.2 s until the "Start at depth (dev)" row appears.
    6. Enter 5 in the depth field and tap Start. Depth 5 is the shallowest non-breather floor whose difficulty curve gives at least one dark blob (it gives 3, plus 2 water pools), so the floor has both a dark region and water. The HUD shows the DEV chip.
    7. Walk at least 50 steps by tapping, mixed the way you normally play: through a water pool, into a dark region and back out, at least one encounter card (fight, flee, parley — anything), and at least one tab switch (Gear or Hero, then back to the map). Do not steer around things to make the numbers look good.
    8. Open Settings again (the dev row stays revealed for the session) and read the line under the Start button. It reads like: step 7.4 / 12.1 / 30.2 · dispatch 1.1 / 2.0 / 4.0 · paint 3.2 / 6.0 / 9.9 · draw 1.9 / 4.3 / 7.7 ms (med / p95 / max, n=57) — three numbers per row are median / p95 / max in milliseconds; n is the number of steps in the ring (last 100). Optional cross-check: `adb logcat -s chromium | grep mzperf` prints the same numbers as JSON every 10 steps.
    9. Report back in this shape, copying the numbers exactly as shown (do not round): step med / p95 / max, n; dispatch med / p95 / max; paint med / p95 / max; draw med / p95 / max; device build number (Settings → About phone → Build number); APK commit (from the Build line above); jank — anything you saw and where (a stutter on a step, a late repaint, a tab flash) — or "none".
    10. Then run the v1.6 UAT batch (docs/UAT-v1.6.md, 26 items) on the same APK; its results go to the milestone close, not to this phase.

    4. Do NOT run `npm run android:debug` or any adb command from this plan (orchestrator's job, after the plan lands). Commit exactly `docs/PERF-BASELINE.md` and the SUMMARY: `docs(49-01): PERF-BASELINE.md method + planned protocol (depth 5) + re-measure recipe; device checklist (PERF-01)`.
  </action>
  <verify>
    <automated>grep -cE "^## (Method|Device|Protocol|BEFORE|Jank report|Decision|AFTER|How to re-measure)$" docs/PERF-BASELINE.md && grep -c "Filled by Plan 49-02" docs/PERF-BASELINE.md && grep -c "## User checklist\|## Device report\|## Ship proof (criterion 4)" .planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md && node -e "import('./engine/difficulty.js').then(m => { for (let d = 5; d <= 999; d++) { const c = m.difficultyCurve(d); if (!c.breather && c.darkBlobs >= 1) { console.log(JSON.stringify({ D: d, breather: c.breather, darkBlobs: c.darkBlobs, waterPools: c.waterPools })); break; } } })"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## (Method|Device|Protocol|BEFORE|Jank report|Decision|AFTER|How to re-measure)$" docs/PERF-BASELINE.md` prints 8, and `grep -nE "^## " docs/PERF-BASELINE.md` lists them in that order
    - `grep -c "Filled by Plan 49-02" docs/PERF-BASELINE.md` prints 5 (Device, BEFORE, Jank report, Decision, AFTER)
    - `grep -c "twice per step\|draws the canvas twice" docs/PERF-BASELINE.md` prints ≥ 1 (the double-draw finding is recorded); `grep -c "paint − draw\|paint - draw" docs/PERF-BASELINE.md` prints ≥ 1
    - `grep -c "adb install -r android/app/build/outputs/apk/debug/app-debug.apk" docs/PERF-BASELINE.md` prints ≥ 1; `grep -c "adb logcat -s chromium" docs/PERF-BASELINE.md` prints ≥ 1; `grep -ci "chrome://inspect\|remote profiling" docs/PERF-BASELINE.md` prints ≥ 1
    - the `node -e` prints `{"D":5,"breather":false,"darkBlobs":3,"waterPools":2}` and the SUMMARY quotes that exact line
    - `grep -c "## Ship proof (criterion 4)" .planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md` prints 1; `grep -c "## User checklist" …49-01-SUMMARY.md` prints 1; `grep -c "## Device report" …49-01-SUMMARY.md` prints 1; `grep -c "Plan 49-02 stops if this section has no numbers" …49-01-SUMMARY.md` prints 1
    - `grep -c "^10\. Then run the v1.6 UAT batch" …49-01-SUMMARY.md` prints 1 (the checklist is complete and numbered 1-10)
    - `git show --stat HEAD --format= | grep -E "^ " | wc -l` prints 2 (the doc + the SUMMARY only); no `android/`, `www/`, or `.planning/` file beyond the SUMMARY in the commit
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The doc's method half and the re-measure recipe exist; the report half is reserved for 49-02 by name; the SUMMARY carries the ship proof, the verbatim 10-step checklist with depth 5, and the empty Device report section the orchestrator fills.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device console -> adb logcat | the `[mzperf]` line is readable by anyone with adb access to the phone (dev runs only) |
| Settings sheet DOM | the readout writes engine-derived numbers via textContent, never markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-01 | Information disclosure | `[mzperf]` console line / #mw-dev-perf | low | mitigate | only timing numbers (no state, no save data) and only in a `state.dev` run — the source pins in perfMarks.test.js prove the gate; a normal player never triggers it |
| T-49-02 | Tampering | perfReadout -> #mw-dev-perf | low | mitigate | `textContent` write of a formatted number string, never innerHTML; input is the module's own summary, not user text |
| T-49-03 | Denial of service | the marks' own cost on the step path | low | mitigate | seven `perf ? x : 0` reads and four array pushes per step in a dev run, zero in a normal run; the ring is capped at 100 per row so memory is bounded |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 49 |
</threat_model>

<verification>
- `node --test test/unit/perfMarks.test.js` → 20 pass; `npm test` → 3,313 pass, fail 0
- `npm run build:www` → 0; `grep -rn "performance.now" www/` → 7 lines in `www/index.html`, every one guarded; `npm run boot:check` → 4/4
- `node --test test/unit/shell-tab-snapshots.test.js` → 10/10, fixtures diff empty; bridge-registry fail 0; `bridge-doc --check` 0; `stale-terms.mjs` 0; fence empty; master hash unchanged
- `docs/PERF-BASELINE.md` has the 8 sections; the SUMMARY has the ship proof, the checklist and the empty Device report section
</verification>

<success_criteria>
- PERF-01 measurement half: a dev run shows step / dispatch / paint / draw med / p95 / max on #mw-dev-perf and logs `[mzperf]` every 10 steps; a normal run reads no clock (source-pinned)
- Criterion 4 (no instrumentation ships ungated) is proven by the recorded www/ grep
- No bridge, no engine/content/parity byte, no fixture change, no HUD change; all gates green at each of the three commits
- The user has a 10-step checklist with depth 5 and a report shape; 49-02 has a named section to read from
</success_criteria>

<output>
Create `.planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md` when done. It MUST carry: the three commit hashes; `npm test` counts after Task 1 (3,303) and Task 2 (3,313); the snapshot output (10/10, fixtures diff empty); boot:check output; `## Ship proof (criterion 4)` with both grep outputs verbatim; the depth-D command and output verbatim; `## User checklist` verbatim (numbered 1-10, depth 5 unless re-measured); `## Device report` with only the orchestrator-appends-here line; the fence outputs (status empty, master hash); and a `## Judgment calls for the user` list: (1) the instrumentation stays dev-gated rather than being removed (one revert commit undoes it), (2) `dispatch` is the fourth row's name (it brackets dispatchWithNarration — engine action + narration fold + rail push — not the engine alone), (3) the readout shows max as well as med / p95 so the report needs no logcat, (4) the step row brackets the whole stepWith body (including the trailing draw, Oracle log append and camera nudge), a superset of "dispatch to the end of paint" — it can only over-report, never under-report.
</output>
