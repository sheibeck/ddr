---
phase: 49-measure-first-perf-pass
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - docs/PERF-BASELINE.md
  - .planning/REQUIREMENTS.md
  - mazeworld.html          # conditional — only a fix that cites a qualifying row (Task 1, branch B)
  - src/browser/            # conditional — same rule; presentation-only
autonomous: false
requirements: [PERF-01, PERF-02]

must_haves:
  truths:
    - "PERF-01: docs/PERF-BASELINE.md carries the Pixel 7 BEFORE table (step / dispatch / paint / draw × median / p95 / max / n, over ≥ 50 steps on a water + dark floor), the user's jank report in the user's words, the device build number, the APK commit and the date — every number quoted exactly as the user reported it, never rounded or estimated by the executor"
    - "PERF-02: the Decision section names the row(s) with median or p95 ≥ 16 ms, or the quoted jank, or says 'none'; every code change in the phase beyond the 49-01 instrumentation cites one of those; if none qualifies the phase's whole diff beyond the instrumentation is the doc — zero code — and the SUMMARY says, in these words, 'no row qualified; the phase closes with the baseline and no fix'"
    - "If a fix landed: it is one presentation-only commit whose message cites the row; the DOM snapshots still compare byte-equal; the doc's AFTER table (from a second device run on a second APK) shows that row below 16 ms with no other row slower — or the fix commit is reverted and the doc says so"
    - "The plan does not start without the user's numbers: Task 1 reads 49-01-SUMMARY.md's `## Device report` section and stops (BLOCKED) if it has none"
    - "Closing gates recorded verbatim: ROADMAP criteria 1-4 each with its evidence, npm test fail 0 (count), build:www, boot:check 4/4, DOM snapshots 10/10 unchanged, stale-terms exit 0, bridge-registry + bridge-doc --check, the engine fence (diff empty against 9c9a755, master hash), wc -l mazeworld.html at close; PERF-01 and PERF-02 marked complete in REQUIREMENTS.md"
  artifacts:
    - "docs/PERF-BASELINE.md — Device / Protocol (as run) / BEFORE / Jank report / Decision / AFTER sections filled from the report (AFTER only if a fix landed)"
    - ".planning/REQUIREMENTS.md — PERF-01 and PERF-02 checked and their table rows Complete"
    - ".planning/phases/49-measure-first-perf-pass/49-02-SUMMARY.md — the phase-closing SUMMARY with criteria 1-4 verbatim and the fix-or-no-fix sentence"
  key_links:
    - "49-01-SUMMARY.md `## Device report` (appended by the orchestrator) -> Task 1 -> docs/PERF-BASELINE.md BEFORE table + Decision"
    - "Decision row -> (branch B only) one commit citing it -> Task 2 checkpoint (AFTER run on a second APK) -> Task 3 AFTER table + the < 16 ms / no-other-row-slower check (else git revert)"
    - "Task 3 closing gates -> REQUIREMENTS.md PERF-01/02 Complete -> the orchestrator's VERIFICATION.md"
  prohibitions:
    - statement: "MUST NOT change a byte under engine/, content/, test/parity/ (master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0) — a fix is presentation-only, even if the dispatch row is the one that qualifies"
      requirement_id: PERF-02
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT land any code change that does not cite a BEFORE row with median or p95 ≥ 16 ms or a jank quoted from the user's report — the ROADMAP's 'measure first, fix only what is measured' rule, verbatim"
      requirement_id: PERF-02
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT round, estimate, interpolate or 'tidy' a reported number; MUST NOT invent a device build number or APK commit — a missing datum is written as 'not reported' and flagged in the SUMMARY"
      requirement_id: PERF-01
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT change rendered DOM in a fix (the Phase 47 snapshots are the lock; MZ_SNAPSHOT_UPDATE is never set) — if the only viable fix changes rendered DOM, stop and hand the proposal to the user instead of landing it"
      requirement_id: PERF-02
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT build or install an APK from this plan; MUST NOT use git add -A; MUST NOT touch icons/, sfx/, or .planning/ beyond 49-02-SUMMARY.md and the two PERF rows of REQUIREMENTS.md"
      requirement_id: PERF-02
      category: values
      status: unverified
      verification: judgment
---

<objective>
Close Phase 49 from the user's Pixel 7 report: fill `docs/PERF-BASELINE.md`'s report half (CONTEXT `## docs/PERF-BASELINE.md (criterion 1)`) with the numbers exactly as reported, apply the fix rule (CONTEXT `## Fix rule (PERF-02)` — verbatim from the ROADMAP: a code change is allowed only if it cites a row with median or p95 ≥ 16 ms per step, or a jank the user confirmed on device, quoted), land at most ONE presentation-only fix commit citing that row and run the AFTER protocol on a second APK if — and only if — a row qualifies, then record the closing gates and mark PERF-01/PERF-02 complete.

Dispatched only after the orchestrator has appended the user's device report to `49-01-SUMMARY.md` — this plan is written now but cannot start until those numbers exist (CONTEXT `## Claude's Discretion`, plan slicing).

Purpose: PERF-01 (the numbers are on record with method, commit and device build) and PERF-02 (only a measured hotspot or confirmed jank changed code — otherwise nothing did, and the SUMMARY says so in those words).

Output: the finished `docs/PERF-BASELINE.md`, REQUIREMENTS.md rows, the phase-closing SUMMARY; possibly one fix commit + its AFTER row.

Task shape note for the orchestrator: three tasks — Task 1 (doc + decision, plus the fix commit on branch B only) is the only task that may write code; Task 2 is a checkpoint that is SKIPPED on branch A (no row qualified); Task 3 is docs + gates. The checkpoint sits in this plan rather than a third plan because the CONTEXT locks two plans and the AFTER run is meaningless without the fix that precedes it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/49-measure-first-perf-pass/49-CONTEXT.md
@.planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md
@docs/PERF-BASELINE.md
</context>

<!-- planner-discipline-allow: performance.now -->
<!-- planner-discipline-allow: MZ_SNAPSHOT_UPDATE -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/` (master `test/parity/prototype-master.js.txt` hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`). Presentation-only: `mazeworld.html`, `src/browser/`, `test/unit/`, `docs/`.
- **No `git add -A`.** Stage the named files only. Never touch `icons/`, `sfx/`, or `.planning/` beyond this phase's own artifacts (`49-02-SUMMARY.md`; the two PERF rows + checkboxes in `REQUIREMENTS.md`, the precedent being 48-05's `0ed4d57`).
- **The fix rule, verbatim (PERF-02 / ROADMAP criterion 2):** a code change is allowed only if it cites a baseline row with median or p95 ≥ 16 ms per step, or a jank the user confirmed on device (quoted). Otherwise the phase's whole diff beyond the instrumentation is `docs/PERF-BASELINE.md`, and the SUMMARY says "no row qualified; the phase closes with the baseline and no fix" in those words — plus criterion 2's own words "zero code".
- **Numbers are the user's:** copy them as reported. If the report gives a number with more or fewer decimals than the readout format, keep the report's form. Nothing is rounded, averaged, or estimated by the executor.
- **No behaviour change:** the Phase 47 DOM-snapshot smoke (10/10, no fixture change) and `boot:check` 4/4 hold at every commit — a perf fix must not change rendered DOM; if it must (e.g. batching), that is a user decision: stop and ask, never regenerate a fixture.
- **Retired vocabulary:** `node tools/stale-terms.mjs` exit 0 at every commit (any new comment in a fix obeys the Phase 48 tripwire).
- **Gate set at every commit:** `npm test` fail 0 (3,313 after 49-01; record the count), `npm run build:www` exit 0, `npm run boot:check` 4/4, DOM snapshots 10/10 with no fixture change, bridge-registry fail 0 + `bridge-doc --check` exit 0, stale-terms exit 0, fence empty, `grep -rn "performance.now" www/` still only the 7 guarded lines.
- **Windows executor environment:** Git Bash, no `jq`; Write tool for new files, Edit tool for `mazeworld.html` (CRLF working copy — never a whole-file rewrite). No `npm run android:debug`, no adb — the orchestrator builds and the user installs.
- **Verification agents are off:** the acceptance criteria ARE the verification; record every command's actual output in the SUMMARY.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: read the device report (stop if absent) → fill PERF-BASELINE.md's report sections → apply the fix rule → on branch B only, land the one fix commit</name>
  <files>docs/PERF-BASELINE.md, mazeworld.html, src/browser/</files>
  <read_first>
    - .planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md — `## Device report` (the user's numbers, appended by the orchestrator; the ONLY source for every number in this task), `## User checklist` (the protocol they were asked to follow — compare against what they report having done), `## Ship proof (criterion 4)`, and the depth-D line
    - docs/PERF-BASELINE.md as 49-01 left it — the five sections carrying the single line "Filled by Plan 49-02 …" are this task's targets; the Method / Protocol / How to re-measure text stays
    - 49-CONTEXT.md `## Fix rule (PERF-02)` and `## docs/PERF-BASELINE.md (criterion 1)` (the section contract)
    - .planning/ROADMAP.md Phase 49 success criteria 1-3 (the exact wording Task 3 must quote)
    - ONLY on branch B: mazeworld.html `function stepWith(action) {` (the four brackets — the row that qualified tells you which region to read next: dispatch → `function dispatchWithNarration(action) {` (its shell half: `fightLogLinesFor`/`linesForAction`/`railCardFor`/`railPush`/`window.renderRail?.()`); paint → classic `function paint() {` (HUD writes, `window.__mzTabs.gear/hero(...)`, `renderEncounter()`, `renderRail()`, then `draw()`); draw → classic `function draw() {` (fog fill, the per-cell loop, icons, party); step-only → the remainder `for (const line of html) window.logLine(line)` (Oracle append) and `mzKeepPartyInView`/`mzCenterMap`)
  </read_first>
  <action>
    **A. Gate on the report.** Open `49-01-SUMMARY.md` and locate `## Device report`. If the section is missing, or contains no numeric row (no line with a `step` median), STOP: write nothing, commit nothing, and return `## BLOCKED — device report absent in 49-01-SUMMARY.md` with the exact text found under that heading. Do not proceed to B.

    **B. The doc's report half (Edit tool on docs/PERF-BASELINE.md).**
    1. *Device*: replace the reserved line with: Pixel 7; Android build number exactly as reported (or "not reported"); APK commit as reported / as the orchestrator's Build line gives it (or "not reported"); the session date (from the Device report's own date line, else today's date with "(session date not reported)"); "measurement build: the 49-01 instrumentation at commit <49-01 Task 2 hash>".
    2. *Protocol*: keep the planned paragraph and add a "**As run**" paragraph: depth started (as reported — the checklist said 5), steps walked (the report's n; if n = 100 write "≥ 100 (ring cap reached)"; if the report carries a total from the `[mzperf]` JSON, quote it), and what the user says was mixed in (water, dark, encounter card, tab switch) — each marked "yes" / "no" / "not reported", in the user's words where given.
    3. *BEFORE*: a table `| Row | median (ms) | p95 (ms) | max (ms) | n |` with the four rows step, dispatch, paint, draw, every cell copied from the report; a missing cell reads "not reported". Under the table, one line: "Numbers are the user's report, quoted as given."
    4. *Jank report*: the user's words in a blockquote, verbatim (including "none").
    5. *Decision*: apply the rule mechanically and show the work — a line per row: `<row>: median <m> ms, p95 <p> ms → qualifies` / `→ does not qualify`, then the jank: `jank: "<quote>" → qualifies as a confirmed jank at <where>` or `jank: none reported`. Then the verdict, one of:
       - **A — nothing qualifies:** "No row measured ≥ 16 ms (median or p95) and no jank was reported. Beyond the 49-01 instrumentation (the measurement method under criterion 1), the phase's whole diff is this document — zero code. No row qualified; the phase closes with the baseline and no fix."
       - **B — a row or a jank qualifies:** name it, and the target: dispatch → only the shell half of `dispatchWithNarration` (narration fold, rail card, `renderRail`) is fixable — the engine half is fenced by the milestone gate; if the dispatch row qualifies and the shell half cannot plausibly account for ≥ 16 ms (its per-step work is a few string folds and one rail push), write "the qualifying row is engine-side; fenced by the milestone gate (no engine bytes this milestone); recorded for the next tuning pass — no fix", and treat the plan as branch A from here on. paint → the DOM re-render (the two tab mounts run on every step even when the Gear/Hero tab is hidden; renderEncounter; renderRail; the embedded draw). draw → the canvas per-cell loop (fog fill, wall bevels, icons). step-only (step ≥ 16 while dispatch, paint and draw are each < 16) → the remainder is `step − (dispatch + paint + draw)`: the Oracle `logLine` append (a DOM write per narration line, plus scrolling) or the camera nudge — cite the arithmetic. jank → the surface the user named (a tab flash → the tab switch path `showTab`; a late repaint → `paint()`'s ordering; a stutter on a step → the step row's largest sub-row).
    6. Commit exactly `docs/PERF-BASELINE.md`: `docs(49-02): PERF-BASELINE.md — Pixel 7 BEFORE table, jank report, decision (PERF-01)`.

    **C. Branch B only — the one fix commit.** Skip this entire section on branch A.
    7. Read the region named by the Decision. Choose the smallest presentation-only change that removes the cost the row measures — examples of the allowed kind: skip a tab mount when its screen is hidden (only if the mounted DOM is identical when it is next shown — the snapshot's idempotency test is the guard); hoist an invariant out of draw()'s per-cell loop; avoid re-reading `getBoundingClientRect` per cell; batch Oracle `logLine` appends into one fragment. Not allowed: anything that changes the rendered DOM of the snapshotted roots (Gear / Hero / Store) or any rendered text, any engine/content/parity byte, any new setting, any behaviour change (a step must still produce identical events, identical Oracle lines, identical rail cards).
    8. If the only viable fix changes rendered DOM (e.g. a batched Oracle write that changes node structure, a tab mount that must be restructured): do NOT land it. Write `## Fix proposal needing a ruling (rendered DOM would change)` in the SUMMARY — the row cited, the change proposed, the DOM that would differ, the fixture(s) that would move — commit nothing further, and return `## BLOCKED — fix needs a user decision`. The orchestrator takes it to the user.
    9. Otherwise land it as ONE commit touching only shell/`src/browser` files (add a test pin in `test/unit/` if the change has a source-checkable shape — optional, same commit). Commit message: `perf(49-02): <what changed> — cites docs/PERF-BASELINE.md BEFORE row <row> (median <m> / p95 <p> ms) (PERF-02)` (for a jank: `— cites the user's confirmed jank: "<short quote>"`). Every gate in the ground rules, at this commit, including `node --test test/unit/shell-tab-snapshots.test.js` 10/10 with `git diff --stat -- test/unit/fixtures/` empty and `grep -rn "performance.now" www/` still 7 guarded lines.
    10. Append to the doc's *Decision* section: "Fix landed: <commit hash> — <one sentence>. AFTER run pending (Task 2)." — amend nothing else. Commit `docs/PERF-BASELINE.md`: `docs(49-02): decision — fix <hash> landed, AFTER run pending`.
  </action>
  <verify>
    <automated>grep -c "## Device report" .planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md && grep -c "Filled by Plan 49-02" docs/PERF-BASELINE.md; grep -cE "^\| (step|dispatch|paint|draw) \|" docs/PERF-BASELINE.md && grep -cE "qualifies|does not qualify" docs/PERF-BASELINE.md && grep -c "no row qualified; the phase closes with the baseline and no fix\|Fix landed:\|fenced by the milestone gate" docs/PERF-BASELINE.md && git status --porcelain engine/ content/ test/parity/ | wc -l && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Device report" .planning/phases/49-measure-first-perf-pass/49-01-SUMMARY.md` prints 1 and the section contains at least one line with the word `step` followed by numbers — else the task returned BLOCKED with zero commits (`git status --porcelain docs/` empty)
    - `grep -c "Filled by Plan 49-02" docs/PERF-BASELINE.md` prints 1 on branch A (only the AFTER section keeps it — reworded by Task 3) or 1 on branch B before Task 3 (the AFTER section, awaiting the run); it never prints ≥ 2 after this task
    - `grep -cE "^\| (step|dispatch|paint|draw) \|" docs/PERF-BASELINE.md` prints 4 (the BEFORE table has all four rows)
    - `grep -cE "→ (qualifies|does not qualify)" docs/PERF-BASELINE.md` prints 4 (one verdict line per row) and `grep -c "^jank:\|jank: " docs/PERF-BASELINE.md` prints ≥ 1
    - `grep -c "Numbers are the user's report, quoted as given." docs/PERF-BASELINE.md` prints 1
    - `grep -c "\*\*As run\*\*" docs/PERF-BASELINE.md` prints 1
    - branch A: `grep -c "no row qualified; the phase closes with the baseline and no fix" docs/PERF-BASELINE.md` prints 1 and `grep -c "zero code" docs/PERF-BASELINE.md` prints 1; `git log --oneline 49-01-close..HEAD -- mazeworld.html src/browser/` (use the 49-01 Task 3 hash) prints nothing
    - branch B: `git log -1 --format=%s -- mazeworld.html src/browser/ | grep -c "cites docs/PERF-BASELINE.md BEFORE row\|cites the user's confirmed jank"` prints 1; exactly one such commit since 49-01's close; `grep -c "Fix landed:" docs/PERF-BASELINE.md` prints 1
    - branch B: `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 10` / `# fail 0`; `git diff --stat <49-01 close>..HEAD -- test/unit/fixtures/` prints nothing; `npm run boot:check` prints four PASS lines; `npm run build:www` exits 0 and `grep -rn "performance.now" www/ | wc -l` prints 7; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `node tools/stale-terms.mjs; echo $?` prints 0
    - every branch: `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat 9c9a755..HEAD -- engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The doc carries the user's numbers verbatim, the row-by-row rule application and the verdict; on branch A nothing but the doc changed; on branch B exactly one presentation-only commit cites the qualifying row and every gate is green, with the AFTER run pending.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: AFTER run on a second APK — branch B only (SKIP on branch A: record "Task 2 skipped — no row qualified" in the SUMMARY and go to Task 3)</name>
  <files>docs/PERF-BASELINE.md</files>
  <action>Branch A (Task 1's Decision says "none" or "fenced by the milestone gate — no fix"): do NOT pause here. Write the line "Task 2 skipped — no row qualified, no fix landed, no AFTER run" into the SUMMARY's Task Commits section and continue to Task 3. Branch B (a fix commit landed in Task 1): do nothing further in code; present the what-built / how-to-verify text below and STOP — do not write, infer or pre-fill any AFTER number; Task 3 writes only what the user's resume text says. This checkpoint is never auto-approved.</action>
  <verify>
    <automated>grep -c "Fix landed:" docs/PERF-BASELINE.md</automated>
  </verify>
  <done>Branch A: the skip line is in the SUMMARY and Task 3 runs. Branch B: the executor is paused with the AFTER protocol presented; the grep prints 1 (a fix is on record awaiting its AFTER row).</done>
  <what-built>Branch B: the one fix commit from Task 1 (hash and cited row are in docs/PERF-BASELINE.md's Decision section). The 49-01 instrumentation is unchanged, so the same readout and `[mzperf]` line report the AFTER numbers. Nothing else changed. (Branch A: nothing was built — this checkpoint does not apply; the executor skips it.)</what-built>
  <how-to-verify>
    Orchestrator: build the second debug APK with `npm run android:debug` at the fix commit, record its commit hash as APK_COMMIT_AFTER.
    User — repeat the 49-01 checklist steps 1-9 exactly (uninstall a Play build if present; `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`; force-stop + monkey relaunch; Title → ENTER → roll → Settings → long-press Version → Start at depth 5; walk ≥ 50 tapped steps through water, into and out of a dark region, with ≥ 1 encounter card and ≥ 1 tab switch; read the line under the Start button), then report:
      1. step median / p95 / max, n
      2. dispatch median / p95 / max
      3. paint median / p95 / max
      4. draw median / p95 / max
      5. device build number (Settings → About phone → Build number)
      6. APK commit (APK_COMMIT_AFTER)
      7. jank — anything seen and where, or "none"; and specifically whether the BEFORE jank (if one was reported) is gone.
    Expected outcome: the cited row's median AND p95 are below 16 ms and no other row is slower than its BEFORE median / p95; the BEFORE jank (if any) is gone.
  </how-to-verify>
  <resume-signal>Paste the AFTER report (the seven items above, numbers copied exactly as shown on the phone). Type "skip" only if this is branch A and the checkpoint was reached in error.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: AFTER table (branch B) or the no-fix sentence (branch A); ROADMAP criteria 1-4 verbatim; closing gates; PERF-01/02 complete; the phase SUMMARY</name>
  <files>docs/PERF-BASELINE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - The Task 2 resume text (branch B) — the AFTER numbers, again the ONLY source for the AFTER table
    - .planning/ROADMAP.md `### Phase 49` success criteria 1-4 (quote each verbatim in the SUMMARY with its evidence)
    - .planning/REQUIREMENTS.md lines 47-48 (`- [ ] **PERF-01**`, `- [ ] **PERF-02**`) and the traceability rows `| PERF-01 | Phase 49 | Pending |`, `| PERF-02 | Phase 49 | Pending |` (≈L92-93) — the 48-05 precedent (`0ed4d57`) flipped DOCS-01..03 the same way
    - .planning/phases/48-stale-docs-comments-test-names-purge/48-05-SUMMARY.md — the closing-SUMMARY shape this milestone uses (criteria verbatim, gate outputs per commit, judgment calls for the user)
    - 49-01-SUMMARY.md `## Ship proof (criterion 4)` (re-run the same two greps now and paste both — the numbers must match unless a fix touched stepWith)
  </read_first>
  <action>
    1. *AFTER* section of `docs/PERF-BASELINE.md` (Edit tool):
       - Branch A: replace the reserved line with "No fix landed — no row qualified; the phase closes with the baseline and no fix. No AFTER run."
       - Branch B: a table identical in shape to BEFORE, filled from the resume text verbatim, plus device build number, APK_COMMIT_AFTER and the user's AFTER jank words in a blockquote. Then the check, shown: "cited row <row>: median <m> ms, p95 <p> ms → below 16 ms: yes/no; other rows slower than BEFORE: <list or none>; BEFORE jank gone: yes/no/not applicable". If the cited row is still ≥ 16 ms (median or p95) OR any other row's median or p95 is above its BEFORE value: `git revert --no-edit <fix hash>` (one commit, message auto-generated by revert is fine), write "Fix <hash> reverted (<reason>) — the phase closes with the baseline and no fix; the finding stands for the next perf pass." in both the AFTER and Decision sections, and treat the rest of this task as branch A for the wording. Otherwise write "Fix <hash> stands."
    2. `.planning/REQUIREMENTS.md`: `- [ ] **PERF-01**` → `- [x] **PERF-01**`, same for PERF-02; the two traceability rows `Pending` → `Complete`. Nothing else in the file.
    3. Run and record every closing gate: `npm test 2>&1 | grep -E "^# (pass|fail)"`; `npm run build:www`; `npm run boot:check`; `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)"` + `git diff --stat 9c9a755..HEAD -- test/unit/fixtures/`; `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)"` + `node tools/bridge-doc.mjs --check; echo $?`; `node tools/stale-terms.mjs; echo $?`; `grep -rn "performance.now" www/` + `grep -rn "performance.now" www/ | grep -vc "perf ? \|if (perf)"`; `git diff --stat 9c9a755..HEAD -- engine/ content/ test/parity/`; `git hash-object test/parity/prototype-master.js.txt`; `wc -l mazeworld.html`; `git log --oneline 9c9a755..HEAD -- mazeworld.html src/browser/ test/unit/ docs/` (the phase's complete code + doc commit list).
    4. Commit exactly `docs/PERF-BASELINE.md` and `.planning/REQUIREMENTS.md`: `docs(49-02): Phase 49 close — AFTER row / no-fix sentence, criteria 1-4 verified, PERF-01/02 complete`.
    5. Write `49-02-SUMMARY.md` (this commit or the orchestrator's — per the summary template) with, in order after the template sections: `## ROADMAP criteria 1-4 (verbatim, with evidence)` — each criterion quoted, then its evidence (1: the doc's sections + the BEFORE table's n and floor; 2: the commit list from step 3 with every code commit's cited row, or the exact sentence "Beyond the 49-01 instrumentation, the phase's whole diff is docs/PERF-BASELINE.md — zero code. No row qualified; the phase closes with the baseline and no fix."; 3: the AFTER table + fence, or "no fix landed — not applicable"; 4: the two grep outputs verbatim); `## Gate outputs per commit`; `## Fence` (diff empty since 9c9a755, hash); `## Judgment calls for the user` (carry forward 49-01's four, plus any this plan made — e.g. the dispatch-row-is-engine-side ruling if it happened); and `## Next` — "milestone close: the 26-item v1.6 UAT batch results (docs/UAT-v1.6.md) go to the milestone audit; the instrumentation removal is one revert of 49-01's Task 2 commit if the user wants it out".
  </action>
  <verify>
    <automated>grep -c "Filled by Plan 49-02" docs/PERF-BASELINE.md; grep -c "no row qualified; the phase closes with the baseline and no fix\|Fix .* stands\.\|reverted" docs/PERF-BASELINE.md && grep -cE "^\- \[x\] \*\*PERF-0[12]\*\*" .planning/REQUIREMENTS.md && grep -cE "^\| PERF-0[12] \| Phase 49 \| Complete \|" .planning/REQUIREMENTS.md && npm test 2>&1 | grep -E "^# (pass|fail)" && node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)" && git diff --stat 9c9a755..HEAD -- engine/ content/ test/parity/ test/unit/fixtures/ | wc -l && git hash-object test/parity/prototype-master.js.txt && wc -l mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Filled by Plan 49-02" docs/PERF-BASELINE.md` prints 0 (every reserved line replaced)
    - `grep -cE "^## (Method|Device|Protocol|BEFORE|Jank report|Decision|AFTER|How to re-measure)$" docs/PERF-BASELINE.md` prints 8 (the section contract intact)
    - branch A: `grep -c "no row qualified; the phase closes with the baseline and no fix" docs/PERF-BASELINE.md` prints ≥ 2 (Decision + AFTER) and the SUMMARY contains both that sentence and the words `zero code`
    - branch B: `grep -cE "^\| (step|dispatch|paint|draw) \|" docs/PERF-BASELINE.md` prints 8 (BEFORE + AFTER tables); `grep -c "Fix .* stands\.\|reverted" docs/PERF-BASELINE.md` prints ≥ 1; if reverted, `git log --oneline -1 | grep -c "Revert"` prints 1 and the fence + snapshot gates below still hold
    - `grep -cE "^\- \[x\] \*\*PERF-0[12]\*\*" .planning/REQUIREMENTS.md` prints 2; `grep -cE "^\| PERF-0[12] \| Phase 49 \| Complete \|" .planning/REQUIREMENTS.md` prints 2; `git diff --numstat HEAD~1 HEAD -- .planning/REQUIREMENTS.md` shows 4 added / 4 removed (nothing else in the file moved)
    - `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0` (count recorded: 3,313 + any branch-B pin); `npm run build:www` exits 0; `npm run boot:check` prints four PASS lines
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 10` / `# fail 0`; `git diff --stat 9c9a755..HEAD -- test/unit/fixtures/` prints nothing; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `node tools/bridge-doc.mjs --check; echo $?` prints 0; `node tools/stale-terms.mjs; echo $?` prints 0
    - `grep -rn "performance.now" www/ | grep -vc "perf ? \|if (perf)"` prints 0 and `grep -rl "performance.now" www/` prints exactly `www/index.html`
    - `git diff --stat 9c9a755..HEAD -- engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - the SUMMARY quotes ROADMAP criteria 1-4 verbatim, each followed by evidence; on branch A it contains the exact sentence "no row qualified; the phase closes with the baseline and no fix"
    - `git show --stat HEAD --format= | grep -E "^ " | grep -vc "docs/PERF-BASELINE.md\|.planning/REQUIREMENTS.md\|49-02-SUMMARY.md"` prints 0 (the closing commit touches nothing else)
  </acceptance_criteria>
  <done>PERF-BASELINE.md is complete (BEFORE, decision, AFTER or the no-fix sentence); PERF-01/02 are marked complete; every closing gate and criteria 1-4 are recorded with evidence; the fence is intact since 9c9a755; the SUMMARY says, in those words, whether the phase closed with the baseline and no fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user report -> docs/PERF-BASELINE.md | free text from the orchestrator-appended report is copied into a doc (markdown only, never executed or rendered by the app) |
| fix commit -> shipped shell | the only code this plan may ship; presentation-only, gated by the ≥ 16 ms / quoted-jank rule |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-49-04 | Repudiation | BEFORE / AFTER numbers | medium | mitigate | every number is quoted from the user's report with the device build number, APK commit and date beside it; the executor never rounds or estimates ("not reported" is written instead) — the SUMMARY carries the same numbers so the doc and the phase record agree |
| T-49-05 | Tampering | a branch-B fix in the shell | medium | mitigate | one commit, presentation-only, cited row in the message; DOM snapshots byte-equal (fixtures never regenerated), boot:check, full suite, fence and the 7-line www grep at that commit; reverted by Task 3 if the AFTER run does not show the row below 16 ms with no other row slower |
| T-49-06 | Elevation of privilege | REQUIREMENTS.md edit | low | mitigate | only the two PERF rows and their checkboxes change (numstat 4/4 pinned in Task 3's criteria) |
| T-49-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 49 |
</threat_model>

<verification>
- Task 1 cannot run without `## Device report` numbers (BLOCKED otherwise); the doc's BEFORE table has four rows quoted as given; the Decision shows the rule applied per row
- Branch A: no commit touches `mazeworld.html`/`src/browser/`; the doc and SUMMARY carry "no row qualified; the phase closes with the baseline and no fix" and "zero code"
- Branch B: exactly one `perf(49-02): … cites …` commit; the checkpoint's AFTER report fills the AFTER table; the row is < 16 ms with no other row slower, or the fix is reverted
- Closing gates green; PERF-01/02 Complete; fence intact since 9c9a755
</verification>

<success_criteria>
- ROADMAP criterion 1: `docs/PERF-BASELINE.md` records the per-step timings (median / p95 over ≥ 50 steps on a water + dark floor) from the Pixel 7, with the method, the APK commit and the device build number
- ROADMAP criterion 2: every code change beyond the instrumentation cites a ≥ 16 ms row or a quoted jank — or the SUMMARY says "zero code" / "no row qualified; the phase closes with the baseline and no fix" in those words
- ROADMAP criterion 3: an AFTER row below 16 ms with no other row slower if a fix landed, presentation-only, fence intact
- ROADMAP criterion 4: the www/ grep after build:www shows only the dev-gated sites (re-recorded at close)
- PERF-01 and PERF-02 marked complete
</success_criteria>

<output>
Create `.planning/phases/49-measure-first-perf-pass/49-02-SUMMARY.md` when done. It MUST carry: the branch taken (A or B) and why (the per-row rule lines); the commit list since 9c9a755 for `mazeworld.html`, `src/browser/`, `test/unit/`, `docs/` with each code commit's cited row; `## ROADMAP criteria 1-4 (verbatim, with evidence)`; `## Gate outputs per commit`; `## Fence`; the two www/ grep outputs verbatim; `wc -l mazeworld.html` at close; `## Judgment calls for the user`; `## Next` (milestone close; the UAT batch results route; the one-revert instrumentation removal option). On branch A the SUMMARY contains the exact sentence: "no row qualified; the phase closes with the baseline and no fix".
</output>
