---
phase: 45-collapse-the-phase-37-hedges
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - engine/state.js
  - engine/saveState.js
  - engine/derived.js
  - src/browser/engineAdapter.js
  - tools/lib/tuning-bot.mjs
  - test/parity/harness/comparables.js
  - test/parity/chargen-parity.test.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/parity/full-suite.test.js
  - test/parity/fixtures/action-script.chargen.json
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixtures/action-script.economy.json
  - test/parity/fixtures/action-script.encounters.json
  - test/parity/fixtures/action-script.movement.json
  - test/parity/fixtures/action-script.schema.md
  - test/parity/divergence-records.test.js
  - test/unit/worn-migration.test.js
  - test/unit/engineAdapter.test.js
  - test/unit/bot-tactics.test.js
  - test/unit/class-pass-ledger.test.js
  - test/unit/item-activation.test.js
  - test/unit/worn-model.test.js
  - test/unit/worn-slots.test.js
  - test/unit/save-validation.test.js
autonomous: true
requirements: [HEDGE-01, HEDGE-02, HEDGE-03]
tags: [engine, worn-model, save-migration, parity-harness, fixtures, declared-divergence, single-path]

must_haves:
  truths:
    - "HEDGE-01: `newRun(seed, exclude, { startDepth, force, storeRoll })` has no worn option — `reconcileWorn(c)` runs on every fresh roll, so `newRun(2)` with NO options yields a Thief whose starting cloak is `c.worn.cloak` and whose `c.items` is `[]`; `test/unit/chargen-rng-pin.test.js` is unedited and green (zero rng draws added)"
    - "HEDGE-01 [empty]: `newRun(1)` (Fighter) and `newRun(7)` (Magic User), called with no options, carry `c.worn` deep-equal to `{}` and `c.items` deep-equal to `[]` — pinned option-free"
    - "HEDGE-01 [adjacency]: for every chargen fixture seed, `newRun(seed).c.worn` has at most one populated key and it is `cloak` when present (chargen issues at most one slot item — the Thief's single cloak — so no two equal items ever meet in the fresh-roll reconcile); `c.items` contains no `kind: \"cloak\"` entry"
    - "HEDGE-01 [ordering]: two `newRun(seed)` calls in one process are JSON-identical for every chargen fixture seed, `c.worn` included"
    - "HEDGE-02: `validateSave(raw, { freshSeed })` always reconciles and always returns `{ ok: true, value, wornReport }` (`wornReport` is `[]` when nothing moved — one shape, never a conditional key); `rehydrate(obj)` takes no options and always reconciles; a v1.4-era save (no `c.worn`, two identical Rings of Power + two cloaks + a potion stacked in the bag) loads through both with no option argument and comes out legal — jewelry1/jewelry2 = the two rings, cloak = the first cloak, `c.items` = [potion, second cloak], the report returned once"
    - "HEDGE-02 [ordering]: a legacy save carrying three Rings of Power distinguishable only by `txt` (`first`/`second`/`third`) loads with `worn.jewelry1.txt === \"first\"`, `worn.jewelry2.txt === \"second\"`, `c.items` = [the third] — the first copies in bag order are worn, extras stay bagged in their original order, and the report is `[{ slot: \"jewelry\", worn: [\"Ring of Power\", \"Ring of Power\"], bagged: [\"Ring of Power\"] }]`"
    - "HEDGE-02 [adjacency]: two identical rings neither merge nor collide — both are worn (jewelry1, jewelry2) and nothing is bagged; `worn` entries stay distinct objects"
    - "HEDGE-02 [empty]: a legacy save with no slot items (a Fighter with `items: []`) loads to `c.worn` deep-equal `{}` and `wornReport` deep-equal `[]`; a save that already carries `c.worn` loads byte-identical in `worn`/`items` and reports `[]`"
    - "HEDGE-02 [idempotency]: `rehydrate(rehydrate(x))` deep-equals `rehydrate(x)` on the v1.4-era save, and `validateSave` run twice over its own output moves nothing the second time (`wornReport` `[]`) — pinned with no option"
    - "HEDGE-03 [adjacency]: every declared divergence record across all six fixtures is narrow — `fields` non-empty, `before`/`after` carry only declared keys, and every declared field differs before vs after (`notDeepStrictEqual`) — enforced by `test/parity/divergence-records.test.js`, so a fixture whose scan diff is empty cannot be declared without failing the suite"
    - "HEDGE-03 [empty]: the holders declaring `worn` in a record's `fields` are EXACTLY the site ids on `tools/worn-fixture-scan-output.txt`'s `MOVED SET` line (parsed by `divergence-records.test.js`) — no undeclared moved site, no declared unmoved site; the set is non-empty"
    - "HEDGE-03 [ordering]: re-running `node tools/worn-fixture-scan.mjs` after the engine edit produces stdout byte-identical to the committed BEFORE readout (`git diff --exit-code tools/worn-fixture-scan-output.txt` after redirecting a fresh run into it prints nothing)"
    - "HEDGE-03: `git status --porcelain test/parity/fixtures` at the commit lists only the fixture files that hold a MOVED SET site (plus `action-script.schema.md`); every other fixture is byte-identical; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`"
    - "`grep -rn \"wornSlots\" engine/ src/ mazeworld.html tools/ test/ | wc -l` prints `0` and `grep -c \"stripWornField\" test/parity/harness/comparables.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js engine/state.js` prints `0` for every file — no option, no option-keyed branch, no Phase 37 carve-out survives"
    - "`RUN_FLAGS` deep-equals `{ storeRoll: true }` and `playRun` states carry `c.worn` (a forced Thief starts with its cloak worn); `class-matrix.test.js`'s `meta.runFlags` pin follows `RUN_FLAGS` unchanged"
    - "This plan is ONE commit: `npm test` ends `# fail 0` and `npm run build:www` exits 0 at that commit; no intermediate commit exists between Plan 01's and it"
  artifacts:
    - path: "engine/state.js"
      provides: "newRun with the single worn path"
      contains: "reconcileWorn(c);"
    - path: "engine/saveState.js"
      provides: "validateSave/rehydrate reconciling unconditionally, one return shape"
      contains: "return { ok: true, value, wornReport };"
    - path: "test/parity/harness/comparables.js"
      provides: "dropEmptyWorn (replaces the Phase 37 strip) + the undefined-omission rule in chargenShiftDiffs/pickFields"
      contains: "export function dropEmptyWorn("
    - path: "test/parity/divergence-records.test.js"
      provides: "every record narrow + the MOVED SET ↔ declared-holders equality"
      contains: "MOVED SET"
    - path: "test/unit/worn-migration.test.js"
      provides: "the HEDGE-01 and HEDGE-02 success-criterion pins, option-free"
      contains: "HEDGE-02"
  key_links:
    - from: "engine/state.js#newRun"
      to: "engine/derived.js#reconcileWorn"
      via: "unconditional call after grantLevelAbilities, before genFloor (no rng)"
      pattern: "^\\s*reconcileWorn\\(c\\);"
    - from: "engine/saveState.js#validateSave"
      to: "engine/derived.js#reconcileWorn"
      via: "unconditional, null-coalesced to [] for the one return shape"
      pattern: "reconcileWorn\\(value\\.c\\) \\?\\? \\[\\]"
    - from: "src/browser/engineAdapter.js#boot"
      to: "engine/saveState.js#validateSave / #rehydrate"
      via: "no worn option; bootWornReport = check.wornReport"
      pattern: "validateSave\\(raw, \\{ freshSeed \\}\\)"
    - from: "test/parity/chargen-parity.test.js + full-suite.test.js"
      to: "comparables.js#dropEmptyWorn + the fixture's divergences[seed] record (fields items/worn)"
      via: "engine-side dropEmptyWorn, then the record's before/after assertions, then stripDeclaredFields on both sides"
      pattern: "dropEmptyWorn\\("
    - from: "test/parity/divergence-records.test.js"
      to: "tools/worn-fixture-scan-output.txt"
      via: "parses the MOVED SET line and maps site ids to fixture holders"
      pattern: "MOVED SET"
  prohibitions:
    - statement: "MUST NOT edit `test/parity/prototype-master.js.txt`, and MUST NOT blanket-regenerate or blanket-strip: every moved site gets its own record with measured before≠after values from the scan; any fixture not on the MOVED SET line stays byte-identical"
      status: resolved
      verification: test
    - statement: "MUST NOT keep, rename, or re-introduce a run/load option or an option-keyed branch under any name (no 'legacy state' path kept for fixtures, bots, tools or tests; no `reconcileWorn` guard on a flag) — a fixture or test that the single path moves is re-pinned to the single path, never shielded"
      status: resolved
      verification: test
    - statement: "MUST NOT change any gameplay rule or rng draw: `reconcileWorn`'s wear rule, `eff()`, `autoWearSlot`, `freeWornKey`, chargen draws and the load-chain order are untouched; the engine diff is exactly `engine/state.js` (signature + the unconditional call + comments), `engine/saveState.js` (the two unconditional calls, the one return shape, JSDoc) and the `engine/derived.js` `reconcileWorn` JSDoc; `test/unit/chargen-rng-pin.test.js` is unedited"
      status: resolved
      verification: test
    - statement: "MUST NOT edit the frozen `docs/class-pass/*.json` readouts or `docs/CLASS-PASS.md`'s Phase 42 transcription, and MUST NOT 'fix' the stored v1.5 AFTER `runFlags` to the new shape — the ledger test's stored-file assertion is reworded to what is true of the frozen files without spelling the retired identifier"
      status: resolved
      verification: judgment
---

<!-- planner-discipline-allow: wornSlots -->
<!-- planner-discipline-allow: stripWornField -->

<objective>
Collapse the Phase 37 hedges into the engine's only path and land the fixtures that measurably move in the SAME commit, so the suite is never red at a commit boundary: `newRun` always creates `c.worn` (zero rng draws), `validateSave`/`rehydrate` always reconcile (one return shape, `wornReport: []` when nothing moved), the shell adapter and the tuning bot drop their option arguments, the parity harness replaces the Phase 37 carve-out with `dropEmptyWorn` (an empty map is the engine's spelling of the prototype's "no worn model"; a populated map reaches the diff and must be declared), exactly the MOVED SET sites from Plan 01's committed scan get their `items`/`worn` before/after records, a new `divergence-records.test.js` enforces "declared set == measured set, every field before≠after", and every test that pinned the option-off/legacy behaviour is re-pinned to the single path — including the two ROADMAP success-criterion pins (HEDGE-01 `newRun(2)` no-option cloak worn; HEDGE-02 v1.4-era save reconciled with no option).

Purpose: the greenfield ruling (2026-09-17) — no dual-path code, no option kept for fixtures, the bot plays the new rules; fixtures follow by declaration, never by silence. This is the ONLY fixture-moving phase of v1.6, bounded to the files listed in the frontmatter.
Output: one commit; `wornSlots` ×0 in engine/src/shell/tools/test; the scan re-run byte-identical; `npm test` fail 0; `npm run build:www` green.

IMPORTANT — commit discipline: Tasks 1 and 2 must NOT be committed on their own (the suite is red between them by construction — the engine moves before the pins/records catch up). The single commit for this plan is made at the end of Task 3 once every gate passes. Never run `git commit` in Task 1 or Task 2.

Never spell the retired option name or the deleted strip's name in any NEW comment, test name, or doc string you write — refer to them as "the Phase 37 run/load option" and "the Phase 37 worn carve-out"; success criterion 1's grep counts every occurrence, comments included.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-collapse-the-phase-37-hedges/45-CONTEXT.md
@.planning/phases/45-collapse-the-phase-37-hedges/45-01-SUMMARY.md
@tools/worn-fixture-scan-output.txt
</context>

<phase_facts>
- Line numbers below are HEAD `7447629` numbers; locate by name if they drift.
- `engine/derived.js:350` `reconcileWorn(c)` returns `null` when `c` already owns `worn` (the no-op), else the report array (`[]` when nothing wearable). `validateSave` must therefore coalesce `null` to `[]` for the one-shape return; `rehydrate` discards the return value as today.
- `test/parity/harness/diffState.js#findDivergence` compares object KEY SETS — a measured `{ worn: undefined }` vs a declared `{}` is a "key set mismatch". JSON cannot spell `undefined`, so a field the prototype never carries (`worn`) is declared by OMITTING it from `before`; `chargenShiftDiffs` and `pickFields` therefore skip a field whose measured value is `undefined` on that side (the key-set comparison still catches a field declared on a side that lacks it, and a field present where the record omits it). The chargen-parity per-field loop already handles it (`diffState(undefined, undefined)` is `null`) and the well-formedness guard's `notDeepStrictEqual(undefined, {…})` passes.
- Existing record shapes: chargen `divergences[seed]` = `{ phase, requirements[], fields[], before{}, after{}, rationale }` (seeds 1/2/3/4/6/7/13/15/24/29/32 today — 11, the cap at `chargen-parity.test.js:184`; seed 2 already declares `items` for the w4n CLOAKS d7 move); scenario/script `chargenDivergence` (Phase 38 idiom, same shape; `flee`/`chest`/`affliction`/`movement` already declare `items`); the economy script's action-path `divergence` (`kind: "action-path"`, `fromAction: 0`, `fields: ["gold","weapon","items"]`, `phase: "24+39"` — the multi-phase string precedent). Every Thief-hero site already has a `chargenDivergence`; extend, do not duplicate.
- `combat-parity.test.js:310` asserts `lose-plain` never carries an ACTION-PATH record; a `chargenDivergence` on it is fine (Phase 38 added one).
- The cosmetic strips `stripCloakArmorTxt` / `stripReauthoredEveryField` / `stripBagArmorFields` map over `c.items` ONLY, so a worn cloak reaches the record assertion raw — a record's `after.worn.cloak` is the engine's item exactly as `newRun` built it (`txt` reworded, `every` re-authored, whatever the row carries), which is what the scan's `after.worn (engine @chargen)` JSON prints. Paste it verbatim; never "normalise" it to the prototype's row.
- Scratch paths: Git Bash exposes `$TEMP` (the Windows temp directory); if it is unset use `${TMPDIR:-/tmp}`. Scratch files are never committed.
- `src/browser/rail.js#wornReconcileCard(report)` returns `null` for `[]` (it filters on `bagged.length`), so `takeBootWornReport()` returning `[]` for an already-migrated save renders exactly what `null` rendered; consumed-once semantics (null after read) are unchanged.
- `tools/lib/class-matrix.mjs:451` writes `meta.runFlags: RUN_FLAGS` — a fresh readout omits the retired key automatically once `RUN_FLAGS` changes; `test/unit/class-matrix.test.js:299` compares against the imported `RUN_FLAGS` and follows.
- `docs/class-pass/v15-after.json` / `v15-after-depth20.json` are frozen history: `meta.runFlags` there has two keys (`storeRoll` and the retired one). `class-pass-ledger.test.js:437-438` pins that literal today and must be reworded, not "fixed".
- Tests known to pin the option-off/legacy behaviour (all must be re-pinned in Task 3): `worn-migration.test.js` (30 option sites + the header + L55-67/L130-149/L184-190 inversions), `engineAdapter.test.js` L83-85, L112-114, L161-168, L170-179, `bot-tactics.test.js` L787-788 + L810 name, `class-pass-ledger.test.js` L437-438, `item-activation.test.js` L373-377, `worn-model.test.js` L373-379, L434-441, L443-465, `worn-slots.test.js` L763-772, L780-803, L805-820, `save-validation.test.js` L148, L256 (hand-built old saves now gain `worn: {}`), `chargen-parity.test.js` shape assertion (`worn` joins the engine shape). Hand-built `legacyHero()`/no-worn states elsewhere (`worn-slots.test.js` L277-282, L297, `bot-tactics.test.js:568`, `combatMenu.test.js:325`) test engine tolerance of a bare object and stay green — leave them.
- Windows executor: Git Bash, no `jq`; write any multi-line helper script with the Write tool; `npm test` ≈ 25 s.
</phase_facts>

<flagged_assumptions>
- **A-3 (HEDGE-02 concurrency — n/a):** the load chain is synchronous, single-threaded, and pure over its own clone; there is no interruption or parallel-run guarantee to state beyond idempotency. Flagged, not authored: no concurrency truth exists for this requirement.
- **A-4 (declared consequence):** `takeBootWornReport()` returns `[]` (not `null`) for a valid save that was already migrated, because `validateSave` now has one return shape. Rendering is identical (`wornReconcileCard([])` is `null`); `engineAdapter.test.js` L161-168 is re-pinned to `[]` with that rationale. If Task 3 finds any consumer that branches on `null` vs `[]`, record it in the SUMMARY — none is known.
- **A-5:** the enumerated test list in phase_facts is the planner's grep-derived expectation; `npm test` after Task 1+2 is the authority. Every failing test must be one of two kinds — a pin of the option-off/legacy behaviour (re-pin to the single path, or delete with a reason if it pins a state `newRun` can no longer produce) or a parity site the scan named (declare it). Anything else is a bug in this plan's edits, not a pin to move. List every re-pinned/deleted test with its reason in the SUMMARY.
- **A-6:** the MOVED SET is expected to be the 13 Thief-hero sites (chargen 2/3/4; combat win/lose-plain/flee/parley; economy; encounters chest/tablefour/faerie/affliction; movement) with no mid-script wear anywhere; `tools/worn-fixture-scan-output.txt` decides. A site with a mid-script wear needs an action-path record (`kind: "action-path"`, `fromAction` = that index, end-state `items`/`worn`) instead of a chargen-time record — none expected.
</flagged_assumptions>

<artifacts_this_phase_produces>
- `test/parity/harness/comparables.js#dropEmptyWorn(c)` (exported; replaces the Phase 37 carve-out in the three shared chains and the three local mirrors); `chargenShiftDiffs`/`pickFields` undefined-omission rule.
- `test/parity/divergence-records.test.js` — tests `every declared divergence record across all fixtures is narrow and well-formed` and `HEDGE-03: the holders declaring worn are exactly the scan's MOVED SET`.
- Fixture record extensions on every MOVED SET holder: `fields` += `"items"` (where absent) and `"worn"`; `after.items` (chargen, or END for the economy action-path record) and `after.worn`; `before.items` (prototype); `requirements` += `"HEDGE-03"`; `phase` = `"<old>+45"`; rationale paragraph appended `Phase 45 (HEDGE-01/03, 2026-09-19): …`. `action-script.schema.md` documents the omission rule and the `worn` field.
- `test/unit/worn-migration.test.js` new test names: `HEDGE-01: newRun(2) with no options wears the Thief's starting cloak — c.worn.cloak set, c.items empty, every other field and rngState untouched`; `HEDGE-01: newRun(1) (Fighter) and newRun(7) (Magic User) with no options carry c.worn = {} and c.items = []`; `HEDGE-01: every chargen fixture seed creates c.worn with at most a cloak key, no cloak left in c.items, JSON-identical across two calls`; `HEDGE-02: a v1.4-era save (no c.worn, two rings + two cloaks + a potion stacked in the bag) loads through validateSave/rehydrate with NO option — both rings and the first cloak worn, the second cloak bagged, wornReport returned once`; `HEDGE-02 ordering: three identical Rings of Power — the first two in bag order wear jewelry1/jewelry2, the third stays bagged, reported once`; `HEDGE-02 idempotency: rehydrate twice deep-equals rehydrate once, and validateSave over its own output reports []`.
- `RUN_FLAGS = Object.freeze({ storeRoll: true })`.
- (Plan 01) `tools/worn-fixture-scan.mjs` + `tools/worn-fixture-scan-output.txt`. (Plan 03) FIXTURE-INVENTORY.md Phase 45 section; GEAR-SLOTS.md corrections; the smoke readout.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Collapse the engine, the adapter and the bot — newRun always reconciles, validateSave/rehydrate unconditional with one return shape, RUN_FLAGS = { storeRoll: true } (NO commit)</name>
  <files>engine/state.js, engine/saveState.js, engine/derived.js, src/browser/engineAdapter.js, tools/lib/tuning-bot.mjs</files>
  <read_first>
    - engine/state.js L118-172 (the `newRun` JSDoc incl. the option paragraph L131-144, the `@param` line L148, the signature L151, the gated call + its comment L164-170)
    - engine/saveState.js L255-265 (`sanitizeWorn` JSDoc sentence about "a no-option load" and "the option-gated reconcileWorn below"), L480-486 (`foldLegacyCounters` JSDoc sentence "so it sees an already-migrated c.worn when … was requested"), L535-562 (`validateSave` JSDoc option paragraph, `@param`, `@returns`, the `const wornSlots` local L560), L616-622 and L670-684 (the sanitizeWorn-before-reconcile comment and the gated return), L686-712 (`rehydrate` JSDoc + `@param`, signature L708), L725-735 (the rehydrate chain comment mentioning "option-gated"), L780-793 (the gated call + comment)
    - engine/derived.js L300-349 (`reconcileWorn` JSDoc — the "The ONLY callers are …" paragraph at L340-347 is the comment this phase rewrites)
    - src/browser/engineAdapter.js L90-131 (`bootWornReport` + `takeBootWornReport`), L318-350 (`startNewRun` JSDoc paragraph L329-335 and the `initRun` call L348), L352-395 (`boot` JSDoc L362-370, the `validateSave`/`rehydrate` calls L381-384, the throwaway-run comment L392-393)
    - tools/lib/tuning-bot.mjs L560-575 (`RUN_FLAGS` JSDoc + declaration), L1349 (the spread into `newRun`)
    - src/browser/rail.js L455-478 (`wornReconcileCard` — confirms `[]` renders nothing; read only)
  </read_first>
  <action>
    engine/state.js — `newRun(seed, exclude = [], { startDepth = 1, force = null, storeRoll = false } = {})`: remove the fourth option from the destructure and from the `@param` type/description; replace `if (…) reconcileWorn(c);` with an unconditional `reconcileWorn(c);` at the same position (after `grantLevelAbilities`, before `startAt`/`genFloor`). Delete the JSDoc paragraph that introduced the option (L131-144) and the comment block above the call (L164-169); in their place write ONE short paragraph: Phase 45 (HEDGE-01) — `reconcileWorn(c)` runs on every fresh roll: creates `c.worn` (`{}` for a Fighter/Magic User; a Thief's starting cloak worn), plain reassignment with zero rng draws, so `rngState` and the floor are byte-identical to the pre-Phase-45 roll (`test/unit/chargen-rng-pin.test.js`, unedited); the parity fixtures this moves are declared per site (`test/parity/FIXTURE-INVENTORY.md`, Phase 45 section) and the harness keeps a populated `c.worn` in the comparable via `dropEmptyWorn`. Keep the `storeRoll` prose exactly as it is (that option is out of scope — CONTEXT "Deferred Ideas").

    engine/saveState.js — `validateSave(raw, options = {})`: delete the `const wornSlots = …` local and the option paragraph in the JSDoc; `@param {{ freshSeed?: number }} [options]`; `@returns {{ ok: true, value: object, wornReport: Array } | { ok: false, reason: string }}`. Replace the gated tail with `const wornReport = reconcileWorn(value.c) ?? [];` and `return { ok: true, value, wornReport };` (the `?? []` is the null-to-empty coalesce: an already-migrated save reports an empty reconciliation — one shape, never a conditional key). Rewrite the comment above it (L670-682) to say exactly that: unconditional since Phase 45 (HEDGE-02); `reconcileWorn` is a no-op returning null on a save that already carries `worn`, coalesced to `[]`; `wornReport` is a return value, never a serialized field. `rehydrate(obj)`: drop the `options` parameter and its `@param`; delete the option paragraph in its JSDoc (L693-707) and replace with one sentence (reconciles unconditionally since Phase 45 — a no-op after `validateSave`'s own call, one-shot for a direct caller; the report is discarded, `boot()` reads `validateSave`'s); make the call at L791 unconditional (`reconcileWorn(state.c);`) with its comment reduced to that sentence. Fix the three stray sentences that describe the gate: `sanitizeWorn`'s JSDoc (L260-262 — it now reads: when the key is absent this does nothing; creation is `reconcileWorn`'s job, which both load chains run unconditionally), `foldLegacyCounters`'s (L484 — "so it runs before the unconditional `reconcileWorn`" — keep the ORDER of calls exactly as today; only the sentence changes), the chain comments at L620 and L731 ("before the option-gated reconcileWorn" → "before reconcileWorn"). Do not reorder any call in either chain.

    engine/derived.js — in `reconcileWorn`'s JSDoc replace the paragraph L340-347 ("The ONLY callers are Plan 03's … option … and the option-gated save-load path … never creates `c.worn`") with: Callers — `engine/state.js#newRun` on every fresh roll and both load chains (`validateSave`/`rehydrate`), all unconditional since Phase 45 (HEDGE-01/02); returns `null` (touching nothing) when `c` already owns `worn`, which is what makes a second load a no-op. Nothing else in derived.js changes.

    src/browser/engineAdapter.js — `startNewRun`: `initRun(safeSeed, exclude, { startDepth, storeRoll: true })`; delete the JDoc paragraph L329-335 (the option "following the exact same rule as storeRoll") — the storeRoll paragraph above it stays. `boot`: `const check = validateSave(raw, { freshSeed });`, `bootWornReport = check.wornReport;` (the `?? null` is dead — the shape is fixed), `currentState = rehydrate(check.value);`; rewrite the JSDoc paragraph L362-370 to: this IS the shell's real load path; every valid save is reconciled by `validateSave` (unconditional since Phase 45) and its `wornReport` — `[]` when nothing moved, including an already-migrated save — is stashed for `takeBootWornReport()`; the rail card filters on `bagged.length`, so an empty report shows nothing; the fresh-run fallback below leaves it `null`. Delete the sentence at L392-393 ("This throwaway pre-title run deliberately does NOT pass … (mirrors storeRoll) — it never has a save to migrate."); the `readRecentNames` sentence above it stays. `takeBootWornReport()` and its JSDoc: unchanged.

    tools/lib/tuning-bot.mjs — `export const RUN_FLAGS = Object.freeze({ storeRoll: true });` and rewrite the JSDoc L564-573: the bot plays the shipped run rules; `storeRoll` enables the depth-rolled store stock; the worn-slot model needs no flag since Phase 45 (HEDGE-01 — `newRun` always creates `c.worn`, so a Thief starts with its cloak worn); `meta.runFlags` records this object; the v1.5 AFTER readouts (`docs/class-pass/v15-after*.json`) recorded the two-flag era and are frozen history — never regenerate them to match. The spread at L1349 is unchanged.

    After the edits: `grep -rn "wornSlots" engine/ src/ tools/` must print nothing; `node --test test/unit/chargen-rng-pin.test.js` must pass (proof of zero added draws); `npm run build:www` must exit 0. `npm test` WILL be red now (pins + parity) — that is expected; do NOT commit.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test "$(grep -rn "wornSlots" engine/ src/ tools/ mazeworld.html | wc -l)" = "0" && grep -c "^  reconcileWorn(c);" engine/state.js && grep -c "reconcileWorn(value.c) ?? \[\]" engine/saveState.js && grep -c "return { ok: true, value, wornReport };" engine/saveState.js && grep -c "export function rehydrate(obj) {" engine/saveState.js && grep -c "Object.freeze({ storeRoll: true })" tools/lib/tuning-bot.mjs && node --test test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# fail 0" && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "wornSlots" engine/ src/ tools/ mazeworld.html | wc -l` prints `0`.
    - `grep -cE "^export function newRun\(seed, exclude = \[\], \{ startDepth = 1, force = null, storeRoll = false \} = \{\}\) \{" engine/state.js` prints `1`; `grep -c "^  reconcileWorn(c);" engine/state.js` prints `1`; `grep -c "if (wornSlots)" engine/state.js` prints `0`.
    - `grep -c "reconcileWorn(value.c) ?? \[\]" engine/saveState.js` prints `1`; `grep -c "return { ok: true, value, wornReport };" engine/saveState.js` prints `1`; `grep -c "return wornReport ?" engine/saveState.js` prints `0`; `grep -c "^export function rehydrate(obj) {" engine/saveState.js` prints `1`; `grep -c "^  reconcileWorn(state.c);" engine/saveState.js` prints `1`; `grep -c "option-gated" engine/saveState.js engine/state.js engine/derived.js` prints `0` for each file.
    - `git diff --stat -- engine/` lists exactly `engine/state.js`, `engine/saveState.js`, `engine/derived.js`; `git diff -- engine/derived.js | grep -cE "^[-+][^-+]" ` counts only comment lines (every changed line begins with ` *` or `/**` after the diff marker: `git diff -- engine/derived.js | grep -E "^[-+][^-+]" | grep -vcE "^[-+]\s*(\*|/\*\*)"` prints `0`).
    - `grep -c "initRun(safeSeed, exclude, { startDepth, storeRoll: true })" src/browser/engineAdapter.js` prints `1`; `grep -c "validateSave(raw, { freshSeed })" src/browser/engineAdapter.js` prints `1`; `grep -c "rehydrate(check.value)" src/browser/engineAdapter.js` prints `1`; `grep -c "bootWornReport = check.wornReport;" src/browser/engineAdapter.js` prints `1`.
    - `grep -c "export const RUN_FLAGS = Object.freeze({ storeRoll: true });" tools/lib/tuning-bot.mjs` prints `1`.
    - `node --test test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0` and `git diff --stat -- test/unit/chargen-rng-pin.test.js` prints nothing.
    - `npm run build:www` exits 0. `git log -1 --format=%s` still shows Plan 01's commit (nothing committed in this task).
  </acceptance_criteria>
  <done>Engine, adapter and bot are single-path; the option identifier is gone from engine/src/tools; the rng-cursor pin is untouched and green; nothing committed.</done>
</task>

<task type="auto">
  <name>Task 2: Harness — dropEmptyWorn replaces the Phase 37 carve-out, undefined-omission rule, worn in the chargen shape; declare exactly the MOVED SET with before/after records; add divergence-records.test.js (NO commit)</name>
  <files>test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, test/parity/chargen-parity.test.js, test/parity/full-suite.test.js, test/parity/fixtures/action-script.chargen.json, test/parity/fixtures/action-script.combat.json, test/parity/fixtures/action-script.economy.json, test/parity/fixtures/action-script.encounters.json, test/parity/fixtures/action-script.movement.json, test/parity/fixtures/action-script.schema.md, test/parity/divergence-records.test.js</files>
  <read_first>
    - tools/worn-fixture-scan-output.txt (whole file — the MOVED SET line is the declared set; the `## Record values` blocks are the paste source for `before.items`/`after.items`/`after.worn`, and `end.*` for the economy action-path record)
    - test/parity/harness/comparables.js L200-275 (`stripTimersField`, the Phase 37 strip at L206-221 to delete, `stripAbilitiesField`/`stripPhobiaFields`/`stripSpellSeen` JSDocs that cite the deleted name), L520-526 / L604-610 / L1113-1119 (the three comparable chains), L776-805 (`stripScenarioDivergence`), L829-880 (`pickFields`, `declaredEndDiffs`), L996-1077 (`chargenDivergenceFor`, `chargenShiftOf`, `stripChargenShift`, `chargenShiftDiffs`, `stripDeclaredFields`)
    - test/parity/combat-parity.test.js L150-180, test/parity/magic-parity.test.js L110-135, test/parity/movement-parity.test.js L90-112 (the three local comparables: the comment block and the destructure line that strip `worn`), and the harness imports at the top of each
    - test/parity/chargen-parity.test.js L1-100 (`CHARACTER_FIELDS`, its engine-only tail `darkFor`/`bag`/`abilities`), L100-176 (the loop: shape assertion, engine-side destructure, the record loop), L178-203 (the well-formedness guard with the `<= 11` cap)
    - test/parity/full-suite.test.js L35-99 (imports, `CHARACTER_FIELDS` mirror), L103-149 (the chargen sub-test mirror)
    - test/parity/fixtures/action-script.chargen.json (`divergences` map — seeds 2/3/4 records verbatim), action-script.combat.json (`scenarios[]` win/lose-plain/flee/parley `chargenDivergence`), action-script.economy.json (top-level `divergence` action-path record and `chargenDivergence`), action-script.encounters.json (chest/tablefour/faerie/affliction `chargenDivergence`), action-script.movement.json (top-level `chargenDivergence`), action-script.schema.md L28-135 (record documentation)
    - test/parity/FIXTURE-INVENTORY.md L725-800 (the Phase 38 declared-record precedent) and L399-437 (the economy action-path record's history)
    - test/parity/fixture-inventory.test.js L1-32 (fixture-loading idiom to copy for the new test)
  </read_first>
  <action>
    comparables.js — delete the Phase 37 strip function and its JSDoc (L206-221). Add, in the same place, `export function dropEmptyWorn(c)`: returns `c` unchanged when `c` is falsy or has no own `worn` key; when `c.worn` is a plain object with zero own keys returns a shallow copy without `worn`; otherwise (a populated map, or a malformed value) returns `c` unchanged so it reaches the diff. JSDoc: Phase 45 (HEDGE-01/03) — `c.worn` exists on EVERY engine character now; the frozen prototype has no worn model, so an EMPTY map is the engine's spelling of the prototype's only state and is dropped, while a POPULATED map is a real, measured divergence that the holder must declare (`divergences[seed]` / `chargenDivergence` / action-path `fields`, with `items` and `worn` before/after) — measured by `tools/worn-fixture-scan.mjs`, never blanket-stripped; contrast `stripTimersField` (a structural tripwire on a field no fixture populates). Replace the deleted name with `dropEmptyWorn(` in the three chains (movement/combat/economy comparables) at the same position in the wrap order. Rewrite the sentences in `stripAbilitiesField`/`stripPhobiaFields`/`stripSpellSeen`'s JSDocs that cite the deleted name ("exactly like …" → "exactly like stripTimersField"; "wired … directly around …" → "directly around dropEmptyWorn"). `chargenShiftDiffs`: assign `before[f]`/`after[f]` only when the measured value is not `undefined`; `pickFields`: same skip; add one JSDoc sentence to each: a field one side never carries (the prototype has no `worn`) is declared by omitting it from that side's map — JSON has no `undefined` — and the key-set comparison in `diffState` still catches a field declared on a side that lacks it or present where the record omits it.

    The three local mirrors (combat/magic/movement parity tests): remove `worn` from the destructure list, import `dropEmptyWorn` from the harness, and wrap the destructured rest with it before the existing outer wrappers (e.g. `rest.c = stripReauthoredEveryField(stripCloakArmorTxt(dropEmptyWorn(cRest)))`); replace the Phase 37 comment lines ("strip the new engine-only lazily-created c.worn slot map too (see harness …) — mirrored here") with the Phase 45 sentence (drop an empty worn map; a populated one is declared on the scenario's `chargenDivergence`). Every site that mirrors the harness changes together (the Phase 21-03 lesson).

    chargen-parity.test.js and full-suite.test.js (chargen sub-test): add `"worn"` to `CHARACTER_FIELDS` with a Phase 45 comment (engine-only worn map on every character; a populated one — a Thief's cloak — is declared per seed in `divergences`; an empty one is dropped engine-side); apply `dropEmptyWorn` to the engine side after the existing `stripCloakArmorTxt` step (import it from the harness); leave the per-field record loop as is (`diffState(undefined, undefined)` is `null` for `before.worn`). In the well-formedness guard, keep the cap equal to the number of `divergences` keys after your edits (11 if the MOVED SET names only seeds 2/3/4; raise it by the number of NEW chargen records otherwise) and add "Phase 45 (HEDGE-03) extends seeds 2/3/4 (+ any new ones) with `items`/`worn`" to the comment.

    Fixture records — for EVERY site on the MOVED SET line, and only those: locate its holder (chargen seed → `divergences[String(seed)]`; a combat/magic/encounters scenario → that scenario's `chargenDivergence`; the movement/economy script → the top-level `chargenDivergence`), then extend it: `fields` gains `"items"` if absent and `"worn"`; `before.items` = the scan's `before.items (prototype @chargen)` JSON (must equal what is already there where `items` was declared); `after.items` = the scan's `after.items (engine @chargen)`; `after.worn` = the scan's `after.worn (engine @chargen)`; NO `worn` key in `before` (the prototype never carries it — the omission rule); `requirements` gains `"HEDGE-03"`; `phase` becomes the string `"<existing>+45"` (e.g. `"38+45"`; economy's action-path record `"24+39+45"`); append to `rationale` a paragraph beginning `Phase 45 (HEDGE-01/03, 2026-09-19):` stating that `newRun` now wears the Thief's starting cloak at chargen (`c.worn.cloak`), so `c.items` loses it and `c.worn` is populated; zero rng draws; measured by `tools/worn-fixture-scan.mjs` (BEFORE readout committed in 45-01). A holder with no existing record (only if the scan names one) gets a fresh record in the same shape with `phase: 45`. The economy script ALSO extends its action-path `divergence` record: `fields` gains `"worn"` (it already has `items`), `after.items` = the scan's `end.after.items (engine @end)`, `after.worn` = `end.after.worn (engine @end)`, `before.items` unchanged (equals `end.before.items`), same `requirements`/`phase`/`rationale` treatment. A site whose scan row shows a mid-script wear (none expected — A-6) gets an action-path record from that index instead; say so in the SUMMARY. Do not touch any `seed`/`seeds`/`actions`/`scenarios` array or any other fixture byte. Keep the JSON formatting of each file (2-space indent, existing key order) so the diff is only the records.

    action-script.schema.md — in the `divergences`/`chargenDivergence` documentation add the omission rule (a field one side never carries is declared by omitting it from that side's map; `diffState`'s key-set comparison keeps that honest) and note `worn` as a declarable field since Phase 45 with `items`.

    New `test/parity/divergence-records.test.js` (node:test, loads the six fixture JSONs with the `fixture-inventory.test.js` idiom): test 1 `every declared divergence record across all fixtures is narrow and well-formed` — collect every record (`divergences[*]`; each scenario's `divergence` and `chargenDivergence`; each script's top-level `divergence` and `chargenDivergence`) with a human id (`<file>#<seed|scenario|script>:<divergences|divergence|chargenDivergence>`); assert `phase` truthy, `requirements` a non-empty array, `fields` a non-empty array, `before`/`after` plain objects whose every key is in `fields`, `rationale` a non-empty string, and for every field `assert.notDeepStrictEqual(before[f], after[f])` with the "no real before/after difference — this looks like a blanket regeneration" message; for `kind: "action-path"` assert integer `fromAction`. Test 2 `HEDGE-03: the holders declaring worn are exactly the scan's MOVED SET` — read `tools/worn-fixture-scan-output.txt`, parse the single `^MOVED SET \((\d+)\): (.*)$` line into a Set of ids (assert the count matches), build the Set of holder ids that have ANY record whose `fields` includes `"worn"` using the same id vocabulary (`action-script.chargen.json#seed-N`, `<file>#<scenario.name>`, `<file>#script`), and `assert.deepStrictEqual([...declared].sort(), [...moved].sort())`; also assert the set is non-empty. Run `node --test test/parity/` — every parity file must now pass (if any parity test still reports a divergence at a path other than the declared fields on a MOVED SET site, that path is a further measured field of the same site: add it to the record with its measured before/after and note it in the SUMMARY; if a site NOT on the MOVED SET diverges, the scan is wrong — stop and fix the scan, never the harness). Do NOT commit.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test "$(grep -c "stripWornField" test/parity/harness/comparables.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js | grep -vc ":0$")" = "0" && grep -c "^export function dropEmptyWorn(" test/parity/harness/comparables.js && node --test test/parity/ 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stripWornField" test/parity/harness/comparables.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js test/parity/chargen-parity.test.js test/parity/full-suite.test.js` prints `0` for every file; `grep -c "^export function dropEmptyWorn(" test/parity/harness/comparables.js` prints `1`; `grep -c "dropEmptyWorn(" test/parity/harness/comparables.js` ≥ 4 (definition + three chains); `grep -c "dropEmptyWorn(" test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js test/parity/chargen-parity.test.js test/parity/full-suite.test.js` ≥ 1 for each file.
    - `grep -c '"worn",' test/parity/chargen-parity.test.js` ≥ 1 and `grep -c '"worn",' test/parity/full-suite.test.js` ≥ 1 (the engine shape lists it).
    - `node --test test/parity/divergence-records.test.js 2>&1 | grep -E "^# (pass|fail) [0-9]+"` prints `# pass 2` and `# fail 0`.
    - `node --test test/parity/ 2>&1 | grep -E "^# fail 0"` prints `# fail 0` (chargen, combat, magic, movement, economy, encounters, full-suite, fixture-inventory, pending-fight-audit, smoke, divergence-records all green).
    - `git status --porcelain test/parity/fixtures` lists `action-script.schema.md` plus exactly the fixture files that own a MOVED SET site and no other fixture file; `git diff -- test/parity/fixtures/*.json | grep -cE '^[-+]\s*"(seed|seeds|actions|scenarios|name)":'` prints `0` (no seed/action/scenario byte moved).
    - For every MOVED SET id, a node check script (written with the Write tool to a scratch path, run with `node`) prints `OK <id>` after asserting its holder's record has `"worn"` in `fields`, an `after.worn` object with ≥ 1 key, no `worn` key in `before`, `"HEDGE-03"` in `requirements`, and a `phase` string ending in `+45` (or the number 45 for a new record); the script prints `OK` lines = N and no `FAIL` line.
    - `node tools/worn-fixture-scan.mjs > "$TEMP/wfs-after.txt" && diff "$TEMP/wfs-after.txt" tools/worn-fixture-scan-output.txt` prints nothing (the AFTER run is byte-identical to the committed BEFORE readout).
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`. `git log -1 --format=%s` still shows Plan 01's commit.
  </acceptance_criteria>
  <done>The harness keeps a populated worn in the comparable and drops an empty one; exactly the measured set is declared with before≠after records; the records test pins declared == measured; the whole parity directory is green; nothing committed.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin every option-off/legacy test to the single path, add the HEDGE-01/HEDGE-02 success-criterion pins, run the closing gates, and make the plan's ONE commit</name>
  <files>test/unit/worn-migration.test.js, test/unit/engineAdapter.test.js, test/unit/bot-tactics.test.js, test/unit/class-pass-ledger.test.js, test/unit/item-activation.test.js, test/unit/worn-model.test.js, test/unit/worn-slots.test.js, test/unit/save-validation.test.js</files>
  <read_first>
    - test/unit/worn-migration.test.js (whole file, 342 lines — every option site, the header, the `illegalOldSave()` fixture at L69-79, the inverted tests at L36-47, L55-62, L64-67, L130-140, L142-149, L156-162, L184-190, the idempotence pair L194-208, the report-shape pin L340-345)
    - test/unit/engineAdapter.test.js L76-116 (boot contracts: L83-85, L112-114), L118-180 (the Phase 37 boot tests: L161-168 already-migrated, L170-179 startNewRun vs initRun)
    - test/unit/bot-tactics.test.js L783-830 (`RUN_FLAGS` pin L787-788, the test name at L810)
    - test/unit/class-pass-ledger.test.js L425-440 (the stored-file `runFlags` literal pins L437-438)
    - test/unit/item-activation.test.js L370-380
    - test/unit/worn-model.test.js L365-380, L430-466
    - test/unit/worn-slots.test.js L755-822 (the three `Task 3 sweep` tests built on `newRun(3)`), L285-300 and L365-380 (the `hero({ c: { worn: {} } })` pins that show the worn-model outcome of `takeFind`/`takeLoot` — the expected events/keys to mirror)
    - test/unit/save-validation.test.js L20-32 (the `newRun` round-trips — unaffected), L135-150 and L245-258 (the two hand-built old-save `deepStrictEqual(state.c, { ...oldSave.c, bag, abilities })` pins)
    - docs/class-pass/v15-after.json `meta.runFlags` (read via a node one-liner printing the JSON — it has two keys; frozen)
  </read_first>
  <action>
    Run `npm test` first and capture the failing test list — it is the authority (A-5). Then re-pin, file by file:

    worn-migration.test.js — rewrite the header comment for Phase 45: direct unit coverage of the single worn path — `newRun` (every fresh roll creates `c.worn`, zero rng draws) and the unconditional load reconcile (`validateSave` always returns `wornReport`, `rehydrate` reconciles, both idempotent), plus `sanitizeWorn` tolerance. Delete every option argument from every `newRun`/`validateSave`/`rehydrate` call (the calls become the default path). Replace L36-47 with the HEDGE-01 pin `HEDGE-01: newRun(2) with no options wears the Thief's starting cloak — c.worn.cloak set, c.items empty, every other field and rngState untouched`: `const s = newRun(2)`; assert `s.c.cls === "Thief"`, `s.c.worn.cloak.kind === "cloak"`, `deepStrictEqual(s.c.items, [])`, `Object.keys(s.c.worn)` deep-equals `["cloak"]`, and `s.c.worn.cloak.n` equals the `n` of the cloak row the seed rolls (read it off `s.c.worn.cloak` itself — a Thief's one starting cloak); add a comment that the zero-draw proof for `rngState`/floor is `test/unit/chargen-rng-pin.test.js` (unedited by this phase) — do NOT restate or edit that pin. Replace L49-53 with `HEDGE-01: newRun(1) (Fighter) and newRun(7) (Magic User) with no options carry c.worn = {} and c.items = []`. Replace L55-62 with `HEDGE-01: every chargen fixture seed creates c.worn with at most a cloak key, no cloak left in c.items, JSON-identical across two calls` (loop `CHARGEN_FIXTURE_SEEDS`: `"worn" in c`, `Object.keys(c.worn).every((k) => k === "cloak")`, `c.items.some((it) => it.kind === "cloak") === false`, two calls `JSON.stringify`-equal). Delete L64-67 (the storeRoll-alone test — its premise is gone). Rewrite the Pitfall 8 trio (L81-113) as the HEDGE-02 pin `HEDGE-02: a v1.4-era save (no c.worn, two rings + two cloaks + a potion stacked in the bag) loads through validateSave/rehydrate with NO option — both rings and the first cloak worn, the second cloak bagged, wornReport returned once` (same `illegalOldSave()` fixture and the same expected `worn`/`items`/`wornReport` values as today; then `rehydrate(check.value)` deep-equals in `worn`/`items`; then `validateSave(JSON.stringify(serializeRun(rehydrate(check.value))))` reports `deepStrictEqual(wornReport, [])` — "returned once"), keep the direct-`rehydrate` case option-free. Add `HEDGE-02 ordering: three identical Rings of Power — the first two in bag order wear jewelry1/jewelry2, the third stays bagged, reported once`: a `newRun(1)` Fighter with `c.items = [ring("first"), ring("second"), ring("third")]` where `ring(tag)` is `ringOfPower()` with `txt` set to the tag; `validateSave` with no option → `worn.jewelry1.txt === "first"`, `worn.jewelry2.txt === "second"`, `items` deep-equals `[ring("third")]`, `wornReport` deep-equals `[{ slot: "jewelry", worn: ["Ring of Power", "Ring of Power"], bagged: ["Ring of Power"] }]`. Invert L130-140 to `a save that already carries worn loads byte-identical in worn/items and reports [] (one shape)`. Invert L142-149 to `the unconditional reconcile creates c.worn on a legacy save: validateSave and rehydrate both yield worn = {} for a Fighter with no slot items`. Invert L184-190 to the same truth from the sanitizeWorn side (`no worn key at all → {} after load, created by reconcileWorn, not by sanitizeWorn`). Rename L194 to `HEDGE-02 idempotency: rehydrate twice deep-equals rehydrate once, and validateSave over its own output reports []` and add that second assertion. Everything else: option-free, names unchanged; fix the L231-235 comment ("WITH and WITHOUT the … option" → "on every load").

    engineAdapter.test.js — L83-85: a fresh boot fallback run now carries `c.worn` (assert `"worn" in state.c` and `takeBootWornReport() === null` — no save was loaded); L112-114 likewise; L161-168: `newRun(5)` (no option) and `deepStrictEqual(takeBootWornReport(), [])` with the message "one report shape — an already-migrated save reports an empty reconciliation; wornReconcileCard([]) renders nothing" (A-4); L170-179: rename to `startNewRun() and initRun(seed) both create c.worn on the fresh roll (single path)` and assert `"worn" in getState().c` after `initRun(4302)` too.

    bot-tactics.test.js — L787-788: name `RUN_FLAGS is { storeRoll: true }; playRun's state carries storeRoll and c.worn, and a Thief starts with its cloak worn`, `deepStrictEqual(RUN_FLAGS, { storeRoll: true })`, the rest unchanged; L810: rename to `a bot hero with two worn jewels (playRun's single-path shape) taking a third find stows it — findTaken only, no itemEquipped`.

    class-pass-ledger.test.js — L437-438: replace the two literal `deepStrictEqual` pins with assertions that stay true of the FROZEN files without spelling the retired identifier: `assert.equal(v15After.meta.runFlags.storeRoll, true)`, `assert.equal(v15AfterDeep.meta.runFlags.storeRoll, true)`, `assert.deepStrictEqual(v15After.meta.runFlags, v15AfterDeep.meta.runFlags)`, and `assert.equal(Object.keys(v15After.meta.runFlags).length, 2, "the v1.5 AFTER readouts recorded the two run flags of their era — storeRoll plus the Phase 37 worn-slot option Phase 45 collapsed into the engine's only path; frozen history, never regenerated to today's RUN_FLAGS")`. Also update the test's name (`runFlags present` → `runFlags recorded (two-flag era)`).

    item-activation.test.js — L373-377: drop the option, rename to `validateSave folds legacy counters through the real load chain with the tampered-charges clamp applied`.

    worn-model.test.js — L373-379: replace the `"worn" in c === false` line with `assert.equal(typeof c.worn, "object")`; L434-441: rename to `chargen shape: newRun(seed).c always carries worn, and two fresh newRun calls in the same process are byte-identical` and assert `"worn" in a.c` true; L443-465: rename to `rng invariance: 20 legal moves from newRun(3) never add a worn key beyond chargen's and produce a deterministic rngState/event sequence`, track `Object.keys(state.c.worn).length` and assert it equals `Object.keys(newRun(3).c.worn).length` (1 — the cloak) at the end of both drives.

    worn-slots.test.js — delete L763-772 (`equipItem on the Thief's starting cloak is unaffected by Plan 02`) and L805-820 (`useItem of the bagged starting cloak on a legacy state …`) — both pin a fresh-run state (`newRun(3)` with a bagged cloak and no `worn`) that `newRun` can no longer produce; the worn-cloak use path is pinned by this file's `useItem` worn-slot tests. Rewrite L780-803 (takeFind/takeLoot "on a legacy state") to the single-path outcome on `newRun(3)`: the ring lands in `c.worn.jewelry1`, `c.items.length` is unchanged, and the events match the `hero({ c: { worn: {} } })` pins at L365-380 (read them for the exact event list — mirror, do not guess); rename both without the "legacy state" wording.

    save-validation.test.js — L148 and L256: add `worn: {}` to the expected `c` literal with the comment `Phase 45 (HEDGE-02): an old save with no c.worn gains an empty worn map from the unconditional reconcile`.

    Any OTHER failure `npm test` reports: classify per A-5, fix, and list it in the SUMMARY's `## Tests re-pinned or deleted` table with file, line, kind (re-pinned / deleted), reason.

    Closing gates for this commit (all must pass; paste outputs into the SUMMARY): `npm test` → `# fail 0` (record the final pass count and reconcile it against the deletions/additions: −2 `worn-slots` sweeps, −1 storeRoll-alone, +1 three-ring ordering, +2 `divergence-records`, ± anything else); `npm run build:www` → exit 0; `grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/ | wc -l` → `0`; `node tools/shell-sweep.mjs refs wornSlots` → 0; `node tools/worn-fixture-scan.mjs > "$TEMP/wfs-after.txt" && diff "$TEMP/wfs-after.txt" tools/worn-fixture-scan-output.txt` → empty; `git hash-object test/parity/prototype-master.js.txt` → `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` → only the MOVED SET holders' files + `action-script.schema.md`; `git diff --stat -- engine/ content/` → exactly `engine/state.js`, `engine/saveState.js`, `engine/derived.js`; `git diff --stat -- test/unit/chargen-rng-pin.test.js docs/class-pass/` → empty. Then the ONE commit of this plan with every file from Tasks 1-3: `refactor(45-02): collapse the Phase 37 hedges — newRun always wears, load always reconciles, no run/load option; measured fixtures declared, tests re-pinned to the single path (HEDGE-01/02/03)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && npm test 2>&1 | grep -E "^# fail 0" && npm run build:www && test "$(grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/ | wc -l)" = "0" && node tools/worn-fixture-scan.mjs > "$TEMP/wfs-after.txt" && diff "$TEMP/wfs-after.txt" tools/worn-fixture-scan-output.txt && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && node --test test/unit/worn-migration.test.js test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `npm run build:www` exits 0.
    - `grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/ | wc -l` prints `0` (ROADMAP criterion 1, first half).
    - `node --test test/unit/worn-migration.test.js 2>&1 | grep -c "HEDGE-01: newRun(2) with no options wears the Thief's starting cloak"` prints `1` and the file reports `# fail 0` (criterion 1, second half); `grep -c "HEDGE-02: a v1.4-era save" test/unit/worn-migration.test.js` prints `1` and `grep -c "HEDGE-02 ordering: three identical Rings of Power" test/unit/worn-migration.test.js` prints `1` (criterion 2); `grep -cE "\{ ?wornSlots" test/unit/*.test.js` prints `0` for every file.
    - `git diff --stat -- test/unit/chargen-rng-pin.test.js docs/class-pass/ docs/CLASS-PASS.md` prints nothing; `node --test test/unit/chargen-rng-pin.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `grep -c "deepStrictEqual(RUN_FLAGS, { storeRoll: true })" test/unit/bot-tactics.test.js` prints `1`; `grep -c "Object.keys(v15After.meta.runFlags).length, 2" test/unit/class-pass-ledger.test.js` prints `1`; `node --test test/unit/class-pass-ledger.test.js test/unit/class-matrix.test.js test/unit/bot-tactics.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `diff "$TEMP/wfs-after.txt" tools/worn-fixture-scan-output.txt` prints nothing (HEDGE-03 ordering: BEFORE == AFTER).
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git diff --stat HEAD -- engine/ content/` (before the commit) lists exactly `engine/state.js`, `engine/saveState.js`, `engine/derived.js`.
    - After the commit: `git log -1 --format=%s` starts with `refactor(45-02):`; `git log --oneline` shows exactly one commit after Plan 01's; `git status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>Every test pins the single path; the two ROADMAP success-criterion pins exist and pass with no option; the AFTER scan equals the BEFORE readout; one green commit lands the whole collapse.</done>
</task>

</tasks>

<verification>
- Exactly one commit for this plan; `npm test` fail 0 and `build:www` green at it (ROADMAP criterion 5).
- Criteria 1, 2 and 3 hold at the commit: identifier grep 0 + the no-option pins; the declared set equals the scan's MOVED SET (enforced by `divergence-records.test.js`), every other fixture byte-identical, master hash unchanged; criterion 4's first half (`RUN_FLAGS`) holds — the smoke is Plan 03's.
</verification>

<success_criteria>
- `newRun(2)` no-option → cloak worn; `validateSave`/`rehydrate` no-option → v1.4 save reconciled, report once; idempotent.
- Harness: `dropEmptyWorn` + declared `items`/`worn` records on exactly the measured set; `divergence-records.test.js` green.
- No `wornSlots` in engine/src/shell/tools/test; `chargen-rng-pin.test.js` untouched and green; scan BEFORE == AFTER.
</success_criteria>

<output>
Create `.planning/phases/45-collapse-the-phase-37-hedges/45-02-SUMMARY.md` with: `## Engine diff` (the three files, what changed, the one return shape); `## Moved fixtures` — a table per MOVED SET site: holder, record kind, fields added, `before.items` → `after.items`, `after.worn` (names), and the `Phase 45 (HEDGE-01/03, 2026-09-19):` rationale sentence; `## Scan AFTER` (the `diff` result — empty — and the `MOVED SET (N):` line); `## Tests re-pinned or deleted` (file, line, re-pinned/deleted, reason — every one, including any A-5 extras); `## Declared consequences` (A-4: `takeBootWornReport()` returns `[]` for an already-migrated save; the ledger test's stored-file assertion reworded — do NOT regenerate `docs/class-pass/*.json`); `## Gate outputs` (every command above with its output); `## Human verification (deferred to end of run)` — a fresh Thief starts with the cloak in the WORN row of the Gear tab; resuming the current on-device save shows the reconciliation card once (or no card if already migrated); nothing else is user-visible; `## Flagged assumptions` (A-3..A-6 final status).
</output>