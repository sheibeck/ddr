---
phase: 45-collapse-the-phase-37-hedges
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/worn-fixture-scan.mjs
  - tools/worn-fixture-scan-output.txt
autonomous: true
requirements: [HEDGE-03]
tags: [parity, fixtures, worn-model, measure-first, scan]

must_haves:
  truths:
    - "HEDGE-03 (measure first): `tools/worn-fixture-scan.mjs` replays every parity replay site (14 chargen seeds, 6 combat scenarios, 4 magic scenarios, the movement script, the economy script, 5 encounters scenarios) in lockstep with the frozen prototype sandbox, with the engine's start character passed through `reconcileWorn` explicitly, and prints — per site — the hero, whether `c.worn` is populated at chargen, the first action index at which a worn key appears mid-script (or `never`), and the prototype/engine `items` and engine `worn` at chargen and at end"
    - "HEDGE-03 [ordering]: the scan is deterministic — two consecutive runs produce byte-identical stdout, and the committed `tools/worn-fixture-scan-output.txt` equals a fresh run (`diff` prints nothing)"
    - "HEDGE-03 [empty]: the committed output's `MOVED SET (N):` line lists N ≥ 5 site ids and N equals the number of ids on the line; the five GEAR-SLOTS §2 predictions (`action-script.chargen.json#seed-2`, `#seed-3`, `#seed-4`, `action-script.combat.json#flee`, `action-script.economy.json#script`) each appear exactly once; the output contains no `UNEXPLAINED` row (every site whose prototype/engine `items` differ has a populated `worn` explaining it)"
    - "The scan is invariant across the collapse by construction: its ONLY simulation of Phase 45 is the explicit `reconcileWorn(engineState.c)` call right after `newRun(seed)` (a no-op once `newRun` creates `c.worn` itself), and it never uses the harness's worn strip — so Plan 02 can re-run it after the engine edit and require byte-identical output"
    - "This plan changes zero bytes under `engine/`, `src/`, `content/`, `mazeworld.html`, `test/` — `git diff --stat HEAD~1 -- engine/ src/ content/ mazeworld.html test/` prints nothing at the plan's commit, and `npm test` still ends `# fail 0`"
  artifacts:
    - path: "tools/worn-fixture-scan.mjs"
      provides: "the Phase 45 live worn-exposure scan (report tool, exit 0, never a node:test file)"
      contains: "reconcileWorn(engineState.c)"
    - path: "tools/worn-fixture-scan-output.txt"
      provides: "the committed BEFORE readout — the measured moved set Plan 02 declares and Plan 03 pastes into FIXTURE-INVENTORY.md"
      contains: "MOVED SET ("
  key_links:
    - from: "tools/worn-fixture-scan.mjs"
      to: "test/parity/harness/sandboxPrototype.js#loadPrototypeSandbox"
      via: "one sandbox per site, driven in lockstep exactly as test/parity/full-suite.test.js drives it"
      pattern: "loadPrototypeSandbox\\(\\{ seed"
    - from: "tools/worn-fixture-scan.mjs"
      to: "test/parity/harness/comparables.js#applyStartCombat / #runEconomyAction"
      via: "the same engine-side dispatch helpers the parity tests use (never a private re-implementation)"
      pattern: "runEconomyAction\\(ctx, engineState, action\\)"
  prohibitions:
    - statement: "MUST NOT edit `test/parity/prototype-master.js.txt`, any fixture JSON, the parity harness, or any `engine/` file in this plan — the scan measures; it changes nothing"
      status: resolved
      verification: test
    - statement: "MUST NOT hard-code or assume the moved set from GEAR-SLOTS §2 — the five predicted sites are a checklist the output must contain, never the answer; every additional site the replay finds is reported, not filtered"
      status: resolved
      verification: judgment
    - statement: "MUST NOT make the scan depend on the harness's worn carve-out (`stripWornField` today, `dropEmptyWorn` after Plan 02) or on any fixture's declared records — the scan reads `c.items`/`c.worn` directly so its output cannot change when Plan 02 rewrites the harness"
      status: resolved
      verification: judgment
---

<objective>
Measure first. Add the committed, repeatable live scan that answers HEDGE-03's only open question — WHICH parity fixtures move once `c.worn` is created on every fresh character — by replaying every parity replay site against the real engine in lockstep with the frozen prototype sandbox, with the engine's start character passed through `reconcileWorn` explicitly (the one-line simulation of the collapse), and commit its output as the BEFORE readout. Plan 02 declares exactly this measured set and re-runs the scan after the engine edit to prove the measurement holds; Plan 03 pastes it into `FIXTURE-INVENTORY.md`.

Purpose: ROADMAP success criterion 3 says the moved set is "exactly the set a live scan measured before the edit (not assumed from GEAR-SLOTS §2 — the scan is committed with its output)". GEAR-SLOTS §2 predicts five sites; `engine/character.js:551` issues a starting cloak to EVERY Thief, so every Thief-hero replay site (Phase 38's inventory table names ~13) is expected to move — but expectation is not measurement. This plan's output is the input Plan 02 pastes from.
Output: `tools/worn-fixture-scan.mjs` (report tool, in the `tools/terrain-fixture-scan.mjs` pattern) and `tools/worn-fixture-scan-output.txt` (its committed stdout).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-collapse-the-phase-37-hedges/45-CONTEXT.md
@tools/terrain-fixture-scan.mjs
@test/parity/full-suite.test.js
</context>

<phase_facts>
- HEAD at planning: `7447629` (Phase 44 closed at `74476293e3f6`); 3,243 tests green; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (the recorded master hash every plan of this phase re-checks).
- `engine/derived.js:350` `reconcileWorn(c)`: returns `null` and touches nothing when `c` already has an own `worn` key; otherwise creates `c.worn = {}`, wears the first item of each slot family in bag order (`freeWornKey`), leaves extras bagged, returns the per-family report. Zero rng draws. Today `newRun(seed)` never calls it without the retired option; after Plan 02 `newRun` always does — so a scan that calls it explicitly after `newRun` measures the same state graph before and after the collapse.
- Every Thief starts with exactly one cloak (`engine/character.js:551`: `items: cls === "Thief" ? [Object.assign({ kind: "cloak" }, CLOAKS[rng.d(CLOAKS.length) - 1])] : []`); Fighters and Magic Users start with `items: []`. So at chargen a populated `c.worn` means exactly `{ cloak }` and `c.items` loses that one entry.
- Phase 38's FIXTURE-INVENTORY.md table lists the Thief-hero sites: chargen seeds 2/3/4; combat win(3)/lose-plain(1119)/flee(17)/parley(303); economy script (3); encounters chest(2)/tablefour(3)/faerie(38)/affliction(160); movement script (256). Magic scenarios (potion 1 Fighter; cast-damage 8 / heal 7 / scroll 7 Magic Users), combat lose(14)/lose-apprentice(127), encounters trap(1) are non-Thieves. This is the planner's expectation for the MOVED SET — the scan decides.
- Finds in every encounters/economy fixture are deferred into `state.pendingFind` (ECON-03, never taken by a scripted action) and the store stock in the economy fixture carries no jewelry/cloak line — so no mid-script auto-wear is expected anywhere; the scan's `first mid-script wear` column measures it instead of assuming it.
- `test/parity/full-suite.test.js` L100-375 is the one file that already replays every site with the shared dispatch helpers (`applyStartCombat`, `runEconomyAction`, `applyAction`, the sandbox's `ctx.move/startCombat/playerStrike/flee/parley/castSpell/...`); the scan copies its dispatch shape per site, replacing every `assert` with a row.
- Windows executor: Git Bash, no `jq`; write multi-line scripts with the Write tool, never heredocs or `node -e` one-liners with backslashes. Scratch paths: `$TEMP` (the Windows temp directory, exposed by Git Bash); if unset use `${TMPDIR:-/tmp}`. Scratch files are never committed.
</phase_facts>

<flagged_assumptions>
- **A-1:** "populated `c.worn` at any compare point" is equivalent to "the site's parity comparable moves": a populated map means an item left the engine's `c.items` that the prototype still holds. The scan cross-checks this by also comparing prototype vs engine `items` names at chargen and at end — a site where they differ with an EMPTY `worn` is printed as `UNEXPLAINED` and must be investigated (expected count 0).
- **A-2:** The prototype sandbox for a site is loaded exactly as the parity tests load it (`loadPrototypeSandbox({ seed })` — its boot already rolled the character once); the economy site bumps `gold` to 5000 on both sides before the actions exactly as `full-suite.test.js` does, so the end-state values the scan prints are the values `declaredEndDiffs` will measure in Plan 02.
</flagged_assumptions>

<artifacts_this_phase_produces>
- `tools/worn-fixture-scan.mjs` — the HEDGE-03 live scan (this plan).
- `tools/worn-fixture-scan-output.txt` — committed BEFORE readout; site-id vocabulary `action-script.chargen.json#seed-N`, `action-script.combat.json#<scenario>`, `action-script.magic.json#<scenario>`, `action-script.encounters.json#<scenario>`, `action-script.economy.json#script`, `action-script.movement.json#script`; summary lines `MOVED SET (N): …` and `WORN EXPOSURE: N of M replay sites`; a `## Record values` section per moved site (this plan). Plan 02's `test/parity/divergence-records.test.js` parses the `MOVED SET` line; Plan 03 pastes the whole file into FIXTURE-INVENTORY.md's Phase 45 section.
- (Plan 02) `dropEmptyWorn` in `comparables.js` replacing the Phase 37 strip; the undefined-omission rule in `chargenShiftDiffs`/`pickFields`; `worn` in the chargen `CHARACTER_FIELDS`; fixture record extensions (`fields` += `items`/`worn`, `after.worn`, `requirements` += `HEDGE-03`, `phase` `"<old>+45"`); `test/parity/divergence-records.test.js`; the HEDGE-01/HEDGE-02 pins in `test/unit/worn-migration.test.js`. (Plan 03) FIXTURE-INVENTORY.md section `## Phase 45: worn-model collapse (HEDGE-01..03) — the Thief starting cloak is worn at chargen; measured moved set, declared per site`; GEAR-SLOTS.md corrections; the 143-cell smoke readout in the SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write tools/worn-fixture-scan.mjs — every parity replay site in lockstep with the prototype, engine start character reconciled, one row per site, MOVED SET + record values</name>
  <files>tools/worn-fixture-scan.mjs</files>
  <read_first>
    - tools/terrain-fixture-scan.mjs (whole file, 233 lines — the report-tool pattern to copy: header comment, `readFixture`, per-fixture scan functions, the markdown table + summary lines, `main()`, always exit 0)
    - test/parity/full-suite.test.js L26-60 (imports + fixture loading), L103-149 (chargen sub-test: `loadPrototypeSandbox({ seed })` then `ctx.S.c` vs `newRun(seed).c`), L149-173 (movement: `ctx.move(action.dir)` + `applyAction`), L173-223 (combat: `startCombat` → `ctx.startCombat(action.wandering, action.forced)` + `applyStartCombat`; `attack` → `ctx.playerStrike()`; `flee`/`parley` → `ctx[type]()` + `applyAction`), L223-302 (magic: `startCombat`, `castSpell` → `ctx.castSpell(action.idx)`, every other type → `ctx[action.type]()` + `applyAction({ type })`), L302-360 (economy: `gold = 5000` on both sides then `runEconomyAction(ctx, engineState, action)`; encounters: `runEconomyAction` per action) — copy each dispatch branch verbatim, drop every assert
    - test/parity/harness/comparables.js L616-640 (`applyStartCombat` export), L1120-1151 (`applyInternal`, `INTERNAL_FNS`, `runEconomyAction`) — the dispatch helpers to import, never re-implement
    - test/parity/harness/sandboxPrototype.js L150-237 (`loadPrototypeSandbox` — what `ctx` exposes: `S`, `move`, `startCombat`, `playerStrike`, `flee`, `parley`, `castSpell`, `openStore`, `buyFrom`, `leaveStore`, `springTrap`, `openChest`, `encounterDot`, `descend`, and the magic scenario verbs)
    - engine/derived.js L300-377 (`reconcileWorn` JSDoc + body — what "populated worn" means; `slotFor`/`freeWornKey` families `jewelry`/`cloak`)
    - engine/character.js L545-553 (the Thief starting cloak draw)
    - test/parity/fixtures/action-script.chargen.json (top: `seeds` array), action-script.combat.json / .magic.json / .encounters.json (`scenarios[].name/seed/actions`), action-script.economy.json / .movement.json (`seed`, `actions`)
  </read_first>
  <action>
    Create `tools/worn-fixture-scan.mjs` (ESM, `#!/usr/bin/env node`, dev-only, makes no assertions, always exits 0 — NOT a `*.test.js` file). Header comment: Phase 45 (HEDGE-03) — the live worn-exposure scan: replays every parity replay site through the real engine in lockstep with the frozen prototype sandbox, with the engine's freshly rolled character passed through `reconcileWorn` (the ONE line that simulates the collapse before Plan 02 lands it; a no-op afterwards because `newRun` then creates `c.worn` itself — which is exactly why the output must be byte-identical before and after the engine edit), and reports which sites end up with a populated `c.worn` (an item left the engine's bag that the prototype still carries = a real, comparable-visible divergence to DECLARE, never to strip). Document the run form `node tools/worn-fixture-scan.mjs > tools/worn-fixture-scan-output.txt` and that Plan 02's `test/parity/divergence-records.test.js` parses the `MOVED SET` line and FIXTURE-INVENTORY.md's Phase 45 section quotes the whole output.

    Imports: `newRun`, `applyAction` from `../engine/engine.js`; `reconcileWorn` from `../engine/derived.js`; `loadPrototypeSandbox` from `../test/parity/harness/sandboxPrototype.js`; `applyStartCombat`, `runEconomyAction` from `../test/parity/harness/comparables.js`; `fs`/`path`/`url` for `readFixture` exactly as the terrain scan does. Do NOT import any `*Comparable`/`strip*` helper — the scan reads `c.items` and `c.worn` directly so its output is independent of the harness carve-outs Plan 02 rewrites.

    Helpers: `startEngine(seed)` — `const state = newRun(seed); reconcileWorn(state.c); return state;` with the invariance comment above; `itemNames(items)` — `(Array.isArray(items) ? items : []).map((it) => it && it.n).join(", ")` or the literal `(none)` when empty; `wornNames(worn)` — `Object.entries(worn || {}).map(([k, it]) => k + "=" + (it && it.n)).join(", ")` or `(none)`; `wornKeyCount(c)` — number of own keys of `c.worn` when it is a plain object, else 0; `snapshot(ctx, engineState)` returning `{ protoItems: itemNames(ctx.S.c.items), engineItems: itemNames(engineState.c.items), engineWorn: wornNames(engineState.c.worn), protoItemsJson: JSON.stringify(ctx.S.c.items), engineItemsJson: JSON.stringify(engineState.c.items), engineWornJson: JSON.stringify(engineState.c.worn ?? null) }`.

    `replaySite({ fixture, site, seed, hero, run })` — generic driver: loads `ctx = loadPrototypeSandbox({ seed })`, `engineState = startEngine(seed)`, takes the chargen snapshot and `chargenKeys = wornKeyCount(engineState.c)`, then calls `run(ctx, engineState, onAfterAction)` where the site-specific `run` dispatches each action on both sides (copying full-suite's branches) and calls `onAfterAction(i, nextEngineState)` after each; `onAfterAction` records `firstMidScriptWear = i` the first time `wornKeyCount(next.c) > chargenKeys` (else `never`). After the run take the end snapshot. Row fields: `fixture`, `site` (id form `<fixture>#seed-N` for chargen, `<fixture>#<scenario.name>` for scenarios, `<fixture>#script` for the movement/economy scripts), `seed`, `hero` = `c.cls + " " + c.sub + " " + c.race` off the engine's `c`, `wornAtChargen` = `engineWorn` at chargen, `firstMidScriptWear`, the six name strings (proto/engine items at chargen and end, engine worn at chargen and end), `moved` = `chargenKeys > 0 || firstMidScriptWear !== "never"`, `unexplained` = (`protoItems !== engineItems` at chargen or at end) && !moved.

    Sites, in this fixed order (the order IS the output order): chargen — every seed of `CHARGEN.seeds`, `run` does nothing (chargen == end); movement — the single script, `run` = for each action `ctx.move(action.dir)` then `applyAction(engineState, action)`; combat — each scenario, the four branches (`startCombat` via `ctx.startCombat(action.wandering, action.forced)` + `applyStartCombat(engineState, action.wandering, action.forced)`; `attack` via `ctx.playerStrike()` + `applyAction({ type: "attack" })`; `flee`/`parley` via `ctx[type]()` + `applyAction({ type })`; anything else throws with the same message full-suite uses); magic — each scenario, full-suite's magic branches (`startCombat`, `castSpell` with `idx`, otherwise `ctx[action.type]()` + `applyAction({ type: action.type })`); economy — the script: set `ctx.S.c.gold = 5000` and `engineState.c.gold = 5000` AFTER the chargen snapshot (so `before.items` is the untouched chargen bag and the gold bump matches full-suite), then `runEconomyAction(ctx, engineState, action)` per action; encounters — each scenario via `runEconomyAction`. The economy END snapshot is the value Plan 02 writes into the action-path record's `after.items`/`after.worn`, so it must be taken after the last (`leaveStore`) action, on the same state the parity test's `declaredEndDiffs` reads.

    Output (stdout only, deterministic, no timestamps, no absolute paths): a one-line title `# worn-fixture-scan — Phase 45 (HEDGE-03): c.worn created on every fresh character`; a markdown table with columns `Fixture | Site | Seed | Hero | worn at chargen | first mid-script wear | items @chargen (prototype) | items @chargen (engine) | worn @chargen (engine) | items @end (prototype) | items @end (engine) | worn @end (engine) | moved`; an `UNEXPLAINED: <site>` line for each row with `unexplained` true (expected none); a blank line; `MOVED SET (N): id1, id2, …` (N = count, ids comma+space separated, in table order); `WORN EXPOSURE: N of M replay sites`; then `## Record values (JSON, per moved site)` followed, for each moved row in order, by a `### <site>` heading and six labelled single-line JSON entries: `before.items (prototype @chargen): <json>`, `after.items (engine @chargen): <json>`, `after.worn (engine @chargen): <json>`, `end.before.items (prototype @end): <json>`, `end.after.items (engine @end): <json>`, `end.after.worn (engine @end): <json>` — these are the exact values Plan 02 pastes into `before`/`after` maps (chargen values for `divergences`/`chargenDivergence` records, END values for the economy action-path record).

    Run it twice to a scratch path outside the repo and `diff` the two outputs (must be empty). Do not commit yet — Task 2 commits the tool together with its output.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/worn-fixture-scan.mjs > "$TEMP/wfs-a.txt" && node tools/worn-fixture-scan.mjs > "$TEMP/wfs-b.txt" && diff "$TEMP/wfs-a.txt" "$TEMP/wfs-b.txt" && grep -E "^MOVED SET \([0-9]+\):" "$TEMP/wfs-a.txt" && grep -E "^WORN EXPOSURE: [0-9]+ of [0-9]+ replay sites$" "$TEMP/wfs-a.txt"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/worn-fixture-scan.mjs` exits 0 and prints the title line, one table row per replay site (14 chargen + 1 movement + 6 combat + 4 magic + 1 economy + 5 encounters = 31 rows: `grep -c '^| action-script\.' "$TEMP/wfs-a.txt"` prints `31`), a `MOVED SET (N):` line and a `WORN EXPOSURE: N of 31 replay sites` line.
    - Two consecutive runs are byte-identical: `diff "$TEMP/wfs-a.txt" "$TEMP/wfs-b.txt"` prints nothing.
    - `grep -c "reconcileWorn(state.c)" tools/worn-fixture-scan.mjs` prints `1`; `grep -cE "import .*(Comparable|strip[A-Z])" tools/worn-fixture-scan.mjs` prints `0` (no harness carve-out imported); `grep -c "runEconomyAction(ctx, engineState, action)" tools/worn-fixture-scan.mjs` ≥ 1; `grep -c "applyStartCombat(" tools/worn-fixture-scan.mjs` ≥ 1.
    - `grep -c "^UNEXPLAINED:" "$TEMP/wfs-a.txt"` prints `0`.
    - For each of `action-script.chargen.json#seed-2`, `action-script.chargen.json#seed-3`, `action-script.chargen.json#seed-4`, `action-script.combat.json#flee`, `action-script.economy.json#script`: `grep -c "^### <id>$" "$TEMP/wfs-a.txt"` prints `1` (the GEAR-SLOTS §2 checklist is present in the record-values section).
    - `grep -c "^### " "$TEMP/wfs-a.txt"` equals the N in `MOVED SET (N):` (one record-values block per moved site).
    - `git status --porcelain -- engine/ src/ content/ mazeworld.html test/` prints nothing (the scan changed no engine/harness bytes).
  </acceptance_criteria>
  <done>The scan exists, is deterministic, uses the shared dispatch helpers, reads `c.items`/`c.worn` directly, prints the 31-row table, MOVED SET, WORN EXPOSURE and per-site record values.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the BEFORE readout — tools/worn-fixture-scan-output.txt — and prove nothing else moved</name>
  <files>tools/worn-fixture-scan-output.txt</files>
  <read_first>
    - The Task 1 scratch output (`$TEMP/wfs-a.txt`) — read the MOVED SET line and every row whose `moved` column is true; compare against the phase_facts expectation (chargen 2/3/4; combat win/lose-plain/flee/parley; economy; encounters chest/tablefour/faerie/affliction; movement) and note any difference for the SUMMARY (a difference is the measurement, not an error — unless an `UNEXPLAINED` line appears)
    - tools/voice-sample-output.txt (the precedent for a committed tool readout living next to its tool)
  </read_first>
  <action>
    Run `node tools/worn-fixture-scan.mjs > tools/worn-fixture-scan-output.txt` from the repo root (the committed path; the same command form Plan 02 re-runs after the engine edit and Plan 03 quotes). Confirm the committed file equals a fresh run and equals the Task 1 scratch output. Record in the SUMMARY: the full `MOVED SET (N):` line, the `WORN EXPOSURE` line, the table rows with `moved` true, and — for every moved site — whether it matches the planner's expectation (Thief hero → moves) or not; state explicitly that no site shows a mid-script wear (or list the ones that do — Plan 02 then needs an action-path record for them instead of a chargen-time record). Then `npm test` (must still be 3,243 pass / fail 0 — this plan added no test and touched no engine byte) and `npm run build:www`. Commit the two files together: `feat(45-01): worn-fixture-scan — live measurement of the fixtures c.worn moves, BEFORE readout committed (HEDGE-03)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/worn-fixture-scan.mjs > "$TEMP/wfs-c.txt" && diff "$TEMP/wfs-c.txt" tools/worn-fixture-scan-output.txt && git status --porcelain -- engine/ src/ content/ mazeworld.html test/ | wc -l && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `diff <(node tools/worn-fixture-scan.mjs) tools/worn-fixture-scan-output.txt` prints nothing (the committed file is a fresh run).
    - `grep -E "^MOVED SET \(([0-9]+)\):" tools/worn-fixture-scan-output.txt` prints one line, and the N inside the parentheses equals the number of comma-separated ids after the colon (count via a node script written to a scratch file, or `grep -o '#' ` on that line: `grep -E "^MOVED SET" tools/worn-fixture-scan-output.txt | grep -o "action-script\." | wc -l` equals N).
    - `git diff --stat HEAD~1 -- engine/ src/ content/ mazeworld.html test/` prints nothing at the commit; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `npm test 2>&1 | grep -E "^# (pass|fail) [0-9]+"` prints `# pass 3243` and `# fail 0`; `npm run build:www` exits 0.
    - `git log -1 --stat` shows exactly `tools/worn-fixture-scan.mjs` and `tools/worn-fixture-scan-output.txt`.
  </acceptance_criteria>
  <done>The BEFORE readout is committed next to its tool, byte-equal to a fresh run, with the suite green and zero engine/harness bytes changed.</done>
</task>

</tasks>

<verification>
- One commit; suite green; no engine/harness/fixture bytes changed; the scan output committed with a `MOVED SET` line Plan 02 treats as the declared set (and its records test parses).
- The five GEAR-SLOTS §2 predictions are present in the MOVED SET; every extra site the scan found is reported in the SUMMARY as the measurement.
</verification>

<success_criteria>
- `tools/worn-fixture-scan.mjs` replays all 31 sites in lockstep with the sandbox, deterministic, harness-independent.
- `tools/worn-fixture-scan-output.txt` committed, equal to a fresh run, `UNEXPLAINED` count 0.
- `npm test` `# fail 0`, `npm run build:www` exit 0, master hash unchanged.
</success_criteria>

<output>
Create `.planning/phases/45-collapse-the-phase-37-hedges/45-01-SUMMARY.md` with: `## Scan output` (the `MOVED SET (N):` and `WORN EXPOSURE` lines verbatim plus every `moved` row of the table); `## Expectation vs measurement` (per moved site: expected/unexpected, and the explicit "no mid-script wear anywhere" statement or the list of sites with one); `## Gate outputs` (`diff` empty, `npm test` counts, `build:www`, master hash, `git diff --stat HEAD~1 -- engine/ src/ content/ mazeworld.html test/` empty); `## Human verification (deferred to end of run)` — "none for this plan (a measurement tool; nothing user-visible)"; `## Flagged assumptions` (A-1/A-2 status).
</output>
