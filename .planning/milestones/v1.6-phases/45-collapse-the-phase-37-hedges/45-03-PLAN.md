---
phase: 45-collapse-the-phase-37-hedges
plan: 03
type: execute
wave: 3
depends_on: ["45-02"]
files_modified:
  - test/parity/FIXTURE-INVENTORY.md
  - docs/GEAR-SLOTS.md
autonomous: true
requirements: [HEDGE-01, HEDGE-02, HEDGE-03]
tags: [fixture-inventory, docs, tuning-bot, smoke, closing-gates]

must_haves:
  truths:
    - "HEDGE-03: `test/parity/FIXTURE-INVENTORY.md` carries a `## Phase 45: worn-model collapse (HEDGE-01..03) — the Thief starting cloak is worn at chargen; measured moved set, declared per site` section with the moved-set table (holder, record kind, `items` before → after, `worn` after) and the committed scan output quoted verbatim; its generated roster block is regenerated by `node tools/fixture-inventory.mjs` and `test/parity/fixture-inventory.test.js` is green"
    - "`docs/GEAR-SLOTS.md` no longer states that fixtures/bots/tools/tests never create `c.worn`, that the migration is option-gated, or that `RUN_FLAGS` carries a worn flag: every remaining line that mentions the retired identifier also says `Phase 45` (an explicitly historical mention), and §2 / §3 / §5 / §7 describe the single path"
    - "HEDGE-01 (bot, ROADMAP criterion 4): a fresh 143-cell × 3-seed smoke (`node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --out <scratch>`) reports 143 cells, 0 stuck runs, `meta.runFlags` deep-equal `{ storeRoll: true }`, and its `Bot:` line equals `docs/class-pass/v15-after.json` `meta.bot` with `seeds=40` replaced by `seeds=3` (the frozen parameter line; only the run size differs); `node tools/class-pass-diff.mjs --gate --after <scratch>` exits 0 with `cannot-act cells: 0 of 143`; the readout is written to a scratch path, not `docs/class-pass/`"
    - "ROADMAP success criteria 1–5 are verified verbatim at the closing commit and their outputs are in the SUMMARY: identifier grep 0 + the no-option pins green; the v1.4-era save pin green; `divergence-records.test.js` green, `git status --porcelain test/parity/fixtures` clean, master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `RUN_FLAGS`/smoke as above; `npm test` `# fail 0` and `npm run build:www` exit 0 at every commit of the phase (`git log` from Plan 01's commit to HEAD shows each commit, and each was gated)"
    - statement: "The Phase 42 transcription in `docs/CLASS-PASS.md` (`Run flags. RUN_FLAGS = { storeRoll: true, … }`) and the frozen `docs/class-pass/*.json` readouts are untouched — they are history for Phase 48's docs sweep, not this phase's prose fix"
      verification: backstop
  artifacts:
    - path: "test/parity/FIXTURE-INVENTORY.md"
      provides: "the Phase 45 section: moved set + scan output + byte-identical-elsewhere statement"
      contains: "## Phase 45: worn-model collapse (HEDGE-01..03)"
    - path: "docs/GEAR-SLOTS.md"
      provides: "§2/§3/§5/§7 corrected to the single path"
      contains: "Phase 45"
  key_links:
    - from: "test/parity/FIXTURE-INVENTORY.md Phase 45 section"
      to: "tools/worn-fixture-scan-output.txt"
      via: "verbatim quote of the committed readout (the MOVED SET line must match)"
      pattern: "MOVED SET \\("
    - from: "the smoke readout (scratch)"
      to: "docs/class-pass/v15-after.json#meta.bot"
      via: "Bot: line equality modulo the seeds= field"
      pattern: "Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5\\(caster\\)  potion<0.5  camp<0.5  seeds=3  workers=4  startDepth=1"
  prohibitions:
    - statement: "MUST NOT write the smoke readout under `docs/class-pass/` or add a ledger entry to `docs/CLASS-PASS.md` — it is a smoke, not a readout; MUST NOT edit any frozen readout JSON"
      status: resolved
      verification: test
    - statement: "MUST NOT edit any `engine/`, `src/`, `content/`, `tools/`, `mazeworld.html` or `test/` file in this plan other than `test/parity/FIXTURE-INVENTORY.md` — the collapse is complete; this plan documents and gates"
      status: resolved
      verification: test
    - statement: "MUST NOT sweep `wornSlots`/flag-off prose outside `docs/GEAR-SLOTS.md` (Phase 48 DOCS-01/02) and MUST NOT rename identifiers elsewhere (Phase 46 NAME-02) — only the prose this phase made false is corrected"
      status: resolved
      verification: judgment
---

<!-- planner-discipline-allow: wornSlots -->

<objective>
Close the phase: record the measured moved set in `test/parity/FIXTURE-INVENTORY.md` (Phase 45 section + regenerated roster block), correct the prose in `docs/GEAR-SLOTS.md` that Plan 02 made false (§2 "Fixture impact: ZERO", §3 "absent on every fixture", §5's option-gated migration / `newRun` option / bot flag bullets, §7's Phase 42 note), run the 143-cell × 3-seed tuning-bot smoke on the single path (ROADMAP criterion 4) with its readout in a scratch path, then verify ROADMAP success criteria 1–5 verbatim and write the closing SUMMARY with the scan output, the moved-fixture list with before/after values, the `Bot:` line comparison and the deferred Pixel 7 items.

Purpose: HEDGE-03's "FIXTURE-INVENTORY.md is regenerated" and criterion 4's bot smoke are the last two acceptance checks; the docs fix is scoped to the one file whose prose this phase falsified (the repo-wide sweep is Phase 48).
Output: two commits (inventory + docs; closing summary), each gated by `npm test` fail 0 and `npm run build:www`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/45-collapse-the-phase-37-hedges/45-CONTEXT.md
@.planning/phases/45-collapse-the-phase-37-hedges/45-01-SUMMARY.md
@.planning/phases/45-collapse-the-phase-37-hedges/45-02-SUMMARY.md
@tools/worn-fixture-scan-output.txt
</context>

<phase_facts>
- `tools/fixture-inventory.mjs` prints the roster table to paste between `<!-- fixture-inventory:generated:begin -->` / `<!-- fixture-inventory:generated:end -->` in FIXTURE-INVENTORY.md; the phase sections below the block are hand-written prose (Phase 38's section at L725-800 is the declared-record precedent; Phase 41's at L1235-1310 is the live-scan-quoted precedent). The roster is about creatures rolled per fight, so the regenerated block is expected byte-identical — regenerate and diff anyway.
- `docs/GEAR-SLOTS.md` false-after-Phase-45 prose: §2 L43-52 ("Fixture impact: ZERO … Nothing in the fixture/bot/tools/test call paths ever passes the option or calls reconcileWorn — the model is entirely opt-in"), L56-58 ("kept only for states that predate the model (fixtures, bots, un-migrated saves)"); §3 L151-152 ("lazily created. Absent on every fixture, bot run and un-migrated save"); §5 L208-221 (migration "gated on the shell-only option", `validateSave(raw, { … })`/`rehydrate(obj, { … })`, "Fresh runs: newRun(seed, exclude, { … })"), L225-227 ("Creation (reconcileWorn) is option-gated; tolerance (sanitizeWorn) is unconditional"), L228-238 (report "only when the migration actually ran … boot() passes … on every real load"), L249-262 ("The option gate (surfaced assumption, Plan 03)" bullet — the six round-trip contracts it names were re-pinned in 45-02), L263-272 ("Bot / balance … RUN_FLAGS = { storeRoll: true, … }"); §7 L315-318 ("Phase 42 — landed: … now plays under { … } (RUN_FLAGS)"). The §3 blockquote at L145-148 (`> **260918-wy1 update …**`) is the in-place-correction precedent.
- `docs/CLASS-PASS.md` L100-116 transcribes Phase 42's `RUN_FLAGS` verbatim as history ("This section transcribes … from tuning-bot.mjs's own JSDoc") — leave it; Phase 48 decides its wording. `test/unit/class-pass-ledger.test.js` pins other parts of that ledger — another reason not to touch it here.
- Smoke precedent: `docs/CLASS-PASS.md:257` — `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --out <path>`; 143 cells × 3 seeds = 429 runs, ~70 s wall time; `meta.bot` for a 3-seed run reads `… seeds=3  workers=4  startDepth=1` (the `seeds=` field is the run size — `botLine` in `tools/lib/tuning-bot.mjs:1535-1541`), so "byte-equal to the v1.5 AFTER pin" means equal after substituting `seeds=40` → `seeds=3`. `docs/class-pass/v15-after.json` `meta.bot` = `Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5(caster)  potion<0.5  camp<0.5  seeds=40  workers=4  startDepth=1`.
- `tools/class-pass-diff.mjs --gate --after <file>` prints `cannot-act cells: N of 143` and exits 3 when N > 0 (a stuck run makes a cell cannot-act).
- Windows executor: Git Bash; `$TEMP` resolves to the user's temp directory (e.g. `C:/Users/Dell/AppData/Local/Temp`); write the smoke-check script with the Write tool to an absolute scratch path and run it with `node`.
</phase_facts>

<flagged_assumptions>
- **A-7:** the regenerated roster block is byte-identical (worn state never changes which creatures a fight rolls); if it is not, the difference is a scan/engine bug — stop and report, do not paste a changed roster.
- **A-8:** the 3-seed smoke is a direction/reachability check (Phase 42's own caveat): pooled numbers are quoted for the SUMMARY but are not a tuning verdict; only `cells`, `stuck`, `runFlags`, the `Bot:` line and the `--gate` exit code are acceptance checks.
</flagged_assumptions>

<artifacts_this_phase_produces>
- `test/parity/FIXTURE-INVENTORY.md` — `## Phase 45: worn-model collapse (HEDGE-01..03) — the Thief starting cloak is worn at chargen; measured moved set, declared per site` with sub-headings `### The live scan (tools/worn-fixture-scan.mjs)`, `### Moved set — declared records`, `### Byte-identical elsewhere (Phase 45)` (this plan).
- `docs/GEAR-SLOTS.md` — a `> **Phase 45 update (2026-09-19)**` blockquote at the top of §5 and in-place corrections in §2/§3/§5/§7 (this plan).
- The smoke readout at a scratch path (quoted in the SUMMARY, not committed) and the `45-03-SUMMARY.md` closing sections (this plan).
- (Plan 01) `tools/worn-fixture-scan.mjs` + `tools/worn-fixture-scan-output.txt`. (Plan 02) `dropEmptyWorn`, the undefined-omission rule, `test/parity/divergence-records.test.js`, the fixture record extensions, the HEDGE-01/02 pins, `RUN_FLAGS = { storeRoll: true }`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: FIXTURE-INVENTORY.md Phase 45 section + regenerated roster; docs/GEAR-SLOTS.md prose corrected to the single path</name>
  <files>test/parity/FIXTURE-INVENTORY.md, docs/GEAR-SLOTS.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md L1-52 (regeneration instructions + the generated block markers), L725-800 (Phase 38 section — the table format for declared records), L1235-1310 (Phase 41 section — how a live scan's output is quoted), L1554-1621 (the last section, to append after)
    - tools/worn-fixture-scan-output.txt (whole file — quoted verbatim)
    - .planning/phases/45-collapse-the-phase-37-hedges/45-02-SUMMARY.md `## Moved fixtures` (the per-site before/after table to transcribe)
    - docs/GEAR-SLOTS.md L38-62 (§2 fixture impact + assumption-delta), L143-160 (§3 blockquote precedent + Storage bullet), L206-275 (§5 whole section), L311-324 (§7)
    - tools/fixture-inventory.mjs (30 lines) and test/parity/fixture-inventory.test.js L206-227 (the generated-block pin)
  </read_first>
  <action>
    FIXTURE-INVENTORY.md: run `node tools/fixture-inventory.mjs` and replace the text between the `begin`/`end` markers with its stdout verbatim (expected identical — A-7; if `git diff` shows a roster change, stop). Append a new section at the end of the file: `## Phase 45: worn-model collapse (HEDGE-01..03) — the Thief starting cloak is worn at chargen; measured moved set, declared per site`. Body: one paragraph on what changed (`newRun` always runs `reconcileWorn` — zero rng draws, `chargen-rng-pin.test.js` untouched; every load reconciles; the run/load option and the Phase 37 harness carve-out are gone; `dropEmptyWorn` drops an empty map and a populated map is declared); `### The live scan (tools/worn-fixture-scan.mjs)` — the run form, then the committed `tools/worn-fixture-scan-output.txt` quoted verbatim in a fenced block (title line, table, MOVED SET, WORN EXPOSURE, record values); `### Moved set — declared records` — a table in the Phase 38 format: `| Fixture | Site / seed | Hero | record | fields added | items before (prototype) | items after (engine) | worn after (engine) | Rationale |`, one row per MOVED SET site, values from 45-02-SUMMARY's `## Moved fixtures` (the economy script gets two rows: its `chargenDivergence` (chargen values) and its action-path `divergence` (END values)); `### Byte-identical elsewhere (Phase 45)` — every non-Thief site by name (chargen seeds not on the MOVED SET, combat lose/lose-apprentice, all four magic scenarios, encounters trap) carries no record change; `prototype-master.js.txt` hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` unchanged; no `seed`/`actions`/`scenarios` array touched; the scan re-run after the edit was byte-identical to the BEFORE readout; the `divergence-records.test.js` equality (declared holders == MOVED SET) is the standing guard. Run `node --test test/parity/fixture-inventory.test.js test/parity/divergence-records.test.js`.

    docs/GEAR-SLOTS.md: add at the top of §5 (after the heading, before the first bullet) a blockquote `> **Phase 45 update (2026-09-19):** the bullets below describe the ORIGINAL Phase 37 option-gated design. Since Phase 45 (HEDGE-01/02) there is one path: \`newRun(seed)\` always creates \`c.worn\` (the Thief's starting cloak worn, zero rng draws) and \`validateSave\`/\`rehydrate\` always reconcile — \`validateSave\` returns \`{ ok, value, wornReport }\` with \`wornReport: []\` when nothing moved. The parity fixtures this moved are declared per site — see \`test/parity/FIXTURE-INVENTORY.md\`, Phase 45.` Then correct in place: §2 L43-52 — retitle the paragraph `**Fixture impact (revised Phase 45):**` and state the measured moved set (N sites, every Thief-hero replay site, listed by id) declared with before/after records, every other fixture byte-identical, master hash unchanged; L56-58 — "kept only for states that predate the model" → "no legacy variant survives; since Phase 45 every state carries `c.worn`" (keep the PROMOTE decision text). §3 L151-152 — "lazily created. Absent on every fixture, bot run and un-migrated save." → "created by `newRun` for every character (`{}` when no slot item was issued) and by the unconditional load reconcile (Phase 45); never absent on an engine-produced state." §5 bullets — "Migration runs on load, gated on …" → "Migration runs on every load (unconditional since Phase 45) — `validateSave(raw, { freshSeed })` / `rehydrate(obj)` …"; "Fresh runs: `newRun(seed, exclude, { … })` — an option only the shell's new-game path sets" → "Fresh runs: `newRun(seed)` — every caller (shell, bot, tools, tests, fixtures) — creates `c.worn` and wears the Thief's starting cloak, zero rng draws (Phase 45)"; "Creation (reconcileWorn) is option-gated; tolerance (sanitizeWorn) is unconditional" → "both unconditional (Phase 45); the two never overlap in what they touch"; the report bullet — "carries this as `wornReport` only when the migration actually ran … `boot()` passes … on every real load" → "always carries `wornReport` (`[]` when nothing moved, including an already-migrated save); `boot()` stashes it; `takeBootWornReport()` returns it once"; replace the "**The option gate (surfaced assumption, Plan 03):**" bullet with "**The option gate — collapsed (Phase 45):**" summarising that the six round-trip contracts it named were re-pinned to the single path in 45-02 (name them) and the option is gone; "**Bot / balance:**" — `RUN_FLAGS = { storeRoll: true }` since Phase 45 (the v1.5 AFTER readouts recorded the two-flag era in `meta.runFlags` and are frozen history). §7 — the Phase 42 bullet gains "(Phase 45: the flag is gone — `RUN_FLAGS = { storeRoll: true }`; the bot plays the engine's only path)". Every line that still spells the retired identifier must also contain `Phase 45` (historical mention); rephrase any that would not. Do not touch §1, §4, §6, §8 or any other doc.

    `npm test` (fail 0) and `npm run build:www`, then commit both files: `docs(45-03): FIXTURE-INVENTORY Phase 45 section (measured moved set + scan), GEAR-SLOTS prose on the single worn path (HEDGE-03)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && grep -c "^## Phase 45: worn-model collapse (HEDGE-01..03)" test/parity/FIXTURE-INVENTORY.md && grep -c "^MOVED SET (" test/parity/FIXTURE-INVENTORY.md && test "$(grep -n "wornSlots" docs/GEAR-SLOTS.md | grep -vc "Phase 45")" = "0" && node --test test/parity/fixture-inventory.test.js test/parity/divergence-records.test.js 2>&1 | grep -E "^# fail 0" && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Phase 45: worn-model collapse (HEDGE-01..03) — the Thief starting cloak is worn at chargen; measured moved set, declared per site" test/parity/FIXTURE-INVENTORY.md` prints `1`; `grep -c "^### The live scan (tools/worn-fixture-scan.mjs)" test/parity/FIXTURE-INVENTORY.md` prints `1`; `grep -c "^### Moved set — declared records" test/parity/FIXTURE-INVENTORY.md` prints `1`; `grep -c "^### Byte-identical elsewhere (Phase 45)" test/parity/FIXTURE-INVENTORY.md` prints `1`.
    - The quoted MOVED SET line matches the committed readout: `grep "^MOVED SET (" tools/worn-fixture-scan-output.txt` and `grep "^MOVED SET (" test/parity/FIXTURE-INVENTORY.md` print the same line.
    - `git diff HEAD~1 -- test/parity/FIXTURE-INVENTORY.md | grep -cE "^[-+]\| " ` shows added rows only within the new section (no roster row changed: `git diff HEAD~1 -- test/parity/FIXTURE-INVENTORY.md | grep -c "^-| "` prints `0`).
    - `node --test test/parity/fixture-inventory.test.js test/parity/divergence-records.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `grep -n "wornSlots" docs/GEAR-SLOTS.md | grep -vc "Phase 45"` prints `0`; `grep -c "Phase 45 update (2026-09-19)" docs/GEAR-SLOTS.md` prints `1`; `grep -c "Fixture impact (revised Phase 45)" docs/GEAR-SLOTS.md` prints `1`; `grep -c "Fixture impact: ZERO" docs/GEAR-SLOTS.md` prints `0`; `grep -c "entirely opt-in" docs/GEAR-SLOTS.md` prints `0`; `grep -c "Absent on every fixture" docs/GEAR-SLOTS.md` prints `0`; `grep -c "RUN_FLAGS = { storeRoll: true }" docs/GEAR-SLOTS.md` ≥ 1.
    - `git diff --stat HEAD~1` lists exactly `test/parity/FIXTURE-INVENTORY.md` and `docs/GEAR-SLOTS.md`; `git diff --stat HEAD~1 -- docs/CLASS-PASS.md docs/class-pass/ engine/ src/ content/ tools/ mazeworld.html` prints nothing.
    - `npm test 2>&1 | grep -E "^# fail 0"` prints `# fail 0`; `npm run build:www` exits 0.
  </acceptance_criteria>
  <done>The inventory documents the measured, declared set with the scan quoted; the roster block is regenerated; GEAR-SLOTS describes the single path; suite green at the commit.</done>
</task>

<task type="auto">
  <name>Task 2: The 143-cell × 3-seed tuning-bot smoke on the single path — 0 stuck, runFlags { storeRoll: true }, Bot: line equal to the v1.5 AFTER pin (modulo seeds=)</name>
  <files>(none committed — the readout goes to a scratch path; the SUMMARY quotes it)</files>
  <read_first>
    - docs/CLASS-PASS.md L250-290 (the Phase 42 3-seed smoke: command form, wall time, the measures table, the `--gate` line)
    - tools/lib/tuning-bot.mjs L1526-1541 (`botLine` — the `seeds=`/`workers=` fields are appended from opts) and L560-575 (`RUN_FLAGS`, now `{ storeRoll: true }`)
    - tools/class-pass-diff.mjs L20-60 and L85-95 (`--gate` semantics: cannot-act = stuck > 0 or completed === 0 or meanKills < 0.5; exit 3 on any)
    - tools/lib/class-matrix.mjs L440-460 (`meta` shape: `cells`, `runFlags`, `bot`, `excluded`)
    - docs/class-pass/v15-after.json `meta` (via a node print — `bot`, `runFlags`, `cells`)
  </read_first>
  <action>
    Run `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --out "$TEMP/45-hedge-smoke.json"` from the repo root (≈ 70 s; 143 cells × 3 seeds = 429 runs). Then `node tools/class-pass-diff.mjs --gate --after "$TEMP/45-hedge-smoke.json"` (must exit 0 and print `cannot-act cells: 0 of 143`). Write `C:/Users/Dell/AppData/Local/Temp/45-smoke-check.mjs` with the Write tool (absolute path; resolve `$TEMP` if it differs): it reads the smoke JSON and `docs/class-pass/v15-after.json`, and prints, one per line: `cells=<smoke.meta.cells>` (expect 143; also assert `smoke.cells.length === 143`), `runs=<sum of cells[].n>` (expect 429), `stuck=<sum of cells[].stuck>` (expect 0), `runFlags=<JSON.stringify(smoke.meta.runFlags)>` (expect `{"storeRoll":true}`), `bot=<smoke.meta.bot>`, `pin=<v15After.meta.bot>`, `botMatch=<smoke.meta.bot === v15After.meta.bot.replace("seeds=40", "seeds=3")>` (expect `true`), `pooled=<JSON.stringify(smoke.rollups.pooled)>`; exit 1 with `FAIL <reason>` if any expectation fails, else print `SMOKE OK`. Run it with `node`. Do NOT copy the readout into `docs/class-pass/` and do NOT add a ledger entry — this is a smoke, not a readout (CONTEXT criterion 4). Record in the SUMMARY: the exact command, wall time, engine commit (`git rev-parse --short HEAD`), the check script's full output (the `Bot:` line vs the pin side by side, `runFlags`, `stuck`), the `--gate` line, and the pooled row (meanDepth/p50/reach5/reach10/reach20/meanKills/stuck) next to the v1.5 tactics smoke's row from `docs/CLASS-PASS.md:264-274` as a direction check only (A-8 — not a verdict). Nothing to commit in this task.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/class-pass-diff.mjs --gate --after "$TEMP/45-hedge-smoke.json" && node "$TEMP/45-smoke-check.mjs" | grep -E "^(SMOKE OK|runFlags=\{\"storeRoll\":true\}|stuck=0|cells=143|botMatch=true)$"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --out "$TEMP/45-hedge-smoke.json"` exits 0 and the file exists.
    - `node tools/class-pass-diff.mjs --gate --after "$TEMP/45-hedge-smoke.json"` exits 0 and prints `cannot-act cells: 0 of 143`.
    - `node "$TEMP/45-smoke-check.mjs"` prints `cells=143`, `runs=429`, `stuck=0`, `runFlags={"storeRoll":true}`, `botMatch=true`, `SMOKE OK`, and its `bot=` line equals `Bot: exploreBudget=50  maxActions=5000  party=off  flee=0.3/0.5(caster)  potion<0.5  camp<0.5  seeds=3  workers=4  startDepth=1`.
    - `git status --porcelain` prints nothing (no readout or script landed in the repo; `ls docs/class-pass/` is unchanged from Plan 02's commit).
  </acceptance_criteria>
  <done>The bot plays the single path: 143 cells, 429 runs, 0 stuck, the frozen parameter line intact, the retired flag absent from `meta.runFlags`; readout kept out of the repo.</done>
</task>

<task type="auto">
  <name>Task 3: Closing gates — ROADMAP success criteria 1–5 verbatim — and the phase-closing SUMMARY</name>
  <files>.planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md, the `### Phase 45` section (the five success criteria — run each verbatim)
    - .planning/phases/45-collapse-the-phase-37-hedges/45-01-SUMMARY.md and 45-02-SUMMARY.md (the scan output, the moved-fixture table with before/after values, the tests re-pinned/deleted table, the declared consequences — consolidated here)
    - .planning/phases/44-retire-the-classic-engine-from-the-shell/44-04-SUMMARY.md L318-400 (the closing-summary format: `## Success criteria` with outputs, `## Gate outputs` per commit, `## Human verification (deferred to end of run)`, `## Flagged assumptions`)
  </read_first>
  <action>
    Run and capture every closing gate: (1) `grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/ | wc -l` → `0`; `node tools/shell-sweep.mjs refs wornSlots` → 0; `node --test test/unit/worn-migration.test.js` → `# fail 0` with the `HEDGE-01: newRun(2) with no options …` test listed as `ok`. (2) the same run's `HEDGE-02: a v1.4-era save …`, `HEDGE-02 ordering: …` and `HEDGE-02 idempotency: …` tests `ok`. (3) `node --test test/parity/divergence-records.test.js` → `# pass 2`; `diff <(node tools/worn-fixture-scan.mjs) tools/worn-fixture-scan-output.txt` → empty; `git status --porcelain test/parity/fixtures` → empty; `git hash-object test/parity/prototype-master.js.txt` → `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `node --test test/parity/fixture-inventory.test.js` → `# fail 0`; `git diff --stat 7447629 -- test/parity/fixtures/` → lists only the MOVED SET holders' files + `action-script.schema.md`. (4) `grep -c "Object.freeze({ storeRoll: true })" tools/lib/tuning-bot.mjs` → `1`; the Task 2 smoke check output (`stuck=0`, `runFlags={"storeRoll":true}`, `botMatch=true`). (5) `npm test` → `# fail 0` (record the final count vs the 3,243 start and reconcile against 45-02's table); `npm run build:www` → exit 0; `git log --oneline 7447629..HEAD` — list every phase commit (45-01, 45-02, 45-03 Task 1, this one) with the note that each was gated (`npm test` fail 0 + `build:www`) per its SUMMARY. Also: `git diff --stat 7447629 -- engine/ content/` → exactly `engine/state.js`, `engine/saveState.js`, `engine/derived.js`; `git diff --stat 7447629 -- test/unit/chargen-rng-pin.test.js docs/class-pass/ docs/CLASS-PASS.md` → empty; `wc -l mazeworld.html` unchanged (6356 — the shell was not edited).

    Write `45-03-SUMMARY.md` with: `## Success criteria` (the five ROADMAP criteria, each with its command(s) and output); `## Scan output` (the committed `MOVED SET (N):` and `WORN EXPOSURE` lines, and the BEFORE == AFTER diff result); `## Moved fixtures (before/after)` (the consolidated per-site table from 45-02 — holder, record kind, `items` before → after, `worn` after); `## Bot: line comparison` (smoke line vs `v15-after.json` `meta.bot`, `runFlags`, `stuck`, `--gate`); `## Tests re-pinned or deleted (phase total)` (45-02's table); `## Declared consequences` (`takeBootWornReport()` returns `[]` for an already-migrated save; the ledger test's stored-file assertion reworded — `docs/class-pass/*.json` and `docs/CLASS-PASS.md`'s Phase 42 transcription are frozen history, DO NOT regenerate the ledger to the new `RUN_FLAGS` shape — state this explicitly for future readers); `## Gate outputs` (per commit); `## Human verification (deferred to end of run)` — the Pixel 7 items: a fresh Thief starts with the cloak in the WORN row of the Gear tab (`c.worn.cloak`, not in the BAG); resuming the current on-device save shows the reconciliation rail card once if it bagged an extra (or no card if already migrated / nothing bagged), and the Oracle line; a Fighter's Gear tab shows an empty worn row set and an unchanged bag; nothing else is user-visible; `## Flagged assumptions` (A-1..A-8 final status); `## Next phase readiness` (Phase 46 NAME-02's zero-straggler grep for the retired identifier is already 0 in engine/src/shell/tools/test — `docs/` mentions are Phase 48's). Commit: `docs(45-03): phase 45 closing summary — success criteria 1–5 verified, moved set, Bot: line, deferred Pixel 7 items`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test "$(grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/ | wc -l)" = "0" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && test -z "$(git status --porcelain test/parity/fixtures)" && node --test test/parity/divergence-records.test.js test/unit/worn-migration.test.js 2>&1 | grep -E "^# fail 0" && npm test 2>&1 | grep -E "^# fail 0" && grep -c "## Human verification (deferred to end of run)" .planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "wornSlots" engine/ src/ mazeworld.html tools/ test/ | wc -l` prints `0`; `node tools/shell-sweep.mjs refs wornSlots` reports 0.
    - `node --test test/unit/worn-migration.test.js 2>&1 | grep -cE "^ok .*HEDGE-0[12]"` ≥ 4 and the file reports `# fail 0`.
    - `node --test test/parity/divergence-records.test.js 2>&1 | grep -E "^# pass 2"` prints `# pass 2`; `diff <(node tools/worn-fixture-scan.mjs) tools/worn-fixture-scan-output.txt` prints nothing; `git status --porcelain test/parity/fixtures` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `grep -c "Object.freeze({ storeRoll: true })" tools/lib/tuning-bot.mjs` prints `1`; the SUMMARY contains `botMatch=true`, `stuck=0` and `runFlags={"storeRoll":true}` (`grep -c "botMatch=true" …/45-03-SUMMARY.md` prints ≥ 1).
    - `npm test 2>&1 | grep -E "^# fail 0"` prints `# fail 0`; `npm run build:www` exits 0; `git diff --stat 7447629 -- engine/ content/` lists exactly the three engine files; `git diff --stat 7447629 -- test/unit/chargen-rng-pin.test.js docs/class-pass/ docs/CLASS-PASS.md mazeworld.html` prints nothing.
    - `grep -c "^## Success criteria" …/45-03-SUMMARY.md`, `grep -c "^## Scan output"`, `grep -c "^## Moved fixtures (before/after)"`, `grep -c "^## Bot: line comparison"`, `grep -c "^## Tests re-pinned or deleted (phase total)"`, `grep -c "^## Human verification (deferred to end of run)"` each print `1`.
    - `git status --porcelain` prints nothing after the SUMMARY commit.
  </acceptance_criteria>
  <done>All five ROADMAP criteria verified verbatim with outputs in the SUMMARY; the phase closes with the scan, the moved set, the Bot: line comparison and the deferred Pixel 7 checklist recorded.</done>
</task>

</tasks>

<verification>
- Two commits (inventory + docs; closing summary), suite green at each.
- ROADMAP success criteria 1–5 verified verbatim; the smoke's readout kept out of the repo; no engine/shell/tool bytes touched by this plan.
</verification>

<success_criteria>
- FIXTURE-INVENTORY.md Phase 45 section present, roster regenerated, `fixture-inventory.test.js` + `divergence-records.test.js` green.
- GEAR-SLOTS.md describes the single path; every remaining retired-identifier mention is explicitly historical.
- Smoke: 143 cells, 429 runs, 0 stuck, `runFlags {"storeRoll":true}`, `Bot:` line equal to the v1.5 AFTER pin modulo `seeds=`.
- Closing SUMMARY with the required sections.
</success_criteria>

<output>
Create `.planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md` as specified in Task 3: `## Success criteria` (five, with outputs); `## Scan output`; `## Moved fixtures (before/after)`; `## Bot: line comparison`; `## Tests re-pinned or deleted (phase total)`; `## Declared consequences`; `## Gate outputs` (per commit); `## Human verification (deferred to end of run)`; `## Flagged assumptions` (A-1..A-8); `## Next phase readiness`.
</output>
