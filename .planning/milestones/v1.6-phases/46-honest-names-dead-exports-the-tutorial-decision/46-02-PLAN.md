---
phase: 46-honest-names-dead-exports-the-tutorial-decision
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - mazeworld.html
  - tools/store-screenshots/bot.js
  - test/unit/shell-combat-over.test.js
  - test/unit/shell-map-rail.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-map-viewport.test.js
  - engine/movement.js
  - engine/engine.js
  - engine/saveState.js
  - engine/state.js
  - engine/events.js
  - engine/death.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - src/browser/eventNarration.js
  - src/browser/narrationLines.js
  - test/unit/narrationLinesCoverage.test.js
  - test/unit/formatEventsCoverage.test.js
  - tools/lib/tuning-bot.mjs
  - tools/lib/class-matrix.mjs
  - tools/tune-difficulty.mjs
  - test/unit/movement.test.js
  - test/unit/endless-descent.test.js
  - test/unit/engine-purity.test.js
  - test/unit/permadeath.test.js
  - test/unit/effects.test.js
  - test/unit/death.test.js
  - test/unit/engineAdapter.test.js
  - test/unit/save-validation.test.js
autonomous: true
requirements: [DEAD-04]

must_haves:
  truths:
    - "grep -rnE \"winGame|state\\.won|\\.won\\b\" engine/ src/ mazeworld.html test/ tools/ test/parity/harness/ prints only the frozen prototype master and the two COMBAT_COPY.over.won lines in the shell (the FIGHT-outcome copy key, rep.over = \"won\", a live identifier that is not the run flag) — both listed in the SUMMARY as survivors with that reason; nothing else"
    - "engine/ carries no won run flag anywhere: newRun() produces no won key, serializeRun emits none, validateSave/rehydrate copy none, no move/useTool/makeCamp/abandon guard reads one, events.js defines no WON type and no won() creator, death.js writes no walked-out note"
    - "DEAD-04 (pin, not assumption): a stale save carrying won: true still loads — validateSave returns ok: true and the rehydrated state has no won key — pinned by a new test in test/unit/save-validation.test.js"
    - "The abandon guard in engine/engine.js reads dead alone: if (!next.dead) die(next, \"abandon\", null, rng, events)"
    - "The parity suite (node --test test/parity/*.test.js) passes with exactly one harness change — the top-level won destructured out beside beats/seed/rngState/version in all six comparables (three harness functions + three local test comparables) — a retired-field carve-out on the prototype side, the stripRetiredCounterFields precedent; zero fixture files move; the master hash is unchanged"
    - "EVENT_NARRATION and ORACLE_ONLY no longer carry a won entry, KNOWN_INDIRECT_TYPES in both coverage guards no longer lists it, and both guards pass (LINE_FOR 250 keys, ORACLE_ONLY 20 entries, no unreachable entry)"
    - "The shell never reads a won run flag: no won card (no 'Through the Gate' markup), no .stone.won CSS rule, no walked-out stone branch, hasActiveDelveSave/hadSaveAtLaunch/hasLiveRun/renderRail/the stair gate/the combat snapshot all read dead alone"
    - "The removal lands as two green commits in dependency order — the shell layer first (behaviour-identical while the engine flag still exists), then the engine + harness + tables + tools + tests layer — each with its own grep in the body"
  artifacts:
    - path: "engine/saveState.js"
      provides: "validateSave/rehydrate whitelists without a won line — a stale won key is dropped by construction (tolerant load)"
    - path: "test/parity/harness/comparables.js"
      provides: "movementComparable/combatComparable/economyComparable destructure the prototype-side won out with a Phase 46 retired-field comment"
      contains: "Phase 46 (DEAD-04"
    - path: "test/unit/save-validation.test.js"
      provides: "the DEAD-04 stale-save pin (won: true loads, key dropped)"
      contains: "DEAD-04"
  key_links:
    - from: "engine/engine.js"
      to: "engine/death.js"
      via: "the abandon case calls die() guarded on !next.dead only"
      pattern: "if \\(!next\\.dead\\) die\\(next, \"abandon\""
    - from: "test/parity/*.test.js"
      to: "test/parity/harness/comparables.js"
      via: "every comparable strips the prototype-side won before diffState"
      pattern: "version, won,"
  prohibitions:
    - statement: "No fixture file under test/parity/fixtures moves and test/parity/prototype-master.js.txt is never edited; if any parity test needs more than the six one-word destructure edits (a regenerated fixture, a reconcile, a master edit), the executor STOPS and reports the finding instead of declaring a divergence"
      status: unresolved
    - statement: "A stale save carrying won: true (or any won value) is never rejected — the key is dropped, the run resumes; no migration code, no legacy-key fold beyond the whitelist omission"
      status: unresolved
    - statement: "content/ is untouched (the won epitaph bank in content/epitaphs.js is content, not an engine identifier — it stays for a later sweep); no narration text other than the unreachable won entry changes"
      status: unresolved
    - statement: "The fight-outcome key won (COMBAT_COPY.over.won, rep.over, headerOver) is a live identifier for winning a FIGHT and is not renamed, rewritten or indirected to appease the grep — it is listed as a survivor"
      status: unresolved
---

<!-- planner-discipline-allow: winGame -->
<!-- planner-discipline-allow: state.won -->
<!-- planner-discipline-allow: S.won -->
<!-- planner-discipline-allow: parsed.won -->
<!-- planner-discipline-allow: after.won -->
<!-- planner-discipline-allow: stone.won -->
<!-- planner-discipline-allow: Through the Gate -->
<!-- planner-discipline-allow: btn-again -->
<!-- planner-discipline-allow: won: false -->
<!-- planner-discipline-allow: won: !!obj.won -->

<objective>
Remove `engine/movement.js#winGame()` and the `won` run flag end to end — the four `state.won` guards (`movement.js` move/useTool/makeCamp, `engine.js` abandon), the `won: false` initializer in `engine/state.js`, the `won` lines in `validateSave`/`rehydrate`, the `WON` event type + `won()` creator in `engine/events.js`, the walked-out note branch in `engine/death.js`, the `won` entries in `EVENT_NARRATION`/`ORACLE_ONLY` and the two coverage guards' `KNOWN_INDIRECT_TYPES`, every shell `S.won` read (the won card, the graveyard stone branch + CSS, the resume/title/rail/stair/snapshot checks), the tools' `state.won`/`r.won` reads, and every test pin — with a stale `won: true` save still loading (tolerant, pinned) and the parity harness gaining exactly one retired-field strip (DEAD-04; CONTEXT "DEAD-04" decisions).

**Premise correction the executor must carry into the SUMMARY (verified 2026-09-19):** DEAD-04 and ROADMAP criterion 2 speak of removing "the harness carve-out that strips `won`". No such carve-out exists — `grep -n won test/parity/harness/*.js` returns 0 lines. Today the field is compared LIVE on both sides (the frozen prototype initialises its top-level won flag to `false`; the engine did too; `diffState` reports a key-set mismatch when one side lacks a key). Removing the engine field therefore REQUIRES a prototype-side retired-field strip — one word added to the six top-level destructures — which is the `stripRetiredCounterFields` precedent (Phase 39 / 260918-w4n: "strip from WHICHEVER side carries it"). The criterion-2 grep still returns 0 (a destructured `won` carries no dot), no comparable reconciles or special-cases a won state, no fixture moves, the master is not edited. This is the finding; it is recorded, not declared as a divergence. If the parity suite needs ANYTHING beyond those six one-word edits, STOP — do not regenerate, do not declare.

Purpose: RUN-04 (endless descent) made winning unreachable in v1.0; the flag, its guards, its serialization and its narration entry have been dead code since. Every surviving read of it is a lie about the game.

Output: two commits (shell layer; engine + harness + tables + tools + tests layer), `46-02-SUMMARY.md` with the engine diff quoted in full, the fence outputs and the two survivors.

Probe rows carried by this plan: DEAD-04 unclassified → authored as the stale-save truth above plus the flagged assumption in `<verification>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-CONTEXT.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-01-SUMMARY.md
@engine/movement.js
@engine/engine.js
@engine/saveState.js
@engine/state.js
@engine/events.js
@engine/death.js
@test/parity/harness/comparables.js
@test/parity/harness/diffState.js

**Phase facts (verified 2026-09-19 at 39c5a0f; shell line numbers may drift by a few lines after 46-01's comment reword — grep the quoted anchors):**
- Engine sites: `movement.js:36` imports `won` from events.js and `:32` imports `epitaphFor, epitaphCtx` from death.js (both only for `winGame`); `winGame` L985-1002 (JSDoc from L985); the guards `movement.js:138` (move), `:559` (useTool), `:784` (makeCamp) each read `|| state.won`; `engine.js:119` abandon guard `if (!next.dead && !next.won)`; `saveState.js:644` (validateSave) and `:749` (rehydrate) each `won: !!obj.won,` inside a WHITELISTED object literal — `serializeRun` (L35) spreads state, so it needs no edit; `state.js:222` `won: false,`; `events.js:17` `WON: "won",` and `:35-36` the `won()` creator; `death.js:77` `note: state.deathNote || (cause === "won" ? "walked out" : "died"),`.
- Harness: NO won handling exists today. The six top-level destructures that must gain `won`: `test/parity/harness/comparables.js:512` (movementComparable), `:600` (combatComparable), `:1124` (economyComparable); `test/parity/combat-parity.test.js:130`; `test/parity/magic-parity.test.js:91`; `test/parity/movement-parity.test.js:69`. `diffState.js:findDivergence` returns a key-set mismatch when one side lacks a key — so without the strip every parity test fails at `$`.
- Fixture files contain no `"won"` key (`grep -lE '"won"' test/parity/fixtures/*` is empty) — the strip changes no fixture.
- Narration: `src/browser/eventNarration.js:286` `won: (e) => ...The Gate...`; `src/browser/narrationLines.js` ORACLE_ONLY entry `"won", // dedicated victory banner` (was toasts.js:201); `KNOWN_INDIRECT_TYPES = ["died", "won", ...]` in `test/unit/narrationLinesCoverage.test.js` (~L75) and `test/unit/formatEventsCoverage.test.js:55` (+ its L23 comment). After the edit: `LINE_FOR` 250 keys, `ORACLE_ONLY` 20, `EVENT_NARRATION` 269.
- Shell reads (anchors): `S.dead || S.won || S.combat` in the over-map gate (~L3162); `if (window.__mzStair && !S.combat && !S.dead && !S.won) {` (~L3947); the won card `if (S.won) {` … `Through the Gate` … `btn-again` (~L3984-3996, with its "04-DR3" comment); `railEl.hidden = !!(S.combat || S.dead || S.won) || idle;` (~L4356); `hadSaveAtLaunch = !!(parsed && !parsed.dead && !parsed.won);` (~L5329); `if (!after || after.dead || after.won) { combatSnap = null; return; }` (~L5414); `hasActiveDelveSave()` returns `!!(S && S.c && !S.dead && !S.won)` (~L5895, comment L5894 "neither dead nor won"); `hasLiveRun: !!(S && S.c && !S.dead && !S.won),` (~L6327); graveyard stone `el.className = "stone" + (g.cause === "won" ? " won" : "");` and `${g.cause === "won" ? "Walked out" : g.note...}` (~L3124/3128); CSS `.stone.won{...}` and `.stone.won .stone-note{...}` (L815/819). `tools/store-screenshots/bot.js:10` reads `S.won||`.
- Fight-outcome survivors (NOT the run flag, keep): `COMBAT_COPY.over.won` at ~L3808 and ~L4007, `headerOver: { won: ... }` / `over: { won: {...} }` at L2167/2169, `rep.over = ... "won"` at ~L4037/5441 — these key the "THEY ARE DOWN" copy by the FIGHT outcome.
- Shell test anchors on the won card: `sliceBetween(CODE, "if (S.dead) {", "if (S.won) {")` in `shell-combat-over.test.js:68` and `shell-map-rail.test.js:97`; `WON_GUARD = 'if (S.won) {'` in `shell-loot-screen.test.js:177` (used at L224); `shell-map-viewport.test.js:274` indexes the stair-guard line; `shell-map-rail.test.js:404` pins the `railEl.hidden` regex; `shell-combat-over.test.js:374-380` pins the won card body + store region. The unique next-branch anchor after the death branch is `if (!S.combat && S.beats && S.beats.groups && S.beats.groups.length) {` (one occurrence in the shell).
- Tools: `tools/lib/tuning-bot.mjs:1354,1372,1373,1381,1387`; `tools/lib/class-matrix.mjs:207`; `tools/tune-difficulty.mjs:119,150,203`. The frozen `docs/class-pass/*.json` readouts and `tools/voice-sample-output.txt` are history — never regenerated (Phase 45 ruling).
- Unit-test pins: `movement.test.js:27` import + `:741-750` the dormant winGame test + `:709-715`; `endless-descent.test.js:29,49,54`; `engine-purity.test.js:122` (name), `:153` (key list), `:159`; `permadeath.test.js:27`; `effects.test.js:380,435`; `death.test.js:192-196`; `engineAdapter.test.js:220-232` (formatEvents with a won event, expects 4 lines + "Gate"); `save-validation.test.js:104-130` and `:242-256` (old-save INPUTS carrying `won: false` — keep them, they are stale-save inputs by design).
- Roughly 20 other unit tests build hand-made states with an inert `won: false` key (abilities, afraid, combat, encounters, … and the class-matrix row literals). They match neither the criterion-2 grep (no dot) nor NAME-02's list; they are NOT in scope — count them in the SUMMARY for Phase 48.
- Baseline entering this plan: `npm test` 3,240 / fail 0 (after 46-01).
</context>

<tasks>

<task type="auto">
  <name>Task 1: shell layer — every S.won read, the won card, the graveyard stone branch and CSS, the screenshot bot; shell test anchors re-pointed (one commit, behaviour-identical)</name>
  <files>mazeworld.html, tools/store-screenshots/bot.js, test/unit/shell-combat-over.test.js, test/unit/shell-map-rail.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-map-viewport.test.js</files>
  <read_first>
    - mazeworld.html: the CSS rules at L810-822, the graveyard render at L3118-3132, the gates at L3158-3164, the renderEncounter branches L3940-4000 (stair gate, death branch, won card, beats branch), the rail L4350-4358, the boot L5322-5332, the combat snapshot L5410-5416, hasActiveDelveSave L5890-5897, the back-button spec L6323-6329, and the fight-outcome copy at L2165-2170 / L3805-3810 / L4005-4008 (to see what is NOT the run flag)
    - test/unit/shell-combat-over.test.js L60-70 and L370-382; test/unit/shell-map-rail.test.js L92-100 and L398-406; test/unit/shell-loot-screen.test.js L175-230; test/unit/shell-map-viewport.test.js L270-280
  </read_first>
  <action>
    Delete every read of the run flag from the shell while the engine still initialises it (so this commit is behaviour-identical — the flag is always false):

    1. `mazeworld.html`: in the over-map gate (~L3162) drop `S.won ||`; the stair gate (~L3947) drop `&& !S.won`; delete the whole won-card block (`if (S.won) { ... return; }` with its "Through the Gate" markup, the `btn-again` wiring and the 04-DR3 comment inside it — ~L3984-3996), leaving the "whatever just happened comes first" comment and the beats branch that follow; `railEl.hidden` (~L4356) becomes `!!(S.combat || S.dead) || idle;` and its comment "Combat/death/win still own their screens" loses "/win"; `hadSaveAtLaunch` (~L5329) becomes `!!(parsed && !parsed.dead)`; the combat snapshot (~L5414) becomes `if (!after || after.dead) { ... }` and its comment "death/win have their own cards" loses "/win"; `hasActiveDelveSave` (~L5895) returns `!!(S && S.c && !S.dead)` and its comment says "not dead"; `hasLiveRun` (~L6327) becomes `!!(S && S.c && !S.dead)`; the graveyard stone (~L3124/3128) becomes `el.className = "stone";` and `<p class="stone-note">${g.note.replace(/^\w/, c => c.toUpperCase())}</p>` (no stone can carry a walked-out cause — the engine never wrote one to a shipped graveyard; a stone's `note` already reads "walked out" if one ever existed); delete the two CSS rules `.stone.won{...}` and `.stone.won .stone-note{...}` (L815/819); the won card was the ONLY user of the `.deathcard` CSS rules (L842-844 — verify with `grep -c deathcard mazeworld.html` = 3 after the card is gone, i.e. only the CSS remains) so delete those three rules too; and drop the clause `, the won card's reroll` from the two keyboard comments at L1070 and L1233 (they describe the deleted surface). Do NOT touch `COMBAT_COPY.over.won`, `headerOver: { won: ... }`, `rep.over = ... "won"` — those key the THEY ARE DOWN copy by the FIGHT outcome and are live.
    2. `tools/store-screenshots/bot.js:10`: drop `S.won||` from the `blocked` expression.
    3. Shell test anchors: in `shell-combat-over.test.js:68` and `shell-map-rail.test.js:97` the death-branch END anchor becomes the unique beats-branch line `if (!S.combat && S.beats && S.beats.groups && S.beats.groups.length) {`; `shell-loot-screen.test.js:177` — rename `WON_GUARD` to `BEATS_GUARD` holding that same line and update its use at ~L224; `shell-map-viewport.test.js:274` indexes `if (window.__mzStair && !S.combat && !S.dead) {`; `shell-map-rail.test.js:404` regex becomes `railEl\.hidden = !!\(S\.combat \|\| S\.dead\) \|\| idle;`; `shell-combat-over.test.js:374-380` — retitle to "Phase 46: the retired won card is gone and the store region is untouched", replace the two won-branch assertions with `assert.doesNotMatch(CODE, /Through the Gate/)` and `assert.equal((CODE.match(/if \(S\.dead\) \{/g) || []).length, 1)`, keep the store-region assertions.
    4. Gates: `grep -nE "S\.won|parsed\.won|after\.won|stone\.won|Through the Gate" mazeworld.html tools/store-screenshots/bot.js` → nothing; `grep -nE "\.won\b" mazeworld.html` → exactly the two `COMBAT_COPY.over.won` lines (record them); `npm test` fail 0 (3,240); `npm run build:www`; `npm run boot:check` 4 PASS. Commit as `refactor(46-02): shell no longer reads the retired won run flag — won card, stone branch + CSS, resume/title/rail/stair/snapshot checks read dead alone (DEAD-04, shell layer)` with the grep line and the two survivors in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && ! grep -qE "S\.won|parsed\.won|after\.won|stone\.won|Through the Gate" mazeworld.html tools/store-screenshots/bot.js && [ "$(grep -cE '\.won\b' mazeworld.html)" = "2" ] && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "S\.won|parsed\.won|after\.won|stone\.won|Through the Gate|btn-again" mazeworld.html tools/store-screenshots/bot.js` prints nothing.
    - `grep -nE "\.won\b" mazeworld.html` prints exactly 2 lines, both containing `COMBAT_COPY.over.won`.
    - `grep -c "deathcard" mazeworld.html` prints 0; `grep -c "won card" mazeworld.html` prints 0.
    - `grep -cE "railEl\.hidden = !!\(S\.combat \|\| S\.dead\) \|\| idle;" mazeworld.html` prints 1; `grep -cE "hadSaveAtLaunch = !!\(parsed && !parsed\.dead\);" mazeworld.html` prints 1; `grep -cE "return !!\(S && S\.c && !S\.dead\);" mazeworld.html` prints 1.
    - `node --test test/unit/shell-combat-over.test.js test/unit/shell-map-rail.test.js test/unit/shell-loot-screen.test.js test/unit/shell-map-viewport.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3240` and `# fail 0`; `npm run build:www` exits 0; `npm run boot:check 2>&1 | grep -c PASS` prints 4.
    - `git diff --stat HEAD~1 -- engine/ content/ src/` prints nothing (this commit is shell + tools + shell tests only); `git status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>The shell and the screenshot bot never read a won run flag; the won card, stone branch and CSS are gone; the fight-outcome copy key survives untouched; shell pins re-pointed; one green commit.</done>
</task>

<task type="auto">
  <name>Task 2: engine + harness + narration tables + tools + unit tests — winGame and the won flag removed, stale saves tolerant, one retired-field strip (one commit)</name>
  <files>engine/movement.js, engine/engine.js, engine/saveState.js, engine/state.js, engine/events.js, engine/death.js, test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, src/browser/eventNarration.js, src/browser/narrationLines.js, test/unit/narrationLinesCoverage.test.js, test/unit/formatEventsCoverage.test.js, tools/lib/tuning-bot.mjs, tools/lib/class-matrix.mjs, tools/tune-difficulty.mjs, test/unit/movement.test.js, test/unit/endless-descent.test.js, test/unit/engine-purity.test.js, test/unit/permadeath.test.js, test/unit/effects.test.js, test/unit/death.test.js, test/unit/engineAdapter.test.js, test/unit/save-validation.test.js</files>
  <read_first>
    - engine/saveState.js L505-526 (foldLegacyCounters — the tolerant-load idiom: retired keys are read once and deleted, never rejected) and L636-650 / L740-755 (the whitelisted validateSave/rehydrate objects: dropping a line IS the tolerant load — the key is simply never copied)
    - engine/movement.js L30-37 and L980-1002; engine/engine.js L110-121; engine/state.js L215-230; engine/events.js L10-37; engine/death.js L60-80
    - test/parity/harness/comparables.js L130-155 (stripFlightFields + stripRetiredCounterFields — the retired-field carve-out precedent and its comment wording) and the three destructures at L512 / L600 / L1124; test/parity/movement-parity.test.js L40-70, combat-parity.test.js L105-131, magic-parity.test.js L68-92
    - test/parity/harness/diffState.js L73-115 (key-set mismatch semantics — why the strip is required)
    - test/unit/narrationLinesCoverage.test.js L70-80 and L151-163; test/unit/formatEventsCoverage.test.js L20-25 and L55
    - the unit-test lines listed in Phase facts
  </read_first>
  <action>
    1. **engine/** (the only engine edit of the phase — six files, all `won`):
       - `movement.js`: delete `winGame` and its JSDoc (L985-1002); the import from `./death.js` becomes `{ die }` and from `./events.js` becomes `{ moved, floorChanged }`; drop `|| state.won` from the three guards (move L138, useTool L559, makeCamp L784) so each reads `if (state.combat || state.store || state.dead) return events;`.
       - `engine.js:119`: `if (!next.dead) die(next, "abandon", null, rng, events);` and the comment above it says "already over (dead)".
       - `saveState.js`: delete the `won: !!obj.won,` line in validateSave (L644) and in rehydrate (L749); the adjacent `dev:`/`storeRoll:` comments that say "like dead/won" say "like dead". No fold, no read-back: the objects are whitelists, so a stale `won` key is dropped by construction — that IS the tolerant load (state it in a one-line comment where the line was).
       - `state.js:222`: delete `won: false,`; the `dev:` comment below says "exactly like dead above".
       - `events.js`: delete `WON: "won",` from EVENT_TYPES and the `won()` creator with its JSDoc (L35-36).
       - `death.js:77`: `note: state.deathNote || "died",`.
       - Comment sweep inside these six files only (the comment goes with the flag): every `dead/won` phrase becomes `dead` and every `dev/dead/won` becomes `dev/dead` (`engine.js:118`, `movement.js:549`, `saveState.js:645/648/661/740/750/753/762`, `state.js:224`); after this `grep -rnwE "won|WON" engine/` prints nothing. No other comment edits in engine/.
    2. **Harness — the one retired-field strip:** add `won` to the six top-level destructures, right after `version` (`const { beats, seed, rngState, version, won, ...` in comparables.js L512/L600/L1124 and the local comparables in combat-parity.test.js L130, magic-parity.test.js L91, movement-parity.test.js L69). Above the comparables.js movementComparable destructure add a comment headed `Phase 46 (DEAD-04, retired-field carve-out — the stripRetiredCounterFields precedent):` explaining that the frozen prototype still initialises its top-level won flag to false at every fixture replay and no fixture ever wins, the engine no longer carries the field, so the prototype-side literal is stripped and the key-set compare is a no-op difference; the two other harness sites and the three local comparables cite that comment in one line each. Write the comment WITHOUT a dot before the word won (the criterion-2 grep matches a dotted form).
    3. **Narration tables + guards:** delete the `won:` entry in `src/browser/eventNarration.js` (~L286) and the `"won", // dedicated victory banner` entry from ORACLE_ONLY in `src/browser/narrationLines.js`; remove `"won"` from `KNOWN_INDIRECT_TYPES` in `test/unit/narrationLinesCoverage.test.js` and `test/unit/formatEventsCoverage.test.js` (and from the L23 comment there).
    4. **Tools:** `tools/lib/tuning-bot.mjs` — the loop guard becomes `while (!state.dead && actions < ctx.opts.maxActions)`, `stuck` becomes `!state.dead && actions >= ctx.opts.maxActions`, `outcome` becomes `state.dead ? "dead" : stuck ? "stuck" : "unknown"`, delete the `won: state.won,` result field, `cause` becomes `state.deathNote || (actions >= ctx.opts.maxActions ? "maxActionsHit" : "unknown")`; `tools/lib/class-matrix.mjs:207` delete `won: run.won,`; `tools/tune-difficulty.mjs` delete `wonCount` (L119), print `Outcome: ${deadCount} dead, ${stuckCount} stuck (...)` (L150), delete the `won:` JSON field (L203); the `outcome` JSDoc at `tuning-bot.mjs:1323` lists `"dead" | "stuck" | "unknown"` (the comment goes with the field). Never regenerate `docs/class-pass/*.json` or `tools/voice-sample-output.txt`.
    5. **Unit tests:** `movement.test.js` — drop `winGame` from the import (L27), delete the dormant winGame test (L741-750), and at L715 assert `!("won" in state)` with the message "the run carries no won flag" (reword the L709 comment to "never marks the run as won"); `endless-descent.test.js` L29/49/54, `permadeath.test.js:27`, `engine-purity.test.js:159` — each becomes `assert.ok(!("won" in state), ...)`; `engine-purity.test.js:153` drop `"won"` from the required-shape key list and reword the L122 test name to say "already dead"; `effects.test.js` L380/435 drop `|| state.won` / `|| s.won`; `death.test.js:192-196` — retitle to "bury falls back to a plain died note when deathNote is empty", build `fixedState({ deathNote: "", epitaph: "Rest in pieces." })`, call `bury(state, "trap", null, [], () => 1)`, expect `graves[0].note === "died"`; `engineAdapter.test.js:220-232` — drop the won event from the input array, expect `html.length === 3`, drop the "Gate" assertion, reword the message; `save-validation.test.js` — ADD a test named `DEAD-04: a stale save carrying won: true loads tolerantly — ok, and the rehydrated state has no won key` that spreads the existing old-save literal (L111-119) with `won: true`, asserts `validateSave(JSON.stringify(save), { freshSeed: 777 }).ok === true`, `!("won" in check.value)`, and `!("won" in rehydrate(check.value))`; leave the two existing `won: false` inputs as they are (stale-save inputs).
    6. **Gates (the STOP rule):** run `node --test test/parity/*.test.js` FIRST after steps 1-2. It must pass with only the six one-word destructure edits. If any parity test still fails, do NOT regenerate a fixture, do NOT touch the master, do NOT add a reconcile — stop, record the failing site and the divergence path in the SUMMARY under `## BLOCKER`, and end the plan. Then: `git status --porcelain test/parity/fixtures` → empty; `git hash-object test/parity/prototype-master.js.txt` → `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git diff --stat 39c5a0f -- engine/` lists exactly movement.js, engine.js, saveState.js, state.js, events.js, death.js; `git diff --stat 39c5a0f -- content/` empty; the criterion-2 grep (below); `npm test` fail 0 (expect 3,240 — one dormant winGame test deleted, one stale-save pin added); `npm run build:www`; `npm run boot:check` 4 PASS. Commit as `refactor(46-02): remove winGame and the won run flag — guards, serialize/validate/rehydrate, events, death note, narration entries, tools, tests; stale won:true saves load tolerantly; prototype-side won stripped in the six comparables (DEAD-04)` with the grep line and the survivors in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && ! grep -rE "winGame|state\.won|\.won\b" engine/ src/ tools/ test/parity/harness/ | grep -qv "prototype-master" ; node --test test/parity/*.test.js 2>&1 | grep -E "^# fail" && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "winGame|state\.won|\.won\b" engine/ src/ mazeworld.html test/ tools/ test/parity/harness/ | grep -vE "prototype-master\.js\.txt|COMBAT_COPY\.over\.won"` prints nothing; `grep -rnE "winGame|state\.won|\.won\b" engine/ src/ mazeworld.html test/ tools/ test/parity/harness/ | grep -c "COMBAT_COPY\.over\.won"` prints 2.
    - `grep -rnwE "won|WON" engine/` prints nothing (no won identifier, event type or note anywhere under engine/).
    - `grep -cE "if \(!next\.dead\) die\(next, \"abandon\", null, rng, events\);" engine/engine.js` prints 1; `grep -cE "\|\| state\.won" engine/movement.js` prints 0; `grep -cE "^import \{ die \} from \"\./death\.js\";" engine/movement.js` prints 1; `grep -cE "^import \{ moved, floorChanged \} from \"\./events\.js\";" engine/movement.js` prints 1.
    - `grep -cE "version, won," test/parity/harness/comparables.js` prints 3; `grep -cE "version, won," test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js` prints 1 for each; `grep -cE "Phase 46 \(DEAD-04" test/parity/harness/comparables.js` is at least 1.
    - `node --test test/parity/*.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git status --porcelain test/parity/fixtures` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `git diff --stat 39c5a0f -- engine/ | grep -cE "engine/(movement|engine|saveState|state|events|death)\.js"` prints 6 and `git diff --stat 39c5a0f -- engine/ | grep -c "engine/"` prints 6 (no seventh engine file); `git diff --stat 39c5a0f -- content/` prints nothing.
    - `grep -cE "\"won\"" src/browser/eventNarration.js src/browser/narrationLines.js test/unit/narrationLinesCoverage.test.js test/unit/formatEventsCoverage.test.js` prints 0 for each; `grep -cE "^\s*won:" src/browser/eventNarration.js` prints 0.
    - `node --test test/unit/narrationLinesCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/save-validation.test.js test/unit/movement.test.js test/unit/endless-descent.test.js test/unit/engine-purity.test.js test/unit/permadeath.test.js test/unit/effects.test.js test/unit/death.test.js test/unit/engineAdapter.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `grep -c "DEAD-04" test/unit/save-validation.test.js` is at least 1.
    - `grep -nE "\bwon\b" tools/lib/tuning-bot.mjs tools/lib/class-matrix.mjs tools/tune-difficulty.mjs | grep -vE "won'"` prints nothing (the only surviving word is the contraction in an unrelated comment at tuning-bot.mjs:399).
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3240` and `# fail 0`; `npm run build:www` exits 0; `npm run boot:check 2>&1 | grep -c PASS` prints 4.
    - `git status --porcelain` prints nothing after the commit; `git log --oneline 39c5a0f..HEAD | grep -vcE "^[0-9a-f]+ docs\("` prints 4 (two from 46-01, two from this plan; SUMMARY docs commits excluded by the filter).
  </acceptance_criteria>
  <done>winGame and the won run flag are gone from engine, narration tables, tools and tests; a stale won: true save loads with the key dropped (pinned); the parity suite passes with exactly the six-destructure strip; no fixture moved; master hash unchanged; one green commit with its grep.</done>
</task>

<task type="auto">
  <name>Task 3: fence proof and 46-02-SUMMARY.md — the engine diff quoted, the premise correction, survivors, test deltas, deferred human checks</name>
  <files>.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md (section shapes)
    - $HOME/.claude/gsd-core/templates/summary.md
  </read_first>
  <action>
    Re-run and paste verbatim: the criterion-2 grep with the two survivor lines and their reason (fight-outcome copy key); `git diff 39c5a0f -- engine/` IN FULL (it is small — every hunk is a won/winGame/WON/walked-out/import line; this is the "engine diff limited to the won removal" proof); `git diff 39c5a0f -- test/parity/harness/comparables.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js` (the six one-word strips + the comment); `git status --porcelain test/parity/fixtures` (empty); `git hash-object test/parity/prototype-master.js.txt`; `node --test test/parity/*.test.js` pass/fail lines; `npm test` / `build:www` / `boot:check` lines at each of the two commits; `git log --oneline 39c5a0f..HEAD`.

    Sections by name: `## Premise correction (DEAD-04 "harness carve-out")` — quote `grep -n won test/parity/harness/*.js` at 39c5a0f (0 lines), explain the live compare + key-set mismatch, name the `stripRetiredCounterFields` precedent, state that the intent of criterion 2 ("the comparables carry no won handling") is met in substance — no reconcile, no special case, one retired-field strip — and that the grep is 0; `## Survivors of the criterion-2 grep` (the two `COMBAT_COPY.over.won` lines with line numbers); `## Engine diff (the won removal only)`; `## Tests re-pinned or deleted` (table: the dormant winGame test deleted, the stale-save pin added, the death/engineAdapter/engine-purity/endless-descent/permadeath/effects/movement re-pins, the five shell anchors, the two coverage-guard KNOWN_INDIRECT_TYPES edits); `## Out of scope, counted for Phase 48` (the N hand-built test states with an inert `won: false` key, listed by file — `grep -rlE "won: false" test/unit | wc -l`); `## Human verification (deferred to end of run)` — (1) a save from the current Play build (which serialized `won: false`) resumes on the Pixel 7 and plays on (the dropped key), (2) THEY ARE DOWN still shows after a won fight (the fight-outcome key is untouched), (3) the graveyard renders every stone with its note.

    `requirements-completed: [DEAD-04]`. Commit as `docs(46-02): complete winGame/won removal plan`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test -f .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-02-SUMMARY.md && grep -cE "^## (Premise correction|Survivors of the criterion-2 grep|Engine diff|Tests re-pinned or deleted|Out of scope|Human verification)" .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## (Premise correction|Survivors of the criterion-2 grep|Engine diff|Tests re-pinned or deleted|Out of scope|Human verification)" 46-02-SUMMARY.md` prints 6.
    - The SUMMARY contains `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `# fail 0`, `# pass 3240`, `stripRetiredCounterFields`, and a `diff --git a/engine/movement.js` line (the quoted engine diff).
    - `grep -c "COMBAT_COPY.over.won" 46-02-SUMMARY.md` is at least 2.
  </acceptance_criteria>
  <done>46-02-SUMMARY.md is the record: premise correction, the full engine diff, the six-strip harness diff, fence outputs, survivors, test deltas, the Phase 48 count and the deferred device checks.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- No new module. Deleted: `engine/movement.js#winGame`, `EVENT_TYPES.WON` + `won()` in `engine/events.js`, the `won` initializer/serialization/guards, the `won` narration entries, the shell won card + `.stone.won` CSS.
- New test: the DEAD-04 stale-save pin in `test/unit/save-validation.test.js`.
- Harness: `won` destructured out (prototype side) in the three `*Comparable()` functions and the three local test comparables, with a Phase 46 retired-field comment.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage → validateSave/rehydrate | untrusted JSON (a stale or crafted save) enters the whitelisted rebuild; unknown/retired keys are dropped, never rejected |
| graveyard JSON → DOM | `renderGraves` inserts `g.note`/`g.epitaph` via innerHTML (pre-existing surface; this plan removes a branch, adds none) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-03 | Tampering | validateSave / rehydrate whitelist | medium | mitigate | a crafted `won: true` (or any won value) is never copied into state — pinned by the DEAD-04 stale-save test; no code path reads the key |
| T-46-04 | Denial of service | parity harness key-set compare | medium | mitigate | the prototype-side strip is one word in six sites, proven by the full parity suite at the commit; the STOP rule forbids any silent fixture regeneration |
| T-46-05 | Information disclosure | graveyard stone note | low | accept | the removed branch only substituted a literal; `g.note` was already rendered for every other cause |
| T-46-SC | Tampering | npm installs | low | accept | no package installed or upgraded; package.json untouched |
</threat_model>

<verification>
- ROADMAP criterion 2 holds: the criterion grep returns 0 outside the frozen master and the two enumerated fight-outcome survivors; `saveState.js` neither serializes nor validates `won`; a stale `won: true` save loads (pinned); the abandon guard reads `dead` alone; no comparable reconciles or special-cases a won state (one documented retired-field strip).
- Engine-gate fence: `git diff --stat 39c5a0f -- engine/` is exactly the six won files; `content/` untouched; zero fixture moves; master hash unchanged.
- Flagged assumption (DEAD-04 unclassified probe row): no parity fixture, bot run or shipped save ever reached a won state — every prototype-side replay carries the flag as `false`, so the retired-field strip is a no-op difference. Evidence: the parity suite passes with only the six destructure edits; `grep -lE '"won"' test/parity/fixtures/*` is empty. If the suite needed more, the STOP rule fires.
</verification>

<success_criteria>
- Two green commits (shell layer; engine + harness + tables + tools + tests), each with its grep in the body.
- `npm test` 3,240 / fail 0; `build:www` exit 0; `boot:check` 4 PASS at both commits.
- 46-02-SUMMARY.md carries the premise correction, the full engine diff, the harness diff, the survivors and the deferred device checks.
</success_criteria>

<output>
Create `.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-02-SUMMARY.md` when done (sections per Task 3).
</output>
