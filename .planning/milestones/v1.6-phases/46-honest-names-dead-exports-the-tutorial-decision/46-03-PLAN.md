---
phase: 46-honest-names-dead-exports-the-tutorial-decision
plan: 03
type: execute
wave: 3
depends_on: ["46-02"]
files_modified:
  - src/browser/settings.js
  - test/unit/settings.test.js
  - test/unit/shell-gear-toolbar.test.js
  - src/browser/tutorial.js
  - test/unit/tutorial.test.js
  - test/unit/icons.test.js
  - src/browser/icons.js
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [NAME-02, DEAD-05]

must_haves:
  truths:
    - "src/browser/settings.js has exactly four fields — sound, haptics, textSize, confirmBeforeQuit — in SETTINGS_DEFAULTS and ALLOWED_VALUES; no identifier or string in src/, engine/, content/, tools/ or the shell spells the retired control-scheme setting key or its dpad value (NAME-02 D-pad row)"
    - "A persisted settings blob carrying the retired control-scheme key (with either old value) loads tolerantly: readSettings returns the four defaults merged with the blob's recognised keys, the retired key is absent from the result, writeSetting for that key is a no-op — pinned by a test that builds the retired key from string fragments (the shell-map-invariants RETIRED-map idiom), so no grep over test/ spells it either"
    - "src/browser/tutorial.js and test/unit/tutorial.test.js do not exist; test/unit/icons.test.js exists (git mv from tutorial.test.js) and carries exactly the 15 icons.js tests that file held; src/browser/icons.js's header cites icons.test.js; grep -rn tutorial src/ engine/ content/ tools/ mazeworld.html test/ prints nothing (DEAD-05)"
    - "The decision is recorded in three places: a PROJECT.md Key Decisions row, the PROJECT.md Active-requirements onboarding row, and the REQUIREMENTS.md UX-06 backlog row — each stating the 04-era sequencer was deleted in Phase 46 and UX-06 is rebuilt from scratch on the Phase 47 modular shell"
    - "No storage-key migration or reservation exists for the tutorial-seen key: it was never written by a shipped build (the overlay was never wired), so nothing reads, folds or reserves it"
    - "Each deletion is its own green commit with its grep: the control-scheme setting (commit 5 of the phase), then tutorial.js + the test move + the docs notes (commit 6)"
  artifacts:
    - path: "src/browser/settings.js"
      provides: "the four-field settings model with tolerant read (unrecognised keys dropped)"
    - path: "test/unit/icons.test.js"
      provides: "the 15 icons.js pins (FEATURE_ICONS, PLAYER_MARKER_ICON, featureKeyForCell, drawFeatureIcon rotation/scale)"
      min_lines: 120
    - path: ".planning/PROJECT.md"
      provides: "the DEAD-05 Key Decisions row"
      contains: "Phase 46 deleted the 04-era sequencer"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the UX-06 backlog note"
      contains: "Phase 46 deleted the 04-era sequencer"
  key_links:
    - from: "src/browser/settings.js#readSettings"
      to: "SETTINGS_DEFAULTS"
      via: "only keys present in SETTINGS_DEFAULTS are merged from the persisted blob (the tolerant read — unchanged mechanism, one fewer key)"
      pattern: "for \\(const key of Object\\.keys\\(SETTINGS_DEFAULTS\\)\\)"
    - from: "src/browser/icons.js"
      to: "test/unit/icons.test.js"
      via: "header comment cites the pin file"
      pattern: "test/unit/icons\\.test\\.js"
  prohibitions:
    - statement: "No test file is deleted: tutorial.test.js is MOVED to icons.test.js and only its ten tutorial-sequencer/tutorial-seen tests are removed; the fifteen icons.js tests survive verbatim"
      status: unresolved
    - statement: "No legacy reservation, migration or tolerant read is added for the tutorial-seen storage key, and no alias for the deleted control-scheme setting is kept — old blobs simply drop the key"
      status: unresolved
    - statement: "No visible UI changes — there is no control for the deleted setting and no tutorial overlay was ever wired; engine/, content/, fixtures and the master hash are untouched"
      status: unresolved
---

<!-- planner-discipline-allow: controlScheme -->
<!-- planner-discipline-allow: dpad -->
<!-- planner-discipline-allow: tutorial -->
<!-- planner-discipline-allow: tutorial.js -->
<!-- planner-discipline-allow: tutorial.test.js -->
<!-- planner-discipline-allow: tutorialSeen -->
<!-- planner-discipline-allow: handedness -->

<objective>
Two deletions, two commits: (1) the `controlScheme` setting (`src/browser/settings.js:44` default `"dpad"`, `:55` allowed `["tap","dpad"]`) — tap-to-move has been the only movement surface since v1.4/v1.5, no UI control exists for it, and it is the last D-pad identifier in the shipped surface (NAME-02's `dpad|d-pad` row); old persisted blobs carrying the key load tolerantly and drop it (the Phase 33 `handedness` precedent, `settings.test.js:126-145`). (2) `src/browser/tutorial.js` (98 lines, zero references) and its ten tests — the module encodes the 04-era UI (fixed steps on a D-pad map; `04-10-PLAN.md` is already marked re-plan-don't-execute) and UX-06 is rebuilt from scratch on the Phase 47 modular shell — with the decision recorded in PROJECT.md (Key Decisions + the onboarding Active row) and the REQUIREMENTS.md UX-06 backlog row (DEAD-05, user-accepted 2026-09-19). `test/unit/tutorial.test.js` is NOT deleted wholesale: fifteen of its twenty-five tests pin `src/browser/icons.js` (FEATURE_ICONS, featureKeyForCell, drawFeatureIcon) — the file is `git mv`'d to `test/unit/icons.test.js` and only the ten tutorial tests go (the CONTEXT's "delete tutorial.test.js (277 lines)" scouting missed this; recorded).

Purpose: every surviving module and setting is named for what the game does today; the one parked module has a recorded decision instead of a question.

Output: two commits; `46-03-SUMMARY.md`.

Probe rows carried by this plan: DEAD-05 unclassified → authored as the "does not exist / icons tests survive" truth plus the flagged assumption in `<verification>`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-CONTEXT.md
@src/browser/settings.js
@test/unit/settings.test.js
@src/browser/tutorial.js
@test/unit/tutorial.test.js
@src/browser/icons.js

**Phase facts (verified 2026-09-19 at 39c5a0f):**
- `settings.js`: `SETTINGS_DEFAULTS` L34-46 (the `controlScheme: "dpad"` line L44 sits under a 6-line device-review comment L38-43), `ALLOWED_VALUES` L51-57 (L55), header comments say "five persisted UX settings" (L3), "All five fields" (L16), "never five separate keys" (L18), and `readSettings`'s JSDoc says "the full six-field settings object" (L63). `readSettings` merges only `Object.keys(SETTINGS_DEFAULTS)` from the blob (L79-83) — so removing the key IS the tolerant load; `writeSetting` rejects unknown keys (L98). The shell reads `settings.textSize` only (L1876/L5178) — nothing reads the control scheme.
- `settings.test.js`: L49-55 (defaults test spells the key), L57-77 (5-field round-trip), L105-107 (invalid-value branch for the key), L126-145 (the Phase 33 handedness tolerant-load test — the pattern to copy; it also pins the 5-key list at L141-142). `shell-gear-toolbar.test.js:195-208` pins the 5-key order.
- `dpad`/`controlScheme` identifier hits outside comments in the shipped surface: only `settings.js:44` and `:55`. The shell's `D-pad` mentions (L404 CSS comment, L3168/4029/4624/5171/5216 JS comments) are comments — Phase 48 (DOCS-01). `shell-map-invariants.test.js` and `shell-map-viewport.test.js` already prove the shell carries zero `dpad` literals.
- `tutorial.js` exports (L21-92): `TUTORIAL_SEEN_KEY = "mazeworld.tutorialSeen"`, `COACH_MARK_COPY_MAX_CHARS`, `COACH_MARK_STEPS`, `makeTutorialSequencer`, `getTutorialSeen`, `setTutorialSeen`. Referenced only by `test/unit/tutorial.test.js` (import L23-31) and one comment in `src/browser/icons.js:9` ("see test/unit/tutorial.test.js"). `www/src/browser/tutorial.js` is a build copy (git-ignored; `build:www` wipes `www/` first).
- `tutorial.test.js` (277 lines, 25 tests): tutorial tests at L65, 71, 80, 90, 99 (sequencer), 107, 113, 126 (tutorial-seen / storage), 134, 143 (COACH_MARK_STEPS) = 10; icons tests from L156 to the end = 15. Helpers: `makeFakeCtx` (L35-46, icons tests only) and `withFakeLocalStorage` (L47-62, tutorial tests only); `flushStorage` import L31 (tutorial tests only). Its header comment L1-19 describes the 04-03 TDD RED/GREEN origin.
- PROJECT.md anchors: `## Key Decisions` table at L218 (rows are `| Decision | Rationale | Outcome |`); the onboarding Active row at L53 `- [ ] Player **onboarding/tutorial** (first-run coach marks, 04-10 / UX-06) — deliberately LAST, once the UI settles`; the existing related row at L248 (UIF-04 dropped). REQUIREMENTS.md L54: `- **UX-06** first-run tutorial (rebuilt on the modular shell — the reason SHELL-01..03 exist)`.
- Baseline entering this plan: `npm test` 3,240 / fail 0 (after 46-02).
</context>

<tasks>

<task type="auto">
  <name>Task 1: delete the controlScheme setting — four-field model, tolerant read of old blobs pinned with fragment-built literals (one commit)</name>
  <files>src/browser/settings.js, test/unit/settings.test.js, test/unit/shell-gear-toolbar.test.js</files>
  <read_first>
    - src/browser/settings.js L1-30 (header prose that counts fields), L34-57, L59-84 (readSettings), L86-104 (writeSetting)
    - test/unit/settings.test.js L40-145 (every pin that spells the key; the Phase 33 handedness tolerant-load test at L126-145 is the pattern)
    - test/unit/shell-gear-toolbar.test.js L195-208
    - test/unit/shell-map-invariants.test.js L73-80 (the fragment-built RETIRED literal idiom)
  </read_first>
  <action>
    `src/browser/settings.js`: delete L38-44 (the device-review comment block and the `controlScheme: "dpad",` default) and L55 (the allowed-values row); reword the header counts — "five persisted UX settings" → "four", "All five fields" → "All four fields", "never five separate keys" → "never four separate keys" — and `readSettings`'s JSDoc "full six-field" → "full four-field". Nothing else changes: `readSettings` already merges only `SETTINGS_DEFAULTS` keys, which is the tolerant load (an old blob's retired key is never copied; the next `writeSetting` rewrites the blob without it).

    `test/unit/settings.test.js`: L49-55 — retitle to "SETTINGS_DEFAULTS: the four fields' defaults" and drop the control-scheme assertion; L57-77 — retitle "each of the 4 fields round-trips", drop the control-scheme write and the expected key; L105-107 — delete the three control-scheme lines; L141-143 — the expected key list becomes `["sound", "haptics", "textSize", "confirmBeforeQuit"]` and the length 4. ADD, after the handedness test, `Phase 46 (NAME-02): a stored control-scheme key is ignored silently` — build the retired key and value from fragments (`const RETIRED_KEY = "control" + "Scheme"; const RETIRED_VALUE = "dp" + "ad";`), store `JSON.stringify({ textSize: "S", [RETIRED_KEY]: RETIRED_VALUE })` under `SETTINGS_STORAGE_KEY`, assert `readSettings()` deep-equals `{ ...SETTINGS_DEFAULTS, textSize: "S" }`, `RETIRED_KEY in settings` is false, and `await writeSetting(RETIRED_KEY, "tap")` returns an object deep-equal to the current settings with no such key; also assert the settings source (`fs.readFileSync` of settings.js) does not contain `RETIRED_KEY` or `RETIRED_VALUE` (the handedness pin's `doesNotMatch` shape). Do this so `grep -rnE "controlScheme|dpad" test/` also returns 0.

    `test/unit/shell-gear-toolbar.test.js:195-208`: the expected key list drops the retired key (4 entries) and the test title says "exactly 4 fields".

    Gates: `grep -rnE "controlScheme|dpad" src/ engine/ content/ tools/ mazeworld.html test/` → nothing; `node --test test/unit/settings.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-viewport.test.js` fail 0; `npm test` fail 0 (expect 3,241 — one tolerant-load pin added); `npm run build:www`; `npm run boot:check` 4 PASS (settings.js loads at boot). Commit as `refactor(46-03): delete the controlScheme setting — four-field settings model, old blobs drop the key on read (NAME-02, D-pad row)` with the grep line in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && ! grep -rqE "controlScheme|dpad" src/ engine/ content/ tools/ mazeworld.html test/ && node --test test/unit/settings.test.js test/unit/shell-gear-toolbar.test.js 2>&1 | grep -E "^# fail" && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "controlScheme|dpad" src/ engine/ content/ tools/ mazeworld.html test/` prints nothing.
    - `grep -cE "^\s*(sound|haptics|textSize|confirmBeforeQuit):" src/browser/settings.js` prints 8 (four defaults + four allowed rows) and `grep -cE "^\s*[a-zA-Z]+: (true|\"|\[)" src/browser/settings.js` prints 8 (no fifth field in either table).
    - `grep -cE "Phase 46 \(NAME-02\)" test/unit/settings.test.js` is at least 1; `grep -cE "\"control\" \+ \"Scheme\"" test/unit/settings.test.js` prints 1.
    - `node --test test/unit/settings.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-viewport.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`.
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3241` and `# fail 0`; `npm run build:www` exits 0; `npm run boot:check 2>&1 | grep -c PASS` prints 4.
    - `git diff --stat HEAD~1 -- engine/ content/ mazeworld.html` prints nothing; `git status --porcelain` prints nothing.
  </acceptance_criteria>
  <done>settings.js is a four-field model; an old blob with the retired key loads with the key dropped (pinned without spelling it); the 5-key pins are re-pinned to 4; one green commit with its grep.</done>
</task>

<task type="auto">
  <name>Task 2: delete tutorial.js, move tutorial.test.js → icons.test.js keeping the 15 icons pins, record the DEAD-05 decision in PROJECT.md and REQUIREMENTS.md (one commit)</name>
  <files>src/browser/tutorial.js, test/unit/tutorial.test.js, test/unit/icons.test.js, src/browser/icons.js, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - test/unit/tutorial.test.js L1-64 (header, imports, the two helpers), L64-155 (the ten tutorial tests to remove), L156-277 (the fifteen icons tests to keep)
    - src/browser/icons.js L1-16 (header; L9 cites the test file)
    - .planning/PROJECT.md L50-56 (the onboarding Active row), L218-222 (Key Decisions header + row shape), L248 (the UIF-04 row — the nearest precedent)
    - .planning/REQUIREMENTS.md L50-56 (the UX-06 backlog row)
  </read_first>
  <action>
    1. `git rm src/browser/tutorial.js`.
    2. `git mv test/unit/tutorial.test.js test/unit/icons.test.js`; in the moved file delete the `tutorial.js` import block (L23-30), the `flushStorage` import (L31), the `withFakeLocalStorage` helper (L47-62), and the ten tutorial tests (from the `// --- sequencer transitions` divider at ~L64 through the second `COACH_MARK_STEPS` test ending ~L155) — keep `makeFakeCtx` and every icons test verbatim; rewrite the header comment (L1-19) as `// test/unit/icons.test.js` — pins for src/browser/icons.js: FEATURE_ICONS / PLAYER_MARKER_ICON / featureKeyForCell / drawFeatureIcon rotation + scale; note in one line that the file was moved from the 04-era tutorial test in Phase 46 when the coach-mark sequencer was deleted (DEAD-05) so history follows. `src/browser/icons.js:9`: the citation becomes `test/unit/icons.test.js`.
    3. `.planning/PROJECT.md`: (a) append a Key Decisions row (table at L218): Decision — "`src/browser/tutorial.js` (the 04-era coach-mark sequencer) deleted; UX-06 first-run tutorial rebuilt from scratch on the Phase 47 modular shell — Phase 46, 2026-09-19 (user)"; Rationale — "the module encoded the 04-era UI (fixed steps on a D-pad map; `04-10-PLAN.md` already marked re-plan-don't-execute), had zero references, and its storage key was never written by a shipped build (the overlay was never wired) — no migration, no legacy-key reservation"; Outcome — "✓ deleted with its ten sequencer/seen tests; the fifteen icons.js pins it also held moved to `test/unit/icons.test.js`; PROJECT/REQUIREMENTS UX-06 rows annotated"; (b) the onboarding Active row (L53) gains " — rebuilt from scratch on the modular shell (Phase 46 deleted the 04-era sequencer)". `.planning/REQUIREMENTS.md:54`: the UX-06 row becomes `- **UX-06** first-run tutorial — rebuilt from scratch on the modular shell (Phase 46 deleted the 04-era sequencer; the reason SHELL-01..03 exist)`. Use Edit for both files (scoped replacements only).
    4. Gates: `grep -rnE "tutorial" src/ engine/ content/ tools/ mazeworld.html test/` → nothing; `test -f src/browser/tutorial.js` fails; `test -f test/unit/tutorial.test.js` fails; `node --test test/unit/icons.test.js` → `# tests 15`, fail 0; `npm test` fail 0 (expect 3,231 — ten tutorial tests removed); `npm run build:www` (then `test -f www/src/browser/tutorial.js` fails — the build wipes www/ first); `npm run boot:check` 4 PASS. Commit as `refactor(46-03): delete src/browser/tutorial.js (04-era coach-mark sequencer, zero references) — tutorial tests removed, icons pins moved to test/unit/icons.test.js, UX-06 rebuilds on the modular shell (DEAD-05)` including the two .planning files, with the grep line in the body.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && ! test -f src/browser/tutorial.js && ! test -f test/unit/tutorial.test.js && ! grep -rqE "tutorial" src/ engine/ content/ tools/ mazeworld.html test/ && node --test test/unit/icons.test.js 2>&1 | grep -E "^# (tests|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/tutorial.js` exits 1; `test -f test/unit/tutorial.test.js` exits 1; `test -f test/unit/icons.test.js` exits 0; `git log --follow --oneline -- test/unit/icons.test.js | wc -l` is greater than 1.
    - `grep -rnE "tutorial" src/ engine/ content/ tools/ mazeworld.html test/` prints nothing.
    - `node --test test/unit/icons.test.js 2>&1 | grep -E "^# tests"` prints `# tests 15` and `grep -E "^# fail"` prints `# fail 0`; `grep -cE "^test\(\"icons\.js|^test\(\"featureKeyForCell|^test\(\"drawFeatureIcon" test/unit/icons.test.js` prints 15.
    - `grep -c "test/unit/icons.test.js" src/browser/icons.js` prints 1.
    - `grep -c "Phase 46 deleted the 04-era sequencer" .planning/PROJECT.md` prints at least 1 and `grep -c "Phase 46 deleted the 04-era sequencer" .planning/REQUIREMENTS.md` prints 1; `grep -cE "^\| .*tutorial\.js.*deleted.*Phase 46" .planning/PROJECT.md` prints 1 (the Key Decisions row).
    - `npm test 2>&1 | grep -E "^# (pass|fail)"` prints `# pass 3231` and `# fail 0`; `npm run build:www` exits 0 and `test -f www/src/browser/tutorial.js` exits 1; `npm run boot:check 2>&1 | grep -c PASS` prints 4.
    - `git diff --stat HEAD~1 -- engine/ content/ mazeworld.html` prints nothing; `git status --porcelain` prints nothing; `git log --oneline 39c5a0f..HEAD | grep -vcE "^[0-9a-f]+ docs\("` prints 6 (SUMMARY docs commits excluded by the filter).
  </acceptance_criteria>
  <done>tutorial.js and its ten tests are gone, the fifteen icons pins live in icons.test.js with history, icons.js cites the new file, the decision is recorded in PROJECT.md (two places) and REQUIREMENTS.md; one green commit with its grep.</done>
</task>

<task type="auto">
  <name>Task 3: 46-03-SUMMARY.md — both greps, the test move record, the docs notes, deferred human checks</name>
  <files>.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/45-collapse-the-phase-37-hedges/45-03-SUMMARY.md (section shapes)
    - $HOME/.claude/gsd-core/templates/summary.md
  </read_first>
  <action>
    Paste verbatim: the two grep commands with "(empty)"; `git log --oneline 39c5a0f..HEAD` (six commits); the per-commit `npm test` / `build:www` / `boot:check` lines; `git diff --stat 39c5a0f -- engine/ content/` (six engine files from 46-02, nothing else — content empty); `git hash-object test/parity/prototype-master.js.txt`; `git status --porcelain test/parity/fixtures` (empty).

    Sections by name: `## The tutorial decision (DEAD-05)` — deleted, with the rationale and the three places it is recorded; `## Test file moved, not deleted` — `git mv test/unit/tutorial.test.js test/unit/icons.test.js`, ten tutorial tests removed (list their titles), fifteen icons tests kept, why (the CONTEXT scouting counted the file as tutorial-only); `## Tests re-pinned or deleted` (settings 5→4 re-pins, the new fragment-built tolerant-load pin, the gear-toolbar key list); `## NAME-02 allowed survivors (this plan)` — none (the shell's D-pad comment mentions are Phase 48's); `## Storage keys` — the retired settings key is dropped on read/rewritten away on the next write, no code names it; the tutorial-seen key was never written by a shipped build, nothing reserves it; `## Human verification (deferred to end of run)` — (1) on the Pixel 7, Settings still shows sound / haptics / text size / confirm-before-quit and each persists across a relaunch (four-field model), (2) nothing else is user-visible.

    `requirements-completed: [DEAD-05]` (NAME-02 closes in 46-04 with the recorded grep). Commit as `docs(46-03): complete controlScheme + tutorial.js deletion plan`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test -f .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-03-SUMMARY.md && grep -cE "^## (The tutorial decision|Test file moved, not deleted|Tests re-pinned or deleted|NAME-02 allowed survivors|Storage keys|Human verification)" .planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-03-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^## (The tutorial decision|Test file moved, not deleted|Tests re-pinned or deleted|NAME-02 allowed survivors|Storage keys|Human verification)" 46-03-SUMMARY.md` prints 6.
    - The SUMMARY contains `# pass 3231`, `# fail 0`, `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `git mv test/unit/tutorial.test.js test/unit/icons.test.js`, and both grep commands each followed by `(empty)`.
  </acceptance_criteria>
  <done>46-03-SUMMARY.md records both deletions, the test move, the decision's three recording places, the storage-key stance and the deferred device check.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (this plan's share)

- `test/unit/icons.test.js` — git mv from `test/unit/tutorial.test.js`; 15 icons.js pins.
- Deleted: `src/browser/tutorial.js`; the `controlScheme` setting (default + allowed row) in `src/browser/settings.js`.
- New test: the fragment-built tolerant-load pin for the retired settings key in `test/unit/settings.test.js`.
- Docs: PROJECT.md Key Decisions row + onboarding row note; REQUIREMENTS.md UX-06 row note.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| storage → readSettings | an untrusted/old JSON blob; only recognised keys with allowed values are merged (unchanged mechanism, one fewer key) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-06 | Tampering | settings blob with the retired key or an unknown value | low | mitigate | `readSettings` merges only `SETTINGS_DEFAULTS` keys with allowed values; pinned by the new fragment-built test and the existing T-04-04 pins |
| T-46-07 | Denial of service | boot with a stale settings blob | low | mitigate | `readSettings` never throws (corrupt JSON → defaults, pinned); `boot:check` 4 PASS at the commit |
| T-46-SC | Tampering | npm installs | low | accept | no package installed or upgraded |
</threat_model>

<verification>
- ROADMAP criterion 4 holds: `tutorial.js` deleted together with its tests and the icons.js comment, with the note in PROJECT.md Key Decisions and the UX-06 backlog row (REQUIREMENTS.md) and the onboarding Active row; the choice is recorded in the SUMMARY.
- NAME-02 D-pad row: zero `controlScheme|dpad` identifier hits in src/, engine/, content/, tools/, the shell — and in test/ (fragment-built pin).
- Flagged assumption (DEAD-05 unclassified probe row): no shipped build ever wrote the `mazeworld.tutorialSeen` storage key — the coach-mark overlay was never wired into the shell (Phase 33 research: zero references; UIF-04 dropped for the same reason). Consequence: no migration or reservation. If a device ever shows a stale value under that key it is inert — nothing reads it.
- Engine-gate fence: `git diff --stat 39c5a0f -- engine/` still lists only 46-02's six files; `content/` untouched; zero fixture moves; master hash unchanged.
</verification>

<success_criteria>
- Two green commits, each with its grep; `npm test` 3,231 / fail 0; `build:www` exit 0; `boot:check` 4 PASS.
- `icons.test.js` carries 15 tests; `tutorial.js`/`tutorial.test.js` absent; the decision recorded in three places.
</success_criteria>

<output>
Create `.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-03-SUMMARY.md` when done (sections per Task 3).
</output>
