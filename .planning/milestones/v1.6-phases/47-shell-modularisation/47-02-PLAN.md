---
phase: 47-shell-modularisation
plan: 02
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/browser/bridge.js
  - test/unit/bridge-registry.test.js
  - tools/ident-sweep.mjs
  - tools/bridge-doc.mjs
  - docs/SHELL-MODULES.md
  - mazeworld.html
autonomous: true
requirements: [SHELL-04]

must_haves:
  truths:
    - "SHELL-04 (empty): test/unit/bridge-registry.test.js fails on an empty registry map (every live name reported as unlisted) and on a name the shell or a module defines but the map lacks (a synthetic `window.__mzNotListed = 1` source is reported unlisted) — both proven fail-first inside the test with synthetic inputs, never against the real files"
    - "SHELL-04 (adjacency): `__mzTabs` will be ONE registry name (one frozen object with gear/hero/store keys), never three — the registry test asserts no live name matches ^__mz(Gear|Hero|Store)(Tab|Screen)$ from this plan on, and Plan 03 adds the single `__mzTabs` entry"
    - "SHELL-04 (ordering): Object.keys(BRIDGE) is alphabetically sorted (asserted) and the set-equality check compares sorted arrays, so grep order and file order never matter"
    - "Every window.__mz* name that appears in comment-stripped mazeworld.html or src/browser/*.js (bridge.js excluded from the scan) is a key of BRIDGE with a non-empty owner, a consumers array and a purpose; a stale key (listed but defined nowhere) fails the test"
    - "docs/SHELL-MODULES.md's ## Module bridge table is generated from BRIDGE by tools/bridge-doc.mjs and the registry test asserts the table's name column equals bridgeNames()"
    - "A window.__mz* name with zero readers anywhere (measured: __mzBags) is deleted, not registered with an empty consumer list"
  artifacts:
    - "src/browser/bridge.js — export const BRIDGE (frozen), export function bridgeNames()"
    - "test/unit/bridge-registry.test.js — set-equality + teeth + sorted-keys + entry-shape + doc-sync"
    - "tools/ident-sweep.mjs — now exports { stripJs, stripHtml }; main() runs only when invoked directly"
    - "tools/bridge-doc.mjs — prints/writes/checks the markdown bridge table from BRIDGE"
    - "docs/SHELL-MODULES.md — the module contract + the generated ## Module bridge section"
  key_links:
    - "bridge-registry.test.js -> tools/ident-sweep.mjs#stripHtml/stripJs (comment-stripped scan, so a comment mentioning a retired name is never a false live name)"
    - "bridge-registry.test.js -> src/browser/bridge.js#BRIDGE (the source of truth) and -> docs/SHELL-MODULES.md (the generated table between the bridge-table markers)"
    - "Plans 03-05 -> BRIDGE: every carve that adds or removes a window.__mz* name edits BRIDGE in the same commit or this test goes red"
  prohibitions:
    - statement: "MUST NOT register a bridge name with an empty consumers list to keep it alive — a name nobody reads is deleted in the same commit"
      requirement_id: SHELL-04
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT keep an old shell helper or window.__mz* bridge alive once its module owns the behaviour — no shims, no dual paths"
      requirement_id: SHELL-04
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT change any rendered DOM or text of the Gear tab, Hero tab or Store — the Plan 01 fixtures stay byte-equal through this plan"
      requirement_id: SHELL-04
      category: safety
      status: unverified
      verification: test
---

<objective>
List every `window.__mz*` bridge name in one place — `src/browser/bridge.js`, a frozen map of `{ owner, consumers, purpose }` per name — enforced by `test/unit/bridge-registry.test.js` (set-equality between the map's keys and a comment-stripped `__mz\w+` scan over `mazeworld.html` + `src/browser/*.js`), and generate the `## Module bridge` section of the new `docs/SHELL-MODULES.md` from that map. Delete the one bridge that already has zero readers (`__mzBags`).

Purpose: ROADMAP Phase 47 criterion 4 / SHELL-04 ("the module bridge surface is listed in one place with each entry's owner; a test fails when the shell or any module defines a `__mz*` name the registry does not list"). Landing this BEFORE the carves means every bridge Plans 03-05 add or retire is caught by a live test in the same commit.

Output: `src/browser/bridge.js`, `test/unit/bridge-registry.test.js`, `tools/bridge-doc.mjs`, `docs/SHELL-MODULES.md`, `tools/ident-sweep.mjs` (exports), `mazeworld.html` (one dead assignment removed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-shell-modularisation/47-CONTEXT.md
@.planning/phases/46-honest-names-dead-exports-the-tutorial-decision/46-04-SUMMARY.md
@tools/ident-sweep.mjs
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: __mzBags -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt` (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **Windows executor environment:** Git Bash, no `jq`; heredocs and `node -e` one-liners mangle backslashes — multi-line scripts are written to files with the Write tool.
- **Line numbers drift:** anchor by function name / marker string; numbers below were measured at HEAD `0129ca3`.
- **Verification agents are off:** the acceptance criteria ARE the verification.
- **Measured at planning (comment-stripped scan over mazeworld.html + src/browser/*.js):** 55 unique `__mz*` names; `__mzBags` (module script, `window.__mzBags = BAGS;`) has zero readers; three names live only in src/browser (test-injection hooks: `__mzAppImportOverride` in nativeChrome.js, `__mzHapticsImportOverride` in haptics.js, `__mzPreferencesOverride` in storage.js — consumers are test files under test/). The CONTEXT's "53" was a pre-count; the test computes the live set.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: ident-sweep exports + src/browser/bridge.js + the registry test (with __mzBags deleted)</name>
  <files>tools/ident-sweep.mjs, src/browser/bridge.js, test/unit/bridge-registry.test.js, mazeworld.html</files>
  <read_first>
    - tools/ident-sweep.mjs (whole file: `stripJs`, `stripHtmlComments`, `stripHtml`, `collectFiles`, `main()` — note `main()` is currently called unconditionally at the bottom)
    - mazeworld.html: every code line that assigns or reads a `window.__mz*` name — enumerate with `node tools/ident-sweep.mjs "__mz\w+"` (prints every comment-stripped code line per file with its line number; exit 1 is expected because n > 0) and, per name, `node tools/ident-sweep.mjs "__mzName\b"` to see its assignment site(s) and readers
    - src/browser/nativeChrome.js (`__mzAppImportOverride`), src/browser/haptics.js (`__mzHapticsImportOverride`), src/browser/storage.js (`__mzPreferencesOverride`) and their test consumers: `grep -rln "__mzAppImportOverride\|__mzHapticsImportOverride\|__mzPreferencesOverride" test/`
    - test/unit/shell-no-content-copies.test.js (header/comment style of a source-pin suite; the `MZ_HTML` override precedent — NOT needed here because the teeth tests use synthetic inputs)
  </read_first>
  <action>
    1. **tools/ident-sweep.mjs** — make the strippers importable without running the CLI: add `export { stripJs, stripHtml };` and replace the bare trailing `main();` with a direct-invocation guard (`process.argv[1]` resolved with `path.resolve` equals `url.fileURLToPath(import.meta.url)` -> call `main()`; otherwise do nothing). Update the head comment's Usage block with one line: "importable: `import { stripJs, stripHtml } from \"../../tools/ident-sweep.mjs\"` (test/unit/bridge-registry.test.js)". `--self-test` and every documented CLI behaviour stay byte-identical.

    2. **mazeworld.html** — delete the dead `window.__mzBags = BAGS;` assignment from the module script (measured: its only occurrence is the assignment). If `BAGS` then has no other use in the module script's import line, drop `BAGS` from that `content/index.js` import too (`node tools/ident-sweep.mjs "\bBAGS\b"` must show zero mazeworld.html lines afterwards; engine/src hits are fine). Record the deletion in the SUMMARY.

    3. **src/browser/bridge.js** — `export const BRIDGE = Object.freeze({ ... })` with one entry per live name, keys in alphabetical order, each entry frozen: `owner` (exactly one of `"mazeworld.html (classic)"`, `"mazeworld.html (module)"`, or a `src/browser/<file>.js` path — the script/file that assigns or declares the name; for a state-carrying global assigned by both scripts — `__mzFightLog`, `__mzCombatMenu`, `__mzFightEnd`, `__mzStair`, `__mzRail`, `__mzLootReport`, `__mzPendingNarration` — the owner is the script that INITIALISES it, and the other script appears in consumers with "(also writes)"), `consumers` (array of strings naming the reading side: `"mazeworld.html (classic: <function>)"`, `"mazeworld.html (module: <function>)"`, `"src/browser/<file>.js"` or a test path for the three injection hooks), `purpose` (one sentence, present tense, no phase-number archaeology). Export `bridgeNames()` returning `Object.keys(BRIDGE)` sorted. Head comment: this map is the SOURCE OF TRUTH for SHELL-04; adding/removing a `window.__mz*` anywhere requires editing it in the same commit; `tools/bridge-doc.mjs --write` regenerates the doc table.

    4. **test/unit/bridge-registry.test.js** — `import { stripJs, stripHtml } from "../../tools/ident-sweep.mjs"`, `import { BRIDGE, bridgeNames } from "../../src/browser/bridge.js"`. Implement `liveBridgeNames(sources)` where `sources` is `[{ path, text, kind }]` -> a sorted array of unique `__mz\w+` matches over the stripped text (`stripHtml` for kind html, `stripJs` for js). Default sources: `mazeworld.html` (html) + every `src/browser/*.js` (js) EXCEPT `bridge.js` (its keys are the names themselves — scanning it would make every stale key look live). Implement `diffNames(live, keys)` -> `{ unlisted: live - keys, stale: keys - live }`.
       Tests: (a) set-equality: `diffNames(liveBridgeNames(defaultSources()), bridgeNames())` has empty `unlisted` AND empty `stale`, with the failure message listing both arrays; (b) every entry has a non-empty string `owner` matching the allowed forms, a `consumers` array with at least one non-empty string, a non-empty `purpose`; (c) ordering: `Object.keys(BRIDGE)` deep-equals its sorted copy; (d) teeth — empty map: `diffNames(liveBridgeNames(defaultSources()), [])` reports every live name unlisted (length equals the live count, which must be >= 50); (e) teeth — unlisted definition: `liveBridgeNames([{ path: "scratch.js", text: "window.__mzNotListed = 1;", kind: "js" }])` equals `["__mzNotListed"]` and `diffNames` against `bridgeNames()` reports it unlisted; (f) teeth — stripper: a source whose ONLY mention is inside a `//` comment or an HTML comment yields no live name; (g) adjacency: no live name matches `/^__mz(Gear|Hero|Store)(Tab|Screen)$/` (a tab surface is reached through the single `__mzTabs` object, Plan 03); (h) the three injection hooks' consumers are test paths that exist on disk (`fs.existsSync`).
       Head comment: SHELL-04's gate; why bridge.js is excluded from the scan; why the scan is comment-stripped (Phase 46's ident-sweep precedent).
  </action>
  <verify>
    <automated>node tools/ident-sweep.mjs --self-test && node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)" && node tools/ident-sweep.mjs "__mzBags\b"; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/ident-sweep.mjs --self-test` prints `self-test: PASS`
    - `node -e "import('./tools/ident-sweep.mjs').then(m => console.log(typeof m.stripJs, typeof m.stripHtml))"` prints `function function` and nothing else (no usage error, exit 0 — main() no longer runs on import)
    - `grep -cE "^export \{ stripJs, stripHtml \};" tools/ident-sweep.mjs` prints 1
    - `test -f src/browser/bridge.js` exit 0; `grep -cE "^export const BRIDGE = Object\.freeze\(" src/browser/bridge.js` prints 1; `grep -cE "^export function bridgeNames\(" src/browser/bridge.js` prints 1
    - `node -e "import('./src/browser/bridge.js').then(m => { const k = m.bridgeNames(); console.log(k.length, JSON.stringify(k) === JSON.stringify([...k].sort()), Object.isFrozen(m.BRIDGE)) })"` prints `<N> true true` with N >= 50 (record N in the SUMMARY; measured expectation 54 = 55 live names minus the deleted `__mzBags`)
    - `node tools/ident-sweep.mjs "__mzBags\b"` prints `ident-sweep __mzBags\b: 0` and exits 0
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `grep -E "^# pass"` prints a number >= 8
    - `grep -c "__mzNotListed" test/unit/bridge-registry.test.js` prints a number >= 2 (the fail-first teeth case exists)
    - `grep -cE "bridge\.js" test/unit/bridge-registry.test.js` prints a number >= 2 (import + scan exclusion)
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` (the Plan 01 fixtures are untouched: `git diff --stat HEAD -- test/unit/fixtures/` empty)
  </acceptance_criteria>
  <done>bridge.js lists every live `__mz*` name with owner/consumers/purpose; the registry test proves set-equality and its own teeth; `__mzBags` is gone; ident-sweep is importable and its CLI unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: tools/bridge-doc.mjs + docs/SHELL-MODULES.md (generated ## Module bridge) + doc-sync pin + gates</name>
  <files>tools/bridge-doc.mjs, docs/SHELL-MODULES.md, test/unit/bridge-registry.test.js</files>
  <read_first>
    - src/browser/bridge.js (Task 1)
    - .planning/phases/47-shell-modularisation/47-CONTEXT.md `<decisions>` — the module contract (`renderGearTab/renderHeroTab/renderStoreScreen(host, state, deps)`, no window reads in modules, `window.__mzTabs` assigned before the first paint, what stays in viewModels.js, the drop shelf stays shared, the store owns its whole `S.store` branch)
    - docs/RELEASING.md (house style for a docs/ page: short sections, commands in fences)
  </read_first>
  <action>
    1. **tools/bridge-doc.mjs** (node built-ins + `import { BRIDGE } from "../src/browser/bridge.js"`): `renderTable()` returns a markdown table `| Name | Owner | Consumers | Purpose |` with one row per key in sorted order (consumers joined with `<br>`). CLI: no args -> print the table; `--write` -> replace everything between the marker lines `<!-- bridge-table:start -->` and `<!-- bridge-table:end -->` in `docs/SHELL-MODULES.md` with the table (markers kept, error if a marker is missing); `--check` -> exit 1 with a one-line message when the doc's table differs from the generated one. Direct-invocation guard like ident-sweep; export `renderTable` for the test.

    2. **docs/SHELL-MODULES.md** — sections, in this order:
       - `# Shell modules` — one paragraph: `mazeworld.html` is a mount point; the Gear tab, Hero tab and Store render from `src/browser/gearTab.js`, `heroTab.js`, `storeScreen.js` (landing in Plans 03-05; this doc is written first so the contract is fixed before the carves).
       - `## Contract` — `renderGearTab(host, state, deps)`, `renderHeroTab(host, state, deps)`, `renderStoreScreen(host, state, deps)`: `host` is the mount element (`#screen-gear`, `#screen-hero`, `#enc-body`), `state` is the engine state (`S`), `deps` is the object the classic script's `tabDeps()` builds — list the keys: `guardTap`, `useItem`, `equipItem`, `unequip`, `dropItem`, `sellItem`, `takeLoot`, `leaveLoot`, `buyItem`, `leaveStore`, `dismissJoiner`, `castSpell` — each an action closure over the matching `window.mz*` bridge. Rules: a module reaches the page only through `host`, `host.ownerDocument` and `deps` (never `window`/`document` globals); a module imports content/engine/viewModels directly; `paint()` makes exactly one call per surface (`window.__mzTabs.gear(...)`, `window.__mzTabs.hero(...)`), `renderEncounter()`'s store branch is one `window.__mzTabs.store(...)` call; `window.__mzTabs = Object.freeze({ gear, hero, store })` is assigned by the module script before the first `paint()`; the shared carried-item list (`renderCarriedList`, exported by gearTab.js) is reached by the classic loot card through `window.__mzCarriedList`.
       - `## What stays shared` — `src/browser/viewModels.js` keeps `armorDisplay`, `bagArmorText`, `USABLE_COPY`, `usableBy`, `lootCompare`, `dropShelfItems`, `oracleLogViewModel`; `renderDropShelf` stays in the shell (LOOT and FIND cards); the encounter overlay host/frame stays in the shell.
       - `## Module bridge` — one sentence ("generated from `src/browser/bridge.js` by `node tools/bridge-doc.mjs --write`; `test/unit/bridge-registry.test.js` fails when this table and the map disagree, or when the shell/modules define a name the map lacks") then the two marker lines with the generated table between them, then "How to add a bridge name: assign it, add its entry to `BRIDGE`, run `--write`, commit all three together."
       - `## DOM snapshots` — the harness (`test/unit/harness/recordingDom.js`, `shellSandbox.js`), the standing test, the fixtures dir, the rule: fixtures are regenerated only with `MZ_SNAPSHOT_UPDATE=1` for a declared, deliberate DOM change with rationale in the commit message — never to make a carve pass.
       - `## Line budget` — placeholder line "Filled by Plan 05: final `wc -l mazeworld.html` and the per-carve deltas."
       Run `node tools/bridge-doc.mjs --write` to fill the table.

    3. **test/unit/bridge-registry.test.js** — add test (i): read `docs/SHELL-MODULES.md`, take the lines between the two markers, parse the `| Name |` column of every data row (skip the header and separator rows), and assert the sorted names deep-equal `bridgeNames()`; add test (j): `renderTable()` from tools/bridge-doc.mjs contains one row per key.

    4. Gates: `npm run build:www` then `npm test`, `npm run boot:check`, fence. Commit (`feat(47-02): window.__mz* bridge registry + test + generated doc`) — one commit for Tasks 1-2 is acceptable if Task 1 was not committed separately; otherwise two.
  </action>
  <verify>
    <automated>node tools/bridge-doc.mjs --check && node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)" && grep -c "bridge-table:start" docs/SHELL-MODULES.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/bridge-doc.mjs` exit 0; `node tools/bridge-doc.mjs --check` exits 0
    - `test -f docs/SHELL-MODULES.md` exit 0; `grep -c "^## Module bridge" docs/SHELL-MODULES.md` prints 1; `grep -c "bridge-table:start" docs/SHELL-MODULES.md` prints 1; `grep -c "bridge-table:end" docs/SHELL-MODULES.md` prints 1
    - `grep -cE "^## (Contract|What stays shared|DOM snapshots|Line budget)" docs/SHELL-MODULES.md` prints 4
    - `grep -c "renderGearTab(host, state, deps)" docs/SHELL-MODULES.md` prints a number >= 1; `grep -c "__mzTabs" docs/SHELL-MODULES.md` prints a number >= 2
    - `awk '/bridge-table:start/{f=1;next} /bridge-table:end/{f=0} f' docs/SHELL-MODULES.md | grep -c "^| __mz"` prints the same number as `node -e "import('./src/browser/bridge.js').then(m => console.log(m.bridgeNames().length))"`
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `grep -E "^# pass"` prints a number >= 10
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass"` prints a number greater than the Plan 01 total (record it)
    - `npm run boot:check` prints four PASS lines
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `git diff --stat HEAD~2 -- test/unit/fixtures/` prints nothing (no fixture touched in this plan)
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The doc's bridge table is generated from the map and pinned equal to it; the contract the carves implement is written down; all gates green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's slice (the full phase list is in 47-01-PLAN.md and 47-05-PLAN.md):

- `src/browser/bridge.js` — `BRIDGE` (frozen registry: one entry per live `window.__mz*` name with `owner`, `consumers`, `purpose`), `bridgeNames()`
- `test/unit/bridge-registry.test.js` — set-equality, entry shape, sorted keys, fail-first teeth (empty map, unlisted definition, comment-only mention), adjacency, doc-sync
- `tools/ident-sweep.mjs` — exports `stripJs`/`stripHtml`; CLI only when run directly
- `tools/bridge-doc.mjs` — generates / writes / checks the `## Module bridge` table
- `docs/SHELL-MODULES.md` — contract, shared-stays list, generated bridge table, snapshot rule, line-budget placeholder
- `mazeworld.html` — `window.__mzBags` (dead) deleted
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classic script <-> module script | `window.__mz*` globals are the only seam between the two scripts; the registry makes that seam enumerable |
| tests -> src/browser modules | the three injection hooks (`__mz*Override`) let tests replace native imports; they are listed with their test consumers, never reachable from shipped code paths without a test setting them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-03 | Tampering | window.__mz* global surface | medium | mitigate | every name is registry-listed with an owner; an unlisted name (a new global sneaking in) fails bridge-registry.test.js |
| T-47-04 | Information disclosure | docs/SHELL-MODULES.md | low | accept | the doc lists internal bridge names only (no secrets, no user data); the app is offline and ships no docs/ |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 47 |
</threat_model>

<verification>
- `node --test test/unit/bridge-registry.test.js` -> `# fail 0`
- `node tools/bridge-doc.mjs --check` exit 0
- `npm test` `# fail 0`; `npm run build:www` exit 0; `npm run boot:check` four PASS; snapshot test green with untouched fixtures; fence empty
</verification>

<success_criteria>
- One registry (`src/browser/bridge.js`) lists every live `window.__mz*` name with owner + consumers; a test fails on an unlisted or stale name and proves it fail-first with synthetic inputs
- `docs/SHELL-MODULES.md` carries the contract and a table generated from the map, pinned equal to it
- `__mzBags` deleted; no other shell behaviour changed (snapshot fixtures byte-equal)
</success_criteria>

<output>
Create `.planning/phases/47-shell-modularisation/47-02-SUMMARY.md` when done. It MUST carry: the registry count (N), the owner split (classic / module / src/browser), the `__mzBags` deletion evidence (`ident-sweep` output), the registry test's pass count, the doc `--check` output, `npm test` totals, `boot:check` output, the fence outputs, and a `## Human verification (deferred to end of run)` section stating nothing user-visible changed.
</output>
