---
phase: 47-shell-modularisation
plan: 03
type: execute
wave: 3
depends_on: ["47-01", "47-02"]
files_modified:
  - src/browser/gearTab.js
  - src/browser/viewModels.js
  - src/browser/combatMenu.js
  - src/browser/bridge.js
  - mazeworld.html
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/gearTab.test.js
  - test/unit/itemRowState.test.js
  - test/unit/gear-panels.test.js
  - test/unit/lootCompare.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/shell-clarity-43.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-gear-toolbar.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-spells-40.test.js
  - test/unit/shell-armor-display.test.js
  - test/unit/shell-fight-gate.test.js
  - test/unit/shell-input-guards.test.js
  - test/unit/shell-company-panel.test.js
autonomous: true
requirements: [SHELL-01, SHELL-04]

must_haves:
  truths:
    - "SHELL-01 (unclassified -> truth): after this plan the Plan 01 Gear fixtures (thief.gear, thief.gear-confirms, mu.gear) AND the Hero/Store fixtures compare byte-equal under a plain `node --test test/unit/shell-tab-snapshots.test.js` — no fixture file changes in this plan's commits"
    - "paint() contains no Gear render body: exactly one `window.__mzTabs.gear(document.getElementById(\"screen-gear\"), S, tabDeps())` call, and the classic script declares no renderCarriedList / wornSlotRow / drop-or-swap confirm state"
    - "src/browser/gearTab.js reads no window/document global (comment-stripped scan) — it reaches the page through host, host.ownerDocument and deps only"
    - "Every element id gearTab.js writes lives inside the #screen-gear markup section (structural pin in test/unit/gearTab.test.js)"
    - "SHELL-04 (adjacency): `__mzTabs` is one registry name; the registry test stays green with `__mzTabs` and `__mzCarriedList` added and the six bridges whose only readers moved (`__mzGear`, `__mzItemRowState`, `__mzWornSlots`, `__mzWornKeysOf`, `__mzSlotFor`, `__mzSellPrice`) deleted in the same commit — verified live, not assumed"
    - "Every pin that read moved Gear code is re-homed to gearTab.js (ledger in the SUMMARY); zero test files deleted, zero assertions deleted"
  artifacts:
    - "src/browser/gearTab.js — renderGearTab(host, state, deps), renderCarriedList(container, state, items, opts, deps), bagUsage, GEAR_COPY, emptySlotRows, ITEM_STATE_COPY, itemRowState"
    - "test/unit/gearTab.test.js — the module's source-pin suite (module pins + shell mount pins)"
    - "mazeworld.html — tabDeps(), the gear mount call, window.__mzTabs (gear only for now) + window.__mzCarriedList assigned before the first paint"
  key_links:
    - "paint() -> window.__mzTabs.gear(host, S, tabDeps()) -> gearTab.js#renderGearTab -> renderCarriedList (BAG rows)"
    - "renderEncounter() loot card (classic) -> window.__mzCarriedList(...) -> gearTab.js#renderCarriedList (guard:true rows through deps.guardTap)"
    - "src/browser/combatMenu.js -> gearTab.js#itemRowState (import re-pointed; the ITEMS submenu rows keep the one row-state rule)"
    - "test/unit/harness/shellSandbox.js#wireBridges -> the real gearTab.js exports (the harness is the module script's twin)"
  prohibitions:
    - statement: "MUST NOT change any rendered DOM or text of the Gear tab — the Plan 01 fixtures are the lock; MZ_SNAPSHOT_UPDATE is never set in this plan"
      requirement_id: SHELL-01
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT read window/document globals inside gearTab.js — host, host.ownerDocument and deps only"
      requirement_id: SHELL-01
      category: values
      status: unverified
      verification: test
    - statement: "MUST NOT keep the classic renderCarriedList, the confirm-state trio, canRead, or a bridge with zero readers alive after gearTab.js owns them"
      requirement_id: SHELL-04
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete a source pin — re-home every failing assertion to gearTab.js or the new mount call"
      requirement_id: SHELL-01
      category: values
      status: unverified
      verification: judgment
---

<objective>
Carve the Gear tab out of `mazeworld.html` into `src/browser/gearTab.js`: first the tab-only view models (`bagUsage`, `GEAR_COPY`, `emptySlotRows`, `ITEM_STATE_COPY`, `itemRowState`) move with their tests (a pure move, one commit), then the render bodies — the ON YOU / ALSO ON YOU / BAG writes from `paint()` and the shared carried-item list `renderCarriedList` with its drop/swap confirms — move behind `renderGearTab(host, state, deps)` and `paint()` makes exactly one mount call through `window.__mzTabs.gear(...)`. Old classic helpers and bridges whose only readers moved are deleted in the same commit; the registry and the snapshot harness follow.

Purpose: SHELL-01 — the Gear tab renders from a named module with its own source-pin suite; `mazeworld.html` only mounts it. Pixel-identical by construction: the Plan 01 fixtures must compare byte-equal after the carve.

Output: `src/browser/gearTab.js`, `test/unit/gearTab.test.js`, a slimmer `viewModels.js`, the shell's `tabDeps()` + mount call, updated `bridge.js`/doc/harness, re-homed pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-shell-modularisation/47-CONTEXT.md
@docs/SHELL-MODULES.md
@src/browser/viewModels.js
@src/browser/combatPanel.js
@src/browser/bridge.js
@test/unit/harness/shellSandbox.js
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->
<!-- planner-discipline-allow: renderCarriedList -->
<!-- planner-discipline-allow: __mzGear -->
<!-- planner-discipline-allow: __mzItemRowState -->
<!-- planner-discipline-allow: __mzWornSlots -->
<!-- planner-discipline-allow: __mzWornKeysOf -->
<!-- planner-discipline-allow: __mzSlotFor -->
<!-- planner-discipline-allow: __mzSellPrice -->
<!-- planner-discipline-allow: canRead -->
<!-- planner-discipline-allow: revertDropConfirm -->
<!-- planner-discipline-allow: revertSwapConfirm -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt` (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`). Modules import engine/content — they never edit them.
- **No behaviour change:** the committed BEFORE fixtures are the lock. If the snapshot test goes red after a move, fix the module, never the fixture. `MZ_SNAPSHOT_UPDATE` is not set anywhere in this plan.
- **Greenfield ruling:** no shim keeping an old global alive once its module owns it; `paint()` makes exactly one call per surface; old shell helpers are deleted in the same commit their module replaces them, with `node tools/shell-sweep.mjs refs <name>` proving 0 references.
- **Locked view-model split (CONTEXT, user-accepted):** the five Gear exports move even though `itemRowState` and `bagUsage` also have non-Gear readers (measured: `combatMenu.js` imports `itemRowState`; the loot card, the rail find card and the store read `window.__mzBagUsage`). Honour the list: those readers re-point their import/bridge to gearTab.js; the `__mzBagUsage` bridge stays (it still has classic readers) and is imported from gearTab.js.
- **Windows executor environment:** Git Bash, no `jq`; multi-line scripts go in files via the Write tool. Line numbers below were measured at `0129ca3` and WILL have drifted — anchor by the function names/marker strings given.
- **Verification agents are off:** the acceptance criteria ARE the verification.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: move the Gear-tab view models to src/browser/gearTab.js (pure move, tests follow)</name>
  <files>src/browser/gearTab.js, src/browser/viewModels.js, src/browser/combatMenu.js, mazeworld.html, test/unit/harness/shellSandbox.js, test/unit/itemRowState.test.js, test/unit/gear-panels.test.js, test/unit/lootCompare.test.js, test/unit/hp-not-wp.test.js, test/unit/shell-clarity-43.test.js, test/unit/shell-loot-screen.test.js</files>
  <read_first>
    - src/browser/viewModels.js: the import block (lines 9-14), the private helpers (lines 21-100 — `skillTableFor`, `damageBracket`, `nextLevelValue`, `snarkLine`, `quirkText` serve characterSheetViewModel and STAY), and the five exports with their doc comments: `bagUsage` (grep `^export function bagUsage`), `GEAR_COPY` (`^export const GEAR_COPY`), `emptySlotRows`, `ITEM_STATE_COPY`, `itemRowState` — note which imported names each body uses (`bagCap`, `canStow`, `slotItems`, `takesBagSlot`, `WORN_SLOTS`, `activationFor`, `itemTimerId`, `chargesTimerId`, `isReady`, `remaining`, `BAGS`, ...)
    - src/browser/combatMenu.js line 16: `import { characterSheetViewModel, itemRowState } from "./viewModels.js";`
    - mazeworld.html module script: the two `from "./src/browser/viewModels.js"` import lines (grep them) and the bridge lines `window.__mzBagUsage = bagUsage;`, `window.__mzItemRowState = itemRowState;`, `window.__mzGear = { emptySlotRows, copy: GEAR_COPY };`
    - test/unit/harness/shellSandbox.js `wireBridges` (the import twin)
    - The importing tests: `grep -ln "viewModels.js" test/unit/*.js` — of those, re-point only the ones importing a MOVED name: itemRowState.test.js, gear-panels.test.js (`emptySlotRows, GEAR_COPY` move; `dropShelfItems` stays), lootCompare.test.js (`bagUsage` moves; `lootCompare, bagArmorText` stay), hp-not-wp.test.js (`ITEM_STATE_COPY, GEAR_COPY` move; the other three copies stay until Plan 04), shell-clarity-43.test.js (`GEAR_COPY`), and shell-loot-screen.test.js's "the module bridges lootCompare/bagUsage from viewModels.js" source pin
    - src/browser/combatPanel.js head comment (the module header style to copy)
  </read_first>
  <action>
    1. Create `src/browser/gearTab.js` with a head comment in the combatPanel.js style ("src/browser/gearTab.js — the GEAR tab: ON YOU / ALSO ON YOU / BAG rendering, the shared carried-item list with its drop/swap confirms, and the Gear view models. Contract: renderGearTab(host, state, deps) — see docs/SHELL-MODULES.md. No window/document globals: host.ownerDocument + deps only."). Move the five exports out of viewModels.js VERBATIM (bodies and doc comments unchanged) in their original order, together with exactly the imports their bodies use (from `../../content/index.js`, `../../engine/derived.js`, `../../engine/items.js`, `../../engine/effects.js`, ... — copy the specifiers from viewModels.js's own import block). Remove those five from viewModels.js and prune viewModels.js's import block to what its remaining exports still reference (every imported identifier must be used at least once — check with a grep loop over the import names).
    2. Re-point importers: `src/browser/combatMenu.js` -> `import { characterSheetViewModel } from "./viewModels.js";` plus `import { itemRowState } from "./gearTab.js";`. Module script: remove `bagUsage`, `itemRowState`, `emptySlotRows`, `GEAR_COPY` from the two viewModels.js import lines and add ONE line `import { bagUsage, itemRowState, emptySlotRows, GEAR_COPY } from "./src/browser/gearTab.js";` next to them; the three bridge assignments (`__mzBagUsage`, `__mzItemRowState`, `__mzGear`) are unchanged in this task (their classic readers still exist until Task 2). Harness `wireBridges`: import the same four names from gearTab.js.
    3. Re-point the tests listed in read_first (same file names — history follows; only the import path/source-read path changes; no assertion text changes). For shell-loot-screen.test.js's bridge-source pin, the assertion now reads the gearTab.js import line for `bagUsage` and the viewModels.js line for `lootCompare`. Add `src/browser/gearTab.js` to hp-not-wp.test.js's module scan (a new test (f): `stringLiteralsOf(stripComments(readFileSync("src/browser/gearTab.js")))` scanned with the same `PLAYER_WP` + allowlist; Plans 04/05 append their modules to the same list).
    4. Gates: `npm run build:www`, `npm test` (fail 0 — same count as Plan 02's total: a pure move adds no test), `node --test test/unit/shell-tab-snapshots.test.js` green with no fixture change, `npm run boot:check` four PASS. Commit: `refactor(47-03): move the Gear-tab view models to src/browser/gearTab.js (bagUsage, GEAR_COPY, emptySlotRows, ITEM_STATE_COPY, itemRowState)`.
  </action>
  <verify>
    <automated>node -e "import('./src/browser/gearTab.js').then(m => console.log(['bagUsage','GEAR_COPY','emptySlotRows','ITEM_STATE_COPY','itemRowState'].every(k => k in m)))" && node -e "import('./src/browser/viewModels.js').then(m => console.log(!('bagUsage' in m) && !('itemRowState' in m) && ('lootCompare' in m) && ('dropShelfItems' in m)))" && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/gearTab.js` exit 0
    - `grep -cE "^export (function|const) (bagUsage|GEAR_COPY|emptySlotRows|ITEM_STATE_COPY|itemRowState)\b" src/browser/gearTab.js` prints 5; the same grep over `src/browser/viewModels.js` prints 0
    - `grep -cE "^export (function|const) (armorDisplay|bagArmorText|USABLE_COPY|usableBy|lootCompare|dropShelfItems|oracleLogViewModel|characterSheetViewModel|ABILITY_VIEW_COPY|RATIONS_COPY|eatsLineFor|rationsViewModel|grimoireViewModel)\b" src/browser/viewModels.js` prints 13 (nothing else moved)
    - `grep -c 'from "./gearTab.js"' src/browser/combatMenu.js` prints 1; `grep -cE 'import \{ characterSheetViewModel \} from "./viewModels.js"' src/browser/combatMenu.js` prints 1
    - `grep -c 'from "./src/browser/gearTab.js"' mazeworld.html` prints 1; `grep -cE "window\.__mz(BagUsage|ItemRowState|Gear) = " mazeworld.html` prints 3 (bridges untouched in this task)
    - `grep -c 'gearTab.js' test/unit/itemRowState.test.js test/unit/gear-panels.test.js test/unit/lootCompare.test.js test/unit/hp-not-wp.test.js test/unit/shell-clarity-43.test.js test/unit/harness/shellSandbox.js` prints a non-zero count for each file
    - `git diff --stat HEAD~1 --diff-filter=D -- test/` prints nothing (no test file deleted)
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm run boot:check` prints four PASS lines
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git diff --stat HEAD~1 -- test/unit/fixtures/` prints nothing
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` (no bridge added or removed by a pure move)
    - `git status --porcelain engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The five Gear view models live in gearTab.js with every importer and test re-pointed; behaviour, bridges and fixtures unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: renderGearTab + renderCarriedList into gearTab.js; paint() mounts through window.__mzTabs.gear; old helpers and dead bridges deleted; pins re-homed; gearTab.test.js</name>
  <files>src/browser/gearTab.js, src/browser/bridge.js, mazeworld.html, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/gearTab.test.js, test/unit/shell-worn-slots.test.js, test/unit/shell-gear-toolbar.test.js, test/unit/shell-gear-39.test.js, test/unit/shell-clarity-43.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-spells-40.test.js, test/unit/shell-armor-display.test.js, test/unit/shell-fight-gate.test.js, test/unit/shell-input-guards.test.js, test/unit/shell-company-panel.test.js</files>
  <read_first>
    - mazeworld.html `function paint()` — the Gear writes: the `m-gold` textContent write in the HUD group near the top; the `#s-kit` block (from `const kit = document.getElementById("s-kit");` through the `for (const [a, b] of rows)` loop — note it reads `gearCopy`, `canRead()`, `maxCharges()`); the carried/on-you block from the comment `// carried treasure (Phase 13, ECON-04/05` through the `renderCarriedList(carry, items, {...})` call (reads `w`, `armorD`, `bagFull`, `gearCopy`, `window.__mzGear.emptySlotRows`, `window.__mzBagUsage`, `window.__mzItemRowState`, `window.__mzWornSlots`, `window.mzUnequip`, `window.mzUseItem`)
    - mazeworld.html: the confirm-state trio pair (`const DROP_CONFIRM_MS = 3000;` ... `function revertDropConfirm()`, `const SWAP_CONFIRM_MS = 3000;` ... `function revertSwapConfirm()`), `function renderCarriedList(container, items, opts = {})` whole body (`mkBtn` with `opts.guard ? guardTap(...)`, `mkDropConfirm`, `mkSwapConfirm`, the actions loop reading `window.__mzItemRowState`, `window.__mzArmorDisplay.bagArmorText`, `window.__mzSlotFor`, `window.__mzWornKeysOf`, `window.__mzSellPrice`, `window.__mzLootCompare`, `window.mzUseItem/mzEquipItem/mzDropItem/mzSellItem/mzTakeLoot/mzLeaveLoot`), `function canRead()`, the loot-card call `renderCarriedList(wrap.querySelector("#loot-list"), S.pendingLoot, {` and the store call `renderCarriedList(document.getElementById("sell-list"), S.c.items, {`
    - mazeworld.html module script: `window.__mzSellPrice = (item) => { ... sellPriceFor(item, st?.c?.race, st?.c?.sub) }` (the module reproduces this with a direct `sellPriceFor` import), `window.__mzSlotFor = slotFor; window.__mzWornSlots = WORN_SLOTS; window.__mzWornKeysOf = WORN_KEYS_OF;`, `window.__mzItemRowState = itemRowState;`, `window.__mzGear = ...`, `window.__mzTables = Object.freeze(...)` (the `__mzTabs` assignment goes right after `__mzGear`'s former position)
    - engine/derived.js exports `slotFor`, `WORN_SLOTS`, `WORN_KEYS_OF`, `eff`; engine/economy.js `sellPriceFor(item, race, sub)`; engine/magic.js `canRead(state)`; engine/movement.js `maxCharges(c)`; content/index.js `WEAPONS`; src/browser/viewModels.js `armorDisplay`, `bagArmorText`, `lootCompare`
    - src/browser/bridge.js entries for `__mzGear`, `__mzItemRowState`, `__mzWornSlots`, `__mzWornKeysOf`, `__mzSlotFor`, `__mzSellPrice`, `__mzBagUsage`, `__mzLootCompare`, `__mzArmorDisplay`
    - The pin files listed in this task's files — read each file's region extractors (`sliceBetween`, `*Region()` helpers built on `CODE.indexOf("function renderCarriedList(")`, `"const DROP_CONFIRM_MS = 3000;"`, `"const wornSlotRow = (slot, it) => {"`, etc.) to see which marker strings they use
    - test/unit/shell-no-content-copies.test.js `stripComments` + `extractScriptRegions` (copy for gearTab.test.js), tools/ident-sweep.mjs `stripJs` (importable since Plan 02)
  </read_first>
  <action>
    **A. gearTab.js — the render bodies.**
    1. Module-private confirm state: move the two trios VERBATIM (`DROP_CONFIRM_MS`, `dropConfirmRevert`, `revertDropConfirm`, `SWAP_CONFIRM_MS`, `swapConfirmRevert`, `revertSwapConfirm`) with their comments.
    2. `export function renderCarriedList(container, state, items, opts = {}, deps = {})` — the classic body verbatim with ONLY these substitutions: `const doc = container.ownerDocument` replaces the document global (createElement, the capture-phase pointerdown add/removeEventListener); `state` replaces the classic S (`itemRowState(state, it)`, `state.c.worn`, `lootCompare(state.c, it)`); direct imports replace the bridges — `itemRowState` (local), `bagArmorText`/`lootCompare` (./viewModels.js), `slotFor`/`WORN_KEYS_OF` (engine/derived.js), `sellPriceFor(it, state.c.race, state.c.sub)` (engine/economy.js — the same value the retired closure computed); `deps.guardTap(bt, onClick)` replaces the classic guardTap; `deps.useItem(i)`, `deps.equipItem(i)` / `deps.equipItem(i, choice.slot)`, `deps.dropItem(i)`, `deps.sellItem(i)`, `deps.takeLoot(i, true)` / `deps.takeLoot(i)`, `deps.leaveLoot(i)` replace the optional-chained window.mz* calls (call them through `deps.<key>?.(...)` so an absent dep is the same no-op as today's absent bridge). Keep `mkBtn`, `mkDropConfirm`, `mkSwapConfirm`, the `gearRow` re-parenting and every class name byte-identical.
    3. `export function renderGearTab(host, state, deps = {})`: `const doc = host.ownerDocument; const c = state.c;` then, verbatim from paint(): the `m-gold` write; the ALSO ON YOU `#s-kit` block (`gearCopy` = `GEAR_COPY`, `canRead(state)` from engine/magic.js, `maxCharges(c)` from engine/movement.js — both formulas identical to the classic helpers); then the ON YOU/BAG block: `const w = WEAPONS[c.weapon] || WEAPONS["Club"]`, `const armorD = armorDisplay(c)`, `usage = bagUsage(c)`, `#s-carry-n`, `carry`/`onyou` clears, `empties = emptySlotRows(c)`, `emptyFor`, `headRow`, `emptyRow`, `gearBtn`, `wornRow` (`deps.unequip?.(slot)`), the weapon/armor rows, `wornSlotRow` (`itemRowState(state, it)`, `deps.useItem?.({ slot })`, `deps.unequip?.(slot)`), `for (const slot of WORN_SLOTS)`, and finally `renderCarriedList(carry, state, items, { clear: false, actions: ["use", "equip", "drop"], gearRow: true, emptyHtml: <the same string> }, deps)`. Keep the helper names — the pins re-home by them. Every `getElementById` in this function targets an id inside `#screen-gear` (`m-gold`, `s-kit`, `s-carry-n`, `s-onyou`, `s-carry`).

    **B. mazeworld.html — the carve (same commit).**
    4. Add `function tabDeps()` in the classic script immediately above `function paint()`, returning a plain object with exactly these keys: `guardTap` (the classic function itself), and action closures `useItem`, `equipItem`, `unequip`, `dropItem`, `sellItem`, `takeLoot`, `leaveLoot`, `buyItem`, `leaveStore`, `dismissJoiner`, `castSpell` — each forwarding its arguments to the matching optional-chained `window.mz*` bridge (`mzUseItem`, `mzEquipItem`, `mzUnequip`, `mzDropItem`, `mzSellItem`, `mzTakeLoot`, `mzLeaveLoot`, `mzBuyItem`, `mzLeaveStore`, `mzDismissJoiner`, `mzCastSpell`). One short comment: the deps object every tab module receives (docs/SHELL-MODULES.md).
    5. In `paint()`: delete the `m-gold` write, the `#s-kit` block, `const gearCopy = window.__mzGear.copy;`, and the carried/on-you block up to and including the `renderCarriedList(carry, ...)` call; in its place ONE line: `window.__mzTabs.gear(document.getElementById("screen-gear"), S, tabDeps());`. Keep `w`, `armorD`, `bonus` and every Hero write (Plan 04 takes them). Delete the two confirm trios, `function renderCarriedList` with its doc block, and `function canRead()` (its only caller moved; if the "talking, singing, reading" section banner is now empty, delete it too). Loot card: `window.__mzCarriedList(wrap.querySelector("#loot-list"), S, S.pendingLoot, { ...same opts... }, tabDeps())`. Store sell list: `window.__mzCarriedList(document.getElementById("sell-list"), S, S.c.items, { ...same opts... }, tabDeps())` (Plan 05 moves this call into storeScreen.js).
    6. Module script: extend the gearTab.js import to `renderGearTab, renderCarriedList`; assign `window.__mzTabs = Object.freeze({ gear: renderGearTab });` and `window.__mzCarriedList = renderCarriedList;` where `window.__mzGear` used to be (before `await boot(`). Delete the assignments `window.__mzGear`, `window.__mzItemRowState`, `window.__mzWornSlots`, `window.__mzWornKeysOf`, `window.__mzSlotFor`, `window.__mzSellPrice` ONLY after `node tools/ident-sweep.mjs "__mzName\b"` shows exactly one code line (the assignment) for each; prune now-unused import bindings (`emptySlotRows`, `GEAR_COPY`, `itemRowState`, `slotFor`, `WORN_SLOTS`, `WORN_KEYS_OF`, `sellPriceFor` — keep any that another module-script line still uses). `window.__mzBagUsage` STAYS (loot card, rail find card, store still read it).
    7. `src/browser/bridge.js`: add `__mzTabs` (owner `mazeworld.html (module)`, consumers `mazeworld.html (classic: paint(), renderEncounter())`, purpose "the tab modules' render functions, one frozen object") and `__mzCarriedList` (owner module, consumer classic loot card + store sell list); delete the six retired entries. `node tools/bridge-doc.mjs --write`.
    8. Harness `wireBridges`: drop the six retired bridge lines; add `__mzTabs` = `Object.freeze({ gear: renderGearTab })` and `__mzCarriedList = renderCarriedList` imported from the real module.

    **C. Pins (same commit) — re-home, never delete.** Run `npm test`; for every failing assertion in the files listed, point its source read at gearTab.js: add `const GEAR_SRC = fs.readFileSync(path.join(REPO_ROOT, "src/browser/gearTab.js"), "utf8")` (+ the file's own `stripComments`) and re-target the region extractor / `assert.match` at `GEAR_SRC`; keep the assertion text; update marker strings only where a signature changed (`function renderCarriedList(container, state, items, opts = {}, deps = {}) {`, `deps.useItem?.(`, `deps.guardTap(bt, onClick)`, `deps.equipItem?.(i, choice.slot)`). Pins that asserted the classic CALL SITE now assert `window.__mzTabs.gear(` / `window.__mzCarriedList(`. Measured ledger (assertions whose only matches were in moved code): shell-worn-slots (35), shell-gear-toolbar (14), shell-clarity-43 (15 Gear-region pins), shell-loot-screen (7 + the `renderCarriedList(` call pins), shell-gear-39 (6), shell-spells-40 (6), shell-armor-display (3 + the `bagArmorText(it)` pins), shell-fight-gate (1), shell-input-guards (1: the `opts.guard ? guardTap(bt, onClick)` pin -> `deps.guardTap`), shell-company-panel (1: `const DROP_CONFIRM_MS = 3000;` now in gearTab.js). Record the final per-file ledger in the SUMMARY.

    **D. test/unit/gearTab.test.js** (source-pin style, `stripJs` from tools/ident-sweep.mjs for the module, `extractScriptRegions` for the shell): (1) exports `renderGearTab`, `renderCarriedList`, and the five view models exist (import + typeof); (2) comment-stripped gearTab.js contains no `window.` / `document.` token and at least one `ownerDocument`; (3) every id in `getElementById("<id>")` calls of gearTab.js occurs in the `<section class="mw-screen" id="screen-gear"` ... `</section>` markup slice of mazeworld.html; (4) the confirm trios are declared once each in gearTab.js and zero times in the classic script; (5) classic `paint()` region contains `window.__mzTabs.gear(document.getElementById("screen-gear"), S, tabDeps());` exactly once and no `s-onyou`/`s-carry`/`s-kit`/`m-gold` id; (6) the classic script declares no `function renderCarriedList(`; (7) `window.__mzCarriedList(` appears in the classic script (count 2 now — loot card + store; the test asserts >= 1 and Plan 05 tightens to 1); (8) the module script assigns `window.__mzTabs = Object.freeze({ gear: renderGearTab })` (exact text — Plans 04/05 update this pin) before `await boot(`; (9) `function tabDeps()` appears once and its body names all 12 keys; (10) voice: `GEAR_COPY` and `ITEM_STATE_COPY` string leaves clear `content/safety-wordlist.js` (copy the pattern from gear-panels.test.js).

    **E. Gates + commit.** `npm run build:www`; `npm test` fail 0; `node --test test/unit/shell-tab-snapshots.test.js` fail 0 with NO fixture change; `npm run boot:check` 4 PASS; zero-ref sweeps; fence. Commit: `refactor(47-03): src/browser/gearTab.js owns the Gear tab — paint() mounts it through window.__mzTabs.gear; renderCarriedList + confirms move; dead bridges deleted`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-tab-snapshots.test.js test/unit/gearTab.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)" && grep -v '^\s*//' mazeworld.html | grep -c "window.__mzTabs.gear(document.getElementById(\"screen-gear\"), S, tabDeps());" && node tools/shell-sweep.mjs refs renderCarriedList wornSlotRow revertDropConfirm revertSwapConfirm DROP_CONFIRM_MS SWAP_CONFIRM_MS canRead; echo "sweep-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export function (renderGearTab|renderCarriedList)\(" src/browser/gearTab.js` prints 2
    - `node tools/ident-sweep.mjs "\bwindow\." | grep -c "src/browser/gearTab.js"` prints 0; `node tools/ident-sweep.mjs "\bdocument\." | grep -c "src/browser/gearTab.js"` prints 0; `grep -c "ownerDocument" src/browser/gearTab.js` prints a number >= 2
    - `grep -v '^\s*//' mazeworld.html | grep -c 'window.__mzTabs.gear(document.getElementById("screen-gear"), S, tabDeps());'` prints 1
    - `grep -v '^\s*//' mazeworld.html | grep -c "^function tabDeps()"` prints 1; `awk '/^function tabDeps\(\)/,/^}/' mazeworld.html | grep -cE "guardTap|useItem|equipItem|unequip|dropItem|sellItem|takeLoot|leaveLoot|buyItem|leaveStore|dismissJoiner|castSpell"` prints a number >= 12
    - `node tools/shell-sweep.mjs refs renderCarriedList wornSlotRow revertDropConfirm revertSwapConfirm DROP_CONFIRM_MS SWAP_CONFIRM_MS canRead` prints `refs <name>: 0` for every name and exits 0 — EXCEPT `renderCarriedList`, which may report exactly the module-script import binding + the `__mzCarriedList` assignment (2); `grep -cE "^function renderCarriedList\(" mazeworld.html` prints 0
    - `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -cE '"(s-onyou|s-carry|s-carry-n|s-kit|m-gold)"'` prints 0
    - `grep -v '^\s*//' mazeworld.html | grep -c "window.__mzCarriedList(" ` prints 2 (loot card + store sell list)
    - `grep -c "window.__mzTabs = Object.freeze({ gear: renderGearTab });" mazeworld.html` prints 1; `grep -c "window.__mzCarriedList = renderCarriedList;" mazeworld.html` prints 1
    - For each of `__mzGear` `__mzItemRowState` `__mzWornSlots` `__mzWornKeysOf` `__mzSlotFor` `__mzSellPrice`: `node tools/ident-sweep.mjs "<name>\b"` prints `ident-sweep <name>\b: 0` (bridge.js is in src/ — its keys are gone too)
    - `node -e "import('./src/browser/bridge.js').then(m => console.log(m.bridgeNames().includes('__mzTabs') && m.bridgeNames().includes('__mzCarriedList') && !m.bridgeNames().includes('__mzGear')))"` prints true; `node tools/bridge-doc.mjs --check` exits 0
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git diff --stat HEAD~2 -- test/unit/fixtures/` prints nothing; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1 (only the Plan 01 capture commit)
    - `node --test test/unit/gearTab.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `grep -E "^# pass"` prints a number >= 10
    - `git diff --stat HEAD~2 --diff-filter=D -- test/` prints nothing (no test file deleted); for every re-pointed pin file F, `grep -c "^test(" F` equals `git show HEAD~2:F | grep -c "^test("` (no test case removed — every change is a re-target; the SUMMARY ledger lists each file's count)
    - `grep -c "gearTab.js" test/unit/hp-not-wp.test.js` prints a number >= 1
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass"` prints a number greater than Task 1's total (gearTab.test.js added; record it)
    - `npm run boot:check` prints four PASS lines
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `wc -l < mazeworld.html` prints a number at least 300 lower than the pre-plan count (record before/after in the SUMMARY)
  </acceptance_criteria>
  <done>The Gear tab renders from gearTab.js behind one mount call; the loot card and store reach the shared list through `window.__mzCarriedList`; six dead bridges and the classic helpers are gone with the registry/doc/harness updated; every pin re-homed; all fixtures byte-equal; all gates green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's slice (the full phase list is in 47-01-PLAN.md and 47-05-PLAN.md):

- `src/browser/gearTab.js` — `renderGearTab(host, state, deps)`, `renderCarriedList(container, state, items, opts, deps)`, `bagUsage`, `GEAR_COPY`, `emptySlotRows`, `ITEM_STATE_COPY`, `itemRowState`
- `test/unit/gearTab.test.js` — module + mount pins
- `mazeworld.html` — `tabDeps()`, the `window.__mzTabs.gear(...)` mount, `window.__mzTabs` (gear) + `window.__mzCarriedList` assigned in the module script; classic `renderCarriedList`, the confirm trios, `canRead`, and six dead bridges deleted
- `src/browser/viewModels.js` — five Gear exports removed; `src/browser/combatMenu.js` — `itemRowState` import re-pointed
- `src/browser/bridge.js` + `docs/SHELL-MODULES.md` — `__mzTabs`, `__mzCarriedList` added; six entries removed
- Re-homed pins: shell-worn-slots, shell-gear-toolbar, shell-gear-39, shell-clarity-43, shell-loot-screen, shell-spells-40, shell-armor-display, shell-fight-gate, shell-input-guards, shell-company-panel, itemRowState, gear-panels, lootCompare, hp-not-wp (+ module scan)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state -> DOM | item names/txt (content + engine data, never free-text user input) are written into the Gear rows |
| deps closures -> window.mz* bridges | the module dispatches only through the closures the classic script hands it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-05 | Tampering | gearTab.js row builders | medium | mitigate | the T-38-11 discipline (createElement/textContent for item names in `wornSlotRow` and the carried rows) moves verbatim; the re-homed shell-worn-slots / shell-gear-39 pins assert it on gearTab.js; the byte-equal snapshot proves no innerHTML path was introduced |
| T-47-06 | Elevation of privilege | deps object | low | mitigate | deps carries only the twelve named action closures over existing bridges; gearTab.test.js pins the key list, and the module never reaches `window` |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 47 |
</threat_model>

<verification>
- Snapshot test green with zero fixture changes (Gear AND Hero AND Store fixtures)
- `gearTab.test.js`, `bridge-registry.test.js` green; `npm test` fail 0; `build:www`, `boot:check` green; zero-ref sweeps 0; fence empty
</verification>

<success_criteria>
- SHELL-01: the Gear tab (ON YOU / ALSO ON YOU / BAG, equip/use/drop/swap confirms) renders from `src/browser/gearTab.js` with its own source-pin suite; `paint()` only mounts it
- Six bridges whose only readers moved are deleted with the registry and doc in sync; `__mzTabs` + `__mzCarriedList` registered
- Every pin re-homed, none deleted; fixtures byte-equal; all gates green
</success_criteria>

<output>
Create `.planning/phases/47-shell-modularisation/47-03-SUMMARY.md` when done. It MUST carry: the per-file pin re-point ledger (file -> assertions re-targeted), the snapshot test output (7 fixtures compared, 0 diffs), the bridge delta (+2 / -6 with the ident-sweep proofs), the deleted classic helpers with their `shell-sweep refs` outputs, `wc -l` before/after, `npm test` totals, `boot:check` output, the fence outputs, and a `## Human verification (deferred to end of run)` section: Gear tab ON YOU / BAG rows, Use/Equip/Unequip, the Drop and Swap two-tap confirms, and the loot card's Stow/Equip now/Leave rows look and behave exactly as before on the Pixel 7 (milestone-close batch).
</output>
