---
phase: 47-shell-modularisation
plan: 04
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - src/browser/heroTab.js
  - src/browser/viewModels.js
  - src/browser/combatMenu.js
  - src/browser/bridge.js
  - mazeworld.html
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/heroTab.test.js
  - test/unit/gearTab.test.js
  - test/unit/characterSheetViewModel.test.js
  - test/unit/armorDisplay.test.js
  - test/unit/rationsViewModel.test.js
  - test/unit/grimoireViewModel.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/shell-company-panel.test.js
  - test/unit/shell-clarity-43.test.js
  - test/unit/shell-abilities.test.js
  - test/unit/shell-party-camp.test.js
  - test/unit/shell-armor-display.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/shell-narration-wiring.test.js
  - test/unit/shell-no-content-copies.test.js
autonomous: true
requirements: [SHELL-02, SHELL-04]

must_haves:
  truths:
    - "SHELL-02 (unclassified -> truth): after this plan the Plan 01 Hero fixtures (thief.hero, mu.hero) AND every other fixture compare byte-equal under a plain snapshot run — no fixture file changes in this plan's commits"
    - "paint() contains no Hero render body: exactly one `window.__mzTabs.hero(document.getElementById(\"screen-hero\"), S, tabDeps())` call; the classic script declares no renderPartyRoster, no dismiss-confirm trio, no escText, no skillTable/skill/maxCharges/R_/upkeep/eff helpers, no mzSpellCharges; the module script declares no renderGrimoire"
    - "src/browser/heroTab.js reads no window/document global (comment-stripped scan); every id it writes lives inside the #screen-hero markup section"
    - "SHELL-04: the registry stays green with `__mzRenderGrimoire`, `__mzAbilities`, `__mzStrikeDie`, `__mzToHit`, `__mzEff` deleted (each proven zero-reader live before deletion) and `__mzTables` trimmed to the keys the classic script still reads (measured expectation: ROMAN)"
    - "The BEFORE-only grimoire seam in test/unit/harness/shellSandbox.js is deleted in the same commit heroTab.js owns the grimoire"
    - "Every pin that read moved Hero code is re-homed to heroTab.js (ledger in the SUMMARY); zero test files deleted, zero test cases removed"
  artifacts:
    - "src/browser/heroTab.js — renderHeroTab(host, state, deps), characterSheetViewModel, ABILITY_VIEW_COPY, RATIONS_COPY, eatsLineFor, rationsViewModel, grimoireViewModel"
    - "test/unit/heroTab.test.js — module + mount pins"
    - "mazeworld.html — the hero mount call; window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab })"
  key_links:
    - "paint() -> window.__mzTabs.hero(host, S, tabDeps()) -> heroTab.js#renderHeroTab -> sheet / skills / abilities / rations / grimoire / dossier / Company panel"
    - "src/browser/combatMenu.js -> heroTab.js#characterSheetViewModel (ABILITIES submenu rows keep the one state-suffix rule)"
    - "heroTab.js -> engine/derived.js#strikeDie/toHit/eff/upkeep, engine/movement.js#maxCharges, content/index.js#ROMAN/THRESHOLDS/WEAPONS/RACES/FIGHTER_SKILLS/THIEF_SKILLS/RACE_NOTE/CLASS_NOTE/SUB_NOTE (the tables the classic script used to reach through window.__mzTables)"
  prohibitions:
    - statement: "MUST NOT change any rendered DOM or text of the Hero tab — the Plan 01 fixtures are the lock; MZ_SNAPSHOT_UPDATE is never set in this plan"
      requirement_id: SHELL-02
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT read window/document globals inside heroTab.js — host, host.ownerDocument and deps only"
      requirement_id: SHELL-02
      category: values
      status: unverified
      verification: test
    - statement: "MUST NOT keep renderPartyRoster, renderGrimoire, the classic helper wrappers, mzSpellCharges, or a zero-reader bridge alive after heroTab.js owns them"
      requirement_id: SHELL-04
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete a source pin — re-home every failing assertion to heroTab.js or the new mount call"
      requirement_id: SHELL-02
      category: values
      status: unverified
      verification: judgment
---

<objective>
Carve the Hero tab out of `mazeworld.html` into `src/browser/heroTab.js`: first the Hero-only view models (`characterSheetViewModel`, `ABILITY_VIEW_COPY`, `RATIONS_COPY`, `eatsLineFor`, `rationsViewModel`, `grimoireViewModel`) move with their tests (pure move, one commit), then the render bodies — the character sheet, skills, abilities, RATIONS, dossier writes from `paint()`, the module script's `renderGrimoire`, and the Company panel (`renderPartyRoster` + its DISMISS confirm) — move behind `renderHeroTab(host, state, deps)`; `paint()` makes one mount call through `window.__mzTabs.hero(...)`. The classic helper wrappers that only the Hero writes used (`skillTable`, `skill`, `maxCharges`, `R_`, `upkeep`, `eff`, `mzSpellCharges`) and the bridges that lose their last reader are deleted in the same commit; `__mzTables` shrinks to what the classic script still reads.

Purpose: SHELL-02 — the Hero tab (sheet, dossier, Company panel, Grimoire, abilities, RATIONS) renders from a named module with its own source-pin suite. Pixel-identical: the Plan 01 fixtures must compare byte-equal after the carve.

Output: `src/browser/heroTab.js`, `test/unit/heroTab.test.js`, a `viewModels.js` holding only the shared view models, the shell's hero mount call, updated `bridge.js`/doc/harness (grimoire seam deleted), re-homed pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-shell-modularisation/47-CONTEXT.md
@.planning/phases/47-shell-modularisation/47-03-SUMMARY.md
@docs/SHELL-MODULES.md
@src/browser/gearTab.js
@src/browser/bridge.js
@test/unit/harness/shellSandbox.js
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->
<!-- planner-discipline-allow: renderPartyRoster -->
<!-- planner-discipline-allow: renderGrimoire -->
<!-- planner-discipline-allow: __mzRenderGrimoire -->
<!-- planner-discipline-allow: __mzAbilities -->
<!-- planner-discipline-allow: __mzStrikeDie -->
<!-- planner-discipline-allow: __mzToHit -->
<!-- planner-discipline-allow: __mzEff -->
<!-- planner-discipline-allow: mzSpellCharges -->
<!-- planner-discipline-allow: skillTable -->
<!-- planner-discipline-allow: escText -->
<!-- planner-discipline-allow: revertDismissConfirm -->
<!-- planner-discipline-allow: DISMISS_CONFIRM_MS -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt` (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- **No behaviour change:** the committed BEFORE fixtures are the lock; a red snapshot means fix the module. `MZ_SNAPSHOT_UPDATE` is never set.
- **Greenfield ruling:** no shims; one mount call per surface; old helpers deleted in the same commit; `shell-sweep refs` = 0 proves it.
- **Locked view-model split (CONTEXT):** the six Hero exports move even though `characterSheetViewModel` also feeds `combatMenu.js` (measured) — that import re-points to heroTab.js. `__mzRations` STAYS bridged (the rail's find card reads `window.__mzRations.eatsLine`) and is imported from heroTab.js.
- **Engine equivalents replace the classic wrappers** (verified identical at planning): classic `upkeep()` == `engine/derived.js#upkeep(c)`; classic `maxCharges()` == `engine/movement.js#maxCharges(c)`; classic `skill(n)` == `engine/derived.js#skill(c, n)`; classic `eff(key)` delegates to `engine/derived.js#eff(c, key)`; classic `mzSpellCharges()` == `{ left: Math.max(0, maxCharges(c) - c.spellsUsed), max: maxCharges(c) }` for a Magic User, else null. The snapshot fixtures prove the swap is invisible.
- **Windows executor environment:** Git Bash, no `jq`; multi-line scripts in files. Line numbers have drifted twice by now — anchor by names.
- **Verification agents are off:** the acceptance criteria ARE the verification.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: move the Hero-tab view models to src/browser/heroTab.js (pure move, tests follow)</name>
  <files>src/browser/heroTab.js, src/browser/viewModels.js, src/browser/combatMenu.js, mazeworld.html, test/unit/harness/shellSandbox.js, test/unit/characterSheetViewModel.test.js, test/unit/armorDisplay.test.js, test/unit/rationsViewModel.test.js, test/unit/grimoireViewModel.test.js, test/unit/hp-not-wp.test.js, test/unit/shell-abilities.test.js</files>
  <read_first>
    - src/browser/viewModels.js: the private helpers `skillTableFor`, `damageBracket`, `nextLevelValue`, `snarkLine`, `quirkText` (grep each for callers — if only `characterSheetViewModel` uses them they move; `armorDisplay` must keep whatever it uses) and the six exports with their doc comments: `ABILITY_VIEW_COPY`, `characterSheetViewModel`, `RATIONS_COPY`, `eatsLineFor`, `rationsViewModel`, `grimoireViewModel`
    - src/browser/combatMenu.js line 16 (`characterSheetViewModel` import) and any `viewModels.js#grimoireViewModel` mention in its comments (leave comments alone — Phase 48)
    - mazeworld.html module script: the `from "./src/browser/viewModels.js"` import lines (`characterSheetViewModel`, `grimoireViewModel`, `rationsViewModel`, `eatsLineFor` move), and the bridge lines `window.__mzAbilities = { ..., sheet: characterSheetViewModel }`, `window.__mzRations = { view: rationsViewModel, eatsLine: eatsLineFor }`
    - test/unit/harness/shellSandbox.js: `wireBridges` (`__mzAbilities`, `__mzRations`) and `wireLegacyGrimoire` (imports `grimoireViewModel` — re-point to heroTab.js in this task; the seam itself is deleted in Task 2)
    - test/unit/characterSheetViewModel.test.js — its import AND the source pin at ~line 199 ("src/browser/viewModels.js does not read the design mockup's placeholder field shape") which reads the viewModels.js source; test/unit/armorDisplay.test.js (imports `characterSheetViewModel` alongside `armorDisplay, bagArmorText` — split the import); rationsViewModel.test.js; grimoireViewModel.test.js; hp-not-wp.test.js (`ABILITY_VIEW_COPY, RATIONS_COPY` move; `USABLE_COPY` stays); shell-abilities.test.js (the "window.__mzAbilities bridges byId/roundsLeft/isReady/sheet from ... viewModels.js" source pin)
  </read_first>
  <action>
    1. Create `src/browser/heroTab.js` with a head comment in the gearTab.js style ("the HERO tab: character sheet, special skills, abilities, RATIONS, Grimoire, the Game Master's notes and the Company panel, plus the Hero view models. Contract: renderHeroTab(host, state, deps) — docs/SHELL-MODULES.md. No window/document globals."). Move the six exports and the private helpers only they use out of viewModels.js VERBATIM, in their original order, with exactly the imports their bodies need (copy specifiers from viewModels.js's import block). Prune viewModels.js's import block to what its seven remaining exports reference (every imported identifier used at least once).
    2. Re-point importers: `src/browser/combatMenu.js` -> `import { characterSheetViewModel } from "./heroTab.js";` (its gearTab.js import from Plan 03 stays). Module script: remove the four moved names from the viewModels.js import lines and add ONE `import { characterSheetViewModel, grimoireViewModel, rationsViewModel, eatsLineFor } from "./src/browser/heroTab.js";`; bridge assignments unchanged in this task. Harness: `wireBridges` and `wireLegacyGrimoire` import the moved names from heroTab.js.
    3. Re-point the tests in read_first (same file names; import paths and the source-read path of characterSheetViewModel.test.js's ~199 pin change to `src/browser/heroTab.js`; assertion text unchanged). Append `src/browser/heroTab.js` to hp-not-wp.test.js's module-scan list from Plan 03.
    4. Gates: `npm run build:www`, `npm test` fail 0 (same count as Plan 03's final), snapshot test green with no fixture change, `npm run boot:check` four PASS. Commit: `refactor(47-04): move the Hero-tab view models to src/browser/heroTab.js (characterSheetViewModel, ABILITY_VIEW_COPY, RATIONS_COPY, eatsLineFor, rationsViewModel, grimoireViewModel)`.
  </action>
  <verify>
    <automated>node -e "import('./src/browser/heroTab.js').then(m => console.log(['characterSheetViewModel','ABILITY_VIEW_COPY','RATIONS_COPY','eatsLineFor','rationsViewModel','grimoireViewModel'].every(k => k in m)))" && node -e "import('./src/browser/viewModels.js').then(m => console.log(Object.keys(m).sort().join(',')))" && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/heroTab.js` exit 0
    - `grep -cE "^export (function|const) (characterSheetViewModel|ABILITY_VIEW_COPY|RATIONS_COPY|eatsLineFor|rationsViewModel|grimoireViewModel)\b" src/browser/heroTab.js` prints 6; the same grep over `src/browser/viewModels.js` prints 0
    - `node -e "import('./src/browser/viewModels.js').then(m => console.log(Object.keys(m).sort().join(',')))"` prints exactly `USABLE_COPY,armorDisplay,bagArmorText,dropShelfItems,lootCompare,oracleLogViewModel,usableBy`
    - `grep -c 'from "./heroTab.js"' src/browser/combatMenu.js` prints 1; `grep -c 'from "./viewModels.js"' src/browser/combatMenu.js` prints 0
    - `grep -c 'from "./src/browser/heroTab.js"' mazeworld.html` prints 1
    - `grep -c "heroTab.js" test/unit/characterSheetViewModel.test.js test/unit/armorDisplay.test.js test/unit/rationsViewModel.test.js test/unit/grimoireViewModel.test.js test/unit/hp-not-wp.test.js test/unit/shell-abilities.test.js test/unit/harness/shellSandbox.js` prints a non-zero count for each file
    - `git diff --stat HEAD~1 --diff-filter=D -- test/` prints nothing
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm run boot:check` prints four PASS lines
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git diff --stat HEAD~1 -- test/unit/fixtures/` prints nothing
    - `node --test test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `git status --porcelain engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The six Hero view models live in heroTab.js; viewModels.js holds exactly the seven shared exports; importers/tests/harness re-pointed; behaviour and fixtures unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: renderHeroTab (sheet, skills, abilities, rations, grimoire, dossier, Company) into heroTab.js; paint() mounts through window.__mzTabs.hero; classic wrappers, dead bridges and the grimoire seam deleted; pins re-homed; heroTab.test.js</name>
  <files>src/browser/heroTab.js, src/browser/bridge.js, mazeworld.html, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/heroTab.test.js, test/unit/gearTab.test.js, test/unit/shell-company-panel.test.js, test/unit/shell-clarity-43.test.js, test/unit/shell-abilities.test.js, test/unit/shell-party-camp.test.js, test/unit/shell-armor-display.test.js, test/unit/shell-gear-39.test.js, test/unit/shell-narration-wiring.test.js, test/unit/shell-no-content-copies.test.js</files>
  <read_first>
    - mazeworld.html `function paint()` — the Hero writes, in order: `s-level`/`s-name`/`s-tag`, the `btn-abandon-character` label flip block, `s-wp`/`s-wpmax`, `const pct = clamp(...)` (ALSO read by the HUD `mw-hud-wp`/`mw-hud-wpfill` block right below — that block STAYS), `s-wpfill`, `paintConditions(c)` (STAYS), `s-die`/`s-hit` (window.__mzStrikeDie/__mzToHit), `const w = ...`, `const bonus = ...` (R_(), eff("dmg")), `s-dmg`, `const armorD = window.__mzArmorDisplay.armorDisplay(c)`, `s-arm`, `s-int`, `s-sp`, `s-next` (THRESHOLDS), `s-cost` (upkeep()), the RATIONS pair (`window.__mzRations.view(S)`), `s-trait` (R_().note), the special-skills block (`skillTable(c.cls)`, `s-vp`, `s-skills`), `function renderAbilityRows(c)` + its call (window.__mzAbilities.sheet), the dossier block (`doss-who`, `doss`, window.__mzTables.RACE_NOTE/CLASS_NOTE/SUB_NOTE), `window.__mzRenderGrimoire?.();`, `renderPartyRoster();`
    - mazeworld.html: `const DISMISS_CONFIRM_MS = 3000;` ... `function revertDismissConfirm()`, `const escText = ...`, `function renderPartyRoster()` whole body; the classic helpers `function skillTable(cls)`, `const skill = n => ...`, `const maxCharges = () => ...`, `const R_ = () => ...`, `function upkeep()`, `function eff(key)` (with its delegating comment), `window.mzSpellCharges = () => ...`; the classic `clamp` (STAYS — the HUD block uses it)
    - mazeworld.html module script: `function renderGrimoire() {` through `window.__mzRenderGrimoire = renderGrimoire;` and its doc comment; the bridge lines `window.__mzAbilities`, `window.__mzStrikeDie`, `window.__mzToHit`, `window.__mzEff`, `window.__mzTables = Object.freeze({ ... })` and its `content/index.js` import line; `window.__mzTabs = Object.freeze({ gear: renderGearTab });`
    - engine/derived.js `strikeDie(c)`, `toHit(state)`, `eff(c, key)`, `upkeep(c)`, `skill(c, n)`; engine/movement.js `maxCharges(c)`; content/index.js `ROMAN`, `THRESHOLDS`, `WEAPONS`, `RACES`, `FIGHTER_SKILLS`, `THIEF_SKILLS`, `RACE_NOTE`, `CLASS_NOTE`, `SUB_NOTE`; src/browser/viewModels.js `armorDisplay`
    - test/unit/harness/shellSandbox.js `wireLegacyGrimoire` (delete) and `wireBridges`
    - test/unit/shell-no-content-copies.test.js tests 2, 3, 4 (the exact `window.__mzTables` import/bridge lines and the survivors list — these pins are re-homed when `__mzTables` shrinks)
    - The pin files in this task's files list — read their region extractors (`function renderPartyRoster() {` -> `\n}\n`, `window.mzDismissJoiner = function dismissJoinerBridge(`, `function renderAbilityRows(c)`, `"s-die")...__mzStrikeDie`, `window.mzSpellCharges`, ...)
  </read_first>
  <action>
    **A. heroTab.js — the render body.**
    1. Module-private: `clamp` (one line), `escText` (verbatim), the DISMISS trio (`DISMISS_CONFIRM_MS`, `dismissConfirmRevert`, `revertDismissConfirm`, verbatim with comment), `skillTable(cls)` returning `FIGHTER_SKILLS`/`THIEF_SKILLS`/null from the content import, `renderAbilityRows(doc, state)` (verbatim body; rows from `characterSheetViewModel(state).abilities`), `renderGrimoire(doc, state, deps)` (verbatim from the module script; `grimoireViewModel(state)`; the charges readout computed inline with the retired `mzSpellCharges` formula via `maxCharges(c)`; `deps.castSpell?.(row.idx)`), `renderPartyRoster(doc, state, deps)` (verbatim; `eatsLineFor(m)` local; `ROMAN[lvl - 1]`; `state.combat`; `deps.dismissJoiner?.(idx)`).
    2. `export function renderHeroTab(host, state, deps = {})`: `const doc = host.ownerDocument; const c = state.c;` then the Hero writes verbatim in paint()'s order with only these substitutions: `ROMAN`/`THRESHOLDS`/`WEAPONS`/`RACES`/`RACE_NOTE`/`CLASS_NOTE`/`SUB_NOTE` direct imports replace the `window.__mzTables.*` reads; `strikeDie(c)`/`toHit(state)` replace the two bridges; `const R = RACES[c.race]` replaces `R_()`; `eff(c, "dmg")` replaces `eff("dmg")`; `armorDisplay(c)` replaces the bridge; `upkeep(c)` replaces `upkeep()`; `rationsViewModel(state)` replaces `window.__mzRations.view(S)`; `state.dead` replaces `S.dead`; then `renderAbilityRows(doc, state)`, the dossier loop, `renderGrimoire(doc, state, deps)`, `renderPartyRoster(doc, state, deps)`. Every `getElementById` targets an id inside `#screen-hero` (`s-level`, `s-name`, `s-tag`, `btn-abandon-character`, `s-wp`, `s-wpmax`, `s-wpfill`, `s-die`, `s-hit`, `s-dmg`, `s-arm`, `s-int`, `s-sp`, `s-next`, `s-cost`, `s-rations`, `s-rations-n`, `s-trait`, `s-vp`, `s-skills`, `s-abilities`, `s-grim-count`, `s-grimoire`, `doss-who`, `doss`, `hero-party`, `hero-party-list`).

    **B. mazeworld.html — the carve (same commit).**
    3. In `paint()`: keep the HUD group at the top, keep ONE `const pct = clamp(c.wp / c.maxWP * 100, 0, 100);` immediately above the HUD `mw-hud-wp` block, keep `paintConditions(c);`; delete every Hero write listed in read_first (through `renderPartyRoster();`) and put ONE line where the sheet writes began: `window.__mzTabs.hero(document.getElementById("screen-hero"), S, tabDeps());` — placed BEFORE the gear mount so paint()'s order reads hero, gear, renderEncounter, renderRail, draw. Delete `function renderPartyRoster` with its comment block, the DISMISS trio, `escText`. Delete the classic `skillTable`, `skill`, `maxCharges`, `R_`, `upkeep`, `eff`, and `window.mzSpellCharges` — each ONLY after `node tools/shell-sweep.mjs refs <name>` shows the definition as the sole survivor (if any of them still has a live caller elsewhere, keep it and record why in the SUMMARY). Module script: delete `function renderGrimoire` with its doc comment and `window.__mzRenderGrimoire = renderGrimoire;`; delete `window.__mzAbilities`, `window.__mzStrikeDie`, `window.__mzToHit`, `window.__mzEff` after ident-sweep shows each has exactly one code line (the assignment); prune their import bindings (`ABILITY_BY_ID`, `abilityRoundsLeft`, `isReady`, `strikeDie`, `toHit`, `eff` — keep any still used). Update `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab });` and the heroTab.js import.
    4. Trim `__mzTables`: measure `grep -oE "__mzTables\.[A-Z_]+" mazeworld.html | sort -u` over the CLASSIC region after the deletions (expected: `ROMAN` only — the combat report line, the graves stone line, the rail's joiner card); rewrite the bridge as `window.__mzTables = Object.freeze({ <surviving keys> });` and shrink its `content/index.js` import line to those keys (plus any other content name the module script still uses on other lines). `__mzRations` stays; `__mzNightlyEats` stays; `__mzConditionsOf` stays.
    5. `src/browser/bridge.js`: delete `__mzRenderGrimoire`, `__mzAbilities`, `__mzStrikeDie`, `__mzToHit`, `__mzEff`; update `__mzTables`'s purpose/consumers to the surviving keys; update `__mzTabs`'s purpose ("gear + hero"). `node tools/bridge-doc.mjs --write`.
    6. Harness: delete `wireLegacyGrimoire` and its call; `__mzTabs` = `Object.freeze({ gear: renderGearTab, hero: renderHeroTab })`; drop the five retired bridge lines; mirror the trimmed `__mzTables`.

    **C. Pins (same commit) — re-home, never delete.** `npm test`; re-target every failing assertion at `HERO_SRC` (heroTab.js read + stripComments) or the new mount call; keep assertion text; update marker strings only where a signature changed (`function renderPartyRoster(doc, state, deps) {`, `deps.dismissJoiner?.(idx)`, `function renderGrimoire(doc, state, deps) {`, `deps.castSpell?.(row.idx)`, `strikeDie(c)`, `toHit(state)`). Measured ledger: shell-company-panel (18 Hero-only + the partial ones — `mw-drop-confirm`/`pointerdown`/`getElementById(` counts now span heroTab.js + the shell; the "DROP_CONFIRM_MS untouched" clause reads gearTab.js), shell-clarity-43 (11), shell-abilities (7 + the bridge pin -> heroTab.js's `characterSheetViewModel(state)` call), shell-party-camp (6), shell-armor-display (3), shell-gear-39 (2: the `s-die`/`s-hit` lines), shell-narration-wiring (the `window.mzSpellCharges` pin -> the grimoire charges readout in heroTab.js via `maxCharges(`), shell-no-content-copies tests 2/3/4 (test 2: the dossier reads `RACE_NOTE[c.race]`/`CLASS_NOTE[c.cls]`/`SUB_NOTE[c.sub]` in heroTab.js via the content import and the classic script has ZERO such reads; test 3: the trimmed exact import + bridge lines, still before `await boot(`; test 4: the survivors list is the trimmed key set and every other former key has zero classic reads), gearTab.test.js (the `__mzTabs` exact-text pin -> the two-key object). Record the per-file ledger in the SUMMARY.

    **D. test/unit/heroTab.test.js** (same style as gearTab.test.js): (1) exports exist (`renderHeroTab` + the six view models); (2) comment-stripped heroTab.js has no `window.` / `document.` token and uses `ownerDocument`; (3) every `getElementById("<id>")` id in heroTab.js occurs inside the `<section class="mw-screen" id="screen-hero"` ... `</section>` markup slice; (4) `escText`, the DISMISS trio, `renderPartyRoster`, `renderAbilityRows`, `renderGrimoire` are declared once each in heroTab.js and zero times in mazeworld.html (both scripts, comment-stripped); (5) classic `paint()` contains `window.__mzTabs.hero(document.getElementById("screen-hero"), S, tabDeps());` exactly once, BEFORE the gear mount line, and no `"s-` id, no `"doss`; (6) the module script assigns `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab })` before `await boot(`; (7) every key in the `window.__mzTables = Object.freeze({ ... })` literal has at least one `window.__mzTables.<KEY>` read in the classic script (no dead key); (8) the literals "Spells are the trick.", "Send them off?", "DISMISS", "Combat only", "No spells learned yet." live in heroTab.js and not in the shell; (9) the T-38-11 rule — `renderAbilityRows` region contains no `innerHTML`; (10) `paintConditions(` is still called from `paint()` (the HUD strip did not move).

    **E. Gates + commit.** `npm run build:www`; `npm test` fail 0; snapshot test fail 0 with NO fixture change; `npm run boot:check` 4 PASS; zero-ref sweeps; fence. Commit: `refactor(47-04): src/browser/heroTab.js owns the Hero tab — paint() mounts it through window.__mzTabs.hero; grimoire + Company panel move; classic wrappers and dead bridges deleted`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-tab-snapshots.test.js test/unit/heroTab.test.js test/unit/gearTab.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# (pass|fail)" && grep -v '^\s*//' mazeworld.html | grep -c 'window.__mzTabs.hero(document.getElementById("screen-hero"), S, tabDeps());' && node tools/shell-sweep.mjs refs renderPartyRoster renderGrimoire escText revertDismissConfirm DISMISS_CONFIRM_MS skillTable upkeep mzSpellCharges; echo "sweep-exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^export function renderHeroTab\(" src/browser/heroTab.js` prints 1
    - `node tools/ident-sweep.mjs "\bwindow\." | grep -c "src/browser/heroTab.js"` prints 0; `node tools/ident-sweep.mjs "\bdocument\." | grep -c "src/browser/heroTab.js"` prints 0; `grep -c "ownerDocument" src/browser/heroTab.js` prints a number >= 1
    - `grep -v '^\s*//' mazeworld.html | grep -c 'window.__mzTabs.hero(document.getElementById("screen-hero"), S, tabDeps());'` prints 1, and `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -nE "__mzTabs\.(hero|gear)\("` shows the hero line before the gear line
    - `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -cE '"(s-level|s-name|s-tag|s-wp|s-wpmax|s-wpfill|s-die|s-hit|s-dmg|s-arm|s-int|s-sp|s-next|s-cost|s-rations|s-rations-n|s-trait|s-vp|s-skills|s-abilities|doss-who|doss|btn-abandon-character)"'` prints 0; `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -c "paintConditions(c);"` prints 1; `awk '/^function paint\(\)/,/^}/' mazeworld.html | grep -c "const pct = clamp("` prints 1
    - `node tools/shell-sweep.mjs refs renderPartyRoster renderGrimoire escText revertDismissConfirm dismissConfirmRevert DISMISS_CONFIRM_MS renderAbilityRows skillTable upkeep mzSpellCharges R_` prints `refs <name>: 0` for every name and exits 0; `node tools/shell-sweep.mjs refs skill maxCharges eff` prints 0 for each OR the SUMMARY records the surviving live caller of any kept one
    - `grep -cE "^\s*function renderGrimoire\(\)" mazeworld.html` prints 0
    - For each of `__mzRenderGrimoire` `__mzAbilities` `__mzStrikeDie` `__mzToHit` `__mzEff`: `node tools/ident-sweep.mjs "<name>\b"` prints `ident-sweep <name>\b: 0`
    - `grep -c "window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab });" mazeworld.html` prints 1
    - Every key K in the `window.__mzTables = Object.freeze({` literal satisfies `awk '/^<script>$/,/^<\/script>$/' mazeworld.html | grep -v '^\s*//' | grep -c "window.__mzTables.K"` >= 1 (record the surviving key list in the SUMMARY; measured expectation: ROMAN)
    - `grep -c "wireLegacyGrimoire" test/unit/harness/shellSandbox.js` prints 0; `grep -c "heroTab.js" test/unit/harness/shellSandbox.js` prints a number >= 1
    - `node -e "import('./src/browser/bridge.js').then(m => { const k = m.bridgeNames(); console.log(!k.includes('__mzRenderGrimoire') && !k.includes('__mzAbilities') && !k.includes('__mzStrikeDie') && !k.includes('__mzToHit') && !k.includes('__mzEff') && k.includes('__mzRations') && k.includes('__mzTables')) })"` prints true; `node tools/bridge-doc.mjs --check` exits 0
    - `node --test test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/gearTab.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`; `git diff --stat HEAD~2 -- test/unit/fixtures/` prints nothing; `git log --oneline -- test/unit/fixtures/shell-snapshots | wc -l` prints 1
    - `node --test test/unit/heroTab.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `grep -E "^# pass"` prints a number >= 10
    - `git diff --stat HEAD~2 --diff-filter=D -- test/` prints nothing; for every re-pointed pin file F, `grep -c "^test(" F` equals `git show HEAD~2:F | grep -c "^test("`
    - `grep -c "heroTab.js" test/unit/hp-not-wp.test.js` prints a number >= 1
    - `npm run build:www` exits 0; `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass"` prints a number greater than Task 1's total (record it)
    - `npm run boot:check` prints four PASS lines
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `wc -l < mazeworld.html` prints a number at least 300 lower than the post-Plan-03 count (record before/after)
  </acceptance_criteria>
  <done>The Hero tab renders from heroTab.js behind one mount call placed before the gear mount; the grimoire, Company panel and abilities list live in the module; the classic wrappers, five dead bridges and the harness seam are gone with the registry/doc/harness in sync; `__mzTables` carries only keys the classic script reads; every pin re-homed; fixtures byte-equal; gates green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

This plan's slice (the full phase list is in 47-01-PLAN.md and 47-05-PLAN.md):

- `src/browser/heroTab.js` — `renderHeroTab(host, state, deps)` + private `renderAbilityRows`, `renderGrimoire`, `renderPartyRoster`, `escText`, the DISMISS trio; exports `characterSheetViewModel`, `ABILITY_VIEW_COPY`, `RATIONS_COPY`, `eatsLineFor`, `rationsViewModel`, `grimoireViewModel`
- `test/unit/heroTab.test.js` — module + mount pins
- `mazeworld.html` — the `window.__mzTabs.hero(...)` mount; `window.__mzTabs` (gear + hero); classic `renderPartyRoster`, the DISMISS trio, `escText`, `skillTable`/`skill`/`maxCharges`/`R_`/`upkeep`/`eff`/`mzSpellCharges`, the module-script `renderGrimoire`, and five dead bridges deleted; `__mzTables` trimmed
- `src/browser/viewModels.js` — exactly the seven shared exports remain; `src/browser/combatMenu.js` — `characterSheetViewModel` import re-pointed
- `src/browser/bridge.js` + `docs/SHELL-MODULES.md` — five entries removed, `__mzTables`/`__mzTabs` updated
- `test/unit/harness/shellSandbox.js` — BEFORE-only grimoire seam deleted; `__mzTabs` twin carries gear + hero
- Re-homed pins: shell-company-panel, shell-clarity-43, shell-abilities, shell-party-camp, shell-armor-display, shell-gear-39, shell-narration-wiring, shell-no-content-copies (tests 2/3/4), characterSheetViewModel, armorDisplay, rationsViewModel, grimoireViewModel, hp-not-wp (+ module scan), gearTab.test.js (`__mzTabs` literal)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| party member sheet -> Company card innerHTML | member name/sub/race/cls/weapon strings are interpolated into innerHTML — `escText` escapes them (moves verbatim) |
| content tables -> dossier/grimoire innerHTML | RACE_NOTE/CLASS_NOTE/SUB_NOTE and spell txt are content, not user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-07 | Tampering | heroTab.js#renderPartyRoster | medium | mitigate | `escText` moves verbatim and the re-homed shell-company-panel "Sheet fields ... are escText()-escaped" pin asserts it on heroTab.js; the byte-equal mu.hero fixture (one joiner card) proves the escaped output is unchanged |
| T-47-08 | Tampering | heroTab.js#renderAbilityRows | low | mitigate | T-38-11 (createElement/textContent only, no innerHTML in the abilities list) pinned by heroTab.test.js (9) |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in Phase 47 |
</threat_model>

<verification>
- Snapshot test green with zero fixture changes (all seven fixtures)
- `heroTab.test.js`, `gearTab.test.js`, `bridge-registry.test.js`, `shell-no-content-copies.test.js` green; `npm test` fail 0; `build:www`, `boot:check` green; zero-ref sweeps 0; fence empty
</verification>

<success_criteria>
- SHELL-02: the Hero tab (sheet, dossier, Company panel, Grimoire, abilities, RATIONS) renders from `src/browser/heroTab.js` with its own source-pin suite; `paint()` only mounts it
- Five bridges and seven classic wrappers gone, `__mzTables` trimmed, registry/doc/harness in sync, `__mzTabs` carries gear + hero
- Every pin re-homed, none deleted; fixtures byte-equal; all gates green
</success_criteria>

<output>
Create `.planning/phases/47-shell-modularisation/47-04-SUMMARY.md` when done. It MUST carry: the per-file pin re-point ledger, the snapshot output (7 compared, 0 diffs), the bridge delta (-5, `__mzTables` surviving keys) with ident-sweep proofs, the deleted classic helpers with their `shell-sweep refs` outputs, `wc -l` before/after, `npm test` totals, `boot:check` output, the fence outputs, a note for Phase 48 (comments elsewhere that still mention `viewModels.js#RATIONS_COPY`/`grimoireViewModel` — e.g. src/browser/eventNarration.js, src/browser/combatMenu.js, the Hero-screen HTML comments — are stale prose, not code, and are left for DOCS-02), and a `## Human verification (deferred to end of run)` section: the Hero tab's sheet, special skills, abilities, RATIONS line, Grimoire (Cast buttons, "Combat only" hints, charges readout), Game Master's notes and the Company panel (DISMISS two-tap confirm) look and behave exactly as before on the Pixel 7.
</output>