---
phase: 47-shell-modularisation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/harness/recordingDom.js
  - test/unit/harness/shellSandbox.js
  - test/unit/shell-tab-snapshots.test.js
  - test/unit/fixtures/shell-snapshots/thief.hero.txt
  - test/unit/fixtures/shell-snapshots/thief.gear.txt
  - test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt
  - test/unit/fixtures/shell-snapshots/thief-store.store.txt
  - test/unit/fixtures/shell-snapshots/mu.hero.txt
  - test/unit/fixtures/shell-snapshots/mu.gear.txt
  - test/unit/fixtures/shell-snapshots/mu-store.store.txt
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03]

must_haves:
  truths:
    - "SHELL-01 (unclassified -> truth): the Gear tab DOM the shell writes into #m-gold/#s-kit/#s-carry-n/#s-onyou/#s-carry for the two fixed states is captured BEFORE any body moves, committed as fixtures, and node --test test/unit/shell-tab-snapshots.test.js compares the live serialization to those fixtures byte-for-byte (a diff = the carve is wrong, never the fixture)"
    - "SHELL-02 (unclassified -> truth): the Hero tab DOM (s-level .. doss, s-grimoire, hero-party-list) for the two fixed states is captured and committed the same way, and the snapshot test compares it byte-for-byte"
    - "SHELL-03 (idempotency): rendering the store twice from the same state (two renderEncounter() calls) serializes byte-equal to a single render and #shelf/#sell-list carry no duplicate rows — asserted by the snapshot test"
    - "The recording document serializes with a stable order (sorted attribute/data/style keys, insertion-ordered children) so two renders of the same state always serialize identically"
    - "Every fixture is produced only under MZ_SNAPSHOT_UPDATE=1; a plain node --test run never writes a fixture"
  artifacts:
    - "test/unit/harness/recordingDom.js — createRecordingDocument() (element stub + serializeElements(doc, ids))"
    - "test/unit/harness/shellSandbox.js — loadShellSandbox({ doc }) running mazeworld.html's classic <script> under node:vm with the module-script bridge twin (wireBridges), fixedStates(), SNAPSHOT_IDS"
    - "test/unit/shell-tab-snapshots.test.js — the standing 3-screen DOM smoke"
    - "test/unit/fixtures/shell-snapshots/*.txt — the seven committed BEFORE snapshots"
  key_links:
    - "shellSandbox.js -> mazeworld.html classic script (vm.runInContext of the text between the column-0 <script> and </script> tags — the same region split shell-no-content-copies.test.js uses)"
    - "shellSandbox.js#wireBridges -> the same content/engine/src imports the module script assigns as window.__mz* (mirrored by name, so a missing bridge throws in the test, never silently renders less)"
    - "paint() (classic, inside the vm) -> the recording document -> serializeElements -> fixture text"
  flagged_assumptions:
    - "SHELL-01 coverage: the two fixed states cover empty-slot rows (mu: armor/jewelry1/jewelry2/cloak empty), worn rows (thief: starting cloak worn + two jewelry pieces equipped), bag rows (weapon, armor, jewelry, potion, tool), the drop confirm (armed in the gear-confirms fixture) and the swap confirm (thief's third jewelry piece, armed in the same fixture). NOT covered by any snapshot: a destroyed armor piece row, a bag-less dev character, a Magic User's bagged staff row, a Fighter's ON YOU. Those rows are locked by the re-pointed source pins, not by a snapshot."
    - "SHELL-02 coverage: the Thief (special skills, 12 vp) and Magic User (grimoire + charges, 'Spells are the trick.', Company panel with one joiner) sheets are snapshotted. NOT covered: a Fighter sheet (8 vp, FIGHTER_SKILLS rows), the S.dead abandon-label flip, a downed joiner, in-combat ability states."
    - "SHELL-03 concurrency: not applicable — paint()/renderEncounter() are synchronous and single-threaded; the only timers on these surfaces are the confirm reverts (drop/swap/dismiss), which never touch the store. Flagged, not tested."
  prohibitions:
    - statement: "MUST NOT alter any rendered DOM or text of the Gear tab, Hero tab or Store — the committed BEFORE fixtures are the lock; an AFTER diff means the carve is wrong, never the fixture"
      requirement_id: SHELL-01
      category: safety
      status: unverified
      verification: test
    - statement: "MUST NOT read window/document globals inside gearTab.js, heroTab.js or storeScreen.js — a module reaches the page only through host, host.ownerDocument and the explicit deps object"
      requirement_id: SHELL-01
      category: values
      status: unverified
      verification: test
    - statement: "MUST NOT keep an old shell helper or window.__mz* bridge alive once its module owns the behaviour — no shims, no dual paths"
      requirement_id: SHELL-04
      category: values
      status: unverified
      verification: judgment
    - statement: "MUST NOT delete a source pin — every shell-*.test.js assertion that pinned moved code is re-homed to the module, never removed"
      requirement_id: SHELL-01
      category: values
      status: unverified
      verification: judgment
---

<objective>
Build the measure-first lock for Phase 47: a node harness that runs `mazeworld.html`'s classic `<script>` headless (node:vm, the `test/parity/harness/sandboxPrototype.js` technique) against a recording fake document, renders the Gear tab, the Hero tab and the Store from fixed deterministic states, serializes the DOM the shell wrote with a stable order, and commits those serializations as BEFORE fixtures — before a single line of the three render bodies moves.

Purpose: ROADMAP Phase 47 criterion 5 ("pixel-identical") and the CONTEXT decision "a DOM snapshot before/after per screen ... captured and committed as a fixture ... the serialized DOM must be byte-equal". Plans 03-05 carve against these fixtures; if an AFTER run differs, the carve is wrong, not the snapshot.

Output: `test/unit/harness/recordingDom.js`, `test/unit/harness/shellSandbox.js`, `test/unit/shell-tab-snapshots.test.js`, seven fixtures under `test/unit/fixtures/shell-snapshots/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-shell-modularisation/47-CONTEXT.md
@test/parity/harness/sandboxPrototype.js
@test/unit/shell-no-content-copies.test.js
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->

<phase_ground_rules>
- **Engine-gate fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt` (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`). This plan touches `test/unit/` only.
- **Windows executor environment:** Git Bash, no `jq`; heredocs and `node -e` one-liners mangle backslashes — every multi-line script is written to a file with the Write tool and run with `node <file>`.
- **Line numbers drift:** every anchor below is a function name or a marker string measured at HEAD `0129ca3` (shell 6,339 lines); re-locate with grep, never trust the number.
- **Verification agents are off:** the acceptance criteria ARE the verification. Run every command; paste outputs into the SUMMARY.
- **Baseline measured this session:** `npm test` -> `# tests 3231 / # pass 3231 / # fail 0`; `wc -l < mazeworld.html` -> 6339.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: recordingDom.js — a recording fake document with a stable serializer</name>
  <files>test/unit/harness/recordingDom.js</files>
  <read_first>
    - test/parity/harness/sandboxPrototype.js (the whole file — `makeFakeElement`, the auto-creating `getElementById` map, the `innerHTML`/`textContent` defineProperty pattern, and the "add a stub only where the sandbox throws, comment the shell line that needed it" rule from 01-RESEARCH A4)
    - mazeworld.html `paint()` (grep `^function paint\(\)`), `renderCarriedList` (grep `^function renderCarriedList\(`), `renderPartyRoster` (grep `^function renderPartyRoster\(`), the `S.store` branch inside `renderEncounter()` (grep `if \(S\.store\) \{`), and the module script's `function renderGrimoire()` — read them ONLY to enumerate the element API the three surfaces call: createElement, createTextNode, appendChild, replaceChildren, replaceWith, isConnected, contains, querySelector(".mw-gear-drop"), querySelectorAll(":scope > button"), classList.add/remove/toggle/contains, className, dataset, style.*, hidden, disabled, title, textContent, innerHTML (assign AND `+=`), setAttribute/getAttribute/removeAttribute, onclick, addEventListener/removeEventListener (element AND document level, capture flag), getContext (canvas), scrollTop, clientWidth
  </read_first>
  <action>
    Create `test/unit/harness/recordingDom.js` (ES module, node built-ins only, NO top-level side effects — `node --test` sweeps every .js under test/ as a test file, so the module must import cleanly with zero tests, exactly like `test/parity/harness/*.js`).

    Export `createRecordingDocument()` returning `{ document, elementsById, serializeElements }`:

    1. **Element stub** (`makeElement(tag, doc)`): fields `tagName` (lowercase), `id` (settable; setting it also registers the element in `elementsById`), `className` (string, source of truth for `classList` — `add` appends when absent, `remove`, `toggle(name, force)`, `contains`), `attributes` (a Map written by `setAttribute`, read by `getAttribute`/`hasAttribute`, cleared by `removeAttribute`), `dataset` (plain object), `style` (plain object), `hidden`/`disabled` (booleans, default false), `title`, `type`, `value` (strings), `onclick` (null), `children` (array), `parentNode`, `ownerDocument` (the document), `isConnected` (true), `firstChild`/`lastChild` derived, `scrollTop` 0, `clientWidth`/`clientHeight`/`offsetWidth` numbers, `getBoundingClientRect()` all-zeros, `getContext()` a no-op 2D context object (copy `makeFakeCanvasContext`'s method list), `focus`/`blur`/`click`/`scrollIntoView`/`setPointerCapture`/`insertAdjacentHTML` no-ops, `closest()` null, `addEventListener`/`removeEventListener` no-ops, `appendChild(child)` (sets `parentNode`, returns child), `insertBefore`, `removeChild`, `remove()`, `replaceChildren(...nodes)`, `replaceWith(node)` (swaps itself in `parentNode.children`; a detached element is a no-op), `contains(node)` (deep), `querySelector(sel)`/`querySelectorAll(sel)` over descendants supporting exactly: `#id`, `.class`, `tag`, and `:scope > tag` (direct children by tag) — throw on any other selector so an unsupported selector is loud, never a silent empty result.
    2. **Content tracking**: define `innerHTML` and `textContent` as accessor properties. Setting `innerHTML` stores `{ kind: "html", value }` and clears `children`; the getter returns the stored html string when kind is html, else "" (so the store's `+=` append reads back what was written). Setting `textContent` stores `{ kind: "text", value: String(value) }` and clears `children`; the getter returns the text when kind is text, else the concatenated textContent of children. `createTextNode(text)` returns `{ nodeType: 3, textContent: text }`.
    3. **Document**: `getElementById(id)` auto-creates and memoises (the parity harness's Map trick — the shell's markup is never parsed, so every id the shell writes becomes its own recorded root); `createElement(tag)`, `createTextNode`, `querySelector("#id")` (id-only; throw otherwise), `querySelectorAll(...)` returns `[]` (the tab-init `.mw-tab` scan and the arm-delay sweep), `addEventListener`/`removeEventListener` no-ops (the confirm's document-level pointerdown capture listener), `body` and `documentElement` elements.
    4. **Serializer** `serializeElements(ids)` -> one string: for each id in order, a `## #<id>` header line then `serialize(el, depth)`: one line per node, two-space indent per depth — `<tag` + ` id=` + ` class=` (when non-empty) + every `attributes` entry sorted by name + every `dataset` key sorted as ` data-<k>=` + ` style=` with keys sorted joined `k:v;` + ` hidden`/` disabled` flags when true + ` title=`/` type=`/` value=` when non-empty + ` onclick` when a function is attached + `>`; every string value via `JSON.stringify`. Then, when content.kind is html: an indented `html=<JSON>` line; when text: `text=<JSON>`; then children recursively; text nodes as `#text <JSON>`. Newlines `\n` only. The output must be a pure function of the recorded writes (no Date, no random, no object identity).

    Head comment: what this is (the Phase 47 DOM-snapshot recording document), why ids are roots (no HTML parser — `getElementById` auto-creates), and the A4 rule for adding stubs.
  </action>
  <verify>
    <automated>node --test test/unit/harness/recordingDom.js && node -e "import('./test/unit/harness/recordingDom.js').then(m => { const d = m.createRecordingDocument(); const el = d.document.getElementById('x'); el.innerHTML = '<b>a</b>'; const li = d.document.createElement('li'); li.className = 'a'; li.classList.add('b'); li.style.marginTop = '4px'; li.textContent = 't'; el.appendChild(li); const s1 = d.serializeElements(['x']); console.log(s1); process.exit(s1 === d.serializeElements(['x']) && /class=\"a b\"/.test(s1) && /html=\"<b>a<\/b>\"/.test(s1) ? 0 : 1) })"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/harness/recordingDom.js` exit 0
    - `node --test test/unit/harness/recordingDom.js` exits 0 (imports cleanly, zero tests)
    - `grep -cE "^export function createRecordingDocument\(" test/unit/harness/recordingDom.js` prints 1
    - `grep -cE "serializeElements" test/unit/harness/recordingDom.js` prints a number >= 2 (definition + export/return)
    - `grep -cE "querySelectorAll|replaceWith|replaceChildren|createTextNode|ownerDocument" test/unit/harness/recordingDom.js` prints a number >= 5
    - The verify command's inline probe prints a serialization containing `class="a b"`, `style="marginTop:4px;"`, `html="<b>a</b>"` and `text="t"` and exits 0 (two serializations of the same document are identical)
    - `grep -cE "Math\.random|Date\.now" test/unit/harness/recordingDom.js` prints 0 (the serializer is a pure function of the recorded writes)
  </acceptance_criteria>
  <done>A recording document exists whose serialization is deterministic, covers every element API the three surfaces call, and throws loudly on unsupported selectors.</done>
</task>

<task type="auto">
  <name>Task 2: shellSandbox.js — run the classic script under node:vm with the module-script bridge twin, plus the fixed states</name>
  <files>test/unit/harness/shellSandbox.js</files>
  <read_first>
    - test/parity/harness/sandboxPrototype.js (`loadPrototypeSandbox`: vm.createContext, `sandbox.window = sandbox`, the `let S` live-getter trick — `window.__mzState` already gives the shell a setter, so that trick is NOT needed here)
    - test/unit/shell-no-content-copies.test.js `extractScriptRegions` (the column-0 `\n<script>\n` / `\n</script>\n` region split — reuse the same markers)
    - mazeworld.html, the module script's bridge block: grep `window.__mzTables = Object.freeze(` and read from `window.__mzBags = BAGS;` down to `window.__mzGear = { emptySlotRows, copy: GEAR_COPY };` — this is the assignment list `wireBridges` mirrors; plus the module script's import lines for those names (grep `from "./src/browser/viewModels.js"`, `from "./engine/derived.js"`, `from "./content/index.js"`, `from "./engine/economy.js"`, `from "./engine/movement.js"`, `from "./engine/abilities.js"`, `from "./engine/effects.js"`, `from "./engine/items.js"`)
    - mazeworld.html `function renderGrimoire() {` (module script) through `window.__mzRenderGrimoire = renderGrimoire;` — the ONE module-script-defined DOM function the Hero surface reaches; its only free identifiers are `document`, `window`, `grimoireViewModel`
    - mazeworld.html classic top level: `(function initOracleScroll()`, `(function initTabs()`, `const cv = document.getElementById("maze"); const ctx = cv.getContext("2d");`, `(function initMazeViewportControls()`, the trailing `addEventListener("keydown"` / `addEventListener("resize"` — the load-time side effects the sandbox must survive
    - engine/state.js `newRun(seed, exclude, { force })` and `addPartyMember(state, member)`; engine/character.js `rollCharacter(rng, exclude, force)`; engine/items.js `rollJewel(rng)`, `rollCloak(rng)`, `rollBlade(rng, depth, magical)`, `rollMailPiece(rng)`, `toolItem(key)`, `stowItem(state, it, events, quiet)`, `equipItem(state, i, events, target)`, `bagCap`; engine/economy.js `openStore(state, rng, events)` (sets `state.store = { stock, haggle, race }`); engine/rng.js `makeRng(seed)`; src/browser/viewModels.js `bagUsage(c)` (the `.full` flag)
  </read_first>
  <action>
    Create `test/unit/harness/shellSandbox.js` (ES module, node built-ins + repo imports, no top-level side effects).

    **`loadShellSandbox({ doc })`** — `doc` is a `createRecordingDocument()` result:
    1. Read `mazeworld.html` from the repo root (resolve via `import.meta.url`, normalise `\r\n` to `\n`), extract the classic script region with the same column-0 markers `shell-no-content-copies.test.js` uses, and the module script region likewise (needed for step 4 only).
    2. Build the sandbox object: `document: doc.document`, `console`, `Math`, `setTimeout` (returns an incrementing integer, never fires), `clearTimeout` no-op, `addEventListener`/`removeEventListener` no-ops, `requestAnimationFrame`/`cancelAnimationFrame` no-ops, `getComputedStyle: () => ({ getPropertyValue: () => "" })`, `matchMedia: () => ({ matches: false, addEventListener() {} })`, `localStorage` (in-memory Map stub), `navigator: { userAgent: "node", vibrate() {} }`, `performance: { now: () => 0 }`, `innerWidth 400`, `innerHeight 800`, `devicePixelRatio 1`, `location: { search: "" }`; then `sandbox.window = sandbox` (top-level identifiers reachable bare AND via `window.`). Use `Object.create(Math)` for `Math` only if a seeded random is needed — the three surfaces draw no rng during render, so plain `Math` is fine. `vm.createContext(sandbox)`; `vm.runInContext(classicSource, context, { filename: "mazeworld.html#classic" })`.
    3. **`wireBridges(context)`** — assign onto `context.window` exactly the bridge objects the module script assigns, by importing the same names from the same files: `__mzTables` (RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES from content/index.js), `__mzNightlyEats`, `__mzPartyCap`, `__mzSellPrice` (the same closure over `context.window.__mzState`), `__mzConditionsOf`, `__mzEther`, `__mzMapView`, `__mzAbilities` ({ byId: ABILITY_BY_ID, roundsLeft: abilityRoundsLeft, isReady, sheet: characterSheetViewModel }), `__mzEff`, `__mzSlotFor`, `__mzWornSlots`, `__mzWornKeysOf`, `__mzInputGuards`, `__mzArmorDisplay`, `__mzBagUsage`, `__mzTakesBagSlot`, `__mzLootCompare`, `__mzHasTool`, `__mzToolIndex`, `__mzToHit`, `__mzStrikeDie`, `__mzItemRowState`, `__mzUsableBy`, `__mzRations`, `__mzDropShelfItems`, `__mzGear`. Keep the list in ONE place with a comment: "twin of the module script's bridge block — when a carve moves an export, re-point the import here in the same commit". Bridges the three surfaces never reach (`__mzRailVM`, `__mzFightLogVM`, `__mzCombatVM`, `__mzIconMap`, `__mzMapMarks`, `__mzCanvasSizing`, `__mzTapStep`, `__mzControls`, `__mzIconsApi`, `__mzHaptics`, `__mzSettings`) are NOT wired; `renderRail()` returns early without `__mzRailVM`, and `draw()` is stubbed (next step).
    4. **BEFORE-only grimoire seam** (deleted by Plan 04 in the commit heroTab.js takes the grimoire): locate in the module-script text the block starting at the line `  function renderGrimoire() {` and ending with the line `  window.__mzRenderGrimoire = renderGrimoire;`; if found, set `context.grimoireViewModel = grimoireViewModel` (imported from src/browser/viewModels.js) and `vm.runInContext` that block in the same context (its free identifiers are exactly `document`, `window`, `grimoireViewModel`). If the block is absent, do nothing (Plan 04 removes this whole step). Name the function `wireLegacyGrimoire` and mark it `// BEFORE-only seam — Plan 04 deletes` in its head comment.
    5. Stub the heavy classic globals AFTER the script ran (function declarations are writable global properties in a vm Script): `context.draw = () => {}`, `context.renderRail = () => {}` and `context.window.renderRail = context.renderRail`. Keep `paintConditions` live (it renders the HUD condition strip, harmless).
    6. Return `{ context, setState: (s) => context.window.__mzState.set(s), paint: () => context.paint(), renderEncounter: () => context.renderEncounter(), doc }`.
    7. **A4 rule:** if `vm.runInContext` or `paint()` throws on a missing browser API, add the smallest stub to the sandbox object or the element stub, with a comment naming the shell function that required it. Never stub a `window.__mz*` bridge with a dummy — wire the real import.

    **`SNAPSHOT_IDS`** (exported, frozen): `hero: ["s-level","s-name","s-tag","btn-abandon-character","s-wp","s-wpmax","s-wpfill","s-die","s-hit","s-dmg","s-arm","s-int","s-sp","s-next","s-cost","s-rations","s-rations-n","s-trait","s-vp","s-skills","s-abilities","s-grim-count","s-grimoire","doss-who","doss","hero-party","hero-party-list"]`, `gear: ["m-gold","s-kit","s-carry-n","s-onyou","s-carry"]`, `store: ["enc-panel","enc-body","shelf","sell-head","sell-list","a-leave"]`.

    **`fixedStates()`** (exported) — deterministic, engine-built, no `Math.random`:
    - `thief`: `newRun(2, [], { force: { cls: "Thief" }, storeRoll: true })`. Then, with `const rng = makeRng(11)`: `stowItem` a `rollBlade(rng, 1, false)` weapon, a `rollMailPiece(rng)` armor, three `rollJewel(rng)` pieces (re-roll with the same rng until the three names differ), a `toolItem("rope")`-style tool (use the first key of content TOOLS if "rope" is not a key), and a potion object shaped like engine/economy.js's `potionLine` item (`{ kind: "potion", n, txt, eff2, uses: 1 }` from the first `STORE_POTION_POOL`/`POTIONS` entry). `equipItem` the first two jewelry pieces so `c.worn.jewelry1`/`jewelry2` are occupied (the third stays in the bag and renders the swap-confirm Equip). Then push filler jewelry from the same rng until `bagUsage(c).full` is true (slot-consuming items only) so the store shows the "Bag full" line and the sell+drop actions. Set `c.kills = 3` and `c.spellsUsed = 0`.
    - `mu`: `newRun(3, [], { force: { cls: "Magic User" } })`; `state.c.spellsUsed = 1`; `addPartyMember(state, rollCharacter(makeRng(4)))` (assert it returned true — PARTY_CAP is 1).
    - `thiefStore`: a structuredClone of `thief` with `openStore(state, makeRng(5))` applied (sets `state.store`).
    - `muStore`: a structuredClone of `mu` with `openStore(state, makeRng(6))` applied (a Magic User with nothing to sell exercises the hidden `sell-head` path — assert `state.c.items.length === 0` or, if chargen gave the MU items, empty `state.c.items` before opening the store and note it in the head comment).
    Return `{ thief, mu, thiefStore, muStore }`. Every engine call goes through the exported engine functions — the harness never hand-mutates `c.worn`.
  </action>
  <verify>
    <automated>node --test test/unit/harness/shellSandbox.js && node -e "Promise.all([import('./test/unit/harness/recordingDom.js'), import('./test/unit/harness/shellSandbox.js')]).then(([r, s]) => { const st = s.fixedStates(); for (const k of ['thief','mu','thiefStore','muStore']) { const doc = r.createRecordingDocument(); const sb = s.loadShellSandbox({ doc }); sb.setState(st[k]); sb.paint(); const out = doc.serializeElements(s.SNAPSHOT_IDS.gear.concat(s.SNAPSHOT_IDS.hero, s.SNAPSHOT_IDS.store)); if (out.length < 2000) { console.error(k, 'serialization too short', out.length); process.exit(1); } } console.log('all four states painted'); })"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/harness/shellSandbox.js` exit 0
    - `grep -cE "^export function (loadShellSandbox|fixedStates)\(" test/unit/harness/shellSandbox.js` prints 2
    - `grep -cE "^export const SNAPSHOT_IDS" test/unit/harness/shellSandbox.js` prints 1
    - `grep -c "wireLegacyGrimoire" test/unit/harness/shellSandbox.js` prints a number >= 2 (definition + call) and `grep -c "BEFORE-only seam" test/unit/harness/shellSandbox.js` prints 1
    - `grep -cE "vm\.runInContext" test/unit/harness/shellSandbox.js` prints a number >= 2 (classic script + grimoire seam)
    - `grep -cE "__mz(Tables|Rations|Gear|BagUsage|ItemRowState|ArmorDisplay|Abilities|StrikeDie|ToHit|WornSlots|WornKeysOf|SlotFor|SellPrice|UsableBy|LootCompare|DropShelfItems|Eff|NightlyEats|ConditionsOf|TakesBagSlot)\b" test/unit/harness/shellSandbox.js` prints a number >= 20
    - The verify command prints `all four states painted` and exits 0 (paint() ran on every fixed state without throwing; each combined serialization is at least 2,000 characters)
    - `grep -c "Math.random" test/unit/harness/shellSandbox.js` prints 0 (states are rng-seeded through makeRng only)
    - `git status --porcelain engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The classic script loads under node:vm with the real bridges wired, `paint()` renders both fixed states (and the store on the two store states) into the recording document, and the fixed states are engine-built and deterministic.</done>
</task>

<task type="auto">
  <name>Task 3: shell-tab-snapshots.test.js — capture the seven BEFORE fixtures, then prove the standing test compares (never writes)</name>
  <files>test/unit/shell-tab-snapshots.test.js, test/unit/fixtures/shell-snapshots/thief.hero.txt, test/unit/fixtures/shell-snapshots/thief.gear.txt, test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt, test/unit/fixtures/shell-snapshots/thief-store.store.txt, test/unit/fixtures/shell-snapshots/mu.hero.txt, test/unit/fixtures/shell-snapshots/mu.gear.txt, test/unit/fixtures/shell-snapshots/mu-store.store.txt</files>
  <read_first>
    - test/unit/harness/recordingDom.js and test/unit/harness/shellSandbox.js (from Tasks 1-2)
    - mazeworld.html `renderCarriedList` — the `mkDropConfirm` arm (`drop.classList.add("mw-gear-drop")`, the `mw-drop-confirm` wrap with "Drop it?") and `mkSwapConfirm` arm (the `mw-swap-confirm` wrap: "Swap for which?" + one button per worn piece + No) — the DOM the gear-confirms fixture must show
    - test/unit/shell-no-content-copies.test.js (the sibling source-pin style for the header comment and MZ_* env override precedent)
  </read_first>
  <action>
    Create `test/unit/shell-tab-snapshots.test.js`:
    - Fixture dir `test/unit/fixtures/shell-snapshots/`; `UPDATE = process.env.MZ_SNAPSHOT_UPDATE === "1"`. A helper `check(name, text)`: when UPDATE, `mkdirSync` + write `<name>.txt` and pass; otherwise read the fixture (a missing fixture is a FAILURE with the message "run once with MZ_SNAPSHOT_UPDATE=1 to capture the BEFORE fixture") and `assert.equal(text, fixture)` — on mismatch the assertion message must print the first differing line number and both lines (write a small `firstDiff(a, b)` helper) so the SUMMARY can quote it.
    - One fresh `createRecordingDocument()` + `loadShellSandbox` per state (never share a document between states).
    - Tests: (1) `thief.hero` = paint() then `serializeElements(SNAPSHOT_IDS.hero)`; (2) `thief.gear` likewise with `SNAPSHOT_IDS.gear`; (3) `thief.gear-confirms`: after paint(), walk `#s-carry`'s descendants, call `onclick()` on the first button whose textContent is "Drop" and on EVERY button whose textContent is "Equip" (plain Equips are no-ops in the sandbox — the deps bridges are undefined there; the third jewelry row's Equip arms the swap confirm), then serialize `["s-carry"]` and assert the text contains both `"Drop it?"` and `"Swap for which?"` before comparing to the fixture; (4) `thief-store.store`: paint() on `thiefStore`, serialize `SNAPSHOT_IDS.store`, assert it contains `Bag full (` and `"sold"`-or-`" wm"` shelf rows; (5) `mu.hero` (assert it contains `Spells are the trick.` and a `mw-party-member` card); (6) `mu.gear`; (7) `mu-store.store`.
    - SHELL-03 idempotency truth (8): on `thiefStore`, after the first paint() call `renderEncounter()` a second time and assert the store serialization equals the first one exactly (no duplicate `goods` rows: count occurrences of `class="goods` in both — equal).
    - Determinism truth (9): two independent sandboxes on `thief` serialize `hero+gear` identically.
    - Guard (10): when not updating, assert that all seven fixture files exist and are non-empty (a plain run must never be able to pass on a missing or empty fixture).
    Head comment: this is ROADMAP Phase 47 criterion 5's lock and the milestone's 3-screen smoke; fixtures are captured ONCE before the carves; a diff after a carve means the carve is wrong; regenerating fixtures requires a declared, deliberate DOM change with rationale (the fixture-regeneration rule).

    Then: run `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js` ONCE to write the seven fixtures; inspect each fixture (they must be non-trivial: worn rows with `Unequip`, `mw-onyou-head` rows, the kit head row, `mw-party-member`, `grim-lvl`, the store `goods` rows); then run the plain `node --test test/unit/shell-tab-snapshots.test.js` and `npm test`. Commit the harness, the test and the seven fixtures together (`test(47-01): shell tab DOM snapshot harness + BEFORE fixtures`).
  </action>
  <verify>
    <automated>node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# (pass|fail)" && ls test/unit/fixtures/shell-snapshots/ | wc -l && grep -c "Swap for which?" test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt && grep -c "Drop it?" test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt</automated>
  </verify>
  <acceptance_criteria>
    - `ls test/unit/fixtures/shell-snapshots/*.txt | wc -l` prints 7
    - `node --test test/unit/shell-tab-snapshots.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `grep -E "^# pass"` prints a number >= 9
    - `grep -c "Swap for which?" test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt` prints a number >= 1; `grep -c "Drop it?" test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt` prints a number >= 1
    - `grep -c "mw-worn-tag" test/unit/fixtures/shell-snapshots/thief.gear.txt` prints a number >= 3 (starting cloak + two jewelry pieces worn); `grep -c "mw-worn-empty" test/unit/fixtures/shell-snapshots/mu.gear.txt` prints a number >= 3 (empty armor/jewelry/cloak rows)
    - `grep -c "Bag full (" test/unit/fixtures/shell-snapshots/thief-store.store.txt` prints 1; `grep -c 'class="goods' test/unit/fixtures/shell-snapshots/thief-store.store.txt` prints a number >= 5
    - `grep -c "Spells are the trick." test/unit/fixtures/shell-snapshots/mu.hero.txt` prints 1; `grep -c "mw-party-member" test/unit/fixtures/shell-snapshots/mu.hero.txt` prints a number >= 1; `grep -c "grim-lvl" test/unit/fixtures/shell-snapshots/mu.hero.txt` prints a number >= 1
    - `grep -c "mw-kit-head" test/unit/fixtures/shell-snapshots/thief.gear.txt` prints 1 (the ALSO ON YOU head row lives in the Gear snapshot)
    - `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass"` prints a number > 3231 (record the new total in the SUMMARY)
    - `npm run build:www` exits 0; `npm run boot:check` prints four PASS lines
    - `git status --porcelain engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `git log --oneline -1 -- test/unit/fixtures/shell-snapshots/thief.hero.txt` prints a commit (the BEFORE fixtures are committed before any carve)
  </acceptance_criteria>
  <done>Seven BEFORE fixtures are committed; the standing snapshot test compares (never writes) under a plain run, proves store idempotency and serializer determinism, and the full suite, build and boot gates are green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Phase-level list (the plan that creates each item in brackets):

- `src/browser/gearTab.js` [03] — exports `renderGearTab(host, state, deps)`, `renderCarriedList(container, state, items, opts, deps)`, and the moved view models `bagUsage`, `GEAR_COPY`, `emptySlotRows`, `ITEM_STATE_COPY`, `itemRowState`
- `src/browser/heroTab.js` [04] — exports `renderHeroTab(host, state, deps)` and the moved view models `characterSheetViewModel`, `ABILITY_VIEW_COPY`, `RATIONS_COPY`, `eatsLineFor`, `rationsViewModel`, `grimoireViewModel`
- `src/browser/storeScreen.js` [05] — exports `renderStoreScreen(host, state, deps)` and `STORE_ROLL_COPY`
- `src/browser/bridge.js` [02] — exports `BRIDGE` (the frozen `window.__mz*` registry map: owner / consumers / purpose per name) and `bridgeNames()`
- `src/browser/viewModels.js` [03, 04 — modified] — keeps only the shared view models: `armorDisplay`, `bagArmorText`, `USABLE_COPY`, `usableBy`, `lootCompare`, `dropShelfItems`, `oracleLogViewModel`
- `mazeworld.html` [03, 04, 05 — modified] — `paint()` makes one `window.__mzTabs.gear(...)` and one `window.__mzTabs.hero(...)` call; `renderEncounter()`'s store branch is one `window.__mzTabs.store(...)` call; `tabDeps()` builds the deps object; the module script assigns `window.__mzTabs = Object.freeze({ gear: renderGearTab, hero: renderHeroTab, store: renderStoreScreen })` and `window.__mzCarriedList`
- `test/unit/harness/recordingDom.js`, `test/unit/harness/shellSandbox.js`, `test/unit/shell-tab-snapshots.test.js`, `test/unit/fixtures/shell-snapshots/*.txt` (7) [01] — the DOM-snapshot harness, the standing 3-screen smoke, the BEFORE fixtures
- `test/unit/bridge-registry.test.js` [02], `tools/bridge-doc.mjs` [02], `tools/ident-sweep.mjs` [02 — modified: exports `stripJs`/`stripHtml`, CLI guarded]
- `test/unit/gearTab.test.js` [03], `test/unit/heroTab.test.js` [04], `test/unit/storeScreen.test.js` [05] — the module source-pin suites
- `docs/SHELL-MODULES.md` [02, regenerated 03-05] — the module contract + the generated `## Module bridge` table
- Re-pointed pins [03-05]: `shell-worn-slots`, `shell-gear-toolbar`, `shell-gear-39`, `shell-clarity-43`, `shell-loot-screen`, `shell-spells-40`, `shell-armor-display`, `shell-fight-gate`, `shell-input-guards`, `shell-company-panel`, `shell-abilities`, `shell-party-camp`, `shell-map-store-polish`, `shell-combat-over`, `shell-narration-wiring`, `shell-no-content-copies` (widened), `hp-not-wp`, `characterSheetViewModel`, `armorDisplay`, `rationsViewModel`, `grimoireViewModel`, `itemRowState`, `gear-panels`, `lootCompare`, `usableBy`
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source -> node:vm | The harness executes mazeworld.html's classic script text in a vm context; it is first-party source, not untrusted input |
| fixture files -> test | Committed text compared with assert.equal; never evaluated |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering | test/unit/shell-tab-snapshots.test.js fixtures | medium | mitigate | fixtures are written ONLY under MZ_SNAPSHOT_UPDATE=1; the plain run compares; Plans 03-05 never set the env var (their acceptance criteria grep the git log for a fixture change and require none) |
| T-47-02 | Repudiation | BEFORE capture ordering | medium | mitigate | the fixtures are committed in this plan (wave 1) before any render body moves; `git log --oneline -1 -- <fixture>` is an acceptance criterion |
| T-47-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs anywhere in Phase 47 (node built-ins + repo imports only); the package legitimacy gate is not applicable |
</threat_model>

<verification>
- `node --test test/unit/shell-tab-snapshots.test.js` -> `# fail 0`, seven fixtures compared (not written)
- `npm test` -> `# fail 0`; `npm run build:www` exit 0; `npm run boot:check` four PASS
- Fence: `git status --porcelain engine/ content/ test/parity/` empty; master hash unchanged
</verification>

<success_criteria>
- Seven BEFORE fixtures committed, captured from engine-built deterministic states through the real classic `paint()` running under node:vm with the real bridges wired
- The standing snapshot test compares byte-for-byte, proves store render idempotency and serializer determinism, and fails loudly on a missing fixture
- No file under `engine/`, `content/`, `test/parity/` changed; `mazeworld.html` unchanged in this plan (`git diff --stat HEAD~N -- mazeworld.html` empty for this plan's commits)
</success_criteria>

<output>
Create `.planning/phases/47-shell-modularisation/47-01-SUMMARY.md` when done. It MUST carry: the stub list the A4 iteration added (each with the shell function that required it), the wired-bridge list, the seven fixture sizes (`wc -c`), the `npm test` totals before/after (3,231 -> N), the `boot:check` output, the fence outputs, and a `## Human verification (deferred to end of run)` section stating that nothing user-visible changed in this plan (harness + fixtures only).
</output>
