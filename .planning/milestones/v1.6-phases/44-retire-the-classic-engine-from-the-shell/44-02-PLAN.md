---
phase: 44-retire-the-classic-engine-from-the-shell
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - mazeworld.html
  - test/unit/shell-combat-actions.test.js
  - test/unit/shell-combat-screen.test.js
  - test/unit/shell-fight-log.test.js
  - test/unit/shell-input-guards.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-party-camp.test.js
  - test/unit/shell-armor-display.test.js
autonomous: true
requirements: [DEAD-01, DEAD-03]
tags: [dead-code, shell, movement-engine, orphaned-tables]

must_haves:
  truths:
    - "After layer 2 the classic movement/encounter engine (move, newDay, teleport, reveal, die, the find/trap/chest/encounter helpers) has zero word-boundary references in mazeworld.html outside comments and strings; the two surviving bare move(...) calls in tapStep() and the keydown handler read window.move(...) explicitly; the else-makeCamp() fallback is gone"
    - "After layer 3 every classic table and helper that only the deleted engine reached (the bestiary/encounter/treasure/skill/chargen/combat-math tables and helpers listed in Task 2) is gone with zero references, and the classic copies of content/ exports that only dead code read (BESTIARY, ENCOUNTER_TABLES, ENC_TYPES, ENC_ALIAS, SPELLS, MU_CHART, CLASSES, STRIKE_DICE, KIT, FREE_SKILL, CLOAKS, JEWELRY, STAVES, POTIONS, FOODS, TRAPS, AFFLICTIONS, FAERIE, MISC_MAGIC, MAGIC_ARMOR_TABLE, ARMORS, WEAPON_MAX, WEAPON_TYPE_TABLE, WEAPON_BONUS_TABLE, SPELL_LEVEL_TABLE, CLIMB_TABLE, LEAP_TABLE, DIRECTION_TABLE, TEMPERAMENTS, MOTIVES, PHOBIAS, NAMES, EPITAPHS, INSANITY, BLADE_NAMES) no longer exist in the classic script"
    - "The five shell tests whose region end-anchor was `function vitalsStrip()` and the one anchored on the classic `function move(dir)` slice the same live code as before via the next surviving declaration (`function wireDeathConfirm()`, `function eff(key)`), and the CLOAKS/Cloak-of-Armor txt mirror case is deleted with its reason recorded"
    - "npm run build:www exits 0, npm run boot:check passes all four checks, and npm test ends with '# fail 0' at BOTH commit boundaries of this plan"
    - statement: "Deleting layers 2 and 3 changes the outcome or relative order of no surviving test: node --test reports '# fail 0' at each commit and the suite count changes only by the one CLOAKS-mirror case deleted (recorded in the SUMMARY)"
      verification: backstop
  artifacts:
    - path: "mazeworld.html"
      provides: "the shell without the classic movement/encounter engine and its orphaned tables (≤ 6,900 lines after this plan)"
      contains: "window.move(res.dir)"
  key_links:
    - from: "mazeworld.html tapStep()"
      to: "module window.move = function engineMove(dir)"
      via: "explicit bridge call"
      pattern: "window\\.move\\(res\\.dir\\)"
    - from: "mazeworld.html camp sheet SLEEP handler"
      to: "module window.mzMakeCamp"
      via: "single bridge call, no classic fallback"
      pattern: "closeCampSheet\\(\\);\\s*window\\.mzMakeCamp\\(\\)"
  prohibitions:
    - statement: "MUST NOT change any rule outcome or any byte under engine/, content/, test/parity/fixtures/ or test/parity/prototype-master.js.txt — the game plays identically through engine/"
      status: resolved
      verification: test
    - statement: "MUST NOT keep or add any 'else classicX()' / just-in-case fallback or a second code path for the same player action — the camp sheet calls window.mzMakeCamp() and nothing else"
      status: resolved
      verification: test
    - statement: "MUST NOT delete or weaken a live persistence path or the graves sentinel block; MUST NOT delete a declaration the sweep reports as referenced from live code (record it instead)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT re-point a shell pin by widening it into a no-op — every re-anchored region must still contain the code its assertions were written for (same start anchor, next surviving end anchor)"
      status: resolved
      verification: judgment
---

<objective>
Delete deletion layers 2 and 3 of the classic engine from `mazeworld.html` (DEAD-01): first the classic movement/encounter engine (`move`, `newDay`, `teleport`, `reveal`, `die`, `winGame`, the trap/chest/find/faerie/affliction helpers, the treasure rollers, the epitaph tables) while rewriting the two surviving bare `move(` calls to the explicit `window.move(` bridge and collapsing the `else makeCamp()` fallback; then the orphaned classic tables and combat-math/chargen helpers that only the deleted engine ever read. Each layer is its own green commit; the six shell pins these layers break are re-anchored in the same commit (DEAD-03's "each former tripwire re-pointed … `shell-map-invariants` stays green").

Purpose: layer 1 (Plan 44-01) left ~90 orphaned declarations behind by design (one layer per commit). This plan removes them in two bisectable cuts, so any boot regression points at one commit.
Output: `mazeworld.html` ≤ ~6,900 lines; 7 test files re-anchored; `44-02-SUMMARY.md` with the layer-2 and layer-3 deleted-symbol lists.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-CONTEXT.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-01-SUMMARY.md
</context>

<phase_facts>
- Line numbers below are `ba45dfd` numbers (the map); after Plan 44-01 they have shifted — always locate by name with `grep -n` first.
- Gates from Plan 44-01: `node tools/shell-sweep.mjs refs <names>` (must exit 0 with `refs NAME: 0` for every deleted name), `node tools/shell-sweep.mjs orphans` (advisory seed), `npm run build:www`, `npm run boot:check` (4 PASS), `npm test` (`# fail 0`).
- Object-literal keys are not references: `COMBAT_DISPATCH` (`flee:`, `sing:` …), `CAUSE_TEXT`-style tables, `COMBAT_COPY` (`bury:`), and `RACES` rows (`toHit:`) share names with deleted functions — the sweep tool already excludes keys.
- The graves sentinel block (`/* @gsd:dual-write-convergence-extract:graves:start */` … `:end */`, `ba45dfd` L5275–L5330: `GRAVE_KEY`, `graves`, `loadGraves`, `saveGraves`) is LIVE by ruling and untouched in this plan; `bury()` (L5353–5364, AFTER the block) is dead and goes in layer 2.
- Scope fence unchanged: no bytes under `engine/`, `content/`, `test/parity/fixtures/`, `test/parity/prototype-master.js.txt`.
</phase_facts>

<flagged_assumptions>
- **A-1 (carried from 44-01, DEAD-01 "unclassified"):** the per-layer name lists below are the planner's reachability estimate. A name that `shell-sweep refs` reports as still referenced from live code after deletion is restored (with its comment) and listed in the SUMMARY under "kept, with the live reference" — never force-deleted.
- **A-3:** `newBeat`/`CAPTURE` (L2830–2842) may or may not survive layer 2 (`beginEvent` was their classic caller; the module's `mzCombatReport` path may call `newBeat` through `window`). Decide by `refs` after the layer-2 deletion; do not pre-judge.
</flagged_assumptions>

<artifacts_this_phase_produces>
- (Plan 44-01) `tools/shell-boot-check.mjs`, `tools/shell-sweep.mjs`, `package.json` `boot:check`, the 504-case matrix describe in `test/unit/parley.test.js`.
- (Plan 44-03) `window.__mzTables` bridge global; `test/unit/shell-no-content-copies.test.js`.
- (Plan 44-04) the two "Delve resumed." lines re-homed in the module boot path.
- This plan creates no new symbols; it deletes and re-anchors. Re-anchored test regions keep their existing describe names.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Deletion layer 2 — the classic movement/encounter engine, the explicit window.move bridge calls, the else-makeCamp fallback, and the six pins it breaks</name>
  <files>mazeworld.html, test/unit/shell-combat-actions.test.js, test/unit/shell-combat-screen.test.js, test/unit/shell-fight-log.test.js, test/unit/shell-input-guards.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-party-camp.test.js</files>
  <read_first>
    - mazeworld.html (ba45dfd ranges): L2686–2711 (`reveal` + comment), L2843–2844 (`beginEvent`, `evt`), L2228–2345 (`EPITAPHS` table + `epitaphFor`), L3611–3713 (`/* movement */` banner, `DIRV`, `OPP`, `move`), L3714–3769 (`newDay`), L3796–3853 (`teleport`, `bestTeleportDir`), L3867–3877 (`winGame`), L3896–3946 (`giveItem`, `LOOT_DIVISOR`, `gainWilmst`, `hasPicks`, `rollJewel`, `rollCloak`, `rollStaff`, `rollBlade`, `rollMailPiece`, `rollTreasureItem`), L4326–4516 (`springTrap`, `openChest`, `encounterDot`, `tableFour`, `findFood`, `findGrimoire`, `findGear`, `findMisc`, `meetFaerie`, `catchAffliction`, `goInsane`, `newPhobia`, `fallDark`), L5234–5272 (`CAUSE_TEXT`, `die`, `epitaphCtx`), L5353–5364 (`bury`), L5525–5533 (`vitalsStrip`), L6868–6878 (the camp sheet SLEEP handler with `else makeCamp()`), L6958–6964 (`tapStep` tail: `move(res.dir)`), L7148–7151 (keydown: `move(dirKeys[k])`)
    - mazeworld.html L5544–5556 (`wireDeathConfirm` — the next surviving declaration after `vitalsStrip`; NOTE its own `newGame()` fallback is Plan 44-04's, not this task's), L3881–3895 (`eff` — the next surviving declaration after `paint()` once `DIRV`/`OPP`/`move`…`winGame` are gone)
    - test/unit/shell-combat-actions.test.js L89, test/unit/shell-combat-screen.test.js L92, test/unit/shell-fight-log.test.js L92, test/unit/shell-input-guards.test.js L67, test/unit/shell-loot-screen.test.js L86 (the `function vitalsStrip()` end anchors), test/unit/shell-party-camp.test.js L140 (`"\nfunction move(dir)"` end anchor), test/unit/shell-terrain-41.test.js L133–146 (tolerant of a missing classic `move`/`reveal` — no edit expected; confirm green)
  </read_first>
  <action>
    Delete from the classic script, each with the doc comment attached directly above it: `reveal`, `beginEvent`, `evt`, `EPITAPHS`, `epitaphFor`, the `/* ---------------- movement ---------------- */` banner together with `DIRV`, `OPP`, `move`, `newDay`, `teleport`, `bestTeleportDir`, `winGame` (the classic shell copy only — the engine's `winGame`/`state.won` is Phase 46 DEAD-04), `giveItem`, `LOOT_DIVISOR`, `gainWilmst`, `hasPicks`, `rollJewel`, `rollCloak`, `rollStaff`, `rollBlade`, `rollMailPiece`, `rollTreasureItem`, `springTrap`, `openChest`, `encounterDot`, `tableFour`, `findFood`, `findGrimoire`, `findGear`, `findMisc`, `meetFaerie`, `catchAffliction`, `goInsane`, `newPhobia`, `fallDark`, `CAUSE_TEXT`, `die`, `epitaphCtx`, `bury`, `vitalsStrip`. Leave `makeCamp` and `descend` already gone (layer 1); leave `newGame`/`classicNewGame`/`save`/`load`/`SAVE_KEY` and the five `newGame()` fallback sites for Plan 44-04; leave the graves sentinel block untouched.

    Rewrite the two surviving bare calls that resolve to the module override so the shell reads honestly: in `tapStep()` replace `move(res.dir);` with `window.move(res.dir);` and shorten the two-line comment above it to state that `window.move` is the module's `engineMove` bridge (the classic `move()` no longer exists); in the keydown handler replace `move(dirKeys[k])` with `window.move(dirKeys[k])`. In the camp sheet SLEEP handler replace the conditional `if (window.mzMakeCamp) window.mzMakeCamp(); else makeCamp();` with the single call `window.mzMakeCamp();` (greenfield: a missing bridge is a boot bug to surface).

    Re-anchor the pins this layer breaks, same commit: in `shell-combat-actions.test.js`, `shell-combat-screen.test.js`, `shell-fight-log.test.js`, `shell-input-guards.test.js` change the `sliceBetween(CODE, "let encRenderedAt = 0;", "function vitalsStrip()")` end anchor to `"function wireDeathConfirm()"`; in `shell-loot-screen.test.js` change `CODE.indexOf("function vitalsStrip()")` to `CODE.indexOf("function wireDeathConfirm()")`; in `shell-party-camp.test.js` change the `paintRegion` end anchor `"\nfunction move(dir)"` to `"\nfunction eff(key)"` (the first surviving top-level declaration after `paint()`). Each region keeps its start anchor, so it still contains exactly the live code its assertions were written for.

    Gates, in order: `node tools/shell-sweep.mjs refs reveal beginEvent evt EPITAPHS epitaphFor DIRV OPP move newDay teleport bestTeleportDir winGame giveItem LOOT_DIVISOR gainWilmst hasPicks rollJewel rollCloak rollStaff rollBlade rollMailPiece rollTreasureItem springTrap openChest encounterDot tableFour findFood findGrimoire findGear findMisc meetFaerie catchAffliction goInsane newPhobia fallDark CAUSE_TEXT die epitaphCtx bury vitalsStrip` — NOTE `move` and `die` must report 0 as bare identifiers; `window.move` is excluded from the count only if the tool is told so — run `refs move` and confirm the only hits are `window.move(...)` bridge calls and the module's `window.move = function engineMove` assignment, then list those hits in the SUMMARY as the expected survivors (or extend the tool with a `--ignore-window` flag for this one check); then `node tools/shell-sweep.mjs refs newBeat CAPTURE` to decide A-3 (delete both only if 0 outside their own declarations); `npm run build:www`; `npm run boot:check`; `npm test`. Record `wc -l < mazeworld.html`. Commit: `refactor(44-02): delete the classic movement/encounter engine (layer 2, DEAD-01); explicit window.move bridge calls; no else-makeCamp fallback`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/shell-sweep.mjs refs reveal beginEvent evt EPITAPHS epitaphFor DIRV OPP newDay teleport bestTeleportDir winGame giveItem LOOT_DIVISOR gainWilmst hasPicks rollJewel rollCloak rollStaff rollBlade rollMailPiece rollTreasureItem springTrap openChest encounterDot tableFour findFood findGrimoire findGear findMisc meetFaerie catchAffliction goInsane newPhobia fallDark CAUSE_TEXT die epitaphCtx bury vitalsStrip && grep -c "window.move(res.dir)" mazeworld.html && npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/shell-sweep.mjs refs <the 41 layer-2 names excluding move>` exits 0 with `refs NAME: 0` for each; `grep -cE "^\s*function move\(dir\)" mazeworld.html` prints `0`; `node tools/shell-sweep.mjs refs move --ignore-window` prints `refs move: 0` and exits 0 (no bare `move(` CALL remains in either script — the tool ignores comments such as "the engine re-rolls synchronously inside move()" that live inside surviving functions, which Phase 48 owns; `window.move(` calls are the expected survivors and are ignored by the flag).
    - `grep -c "window.move(res.dir)" mazeworld.html` prints `1`; `grep -c "window.move(dirKeys\[k\])" mazeworld.html` prints `1`.
    - `grep -c "else makeCamp()" mazeworld.html` prints `0`; `grep -cE "closeCampSheet\(\);\s*$" mazeworld.html` ≥ 1 and the SLEEP handler contains `window.mzMakeCamp();` on its own line (`grep -c "^\s*window.mzMakeCamp();" mazeworld.html` prints `1`).
    - `grep -c "function wireDeathConfirm()" test/unit/shell-combat-actions.test.js test/unit/shell-combat-screen.test.js test/unit/shell-fight-log.test.js test/unit/shell-input-guards.test.js test/unit/shell-loot-screen.test.js` prints `1` for each file; `grep -c 'function eff(key)' test/unit/shell-party-camp.test.js` prints `1`; `grep -c "vitalsStrip" test/unit/*.test.js` prints `0` for every file.
    - The graves sentinel markers are intact: `grep -c "@gsd:dual-write-convergence-extract:graves:start" mazeworld.html` prints `1` and `grep -c "@gsd:dual-write-convergence-extract:graves:end" mazeworld.html` prints `1`; `grep -c "async function saveGraves()" mazeworld.html` prints `1`.
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines; `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `node --test test/unit/shell-map-invariants.test.js test/unit/shell-terrain-41.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing.
  </acceptance_criteria>
  <done>The classic movement/encounter engine is gone with zero references, the shell's two movement entry points call `window.move` explicitly, the camp sheet has a single bridge path, six pins are re-anchored, and build + boot + suite are green at the commit.</done>
</task>

<task type="auto">
  <name>Task 2: Deletion layer 3 — the orphaned classic tables and combat-math/chargen helpers, plus the CLOAKS txt-mirror case</name>
  <files>mazeworld.html, test/unit/shell-armor-display.test.js</files>
  <read_first>
    - mazeworld.html (ba45dfd ranges): L1793–1795 (`D`, `pick`, `clamp` — delete ONLY `pick` here; `D` is still read by the surviving classic `WEAPONS`/`CLASSES` closures until Plan 44-03 bridges `WEAPONS`, and `clamp` is live), L1799 (`STRIKE_DICE`), L1803–1819 (`CLASSES`), L1850–1872 (`WEAPON_MAX`, `WEAPON_TYPE_TABLE`, `WEAPON_BONUS_TABLE`, `ARMORS`, `MAGIC_ARMOR_TABLE`, `priceFor`), L1875–1885 (`KIT`), L1914 (`FREE_SKILL`), L1917–1933 (`rollSkills`, `skillTier` — KEEP `skillTable` L1916, live via `paint()`), L1938–2026 (`BLADE_NAMES`, `JEWELRY`, `CLOAKS`, `STAVES`, `POTIONS`, `FOODS`, `TRAPS`, `AFFLICTIONS`, `FAERIE`, `MISC_MAGIC`, `SPELL_LEVEL_TABLE`, `CLIMB_TABLE`, `LEAP_TABLE`, `DIRECTION_TABLE`), L2042–2051 (`RACE_D8`, `TEMPERAMENTS`, `MOTIVES`, `PHOBIAS` — KEEP `RACES` L2029–2041, live via `R_`), L2055–2135 (`BESTIARY`, `ENC_TYPES`, `ENCOUNTER_TABLES`, `ENC_ALIAS`), L2137–2224 (`SPELLS`, `MU_CHART`, `schoolAllowed`, `schoolGate`, `schoolBonus`, `canLearn`, `rollGrimoire`, `INSANITY` — KEEP `maxCharges` L2225, live via `mzSpellCharges`), L2474–2482 (`NAMES`, `nameFor`), L2568–2583 (`bfs`, `shuffle`), L2909–2962 (`strikeDie`, `toHit`, `inDark`, `climbBonus`, `leapBonus`, `foeDie`, `foeToHitVs`, `weaponDamage`, `levelFromSP` — KEEP `R_` L2908 and `upkeep` L2961, both live via `paint()`)
    - mazeworld.html L3261–3300 and L3340–3360 (`paint()`'s reads of `ROMAN`, `WEAPONS`, `THRESHOLDS`, `R_()`, `skillTable()` — these survivors are Plan 44-03's; do not touch them here)
    - test/unit/shell-armor-display.test.js L155–165 (the "classic (dead) CLOAKS table's Cloak of Armor txt mirrors the live string" case) and L133–140 (`renderDropShelfRegion` — self-adjusting `\nfunction ` end anchor, no edit)
    - test/unit/hp-not-wp.test.js L1–40 (imports the content tables — does not read the classic tables; no edit expected)
  </read_first>
  <action>
    Delete from the classic script, each with the doc comment attached directly above it: `pick`, `STRIKE_DICE`, `CLASSES`, `WEAPON_MAX`, `WEAPON_TYPE_TABLE`, `WEAPON_BONUS_TABLE`, `ARMORS`, `MAGIC_ARMOR_TABLE`, `priceFor`, `KIT`, `FREE_SKILL`, `rollSkills`, `skillTier`, `BLADE_NAMES`, `JEWELRY`, `CLOAKS`, `STAVES`, `POTIONS`, `FOODS`, `TRAPS`, `AFFLICTIONS`, `FAERIE`, `MISC_MAGIC`, `SPELL_LEVEL_TABLE`, `CLIMB_TABLE`, `LEAP_TABLE`, `DIRECTION_TABLE`, `RACE_D8`, `TEMPERAMENTS`, `MOTIVES`, `PHOBIAS`, `BESTIARY`, `ENC_TYPES`, `ENCOUNTER_TABLES`, `ENC_ALIAS`, `SPELLS`, `MU_CHART`, `schoolAllowed`, `schoolGate`, `schoolBonus`, `canLearn`, `rollGrimoire`, `INSANITY`, `NAMES`, `nameFor`, `bfs`, `shuffle`, `strikeDie`, `toHit`, `inDark`, `climbBonus`, `leapBonus`, `foeDie`, `foeToHitVs`, `weaponDamage`, `levelFromSP`. Section banners (`/* ---------------- xxx ---------------- */`) whose every declaration is gone go too; a banner that still heads surviving code stays. Do NOT delete `D`, `clamp`, `skill`, `skillTable`, `R_`, `upkeep`, `maxCharges`, `RACES`, `ROMAN`, `THRESHOLDS`, `WEAPONS`, `FIGHTER_SKILLS`, `THIEF_SKILLS`, `RACE_NOTE`, `CLASS_NOTE`, `SUB_NOTE` in this task (`D` orphans only after 44-03; the nine tables are 44-03's bridge work).

    Before deleting `NAMES` confirm with `node tools/shell-sweep.mjs refs NAMES nameFor` that only their own declarations and `rollCharacter`-era code (already gone) referenced them; before deleting `CLASSES` confirm `refs CLASSES` reports only the classic declaration (the module's `import { RACES, CLASSES, … }` is the module's own binding — the tool's `refs` mode will list that import line: treat the `import {` line as the expected survivor and record it).

    In `test/unit/shell-armor-display.test.js` delete the test case "Phase 28 (ARMOR-04): the classic (dead) CLOAKS table's Cloak of Armor txt mirrors the live string" (reason for the SUMMARY: the classic `CLOAKS` table is deleted; `content/treasure-tables.js` is the single source and `hp-not-wp.test.js` + the treasure unit tests already read it directly — this is the "classic CLOAKS/Cloak of Armor txt mirror" DEAD-03 names). Keep the file's `import { CLOAKS }` only if another case still uses it; otherwise drop the import too.

    Gates: `node tools/shell-sweep.mjs refs pick STRIKE_DICE CLASSES WEAPON_MAX WEAPON_TYPE_TABLE WEAPON_BONUS_TABLE ARMORS MAGIC_ARMOR_TABLE priceFor KIT FREE_SKILL rollSkills skillTier BLADE_NAMES JEWELRY CLOAKS STAVES POTIONS FOODS TRAPS AFFLICTIONS FAERIE MISC_MAGIC SPELL_LEVEL_TABLE CLIMB_TABLE LEAP_TABLE DIRECTION_TABLE RACE_D8 TEMPERAMENTS MOTIVES PHOBIAS BESTIARY ENC_TYPES ENCOUNTER_TABLES ENC_ALIAS SPELLS MU_CHART schoolAllowed schoolGate schoolBonus canLearn rollGrimoire INSANITY NAMES nameFor bfs shuffle strikeDie toHit inDark climbBonus leapBonus foeDie foeToHitVs weaponDamage levelFromSP` (expected survivors to record, not failures: the module's `import { RACES, CLASSES, BAGS, ABILITY_BY_ID }` line and its `import { … toHit, strikeDie, … } from "./engine/derived.js"` line — everything else 0); `npm run build:www`; `npm run boot:check`; `npm test`. Record `wc -l < mazeworld.html`. Commit: `refactor(44-02): delete the orphaned classic tables and combat-math helpers (layer 3, DEAD-01); drop the CLOAKS txt-mirror pin`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node tools/shell-sweep.mjs refs pick STRIKE_DICE WEAPON_MAX WEAPON_TYPE_TABLE WEAPON_BONUS_TABLE ARMORS MAGIC_ARMOR_TABLE priceFor KIT FREE_SKILL rollSkills skillTier BLADE_NAMES JEWELRY CLOAKS STAVES POTIONS FOODS TRAPS AFFLICTIONS FAERIE MISC_MAGIC SPELL_LEVEL_TABLE CLIMB_TABLE LEAP_TABLE DIRECTION_TABLE RACE_D8 TEMPERAMENTS MOTIVES PHOBIAS BESTIARY ENC_TYPES ENCOUNTER_TABLES ENC_ALIAS SPELLS MU_CHART schoolAllowed schoolGate schoolBonus canLearn rollGrimoire INSANITY NAMES nameFor bfs shuffle inDark climbBonus leapBonus foeDie foeToHitVs weaponDamage levelFromSP && npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^<script>$/,/^<script type="module">$/p' mazeworld.html | grep -cE "^\s*(const|let|var|function|async function)\s+(STRIKE_DICE|CLASSES|WEAPON_MAX|WEAPON_TYPE_TABLE|WEAPON_BONUS_TABLE|ARMORS|MAGIC_ARMOR_TABLE|KIT|FREE_SKILL|BLADE_NAMES|JEWELRY|CLOAKS|STAVES|POTIONS|FOODS|TRAPS|AFFLICTIONS|FAERIE|MISC_MAGIC|SPELL_LEVEL_TABLE|CLIMB_TABLE|LEAP_TABLE|DIRECTION_TABLE|TEMPERAMENTS|MOTIVES|PHOBIAS|BESTIARY|ENC_TYPES|ENCOUNTER_TABLES|ENC_ALIAS|SPELLS|MU_CHART|INSANITY|NAMES|EPITAPHS|CAUSE_TEXT)\b"` prints `0` (no classic declaration of these `content/` export names remains in the classic script).
    - `sed -n '/^<script>$/,/^<script type="module">$/p' mazeworld.html | grep -cE "^\s*(const|let|function)\s+(pick|priceFor|rollSkills|skillTier|RACE_D8|schoolAllowed|schoolGate|schoolBonus|canLearn|rollGrimoire|nameFor|bfs|shuffle|strikeDie|toHit|inDark|climbBonus|leapBonus|foeDie|foeToHitVs|weaponDamage|levelFromSP)\b"` prints `0`.
    - Survivors intact: `grep -cE "^const D = " mazeworld.html` prints `1`; `grep -cE "^const R_ = " mazeworld.html` prints `1`; `grep -cE "^function skillTable\(" mazeworld.html` prints `1`; `grep -cE "^function upkeep\(" mazeworld.html` prints `1`; `grep -cE "^const maxCharges = " mazeworld.html` prints `1`; `grep -cE "^const (RACES|ROMAN|THRESHOLDS|WEAPONS|FIGHTER_SKILLS|THIEF_SKILLS|RACE_NOTE|CLASS_NOTE|SUB_NOTE)\s+= " mazeworld.html` prints `9` (note `ROMAN`/`THRESHOLDS` are column-aligned with extra spaces before `=`).
    - `grep -c "Cloak of Armor txt mirrors" test/unit/shell-armor-display.test.js` prints `0`.
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines; `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `node --test test/unit/shell-map-invariants.test.js test/unit/hp-not-wp.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `wc -l < mazeworld.html` prints a number ≤ 6900 (record the exact value; the phase target ≤ 6710 is Plan 44-04's closing gate).
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing.
  </acceptance_criteria>
  <done>Every orphaned classic table and helper is gone with zero references (the module's own import lines recorded as the expected survivors), the nine bridge-bound tables and the live helpers remain, the CLOAKS mirror pin is deleted with its reason, and the gates are green at the commit.</done>
</task>

</tasks>

<verification>
- Two commits (layer 2, layer 3), each with `npm run build:www` exit 0, `npm run boot:check` 4 PASS, `npm test` `# fail 0`, `shell-map-invariants` green.
- No bare `move(` call, no `else makeCamp()`, no classic `function move(dir)`.
- Engine gate diff empty.
</verification>

<success_criteria>
- Layer-2 and layer-3 name lists deleted with `refs 0` each (expected import-line survivors recorded, not counted as failures).
- `mazeworld.html` ≤ 6,900 lines after this plan.
- Seven test files re-anchored/trimmed with reasons recorded; suite green at both commits.
</success_criteria>

<output>
Create `.planning/phases/44-retire-the-classic-engine-from-the-shell/44-02-SUMMARY.md` with:
1. `## Deleted symbols (layer 2)` and `## Deleted symbols (layer 3)` — every declaration deleted, with its `ba45dfd` line range, and a `### Kept, with the live reference` list per layer (A-1) including the A-3 decision on `newBeat`/`CAPTURE`.
2. `## Tests re-pointed or deleted` — one reason line each: `shell-combat-actions`, `shell-combat-screen`, `shell-fight-log`, `shell-input-guards`, `shell-loot-screen` (end anchor `function vitalsStrip()` → `function wireDeathConfirm()`), `shell-party-camp` (end anchor `\nfunction move(dir)` → `\nfunction eff(key)`), `shell-armor-display` (CLOAKS txt-mirror case deleted — classic `CLOAKS` gone, `content/treasure-tables.js` single source).
3. `## Gate outputs` — commands + results at both commits: sweep `refs` (all 0, with the expected import-line survivors listed), `npm run build:www`, `npm run boot:check`, `npm test` (`# fail 0`, count), `wc -l < mazeworld.html` after each layer, engine-gate diff (empty).
4. `## Human verification (deferred to end of run)` — Pixel 7: tap-to-move still steps the party (window.move bridge); keyboard arrows on a hardware/BT keyboard still move (keydown path); MAKE CAMP → SLEEP still camps through the engine (no fallback); a trap / chest / find / faerie encounter reached by walking still resolves through the engine rail cards.
5. `## Flagged assumptions` — A-1 status, A-3 decision.
</output>
