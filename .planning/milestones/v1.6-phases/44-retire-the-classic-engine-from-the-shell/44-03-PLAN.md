---
phase: 44-retire-the-classic-engine-from-the-shell
plan: 03
type: execute
wave: 3
depends_on: ["44-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-no-content-copies.test.js
  - test/unit/shell-company-panel.test.js
autonomous: true
requirements: [DEAD-02]
tags: [dead-code, shell, content-single-source, dossier, source-pin]

must_haves:
  truths:
    - "The Hero-tab dossier renders window.__mzTables.RACE_NOTE[c.race], window.__mzTables.CLASS_NOTE[c.cls] and window.__mzTables.SUB_NOTE[c.sub] — the module assigns window.__mzTables from content/index.js imports before boot(); the classic RACE_NOTE/CLASS_NOTE/SUB_NOTE tables (13 drifted SUB_NOTE rows, 3 drifted RACE_NOTE rows) no longer exist"
    - "Dossier byte-equality is JavaScript string equality (===) against the strings content/flavor.js exports — no normalization, no trimming: the pin test asserts tables.SUB_NOTE[k] === flavor.SUB_NOTE[k] for every one of the 24 keys (and the 6 RACE_NOTE / 3 CLASS_NOTE keys) and that none of those exported strings appears verbatim anywhere in mazeworld.html"
    - "Every one of the 24 SUB_NOTE keys, 6 RACE_NOTE keys and 3 CLASS_NOTE keys resolves through the bridge (Object.keys(...).length asserted 24/6/3); a sub-class with no row cannot occur because the dossier is keyed by c.sub over the same 24-key object engine chargen draws from"
    - "mazeworld.html declares no const/let/var/function whose name is any of content/index.js's 85 export names, in either script (the module's import bindings are not declarations) — test/unit/shell-no-content-copies.test.js fails if any such declaration is ever added back"
    - "The surviving classic renderers read ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS and RACES through window.__mzTables (the shell's Hero/Gear/graves/rail text now shows the content/ values — the classic copies of WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS and RACES had drifted text)"
  artifacts:
    - path: "test/unit/shell-no-content-copies.test.js"
      provides: "DEAD-02 source pin: no classic copy of any content/ export; dossier + survivors read via window.__mzTables; 24/6/3 byte-equality"
      min_lines: 60
    - path: "mazeworld.html"
      provides: "window.__mzTables bridge assignment in the module; bridge reads in paint()/skillTable()/R_/renderGraves/renderPartyRoster/renderEncounter/renderRail/mzCombatReport"
      contains: "window.__mzTables = "
  key_links:
    - from: "mazeworld.html module script"
      to: "content/index.js"
      via: "separate import line for the eight bridged tables (RACES reuses the existing import)"
      pattern: "import \\{ RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS \\} from \"\\./content/index\\.js\";"
    - from: "mazeworld.html classic dossier render"
      to: "window.__mzTables"
      via: "bridge reads"
      pattern: "window\\.__mzTables\\.SUB_NOTE\\[c\\.sub\\]"
  prohibitions:
    - statement: "MUST NOT change any byte under engine/ or content/ — content/flavor.js is canon; the 13 drifted classic SUB_NOTE rows and 3 drifted RACE_NOTE rows are discarded, never merged back into content/"
      status: resolved
      verification: test
    - statement: "MUST NOT carve the dossier or Hero tab into a module (Phase 47 heroTab.js) — this plan only swaps the data source behind the existing classic render"
      status: resolved
      verification: judgment
    - statement: "MUST NOT preserve any classic table as a fallback ('|| classic copy') or add a per-table second bridge — one module-assigned window.__mzTables object, read lazily inside functions, no parse-time reads"
      status: resolved
      verification: test
    - statement: "MUST NOT weaken the pin into a name-list check that a future table copy could dodge — the pin derives its name set from Object.keys(content/index.js) at test time"
      status: resolved
      verification: judgment
---

<objective>
Make `content/` the single source for every table the shell still renders (DEAD-02): the module hands `RACE_NOTE`, `CLASS_NOTE`, `SUB_NOTE`, `ROMAN`, `THRESHOLDS`, `WEAPONS`, `FIGHTER_SKILLS`, `THIEF_SKILLS` and `RACES` across on one bridge object (`window.__mzTables`, the established `__mzState`/`__mzBags`/`__mzIconMap` pattern), the nine classic copies are deleted, the Hero-tab dossier and the other surviving readers read the bridge, and a new source-pin test fails if `mazeworld.html` ever declares a copy of any `content/` export again — with a 24-way byte-equality check on the sub-class dossier strings.

Purpose: ROADMAP success criterion 3. The classic `SUB_NOTE` had 13 rows drifted from `content/flavor.js`; the planner's drift probe also found the classic `RACE_NOTE` (3 rows), `WEAPONS` (labels), `FIGHTER_SKILLS` (13 rows), `THIEF_SKILLS` (8 rows) and `RACES` (3 rows) drifted — the Hero/Gear tabs have been showing stale text. After this plan the shell cannot drift because it holds no copy.
Output: `mazeworld.html` with `window.__mzTables`; `test/unit/shell-no-content-copies.test.js`; `shell-company-panel.test.js` re-pointed; `44-03-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-CONTEXT.md
@.planning/phases/44-retire-the-classic-engine-from-the-shell/44-02-SUMMARY.md
@content/flavor.js
@content/index.js
@test/unit/shell-map-invariants.test.js
</context>

<phase_facts>
- `content/index.js` re-exports 85 names (`export * from` every content module); all nine bridged tables are among them. `RACES` and `CLASSES` are ALREADY imported by the module on the byte-pinned line `import { RACES, CLASSES, BAGS, ABILITY_BY_ID } from "./content/index.js";` (`shell-abilities.test.js` L106) — add a SEPARATE import line for the other eight (the repo's convention for pinned import lines, see the comment above `ba45dfd` L7280).
- Field compatibility (verified): the shell reads `w.lab` from `WEAPONS` rows, `.cost`/`.txt` from skill rows, `.dmg`/`.wpnBonus`/`.upkeep`/`.note` from `RACES` rows — all present with the same names in `content/`. `content/` `WEAPONS` rows use `dice: {n, sides, bonus}` instead of the classic `d: () => D(n)` closure; nothing live in the shell calls `w.d()`.
- Bridge timing (verified): every surviving classic read is inside a function (`paint`, `skillTable`, `R_`, `upkeep`, `renderGraves`, `renderPartyRoster`, `renderEncounter`, `renderRail`, `mzCombatReport`, the classic boot's resume line); the module's first `window.paint()` (`ba45dfd` L7668) runs after all bridge assignments. The top-level `loadGraves().then(() => renderGraves())` at `ba45dfd` L1784 rejects at parse time (`window.mzStorage` undefined) and is caught — unchanged.
- Line numbers are `ba45dfd` numbers; locate by name.
</phase_facts>

<flagged_assumptions>
- **A-1 (carried):** the survivor set {ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES} is the planner's measurement after Plans 44-01/02; run `node tools/shell-sweep.mjs orphans` and `grep -nE "^const (ROMAN|THRESHOLDS|WEAPONS|FIGHTER_SKILLS|THIEF_SKILLS|RACES|RACE_NOTE|CLASS_NOTE|SUB_NOTE)\s+="` first — any classic declaration named like a `content/` export that is still live goes on the bridge; any that is dead is deleted outright (no bridge key for dead tables).
- **A-4:** after this plan the classic `D` die helper (`const D = n => 1 + Math.floor(Math.random() * n)`) should have no live readers (its last were the deleted `WEAPONS`/`CLASSES` closures) — its deletion is Plan 44-04's fixed-point sweep, decided by `refs D`, not here.
</flagged_assumptions>

<artifacts_this_phase_produces>
- `window.__mzTables` — module-assigned, frozen, read-only bridge object `{ RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES }` (this plan; Phase 47's SHELL-04 bridge registry will list it).
- `test/unit/shell-no-content-copies.test.js` — new DEAD-02 source-pin test (this plan). Test names: "DEAD-02: mazeworld.html declares no copy of any content/ export (classic or module script)", "DEAD-02: the Hero-tab dossier reads RACE_NOTE/CLASS_NOTE/SUB_NOTE only through window.__mzTables", "DEAD-02: the module assigns window.__mzTables from content/index.js before boot()", "DEAD-02: every surviving classic read of ROMAN/THRESHOLDS/WEAPONS/FIGHTER_SKILLS/THIEF_SKILLS/RACES goes through window.__mzTables", "DEAD-02: all 24 SUB_NOTE, 6 RACE_NOTE and 3 CLASS_NOTE dossier strings are byte-equal (===) to content/flavor.js and appear nowhere in mazeworld.html".
- (Plan 44-01) `tools/shell-boot-check.mjs`, `tools/shell-sweep.mjs`, `boot:check`, the 504-case parley matrix. (Plan 44-04) the re-homed "Delve resumed." lines.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Bridge the nine content/ tables across as window.__mzTables, rewrite every surviving classic read, delete the nine classic copies</name>
  <files>mazeworld.html, test/unit/shell-company-panel.test.js</files>
  <read_first>
    - mazeworld.html module script: L7278–7282 (the pinned `content/index.js` import line and the convention comment above it), L7300–7316 (`window.__mzBags = BAGS;` — the bridge-assignment comment style to copy), L7640–7670 (module boot: bridges are all assigned before `boot()` / first `paint()`)
    - mazeworld.html classic script (ba45dfd): L1799–1801 (`STRIKE_DICE` gone in 44-02; `THRESHOLDS`, `ROMAN`), L1821–1849 (`WEAPONS`), L1889–1916 (`FIGHTER_SKILLS`, `THIEF_SKILLS`, `skillTable`), L2029–2041 (`RACES`), L2431–2472 (`RACE_NOTE`, `CLASS_NOTE`, `SUB_NOTE` + the "Maze Master's opinion of you" banner), L2884–2902 (`mzCombatReport` reads `ROMAN`), L2908 (`const R_ = () => RACES[S.c.race];`), L3293–3296 (`paint()` `s-level` reads `ROMAN`), L3344–3358 (`paint()` reads `WEAPONS`, `R_()`, `THRESHOLDS`), L3520–3526 (`paint()` `wornRow` reads `WEAPONS`), L3586–3594 (the dossier loop reading `RACE_NOTE`/`CLASS_NOTE`/`SUB_NOTE`), L3405–3410 (`paint()` calls `skillTable(c.cls)`), L5397–5400 (`renderGraves` reads `ROMAN`), L5637–5640 (`renderPartyRoster` reads `ROMAN`), L6267–6270 (`renderEncounter` won branch reads `ROMAN`), L6536–6539 (`renderRail` reads `ROMAN`), L7172–7175 (classic boot resume line reads `ROMAN` — rewrite to the bridge here; Plan 44-04 moves the line into the module)
    - test/unit/shell-company-panel.test.js L35–45 (imports `SUB_NOTE` from content/flavor.js), L68–96 (`subNoteRegion` sliced between `const SUB_NOTE = {` and `const NAMES = {`, and the "classic Cutthroat row is byte-identical" case), L155–166 (the `ROMAN[lvl - 1]` regex pin — still matches the bridge read)
    - content/index.js (export list), content/flavor.js L24–66 (the three tables), content/races.js, content/weapons.js L50–60, content/skills.js (field names)
  </read_first>
  <action>
    Module script: directly below the pinned line `import { RACES, CLASSES, BAGS, ABILITY_BY_ID } from "./content/index.js";` add a NEW line `import { RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS } from "./content/index.js";` preceded by a two-line comment in the file's convention ("Phase 44 (DEAD-02) — a SEPARATE import line so the pinned line above stays byte-identical"). Next to `window.__mzBags = BAGS;` add `window.__mzTables = Object.freeze({ RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES });` with a comment in the `__mzBags` style stating: read-only display data handed to the classic renderers that cannot `import`; the ENGINE reads `content/` directly; `content/` is the single source (DEAD-02); the classic copies are gone.

    Classic script: rewrite every surviving read to the bridge — `skillTable(cls)` returns `window.__mzTables.FIGHTER_SKILLS` / `window.__mzTables.THIEF_SKILLS`; `const R_ = () => window.__mzTables.RACES[S.c.race];`; every `ROMAN[` read in `mzCombatReport`, `paint()`, `renderGraves`, `renderPartyRoster`, `renderEncounter`, `renderRail` and the classic boot resume line becomes `window.__mzTables.ROMAN[`; `THRESHOLDS[c.level]` in `paint()` becomes `window.__mzTables.THRESHOLDS[c.level]`; both `WEAPONS[` reads in `paint()` become `window.__mzTables.WEAPONS[` (keep the `|| ...WEAPONS["Club"]` default); the dossier loop's three cells become `window.__mzTables.RACE_NOTE[c.race]`, `window.__mzTables.CLASS_NOTE[c.cls]`, `window.__mzTables.SUB_NOTE[c.sub]` (the loop, labels and `${text || ""}` markup stay exactly as they are — this is a data-source swap, not a carve-out). Then delete the nine classic declarations `THRESHOLDS`, `ROMAN`, `WEAPONS`, `FIGHTER_SKILLS`, `THIEF_SKILLS`, `RACES`, `RACE_NOTE`, `CLASS_NOTE`, `SUB_NOTE` with the comments attached to them (including the "the Maze Master's opinion of you" banner). No local alias like `const ROMAN = window.__mzTables.ROMAN` anywhere in the classic script — that would be a parse-time read before the module runs AND a re-declared content name.

    `test/unit/shell-company-panel.test.js`: delete the "SUB_NOTE: the classic Cutthroat row is byte-identical to content/flavor.js and states the odds in plain words" case and the `subNoteRegion` helper it used (reason for the SUMMARY: the classic `SUB_NOTE` no longer exists; `shell-no-content-copies.test.js` proves the stronger property for all 24 rows); keep the `SUB_NOTE` import only if another case in the file still reads it, else drop it; leave the `ROMAN[lvl - 1]` regex pin (it still matches the bridge read).

    Gates: `node tools/shell-sweep.mjs refs RACE_NOTE CLASS_NOTE SUB_NOTE ROMAN THRESHOLDS WEAPONS FIGHTER_SKILLS THIEF_SKILLS RACES` — expected survivors to record: the two module import lines, the `window.__mzTables = Object.freeze({...})` assignment, and every `window.__mzTables.NAME` read (the tool counts `window.NAME` only when NAME directly follows `window.`, so `window.__mzTables.ROMAN` is not a `ROMAN` reference — confirm the tool's report lists only import/assignment lines); `npm run build:www`; `npm run boot:check` (the `painted` check reads `id="s-level">Lvl ` — proof the `ROMAN` bridge read works at boot); `npm test`. Commit: `refactor(44-03): content/ is the single source — window.__mzTables bridge, nine classic table copies deleted (DEAD-02)`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && grep -c 'window.__mzTables = Object.freeze({ RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES });' mazeworld.html && grep -c 'window.__mzTables.SUB_NOTE\[c.sub\]' mazeworld.html && npm run build:www && npm run boot:check && npm test 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS } from "./content/index.js";' mazeworld.html` prints `1` and `grep -c 'import { RACES, CLASSES, BAGS, ABILITY_BY_ID } from "./content/index.js";' mazeworld.html` prints `1` (pinned line untouched).
    - `grep -c 'window.__mzTables = Object.freeze({ RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES });' mazeworld.html` prints `1`, and that line's number is smaller than the line number of `const engineState = await boot(freshSeed);` (`grep -n` both; assignment precedes boot).
    - `grep -c 'window.__mzTables.RACE_NOTE\[c.race\]' mazeworld.html`, `grep -c 'window.__mzTables.CLASS_NOTE\[c.cls\]' mazeworld.html`, `grep -c 'window.__mzTables.SUB_NOTE\[c.sub\]' mazeworld.html` each print `1`; `grep -cE "(^|[^.A-Za-z_])(RACE_NOTE|CLASS_NOTE|SUB_NOTE)\[" mazeworld.html` prints `0` (no bare table read remains).
    - `sed -n '/^<script>$/,/^<script type="module">$/p' mazeworld.html | grep -cE "^\s*(const|let|var|function|async function)\s+(RACE_NOTE|CLASS_NOTE|SUB_NOTE|ROMAN|THRESHOLDS|WEAPONS|FIGHTER_SKILLS|THIEF_SKILLS|RACES)\b"` prints `0`.
    - `grep -cE "(^|[^.A-Za-z_])ROMAN\[" mazeworld.html` prints `0` and `grep -c "window.__mzTables.ROMAN\[" mazeworld.html` prints a number ≥ 6 (mzCombatReport, paint, renderGraves, renderPartyRoster, renderEncounter, renderRail, plus the classic boot resume line until 44-04); `grep -cE "(^|[^.A-Za-z_])(WEAPONS|THRESHOLDS|RACES|FIGHTER_SKILLS|THIEF_SKILLS)\[" mazeworld.html` prints `0`.
    - `grep -c "const SUB_NOTE = {" test/unit/shell-company-panel.test.js` prints `0`; `grep -c "ROMAN\\\\\[lvl - 1\\\\\]" test/unit/shell-company-panel.test.js` prints `1` (the regex pin is kept as-is).
    - `npm run build:www` exits 0; `npm run boot:check` prints four `PASS` lines (`painted` proves the bridge is live at first paint); `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `node --test test/unit/shell-map-invariants.test.js test/unit/shell-company-panel.test.js test/unit/shell-abilities.test.js test/unit/hp-not-wp.test.js 2>&1 | grep -E "^# fail 0"` prints `# fail 0`.
    - `git diff --stat ba45dfd -- engine/ content/ test/parity/fixtures/ test/parity/prototype-master.js.txt` prints nothing.
  </acceptance_criteria>
  <done>One bridge object carries the nine tables from `content/` to the classic renderers; no classic copy remains; the dossier and every surviving read go through `window.__mzTables`; build, headless boot and the suite are green.</done>
</task>

<task type="auto">
  <name>Task 2: The DEAD-02 source-pin test — no second copy of any content/ export, and 24/6/3 dossier byte-equality</name>
  <files>test/unit/shell-no-content-copies.test.js</files>
  <read_first>
    - test/unit/shell-map-invariants.test.js L1–60 (the readFileSync + `stripComments` + `countOf` source-pin style to mirror; note its comment-stripping order: line comments first, then block comments)
    - test/unit/shell-company-panel.test.js L35–60 (how a shell pin imports a content table and slices a region)
    - content/index.js (all `export *` lines — the pin derives its name set from `Object.keys(await import("../../content/index.js"))`, never from a hand-written list)
    - content/flavor.js L24–66
    - mazeworld.html: the `<script>` / `<script type="module">` / `</script>` column-0 tag lines (region boundaries), the `window.__mzTables = Object.freeze({...})` assignment and the separate import line from Task 1, the dossier loop in `paint()`
  </read_first>
  <action>
    Create `test/unit/shell-no-content-copies.test.js` (node:test + node:assert/strict, ESM) in the readFileSync-regex style, with a header comment stating it is Phase 44 DEAD-02's pin (ROADMAP success criterion 3) and that it must keep failing for ANY future classic copy of a `content/` table. Load `mazeworld.html` (normalize `\r\n`), split it into `classic` (between the column-0 `<script>` line and the next `</script>`) and `mod` (between the column-0 `<script type="module">` line and its `</script>`), and strip comments from each (line comments first, then block comments, as `shell-map-invariants` does). Import `* as content from "../../content/index.js"` and `{ RACE_NOTE, CLASS_NOTE, SUB_NOTE } from "../../content/flavor.js"`.

    Tests (names as listed under "Artifacts this phase produces"): (1) for every `name` of `Object.keys(content)` (assert there are at least 80), assert no line of `classic` or `mod` matches `^\s*(?:const|let|var|function|async function)\s+NAME\b` — report every offender in one message (`import { … }` lines are not declarations and must not match); (2) the dossier pin: `classic` contains exactly one `window.__mzTables.RACE_NOTE[c.race]`, one `window.__mzTables.CLASS_NOTE[c.cls]`, one `window.__mzTables.SUB_NOTE[c.sub]`, and zero bare `RACE_NOTE[` / `CLASS_NOTE[` / `SUB_NOTE[` reads; (3) the bridge pin: `mod` contains exactly one `window.__mzTables = Object.freeze({ RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS, RACES });`, the import line `import { RACE_NOTE, CLASS_NOTE, SUB_NOTE, ROMAN, THRESHOLDS, WEAPONS, FIGHTER_SKILLS, THIEF_SKILLS } from "./content/index.js";`, and the assignment's index is smaller than the index of `await boot(`; (4) the survivors pin: `classic` has zero bare reads matching `(^|[^.\w])(ROMAN|THRESHOLDS|WEAPONS|FIGHTER_SKILLS|THIEF_SKILLS|RACES)\[` and at least one `window.__mzTables.` read for each of the six; (5) the byte-equality pin: assert `Object.keys(SUB_NOTE).length === 24`, `Object.keys(RACE_NOTE).length === 6`, `Object.keys(CLASS_NOTE).length === 3`; build `tables` the way the module does (`{ RACE_NOTE: content.RACE_NOTE, CLASS_NOTE: content.CLASS_NOTE, SUB_NOTE: content.SUB_NOTE }`) and for every key assert `tables.SUB_NOTE[k] === SUB_NOTE[k]` (strict `===`, the same expression the dossier renders), likewise for RACE_NOTE and CLASS_NOTE; and assert that none of those 33 exported strings occurs verbatim in the RAW `mazeworld.html` text (`RAW.includes(str) === false`) — the shell reaches the text only through the bridge. Use `assert.equal(countOf(...), n)` style counts, never regexes that spell a banned declaration whole where a string-concatenation avoids self-matching (mirror shell-map-invariants' discipline for the literal `const SUB_NOTE`).

    Prove the pin has teeth before committing: temporarily append a line `const SUB_NOTE = {};` inside the classic script of a SCRATCH copy of mazeworld.html (write the copy to the OS temp dir, point the test at it via an env var `MZ_HTML` the test honours with `process.env.MZ_HTML || <repo path>`), run `node --test test/unit/shell-no-content-copies.test.js` with `MZ_HTML` set and confirm test (1) FAILS naming `SUB_NOTE`; then run without the env var and confirm all pass. Record both outputs in the SUMMARY. Commit: `test(44-03): DEAD-02 source pin — no classic copy of any content/ export; 24/6/3 dossier byte-equality`.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && node --test test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# (pass|fail) [0-9]+" && node --test test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/shell-no-content-copies.test.js` and `node --test test/unit/shell-no-content-copies.test.js 2>&1 | grep -E "^# pass [0-9]+"` prints `# pass 5` and `… | grep -E "^# fail 0"` prints `# fail 0`.
    - `grep -c 'Object.keys(SUB_NOTE).length, 24' test/unit/shell-no-content-copies.test.js` prints `1` (or the equivalent `assert.equal(Object.keys(SUB_NOTE).length, 24)` form — one occurrence).
    - `grep -c "process.env.MZ_HTML" test/unit/shell-no-content-copies.test.js` prints `1`, and the fail-first run against the scratch copy (recorded in the SUMMARY) shows `# fail 1` with `SUB_NOTE` in the failure message.
    - `grep -c "content/index.js" test/unit/shell-no-content-copies.test.js` ≥ 1 and `grep -c "new Function" test/unit/shell-no-content-copies.test.js` prints `0`; `grep -rl "new Function" test/` prints nothing.
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`.
  </acceptance_criteria>
  <done>The pin exists, derives its name set from `content/index.js`, proves the dossier's 24/6/3 byte-equality by `===`, fails first on an injected classic copy, and the full suite is green.</done>
</task>

</tasks>

<verification>
- `npm run build:www` exit 0; `npm run boot:check` 4 PASS; `npm test` `# fail 0` at both commits.
- No classic declaration of any `content/` export name (pin test + the sed/grep criteria).
- Engine gate diff empty.
</verification>

<success_criteria>
- ROADMAP success criterion 3 holds: the dossier line for every one of the 24 sub-classes is byte-equal to `content/flavor.js`, the classic `SUB_NOTE` is gone, and a source-pin test fails on any second copy of any exported `content/` table.
- Six further drifted classic tables (`RACE_NOTE`, `WEAPONS`, `FIGHTER_SKILLS`, `THIEF_SKILLS`, `RACES`, plus `CLASS_NOTE`/`ROMAN`/`THRESHOLDS`) read from `content/` through one bridge.
</success_criteria>

<output>
Create `.planning/phases/44-retire-the-classic-engine-from-the-shell/44-03-SUMMARY.md` with:
1. `## Deleted symbols` — the nine classic table declarations with their `ba45dfd` line ranges; `## Added symbols` — `window.__mzTables` (module), the separate import line, `test/unit/shell-no-content-copies.test.js` (5 tests).
2. `## Drift discarded` — which classic rows differed from `content/` (SUB_NOTE 13 rows, RACE_NOTE 3, RACES 3, WEAPONS labels, FIGHTER_SKILLS 13, THIEF_SKILLS 8 — as measured) and the statement that `content/` is canon and no byte of it changed.
3. `## Tests re-pointed or deleted` — `shell-company-panel.test.js` (Cutthroat byte-identical case + `subNoteRegion` deleted — superseded by the 24-row pin); `shell-no-content-copies.test.js` added (fail-first output included).
4. `## Gate outputs` — sweep `refs` for the nine names (expected import/assignment survivors listed), `npm run build:www`, `npm run boot:check`, `npm test` (`# fail 0`, count), `wc -l < mazeworld.html`, engine-gate diff (empty).
5. `## Human verification (deferred to end of run)` — Pixel 7: the Hero-tab dossier reads correctly for a Thief, a Fighter and a Magic User (race / class / sub-class paragraphs present, the Cutthroat and Knight rows show the `content/flavor.js` wording); the Hero-tab skills list shows the `content/skills.js` descriptions; the HUD level reads `Lvl I`; the graveyard stones show `lvl I` etc.
6. `## Flagged assumptions` — A-1 (survivor set as measured), A-4 (`D` now orphaned — handed to 44-04).
</output>
