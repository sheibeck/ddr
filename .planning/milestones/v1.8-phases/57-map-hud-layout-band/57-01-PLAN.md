---
phase: 57-map-hud-layout-band
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/hudBands.js
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - mazeworld.html
  - test/unit/hudBands.test.js
  - test/unit/hud-bands-layout.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/shell-gear-toolbar.test.js
autonomous: true
requirements: [LAYOUT-04, LAYOUT-05]

must_haves:
  truths:
    - "A tap on MARKS, CENTRE, MAKE CAMP or the gear chip dispatches ZERO move actions: `#mw-map-chips` is no longer a descendant of `#mw-maze-viewport`, and the tap/hold/drag/pinch gesture tracker is bound to `#mw-maze-viewport` alone (initMazeViewportControls), so no chip tap can reach tapStep()."
    - "The rect handed to keepInViewAxis measures only the canvas area: `#mw-maze-viewport`'s box no longer contains the chip band, so the 'within 2 cells of an edge' threshold is honest at the top edge. keepPartyInView()'s own body is unchanged — the honesty comes from the box, not from new math."
    - "The counter row holds at 999, 1,000 and 99,999 Squares: each counter number renders in a fixed-width tabular-nums slot sized for COUNTER_DIGIT_SLOT (5) digits, so growth inside the cap cannot widen the row or push the HP block. Past the cap the value clamps to a 5-glyph overflow form rather than widening."
    - "Band 3 (`.mw-cond-strip`) stays hidden when there are no conditions and the remaining bands close up with no reserved gap (paintConditions' existing `host.hidden = true` path is untouched and `.mw-cond-strip[hidden]` still collapses it)."
    - "A character with no sub-class, or with a missing name, still renders a non-empty band 1 identity line — identityLine() omits the parenthesised sub-class rather than emitting empty parentheses, and substitutes a placeholder for a missing name."
    - "The four bands render in the ruled order — identity+vitals, counters, conditions, chips — and that order is asserted STRUCTURALLY (source index of each band's anchor id is strictly increasing in mazeworld.html), not visually."
    - "A long `Name — Race Class (Sub) · Lvl N` truncates rather than wrapping or pushing the HP block: the identity element carries min-width:0 + overflow:hidden + text-overflow:ellipsis + white-space:nowrap, and the HP track is no longer a fixed 64px — it takes the remaining width with a floor, so the bar keeps a minimum width at the largest text size."
    - "On a screen where the rail has never shown, no conditions exist and the party has not moved, the first paint() renders complete bands and throws nothing — band 3 is hidden, bands 1/2/4 are complete."
    - statement: "At every supported text size (S/M/L via textScaleForSize) a long identity line still truncates and the HP bar still reads, on a real Pixel 7 screen width."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "With the party 1-2 cells below the chip band, tapping each of MARKS / CENTRE / MAKE CAMP / gear moves the party zero squares; walking the party to the top edge fires the nudge with the band fully clear of the map."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "At 1,000+ Squares nothing in the HUD overlaps on the device."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
  artifacts:
    - src/browser/hudBands.js
    - test/unit/hudBands.test.js
    - test/unit/hud-bands-layout.test.js
  key_links:
    - "window.__mzHudBands — the ONE bridge paint() reads identityLine/counterSlots through; BRIDGE row + regenerated docs/SHELL-MODULES.md in the SAME commit or bridge-registry.test.js fails."
    - "#mw-map-chips must be a sibling ABOVE .mazebox inside #screen-maze — its removal from #mw-maze-viewport is simultaneously the LAYOUT-04(a) hit-test fix and the LAYOUT-04(b) rect fix."
    - "paint()'s ids m-floor / m-day / m-steps / m-rations / mw-hud-wp / mw-hud-wpfill / m-rat-wrap and the low/critical threshold classes survive the reflow unchanged."
  prohibitions:
    - "MUST NOT modify any file under engine/ or content/."
    - "MUST NOT re-fog or un-reveal any cell — engine/movement.js's cumulative reveal() is correct and untouched; the darkness work is legibility only."
    - "MUST NOT add, remove or reorder any rng draw; MUST NOT move a parity fixture, edit test/parity/prototype-master.js.txt, or touch test/parity/harness/comparables.js."
    - "MUST NOT shift the map viewport on any event other than a drag, the keep-in-view nudge, CENTRE, stairs, teleport or a new run (the Phase 35 / 260918-vm3 stationary-camera ruling)."
    - "MUST NOT use display:none for rail visibility — it cannot be animated and is the cause of the current reflow."
    - "MUST NOT add easing curves, transition tuning, typed text or combat pacing — that is Phase 58 (MOTION-01..05). This phase makes motion possible; it does not add it."
    - "MUST NOT recompute revealRadius/inDark in the shell instead of reading the engine's via the bridge."
    - "MUST NOT add a third-party dependency or a network call."
---

<objective>
Reflow the map screen's chrome into four stacked layout bands and lift the map chip strip out of the viewport.

Per D-05 (chip strip leaves the viewport) and D-06/D-07/D-08 (four stacked HUD bands in the 2026-09-21 ruled order, identity line restored, 5-digit fixed-width counters), this plan turns the single clipping HUD row plus the absolutely-positioned over-map chip overlay into four bands: (1) identity + vitals, (2) counters, (3) condition chips, (4) map chips.

Purpose: LAYOUT-04 and LAYOUT-05. Removing `.mw-map-chips` from `#mw-maze-viewport` fixes BOTH reported symptoms with one change — chip taps can no longer reach the canvas's tap-to-move, and `keepInViewAxis`'s `rect` becomes the true visible map. Stacking the HUD stops the HP block landing on Rations once Squares passes four digits.

Output: a new pure `src/browser/hudBands.js`, one new bridge name, the restructured shell markup/CSS, and re-pinned shell source tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-map-hud-layout-band/57-CONTEXT.md
</context>

<assumption_delta_decision>
The detector returned `detected: true` on a single `pluralization` signal: the term "also" inside LAYOUT-04's *"never also moves the party"*. **Disposition: `no-change`.** This is a lexical false positive — "also" there is an adverb in a sentence about one tap not having two effects, not the introduction of a second variant of an existing abstraction. No identity model is being generalized by this phase. Record this block verbatim in a PLAN.md; do not raise an identity question with the user.
</assumption_delta_decision>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 57-01's share)

| Kind | Name | Notes |
|---|---|---|
| New module | `src/browser/hudBands.js` | Pure. Exports `HUD_BAND_ANCHORS`, `COUNTER_DIGIT_SLOT`, `identityLine(c)`, `counterSlots(state)`. No DOM, no window, no timers. |
| New bridge name | `window.__mzHudBands` | `{ identityLine, counterSlots }`. Owner `mazeworld.html (module)`, consumer `mazeworld.html (classic: paint — the HUD identity line and the fixed-width counter slots)`. |
| New CSS classes | `.mw-hud-identity`, `.mw-hud-name`, `.mw-hud-counters` | Bands 1 and 2. `.mw-hud-top` and `.mw-hud-row` are retired by this plan. |
| Changed CSS rule | `.mw-map-chips` | Becomes a static layout band (no `position`, no `z-index`). |
| New element id | `mw-hud-name` | Band 1's identity text node. |
| New tests | `test/unit/hudBands.test.js`, `test/unit/hud-bands-layout.test.js` | Module behaviour; structural band-order/containment proof. |
| Re-pinned tests | `test/unit/shell-map-hud.test.js` (a)/(b)/(c)/(h), `test/unit/shell-gear-toolbar.test.js` | Re-pinned to the new layout — NEVER loosened or deleted. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: src/browser/hudBands.js — the pure identity/counter formatters</name>
  <files>src/browser/hudBands.js, test/unit/hudBands.test.js</files>
  <read_first>
    - `src/browser/heroTab.js:595-610` — the existing identity strings (`s-name` = `c.name`, `s-tag` = `${c.race} ${c.sub} · ${c.cls}`). Band 1's line is a THIRD render target of the same character fields, never a second source of truth for them.
    - `src/browser/viewModels.js` head comment — the house shape for a pure, DOM-free, window-free formatter module in `src/browser/`.
    - `src/browser/settings.js:120-150` — `textScaleForSize` / `clampTextScale` / `effectiveTextScale`, the S/M/L multipliers the band-1 truncation must survive.
    - `mazeworld.html:2408-2423` — paint()'s four current counter writes (`m-floor`, `m-day`, `m-steps`, `m-rations`, and the `m-rat-wrap` warn toggle).
    - `test/unit/hp-not-wp.test.js:40-55` — the standing `PLAYER_WP` guard; any new player-facing string must say HP, never WP.
  </read_first>
  <action>
Create `src/browser/hudBands.js` as a pure, DOM-free, window-free, timer-free module in the same shape as `src/browser/viewModels.js`. Head comment must state: Phase 57 (LAYOUT-05); this module owns the band-1 identity string and the band-2 fixed-width counter slots; the four band ids are the 2026-09-21 user ruling's order and that ruling SUPERSEDES Phase 35 ruling 5's single-row HUD.

Exports:

- `HUD_BAND_ANCHORS` — a frozen array of the four band anchor element ids in the ruled top-to-bottom order: the band-1 identity element, the band-2 counters element, `mm-conditions`, `mw-map-chips`. This is the constant `test/unit/hud-bands-layout.test.js` asserts source order against; it must be the single place the order is written down.
- `COUNTER_DIGIT_SLOT` — the integer 5. The digit width the fixed-width numeral slot is sized for (D-08: Squares grows unbounded; a five-digit slot covers 99,999).
- `COUNTER_OVERFLOW` — the 5-glyph string a value past the cap renders as. Use `99999` followed by a plus sign so the slot width is never exceeded. Record in the doc comment that this is the named behaviour past the cap: clamp the glyph count, never widen the row, never wrap, never abbreviate to a `k` form (D-08 forbids abbreviation).
- `identityLine(c)` — returns `Name — Race Class (Sub) · Lvl N`. Rules, each of which is a test case: a falsy `c.sub` omits the parenthesised group AND its leading space entirely (never empty parentheses); a falsy `c.name` substitutes a non-empty placeholder so band 1 never collapses to an empty node; a falsy `c.level` renders `Lvl 1`; a missing or null `c` returns the placeholder-only form and never throws. Separator characters are the em dash and the middle dot already used elsewhere in the shell. The string is player-facing: no "WP", no banned terms.
- `counterSlots(state)` — returns a frozen array of four `{ id, label, text, over }` records in the order Depth, Day, Squares, Rations, with ids `m-floor`, `m-day`, `m-steps`, `m-rations`. `text` is the numeric value as a string when its digit count is within `COUNTER_DIGIT_SLOT`, otherwise `COUNTER_OVERFLOW`, and `over` is the corresponding boolean. A missing/partial `state` yields zeroes rather than throwing. The label strings are Depth, Day, Squares, Rations — note in the doc comment that the band-2 label for `m-floor` is "Depth" per the 2026-09-21 ruling's wording while the element id stays `m-floor` (paint()'s contract, D-09).

Write `test/unit/hudBands.test.js` covering: the four identityLine rules above; a long name/race/class/sub combination returns one single-line string with no newline; counterSlots at 999, 1000, 99999 all report `over` false with `text` equal to the plain digits; at 100000 and beyond `over` is true and `text` is `COUNTER_OVERFLOW` whose length equals `COUNTER_DIGIT_SLOT` plus one; `HUD_BAND_ANCHORS` has exactly four entries in the ruled order; the module's exports are frozen. Add a teeth check: raising `COUNTER_DIGIT_SLOT` in a local copy would change which value overflows — assert against the constant, not a hardcoded 5, everywhere except one explicit pin that `COUNTER_DIGIT_SLOT === 5`.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/hudBands.test.js` exits 0 with at least 10 assertions passing.
    - `node -e "import('./src/browser/hudBands.js').then(m=>{const s=['window','document','setTimeout','localStorage'];})"` is not the check — instead: `grep -nE '\b(document|window|globalThis|setTimeout|setInterval|localStorage)\b' src/browser/hudBands.js` returns no matches (purity gate; the module has no DOM/clock/storage surface at all, so this grep is a true zero on the whole file).
    - `node -e "import('./src/browser/hudBands.js').then(m=>{console.log(m.identityLine({name:'Ardwin',race:'Human',cls:'Thief',sub:null,level:3}))})"` prints a line with no parentheses and no double space.
    - `node -e "import('./src/browser/hudBands.js').then(m=>{console.log(JSON.stringify(m.counterSlots({floor:{depth:2},day:4,steps:100000,c:{rations:5}})))}"` shows `m-steps` with `over: true`.
    - `node --test test/unit/hp-not-wp.test.js` still exits 0 (the new player-facing identity string is HP-clean).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/hudBands.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <done>hudBands.js exists as a pure module with the five exports, its unit test passes, and the purity grep is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure the shell into four bands and lift the chip strip out of the viewport</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - `mazeworld.html:866-897` — the current `.mw-hud` / `.mw-hud-top` / `.mw-hud-row` / `.mw-hud-item` / `.mw-hud-wp*` CSS block and the Phase 35 ruling-5 comment at `:867-873` that must record the reversal.
    - `mazeworld.html:899-906` — `.mw-cond-strip` CSS (band 3, unchanged behaviour; still hidden when empty).
    - `mazeworld.html:1365-1391` — the current HUD markup and the `.mw-cond-strip` host.
    - `mazeworld.html:337-345` — `.mw-maze-viewport`, `.mw-vignette`, `.mw-map-chips`, `.mw-map-chips-gap`, `.mw-map-chip` CSS.
    - `mazeworld.html:1398-1432` — `#screen-maze` / `.mazebox` / `.mw-maze-viewport` markup and the chip block inside it.
    - `mazeworld.html:2408-2423` and `:2440-2462` — paint()'s counter writes and the HP text/fill writes with their 25/50/22 thresholds.
    - `mazeworld.html:3913-3950`, `:4590` — the four chip listeners (`mw-chip-marks`, `mw-chip-centre`, `btn-camp`, `mw-gear-btn`); they must remain exactly one apiece and keep their ids.
    - `mazeworld.html:4049-4080` — `initMazeViewportControls`: the gesture tracker binds to `#mw-maze-viewport`. This is the structural reason the chip move alone fixes LAYOUT-04(a).
    - `mazeworld.html:3966-3989` — `keepPartyInView()`; it must stay byte-unchanged.
    - `src/browser/bridge.js:1-60` and the `__mzSfxBackendOverride` entry at `:235-239` — the three-part bridge pattern (export + BRIDGE row + regenerated doc, one commit).
    - `tools/bridge-doc.mjs:1-40` — the `--write` regen entrypoint.
    - `mazeworld.html:4415-4430` — the module script's bridge-assignment block, where `__mzHudBands` joins.
  </read_first>
  <action>
Restructure the HUD and the chip strip. Four changes, one commit.

(1) **Markup — bands 1 and 2.** Inside `<header class="mw-hud" id="mw-hud">`, replace the single `.mw-hud-top` row with two sibling rows. Band 1 is a `div.mw-hud-identity` holding, left to right: a `span.mw-hud-name` with id `mw-hud-name` (the identity line), the existing `.mw-hud-wp` block with its `mw-hud-wp` / `mw-hud-wpfill` ids and markup intact, and the existing `#mw-dev-chip` span. Band 2 is a `div.mw-hud-counters` holding the four existing `.mw-hud-item` divs unchanged — same ids `m-floor`, `m-day`, `m-steps`, `m-rations`, same `#m-rat-wrap` wrapper, same `.mw-hud-floor` / `.mw-hud-rations` classes — with the Floor label text changed to Depth per the ruling. `.mw-hud-top` and `.mw-hud-row` disappear from both markup and CSS. Band 3, the `.mw-cond-strip` host `#mm-conditions`, stays exactly where it is, directly after `</header>`.

(2) **Markup — band 4.** Move the entire `<div class="mw-map-chips" id="mw-map-chips"> … </div>` block out of `.mw-maze-viewport` and make it the FIRST child of `<section id="screen-maze">`, immediately before `<div class="mazebox">`. Keep the element's class and id, keep all four buttons with their ids and their MARKS / CENTRE / gap / MAKE CAMP / gear order (gear right of MAKE CAMP, v1.4 ruling). Nothing else moves out of the viewport: the canvas, `.mw-vignette` and `#mw-party-pulse` stay inside it.

(3) **CSS.** Rewrite the `.mw-map-chips` rule as a static layout band: no `position` declaration at all, no `z-index`, `flex:none`, a flex row with the existing 6px gap, vertical centring, a small symmetric padding, and the same dark band background and 3px bottom rule the condition strip uses so it reads as the fourth header band. `.mw-map-chips-gap` keeps `flex:1`. `.mw-map-chip` and its `.camp` / `.gear` / `:active` variants are untouched. Add `.mw-hud-identity` as a flex row with centred items and a 10px gap; `.mw-hud-name` takes the remaining width and truncates — it must carry min-width:0, flex growth, hidden overflow, ellipsis text-overflow and nowrap white-space, in the HUD label font at the existing `--mw-font-hud-label` scale. Add `.mw-hud-counters` as the band-2 flex row: nowrap, hidden overflow, the existing 8px gap and baseline alignment. Give `.mw-hud-item b` a fixed-width numeral slot: inline-block display, right-aligned text, and a min-width expressed in `ch` units equal to `COUNTER_DIGIT_SLOT`, on top of the `font-variant-numeric:tabular-nums` it already has. Change `.mw-hud-wp` from `flex:none;margin-left:auto` to a bounded flexible box that keeps a minimum width, and change `.mw-hud-wptrack` from its fixed 64px width to full width of that box with a min-width floor, so the bar takes the remaining width and can never collide with the identity text. The `.mw-hud` safe-area top padding stays on the header, which is band 1's own container.

(4) **Comment + paint() + bridge.** Rewrite the Phase 35 ruling-5 comment above `.mw-hud` so it records the reversal explicitly: name Phase 35 MAP-01 ruling 5 (single HUD row, character line retired), state that the 2026-09-21 user ruling supersedes it, and list the four bands in order. Do not silently contradict the old comment. In `paint()`, route the four counter writes through `window.__mzHudBands.counterSlots(S)` — one loop over the returned records assigning `textContent` by id — and add ONE assignment writing `#mw-hud-name`'s textContent from `window.__mzHudBands.identityLine(c)`, with a `title` attribute carrying the same string so a truncated line is still readable on long-press. Both reads are fail-open: a missing bridge leaves the existing text in place rather than throwing. The `m-rat-wrap` warn toggle, the HP text/fill writes and their 25/50/22 thresholds, and `paintConditions(c)` are unchanged. In the module script's bridge block add `window.__mzHudBands = { identityLine, counterSlots };` with the `hudBands.js` import, add the matching `__mzHudBands` row to `BRIDGE` in `src/browser/bridge.js` (owner `mazeworld.html (module)`, one consumer string naming paint and the HUD identity line plus the fixed-width counter slots), and run `node tools/bridge-doc.mjs --write` in the SAME commit.
  </action>
  <acceptance_criteria>
    - Structural containment (the LAYOUT-04 proof): a node one-liner that reads `mazeworld.html`, slices from `<div class="mw-maze-viewport"` to `<!-- DR5: the encounter/feature-event panel`, and asserts the slice does NOT contain `id="mw-map-chips"` — prints OK and exits 0.
    - Band order (structural, not visual): a node one-liner that reads `mazeworld.html` and asserts `indexOf('id="mw-hud-name"') < indexOf('class="mw-hud-counters"') < indexOf('id="mm-conditions"') < indexOf('id="mw-map-chips"')`, all four found — exits 0.
    - `grep -nE '^\.mw-map-chips\{' mazeworld.html` returns exactly one line, and that line matches `/^\.mw-map-chips\{(?!.*position:)(?!.*z-index)/` — assert with a node regex test, not a shell grep, so the negative lookaheads are honest.
    - Chip listeners intact: `grep -cE 'getElementById\("(mw-chip-marks|mw-chip-centre|btn-camp|mw-gear-btn)"\)' mazeworld.html` returns 5 (four listener sites plus paint()'s `btn-camp` short-state read) — count anchored on the full `getElementById("…")` call form, never a bare id substring, because the same ids also appear inside markup attributes and comment prose.
    - Ids preserved: a node check asserts each of `m-floor`, `m-day`, `m-steps`, `m-rations`, `m-rat-wrap`, `mw-hud-wp`, `mw-hud-wpfill`, `mw-dev-chip`, `mm-conditions`, `mw-map-chips` appears exactly once as an `id="…"` attribute in `mazeworld.html`.
    - `keepPartyInView` untouched: `git diff -U0 mazeworld.html | grep -c 'keepInViewAxis'` returns 0 (the nudge's own body is not in the diff).
    - `node --test test/unit/bridge-registry.test.js` exits 0 — both the set-equality half and the doc-sync half, proving the BRIDGE row and the regenerated `docs/SHELL-MODULES.md` landed with the export.
    - `node --test test/unit/shell-map-viewport.test.js test/unit/shell-map-invariants.test.js` exits 0 (the gesture-tracker host, the four chip listeners, the camera call-site counts and the rail's between-main-and-nav position are all unaffected by this plan).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bridge-registry.test.js test/unit/shell-map-viewport.test.js test/unit/shell-map-invariants.test.js</automated>
  </verify>
  <done>The chip strip is a static band above `.mazebox`, the HUD is two stacked bands feeding band 3 and band 4, `__mzHudBands` is bridged and documented, and the viewport-region tests are green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the shell source tests and add the band-layout structural test</name>
  <files>test/unit/hud-bands-layout.test.js, test/unit/shell-map-hud.test.js, test/unit/shell-gear-toolbar.test.js</files>
  <read_first>
    - `test/unit/shell-map-hud.test.js:1-92` — the file's comment-stripping and `sliceBetween` / `fnRegion` helpers, which the new test should reuse in shape.
    - `test/unit/shell-map-hud.test.js:93-160` — tests (a), (b), (c): the HUD markup pin, the `mw-cond-strip`-after-`</header>` layout pin, and the byte-exact `.mw-hud-top` / `.mw-hud-row` / `.mw-hud-item` / `.mw-hud-wp*` CSS pins. These are the assertions this plan moves.
    - `test/unit/shell-map-hud.test.js:281-310` — test (h): the chip ids/order/classes pin and the `.mw-map-chip.camp`/`.gear` colour pins.
    - `test/unit/shell-gear-toolbar.test.js:80-90` and `:190-200` — the `sliceBetween('<div class="mw-map-chips"', '<section class="mw-overlay"')` region and the `^\.mw-map-chips\{` / `^\.mw-map-chips-gap\{flex:1\}$` rule pins.
    - `test/unit/harness/shellSandbox.js:131-200` — `loadShellSandbox({doc})`, `setState`, `paint()`; and `test/unit/harness/recordingDom.js:340-360` — `getElementById` auto-creates elements by id, so the sandbox can prove paint()'s WRITES but cannot prove markup ORDER. Order is proven against the source text instead.
  </read_first>
  <action>
Re-pin, never loosen. Every assertion below is moved to the new layout with the same or greater strictness; none is deleted.

In `test/unit/shell-map-hud.test.js`: update test (a) so the HUD-markup region assertion reads the two-band structure — band 1 contains `id="mw-hud-name"`, the `mw-hud-wp` block and `#mw-dev-chip`; band 2 contains the four counter items in Depth/Day/Squares/Rations order with their kept ids; the character-line ids Phase 35 retired stay at zero occurrences. Update test (b) so the layout pin reads: band 1 precedes band 2, `.mw-cond-strip` still sits directly after `</header>` and before `<main>`, and `#mw-map-chips` now sits inside `#screen-maze` before `<div class="mazebox">`. Update test (c) so the CSS pins are byte-exact against the NEW `.mw-hud-identity` / `.mw-hud-name` / `.mw-hud-counters` / `.mw-hud-item` / `.mw-hud-item b` / `.mw-hud-wp` / `.mw-hud-wptrack` rules, and assert that `.mw-hud-top{` and `.mw-hud-row{` no longer appear as rule openers anywhere in the style block. Update test (h) so the chip region is sliced from the new location and still pins the five children in MARKS / CENTRE / gap / MAKE CAMP / gear order with their classes and ids, plus the new static-band `.mw-map-chips` rule.

In `test/unit/shell-gear-toolbar.test.js`: change the chip-region slice's end marker from the encounter-overlay opener to `<div class="mazebox">`, since the chip band is now a sibling above `.mazebox` and the old end marker would swallow the whole viewport. Re-pin the `^\.mw-map-chips\{` rule assertion to the new static-band declaration list. `^\.mw-map-chips-gap\{flex:1\}$` stays as-is.

Create `test/unit/hud-bands-layout.test.js`. It is the LAYOUT-04/LAYOUT-05 structural proof and must contain, each as its own named test: (1) the four bands' anchors appear in `mazeworld.html` in exactly the order `HUD_BAND_ANCHORS` declares, imported from `src/browser/hudBands.js` so the constant and the markup can never drift; (2) the `.mw-maze-viewport` slice contains no chip markup and no `.mw-map-chip` class occurrence — the LAYOUT-04(a) hit-test proof, stated in the test name as "a chip tap can never reach the canvas gesture tracker because the chips are not inside its host"; (3) the gesture tracker's host is still `#mw-maze-viewport` — assert `initMazeViewportControls`'s region contains exactly one `getElementById("mw-maze-viewport")` call and the `pointerdown` listener is attached to that same local — the LAYOUT-04(b) rect-honesty proof, since the rect it measures is now chip-free; (4) a BEHAVIOUR section using `loadShellSandbox` + `fixedStates` that sets a state with `steps` at 999, 1000 and 100000 and asserts `#m-steps`'s textContent equals `counterSlots`' `text` for each, and that `#mw-hud-name`'s textContent equals `identityLine(c)` — proving paint() routes through the bridge rather than formatting inline; (5) a first-frame test: a freshly-booted fixed state with no conditions paints without throwing, `#mm-conditions` is hidden, and bands 1/2 are non-empty.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/hud-bands-layout.test.js` exits 0 with five named tests passing.
    - `node --test test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js` exits 0.
    - Re-pinned, not loosened: `git diff --numstat test/unit/shell-map-hud.test.js` shows deletions and insertions in the same tests; a node check asserts the file still contains the same COUNT of top-level `test(` declarations as at the phase base commit `4d43edf` (`git show 4d43edf:test/unit/shell-map-hud.test.js | grep -c '^test('` equals `grep -c '^test(' test/unit/shell-map-hud.test.js`) — no test was dropped to make the suite pass.
    - The same no-test-dropped check passes for `test/unit/shell-gear-toolbar.test.js`.
    - `node --test` over the whole `test/unit/` directory exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test "test/unit/**/*.test.js"</automated>
  </verify>
  <done>The band order and chip containment are proven structurally, paint()'s writes are proven through the sandbox, and no pre-existing shell test was deleted or weakened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → shell DOM | The only untrusted input on this surface is a finger. No network, no file, no IPC crosses into this phase's code. |
| engine state → shell render | `S` is produced by the local engine and is trusted; the shell only reads it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-01 | Tampering | `identityLine(c)` writing `#mw-hud-name` | low | mitigate | Written via `textContent`, never `innerHTML` — a tampered save's `c.name` can never inject markup. Pinned by the band-layout test asserting the assignment form. |
| T-57-02 | Denial of Service | `counterSlots` on an unbounded `steps` | low | mitigate | `COUNTER_DIGIT_SLOT` clamps the rendered glyph count, so no value can widen the row or force reflow churn. |
| T-57-03 | Information Disclosure | HUD identity line | low | accept | The line shows the player their own character; there is no second party and no network. |
| T-57-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this phase — `package.json` and `package-lock.json` are untouched (offline gate). Any install attempt is out of scope and would require a blocking human checkpoint. |
</threat_model>

<verification>
- `node --test "test/unit/**/*.test.js"` exits 0.
- `node --test test/unit/bridge-registry.test.js` green on both halves (set-equality AND doc-sync).
- `git diff --stat 4d43edf..HEAD -- engine/ content/ test/parity/` is empty.
- `git diff 4d43edf..HEAD -- package.json package-lock.json` is empty.
</verification>

<success_criteria>
- `#mw-map-chips` is outside `#mw-maze-viewport` and its CSS carries neither `position` nor `z-index`.
- The four bands appear in the ruled order, asserted against `HUD_BAND_ANCHORS`.
- Every `paint()` id named in D-09 survives; the Phase 35 ruling-5 comment records its own reversal.
- `src/browser/hudBands.js` is pure and bridged as `__mzHudBands`, with the BRIDGE row and the regenerated doc in the same commit.
- No pre-existing shell test was deleted or weakened.
</success_criteria>

<output>
Create `.planning/phases/57-map-hud-layout-band/57-01-SUMMARY.md` when done.
</output>
