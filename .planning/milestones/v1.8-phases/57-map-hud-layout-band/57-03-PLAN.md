---
phase: 57-map-hud-layout-band
plan: 03
type: execute
wave: 3
depends_on: ["57-02"]
files_modified:
  - src/browser/rail.js
  - mazeworld.html
  - test/unit/rail.test.js
  - test/unit/rail-dismiss.test.js
  - test/unit/shell-map-rail.test.js
autonomous: true
requirements: [LAYOUT-02, LAYOUT-03]

must_haves:
  truths:
    - "Every rail card kind resolves to exactly one of {dismissible-by-body-tap, locked} — `railDismissKind(locked, buttonCount)` is total, returns one of exactly two string values for every input including zero, negative, non-numeric and missing arguments, and there is no third state and no card that is neither."
    - "A tap whose target is a rail BUTTON routes to that button and is never treated as a body tap: the body handler returns early when the target resolves to a `.mw-rail-btn`, before any dismiss or pulse."
    - "A body tap on a locked card pulses and leaves the card present: `railLocked()` true, or a live action row with at least one button, both classify as locked, and the locked branch calls `railPulse()` and returns without clearing `railTimer` or the rail view-model."
    - "No rail tap reaches `tapStep()`: the rail is not a descendant of `#mw-maze-viewport`, and the tap/hold/drag/pinch tracker is bound to that element alone — a structural guarantee, asserted as such, not a stopPropagation hope."
    - "A tap arriving inside the arm window is swallowed: the handler gates on `window.__mzInputGuards.isArmed(railShownAt, Date.now())`, where `railShownAt` is stamped in `renderRail()`'s new-card branch, so the gesture that CAUSED a card can never also dismiss it."
    - "The new hold is a function of line count and is bounded at both ends: `holdForCard(card)` is the named base hold plus `HOLD_PER_LINE` for each line beyond the first, clamped to `[HOLD_MIN, HOLD_MAX]`. A one-line card is never shorter than today's floor (HOLD_MIN 4400 exceeds the old 2200 minimum) and a pathologically long card never exceeds HOLD_MAX (16000 ms) — that is the named cap."
    - "Every named RAIL_HOLD key plus WORN_RECONCILE_HOLD carries a new pinned value at exactly twice its pre-phase number: default 8400, dull 4800, dullShort 4400, mark 6800, day 6000, floor 10000, camp 10400, level 12000, WORN_RECONCILE_HOLD 12000. The tests are RE-PINNED to the new numbers, never loosened and never deleted."
    - "On a screen where the rail has never shown, a body tap on the resting rail does nothing and throws nothing — the resting overlay has pointer-events disabled and `railShownAt` is still at its initial value."
  artifacts:
    - test/unit/rail-dismiss.test.js
  key_links:
    - "`holdForCard` and `railDismissKind` reach the classic script through the EXISTING `window.__mzRailVM` bridge — no new bridge name, so `bridge-registry.test.js`'s set-equality is untouched. If the BRIDGE row's purpose string is edited, `node tools/bridge-doc.mjs --write` must run in the same commit."
    - "`renderRail()`'s single `setTimeout` is the ONE auto-clear path; it must key on `holdForCard(rail.card)`, not on `rail.card.hold` directly, or the per-line scaling never reaches the screen."
    - "The dismiss handler must NOT be wired through `guardTap` — see the Task 2 note; it uses the same `isArmed` predicate from `src/browser/inputGuards.js` instead."
  prohibitions:
    - "MUST NOT modify any file under engine/ or content/."
    - "MUST NOT re-fog or un-reveal any cell — engine/movement.js's cumulative reveal() is correct and untouched; the darkness work is legibility only."
    - "MUST NOT add, remove or reorder any rng draw; MUST NOT move a parity fixture, edit test/parity/prototype-master.js.txt, or touch test/parity/harness/comparables.js."
    - "MUST NOT shift the map viewport on any event other than a drag, the keep-in-view nudge, CENTRE, stairs, teleport or a new run (the Phase 35 / 260918-vm3 stationary-camera ruling)."
    - "MUST NOT use display:none for rail visibility — it cannot be animated and is the cause of the current reflow."
    - "MUST NOT add easing curves, transition tuning, typed text or combat pacing — that is Phase 58 (MOTION-01..05). This phase makes motion possible; it does not add it."
    - "MUST NOT recompute revealRadius/inDark in the shell instead of reading the engine's via the bridge."
    - "MUST NOT add a third-party dependency or a network call."
---

<objective>
Give the rail a guarded tap-to-dismiss and a hold long enough to finish reading.

Per D-03 (holds roughly double AND scale by line count; re-pin the tests, never loosen them) and D-04 (tap-to-dismiss, guarded: a locked card or a card with buttons pulses instead of dismissing, a tap on a rail button still routes to the button, and the tap does not fall through to `tapStep()`).

Purpose: LAYOUT-02 and LAYOUT-03. The rail inherited the old toast numbers when it replaced toasts in Phase 35; the 2026-09-15 toast ruling ("about 2x, tap to dismiss") was never applied to it. Tap-to-dismiss is what makes the longer hold cheap.

Output: new bounded, line-scaled hold values and a total dismiss classifier in `src/browser/rail.js`, the shell's guarded body-tap handler, and a re-pinned test suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-map-hud-layout-band/57-CONTEXT.md
@.planning/phases/57-map-hud-layout-band/57-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 57-03's share)

| Kind | Name | Notes |
|---|---|---|
| New exports | `HOLD_PER_LINE`, `HOLD_MIN`, `HOLD_MAX`, `holdForCard(card)`, `railDismissKind(locked, buttonCount)`, `RAIL_DISMISS_KINDS` | All in `src/browser/rail.js`; all pure. |
| Changed constants | `RAIL_HOLD` (all eight keys), `WORN_RECONCILE_HOLD` | Each exactly doubled: 8400 / 4800 / 4400 / 6800 / 6000 / 10000 / 10400 / 12000, and 12000. |
| Extended bridge | `window.__mzRailVM` gains `holdForCard` and `dismissKind` | No NEW bridge name — the registry's set-equality is unaffected. |
| New shell globals | `railShownAt`, the `#mw-rail` body-tap handler | One stamp, one handler, each defined exactly once. |
| New test | `test/unit/rail-dismiss.test.js` | Classifier totality, hold bounds, and the shell handler's four branches through the sandbox. |
| Re-pinned tests | `test/unit/rail.test.js`, `test/unit/shell-map-rail.test.js` (f) | Re-pinned to the new numbers and the new timer call — NEVER loosened or deleted. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: rail.js — doubled, line-scaled, bounded holds and a total dismiss classifier</name>
  <files>src/browser/rail.js, test/unit/rail.test.js</files>
  <read_first>
    - `src/browser/rail.js:44-68` — the `RAIL_HOLD` table and `WORN_RECONCILE_HOLD`, with the doc comments that explain what a named hold is for.
    - `src/browser/rail.js:148-230` — `RAIL_FAMILY` and its per-family hold overrides (`mark`, `floor`, `level`, `day`, `camp`, `dull`), which must keep pointing at the same named keys.
    - `src/browser/rail.js:320-350` — `railFamilyFor`'s default resolution (`fam.hold ?? RAIL_HOLD.default`) and the six fallback branches.
    - `src/browser/rail.js:387-460` — `railCardFor`'s fold, the `hold` it computes as the max family hold across lines, `railLineCard`'s signature, and the card shape (`{icon, iconKey, title, lines, tone, hold}`) that `holdForCard` receives.
    - `src/browser/rail.js:490-530` — `wornReconcileCard` and the ability-pool card, the two direct `WORN_RECONCILE_HOLD` / `RAIL_HOLD.level` consumers.
    - `test/unit/rail.test.js:42-52` and `:107-126` — the TWO range pins (`RAIL_HOLD` values within [2200, 6000], and every `LINE_FOR` key resolving to a hold in the same range). Both are re-pinned here.
    - `test/unit/rail.test.js:160-200`, `:240-260`, `:340-355` — the `deepEqual` assertions that carry hold values (`railFamilyFor` rows, `railLineCard` literals, `wornReconcileCard`).
    - `test/unit/tapStep.test.js` and `test/unit/ability-pool.test.js` — the two other files importing hold constants; check whether either pins a literal number.
  </read_first>
  <action>
Rewrite the hold vocabulary in `src/browser/rail.js`.

**New values.** Double every named hold, keeping the key names and the frozen-object shape: default 8400, dull 4800, dullShort 4400, mark 6800, day 6000, floor 10000, camp 10400, level 12000. Set `WORN_RECONCILE_HOLD` to 12000. Update the `RAIL_HOLD` doc comment to record that these are the 2026-09-15 toast ruling's "about 2x" applied to the rail, which inherited the old toast numbers in Phase 35, and that tap-to-dismiss (LAYOUT-02) is what makes the longer hold cheap.

**New bounds and scaling.** Add three frozen constants with doc comments recording the chosen numbers as Claude's-discretion values under D-03: `HOLD_PER_LINE` = 900 (milliseconds added per line beyond the first — the user's complaint was "all the text", so a four-line card must hold longer than a one-line card), `HOLD_MIN` = 4400 (the floor; note that it is exactly the doubled `dullShort`, and that it is comfortably above the old 2200 floor so no card can ever get shorter than it is today), `HOLD_MAX` = 16000 (the cap; note that this is the named answer to "a pathologically long card must not hold indefinitely" — beyond roughly thirteen lines the hold stops growing and the player dismisses by tapping).

**`holdForCard(card)`.** A pure function taking the card shape `railCardFor` and `railLineCard` produce. It reads the card's own `hold` as the base (falling back to `RAIL_HOLD.default` when absent or non-finite), adds `HOLD_PER_LINE` once per line beyond the first (line count from `card.lines.length`, treating a missing/empty lines array as one line, never as zero), and clamps the result into `[HOLD_MIN, HOLD_MAX]`. It never throws on a missing or malformed card — a null card yields `HOLD_MIN`. Document that a line carrying a roll is still one line, because the roll renders under its own line rather than as a separate card row.

**`railDismissKind(locked, buttonCount)` and `RAIL_DISMISS_KINDS`.** Export a frozen two-element array of the only two kinds — the dismissible kind and the locked kind, as string literals — and a total classifier returning exactly one of them: locked when `locked` is truthy OR when `buttonCount` coerces to a number greater than zero; dismissible otherwise. Every other input, including undefined, NaN, a negative count and a missing second argument, resolves to dismissible. Document that this is the Phase 35 ruling 3 rule in one place: a decision card's buttons are the ONLY way out of it, and the rail body tap must never become a second exit.

**Re-pin `test/unit/rail.test.js`.** Change both range assertions from the old window to `[HOLD_MIN, HOLD_MAX]`, imported from the module rather than hardcoded. ADD a new test pinning all eight `RAIL_HOLD` values and `WORN_RECONCILE_HOLD` to their exact new numbers — an explicit table, so a future drift is a failing diff and not a silently-widened range. Update every `deepEqual` that carries a hold value to the new numbers. Add tests for `holdForCard`: a one-line card returns its base clamped at or above `HOLD_MIN`; a four-line card returns base plus three increments; a card with forty lines returns exactly `HOLD_MAX`; a null card returns `HOLD_MIN`; a card whose base is below `HOLD_MIN` is raised to it. Add tests for `railDismissKind`: the two-value totality sweep over a matrix of `locked` in {true, false, undefined, null} crossed with `buttonCount` in {0, 1, 2, undefined, NaN, -1}, asserting every result is a member of `RAIL_DISMISS_KINDS` and that the locked/dismissible split is exactly as specified — and an assertion that `RAIL_DISMISS_KINDS` has length 2, so a third state cannot be introduced without failing this test.

If `tapStep.test.js` or `ability-pool.test.js` pins a literal hold number, re-pin it to the new value in this same commit.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/rail.test.js` exits 0.
    - Exact new values: `node -e "import('./src/browser/rail.js').then(m=>{const e={default:8400,dull:4800,dullShort:4400,mark:6800,day:6000,floor:10000,camp:10400,level:12000};for(const k in e){if(m.RAIL_HOLD[k]!==e[k])throw new Error(k);}if(m.WORN_RECONCILE_HOLD!==12000)throw new Error('worn');console.log('OK')})"` prints OK.
    - Exactly doubled, proven against the phase base rather than asserted in prose: a node script imports the current module and the base version obtained with `git show 4d43edf:src/browser/rail.js` written to a temp file, and asserts each named hold is exactly 2x its base value and that the key SETS are identical — exits 0.
    - Bounds: `node -e "import('./src/browser/rail.js').then(m=>{if(m.holdForCard({hold:m.RAIL_HOLD.default,lines:new Array(40)})!==m.HOLD_MAX)throw 0;if(m.holdForCard(null)!==m.HOLD_MIN)throw 1;console.log('OK')})"` prints OK.
    - Totality: `node -e "import('./src/browser/rail.js').then(m=>{if(m.RAIL_DISMISS_KINDS.length!==2)throw 0;for(const l of [true,false,undefined,null])for(const b of [0,1,2,undefined,NaN,-1]){if(!m.RAIL_DISMISS_KINDS.includes(m.railDismissKind(l,b)))throw 1;}console.log('OK')})"` prints OK.
    - No test dropped: `git show 4d43edf:test/unit/rail.test.js | grep -c '^test('` is less than or equal to `grep -c '^test(' test/unit/rail.test.js` — tests were added, none removed.
    - `node --test "test/unit/**/*.test.js"` exits 0 (catches any other file pinning an old hold literal).
  </acceptance_criteria>
  <verify>
    <automated>node --test "test/unit/**/*.test.js"</automated>
  </verify>
  <done>All eight named holds plus `WORN_RECONCILE_HOLD` are exactly doubled, `holdForCard` scales by line count within a named floor and cap, `railDismissKind` is total over two kinds, and every affected test is re-pinned to the new numbers.</done>
</task>

<task type="auto">
  <name>Task 2: The guarded body-tap dismiss handler and the line-scaled auto-clear timer</name>
  <files>mazeworld.html</files>
  <read_first>
    - `mazeworld.html:2746-2760` — `guardTap(btn, fn)`: it stamps `aria-disabled="true"` on its target and reads the SHARED `encRenderedAt` stamp through `encArmed()`. Read the Task 2 note below before deciding to use it.
    - `mazeworld.html:2736-2745` — `armEncounterButtons()` and its sweep selector `#mw-rail [aria-disabled="true"]`, which is a DESCENDANT selector and would never clear an attribute placed on `#mw-rail` itself.
    - `mazeworld.html:2699-2701` — `railLocked()`, the one global movement lock.
    - `mazeworld.html:3766-3800` — `renderRail()`'s `isNew` branch: `lastRailKeyShown`, `armEncounterButtons()`, the `mw-rail-new` re-trigger, `syncRailLive`, and the single `setTimeout` keyed on `rail.card.hold || 4200`.
    - `mazeworld.html:3805-3814` — `railPulse()` and the `window.renderRail` / `window.mzRailPulse` bridge assignments.
    - `mazeworld.html:4488-4502` — the module script's `window.__mzRailVM = { card, push, clear, lineCard, announcement, copy }` assignment and `window.mzRailLine`, where the two new entries join.
    - `mazeworld.html:2358`, `:4018-4019`, `:5638` — the four shell call sites passing a literal hold number to `mzRailLine`.
    - `src/browser/inputGuards.js:20-30, 58-66` — `ARM_DELAY_MS` and `isArmed(renderedAt, now)`, including its fail-open contract on a non-finite stamp.
    - `mazeworld.html:4049-4080` — `initMazeViewportControls`: the gesture tracker is bound to `#mw-maze-viewport`, which is why a rail tap structurally cannot reach `tapStep()`.
  </read_first>
  <action>
**Note on `guardTap` — read first.** D-04 says "a `guardTap` handler on the rail element". Do NOT use the `guardTap` wrapper here, for two concrete reasons found in the code: it sets `aria-disabled="true"` on its target, and the arm sweep that clears that attribute uses a descendant selector, so the attribute would stay on the rail's live-region host forever; and it gates on the SHARED `encRenderedAt` stamp, which for a no-button card is whatever an older render left behind — exactly the wrong guard for exactly the cards this handler serves. Honour D-04's intent instead by using the same arm predicate `guardTap` is built on: `window.__mzInputGuards.isArmed(...)`, against a stamp this plan adds for the rail itself. Record this deviation in the plan SUMMARY.

**The stamp.** Add a module-scope `let railShownAt = 0;` beside the existing `lastRailKeyShown` declaration. In `renderRail()`'s `isNew` branch, set it to `Date.now()` on the same line group that assigns `lastRailKeyShown`, so every newly-shown card restarts its own arm window.

**The timer.** In the same `isNew` branch, replace the auto-clear `setTimeout`'s delay expression with a call to the bridged `holdForCard` against the live card, falling back to the doubled default when the bridge is missing. Keep the single-`setTimeout` shape, the surrounding `clearTimeout` and the `!idle && !buttons.length && rail.card` condition exactly as they are — the auto-clear path stays one timer, one condition, one call site.

**The handler.** Wire the body tap exactly once, next to the other one-time element listeners in the classic script. Assign `onclick` on `#mw-rail` (not a capture listener, not a pointerdown) with a handler that runs these four checks in this order and returns after each:
1. If the event's target resolves to a rail action button — walk up with the target's own `closest` method when it exists, matching the rail action-button class — return immediately and let the button's own handler run. A tap on a rail BUTTON is never a body tap.
2. If `window.__mzInputGuards.isArmed(railShownAt, Date.now())` is false, return with no effect. The gesture that caused a card can never also dismiss it.
3. Classify with the bridged `dismissKind`, passing `railLocked()` and the live button count read from `#mw-rail-actions`' child count. If the result is the locked kind, call `railPulse()` and return — the card stays, and its buttons remain the only way out (Phase 35 ruling 3).
4. Otherwise dismiss: clear `railTimer`, null it, replace `window.__mzRail` with the view-model's `clear` result, and call `renderRail()`.

Add a comment above the handler naming Phase 57 (LAYOUT-02), stating the four-step order, why `guardTap` is not the wrapper used here, and that the rail is not a descendant of the map viewport so a rail tap has no path to `tapStep()` — the tap-through guarantee is structural, not a propagation trick.

**Bridge and literals.** Extend the module script's `window.__mzRailVM` object with `holdForCard` and a `dismissKind` entry bound to `railDismissKind`, importing both from `rail.js` alongside the existing imports. No new `window.__mz*` NAME is created. Finally, double the four literal holds at the `mzRailLine` call sites so the whole surface speaks the new vocabulary: the condition-chip explain card, the two hold-inspect refusal cards, and the quit card.
  </action>
  <acceptance_criteria>
    - Single wiring: `grep -cE 'getElementById\("mw-rail"\)\.onclick' mazeworld.html` returns 1 — anchored on the full assignment form, not on the bare id, which also appears in markup and prose.
    - Single stamp: a node check slices `renderRail`'s comment-stripped body and asserts it contains exactly one `railShownAt = Date.now()`; a file-wide check asserts exactly one `let railShownAt` declaration.
    - Timer routed through the module: the same sliced body contains exactly one `setTimeout(` and that call's delay expression contains `holdForCard`; it contains zero occurrences of the old four-digit default literal as a delay fallback — assert by matching the delay expression itself, not by a file-wide number grep.
    - Bridge extended without a new name: `node --test test/unit/bridge-registry.test.js` exits 0 (set-equality unchanged — `__mzRailVM` already exists), and `grep -cE '__mzRailVM = \{' mazeworld.html` returns 1.
    - Literals doubled: a node check asserts every `mzRailLine?.(` call site in the comment-stripped shell passes a hold argument that is a member of the new `RAIL_HOLD` value set or equals one of the four doubled literals — no pre-phase hold number survives at a call site.
    - Structural tap-through proof: a node check asserts `id="mw-rail"` does not appear inside the `<div class="mw-maze-viewport"` … closing slice, and that `initMazeViewportControls` binds `pointerdown` to the viewport element alone.
    - `node --test test/unit/shell-map-rail.test.js test/unit/shell-map-invariants.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bridge-registry.test.js test/unit/shell-map-rail.test.js test/unit/shell-map-invariants.test.js</automated>
  </verify>
  <done>One guarded body-tap handler with the four ordered checks, one line-scaled auto-clear timer, two new entries on an existing bridge, and no pre-phase hold literal left at a call site.</done>
</task>

<task type="auto">
  <name>Task 3: test/unit/rail-dismiss.test.js and the re-pinned renderRail source test</name>
  <files>test/unit/rail-dismiss.test.js, test/unit/shell-map-rail.test.js</files>
  <read_first>
    - `test/unit/shell-map-rail.test.js:215-240` — test (f): the renderRail pin covering decision precedence, the key gate, `armEncounterButtons`, the generic guardTap wiring, the five action ids, and "one setTimeout keyed on `.hold`". The timer half of that pin moves here.
    - `test/unit/shell-map-rail.test.js:360-365` — test (m), the arm-sweep selector pin. It must still hold unchanged, because this plan does NOT put `aria-disabled` on the rail container.
    - `test/unit/harness/shellSandbox.js:131-195` — `loadShellSandbox({doc})`, the stubbed `setTimeout` that never fires, and the fact that `renderRail` is stubbed out AFTER the classic script runs. Read this carefully: the harness replaces `context.renderRail`, so a test that wants the REAL `renderRail` must capture it before the stub or re-assign it back.
    - `test/unit/harness/recordingDom.js:105-200, 340-375` — `makeElement`'s surface: `onclick`, `dataset`, `classList`, `children`, `hidden`, and `getElementById`'s auto-create behaviour. Elements have no `closest` method, so a test drives the handler with a synthetic event object carrying its own `target`.
    - `test/unit/inputGuards.test.js` — the existing `isArmed` coverage this plan reuses rather than duplicating.
  </read_first>
  <action>
Re-pin `test/unit/shell-map-rail.test.js` test (f) so the timer clause reads "one setTimeout whose delay comes from `holdForCard`", keeping every other clause of that test byte-identical. Leave test (m) alone and add a one-line comment above it recording WHY it is unchanged: Phase 57's dismiss handler deliberately does not stamp `aria-disabled` on the rail container, so the descendant sweep selector still covers every element that needs clearing.

Create `test/unit/rail-dismiss.test.js` with two sections.

**Section A — the pure classifier and the bounds**, importing from `src/browser/rail.js` directly. Named tests: the two-kind totality sweep (the same matrix as Task 1's, restated here as the LAYOUT-02 acceptance rather than as module coverage, and asserting `RAIL_DISMISS_KINDS.length === 2` so a third state fails loudly); a one-line card's hold is at least the old pre-phase maximum, proving nothing got shorter; each additional line adds exactly `HOLD_PER_LINE` until the cap; the cap is reached and never exceeded.

**Section B — the shell handler's four branches**, through `loadShellSandbox`. For each branch, build a recording document, load the sandbox, restore the real `renderRail` if the harness stubbed it, set a fixed state, put a card on `window.__mzRail`, render, then invoke `#mw-rail`'s `onclick` with a synthetic event whose `target` is either a stub exposing a `closest` method that returns a truthy button (the button-target case) or one whose `closest` returns null (the body case). Assert:
1. Button target: the rail view-model is unchanged and `railPulse` did not run — the handler returned before classifying.
2. Unarmed body tap (stamp set to the current time, so the arm window has not elapsed): the view-model is unchanged.
3. Armed body tap on a locked card — once with `railLocked()` raised through a pending decision on the state, once with a live action row of one button: in both cases the card is still present and the pulse class was re-triggered on the rail element.
4. Armed body tap on a plain card with no buttons and no lock: the rail view-model is cleared and the element ends in its hidden/idle state.

Add a fifth test for the never-shown case: with no card ever rendered, invoking the handler throws nothing and leaves the view-model at its resting value.

Every count-shaped assertion in this file uses `stripHtml` from `tools/ident-sweep.mjs` or operates on a sliced function body — no bare substring counts over the whole file.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/rail-dismiss.test.js` exits 0 with both sections passing and at least nine named tests.
    - `node --test test/unit/shell-map-rail.test.js` exits 0; `git show 4d43edf:test/unit/shell-map-rail.test.js | grep -c '^test('` equals `grep -c '^test(' test/unit/shell-map-rail.test.js` (test (f) re-pinned, none dropped).
    - Teeth check, recorded in the SUMMARY: temporarily invert the locked branch so a locked card dismisses, confirm section B tests 3 FAIL, then revert. Separately, temporarily raise `HOLD_MAX` to a very large number and confirm the cap test FAILS, then revert.
    - `node --test "test/unit/**/*.test.js"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test "test/unit/**/*.test.js"</automated>
  </verify>
  <done>The classifier's totality, the hold bounds and all four handler branches are proven automatically, with recorded teeth checks, and no pre-existing rail test was dropped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → rail body | A body tap is now an action (dismiss). The boundary question is whether it can ever dismiss something the player must decide, or double as a move. |
| presentation state → movement lock | `railLocked()` gates every movement entry path; the dismiss handler must read it, never bypass it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-57-07 | Elevation of Privilege | Body tap clearing a pending decision card | high | mitigate | `railDismissKind` classifies a locked rail or any live action row as locked; the locked branch pulses and returns without touching `railTimer` or the view-model. Proven by section B test 3 with a demonstrated teeth check. |
| T-57-08 | Spoofing | The gesture that created a card also dismissing it | medium | mitigate | `isArmed(railShownAt, Date.now())` with the rail's own stamp, set in `renderRail()`'s new-card branch. Proven by section B test 2. |
| T-57-09 | Denial of Service | A body tap falling through and stepping the party | medium | mitigate | Structural: the rail is not a descendant of `#mw-maze-viewport` and the gesture tracker binds to that element alone. Asserted in Task 2's acceptance criteria. |
| T-57-10 | Denial of Service | An unbounded hold pinning a card on screen | low | mitigate | `HOLD_MAX` (16000 ms) caps the scaled hold, and tap-to-dismiss gives the player an unconditional exit on any non-decision card. |
| T-57-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install task exists in this phase — `package.json` and `package-lock.json` are untouched. Any install attempt would require a blocking human checkpoint. |
</threat_model>

<verification>
- `node --test "test/unit/**/*.test.js"` exits 0.
- `node --test test/unit/bridge-registry.test.js` green on both halves.
- `npm run build:www` exits 0.
- `git diff --stat 4d43edf..HEAD -- engine/ content/ test/parity/` is empty.
</verification>

<success_criteria>
- Every named hold is exactly twice its pre-phase value, and the tests carry the new numbers explicitly.
- `holdForCard` scales by line count inside a named floor and a named cap.
- `railDismissKind` is total over exactly two kinds; a locked card can only be cleared by making its decision.
- A tap on a rail button routes to the button; a tap inside the arm window is swallowed; no rail tap reaches `tapStep()`.
- No new `window.__mz*` name was created.
</success_criteria>

<output>
Create `.planning/phases/57-map-hud-layout-band/57-03-SUMMARY.md` when done.
</output>
