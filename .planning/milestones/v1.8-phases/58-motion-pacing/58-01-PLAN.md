---
phase: 58-motion-pacing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/motion.js
  - src/browser/cameraGlide.js
  - src/browser/typewriter.js
  - test/unit/motion.test.js
  - test/unit/camera-glide.test.js
  - test/unit/typewriter.test.js
autonomous: true
requirements: [MOTION-01, MOTION-02, MOTION-04, MOTION-05]

must_haves:
  truths:
    - "`src/browser/motion.js#prefersReducedMotion(win)` is the ONE reduced-motion predicate for every JS-timed effect in this phase (D-17). It calls `win.matchMedia` with the exported `REDUCED_MOTION_QUERY` FRESH on every call and returns that list's `matches`, so a change to the OS setting mid-session is seen by the very next read. When `matchMedia` is missing, is not a function, or throws, it returns true: an effect that cannot tell whether motion is welcome resolves instantly to its end state, which is always correct."
    - "`onReducedMotionChange(win, cb)` subscribes to the media-query list's change notification and calls `cb(boolean)` with the new value. It returns an unsubscribe function, is a no-op (returning a no-op unsubscribe) when the API is missing, and never throws. This is how an effect already in flight snaps to its end state when the user turns reduced motion on mid-effect."
    - "`createPanelMotion(...)` is the ONE shared close helper (D-08). `close(el, onHidden)` sets `el.hidden = true` only after `CLOSE_MS + CLOSE_SLACK_MS` on the injected timer, and marks the element `data-motion=\"closing\"` meanwhile so CSS can run the close animation. Under reduced motion it sets `hidden` synchronously and writes nothing else. `open(el)` cancels a pending close on that element (no `onHidden`) and un-hides it. `finishAll()` lands every pending close now. Closing an element that is already hidden and not closing is a no-op. Nothing can be stranded invisible (the timer always fires) or stuck visible (open always cancels)."
    - "The close helper never waits on a completion event. It never listens for the transition-end or animation-end DOM events: the Phase 32 CSCR-08 rule forbids completion-event listeners, and the blanket prefers-reduced-motion CSS rule zeroes animations, so they would never fire. The decision's 'timeout fallback' is therefore the mechanism itself, and the timer is set to the CSS close duration plus one frame of slack so `hidden` always lands after the visual close has ended."
    - "`src/browser/cameraGlide.js#createCameraGlide(...)` eases a camera point toward a target over `PAN_MS` (200) with ease-out cubic (D-04). `to(from, target, apply)` retargets mid-flight from the point the caller passes, which is the currently displayed camera, so rapid tap-to-move re-aims and never queues or stutters. `cancel()` freezes where it is and applies nothing more, so the finger wins (D-02). `finish()` applies the target now. Under reduced motion `to` applies the target synchronously and schedules nothing. If reduced motion turns on mid-flight, the very next frame applies the target."
    - "`src/browser/typewriter.js` types text on at `TYPE_MS_PER_CHAR` (12) per character with a `TYPE_MAX_MS` (700) cap per block (D-13), so a long card speeds up rather than dragging. While a block types, each target holds a typed span plus an invisible rest span, which reserves the block's final layout (a card never grows line by line). The `ariaHost` carries aria-hidden for exactly the typing window and gets its prior value restored afterwards (D-16). `complete(key)` writes the full text and calls `onDone` once. `cancel(key)` writes the full text and never calls `onDone`. Starting a new block on the same key cancels the old one. `adopt(key, targets, ariaHost)` hands an in-flight block to freshly rendered elements at its CURRENT progress, keeping its start time and its `onDone`. The shell re-renders on every `paint()` (paint calls renderRail and renderEncounter), so without `adopt` a same-content re-render mid-typing would cut the effect short or restart it. Under reduced motion `type` writes the full text, calls `onDone` synchronously, and never touches aria-hidden."
    - "Every duration is a named, exported, test-pinned constant: `OPEN_MS` 180, `MENU_OPEN_MS` 160 (the mock's `.16s` menu rise), `CLOSE_MS` 120, `CLOSE_SLACK_MS` 34, `PAN_MS` 200, `TYPE_MS_PER_CHAR` 12, `TYPE_MAX_MS` 700. `CLOSE_MS + CLOSE_SLACK_MS` is below `inputGuards.js#DISMISS_SETTLE_MS` (250). A tap that lands during a close animation is therefore still inside the Date.now() settle window and can never reach the map unguarded."
    - "All three modules are pure presentation cores. They have no reference to the `window`/`document` globals (every clock, scheduler, document and predicate is injected), no import from engine/ or content/, no pseudo-random source and no new `window.__mz*` name. `node --test` can drive them with a hand-rolled fake clock."
  artifacts:
    - src/browser/motion.js
    - src/browser/cameraGlide.js
    - src/browser/typewriter.js
    - test/unit/motion.test.js
    - test/unit/camera-glide.test.js
    - test/unit/typewriter.test.js
  key_links:
    - "These exports are the contract for plans 58-03 (camera glide), 58-04 (panel motion) and 58-05/58-06 (typewriter). The module script constructs one instance of each with the real clock and `reduced: () => prefersReducedMotion(window)`, and bridges it for the classic script. The classic script never imports these files."
    - "shell-no-content-copies.test.js criterion 3 fails the build if mazeworld.html declares a name any src/browser module exports. Every export name chosen here must be absent from mazeworld.html's classic and module scripts (checked in Task 3)."
    - "bridge-registry.test.js scans src/browser/ for live `__mz` names. These modules introduce none, so no BRIDGE row is owed by this plan."
  prohibitions:
    - "MUST NOT modify mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md or any existing test file. This plan is wave 1 and must stay file-disjoint from 58-02."
    - "MUST NOT modify any file under engine/ or content/; MUST NOT add, remove or reorder any rng draw; MUST NOT move a parity fixture."
    - "MUST NOT add a completion-event listener (transition-end or animation-end) anywhere, and MUST NOT make any tap-safety guard depend on a transition or animation."
    - "MUST NOT read the `window` or `document` global inside these modules. The caller always passes them in."
    - "MUST NOT add an in-app reduced-motion setting. The OS preference is the only input (D-17; the in-app toggle is a Deferred Idea)."
    - "MUST NOT add a third-party dependency or a network call."
---

<objective>
Build the three pure motion cores every later Phase 58 plan wires into the shell: the shared reduced-motion predicate and animated-close helper (`src/browser/motion.js`), the retargetable camera tween (`src/browser/cameraGlide.js`), and the typing schedule and controller (`src/browser/typewriter.js`).

Per D-02, D-04, D-07, D-08, D-13, D-16 and D-17 (numbered in 58-CONTEXT.md's decision-bullet order: map pan D-01..D-04, panels D-05..D-08, combat beat D-09..D-12, typed text and reduced motion D-13..D-17).

Purpose: MOTION-01/02/04/05 need timed JS effects that are testable without a browser and that all consult ONE reduced-motion predicate. The Modularity gate puts each effect in a `src/browser/` module with a pure core and a thin shell call site. This plan is that core. It touches no shell file, so it runs in parallel with 58-02.

Output: three modules, three test files, every duration a named constant with a pinned test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-motion-pacing/58-CONTEXT.md
</context>

<discovery>
## Read before Task 1

**Record the base.** Run `git log --diff-filter=A --format=%H -- .planning/phases/58-motion-pacing/58-CONTEXT.md | tail -1` and record it in the SUMMARY as BASE_58 (the commit that added this phase's CONTEXT; Phase 57 is closed by the time any 58 plan executes, and the orchestrator also records Phase 57's closing commit in STATE.md). Every engine gate below diffs against BASE_58. (BASE_58 resolves to a01b38e, which predates 57-05's shell commits. Use it only for the engine/content/parity diff and the never-shrink test counts, which Phase 57 cannot disturb. Plan 58-07's byte comparison of `paint()`/`draw()` uses the commit just before Phase 58's first code instead.)

**House patterns to copy.** `src/browser/inputGuards.js` is the model for a clock-free module: every time reading arrives as an argument (`now`), and the head comment explains why no guard may ever be a CSS transition. `src/browser/controls.js` is the model for pure presentation math (`keepInViewAxis`). `src/browser/sfx.js`'s `createVariation()` is the model for a small factory returning a closure-backed object.

**Why the close helper is timer-driven.** `test/unit/shell-combat-screen.test.js` (the CSCR-08 test "no transitionend/animationend listener exists anywhere") and `shell-input-guards.test.js` pin that the shell never listens for a completion event, because the blanket reduced-motion rule (`@media (prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}`, pinned byte-exact by shell-map-hud.test.js) makes such events never fire. D-08 asks for "hidden only after the close transition ends, with a timeout fallback". A timer set to the CSS close duration plus a frame of slack satisfies both halves: hidden lands after the visual close by construction, and the timer cannot be stranded. Record this reading in the SUMMARY.

**Typing layout.** Writing a growing prefix into a text node makes a multi-line rail card grow line by line while it types, which is exactly the "jarring" motion the user asked to remove. The controller therefore writes two spans per target: the typed prefix, and the rest of the text in a span carrying the class `mw-type-rest`. Plan 58-05 styles that class `visibility:hidden`, so the block holds its final size from the first frame. When typing ends, the target's `textContent` becomes the plain full text again.
</discovery>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Name | Notes |
|---|---|---|
| New module | `src/browser/motion.js` | `REDUCED_MOTION_QUERY`, `prefersReducedMotion`, `onReducedMotionChange`, `OPEN_MS`, `MENU_OPEN_MS`, `CLOSE_MS`, `CLOSE_SLACK_MS`, `EASE_OUT_CSS`, `EASE_IN_CSS`, `createPanelMotion`. |
| New module | `src/browser/cameraGlide.js` | `PAN_MS`, `easeOutCubic`, `glidePoint`, `createCameraGlide`. |
| New module | `src/browser/typewriter.js` | `TYPE_MS_PER_CHAR`, `TYPE_MAX_MS`, `TYPE_REST_CLASS`, `typeDurationMs`, `typedCount`, `splitTyped`, `createTypewriter` (`type`/`adopt`/`complete`/`cancel`/`active`/`completeAll`). |
| New tests | `test/unit/motion.test.js`, `test/unit/camera-glide.test.js`, `test/unit/typewriter.test.js` | Pure and fake-clock behaviour pins. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/browser/motion.js: the one reduced-motion predicate, the change subscription, the durations and the shared close helper</name>
  <files>src/browser/motion.js, test/unit/motion.test.js</files>
  <read_first>
    - `src/browser/inputGuards.js`, the whole file: the clock-free house style, the `ARM_DELAY_MS`/`DISMISS_SETTLE_MS` constants this module's durations are checked against, and its head comment on why no guard may be a transition.
    - `mazeworld.html` `@media (prefers-reduced-motion:reduce)` (~L867): the blanket CSS rule. It stays byte-identical in this phase. This module is the JS twin of it.
    - `mazeworld.html` `closeMarksLegend` / `closeCampSheet` (~L4040-4080) and `.mw-legend-sheet[hidden]` (~L1136): the `hidden` toggles plan 58-04 routes through `createPanelMotion`. Note that every close already stamps `lastDismissAt`.
    - `test/unit/shell-combat-screen.test.js`, the CSCR-08 completion-listener test (~L282), and `test/unit/shell-input-guards.test.js` `guardHelpersRegion` (~L66, ~L124): the rules this module must also obey.
  </read_first>
  <behavior>
    - prefersReducedMotion({ matchMedia: (q) => ({ matches: q === REDUCED_MOTION_QUERY }) }) is true; with matches false it is false.
    - A stub whose `matches` flips between two calls returns the new value on the second call. No caching.
    - prefersReducedMotion(undefined), prefersReducedMotion({}), and a matchMedia that throws all return true.
    - onReducedMotionChange with a stub list exposing addEventListener("change", fn): firing the stored fn with { matches: true } calls cb(true); the returned function removes the listener; a stub with no addEventListener but a legacy addListener still subscribes; a missing API returns a callable no-op and throws nothing.
    - Durations: OPEN_MS 180, MENU_OPEN_MS 160, CLOSE_MS 120, CLOSE_SLACK_MS 34; EASE_OUT_CSS "ease-out", EASE_IN_CSS "ease-in"; CLOSE_MS + CLOSE_SLACK_MS < DISMISS_SETTLE_MS (imported from inputGuards.js).
    - createPanelMotion with a fake timer queue and reduced false: close(el, cb) leaves el.hidden false, sets el.dataset.motion "closing", and schedules exactly one timer at CLOSE_MS + CLOSE_SLACK_MS. Advancing the fake timer sets el.hidden true, removes dataset.motion, and calls cb once.
    - open(el) during a pending close clears that timer, removes dataset.motion, keeps el.hidden false, and never calls cb. Advancing time afterwards changes nothing.
    - close on an already-hidden, not-closing element does nothing: no timer, no dataset write, no cb. A second close while closing does not add a second timer.
    - With reduced true, close(el, cb) sets hidden true synchronously, calls cb once, schedules no timer, and leaves el.dataset without a motion key.
    - finishAll() with two pending closes hides both, calls both callbacks once, and leaves no timer live.
    - close(null) and open(null) are no-ops that do not throw.
  </behavior>
  <action>
Create `src/browser/motion.js` as a pure, clock-free module, per D-07, D-08 and D-17. The head comment names Phase 58 (MOTION-02/05). It says this module is the JS twin of the blanket reduced-motion CSS rule, that every JS-timed effect in the shell consults `prefersReducedMotion` and nothing else, and that the close helper is timer-driven on purpose (the CSCR-08 rule plus the reduced-motion zeroing; see the discovery note). Import nothing; the relation to `DISMISS_SETTLE_MS` is pinned by the test, not by a runtime import.

Exports:
- `REDUCED_MOTION_QUERY`, the string `(prefers-reduced-motion: reduce)`.
- `prefersReducedMotion(win)`. Inside a try, call `win.matchMedia(REDUCED_MOTION_QUERY)` fresh on every call and return a strict boolean of its `matches`. Return true when `win` is falsy, when `win.matchMedia` is not a function, or when anything throws. Document the fail-to-instant rule in its JSDoc.
- `onReducedMotionChange(win, cb)`. Obtain the media-query list the same way. Subscribe with `addEventListener("change", handler)` when it exists, else with the legacy `addListener(handler)`. `handler` calls `cb(!!event.matches)` inside a try. Return a function that removes the same handler through the matching API. Return a no-op function when anything is missing or throws. Never throw.
- `OPEN_MS = 180`, `MENU_OPEN_MS = 160` (comment: the mock's `.16s` menu rise, `design/Mazeworld Map.dc.html`, within D-07's "~180"), `CLOSE_MS = 120`, `CLOSE_SLACK_MS = 34` (comment: two 60 Hz frames, so the hide lands after the CSS close has painted its last frame), `EASE_OUT_CSS = "ease-out"`, `EASE_IN_CSS = "ease-in"`. Each gets a one-line JSDoc naming D-07.
- `createPanelMotion({ setTimeout, clearTimeout, reduced, closeMs = CLOSE_MS, slackMs = CLOSE_SLACK_MS })` returns `{ open, close, isClosing, finishAll }`, closing over a `Map` from element to `{ timer, onHidden }`:
  - `close(el, onHidden)`: return if `el` is falsy. Return if `el.hidden` is already true and the element is not pending. Return if it is already pending (no second timer). If `reduced()` is truthy, set `el.hidden = true`, call `onHidden` if it is a function, and return. Otherwise set `el.dataset.motion = "closing"`, schedule `finish(el)` with `setTimeout` after `closeMs + slackMs`, and record it.
  - `finish(el)` (internal): take and delete the pending record, `delete el.dataset.motion`, set `el.hidden = true`, and call the recorded `onHidden` once inside a try.
  - `open(el)`: return if `el` is falsy. If pending, `clearTimeout` its timer, delete the record, and `delete el.dataset.motion`, WITHOUT calling `onHidden`. Then set `el.hidden = false`.
  - `isClosing(el)`: whether `el` is pending.
  - `finishAll()`: clear each pending timer and `finish` every pending element.
  - Every method is wrapped so it never throws into a caller. Write NO dataset key or attribute on the reduced path and on the open path beyond what is listed. Plan 58-04's snapshot fixtures depend on the reduced path leaving the DOM exactly as a bare `hidden` toggle would.

The module holds no completion-event listener, no global `window`/`document` read and no pseudo-random source.

Create `test/unit/motion.test.js` with one named test per `<behavior>` bullet. Use a tiny hand-rolled fake timer queue inside the test file (an array of `{ id, at, fn }`, a `now` counter, and an `advance(ms)` that runs due entries in time order). Build fake elements as plain objects `{ hidden, dataset: {} }`. Add two source tests over the comment-stripped module (`stripJs` from `tools/ident-sweep.mjs`): the stripped source contains neither the transition-end nor the animation-end event name, and it references neither the `window` global nor the `document` global as an identifier (use `\bwindow\.` and `\bdocument\.` on the stripped text).
  </action>
  <acceptance_criteria>
    - `node --test test/unit/motion.test.js` exits 0, with one named test per behaviour bullet plus the two source tests.
    - A node check imports `src/browser/motion.js` and asserts `OPEN_MS === 180 && MENU_OPEN_MS === 160 && CLOSE_MS === 120 && CLOSE_SLACK_MS === 34` and `CLOSE_MS + CLOSE_SLACK_MS < DISMISS_SETTLE_MS` (DISMISS_SETTLE_MS imported from `src/browser/inputGuards.js`). Exits 0.
    - Teeth check, recorded in the SUMMARY: temporarily make `prefersReducedMotion` cache its first answer in a module-scope variable, and confirm the "no caching" test FAILS. Revert with `git checkout -- src/browser/motion.js` (core.autocrlf=true; never hand-undo).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/motion.test.js</automated>
  </verify>
  <done>`motion.js` exports the one live reduced-motion predicate, its change subscription, the pinned durations and a cancellable, timer-driven close helper, all proven with a fake clock.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/browser/cameraGlide.js: the retargetable, cancellable ease-out camera tween</name>
  <files>src/browser/cameraGlide.js, test/unit/camera-glide.test.js</files>
  <read_first>
    - `mazeworld.html` `let cam = { x: 0, y: 0 };` and its comment block (~L1885-1898): `cam` is the grid point under the viewport centre, in cell units, fractional. The tween interpolates that point, never CSS pixels, so a zoom change mid-glide cannot break it.
    - `mazeworld.html` `keepPartyInView()` (~L4105), `centerMap()` (~L4092), `anchorCamOnParty(offsetPx)` (~L1917) and the pointer pipeline `initMazeViewportControls` (~L4175): the call sites plan 58-03 wires. Read them now so the API fits them.
    - `src/browser/controls.js#keepInViewAxis`: the pure house style for camera math.
  </read_first>
  <behavior>
    - PAN_MS === 200. easeOutCubic(0) === 0, easeOutCubic(1) === 1, easeOutCubic(0.5) === 0.875, and inputs below 0 or above 1 clamp.
    - glidePoint({x:0,y:0}, {x:10,y:-4}, 1) is {x:10,y:-4}; at t=0 it equals from.
    - With a fake clock and fake raf (the raf queue runs on advance at 16 ms frames), reduced false: to({x:0,y:0}, {x:10,y:0}, apply) applies nothing synchronously and schedules one frame. Frames apply monotonically increasing x strictly between 0 and 10. The first frame at or after 200 ms applies exactly {x:10,y:0}, after which active() is false and no frame is scheduled.
    - Retarget: at 100 ms, to(lastApplied, {x:20,y:0}, apply) continues from lastApplied (the next applied x is at least lastApplied.x, with no jump backward) and lands exactly {x:20,y:0} 200 ms after the retarget. target() reports {x:20,y:0} while in flight.
    - cancel() mid-flight: no further apply, active() false, target() null.
    - finish() mid-flight: applies exactly the target once, synchronously; no frame remains.
    - Reduced true at to(): applies the target synchronously once; no frame scheduled; active() false.
    - Reduced flips to true mid-flight: the next frame applies the target exactly and stops.
    - durationMs 0 behaves like reduced (synchronous landing).
  </behavior>
  <action>
Create `src/browser/cameraGlide.js`, a pure module, per D-02 and D-04. The head comment names Phase 58 (MOTION-01). It records the ruling that this changes HOW the camera moves, never WHEN (the Phase 35 / 260918-vm3 stationary-camera ruling), and that drag and pinch never use it (D-02). Import nothing.

Exports:
- `PAN_MS = 200` (JSDoc: D-04's ~200 ms ease-out).
- `easeOutCubic(t)`: clamp t to [0, 1], then return `1 - (1 - t) ** 3`.
- `glidePoint(from, to, t)`: returns a new `{ x, y }`, from plus (to minus from) times `easeOutCubic(t)`, per axis.
- `createCameraGlide({ now, raf, cancelRaf, reduced, durationMs = PAN_MS })` returns `{ to, cancel, finish, target, active }`, closing over `run = null | { from, target, t0, apply, frame }`:
  - `to(from, target, apply)`: copy both points. If `reduced()` is truthy or `durationMs <= 0`: cancel any run, call `apply(copy of target)`, and return. Otherwise cancel any pending frame of an existing run, set `run = { from, target, t0: now(), apply }`, and schedule `step` through `raf`. Because the caller passes the currently displayed point as `from`, a retarget re-aims from wherever the map is and always takes a fresh `durationMs`.
  - `step` (internal): if there is no run, return. If `reduced()` is truthy or `(now() - t0) / durationMs >= 1`, apply a copy of the target, clear the run and return. Otherwise apply `glidePoint(from, target, elapsed / durationMs)` and schedule the next `step`.
  - `cancel()`: `cancelRaf` the pending frame, clear the run, and apply nothing.
  - `finish()`: if a run exists, cancel its frame, apply its target once and clear it.
  - `target()`: a copy of the run's target, or null. `active()`: whether a run exists.
  - Wrap `apply` calls in a try, so a throwing apply ends the run instead of leaving a scheduled frame.

Create `test/unit/camera-glide.test.js` with one named test per `<behavior>` bullet. Use an inline fake clock whose `advance(ms)` steps in 16 ms frames and runs the raf queue each frame. Add the same two stripped-source checks as Task 1 (no window/document global, no completion-event name).
  </action>
  <acceptance_criteria>
    - `node --test test/unit/camera-glide.test.js` exits 0 with every behaviour bullet as a named test.
    - Teeth check, recorded in the SUMMARY: temporarily make `to()` ignore its `from` argument and restart from the ORIGINAL run's `from` on a retarget, and confirm the "retarget continues from lastApplied" test FAILS. Revert with `git checkout -- src/browser/cameraGlide.js`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/camera-glide.test.js</automated>
  </verify>
  <done>The camera tween eases out over 200 ms, retargets from the displayed point, cancels without moving, finishes exactly on target, and lands synchronously under reduced motion or a mid-flight preference change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: src/browser/typewriter.js: the typing schedule and a keyed, accessible, cancellable controller</name>
  <files>src/browser/typewriter.js, test/unit/typewriter.test.js</files>
  <read_first>
    - `mazeworld.html` `renderRail()` (~L3665): how `#mw-rail-lines` is built from `.mw-rail-line` / `.mw-rail-roll` divs, and `syncRailLive()`. The rail's typed block is every line and roll element in document order, and the announcer keeps receiving the full text (D-16).
    - `mazeworld.html` `renderMajorOverlay(host, spec)` (~L2967) and `renderFightLog(host)` (~L2911): the other two typing hosts (`.mw-major-line`, `.cb-log-text`).
    - `test/unit/harness/recordingDom.js` (~L100-180, ~L320-335): what a recording element supports (textContent, className, dataset, setAttribute/getAttribute/removeAttribute, appendChild). Plans 58-05/58-06 run this controller against it, so use nothing beyond that surface.
  </read_first>
  <behavior>
    - TYPE_MS_PER_CHAR === 12 and TYPE_MAX_MS === 700. typeDurationMs(0), typeDurationMs(-3) and typeDurationMs(NaN) are 0. typeDurationMs(10) is 120. typeDurationMs(58) is 696. typeDurationMs(59) and typeDurationMs(5000) are 700.
    - typedCount(40, 0) is 0; typedCount(40, 240) is 20; typedCount(40, 480) is 40; typedCount(40, 10000) is 40; typedCount(200, 350) is 100 (the capped block runs at 200/700 characters per ms).
    - splitTyped(["abc", "defg"], 5) is [{typed:"abc",rest:""},{typed:"de",rest:"fg"}]; count 0 gives all rest; count past the total gives all typed.
    - createTypewriter with a fake clock/raf, a fake doc, and reduced false: type("rail", { targets:[a,b], texts:["abc","defg"], ariaHost:h, onDone }) sets h's aria-hidden to "true" synchronously. Each target holds exactly two children (typed span, then a span whose className is TYPE_REST_CLASS). Across frames the concatenated typed text grows monotonically. When the block completes (the first frame at or after typeDurationMs(7)), a and b textContent are exactly "abc"/"defg" with no child spans, h's aria-hidden is removed, onDone fires exactly once, and active("rail") is false.
    - An ariaHost that already carried aria-hidden="true" before typing keeps "true" afterwards (prior value restored, not blindly removed).
    - complete("rail") mid-block writes full text, restores aria, fires onDone once, and returns true. A second complete returns false and does not fire onDone again.
    - cancel("rail") mid-block writes full text, restores aria, and never fires onDone.
    - type on a key that is already typing cancels the old block (its targets get their full text, its onDone never fires) and starts the new one.
    - completeAll() completes every active key.
    - adopt("rail", [c, d], h2) at 60 ms into a 7-character block moves the run: c and d immediately hold typed/rest spans at the SAME typed count the old targets showed, h2 gets aria-hidden "true", the old host's prior aria value is restored, typing continues from that count (never from 0), and the block completes at the ORIGINAL end time with onDone once. adopt on an inactive key returns false and writes nothing; adopt with a different number of targets than the run's texts cancels the run (no onDone) and returns false.
    - Reduced true at type(): full text written synchronously, onDone fired synchronously once, aria-hidden never set (the stub records zero setAttribute calls for it), no frame scheduled.
    - Reduced flips to true mid-block: the next frame completes the block (onDone once).
    - A block whose texts are all zero-length completes synchronously with onDone once and never sets aria-hidden.
  </behavior>
  <action>
Create `src/browser/typewriter.js`, a pure module, per D-13 and D-16. The head comment names Phase 58 (MOTION-04/05). It states three rules. What types is decided by the caller (rail cards, the encounter overlay's line, fight-log rows as the combat beat reveals them); the Oracle history, HUD numbers and buttons never type (D-14). The element that is visibly typing is aria-hidden for exactly the typing window, and the caller keeps feeding the complete text to its announcer or description node (D-16). The rest span reserves the final layout. Import nothing.

Exports:
- `TYPE_MS_PER_CHAR = 12`, `TYPE_MAX_MS = 700` (JSDoc: D-13, "fast, not slow"; a long block speeds up rather than drags), and `TYPE_REST_CLASS = "mw-type-rest"`.
- `typeDurationMs(chars)`: 0 for a non-finite or non-positive count; otherwise `Math.min(chars * TYPE_MS_PER_CHAR, TYPE_MAX_MS)`.
- `typedCount(total, elapsedMs)`: an integer in [0, total]. Return total when `elapsedMs >= typeDurationMs(total)`. Otherwise return `Math.floor(total * elapsedMs / typeDurationMs(total))`, clamped.
- `splitTyped(texts, count)`: distribute `count` characters across `texts` in order, returning `[{ typed, rest }]`.
- `createTypewriter({ now, raf, cancelRaf, reduced, doc })` returns `{ type, adopt, complete, cancel, active, completeAll }`, closing over a `Map` from key to a run `{ targets, texts, spans, ariaHost, prevAria, onDone, t0, total, frame }`:
  - `type(key, { targets, texts, ariaHost = null, onDone = null })`: first `cancel(key)` if that key is active. Normalise `texts` to strings, defaulting missing entries to "". If `reduced()` is truthy or the total length is 0, write each target's `textContent` as its full text, call `onDone` once, and return. Otherwise record the ariaHost's current aria-hidden value (null when absent) and set it to "true". For each target, clear it and append two spans made with `doc.createElement("span")` (the second gets `className = TYPE_REST_CLASS`). Record `t0 = now()`, write the first frame (typed text empty, rest text full), and schedule `step` through `raf`.
  - `step(key)`: if `reduced()` is truthy or `typedCount(total, now() - t0) >= total`, run the internal `finishRun(key, true)`. Otherwise write each target's typed/rest span text from `splitTyped` and schedule the next step.
  - `finishRun(key, callDone)`: `cancelRaf` the frame, delete the run, set each target's `textContent` to its full text (this drops the spans), then restore aria (remove the attribute when the prior value was null, otherwise set it back). If `callDone` is set, call `onDone` once inside a try.
  - `adopt(key, targets, ariaHost = null)`: return false when the key has no run. When `targets.length` differs from the run's text count, `finishRun(key, false)` and return false. Otherwise restore the old host's aria value, record the new host's prior value and set it to "true", build fresh typed/rest spans on each new target, swap them into the run (keeping `t0`, `total`, `texts` and `onDone`), write the current frame immediately, and return true. The scheduled frame keeps running against the new spans.
  - `complete(key)`: when active, `finishRun(key, true)` and return true; otherwise return false. `cancel(key)`: when active, `finishRun(key, false)`. `active(key)`: whether that key has a run. `completeAll()`: complete every active key.
  - Every public method is wrapped so it never throws into a render path.

Create `test/unit/typewriter.test.js` with one named test per `<behavior>` bullet. Use an inline fake clock/raf and a minimal fake doc whose elements model `textContent` (setting it replaces all children with one text value; getting it concatenates the children's text), `appendChild`, `className`, and `setAttribute`/`getAttribute`/`removeAttribute` with a call log. Add the two stripped-source checks from Task 1.

**Name check before committing (all three modules).** Run a node script that collects every export name from the three new modules and asserts that none is declared with `const`/`let`/`var`/`function` in `mazeworld.html` (the same regex shell-no-content-copies.test.js criterion 3 uses). Then run `node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js`.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/typewriter.test.js` exits 0 with every behaviour bullet as a named test.
    - `node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js` exits 0 (no export name collides with a shell declaration, and no new live bridge name appeared).
    - Teeth check, recorded in the SUMMARY: temporarily remove the aria restore from `finishRun` and confirm the "prior value restored" and "aria-hidden is removed" tests FAIL. Revert with `git checkout -- src/browser/typewriter.js`.
    - Phase gate for this plan: `npm test` reports fail 0; `npm run build:www` exits 0; `git diff --stat "$BASE_58"..HEAD -- engine/ content/ test/parity/` is empty; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The typing schedule is pinned at 12 ms per character with a 700 ms block cap, and the keyed controller types without reflow, hides only the typing element from assistive tech, and lands synchronously under reduced motion. The three modules pass the name-collision and bridge checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS accessibility setting → shell | `prefers-reduced-motion` arrives through `matchMedia`; it is the only input and it is trusted. |
| timer/raf → DOM | Injected schedulers drive every write; a stranded timer or frame is the failure mode. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-58-01 | Denial of Service | A panel stranded visible or invisible by a close that never completes | high | mitigate | `createPanelMotion` hides on a timer, never on a completion event; `open` cancels a pending close; `finishAll` lands everything. Each is pinned in motion.test.js. |
| T-58-02 | Denial of Service | A reduced-motion user losing information to an effect that waits on animation | high | mitigate | `prefersReducedMotion` fails to true, and every controller lands its end state synchronously when it reads true (motion/camera-glide/typewriter tests). |
| T-58-03 | Information Disclosure (accessibility) | A screen reader reading half-typed text | medium | mitigate | The typewriter hides the typing host from assistive tech for exactly the typing window and restores the prior value (typewriter.test.js). |
| T-58-SC | Tampering | npm installs | high | mitigate | No package install in this plan; `package.json`/`package-lock.json` are untouched. |
</threat_model>

<verification>
- `node --test test/unit/motion.test.js test/unit/camera-glide.test.js test/unit/typewriter.test.js` exits 0.
- `npm test` reports fail 0; `npm run build:www` exits 0.
- `git diff --stat "$BASE_58"..HEAD -- engine/ content/ test/parity/` is empty.
</verification>

<success_criteria>
- One reduced-motion predicate exists, reads the OS setting live, and fails to instant.
- A timer-driven, cancellable close helper exists with pinned 180/160/120 ms durations.
- A retargetable, cancellable 200 ms ease-out camera tween exists.
- A keyed typewriter exists at 12 ms per character with a 700 ms cap; it reserves layout and hides the typing host from assistive tech for exactly the typing window.
- No shell file changed; no engine byte changed.
</success_criteria>

<output>
Create `.planning/phases/58-motion-pacing/58-01-SUMMARY.md` when done.
</output>
