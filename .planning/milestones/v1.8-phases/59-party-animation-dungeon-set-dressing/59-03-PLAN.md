---
phase: 59-party-animation-dungeon-set-dressing
plan: 03
type: execute
wave: 2
depends_on: ["59-01", "59-02"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/party-sprite-shell.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/reduced-motion.test.js
autonomous: true
requirements: [ANIM-01, ANIM-03]

must_haves:
  truths:
    - "The party marker is a DOM sprite over the canvas, `#mw-party-sprite`, the next sibling of `#mw-party-pulse` inside `.mw-maze-viewport` (D-01). `draw()` no longer paints the party at all: the party-icon draw, the canvas radial glow under it and the dot fallback are gone, and draw() only shrank."
    - "While the party stands still, the sprite plays the idle loop over the shipped `party_idle_1..4` art: ~280 ms a frame, a ~1.1 s cycle (ANIM-01, D-02). It is a CSS frame cycle with no canvas redraw and no JS timer: four stacked frame images share one opacity-only keyframe, phase-shifted by `idleFrameDelaysMs()` so exactly one is opaque at a time. The keyframe animates only opacity (the 2026-09-17 composited-only PERF rule), and a test pins every CSS duration and delay to partySprite.js's constants."
    - "Reduced motion (D-05): the pinned blanket rule removes every animation, so each frame falls back to its base style. Only idle frame 1's base style is opaque, so the idle loop freezes on frame 1. The ring's `mwglow` pulse stops the same way (the glow does not pulse)."
    - "The sprite sits exactly on the party's canvas cell, in lockstep with the canvas. `positionCanvas()` calls `positionPartySprite(rect)` directly before its final `positionPartyPulse(rect)`. The sprite is placed by a `translate3d` computed by `window.__mzPartySprite.box` from the canvas's own device-pixel-rounded origin, so at rest its offset equals the canvas's `translate3d` plus `px*CELL`/`py*CELL`, at dpr 1 and 2.625 alike, and it moves on every camera-glide frame because `applyCam` calls `positionCanvas`."
    - "The ring is dialled back (ANIM-03, D-04): the hard 3 px dark ring is gone. A faint warm glow stays at half of today's strength: the outer halo's alpha drops from .5 to .25, and the soft radial glow the canvas used to paint under the party moves onto the ring element at half its alpha (.55 to .28), so it travels with the marker. The `mwglow` pulse, `will-change` and the `.covered` rule are unchanged."
    - "The sprite never swallows a tap (`pointer-events:none`), sits above the ring and below the rail (z 3, between the ring's 2 and the rail's 4), and hides and pauses whenever the encounter panel covers the map, through a CSS sibling rule on the ring's existing `.covered` class, so renderEncounter's pinned toggles are untouched."
    - "The art is fail-safe (D-01): the markup starts at `data-art=\"static\"`, which shows the shipped `party.png`, the first frame and the fallback. After boot the module script sets `frames` once all 8 frame images have decoded, `static` if only `party.png` did, or `none` (a plain gold dot) if neither did, so the party is never invisible."
    - "`window.__mzPartySprite` is the one new bridge name: ONE `createPartySprite` instance built with `reduced: () => prefersReducedMotion(window)` (Phase 58's one predicate) and `render` bound to the classic `positionParty`. `settleAllMotion()` finishes it. Its BRIDGE row and `node tools/bridge-doc.mjs --write` land in the same commit, and Phase 58's cross-effect audit is extended to it."
    - statement: "On the Pixel 7, the standing party gently cycles its idle frames (about one breath a second), reads as the highlight on its own, and the old black ring is gone, leaving only a soft warm glow."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "With Android's Remove animations on, the standing party shows its first idle frame, still, with a steady glow."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "The marker stays glued to its square while the map glides, drags and pinches, and disappears under the encounter panel with no flicker when a fight opens."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
  artifacts:
    - test/unit/party-sprite-shell.test.js
  key_links:
    - "`positionCanvas()` → `positionPartySprite(rect)` → `positionPartyPulse(rect)` is the one lockstep path: the canvas, the sprite and the ring are placed in the same call from the same `cam`. `positionPartySprite` is defined AFTER `positionPartyPulse`, never between it and `positionCanvas`, because shell-map-viewport (m) slices `positionCanvas` up to `function positionPartyPulse(rect)` and pins that it ends with `positionPartyPulse(rect);`."
    - "`partyShown()` (classic) is the ONE read of where the marker is drawn: `window.__mzPartySprite.displayed(partyCentre())`, or `partyCentre()` when the bridge is missing. Both the sprite and the ring read it. Tap hit-testing keeps reading `cameraPan()` (the real party), untouched."
    - "Plan 59-04 adds the step trigger. It calls `window.__mzPartySprite.stepTo` from a classic `glideParty(from)`; the `render: () => window.mzPositionParty?.()` wiring made here is what repaints each glide frame."
  prohibitions:
    - "MUST NOT start a step glide anywhere in this plan (59-04 owns the trigger), and MUST NOT change `cameraPan()`, `screenToCell`'s call, `centerMap()`, `anchorCamOnParty()`, `keepPartyInView()` or the stepWith tail."
    - "MUST NOT animate any property other than opacity in the sprite's keyframes; no background-image, width, height, left or top animation."
    - "MUST NOT change the blanket reduced-motion CSS rule, `@keyframes mwglow`, `.mw-party-pulse.covered`, or renderEncounter's two `covered` class toggles."
    - "MUST NOT grow `draw()` or `paint()`: draw() loses the party block and gains nothing; paint() is untouched."
    - "MUST NOT use any art other than the shipped `icons/optimized/party.png` and `party_idle_1..4` / `party_step_1..4`."
    - "MUST NOT add a party-animation setting (D-17); reduced motion is the OS preference through Phase 58's predicate only."
    - "MUST NOT modify any file under engine/ or content/; no rng draw; no parity fixture move; MUST NOT loosen or delete a test."
---

<objective>
Put the party on the map as a living DOM sprite. It plays a composited idle loop from the shipped art, sits exactly on its canvas cell in lockstep with the camera, and is fail-safe to a static frame. The stark ring is dialled back to a faint travelling glow, and the canvas stops drawing the party.

Per D-01, D-02, D-04, D-05 and D-17 (numbered in 59-CONTEXT.md's decision-bullet order: party animation D-01..D-05, prop placement D-06..D-10, dimming D-11..D-14, settings D-15..D-17). D-01: the marker is a DOM sprite positioned by the same forward transform as the ring, and draw() stops drawing the party icon; party.png stays the fallback and first frame. D-02: idle ~280 ms a frame. D-04: drop the hard ring, keep a faint warm glow at about half strength. D-05: reduced motion freezes idle on frame 1 and the glow does not pulse. D-17: no separate animation toggle.

Purpose: ANIM-01 ("the party marker plays an idle cycle while the player is standing still") and ANIM-03 ("the dark ring around the party marker is dialled back so the marker itself reads as the highlight"). The user's words (999.1): "Dial back the black circle around the party marker — the party is already highlighted, the ring is too stark."

Output: the sprite markup and CSS, the ring's new CSS, the canvas party removal, the classic placement functions, the `__mzPartySprite` bridge with its art detection, `party-sprite-shell.test.js`, the shell sandbox's `stubDraw`/`canvasContext` options, and the re-pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-CONTEXT.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-01-SUMMARY.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-02-SUMMARY.md
@.planning/phases/58-motion-pacing/58-03-SUMMARY.md
@.planning/phases/58-motion-pacing/58-07-SUMMARY.md
</context>

<discovery>
## Read before Task 1

**Phase 58 has landed underneath you.** Find every site by function or selector name; the ~L hints below are pre-58 and have drifted. Re-verify each assumption and record the result in the SUMMARY:

1. 58-03's `positionCanvas()` writes the canvas's `transform` as `translate3d(X, Y, 0)` with `X`/`Y` = `Math.round(v * dpr) / dpr` of `tx`/`ty`, writes no `left`/`top`, and still ends with `positionPartyPulse(rect);`. `positionPartyPulse(rect)` still writes the ring's `left`/`top` (the ring keeps left/top because `mwglow` owns its `transform`).
2. `applyCam(p)` sets `cam` and calls `positionCanvas()`, so every camera-glide frame re-places whatever `positionCanvas` places.
3. The module script has `window.__mzCameraGlide = createCameraGlide({ ..., reduced: () => prefersReducedMotion(window) })`, imports `prefersReducedMotion` once from `./src/browser/motion.js`, and defines `settleAllMotion()` subscribed once through `onReducedMotionChange(window, ...)`.
4. `test/unit/reduced-motion.test.js` carries 58-07's audit section: one predicate; N controllers each built with the predicate; `settleAllMotion` settles them all; a mid-session flip with real instances; and SHA-256 digests of the comment-stripped `paint()` and `draw()` bodies pinned as literals. This plan extends the first four to the sprite and re-pins the draw() digest.
5. `loadShellSandbox({ doc, reducedMotion = true, clock = null, stubRail })` defaults to reduced motion and wires the real `__mzCameraGlide` in `wireBridges`; `test/unit/harness/fakeClock.js` exists.
6. 58-04 kept renderEncounter's two lines that add and remove the `covered` class on `#mw-party-pulse` (shell-map-hud (h) and shell-map-rail pin them), even though it routed the panel's open/close through its motion helper. If 58-04 changed those lines, keep the sprite's covered state keyed on whatever class the ring now carries and record it.
7. 58-04's CSS/JS duration no-drift test in `panel-motion.test.js`: confirm its scope is the MOTION-02 surfaces only, so the sprite's new `1.12s` cycle is not caught by it. If it scans every duration in the stylesheet, extend its allow-list with the sprite rules pinned to partySprite.js constants, and record it.

**Record the base.** `BASE_59=$(git log -1 --format=%H -- .planning/phases/59-party-animation-dungeon-set-dressing/59-CONTEXT.md)`; it must print a 40-hex hash. Before editing `draw()`, record from HEAD the comment-stripped `draw()` body (`stripHtml` from tools/ident-sweep.mjs, sliced from `function draw()` to the next top-level `\nfunction `) and its non-blank line count as PRE_DRAW_LINES.

**Why a stacked, opacity-animated frame set.** See 59-01's discovery: the 2026-09-17 PERF fix established that the map's animated DOM chrome animates only composited properties. Four idle images, one shared keyframe `mwpartyidle` (0% opacity 1, 25% opacity 0, 100% opacity 0) with `step-end` timing and the delays from `idleFrameDelaysMs()` give exactly one opaque frame at a time. Opacity with a step timing function composites in Chromium. The step frames are four more images made opaque by attribute selectors while `data-pose="step"`; 59-04 drives `data-frame` from the glide.

**Why a sibling rule for covered.** renderEncounter's two `covered` toggles on `#mw-party-pulse` are pinned byte-for-byte by shell-map-hud (h) and shell-map-rail. A general-sibling rule, `.mw-party-pulse.covered ~ .mw-party-sprite`, hides and pauses the sprite from the same class with no JS change. It requires the sprite to FOLLOW the ring in the markup.

**Why the canvas glow moves to the ring.** The canvas painted a radial glow (alpha .55 at the centre fading to 0 at half a cell) under party.png. A canvas glow is drawn at the party's real square and cannot glide with the sprite, so it would sit a square ahead of the marker during every step. Halving it and moving it onto the ring, which follows the displayed point, keeps "a faint warm glow at about half today's strength" (D-04) and keeps it with the marker. Record this reading in the SUMMARY.
</discovery>

<artifacts_this_phase_produces>
## Artifacts this plan produces

| Kind | Name | Notes |
|---|---|---|
| New element | `#mw-party-sprite` (`.mw-party-sprite`) with 8 `img.mw-party-frame` children | The DOM marker; next sibling of `#mw-party-pulse`. |
| New CSS | `.mw-party-sprite`, `.mw-party-frame` and their `data-set`/`data-k`/`data-pose`/`data-frame`/`data-art` rules, `@keyframes mwpartyidle`, the `.covered` sibling rules | Opacity-only animation. |
| Changed CSS | `.mw-party-pulse` | No dark ring; halo .25; radial background .28. |
| New classic functions | `partyShown()`, `positionPartySprite(rect)`, `positionParty()` (+ `window.mzPositionParty`) | Defined after `positionPartyPulse`. |
| Changed classic functions | `draw()` (party block removed), `positionCanvas()` (one call added), `positionPartyPulse(rect)` (reads `partyShown()`) | |
| New bridge | `window.__mzPartySprite` | A `createPartySprite` instance; BRIDGE row plus doc regen. |
| Harness options | `loadShellSandbox({ ..., stubDraw = true, canvasContext = null })` | Real draw() on a recording context. |
| New test | `test/unit/party-sprite-shell.test.js` | Markup, CSS, lockstep, draw(), module anchors. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Markup and CSS: the sprite, its composited idle loop, the step and fallback states, the covered rule, and the dialled-back ring</name>
  <files>mazeworld.html</files>
  <read_first>
    - `mazeworld.html` the viewport markup (~L1567-1574): `<div class="mw-maze-viewport" id="mw-maze-viewport">`, the canvas, `#mw-vignette`, the ring comment and `<div class="mw-party-pulse" id="mw-party-pulse" aria-hidden="true"></div>`.
    - `mazeworld.html` CSS ~L339-376: `.mw-maze-viewport`, `.mw-maze-viewport canvas` (58-03 appended `will-change:transform`), `.mw-vignette`, the Phase 35 / 2026-09-17 PERF comment, `.mw-party-pulse`, `.mw-party-pulse.covered` and `@keyframes mwglow`.
    - `mazeworld.html` the blanket `@media (prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}` rule: byte-identical in this phase.
    - The z-ladder: `.mw-rail` z 4, `.mw-hud-menu-scrim` z 5, `.mw-hud-menu-wrap` z 6, `.mw-overlay` z 8; the ring is z 2 inside the viewport.
    - `src/browser/partySprite.js` (59-01): `PARTY_FRAME_ICONS`, `IDLE_CYCLE_MS`, `IDLE_FRAME_MS`, `idleFrameDelaysMs()`.
  </read_first>
  <action>
Per D-01, D-02, D-04 and D-05. Markup and CSS only.

**Markup.** Directly after the `#mw-party-pulse` element, add `<div class="mw-party-sprite" id="mw-party-sprite" aria-hidden="true" data-art="static" data-pose="idle">` with eight children in `PARTY_FRAME_ICONS` order. Each child is an `<img class="mw-party-frame">` carrying `data-set` ("idle" for the first four, "step" for the last four), `data-k` (1..4 within its set), `src="icons/optimized/<stem>.png"`, `alt=""`, `draggable="false"` and `decoding="async"`. Extend the ring's markup comment, or add one above the sprite, naming Phase 59 (ANIM-01): the party marker is this DOM sprite, placed by positionPartySprite() from positionCanvas(); the canvas no longer paints the party; the sprite must stay AFTER the ring because the covered state is a sibling rule.

**Sprite CSS** (next to the ring rules; precede it with a comment naming Phase 59 ANIM-01/D-01/D-02/D-05 and the composited-only rule):
- `.mw-party-sprite`: absolute at left 0 and top 0, z-index 3, `pointer-events:none`, `will-change:transform`.
- `.mw-party-frame`: absolute, left 0, top 0, width and height 100%, `object-fit:contain`, `opacity:0`, `pointer-events:none`, `-webkit-user-drag:none`.
- `.mw-party-frame[data-set="idle"]`: `animation:mwpartyidle 1.12s step-end infinite`.
- `.mw-party-frame[data-set="idle"][data-k="1"]`: `opacity:1`. This is the ONLY base-opaque idle frame (the reduced-motion freeze).
- `.mw-party-frame[data-set="idle"][data-k="2"]`, `[data-k="3"]`, `[data-k="4"]`: `animation-delay` of `-.84s`, `-.56s` and `-.28s` (the seconds form of `idleFrameDelaysMs()`).
- `@keyframes mwpartyidle{0%{opacity:1}25%{opacity:0}100%{opacity:0}}`, defined exactly once.
- `.mw-party-sprite[data-pose="step"] .mw-party-frame[data-set="idle"]`: `visibility:hidden`.
- For k from 1 to 4: `.mw-party-sprite[data-pose="step"][data-frame="k"] .mw-party-frame[data-set="step"][data-k="k"]`: `opacity:1`.
- `.mw-party-sprite[data-art="static"]`: `background:url(icons/optimized/party.png) center/contain no-repeat`. `.mw-party-sprite[data-art="none"]`: a gold dot, `background:radial-gradient(closest-side,#f4dc94 72%,rgba(244,220,148,0) 74%)` (the retired canvas fallback's colour and radius). `.mw-party-sprite[data-art="static"] .mw-party-frame` and `.mw-party-sprite[data-art="none"] .mw-party-frame`: `display:none`.
- `.mw-party-pulse.covered ~ .mw-party-sprite`: `visibility:hidden`. `.mw-party-pulse.covered ~ .mw-party-sprite .mw-party-frame`: `animation-play-state:paused`.

**Ring CSS** (ANIM-03, D-04). Rewrite the `.mw-party-pulse` rule's paint declarations: remove the spread-only dark ring from its `box-shadow`, keeping only `0 0 14px 3px rgba(232,201,122,.25)`, and add `background:radial-gradient(closest-side,rgba(232,201,122,.28) 15%,rgba(232,201,122,0) 100%)`. Keep `position`, `border-radius`, `pointer-events`, `z-index:2`, `will-change:transform,opacity` and `animation:mwglow 1.6s ease-in-out infinite` exactly. Extend the Phase 35 / PERF comment above it with two sentences naming Phase 59 (ANIM-03, the user's 999.1 words): the hard ring is gone, and the glow the canvas used to paint under the party now lives here at half strength so it travels with the marker. Leave `.mw-party-pulse.covered` and `@keyframes mwglow` byte-identical.
  </action>
  <acceptance_criteria>
    - Node checks over `mazeworld.html` (style regions anchored to whole rules with `^...$` in multiline mode, markup sliced from `<div class="mw-maze-viewport" id="mw-maze-viewport">` to `<!-- DR5: the encounter/feature-event panel`):
      - the viewport slice contains exactly one `id="mw-party-sprite"`, its index is greater than the index of `id="mw-party-pulse"`, and exactly eight `<img class="mw-party-frame"` occurrences whose `src` values, in order, are `icons/optimized/<stem>.png` for each stem of `PARTY_FRAME_ICONS` (imported from src/browser/partySprite.js);
      - `^@keyframes mwpartyidle\{0%\{opacity:1\}25%\{opacity:0\}100%\{opacity:0\}\}$` matches exactly once;
      - `^\.mw-party-frame\[data-set="idle"\]\{animation:mwpartyidle 1\.12s step-end infinite\}$` matches exactly once;
      - exactly one rule whose selector starts with `.mw-party-frame[data-set="idle"][data-k=` declares `opacity:1`, and its k is 1;
      - the single `^\.mw-party-pulse\{[^}]*\}$` rule contains `box-shadow:0 0 14px 3px rgba(232,201,122,.25)`, contains `rgba(232,201,122,.28)`, contains `will-change:transform,opacity` and `animation:mwglow 1.6s ease-in-out infinite`, and does not contain `0 0 0 3px`;
      - `^\.mw-party-pulse\.covered\{animation-play-state:paused;visibility:hidden\}$` and the blanket reduced-motion rule each still match exactly once.
    - `npm run build:www` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www</automated>
  </verify>
  <done>The sprite exists with its eight shipped frames, a composited idle loop that freezes on frame 1 under reduced motion, step and fallback states, and the covered rule; the ring is a faint, pulsing warm glow with no dark edge.</done>
</task>

<task type="auto">
  <name>Task 2: JS: the canvas stops drawing the party; the sprite and ring are placed from one displayed point; the __mzPartySprite bridge with art detection and settle</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - `mazeworld.html` `draw()` (~L2088-2200): the header comment block above it, `const { g, px, py } = S.floor;`, the feature loop, and the party-marker block after it (the block that reads the party icon from the icon map, paints a radial glow, and falls back to a gold dot with a letter), then the final `positionCanvas();`.
    - `mazeworld.html` `partyCentre()`, `cameraPan()`, `positionCanvas()` and `positionPartyPulse(rect)` as landed after 58-03 (~L2001-2090).
    - `mazeworld.html` module script: the icons import line (`import { preloadIcons, featureKeyForCell, drawFeatureIcon, PLAYER_MARKER_ICON } from "./src/browser/icons.js";`), the bridge block with `window.__mzIconsApi = { ... }` and `window.__mzIconMap = await preloadIcons("./icons/optimized");` (~L5104-5110), the landed `window.__mzCameraGlide = createCameraGlide(` construction, and `settleAllMotion()`.
    - `src/browser/bridge.js`: the sorted BRIDGE object (`__mzPartySprite` sorts between `__mzPartyCap` and `__mzPendingNarration`), the `__mzIconsApi` row, and a Phase 58 row such as `__mzCameraGlide` as the row shape.
    - `tools/bridge-doc.mjs` (`--write` regenerates docs/SHELL-MODULES.md's Module bridge table).
    - `test/unit/shell-map-viewport.test.js` (m) `positionCanvasRegion()` (~L400): it slices from `function positionCanvas()` to `function positionPartyPulse(rect)`, so no new function may be declared between them.
  </read_first>
  <action>
Per D-01, D-04 and D-05.

**draw().** Delete the whole party-marker block that follows the feature loop, together with its comment. Put a two-line comment in its place naming Phase 59 (ANIM-01, D-01): the party marker is the DOM sprite `#mw-party-sprite`, placed by positionPartySprite() from positionCanvas() below; the canvas no longer paints it. Update the sentence in the comment block above `function draw()` that says the party is drawn from the PNG pipeline, so it says the marks are, and the party is the DOM sprite. Change `const { g, px, py } = S.floor;` to `const { g } = S.floor;` unless a test pins that exact line (check `test/` first; if pinned, leave it and record why). Keep the final `positionCanvas();`. Add nothing else to draw().

**partyShown().** Add a top-level `function partyShown()` directly AFTER `positionPartyPulse` (never between `positionCanvas` and `positionPartyPulse`). It returns null when `!S`; otherwise it reads `window.__mzPartySprite` at call time and returns `sp.displayed(partyCentre())` when `sp && sp.displayed`, else `partyCentre()`. Comment: the one read of where the marker is DRAWN (a gliding point during a step, the real cell otherwise); tap hit-testing keeps reading `cameraPan()`, the real party.

**positionPartySprite(rect).** Add it after `partyShown`. Return when `!S`, when `#mw-party-sprite` is missing, or when `window.__mzPartySprite` or its `box` is missing (no module means no run, so there is nothing to place). Compute `shown = partyShown()` FIRST (it may end a stale glide), then `box = sp.box({ vw: rect.width, vh: rect.height, cam, shown, cell: CELL, pad: CANVAS_PAD, dpr: window.devicePixelRatio || 1 })`. Write `el.style.width` and `el.style.height` as `CELL + "px"` and `el.style.transform` as `translate3d(${box.x}px, ${box.y}px, 0)`. Then write the pose and frame only when they changed: `el.dataset.pose` from `sp.pose()`, and `el.dataset.frame` as the string of `sp.frame()` when it is non-zero, deleting the key when it is zero. Comment: Phase 59 (D-01), the same camera and the canvas's own rounded origin, so the sprite and the canvas can never disagree.

**positionParty().** Add it after `positionPartySprite`. When `S` and the viewport exist, take `rect` from the viewport's `getBoundingClientRect()` and call `positionPartySprite(rect); positionPartyPulse(rect);`. Then `window.mzPositionParty = positionParty;`. Comment: the per-frame repaint of the marker and its glow during a step glide (the controller's `render`), without re-placing the canvas.

**positionCanvas().** Insert `positionPartySprite(rect);` immediately before its final `positionPartyPulse(rect);`, which stays its last statement. Add one comment line naming Phase 59: canvas, sprite and ring are placed in one call from one camera.

**positionPartyPulse(rect).** Replace its `const p = cameraPan();` with a read of `partyShown()` converted to the same screen offset (`(s.x - cam.x) * CELL`, `(s.y - cam.y) * CELL`). The width, height, left and top writes stay as they are. At rest `partyShown()` equals `partyCentre()`, so the ring lands exactly where it does today. Comment: Phase 59, the glow follows the displayed marker.

**Module script.** Add the import `import { createPartySprite, spriteArt, PARTY_FRAME_ICONS } from "./src/browser/partySprite.js";` on its own line with a comment naming Phase 59. Next to the landed `window.__mzCameraGlide = createCameraGlide(` construction, assign `window.__mzPartySprite = createPartySprite({ now: () => performance.now(), raf: (cb) => requestAnimationFrame(cb), cancelRaf: (id) => cancelAnimationFrame(id), reduced: () => prefersReducedMotion(window), render: () => window.mzPositionParty?.() });`, matching the camera glide's own `now`/`raf`/`cancelRaf` wrappers exactly as landed, with a comment: the marker's step glide (ANIM-02, wired in 59-04) and its canvas-lockstep placement math; the same predicate as every Phase 58 controller. Add `window.__mzPartySprite?.finish?.();` to `settleAllMotion()`'s body with a comment naming Phase 59.

Directly after the `window.__mzIconMap = await preloadIcons("./icons/optimized");` line (which stays byte-identical), add the art detection. Find `#mw-party-sprite`. Collect its `img.mw-party-frame` children. Await `Promise.all` of each image's `decode()`, with a per-image catch so a broken image resolves. Then set `sprite.dataset.art = spriteArt({ framesReady: <each image complete with a positive naturalWidth>, staticReady: <the same test on window.__mzIconMap[PLAYER_MARKER_ICON]> })`. Comment: D-01, party.png is the first frame and the fallback, and the party is never invisible.

Remove `PLAYER_MARKER_ICON` from the `window.__mzIconsApi = { ... }` literal (the classic no longer reads it; the module imports it directly for the art check), unless a test pins that literal (check `test/` first; if pinned, leave it and record why).

**bridge.js.** Add the `__mzPartySprite` row in sorted position. The owner is `mazeworld.html (module)`. The consumers are the classic `partyShown` (the displayed point) and `positionPartySprite` (box, pose, frame), plus the module `settleAllMotion` (finish); plan 59-04 adds the classic `glideParty` (stepTo) to this list when it lands. The purpose is that it bridges the pure `src/browser/partySprite.js` marker controller (Phase 59, ANIM-01/02) so the classic placement reads one lockstep box and one glide, never a second copy of the camera math. Update the `__mzIconsApi` row's consumer text: draw reads featureKeyForCell/drawFeatureIcon; the party marker moved to the DOM sprite in Phase 59. Run `node tools/bridge-doc.mjs --write` in the SAME commit.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js` exits 0.
    - Node checks over `stripHtml(mazeworld.html)`, each function sliced from `function NAME(` to the next top-level `\nfunction `:
      - the `draw` slice matches none of `/PLAYER_MARKER_ICON/`, `/createRadialGradient\(/` and `/fillText\(/`, still matches `/iconsApi\.drawFeatureIcon\(ctx, img, x \* CELL, y \* CELL, CELL, c\.dir, 0\.75\)/`, and ends with `positionCanvas();` before its closing brace; its non-blank line count is less than PRE_DRAW_LINES;
      - the `positionCanvas` slice ends with `positionPartySprite(rect);` followed by `positionPartyPulse(rect);` and the closing brace;
      - the `positionPartyPulse` slice contains `partyShown()` and no `cameraPan()`;
      - exactly one each of `function partyShown()`, `function positionPartySprite(rect)`, `function positionParty()` and `window.mzPositionParty = positionParty;`, and the index of `function positionPartyPulse(rect)` is less than the index of `function partyShown()`;
      - exactly one `window.__mzPartySprite = createPartySprite(`, whose argument slice contains `reduced: () => prefersReducedMotion(window)` and `render: () => window.mzPositionParty?.()`; the `settleAllMotion` slice contains `window.__mzPartySprite?.finish?.();`;
      - `window.__mzIconMap = await preloadIcons("./icons/optimized");` occurs exactly once and precedes the one `dataset.art = spriteArt(`.
    - `node --test test/unit/shell-map-viewport.test.js test/unit/shell-terrain-41.test.js` exits 0.
    - `npm run build:www` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bridge-registry.test.js test/unit/shell-map-viewport.test.js && npm run build:www</automated>
  </verify>
  <done>The canvas no longer draws the party; the sprite and the ring are placed from one displayed point in the same call as the canvas; the bridge is built on Phase 58's predicate, settled by settleAllMotion, and documented; the art falls back safely.</done>
</task>

<task type="auto">
  <name>Task 3: party-sprite-shell.test.js, the sandbox's real-draw option, the audit extension and the exact re-pins</name>
  <files>test/unit/harness/shellSandbox.js, test/unit/party-sprite-shell.test.js, test/unit/shell-map-hud.test.js, test/unit/reduced-motion.test.js</files>
  <read_first>
    - `test/unit/harness/shellSandbox.js` as landed after Phase 58: `loadShellSandbox`'s options, the `draw`/`renderRail` stubs, `wireBridges` (and how 58-03 wires the real `__mzCameraGlide` with the clock), and the "never stub a bridge with a dummy" rule.
    - `test/unit/harness/recordingCanvas.js` (59-02) and `test/unit/harness/recordingDom.js#getElementById` (it memoises by id, so a test can pre-register `maze` with a custom `getContext`).
    - `test/unit/map-pan.test.js` (58-03): how it overrides the viewport's `getBoundingClientRect`, sets `devicePixelRatio`, reads `cam` and parses the canvas `translate3d`.
    - `test/unit/shell-map-hud.test.js` (h) (~L402-420) and (i) (~L424-462): the ring's exact CSS pin and the draw() needle and absence lists.
    - `test/unit/reduced-motion.test.js`: 58-07's audit section (the controller list, the settle-all body check, the mid-session flip, the paint/draw digests and how 58-07 computed them).
  </read_first>
  <action>
**shellSandbox.js.** Add two options to `loadShellSandbox`: `stubDraw = true` and `canvasContext = null`. When `canvasContext` is given, before running the classic script, set `doc.document.getElementById("maze").getContext` to return it. When `stubDraw` is false, do not stub `draw`, and wire what draw() reads: `__mzIconsApi` from the real `featureKeyForCell`/`drawFeatureIcon`, `__mzIconMap` with one fake decoded image per `FEATURE_ICONS` name (`{ complete: true, naturalWidth: 144, key: name }`, the harness's stand-in for a browser Image, like the fake canvas), and `__mzMapMarks` from the real `src/browser/mapMarks.js` exports. In `wireBridges`, wire `__mzPartySprite` from the REAL `createPartySprite`, using the clock's (or the inert default) scheduler, `reduced: () => prefersReducedMotion(context.window)` and `render: () => context.window.mzPositionParty?.()`. Comment each addition with Phase 59. Every existing default stays exactly as it is.

**party-sprite-shell.test.js.** Create it with named tests, one per claim:
1. Markup: the sprite follows the ring inside the viewport, is aria-hidden, starts at `data-art="static"` and `data-pose="idle"`, and holds the 8 frames in `PARTY_FRAME_ICONS` order with `icons/optimized/<stem>.png` sources, `alt=""` and `draggable="false"`.
2. CSS/JS no-drift: the idle rule's duration equals `IDLE_CYCLE_MS / 1000` seconds, and the k = 2..4 delays equal `idleFrameDelaysMs()` in seconds, parsed from the stylesheet.
3. Composited only: `@keyframes mwpartyidle` occurs once and declares only `opacity`; no sprite or frame rule declares a transition.
4. Reduced-motion freeze (ANIM-01, D-05): the blanket rule is byte-identical and present once, and exactly one idle frame (k = 1) has a base `opacity:1`, so with animations removed only frame 1 shows.
5. Step states: for each k from 1 to 4 exactly one rule makes step frame k opaque under `data-pose="step"` with `data-frame="k"`, and idle frames are hidden in the step pose.
6. Fallback art: `data-art="static"` shows `icons/optimized/party.png` and hides the frames; `data-art="none"` shows the gold dot and hides the frames.
7. Covered: the `.mw-party-pulse.covered ~ .mw-party-sprite` rules hide the sprite and pause its frames, and the sprite's source index is after the ring's.
8. Pointer and z safety: `.mw-party-sprite` declares `pointer-events:none` and `z-index:3`, and the ring stays at z 2 and the rail at z 4.
9. The ring (ANIM-03): no spread-only dark ring, a .25 halo, a .28 radial background, and `mwglow` and `will-change` unchanged.
10. draw() paints no party (behaviour): with `stubDraw: false`, a recording context from `createRecordingContext()` and a real `newRun` state, `draw()` records no drawImage of the fake party image, no `arc`, no `fillText` and no `createRadialGradient`. Every feature on a seen, visible cell is still drawn at `Math.round(CELL * 0.75)`.
11. Lockstep at rest (D-01): with the viewport rect overridden to 400x600 and a fractional `cam`, at `devicePixelRatio` 1 and at 2.625 (the Pixel 7), after `positionCanvas()` the sprite's `translate3d` x and y equal the canvas's `translate3d` x and y plus `px * CELL` and `py * CELL` within 1e-9, its width and height are `CELL` px, and its `data-pose` is "idle". The ring's `left`/`top` equal the pre-phase values `rect.width / 2 + cameraPan().x - CELL / 2` (and likewise y).
12. Module anchors: the bridge is built with the shared predicate and the `mzPositionParty` render, `settleAllMotion` finishes it, and the art detection follows the preload line.

**Re-pins, exact and never loosened.**
- `shell-map-hud.test.js` (h): re-pin the `.mw-party-pulse` regex to the new declaration list (the .25 halo and the .28 radial background, with `will-change:transform,opacity` and `animation:mwglow 1.6s ease-in-out infinite` still asserted). Keep the keyframes and blanket-rule assertions. Add a comment naming Phase 59 (ANIM-03, the user's 999.1 words).
- `shell-map-hud.test.js` (i): move the four party-marker needles (the `PLAYER_MARKER_ICON` read, the party `drawFeatureIcon` call, `createRadialGradient(` and `P.party`) from the must-include list to the must-not-include list, with a comment naming Phase 59 (ANIM-01, D-01: the marker is a DOM sprite). Keep every other needle and the `positionCanvas();`-last assertion.
- `reduced-motion.test.js` audit: add `createPartySprite(` to the list of controllers that must be built with `reduced: () => prefersReducedMotion(window)`; add `window.__mzPartySprite?.finish?.()` to the settle-all body check; add a real `createPartySprite` on the shared fake clock and flag to the mid-session flip test (start a `stepTo`, flip, settle, then assert the marker is idle at its target and nothing is pending). Re-pin the `draw()` digest: compute the SHA-256 of the new comment-stripped draw() body with the same one-off method 58-07 used, replace the literal, and extend its comment with a sentence naming Phase 59 plan 03 (the canvas party marker was removed; nothing was added). Leave the `paint()` digest byte-identical: paint() is untouched.
- Fix any other assertion the full suite shows was pinned to a line Task 2 changed, the same way (re-pin to the exact new source, with a Phase 59 comment). For every touched test file, the count of `^test(` lines must not drop against BASE_59.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/party-sprite-shell.test.js test/unit/shell-map-hud.test.js test/unit/reduced-motion.test.js test/unit/map-pan.test.js test/unit/shell-map-viewport.test.js test/unit/shell-map-rail.test.js test/unit/shell-tab-snapshots.test.js` exits 0.
    - For each re-pinned file, `git show "$BASE_59":<file> | grep -c '^test('` is less than or equal to `grep -c '^test(' <file>`.
    - Teeth check, run AFTER this task's commit and recorded in the SUMMARY: temporarily make `positionPartySprite` use the UNROUNDED origin (pass `dpr: 1` to `box`), confirm lockstep test 11 FAILS at dpr 2.625, then revert with `git checkout -- mazeworld.html`.
    - Plan gate: `npm test` reports fail 0; `npm run build:www` exits 0; `git diff --stat "$BASE_59"..HEAD -- engine/ content/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `node tools/shell-sweep.mjs orphans` does not list `partyShown`, `positionPartySprite` or `positionParty`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The sprite's markup, composited idle loop, reduced freeze, fallbacks, covered state, lockstep placement and the canvas's silence are proven; the ring's dial-back is pinned; Phase 58's audit covers the new controller; every moved pin is re-pinned exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → map | A new element now sits over the canvas; it must never intercept a tap or hold. |
| boot image decode → marker visibility | A missing or broken frame must never leave the party invisible. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-08 | Denial of Service | The sprite swallowing tap-to-move or hold-inspect | high | mitigate | `pointer-events:none` on the sprite and frames (test 8); hit-testing still reads `cameraPan()`, untouched. |
| T-59-09 | Denial of Service | The party invisible because a frame failed to load | high | mitigate | Markup starts at `data-art="static"` (party.png), and the module downgrades to `static` or `none` (a gold dot) from the decode results (tests 6, 12). |
| T-59-10 | Denial of Service (PERF) | A non-composited idle animation re-rasterising the map on the Pixel 7 | medium | mitigate | The keyframe animates opacity only (test 3); the covered rule pauses it under the encounter panel (test 7). Phase 60 measures step time. |
| T-59-11 | Tampering (input integrity) | The marker drawn off its real cell, so a tap looks misaligned | medium | mitigate | One placement path from the canvas's own rounded origin; lockstep test 11 at dpr 1 and 2.625. |
| T-59-SC | Tampering | npm installs | high | mitigate | No package install. |
</threat_model>

<verification>
- `npm test` reports fail 0; `npm run build:www` exits 0.
- `node --test test/unit/bridge-registry.test.js` passes set-equality and doc-sync.
- `git diff --stat "$BASE_59"..HEAD -- engine/ content/ test/parity/` is empty.
</verification>

<success_criteria>
- The party is a DOM sprite with a composited ~1.1 s idle loop from the shipped art, frozen on frame 1 under reduced motion.
- The canvas no longer draws the party; the sprite sits exactly on its cell in lockstep with the canvas at every dpr.
- The dark ring is gone; a faint warm glow at half strength remains and follows the marker.
- The marker never intercepts input, hides under the encounter panel, and is never invisible.
</success_criteria>

<output>
Create `.planning/phases/59-party-animation-dungeon-set-dressing/59-03-SUMMARY.md` when done.
</output>
