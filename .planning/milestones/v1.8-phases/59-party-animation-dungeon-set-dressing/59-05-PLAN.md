---
phase: 59-party-animation-dungeon-set-dressing
plan: 05
type: execute
wave: 4
depends_on: ["59-02", "59-04"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/dressing-shell.test.js
  - test/unit/phase59-gates.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/reduced-motion.test.js
autonomous: true
requirements: [DRESS-01, DRESS-02, DRESS-03, DRESS-04, DRESS-05]

must_haves:
  truths:
    - "Floors carry scattered ambient props from the 54 `set_dungeon_*` images (DRESS-01): `draw()` gains exactly ONE statement, `window.__mzDressing?.drawLayer?.(ctx, S, CELL, visible);`, placed after the maze border and before the feature icons, so every prop is drawn BENEATH the features (D-11), and the party sprite is a DOM layer above the canvas."
    - "A prop on a walkable square is drawn at 35% opacity and 60% of a square, a wall prop at 85%, and a feature icon keeps full opacity at 75%, so a prop is never mistaken for an encounter (DRESS-02, D-11..D-13). The canvas alpha is restored before the features draw."
    - "No prop is ever drawn on a square holding a feature, the stairs or the party, nor on the party's floor-start square, an unseen square, or a square outside the render window the map itself shows (DRESS-03, D-10). The window is draw()'s own `visible` predicate (window.__mzMapView.inViewWindow, which 57-04's vignette explains through __mzDarkness); nothing recomputes a darkness radius."
    - "The same seed and depth always dress a floor identically, on every repaint and after a save/reload, and dressing never changes a run: the serialized GameState is byte-identical before and after drawing, and the zero-fixture-move gate holds (DRESS-04)."
    - "A 'Set dressing: On / Off' row sits in the Settings sheet after Haptics, default On, persisted in `ddr.settings.v1` beside Sound and independent of it (DRESS-05, D-15). Flipping it redraws the map at once. Off draws nothing, and while it is Off the 54 images are never requested (D-16)."
    - "The 54 images load lazily (D-14): nothing requests them until one frame after the first map paint (`requestAnimationFrame`, then `setTimeout`, after `__mzClassicBoot`), and only while the setting is On. When they arrive the map redraws once. Cold start therefore does not grow; Phase 60 measures it against `docs/PERF-BASELINE.md`."
    - "`window.__mzDressing` is the one new bridge name, built from `createDressingBridge({ art: dressingArt, drawIcon: drawFeatureIcon })`. Its BRIDGE row and `node tools/bridge-doc.mjs --write` land in the same commit."
    - "Phase gate for Phase 59: `npm test` fail 0; `npm run build:www` exit 0; the engine/content/parity diff against BASE_59 is empty; the prototype master hash is `a1f4d0dc…`; `package.json`/`package-lock.json` are unchanged; `bridge-registry.test.js` is green; `paint()` is untouched and `draw()` only lost the party block and gained the one dressing call."
    - statement: "On the Pixel 7, each floor shows a handful of dim props on paths and brighter ones on walls; none is ever mistaken for an encounter, a chest or a crevice; no prop covers the stairs, a feature or the party; props only appear where the map is revealed and inside the dark window."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "Settings → Set dressing Off clears every prop at once and On brings them back; the Sound setting is unaffected either way."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
    - statement: "Cold start with Set dressing On shows the map as fast as before, with the props appearing a moment after it; closing and reopening the app mid-floor shows the props in the same places."
      verification: "deferred device check — Phase 60 batched Pixel 7 session"
  artifacts:
    - test/unit/dressing-shell.test.js
    - test/unit/phase59-gates.test.js
  key_links:
    - "`const dressingArt = createDressingArt({ load: () => loadIconSet(\"./icons/optimized\", DRESSING_ICON_NAMES), onReady: () => window.draw?.() })` is declared BEFORE the module's first `applySettings(await readSettings())`, because `applySettings` calls `dressingArt.setEnabled(...)` and a later `const` would throw in the temporal dead zone."
    - "`loadIconSet(` appears in the module script only inside that `load` callback, so no path can load the dressing art eagerly."
    - "The settings sheet's delegated click handler already writes any `data-setting` row through `writeSetting` and `applySettings`; the new row needs only its markup plus one `if (key === \"dressing\") window.draw?.();` redraw line."
  prohibitions:
    - "MUST NOT add any body to `draw()` or `paint()` beyond the one dressing call; opacity, scale, exclusions and placement stay in src/browser/dressing.js."
    - "MUST NOT make props tappable, inspectable (hold-inspect) or rule-bearing, and MUST NOT mention them in the Oracle, the rail or any announcer."
    - "MUST NOT load the dressing images before the first map paint, or at all while the setting is Off."
    - "MUST NOT change the Sound, Haptics, Text size or Confirm rows, their persistence, or Phase 56's Sound gate."
    - "MUST NOT add a party-animation setting (D-17)."
    - "MUST NOT modify any file under engine/ or content/; no engine rng draw; no parity fixture move; MUST NOT loosen or delete a test; MUST NOT edit REQUIREMENTS.md, ROADMAP.md or STATE.md (the orchestrator owns them)."
---

<objective>
Wire the set dressing into the map and Settings: one prop-layer call in draw(), beneath the feature icons; the lazy, Off-aware image load after the first map paint; the Set dressing On/Off row. Then close the phase: the modularity proof, the full gate, and the Phase 60 batched device checklist.

Per D-10, D-11, D-14, D-15, D-16 and D-17 (numbered in 59-CONTEXT.md's decision-bullet order: party animation D-01..D-05, prop placement D-06..D-10, dimming D-11..D-14, settings D-15..D-17). D-10: never on a feature, the stairs, the party or its arrival square, and respecting fog and the render window. D-11: floor props dimmed, drawn beneath the feature and party layers. D-14: lazy-load the images after the first map paint. D-15: a Set dressing On/Off row, default On, persisted with Sound and independent of it. D-16: Off draws nothing and never loads. D-17: no separate party-animation toggle. D-06..D-09, D-12 and D-13 are implemented by 59-02's module; this plan wires them in.

Purpose: DRESS-01..05. The user's words (999.3): props "on walls, or on paths provided they are DIMMED so they are never confused with the real encounter icons. Set dressing only: not interactable, no rules effect, pure ambiance."

Output: the draw() call, the module wiring and `__mzDressing` bridge, the Settings row, the sandbox's dressing option, `dressing-shell.test.js`, `phase59-gates.test.js`, the re-pins, and the phase gate plus Phase 60 checklist in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-CONTEXT.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-02-SUMMARY.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-03-SUMMARY.md
@.planning/phases/59-party-animation-dungeon-set-dressing/59-04-SUMMARY.md
@.planning/phases/57-map-hud-layout-band/57-04-SUMMARY.md
</context>

<discovery>
## Read before Task 1

**Re-read the landed code.** Find every site by name; the ~L hints are pre-58. Re-verify and record in the SUMMARY:

1. `draw()` as left by 59-03: the `visible` predicate definition (pinned exactly once by shell-terrain-41), the cell loop, `ctx.strokeRect(3, 3, size - 6, size - 6);`, the feature loop with `iconsApi.drawFeatureIcon(ctx, img, x * CELL, y * CELL, CELL, c.dir, 0.75)`, and the final `positionCanvas();`.
2. The module script's settings block: `let currentSettings = null;`, `applySettings(settings)` (Phase 56 calls `applySfxSettings(settings)` there), `renderSettingsSheet()`, the landed `openSettingsSheet`/`closeSettingsSheet` (58-04 routes them through its motion helper), the delegated `#mw-settings-rows` click handler, and the first `applySettings(await readSettings())` call.
3. The icons bridge block (`window.__mzIconsApi = ...`, `window.__mzIconMap = await preloadIcons("./icons/optimized");`, 59-03's art detection after it) and `await window.__mzClassicBoot();`.
4. 59-02's exports landed as planned: `createDressingArt`, `createDressingBridge`, `DRESSING_ICON_NAMES` in dressing.js; `loadIconSet` in icons.js; `dressing` in `SETTINGS_DEFAULTS`.
5. `test/unit/reduced-motion.test.js`'s audit section: the `draw()` digest literal as re-pinned by 59-03.

Record BASE_59 = `git log -1 --format=%H -- .planning/phases/59-party-animation-dungeon-set-dressing/59-CONTEXT.md` (it must print a 40-hex hash). Also record PRE59, the commit before Phase 59's first code: `git rev-parse "$(git log --reverse --format=%H "$BASE_59"..HEAD -- src/browser/partySprite.js src/browser/dressing.js | head -1)^"`. PRE59's comment-stripped `draw()` body is the "before Phase 59" reference for the modularity proof.

**Why the window predicate, not __mzDarkness.** 57-04's `__mzDarkness` exposes `mapViewRadius` for the vignette. The render window itself (which cells draw() paints) is the Phase 41 `inViewWindow` predicate on `__mzMapView`, already wrapped as draw()'s local `visible`. Passing that same `visible` into the layer guarantees a prop is drawn only where its cell is drawn, with zero recomputation. Record this reading of CONTEXT's "respect the 57-04 darkness render window" in the SUMMARY.

**Why redraw on the settings flip.** The layer reads `dressingArt.enabled()` on every draw, so the flip takes effect on the next repaint; the handler forces that repaint immediately. Turning the setting On after boot also starts the lazy load (the controller was released after the first paint), and its `onReady` redraws once the images decode.
</discovery>

<tasks>

<task type="auto">
  <name>Task 1: The draw() prop-layer call, the lazy Off-aware art, the __mzDressing bridge and the Set dressing row</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - `mazeworld.html` `draw()` in full (as left by 59-03).
    - `mazeworld.html` Settings sheet markup (~L1386-1440): the Haptics row (`data-setting="haptics"`), the version row `#mw-settings-version-row`, and the sheet's header comment.
    - `mazeworld.html` module script: the icons import line, the settings block (`let currentSettings`, `applySettings`, `renderSettingsSheet`, the delegated rows handler), the icon bridge block, and `await window.__mzClassicBoot();`.
    - `src/browser/dressing.js` and `src/browser/icons.js#loadIconSet` (59-02).
    - `src/browser/bridge.js`: `__mzDressing` sorts between `__mzDescend` and `__mzDropShelfItems`; use the `__mzDarkness` row as the row shape.
    - `test/unit/shell-no-content-copies.test.js` criterion 3: `dressingArt` must not collide with any src/browser export (it does not).
  </read_first>
  <action>
Per D-10, D-11, D-14, D-15 and D-16.

**draw().** Directly after `ctx.strokeRect(3, 3, size - 6, size - 6);` and before the comment that opens the feature-icon block, add exactly one statement, `window.__mzDressing?.drawLayer?.(ctx, S, CELL, visible);`, preceded by a short comment naming Phase 59 (DRESS-01..05): ambient props beneath the feature icons; placement, dimming, the feature/stairs/party/fog/window exclusions and the Off rule all live in src/browser/dressing.js; `visible` is passed so a prop is drawn only where its cell is. Add nothing else.

**Settings markup** (D-15). After the Haptics row and before the version row, add a row identical in shape to Haptics: label "Set dressing", options container `data-setting="dressing"`, two `mw-settings-opt` buttons with `data-value` "true" (On) and "false" (Off). Add an HTML comment naming Phase 59 (DRESS-05): ambient props on/off, default On, independent of Sound; Off also stops the prop images from loading.

**Module script.**
- Add two import lines, each with a Phase 59 comment: `import { createDressingArt, createDressingBridge, DRESSING_ICON_NAMES } from "./src/browser/dressing.js";` and `import { loadIconSet } from "./src/browser/icons.js";`. The second is a separate line so the existing icons import line stays byte-identical.
- Immediately before `let currentSettings = null;`, declare `const dressingArt = createDressingArt({ load: () => loadIconSet("./icons/optimized", DRESSING_ICON_NAMES), onReady: () => window.draw?.() });` with a comment: D-14/D-16, the 54 prop images load once, only after the first map paint and only while Set dressing is On; declared before the first applySettings call, which reads it.
- In `applySettings(settings)`, after the Phase 56 `applySfxSettings(settings);` line, add `dressingArt.setEnabled(settings.dressing === true);` with a one-line Phase 59 comment.
- In the delegated `#mw-settings-rows` click handler, after `renderSettingsSheet();`, add `if (key === "dressing") window.draw?.();` with a comment: repaint at once so Off clears every prop and On shows them (once loaded).
- Next to `window.__mzIconsApi = ...`, assign `window.__mzDressing = createDressingBridge({ art: dressingArt, drawIcon: drawFeatureIcon });` with a comment naming Phase 59: the prop layer draw() calls; it draws nothing while Off or before the images arrive.
- Directly after `await window.__mzClassicBoot();`, add `requestAnimationFrame(() => setTimeout(() => dressingArt.release(), 0));` with a comment: D-14, release the lazy load one presented frame after the first map paint, so cold start never waits on the 54 images; the controller still loads nothing while Off.

**bridge.js.** Add the `__mzDressing` row in sorted position. The owner is `mazeworld.html (module)`. The consumer is the classic `draw` (the ambient prop layer beneath the feature icons). The purpose is that it bridges the pure `src/browser/dressing.js` layer (Phase 59, DRESS-01..05) so draw() places, dims and excludes props with one thin call and no second copy of the rules; it draws nothing while Set dressing is Off. Run `node tools/bridge-doc.mjs --write` in the SAME commit.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/shell-terrain-41.test.js test/unit/settings.test.js` exits 0.
    - Node checks over `stripHtml(mazeworld.html)`:
      - the `draw` slice (from `function draw()` to the next top-level `\nfunction `) contains exactly one `window.__mzDressing?.drawLayer?.(ctx, S, CELL, visible);`, whose index is greater than that of `ctx.strokeRect(3, 3, size - 6, size - 6)` and less than that of `iconsApi.featureKeyForCell(c)`, and the slice matches no `/globalAlpha/`;
      - `const dressingArt = createDressingArt(` occurs exactly once and its index is less than the index of the first `applySettings(await readSettings())`;
      - `loadIconSet(` occurs exactly once in the module script, inside the `createDressingArt(` argument slice;
      - `dressingArt.setEnabled(settings.dressing === true);` occurs once, inside the `applySettings` slice; `if (key === "dressing") window.draw?.();` occurs once;
      - `window.__mzDressing = createDressingBridge({ art: dressingArt, drawIcon: drawFeatureIcon });` occurs once;
      - `dressingArt.release()` occurs exactly once, inside `requestAnimationFrame(() => setTimeout(`, and after `await window.__mzClassicBoot();`;
      - the markup has exactly one `data-setting="dressing"`, after `data-setting="haptics"` and before `id="mw-settings-version-row"`.
    - `npm run build:www` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bridge-registry.test.js test/unit/shell-terrain-41.test.js && npm run build:www</automated>
  </verify>
  <done>draw() gains one prop-layer call beneath the features; the art loads lazily and never while Off; the Settings sheet has a Set dressing row that repaints at once; the bridge is registered and documented.</done>
</task>

<task type="auto">
  <name>Task 2: dressing-shell.test.js on the real draw(), the sandbox's dressing option and the re-pins</name>
  <files>test/unit/harness/shellSandbox.js, test/unit/dressing-shell.test.js, test/unit/shell-map-hud.test.js, test/unit/reduced-motion.test.js</files>
  <read_first>
    - `test/unit/harness/shellSandbox.js` (59-03's `stubDraw`/`canvasContext` options and the icon bridges it wires for a real draw()).
    - `test/unit/party-sprite-shell.test.js` test 10 (59-03): the real-draw() setup to reuse.
    - `test/unit/harness/recordingCanvas.js` (59-02): `createRecordingContext`, `imageDraws()`.
    - `test/unit/darkness-vignette.test.js` and `test/unit/darkness-filter.test.js` (57-04): how a test puts the party on a dark square without a waiver, so the render window collapses to 3x3.
    - `engine/saveState.js#serializeRun`, `validateSave`, `rehydrate` (read only).
    - `test/unit/shell-map-hud.test.js` (i) and `test/unit/reduced-motion.test.js`'s draw() digest.
  </read_first>
  <action>
**shellSandbox.js.** Add a `dressing = null` option to `loadShellSandbox`. When given `{ enabled, images }`, wire `__mzDressing` from the REAL `createDressingBridge` with `art: { enabled: () => enabled, images: () => images }` and the real `drawFeatureIcon`. When not given, wire the real bridge with an enabled art whose images are null: the boot state before the lazy load, which draws nothing. Comment: Phase 59; the art source is injected like the clock, and the bridge and its rules are the real module.

**dressing-shell.test.js.** Create it with named tests, one per claim, on the real `draw()` (`stubDraw: false`, a `createRecordingContext()` canvas, fake decoded images for all 54 `DRESSING_ICON_NAMES`, and a real `newRun` state cloned with every cell `seen` unless a test says otherwise):
1. Props draw beneath the features (D-11): every prop drawImage precedes the first feature drawImage in the recorded call order.
2. Dimming and size (DRESS-02): every floor-prop draw records alpha 0.35 and every wall-prop draw 0.85, all at `Math.round(CELL * 0.6)`; every feature draw records alpha 1 at `Math.round(CELL * 0.75)`.
3. Exclusions (DRESS-03): with `propsFor(S)` known, no prop draw lands on a cell whose `feat` is set (the stairs included), on the party's square, or on (1,1). For the resolved-feature case, search seeds once with a one-off script for a `newRun` seed whose placement puts a floor prop on a feature cell, pin that seed as a literal with a comment, and assert the prop is not drawn while the feature stands and IS drawn once that cell's `feat` is cleared on a clone.
4. Fog: on a clone where only part of the grid is `seen`, no prop draws on an unseen cell.
5. The render window: with the party on a dark square and no waiver (the 57-04 technique), no prop draws outside the 3x3 window and props inside it still draw.
6. Off (DRESS-05, D-16): with `dressing: { enabled: false, images }`, zero prop draws, and the feature draw list equals the enabled run's feature draws exactly.
7. Before the lazy load (D-14): the default sandbox (images null) records zero prop draws and throws nothing.
8. Determinism (DRESS-04): two `draw()` calls record identical call lists, and a state round-tripped through serializeRun, validateSave and rehydrate draws its props at identical positions.
9. The engine is untouched: `JSON.stringify(S)` before and after `draw()` is identical.
10. Settings wiring (source): the Set dressing row's shape and position, the handler redraw, `setEnabled` in `applySettings`, and the TDZ order (`dressingArt` declared before the first `applySettings(await readSettings())`).
11. Lazy load (source): `loadIconSet(` appears in the module script only inside the `createDressingArt(` argument slice, and `dressingArt.release()` is scheduled once, inside `requestAnimationFrame(() => setTimeout(`, after `await window.__mzClassicBoot();`.

**Re-pins, exact and never loosened.**
- `shell-map-hud.test.js` (i): add `window.__mzDressing?.drawLayer?.(ctx, S, CELL, visible)` to the draw() must-include needles, with a comment naming Phase 59 (DRESS-01). Keep `globalAlpha` in the must-not-include list: the alpha lives in dressing.js, never in draw().
- `reduced-motion.test.js` audit: re-pin the `draw()` SHA-256 digest to the new comment-stripped body (same one-off method) and extend its comment with a sentence naming Phase 59 plan 05 (exactly one statement added: the dressing layer call). Leave the `paint()` digest byte-identical.
- For each touched test file, the count of `^test(` lines must not drop against BASE_59.
  </action>
  <acceptance_criteria>
    - `node --test test/unit/dressing-shell.test.js test/unit/shell-map-hud.test.js test/unit/reduced-motion.test.js test/unit/party-sprite-shell.test.js test/unit/shell-tab-snapshots.test.js` exits 0.
    - For each re-pinned file, `git show "$BASE_59":<file> | grep -c '^test('` is less than or equal to `grep -c '^test(' <file>`.
    - Teeth check, run AFTER this task's commit and recorded in the SUMMARY: temporarily move the `window.__mzDressing?.drawLayer?.(...)` statement to after the feature loop, confirm dressing-shell test 1 FAILS, then revert with `git checkout -- mazeworld.html`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/dressing-shell.test.js test/unit/reduced-motion.test.js</automated>
  </verify>
  <done>On the real draw(): props sit beneath features, dimmed and small, never on a feature, stairs, party, start, unseen or out-of-window square; Off and pre-load draw nothing; the dressing is identical across repaints and reloads and never touches the state.</done>
</task>

<task type="auto">
  <name>Task 3: The Phase 59 gates test, the full phase gate and the Phase 60 device checklist</name>
  <files>test/unit/phase59-gates.test.js</files>
  <read_first>
    - `test/unit/reduced-motion.test.js` audit section (58-07, extended by 59-03/59-05): what it already proves, so this file does not duplicate it.
    - `src/browser/bridge.js` (the `__mzPartySprite` and `__mzDressing` rows) and `tools/shell-sweep.mjs` (`orphans`).
    - The 59-03 and 59-04 plans' deferred device checks (their must_haves `statement` entries).
  </read_first>
  <action>
Create `test/unit/phase59-gates.test.js` with a head comment: Phase 59's closing gates (Modularity and Presentation), durable so a later change cannot quietly undo them. Named tests, one per claim:
1. draw() only shrank and gained one call: its comment-stripped body contains exactly one statement referencing `__mzDressing`, no read of the party marker art, no radial gradient and no text drawing, and its non-blank line count is less than PRE59's. Before writing this test, compute PRE59's count from `git show "$PRE59":mazeworld.html` with a one-off node script and pin it as a literal, with a comment naming PRE59's hash and the command used, so `npm test` never shells out to git.
2. The two Phase 59 modules are pure: `stripJs(src/browser/partySprite.js)` imports only `./cameraGlide.js`, `stripJs(src/browser/dressing.js)` imports only `../../engine/rng.js`, and neither matches `/\bwindow\.|\bdocument\.|Math\.random|Date\.now|matchMedia/`.
3. The two Phase 59 bridges are registered and assigned once: `__mzPartySprite` and `__mzDressing` are BRIDGE keys, and each `window.<name> = ` assignment occurs exactly once in the stripped module script.
4. Nothing in engine/ or content/ imports from src/browser/ (the dependency direction the Presentation gate relies on).

Then run the phase gate and record every result in the SUMMARY:
- `npm test` reports fail 0.
- `npm run build:www` exits 0.
- `git diff --stat "$BASE_59"..HEAD -- engine/ content/ test/parity/` prints nothing.
- `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
- `git diff "$BASE_59"..HEAD -- package.json package-lock.json` prints nothing (offline gate: no new dependency).
- `node --test test/unit/bridge-registry.test.js` is green on both halves.
- `node tools/shell-sweep.mjs orphans` lists none of `partyShown`, `positionPartySprite`, `positionParty` and `glideParty`.
- The four ANIM/DRESS requirement groups map to their proving tests: ANIM-01 (party-sprite-shell 1-8, party-sprite 1), ANIM-02 (party-glide 1-7, party-sprite 2), ANIM-03 (party-sprite-shell 9), DRESS-01 (dressing 1, dressing-shell 1), DRESS-02 (dressing-shell 2), DRESS-03 (dressing-shell 3-5), DRESS-04 (dressing-determinism, dressing-shell 8-9), DRESS-05 (settings, dressing-shell 6, 10). Write this table into the SUMMARY.

Finally, write into the SUMMARY a "Phase 60 batched Pixel 7 checklist" that collects, verbatim, every deferred device check from plans 59-03, 59-04 and this plan (the orchestrator lifts it into VERIFICATION.md's `human_verification`). Add: a TalkBack item (the marker and the props add nothing to what TalkBack announces; the map still reads as before); a Remove-animations item (idle frozen on frame 1, steps land instantly, the glow is steady); and a cold-start item for Phase 60's PERF-03 measurement (Set dressing On vs Off, against `docs/PERF-BASELINE.md`; the 8 party frames of ~142 KB load eagerly, the ~980 KB of props lazily).
  </action>
  <acceptance_criteria>
    - `node --test test/unit/phase59-gates.test.js` exits 0.
    - Teeth check, run AFTER this task's commit and recorded in the SUMMARY: temporarily add a second prop-layer call to draw() (a copy of the first), confirm phase59-gates test 1 FAILS, then revert with `git checkout -- mazeworld.html`.
    - Phase gate: every command listed in the action passes as stated, and the SUMMARY records each output.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Phase 59's modularity and presentation gates are pinned durably, the full gate is green and recorded, and the Phase 60 device checklist is written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted settings → the prop layer | The Set dressing value gates both drawing and loading. |
| engine state → draw() | The prop layer reads the live GameState on every repaint and must never write it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-59-15 | Tampering | draw() mutating the run through the prop layer | critical | mitigate | Test 9: the serialized state is identical before and after draw(); the determinism ledger (59-02) and the zero-fixture gate back it. |
| T-59-16 | Spoofing (legibility) | A prop read as an encounter or covering the stairs | high | mitigate | Tests 1-3: beneath the features, 0.35 alpha and 0.6 scale, never on a feature, stairs or party square; the device read is a Phase 60 check. |
| T-59-17 | Denial of Service (PERF) | 54 images slowing cold start | medium | mitigate | Released one presented frame after the first map paint, never while Off (test 11); Phase 60 measures it. |
| T-59-18 | Information Disclosure (accessibility) | Props adding noise for TalkBack users | low | accept | Props are canvas pixels with no DOM or announcer text; the checklist's TalkBack item confirms it on device. |
| T-59-SC | Tampering | npm installs | high | mitigate | `package.json`/`package-lock.json` diff against BASE_59 is empty (gate). |
</threat_model>

<verification>
- `npm test` reports fail 0; `npm run build:www` exits 0.
- `git diff --stat "$BASE_59"..HEAD -- engine/ content/ test/parity/` is empty; the prototype master hash is unchanged; `package.json`/`package-lock.json` are unchanged.
- `bridge-registry.test.js` is green.
</verification>

<success_criteria>
- Floors carry deterministic, dimmed, non-interactive props drawn beneath the features, never on a feature, stairs or party square.
- Set dressing On/Off works live, is independent of Sound, and Off neither draws nor loads.
- The prop images load after the first map paint.
- The phase closes with draw() smaller than before plus one call, paint() untouched, the engine untouched, and the Phase 60 device checklist recorded.
</success_criteria>

<output>
Create `.planning/phases/59-party-animation-dungeon-set-dressing/59-05-SUMMARY.md` when done.
</output>
