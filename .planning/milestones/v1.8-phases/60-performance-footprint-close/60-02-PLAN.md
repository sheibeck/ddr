---
phase: 60-performance-footprint-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/PERF-BASELINE.md
  - docs/UAT-v1.8.md
autonomous: true
requirements: [PERF-03]

must_haves:
  truths:
    - "Two debug APKs and two signed release AABs are built outside the repo tree, under `C:/projects/mazeworld-perf60/`. One pair comes from the `v1.7` tag (`6c299ab`) and one from this plan's starting HEAD. Each pair is built in its own temporary detached `git worktree`, created with `core.autocrlf=false`, by the same npm scripts (`android:debug`, then `android:release`) under the pinned JDK. This is CONTEXT 'Side-by-side baseline' plus the Claude's-Discretion 'temporary worktree … never a checkout that disturbs master'."
    - "No version bump happens anywhere, per CONTEXT 'AAB size': an `android:release`-shaped build WITHOUT the version bump. `android/version.properties` reads versionCode=6 / versionName=1.5.0 in both temporary trees and in the main checkout, and both builds stamp exactly `<span id=\"mw-app-version\">1.5.0 (6)</span>` into `www/index.html`."
    - "Master is undisturbed. The main checkout's `git status --porcelain` output, and the size and mtime of its `android/app/build/outputs/bundle/release/app-release.aab` (the last locally built release bundle), are identical before and after the builds. Both temporary worktrees are removed and pruned."
    - "`docs/PERF-BASELINE.md` gains an appended `## v1.8 — Phase 60` section; every Phase 49 line stays byte-for-byte as history (additions only). The section records the method, the thresholds, both AAB byte sizes with their sha256 and the delta, a per-build `www/` byte breakdown, and the footprint note: the 54 `set_dungeon_*` props and the 9 party frames were already bundled, unused, in the v1.7 AAB, so the v1.7→v1.8 AAB delta measures the 30 clips plus the code, not the images. It also holds empty device tables (Device, Cold start, Step time, Verdicts, Dispositions) for plan 60-03."
    - "`docs/UAT-v1.8.md` merges every deferred device check from Phases 56–59 verbatim, each exactly once. Items are grouped by phase, newest first. The perf protocol comes first as section A, and it gives the Settings path for each build (v1.7: the ⚙ gear chip right of MAKE CAMP; v1.8: ☰ → SETTINGS). This is CONTEXT 'One checklist'."
  artifacts:
    - docs/PERF-BASELINE.md
    - docs/UAT-v1.8.md
  key_links:
    - "`C:/projects/mazeworld-perf60/artifacts/` is plan 60-03's only input for the builds. It holds `ddr-v1.7-6c299ab-debug.apk`, `ddr-v1.8-<sha7>-debug.apk`, `sizes.json`, `www-v1.7.json` and `www-v1.8.json`. The two AABs are deleted once their bytes and sha256 are recorded. Plan 60-03 reads the v1.8 APK's full commit from `sizes.json` for its freshness gate. Do not rename a file or a key."
    - "One `node_modules` serves both builds only because `package.json`, `package-lock.json`, `android/` and `capacitor.config.json` are byte-identical between `v1.7` and HEAD. This was verified at planning time and is re-verified as the first build step, which stops the plan if it no longer holds."
    - "`core.autocrlf=false` on the worktree add makes each temporary tree's text files byte-identical to the index (LF), like the main checkout. A default fresh worktree on this machine checks out CRLF (`.planning/phases/58-motion-pacing/deferred-items.md`). That would inflate `www/` and the AAB by about one byte per line, a different amount at each commit, and bias the size comparison."
  prohibitions:
    - "MUST NOT build in the main checkout, and MUST NOT write into its `www/`, `android/app/build/` or `node_modules/`. The main checkout is only read from, to copy `node_modules`, `android/local.properties` and `android/keystore.properties` into the temporary trees."
    - "MUST NOT run the version-bump script or the npm script that chains it with the release build, and MUST NOT change `android/version.properties` anywhere. The only release build is `npm run android:release` inside a temporary tree."
    - "MUST NOT upload, publish or push anything. Neither AAB is an upload candidate, because versionCode 6 is already on Play. Asking about a Play internal-testing push is a separate, post-milestone question (CONTEXT Deferred)."
    - "MUST NOT print, log, commit or quote the contents of `android/keystore.properties`. The copies exist only inside the temporary trees and are deleted with them."
    - "MUST NOT commit any APK, AAB or JSON artifact; they live outside the repo. MUST NOT modify any file under engine/, content/, test/parity/, src/, tools/ or mazeworld.html. MUST NOT edit REQUIREMENTS.md, ROADMAP.md or STATE.md."
    - "MUST NOT remove or touch any worktree this plan did not create. In particular, the pre-existing `…/scratchpad/build-7d3dc11` worktree from 2026-09-18 is out of scope."
---

<objective>
Produce the four build artifacts that PERF-03's side-by-side comparison needs, without disturbing master. Then record the AAB size change. Then lay down the two documents the device session will fill: the v1.8 section of `docs/PERF-BASELINE.md` and the merged `docs/UAT-v1.8.md` checklist.

This implements CONTEXT 'Side-by-side baseline' (a debug APK from the `v1.7` tag and one from v1.8 HEAD), 'AAB size' (signed release AABs at both commits, no version bump, bytes and delta recorded, no device needed) and 'One checklist' (`docs/UAT-v1.8.md` merges every deferred device check from Phases 56–59, grouped by phase, with the perf protocol at the top).

Purpose: PERF-03 criterion 3 (the AAB size change is measured and recorded), plus everything criteria 1, 2 and 4 need ready before the Pixel 7 session. The v1.8 debug APK built here is the milestone's one debug APK under the deferred-UAT protocol.

Output: artifacts under `C:/projects/mazeworld-perf60/artifacts/` (not committed), an appended v1.8 section in `docs/PERF-BASELINE.md`, and the new `docs/UAT-v1.8.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-performance-footprint-close/60-CONTEXT.md
@docs/PERF-BASELINE.md
@docs/UAT-v1.7.md
@docs/RELEASING.md
</context>

<discovery>
## Facts established while planning. Re-verify each one, and record it in the SUMMARY.

- **The v1.7 tag builds with today's toolchain.** `git diff --quiet v1.7 HEAD -- package.json package-lock.json android/ capacitor.config.json` exits 0, so the npm scripts, the Capacitor 8.5.1 packages, AGP 8.13.0 / Gradle 8.14.3 and the Android project are all identical at both commits. Among the build inputs, the only file that differs is `tools/build-www.mjs`: HEAD adds `copySfx()`, and v1.7 has no `www/sfx/`. `sfx/enemy-batrat.mp3` exists at v1.7 but is never copied there. `v1.7` is an annotated tag, so use `v1.7^{commit}` (= `6c299ab07e91cd8d00b30e8e690210172c3b4be7`).
- **The dressing images were already in the v1.7 bundle.** Commit `90dc06c` (2026-09-19, "54 dungeon dressing icons, party frames re-cut") is an ancestor of `v1.7`, and v1.7's `copyIcons()` copies the whole `icons/optimized/` directory. The v1.7 AAB therefore already carries the 54 `set_dungeon_*` PNGs (893,505 bytes in git) and the 9 party frames (181,584 bytes), unused. `git diff v1.7 HEAD -- icons/` is empty. At HEAD the 30 clips total 372,480 bytes. The v1.7→v1.8 AAB delta is therefore the clips plus the code, not the images, and the doc must say so in those words rather than let the small delta read as "the 54 images are free".
- **The engine is identical at both commits.** `git diff --quiet v1.7 HEAD -- engine/ content/ test/parity/` exits 0, which is the presentation gate. So depth 5 is the same floor recipe for both builds: `difficultyCurve(5)` gives `{"breather":false,"darkBlobs":3,"waterPools":2}`, matching Phase 49's protocol. Re-run the one-liner to confirm.
- **Both builds stamp "1.5.0 (6)".** `android/version.properties` is unchanged since v1.7, so the Settings Version label cannot tell the two builds apart on the phone. The visible tells are on v1.8 only: the ☰ HUD menu and the Sound / Set dressing rows. Plan 60-03's install log is the record of which build is on the phone.
- **The Settings path differs between builds.** v1.7 has `#mw-gear-btn`, the ⚙ chip right of MAKE CAMP. v1.8 has ☰ → SETTINGS (57-05). In both, a ~1.2 s hold on the Version label reveals the `Start at depth (dev)` row, and `#mw-dev-perf` sits under its Start button.
- **Machine facts (STATE Ground Truth).** `JAVA_HOME` is `C:\Program Files\Microsoft\jdk-21.0.10.7-hotspot\` in executor shells. `tools/pin-jdk.mjs` re-applies it after `cap sync`. The main checkout's `android/local.properties` has `sdk.dir=C:/Users/Dell/AppData/Local/Android/Sdk`. The main checkout's `android/keystore.properties` points at `C:/Users/Dell/android_store_keys/delvedierepeat.jks` (alias `key0`). Without that file `bundleRelease` still succeeds but the bundle is UNSIGNED (`docs/RELEASING.md`), which is why this plan verifies the signature. `node_modules` is about 55 MB.
- **A GSD executor worktree has no `node_modules`, `local.properties` or `keystore.properties`.** That is why every build runs in this plan's own temporary trees, fed from the main checkout. Locate the main checkout as the first `worktree ` line of `git worktree list --porcelain` (at planning time, `C:/projects/mazeworld`).
- **A precedent.** A 2026-09-18 session built from a detached worktree with a `node_modules` link (`…/scratchpad/build-7d3dc11`, still registered). This plan copies `node_modules` instead of linking it. The reason: Gradle builds the `:capacitor-android` module inside `node_modules/@capacitor/android/capacitor/build`, and with a link that build directory would be the main checkout's own.
</discovery>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 60-02's share)

| Kind | Name | Notes |
|---|---|---|
| Out-of-repo artifacts | `C:/projects/mazeworld-perf60/artifacts/ddr-v1.7-6c299ab-debug.apk`, `ddr-v1.8-<sha7>-debug.apk` | The two builds plan 60-03 installs. The v1.8 one is the milestone's debug APK. Never committed. |
| Out-of-repo record | `…/artifacts/sizes.json`, `www-v1.7.json`, `www-v1.8.json` | For each build: the full commit, APK/AAB bytes and sha256, and a `www/` byte breakdown. The AABs themselves are deleted after recording. |
| Doc section | `docs/PERF-BASELINE.md` `## v1.8 — Phase 60 (PERF-03): v1.7 vs v1.8 side by side` | Method, thresholds, AAB table, footprint note, and empty device tables. |
| New doc | `docs/UAT-v1.8.md` | The one batched Pixel 7 checklist: perf protocol first, then Phases 59 → 56 verbatim, then the older open batches. |
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build v1.7 and v1.8 (debug APK + signed AAB each) in temporary detached worktrees outside the repo</name>
  <files>C:/projects/mazeworld-perf60/artifacts/ (out-of-repo build outputs; no repo file changes)</files>
  <read_first>
    - `package.json`: the `cap:sync`, `android:debug` and `android:release` scripts. `android:release` is `cap:sync` → `pin-jdk` → `gradle.mjs bundleRelease`, with no bump.
    - `tools/pin-jdk.mjs` (header): it reads `JAVA_HOME` and throws if it is unset.
    - `tools/gradle.mjs`: it spawns `gradlew.bat` from `android/` relative to its own location, so it works unchanged inside a temporary tree.
    - `tools/build-www.mjs:250-323`: `vendorCapacitorPackages()` reads `node_modules/@capacitor/*`, and `writeIndexHtml()` stamps the `#mw-app-version` span from `android/version.properties`.
    - `docs/RELEASING.md`: why a missing `keystore.properties` yields an unsigned bundle.
    - `.gitignore`: `www/`, `android/local.properties`, `android/keystore.properties`, `*.apk` and `*.aab` are all ignored, so nothing built here can be staged by accident.
  </read_first>
  <action>
Build sequentially, v1.7 first and v1.8 second. Never run two Gradle builds at once. Run each npm script in its own Bash call with the 600000 ms timeout. If a first cold Gradle build in a fresh project directory runs past that cap, re-issue it with `run_in_background` and a Monitor on the Gradle output, rather than splitting it into half-steps.

**Pre-flight (stop the plan if any check fails, and report which one):**
1. Record `HEAD_SHA` as `git rev-parse HEAD` and `V17_SHA` as `git rev-parse "v1.7^{commit}"` (expect `6c299ab07e91cd8d00b30e8e690210172c3b4be7`). Record `MAIN` as the path on the first `worktree ` line of `git worktree list --porcelain`.
2. Toolchain parity: `git diff --quiet "$V17_SHA" "$HEAD_SHA" -- package.json package-lock.json android/ capacitor.config.json` must exit 0. If it does not, stop: one `node_modules` can no longer serve both builds.
3. These must all exist: `$MAIN/node_modules/@capacitor/cli/package.json`, `$MAIN/android/local.properties`, `$MAIN/android/keystore.properties`, and `$JAVA_HOME/bin/jarsigner.exe`.
4. Set `ROOT=C:/projects/mazeworld-perf60`. If it already exists from an earlier attempt: `git worktree remove --force` every registered worktree whose path starts with it, then delete the directory and run `git worktree prune`. Then create `$ROOT/artifacts`.
5. Snapshot the main checkout. Write `git -C "$MAIN" status --porcelain=v1` to `$ROOT/artifacts/main-status-before.txt`. If `$MAIN/android/app/build/outputs/bundle/release/app-release.aab` exists, record its size in bytes and its mtime in ms (read them with a `node -e` statSync call).

**For each build, in order `(v1.7, $V17_SHA)` then `(v1.8, $HEAD_SHA)`, with `T="$ROOT/<label>"` and `S7` = the first 7 characters of the sha:**
1. `git -c core.autocrlf=false worktree add --detach "$T" <sha>`
2. Copy `$MAIN/node_modules` to `$T/node_modules` (recursive copy, not a link). Copy `$MAIN/android/local.properties` and `$MAIN/android/keystore.properties` into `$T/android/`.
3. From `$T`, run `npm run android:debug` (exit 0), then `npm run android:release` (exit 0). Both `cd` into `$T` in the same Bash call.
4. Checks inside `$T`:
   - `git -C "$T" diff --quiet -- android/version.properties` exits 0.
   - `$T/www/index.html` contains exactly one match of the regex `<span id="mw-app-version">1\.5\.0 \(6\)<\/span>`.
   - Running `"$JAVA_HOME/bin/jarsigner" -verify` on `$T/android/app/build/outputs/bundle/release/app-release.aab` prints a line matching `^jar verified\.`.
   - `$T/www/icons/optimized/` holds 54 files matching `^set_dungeon_.*\.png$`.
   - For v1.7 only, `$T/www/sfx` does not exist. For v1.8 only, `$T/www/sfx` holds exactly 30 files matching `\.mp3$`.
5. Compute the `www/` breakdown with a `node -e` script that sums file bytes recursively. Write `{ indexHtml, sfx, icons, fonts, vendor, src, engine, content, assets, total }` to `$ROOT/artifacts/www-<label>.json`. A directory that does not exist counts as 0.
6. Copy `app-debug.apk` (`android/app/build/outputs/apk/debug/`) to `$ROOT/artifacts/ddr-<label>-<S7>-debug.apk`. Copy `app-release.aab` to `$ROOT/artifacts/ddr-<label>-<S7>-release.aab`.
7. From `$T`, run `node tools/gradle.mjs --stop`. This releases the Gradle daemon's Windows file locks so that the worktree can be removed.

**After both builds:**
1. With a `node -e` script (`fs.statSync` for bytes, `crypto.createHash("sha256")` over the file for the hash), write `$ROOT/artifacts/sizes.json`. For each label it holds `{ sha, sha7, apkFile, apkBytes, apkSha256, aabFile, aabBytes, aabSha256, builtAt }`, where `builtAt` is an ISO timestamp. At the top level it holds `aabDeltaBytes` and `apkDeltaBytes`, each v1.8 minus v1.7.
2. `git worktree remove --force "$ROOT/v1.7"`, then the same for `"$ROOT/v1.8"`, then `git worktree prune`. If a removal fails on a lock, stop the daemon again, wait, and retry once.
3. Master-undisturbed proof:
   - `git -C "$MAIN" status --porcelain=v1` is byte-identical to `main-status-before.txt`.
   - The main AAB's size and mtime are unchanged, or it is still absent if it was absent before.
   - `git -C "$MAIN" diff --quiet -- android/version.properties` exits 0.

Do not commit anything in this task: its outputs live outside the repo. Never echo `keystore.properties` or any line of it. When a check needs to confirm that the file exists, test its existence only.
  </action>
  <acceptance_criteria>
    - `$ROOT/artifacts/` holds four build files whose names match `^ddr-v1\.(7|8)-[0-9a-f]{7}-(debug\.apk|release\.aab)$`, with exactly one of each label × kind, plus `sizes.json`, `www-v1.7.json`, `www-v1.8.json` and `main-status-before.txt`.
    - `sizes.json` parses. `v1.7.sha` equals `git rev-parse "v1.7^{commit}"`, and `v1.8.sha` equals this plan's recorded `HEAD_SHA`. Every byte count is a positive integer. `aabDeltaBytes` equals `v1.8.aabBytes − v1.7.aabBytes`.
    - Both jarsigner runs printed a line matching `^jar verified\.`. Both version stamps matched the anchored span regex exactly once. The sfx and set_dungeon file-count checks passed with the exact counts above.
    - `git worktree list --porcelain` has zero lines matching `^worktree C:/projects/mazeworld-perf60/`.
    - The master-undisturbed proof passed: identical status, identical main AAB stat, and `version.properties` clean.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('C:/projects/mazeworld-perf60/artifacts/sizes.json');const ok=['v1.7','v1.8'].every(k=>Number.isInteger(s[k].aabBytes)&&s[k].aabBytes>0&&Number.isInteger(s[k].apkBytes)&&s[k].apkBytes>0)&&s.aabDeltaBytes===s['v1.8'].aabBytes-s['v1.7'].aabBytes;process.exit(ok?0:1)"</automated>
  </verify>
  <done>Four signed/debug artifacts exist outside the repo, built by identical scripts from `v1.7` and HEAD. No version was bumped. The temporary trees are gone, and the main checkout is provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: PERF-BASELINE.md — record the AAB change and scaffold the v1.8 device tables</name>
  <files>docs/PERF-BASELINE.md</files>
  <read_first>
    - `docs/PERF-BASELINE.md` (whole file, 357 lines): the Method / Device / Protocol / BEFORE / Decision / AFTER / How to re-measure house style, the four-row table shape `| Row | median (ms) | p95 (ms) | max (ms) | n |`, and the verbatim readout code-block convention.
    - `C:/projects/mazeworld-perf60/artifacts/sizes.json`, `www-v1.7.json`, `www-v1.8.json` (from Task 1).
    - `.planning/phases/60-performance-footprint-close/60-CONTEXT.md`: 'Baseline & measurement' and 'Regression thresholds & dispositions', to quote the thresholds.
    - `.planning/phases/60-performance-footprint-close/60-01-PLAN.md` must_haves: the `tools/cold-start.mjs` CLI contract (`run --label --runs --warmup --serial --out`, `judge`, `adb-path`). The Method text names it exactly.
  </read_first>
  <action>
Record `BASE_60_02` as `git rev-parse HEAD` before this task's commit.

Re-confirm the depth-5 protocol against the engine with a `node -e` one-liner that imports `./engine/difficulty.js` and prints `difficultyCurve(5)`'s `breather` / `darkBlobs` / `waterPools`. Expect `{"breather":false,"darkBlobs":3,"waterPools":2}`. If the result differs, record the actual numbers and the shallowest depth ≥ 5 that has at least one dark blob and one water pool, and use that depth in both docs.

Directly under the H1, add one line pointing readers to the new section. Change nothing else above the new section.

Append this section at the end of the file:

**`## v1.8 — Phase 60 (PERF-03): v1.7 vs v1.8 side by side`.** Open it with a short paragraph. Phase 60 measures v1.8's footprint (30 clips, the party frames and the 54 set-dressing props, plus the Phase 57–59 shell work) against a v1.7 build measured in the same Pixel 7 session. The Phase 49 sections above remain the history of v1.6's instrumented pass, and none of their rows is re-used as the v1.7 number.

**`### Method (v1.8)`.** State each of the following:
- Builds: v1.7 is tag `v1.7` at `6c299ab`, and v1.8 is the recorded HEAD sha. Both were built in temporary detached worktrees with `core.autocrlf=false`, by `npm run android:debug` and `npm run android:release`, with no version bump. Both stamp 1.5.0 (6). Give the toolchain-identity fact.
- Cold start: `node tools/cold-start.mjs run --label <build> --runs 10 --warmup 1` per build. It force-stops, waits 1.5 s, launches with `am start -W`, then holds 4 s. There is 1 unrecorded warm-up launch, then 10 measured launches that must all report `LaunchState: COLD`. Median and p95 use the Phase 49 nearest-rank method, and at n = 10 the p95 is the slowest launch. App data is preserved across the install-overs and is the same for both builds.
- The TotalTime caveat, verbatim in meaning: `capacitor.config.json` holds the splash (`launchAutoHide: false`) until the web boot hides it, and this doc does not assert whether the platform's `TotalTime` includes that hold. Both builds share a byte-identical native shell (`android/` is unchanged since v1.7), so the comparison is like for like either way.
- Step time: the Phase 49 Protocol above, walked once per build in the same session, from depth 5 (paste the re-confirmed curve JSON). Give the two Settings paths (v1.7: ⚙ chip right of MAKE CAMP; v1.8: ☰ → SETTINGS). Shipped defaults apply: Sound On, Set dressing On, Android Remove animations off.
- AAB: the file bytes of the signed `app-release.aab` at each commit, with sha256.
- The thresholds, quoted from 60-CONTEXT. Each is strictly greater, applied by `node tools/cold-start.mjs judge`, with deltas rounded to 0.001. "2 MB" is read as 2,000,000 bytes. The step threshold gates the `step` row (the superset row); `dispatch` / `paint` / `draw` are recorded for attribution only.

**`### AAB size`.** This one is filled now, from `sizes.json`. It holds a table `| Build | Commit | AAB bytes | AAB sha256 | Debug APK bytes |`, with one row per build and a `Delta (v1.8 − v1.7)` row giving the byte delta and its percentage. Below it, a `www/` breakdown table (uncompressed bytes per top-level entry: index.html, sfx, icons, fonts, vendor, src, engine, content, assets and total), with v1.7, v1.8 and delta columns. Then the footprint note, in plain words. The 54 `set_dungeon_*` props (893,505 B) and the 9 party frames (181,584 B) were committed in `90dc06c` before the v1.7 tag, and v1.7's whole-directory icon copy already shipped them, unused. Both AAB columns therefore contain them, and the delta is essentially the 30 clips (372,480 B) plus the code. Re-derive all three byte totals with `git ls-tree -r -l HEAD` sums rather than copying them from this plan. If the main checkout's `app-release.aab` exists, add one context line with its bytes and mtime, labelled as the last locally built release bundle. Per STATE, the most recent Play upload (1.5.0 vc6, 2026-09-19) predates the icon commit, which makes it the closest measure of what testers download today. Label that line context, not a baseline. Do not write a verdict here; Verdicts is filled by plan 60-03 through `judge`.

**`### Device`, `### Cold start (TotalTime, ms)`, `### Step time (ms)`, `### Verdicts (PERF-03)` and `### Dispositions`.** Leave these empty, with an italic `_(filled by plan 60-03 in the device session)_` line in each:
- Device: a two-column Field / Value table. Rows: Device, Android version, Build number (`ro.build.display.id`), Session date, Battery / temperature at start and end, Install order, APK files (both names and sha256, filled now from `sizes.json`).
- Cold start: `| Build | Commit | n | median | p95 | min | max | LaunchStates | samples (ms, run order) |`, with one empty row per build.
- Step time: `| Row | v1.7 median | v1.7 p95 | v1.7 max | v1.7 n | v1.8 median | v1.8 p95 | v1.8 max | v1.8 n |`, with the four rows step / dispatch / paint / draw empty. Add two empty verbatim-readout code blocks labelled v1.7 and v1.8, and an As-run line per build (coverage of water / dark / encounter / tab switch, and jank).
- Verdicts: an empty placeholder for the `judge` table.
- Dispositions: a placeholder that states the rule. Every regression gets a fix or an explained, accepted miss, in the user's words. If nothing regresses, the section says so in words. A disposition is never written on the user's behalf.

Once the section is written and checked, delete the two `*-release.aab` files from `$ROOT/artifacts/`. Their bytes and hashes are recorded, and they are not upload candidates. Keep the APKs and the JSON files.

Commit `docs/PERF-BASELINE.md` alone.
  </action>
  <acceptance_criteria>
    - `grep -cE '^## v1\.8 — Phase 60' docs/PERF-BASELINE.md` returns 1.
    - Additions only: for the commit this task made, the deletions column of `git diff --numstat "$BASE_60_02" HEAD -- docs/PERF-BASELINE.md` is 0.
    - Section structure, asserted on the slice from the `## v1.8 — Phase 60` line to the end of file: each of the line-anchored headings `### Method (v1.8)`, `### AAB size`, `### Device`, `### Cold start (TotalTime, ms)`, `### Step time (ms)`, `### Verdicts (PERF-03)` and `### Dispositions` occurs exactly once, in that order.
    - A node check reads `sizes.json` and asserts that the AAB table rows (found by a line-anchored regex beginning `^\| v1\.7 \|` or `^\| v1\.8 \|` inside the `### AAB size` slice) carry exactly `sizes.json`'s `aabBytes`, and that the delta row carries `aabDeltaBytes`.
    - `ls C:/projects/mazeworld-perf60/artifacts/` lists no file ending `.aab`, and still lists both `-debug.apk` files.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/PERF-BASELINE.md','utf8').split(/\r?\n/);const i=t.findIndex(l=>l.startsWith('## v1.8 — Phase 60'));if(i<0)process.exit(1);const s=t.slice(i);const hs=['### Method (v1.8)','### AAB size','### Device','### Cold start (TotalTime, ms)','### Step time (ms)','### Verdicts (PERF-03)','### Dispositions'];let p=-1;for(const h of hs){const idx=s.map((l,k)=>l===h?k:-1).filter(k=>k>=0);if(idx.length!==1||idx[0]<=p)process.exit(1);p=idx[0]}"</automated>
  </verify>
  <done>The AAB change is recorded with bytes, hashes, a per-directory breakdown and an honest footprint note. The device tables stand ready for plan 60-03, and no Phase 49 line moved.</done>
</task>

<task type="auto">
  <name>Task 3: docs/UAT-v1.8.md — the one batched Pixel 7 checklist</name>
  <files>docs/UAT-v1.8.md</files>
  <read_first>
    - `docs/UAT-v1.7.md` (whole file, 88 lines): the house format. It has a `**Build:**` / `**Protocol:**` / `**Suggested order:**` header, lettered `## X. Phase NN — Name (count)` sections newest-first, `| # | Check | Result |` tables with `NN-k` ids, a "Still open from earlier milestones" section, and a verdict block.
    - The `human_verification` frontmatter lists of `.planning/phases/56-sound-effects-audio-settings/56-VERIFICATION.md` (6 items), `.planning/phases/57-map-hud-layout-band/57-VERIFICATION.md` (10 items) and `.planning/phases/58-motion-pacing/58-VERIFICATION.md` (7 items).
    - Phase 59's list. Use `.planning/phases/59-party-animation-dungeon-set-dressing/59-VERIFICATION.md`'s `human_verification` when that file exists: it is the orchestrator's authoritative merge. Otherwise use the union of the `## Human Verification — Phase 60 batched Pixel 7 checklist` sections of 59-03-SUMMARY.md and 59-04-SUMMARY.md, plus the Phase 60 checklist section of 59-05-SUMMARY.md, plus 59-02-SUMMARY.md's coverage entry `D11` (the legibility check).
    - `C:/projects/mazeworld-perf60/artifacts/sizes.json`: the v1.8 APK file name, commit and sha256 for the Build line.
  </read_first>
  <action>
Create `docs/UAT-v1.8.md` in the UAT-v1.7 house format.

**Header.**
- H1: `# v1.8 Pixel 7 device round — Phase 60 (Performance & Footprint Close)`.
- `**Build:**` the v1.8 debug APK (file name under `C:/projects/mazeworld-perf60/artifacts/`, the full commit, and sha256 from `sizes.json`). It is the milestone's one debug APK. Add the v1.7 baseline APK (tag `v1.7`, `6c299ab`), which is installed only for the section-A comparison. Note that both show "Version 1.5.0 (6)" in Settings, and that v1.8 is the one with the ☰ menu and the Sound row.
- `**Protocol:**` deferred-UAT: this is the one batched session. The executor installs the builds and runs the cold-start launches. Record results as the user states them. Findings become todos or quick tasks, never mid-run edits. PERF-03 regressions get a disposition in `docs/PERF-BASELINE.md`.
- `**Sources:**` the four files the items were merged from, naming which Phase 59 source was used.
- `**Suggested order:**` in this sequence:
  1. A1, hands off while the executor launches both builds.
  2. A2 on v1.7.
  3. The executor switches to v1.8. If the user agreed to the data wipe, this is a fresh install, and 56-3's airplane-mode first launch comes first.
  4. A3 on v1.8.
  5. Sections B–E.
  6. Every item that needs Android's Remove animations, together in one pass at the end.
  7. Every TalkBack item, together.
  8. Section F only if the user has appetite for it.

**`## A. Performance — PERF-03 (5)`.** A `| # | Step | Who | Result |` table:
- A1: cold start, 1 warm-up + 10 COLD launches per build, run by the executor. The user keeps the phone unlocked, on the charger and untouched for about 3 minutes.
- A2: the step walk on v1.7. Give the exact path: Title → ENTER → any roll → ⚙ chip right of MAKE CAMP → hold the Version label about 1.2 s → depth 5 → Start. Then walk at least 50 mixed steps through a water pool, into a dark region and out again, with at least one encounter card and at least one Gear or Hero tab switch and back. Then read the `#mw-dev-perf` line under Start. Report the line verbatim, which of the four coverage items were hit, and any jank or "none".
- A3: the same walk on v1.8, with Settings reached by ☰ → SETTINGS.
- A4: AAB size, already measured by plan 60-02 with no device. Link to the `### AAB size` section.
- A5: the user's ruling on each regression the executor flags. The options are accept with a reason, fix, or keep walking the same run and re-read (the later read at n ≥ 50 supersedes, as in Phase 49's n=47 → n=61).

**Sections B–E, newest first.** `## B. Phase 59 — Party Animation & Dungeon Set Dressing (N)`, `## C. Phase 58 — Motion & Pacing (7)`, `## D. Phase 57 — Map & HUD Layout Band (10)` and `## E. Phase 56 — Sound Effects & Audio Settings (6)`. Each table is `| # | Check | When | Result |`. The ids are `59-k` / `58-k` / `57-k` / `56-k`, with k running 1.. in source order. Each Check cell is the source item's text **verbatim**: do not paraphrase, shorten or merge. If an item's text contains a `|`, escape it as `\|`.
- For Phase 59 from SUMMARYs: drop an item whose text is identical to an earlier one. If 59-05's legibility item covers `D11`, fold `D11` into it and say so in one line under the table.
- The When cell says when to run the item:
  - `reduced-motion pass`: any item that needs Android's Remove animations.
  - `TalkBack pass`: any TalkBack item.
  - `fresh install (destructive)`: only 56-3. It wipes this phone's save and graveyard, so run it only if the user OK'd that.
  - `any time`: everything else.
- **Every Result cell stays EMPTY.** Plan 60-03 detects an unrecorded item by an empty Result cell, so nothing may be pre-filled there.

**`## F. Still open from earlier milestones`.** `docs/UAT-v1.7.md` (the four-run DR checklist + 25), `docs/UAT-v1.6.md` (26) and `docs/UAT-v1.5.md` (140). These are optional in the same sitting and are not this phase's requirement.

**`## Session record`** (filled by plan 60-03). Leave placeholders for: the install log, with each APK installed and at what time; the final build left on the phone; the regressions and rulings, with a pointer to `docs/PERF-BASELINE.md#dispositions`; and the findings → todos list.

Commit `docs/UAT-v1.8.md` alone.
  </action>
  <acceptance_criteria>
    - `docs/UAT-v1.8.md` exists. The line-anchored headings `^## A\. Performance`, `^## B\. Phase 59`, `^## C\. Phase 58`, `^## D\. Phase 57`, `^## E\. Phase 56`, `^## F\. Still open` and `^## Session record` each occur exactly once, in that order.
    - Verbatim coverage, asserted by a node script. For 56/57/58, the script parses each VERIFICATION frontmatter's `human_verification:` block, taking each line anchored `^  - "(.*)"$` and passing it through `JSON.parse` of the quoted part. It asserts that the count of rows anchored `^\| 5N-\d+ \|` in the doc equals the source length for each of N = 6, 7, 8, and that every source string appears as a complete table cell (preceded by `| ` and followed by ` |`) in that phase's section slice.
    - The same script checks Phase 59 against whichever source was used. If `59-VERIFICATION.md` exists it must have been the source, and the row count equals its list length. Otherwise the count equals the deduplicated SUMMARY union, less any documented D11 fold.
    - Section A has exactly five rows anchored `^\| A[1-5] \|`.
    - Every row anchored `^\| 5[6-9]-\d+ \|` splits (on unescaped `|`) into exactly four cells. Its When cell is one of `any time`, `reduced-motion pass`, `TalkBack pass` or `fresh install (destructive)`, with the last used exactly once (row 56-3), and its Result cell is empty after trimming.
    - The `**Build:**` line carries the v1.8 APK's full commit, and it equals `sizes.json`'s `v1.8.sha`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/UAT-v1.8.md','utf8');const hs=[/^## A\. Performance/m,/^## B\. Phase 59/m,/^## C\. Phase 58/m,/^## D\. Phase 57/m,/^## E\. Phase 56/m,/^## F\. Still open/m,/^## Session record/m];let p=-1;for(const h of hs){const i=t.search(h);if(i<=p)process.exit(1);p=i}const a=(t.match(/^\| A[1-5] \|/gm)||[]).length;process.exit(a===5?0:1)"</automated>
  </verify>
  <done>One checklist carries every deferred Phase 56–59 device check verbatim, each exactly once, with the perf protocol first and each build's Settings path spelled out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main checkout → temporary trees | Machine-local secrets (`keystore.properties`) and `node_modules` are copied out of the main checkout. |
| Build output → Play | A signed AAB is an upload-capable artifact. |
| Plan → master | Any checkout or build in the main tree would disturb master mid-milestone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-60-05 | Information Disclosure | `keystore.properties` copies | high | mitigate | Copied only into out-of-repo temporary trees, and never printed (the action forbids echoing it; existence is tested, never content). The copies are deleted when the trees are removed with `git worktree remove --force`. `.gitignore` already excludes the file. |
| T-60-06 | Tampering | Master disturbed by a checkout or build | high | mitigate | Builds run only in detached temporary worktrees outside the repo. The main checkout's status snapshot, its release-AAB stat and its `version.properties` are asserted identical afterwards (Task 1 acceptance). |
| T-60-07 | Repudiation | A re-used versionCode AAB mistaken for an upload | medium | mitigate | No bump anywhere (asserted in both trees and in main), both AABs deleted after recording, and no upload step. Play would also reject versionCode 6 as already used. |
| T-60-08 | Repudiation | A biased size comparison (CRLF worktree, or a different toolchain per commit) | medium | mitigate | `core.autocrlf=false` on both worktree adds, plus the toolchain-parity pre-flight that stops the plan on any difference. |
| T-60-SC | Tampering | npm/pip/cargo installs | high | mitigate | No install: `node_modules` is copied from the main checkout, and `package.json` / `package-lock.json` are unchanged at both commits. |
</threat_model>

<verification>
- `sizes.json` is consistent (Task 1 verify). `git worktree list --porcelain` shows no `mazeworld-perf60` entry. The main checkout is provably untouched.
- `docs/PERF-BASELINE.md` has the v1.8 section (additions only), and `docs/UAT-v1.8.md` has every source item verbatim.
- `npm test` fail 0 and `npm run build:www` exit 0 on the main checkout. If this plan runs in an isolated worktree without `node_modules`, record "not run here; the orchestrator runs it after merge", following the 59-01 precedent. The known 58 CRLF worktree-only failures must be reported, not fixed.
- `git diff --quiet v1.7 HEAD -- engine/ content/ test/parity/` exits 0. `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`. `package.json` / `package-lock.json` are unchanged.
</verification>

<success_criteria>
- Both builds exist, from `v1.7` and HEAD, built identically without a version bump, and master was never disturbed.
- The AAB size change is recorded with bytes, sha256, a breakdown and an honest footprint note (PERF-03 criterion 3).
- The device session has its record ready: the PERF-BASELINE v1.8 tables and the merged UAT-v1.8 checklist.
</success_criteria>

<output>
Create `.planning/phases/60-performance-footprint-close/60-02-SUMMARY.md` when done.
</output>
