---
phase: 65-run-record-personal-bests
plan: 04
type: execute
wave: 2
depends_on: ["65-01", "65-02", "65-03"]
files_modified:
  - content/season.js
  - content/index.js
  - engine/death.js
  - test/unit/run-summary.test.js
  - src/browser/engineAdapter.js
  - src/browser/storage.js
  - src/browser/settings.js
  - test/unit/engineAdapter.test.js
  - test/unit/new-run-loop.test.js
  - test/unit/bests-adapter.test.js
autonomous: true
requirements: [RUN-01, RUN-02, RUN-03, RUN-04]
user_setup: []

must_haves:
  truths:
    - "Every death's run summary (the die() return value, every bury() graveyard stone, and every ddr.bests.v1 entry) carries season (the SEASON constant, 1 in v2.0), the run seed, the action count acts, and an 8-hex integrity hash, with zero new rng draws"
    - "The run hash is the run's stable id: the same seed and the same action sequence always give the same summary hash, and changing the epitaph, the death note or the wall-clock when never changes it"
    - "Empty (summary): a hand-built state with no acts field produces a summary with acts 0 and a valid hash, and never throws"
    - "A durable ddr.bests.v1 record is written through mzStorage (Capacitor Preferences plus the localStorage mirror) at the existing died-event choke point, back-to-back with the three graveyard writes, and never for a dev start-at-depth run"
    - "boot() loads ddr.bests.v1 into memory before it returns, so the death panel can learn about a new best synchronously. If the key is absent or unparseable, it backfills from ddr.graveyard.v1 (season 0 entries, no seed or acts) and writes the result"
    - "The stored graveyard now keeps up to 60 stones (GRAVE_CAP 60, matching the engine's bury() cap), and the bests record survives the trim: after 62 deaths the first, deepest run is gone from the graveyard yet is still DEEPEST #1 in ddr.bests.v1"
    - "ddr.best.v1 is retired: the adapter never writes or reads it, a stored value stays on disk untouched, and storage.js's LEGACY_KEYS migration is unchanged. ddr.graveyard.total.v1 keeps counting every death untrimmed"
    - "An existing graveyard, the legacy ddr.best.v1 / ddr.graveyard.total.v1 keys, and an old in-progress save with no acts field all load through boot() without error; the old save's acts loads as 0"
    - "Empty (record): the first death on a device with no stones reports { first: true, newBests: [] }"
    - "takeDeathRecord() returns { first, newBests, summary } once per recorded death and null after that; it is null after a dev-run death and after initRun/startNewRun starts a new run"
    - "Idempotency: a died event is folded into the record exactly once; takeDeathRecord() consumes the report on read"
    - "Concurrency: the in-memory record is updated synchronously inside dispatch before any await, so two deaths dispatched back-to-back without a flush both end up in the stored record; the bests write is tracked, so waitForPending() awaits it"
    - "Fail-safe: with storage blocked (getItem/setItem throwing), boot resolves, a death through dispatch never throws and never falls closed to a fresh run, and the in-memory report still works"
    - "If a death arrives before the record is loaded this session, the persist path loads (or backfills) the record first and then folds the run in. It never overwrites the stored record with a fresh empty one"
    - "The shared board table is one table: content/boards.js BOARD_COPY keys deepStrictEqual engine/records.js BOARD_IDS"
    - statement: "A process kill between the death and the ddr.bests.v1 write loses at most that one run from the record and never corrupts it; the next boot loads the previous complete record"
      verification: backstop
  prohibitions:
    - statement: "The personal-bests record must never leave the device: no network call, no telemetry, no new dependency or SDK. It lives only in mzStorage"
      status: unresolved
      verification: judgment
    - statement: "Dev start-at-depth runs must never enter the bests record, the graveyard, the total or the recent-names window"
      status: unresolved
      verification: judgment
    - statement: "Migration must not delete or overwrite the retired ddr.best.v1 value or any existing graveyard stone"
      status: unresolved
      verification: judgment
    - statement: "No GameState field may be added for the bests record or the death report; cross-run data stays adapter-owned"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "content/season.js"
      provides: "SEASON = 1 with its hand-bump changelog"
      contains: "export const SEASON = 1"
    - path: "engine/death.js"
      provides: "exported buildRunSummary carrying season, seed, acts, hash"
      exports: ["buildRunSummary", "die", "bury", "epitaphFor", "epitaphCtx", "forfeitLoot"]
    - path: "src/browser/engineAdapter.js"
      provides: "ddr.bests.v1 load/backfill/update/persist, GRAVE_CAP 60, the one-shot death report"
      exports: ["loadBests", "getBests", "takeDeathRecord"]
    - path: "test/unit/bests-adapter.test.js"
      provides: "adapter-level bests, backfill, legacy-key, cap, one-shot and fail-safe tests"
      min_lines: 200
    - path: "test/unit/run-summary.test.js"
      provides: "summary fields and the hash-as-id invariants"
      min_lines: 60
  key_links:
    - from: "engine/death.js#buildRunSummary"
      to: "engine/records.js#runHash"
      via: "import { runHash } from ./records.js; summary.hash = runHash(summary)"
      pattern: "runHash\\("
    - from: "engine/death.js"
      to: "content/season.js"
      via: "SEASON through the content/index.js barrel"
      pattern: "SEASON"
    - from: "src/browser/engineAdapter.js#dispatch"
      to: "engine/records.js#updateBests"
      via: "a synchronous in-memory fold at the died-event choke point, then a tracked persist"
      pattern: "updateBests\\("
    - from: "src/browser/engineAdapter.js#boot"
      to: "src/browser/engineAdapter.js#loadBests"
      via: "awaited after migrateLegacyKeys, before the save load"
      pattern: "await loadBests\\(\\)"
---

<objective>
Wire the run record end to end.

(1) RUN-01: `buildRunSummary` gains `season` (from a new `SEASON` content constant), `seed`, `acts` (from Plan 65-01) and `hash` (engine/records.js#runHash from Plan 65-02). It is exported, per CONTEXT "Run summary fields (RUN-01)".

(2) RUN-02 and RUN-03, in the adapter:
- `ddr.bests.v1` is loaded into memory at boot, backfilling from the graveyard when it is absent;
- deaths are folded in synchronously at the existing died-event choke point, with the write persisted back-to-back with the graveyard writes;
- `GRAVE_CAP` goes back to 60;
- `ddr.best.v1` is retired outright (greenfield ruling).

(3) RUN-04: a one-shot `takeDeathRecord()` hands the death panel its `{ first, newBests, summary }` report, mirroring `takeBootWornReport()`. No GameState field, per CONTEXT's discretion note.

Purpose: every later v2.0 phase (the panel, PGS submission, "you placed X") reads real, durable data.
Output: content/season.js, the updated death.js and engineAdapter.js, and two new test files (plus re-pinned adapter tests).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-run-record-personal-bests/65-CONTEXT.md
@.planning/phases/65-run-record-personal-bests/65-01-SUMMARY.md
@.planning/phases/65-run-record-personal-bests/65-02-SUMMARY.md
@.planning/phases/65-run-record-personal-bests/65-03-SUMMARY.md
@engine/death.js
@src/browser/engineAdapter.js

<interfaces>
From Plan 65-01: `state.acts` is a non-negative integer on every engine GameState, and 0 on a loaded pre-Phase-65 save.
From Plan 65-02 (engine/records.js): `runHash(run) -> 8-hex`; `BOARD_IDS`; `emptyBests()`; `sanitizeBests(raw)`; `updateBests(record, summary) -> { record, newBests, first }`; `backfillBests(graves)`. BestsRecord is `{ v:1, runs, boards:{deep,lean,days,kills,purse}, lineage, last }`.
From Plan 65-03: `BOARD_COPY` (content/boards.js) is keyed deep/lean/combo/days/kills/purse/yard; `newBestView(report)` (src/browser/newBest.js) consumes this plan's report shape.

The contract this plan creates (Plan 65-05 and Phase 66 consume it), all in src/browser/engineAdapter.js:
- `loadBests(): Promise<BestsRecord>`: never rejects
- `getBests(): BestsRecord | null`: the in-memory record (null before the first load this session). Callers treat it as read-only
- `takeDeathRecord(): { first: boolean, newBests: string[], summary: RunSummary } | null`: one-shot
- RunSummary: `{ name, race, sub, cls, level, sp, floor, day, steps, gold, kills, cause, note, epitaph, when, season, seed, acts, hash }`
</interfaces>

<assumption_delta_decision>
signal: pluralization on "also" in RUN-01 ("a run summary ... that also carries a rules/season version ..."). Mostly a lexical false positive, with one genuine identity shift.
identity_shift: a run summary gains a stable identity for the first time. Until now a tombstone was identified only by its array position and its `when` timestamp.
primary_noun: the run's `hash` (FNV-1a 32-bit over the canonical fields season, seed, acts, floor, steps, day, kills, gold, sp, level, race, sub, cls, name, cause; engine/records.js#runHash).
decision: promote. The hash is THE run id: the key of the bests record's deduped runs table now, and of Phase 68's submission queue later. `when` is demoted to a display and detail field.
invariant: the same seed plus the same action sequence always yields the same summary hash, and editing epitaph or deathNote copy (or the wall clock) never changes it.
test: Task 1 below (test/unit/run-summary.test.js, the "hash is the run id" tests).
</assumption_delta_decision>

## Flagged assumptions (unresolved edges, surfaced, not dropped)

- **RUN-03 (edge probe category "unclassified", status unresolved):** there is no catalogue of every legacy data shape on real devices. This plan assumes graveyard stones are either the E12-era buildRunSummary shape (15 fields) or older partial objects (for example `{ name, cause }`, as in the E12 migration test). It skips any stone without a finite numeric floor rather than guessing values. It assumes `ddr.best.v1` holds a bare number string, which is never read either way. And it assumes pre-Phase-65 saves pass through validateSave's existing tolerant chain. The Pixel 7 upgrade-install check below is the backstop for shapes nobody has seen yet.
- **LINEAGE counts after backfill** start from the stones still on the device (at most 5 today), not the true all-time count. Trimmed stones are unrecoverable. CONTEXT accepts this ("seed the record from the stones in ddr.graveyard.v1").

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SEASON and the run summary's season, seed, acts and hash fields</name>
  <files>content/season.js, content/index.js, engine/death.js, test/unit/run-summary.test.js</files>
  <read_first>
    - engine/death.js (the whole file, 147 lines: buildRunSummary at 61-81, die at 107-134, bury at 144-147, the content import at line 12)
    - content/index.js (the barrel; append one export line)
    - engine/records.js (runHash and RUN_HASH_FIELDS, from Plan 65-02)
    - test/unit/death.test.js (lines 120-205: the existing die/bury summary tests that must stay green unedited)
    - .planning/phases/65-run-record-personal-bests/65-CONTEXT.md ("Season" and "Integrity hash" bullets)
  </read_first>
  <behavior>
    - SEASON === 1 (imported from content/index.js)
    - For s = newRun(9001) driven through applyAction by [camp, camp, abandon], buildRunSummary(finalState, "abandon", 123) has season 1, seed 9001, acts 3, when 123, a hash matching /^[0-9a-f]{8}$/, and hash === runHash(summary)
    - hash is the run id: replaying seed 9001 with the same action list from a fresh newRun gives the same hash; buildRunSummary with when 999 gives the same hash; runHash({ ...summary, epitaph: "edited", note: "edited" }) === summary.hash; seed 9002 with the same actions gives a different hash
    - die(state, "trap", null, rng, [], () => 42) returns a summary whose hash equals bury(stateAfterDie, "trap", null, [], () => 42)[0].hash
    - buildRunSummary leaves its input state deepStrictEqual to a structuredClone taken before the call (no mutation, rngState untouched)
    - A hand-built state with no acts gives summary.acts === 0 and a valid hash (no throw)
    - test/unit/death.test.js passes unedited
  </behavior>
  <action>
1. Create content/season.js (pure data). It exports `SEASON = 1` and carries a changelog comment block: "Season changelog: bump SEASON by hand, adding one line here, whenever a balance change moves the depth curve (per CONTEXT: no rules string, no tie to the app version). 1: v2.0 Leaderboards (2026-09-23), the ledger opens." Append `export * from "./season.js";` as the last line of content/index.js.

2. engine/death.js:
   - Extend the content import to `import { EPITAPHS, CAUSE_TEXT, ROMAN, SEASON } from "../content/index.js";`.
   - Add `import { runHash } from "./records.js";`.
   - Change `function buildRunSummary` to `export function buildRunSummary`. Build the existing 15-field object exactly as today, then add `season: SEASON`, `seed: state.seed`, `acts: Number.isInteger(state.acts) && state.acts >= 0 ? state.acts : 0` as the trailing fields, then set `summary.hash = runHash(summary)` and return it.
   - Update its JSDoc: "Phase 65 (RUN-01): season, seed, acts and the FNV-1a integrity hash. The hash is the run's stable id: it excludes when, note and epitaph, so copy edits and the wall clock never change it. No rng."
   - die() and bury() keep calling it unchanged.

3. Create test/unit/run-summary.test.js covering every `<behavior>` bullet (RED first, then GREEN). Name the invariant tests with the phrase "hash is the run id".
  </action>
  <verify>
    <automated>node --test test/unit/run-summary.test.js test/unit/death.test.js test/determinism/content-is-pure-data.test.js test/unit/shell-no-content-copies.test.js test/unit/permadeath.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const SEASON = 1" content/season.js` finds the constant, and `grep -c "season.js" content/index.js` equals 1
    - `grep -n "export function buildRunSummary" engine/death.js` finds the export, and `grep -c "runHash(" engine/death.js` is at least 1
    - `node --test test/unit/run-summary.test.js` passes, including the "hash is the run id" tests; `node --test test/unit/death.test.js` passes with that file unedited
    - `npm test` reports 0 failures, and the parity suite stays green with `git diff --stat -- test/parity/fixtures/` empty
  </acceptance_criteria>
  <done>Every summary (die, bury, and later the bests record) carries season 1, the seed, the action count and a stable 8-hex id that ignores copy and wall-clock time.</done>
</task>

<task type="auto">
  <name>Task 2: retire the single best-depth key and restore the 60-stone graveyard, re-pinning the tests that encoded the old rules</name>
  <files>src/browser/engineAdapter.js, src/browser/storage.js, src/browser/settings.js, test/unit/engineAdapter.test.js, test/unit/new-run-loop.test.js</files>
  <read_first>
    - src/browser/engineAdapter.js (the whole file: the key constants at 43-87, getBest/recordBest at 203-242, persistGrave at 244-303, startNewRun at 305-343)
    - test/unit/engineAdapter.test.js (the imports at 31-40; the best-depth tests at 266-310 and 893-904; the E12 cap tests at 596-678)
    - test/unit/new-run-loop.test.js (the whole file, 70 lines)
    - src/browser/storage.js (header comments at lines 20-60 and 315 that name the retired functions; the LEGACY_KEYS code stays byte-identical)
    - src/browser/settings.js (line 23 comment)
  </read_first>
  <action>
Greenfield ruling (2026-09-17) and CONTEXT "Backfill and legacy keys (RUN-03)": no dual path; stop writing the old key outright.

1. src/browser/engineAdapter.js:
   - Delete the `BEST_KEY` constant and its comment block, the exported `getBest()`, the module-internal `recordBest()`, and the `if (currentState && !currentState.dev) { await recordBest(...) }` block at the top of `startNewRun`.
   - Rewrite startNewRun's JSDoc so it no longer describes recording a best depth. Keep the dev and storeRoll notes.
   - Replace the comment above the key constants with one that says ddr.best.v1 is retired in Phase 65: the value stays on disk untouched, is never read into the bests record, and storage.js's LEGACY_KEYS migration still copies it unchanged.
   - Fix the remaining comments that point at the removed functions (readRecentNames' doc, persistGrave's doc), describing the same fail-safe posture without naming the removed functions.
   <!-- planner-discipline-allow: getBest -->
   <!-- planner-discipline-allow: recordBest -->
2. Set `GRAVE_CAP = 60` (engineAdapter.js line ~86). Rewrite its comment: "Phase 65 (CONTEXT, RUN-02) deliberately reverses audit-batch E12 part 1. The mock's GRAVEYARD board lists everyone you have rolled and lost, deepest first, so the adapter stores up to 60 stones, matching engine/death.js#bury's own cap." Update the RECENT_NAMES comment (which says "capped SEPARATELY from the 5-grave cap") and persistGrave's "Part 1" comment to say 60.
3. src/browser/storage.js and src/browser/settings.js: comment-only edits that stop naming the removed adapter functions. The executable code in storage.js, including `BEST_KEY` and `LEGACY_KEYS`, stays byte-identical. Check with `git diff src/browser/storage.js`: only comment lines change.
4. test/unit/engineAdapter.test.js:
   - Remove `getBest` from the import list.
   - Delete the three best-depth tests: "startNewRun(seed) records the ending run's floor.depth ...", "startNewRun(seed) keeps the higher of two recorded bests", and "getBest() returns 0 when nothing is stored ...".
   - Delete "D-13: ending a dev run via startNewRun does not record its depth as best". Dev-run exclusion from the bests record is re-covered in Task 3.
   - Re-pin the two E12 cap tests to the new cap: the 7-death test asserts graves.length === 7, and the 30-death test asserts graves.length === 30. Retitle both to say "stores up to 60".
   - Add one test: 62 abandons produce graves.length === 60 while GRAVE_TOTAL_KEY reads 62.
   Never loosen an assertion; replace it with the new rule's exact value.
5. test/unit/new-run-loop.test.js:
   - Remove the `getBest` import and the "records the ended run's deepest floor as the best-depth" test.
   - Replace it with "startNewRun() never writes the retired best-depth key". The test pre-seeds `ddr.best.v1` = "7" in the fake store, ends a depth-12 run through startNewRun, and asserts the stored value is still "7". In a second fresh store, it asserts the key is still absent (null) after startNewRun.
  </action>
  <verify>
    <automated>node --test test/unit/engineAdapter.test.js test/unit/new-run-loop.test.js test/persistence/ test/unit/settings.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --input-type=module -e "import('./src/browser/engineAdapter.js').then(m=>process.exit(('getBest' in m)?1:0))"` exits 0 (the export is gone)
    - `grep -n "const GRAVE_CAP = 60" src/browser/engineAdapter.js` finds the constant
    - `git diff src/browser/storage.js` touches only comment lines, and `grep -n "const LEGACY_KEYS = \[RUN_SAVE_KEY, BEST_KEY, GRAVE_KEY\]" src/browser/storage.js` still matches
    - `node --test test/unit/engineAdapter.test.js test/unit/new-run-loop.test.js test/persistence/` passes, and the new 62-death test asserts graves.length 60 and total 62
  </acceptance_criteria>
  <done>The adapter no longer writes, reads or exports anything for the single best-depth key. The stored graveyard holds 60 stones. Every test that encoded the old rules states the new rule exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the ddr.bests.v1 record (boot load and backfill, synchronous fold at death, tracked persist) and the one-shot death report</name>
  <files>src/browser/engineAdapter.js, test/unit/bests-adapter.test.js</files>
  <read_first>
    - src/browser/engineAdapter.js (after Task 2: boot at ~362, dispatch at ~413-463 with the died-event choke point, persistGrave, initRun, takeBootWornReport and bootWornReport at 91-131 as the one-shot precedent, track/waitForPending at 133-168)
    - engine/records.js (emptyBests, sanitizeBests, updateBests, backfillBests, BOARD_IDS)
    - src/browser/storage.js (getItem returns null on any failure; setItem never rejects; per-key write queue)
    - test/unit/engineAdapter.test.js (withFakeLocalStorage and firstOpenPlainDir helpers to mirror; the blocked-storage test at ~813)
    - test/persistence/flush-drain.test.js (lines 145-200: how waitForPending is exercised)
    - .planning/phases/65-run-record-personal-bests/65-CONTEXT.md ("Load and write", "Backfill", "Where", "What counts", and the Claude's Discretion note on takeDeathRecord)
  </read_first>
  <behavior>
    - The FIRST test in the file, before any loadBests or boot (node --test runs each file in its own process, so module state is fresh), covers the lazy path. Pre-seed ddr.graveyard.v1 with 2 legacy stones, initRun(77), dispatch abandon. takeDeathRecord() is null. After await waitForPending() and flush(), the stored ddr.bests.v1 parses to a record with 3 runs (two season 0, one season 1), and its lineage counts sum to 3
    - boot backfill: the store holds 3 full legacy stones plus one `{ name, cause }`, ddr.best.v1 "7", ddr.graveyard.total.v1 "12", and a pre-Phase-65 run save with no acts. await boot(5) resolves; getState().acts === 0; getBests() has exactly 3 runs, all season 0 with no seed or acts own-property; after flush the stored ddr.bests.v1 deepStrictEquals getBests(); ddr.best.v1 is still "7"
    - boot with an existing valid ddr.bests.v1 loads it as sanitizeBests(stored). Graveyard stones are NOT re-folded (the lineage counts equal the stored ones)
    - boot with ddr.bests.v1 = "{not json" falls back to backfill from the graveyard, resolves, and never throws
    - First-ever death (fresh store, await loadBests(), startNewRun, abandon): takeDeathRecord() returns { first: true, newBests: [], summary } where summary has season 1, seed, acts, and an 8-hex hash. A second takeDeathRecord() call returns null
    - getBests() includes the new hash IMMEDIATELY after dispatch returns, before any await (the synchronous fold)
    - A deeper second death -> newBests includes "deep" and "lean". A shallower third death -> the report is { first: false, newBests: [] }
    - The stored graves[0] deepStrictEquals the stored record's runs[graves[0].hash] and carries season, seed, acts and hash
    - acts through dispatch: startNewRun, then 2 invalid actions, then 3 camp actions, then abandon -> the stone's acts === 4
    - A dev run (startNewRun(seed, { startDepth: 20 })) dying: the stored ddr.bests.v1 is unchanged, getBests() is unchanged, takeDeathRecord() is null
    - 62 deaths: the first run's floor.depth is set to 9 before abandon and the other 61 die on floor 1. The graveyard holds 60 stones and none of them has the first run's hash, yet the stored record's boards.deep[0] is that hash
    - Back-to-back deaths: await startNewRun(a); dispatch abandon; await startNewRun(b); dispatch abandon, with no flush in between. After waitForPending and flush, the stored record contains both hashes
    - After a death, await waitForPending() alone (no storage flush) leaves ddr.bests.v1 written (the write is tracked)
    - Blocked storage (getItem/setItem throw): await boot(3) resolves; dispatch abandon does not throw; getState().dead === true with the same character name (it did not fall closed to a fresh run); takeDeathRecord() returns a report
    - initRun/startNewRun clears a pending report: death, then startNewRun, then takeDeathRecord() returns null
    - Source scan: the comment-stripped engineAdapter.js contains no ddr.best.v1 literal and no identifier for the removed best-depth reader or writer. It has no raw localStorage calls and no Math.random or document (the existing guards still pass)
    - The shared board table: Object.keys(BOARD_COPY) (content/boards.js) deepStrictEquals [...BOARD_IDS] (engine/records.js)
  </behavior>
  <action>
In src/browser/engineAdapter.js (CONTEXT "Load and write" and "Backfill"; the discretion choice is the one-shot `takeDeathRecord()`, mirroring `takeBootWornReport()`):

1. Imports:
   - `import { bury, buildRunSummary } from "../../engine/death.js";` (extend the existing line)
   - `import { emptyBests, sanitizeBests, updateBests, backfillBests } from "../../engine/records.js";`

2. Constant: `const BESTS_KEY = "ddr.bests.v1";`. Comment: adapter-owned cross-run data, never GameState; the all-time, season-tagged personal-bests record (Phase 65, RUN-02).

3. Module state, next to bootWornReport:
   - `let bests = null;`: the in-memory record, null until loaded this session.
   - `let deathRecord = null;`: the one-shot report. Adapter-side, never serialized.

4. `export async function loadBests()`. It never rejects: wrap the body in try/catch, set `bests = emptyBests()` on error, and return bests.
   - Read BESTS_KEY. If the value is a non-null string and JSON.parse succeeds, set `bests = sanitizeBests(parsed)` and return it.
   - Otherwise (absent, or a parse failure), read GRAVE_KEY, parse it (a non-array or corrupt value becomes []), set `bests = backfillBests(graves)`, and enqueue the write with `track(storage.setItem(BESTS_KEY, JSON.stringify(bests)))` (not awaited). Return bests.

5. `export function getBests()` returns `bests`.

6. `export function takeDeathRecord()`: read `deathRecord`, set it to null, return what was read. Its JSDoc mirrors takeBootWornReport's: one-shot, consumed on read, never serialized, cleared by initRun.

7. `initRun`: set `deathRecord = null;` before creating the run.

8. `boot`: add `await loadBests();` right after `await storage.migrateLegacyKeys();`. The backfill then reads the migrated graveyard, and the record is in memory before boot resolves.

9. A module-private `recordDeath(state, cause, when)`. It is synchronous and never throws, so a failure here can never reach dispatch's fail-closed catch and replace the dead run with a fresh one. Inside try/catch:
   - `const summary = buildRunSummary(state, cause, when);`
   - if `bests` is non-null: `const r = updateBests(bests, summary); bests = r.record; deathRecord = { first: r.first, newBests: r.newBests, summary };` and return `{ summary, bestsJson: JSON.stringify(bests) }`;
   - else: `deathRecord = null;` and return `{ summary, bestsJson: null }`.
   - On any error: `deathRecord = null;` and return `{ summary: null, bestsJson: null }`.

10. dispatch's died-event block becomes:
    - `if (diedEvent) { if (currentState.dev) { deathRecord = null; } else { const when = typeof currentState.deathAt === "number" ? currentState.deathAt : Date.now(); const { summary, bestsJson } = recordDeath(currentState, diedEvent.cause, when); track(persistGrave(currentState, diedEvent.cause, when, summary, bestsJson)); } }`
    - Keep the existing CR-01/CR-02/D-13 comments and add a Phase 65 note: the fold is synchronous (so the death panel reads it at once), and the write rides the same tracked persist.

11. persistGrave(state, cause, when, summary, bestsJson). Inside the existing try:
    - First, if `summary && bestsJson === null`, this is the lazy path: the record was not loaded yet this session. `await loadBests();` then `const r = updateBests(bests, summary); bests = r.record; bestsJson = JSON.stringify(bests);`. This never writes a fresh empty record over a stored one.
    - Then the existing three reads, and `bury(state, cause, null, prevGraves, () => when).slice(0, GRAVE_CAP)`. It is the same pure buildRunSummary with the same `when`, so the stone equals the bests entry.
    - Then enqueue all writes back-to-back in one `Promise.all`: the three existing setItem calls plus `storage.setItem(BESTS_KEY, bestsJson)` when bestsJson is non-null.
    Update the JSDoc to describe the four keys.

12. Create test/unit/bests-adapter.test.js covering every `<behavior>` bullet, with the lazy-path test FIRST in the file. Every other test starts inside withFakeLocalStorage by calling `await loadBests()` (or boot) to reset the in-memory record. Use `flush` from storage.js and `waitForPending` from the adapter.
  </action>
  <verify>
    <automated>node --test test/unit/bests-adapter.test.js test/unit/engineAdapter.test.js test/persistence/ && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const BESTS_KEY = \"ddr.bests.v1\"" src/browser/engineAdapter.js` finds the constant
    - `grep -c "export function takeDeathRecord\|export async function loadBests\|export function getBests" src/browser/engineAdapter.js` equals 3
    - `grep -n "await loadBests()" src/browser/engineAdapter.js` shows it inside boot, after migrateLegacyKeys
    - `node --test test/unit/bests-adapter.test.js` passes with at least 16 tests, the lazy-path test declared first
    - `npm test` reports 0 failures, including test/persistence/dual-write-convergence.test.js's "no raw localStorage" guard and engineAdapter.test.js's "no Math.random or document" guard
  </acceptance_criteria>
  <done>Deaths fold into a durable, season-tagged, all-time ddr.bests.v1 record that boots before first render, backfills honestly, survives the 60-stone trim, ignores dev runs, never throws into the death flow, and hands the death panel a one-shot report.</done>
</task>

</tasks>

<verification>
- `npm test` fully green
- `git diff --stat -- test/parity/fixtures/ test/parity/prototype-master.js.txt` empty (the summary fields never reach a comparable, since graveyard entries are not compared)
- `git diff src/browser/storage.js` is comment-only
</verification>

<success_criteria>
- RUN-01: the summary carries season, seed, acts and hash, with zero new rng draws
- RUN-02: ddr.bests.v1 is durable through mzStorage, season-tagged, all-time, and survives the 60-stone trim
- RUN-03: legacy keys, old graveyards and old saves load cleanly, and the record backfills from the existing stones
- RUN-04: the death panel can read a one-shot new-best report synchronously
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| content/season.js, `SEASON` (= 1) | content constant | re-exported via content/index.js |
| `buildRunSummary(state, cause, when)` | now exported | engine/death.js |
| summary `season`, `seed`, `acts`, `hash` | RunSummary fields | on die() returns, graveyard stones and bests runs |
| `ddr.bests.v1` (`BESTS_KEY`) | storage key | mzStorage (Preferences + localStorage mirror) |
| `loadBests()`, `getBests()`, `takeDeathRecord()` | adapter exports | src/browser/engineAdapter.js |
| `bests`, `deathRecord`, `recordDeath()` | adapter module-private | never serialized |
| `GRAVE_CAP` = 60 | adapter constant | was 5 |
| removed: the single best-depth key constant, its reader export and its writer | retired | greenfield ruling |
| test/unit/run-summary.test.js, test/unit/bests-adapter.test.js | new test files | |

<output>
Create `.planning/phases/65-run-record-personal-bests/65-04-SUMMARY.md` when done. Include the final adapter export list and the report shape. Include a "Human verification (deferred to end of run)" heading listing these checks for the Phase 69 Pixel 7 batch:
(1) install this build OVER an existing install that has graves: the app boots, the Dead tab still lists stones, and there is no crash (backfill);
(2) die twice: the Dead tab count rises, and the app restarts cleanly;
(3) with storage cleared in Android settings, a first death works without error.
</output>

## Must haves: decision coverage (65-CONTEXT.md)

- D-01 (SEASON = 1 in content/season.js), D-03 (hash on the summary), D-05 (GRAVE_CAP 60), D-07 (ddr.bests.v1 loaded at boot, updated at persistGrave), D-08 (backfill wiring), D-09 (ddr.best.v1 retired), D-10 (graveyard total unchanged) and D-11 (tolerant old-save load) are implemented by this plan.
