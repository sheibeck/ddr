---
phase: 71-device-round-polish-ii
plan: 03
type: execute
wave: 2
depends_on: ["71-01", "71-02"]
files_modified:
  - src/browser/foeConditions.js
  - test/unit/foe-conditions.test.js
  - src/browser/combatMenu.js
  - test/unit/combatMenu.test.js
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/shell-spells-40.test.js
  - test/unit/shell-combat-screen.test.js
  - test/unit/combat-lock-shell.test.js
  - test/unit/hp-not-wp.test.js
autonomous: true
requirements: [POLISH-09, POLISH-07]
tags: [vanilla-js, presentation-only, combat, conditions, input-lock, milestone-v2.0]

must_haves:
  truths:
    - "Every condition an ability, spell or item puts on a foe shows as a chip on that live foe while it lasts, from ONE pure table in src/browser/foeConditions.js: Hamstrung and Marked (missing today), Stunned, Blind (with Dirty Trick's rounds), Asleep, Frozen, Acid, Poison/Ice, Stupefied, Shrunk, Fixated, Frenzied, and the combat-wide Weakened (D-14)"
    - "Timed conditions show their rounds ('Blind · 2', 'Acid · 3', 'Weakened · 2'); foe debuffs use tone good, Frenzied (a foe buff) tone bad; chip order is the table order and stable (D-14)"
    - "A coverage test scans engine/abilities.js, engine/magic.js, engine/combat.js, engine/foeAbilities.js and engine/items.js for every assignment to a foe field and every combat-wide flag; each field is either a table entry or on an explicit test-owned not-a-condition list with a one-line reason, and a self-check proves the scan finds hamstrung, marked, stunned and blindFor, so a new foe effect with no chip fails the build (D-14)"
    - "foeStatusBadges reads the table through the bridge and chips during a beat read the beat's frame state, so a chip and the foe's HP move with the same line (D-14)"
    - "While a round's beats play, every combat action in #cb-act renders visibly unavailable: data-locked, dimmed, no raised shadow, aria-disabled that the 250 ms arm sweep never strips, no press feedback (pointer-events none); the prompt reads the mock's 'HOLD · THE DICE ARE STILL OUT'; the moment the round settles the settle render draws them live again (D-05)"
    - "A tap during playback, including one on a locked action, lands the round's result through Phase 58's single beatHurryTap and never acts; no tap is queued or replayed after the settle; a tap one frame before the settle only skips, and a fresh tap ARM_DELAY_MS after the settle acts exactly once (D-05, D-06)"
    - "Foe cards are never dimmed during playback — they show the fight; opening the ☰ or switching tabs still lands the round first (Phase 70 R-C, unchanged) (D-06)"
  artifacts:
    - path: "src/browser/foeConditions.js"
      provides: "FOE_CONDITIONS, FOE_CONDITION_COPY, foeConditionChips(foe, state)"
      exports: ["FOE_CONDITIONS", "FOE_CONDITION_COPY", "foeConditionChips"]
    - path: "test/unit/foe-conditions.test.js"
      provides: "per-condition chip pins and the engine-scan coverage guard"
    - path: "src/browser/combatMenu.js"
      provides: "combatMenuViewModel(state, { locked }) -> prompt swaps to the resolving line and every action carries locked"
      contains: "resolving"
    - path: "mazeworld.html"
      provides: "foeStatusBadges as a thin reader of window.__mzFoeConditions; the #cb-act data-locked lock with its CSS and the arm-sweep exclusion"
      contains: "__mzFoeConditions"
    - path: "test/unit/combat-lock-shell.test.js"
      provides: "the lock, skip and no-replay proof against a real resolved round in the shell sandbox"
  key_links:
    - from: "mazeworld.html foeStatusBadges(f, V)"
      to: "src/browser/foeConditions.js foeConditionChips"
      via: "window.__mzFoeConditions.chips"
      pattern: "__mzFoeConditions"
    - from: "mazeworld.html renderActionArea"
      to: "window.__mzCombatVM.menu(V, { locked })"
      via: "locked = !!window.__mzBeat?.active?.()"
      pattern: "locked"
    - from: "mazeworld.html armEncounterButtons sweep"
      to: "locked actions keep aria-disabled"
      via: "the sweep selector skips [data-locked]"
      pattern: "data-locked"
---

<objective>
Make every enemy condition visible, and stop the combat actions from looking live while a round is still playing (POLISH-09 via D-14; the input-lock half of POLISH-07 via D-05 and D-06).

- D-14: "the hamstring ability doesn't show up on enemies as a condition chit." `foeStatusBadges(f)` is a hand-written list that misses Hamstrung and Marked, and likely Pommel's Stunned and Dirty Trick's timed blindness. Move the mapping into ONE pure table that the chips read, plus a coverage test that fails when the engine gains a foe effect with no chip. The long-press card in 71-04 reads the same table.
- D-05: while a round's beats play, the actions look unavailable and a tap never queues.
- D-06: a tap during playback reveals the result and never acts.

Source todos: D-14 has no todo file (it comes from the user's report in 71-CONTEXT D-14). This plan closes item 1 (the input lock) of `.planning/todos/pending/2026-09-24-combat-lock-actions-while-a-round-plays-and-keep-the-round-summary-visible.md`. Its item 2 (the round-summary band, D-07) is 71-05's, so the SUMMARY says "partially closes — item 1 of 2", and the orchestrator moves that todo only after 71-05.

Output: src/browser/foeConditions.js and its coverage guard, the shell reading it, the resolving prompt and lock in the combat menu and shell, and the lock/skip/no-replay proof.

Planner rulings (record in the SUMMARY):
- R-08, D-06 already exists: Phase 58's beatHurryTap is a capture-phase click listener on #enc-panel that stops propagation and hurries a live beat, and Phase 70 R-C hurries on ☰ open or tab switch.
  - This plan adds NO second listener. CSCR-08 pins exactly one capture click listener on #enc-panel.
  - It pins the skip for a tap on a LOCKED action. Locked buttons take `pointer-events:none`, so the tap lands on the action area inside #enc-panel and beatHurryTap still catches it.
  - No conflict with Phase 63's combat lock, which is the Gear sheet's equip refusal mid-fight, a different surface.
- R-09, what locks: only the action area (#cb-act: the grid, a submenu's rows and its BACK chip). Foe cards, YOUR LOT and the log keep their look, because they are the fight being shown. A tap on them still skips (R-08).
  - The Phase 32 invisible 250 ms arm window after the settle stays as the ghost-tap guard. It swallows, never queues. "Re-enable the moment the round settles" is the settle render drawing the actions live.
- R-10, the busy prompt: while locked, the prompt above the grid reads the user's combat v2 mock line, "HOLD · THE DICE ARE STILL OUT", in the mock's dim colour (#8f856f). It is new COMBAT_MENU_COPY, voice-scanned. The prompt's position and size do not change; the round-summary strip (D-07) and its RESOLVING label are 71-05's.
- R-11, the coverage guard reads the engine and never edits it. D-14 allows "scan the engine's assignments, or keep an explicit pinned list next to the apply* functions". The engine is untouched in this phase, so the guard scans, with a test-owned not-a-condition list.
- R-12, Turn Undead and Gate set `turned` together with alive = false. Such a foe is out of the fight and its card reads DOWN, so `turned` goes on the not-a-condition list with that reason (chips render only for live foes).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-device-round-polish-ii/71-CONTEXT.md
@.planning/todos/pending/2026-09-24-combat-lock-actions-while-a-round-plays-and-keep-the-round-summary-visible.md
@.claude/CLAUDE.md
@src/browser/combatMenu.js
@src/browser/combatPanel.js
@design/COMBAT-V2-NOTES.md

<interfaces>
Post-71-01 master. Line numbers are approximate; grep each anchor.

Engine foe-field writes (read-only here):
- engine/abilities.js:
  - applyPommel `t.stunned = true`;
  - Dirty Trick `t.blind = true; t.blindFor = 2`;
  - applyHamstring `t.hamstrung = true`;
  - applyMark `t.marked = true`;
  - Poisoned Edge `t.dot = {…}`;
  - the combat-wide `C.braced`, `C.abilityStrike`.
- engine/magic.js:
  - `t.asleep`, `t.frozen`, `t.acid = { rounds }`, `t.dot = { left, by: "ice" }`, `t.blind`, `t.stupid`, `t.frenzied`;
  - `f.shrunk`, `f.fixated`, and `f.turned` together with alive = false;
  - the combat-wide `C.weakened` + `C.foeToHitPenalty` (Weaken; the duration lives on the hero's `c.timers["spell:weaken"].left`).
- engine/combat.js:
  - `f.asleep` (a countdown, decremented each foe turn), `f.frozen`, `f.stunned = false` (consumed), the `f.blindFor` countdown (deleted at 0), `f.lives`, `f.fled`;
  - combat-wide `C.afraid` (the HERO's fear), `C.inspired`, `C.ally/allies`, `C.first`, `C.cut`, `C.opened`, `C.parleyTried/Insulted`, `C.pendingFoes`.
- engine/foeAbilities.js: summoned foes, built with the same shape.
- engine/items.js: `combat.weakened`.

mazeworld.html:
- foeStatusBadges(f) (~3621): today's hand-written list of Asleep, Frozen, Acid · n, Blind, Stupefied, Shrunk, Fixated, Frenzied (bad), Weakened [· n] and Ice/Poison · n. It reads the live `S` for the combat-wide flag, even while a beat shows its frame.
- The combat branch (~4509): `chipsFor: (f) => foeStatusBadges(f).map((b) => b.t)`. shell-combat-screen CSCR-02/08 pins that callback string exactly once.
- renderActionArea(host) (~4042): `const vm = window.__mzCombatVM.menu(window.__mzBeat?.view?.()?.state || S);` then either the submenu (#cb-submenu, #cb-back, the .cb-row list via cbRow) or the prompt (.cb-prompt), the #cb-grid and four .cb-btn. Each is wired with guardTap.
- guardTap (~3576) sets aria-disabled="true" and an onclick gated on encArmed().
- encArmed (~3534) returns false while window.__mzBeat.active().
- armEncounterButtons (~3550) starts a 250 ms timer that removes aria-disabled from `#enc-panel [aria-disabled="true"]:not(.cb-foe.dead), #mw-rail …, .mw-legend-sheet … :not([data-off])`. So today a live beat's buttons lose aria-disabled between lines.
- beatHurryTap (~4938) plus its one capture listener on #enc-panel (~4944).
- The module's `window.__mzCombatVM = { header, foes, lot, overlay, menu }` (~5992).
- The combat CSS (~763-783): .cb-act, .cb-prompt, .cb-grid, .cb-btn(-off), .cb-chip, .cb-row(-off).

Pins to respect (read before editing):
- shell-combat-screen.test.js:
  - CSCR-01 ordering;
  - CSCR-02 "guardTap(el, () => onPick( exactly once";
  - the chipsFor pin;
  - CSCR-08: no card/body/panel onclick or addEventListener in the renderFightLog→noteCombat region, no aria-disabled token anywhere in <style> (six other shell tests also forbid it), no transitionend/animationend, and exactly one capture click listener on #enc-panel, beatHurryTap, whose body never dispatches, renders or touches S.
- combat-beat-shell.test.js (4)(5), which proves the existing swallow and hurry.
- shell-spells-40.test.js (~130), which slices foeStatusBadges' body for spell:weaken and f.dot.
- reduced-motion.test.js: the paint()/draw() SHA-256 pins. Never edit paint() or draw().
- bridge-registry: a new __mz name needs a BRIDGE entry in the same commit, then tools/bridge-doc.mjs --write and --check.
- shell-no-content-copies criterion 3: the shell declares no name any module exports.
- test/unit/harness/shellSandbox.js wires the module bridges for the sandbox (__mzCombatVM ~256, __mzBeat ~275). It has recordingDom, and a fakeClock via loadShellSandbox({ reducedMotion: false, clock }).
- combat-beat-shell.test.js has midFightRound()/driveHandoff() helpers to copy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: foeConditions.js — one chip table for every foe condition, with an engine-scan coverage guard (D-14)</name>
  <files>src/browser/foeConditions.js, test/unit/foe-conditions.test.js, test/unit/hp-not-wp.test.js</files>
  <behavior>
    - FOE_CONDITIONS is a frozen, ordered table. Each entry is { key, label (from FOE_CONDITION_COPY), tone, when(foe, state), rounds(foe, state) → integer or null }. It covers:
      - Stunned (stunned);
      - Blind (blind || blindFor > 0), with rounds = blindFor when > 0;
      - Hamstrung (hamstrung);
      - Marked (marked);
      - Asleep (asleep > 0), with rounds = asleep;
      - Frozen (frozen);
      - Acid (acid.rounds > 0), with rounds;
      - Poison or Ice (dot.left > 0), labelled Ice when dot.by === "ice", with rounds = left;
      - Stupefied (stupid);
      - Shrunk (shrunk);
      - Fixated (fixated);
      - Frenzied (frenzied, tone bad);
      - Weakened (state.combat.weakened), with rounds from state.c.timers["spell:weaken"].left when > 0, else the bare label, as today.
      Every foe debuff is tone "good"; Frenzied is "bad".
    - foeConditionChips(foe, state) returns a frozen array of frozen { key, label, tone, rounds, text } in table order, where text is `label · rounds` when rounds is set and label otherwise.
      - A dead or non-object foe, or null/malformed state, returns [] or the foe-only chips, and never throws (a getter that throws counts).
      - Calling it twice gives deep-equal results.
    - The coverage guard reads, as text, engine/abilities.js, engine/magic.js, engine/combat.js, engine/foeAbilities.js and engine/items.js, and collects:
      - (a) every `X.field` assignment (`=`, `+=`, `-=`, `++`, `--`, `delete`) where X is a foe binding: t, f, foe, target or tgt;
      - (b) every combat-wide assignment on C., combat. or state.combat.
      Each collected field must be a FOE_CONDITIONS key (or a field read by one, such as blindFor, acid, dot and weakened), or appear on the test-owned NOT_A_CONDITION map with a one-line reason: wp, maxWP, alive, fled, lives, turned (R-12), foeToHitPenalty (paired with Weakened), afraid (the hero's fear), braced, inspired, abilityStrike, ally, allies, first, target, round, cut, opened, spellOpen, parleyTried, parleyInsulted, pendingFoes, plus anything the scan truthfully finds that is not a foe condition, each with its reason.
    - A self-check asserts the scan finds hamstrung, marked, stunned, blindFor and weakened, so a regex that goes blind fails. A synthetic source string with `t.newHex = true` is reported as uncovered.
    - FOE_CONDITION_COPY is frozen. Its leaves pass PLAYER_WP (registered in hp-not-wp.test.js's banks) and BANNED.
  </behavior>
  <action>
RED first: create test/unit/foe-conditions.test.js with:
- one case per condition, built from real engine shapes where cheap. Call engine/abilities.js's apply* functions and engine/magic.js's casts on a fixed combat state when they are exported and pure enough; otherwise set the field exactly as the engine writes it, citing the line.
- the rounds cases, including blindFor 1 → "Blind · 1", a blindFor that has been deleted → "Blind" when blind is still true, and Weakened with and without the hero's timer;
- the tone and order cases, and the malformed and hostile inputs;
- the coverage guard, its self-check and its synthetic-miss check.
Register FOE_CONDITION_COPY in hp-not-wp.test.js. Run and see it fail. Commit as test(71-03).

GREEN, per D-14 and R-11/R-12: create src/browser/foeConditions.js, pure with no DOM, window or timers. It imports nothing from engine/ that draws rng; reading `state.c.timers` is plain data. Its header comment names:
- D-14 and the user's report;
- the one-table rule: the combat chips and 71-04's long-press card both read it;
- the coverage guard;
- the house label style.
The labels are Stunned, Blind, Hamstrung, Marked, Asleep, Frozen, Acid, Poison, Ice, Stupefied, Shrunk, Fixated, Frenzied and Weakened. Run green. Commit as feat(71-03): one chip table for every foe condition.
  </action>
  <verify>
    <automated>node --test test/unit/foe-conditions.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <done>foeConditions.js covers every foe-affecting field the engine sets, including Hamstrung, Marked, Stunned and Dirty Trick's timed Blind. The engine-scan guard fails on any uncovered new field and proves it still sees the known ones. The copy passes both scans, and the listed tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The shell's chips read the table, and the combat actions lock visibly while a round plays (D-14, D-05)</name>
  <files>mazeworld.html, src/browser/combatMenu.js, test/unit/combatMenu.test.js, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/shell-spells-40.test.js, test/unit/shell-combat-screen.test.js</files>
  <behavior>
    - Chips:
      - window.__mzFoeConditions = { chips: foeConditionChips }, assigned in the module next to __mzCombatVM.
      - foeStatusBadges(f, V) keeps its name and { t, tone } return shape as a thin reader: `window.__mzFoeConditions?.chips?.(f, V || S)` mapped to { t: text, tone }, and [] without the bridge.
      - The combat branch's callback becomes `chipsFor: (f) => foeStatusBadges(f, V).map((b) => b.t)`, still exactly once, so a beat frame's chips read the frame state.
      - A Hamstrung, Marked or Stunned foe shows its chip on its card in the sandbox.
    - combatMenuViewModel(state, opts):
      - with opts.locked === true, prompt is COMBAT_MENU_COPY.resolving and every action and submenu row carries locked: true;
      - with no opts, or locked falsy, the output is deep-equal to today's, so every existing combatMenu pin still passes.
      - The resolving copy is a new COMBAT_MENU_COPY leaf, exactly the mock's busy prompt "HOLD · THE DICE ARE STILL OUT" (design/COMBAT-V2-NOTES.md, section 4). It is voice-scanned by combatMenu.test.js and walked by hp-not-wp through COMBAT_MENU_COPY.
    - renderActionArea:
      - computes `const locked = !!window.__mzBeat?.active?.();` and passes { locked } to the menu VM;
      - when locked, sets #cb-act's data-locked="1" and, on every .cb-btn, .cb-row and #cb-back it builds, data-locked="1" plus aria-disabled="true";
      - when not locked, sets neither.
      guardTap wiring is unchanged, and encArmed still swallows the tap.
    - armEncounterButtons: the #enc-panel part of the sweep selector gains :not([data-locked]), so a locked action keeps aria-disabled between beat lines. The #mw-rail and .mw-legend-sheet parts are unchanged, and the selector still contains no #mw-hud-menu.
    - CSS, under a comment naming Phase 71 D-05:
      - `#cb-act[data-locked="1"]` dims the prompt (the mock's #8f856f) and the actions (opacity about .45). It drops the buttons' raised shadow (box-shadow none, per the mock's busy state) and gives the locked buttons `pointer-events:none` plus a neutralised :active (no transform), so there is no press feedback;
      - no aria-disabled token in <style> and no transition or animation;
      - the prompt keeps its position and size (R-10: text only).
  </behavior>
  <action>
RED first:
- combatMenu.test.js: locked prompt and flags; unlocked deep-equal to today's; the new copy's voice scan.
- shell-spells-40.test.js: re-point the foeStatusBadges body slice. The spell:weaken and f.dot reads now live in foeConditions.js, so pin that the shell function reads window.__mzFoeConditions and that foeConditions.js reads `spell:weaken` and `dot`.
- shell-combat-screen.test.js: the chipsFor pin becomes `foeStatusBadges(f, V)`, exactly once.
- Add source pins in shell-combat-screen.test.js:
  - renderActionArea computes locked from __mzBeat.active and passes { locked };
  - the sweep selector carries :not([data-locked]);
  - a `#cb-act[data-locked="1"]` rule exists with pointer-events:none;
  - CSCR-08 still holds.
Run and see them fail. Commit as test(71-03).

GREEN, per D-14, D-05 and R-09/R-10:
1. src/browser/combatMenu.js: add the resolving copy and the optional { locked } parameter, and document it in the JSDoc.
2. mazeworld.html:
   - Module: import foeConditionChips and assign window.__mzFoeConditions.
   - Classic: rewrite foeStatusBadges as the thin reader. Keep its header comment's history, and add the D-14 note: one table, src/browser/foeConditions.js, which the long-press card also reads.
   - Update the chipsFor call.
   - Change renderActionArea and the sweep selector as in behavior, with a comment naming D-05 and R-09.
   - Add the CSS.
   - Do not touch paint(), draw(), beatHurryTap or its listener.
3. src/browser/bridge.js: add __mzFoeConditions { owner: "mazeworld.html (module)", consumers: the classic foeStatusBadges, purpose: the one foe-condition chip table (Phase 71 D-14) }, in alphabetical order. Run `node tools/bridge-doc.mjs --write` then `--check`, and save docs/SHELL-MODULES.md with LF if --check complains only about CRLF. Add a D-14 sentence to the combat section of docs/SHELL-MODULES.md.
4. test/unit/harness/shellSandbox.js: wire `w.__mzFoeConditions = { chips: foeConditionChips }` beside __mzCombatVM.
Run the Task 2 files plus combat-beat-shell, shell-input-guards, shell-dead-foe-target, bridge-registry, shell-no-content-copies, reduced-motion, hud-menu-layout and hp-not-wp green. Commit as feat(71-03): chips from the table, and the combat actions lock while a round plays.
  </action>
  <verify>
    <automated>node --test test/unit/combatMenu.test.js test/unit/shell-spells-40.test.js test/unit/shell-combat-screen.test.js test/unit/combat-beat-shell.test.js test/unit/shell-input-guards.test.js test/unit/shell-dead-foe-target.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/reduced-motion.test.js test/unit/hud-menu-layout.test.js test/unit/hp-not-wp.test.js test/unit/combatPanel.test.js && node tools/bridge-doc.mjs --check</automated>
  </verify>
  <done>Foe chips come from foeConditions.js through the bridge and read the beat's frame state. The action area is data-locked, dimmed, aria-disabled (never stripped mid-beat) and pointer-inert while a beat plays, with the resolving prompt. Unlocked output is unchanged. Every listed test passes, and bridge-doc --check exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The lock, skip and no-replay proof on a real resolved round (D-05, D-06)</name>
  <files>test/unit/combat-lock-shell.test.js</files>
  <behavior>
    Built with the shell sandbox (reducedMotion false, a fake clock), using combat-beat-shell's own midFightRound/driveHandoff pattern (copied, not imported):
    - (1) At the first beat render, #cb-act has data-locked "1". #cb-strike, #cb-spells, #cb-items and #cb-social each carry data-locked and aria-disabled "true". The prompt text equals COMBAT_MENU_COPY.resolving.
    - (2) After advancing the clock past ARM_DELAY_MS but still mid-beat, the locked buttons still carry aria-disabled "true". This proves the sweep exclusion.
    - (3) Calling beatHurryTap with a fake event whose target is #cb-strike stops propagation, ends the beat and never calls window.mzAttack.
    - (4) No replay: across the settle and past ARM_DELAY_MS, with the clock advanced a further 2 s, mzAttack is still never called.
    - (5) After the settle, #cb-act has no data-locked, the prompt is back to COMBAT_MENU_COPY.prompt, and the buttons carry no data-locked. A tap within ARM_DELAY_MS of the settle render is swallowed. A fresh tap after ARM_DELAY_MS calls mzAttack exactly once.
    - (6) One step either side of the settle, with a second run of the scenario: a direct #cb-strike onclick() one clock frame before the beat ends calls nothing, and the settle then shows the result with nothing replayed.
    - (7) A foe card is never data-locked or dimmed during the beat.
    - (8) window.__mzShowTab("oracle") mid-beat still ends the beat (Phase 70 R-C), and the next render is unlocked.
  </behavior>
  <action>
Write test/unit/combat-lock-shell.test.js to the behavior above (the RED step). If every case passes on the Task 2 code, commit as test(71-03): lock, skip and no-replay proof (D-05, D-06). That proof is the point, and it is also D-05's "a test pins that" for no queued or replayed taps.
If any case fails, fix the shell code inside Task 2's files within the D-05/D-06 contract and rerun. Never add a second capture click listener on #enc-panel (R-08), and never touch beatHurryTap's body. Record any such fix as a deviation in the SUMMARY.
Then run `npm test`: 0 failures on master, and in a worktree only the 7 known CRLF doc-ledger failures.
  </action>
  <verify>
    <automated>node --test test/unit/combat-lock-shell.test.js test/unit/combat-beat-shell.test.js test/unit/shell-combat-screen.test.js</automated>
    <human-check>Deferred to the Phase 71 device round (folded into docs/UAT-v2.0.md section M by 71-06):
      1. In a multi-foe fight, press STRIKE and watch the round type out. The four action buttons go dim at once, lose their raised shadow, don't depress when tapped, and the prompt reads "HOLD · THE DICE ARE STILL OUT". They come back the moment the round finishes.
      2. Spam STRIKE while a round types. The first tap lands the whole round at once, and no extra strike happens afterwards.
      3. Tap a foe card or the log while a round types. The round lands at once and the aim does not change.
      4. Use Hamstring, Mark, Pommel Strike and Dirty Trick on a foe. Each shows its chip on that foe's card (Blind with its rounds), and each disappears when it wears off. Weaken still shows Weakened with its rounds on every foe.
      5. Open the ☰ mid-round: the round lands first, as before.</human-check>
  </verify>
  <done>The sandbox proof shows the lock, the skip on a locked tap, no queued or replayed tap across the settle, the un-lock on the settle, and unlocked foe cards. The full suite is green (worktree: the 7 CRLF failures only).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player tap → combat dispatch | A tap during a half-played round must never act on stale state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-05 | Tampering (of the run by a stray tap) | the action area during a beat | medium | mitigate | encArmed() stays false for the whole beat; locked buttons are pointer-inert so the tap reaches beatHurryTap, which stops propagation; combat-lock-shell (3)(4)(6) prove no act and no replay |
| T-71-06 | Repudiation (a silent missing chip) | foe condition display | low | mitigate | the engine-scan coverage guard fails the build on any foe field with no chip and no reasoned exclusion |
</threat_model>

<verification>
- All three tasks' verify commands pass. `npm test` has 0 failures (in a worktree, only the 7 known CRLF doc-ledger failures). bridge-doc --check exits 0.
- There is no engine/, content/ or test/parity/ change, and paint()/draw() are byte-identical (reduced-motion SHA-256 pins).
- #enc-panel still carries exactly one capture click listener (CSCR-08).
</verification>

<success_criteria>
- D-14: every ability, spell and item condition on a foe shows as a chip from one table, and a scan guard fails on a new uncovered effect.
- D-05: while a round plays, the actions are visibly unavailable, taps never queue, and they re-enable at the settle.
- D-06: a tap during playback, including on a locked action, lands the result and never acts.
</success_criteria>

<output>
Create `.planning/phases/71-device-round-polish-ii/71-03-SUMMARY.md` when done. It states:
- that D-14 has no todo file, and that it partially closes the combat-lock todo (item 1 of 2; 71-05 closes it);
- the final FOE_CONDITIONS table and the NOT_A_CONDITION list with reasons;
- rulings R-08..R-12;
- a "Human verification (deferred)" section listing the five checks above.
</output>
