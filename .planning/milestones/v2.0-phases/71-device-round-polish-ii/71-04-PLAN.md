---
phase: 71-device-round-polish-ii
plan: 04
type: execute
wave: 3
depends_on: ["71-03"]
files_modified:
  - src/browser/foeDetails.js
  - test/unit/foeDetails.test.js
  - src/browser/longPress.js
  - test/unit/longPress.test.js
  - src/browser/combatPanel.js
  - test/unit/combatPanel.test.js
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/foe-inspect-shell.test.js
  - test/unit/shell-combat-screen.test.js
  - test/unit/hud-menu-layout.test.js
  - test/unit/hp-not-wp.test.js
autonomous: true
requirements: [POLISH-08]
tags: [vanilla-js, presentation-only, combat, rail, gesture, accessibility, milestone-v2.0]

must_haves:
  truths:
    - "Holding a foe card for HOLD_MS (450 ms, tapStep.js's own hold constant) with no more than TAP_MAX_TRAVEL_PX (10 px) of travel raises one rail card for that foe; exactly 450 ms fires, 449 does not; 10 px of travel still counts, 11 px cancels; a second finger, a pointercancel or a scroll cancels (D-08)"
    - "A long press never also fires the short tap: the click that follows a fired long press is swallowed before any listener (a window capture-phase suppressor, consumed once), so targeting and every other tap meaning are unchanged and a long press during a beat does not skip the round (D-08)"
    - "On trigger the phone gives one light haptic through maybeHaptic(currentSettings, 'Light'), which respects the Haptics setting; foe cards suppress text selection, the touch callout and the context menu (D-08)"
    - "The card shows name and family, HP current/max (HP, never WP), defence (armour, to-hit need, magic-only / dagger-only / half damage), attacks and a per-swing damage range computed only through the engine's own exported pure helpers, abilities (FOE_ABILITIES names), resistances and weaknesses (INT, DAMAGE_MULTIPLIERS rows), current effects from 71-03's foeConditions table, and one deadpan flavour line; nothing the rules hide (fleesBelow, internal cadences) is shown (D-09)"
    - "It is a no-decision card: a body tap dismisses it; while S.combat is set it holds until dismissed and tracks the fight (HP and effects re-derived on every rail repaint, no re-rise, no re-announce, no typing); when the fight ends with it up it takes the normal RAIL_HOLD from that moment; a new long press replaces it; it never shows over the title or the roller (D-10)"
    - "In combat the card is the one combat-legal rail card: it shows over the encounter overlay (z 8, after the overlay in DOM order, below the ☰ scrim 9) with its bottom edge at the top of the action area, so it never covers the action buttons; every other card stays hidden in combat as today (D-10)"
    - "Every foe card has a visually hidden sibling Details button (accessible name 'Details: <NAME>') that raises the same card, for TalkBack users; it never changes the target (D-11)"
    - "foeDetailsCard is a pure view model, tested for every creature in every BESTIARY family and for a malformed or unknown foe (no throw, a fallback card); all its copy is in one frozen FOE_DETAILS_COPY, walked by the HP-not-WP guard and the voice scan; bestiary notes that say 'wp' are shown as HP, and the same normaliser fixes the foe card's meta line (D-12)"
  artifacts:
    - path: "src/browser/foeDetails.js"
      provides: "foeDetailsCard(foeIndex, state) -> a rail card { kind: 'foe', foe, icon, title, tone, lines, hold }, FOE_DETAILS_COPY, detailsLabel(name)"
      exports: ["foeDetailsCard", "FOE_DETAILS_COPY", "detailsLabel"]
    - path: "src/browser/longPress.js"
      provides: "createLongPress({ holdMs, maxTravel, setTimeout, clearTimeout, onLongPress }) -> { down, move, up, cancel, consumeClick }"
      exports: ["createLongPress"]
    - path: "mazeworld.html"
      provides: "the foe-card gesture wiring, the window click suppressor, window.mzInspectFoe, the Details sibling buttons, the combat-legal foe card in renderRail and its CSS"
      contains: "mzInspectFoe"
    - path: "test/unit/foe-inspect-shell.test.js"
      provides: "sandbox and source pins for the rail card in combat, the Details button and the wiring"
  key_links:
    - from: "mazeworld.html (module) long press / Details button"
      to: "window.mzInspectFoe(i) -> railPush(window.__mzRail, foeDetailsCard(i, V)) -> renderRail()"
      via: "the one entry point for both gestures"
      pattern: "mzInspectFoe"
    - from: "mazeworld.html renderRail"
      to: "window.__mzFoeInspect.card(i, V)"
      via: "re-derives the foe card's lines on every repaint while S.combat is set"
      pattern: "__mzFoeInspect"
    - from: "src/browser/foeDetails.js"
      to: "src/browser/foeConditions.js foeConditionChips"
      via: "the current-effects line (D-14's one table)"
      pattern: "foeConditionChips"
---

<objective>
Let the player inspect any enemy with a long press (POLISH-08; D-08, D-09, D-10, D-11, D-12). Closes source todo `.planning/todos/pending/2026-09-24-long-press-an-enemy-for-a-detailed-rail-card.md`, which the SUMMARY must name.

Purpose: mid-fight, a foe card shows only its essentials, and there is no way to inspect an enemy in depth. The user asked: "long pressing an enemy should pop up a rail with detailed enemy information." The locked contract:
- D-08: the gesture.
- D-09: what the card says.
- D-10: the rail rules.
- D-11: a TalkBack Details action.
- D-12: a pure, fully tested view model.

Output: the pure foeDetails.js view model and longPress.js recognizer, the shell wiring, the combat-legal rail card, the Details buttons and their tests.

Planner rulings (record in the SUMMARY):
- R-13, what counts as a foe surface (D-08): the long press attaches to every rendered foe card (`.cb-foe[data-foe]`) on the combat screen, both in a live round and in a beat's frame. The pending-combat MAJOR OVERLAY names its foes in one prose line with no per-foe element, so there is nothing to press there. The combat screen with every foe card is one tap (FIGHT IT OUT) away. No other encounter screen renders a foe.
- R-14, the combat-legal rail (D-10): today renderRail hides the rail whenever S.combat or S.dead is set. The foe card becomes the one exception.
  - It shows over the combat screen at z-index 8. That is equal to the overlay, and the rail follows #mw-screens in DOM order, so it paints above; it stays below the ☰ scrim (9) and wrap (10).
  - Its bottom edge is lifted to the top edge of #cb-act, measured at render time, so the action buttons are never covered. It may cover the lower foe list or log; a body tap dismisses it.
  - Every other card stays hidden in combat, as today.
  - This is the mechanism backlog 999.5's "status chit in combat shows nothing" todo needs later. That todo stays in 999.5.
- R-15, live card (D-09/D-10): while S.combat is set, renderRail re-derives the card from the same state the foe cards render: the beat frame's state during playback, otherwise S.
  - A same-card repaint never re-rises, re-announces or re-arms.
  - The card shows at once, with no typewriter, because it is a reference card the player asked for.
  - When S.combat clears with the card up, its holdForCard timer starts from that moment.
- R-16, HP never WP (D-12): two bestiary notes say "wp" ("two attacks, 1 wp each", "venom: 2 wp a round…"). Content is not edited. combatPanel.js exports playerNote(note), which rewrites a standalone wp/WP token to HP. The foe card meta line uses it, and foeDetails.js imports it from there (one direction only, so there is no import cycle). That meta line shows sp.note today and so leaks "WP" on two creatures, a pre-existing HP-not-WP gap closed here.
- R-17, a long press during a beat: it is not a tap. It raises the card and the round keeps playing. The click suppressor runs at window capture, before #enc-panel's beatHurryTap.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/71-device-round-polish-ii/71-CONTEXT.md
@.planning/todos/pending/2026-09-24-long-press-an-enemy-for-a-detailed-rail-card.md
@.planning/phases/71-device-round-polish-ii/71-03-SUMMARY.md
@.claude/CLAUDE.md
@src/browser/foeConditions.js
@src/browser/combatPanel.js
@src/browser/rail.js
@content/bestiary.js

<interfaces>
Post-71-03 master. Line numbers are approximate; grep each anchor.

The foe object (engine/combat.js ~258): { name, type (a BESTIARY family key), lvl, size, intel, wp, maxWP, alive, asleep, sp (the bestiary sp: atk, dmg {n,sides,bonus}, ar, toHit, magicOnly, daggerOnly, noArmor, halfDmg, strikesAs, twice, note, …), lives, abilities? (FOE_ABILITIES ids) }. Plus the condition fields 71-03 tabled.

Engine pure helpers usable for the damage range (read, never edit):
- engine/combat.js foeLevelBase(f);
- engine/difficulty.js difficultyCurve(depth) and foeHitFor(raw, curve).
A melee hit is foeHitFor(foeLevelBase(f) + dice, curve), where dice is sp.dmg or d6 when absent, and a crit doubles the dice. Per-swing range = [foeHitFor(base + diceMin, curve), foeHitFor(base + diceMax, curve)], before armour, crits and effects. The number of swings is sp.atk || 1.

Content (read only):
- content/bestiary.js BESTIARY, ENC_TYPES;
- content/foe-abilities.js FOE_ABILITIES (ids → names);
- content/damage-multipliers.js DAMAGE_MULTIPLIERS.
- content/safety-wordlist.js BANNED/ALLOWLIST.

src/browser/rail.js:
- railPush(rail, card) stamps seq;
- the card shape is { icon, iconKey, title, lines: [{ text, roll }], tone, hold };
- holdForCard(card); RAIL_HOLD.

src/browser/tapStep.js: HOLD_MS = 450, TAP_MAX_TRAVEL_PX = 10, bridged as window.__mzTapStep.

src/browser/haptics.js maybeHaptic(settings, style) is imported in the module script (~5630). `currentSettings` is the module's settings mirror.

mazeworld.html:
- renderFoeCards(host, vm, onPick, hitFoe) (~3825): builds .cb-foe[data-foe] per card, and for a live card sets role=button and `guardTap(el, () => onPick(c.i))`, which CSCR-02 pins exactly once.
- CSCR-08 pins:
  - no `card.onclick`, `card.addEventListener`, `body.*` or `panel.*` listeners in the renderFightLog→noteCombat region;
  - exactly one capture click listener on #enc-panel (beatHurryTap).
- renderRail (~4570):
  - `railEl.hidden = !!(S.combat || S.dead) || idle;` sets data-shown;
  - the isNew block starts the typewriter (onDone → hold timer);
  - `railShownAt` is the arm stamp;
  - the #mw-rail onclick dismiss (~4895): arm check → typing complete → railVM.dismissKind(railLocked(), buttonCount) → clear.
  - railLocked() is false in combat.
- renderEncounter calls `window.renderRail?.()` on every active render, including each beat line.
- The rail CSS (~850-879): .mw-rail z 4, bottom 0, the transform slide.
- The module's `window.mzRailLine` (~5980) is the one-shot entry point pattern to mirror. dungeonVisible() (~6395) is the title/roller/title-panel predicate (false while any is up).
- The CSS `.sr-only` utility (~999).

Pins to respect:
- hud-menu-layout (5), the z-ladder (base .mw-rail rule z 4);
- rail-overlay (1)-(7) (renderRail never calls fit( or keeps the party in view; motion constants);
- rail-dismiss / shell-map-rail;
- reduced-motion (paint()/draw() SHA-256; never edit them);
- no aria-disabled token in <style>;
- bridge-registry (new __mz names in src/browser/bridge.js in the same commit, then bridge-doc --write/--check);
- shell-no-content-copies criterion 3;
- combatPanel.test.js (meta pins and the COPY voice scan);
- hp-not-wp.test.js banks.
- test/unit/harness/shellSandbox.js: loadShellSandbox({ stubRail: false }) exercises the real renderRail; recordingDom's addEventListener is a no-op, so the wiring is proven by the pure-module tests plus source pins, and the Details button by calling its onclick.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: foeDetails.js — the pure long-press card view model (D-09, D-12)</name>
  <files>src/browser/foeDetails.js, test/unit/foeDetails.test.js, src/browser/combatPanel.js, test/unit/combatPanel.test.js, test/unit/hp-not-wp.test.js</files>
  <behavior>
    - foeDetailsCard(i, state) reads state.combat.foes[i] and returns a rail card:
      - { kind: "foe", foe: i, icon: FOE_GLYPHS[type] (combatPanel.js), iconKey: null, title: NAME, tone: "info", hold: RAIL_HOLD.default, lines }.
      - lines are in this order, each { text, roll: null }:
        1. family and size ("LAIR BEASTS · SIZE S");
        2. HP "HP cur / max", or DOWN for a dead foe;
        3. defence: AR from sp.ar, "hit only on n+" from sp.toHit, and the magic-only, dagger-only and half-damage labels, joined; omitted when none apply;
        4. attack: swings (sp.atk || 1), the damage dice label, and the per-swing range "hits for a–b before armour", computed through foeLevelBase, difficultyCurve(state.floor.depth) and foeHitFor only. Doubled swings when Frenzied are shown via the effect, not re-computed;
        5. abilities: FOE_ABILITIES names for f.abilities, plus playerNote(sp.note);
        6. resistances and weaknesses: INT n, the DAMAGE_MULTIPLIERS rows matching this foe's type or name as plain words, and kill-it-twice with the lives left when lives > 1;
        7. current effects: foeConditionChips(foe, state) texts joined, or a deadpan "nothing on it yet" line;
        8. one flavour line from FOE_DETAILS_COPY.flavour[type], with a fallback.
    - It never shows fleesBelow, sp.every, cadences or any rng detail (only what a player could plausibly know).
    - Out of range i, a null or foe-less state, a foe with no type, sp or name, an unknown family, or non-number HP all yield a fallback card ("SOMETHING", "FAMILY UNKNOWN", "HP ?") with no throw. A hostile getter does not throw either.
    - It is pure: two calls deep-equal, and it never mutates state.
    - combatPanel.js playerNote(text) replaces a standalone wp/WP token with HP and leaves code-like fragments alone (the hp-not-wp PLAYER_WP rule). The foe card meta uses it, so Bat/Rat and Viper read HP. foeDetails.js imports it, never the reverse.
    - detailsLabel(name) returns FOE_DETAILS_COPY's "Details: {name}" with the name filled in (the D-11 accessible name).
    - For EVERY creature in every BESTIARY family, built as the engine builds a foe (name/type/lvl = tier index + 1/size/intel/wp/maxWP/sp/lives/abilities), every card line passes PLAYER_WP and BANNED, the title is the creature's name, and the family line names its family.
    - FOE_DETAILS_COPY is frozen: labels, the fallback words, the "nothing on it" line, the Details accessible-name template "Details: {name}", and six family flavour lines plus a fallback, in the house deadpan, family-friendly voice. It is registered in hp-not-wp.test.js and voice-scanned.
  </behavior>
  <action>
RED first:
- Create test/unit/foeDetails.test.js: the every-creature sweep, the line order, one hand-checked damage range (computed in the test from the same exported engine helpers, never retyped numbers), the hidden-fields absence (for example the Djinni card never mentions 0.25 or a flee threshold), the malformed and unknown cases, the conditions line agreeing with foeConditionChips, idempotency and the copy scans.
- combatPanel.test.js: pin that the Bat/Rat and Viper meta lines contain no standalone WP, and that other creatures' meta is unchanged.
- Register FOE_DETAILS_COPY in hp-not-wp.test.js.
Run and see them fail. Commit as test(71-04).

GREEN:
- Create src/browser/foeDetails.js. It is pure and imports only:
  - content/bestiary.js (ENC_TYPES), content/foe-abilities.js and content/damage-multipliers.js;
  - engine/combat.js foeLevelBase, engine/difficulty.js difficultyCurve/foeHitFor;
  - ./foeConditions.js, ./combatPanel.js (FOE_GLYPHS, playerNote) and ./rail.js RAIL_HOLD.
  Its header comment names D-09 and D-12, the "only what the player could plausibly know" rule with the list of fields deliberately left out, and R-16.
- combatPanel.js: add and export playerNote, and route the meta note through it (R-16), with a comment.
- If a needed engine helper is not exported, do NOT export it and do NOT restate its formula. Show the dice label and "strikes as a level n" text instead of a numeric range, and list the gap in the SUMMARY.
Run green. Commit as feat(71-04): the foe details card view model.
  </action>
  <verify>
    <automated>node --test test/unit/foeDetails.test.js test/unit/combatPanel.test.js test/unit/hp-not-wp.test.js test/unit/foe-conditions.test.js</automated>
  </verify>
  <done>foeDetailsCard builds a D-09 card for every creature and a fallback for malformed input, with no hidden values and no WP. The meta leak is closed. The copy is frozen and scanned, and every listed test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: longPress.js — the long-press recognizer (D-08)</name>
  <files>src/browser/longPress.js, test/unit/longPress.test.js</files>
  <behavior>
    - createLongPress({ holdMs, maxTravel, setTimeout, clearTimeout, onLongPress, now }) returns { down(p), move(p), up(p), cancel(), consumeClick() }, where p = { id, x, y, foe }. The defaults for holdMs and maxTravel come from tapStep.js's HOLD_MS/TAP_MAX_TRAVEL_PX, imported, not retyped.
    - down arms one timer. When it fires with the same pointer still down and travel <= maxTravel, it calls onLongPress(foe) exactly once and marks the press fired.
      - At exactly holdMs it fires; at holdMs - 1 it has not.
      - Travel of exactly maxTravel still fires; maxTravel + 1 (measured from the down point, the max travel seen) cancels.
    - A second down while one is armed (two fingers) cancels both, and nothing fires.
    - up before holdMs cancels, so nothing fires and consumeClick() returns false. up after a fired press returns fired: true.
    - consumeClick() returns true exactly once after a fired press. It returns false on a second call, false again after the next down, and false once a bounded window has passed with no click (so a lost click can never swallow a later tap). The window uses the injected `now`.
    - cancel() (pointercancel, scroll, a re-render that loses the pointer) clears everything; a pending timer never fires afterwards.
    - It never throws on a missing or malformed p, and has no DOM, window or Date access except through the injected functions.
  </behavior>
  <action>
RED first: create test/unit/longPress.test.js with a fake timer and a fake `now` covering every behavior bullet, including the exact-threshold and one-either-side cases. Run and see it fail. Commit as test(71-04).
GREEN: create src/browser/longPress.js, a pure factory. Its header comment says:
- it is D-08's recognizer: fire at the hold, cancel on move, scroll or a second finger;
- the suppression contract: consumeClick is what the shell's window capture listener asks, so the long press never also fires the tap;
- it mirrors the map's hold-inspect constants.
Run green. Commit as feat(71-04): the long-press recognizer.
  </action>
  <verify>
    <automated>node --test test/unit/longPress.test.js</automated>
  </verify>
  <done>The recognizer fires at exactly the hold, cancels on travel, a second pointer, cancel or scroll, and hands the shell a once-only click-suppression answer. Every threshold and edge is pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shell wiring — gesture, haptic, the combat-legal rail card and the Details action (D-08, D-10, D-11)</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, test/unit/harness/shellSandbox.js, test/unit/foe-inspect-shell.test.js, test/unit/shell-combat-screen.test.js, test/unit/hud-menu-layout.test.js</files>
  <behavior>
    - Module script:
      - imports foeDetailsCard and createLongPress;
      - window.__mzFoeInspect = { card: foeDetailsCard, label: detailsLabel };
      - window.mzInspectFoe(i): a no-op when dungeonVisible() is false (the title, the roller or the title panel is up), when there is no state, or when the foe index is out of range. Otherwise it pushes foeDetailsCard(i, V) through railPush onto window.__mzRail and calls window.renderRail(), where V = window.__mzBeat?.view?.()?.state || the live state.
    - Gesture (module):
      - one pointerdown listener on #enc-panel acts only when the target's closest('.cb-foe[data-foe]') exists, and calls lp.down with that foe index;
      - pointermove, pointerup and pointercancel listeners on document (capture) feed lp.move/up/cancel;
      - one scroll listener on document (capture) calls lp.cancel;
      - one click listener on window (capture) calls e.stopPropagation() and e.preventDefault() when lp.consumeClick() is true;
      - one contextmenu listener on #enc-panel calls preventDefault for a .cb-foe target;
      - onLongPress(i) calls maybeHaptic(currentSettings, "Light") and window.mzInspectFoe(i).
      None of these is a click listener on #enc-panel, so CSCR-08's single-capture-click pin holds.
    - renderFoeCards (classic): after each card element, a sibling `<button class="sr-only cb-foe-details" type="button">`. Its aria-label and text are `window.__mzFoeInspect?.label?.(c.name)`, falling back to the card name, because the shell declares no copy. Its handler, `guardTap(details, () => window.mzInspectFoe?.(c.i))`, never touches S.combat.target. CSCR-02's "guardTap(el, () => onPick( exactly once" still holds.
    - renderRail:
      - foeCardUp = rail.card?.kind === "foe";
      - while S.combat is set and foeCardUp, the card's lines, title and icon are re-derived through window.__mzFoeInspect.card(rail.card.foe, V) on every repaint (R-15);
      - `railEl.hidden = !!(((S.combat && !foeCardUp) || S.dead)) || idle`;
      - railEl.dataset.over = "combat" while S.combat && foeCardUp, else it is removed;
      - with data-over set, the rail's bottom is lifted to the top of #cb-act (getBoundingClientRect, with a 0 fallback) through a CSS custom property;
      - a foe card is never typed (it shows at once) and starts no hold while S.combat is set;
      - when S.combat is null and the up card is a foe card with no timer running, the holdForCard timer starts once (no re-rise, no re-announce);
      - the idle, decision and other-card branches behave exactly as today.
    - CSS:
      - `#mw-rail[data-over="combat"]` sets z-index 8, a bottom offset from the custom property, and a max-height (about 45% of the stage) with overflow-y auto;
      - `.cb-foe` gets user-select none, -webkit-user-select none and -webkit-touch-callout none;
      - no new transition or animation;
      - no aria-disabled token in <style>.
    - The sandbox (stubRail false) proves:
      - (a) with combat live, window.__mzRail holding a foe card and renderRail(), the rail is not hidden, data-over is "combat", and the lines include "HP";
      - (b) the same state with a non-foe card keeps the rail hidden;
      - (c) after the foe's wp changes and renderRail() runs again, the HP line updates and lastRailKeyShown/the announcement are not re-issued;
      - (d) with S.combat set to null and renderRail() called, data-over is removed and a hold timer is scheduled;
      - (e) the Details button's onclick calls a stubbed window.mzInspectFoe with the card's index and leaves S.combat.target unchanged;
      - (f) the #mw-rail body tap, after the arm window, dismisses the foe card in combat.
  </behavior>
  <action>
RED first:
- Create test/unit/foe-inspect-shell.test.js with the sandbox cases (a)-(f). Add source pins over the comment-stripped shell for:
  - the module listeners (pointerdown on #enc-panel gated on .cb-foe[data-foe]; document-level move/up/cancel/scroll; the window capture click suppressor calling consumeClick; contextmenu preventDefault);
  - mzInspectFoe's dungeonVisible guard;
  - the maybeHaptic "Light" call;
  - the CSS rules.
- shell-combat-screen.test.js: keep CSCR-02 and CSCR-08 green, and add a pin that renderFoeCards builds one .cb-foe-details sibling per card.
- hud-menu-layout.test.js: add a (5b) pin that the combat-legal rail rule's z-index is 8, equal to .mw-overlay and below the scrim, and that #mw-rail follows #mw-screens in the markup (DOM order decides the tie).
- Wire the new bridge into test/unit/harness/shellSandbox.js.
Run and see them fail. Commit as test(71-04).

GREEN in mazeworld.html per D-08, D-10, D-11 and rulings R-13..R-17:
- Add the module imports, the bridge, window.mzInspectFoe beside window.mzRailLine, and the gesture listeners with a comment block that names D-08, R-17 and why each listener sits where it does.
- Extend renderFoeCards with the Details sibling (D-11).
- Change renderRail as in behavior, with a comment naming D-10, R-14 and R-15. Never call fit( or keep-in-view from renderRail (rail-overlay (5)). Do not edit paint() or draw().
- Add the CSS next to the rail and combat rules, with a Phase 71 comment.
- src/browser/bridge.js: add __mzFoeInspect (owner: the module; consumers: the classic renderRail; purpose: the long-press foe card view model, Phase 71 D-09/D-10), in alphabetical order. Then run bridge-doc --write/--check, saving with LF if --check complains only about CRLF.
- docs/SHELL-MODULES.md: a Phase 71 paragraph covering long press → mzInspectFoe → the combat-legal foe card, the Details action, and that 999.5's status-chit card can reuse the combat-legal path.
Run the Task 3 files plus rail-overlay, rail-dismiss, shell-map-rail, rail, combat-beat-shell, combat-lock-shell, shell-input-guards, reduced-motion, bridge-registry, shell-no-content-copies and hp-not-wp green. Commit as feat(71-04): long-press a foe for its details card.
Then run `npm test`: 0 failures on master, and in a worktree only the 7 known CRLF failures.
  </action>
  <verify>
    <automated>node --test test/unit/foe-inspect-shell.test.js test/unit/shell-combat-screen.test.js test/unit/hud-menu-layout.test.js test/unit/rail-overlay.test.js test/unit/rail-dismiss.test.js test/unit/shell-map-rail.test.js test/unit/rail.test.js test/unit/combat-beat-shell.test.js test/unit/combat-lock-shell.test.js test/unit/shell-input-guards.test.js test/unit/reduced-motion.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/hp-not-wp.test.js && node tools/bridge-doc.mjs --check</automated>
    <human-check>Deferred to the Phase 71 device round (folded into docs/UAT-v2.0.md section M by 71-06):
      1. In a multi-foe fight, long-press each foe in turn. A light buzz, then one rail card for that foe: name and family, HP (never WP), defence, attacks and damage range, abilities, resistances, current effects, and a flavour line. Each new long press replaces the card.
      2. A short tap on a foe still aims at it, and a long press never changes the aim. No text-selection handles or context menu ever appear on a foe card.
      3. The card sits above the action buttons, never over them, at text sizes M and L. It stays up through several rounds with its HP and effects updating. A tap on the card dismisses it.
      4. Kill the last foe with the card up: the card clears on its own after its normal hold.
      5. Long-press a foe while a round is still typing: the card appears and the round keeps playing (it is not skipped).
      6. TalkBack on: swipe through a foe card, and the next stop is "Details: <name>". Double-tap it and the same card appears, with the aim unchanged.
      7. Haptics Off in Settings: the long press still works, with no buzz.</human-check>
  </verify>
  <done>A long press or the Details action raises one live, dismissible foe card over the combat screen, clear of the actions. Targeting is unchanged, and the long press never fires the tap. Every listed test passes, and bridge-doc --check exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player gesture → combat state | A long press must never act (target, strike or skip) |
| bestiary/engine data → player-facing card | The card must not reveal hidden rules values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-07 | Tampering (a stray act from a gesture) | long press / Details | medium | mitigate | the once-only window capture suppressor swallows the trailing click; the Details handler never writes S.combat.target; mzInspectFoe only pushes a presentation card (never state, never saved); pinned by longPress.test and foe-inspect-shell (e) |
| T-71-08 | Information disclosure (of hidden rules values) | foeDetailsCard | low | mitigate | the view model reads only the listed fields; the tests assert fleesBelow/cadence fields never appear |
</threat_model>

<verification>
- All three tasks' verify commands pass. `npm test` has 0 failures (in a worktree, only the 7 known CRLF failures). bridge-doc --check exits 0.
- There is no engine/, content/ or test/parity/ change, and paint()/draw() are byte-identical.
- #enc-panel still has exactly one capture click listener (beatHurryTap).
</verification>

<success_criteria>
- D-08: a ~450 ms long press on a foe card raises the card, never fires the short tap, cancels on move or scroll, buzzes lightly, and shows no selection or context menu.
- D-09: the card carries family, HP, defence, attack and damage range, abilities, resistances, current effects and a flavour line, and nothing hidden.
- D-10: no-decision (a body tap dismisses it), held in combat, RAIL_HOLD otherwise, never over the title or roller, replaced by a new press.
- D-11: a TalkBack Details action on every foe card.
- D-12: a pure view model tested over every creature and malformed input, with its copy in the voice and HP-not-WP scans.
</success_criteria>

<output>
Create `.planning/phases/71-device-round-polish-ii/71-04-SUMMARY.md` when done. It names the source todo it closes (the long-press todo), rulings R-13..R-17, any engine-helper gap from Task 1, the 999.5 status-chit note, and a "Human verification (deferred)" section listing the seven checks above.
</output>
