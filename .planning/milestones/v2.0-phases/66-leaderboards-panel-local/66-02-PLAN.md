---
phase: 66-leaderboards-panel-local
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/boards.js
  - test/unit/boards-copy.test.js
  - test/voice/safety-scan.test.js
  - test/unit/hp-not-wp.test.js
autonomous: true
requirements: [BOARD-02, BOARD-03, BOARD-07, BOARD-08]
user_setup: []

must_haves:
  truths:
    - "Every board in BOARD_COPY carries its mock mark and colour: DEEPEST ▼ #d3c49f, LEANEST ▪ #e8c97a, LINEAGE ◆ #b9a4ef, LONGEST ⧗ #8fb08a, BUTCHERY ✕ #e07260, PURSE ● #e8c97a, GRAVEYARD ✝ #c9bda0 (D-11)"
    - "Six rule lines are the mock's text verbatim; LEANEST's rule line is the one re-voice and describes squares per floor (D-09, D-11)"
    - "The two footnotes are the mock's text verbatim, including GRAVEYARD's 'Epitaphs are written by the dungeon, not by you. There is no appeal.' (BOARD-07, D-11)"
    - "The signed-out copy exists as pure data: the strip label 'PLAY GAMES · SIGNED OUT', the source line 'Your dead only', a 'nobody' glyph, dimmable ALL/FRIENDS labels, and one in-voice note per scope (D-06)"
    - "The empty-board line is 'Nobody of yours has qualified for this board yet.' and the standing card's empty label is 'NO ENTRY' (D-12)"
    - "A new standing-quip bank holds at least 6 lines split into first place, top ten and outside-the-ten sub-banks, none claiming a pin or a global rank (D-07)"
    - "BOARD_COPY's existing tab, title, unit and unitOne fields are unchanged, so the Phase 65 death-panel announcement reads exactly as before"
    - "Every new string passes the family-friendly safety scan and the player-facing 'HP not WP' scan; content/ stays pure data (no functions)"
  prohibitions:
    - statement: "Must not word any local-only copy as a global or friends ranking ('worldwide', 'among friends', a handle, or a YOU/FRIEND tag) while the player is signed out"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "content/boards.js"
      provides: "BOARD_COPY marks/colours/panel units/mock rule lines, BOARD_FOOTNOTES, BOARDS_PANEL_COPY, STANDING_LINES"
      contains: "BOARDS_PANEL_COPY"
    - path: "test/unit/boards-copy.test.js"
      provides: "verbatim-mock and shape pins for the panel copy"
      min_lines: 60
  key_links:
    - from: "test/voice/safety-scan.test.js#collectAuthoredStrings"
      to: "content/boards.js new exports"
      via: "recursive string-leaf walk, like walkBoardCopy"
      pattern: "BOARDS_PANEL_COPY"
---

<objective>
Write the Leaderboards panel's voice as pure data in content/boards.js. Implements D-11 (marks, colours, rule lines and footnotes verbatim from the mock), D-09 (LEANEST's rule line re-voiced), D-06 (the signed-out strip, chips and notes), D-07 (the standing-card quip bank), D-12 (the empty states) and the D-01 bottom-bar labels.

Purpose: the view model (66-04) and the renderer never hold copy of their own; every player-facing word is scanned once, here.
Output: the extended content/boards.js, a verbatim/shape pin test, and safety-scan plus hp-not-wp registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/65-run-record-personal-bests/65-03-SUMMARY.md

<interfaces>
- content/boards.js (Phase 65) exports BOARD_COPY (keys deep, lean, combo, days, kills, purse, yard in that order; fields tab, title, rule, unit, unitOne), NEW_BEST_HEAD, NEW_BEST_LINES, FIRST_DEATH_LINES. It is imported directly (not through content/index.js).
- src/browser/newBest.js reads BOARD_COPY[id].title/.unit/.unitOne; test/unit/newBest.test.js and test/unit/bests-adapter.test.js pin Object.keys(BOARD_COPY) to BOARD_IDS order.
- The consumer, src/browser/boardsView.js (66-04), fills `{token}` placeholders itself, because content/ may not hold functions (test/determinism/content-is-pure-data.test.js).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: extend BOARD_COPY and add BOARD_FOOTNOTES, BOARDS_PANEL_COPY and STANDING_LINES</name>
  <files>content/boards.js</files>
  <read_first>
    - content/boards.js (the whole file)
    - design/Mazeworld Boards Panel.dc.html (the BOARDS array at ~177-201 for tab/mark/col/title/rule/unit, the footnote and scopeLine strings at ~305-382, the divider label, the standing card strings)
    - src/browser/newBest.js (which BOARD_COPY fields it reads, so they stay untouched)
    - test/determinism/content-is-pure-data.test.js (content/ must be JSON-serializable data)
  </read_first>
  <action>
All strings below are exact. "·" is U+00B7 with one space each side; "—" is U+2014; the GRAVEYARD scope line uses the curly apostrophe U+2019 exactly as the mock does.

1. BOARD_COPY (implements D-11, D-09): keep each board's tab, title, unit and unitOne exactly as they are. Add `mark`, `col` and `unitLabel`, and replace `rule`:
   - deep: mark "▼", col "#d3c49f", unitLabel "FLOOR", rule "Lowest floor reached before dying. Ties broken by the fewer squares walked to get there."
   - lean: mark "▪", col "#e8c97a", unitLabel "FLOOR · SQ", rule "Squares walked per floor descended. Efficiency, of a sort." (the D-09 re-voice, from CONTEXT's specifics)
   - combo: mark "◆", col "#b9a4ef", unitLabel "FLOOR", rule "Every race and class combination rolled so far, ranked by the deepest floor any of them managed."
   - days: mark "⧗", col "#8fb08a", unitLabel "DAYS", rule "Days survived underground. Rations are the real opponent."
   - kills: mark "✕", col "#e07260", unitLabel "KILLS", rule "Things killed before being killed. Not correlated with depth, which is the joke."
   - purse: mark "●", col "#e8c97a", unitLabel "WILMST", rule "Wilmst carried at the moment of death. All of it still down there."
   - yard: mark "✝", col "#c9bda0", unitLabel "FLOOR · SQ", rule "Everyone you have rolled and lost, deepest first, with what was said over them. Not ranked against anybody."
   Update the file header comment: Phase 66 added mark/col/unitLabel and adopted the mock's rule lines (LEANEST re-voiced for its squares-per-floor ranking); the lowercase `unit`/`unitOne` stay for the death-panel announcement.

2. `export const BOARD_FOOTNOTES` (D-11): ranked "Top ten only. Boards count the dead — living characters are provisional and the dungeon keeps no provisional records."; yard "Epitaphs are written by the dungeon, not by you. There is no appeal."

3. `export const BOARDS_PANEL_COPY`, a deep-frozen nested object:
   - head: title "LEADERBOARDS", interred "INTERRED", back "Back" (the chevron's accessible label)
   - scope: ranked "Your dead only. The world has not been told."; yard "Your own dead. Nobody else’s business."
   - strip (D-06): glyph "?", label "PLAY GAMES · SIGNED OUT", source "Your dead only"
   - chips: all "ALL", friends "FRIENDS"
   - note (D-06): all "Nobody out there can see you yet."; friends "Your friends have not been told you exist. It may be kinder that way."
   - empty (D-12): "Nobody of yours has qualified for this board yet."
   - divider: "NOT IN THE TOP TEN · YOUR BEST RUN"
   - standing (D-07, D-12): noEntry "NO ENTRY"; noPlace "—"; noNote "The ledger opens at your first funeral."; ofYours "of {n} of yours."; ofCombos "of {n} combinations."; interred "INTERRED"; yardNote "rolled, delved, and buried. Deepest was {name} on floor {floor}."
   - stats: floor "FLOOR", days "DAYS", squares "SQUARES", kills "KILLS", exp "EXP", wilmst "WILMST"
   - lineage: one "{n} INTERRED"; many "{n} INTERRED, NO SURVIVORS"; detail "{n} rolled, {n} dead. Deepest was {name}, floor {floor} in {steps} squares. {epitaph}"
   - level "LVL"; sep " · "
   - dock (D-01): title "BACK TO TITLE"; roll "ROLL A NEW HERO"; dungeon "BACK TO THE DUNGEON"
   Document the `{token}` convention in a comment: the view model fills tokens; content/ holds no functions.

4. `export const STANDING_LINES` (D-07), deep-frozen, three arrays:
   - first: "Enjoy it."; "Your best yet. The bar was on the floor, and so are you."; "First place among your own dead. It is a small room."
   - ten: "Holding the top ten, for now."; "Respectable, for a corpse."; "In the top ten. The rest of your dead are taking notes."
   - rest: "Outside your own top ten. Even your ghosts are unimpressed."; "Not your finest hour. Not your worst, either, probably."; "Somewhere in the middle of the pile. Literally."
   No line may mention a pin, "worldwide", friends, or another player.

Keep every string family-friendly and free of the retired-surface word that tools/stale-terms.mjs enforces. Player text says HP, never the retired two-letter health abbreviation (no line here needs either).
  </action>
  <verify>
    <automated>node --test test/unit/newBest.test.js test/unit/bests-adapter.test.js test/determinism/content-is-pure-data.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const BOARDS_PANEL_COPY" content/boards.js` equals 1; the same holds for `export const BOARD_FOOTNOTES` and `export const STANDING_LINES`
    - `grep -c "Epitaphs are written by the dungeon, not by you. There is no appeal." content/boards.js` equals 1
    - `grep -c "Squares walked per floor descended. Efficiency, of a sort." content/boards.js` equals 1
    - `node --test test/unit/newBest.test.js test/determinism/content-is-pure-data.test.js` passes unedited
  </acceptance_criteria>
  <done>content/boards.js holds every word the panel shows, with the mock's marks, colours, rules and footnotes, and the Phase 65 fields are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pin the copy verbatim and register it with the safety and HP scans</name>
  <files>test/unit/boards-copy.test.js, test/voice/safety-scan.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - test/voice/safety-scan.test.js (the Phase 65 BOARD_COPY import at ~75 and the walkBoardCopy recursive walk at ~379-391)
    - test/unit/hp-not-wp.test.js (section (b): collectStringLeaves and how COPY objects are registered)
    - content/boards.js (as written by Task 1)
  </read_first>
  <behavior>
    - Object.keys(BOARD_COPY) still equals ["deep","lean","combo","days","kills","purse","yard"]
    - Each board has a one-character mark, a #rrggbb col, a non-empty unitLabel, title and rule, and the exact values listed in Task 1
    - The six non-LEANEST rule lines equal the mock strings verbatim; LEANEST's rule is not the mock's old "Depth first, then economy..." line and contains "per floor"
    - BOARD_FOOTNOTES.ranked and .yard equal the mock verbatim
    - BOARDS_PANEL_COPY and STANDING_LINES are deep-frozen; every leaf is a non-empty string; each template's tokens are only from {n,name,floor,steps,epitaph}
    - STANDING_LINES has first/ten/rest arrays of at least 2 each and at least 6 lines in total; no line matches /worldwide|friend|pinned|@/i
    - BOARD_COPY tab/title/unit/unitOne match their Phase 65 values (pin the literal expected object for those four fields)
  </behavior>
  <action>
1. Create test/unit/boards-copy.test.js covering every behaviour bullet, importing from ../../content/boards.js.
2. test/voice/safety-scan.test.js: extend the Phase 65 import line to also import BOARD_FOOTNOTES, BOARDS_PANEL_COPY and STANDING_LINES, and walk each through the same recursive string-leaf walk used for BOARD_COPY (labels "BOARD_FOOTNOTES", "BOARDS_PANEL_COPY", "STANDING_LINES"), so they join the completeness and load-bearing meta-tests.
3. test/unit/hp-not-wp.test.js: register the same three exports in section (b) the way the other COPY objects are walked.
  </action>
  <verify>
    <automated>node --test test/unit/boards-copy.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/boards-copy.test.js` passes with at least 8 tests
    - `grep -c "BOARDS_PANEL_COPY" test/voice/safety-scan.test.js` is at least 2 (import and walk) and `grep -c "BOARDS_PANEL_COPY" test/unit/hp-not-wp.test.js` is at least 2
    - `node --test test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js test/unit/stale-terms.test.js` passes
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The panel's copy is pinned to the mock, scanned for voice safety and HP wording, and proven not to break the death-panel announcement.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures)
- No Android build, APK or device step
</verification>

<success_criteria>
- BOARD-03/BOARD-07: marks, titles, rule lines and footnotes exist in voice, mock-verbatim except LEANEST's deliberate re-voice
- BOARD-08: the signed-out strip, chips and notes exist as copy
- All new copy passes the safety and HP scans
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `BOARD_COPY[id].mark` / `.col` / `.unitLabel` | content fields | mock marks, colours, uppercase panel units |
| `BOARD_COPY[id].rule` | content field (re-worded) | mock verbatim; LEANEST re-voiced |
| `BOARD_FOOTNOTES` | content export | ranked + yard |
| `BOARDS_PANEL_COPY` | content export | header, scope, strip, chips, notes, empty, divider, standing, stats, lineage, dock |
| `STANDING_LINES` | content export | first / ten / rest quip banks |
| test/unit/boards-copy.test.js | new test file | |

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-02-SUMMARY.md` when done. Include a "Human verification (deferred to end of run)" heading with one item for the Phase 69 batch: a read-through of the panel's copy in voice on the device (tone is a taste call the scans cannot make). Do NOT create `.planning/phases/66-*/deferred-items.md`.
</output>
