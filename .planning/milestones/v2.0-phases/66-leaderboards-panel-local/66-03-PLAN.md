---
phase: 66-leaderboards-panel-local
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/boardsPanel.js
  - test/unit/boardsPanel-dom.test.js
  - test/unit/boardsPanel.test.js
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-05, BOARD-06, BOARD-07, BOARD-08]
user_setup: []

must_haves:
  truths:
    - "renderBoardsPanel draws the mock's structure from a view object alone: header (optional ◀ chevron, LEADERBOARDS, scope line, INTERRED count), identity strip with ALL/FRIENDS chips, seven-chip rail, board head (mark, title, rule), rows or an empty/note line, the standing card, the footnote, and the title-mode dock"
    - "The renderer builds DOM only with createElement/textContent/attributes/inline styles and never assigns an HTML string, reaches the page only through host.ownerDocument and its arguments, and imports no copy (every word comes from the view)"
    - "A row renders rank, avatar (initials on the view's colour), headline, an optional tag, an optional name line, the RACE SUB · LVL line, a min-max value bar and value + unit; an open row adds the detail line and six stat chips (BOARD-04, BOARD-06)"
    - "A row whose divider label is non-empty is preceded by the NOT IN THE TOP TEN divider, so the Phase 68 pin needs no renderer change (BOARD-05)"
    - "Tapping a row opens it and closes any other open row; tapping it again closes it (one row open at a time, D-13)"
    - "The active chip is centred in the rail after every render: the target is the mock's syncRail formula clamped to the scrollable range, it only scrolls when off by more than 2px, and it scrolls instantly (behavior auto) under reduced motion (D-13)"
    - "The rail and list elements persist across re-renders, so a row tap never resets the list's scroll position; a board or scope change resets the list to the top"
    - "openFromTab opens on the last board viewed (ddr.boards.last.v1 through the injected prefs, try/catch-wrapped), falling back to DEEPEST; openFromTitle always opens GRAVEYARD (D-04)"
    - "openFromTitle marks the page (body data-boards-entry='title') so the shell can hide the tab bar; tab entry never sets it (D-01, D-02)"
    - "back() in title mode routes to 'dungeon' when a live hero exists, else 'title', clears the title marker first and returns true; in tab mode it returns false and routes nothing (D-03)"
    - "The dock buttons route 'title', 'roll' or 'dungeon' with the stored hasHero, exactly like the chevron (D-01)"
    - "Tapping a lit ALL/FRIENDS chip returns to the local list; tapping the other chip switches scope; neither touches the rail, storage or the network (D-06)"
    - "Every open (tab or title) starts on the local list with no row open (the local list is the default view)"
    - "A readData() that throws still renders the panel from empty data instead of breaking the tab switch"
  prohibitions:
    - statement: "Must not make any network call or reference any network-capable API from the panel module; signed out, the panel is local-only"
      status: unresolved
      verification: judgment
    - statement: "Must not surface the signed-out ALL/FRIENDS note, or any panel message, on the rail or as a transient notification; it stays inside the panel"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/boardsPanel.js"
      provides: "BOARDS_CLASSES, BOARDS_LAST_KEY, railScrollTarget, renderBoardsPanel, createBoardsPanel"
      contains: "export function createBoardsPanel"
    - path: "test/unit/boardsPanel-dom.test.js"
      provides: "recordingDom tests of the renderer against hand-built views"
      min_lines: 120
    - path: "test/unit/boardsPanel.test.js"
      provides: "controller tests: entry modes, board memory, scope toggle, row toggle, back routing, rail centring"
      min_lines: 120
  key_links:
    - from: "src/browser/boardsPanel.js#createBoardsPanel"
      to: "the injected buildView (src/browser/boardsView.js#boardsView, wired in 66-06)"
      via: "render() calls buildView({ ...readData(), board, scope, open, entry, hasHero, signedIn: false })"
      pattern: "buildView\\("
    - from: "src/browser/boardsPanel.js#back"
      to: "the injected onRoute"
      via: "route(hasHero ? 'dungeon' : 'title')"
      pattern: "onRoute"
---

<objective>
Build the Leaderboards panel's DOM renderer and its stateful controller as a new modular-shell screen module, src/browser/boardsPanel.js. Implements D-14 (the module, gearTab.js/roller.js pattern: page access only through the host and injected seams), D-13 (rows, tap-expand one at a time, rail auto-centring, reduced motion), D-01/D-02 (title vs tab entry chrome), D-03 (back() mirrors the chevron), D-04 (first board and last-board memory), D-06 (dimmed chips that swap in an in-panel note) and D-16 (DOM and controller tests).

The view object is produced by 66-04's boardsView; this plan injects `buildView`, so it runs in wave 1 and its tests use hand-built views.

Purpose: one renderer that Phase 68's global rows reuse unchanged.
Output: src/browser/boardsPanel.js and two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md

<interfaces>
The view contract (66-04 produces it; this plan renders it; both plans pin the same shape):

- `view.header`: { title, scopeLine, interred (number), interredLabel, back (boolean: show the chevron), backLabel }
- `view.strip`: null (GRAVEYARD) or { glyph, label, source, scopes: [{ id: "all"|"friends", label, on (boolean), dim (boolean) }] }
- `view.rail`: seven entries { id, tab, on (boolean), col } in BOARD_IDS order
- `view.board`: { id, mark, col, title, rule }
- `view.body`: { kind: "rows", rows } or { kind: "empty", line } or { kind: "note", line }
- row: { key, rank (string, "" for unranked), top, podium, you (booleans), divider (string, "" for none), avatar: { initials, bg }, headline, tag (string, "" for none), name (string, "" to omit), line, barPct (integer 3..100), val, unit, open (boolean), detail, stats: six { k, v } }
- `view.standing`: { label, place, note }
- `view.footnote`: string
- `view.dock`: null (tab entry) or [{ id: "title"|"roll"|"dungeon", label, primary (boolean) }]

Controller seams: `createBoardsPanel({ host, buildView, readData, prefs, reducedMotion, onRoute })`
- `readData()` returns { bests, graves, total } (66-06 wires the adapter's getBests()/getGraveyard())
- `prefs` is a Storage-like object (getItem/setItem) or null; `reducedMotion()` returns a boolean; `onRoute(action, { hasHero })` is the shell's router
- engine/records.js exports BOARD_IDS (already on master, Phase 65)

Harness facts: test/unit/harness/recordingDom.js elements record onclick (addEventListener is a no-op), support replaceChildren/appendChild/querySelector with #id, .class, tag and ":scope > tag" selectors only (no attribute selectors), and have plain-object dataset/style; they do not have scrollLeft/offsetLeft/scrollWidth/scrollTo until a test assigns them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderBoardsPanel, railScrollTarget and BOARDS_CLASSES</name>
  <files>src/browser/boardsPanel.js, test/unit/boardsPanel-dom.test.js</files>
  <read_first>
    - design/Mazeworld Boards Panel.dc.html (the whole markup block ~18-143 and syncRail ~224-232: structure, per-element styles, the top-row/you-row highlight rules, the long-value font step)
    - src/browser/gearTab.js (header comment and one render function: the host.ownerDocument + deps discipline, onclick wiring)
    - src/browser/roller.js (createRoller: the stateful-factory shape and frozen return)
    - test/unit/harness/recordingDom.js (createRecordingDocument, element fields, selector support)
    - test/unit/gear-tab-dom.test.js (how a module renderer is driven against recordingDom)
  </read_first>
  <behavior>
    - First render into an empty host creates exactly one `.mw-bd` root holding, in order, `.mw-bd-head`, `.mw-bd-strip`, `.mw-bd-rail`, `.mw-bd-boardhead`, `.mw-bd-body`, `.mw-bd-dock`; a second render reuses the same root, rail and body elements (identity preserved)
    - header.back false renders no `.mw-bd-back`; true renders a button with the backLabel as its aria-label whose onclick calls handlers.onBack
    - `.mw-bd-interred-n` text is String(header.interred); the scope line and title texts come from the view
    - strip null sets `.mw-bd-strip` hidden; a strip renders the nobody glyph, the label, the source and one `.mw-bd-scope-chip` per scope with data-on/data-dim "1"/"0" and aria-pressed; a chip's onclick calls handlers.onScope(id)
    - Seven `.mw-bd-chip` buttons in view order; the on chip has data-on "1", aria-pressed "true" and inline background equal to its col; onclick calls handlers.onBoard(id)
    - `.mw-bd-mark` text is the mark with inline colour col; title and rule texts from the view
    - body kind "empty" renders one `.mw-bd-empty`; kind "note" renders one `.mw-bd-note`; neither renders a row
    - Rows: one `.mw-bd-entry` per row; a non-empty divider adds `.mw-bd-divider` with `.mw-bd-divider-label` text before the row; `.mw-bd-row` carries data-key/data-top/data-podium/data-you/data-open, role "button", tabindex "0", aria-expanded; its onclick calls handlers.onRow(key)
    - The top row's rank and value get the board colour inline, and its bar fill gets the board colour; `.mw-bd-bar-fill` width is `${barPct}%`; `.mw-bd-val` gets data-long "1" when val is longer than 5 characters
    - An empty tag renders no `.mw-bd-tag`; an empty name renders no `.mw-bd-name`; the avatar's text is the initials with inline background avatar.bg
    - An open row renders `.mw-bd-detail` with the detail text and exactly six `.mw-bd-stat` chips (k and v texts); a closed row renders none
    - After the rows (or empty/note line) the body always holds `.mw-bd-standing` (label, note, place texts) then `.mw-bd-foot` (footnote)
    - dock null sets `.mw-bd-dock` hidden; a dock renders one `.mw-bd-dock-btn` per entry with data-action and data-primary, onclick calling handlers.onDock(id)
    - Every className the renderer emits is listed in BOARDS_CLASSES (walk the rendered tree for a full view with every optional part and compare)
    - railScrollTarget({offsetLeft:300,offsetWidth:60,clientWidth:360,scrollWidth:900}) === 150; it clamps to 0 at the left edge and to scrollWidth - clientWidth at the right edge; non-finite inputs are treated as 0 and it never returns NaN
    - Source pins (comment-stripped): no document./window./globalThis. references, no HTML-string property assignment, no import from content/, and no network identifier (fetch, XMLHttpRequest, WebSocket, EventSource, sendBeacon)
  </behavior>
  <action>
Create src/browser/boardsPanel.js with a header comment in the gearTab.js style: Phase 66 (BOARD-01..08, D-13/D-14), the Leaderboards panel renderer and controller; pure DOM over host.ownerDocument; every word comes from the view (content/boards.js through boardsView.js); no window/document globals; no network.

1. `export const BOARDS_CLASSES`: a frozen array of every class name the renderer emits: mw-bd, mw-bd-head, mw-bd-back, mw-bd-headtext, mw-bd-title, mw-bd-scope, mw-bd-interred, mw-bd-interred-n, mw-bd-interred-label, mw-bd-strip, mw-bd-av, mw-bd-av-nobody, mw-bd-strip-text, mw-bd-strip-label, mw-bd-strip-source, mw-bd-scopes, mw-bd-scope-chip, mw-bd-rail, mw-bd-chip, mw-bd-boardhead, mw-bd-mark, mw-bd-boardtext, mw-bd-boardtitle, mw-bd-rule, mw-bd-body, mw-bd-empty, mw-bd-note, mw-bd-entry, mw-bd-divider, mw-bd-divider-dots, mw-bd-divider-label, mw-bd-row, mw-bd-rank, mw-bd-main, mw-bd-idline, mw-bd-handle, mw-bd-tag, mw-bd-name, mw-bd-line, mw-bd-bar, mw-bd-bar-fill, mw-bd-detail, mw-bd-detail-text, mw-bd-stats, mw-bd-stat, mw-bd-stat-k, mw-bd-stat-v, mw-bd-valcell, mw-bd-val, mw-bd-unit, mw-bd-standing, mw-bd-standing-text, mw-bd-standing-label, mw-bd-standing-note, mw-bd-standing-place, mw-bd-foot, mw-bd-dock, mw-bd-dock-btn. 66-05's CSS test asserts each has a rule.

2. `export function railScrollTarget({ offsetLeft, offsetWidth, clientWidth, scrollWidth })`: the mock's syncRail formula. Coerce each input to a finite number (else 0) and return the larger of 0 and the smaller of (offsetLeft minus half of (clientWidth minus offsetWidth)) and (scrollWidth minus clientWidth).

3. `export function renderBoardsPanel(host, view, handlers = {})`, where `doc = host.ownerDocument`:
   - Ensure the skeleton: find an existing `.mw-bd` child of host (via querySelector) or create it with the six section children in the order head, strip, rail, boardhead, body, dock. Section elements persist across calls; only their children are replaced (replaceChildren), so the rail's and the body's scroll positions survive a re-render.
   - Root: data-board = view.board.id; data-entry = "title" when view.header.back, else "tab".
   - Head: when view.header.back, a `button.mw-bd-back` (type "button", aria-label backLabel, text "◀", onclick → handlers.onBack?.()); then `div.mw-bd-headtext` holding `span.mw-bd-title` and `span.mw-bd-scope`; then `div.mw-bd-interred` holding `span.mw-bd-interred-n` and `span.mw-bd-interred-label`.
   - Strip: hidden when view.strip is null; otherwise `div.mw-bd-av mw-bd-av-nobody` (glyph, aria-hidden "true"), `div.mw-bd-strip-text` (`span.mw-bd-strip-label`, `span.mw-bd-strip-source`), and `div.mw-bd-scopes` with a `button.mw-bd-scope-chip` per scope (type button, data-scope, data-on, data-dim, aria-pressed, onclick → handlers.onScope?.(id)).
   - Rail: one `button.mw-bd-chip` per rail entry (type button, data-board, data-on, aria-pressed, text tab, onclick → handlers.onBoard?.(id)); the on chip gets inline background col, colour "#14110c" and box-shadow "inset 0 0 0 1px " + col (the mock's chip()).
   - Board head: `span.mw-bd-mark` (text mark, inline colour col, aria-hidden "true"), then `div.mw-bd-boardtext` with `span.mw-bd-boardtitle` and `span.mw-bd-rule`.
   - Body: kind "empty" gives `p.mw-bd-empty`; kind "note" gives `p.mw-bd-note`; kind "rows" gives, per row, a `div.mw-bd-entry` holding an optional divider (`div.mw-bd-divider` with `span.mw-bd-divider-dots` text "···" and `span.mw-bd-divider-label` text row.divider) and then the `div.mw-bd-row` described in the behaviour list: rank span, avatar div, `div.mw-bd-main` (idline with handle and an optional tag; an optional name line; the line; the bar; and, when open, `div.mw-bd-detail` with `span.mw-bd-detail-text` and `div.mw-bd-stats` of six `div.mw-bd-stat` each holding `span.mw-bd-stat-k` and `span.mw-bd-stat-v`), and `div.mw-bd-valcell` (`span.mw-bd-val`, `span.mw-bd-unit`). For the top row, set the rank's and value's inline colour and the bar fill's inline background to view.board.col, and the row's inline box-shadow to "inset 0 -1px 0 #241f16, inset 3px 0 0 " + col (the mock's top-row stripe). After the rows (or the empty/note line), always append `div.mw-bd-standing` (`div.mw-bd-standing-text` with `span.mw-bd-standing-label` and `span.mw-bd-standing-note`, then `span.mw-bd-standing-place`) and `p.mw-bd-foot`.
   - Dock: hidden when view.dock is null; otherwise one `button.mw-bd-dock-btn` per entry (type button, data-action id, data-primary, text label, onclick → handlers.onDock?.(id)).
   - Use only createElement, className, textContent, setAttribute, dataset, style properties, hidden, onclick, appendChild and replaceChildren. Never assign an HTML string. Every handler call is optional-chained.

4. Create test/unit/boardsPanel-dom.test.js covering every behaviour bullet with createRecordingDocument() and a local makeView() fixture that exercises every optional part (chevron, strip, divider, tag, name, open row, dock). Apply the comment-stripping approach from test/unit/gear-tab-dom.test.js for the source pins.
  </action>
  <verify>
    <automated>node --test test/unit/boardsPanel-dom.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function renderBoardsPanel" src/browser/boardsPanel.js` equals 1; `grep -c "export function railScrollTarget" src/browser/boardsPanel.js` equals 1; `grep -c "export const BOARDS_CLASSES" src/browser/boardsPanel.js` equals 1
    - `node --test test/unit/boardsPanel-dom.test.js` passes with at least 16 tests, including the source pins and the BOARDS_CLASSES completeness walk
    - `node --test test/unit/bridge-registry.test.js test/unit/stale-terms.test.js` passes (the new module adds no unregistered window bridge and no retired term)
  </acceptance_criteria>
  <done>The renderer draws every part of the mock's panel from a view object, keeps its rail and list elements across re-renders, and is proven global-free, copy-free and network-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createBoardsPanel, the controller: entry modes, board memory, scope and row toggles, back routing, rail centring</name>
  <files>src/browser/boardsPanel.js, test/unit/boardsPanel.test.js</files>
  <read_first>
    - src/browser/boardsPanel.js (Task 1's renderer)
    - src/browser/roller.js (the frozen-API factory shape)
    - src/browser/motion.js (prefersReducedMotion's contract, which the shell will inject as reducedMotion)
    - engine/records.js (BOARD_IDS)
    - .planning/phases/66-leaderboards-panel-local/66-CONTEXT.md (D-01..D-04, D-06, D-13)
  </read_first>
  <behavior>
    - openFromTab() with prefs null opens "deep"; prefs holding "kills" under ddr.boards.last.v1 opens "kills"; a stored unknown id opens "deep"; a prefs whose getItem throws opens "deep" and does not throw
    - openFromTitle({ hasHero: false }) opens "yard" whatever prefs holds, sets host.ownerDocument.body.dataset.boardsEntry to "title", makes isTitleOpen() true, and passes entry "title" and hasHero false to buildView
    - Every open passes scope "local", open null and signedIn false to buildView
    - A chip's onclick (via the rendered DOM) switches board, stores it with prefs.setItem(BOARDS_LAST_KEY, id) (a throwing setItem is swallowed), closes any open row and sets the body's scrollTop to 0
    - A row's onclick opens it; another row's onclick opens that one instead; the first row's onclick again closes it (buildView sees open === key, then the other key, then null), and a row toggle leaves the body's scrollTop untouched
    - The ALL chip's onclick sets scope "all"; again returns to "local"; ALL then FRIENDS gives "friends"; a board switch keeps the scope (the mock's behaviour); a scope change closes any open row and resets scrollTop
    - back() in tab mode returns false and calls no route; in title mode with hasHero true it calls onRoute("dungeon", { hasHero: true }), deletes body.dataset.boardsEntry and makes isTitleOpen() false; with hasHero false it routes "title"
    - The chevron's and each dock button's onclick route exactly like back() and onRoute(id, { hasHero }) respectively
    - onDeadTab() in tab state (or before any open) calls openFromTab(); in title mode it only re-centres (buildView is not called again)
    - centreRail(): with the on chip's offsets set by the test and rail.scrollTo present, it calls scrollTo({ left: target, behavior: "auto" }) when reducedMotion() is true and "smooth" when false; without scrollTo it assigns scrollLeft; a current scrollLeft within 2px of the target calls nothing
    - A readData that throws still renders (buildView receives graves [] and total 0 and a null bests)
    - refresh() re-renders only while the panel is open
    - state() returns a frozen snapshot { entry, board, scope, open, hasHero }
  </behavior>
  <action>
In src/browser/boardsPanel.js add (implements D-01, D-02, D-03, D-04, D-06, D-13):

1. `export const BOARDS_LAST_KEY = "ddr.boards.last.v1";` with a comment: D-04, a per-viewer convenience key, not game data, read and written only through the injected prefs and always inside try/catch.

2. `export function createBoardsPanel({ host, buildView, readData, prefs = null, reducedMotion = () => false, onRoute })`, returning a frozen `{ openFromTab, openFromTitle, onDeadTab, back, isTitleOpen, centreRail, refresh, state }`. Internal state: entry (null, "tab" or "title"), board, scope ("local", "all" or "friends"), open (a row key or null), hasHero.
   - openFromTab(): entry "tab"; board = the stored value from prefs.getItem(BOARDS_LAST_KEY) when it is in BOARD_IDS, else "deep"; scope "local"; open null; delete the body's boardsEntry marker; render with a scroll reset.
   - openFromTitle({ hasHero }): entry "title"; hasHero = !!hasHero; board "yard" (D-04: the button says View the Dead); scope "local"; open null; set host.ownerDocument.body.dataset.boardsEntry = "title"; render with a scroll reset.
   - onDeadTab(): when entry is "title", only centreRail(); otherwise openFromTab(). This is what the shell's tab switch calls.
   - Handlers passed to renderBoardsPanel: onBoard(id) ignores ids outside BOARD_IDS, else sets board, closes any open row, stores the id in prefs (try/catch) and re-renders with a scroll reset; onScope(id) sets scope to "local" when id is already the scope, else id, closes any open row and re-renders with a scroll reset; onRow(key) toggles open between key and null and re-renders without a scroll reset; onBack() calls back(); onDock(id) routes id when it is "title", "roll" or "dungeon".
   - back(): returns false in tab mode (or closed); in title mode routes hasHero ? "dungeon" : "title" and returns true.
   - route(action) (private): capture hasHero, set entry null, delete the body's boardsEntry marker, then call onRoute?.(action, { hasHero }).
   - isTitleOpen(): entry === "title".
   - render({ reset }) (private): data = readData() inside try/catch (fallback { bests: null, graves: [], total: 0 }); view = buildView({ ...data, board, scope, open, entry, hasHero, signedIn: false }); renderBoardsPanel(host, view, handlers); centreRail(); when reset, set the `.mw-bd-body` element's scrollTop to 0. Wrap buildView and the render in one try/catch so a failure leaves the previous DOM in place rather than breaking the tab switch.
   - centreRail(): find `.mw-bd-rail`, walk its children for the chip whose dataset.on is "1" (recordingDom has no attribute selectors), compute railScrollTarget from that chip's offsetLeft/offsetWidth and the rail's clientWidth/scrollWidth, and, only when the absolute difference from Number(rail.scrollLeft) (0 when not finite) exceeds 2, call rail.scrollTo({ left, behavior: reducedMotion() ? "auto" : "smooth" }) when scrollTo is a function, else assign rail.scrollLeft. This is D-13's auto-centre, with prefers-reduced-motion respected.
   - refresh(): render without a reset when entry is not null.
   - state(): Object.freeze of the current fields.
   signedIn is always false this phase: the ALL/FRIENDS chips are dimmed and swap in the view's in-panel note (D-06); Phase 67 supplies a real sign-in state through this same call.

3. Create test/unit/boardsPanel.test.js covering every behaviour bullet with createRecordingDocument(), a recording fake buildView (it records each input and returns a small valid view built from it, so chip/row/dock onclicks exist) and fake prefs/onRoute/reducedMotion.
  </action>
  <verify>
    <automated>node --test test/unit/boardsPanel.test.js test/unit/boardsPanel-dom.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function createBoardsPanel" src/browser/boardsPanel.js` equals 1 and `grep -c "ddr.boards.last.v1" src/browser/boardsPanel.js` equals 1
    - `node --test test/unit/boardsPanel.test.js` passes with at least 14 tests, covering both entry modes, back routing with and without a hero, the dock, board memory, the scope toggle, the one-open-row rule, onDeadTab and the reduced-motion centring branch
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The panel controller opens correctly from either entry, remembers the last board, toggles scope and rows per the mock, routes back exactly like the chevron, and centres the active chip with reduced motion respected.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures)
- No Android build, APK or device step
</verification>

<success_criteria>
- D-14: the panel is a src/browser/ module reached through injected seams only
- D-13/D-06/D-04/D-03: every interaction the mock and CONTEXT specify is implemented and unit-tested
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| src/browser/boardsPanel.js | new module | renderer + controller |
| `BOARDS_CLASSES` | export | every emitted class; 66-05's CSS test consumes it |
| `BOARDS_LAST_KEY` | export | "ddr.boards.last.v1" (D-04) |
| `railScrollTarget(dims)` | export | the mock's syncRail formula, clamped |
| `renderBoardsPanel(host, view, handlers)` | export | persistent-skeleton DOM renderer |
| `createBoardsPanel({ host, buildView, readData, prefs, reducedMotion, onRoute })` | export | frozen { openFromTab, openFromTitle, onDeadTab, back, isTitleOpen, centreRail, refresh, state } |
| body `data-boards-entry="title"` | page marker | set in title mode; 66-05's CSS hides the tab bar and rail on it |
| test/unit/boardsPanel-dom.test.js, test/unit/boardsPanel.test.js | new test files | |

## Flagged assumptions (unresolved edge rows)

- BOARD-06 (edge probe: unclassified): the expand interaction is taken to be "tap toggles, one row open at a time, board or scope change closes it", per the mock's state handling; no other edge was inferred.
- BOARD-01 (edge probe: unclassified): both entries' routing is implemented here and wired in 66-06/66-07; an entry while another overlay is up is out of scope (the title screen covers the app).

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-03-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: the active chip glides to the centre on a board switch and jumps instantly with the OS reduce-motion setting on; a tap on a row deep in a long GRAVEYARD list opens it without the list jumping to the top. Do NOT create `.planning/phases/66-*/deferred-items.md`.
</output>
