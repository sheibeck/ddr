---
phase: 66-leaderboards-panel-local
plan: 06
type: execute
wave: 3
depends_on: ["66-01", "66-03", "66-04", "66-05"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - test/unit/harness/shellSandbox.js
  - test/unit/shell-boards-panel.test.js
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-06, BOARD-07, BOARD-08]
user_setup: []

must_haves:
  truths:
    - "Opening the in-game DEAD tab shows the Leaderboards panel in #screen-dead, with the game tab bar visible, DEAD active, no chevron and no dock (BOARD-01, D-02)"
    - "The DEAD tab opens on the last board viewed, else DEEPEST, with the local list and no row open (D-04)"
    - "The panel reads only the adapter's in-memory bests record and graveyard (getBests/getGraveyard), never storage, so a death that just happened already shows when the tab opens (D-15)"
    - "INTERRED in the header is the lifetime total from the adapter (D-08)"
    - "The old graveyard screen is gone: its markup (#yard, #yard-count, the yard wrapper), its CSS (.yard*, .stone*), its classic renderers and its 'Showing last' count copy are deleted, with no dual path (D-14, greenfield ruling)"
    - "The title's VIEW THE DEAD button is gated on the adapter's lifetime total instead of a classic bridge, so it appears after a first death mid-session (D-14)"
    - "window.__mzBoards is the one registered bridge between the classic tab switch and the module-owned panel; it is listed in src/browser/bridge.js and the generated docs/SHELL-MODULES.md table, and the retired graves-count bridge is removed from both"
    - "routeFromBoards sends 'title' to the map then the title screen (keeping the resume decision), 'dungeon' to the map plus the one-shot load report, and 'roll' to the character roller (D-01)"
    - "The shell makes no network call for the panel: the comment-stripped mazeworld.html references no network-capable API (BOARD-08)"
    - "Existing shell behaviour holds: the HUD and condition strip still hide on DEAD, every other tab renders as before, and the full test suite stays green"
  prohibitions:
    - statement: "Must not keep the classic graveyard screen alongside the new panel (no dual path, no hidden fallback renderer)"
      status: unresolved
      verification: judgment
    - statement: "Must not read storage from the panel or the shell's panel wiring; the adapter is the only reader"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "mazeworld.html"
      provides: "the emptied #screen-dead host, the showTab DEAD hook, the module-side panel instance, routeFromBoards, window.__mzBoards, the getGraveyard-gated refreshTitleDead"
      contains: "createBoardsPanel({"
    - path: "src/browser/bridge.js"
      provides: "the __mzBoards registry entry (and no __mzGravesCount)"
      contains: "__mzBoards"
    - path: "docs/SHELL-MODULES.md"
      provides: "the Leaderboards panel section and the regenerated bridge table"
      contains: "boardsPanel.js"
    - path: "test/unit/harness/shellSandbox.js"
      provides: "the real __mzBoards wired over an injected data source"
      contains: "createBoardsPanel"
    - path: "test/unit/shell-boards-panel.test.js"
      provides: "source pins and sandbox behaviour tests for the DEAD tab panel"
      min_lines: 150
  key_links:
    - from: "mazeworld.html (classic: showTab)"
      to: "window.__mzBoards.onDeadTab"
      via: "the name === 'dead' branch"
      pattern: "window\\.__mzBoards\\?\\.onDeadTab\\?\\.\\(\\)"
    - from: "mazeworld.html (module: the panel instance)"
      to: "src/browser/engineAdapter.js#getBests / #getGraveyard"
      via: "readData()"
      pattern: "getGraveyard\\(\\)"
    - from: "mazeworld.html (module: the panel instance)"
      to: "src/browser/boardsView.js#boardsView"
      via: "buildView: boardsView"
      pattern: "buildView: boardsView"
---

<objective>
Make the DEAD tab the Leaderboards panel. Implements D-14 (mount src/browser/boardsPanel.js through a registered bridge, replace #screen-dead's markup, delete the classic graveyard renderers and the "Showing last" copy, and move the title gate to the new data seam), D-02 (tab entry keeps the tab bar with DEAD active and no chevron), D-04 (the DEAD tab opens on the last board viewed), D-08 (INTERRED from the adapter's lifetime total), D-15 (the renderer is fed only by boardsView over the adapter's in-memory records), the D-01 routes the title-mode chrome will use, and D-16's shell tests through the shellSandbox/recordingDom harness.

Purpose: the player sees the new panel on the DEAD tab, fully offline.
Output: mazeworld.html wiring and deletions, the bridge registry and docs, the sandbox wiring, and test/unit/shell-boards-panel.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/66-leaderboards-panel-local/66-01-SUMMARY.md
@.planning/phases/66-leaderboards-panel-local/66-03-SUMMARY.md
@.planning/phases/66-leaderboards-panel-local/66-04-SUMMARY.md
@.planning/phases/66-leaderboards-panel-local/66-05-SUMMARY.md

<interfaces>
- src/browser/engineAdapter.js (66-01): getBests() returns the in-memory BestsRecord or null; getGraveyard() returns { graves, total } or null.
- src/browser/boardsView.js (66-04): boardsView(input) returns the view object.
- src/browser/boardsPanel.js (66-03): createBoardsPanel({ host, buildView, readData, prefs, reducedMotion, onRoute }) returns frozen { openFromTab, openFromTitle, onDeadTab, back, isTitleOpen, centreRail, refresh, state }; onRoute(action, { hasHero }) with action "title" | "roll" | "dungeon".
- mazeworld.html module script (read current code: Phase 65-05 merged its own separate import lines and the window.__mzDeathRecord parcel just before this phase): the pinned engineAdapter import line, the prefersReducedMotion import from motion.js, the window.__mzTabs assignment region, window.mzReturnToTitle, surfaceWornReconcile(), showTitleScreen({ allowResume }), hideTitleScreen(), refreshTitleDead(), initTitleScreen, window.mzStartRoll.
- mazeworld.html classic script: showTab(name) inside initTabs (its name === "dead" branch re-fetches graves through the classic loader today), the graveyard block (loader, saver, the graves-count bridge, the loading placeholder, the stone renderer), window.__mzClassicBoot.
- tools/bridge-doc.mjs: `--write` regenerates the docs/SHELL-MODULES.md bridge table; `--check` verifies it.
- test/unit/bridge-registry.test.js: live names in mazeworld.html plus src/browser/*.js must set-equal BRIDGE; BRIDGE is alphabetical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: empty #screen-dead, delete the classic graveyard screen, and hook showTab's DEAD branch to the bridge</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html #screen-dead markup (search `id="screen-dead"`) and the DEAD comment above it
    - mazeworld.html classic CSS for the old graveyard (search `.yard-wrap{`, `.yard{`, `.stone{`, `.stone-`, `.yard-loading{`, and the ~line 203 comment that mentions `.yard-head h2 span`)
    - mazeworld.html classic graveyard block (search "the graveyard ----------------": the loader/saver sentinel block, the graves-count bridge, the loading placeholder and the stone renderer) and window.__mzClassicBoot
    - mazeworld.html classic showTab (search "function showTab(name)"), especially the E9 comment and the name === "dead" branch at its end
    - test/unit/hud-menu-layout.test.js test (13) (it calls __mzShowTab("dead") in the sandbox and pins the #screen-dead padding line)
  </read_first>
  <action>
Per D-14 (greenfield: delete, never keep alongside):

1. Markup: reduce #screen-dead to its own section tag (keep class "mw-screen", id "screen-dead", data-screen "dead" and hidden) with no children, and replace the old "graveyard presentation shell" comment with: Phase 66 (BOARD-01, D-14), the Leaderboards panel mounts here, rendered by src/browser/boardsPanel.js through window.__mzBoards.

2. CSS: delete the old graveyard rules: .yard-wrap, the three .yard-head rules, .yard, .yard-empty, the multi-line .stone rule, every .stone-* rule, and .yard-loading. Reword the ~line 203 comment so it no longer names the deleted .yard-head selector. Check whether .mw-empty/.mw-empty-head/.mw-empty-body/.mw-error have any other user in the shell or src/browser; delete them only if the classic stone renderer was their last user, and record the finding in the SUMMARY.

3. Classic script: delete the loading-placeholder function and the stone-renderer function together with their comments (the "Showing last 5 of N" count copy and the "NOBODY'S DOWN THERE YET" / "THE LEDGER WON'T OPEN" states go with them; the panel's empty state replaces them per D-12). Leave the loader/saver sentinel block in place for 66-07. window.__mzClassicBoot becomes: await the classic loader, then fit(), then paint(); update its comment (the Phase 66 panel renders on the DEAD tab; the loader remains only until 66-07 retires it).

4. showTab: replace the E9 comment and the DEAD re-fetch branch at the end of showTab with `if (name === "dead") window.__mzBoards?.onDeadTab?.();` and a comment: Phase 66 (BOARD-01, D-02/D-04/D-15), the DEAD tab opens the module-owned Leaderboards panel, which reads the adapter's in-memory bests and graveyard synchronously (a death is already folded in by the time the tab opens), so there is no storage re-fetch; optional-chained because the initial showTab("maze") runs before the module script assigns the bridge.

Leave the classic graves-count bridge in place in this task; Task 2 deletes it together with its registry entry, so every task commit keeps the bridge registry test green.
  </action>
  <verify>
    <automated>node --test test/unit/hud-menu-layout.test.js test/unit/shell-tab-snapshots.test.js test/unit/boards-css.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "window.__mzBoards?.onDeadTab?.();" mazeworld.html` equals 1
    - `grep -c 'id="screen-dead"' mazeworld.html` equals 1
    - `node --test test/unit/hud-menu-layout.test.js test/unit/boards-css.test.js` passes (test 13's DEAD off-tab behaviour and the untouched padding pin still hold)
    - The absence of the deleted markup, CSS rules, renderers and copy is pinned by test/unit/shell-boards-panel.test.js in Task 3
  </acceptance_criteria>
  <done>The DEAD screen is an empty mount point, the classic graveyard screen is deleted outright, and the tab switch hands the DEAD tab to the bridge.</done>
</task>

<task type="auto">
  <name>Task 2: the module-side panel: imports, instance, routeFromBoards, window.__mzBoards, the title gate on getGraveyard, and the bridge registry and docs</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - mazeworld.html module imports (search `import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from "./src/browser/engineAdapter.js";`; that line stays byte-identical, and 65-05's separate takeDeathRecord/newBestView import lines follow it)
    - mazeworld.html module bridge region (search "window.__mzTabs = Object.freeze(") and the prefersReducedMotion import
    - mazeworld.html title-screen functions: hideTitleScreen, hasActiveDelveSave, refreshTitleDead, showTitleScreen, window.mzReturnToTitle, surfaceWornReconcile, initTitleScreen
    - src/browser/bridge.js (the header comment, an existing module-owned entry such as __mzTabs, the __mzGravesCount and __mzShowTab entries, alphabetical order)
    - tools/bridge-doc.mjs (header: --write / --check)
    - docs/SHELL-MODULES.md (intro paragraph, "### Screens", "### Gear action sheet (Phase 63)")
  </read_first>
  <action>
1. Module imports, each on its own line with a "Phase 66 (BOARD-01..08)" comment, after the Phase 65 engineAdapter import lines (the pinned line stays byte-identical):
   - `import { getBests, getGraveyard } from "./src/browser/engineAdapter.js";`
   - `import { createBoardsPanel } from "./src/browser/boardsPanel.js";`
   - `import { boardsView } from "./src/browser/boardsView.js";`

2. Directly after the `window.__mzGearSheet = renderGearSheet;` line in the bridge region, create the panel (D-14, D-15):
   - `const boardsPanel = createBoardsPanel({ host: document.getElementById("screen-dead"), buildView: boardsView, readData: readBoardsData, prefs: boardsPrefs(), reducedMotion: () => prefersReducedMotion(window), onRoute: routeFromBoards });`
   - `window.__mzBoards = Object.freeze({ onDeadTab: boardsPanel.onDeadTab });`
   - Two module-level function declarations beside it: readBoardsData() returns { bests: getBests(), graves: the snapshot's graves or [], total: the snapshot's total or 0 } from one getGraveyard() call; boardsPrefs() returns window.localStorage inside try/catch (null when access throws), the D-04 per-viewer convenience store.
   Comment the block: Phase 66 (D-14/D-15), the Leaderboards panel reads only the adapter's in-memory records through boardsView; presentation only, never a field on state.

3. `function routeFromBoards(action, { hasHero } = {})`, declared right after window.mzReturnToTitle (D-01; the controller has already cleared its title marker before calling it):
   - "title": `window.__mzShowTab?.("maze")`, then `showTitleScreen({ allowResume: !!hasHero })`. Passing allowResume preserves the title's own resume decision; recomputing it from S would misread boot()'s throwaway fresh-run character as a live hero.
   - "dungeon": `window.__mzShowTab?.("maze")`, then `surfaceWornReconcile()` (exactly what ENTER does on resume).
   - "roll": `window.mzStartRoll()` (the roller's onCommit already lands on the map).
   - Anything else does nothing.

4. refreshTitleDead (D-14's title gate moves to the new data seam): read the lifetime total from getGraveyard() (0 when it is null) instead of the classic bridge, and reword its DR18 comment and initTitleScreen's related comment accordingly. Then delete the classic graves-count bridge definition and its comment from the classic script (it has no consumer left); the classic loader/saver sentinel block itself stays until 66-07.

5. src/browser/bridge.js: add a frozen `__mzBoards` entry in alphabetical position (after __mzBeat, before __mzCameraGlide): owner "mazeworld.html (module)"; consumers ["mazeworld.html (classic: showTab — the DEAD tab opens the Leaderboards panel, or re-centres its rail when the panel is already open from the title)"]; purpose "Bridges the module-owned Leaderboards panel (src/browser/boardsPanel.js over boardsView.js and the adapter's in-memory bests record and graveyard) so the classic tab switch opens it without importing a module; presentation only, never a field on state." Delete the graves-count entry. Append "mazeworld.html (module: routeFromBoards — back to the map when the panel's title mode exits)" to __mzShowTab's consumers. Then run `node tools/bridge-doc.mjs --write` (never hand-edit the table).

6. docs/SHELL-MODULES.md: mention boardsPanel.js and boardsView.js in the intro paragraph, and add a "### Leaderboards panel (Phase 66)" section after "### Gear action sheet (Phase 63)" describing: boardsView(input) as the pure D-15 seam (inputs and the view contract's top-level fields); createBoardsPanel's seams and frozen API; the two entry modes (tab: tab bar visible, no chevron; title: body data-boards-entry="title" hides the tab bar and rail, chevron + dock); the ddr.boards.last.v1 convenience key; window.__mzBoards as the one bridge; and that Phases 67/68 add sign-in and global sources behind the same seam.
  </action>
  <verify>
    <automated>node tools/bridge-doc.mjs --check && node --test test/unit/bridge-registry.test.js test/unit/stale-terms.test.js test/unit/shell-no-content-copies.test.js test/unit/dressing-shell.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { getBests, getGraveyard } from "./src/browser/engineAdapter.js";' mazeworld.html` equals 1, and the pinned line `import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from "./src/browser/engineAdapter.js";` still counts exactly 1
    - `grep -c "buildView: boardsView" mazeworld.html` equals 1; `grep -c "window.__mzBoards = Object.freeze({ onDeadTab: boardsPanel.onDeadTab });" mazeworld.html` equals 1; `grep -c "function routeFromBoards(action" mazeworld.html` equals 1
    - `grep -c "__mzBoards" src/browser/bridge.js` is at least 1 and `grep -c "__mzBoards" docs/SHELL-MODULES.md` is at least 2 (table row and prose)
    - `node tools/bridge-doc.mjs --check` exits 0 and `node --test test/unit/bridge-registry.test.js` passes
  </acceptance_criteria>
  <done>The module owns one panel instance fed by the adapter through boardsView, the DEAD tab reaches it through one registered bridge, the title gate reads the adapter, and the docs describe the new screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: wire the real panel into the shell sandbox and pin the DEAD tab end to end</name>
  <files>test/unit/harness/shellSandbox.js, test/unit/shell-boards-panel.test.js</files>
  <read_first>
    - test/unit/harness/shellSandbox.js (wireBridges, loadShellSandbox's options, the "never stub a bridge with a dummy" rule, the fake localStorage in the sandbox)
    - test/unit/hud-menu-layout.test.js (freshSandbox usage around test 13)
    - test/unit/shell-combat-over.test.js (comment stripping and function-region extraction helpers)
    - mazeworld.html (Tasks 1 and 2's changes)
  </read_first>
  <behavior>
    - Sandbox: with boards data { bests: a record of 12 runs built by updateBests, graves: those 12 stones newest first, total: 37 }, `__mzShowTab("dead")` leaves `#screen-dead` holding one `.mw-bd` root; `.mw-bd-interred-n` reads "37"; there are seven `.mw-bd-chip`; the active chip is "deep"; there are ten `.mw-bd-row`; there is no `.mw-bd-back`; `.mw-bd-dock` is hidden; the body has no boardsEntry marker; `#mw-hud` still has data-offtab "1"
    - A LEANEST chip onclick makes "lean" active and stores "lean" under ddr.boards.last.v1 in the sandbox localStorage; showTab("maze") then showTab("dead") reopens on "lean"
    - A row onclick renders exactly one `.mw-bd-detail` with six `.mw-bd-stat`; another row's onclick still leaves exactly one; the same row again leaves none
    - With empty data (null bests, no graves, total 0) the DEAD tab shows INTERRED "0", a `.mw-bd-empty` line and a NO ENTRY standing card
    - routeFromBoards (extracted from the module script by region and evaluated with fake window/showTitleScreen/surfaceWornReconcile): "title" with hasHero true calls __mzShowTab("maze") then showTitleScreen({ allowResume: true }); "title" with hasHero false passes allowResume false; "dungeon" calls __mzShowTab("maze") then surfaceWornReconcile(); "roll" calls mzStartRoll(); "nope" calls nothing
    - Source pins over comment-stripped code: the classic script defines neither deleted graveyard render function (the loading placeholder, the stone renderer) and no graves-count bridge; the markup has no #yard or #yard-count element and #screen-dead has no children; the style block has no .yard or .stone rules; the "Showing last" copy is gone; showTab's DEAD branch calls window.__mzBoards?.onDeadTab?.(); refreshTitleDead calls getGraveyard(); the module script contains the three Phase 66 import lines
    - Zero network (BOARD-08): the comment-stripped mazeworld.html contains no fetch(, XMLHttpRequest, WebSocket, EventSource or sendBeacon
  </behavior>
  <action>
1. test/unit/harness/shellSandbox.js: import createBoardsPanel (../../../src/browser/boardsPanel.js) and boardsView (../../../src/browser/boardsView.js). loadShellSandbox gains an option `boards = null` ({ bests, graves, total }). Build the real panel over the sandbox document exactly as the module script does: host = the document's #screen-dead, buildView = boardsView, readData returning the option's data (default { bests: null, graves: [], total: 0 }), prefs = the sandbox's fake localStorage, reducedMotion always true, and onRoute pushing { action, hasHero } into an array. Assign context.window.__mzBoards = Object.freeze({ onDeadTab: panel.onDeadTab }). Return the panel and the route array on the sandbox result as `boardsPanel` and `boardsRoutes`. Comment it in the file's style: the REAL panel and view model, only the data source injected (like the dressing art source).

2. Create test/unit/shell-boards-panel.test.js covering every behaviour bullet. Use the comment-stripping and region-extraction helpers from test/unit/shell-combat-over.test.js, read mazeworld.html with CRLF normalised, and build fixture runs with engine/records.js (runHash, emptyBests, updateBests).
  </action>
  <verify>
    <automated>node --test test/unit/shell-boards-panel.test.js test/unit/hud-menu-layout.test.js test/unit/shell-tab-snapshots.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-boards-panel.test.js` passes with at least 12 tests, including the sandbox behaviour tests, the routeFromBoards extraction tests, the deletion pins and the network pin
    - `grep -c "createBoardsPanel" test/unit/harness/shellSandbox.js` is at least 1
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The DEAD tab renders the real panel over real view-model output in the shell sandbox, its interactions and routes are proven, and the old screen's absence is pinned.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures), including bridge-registry, stale-terms, hud-menu-layout and parity
- `node tools/bridge-doc.mjs --check` exits 0
- No Android build, APK or device step
</verification>

<success_criteria>
- BOARD-01 (tab half): the panel replaces the DEAD screen and opens from the DEAD tab
- D-14: the classic screen is deleted, one bridge is registered and documented
- BOARD-08: the panel runs on local data with zero network references
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `window.__mzBoards` | module-owned bridge | `{ onDeadTab }`; registered in src/browser/bridge.js, documented in docs/SHELL-MODULES.md |
| `boardsPanel` (module const) | shell wiring | the one createBoardsPanel instance over #screen-dead |
| `readBoardsData()`, `boardsPrefs()` | module functions | the adapter read and the D-04 prefs store |
| `routeFromBoards(action, { hasHero })` | module function | "title" / "dungeon" / "roll" |
| Phase 66 module imports | shell wiring | getBests/getGraveyard, createBoardsPanel, boardsView (separate lines) |
| `boards` option, `boardsPanel`/`boardsRoutes` results | test harness | test/unit/harness/shellSandbox.js |
| test/unit/shell-boards-panel.test.js | new test file | |
| deleted: #screen-dead's old children, .yard*/.stone* CSS, the classic stone renderer and loading placeholder, the graves-count bridge | greenfield removals | |

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-06-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch (docs/UAT-v2.0.md): (1) the DEAD tab shows the panel with the tab bar visible and DEAD lit, no chevron; (2) after a death, the DEAD tab lists the new run at once and INTERRED went up by one; (3) the tab reopens on the last board viewed; (4) every board renders, empty or not, with the panel working in airplane mode; (5) the title's View the Dead appears after the first-ever death without a restart. Do NOT create `.planning/phases/66-*/deferred-items.md`.
</output>
