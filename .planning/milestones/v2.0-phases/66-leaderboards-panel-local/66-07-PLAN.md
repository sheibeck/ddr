---
phase: 66-leaderboards-panel-local
plan: 07
type: execute
wave: 4
depends_on: ["66-06"]
files_modified:
  - mazeworld.html
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
  - src/browser/storage.js
  - test/persistence/dual-write-convergence.test.js
  - test/persistence/harness/sandboxClassicPersistence.js
  - test/unit/shell-boards-entry.test.js
autonomous: true
requirements: [BOARD-01, BOARD-08]
user_setup: []

must_haves:
  truths:
    - "The title's VIEW THE DEAD opens the panel in title mode on GRAVEYARD: the title screen hides, the game tab bar and rail are hidden, the header shows the ◀ chevron, and the bottom bar shows BACK TO TITLE + ROLL A NEW HERO with no live hero, or a single BACK TO THE DUNGEON with one (BOARD-01, D-01, D-04)"
    - "The chevron and the bottom bar return to wherever the panel came from: the title (keeping its resume decision), the dungeon (on the map, as ENTER does on resume), or the roller; the tab bar is visible again the moment title mode ends (D-01)"
    - "The Android back button on a title-opened panel does exactly what the chevron does and nothing else: it never clears a live run's combat, store or beat state (D-03)"
    - "On a tab-opened panel, the Android back button keeps its existing behaviour (nativeChrome.js is unchanged) (D-03)"
    - "The classic graveyard loader, saver and their extraction sentinel are deleted; the adapter is the only reader and writer of ddr.graveyard.v1 and ddr.graveyard.total.v1, and the persistence suite proves the adapter's read goes through the shared storage abstraction (D-14)"
    - "Boot still fits and paints the map before the title shows; the title gate reads the adapter"
  prohibitions:
    - statement: "Must not strand the player: every exit from a title-opened panel (chevron, dock, Android back) leads to the title, the dungeon or the roller, and the tab bar never stays hidden after title mode ends"
      status: unresolved
      verification: judgment
    - statement: "Must not make any network call on any panel entry or exit path; signed out, the whole flow is local"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "mazeworld.html"
      provides: "the title-mode VIEW THE DEAD entry, the back-button branch in getGameContext, the slimmed classic boot"
      contains: "boardsPanel.openFromTitle({ hasHero: resumeIntent })"
    - path: "test/unit/shell-boards-entry.test.js"
      provides: "title entry, chevron/dock/back routing and classic-loader retirement pins"
      min_lines: 110
    - path: "test/persistence/dual-write-convergence.test.js"
      provides: "the adapter-side graveyard read convergence test replacing the classic extraction test"
      contains: "loadGraveyard"
  key_links:
    - from: "mazeworld.html (module: initTitleScreen — #mw-title-dead onclick)"
      to: "src/browser/boardsPanel.js#openFromTitle"
      via: "hideTitleScreen(); boardsPanel.openFromTitle({ hasHero: resumeIntent }); window.__mzShowTab?.('dead')"
      pattern: "openFromTitle\\(\\{ hasHero: resumeIntent \\}\\)"
    - from: "mazeworld.html (module: getGameContext)"
      to: "src/browser/boardsPanel.js#back"
      via: "hasOpenModal includes isTitleOpen(); closeModal's first statement routes through back()"
      pattern: "boardsPanel\\.isTitleOpen\\(\\)"
---

<objective>
Finish BOARD-01's second entry and retire the last classic graveyard code. Implements D-01 (the title's VIEW THE DEAD opens the panel in title mode with the chevron and the bottom bar, the tab bar hidden), D-03 (the Android back button mirrors the chevron on a title-opened panel and leaves tab behaviour alone), D-04 (a title entry opens on GRAVEYARD) and the remainder of D-14 (the classic loader/saver and their persistence harness are deleted; the adapter is the one graveyard reader), with D-16's entry and routing tests.

Purpose: the panel's back affordances return to wherever it was opened from, and no dual graveyard path remains.
Output: mazeworld.html entry and back wiring plus deletions, the bridge registry/doc touch, the persistence test update, and test/unit/shell-boards-entry.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/66-leaderboards-panel-local/66-03-SUMMARY.md
@.planning/phases/66-leaderboards-panel-local/66-06-SUMMARY.md

<interfaces>
- 66-06 left in mazeworld.html's module script: `const boardsPanel = createBoardsPanel({...})`, `window.__mzBoards = Object.freeze({ onDeadTab: boardsPanel.onDeadTab })`, `function routeFromBoards(action, { hasHero } = {})`, and refreshTitleDead reading getGraveyard().
- boardsPanel (66-03): openFromTitle({ hasHero }) sets entry "title", board "yard" and the body marker, then renders; onDeadTab() only re-centres the rail while title mode is open; back() routes hasHero ? "dungeon" : "title" and returns true in title mode, false otherwise; isTitleOpen().
- The module's title code: resumeIntent (a `let` holding what ENTER would do: resume a live delve or roll), hideTitleScreen(), initTitleScreen (wires #mw-title-dead's onclick to hideTitleScreen() then __mzShowTab("dead") today).
- getGameContext (inside the registerNativeChrome call at the end of the module): hasOpenModal, hasLiveRun, isAtRoot, closeModal (its first statement closes the ☰ menu), navigateBack, showConfirmQuit. src/browser/nativeChrome.js#decideBackAction runs close-modal first whenever hasOpenModal is true.
- The classic graveyard sentinel block (search "@gsd:dual-write-convergence-extract:graves:start") still holds the loader and saver, and window.__mzClassicBoot still awaits the loader, after 66-06.
- test/persistence/harness/sandboxClassicPersistence.js extracts that sentinel block; test/persistence/dual-write-convergence.test.js is its only user.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VIEW THE DEAD opens the panel in title mode, and the Android back button mirrors the chevron</name>
  <files>mazeworld.html, test/unit/shell-boards-entry.test.js</files>
  <read_first>
    - mazeworld.html module: initTitleScreen (the #mw-title-dead onclick and its DR18 comment), resumeIntent's doc comment, hideTitleScreen, the boardsPanel instance and routeFromBoards from 66-06
    - mazeworld.html module: the registerNativeChrome call and its getGameContext (hasOpenModal, closeModal)
    - src/browser/nativeChrome.js (decideBackAction and the backButton listener; read only, it does not change)
    - src/browser/boardsPanel.js (openFromTitle, onDeadTab, back, isTitleOpen)
    - test/unit/shell-combat-over.test.js (comment-stripping and region extraction helpers)
    - test/unit/harness/recordingDom.js (to drive a real createBoardsPanel in the routing test)
  </read_first>
  <behavior>
    - Region pin: the #mw-title-dead onclick calls hideTitleScreen(), then boardsPanel.openFromTitle({ hasHero: resumeIntent }), then window.__mzShowTab?.("dead"), in that order (title mode is set before the tab switch, so onDeadTab only re-centres)
    - Region pin: getGameContext's hasOpenModal expression includes boardsPanel.isTitleOpen(); closeModal's first statement is `if (boardsPanel.isTitleOpen()) { boardsPanel.back(); return; }`, placed before the ☰ menu escape and before any S mutation
    - src/browser/nativeChrome.js is unchanged by this plan (git diff of that file is empty)
    - Routing, with a real createBoardsPanel over createRecordingDocument and the real boardsView: openFromTitle({ hasHero: false }) renders the chevron and two dock buttons (title, roll) on GRAVEYARD and sets body.dataset.boardsEntry "title"; the chevron's onclick routes ("title", { hasHero: false }) and deletes the marker; openFromTitle({ hasHero: true }) renders one dock button (dungeon), and back() routes ("dungeon", { hasHero: true }) and returns true; after either, isTitleOpen() is false and a later onDeadTab() opens tab mode (no chevron, dock hidden)
    - The closeModal branch extracted from the module and evaluated with a fake boardsPanel (isTitleOpen true) and a fake S carrying beats and store: it calls back() once and leaves S.beats and S.store untouched
  </behavior>
  <action>
1. initTitleScreen (implements D-01, D-04): the #mw-title-dead onclick becomes hideTitleScreen(); boardsPanel.openFromTitle({ hasHero: resumeIntent }); window.__mzShowTab?.("dead"). Rewrite its DR18 comment: Phase 66 (BOARD-01, D-01/D-04), VIEW THE DEAD opens the Leaderboards panel in title mode on GRAVEYARD; hasHero is the title's own resume decision (ENTER's resumeIntent), never recomputed from S, because boot() leaves a throwaway fresh-run character in S that is not a live hero; title mode is set before the tab switch, so the DEAD branch only re-centres the rail.

2. getGameContext (implements D-03): prepend `boardsPanel.isTitleOpen() ||` to hasOpenModal, so a back press on a title-opened panel goes to close-modal. In closeModal, add as the FIRST statement: `if (boardsPanel.isTitleOpen()) { boardsPanel.back(); return; }`, commented: Phase 66 (D-03), the back button mirrors the chevron (to the dungeon when a live hero exists, else the title), and returns before the ☰ escape and before any S mutation, so a resumed run's combat, store or beats are never cleared by leaving the panel. A tab-opened panel is untouched: nativeChrome.js keeps its existing tab back behaviour.

3. Create test/unit/shell-boards-entry.test.js covering every behaviour bullet (read mazeworld.html with CRLF normalised; extract the closeModal arrow body by region for the evaluated test; build fixture data with engine/records.js).
  </action>
  <verify>
    <automated>node --test test/unit/shell-boards-entry.test.js test/unit/shell-boards-panel.test.js test/unit/boardsPanel.test.js test/persistence/back-button-logic.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "boardsPanel.openFromTitle({ hasHero: resumeIntent });" mazeworld.html` equals 1
    - `grep -c "if (boardsPanel.isTitleOpen()) { boardsPanel.back(); return; }" mazeworld.html` equals 1
    - `git diff --stat -- src/browser/nativeChrome.js` prints nothing
    - `node --test test/unit/shell-boards-entry.test.js` passes with at least 6 tests, including the evaluated closeModal test and the real-controller routing tests
  </acceptance_criteria>
  <done>VIEW THE DEAD opens the panel in title mode, and every exit (chevron, dock, Android back) returns the player to the title, the dungeon or the roller without touching a live run's state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: retire the classic graveyard loader, its sentinel and its persistence harness</name>
  <files>mazeworld.html, src/browser/bridge.js, docs/SHELL-MODULES.md, src/browser/storage.js, test/persistence/dual-write-convergence.test.js, test/persistence/harness/sandboxClassicPersistence.js, test/unit/shell-boards-entry.test.js</files>
  <read_first>
    - mazeworld.html classic: the sentinel-marked graveyard block (search "@gsd:dual-write-convergence-extract:graves:start" through its end marker) and window.__mzClassicBoot with its comment block
    - mazeworld.html module: the comment above `await window.__mzClassicBoot();` (it describes the boot as the graveyard load plus the first fit/paint)
    - test/persistence/dual-write-convergence.test.js (header comment and its third test, which drives the classic extraction)
    - test/persistence/harness/sandboxClassicPersistence.js (confirm dual-write-convergence.test.js is its only importer)
    - src/browser/storage.js (the ~line 46 comment naming the classic saver/loader)
    - src/browser/bridge.js (the __mzClassicBoot entry's purpose text)
    - test/unit/dressing-shell.test.js test (11) (it pins `await window.__mzClassicBoot();` in the module script; that line stays)
  </read_first>
  <behavior>
    - The comment-stripped classic script declares no top-level graves/gravesTotal/gravesLoadError bindings, no GRAVE_KEY or GRAVE_TOTAL_KEY constant and no loader/saver function, and neither sentinel comment remains anywhere in mazeworld.html
    - window.__mzClassicBoot's body is fit() then paint(), and the module still awaits it before the title screen initialises
    - test/persistence/dual-write-convergence.test.js: its classic-extraction test is replaced by one that writes a graveyard and a total through storage.setItem, calls the adapter's loadGraveyard(), and gets back the same stones and total, proving the adapter's read uses the shared storage instance; the engineAdapter no-raw-localStorage guard stays
    - test/persistence/harness/sandboxClassicPersistence.js no longer exists, and no file imports it
  </behavior>
  <action>
Per D-14 (greenfield, the adapter is the one graveyard owner):

1. mazeworld.html classic script: delete the whole sentinel-marked graveyard block (both sentinel comments and everything between them: the graveyard key constants, the three module-level bindings, the loader and the saver) together with the "the graveyard" section header comment above it. window.__mzClassicBoot keeps its name and async signature (the module awaits it and test/unit/dressing-shell.test.js pins that await) but its body is just fit() then paint(); rewrite its comment block: since Phase 66 the graveyard is read by the adapter (engineAdapter.js#loadGraveyard, awaited inside boot()), so the classic boot only sizes and paints the map. Update the module comment above `await window.__mzClassicBoot();` to match.

2. src/browser/bridge.js: update the __mzClassicBoot purpose to "Exposes the classic script's async boot routine (the first canvas fit and paint) so the module script can await it before running the title screen's own init." Run `node tools/bridge-doc.mjs --write`.

3. Persistence: in test/persistence/dual-write-convergence.test.js replace the classic-extraction test with the adapter graveyard read test from the behaviour list (import loadGraveyard from the adapter; keep withConvergedStorage), remove the sandbox import, and update the header comment (the classic script no longer reads or writes the graveyard; the adapter is the single owner). Delete test/persistence/harness/sandboxClassicPersistence.js.

4. src/browser/storage.js: comment-only update of the note that names the classic saver/loader (the adapter is now the only graveyard reader and writer). No executable change.

5. Extend test/unit/shell-boards-entry.test.js with the classic-retirement pins from the behaviour list.
  </action>
  <verify>
    <automated>node tools/bridge-doc.mjs --check && node --test test/persistence/ test/unit/shell-boards-entry.test.js test/unit/dressing-shell.test.js test/unit/bridge-registry.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/persistence/harness/sandboxClassicPersistence.js` fails (the file is deleted)
    - `grep -c "loadGraveyard" test/persistence/dual-write-convergence.test.js` is at least 2 (import and use)
    - `node tools/bridge-doc.mjs --check` exits 0
    - `node --test test/persistence/ test/unit/shell-boards-entry.test.js test/unit/dressing-shell.test.js` passes
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (the failing-test set must not grow)
  </acceptance_criteria>
  <done>No classic graveyard code remains; the adapter alone reads and writes the graveyard, and the persistence suite proves that read goes through the shared storage abstraction.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures), including persistence, bridge-registry, stale-terms and parity
- `node tools/bridge-doc.mjs --check` exits 0
- No Android build, APK or device step (deferred-UAT protocol)
</verification>

<success_criteria>
- BOARD-01 complete: the panel opens from the DEAD tab and from VIEW THE DEAD, and its back affordances (chevron, dock, Android back) return to wherever it was opened from
- D-14 complete: no classic graveyard code path remains
- BOARD-08: every entry and exit path is local, with zero network calls
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| #mw-title-dead onclick → `boardsPanel.openFromTitle({ hasHero: resumeIntent })` | shell wiring | D-01/D-04 title entry |
| getGameContext `hasOpenModal` / `closeModal` boards branch | shell wiring | D-03 |
| slimmed `window.__mzClassicBoot` | shell | fit + paint only |
| test/unit/shell-boards-entry.test.js | new test file | |
| updated test/persistence/dual-write-convergence.test.js | test | adapter graveyard read convergence |
| deleted: the classic graveyard sentinel block, test/persistence/harness/sandboxClassicPersistence.js | greenfield removals | |

## Flagged assumptions (unresolved edge rows)

- BOARD-01 (edge probe: unclassified): "returns to wherever it was opened from" is read as: tab entry goes back through the tab bar as today; title entry goes to the dungeon when the title's own resume decision says a live hero exists, else to the title; ROLL A NEW HERO goes to the roller. Entering the panel while another overlay is open cannot happen (the title screen covers the app).

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-07-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch (docs/UAT-v2.0.md): (1) with no live hero, View the Dead opens on GRAVEYARD with the chevron, BACK TO TITLE and ROLL A NEW HERO, and no tab bar; (2) BACK TO TITLE returns to the title, and ENTER there still rolls a new hero; (3) ROLL A NEW HERO opens the roller and lands on the map after commit; (4) after Save & quit, View the Dead shows a single BACK TO THE DUNGEON that resumes the same run on the map; (5) the Android back button on the title-opened panel does what the chevron does, and on the DEAD tab it behaves as before; (6) airplane mode changes nothing. Do NOT create `.planning/phases/66-*/deferred-items.md`.
</output>
