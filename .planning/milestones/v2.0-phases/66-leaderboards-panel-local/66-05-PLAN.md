---
phase: 66-leaderboards-panel-local
plan: 05
type: execute
wave: 2
depends_on: ["66-03"]
files_modified:
  - mazeworld.html
  - test/unit/boards-css.test.js
autonomous: true
requirements: [BOARD-01, BOARD-02, BOARD-03, BOARD-04, BOARD-06]
user_setup: []

must_haves:
  truths:
    - "Every class in boardsPanel.js's BOARDS_CLASSES has at least one CSS rule in mazeworld.html, ported from the mock's inline styles onto the dark palette (#14110c ground, #1b170f/#181409 bands, #3a3226/#2c2519 rules)"
    - "Every .mw-bd font size scales with the player's text-size setting (calc(<rem> * var(--mw-text-scale)), the mock's px divided by 16), like the Gear tab"
    - "The panel is a full-height column: header, strip, rail and board head stay put, the list scrolls on its own, and the title-mode dock sits pinned at the bottom with the safe-area inset"
    - "The board rail scrolls horizontally with its scrollbar hidden and never wraps its chips"
    - "The chevron is a 44x44 target (D-01); dock buttons are at least 48px tall; rows and chips use touch-action manipulation so a tap never waits on a double-tap gesture"
    - "While the panel is open from the title (body data-boards-entry='title'), the game tab bar and the rail are hidden (D-01); on tab entry nothing is hidden (D-02)"
    - "The row-expand rise and the value-bar width change are ordinary CSS animation/transition, so the existing blanket prefers-reduced-motion rule removes them (D-13)"
    - "The pinned #screen-dead safe-area padding-top rule is unchanged; the classic graveyard CSS is left for 66-06 to delete with its markup"
  artifacts:
    - path: "mazeworld.html"
      provides: "the .mw-bd-* panel CSS block, the #screen-dead layout reset, the mwrise keyframes and the title-mode tab bar/rail rules"
      contains: ".mw-bd-rail{"
    - path: "test/unit/boards-css.test.js"
      provides: "CSS pins: class coverage, text scale, layout, title-mode rules, the untouched padding pin"
      min_lines: 70
  key_links:
    - from: "test/unit/boards-css.test.js"
      to: "src/browser/boardsPanel.js#BOARDS_CLASSES"
      via: "every emitted class must have a rule"
      pattern: "BOARDS_CLASSES"
    - from: "body[data-boards-entry=\"title\"]"
      to: "#mw-tabbar and #mw-rail"
      via: "display:none while the panel is title-opened"
      pattern: "data-boards-entry=\"title\""
---

<objective>
Port the mock's Leaderboards panel styling into mazeworld.html as one `.mw-bd-*` CSS block. Implements D-13's styling half (rows, value bars, rail, tap-expand animation under the blanket reduced-motion rule), D-01's title-mode chrome (the 44x44 chevron, the dock, and the page marker that hides the tab bar and rail), D-02 (tab entry hides nothing) and the D-14 screen's layout.

CSS only: no markup, script or classic-renderer change, so the live DEAD tab keeps working until 66-06 swaps it (runs in wave 2 beside the pure view model, which touches no shared file).

Purpose: the panel looks like the mock and respects text size and reduced motion.
Output: the CSS block and test/unit/boards-css.test.js.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/66-leaderboards-panel-local/66-03-SUMMARY.md

<interfaces>
- src/browser/boardsPanel.js (66-03) exports BOARDS_CLASSES and emits: root `.mw-bd` (data-board, data-entry); `.mw-bd-scope-chip` and `.mw-bd-chip` with data-on "1"/"0" (scope chips also data-dim); `.mw-bd-row` with data-top/data-podium/data-you/data-open; `.mw-bd-val` with data-long "1"; `.mw-bd-dock-btn` with data-primary "1"/"0"; inline colours for the active chip, the mark, the avatar background and the top row's rank/value/bar/stripe.
- mazeworld.html layout facts: `#app.mw-app` is a 100vh flex column; `.mw-stage` is flex:1/min-height:0; `.mw-screens` is flex:1/min-height:0/overflow-y:auto; `.mw-screen` is display:block with padding 14px; the global `[hidden]{display:none!important}` rule exists; the global `button{}` rule sets padding 14px 16px, min-height 48px, a 2px border, a drop box-shadow and touch-action manipulation; the blanket `@media (prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}` exists; the text-scale token is `--mw-text-scale`.
- test/unit/hud-menu-layout.test.js (13) pins the exact line `#screen-dead{padding-top:calc(14px + var(--safe-area-inset-top, env(safe-area-inset-top, 0px)))}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the .mw-bd CSS block, the #screen-dead layout reset and the title-mode rules</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html CSS around the Dead-tab gate (search "#screen-dead{padding-top") and the Gear tab block (search ".mw-gear-head{") for the text-scale convention and comment style
    - mazeworld.html global rules: the `button{` block, `[hidden]{display:none!important}`, the prefers-reduced-motion blanket rule, `.mw-screens{`, `.mw-screen{`, `.mw-tabbar{`, `.mw-rail{`
    - design/Mazeworld Boards Panel.dc.html (every inline style in the markup ~18-143 and in renderVals: chip(), the scope chip style, the row/rank/handle/tag/name/val/bar styles, AVATAR's box)
    - design/Mazeworld Leaderboards.dc.html (the bottom bar buttons ~43-51)
    - src/browser/boardsPanel.js (BOARDS_CLASSES and the data attributes it sets)
  </read_first>
  <action>
Add one CSS block directly after the pinned `#screen-dead{padding-top:...}` line (leave that line byte-identical), headed by a comment: Phase 66 (BOARD-01..06, D-01/D-13/D-14), the Leaderboards panel, ported from design/Mazeworld Boards Panel.dc.html; the iOS frame is preview chrome only; every font size is calc(<mock px / 16>rem * var(--mw-text-scale)).

Port each mock inline style onto its class (colours, paddings, gaps, borders and inset box-shadows exactly as the mock has them; font family var(--disp) for Press Start 2P text and var(--mono) with weight 700 for Courier Prime values). The structural rules that matter:

1. Layout: a separate `#screen-dead{padding-left:0;padding-right:0;padding-bottom:0;height:100%}` line (the panel is edge to edge). `.mw-bd` is a flex column at height 100% with min-height 0, background #14110c and colour #e6ddc6. Head, strip, rail, board head and dock are flex:none; `.mw-bd-body` is flex:1, min-height 0, overflow-y auto, padding 0 0 26px.
2. Rail: `.mw-bd-rail` is a flex row with gap 5px, overflow-x auto, scrollbar-width none (plus a `::-webkit-scrollbar{display:none}` companion), padding 8px 14px 9px, bottom border 3px #3a3226, background #181409. `.mw-bd-chip` is flex:none, white-space nowrap, padding 7px 9px, min-height 36px, border 0, border-radius 0, font var(--disp) at 7px, colour #9a8f76, background #1b170f, box-shadow inset 0 0 0 1px #2c2519 (the active colours arrive inline).
3. Scope chips: `.mw-bd-scope-chip` mock style (padding 6px 8px, 6.5px var(--disp), #9a8f76 on #1b170f), min-height 36px, border 0; `[data-on="1"]` colour #14110c on #c9bda0; `[data-dim="1"]` opacity .45 (D-06's dimmed signed-out chips).
4. Chevron: `.mw-bd-back` is 44px by 44px, padding 0, border 0, border-radius 0, margin -6px 0 -6px -8px, colour #e8c97a, background #14110c, box-shadow inset 0 0 0 2px #4a4032 (D-01).
5. Rows: `.mw-bd-row` flex row, gap 10px, align flex-start, padding 11px 14px 12px, cursor pointer, touch-action manipulation, box-shadow inset 0 -1px 0 #241f16; `[data-top="1"]` background #191509; `[data-you="1"]` background #1d1810 with the gold 3px stripe; rank colour #8e8369, `[data-podium="1"]` rank #c9bda0, the top row's rank at 21px; the handle #c9bda0 (#e8c97a for a you row) with ellipsis; the tag (you row: #14110c on #e8c97a with 3px 4px padding); the name at 7px var(--disp) #a89c82 (#c9bda0 on the top row); the line at 11px #8e8369; the bar track 3px #221d14 with inset #0f0d09; `.mw-bd-bar-fill` height 100%, background #4a3f2c, transition width .3s steps(6); the value at 20px (24px on the top row; 17px / 19px with data-long="1"); the unit at 6.5px var(--disp) #9a8f76; `.mw-bd-valcell` min-width 74px, right-aligned column.
6. Detail: `.mw-bd-detail` column, gap 6px, margin-top 4px, animation mwrise .22s steps(3); add `@keyframes mwrise{from{transform:translateY(8px);opacity:0}to{transform:translateY(0);opacity:1}}` (the mock's) next to the other keyframes if it does not already exist; stat chips per the mock.
7. Avatars: `.mw-bd-av` 30px square flex-centred, 9px var(--disp), colour #e6ddc6, box-shadow inset 0 0 0 1px rgba(0,0,0,.5), 0 0 0 1px #2c2519 (backgrounds arrive inline); `.mw-bd-av-nobody` background #1b170f, colour #6f6754.
8. Empty and note lines: `.mw-bd-empty` and `.mw-bd-note` share padding 18px 14px, 12px text, colour #8e8369 and a bottom inset rule.
9. Standing card and footnote per the mock (margin 14px 14px 0; place at 22px #e8c97a; footnote 11px #6f6754 at margin 10px 14px 0).
10. Dock: `.mw-bd-dock` flex row, gap 8px, padding 10px 14px calc(12px + var(--safe-area-inset-bottom, env(safe-area-inset-bottom, 0px))), top border 3px #3a3226, background #1b170f. `.mw-bd-dock-btn` flex 1, min-height 48px, the mock's bordered style (2px #4a4032, colour #c9bda0, transparent, box-shadow 0 4px 0 #0d0b08, 7.5px var(--disp)); `[data-primary="1"]` #14110c on #e8c97a with border #e8c97a and box-shadow 0 4px 0 #6b5c3c.
11. Title mode (D-01): `body[data-boards-entry="title"] #mw-tabbar{display:none}` and `body[data-boards-entry="title"] #mw-rail{display:none}`, each on its own line, with a comment: the panel opened from the title owns the screen; tab entry keeps the game tab bar (D-02).

Reset the global button styling on every panel button the way `.mw-tab` does (explicit border, border-radius 0, box-shadow, min-height). Do not add any transition or animation beyond items 5 and 6 (the blanket reduced-motion rule must be able to remove all of the panel's motion). Do not touch the classic graveyard CSS or markup in this plan.
  </action>
  <verify>
    <automated>node --test test/unit/hud-menu-layout.test.js test/unit/shell-tab-snapshots.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^\.mw-bd-rail{" mazeworld.html` equals 1 and that line contains `overflow-x:auto`
    - `grep -c "body\[data-boards-entry=\"title\"\] #mw-tabbar{display:none}" mazeworld.html` equals 1
    - `grep -cF "#screen-dead{padding-top:calc(14px + var(--safe-area-inset-top, env(safe-area-inset-top, 0px)))}" mazeworld.html` equals 1 (the pinned line is unchanged; working-tree files are CRLF, so no end-of-line anchor)
    - `node --test test/unit/hud-menu-layout.test.js test/unit/shell-tab-snapshots.test.js` passes unedited
  </acceptance_criteria>
  <done>mazeworld.html carries the full panel stylesheet, the title-mode rules and the layout reset, and nothing else in the shell changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the panel CSS pin test</name>
  <files>test/unit/boards-css.test.js</files>
  <read_first>
    - test/unit/gear-tab-dom.test.js (the CSS-block pin style: HTML read with CRLF normalised, every font-size scales with --mw-text-scale)
    - mazeworld.html (Task 1's block)
    - src/browser/boardsPanel.js (BOARDS_CLASSES)
  </read_first>
  <behavior>
    - Every name in BOARDS_CLASSES appears as a selector (".name" followed by "{", ",", ":", "[", " " or ".") somewhere in mazeworld.html's style block
    - Every rule whose selector starts with .mw-bd and that declares a font-size uses calc( ... var(--mw-text-scale))
    - `.mw-bd` declares display:flex, flex-direction:column and height:100%; `.mw-bd-body` declares overflow-y:auto and min-height:0; `.mw-bd-rail` declares overflow-x:auto
    - `.mw-bd-back` declares width:44px and height:44px; `.mw-bd-dock-btn` declares min-height:48px; `.mw-bd-row` declares touch-action:manipulation
    - The two title-mode rules exist exactly
    - The pinned #screen-dead padding-top line still exists exactly, and a separate #screen-dead rule zeroes the side and bottom padding
    - The blanket prefers-reduced-motion rule still exists, and the only .mw-bd rules declaring animation or transition are .mw-bd-detail and .mw-bd-bar-fill
    - `@keyframes mwrise` is defined exactly once
  </behavior>
  <action>
Create test/unit/boards-css.test.js covering every behaviour bullet. Read mazeworld.html with CRLF normalised, isolate the first style block, and import BOARDS_CLASSES from ../../src/browser/boardsPanel.js. Mirror the helper style of test/unit/gear-tab-dom.test.js. Name the file header "Phase 66 (BOARD-01..06, D-01/D-13)".
  </action>
  <verify>
    <automated>node --test test/unit/boards-css.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/boards-css.test.js` passes with at least 8 tests
    - `npm test` reports 0 failures, or only the pre-existing worktree CRLF doc-ledger failures named in the Phase 65 SUMMARYs (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The panel's CSS is pinned: every emitted class is styled, text scales, the layout and title-mode rules hold, and reduced motion removes all of its motion.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only the documented pre-existing worktree CRLF failures)
- No Android build, APK or device step
</verification>

<success_criteria>
- The panel CSS exists and is pinned before any markup swaps, so 66-06 is a wiring change only
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `.mw-bd*` rules | CSS block in mazeworld.html | one rule set per BOARDS_CLASSES entry |
| `#screen-dead{padding-left:0;...}` | CSS line | edge-to-edge layout; the pinned padding-top line untouched |
| `@keyframes mwrise` | CSS | the mock's row-expand rise |
| `body[data-boards-entry="title"] #mw-tabbar` / `#mw-rail` | CSS | hidden in title mode (D-01) |
| test/unit/boards-css.test.js | new test file | |

<output>
Create `.planning/phases/66-leaderboards-panel-local/66-05-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: the panel matches the mock's look at text size M and stays usable at the largest text size (header, strip and rail do not crowd out the list); the chips are comfortable to tap. Do NOT create `.planning/phases/66-*/deferred-items.md`.
</output>
