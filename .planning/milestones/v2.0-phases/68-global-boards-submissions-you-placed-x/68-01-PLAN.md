---
phase: 68-global-boards-submissions-you-placed-x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/scoreTag.js
  - test/unit/scoreTag.test.js
  - content/leaderboards.js
  - src/browser/boardScores.js
  - test/unit/boardScores.test.js
  - docs/PLAY-GAMES-SETUP.md
  - test/unit/play-games-runbook.test.js
autonomous: true
requirements: [PGS-03, PGS-06]
user_setup: []

must_haves:
  truths:
    - "encodeTag(summary) always returns a string of at most 64 characters drawn only from A-Z a-z 0-9 - . _ ~ in the fixed field order v1.race.sub.lvl.cause.floor.day.steps.kills.gold.sp.name, carrying no epitaph and no free text other than the adventurer name (D-01; 67 D-18)"
    - "The name rides at full length when it fits the budget left after the numeric prefix (64 minus the prefix length, never less than 16), else as 'First L.', else as the first name hard-truncated to the budget; decodeTag turns '_' back into spaces (67 D-18 truncation rule)"
    - "decodeTag never throws: null, '', a tag over 64 characters, a character outside the charset, an unknown version, fewer than 12 fields, or a non-numeric/over-long number field all return null; an out-of-range race, sub or cause index decodes to '' for that field"
    - "boardScore follows D-16 exactly: deep = floor x 1,000,000 - steps (steps clamped to 0..999,999, result never below 0); lean = Math.round(1000 x steps / max(floor, 1)) on a smaller-is-better board; days = day x 1000 + min(floor, 999); kills = kills x 1000 + min(floor, 999); purse = gold; combo, yard and unknown boards return null (D-01, D-17)"
    - "LEADERBOARD_IDS is a frozen { [season]: { deep, lean, days, kills, purse } } map holding placeholder ids for season 1; leaderboardId(ids, season, board) returns null for a placeholder, empty, missing or malformed id, a missing season, and LINEAGE/GRAVEYARD, so the caller skips that board silently (D-14)"
    - "A unit test fails whenever SEASON (content/season.js) has no entry in LEADERBOARD_IDS, which makes D-15's season-bump process (bump SEASON, add that season's five ids, keep old seasons) enforced rather than remembered"
    - "PGS-03 boundary (edge probe): each numeric tag field is clamped at its cap (level 99, floor 999, day 9,999, steps 99,999, kills 9,999, gold and sp 999,999) and one past the cap encodes the cap; the all-caps tag with the longest generated name is at most 64 characters; deep at steps 999,999 and 1,000,000 both encode floor x 1,000,000 - 999,999"
    - "PGS-03 adjacency (edge probe): two runs with identical board fields encode identical scores and identical tags; deep ties on floor are broken by fewer steps; LEANEST runs with equal squares-per-floor rates at different depths encode the same score (the documented single-score limit, D-16)"
    - "PGS-03 empty (edge probe): a summary with missing or null fields encodes a valid tag of zeros, unknown-index sentinels and an empty name, and boardScores({}) returns five zero-or-positive safe integers"
    - "PGS-03 encoding (edge probe): tag length is counted in UTF-16 code units of an ASCII-only string; the name is NFKD-normalized, spaces become '_', and every character outside A-Z a-z 0-9 _ - is dropped BEFORE the budget is measured"
    - "PGS-03 ordering (edge probe): TAG_RACES, TAG_SUBS and TAG_CAUSES are frozen, append-only literal lists whose sets match content/races.js, content/classes.js + content/flavor.js SUB_NOTE and content/epitaphs.js CAUSE_TEXT; SUBMIT_BOARDS is fixed as deep, lean, days, kills, purse"
    - "PGS-03 precision (edge probe): every score is a non-negative safe integer (inputs truncated and clamped to 0..999,999,999 first); lean rounds half up (1 step over 16 floors gives 63, 1 over 3 gives 333); scoreFallback(board, boardScore(board, s)) recovers the capped floor/steps/day/kills/gold"
  prohibitions:
    - statement: "Must not carry the epitaph, the death note, the seed, the run hash or any free text other than the truncated adventurer name in the public score tag"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/scoreTag.js"
      provides: "TAG_VERSION, TAG_MAX_LENGTH, TAG_RACES, TAG_SUBS, TAG_CAUSES, TAG_FIELD_CAPS, classOfSub, tagName, fitName, encodeTag, decodeTag"
      contains: "export function encodeTag"
    - path: "src/browser/boardScores.js"
      provides: "SUBMIT_BOARDS, SCORE_ORDER, boardScore, boardScores, scoreFallback, isRealLeaderboardId, leaderboardId, knownSeasons, devLeaderboardIds, leaderboardIdsFor, scoreOrdersFor"
      contains: "export function boardScore"
    - path: "content/leaderboards.js"
      provides: "LEADERBOARD_IDS, LEADERBOARD_PLACEHOLDER_PREFIX"
      contains: "LEADERBOARD_IDS"
    - path: "test/unit/scoreTag.test.js"
      provides: "tag round-trip, truncation, charset, caps, malformed-tag and list-freeze tests"
      min_lines: 150
    - path: "test/unit/boardScores.test.js"
      provides: "D-16 encodings, clamps, fallback round-trip, id lookup and season-map tests"
      min_lines: 120
  key_links:
    - from: "src/browser/scoreTag.js#decodeTag"
      to: "content/classes.js CLASSES"
      via: "classOfSub derives the LINEAGE class from the tag's sub-class (67-RESEARCH A4)"
      pattern: "CLASSES"
    - from: "src/browser/boardScores.js#leaderboardId"
      to: "content/leaderboards.js LEADERBOARD_IDS"
      via: "the default id map; placeholders resolve to null"
      pattern: "LEADERBOARD_IDS"
---

<objective>
Build the pure encoding layer every Phase 68 submission and global row relies on: the 64-character score tag, the five per-board score encodings, and the per-season leaderboard ID map. Implements D-01 (five submitted boards, a versioned tag, no epitaph; GRAVEYARD and LINEAGE never submitted), D-14 (the frozen season → five-ID map with placeholder IDs; a missing or placeholder ID resolves to null so that board is skipped silently), D-15 (the season-bump process, enforced by a test and written into docs/PLAY-GAMES-SETUP.md), D-16 (the exact encodings, including LEANEST's smaller-is-better single-score limit, and rows decoding their displayed values from the tag) and D-17 (GRAVEYARD stays local). It carries forward 67 D-18 (no epitaph in the tag, name truncation rule) and 67 D-19 (five PGS boards per season).

This plan qualifies the milestone's older wording "the adventurer's name and epitaph ride in the score tag": per 67 D-18 and 67-RESEARCH Pitfall 3, only the (possibly truncated) name rides in the tag; global rows show a cause line instead of an epitaph.

Research correction recorded here: the research counted 10 delimiters and a 17-character name budget, but 12 fields need 11 delimiters, so the worst-case name budget is 16. The budget is computed per tag (64 minus the actual prefix), so typical runs get more room.

Purpose: one tested, device-free definition of what goes over the wire, used by the queue (68-04), the global boards (68-05) and the panel (68-06).
Output: src/browser/scoreTag.js, src/browser/boardScores.js, content/leaderboards.js, the runbook update, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-RESEARCH.md

<interfaces>
Existing code (read the live files; they are authoritative):
- engine/death.js buildRunSummary(state, cause, when) returns { name, race, sub, cls, level, sp, floor, day, steps, gold, kills, cause, note, epitaph, when, season, seed, acts, hash }. `cause` is a CAUSE_TEXT key string (combat, starve, trap, teleport, fall, gorge, backfire, summon, maze, quake, potion, insanity, poison, abandon, entombed).
- engine/records.js: RANKED_BOARDS = ["deep", "lean", "days", "kills", "purse"]; BOARD_IDS adds "combo" and "yard".
- content/races.js RACES keys in order: Human, Elven, Dwarven, Wilmsry, Fridgian, Troll.
- content/flavor.js SUB_NOTE key order (24): Knight, Guard, Woodsman, Soldier, Barbarian, Master of Arms, Samurai, Bard, Pickpocket, Pilfer, Cat Burglar, Cutthroat, Cloaker, Ninja, Con Artist, Acrobat, Wizard, Warlock, Sorcerer, Court Mage, Illusionist, Cleric, Summoner, Apprentice.
- content/classes.js CLASSES: { "Magic User": { subs: [...] }, "Fighter": { subs }, "Thief": { subs } }.
- content/names.js NAMES: per race { first: [...], sur: [...] }; names contain only letters, spaces and hyphens; the longest combination is 21 characters ("Lithariel Silverbough"); Troll names may be a first name only.
- content/season.js SEASON = 1.
- docs/PLAY-GAMES-SETUP.md and test/unit/play-games-runbook.test.js are created by 67-02 (merged before this plan runs); the runbook test forbids the old working title case-insensitively, so this doc never names the shell file.

Plugin facts (the 0.5.0 definitions, read-only): leaderboard scores are long integers; scoreOrder values are "largerIsBetter" / "smallerIsBetter" and are fixed once a board is published; maxResults is 1..25 and there is no paging.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/browser/scoreTag.js, the 64-character score tag</name>
  <files>src/browser/scoreTag.js, test/unit/scoreTag.test.js</files>
  <read_first>
    - engine/death.js (buildRunSummary's field list)
    - engine/records.js (the house style for a pure, import-light module and its header comment)
    - src/browser/newBest.js (a small pure src/browser module and its never-throw posture)
    - content/races.js, content/flavor.js (SUB_NOTE), content/classes.js, content/epitaphs.js (CAUSE_TEXT), content/names.js
    - .planning/phases/67-play-games-integration-account-chip/67-RESEARCH.md ("Score-Tag Encoding")
  </read_first>
  <behavior>
    - TAG_VERSION is "v1", TAG_MAX_LENGTH is 64; TAG_RACES, TAG_SUBS and TAG_CAUSES are frozen literal arrays in the orders listed in the interfaces block (TAG_CAUSES: combat, starve, trap, teleport, fall, gorge, backfire, summon, maze, quake, potion, insanity, poison, abandon, entombed)
    - new Set(TAG_RACES) equals new Set(Object.keys(RACES)); new Set(TAG_SUBS) equals new Set(Object.keys(SUB_NOTE)) and equals the union of every CLASSES[cls].subs; every Object.keys(CAUSE_TEXT) key is in TAG_CAUSES; no list has duplicates
    - classOfSub("Court Mage") is "Magic User", classOfSub("Ninja") is "Thief", classOfSub("Bard") is "Fighter", classOfSub("Nope") is ""; every TAG_SUBS entry maps to exactly one class
    - tagName("Hilda Ferrow") is "Hilda_Ferrow"; tagName("  Anne-Marie   Voss ") is "Anne-Marie_Voss"; tagName("Ëlwyn Døre") is "Elwyn_Dre"; tagName(null) is ""
    - fitName("Lithariel_Silverbough", 16) is "Lithariel_S."; fitName("Hilda_Ferrow", 16) is "Hilda_Ferrow"; fitName("Abcdefghijklmnopqrstu", 16) is "Abcdefghijklmnop"; fitName("Ab_Cd", 0) is ""
    - encodeTag for { race: "Dwarven", sub: "Pickpocket", level: 3, cause: "combat", floor: 7, day: 22, steps: 431, kills: 19, gold: 4688, sp: 1180, name: "Hilda Ferrow" } is exactly "v1.2.8.3.0.7.22.431.19.4688.1180.Hilda_Ferrow"
    - encodeTag of a summary with every numeric field one past its cap equals encodeTag of the same summary at the caps; negatives, NaN, Infinity and non-numbers encode 0; 7.9 encodes 7
    - An unknown race encodes 9, an unknown sub 99, an unknown cause 99, and each decodes to ""
    - For every name produced by NAMES (every first x sur combination, and every bare first name), encodeTag at all caps has length at most 64 and matches /^[A-Za-z0-9._~-]*$/, and decodeTag(...).name is the full name or "First L." with spaces restored
    - decodeTag(encodeTag(s)) returns a frozen { v: 1, race, sub, cls, level, cause, floor, day, steps, kills, gold, sp, name } equal to the capped fields, with cls from classOfSub
    - decodeTag returns null for null, undefined, 42, "", "v2.2.8.3.0.7.22.431.19.4688.1180.X", an 11-field tag, a tag with "7a" in a number field, a number field of 7 digits, a 65-character tag, and a tag containing a space or "!"; a name field that itself contains "." (the "First L." form) decodes intact
    - Source pins (comment-stripped via tools/ident-sweep.mjs stripJs): no epitaph, note, seed, hash or acts field is read by encodeTag; no Date, Math.random, window., document. or network identifier
  </behavior>
  <action>
Create src/browser/scoreTag.js with a house-style header comment: Phase 68 (PGS-03, D-01, D-16; 67 D-18), the public 64-character score tag; pure, deterministic, never throws; only content data imports (content/classes.js for classOfSub); the index lists are append-only literals, never derived from content key order at runtime, because a reorder would change what every already-submitted tag means.

Exports:
1. TAG_VERSION = "v1"; TAG_MAX_LENGTH = 64; TAG_RACES, TAG_SUBS, TAG_CAUSES as frozen literal arrays (orders in the interfaces block and the behaviour list); TAG_FIELD_CAPS = frozen { level: 99, floor: 999, day: 9999, steps: 99999, kills: 9999, gold: 999999, sp: 999999 }; TAG_UNKNOWN = frozen { race: 9, sub: 99, cause: 99 }.
2. classOfSub(sub): the CLASSES key whose subs include sub, else "".
3. tagName(name): String(name ?? ""), NFKD-normalized, trimmed; runs of whitespace become a single "_"; every character outside A-Z a-z 0-9 _ - is dropped; repeated "_" collapse; leading and trailing "_" are trimmed.
4. fitName(tagged, budget): budget below 1 gives ""; the whole string when it fits; else, when there are at least two "_"-separated words, first word + "_" + the first character of the last word + "." when that fits; else the first word cut to budget characters (no ellipsis, 67 D-18).
5. encodeTag(summary): a non-object summary is treated as {}. Build the eleven prefix fields in order: "v1"; the TAG_RACES index or 9; the TAG_SUBS index or 99; then level, the TAG_CAUSES index or 99, floor, day, steps, kills, gold, sp, each as a non-negative integer (Math.trunc of a finite number, else 0) clamped to its cap and written in plain decimal without padding. Join them with "." and add a trailing "."; the name budget is TAG_MAX_LENGTH minus that prefix length; append fitName(tagName(summary.name), budget). Read only race, sub, level, cause, floor, day, steps, kills, gold, sp and name (no epitaph, per 67 D-18 and this plan's prohibition).
6. decodeTag(tag): return null unless tag is a string of length 1..64 matching the unreserved charset; split on "."; require at least 12 parts, parts[0] === "v1", and every one of parts[1..10] matching /^\d{1,6}$/; name = parts.slice(11).join(".") with "_" turned back into spaces; map the three indices through the lists (out of range gives ""); cls = classOfSub(sub). Return a frozen object with v: 1 and the twelve fields. Wrap the body in try/catch returning null.

Create test/unit/scoreTag.test.js covering every behaviour bullet. The NAMES sweep builds every name the way the name generator composes them (first + " " + sur, and the bare first names) and checks both a typical-value tag and an all-caps tag.
  </action>
  <verify>
    <automated>node --test test/unit/scoreTag.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function encodeTag" src/browser/scoreTag.js` equals 1 and `grep -c "export function decodeTag" src/browser/scoreTag.js` equals 1
    - `node --test test/unit/scoreTag.test.js` passes with at least 20 tests, including the exact worked-example tag, the NAMES sweep, the caps, the malformed-tag nulls and the list-set cross-checks
    - `node --test test/unit/bridge-registry.test.js test/unit/stale-terms.test.js` passes
  </acceptance_criteria>
  <done>A deterministic, URL-safe tag that always fits 64 characters, carries the row's details and the truncated name, and decodes defensively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: content/leaderboards.js and src/browser/boardScores.js, the encodings and the season ID map</name>
  <files>content/leaderboards.js, src/browser/boardScores.js, test/unit/boardScores.test.js</files>
  <read_first>
    - content/season.js (SEASON and its changelog comment)
    - content/boards.js (the content/ header style; content/ holds no functions)
    - test/determinism/content-is-pure-data.test.js (every content/ export must be pure data)
    - engine/records.js (RANKED_BOARDS, compareRuns for deep/lean/days/kills/purse)
    - src/browser/scoreTag.js (Task 1, for the house style)
  </read_first>
  <behavior>
    - LEADERBOARD_IDS is deep-frozen, its keys are season numbers, LEADERBOARD_IDS[1] is { deep: "PLACEHOLDER_DEEPEST_S1", lean: "PLACEHOLDER_LEANEST_S1", days: "PLACEHOLDER_LONGEST_S1", kills: "PLACEHOLDER_BUTCHERY_S1", purse: "PLACEHOLDER_PURSE_S1" }, and every season entry has exactly those five keys; LEADERBOARD_IDS[SEASON] exists (the D-15 guard)
    - SUBMIT_BOARDS deep-equals ["deep", "lean", "days", "kills", "purse"] and equals engine/records.js RANKED_BOARDS; SCORE_ORDER is { deep: "largerIsBetter", lean: "smallerIsBetter", days: "largerIsBetter", kills: "largerIsBetter", purse: "largerIsBetter" }
    - boardScore("deep", { floor: 7, steps: 431 }) is 6999569; steps 999999 and 1000000 both give floor x 1e6 - 999999; floor 0 with steps 5 gives 0 (never negative)
    - boardScore("lean", { floor: 8, steps: 1 }) is 125; { floor: 16, steps: 1 } is 63; { floor: 3, steps: 1 } is 333; floor 0 with steps 7 is 7000; { floor: 2, steps: 10 } and { floor: 4, steps: 20 } give the same score
    - boardScore("days", { day: 22, floor: 7 }) is 22007 and floor 1000 contributes 999; boardScore("kills", { kills: 19, floor: 7 }) is 19007; boardScore("purse", { gold: 4688 }) is 4688
    - boardScore("combo", s), boardScore("yard", s) and boardScore("nope", s) are null; missing fields count as 0; every score for inputs at 999,999,999 (and at 1e15, clamped) is a Number.isSafeInteger
    - boardScores(summary) is a frozen { deep, lean, days, kills, purse } in SUBMIT_BOARDS order
    - scoreFallback("deep", 6999569) is { floor: 7, steps: 431 }; ("days", 22007) is { day: 22, floor: 7 }; ("kills", 19007) is { kills: 19, floor: 7 }; ("purse", 4688) is { gold: 4688 }; ("lean", 125) is { rate: 0.125 }; a negative, fractional or non-finite raw score, or combo/yard, gives null; for 50 generated summaries scoreFallback(b, boardScore(b, s)) recovers the clamped fields for deep, days, kills and purse
    - isRealLeaderboardId("CgkI4a-Rz8YUEAIQAQ") and ("dev_deep_s1") are true; "", "PLACEHOLDER_DEEPEST_S1", "has space", 42 and a 129-character id are false
    - leaderboardId(LEADERBOARD_IDS, 1, "deep") is null (placeholder); with a map holding a real id it returns it; a missing season, a combo or yard board, or a non-object map returns null
    - knownSeasons() is [1]; knownSeasons({ 2: {...}, 1: {...}, x: 3 }) is [1, 2]
    - devLeaderboardIds() gives { 1: { deep: "dev_deep_s1", lean: "dev_lean_s1", days: "dev_days_s1", kills: "dev_kills_s1", purse: "dev_purse_s1" } }; leaderboardIdsFor({ native: true }) is LEADERBOARD_IDS and ({ native: false }) is the dev map
    - scoreOrdersFor(devLeaderboardIds()) maps each dev id to its SCORE_ORDER value ("dev_lean_s1" → "smallerIsBetter") and skips placeholder ids
  </behavior>
  <action>
1. Create content/leaderboards.js (D-14, D-15): header comment stating the rules: one frozen entry per season with exactly the five submitted boards (67 D-19: LINEAGE and GRAVEYARD get no PGS board); IDs come from the Play Console (the Phase 69 runbook); an id starting with the placeholder prefix is not live and its board is skipped silently; a season bump adds a new entry and never edits or deletes an older one (old seasons stay readable in the panel and are never written again, PGS-06); the pure-data rule (no functions). Export LEADERBOARD_PLACEHOLDER_PREFIX = "PLACEHOLDER" and the deep-frozen LEADERBOARD_IDS from the behaviour list.

2. Create src/browser/boardScores.js (D-01, D-14, D-16, D-17) with a header comment: Phase 68, the five per-board score encodings and the season ID lookup; pure; GRAVEYARD and LINEAGE are never submitted (LINEAGE is built from a DEEPEST sample, 67 D-19). Exports:
   - SUBMIT_BOARDS (RANKED_BOARDS re-exported, frozen) and SCORE_ORDER (frozen, the plugin's order strings).
   - SCORE_INPUT_CAP = 999999999. A module-private int(x) truncates a finite number, clamps to 0..SCORE_INPUT_CAP, else 0.
   - boardScore(board, summary) per the behaviour list (lean uses Math.max(floor, 1) and Math.round; the days/kills tiebreak uses Math.min(floor, 999); deep clamps steps to 0..999999 and the result to at least 0).
   - boardScores(summary): frozen object over SUBMIT_BOARDS.
   - scoreFallback(board, rawScore): null unless rawScore is a non-negative safe integer; deep → floor = Math.ceil(raw / 1e6), steps = floor x 1e6 - raw; days/kills → Math.floor(raw / 1000) and raw % 1000 as floor; purse → gold; lean → rate = raw / 1000; every result frozen. This is the minimal-row fallback when a tag does not decode (68-06).
   - isRealLeaderboardId(id): a string of 1..128 characters matching /^[A-Za-z0-9_-]+$/ that does not start with LEADERBOARD_PLACEHOLDER_PREFIX.
   - leaderboardId(ids, season, board): null unless SUBMIT_BOARDS includes board, ids is an object, ids[season] is an object and its id passes isRealLeaderboardId.
   - knownSeasons(ids = LEADERBOARD_IDS): the integer keys at least 1 whose value is an object, ascending.
   - devLeaderboardIds(ids = LEADERBOARD_IDS): a deep-frozen map of the same seasons with "dev_{board}_s{season}" ids, used only by the browser dev loop's fake provider (68-07).
   - leaderboardIdsFor({ native }): LEADERBOARD_IDS when native is true, else devLeaderboardIds().
   - scoreOrdersFor(ids): a frozen { [id]: SCORE_ORDER[board] } over every season and board whose id is real.

3. Create test/unit/boardScores.test.js covering every behaviour bullet, importing SEASON from content/season.js for the D-15 guard.
  </action>
  <verify>
    <automated>node --test test/unit/boardScores.test.js test/determinism/content-is-pure-data.test.js test/unit/scoreTag.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PLACEHOLDER_DEEPEST_S1" content/leaderboards.js` equals 1 and `grep -c "export function boardScore(" src/browser/boardScores.js` equals 1
    - `node --test test/unit/boardScores.test.js` passes with at least 18 tests, including the D-16 examples, the rounding and clamp boundaries, the equal-rate LEANEST case, the fallback round-trip, the id lookup cases and the LEADERBOARD_IDS[SEASON] guard
    - `node --test test/determinism/content-is-pure-data.test.js` passes
  </acceptance_criteria>
  <done>Every board's PGS score, its sort direction and its per-season ID resolve through one pure module, and a season bump without IDs fails the suite.</done>
</task>

<task type="auto">
  <name>Task 3: docs/PLAY-GAMES-SETUP.md, the leaderboards and season-bump sections, and the runbook pin</name>
  <files>docs/PLAY-GAMES-SETUP.md, test/unit/play-games-runbook.test.js</files>
  <read_first>
    - docs/PLAY-GAMES-SETUP.md (the 67-02 runbook: its tone, "What Phase 69 completes" and "Engineering notes carried to Phase 68")
    - test/unit/play-games-runbook.test.js (its pins and the old-title guard built from two string halves)
    - src/browser/scoreTag.js and src/browser/boardScores.js (Tasks 1 and 2)
    - content/season.js and content/leaderboards.js
  </read_first>
  <action>
Per D-15 and D-16 (and so the Phase 69 console steps are exact), update docs/PLAY-GAMES-SETUP.md in its existing numbered-step tone. Never name the old working title or the shell's HTML file.

1. Add a section "Leaderboards (one set of five per season)" with a table, one row per board: the board name (DEEPEST, LEANEST, LONGEST, BUTCHERY, PURSE), its internal key (deep, lean, days, kills, purse), the Play Console "Ordering" setting (Larger is better for all except LEANEST, which is Smaller is better), and the submitted score (the D-16 formula in words). State that the console's raw number is an ordering key, not what players see: rows read their displayed values from the score tag. Note the LEANEST limit: one integer cannot also break ties by depth, so equal squares-per-floor rates rank equally regardless of floor. Say that tamper protection is on by default for new leaderboards and should stay on.
2. Add "Where the IDs go": paste each board's leaderboard ID into content/leaderboards.js under LEADERBOARD_IDS[season] (replacing the PLACEHOLDER_ values); a board whose ID is still a placeholder is skipped silently (submissions stay queued for it, and its global view says the board has not opened yet).
3. Add "Starting a new season" (D-15): create five new leaderboards in the console; add a new LEADERBOARD_IDS entry for the next season number; bump SEASON in content/season.js with a changelog line; never edit or delete an older season's entry (old boards stay readable from the panel's season picker and are never written again); the unit suite fails if SEASON has no entry. Restate the 70-leaderboards cap (five per season lasts 14 seasons).
4. Replace the "Engineering notes carried to Phase 68" section with "How the score tag is built (Phase 68)": the v1 field order, the unreserved charset, the name rule (full name, else "First L.", else the first name cut short; worst-case budget 16 characters, correcting the research's 17), no epitaph (the epitaph stays on the player's own device), and that a tag that fails to decode still shows the handle and the board value.
5. Update test/unit/play-games-runbook.test.js to also pin: "content/leaderboards.js", "LEADERBOARD_IDS", "content/season.js", "Smaller is better", "Larger is better", "PLACEHOLDER", "tamper", and "First L.". Keep every existing pin and the old-title guard.
  </action>
  <verify>
    <automated>node --test test/unit/play-games-runbook.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "content/leaderboards.js" docs/PLAY-GAMES-SETUP.md` is at least 1 and `grep -c "Smaller is better" docs/PLAY-GAMES-SETUP.md` is at least 1
    - `node --test test/unit/play-games-runbook.test.js` passes with the new pins and the unchanged old-title guard
    - `npm test` reports 0 failures, or only failures pre-existing on master before this plan (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The user can create the season's boards, set their ordering and paste the IDs from one document, and the season-bump process is written down where Phase 69 finishes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PGS server → client | Other players' score tags arrive from Google's servers and are untrusted text |
| client → PGS | Our tag is published to every player who views the board |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-01 | Tampering | decodeTag | medium | mitigate | charset, length, field-count and digit-width checks before any use; out-of-range indices decode to ""; never throws (Task 1) |
| T-68-02 | Information disclosure | encodeTag | low | mitigate | only the ten numeric fields and the truncated name are read; epitaph, note, seed, acts and hash are never read (source pin, Task 1) |
| T-68-03 | Tampering | forged high scores | low | accept | PGS tamper protection stays on (documented, Task 3); plausibility checks are a future requirement (D-17) |
</threat_model>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- Engine untouched: `git diff --stat -- engine/ test/parity/` is empty for this plan
- No Android build, APK or device step
</verification>

<success_criteria>
- PGS-03: the tag carries name, race/sub/level, cause and the six stat fields within 64 URL-safe characters
- PGS-06: IDs are keyed per board per season, placeholders are skipped, and a season bump without IDs fails the suite
- D-16 encodings are exact and documented for the console setup
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| src/browser/scoreTag.js | new pure module | TAG_VERSION, TAG_MAX_LENGTH, TAG_RACES, TAG_SUBS, TAG_CAUSES, TAG_FIELD_CAPS, TAG_UNKNOWN, classOfSub, tagName, fitName, encodeTag, decodeTag |
| src/browser/boardScores.js | new pure module | SUBMIT_BOARDS, SCORE_ORDER, SCORE_INPUT_CAP, boardScore, boardScores, scoreFallback, isRealLeaderboardId, leaderboardId, knownSeasons, devLeaderboardIds, leaderboardIdsFor, scoreOrdersFor |
| content/leaderboards.js | new content | LEADERBOARD_IDS (season 1 placeholders), LEADERBOARD_PLACEHOLDER_PREFIX |
| docs/PLAY-GAMES-SETUP.md | doc update | leaderboards table, where the IDs go, starting a new season, how the tag is built |
| test/unit/scoreTag.test.js, test/unit/boardScores.test.js | new tests | |

## Flagged assumptions (unresolved edge rows)

- PGS-06 (edge probe: unclassified): "old-season boards stay readable and are never written again" is enforced in two places: this plan's lookup (the caller always passes the current SEASON for writes) and 68-04's queue, which drops every entry whose season is not the current SEASON before submitting. Readability of old boards is the panel's season picker (68-06), which only reads.
- The research's rejected alternative (base64url bit-packing, which would carry a 21-character name untruncated) stays rejected: the delimited v1 format has room for every field the panel shows. Revisit before shrinking any field if a later phase needs more tag room; a v2 tag would be decoded side by side.
- Placeholder IDs mean a native build submits nothing and shows "not opened yet" global views until the Phase 69 runbook fills content/leaderboards.js. That is expected, not a defect.

<output>
Create `.planning/phases/68-global-boards-submissions-you-placed-x/68-01-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: in Play Console, create the five season-1 leaderboards with the ordering from docs/PLAY-GAMES-SETUP.md and confirm a submitted tag shows in the console's score view. Do NOT create `.planning/phases/68-*/deferred-items.md`.
</output>
