---
phase: 68-global-boards-submissions-you-placed-x
plan: 05
type: execute
wave: 2
depends_on: ["68-01", "68-02"]
files_modified:
  - src/browser/globalBoards.js
  - test/unit/globalBoards.test.js
autonomous: true
requirements: [PGS-05]
user_setup: []

must_haves:
  truths:
    - "view({ board, scope, season }) answers synchronously from an in-memory cache keyed by (season, board, scope) and starts at most one fetch per key; the first call returns a 'loading' snapshot and never waits (D-07: the panel never blocks)"
    - "A cached result is served for 5 minutes; a later view() still returns it and starts one background refresh; a failed refresh keeps the last cached result marked stale; with no cache a failure gives 'unreachable', retried after 30 seconds (D-07)"
    - "When isActive() is false (signed out or Compete OFF) view() returns null and requestFriendsAccess() resolves 'unavailable', both with zero provider calls; a fetch that completes after the player stopped being active is discarded (D-07; the milestone's zero-network gate)"
    - "ALL on a ranked board fetches the top 10 of the public collection plus the player's own score; FRIENDS first asks friendsAccess silently and shows a 'consent' snapshot when consent is required, otherwise fetches the friends top 10 plus the player's own friends score (D-05, D-06)"
    - "LINEAGE (combo) fetches the top 25 DEEPEST scores of the scope and marks the snapshot sampled with the count read; the grouping by race and class happens in the view (68-06) (D-09; 67 D-19)"
    - "A board whose current-season ID is missing or a placeholder gives a 'closed' snapshot with no provider call; GRAVEYARD and the local scope return null (D-14, D-17)"
    - "Every fetched score becomes a GlobalEntry { key, rank, handle, playerId, you, friend, rawScore, run } with run = decodeTag(tag) or null; YOU is the row whose playerId matches the signed-in player, and the player's own score is always marked you (D-05)"
    - "requestFriendsAccess() asks with the consent screen once at a time; 'granted' drops the cached friends snapshots and calls onChange so the next view() fetches; a refusal leaves the consent snapshot in place with no rail card and no modal (D-06)"
    - "PGS-05 adjacency (edge probe): entries keep the order Play Games returned (rank ascending, ties in returned order) and are never re-sorted; two entries for the same player stay two rows with distinct keys"
  prohibitions:
    - statement: "Must not make any leaderboard, friends or rank call while the player is signed out or Compete is OFF, and must not request friends consent from any path other than requestFriendsAccess()"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/globalBoards.js"
      provides: "GLOBAL_TTL_MS, GLOBAL_RETRY_MS, TOP_N, LINEAGE_SAMPLE_N, toGlobalEntry, snapshotOf, createGlobalBoards"
      contains: "export function createGlobalBoards"
    - path: "test/unit/globalBoards.test.js"
      provides: "cache, TTL, stale, unreachable, closed, consent, lineage fetch, isActive gating and YOU-marking tests over the fake provider"
      min_lines: 220
  key_links:
    - from: "src/browser/globalBoards.js#createGlobalBoards"
      to: "provider.loadTopScores / loadPlayerScore / friendsAccess (68-02)"
      via: "one fetch per (season, board, scope), gated on isActive()"
      pattern: "provider\\.loadTopScores\\("
    - from: "src/browser/globalBoards.js#toGlobalEntry"
      to: "src/browser/scoreTag.js#decodeTag"
      via: "rows decode their displayed values from the tag (D-16)"
      pattern: "decodeTag\\("
---

<objective>
Build the fetch-and-cache layer that feeds the panel's ALL and FRIENDS views. Implements D-05 (the public top 10 plus the player's own row, marked YOU), D-06 (the friends collection, with a silent consent check and an explicit consent request from the panel button), D-07 (fetch on panel open or board/scope change per (board, scope, season), cache about 5 minutes, show the last cached result or an unreachable state offline, never block, zero calls when signed out or Compete OFF) and D-09 (LINEAGE reads a top-N DEEPEST sample; 67 D-19). N is 25, the plugin's documented per-call ceiling; the plugin binds no paging (68-02's flagged assumption), so the research's "about 100 via loadMore" is not available.

Purpose: global rows arrive through one tested controller; the view (68-06) only formats a snapshot.
Output: src/browser/globalBoards.js and its tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-CONTEXT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-01-SUMMARY.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-02-SUMMARY.md

<interfaces>
From 68-01: decodeTag(tag) → frozen { v, race, sub, cls, level, cause, floor, day, steps, kills, gold, sp, name } | null; leaderboardId(ids, season, board) → string | null (null for combo and yard, so LINEAGE passes "deep").
From 68-02: provider.loadTopScores({ leaderboardId, collection, maxResults }) → { ok, scores: Score[], total }; provider.loadPlayerScore({ leaderboardId, collection }) → { ok, score: Score | null }; provider.friendsAccess({ request }) → "granted" | "required" | "unavailable"; Score = { rank, rawScore, tag, handle, playerId, friend }; createFakePlayGames({ signedIn, player, boards, orders, online, friendsConsent, interactive }).

The snapshot contract 68-06's boardsView consumes (both plans state it identically):
GlobalSnapshot = frozen {
  status: "loading" | "ready" | "unreachable" | "consent" | "closed",
  board: "deep" | "lean" | "combo" | "days" | "kills" | "purse",
  scope: "all" | "friends",
  season: number,
  entries: GlobalEntry[]   (ranked order as returned; [] unless status is "ready"),
  you: GlobalEntry | null  (the player's own score on this board and scope),
  total: number | null     (the collection's score count when Play Games reports it),
  sampled: number | null   (combo only: how many DEEPEST scores the sample read),
  stale: boolean           (a cached result shown after a failed refresh)
}
GlobalEntry = frozen { key: string, rank: number | null, handle: string, playerId: string, you: boolean, friend: boolean, rawScore: number, run: DecodedTag | null }
Keys: an entry's key is "g:" + (playerId or "anon") + ":" + its index; the player's own entry's key is "g:you".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the pure entry and snapshot builders</name>
  <files>src/browser/globalBoards.js, test/unit/globalBoards.test.js</files>
  <read_first>
    - src/browser/scoreTag.js (decodeTag, 68-01)
    - src/browser/playGames.js (normalizeScore and the Score shape, 68-02)
    - src/browser/boardsView.js (the house style for pure view helpers)
  </read_first>
  <behavior>
    - GLOBAL_TTL_MS is 300000, GLOBAL_RETRY_MS 30000, TOP_N 10, LINEAGE_SAMPLE_N 25
    - toGlobalEntry(score, index, { meId, scope }) for { rank: 3, rawScore: 6999569, tag: <a valid v1 tag>, handle: "Rival", playerId: "r1", friend: true } gives a frozen { key: "g:r1:2", rank: 3, handle: "Rival", playerId: "r1", you: false, friend: true, rawScore: 6999569, run: decodeTag(tag) }
    - A playerId equal to meId gives you true; in the friends scope every non-you entry has friend true; an empty playerId gives key "g:anon:<index>"; a malformed tag gives run null (the row still exists)
    - A missing or non-integer rank stays null; a non-finite rawScore becomes 0; a non-string handle becomes ""
    - snapshotOf({ status, board, scope, season, entries, you, total, sampled, stale }) returns a deep-frozen GlobalSnapshot with defaults entries [], you null, total null, sampled null, stale false, and entries forced to [] unless status is "ready"
    - Entries keep input order (never re-sorted), including two entries with equal rawScore and two entries from one playerId (distinct keys by index)
  </behavior>
  <action>
Create src/browser/globalBoards.js with a header comment: Phase 68 (PGS-05, D-05..D-07, D-09), the global and friends boards' fetch-and-cache controller over the injected Play Games provider; the snapshot contract (quote the interfaces block's field list); rows decode their values from the tag (D-16); no DOM, storage, clock of its own (now is injected) or network except through the provider; zero calls while inactive.

Export GLOBAL_TTL_MS, GLOBAL_RETRY_MS, TOP_N, LINEAGE_SAMPLE_N, toGlobalEntry and snapshotOf as the behaviour list states. Write the test file's first half covering every behaviour bullet.
  </action>
  <verify>
    <automated>node --test test/unit/globalBoards.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function toGlobalEntry" src/browser/globalBoards.js` equals 1 and `grep -c "export function snapshotOf" src/browser/globalBoards.js` equals 1
    - `node --test test/unit/globalBoards.test.js` passes, including the YOU/FRIEND marking, the malformed-tag entry and the order-preservation cases
  </acceptance_criteria>
  <done>Fetched scores become frozen, decoded, correctly tagged entries in the order Play Games returned them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createGlobalBoards, the cache, fetch and consent controller</name>
  <files>src/browser/globalBoards.js, test/unit/globalBoards.test.js</files>
  <read_first>
    - src/browser/globalBoards.js (Task 1)
    - src/browser/playGames.js (createFakePlayGames: boards seed, orders, setOnline, friendsConsent, calls())
    - src/browser/boardScores.js (leaderboardId, devLeaderboardIds, scoreOrdersFor)
    - src/browser/scoreTag.js (encodeTag, to build valid seeded tags in tests)
  </read_first>
  <behavior>
    - createGlobalBoards({ provider, ids, isActive, playerId, onChange, now }) returns a frozen { view, requestFriendsAccess, clear }
    - Inactive: view() returns null and the provider's calls() stays empty; requestFriendsAccess() resolves "unavailable" with no call
    - view({ board: "yard", ... }), view({ scope: "local", ... }) and an unknown board return null with no call
    - First view({ board: "deep", scope: "all", season: 1 }) returns status "loading" and starts exactly one loadTopScores (maxResults 10, collection "public") and one loadPlayerScore; a second view() before they settle starts nothing new; when they settle onChange fires once and view() returns status "ready" with the decoded entries, you and total
    - The player's own score is returned as you with key "g:you" and you true even when the provider's score has no playerId; the matching top-10 entry is also marked you
    - Within 5 minutes view() returns the same frozen snapshot object with no new call; at now + 300001 it returns that snapshot and starts one refresh; a successful refresh replaces it
    - A failed refresh keeps the previous entries with stale true; a failure with no cache gives "unreachable"; view() within 30 seconds of that failure starts no new call and at + 30001 retries
    - A board whose id is a placeholder gives "closed" with zero calls (DEEPEST placeholder also closes LINEAGE)
    - LINEAGE: view({ board: "combo", scope: "all" }) calls loadTopScores on the DEEPEST id with maxResults 25 and gives sampled equal to the entry count; it also loads the player's DEEPEST score as you
    - FRIENDS with consent granted: friendsAccess({ request: false }) then the friends top 10 and the player's friends score; with consent required: status "consent", no score call; with friendsAccess "unavailable": "unreachable"
    - requestFriendsAccess() calls friendsAccess({ request: true }) once even if called twice concurrently; "granted" drops the friends snapshots, calls onChange, and the next friends view() fetches rows; "required" (declined) leaves the consent snapshot and calls nothing else
    - A fetch that settles after isActive() turned false, or after clear(), writes no cache entry and does not call onChange; clear() drops every snapshot so the next view() is "loading" again
    - A throwing onChange never breaks the controller; the provider is never called with a season other than the one in the view() argument
  </behavior>
  <action>
Add `export function createGlobalBoards({ provider, ids, isActive, playerId = () => "", onChange = null, now = () => Date.now() })` to src/browser/globalBoards.js, implementing D-05, D-06, D-07 and D-09:
- Cache: a Map from "season|board|scope" to { snapshot, fetchedAt, retryAt, inFlight }. A module-private generation counter is bumped by clear().
- view({ board, scope, season }): return null unless isActive() is true, scope is "all" or "friends", and board is one of deep, lean, combo, days, kills, purse. Resolve the id with leaderboardId(ids, season, board === "combo" ? "deep" : board); a null id caches and returns a "closed" snapshot with no call (D-14). Otherwise: no cache entry → start fetch and return a "loading" snapshot; a cached entry older than GLOBAL_TTL_MS (or past its retryAt after a failure) with nothing in flight → start a background refresh and return the cached snapshot; else return the cached snapshot.
- fetch(key): friends scope first awaits provider.friendsAccess({ request: false }): "required" → a "consent" snapshot; "unavailable" → treated as a failure. Then Promise.all of provider.loadTopScores({ leaderboardId, collection, maxResults: combo ? LINEAGE_SAMPLE_N : TOP_N }) and provider.loadPlayerScore({ leaderboardId, collection }). Build entries with toGlobalEntry (meId = playerId()), you from the player score with key "g:you" and you true (and mark the matching top entry you), total from the top-scores result, sampled = entries.length for combo. On success cache a "ready" snapshot with fetchedAt now(); on failure keep a cached ready snapshot with stale true, else cache "unreachable", and set retryAt = now() + GLOBAL_RETRY_MS. Discard the result (no cache write, no onChange) when the generation changed or isActive() is false. Call onChange inside try/catch after each cache write.
- requestFriendsAccess(): "unavailable" when inactive; share one in-flight promise; call provider.friendsAccess({ request: true }); on "granted" delete every friends-scope cache entry and call onChange; resolve the answer. This is the only place request: true is ever sent (D-06).
- clear(): bump the generation and empty the cache.
Finish test/unit/globalBoards.test.js covering every behaviour bullet with createFakePlayGames seeded with boards whose tags come from encodeTag, dev ids from devLeaderboardIds(), orders from scoreOrdersFor, and a manual now().
  </action>
  <verify>
    <automated>node --test test/unit/globalBoards.test.js test/unit/playGames.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function createGlobalBoards" src/browser/globalBoards.js` equals 1
    - `node --test test/unit/globalBoards.test.js` passes with at least 24 tests in total, including inactive zero-calls, the TTL boundary, stale-on-failure, the 30-second retry, closed, LINEAGE's 25-sample, both consent paths and the discard-after-inactive case
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The panel can ask for any board and scope at any time and always gets an immediate, honest snapshot, with Play Games touched only when signed in and competing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PGS server → client | handles, ranks, raw scores and tags of other players are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-07 | Tampering | remote tags and handles | medium | mitigate | tags decoded defensively (decodeTag returns null); handles coerced to strings; 68-06 renders with textContent only |
| T-68-08 | Information disclosure | friends list | medium | mitigate | consent requested only by requestFriendsAccess(); no friends data fetched without it; nothing persisted (memory cache only) |
| T-68-09 | Denial of service | slow or hung fetches | low | mitigate | 68-02's call timeout; one fetch per key; the panel reads the cache synchronously |
</threat_model>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step
</verification>

<success_criteria>
- PGS-05: ALL and FRIENDS snapshots come from Play Games top scores, the friends collection and the player's own score; LINEAGE reads a DEEPEST sample
- D-07: nothing blocks, the cache lasts about 5 minutes, offline shows the last result or an honest unreachable state, and signed-out or Compete-OFF means zero calls
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| src/browser/globalBoards.js | new module | GLOBAL_TTL_MS, GLOBAL_RETRY_MS, TOP_N, LINEAGE_SAMPLE_N, toGlobalEntry, snapshotOf, createGlobalBoards |
| `createGlobalBoards({ provider, ids, isActive, playerId, onChange, now })` | export | frozen { view, requestFriendsAccess, clear } |
| GlobalSnapshot / GlobalEntry | data contract | consumed by 68-06 boardsView |
| test/unit/globalBoards.test.js | new test | |

## Flagged assumptions (unresolved edge rows)

- LINEAGE's sample is 25 scores (the plugin's per-call ceiling). With few players every combination may be present; with many, rare lineages fall outside the sample, which the footnote says plainly (D-09).
- The 30-second retry after a failure is Claude's discretion (D-07 names only the 5-minute cache); it keeps a reconnect from being hidden behind a 5-minute stale "unreachable".

<output>
Create `.planning/phases/68-global-boards-submissions-you-placed-x/68-05-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: on the Pixel 7, ALL fills within a few seconds of opening; airplane mode shows the last result or the unreachable note; FRIENDS shows the consent note and button on a fresh account. Do NOT create `.planning/phases/68-*/deferred-items.md`.
</output>
