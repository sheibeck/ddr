---
phase: 68-global-boards-submissions-you-placed-x
plan: 06
type: execute
wave: 2
depends_on: ["68-01", "68-03"]
files_modified:
  - content/boards.js
  - test/unit/boards-copy.test.js
  - src/browser/boardsView.js
  - test/unit/boardsView.test.js
  - src/browser/boardsPanel.js
  - test/unit/boardsPanel.test.js
  - test/unit/boardsPanel-dom.test.js
  - mazeworld.html
  - test/unit/boards-css.test.js
autonomous: true
requirements: [PGS-05, PGS-06]
user_setup: []

must_haves:
  truths:
    - "Signed in on a ranked board with scope ALL or FRIENDS, boardsView renders the global snapshot through the Phase 66 row component: the Play Games handle as the headline with its initials avatar, a YOU or FRIEND tag, the adventurer name and 'RACE SUB · LVL n' decoded from the tag, the board value and unit, a value bar, and an expand row with the cause line and the six stat chips and no epitaph (D-05, D-16; 67 D-18)"
    - "When the player's own row is not among the fetched top ten, it is pinned last under 'NOT IN THE TOP TEN · YOUR BEST RUN' (D-05)"
    - "The standing card on a global view shows the player's real rank, e.g. '3RD' with 'of 9,044 interred worldwide.' (or 'of N among friends.') plus a quip banded 1st / top 10 / top 100 / the rest; with no score of the player's it reads NO ENTRY with the no-entry note (D-05)"
    - "The snapshot status maps to one in-panel body each: loading and unreachable to notes, closed to the not-opened note, consent to the note plus a SHOW MY FRIENDS button that calls the injected consent handler, an empty ready board to the empty note; nothing is a modal or a rail card (D-06, D-07)"
    - "LINEAGE on a global view groups the DEEPEST sample by race and class, keeping the first (best-ranked) entry per combination, and its footnote says it is a sample of the top N (D-09)"
    - "A tag that does not decode still renders a minimal row: the handle plus the board value recovered from the raw score (LEANEST as squares per floor), with no stats (D-16)"
    - "The header shows a small SEASON label for the viewed season; a season picker appears only when at least two seasons exist, on a global view, and switching it re-renders that season's boards read-only (D-08, PGS-06)"
    - "Signed out or Compete OFF, every view is exactly the Phase 66/67 local view, the panel asks the global seam nothing, and GRAVEYARD stays local in every state (D-07, D-17)"
    - "The 67-04 'coming online' notes are retired from content/boards.js and the view (greenfield: the live global views replace them)"
    - "PGS-05 empty (edge probe): an empty ready snapshot renders the empty note with a standing card of NO ENTRY; a null or malformed global input on a signed-in global view renders the unreachable note; an empty LINEAGE sample renders the empty note and the sampled footnote with n 0"
    - "PGS-05 ordering (edge probe): global rows render in snapshot order with the rank Play Games reported (the list position when the rank is missing); the pinned YOU row is always last"
    - "PGS-05 adjacency (edge probe): two LINEAGE entries of the same race and class collapse to the first; two global entries with equal values keep their snapshot order and both render"
  prohibitions:
    - statement: "Must not render a remote avatar image or an epitaph on a global or friends row, and must not present local rows as global or friends rankings"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/boardsView.js"
      provides: "the global, season and seasons inputs; global rows, lineage grouping, standing, notes, consent body, season header"
      contains: "buildGlobalRows"
    - path: "src/browser/boardsPanel.js"
      provides: "the consent body, the season label and picker, and createBoardsPanel's global/seasons/onFriendsConsent seams"
      contains: "onFriendsConsent"
    - path: "mazeworld.html"
      provides: "CSS for .mw-bd-season, .mw-bd-seasons, .mw-bd-season-chip and .mw-bd-consent"
      contains: ".mw-bd-consent"
  key_links:
    - from: "src/browser/boardsPanel.js#createBoardsPanel render()"
      to: "the injected global({ board, scope, season }) seam (68-07 passes globalBoards.view)"
      via: "called only when signed in, on a global scope, off GRAVEYARD"
      pattern: "global\\("
    - from: "src/browser/boardsView.js global rows"
      to: "src/browser/boardScores.js#scoreFallback"
      via: "the minimal row when a tag does not decode"
      pattern: "scoreFallback\\("
---

<objective>
Bring the Leaderboards panel's ALL and FRIENDS views to life behind the Phase 66 seam. Implements D-05 (the public top 10 with handles, YOU and FRIEND tags, the pinned "NOT IN THE TOP TEN · YOUR BEST RUN" row and the real-rank standing card with a quip), D-06 (the friends view and its in-panel consent note with a button), D-07 (loading, unreachable and stale states that never block; signed out or Compete OFF keeps the Phase 66 local views and asks for nothing), D-08 (the SEASON label and a picker only once a second season exists), D-09 (LINEAGE grouped from the DEEPEST sample with an honest footnote), D-16 (rows decode their values from the tag, with a minimal row when a tag does not decode) and D-17 (GRAVEYARD stays local). It retires 67-04's "global boards are coming online" notes, which the live views replace.

Purpose: the panel shows the world's dead and the player's place among them, honestly, in every network state.
Output: boardsView and boardsPanel global branches, the panel CSS, the copy cleanup, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-CONTEXT.md
@.planning/phases/66-leaderboards-panel-local/66-CONTEXT.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-01-SUMMARY.md
@.planning/phases/68-global-boards-submissions-you-placed-x/68-03-SUMMARY.md

<interfaces>
Live code (Phase 66 plus 67-04, merged; read it, it is authoritative):
- src/browser/boardsView.js boardsView({ bests, graves, total, board, scope, open, entry, hasHero, signedIn, player, recentHash }) → { header, strip, rail, board, body, standing, footnote, dock }; body kinds rows | empty | note; row shape { key, rank, top, podium, you, divider, headline, tag, name, line, detail, val, unit, stats, avatar, open, barPct }; helpers levelLine, causeLine, statChips, valueText, metricFor, barPercents, finalize, cutTopTen, ordinal, initialsOf, avatarColour, fill, pickQuip. 67-04 added the signed-in strip (avatar, label, source "PLAY GAMES · SIGNED IN", undimmed chips) and, signed in with scope all/friends, body { kind: "note", line: BOARDS_PANEL_COPY.note.live[scope] }.
- src/browser/boardsPanel.js: BOARDS_CLASSES; buildHead, buildStrip (avatar branch from 67-04), buildRow (row.tag rendered as span.mw-bd-tag; data-you), buildStanding, buildBody; createBoardsPanel({ host, buildView, readData, prefs, reducedMotion, onRoute, identity }) with state entry/board/scope/open/hasHero; handlers onBoard/onScope/onRow/onBack/onDock; render() passes signedIn and player from identity().
- mazeworld.html style block: the .mw-bd-* rules; test/unit/boards-css.test.js pins that every BOARDS_CLASSES name has a selector, every .mw-bd font-size uses var(--mw-text-scale), only .mw-bd-detail and .mw-bd-bar-fill animate, and the blanket reduced-motion rule exists; .mw-bd-scope-chip is the chip style to mirror; .mw-bd-row[data-you="1"] .mw-bd-tag already styles YOU.
- From 68-03: BOARDS_PANEL_COPY.global { scope { all, friends }, season, loading, unreachable, closed, empty, consent, consentButton, you, friend, anon, foe, leanRateUnit, noEntry, ofWorld, ofFriends, ofSampled, sampledFoot } and GLOBAL_STANDING_LINES { first, ten, hundred, rest }.
- From 68-01: scoreFallback(board, rawScore); content/epitaphs.js CAUSE_TEXT (the {foe} token is filled with BOARDS_PANEL_COPY.global.foe for global rows).

The snapshot contract (68-05 produces it; this plan consumes it as plain data and does not import globalBoards.js):
GlobalSnapshot = frozen { status: "loading" | "ready" | "unreachable" | "consent" | "closed", board, scope: "all" | "friends", season: number, entries: GlobalEntry[], you: GlobalEntry | null, total: number | null, sampled: number | null, stale: boolean }
GlobalEntry = frozen { key, rank: number | null, handle, playerId, you: boolean, friend: boolean, rawScore: number, run: { v, race, sub, cls, level, cause, floor, day, steps, kills, gold, sp, name } | null }

View additions this plan defines:
- inputs: global (GlobalSnapshot | null), season (the viewed season number, default 1), seasons (ascending season numbers, default [season])
- header.season = { label: "SEASON {n}", picker: null | [{ n, label, on }] }
- body kind "consent": { kind: "consent", line, action: { id: "friendsConsent", label } }
- standing may be null (no card) while a global view is not ready
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: boardsView's global branch, season header and the copy cleanup</name>
  <files>content/boards.js, test/unit/boards-copy.test.js, src/browser/boardsView.js, test/unit/boardsView.test.js</files>
  <read_first>
    - src/browser/boardsView.js (the whole module, including 67-04's signed-in strip and live-note branch)
    - test/unit/boardsView.test.js (the fixtures, the purity/determinism pins, 67-04's signed-in tests and its "no worldwide / among friends" pins)
    - content/boards.js (BOARDS_PANEL_COPY.note.live, strip.live, the 68-03 global block, GLOBAL_STANDING_LINES)
    - test/unit/boards-copy.test.js (the note.live pins)
    - src/browser/scoreTag.js (decodeTag, to build fixture entries) and src/browser/boardScores.js (scoreFallback)
  </read_first>
  <behavior>
    - Signed in, board deep, scope all, a ready snapshot of three entries: body.kind "rows"; row 1 has headline the handle, avatar from initialsOf/avatarColour of the handle, tag "" (or "FRIEND" for a friend entry, "YOU" for the player's), name the decoded adventurer name, line "DWARVEN PICKPOCKET · LVL III", val the decoded floor, unit "FLOOR", stats the six chips from the decoded run, detail the cause line (combat → "Cut down by a foe") with no epitaph, rank the reported rank as a string, top/podium by position
    - A snapshot whose you entry is not in entries appends it last with divider "NOT IN THE TOP TEN · YOUR BEST RUN" and tag "YOU"; when the player is already in entries nothing is appended
    - LEANEST rows show "floor · steps" from the tag; PURSE groups digits; a run-null entry shows the handle, val from scoreFallback (deep floor, days day, kills kills, purse grouped gold, lean the rate to one decimal with unit "SQ / FLOOR"), name "", line "", stats [], detail ""; a rawScore that scoreFallback rejects shows "—"
    - An entry with an empty handle uses BOARDS_PANEL_COPY.global.anon as the headline
    - Bars follow the Phase 66 min-max rule over the rendered rows' metrics
    - Standing (ranked board, scope all, you rank 3, total 9044): label = the you run's name upper-cased + " · " + the unit label, place "3RD", note "of 9,044 interred worldwide. " + a GLOBAL_STANDING_LINES.ten line; scope friends uses "of N among friends."; total null falls back to the entry count; you null gives { label: "NO ENTRY", place: "—", note: global.noEntry }; bands switch at ranks 1/2, 10/11, 100/101; the quip pick is deterministic
    - Status loading gives body { kind: "note", line: global.loading } and standing null; unreachable gives global.unreachable; closed gives global.closed; consent gives { kind: "consent", line: global.consent, action: { id: "friendsConsent", label: "SHOW MY FRIENDS" } }; ready with no entries gives { kind: "empty", line: global.empty } and the NO ENTRY standing (or the ranked standing when you exists); a stale ready snapshot renders its rows normally
    - Signed in with global null, undefined or malformed on a global scope gives the unreachable note (never a throw)
    - LINEAGE global: entries grouped by "race cls" (entries with run null or an empty race or cls skipped), first entry per key kept, in order; row name "DWARVEN THIEF", headline the handle, line the level line, val the floor, unit "FLOOR"; footnote fill(global.sampledFoot, { n: snapshot.sampled }); standing place = the you run's combination position among the groups (NO ENTRY when absent) with note "of N lineages in the sample. " plus a quip
    - Header: header.season.label is "SEASON 1" for season 1 in every view; picker is null with seasons [1]; with seasons [1, 2], signed in, scope all, board deep, season 2 the picker is [{ n: 1, label: "SEASON 1", on: false }, { n: 2, label: "SEASON 2", on: true }]; the picker is null on GRAVEYARD, on the local scope and when signed out
    - Header scope line: signed in with scope all gives global.scope.all, friends gives global.scope.friends; otherwise the Phase 66 lines
    - Signed out (any scope, any global input): the view deep-equals the pre-68 view for the same data except header.season; the Phase 66 signed-out notes still show on all/friends; GRAVEYARD ignores scope and global in every state
    - BOARDS_PANEL_COPY.note has no live key and BOARDS_PANEL_COPY.strip.live is unchanged; no view references note.live
    - Purity: a deep-frozen snapshot is never mutated; two calls deepStrictEqual; no DOM, clock, randomness or network reference
  </behavior>
  <action>
Per D-05, D-06, D-07, D-08, D-09, D-16 and D-17:

1. content/boards.js: remove the `live` key from BOARDS_PANEL_COPY.note (keep note.all and note.friends, the Phase 66 signed-out notes, and keep strip.live). Update the block comment (Phase 68 replaces 67-04's coming-online notes with live views). In test/unit/boards-copy.test.js drop the note.live pins and add a pin that note has exactly the keys all and friends.

2. src/browser/boardsView.js: accept `global`, `season` (a positive integer, default 1) and `seasons` (an array of positive integers, default [season]). Add module-private builders:
   - globalCause(run): CAUSE_TEXT[run.cause] with {foe} filled by BOARDS_PANEL_COPY.global.foe, first letter capitalised; "" for an unknown cause.
   - globalRow(board, entry, index): the row shape from the behaviour list; the avatar is computed from the handle (pass an avatar key so finalize() uses the handle rather than the run name for global rows); tag "YOU" when entry.you, "FRIEND" when entry.friend, else ""; when entry.run is null use scoreFallback for val and metric as listed.
   - buildGlobalRows(board, snap): entries mapped through globalRow, plus the pinned you row with BOARDS_PANEL_COPY.divider when snap.you is set and no entry is you; finalize with skipCut true.
   - buildGlobalLineageRows(snap): the grouping from the behaviour list.
   - buildGlobalStanding(board, scope, snap, groups): as listed; the quip bank is GLOBAL_STANDING_LINES by band (first / ten / hundred / rest), picked deterministically from the rank and the board index.
   - a snapshot guard: anything that is not an object with a known status is treated as unreachable.
   Body selection order: yard → the Phase 66 graveyard rows; scope local → the Phase 66 local rows; not signed in → the Phase 66 note[scope]; signed in → the global mapping (status → body, standing and footnote as listed). Header gains season { label, picker } and the signed-in global scope lines. Leave the strip (67-04), rail, dock and every local path unchanged. Update the header comment: Phase 68 adds the global source at this seam (snapshot contract from 68-05; rows decode the tag, D-16; no epitaph on global rows, 67 D-18).

3. test/unit/boardsView.test.js: add a test per behaviour bullet with fixture snapshots built from encodeTag/decodeTag and deep-frozen; replace 67-04's live-note tests with the global-view tests; keep the signed-out "no worldwide / among friends / @" pin and narrow any signed-in "no worldwide" pin to the local scope.
  </action>
  <verify>
    <automated>node --test test/unit/boardsView.test.js test/unit/boards-copy.test.js test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function buildGlobalRows" src/browser/boardsView.js` equals 1 and `grep -c "scoreFallback(" src/browser/boardsView.js` is at least 1
    - `node --test test/unit/boardsView.test.js test/unit/boards-copy.test.js` passes, including the pinned-YOU row, the minimal row, every status body, the LINEAGE grouping and footnote, the standing bands and the season picker cases
    - `node --test test/voice/safety-scan.test.js test/unit/hp-not-wp.test.js` passes
  </acceptance_criteria>
  <done>The pure view turns any global snapshot into honest rows, notes and a real-rank standing card, and leaves every signed-out and local view as it was.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: boardsPanel's consent button, season label and picker, the global seams, and the CSS</name>
  <files>src/browser/boardsPanel.js, test/unit/boardsPanel.test.js, test/unit/boardsPanel-dom.test.js, mazeworld.html, test/unit/boards-css.test.js</files>
  <read_first>
    - src/browser/boardsPanel.js (BOARDS_CLASSES, buildHead, buildBody, buildStanding, createBoardsPanel and its render(), 67-04's identity seam)
    - test/unit/boardsPanel-dom.test.js (makeView fixtures, the BOARDS_CLASSES completeness walk, the source pins)
    - test/unit/boardsPanel.test.js (the recording buildView and the mutable identity fake)
    - test/unit/boards-css.test.js (every pin listed in the interfaces block)
    - mazeworld.html style block: the .mw-bd-scope-chip, .mw-bd-note, .mw-bd-dock-btn and .mw-bd-headtext rules
  </read_first>
  <behavior>
    - BOARDS_CLASSES adds "mw-bd-season", "mw-bd-seasons", "mw-bd-season-chip" and "mw-bd-consent", and the completeness walk passes over a view set that includes a picker and a consent body
    - The header renders span.mw-bd-season with header.season.label inside the head text; with a picker it renders div.mw-bd-seasons holding one button.mw-bd-season-chip per season (type button, data-season, data-on, aria-pressed) whose onclick calls handlers.onSeason(n)
    - A consent body renders p.mw-bd-note with the line and a button.mw-bd-consent (type button, data-action "friendsConsent", the label) whose onclick calls handlers.onConsent(); rows, empty and note bodies render as before
    - A null standing renders no .mw-bd-standing; the footnote still renders
    - createBoardsPanel gains optional `global` (a function), `seasons` (a function returning { current, all }) and `onFriendsConsent` (a function); defaults make the panel behave exactly as before
    - render() calls global({ board, scope, season }) only when identity() is signed in, scope is not "local" and the board is not "yard"; it passes the answer as `global` (null when not called or when global throws), plus `season` and `seasons: all` to buildView
    - The viewed season starts at seasons().current on openFromTab and openFromTitle; onSeason(n) switches to n only when n is in seasons().all, clears the open row and re-renders with reset; state() includes season
    - onConsent calls onFriendsConsent once and never throws when it is missing or throws
    - With identity signed out, opening ALL calls global zero times
    - Source pins still hold: no window., document. or globalThis. reference and no network identifier in boardsPanel.js
    - CSS: each new class has a rule; font sizes use var(--mw-text-scale); .mw-bd-season-chip and .mw-bd-consent are at least 44px tall; none of the new rules animates or transitions; the existing boards-css pins still pass
  </behavior>
  <action>
Per D-06, D-07 and D-08, in src/browser/boardsPanel.js:
1. Extend BOARDS_CLASSES with the four names. In buildHead, append span.mw-bd-season (text view.header.season.label) to the head text block when view.header.season exists, and a div.mw-bd-seasons of season chips when its picker is an array (the chip markup mirrors the scope chips). In buildBody, render the consent body (note paragraph plus the consent button). Make the standing card optional (skip when null).
2. createBoardsPanel({ ..., global = null, seasons = () => ({ current: 1, all: [1] }), onFriendsConsent = null }): add a `season` state; read seasons() inside try/catch (a throw means { current: 1, all: [1] }); openFromTab/openFromTitle reset season to the current one; add handlers onSeason(n) and onConsent(); in render(), after reading identity, compute the global snapshot per the behaviour list (inside try/catch) and pass global, season and seasons to buildView. state() includes season. Update the header comment: Phase 68 adds the global snapshot seam (68-07 passes the global-boards controller's view), the season picker and the consent handler; the panel still reaches Play Games only through injected functions.
3. mazeworld.html style block, next to the .mw-bd-scope-chip rules: .mw-bd-season (a small label in the header, var(--disp) at a scaled small size, the dim header colour); .mw-bd-seasons (a wrapping flex row with a small gap); .mw-bd-season-chip (the scope-chip look, min-height 44px, data-on="1" inverted like the scope chip); .mw-bd-consent (a full-width, 48px-tall button in the dock-button style, placed under the note). Every font-size uses calc(... * var(--mw-text-scale)); no animation or transition.
4. Tests: extend test/unit/boardsPanel-dom.test.js (makeView variants with a picker and a consent body; the season-chip and consent clicks; null standing) and test/unit/boardsPanel.test.js (global called only when signed in on a global scope off GRAVEYARD, its answer reaching buildView, a throwing global, the season state and onSeason guard, onConsent). In test/unit/boards-css.test.js add a pin that .mw-bd-season-chip and .mw-bd-consent declare a min-height of at least 44px.
  </action>
  <verify>
    <automated>node --test test/unit/boardsPanel.test.js test/unit/boardsPanel-dom.test.js test/unit/boards-css.test.js test/unit/boardsView.test.js test/unit/shell-boards-panel.test.js test/unit/bridge-registry.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "mw-bd-consent" src/browser/boardsPanel.js` is at least 1 and `grep -c "onFriendsConsent" src/browser/boardsPanel.js` is at least 1
    - `grep -c ".mw-bd-season-chip" mazeworld.html` is at least 1 and `grep -c ".mw-bd-consent" mazeworld.html` is at least 1
    - `node --test test/unit/boardsPanel.test.js test/unit/boardsPanel-dom.test.js test/unit/boards-css.test.js test/unit/shell-boards-panel.test.js` passes
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>The panel draws the consent button, the season label and picker, and reads global snapshots through injected seams only when the player is signed in on a global view.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PGS data → DOM | other players' handles and decoded tag text are rendered in the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-68-10 | Tampering (injection) | row rendering | medium | mitigate | the panel builds DOM with createElement/textContent only (existing source pin: no HTML-string assignment); decoded tag fields are charset-limited by decodeTag |
| T-68-11 | Information disclosure | remote images | low | mitigate | avatars are initials from the handle; no image URL exists in the snapshot (68-02 normalization) |
</threat_model>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step
</verification>

<success_criteria>
- PGS-05: ALL and FRIENDS render Play Games rows with YOU/FRIEND tags, the pinned best run, the real-rank standing card, the consent path and LINEAGE's sampled form
- PGS-06: the viewed season is labelled and older seasons are selectable read-only once they exist
- Signed out or Compete OFF, the panel is exactly the local Phase 66 panel
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| boardsView `global`, `season`, `seasons` inputs | view seam | GlobalSnapshot contract from 68-05 |
| `header.season`, body kind `consent`, nullable `standing` | view shape additions | |
| buildGlobalRows, buildGlobalLineageRows, buildGlobalStanding, globalCause | boardsView internals | |
| `mw-bd-season`, `mw-bd-seasons`, `mw-bd-season-chip`, `mw-bd-consent` | BOARDS_CLASSES + CSS | |
| createBoardsPanel `global`, `seasons`, `onFriendsConsent` | panel seams | 68-07 wires them |
| removed: BOARDS_PANEL_COPY.note.live | greenfield removal | replaced by live views |

## Flagged assumptions (unresolved edge rows)

- The local views keep the SEASON label of the current season even though local personal bests are all-time (tagged by season). The label names the season the world boards are in; a later phase can hide it on local views if it reads as misleading.

<output>
Create `.planning/phases/68-global-boards-submissions-you-placed-x/68-06-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: on the Pixel 7 at default and largest text sizes, the ALL rows, YOU and FRIEND tags, the pinned divider row, the standing card, the consent button and the SEASON label all fit and read correctly. Do NOT create `.planning/phases/68-*/deferred-items.md`.
</output>
