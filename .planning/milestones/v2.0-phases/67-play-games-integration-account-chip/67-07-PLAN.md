---
phase: 67-play-games-integration-account-chip
plan: 07
type: execute
wave: 2
depends_on: ["67-02", "67-03"]
files_modified:
  - src/browser/accountChip.js
  - test/unit/accountChip-dom.test.js
  - test/unit/accountChip.test.js
autonomous: true
requirements: [PGS-02, ACCT-01, ACCT-02]
user_setup: []

must_haves:
  truths:
    - "renderAccountChip draws the chip's face from a chip view alone: the initials on an inline background for 'avatar', the dim ? glyph for 'nobody' and 'pending', plus the aria-label; it reuses the face element across renders (D-07)"
    - "renderAccountSheet draws the D-10 rows from a sheet view alone: the identity block (face, name, status), then Sign in, Stop competing or a disabled SIGNING IN… when the view has an action, the helper line when it has one, the Compete ON/OFF toggle and the Settings row; every tap goes to an injected handler (D-09, D-10)"
    - "createAccountController boots once: with Compete OFF it makes zero provider calls, zero settings writes and zero cards (D-02); with Compete ON it runs one silent provider.init() without blocking its caller, and the result drives the state (D-01)"
    - "The first successful sign-in raises the welcome rail card once and persists pgsWelcomed; a later sign-in with pgsWelcomed set raises nothing (D-04)"
    - "A failed, declined or timed-out attempt leaves the player signed out, raises one failed rail card through notify (never a modal), and schedules no retry; the next automatic attempt is the next launch, and a manual retry comes only from signIn() (D-11)"
    - "Setting Compete to its current value is a no-op (no settings write, no provider call, no state change); boot() called twice starts one attempt; the welcome card shows at most once per install through the persisted pgsWelcomed flag"
    - "One sign-in attempt at a time: signIn() while an attempt is pending is ignored; a result that arrives after Compete was turned OFF, after a newer attempt began, or after the silent attempt timed out is discarded; Compete OFF always wins"
    - "Stop competing is setCompete(false): it persists Compete OFF, invalidates any in-flight attempt and shows the nobody chip; the controller calls no sign-out (the provider has none) and never fakes a signed-out SDK (D-03)"
    - "The controller never persists the player's id or display name: the only settings it writes are compete and pgsWelcomed"
    - "subscribe(listener) notifies every state change (the shell re-renders the chips, the sheet and the Leaderboards panel; Phase 68 purges its queue on Compete OFF), and a throwing listener never breaks the others"
  prohibitions:
    - statement: "Must not persist the Play Games player id or display name to device storage; the identity lives in memory for the session only"
      status: unresolved
      verification: judgment
    - statement: "Must not start the interactive sign-in from boot, from a Compete toggle, or from any path other than signIn()"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "src/browser/accountChip.js"
      provides: "ACCOUNT_CLASSES, renderAccountChip, renderAccountSheet, createAccountController"
      contains: "export function createAccountController"
    - path: "test/unit/accountChip-dom.test.js"
      provides: "recordingDom tests of the chip and sheet renderers"
      min_lines: 110
    - path: "test/unit/accountChip.test.js"
      provides: "controller tests: boot, cards, idempotency, concurrency, timeout, Compete, persistence"
      min_lines: 180
  key_links:
    - from: "src/browser/accountChip.js#createAccountController"
      to: "the injected provider (src/browser/playGames.js createPlayGames / createFakePlayGames)"
      via: "provider.init() (silent) and provider.signIn() (interactive) only"
      pattern: "provider\\.(init|signIn)\\("
    - from: "src/browser/accountChip.js#createAccountController"
      to: "src/browser/account.js (normalizeAccountState, accountCard, accountIdentity, accountChipView, accountSheetView)"
      via: "direct import; the controller owns transitions, account.js owns what they look like"
      pattern: "from \"./account.js\""
---

<objective>
Build the account chip's DOM renderers and the controller that runs sign-in. Implements D-01 (a non-blocking silent attempt at boot with Compete ON), D-02 (Compete OFF means zero provider calls), D-03 (Stop competing is Compete OFF; no sign-out), D-04 (the welcome card once, persisted), D-07 (the chip faces), D-09/D-10 (the sheet rows and their handlers) and D-11 (one failed card, no nagging, manual retry only from Sign in), with the two ACCT-02 edge rows from the probe (idempotency and concurrency) authored as truths.

Purpose: every account behaviour is decided in one tested controller; the shell (67-08) only wires DOM elements, the rail and the settings path to it.
Output: src/browser/accountChip.js and two test files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-02-SUMMARY.md
@.planning/phases/67-play-games-integration-account-chip/67-03-SUMMARY.md

<interfaces>
From 67-02 (src/browser/playGames.js): a provider has init(), signIn(), isAuthenticated() and getPlayer(). init() and signIn() resolve { signedIn: boolean, player: { id, displayName } | null } and never reject (defend anyway). init() is silent and signIn() interactive.
From 67-02 (src/browser/settings.js): readSettings() resolves the full settings (compete default true, pgsWelcomed default false); writeSetting(key, value) resolves the new settings and never throws.
From 67-03 (src/browser/account.js): ACCOUNT_STATUS, normalizeAccountState(input), accountChipView(state), accountSheetView(state), accountCard("welcome" | "failed") → { title, line, tone, hold } or null, accountIdentity(state) → { signedIn, player }.
Chip view: { face: "avatar" | "nobody" | "pending", initials, bg, glyph, label }.
Sheet view: { title, identity: { face, initials, bg, glyph, name, status }, action: null | { id: "signIn" | "stopCompeting" | "pending", label, disabled }, help, compete: { label, on, options: [{ value, label }] }, settings: { label } }.
Class contract (pinned identically in 67-05's CSS test): ACCOUNT_CLASSES = mw-acct-face, mw-acct-initials, mw-acct-glyph, mw-acct-id, mw-acct-id-text, mw-acct-name, mw-acct-status, mw-acct-action, mw-acct-help, mw-acct-row, mw-acct-label, mw-acct-options, mw-acct-opt, mw-acct-settings.
Harness: test/unit/harness/recordingDom.js elements record onclick, support replaceChildren/appendChild/querySelector (#id, .class, tag), and have plain-object dataset/style; there are no attribute selectors.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ACCOUNT_CLASSES, renderAccountChip and renderAccountSheet</name>
  <files>src/browser/accountChip.js, test/unit/accountChip-dom.test.js</files>
  <read_first>
    - src/browser/boardsPanel.js (the renderer discipline: host.ownerDocument only, createElement/textContent/dataset/style/onclick, optional-chained handlers, a classes export plus a completeness walk)
    - test/unit/boardsPanel-dom.test.js (driving a renderer against recordingDom; the comment-stripped source pins)
    - test/unit/harness/recordingDom.js
    - src/browser/account.js (the view contracts, 67-03)
  </read_first>
  <behavior>
    - renderAccountChip(button, { face: "avatar", initials: "HF", bg: "#5c4a6b", glyph: "", label: "Play Games account: Hilda Ferrow" }) leaves exactly one .mw-acct-face child with data-state "avatar", aria-hidden "true", inline background "#5c4a6b" and one .mw-acct-initials child with text "HF"; the button's aria-label equals the label
    - Rendering "nobody" next leaves the same face element (identity preserved), data-state "nobody", background "", and one .mw-acct-glyph with text "?"; "pending" gives data-state "pending" with the glyph
    - A button whose static markup already holds a .mw-acct-face is reused, not duplicated
    - renderAccountSheet({ rows, title }, signedOutView, handlers): title text = view.title; rows hold, in order, .mw-acct-id (a face, then .mw-acct-id-text with .mw-acct-name and .mw-acct-status), a .mw-acct-action button with data-action "signIn", no .mw-acct-help, a .mw-acct-row with .mw-acct-label and .mw-acct-options holding two .mw-acct-opt buttons (data-value "true"/"false", the Compete-ON one with class active and aria-pressed "true"), and a .mw-acct-settings button
    - A signed-in view gives data-action "stopCompeting" and a .mw-acct-help with the helper text; a pending view gives a disabled action; a Compete-OFF view gives no .mw-acct-action, the off help, and the OFF option active
    - onclick wiring: the signIn action calls handlers.onSignIn, the stopCompeting action calls handlers.onStopCompeting, a disabled or pending action calls nothing, the options call handlers.onCompete(true) and onCompete(false), and the settings row calls handlers.onSettings; missing handlers never throw
    - A re-render replaces the rows (no duplicates)
    - Every class emitted across the four state renders and both chip faces is in ACCOUNT_CLASSES, and every ACCOUNT_CLASSES entry is emitted by at least one of them
    - Source pins (comment-stripped): no document., window. or globalThis. reference, no HTML-string property assignment, no import from content/, no localStorage, mzStorage or Preferences reference, and no network identifier (fetch, XMLHttpRequest, WebSocket, EventSource, sendBeacon)
  </behavior>
  <action>
Create src/browser/accountChip.js with a header comment: Phase 67 (ACCT-01/02, PGS-02), the account chip and sheet's DOM renderers and the account controller; DOM only through the host's ownerDocument; every word comes from the view (content/account.js through account.js); the Play Games provider is injected, so this module never names the plugin; no window/document globals; no storage except through the injected settings functions; no network.

1. `export const ACCOUNT_CLASSES`: the frozen list from the interfaces block, in that order.

2. `export function renderAccountChip(button, view)` (D-07): find the button's existing `.mw-acct-face` child (querySelector) or create one and append it. Set its dataset.state to view.face and aria-hidden "true". For "avatar", replace its children with `span.mw-acct-initials` (text view.initials) and set style.background to view.bg. Otherwise replace them with `span.mw-acct-glyph` (text view.glyph) and set style.background to "". Set the button's aria-label to view.label. Tolerate a null button (no-op).

3. `export function renderAccountSheet({ rows, title }, view, handlers = {})` (D-09/D-10): set title.textContent when title exists; build and replaceChildren on rows with the order given in the behaviour list. The identity face uses the same markup as the chip face. The action button gets type "button", data-action = id, text = label and disabled = view.action.disabled; its onclick calls onSignIn for "signIn" and onStopCompeting for "stopCompeting", and does nothing otherwise. The help paragraph appears only when view.help is non-empty. The Compete row has a label span and an options div with one `button.mw-acct-opt` per option (type button, data-value String(value), aria-pressed, class "mw-acct-opt active" when value === view.compete.on); onclick calls onCompete(value). The settings row is a `button.mw-acct-settings` whose onclick calls onSettings. Use only createElement, className, textContent, setAttribute, dataset, style, disabled, onclick, appendChild and replaceChildren. Never assign an HTML string. Optional-chain every handler.

4. Create test/unit/accountChip-dom.test.js covering every behaviour bullet with createRecordingDocument() and views built by src/browser/account.js's real accountChipView/accountSheetView for each status (so the renderer is tested against the real contract).
  </action>
  <verify>
    <automated>node --test test/unit/accountChip-dom.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const ACCOUNT_CLASSES" src/browser/accountChip.js` equals 1, `grep -c "export function renderAccountChip" src/browser/accountChip.js` equals 1 and `grep -c "export function renderAccountSheet" src/browser/accountChip.js` equals 1
    - `node --test test/unit/accountChip-dom.test.js` passes with at least 14 tests, including the face reuse, all four sheet states, the handler wiring, the ACCOUNT_CLASSES completeness walk and the source pins
  </acceptance_criteria>
  <done>The chip and the sheet render every account state from their views, with every tap routed to an injected handler.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: createAccountController: boot, sign-in, Compete, cards, timeout and subscriptions</name>
  <files>src/browser/accountChip.js, test/unit/accountChip.test.js</files>
  <read_first>
    - src/browser/accountChip.js (Task 1)
    - src/browser/account.js (normalizeAccountState, accountCard, accountIdentity, the views)
    - src/browser/playGames.js (the provider contract and createFakePlayGames, 67-02)
    - src/browser/settings.js (readSettings/writeSetting, the compete/pgsWelcomed fields)
    - src/browser/cameraGlide.js or src/browser/typewriter.js (the injected-timer testing pattern)
    - .planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md (D-01..D-04, D-11)
  </read_first>
  <behavior>
    - Before boot, state() is pending with Compete ON (the chip shows the pending face)
    - boot() with settings compete false: status "off"; the provider records zero calls; notify and settings.write are never called; one change is emitted
    - boot() with compete true: status "pending" is emitted, provider.init() is called once, and boot's promise does not wait on init (the test resolves init later, after boot() has returned)
    - init → { signedIn: true, player: P } with pgsWelcomed false: status "signedIn", identity() { signedIn: true, player: P }, notify called once with accountCard("welcome"), settings.write("pgsWelcomed", true) called once; with pgsWelcomed true: no card, no write
    - init → signed out: status "signedOut", notify once with accountCard("failed"), no further provider call and no timer left pending
    - init rejects or throws: same as signed out (the controller never rejects)
    - Silent timeout: init never settles; the injected timer fires after timeoutMs (default 20000): status "signedOut" plus the failed card; a later signed-in resolution of that init is discarded
    - boot() called twice: one init call and the same promise
    - signIn() when signed out with Compete ON: provider.signIn() once; success signs in (welcome card only if not yet welcomed); decline gives signedOut plus the failed card; no timeout applies to the interactive attempt
    - signIn() while pending, when signed in, or with Compete OFF: no provider call; two rapid signIn() calls make one provider call
    - setCompete(false) while an attempt is pending: status "off" at once, write("compete", false), and the attempt's later signed-in result is discarded (status stays "off", no card)
    - setCompete(false) when signed in: status "off", identity signed out, write("compete", false); no provider call of any kind (there is no sign-out)
    - setCompete to its current value: no write, no provider call, no emit
    - setCompete(true) from "off": write("compete", true), then one silent provider.init(), with its result applied as at boot (including the failed card on failure)
    - Every settings.write call across all tests uses key "compete" or "pgsWelcomed", and no written value is the player's id or display name
    - subscribe(fn) receives each new state snapshot; the returned function unsubscribes; a listener that throws does not stop the others or the controller
    - stopCompeting() behaves exactly like setCompete(false)
  </behavior>
  <action>
In src/browser/accountChip.js add `export function createAccountController({ provider, settings, notify, timeoutMs = 20000, setTimer = setTimeout, clearTimer = clearTimeout })` (implements D-01, D-02, D-03, D-04, D-11). `settings` is { read, write }, where the shell passes readSettings and writeSetting. `notify(card)` receives an accountCard object, and the shell delivers it to the rail. Return a frozen API: boot, signIn, setCompete, stopCompeting, state, identity, chipView, sheetView, subscribe.

Internals:
- `current` starts as normalizeAccountState({ compete: true, status: "pending" }); `seq` is an attempt token; `booted` memoizes boot's promise; `welcomed` mirrors the persisted flag; `listeners` is a set.
- emit(): set `current` to a new normalized state and call every listener with it, each inside try/catch.
- boot() (D-01/D-02): memoized. Read settings inside try/catch (on failure use compete true, pgsWelcomed false). Set welcomed = pgsWelcomed === true. With compete false, emit status "off" and resolve, touching no provider method. Otherwise start attempt("silent") WITHOUT awaiting it, so boot resolves once the attempt has started and the shell never waits on sign-in.
- attempt(kind): token = ++seq; emit pending; call provider.init() for "silent" or provider.signIn() for "interactive", wrapped so a throw or rejection becomes { signedIn: false, player: null }. For "silent" only, race it against a timer of timeoutMs that settles the attempt as a failure; clear the timer when the call settles first. Settle only when token === seq and compete is still on (otherwise drop the result). On success emit signedIn with the player; when !welcomed, set welcomed = true, fire settings.write("pgsWelcomed", true) with errors swallowed, and notify(accountCard("welcome")) (D-04). On failure emit signedOut with player null and notify(accountCard("failed")) (D-11). Schedule nothing after a failure: no retry and no timer.
- signIn(): only when compete is on and status is "signedOut", run attempt("interactive"); otherwise do nothing (one attempt at a time).
- setCompete(on): coerce to a boolean; return at once when it equals current.compete (idempotent: no write, no emit, no provider call). Update the in-memory state first, then fire settings.write("compete", value) with errors swallowed. For false: ++seq (invalidates any in-flight attempt) and emit status "off" with player null. The provider has no sign-out and none is called (D-03). For true: start attempt("silent").
- stopCompeting(): setCompete(false).
- state(): the frozen current state; identity(): accountIdentity(current); chipView()/sheetView(): the account.js views over current.
- subscribe(fn): add it and return an unsubscribe function.
The controller never writes any key other than compete and pgsWelcomed, and never stores the player anywhere but `current`.

Create test/unit/accountChip.test.js covering every behaviour bullet with a scripted provider (deferred promises the test resolves or rejects, with a call log), recording settings { read, write }, a recording notify and a manual fake timer (setTimer/clearTimer).
  </action>
  <verify>
    <automated>node --test test/unit/accountChip.test.js test/unit/accountChip-dom.test.js test/unit/account.test.js test/unit/playGames.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function createAccountController" src/browser/accountChip.js` equals 1
    - `node --test test/unit/accountChip.test.js` passes with at least 20 tests, including Compete-OFF zero calls, the non-blocking boot, the welcome-once and failed-card paths, the timeout with a discarded late result, the concurrency, idempotency and Compete-OFF-wins cases, and the write-keys pin
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
  </acceptance_criteria>
  <done>One controller decides every sign-in, Compete and card outcome, proven with fakes, including the edge rows from the probe.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow)
- No Android build, APK or device step
</verification>

<success_criteria>
- D-01/D-02/D-11: the launch attempt is silent, non-blocking, skipped entirely with Compete OFF, and never retried automatically
- D-03/D-04/D-10: Stop competing, the welcome card once, and the sheet rows behave as specified
- ACCT-02's idempotency and concurrency edges are explicit, tested truths
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| src/browser/accountChip.js | new module | renderers + controller |
| `ACCOUNT_CLASSES` | export | the class contract 67-05's CSS covers |
| `renderAccountChip(button, view)` | export | the chip face |
| `renderAccountSheet({ rows, title }, view, handlers)` | export | the D-10 rows |
| `createAccountController({ provider, settings, notify, timeoutMs, setTimer, clearTimer })` | export | frozen { boot, signIn, setCompete, stopCompeting, state, identity, chipView, sheetView, subscribe } |
| test/unit/accountChip-dom.test.js, test/unit/accountChip.test.js | new tests | |

## Flagged assumptions (unresolved edge rows)

- PGS-02 (edge probe: unclassified): the silent attempt's 20-second timeout is Claude's discretion. A device whose Play Games answers later than that shows the failed card and stays signed out until the next launch or a Sign in tap.
- D-11 frequency: a Compete-ON player with no Play Games profile sees one failed card per launch. The card itself says how to turn Compete off, and that is the intended off-switch; the user may prefer once per install (a one-flag change).

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-07-SUMMARY.md` when done, with a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch: a first-ever sign-in shows the welcome card once and never again after a restart; a declined interactive sign-in shows the failed card and nothing modal; Stop competing turns the chip into the nobody glyph at once, and a later Compete ON signs back in silently. Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>
