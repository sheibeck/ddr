---
phase: 67-play-games-integration-account-chip
plan: 08
type: execute
wave: 3
depends_on: ["67-02", "67-03", "67-04", "67-05", "67-06", "67-07"]
files_modified:
  - mazeworld.html
  - docs/SHELL-MODULES.md
  - test/unit/shell-account.test.js
  - test/unit/shell-boards-entry.test.js
autonomous: true
requirements: [PGS-02, ACCT-01, ACCT-02]
user_setup: []

must_haves:
  truths:
    - "On native the shell builds the real provider (createPlayGames); in the browser dev loop it builds the in-memory fake, seeded signed in only when the dev setting pgsDevSignedIn is on; the shell never names the plugin package itself (D-12)"
    - "At launch the shell starts account.boot() after the title screen is initialized and never awaits it: boot, the title and play proceed whatever sign-in does; with Compete OFF no provider method is ever called (D-01, D-02)"
    - "Both account chips (band 2 and the title's top-right) render the controller's chip view on every account change: the initials avatar signed in with Compete ON, the nobody glyph otherwise (D-05, D-06, D-07)"
    - "Tapping either chip opens the account bottom sheet with the D-10 rows; the band-2 chip refuses to open while an encounter is active (the ☰ menu's rule); the scrim, Close and the Android back button close it (D-09, D-10)"
    - "The sheet's Sign in row calls the interactive sign-in, Stop competing and the Compete toggle go through the controller, and Settings closes the account sheet and opens the existing settings sheet through openSettingsSheet (D-03, D-10)"
    - "The welcome and failed cards reach the rail through window.mzRailLine, and only once the dungeon is visible: a card raised while the title, the roller or the title-opened Leaderboards panel is up is parked and delivered when the player lands on the map; the latest card wins, and each is delivered once (D-04, D-11)"
    - "The Leaderboards panel reads the account identity through its identity() seam and re-renders on every account change, so the strip goes live the moment sign-in succeeds and reverts when Compete goes OFF (D-08)"
    - "The ☰ menu's four legacy row ids, their listeners and the SETTINGS row are unchanged, and no new window.__mz bridge is added (the account lives in the module script)"
    - "The last-wave debug APK builds (npm run android:debug) with the whole phase in it; no device install, no release/AAB"
  prohibitions:
    - statement: "Must not block boot, the title screen or any tap on a Play Games call, and must not show any Play Games prompt, card or sheet as a modal over play"
      status: unresolved
      verification: judgment
  artifacts:
    - path: "mazeworld.html"
      provides: "the provider/controller wiring, chip and sheet handlers, card parking, the boards identity seam, the back-button branch, the non-awaited boot"
      contains: "createAccountController({"
    - path: "docs/SHELL-MODULES.md"
      provides: "the Play Games account section"
      contains: "playGames.js"
    - path: "test/unit/shell-account.test.js"
      provides: "source pins and region-extracted behaviour tests for the account wiring"
      min_lines: 140
  key_links:
    - from: "mazeworld.html (module: the account block)"
      to: "src/browser/accountChip.js#createAccountController"
      via: "createAccountController({ provider, settings: { read, write }, notify: parkAccountCard })"
      pattern: "notify: parkAccountCard"
    - from: "mazeworld.html (module: the Leaderboards panel instance)"
      to: "the account controller's identity()"
      via: "createBoardsPanel({ ..., identity: () => ... account.identity() ... })"
      pattern: "identity: \\(\\) =>"
    - from: "mazeworld.html (module: the account block)"
      to: "window.mzRailLine"
      via: "flushAccountCard() delivers the parked card when the dungeon is visible"
      pattern: "mzRailLine\\?\\.\\(card\\.title"
---

<objective>
Wire the Play Games account into the running shell. Implements D-01 (a non-blocking auto sign-in at launch with Compete ON), D-02 (Compete OFF: no provider call), D-04 and D-11 (the two rail cards, delivered only when the dungeon is visible), D-05 and D-06 (both chips live), D-07 (the faces follow the account state), D-08 (the Leaderboards identity strip reads the account), D-09 and D-10 (the sheet and its rows, with Settings routed to openSettingsSheet) and D-12 (native provider vs the browser fake, chosen by platform, with the dev seed). The ☰ menu's four legacy row ids keep routing unchanged (D-05; Claude's discretion on the wiring).

Purpose: the player sees and controls their Play Games identity from the HUD and the title, and the game never waits on it.
Output: mazeworld.html module wiring, the docs/SHELL-MODULES.md section, test/unit/shell-account.test.js, and the phase's last-wave debug APK.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/67-play-games-integration-account-chip/67-CONTEXT.md
@.planning/phases/67-play-games-integration-account-chip/67-04-SUMMARY.md
@.planning/phases/67-play-games-integration-account-chip/67-05-SUMMARY.md
@.planning/phases/67-play-games-integration-account-chip/67-06-SUMMARY.md
@.planning/phases/67-play-games-integration-account-chip/67-07-SUMMARY.md

<interfaces>
From earlier plans in this phase:
- src/browser/playGames.js: createPlayGames() (native; default loader), createFakePlayGames({ signedIn }).
- src/browser/accountChip.js: createAccountController({ provider, settings: { read, write }, notify, timeoutMs, setTimer, clearTimer }) → frozen { boot, signIn, setCompete, stopCompeting, state, identity, chipView, sheetView, subscribe }; renderAccountChip(button, chipView); renderAccountSheet({ rows, title }, sheetView, { onSignIn, onStopCompeting, onCompete, onSettings }); ACCOUNT_CLASSES.
- src/browser/boardsPanel.js: createBoardsPanel({ ..., identity }); refresh() re-renders only while open.
- src/browser/settings.js: readSettings(), writeSetting(key, value); the settings include pgsDevSignedIn.
- mazeworld.html markup (67-05): #mw-acct-chip, #mw-title-acct-chip, #mw-acct-sheet, #mw-acct-scrim, #mw-acct-title, #mw-acct-close, #mw-acct-rows.

The module script today (read the live code; Phase 66 is merged, and its 66-06/66-07 wiring is authoritative over this list): the imports block; the bridge region with the Phase 66 boards panel instance (`const boardsPanel = createBoardsPanel({ ... onRoute: routeFromBoards })`) and window.__mzBoards; applySettings/currentSettings and `applySettings(await readSettings())`; openSettingsSheet/closeSettingsSheet (panelMotion.open/close, then window.mzKeepPartyInView); `const panelMotion = createPanelMotion(...)`; the roller mount (createRoller({ ..., onCommit })); hideTitleScreen, showTitleScreen, refreshTitleDead, routeFromBoards, initTitleScreen (an IIFE); window.mzRailLine(title, line, tone, hold, icon, iconKey); registerNativeChrome's getGameContext (hasOpenModal / closeModal, with 66-07's title-mode boards branch). The classic script owns hasActiveEncounter() (a global function) and hudMenuEvent. The Phase 66 title-mode marker is document.body.dataset.boardsEntry === "title".
Rules: module-side state is presentation only, never a field on S; any new window.__mz name needs a src/browser/bridge.js entry plus `node tools/bridge-doc.mjs --write` (this plan adds none); the pinned engineAdapter import line stays byte-identical.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: module wiring: provider, controller, chips, sheet, card parking, boards identity, back button and the non-awaited boot</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html module imports (the pinned engineAdapter line and the Phase 65/66 separate import lines)
    - mazeworld.html module bridge region: the Phase 66 boards panel block (createBoardsPanel, window.__mzBoards, readBoardsData, routeFromBoards)
    - mazeworld.html: applySettings/currentSettings, openSettingsSheet/closeSettingsSheet and their listener lines, `applySettings(await readSettings())`, `const panelMotion`, the createRoller({ onCommit }) mount, hideTitleScreen/showTitleScreen/initTitleScreen, window.mzRailLine, surfaceWornReconcile (the rail-card delivery precedent), registerNativeChrome's getGameContext
    - mazeworld.html classic script: hasActiveEncounter() and hudMenuEvent (the ☰ encounter rule)
    - src/browser/accountChip.js and src/browser/playGames.js (the APIs)
  </read_first>
  <action>
All changes are in mazeworld.html's module script, with a "Phase 67 (…)" comment on each block.

1. Imports, each on its own line after the Phase 66 lines: `import { createPlayGames, createFakePlayGames } from "./src/browser/playGames.js";` and `import { createAccountController, renderAccountChip, renderAccountSheet } from "./src/browser/accountChip.js";`. The pinned engineAdapter import line stays byte-identical.

2. Boards identity (D-08): declare `let account = null;` at module level textually BEFORE the Phase 66 boards panel instance. Add `identity: () => (account ? account.identity() : { signedIn: false, player: null })` to that createBoardsPanel({ … }) call. Declaring it first means the identity arrow can never hit a temporal dead zone.

3. The account block (D-01, D-02, D-12), placed right after `applySettings(await readSettings())`'s try/catch:
   - `const pgsNative = !!window.Capacitor?.isNativePlatform?.();`
   - the provider: createPlayGames() when pgsNative, else createFakePlayGames({ signedIn: currentSettings?.pgsDevSignedIn === true }). Comment it: D-12, the browser dev loop never loads the plugin; the dev seed is read once here, next launch; native ignores it.
   - `account = createAccountController({ provider, settings: { read: readSettings, write: writeAccountSetting }, notify: parkAccountCard });`. writeAccountSetting(key, value) awaits writeSetting and then applySettings(next), so the module's settings mirror stays coherent (Phase 68 reads Compete from it).
   - renderAccountChips(): render account.chipView() into both #mw-acct-chip and #mw-title-acct-chip through renderAccountChip.
   - `account.subscribe(() => { renderAccountChips(); if the account sheet is open, re-render it; boardsPanel.refresh(); })`, then renderAccountChips() once, which shows the pending face until boot resolves.

4. The sheet (D-09, D-10): openAccountSheet({ fromHud }). When fromHud is true and `window.__mzState && hasActiveEncounter()`, return without opening (the ☰ menu's T-57-17 rule; the classic global function is visible to the module). Otherwise renderAccountSheet({ rows: #mw-acct-rows, title: #mw-acct-title }, account.sheetView(), handlers) and panelMotion.open(the sheet). The handlers are: onSignIn → account.signIn(); onStopCompeting → account.stopCompeting(); onCompete(v) → account.setCompete(v); onSettings → closeAccountSheet() then openSettingsSheet(). closeAccountSheet() calls panelMotion.close(the sheet) then window.mzKeepPartyInView?.(), the settings sheet's pattern. Wire the listeners: #mw-acct-chip click → openAccountSheet({ fromHud: true }); #mw-title-acct-chip click → openAccountSheet({ fromHud: false }); #mw-acct-scrim and #mw-acct-close clicks → closeAccountSheet(). Declare the functions as hoisted function declarations next to openSettingsSheet.

5. Card delivery (D-04, D-11, the rail is the one feedback surface): `let pendingAccountCard = null;`. Three function declarations:
   - dungeonVisible(): true only when #mw-title-screen is hidden, #mw-roller-screen is hidden, and document.body.dataset.boardsEntry is not "title".
   - parkAccountCard(card): ignore a null card; otherwise set pendingAccountCard = card (the latest wins) and call flushAccountCard().
   - flushAccountCard(): when a card is parked and dungeonVisible(), clear the slot first, then `window.mzRailLine?.(card.title, card.line, card.tone, card.hold)`.
   Call flushAccountCard() at the end of hideTitleScreen(), at the end of the roller's onCommit (after the map lands), and at the end of routeFromBoards' "dungeon" branch. It is a rail card only, never a modal.

6. The Android back button: in getGameContext, OR `!document.getElementById("mw-acct-sheet")?.hidden` into hasOpenModal. Make closeModal's FIRST statement close the account sheet and return when it is open (it is the top-most layer), ahead of the Phase 66 boards branch and the ☰ escape.

7. The boot kick-off (D-01): directly after the initTitleScreen IIFE, call `account.boot().catch(() => {});`, which is deliberately not awaited. Comment it: the launch sign-in never blocks boot, the title or play; with Compete OFF the controller calls no provider method (D-02); on native the plugin is reached only through playGames.js.

8. No new window.__mz name. The ☰ menu markup, HUD_MENU_ITEMS and the four legacy listener lines stay untouched. Comments and new lines use none of tools/stale-terms.mjs's retired words.
  </action>
  <verify>
    <automated>node --test test/unit/hud-menu-layout.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js test/unit/shell-no-content-copies.test.js test/unit/account-layout.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "./src/browser/playGames.js";' mazeworld.html` equals 1 and `grep -c 'from "./src/browser/accountChip.js";' mazeworld.html` equals 1
    - `grep -c "notify: parkAccountCard" mazeworld.html` equals 1, `grep -c "account.boot().catch(" mazeworld.html` equals 1 and `grep -c "function flushAccountCard" mazeworld.html` equals 1
    - `grep -c "identity: () =>" mazeworld.html` is at least 1
    - The pinned line `import { boot, dispatch, startNewRun, waitForPending, takeBootWornReport } from "./src/browser/engineAdapter.js";` still counts exactly 1
    - `node --test test/unit/hud-menu-layout.test.js test/unit/bridge-registry.test.js test/unit/stale-terms.test.js` passes
  </acceptance_criteria>
  <done>The shell runs sign-in in the background, shows the account on both chips and the Leaderboards strip, opens the sheet from either chip, and delivers the two cards on the rail at the right moment.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: docs/SHELL-MODULES.md, the shell tests, and the last-wave debug APK</name>
  <files>docs/SHELL-MODULES.md, test/unit/shell-account.test.js, test/unit/shell-boards-entry.test.js</files>
  <read_first>
    - docs/SHELL-MODULES.md (the intro paragraph and the "### Leaderboards panel (Phase 66)" section)
    - test/unit/shell-boards-panel.test.js and test/unit/shell-boards-entry.test.js (Phase 66's comment stripping, region extraction and evaluate-with-fakes helpers, and any closeModal first-statement pin)
    - test/unit/shell-combat-over.test.js (the region-extraction helper)
    - mazeworld.html (Task 1's changes)
    - src/browser/accountChip.js (ACCOUNT_CLASSES)
  </read_first>
  <behavior>
    - Source pins over the comment-stripped module script: the two Phase 67 import lines; createPlayGames( and createFakePlayGames( both present, selected by an isNativePlatform check; the plugin's npm package name absent from the whole comment-stripped shell (D-12); `account.boot()` appears after the initTitleScreen IIFE and is not preceded by await; the createBoardsPanel({ … }) region contains identity:, and `let account = null;` appears before it; getGameContext's hasOpenModal mentions mw-acct-sheet and closeModal's first statement handles the account sheet; openAccountSheet checks hasActiveEncounter() for the HUD chip; the onSettings handler calls closeAccountSheet() before openSettingsSheet(); flushAccountCard() is called inside hideTitleScreen, the roller's onCommit and routeFromBoards' dungeon branch
    - Card parking behaviour (dungeonVisible/parkAccountCard/flushAccountCard extracted by region and evaluated with a fake document, a fake body dataset and a recording window.mzRailLine): a card parked while the title is visible is not delivered; hiding the title and flushing delivers it once with (title, line, tone, hold); two cards parked before a flush deliver only the latest; a flush with nothing parked does nothing; a card parked while the body is in boards title mode waits
    - Every ACCOUNT_CLASSES entry has a CSS rule in mazeworld.html's style block (a cross-check of 67-05's literal list against 67-07's export)
    - The ☰ menu is untouched: the four row ids and their listener lines (openMarksLegend, the centre handler, openCampSheet, openSettingsSheet) are still present
    - The comment-stripped shell still has no network-capable call (fetch(, XMLHttpRequest, WebSocket, EventSource, sendBeacon)
  </behavior>
  <action>
1. docs/SHELL-MODULES.md: name playGames.js, account.js and accountChip.js in the intro paragraph, and add a "### Play Games account (Phase 67)" section after the Leaderboards panel section. It covers: playGames.js as the D-12 seam (native vs fake, lazy plugin load, no sign-out, the reserved Phase 68 methods); account.js as the pure view model; accountChip.js's renderers and controller (boot, signIn, setCompete, subscribe; the edge rules: one attempt at a time and Compete OFF always wins); the shell wiring (the provider chosen by isNativePlatform, the dev seed, both chips, the sheet with its encounter refusal and back-button close, card parking until the dungeon is visible, the Leaderboards identity seam); and "no new window.__mz bridge: the account lives in the module script". Then run `node tools/bridge-doc.mjs --check`, which must pass unchanged.

2. Create test/unit/shell-account.test.js covering every behaviour bullet, with the comment-stripping and region-extraction helpers the Phase 66 shell tests use and mazeworld.html read with CRLF normalized. Build the plugin package name in the test from two string halves so no other scan counts it.

3. When test/unit/shell-boards-entry.test.js pins the Phase 66 boards branch as closeModal's literal first statement, update that pin to "the first statement after the Phase 67 account-sheet branch", with a one-line comment, in this commit.

4. Run `npm test`. Then build the phase's last-wave debug APK (the deferred-UAT protocol builds it after the last wave): in a worktree run `npm ci` first when node_modules is missing, and copy android/local.properties from C:/projects/mazeworld/android/ when missing (never commit it); run `npm run android:debug`; then confirm `git diff --quiet HEAD -- android/gradle.properties` (restore it with git checkout when cap sync reordered it). Record the APK path and the build tail in the SUMMARY. No device install and no release/AAB build.
  </action>
  <verify>
    <automated>node --test test/unit/shell-account.test.js test/unit/shell-boards-entry.test.js test/unit/shell-boards-panel.test.js && node tools/bridge-doc.mjs --check && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-account.test.js` passes with at least 16 tests, including the source pins, the card-parking behaviour, the ACCOUNT_CLASSES cross-check and the ☰-unchanged pin
    - `grep -c "Play Games account (Phase 67)" docs/SHELL-MODULES.md` equals 1 and `node tools/bridge-doc.mjs --check` exits 0
    - `npm test` reports 0 failures, or only failures pre-existing on master (the failing-test set must not grow)
    - `npm run android:debug` exited 0 and the SUMMARY records the debug APK path; `git diff --quiet HEAD -- android/gradle.properties` exits 0 afterwards
  </acceptance_criteria>
  <done>The wiring is documented and pinned, the whole suite is green, and the phase's debug APK is built for the deferred device batch.</done>
</task>

</tasks>

<verification>
- `npm test` green (or only failures pre-existing on master; the set must not grow), including bridge-registry, stale-terms, hud-menu-layout, account-layout and parity
- `node tools/bridge-doc.mjs --check` exits 0
- `npm run android:debug` succeeds; no device install, no release/AAB build
</verification>

<success_criteria>
- PGS-02: auto sign-in runs at launch without blocking, and decline, no profile or failure leave the game fully playable
- ACCT-01: the chip shows the avatar signed in and the nobody glyph signed out, on band 2 and the title
- ACCT-02: tapping the chip opens the sheet with Sign in / Stop competing, the Compete toggle and Settings
</success_criteria>

## Artifacts this phase produces (this plan)

| Symbol / path | Kind | Notes |
|---|---|---|
| `let account` / `createAccountController({ … notify: parkAccountCard })` | shell wiring | the one account controller |
| `pgsNative`, the provider selection | shell wiring | native → createPlayGames(), browser → createFakePlayGames({ signedIn: pgsDevSignedIn }) |
| `writeAccountSetting`, `renderAccountChips` | module functions | the settings mirror stays coherent; both chips render |
| `openAccountSheet({ fromHud })`, `closeAccountSheet()` | module functions | the sheet, refused on the HUD during an encounter |
| `dungeonVisible()`, `parkAccountCard(card)`, `flushAccountCard()` | module functions | rail delivery once the map is visible |
| `identity:` on createBoardsPanel | shell wiring | the D-08 strip goes live |
| the getGameContext account-sheet branch | shell wiring | the Android back button closes the sheet first |
| `account.boot().catch(() => {})` after initTitleScreen | shell wiring | non-blocking launch sign-in |
| docs/SHELL-MODULES.md "Play Games account (Phase 67)" | doc | |
| test/unit/shell-account.test.js | new test | |
| the last-wave debug APK | build output | for the Phase 69 device batch (not installed here) |

## Flagged assumptions (unresolved edge rows)

- ACCT-02: a card raised during combat is handed to the rail as usual, and the rail's own combat hiding decides whether it is seen before it expires. This was judged acceptable, because both cards come from boot, a Compete toggle or a Sign in tap, none of which happen mid-fight except a slow launch attempt.

<output>
Create `.planning/phases/67-play-games-integration-account-chip/67-08-SUMMARY.md` when done, with the debug APK path, plus a "Human verification (deferred to end of run)" heading listing, for the Phase 69 batch (docs/UAT-v2.0.md): (1) the band-2 chip sits left of ☰ and the title chip in the corner, both showing the nobody glyph at first launch with the placeholder APP_ID and one failure card after ENTER; (2) after the console setup, auto sign-in on a tester account shows the initials on both chips, the welcome card once, and the Leaderboards strip with PLAY GAMES · SIGNED IN; (3) declining the interactive Sign in shows the failure card and no modal; (4) no Play Games profile on the device leaves the game fully playable; (5) Stop competing flips both chips and the strip to nobody, and with Compete OFF a cold boot makes no Play Games network traffic (capture); (6) the account sheet's Settings row opens the settings sheet above the title screen; (7) the Android back button closes the account sheet first; (8) the chip is ignored during combat. Do NOT create `.planning/phases/67-*/deferred-items.md`.
</output>
