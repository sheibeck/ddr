---
phase: 64-device-close-uat-batch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/UAT-v1.9.md
autonomous: true
requirements: [GSCR-12]
must_haves:
  truths:
    - "A v1.9 debug APK built from master after Phase 63 exists outside the repo as ddr-v1.9-<shorthash>-debug.apk, with its sha256 recorded"
    - "docs/UAT-v1.9.md carries every human_verification item from 61/62/63-VERIFICATION.md (7 + 7 + 8) plus 63-04's two carried-forward device assumptions, each in a | # | Step | Who | Result | row with an empty Result cell"
    - "Items needing a special setup are marked 'if available'; a skip is recorded as 'not reached', never as a pass"
  artifacts:
    - path: docs/UAT-v1.9.md
      provides: the one batched Pixel 7 checklist for v1.9
  key_links:
    - from: docs/UAT-v1.9.md
      to: .planning/phases/6[1-3]-*/6?-VERIFICATION.md
      via: sources section lists each file and its item count
---

<objective>
Build the v1.9 debug APK and write `docs/UAT-v1.9.md`, the milestone's one batched Pixel 7 checklist (GSCR-12, the checklist half). Written by the orchestrator (planner skipped: a small, mechanical phase with the user's phone waiting). It is executed inline.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build the debug APK</name>
  <read_first>package.json (android:debug script), .planning/phases/64-device-close-uat-batch/64-CONTEXT.md</read_first>
  <action>Run `npm run android:debug` on the clean master HEAD. Copy `android/app/build/outputs/apk/debug/app-debug.apk` to the session scratchpad as `ddr-v1.9-<shorthash>-debug.apk`, and record its byte size and sha256. Commit no build output.</action>
  <acceptance_criteria>
    - `npm run android:debug` exits 0
    - the renamed APK exists and `sha256sum` prints its hash
    - `git status --short` shows no build output staged
  </acceptance_criteria>
</task>
<task type="auto">
  <name>Task 2: Write docs/UAT-v1.9.md</name>
  <read_first>docs/UAT-v1.8.md (format), .planning/phases/61-gear-rules-store-purchase-fix/61-VERIFICATION.md, .planning/phases/62-gear-tab-layout-rebuild/62-VERIFICATION.md, .planning/phases/63-action-sheet-combat-lock-accessibility/63-VERIFICATION.md, 63-04-SUMMARY.md</read_first>
  <action>Write the checklist: the build header (APK name, full commit, sha256, Settings version label), protocol (an in-session walk with the user, results recorded as stated, findings go to todos), sources with counts, and the suggested order: Gear outside a fight → the sheet → store → fight → reduced motion → TalkBack. Sections A (Gear tab layout, from 62), B (action sheet, from 63), C (store and upgrade line, from 61), D (combat lock, from 61 + 63), E (reduced motion + back), F (TalkBack). Every row has an empty Result cell; setup-dependent rows are marked "if available".</action>
  <acceptance_criteria>
    - `grep -c "^| [A-F][0-9]" docs/UAT-v1.9.md` ≥ 24
    - the file names all three VERIFICATION sources and the APK sha256
    - `grep -n "WP" docs/UAT-v1.9.md` finds no player-facing WP
  </acceptance_criteria>
</task>
</tasks>

## Artifacts this phase produces
- `docs/UAT-v1.9.md` (new)
- `ddr-v1.9-<shorthash>-debug.apk` (scratchpad, not committed)
