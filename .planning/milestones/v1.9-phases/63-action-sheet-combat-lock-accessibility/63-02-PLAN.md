---
phase: 63-action-sheet-combat-lock-accessibility
plan: 02
type: execute
wave: 2
depends_on: ["63-01"]
files_modified:
  - src/browser/gearSheet.js
  - test/unit/gear-sheet-dom.test.js
autonomous: true
requirements: [GSCR-07, GSCR-08, GSCR-09, GSCR-10, GRULE-02]

must_haves:
  truths:
    - "renderGearSheet(host, state, target, deps) fills #mw-gear-sheet-label / -title / -note / -why / -actions / -cancel (GEAR_SHEET_IDS) from gearSheetModel via createElement/textContent only, and returns true; for a target gearSheetModel resolves to null it returns false and writes nothing"
    - "#mw-gear-sheet-why shows lootCompare's explained line for a legal bag weapon/armor (the Spiked Staff case reads 'd8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade') and is hidden when the model's why is ''"
    - "Every action is a real <button type='button' class='mw-gsheet-act'> in model order, with aria-label = its label, a label span, a sub span whose id is mw-gear-sheet-sub-<n> and which the button names in aria-describedby, and an aria-hidden '›' chevron"
    - "A greyed action carries data-off='1' and aria-disabled='true', has NO click handler (tapping it does nothing), and its sub span's text is the engine's reason; an enabled action is wired through deps.guardTap(btn, handler)"
    - "Tapping an enabled non-confirm action calls deps.closeGearSheet() FIRST and then exactly one dispatch dep with the run's arguments: equipItem(i) (weapon/armor, one argument) or equipItem(i, slot) (cloak/jewelry), unequip(slot), useItem({ slot }) or useItem(i), dropItem(i)"
    - "DROP is a tap-again confirm: the first tap relabels it 'DROP IT? · tap again' (label span and aria-label), sets data-armed, dispatches nothing and keeps the sheet open; a second tap within 3000 ms closes the sheet and then calls dropItem(i); after 3000 ms, or on any re-render, it reverts to 'DROP'. The armed state is module-local, never on state"
    - "CANCEL reads 'CANCEL' and is wired through deps.guardTap to deps.closeGearSheet()"
    - "Mid-fight (state.combat set) the rendered SWAP / EQUIP / UNEQUIP rows are greyed with the Phase 61 line as their sub text while USE and DROP stay wired"
    - "Edge GSCR-08/encoding: an item named with an apostrophe, double quotes, '<b>' markup and an emoji renders verbatim through textContent in the title, in 'SWAP FOR <name>' / 'EQUIP <name>' labels and in '<name> comes off and goes to the bag.' subs; no node under the sheet ids carries an HTML-string write, and gearSheet.js (comments stripped) has zero HTML-string sinks"
    - "Edge GSCR-09/ordering: in a shared call log, closeGearSheet is recorded before the dispatch dep for every enabled action (DROP's second tap included), so the sheet is gone before the engine's events reach the rail and it never re-renders from post-action state"
    - "Rendering twice on the same host serializes byte-identically, and the serialized sheet carries no standalone wp/WP token"
  artifacts:
    - path: "src/browser/gearSheet.js"
      provides: "GEAR_SHEET_IDS, SHEET_DROP_CONFIRM_MS and renderGearSheet(host, state, target, deps)"
      contains: "export function renderGearSheet("
    - path: "test/unit/gear-sheet-dom.test.js"
      provides: "recordingDom render, a11y, dispatch-order, tap-again, encoding and idempotency suite"
      min_lines: 220
  key_links:
    - from: "src/browser/gearSheet.js#renderGearSheet"
      to: "src/browser/gearSheet.js#gearSheetModel"
      via: "one model call per render; the renderer holds no rule of its own"
      pattern: "gearSheetModel\\(state, target\\)"
    - from: "src/browser/gearSheet.js#renderGearSheet"
      to: "deps.guardTap / deps.closeGearSheet / deps.equipItem|unequip|useItem|dropItem"
      via: "the tabDeps() bridges; no window/document global"
      pattern: "deps\\.closeGearSheet"
  prohibitions:
    - requirement_id: GSCR-09
      category: transparency
      flagged: true
      statement: "MUST NOT render any outcome or result text inside the sheet, and MUST NOT raise a toast: the sheet closes and the rail speaks in voice"
    - requirement_id: GSCR-09
      category: honesty
      flagged: true
      statement: "MUST NOT let a tap on a greyed action dispatch anything: a greyed row is explanation, not a hidden live button"
---

<objective>
Add the sheet's DOM renderer to `src/browser/gearSheet.js`. `renderGearSheet(host, state, target, deps)` turns Plan 01's model into the sheet body: the header, one real button per action with the reason wired for TalkBack, CANCEL, and the DROP tap-again confirm. Every live action closes the sheet and then dispatches through the existing tabDeps bridges.

Purpose: GSCR-07/08's actions become tappable, GSCR-09's reasons become visible and accessible with dispatch ordered for the rail, GSCR-10's CANCEL and button semantics are in place, and GRULE-02's greying renders. There is no shell wiring yet: Plan 04 mounts this through a `window.__mzGearSheet` bridge and owns the open/close lifecycle.

Implements CONTEXT: "DROP arms a tap-again confirm inside the sheet" ("DROP IT? · tap again", 3 s, DOM-local), "Outcome goes to the rail only" (no toast, no in-sheet result, a greyed tap does nothing), and "TalkBack" (real buttons, aria-disabled with the reason as the accessible description). Discretion: the ids `mw-gear-sheet-*`, the classes `mw-gsheet-*`, and `data-off` as the greyed hook (not an ARIA-state selector, because three sibling CSS suites forbid that token in the style block).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-CONTEXT.md
@.planning/phases/63-action-sheet-combat-lock-accessibility/63-01-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
From Plan 01 (src/browser/gearSheet.js). Read 63-01-SUMMARY.md for any recorded deviation.
- `GEAR_SHEET_COPY` groups: head{worn,empty,bag,fromBag,sep,nothingWorn}, act{use,unequip,discard,swapFor,equip,equipTo,swapInto,drop,dropConfirm,nothing}, sub{…}, use{…}, cancel.
- `gearSheetModel(state, target)` → null | `{ target, label, title, note, why, actions: [{ key, label, sub, reason, enabled, run, confirm }] }`. `run` is an engine action: `{ type: "useItem", slot }` or `{ type: "useItem", i }`, `{ type: "unequipSlot", slot }`, `{ type: "equipItem", i }` or `{ type: "equipItem", i, slot }`, `{ type: "dropItem", i }`, or null (NOTHING TO EQUIP only).
From src/browser/gearTab.js: `GEAR_COPY.chevron` "›".
tabDeps() (mazeworld.html, as of Phase 62): `guardTap(btn, fn)`, `useItem(ref)`, `equipItem(i, slot)` (forwards slot to window.mzEquipItem, which omits it from the action when undefined), `unequip(slot)`, `dropItem(i)`. Plan 04 adds `openGearSheet` / `closeGearSheet`. In this plan's tests deps are spies.
test/unit/harness/recordingDom.js: `createRecordingDocument()`; getElementById auto-creates; elements support dataset, setAttribute/getAttribute, hidden, replaceChildren, textContent, onclick; `serializeElements(ids)`; `el._content.kind` is "html" after an HTML-string write.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: renderGearSheet — header, action buttons, greyed a11y, dispatch order</name>
  <files>src/browser/gearSheet.js, test/unit/gear-sheet-dom.test.js</files>
  <read_first>
    - src/browser/gearSheet.js (Plan 01's copy and model, the whole file)
    - src/browser/gearTab.js (renderGearTab :773-938, the el()/head()/useCellEl() closure style and the createElement/textContent discipline to mirror)
    - test/unit/gear-tab-dom.test.js (:150-190 spy/gearDeps/renderFresh helpers; :562-607 encoding, idempotency and no-WP tests, the patterns to mirror)
    - test/unit/harness/recordingDom.js (:195-440 element model, :480-523 serializer)
    - mazeworld.html (:3354-3381 armEncounterButtons/guardTap, the guard contract deps.guardTap stands in for; :2930-2949 tabDeps)
    - design/Mazeworld Gear.dc.html (:117-137 the sheet markup, the visual reference)
  </read_first>
  <behavior>
    - A spy deps object records every call into one shared ordered log: guardTap stub sets btn.onclick = fn and logs the button; closeGearSheet, equipItem, unequip, useItem and dropItem log their name and args.
    - Worn jewelry1 sheet (richChar-style, bag not full, one fitting bag jewel): label/title/note text equal the model's; #mw-gear-sheet-why is hidden; #mw-gear-sheet-actions holds one button per model action in order, each with type "button", class "mw-gsheet-act", data-key = action.key, aria-label = action.label; each button's sub span id is "mw-gear-sheet-sub-<index>" and equals its aria-describedby.
    - Enabled actions: guardTap was called once per enabled action button plus once for CANCEL. Clicking SWAP FOR logs ["closeGearSheet"] then ["equipItem", i, "jewelry1"]. USE logs close then ["useItem", { slot: "jewelry1" }]. UNEQUIP logs close then ["unequip", "jewelry1"].
    - Weapon swap dispatch: equipItem is called with exactly one argument (i), with no undefined second argument recorded.
    - Bag staff USE: close then ["useItem", i].
    - Greyed: the thief fixture's jewelry1 UNEQUIP (full bag) has data-off "1", aria-disabled "true", onclick null, its sub text "Bag is full — free a slot first.", and guardTap was never called with it.
    - Combat: the Spiked Staff state with combat { pending: true } renders SWAP INTO WEAPON greyed with sub === the model's reason (the Phase 61 line) while DROP has a click handler. The same state without combat has both wired, and #mw-gear-sheet-why shows the explained line.
    - Vanished target: renderGearSheet(host, state, { from: "bag", i: 99, n: "x" }, deps) returns false and the actions container receives no new children.
    - Returns true for every resolvable target in the thief and mu fixtures (all five worn slots plus every bag card).
  </behavior>
  <action>
Extend `src/browser/gearSheet.js`. Keep Plan 01's exports byte-stable.

1. `export const GEAR_SHEET_IDS = Object.freeze({ label: "mw-gear-sheet-label", title: "mw-gear-sheet-title", note: "mw-gear-sheet-note", why: "mw-gear-sheet-why", actions: "mw-gear-sheet-actions", cancel: "mw-gear-sheet-cancel" })`. Plan 04's markup declares exactly these ids inside `#mw-gear-sheet`.

2. `export function renderGearSheet(host, state, target, deps = {})`:
- `doc = host.ownerDocument`.
- First call the module-local drop-confirm revert (Task 2 adds it; add a no-op-safe stub now). A fresh render never keeps an armed DROP.
- `m = gearSheetModel(state, target)`. If `m` is null, return false before touching the DOM.
- Write label, title and note with textContent. Set the why element's textContent to `m.why` and `hidden = !m.why`.
- Build the action buttons with small local closures in the renderGearTab style, all inside this function's own region:
  - The button: `doc.createElement("button")`, `type = "button"`, `className = "mw-gsheet-act"`, `dataset.key = a.key`, and `setAttribute("aria-label", a.label)`.
  - Its first child is a span `mw-gsheet-act-main` holding a span `mw-gsheet-act-label` (a.label) and a span `mw-gsheet-act-sub`. The sub span's `id = "mw-gear-sheet-sub-" + index` and its text is a.sub.
  - Its second child is a span `mw-gsheet-chev` with `GEAR_COPY.chevron` and `aria-hidden="true"`.
  - The button's `aria-describedby` is set to the sub span's id.
- Greyed (`!a.enabled`): `dataset.off = "1"` and `setAttribute("aria-disabled", "true")`. Assign NO click handler, so tapping does nothing.
- Enabled: `deps.guardTap ? deps.guardTap(btn, handler) : (btn.onclick = handler)`. The handler for a non-confirm action calls `deps.closeGearSheet?.()` and THEN a local `dispatchRun(a.run, deps)`. Task 2 adds the confirm handler.
- Replace the actions container's children with the buttons in model order.
- CANCEL: textContent `GEAR_SHEET_COPY.cancel`, wired through guardTap (or onclick) to `deps.closeGearSheet?.()`.
- Return true.

3. `dispatchRun(run, deps)` is module-private. It switches on `run.type`:
- "equipItem": `run.slot ? deps.equipItem?.(run.i, run.slot) : deps.equipItem?.(run.i)`. Never pass an explicit undefined second argument.
- "unequipSlot": `deps.unequip?.(run.slot)`.
- "useItem": `deps.useItem?.(run.slot ? { slot: run.slot } : run.i)`.
- "dropItem": `deps.dropItem?.(run.i)`.

4. Discipline for this file:
- Only createElement, textContent, setAttribute, dataset and replaceChildren.
- No HTML-string sinks: never the element HTML-setter properties, not even named in a comment. Task 2's acceptance greps for them.
- No window or document global; reach the page only through `host.ownerDocument`.
- No outcome text anywhere in the sheet (rail only).

5. Create `test/unit/gear-sheet-dom.test.js`. Write the `<behavior>` tests first (RED), then implement (GREEN). Mount on `createRecordingDocument()` with `host = doc.document.getElementById("mw-gear-sheet")`. Reuse the richChar/fixedChar builder pattern (copy it locally), `fixedStates()` from the harness, and the Spiked Staff hero from `newRun(7)` as in Plan 01's test. Compare rendered text against `gearSheetModel` output, never retyped rule literals, except the strings the behavior list pins.
  </action>
  <verify>
    <automated>node --test test/unit/gear-sheet-dom.test.js test/unit/gear-sheet-model.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-sheet-dom.test.js` exits 0 with at least 12 passing tests
    - `grep -c "export function renderGearSheet(" src/browser/gearSheet.js` = 1 and `grep -c "export const GEAR_SHEET_IDS" src/browser/gearSheet.js` = 1
    - `grep -c "aria-describedby" src/browser/gearSheet.js` >= 1, `grep -c "deps.closeGearSheet" src/browser/gearSheet.js` >= 1, `grep -c "gearSheetModel(state, target)" src/browser/gearSheet.js` >= 1
    - `node --test test/unit/gear-sheet-model.test.js` still exits 0 (Plan 01 unchanged)
    - `git diff --stat -- engine/ content/ test/parity/ mazeworld.html src/browser/gearTab.js test/unit/fixtures/` prints nothing
  </acceptance_criteria>
  <done>The sheet body renders from the model alone. Greyed actions are inert and say why to sighted and TalkBack users. Every live tap closes the sheet, then dispatches exactly one existing bridge with the right arguments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DROP tap-again confirm, CANCEL, encoding, idempotency and contract pins</name>
  <files>src/browser/gearSheet.js, test/unit/gear-sheet-dom.test.js</files>
  <read_first>
    - src/browser/gearSheet.js (as left by Task 1)
    - src/browser/gearTab.js (:532-553 DROP_CONFIRM_MS / dropConfirmRevert / revertDropConfirm, the module-local revert pattern to mirror under distinct names)
    - test/unit/gear-tab-dom.test.js (:562-607 encoding / HTML-write walk / idempotency / no-WP patterns)
    - test/unit/gearTab.test.js (:113-119 the no-globals pin pattern)
  </read_first>
  <behavior>
    - DROP first tap (via its guardTap-installed handler): the label span reads "DROP IT? · tap again", aria-label equals the same, dataset.armed "1", closeGearSheet and dropItem are NOT called, and the sheet body is otherwise unchanged.
    - DROP second tap within the window: the log reads close, then ["dropItem", i]; the armed state is cleared.
    - With node:test mock timers enabled for setTimeout: first tap, then tick 2999 → still armed; tick 1 more → the label reverts to "DROP", aria-label "DROP", no data-armed, and nothing was dispatched.
    - Re-render while armed: calling renderGearSheet again reverts, so the fresh DROP reads "DROP" and a single tap only arms again.
    - CANCEL: the text is "CANCEL", and clicking it logs exactly ["closeGearSheet"].
    - Edge GSCR-08/encoding: a jewel named `Thief's "Lucky" <b>Ring</b> 🎲` in the bag while jewelry1 is worn. Its bag sheet title reads the name verbatim. The worn jewelry1 sheet's label reads "SWAP FOR " + the name. The bag sheet's SWAP INTO JEWELRY 1 sub starts with the worn piece's name when that piece carries the tricky name. No node under the six GEAR_SHEET_IDS has _content.kind "html".
    - Edge GSCR-09/ordering: for every enabled action in the thief and Spiked Staff sheets (DROP via two taps), the index of "closeGearSheet" in the log is lower than the index of the dispatch call.
    - Idempotency: rendering the same target twice on one host serializes the six ids byte-identically.
    - No WP: the serialized sheet for the thief's worn armor and bag sheets carries no standalone wp/WP token (the same regex gear-tab-dom.test.js uses).
    - Contract: gearSheet.js, CRLF-normalised and comments stripped, contains no `window.` or `document.` global read and at least one ownerDocument read; it contains no HTML-string sink.
  </behavior>
  <action>
In `src/browser/gearSheet.js`:

1. Module-local confirm state, following gearTab.js's drop-confirm revert pattern under distinct names so gearTab.test.js's once-each pins on the gearTab names never see a second declaration:
- `export const SHEET_DROP_CONFIRM_MS = 3000;`
- `let sheetDropRevert = null;`
- `function revertSheetDrop() { if (!sheetDropRevert) return; const r = sheetDropRevert; sheetDropRevert = null; r(); }`

Replace Task 1's stub call at the top of renderGearSheet with `revertSheetDrop()`. This state is presentation-only and never on state; serializeRun spreads state.

2. The confirm handler for an action with `confirm: true` (DROP):
- If the button is not armed: call `revertSheetDrop()`, set the label span text and the aria-label to `GEAR_SHEET_COPY.act.dropConfirm`, and set `dataset.armed = "1"`. Start `timer = setTimeout(revertSheetDrop, SHEET_DROP_CONFIRM_MS)`. Set `sheetDropRevert` to a function that clears the timer, restores the label text and aria-label to `a.label`, and deletes `dataset.armed`. Return without closing or dispatching.
- If it is armed: `revertSheetDrop()`, then `deps.closeGearSheet?.()`, then `dispatchRun(a.run, deps)`.
- Use the plain setTimeout/clearTimeout globals, as gearTab.js does. They are not window reads.

3. Keep CANCEL as Task 1 wired it.

4. Add the `<behavior>` tests to `test/unit/gear-sheet-dom.test.js`. Use `t.mock.timers.enable({ apis: ["setTimeout"] })` for the 3000 ms revert (Node >= 22). The contract test strips comments with the same order-sensitive stripper gear-tab-dom.test.js uses, then asserts:
- no `/\bwindow\./` and no `/\bdocument\./`
- `/ownerDocument/` is present
- none of the three HTML-string sinks: the element's HTML setter, its outer-HTML setter and the adjacent-HTML insert method. Build their names in the test from split string fragments so the test source itself stays clean.
  </action>
  <verify>
    <automated>node --test test/unit/gear-sheet-dom.test.js test/unit/gear-sheet-model.test.js test/unit/gearTab.test.js && npm run build:www && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-sheet-dom.test.js` exits 0 with at least 22 passing tests, including names beginning "Edge GSCR-08/encoding" and "Edge GSCR-09/ordering"
    - `grep -c "innerHTML" src/browser/gearSheet.js` = 0 and `grep -c "insertAdjacentHTML" src/browser/gearSheet.js` = 0
    - `grep -c "export const SHEET_DROP_CONFIRM_MS = 3000;" src/browser/gearSheet.js` = 1 and `grep -c "function revertSheetDrop()" src/browser/gearSheet.js` = 1
    - `grep -c "DROP_CONFIRM_MS = 3000;" src/browser/gearTab.js` = 1 (gearTab.js untouched)
    - `npm run build:www` exits 0 and `npm test` reports fail 0 (worktree CRLF false-fails in class-pass-ledger/flee-ledger excepted)
    - `git diff --stat -- engine/ content/ test/parity/ mazeworld.html src/browser/gearTab.js test/unit/fixtures/` prints nothing
  </acceptance_criteria>
  <done>DROP needs a deliberate second tap within 3 s, reverts on time or on re-render, and never dispatches on the first tap. CANCEL closes. Hostile names render as text. The renderer is idempotent, WP-free and pinned to the module contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file → sheet DOM | item names and txt from a tamperable save become button labels, titles and sub-lines |
| pointer → sheet action | a tap that opened the sheet can land on a freshly rendered action (a ghost tap) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-63-03 | Tampering | renderGearSheet | medium | mitigate | createElement/textContent only. A test walks every node under the sheet ids for an HTML-string write, and a comment-stripped source pin forbids the three HTML-string sinks |
| T-63-04 | Spoofing (ghost tap) | enabled sheet actions | low | mitigate | Every live action and CANCEL go through deps.guardTap (the Phase 32 250 ms arm window). Greyed actions carry no handler at all |
| T-63-05 | Repudiation (accidental drop) | DROP | low | mitigate | Tap-again confirm with a 3000 ms revert. The first tap never dispatches, and any re-render disarms it |
</threat_model>

<verification>
- `node --test test/unit/gear-sheet-dom.test.js test/unit/gear-sheet-model.test.js` green
- `npm run build:www && npm test` green (fail 0)
- gearSheet.js has no HTML-string sink and no window/document global; engine/content/parity/mazeworld.html/gearTab.js/fixtures untouched
</verification>

<success_criteria>
- The sheet renders every model action as an accessible button; greyed buttons explain and do nothing
- Every live action closes the sheet before it dispatches one existing bridge; DROP needs two taps
</success_criteria>

## Probe fallback record

This plan authors 2 of the 13 edge rows: GSCR-08/encoding and GSCR-09/ordering (see 63-01-PLAN.md for the phase-wide ledger). It keeps 2 prohibitions, flagged-unverified with no check descriptor.

## Artifacts this phase produces

This plan's share.

| Artifact | Kind | Where |
|---|---|---|
| GEAR_SHEET_IDS | frozen id map (the six sheet render roots) | src/browser/gearSheet.js |
| renderGearSheet(host, state, target, deps) | DOM renderer (createElement/textContent only) | src/browser/gearSheet.js |
| SHEET_DROP_CONFIRM_MS / revertSheetDrop | module-local tap-again confirm state | src/browser/gearSheet.js |
| test/unit/gear-sheet-dom.test.js | new DOM suite (a11y attributes, dispatch order, tap-again, encoding, idempotency, contract) | test/unit/ |

<output>
Create `.planning/phases/63-action-sheet-combat-lock-accessibility/63-02-SUMMARY.md` when done. Record the renderer's exact DOM shape (tags, classes, ids, attributes per action), the deps keys it calls, the test count and the `npm test` result. Plan 04 wires the shell against it.
</output>
