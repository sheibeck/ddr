---
phase: 62-gear-tab-layout-rebuild
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - mazeworld.html
  - src/browser/gearTab.js
  - test/unit/gear-tab-dom.test.js
  - test/unit/harness/shellSandbox.js
  - test/unit/shell-tab-snapshots.test.js
  - test/unit/fixtures/shell-snapshots/thief.gear.txt
  - test/unit/fixtures/shell-snapshots/mu.gear.txt
  - test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt
  - test/unit/gearTab.test.js
  - test/unit/gear-panels.test.js
  - test/unit/shell-clarity-43.test.js
  - test/unit/shell-armor-display.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-spells-40.test.js
  - test/unit/shell-loot-screen.test.js
autonomous: true
requirements: [GSCR-01, GSCR-02, GSCR-03, GSCR-04, GSCR-05, GSCR-06, GSCR-11]

must_haves:
  truths:
    - "#screen-gear renders, in source order: a slim stat header (#gear-stats: ARMOR RATING then WILMST), WORN (#gear-worn-head, #gear-worn), BAG (#gear-bag-head, #gear-bag-meter, #gear-bag), CONSUMABLES (#gear-cons-head, #gear-cons) and ALSO ON YOU (#gear-kit-head, #gear-kit). The old ON YOU / ALSO ON YOU / BAG panels and their CSS are gone"
    - "The WORN head reads 'WORN' with the '<n> / 5' count. #gear-worn holds exactly five li.mw-gear-row in data-slot order weapon, armor, cloak, jewelry1, jewelry2. Each row carries the slot label, name, note, value, an optional USE cell and a '›' chevron; an empty row carries the mw-gear-row-empty class and its italic in-voice line"
    - "A worn or bag-only activatable item renders a .mw-gear-use cell whose data-phase equals itemRowState(state, it).kind, with the USE/ACTIVE/COOLING label and squares sub from gearUseCell. Tapping it calls deps.useItem with { slot } for a worn row and the true c.items index for a bag card, the same path that costs the turn in combat"
    - "The BAG head count equals bagUsage(c).text. There is one pip per slot. At capacity the count and the pips carry mw-gear-full and a 'BAG FULL · DROP OR USE SOMETHING' line renders. The free-ride line renders only for a capped bag"
    - "Each bag card shows its name, the slot tag (with a mw-gear-tag-swap class for '· SWAP') and the desc. Bag-only activatables carry the USE cell. An empty bag renders the in-voice 'NOTHING LEFT TO CARRY' state"
    - "CONSUMABLES shows '<n> HELD' and one card per consumables row, each with a '×N' quantity and a USE/READ button. A ×0 healing potion's button is disabled. A READ the hero cannot perform is disabled and shows the engine's reason line. Taps call deps.drinkPotion, deps.useItem(i) and deps.readScroll"
    - "Interim actions stay reachable. A filled worn row has an Unequip button, disabled and reading 'Bag full' when the bag is full and the piece needs a slot. Each bag card carries renderCarriedList's own Equip / swap-confirm and two-tap Drop confirm, harvested per card through one gearRow:true call, so no confirm logic is forked"
    - "renderCarriedList's source and output are unchanged. The store and loot surfaces render byte-identically (the thief-store/mu-store snapshots and the shell-loot-screen tests stay green), and only the three gear snapshot fixtures are regenerated, deliberately"
    - "Every new Gear-tab CSS rule sizes its text with calc(<rem> * var(--mw-text-scale)) and carries no transition or animation (reduced-motion safe). No player-facing text on the tab says WP"
    - "tabDeps() gains drinkPotion and readScroll closures over the existing window.mzDrinkPotion / window.mzReadScroll bridges. No new engine action and no new __mz bridge is added"
    - "Edge GSCR-02/encoding: a worn or bagged item named with an apostrophe, double quotes, '<b>' markup and an emoji renders verbatim through textContent. No node under the ten gear ids carries an HTML-string write, and renderGearTab's region has zero HTML-string assignments (comment-stripped source pin)"
    - "Edge GSCR-03/ordering: within a worn row the children are ordered slot label, main (name, note), value, USE cell when present, chevron, then the actions row"
    - "Edge GSCR-05/encoding: bag card names and descs, including an armor bagArmorText desc and a usableBy suffix, render verbatim through textContent, and the swap-confirm label reads the worn piece's name as text"
  artifacts:
    - path: "mazeworld.html"
      provides: "#screen-gear section skeleton (10 ids), the Phase 62 Gear CSS block, tabDeps drinkPotion/readScroll"
      contains: "id=\"gear-worn\""
    - path: "src/browser/gearTab.js"
      provides: "renderGearTab rewritten on the Plan 01 models"
      contains: "gearWornModel(state)"
    - path: "test/unit/gear-tab-dom.test.js"
      provides: "DOM render, dispatch, encoding, idempotency and CSS pin suite for the new tab"
      min_lines: 250
    - path: "test/unit/fixtures/shell-snapshots/thief.gear.txt"
      provides: "regenerated gear snapshot (declared)"
      contains: "## #gear-stats"
  key_links:
    - from: "mazeworld.html#paint"
      to: "src/browser/gearTab.js#renderGearTab"
      via: "the unchanged window.__mzTabs.gear(document.getElementById(\"screen-gear\"), S, tabDeps()) mount"
      pattern: "window\\.__mzTabs\\.gear\\("
    - from: "src/browser/gearTab.js#renderGearTab"
      to: "src/browser/gearTab.js#renderCarriedList"
      via: "one per-card gearRow:true call whose .mw-gear-actions row is moved into the card"
      pattern: "gearRow: true"
    - from: "mazeworld.html#tabDeps"
      to: "window.mzDrinkPotion / window.mzReadScroll"
      via: "drinkPotion/readScroll closures"
      pattern: "drinkPotion: \\(\\) => window\\.mzDrinkPotion\\?\\.\\(\\)"
    - from: "test/unit/harness/shellSandbox.js#SNAPSHOT_IDS.gear"
      to: "mazeworld.html #screen-gear ids"
      via: "the ten gear ids the snapshot serializes"
      pattern: "gear-stats"
  prohibitions:
    - requirement_id: GSCR-03
      category: transparency
      flagged: true
      statement: "MUST NOT surface a tap's outcome as a toast or as inline refusal text under a row: every outcome reaches the player through the rail. The only standing reason text on the tab is the READ row's state line that GSCR-06 requires"
    - requirement_id: GSCR-05
      category: honesty
      flagged: true
      statement: "MUST NOT show a USE button on a bagged cloak or jewel: equippable gear only works from its slot, so the tab never implies a bagged ring does something"
    - requirement_id: GSCR-01
      category: values
      flagged: true
      statement: "MUST NOT show the token WP anywhere on the Gear tab: player text says HP"
---

<objective>
Rebuild the Gear tab's DOM to the mock:
- Replace the `#screen-gear` markup with a ten-id skeleton and add the mock's look as a Phase 62 CSS block on the existing tokens.
- Rewrite `renderGearTab(host, state, deps)` so it only turns the Plan 01 models into DOM, using createElement and textContent.
- Keep today's actions reachable in-row until the Phase 63 sheet: Unequip on worn rows, plus Equip, the swap confirm and the two-tap Drop on bag cards, harvested from renderCarriedList itself.
- Wire the potion and scroll buttons through two new tabDeps closures.
- Regenerate the three gear snapshots, deliberately and declared.
- Migrate the source pins that were written against the old two-panel renderer.

Purpose: GSCR-01..06 on screen, with GSCR-11's rendering half, because renderCarriedList stays untouched, so the store, loot and combat surfaces cannot move.

Implements CONTEXT: "Styling", "Interim actions", "Section order", "Empty slots … in italics", "Shared lists (GSCR-11): the Gear tab gets its OWN renderer … renderCarriedList stays exactly as it is", and the standing rulings (rail only, no toasts, greenfield replacement with declared snapshot regeneration).

Not in this plan (Phase 63): the bottom action sheet, tappable rows, combat-greyed actions, the back button and TalkBack sheet reading. Also not here: the mock's "TAP A SLOT…" hint line, which describes the sheet, and the mock's toast strip. The rows stay non-tappable; the chevron is decorative (aria-hidden) until the sheet lands.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/62-gear-tab-layout-rebuild/62-CONTEXT.md
@.planning/phases/62-gear-tab-layout-rebuild/62-01-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
From Plan 01, in src/browser/gearTab.js. Read 62-01-SUMMARY.md for any recorded deviation.
- `GEAR_COPY`: worn, bag, alsoOnYou, freeRide, empty{weapon,armor,cloak,jewelry1,jewelry2}, armorRating, wilmst, consumables, count, held, slot{…}, family{…}, swap, emptyName, noValue, chevron "›", bagFull, bagEmptyHead, bagEmptyBody, use{use,active,cooling,read}, healingPotion, healingDesc, scrolls, scrollDesc, qty, kit{…}, act{unequip,bagFull}, plus the legacy onYou/wielded (deleted by this plan).
- `GEAR_WORN_ORDER` = ["weapon","armor","cloak","jewelry1","jewelry2"].
- `gearHeaderModel(state)` returns `{ stats: [{ key, label, value, text } ×2] }`.
- `gearUseCell(state, it)` returns null or `{ phase, label, sub, remaining }`.
- `gearWornModel(state)` returns `{ filled, max, countText, rows: [{ key, label, filled, name, note, value, use, useRef, unequip: null | { slot, blocked } }] }`.
- `gearBagMeterModel(state)` returns `{ have, slots, full, countText, pips: boolean[], fullLine, freeRide }`.
- `gearBagCardsModel(state)` returns `[{ i, name, desc, family, tag, swap, use, useRef }]`.
- `gearConsumablesModel(state)` returns `{ held, heldText, rows: [{ key, name, qty, qtyText, desc, verb, enabled, reason, dispatch: { type: "drinkPotion" } | { type: "useItem", i } | { type: "readScroll" } }] }`.
- `gearKitRows(state)` returns `[{ label, value }]`.

renderCarriedList(container, state, items, opts, deps) must stay byte-identical. With `{ actions: ["equip", "drop"], gearRow: true, filter }` it builds one li per matching item, holding a `div.mw-gear-actions` with:
- Equip (weapon/armor directly; jewelry/cloak via Equip or the "Swap for …?" confirm when every key of the family is taken);
- the two-tap "Drop it? [Yes] [No]" confirm.

Both confirms dispatch deps.equipItem(i, slot) and deps.dropItem(i) with the TRUE c.items index, and share the module-level revert slots. recordingDom supports `querySelector(".mw-gear-actions")` and re-parenting via appendChild (it detaches from the old parent).

Test pin constraints you must keep:
- `export function renderGearTab(` stays the declaration immediately after renderCarriedList. Several suites slice `export function renderCarriedList(` → `export function renderGearTab(` and count `className = "mw-drop-confirm"` / `"mw-gear-actions"` / `setTimeout(revertDropConfirm` exactly once in that slice, so every new DOM helper lives inside renderGearTab or after it.
- `gearRow: true` must appear exactly once in gearTab.js (shell-gear-toolbar/shell-worn-slots pins), which is the per-card harvest call.
- The opening tag `<section class="mw-screen" id="screen-gear" data-screen="gear" hidden>` must stay exact (heroTab.test.js/gearTab.test.js slice on it).
- The screen's closing `</section>` must still be followed by a blank line and `    <!-- ORACLE` (shell-clarity-43's slice end marker).
- `.mw-gear-actions`, `.mw-drop-confirm`, `.mw-drop-confirm button.small`, `.mw-swap-confirm`, `.mw-swap-confirm button.small` CSS rules stay byte-identical (shell-gear-toolbar/shell-worn-slots pins).

mazeworld.html today:
- #screen-gear markup :1845-1863, with the Phase 43 comment above it.
- Old gear CSS: .mw-onyou-head/.mw-worn-empty/.mw-kit-head :250-257 with their comment; .mw-wilmst/.mw-wilmst b :1106-1107.
- tabDeps() :2868-2883.
- The paint() mount :3000, which stays unchanged.
- The combat dispatch map :3693-3706 already routes drinkPotion/readScroll to window.mzDrinkPotion/mzReadScroll (:6710-6711), which runs engineCombatAction: in combat it is a combat action, and out of combat the rail card comes from dispatchWithNarration.

Text scale: `--mw-text-scale` is set on #app at runtime. The :root `--mw-font-*` tokens substitute it at :root, where it stays 1, so write each new font-size as `calc(<px/16>rem * var(--mw-text-scale))` on the rule itself, which is evaluated on the element and inherits #app's value.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 62 Gear CSS block + tabDeps drinkPotion/readScroll + CSS/deps source pins</name>
  <files>mazeworld.html, test/unit/gear-tab-dom.test.js</files>
  <read_first>
    - mazeworld.html :60-141 (the :root tokens), :224-257 (ul.kit, ul.skills and the old gear rules), :466-529 (the global button rules), :1100-1110 (.mw-wilmst), :1115-1133 (.mw-screen), :1171-1174 (.mw-empty*), :1845-1863 (#screen-gear), :2868-2883 (tabDeps)
    - design/Mazeworld Gear.dc.html :21-101 (markup/styles) and :384-472 (useCell/renderVals style objects): the visual spec
    - test/unit/shell-gear-toolbar.test.js :149-165 and test/unit/shell-worn-slots.test.js :243-255 (the CSS pins that must stay green)
    - test/unit/shell-input-guards.test.js :344-350 (no aria-disabled selector in the style block)
  </read_first>
  <action>
**CSS.** In `mazeworld.html`'s style block, at the old Phase 43 gear rules (~:250-257):
- Delete the `.mw-onyou-head`, `.mw-worn-empty` and `.mw-kit-head` rules and their Phase 43 comment.
- Delete `.mw-wilmst` and `.mw-wilmst b` (~:1106-1107).
- Keep `.mw-gear-actions`, `.mw-drop-confirm*` and `.mw-swap-confirm*` exactly as they are.

In their place, add a commented "Phase 62 (GSCR-01..06): the Gear tab — design/Mazeworld Gear.dc.html mapped onto the existing tokens" block. Use one rule per line, in the file's compact `selector{prop:val;…}` style. Every font-size is `calc(<rem> * var(--mw-text-scale))`, with rem equal to the mock px / 16. Add no transition or animation, and no aria-disabled selector. The rules:
- `.mw-gear-stats`: display flex, justify-content flex-end, align-items flex-start, gap 22px, margin -14px -14px 16px, padding 9px 14px 10px, border-bottom 3px solid var(--rule), background var(--paper-2).
- `.mw-gear-stat`: flex column, align-items flex-end, gap 3px.
- `.mw-gear-stat-num`: var(--mono), weight 700, 1.0625rem, var(--ditto), tabular-nums.
- `.mw-gear-stat-label`: var(--disp), 0.375rem, #9a8f76.
- `.mw-gear-sec + .mw-gear-sec`: margin-top 30px.
- `.mw-gear-head`: flex, space-between, align-items center, margin 0, padding 0 0 9px, border-bottom 3px solid var(--rule), font-weight 400, font-size 0.875rem.
- `.mw-gear-head-title`: var(--disp), 0.5625rem, #c9bda0.
- `.mw-gear-head-count`: var(--mono), weight 700, 0.875rem, var(--ditto).
- `#gear-bag-head .mw-gear-head-count`: 1.0625rem.
- `.mw-gear-head-count.mw-gear-full`: color var(--stamp).
- `.mw-gear-worn,.mw-gear-cards`: list-style none, margin 0, padding 0.
- `.mw-gear-row`: flex, flex-wrap wrap, align-items center, gap 11px, padding 11px 0, border-bottom 2px solid #241f16.
- `.mw-gear-slot`: flex none, width 62px, var(--disp), 0.40625rem, line-height 1.5, #8f856f.
- `.mw-gear-main`: flex 1, min-width 0, flex column, gap 4px.
- `.mw-gear-name`: var(--mono), weight 700, 0.96875rem, var(--ink).
- `.mw-gear-note`: var(--mono), weight 700, 0.78125rem, line-height 1.35, #8f856f.
- `.mw-gear-val`: flex none, text-align right, var(--mono), weight 700, 0.9375rem, #c9bda0.
- `.mw-gear-row-empty .mw-gear-slot`: #5c5342.
- `.mw-gear-row-empty .mw-gear-name`: #6f6754, italic.
- `.mw-gear-row-empty .mw-gear-note`: #5c5342, italic.
- `.mw-gear-row-empty .mw-gear-val`: #4a4232.
- `.mw-gear-chev`: flex none, var(--mono), weight 700, 0.9375rem, #5c5342.
- `.mw-gear-use`: flex none, flex column, align-items center.
- `.mw-gear-use-btn`: min-width 74px, min-height 48px, padding 9px 11px, border 0, border-radius 0, background #241f16, box-shadow inset 0 0 0 1px #2c2519, var(--disp), 0.4375rem, letter-spacing 0, #6f6754.
- `.mw-gear-use[data-phase="ready"] .mw-gear-use-btn`, and the same for charges and consumable: background #2a2416, box-shadow inset 0 0 0 1px var(--mw-btn-border), color var(--ditto).
- `.mw-gear-use[data-phase="effect"] .mw-gear-use-btn`: #8fb08a.
- `.mw-gear-use-sub`: margin-top 4px, nowrap, var(--mono), weight 700, 0.71875rem, #6f6754.
- `.mw-gear-use[data-phase="effect"] .mw-gear-use-sub`: #8fb08a.
- `.mw-gear-pips`: flex, gap 4px, margin-top 11px.
- `.mw-gear-pip`: flex 1, height 10px, background #2c2519, box-shadow inset 0 0 0 1px #0f0d09.
- `.mw-gear-pip-on`: var(--ditto).
- `.mw-gear-pips.mw-gear-full .mw-gear-pip-on`: var(--stamp).
- `.mw-gear-full-line`: margin 8px 0 0, var(--mono), weight 700, 0.78125rem, var(--stamp).
- `.mw-gear-free`: margin 6px 0 0, var(--mono), weight 700, 0.6875rem, #8f856f.
- `.mw-gear-card`: flex, flex-wrap wrap, align-items center, gap 10px, margin-top 12px, padding 12px, border 2px solid #2c2519, background var(--paper-2).
- `.mw-gear-card-main`: flex 1, min-width 0.
- `.mw-gear-card-top`: flex, flex-wrap wrap, align-items baseline, gap 8px.
- `.mw-gear-card .mw-gear-name`: var(--disp), weight 400, 0.53125rem, line-height 1.5, text-transform uppercase.
- `.mw-gear-tag`: var(--disp), 0.375rem, line-height 1.5, #8f856f.
- `.mw-gear-tag-swap`: #b9a4ef.
- `.mw-gear-desc`: margin-top 6px, var(--mono), weight 700, 0.90625rem, line-height 1.4, var(--ink-soft).
- `.mw-gear-qty`: var(--mono), weight 700, 0.9375rem, #c9bda0.
- `.mw-gear-qty-zero`: #8f856f.
- `.mw-gear-cons-tag`: margin 6px 0 0, var(--mono), weight 700, 0.78125rem, var(--amber). This is the refusal amber.
- `.mw-gear-cons-btn`: flex none, min-height 48px, padding 12px 13px, border 0, border-radius 0, background var(--mw-gold-btn), color var(--paper), box-shadow 0 4px 0 var(--mw-btn-border), var(--disp), 0.5rem, letter-spacing 0.
- `.mw-gear-cons-btn:disabled`: background #2c2519, color #8f856f, box-shadow none.
- `.mw-gear-kit li`: 0.8125rem.
- `#gear-bag .mw-empty-head`: 0.75rem.
- `#gear-bag .mw-empty-body`: 0.8125rem.

**tabDeps.** In `function tabDeps()`, add `drinkPotion: () => window.mzDrinkPotion?.(),` and `readScroll: () => window.mzReadScroll?.(),` right after the `useItem` key. Leave every existing key byte-identical. The bridges already exist; this adds no engine action and no bridge.

**Tests.** Create `test/unit/gear-tab-dom.test.js` with a first section of source pins on mazeworld.html. Mirror gearTab.test.js's HTML read (CRLF-normalized) and its stripComments/extractScriptRegions helpers. The pins:
- (a) Every style-block line whose selector starts with `.mw-gear-` or `#gear-`, and that declares a font-size, has that font-size containing `var(--mw-text-scale)`. At least 20 such font-size declarations are found (the rule list below has 24).
- (b) None of those lines contains "transition" or "animation".
- (c) `.mw-gear-row{`, `.mw-gear-card{`, `.mw-gear-head{`, `.mw-gear-use-btn{` and `.mw-gear-pip{` each appear exactly once.
- (d) The three Phase 43 rule selectors and `.mw-wilmst{` appear zero times as rule starts (regex anchored at line start).
- (e) The tabDeps region names drinkPotion and readScroll, mapped to window.mzDrinkPotion?.() and window.mzReadScroll?.().

The render section is added in Task 2.
  </action>
  <verify>
    <automated>node --test test/unit/gear-tab-dom.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-input-guards.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-tab-dom.test.js` exits 0 (the CSS/deps pins pass)
    - `grep -c "drinkPotion: () => window.mzDrinkPotion?.()," mazeworld.html` = 1 and `grep -c "readScroll: () => window.mzReadScroll?.()," mazeworld.html` = 1
    - `grep -c "^\.mw-gear-row{" mazeworld.html` = 1 and `grep -c "^\.mw-gear-card{" mazeworld.html` = 1
    - `grep -c "var(--mw-text-scale)" mazeworld.html` >= 27 (7 existing :root token lines plus at least 20 new font-size rules)
    - `node --test test/unit/shell-gear-toolbar.test.js test/unit/shell-input-guards.test.js test/unit/shell-combat-actions.test.js` exits 0
  </acceptance_criteria>
  <done>The mock's look is in the style block on the existing tokens, scaled by the S/M/L setting and reduced-motion safe. The old panel rules are gone, and the Gear tab can dispatch potions and scrolls through tabDeps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: #screen-gear skeleton + renderGearTab rewrite on the models, harness ids, declared gear snapshot regeneration (test-first)</name>
  <files>mazeworld.html, src/browser/gearTab.js, test/unit/gear-tab-dom.test.js, test/unit/harness/shellSandbox.js, test/unit/shell-tab-snapshots.test.js, test/unit/fixtures/shell-snapshots/thief.gear.txt, test/unit/fixtures/shell-snapshots/mu.gear.txt, test/unit/fixtures/shell-snapshots/thief.gear-confirms.txt</files>
  <read_first>
    - src/browser/gearTab.js (whole file after Plan 01)
    - .planning/phases/62-gear-tab-layout-rebuild/62-01-SUMMARY.md (the model shapes as built)
    - test/unit/harness/recordingDom.js :190-523 (the element model: children, _content, dataset, querySelector support, replaceChildren, serializer)
    - test/unit/harness/shellSandbox.js :340-567 (loadShellSandbox, SNAPSHOT_IDS, fixedStates: thief has a full small bag, two worn jewels and a third in the bag)
    - test/unit/shell-tab-snapshots.test.js (whole file, 197 lines: the regeneration rules and the gear-confirms test)
    - mazeworld.html :1845-1870 (#screen-gear and the ORACLE comment after it)
  </read_first>
  <behavior>
    Render tests call `renderGearTab(host, state, deps)` directly: `doc = createRecordingDocument()`, `host = doc.document.getElementById("screen-gear")`, and deps are spies recording (name, args). States are engine-built (`newRun` + `stowItem`/`equipItem`, as fixedStates does) or plain `{ c }` where a case needs exact fields.
    - Header: #gear-stats has two .mw-gear-stat children. The first holds .mw-gear-stat-num === String(armorDisplay(c).ar) and .mw-gear-stat-label "ARMOR RATING"; the second holds the gold text and "WILMST".
    - WORN: #gear-worn-head reads the title "WORN" and a count equal to gearWornModel(state).countText. #gear-worn has exactly 5 li whose data-slot values, in order, are weapon, armor, cloak, jewelry1, jewelry2.
    - An empty row has class mw-gear-row-empty, and its .mw-gear-note text equals GEAR_COPY.empty[key]. Every row ends with a .mw-gear-chev "›" carrying aria-hidden "true".
    - Row child order (Edge GSCR-03/ordering): .mw-gear-slot, .mw-gear-main, .mw-gear-val, then .mw-gear-use when present, then .mw-gear-chev, then .mw-gear-actions when present.
    - USE cell: a worn jewel with a cooldown record (left 12) renders .mw-gear-use with data-phase "cooldown", button text "COOLING", sub "12 SQ". Clicking the button records useItem({ slot: "jewelry1" }). A bagged staff's cell click records useItem(<its index>). A bagged jewel card has no .mw-gear-use.
    - Unequip: a worn jewel on a non-full bag has an Unequip button whose click records unequip("jewelry1"). On a full bag it is disabled and reads "Bag full". Destroyed armor on a full bag stays enabled.
    - BAG, full small bag: the head count text equals bagUsage(c).text and has class mw-gear-full. .mw-gear-pips has 4 children, all .mw-gear-pip-on, and the pips container has mw-gear-full. A .mw-gear-full-line reads "BAG FULL · DROP OR USE SOMETHING". A .mw-gear-free line is present.
    - BAG, one under cap: no full line and no mw-gear-full. Bag-less: no .mw-gear-pips and no .mw-gear-free.
    - Bag cards: one li.mw-gear-card per gearBagCardsModel card, with data-i equal to the card index. A jewel card with both keys worn has .mw-gear-tag text "JEWELRY · SWAP" and class mw-gear-tag-swap. A rope card has no .mw-gear-tag.
    - Bag card actions: each card's .mw-gear-actions holds renderCarriedList's buttons. Clicking a card's Drop shows "Drop it?", and Yes records dropItem(<true index>). Clicking the third jewel's Equip shows "Swap for which?".
    - Empty bag: #gear-bag holds one li.mw-empty with "NOTHING LEFT TO CARRY" and the body line.
    - CONSUMABLES: the head count equals heldText. A ×0 heal has a disabled .mw-gear-cons-btn and a .mw-gear-qty-zero. An enabled heal click records drinkPotion(). A buff group click records useItem(<first index>). A Magic User's READ click records readScroll(). A Pilfer with scrolls has READ disabled and a .mw-gear-cons-tag equal to LINE_FOR.scrollRefused({ reason: "pilfer" }).text.
    - ALSO ON YOU: #gear-kit-head reads "ALSO ON YOU", and #gear-kit li count equals gearKitRows(state).length. Each li's text node is the label and its span is the value.
    - Encoding (Edge GSCR-02/encoding, GSCR-05/encoding): a worn jewel and a bagged jewel, each named `Thief's "Lucky" <b>Ring</b> 🎲`, render the exact string as .mw-gear-name text. Walking every node under the ten gear ids finds zero nodes with `_content.kind === "html"`. The comment-stripped renderGearTab region (from `export function renderGearTab(` to EOF) contains no HTML-string assignment.
    - Idempotency: rendering twice gives byte-identical `doc.serializeElements(SNAPSHOT_IDS.gear)`.
    - No WP: the serialized gear text contains no standalone WP token (reuse hp-not-wp's PLAYER_WP regex shape).
  </behavior>
  <action>
**Tests first.** Add the render section to `test/unit/gear-tab-dom.test.js`, covering every behavior bullet, and run it; it must fail against today's renderer. Then implement.

**Markup (mazeworld.html).** Replace everything inside the `#screen-gear` section. Keep the opening tag and the `</section>` + blank line + `<!-- ORACLE` sequence exact. The new inner markup, in this order:
- `<div class="mw-gear-stats" id="gear-stats"></div>`
- `<section class="mw-gear-sec" id="gear-worn-sec">` holding `<h2 class="mw-gear-head" id="gear-worn-head"></h2>` and `<ul class="mw-gear-worn" id="gear-worn"></ul>`
- `<section class="mw-gear-sec" id="gear-bag-sec">` holding `<h2 class="mw-gear-head" id="gear-bag-head"></h2>`, `<div class="mw-gear-meter" id="gear-bag-meter"></div>` and `<ul class="mw-gear-cards" id="gear-bag"></ul>`
- `<section class="mw-gear-sec" id="gear-cons-sec">` holding `<h2 class="mw-gear-head" id="gear-cons-head"></h2>` and `<ul class="mw-gear-cards" id="gear-cons"></ul>`
- `<section class="mw-gear-sec" id="gear-kit-sec">` holding `<h2 class="mw-gear-head" id="gear-kit-head"></h2>` and `<ul class="kit mw-gear-kit" id="gear-kit"></ul>`

Rewrite the comment above the section: Phase 62 (GSCR-01..06), the mock layout, the ten render roots filled by gearTab.js#renderGearTab, and the note that the Phase 63 sheet will make the rows tappable. Do not use the two-letter hit-points token that the hp-not-wp scan forbids.

**renderGearTab (src/browser/gearTab.js).** Replace the whole body, keeping the signature and its position directly after renderCarriedList, and rewrite its JSDoc for Phase 62. Use `doc = host.ownerDocument` and `c = state.c`. Define small helpers as closures inside renderGearTab, or as functions placed after it:
- `el(tag, cls, text)`: createElement, set className when given, set textContent only when text is a string.
- `head(id, title, countText, full)`: `getElementById(id).replaceChildren(span.mw-gear-head-title(title), …)`, appending `b.mw-gear-head-count` (plus " mw-gear-full" when full) only when countText is non-empty.
- `useCellEl(cell, ref, name)`:
  - `div.mw-gear-use`, with `dataset.phase = cell.phase`.
  - `button.mw-gear-use-btn` with text cell.label, `setAttribute("aria-label", cell.label + " " + name)`, and onclick `() => deps.useItem?.(ref)`.
  - `span.mw-gear-use-sub` with cell.sub, only when sub is non-empty.

The render steps:
1. **Header:** `gearHeaderModel(state)` → `#gear-stats.replaceChildren(...)`, one `div.mw-gear-stat` per stat holding `b.mw-gear-stat-num(text)` and `span.mw-gear-stat-label(label)`.
2. **WORN:** `w = gearWornModel(state)`; `head("gear-worn-head", GEAR_COPY.worn, w.countText, false)`; `#gear-worn.replaceChildren(...rows)`. Each row is an `li.mw-gear-row`, with " mw-gear-row-empty" added when not filled and `dataset.slot = row.key`. Its children, in this order:
   - span.mw-gear-slot(label)
   - div.mw-gear-main holding span.mw-gear-name(name) and span.mw-gear-note(note)
   - span.mw-gear-val(value)
   - the useCellEl(row.use, row.useRef, row.name) when row.use is set
   - span.mw-gear-chev(GEAR_COPY.chevron) with aria-hidden "true"
   - when row.unequip is set, a div.mw-gear-actions holding one `button.small.mw-gear-act`. Its text is GEAR_COPY.act.bagFull when blocked, else GEAR_COPY.act.unequip. It is disabled when blocked; otherwise onclick is `() => deps.unequip?.(row.unequip.slot)`.
3. **BAG:** `m = gearBagMeterModel(state)`; `head("gear-bag-head", GEAR_COPY.bag, m.countText, m.full)`; `#gear-bag-meter.replaceChildren(...)` with:
   - when m.pips.length: div.mw-gear-pips (plus " mw-gear-full" when full), aria-hidden "true", with one div.mw-gear-pip per pip (plus " mw-gear-pip-on" when lit);
   - p.mw-gear-full-line(m.fullLine) when set;
   - p.mw-gear-free(m.freeRide) when set.
4. **Cards:** `cards = gearBagCardsModel(state)`.
   - When there are none, render `li.mw-empty` holding `b.mw-empty-head(GEAR_COPY.bagEmptyHead)` and `i.mw-empty-body(GEAR_COPY.bagEmptyBody)`.
   - Otherwise each card is an `li.mw-gear-card` with `dataset.i = String(card.i)`, holding:
     - div.mw-gear-card-main, containing div.mw-gear-card-top (span.mw-gear-name(name), plus span.mw-gear-tag (+ " mw-gear-tag-swap" when card.swap) when card.tag is set) and div.mw-gear-desc(desc);
     - the useCellEl(card.use, card.useRef, card.name) when set;
     - the chevron;
     - the interim actions: `tmp = doc.createElement("ul")`, then `renderCarriedList(tmp, state, c.items, { actions: ["equip", "drop"], gearRow: true, filter: (x) => x === c.items[card.i] }, deps)`, then move `tmp.querySelector(".mw-gear-actions")`, when found, into the card with appendChild. This reuses the ONE Equip/swap-confirm/Drop-confirm implementation; renderCarriedList itself is not edited.
5. **CONSUMABLES:** `k = gearConsumablesModel(state)`; `head("gear-cons-head", GEAR_COPY.consumables, k.heldText, false)`. Each row is an `li.mw-gear-card.mw-gear-cons` with `dataset.key = row.key`, holding:
   - div.mw-gear-card-main, containing div.mw-gear-card-top (span.mw-gear-name(name) and span.mw-gear-qty(qtyText), plus " mw-gear-qty-zero" when qty is 0), div.mw-gear-desc(desc), and p.mw-gear-cons-tag(reason) when reason is set;
   - `button.mw-gear-cons-btn(verb)`. It is disabled when !enabled; otherwise onclick dispatches by row.dispatch.type: drinkPotion → `deps.drinkPotion?.()`, useItem → `deps.useItem?.(row.dispatch.i)`, readScroll → `deps.readScroll?.()`.
6. **ALSO ON YOU:** `head("gear-kit-head", GEAR_COPY.alsoOnYou, "", false)`; `#gear-kit.replaceChildren(...)`, one li per gearKitRows row, holding `doc.createTextNode(label)` then `span(value)`.

Delete the old body wholesale: the m-gold write, the s-kit / s-onyou / s-carry / s-carry-n writes, wornRow, wornSlotRow, headRow, emptyRow, gearBtn and the emptyHtml string. Also delete the now-unused `onYou` and `wielded` leaves from GEAR_COPY, and remove any import left unused. Keep the pinned engine/derived.js import line byte-identical; every name in it is still used. Do not touch renderCarriedList, the confirm constants or their revert functions.

**Harness.**
- In `test/unit/harness/shellSandbox.js`, set `SNAPSHOT_IDS.gear` to ["gear-stats","gear-worn-head","gear-worn","gear-bag-head","gear-bag-meter","gear-bag","gear-cons-head","gear-cons","gear-kit-head","gear-kit"] and update its comment.
- In `test/unit/shell-tab-snapshots.test.js`, re-point the gear-confirms test from #s-carry to #gear-bag: walk #gear-bag's buttons, click the first "Drop", click every "Equip", assert "Drop it?" and "Swap for which?", then `check("thief.gear-confirms", doc.serializeElements(["gear-bag"]))`. Update the test titles and comments to say the Gear tab is now header / WORN / BAG / CONSUMABLES / ALSO ON YOU.

**Snapshots (declared, deliberate).**
1. Run `node --test test/unit/shell-tab-snapshots.test.js`. Confirm that the only failures are thief.gear, mu.gear and thief.gear-confirms.
2. Run `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`, then run it again without the variable; it must pass.
3. `git diff --stat -- test/unit/fixtures/shell-snapshots/` must list only those three files. If a hero or store fixture shows up and the diff is line-ending only (the CRLF worktree note), restore it with `git checkout -- <file>`. If it is a real content change, STOP and fix the render: renderCarriedList must not move.
4. In the SUMMARY, name the DOM change: the old ids replaced by the ten gear ids, the five WORN rows, the pips, the cards, the consumables and the kit list.
  </action>
  <verify>
    <automated>node --test test/unit/gear-tab-dom.test.js test/unit/gear-view-models.test.js test/unit/shell-tab-snapshots.test.js test/unit/storeScreen.test.js test/unit/store-rows.test.js</automated>
    <human-check>Deferred to the Phase 64 Pixel 7 batch, per the deferred-UAT protocol (no mid-run device pause). Check that the Gear tab matches the mock at S/M/L text sizes, that the pips turn red at a full bag, and that the potion USE and scroll READ outcomes land on the rail.</human-check>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/gear-tab-dom.test.js` exits 0 with at least 25 passing tests, including test names containing "Edge GSCR-02/encoding", "Edge GSCR-03/ordering" and "Edge GSCR-05/encoding"
    - `grep -c 'id="gear-worn"' mazeworld.html` = 1, and the same holds for each of gear-stats, gear-worn-head, gear-bag-head, gear-bag-meter, gear-bag, gear-cons-head, gear-cons, gear-kit-head, gear-kit
    - `grep -c "gearWornModel(state)" src/browser/gearTab.js` >= 1, `grep -c "gearBagCardsModel(state)" src/browser/gearTab.js` >= 1, `grep -c "gearConsumablesModel(state)" src/browser/gearTab.js` >= 1, and `grep -c "gearRow: true" src/browser/gearTab.js` = 1
    - `node --test test/unit/shell-tab-snapshots.test.js` exits 0 without MZ_SNAPSHOT_UPDATE, and `git diff --stat -- test/unit/fixtures/shell-snapshots/` lists only thief.gear.txt, mu.gear.txt and thief.gear-confirms.txt
    - `node --test test/unit/storeScreen.test.js test/unit/store-rows.test.js` exits 0. In `node --test test/unit/shell-loot-screen.test.js`, the only failures are the two tests that slice the old renderGearTab carry region (the bagUsage readout test and "quick 260918-vvt (e)"), which Task 3 re-points. Record the failing test names.
  </acceptance_criteria>
  <done>The Gear tab renders the mock's five sections from the Plan 01 models through createElement and textContent. Today's actions stay reachable in-row by reusing renderCarriedList. The gear snapshots are regenerated and declared, and the store and loot surfaces are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the old two-panel source pins, then the full suite, build and boot checks</name>
  <files>test/unit/gearTab.test.js, test/unit/gear-panels.test.js, test/unit/shell-clarity-43.test.js, test/unit/shell-armor-display.test.js, test/unit/shell-gear-39.test.js, test/unit/shell-worn-slots.test.js, test/unit/shell-spells-40.test.js, test/unit/shell-loot-screen.test.js</files>
  <read_first>
    - test/unit/gearTab.test.js (whole, 224 lines)
    - test/unit/shell-clarity-43.test.js :95-120 (the helpers) and :325-433 (the Gear tab tests)
    - test/unit/shell-armor-display.test.js :90-110
    - test/unit/shell-gear-39.test.js :140-156
    - test/unit/shell-worn-slots.test.js :60-175 (tests 1-3 pin renderCarriedList and must stay unchanged; tests 4-5 pin the old worn rows)
    - test/unit/shell-spells-40.test.js :95-114
    - test/unit/shell-loot-screen.test.js :255-275 and :348-352
    - test/unit/gear-panels.test.js :38-56
    - src/browser/gearTab.js (after Task 2)
  </read_first>
  <action>
Run `npm test` and list every failure. For each one, classify it:
- **(a)** It pins the OLD renderGearTab body or the old #screen-gear markup/CSS. Retire it and write its replacement pin on the new structure, keeping the test's intent. Each retirement plus replacement is named in the SUMMARY.
- **(b)** Anything else, especially any store, loot, combat, hero or renderCarriedList-region pin. That is a regression: fix the source, never the test.

The known (a) set, with the replacement for each:
- **gearTab.test.js.**
  - Exports test: GEAR_WORN_ORDER and the eight models are present (from Plan 01).
  - The paint() no-literal test also forbids the new gear ids in paint().
  - The tabDeps test becomes "names all 14 deps keys", adding drinkPotion and readScroll.
  - The getElementById-in-#screen-gear test stays and passes on the ten new ids.
- **gear-panels.test.js.** The GEAR_COPY deepEqual pin becomes the final shape without onYou/wielded.
- **shell-clarity-43.test.js.**
  - "Markup: … two panels …" becomes: #screen-gear holds zero `<section class="panel"` and the ten gear ids in source order (gear-stats < gear-worn-head < gear-worn < gear-bag-head < gear-bag-meter < gear-bag < gear-cons-head < gear-cons < gear-kit-head < gear-kit).
  - "GEAR_COPY.onYou/bag match the two h2 headings" becomes: GEAR_COPY.worn/bag/consumables/alsoOnYou equal WORN/BAG/CONSUMABLES/ALSO ON YOU, and the four head ids are h2 elements in the markup.
  - The "#s-onyou … wornRow/wornSlotRow/headRow/emptyRow" test becomes: renderGearTab reads gearWornModel(state), and emptySlotRows(c) is read inside gearWornModel's region only.
  - The "WORN_SLOTS loop and wornSlotRow's own body stay innerHTML-free" test becomes: renderGearTab's region carries no HTML-string write, and the emptySlotRows WORN_SLOTS loop remains.
  - The "Kit … ALSO ON YOU heads the list" test becomes: gearKitRows has no weapon/armor rows and no "Potions"/"Scrolls"/"Wilmst" row, and renderGearTab writes GEAR_COPY.alsoOnYou to #gear-kit-head.
  - The CSS test becomes: the three Phase 43 rules and .mw-wilmst are absent, and .mw-gear-row/.mw-gear-card/.mw-gear-head exist once each with no transition/animation/aria-disabled token.
  - The build-artefact test swaps onyou-panel/mw-kit-head for gear-worn/mw-gear-row.
  - Delete the now-unused paintCarryRegion/kitRegion helpers.
- **shell-armor-display.test.js.** The "gear worn row is wired through armorD, with a noSlotNeeded param" test becomes a behavioral test on gearWornModel: a destroyed Mail on a full bag gives an armor row with unequip.blocked false, an intact piece on a full bag gives blocked true, and an active Cloak of Armor gives the armor note === armorDisplay(c).under. Keep the bagArmorText/renderCarriedList pin at :104-106 unchanged.
- **shell-gear-39.test.js.** In "Gear tab: wornSlotRow and renderCarriedList's row builder both read itemRowState", replace the wornSlotRow half with a pin that the gearUseCell region calls `itemRowState(state, it)` and carries no `it.every ?`. Keep the renderCarriedList rowsRegion half unchanged.
- **shell-worn-slots.test.js.** Replace tests 4 and 5 ("Worn rows: …"):
  - gearWornModel iterates GEAR_WORN_ORDER.
  - The worn USE cell dispatches `deps.useItem?.(ref)`, where a worn row's ref is `{ slot }` (behavioral: render the thief fixture and click a worn row's USE, then assert the spy saw `{ slot: "jewelry1" }`).
  - Unequip dispatches `deps.unequip?.(`.
  - `gearRow: true` appears exactly once in GEAR_SRC, inside renderGearTab's region.

  Tests 1-3 and every later test stay byte-identical.
- **shell-spells-40.test.js.** The two "Hero-tab kit" source pins become behavioral tests on gearKitRows. Assert the exact pairs: ward "9 hp left · 3 rds", "Mirror Self" "3 rds", "Sense Presence" "till the fight ends", "Sense Danger" "armed", "Map the Floor" "12 sq". Also assert the order Sense Danger < Map the Floor < Kills.
- **shell-loot-screen.test.js.** Re-point paintCarryRegion to the `export function gearBagMeterModel(` region (up to the next `\nexport `) and keep the intent:
  - the bagUsage test: `bagUsage(c)`, `usage.text`, `usage.full`;
  - the vvt (e) test: `GEAR_COPY.freeRide` and `usage.slots !== null`.

Run `npm test` again until it is green with fail 0. Then:
- `npm run build:www` must exit 0.
- Run `npm run boot:check`. Record exit 0, or the documented environment block (STATE.md Blockers, 162-byte dump) if it reproduces.
- `git diff --stat -- engine/ content/ test/parity/` must print nothing.

If a fresh worktree shows CRLF false-failures in the snapshot/ledger tests (the known environment note), record them and confirm the same tests pass on the main checkout. Do not edit fixtures to paper over them.
  </action>
  <verify>
    <automated>npm test && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 (fail 0)
    - `npm run build:www` exits 0; `npm run boot:check` exits 0, or the SUMMARY records the documented environment block verbatim
    - `git diff --stat -- engine/ content/ test/parity/` prints nothing
    - `git diff -- src/browser/gearTab.js` shows no change inside the `export function renderCarriedList(` … `export function renderGearTab(` span (renderCarriedList is byte-identical)
    - `git diff --stat -- test/unit/fixtures/shell-snapshots/` lists only thief.gear.txt, mu.gear.txt and thief.gear-confirms.txt
    - the SUMMARY lists every retired pin with the replacement written for it
  </acceptance_criteria>
  <done>Every pin written against the two-panel layout has been replaced by an equivalent pin on the new structure. The store, loot, combat and hero pins are untouched and green, and the suite, build and boot checks pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file → Gear tab DOM | item names and txt from a deserialized save reach the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-62-03 | Tampering | renderGearTab | medium | mitigate | every item-derived string goes through textContent, and aria-label goes through setAttribute. The DOM test walks all ten roots for HTML-string writes, and a comment-stripped source pin forbids them in renderGearTab's region (T-38-11 convention) |
| T-62-04 | Elevation | the tabDeps drinkPotion/readScroll closures | low | accept | they forward to the existing engine bridges through dispatch/applyAction; the engine validates potions/scrolls/canRead and refuses with its own events, and no state is mutated off-band |
</threat_model>

<verification>
- `npm test` green; `npm run build:www` exits 0; `npm run boot:check` passes or records the documented environment block
- Only the three gear snapshot fixtures change, declared in the SUMMARY; renderCarriedList is byte-identical; store/loot snapshots are unchanged
- engine/, content/ and test/parity/ are untouched
</verification>

<success_criteria>
- GSCR-01..06 visible on the tab exactly as the models state them; GSCR-11 rendering half: the shared list and every other surface are unchanged
- The three DOM-level edge truths (GSCR-02/encoding, GSCR-03/ordering, GSCR-05/encoding) have named passing tests
</success_criteria>

## Artifacts this phase produces

This plan's share. Plans 01 and 03 list theirs.

| Artifact | Kind | Where |
|---|---|---|
| #screen-gear skeleton: gear-stats, gear-worn-head, gear-worn, gear-bag-head, gear-bag-meter, gear-bag, gear-cons-head, gear-cons, gear-kit-head, gear-kit | markup (replaces onyou-panel/bag-panel) | mazeworld.html |
| Phase 62 Gear CSS block (.mw-gear-*), text-scale aware, no motion | CSS | mazeworld.html |
| tabDeps drinkPotion / readScroll | shell deps closures (existing bridges) | mazeworld.html |
| renderGearTab rewritten on the Plan 01 models, with per-card harvested interim actions | tab renderer | src/browser/gearTab.js |
| test/unit/gear-tab-dom.test.js | new DOM + CSS pin suite | test/unit/ |
| SNAPSHOT_IDS.gear = the ten gear ids | harness | test/unit/harness/shellSandbox.js |
| regenerated thief.gear / mu.gear / thief.gear-confirms | declared snapshot change | test/unit/fixtures/shell-snapshots/ |
| migrated gear source pins (8 files) | test maintenance | test/unit/ |

<output>
Create `.planning/phases/62-gear-tab-layout-rebuild/62-02-SUMMARY.md` when done. Record:
- the declared snapshot DOM change;
- each retired pin with its replacement;
- the test count;
- the `npm test` / build:www / boot:check results;
- confirmation that renderCarriedList and the store/loot fixtures are byte-identical.
</output>
