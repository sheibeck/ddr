---
phase: 61-gear-rules-store-purchase-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/derived.js
  - src/browser/upgradeWhy.js
  - src/browser/viewModels.js
  - mazeworld.html
  - test/unit/upgrade-why.test.js
  - test/unit/lootCompare.test.js
  - test/unit/gear-axes.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/shell-clarity-43.test.js
  - test/unit/shell-loot-screen.test.js
autonomous: true
requirements: [STORE-03]

must_haves:
  truths:
    - "The one verdict is unchanged: lootCompare(c, it).upgrade === (weaponUpgradeDelta(c, it) > 0) for weapons and (armorUpgradeDelta(c, it) > 0) for armor; the explanation never decides anything"
    - "For every WEAPONS key × bonus 0..2 × four sample heroes (a level-3 Magic User with a Quarter Staff, a Knight with prof 2, a noCrit Guard, a bare-handed Fighter), Math.round((parts.got.strike − parts.have.strike) × 100) / 100 === weaponUpgradeDelta(c, it), so the explanation's per-swing numbers can never disagree with the verdict"
    - "A level-3 Human Wizard (newRun(7), level set to 3) holding a Quarter Staff at prof 0 sees the Spiked Staff explained as exactly 'd8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing', and lootCompare's line ends ' · not an upgrade'"
    - "The explanation names only the terms that differ: dice when the labels differ, to-hit when weaponNeedMod differs, crit range when it differs and the hero can crit, the per-swing numbers always, and 'loses your +N practiced bonus' when c.prof > 0"
    - "Armor keeps today's wording byte-identical: 'AR {new} vs your AR {worn} · upgrade|not an upgrade', with the durability in sub"
    - "The loot screen shows the explained line automatically (it already renders lootCompare.line), and the find card shows it for a weapon or armor find"
    - "Empty edge: a bare-handed hero reads 'd8 vs your bare hands' with no crit term; a hero wearing Nothing reads 'AR 8 vs your AR 0 · upgrade'; upgradeWhyText(null/undefined/a string/an unknown kind) returns ''"
    - "Idempotency edge: gearCompareParts, upgradeWhyText and lootCompare are pure: two calls on the same (c, it) are deep-equal, and neither argument is mutated"
    - "Concurrency edge: the explanation is recomputed from state on every render (never cached on state, no new serialized field), so a save/resume or a re-render always shows the line for the hero as they are now"
    - "engine/derived.js#expectedStrike output is byte-identical after noCritFor is extracted (the bot smoke is identical and the existing gear-axes pins pass unchanged)"
  artifacts:
    - path: "engine/derived.js"
      provides: "noCritFor(c) and gearCompareParts(c, it)"
      exports: ["noCritFor", "gearCompareParts"]
    - path: "src/browser/upgradeWhy.js"
      provides: "UPGRADE_WHY_COPY + upgradeWhyText(parts) + swingText(x); engine-free, import-free, so eventNarration/narrationLines can use it in Plan 03"
      exports: ["UPGRADE_WHY_COPY", "upgradeWhyText", "swingText"]
    - path: "src/browser/viewModels.js"
      provides: "lootCompare with the explained line + additive `why` field"
      contains: "upgradeWhyText"
    - path: "test/unit/upgrade-why.test.js"
      provides: "consistency, Spiked Staff, per-term, edge, purity and voice tests"
      min_lines: 120
  key_links:
    - from: "src/browser/viewModels.js#lootCompare"
      to: "engine/derived.js#gearCompareParts"
      via: "import; parts formatted by upgradeWhyText"
      pattern: "gearCompareParts\\(c, it\\)"
    - from: "engine/derived.js#gearCompareParts"
      to: "engine/derived.js#expectedStrike"
      via: "the same two call shapes engine/items.js#weaponUpgradeDelta uses"
      pattern: "expectedStrike\\(c, c\\.weapon, c\\.magicWpn \\|\\| 0, c\\.prof \\|\\| 0\\)"
    - from: "mazeworld.html find card (S.pendingFind branch)"
      to: "src/browser/viewModels.js#lootCompare"
      via: "window.__mzLootCompare(c, it).line"
      pattern: "__mzLootCompare\\(c, it\\)\\.line"
  prohibitions:
    - requirement_id: STORE-03
      category: transparency
      flagged: true
      statement: "MUST NOT show an explanation whose verdict word disagrees with what the engine will do: the 'upgrade / not an upgrade' word is always the sign of the engine's own weaponUpgradeDelta/armorUpgradeDelta, never a second rule"
    - requirement_id: STORE-03
      category: values
      flagged: true
      statement: "MUST NOT turn the advice into a gate: the explanation never disables BUY, TAKE or EQUIP; it only explains"
---

<objective>
STORE-03, the explanation core. Keep the ONE hit-math verdict (`weaponUpgradeDelta` via `expectedStrike`) and make its line explain itself. The line names the terms that differ: dice, to-hit, crit range, the per-swing numbers, and a lost kit proficiency. Show it on the loot screen and the find card now; Plan 03 puts it on the rail, and Plan 04 on the store rows.

Purpose: the user bought a Spiked Staff that the game called "not an upgrade" with no reason given. Per the CONTEXT ruling, the verdict is true under the rules. At level 3 a Magic User's expected strike is 4.05 with the Spiked Staff (d8, `need: -1`) against 5.0 with the Quarter Staff (d6). The line just has to say why.

Implements CONTEXT "The upgrade line (STORE-03, reworded)":
- Keep the ONE verdict.
- The line explains itself.
- It shows on store rows, the loot screen and the find card (store rows land in Plan 04).
- It is advice only.

Design choices (Claude's discretion; they keep narration single-sourced):
- `engine/derived.js#gearCompareParts(c, it)` returns plain-data parts from the same derived helpers the verdict uses. It does not restate the arithmetic, and it is pinned equal to `weaponUpgradeDelta` by test.
- A new engine-free module, `src/browser/upgradeWhy.js`, formats those parts. It has no imports, which lets `narrationLines.js` (purity guard T-25-23) and `eventNarration.js` (never imports engine/) format the same parts in Plan 03.

Output: the parts helper, the formatter module, the rewired lootCompare, the find card line, and tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/61-gear-rules-store-purchase-fix/61-CONTEXT.md
@.planning/todos/pending/2026-09-21-store-purchase-charged-then-rejected-as-not-an-upgrade-spike.md
@.claude/CLAUDE.md

<interfaces>
engine/derived.js (current):
- `strikeDie(c)` :766; `classNeed(c)` :884; `weaponNeedMod(x)` :904 (x = base string or a character; 0 for Fists/unknown); `weaponCrit(c)` :916 (WEAPONS[c.weapon].crit || 1; 1 for unknown)
- `expectedStrike(c, base, bonus = 0, prof = 0)` :1021, returning 0 when WEAPONS[base] is missing (so Fists → 0). Line 1028 is `const noCrit = c.sub === "Guard" || c.sub === "Soldier" || eff(c, "noCrit") > 0;`
- `WEAPONS` rows: `{ dice, lab: "d8", cost, cls, need, crit }` (content/weapons.js). Spiked Staff: lab "d8", need -1, crit 1. Quarter Staff: lab "d6", need 0, crit 1. Rapier: lab "d6", need 1, crit 2.
engine/items.js (unchanged by this plan, READ ONLY):
- `weaponUpgradeDelta(c, it)` :363: `Math.round((expectedStrike(c, it.base, it.bonus || 0, 0) - expectedStrike(c, c.weapon, c.magicWpn || 0, c.prof || 0)) * 100) / 100`
- `armorUpgradeDelta(c, it)` :374: `it.ar - c.ar`
src/browser/viewModels.js: `lootCompare(c, it)` :180 returns `{ kind, legal, reason, delta, upgrade, equipNow, line, sub, usable }`; `refusalText(reason, letters)` builds the "can't use (...)" fragment; `USABLE_COPY` frozen at :102.
mazeworld.html find card: in the rail decision-card chain, `} else if (S.pendingFind && !S.combat && !S.store) {` (~:4300). It builds `lines.push({ text: \`${it.n || "An item"}${findSub ? ...}${usable ? ...}\`, roll: null });` (~:4312), and `window.__mzLootCompare` is already bridged (~:5410; src/browser/bridge.js registry).
Measured: newRun(7).c is a Human Wizard (Quarter Staff, prof 0). With c.level = 3: expectedStrike(Spiked Staff) = 4.050000000000001 and expectedStrike(Quarter Staff) = 5, a delta of −0.95.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: gearCompareParts + noCritFor (derived.js) and the upgradeWhy.js formatter, test-first</name>
  <files>engine/derived.js, src/browser/upgradeWhy.js, test/unit/upgrade-why.test.js</files>
  <read_first>
    - engine/derived.js (lines 760-1041: strikeDie, classNeed, weaponNeedMod, weaponCrit, weaponDiceMean, expectedStrike)
    - engine/items.js (lines 348-376: weaponUpgradeDelta/armorUpgradeDelta — read only)
    - content/weapons.js (WEAPONS rows: lab/need/crit)
    - content/kit.js (kit proficiencies: Knight/Guard +2, Woodsman +3, Cleric +1, Magic User subs 0)
    - src/browser/narrationLines.js (header purity contract, lines 1-45; this module must honour the same rules)
    - test/unit/hp-not-wp.test.js (PLAYER_WP regex, lines 38-50) and content/safety-wordlist.js (BANNED/ALLOWLIST shape)
  </read_first>
  <behavior>
    - The consistency test: for every WEAPONS key × bonus 0..2 × the four sample heroes, round2(got.strike − have.strike) === weaponUpgradeDelta(c, it).
    - Spiked Staff: newRun(7).c with level 3, weapon "Quarter Staff", prof 0, magicWpn 0 gives upgradeWhyText(gearCompareParts(c, spiked)) === "d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing" (U+2212 minus, U+00B7 middle dots).
    - Same weapon (Quarter Staff vs Quarter Staff, prof 0) gives "5.0 vs 5.0 a swing": no dice, to-hit or crit term.
    - Rapier vs Quarter Staff for a hero who can crit includes "+1 to hit" and "crits on 1–2 vs your 1" (U+2013 en dash).
    - A Guard (noCrit) comparing Rapier vs Quarter Staff has no "crits on" term.
    - Bare-handed (c.weapon "Fists", magicWpn 0) buying a Spiked Staff: the text starts "d8 vs your bare hands", has "−1 to hit" (Fists need mod 0), and has no crit term.
    - A Knight-style hero with prof 2 and a Long Sword comparing a Long Sword: the text ends "loses your +2 practiced bonus".
    - An enchanted candidate (bonus 2) labels its dice "d8 +2".
    - Armor: upgradeWhyText(gearCompareParts(c, {kind:"armor", ar:15, ...})) with c.ar 6 === "AR 15 vs your AR 6".
    - upgradeWhyText(null), (undefined), ("parley") and ({ kind: "cloak" }) all return "". gearCompareParts(c, {kind:"cloak"}) returns null, and an unknown weapon base returns null.
    - Purity: two calls deep-equal; c and it are deep-equal to their pre-call clones.
    - The upgradeWhy.js source has no import statement and no Math.random/Date.now. UPGRADE_WHY_COPY is frozen. No leaf, and no rendered sample, carries a standalone wp/WP token or a BANNED word (word-boundary, ALLOWLIST-aware).
  </behavior>
  <action>
**Bot smoke baseline first.** Before editing engine/derived.js, run `node tools/tune-difficulty.mjs --seeds=20 --json > ${TMPDIR:-/tmp}/61-02-bot-before.json` (about 90 seconds, deterministic). Task 2 compares against it.

**engine/derived.js:**
1. Add an exported `noCritFor(c)` just above expectedStrike. It returns `c.sub === "Guard" || c.sub === "Soldier" || eff(c, "noCrit") > 0`. This is exactly expectedStrike's current rule. Replace expectedStrike's own `const noCrit = ...` right-hand side with `noCritFor(c)`; nothing else in expectedStrike changes. Do NOT touch engine/combat.js's separate in-fight noCrit, which also reads darkness; that is a different rule.
2. Add an exported `gearCompareParts(c, it)` right after expectedStrike. It is pure, draws no rng and is JSON-safe.
   - For `it.kind === "weapon"` with `WEAPONS[it.base]` present, return `{ kind: "weapon", got, have, lostProf }`:
     - `got`: `{ lab: WEAPONS[it.base].lab + (it.bonus ? " +" + it.bonus : ""), need: weaponNeedMod(it.base), crit: noCritFor(c) ? 0 : (WEAPONS[it.base].crit || 1), strike: expectedStrike(c, it.base, it.bonus || 0, 0) }`
     - `have`, when `WEAPONS[c.weapon]` exists: `{ lab: WEAPONS[c.weapon].lab + (c.magicWpn ? " +" + c.magicWpn : ""), need: weaponNeedMod(c.weapon), crit: noCritFor(c) ? 0 : weaponCrit(c), strike: expectedStrike(c, c.weapon, c.magicWpn || 0, c.prof || 0) }`
     - `have` when bare-handed: `{ lab: null, need: 0, crit: null, strike: expectedStrike(c, c.weapon, c.magicWpn || 0, c.prof || 0) }`
     - `lostProf`: `c.prof > 0 ? c.prof : 0`
     - The two `strike` calls must use exactly weaponUpgradeDelta's argument shapes. That is the "never restates the arithmetic" rule: same helper, same inputs.
   - For `it.kind === "armor"`, return `{ kind: "armor", got: { ar: it.ar }, have: { ar: c.ar } }`.
   - Anything else, including an unknown weapon base, returns `null`.
   - JSDoc: Phase 61 / STORE-03; the verdict stays weaponUpgradeDelta/armorUpgradeDelta; the consistency test pins them together.

**src/browser/upgradeWhy.js (new).** A header comment says: engine-free, zero imports, pure, so narrationLines.js and eventNarration.js can import it (Plan 03) without breaking their purity contracts; copy lives in the frozen UPGRADE_WHY_COPY walked by hp-not-wp. It exports:
- `UPGRADE_WHY_COPY = Object.freeze({ dice: "{got} vs your {have}", bareHands: "bare hands", toHit: "{signed} to hit", crit: "crits on {got} vs your {have}", swing: "{got} vs {have} a swing", lostProf: "loses your +{n} practiced bonus", armor: "AR {got} vs your AR {have}", upgrade: "upgrade", notUpgrade: "not an upgrade", sep: " · " })`
- `swingText(x)`: `(Math.round(x * 10) / 10).toFixed(1)`. Half-up at one decimal, so 4.050000000000001 → "4.1" and 5 → "5.0".
- `critRange(n)`: module-private; 1 → "1", 2 → "1–2" (U+2013).
- `upgradeWhyText(parts)`: returns "" unless parts is a non-null object whose kind is "weapon" or "armor".
  - Armor: the armor template with got.ar / have.ar.
  - Weapon: collect terms in this fixed order, then join with sep:
    - (a) dice, when got.lab !== have.lab (have.lab null → bareHands);
    - (b) to-hit, when d = got.need − have.need is non-zero, as "+d" or "−|d|" (U+2212);
    - (c) crit, when have.crit is not null and got.crit !== have.crit;
    - (d) swing, always;
    - (e) lostProf, when lostProf > 0.

**test/unit/upgrade-why.test.js (new).** Cover every <behavior> bullet. Build heroes from `newRun(seed).c` with explicit field overrides (level, weapon, prof, magicWpn, sub), and name the seed per hero. For the voice check, import BANNED/ALLOWLIST from content/safety-wordlist.js and scan with a word-boundary regex. Mirror test/unit/shell-map-store-polish.test.js's STORE_ROLL_COPY safety section, and hp-not-wp's PLAYER_WP regex.
  </action>
  <verify>
    <automated>node --test test/unit/upgrade-why.test.js test/unit/gear-axes.test.js test/unit/bag-cap-gate.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/upgrade-why.test.js` exits 0 with at least 12 passing tests, one of them asserting the exact string `d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing`
    - `grep -c "export function gearCompareParts" engine/derived.js` = 1 and `grep -c "export function noCritFor" engine/derived.js` = 1
    - `grep -c "noCritFor(c)" engine/derived.js` >= 2 (expectedStrike plus gearCompareParts read the one rule)
    - `grep -c "export const UPGRADE_WHY_COPY = Object.freeze" src/browser/upgradeWhy.js` = 1 and `grep -c "export function upgradeWhyText" src/browser/upgradeWhy.js` = 1
    - `node --test test/unit/gear-axes.test.js test/unit/bag-cap-gate.test.js` exits 0 (expectedStrike and weaponUpgradeDelta pins unchanged; gear-axes' lootCompare line test is updated in Task 2, so run the whole file again there)
    - `git diff --stat -- engine/items.js engine/combat.js` prints nothing
  </acceptance_criteria>
  <done>The parts helper and the formatter exist, are pure and pinned equal to the engine verdict. The Spiked Staff reads exactly as CONTEXT specifies, and the refactor leaves expectedStrike byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: lootCompare explains itself; the find card shows it; pins updated; bot smoke</name>
  <files>src/browser/viewModels.js, mazeworld.html, test/unit/lootCompare.test.js, test/unit/gear-axes.test.js, test/unit/hp-not-wp.test.js, test/unit/shell-clarity-43.test.js, test/unit/shell-loot-screen.test.js</files>
  <read_first>
    - src/browser/viewModels.js (whole file, 280 lines)
    - mazeworld.html (lines 4296-4320: the find decision card; lines 4108-4120: the loot screen's subFor that already joins cmp.line/cmp.sub)
    - test/unit/lootCompare.test.js (whole file)
    - test/unit/gear-axes.test.js (lines 360-395)
    - test/unit/hp-not-wp.test.js (imports and the (b) copy-object list at ~:131)
    - test/unit/shell-clarity-43.test.js (findRegion + the find-card pins at :195-215)
    - test/unit/shell-loot-screen.test.js (any assertion on the weapon line text)
  </read_first>
  <action>
**src/browser/viewModels.js, lootCompare.** Import `gearCompareParts` from "../../engine/derived.js" (extend the existing derived import) and `upgradeWhyText, UPGRADE_WHY_COPY` from "./upgradeWhy.js".
- Weapon, legal: `const why = upgradeWhyText(gearCompareParts(c, it));` and `line = \`${why}${UPGRADE_WHY_COPY.sep}${upgrade ? UPGRADE_WHY_COPY.upgrade : UPGRADE_WHY_COPY.notUpgrade}\``.
- Weapon, illegal: `line` stays exactly today's `can't use (...)`, and `why` is null.
- Armor, legal: build `why` the same way ("AR 15 vs your AR 6"), with `line = why + sep + verdict`. That is byte-identical to today's armor line, so the armor pins must pass untouched.
- Bag and the fallthrough kinds carry `why: null`.
- Add `why` to every return object. This is an additive field; every existing field keeps its value. The verdict fields (`delta`, `upgrade`, `equipNow`) still come ONLY from weaponUpgradeDelta/armorUpgradeDelta (CONTEXT "Keep the ONE verdict").
- Update the JSDoc return shape and add a Phase 61 / STORE-03 note: advice only, never a gate.

**mazeworld.html.** The ONLY edit this plan makes here: in the find card branch (`S.pendingFind && !S.combat && !S.store`), directly after the existing `lines.push({ text: \`${it.n || "An item"}...\`, roll: null });`, add one statement: `if (it.kind === "weapon" || it.kind === "armor") lines.push({ text: window.__mzLootCompare(c, it).line, roll: null });`. Change nothing else in mazeworld.html. The loot screen needs no edit, because its `subFor` already renders `cmp.line`.

**Test pins (greenfield: update the pins, keep no old wording alive):**
- `test/unit/lootCompare.test.js`: the Broadsword +2 upgrade test's line becomes the measured explained line, "{why} · upgrade", with why computed live and then pinned as a literal in the test. Also assert `cmp.why` equals `upgradeWhyText(gearCompareParts(c, it))`. The Dagger test's line becomes "{why} · not an upgrade", pinned the same way. Armor pins stay unchanged. The existing "lootCompare.upgrade === takeItem's outcome" sweep stays unchanged.
- `test/unit/gear-axes.test.js`: the line test becomes "the line ends ' · upgrade' exactly when delta > 0 and ' · not an upgrade' otherwise, and contains ' a swing'".
- `test/unit/hp-not-wp.test.js`: import `UPGRADE_WHY_COPY` from "../../src/browser/upgradeWhy.js" and add it to the (b) copy-object list.
- `test/unit/shell-clarity-43.test.js`: add one pin that `findRegion()` contains `window.__mzLootCompare(c, it).line` guarded by the weapon/armor kind check. The existing find-card pins must keep passing.
- `test/unit/shell-loot-screen.test.js`: only if it pins the old "+N a swing" or bare "not an upgrade" text, repoint it at the new line shape. Otherwise leave it untouched.

**Bot smoke.** `node tools/tune-difficulty.mjs --seeds=20 --json > ${TMPDIR:-/tmp}/61-02-bot-after.json`, then cmp against the Task 1 baseline. It must be identical, which proves the noCritFor extraction is byte-identical. Record it in SUMMARY.

**Full checks.** `npm test` green. `npm run boot:check` exits 0 (the shell boots with the edited find card).
  </action>
  <verify>
    <automated>node --test test/unit/lootCompare.test.js test/unit/gear-axes.test.js test/unit/hp-not-wp.test.js test/unit/shell-clarity-43.test.js test/unit/shell-loot-screen.test.js test/unit/upgrade-why.test.js && npm test && npm run boot:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gearCompareParts(c, it)" src/browser/viewModels.js` >= 2 (the weapon and armor branches)
    - `grep -c "from \"./upgradeWhy.js\"" src/browser/viewModels.js` = 1
    - `grep -c "__mzLootCompare(c, it).line" mazeworld.html` = 1
    - `git diff --numstat -- mazeworld.html` shows at most 2 added lines and 0 deleted lines
    - `node --test test/unit/lootCompare.test.js` exits 0, and its armor assertions `AR 15 vs your AR 6 · upgrade` and `AR 6 vs your AR 6 · not an upgrade` are unchanged
    - `cmp "${TMPDIR:-/tmp}/61-02-bot-before.json" "${TMPDIR:-/tmp}/61-02-bot-after.json"` exits 0
    - `npm test` exits 0 (fail 0) and `npm run boot:check` exits 0
    - `git diff --stat -- engine/items.js engine/economy.js src/browser/storeScreen.js test/parity/` prints nothing
  </acceptance_criteria>
  <done>The loot screen and the find card show the explained line, the verdict is unchanged, the pins are updated with no old wording kept alive, the bot is unchanged, and the suite and boot check pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content/save data → view model | item fields (base, bonus, ar) come from content or a tolerant-loaded save |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-03 | Denial of Service | gearCompareParts on a tampered item (unknown base) | low | mitigate | returns null for an unknown weapon base; upgradeWhyText returns "" for any non-parts input; neither throws |
| T-61-04 | Tampering | find-card innerHTML | low | accept | the rail card lines are rendered as text (`lines[].text`), and the line is built from content labels and numbers only |
</threat_model>

<verification>
- `npm test` green; `npm run boot:check` exits 0
- The bot smoke is byte-identical (expectedStrike refactor proven)
- The Spiked Staff explanation string is pinned by test
</verification>

<success_criteria>
- STORE-03 core: the explanation (dice, to-hit, crit, per-swing numbers, lost proficiency) is built from the same derived helpers as the ONE verdict and pinned equal to it. It shows on the loot screen and the find card, and it is advice only.
- The Spiked Staff case reads "d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade" for a level-3 Magic User.
</success_criteria>

## Artifacts this phase produces

This plan's share. Plans 01, 03 and 04 list theirs.

| Artifact | Kind | Where |
|---|---|---|
| `noCritFor(c)` | exported pure helper (expectedStrike reads it) | engine/derived.js |
| `gearCompareParts(c, it)` → `{kind:"weapon", got:{lab,need,crit,strike}, have:{…}, lostProf}` \| `{kind:"armor", got:{ar}, have:{ar}}` \| `null` | exported pure parts helper (Plan 03 puts it on the `purchaseBagged` event) | engine/derived.js |
| `src/browser/upgradeWhy.js` | NEW engine-free, import-free module | src/browser/ |
| `UPGRADE_WHY_COPY` | frozen copy object (dice/bareHands/toHit/crit/swing/lostProf/armor/upgrade/notUpgrade/sep) | src/browser/upgradeWhy.js |
| `upgradeWhyText(parts)`, `swingText(x)` | exported pure formatters | src/browser/upgradeWhy.js |
| `lootCompare(...).why` | additive view-model field; `line` now "{why} · upgrade\|not an upgrade" for weapons | src/browser/viewModels.js |
| find card compare line | one shell line via `window.__mzLootCompare` | mazeworld.html |
| `test/unit/upgrade-why.test.js` | new unit suite | test/unit/ |

<output>
Create `.planning/phases/61-gear-rules-store-purchase-fix/61-02-SUMMARY.md` when done. Record the measured explained lines pinned in lootCompare.test.js, the seeds used per sample hero, the bot cmp result and the `npm test` count.
</output>
