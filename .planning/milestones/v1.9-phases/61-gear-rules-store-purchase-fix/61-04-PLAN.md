---
phase: 61-gear-rules-store-purchase-fix
plan: 04
type: execute
wave: 3
depends_on: ["61-02", "61-03"]
files_modified:
  - src/browser/viewModels.js
  - src/browser/storeScreen.js
  - test/unit/store-rows.test.js
  - test/unit/hp-not-wp.test.js
  - test/voice/safety-scan.test.js
  - test/unit/shell-clarity-43.test.js
  - test/unit/fixtures/shell-snapshots/thief-store.store.txt
  - test/unit/fixtures/shell-snapshots/mu-store.store.txt
autonomous: true
requirements: [STORE-02, STORE-03]

must_haves:
  truths:
    - "A store row is disabled exactly when the engine would refuse the buy: storeRowState(c, line).disabled === (line.sold || storeBuyRefusal(c, line) !== null) for every line of real openStore stock across several heroes, and a buyItem on an enabled row always emits 'bought' while a buyItem on a disabled row never does"
    - "An illegal weapon/armor/premium row is disabled and shows its reason on the row (lootCompare's 'can't use (…)' line); a bag-bound buy against a full bag (a not-better gear line, lockpicks, a tool) is disabled and shows 'bag full — sell or drop something first'"
    - "Weapon, armor and premium rows show the explained upgrade line (lootCompare.line: dice / to-hit / crit / per-swing / lost proficiency · upgrade|not an upgrade) before buying"
    - "A Magic User (newRun(7), level 3) holding a Quarter Staff sees the Spiked Staff row ENABLED with 'd8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade' on it: the advice never disables BUY"
    - "An upgrade row stays enabled even when the bag is full (the old piece is traded in, no slot needed)"
    - "All new row copy lives in the frozen STORE_ROW_COPY walked by the hp-not-wp guard and the voice safety scan; UPGRADE_WHY_COPY joins the voice scan too; the rail stays the one feedback surface (the row's reason is row state, not a post-tap refusal message)"
    - "The two store DOM snapshots change only on the gear rows' sub text and disabled flags, regenerated deliberately with MZ_SNAPSHOT_UPDATE=1 and the exact DOM change named in SUMMARY"
  artifacts:
    - path: "src/browser/viewModels.js"
      provides: "storeRowState(c, line) + STORE_ROW_COPY"
      exports: ["storeRowState", "STORE_ROW_COPY"]
    - path: "src/browser/storeScreen.js"
      provides: "rows read storeRowState: disabled + reason + explained line"
      contains: "storeRowState(c, item)"
    - path: "test/unit/store-rows.test.js"
      provides: "row-state vs engine agreement, Spiked Staff row, reason text, DOM render"
      min_lines: 100
  key_links:
    - from: "src/browser/storeScreen.js"
      to: "src/browser/viewModels.js#storeRowState"
      via: "one call per stock row"
      pattern: "storeRowState\\(c, item\\)"
    - from: "src/browser/viewModels.js#storeRowState"
      to: "engine/economy.js#storeBuyRefusal"
      via: "import; the same predicate buyFrom settles with"
      pattern: "storeBuyRefusal\\(c, line\\)"
    - from: "src/browser/viewModels.js#storeRowState"
      to: "src/browser/viewModels.js#lootCompare"
      via: "the explained line and the can't-use reason"
      pattern: "lootCompare\\(c, "
  prohibitions:
    - requirement_id: STORE-02
      category: transparency
      flagged: true
      statement: "MUST NOT disable a store row without showing why on the row: a greyed BUY always carries its reason (can't use / bag full), except a gold shortfall, whose price already says it"
    - requirement_id: STORE-03
      category: values
      flagged: true
      statement: "MUST NOT let the upgrade advice disable BUY: a legal not-better item's row stays enabled and buys into the bag"
---

<objective>
Finish STORE-02 and STORE-03 on the store screen itself. Each stock row reads one new view model, `storeRowState(c, line)`, built on the engine's own `storeBuyRefusal` (Plan 03) and `lootCompare` (Plan 02). From it the row:
- disables exactly when the engine would refuse;
- shows the reason on the row ("can't use (…)" or "bag full — …");
- shows the explained upgrade line on weapon, armor and premium rows, as advice that never disables BUY.

Purpose:
- STORE-02: "a real refusal … happens before payment, with the reason on the row".
- STORE-03: the line "shows on store weapon and armor rows" and "is advice that never disables BUY".
- The Spiked Staff acceptance, store-row half: the row, before buying, says why the staff isn't an upgrade.

Implements CONTEXT:
- "Refusals before payment" (the store row, the `row.disabled` line, is disabled for an illegal item or a bag-full bag-bound buy, and shows the reason);
- "The upgrade line … Where: weapon/armor store rows (new)";
- "Advice only".

Output: the view model and its copy, the rewired store rows, a new test suite, updated source pins, and the two store DOM snapshots regenerated deliberately.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/61-gear-rules-store-purchase-fix/61-CONTEXT.md
@.planning/phases/61-gear-rules-store-purchase-fix/61-02-SUMMARY.md
@.planning/phases/61-gear-rules-store-purchase-fix/61-03-SUMMARY.md
@.claude/CLAUDE.md

<interfaces>
engine/economy.js (after Plan 03): `storeBuyRefusal(c, line)` → `null` | `{ reason: "insufficientGold", short }` | `{ reason: "bagFull", have, slots }` | `{ reason: "wrongClass"|"acrobat"|"tooHeavy"|"noArmor"|"woodsman" }`; `openStore(state, rng, events)`; stock line `{ n, sub, cost, effectId, effectParams: { item } | null, sold }`.
src/browser/viewModels.js (after Plan 02): `lootCompare(c, it)` → `{ kind, legal, reason, delta, upgrade, equipNow, line, sub, usable, why }`. For an illegal weapon/armor, `line` is "can't use (…)". For a legal one, `line` is "{why} · upgrade|not an upgrade". `usableBy(it, c)` and the frozen `USABLE_COPY` are also here.
src/browser/storeScreen.js (current :63-75): per stock row it sets `row.disabled = !!item.sold || c.gold < item.cost;`, derives `sub` (repair-row special case, else `item.sub`), `usable = item.effectParams && item.effectParams.item ? usableBy(item.effectParams.item, c) : ""`, and sets innerHTML `<span class="g-n">${item.n}${sub || usable ? \`<i>${sub || ""}${sub && usable ? " " : ""}${usable}</i>\` : ""}</span><span class="g-c">…</span>`. STORE_ROLL_COPY must keep exactly 2 occurrences in this file (test/unit/shell-map-store-polish.test.js pin).
test/unit/shell-tab-snapshots.test.js: the store snapshots `thief-store.store` and `mu-store.store` under test/unit/fixtures/shell-snapshots/ are regenerated ONLY with `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`, and the SUMMARY must name the exact DOM change and why.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: storeRowState view model + STORE_ROW_COPY, test-first</name>
  <files>src/browser/viewModels.js, test/unit/store-rows.test.js</files>
  <read_first>
    - src/browser/viewModels.js (whole file, including Plan 02's lootCompare changes)
    - engine/economy.js (storeBuyRefusal, deliverGear, buyFrom, openStore — after Plan 03)
    - test/unit/store-delivery.test.js (Plan 03's hero/line builders — reuse its shapes)
    - test/unit/shell-map-store-polish.test.js (the STORE_ROLL_COPY voice-safety section, :240-270, the pattern for a copy-object safety check)
  </read_first>
  <behavior>
    - Spiked Staff: newRun(7).c, level 3, Quarter Staff, prof 0, gold 500, with a buyWeapon Spiked Staff line (cost 100), gives `{ disabled: false, refusal: null, reasonText: null, compareLine: "d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade", showUsable: true }`.
    - A Magic User with a Broadsword line gives disabled true, refusal "wrongClass", reasonText === lootCompare(c, it).line (it starts "can't use ("), compareLine null and showUsable false.
    - A full bag with a not-better weapon line gives disabled true, refusal "bagFull" and reasonText === STORE_ROW_COPY.bagFull.
    - A full bag with a lockpicks line gives disabled true, refusal "bagFull" and reasonText === STORE_ROW_COPY.bagFull.
    - A full bag with an UPGRADE weapon line gives disabled false, and compareLine ends " · upgrade".
    - Gold short on a food line gives disabled true, refusal "insufficientGold" and reasonText null.
    - A sold line gives disabled true, refusal null, reasonText null and compareLine null.
    - Non-gear lines (food, potion, rations, repair, scroll, tool) always have compareLine null.
    - Agreement sweep: for heroes from at least 6 newRun seeds covering Fighter/Thief/Magic User, each with gold set to 0, then exactly the line cost, then 99999, and each with an empty bag and then a full one, and for every line of a real openStore stock (store opened with makeRng(state.rngState)): disabled === (line.sold || storeBuyRefusal(c, line) !== null). On a clone, applyAction buyItem on that idx emits "bought" iff !disabled.
    - Purity: storeRowState does not mutate c or line, and two calls deep-equal.
    - STORE_ROW_COPY is frozen, and every leaf passes the PLAYER_WP regex and the BANNED/ALLOWLIST word-boundary scan.
  </behavior>
  <action>
In `src/browser/viewModels.js`:
- Import `storeBuyRefusal` from "../../engine/economy.js".
- Add `export const STORE_ROW_COPY = Object.freeze({ bagFull: "bag full — sell or drop something first" })` next to USABLE_COPY. Its JSDoc says: frozen, and walked by hp-not-wp plus the voice scan.
- Add an exported pure `storeRowState(c, line)`, returning `{ disabled, refusal, reasonText, compareLine, showUsable }`:
  - `refusal = line.sold ? null : storeBuyRefusal(c, line)`
  - `disabled = !!line.sold || refusal !== null`
  - `it = line.effectParams && line.effectParams.item`, and `gear = !!it && (it.kind === "weapon" || it.kind === "armor")`
  - `cmp = gear ? lootCompare(c, it) : null`
  - `reasonText` is:
    - `cmp.line` when `refusal` is a legality reason (`gear && cmp && !cmp.legal`), the "can't use (…)" text from the one refusalText rule;
    - `STORE_ROW_COPY.bagFull` when `refusal?.reason === "bagFull"`;
    - otherwise `null`, including insufficientGold, since the price column already says it and today's row behaves the same.
  - `compareLine = !line.sold && gear && cmp.legal ? cmp.line : null`. This is advice; it never feeds `disabled`.
  - `showUsable = !(gear && cmp && !cmp.legal)`. The can't-use reason already names who can, so the "(usable by … — not you)" suffix would only repeat it.
  - JSDoc: Phase 61 (STORE-02/03). The row can never disagree with buyFrom because both read storeBuyRefusal. The verdict and explanation come only from lootCompare, and advice never disables.

Create `test/unit/store-rows.test.js` covering every <behavior> bullet. Build heroes and lines with the same shapes Plan 03's store-delivery.test.js uses. For the agreement sweep, open the store with `openStore(state, makeRng(state.rngState), [])` on a clone per hero. For the "full bag" variant, push plain slot items until `slotItems(c).length === bagCap(c)`.
  </action>
  <verify>
    <automated>node --test test/unit/store-rows.test.js test/unit/lootCompare.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/store-rows.test.js` exits 0 with at least 10 passing tests, including the exact compareLine `d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade` for the enabled Spiked Staff row
    - `grep -c "export function storeRowState" src/browser/viewModels.js` = 1 and `grep -c "export const STORE_ROW_COPY = Object.freeze" src/browser/viewModels.js` = 1
    - `grep -c "storeBuyRefusal(c, line)" src/browser/viewModels.js` = 1
    - the agreement-sweep test runs at least 100 (hero, line) pairs and asserts row disabled === engine refusal and "bought" iff enabled
  </acceptance_criteria>
  <done>A single view model states each row's buyability, reason and advice line from the engine's own predicate and verdict, and a sweep proves it never disagrees with buyFrom.</done>
</task>

<task type="auto">
  <name>Task 2: Store rows render the state; copy scans; pins and store snapshots updated; full checks</name>
  <files>src/browser/storeScreen.js, test/unit/hp-not-wp.test.js, test/voice/safety-scan.test.js, test/unit/shell-clarity-43.test.js, test/unit/store-rows.test.js, test/unit/fixtures/shell-snapshots/thief-store.store.txt, test/unit/fixtures/shell-snapshots/mu-store.store.txt</files>
  <read_first>
    - src/browser/storeScreen.js (whole file, 90 lines)
    - test/unit/shell-clarity-43.test.js (the store-rows source pin at :226-236)
    - test/unit/shell-map-store-polish.test.js (the STORE_ROLL_COPY count pin — must stay at exactly 2)
    - test/unit/shell-tab-snapshots.test.js (header rules for regenerating a snapshot) and test/unit/fixtures/shell-snapshots/thief-store.store.txt, mu-store.store.txt
    - test/unit/hp-not-wp.test.js (the (b) copy-object list at ~:131) and test/voice/safety-scan.test.js (collectAuthoredStrings at ~:262)
    - test/unit/harness/recordingDom.js (createRecordingDocument, for a direct renderStoreScreen DOM test)
  </read_first>
  <action>
**src/browser/storeScreen.js.**
- Import `storeRowState` from "./viewModels.js", extending the existing import.
- In the stock loop, compute `const rs = storeRowState(c, item);` once per row, and set `row.disabled = rs.disabled;` (it replaces the gold-only expression).
- Keep the repair-row `sub` special case byte-identical.
- Build `usable` only when `rs.showUsable` (otherwise ""), keeping the existing `item.effectParams && item.effectParams.item` guard.
- Compose the italic sub from, in order, `sub`, `rs.compareLine`, `rs.reasonText`: join the non-empty ones with " · ", then append the usable suffix with a single space as today. Keep the existing `<i>…</i>` wrapper and the `g-c` price/sold span.
- Update the file-header comment with a Phase 61 note: row state comes from `storeRowState`, the reason is row state and not a post-tap message, and the rail stays the one feedback surface.
- Do not touch the header, the purse line, the storeRoll line (STORE_ROLL_COPY stays at exactly 2 occurrences), the bag-full header line, the sell list or the Leave button.

**Copy scans.**
- `test/unit/hp-not-wp.test.js`: import `STORE_ROW_COPY` from "../../src/browser/viewModels.js" (extend the USABLE_COPY import) and add it to the (b) list.
- `test/voice/safety-scan.test.js`: in `collectAuthoredStrings`, add every string leaf of `UPGRADE_WHY_COPY` (from "../../src/browser/upgradeWhy.js") and `STORE_ROW_COPY`, with a `// Phase 61 (STORE-02/03)` comment.

**Source pin.** In `test/unit/shell-clarity-43.test.js`, repoint the store-rows test at the new expressions:
- the usable line is still guarded on `item.effectParams && item.effectParams.item`, and now also on `rs.showUsable`;
- the sub composition reads `rs.compareLine` and `rs.reasonText`;
- `row.disabled = rs.disabled`.
Keep the test's intent: usable is guarded and appended into the `<i>` sub.

**DOM test.** Add to `test/unit/store-rows.test.js`: render `renderStoreScreen(host, state, {})` into `createRecordingDocument()` (build the host the way the recording DOM supports it; follow shell-tab-snapshots/rail-dismiss if a paint sandbox is simpler) for the Spiked Staff state plus an illegal Broadsword line. Assert:
- the Spiked Staff button is not disabled, and its text contains "d8 vs your d6 · −1 to hit · 4.1 vs 5.0 a swing · not an upgrade";
- the Broadsword button is disabled, and its text contains "can't use (".

**Snapshots (a declared, deliberate DOM change).**
- Run `node --test test/unit/shell-tab-snapshots.test.js` and confirm that the only failures are `thief-store.store` and/or `mu-store.store`.
- Inspect the first diff: it must be gear-row sub text (the compare line or reason) and/or disabled flags only.
- Then run `MZ_SNAPSHOT_UPDATE=1 node --test test/unit/shell-tab-snapshots.test.js`, then re-run it without the variable and expect a pass.
- Name the exact DOM change (which rows, old → new sub text, any disabled flag flips) in SUMMARY. If any hero/gear snapshot or any non-gear row changes, STOP and fix the render instead.

**Full checks.**
- `npm test` green, fail 0.
- `npm run build:www` exits 0.
- `npm run boot:check` exits 0.
- `git diff --stat -- engine/ content/ test/parity/` prints nothing: this plan touches no engine bytes.
  </action>
  <verify>
    <automated>node --test test/unit/store-rows.test.js test/unit/shell-clarity-43.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-tab-snapshots.test.js test/unit/hp-not-wp.test.js test/voice/safety-scan.test.js && npm test && npm run build:www && npm run boot:check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "storeRowState(c, item)" src/browser/storeScreen.js` = 1 and `grep -c "row.disabled = rs.disabled" src/browser/storeScreen.js` = 1
    - `grep -c "STORE_ROLL_COPY" src/browser/storeScreen.js` = 2 (unchanged)
    - `grep -c "STORE_ROW_COPY" test/unit/hp-not-wp.test.js` >= 1, `grep -c "STORE_ROW_COPY" test/voice/safety-scan.test.js` >= 1, and `grep -c "UPGRADE_WHY_COPY" test/voice/safety-scan.test.js` >= 1
    - `git diff --stat -- test/unit/fixtures/shell-snapshots/` lists only `thief-store.store.txt` and/or `mu-store.store.txt`
    - `node --test test/unit/shell-tab-snapshots.test.js` exits 0 without MZ_SNAPSHOT_UPDATE set
    - `git diff --stat -- engine/ content/ test/parity/ mazeworld.html` prints nothing
    - `npm test` exits 0 (fail 0); `npm run build:www` exits 0; `npm run boot:check` exits 0
  </acceptance_criteria>
  <done>The store shelf disables exactly the rows the engine would refuse, shows each reason on the row, and shows the explained upgrade line on gear rows without ever disabling BUY for it. The copy is scanned, the pins and snapshots are updated deliberately, and every check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| state → store row innerHTML | row text is built from content names/labels, numbers and frozen copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-61-07 | Tampering | storeScreen innerHTML | low | accept | the added text comes only from WEAPONS labels, numbers, lootCompare's refusalText and the frozen STORE_ROW_COPY (no user-authored strings); the same innerHTML contract as today's `sub`/`usable` |
| T-61-08 | Spoofing | row state vs engine | medium | mitigate | the agreement sweep proves disabled === the engine refusal and "bought" iff enabled, across seeds, gold levels and bag states |
</threat_model>

<verification>
- `npm test` green; `npm run build:www` and `npm run boot:check` exit 0
- Only the two store snapshots change, deliberately, with the DOM change named in SUMMARY
- engine/, content/ and test/parity/ are untouched by this plan
</verification>

<success_criteria>
- STORE-02 (row half): a real refusal (class/race, bag full) shows on the row before payment, and the row is disabled exactly when the engine would refuse.
- STORE-03 (row half): store weapon/armor/premium rows show the explained line; the Spiked Staff row is enabled and says why it isn't an upgrade.
</success_criteria>

## Artifacts this phase produces

This plan's share. Plans 01–03 list theirs.

| Artifact | Kind | Where |
|---|---|---|
| `storeRowState(c, line)` → `{ disabled, refusal, reasonText, compareLine, showUsable }` | exported pure view model (reads engine `storeBuyRefusal` + `lootCompare`) | src/browser/viewModels.js |
| `STORE_ROW_COPY` (`bagFull`) | frozen copy object (hp-not-wp + voice scanned) | src/browser/viewModels.js |
| store rows: disabled + reason + explained line | shell render | src/browser/storeScreen.js |
| `test/unit/store-rows.test.js` | new unit + DOM suite (agreement sweep) | test/unit/ |
| regenerated `thief-store.store.txt` / `mu-store.store.txt` | declared DOM snapshot change | test/unit/fixtures/shell-snapshots/ |

<output>
Create `.planning/phases/61-gear-rules-store-purchase-fix/61-04-SUMMARY.md` when done. Record the snapshot DOM change (rows, old → new text, disabled flips), the agreement-sweep pair count, and the `npm test` / build / boot results.
</output>
