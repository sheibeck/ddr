---
phase: 29-end-of-combat-loot-bag-cap
plan: 03
type: execute
wave: 3
depends_on: ["29-01", "29-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-armor-display.test.js
autonomous: true
requirements: [LOOT-02, LOOT-03, LOOT-04, LOOT-05, LOOT-06]

must_haves:
  truths:
    - "LOOT-02: when combat ends with a non-empty pile, noteCombat does NOT build the 'Move on' report beats — it stashes the report on window.__mzLootReport and renderEncounter's loot branch (guarded by S.pendingLoot.length && !S.combat && !S.store) renders one card: the report lines, one row per pile entry in pile order (two identical drops = two rows) with per-row Take/Stow, Leave and (when lootCompare(c,it).equipNow) Equip now, plus Take all / Leave all in the .actions row (test/unit/shell-loot-screen.test.js source assertions)"
    - "LOOT-02 empty: an empty pile changes nothing — noteCombat still builds the normal combat-report beats and the loot branch is unreachable (guard requires length)"
    - "LOOT-03: each weapon/armor row's sub-line is lootCompare(c, it).line followed by lootCompare(c, it).sub ('+2 damage' / 'not an upgrade' / 'can't use (Fighter only)' / 'AR 15 vs your AR 6 · upgrade'), Equip now dispatches takeLoot {i, equip:true} and Stow dispatches takeLoot {i}; Equip now is absent when equipNow is false"
    - "LOOT-04: the gear panel readout, the find card, the loot screen and the store all read window.__mzBagUsage(c) — no raw array-length capacity count survives in mazeworld.html (four negative greps); a full bag shows the same 'Bag full (h/s)' line and the shared renderDropShelf drop-to-make-room shelf on the find card and the loot screen, and Drop buttons in the store's 'Your gear' list"
    - "LOOT-05: a kind:\"bag\" row reads its lootCompare line ('6 slots — you carry 4') and taking it raises the bagUpgraded toast/Oracle line already wired in Plan 01 (no shell-specific handling beyond the row)"
    - "LOOT-06: hasActiveEncounter() returns true while S.pendingLoot is non-empty, so the map stays parked and window.move refuses input; on resume from a save with a pile the loot screen re-appears (branch reachable with no combat, no beats, no report)"
    - "Tap safety: the loot screen reuses exactly the find card's protections — the overlay covers the D-pad, hasActiveEncounter() gates every movement path, buttons inherit the >=48dp base and the .actions gap; no new guard mechanism (Phase 32 owns the systematic redesign)"
    - "npm run build:www exits 0 and npm test is green (baseline 1485 + all Phase 29 tests); parity untouched"
  artifacts:
    - "mazeworld.html: window.__mzBagUsage, window.__mzLootCompare, window.__mzLootReport, window.mzTakeLoot/mzLeaveLoot/mzTakeAllLoot/mzLeaveAllLoot; renderDropShelf(shelf, items); renderCarriedList opts.subFor + lootEquip/lootTake/lootLeave actions; loot branch in renderEncounter; hasActiveEncounter + noteCombat updates; bagUsage-driven readouts in paint()/find card/store"
    - "test/unit/shell-loot-screen.test.js (source assertions incl. the four negative greps)"
    - "test/unit/shell-armor-display.test.js §6 updated: bagArmorText(bi) is asserted inside renderDropShelf (the shelf moved there; intent unchanged)"
  key_links:
    - "mazeworld.html#noteCombat -> window.__mzLootReport (report folded into the loot card, no dismiss-then-continue chain)"
    - "mazeworld.html#renderEncounter loot branch -> renderCarriedList(S.pendingLoot, {actions:[lootEquip, lootTake, lootLeave], subFor}) -> window.mzTakeLoot/mzLeaveLoot -> inventoryAction -> applyAction"
    - "mazeworld.html#hasActiveEncounter includes S.pendingLoot.length (movement parked while the decision is pending)"
    - "window.__mzBagUsage (viewModels.bagUsage) is the ONLY capacity readout in the shell"
  prohibitions:
    - requirement_id: LOOT-02
      category: safety
      statement: "The loot screen must never be dismissable by a movement input — no D-pad, keydown or swipe path may resolve or skip the pile; only its own buttons (routed through the engine actions) may change it"
      status: unresolved
      verification: judgment
    - requirement_id: LOOT-04
      category: transparency
      statement: "The shell must never compute bag capacity itself — every readout and every full-bag gate reads window.__mzBagUsage so the screen can never disagree with the engine's stowItem verdict"
      status: unresolved
      verification: judgment
---

<objective>
Wire the pending pile into the classic shell: a loot screen card that replaces the "encounter cleared" report when a fight ends with drops (per-item Take/Stow, Leave, Equip now with the compare readout; Take all / Leave all), the map parked while it is up, the same card on resume, and every capacity readout / bag-full gate routed through the one `bagUsage` view-model with the find card's drop-to-make-room shelf reused on the loot screen and in the store.

Purpose: LOOT-02/03 need a decision surface; LOOT-04 needs the shell to stop counting slots by hand (RESEARCH Pitfall 2's three shell sites) and to offer the same bag-full feedback everywhere; LOOT-06 needs the screen back on resume; RESEARCH Pitfall 4 needs the report and the loot decision on ONE card.

Output: loot screen + bridges + readouts in mazeworld.html, `renderDropShelf`, `renderCarriedList` loot actions, a shell source-assertion test, Phase 28 test §6 adjusted, `npm run build:www` exit 0, full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-CONTEXT.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-01-SUMMARY.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-02-SUMMARY.md
@.planning/phases/28-armor-integrity-durability/28-03-SUMMARY.md

Plans 01/02 landed: `bagUsage(c) -> {have, slots|null, full, text}` and `lootCompare(c, it) -> {kind, legal, reason, delta, upgrade, equipNow, line, sub}` in src/browser/viewModels.js; engine actions `takeLoot {i, equip?}`, `leaveLoot {i}`, `takeAllLoot`, `leaveAllLoot`; events lootDropped/lootTaken/lootLeft/lootForfeited/bagUpgraded and the richer bagFull all have toasts + Oracle lines (dispatchWithToasts raises them — the shell adds no toast code).

Shell conventions (mazeworld.html): the classic script renders cards from `S.pending*` in renderEncounter(); the trailing `<script type="module">` bridges pure helpers as read-only `window.__mz*` names (`__mzBags`, `__mzArmorDisplay`) and actions as `window.mz*` functions routed through `inventoryAction()` -> `dispatchWithToasts()` -> `applyAction`. The orchestrator's note calls the movement guard `hasPendingSurface`; in this codebase it is `hasActiveEncounter()` (~L4703) — that is the function to extend. The find card's D-pad protection today = the overlay covering the D-pad + `hasActiveEncounter()` gating `window.move` + >=48dp buttons in a `.actions` row; reuse exactly that (Phase 32 owns the systematic guard).

Decisions honoured from CONTEXT.md: loot screen shows when combat ends with pendingLoot.length > 0, replacing the "encounter cleared" card, and re-appears on resume; Take/Leave per item + Take all/Leave all; Equip now (hidden when illegal or not an upgrade) vs Stow for weapon/armor, Take for everything else; the screen closes when the pile is empty; slot readouts count via the shared helper ("no ad-hoc counts left anywhere in the shell"); bagFull feedback = toast + the find card's drop-to-make-room shelf reused on the loot screen and in the store; markup reuses `.enc-head/.enc-sub/.actions/.shelf`.

Comment-text discipline: Task 3 negative-greps the four OLD capacity expressions from paint()/the find card. Do not write those expressions into new comments — refer to them as "the raw array-length count".
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bridges, helpers, movement guard, and the report hand-off</name>
  <files>mazeworld.html, test/unit/shell-loot-screen.test.js</files>
  <read_first>
    - mazeworld.html lines 5568-5620 (module imports + the __mzBags/__mzArmorDisplay bridge block), 6380-6425 (inventoryAction + the mzTakeFind/mzLeaveFind/mzDropItem bridges), 4686-4705 (hasActiveEncounter), 5886-5925 (noteCombat), 3483-3540 (renderCarriedList — opts.actions vocabulary and the `sub` line), 4975-4990 (the find card's inline drop-shelf loop to extract)
    - src/browser/viewModels.js lootCompare/bagUsage (Plan 01)
    - test/unit/shell-armor-display.test.js (the source-assertion test pattern: readFileSync + line-comments-first stripComments + region slicing) and test/unit/shell-party-camp.test.js (region helpers)
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md "Pitfall 4" (the report must not show before the loot decision)
  </read_first>
  <action>
    1. Module bridges (trailing module script): extend the viewModels import to `{ characterSheetViewModel, grimoireViewModel, armorDisplay, bagArmorText, lootCompare, bagUsage }`. Next to `window.__mzArmorDisplay` add `window.__mzBagUsage = bagUsage;` and `window.__mzLootCompare = lootCompare;` with the same read-only-bridge comment style (Phase 29 LOOT-03/04: the ONE capacity readout and the ONE compare verdict; the engine alone enforces the cap). Next to `window.mzTakeFind` add: `window.mzTakeLoot = (i, equip) => inventoryAction(equip ? { type: "takeLoot", i, equip: true } : { type: "takeLoot", i });`, `window.mzLeaveLoot = (i) => inventoryAction({ type: "leaveLoot", i });`, `window.mzTakeAllLoot = () => inventoryAction({ type: "takeAllLoot" });`, `window.mzLeaveAllLoot = () => inventoryAction({ type: "leaveAllLoot" });` with a Phase 29 comment (pure engine actions; the loot card dismisses itself once S.pendingLoot empties because hasActiveEncounter turns false).
    2. `hasActiveEncounter()`: add `(S.pendingLoot && S.pendingLoot.length)` to the returned expression (between `S.pendingFind` and the beats clause) and extend the doc comment: Phase 29 (LOOT-02/06) — a non-empty pending pile is a decision surface exactly like pendingFind; it parks the map (window.move re-checks this) until every item is taken or left, and it is what makes the card re-appear on resume since the pile is serialized.
    3. `noteCombat()`: in the `wasCombat && !has` branch, after `const rep = ...`, replace the `if (rep && !after.beats) after.beats = ...` line with: `if (after.pendingLoot && after.pendingLoot.length) { window.__mzLootReport = rep; } else if (rep && !after.beats) { after.beats = { groups: [rep], i: 0, action: null }; }`. In the combat-START branch (`!wasCombat && has`) add `window.__mzLootReport = null;`. Comment: RESEARCH Pitfall 4 — with drops pending, the report is folded into the loot card (Task 2) instead of a "Move on" card that would precede it; presentation-only transient, never on state.
    4. `renderCarriedList(container, items, opts)`: (a) compute `const sub = typeof opts.subFor === "function" ? opts.subFor(it) : (it.kind === "armor" ? window.__mzArmorDisplay.bagArmorText(it) : (it.txt ?? ""));` — keep the existing armor/txt default byte-for-byte so the Phase 28 assertion on `bagArmorText(it)` still matches; (b) add three action names to the loop: `"lootEquip"` -> `if ((it.kind === "weapon" || it.kind === "armor") && window.__mzLootCompare && window.__mzLootCompare(S.c, it).equipNow) li.appendChild(mkBtn("Equip now", () => window.mzTakeLoot?.(i, true)));`, `"lootTake"` -> `li.appendChild(mkBtn(it.kind === "weapon" || it.kind === "armor" ? "Stow" : "Take", () => window.mzTakeLoot?.(i)));`, `"lootLeave"` -> `li.appendChild(mkBtn("Leave", () => window.mzLeaveLoot?.(i)));`. Update the renderer's header comment: four hosts now (GEAR tab, store sell list, combat use list, loot screen).
    5. Extract the find card's inline drop-shelf loop into a classic-script function `function renderDropShelf(shelf, items)` placed directly after renderCarriedList: for each `(bi, i)` build the `.goods` button row exactly as today (name, `biSub` via `bi.kind === "armor" ? window.__mzArmorDisplay.bagArmorText(bi) : bi.txt`, "Drop" caption, `onclick -> window.mzDropItem?.(i)`) and append it. Replace the find card's loop with `renderDropShelf(shelf, c.items || []);` (leave every other find-card line untouched in this task — Task 3 touches its readout).
    6. Create `test/unit/shell-loot-screen.test.js` (header: Phase 29 Plan 03 — source assertions on mazeworld.html, same readFileSync + stripComments approach as shell-armor-display.test.js). Task 1 assertions: the module import line includes `lootCompare, bagUsage`; `window.__mzBagUsage = bagUsage;` and `window.__mzLootCompare = lootCompare;` present; the four `window.mzTakeLoot = ` / `window.mzLeaveLoot = ` / `window.mzTakeAllLoot = ` / `window.mzLeaveAllLoot = ` bridges present and each contains `inventoryAction(`; the hasActiveEncounter return expression contains `S.pendingLoot && S.pendingLoot.length`; the noteCombat region (slice from `function noteCombat(` to `function hapticForEvents(`) contains `after.pendingLoot` and `window.__mzLootReport = rep` and `window.__mzLootReport = null`; the renderCarriedList region contains `opts.subFor`, `"lootEquip"`, `"lootTake"`, `"lootLeave"`, `"Equip now"`; `function renderDropShelf(` exists and its region (to the next `function ` or `/* ----` marker) contains `bagArmorText(bi)` and `window.mzDropItem`; the pendingFind region calls `renderDropShelf(`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-loot-screen.test.js test/unit/shell-party-camp.test.js test/unit/shell-toast-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-loot-screen.test.js test/unit/shell-party-camp.test.js test/unit/shell-toast-wiring.test.js` exits 0
    - `grep -c "window.__mzBagUsage = bagUsage;" mazeworld.html` == 1; `grep -c "window.__mzLootCompare = lootCompare;" mazeworld.html` == 1
    - `grep -c "window.mzTakeLoot = " mazeworld.html` == 1; `grep -c "window.mzLeaveLoot = " mazeworld.html` == 1; `grep -c "window.mzTakeAllLoot = " mazeworld.html` == 1; `grep -c "window.mzLeaveAllLoot = " mazeworld.html` == 1
    - `grep -c "S.pendingLoot && S.pendingLoot.length" mazeworld.html` >= 1 (hasActiveEncounter)
    - `grep -c "window.__mzLootReport = rep" mazeworld.html` == 1; `grep -c "window.__mzLootReport = null" mazeworld.html` == 1
    - `grep -c "function renderDropShelf(" mazeworld.html` == 1; `grep -c "renderDropShelf(" mazeworld.html` >= 2
    - `grep -c "\"lootEquip\"" mazeworld.html` >= 1; `grep -c "\"lootTake\"" mazeworld.html` >= 1; `grep -c "\"lootLeave\"" mazeworld.html` >= 1; `grep -c "opts.subFor" mazeworld.html` >= 1
  </acceptance_criteria>
  <done>Bridges exist, movement is parked on a pending pile, the report is handed to the loot card instead of a Move-on beat, the shared list renderer knows the loot actions, and the drop shelf is a shared function — all pinned by source assertions.</done>
</task>

<task type="auto">
  <name>Task 2: The loot screen card</name>
  <files>mazeworld.html, test/unit/shell-loot-screen.test.js</files>
  <read_first>
    - mazeworld.html lines 4795-4935 (renderEncounter: the beats branch ends with `return;` right before the `// Phase 10 (Party UI, PARTY-01 UI half)` joiner comment — the loot branch goes between them), 4940-4993 (the find card — the enc-head/enc-sub/actions/shelf markup and the rust "bag is full" line to mirror), 2733-2752 (mzCombatReport's {title, tone, lines} shape that __mzLootReport carries)
    - mazeworld.html CSS: `.actions` (L561-572), `.shelf`/`.goods` (L575-592), `.skills` list rows used by renderCarriedList hosts
    - test/unit/shell-party-camp.test.js lines 50-70 (pendingJoinerRegion slices from the joiner guard to the pendingFind guard — the loot branch must sit BEFORE the joiner comment/guard so that region stays intact)
    - src/browser/viewModels.js lootCompare/bagUsage return shapes
  </read_first>
  <action>
    1. In `renderEncounter()`, immediately after the beats branch's closing `return; }` and BEFORE the `// Phase 10 (Party UI, PARTY-01 UI half)` comment, add the loot branch guarded exactly by `if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {`. Inside:
       - `const c = S.c; const usage = window.__mzBagUsage(c); const rep = window.__mzLootReport;`
       - Head: `<p class="enc-head" style="color:var(--moss)">${rep ? rep.title : "The spoils"}</p>` then, when `rep && rep.lines && rep.lines.length`, `<div class="evt">${rep.lines.map((l) => `<p>${l}</p>`).join("")}</div>` (the folded combat report — one card, no Move-on first).
       - Sub: `<p class="enc-sub">What they left behind · bag ${usage.text || "unlimited"}</p>` then `<ul class="skills" id="loot-list"></ul>`.
       - Full-bag block: `const needsSlot = S.pendingLoot.some((it) => it.kind !== "potion" && it.kind !== "bag");` when `usage.full && needsSlot` append `<p class="enc-sub" style="color:var(--rust)">Bag full (${usage.have}/${usage.slots}). Drop something to make room, or leave it.</p><div class="shelf" id="loot-drop-shelf"></div>`.
       - Actions: `<div class="actions"><button class="primary" id="a-loot-take-all">Take all</button><button id="a-loot-leave-all">Leave all</button></div>`.
       - Then `renderCarriedList(document.getElementById("loot-list"), S.pendingLoot, { actions: ["lootEquip", "lootTake", "lootLeave"], subFor: (it) => { const cmp = window.__mzLootCompare(c, it); return cmp.sub ? `${cmp.line} · ${cmp.sub}` : cmp.line; } });`, then if the shelf exists `renderDropShelf(document.getElementById("loot-drop-shelf"), c.items || []);`, then wire `a-loot-take-all` -> `window.mzTakeAllLoot?.()` and `a-loot-leave-all` -> `window.mzLeaveAllLoot?.()`; `return;`.
       - Doc comment on the branch: Phase 29 (LOOT-02/03/04/06) — the end-of-combat loot decision (CONTEXT §Pending pile & loot screen); replaces the encounter-cleared card (noteCombat hands the report over on window.__mzLootReport); re-appears on resume because pendingLoot is serialized; per-row buttons route through window.mzTakeLoot/mzLeaveLoot -> engine; the card dismisses itself when the pile empties; tap safety = the find card's (overlay over the D-pad, hasActiveEncounter gate, >=48dp buttons, .actions gap) — Phase 32 owns the systematic guard.
    2. Extend `test/unit/shell-loot-screen.test.js`: a `lootRegion()` helper slicing from the loot guard string to `if (S.pendingJoiner && !S.combat && !S.store)`; assert the region contains `window.__mzBagUsage(c)`, `window.__mzLootReport`, `id="loot-list"`, `renderCarriedList(` with `"lootEquip", "lootTake", "lootLeave"`, `subFor`, `window.__mzLootCompare(c, it)`, `id="loot-drop-shelf"`, `renderDropShelf(`, `id="a-loot-take-all"`, `id="a-loot-leave-all"`, `window.mzTakeAllLoot`, `window.mzLeaveAllLoot`, `Bag full (`; assert the loot guard's index is LESS than the joiner guard's index and GREATER than the index of `if (S.won) {` (ordering: after death/won/beats, before joiner/find/store); assert `pendingJoinerRegion`-style bounds (joiner guard -> pendingFind guard) do not contain `pendingLoot` (the joiner region is untouched).
  </action>
  <verify>
    <automated>node --test test/unit/shell-loot-screen.test.js test/unit/shell-party-camp.test.js test/unit/shell-armor-display.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-loot-screen.test.js test/unit/shell-party-camp.test.js` exits 0
    - `grep -c "if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {" mazeworld.html` == 1
    - `grep -c "id=\"a-loot-take-all\"" mazeworld.html` == 1; `grep -c "id=\"a-loot-leave-all\"" mazeworld.html` == 1; `grep -c "id=\"loot-list\"" mazeworld.html` == 1; `grep -c "id=\"loot-drop-shelf\"" mazeworld.html` == 1
    - `grep -c "actions: \[\"lootEquip\", \"lootTake\", \"lootLeave\"\]" mazeworld.html` == 1
    - `grep -c "window.__mzLootCompare(c, it)" mazeworld.html` >= 1
  </acceptance_criteria>
  <done>A single loot card renders the folded report, one row per drop with compare text and Equip now/Stow-or-Take/Leave, the shared drop shelf when the bag is full, and Take all/Leave all — sitting before the joiner/find/store branches.</done>
</task>

<task type="auto">
  <name>Task 3: Route every readout through bagUsage, store drop option, Phase 28 test adjust, build + full-suite gate</name>
  <files>mazeworld.html, test/unit/shell-loot-screen.test.js, test/unit/shell-armor-display.test.js</files>
  <read_first>
    - mazeworld.html lines 3009-3042 (paint(): the `items`/`bagSlots` consts, the #s-carry-n readout line, the `bagFull` const used by wornRow's Unequip gating), 4950-4975 (find card: the `slots`/`full` consts and the rust full-bag line), 5000-5032 (store branch: shelf render + the "Your gear" sell list call)
    - test/unit/shell-armor-display.test.js lines 108-125 (§6 pendingFindRegion assertions — `bagArmorText(bi)` moved into renderDropShelf in Task 1; the intent "the drop shelf reads bagArmorText" is preserved by asserting on the renderDropShelf region instead)
    - tools/build-www.mjs header (what `npm run build:www` does — it must still exit 0 with the edited HTML)
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-RESEARCH.md "Pitfall 2" (the three shell count sites) and "Anti-Patterns" (no fourth local computation)
  </read_first>
  <action>
    1. `paint()` carried-treasure block: keep `const items = c.items || [];` (renderCarriedList still needs the array) but delete the `bagSlots` const; add `const usage = window.__mzBagUsage(c);`; write `#s-carry-n` from `usage.text`; set `const bagFull = usage.full;` (the wornRow Unequip gating keeps working unchanged). Update the comment: Phase 29 (LOOT-04) — the readout and the full-bag gate read the ONE view-model (potions exempt), never a local count.
    2. Find card: delete the `slots` const and replace the raw-length `full` const with `const usage = window.__mzBagUsage(c); const full = usage.full;`; change the rust line to `Your bag is full (${usage.have}/${usage.slots}). Drop something to make room, or leave it.` (the `findSub`/`bagArmorText(it)` line and the Take/Leave wiring stay byte-identical).
    3. Store branch: after `const ad = ...` add `const usage = window.__mzBagUsage(S.c);`; when `usage.full` insert, above the `sell-head` paragraph, `<p class="enc-sub" style="color:var(--rust)">Bag full (${usage.have}/${usage.slots}) — sell or drop something to make room.</p>` (build it into the same innerHTML template with a ternary so the store's existing element ids are untouched); change the sell-list call to `renderCarriedList(document.getElementById("sell-list"), S.c.items, { actions: usage.full ? ["sell", "drop"] : ["sell"] });`. Comment: Phase 29 (LOOT-04) — the store's drop-to-make-room option; a refused lockpick buy costs nothing (engine gate) and its bagFull toast names have/slots.
    4. `test/unit/shell-armor-display.test.js` §6: keep the `bagArmorText(it)` assertion on the pendingFind region; move the `bagArmorText(bi)` assertion to a new `renderDropShelfRegion()` (slice from `function renderDropShelf(` to the next `\nfunction ` occurrence) and additionally assert the pendingFind region contains `renderDropShelf(`. Update the file's header bullet 6 wording to say the shelf now lives in the shared renderDropShelf (Phase 29). Intent unchanged: every bag armor row still shows durability.
    5. Extend `test/unit/shell-loot-screen.test.js` with the LOOT-04 gates on the comment-stripped CODE: (a) the paint region (slice from `// carried treasure` marker or `const carry = document.getElementById("s-carry");` to `renderCarriedList(carry,`) contains `window.__mzBagUsage(c)` and `usage.text` and `usage.full`; (b) the pendingFind region contains `window.__mzBagUsage(c)` and `${usage.have}/${usage.slots}`; (c) the store region (from `if (S.store) {` to `const C = S.combat;`) contains `window.__mzBagUsage(S.c)` and `["sell", "drop"]`; (d) NEGATIVE: CODE contains none of the four old capacity expressions — assert with `assert.ok(!CODE.includes(<literal>))` for each of: the paint readout template that interpolated the raw array length against bagSlots, the paint comparison of the raw array length against bagSlots, the find-card comparison of the raw array length against slots, and the find-card template that interpolated the raw array length against slots (write the four literals in the TEST file only, copied from the pre-change source at the line ranges in read_first; never in mazeworld.html).
    6. Gates: `npm run build:www` must exit 0; then the FULL suite `npm test` must be green with `git status --porcelain test/parity` empty.
  </action>
  <verify>
    <automated>npm run build:www && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:www` exits 0 and `www/index.html` exists
    - `npm test` exits 0 with `# fail 0` and `# pass` >= 1535
    - `git status --porcelain test/parity` prints nothing
    - `node --test test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-party-camp.test.js test/unit/shell-toast-wiring.test.js` exits 0
    - `grep -c "items.length >= bagSlots" mazeworld.html` == 0
    - `grep -c "(c.items || \[\]).length >= slots" mazeworld.html` == 0
    - `grep -c "\${items.length} / \${bagSlots}" mazeworld.html` == 0
    - `grep -c "(c.items || \[\]).length}/\${slots}" mazeworld.html` == 0
    - `grep -c "window.__mzBagUsage(" mazeworld.html` >= 4 (paint, find card, loot screen, store)
    - `grep -c "\[\"sell\", \"drop\"\]" mazeworld.html` == 1
    - `grep -c "bagArmorText(bi)" mazeworld.html` == 1 (inside renderDropShelf only)
  </acceptance_criteria>
  <done>No raw capacity count survives in the shell; the gear panel, find card, loot screen and store all read bagUsage; the store offers Drop when full; the Phase 28 shelf assertion follows the shelf into its shared function; the web build and the full suite are green.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (Plan 03's share marked ✔)

| Artifact | Kind | Owner |
|---|---|---|
| Engine gate, content tiers, deltas, `bagUpgradeTier`, store pre-pay fix, `bagFull` have/slots, `bagUpgraded`, `lootCompare`/`bagUsage` | Plan 01 | 01 |
| `pendingLoot` model/serialization, `offerLoot` + four handlers/actions, `forfeitLoot`, killFoe redirect + guarded draw, `reconcilePendingLoot`, loot event entries | Plan 02 | 02 |
| `window.__mzBagUsage`, `window.__mzLootCompare` (read-only bridges) | shell bridge | ✔ 03 |
| `window.__mzLootReport` (presentation transient set by noteCombat, read by the loot card) | shell transient | ✔ 03 |
| `window.mzTakeLoot(i, equip)`, `window.mzLeaveLoot(i)`, `window.mzTakeAllLoot()`, `window.mzLeaveAllLoot()` | shell action bridges | ✔ 03 |
| `hasActiveEncounter()` includes a non-empty `S.pendingLoot` | shell guard | ✔ 03 |
| `noteCombat` hands the report to the loot card instead of building Move-on beats when drops are pending | shell change | ✔ 03 |
| `renderCarriedList` `opts.subFor` + `lootEquip`/`lootTake`/`lootLeave` actions | shell helper | ✔ 03 |
| `renderDropShelf(shelf, items)` (find card + loot screen) | shell helper | ✔ 03 |
| Loot screen branch in `renderEncounter` (`#loot-list`, `#loot-drop-shelf`, `#a-loot-take-all`, `#a-loot-leave-all`) | shell | ✔ 03 |
| `bagUsage`-driven readouts in `paint()`, the find card and the store; store `["sell", "drop"]` when full | shell change | ✔ 03 |
| `test/unit/shell-loot-screen.test.js`; `test/unit/shell-armor-display.test.js` §6 adjusted | tests | ✔ 03 |
</artifacts>

<verification>
- `npm run build:www` exit 0; `npm test` green; parity untouched.
- Four negative greps prove no raw capacity count remains in mazeworld.html; `window.__mzBagUsage(` appears at all four readout sites.
- The loot branch sits after the beats branch and before the joiner branch; the joiner and find regions used by existing shell tests are intact.
</verification>

<success_criteria>
- LOOT-02: one loot card with per-item Take/Stow + Leave and Take all/Leave all replaces the encounter-cleared report; the map is parked; the card returns on resume.
- LOOT-03: weapon/armor rows show the compare readout from lootCompare and offer Equip now vs Stow.
- LOOT-04: every readout and full-bag gate in the shell reads bagUsage; bag-full feedback + drop-to-make-room on the find card, loot screen and store.
- LOOT-05/06: bag rows read their tier line; forfeit/resume behaviour surfaces through the same card and the Plan 02 events.
- `npm run build:www` exit 0; full suite green.
</success_criteria>

<human_verification>
Deferred to the end of the autonomous run (the executor copies this block into SUMMARY.md under "## Human verification (deferred to end of run)"):
1. Pixel 7: win a fight in which a foe drops something — ONE card appears titled "Victory" with the round/HP/gold lines and the drop listed below with Take/Leave (weapon/armor: Equip now when it is an upgrade, Stow otherwise); no "Move on" card precedes it; the D-pad does nothing while it is up.
2. Tap Equip now on an upgrade weapon: the toast says "Equipped: … (weapon)", the GEAR tab shows it worn and the old weapon in the bag.
3. Fill the bag (4 gear in a small bag), win a fight with a gear drop: the card shows "Bag full (4/4). Drop something to make room, or leave it." with the drop shelf; Take shows the "Bag full (4/4) — drop something to make room." toast and the row stays; Drop a bag item then Take works.
4. With a pile showing, background the app, kill it, relaunch: the same loot card is back with the same rows in the same order.
5. Start a fight at floor 2+ carrying the small bag and win several fights: eventually a "Medium bag" row appears; Take it — toast "Bigger bag: 6 slots.", GEAR readout shows "x / 6".
6. Store with a full bag: the "Your gear" list shows Drop buttons and the rust "Bag full (4/4) — sell or drop something to make room." line.
</human_verification>

<output>
Create `.planning/phases/29-end-of-combat-loot-bag-cap/29-03-SUMMARY.md` when done (include the deferred human-verification block above).
</output>
