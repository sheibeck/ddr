---
phase: 28-armor-integrity-durability
plan: 03
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-armor-display.test.js
  - .planning/PROJECT.md
autonomous: true
requirements: [ARMOR-01, ARMOR-02, ARMOR-04]
tags: [armor, shell, hud, gear-panel, store, key-decision]

must_haves:
  truths:
    - "paint() writes #s-arm (the live HUD Armor line) from armorDisplay(c).line — after a soaked hit of 39 on 45/45 Plate the line reads Plate · AR 15 · 6/45 hp instead of Plate (AR 15); the reported 'toast says wear, panel shows no damage' discrepancy is closed at its actual site (RESEARCH.md root cause)"
    - "The #s-kit armor row and the gear panel worn row are built from the same armorDisplay object (label / sub / under) — no inline current/max template literal survives in mazeworld.html"
    - "Every bag armor row (GEAR tab, store sell list, combat use list via renderCarriedList, the full-bag drop shelf, and the pending-find card) shows bagArmorText(it) — remaining durability with the hp label — never the frozen it.txt for kind armor"
    - "With the Cloak of Armor carried the gear worn row headline is Cloak of Armor with sub AR 15 · magic plate, never wears · under the cloak: Leather 9/15 hp, and the Unequip control still targets the worn piece underneath (armor slot), disabled only when a slot is needed and the bag is full"
    - "A destroyed worn piece's gear row offers Unequip even on a full bag (no slot is needed — the piece is discarded, Plan 01's unequipSlot branch)"
    - "The store repair row's sub-label reads the worn piece via armorDisplay(c).wornSub plus the hp-to-mend count (hp label) while engine/economy.js's stock sub string is untouched (parity-safe)"
    - "The classic CLOAKS table's Cloak of Armor line in mazeworld.html carries the same rule text as content/treasure-tables.js (one string, two copies, both greppable)"
    - "PROJECT.md's Key Decisions table has a row recording: armor soak-vs-wear canon kept (rulebook p.44 = prototype = engine); Cloak of Armor = never-wearing magic Plate for any carrier; destroyed armor is gone"
    - "The full suite stays green (npm test: 0 failures) and the shell source-assertion test pins every wiring point above"
  artifacts:
    - path: "mazeworld.html"
      provides: "window.__mzArmorDisplay bridge; #s-arm / #s-kit / gear worn row / renderCarriedList / find card / drop shelf / store repair row wired to the formatter; classic CLOAKS txt"
    - path: "test/unit/shell-armor-display.test.js"
      provides: "source-assertion pins for the bridge and each wiring site"
    - path: ".planning/PROJECT.md"
      provides: "Key Decision row for the ARMOR-01 ruling"
  key_links:
    - from: "mazeworld.html module script"
      to: "src/browser/viewModels.js#armorDisplay / #bagArmorText"
      via: "import { ..., armorDisplay, bagArmorText } + window.__mzArmorDisplay = { armorDisplay, bagArmorText }"
    - from: "mazeworld.html paint() (#s-arm, #s-kit, gear worn row)"
      to: "window.__mzArmorDisplay.armorDisplay(c)"
      via: "one armorD const computed once per paint()"
    - from: "mazeworld.html renderCarriedList / find card / drop shelf"
      to: "window.__mzArmorDisplay.bagArmorText(it)"
      via: "it.kind === 'armor' branch replacing it.txt"
  prohibitions:
    - statement: "MUST NOT edit engine/economy.js's store stock sub string (the repair line's engine text is compared by the economy parity harness) — the hp relabel is done in the shell renderer only"
      status: resolved
      verification: judgment
    - statement: "MUST NOT rebuild any armor string inline in mazeworld.html (no second AR/current/max template) — every site reads the bridged formatter"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit test/parity/prototype-master.js.txt (its own copy of the old cloak text stays frozen) or any parity fixture"
      status: resolved
      verification: judgment
    - statement: "MUST NOT touch the classic dead-code combat cluster (foeTurn/startCombat around mazeworld.html L4357-4480) or the classic takeItem — only the live paint()/renderCarriedList/renderEncounter sites and the CLOAKS data line"
      status: resolved
      verification: judgment
    - statement: "MUST NOT add iOS, ads, analytics, or network code (Android-only offline Capacitor shell)"
      status: resolved
      verification: judgment
---

<objective>
Wire the shell to the shared armor formatter at every site armor is named, pin the wiring with a source-assertion test, and record the ARMOR-01 ruling as a Key Decision — the SHELL + DOCUMENTATION half of Phase 28 and the plan that actually closes the reported bug.

Implements CONTEXT.md "Toast <-> panel truth" (durability displayed EVERYWHERE armor is named through ONE shared formatter: sheet/HUD line, gear worn row, bag rows, store repair line; unit "hp" everywhere), "Cloak of Armor legibility" (effective-armor readout on the HUD + gear worn row with the piece underneath listed; repairs still key off the worn piece), and "Soak-vs-wear ruling (ARMOR-01) — KEEP CANON" (the Key Decision row is a deliverable of THIS phase — ROADMAP success criterion 5).

Purpose: RESEARCH.md's root cause is that `#s-arm` — the HUD line every player watches — was never wired to durability while two other displays were, and the bag rows print a frozen `txt`. Routing all seven sites through `window.__mzArmorDisplay` removes the class of bug, not just the instance.
Output: `mazeworld.html` edits (bridge + 7 sites + classic CLOAKS line), `test/unit/shell-armor-display.test.js`, PROJECT.md Key Decision row, full `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-armor-integrity-durability/28-CONTEXT.md
@.planning/phases/28-armor-integrity-durability/28-RESEARCH.md
@.planning/phases/28-armor-integrity-durability/28-02-SUMMARY.md
@src/browser/viewModels.js
@test/unit/shell-party-camp.test.js
</context>

## Artifacts this phase produces (whole phase — Plans 01..03)

| Kind | Symbol | Where | Plan |
|------|--------|-------|------|
| Bag-item fields | `left`, `patches` on `kind: "armor"` bag items | `engine/items.js#wornArmorItem` / `equipItem` | 01 |
| Event payload flags | `armorSoaked.underMin`, `armorSoaked.magic`, `itemUnequipped.destroyed` | `engine/combat.js`, `engine/items.js` | 01 |
| Harness helper | `stripBagArmorFields(c)` in all three `*Comparable()` | `test/parity/harness/comparables.js` | 01 |
| Formatter exports | `armorDisplay(c)` -> `{ label, ar, current, max, worn, destroyed, magic, sub, wornSub, under, line }`; `bagArmorText(it)` | `src/browser/viewModels.js` | 02 |
| Copy / content | four-outcome toast + Oracle lines; destroyed-unequip narration; Cloak of Armor `txt` | `src/browser/toasts.js`, `src/browser/eventNarration.js`, `content/treasure-tables.js` | 02 |
| Test files | `test/unit/armor-durability.test.js`, `test/unit/armorDisplay.test.js` | | 01 / 02 |
| Shell bridge | `window.__mzArmorDisplay = { armorDisplay, bagArmorText };` | `mazeworld.html` module script (beside `window.__mzConditionsOf`) | 03 |
| Shell wiring | `paint()`: `const armorD = window.__mzArmorDisplay.armorDisplay(c);` -> `#s-arm` = `armorD.line`; `#s-kit` armor row `[armorD.label, armorD.under ? armorD.sub + " · " + armorD.under : armorD.sub]`; gear `wornRow(armorD.label, <same sub>, "armor", armorD.worn, armorD.destroyed)` | `mazeworld.html` | 03 |
| Shell wiring | `renderCarriedList` armor rows, pending-find card, full-bag drop shelf -> `window.__mzArmorDisplay.bagArmorText(it)`; store shelf `repairArmor` row -> `armorDisplay(S.c).wornSub` + hp-to-mend | `mazeworld.html` | 03 |
| Content mirror | classic `CLOAKS` Cloak of Armor line txt = the Plan 02 string | `mazeworld.html` (~L1815) | 03 |
| Test file | `test/unit/shell-armor-display.test.js` | | 03 |
| Documentation | Key Decision row (text in Task 3) | `.planning/PROJECT.md` | 03 |

<tasks>

<task type="auto">
  <name>Task 1: Bridge the formatter and wire the HUD line, kit row and gear worn row (paint())</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html lines 5530-5582 (the module script's import block — `import { characterSheetViewModel, grimoireViewModel } from "./src/browser/viewModels.js";` at ~L5540 — and the read-only bridge pattern `window.__mzBags` / `window.__mzNightlyEats` / `window.__mzConditionsOf` with their doc comments)
    - mazeworld.html lines 2874-2975 (`paint()` start; the stat lines incl. `#s-arm` at ~L2954; the `#s-kit` rows array at ~L2964-2975 whose second entry is the armor row)
    - mazeworld.html lines 3018-3037 (`gearBtn`, `wornRow(label, sub, slot, canUnequip)` and the two `wornRow(...)` calls — armor at ~L3035-3036)
    - mazeworld.html line ~1815 (classic `CLOAKS` table's Cloak of Armor entry — DEAD code, updated only so the two copies of the string match)
    - content/treasure-tables.js line 30 (the exact Plan 02 cloak string to mirror)
    - src/browser/viewModels.js (`armorDisplay` / `bagArmorText` doc comments + the field contract)
    - mazeworld.html lines 4357-4480 — READ to know what NOT to touch (classic dead combat cluster; its own `${S.c.armorWP}/${S.c.armorMax}` line at ~L4474 is dead and stays)
  </read_first>
  <action>
    **Module import + bridge** (per RESEARCH.md Open Question 1 recommendation): extend the viewModels import to `import { characterSheetViewModel, grimoireViewModel, armorDisplay, bagArmorText } from "./src/browser/viewModels.js";`. Directly after the `window.__mzConditionsOf = conditionsOf;` line add `window.__mzArmorDisplay = { armorDisplay, bagArmorText };` with a doc comment in the same voice as the `__mzConditionsOf` block: Phase 28 (ARMOR-02/04) — bridge the PURE armor formatters so the classic `paint()`/`renderCarriedList`/`renderEncounter` render every armor string from ONE engine-derived source (`armorSoak(c)` under the hood); read-only, no rng, safe every paint. Note that this assignment runs before `await window.__mzClassicBoot()` further down, so `paint()` can rely on the bridge unconditionally.

    **paint() — one lookup, three sites**: just before the `#s-arm` assignment add `const armorD = window.__mzArmorDisplay.armorDisplay(c);` (a short comment: Phase 28 — durability + the cloak's effective plate come from the shared formatter; this HUD line never showed durability before, which was the reported "toast says wear, panel shows no damage" bug). Then:
    1. `#s-arm`: `document.getElementById("s-arm").textContent = armorD.line;` (replacing the name + optional `(AR n)` concatenation).
    2. `#s-kit` armor row: replace the array entry that today pairs `c.armor` with a ternary between the inline current/max hp template and "destroyed" with `[armorD.label, armorD.under ? `${armorD.sub} · ${armorD.under}` : armorD.sub]`.
    3. Gear worn row: change `wornRow`'s signature to `(label, sub, slot, canUnequip, noSlotNeeded)` and its button to `gearBtn(bagFull && !noSlotNeeded ? "Bag full" : "Unequip", () => window.mzUnequip?.(slot), bagFull && !noSlotNeeded)` (the weapon call passes nothing -> unchanged behavior). Replace the armor call + its `if (c.armor && c.armor !== "Nothing")` guard with: `if (armorD.worn || armorD.magic) wornRow(armorD.label, armorD.under ? `${armorD.sub} · ${armorD.under}` : armorD.sub, "armor", armorD.worn, armorD.destroyed);`. Comment: with the cloak the headline is the cloak (effective armor) and the Unequip still stows the piece underneath; a destroyed piece needs no slot (Plan 01's `unequipSlot` discards it), so a full bag does not block it.

    **Classic CLOAKS line (~L1815)**: set that entry's `txt` to the identical Plan 02 string `soaks as plate (AR 15) over whatever you wear — any class, never wears out, light as a rumor` so the dead copy never contradicts the live one. Do not touch `test/parity/prototype-master.js.txt`.

    Sanity: `node --check` cannot parse HTML — instead run `npm test` for the shell/toast wiring tests (`node --test test/unit/shell-toast-wiring.test.js test/unit/shell-party-camp.test.js test/unit/shell-oracle-panel.test.js`) to be sure nothing else in the file broke, and open `mazeworld.html` in a browser if one is available (optional; the Task 2 source-assertion test is the required proof).
  </action>
  <verify>
    <automated>grep -cF 'window.__mzArmorDisplay = { armorDisplay, bagArmorText };' mazeworld.html && grep -cF 'getElementById("s-arm").textContent = armorD.line;' mazeworld.html && node --test test/unit/shell-toast-wiring.test.js test/unit/shell-party-camp.test.js test/unit/shell-oracle-panel.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'import { characterSheetViewModel, grimoireViewModel, armorDisplay, bagArmorText } from "./src/browser/viewModels.js";' mazeworld.html` == 1
    - `grep -cF 'window.__mzArmorDisplay = { armorDisplay, bagArmorText };' mazeworld.html` == 1
    - `grep -cF 'const armorD = window.__mzArmorDisplay.armorDisplay(c);' mazeworld.html` == 1
    - `grep -cF 'getElementById("s-arm").textContent = armorD.line;' mazeworld.html` == 1
    - `grep -cF '${c.armorWP}/${c.armorMax} hp' mazeworld.html` == 0 (both inline templates gone; the dead classic line uses `S.c.` and is untouched)
    - `grep -cF 'wornRow(armorD.label' mazeworld.html` == 1 and `grep -cF 'const wornRow = (label, sub, slot, canUnequip, noSlotNeeded) =>' mazeworld.html` == 1
    - `grep -cF 'light as a rumor' mazeworld.html` == 1
    - `grep -cF 'a full suit of plate that weighs nothing' mazeworld.html` == 0
    - `git hash-object test/parity/prototype-master.js.txt` == a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `node --test test/unit/shell-toast-wiring.test.js test/unit/shell-party-camp.test.js test/unit/shell-oracle-panel.test.js` exits 0
  </acceptance_criteria>
  <done>The HUD Armor line, the kit row and the gear worn row all render from the one bridged formatter; the cloak headline + underneath piece show on the worn row; the classic cloak string mirrors the live one.</done>
</task>

<task type="auto">
  <name>Task 2: Bag rows, find card, drop shelf, store repair row — plus the shell source-assertion test</name>
  <files>mazeworld.html, test/unit/shell-armor-display.test.js</files>
  <read_first>
    - mazeworld.html lines 3470-3520 (`renderCarriedList` — the `li.innerHTML` row template at ~L3502 printing `it.txt`)
    - mazeworld.html lines 4940-4968 (the pending-find card: `enc-sub` line with `it.txt` at ~L4952; the full-bag drop shelf rows with `bi.txt` at ~L4962)
    - mazeworld.html lines 4972-4990 (the store shelf: `st.stock.forEach((item, i) => ...)` row template with `item.sub` at ~L4985)
    - engine/economy.js lines 250-256 (the engine's repair stock line: `n` = `Repair your <armor>`, `effectId: "repairArmor"`, `sub` = points text — READ ONLY, do not edit)
    - test/unit/shell-party-camp.test.js (whole file — the source-assertion pattern to mirror: readFileSync + CRLF normalize + comment strip + region slicing + assert.match)
    - src/browser/viewModels.js (`bagArmorText` + `armorDisplay.wornSub`)
  </read_first>
  <action>
    **renderCarriedList (~L3502)**: change the `<i>` sub-text to `it.kind === "armor" ? window.__mzArmorDisplay.bagArmorText(it) : (it.txt ?? "")`. Comment: Phase 28 (ARMOR-03) — a stowed piece carries `left`; the row must show remaining durability, never the frozen `txt`. This single change covers the GEAR tab, the store "Your gear" sell list and the combat use list (all three hosts share this renderer).

    **Pending-find card (~L4952)**: the `enc-sub` suffix becomes `it.kind === "armor" ? ` · ${window.__mzArmorDisplay.bagArmorText(it)}` : it.txt ? ` · ${it.txt}` : ""` (a found piece is fresh, so this reads `AR n · max/max hp` — truthful and hp-labelled).

    **Full-bag drop shelf (~L4962)**: the row's `<i>` becomes `bi.kind === "armor" ? window.__mzArmorDisplay.bagArmorText(bi) : bi.txt` (guarded the same way the current `bi.txt ?` conditional is).

    **Store shelf repair row (~L4985)**: before the `forEach`, add `const ad = window.__mzArmorDisplay.armorDisplay(S.c);`. Inside, compute `const sub = item.effectId === "repairArmor" ? `${ad.wornSub} · ${S.c.armorMax - S.c.armorWP} hp to mend at a tenth of its cost each` : item.sub;` and render `sub` where `item.sub` was rendered (both the conditional and the interpolation). Comment: Phase 28 — the repair line is relabelled in the SHELL only; `engine/economy.js`'s stock `sub` string is compared by the economy parity harness and stays untouched. Repairs key off the worn piece even under a cloak (`wornSub`, not `sub`).

    **New test `test/unit/shell-armor-display.test.js`** (mirror `shell-party-camp.test.js`'s header/pattern; also import `CLOAKS` from `../../content/treasure-tables.js`). Assertions, against the comment-stripped source `CODE`:
    1. the import line and the bridge line (exact strings from Task 1's acceptance criteria) each match once;
    2. `assert.match(CODE, /const armorD = window\.__mzArmorDisplay\.armorDisplay\(c\);/)` and `assert.match(CODE, /getElementById\("s-arm"\)\.textContent = armorD\.line;/)`;
    3. `assert.equal((CODE.match(/\$\{c\.armorWP\}\/\$\{c\.armorMax\} hp/g) || []).length, 0)` — no inline current/max template survives;
    4. `assert.match(CODE, /wornRow\(armorD\.label, /)` and `/const wornRow = \(label, sub, slot, canUnequip, noSlotNeeded\) =>/`;
    5. slice the `renderCarriedList` function region (from `function renderCarriedList(` to the next `/* ---------------- the store`) and assert it matches `/it\.kind === "armor" \? window\.__mzArmorDisplay\.bagArmorText\(it\)/`;
    6. slice the store region (from `if (S.store) {` to `document.getElementById("a-leave").onclick`) and assert it matches `/item\.effectId === "repairArmor"/` and `/ad\.wornSub/` and `/hp to mend/`;
    7. slice the pending-find region (from `if (S.pendingFind && !S.combat && !S.store)` to `if (S.store) {`) and assert it matches `/bagArmorText\(it\)/` and `/bagArmorText\(bi\)/`;
    8. `const cloak = CLOAKS.find((k) => k.n === "Cloak of Armor"); assert.ok(HTML.includes(cloak.txt))` — the classic copy mirrors the live string — and the PRE-Phase-28 flavor line is gone from the live HTML: read `test/parity/prototype-master.js.txt` (frozen; it still holds the old Cloak of Armor entry), regex-extract that entry's `txt` value (the `{n:"Cloak of Armor", ... txt:"..."}` line), assert it is a non-empty string different from `cloak.txt`, and `assert.equal(HTML.includes(OLD_TXT), false)`. Never paste the old string into the test — deriving it from the frozen master keeps the test honest if the master's wording is ever consulted again.
    Note: the raw `HTML` (not comment-stripped) is fine for assertion 8; use `CODE` for the rest. Test names should cite "Phase 28 (ARMOR-02/03/04)".

    Run `node --test test/unit/shell-armor-display.test.js` — green.
  </action>
  <verify>
    <automated>node --test test/unit/shell-armor-display.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cF 'it.kind === "armor" ? window.__mzArmorDisplay.bagArmorText(it)' mazeworld.html` == 2 (renderCarriedList row + find card)
    - `grep -cF 'bi.kind === "armor" ? window.__mzArmorDisplay.bagArmorText(bi)' mazeworld.html` == 1
    - `grep -cF 'item.effectId === "repairArmor"' mazeworld.html` == 1 and `grep -cF 'hp to mend at a tenth of its cost each' mazeworld.html` == 1 and `grep -cF 'const ad = window.__mzArmorDisplay.armorDisplay(S.c);' mazeworld.html` == 1
    - `git diff --stat engine/economy.js | wc -l` == 0 (engine store text untouched)
    - `test/unit/shell-armor-display.test.js` exists, `grep -c "^test(" test/unit/shell-armor-display.test.js` >= 6, and `node --test test/unit/shell-armor-display.test.js` exits 0
    - `grep -c "readFileSync" test/unit/shell-armor-display.test.js` >= 1 and `grep -c "CLOAKS" test/unit/shell-armor-display.test.js` >= 2
  </acceptance_criteria>
  <done>Every bag/find/store armor string comes from bagArmorText or armorDisplay, the engine's store text is untouched, and the shell wiring is pinned by a source-assertion test.</done>
</task>

<task type="auto">
  <name>Task 3: Record the ARMOR-01 Key Decision in PROJECT.md and run the full-suite gate</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md lines 156-180 (the `## Key Decisions` table: `| Decision | Rationale | Outcome |` and the last rows — append after the final existing row, matching the table's column style)
    - .planning/phases/28-armor-integrity-durability/28-CONTEXT.md ("Soak-vs-wear ruling (ARMOR-01) — KEEP CANON" — the ruling being recorded, including the user's "any carrier" addition)
    - .planning/phases/28-armor-integrity-durability/28-RESEARCH.md ("Soak/Wear Rule Audit (ARMOR-01)" — the audit evidence: rulebook p.44, prototype `foeTurn` block, live engine block agree; only the two pre-declared divergences exist)
  </read_first>
  <action>
    Append ONE row to the end of PROJECT.md's Key Decisions table (use Edit, scoped to the table — never rewrite the file), with exactly these three cells:

    Decision: `Armor soak-vs-wear: canon kept (rulebook p.44 = prototype = engine — d20 ≤ AR soaks the whole blow; blocked damage above the piece's Min is charged to durability; Dwarves wear at half); Cloak of Armor = never-wearing magic Plate for ANY carrier (no class gate); destroyed armor is gone (no repair, no bag copy); durability rides on the bag item (left/patches) when a piece leaves the body`

    Rationale: `Phase 28 audit (2026-09-15): rulebook, frozen prototype and live engine already agree byte-for-byte, so changing the rule would be tuning, not a fidelity fix; the real defects were display (the HUD armor line never showed durability) and data-model (durability reset to full on every re-equip). User addition: the cloak counts as armor for anyone who can carry a cloak`

    Outcome: `✓ Landed in Phase 28 — no fixture regenerated, no new parity divergence (left/patches carved out of all three comparables as a structural tripwire); toast wear and displayed durability pinned equal by test/unit/armorDisplay.test.js`

    Then run the FULL gate: `npm test` -> `# fail 0` (expected pass count >= 1448 + the three new test files' tests), `git status --porcelain test/parity/` empty, master hash unchanged.
  </action>
  <verify>
    <automated>grep -c "Armor soak-vs-wear: canon kept" .planning/PROJECT.md && npm test 2>&1 | tail -8 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Armor soak-vs-wear: canon kept" .planning/PROJECT.md` == 1 and `grep -c "never-wearing magic Plate for ANY carrier" .planning/PROJECT.md` == 1 and `grep -c "destroyed armor is gone" .planning/PROJECT.md` == 1
    - the new row sits inside the `## Key Decisions` table (the line starts with `| Armor soak-vs-wear` and the table's header row count is unchanged: `grep -c "^| Decision | Rationale | Outcome |" .planning/PROJECT.md` == 1)
    - `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0` and `npm test 2>&1 | grep -E "^# pass"` shows >= 1480
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt | wc -l` == 0 and `git hash-object test/parity/prototype-master.js.txt` == a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `node --test test/voice/safety-scan.test.js test/unit/engine-purity.test.js test/roundtrip/serialize-rehydrate.test.js test/parity/full-suite.test.js` exits 0
  </acceptance_criteria>
  <done>The ARMOR-01 ruling is a Key Decision row in PROJECT.md and the whole phase is green under the full suite with parity byte-identical.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/shell-armor-display.test.js` green (bridge + every wiring site pinned).
- `npm test` -> `# fail 0`; parity untouched; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
- PROJECT.md Key Decisions row present (ROADMAP success criterion 5).
</verification>

<success_criteria>
- ROADMAP criterion 1: the HUD armor line (`#s-arm`) shows `current/max hp` and moves with every soaked hit — the observed discrepancy is gone at its actual site.
- ROADMAP criterion 3: the gear worn row and HUD line show the Cloak of Armor as the effective armor whenever it is carried, with the piece underneath listed.
- ROADMAP criterion 5: the keep-canon ruling is recorded in PROJECT.md; no rule change shipped, so no divergence record is needed.
- Every bag armor row shows remaining durability with the hp label; the store repair row uses the hp label without touching engine text.
</success_criteria>

<human_verification>
Not a blocking task (autonomous run) — UAT notes for the next Pixel 7 session:
1. Start a run with armor, get hit until a soak lands: the HUD Armor line must drop by exactly the toast's "wear N" (e.g. `Plate · AR 15 · 6/45 hp` after "Armour takes 39 · wear 39").
2. GEAR tab: Unequip the damaged piece, Equip it again — the durability must be unchanged (no free repair).
3. With a Cloak of Armor in the bag: HUD/worn row read `Cloak of Armor · AR 15 · magic plate, never wears · under the cloak: <piece> x/y hp`; soaked hits toast "The cloak's plate takes N · never wears".
4. The four toasts read distinctly on device: "Armour takes N · wear N", "Armour shrugs off N · under its min, no wear", "The cloak's plate takes N · never wears", "Your armour gives out."
5. Store: the Repair row's sub-label reads `AR n · x/y hp · k hp to mend at a tenth of its cost each`.
</human_verification>

<output>
Create `.planning/phases/28-armor-integrity-durability/28-03-SUMMARY.md` when done (record the `npm test` pass/fail line, the master hash, and the PROJECT.md row).
</output>
