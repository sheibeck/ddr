---
phase: 28-armor-integrity-durability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/items.js
  - engine/combat.js
  - test/parity/harness/comparables.js
  - test/unit/armor-durability.test.js
  - test/unit/feedback-payload.test.js
autonomous: true
requirements: [ARMOR-01, ARMOR-03, ARMOR-05]
tags: [armor, durability, parity, engine]

must_haves:
  truths:
    - "Unequipping a worn piece at 20/45 hp with 1 patch produces a bag item carrying left: 20 and patches: 1 (never wp-as-durability), and re-equipping it restores c.armorWP === 20, c.patches === 1, c.armorMax === 45"
    - "unequip -> equip -> unequip -> equip on a 20/45 piece with patches 1 leaves armorWP 20 / patches 1 / armorMax 45 exactly (idempotent transfer, no drift)"
    - "equipItem swaps in place: the stowed piece lands at the SAME bag index the equipped item came from (items[1] stays index 1, items[0] untouched)"
    - "wornArmorItem returns null for a bare slot (Nothing / AR 0) AND for a destroyed piece (armorWP <= 0, even while c.ar is still > 0); unequipSlot on a destroyed piece bares the slot (armor Nothing, ar 0, armorMax 0) and adds NO bag item, even on a full bag, emitting itemUnequipped with destroyed: true"
    - "A piece destroyed by applyFoeDamageToPlayer (armorWP 3, dmg 5 -> 0) and then unequipped yields zero bag items — assumption A1 accepted: combat's armorDestroyed path does NOT reset c.ar/c.armor/c.armorMax; the guard lives in wornArmorItem"
    - "A legacy bag armor item with no left field equips at full: c.armorWP === it.wp and c.patches === 0 (tolerant read, no migration pass)"
    - "A bag armor item's left/patches survive JSON.parse(JSON.stringify(state)) byte-for-byte and the item object never contains an undefined-valued key"
    - "A soaked blow with dmg exactly equal to armorMin charges no wear and the armorSoaked payload carries underMin: true; dmg = armorMin + 1 charges wear = dmg for a Human (no underMin key)"
    - "Wear is capped at remaining durability (armorWP 3, dmg 5 -> wear 3, armorWP 0, armorDestroyed emitted); a Dwarf pays Math.ceil(dmg * 0.5) (dmg 5 -> wear 3, halved: true); a d20 soak roll equal to AR soaks and AR + 1 lets the blow through"
    - "With the Cloak of Armor carried the armorSoaked payload carries magic: true and wear 0 with no underMin key, and c.armorWP is unchanged; the cloak's effect is bag-position independent"
    - "movementComparable, combatComparable and economyComparable each strip left/patches from every kind: armor element of c.items while leaving wp/ar/n and non-armor items untouched (stripBagArmorFields wired into all three)"
    - "The full suite stays green (npm test: 0 failures, parity 33/33 byte-identical) with no fixture regenerated and test/parity/prototype-master.js.txt unchanged"
    - statement: "Inventory actions are synchronous pure functions over a structuredClone'd state; no interleaving/concurrency path exists (engine-purity + roundtrip guards stay green)"
      verification: backstop
  artifacts:
    - path: "engine/items.js"
      provides: "wornArmorItem carries left/patches and null-guards a destroyed piece; equipItem restores left/patches; unequipSlot's destroyed-armor branch; rollMailPiece txt reads hp"
    - path: "engine/combat.js"
      provides: "additive underMin / magic flags on the armorSoaked payload (soak/wear math untouched)"
    - path: "test/parity/harness/comparables.js"
      provides: "stripBagArmorFields(c) nested-array carve-out wired into all three *Comparable() functions"
    - path: "test/unit/armor-durability.test.js"
      provides: "engine pin tests: exploit closed, destroyed gone, boundary/cap/Dwarven/roll-edge, flags, legacy read, roundtrip, comparables strip"
  key_links:
    - from: "engine/items.js#wornArmorItem"
      to: "engine/items.js#equipItem armor branch"
      via: "left / patches fields on the reconstructed bag item, read back with it.left ?? it.wp and it.patches ?? 0"
    - from: "engine/combat.js#applyFoeDamageToPlayer"
      to: "src/browser/toasts.js + src/browser/eventNarration.js (Plan 02)"
      via: "armorSoaked { underMin?: true, magic?: true } additive payload flags"
    - from: "test/parity/harness/comparables.js#stripBagArmorFields"
      to: "movementComparable / combatComparable / economyComparable"
      via: "wrapped around the existing rest.c strip chain in all three"
  prohibitions:
    - statement: "MUST NOT add any new rng draw (rng.d / rng.pick / rng.shuffle) to engine/items.js or engine/combat.js — the rng call counts stay at their baselines (items.js 15, combat.js 50)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit test/parity/prototype-master.js.txt (git hash-object stays a1f4d0dc29782218d8e5aab65bc5989c33f917f0) and MUST NOT regenerate any parity fixture"
      status: resolved
      verification: judgment
    - statement: "MUST NOT introduce a new event TYPE — only additive payload flags on the existing armorSoaked and itemUnequipped events"
      status: resolved
      verification: judgment
    - statement: "MUST NOT change the worn-armor scalars' semantics (c.armorWP / c.armorMax / c.patches) or any line of the combat soak/wear math (d20 <= AR gate, dmg > min charge, Math.min cap, Math.max floor, armorDestroyed condition)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT reset c.ar / c.armor / c.armorMax inside combat's armorDestroyed path (assumption A1 accepted — the destroyed guard lives in wornArmorItem only)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT touch takeItem's armor branch (fresh pieces always equip at full — no random pre-wear, a deferred idea) and MUST NOT add bag-piece repair at the store (deferred idea)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT read document/window/localStorage/Math.random anywhere under engine/ (engine-purity guard)"
      status: resolved
      verification: judgment
---

<objective>
Move armor durability onto the bag item when a piece leaves the body, close the destroyed-armor gap, and add the two additive `armorSoaked` outcome flags — the ENGINE half of Phase 28 — with the parity carve-out that keeps solo play byte-identical.

Implements the CONTEXT.md locked decisions "Durability on the item (ARMOR-03) — MINIMAL MODEL" (worn scalars unchanged; `left`/`patches` ride the bag item; fresh pieces always full; destroyed armor is gone; tolerant legacy read) and the ARMOR-05 half of "Toast <-> panel truth" (additive `underMin` / `magic` flags on the existing `armorSoaked` event, no new event types). The soak/wear math is NOT changed — ARMOR-01's ruling is "keep canon" and this plan's grep gates PROVE the soak block is untouched (the Key Decision row itself is written in Plan 03).

**Assumption A1 (RESEARCH.md) is ACCEPTED here, by design:** combat's `armorDestroyed` path keeps zeroing only `c.armorWP` (never `c.ar`/`c.armor`/`c.armorMax`), so the sheet can still read "PLATE · destroyed"; the "destroyed armor is gone" rule is enforced entirely by `wornArmorItem`'s new `c.armorWP <= 0` null-guard plus a destroyed-armor branch in `unequipSlot`. This is CONTEXT.md's "destroyed armor is GONE ... clears the slot with no bag copy" decision, under its Claude's-discretion clause.

Purpose: kill the unequip -> swap -> re-equip full-repair exploit (and its worse cousin: unequipping a DESTROYED piece today reconstructs it at full), and give the presentation layer (Plans 02/03) the four distinguishable outcomes.
Output: `engine/items.js` + `engine/combat.js` edits, `stripBagArmorFields` in the parity harness, `test/unit/armor-durability.test.js`, two additive assertions in `test/unit/feedback-payload.test.js`, full `npm test` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-armor-integrity-durability/28-CONTEXT.md
@.planning/phases/28-armor-integrity-durability/28-RESEARCH.md
@engine/items.js
@engine/combat.js
@engine/derived.js
@test/parity/harness/comparables.js
@test/unit/inventory-actions.test.js
@test/unit/combat.test.js
@test/unit/feedback-payload.test.js
</context>

## Artifacts this phase produces (whole phase — Plans 01..03)

| Kind | Symbol | Where | Plan |
|------|--------|-------|------|
| Bag-item field | `left` (number: remaining durability of a piece that has been WORN) on `kind: "armor"` bag items | written by `engine/items.js#wornArmorItem`, read by `equipItem` (`it.left ?? it.wp`) and the bag formatter | 01 |
| Bag-item field | `patches` (number) on `kind: "armor"` bag items | written by `wornArmorItem`, read by `equipItem` (`it.patches ?? 0`) | 01 |
| Event payload flag | `armorSoaked.underMin: true` (soaked, dmg <= armor min, no wear) | `engine/combat.js#applyFoeDamageToPlayer` | 01 |
| Event payload flag | `armorSoaked.magic: true` (Cloak of Armor plate soaked, never wears) | `engine/combat.js#applyFoeDamageToPlayer` | 01 |
| Event payload flag | `itemUnequipped.destroyed: true` (+ `item: { kind:"armor", n, armor, ar, left: 0 }` descriptor, NO bag copy) | `engine/items.js#unequipSlot` | 01 |
| Harness helper | `stripBagArmorFields(c)` — nested-array strip of `left`/`patches` from `c.items[]` armor elements, wired into `movementComparable`/`combatComparable`/`economyComparable` | `test/parity/harness/comparables.js` | 01 |
| Unit label fix | `rollMailPiece` txt unit token becomes `hp` | `engine/items.js` | 01 |
| Test file | `test/unit/armor-durability.test.js` | engine pins | 01 |
| Formatter | `armorDisplay(c)` -> `{ label, ar, current, max, worn, destroyed, magic, sub, wornSub, under, line }` | `src/browser/viewModels.js` (export) | 02 |
| Formatter | `bagArmorText(it)` -> `"AR n · left/max hp"` or `"AR n · destroyed"` | `src/browser/viewModels.js` (export) | 02 |
| View-model change | `characterSheetViewModel` ARMOR stat value = `${label.toUpperCase()} · ${sub}` (+ `under` field) | `src/browser/viewModels.js` | 02 |
| Copy | four-outcome `armorSoaked` toast + Oracle lines; `itemUnequipped` destroyed narration | `src/browser/toasts.js`, `src/browser/eventNarration.js` | 02 |
| Content | Cloak of Armor `txt` states the rule | `content/treasure-tables.js` (+ classic `CLOAKS` line in `mazeworld.html`) | 02 / 03 |
| Test file | `test/unit/armorDisplay.test.js` (reproduction test: toast wear === displayed durability delta; formatter cases; four-outcome strings) | presentation pins | 02 |
| Shell bridge | `window.__mzArmorDisplay = { armorDisplay, bagArmorText }` | `mazeworld.html` module script | 03 |
| Shell wiring | `#s-arm`, `#s-kit` armor row, gear worn row, `renderCarriedList` armor rows, find card, find drop-shelf, store repair row all read the formatter | `mazeworld.html` | 03 |
| Test file | `test/unit/shell-armor-display.test.js` (source-assertion test on mazeworld.html) | shell pins | 03 |
| Documentation | Key Decision row "Armor soak-vs-wear: canon kept ..." | `.planning/PROJECT.md` Key Decisions table | 03 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the engine pin tests FIRST (RED) — test/unit/armor-durability.test.js</name>
  <files>test/unit/armor-durability.test.js</files>
  <read_first>
    - test/unit/combat.test.js (lines 42-110: the file-local `fakeRng`, `fixedFighter`, `fixedFloor`, `fixedState`, `fixedFoe`, `fixedCombat` helpers — replicate them locally in the new file; lines 640-673: the existing `applyFoeDamageToPlayer` soak/destroy tests whose call shape `applyFoeDamageToPlayer(state, foe, fakeRng([soakRoll]), events, { dmg, roll: 3, need: 5 })` you reuse)
    - test/unit/inventory-actions.test.js (lines 28-58: `fixedChar`/`fixedState`/`ARMOR` helpers and the equip/unequip test idiom; lines 160-168 and 214-222: the two full-durability tests this file's partial-durability cases complement)
    - engine/items.js (lines 345-364 `wornArmorItem`, 422-460 `equipItem`, 473-497 `unequipSlot`) — the CURRENT (buggy) behavior the RED tests must fail against
    - engine/combat.js (lines 1465-1510) — the soak block and the current `armorSoaked` payload
    - engine/derived.js (lines 210-242) — `armorSoak(c)` and how `eff(c,"cloakArmor")` derives the plate
    - test/parity/harness/comparables.js (lines 87-102 `stripBagField`; 145-178 `movementComparable`; 224-245 `combatComparable`; 596-620 `economyComparable`) — the exported comparables the strip assertion calls
  </read_first>
  <behavior>
    - exploit closed: worn Plate ar 15 armorWP 20 armorMax 45 patches 1, bag [Cloth] -> equipItem(0): items[0] is Plate with left 20, patches 1, wp 45 (armorMax retained on wp); then equipItem(0) again -> c.armorWP 20, c.patches 1, c.armorMax 45 (NOT 45/0)
    - idempotent: unequipSlot("armor") -> equipItem(last) -> unequipSlot -> equipItem: armorWP 20 / patches 1 / armorMax 45 at the end
    - index-stable swap: bag [Dagger(weapon), Cloth(armor)], worn Plate 20/45 -> equipItem(1): items[1].armor === "Plate", items[1].left === 20, items[0].base === "Dagger", items.length 2
    - legacy read: bag [ARMOR("Cloth", 3) with NO left/patches keys] -> equipItem(0): c.armorWP === 20 (the helper's wp), c.patches === 0
    - fresh unequip records left even at full: worn Leather 15/15 patches 0 -> unequipSlot: items[0].left === 15, items[0].patches === 0 (concrete numbers, never undefined)
    - destroyed gone (unequip): worn Studded ar 15 armorWP 0 armorMax 20 -> unequipSlot("armor"): c.armor "Nothing", c.ar 0, c.armorMax 0, c.armorWP 0, items.length 0, one event { type:"itemUnequipped", slot:"armor", destroyed:true } whose item.n === "Studded"; NO bagFull event even when the bag is full (4 weapons in a "small" bag)
    - destroyed gone (swap): worn Studded armorWP 0, bag [Cloth] -> equipItem(0): c.armor "Cloth", items.length 0
    - destroyed via combat then unequip (A1): fixedState ar 15 armorWP 3 armorMax 20 armorMin 0 armor "Studded"; applyFoeDamageToPlayer(..., fakeRng([10]), events, { dmg:5, roll:3, need:5 }) -> armorWP 0 AND c.ar still 15; then unequipSlot -> items.length 0, c.armor "Nothing"
    - roundtrip: after the 20/45 patches 1 unequip, JSON.parse(JSON.stringify(state)) deepStrictEqual state; Object.values(items[0]) contains no undefined
    - boundary (underMin): Human ar 10 armorMin 5 armorWP 10 armorMax 10, soak roll 5, dmg 5 -> soaked.wear 0, soaked.underMin === true, "magic" in soaked === false, armorWP 10; dmg 6 -> soaked.wear 6, "underMin" in soaked === false, armorWP 4
    - cap: armorWP 3 armorMax 20 armorMin 0, dmg 5 -> wear 3, armorWP 0, events include armorDestroyed
    - Dwarven: race "Dwarven" ar 10 armorMin 0 armorWP 10, dmg 5 -> wear 3, halved true, armorWP 7
    - roll edge: ar 10, soak roll 10 -> armorSoaked present; soak roll 11 -> struckByFoe present, no armorSoaked
    - magic: Human, items [{ n:"Cloak of Armor", eff:{ cloakArmor:1 } }], armor Leather ar 6 armorWP 15 armorMax 15 armorMin 1, soak roll 12, dmg 5 -> soaked.magic === true, wear 0, "underMin" in soaked === false, armorWP 15; the same with the cloak at items index 1 (behind a weapon) gives identical payload
    - comparables: const s = newRun(1); s.c.items = [{ kind:"armor", n:"Plate", armor:"Plate", ar:15, wp:45, min:2, cls:"F", left:20, patches:1, txt:"AR 15, 45 hp" }, { kind:"weapon", n:"Dagger", base:"Dagger", bonus:0, txt:"Dagger" }]; for each of movementComparable/combatComparable/economyComparable: out.c.items[0] has no "left"/"patches" keys, out.c.items[0].wp === 45, out.c.items[1] deepEqual the weapon; the ORIGINAL s.c.items[0].left is still 20 (no mutation)
  </behavior>
  <action>
    Create `test/unit/armor-durability.test.js` (node:test + node:assert/strict, same header-comment style as the sibling files: "Phase 28 (ARMOR-03/ARMOR-05) — item-carried durability, destroyed-armor guard, armorSoaked outcome flags"). Import `equipItem`, `unequipSlot` from `../../engine/items.js`; `applyFoeDamageToPlayer` from `../../engine/combat.js`; `newRun` from `../../engine/engine.js`; `movementComparable`, `combatComparable`, `economyComparable` from `../parity/harness/comparables.js`. Define local `fakeRng`, `fixedFighter`, `fixedFloor`, `fixedState`, `fixedFoe`, `fixedCombat` mirroring `test/unit/combat.test.js` lines 42-110 (do not import them — they are file-private there), plus an `ARMOR(name, ar, cls = "FT")` bag-item helper mirroring `test/unit/inventory-actions.test.js` line 58 (wp 20, min 1, NO left/patches keys — that is the legacy shape). Give `fixedFighter` a `bag: "small"` field so the full-bag destroyed case can be driven (4 weapons = cap 4).

    Write one `test(...)` per bullet in `<behavior>` above, with the exact numbers given. For the soak tests set `state.combat = fixedCombat([foe])` before calling `applyFoeDamageToPlayer` (it reads state.combat). For the comparables test build the state from `newRun(1)` so every top-level field the comparables destructure exists.

    Run `node --test test/unit/armor-durability.test.js` and CONFIRM it FAILS (RED) on the exploit/destroyed/flag cases against today's engine (equipItem currently resets to full; wornArmorItem rebuilds a destroyed piece; no underMin/magic flags; the comparables do not strip `left`). The legacy-read, cap, Dwarven and roll-edge cases may already pass — that is expected; the file as a whole must be red.
  </action>
  <verify>
    <automated>node --test test/unit/armor-durability.test.js; test $? -ne 0 && echo RED-AS-EXPECTED</automated>
  </verify>
  <acceptance_criteria>
    - `test/unit/armor-durability.test.js` exists and `grep -c "^test(" test/unit/armor-durability.test.js` >= 14
    - `grep -c "destroyed: true\|destroyed === true\|\.destroyed, true" test/unit/armor-durability.test.js` >= 1
    - `grep -c "underMin" test/unit/armor-durability.test.js` >= 3 and `grep -c "magic" test/unit/armor-durability.test.js` >= 2
    - `grep -c "movementComparable\|combatComparable\|economyComparable" test/unit/armor-durability.test.js` >= 3
    - `node --test test/unit/armor-durability.test.js` exits NON-zero before Task 2 (RED) — record the failing test names in the SUMMARY
  </acceptance_criteria>
  <done>The new test file exists, covers every behavior bullet, and is red against the unmodified engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry durability on the item, guard destroyed armor, add the armorSoaked flags (GREEN)</name>
  <files>engine/items.js, engine/combat.js, test/unit/feedback-payload.test.js</files>
  <read_first>
    - engine/items.js (lines 305-318 header comment on the pure inventory actions; 345-364 `wornArmorItem`; 422-460 `equipItem`; 466-497 `unequipSlot`; 130-143 `rollMailPiece`; 255-300 `takeItem` — READ ONLY, do not edit)
    - engine/combat.js (lines 1465-1510: the soak block; note the `...(R.armorWear && wear > 0 ? { halved: true } : {})` spread idiom you extend)
    - engine/derived.js (lines 210-242 `armorSoak`) — `av.magic` is the cloak signal; `av.min` is the worn min
    - test/unit/feedback-payload.test.js (lines 354-389: the three existing armorSoaked payload tests you extend additively)
    - test/unit/armor-durability.test.js (Task 1) — the target behavior
  </read_first>
  <behavior>
    - all Task 1 tests pass except the comparables test (Task 3)
    - test/unit/inventory-actions.test.js, test/unit/combat.test.js, test/unit/feedback-payload.test.js stay green
  </behavior>
  <action>
    **engine/items.js — `wornArmorItem(c)`** (per CONTEXT.md "Durability on the item (ARMOR-03) — MINIMAL MODEL"): extend the null-guard so it also returns null when `c.armorWP <= 0` (a destroyed piece is never reconstructed — RESEARCH.md's second gap / assumption A1). In the returned object keep `wp: c.armorMax` (the piece's max) and ADD two concrete numeric fields: `left: c.armorWP` and `patches: c.patches || 0`. Keep `txt` as is. Rewrite the JSDoc: the reconstructed piece now carries its REMAINING durability and patch count so re-equipping does NOT repair it (the old "re-equipping repairs it" sentence is the exploit — delete it), and state that a destroyed piece (armorWP <= 0) yields null — it is gone (rulebook p.44 "permanently destroyed").

    **engine/items.js — `equipItem` armor branch**: replace the two reset lines with `c.armorWP = it.left ?? it.wp;` and `c.patches = it.patches ?? 0;` (`c.armorMax = it.wp` stays). Extend the JSDoc: a piece that has been worn carries `left`/`patches` and comes back at that durability; a fresh piece (store, foe drop, kit, treasure — no `left`) equips at full; a pre-v1.3 save's bag armor without `left` reads as full via the `??`. Note that when the worn piece is destroyed `wornArmorItem` returns null so the swap simply removes the equipped item from the bag (the destroyed piece is gone, no bag copy).

    **engine/items.js — `unequipSlot`**: BEFORE the existing `if (!worn) return events;` add a destroyed-armor branch: when `slot === "armor"` and `!worn` and `c.armor && c.armor !== "Nothing" && c.ar > 0 && c.armorWP <= 0`, bare the slot exactly as the existing armor branch does (armor "Nothing", ar 0, armorMin 0, armorMax 0, armorWP 0, patches 0), push `{ type: "itemUnequipped", item: { kind: "armor", n: <the old c.armor>, armor: <old c.armor>, ar: <old c.ar>, left: 0 }, slot: "armor", destroyed: true }` and return — NO bag-cap check (nothing is stowed) and NO `c.items.push`. Capture the old name/ar into consts before clearing. Add a JSDoc sentence: a destroyed piece needs no slot and leaves no bag copy — the additive `destroyed` flag lets the Oracle narrate it (Phase 25 additive-payload pattern; not a new event type).

    **engine/items.js — `rollMailPiece`**: the `txt` template's trailing unit token is the old two-letter unit; change that single token to `hp` (RESEARCH.md assumption A2 resolved — this IS the "bag txt says wp" site; `test/parity/harness/comparables.js#normalizeHpUnit` already normalizes this token on both sides of the store premium line, so parity is unaffected).

    **engine/combat.js — `applyFoeDamageToPlayer`** (per CONTEXT.md "Toast <-> panel truth", four outcomes via ADDITIVE flags): declare `let underMin = false;` beside the existing `let wear = 0;`. Inside the soaked branch, after the existing `if (!av.magic && dmg > av.min) { ... }` block add `else if (!av.magic) underMin = true;` — do NOT touch any line inside that block, the `dmg = 0;` line, or the `armorDestroyed` push. In the `armorSoaked` events.push add two more spreads after the `halved` one: `...(underMin ? { underMin: true } : {})` and `...(av.magic ? { magic: true } : {})`. Add a short comment: "Phase 28 (ARMOR-05, additive payload): `underMin` = soaked but the blow was at/under the armour's min (no wear); `magic` = the Cloak of Armor's plate took it (never wears). Flags only — the soak/wear expressions above are byte-identical to before." No rng call is added or moved.

    **test/unit/feedback-payload.test.js**: in the "magic plate (Cloak of Armor) never wears — wear: 0" test add `assert.equal(soaked.magic, true);` and `assert.equal("underMin" in soaked, false);`; in the "wear is NOT halved for a Human" test add `assert.equal("underMin" in soaked, false);` and `assert.equal("magic" in soaked, false);`. Nothing else in that file changes.

    Run `node --test test/unit/armor-durability.test.js test/unit/inventory-actions.test.js test/unit/combat.test.js test/unit/feedback-payload.test.js test/unit/items.test.js` — everything green except the single comparables test (Task 3).
  </action>
  <verify>
    <automated>node --test test/unit/inventory-actions.test.js test/unit/combat.test.js test/unit/feedback-payload.test.js test/unit/items.test.js && grep -c "left: c.armorWP" engine/items.js && grep -c "underMin: true" engine/combat.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "left: c.armorWP" engine/items.js` == 1 and `grep -c "patches: c.patches || 0" engine/items.js` == 1
    - `grep -c "c.armorWP = it.left ?? it.wp" engine/items.js` == 1 and `grep -c "c.patches = it.patches ?? 0" engine/items.js` == 1
    - `grep -c "c.armorWP <= 0" engine/items.js` >= 2 (wornArmorItem guard + unequipSlot destroyed branch)
    - `grep -c "destroyed: true" engine/items.js` == 1
    - `grep -cF '} wp`' engine/items.js` == 0 (rollMailPiece unit token now hp)
    - `grep -c "m.wp} hp" engine/items.js` == 1
    - `grep -c "underMin: true" engine/combat.js` == 1 and `grep -c "magic: true" engine/combat.js` == 1 and `grep -c "else if (!av.magic) underMin = true;" engine/combat.js` == 1
    - soak math untouched: `grep -cF "wear = Math.min(c.armorWP, rawWear);" engine/combat.js` == 1, `grep -cF "c.armorWP = Math.max(0, c.armorWP - rawWear);" engine/combat.js` == 1, `grep -cF 'if (!av.magic && c.armorWP <= 0) events.push({ type: "armorDestroyed" });' engine/combat.js` == 1, `grep -cF "if (soak <= av.ar) {" engine/combat.js` == 1
    - no new rng: `grep -c 'rng\.\(d\|pick\|shuffle\)(' engine/items.js` == 15 and `grep -c 'rng\.\(d\|pick\|shuffle\)(' engine/combat.js` == 50
    - takeItem untouched: `git diff --unified=0 engine/items.js | grep -c "^[-+].*it.ar <= c.ar"` == 0
    - `node --test test/unit/armor-durability.test.js 2>&1 | grep -c "^not ok"` == 1 (only the comparables test still red) — or 0 if Task 3 was folded in
    - `node --test test/unit/inventory-actions.test.js test/unit/combat.test.js test/unit/feedback-payload.test.js test/unit/items.test.js` exits 0
    - `grep -c "assert.equal(soaked.magic, true)" test/unit/feedback-payload.test.js` == 1
  </acceptance_criteria>
  <done>Re-equip preserves durability, destroyed armor never becomes a bag item (unequip or swap, full bag or not), armorSoaked carries underMin/magic, and every pre-existing test stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Parity carve-out for the new bag-item fields + full-suite gate</name>
  <files>test/parity/harness/comparables.js</files>
  <read_first>
    - test/parity/harness/comparables.js (lines 54-138: the five top-level strip helpers and their doc-comment convention — copy the tone; lines 145-178 `movementComparable`; 224-245 `combatComparable`; 596-620 `economyComparable` — the three `rest.c = ...` chains you wrap; lines 213-222 `stripFoeAbilityState` — the "structural tripwire added with zero fixture exposure" precedent to cite)
    - .planning/phases/28-armor-integrity-durability/28-RESEARCH.md ("Parity Carve-Out Pattern Gap" — why no existing helper reaches inside c.items[])
    - test/unit/armor-durability.test.js (the comparables test that must go green)
  </read_first>
  <action>
    Add a new module-private helper `stripBagArmorFields(c)` directly after `stripNameField` (per the Engine Gate: every new serialized field carved out of ALL THREE comparables). Behavior: return `c` unchanged when `!c` or `c.items` is not an array; otherwise return `{ ...c, items: c.items.map(...) }` where every element with `kind === "armor"` is rebuilt WITHOUT its `left` and `patches` keys (rest-destructure, exactly as `stripBagField` drops `bag`) and every other element is passed through by reference. Never mutate the input.

    Doc comment (same voice as `stripBagField`/`stripFoeEffectField`): Phase 28 (ARMOR-03) makes a worn piece carry its remaining durability (`left`) and patch count (`patches`) onto the bag item when it leaves the body (`engine/items.js#wornArmorItem`); the frozen prototype (`test/parity/prototype-master.js.txt` — DO NOT EDIT) rebuilds a stowed piece at full and never sets either field. No fixture drives `equipItem`/`unequipSlot` (all five inventory actions are pure and fixture-free — see items.js's header), so this is a STRUCTURAL tripwire like `stripFoeAbilityState` — a no-op on every current fixture — that keeps a future equip-driving fixture from ever reaching the diff on this deliberate, permanent divergence. Unlike every strip helper above, the fields live INSIDE `c.items[]` elements, so this helper maps the array instead of destructuring a top-level key.

    Wire it into all three chains by wrapping the OUTERMOST call: in `movementComparable`, `combatComparable` and `economyComparable`, change `rest.c = stripFoeEffectField(...)` to `rest.c = stripBagArmorFields(stripFoeEffectField(...))` (inner chain untouched).

    Then run the FULL gate: `npm test` must report 0 failures (baseline 1448 + this plan's new tests), the parity suites stay 33/33 byte-identical with no fixture regenerated, and `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` prints nothing.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -8 | grep -E "^# (pass|fail)" && git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function stripBagArmorFields(c)" test/parity/harness/comparables.js` == 1
    - `grep -c "stripBagArmorFields(" test/parity/harness/comparables.js` == 4 (1 definition + 3 call sites)
    - `grep -c 'it.kind === "armor"' test/parity/harness/comparables.js` >= 1
    - `node --test test/unit/armor-durability.test.js` exits 0 (all green, including the comparables test)
    - `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0` and `npm test 2>&1 | grep -E "^# pass"` shows >= 1462
    - `node --test test/parity/full-suite.test.js test/parity/combat-parity.test.js test/parity/economy-parity.test.js test/parity/movement-parity.test.js test/parity/magic-parity.test.js test/parity/chargen-parity.test.js` exits 0
    - `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt | wc -l` == 0
    - `git hash-object test/parity/prototype-master.js.txt` == a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `node --test test/unit/engine-purity.test.js test/roundtrip/serialize-rehydrate.test.js` exits 0
  </acceptance_criteria>
  <done>The nested-array carve-out is wired into all three comparables, the new test file is fully green, and the full suite passes with parity byte-identical and no fixture/master change.</done>
</task>

</tasks>

<verification>
- `npm test` -> `# fail 0`; pass count >= 1462 (1448 baseline + Task 1's tests).
- Parity: all six `test/parity/*.test.js` green; `git status --porcelain test/parity/` empty.
- Engine purity + roundtrip guards green (`test/unit/engine-purity.test.js`, `test/roundtrip/serialize-rehydrate.test.js`).
- Grep gates in Task 2 prove the soak/wear math lines are byte-identical and rng call counts unchanged.
</verification>

<success_criteria>
- ROADMAP success criterion 2 (re-equip preserves remaining durability; the full-repair exploit no longer exists) is proven by `test/unit/armor-durability.test.js`.
- The destroyed-armor gap (RESEARCH.md finding 3 / assumption A1) is closed at `wornArmorItem` + `unequipSlot` with no change to combat's destroy path.
- `armorSoaked` now carries `underMin` / `magic` so Plan 02 can render all four outcomes distinctly (ROADMAP criterion 4, engine half).
- No new event type, no new rng draw, no fixture regenerated, master untouched.
</success_criteria>

<output>
Create `.planning/phases/28-armor-integrity-durability/28-01-SUMMARY.md` when done (list the RED test names from Task 1, the GREEN run, the `npm test` pass/fail line, and the master hash).
</output>
