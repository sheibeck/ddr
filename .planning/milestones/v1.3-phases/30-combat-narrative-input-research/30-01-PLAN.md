---
phase: 30-combat-narrative-input-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COMBAT-NARRATIVE-DESIGN.md
autonomous: true
requirements: [CMBUI-01]
tags: [combat-ui, design-doc, research, toasts, input-guards]

must_haves:
  truths:
    - "SC1 (CMBUI-01 survey): docs/COMBAT-NARRATIVE-DESIGN.md exists with the seven locked sections in this order — 1 Current state, 2 Survey, 3 Scorecard, 4 Recommended design, 5 Runner-up, 6 Phase 32 build contract, 7 Ratification — comparing all six patterns (scrolling log/ledger, batched round-summary card, auto-advance/tap-to-pause, ticker/marquee, mis-tap guards, and today's per-event toast stack as the baseline row) with 2–3 shipped examples each, every CITED example tagged with one https URL and every ASSUMED example tagged as such"
    - "SC2 (CMBUI-01 recommendation): §4 recommends the Round Card and states a tradeoff against EACH alternative (patterns 1, 3, 4, 5-alone, 6-baseline, and the runner-up) in one table; §5 fully specifies the Guarded Bundled Toast so the user can pick it instead; both §4 and §5 address the four hard constraints by name — Tap budget, Oracle completeness, No double-reporting, Button guards"
    - "SC3 (Key Decision before Phase 32): §7 carries the single line `**Ratified design:** _pending user pick_` plus ready-to-paste `| Decision | Rationale | Outcome |` row drafts for both candidates; the PROJECT.md row and the ratification pause are the ORCHESTRATOR's steps after this plan — the executor writes neither"
    - "Boundary probe (D-scorecard scope): the §3.2 score table has exactly six rows (patterns 1–6, baseline included) × seven criteria (Taps, Coherence, Thumb-safety, Oracle-fit, Voice, Cost, Pacing) with no empty cells — every cell an integer 1–5"
    - "Precision probe (D-scorecard arithmetic): §3.1 states and justifies the weights BEFORE any score appears and they sum to 100 (20/20/20/10/10/15/5 = 100, i.e. 0.20/0.20/0.20/0.10/0.10/0.15/0.05 = 1.00); each row's weighted total is shown to two decimals and recomputes from the table within ±0.005 (automated node recompute gate); a worked arithmetic line is shown for the winner (4.75) and the baseline (2.90)"
    - "Current state carries the live-verified counts — TOAST_FOR 199, ORACLE_ONLY 20, FEATURE_EVENTS 66, EVENT_NARRATION 218, MAX_TOASTS 4, lifetime constants 3000/60/9000/1200 with the 12,600 ms worst case, 98 pinning tests across five files — and the code-read correction: the encounter overlay (#enc-panel .mw-overlay inset:0) already covers the D-pad, so the real mis-tap mechanism is rapid re-render coordinate collision with zero arm delay; §6.3 designs against that mechanism"
    - "Build contract: one ASCII wireframe per state (mid-round Round Card, Fight! gate, encounter cleared → loot card, joiner offer, death) with a tap budget row for each transition; ARM_DELAY_MS = 250 and DISMISS_SETTLE_MS = 250 specified as Date.now() timestamp checks that are never CSS-transition-dependent; the hit-zone rule and the dismissal rule stated; a toast-vs-round-surface routing table with no event reported twice; Oracle behaviour unchanged (complete, unabridged); Phase 28 armor outcomes and the Phase 29 loot card fold-in; aria-live and reduced-motion; the list of existing tests Phase 32 re-pins"
    - "No code touched: npm test still reports 1593 pass / 0 fail and `git diff --stat dfc2c61..HEAD -- engine src content mazeworld.html test` prints nothing"
  artifacts:
    - "docs/COMBAT-NARRATIVE-DESIGN.md — headings: `## 1. Current state`, `## 2. Survey`, `## 3. Scorecard` (`### 3.1 Weights`, `### 3.2 Scores`), `## 4. Recommended design — the Round Card`, `## 5. Runner-up — the Guarded, Bundled Toast`, `## 6. Phase 32 build contract` (`### 6.1 Screen anatomy` … `### 6.8 Tests Phase 32 will re-pin`), `## 7. Ratification`, `## Appendix A — Sources`, `## Appendix B — Assumptions log`"
  key_links:
    - "docs §1 counts ↔ src/browser/toasts.js / src/browser/eventNarration.js live exports (re-verified by import at write time, not copied blind)"
    - "docs §3.2 scores and weights ↔ 30-RESEARCH.md §Scorecard (identical numbers; the doc is the ratifiable record, RESEARCH is the working notes)"
    - "docs §6 ↔ Phase 32 plan-phase: the build contract is the spec CMBUI-02..06 are planned from (6.1/6.2 → CMBUI-02/03; 6.3 → CMBUI-04/05; 6.4/6.5 → CMBUI-02's Oracle clause)"
    - "docs §7 ↔ orchestrator: one AskUserQuestion (§4 vs §5 with the §3.2 summary) → PROJECT.md Key Decisions row → §7 line overwritten"
    - "every https URL in the doc ↔ a verbatim occurrence in 30-RESEARCH.md (provenance gate — no invented URLs)"
  prohibitions:
    - "No code changes: engine/, src/, content/, mazeworld.html and test/ are untouched — this phase produces exactly one markdown file; the executor does not start Phase 32"
    - "No invented URLs: every https URL in the doc appears verbatim in 30-RESEARCH.md; examples RESEARCH tagged ASSUMED stay ASSUMED with no URL attached"
    - "Oracle completeness is never traded away: no section proposes abridging, filtering, or replacing the Oracle log; the round surface is a VIEW of the current round, never a second writer"
    - "The doc never claims a decision before ratification: §4 is a recommendation, the only `Ratified design:` line reads pending until the orchestrator fills it"
    - "No PROJECT.md, ROADMAP.md or STATE.md edit by the executor — the Key Decision row is the orchestrator's step after the user's pick"
---

<objective>
Write `docs/COMBAT-NARRATIVE-DESIGN.md` — the decision-ready survey of combat-feedback patterns, the weighted scorecard, the recommended Round Card design with tradeoffs against every alternative, the fully specified Guarded Bundled Toast runner-up, and the Phase 32 build contract — from the completed research in 30-RESEARCH.md and a fresh read of the toast/renderEncounter/Oracle sources, in the section order CONTEXT.md locks, ending with a Ratification section the orchestrator fills after the user's pick.

Purpose: CMBUI-01 requires the design to be written and ratified BEFORE Phase 32 builds it. Phase 32's plan-phase will read §6 as its spec, so every number (guard timings, tap budgets, counts, test list) must be concrete and checkable here. The user's own pain points (round narrative arrives as a toast stack; dismiss-then-continue chains; decision buttons under the D-pad thumb; cards dismissed by movement taps) each map to a named hard constraint the recommendation must satisfy.

Output: one new file, `docs/COMBAT-NARRATIVE-DESIGN.md`, committed; no code, test, or planning-file changes by the executor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-combat-narrative-input-research/30-CONTEXT.md
@.planning/phases/30-combat-narrative-input-research/30-RESEARCH.md
@.planning/phases/29-end-of-combat-loot-bag-cap/29-03-SUMMARY.md
@docs/PARLEY-REBALANCE.md

This is a DOCUMENTATION-ONLY phase (CONTEXT.md: "Paper-only phase: no engine, shell, or content code changes; no on-device prototyping"). The one artifact is `docs/COMBAT-NARRATIVE-DESIGN.md`. Do not edit `engine/`, `src/`, `content/`, `mazeworld.html`, `test/`, `PROJECT.md`, `ROADMAP.md`, or `STATE.md`. Do not begin Phase 32 work. Verification agents are OFF for this run, so every acceptance criterion below is a grep / section-presence / arithmetic assertion the executor runs itself.

House style: `docs/PARLEY-REBALANCE.md`, `docs/DIFFICULTY-RETUNE.md`, `docs/CLASS-PASS.md` — ledger documents with a short framing paragraph, tables for anything comparable, code identifiers in backticks, file:line anchors for code claims, numbers verified rather than remembered, and a recorded verdict. Product name in prose is "Delve, Die, Repeat" (STATE.md Ground Truth: the old working title survives only in filenames and code ids such as `mazeworld.html` — never in doc prose).

Locked decisions honoured (30-CONTEXT.md): six patterns incl. the baseline row; 2–3 shipped examples per pattern with URLs; a weighted scorecard whose weights are fixed and justified before scoring; deliverable section order current-state → survey → scorecard → recommendation-with-tradeoffs-against-EACH-alternative → runner-up → Phase 32 build contract; four hard constraints (≤1 deliberate tap to move on and zero taps to read; Oracle complete and unabridged; in-combat per-event toasts replaced with no double-reporting while out-of-combat Phase 25/25.1 toasts stand; button guards = no shared hit zone + ~250 ms arm delay + no incidental dismissal); ONE ratification pause performed by the orchestrator after this plan; wireframes per state (Claude's discretion → yes). Deferred and therefore absent from the doc's recommendation: any implementation, haptics as a decision factor (may be named as an enhancer only), the tutorial (UX-06).

Citation discipline (orchestrator): tag every example `[CITED: https://…]` (one URL per tag — use two tags for two sources; the provenance gate tokenizes on `]` and whitespace) or `[ASSUMED]`, exactly mirroring 30-RESEARCH.md's tagging. Never add a URL that does not appear verbatim in 30-RESEARCH.md. Pattern 4's examples are all ASSUMED in RESEARCH — say so plainly rather than hunting for links.

<!-- planner-discipline-allow: Mazeworld -->
<!-- planner-discipline-allow: Delve, Die, Repeat -->
<!-- planner-discipline-allow: Ratified design: -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft the survey half — current state, survey table, scorecard, recommendation, runner-up</name>
  <files>docs/COMBAT-NARRATIVE-DESIGN.md</files>
  <read_first>
    - .planning/phases/30-combat-narrative-input-research/30-RESEARCH.md in full (473 lines) — §Current State — Code Read, §Pattern Survey, §Scorecard, §Recommended Design, §Runner-Up Design, §Tradeoffs Against Each Alternative, §Sources are the source material for §1–§5
    - .planning/phases/30-combat-narrative-input-research/30-CONTEXT.md in full — the locked section order, the four hard constraints, the deferred list
    - .planning/REQUIREMENTS.md lines 10–14 (v1.3 Grounding "Combat feedback today" — note it says 189 toast types; the live count is now 199) and lines 35–42 (CMBUI-01..06)
    - .planning/ROADMAP.md "### Phase 30" and "### Phase 32" sections (lines 166–212) — Phase 32's five success criteria are what §6 must make buildable
    - .planning/phases/29-end-of-combat-loot-bag-cap/29-03-SUMMARY.md frontmatter (`provides:` and `tech-stack.patterns`) — the `window.__mzLootReport` hand-off and the "report + decision folded into ONE card" pattern the Round Card generalizes
    - docs/PARLEY-REBALANCE.md lines 1–60 — house style for a design ledger (framing paragraph, decision table, verified numbers)
    - src/browser/toasts.js lines 40–100 (PRIORITY, MAX_TOASTS, CARD_EVENTS, NARRATIVE_ACTIONS, TOAST_BASE_MS/TOAST_PER_CHAR_MS/TOAST_CAP_MS/TOAST_STACK_BONUS_MS, toastLifetime), lines 155–190 (ORACLE_ONLY, FEATURE_EVENTS heads), lines 816–850 (toastsForAction head — the grouper names enemyRound/yourRound/spellChain/fleeChain/parleyChain/chestChain/killFold)
    - src/browser/eventNarration.js lines 60–80 (EVENT_NARRATION head) and src/browser/viewModels.js `oracleLogViewModel`
    - mazeworld.html lines 693–702 (toast host CSS: position fixed near the top), line 773 (the blanket prefers-reduced-motion rule), line 1342 (`#mw-toast-host` aria-live markup), lines 1396–1410 (`#enc-dismiss-slot` header slot), lines 4724–4735 (`hasActiveEncounter()`), lines 4832–4860 (`renderEncounter()` head and branch order), lines 5228–5235 (the `S.lastExchange` "Last exchange" footnote the Round Card replaces)
    - src/browser/controls.js lines 15–30 (TAP_MOVE_THRESHOLD_PX = 10, TAP_MAX_DURATION_MS = 350 — the existing numeric input vocabulary)
  </read_first>
  <action>
    Verify before writing. Run the live-count one-liner `node --input-type=module -e "const t=await import('./src/browser/toasts.js');const n=await import('./src/browser/eventNarration.js');const sz=x=>x instanceof Set?x.size:Object.keys(x).length;console.log(sz(t.TOAST_FOR),sz(t.ORACLE_ONLY),sz(t.FEATURE_EVENTS),t.MAX_TOASTS,sz(n.EVENT_NARRATION))"` — expected `199 20 66 4 218`. Use whatever it prints (these are the doc's numbers); if any differs from RESEARCH, use the live value and add a one-line note. Also confirm `grep -c "test(" test/unit/narrativeToasts.test.js test/unit/shell-toast-wiring.test.js test/unit/feedback-payload.test.js test/unit/oracleLogViewModel.test.js test/unit/shell-oracle-panel.test.js` is consistent with the 15/28/38/8/9 = 98 table (use `it(` if that is the file's idiom); if a count differs, carry the live count.

    Create `docs/COMBAT-NARRATIVE-DESIGN.md` with the Write tool (single call; if the runtime truncates, fall back to Write-then-Edit-append per the execute workflow). Title line: `# Combat Narrative & Input Design (Phase 30) — survey, scorecard, and Phase 32 build contract`. Preamble (4–6 lines): what the doc is; requirement CMBUI-01; that §4 is a RECOMMENDATION pending the ratification recorded in §7; that Phase 31 is engine-only and Phase 32 builds from §6; the product name "Delve, Die, Repeat"; the citation legend (CITED = URL read during research; ASSUMED = genre knowledge, no URL).

    Section 1 heading exactly `## 1. Current state — what the player sees today`, with subsections `### 1.1 The toast system`, `### 1.2 renderEncounter and the encounter overlay`, `### 1.3 The Oracle log`, `### 1.4 Round event volume`, `### 1.5 What actually mis-taps (code-read correction)`, `### 1.6 Tests pinning today's behaviour`. Content, all from RESEARCH §Current State with file anchors: 1.1 — a constants table (Constant | Live value | Purpose) with rows TOAST_FOR 199, ORACLE_ONLY 20, FEATURE_EVENTS 66, MAX_TOASTS 4, PRIORITY {block 0, you 1, them 2, feature 3, other 4}, CARD_EVENTS {floorChanged, leveled}, NARRATIVE_ACTIONS {move, camp, resolveJoiner}, TOAST_BASE_MS 3000 / TOAST_PER_CHAR_MS 60 / TOAST_CAP_MS 9000 / TOAST_STACK_BONUS_MS 1200 with the toastLifetime formula and the 12,600 ms worst case; then a paragraph on the toastsForAction pipeline naming the groupers (encounterStart, enemyRound with the 3+-foe collapse, yourRound, spellChain, fleeChain/parleyChain/chestChain, killFold), the type dedupe, priority sort, and the slice to MAX_TOASTS; note that REQUIREMENTS' grounding row says 189 because later phases added events. 1.2 — the branch order of renderEncounter (S.dead → S.won → S.beats incl. the DR17 awaitingFight roster → S.pendingLoot → S.pendingJoiner → S.pendingFind → S.store → live combat panel with the Fight! gate and the 1–7 action bar); that `#enc-panel` is `.mw-overlay` `position:absolute; inset:0` inside `.mazebox`, which also contains `.mazefoot`'s D-pad, so the D-pad is covered for hit-testing whenever any surface is up; the `hasActiveEncounter()` predicate spelled out; `#mw-toast-host` fixed near the TOP (`calc(102px + safe-area-inset-top)`), not near the D-pad; the DR16 `#enc-dismiss-slot` precedent (positional, not time-gated); the Phase 29 loot card as the already-shipped proof of the batched pattern (`window.__mzLootReport`). 1.3 — EVENT_NARRATION 218 entries; two-writer split with TOAST_FOR + ORACLE_ONLY = 219 (the 1-entry gap is bookkeeping, not a leak); oracleLogViewModel newest-first with the reveal flag; full-screen tab that opens at newest (`window.__mzOracleToNewest()`). 1.4 — PARTY_CAP 1, FOE_CAP_MAX 4, the afterPlayerAction chain (player action → allyTurn → alliesTurn → foeTurn, possibly a second foeTurn on won initiative), 10–15 raw events per busy round folded to at most 4 toasts each lingering 3–12.6 s — this is the CMBUI-02 complaint restated in numbers. 1.5 — MUST contain the phrase "rapid re-render coordinate collision" and state plainly: the overlay already covers the D-pad, so there is no bleed-through path; no `pointerdown`/`setTimeout`/armed guard exists on any card button (every `.onclick` is wired in the same tick `#enc-body.innerHTML` is rebuilt); the two real failure modes are (a) a fast second tap landing on a NEW button rendered at the same coordinate as the OLD one (Move on → next round's 1·Strike) and (b) an overlay closing synchronously inside a click handler so an eager double-tap's second touch reaches the now-live D-pad; the one in-source use of "armed" (death card CONFIRM) means always-clickable, the opposite of a gate; `touch-action:manipulation` on the canvas means the legacy 300 ms tap delay is already gone, so any guard must be a deliberate JS timestamp check. 1.6 — the five-file table (test/unit/narrativeToasts.test.js 15, shell-toast-wiring.test.js 28, feedback-payload.test.js 38, oracleLogViewModel.test.js 8, shell-oracle-panel.test.js 9; total 98) plus the 25-05 toastsCoverage/toastTable partition invariants; npm test baseline 1593/1593.

    Section 2 heading exactly `## 2. Survey — six patterns against the criteria`, subsections `### 2.1 Shipped examples per pattern` and `### 2.2 Survey table`. 2.1: for each of the six patterns a bold label, a one-sentence definition, 2–3 example bullets each ending in its tag, and a short "Fit for this game" paragraph — carry RESEARCH's exact example set and tags: Pattern 1 (DCSS/traditional roguelike message logs ASSUMED; Cogmind ASSUMED; roguelike log architecture CITED yosenspace); Pattern 2 (Darkest Dungeon combat mechanics CITED wiki; Darkest Dungeon Tablet Edition CITED App Store; Fire Emblem/Advance Wars result panels ASSUMED; Into the Breach telegraphing CITED gamedeveloper AND CITED interfaceingame as two tags); Pattern 3 (Hoplite CITED Wikipedia and CITED Medium as two tags, incl. the opt-in Fat Finger Mode observation; Card Crawl/Dicey Dungeons ASSUMED; idle/auto-battle speed toggles ASSUMED); Pattern 4 (Diablo floating numbers ASSUMED; Pokémon-style tap-to-advance boxes ASSUMED; MMO floating combat text ASSUMED — state explicitly that no URL was read for this pattern); Pattern 5 (Hoplite Fat Finger Mode CITED Wikipedia; Material touch-target guidance CITED m1.material.io AND CITED support.google.com as two tags — 48×48dp, 8dp separation; the 300 ms tap-delay history CITED developer.chrome.com); Pattern 6 (this project's own `src/browser/toasts.js` and `mazeworld.html#renderEncounter` — file paths, no URL, cross-reference §1). Take the full https URLs from RESEARCH's "### Secondary" Sources list (eleven URLs); one URL per tag. 2.2: one table, columns `Pattern | Examples | Taps-to-move-on | Coherence | Thumb-safety | Oracle fit | Voice room | Impl. cost | Pacing`, six rows whose first cell begins `1. ` … `6. ` (e.g. `1. Scrolling log / ledger`, `2. Batched round-summary card`, `3. Auto-advance / tap-to-pause`, `4. Ticker / floating text`, `5. Mis-tap guards only`, `6. Today's toast stack (baseline)`); every criterion cell is a short phrase describing HOW the pattern handles that criterion — no numeric scores in this table (scores belong to §3).

    Section 3 heading exactly `## 3. Scorecard`. `### 3.1 Weights (fixed before scoring)`: a table `Criterion | Weight | Justification` with RESEARCH's seven rows in order — Taps-to-move-on (CMBUI-03) 20%, Narrative coherence per round (CMBUI-02) 20%, Thumb-safety (CMBUI-04/05) 20%, Oracle-remains-the-log fit 10%, Room for the sarcastic voice 10%, Implementation cost inside the existing toast/card/beats system 15%, 5–10-minute session pacing 5% — and RESEARCH's justifications (the three hard constraints share 60%; cost at 15% because this is a solo-dev vanilla-JS shell with a 1593-test suite; Oracle-fit and voice at 10% as identity-preserving but writable-around; pacing at 5% as a tuning knob). Directly under the table, this exact line: `**Weights sum:** 20 + 20 + 20 + 10 + 10 + 15 + 5 = 100` followed on the same line by the decimal form 0.20/0.20/0.20/0.10/0.10/0.15/0.05 = 1.00. `### 3.2 Scores`: the header row exactly `| Pattern | Taps (.20) | Coherence (.20) | Thumb-safety (.20) | Oracle-fit (.10) | Voice (.10) | Cost (.15) | Pacing (.05) | Weighted total |`; six rows whose first cell begins with the pattern number and a period (bold allowed, e.g. `**2. Batched round-summary card**`), the seven integer scores from RESEARCH (row 1: 5 3 3 2 3 3 4 → 3.35; row 2: 5 5 4 5 5 5 4 → 4.75; row 3: 5 2 2 3 3 2 3 → 2.85; row 4: 3 2 4 3 1 3 4 → 2.85; row 5: 2 2 5 3 3 4 3 → 3.15; row 6: 2 2 2 4 4 5 3 → 2.90), and the total as a two-decimal number (bold allowed). Under the table: a worked arithmetic line for row 2 (5×.20 + 5×.20 + 4×.20 + 5×.10 + 5×.10 + 5×.15 + 4×.05 = 1.00 + 1.00 + 0.80 + 0.50 + 0.50 + 0.75 + 0.20 = 4.75) and for row 6 (= 2.90); the sentence that every total is Σ(score × weight) from this table; the one-line weakest-score justification per pattern from RESEARCH; then the verdict paragraph — winner Pattern 2 at 4.75 combined with Pattern 5's guard layer as a MANDATORY addition (lifting thumb-safety 4→5), runner-up the Pattern 5+6 hybrid "Guarded, Bundled Toast" (3.15 as scored, materially higher on coherence once bundling is counted).

    Section 4 heading exactly `## 4. Recommended design — the Round Card`, subsections `### 4.1 Concept`, `### 4.2 Hard-constraint compliance`, `### 4.3 Tradeoffs against each alternative`. 4.1: RESEARCH's concept — extend the Phase 29 loot-card machinery to every round; reuse toastsForAction's pure groupers unmodified and retarget their OUTPUT to one persistent DOM block that renderEncounter rebuilds each render; the block lives in the `S.lastExchange` "Last exchange" slot (below the always-visible foe roster, above the action bar), replacing that footnote; no new overlay layer or z-index; then the Round Card ASCII wireframe from RESEARCH inside a fenced block (head → roster → THIS ROUND block → action bar with the ARM_DELAY_MS note). 4.2: a table with exactly four rows whose first cells are `**Tap budget**`, `**Oracle completeness**`, `**No double-reporting**`, `**Button guards**` — each row states the constraint (≤1 deliberate tap to move on and zero taps to read; the Oracle stays the complete unabridged log and the round card is a view; in-combat per-event toasts are replaced and no event is reported twice while out-of-combat toasts stand; no shared hit zone + ARM_DELAY_MS = 250 + no incidental dismissal) and HOW the Round Card meets it with the mechanism/number. 4.3: a table `Alternative | Why the Round Card wins over it` with six rows — Pattern 1 (scrolling log), Pattern 3 (auto-advance), Pattern 4 (ticker/floating text), Pattern 5 alone (guards, no narrative change), Pattern 6 (today's baseline), Runner-up (Guarded Bundled Toast) — carrying RESEARCH's tradeoff text, and a closing sentence naming what the Round Card gives up versus the runner-up (more test re-pinning, a new DOM block inside renderEncounter).

    Section 5 heading exactly `## 5. Runner-up — the Guarded, Bundled Toast`, subsections `### 5.1 Concept`, `### 5.2 Hard-constraint compliance`, `### 5.3 Why it is second, and when to pick it`. 5.1: keep the floating-toast aesthetic and the fixed-top `#mw-toast-host`; bundle a round's toasts into exactly ONE toast (MAX_TOASTS effectively 1 for in-combat events); add the same ARM_DELAY_MS/DISMISS_SETTLE_MS guards to the action bar; the runner-up ASCII wireframe from RESEARCH in a fenced block; tap budget identical to §4 (0 extra mid-round, 1 tap at every decision gate); re-derive toastLifetime's constants against the bundled toast's typical character count (RESEARCH Open Question 2) so a slow reader is not cut off. 5.2: the same four-row compliance table (`**Tap budget**`, `**Oracle completeness**`, `**No double-reporting**`, `**Button guards**`) answered honestly for this design — Tap budget met, but "zero taps to read" is weaker because a toast expires (state the lifetime mitigation); Oracle completeness unchanged; No double-reporting met by routing in-combat events to the one bundled toast; Button guards identical numbers. 5.3: RESEARCH's reasons (floating, timed, auto-dismissing surface away from the roster; keeps toasts as the primary combat channel) and the honest case FOR picking it (cheaper; most of the 98 tests need a bundling-count update rather than a structural rewrite; preserves the current visual language).

    End the file with the single heading line `## 6. Phase 32 build contract` and nothing after it — Task 2 fills the section in place. Do not write §7 or the appendices in this task (order is locked: §6 must precede §7).
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && D=docs/COMBAT-NARRATIVE-DESIGN.md && test "$(grep -o '^## [1-6]\.' $D | tr -dc '0-9' | tr -d '\n')" = "123456" && test "$(tail -1 $D)" = "## 6. Phase 32 build contract" && grep -qF 'Weights sum:' $D && grep -qF '= 100' $D && grep -q 'rapid re-render coordinate collision' $D && test "$(awk '/^### 2\.2 /{f=1} /^## 3\. /{f=0} f' $D | grep -c '^| [1-6]\. ')" = "6" && node -e "const t=require('fs').readFileSync('docs/COMBAT-NARRATIVE-DESIGN.md','utf8');const s=t.split('### 3.2 Scores')[1].split('\n## ')[0];const W=[.2,.2,.2,.1,.1,.15,.05];const rows=s.split('\n').filter(l=>/^\|\s*\**[1-6]\./.test(l));if(rows.length!==6)throw new Error('expected 6 score rows, got '+rows.length);for(const r of rows){const c=r.split('|').map(x=>x.replace(/\*/g,'').trim()).filter(Boolean);if(c.length!==9)throw new Error('expected 9 cells: '+r);const sc=c.slice(1,8).map(Number);if(sc.some(v=>!Number.isInteger(v)||v<1||v>5))throw new Error('bad score cell: '+r);const tot=sc.reduce((a,v,i)=>a+v*W[i],0);const stated=Number(c[8]);if(Math.abs(tot-stated)>0.005)throw new Error('total mismatch '+c[0]+': '+tot.toFixed(2)+' vs '+stated);}console.log('scorecard OK')" && test -z "$(for u in $(grep -o 'https://[^] ]*' $D | sort -u); do grep -qF -- "$u" .planning/phases/30-combat-narrative-input-research/30-RESEARCH.md || echo "INVENTED: $u"; done)" && echo TASK1-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o '^## [1-6]\.' docs/COMBAT-NARRATIVE-DESIGN.md | tr -dc '0-9' | tr -d '\n'` prints `123456` (sections 1–6 present, in order, once each)
    - `tail -1 docs/COMBAT-NARRATIVE-DESIGN.md` is exactly `## 6. Phase 32 build contract`
    - `grep -cF '## 1. Current state' docs/COMBAT-NARRATIVE-DESIGN.md` == 1; `grep -cF '## 2. Survey' …` == 1; `grep -cF '## 3. Scorecard' …` == 1; `grep -cF '## 4. Recommended design — the Round Card' …` == 1; `grep -cF '## 5. Runner-up — the Guarded, Bundled Toast' …` == 1
    - `grep -cF '### 1.5 What actually mis-taps' …` == 1 and `grep -c 'rapid re-render coordinate collision' …` >= 1 and `grep -cF 'inset:0' …` >= 1 (the correction is stated)
    - Live counts present: `grep -cF 'TOAST_FOR' …` >= 1, `grep -c '\b199\b' …` >= 1, `grep -c '\b218\b' …` >= 1, `grep -cF 'MAX_TOASTS' …` >= 1, `grep -c '12,600' …` >= 1, `grep -c '\b98\b' …` >= 1, `grep -cF '1593' …` >= 1
    - `grep -cF 'window.__mzLootReport' …` >= 1 (the Phase 29 proof-of-concept is named)
    - `awk '/^### 2\.2 /{f=1} /^## 3\. /{f=0} f' docs/COMBAT-NARRATIVE-DESIGN.md | grep -c '^| [1-6]\. '` == 6 (survey table has all six rows incl. the baseline)
    - Citation tags: `grep -o '\[CITED:[^]]*\]' … | wc -l` >= 10 and equals `grep -o '\[CITED: https://[^]]*\]' … | wc -l` (every CITED tag carries one https URL); `grep -c '\[ASSUMED\]' …` >= 8
    - URL provenance: `for u in $(grep -o 'https://[^] ]*' docs/COMBAT-NARRATIVE-DESIGN.md | sort -u); do grep -qF -- "$u" .planning/phases/30-combat-narrative-input-research/30-RESEARCH.md || echo "INVENTED: $u"; done` prints nothing; `grep -o 'https://[^] ]*' … | sort -u | wc -l` >= 10
    - `grep -cF '### 3.1 Weights' …` == 1 and `grep -cF '### 3.2 Scores' …` == 1 and §3.1 precedes §3.2 (`grep -n` line numbers ascending)
    - `grep -cF '**Weights sum:** 20 + 20 + 20 + 10 + 10 + 15 + 5 = 100' …` == 1
    - The node recompute gate in `<verify>` prints `scorecard OK` (six rows, nine cells each, integer 1–5 scores, totals within ±0.005 of Σ score×weight)
    - `grep -cF '4.75' …` >= 2 (table total + worked line) and `grep -cF '2.90' …` >= 2
    - §4 and §5 each contain all four constraint labels: `awk '/^## 4\. /{f=1} /^## 5\. /{f=0} f' … | grep -c 'Tap budget'` >= 1, same for `Oracle completeness`, `No double-reporting`, `Button guards`; and `awk '/^## 5\. /{f=1} /^## 6\. /{f=0} f' … | grep -c '<label>'` >= 1 for each of the four
    - `awk '/^### 4\.3 /{f=1} /^## 5\. /{f=0} f' … | grep -c '^| '` >= 8 (header + separator + six alternative rows)
    - Wireframes: `awk '/^## 4\. /{f=1} /^## 5\. /{f=0} f' … | grep -c '^```'` >= 2 and `awk '/^## 5\. /{f=1} /^## 6\. /{f=0} f' … | grep -c '^```'` >= 2
    - `grep -cF 'ARM_DELAY_MS' …` >= 2 and `grep -cF 'DISMISS_SETTLE_MS' …` >= 1
    - `grep -cF 'Delve, Die, Repeat' …` >= 1 and `grep -v '^<!--' docs/COMBAT-NARRATIVE-DESIGN.md | grep -c 'Mazeworld'` == 0 (product name in prose; the old working title only ever appears as the lowercase filename)
    - `git status --porcelain -- engine src content mazeworld.html test` prints nothing
  </acceptance_criteria>
  <done>Sections 1–5 exist in the locked order with live-verified counts, the code-read correction, six survey rows with tagged examples and provenance-checked URLs, a weights table summing to 100 fixed before a six-row score table whose totals recompute, a Round Card recommendation with a four-constraint compliance table and tradeoffs against every alternative, and a fully specified runner-up — the file ending at the §6 heading for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 32 build contract, the Ratification section and appendices; self-review the whole doc against the locked constraints</name>
  <files>docs/COMBAT-NARRATIVE-DESIGN.md</files>
  <read_first>
    - docs/COMBAT-NARRATIVE-DESIGN.md as written by Task 1 (§1.5 and §4.1/§4.2 in particular — §6 must be consistent with the correction and the constraint labels already used)
    - .planning/phases/30-combat-narrative-input-research/30-RESEARCH.md lines 229–430 — §Screen anatomy, §Tap budget per state, §Guard rules, §What stays a toast vs. joins the round surface, §Oracle behaviour, §How Phase 28 armor outcomes / Phase 29 loot fold in, §Runner-Up, §Pitfalls 1–5, §Don't Hand-Roll, §Assumptions Log A1–A5, §Open Questions 1–2, §Sources
    - .planning/phases/30-combat-narrative-input-research/30-CONTEXT.md "Ratification & hand-off" and "Design constraints the recommendation MUST satisfy"
    - .planning/ROADMAP.md "### Phase 32" (lines 197–212) — the five success criteria §6 is the spec for
    - .planning/PROJECT.md Key Decisions table (header `| Decision | Rationale | Outcome |`) — the row shape §7's drafts must match
    - mazeworld.html lines 4832–4990 (renderEncounter: death/victory cards, the beats "Move on" + DR17 awaitingFight roster branch with the Fight! button, the Phase 29 loot branch with `#a-loot-take-all`/`#a-loot-leave-all`, the joiner branch, the find card) and lines 5228–5235 (the `S.lastExchange` footnote) — the states the wireframes depict
    - src/browser/controls.js lines 15–30 (TAP_MOVE_THRESHOLD_PX / TAP_MAX_DURATION_MS — the sibling numeric vocabulary)
    - test/unit/shell-loot-screen.test.js header (the readFileSync + stripComments source-assertion pattern Phase 32's new region tests will copy)
  </read_first>
  <action>
    Use the Edit tool: `old_string` = the file's last line `## 6. Phase 32 build contract`; `new_string` = that heading followed by the full §6 body, §7, Appendix A and Appendix B below. (If the runtime truncates a single Edit, split into two Edits at the §7 boundary.)

    §6 opens with two sentences: this section is Phase 32's spec for CMBUI-02..06 and is written for the RECOMMENDED design; where the runner-up differs, the difference is called out inline under "If the runner-up is ratified". Then eight subsections with these exact headings:

    `### 6.1 Screen anatomy` — state that `#enc-panel`/`.mw-overlay` geometry relative to `.mazebox`/`.mazefoot` is unchanged and that the round-narrative block occupies the `S.lastExchange` "Last exchange" slot (below the always-first foe roster, above the `.actions` bar), replacing that footnote; the foe roster is never hidden (RESEARCH Pitfall 5); then one fenced ASCII wireframe per state, each preceded by a bold label on its own line, exactly: `**State: mid-round (Round Card)**`, `**State: Fight! gate (ambush preview)**`, `**State: encounter cleared → loot card**`, `**State: joiner offer**`, `**State: death**`. Each wireframe marks its decision buttons and annotates them with ARM_DELAY_MS; the mid-round frame shows head → roster → THIS ROUND block → 1·Strike/2·Potion/3·Flee bar; the Fight! frame shows the DR17 awaitingFight roster with the single Fight! button; the loot frame is the Phase 29 card (Victory head with the folded report, the spoils rows, Take all / Leave all); the joiner frame shows the offer text with Take them along / Leave them; the death frame shows the epitaph with Confirm and the elective Review the Oracle. Close with an "If the runner-up is ratified" paragraph: the THIS ROUND block is instead one bundled toast in `#mw-toast-host`; everything else in the frames is identical.

    `### 6.2 Tap budget per state` — a table `State transition | Taps required | What Phase 32 must preserve or change` with rows whose first cells begin exactly: `Mid-round → next round` (0 extra taps beyond the chosen action; the narrative is always visible; no Move-on gate is inserted mid-fight — preserved, not reduced); `Fight! gate → first round` (1 tap on Fight!; the gate itself is Phase 31's business, the button's guard is Phase 32's); `Encounter cleared → loot resolved → move on` (1 tap: Take all / Leave all, or per-item then implicitly done — Phase 29, preserved verbatim); `Joiner offer` (1 tap); `Death` (1 tap Confirm; Review the Oracle is an elective second tap); `Floor change / level-up Move on` (1 tap; out of CMBUI-02's in-combat scope, must not regress; the guard still applies to the `#enc-dismiss-slot` button). State the invariant under the table: no state requires a dismiss-then-continue chain, and the total deliberate taps to leave any surface is 1.

    `### 6.3 Guard rules` — first restate the correction in one sentence (the guards are designed against rapid re-render coordinate collision and the synchronous-dismiss gap, not against D-pad bleed-through, which the overlay already prevents). Then four numbered rules: (1) `ARM_DELAY_MS = 250` — every freshly rendered decision button (round-card action bar, Fight!, joiner accept/decline, loot per-row and Take all/Leave all, find Take/Leave, death Confirm, the `#enc-dismiss-slot` Move on) records `renderedAt = Date.now()` when renderEncounter builds it and its handler no-ops (or the button carries `disabled` — Phase 32 picks whichever fits the existing `.disabled` CSS convention) while `Date.now() - renderedAt < ARM_DELAY_MS`; (2) `DISMISS_SETTLE_MS = 250` — when `hasActiveEncounter()` transitions true → false record `lastDismissAt = Date.now()`, and `window.move`'s guard gains a second clause refusing input while `Date.now() - lastDismissAt < DISMISS_SETTLE_MS`; (3) hit-zone rule — decision buttons never share the D-pad's hit zone; satisfied structurally today because `.mw-overlay` `inset:0` covers `.mazefoot`, so Phase 32's obligation is to render every decision button INSIDE `#enc-panel` and never introduce one outside the overlay, and to keep the roster/narrative/actions vertical order so the action bar's coordinates stay stable between rounds; (4) dismissal rule — nothing important dismisses on a tap in the map/D-pad region; the round card has NO tap-to-dismiss gesture at all (it is rebuilt by the next render, never dismissed), every other surface dismisses only via its own buttons, and Phase 32 must not add any tap-anywhere-to-dismiss behaviour. Then: the "distinct gesture" technique is explicitly not adopted (Hoplite's Fat Finger Mode costs an extra tap; the two timing guards reach safety at zero tap cost). Then the accessibility-critical rule in bold: both guards are `Date.now()` timestamp checks and must never depend on a CSS transition or animation duration, because the blanket `prefers-reduced-motion` rule (mazeworld.html line 773) sets `transition:none` and `animation:none` for those users and would silently zero a transition-based guard. Name the sibling constants `TAP_MOVE_THRESHOLD_PX = 10` and `TAP_MAX_DURATION_MS = 350` in `src/browser/controls.js` as the vocabulary the two new constants join (module placement is Phase 32's call). If the runner-up is ratified: identical numbers on the action bar; the bundled toast itself needs no arm delay (non-interactive except its own optional tap-to-dismiss, which must carry `touch-action:manipulation` like `.mw-toast`).

    `### 6.4 Toast-vs-round-surface routing` — a table `Event family | Examples | In combat | Out of combat | Rule` with rows: player outcomes (`struck`, `strikeMissed`, `foeKilled` via killFold) → round card / n/a; foe outcomes (`struckByFoe`, `foeMissed`, `armorSoaked` and the four Phase 28 armor outcomes) → round card / n/a; party and ally (`memberStruck`, ally strikes and casts) → round card / n/a; spell chains (`spellThrown` → outcome, 3+ targets collapsed) → round card / n/a; in-combat rolls (flee, parley, chest chains) → round card / n/a; foe abilities and conditions → round card / n/a; feature events (FEATURE_EVENTS) → round card in combat, toast out of combat; refusals and blocks (`PRIORITY.block` tier — `strikeRefused`, `fleeRefused`, `noChargesLeft`, `spellNotKnown`, and kin) → STAYS a toast in both (a refusal is a direct response to the just-tapped button); `encounterStarted` and its same-action followers → the first round card; loot pile events (`lootDropped` mid-fight → round card; `lootTaken`/`lootLeft` at the loot card → toast as Phase 29 ships); out-of-combat narrative (`move`, `camp`, teleport, find) → toast unchanged; `CARD_EVENTS` (`floorChanged`, `leveled`) → the `S.beats` Move on card unchanged. Under the table, the rule in bold: every event has exactly ONE in-combat presentation destination — nothing is both toasted and folded into the round card — and the Oracle receives every event regardless of destination. Note that the 25-05 partition invariants (every type in exactly one of TOAST_FOR / ORACLE_ONLY; FEATURE_EVENTS ⊆ TOAST_FOR) hold structurally if Phase 32 keeps routing through `toastsForAction` and only retargets its output. If the runner-up is ratified: identical routing; "round card" reads "the one bundled toast".

    `### 6.5 Oracle behaviour` — unchanged and unabridged: EVENT_NARRATION's 218 entries, full-screen tab, opens at newest via `window.__mzOracleToNewest()`, the dice-reveal setting untouched; the round surface derives its grouped text from a copy of the same `events[]` array the Oracle's `logLine` calls consume and is never a second writer into `S.log`; state explicitly that any Phase 32 change that drops, filters, or summarizes an Oracle line is out of contract.

    `### 6.6 Phase 28 armor outcomes and the Phase 29 loot card` — the four armor outcomes (soaked-with-wear, soaked-without-wear, magic-plate soak, armor gives out) are existing event shapes with TOAST_FOR/EVENT_NARRATION entries and fold into the round card through the same grouping path (no new event types, no engine work); the Phase 29 loot card is unchanged — the `noteCombat` → `window.__mzLootReport` hand-off stays and the card IS the batched pattern applied at encounter-clear; the "Last exchange" footnote (`S.lastExchange`) is superseded by the round card in the same slot.

    `### 6.7 Accessibility` — `aria-live="polite"` on a persistent wrapper element that survives `#enc-body`'s `innerHTML` rebuilds (live regions announce reliably only when the region node persists — RESEARCH Pitfall 1; the `#mw-toast-host` region today is `aria-live="polite" aria-atomic="false"`); reduced motion: the guards are `Date.now()` checks independent of any transition (cross-reference 6.3); any new tappable element carries `touch-action:manipulation` like `.mw-toast`/`.mw-chip` (Pitfall 3); buttons keep the ≥48dp min-height and the `.actions` gap (Material 48×48dp / 8dp separation, CITED in §2); reading speed 12–20 CPS comfortable, 20–25 CPS upper bound (ASSUMED, A5) informs whether the block needs a line cap — RESEARCH Open Question 1: Phase 32 measures a worst-case round (4 foes, frenzied, abilities) against the harness before fixing a cap, keeping enemyRound's 3+-collapse discipline either way. Haptics (`@capacitor/haptics`, already present) may be named as an enhancer here in one sentence, never as a decision factor.

    `### 6.8 Tests Phase 32 will re-pin` — a table `Test file | Tests | Pins today | Round Card impact | Bundled Toast impact` with the five files (test/unit/narrativeToasts.test.js 15; test/unit/shell-toast-wiring.test.js 28; test/unit/feedback-payload.test.js 38; test/unit/oracleLogViewModel.test.js 8; test/unit/shell-oracle-panel.test.js 9; total 98) — Oracle tests unchanged under both designs; toast-wiring and feedback-payload tests re-pinned for routing (Round Card) or bundling count (Bundled Toast); then the 25-05 toastsCoverage/toastTable partition invariants that must keep passing; then the new tests Phase 32 owes: a `renderEncounter` round-card region source-assertion test in the shell-loot-screen.test.js style, guard-constant assertions (both constants present, both compared against `Date.now()`, neither referencing `transitionend`/animation), a routing test that asserts no in-combat event type maps to both destinations; the npm test baseline 1593/1593 that Phase 32 grows from.

    `## 7. Ratification` — the first body line exactly `**Ratified design:** _pending user pick_`. Then a short "How this is ratified" paragraph: the orchestrator presents §4 and §5 with the §3.2 totals in one AskUserQuestion; the user picks or redirects; if redirected, §4 is amended to the chosen design before the PROJECT.md row is written; the row is written immediately after the pick, inside Phase 30, so Phase 32 is planned against a recorded decision. Then `**Key Decision row drafts (orchestrator copies ONE after the pick):**` and a `| Decision | Rationale | Outcome |` table with two rows — one for the Round Card (decision: combat round narrative = one always-visible Round Card in the encounter panel + ARM_DELAY_MS/DISMISS_SETTLE_MS 250 ms guards; rationale: 4.75/5 on the fixed scorecard, generalizes the shipped Phase 29 loot card, reuses toastsForAction's groupers; outcome: Phase 32 builds from §6), one for the Guarded Bundled Toast (decision/rationale/outcome in the same shape, honest about why it would be chosen: minimal change, less test churn). Label both rows clearly as drafts.

    `## Appendix A — Sources` — three lists: Primary (HIGH — direct code inspection: the file paths and functions read, npm test 1593/1593); Secondary (MEDIUM — WebSearch, official pages): the eleven https URLs from RESEARCH's Secondary list as bullets in the form `- https://… — <title>` (one URL per bullet, URL first, then a space); Tertiary (LOW — not independently re-verified): the ASSUMED conventions A1–A5. `## Appendix B — Assumptions log` — the A1–A5 table (`# | Claim | Section | Risk if wrong`) from RESEARCH, plus a one-line confidence statement (recommendation rests on HIGH-confidence code-read evidence; the shipped-game examples are illustrative).

    Self-review after the Edit: run every command in this task's acceptance criteria; fix any failure in the doc (never by weakening a gate). Re-read §4.2 and §5.2 and confirm the four labels are spelled identically in both; confirm no https URL exists outside the CITED tags and Appendix A bullets; confirm the only line containing `Ratified design:` is the pending line.
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && D=docs/COMBAT-NARRATIVE-DESIGN.md && test "$(grep -o '^## [1-7]\.' $D | tr -dc '0-9' | tr -d '\n')" = "1234567" && test "$(grep -cF '**Ratified design:** _pending user pick_' $D)" = "1" && test "$(grep -c 'Ratified design:' $D)" = "1" && for h in '### 6.1 Screen anatomy' '### 6.2 Tap budget per state' '### 6.3 Guard rules' '### 6.4 Toast-vs-round-surface routing' '### 6.5 Oracle behaviour' '### 6.6 Phase 28 armor outcomes and the Phase 29 loot card' '### 6.7 Accessibility' '### 6.8 Tests Phase 32 will re-pin' '## Appendix A — Sources' '## Appendix B — Assumptions log'; do test "$(grep -cF "$h" $D)" = "1" || { echo "MISSING: $h"; exit 1; }; done && for s in 'State: mid-round (Round Card)' 'State: Fight! gate (ambush preview)' 'State: encounter cleared → loot card' 'State: joiner offer' 'State: death'; do grep -qF "$s" $D || { echo "MISSING: $s"; exit 1; }; done && test "$(awk '/^### 6\.1 /{f=1} /^### 6\.2 /{f=0} f' $D | grep -c '^```')" -ge 10 && grep -qF 'ARM_DELAY_MS = 250' $D && grep -qF 'DISMISS_SETTLE_MS = 250' $D && grep -qF 'Date.now()' $D && grep -qF 'prefers-reduced-motion' $D && grep -qF 'aria-live' $D && grep -qF 'touch-action' $D && for t in armorSoaked foeKilled strikeRefused floorChanged CARD_EVENTS PRIORITY.block; do awk '/^### 6\.4 /{f=1} /^### 6\.5 /{f=0} f' $D | grep -qF "$t" || { echo "MISSING in 6.4: $t"; exit 1; }; done && for l in 'Tap budget' 'Oracle completeness' 'No double-reporting' 'Button guards'; do awk '/^## 4\. /{f=1} /^## 5\. /{f=0} f' $D | grep -qF "$l" || { echo "MISSING in §4: $l"; exit 1; }; awk '/^## 5\. /{f=1} /^## 6\. /{f=0} f' $D | grep -qF "$l" || { echo "MISSING in §5: $l"; exit 1; }; done && test -z "$(for u in $(grep -o 'https://[^] ]*' $D | sort -u); do grep -qF -- "$u" .planning/phases/30-combat-narrative-input-research/30-RESEARCH.md || echo "INVENTED: $u"; done)" && test "$(grep -o 'https://[^] ]*' $D | sort -u | wc -l)" -ge 10 && echo TASK2-OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -o '^## [1-7]\.' docs/COMBAT-NARRATIVE-DESIGN.md | tr -dc '0-9' | tr -d '\n'` prints `1234567` (all seven locked sections, in order, once each)
    - Each of these headings occurs exactly once (`grep -cF`): `### 6.1 Screen anatomy`, `### 6.2 Tap budget per state`, `### 6.3 Guard rules`, `### 6.4 Toast-vs-round-surface routing`, `### 6.5 Oracle behaviour`, `### 6.6 Phase 28 armor outcomes and the Phase 29 loot card`, `### 6.7 Accessibility`, `### 6.8 Tests Phase 32 will re-pin`, `## 7. Ratification`, `## Appendix A — Sources`, `## Appendix B — Assumptions log`
    - Wireframes per state: `grep -cF` >= 1 for each of `State: mid-round (Round Card)`, `State: Fight! gate (ambush preview)`, `State: encounter cleared → loot card`, `State: joiner offer`, `State: death`; and `awk '/^### 6\.1 /{f=1} /^### 6\.2 /{f=0} f' … | grep -c '^```'` >= 10 (five fenced frames)
    - Tap budget rows: `awk '/^### 6\.2 /{f=1} /^### 6\.3 /{f=0} f' … | grep -c '^| '` >= 8 and that slice contains `Mid-round → next round`, `Fight! gate`, `Encounter cleared → loot resolved → move on`, `Joiner offer`, `Death`, `Floor change / level-up`
    - Guard numbers: `grep -cF 'ARM_DELAY_MS = 250' …` >= 1; `grep -cF 'DISMISS_SETTLE_MS = 250' …` >= 1; `grep -cF 'Date.now()' …` >= 2; `grep -cF 'prefers-reduced-motion' …` >= 1; `grep -cF 'TAP_MAX_DURATION_MS' …` >= 1; `awk '/^### 6\.3 /{f=1} /^### 6\.4 /{f=0} f' … | grep -c 'coordinate collision'` >= 1 (the guards are designed against the real mechanism)
    - Routing table tokens in §6.4 (`awk '/^### 6\.4 /{f=1} /^### 6\.5 /{f=0} f' … | grep -cF <token>` >= 1 each): `armorSoaked`, `foeKilled`, `strikeRefused`, `floorChanged`, `CARD_EVENTS`, `PRIORITY.block`, `toastsForAction`; and `awk … | grep -c '^| '` >= 12 (header + separator + ten or more families)
    - `awk '/^### 6\.5 /{f=1} /^### 6\.6 /{f=0} f' … | grep -c '\b218\b'` >= 1 and the same slice contains `__mzOracleToNewest`
    - `awk '/^### 6\.6 /{f=1} /^### 6\.7 /{f=0} f' … | grep -cF '__mzLootReport'` >= 1 and the same slice contains `S.lastExchange`
    - `awk '/^### 6\.7 /{f=1} /^### 6\.8 /{f=0} f' … | grep -cF 'aria-live'` >= 1 and the same slice contains `touch-action`, `48dp`, `Date.now()`
    - `awk '/^### 6\.8 /{f=1} /^## 7\. /{f=0} f' …` contains all five filenames `narrativeToasts.test.js`, `shell-toast-wiring.test.js`, `feedback-payload.test.js`, `oracleLogViewModel.test.js`, `shell-oracle-panel.test.js`, plus `\b98\b`, `toastsCoverage`, and `1593`
    - Ratification: `grep -cF '**Ratified design:** _pending user pick_' …` == 1 and `grep -c 'Ratified design:' …` == 1 (the pending line is the ONLY ratified-design statement); `awk '/^## 7\. /{f=1} /^## Appendix A/{f=0} f' … | grep -cF '| Decision | Rationale | Outcome |'` == 1 and that slice has >= 2 rows beginning `| ` after the separator (two drafts); `grep -cF '## 4. Recommended design' …` == 1
    - Appendix A: `awk '/^## Appendix A/{f=1} /^## Appendix B/{f=0} f' … | grep -c '^- https://'` >= 10
    - Appendix B: `awk '/^## Appendix B/{f=1} f' … | grep -c '^| A[1-5] '` == 5
    - Both compliance tables still intact: for each of `Tap budget`, `Oracle completeness`, `No double-reporting`, `Button guards`, `awk '/^## 4\. /{f=1} /^## 5\. /{f=0} f' … | grep -c '<label>'` >= 1 AND `awk '/^## 5\. /{f=1} /^## 6\. /{f=0} f' … | grep -c '<label>'` >= 1
    - Provenance re-run: the INVENTED loop from Task 1 prints nothing over the finished doc; `grep -o 'https://[^] ]*' … | sort -u | wc -l` >= 10; CITED tag count equals CITED-with-https count
    - Scorecard recompute (Task 1's node gate) still prints `scorecard OK` on the finished doc
    - `grep -cF 'Delve, Die, Repeat' …` >= 1 and `grep -v '^<!--' docs/COMBAT-NARRATIVE-DESIGN.md | grep -c 'Mazeworld'` == 0
    - `git status --porcelain -- engine src content mazeworld.html test` prints nothing
  </acceptance_criteria>
  <done>The doc is complete: §6 gives Phase 32 a per-state wireframe, a tap budget with the 1-tap invariant, two 250 ms Date.now() guards plus the hit-zone and dismissal rules designed against the real mis-tap mechanism, a no-double-reporting routing table, unchanged Oracle behaviour, the Phase 28/29 fold-in, accessibility rules, and the 98-test re-pin list; §7 holds the pending ratification line and two Key Decision row drafts; appendices carry provenance-checked sources and the assumptions log; every self-review gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite and no-code-diff gate, final review pass, commit</name>
  <files>docs/COMBAT-NARRATIVE-DESIGN.md</files>
  <read_first>
    - docs/COMBAT-NARRATIVE-DESIGN.md in full — one uninterrupted read for voice, consistency (the same numbers in §1, §3, §6.3, §6.8), and any accidental scope-reducing or decision-claiming language
    - This plan's `must_haves.truths` and `must_haves.prohibitions` (the checklist for the read)
    - `git log --oneline -5` and `git status --porcelain` — the phase started at dfc2c61 (`docs(30): combat narrative & input research`); only `docs/COMBAT-NARRATIVE-DESIGN.md` and `.planning/**` may differ from it
  </read_first>
  <action>
    Run the code-integrity gates first: `test -z "$(git diff --stat dfc2c61..HEAD -- engine src content mazeworld.html test)"` and `test -z "$(git status --porcelain -- engine src content mazeworld.html test tools package.json package-lock.json)"` must both hold — if either fails, the executor has strayed; revert those paths (`git checkout -- <path>` for tracked, delete untracked) rather than committing them, because this phase's contract is documentation only.

    Run `npm test` and confirm the summary reports `# tests 1593`, `# pass 1593`, `# fail 0` — identical to the Phase 29 close and the RESEARCH baseline, which proves no test file or code path moved.

    Read the whole doc once, then run the complete gate set from Tasks 1 and 2 (section order 1234567, weights sum, node scorecard recompute, six survey rows, four constraint labels in §4 and §5, five wireframe states, guard constants, routing tokens, ratification line count, URL provenance loop, product-name checks). Fix any drift in the doc only. While reading, remove any phrasing that (a) implies the design is already chosen (the only permitted ratified-design statement is §7's pending line), (b) proposes trimming the Oracle, or (c) defers a locked constraint to a later phase — the four hard constraints are met by the design in §4/§5, not scheduled for later.

    Commit the doc (and nothing else): `git add docs/COMBAT-NARRATIVE-DESIGN.md` then commit with the message `docs(30-01): combat narrative & input design — survey, scorecard, Phase 32 build contract` followed by the two trailers the run uses (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`). If Tasks 1 and 2 already committed their halves, this commit carries only the review-pass fixes (or is skipped if the tree is clean — state which in the SUMMARY). Do NOT edit PROJECT.md, ROADMAP.md, or STATE.md — the Key Decision row and the plan-list tick are the orchestrator's steps after the user's pick.

    Write `.planning/phases/30-combat-narrative-input-research/30-01-SUMMARY.md` per the summary template, including: the §3.2 totals in a one-line table (so the orchestrator can paste them into the AskUserQuestion), the two candidate names exactly as the doc's §4/§5 headings spell them, the note that ratification and the PROJECT.md row are pending the orchestrator's pause, and the confirmation lines from the three gates (1593/1593; empty code diff; gate set green).
  </action>
  <verify>
    <automated>cd /c/projects/mazeworld && test -z "$(git diff --stat dfc2c61..HEAD -- engine src content mazeworld.html test)" && test -z "$(git status --porcelain -- engine src content mazeworld.html test)" && npm test 2>&1 | grep -E '^# (pass|fail) ' | tr '\n' ' ' | grep -q '# pass 1593 # fail 0' && D=docs/COMBAT-NARRATIVE-DESIGN.md && test "$(grep -o '^## [1-7]\.' $D | tr -dc '0-9' | tr -d '\n')" = "1234567" && test "$(grep -c 'Ratified design:' $D)" = "1" && grep -qF '**Ratified design:** _pending user pick_' $D && grep -qF '**Weights sum:** 20 + 20 + 20 + 10 + 10 + 15 + 5 = 100' $D && test -z "$(for u in $(grep -o 'https://[^] ]*' $D | sort -u); do grep -qF -- "$u" .planning/phases/30-combat-narrative-input-research/30-RESEARCH.md || echo "INVENTED: $u"; done)" && git log --oneline -3 | grep -q 'docs(30-01)' && echo TASK3-OK</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat dfc2c61..HEAD -- engine src content mazeworld.html test` prints nothing (no code, shell, content, or test change since the phase started)
    - `git status --porcelain -- engine src content mazeworld.html test tools package.json package-lock.json` prints nothing
    - `npm test` summary lines read `# tests 1593`, `# pass 1593`, `# fail 0`
    - `git log --oneline -3` shows a commit whose subject begins `docs(30-01):` and `git show --stat HEAD -- docs` lists `docs/COMBAT-NARRATIVE-DESIGN.md` (or the Task 1/2 commits already carry it and the tree is clean — recorded in the SUMMARY)
    - `git show --stat HEAD` lists no path under `engine/`, `src/`, `content/`, `test/`, and not `mazeworld.html`, `PROJECT.md`, `ROADMAP.md`, or `STATE.md`
    - The full Task 1 + Task 2 gate set passes on the committed doc: `grep -o '^## [1-7]\.' … | tr -dc '0-9' | tr -d '\n'` == `1234567`; `grep -c 'Ratified design:' …` == 1 with the pending line present; weights-sum line present; node scorecard recompute prints `scorecard OK`; six survey rows; four constraint labels in both §4 and §5; five `State:` labels; `ARM_DELAY_MS = 250` and `DISMISS_SETTLE_MS = 250` present; INVENTED loop prints nothing; `grep -v '^<!--' … | grep -c 'Mazeworld'` == 0
    - `.planning/phases/30-combat-narrative-input-research/30-01-SUMMARY.md` exists and contains the six §3.2 totals (3.35, 4.75, 2.85, 2.85, 3.15, 2.90), both candidate names, and the words `pending` and `orchestrator` in its ratification note
  </acceptance_criteria>
  <done>The suite is unchanged at 1593/1593, nothing outside `docs/` and `.planning/` differs from dfc2c61, the finished doc passes every locked-constraint gate, the doc is committed with the run's trailers, and the SUMMARY hands the orchestrator the scorecard totals and both candidate names for the ratification pause.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

| Artifact | Kind | Sections / contents | Owner |
|---|---|---|---|
| `docs/COMBAT-NARRATIVE-DESIGN.md` | design ledger (the ONLY deliverable) | `## 1. Current state — what the player sees today` (1.1 toast system · 1.2 renderEncounter and the encounter overlay · 1.3 Oracle log · 1.4 round event volume · 1.5 what actually mis-taps (code-read correction) · 1.6 tests pinning today's behaviour) · `## 2. Survey — six patterns against the criteria` (2.1 shipped examples per pattern · 2.2 survey table) · `## 3. Scorecard` (3.1 weights fixed before scoring, summing to 100 · 3.2 six-row score table, totals to two decimals) · `## 4. Recommended design — the Round Card` (4.1 concept + wireframe · 4.2 hard-constraint compliance · 4.3 tradeoffs against each alternative) · `## 5. Runner-up — the Guarded, Bundled Toast` (5.1 concept + wireframe · 5.2 hard-constraint compliance · 5.3 why second / when to pick it) · `## 6. Phase 32 build contract` (6.1 screen anatomy, five state wireframes · 6.2 tap budget per state · 6.3 guard rules ARM_DELAY_MS = 250 / DISMISS_SETTLE_MS = 250 / hit-zone / dismissal · 6.4 toast-vs-round-surface routing · 6.5 Oracle behaviour · 6.6 Phase 28/29 fold-in · 6.7 accessibility · 6.8 tests to re-pin) · `## 7. Ratification` (pending line + two Key Decision row drafts) · `## Appendix A — Sources` · `## Appendix B — Assumptions log` | ✔ 30-01 |
| `.planning/phases/30-combat-narrative-input-research/30-01-SUMMARY.md` | execution summary | scorecard totals, both candidate names, gate confirmations, ratification-pending note | ✔ 30-01 |
| PROJECT.md Key Decision row; §7 `Ratified design` line filled; ROADMAP plan tick | orchestrator steps after the user's pick | — | orchestrator (NOT this plan) |
</artifacts>

<assumption_delta_decision>
- **Signal:** `assumption-delta scan 30` detected one `chosen` signal — ROADMAP's Phase 30 text "a recommended design … is chosen".
- **Noun:** combat round feedback surface
- **Decision:** `no-change`
- **Rationale:** this is a UI design choice for one presentation surface (where a round's narrative renders and how its buttons are guarded), not a data-model or identity generalization. No primary key, serialized shape, `S`-state field, event type, or `*Comparable()` carve-out changes; the engine is untouched by design (paper-only phase, and Phase 32 is `src/browser/*`/`mazeworld.html` only). No invariant test is warranted by this phase; Phase 32 owes the routing/guard tests §6.8 lists.
</assumption_delta_decision>

<verification>
- Section order gate: `grep -o '^## [1-7]\.' docs/COMBAT-NARRATIVE-DESIGN.md | tr -dc '0-9' | tr -d '\n'` == `1234567` — the CONTEXT-locked order (current state → survey → scorecard → recommendation → runner-up → build contract → ratification).
- Boundary probe: six survey rows (`### 2.2` slice) and six score rows with nine cells each (node recompute gate) — the baseline row is scored, no empty cells.
- Precision probe: `**Weights sum:** 20 + 20 + 20 + 10 + 10 + 15 + 5 = 100` present before `### 3.2 Scores`; every weighted total recomputes within ±0.005 (node gate prints `scorecard OK`).
- Four hard constraints by name in BOTH §4 and §5: `Tap budget`, `Oracle completeness`, `No double-reporting`, `Button guards`.
- Code-read correction stated in §1.5 AND §6.3 (`coordinate collision` in both slices); guards are `Date.now()` checks with `prefers-reduced-motion` named.
- Five per-state wireframes (`State:` labels) in §6.1; both designs' wireframes in §4 and §5.
- Provenance: every `https://` URL in the doc appears verbatim in 30-RESEARCH.md (loop prints nothing); every CITED tag carries one https URL; ASSUMED examples carry none.
- Ratification: exactly one `Ratified design:` line and it reads `_pending user pick_`.
- No code touched: `npm test` 1593/1593; `git diff --stat dfc2c61..HEAD -- engine src content mazeworld.html test` empty.
- The ratification pause, the PROJECT.md Key Decision row, and the ROADMAP plan tick are performed by the ORCHESTRATOR after this plan completes — no `<human_verification>` block is needed inside the plan.
</verification>

<success_criteria>
- SC1: `docs/COMBAT-NARRATIVE-DESIGN.md` compares all six patterns (the five CMBUI-01 names + today's toast stack) with tagged shipped examples and a survey table, in the locked section order.
- SC2: §4 recommends the Round Card with tradeoffs against each alternative; §5 fully specifies the runner-up; both satisfy the four hard constraints by name; the scorecard's weights are fixed and justified before six rows of reproducible totals.
- SC3 (hand-off): §7 gives the orchestrator the pending line and two Key Decision row drafts; the orchestrator's pause and PROJECT.md row happen after this plan, before Phase 32 is planned.
- Phase 32 spec: §6 carries per-state wireframes, the 1-tap budget, ARM_DELAY_MS/DISMISS_SETTLE_MS = 250 as Date.now() checks, hit-zone and dismissal rules against the real mis-tap mechanism, a no-double-reporting routing table, unchanged Oracle behaviour, Phase 28/29 fold-in, accessibility, and the 98-test re-pin list.
- `npm test` 1593/1593; no diff outside `docs/` and `.planning/`.
</success_criteria>

<output>
Create `.planning/phases/30-combat-narrative-input-research/30-01-SUMMARY.md` when done — include the §3.2 totals table, both candidate design names as §4/§5 spell them, the three gate confirmations, and the line that ratification + the PROJECT.md Key Decision row are pending the orchestrator's pause.
</output>
