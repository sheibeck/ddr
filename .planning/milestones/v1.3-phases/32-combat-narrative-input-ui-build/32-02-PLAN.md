---
phase: 32-combat-narrative-input-ui-build
plan: 02
type: execute
wave: 2
depends_on: ["32-01"]
files_modified:
  - mazeworld.html
  - src/browser/toasts.js
  - test/unit/shell-toast-wiring.test.js
  - test/unit/shell-round-card.test.js
autonomous: true
requirements: [CMBUI-02, CMBUI-03]

must_haves:
  truths:
    - "CMBUI-02: while the post-dispatch state has a combat, every toast toastsForAction returns for that action — EXCEPT PRIORITY.block refusals — becomes one line of ONE Round Card rendered inside #enc-body below the foe roster and above the Fight!/action bar; no in-combat narrative toast reaches #mw-toast-host; refusals still toast; the two hand-written 'Already at full health.' block toasts are untouched; the Oracle still receives every html line (the logLine loops are untouched) — the card is a VIEW of the same events, never a second writer into S.log"
    - "The ONE routing seam is dispatchWithToasts: a single if/else inside its toastsForAction loop (card line vs window.mzToast), so an event can never be both toasted and carded (structural exclusivity, pinned by test)"
    - "Pre-Fight! preview (combat.pending): the card shows the folded encounterStarted line under the preview header; after Fight! the card shows round 1 = the fight action's folded events (combatJoined initiative line, any pre-emptive strike, the Afraid line) under 'Round 1 · how that went'; each later action that yields ≥1 card line replaces the card with that action's lines under the round the player acted in"
    - "The card persists across Spells/Items re-renders and refusal taps (zero card lines → previous card kept) and is cleared when the post-dispatch state has no combat (endCombat, flee, death, parley-out) — the state that ends combat still toasts its own lines (fled/kill/parley) so the player never loses feedback when the panel hides or swaps to the loot card"
    - "The card is UNCAPPED (CONTEXT Area 1 #5: no hard cap, inner scroll; §6.4: every in-combat event has exactly ONE destination — a fifth folded line must not vanish): lines = the FULL deduped, priority-sorted toastsForAction output for the action, requested with the new optional `{ limit: Infinity }` argument and passed through narrativeToastText — same order, same text, groupers/dedupe/sort untouched; NO card-side cap of any kind (the old slice(-6) contract is gone); .round-card has max-height:40% with overflow-y:auto so the action bar's coordinates do not move between rounds; touch-action:manipulation on the block"
    - "The ONLY src/browser/toasts.js change is presentation-tier: toastsForAction(type, events, ctx = {}, opts = {}) reads `const { limit = MAX_TOASTS } = opts || {}` and slices to `limit` instead of the literal MAX_TOASTS — the default call is byte-for-byte the same output, the toast host path in dispatchWithToasts still caps at MAX_TOASTS, groupers/dedupeByType/priority-sort/MAX_TOASTS/TOAST_FOR/ORACLE_ONLY/FEATURE_EVENTS are untouched; narrativeToasts, feedback-payload, toastsForAction, toastTable and toastsCoverage test files stay untouched on disk and green"
    - "Storage: the card is window.__mzRoundCard = { seq, pending, round, lines } — module-level presentation state like window.__mzLootReport; never a field on S (serializeRun spreads the whole state, so an S field WOULD be saved), never written to S.log"
    - "S.lastExchange / S.exchangeN are gone from the file (zero non-comment occurrences), with the old 'Last exchange' block, its .exchange CSS and its flash keyframes"
    - "Accessibility (RESEARCH Pitfall 1): #enc-round-live is a PERSISTENT sr-only aria-live='polite' aria-atomic='true' sibling of #enc-body inside #enc-panel (static HTML, never rebuilt); it is updated by textContent only when the card's seq changes (a NEW card), so sub-menu re-renders never re-announce; the visible block is built with textContent, never innerHTML interpolation"
    - "CMBUI-03: no dismiss gate is added anywhere — the card has no tap-to-dismiss and no surface gains a tap-anywhere-to-dismiss; every existing 1-tap decision surface (loot, joiner, find, death, Move on) is untouched"
    - "Tests: shell-toast-wiring re-pinned for routing (still ≥ 21), new shell-round-card test (round-card source region, routing exclusivity, S-field removal, voice scan of the new header copy), narrativeToasts/oracleLogViewModel/shell-oracle-panel/toastsCoverage/toastTable unchanged and green; npm test grows; engine/content/test/parity diff empty"
  artifacts:
    - "src/browser/toasts.js: optional 4th `opts = {}` argument on toastsForAction with `limit` defaulting to MAX_TOASTS (pipeline tail only)"
    - "mazeworld.html: import { PRIORITY, MAX_TOASTS, narrativeToastText } line; dispatchWithToasts routing branch (uncapped card list, MAX_TOASTS-capped toast queue) + window.__mzRoundCard; ROUND_CARD_COPY; .round-card CSS; #enc-round-live static region; renderEncounter round-card region (after the foe roster, before the C.pending gate); syncRoundCardLive(); the old exchange block/CSS/S-fields deleted"
    - "test/unit/shell-round-card.test.js (new); test/unit/shell-toast-wiring.test.js (routing re-pin additions)"
  key_links:
    - "button tap → window.mz* → engineCombatAction/inventoryAction/window.move → dispatchWithToasts → toastsForAction → (inCombat && !block) window.__mzRoundCard.lines | else window.mzToast → renderEncounter reads window.__mzRoundCard → .round-card + #enc-round-live"
    - "post-dispatch result.state.combat === null → window.__mzRoundCard = null (cleared at endCombat) → renderEncounter's loot/beats/death branches render without a card"
---

<objective>
Build the Round Card (design §4, build contract §6): retarget `dispatchWithToasts`'s in-combat output — the FULL folded list, uncapped via a new optional `limit` argument on `toastsForAction` (groupers untouched; the toast host keeps `MAX_TOASTS`) — from the toast host to one always-visible narrative block inside the encounter panel — below the foe roster, above the Fight!/action bar — replacing the `S.lastExchange` "Last exchange" footnote and its S-fields entirely. Refusals stay toasts; out-of-combat toasts are untouched; the Oracle is untouched. Add a persistent `aria-live` announcer that survives `#enc-body` rebuilds, pin the routing exclusivity and the new region with source-assertion tests, and re-pin `shell-toast-wiring` for routing.

Purpose: CMBUI-02 (one coherent place for a round's narrative, Oracle complete) and CMBUI-03 (no new dismiss gate — the card is read at zero taps and never dismissed). This is the player-visible half of the phase; 32-03 adds the guards on top of this DOM.
Output: `mazeworld.html` edits (module seam, classic renderEncounter region, CSS, static HTML), `test/unit/shell-round-card.test.js`, `shell-toast-wiring` re-pins, `npm test` `# fail 0`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md

Commit trailers (every commit in this phase, no exceptions — task commits and the metadata commit alike): end every commit message with these two lines, in this order:

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS

Hard scope rule for the whole phase: shell/presentation/tests only. Do not edit anything under `engine/`, `content/`, or `test/parity/` (including `test/parity/prototype-master.js.txt`); `src/browser/toasts.js`'s groupers, dedupeByType, priority sort and MAX_TOASTS are reused unmodified — the ONE permitted toasts.js edit is the optional `limit` argument on toastsForAction's pipeline tail (Task 1), and the card gets NO cap of its own. UAT is deferred to the end of the autonomous run — never pause for a device check; record Pixel 7 items in the SUMMARY instead.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-combat-narrative-input-ui-build/32-CONTEXT.md
@.planning/phases/32-combat-narrative-input-ui-build/32-RESEARCH.md
@.planning/phases/31-combat-start-gating-effect-hygiene/31-03-SUMMARY.md
@docs/COMBAT-NARRATIVE-DESIGN.md
@src/browser/toasts.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Give toastsForAction an optional limit; route in-combat toasts UNCAPPED to window.__mzRoundCard inside dispatchWithToasts; delete the S.lastExchange/S.exchangeN pair; re-pin shell-toast-wiring for routing</name>
  <files>src/browser/toasts.js, mazeworld.html, test/unit/shell-toast-wiring.test.js</files>
  <read_first>
    - mazeworld.html ~L6110-6115 (dispatchWithToasts — the exact seam, verbatim in 32-RESEARCH "Code Examples"), ~L6029-6073 (noteCombat: read only, no change needed — the card is cleared from the post-dispatch state, see step 3), ~L6117-6124 (engineMove samples wasCombat BEFORE calling dispatchWithToasts — the same pre/post pattern), ~L5638-5643 (the two existing toasts.js import lines — pinned byte-for-byte by shell-toast-wiring; add a NEW third line, never edit these), ~L2211 (window.__mzState = { get, set } — the pre-dispatch state read), ~L6043 (window.__mzLootReport — the presentation-only-transient precedent the card copies)
    - mazeworld.html ~L2660-2685 (the dead classic act(): the S-field writes in its combat branch), ~L3910 (classic startCombat's reset line), ~L4466 (classic endCombat's reset line) — the three classic S-field sites
    - src/browser/toasts.js L47-50 (PRIORITY, block = 0; MAX_TOASTS = 4 — "the host's visible cap"), L139-145 (narrativeToastText), L826-858 (toastsForAction: groupers → direct TOAST_FOR builders → dedupeByType → priority sort → `.slice(0, MAX_TOASTS)` → map to { text, tone, priority } — the slice is the ONLY line this plan changes; the card consumes the UNSLICED list, the toast host keeps the cap)
    - engine/engine.js L34-46 (applyAction structuredClones the state — so the pre-dispatch object read from window.__mzState.get() keeps its pre-action combat.round; read only)
    - engine/combat.js L226 (startCombat: round 1 + pending), L1155 (afterPlayerAction's round++ runs at the END of the player's action, so the post-dispatch round is already the NEXT round — this is why the card labels the round sampled BEFORE dispatch)
    - test/unit/shell-toast-wiring.test.js L150-215 (the existing dispatchWithToasts/import pins that must keep passing)
    - 32-RESEARCH.md Pitfall 2 (routing predicate) and Pitfall 3 (the two hand-written full-health toasts bypass the pipeline — leave them alone)
  </read_first>
  <action>
    <!-- planner-discipline-allow: lastExchange -->
    <!-- planner-discipline-allow: exchangeN -->
    <!-- planner-discipline-allow: S.roundCard -->
    <!-- planner-discipline-allow: state.roundCard -->
    <!-- planner-discipline-allow: slice(0, MAX_TOASTS) -->
    <!-- planner-discipline-allow: deduped.slice(0, limit) -->
    0. toasts.js limit option (presentation tier, groupers untouched — CONTEXT Area 1 #5 "no hard cap"): change the signature to `export function toastsForAction(type, events, ctx = {}, opts = {})`, add `const { limit = MAX_TOASTS } = opts || {};` at the top of the function (after the empty-events early return is fine), and change the final `deduped.slice(0, MAX_TOASTS)` to `deduped.slice(0, limit)`. Nothing else in the file moves: encounterStart/enemyRound/yourRound/spellChain/fleeChain/parleyChain/chestChain/killFold, dedupeByType, the priority sort, MAX_TOASTS, TOAST_FOR, ORACLE_ONLY, FEATURE_EVENTS, CARD_EVENTS, NARRATIVE_ACTIONS stay byte-identical. Extend the function's doc comment: Phase 32 (CMBUI-02) — `opts.limit` (default MAX_TOASTS, the toast host's visible cap) lets the Round Card request the FULL folded list with `{ limit: Infinity }` because every in-combat event has exactly one presentation destination (design §6.4) and a fifth folded line must not vanish; the default call is unchanged. Run `node --test test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/narrativeToasts.test.js` — all green, none of those files edited (the purity scan still passes: no clock/DOM/engine import was added).
    1. Imports: below the existing `import { CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime } from "./src/browser/toasts.js";` line add a SEPARATE line `import { PRIORITY, MAX_TOASTS, narrativeToastText } from "./src/browser/toasts.js";` with a one-line comment (Phase 32, CMBUI-02: PRIORITY.block keeps refusals as toasts; MAX_TOASTS caps only the toast queue; narrativeToastText is the card's defensive text prep). Never modify the two existing toasts.js import lines (shell-toast-wiring pins them byte-for-byte).
    2. Routing (the ONE seam, design §6.4 / CONTEXT Area 3): rewrite dispatchWithToasts so that it (a) reads `const before = window.__mzState.get();` and `const roundBefore = before && before.combat ? before.combat.round : null;` BEFORE calling dispatch (applyAction clones, so `before` stays pre-action); (b) runs `const result = dispatch(action);` and builds `ctx` exactly as today (keep the literal `NARRATIVE_ACTIONS.has(action.type) ? { narrate: narrateEvent } : {}` expression — it is pinned); (c) computes `const inCombat = !!(result.state && result.state.combat);` — the POST-dispatch combat (CONTEXT: "while state.combat is non-null"); this deliberately differs from RESEARCH assumption A2 (pre-dispatch wasCombat) because the action that ENDS combat must still toast its own lines: a successful flee hides the panel, a kill swaps to the loot card, a death shows the death card — a card-routed line there would be invisible; the encounter-starting move, by contrast, has a post-dispatch combat and so its encounterStart fold lands on the preview card as §6.4 requires; (d) requests the UNCAPPED folded list — `for (const t of toastsForAction(action.type, result.events, ctx, { limit: Infinity }))` — and partitions it with ONE if/else: `if (inCombat && t.priority !== PRIORITY.block)` push `narrativeToastText(t.text) || t.text` onto a local `lines` array, `else` push `t` onto a local `queue` array; then raise the toast queue with the host's own cap, `for (const t of queue.slice(0, MAX_TOASTS)) window.mzToast?.(t.text, t.tone);` — this is the only mzToast call inside the function, so an item is card OR toast, never both, the card never loses a folded line (CONTEXT Area 1 #5, §6.4 one-destination invariant), and out of combat the queue is exactly today's toastsForAction output (same order, same MAX_TOASTS cap — Phase 25/25.1 rules stand); (e) after the loop: if `!inCombat` set `window.__mzRoundCard = null` (cleared at endCombat — flee/kill/death/parley all pass through here, so noteCombat needs no change); else if `lines.length` set `window.__mzRoundCard = { seq: ++roundCardSeq, pending: !!result.state.combat.pending, round: roundBefore ?? result.state.combat.round, lines }` where `let roundCardSeq = 0;` is a module-level counter declared beside the function; else if `roundBefore === null` (a combat that just started but produced no card line — defensive) set it to null; otherwise leave the previous card in place (a refusal tap or a zero-line action keeps last round's card, CONTEXT Area 1); (f) return result unchanged. Rewrite the function's doc comment: keep the Phase 25/25.1 paragraphs, append a Phase 32 paragraph naming the rule (in combat, card; refusals and everything out of combat, toast; the card is cleared the moment the post-dispatch state has no combat; the round label is the round the player ACTED in — afterPlayerAction's round++ has already advanced combat.round by the time the render runs, so the topbar's "round N+1" and the card's "Round N" are both truthful) and stating that window.__mzRoundCard is presentation-only and never stored on state (serializeRun spreads S, so an S field would be saved — CONTEXT: never serialized).
    3. Do NOT wrap or alter window.mzToast itself, and leave the two pre-dispatch `window.mzToast?.("Already at full health.", "block")` calls (a-potion onclick and the key-2 path) byte-identical — they never enter toastsForAction (RESEARCH Pitfall 3).
    4. Delete the S-field pair everywhere: in the dead classic act() drop the `S.lastExchange = null;` inside its combat-ended branch and the whole else branch that assigned `S.lastExchange`/`S.exchangeN` (the try/finally, the beats synthesis and the trailing renderEncounter() stay); in classic startCombat drop the trailing `S.lastExchange = null;` from its reset line; in classic endCombat drop its `S.lastExchange = null;` line. The renderEncounter block that READ them is removed in Task 2 (do it here if you prefer — Task 2 owns the replacement region either way). Afterwards `grep -v '^\s*//' mazeworld.html | grep -c 'lastExchange'` and the same for `exchangeN` must both print 0 (parity harness files still destructure the names OUT of comparables — those are under test/parity and stay untouched).
    5. Re-pin test/unit/shell-toast-wiring.test.js (routing, design §6.8): keep every existing test; ADD (a) a pin that the separate `import { PRIORITY, MAX_TOASTS, narrativeToastText } from "./src/browser/toasts.js";` line exists exactly once; (b) a dispatchWithToasts REGION pin (slice CODE from `function dispatchWithToasts(action)` to `window.move = function engineMove`): the region contains `const inCombat = !!(result.state && result.state.combat)`, exactly one `toastsForAction(action.type, result.events, ctx, { limit: Infinity })` (the card path is uncapped), exactly one `if (inCombat && t.priority !== PRIORITY.block)`, exactly one `queue.slice(0, MAX_TOASTS)` (the toast path keeps the host cap), exactly one `window.mzToast?.(t.text, t.tone)`, `narrativeToastText(t.text)`, and `window.__mzRoundCard = null`; (b2) a toasts.js pin: `export function toastsForAction(type, events, ctx = {}, opts = {})` and `deduped.slice(0, limit)` each occur once in src/browser/toasts.js and the literal `slice(0, MAX_TOASTS)` no longer does; (c) a pin that the two full-health block toasts are still exactly 2 (extend the existing test's message or add a sibling); (d) a pin that `window.mzToast = function` is still defined exactly once and is not reassigned anywhere else (`window.mzToast = ` occurs once) — routing lives in the pipeline, not in a wrapper. Update the file header comment's summary list with the Phase 32 routing bullet.
    6. Run `node --test test/unit/shell-toast-wiring.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/narrativeToasts.test.js` → `# fail 0`; `git status --porcelain test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/feedback-payload.test.js` empty. Commit: `feat(32-02): toastsForAction limit option; route in-combat toasts uncapped to the round card; drop the last-exchange S-fields`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-toast-wiring.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/narrativeToasts.test.js test/unit/shell-fight-gate.test.js 2>&1 | tail -5 && git status --porcelain test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/feedback-payload.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function toastsForAction(type, events, ctx = {}, opts = {})' src/browser/toasts.js` == 1 and `grep -c 'const { limit = MAX_TOASTS } = opts || {};' src/browser/toasts.js` == 1 and `grep -c 'deduped.slice(0, limit)' src/browser/toasts.js` == 1 and `grep -c 'slice(0, MAX_TOASTS)' src/browser/toasts.js` == 0
    - `git diff 9174e6e..HEAD -- src/browser/toasts.js | grep -c '^[-+]function \|^[-+]export const'` == 0 (no grouper, constant or set was edited — only the pipeline tail)
    - `grep -c 'import { PRIORITY, MAX_TOASTS, narrativeToastText } from "./src/browser/toasts.js";' mazeworld.html` == 1
    - `grep -c 'toastsForAction(action.type, result.events, ctx, { limit: Infinity })' mazeworld.html` == 1 and `grep -c 'queue.slice(0, MAX_TOASTS)' mazeworld.html` == 1
    - `grep -c 'import { toastsForAction } from "./src/browser/toasts.js";' mazeworld.html` == 1 and `grep -c 'import { CARD_EVENTS, NARRATIVE_ACTIONS, toastLifetime } from "./src/browser/toasts.js";' mazeworld.html` == 1 (untouched)
    - `grep -c 'if (inCombat && t.priority !== PRIORITY.block)' mazeworld.html` == 1
    - `grep -c 'window.mzToast?.(t.text, t.tone)' mazeworld.html` == 1
    - `grep -c 'window.__mzRoundCard = ' mazeworld.html` >= 2
    - `grep -c 'const roundBefore = before && before.combat ? before.combat.round : null;' mazeworld.html` == 1
    - `grep -c 'Already at full health.", "block")' mazeworld.html` == 2
    - `grep -c 'window.mzToast = ' mazeworld.html` == 1
    - `grep -v '^\s*//' mazeworld.html | grep -c 'lastExchange'` == 0 and `grep -v '^\s*//' mazeworld.html | grep -c 'exchangeN'` == 0
    - `grep -c 'S.roundCard' mazeworld.html` == 0 and `grep -c 'state.roundCard' mazeworld.html` == 0 (the card is never a state field)
    - `git status --porcelain test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/feedback-payload.test.js` prints nothing
    - node --test over the files in verify → `# fail 0` and shell-toast-wiring `# pass` >= 25
  </acceptance_criteria>
  <done>In combat, dispatchWithToasts feeds the card every folded non-refusal toast (uncapped) and toasts only refusals; out of combat the toast host output is byte-identical to today (MAX_TOASTS kept); the S-field pair no longer exists; the routing is pinned as a single if/else and the limit option is pinned in toasts.js.</done>
</task>

<task type="auto">
  <name>Task 2: Render the Round Card in renderEncounter (roster → card → Fight!/actions), add its CSS, the persistent aria-live announcer, and the header copy</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html ~L5171-5205 (renderEncounter's live-combat branch: `const list = document.createElement("div"); list.className = "foes"; … body.appendChild(list);` then the Phase 31 `if (C.pending)` Fight! gate — the card region goes BETWEEN these two), ~L5266-5272 (the old exchange block after the spell menu — delete it), ~L4869-4886 (renderEncounter's top: hasActiveEncounter early-return, body.innerHTML = "", the persistent #enc-round/#enc-dismiss-slot handling — the precedent for "persistent node, textContent rewritten")
    - mazeworld.html ~L1398-1409 (the static #enc-panel markup: .enc-topbar then #enc-body — the announcer is added after #enc-body), L786 (.sr-only utility already exists), L1342 (#mw-toast-host's aria-live/aria-atomic attributes), L1367 (#mw-flash, a persistent aria-live region)
    - mazeworld.html ~L598-605 (the .exchange rules + flash keyframes + its reduced-motion line — replace with .round-card rules), L563-567 (#enc-body flex column + .actions margin-top:auto — the card is a flex item of this column), L336-342 (.mw-overlay: position:absolute; inset:0 → definite height, so a percentage max-height on the card resolves), L448 (button already carries touch-action:manipulation globally), L657-659 (#enc-body p .roll{display:none} — stays; its comment mentions the old block: update the comment wording only)
    - mazeworld.html ~L2815-2830 (CONDITION_COPY — put ROUND_CARD_COPY right after it, same const-object style)
    - docs/COMBAT-NARRATIVE-DESIGN.md §6.1 mid-round wireframe (roster → THIS ROUND → action bar; "THIS ROUND" is a placeholder), §6.7 (aria-live on a persistent wrapper; touch-action:manipulation; max-height/line-cap measurement)
    - content/safety-wordlist.js (BANNED corpus the new copy must clear)
  </read_first>
  <action>
    <!-- planner-discipline-allow: Last exchange -->
    <!-- planner-discipline-allow: exflash -->
    <!-- planner-discipline-allow: .exchange{ -->
    <!-- planner-discipline-allow: className = "exchange" -->
    <!-- planner-discipline-allow: slice(-6) -->
    1. Copy (Claude's discretion, family-friendly sarcasm): after CONDITION_COPY add `const ROUND_CARD_COPY = { preview: "The stare-down", round: (n) => \`Round ${n} · how that went\` };` with a comment: Phase 32 (CMBUI-02) — the Round Card's header copy; `preview` heads the pre-Fight! card (nothing has rolled yet), `round(n)` heads a fought round where n is the round the player ACTED in (the topbar counter is already on the next round by render time). Both strings are player-facing; keep them clear of content/safety-wordlist.js (Task 3 scans them).
    2. Static HTML: inside `<section class="mw-overlay" id="enc-panel" hidden>`, directly after `<div id="enc-body"></div>`, add `<div class="sr-only" id="enc-round-live" aria-live="polite" aria-atomic="true"></div>` with an HTML comment: Phase 32 (CMBUI-02, design §6.7 / RESEARCH Pitfall 1) — the Round Card's screen-reader announcer. It PERSISTS (renderEncounter only ever rewrites its textContent, never rebuilds it) because a live region recreated inside #enc-body's innerHTML rebuild is a new node to assistive tech and is not announced reliably; the visible card lives in #enc-body for layout, this node carries the announcement. It sits AFTER #enc-body so browse order reads the visible card first.
    3. CSS: replace the `.exchange{…}`, `.exchange h4{…}`, `.exchange p{…}`, `.exchange p:last-child{…}`, the `exflash` keyframes and the reduced-motion `.exchange` line with the Round Card rules, headed by a comment (Phase 32 CMBUI-02/03: ONE always-visible narrative block between the foe roster and the action bar, replacing the last-exchange footnote; no line cap — a long round scrolls INSIDE the block so the action bar below never moves between rounds (§6.3 rule 3); no entrance animation on purpose — the guards and the block never depend on a transition, and reduced-motion users get the identical experience): `.round-card{flex:0 1 auto;min-height:0;max-height:40%;overflow-y:auto;margin:0 0 12px;padding:10px 11px;background:var(--paper);border:1px solid var(--rule);border-left:3px solid var(--ditto);touch-action:manipulation}` (the percentage resolves because .mw-overlay is absolutely positioned with inset:0 and #enc-body is its stretched flex child; keep `max-height:40%` literally — Task 3 pins it), `.round-card h4{font-family:var(--mono);font-size:9.5px;letter-spacing:.14em;text-transform:uppercase;color:var(--ink-soft);margin:0 0 6px;font-weight:400}`, `.round-card p{font-family:var(--body);font-size:15px;line-height:1.45;margin:0 0 6px;color:var(--ink)}`, `.round-card p:last-child{margin-bottom:0}`. Update the `#enc-body p .roll` comment so it no longer refers to the removed footnote by name (say "the Round Card" instead). Afterwards the file must contain no `.exchange{` rule, no `class="exchange"`/`className = "exchange"`, and no `exflash` keyframe.
    4. renderEncounter — the card region (CMBUI-02): immediately after `body.appendChild(list);` (the foe roster) and BEFORE the `if (C.pending) {` Fight! gate, add: `const rc = window.__mzRoundCard;` and, when `rc && Array.isArray(rc.lines) && rc.lines.length`, build `const card = document.createElement("section"); card.className = "round-card"; card.id = "round-card"; card.dataset.seq = String(rc.seq);` set `card.setAttribute("aria-label", "This round")`, append an `h4` whose textContent is `rc.pending ? ROUND_CARD_COPY.preview : ROUND_CARD_COPY.round(rc.round)`, then one `p` per line with `p.textContent = line` (textContent ONLY — toast text is plain prose by contract (T-25.1-01) and must never be template-interpolated into innerHTML), then `body.appendChild(card)`. Then call `syncRoundCardLive(rc)` (step 5) whether or not a card rendered. Comment the region: Phase 32 (CMBUI-02/03, design §4/§6.1) — the Round Card: the current round's folded lines (dispatchWithToasts → window.__mzRoundCard), rendered in the slot the last-exchange footnote used to occupy, above the Fight! gate / action bar; NO tap-to-dismiss and no dismiss gate (the next action's render replaces it; hasActiveEncounter() false clears it) — reading costs zero taps and moving on costs no extra tap (CMBUI-03); the foe roster stays first (RESEARCH Pitfall 5: the loot/joiner/find/death branches above are decision surfaces and intentionally carry no card). The pending preview thus reads: head → roster → card (preview header + the encounterStarted line) → Fight!.
    5. Announcer helper, defined next to hasActiveEncounter(): `let roundCardAnnouncedSeq = 0;` and `function syncRoundCardLive(rc)`: fetch `#enc-round-live`; if `!rc` (or no lines) → if its textContent is non-empty set it to "" and reset `roundCardAnnouncedSeq = 0`, return; if `rc.seq === roundCardAnnouncedSeq` return (a Spells/Items/target re-render must not re-announce the same card); otherwise set `roundCardAnnouncedSeq = rc.seq` and `live.textContent = header + ". " + rc.lines.join(" ")` using the same ROUND_CARD_COPY header. Also call `syncRoundCardLive(null)` in renderEncounter's `!hasActiveEncounter()` early-return branch (right where the panel is hidden) so the announcer empties when the overlay closes.
    6. Delete the old exchange block from renderEncounter (the `if (S.lastExchange …)` block after the spell menu that built the div with the "Last exchange" heading and sliced the last 6 lines) — the card above replaces it in the roster-adjacent slot; do NOT reintroduce any slice cap.
    7. Sanity in a browser is not possible in this run; instead run `node --test test/unit/shell-fight-gate.test.js test/unit/shell-loot-screen.test.js test/unit/shell-toast-wiring.test.js` (their renderEncounter region pins must still hold: `if (C.pending)` still lives between `function renderEncounter` and `function noteCombat`; the loot branch still precedes the joiner branch) and `npm run build:www` (exit 0). Commit: `feat(32-02): render the Round Card below the foe roster with a persistent aria-live announcer; retire the last-exchange block`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-fight-gate.test.js test/unit/shell-loot-screen.test.js test/unit/shell-toast-wiring.test.js 2>&1 | tail -5 && npm run build:www 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const ROUND_CARD_COPY = ' mazeworld.html` == 1 and `grep -c 'ROUND_CARD_COPY' mazeworld.html` >= 4
    - `grep -c 'id="enc-round-live" aria-live="polite" aria-atomic="true"' mazeworld.html` == 1 and the line number of `id="enc-round-live"` is greater than the line number of `<div id="enc-body"></div>` and less than the line number of the `</section>` that closes `#enc-panel`
    - `grep -c '^\.round-card{' mazeworld.html` == 1 and `grep '^\.round-card{' mazeworld.html | grep -c 'max-height:40%'` == 1 and `grep '^\.round-card{' mazeworld.html | grep -c 'overflow-y:auto'` == 1 and `grep '^\.round-card{' mazeworld.html | grep -c 'touch-action:manipulation'` == 1
    - `grep -c 'className = "round-card"' mazeworld.html` == 1 and `grep -c 'card.id = "round-card"' mazeworld.html` == 1
    - `grep -c 'function syncRoundCardLive(' mazeworld.html` == 1 and `grep -c 'syncRoundCardLive(' mazeworld.html` >= 3
    - the line number of `className = "round-card"` is greater than the line number of `list.className = "foes"` and less than the line number of `if (C.pending) {`
    - `grep -c '\.exchange{' mazeworld.html` == 0 and `grep -c 'exflash' mazeworld.html` == 0 and `grep -c 'className = "exchange"' mazeworld.html` == 0
    - `grep -v '^\s*//' mazeworld.html | grep -c 'Last exchange'` == 0
    - `grep -c 'slice(-6)' mazeworld.html` == 0
    - `grep -c 'round-card' mazeworld.html` >= 6 (CSS rules + class + id + comments)
    - `npm run build:www` exit code 0; node --test over the files in verify → `# fail 0`
  </acceptance_criteria>
  <done>The encounter panel shows head → roster → Round Card → Fight!/actions; the card scrolls inside itself past 40% of the panel; a new card is announced once through a persistent live region; the last-exchange footnote, CSS and S-fields are gone.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the Round Card region, the routing exclusivity and the copy's voice safety in test/unit/shell-round-card.test.js; run the §6.8 list and the partition invariants</name>
  <files>test/unit/shell-round-card.test.js</files>
  <read_first>
    - test/unit/shell-loot-screen.test.js L24-60 and L180-215 (readFileSync + CRLF normalize + stripComments + region slicing between two anchors — copy this style verbatim)
    - test/unit/toastTable.test.js L38-60 (the REFUSAL_TYPES list and the bare-{type} builder call pattern) and test/unit/toastsCoverage.test.js L139-200 (the 25-05 partition invariants that must keep passing)
    - test/voice/safety-scan.test.js L1-60 (how BANNED/ALLOWLIST are imported and matched with word boundaries — mirror the matcher for the two new header strings; the scan is structure-driven and does not see mazeworld.html copy on its own)
    - src/browser/toasts.js L826-858 (toastsForAction output shape + the Task 1 `opts.limit`), L47-50 (PRIORITY, MAX_TOASTS), L185 (FEATURE_EVENTS)
  </read_first>
  <action>
    <!-- planner-discipline-allow: lastExchange -->
    <!-- planner-discipline-allow: exchangeN -->
    1. Create test/unit/shell-round-card.test.js with the shell-loot-screen helpers (RAW_HTML, HTML, stripComments, CODE) and a `renderEncounterRegion()` helper slicing CODE from `function renderEncounter()` to `function noteCombat(` (the same anchors shell-fight-gate uses) and a `dispatchRegion()` slicing from `function dispatchWithToasts(action)` to `window.move = function engineMove`.
    2. Round-card region pins (the owed "renderEncounter round-card source assertion"): the region reads `window.__mzRoundCard`; contains `className = "round-card"` exactly once; the index of that class assignment is after `list.className = "foes"` and before `if (C.pending)` inside the region; every line is written through `textContent` (the region's card block, sliced from `const rc = window.__mzRoundCard` to `body.appendChild(card)`, contains `p.textContent = line` and contains no `innerHTML`); the region calls `syncRoundCardLive(`; the region contains no `slice(-6)`; the file's CODE has zero occurrences of `lastExchange`, `exchangeN`, `S.roundCard` and `state.roundCard`; the raw HTML has zero `exflash` and zero `.exchange{`.
    3. Persistent announcer pins: HTML contains `id="enc-round-live"` with `aria-live="polite"` and `aria-atomic="true"` and class `sr-only`; its index is after `<div id="enc-body"></div>` and before the `#enc-panel` closing `</section>`; CODE never assigns `innerHTML` on the announcer (search the `function syncRoundCardLive` region for `textContent` present and `innerHTML` absent); `syncRoundCardLive` compares `rc.seq` against `roundCardAnnouncedSeq` (seq-gated announcement).
    4. CSS pins: the `.round-card{` rule carries `max-height:40%`, `overflow-y:auto`, `touch-action:manipulation`, and does not carry `animation`; no `.exchange` rule remains.
    5. Routing exclusivity (the owed test, design §6.4 last paragraph): (a) SOURCE — the dispatchRegion contains exactly one `if (inCombat && t.priority !== PRIORITY.block)` and exactly one `window.mzToast?.(` (structural: one if/else, so no toast object can take both branches); (b) BEHAVIOUR — replicate the routing predicate in the test as `const destinationFor = (inCombat, t) => (inCombat && t.priority !== PRIORITY.block ? "card" : "toast")`, then for every `type` in `Object.keys(TOAST_FOR)` call the builder bare (`TOAST_FOR[type]({ type }, {})`) and record `destinationFor(true, t)`; assert the resulting card-set and toast-set are disjoint and their union is every TOAST_FOR type; assert every entry of toastTable's REFUSAL_TYPES list (copy the array) is in the toast-set; assert `encounterStarted`, `combatJoined`, `phobiaAfraid`, `struck`, `strikeMissed`, `struckByFoe`, `foeMissed`, `frenzy`, `lootDropped` are in the card-set; assert `lootTaken` and `lootLeft` route to "toast" when `inCombat` is false (`destinationFor(false, …)`) — the loot card is out of combat, so Phase 29's toasts stand; (c) run the real pipeline on one mixed in-combat action: `toastsForAction("attack", [{ type: "struck", name: "Giant Rat", dmg: 4 }, { type: "strikeRefused" }], {})` and assert exactly the block item maps to "toast" and the rest to "card"; (d) UNCAPPED card, capped host (CONTEXT Area 1 #5): build a synthetic six-toast action — pick six distinct `TOAST_FOR` types whose bare builder is not PRIORITY.block and which each survive the pipeline alone (`toastsForAction("attack", [{ type }], {}).length === 1` — e.g. frenzy, phobiaAfraid, lootDropped, combatJoined and two more chosen by that filter, so the test never depends on grouper details), then assert `toastsForAction("attack", sixEvents, {}, { limit: Infinity }).length === 6` (every folded line reaches the card), `toastsForAction("attack", sixEvents, {}).length === MAX_TOASTS` (the default/out-of-combat toast path still caps at 4), and that applying the routing partition with inCombat true yields 6 card lines and 0 toasts while inCombat false yields 0 card lines and a queue whose `slice(0, MAX_TOASTS)` is 4 long; (e) assert the toasts.js signature carries `opts = {}` and `deduped.slice(0, limit)` (readFileSync the module) so a future revert to a hard slice fails here.
    6. Voice safety of the new copy: extract the `const ROUND_CARD_COPY = {…};` object literal text from CODE, collect its string/template literal contents (preview + the round template with a numeric placeholder substituted), and assert no word-boundary match against any `BANNED` term from content/safety-wordlist.js unless it is in `ALLOWLIST` (mirror safety-scan.test.js's matcher). Also assert both header strings are non-empty.
    7. Run the full §6.8 list + invariants: `node --test test/unit/shell-round-card.test.js test/unit/narrativeToasts.test.js test/unit/shell-toast-wiring.test.js test/unit/feedback-payload.test.js test/unit/oracleLogViewModel.test.js test/unit/shell-oracle-panel.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/voice/safety-scan.test.js` → `# fail 0`; confirm `git status --porcelain test/unit/narrativeToasts.test.js test/unit/oracleLogViewModel.test.js test/unit/shell-oracle-panel.test.js test/unit/feedback-payload.test.js test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js` is empty (unchanged files, per §6.8/RESEARCH finding 3 — the limit option is backward-compatible, so the toasts.js suites need no edit). Then `npm test` → `# fail 0`. Commit: `test(32-02): pin the Round Card region, routing exclusivity and header copy voice`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/shell-round-card.test.js test/unit/narrativeToasts.test.js test/unit/shell-toast-wiring.test.js test/unit/feedback-payload.test.js test/unit/oracleLogViewModel.test.js test/unit/shell-oracle-panel.test.js test/unit/toastsCoverage.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/voice/safety-scan.test.js 2>&1 | tail -5 && git status --porcelain test/unit/narrativeToasts.test.js test/unit/oracleLogViewModel.test.js test/unit/shell-oracle-panel.test.js test/unit/feedback-payload.test.js test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/shell-round-card.test.js` and `node --test test/unit/shell-round-card.test.js` → `# fail 0` and `# pass` >= 8
    - `grep -c 'destinationFor' test/unit/shell-round-card.test.js` >= 3 and `grep -c 'BANNED' test/unit/shell-round-card.test.js` >= 1
    - `grep -c 'limit: Infinity' test/unit/shell-round-card.test.js` >= 2 and `grep -c 'MAX_TOASTS' test/unit/shell-round-card.test.js` >= 2
    - `git status --porcelain test/unit/narrativeToasts.test.js test/unit/oracleLogViewModel.test.js test/unit/shell-oracle-panel.test.js test/unit/feedback-payload.test.js test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js` prints nothing
    - `npm test` → `# fail 0` and `# pass` >= 1870
    - `git diff --stat 9174e6e..HEAD -- engine content test/parity` prints nothing
  </acceptance_criteria>
  <done>The Round Card region, the persistent announcer, the single-seam routing (uncapped card, capped host — six folded toasts become six card lines), the limit option and the new copy's voice are all pinned; the §6.8 list is green with the "unchanged" files literally unchanged; the partition invariants hold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events → DOM text (round card) | event-derived prose becomes DOM content inside #enc-body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-32-03 | Tampering | round-card line rendering | low | mitigate | every line is set via textContent (never innerHTML interpolation), and passed through narrativeToastText first — pinned by shell-round-card.test.js |
| T-32-04 | Information Disclosure | window.__mzRoundCard persisted to the save | low | mitigate | the card is a module-level window property, never an S field (serializeRun spreads S) — pinned: zero S.roundCard/state.roundCard occurrences |
</threat_model>

<verification>
- `npm test` `# fail 0`; `npm run build:www` exit 0; `git diff --stat 9174e6e..HEAD -- engine content test/parity` empty; `test/parity/prototype-master.js.txt` untouched.
- shell-round-card + re-pinned shell-toast-wiring green; narrativeToasts / oracleLogViewModel / shell-oracle-panel / feedback-payload unchanged on disk and green; toastsCoverage/toastTable (25-05 partition invariants) green.
</verification>

<success_criteria>
- ROADMAP success criteria 1 and 2 are code-complete: a round's narrative lands in one block in the encounter panel (Oracle unabridged), and nothing new has to be dismissed — moving on is still the one action tap.
- Refusals still toast; out-of-combat toasts untouched; no engine change.
</success_criteria>

<output>
Create `.planning/phases/32-combat-narrative-input-ui-build/32-02-SUMMARY.md` when done. The SUMMARY MUST carry: `## Self-Check` (files exist, commit hashes found, `npm test` counts, build exit code, the empty engine/content/test/parity diff, the four unchanged §6.8 files), `## Deviations` (or "None"), and `## Human verification (deferred to end of run)` with Pixel 7 bullets, at minimum: (1) walk into an encounter — the panel shows head → foe roster → the "The stare-down" card holding the encounter line → Fight!, and NO narrative toast appears at the top of the screen; (2) tap Fight! — the card becomes "Round 1 · how that went" with the initiative line (and any pre-emptive strike / "Afraid" line), still no toast; (3) tap Strike — the card is replaced by that round's lines, the topbar counter reads the next round, the action bar has not moved; (4) open Spells / tap a cooling Use item / tap Potion at full health — the card persists and the refusal ("Already at full health." / the engine's reason) arrives as a toast; (5) a long round (several foes) scrolls inside the card rather than pushing the action bar; (6) kill the last foe — the loot card appears and the kill line is a toast over it; flee — the panel hides and the flee line is a toast; (7) TalkBack on: a new round is announced once, opening Spells does not re-announce. Never instruct a pause for device checks.
</output>
