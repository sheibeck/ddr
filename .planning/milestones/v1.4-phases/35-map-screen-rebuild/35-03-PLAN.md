---
phase: 35-map-screen-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["35-01", "35-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-map-hud.test.js
  - test/unit/shell-gear-toolbar.test.js
autonomous: true
requirements: [MAP-01, MAP-06, MAP-07, MAP-08]
tags: [vanilla-js, hud, condition-chips, canvas-palette, mark-glyphs, marks-sheet, camp-sheet, viewport-chips, source-assertion-tests]

must_haves:
  truths:
    - "The map column's HUD strip is the mock's: FLOOR (gold) · DAY · SQUARES · RATIONS (red #e05a48 at ≤ 2) on the left in Press Start 2P 6.5px labels / Courier 700 16px values, and x/y WP (red at ≤ 25 %) over a 64×5 bar (#7f9d4e > 50 %, #e8c97a > 22 %, else #e05a48) on the right; bg #1b170f, border-bottom 3px #3a3226, padding 8px 14px 9px (+ safe-area top); the name/class/hp character line is gone (armor and gold live on the Hero/Gear tabs only); the settings gear and the DEV chip stay in the strip (MAP-01, ruling 5)"
    - "A condition-chip strip sits directly under the HUD strip (bg #181409, border-bottom 3px #3a3226, padding 6px 14px 7px, hidden when empty): one chip per conditionsOf(S) descriptor — flex:none, border 1px tone edge, bg #1b170f, padding 3px 5px, Press Start 2P 5.5px tone ink (bad #a63a2c/#e07260, warn #6b5c3c/#e8c97a, odd #5b4a86/#b9a4ef, good #5e7a3c/#a8cc72), a Courier 700 10px #8f856f remaining-count suffix when the condition has one; tapping a chip (guarded) puts its explanation in the rail as an info card (MAP-01, ruling 5)"
    - "The canvas renderer keeps its engine-fed drawing but paints the mock's palette — fog #0b0a08, seen walls as raised #2c261c blocks with the 2px #4a3f2c / #16120c bevel, floor #645c48 with the 1px #554d3b inset, a 6px #443a26 outer border — draws every seen feature as its coloured text glyph at 60 % of the cell (● ◆ ▲ ✕ ▪ ⧗ ▼, the one-way ▲ rotated to its passable direction) and the party as a #f4dc94 dot with the static glow plus a DOM ring that pulses (mwglow, reduced-motion safe); fog-of-war and marks behave as today; the inset vignette stays (MAP-07, decision 3)"
    - "MARKS opens the WHAT THE MARKS MEAN bottom sheet whose rows are glyph (22px, coloured, width 26) · name · description from mapMarks.js's MARKS_LEGEND (no PNG rows); CENTRE recenters; MAKE CAMP opens a bottom sheet (title, the mock copy, SLEEP / 1 RATION gold + WALK / ON bordered) whose SLEEP dispatches the existing camp bridge and whose refusal / result are rail cards; both sheets close on their scrim, stamp the settle window, and their buttons go through guardTap; the three chips sit over the viewport top-left / top-right per the spec block (MAP-06, MAP-08)"
    - "The Phase 33 chip row, the unused flash element, the HUD character line and the PNG draw path are retired; the party rail (companion cards) stays, hidden when solo, beneath the chip strip"
  artifacts:
    - path: "mazeworld.html"
      provides: "the rebuilt HUD strip + .mw-hud-wp*, .mw-cond-strip / .mw-cond[data-tone] + CONDITION_TONE / CONDITION_EXPLAIN + the guarded chip taps, .mw-map-chips / .mw-map-chip, the palette/glyph draw(), #mw-party-pulse + positionPartyPulse(), the glyph legend rows, #mw-camp-sheet + openCampSheet()/closeCampSheet(), MAP_COPY, the window.__mzMapMarks bridge"
    - path: "test/unit/shell-map-hud.test.js"
      provides: "source-assertion pins for every truth above + the voice scan of CONDITION_EXPLAIN and MAP_COPY"
    - path: "test/unit/shell-gear-toolbar.test.js"
      provides: "re-pinned UIF-05 chip-row assertions against .mw-map-chips"
  key_links:
    - from: "draw() (classic)"
      to: "src/browser/mapMarks.js"
      via: "window.__mzMapMarks.MAP_PALETTE / markForCell / MARK_SCALE / ONEWAY_ROTATION_DEG"
      pattern: "window\\.__mzMapMarks"
    - from: "paintConditions() (classic)"
      to: "window.mzRailLine (Plan 02)"
      via: "guardTap(btn, () => window.mzRailLine?.(label…, CONDITION_EXPLAIN[cn.key]…, \"info\", 4200))"
      pattern: "CONDITION_EXPLAIN\\["
    - from: "openCampSheet() (classic)"
      to: "window.mzMakeCamp (module, unchanged)"
      via: "guardTap(sleepBtn, () => { closeCampSheet(); window.mzMakeCamp(); })"
      pattern: "window\\.mzMakeCamp"
  prohibitions:
    - "No edits under engine/, content/, test/parity/; no edit to src/browser/icons.js and no deletion under icons/ (decision 3 keeps the assets on disk)"
    - "No PNG draw or <img> row survives in draw() or renderMarksLegend(); no graph-paper grid lines; no live getComputedStyle palette reads"
    - "No innerHTML for a chip whose label comes from engine content (affliction kind) — createElement/textContent"
    - "No D-pad / pointer-model / stair work in this plan (Plan 04); the D-pad and control bar stay byte-identical here"
    - "No new tap target without guardTap except the sheets' scrim (the allowed dismiss) and the pre-existing MARKS/CENTRE chips"
---

<objective>
Rebuild the map column's chrome to the spec: the HUD strip (FLOOR · DAY · SQUARES · RATIONS · WP bar) with the condition-chip strip beneath it (ruling 5 — the user moved the chips into the HUD), the MARKS / CENTRE / MAKE CAMP chips over the viewport with their two bottom sheets, and the canvas renderer's palette + coloured mark glyphs + pulsing party marker (MAP-01, MAP-06, MAP-07). Everything reads the pure modules from Plan 01 (`mapMarks.js`) and writes into the rail from Plan 02 (chip explanations, camp refusal/result).

**Decisions recorded here:**
- **Decision 3 (marks): coloured text glyphs on the canvas and in the legend replace the Phase-4 PNG icons — a deliberate reversal of the Phase-4 icon decision (user-imported mock, 2026-09-16).** `icons/*.png`, `src/browser/icons.js` and the boot-time preload/bridge lines stay untouched (no asset deletion; the pure `featureKeyForCell` mapper is still the feat→key source through `mapMarks.js`); only `draw()` and `renderMarksLegend()` stop referencing the PNG pipeline.
- **Ruling 5 (chips in the HUD):** the strip lives directly under the HUD strip, not over the viewport; armor and gold are NOT in the HUD (they already live on the Hero/Gear tabs — nothing to move); the HUD character line (name / subclass / level / hp) is retired in favour of the mock's right-hand WP readout — the Hero tab keeps the full sheet.
- **Claude's discretion:** chip tone per condition key is `CONDITION_TONE` (below); chip explanations are `CONDITION_EXPLAIN` (exact strings below, family-friendly sarcasm); the polarity ▲/▼ marker glyph of the old chips is dropped (the mock has none; the label itself names the condition); the gear button and DEV chip stay in the HUD strip's right cluster after the WP block; the party rail keeps its Phase 10 behaviour beneath the chip strip; the pulsing party marker is a DOM ring over the canvas (no rAF render loop) so the blanket reduced-motion rule zeroes it; the camp sheet's SLEEP is refused (pulse) while a rail decision is pending (ruling 3 — no dispatch past an active choice); the pinch clamp stays at its current values here (Plan 04 re-ranges ZOOM_MAX to 2.0).

Purpose: the mock's look for everything around the viewport, and the sheets the chips open.
Output: `mazeworld.html` (HUD, chips, canvas, sheets, MAP_COPY, retirements), `test/unit/shell-map-hud.test.js`, re-pinned `shell-gear-toolbar.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-map-screen-rebuild/35-CONTEXT.md
@.planning/phases/35-map-screen-rebuild/35-RESEARCH.md
@.planning/phases/35-map-screen-rebuild/35-01-SUMMARY.md
@.planning/phases/35-map-screen-rebuild/35-02-SUMMARY.md
@.planning/milestones/v1.3-phases/33-ui-feel-store-polish/33-02-SUMMARY.md
@design/Mazeworld Map.dc.html
@design/Mazeworld Map Panel.dc.html
</context>

<artifacts_this_phase_produces>
See 35-01-PLAN.md "Artifacts this phase produces" for the full phase list. This plan creates: classic `MAP_COPY` (camp copy; Plan 04 adds stair), `CONDITION_TONE`, `CONDITION_EXPLAIN`, `positionPartyPulse()`, `openCampSheet()`, `closeCampSheet()`, `lastCondKeyShown`; the `window.__mzMapMarks` bridge; DOM ids `mw-hud-wp`, `mw-hud-wpfill`, `mw-map-chips`, `mw-party-pulse`, `mw-camp-sheet`, `mw-camp-scrim`, `mw-camp-copy`, `mw-camp-sleep`, `mw-camp-walk` (kept: `m-floor`, `m-day`, `m-steps`, `m-rations`, `m-rat-wrap`, `mm-conditions`, `mw-dev-chip`, `mw-gear-btn`, `mw-party-rail`, `mw-chip-marks`, `mw-chip-centre`, `btn-camp`, `mw-legend-*`); CSS `mw-hud-wp mw-hud-wp-text (low) mw-hud-wptrack mw-hud-wpfill (mid|low)`, `mw-cond-strip mw-cond[data-tone] mw-cond-detail`, `mw-map-chips mw-map-chips-gap mw-map-chip (camp)`, `mw-party-pulse`, `mw-legend-glyph`, `mw-camp-copy mw-camp-actions mw-camp-btn (secondary)`, `@keyframes mwglow`; `test/unit/shell-map-hud.test.js`. It retires: the HUD character line (markup, CSS, paint writes), the old chip-track CSS, the Phase 33 viewport chip row + its CSS, the flash element + its function, the PNG draw path + the graph-paper/ink-edge passes in draw(), the legacy palette table, the `<img>` legend rows.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: HUD strip + condition-chip strip (markup, CSS, paint/paintConditions, CONDITION_TONE/EXPLAIN, guarded chip → rail)</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html lines 1369-1416 (the current header: .mw-hud-top with the stat row + DEV chip + gear, the character line block (name / class / hp readout) to retire, the condition track to re-home, the party rail — read the whole block), 880-935 (HUD + condition + party-rail CSS: rewrite .mw-hud, delete the character-line rules and the old chip rules; keep .mw-party-rail*, .mw-map-hptrack / .mw-map-hpfill which renderPartyRail still uses), 946-965 (.mw-hud-item / b / floor / rations rules), 125-136 (the two HUD font vars — re-range to 6.5px / 16px)
    - mazeworld.html lines 2917-2940 (CONDITION_COPY — keep byte-identical; add CONDITION_TONE and CONDITION_EXPLAIN directly after FOE_EFFECT_LABEL at L2980), 2988-3032 (paintConditions — keep the label/detail resolution chain byte-identical (test/unit/foe-effect-chip.test.js pins the foeEffect branch, the affliction branch and the generic fallback ordering); replace only the output construction), 3034-3100 (paint(): the m-floor/m-day/m-steps/m-rations/m-rat-wrap writes stay; the character-line + hp-readout writes at L3090-3098 go; the `pct` const is reused for the WP readout)
    - mazeworld.html Plan 02 regions: `window.mzRailLine` (module bridge), `guardTap` / `armEncounterButtons` (L5027-5040)
    - design/Mazeworld Map.dc.html lines 21-39 (the HUD strip + condition strip template — every inline style value) and 61-77 (rationStyle / wpStyle / wpBar thresholds; the chip TONE table and chip/left styles)
    - test/unit/foe-effect-chip.test.js lines 62-96 (the paintConditions pins that must keep matching), test/unit/shell-party-camp.test.js lines 75-98 (the campBtn short-state block in paint() — unchanged)
  </read_first>
  <action>
    <!-- planner-discipline-allow: hud-name -->
    <!-- planner-discipline-allow: hud-cls -->
    <!-- planner-discipline-allow: mm-hp -->
    <!-- planner-discipline-allow: mw-hud-char -->
    <!-- planner-discipline-allow: mw-cond-track -->
    Discipline: the five identifiers above name what to delete; none may remain in mazeworld.html afterwards — code or comment (describe them by role: "the DR13 character line", "the DR15-B chip track").
    1. Font vars: `--mw-font-hud-label` → `calc(0.40625rem * var(--mw-text-scale))` (6.5px at M) and `--mw-font-hud-num` → `calc(1rem * var(--mw-text-scale))` (16px at M); update their comments to cite the Map mock.
    2. Header markup: keep `<header class="mw-hud" id="mw-hud">` and `.mw-hud-top`; inside `.mw-hud-row` change the third item's label text from `Moves` to `Squares` (id `m-steps` unchanged — it IS the run's squares-walked counter); after the row add the WP block `<div class="mw-hud-wp"><span class="mw-hud-wp-text" id="mw-hud-wp">0/0 WP</span><div class="mw-hud-wptrack"><div class="mw-hud-wpfill" id="mw-hud-wpfill" style="width:100%"></div></div></div>`, then the existing DEV chip and gear button (unchanged ids). Delete the whole character-line block (its comment, the name span, the class span, the hp readout with its two b's and the track/fill). Move the condition element OUT of the header to directly after `</header>` as `<div class="mw-cond-strip" id="mm-conditions" hidden></div>` (id kept) with a comment: Phase 35 ruling 5 — condition chips in a strip directly under the HUD strip; tap = explanation in the rail. Move the party rail div after it (unchanged id/class; it is hidden when solo).
    3. HUD CSS: `.mw-hud{flex:none;background:#1b170f;border-bottom:3px solid #3a3226;padding:calc(8px + var(--safe-area-inset-top, env(safe-area-inset-top, 0px))) 14px 9px;display:flex;flex-direction:column}` (drop sticky/z-index — the app column is a flex layout since Plan 02); `.mw-hud-top{display:flex;align-items:center;gap:10px}`; `.mw-hud-row{display:flex;gap:8px;align-items:baseline;flex:1;min-width:0;overflow:hidden;flex-wrap:nowrap}`; `.mw-hud-item{font-family:var(--disp);font-size:var(--mw-font-hud-label);letter-spacing:0;text-transform:uppercase;color:#9a8f76;display:flex;align-items:baseline;gap:2px;white-space:nowrap}`; `.mw-hud-item b{font-family:var(--mono);font-size:var(--mw-font-hud-num);font-weight:700;color:#e6ddc6;font-variant-numeric:tabular-nums}`; `.mw-hud-floor b{color:#e8c97a}`; `.mw-hud-rations.warn b{color:#e05a48}`; `.mw-hud-wp{flex:none;display:flex;flex-direction:column;gap:4px;align-items:flex-end;margin-left:auto}`; `.mw-hud-wp-text{font-family:var(--mono);font-weight:700;font-size:var(--mw-font-hud-num);white-space:nowrap;color:#e6ddc6}`; `.mw-hud-wp-text.low{color:#e05a48}`; `.mw-hud-wptrack{width:64px;height:5px;background:#2c2519;box-shadow:inset 0 0 0 1px #0f0d09}`; `.mw-hud-wpfill{height:100%;background:#7f9d4e;transition:width .25s ease}`; `.mw-hud-wpfill.mid{background:#e8c97a}`; `.mw-hud-wpfill.low{background:#e05a48}`. Delete the six character-line rules (char, name, cls, lvl, hp, hp-lab) and the old chip-track rules (the track, the base chip rule, the ::before marker rules, the good/bad rules, the detail rule) — then add the strip rules: `.mw-cond-strip{flex:none;display:flex;gap:5px;overflow-x:auto;padding:6px 14px 7px;border-bottom:3px solid #3a3226;background:#181409}`; `.mw-cond-strip[hidden]{display:none}`; `.mw-cond{display:flex;align-items:baseline;gap:4px;flex:none;border:1px solid var(--cond-edge);background:#1b170f;padding:3px 5px;min-height:0;min-width:0;border-radius:0;box-shadow:none;font-family:var(--disp);font-size:5.5px;line-height:1.5;color:var(--cond-ink);white-space:nowrap;cursor:pointer}`; `.mw-cond[data-tone="bad"]{--cond-edge:#a63a2c;--cond-ink:#e07260}`, warn `#6b5c3c`/`#e8c97a`, odd `#5b4a86`/`#b9a4ef`, good `#5e7a3c`/`#a8cc72`; `.mw-cond-detail{font-family:var(--mono);font-weight:700;font-size:10px;color:#8f856f}`. `.mw-party-rail` gains `padding:7px 14px;background:#1b170f;margin-top:0;border-top:0;border-bottom:3px solid #3a3226` so it reads as a strip too.
    4. Constants after FOE_EFFECT_LABEL: `const CONDITION_TONE = { haste: "good", invis: "odd", acute: "good", ether: "odd", might: "good", flight: "odd", darkness: "warn", afraid: "warn", ward: "good", foeEffect: "bad", affliction: "bad" };` and `const CONDITION_EXPLAIN = { haste: "Two strikes a round while it lasts. Spend them on something that deserves it.", invis: "They swing at where you were. Enjoy it; it wears off.", acute: "You strike first on a d6. The maze is briefly unfair in your favour.", ether: "Walls are a suggestion and crevices are a rumour. Count the squares.", might: "Extra damage until the day ends. Hit things.", flight: "Walls and crevices cost nothing while the charge holds. Then they cost the usual.", darkness: "You see your own square and not much else. Walk carefully, or walk into something.", afraid: "A penalty on every roll until it passes. Nobody is proud of this.", ward: "A pool of hit points that is not yours, soaking blows until it runs dry.", foeEffect: "Something they did to you. It wears off in a few rounds, which is more than most things do.", affliction: "It takes a bite out of you on a schedule. Find a cure before it finds the rest.", default: "A condition. The maze does not explain itself further." };` — ONE object literal each (voice-scannable), with a comment that both are presentation copy only.
    5. paintConditions(c): keep everything through the `detail` computation byte-identical; replace the `.map(...).join("")` innerHTML build with: `host.innerHTML = ""; const keyList = conds.map((cn) => cn.key + ":" + (cn.kind || "")).join("|");` then for each descriptor create `const btn = document.createElement("button"); btn.type = "button"; btn.className = "mw-cond"; btn.dataset.tone = CONDITION_TONE[cn.key] || (cn.polarity === "bad" ? "bad" : "good"); btn.dataset.key = cn.key; btn.textContent = label; if (detail) { const d = document.createElement("span"); d.className = "mw-cond-detail"; d.textContent = detail; btn.appendChild(d); } host.appendChild(btn); guardTap(btn, () => window.mzRailLine?.(label.toUpperCase(), CONDITION_EXPLAIN[cn.key] || CONDITION_EXPLAIN.default, "info", 4200, "·"));` then `if (keyList !== lastCondKeyShown) { lastCondKeyShown = keyList; armEncounterButtons(); }` (module-scope `let lastCondKeyShown = "";` declared above paintConditions; comment: key-gated so a repaint after every step does not re-arm the chips under the thumb — only a changed chip set does). `host.hidden = false;` at the end as today; the empty case keeps `host.hidden = true`.
    6. paint(): delete the character-line writes (the two name/class lookups and their two writes, the hp/hpmax writes, the fill width/low toggles — L3090-3098); after `fill.classList.toggle("low", pct < 34);` add `const wpText = document.getElementById("mw-hud-wp"); if (wpText) { wpText.textContent = \`${Math.max(0, c.wp)}/${c.maxWP} WP\`; wpText.classList.toggle("low", pct <= 25); } const wpFill = document.getElementById("mw-hud-wpfill"); if (wpFill) { wpFill.style.width = pct + "%"; wpFill.classList.toggle("mid", pct <= 50 && pct > 22); wpFill.classList.toggle("low", pct <= 22); }` with a comment citing the mock thresholds (> 50 green, > 22 gold, else red; text red at ≤ 25 %).
  </action>
  <verify>
    <automated>grep -c 'id="mw-hud-wp"' mazeworld.html && grep -c 'class="mw-cond-strip" id="mm-conditions"' mazeworld.html && grep -c "^const CONDITION_TONE = {" mazeworld.html && grep -c "^const CONDITION_EXPLAIN = {" mazeworld.html && test "$(grep -c 'hud-name\|hud-cls\|mm-hp\|mw-hud-char\|mw-cond-track' mazeworld.html)" = "0" && node --test test/unit/foe-effect-chip.test.js test/unit/shell-party-camp.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-hud-wp"' mazeworld.html` = 1; `grep -c 'id="mw-hud-wpfill"' mazeworld.html` = 1; `grep -c ">Squares<b id=\"m-steps\">" mazeworld.html` = 1.
    - `grep -c ">Moves<" mazeworld.html` = 0 (the old label text).
    - `grep -c "hud-name\|hud-cls\|mm-hp\|mw-hud-char\|mw-cond-track" mazeworld.html` = 0 (raw file).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const h=s.indexOf('</header>');const c=s.indexOf('class=\"mw-cond-strip\" id=\"mm-conditions\"');const p=s.indexOf('id=\"mw-party-rail\"');const m=s.indexOf('<main class=\"mw-screens\"');process.exit(h<c&&c<p&&p<m?0:1)"` exits 0.
    - `.mw-hud{` rule contains `background:#1b170f`, `border-bottom:3px solid #3a3226`, `14px 9px`, and no `position:sticky`; `grep -c "^\.mw-hud-floor b{color:#e8c97a}" mazeworld.html` = 1; `grep -c "^\.mw-hud-rations\.warn b{color:#e05a48}" mazeworld.html` = 1; `.mw-hud-wptrack{` contains `width:64px;height:5px`; `grep -c "^\.mw-hud-wpfill\.mid{background:#e8c97a}\|^\.mw-hud-wpfill\.low{background:#e05a48}\|^\.mw-hud-wp-text\.low{color:#e05a48}" mazeworld.html` = 3.
    - `.mw-cond-strip{` contains `padding:6px 14px 7px`, `background:#181409`, `border-bottom:3px solid #3a3226`; `.mw-cond{` contains `padding:3px 5px`, `font-size:5.5px`, `background:#1b170f`; the four tone rules `.mw-cond[data-tone="bad"]{--cond-edge:#a63a2c;--cond-ink:#e07260}` / warn `#6b5c3c;--cond-ink:#e8c97a` / odd `#5b4a86;--cond-ink:#b9a4ef` / good `#5e7a3c;--cond-ink:#a8cc72` each = 1; `.mw-cond-detail{` contains `font-size:10px` and `color:#8f856f`.
    - `--mw-font-hud-label:calc(0.40625rem * var(--mw-text-scale))` and `--mw-font-hud-num:calc(1rem * var(--mw-text-scale))` each = 1.
    - paintConditions region (`function paintConditions(c)` → `function paint()`): contains `cn.key === "foeEffect" ? (FOE_EFFECT_LABEL[cn.kind] || CONDITION_COPY.foeEffect.label)`, `(CONDITION_COPY[cn.key]?.label || cn.key)`, `document.createElement("button")`, `btn.dataset.tone = CONDITION_TONE[cn.key] ||`, `CONDITION_EXPLAIN[cn.key] || CONDITION_EXPLAIN.default`, `guardTap(btn,`, `window.mzRailLine?.(`, `lastCondKeyShown`, `armEncounterButtons();`; zero `innerHTML` other than the one `host.innerHTML = "";` clear.
    - paint() region: `grep -c 'wpText.textContent = `${Math.max(0, c.wp)}/${c.maxWP} WP`' mazeworld.html` = 1; `grep -c 'wpFill.classList.toggle("low", pct <= 22)' mazeworld.html` = 1; `grep -c 'wpText.classList.toggle("low", pct <= 25)' mazeworld.html` = 1.
    - `node --test test/unit/foe-effect-chip.test.js test/unit/shell-party-camp.test.js` exits 0 without edits to either file.
  </acceptance_criteria>
  <done>The HUD strip and the condition-chip strip match the mock's values, chips explain themselves in the rail through guardTap, and the character line is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Viewport chrome + canvas palette/glyph renderer + party pulse + MARKS legend glyph rows + MAKE CAMP sheet (MAP_COPY, openCampSheet/closeCampSheet, settle stamps)</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html lines 300-333 (.mazebox, .mw-maze-viewport, .mw-vignette, the Phase 33 chip row + chip rules + the camp margin rule, the flash rules — restyle/replace), 1421-1443 (the viewport markup: canvas, vignette, the chip row, the flash element — rewrite the chip row, delete the flash element; the control bar below stays for Plan 04), 2536-2740 (fit / positionCanvas / reveal / the legacy palette table / draw() — rewrite draw()'s passes and the palette source; keep fit(), reveal(), positionCanvas()'s transform and the `positionCanvas();` call at draw()'s end), 6120-6168 (MARKS legend data + renderMarksLegend + open/close + the three chip listeners + centerMap — the data moves to mapMarks.js (Plan 01), the rows become glyph spans, closeMarksLegend stamps the settle window), 6071-6073 (the camp chip's onclick — now opens the sheet), 6100-6110 (the unused flash function — delete), 1136-1163 (.mw-legend-sheet chrome CSS — reuse; replace the img row rule with the glyph rule), 1293-1302 (the legend sheet markup — add the camp sheet as a sibling after the settings sheet at L1314+), 2944-2960 (COMBAT_COPY — add MAP_COPY right after it), 6635-6638 (the icons/controls bridges — add the mapMarks bridge beside them; leave the preload line untouched), 7488-7506 (window.mzMakeCamp — unchanged; the sheet's SLEEP calls it), 5027-5040 (guardTap/armEncounterButtons), 4986 area (railLocked from Plan 02)
    - src/browser/mapMarks.js (Plan 01 exports) and src/browser/rail.js RAIL_COPY (nothing new needed here)
    - design/Mazeworld Map Panel.dc.html lines 24-31 (viewport chrome: vignette, MARKS/CENTRE/MAKE CAMP inline styles), 68-80 (MAKE CAMP sheet template: title, copy, SLEEP / 1 RATION gold flex 1 + WALK / ON bordered), 114-117 (palette + glyph block), 133-140 (SHEETS block: legend rows glyph 22px width 26 · name · description; camp copy + "no food → rail bad NOTHING TO EAT")
    - test/unit/shell-gear-toolbar.test.js lines 60-75 (chipsMarkup()/mazefootMarkup() helpers — the chip helper's markers change), 155-197 (the UIF-05 pins to re-pin in Task 3), test/unit/shell-party-camp.test.js lines 75-98 (`document.getElementById("btn-camp").onclick` must still exist once; `#btn-camp[data-short="1"]{` must still exist)
  </read_first>
  <action>
    <!-- planner-discipline-allow: mw-viewport-chips -->
    <!-- planner-discipline-allow: mw-flash -->
    <!-- planner-discipline-allow: flashMessage -->
    <!-- planner-discipline-allow: MAZE_CANVAS_COLORS -->
    <!-- planner-discipline-allow: iconReady -->
    <!-- planner-discipline-allow: mw-legend-row img -->
    Discipline: the six identifiers above name what to delete; none may remain in mazeworld.html afterwards — code or comment.
    1. Layout/CSS: `.mazebox{position:relative;display:flex;flex-direction:column;flex:1;min-height:0;background:#14110c;padding:0;border:0;box-shadow:none}`; `.mw-maze-viewport{position:relative;width:100%;flex:1;min-height:220px;overflow:hidden;background:#080705;border:0;touch-action:none;-webkit-user-select:none;user-select:none}` (the user-select lines prepare Plan 04's hold-to-inspect — no context menu / selection on a long press); keep `.mw-vignette` as is. Replace the Phase 33 chip-row rules, the chip rules, the camp margin rule and the flash rules with: `.mw-map-chips{position:absolute;left:10px;right:10px;top:10px;display:flex;gap:6px;align-items:flex-start;z-index:3}`, `.mw-map-chips-gap{flex:1}`, `.mw-map-chip{font-family:var(--disp);font-size:7px;letter-spacing:0;padding:9px 10px;min-height:48px;min-width:48px;background:#1b170f;border:2px solid #4a4032;color:#e8c97a;box-shadow:0 3px 0 #0d0b08;border-radius:0;display:flex;align-items:center;justify-content:center;cursor:pointer}`, `.mw-map-chip.camp{background:#241d12;border-color:#6b5c3c}`, `.mw-map-chip:active{transform:translateY(2px);box-shadow:0 1px 0 #0d0b08}`; keep the existing `#btn-camp[data-short="1"]{…}` rule (shell-party-camp pins it) restyled if you like (opacity .75 is fine). Add `.mw-party-pulse{position:absolute;border-radius:50%;pointer-events:none;z-index:2;animation:mwglow 1.6s ease-in-out infinite}` and `@keyframes mwglow{0%,100%{box-shadow:0 0 0 3px #14110c,0 0 10px 2px rgba(232,201,122,.35)}50%{box-shadow:0 0 0 3px #14110c,0 0 16px 4px rgba(232,201,122,.65)}}` (the L855 blanket rule zeroes it under reduced motion — the canvas keeps its static glow). Legend: replace the `.mw-legend-row img{…}` rule with `.mw-legend-glyph{width:26px;flex:none;font-size:22px;line-height:1;text-align:center}`. Camp sheet: `.mw-camp-copy{font-family:var(--mono);font-weight:700;font-size:16px;line-height:1.5;margin:13px 0 0;color:#c9bda0}`, `.mw-camp-actions{display:flex;gap:10px;margin-top:20px}`, `.mw-camp-btn{flex:1;white-space:pre-line;text-align:center;font-family:var(--disp);font-size:8px;line-height:1.6;letter-spacing:0;padding:15px 8px;min-height:48px;background:#e8c97a;color:#14110c;border:0;border-radius:0;box-shadow:0 4px 0 #6b5c3c;cursor:pointer}`, `.mw-camp-btn.secondary{flex:0 0 auto;padding:15px 16px;background:transparent;border:2px solid #4a4032;color:#c9bda0;box-shadow:0 4px 0 #0d0b08}`.
    2. Viewport markup: inside `.mw-maze-viewport` after the vignette replace the chip row with `<div class="mw-map-chips" id="mw-map-chips"><button type="button" class="mw-map-chip" id="mw-chip-marks">MARKS</button><button type="button" class="mw-map-chip" id="mw-chip-centre">CENTRE</button><span class="mw-map-chips-gap" aria-hidden="true"></span><button type="button" class="mw-map-chip camp" id="btn-camp">MAKE CAMP</button></div>` and add `<div class="mw-party-pulse" id="mw-party-pulse" aria-hidden="true"></div>` right after the canvas; delete the flash element and its comment. Delete the flash function + its comment and the `flashTimer` let in the classic script (no caller exists).
    3. Camp sheet markup after the settings sheet: `<div id="mw-camp-sheet" class="mw-legend-sheet" hidden><div class="mw-legend-scrim" id="mw-camp-scrim"></div><div class="mw-legend-panel" role="dialog" aria-label="Make camp"><div class="mw-legend-head"><span>Make camp</span></div><p class="mw-camp-copy" id="mw-camp-copy"></p><div class="mw-camp-actions"><button type="button" class="mw-camp-btn" id="mw-camp-sleep"></button><button type="button" class="mw-camp-btn secondary" id="mw-camp-walk"></button></div></div></div>` (labels/copy are written from MAP_COPY at open time so the voice scan covers them).
    4. `const MAP_COPY = { camp: { copy: "Eight hours asleep in a corridor that has already killed better people than you. One ration. We roll to see what wanders past.", sleep: "SLEEP\n1 RATION", walk: "WALK\nON" } };` after COMBAT_COPY (one object literal; Plan 04 adds `stair`).
    5. Module bridge beside `window.__mzIconsApi`: `import { MAP_PALETTE, MARK_GLYPHS, MARK_SCALE, ONEWAY_ROTATION_DEG, MARKS_LEGEND, markForCell, legendFor } from "./src/browser/mapMarks.js";` and `window.__mzMapMarks = { MAP_PALETTE, MARK_GLYPHS, MARK_SCALE, ONEWAY_ROTATION_DEG, MARKS_LEGEND, markForCell, legendFor };` — placed BEFORE the classic boot call. Leave the icons preload/bridge lines exactly as they are (decision 3: assets and module stay; the renderer and legend simply stop reading them).
    6. draw(): delete the legacy palette table and rewrite the function's passes — `const M = window.__mzMapMarks; const P = M ? M.MAP_PALETTE : { fog: "#0b0a08", wall: "#2c261c", wallLight: "#4a3f2c", wallDark: "#16120c", floor: "#645c48", floorDark: "#3d372a", floorInset: "#554d3b", border: "#443a26", party: "#f4dc94" };` (a) fill the whole canvas `P.fog`; (b) per seen cell: wall → `fillRect(X, Y, CELL, CELL)` in `P.wall`, then the bevel: a 2px top strip and a 2px left strip in `P.wallLight`, a 2px bottom strip and a 2px right strip in `P.wallDark`; floor → `P.floorDark` when `c.dark` else `P.floor`, then `strokeRect(X + .5, Y + .5, CELL - 1, CELL - 1)` with `lineWidth 1` in `P.floorInset`; unseen cells are left as fog (no haze pass, no graph-paper pass, no ink-edge pass — all three legacy passes are deleted); (c) the outer border: `lineWidth 6`, `strokeStyle P.border`, `strokeRect(3, 3, size - 6, size - 6)`; (d) marks: for every seen cell with a feat, `const mark = M && M.markForCell(c); if (!mark) continue;` set `ctx.font = \`${Math.round(CELL * (M.MARK_SCALE))}px "Courier Prime", "Noto Sans Symbols", sans-serif\``, `textAlign "center"`, `textBaseline "middle"`, `fillStyle mark.color`; for `mark.key === "onewaydoor"` use `ctx.save(); ctx.translate(cx, cy); ctx.rotate((M.ONEWAY_ROTATION_DEG[c.dir] || 0) * Math.PI / 180); ctx.fillText(mark.glyph, 0, 0); ctx.restore();` else `ctx.fillText(mark.glyph, cx, cy + CELL * .03)`; (e) the party: keep the radial-gradient glow, then `fillStyle P.party; arc(cx, cy, CELL * .36)` — delete the PNG marker branch, the icon-ready helper and the icons bridge reads; (f) `positionCanvas();` stays last. Update the function's doc comment (palette from mapMarks.js; glyphs per decision 3; no live getComputedStyle reads).
    7. `positionCanvas()`: after setting the canvas left/top call `positionPartyPulse(rect)`; add `function positionPartyPulse(rect)`: `const ring = document.getElementById("mw-party-pulse"); if (!ring) return; ring.style.width = ring.style.height = CELL + "px"; ring.style.left = (rect.width / 2 + pan.x - CELL / 2) + "px"; ring.style.top = (rect.height / 2 + pan.y - CELL / 2) + "px";` (the party is always at the viewport centre + pan by positionCanvas's own transform).
    8. Legend: delete the classic legend data array (it lives in mapMarks.js now); `renderMarksLegend()` builds each row with createElement — `div.mw-legend-row` containing `span.mw-legend-glyph` (textContent = `M.MARK_GLYPHS[row.key].glyph`, `style.color` = its color), `span.mw-legend-row-name` (textContent row.name), `span.mw-legend-row-desc` (textContent row.desc) — iterating `window.__mzMapMarks.MARKS_LEGEND`; keep the build-once guard. `closeMarksLegend()` adds `lastDismissAt = Date.now();` (the settle window gates the first step after the sheet closes — MAP-08). The MARKS/CENTRE listeners stay as they are (pre-existing, not §6.3 decision buttons).
    9. Camp sheet functions (classic, after closeMarksLegend): `function openCampSheet()`: `if (!S || hasActiveEncounter()) return; if (railLocked()) { railPulse(); return; }` (comment: ruling 3 — no camp past an active choice), write `MAP_COPY.camp.copy` / `.sleep` / `.walk` into `#mw-camp-copy` / `#mw-camp-sleep` / `#mw-camp-walk` via textContent, `sheet.hidden = false; armEncounterButtons(); guardTap(document.getElementById("mw-camp-sleep"), () => { closeCampSheet(); if (window.mzMakeCamp) window.mzMakeCamp(); else makeCamp(); }); guardTap(document.getElementById("mw-camp-walk"), closeCampSheet);` `function closeCampSheet()`: `sheet.hidden = true; lastDismissAt = Date.now();`. Wire `document.getElementById("mw-camp-scrim").addEventListener("click", closeCampSheet);` (the scrim is the one allowed tap-to-dismiss) and change the camp chip's onclick to `document.getElementById("btn-camp").onclick = openCampSheet;` (the sheet's SLEEP is the camp dispatch; the engine's refusal reaches the rail as NOTHING TO EAT through dispatchWithToasts; the paint() short-state aria-label block stays untouched).
    10. Comments: describe retired things by role ("the Phase 33 chip row", "the 04-06 flash line", "the Phase-4 PNG marks") — never by the retired identifiers.
  </action>
  <verify>
    <automated>grep -c 'id="mw-map-chips"' mazeworld.html && grep -c 'id="mw-camp-sheet"' mazeworld.html && grep -c "^function openCampSheet()" mazeworld.html && grep -c "window.__mzMapMarks = {" mazeworld.html && test "$(grep -c 'mw-viewport-chips\|mw-flash\|flashMessage\|MAZE_CANVAS_COLORS\|iconReady' mazeworld.html)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-map-chips"' mazeworld.html` = 1; chip order by indexOf inside the viewport markup: `id="mw-chip-marks"` < `id="mw-chip-centre"` < `class="mw-map-chips-gap"` < `id="btn-camp"`; the camp button tag carries `class="mw-map-chip camp"`; `grep -c 'id="mw-party-pulse"' mazeworld.html` = 1.
    - `grep -c "mw-viewport-chips\|mw-flash\|flashMessage\|MAZE_CANVAS_COLORS\|iconReady" mazeworld.html` = 0 (raw file).
    - `.mazebox{` rule contains `padding:0` and `background:#14110c` and no `box-shadow:var(--shadow)`; `.mw-maze-viewport{` contains `user-select:none` and `touch-action:none`; `.mw-map-chips{` contains `left:10px;right:10px;top:10px`; `.mw-map-chip{` contains `font-size:7px`, `padding:9px 10px`, `border:2px solid #4a4032`, `box-shadow:0 3px 0 #0d0b08`; `.mw-map-chip.camp{` contains `background:#241d12;border-color:#6b5c3c`; `grep -c "@keyframes mwglow" mazeworld.html` = 1; `grep -c "^\.mw-legend-glyph{" mazeworld.html` = 1; `#btn-camp[data-short="1"]{` still exists.
    - `grep -c "mw-legend-row img" mazeworld.html` = 0 (the PNG legend-row rule is gone).
    - draw() region (`function draw()` → `/* ---------------- log`): contains `window.__mzMapMarks`, `P.fog`, `P.wall`, `P.wallLight`, `P.wallDark`, `P.floor`, `P.floorDark`, `P.floorInset`, `P.border`, `P.party`, `markForCell(c)`, `MARK_SCALE`, `ONEWAY_ROTATION_DEG[c.dir]`, `ctx.rotate(`, `ctx.fillText(mark.glyph`, `strokeRect(3, 3, size - 6, size - 6)`; zero `drawFeatureIcon`, zero `__mzIconMap`, zero `__mzIconsApi`, zero `getComputedStyle`, zero `globalAlpha` (the graph-paper pass is gone) — `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function draw()');const b=s.indexOf('/* ---------------- log',a);const r=s.slice(a,b).replace(/\/\/[^\n]*/g,'');process.exit(/drawFeatureIcon|__mzIconMap|__mzIconsApi|getComputedStyle|globalAlpha/.test(r)?1:0)"` exits 0.
    - `grep -c "window.__mzIconMap = await preloadIcons" mazeworld.html` = 1 (untouched); `git diff --stat -- src/browser/icons.js icons` prints nothing.
    - `grep -c "^function positionPartyPulse(rect)" mazeworld.html` = 1; positionCanvas region contains `positionPartyPulse(rect)`.
    - renderMarksLegend region (`function renderMarksLegend()` → `function openMarksLegend()`): contains `window.__mzMapMarks`, `MARKS_LEGEND`, `mw-legend-glyph`, `style.color`; zero `<img`, zero `icons/optimized`, zero `innerHTML` other than an optional `rows.innerHTML = ""` clear; `grep -c "const MARKS_LEGEND = \[" mazeworld.html` = 0.
    - `grep -c "^const MAP_COPY = {" mazeworld.html` = 1 and its literal contains `sleep: "SLEEP\n1 RATION"`, `walk: "WALK\nON"` and the camp copy sentence starting `Eight hours asleep`.
    - openCampSheet region (`function openCampSheet()` → `function closeCampSheet()`): contains `hasActiveEncounter()`, `railLocked()`, `railPulse();`, `armEncounterButtons();`, `guardTap(document.getElementById("mw-camp-sleep")`, `guardTap(document.getElementById("mw-camp-walk")`, `window.mzMakeCamp`; closeCampSheet and closeMarksLegend each contain `lastDismissAt = Date.now();`; `grep -c 'document.getElementById("btn-camp").onclick = openCampSheet;' mazeworld.html` = 1; `grep -c 'document.getElementById("mw-camp-scrim").addEventListener("click", closeCampSheet);' mazeworld.html` = 1.
    - `grep -c "lastDismissAt = Date.now()" mazeworld.html` = 3 (the dismissal transition + two sheet closes); `grep -c "encounterSettled()" mazeworld.html` = 2 (unchanged: its definition + engineMove's caller).
    - `node --test test/unit/shell-party-camp.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-input-guards.test.js test/unit/tutorial.test.js` exits 0 without edits.
  </acceptance_criteria>
  <done>The viewport wears the mock's chrome and palette with coloured glyph marks and a pulsing party ring; MARKS shows glyph rows; MAKE CAMP is a guarded sheet whose SLEEP dispatches camp; both sheets stamp the settle window on close.</done>
</task>

<task type="auto">
  <name>Task 3: shell-map-hud.test.js (HUD, chips, chrome, canvas, sheets, voice scan) + re-pin shell-gear-toolbar's UIF-05 chip-row tests</name>
  <files>test/unit/shell-map-hud.test.js, test/unit/shell-gear-toolbar.test.js</files>
  <read_first>
    - test/unit/shell-map-rail.test.js (Plan 02 — helpers and the concatenated-literal technique to copy), test/unit/shell-combat-over.test.js lines 300-350 (the BANNED matcher + object-literal leaf extractor)
    - test/unit/shell-gear-toolbar.test.js lines 60-75 (chipsMarkup()/mazefootMarkup() helpers) and 155-197 (the UIF-05 tests)
    - mazeworld.html regions written in Tasks 1-2
  </read_first>
  <action>
    1. Create `test/unit/shell-map-hud.test.js` (header: Phase 35 MAP-01/06/07/08, Plan 03) with tests for: (a) HUD markup — the four stat items in order FLOOR/DAY/SQUARES/RATIONS with their kept ids, the `Squares` label, the WP block ids, the DEV chip + gear still inside `.mw-hud-top`, zero character-line ids (concatenate the retired literals); (b) the chip strip element sits between `</header>` and the party rail, which sits before `<main`; (c) HUD CSS values (every literal from Task 1's criteria); (d) chip CSS values + the four tone rules; (e) CONDITION_TONE keys ⊆ CONDITION_COPY keys ∪ {affliction}; CONDITION_EXPLAIN has every CONDITION_TONE key plus `default`; every CONDITION_EXPLAIN value and every MAP_COPY leaf is non-empty and clear of BANNED; (f) paintConditions — createElement("button"), data-tone from CONDITION_TONE with the polarity fallback, guardTap + mzRailLine wiring, the key gate, the foeEffect/affliction label chain untouched (re-assert the three foe-effect-chip anchors); (g) paint() writes the WP text/fill with the 25/50/22 thresholds and no character-line writes; (h) chrome — chip ids/order/classes, pulse element, zero retired chip-row/flash literals, `.mw-map-chip.camp` colours; (i) canvas — draw() region positive/negative pins from Task 2's criteria, the preload line untouched; (j) legend — glyph rows, no img/PNG path, MARKS_LEGEND from the bridge; (k) camp sheet — markup ids, MAP_COPY.camp literals, openCampSheet's hasActiveEncounter/railLocked/arm/guardTap wiring, closeCampSheet + closeMarksLegend settle stamps, scrim listener, btn-camp onclick; (l) settle-stamp count = 3 and encounterSettled() count = 2; (m) BEHAVIOUR — import mapMarks.js and assert `markForCell({feat:"one", dir:"S"})` maps to onewaydoor and `ONEWAY_ROTATION_DEG.S === 180` (the rotation draw() applies).
    2. Re-pin `test/unit/shell-gear-toolbar.test.js`: `chipsMarkup()` slices from `<div class="mw-map-chips"` to `<section class="mw-overlay"` (covers the chip row, the pulse ring and the control bar — the control bar is retired in Plan 04, so the mazefoot test stays for now); UIF-05 test 1: order marks < centre < camp and the camp tag carries `class="mw-map-chip camp"`; test 4 (CSS): `.mw-map-chips{` has `left:10px` / `right:10px`, `.mw-map-chips-gap{flex:1}` exists, `#btn-camp[data-short="1"]{` exists, `.mazefoot{` still has `justify-content:center` (Plan 04 retires this line); the remaining UIF-01/UIF-05 tests stay as they are.
    3. Run both files plus the neighbours.
  </action>
  <verify>
    <automated>node --test test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-party-camp.test.js test/unit/foe-effect-chip.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-input-guards.test.js test/unit/shell-map-rail.test.js && npm test 2>&1 | grep -q "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-map-hud.test.js` exits 0 with at least 14 tests; `grep -c "BANNED" test/unit/shell-map-hud.test.js` >= 1; `grep -c "CONDITION_EXPLAIN\|MAP_COPY" test/unit/shell-map-hud.test.js` >= 2.
    - `grep -c "mw-map-chips" test/unit/shell-gear-toolbar.test.js` >= 2; `npm test` prints `# fail 0`.
  </acceptance_criteria>
  <done>HUD, chips, chrome, canvas palette/glyphs, legend and camp sheet are pinned with source assertions and the new copy is voice-scanned; the Phase 33 chip tests are re-pinned to the new row.</done>
</task>

</tasks>

<verification>
- `npm test` `# fail 0`; `npm run build:www` exit 0 (www/src/browser/mapMarks.js present); `git diff --stat -- engine content test/parity src/browser/icons.js icons` empty; master hash unchanged.
- SUMMARY contract (deferred UAT — MAP-10): end `35-03-SUMMARY.md` with `## Human verification (deferred to end of run)` listing at least: (1) the HUD reads FLOOR (gold) · DAY · SQUARES · RATIONS left, x/y WP over a thin bar right; RATIONS turns red at 2; the bar turns gold under half and red under a quarter; no name/class line; the gear opens Settings; (2) drink a haste potion / get poisoned — a tone-coloured chip strip appears under the HUD with the label and a count; tapping the chip puts its explanation in the rail; the strip vanishes when the condition ends; (3) the maze draws dark fog, raised dark-brown wall blocks with a light/dark bevel, warm-stone floor squares with a thin inset, a thick outer border; every mark is a coloured glyph (red ● encounter, purple ◆ teleport, green ▲ door pointing its way, red ✕ trap, ▪ box, ⧗ crevice, ▼ stair) — none renders as an empty box; the party is a gold dot with a pulsing ring (no pulse with reduced motion); (4) MARKS opens WHAT THE MARKS MEAN with coloured glyph rows; scrim closes it; the first step after closing within a quarter second is ignored; (5) CENTRE snaps the map back on the party; (6) MAKE CAMP opens the sheet with the copy and SLEEP / 1 RATION + WALK / ON; SLEEP with rations → CAMP MADE (green) in the rail with the rest line; SLEEP without rations → NOTHING TO EAT (red) with the need/have numbers; a tap on SLEEP within 250 ms of the sheet opening is swallowed; WALK ON closes it; with a Joiner card pending the chip only pulses the rail card.
</verification>

<success_criteria>
- ROADMAP success criterion 4 (HUD + condition chips match the mock; MARKS / CENTRE / MAKE CAMP work from the top chips with their sheets) and the canvas half of the goal (palette, coloured mark glyphs, gold pulsing party marker, vignette) are source-provable; decision 3 and ruling 5 are recorded in the objective.
</success_criteria>

<output>
Create `.planning/phases/35-map-screen-rebuild/35-03-SUMMARY.md` when done (with the deferred-UAT section per the verification contract).
</output>
