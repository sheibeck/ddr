---
phase: 35-map-screen-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/rail.js
  - src/browser/tapStep.js
  - src/browser/mapMarks.js
  - test/unit/rail.test.js
  - test/unit/tapStep.test.js
  - test/unit/mapMarks.test.js
autonomous: true
requirements: [MAP-02, MAP-03, MAP-04, MAP-07]
tags: [vanilla-js, pure-modules, rail-view-model, tap-to-step, map-marks, voice-scan, tdd]

must_haves:
  truths:
    - "railCardFor(type, events, folded, ctx) turns one out-of-combat dispatch into ONE rail card {tone, icon, title, lines:[{text, roll}], hold} — every folded toastsForAction entry becomes a line (uncapped), lines stack newest-first (descending event idx), the head line (lowest PRIORITY, then lowest idx) picks the family's icon/title/tone, hold is the max family hold across the lines (MAP-03)"
    - "Every TOAST_FOR key resolves through railFamilyFor(type, tone, priority) to a family with a non-empty title, a tone in RAIL_TONES and a hold in [2200, 6000]; PRIORITY.block entries are always dull (the re-read partition invariant: rail or log)"
    - "RAIL_DIRECT = floorChanged, dayBegan, findTaken, findLeft is a strict subset of ORACLE_ONLY and each yields a rail line from the Oracle narration (dice stripped) so the retired Move-on card's floor arrival and the find outcome still reach the player (MAP-03/04)"
    - "rollLineFor(event, narrate) returns the Oracle sentence with its dice when the narration carries a roll span, else a generic 'roll N · M to clear · −K hp' line built from a numeric event.roll (need/hurt optional), else null — so climb events (no roll field today) show no dice line and a later additive engine payload lights them up with zero shell change (orchestrator decision 1)"
    - "resolveStep(pos, target, isOpen) returns {kind:'here'} on the party's own square, {kind:'step', dir} choosing the dominant axis first and the other axis as fallback, and {kind:'blocked'} when both candidate squares fail isOpen — never a diagonal, never a raw tap coordinate (MAP-02)"
    - "inspectCell(cell, legendFor) returns the four inspect cards (UNWALKED / SOLID ROCK / mark legend row as odd 3400 / EMPTY CORRIDOR) from a plain {seen, wall, feat} cell (MAP-02)"
    - "MARK_GLYPHS carries the seven CONTEXT glyph/colour pairs plus wall and party; MARKS_LEGEND keeps the nine legend rows' names and descriptions verbatim, keyed by glyph key, not PNG name (MAP-07, orchestrator decision 3)"
    - "Every RAIL_COPY string leaf and every MARKS_LEGEND name/desc is non-empty and clear of content/safety-wordlist.js BANNED"
  artifacts:
    - path: "src/browser/rail.js"
      provides: "RAIL_TONES, RAIL_HOLD, RAIL_COPY, RAIL_FAMILY, RAIL_DIRECT, railFamilyFor, rollLineFor, railCardFor, railLineCard, emptyRail, railPush, railClear, railAnnouncement"
      min_lines: 150
    - path: "src/browser/tapStep.js"
      provides: "DIR_VECTORS, TAP_MAX_TRAVEL_PX, HOLD_MS, resolveStep, inspectCell"
      min_lines: 50
    - path: "src/browser/mapMarks.js"
      provides: "MAP_PALETTE, MARK_GLYPHS, MARK_SCALE, ONEWAY_ROTATION_DEG, MARKS_LEGEND, markForCell, legendFor"
      min_lines: 50
    - path: "test/unit/rail.test.js"
      provides: "family coverage over every TOAST_FOR key, RAIL_DIRECT subset proof, roll-line precedence, stacking/head/hold rules, push/clear/announcement seq, voice scan"
    - path: "test/unit/tapStep.test.js"
      provides: "here/step/blocked cases incl. dominant-axis and fallback, ties, out-of-grid; inspectCell four cards"
    - path: "test/unit/mapMarks.test.js"
      provides: "glyph table completeness, legend row voice scan, markForCell mapping for every engine feat, rotation table"
  key_links:
    - from: "src/browser/rail.js"
      to: "src/browser/toasts.js"
      via: "imports PRIORITY, ORACLE_ONLY, TOAST_FOR, narrativeToastText, oracleDetailText — never re-implements the fold"
      pattern: "from \"./toasts.js\""
    - from: "src/browser/rail.js"
      to: "src/browser/eventNarration.js"
      via: "ctx.narrate defaults to narrateEvent for the roll line and the RAIL_DIRECT lines"
      pattern: "narrateEvent"
    - from: "src/browser/mapMarks.js"
      to: "src/browser/icons.js"
      via: "imports the pure featureKeyForCell mapper only (never preloadIcons/drawFeatureIcon)"
      pattern: "import \\{ featureKeyForCell \\} from \"./icons.js\""
  prohibitions:
    - "No edits under engine/, content/, test/parity/; no edit to src/browser/toasts.js, src/browser/controls.js, src/browser/icons.js, src/browser/fightLog.js (their tests stay byte-identical green)"
    - "No DOM, window, timer, Math.random or Date.now access in the three new modules (pure, node --test importable)"
    - "No fabricated dice numbers: rollLineFor never invents a roll/need the event payload does not carry"
    - "No mazeworld.html edit in this plan (Wave 2 owns the shell)"
---

<objective>
Build the three pure presentation modules the rebuilt map screen is wired to in Plans 02–04, exactly as Phase 34-01 built `fightLog.js`/`combatMenu.js`/`combatPanel.js` before any shell wiring: `src/browser/rail.js` (the RAIL view-model — event family → icon/title/tone/hold, the roll-line rule, one card per dispatch, seq-gated push/announce), `src/browser/tapStep.js` (tap-to-step direction resolution and hold-inspect copy — the "step toward a distant tap" function `controls.js` never had, RESEARCH Pitfall 2), and `src/browser/mapMarks.js` (the mock's canvas palette, the coloured mark glyphs and the legend rows the renderer and the MARKS sheet both read — orchestrator decision 3, the deliberate reversal of the Phase-4 PNG-icon decision). Each ships with its `node --test` suite and a BANNED voice scan of every new player-facing string.

**Orchestrator decisions recorded here (the executor never guesses):**
- **Decision 1 (climb dice, RESEARCH OQ1): NO engine change.** `fellClimbing`/`fellInGorge`/`climbedOver`/`leaptOver` (`engine/movement.js` L204-208) carry no `roll`/`need`, so `rollLineFor` yields null for them today and the rail shows the outcome line only (the narration already states the hp lost). `rollLineFor` is written generically — narration roll span first, else a numeric `event.roll` (+ `need`, + `hurt`) — so a later additive payload lights climb dice up with zero shell change. The gap is a deferred follow-up ("climb dice payload — quick task after UAT"), restated in Plan 05's SUMMARY.
- **Decision 4 (no PICK THE LOCK):** chest locks auto-resolve in the engine (`chestLockRolled`/`chestOpened`/`chestLocked`, `engine/encounters.js#openChest`); the rail's "A LOCKED BOX" family is an auto-clearing card with no action row; the only chest decision is the existing `S.pendingFind` TAKE IT / LEAVE IT (Plan 02). `RAIL_COPY` therefore carries no lock-pick button label.
- **Decision 3 (marks):** coloured text glyphs (● ◆ ▲ ✕ ▪ ⧗ ▼ at 60 % of the cell) replace the PNG draws on the canvas and the `<img>` rows in the legend; `icons/*.png` and `src/browser/icons.js` stay on disk untouched (no asset deletion) — `mapMarks.js` imports only the pure `featureKeyForCell` mapper from `icons.js`.
- **Toast retirement is total (decision 6) but `toasts.js` stays** as the pure folder: `rail.js` consumes `toastsForAction(..., {limit: Infinity, withIdx: true})` output and `TOAST_FOR`/`ORACLE_ONLY`/`PRIORITY`/`narrativeToastText`/`oracleDetailText` unchanged; the `toastTable`/`toastsCoverage` partition invariants are re-read as "rail or log" by `rail.test.js`'s family-coverage test.

**Claude's discretion exercised (CONTEXT "Claude's Discretion"):** the title/tone table per event family lives in `RAIL_FAMILY` (below, exact strings); the tap-to-step helper is a NEW module `src/browser/tapStep.js` (controls.js stays untouched so `controls.test.js`'s `TAP_MAX_DURATION_MS = 350` pin holds; the hold threshold is tapStep.js's own `HOLD_MS = 450`); the pending-decision lock is represented by `window.__mzRail.pending` (Plan 02) — this plan only shapes `emptyRail()` with a `pending: null` slot.

Purpose: the view-models the shell wires in Waves 2–4, testable without a DOM.
Output: `src/browser/rail.js`, `src/browser/tapStep.js`, `src/browser/mapMarks.js`, `test/unit/rail.test.js`, `test/unit/tapStep.test.js`, `test/unit/mapMarks.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-map-screen-rebuild/35-CONTEXT.md
@.planning/phases/35-map-screen-rebuild/35-RESEARCH.md
@.planning/phases/34-combat-screen-rebuild/34-01-SUMMARY.md
@design/Mazeworld Map Panel.dc.html
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Phase 35, all plans — for reference; this plan creates the starred ones)

Modules / exports:
- * `src/browser/rail.js`: `RAIL_TONES`, `RAIL_HOLD`, `RAIL_COPY`, `RAIL_FAMILY`, `RAIL_DIRECT`, `railFamilyFor(type, tone, priority)`, `rollLineFor(event, narrate)`, `railCardFor(type, events, folded, ctx)`, `railLineCard(title, line, tone, hold, icon)`, `emptyRail()`, `railPush(rail, card)`, `railClear(rail)`, `railAnnouncement(rail, announcedSeq)`
- * `src/browser/tapStep.js`: `DIR_VECTORS`, `TAP_MAX_TRAVEL_PX`, `HOLD_MS`, `resolveStep(pos, target, isOpen)`, `inspectCell(cell, legendFor)`
- * `src/browser/mapMarks.js`: `MAP_PALETTE`, `MARK_GLYPHS`, `MARK_SCALE`, `ONEWAY_ROTATION_DEG`, `MARKS_LEGEND`, `markForCell(cell)`, `legendFor(feat)`
- mazeworld.html classic script (Plans 02–04): `MAP_COPY`, `CONDITION_TONE`, `CONDITION_EXPLAIN`, `railLocked()`, `renderRail()`, `syncRailLive(card)`, `railPulse()`, `railShow(card)`, `openCampSheet()`, `closeCampSheet()`, `tapStep(clientX, clientY)`, `inspectAt(clientX, clientY)`, `positionPartyPulse()`; module script: `stepTargetsExit(dir)`, `stepNow(dir)`
Globals (window): `__mzRail`, `__mzRailVM`, `__mzTapStep`, `__mzMapMarks`, `__mzStair`, `__mzDescend`, `mzRailPulse`, `mzRailLine`, `renderRail`
DOM ids: `mw-rail`, `mw-rail-icon`, `mw-rail-title`, `mw-rail-lines`, `mw-rail-peek`, `mw-rail-actions`, `mw-rail-live`, `mw-rail-climb`, `mw-hud-wp`, `mw-hud-wpfill`, `mm-conditions` (kept, restyled), `mw-map-chips`, `mw-chip-marks` / `mw-chip-centre` / `btn-camp` (kept), `mw-camp-sheet`, `mw-camp-scrim`, `mw-camp-sleep`, `mw-camp-walk`, `mw-party-pulse`, `find-drop-shelf` (kept), `a-join-yes` / `a-join-no` / `a-find-take` / `a-find-leave` (kept, now in the rail)
CSS classes: `mw-rail mw-rail-row mw-rail-icon mw-rail-body mw-rail-title mw-rail-lines mw-rail-line mw-rail-roll mw-rail-peek mw-rail-actions mw-rail-btn (secondary) mw-rail-new mw-rail-pulse`, `mw-hud-wp mw-hud-wp-text (low) mw-hud-wptrack mw-hud-wpfill (mid|low)`, `mw-cond-strip mw-cond[data-tone] mw-cond-detail`, `mw-map-chips mw-map-chip (camp)`, `mw-party-pulse`, `mw-legend-glyph`, `mw-camp-copy mw-camp-actions mw-camp-btn (secondary)`; keyframes `mwglow`, `mwpulse`
Tests: * `test/unit/rail.test.js`, * `test/unit/tapStep.test.js`, * `test/unit/mapMarks.test.js`, `test/unit/shell-map-rail.test.js` (02), `test/unit/shell-map-hud.test.js` (03), `test/unit/shell-map-viewport.test.js` (04)
Retired (Plans 02–04): the DR13 toast host element, its CSS block and the global toast builder + lifetime bridge; the MAX_TOASTS / toastLifetime / CARD_EVENTS imports in the module script; the Phase 33 D-pad + control bar markup, CSS and click listener; the `.mw-viewport-chips` row; the HUD character line (name / class / hp readout); the legacy beats "Move on" card path (`a-next`, dismiss-slot fill, `stepping()`); the unused flash toast; the joiner/find branches of `renderEncounter` (re-homed in `renderRail`); the PNG draw path in `draw()` and the `<img>` legend rows.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/browser/rail.js — the RAIL view-model (families, roll line, one card per dispatch, seq push/announce) + rail.test.js</name>
  <files>src/browser/rail.js, test/unit/rail.test.js</files>
  <read_first>
    - src/browser/toasts.js lines 1-110 (TONES, PRIORITY, MAX_TOASTS, CARD_EVENTS, NARRATIVE_ACTIONS), 139-172 (narrativeToastText / oracleDetailText — reuse, never re-implement), 174-200 (ORACLE_ONLY — RAIL_DIRECT must be a subset), 859-895 (toastsForAction's withIdx return shape `{ text, tone, priority, idx, type? }` — the `folded` argument railCardFor receives), 897-945 and 1290-1303 (the movement/camp/chest builders: oneWayBlocked, climbedOver, leaptOver, fellClimbing, fellInGorge, rested, wentHungry, wanderingMonster, campFailed, teleported, leveled, tableFour, tableFourNoop, trapSprung, chestOpened, chestLockRolled, chestLocked)
    - src/browser/fightLog.js lines 1-60 and 74-130 (the pure fold-then-bridge precedent: fightLogLinesFor's `{limit: Infinity, withIdx: true}` call, the roll via oracleDetailText(narrateEvent(events[idx])), appendFightLog's seq/id discipline, fightLogAnnouncement's seq gate — mirror the shapes, do not import fightLog.js)
    - src/browser/eventNarration.js lines 85-100, 120-165, 544-548, 661 (the narration builders for the movement/camp/chest/find events — note floorChanged/leveled/dayBegan carry `depth`/`level`/`day`; fellClimbing/fellInGorge embed the hp inside a `hurt` span with NO roll span; wanderingMonster and trapSprung DO carry a roll span), 718-724 (narrateEvent)
    - engine/movement.js lines 200-212 (the four climb/leap event pushes: `{ type, hurt }` on failure, `{ type }` on success — no roll/need), 445-457 and 526-567 (dayBegan `{day, camped}`, rested `{amount, doubled?}`, wentHungry `{cost}`, wanderingMonster `{hours, bard}`, campFailed `{need, have, members?}`), 691-701 (descend → spGained + floorChanged(depth))
    - engine/events.js lines 30-34 (leveled(level, wpGain)) and the floorChanged builder
    - test/unit/fightLog.test.js (the test-file conventions for a pure module: import the module, synthetic event arrays, `node --test`)
    - test/unit/shell-combat-over.test.js lines 300-350 (the BANNED/ALLOWLIST matcher + leaf extractor to copy for the voice scan)
    - design/Mazeworld Map Panel.dc.html lines 112-131 (RAIL style/tone block + hold times) and 141-153 (SAMPLE RAIL EVENTS — titles/lines/tones; the PICK THE LOCK sample is the toy mechanic and is NOT built)
  </read_first>
  <behavior>
    - railFamilyFor("trapSprung", "hurt", PRIORITY.them) → { icon: "✕", title: "A TRAP", tone: "bad", hold: 4200 }; railFamilyFor("trapAvoided", "miss", PRIORITY.other) → tone "good", title "A TRAP"; railFamilyFor("teleported", ...) → { icon: "◆", title: "TELEPORTED", tone: "odd" }; oneWayBlocked → { "▲", "ONE-WAY DOOR", "odd", 3400 }; chestOpened / chestLockRolled / scrollFound → { "▪", "A LOCKED BOX", "good" }; chestLocked → { "▪", "THE LOCK HOLDS", "bad" }; climbedOver → { "⧗", "CLIMBED", "good" }; leaptOver → { "⧗", "CLEARED IT", "good" }; fellClimbing / fellInGorge → { "⧗", "FELL", "bad" }; flownOver / phasedThrough → { "⧗", "OVER IT", "odd" }; floorChanged → { "▼", "FLOOR {n}", "odd", 5000 }; leveled → { "★", "SKILL LEVEL {n}", "good", 6000 }; dayBegan → { "☾", "DAY {n}", "dull", 3000 }; rested → { "☾", "CAMP MADE", "good", 5200 }; wentHungry / campFailed → { "☾", "NOTHING TO EAT", "bad" }; wanderingMonster → { "●", "SOMETHING WANDERED IN", "bad" }; tableFour → { "●", "THE DOT", "info" }; tableFourNoop → { "●", "THE DOT", "dull", 2400 }; joinerJoined → { "◇", "COMPANY", "good" }; joinerDeclined / joinerLeft → { "◇", "COMPANY", "dull" }; findTaken → { "▪", "TAKEN", "good" }; findLeft → { "▪", "LEFT IT", "dull", 2400 }; heightsFear / waterFear / trappedPanic → { "·", "AFRAID", "bad" }
    - Fallback (any TOAST_FOR key not in RAIL_FAMILY): priority === PRIORITY.block → { "·", "NOTHING DOING", "dull", 2400 } regardless of tone; else by toast tone: hurt → { "·", "THAT HURT", "bad" }, hit → { "·", "WELL THEN", "good" }, miss → { "·", "NOTHING", "dull", 2400 }, magic → { "·", "MAGIC", "odd" }, dodge → { "·", "CLOSE", "info" }, beat/other → { "·", "MEANWHILE", "info" }; hold defaults to RAIL_HOLD.default (4200) unless the family says otherwise
    - Every key of TOAST_FOR resolves to a family whose title is non-empty, tone ∈ RAIL_TONES, 2200 ≤ hold ≤ 6000
    - rollLineFor({type:"trapSprung", name:"Pit", dmg: 4}, narrateEvent) → the narration's roll sentence ("Pit finds you first. 4 hp." — dice kept, tags stripped); rollLineFor({type:"fellClimbing", hurt: 3}, narrateEvent) → null (no roll span, no `roll` field); rollLineFor({type:"fellClimbing", hurt: 3, roll: 7, need: 4}, () => "") → "roll 7 · 4 to clear · −3 hp"; rollLineFor({type:"x", roll: 12}, () => "") → "roll 12"; rollLineFor({type:"x", hurt: 3}, () => "") → null (hurt alone never fabricates a dice line)
    - railCardFor("move", [moved, trapSprung(dmg 4), leveled(3, 5)], folded, {narrate}) where folded = toastsForAction("move", events, {narrate}, {limit: Infinity, withIdx: true}) → ONE card: lines ordered by descending idx (the leveled line first, then the trap line), head = the folded[0] entry (trapSprung, PRIORITY.them beats leveled's PRIORITY.feature) so icon "✕", title "A TRAP", tone "bad"; hold = max(4200, 6000) = 6000; the trap line carries its roll, the leveled line has no roll
    - railCardFor("move", [moved, floorChanged(3)], [], {narrate}) → a card from RAIL_DIRECT alone: title "FLOOR 3" (the {n} placeholder resolved from event.depth), tone odd, one line = narrativeToastText(narrate(floorChanged)) ("Floor 3. The air gets worse, and takes it personally."), hold 5000; dayBegan resolves {n} from event.day, leveled from event.level
    - railCardFor("move", [moved], [], ctx) → null (nothing to show); railCardFor("takeFind", [findTaken(...)], [], ctx) → title "TAKEN" good
    - RAIL_DIRECT ⊂ ORACLE_ONLY (every member is ORACLE_ONLY) and RAIL_DIRECT ∩ Object.keys(TOAST_FOR) = ∅
    - emptyRail() → { seq: 0, card: null, pending: null }; railPush(rail, card) → a NEW object with seq+1, card = {...card, seq: newSeq}, pending preserved, the input untouched; railClear(rail) → same seq, card null, pending preserved
    - railAnnouncement(rail, announcedSeq): { seq: 0, text: "" } for a null card or when rail.card.seq <= announcedSeq; else { seq: card.seq, text: `${title}. ${lines.map(l => l.text).join(" ")}` }
    - railLineCard("YOU ARE HERE", "For the moment…", "dull", 2200) → { icon: "·", title, lines: [{ text, roll: null }], tone: "dull", hold: 2200 } (icon overridable by the fifth arg)
    - Voice: every string leaf of RAIL_COPY (recursively, incl. RAIL_FAMILY titles) is non-empty and clear of BANNED (ALLOWLIST honoured)
  </behavior>
  <action>
    1. Create `src/browser/rail.js` with a header comment mirroring fightLog.js's purity contract (presentation only; no DOM/window/timers/storage; imports from `./toasts.js` — PRIORITY, ORACLE_ONLY, TOAST_FOR, narrativeToastText, oracleDetailText — and `narrateEvent` from `./eventNarration.js`; never from engine/). State in the header that this module is the OUT-OF-COMBAT twin of fightLog.js: the same `toastsForAction(..., {limit: Infinity, withIdx: true})` fold feeds both surfaces (rail or log — the Phase 32 partition re-read), and that the family table is the one place the rail's icon/title/tone/hold vocabulary lives.
    2. Export `RAIL_TONES = Object.freeze(["info", "good", "bad", "odd", "dull"])` and `RAIL_HOLD = Object.freeze({ default: 4200, dull: 2400, dullShort: 2200, mark: 3400, day: 3000, floor: 5000, camp: 5200, level: 6000 })`.
    3. Export `RAIL_COPY` as ONE object literal holding every player-facing string this module owns, so the voice scan can walk it: `idleTitle: "FLOOR {n} · NOTHING IS HAPPENING"`, `idleLine: "Tap anywhere to step one square that way. Hold a square to inspect it, drag to look around, pinch to zoom."`, `peekStep: "TAP TO STEP"`, `peekPinch: "PINCH OUT"`, `noWay: { title: "NO WAY THAT DIRECTION", line: "Rock, both ways you tried. The corridor has opinions." }`, `here: { title: "YOU ARE HERE", line: "For the moment, and with no particular claim to it." }`, `unwalked: { title: "UNWALKED", line: "The lantern does not reach. Walk it and find out." }`, `rock: { title: "SOLID ROCK", line: "A square of it, same as the corridor. No way through." }`, `empty: { title: "EMPTY CORRIDOR", line: "Walked, lit, and entirely uninteresting. Enjoy it." }`, `joiner: { title: "COMPANY", yes: "TAKE THEM ALONG", no: "LEAVE THEM" }`, `find: { title: "SOMETHING WORTH TAKING", take: "TAKE IT", takeNow: "TAKE IT NOW", leave: "LEAVE IT", full: "Your bag is full ({have}/{slots}). Drop something to make room, or leave it." }`, `climb: { retry: "CLIMB IT" }`, `quit: { title: "BACK AGAIN TO QUIT", line: "Press back once more and this delve is abandoned. Nobody will write it down." }`, `fallback: { block: "NOTHING DOING", hurt: "THAT HURT", hit: "WELL THEN", miss: "NOTHING", magic: "MAGIC", dodge: "CLOSE", beat: "MEANWHILE" }`.
    4. Export `RAIL_FAMILY` — a frozen object keyed by event type with `{ icon, title, tone, hold? }` exactly as the behavior block lists (trap family incl. trapAvoided/trapDisarmed/trapDoubled/trapPoisoned — the avoided/disarmed rows are tone good, the sprung/doubled/poisoned rows bad; teleported; oneWayBlocked; chestOpened/chestLockRolled/scrollFound/chestLocked; climbedOver/leaptOver/fellClimbing/fellInGorge/flownOver/phasedThrough; floorChanged/leveled/dayBegan; rested/wentHungry/campFailed/wanderingMonster; tableFour/tableFourNoop; joinerJoined/joinerDeclined/joinerLeft; findTaken/findLeft; heightsFear/waterFear/trappedPanic). Titles with `{n}` are resolved by railCardFor from the head event's `depth` / `level` / `day` field (first numeric of those three present).
    5. Export `RAIL_DIRECT = new Set(["floorChanged", "dayBegan", "findTaken", "findLeft"])` with a comment: these are ORACLE_ONLY in toasts.js (a dedicated card/HUD cue used to carry them); the Move-on card and the find prompt overlay are retired this phase, so the rail surfaces them from the Oracle narration — toasts.js is NOT edited (the partition invariant stays byte-identical).
    6. Export `railFamilyFor(type, tone, priority)`: RAIL_FAMILY[type] if present (with hold defaulted to RAIL_HOLD.default); else the block/tone fallback rows from RAIL_COPY.fallback with icon "·" (block/miss → dull 2400; hurt → bad; hit → good; magic → odd; dodge/beat/anything → info; hold RAIL_HOLD.default unless dull).
    7. Export `rollLineFor(event, narrate)`: (a) `const sentence = oracleDetailText(narrate(event))` — if non-empty return it; (b) else if `typeof event.roll === "number"` build `roll ${event.roll}` + (`typeof event.need === "number"` ? ` · ${event.need} to clear` : "") + (`typeof event.hurt === "number"` ? ` · −${event.hurt} hp` : "") and return it; (c) else null. Comment the orchestrator decision 1 rationale (climb events carry no roll today → null; additive payload later lights it up; never fabricate).
    8. Export `railCardFor(type, events, folded, ctx = {})`: `narrate = ctx.narrate || narrateEvent`; build `lines` = for each folded entry `{ text: narrativeToastText(t.text) || t.text, roll: t.idx != null ? rollLineFor(events[t.idx], narrate) : null, idx: t.idx ?? -1, priority: t.priority, type: t.type }` PLUS for each event e (with its index) whose type is in RAIL_DIRECT: `{ text: narrativeToastText(narrate(e)), roll: rollLineFor(e, narrate), idx, priority: PRIORITY.other, type: e.type }` (skip if the text strips to empty); if no lines → return null; `head` = the line with the lowest priority, ties by lowest idx; `fam = railFamilyFor(head.type, toneOf(head), head.priority)` where toneOf reads the folded entry's toast tone (RAIL_DIRECT lines have none → "beat"); title = fam.title with `{n}` replaced from the head event's depth/level/day; `hold = Math.max(...lines.map(l => railFamilyFor(l.type, …).hold))`; lines sorted by descending idx; return `{ tone: fam.tone, icon: fam.icon, title, lines: lines.map(({ text, roll }) => ({ text, roll })), hold }`. No `seq` here — railPush stamps it.
    9. Export `railLineCard(title, line, tone, hold, icon = "·")` → `{ icon, title, lines: [{ text: line, roll: null }], tone, hold }`; `emptyRail()` → `{ seq: 0, card: null, pending: null }`; `railPush(rail, card)` → `{ seq: s, card: { ...card, seq: s }, pending: (rail || emptyRail()).pending }` with `s = (rail ? rail.seq : 0) + 1`, never mutating the input; `railClear(rail)` → `{ ...rail, card: null }`; `railAnnouncement(rail, announcedSeq)` → per the behavior block.
    10. Create `test/unit/rail.test.js` (header: Phase 35 MAP-03/MAP-04, Plan 01) covering every behavior bullet above: import TOAST_FOR/ORACLE_ONLY/PRIORITY/toastsForAction from toasts.js and narrateEvent to build real folded input (the `move` fixtures: trapSprung + leveled; floorChanged only; a bare moved; a fellClimbing with and without a synthetic roll field; a camp with rested + dayBegan + wanderingMonster); a coverage loop over `Object.keys(TOAST_FOR)` calling railFamilyFor with the builder's own `{ tone, priority }` from `TOAST_FOR[type]({})` (defended: wrap in try/catch and fall back to tone "beat"/PRIORITY.other) asserting title non-empty, tone ∈ RAIL_TONES, hold within [2200, 6000]; the RAIL_DIRECT subset/disjointness proof; push/clear/announcement seq tests; the voice scan (copy the BANNED/ALLOWLIST matcher from shell-combat-over.test.js lines 300-320; walk RAIL_COPY and RAIL_FAMILY recursively).
  </action>
  <verify>
    <automated>node --test test/unit/rail.test.js && node --test test/unit/toastsForAction.test.js test/unit/toastTable.test.js test/unit/toastsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/fightLog.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/rail.test.js` exits 0 with at least 14 tests.
    - `grep -c "^export const RAIL_TONES\|^export const RAIL_HOLD\|^export const RAIL_COPY\|^export const RAIL_FAMILY\|^export const RAIL_DIRECT" src/browser/rail.js` = 5; `grep -c "^export function railFamilyFor(type, tone, priority)\|^export function rollLineFor(event, narrate)\|^export function railCardFor(type, events, folded, ctx = {})\|^export function railLineCard(\|^export function emptyRail()\|^export function railPush(rail, card)\|^export function railClear(rail)\|^export function railAnnouncement(rail, announcedSeq)" src/browser/rail.js` = 8.
    - `grep -c 'from "./toasts.js"' src/browser/rail.js` = 1; `grep -c 'from "./eventNarration.js"' src/browser/rail.js` = 1; `grep -c "from \"../../engine\|from \"../engine\|engine/" src/browser/rail.js` = 0.
    - `node -e "const s=require('fs').readFileSync('src/browser/rail.js','utf8').replace(/\/\/[^\n]*/g,'').replace(/\/\*[\s\S]*?\*\//g,'');process.exit(/document\.|window\.|setTimeout|Math\.random|Date\.now/.test(s)?1:0)"` exits 0 (pure).
    - `node -e "import('./src/browser/rail.js').then(m=>{const {RAIL_DIRECT}=m;return import('./src/browser/toasts.js').then(t=>{for(const k of RAIL_DIRECT){if(!t.ORACLE_ONLY.has(k)||t.TOAST_FOR[k])process.exit(1)}process.exit(0)})})"` exits 0.
    - `node -e "import('./src/browser/rail.js').then(m=>{const r=m.rollLineFor({type:'fellClimbing',hurt:3},()=>'');const r2=m.rollLineFor({type:'fellClimbing',hurt:3,roll:7,need:4},()=>'');process.exit(r===null&&r2==='roll 7 · 4 to clear · −3 hp'?0:1)})"` exits 0.
    - `git diff --stat -- src/browser/toasts.js src/browser/fightLog.js src/browser/eventNarration.js engine content test/parity` prints nothing.
    - The five pre-existing suites in the verify command exit 0 untouched.
  </acceptance_criteria>
  <done>rail.js folds any out-of-combat dispatch into one tone/title/hold-typed card with a generic roll line, covers every TOAST_FOR key and the four RAIL_DIRECT Oracle-only events, and is fully pinned + voice-scanned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: src/browser/tapStep.js (step-toward-a-tap + hold-inspect) and src/browser/mapMarks.js (palette, glyphs, legend) + their tests</name>
  <files>src/browser/tapStep.js, src/browser/mapMarks.js, test/unit/tapStep.test.js, test/unit/mapMarks.test.js</files>
  <read_first>
    - src/browser/controls.js (whole file, 118 lines — screenToCell is the ONLY hit-test transform and stays there; resolveTapDirection is the exact-adjacency building block whose DIR semantics (E = +x, S = +y) resolveStep must match; TAP_MAX_DURATION_MS = 350 is pinned by test/unit/controls.test.js line 44 and must NOT change)
    - src/browser/icons.js lines 17-60 (FEATURE_ICONS, PLAYER_MARKER_ICON, featureKeyForCell — the pure feat→key mapper mapMarks.js imports; the PNG preload/draw functions are NOT imported), 103-125 (ONEWAYDOOR_ROTATION_DEG — the PNG points EAST so E = 0°; the ▲ glyph points NORTH so the new table is N = 0, E = 90, S = 180, W = 270)
    - mazeworld.html lines 6125-6135 (the nine MARKS_LEGEND rows to move verbatim — names and descs unchanged, `icon` becomes `key`), 2634-2640 (MAZE_CANVAS_COLORS, the palette table draw() reads today)
    - design/Mazeworld Map Panel.dc.html lines 103-111 (INTERACTION: tap dominant-axis/fallback/both-blocked/own-square, hold ≥ 450 ms and the four inspect cards, drag > 10 px, pinch 0.6–2.0) and 114-117 (CHROME palette: fog #0b0a08, wall #2c261c with inset bevel #4a3f2c / #16120c, floor #645c48 inset #554d3b, grid border 6px #443a26, player #f4dc94, marks 60 % of the cell with the glyph colours)
    - .planning/phases/35-map-screen-rebuild/35-CONTEXT.md "Viewport & movement" bullet (the seven glyph/colour pairs) and 35-RESEARCH.md Pitfall 2 (resolveTapDirection is adjacency-only; the new function computes clamp(dx,-1,1)/clamp(dy,-1,1), prefers the larger |delta|, falls back to the other axis) and Pitfall 6 (glyphs are a rendering-METHOD change; rotate the one-way glyph via ctx.save/translate/rotate/restore)
    - test/unit/controls.test.js (fixture style for pure pointer/cell math), test/unit/tutorial.test.js lines 150-200 (the featureKeyForCell mapping table — mapMarks.test.js reuses the same feat list)
  </read_first>
  <behavior>
    - resolveStep({x:5,y:5}, {x:5,y:5}, open) → { kind: "here" }
    - resolveStep({x:5,y:5}, {x:9,y:6}, open) with open() always true → { kind: "step", dir: "E" } (|dx| 4 > |dy| 1); target {x:5,y:1} → "N"; {x:4,y:9} → "S"; {x:1,y:5} → "W"
    - resolveStep({x:5,y:5}, {x:8,y:8}, open) (tie |dx| = |dy|) → dir "E" (ties prefer the horizontal axis)
    - resolveStep({x:5,y:5}, {x:9,y:6}, open) where open(6,5) is false and open(5,6) true → { kind: "step", dir: "S" } (fallback axis)
    - resolveStep({x:5,y:5}, {x:9,y:6}, open) where open(6,5) and open(5,6) are both false → { kind: "blocked" }
    - resolveStep({x:5,y:5}, {x:9,y:5}, open) where open(6,5) is false (dy = 0, no fallback axis) → { kind: "blocked" }
    - resolveStep with a NaN/undefined target or pos → { kind: "blocked" } (never throws, never a diagonal)
    - inspectCell(null, legend) → { title: "SOLID ROCK", line: RAIL_COPY.rock.line, tone: "dull", hold: 2400 }; inspectCell({seen:false}, legend) → UNWALKED dull 2400; inspectCell({seen:true, wall:true}, legend) → SOLID ROCK; inspectCell({seen:true, wall:false, feat:"trap"}, legend) → { title: "TRAP", line: <the TRAP legend desc>, tone: "odd", hold: 3400 }; inspectCell({seen:true, wall:false, feat:null}, legend) → EMPTY CORRIDOR dull 2400; an unknown feat with no legend row → EMPTY CORRIDOR
    - MARK_GLYPHS has exactly the keys encounter, teleport, onewaydoor, trap, chest, crevice, descent, wall, party; the CONTEXT pairs hold: encounter ● #e05a48, teleport ◆ #a78ce8, onewaydoor ▲ #8ec06a, trap ✕ #e05a48, chest ▪ #d3c49f, crevice ⧗ #d3c49f, descent ▼ #d3c49f; party is #f4dc94
    - markForCell({feat:"dot"}) → { key: "encounter", glyph: "●", color: "#e05a48" }; "tele" → teleport; "one" → onewaydoor; "trap"; "chest"; "climb" → wall; "gorge" → crevice; "exit" and "gate" → descent; null/unknown feat → null; a bare string feat is accepted like featureKeyForCell
    - legendFor("trap") → { name: "TRAP", desc: … } (feat → key → legend row); legendFor("nope") → null
    - MARKS_LEGEND has 9 rows, keys in the order encounter, teleport, onewaydoor, trap, chest, crevice, descent, wall, party, each name/desc byte-identical to the mazeworld.html L6125-6135 rows; every name/desc clear of BANNED
    - ONEWAY_ROTATION_DEG = { N: 0, E: 90, S: 180, W: 270 }; MARK_SCALE = 0.6; MAP_PALETTE carries fog/wall/wallLight/wallDark/floor/floorDark/floorInset/border/party/partyGlow with the spec hexes
  </behavior>
  <action>
    1. Create `src/browser/tapStep.js` (pure, DOM-free; header states it extends controls.js — which owns the screen→cell transform — with the CONTEXT's "one square toward the tap" rule, and that controls.js's TAP_MAX_DURATION_MS is the legacy tap/drag split left untouched; this module's HOLD_MS = 450 is the new tap/hold boundary: a pointer still down at 450 ms with ≤ TAP_MAX_TRAVEL_PX travel and one finger is a HOLD; released before that with ≤ 10 px travel and never a second finger is a TAP). Export `DIR_VECTORS = Object.freeze({ N: [0, -1], S: [0, 1], E: [1, 0], W: [-1, 0] })`, `TAP_MAX_TRAVEL_PX = 10`, `HOLD_MS = 450`. Export `resolveStep(pos, target, isOpen)`: guard non-finite inputs → `{ kind: "blocked" }`; `dx = target.x - pos.x`, `dy = target.y - pos.y`; both 0 → `{ kind: "here" }`; `sx = Math.sign(dx)`, `sy = Math.sign(dy)`; first axis = `Math.abs(dx) >= Math.abs(dy) ? "x" : "y"` (ties → x); candidate list = [first axis unit step, then the other axis unit step ONLY if its delta is non-zero]; for each candidate `{x: pos.x + (axis === "x" ? sx : 0), y: pos.y + (axis === "y" ? sy : 0)}` return `{ kind: "step", dir }` for the first with `isOpen(x, y)` truthy (dir from the sign: x+1 → "E", x-1 → "W", y+1 → "S", y-1 → "N"); none → `{ kind: "blocked" }`. Export `inspectCell(cell, legendFor)` importing RAIL_COPY from `./rail.js` and RAIL_HOLD: null/undefined cell or `cell.wall` (when seen) → rock; `!cell.seen` → unwalked (checked BEFORE wall so fog never reveals rock); `cell.feat` with a legend row → `{ title: row.name, line: row.desc, tone: "odd", hold: RAIL_HOLD.mark }`; else empty. All four dull cards use `RAIL_HOLD.dull` (2400).
    2. Create `src/browser/mapMarks.js` (pure; imports ONLY `featureKeyForCell` from `./icons.js`; header records orchestrator decision 3 — the mock's coloured text glyphs replace the Phase-4 PNG icons on the canvas and in the legend as a deliberate, user-authored reversal (2026-09-16); the PNG files and icons.js's preload/draw pipeline stay on disk unreferenced by the map renderer and legend). Export `MAP_PALETTE = Object.freeze({ fog: "#0b0a08", wall: "#2c261c", wallLight: "#4a3f2c", wallDark: "#16120c", floor: "#645c48", floorDark: "#3d372a", floorInset: "#554d3b", border: "#443a26", party: "#f4dc94", partyGlow: "rgba(232,201,122,.55)" })` (floorDark is Claude's discretion for a `.dark` tile — the mock has no dark tiles), `MARK_SCALE = 0.6`, `ONEWAY_ROTATION_DEG = Object.freeze({ N: 0, E: 90, S: 180, W: 270 })`, `MARK_GLYPHS` (frozen; `{ glyph, color }` per key; wall = "▤" #d3c49f — the mock has no wall glyph, chosen from the Geometric Shapes block for font-fallback safety; party = "●" #f4dc94), `MARKS_LEGEND` (the nine rows moved verbatim from mazeworld.html with `key` in place of `icon`), `markForCell(cell)` → `const key = featureKeyForCell(cell); return key && MARK_GLYPHS[key] ? { key, ...MARK_GLYPHS[key] } : null`, `legendFor(feat)` → the MARKS_LEGEND row for `featureKeyForCell(feat)` or null.
    3. Create `test/unit/tapStep.test.js` and `test/unit/mapMarks.test.js` covering every behavior bullet (mapMarks.test.js reuses the BANNED matcher for the legend names/descs and asserts the nine name/desc pairs against a literal copy of the L6125-6135 rows so the move is proven byte-identical).
  </action>
  <verify>
    <automated>node --test test/unit/tapStep.test.js test/unit/mapMarks.test.js test/unit/controls.test.js test/unit/tutorial.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/tapStep.test.js` exits 0 with at least 10 tests; `node --test test/unit/mapMarks.test.js` exits 0 with at least 6 tests.
    - `grep -c "^export const HOLD_MS = 450;" src/browser/tapStep.js` = 1; `grep -c "^export const TAP_MAX_TRAVEL_PX = 10;" src/browser/tapStep.js` = 1; `grep -c "^export function resolveStep(pos, target, isOpen)" src/browser/tapStep.js` = 1; `grep -c "^export function inspectCell(cell, legendFor)" src/browser/tapStep.js` = 1.
    - `grep -c 'import { featureKeyForCell } from "./icons.js";' src/browser/mapMarks.js` = 1; `grep -c "preloadIcons\|drawFeatureIcon" src/browser/mapMarks.js` = 0.
    - `node -e "import('./src/browser/mapMarks.js').then(m=>{const g=m.MARK_GLYPHS;const ok=g.encounter.glyph==='●'&&g.encounter.color==='#e05a48'&&g.teleport.glyph==='◆'&&g.teleport.color==='#a78ce8'&&g.onewaydoor.glyph==='▲'&&g.onewaydoor.color==='#8ec06a'&&g.trap.glyph==='✕'&&g.chest.glyph==='▪'&&g.crevice.glyph==='⧗'&&g.descent.glyph==='▼'&&m.MARKS_LEGEND.length===9&&m.MARK_SCALE===0.6;process.exit(ok?0:1)})"` exits 0.
    - `node -e "import('./src/browser/tapStep.js').then(m=>{const o=()=>true;const a=m.resolveStep({x:5,y:5},{x:9,y:6},o).dir==='E';const b=m.resolveStep({x:5,y:5},{x:9,y:6},(x,y)=>!(x===6&&y===5)).dir==='S';const c=m.resolveStep({x:5,y:5},{x:9,y:5},(x,y)=>!(x===6&&y===5)).kind==='blocked';const d=m.resolveStep({x:5,y:5},{x:5,y:5},o).kind==='here';process.exit(a&&b&&c&&d?0:1)})"` exits 0.
    - `git diff --stat -- src/browser/controls.js src/browser/icons.js engine content test/parity` prints nothing; `node --test test/unit/controls.test.js test/unit/tutorial.test.js` exits 0.
  </acceptance_criteria>
  <done>The tap-to-step resolver, the hold-inspect cards, the canvas palette, the glyph table and the legend rows exist as pure, tested modules ready for the shell wiring.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/rail.test.js test/unit/tapStep.test.js test/unit/mapMarks.test.js` exit 0; `npm test` prints `# fail 0` (2047 baseline + the new tests); no existing test file modified.
- `git diff --stat -- engine content test/parity src/browser/toasts.js src/browser/controls.js src/browser/icons.js` empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
- SUMMARY contract (deferred UAT — MAP-10): end `35-01-SUMMARY.md` with `## Human verification (deferred to end of run)` stating "No device check queued (zero mazeworld.html lines touched, no DOM produced)" — Plan 05 aggregates.
</verification>

<success_criteria>
- Every CONTEXT rail decision (source = toastsForAction fold, uncapped; tone by family; holds 4200 / dull 2200–3400 / level-up 6000; stacking inside one card) and every CONTEXT viewport rule (dominant axis then fallback; both blocked → NO WAY THAT DIRECTION; own square → YOU ARE HERE; hold ≥ 450 ms inspect with the four cards) is a tested pure function.
- Orchestrator decisions 1, 3 and 4 are recorded in the objective and the module headers.
</success_criteria>

<output>
Create `.planning/phases/35-map-screen-rebuild/35-01-SUMMARY.md` when done (with the deferred-UAT section per the verification contract).
</output>
