---
phase: 35-map-screen-rebuild
plan: 05
type: execute
wave: 5
depends_on: ["35-01", "35-02", "35-03", "35-04"]
files_modified:
  - test/unit/shell-map-invariants.test.js
  - mazeworld.html
autonomous: true
requirements: [MAP-08, MAP-09, MAP-10]
tags: [vanilla-js, phase-invariants, voice-scan, executor-gate, android-debug-build, deferred-uat]

must_haves:
  truths:
    - "Phase-wide invariants hold and are pinned by one suite: no toast (host / builder / CSS / lifetime / cap), no D-pad (markup / CSS / listener / data attribute), no flash line, no HUD character line, no PNG draw in the renderer or legend, no Move-on card path; every new tap target (rail buttons, condition chips, camp sheet buttons, stair overlay buttons) is wired through guardTap; the settle window is stamped on the overlay dismissal and both sheet closes; window.move carries exactly one settle clause; no presentation state is written onto S/state (MAP-08, MAP-09)"
    - "Every player-facing string this phase added (RAIL_COPY incl. RAIL_FAMILY titles, MAP_COPY, CONDITION_EXPLAIN, MARKS_LEGEND names/descs) is non-empty and clear of content/safety-wordlist.js BANNED (MAP-09)"
    - "Final gate: npm test prints # fail 0; npm run build:www exits 0 and www/index.html carries none of the retired literals; git diff -- engine content test/parity is empty; git hash-object test/parity/prototype-master.js.txt = a1f4d0dc29782218d8e5aab65bc5989c33f917f0; no package.json / package-lock.json change; no Google Fonts host in mazeworld.html (MAP-09)"
    - "npm run android:debug builds the debug APK once, after the last shell edit of the milestone, and the APK path + size + timestamp are recorded in the SUMMARY; adb install is NOT attempted (the orchestrator/user install it); the SUMMARY's aggregated, grouped, numbered Pixel 7 checklist IS MAP-10's plan-side deliverable for this autonomous run (MAP-10)"
  artifacts:
    - path: "test/unit/shell-map-invariants.test.js"
      provides: "the phase-wide zero-grep sweep, the guarded-id sweep, the presentation-state sweep and the aggregate voice scan"
    - path: ".planning/phases/35-map-screen-rebuild/35-05-SUMMARY.md"
      provides: "the gate outputs, the APK path, the aggregated deferred-UAT checklist (Plans 02–04 of this phase, grouped and numbered, with the Phase 34 27-item list referenced as the companion batch) and the climb-dice deferred follow-up"
  key_links:
    - from: "test/unit/shell-map-invariants.test.js"
      to: "mazeworld.html"
      via: "raw-file and comment-stripped greps for the retired literals (built by concatenation so the test never spells them)"
      pattern: "readFileSync"
    - from: "npm run android:debug"
      to: "android/app/build/outputs/apk/debug/app-debug.apk"
      via: "build:www → cap sync android → pin-jdk → gradle assembleDebug"
      pattern: "app-debug\\.apk"
  prohibitions:
    - "No edits under engine/, content/, test/parity/; no new packages; no Google Fonts; no adb install / am force-stop / monkey in this plan"
    - "No feature work in this plan — only invariant fixes surfaced by the sweep (each fix noted in the SUMMARY with the plan it belongs to)"
    - "No comment in mazeworld.html may name a retired identifier (the sweep greps the raw file)"
---

<objective>
Close Phase 35 (MAP-08/09/10): one invariant suite that pins the whole phase's retirements, guards and copy in a single place; the full executor gate (tests, build, engine diff, master hash, packages, fonts, plus this phase's own zero-grep invariants); the ONE debug APK build of the milestone (this is v1.4's final phase, so the build is no longer stale) with its path recorded and no device install attempted; and the aggregated Pixel 7 checklist that stands in for MAP-10 during this `defer uat to end` run — grouped by plan, numbered, with the Phase 34 27-item list (34-05-SUMMARY.md) referenced as the companion batch the same APK serves.

**Deferred follow-up recorded here (orchestrator decision 1):** climb dice payload — `fellClimbing` / `fellInGorge` / `climbedOver` / `leaptOver` (`engine/movement.js` ~L204-208) carry no `roll` / `need`, so the rail shows the climb OUTCOME lines with no dice line; `rail.js#rollLineFor` already renders any numeric `roll` (+ `need`, + `hurt`) generically, so adding those fields is an additive, parity-safe engine payload change to do as a quick task after UAT — "climb dice payload — quick task after UAT". The SUMMARY restates this verbatim under a `## Deferred follow-ups` heading.

Purpose: the phase's proof and hand-off.
Output: `test/unit/shell-map-invariants.test.js`, any invariant fix in `mazeworld.html`, the gate record + APK path + aggregated checklist in `35-05-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-map-screen-rebuild/35-CONTEXT.md
@.planning/phases/35-map-screen-rebuild/35-RESEARCH.md
@.planning/phases/35-map-screen-rebuild/35-01-SUMMARY.md
@.planning/phases/35-map-screen-rebuild/35-02-SUMMARY.md
@.planning/phases/35-map-screen-rebuild/35-03-SUMMARY.md
@.planning/phases/35-map-screen-rebuild/35-04-SUMMARY.md
@.planning/phases/34-combat-screen-rebuild/34-05-SUMMARY.md
@.planning/phases/34-combat-screen-rebuild/34-VERIFICATION.md
</context>

<artifacts_this_phase_produces>
See 35-01-PLAN.md "Artifacts this phase produces" for the full phase list. This plan creates `test/unit/shell-map-invariants.test.js` and the milestone's debug APK (`android/app/build/outputs/apk/debug/app-debug.apk`, a build output — not committed). It creates no new symbol in mazeworld.html.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: shell-map-invariants.test.js — the phase-wide sweep (retired literals, guarded targets, settle/arm discipline, presentation state, aggregate voice scan) + fix whatever it finds</name>
  <files>test/unit/shell-map-invariants.test.js, mazeworld.html</files>
  <read_first>
    - test/unit/shell-map-rail.test.js, test/unit/shell-map-hud.test.js, test/unit/shell-map-viewport.test.js (Plans 02–04 — helpers, the concatenated-literal technique, the regions; this suite does NOT duplicate their per-region pins, it sweeps the whole file)
    - test/unit/shell-input-guards.test.js lines 145-215 (GUARDED_IDS + countGuardedWiring — extend the idea file-wide here, do not edit that file)
    - test/unit/shell-combat-over.test.js lines 300-350 (BANNED matcher + leaf extractor)
    - src/browser/rail.js (RAIL_COPY, RAIL_FAMILY), src/browser/mapMarks.js (MARKS_LEGEND), mazeworld.html (MAP_COPY, CONDITION_EXPLAIN object literals — extract every double-quoted leaf with the Phase 34 regex approach)
    - .planning/ROADMAP.md Phase 35 "Success Criteria" 1-5 and REQUIREMENTS.md MAP-01..10 (the sweep's test names cite the criterion each pin proves)
  </read_first>
  <action>
    1. Create `test/unit/shell-map-invariants.test.js` (header: Phase 35 MAP-08/09, Plan 05 — the whole-phase sweep). Load RAW = mazeworld.html text, CODE = stripComments(RAW). Build every retired literal by string concatenation (never spelled whole in this file). Tests: (a) SC-1 "no toast anywhere": RAW has zero of the toast builder name, the host id, the host/toast class prefix, the lifetime bridge name, `MAX_TOASTS`, `toastLifetime`; CODE has exactly one `toastsForAction(`; (b) SC-1 "no D-pad": RAW has zero of the three retired D-pad literals and zero `Move north` / `Move west` / `Move south` / `Move east` aria labels; (c) retirements: RAW has zero of the flash element/function names, the character-line ids/class, the old chip-track class, the legacy palette table name, the icon-ready helper name; CODE has zero `CARD_EVENTS`, `beatsTitleFor`, `FEATURE_EVENT_TITLE`, the retired dismiss-control id, `preDeathBeat`, `stepping()`; the draw() region has zero `drawFeatureIcon` / `__mzIconMap`; the legend region has zero `<img`; (d) MAP-08 guarded targets file-wide: for each of `a-join-yes`, `a-join-no`, `a-find-take`, `a-find-leave`, `mw-rail-climb`, `mw-camp-sleep`, `mw-camp-walk`, `mw-major-primary`, `mw-major-secondary`, `btn-death-oracle`, `cb-over-btn`, `a-loot-take-all`, `a-loot-leave-all` — wired via a direct `guardTap(document.getElementById("<id>")` OR a `id: "<id>"` entry with a generic `guardTap(document.getElementById(b.id), b.onTap)` present, and never a bare `getElementById("<id>").onclick =`; condition chips: paintConditions contains `guardTap(btn,`; the three map chips' listeners exist once each (`mw-chip-marks` → openMarksLegend, `mw-chip-centre` → centerMap, `btn-camp` onclick → openCampSheet); (e) settle/arm discipline: `lastDismissAt = Date.now()` count = 3; `encounterSettled()` count = 2; `armEncounterButtons();` appears in renderEncounter, renderRail, paintConditions and openCampSheet; the guard-helper region still has no transition/animation token; the `<style>` block has no `aria-disabled`; (f) no presentation state on S: CODE has zero `S.__mz`, `state.__mz`, `S.rail`, `state.rail`, `S.stair`, `state.stair`, `S.pendingClimb`, `state.pendingClimb`; every phase global exists exactly once as an initialiser: `window.__mzRail = emptyRail();`, `window.__mzStair = null;` (init), `window.__mzRailVM = {`, `window.__mzTapStep = {`, `window.__mzMapMarks = {`, `window.__mzDescend = `, `window.mzRailLine = `, `window.mzRailPulse = railPulse;`, `window.renderRail = renderRail;`; (g) rail-or-log partition: dispatchWithToasts has exactly one `if (wasCombat || inCombat) {`, its else-branch calls railCardFor, and CODE has zero `PRIORITY.block` re-checks outside fightLog/rail imports (mirror shell-fight-log's pin); (h) layout: `id="mw-rail"` sits between `</main>` and `<nav class="mw-tabbar"`; `.mw-tabbar{` has no `position:fixed`; (i) MAP-09 no new packages / fonts: `package.json` dependencies+devDependencies key sets equal HEAD's (read `git show HEAD:package.json` via child_process execSync — Windows-safe), RAW has zero occurrences of the two Google Fonts host names (build the literals by concatenation) and exactly three font-face declarations; (j) aggregate voice scan: every leaf of RAIL_COPY (recursive) + every RAIL_FAMILY title + every MAP_COPY leaf (regex-extracted from CODE's `const MAP_COPY = {` … `\n};`) + every CONDITION_EXPLAIN value (same extraction) + every MARKS_LEGEND name/desc — non-empty, clear of BANNED, ALLOWLIST honoured; (k) SC-5 pins the five ROADMAP success criteria by name in test titles so the SUMMARY's criteria table can cite them.
    2. Run the suite. For every failure, fix mazeworld.html (or the offending test's literal) and record the fix in the SUMMARY under "Invariant fixes" with the plan it belonged to. No feature work.
  </action>
  <verify>
    <automated>node --test test/unit/shell-map-invariants.test.js && npm test 2>&1 | grep -q "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-map-invariants.test.js` exits 0 with at least 11 tests; `grep -c "BANNED" test/unit/shell-map-invariants.test.js` >= 1; `grep -c "RAIL_FAMILY\|MAP_COPY\|CONDITION_EXPLAIN\|MARKS_LEGEND" test/unit/shell-map-invariants.test.js` >= 4.
    - `grep -c "mzToast\|mw-toast\|__mzToastLifetime\|MAX_TOASTS\|toastLifetime" mazeworld.html` = 0; `grep -c "dpad\|data-dir\|mazefoot" mazeworld.html` = 0; `grep -c "mw-flash\|flashMessage\|hud-name\|hud-cls\|mm-hp\|mw-hud-char\|mw-cond-track\|mw-viewport-chips\|MAZE_CANVAS_COLORS\|iconReady" mazeworld.html` = 0; `grep -c "CARD_EVENTS\|beatsTitleFor\|FEATURE_EVENT_TITLE\|preDeathBeat\|stepping()" mazeworld.html` = 0; `grep -c '"a-next"' mazeworld.html` = 0.
    - `grep -c "lastDismissAt = Date.now()" mazeworld.html` = 3.
    - `grep -c "encounterSettled()" mazeworld.html` = 2.
    - `grep -c "armEncounterButtons();" mazeworld.html` >= 4.
    - `grep -c "@font-face" mazeworld.html` = 3.
    - `grep -c "fonts.googleapis" mazeworld.html` = 0 (no network font host).
    - `npm test` prints `# fail 0`.
  </acceptance_criteria>
  <done>One suite proves the phase's retirements, guards, state discipline and copy for the whole file; anything it caught is fixed and recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Phase 35 executor gate + the debug APK build (no install) + the aggregated deferred Pixel 7 checklist (MAP-09, MAP-10)</name>
  <files>mazeworld.html</files>
  <read_first>
    - .planning/phases/35-map-screen-rebuild/35-01-SUMMARY.md, 35-02-SUMMARY.md, 35-03-SUMMARY.md, 35-04-SUMMARY.md (their "Human verification (deferred to end of run)" sections — aggregate them into this plan's SUMMARY, grouped by plan, numbered continuously)
    - .planning/phases/34-combat-screen-rebuild/34-05-SUMMARY.md "Human verification (deferred to end of run)" (the 27-item Phase 34 list this APK also serves — reference it, do not copy it) and 34-05-PLAN.md Task 3 (the gate pattern)
    - .planning/phases/35-map-screen-rebuild/35-RESEARCH.md "Validation Architecture" (the gate commands) and Pitfall 8 (zoom default — confirm 0.8 in the record)
    - .planning/STATE.md "Build/env" (npm run android:debug = build www → cap sync → pin-jdk → gradle assembleDebug; JAVA_HOME pin; do NOT let anything upgrade AGP/Gradle) and "Device" (deploy is the orchestrator's/user's step — NOT this task's)
    - .planning/ROADMAP.md Phase 35 "Success Criteria" 1-5
  </read_first>
  <action>
    Run the full gate and record every exact output in the SUMMARY, one item per line:
    1. `npm test` — must print `# fail 0`; record the pass count (expected ≥ 2047 + the new suites − the retired toast-wiring tests).
    2. `npm run build:www` — exit 0; afterwards `www/src/browser/rail.js`, `www/src/browser/tapStep.js`, `www/src/browser/mapMarks.js` exist and `www/index.html` has zero of the retired toast / D-pad literals (grep the built file too — the build injects only an import map).
    3. `git diff --stat -- engine content test/parity` — empty; `git status --porcelain test/parity/fixtures test/parity/prototype-master.js.txt` — empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    4. `git diff --stat -- package.json package-lock.json` — empty; the Google Fonts host name does not appear in mazeworld.html (count 0).
    5. Comment-stripper hygiene (Phase 34 D9): `grep -o '/\*' mazeworld.html | wc -l` vs `grep -o '\*/' mazeworld.html | wc -l` — record both; the known pre-existing 152/148 imbalance from the `@capacitor/*` comment mentions is acceptable ONLY if the difference is unchanged from Phase 34's record (4); any new imbalance must be reworded.
    6. Phase invariants — the raw-file zero-greps for the toast set, the D-pad set, the flash/character-line/chip-track/palette set and the Move-on set (exact commands in Task 1's criteria), plus the positive counts: the rail element id occurs once, `renderMajorOverlay(body` occurs twice, `window.mzCenterMap?.()` occurs ten times, the zoom default line (`let zoom = ` followed by `0.8;`) occurs once.
    7. Debug APK: `npm run android:debug` — exit 0 (this is the milestone's final phase, so the build is current; do NOT run `adb install`, `am force-stop` or `monkey` — the phone is intermittently connected and the orchestrator/user handle deploy). Record `ls -l android/app/build/outputs/apk/debug/app-debug.apk` (path, size, timestamp) and the `versionName (versionCode)` stamped into www/index.html's `#mw-app-version`. If gradle fails on the JDK pin, re-run `node tools/pin-jdk.mjs` then `node tools/gradle.mjs assembleDebug` once and record it; do not touch `android/version.properties` (no versionCode bump — that is the Play release path, not this build).
    8. No file changes are expected in this task; if a gate item fails, fix it in mazeworld.html/tests within this plan and re-run.
    9. Write the SUMMARY with: the gate record (1-7); a `## Deferred follow-ups` section restating the climb-dice note verbatim ("climb dice payload — quick task after UAT") and the two intentional non-goals (the vestigial `controlScheme` setting field; the still-preloaded, now-unreferenced PNG icon set kept on disk per decision 3); the `## Human verification (deferred to end of run)` section per the verification contract below.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "^# fail 0" && npm run build:www && test -z "$(git diff --stat -- engine content test/parity)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && test -z "$(git diff --stat -- package.json package-lock.json)" && test "$(grep -c 'mzToast\|mw-toast\|dpad\|data-dir\|mazefoot' www/index.html)" = "0" && npm run android:debug && test -f android/app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` output contains `# fail 0`; `npm run build:www` exits 0; `test -f www/src/browser/rail.js && test -f www/src/browser/tapStep.js && test -f www/src/browser/mapMarks.js` exits 0; `grep -c "mzToast\|mw-toast\|dpad\|data-dir\|mazefoot" www/index.html` = 0.
    - `git diff --stat -- engine content test/parity` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat -- package.json package-lock.json` prints nothing.
    - `grep -c "fonts.googleapis" mazeworld.html` = 0 (no network font host).
    - `npm run android:debug` exits 0 and `test -f android/app/build/outputs/apk/debug/app-debug.apk` exits 0; `git status --porcelain android/version.properties` prints nothing; no `adb` command appears in the executor's transcript for this task.
    - `35-05-SUMMARY.md` contains: the heading `## Human verification (deferred to end of run)` followed by at least 24 numbered Pixel 7 checks grouped under `### Plan 02`, `### Plan 03`, `### Plan 04` (+ a `### Plan 05` build line), one line naming the APK path and its timestamp, one line stating that `adb install -r` + `am force-stop` + relaunch are performed by the orchestrator/user (not this plan), and one line pointing at the Phase 34 27-item list in 34-05-SUMMARY.md as the companion batch the same APK serves; the heading `## Deferred follow-ups` containing the exact phrase `climb dice payload — quick task after UAT`.
  </acceptance_criteria>
  <done>The phase-wide gate is green and recorded, the milestone's debug APK exists at a recorded path without any device install attempt, and MAP-10's on-device round is queued as one grouped, numbered checklist.</done>
</task>

</tasks>

<verification>
- Full gate per Task 2 (npm test `# fail 0`, build:www exit 0 + www zero-greps, engine/content/parity diff empty, master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0, no new packages, no Google Fonts, comment-stripper record, phase invariants, APK built and located, no adb).
- SUMMARY contract (deferred UAT — MAP-10): `35-05-SUMMARY.md` ends with `## Human verification (deferred to end of run)` that (a) states the debug APK path/size/timestamp and that `adb install -r` + `am force-stop` + `monkey` relaunch happen next by the orchestrator/user (a Play-installed build must be uninstalled first — different signer, Preferences data lost), (b) aggregates the numbered Pixel 7 checks from 35-02/03/04-SUMMARY.md grouped by plan and numbered continuously, (c) adds this plan's own checks: (1) the app cold-starts to the map with the new HUD, the idle rail (FLOOR 1 · NOTHING IS HAPPENING, TAP TO STEP) and no D-pad; (2) a full loop — step, trap, chest Take, Joiner accept, wall fall + CLIMB IT, stair GO DOWN, camp — produces rail cards only and never a toast; (3) one full fight from FIGHT IT OUT to THEY ARE DOWN still works exactly as the Phase 34 list describes (the two phases share this APK); (4) the crevice ⧗ and box ▪ glyphs render as glyphs, not empty boxes, on the Pixel 7's fonts (if ⧗ is a box, the follow-up is a one-line glyph swap in mapMarks.js — record it); and (d) points at 34-05-SUMMARY.md's 27 checks as the companion batch.
</verification>

<success_criteria>
- ROADMAP success criterion 5 (guards hold on every new button; `npm test` green with every shell test re-pinned; `build:www` exit 0; engine/content/parity diff empty) is proven by the gate; the Pixel 7 DR round (criterion 5's last clause, MAP-10) is queued as the aggregated checklist with a built APK.
- Every CONTEXT section is traceable across the plan set: User rulings 1/3/4 (Plan 02), 2 (Plan 04), 5 (Plan 03); HUD (03); Viewport & movement (04, palette 03, helpers 01); RAIL (01 + 02); MAJOR OVERLAY (02 death, 04 stair, encounter from Phase 34); Sheets (03); Guards & tests (02–05).
- All six orchestrator decisions are recorded: 1 (Plans 01, 05), 2 (02), 3 (01, 03), 4 (01, 02), 5 (02, 04), 6 (01, 02).
</success_criteria>

<output>
Create `.planning/phases/35-map-screen-rebuild/35-05-SUMMARY.md` when done — with the gate record, the `## Deferred follow-ups` section and the aggregated `## Human verification (deferred to end of run)` section (the MAP-10 deliverable for this run).
</output>
