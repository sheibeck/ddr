---
phase: 34-combat-screen-rebuild
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - mazeworld.html
  - test/unit/shell-combat-screen.test.js
  - test/unit/shell-fight-gate.test.js
  - test/unit/shell-input-guards.test.js
autonomous: true
requirements: [CSCR-01, CSCR-02, CSCR-03, CSCR-06, CSCR-08, CSCR-09]
tags: [vanilla-js, combat-screen, layout, major-overlay, css, source-assertion-tests]

must_haves:
  truths:
    - "A live (fought) combat renders inside #enc-panel[data-mode=\"dark\"] as three bands in DOM order: .cb-head (ENCOUNTER · ROUND N · N STANDING) → .cb-mid (foes → YOUR LOT → › log, the only scrollable band) → .cb-act (border-top 3px #3a3226, bg #1b170f); the map and D-pad are covered as before"
    - "Each foe renders as a .cb-foe card from foeListViewModel: glyph, upper-case name, `SZ · INT i · NOTE` meta, `wp / max` (— when dead), TARGET/DOWN/status tag, 6px red bar; dead cards keep their place at opacity .45; tapping a live card retargets through guardTap (S.combat.target = i; renderEncounter()) — no engine action"
    - "YOUR LOT renders the hero card first (gold, active) then every state.party member and any summoned ally as side-by-side .cb-lot-card cards inside a strip that is overflow-x:auto; the hint reads JUST YOU · NOBODY TO BLAME solo and YOUR LOT · EACH ROLLS THEIR OWN with a joiner"
    - "combat.pending never reaches the combat-panel markup: renderEncounter renders renderMajorOverlay(host, spec) (● 54px #e05a48, SOMETHING IS HERE / THEY ARE ALREADY HERE, the foe line, the roll note, one FIGHT IT OUT button → window.mzFight through guardTap) and returns before any band is built; Enter/Space on the overlay dispatch fight"
    - "renderMajorOverlay is parameterised ({icon, iconTone, title, line, roll, primary:{label,onTap}, secondary?:{label,onTap}}) so Phase 35 can call it for descents and out-of-combat death without editing it"
    - "The bundled Press Start 2P / Courier Prime faces are used through var(--disp) / var(--mono); no Google Fonts link exists; cosmetic animations reuse the existing mwfade/mwrise keyframes and are zeroed by the blanket prefers-reduced-motion rule; every guard stays a Date.now() check"
    - "The old action bar (a-strike … a-scroll, the use-list, the spell menu) still renders — now inside .cb-act — with its Phase 32 guards intact until Plan 04 replaces it"
  artifacts:
    - "mazeworld.html: renderMajorOverlay(host, spec), renderCombatHeader(host, vm), renderFoeCards(host, vm, onPick), renderYourLot(host, vm), the Phase 34 combat-screen CSS block, the window.__mzCombatVM bridge, data-mode=\"dark\" on the combat/pending branches"
    - "test/unit/shell-combat-screen.test.js"
    - "test/unit/shell-fight-gate.test.js and test/unit/shell-input-guards.test.js re-pinned (a-fight → mw-major-primary; the guarded-id region starts at function renderFightLog()"
  key_links:
    - "renderEncounter live-combat branch -> window.__mzCombatVM.{header,foes,lot,overlay}(S) -> renderCombatHeader/renderFoeCards/renderYourLot/renderFightLog into #enc-body"
    - "renderFoeCards -> foeStatusBadges(f) via opts.chipsFor (the chip source is unchanged; foe-effect-chip.test.js stays green)"
    - "keydown S.combat.pending -> window.mzFight (Enter/Space only) — the same guarded arm window as the overlay button"
  prohibitions:
    - "No edits under engine/, content/, test/parity/; no `retarget` engine action; no new field on state.combat"
    - "No Google Fonts <link>; no new @font-face; no new npm package"
    - "No `card.onclick` / `body.onclick` / `panel.onclick` / `*.addEventListener` tap-anywhere-to-dismiss in the render helpers or renderEncounter (name card elements `el`)"
    - "No transition/animation token inside the guard-helper region; no transitionend/animationend listener anywhere"
    - "The toast host is not deleted; out-of-combat surfaces (beats, store, won) keep their legacy styling (no data-mode) — Phase 35 owns them"
---

<objective>
Rebuild the live-combat render into the mock's full-screen layout (CSCR-01/02/03) and move the Fight! gate out of the combat markup into the map's MAJOR OVERLAY (CSCR-06), keeping the Phase 32 guards (CSCR-08): `#enc-panel` gains `data-mode="dark"` for the combat/pending branches, `renderEncounter` builds header → middle (foes → YOUR LOT → › log from Plan 02) → action band from the Plan 01 view-models, foe taps retarget through `guardTap`, and `renderMajorOverlay(host, spec)` renders the pending encounter with one FIGHT IT OUT button. The old 7-button action bar survives inside `.cb-act` for exactly one more wave so `shell-input-guards` keeps proving the guards while Plan 04 builds the 2×2 grid.

**Decision 2 recorded here (RESEARCH Open Question 2 — major overlay function shape):** build the parameterised `renderMajorOverlay(host, spec)` NOW. `spec = { icon, iconTone ("encounter" | "plain"), title, line, roll (optional), primary: { label, onTap }, secondary: { label, onTap } | null }`; it renders `#mw-major` (absolute inset:0 over the whole panel), wires `#mw-major-primary`/`#mw-major-secondary` through `guardTap`, and knows nothing about combat — the combat branch composes the spec from `encounterOverlaySpec(S)` and maps its `dispatch` to `window.mzFight`. Phase 35 calls it for THE STAIR DOWN (GO DOWN / NOT YET) and the out-of-combat THAT IS THAT without touching this function.

**Decision 3 recorded here (CONTEXT "retarget dispatch" inaccuracy):** there is no engine `retarget` action and none is added. Targeting stays the presentation mutation `S.combat.target = i; renderEncounter();` (C.target already serialises harmlessly), now wrapped in `guardTap` on the foe card element; Enter/Space on a focused card routes through the same guarded click. Engine untouched.

Discretion decisions: `#enc-body` keeps innerHTML/appendChild rebuilds per render (Phase 32 precedent) with `#enc-round-live` persisting as a sibling; one attribute `data-mode="dark"` on `#enc-panel` switches the mock palette and hides the legacy `.enc-topbar`; the summoned ally (`C.ally`) folds into YOUR LOT as a card so nothing the old panel showed disappears; the middle band scrolls the newest log entry into view (`block:"nearest"`) after a new log batch so the latest line is visible with three foes on a Pixel 7.

Purpose: the combat screen's chrome and content; Plan 04 fills the action band, Plan 05 the endings.
Output: `mazeworld.html` (CSS block, four render helpers, the rewritten combat branch, keydown pending path), `test/unit/shell-combat-screen.test.js`, re-pinned `shell-fight-gate.test.js` / `shell-input-guards.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-combat-screen-rebuild/34-CONTEXT.md
@.planning/phases/34-combat-screen-rebuild/34-RESEARCH.md
@.planning/phases/34-combat-screen-rebuild/34-01-SUMMARY.md
@.planning/phases/34-combat-screen-rebuild/34-02-SUMMARY.md
@design/Mazeworld Combat Panel.dc.html
@design/Mazeworld Map Panel.dc.html
@src/browser/combatPanel.js
@src/browser/combatMenu.js
</context>

<artifacts_this_phase_produces>
See 34-01-PLAN.md "Artifacts this phase produces" for the full phase list. This plan creates: `renderMajorOverlay(host, spec)`, `renderCombatHeader(host, vm)`, `renderFoeCards(host, vm, onPick)`, `renderYourLot(host, vm)`, `window.__mzCombatVM`, `#enc-panel[data-mode="dark"]`, DOM ids `mw-major`, `mw-major-primary`, `mw-major-secondary`, `cb-head`, `cb-mid`, `cb-foes`, `cb-lot`, `cb-act`, `data-foe`, and the CSS classes `cb-head* cb-mid cb-hint cb-threat cb-foes* cb-foe* cb-bar* cb-lot* cb-act cb-prompt cb-grid cb-btn* cb-submenu cb-sub-* cb-chip cb-row* cb-over* mw-major*` (the grid/submenu/over rules are written here so Plans 04-05 only add markup), and `test/unit/shell-combat-screen.test.js`.
</artifacts_this_phase_produces>

<tasks>

<!-- planner-discipline-allow: "a-fight" -->
<!-- planner-discipline-allow: C.target = i; renderEncounter(); -->
<!-- planner-discipline-allow: mw-round-tick -->
<!-- planner-discipline-allow: fonts.googleapis -->
<!-- fonts.googleapis is only ever written into the TEST file as a negative assertion (Task 3), never into mazeworld.html. -->
<!-- The literals above are DELETION targets (the old Fight! button id, the unguarded retarget handler, the round-tick pulse calls) named so the executor removes the right code; the acceptance criteria negative-grep them afterwards. -->

<task type="auto">
  <name>Task 1: The Phase 34 CSS block and the dark panel mode</name>
  <files>mazeworld.html</files>
  <read_first>
    - design/Mazeworld Combat Panel.dc.html lines 19-127 (the whole template — every inline style is the spec) and comment lines 135-157 (STYLE OBJECTS: foe card, party card, log entry, action btn, submenu, over panel, header, hints, shell)
    - design/Mazeworld Map Panel.dc.html lines 81-93 and comment lines 132-137 (MAJOR OVERLAY style object)
    - mazeworld.html lines 33-56 (@font-face: the bundled faces), 58-95 (:root — `--disp` = Press Start 2P, `--mono`/`--body` = Courier Prime), 340-372 (.mw-overlay / .enc-topbar rules the dark mode overrides), 408-420 (base `button` rule: letter-spacing .06em, font-size 13px, transition — every .cb-* button must reset these), 545-556 (.actions / #enc-body .actions margin-top:auto — reused by the joiner/find cards in dark mode), 550 (#enc-body is already a flex column), 1022-1026 (existing @keyframes mwfade / mwrise — reuse, do not redefine), 756 (the blanket prefers-reduced-motion rule), the .cb-log rules Plan 02 added
    - test/unit/shell-input-guards.test.js lines 271-277 (the style block must carry no aria-disabled selector)
  </read_first>
  <action>
    Add ONE contiguous CSS block titled with a comment "Phase 34 (CSCR-01/02/03/05/06/07) — combat screen, Claude Design spec design/Mazeworld Combat Panel.dc.html + the Map Panel MAJOR OVERLAY; values copied from the STYLE OBJECTS comment; var(--disp) = bundled Press Start 2P, var(--mono) = bundled Courier Prime" placed directly after the `.cb-log` rules from Plan 02. Write these rules with exactly these values (one rule per line, no aria-disabled selector, no transitionend/animationend anywhere):
    - Panel mode: `#enc-panel[data-mode="dark"]{padding:0;background:#120f0a;color:#e6ddc6;overflow:hidden;font-family:var(--mono)}` · `#enc-panel[data-mode="dark"] .enc-topbar{display:none}` · `#enc-panel[data-mode="dark"] #enc-body{flex:1;min-height:0;display:flex;flex-direction:column;overflow:auto;position:relative}`.
    - Header: `.cb-head{flex:none;display:flex;justify-content:space-between;align-items:center;padding:10px 14px 9px;border-bottom:3px solid #6b2c22;background:#1e120f}` · `.cb-head-label{font-family:var(--disp);font-size:8px;color:#e07260}` · `.cb-head-right{display:flex;gap:11px;align-items:baseline;font-family:var(--mono);font-weight:700;font-size:14px}` · `.cb-head-round{color:#e8c97a}` · `.cb-head-standing{color:#c9bda0}`.
    - Middle: `.cb-mid{flex:1;overflow:auto;min-height:0}` · `.cb-hint{font-family:var(--disp);font-size:6.5px;color:#a89c82;line-height:1.7}` · `.cb-threat{font-family:var(--mono);font-weight:700;font-size:13px;color:#8f856f}`.
    - Foes: `.cb-foes{padding:11px 12px 0}` · `.cb-foes-head{display:flex;justify-content:space-between;align-items:center}` · `.cb-foe-list{display:flex;flex-direction:column;gap:6px;margin-top:8px}` · `.cb-foe{border:2px solid #6b2c22;background:#1e120f;padding:10px 11px;cursor:pointer}` · `.cb-foe.target{border-color:#e8c97a;background:#241d12}` · `.cb-foe.dead{border-color:#241f16;background:#15120c;opacity:.45;cursor:default}` · `.cb-foe-row{display:flex;justify-content:space-between;align-items:center;gap:8px}` · `.cb-foe-left{display:flex;align-items:center;gap:8px;min-width:0;flex:1}` · `.cb-foe-glyph{font-size:19px;color:#e07260;width:22px;text-align:center;flex:none}` · `.cb-foe.target .cb-foe-glyph{color:#e8c97a}` · `.cb-foe.dead .cb-foe-glyph{color:#6d6455}` · `.cb-foe-name{font-family:var(--disp);font-size:8px;line-height:1.5;color:#e6ddc6}` · `.cb-foe-meta{font-family:var(--mono);font-weight:700;font-size:12px;color:#a89c82;margin-top:4px}` · `.cb-foe-right{text-align:right;flex:none}` · `.cb-foe-wp{font-family:var(--mono);font-weight:700;font-size:15px;color:#e6ddc6}` · `.cb-foe-tag{font-family:var(--disp);font-size:6px;line-height:1.8;margin-top:5px;color:#e8c97a;min-height:11px}` · `.cb-foe-tag.status{color:#a78ce8}` · `.cb-foe.dead .cb-foe-tag{color:#8f856f}` · `.cb-bar{height:6px;background:#2c2519;margin-top:7px;box-shadow:inset 0 0 0 2px #0f0d09}` · `.cb-bar-fill{height:100%;background:#c4483a;transition:width .3s steps(6)}` · `.cb-foe.dead .cb-bar-fill{background:#3a3226}`.
    - YOUR LOT: `.cb-lot{padding:12px 12px 0}` · `.cb-lot-strip{display:flex;gap:6px;margin-top:8px;overflow-x:auto}` · `.cb-lot-card{flex:1 0 96px;min-width:96px;padding:9px 6px;text-align:center;border:2px solid #3a3226;background:transparent;color:#c9bda0}` · `.cb-lot-card.active{border-color:#e8c97a;background:#241d12;color:#e8c97a}` · `.cb-lot-card.down{background:#15120c;color:#8f856f}` · `.cb-lot-name{font-family:var(--disp);font-size:7px;line-height:1.5}` · `.cb-lot-wp{font-family:var(--mono);font-weight:700;font-size:14px;margin-top:5px}` · `.cb-lot-bar{height:5px;background:#2c2519;margin-top:5px}` · `.cb-lot-fill{height:100%;background:#7f9d4e}` · `.cb-lot-fill.low{background:#e05a48}` · `.cb-lot-card.down .cb-lot-fill{background:#3a3226}` · `.cb-lot-third{font-family:var(--mono);font-weight:700;font-size:12px;color:#a89c82;margin-top:5px}` · `.cb-lot-card.down .cb-lot-third{color:#8f856f}`.
    - Action band + grid (markup lands in Plan 04): `.cb-act{flex:none;border-top:3px solid #3a3226;background:#1b170f;padding:10px 12px 26px}` · `.cb-prompt{font-family:var(--disp);font-size:6.5px;color:#e8c97a;line-height:1.7}` · `.cb-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:9px}` · `.cb-btn{padding:12px 10px;text-align:left;border:2px solid #6b5c3c;border-radius:0;background:#241d12;color:#e8c97a;box-shadow:0 4px 0 #0d0b08;font-family:var(--mono);font-size:12.5px;letter-spacing:0;min-height:48px;cursor:pointer}` · `.cb-btn-accent{border-color:#a63a2c;background:#2a1512;color:#e07260}` · `.cb-btn-off{background:#191510;color:#8f856f;box-shadow:none}` · `.cb-btn-label{font-family:var(--disp);font-size:7.5px;line-height:1.6}` · `.cb-btn-sub{font-family:var(--mono);font-weight:700;font-size:12.5px;margin-top:6px;color:#a89c82;line-height:1.35}` · `.cb-btn-off .cb-btn-sub{color:#8f856f}`.
    - Submenu (markup in Plan 04): `.cb-submenu{animation:mwrise .16s ease}` · `.cb-sub-head{display:flex;justify-content:space-between;align-items:center;gap:8px}` · `.cb-sub-title{font-family:var(--disp);font-size:6.5px;color:#e8c97a;line-height:1.7}` · `.cb-chip{font-family:var(--disp);font-size:7px;color:#c9bda0;padding:7px 9px;border:2px solid #4a4032;border-radius:0;background:transparent;box-shadow:none;letter-spacing:0;flex:none;min-height:0;cursor:pointer}` · `.cb-sub-list{display:flex;flex-direction:column;gap:6px;margin-top:9px;max-height:206px;overflow:auto;padding-right:4px}` · `.cb-row{display:block;width:100%;border:2px solid #6b5c3c;border-radius:0;background:#241d12;padding:10px 11px;text-align:left;color:#e6ddc6;font-family:var(--mono);letter-spacing:0;box-shadow:none;min-height:48px;cursor:pointer}` · `.cb-row-off{border-color:#241f16;background:#191510;color:#8f856f}` · `.cb-row-head{display:flex;justify-content:space-between;align-items:center;gap:8px}` · `.cb-row-label{font-family:var(--disp);font-size:7.5px;line-height:1.6;min-width:0;overflow:hidden}` · `.cb-row-cost{font-family:var(--mono);font-weight:700;font-size:14px;color:#e8c97a;flex:none}` · `.cb-row-off .cb-row-cost{color:#8f856f}` · `.cb-row-desc{font-family:var(--mono);font-weight:700;font-size:13px;color:#a89c82;margin-top:6px;line-height:1.4}`.
    - Over panel (markup in Plan 05): `.cb-over{padding:13px 12px 28px;animation:mwfade .2s ease}` · `.cb-over-title{font-family:var(--disp);font-size:11px;line-height:1.6;color:#e8c97a}` · `.cb-over-title.dead{color:#e07260}` · `.cb-over-line{font-family:var(--mono);font-weight:700;font-size:15px;color:#c9bda0;margin-top:8px;line-height:1.45}` · `.cb-over-lines{padding:12px 12px 0}` · `.cb-over-lines p{font-family:var(--mono);font-weight:700;font-size:15px;line-height:1.45;color:#c9bda0;margin:0 0 8px}` · `.cb-over-lines p.cb-epitaph{color:#e8c97a}` · `.cb-over-actions{display:flex;flex-direction:column;gap:9px}` · `.cb-over-btn{display:block;width:100%;margin-top:13px;text-align:center;font-family:var(--disp);font-size:8px;letter-spacing:0;padding:15px 10px;background:#e8c97a;color:#14110c;border:0;border-radius:0;box-shadow:0 4px 0 #6b5c3c;cursor:pointer}` · `.cb-over-btn.secondary{background:transparent;color:#a89c82;border:2px solid #3a3226;box-shadow:none}` · `.cb-over-btn.dead{background:#2a1512;color:#e07260;border:2px solid #a63a2c;box-shadow:0 4px 0 #0d0b08}` · `.cb-sub{font-family:var(--mono);font-weight:700;font-size:13px;color:#a89c82;margin:10px 12px 0}`.
    - Legacy cards restyled in dark mode (joiner/find/loot rows; Plan 05 sets the attribute on those branches): `#enc-panel[data-mode="dark"] .enc-head{font-family:var(--disp);font-size:11px;line-height:1.6;color:#e8c97a;margin:12px 12px 0}` · `#enc-panel[data-mode="dark"] .enc-sub{font-family:var(--mono);font-weight:700;font-size:13px;color:#a89c82;margin:6px 12px 0}` · `#enc-panel[data-mode="dark"] .evt p{font-family:var(--mono);font-weight:700;font-size:15px;line-height:1.45;color:#c9bda0;margin:8px 12px 0}` · `#enc-panel[data-mode="dark"] .actions{display:flex;gap:9px;padding:13px 12px 28px;flex:none;border-top:3px solid #3a3226;background:#1b170f}` · `#enc-panel[data-mode="dark"] .actions button{flex:1;font-family:var(--disp);font-size:8px;letter-spacing:0;padding:15px 10px;background:transparent;color:#a89c82;border:2px solid #3a3226;border-radius:0;box-shadow:none}` · `#enc-panel[data-mode="dark"] .actions button.primary{background:#e8c97a;color:#14110c;border:0;box-shadow:0 4px 0 #6b5c3c}` · `#enc-panel[data-mode="dark"] ul.skills{margin:8px 12px 0;padding:0;list-style:none;display:flex;flex-direction:column;gap:6px}` · `#enc-panel[data-mode="dark"] ul.skills li{border:2px solid #6b5c3c;background:#241d12;padding:10px 11px}` · `#enc-panel[data-mode="dark"] ul.skills li b{font-family:var(--disp);font-size:7.5px;line-height:1.6;color:#e6ddc6}` · `#enc-panel[data-mode="dark"] ul.skills li i{font-family:var(--mono);font-weight:700;font-size:13px;color:#a89c82;font-style:normal}` · `#enc-panel[data-mode="dark"] ul.skills li button.small{font-family:var(--disp);font-size:7px;letter-spacing:0;color:#c9bda0;padding:7px 9px;border:2px solid #4a4032;border-radius:0;background:transparent;box-shadow:none;min-height:36px}` · `#enc-panel[data-mode="dark"] .shelf{margin:8px 12px 0}`.
    - Major overlay: `.mw-major{position:absolute;inset:0;background:#0d0b08;display:flex;flex-direction:column;justify-content:center;padding:28px 22px;animation:mwfade .22s ease;z-index:2}` · `.mw-major-icon{font-size:54px;color:#d3c49f;text-shadow:0 0 24px rgba(0,0,0,.8)}` · `.mw-major-icon.encounter{color:#e05a48}` · `.mw-major-title{font-family:var(--disp);font-size:15px;line-height:1.6;color:#e8c97a;margin-top:22px}` · `.mw-major-line{font-family:var(--mono);font-weight:700;font-size:17px;line-height:1.5;color:#c9bda0;margin-top:14px}` · `.mw-major-roll{font-family:var(--mono);font-weight:700;font-size:14px;color:#e8c97a;margin-top:12px}` · `.mw-major-actions{display:flex;flex-direction:column;gap:9px;margin-top:26px}` · `.mw-major-btn{font-family:var(--disp);font-size:9px;letter-spacing:0;padding:17px 10px;background:#e8c97a;color:#14110c;border:0;border-radius:0;box-shadow:0 5px 0 #6b5c3c;text-align:center;cursor:pointer}` · `.mw-major-btn.secondary{background:transparent;color:#a89c82;border:2px solid #3a3226;box-shadow:none}`.
    Do NOT add a Google Fonts link, a new @font-face, or new keyframes (mwfade/mwrise already exist at lines 1022/1026). Do not touch the `.foe`/`.foes`/`.spellmenu`/`.deathcard` legacy rules in this task (Plans 04/05 remove what they retire).
  </action>
  <verify>
    <automated>grep -c '^#enc-panel\[data-mode="dark"\]{padding:0;background:#120f0a' mazeworld.html && grep -c '^\.cb-head{flex:none' mazeworld.html && grep -c '^\.mw-major{position:absolute' mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#enc-panel\[data-mode="dark"\]{padding:0;background:#120f0a;color:#e6ddc6;overflow:hidden' mazeworld.html` = 1; `grep -c '^#enc-panel\[data-mode="dark"\] \.enc-topbar{display:none}' mazeworld.html` = 1.
    - `grep -c '^\.cb-head{flex:none;display:flex;justify-content:space-between;align-items:center;padding:10px 14px 9px;border-bottom:3px solid #6b2c22;background:#1e120f}' mazeworld.html` = 1; `grep -c '^\.cb-mid{flex:1;overflow:auto;min-height:0}' mazeworld.html` = 1; `grep -c '^\.cb-act{flex:none;border-top:3px solid #3a3226;background:#1b170f' mazeworld.html` = 1.
    - `grep -c '^\.cb-lot-strip{display:flex;gap:6px;margin-top:8px;overflow-x:auto}' mazeworld.html` = 1; `grep -c '^\.cb-foe\.dead{border-color:#241f16;background:#15120c;opacity:\.45' mazeworld.html` = 1; `grep -c '^\.cb-bar-fill{height:100%;background:#c4483a' mazeworld.html` = 1.
    - `grep -c '^\.cb-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-top:9px}' mazeworld.html` = 1; `grep -c '^\.cb-sub-list{display:flex;flex-direction:column;gap:6px;margin-top:9px;max-height:206px;overflow:auto' mazeworld.html` = 1; `grep -c '^\.cb-over-title\.dead{color:#e07260}' mazeworld.html` = 1.
    - `grep -c '^\.mw-major{position:absolute;inset:0;background:#0d0b08' mazeworld.html` = 1; `grep -c '^\.mw-major-icon\.encounter{color:#e05a48}' mazeworld.html` = 1; `grep -c '^\.mw-major-btn{font-family:var(--disp);font-size:9px' mazeworld.html` = 1.
    - `grep -c "@font-face" mazeworld.html` = 3 (unchanged); `grep -c "@keyframes mwfade" mazeworld.html` = 1; `grep -c "@keyframes mwrise" mazeworld.html` = 1.
    - `grep -c "fonts.googleapis" mazeworld.html` = 0.
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('<style>');const b=s.indexOf('</style>',a);process.exit(s.slice(a,b).includes('aria-disabled')?1:0)"` exits 0.
  </acceptance_criteria>
  <done>Every mock style object exists as a named `.cb-*` / `.mw-major*` rule with the spec's exact values, bound to the bundled faces, with the legacy card classes restyled under `[data-mode="dark"]`.</done>
</task>

<task type="auto">
  <name>Task 2: renderMajorOverlay + header/foes/lot helpers, the rewritten combat branch, the pending branch, keydown Enter/Space</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html: the Plan 02 helper block (`function renderFightLog(host)` just before `function renderEncounter()`), lines 5026-5061 of the current renderEncounter top (active/dismiss transition/armEncounterButtons/body+rd reset/dismissSlot), the live-combat branch from `const C = S.combat;` through the end of the function (the `rd.textContent = round`, the mw-round-tick pulse, the `head` enc-head/enc-sub block, the C.ally block, the `.foes` roster loop with its UNGUARDED `el.onclick = () => { C.target = i; renderEncounter(); }`, the `renderFightLog(body);` call from Plan 02, the `if (C.pending)` a-fight block, the `acts` action bar, the use-list, the spellmenu, and the guardTap wiring at the end), lines 4967-4984 (foeStatusBadges), lines 5763-5802 (keydown: the `if (S.combat.pending)` branch), lines 5950-5985 (bridge block), lines 5905-5920 (module imports)
    - src/browser/combatPanel.js and src/browser/combatMenu.js (Plan 01 — the view-model shapes: header {label, round, standing}; foes {hint, threat, alive, cards[{i, glyph, name, meta, wpLabel, tag, tagTone, pct, alive, targeted, state}]}; lot {hint, cards[{kind, name, wpLabel, pct, barTone, down, active, third}], overflow}; overlay {icon, iconTone, title, line, roll, primary:{label, dispatch}, secondary})
    - test/unit/shell-input-guards.test.js lines 140-188 (GUARDED_IDS + countGuardedWiring — the wiring must be `guardTap(document.getElementById("id"), …)` or `const v = document.getElementById("id"); … guardTap(v, …)`), lines 261-269 (no card.onclick/body.onclick/panel.onclick/addEventListener in the region), lines 249-258 (keydown: `if (!encArmed()) return;` first statement, `window.mzFight?.()` present)
    - test/unit/shell-fight-gate.test.js lines 59-66 (`if (C.pending)` in renderEncounter; `S.combat.pending` in keydown) and 87-99 (CONDITION_COPY pins — untouched)
    - .planning/phases/34-combat-screen-rebuild/34-RESEARCH.md Pattern 1, Pitfall 6, Pitfall 8
  </read_first>
  <action>
    1. Module import + bridge: add `import { combatHeaderViewModel, foeListViewModel, yourLotViewModel, encounterOverlaySpec } from "./src/browser/combatPanel.js";` and `import { combatMenuViewModel } from "./src/browser/combatMenu.js";` on their own lines after the fightLog.js import; after `window.__mzFightLogVM = …` add a Phase 34 comment and `window.__mzCombatVM = { header: combatHeaderViewModel, foes: foeListViewModel, lot: yourLotViewModel, overlay: encounterOverlaySpec, menu: combatMenuViewModel };` (menu is consumed by Plan 04).
    2. Add four classic helpers AFTER `renderFightLog` and BEFORE `function renderEncounter()` (all DOM built with createElement/textContent; innerHTML only for the static two-span header markup below; element variables named `el`, never `card`):
       - `function renderMajorOverlay(host, spec)`: div `#mw-major.mw-major` (role "dialog", aria-label = spec.title); span `.mw-major-icon` + class spec.iconTone, textContent spec.icon; div `.mw-major-title` (spec.title); div `.mw-major-line` (spec.line); div `.mw-major-roll` only when spec.roll; div `.mw-major-actions` with `<button class="mw-major-btn" id="mw-major-primary">` (spec.primary.label) and, when spec.secondary, `<button class="mw-major-btn secondary" id="mw-major-secondary">`; append to host; then `guardTap(document.getElementById("mw-major-primary"), () => spec.primary.onTap());` and `if (spec.secondary) guardTap(document.getElementById("mw-major-secondary"), () => spec.secondary.onTap());`. Doc comment: Phase 34 (CSCR-06, Decision 2) — the map's MAJOR OVERLAY, generic; Phase 35 reuses it for THE STAIR DOWN and out-of-combat death; every button is guarded.
       - `function renderCombatHeader(host, vm)`: div `#cb-head.cb-head` with `innerHTML` of exactly `<span class="cb-head-label"></span><div class="cb-head-right"><span class="cb-head-round"></span><span class="cb-head-standing"></span></div>` then fill the three spans by textContent from vm.label / vm.round / vm.standing; append.
       - `function renderFoeCards(host, vm, onPick)`: section `#cb-foes.cb-foes` (aria-label "Foes"); a `.cb-foes-head` row with span `.cb-hint` (vm.hint) and span `.cb-threat` (vm.threat); a `.cb-foe-list`; per vm.cards: `el` = `<div class="cb-foe [target|dead]" data-foe="i">` containing `.cb-foe-row` → `.cb-foe-left` (span `.cb-foe-glyph` glyph; div holding `.cb-foe-name` name + `.cb-foe-meta` meta) and `.cb-foe-right` (`.cb-foe-wp` wpLabel; `.cb-foe-tag` [+ class "status" when tagTone === "status"] tag) then `.cb-bar > .cb-bar-fill` with `style.width = pct + "%"`. For a live card: `el.tabIndex = 0; el.setAttribute("role", "button");` and `guardTap(el, () => onPick(c.i));` plus `el.onkeydown = (e) => { if (e.key === "Enter" || e.key === " ") { e.preventDefault(); el.click(); } };`. Dead cards get no handler. Comment: Phase 34 (CSCR-02/08, Decision 3) — retargeting is a guarded presentation mutation; no engine action exists or is added.
       - `function renderYourLot(host, vm)`: section `#cb-lot.cb-lot` (aria-label "Your lot"); div `.cb-hint` (vm.hint); div `.cb-lot-strip` with `dataset.overflow = vm.overflow ? "1" : "0"`; per card: `el` = `<div class="cb-lot-card [active] [down]">` → `.cb-lot-name`, `.cb-lot-wp` (wpLabel), `.cb-lot-bar > .cb-lot-fill [low]` (style.width pct%), `.cb-lot-third` (third). Comment: Phase 34 (CSCR-03) — hero first, then every state.party member, then a summoned ally; the strip is overflow-x:auto so more members can join later (PARTY_CAP is 1 today).
    3. At the top of renderEncounter, directly after the existing `armEncounterButtons();` call, add `panel.dataset.mode = "legacy";` (comment: Phase 34 — every branch starts in the pre-Phase-34 styling; the combat/pending branches (and, from Plan 05, the endings/joiner/find) opt into the mock palette by setting "dark"). Then rewrite the live-combat branch of renderEncounter (everything from `const C = S.combat;` to the end of the function) as: `const C = S.combat; rd.textContent = ""; panel.dataset.mode = "dark";` (comment: the legacy topbar is hidden by the dark mode; the mw-round-tick pulse is retired with it); then the pending gate FIRST: `if (C.pending) { const spec = window.__mzCombatVM.overlay(S); renderMajorOverlay(body, { ...spec, primary: { label: spec.primary.label, onTap: () => window.mzFight?.() }, secondary: null }); return; }` (comment: Phase 34 CSCR-06 — the combat screen never renders a pending combat; nothing rolls before the tap, Phase 31 rule); then `renderCombatHeader(body, window.__mzCombatVM.header(S));` · `const mid = document.createElement("div"); mid.className = "cb-mid"; mid.id = "cb-mid";` · `renderFoeCards(mid, window.__mzCombatVM.foes(S, { chipsFor: (f) => foeStatusBadges(f).map((b) => b.t) }), (i) => { S.combat.target = i; renderEncounter(); });` · `renderYourLot(mid, window.__mzCombatVM.lot(S));` · `renderFightLog(mid);` (move the Plan 02 call here) · `body.appendChild(mid);` · after appending, if `window.__mzFightLog && window.__mzFightLog.seq !== lastLogSeqShown` (a new classic module-scope `let lastLogSeqShown = 0;` declared beside `let encWasActive`), set `lastLogSeqShown = window.__mzFightLog.seq` and call `mid.querySelector(".cb-log-entry")?.scrollIntoView({ block: "nearest" })`; then `const act = document.createElement("div"); act.className = "cb-act"; act.id = "cb-act"; body.appendChild(act);` and re-parent the EXISTING action-bar code into it unchanged in behaviour: the `acts` element, the use-list `useWrap` + `renderCarriedList(... guard: true ...)`, and the `menu` spellmenu are appended to `act` instead of `body`, and every existing `guardTap(...)` wiring line for a-strike/a-potion/a-flee/a-spell/a-talk/a-sing/a-scroll stays exactly as it is (Plan 04 replaces this whole block). Delete: the old `<p class="enc-head">${C.type}</p>…` head block, the `.foes` roster loop with its unguarded onclick/onkeydown, the C.ally block (the ally is now a YOUR LOT card), the mw-round-tick lines, and the old `if (C.pending) { … a-fight … }` block. `#a-fight` must not exist anywhere in the file afterwards.
    4. keydown: in the `if (S.combat.pending) { … }` branch keep `S.combat.pending` and `window.mzFight?.()` and drop the `|| k === "1"` so only Enter/Space dispatch fight (comment: Phase 34 CSCR-06 — Enter/Space on the major overlay = FIGHT IT OUT; digits belong to the grid). Leave `if (!encArmed()) return;` as the first statement inside `if (S.combat) {`. Leave the beats-branch Enter/Space line untouched for now (Plan 05 edits it).
    5. Run `node --test test/unit/shell-fight-gate.test.js test/unit/shell-input-guards.test.js test/unit/foe-effect-chip.test.js test/unit/shell-loot-screen.test.js test/unit/shell-map-store-polish.test.js` — expect exactly these failures before Task 3: shell-input-guards "every §6.3 decision button id is wired through guardTap" (a-fight) and possibly the `guard: true` count if you moved the use-list call (you must NOT have — it stays `guard: true`). Everything else must pass.
  </action>
  <verify>
    <automated>grep -c "function renderMajorOverlay(host, spec)" mazeworld.html && grep -c 'panel.dataset.mode = "dark"' mazeworld.html && grep -c "S.combat.target = i; renderEncounter();" mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function renderMajorOverlay(host, spec)" mazeworld.html` = 1; `grep -c "function renderCombatHeader(host, vm)" mazeworld.html` = 1; `grep -c "function renderFoeCards(host, vm, onPick)" mazeworld.html` = 1; `grep -c "function renderYourLot(host, vm)" mazeworld.html` = 1.
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function renderFightLog(host)');const m=s.indexOf('function renderMajorOverlay(host, spec)');const b=s.indexOf('function renderEncounter()');process.exit(a>0&&m>a&&b>m?0:1)"` exits 0 (helper order: renderFightLog → renderMajorOverlay … → renderEncounter).
    - `grep -c 'id="mw-major-primary"\|"mw-major-primary"' mazeworld.html` >= 2; `grep -c 'guardTap(document.getElementById("mw-major-primary")' mazeworld.html` = 1.
    - `grep -c 'id="a-fight"' mazeworld.html` = 0.
    - `grep -v '^\s*//' mazeworld.html | grep -c '"a-fight"'` = 0.
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const r=s.slice(s.indexOf('function renderEncounter()'),s.indexOf('function noteCombat('));const p=r.indexOf('if (C.pending)');const h=r.indexOf('renderCombatHeader(body');const f=r.indexOf('renderFoeCards(mid');const l=r.indexOf('renderYourLot(mid');const g=r.indexOf('renderFightLog(mid)');const a=r.indexOf('act.id = \"cb-act\"');process.exit(p>0&&h>p&&f>h&&l>f&&g>l&&a>g?0:1)"` exits 0 (pending gate before the bands; band order header → foes → lot → log → act).
    - `grep -c "S.combat.target = i; renderEncounter();" mazeworld.html` = 1; `grep -c "guardTap(el, () => onPick(" mazeworld.html` = 1.
    - `grep -c "C.target = i; renderEncounter();" mazeworld.html` = 0.
    - `grep -c "mzRetarget\|retarget" mazeworld.html` = 0.
    - `grep -c 'panel.dataset.mode = "legacy";' mazeworld.html` = 1 and it sits before `panel.dataset.mode = "dark"` in the renderEncounter region (non-dark branches — won, beats, store — keep the pre-Phase-34 styling).
    - `grep -c "chipsFor: (f) => foeStatusBadges(f).map((b) => b.t)" mazeworld.html` = 1.
    - `grep -c 'window.__mzCombatVM = { header: combatHeaderViewModel, foes: foeListViewModel, lot: yourLotViewModel, overlay: encounterOverlaySpec, menu: combatMenuViewModel };' mazeworld.html` = 1; `grep -c 'from "./src/browser/combatPanel.js"' mazeworld.html` = 1; `grep -c 'from "./src/browser/combatMenu.js"' mazeworld.html` = 1.
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const r=s.slice(s.indexOf('function renderFightLog(host)'),s.indexOf('function noteCombat(')).replace(/\/\/[^\n]*/g,'');process.exit(/card\.onclick|body\.onclick|panel\.onclick|body\.addEventListener|panel\.addEventListener|card\.addEventListener/.test(r)?1:0)"` exits 0.
    - `grep -c "window.mzFight?.()" mazeworld.html` >= 2 (overlay onTap + keydown).
    - `grep -c 'k === "1") { e.preventDefault(); window.mzFight' mazeworld.html` = 0.
    - `grep -c "guard: true" mazeworld.html` = 2 (loot card + the still-present combat use-list); `grep -c 'id="a-strike"' mazeworld.html` = 1 (the old bar still renders inside .cb-act until Plan 04).
    - `grep -c 'classList.add("mw-round-tick")' mazeworld.html` = 0.
  </acceptance_criteria>
  <done>A fought combat renders as the mock's three bands from the view-models with guarded retargeting; a pending combat renders only the major overlay; the old action bar survives inside .cb-act for Plan 04.</done>
</task>

<task type="auto">
  <name>Task 3: shell-combat-screen.test.js + re-pin shell-fight-gate and shell-input-guards</name>
  <files>test/unit/shell-combat-screen.test.js, test/unit/shell-fight-gate.test.js, test/unit/shell-input-guards.test.js</files>
  <read_first>
    - test/unit/shell-fight-log.test.js (Plan 02 — copy its stripComments/sliceBetween helpers and region style)
    - test/unit/shell-input-guards.test.js lines 66-95 (region helpers), 140-188 (GUARDED_IDS + countGuardedWiring), 200-210 (guard: true exactly twice — UNCHANGED in this plan)
    - test/unit/shell-fight-gate.test.js lines 54-72 (the pending-gate pins) and 101-120 (CMB-02 pins — unchanged in this plan; Plan 04 re-pins them)
    - mazeworld.html regions written in Tasks 1-2
  </read_first>
  <action>
    1. Create `test/unit/shell-combat-screen.test.js` (header: Phase 34 CSCR-01/02/03/06/08, Plan 03 — source assertions over mazeworld.html; the view-model behaviour is proven in combatPanel.test.js). Regions: `helpersRegion()` = "function renderFightLog(host)" → "function renderEncounter()"; `fnRegion(sig)` = from the exact signature string to the NEXT "\nfunction " after it (per-function slice — use it for every "contains no innerHTML" assertion so later helpers cannot invalidate the test); `renderEncounterRegion()` = "function renderEncounter()" → "function noteCombat("; `combatBranch()` = from "const C = S.combat;" (inside renderEncounterRegion) → the end of the region; `keydownRegion()` = 'addEventListener("keydown"' → 'addEventListener("resize"'; `styleBlock()` = "<style>" → "</style>" of the RAW html. Tests:
       a. Layout order (CSCR-01): inside combatBranch the indexes of `if (C.pending)` < `renderCombatHeader(body` < `mid.id = "cb-mid"` < `renderFoeCards(mid` < `renderYourLot(mid` < `renderFightLog(mid)` < `act.id = "cb-act"` are strictly increasing; `panel.dataset.mode = "dark"` occurs in combatBranch before `if (C.pending)`.
       b. Three bands in CSS: `.cb-head{` has `flex:none`, `border-bottom:3px solid #6b2c22`, `background:#1e120f`; `.cb-mid{` has `flex:1`, `overflow:auto`, `min-height:0`; `.cb-act{` has `flex:none`, `border-top:3px solid #3a3226`, `background:#1b170f`; `#enc-panel[data-mode="dark"]{` has `background:#120f0a` and `color:#e6ddc6`; `#enc-panel[data-mode="dark"] .enc-topbar{display:none}` exists.
       c. Fonts (CSCR-01): styleBlock contains no `fonts.googleapis`; `@font-face` occurs exactly 3 times; `.cb-head-label{` uses `var(--disp)`; `.cb-log-text{` uses `var(--mono)`; `:root{` maps `--disp` to "Press Start 2P" and `--mono` to "Courier Prime".
       d. Foe cards (CSCR-02): `fnRegion("function renderFoeCards(host, vm, onPick)")` contains `cb-foe-glyph`, `cb-foe-name`, `cb-foe-meta`, `cb-foe-wp`, `cb-foe-tag`, `cb-bar-fill`, `dataset.foe` or `data-foe`, `guardTap(el, () => onPick(` exactly once, `el.tabIndex = 0`, and no `innerHTML`; renderEncounterRegion contains `S.combat.target = i; renderEncounter();` once and `chipsFor: (f) => foeStatusBadges(f).map((b) => b.t)` once; CODE contains no `C.target = i; renderEncounter();` and no `retarget`.
       e. YOUR LOT (CSCR-03): `fnRegion("function renderYourLot(host, vm)")` contains `cb-lot-strip`, `cb-lot-card`, `cb-lot-name`, `cb-lot-wp`, `cb-lot-fill`, `cb-lot-third`, `dataset.overflow`; `.cb-lot-strip{` CSS contains `overflow-x:auto`; `.cb-lot-card.active{` has `border-color:#e8c97a`; `.cb-lot-fill.low{background:#e05a48}` exists.
       f. Major overlay (CSCR-06, Decision 2): `function renderMajorOverlay(host, spec)` exists in helpersRegion, and `fnRegion("function renderMajorOverlay(host, spec)")` contains `mw-major-icon`, `mw-major-title`, `mw-major-line`, `mw-major-roll`, `mw-major-actions`, `id="mw-major-primary"` or `"mw-major-primary"`, `guardTap(document.getElementById("mw-major-primary")` once, `spec.secondary` at least twice (optional secondary button — the Phase 35 hook), and no `innerHTML`; combatBranch contains `window.__mzCombatVM.overlay(S)` and `onTap: () => window.mzFight?.()`; the `if (C.pending) {` block ends with `return;` before `renderCombatHeader(`; CODE has zero `a-fight`.
       g. Keys (CSCR-06): keydownRegion contains `S.combat.pending`, `window.mzFight?.()`, and the first non-blank statement after `if (S.combat) {` is `if (!encArmed()) return;`; the pending branch does not dispatch on the digit key (regex `/k === "1"\) \{ e\.preventDefault\(\); window\.mzFight/` absent).
       h. Guards (CSCR-08): the helpers+renderEncounter region ("function renderFightLog(host)" → "function noteCombat(") contains no `card.onclick`, `body.onclick`, `panel.onclick`, `body.addEventListener`, `panel.addEventListener`; the guard-helper region has no transition/animation token; styleBlock has no `aria-disabled` selector; CODE has zero `transitionend`/`animationend` (case-insensitive).
       i. Bridge: `window.__mzCombatVM = {` once; imports from `./src/browser/combatPanel.js` and `./src/browser/combatMenu.js` once each.
       j. Ally folded in: `renderYourLot` is the only consumer — CODE contains no `className = "foe ally"` and no `Fighting for you ·` (the old ally card is gone), and combatPanel.js source contains `allyThird`.
    2. Re-pin `test/unit/shell-input-guards.test.js`: (a) change `renderEncounterRegion()` to `sliceBetween(CODE, "function renderFightLog(host)", "function noteCombat(")` with a comment "Phase 34: the render helpers that wire guarded buttons sit directly before renderEncounter"; (b) in GUARDED_IDS replace `"a-fight"` with `"mw-major-primary"` (comment: Phase 34 CSCR-06 — the Fight! gate is the major overlay's primary button); leave every other id and every other test untouched (the `guard: true` exactly-twice pin still holds in this wave).
    3. Re-pin `test/unit/shell-fight-gate.test.js`: the test "renderEncounter's Fight! gate reads combat.pending" additionally asserts the region contains `renderMajorOverlay(body` and does not contain `"a-fight"`; the keydown test additionally asserts the digit-1 fight dispatch is gone (regex from 1.g). Leave the CMB-02 button pins (lines 101-120) untouched — Plan 04 re-pins them with the grid.
    4. Run `node --test test/unit/shell-combat-screen.test.js test/unit/shell-fight-gate.test.js test/unit/shell-input-guards.test.js test/unit/foe-effect-chip.test.js test/unit/shell-loot-screen.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-fight-log.test.js`, then `npm test` and `npm run build:www`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-combat-screen.test.js test/unit/shell-fight-gate.test.js test/unit/shell-input-guards.test.js && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-combat-screen.test.js` exits 0 with at least 10 tests.
    - `grep -c '"mw-major-primary"' test/unit/shell-input-guards.test.js` = 1; `grep -c 'sliceBetween(CODE, "function renderFightLog(host)", "function noteCombat(")' test/unit/shell-input-guards.test.js` = 1.
    - `grep -c '"a-fight"' test/unit/shell-input-guards.test.js` = 0.
    - `grep -c "renderMajorOverlay(body" test/unit/shell-fight-gate.test.js` >= 1.
    - `npm test` exits 0; `npm run build:www` exits 0.
    - `git diff --stat -- engine content test/parity` is empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
  </acceptance_criteria>
  <done>The layout, foe cards, YOUR LOT, major overlay, guarded retarget and Enter/Space gate are all pinned; the two existing suites are re-pinned to the new ids and region.</done>
</task>

</tasks>

<verification>
- `npm test` green; `npm run build:www` exit 0; `git diff -- engine content test/parity` empty; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
- SUMMARY contract (deferred UAT): end `34-03-SUMMARY.md` with `## Human verification (deferred to end of run)` listing these Pixel 7 checks: (1) walk into an encounter — the screen goes near-black with a red ● , "SOMETHING IS HERE" (one foe) or "THEY ARE ALREADY HERE" (several), the foes named, the d8/d10 roll note and ONE gold FIGHT IT OUT button; nothing happens before the tap; a tap within a quarter second of the overlay appearing is swallowed; (2) after FIGHT IT OUT the combat screen shows the ENCOUNTER header with ROUND 1 and N STANDING, the foe cards (glyph, upper-case name, SIZE · INT · note, hp / max, red bar), the YOUR LOT strip (hero card gold), and the › log with "You move first." / "They move first." on top; (3) tap a second foe card — its border turns gold and the tag reads TARGET; tap a dead card — nothing; (4) accept a Joiner beforehand and confirm YOUR LOT shows the companion card beside the hero, the hint reads YOUR LOT · EACH ROLLS THEIR OWN, and the strip scrolls sideways if the cards exceed the width; (5) the map and D-pad are never visible beneath the combat screen; (6) with the system reduced-motion setting on, the overlay and panel appear without fades and buttons still respond after the 250 ms arm window.
</verification>

<success_criteria>
- ROADMAP success criterion 1 (major overlay on encounter, nothing resolves before the tap, tap opens the combat screen at round 1) is source-provable via the pending-gate-first order and the guarded `mw-major-primary` → `window.mzFight` path.
- CSCR-01/02/03 layout and content are source-pinned to the spec's values; Decisions 2 and 3 are implemented and recorded.
- Guards: every new tap target (foe cards, overlay button) goes through guardTap; the old bar's wiring is untouched pending Plan 04.
</success_criteria>

<output>
Create `.planning/phases/34-combat-screen-rebuild/34-03-SUMMARY.md` when done (include the deferred-UAT section per the verification contract).
</output>
