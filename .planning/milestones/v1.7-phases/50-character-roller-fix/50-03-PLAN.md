---
phase: 50-character-roller-fix
plan: 03
type: execute
wave: 3
depends_on: ["50-01", "50-02"]
files_modified:
  - mazeworld.html
  - test/unit/roller.test.js
  - test/unit/shell-abilities.test.js
  - src/browser/bridge.js
  - docs/SHELL-MODULES.md
autonomous: true
requirements: [ROLL-01]

must_haves:
  truths:
    - "ROLL-01 / CONTEXT 'mazeworld.html becomes a mount that wires the DOM ids and window.mzStartRoll': the module script contains exactly one createRoller({ doc: document, startNewRun, sheetFor: characterSheetViewModel, onCommit }) call and `window.mzStartRoll = roller.start;`; the inline ROLLER_* tables, timers, pickDisplay/setReel, the async startRoll function and the initRollerScreen IIFE are gone (shell-sweep refs = 0)"
    - "SC1 / CONTEXT 'keep commitRolledState as the commit callback': onCommit runs commitRolledState(state) then window.__mzShowTab?.(\"maze\"); commitRolledState's body is unchanged (surfaceAbilityPool(state) still its last statement)"
    - "ROLL-01 'no second startNewRun() on the path': the module script has exactly ONE startNewRun( call expression (the dev start-at-depth row); the roller path reaches the adapter only through the injected reference — pinned by the mount pins appended to test/unit/roller.test.js"
    - "CONTEXT 'keep window.mzStartRoll as the bridge name (both call sites + the bridge registry test)': the two call sites (title ENTER in initTitleScreen, the dead Hero tab's New Character in the classic script) are unchanged; test/unit/bridge-registry.test.js stays green (mzStartRoll is a window.mz* name, not a __mz bridge; the two bridge.js consumer strings that named initRollerScreen are re-worded and the doc table regenerated)"
    - "SC3 / CONTEXT 'Repro-pass record: a three-row table in the SUMMARY (normal / double-tap / Play-again-from-death → reels shown, Hero tab shown, match?)': tools/roller-repro.mjs run against the FIXED shell shows zero mismatches, pasted verbatim as the AFTER table; if the browser cannot be driven the SUMMARY says 'not reproduced in browser; structurally guarded; device check in the Phase 55 batch'"
    - "CONTEXT 'docs/SHELL-MODULES.md — add the roller module row': the doc's Contract section describes createRoller's options/return and the mount wiring; the bridge table is regenerated by tools/bridge-doc.mjs --write"
    - "SC4: git diff --stat for the phase shows no path under engine/, content/ or test/parity/; the master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - "mazeworld.html — `import { createRoller } from \"./src/browser/roller.js\";`, the roller mount, `window.mzStartRoll = roller.start;`; the `import { RACES, CLASSES }` line deleted (its only readers moved)"
    - "test/unit/roller.test.js — appended mount pins (m1–m7)"
    - "test/unit/shell-abilities.test.js — the RACES/CLASSES import pin re-pointed to src/browser/roller.js"
    - "src/browser/bridge.js + docs/SHELL-MODULES.md — consumer strings without initRollerScreen; roller contract row; regenerated table"
    - ".planning/phases/50-character-roller-fix/50-03-repro-after.txt — the AFTER table"
  key_links:
    - "title ENTER / dead Hero tab New Character -> window.mzStartRoll -> roller.start -> chain.then(() => startNewRun()) -> token check -> reels/reveal read sheetFor(rollerPendingState) -> CTA -> commit() -> onCommit -> commitRolledState(state) + window.__mzShowTab(\"maze\") -> paint() -> renderHeroTab reads the same state.c"
    - "mazeworld.html module script -> src/browser/roller.js#createRoller; src/browser/heroTab.js#characterSheetViewModel (sheetFor); src/browser/engineAdapter.js#startNewRun"
---

<objective>
Swap the inline roller block in `mazeworld.html` for the `src/browser/roller.js` mount (one `createRoller({ doc: document, startNewRun, sheetFor: characterSheetViewModel, onCommit })` call; `window.mzStartRoll = roller.start`), keep `commitRolledState` as the commit callback and `window.mzStartRoll` as the bridge name both roll triggers call, re-home the one shell pin that read the deleted `RACES, CLASSES` import, append the mount pins to `test/unit/roller.test.js`, re-word the two `bridge.js` consumer strings that named `initRollerScreen` and regenerate the bridge table, document the roller contract in `docs/SHELL-MODULES.md`, then re-run `tools/roller-repro.mjs` against the fixed shell for the AFTER table that closes SC3, and run the phase gates.

Purpose: ROLL-01 lands in the shipped shell: the reels, the pending state and the CTA commit are one object behind one guarded, serialized roll; the phase's evidence (unit pins + browser table) is recorded.

Output: the swapped `mazeworld.html`; mount pins; re-pointed shell pin; bridge/doc updates; `50-03-repro-after.txt`; `npm run build:www` green; `npm test` fail 0; `npm run boot:check` four PASS; fence clean; SUMMARY with the three-row table and the deferred human-verification section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-character-roller-fix/50-CONTEXT.md
@.planning/phases/50-character-roller-fix/50-01-SUMMARY.md
@.planning/phases/50-character-roller-fix/50-02-SUMMARY.md
@src/browser/roller.js
@docs/SHELL-MODULES.md
@test/unit/heroTab.test.js
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->
<!-- planner-discipline-allow: ROLLER_LOCK_DELAYS -->
<!-- planner-discipline-allow: ROLLER_REVEAL_DELAY -->
<!-- planner-discipline-allow: ROLLER_SPIN_MS -->
<!-- planner-discipline-allow: ROLLER_RACE_NAMES -->
<!-- planner-discipline-allow: ROLLER_CLASS_NAMES -->
<!-- planner-discipline-allow: ROLLER_SUB_NAMES -->
<!-- planner-discipline-allow: rollerSpinTimer -->
<!-- planner-discipline-allow: rollerLockTimers -->
<!-- planner-discipline-allow: rollerPendingState -->
<!-- planner-discipline-allow: clearRollerTimers -->
<!-- planner-discipline-allow: pickDisplay -->
<!-- planner-discipline-allow: setReel -->
<!-- planner-discipline-allow: initRollerScreen -->
<!-- planner-discipline-allow: function startRoll( -->
<!-- planner-discipline-allow: RACES, CLASSES -->

<phase_ground_rules>
- **Shell-only phase, engine fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/**`; `test/parity/prototype-master.js.txt` keeps hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`. No bot readout, no fixture regeneration.
- **One executor on `mazeworld.html` in this wave** — this plan is alone in wave 3.
- **Pinned lines that must stay byte-identical in the module script:** `import { characterSheetViewModel, rationsViewModel, eatsLineFor, renderHeroTab } from "./src/browser/heroTab.js";` (test/unit/shell-abilities.test.js + shell-clarity-43.test.js); `import { ROMAN } from "./content/index.js";` exactly once (shell-no-content-copies.test.js); `function commitRolledState(state) {` with `surfaceAbilityPool(state);` as its LAST statement (shell-abilities.test.js); `window.mzDevStartAtDepth = async function devStartAtDepth(depth) {` and its body (perfMarks.test.js, shell-abilities.test.js).
- **Greenfield ruling:** the inline roller code is deleted in the same commit the mount lands — no fallback, no dual path. Deferred (CONTEXT ## Deferred Ideas): persist-on-commit, recordBest on a superseded roll, back-button roller awareness — untouched.
- **Docs describe the code as it is (Phase 48 standard):** every comment/string that named `initRollerScreen`, the inline word lists, or `window.newGame` on this path is re-worded; `docs/SHELL-MODULES.md` gains the roller contract.
- **Gate order matters:** run `npm run build:www` BEFORE `npm test` (test/unit/hp-not-wp.test.js scans `www/index.html` when present — it must be the swapped shell), then `npm run boot:check` (boots `www/index.html` headless: four PASS lines).
- **Windows executor environment:** Git Bash, no `jq`; multi-line scripts in files; line numbers drift — anchor by names.
- **Verification agents are off:** the acceptance criteria ARE the verification.
</phase_ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: swap the inline roller for the roller.js mount; re-home the shell pin; append mount pins; bridge strings + docs</name>
  <files>mazeworld.html, test/unit/roller.test.js, test/unit/shell-abilities.test.js, src/browser/bridge.js, docs/SHELL-MODULES.md</files>
  <read_first>
    - mazeworld.html module script: (a) the import block — the `04-DR3 (the roll screen)` comment above `import { armorDisplay, bagArmorText, lootCompare } from "./src/browser/viewModels.js";`, the heroTab.js import line (pinned), `import { renderStoreScreen } from "./src/browser/storeScreen.js";`, `import { RACES, CLASSES } from "./content/index.js";` (to delete — its only readers are the inline reel word lists), the `Phase 44 (DEAD-02) — a SEPARATE import line` comment and `import { ROMAN } from "./content/index.js";` (pinned, stays); (b) `function commitRolledState(state) {` and its doc comment (mentions `window.newGame() above` — stale since Phase 44); (c) the inline roller block from the comment line beginning `// Presentation-only word lists for the reel flicker` through the `})();` that closes `(function initRollerScreen() {` — everything in between is deleted; (d) the two call sites `window.mzStartRoll();` (classic script: `#btn-abandon-character` onclick's `st.dead` branch; module script: `initTitleScreen`'s `enterBtn.onclick`) — unchanged.
    - src/browser/roller.js — `createRoller` option names and the returned `{ start, commit, pending, dispose }`.
    - test/unit/roller.test.js (Plan 01) — append the mount-pin section at the end; copy `stripComments` + `extractScriptRegions` + `sliceBetween` from test/unit/heroTab.test.js (sibling-test precedent, not exported).
    - test/unit/shell-abilities.test.js — the test `window.__mzAbilities is retired; characterSheetViewModel/ABILITY_BY_ID/abilityRoundsLeft/isReady are heroTab.js-local...` contains `assert.match(CODE, /import \{ RACES, CLASSES \} from "\.\/content\/index\.js";/);` — the one pin to re-point; `HERO_TAB_RAW`/`HERO_SRC` show how a module source is read in that file.
    - src/browser/bridge.js — the `__mzClassicBoot` entry's consumer string `mazeworld.html (module: initRollerScreen — awaits the classic boot before first paint)` and the `__mzShowTab` entry's consumer string `mazeworld.html (module: initRollerScreen / the death-screen router — switches tabs after boot or death)`.
    - docs/SHELL-MODULES.md — the intro paragraph, `## Contract`, and the `<!-- bridge-table:start -->` … `<!-- bridge-table:end -->` block regenerated by `node tools/bridge-doc.mjs --write`.
    - test/unit/shell-no-content-copies.test.js criterion 3 (no shell re-declaration of any src/browser export) and the `import { ROMAN }` exactly-once pin.
  </read_first>
  <action>
    1. mazeworld.html imports: add, as its own line directly after `import { renderStoreScreen } from "./src/browser/storeScreen.js";`, a one-line Phase 50 comment and `import { createRoller } from "./src/browser/roller.js";`. Delete the `import { RACES, CLASSES } from "./content/index.js";` line. Re-word the `04-DR3 (the roll screen)` comment above the viewModels.js import so it says the roller mounts from src/browser/roller.js, that `characterSheetViewModel` (heroTab.js — the SAME rng-free view model the HERO tab renders from) is injected as its `sheetFor`, and that the reel-flicker word lists live in roller.js (`reelWordLists`); re-word the `Phase 44 (DEAD-02) — a SEPARATE import line` comment so it no longer refers to a "pinned line above" that is gone (say: kept as its own line — shell-no-content-copies pins it exactly once). The heroTab.js and ROMAN import lines stay byte-identical.
    2. mazeworld.html roller block: delete everything from the `// Presentation-only word lists for the reel flicker` comment through the `})();` closing the `initRollerScreen` IIFE, and put the mount in its place — a short Phase 50 (ROLL-01) comment (one serialized `startNewRun()` per roll; a monotonic roll token drops a superseded resolution before it touches the reels or the pending state; reels + CTA read the same pending object — SC1/SC2; `commitRolledState` above is the commit callback; `window.mzStartRoll` stays the bridge name both roll triggers call), then exactly: `const roller = createRoller({` / `doc: document,` / `startNewRun,` / `sheetFor: characterSheetViewModel,` / `onCommit: (state) => {` / `commitRolledState(state);` / `window.__mzShowTab?.("maze");` / `},` / `});` / `window.mzStartRoll = roller.start;`. Update `commitRolledState`'s doc comment: the state is "rolled once by src/browser/roller.js's createRoller (mounted as window.mzStartRoll below), via the SAME startNewRun() seam window.mzDevStartAtDepth above uses" — the function line and body are untouched (`surfaceAbilityPool(state);` remains its last statement). The CSS comment (`.mw-roller-screen` block) and the markup comment that say "the trailing module script's window.mzStartRoll()/#mw-roller-cta wiring" stay true and may stay as they are.
    3. test/unit/shell-abilities.test.js: in that test, replace the RACES/CLASSES `assert.match(CODE, ...)` with two assertions — `assert.equal((CODE.match(/import \{ RACES, CLASSES \} from "\.\/content\/index\.js";/g) || []).length, 0)` (the shell no longer imports them) and `assert.match(ROLLER_SRC, /import \{ RACES, CLASSES \} from "\.\.\/\.\.\/content\/index\.js";/)` (they live in roller.js now), where `ROLLER_SRC` is read + comment-stripped exactly like `HERO_SRC` (add `const ROLLER_RAW = fs.readFileSync(path.join(REPO_ROOT, "src", "browser", "roller.js"), "utf8").replace(/\r\n/g, "\n"); const ROLLER_SRC = stripComments(ROLLER_RAW);` next to HERO_SRC). Add a Phase 50 note to the test's comment.
    4. test/unit/roller.test.js: append a `// ─── mount pins (Phase 50, Plan 03) ───` section that reads mazeworld.html (`HTML_RAW`), splits classic/module regions with `extractScriptRegions`, comment-strips both (`stripComments`), and asserts: (m1) MOD contains `import { createRoller } from "./src/browser/roller.js";` exactly once and `import { RACES, CLASSES } from "./content/index.js";` zero times; (m2) MOD contains exactly one `createRoller(` and the slice from `const roller = createRoller({` to the next `});` contains `doc: document,`, `startNewRun,`, `sheetFor: characterSheetViewModel,`, `commitRolledState(state);` and `window.__mzShowTab?.("maze");`; (m3) MOD contains `window.mzStartRoll = roller.start;` exactly once and `function startRoll(` zero times; (m4) MOD's `startNewRun(` call-expression count (regex `/\bstartNewRun\(/g`) is exactly 1 and that match sits inside the `window.mzDevStartAtDepth = async function devStartAtDepth(depth) {` … `\n  };` region — no second startNewRun call on the roller path; (m5) `window.mzStartRoll()` occurs exactly twice across CLASSIC + MOD; (m6) each of `ROLLER_LOCK_DELAYS`, `ROLLER_REVEAL_DELAY`, `ROLLER_SPIN_MS`, `ROLLER_RACE_NAMES`, `rollerPendingState`, `clearRollerTimers`, `initRollerScreen`, `pickDisplay(`, `setReel(` occurs zero times in CLASSIC and MOD; (m7) `function commitRolledState(state) {` occurs exactly once in MOD.
    5. src/browser/bridge.js: re-word the two consumer strings — `__mzClassicBoot`: `mazeworld.html (module: the boot sequence after the roller mount — awaits the classic boot before first paint)`; `__mzShowTab`: `mazeworld.html (module: the roller mount's onCommit / the death-screen router — switches tabs after commit or death)`. Then `node tools/bridge-doc.mjs --write` to regenerate the table in docs/SHELL-MODULES.md.
    6. docs/SHELL-MODULES.md: extend the intro paragraph ("… and the character roller mounts from `roller.js` (Phase 50)") and add a `### Screens` subsection under `## Contract` (before `## What stays shared`) documenting `createRoller({ doc, startNewRun, sheetFor, onCommit, timers, random, words })` → `Object.freeze({ start, commit, pending, dispose })`: `doc` is the Document (the module reads it only through `getElementById` on the eight `mw-roller-*` ids); `startNewRun` is engineAdapter.js's seam (called with no arguments, serialized — never in flight twice); `sheetFor` is heroTab.js's `characterSheetViewModel`; `onCommit(state)` is the mount's `commitRolledState(state)` + `window.__mzShowTab("maze")`; `timers`/`random`/`words` are test seams (defaults: the global timers, `Math.random`, `reelWordLists()`); the mount assigns `window.mzStartRoll = roller.start` — the bridge both roll triggers call; the same never-`window`/`document` rule applies; a superseded roll (start() re-entered before the previous resolution) is dropped by a monotonic token and the reels/CTA always read the one pending state.
    7. Sanity: `node tools/shell-sweep.mjs refs ROLLER_RACE_NAMES ROLLER_CLASS_NAMES ROLLER_SUB_NAMES ROLLER_LOCK_DELAYS ROLLER_REVEAL_DELAY ROLLER_SPIN_MS rollerSpinTimer rollerLockTimers rollerPendingState clearRollerTimers pickDisplay setReel initRollerScreen` exits 0; `node --test test/unit/roller.test.js test/unit/shell-abilities.test.js test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js test/unit/heroTab.test.js test/unit/shell-clarity-43.test.js test/unit/perfMarks.test.js` fail 0. Commit: `refactor(50-03): mount the roller from src/browser/roller.js — window.mzStartRoll = roller.start, commitRolledState as onCommit; inline roller deleted; pins re-homed; docs (ROLL-01)`.
  </action>
  <verify>
    <automated>node tools/shell-sweep.mjs refs ROLLER_RACE_NAMES ROLLER_CLASS_NAMES ROLLER_SUB_NAMES ROLLER_LOCK_DELAYS ROLLER_REVEAL_DELAY ROLLER_SPIN_MS rollerSpinTimer rollerLockTimers rollerPendingState clearRollerTimers pickDisplay setReel initRollerScreen && node --test test/unit/roller.test.js test/unit/shell-abilities.test.js test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js test/unit/heroTab.test.js test/unit/shell-clarity-43.test.js test/unit/perfMarks.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import { createRoller } from "./src/browser/roller.js";' mazeworld.html` prints 1
    - `grep -c 'import { RACES, CLASSES } from "./content/index.js";' mazeworld.html` prints 0; `grep -c 'import { ROMAN } from "./content/index.js";' mazeworld.html` prints 1; `grep -c 'import { characterSheetViewModel, rationsViewModel, eatsLineFor, renderHeroTab } from "./src/browser/heroTab.js";' mazeworld.html` prints 1
    - `grep -c "createRoller(" mazeworld.html` prints 1; `grep -c "sheetFor: characterSheetViewModel," mazeworld.html` prints 1; `grep -c "window.mzStartRoll = roller.start;" mazeworld.html` prints 1
    - `grep -c "window.mzStartRoll()" mazeworld.html` prints 2
    - `node tools/shell-sweep.mjs refs startNewRun 2>&1 | grep -cE "startNewRun\("` prints 1 (comment-stripped: the only call expression left in the shell is the dev row's `await startNewRun(undefined, { startDepth: depth })`; the mount passes the bare reference `startNewRun,`)
    - `node tools/shell-sweep.mjs refs ROLLER_RACE_NAMES ROLLER_CLASS_NAMES ROLLER_SUB_NAMES ROLLER_LOCK_DELAYS ROLLER_REVEAL_DELAY ROLLER_SPIN_MS rollerSpinTimer rollerLockTimers rollerPendingState clearRollerTimers pickDisplay setReel initRollerScreen` exits 0
    - `grep -c "function commitRolledState(state) {" mazeworld.html` prints 1
    - `grep -c "initRollerScreen" src/browser/bridge.js docs/SHELL-MODULES.md` prints 0 for each file; `grep -c "createRoller" docs/SHELL-MODULES.md` prints a number >= 2; `grep -c "roller.js" docs/SHELL-MODULES.md` prints a number >= 2
    - `grep -c "ROLLER_SRC" test/unit/shell-abilities.test.js` prints a number >= 2
    - `grep -c "mount pins" test/unit/roller.test.js` prints a number >= 1 and `node --test test/unit/roller.test.js 2>&1 | grep -E "^# pass"` prints a number >= 12 (the Plan 01 count plus the appended mount-pin tests; m1–m7 may be grouped into fewer test() calls)
    - `node --test test/unit/roller.test.js test/unit/shell-abilities.test.js test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js test/unit/heroTab.test.js test/unit/shell-clarity-43.test.js test/unit/perfMarks.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0`
    - `git status --porcelain engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The shell mounts the roller through one createRoller call and `window.mzStartRoll = roller.start`; the inline roller is gone; commitRolledState is the commit callback; both call sites untouched; every pin that read the moved code is re-homed; bridge strings and docs describe the new shape.</done>
</task>

<task type="auto">
  <name>Task 2: AFTER repro table against the fixed shell, phase gates, SUMMARY</name>
  <files>.planning/phases/50-character-roller-fix/50-03-repro-after.txt, .planning/phases/50-character-roller-fix/50-03-SUMMARY.md</files>
  <read_first>
    - tools/roller-repro.mjs (Plan 02) and `.planning/phases/50-character-roller-fix/50-02-SUMMARY.md` (the BEFORE table — the AFTER table is compared against it row by row).
    - .planning/phases/50-character-roller-fix/50-CONTEXT.md ## Implementation Decisions › "Root cause & repro strategy" and "Behaviour edges & scope" (the exact SUMMARY wording and the three-row table shape).
  </read_first>
  <action>
    1. `npm run build:www` (exit 0) FIRST, then `npm test 2>&1 | grep -E "^# (tests|pass|fail)"` (fail 0; pass >= 3327 = baseline 3315 + the 11 Plan 01 tests + at least one mount-pin test), then `npm run boot:check` (four PASS lines — the swapped shell boots headless with no uncaught error).
    2. AFTER table: `node tools/roller-repro.mjs --scenario all 2>&1 | tee .planning/phases/50-character-roller-fix/50-03-repro-after.txt`; record the exit code. Expected: every row `yes`, exit 0. If exit 1 (a mismatch AFTER the fix): STOP, diagnose against roller.js/the mount (the tool's Notes column says whether the reels changed after the reveal and which state the Hero tab shows), fix, re-run — SC3 is not met until the table is all-`yes` or the failure is fully explained in the SUMMARY as a tool/driver artefact with evidence. If exit 2 (browser not drivable): the SUMMARY states verbatim "not reproduced in browser; structurally guarded; device check in the Phase 55 batch" with the tool's reason.
    3. Fence: `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git diff --stat <phase-start-commit>..HEAD -- engine/ content/ test/parity/` prints nothing (phase-start commit = the parent of Plan 01's first commit; record the hash); `wc -l mazeworld.html` before (5682 at planning) / after, for the record.
    4. Commit the evidence + SUMMARY: `docs(50-03): AFTER repro table + phase gates (ROLL-01)`.
    5. SUMMARY (`50-03-SUMMARY.md`) MUST carry: the mount diff summary (what was deleted / what replaced it, `wc -l` before/after); the pin re-home ledger (shell-abilities RACES/CLASSES pin → roller.js; the seven mount pins); the bridge.js consumer-string changes; `## SC3 repro pass` with BOTH tables — `### BEFORE (unfixed shell, from 50-02)` and `### AFTER (fixed shell)` — each with exit code, and the three-row reading (normal / double-tap / Play-again-from-death → reels shown, Hero tab shown, match?) plus the mid-reveal bonus row; the gate outputs (build:www, npm test totals, boot:check PASS lines); the fence evidence; and, as its LAST section, `## Human verification (deferred to end of run)` listing the Pixel 7 checks for the Phase 55 batch: (1) cold launch → ENTER → roller → note the three locked reels + name → DESCEND → Hero tab shows the same race / sub-class / class / name; (2) double-tap ENTER → the reels restart and lock once → DESCEND → Hero tab matches the final reels; (3) die → CONFIRM → ENTER (Play again) → DESCEND → Hero tab matches; (3b) dead Hero tab → "New Character" → DESCEND → Hero tab matches the reels.
  </action>
  <verify>
    <automated>npm run build:www && npm test 2>&1 | grep -E "^# (tests|pass|fail)" && npm run boot:check && test -f .planning/phases/50-character-roller-fix/50-03-repro-after.txt && grep -c "^| " .planning/phases/50-character-roller-fix/50-03-repro-after.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:www` exits 0
    - `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0` and `npm test 2>&1 | grep -E "^# pass"` prints a number >= 3327
    - `npm run boot:check 2>&1 | grep -c "PASS"` prints 4
    - `.planning/phases/50-character-roller-fix/50-03-repro-after.txt` exists; `grep -c "^| " .planning/phases/50-character-roller-fix/50-03-repro-after.txt` prints a number >= 5 and `grep -c "| NO |" .planning/phases/50-character-roller-fix/50-03-repro-after.txt` prints 0 — OR the file carries the tool's exit-2 reason and the SUMMARY contains `not reproduced in browser; structurally guarded; device check in the Phase 55 batch`
    - `.planning/phases/50-character-roller-fix/50-03-SUMMARY.md` contains the headings `## SC3 repro pass`, `### BEFORE`, `### AFTER`, and ends with a `## Human verification (deferred to end of run)` section whose text names `normal`, `double-tap` and `Play-again`
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `git diff --stat <phase-start-commit>..HEAD -- engine/ content/ test/parity/` prints nothing and `git status --porcelain` prints nothing after the final commit
  </acceptance_criteria>
  <done>The fixed shell builds, boots headless, passes the full suite, and the AFTER browser table shows zero mismatches (or the explicit not-reproduced fallback); the phase's engine/content/parity fence is proven clean; the SUMMARY records everything and the deferred Pixel 7 checks.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Created by | What it is |
| --- | --- | --- |
| `src/browser/roller.js#createRoller(options)` | Plan 01 | the factory: `{ doc, startNewRun, sheetFor, onCommit, timers, random, words }` → `Object.freeze({ start, commit, pending, dispose })` |
| `src/browser/roller.js#ROLLER_IDS` / `ROLLER_TIMELINE` / `ROLLER_COPY` / `ROLLER_CSS` | Plan 01 | frozen id / timing / copy / class-name tables |
| `src/browser/roller.js#reelWordLists()` | Plan 01 | `{ race, cls, sub }` cosmetic word lists from content/index.js |
| `test/unit/roller.test.js` | Plan 01 (behaviour + module pins), Plan 03 (mount pins m1–m7 appended) | the ROLL-01 suite |
| `tools/roller-repro.mjs` | Plan 02 | dependency-free headless-Chrome (CDP over Node's WebSocket) three-scenario roller/Hero-tab repro driver |
| `mazeworld.html` roller mount: `import { createRoller } from "./src/browser/roller.js";`, `const roller = createRoller({...})`, `window.mzStartRoll = roller.start;` | Plan 03 (this plan) | replaces the inline roller block; `commitRolledState` stays as the `onCommit` callback; the `RACES, CLASSES` shell import is deleted |
| `test/unit/shell-abilities.test.js` `ROLLER_SRC` pin | Plan 03 | the RACES/CLASSES import pin re-pointed to roller.js |
| `src/browser/bridge.js` (2 consumer strings) + `docs/SHELL-MODULES.md` (`### Screens` contract + regenerated bridge table) | Plan 03 | documentation |
| `.planning/phases/50-character-roller-fix/50-02-repro-before.txt`, `50-03-repro-after.txt` | Plans 02 / 03 | the SC3 evidence tables |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roll trigger → window.mzStartRoll (= roller.start) | user taps re-enter the roll at any time |
| roller → commitRolledState | the only state object the roller may hand the live app is the one the reels locked on |
| shell module script → src/browser/roller.js | the mount injects `document`, `startNewRun`, `characterSheetViewModel`, `onCommit`; the module never reaches for globals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Tampering | stale roll resolution reaching the reels/pending state | high | mitigate | roller.js token guard (Plan 01) now wired as the only roll path — mount pins m3/m4/m6 prove no inline copy survives |
| T-50-02 | Tampering | committed state differing from the reels | high | mitigate | onCommit receives the pending object; commitRolledState unchanged; AFTER browser table all-`yes` |
| T-50-09 | Tampering | a second startNewRun() sneaking onto the roller path in the shell | medium | mitigate | mount pin m4: exactly one startNewRun( call expression in the module script, inside mzDevStartAtDepth |
| T-50-10 | Denial of Service | mount regression breaking boot | medium | mitigate | `npm run boot:check` four PASS on the swapped www/index.html; the repro driver fails a scenario on any uncaught page exception |
| T-50-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- Mount pins m1–m7 + the re-pointed shell-abilities pin + shell-no-content-copies + bridge-registry + heroTab/clarity-43/perfMarks pins all green.
- `npm run build:www` exit 0 → `npm test` fail 0 (pass >= 3327) → `npm run boot:check` four PASS.
- `tools/roller-repro.mjs --scenario all` AFTER table: zero `NO` rows (or the explicit fallback sentence).
- Fence: master hash unchanged; `git diff --stat <phase-start>..HEAD -- engine/ content/ test/parity/` empty.
</verification>

<success_criteria>
- SC1: pinned by roller.test.js test 1 + pin 10 (module) and mount pins m2/m7 (the CTA commits the pending object through commitRolledState).
- SC2: pinned by roller.test.js tests 2/3/4/5 and mount pin m4 (no second startNewRun call on the path).
- SC3: the AFTER table in the SUMMARY shows zero mismatches for normal / double-tap / Play-again-from-death (+ the mid-reveal bonus row), or the explicit not-reproduced fallback with the device check deferred to Phase 55.
- SC4: no path under engine/, content/ or test/parity/ in the phase diff; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
- `window.mzStartRoll` remains the bridge name at both call sites; docs/SHELL-MODULES.md documents the roller contract; the bridge table is regenerated.
</success_criteria>

<output>
Create `.planning/phases/50-character-roller-fix/50-03-SUMMARY.md` when done. It MUST carry: the mount diff summary and `wc -l mazeworld.html` before/after; the pin re-home ledger; the bridge.js consumer-string changes; `## SC3 repro pass` with `### BEFORE` (from 50-02) and `### AFTER` tables, exit codes, and the three-row reading; the gate outputs (build:www, npm test totals, boot:check PASS lines); the fence evidence (master hash + empty engine/content/parity diff from the phase-start commit); the commit hashes; and — as its LAST section — `## Human verification (deferred to end of run)` with the Pixel 7 roller/Hero-tab checks (normal / double-tap / Play-again-from-death, plus the dead Hero tab's "New Character" roll) for the Phase 55 device batch.
</output>
