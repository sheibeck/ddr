---
phase: 50-character-roller-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/browser/roller.js
  - test/unit/roller.test.js
autonomous: true
requirements: [ROLL-01]

must_haves:
  truths:
    - "ROLL-01 / SC1: the reel lock and the CTA commit read the same object — every lock step and the reveal derive their label from sheetFor(rollerPendingState) inside the timer callback, never from a sheet captured at await time; the committed state's characterSheetViewModel labels (race/class/sub-class/name/quirk) equal the reel labels shown at reveal (pinned by test/unit/roller.test.js)"
    - "ROLL-01 / SC2, CONTEXT 'Re-entry guard: a monotonic roll token captured before await startNewRun(); a resolution carrying a stale token is dropped (touches neither the reels nor rollerPendingState)': two rolls where the first roll's startNewRun resolves only after the second roll was requested leave zero timers and a null pending state behind for the first, and only the second's labels reach the reels and onCommit"
    - "CONTEXT 'The startNewRun() calls are serialized through a promise chain so the LAST roll is also the adapter's currentState': createRoller invokes the injected startNewRun at most once at a time — a second start() while the first is unresolved does not call startNewRun until the first resolves"
    - "CONTEXT 'rollerPendingState is assigned right after the token-validated await ... the CTA stays disabled until the full reveal and commits that same object': pending() is the rolled state immediately after the await; commit() is a no-op (onCommit not called) until the reveal timer fires; after reveal commit() hands onCommit the identical object (===) that pending() held"
    - "CONTEXT 'A double-tap on a roll trigger SUPERSEDES: the reels restart and lock on the second roll; only the second state can ever reach the Hero tab': a second start() mid-reveal clears the first roll's spin interval and lock timers, unlocks the reels, and the reveal then shows the second roll"
    - "CONTEXT 'extracted to src/browser/roller.js in the Phase 47 module pattern ... createRoller({ doc, startNewRun, sheetFor, timers, onCommit, ... }) factory with injected deferred promises + injectable timers': roller.js reads no window/document global (comment-stripped scan), imports only content/index.js, and the whole race is exercised under fake timers + deferred promises in node --test"
    - "SC4 / CONTEXT 'engine/, content/, every parity fixture and the master hash are untouched': git diff --stat for this plan shows no path under engine/, content/ or test/parity/; test/parity/prototype-master.js.txt hashes a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - "src/browser/roller.js — createRoller(options) -> { start, commit, pending, dispose }; ROLLER_IDS, ROLLER_TIMELINE, ROLLER_COPY, ROLLER_CSS, reelWordLists()"
    - "test/unit/roller.test.js — the race/identity/serialization/CTA-gating behaviour suite + the module source pins (exports, no globals, id containment, timeline constants, one startNewRun() call, sheetFor(rollerPendingState) reads, voice safety)"
  key_links:
    - "createRoller.start() -> chain.then(() => startNewRun()) -> token check -> rollerPendingState = rolledState -> lock/reveal timers read sheetFor(rollerPendingState) -> commit() -> onCommit(rollerPendingState)"
    - "roller.js -> content/index.js#RACES/CLASSES (reel-flicker word lists only — cosmetic, never an rng draw)"
    - "test/unit/roller.test.js -> engine/state.js#newRun(seed) (real rolled states: seeds 1/2/3) + src/browser/heroTab.js#characterSheetViewModel (the real sheetFor) + test/unit/harness/recordingDom.js (the fake doc)"
---

<objective>
Extract the character-roll screen's logic out of `mazeworld.html` into `src/browser/roller.js` — a `createRoller({ doc, startNewRun, sheetFor, onCommit, timers, random, words })` factory in the Phase 47 module pattern — and close the two structural weaknesses the scout found: (1) un-guarded re-entry of the roll (overlapping rolls each pushed their own lock/reveal timers, and a first roll's `await startNewRun()` could resolve after a second roll had reset the screen), fixed by a monotonic roll token plus a serialized `startNewRun()` promise chain; (2) the reels locking on a captured sheet while the CTA committed the pending state, fixed by having every lock step and the reveal read `sheetFor(rollerPendingState)` and the CTA commit that same object. The race is pinned by `test/unit/roller.test.js` under injected deferred promises and fake timers. The shell is NOT touched in this plan — Plan 03 swaps the mount.

Purpose: ROLL-01 — the character the reels reveal is exactly the character that lands on the Hero tab; no second roll on the path, no stale pending state from a superseded roll, no label drift.

Output: `src/browser/roller.js`, `test/unit/roller.test.js`; `npm test` fail 0 (baseline 3315 pass + the new suite); `npm run build:www` green; engine/content/parity fence untouched.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-character-roller-fix/50-CONTEXT.md
@.planning/todos/pending/2026-09-20-roller-reels-do-not-match-the-hero-tab-character.md
@docs/SHELL-MODULES.md
@src/browser/storeScreen.js
@test/unit/heroTab.test.js
@test/unit/harness/recordingDom.js
</context>

<!-- planner-discipline-allow: window. -->
<!-- planner-discipline-allow: document. -->
<!-- planner-discipline-allow: rngState -->
<!-- planner-discipline-allow: ROLLER_LOCK_DELAYS -->
<!-- planner-discipline-allow: ROLLER_REVEAL_DELAY -->
<!-- planner-discipline-allow: ROLLER_SPIN_MS -->
<!-- planner-discipline-allow: clearRollerTimers -->
<!-- planner-discipline-allow: pickDisplay -->
<!-- planner-discipline-allow: setReel -->
<!-- planner-discipline-allow: initRollerScreen -->
<!-- planner-discipline-allow: commitRolledState -->

<assumption_delta_decision>
decision: no-change
signal: pluralization on "second" in the phase goal ("no second roll")
rationale: the trigger word is a negation ("no second roll"); the identity model (one rolled GameState per run, committed once) is unchanged.
</assumption_delta_decision>

<phase_ground_rules>
- **Shell-only phase, engine fence (every commit):** zero bytes change under `engine/`, `content/`, `test/parity/**`; `test/parity/prototype-master.js.txt` keeps hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`. ROLL-01 changes no rule — no bot readout, no fixture regeneration.
- **This plan does not edit `mazeworld.html`.** The inline roller block (`ROLLER_LOCK_DELAYS` / `ROLLER_REVEAL_DELAY` / `ROLLER_SPIN_MS` / `clearRollerTimers` / `pickDisplay` / `setReel` / `initRollerScreen` / `commitRolledState`) stays live until Plan 03. Consequence: `test/unit/shell-no-content-copies.test.js` criterion 3 fails the moment ANY `src/browser/*.js` export name matches a `const|let|var|function` declared inline in the shell — so roller.js's export names MUST be new names (`ROLLER_IDS`, `ROLLER_TIMELINE`, `ROLLER_COPY`, `ROLLER_CSS`, `reelWordLists`, `createRoller`), never the inline ones.
- **Phase 47 module rule (docs/SHELL-MODULES.md):** a module reaches the page only through the injected `doc` and its deps — never `window`/`document` globals; it imports `content/` directly; `test/unit/bridge-registry.test.js` scans every `src/browser/*.js` for `__mz` names, so roller.js must not reference any `window.__mz*` bridge (the mount's `onCommit` callback does the tab switch, Plan 03).
- **Presentation never touches the engine rng:** the reel flicker uses the injected `random` (default `Math.random`), never the engine's seeded rng; roller.js never reads the state's rng cursor field.
- **Locked timeline:** the design mock's startRoll numbers — race lock 900 ms, class lock 1650 ms, sub-class lock 2400 ms, full reveal 3050 ms, flicker every 70 ms — are unchanged (the same values the inline block carries).
- **Greenfield ruling:** no dual paths, no compatibility shims. The persisted-save-at-roll-time behaviour (`startNewRun()` → `persist()` before DESCEND), `recordBest` on a superseded roll, and the back button's roller-unawareness are DEFERRED (CONTEXT ## Deferred Ideas) — do not touch them.
- **Windows executor environment:** Git Bash, no `jq`; multi-line scripts go in files. Line numbers drift — anchor by names.
- **Verification agents are off:** the acceptance criteria ARE the verification; every result goes into the SUMMARY.
</phase_ground_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: create src/browser/roller.js — createRoller with the roll token, the serialized startNewRun chain, and pending-state-only reel reads</name>
  <files>src/browser/roller.js</files>
  <read_first>
    - mazeworld.html: the inline roller block — from the comment line starting `// Presentation-only word lists for the reel flicker` (the `ROLLER_*` word lists and timing constants, `clearRollerTimers`, `pickDisplay`, `setReel`) through `window.mzStartRoll = async function startRoll() {` and the `(function initRollerScreen() {` IIFE — this is the behaviour being moved (screen reset sequence, the reel/CTA/reveal DOM writes, the class names `mw-roller-locked` / `mw-roller-revealed` / `mw-roller-ready`, the CTA copy). Also `function commitRolledState(state) {` just above it (stays in the shell; Plan 03 passes it as `onCommit`).
    - mazeworld.html markup: the `<div id="mw-roller-screen" class="mw-roller-screen" hidden>` block (the eight ids the module writes: mw-roller-screen, mw-roller-cta, mw-roller-reveal, mw-roller-name, mw-roller-quirk, mw-roller-race, mw-roller-class, mw-roller-sub).
    - src/browser/storeScreen.js and src/browser/gearTab.js head comments (the Phase 47 module head-comment style and the "no window/document globals" rule).
    - src/browser/heroTab.js `export function characterSheetViewModel(state)` — the sheet shape the module reads through `sheetFor`: `raceLabel`, `classLabel`, `subLabel`, `name`, `quirk.text`.
    - src/browser/engineAdapter.js `export async function startNewRun(seed, options = {})` — the injected roll function (called with no arguments; sets the adapter's currentState and persists before returning).
    - .planning/phases/50-character-roller-fix/50-CONTEXT.md ## Implementation Decisions › "Fix shape" (the locked shape of the guard, the chain, and the pending-state reads).
  </read_first>
  <behavior>
    - Test 1 (identity): after start() resolves with rolled state A and the fake clock advances 3050 ms, reels/name/quirk show sheetFor(A) labels, commit() calls onCommit exactly once with the identical A object, and sheetFor(committed) labels equal the reels' textContent.
    - Test 2 (superseded resolution): start() twice while the first startNewRun is unresolved; the injected startNewRun has been called once; resolving the first leaves pending() null and zero live timers; the second call then happens, resolves with B, and only B's labels reach the reels and onCommit.
    - Test 3 (mid-reveal supersede): start(), resolve A, advance 1000 ms (race reel locked on A) → start() again clears every timer, unlocks the reels, nulls pending(); resolve B → reveal shows B; commit() hands onCommit B.
    - Test 4 (CTA gating): before the reveal timer, commit() returns false and onCommit is not called; after it, commit() returns true, the screen is hidden, pending() is null, and a second commit() is a no-op.
  </behavior>
  <action>
    Create `src/browser/roller.js` (ES module, Phase 47 head-comment style: "Phase 50 (ROLL-01) — the character-roll screen ('THE TABLES DECIDE'): one real engine roll per start(), a cosmetic reel flicker that locks in sequence on the REAL rolled race/class/sub-class/name/quirk, and a CTA that commits exactly the state the reels showed. Contract: createRoller(options) — see docs/SHELL-MODULES.md. No window/document globals: the injected doc + deps only.").

    Imports: exactly one line — `import { RACES, CLASSES } from "../../content/index.js";` (the read-only content tables the reel-flicker word lists are built from; cosmetic strings only, never fed into anything rng-touching). No engine/ import, no heroTab.js import — the sheet function is injected as `sheetFor` so the module stays a pure presentation seam.

    Exports (these names are NEW on purpose — see phase_ground_rules on shell-no-content-copies criterion 3; do NOT export under the inline block's names):
    - `ROLLER_IDS` — `Object.freeze({ screen: "mw-roller-screen", cta: "mw-roller-cta", reveal: "mw-roller-reveal", name: "mw-roller-name", quirk: "mw-roller-quirk", race: "mw-roller-race", cls: "mw-roller-class", sub: "mw-roller-sub" })`.
    - `ROLLER_TIMELINE` — `Object.freeze({ lock: Object.freeze({ race: 900, cls: 1650, sub: 2400 }), reveal: 3050, spin: 70 })` (the design mock's startRoll timeline, unchanged from the inline constants).
    - `ROLLER_COPY` — `Object.freeze({ falling: "THE DICE ARE STILL FALLING", ready: "DESCEND" })`.
    - `ROLLER_CSS` — `Object.freeze({ locked: "mw-roller-locked", revealed: "mw-roller-revealed", ready: "mw-roller-ready" })`.
    - `reelWordLists()` — returns `{ race: Object.keys(RACES), cls: Object.keys(CLASSES), sub: Object.values(CLASSES).flatMap((cls) => cls.subs) }`.
    - `createRoller(options)` — the factory below.

    Module-private helpers: `noop`, `pickDisplay(list, random)` (returns `list[Math.floor(random() * list.length)]`), `setReel(doc, id, value, locked)` (`el = doc.getElementById(id)`; return if null; `el.textContent = value`; `el.classList.toggle(ROLLER_CSS.locked, !!locked)`), `defaultTimers()` (an object whose four keys `setTimeout` / `clearTimeout` / `setInterval` / `clearInterval` wrap the bare global functions — write them as `(fn, ms) => setTimeout(fn, ms)` style wrappers, never through a `window` reference).

    `createRoller({ doc, startNewRun, sheetFor, onCommit, timers = defaultTimers(), random = Math.random, words = reelWordLists() })`:
    - Private state: `let rollSeq = 0` (the monotonic roll token counter), `let chain = Promise.resolve()` (the serialized startNewRun chain), `let rollerPendingState = null`, `let revealed = false`, `let spinTimer = null`, `let lockTimers = []`.
    - `clearTimers()` — clearInterval(spinTimer) if set and null it; clearTimeout each of lockTimers; reset lockTimers to [].
    - On creation: `const cta = doc.getElementById(ROLLER_IDS.cta)`; if cta exists, `cta.onclick = () => { commit(); }` (the roller's own primary CTA, wired once — the inline initRollerScreen IIFE's job).
    - `async start()`:
      1. `const token = ++rollSeq;` then `clearTimers(); rollerPendingState = null; revealed = false;`.
      2. `const screen = doc.getElementById(ROLLER_IDS.screen); if (!screen) return false;`.
      3. Screen reset, same writes as the inline block: reveal element `classList.remove(ROLLER_CSS.revealed)`; name and quirk `textContent = ""`; CTA `disabled = true`, `classList.remove(ROLLER_CSS.ready)`, `textContent = ROLLER_COPY.falling`; the three reels `setReel(doc, ROLLER_IDS.race, pickDisplay(words.race, random), false)` (likewise cls / sub); `screen.hidden = false`.
      4. The one real roll, serialized: `const run = chain.then(() => startNewRun()); chain = run.then(noop, noop); const rolledState = await run;` — a second start() while this one is unresolved chains behind it, so the adapter's currentState (the persisted save) after the LAST resolution is the LAST roll's state, and startNewRun is never in flight twice.
      5. Token check FIRST: `if (token !== rollSeq) return false;` — a superseded roll's resolution is dropped here and touches neither the reels nor the pending state (no timer is pushed by it).
      6. `rollerPendingState = rolledState;` — assigned right after the token-validated await (the ONLY assignment of a rolled state to it in the module).
      7. `const locked = { race: false, cls: false, sub: false };` `spinTimer = timers.setInterval(flicker, ROLLER_TIMELINE.spin)` where flicker re-picks each still-unlocked reel from `words` via `random`.
      8. Three lock timers via `timers.setTimeout` at `ROLLER_TIMELINE.lock.race/cls/sub`, pushed onto lockTimers; EACH callback starts with `if (token !== rollSeq) return;`, sets `locked.<reel> = true`, then `setReel(doc, ROLLER_IDS.<reel>, sheetFor(rollerPendingState).<raceLabel|classLabel|subLabel>, true)` — the label is derived from the pending state INSIDE the callback (SC1: the reel lock and the CTA commit read the same object; there is no sheet captured at await time and re-used later).
      9. The reveal timer at `ROLLER_TIMELINE.reveal`, also on lockTimers: `if (token !== rollSeq) return;` then clearInterval(spinTimer) and null it; `const sheet = sheetFor(rollerPendingState);` name `textContent = sheet.name`, quirk `textContent = sheet.quirk.text`; reveal element `classList.add(ROLLER_CSS.revealed)`; `revealed = true`; CTA `disabled = false`, `classList.add(ROLLER_CSS.ready)`, `textContent = ROLLER_COPY.ready`.
      10. `return true;`.
    - `commit()`: `if (!revealed || !rollerPendingState) return false;` — the CTA stays inert until the full reveal; then `const state = rollerPendingState; rollerPendingState = null; revealed = false; clearTimers(); onCommit(state);` then `doc.getElementById(ROLLER_IDS.screen).hidden = true` (guard null); `return true`. onCommit receives the very object the reels locked on.
    - `pending()`: returns rollerPendingState (a read-only accessor for tests and the mount).
    - `dispose()`: `rollSeq += 1; clearTimers(); rollerPendingState = null; revealed = false;` (any in-flight resolution is dropped).
    - Return `Object.freeze({ start, commit, pending, dispose })`.

    Voice: the two CTA strings are the inline block's own (already on the markup's button and covered by the family-friendly scan); add nothing player-facing beyond them.

    Sanity before moving on: `node -e "import('./src/browser/roller.js').then(m => console.log(Object.keys(m).sort().join(',')))"` prints `ROLLER_COPY,ROLLER_CSS,ROLLER_IDS,ROLLER_TIMELINE,createRoller,reelWordLists`, and `node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js` stays fail 0 with the inline block still in the shell.
  </action>
  <verify>
    <automated>node -e "import('./src/browser/roller.js').then(m => console.log(Object.keys(m).sort().join(',')))" && node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/browser/roller.js` exits 0
    - `node -e "import('./src/browser/roller.js').then(m => console.log(Object.keys(m).sort().join(',')))"` prints exactly `ROLLER_COPY,ROLLER_CSS,ROLLER_IDS,ROLLER_TIMELINE,createRoller,reelWordLists`
    - `grep -c "^export function createRoller(" src/browser/roller.js` prints 1
    - `grep -c "^import " src/browser/roller.js` prints 1 and `grep -c 'from "../../content/index.js"' src/browser/roller.js` prints 1
    - `node -e "import('./tools/ident-sweep.mjs').then(({stripJs}) => { const s = stripJs(require('fs').readFileSync('src/browser/roller.js','utf8')); console.log(/\bwindow\./.test(s), /\bdocument\./.test(s), /rngState/.test(s), /__mz/.test(s)); })"` prints `false false false false` (comment-stripped: no window/document global, no rng-cursor read, no bridge name)
    - `grep -c "sheetFor(rollerPendingState)" src/browser/roller.js` prints a number >= 4 (three lock steps + the reveal)
    - `grep -c "rollerPendingState = rolledState;" src/browser/roller.js` prints 1
    - `grep -c "if (token !== rollSeq) return" src/browser/roller.js` prints a number >= 5 (the post-await drop + three lock callbacks + the reveal callback)
    - `grep -c "startNewRun()" src/browser/roller.js` prints 1 (the single call expression, inside the chain)
    - `grep -c "Math.random" src/browser/roller.js` prints 1 (the `random` option default only)
    - `node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` (new export names collide with nothing the shell still declares; no `__mz` name in the module)
    - `git status --porcelain engine/ content/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>src/browser/roller.js exists with createRoller + the five named exports; every reel/reveal write reads sheetFor(rollerPendingState) inside its timer callback; a stale token drops a resolution before it touches anything; startNewRun is chained; the module reads no window/document global and imports only content/index.js.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: write test/unit/roller.test.js (race + identity + serialization + CTA gating + module pins) and run the phase gates</name>
  <files>test/unit/roller.test.js</files>
  <read_first>
    - src/browser/roller.js (Task 1 — the exact option names, return shape, and the export names to pin).
    - test/unit/heroTab.test.js — the source-pin conventions to mirror: `stripJs` from tools/ident-sweep.mjs on the module source; the `/\bwindow\./` and `/\bdocument\./` no-globals scan; the id-containment pin that slices the markup section out of mazeworld.html; the BANNED voice scan (section 11).
    - test/unit/harness/recordingDom.js — `createRecordingDocument()` returns `{ document, elementsById, serializeElements }`; `getElementById` auto-creates + memoises; elements carry `textContent`, `hidden`, `disabled`, `onclick`, and a `classList` with `add/remove/toggle(name, force)/contains`.
    - engine/state.js `export function newRun(seed, exclude = [], { startDepth = 1, force = null, storeRoll = false } = {})` — real rolled states for the tests (pure, deterministic per seed; import it from `../../engine/state.js`).
    - src/browser/heroTab.js `characterSheetViewModel(state)` — the real `sheetFor` for the tests.
    - mazeworld.html: the `<div id="mw-roller-screen"` markup block (for the id-containment pin) — it ends at the `<!-- ============ MARKS LEGEND BOTTOM SHEET` comment that follows it.
  </read_first>
  <action>
    Create `test/unit/roller.test.js` (node:test + node:assert/strict; head comment in the heroTab.test.js style naming Phase 50 / ROLL-01 and the two closed weaknesses).

    Test-local helpers (no new harness file):
    - `deferred()` → `{ promise, resolve, reject }`.
    - `flush()` → `await new Promise((r) => setImmediate(r))` (drains every microtask hop of the chain after a deferred resolves).
    - `makeFakeTimers()` → an object with `setTimeout(fn, ms)` / `clearTimeout(id)` / `setInterval(fn, ms)` / `clearInterval(id)` over an internal `Map` of `{ at, fn, every }` keyed by a numeric id, plus `now()`, `pending()` (live timer count) and `async advance(ms)` which fires due timers in `at` order (re-arming intervals by `every`, deleting one-shots), awaiting `Promise.resolve()` between fires, and finally sets the clock to the target.
    - `makeRig({ states })` → builds `createRecordingDocument()`, a `calls = []` array, `startNewRun = () => { const d = deferred(); calls.push(d); return d.promise; }`, `committed = []`, `onCommit = (s) => committed.push(s)`, `timers = makeFakeTimers()`, `random = () => 0`, and `roller = createRoller({ doc: document, startNewRun, sheetFor: characterSheetViewModel, onCommit, timers, random })`; returns everything plus `el(id) = document.getElementById(id)` and `reelText()` = `{ race, cls, sub, name, quirk }` textContents of the five reveal ids.
    - Fixture states: `const A = newRun(1); const B = newRun(2); const C = newRun(3);` and `const sheet = characterSheetViewModel;` — at planning time seed 1 = Fridgian / Fighter / Knight / "Ketill the Silent", seed 2 = Wilmsry / Thief / Cat Burglar / "Corl the Patient", seed 3 = Human / Thief / Pickpocket / "Ivy Marsh"; assert at the top of the file that `sheet(A)` and `sheet(B)` differ in raceLabel, classLabel, subLabel AND name (a precondition guard so the race tests can never pass by coincidence).

    Behaviour tests (each builds its own rig):
    1. "SC1 identity: the reels lock on, and the CTA commits, the ONE pending state" — `p = roller.start()`; `await flush()`; `calls.length === 1`; `calls[0].resolve(A)`; `await flush()`; `await p === true`; `roller.pending() === A` (assigned right after the await); CTA `disabled === true`, `textContent === ROLLER_COPY.falling`; `roller.commit() === false` and `committed.length === 0` (CTA inert before the reveal); `await timers.advance(900)` → race reel `textContent === sheet(A).raceLabel` and `classList.contains(ROLLER_CSS.locked) === true`, cls/sub reels NOT locked; `advance(750)` → cls locked on `sheet(A).classLabel`; `advance(750)` → sub locked on `sheet(A).subLabel`; `advance(650)` (t = 3050) → name `=== sheet(A).name`, quirk `=== sheet(A).quirk.text`, reveal el `classList.contains(ROLLER_CSS.revealed)`, CTA `disabled === false` + `textContent === ROLLER_COPY.ready` + `classList.contains(ROLLER_CSS.ready)`, `timers.pending() === 0` (spin interval cleared); `roller.commit() === true`; `committed.length === 1 && committed[0] === A` (identity, not deep-equal); `sheet(committed[0]).raceLabel/classLabel/subLabel/name` equal the reels'/name's textContent and `sheet(committed[0]).quirk.text` equals the quirk textContent; screen `hidden === true`; `roller.pending() === null`; a second `roller.commit()` returns false and `committed.length` stays 1.
    2. "SC2 superseded resolution: a first roll resolving after a second roll was requested is dropped" — `p1 = roller.start(); p2 = roller.start();` `await flush()`; `calls.length === 1` (serialized — the second startNewRun call has NOT been made); `calls[0].resolve(A)`; `await flush()`; `await p1 === false`; `roller.pending() === null`; `timers.pending() === 0` (the dropped roll pushed no timer); none of the three reels has the locked class; `calls.length === 2` (the chain released the second call only after the first resolved); `calls[1].resolve(B)`; `await flush()`; `await p2 === true`; `roller.pending() === B`; `await timers.advance(3050)`; reels locked on `sheet(B)` labels, name `=== sheet(B).name`; `roller.commit() === true`; `committed` deep-equals `[B]` and `committed[0] === B`; and `sheet(A).name` never appears in the name element (assert `el("mw-roller-name").textContent !== sheet(A).name`).
    3. "SC2 double-tap mid-reveal supersedes: the reels restart and lock on the second roll" — `p1 = roller.start()`; `await flush()`; `calls[0].resolve(A)`; `await flush()`; `await timers.advance(1000)` → race reel locked on `sheet(A).raceLabel`; `p2 = roller.start()` → synchronously: `roller.pending() === null`, `timers.pending() === 0` (the first roll's interval + remaining lock/reveal timers are gone), race reel `classList.contains(ROLLER_CSS.locked) === false`, CTA `disabled === true`; `await flush()`; `calls.length === 2`; `calls[1].resolve(B)`; `await flush()`; `await timers.advance(3050)` → reels locked on `sheet(B)` labels, name `=== sheet(B).name`; `roller.commit()`; `committed[0] === B`; then `await timers.advance(5000)` and re-assert the reels still show `sheet(B)` (no stale timer from the first roll fired later).
    4. "serialization: three rapid starts call startNewRun one at a time, and the last roll is the one that lands" — start() three times; resolve calls in order (A, B, C) with `await flush()` between; assert `calls.length` grows 1 → 2 → 3 only as each resolves; final `roller.pending() === C`; advance 3050; commit hands `C`.
    5. "dispose(): an in-flight resolution after dispose is dropped" — start(); `roller.dispose()`; resolve A; flush; `pending() === null`, `timers.pending() === 0`.
    6. "reel flicker uses only the injected random over reelWordLists()" — a counting `random` spy; after `calls[0].resolve(A)` + `advance(70 * 3)` the spy call count is > 0 and every reel textContent before its lock is a member of the matching `reelWordLists()` list.

    Module source pins (comment-stripped via `stripJs`, mirroring heroTab.test.js (1)–(3)):
    7. exports: `createRoller`, `reelWordLists` are functions; `ROLLER_IDS`, `ROLLER_TIMELINE`, `ROLLER_COPY`, `ROLLER_CSS` are frozen objects (`Object.isFrozen`); `ROLLER_TIMELINE` deep-equals `{ lock: { race: 900, cls: 1650, sub: 2400 }, reveal: 3050, spin: 70 }`; `ROLLER_COPY` deep-equals `{ falling: "THE DICE ARE STILL FALLING", ready: "DESCEND" }`.
    8. no globals: the stripped source matches neither `/\bwindow\./` nor `/\bdocument\./`, contains no `rngState`, contains no `__mz`, and has exactly one `import` statement, from `../../content/index.js`.
    9. id containment: every value of `ROLLER_IDS` appears as `id="<value>"` inside the mazeworld.html slice from `<div id="mw-roller-screen"` to `<!-- ============ MARKS LEGEND BOTTOM SHEET`.
    10. the one-roll/one-object shape: the stripped source contains exactly one `startNewRun()` call expression; `sheetFor(rollerPendingState)` occurs at least 4 times; `rollerPendingState = rolledState;` occurs exactly once and its index is greater than the index of the first `token !== rollSeq` check; `onCommit(state)` occurs exactly once.
    11. voice: every string in `ROLLER_COPY` clears `BANNED` from content/safety-wordlist.js (the heroTab.test.js (11) loop).

    Gates (this is the plan's final task — record every output in the SUMMARY): `node --test test/unit/roller.test.js` fail 0; `npm test 2>&1 | grep -E "^# (tests|pass|fail)"` — fail 0 and pass >= 3315 + 11; `npm run build:www` exits 0; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain engine/ content/ test/parity/` prints nothing. Commit: `feat(50-01): extract the character roller to src/browser/roller.js with a roll token, a serialized startNewRun chain and pending-state-only reel reads (ROLL-01)`.

    SUMMARY must end with `## Human verification (deferred to end of run)` listing the Pixel 7 checks the phase owes (owned by Plan 03's SUMMARY, repeated here verbatim): (1) title ENTER → roller → note the three locked reels + name → DESCEND → Hero tab shows the same race / sub-class / class / name; (2) double-tap ENTER → the reels restart and lock once → DESCEND → Hero tab matches the final reels; (3) die → CONFIRM → ENTER (Play again) → DESCEND → Hero tab matches; also the dead Hero tab's "New Character" button → same check.
  </action>
  <verify>
    <automated>node --test test/unit/roller.test.js 2>&1 | grep -E "^# (pass|fail)" && npm test 2>&1 | grep -E "^# (tests|pass|fail)" && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/unit/roller.test.js` exits 0
    - `node --test test/unit/roller.test.js 2>&1 | grep -E "^# fail"` prints `# fail 0` and `node --test test/unit/roller.test.js 2>&1 | grep -E "^# pass"` prints a count >= 11
    - `grep -c "createRecordingDocument" test/unit/roller.test.js` prints a number >= 1; `grep -c 'from "../../engine/state.js"' test/unit/roller.test.js` prints 1; `grep -c "characterSheetViewModel" test/unit/roller.test.js` prints a number >= 2
    - `grep -c "timers.pending() *=== *0\|timers.pending(), 0" test/unit/roller.test.js` prints a number >= 2 (the dropped-resolution and mid-reveal-supersede timer assertions)
    - `grep -c "committed\[0\], B)\|committed\[0\] === B" test/unit/roller.test.js` prints a number >= 1 and the same grep for `A` (`committed\[0\], A)\|committed\[0\] === A`) prints a number >= 1 (identity assertions in tests 1 and 2/3)
    - `npm test 2>&1 | grep -E "^# fail"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass"` prints a number >= 3326
    - `npm run build:www` exits 0
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `git diff --stat HEAD~1 -- engine/ content/ test/parity/` prints nothing (after the commit) and `git status --porcelain engine/ content/ test/parity/` prints nothing
    - `git diff --stat HEAD~1 -- mazeworld.html` prints nothing (this plan does not touch the shell)
    - `.planning/phases/50-character-roller-fix/50-01-SUMMARY.md` exists, contains the `npm test` totals line, the build:www result, the master-hash line, and a `## Human verification (deferred to end of run)` section
  </acceptance_criteria>
  <done>test/unit/roller.test.js pins the identity contract (SC1), both supersede races (SC2), the serialized chain, CTA gating, dispose, the injected-random flicker, and the module source shape; the full suite is fail 0 with the shell untouched; build:www green; fence clean; SUMMARY written with the deferred human-verification section.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Created by | What it is |
| --- | --- | --- |
| `src/browser/roller.js#createRoller(options)` | Plan 01 | the factory: `{ doc, startNewRun, sheetFor, onCommit, timers, random, words }` → `Object.freeze({ start, commit, pending, dispose })` |
| `src/browser/roller.js#ROLLER_IDS` / `ROLLER_TIMELINE` / `ROLLER_COPY` / `ROLLER_CSS` | Plan 01 | frozen id / timing / copy / class-name tables |
| `src/browser/roller.js#reelWordLists()` | Plan 01 | `{ race, cls, sub }` cosmetic word lists from content/index.js |
| `test/unit/roller.test.js` | Plan 01 (behaviour + module pins), Plan 03 (mount pins appended) | the ROLL-01 suite |
| `tools/roller-repro.mjs` | Plan 02 | dependency-free headless-Chrome (CDP over Node's WebSocket) three-scenario roller/Hero-tab repro driver |
| `mazeworld.html` roller mount: `const roller = createRoller({...})`, `window.mzStartRoll = roller.start` | Plan 03 | replaces the inline roller block; `commitRolledState` stays as the `onCommit` callback |
| `docs/SHELL-MODULES.md` roller contract row; `src/browser/bridge.js` consumer strings (2) regenerated into the bridge table | Plan 03 | documentation |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| roll trigger → createRoller.start() | user taps (title ENTER, dead Hero tab "New Character") re-enter the roll at any time, including mid-reveal |
| adapter → roller | `startNewRun()` resolutions arrive asynchronously, possibly after the roll that requested them was superseded |
| roller → onCommit | the only object the roller may hand to the live app is the one the reels locked on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-50-01 | Tampering | roller.start() stale resolution | high | mitigate | monotonic `rollSeq` token captured before the await; a stale token returns before touching reels or `rollerPendingState`; pinned by roller.test.js tests 2/3/5 |
| T-50-02 | Tampering | reel labels vs committed state | high | mitigate | every lock/reveal callback reads `sheetFor(rollerPendingState)`; `commit()` hands `onCommit` that same object; pinned by test 1 (identity `===`) and pin 10 |
| T-50-03 | Denial of Service | overlapping timer sets from re-entry | medium | mitigate | `clearTimers()` at every start(); token check inside every timer callback; `timers.pending() === 0` assertions after a supersede |
| T-50-04 | Information Disclosure | presentation reading the engine rng | medium | mitigate | flicker uses the injected `random` only; pin 8 asserts no rng-cursor read and a single content/ import |
| T-50-05 | Elevation of Privilege | commit before the reveal (CTA tap while inert) | low | mitigate | `commit()` requires `revealed && rollerPendingState`; pinned by test 4 |
| T-50-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (node:test + existing harness only) |
</threat_model>

<verification>
- `node --test test/unit/roller.test.js` fail 0 (>= 11 tests: 6 behaviour + 5 pins).
- `npm test` fail 0, pass >= 3326; `npm run build:www` exits 0.
- `node --test test/unit/shell-no-content-copies.test.js test/unit/bridge-registry.test.js` fail 0 with the inline shell block still present (export names collide with nothing).
- Fence: `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; no diff under engine/, content/, test/parity/; no diff to mazeworld.html.
</verification>

<success_criteria>
- `src/browser/roller.js` exports exactly `ROLLER_COPY, ROLLER_CSS, ROLLER_IDS, ROLLER_TIMELINE, createRoller, reelWordLists`; reads no window/document global; imports only content/index.js.
- SC1 pinned: committed state `===` the object the reels locked on; `sheetFor(committed)` labels equal the reel/name/quirk text.
- SC2 pinned: a superseded roll's resolution leaves zero timers and a null pending state; only the second roll's labels reach the reels and onCommit; a mid-reveal second start() restarts the reels.
- Serialization pinned: `startNewRun` is never in flight twice.
- Suite fail 0; build green; engine/content/parity untouched; SUMMARY carries the deferred human-verification section.
</success_criteria>

<output>
Create `.planning/phases/50-character-roller-fix/50-01-SUMMARY.md` when done. It MUST carry: the export list printed by the node one-liner, the `node --test test/unit/roller.test.js` pass/fail totals, the `npm test` totals (tests/pass/fail), the `npm run build:www` result, the master-hash line, the `git status --porcelain engine/ content/ test/parity/` (empty) evidence, the commit hash, and — as its LAST section — `## Human verification (deferred to end of run)` with the three Pixel 7 roller/Hero-tab checks (normal / double-tap / Play-again-from-death, plus the dead Hero tab's "New Character" button) for the Phase 55 device batch.
</output>
