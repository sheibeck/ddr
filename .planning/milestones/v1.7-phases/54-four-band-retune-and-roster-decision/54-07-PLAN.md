---
phase: 54-four-band-retune-and-roster-decision
plan: 07
type: execute
wave: 7
depends_on: ["54-06"]
files_modified:
  - engine/difficulty.js
  - test/difficulty/difficulty.test.js
  - test/unit/combat-scaling.test.js
  - test/unit/movement.test.js
  - test/unit/encounters.test.js
  - test/unit/store-roll.test.js
  - test/unit/foe-turn-draw-count.test.js
  - test/unit/chargen-rng-pin.test.js
  - test/unit/class-mitigation.test.js
  - test/determinism/foe-abilities.test.js
  - test/determinism/same-seed-same-result.test.js
  - test/parity/fixtures/action-script.chargen.json
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixtures/action-script.magic.json
  - test/parity/fixtures/action-script.movement.json
  - test/parity/fixtures/action-script.encounters.json
  - test/parity/fixtures/action-script.economy.json
  - test/parity/divergence-records.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - tools/initiative-fixture-scan-output.txt
  - tools/damage-curve-audit.mjs
  - tools/damage-curve-audit-output.txt
  - content/BESTIARY-REBALANCE.md
  - docs/DIFFICULTY-RETUNE.md
  - docs/class-pass/v17-p54-global-after-smoke.json
  - .planning/phases/54-four-band-retune-and-roster-decision/fit/fit-log.jsonl
  - .planning/phases/54-four-band-retune-and-roster-decision/fit/fit-log.md
  - .planning/phases/54-four-band-retune-and-roster-decision/fit/best.json
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-party.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-start-depth-20.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo-1000.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-start-depth-10.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-smoke.txt
  - .planning/phases/54-four-band-retune-and-roster-decision/54-07-SUMMARY.md
autonomous: true
requirements: [BAND-01, BAND-02, BAND-03, TUNE-08]
tags: [fit, coordinate-search, after-readout, per-floor-survival, change-table, miss-table, round-damage-ceiling, roster, damage-curve-audit, bestiary-addendum, parity-declared, ledger, user-ruling-d]

must_haves:
  truths:
    - "The dials are FITTED, not laddered: `node tools/fit-difficulty.mjs --search --start=fit/start.json --budget=80 --seeds=200 --workers=4 --log=fit/fit-log.jsonl --out=fit/best.json` runs to its stop rule (budget / PASS), every evaluation is in the committed log with its dials, score, verdict, per-floor S_L, reach-20, class-pool columns and constraint result; the BEST set (lowest score with constraints ok) is what `engine/difficulty.js`'s `DIALS` ships — each fitted key's JSDoc cites its log line number; the search order, bounds and steps are `SEARCH_PLAN` (54-06) = the CORE 10 coordinates only (FOE_LEVEL.perDepth, FOE_LEVEL.base, HERO_SP_SCALE, FOE_HIT_SCALE.base, FOE_HIT_SCALE.perDepth, FOE_HP_SCALE.base, HERO_HP_SCALE, HERO_REGEN_PER_FLOOR, HAZARD_SCALE.base, ENCOUNTER_DOTS.base; two passes, ≈ 8 evaluations per coordinate) — never re-ordered mid-run; every other dial is HELD at its `fit/start.json` value (still shipped, still in the change table as `held (available)`, never probed); the objective scores floors 1–12 only"
    - "`CLASS_MITIGATION` is touched ONLY by the manual-notch rule: if the fairness/identity constraint is the ONLY thing blocking the best pooled candidate (a candidate with a lower score than the best constrained one, rejected solely by a class-pool rule), the executor adjusts ONE class row by ONE notch (first candidate Thief `evasion: -1`; alternatives MU `spellPower: 1.15`, Fighter `killSpeed: 0.9`, Fighter `hpMul: 1.1`), re-evaluates that candidate, and records the notch in the log and the ledger — at most TWO notches in the phase; otherwise the rows stay identity"
    - "The Ruling C target STANDS; the VERDICT applies its pass bands to floors 1–12 only (±8 pts S_L on 1–10, ±3 on 11–12) — the AFTER solo (200-seed) readout's per-floor table is the verdict; every floor 1–12 outside its band is a recorded MISS with the dominant death class (combat / dot / starvation-exhaustion), the pace at that floor, the reason, and the HELD dial the executor would release next (recorded, not taken) — never chased past the budget. Floors 13–20 and reach-20 are the TAIL: measured, not fitted, recorded with dS only (user cut, 2026-09-21)"
    - "The DEEP TAIL is measured at AFTER with two extra runs on the fitted commit: a 1,000-seed solo (`--seeds=1000`, ≈ 8 min; the same `--max-actions` as the 200-seed runs) whose per-floor table is THE reported tail (floors 13–20 with dS vs the Ruling C S_L, reach-20 vs the 3–5 % band — reported, not pass/fail) and a `--seeds=200 --start-depth=10` slice (p_L 10–20 vs the curve, informational); both go in the ledger under `#### Tail — measured, not fitted` and in the SUMMARY's per-floor table with a `tail` marker on floors 13–20"
    - "The full AFTER is taken on the fitted commit: solo 200, `--party` 200 (backgrounded), the class smoke → `docs/class-pass/v17-p54-global-after-smoke.json`, `--seeds=50 --start-depth=20` (read as p_20 vs 84.5 %), plus the two tail runs above — every transcript with the survival / pace / class-identity blocks; the class matrix's per-cell spread is RECORDED (widest / narrowest cells named) with no fix column; the class-pool fairness + identity table is in the ledger"
    - "TUNE-08 is closed under the ROUND-DAMAGE CEILING, not by per-creature retunes: Herman / Drarl / Vampire / Djinni / Drake each get a ledger row with their raw max hit at the tier's first floor (`foeLevelBase + 2×diceMax`, atk multiplied), the whole-hit scale at that depth, the ceiling at the level a hero reaches that floor (from the pace table), the clamped per-round maximum, and the decision `stays — capped by ROUND_DAMAGE_CEILING`; the depth-20 slice's per-foe death causes are the evidence; no `content/bestiary.js` edit, no tier move; Ruling B's Drake trim (2d10+4 → 2d8+2) is recorded as SUPERSEDED by the ceiling"
    - "`tools/damage-curve-audit.mjs` is re-keyed to the global model (tier reach per band from `foeLevelFor(d)` with the bleed; crit max = `foeHitFor(levelBase + 2×diceMax, curve(d))` clamped by `roundDamageCapFor(level)`; the HP bars fold `HERO_HP_SCALE`), re-run for both `--rule=whole|dice`, the output re-committed, the flagged-cell count before → after in `content/BESTIARY-REBALANCE.md`'s `### Phase 54 addendum` with every remaining row's Disposition refreshed (`capped` / `stays`) and the Drake row's superseded trim noted"
    - "Parity on the fitted commit: every fixture is expected to move (floor 1 changes — dots, hazards, hero HP, rations, foe hit scale); measured with the scan and the suite; EVERY mover declared — the identity-commit records' `after` values re-measured, chargen seeds declared per seed (`divergences[seed]`: `maxWP`, `wp`, `rations`, `potions`/`gold` if moved), combat/magic/encounters scenarios with `chargenDivergence` (chargen-time fields) and/or the `+54` action-path record (`fromAction` measured), movement/economy at their top level; the guard's `EXPECTED` = the measured list; FIXTURE-INVENTORY's Phase 54 section gains `### Fitted dials — measured set`; the master and comparables.js never edited"
    - "The ledger closes BAND-01 / BAND-02 / BAND-03 / TUNE-08 in the Phase 54 H3: `#### Fit — commit …` (the whole evaluation table + the notch record), `#### AFTER (global model) — commit …` (four transcripts; the per-floor table `| Floor | p_L | S_L | target S_L | dS | combat/dot/starve | level | gold | maxWP | verdict |` for floors 1–12 from the 200-seed solo; the class-pool table; the recorded spread), `#### Tail — measured, not fitted (1,000-seed solo; --start-depth=10 slice)` (floors 13–20 with dS only + reach-20 vs 3–5 %, marked `tail`), `#### Change table (BAND-03)` (every removed constant → `removed`; every dial → identity value → fitted OR held value → direction — held dials marked `held (available)`, MAZE_SIZE `available, not implemented (v1.8 candidate)`; the `available, canon` rows), `#### Miss table (BAND-03)` (one row per floor 1–12 outside its band, each naming the HELD dial to release next; floors 13–20 and reach-20 listed as tail rows with dS only, no verdict), `#### Roster under the ceiling (TUNE-08)`, `#### Depth-20 slice — p_L readout`, `#### Parity — measured set (BAND-02)`; the v1.2 H2 stays last; `docs/class-pass/v17-p54-global-after-smoke.json`'s `meta.commit` equals the fitted commit"
  artifacts:
    - path: "engine/difficulty.js"
      provides: "DIALS at the fitted values with per-key JSDoc citing the fit log"
    - path: ".planning/phases/54-four-band-retune-and-roster-decision/fit/{fit-log.jsonl,fit-log.md,best.json}"
      provides: "every evaluation (the evidence), the markdown rendering, the best set"
    - path: "readouts/global-after-{solo,party,start-depth-20,smoke}.txt + docs/class-pass/v17-p54-global-after-smoke.json"
      provides: "the AFTER"
    - path: "readouts/global-after-solo-1000.txt + readouts/global-after-start-depth-10.txt"
      provides: "the measured TAIL (floors 13–20, reach-20) and the depth-10 slice — readouts, not fitted"
    - path: "test/parity/fixtures/*.json + divergence-records.test.js + FIXTURE-INVENTORY.md + tools/initiative-fixture-scan-output.txt"
      provides: "the fitted moved set, declared per site"
    - path: "tools/damage-curve-audit.mjs + tools/damage-curve-audit-output.txt + content/BESTIARY-REBALANCE.md"
      provides: "the audit under the global model; the Phase 54 addendum; TUNE-08's evidence"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "the eight close-out H4s (Fit, AFTER, Tail, Change table, Miss table, Roster, Depth-20 slice, Parity)"
  key_links:
    - from: "fit/fit-log.jsonl (best row)"
      to: "engine/difficulty.js DIALS"
      via: "the executor copies best.json's values into DIALS by hand, then a unit test asserts DIALS deepStrictEqual the merge of identity + best.json"
      why: "the shipped values must be exactly the evaluated ones — no rounding, no 'tidying'"
    - from: "the AFTER solo per-floor table"
      to: "the miss table"
      via: "every MISS row names the dominant death class, the pace at that floor and the HELD dial to release next"
      why: "BAND-03: each miss recorded with the untaken move and the reason — the untaken move under the user's cut is a named held dial, recorded, never taken"
  prohibitions:
    - "never edit test/parity/prototype-master.js.txt or test/parity/harness/comparables.js; never edit content/bestiary.js (no per-monster retune, no tier move)"
    - "never a floor-range constant; never a race- or sub-class-keyed constant; never tune to a single matrix cell"
    - "never a second search run with a re-ordered plan or a moved bound; never exceed the budget; never more than two CLASS_MITIGATION notches; never probe or hand-move a HELD dial (the miss table names it; the executor does not release it)"
    - "never put floors 13–20 or reach-20 into the objective or the verdict (tail is measured, not fitted); never a MAZE_SIZE dial or grid change"
    - "never hand-type a pin or a fixture value; never loosen a pin; never an undeclared mover"
    - "never touch tools/lib/tuning-bot.mjs (the instrument is frozen since 54-04)"
    - "no new persisted key"
---

<objective>
Fit the global model to the Ruling C curve with the committed search (budget 80, four workers, the fair bot, the class-pool constraints), ship the best dial set in `DIALS`, re-pin every test and declare every fixture the fit moves, take the full AFTER, re-key and re-run the damage-curve audit, record the five roster creatures under the round-damage ceiling (TUNE-08), and close the ledger with the change table, the per-floor miss table, the class-pool table and the recorded cell spread.

Purpose: BAND-01 (the per-floor survival curve of USER RULING C is the target the fit is scored against), BAND-02 (the global dials are the reshaped curve; every dial change is cited against the fit log — the readout that moved toward the curve), BAND-03 (the AFTER lands inside the bands or each miss is recorded with its reason and the untaken move; the change table has a row per constant and dial, before → after), TUNE-08 (the roster decision is recorded per creature under the ceiling, the depth-20 slice read as p_L 20–25, parity declared).

Starting point: `fit/start.json` (54-06). SEARCHED (the core 10, in this order — `SEARCH_PLAN`): FOE_LEVEL.perDepth 0.2, FOE_LEVEL.base 0.6, HERO_SP_SCALE 0.28, FOE_HIT_SCALE.base 0.6, FOE_HIT_SCALE.perDepth 0.02, FOE_HP_SCALE.base 0.8, HERO_HP_SCALE 1.25, HERO_REGEN_PER_FLOOR 0.25, HAZARD_SCALE.base 0.5, ENCOUNTER_DOTS.base 7; two passes (full step, then half step), ≈ 8 evaluations per coordinate, budget 80. HELD at their start values (implemented, shipped, never probed — "held (available)"): LOOT_SCALE 0.8, CAMP_HEAL_FRACTION 0.2, ROUND_DAMAGE_CEILING 0.5, FOE_COUNT_SKEW 1, FOE_HP_SCALE.perDepth 0.015, HAZARD_SCALE.perDepth 0.02, ENCOUNTER_DOTS.perDepth 0.3, FOOD_CLOCK 1.5, ABILITY_THREAT {1.0, 0}, STORE_TIER {0, 0.3}, FIGHT_SHARE 1.0, WANDER_RATE 1, FOE_ACCURACY 0, DARK_BLOBS {−0.4, 0.7}, DARK_RADIUS {3, 1}, DOT_HP_FRACTION {0.24, 0.36, 0.6}. MAZE_SIZE: cut from the phase (no dial). Objective: floors 1–12 only; floors 13–20 and reach-20 are the measured tail (a 1,000-seed solo + a `--start-depth=10` slice at AFTER).

Output: the fitted `DIALS`, the fit log (jsonl + md) and `best.json`, re-pinned tests, the fitted parity set declared + regenerated, the four AFTER transcripts + the two tail transcripts + the smoke JSON, the re-keyed audit + output, the BESTIARY addendum, the ledger close-out, `54-07-SUMMARY.md`.

Schema gate: no new persisted key (the fitted values are constants; `HERO_HP_SCALE` / regen / rations / gold / potions touch the same four chargen-or-arrival points as 54-05/06; the maze grid stays 21×21).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-CONTEXT.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-06-SUMMARY.md
</context>

<ground_rules>
- `PHASE_DIR=.planning/phases/54-four-band-retune-and-roster-decision`; `SCRATCH` = the session scratchpad. Entry: HEAD = 54-06's last commit, tree clean. Record `PRE_FIT=$(git rev-parse HEAD)`.
- Commits: plain `git commit -F "$SCRATCH/msg.txt"` with the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Skip `state.advance-plan`.
- Gates at every commit: `npm test` fail 0; `npm run build:www` exit 0; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git diff --stat $PRE_FIT -- content/bestiary.js tools/lib/tuning-bot.mjs test/parity/harness/comparables.js test/parity/prototype-master.js.txt` empty; `node --test` with explicit paths.
- The search runs in the background (`run_in_background`; ≈ 40 min at 4 workers; stdout → `$PHASE_DIR/fit/search-stdout.txt`); poll the log for the `BEST` line; a tool timeout is never a failure; the log is resumable — if the process dies, re-run the same command (it resumes from the log) and record the resume in the ledger. Never start a second search with different flags.
- Timing for the AFTER: `--party` first in the background (~21 min), then solo → slice → smoke; if any SOLO run hits the cap (`Stuck: N of 200`, N > 0), re-run solo and party with `--max-actions=40000` and record it.
- Context discipline: never cat a whole transcript or the whole log; `grep -A32 'Per-floor survival'`, `grep -A25 '^Pace'`, `grep -A6 '^Class identity'`, `tail -3` on the log, the `BEST` line.
- Every pin / fixture value / ledger number from tool output. No device / human checkpoints; the SUMMARY ends with `## Human verification (deferred to end of run)`.
</ground_rules>

<tasks>
<task type="auto">
  <name>Task 1: The fit — run the committed search from fit/start.json (budget 80, 4 workers, 200 seeds), apply the manual-notch rule if and only if a class-pool constraint alone blocks the best pooled candidate, ship the best set in DIALS (JSDoc citing the log), re-pin every test from measured output, declare + regenerate every fixture the fit moves, update the guard and the inventory; the feat commit</name>
  <files>engine/difficulty.js, .planning/phases/54-four-band-retune-and-roster-decision/fit/fit-log.jsonl, .planning/phases/54-four-band-retune-and-roster-decision/fit/fit-log.md, .planning/phases/54-four-band-retune-and-roster-decision/fit/best.json, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/unit/movement.test.js, test/unit/encounters.test.js, test/unit/store-roll.test.js, test/unit/foe-turn-draw-count.test.js, test/unit/chargen-rng-pin.test.js, test/unit/class-mitigation.test.js, test/determinism/foe-abilities.test.js, test/determinism/same-seed-same-result.test.js, test/parity/fixtures/action-script.chargen.json, test/parity/fixtures/action-script.combat.json, test/parity/fixtures/action-script.magic.json, test/parity/fixtures/action-script.movement.json, test/parity/fixtures/action-script.encounters.json, test/parity/fixtures/action-script.economy.json, test/parity/divergence-records.test.js, test/parity/FIXTURE-INVENTORY.md, tools/initiative-fixture-scan-output.txt</files>
  <read_first>
    - tools/fit-difficulty.mjs header + flags; tools/lib/fit-score.mjs `SEARCH_PLAN`, `scoreSurvival`, `classConstraints` (the rejection reasons' wording — the notch rule keys on them)
    - $PHASE_DIR/fit/start.json; engine/difficulty.js `DIALS` (every key's JSDoc — append the fit citation to the fitted ones)
    - the 54-05 SUMMARY's `## Re-pin ledger` (which tests carry measured literals: CURVE_PINS 1..25/35/50, the floor-1 curve object in the guard, the wiring pins at depths 1/5/20, FULL_FIGHTS draw totals, FULL_FIGHT_PINS, the store-roll ladder and draw counts (STORE_TIER is held — expected unchanged; confirm), the chargen rng pin (STARTING_* / FOOD_CLOCK do not add draws — confirm), same-seed-same-result)
    - test/parity/fixtures/action-script.schema.md lines 31-60 (`divergences[seed]` for the chargen seed set; `chargenDivergence` on scenario holders), test/parity/harness/comparables.js (READ ONLY: `chargenShiftOf` / `chargenShiftDiffs` / `declaredEndDiffs`), test/parity/chargen-parity.test.js (how `divergences[seed]` is asserted), the 54-05 SUMMARY's `## What moved at identity` (the records whose `after` values you re-measure here)
    - test/parity/FIXTURE-INVENTORY.md's `## Phase 54: the global difficulty model …` section (append `### Fitted dials — measured set` after the identity subsections)
  </read_first>
  <action>
    (1) Search: `mkdir -p $PHASE_DIR/fit`; background `node tools/fit-difficulty.mjs --search --start=$PHASE_DIR/fit/start.json --budget=80 --seeds=200 --workers=4 --log=$PHASE_DIR/fit/fit-log.jsonl --out=$PHASE_DIR/fit/best.json > $PHASE_DIR/fit/search-stdout.txt 2>&1`; poll `tail -2 $PHASE_DIR/fit/fit-log.jsonl` every few minutes; done when stdout carries `BEST #`. If the process dies, re-run the identical command (resume) and note it.

    (2) The notch rule: from the log, find `bestConstrained` (lowest finite score) and `bestPooled` (lowest score ignoring the constraints — i.e. the lowest `scoreRaw` among rows whose ONLY rejection reasons are class-pool rules). If `bestPooled.score < bestConstrained.score` AND its rejection reasons are class-pool rules only, apply ONE notch to ONE `CLASS_MITIGATION` row chosen by the reason (a Thief pool below pooled p50 or above Fighter's rounds → Thief `evasion: -1` (first) or `killSpeed: 1.15`; an MU pool below → `spellPower: 1.15`; a Fighter pool below or Fighter dmgTaken < Thief's → `hpMul: 1.1` or `killSpeed: 0.9`), evaluate `bestPooled`'s dials + the notch once (`--dials` single evaluation, appended to the same log with `"notch": {...}` in the row), and take it if it now passes the constraints AND scores below `bestConstrained`; at most two notches in total (the second only if the first improved but did not pass); record every notch decision (taken or not, and why) in `fit/fit-log.md`. Otherwise `CLASS_MITIGATION` stays identity and `bestConstrained` is the winner.

    (3) `fit/fit-log.md`: render the log as a table `| # | score | verdict | pass | S1 | S4 | S5 | S8 | S10 | S12 | tail S15 | tail S20 | tail reach20 | F/T/M p50 | F/T/M reach5 | ok / reason | changed coordinate → value |` (every row), the notch record, and the `BEST` line; `fit/best.json` = the winner (the tool writes it; on a notch win, rewrite it by hand from the winning row and say so).

    (4) Ship: copy every key of `best.json` into `engine/difficulty.js`'s `DIALS` (exact values, no rounding); each searched (core-10) key's JSDoc gains `Fitted (Phase 54, USER RULING D, 2026-09-21): <identity/start> -> <value> — fit/fit-log.jsonl #<n>`; every HELD key's JSDoc gains `held (available) — Phase 54 fit did not search this dial; shipped at its start value <v> (user cut, 2026-09-21)`; canon-exposed keys keep their identity JSDoc. Add to `test/difficulty/difficulty.test.js`: `USER RULING D: DIALS deepStrictEqual the merge of the identity column and fit/best.json (the shipped values are the evaluated ones)` (read the JSON from disk in the test; the identity column is a literal in the test file). Then re-pin (paste from `node -e` / the failing test's own message; comment `Phase 54 fit`): `CURVE_PINS` 1..25/35/50; the guard's floor-1 object; `foeLevelFor` map string; `storeTier` / `darkBlobs` / `dots` map strings; the wiring pins at 1/5/20 (wp / hit / count / ceiling); `FULL_FIGHTS` draw totals (explain each moved total: foe level, hit/HP scale — the count table is held); `FULL_FIGHT_PINS` (the tier-first depths move with FOE_LEVEL — the helper recomputes; the fights re-measured); `store-roll` ladder + draw counts (held STORE_TIER — expected unchanged, confirmed by running); `chargen-rng-pin` (no draw change expected — confirm and say so); `same-seed-same-result` (re-measured); `class-mitigation` (the notch value if taken). Any failure not explained by a fitted dial is a STOP.

    (5) Parity on the fitted engine: `node tools/initiative-fixture-scan.mjs > $SCRATCH/scan-fit.txt; diff tools/initiative-fixture-scan-output.txt $SCRATCH/scan-fit.txt`; `node --test test/parity/ 2>&1 | tee $SCRATCH/parity-fit.txt | tail -12`. For every mover: chargen seeds → `divergences[seed]` (`phase: "<existing>+54"` or `"54"`, `requirements: ["BAND-02"]`, `fields` = exactly the chargen fields that differ (`maxWP`, `wp`, `rations`, `gold`, `potions` as measured), `before` from the prototype sandbox, `after` from `newRun(seed).c`, rationale `USER RULING D — fitted dials: HERO_HP_SCALE <v> / FOOD_CLOCK <v> / …`); combat / magic / encounters scenarios → `chargenDivergence` for the chargen-time fields PLUS the `+54` action-path record when the action path itself moves (`fromAction` measured; `fields`/`before`/`after`/`stateFields` from the harness output; the identity-commit records' `after` values are RE-MEASURED and overwritten — never left stale); movement / economy → the same at their top level. `_note` gains one sentence per holder. Guard: `EXPECTED` = the measured holder list (chargen seeds are listed as `action-script.chargen.json#<seed>`), the floor-1 object re-pinned (`difficultyCurve(1)` under the fitted dials), Part (c) count-roll shape kept, `totalSites === 31` kept. `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt`. FIXTURE-INVENTORY: `### Fitted dials — measured set` (the fitted dial values; the scan diff; the suite totals before/after the records; `MOVED SET (<n>)` with a per-holder table `| Holder | Record kind | fromAction | Fields | before → after |`; `### Draw-count pins (fitted)`; `### Byte-identical elsewhere (fitted)` — expected empty or near-empty; say which sites did NOT move and why (e.g. a chargen seed whose class-mean HP rounds to the same number is impossible under HERO_HP_SCALE ≠ 1 — if `HERO_HP_SCALE` fitted to exactly 1.0, say so)).

    (6) Gates + commit: `npm test` fail 0; `build:www` exit 0; master hash; the prohibited-file diff empty. Commit `feat(54-07): fitted global dials (fit/fit-log.jsonl #<best n>, score <s>, verdict <PASS|MISS floors …>, reach-20 <x> %) — DIALS shipped at the evaluated values; every pin re-measured; fitted parity set declared + regenerated (MOVED SET (<n>)); CLASS_MITIGATION <identity | notch: …> (USER RULING D, BAND-01, BAND-02)` with the two trailers. Record `FIT_SHA`.
  </action>
  <verify>
    <automated>test -s .planning/phases/54-four-band-retune-and-roster-decision/fit/fit-log.jsonl && node -e "const fs=require('fs');const rows=fs.readFileSync('.planning/phases/54-four-band-retune-and-roster-decision/fit/fit-log.jsonl','utf8').trim().split('\n').map(JSON.parse);const ok=rows.filter(r=>Number.isFinite(r.score)&&r.constraints&&r.constraints.ok);const best=ok.reduce((a,b)=>b.score<a.score?b:a);console.log('evals',rows.length,'best#',best.n,'score',best.score,'verdict',best.verdict,'reach20',best.reach20,'notches',rows.filter(r=>r.notch).length)" && node --test test/difficulty/difficulty.test.js test/unit/combat-scaling.test.js test/unit/store-roll.test.js test/unit/foe-turn-draw-count.test.js test/determinism/foe-abilities.test.js test/unit/fit-score.test.js test/parity/divergence-records.test.js test/parity/full-suite.test.js test/parity/chargen-parity.test.js && node -e "const b=require('./.planning/phases/54-four-band-retune-and-roster-decision/fit/best.json');const s=require('./.planning/phases/54-four-band-retune-and-roster-decision/fit/start.json');const held=['LOOT_SCALE','CAMP_HEAL_FRACTION','ROUND_DAMAGE_CEILING','FOE_COUNT_SKEW','FOOD_CLOCK','WANDER_RATE','FOE_ACCURACY'];console.log('held-unchanged',held.every(k=>JSON.stringify(b[k])===JSON.stringify(s[k])),'held-perDepth-unchanged',b.FOE_HP_SCALE.perDepth===s.FOE_HP_SCALE.perDepth&&b.HAZARD_SCALE.perDepth===s.HAZARD_SCALE.perDepth&&b.ENCOUNTER_DOTS.perDepth===s.ENCOUNTER_DOTS.perDepth,'no-maze','MAZE_SIZE' in b)" && node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && echo SCAN-CLEAN && grep -c '^### Fitted dials — measured set' test/parity/FIXTURE-INVENTORY.md && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - the log print shows `evals` ≤ 82 (80 + at most 2 notch evaluations), a finite `score`, and `notches` ≤ 2; `fit/best.json` exists and the `DIALS deepStrictEqual identity + best.json` test is green; the verify's held-dial check prints `held-unchanged true held-perDepth-unchanged true no-maze false` (only the core 10 moved from `fit/start.json`; every held dial ships at its held value; no MAZE_SIZE key)
    - every log row's `score` and `verdict` were computed on floors 1–12 only (the `terms` array has 12 entries; `tail` carries 13–20) — `node -e` over the first row pasted in the SUMMARY
    - `grep -c 'fit/fit-log.jsonl #' engine/difficulty.js` >= the number of core-10 coordinates whose fitted value differs from `fit/start.json` (the SUMMARY lists them); every held dial's JSDoc says `held (available) — Phase 54 fit did not search this dial`
    - every listed test file prints `# fail 0`; `SCAN-CLEAN` prints; the inventory grep prints `1`; the guard's `EXPECTED` length equals the `MOVED SET (n)` in the inventory and equals the count of holders carrying a record whose `phase` contains `54` (`node -e` pasted in the SUMMARY)
    - no fixture value or pin was hand-typed (the SUMMARY's re-pin ledger names the source of each); master hash unchanged; `git diff --stat $PRE_FIT -- content/bestiary.js tools/lib/tuning-bot.mjs test/parity/harness/comparables.js` empty
    - `npm test` fail 0; `build:www` exit 0; the feat commit carries the two trailers and names the best evaluation number, the score, the verdict and the moved-set size
  </acceptance_criteria>
  <done>The best constrained dial set from the committed search ships in DIALS with the fit log as evidence; every pin is re-measured and every fixture the fit moved is declared and regenerated.</done>
</task>
<task type="auto">
  <name>Task 2: The AFTER — four readouts on FIT_SHA (party backgrounded; solo; depth-20 slice as p_L; class smoke → v17-p54-global-after-smoke.json) + the two TAIL runs (1,000-seed solo; --start-depth=10 slice — measured, not fitted); the damage-curve audit re-keyed to the global model and re-run (both rules); the round-damage-ceiling table for Herman / Drarl / Vampire / Djinni / Drake (TUNE-08); the BESTIARY-REBALANCE Phase 54 addendum; docs commit</name>
  <files>.planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-party.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-start-depth-20.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo-1000.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-start-depth-10.txt, .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-smoke.txt, docs/class-pass/v17-p54-global-after-smoke.json, tools/damage-curve-audit.mjs, tools/damage-curve-audit-output.txt, content/BESTIARY-REBALANCE.md</files>
  <read_first>
    - tools/damage-curve-audit.mjs lines 1-120 (header, `BANDS`, `--rule`), 118-160 (`classBarAt` / `muBarAt` / `ftBarAt` — fold `HERO_HP_SCALE` via `engine/difficulty.js`'s `heroMeanMaxWpFor`-style arithmetic: bars × the dial), 195-250 (`dmgBonusForBand` → replace with `hitScaleForBand(band)` = max `foeHitScale` over the band's floors and `tierReachable(T, band)` = some floor d in the band with `foeLevelFor(d) ∈ {T, T+1}` (the bleed lets tier T appear one tier below map(d)); `critMaxOf` → `Math.min(foeHitFor(levelBase + 2*diceMax or the whole rule, curveAtMaxScale), roundDamageCapFor(level))`; keep both `--rule` variants), 250-389 (main; the `FLAGGED:` line)
    - tools/damage-curve-audit-output.txt (the BEFORE section shape; the Phase 52 AFTER section — append a `# Phase 54 (global model)` pair of sections rather than deleting history), test/unit/bestiary-yardstick.test.js line 267-295 (the D-04 doc-consistency test on BESTIARY-REBALANCE's `yardstick:after` block — the block must stay byte-identical since bestiary.js is untouched)
    - content/BESTIARY-REBALANCE.md lines 453-560 (the Phase 27 and Phase 52 addenda shape; the `#### Still flagged after the fixes (rule=dice) — pending ruling` table whose rows get their Phase 54 disposition)
    - content/bestiary.js rows for Herman (Humans t4/t5, `strikesAs: 5`, d6), Drarl (Lair Beasts t4/t5, acid, noArmor, d6), Vampire (Walking Dead t5, `atk: 2`, d4), Djinni (Demons t4/t5, caster, d4), Drake (Beasts t4, 2d10+4, breath every 4) — the raw maxima: Herman 25 + 12 = 37; Drarl 25 + 12 = 37 (t5) / 16 + 12 = 28 (t4); Vampire (25 + 8) × 2 = 66 per round; Djinni 25 + 8 = 33; Drake 16 + 48 = 64 (2×(2×10+4) crit) — recompute from the rows, never from this note
    - the AFTER solo `Pace` block (the mean hero level at the first floor of each tier — the ceiling's level input for the roster table)
  </read_first>
  <action>
    Step 1 — the AFTER on FIT_SHA (tree clean; `git diff --stat FIT_SHA -- engine/ content/` empty — paste): background `--party` → `readouts/global-after-party.txt`; solo → `global-after-solo.txt`; `--seeds=50 --start-depth=20` → `global-after-start-depth-20.txt`; `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p54-global-after-smoke.json > readouts/global-after-smoke.txt`; the `--max-actions` rule as in 54-04. The solo (200-seed) `verdict:` line is THE verdict (floors 1–12); paste it into the SUMMARY unedited. THEN the two TAIL runs (user cut, 2026-09-21 — measured, not fitted): `node tools/tune-difficulty.mjs --seeds=1000 > readouts/global-after-solo-1000.txt 2>&1` (≈ 8 min; the same `--max-actions` as the 200-seed runs; run it in the background while the party run is still going if the machine has the cores, else after) — its `Per-floor survival` block is THE reported tail: floors 13–20 with dS vs the Ruling C S_L and reach-20 vs the 3–5 % band, both READOUTS (no pass/fail); and `node tools/tune-difficulty.mjs --seeds=200 --start-depth=10 > readouts/global-after-start-depth-10.txt 2>&1` (p_L 10–20 vs the curve, informational). Quote both blocks in the ledger under `#### Tail — measured, not fitted` and mark floors 13–20 `tail` wherever a per-floor table appears.

    Step 2 — re-key `tools/damage-curve-audit.mjs` to the global model (imports: `difficultyCurve`, `foeLevelFor`, `foeHitFor`, `roundDamageCapFor`, `heroMeanMaxWpFor`, `DIALS` from `engine/difficulty.js`); the band levels still come from the smoke (`--smoke docs/class-pass/v17-p54-global-after-smoke.json`) — verify the flag exists or add `--smoke=<path>`; a tier that no floor of a band can produce prints `n/a` for that band (as today's `null`); the report header names the fitted dials it read. Run `node tools/damage-curve-audit.mjs --rule=whole --smoke docs/class-pass/v17-p54-global-after-smoke.json` and `--rule=dice …`, append both as `# Phase 54 (global model) — rule=whole` / `— rule=dice` sections to `tools/damage-curve-audit-output.txt` (history above kept); record `FLAGGED: <n> rows across <m> row×band cells` for both rules (BEFORE: 65 under Phase 52's dice rule).

    Step 3 — the roster table (TUNE-08), computed by a scratch `node -e` over the fitted engine and pasted: for each of Herman / Drarl / Vampire / Djinni / Drake: `| Creature | Type / tier | First floor the tier appears (foeLevelFor with bleed) | Raw max per round (levelBase + 2×diceMax, × atk) | foeHitScale at that floor | Scaled max | Hero level at that floor (AFTER Pace mean, rounded) | Ceiling = roundDamageCapFor(level) | Clamped per-round max | Depth-20 slice deaths (n / 50) | Decision |` — every Decision reads `stays — capped by ROUND_DAMAGE_CEILING <value>` (no retune, no tier move); a `**Ruling B superseded:**` line records that the Drake's 2d10+4 → 2d8+2 trim is NOT applied (the ceiling bounds its crit instead — the numbers show it). The depth-20 slice's `Death-cause breakdown` top rows are quoted as the evidence.

    Step 4 — `content/BESTIARY-REBALANCE.md`: append `### Phase 54 addendum (USER RULING D, 2026-09-21) — the global model and the round-damage ceiling` with: the audit cell counts before → after per rule; the roster table (Step 3) verbatim; the refreshed Disposition of every row in Phase 52's `#### Still flagged …` table (`capped by ROUND_DAMAGE_CEILING` / `below the bar under FOE_HIT_SCALE` / `stays`), no dice trims, no tier moves; the Ruling B supersession line; the `yardstick:after` block UNTOUCHED (the D-04 test proves it).

    Step 5 — gates: `node --test test/unit/bestiary-yardstick.test.js test/unit/class-pass-ledger.test.js` green; `npm test` fail 0; `build:www` exit 0; master hash; `git diff --stat $PRE_FIT -- content/bestiary.js` empty. Commit `docs(54-07): AFTER readouts on the fitted dials (solo / --party / class smoke / depth-20 slice) + damage-curve audit re-keyed to the global model (both rules, FLAGGED <n>/<m>) + roster under the ceiling (Herman / Drarl / Vampire / Djinni / Drake stay — capped; Ruling B Drake trim superseded) + BESTIARY-REBALANCE Phase 54 addendum (TUNE-08, BAND-03)` with the two trailers. Record `AFTER_SHA`.
  </action>
  <verify>
    <automated>for f in solo party start-depth-20 solo-1000 start-depth-10 smoke; do test -s ".planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-$f.txt" && echo "ok $f"; done; grep -o 'verdict: .*' .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo.txt | head -1; grep -c 'tune-difficulty: 1000 seeded' .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo-1000.txt; grep -o 'reach-20: .*' .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-solo-1000.txt | head -1; grep -c 'start depth 10' .planning/phases/54-four-band-retune-and-roster-decision/readouts/global-after-start-depth-10.txt; node -e "const j=require('./docs/class-pass/v17-p54-global-after-smoke.json');console.log(j.meta.commit,j.cells.length)"; grep -c '^# Phase 54 (global model) — rule=' tools/damage-curve-audit-output.txt; grep -c 'FLAGGED:' tools/damage-curve-audit-output.txt; grep -c '^### Phase 54 addendum' content/BESTIARY-REBALANCE.md; grep -c 'stays — capped by ROUND_DAMAGE_CEILING' content/BESTIARY-REBALANCE.md; node --test test/unit/bestiary-yardstick.test.js; git diff --stat HEAD~1 -- content/bestiary.js | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - the six `ok …` lines print; a `verdict:` line prints (PASS or the listed floors — floors 1–12 only); the 1,000-seed transcript's header grep prints `1` and its `reach-20:` line prints (the tail readout); the depth-10 slice's header grep prints `1`; the smoke's `meta.commit` equals FIT_SHA's short form and `cells.length` is 143
    - the audit-output grep prints `2` (whole + dice Phase 54 sections) and the `FLAGGED:` count grew by exactly 2 vs $PRE_FIT; `grep -c 'foeDmgBonusFor' tools/damage-curve-audit.mjs` == 0 and `grep -c 'roundDamageCapFor\|foeLevelFor\|foeHitFor' tools/damage-curve-audit.mjs` >= 3
    - the addendum grep prints `1`; the `stays — capped` grep prints `5` (one per roster creature); `grep -c 'Ruling B superseded' content/BESTIARY-REBALANCE.md` == 1; `bestiary-yardstick.test.js` green (the AFTER block untouched); `content/bestiary.js` diff empty
    - `npm test` fail 0; `build:www` exit 0; master hash; two trailers
  </acceptance_criteria>
  <done>The AFTER is on disk with the verdict; the audit reads the global model; the five roster creatures are recorded as capped by the ceiling with numbers; the bestiary is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: The ledger close-out — Fit, AFTER (per-floor table with pace + class columns, class-pool table, recorded spread), Change table (BAND-03), Miss table (BAND-03), Roster under the ceiling (TUNE-08), Depth-20 slice p_L, Parity measured set (BAND-02); the verifier-grade phase SUMMARY; docs commit</name>
  <files>docs/DIFFICULTY-RETUNE.md, .planning/phases/54-four-band-retune-and-roster-decision/54-07-SUMMARY.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md — the Phase 54 H3 as it stands (`#### Target — the per-floor survival curve (USER RULING C …)`, `#### Why floor bands failed …`, `#### Global model (USER RULING D) — the dial table`, `#### BEFORE (global model) …`, `#### Identity commit …`, `#### Hero-side / late dials + the fit tool …`); lines 3534-3580 of the v1.2 section (the Phase 27 change-table shape); test/unit/difficulty-retune-ledger.test.js (v1.2 H2 last)
    - $PHASE_DIR/fit/fit-log.md, fit/best.json; the four `global-after-*.txt` blocks; `tools/damage-curve-audit-output.txt`'s Phase 54 sections; content/BESTIARY-REBALANCE.md's Phase 54 addendum; FIXTURE-INVENTORY's `### Fitted dials — measured set`
    - the 54-04 / 54-05 / 54-06 SUMMARYs (commit SHAs, test names, the BEFORE numbers)
  </read_first>
  <action>
    Splice, after `#### Hero-side / late dials + the fit tool …` and before `## v1.2 retune`, in this order:
    (a) `#### Fit — commit <40-hex FIT_SHA> (bounded coordinate search over SEARCH_PLAN; budget 80; 4 workers; 200 seeds)` — Parameters (the exact command, the start file, the stop reason: budget / PASS), the full `fit/fit-log.md` table verbatim, the notch record (taken or "none — the best pooled candidate was also the best constrained one"), the `BEST` line, and `**What the fit moved:**` one sentence per coordinate whose value changed from the start (with direction: harder / easier).
    (b) `#### AFTER (global model) — commit <40-hex FIT_SHA> (readouts committed at <AFTER_SHA>)` — the four fenced transcript blocks (solo: Death-depth distribution, Death-cause top 12, Per-floor survival, Pace, Class identity, Stuck, Bot; party: survival + class identity + Stuck; slice: survival; smoke: pooled + Class spread), then the compact table `| Floor | p_L | S_L | target S_L | dS | combat/dot/starve | level | gold | maxWP | verdict |` for floors 1–20 — floors 1–12 from the 200-seed solo with PASS/MISS, floors 13–20 from the 1,000-seed solo marked `tail` with dS only — the class-pool table `| Class | n | p50 | reach5 | reach10 | dmg taken/fight | rounds/fight | foe miss % | casts def/off | potions | ok |`, the recorded spread (`per class: widest cell (sub/race, p50) / narrowest cell` — "intended; no fix column"), and `**Reading:**` (three sentences: the verdict; what BEFORE → AFTER moved most (p50, reach 5/10/20, the death-class shares); the pace at floors 4/9/14/18 vs the target pace).
    (b2) `#### Tail — measured, not fitted (1,000-seed solo; --start-depth=10 slice) — user cut 2026-09-21` — the 1,000-seed transcript's `Per-floor survival` block verbatim (its `Stuck:` and `Bot:` lines too), a table `| Floor | reached (of 1000) | p_L | S_L | target S_L | dS | note |` for floors 13–20 with the note column reading `tail`, the reach-20 line vs the 3–5 % band (`<x> % — reported, not pass/fail`), the depth-10 slice's survival block verbatim with its p_L 10–20 vs the curve, and the sentence "Floors 13–20 and reach-20 are not in the fit's objective or the verdict; they are recorded here as measured."
    (c) `#### Change table (BAND-03)` — `| Constant / dial | File | Before (5550002) | Identity commit | Fitted / held (FIT_SHA) | Searched? | Direction | Record |`: one row per REMOVED constant (`→ removed`, the ledger row as the record), one row per dial — the core 10 with `fitted` and their log line, every held dial with `held (available)` and its held value, the `available, canon` rows with `— (not searched)`, `MAZE_SIZE` with `available, not implemented (v1.8 candidate)` — the `CLASS_MITIGATION` rows (identity or the notch), and the bot policy rows (54-04) for completeness.
    (d) `#### Miss table (BAND-03)` — one row per floor 1–12 outside its band: `| Floor | S_L | target | dS | dominant death class | pace (level / gold / maxWP) | reason | untaken move (held dial to release next) |` — the untaken move names ONE held dial from `HELD_DIALS` with the direction the miss calls for (e.g. floor 5 too deadly from combat → `ROUND_DAMAGE_CEILING ↓ 0.5 → 0.4`; from dots → `HAZARD_SCALE.perDepth ↓`; from starvation → `FOOD_CLOCK ↑`; a class-pool rejection → the CLASS_MITIGATION notch) — recorded, NOT taken; then the tail rows 13–20 (`tail — dS <x>, no verdict`) and the reach-20 row (`tail — <x> % vs 3–5 %, reported`); if the 1–12 verdict is PASS the fitted rows collapse to one: `none — every floor 1–12 inside its band`, and the tail rows still follow.
    (e) `#### Roster under the ceiling (TUNE-08)` — the Step-3 table from Task 2 verbatim + the Ruling B supersession line + the sentence that the forced-20 slice reads p_20 `<x> %` vs 84.5 % (the Endgame shape is the Ruling C 16–20 segment, not residue) and the tier-5 roster note (7 creatures; first floor of tier 5 under the fitted map; the v1.8 content candidate if < 17).
    (f) `#### Depth-20 slice — p_L readout (USER RULING C)` — the slice's survival block + a line per floor 20–25 vs 84.5 → 88.9 % (informational beyond 20).
    (g) `#### Parity — measured set (BAND-02)` — the scan diffs (identity commit; fitted), the suite totals, `MOVED SET (<n>)` with the holder list, `grep -rc '"BAND-02"' test/parity/fixtures/*.json`, the master hash, "comparables.js untouched", and the pointer to FIXTURE-INVENTORY's Phase 54 section.
    Then: `node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js` green; `npm test` fail 0; `build:www` exit 0; master hash. Commit `docs(54-07): ledger close-out — Fit, AFTER (per-floor table + pace + class pools + recorded spread), Change table, Miss table, Roster under the ceiling, Depth-20 p_L, Parity measured set (BAND-01, BAND-02, BAND-03, TUNE-08)` with the two trailers.

    The SUMMARY `$PHASE_DIR/54-07-SUMMARY.md` (frontmatter as before; `requirements-completed: [BAND-01, BAND-02, BAND-03, TUNE-08]`): `## Success criteria → proof` — ROADMAP SC1 (the Ruling C table is the live target; BAND-01's numeric table superseded — pointer), SC2 (the global dials as the reshaped curve; every fitted value cited to a log line; the export-key-set pin), SC3 (the AFTER verdict verbatim — floors 1–12; the tail readout; the miss table; the change table), SC4 (the roster table; the slice p_20; parity declared), SC5 (no bestiary edit; gates) — each with the test name / file / commit; `## The fit` (start → best; score; verdict; evaluations; notches; the winning dials as JSON); `## AFTER — key numbers` (p50/p90, reach 5/10/20, the per-floor table `| Floor | p_L | S_L | target | dS | verdict/tail |` for 1–20 with floors 13–20 marked `tail` and sourced from the 1,000-seed run, reach-20 from the 1,000-seed run (reported vs 3–5 %), the depth-10 slice p_10..20, death-class shares, class-pool p50s + constraints ok, spread, party p50 + stuck, slice p_20, smoke pooled mean, audit FLAGGED before → after); `## Change table` (verbatim); `## Miss table` (verbatim); `## Roster (TUNE-08)` (verbatim); `## Re-pin ledger` (file → test → old → new → source); `## Parity — measured set` (identity + fitted: scan diffs, totals, EXPECTED, records by kind); `## Gates` (npm test totals at FIT_SHA / AFTER_SHA / the docs commit; build:www; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git diff --stat 5550002 -- content/bestiary.js test/parity/harness/comparables.js` empty); `## Schema` (none — the four touch points; tolerant-load); `## Commits` (FIT_SHA, AFTER_SHA, the docs SHA); `## Deviations`; `## Issues Encountered`; `## Next Phase Readiness` (Phase 55: the debug APK is built from this commit; the Pixel 7 checklist; the v1.8 candidates: Endgame roster, ration-buying bot); `## Human verification (deferred to end of run)` — the Pixel 7 batch items this phase owes: a natural run dies around floors 5–7 (the bot's S_6 ≈ 58 %); floors 1–3 feel survivable (S_3 ≈ 89 %); a failed climb/leap lands you on the far side with the `draggedOver` line; each new floor shows the `floorRegen` line (+N hp); a start-at-20 run meets tier-5 foes and survives a floor about 85 % of the time; a Herman / Drake hit never exceeds ~half your max HP in one round; the Oracle/rail copy for the two new lines reads in voice; the class matrix's widest cells (named) are the "hand you're dealt", not bugs.
  </action>
  <verify>
    <automated>for h in '^#### Fit — commit ' '^#### AFTER (global model) — commit ' '^#### Tail — measured, not fitted' '^#### Change table (BAND-03)' '^#### Miss table (BAND-03)' '^#### Roster under the ceiling (TUNE-08)' '^#### Depth-20 slice — p_L readout' '^#### Parity — measured set (BAND-02)'; do grep -c "$h" docs/DIFFICULTY-RETUNE.md; done; grep -c 'held (available)' docs/DIFFICULTY-RETUNE.md; grep -c 'available, not implemented (v1.8 candidate)' docs/DIFFICULTY-RETUNE.md; node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js; test -f .planning/phases/54-four-band-retune-and-roster-decision/54-07-SUMMARY.md && grep -c '^## Human verification (deferred to end of run)' .planning/phases/54-four-band-retune-and-roster-decision/54-07-SUMMARY.md; git hash-object test/parity/prototype-master.js.txt; git diff --stat 5550002 -- content/bestiary.js test/parity/harness/comparables.js | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - all eight heading greps print `1`; the `held (available)` grep prints >= 14 (one per held dial in the change table, at least) and the `available, not implemented (v1.8 candidate)` grep >= 1 (MAZE_SIZE); the ledger tests are green (the v1.2 H2 still the last H2); the SUMMARY grep prints `1`
    - the Change table has a row for every name in 54-05's remove list AND every dial in 54-06's dial table (`for k in COMBAT_SCALE_FROM_DEPTH FOE_GRACE_AT_2 WALL_FOE_POWER_AT_START ENDGAME_FOE_POWER_AT_END ENCOUNTER_DOT_CAP DARK_HOLD_THROUGH_DEPTH knotFoePowerFor foeDmgBonusFor FOE_LEVEL HERO_SP_SCALE LOOT_SCALE FOE_HIT_SCALE FOE_HP_SCALE HERO_HP_SCALE HERO_REGEN_PER_FLOOR CAMP_HEAL_FRACTION ROUND_DAMAGE_CEILING FOE_COUNT_SKEW HAZARD_SCALE ENCOUNTER_DOTS FOOD_CLOCK ABILITY_THREAT STORE_TIER FIGHT_SHARE WANDER_RATE FOE_ACCURACY MAZE_SIZE DARK_BLOBS CLASS_MITIGATION TIER_SPREAD; do grep -c "$k" docs/DIFFICULTY-RETUNE.md; done` — every count >= 1 in the section between `#### Change table` and `#### Miss table`)
    - the Miss table has one row per floor 1–12 named in the AFTER solo `verdict:` line (or the single `none —` row on PASS), each naming a dominant death class and ONE held dial from `HELD_DIALS` with a direction as the untaken move, followed by the eight tail rows (13–20, `tail`, dS only) and the reach-20 tail row; the Tail section quotes the 1,000-seed and depth-10 survival blocks verbatim
    - the Roster section contains five `stays — capped by ROUND_DAMAGE_CEILING` rows and the `Ruling B superseded` line; the Parity section's `MOVED SET (n)` equals the guard's `EXPECTED` length
    - master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `content/bestiary.js` and `comparables.js` unchanged since 5550002; `npm test` fail 0; `build:www` exit 0; two trailers; the SUMMARY names FIT_SHA, AFTER_SHA and the docs SHA and lists every test re-pinned in this plan
  </acceptance_criteria>
  <done>The ledger closes the phase with the fit, the AFTER, the change table, the miss table, the roster under the ceiling, the slice and the measured parity set; the SUMMARY carries verifier-grade evidence for BAND-01/02/03 and TUNE-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fit tool → engine | worker threads override `DIALS` via `setDialsForTuning`; the shipped values are copied by hand and pinned equal to `best.json` |
| readouts / log → ledger | every number is pasted from a committed transcript or the committed log; the smoke's `meta.commit` pins the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-07-01 | Tampering | shipped dials differ from the evaluated ones | medium | mitigate | the `DIALS deepStrictEqual identity + best.json` unit test; JSDoc cites the log line |
| T-54-07-02 | Repudiation | a "tidied" fit (re-ordered plan, moved bound, extra evaluations) | medium | mitigate | one committed log, append-only, ≤ 82 rows, the command recorded in the ledger; resumes recorded |
| T-54-07-03 | Repudiation | an undeclared fixture move on the fitted engine | medium | mitigate | the guard's `EXPECTED` = the measured set; the scan output committed; master never edited |
| T-54-SC | Tampering | npm/pip/cargo installs | high | accept | no package installs (`git diff --stat $PRE_FIT -- package.json package-lock.json` empty) |
</threat_model>

<verification>
- `fit/fit-log.jsonl` has ≤ 82 rows, a finite best score (floors 1–12) with constraints ok, ≤ 2 notches; only the core 10 coordinates differ between `start.json` and `best.json`; `DIALS` equals identity + `best.json` (unit test); no `MAZE_SIZE` key anywhere
- `npm test` fail 0 and `npm run build:www` exit 0 at FIT_SHA, AFTER_SHA and the docs commit; master hash unchanged; `content/bestiary.js`, `comparables.js`, `tuning-bot.mjs` untouched since 5550002 (bestiary) / 54-04 (bot)
- the parity suite green with every fitted mover declared; the scan output byte-identical to the committed copy
- the four AFTER transcripts + the two tail transcripts (1,000-seed solo; depth-10 slice) exist with the three blocks; the smoke JSON's `meta.commit` = FIT_SHA; the audit output carries both Phase 54 sections; the BESTIARY addendum exists
- the ledger's eight close-out H4s exist (incl. `#### Tail — measured, not fitted`); the v1.2 H2 stays last
</verification>

<success_criteria>
- BAND-01: the fit is scored against the Ruling C curve on floors 1–12; the AFTER per-floor table vs the curve is in the ledger with the verdict (1–12) and the measured tail (13–20, reach-20 — dS only)
- BAND-02: the global dials ship at the evaluated values — the core 10 fitted and cited to the fit log, every other dial held at its start value and marked so; no floor-range constant; no MAZE_SIZE; parity measured and declared
- BAND-03: the change table (every removed constant, every dial, identity → fitted / held) and the miss table (per floor 1–12: dominant death class, reason, the held dial to release next; tail rows dS-only) are in the ledger
- TUNE-08: Herman / Drarl / Vampire / Djinni / Drake recorded under the ceiling with numbers; no retune, no tier move; Ruling B's Drake trim superseded; the depth-20 slice read as p_L; parity declared
- SUMMARY with verifier-grade evidence, ending with `## Human verification (deferred to end of run)`
</success_criteria>

<artifacts_this_phase_produces>
- `engine/difficulty.js` — `DIALS` at the fitted values
- `fit/fit-log.jsonl`, `fit/fit-log.md`, `fit/best.json` — the fit evidence
- `readouts/global-after-{solo,party,start-depth-20,smoke}.txt`, `docs/class-pass/v17-p54-global-after-smoke.json` — the AFTER; `readouts/global-after-solo-1000.txt`, `readouts/global-after-start-depth-10.txt` — the measured tail
- re-pinned tests (difficulty / combat-scaling / movement / encounters / store-roll / foe-turn-draw-count / chargen-rng-pin / class-mitigation / foe-abilities / same-seed-same-result)
- `test/parity/fixtures/*.json` (fitted records), `test/parity/divergence-records.test.js` (EXPECTED), `test/parity/FIXTURE-INVENTORY.md` (`### Fitted dials — measured set`), `tools/initiative-fixture-scan-output.txt`
- `tools/damage-curve-audit.mjs` (re-keyed), `tools/damage-curve-audit-output.txt` (Phase 54 sections), `content/BESTIARY-REBALANCE.md` (Phase 54 addendum)
- `docs/DIFFICULTY-RETUNE.md` — the eight close-out H4s (Fit, AFTER, Tail, Change table, Miss table, Roster, Depth-20 slice, Parity)
- `54-07-SUMMARY.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/54-four-band-retune-and-roster-decision/54-07-SUMMARY.md` when done (verifier-grade; ends with `## Human verification (deferred to end of run)`).
</output>

