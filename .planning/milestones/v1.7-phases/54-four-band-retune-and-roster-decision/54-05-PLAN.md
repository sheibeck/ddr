---
phase: 54-four-band-retune-and-roster-decision
plan: 05
type: execute
wave: 5
depends_on: ["54-04"]
files_modified:
  - engine/difficulty.js
  - engine/combat.js
  - engine/character.js
  - engine/movement.js
  - engine/encounters.js
  - engine/maze.js
  - engine/events.js
  - src/browser/narrationLines.js
  - src/browser/eventNarration.js
  - src/browser/rail.js
  - test/difficulty/difficulty.test.js
  - test/unit/combat-scaling.test.js
  - test/unit/movement.test.js
  - test/unit/encounters.test.js
  - test/unit/foe-turn-draw-count.test.js
  - test/unit/one-and-done-lines.test.js
  - test/unit/narrationLinesCoverage.test.js
  - test/determinism/foe-abilities.test.js
  - test/parity/divergence-records.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixtures/action-script.magic.json
  - test/parity/fixtures/action-script.movement.json
  - test/parity/fixtures/action-script.encounters.json
  - test/parity/fixtures/action-script.economy.json
  - tools/initiative-fixture-scan-output.txt
  - docs/DIFFICULTY-RETUNE.md
  - .planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md
autonomous: true
requirements: [BAND-02]
tags: [difficulty-model, global-dials, foe-level-from-depth, whole-hit-scale, round-damage-ceiling, hero-hp-regen, one-and-done-climb, parity-declared, user-ruling-d]

must_haves:
  truths:
    - "Every floor-range constant and helper is GONE from `engine/difficulty.js` (the complete remove list is in the objective) and the module's export key set contains no name matching `/_(FROM|TO|THROUGH)_DEPTH$|_AT_(START|END|[0-9]+)$|SOFT_K$|GRACE|KNOT|CANON/` — a unit test pins the export key set; every dial is ONE number or `{ base, perDepth }` (a smooth slope over depth, never a range), living on the single frozen `DIALS` object"
    - "The model lands AT IDENTITY where an identity exists: with `DIALS` at the identity column of the dial table, `difficultyCurve(1)` reads canon (dots 10, darkBlobs 0, darkRadius 4, foeLevel 1, foeHitScale 1, foeHpScale 1, hazardScale 1, abilityThreat 1, storeTier 0), floor 1 fights build the same foe objects as the prototype (same lvl, same wp, no dmgBonus key), and a floor-1 fixture with no failed climb/leap, no table-four HP dot, no fed night and no level-cap-limited count roll is byte-identical. What MOVES at identity is exactly and only: (1) `FOE_LEVEL` — the ONE dial with no identity (it replaces `min(c.level, depth)`; landed at the starting map `{ base: 0.6, perDepth: 0.2 }` whose map(1) = 1 keeps every floor-1 fight); (2) the level-keyed foe-count cap `c.level <= 2 ? 2 : 3` — removed (cap 3 everywhere, `FOE_COUNT_SKEW` 0 = the canon face map), so a level-≤2 count roll that canon clamped to 2 now yields 3; (3) the table-four HP dots — fractions of `c.maxWP` (mean-matched at level 1, not per-hero identity); (4) the rested-night heal — `CAMP_HEAL_FRACTION × maxWP` + the same d10 draw (mean-matched at level 1); (5) the one-and-done climb/leap rule. Each is measured, declared and regenerated — never 'expected zero'"
    - "Foe-side power keys to DEPTH: `foeLevelFor(depth) = clamp(round(FOE_LEVEL.base + FOE_LEVEL.perDepth × depth), 1, 5)` with the d4 `TIER_SPREAD` bleed kept (`rng.d(4) <= TIER_SPREAD` → one tier lower, `TIER_SPREAD` 1 = canon); `foeHitFor(raw, curve)` scales the WHOLE hit (`foeLevelBase(f) + (crit ? 2*dice : dice)`) at the hero, member and pursuit sites through ONE helper; `foeWpFor` reads `FOE_HP_SCALE`; `foeDmgBonusFor` and the `dmgBonus` key are gone (superseded by the whole-hit scale); `ROUND_DAMAGE_CEILING` (0 = off) clamps what ONE foe may deal per foeTurn visit across all its swings (frenzy and `sp.atk` included) to `round(ceiling × heroMeanMaxWpFor(c.level))`, applied after the scale and the weakened/shrunk/hamstrung halvings and before `applyFoeDamageToPlayer`; the count roll keeps the canon draw shape verbatim (`r1 <= 2 ? 1 : TABLE[FOE_COUNT_SKEW][r2 - 1]` — a second d4 only when the first is > 2)"
    - "Hero-side power keys to the hero's LEVEL: `heroMaxWpFor(rolled)` (`HERO_HP_SCALE`, chargen and every level-up gain), `heroRegenFor(maxWP)` (`HERO_REGEN_PER_FLOOR`, applied ONCE on arriving at a new floor in `descend`, hero only, event `floorRegen { amount }` pushed only when amount > 0), `heroSpFor(amount)` (`HERO_SP_SCALE` on kill SP, parley SP, the descend bonus and the +10/+25 XP dots — the table-four flat-XP narration strings state the SCALED number), `campHealFor(maxWP, d10)` (`CAMP_HEAL_FRACTION`; doublers stay multipliers), `dotHpFor(kind, maxWP)` (`DOT_HP_FRACTION { small, mid, large }` — the ±10 / −15 / +25 rows as fractions of `c.maxWP`, rounded, min 1), `startingRationsFor(canon)` (`FOOD_CLOCK`); `ENCOUNTER_DOTS { base, perDepth }` (breather floors read `base`), `HAZARD_SCALE { base, perDepth }` (floor 1 included — no `HAZARD_FROM_DEPTH`), `ABILITY_THREAT { base, perDepth }`, `DARK_BLOBS { base, perDepth }` + `DARK_BLOB_CAP`, `DARK_RADIUS { base, perDepth }` + `DARK_RADIUS_CAP`, `STORE_TIER { base, perDepth }` (exposed as `storeTier`, clamp 0..3; consumed by 54-06). `MAZE_SIZE` is CUT from this phase (user, 2026-09-21): no key, no `mazeSize` curve field, `engine/maze.js`'s `GW`/`GH` 21 untouched. `difficultyCurve(depth)` returns `{ depth, breather, dots, darkBlobs, darkRadius, waterPools, storeTier, foeLevel, foeHitScale, foeHpScale, hazardScale, abilityThreat }` — still draw-free, still the single per-depth lookup"
    - "Walls and crevices are ONE AND DONE (54-CONTEXT.md `### Bug fix folded in`): one roll per feature; success crosses clean (`climbedOver` / `leaptOver`, feat cleared); failure takes the fall damage (`scaleHazard`), pushes `fellClimbing` / `fellInGorge` with `hurt`, and — if the hero survives — pushes `draggedOver { feat }` and STILL completes the move (`there.feat = null`, position updated, the same step-cost/reveal/cadence tail as a clean step); a fatal fall still dies in place (`die(state, "fall" | "gorge")`, no move). The tool branch (`useTool`), flight and ether branches are unchanged. The `draggedOver` line is in the deadpan family-friendly voice on the Oracle, the fight-log/rail table and the rail card, BANNED-scanned like Phase 51's initiative line"
    - "`setDialsForTuning(overrides)` is the ONE harness-only hook (exported from `engine/difficulty.js`; deep-merges a partial `DIALS` shape into a module-private live copy and returns a restore function; never referenced by `src/`, `mazeworld.html` or any engine module — pinned by grep in a unit test); the shipped `DIALS` object is frozen and is what every run reads by default"
    - "Parity is MEASURED on the identity commit (`node tools/initiative-fixture-scan.mjs` Part B + `node --test test/parity/`), every mover is DECLARED with a `+54` record (`kind: \"action-path\"` with `fromAction` = the first divergent action, or a `chargenDivergence` / `divergences[seed]` field-strip record where the move is chargen-time), regenerated from engine-measured values, and the guard's `EXPECTED` is the measured list; `test/parity/prototype-master.js.txt` (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`) and `test/parity/harness/comparables.js` are never edited"
  artifacts:
    - path: "engine/difficulty.js"
      provides: "DIALS (frozen), setDialsForTuning, difficultyCurve (new shape), foeLevelFor, foeCountFor(r1, drawSecond), FOE_COUNT_TABLE, foeWpFor, foeHitFor, roundDamageCapFor, heroMeanMaxWpFor, heroMaxWpFor, heroRegenFor, heroSpFor, campHealFor, dotHpFor, startingRationsFor, scaleHazard, abilityCadenceFor, isBreather, BREATHER_EVERY, WATER_POOL_*"
    - path: "engine/combat.js + engine/character.js + engine/movement.js + engine/encounters.js + engine/maze.js"
      provides: "every consumer re-wired to the helpers; no consumer reads a removed key"
    - path: "engine/events.js + src/browser/narrationLines.js + src/browser/eventNarration.js + src/browser/rail.js"
      provides: "`floorRegen` and `draggedOver` events with in-voice lines"
    - path: "test/difficulty/difficulty.test.js + test/unit/combat-scaling.test.js + test/unit/movement.test.js + test/unit/encounters.test.js + test/unit/foe-turn-draw-count.test.js + test/determinism/foe-abilities.test.js + test/unit/one-and-done-lines.test.js"
      provides: "DIALS pins, export-key-set pin, curve pins 1..25/35/50 from node -e, helper identity fast paths, tier map + count table + ceiling + mean-HP pins, wiring pins at depths 1/5/20, draw-shape equality, one-and-done (4 branches), regen, camp heal, dot fractions, re-measured fight/draw pins"
    - path: "test/parity/fixtures/*.json + test/parity/divergence-records.test.js + test/parity/FIXTURE-INVENTORY.md + tools/initiative-fixture-scan-output.txt"
      provides: "the measured identity-commit moved set, declared and regenerated; the Phase 54 inventory section (identity subsection); the guard's measured EXPECTED"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "`#### Identity commit — commit …` (the remove list as landed, the identity column as landed, what moved at identity and why, the curve table d = 1..25)"
  key_links:
    - from: "DIALS (engine/difficulty.js)"
      to: "every consumer site named in the objective"
      via: "the exported pure helpers (no consumer reads DIALS directly except through a helper or the curve)"
      why: "54-06's fit swaps the live dial set through setDialsForTuning — a consumer that read a constant by value would not see the candidate"
    - from: "the identity commit's measured moved set"
      to: "test/parity/fixtures/*.json `+54` records + the guard's EXPECTED"
      via: "tools/initiative-fixture-scan.mjs Part B first-divergent-action + the suite's own failure output"
      why: "the engine gate under Ruling D is 'everything that moves is declared', not 'nothing moves'"
  prohibitions:
    - "never edit test/parity/prototype-master.js.txt or test/parity/harness/comparables.js; never edit content/bestiary.js (no per-monster retune, no tier move)"
    - "never a floor-range constant or helper (no FROM_DEPTH / TO_DEPTH / THROUGH_DEPTH / AT_N / SOFT_K / GRACE / KNOT names; no depth ternaries in a dial's evaluation other than the breather rhythm and the clamps)"
    - "never an undeclared fixture move; never hand-type a pin (every literal from node -e / the test's own failure output); never loosen a pin"
    - "no draw added to difficultyCurve (arity 1; zero rng); the foe-count roll keeps the canon draw shape (a second d4 only when the first is > 2)"
    - "never touch tools/lib/tuning-bot.mjs's policy (54-04 was the one bot commit)"
    - "no new persisted state key (regen and HP scaling touch c.wp / c.maxWP only); never a MAZE_SIZE dial or a grid-size change (cut from this phase; GW/GH stay 21)"
    - "never a race- or sub-class-keyed difficulty constant"
---

<objective>
Replace the floor-range knot table with the GLOBAL DIFFICULTY MODEL of USER RULING D — a single frozen `DIALS` object of dungeon-wide dials (each one number or `{ base, perDepth }`), evaluated per depth by `difficultyCurve` and applied through pure helpers at every consumer — landed AT IDENTITY where an identity exists, with the one-and-done climb/leap rule folded in, every test re-pinned from measured output, and parity measured, declared and regenerated for exactly what moves at identity.

Governing principle (verbatim, from the coordinator): FOE-side power keys to DEPTH; HERO-side power keys to the hero's LEVEL (paced by HERO_SP_SCALE); the ECONOMY keys to depth through foe tier (LOOT_SCALE); the fit sets the gap between the ramps. This plan lands the foe side, the hero HP/regen/SP/camp side, the dots/hazards/food/dark/maze-size/store-tier curve fields and the climb rule; 54-06 lands the economy (LOOT_SCALE, STORE_TIER's store consumer), CLASS_MITIGATION, FOE_ACCURACY, DOT_MIX/FIGHT_SHARE, WANDER_RATE, the exposed-at-canon dials and the fit tool (MAZE_SIZE is cut from the phase); 54-07 fits the CORE 10 coordinates (every other dial held at its start value) against floors 1–12 and measures the tail.

## The remove list (every name goes; history stays in the ledger)
`engine/difficulty.js` constants: `COMBAT_SCALE_FROM_DEPTH`, `FOE_CAP_BASE`, `FOE_CAP_MAX`, `FOE_CAP_SOFT_K`, `FOE_POWER_BASE`, `FOE_POWER_MAX`, `FOE_POWER_SOFT_K`, `ABILITY_THREAT_BASE`, `ABILITY_THREAT_MAX`, `ABILITY_THREAT_SOFT_K`, `FOE_LVL_BIAS`, `FOE_GRACE_AT_1`, `FOE_GRACE_AT_2`, `FOE_GRACE_AT_3`, `FOE_GRACE_AT_4`, `HAZARD_FROM_DEPTH`, `HAZARD_SCALE_AT_START`, `HAZARD_SCALE_AT_3`, `HAZARD_SCALE_AT_4`, `WALL_FROM_DEPTH`, `WALL_TO_DEPTH`, `WALL_FOE_POWER_AT_START`, `WALL_FOE_POWER_AT_END`, `BREAKAWAY_FROM_DEPTH`, `BREAKAWAY_TO_DEPTH`, `BREAKAWAY_FOE_POWER_AT_START`, `BREAKAWAY_FOE_POWER_AT_END`, `ENDGAME_FROM_DEPTH`, `ENDGAME_TO_DEPTH`, `ENDGAME_FOE_POWER_AT_START`, `ENDGAME_FOE_POWER_AT_END`, `WALL_HAZARD_SCALE`, `BREAKAWAY_HAZARD_SCALE`, `ENDGAME_HAZARD_SCALE`, `WALL_ABILITY_THREAT_AT_START`, `WALL_ABILITY_THREAT_AT_END`, `BREAKAWAY_ABILITY_THREAT_AT_START`, `BREAKAWAY_ABILITY_THREAT_AT_END`, `ENDGAME_ABILITY_THREAT_AT_START`, `ENDGAME_ABILITY_THREAT_AT_END`, `ENCOUNTER_DOT_BASE`, `ENCOUNTER_DOT_CAP`, `ENCOUNTER_DOT_SOFT_K`, `DENSITY_CANON_THROUGH_DEPTH`, `DARK_HOLD_THROUGH_DEPTH`, `DARK_RADIUS_BASE` (→ `DIALS.DARK_RADIUS.base`), `DARK_BLOB_CAP` / `DARK_RADIUS_CAP` (→ `DIALS.DARK_BLOB_CAP` / `DIALS.DARK_RADIUS_CAP` — one-number caps, kept as dials). Helpers: `softCap`, `softCapFloat`, `bandLerp`, `knotFoePowerFor`, `knotHazardFor`, `knotAbilityThreatFor`, `foeDmgBonusFor`, the old `foeCountFor(canonCount, curve)` signature. Curve keys: `foeCap`, `foeBonus`, `foeLvlBias`, `foePower`. `engine/combat.js`: `Math.min(c.level, state.floor.depth)`, the level-keyed `cap = c.level <= 2 ? 2 : 3`, the `dmgBonus` key and `(f.dmgBonus || 0)` at the three damage sites. Kept: `BREATHER_EVERY` (rhythm, not difficulty), `WATER_POOL_*`, `isBreather`, `safeDepth`, `scaleHazard`, `abilityCadenceFor`, `foeWpFor` (re-keyed), `foeLevelBase` (combat.js).

## The identity column (landed in this plan)
`FOE_LEVEL { base: 0.6, perDepth: 0.2 }` (no identity exists — the starting map; map(1..25) = `1111222223333344444555555`), `TIER_SPREAD 1`, `FOE_HIT_SCALE { base: 1, perDepth: 0 }`, `FOE_HP_SCALE { base: 1, perDepth: 0 }`, `FOE_COUNT_SKEW 0`, `ROUND_DAMAGE_CEILING 0` (off), `ABILITY_THREAT { base: 1, perDepth: 0 }`, `HERO_HP_SCALE 1`, `HERO_REGEN_PER_FLOOR 0`, `HERO_SP_SCALE 1`, `CAMP_HEAL_FRACTION 0.17` (no identity — mean-matched: level-1 mean maxWP 41.67 → round(0.17 × 41.67) + d10 − 5 has mean 7.6 vs canon d10 + 2·1 = 7.5), `DOT_HP_FRACTION { small: 0.24, mid: 0.36, large: 0.6 }` (mean-matched: 10 / 15 / 25 ÷ 41.67), `FOOD_CLOCK 1`, `ENCOUNTER_DOTS { base: 9, perDepth: 1 }` (= canon `9 + depth`, no cap), `HAZARD_SCALE { base: 1, perDepth: 0 }`, `DARK_BLOBS { base: -0.4, perDepth: 0.7 }` + `DARK_BLOB_CAP 3` (map 1..12 = `012233333333` — floors 1, 2, 4, 5+ as today; floor 3 reads 2 where the retired hold read 1; not fixture-exposed), `DARK_RADIUS { base: 3, perDepth: 1 }` + `DARK_RADIUS_CAP 7` (= today exactly), `STORE_TIER { base: 0, perDepth: 0.3 }` (reproduces today's BAG_FLOORS ladder exactly: tier map 1..12 = `011122223333`). The 54-06 dials (`LOOT_SCALE`, `FOE_ACCURACY`, `DOT_MIX`, `WANDER_RATE`, `FLEE_NEED_MOD`, `PARLEY_NEED_MOD`, `STARTING_GOLD`, `STARTING_POTION_BONUS`, `CLASS_MITIGATION`) are ADDED to `DIALS` here at their identity values (1 / 0 / all 1.0 / 1 / 0 / 0 / 50 / 0 / identity rows) so the object's key set is complete from this commit; their consumer hooks land in 54-06.

Purpose: BAND-02 under USER RULING D — "Remove one-off hacks and bandaids that try to get bands by floor ranges, and establish dials that work on the dungeon as a whole"; "foe level is derived from DEPTH, not the hero's level; hero-side tuning (HP, regen) is in scope". The Ruling C target curve and pass bands STAND (the fit is 54-07's).

Output: the rewired engine at identity, the new events and lines, every test re-pinned, the identity-commit parity set declared + regenerated, `FIXTURE-INVENTORY.md`'s Phase 54 section (identity subsection), the ledger's `#### Identity commit` section, `54-05-SUMMARY.md`.

Schema gate: no new persisted key. `HERO_HP_SCALE` touches `c.maxWP`/`c.wp` at chargen and level-up only; `HERO_REGEN_PER_FLOOR` touches `c.wp` at floor arrival only (no "regen applied" marker — `descend` is the only arrival path and runs once per floor by construction; a loaded save resumes on its current floor with no regen owed); `c.rations` at chargen only; the maze grid stays 21×21 (MAZE_SIZE cut). Old saves tolerant-load: canon HP, no new keys required.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-CONTEXT.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-04-SUMMARY.md
@.planning/phases/52-foe-cadence-damage-curve/52-02-SUMMARY.md
@.planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md
</context>

<ground_rules>
- `PHASE_DIR=.planning/phases/54-four-band-retune-and-roster-decision`; `SCRATCH` = the session scratchpad. Entry: HEAD = 54-04's last commit (`git log -1 --format=%s` starts with `docs(54-04)`), `git status --porcelain` empty. Record `PRE_MODEL=$(git rev-parse HEAD)`.
- Commits: plain `git commit -F "$SCRATCH/msg.txt"`; every message ends with exactly the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Skip `state.advance-plan`.
- Gates at every commit: `npm test` (fail 0) and `npm run build:www` (exit 0); `boot:check` is NOT a gate; `node --test` always with explicit file paths. Master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every commit. `git diff --stat $PRE_MODEL -- content/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js test/parity/prototype-master.js.txt` empty at every commit.
- Tasks 1 and 2 may land as ONE commit (the parity suite is red between the engine edit and the declared records — Phase 51-02's precedent); Task 3 is its own docs commit.
- Every pin is pasted from `node -e` output or the failing test's own message — never hand-computed. A test failure NOT explained by a change in this plan's objective is a STOP (write it into the SUMMARY's Issues section; do not paper over).
- Comment-text discipline: the removed constant names appear in this file's objective and in the ledger; in `engine/difficulty.js` they may appear ONLY inside the single history paragraph of the module header (the export-key-set pin, not a grep, is the gate).
- No device / human checkpoints; the SUMMARY ends with `## Human verification (deferred to end of run)`.
</ground_rules>

<tasks>
<!-- planner-discipline-allow: dmgBonus -->
<!-- planner-discipline-allow: foeHitFor( -->
<!-- planner-discipline-allow: roundDamageCapFor( -->
<!-- planner-discipline-allow: foeCountFor(rng.d(4), () => rng.d(4)) -->

<task type="auto">
  <name>Task 1: The global model in the engine at identity — DIALS + setDialsForTuning + the new difficultyCurve and helpers in engine/difficulty.js (remove list applied); combat.js (foe level from depth, count table with the canon draw shape, whole-hit scale at the three sites, round-damage ceiling, dmgBonus gone); character.js (hero HP scale, rations); movement.js (floor regen, camp heal fraction, ONE-AND-DONE climb/leap); encounters.js (dot HP fractions, XP dots); SP sites; maze.js; the two new events + in-voice lines; every test re-pinned from measured output</name>
  <files>engine/difficulty.js, engine/combat.js, engine/character.js, engine/movement.js, engine/encounters.js, engine/maze.js, engine/events.js, src/browser/narrationLines.js, src/browser/eventNarration.js, src/browser/rail.js, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/unit/movement.test.js, test/unit/encounters.test.js, test/unit/foe-turn-draw-count.test.js, test/unit/one-and-done-lines.test.js, test/unit/narrationLinesCoverage.test.js, test/determinism/foe-abilities.test.js</files>
  <read_first>
    - engine/difficulty.js WHOLE FILE (852 lines: the module header's history paragraphs — keep ONE condensed history paragraph naming Phases 21/27/54 and the retired names; every constant in the remove list; `safeDepth`, `isBreatherOfSafeDepth`, `isBreather`, `difficultyCurve` lines 730-780, the application helpers lines 795-852)
    - engine/combat.js lines 218-300 (`startCombat`: `maxLvl` line 241, `cap` line 243, the d4 ternary line 254 with its NOTE on the short-circuit shape, the foe object literal lines 258-294 with the `dmgBonus` spread), lines 899-915 (`foeLevelBase`), 932-980 (`pursuitStrike` — the damage line 975), 2398-2530 (the `swings` loop line 2400 and the member damage line 2494), 2570-2600 (the hero damage line 2586 → `applyFoeDamageToPlayer`), lines 838-862 (`killFoe`'s `heroShare`), 1225-1236 (parley's `c.sp += sp`)
    - engine/character.js lines 487-545 (`rollCharacter`: `maxWP` line 511-512, `rations` line 542) and 607-620 (`checkLevel`: `gain`/`add` lines 613-616)
    - engine/movement.js lines 170-310 (the climb/gorge block: the `if (!ok) { … return events; }` early return at ~296-302 is what goes; the clean-step tail from line 310 that BOTH branches must now reach), 619-680 (`newDay`: `heal = rng.d(10) + 2 * c.level` line 672 and the doublers), 940-978 (`descend`: the SP bonus line 955, `genFloor`, `floorChanged` — the regen goes after `floorChanged` and before `cutthroatMurderCheck`)
    - engine/encounters.js lines 258-320 (`tableFour`: the `+10 HP` / `-10 HP` / `+10 XP` / `+25 HP` / `+25 XP` / `-15 HP` cases and their narration strings), lines 80-102 (`springTrap`'s `scaleHazard` — unchanged, floor 1 now scaled too by the dial)
    - engine/maze.js lines 100-130 and 300-340 (the `curve.dots` / `curve.darkBlobs` / `curve.darkRadius` consumers — key names unchanged; confirm no consumer reads `foeCap`/`foePower`/`foeLvlBias`/`foeBonus`: `grep -rn "foeCap\|foePower\|foeLvlBias\|foeBonus\|dmgBonus\|foeDmgBonusFor" engine/ src/ tools/ test/ --include=*.js --include=*.mjs`)
    - engine/events.js lines 1-40 (`EVENT_TYPES` + the builder pattern; `floorChanged`), src/browser/narrationLines.js lines 160-240 (`ORACLE_ONLY`) and 973-1000 (`LINE_FOR.climbedOver` / `fellClimbing` / `fellInGorge` — the voice register), src/browser/eventNarration.js lines 120-145, src/browser/rail.js lines 165-180, test/unit/initiative-line.test.js lines 1-60 (the BANNED-scan pattern to copy), test/unit/narrationLinesCoverage.test.js (every engine event type needs a LINE_FOR builder or an ORACLE_ONLY entry)
    - content/classes.js (baseWP / gain dice — `heroMeanMaxWpFor` derives the class-mean bars from these; tools/damage-curve-audit.mjs lines 118-140 `classBarAt` is the same arithmetic), content/misc-tables.js (`THRESHOLDS` untouched — pace is scaled at the SP-gain sites, never the thresholds, so src/browser/heroTab.js's next-level display stays canon)
    - test/difficulty/difficulty.test.js WHOLE FILE, test/unit/combat-scaling.test.js lines 174-260 (`PHASE_27_PINS`, `PHASE_54_PINS`) and every test name listed in its file map (the whole file is rewritten to the model), test/unit/movement.test.js lines 231-320 (the climb/gorge tests) and 700-740 (descend), test/unit/encounters.test.js lines 95-160, test/unit/foe-turn-draw-count.test.js (the FULL_FIGHTS totals — re-measured, never loosened), test/determinism/foe-abilities.test.js lines 75-125 (the tier-forcing `state.c.level = state.floor.depth = tier` — under foe-level-from-depth the test must set `state.floor.depth` to the FIRST depth whose `foeLevelFor(d) === tier`, computed from the live map, and `state.c.level` stays `tier`)
  </read_first>
  <action>
    (A) `engine/difficulty.js` — rewrite. Keep the file's role statement (pure, draw-free, DOM-free) and ONE condensed history paragraph ("Phase 21 / 27 dials, the Phase 54 knot ladder (rungs 1–5, commits b0facb6…1cb56c6) and their constants are retired by USER RULING D — history in docs/DIFFICULTY-RETUNE.md's change table"). Then:
    - `export const DIALS = Object.freeze({ … })` with EVERY key in the identity column of the objective (including the 54-06 keys at identity: `LOOT_SCALE: 1`, `FOE_ACCURACY: 0`, `DOT_MIX: { fight: 1, harm: 1, loot: 1, help: 1 }`, `WANDER_RATE: 1`, `FLEE_NEED_MOD: 0`, `PARLEY_NEED_MOD: 0`, `STARTING_GOLD: 50`, `STARTING_POTION_BONUS: 0`, `CLASS_MITIGATION: { Fighter: { hpMul: 1, armorMul: 1, killSpeed: 1 }, Thief: { evasion: 0, fleeBonus: FLEE_THIEF_BONUS, trapAvoid: 0, killSpeed: 1 }, "Magic User": { spellPower: 1 } }` — import `FLEE_THIEF_BONUS` from `../content/flee.js`); nested objects frozen too (`deepFreeze`). Each key carries a JSDoc: hook, identity value, direction (↑ = harder / easier), `DELIBERATE RULES CHANGE (Phase 54, BAND-02, 2026-09-21, USER RULING D)`.
    - a module-private `let live = DIALS;` and `export function setDialsForTuning(overrides)` — HARNESS-ONLY (JSDoc says so; precedent: `forceParty`): deep-merges `overrides` over `DIALS` into a NEW frozen object assigned to `live`, returns `() => { live = DIALS; }`; throws on an unknown key (typo guard). Every helper and `difficultyCurve` read `live`.
    - `export function foeLevelFor(depth)` → `clamp(Math.round(live.FOE_LEVEL.base + live.FOE_LEVEL.perDepth * safeDepth(depth)), 1, 5)`.
    - `export const FOE_COUNT_TABLE = Object.freeze([[2,2,2,3],[1,2,2,3],[1,2,2,2],[1,1,2,2],[1,1,1,2]])` (index = `FOE_COUNT_SKEW`; row = the SECOND d4's face → foe count; row 0 is canon: P(1,2,3) = .500/.375/.125) and `export function foeCountFor(firstRoll, drawSecond)` → `firstRoll <= 2 ? 1 : FOE_COUNT_TABLE[live.FOE_COUNT_SKEW][drawSecond() - 1]` (the second draw happens ONLY when the first is > 2 — the canon short-circuit, verbatim).
    - `export function foeWpFor(baseWp, curve)` → `curve.foeHpScale === 1 ? baseWp : Math.max(1, Math.round(baseWp * curve.foeHpScale))`; `export function foeHitFor(raw, curve)` → `curve.foeHitScale === 1 ? raw : Math.max(1, Math.round(raw * curve.foeHitScale))`.
    - `export function heroMeanMaxWpFor(level)` → the unweighted mean over the three CLASSES of `(baseWP.base + meanDice(baseWP.dice) + Σ_{k=1}^{level-1} meanDice(gain[k])) × live.HERO_HP_SCALE` (`meanDice(d) = d.n * (d.sides + 1) / 2 + d.bonus`); pinned values at HERO_HP_SCALE 1: 41.67 / 46.17 / 50.00 / 54.50 / 60.00 for levels 1..5 (2 dp). `export function roundDamageCapFor(level)` → `live.ROUND_DAMAGE_CEILING > 0 ? Math.max(1, Math.round(live.ROUND_DAMAGE_CEILING * heroMeanMaxWpFor(level))) : Infinity`.
    - `export function heroMaxWpFor(rolled, cls)` → `const m = live.HERO_HP_SCALE * ((live.CLASS_MITIGATION[cls] && live.CLASS_MITIGATION[cls].hpMul) || 1); return m === 1 ? rolled : Math.max(1, Math.round(rolled * m));` (used for the chargen roll AND each level-up gain); `export function heroRegenFor(maxWP)` → `live.HERO_REGEN_PER_FLOOR === 0 ? 0 : Math.max(0, Math.round(live.HERO_REGEN_PER_FLOOR * maxWP))`; `export function heroSpFor(amount)` → `live.HERO_SP_SCALE === 1 ? amount : Math.round(amount * live.HERO_SP_SCALE)`; `export function campHealFor(maxWP, d10)` → `Math.max(1, Math.round(live.CAMP_HEAL_FRACTION * maxWP) + d10 - 5)`; `export function dotHpFor(kind, maxWP)` → `Math.max(1, Math.round(live.DOT_HP_FRACTION[kind] * maxWP))` (`kind` ∈ small | mid | large); `export function startingRationsFor(canon)` → `live.FOOD_CLOCK === 1 ? canon : Math.max(1, Math.round(canon * live.FOOD_CLOCK))`.
    - `difficultyCurve(depth)`: `d = safeDepth(depth)`, `breather`, `dots = breather ? live.ENCOUNTER_DOTS.base : Math.max(1, Math.round(live.ENCOUNTER_DOTS.base + live.ENCOUNTER_DOTS.perDepth * d))`, `darkBlobs = breather ? 0 : clamp(Math.round(live.DARK_BLOBS.base + live.DARK_BLOBS.perDepth * d), 0, live.DARK_BLOB_CAP)`, `darkRadius = Math.min(Math.round(live.DARK_RADIUS.base + live.DARK_RADIUS.perDepth * d), live.DARK_RADIUS_CAP)`, `waterPools` (unchanged formula), `storeTier = clamp(Math.round(live.STORE_TIER.base + live.STORE_TIER.perDepth * d), 0, 3)`, `foeLevel = foeLevelFor(d)`, `foeHitScale = live.FOE_HIT_SCALE.base + live.FOE_HIT_SCALE.perDepth * d` (when `perDepth === 0` return `base` exactly — no arithmetic drift; same for the other slopes), `foeHpScale`, `hazardScale`, `abilityThreat` likewise. Return the 12-key object in the objective's order (no `mazeSize` — MAZE_SIZE is cut). `scaleHazard`, `abilityCadenceFor`, `isBreather`, `BREATHER_EVERY`, `WATER_POOL_*` unchanged.

    (B) `engine/combat.js` `startCombat`: `const maxLvl = curve.foeLevel;` (the JSDoc: `DELIBERATE RULES CHANGE (Phase 54, BAND-02, 2026-09-21, USER RULING D): foe level is a function of DEPTH (foeLevelFor), never of the hero's level — out-leveling the dungeon is how a strong run breaks away`); the `cap` line and its comment removed; `const n = wandering ? 1 : foeCountFor(rng.d(4), () => rng.d(4));` (keep the NOTE about the short-circuit; the second d4 is drawn inside the thunk only when the first is > 2 — same draw count and position as the ternary); the bleed line `clamp(maxLvl - (rng.d(4) <= liveTierSpread ? 1 : 0), 1, 5)` — read `TIER_SPREAD` through a new exported `tierSpreadFor()` helper (returns `live.TIER_SPREAD`) so 54-06 can expose it; `wp = foeWpFor(picked.wp, curve)`; delete `dmgBonus` and its spread. The three damage sites: hero `let dmg = foeHitFor(foeLevelBase(f) + (crit ? 2 * dice : dice), curve)` → the existing weakened/shrunk/hamstrung halvings → `dmg = Math.min(dmg, Math.max(0, capLeft))` where `capLeft = roundDamageCapFor(c.level) - dealtThisVisit` (a `let dealtThisVisit = 0` declared right before the `swings` loop, incremented by the pre-pipeline `dmg` after each landed swing on hero OR member — one budget per foe per visit, frenzy and `sp.atk` included); member `mDmg` the same helper chain with the same budget; `pursuitStrike` the same helper with its own fresh budget (one strike). `curve` = `difficultyCurve(state.floor.depth)` read once per foeTurn (0 draws). `killFoe`: `c.sp += heroSpFor(heroShare)` and the `foeKilled` event's `spGained` = the scaled number; parley: `c.sp += heroSpFor(sp)` (and its event amount). Update every `Phase 21 (D-02)` comment at these sites to name the new helper.

    (C) `engine/character.js`: `rollCharacter` — after the race `wpMul` line: `maxWP = heroMaxWpFor(maxWP, cls)`; `rations: startingRationsFor(cls === "Fighter" ? 6 : cls === "Thief" ? 5 : 4)`; `checkLevel` — `const add = heroMaxWpFor(R.wpMul ? Math.round(gain * R.wpMul) : gain, c.cls)`. Import the helpers from `./difficulty.js` (character.js must not create a cycle — difficulty.js imports content only; verify with `node -e "import('./engine/character.js')"`).

    (D) `engine/movement.js`: `descend` — `const bonus = heroSpFor(40 + 30 * safeFloorDepth)`; after `events.push(floorChanged(...))`: `const regen = heroRegenFor(state.c.maxWP); if (regen > 0) { const before = state.c.wp; state.c.wp = Math.min(state.c.maxWP, state.c.wp + regen); if (state.c.wp > before) events.push(floorRegen(state.c.wp - before)); }` (0 draws; no event at identity). `newDay` — `let heal = campHealFor(c.maxWP, rng.d(10));` (the SAME d10 draw in the SAME position; the `2 * c.level` term is gone; doublers unchanged; JSDoc: `DELIBERATE RULES CHANGE (Phase 54, USER RULING D): a rested night heals a fraction of maxWP (CAMP_HEAL_FRACTION) — hero-keyed, no depth term; the d10 stays as variance so the draw count is unchanged`). ONE-AND-DONE: in the climb/gorge block replace the `if (!ok) { …; return events; }` early return with: `if (!ok) { hurt = scaleHazard(hurt, difficultyCurve(state.floor.depth)); state.c.wp -= hurt; events.push({ type: climbing ? "fellClimbing" : "fellInGorge", hurt }); if (state.c.wp <= 0) { die(state, climbing ? "fall" : "gorge", null, rng, events, now); return events; } events.push({ type: "draggedOver", feat: there.feat }); } else { events.push({ type: climbing ? "climbedOver" : "leaptOver" }); } there.feat = null;` and let execution FALL THROUGH to the clean-step tail (cost / position / `pendingHazard = null` / steps / reveal / `moved` / cadence) exactly as a clean step does. JSDoc on the block: `DELIBERATE RULES CHANGE (Phase 54, 2026-09-21, user bug-fix ruling): walls and crevices are ONE AND DONE — one roll per feature; a failed roll hurts and STILL crosses (the retry-and-hurt-again loop is gone; the heights-phobia repeat-fall spiral with it)`. Draw count per attempt unchanged (same rolls, same order).

    (E) `engine/encounters.js` `tableFour`: `+10 HP` → `const n = dotHpFor("small", c.maxWP); c.wp = Math.min(c.maxWP, c.wp + n);` with the result string reading the number (`… gives something back. ${n} hp.`); `-10 HP` → `c.wp -= dotHpFor("small", c.maxWP)` (string names `n`); `-15 HP` → `mid`; `+25 HP` → `const n = dotHpFor("large", c.maxWP); c.maxWP += n; c.wp += n;` (string names `n`); `+10 XP` → `c.sp += heroSpFor(10)` (string names the scaled amount), `+25 XP` likewise. JSDoc: `DELIBERATE RULES CHANGE (Phase 54, USER RULING D): the flat HP dots are fractions of the hero's maxWP (DOT_HP_FRACTION) so a dot is the same risk on every floor; the XP dots ride HERO_SP_SCALE like kills`. No other row changes (Food / Ailment / Insanity / Phobia / Darkness / Teleport / Joiner / wilmst cache untouched here — the cache and chest ride LOOT_SCALE in 54-06).

    (F) `engine/maze.js`: consumers of `curve.dots` / `darkBlobs` / `darkRadius` unchanged (keys kept); `GW`/`GH` 21 untouched (MAZE_SIZE is cut from the phase). Confirm by grep that nothing in `engine/`, `src/`, `tools/`, `test/` still imports a removed name (`grep -rn "FOE_GRACE\|WALL_\|BREAKAWAY_\|ENDGAME_\|COMBAT_SCALE_FROM_DEPTH\|ENCOUNTER_DOT_\|DENSITY_CANON\|DARK_HOLD\|HAZARD_FROM_DEPTH\|HAZARD_SCALE_AT\|FOE_CAP_\|FOE_POWER_\|ABILITY_THREAT_\|FOE_LVL_BIAS\|foeDmgBonusFor\|knot" engine/ src/ tools/ test/ --include=*.js --include=*.mjs | grep -v "^test/parity/prototype-master"` → only comments/history lines, which you then rewrite to name the successor; the ledger and the fixtures' `_note` strings are the only allowed survivors).

    (G) Events + lines: `engine/events.js` — `FLOOR_REGEN: "floorRegen"` + `floorRegen(amount)` builder; `draggedOver` pushed inline in movement.js (like `climbedOver`). `src/browser/narrationLines.js` `LINE_FOR.floorRegen` (tone "hit", `priority: PRIORITY.other`, e.g. `A new floor, and the dungeon lets you keep +${amount} hp of it. Do not mistake this for kindness.`) and `LINE_FOR.draggedOver` (tone "hurt", e.g. climb: `Over, eventually. The wall took its cut on the way.`; gorge: `Across, technically. The crevice kept your dignity as a toll.` — choose by `e.feat`); `src/browser/eventNarration.js` the Oracle twins; `src/browser/rail.js` a `draggedOver` card entry (`{ icon: "⧗", title: "OVER, BARELY", tone: "warn" }` — match the existing `climbedOver` entry's shape) and `floorRegen` toast-only (no card — minor event; check how `rail.js` treats a type absent from its table and mirror `rested`'s treatment). New `test/unit/one-and-done-lines.test.js` (copy initiative-line.test.js's BANNED scan): both lines render for both feats, no BANNED term, `narrationLinesCoverage` passes.

    (H) Tests — rewrite/re-pin (paste every literal): `test/difficulty/difficulty.test.js` → `USER RULING D: DIALS is frozen and its key set is exactly [...]` (the sorted key list), `USER RULING D: the module's export key set contains no floor-range name` (regex in the truths), `identity column: difficultyCurve(1..25, 35, 50) deepStrictEqual to the pasted node -e capture` (`CURVE_PINS` regenerated), `foeLevelFor map 1..25 === "1111222223333344444555555"`, `FOE_COUNT_TABLE rows are non-decreasing in count and row 0 is canon`, `heroMeanMaxWpFor(1..5) === [41.67, 46.17, 50, 54.5, 60]` (2 dp), `roundDamageCapFor is Infinity at ceiling 0 and round(0.5 × 41.67) = 21 at level 1 under setDialsForTuning({ ROUND_DAMAGE_CEILING: 0.5 }) — restored after`, `setDialsForTuning throws on an unknown key and restore() returns live to DIALS`, `every helper's identity fast path returns its input by identity (foeWpFor / foeHitFor / heroMaxWpFor / heroSpFor / startingRationsFor / scaleHazard)`, `difficultyCurve stays draw-free` (kept), `breather / non-finite tolerance` (kept, re-pinned), `MAZE_SIZE is absent from DIALS and mazeSize absent from the curve (cut from Phase 54)`, `storeTier map 1..12 === "011122223333"`, `darkBlobs map 1..12 === "012233333333"`, `dots(d) === 9 + d for d in 1..5 and 9 on breathers`. `test/unit/combat-scaling.test.js` → rewritten: `DIALS pins match the ledger's dial table`, `wiring at depth 1: every foe built by startCombat has lvl === foeLevelFor(1) (or one lower on the bleed), wp === row.wp, NO dmgBonus key`, `wiring at depth 5 / 20: lvl === curve.foeLevel or curve.foeLevel - 1; wp === foeWpFor(row.wp, curve)`, `draw-shape equality: startCombat+fight draws N at depth 1 and at depth 20 (measured; the count roll draws 1 or 2 d4 exactly as canon)`, `the level-keyed cap is gone: a level-1 hero whose count rolls (r1 > 2, r2 = 4) faces 3 foes`, `foeHitFor is applied to the whole hit incl. the crit at the hero site (synthetic curve foeHitScale 0.5 → a 25 + 2×6 = 37 Herman crit lands as 19 before the pipeline)`, `round-damage ceiling: with ROUND_DAMAGE_CEILING 0.5 a three-swing Dante's second and third swings are clamped so the visit's pre-pipeline total <= 21 at level 1`, `pursuitStrike uses the same helper`, `abilityCadenceFor / tickAbilityCooldowns identity at abilityThreat 1` (kept). `test/unit/movement.test.js`: replace the four climb/gorge tests with `one-and-done: a failed climb hurts (scaled) AND crosses — position advances, feat cleared, events [fellClimbing, draggedOver, moved…]`, `one-and-done: a failed leap hurts 2d6 AND crosses`, `a fatal fall dies in place (no move, feat kept)`, `a successful climb / leap is byte-identical to before (climbedOver / leaptOver, feat cleared)`, `newDay: a fed night heals round(0.17 × maxWP) + d10 − 5 (min 1) — the d10 is the same draw`, `descend: no floorRegen event at HERO_REGEN_PER_FLOOR 0; under setDialsForTuning({ HERO_REGEN_PER_FLOOR: 0.25 }) the hero gains round(0.25 × maxWP) capped at maxWP and the event carries the amount`, `descend: the SP bonus is heroSpFor(40 + 30·depth)`. `test/unit/encounters.test.js`: `tableFour ±HP dots are fractions of maxWP (maxWP 40 → +10 / −10 / −15 → 10 / 10 / 14; +25 → +24 maxWP)`, `+10 XP / +25 XP ride heroSpFor`, springTrap depth-1 scaled by HAZARD_SCALE (identity 1 → unchanged). `test/unit/foe-turn-draw-count.test.js`: the FULL_FIGHTS totals re-measured (the fights' shapes may move only where the count cap or foe level changed — explain each moved total in a comment). `test/determinism/foe-abilities.test.js`: replace the tier-forcing with `state.floor.depth = firstDepthOfTier(spec.tier)` (a local helper scanning `foeLevelFor(d)` for d = 1..60) and `state.c.level = spec.tier`; re-measure `FULL_FIGHT_PINS` live (comment `Phase 54 (USER RULING D): depth re-anchored to the tier's first floor; hit/hp scales identity`).

    (I) Gates: `node --test test/difficulty/difficulty.test.js test/unit/combat-scaling.test.js test/unit/movement.test.js test/unit/encounters.test.js test/unit/foe-turn-draw-count.test.js test/unit/one-and-done-lines.test.js test/unit/narrationLinesCoverage.test.js test/determinism/foe-abilities.test.js test/unit/chargen-rng-pin.test.js test/determinism/same-seed-same-result.test.js` green; `npm test` (the parity suite is expected RED here — Task 2 declares; everything else green); `build:www` exit 0. Do NOT commit yet unless the parity suite is already green (then commit `feat(54-05): …` as below and Task 2 amends nothing).
  </action>
  <verify>
    <automated>node -e "import('./engine/difficulty.js').then(m=>{const bad=Object.keys(m).filter(k=>/_(FROM|TO|THROUGH)_DEPTH$|_AT_(START|END|[0-9]+)$|SOFT_K$|GRACE|KNOT|CANON/.test(k));console.log('badKeys',JSON.stringify(bad));console.log('map',Array.from({length:25},(_,i)=>m.foeLevelFor(i+1)).join(''));console.log('c1',JSON.stringify(m.difficultyCurve(1)));console.log('frozen',Object.isFrozen(m.DIALS),typeof m.setDialsForTuning);console.log('mean',[1,2,3,4,5].map(l=>m.heroMeanMaxWpFor(l).toFixed(2)).join(' '))})" && node --test test/difficulty/difficulty.test.js test/unit/combat-scaling.test.js test/unit/movement.test.js test/unit/encounters.test.js test/unit/foe-turn-draw-count.test.js test/unit/one-and-done-lines.test.js test/unit/narrationLinesCoverage.test.js test/determinism/foe-abilities.test.js && grep -c "setDialsForTuning" src/browser/*.js mazeworld.html engine/combat.js engine/character.js engine/movement.js engine/encounters.js engine/maze.js | grep -v ':0$' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - the verify prints `badKeys []`, `map 1111222223333344444555555`, a `c1` object whose keys are exactly `depth,breather,dots,darkBlobs,darkRadius,waterPools,storeTier,foeLevel,foeHitScale,foeHpScale,hazardScale,abilityThreat` with `dots:10,darkBlobs:0,darkRadius:4,storeTier:0,foeLevel:1,foeHitScale:1,foeHpScale:1,hazardScale:1,abilityThreat:1`, `frozen true function`, `mean 41.67 46.17 50.00 54.50 60.00`; the last pipeline prints `0` (no non-harness module names the hook)
    - `grep -c "Math.min(c.level, state.floor.depth)" engine/combat.js` == 0; `grep -c "c.level <= 2 ? 2 : 3" engine/combat.js` == 0; `grep -c "dmgBonus" engine/combat.js` == 0; `grep -c "foeHitFor(" engine/combat.js` == 3; `grep -c "roundDamageCapFor(" engine/combat.js` >= 2; `grep -c "foeCountFor(rng.d(4), () => rng.d(4))" engine/combat.js` == 1
    - `grep -c "heroMaxWpFor(" engine/character.js` == 2; `grep -c "startingRationsFor(" engine/character.js` == 1; `grep -c "heroSpFor(" engine/combat.js engine/movement.js engine/encounters.js | awk -F: '{s+=$2} END {print s}'` == 5 (kill, parley, descend, +10 XP, +25 XP)
    - `grep -c "campHealFor(c.maxWP, rng.d(10))" engine/movement.js` == 1; `grep -c "heroRegenFor(" engine/movement.js` == 1; `grep -c '"draggedOver"' engine/movement.js` == 1; `grep -c "return events;" engine/movement.js` is exactly one LESS than at $PRE_MODEL (the early return is gone; the fatal-fall return stays)
    - `grep -c "dotHpFor(" engine/encounters.js` == 4 and `grep -c "heroSpFor(" engine/encounters.js` == 2
    - `grep -c "draggedOver\|floorRegen" src/browser/narrationLines.js src/browser/eventNarration.js` — each file >= 2; `node --test test/unit/one-and-done-lines.test.js` fail 0 (BANNED scan included)
    - every re-pinned literal came from tool output (the SUMMARY's re-pin ledger lists file → test → old → new → source); `git diff --stat $PRE_MODEL -- content/ tools/lib/tuning-bot.mjs test/parity/harness/comparables.js test/parity/prototype-master.js.txt` empty
  </acceptance_criteria>
  <done>The global model is in the engine at identity with every consumer wired through the helpers, the one-and-done rule and the two events landed in voice, and every non-parity test re-pinned from measured output.</done>
</task>
<task type="auto">
  <name>Task 2: Parity on the identity commit — measure (scan Part B + the suite), declare every mover with a +54 record (action-path / chargenDivergence / divergences[seed] as the move demands), regenerate from engine-measured values, retire the knot-era BAND-02 guard for the USER RULING D guard (EXPECTED = the measured list), write FIXTURE-INVENTORY.md's Phase 54 section (identity subsection); the feat commit</name>
  <files>tools/initiative-fixture-scan-output.txt, test/parity/fixtures/action-script.combat.json, test/parity/fixtures/action-script.magic.json, test/parity/fixtures/action-script.movement.json, test/parity/fixtures/action-script.encounters.json, test/parity/fixtures/action-script.economy.json, test/parity/divergence-records.test.js, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - tools/initiative-fixture-scan.mjs lines 1-80 (Part A predictor — unaffected by this plan's edits ONLY where no fight's round count changes; Part B = the lockstep first-divergent-action per combat/magic site) and tools/initiative-fixture-scan-output.txt at $PRE_MODEL (the BEFORE copy — save it to `$SCRATCH/scan-before.txt` BEFORE any engine edit lands; if Task 1 is already in the tree, `git stash` is NOT allowed — use `git show $PRE_MODEL:tools/initiative-fixture-scan-output.txt`)
    - test/parity/fixtures/action-script.schema.md lines 31-140 (the three record kinds: `divergences[seed]` (chargen seed-set), `divergence { kind: "action-path", fromAction, fields, before, after, stateFields, stateBefore, stateAfter }`, `chargenDivergence`), the existing `+52` / `31+51` records in action-script.combat.json (`lose`, `lose-apprentice`, `lose-plain`, `parley`) as the shape to extend (`phase` strings are `+`-joined: append `+54`)
    - test/parity/harness/comparables.js (READ ONLY — never edited): `actionPathDivergenceOf`, `skipsByteDiffAt`, `declaredEndDiffs`, `chargenShiftOf`, `chargenShiftDiffs`
    - test/parity/divergence-records.test.js lines 185-206 (the DMG-02 guard shape) and 363-448 (the BAND-02 knot guard — RETIRED here: its WALL_FROM_DEPTH reach, the Phase-53 floor-1 literal pin, the floor-2 dots/dark canon pins all go)
    - test/parity/FIXTURE-INVENTORY.md lines 1948-2230 (the Phase 52 and Phase 53 sections — the shape: rule paragraph, predictor table, live scan, moved set, draw-count pins, byte-identical elsewhere)
    - .planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md lines 110-140 (how a mover's `after`/`stateAfter` were measured and how a flipped outcome was recorded)
  </read_first>
  <action>
    Step 1 — measure: `node tools/initiative-fixture-scan.mjs > "$SCRATCH/scan-after.txt"; diff "$SCRATCH/scan-before.txt" "$SCRATCH/scan-after.txt"`; `node --test test/parity/ 2>&1 | tee "$SCRATCH/parity-after.txt" | tail -12`; list every failing scenario/seed with its first divergent action and the differing fields (from the suite's own diff output). Classify each mover by cause: `level-cap` (a count roll canon-clamped to 2 now yields 3 — visible as an extra foe on the `encounterStarted`/combat comparable), `dot-hp` (a table-four ±HP row on an encounters scenario), `camp-heal` (a fed `newDay` in the movement/economy script), `one-and-done` (a failed climb/leap in the movement script), `foe-level` (only possible at depth ≥ 2 — expect none). A mover with NONE of these causes is a STOP (something moved that the identity column should not have moved — write it into the SUMMARY, do not declare it).

    Step 2 — declare + regenerate: for every mover add/extend the record on its holder — a combat/magic scenario gets `divergence: { kind: "action-path", phase: "<existing>+54" or "54", requirements: ["BAND-02"], fromAction: <measured first divergent index>, fields: [...], before: {...prototype end values...}, after: {...engine end values...}, stateFields/stateBefore/stateAfter when `dead`/`floor`/`combat` differ, rationale: "USER RULING D — global-difficulty (identity commit): <cause>" }`; a movement/economy script mover gets the same at its top level; a chargen-time move (none expected at identity — HERO_HP_SCALE 1, FOOD_CLOCK 1) would use `chargenDivergence`. Every `before`/`after` value is pasted from the harness's own output (`declaredEndDiffs` null on both sides after the edit — the test proves it). Never edit `actions`, `seed` or `_note` history; append to `_note` one sentence naming Phase 54's cause for that scenario.

    Step 3 — the guard: replace the `BAND-02: … zero; no replay site ever reaches WALL_FROM_DEPTH …` test with `BAND-02 (USER RULING D): the holders declaring Phase 54 are exactly the measured moved set of the identity commit; difficultyCurve(1) reads the identity column; the count roll keeps the canon draw shape` — Part (a) `EXPECTED = [<the measured holder ids, sorted>]` (non-empty is expected; if it IS empty, say so in the comment and keep the assertion); Part (b) `difficultyCurve(1)` deepStrictEqual to the identity-column floor-1 object (pasted); Part (c) a synthetic `startCombat` at depth 1 with a counting rng proves a first d4 ≤ 2 draws exactly one d4 and a first d4 > 2 draws exactly two (the canon shape), regardless of `FOE_COUNT_SKEW` (loop 0..4 via `setDialsForTuning`, restored); Part (d) `totalSites === 31` (keep the replay-site walk). Delete the imports the retired parts used (`WALL_FROM_DEPTH`, `PHASE_53_FLOOR1_CURVE`).

    Step 4 — `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt` (re-committed); `test/parity/FIXTURE-INVENTORY.md`: append at EOF `## Phase 54: the global difficulty model (USER RULING D, BAND-02) — measured set, declared per site` with `### The rule` (the model in two paragraphs: the remove list as landed, the identity column, the five things that move at identity and why), `### The predictor — identity-commit exposure per site` (31 rows: `| Fixture#site | Seed | level-cap roll? | table-four HP dot? | fed night? | failed climb/leap? | fight at depth >= 2? |` from a scratch replay — reuse the guard's `replaySiteEvents` and count `encounterStarted` foes vs the canon clamp, `tableFour` results, `rationsEaten`, `fellClimbing`/`fellInGorge`), `### The live scan (identity commit)` (the scan diff verbatim, the suite totals before/after the records), `### Moved set — declared records (identity commit)` (`MOVED SET (<n>): <holders>`; each record's measured before → after in a table; `grep -c '"BAND-02"' test/parity/fixtures/*.json`), `### Draw-count pins` (the count roll's shape proof; the climb block's draws per attempt unchanged; `newDay`'s d10 kept; `difficultyCurve` draw-free), `### Byte-identical elsewhere (identity commit)` (the non-moved sites; the chargen seed set untouched). State at the top of the section: "54-07 appends `### Fitted dials — measured set` after the fit; every record's `after` is re-measured there."

    Step 5 — gates + THE feat commit (Tasks 1 + 2 together): `npm test` fail 0; `build:www` exit 0; master hash; the prohibited-file diff empty; `node -e` prints from Task 1's verify pasted into `$SCRATCH/msg.txt`'s body. Commit `feat(54-05): the global difficulty model at identity (USER RULING D) — DIALS + setDialsForTuning; floor-range knots/ramps removed; foe level from depth (0.6 + 0.2·d), whole-hit FOE_HIT_SCALE, FOE_HP_SCALE, count table (canon draw shape, level cap gone), ROUND_DAMAGE_CEILING (off), hero HP/regen/SP/camp/dot/food helpers, ENCOUNTER_DOTS/HAZARD_SCALE/ABILITY_THREAT/DARK/STORE_TIER as globals; ONE-AND-DONE climbs/leaps + draggedOver/floorRegen lines; every pin re-measured; identity movers declared + regenerated (MOVED SET (<n>): <holders>)` with the two trailers. Record `MODEL_SHA`.
  </action>
  <verify>
    <automated>node --test test/parity/divergence-records.test.js test/parity/combat-parity.test.js test/parity/magic-parity.test.js test/parity/movement-parity.test.js test/parity/economy-parity.test.js test/parity/chargen-parity.test.js test/parity/full-suite.test.js test/parity/fixture-inventory.test.js && node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && echo SCAN-CLEAN; grep -c '^## Phase 54: the global difficulty model' test/parity/FIXTURE-INVENTORY.md; grep -o 'MOVED SET ([0-9]*)' test/parity/FIXTURE-INVENTORY.md | tail -1; git hash-object test/parity/prototype-master.js.txt; git diff --stat HEAD~1 -- test/parity/harness/comparables.js content/ tools/lib/tuning-bot.mjs | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - the parity suite prints `# fail 0`; `SCAN-CLEAN` prints; the inventory grep prints `1`; a `MOVED SET (n)` line exists and `n` equals the number of holders in the guard's `EXPECTED` and the number of `"BAND-02"`-bearing records whose `phase` contains `54` (`node -e` over the six fixture files — pasted in the SUMMARY)
    - every declared record's `rationale` starts with `USER RULING D — global-difficulty (identity commit): ` and names one of `level-cap` / `dot-hp` / `camp-heal` / `one-and-done` / `foe-level`; no record was hand-typed (the SUMMARY quotes the harness output each `after` came from)
    - `grep -c 'WALL_FROM_DEPTH\|PHASE_53_FLOOR1_CURVE' test/parity/divergence-records.test.js` == 0; the new guard's name contains `USER RULING D`; `totalSites === 31` assertion kept
    - master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; the prohibited-file diff empty; `npm test` fail 0; `build:www` exit 0; the feat commit carries the two trailers and names `MOVED SET (<n>)`
  </acceptance_criteria>
  <done>Exactly what moved at identity is measured, declared and regenerated; the guard pins the measured set; the inventory records the predictor, the scan and the records; the tree is green.</done>
</task>

<task type="auto">
  <name>Task 3: The ledger's `#### Identity commit` section (remove list as landed, identity column as landed, what moved at identity and why, the curve table d = 1..25, the re-pin ledger) + the verifier-grade SUMMARY; docs commit</name>
  <files>docs/DIFFICULTY-RETUNE.md, .planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md — the Phase 54 H3's `#### Global model (USER RULING D) — the dial table` and `#### BEFORE (global model) — commit …` sections (54-04) — the new section goes AFTER the BEFORE section and BEFORE `## v1.2 retune`
    - test/unit/difficulty-retune-ledger.test.js (the v1.2 H2 stays last)
    - this plan's objective (the remove list and the identity column) and the SUMMARY shape in 54-04-SUMMARY.md
  </read_first>
  <action>
    (1) Splice `#### Identity commit — commit <40-hex MODEL_SHA> (the global model landed at identity; USER RULING D)` with: `**Removed (every floor-range constant/helper):**` a table `| Name | Was | Replaced by |` for EVERY name in the objective's remove list; `**Landed (identity column):**` a table `| Dial | Value | Identity? | Note |` (FOE_LEVEL, CAMP_HEAL_FRACTION and DOT_HP_FRACTION marked "no identity — mean-matched / starting map"; DARK_BLOBS "floor 3 reads 2, was 1"); `**What moved at identity, and why:**` the five causes with the measured moved set per cause (from Task 2); `**Curve table:**` `| d | dots | darkBlobs | darkRadius | storeTier | foeLevel | foeHitScale | foeHpScale | hazardScale | abilityThreat |` for d = 1..25 from `node -e` (pasted); `**Re-pin ledger:**` `| File | Test | Old | New | Source |` for every re-pinned literal (difficulty / combat-scaling / movement / encounters / foe-turn-draw-count / foe-abilities / the guard); `**Draw shape:**` the three sentences (count roll canon shape; climb attempt draws unchanged; newDay d10 kept); `**Gates:**` totals.
    (2) `node --test test/unit/difficulty-retune-ledger.test.js` green; `npm test` fail 0; `build:www` exit 0; master hash. Commit `docs(54-05): ledger — identity commit (remove list as landed, identity column, what moved at identity, curve table 1..25, re-pin ledger) (USER RULING D, BAND-02)` with the two trailers.
    (3) `$PHASE_DIR/54-05-SUMMARY.md` (frontmatter as 54-04's; `requirements-completed: [BAND-02]`): `## Success criteria → proof` (ROADMAP SC2 as re-read under Ruling D: "every value keyed to a floor range removed; dials global" — the export-key-set pin + the remove table; SC5: `content/` untouched + gates), `## Remove list (as landed)`, `## Identity column (as landed)`, `## What moved at identity` (per cause: holders, before → after), `## Re-pin ledger`, `## Parity — measured set (identity commit)` (scan diff, suite totals, EXPECTED, the inventory section), `## New events and lines` (the two lines verbatim + the BANNED-scan test name), `## Gates` (npm test totals, build:www, master hash, prohibited-file diff), `## Schema` (none — the four touch points and the tolerant-load argument), `## Commits` (MODEL_SHA, the docs SHA), `## Deviations`, `## Issues Encountered` (any STOP), `## Next Phase Readiness` (54-06 wires LOOT_SCALE / STORE_TIER's consumer / CLASS_MITIGATION / FOE_ACCURACY / DOT_MIX / WANDER_RATE / exposed dials + the fit tool (MAZE_SIZE cut); `setDialsForTuning` is the fit's entry point), `## Human verification (deferred to end of run)` (Pixel 7 batch: a failed climb/leap now lands you on the far side with the `draggedOver` line; a new floor shows no regen line yet (identity); traps on floor 1 unchanged at identity).
  </action>
  <verify>
    <automated>grep -c '^#### Identity commit — commit [0-9a-f]\{40\}' docs/DIFFICULTY-RETUNE.md; node --test test/unit/difficulty-retune-ledger.test.js; test -f .planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md && grep -c '^## Human verification (deferred to end of run)' .planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md; git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - the heading grep prints `1`; the section contains a row for EVERY name in the objective's remove list (`for k in COMBAT_SCALE_FROM_DEPTH FOE_CAP_BASE FOE_CAP_MAX FOE_CAP_SOFT_K FOE_POWER_BASE FOE_POWER_MAX FOE_POWER_SOFT_K ABILITY_THREAT_BASE ABILITY_THREAT_MAX ABILITY_THREAT_SOFT_K FOE_LVL_BIAS FOE_GRACE_AT_1 FOE_GRACE_AT_2 FOE_GRACE_AT_3 FOE_GRACE_AT_4 HAZARD_FROM_DEPTH HAZARD_SCALE_AT_START HAZARD_SCALE_AT_3 HAZARD_SCALE_AT_4 WALL_FROM_DEPTH WALL_TO_DEPTH WALL_FOE_POWER_AT_START WALL_FOE_POWER_AT_END BREAKAWAY_FROM_DEPTH BREAKAWAY_TO_DEPTH BREAKAWAY_FOE_POWER_AT_START BREAKAWAY_FOE_POWER_AT_END ENDGAME_FROM_DEPTH ENDGAME_TO_DEPTH ENDGAME_FOE_POWER_AT_START ENDGAME_FOE_POWER_AT_END WALL_HAZARD_SCALE BREAKAWAY_HAZARD_SCALE ENDGAME_HAZARD_SCALE WALL_ABILITY_THREAT_AT_START WALL_ABILITY_THREAT_AT_END BREAKAWAY_ABILITY_THREAT_AT_START BREAKAWAY_ABILITY_THREAT_AT_END ENDGAME_ABILITY_THREAT_AT_START ENDGAME_ABILITY_THREAT_AT_END ENCOUNTER_DOT_BASE ENCOUNTER_DOT_CAP ENCOUNTER_DOT_SOFT_K DENSITY_CANON_THROUGH_DEPTH DARK_HOLD_THROUGH_DEPTH softCap softCapFloat bandLerp knotFoePowerFor knotHazardFor knotAbilityThreatFor foeDmgBonusFor; do grep -c "$k" docs/DIFFICULTY-RETUNE.md; done` — every count >= 1) and a 25-row curve table
    - the ledger test is green (the v1.2 H2 still last); the SUMMARY grep prints `1`; the SUMMARY names MODEL_SHA and the docs SHA and lists every re-pinned test by name
    - master hash unchanged; `npm test` fail 0; `build:www` exit 0; two trailers on the docs commit
  </acceptance_criteria>
  <done>The identity commit is recorded in the ledger with the complete remove list, the landed identity column, the measured movers and the curve table; the SUMMARY carries verifier-grade evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → engine | `setDialsForTuning` is a module-level override reachable only from Node tooling; the browser bundle never calls it |
| save file → engine | tolerant-load: old saves carry a 21×21 grid and canon HP; no new key is required |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-05-01 | Tampering | `setDialsForTuning` reachable from shipped code | medium | mitigate | exported but grep-pinned absent from `src/`, `mazeworld.html` and every other engine module; throws on unknown keys; `DIALS` frozen |
| T-54-05-02 | Tampering | a corrupted save's `floor.depth` / `c.maxWP` driving the helpers | low | mitigate | `safeDepth` clamps depth; every helper `Math.max(1, …)`; `heroRegenFor` caps at `maxWP` |
| T-54-05-03 | Repudiation | an undeclared fixture move | medium | mitigate | the guard's `EXPECTED` = the measured set; the scan output is committed; master never edited |
| T-54-SC | Tampering | npm/pip/cargo installs | high | accept | no package installs in this plan (`git diff --stat $PRE_MODEL -- package.json package-lock.json` empty) |
</threat_model>

<verification>
- `node -e` export-key scan prints `badKeys []`; the tier map, floor-1 curve, mean-HP bars and frozen `DIALS` print as pinned
- `npm test` fail 0 and `npm run build:www` exit 0 at MODEL_SHA and at the docs commit; master hash unchanged; `content/`, `comparables.js`, `tuning-bot.mjs` untouched
- the parity suite green with every mover declared; `tools/initiative-fixture-scan.mjs` output byte-identical to the committed copy
- FIXTURE-INVENTORY.md's Phase 54 section and the ledger's `#### Identity commit` section exist with the required tables
</verification>

<success_criteria>
- Every floor-range constant/helper removed (export-key-set pin); every Ruling D dial on the frozen `DIALS` object at its identity (or starting/mean-matched) value; `difficultyCurve` returns the 12-key global shape (no mazeSize — MAZE_SIZE cut), draw-free
- Foe level from depth, whole-hit scale at three sites, count table with the canon draw shape and no level cap, round-damage ceiling (off), hero HP/regen/SP/camp/dot/food helpers wired; one-and-done climb/leap with in-voice lines
- Every test re-pinned from measured output; the identity-commit moved set measured, declared, regenerated, guarded and inventoried
- Ledger `#### Identity commit` + SUMMARY with verifier-grade evidence
</success_criteria>

<artifacts_this_phase_produces>
- `engine/difficulty.js` — `DIALS`, `setDialsForTuning`, the new `difficultyCurve`, `foeLevelFor`, `FOE_COUNT_TABLE`, `foeCountFor`, `foeWpFor`, `foeHitFor`, `roundDamageCapFor`, `heroMeanMaxWpFor`, `heroMaxWpFor`, `heroRegenFor`, `heroSpFor`, `campHealFor`, `dotHpFor`, `startingRationsFor`, `tierSpreadFor`
- `engine/combat.js`, `engine/character.js`, `engine/movement.js`, `engine/encounters.js`, `engine/maze.js` — consumers rewired; one-and-done climb/leap; floor regen
- `engine/events.js`, `src/browser/narrationLines.js`, `src/browser/eventNarration.js`, `src/browser/rail.js` — `floorRegen`, `draggedOver`
- `test/difficulty/difficulty.test.js`, `test/unit/combat-scaling.test.js`, `test/unit/movement.test.js`, `test/unit/encounters.test.js`, `test/unit/foe-turn-draw-count.test.js`, `test/unit/one-and-done-lines.test.js`, `test/determinism/foe-abilities.test.js` — rewritten/re-pinned
- `test/parity/fixtures/*.json` (+54 records), `test/parity/divergence-records.test.js` (USER RULING D guard), `test/parity/FIXTURE-INVENTORY.md` (Phase 54 section), `tools/initiative-fixture-scan-output.txt`
- `docs/DIFFICULTY-RETUNE.md` — `#### Identity commit — commit …`
- `54-05-SUMMARY.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md` when done (verifier-grade; ends with `## Human verification (deferred to end of run)`).
</output>
