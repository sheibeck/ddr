---
phase: 54-four-band-retune-and-roster-decision
plan: 06
type: execute
wave: 6
depends_on: ["54-05"]
files_modified:
  - engine/difficulty.js
  - engine/combat.js
  - engine/derived.js
  - engine/character.js
  - engine/economy.js
  - engine/encounters.js
  - engine/items.js
  - engine/magic.js
  - engine/movement.js
  - tools/lib/fit-score.mjs
  - tools/fit-difficulty.mjs
  - test/difficulty/difficulty.test.js
  - test/unit/combat-scaling.test.js
  - test/unit/store-roll.test.js
  - test/unit/encounters.test.js
  - test/unit/movement.test.js
  - test/unit/magic.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/flee-retune.test.js
  - test/unit/class-mitigation.test.js
  - test/unit/dot-mix.test.js
  - test/unit/fit-score.test.js
  - test/parity/divergence-records.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - tools/initiative-fixture-scan-output.txt
  - docs/DIFFICULTY-RETUNE.md
  - .planning/phases/54-four-band-retune-and-roster-decision/fit/start.json
  - .planning/phases/54-four-band-retune-and-roster-decision/54-06-SUMMARY.md
autonomous: true
requirements: [BAND-02]
tags: [economy-dials, loot-scale, store-tier, class-mitigation, foe-accuracy, dot-mix, wander-rate, exposed-dials, fit-tool, coordinate-search, core-10, class-fairness, user-ruling-d]

must_haves:
  truths:
    - "The ECONOMY keys to depth through foe tier: `lootFor(coin)` (`LOOT_SCALE`, ↑ = easier) is applied post-draw, same draw count, at the four coin sites — the kill purse (`engine/combat.js` killFoe), the chest (`engine/encounters.js` openChest), the wilmst cache (tableFour) and the faerie's `d10 × 100` — never at the Pickpocket extra, the cutpurse ability, the grimoire flat 150, or the dev-start purse; `storeTier(depth)` (`engine/economy.js`) reads `difficultyCurve(depth).storeTier` (`STORE_TIER { base: 0, perDepth: 0.3 }` reproduces today's BAG_FLOORS ladder exactly: `011122223333` for depths 1..12); `BAG_FLOORS` itself is untouched (bags are not a difficulty dial); the store's draw count per tier is re-pinned from measured output"
    - "Class identity is Fighter = ABSORB, Thief = AVOID, Magic User = CHOOSE, expressed as `DIALS.CLASS_MITIGATION` (three rows, ever — never a race or sub-class row): `Fighter { hpMul, armorMul, killSpeed }`, `Thief { evasion, fleeBonus, trapAvoid, killSpeed }`, `\"Magic User\" { spellPower }` — all canon-identity, NOT in the automated fit, the manual knob when the fairness constraint alone blocks the best pooled candidate (max two notches per phase; the base Thief has no innate evasion today, so `evasion: -1` is the first candidate). Hooks: `evasion` and `FOE_ACCURACY` in `foeToHitVs` (and its `foeToHitBreakdown` twin, proven equal by the matrix test) for `vs === \"hero\"`; `trapAvoid` on springTrap's dodge threshold for a Thief; Thief `killSpeed` on the backstab opener's damage (`critBy === \"backstab\"`); Fighter `killSpeed` on every Fighter melee `weaponDamage`; Fighter `hpMul` inside `heroMaxWpFor`; Fighter `armorMul` on the `c.ar` `armorSoak` reads; MU `spellPower` on every offensive spell-damage site in `engine/magic.js` through one `spellDamageFor(n, c)` helper — every hook is an `=== 1` / `=== 0` structural fast path at identity"
    - "The late dials exist with the SAME draw counts as today and are exposed at canon: `DOT_MIX { fight, harm, loot, help }` (`FIGHT_SHARE` = `DOT_MIX.fight`) re-maps the d8×d10 encounter result AFTER the two rolls, deterministically, by a fixed conversion table derived from the weights (no third draw); `WANDER_RATE` is the d20 face count per hour slept that wakes you (canon 1; Bard = min(20, WANDER_RATE + 1); the eight d20 draws are unchanged); `FOE_ACCURACY` adds an integer to the foe to-hit need (clamp `Math.max(1, h)` respected); `MAZE_SIZE` is CUT from this phase (user, 2026-09-21): no dial, no engine/shell/bot grid change, `GW`/`GH` stay 21 — listed in the ledger's dial table as `available, not implemented (v1.8 candidate)`; `TIER_SPREAD` (the d4 bleed faces), `FLEE_NEED_MOD` (added to the flee need), `PARLEY_NEED_MOD` (added to the parley need), `STARTING_GOLD`, `STARTING_POTION_BONUS` (added to every class's starting potions) — available, canon"
    - "`tools/fit-difficulty.mjs` evaluates a candidate dial set (a partial `DIALS` JSON) with the fair bot's solo 200-seed run IN-PROCESS across worker threads (each worker imports the engine fresh, calls `setDialsForTuning(candidate)`, plays its seed slice through `playRun`; the seed list is `i*7919+1`, identical to tune-difficulty's), reports the per-floor S_L error vs the Ruling C curve on FLOORS 1–12 ONLY (score = Σ_{L=1..10} ((S_L − T_L)/8)² + Σ_{L=11..12} ((S_L − T_L)/3)², S_L in points, a never-reached floor counts as S_L = 0; floors 13–20 and reach-20 are NOT in the objective or the pass/fail — they are the measured tail, printed as dS-only readouts), the class-pool fairness + identity constraints from the SAME run (per class pool: |p50 − pooled p50| ≤ 1.0 floor (interpolated medians), |reach5 − pooled reach5| ≤ 12 pts, Fighter dmgTakenPerFight ≥ Thief's, Thief roundsPerFight ≤ Fighter's — a violating candidate is REJECTED: score +Infinity, reason logged), the per-class p50 / reach-5 / reach-10 columns and the pace columns, and appends every evaluation as one JSON line to a committed log; `--search` runs bounded coordinate descent over `SEARCH_PLAN` = the CORE 10 coordinates only (FOE_LEVEL.perDepth, FOE_LEVEL.base, HERO_SP_SCALE, FOE_HIT_SCALE.base, FOE_HIT_SCALE.perDepth, FOE_HP_SCALE.base, HERO_HP_SCALE, HERO_REGEN_PER_FLOOR, HAZARD_SCALE.base, ENCOUNTER_DOTS.base — that order; two passes, the second at half step) from `--start`, with a fixed `--budget` of evaluations, deterministic and resumable from the log; every other dial is HELD at its `--start` value and never probed. The class smoke (`tune-classes`, 715 runs ≈ 90 s) is NOT run per evaluation — the constraints read the solo run's class pools (≈ 67 runs per pool); the smoke runs once at BEFORE (54-04) and once at AFTER (54-07)"
    - "Parity is measured on this plan's commits (every dial at identity → expected zero NEW movers; measured with the scan + the suite, never assumed); any mover is declared + regenerated per the engine gate; the master and comparables.js are never edited"
  artifacts:
    - path: "engine/difficulty.js"
      provides: "lootFor, foeAccuracyFor, classEvasionFor, classTrapAvoidFor, classKillSpeedFor, classArmorMulFor, spellPowerFor, fleeNeedModFor, parleyNeedModFor, startingGoldFor, startingPotionsFor, wanderWakeFacesFor, remapEncounterResult + DOT_MIX_FAMILIES, the JSDoc'd DIALS rows for every 54-06 dial"
    - path: "engine/combat.js, engine/derived.js, engine/character.js, engine/economy.js, engine/encounters.js, engine/items.js, engine/magic.js, engine/movement.js"
      provides: "every hook wired (no grid change — MAZE_SIZE is cut)"
    - path: "tools/lib/fit-score.mjs + tools/fit-difficulty.mjs + test/unit/fit-score.test.js"
      provides: "scoreSurvival, classConstraints, SEARCH_PLAN, applyStep, the evaluate/search CLI with worker threads and the JSONL log"
    - path: ".planning/phases/54-four-band-retune-and-roster-decision/fit/start.json"
      provides: "the planner's starting dial set (the `Starting value` column of the dial table) as the fit's --start input"
    - path: "test/unit/class-mitigation.test.js, test/unit/dot-mix.test.js, test/unit/store-roll.test.js (re-pinned), test/unit/feedback-payload.test.js (matrix extended), test/unit/magic.test.js, test/unit/flee-retune.test.js, test/unit/encounters.test.js, test/unit/movement.test.js"
      provides: "identity fast-path pins, hook pins under setDialsForTuning, draw-count pins, the remap table's determinism"
  key_links:
    - from: "DIALS (54-05) + this plan's helpers"
      to: "tools/fit-difficulty.mjs candidates"
      via: "setDialsForTuning in each worker thread"
      why: "the fit must exercise the COMPLETE model (every dial with its hook) before 54-07 searches — a dial without a hook is a no-op coordinate that burns budget"
    - from: "the solo run's per-class pools (cls on each run record, 54-04)"
      to: "the fairness + identity constraints"
      via: "classIdentityReadout over the evaluation's results"
      why: "the constraint is class pools only (never cells); computing it from the same run costs zero extra bot time"
  prohibitions:
    - "never edit test/parity/prototype-master.js.txt or test/parity/harness/comparables.js; never edit content/bestiary.js"
    - "never a floor-range constant; never a race- or sub-class-keyed constant (CLASS_MITIGATION has three rows, ever); never tune to a single matrix cell"
    - "never a draw-shape change without a declared, measured divergence (the encounter remap, the wander wake, the accuracy term and the loot scale are all post-draw; the store's per-tier draw count is re-pinned, not loosened)"
    - "never touch tools/lib/tuning-bot.mjs (the instrument is frozen since 54-04)"
    - "never run the class smoke inside the fit loop; never change the seed list; never probe a held dial or add a coordinate to SEARCH_PLAN"
    - "never a MAZE_SIZE dial or a grid-size change (cut from this phase; GW/GH stay 21)"
    - "no new persisted key (potions/gold/rations at chargen only)"
---

<objective>
Complete the global model so the fit sees ALL of it: the economy dials (`LOOT_SCALE`, `STORE_TIER`'s store consumer), the class-identity constants (`CLASS_MITIGATION`, three rows, canon-identity, the manual knob), `FOE_ACCURACY`, the encounter-mix remap (`DOT_MIX` / `FIGHT_SHARE`), `WANDER_RATE`, and the exposed-at-canon dials (`TIER_SPREAD`, `FLEE_NEED_MOD`, `PARLEY_NEED_MOD`, `STARTING_GOLD`, `STARTING_POTION_BONUS`) — every hook an identity fast path so this plan's commits move nothing (measured) — then build `tools/fit-difficulty.mjs`: the in-process, worker-threaded, deterministic, logged evaluator and bounded coordinate search with the class-pool fairness and identity constraints, and write the fit's starting point to disk.

Governing principle (verbatim): FOE-side power keys to DEPTH; HERO-side power keys to the hero's LEVEL (paced by HERO_SP_SCALE); the ECONOMY keys to depth through foe tier (LOOT_SCALE); the fit sets the gap between the ramps. Spells (`d6 × (casterLevel − spellLevel)`) and level-pool abilities are level-keyed → no new dial (the class identity readout is the guard); potions heal to full (`amount: c.maxWP`) → scale automatically, availability rides LOOT_SCALE; GEAR is the one hero-side system that does not grow with level → `STORE_TIER` biases the store's offers toward better rows as depth rises; phobias: no dial (variance, proportional) — the readout carries afraid triggers / died afraid; the one-and-done rule (54-05) already removed the heights-phobia repeat-fall spiral.

## The dial table (the source of truth — 54-04's ledger copies it verbatim; 54-07's change table records identity → fitted)

USER CUT (2026-09-21, plan approval): the automated search covers the CORE 10 coordinates only (`Search order` 1–10 below); every other dial is IMPLEMENTED and HELD at its listed starting value ("held (available)" — still in the change table with its direction; the miss table names, per missed floor, which held dial the executor would release next — recorded, not taken); `MAZE_SIZE` is CUT from this phase entirely (no dial, no grid change, `GW`/`GH` 21 stay — "available, not implemented (v1.8 candidate)"); the fit's objective scores floors 1–12 only — floors 13–20 and reach-20 are the measured TAIL (54-07: a 1,000-seed solo + a `--start-depth=10` slice), recorded with dS only.

| Dial | Engine hook | Identity value | Starting value | Search bounds (step) | Search order | Direction (↑ =) |
|---|---|---|---|---|---|---|
| `FOE_LEVEL { base, perDepth }` | `foeLevelFor(depth)` → combat.js `maxLvl` (replaces `min(c.level, depth)`; d4 bleed kept) | none — starting map | `{ base: 0.6, perDepth: 0.2 }` (tiers 1/1/1/1 · 2×5 · 3×5 · 4×5 · 5 from 20) | perDepth [0.12, 0.30] (0.03); base [0.3, 1.0] (0.15) | 1 (perDepth), 2 (base) | harder |
| `HERO_SP_SCALE` | `heroSpFor` at kill / parley / descend bonus / ±XP dots | 1 | 0.28 (est.: level 2 by ~5, 3 by ~9, 4 by ~12, 5 by ~16 from the measured kill rate ≈ 2.5 fights/floor × 1.5 foes × 17.5 SP × tier + the 40 + 30·d descend bonus; the fit moves it) | [0.15, 0.6] (0.05) | 3 | easier |
| `FOE_HIT_SCALE { base, perDepth }` | `foeHitFor` on the WHOLE hit at hero / member / pursuit | `{ 1, 0 }` | `{ base: 0.6, perDepth: 0.02 }` (0.62 at 1 → 1.0 at 20 → 1.2 at 30) | base [0.4, 1.0] (0.08); perDepth [0, 0.05] (0.01) | 4 (base), 5 (perDepth) | harder |
| `FOE_HP_SCALE { base, perDepth }` | `foeWpFor` | `{ 1, 0 }` | `{ base: 0.8, perDepth: 0.015 }` (0.815 → 1.1 at 20) | base [0.5, 1.2] (0.1); perDepth held at 0.015 (available) | 6 (base) | harder |
| `HERO_HP_SCALE` | `heroMaxWpFor` at chargen + every level-up gain | 1 | 1.25 | [1.0, 1.8] (0.15) | 7 | easier |
| `HERO_REGEN_PER_FLOOR` | `heroRegenFor` in `descend` (hero only, once per arrival) | 0 | 0.25 | [0, 0.5] (0.1) | 8 | easier |
| `HAZARD_SCALE { base, perDepth }` | `scaleHazard` (traps, falls, leaps — floor 1 included) | `{ 1, 0 }` | `{ base: 0.5, perDepth: 0.02 }` | base [0.3, 1.0] (0.1); perDepth held at 0.02 (available) | 9 (base) | harder |
| `ENCOUNTER_DOTS { base, perDepth }` | `difficultyCurve.dots` (breather = base) | `{ 9, 1 }` (= canon 9 + d) | `{ base: 7, perDepth: 0.3 }` (7 → 13 at 20) | base [5, 10] (1); perDepth held at 0.3 (available) | 10 (base) | harder |
| `LOOT_SCALE` | `lootFor` at kill purse / chest / wilmst cache / faerie | 1 | 0.8 ("already too much money") | held (available); [0.4, 1.5] if released | — | easier |
| `CAMP_HEAL_FRACTION` | `campHealFor` in `newDay` (fed night; same d10 as variance; doublers stay) | none — 0.17 mean-matched | 0.2 | held (available); [0.15, 0.5] if released | — | easier |
| `ROUND_DAMAGE_CEILING` | `roundDamageCapFor(c.level)` — per foe per visit, all swings, post-scale, pre-pipeline | 0 (off) | 0.5 (of `heroMeanMaxWpFor(level)`: 21 at L1, 30 at L5 with HERO_HP_SCALE 1) | held (available); [0.3, 1.0] if released | — | harder |
| `FOE_COUNT_SKEW` | `FOE_COUNT_TABLE[skew]` on the second d4 (canon draw shape) | 0 (P 1/2/3 = .500/.375/.125) | 1 (.625/.250/.125) | held (available); 0..4 if released | — | easier |
| `FOOD_CLOCK` | `startingRationsFor` (chargen rations × clock; small-bag cap 10) | 1 | 1.5 (Fighter 9 / Thief 8 / MU 6) | held (available); [1.0, 1.6] if released | — | easier |
| `ABILITY_THREAT { base, perDepth }` | `abilityCadenceFor` | `{ 1, 0 }` | `{ base: 1.0, perDepth: 0 }` | held (available); base [0.6, 1.2] if released | — | harder |
| `STORE_TIER { base, perDepth }` | `difficultyCurve.storeTier` → `economy.js storeTier(depth)` (clamp 0..3) | `{ 0, 0.3 }` (= today's ladder) | `{ base: 0, perDepth: 0.3 }` | held (available); perDepth [0.15, 0.6] if released | — | easier |
| `FIGHT_SHARE` (= `DOT_MIX.fight`) | `remapEncounterResult` after the d8×d10 (same two draws) | 1.0 (= canon mix, 43/80 fights) | 1.0 | held (available); [0.6, 1.2] if released | — | harder |
| `WANDER_RATE` | `wanderWakeFacesFor(sub)` — d20 faces per hour slept that wake you | 1 | 1 | held (available); {0, 1, 2} if released | — | harder |
| `FOE_ACCURACY` | `foeAccuracyFor()` added to `foeToHitVs` need (+ breakdown) | 0 | 0 | held (available); −3..+3 if released | — | harder |
| `DARK_BLOBS { base, perDepth }` + `DARK_BLOB_CAP` | `difficultyCurve.darkBlobs` | `{ −0.4, 0.7 }` + 3 (map `012233333333`) | same | held (available); perDepth [0.3, 1.0] if released | — | harder |
| `DARK_RADIUS { base, perDepth }` + `DARK_RADIUS_CAP` | `difficultyCurve.darkRadius` | `{ 3, 1 }` + 7 (= today) | same | held (available) | — | harder |
| `DOT_HP_FRACTION { small, mid, large }` | `dotHpFor` at the ±10 / −15 / +25 HP dots | none — `{ 0.24, 0.36, 0.6 }` mean-matched at L1 | same | held (available) | — | harder (the −HP rows) |
| `TIER_SPREAD` | `tierSpreadFor()` — d4 faces that bleed one tier down | 1 | 1 | available, canon | — | easier (more bleed) |
| `DOT_MIX.harm / loot / help` | `remapEncounterResult` | 1.0 each | 1.0 | available, canon | — | harm ↑ harder; loot/help ↑ easier |
| `FLEE_NEED_MOD` / `PARLEY_NEED_MOD` | added to the flee need (`fleeBreakdown`) / the parley need | 0 / 0 | 0 / 0 | available, canon | — | harder |
| `STARTING_GOLD` / `STARTING_POTION_BONUS` | `rollCharacter` | 50 / 0 | 50 / 0 | available, canon | — | easier |
| `CLASS_MITIGATION` | `Fighter { hpMul 1, armorMul 1, killSpeed 1 (≤ 1 allowed) }`, `Thief { evasion 0, fleeBonus = FLEE_THIEF_BONUS, trapAvoid 0, killSpeed 1 (≥ 1 allowed) }`, `"Magic User" { spellPower 1 }` | identity rows | identity rows | manual knob only (max two notches per phase; first candidate Thief `evasion: -1`) | — | per row |
| `MAZE_SIZE` | — (CUT from Phase 54: no dial, no engine/shell/bot grid change; `GW`/`GH` stay 21) | — | — | available, not implemented (v1.8 candidate) | — | harder |

Search budget: 80 evaluations at `--workers=4 --seeds=200` (≈ 25–30 s each in-process across 4 workers ≈ 40 min) over the CORE 10 coordinates ≈ 8 evaluations per coordinate: TWO PASSES over the 10 in order (pass 1 with the full step, pass 2 with half the step), stopping early on `verdict === "PASS"` (floors 1–12 inside their bands) with the constraints ok. No held dial is ever probed by the search.

Output: the wired hooks + tests; the fit tool + its pure scoring library + tests; `fit/start.json`; parity measured (zero expected, declared if not); ledger note; `54-06-SUMMARY.md`.

Schema gate: no new persisted key (`STARTING_GOLD` / `STARTING_POTION_BONUS` touch chargen values only; the store tier is computed per visit; the maze grid stays 21×21).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-CONTEXT.md
@.planning/phases/54-four-band-retune-and-roster-decision/54-05-SUMMARY.md
</context>

<ground_rules>
- `PHASE_DIR=.planning/phases/54-four-band-retune-and-roster-decision`; `SCRATCH` = the session scratchpad. Entry: HEAD = 54-05's docs commit, tree clean. Record `PRE_06=$(git rev-parse HEAD)`.
- Commits: plain `git commit -F "$SCRATCH/msg.txt"` with the two trailer lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` / `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Skip `state.advance-plan`.
- Gates at every commit: `npm test` fail 0; `npm run build:www` exit 0; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git diff --stat $PRE_06 -- content/ test/parity/harness/comparables.js test/parity/prototype-master.js.txt` empty; `node --test` with explicit paths.
- Parity is MEASURED after Task 1 and after Task 2 (`node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` + `node --test test/parity/`); a mover at identity is a STOP unless its cause is an identity-column value this plan changed (none are) — declare only what the measurement shows, with the cause named.
- Every pin from tool output; never loosened. No device / human checkpoints; the SUMMARY ends with `## Human verification (deferred to end of run)`.
</ground_rules>

<tasks>
<!-- planner-discipline-allow: setDialsForTuning -->

<task type="auto">
  <name>Task 1: Economy + class + accuracy + exposed dials — LOOT_SCALE at the four coin sites; STORE_TIER's store consumer; CLASS_MITIGATION hooks (Thief evasion / trapAvoid / backstab killSpeed; Fighter hpMul / armorMul / melee killSpeed; MU spellPower via spellDamageFor); FOE_ACCURACY; FLEE_NEED_MOD / PARLEY_NEED_MOD; STARTING_GOLD / STARTING_POTION_BONUS; TIER_SPREAD exposed — every hook an identity fast path; tests; parity measured; one commit</name>
  <files>engine/difficulty.js, engine/combat.js, engine/derived.js, engine/character.js, engine/economy.js, engine/encounters.js, engine/items.js, engine/magic.js, test/difficulty/difficulty.test.js, test/unit/combat-scaling.test.js, test/unit/store-roll.test.js, test/unit/encounters.test.js, test/unit/magic.test.js, test/unit/feedback-payload.test.js, test/unit/flee-retune.test.js, test/unit/class-mitigation.test.js, tools/initiative-fixture-scan-output.txt</files>
  <read_first>
    - engine/difficulty.js (54-05's DIALS — the 54-06 keys are already present at identity; add the helpers next to their siblings)
    - engine/combat.js line 866 (`coin = Math.round((rng.d(10) * f.lvl * purse) / LOOT_DIVISOR)` → wrap in `lootFor`), lines 718-760 (the backstab opener: the `crit = true; critBy = "backstab"` branch — the Thief `killSpeed` multiplies the opener's damage AFTER the crit doubling, before `damageFoe`), the hero `weaponDamage(c, rng)` melee site in `playerStrike` (Fighter `killSpeed` on every melee strike), the parley roll (grep `function parley` — the need term the `PARLEY_NEED_MOD` adds to, post-draw)
    - engine/derived.js `foeToHitVs` and `foeToHitBreakdown` (lines ~940-1010: add `+ foeAccuracyFor()` and, for `vs === "hero"` and `c.cls === "Thief"`, `+ classEvasionFor(c)` immediately after `h += eff(c, "foeToHit")`, BEFORE the battleRoar/sidestep/smoke/mirror/invis overrides; the breakdown records `{ name: "accuracy", delta }` / `{ name: "evasion", delta }` only when non-zero), `fleeBreakdown` (the need: `FLEE_NEED` + `FLEE_NEED_MOD`), `armorSoak` (the `c.ar` read the Fighter `armorMul` scales — `Math.round(ar * armorMul)` when `!== 1`), `killSpFor` (unchanged — the scale is at the `c.sp +=` sites, 54-05)
    - engine/encounters.js line 144 (chest coin → `lootFor`), line 307 (wilmst cache → `lootFor`), line 449 (faerie → `lootFor`), lines 80-100 (`springTrap`'s dodge threshold `nimble` — a Thief adds `classTrapAvoidFor(c)`), engine/items.js lines 111-122 (`gainWilmst` — UNTOUCHED: the Pickpocket extra and the greed multiplier stay)
    - engine/economy.js lines 250-262 (`STORE_TIER_FLOORS` / `storeTier(depth)` → `storeTier(depth)` returns `difficultyCurve(depth).storeTier`; `STORE_TIER_FLOORS` deleted; the `BAG_FLOORS` import stays only if another consumer needs it), test/unit/store-roll.test.js lines 81-96 (the ladder pin → re-pinned to the curve; the draw-count pin at line 221 re-measured per tier)
    - engine/magic.js lines 195-210, 275-290, 318-330, 495-510 (the offensive spell-damage sites: `rng.d(6) * Math.max(1, c.level - sp.lvl)`, `rollDice × mult`, the afraidDamage-wrapped bolts — each becomes `spellDamageFor(<the rolled amount>, c)`; healing (line 444) and self-damage (line 130) are NOT scaled; name every site in the JSDoc)
    - engine/character.js lines 536-545 (`potions`, `gold: 50` → `startingGoldFor()` / `startingPotionsFor(canon)`)
    - test/unit/feedback-payload.test.js (the `foeToHitVs === foeToHitBreakdown.need` matrix — extend the matrix with `FOE_ACCURACY ∈ {-3, 0, 3}` × Thief evasion `∈ {-1, 0}` via `setDialsForTuning`, restored), test/unit/flee-retune.test.js (the flee need pin — add `FLEE_NEED_MOD` identity), test/unit/magic.test.js (the damage pins — add a `spellPower 1.15` case under `setDialsForTuning`)
  </read_first>
  <action>
    (1) `engine/difficulty.js` helpers (each reads `live`, each has the structural fast path): `lootFor(coin)` (`LOOT_SCALE === 1 ? coin : Math.round(coin * LOOT_SCALE)`), `foeAccuracyFor()` (`live.FOE_ACCURACY`), `classEvasionFor(c)` (`c.cls === "Thief" ? live.CLASS_MITIGATION.Thief.evasion : 0`), `classTrapAvoidFor(c)`, `classKillSpeedFor(c, { opener })` (Thief: `opener ? Thief.killSpeed : 1`; Fighter: `Fighter.killSpeed`; else 1), `classArmorMulFor(c)` (Fighter only), `spellPowerFor(c)` (MU only, else 1), `spellDamageFor(n, c)` (`const p = spellPowerFor(c); return p === 1 ? n : Math.max(0, Math.round(n * p));`), `fleeNeedModFor()`, `parleyNeedModFor()`, `startingGoldFor()`, `startingPotionsFor(canon)` (`Math.max(0, canon + live.STARTING_POTION_BONUS)`), `tierSpreadFor()` (54-05 — confirm exported). JSDoc each with hook, identity, direction.
    (2) Wire every site named in read_first. Fighter `hpMul` is already inside `heroMaxWpFor(rolled, cls)` (54-05) — add its test here. Thief `killSpeed`: in `playerStrike`, after the crit doubling on the opener branch, `dmg = Math.max(1, Math.round(dmg * classKillSpeedFor(c, { opener: critBy === "backstab" })))` guarded by the `=== 1` fast path; Fighter `killSpeed`: the same call with `{ opener: false }` on every Fighter melee strike (one call site covers both — the helper decides). `armorSoak`: read `ar` through `Math.round(ar * classArmorMulFor(c))` only when the mul `!== 1`.
    (3) Tests: new `test/unit/class-mitigation.test.js` — `CLASS_MITIGATION has exactly the three class rows and no race/sub key`, `at identity every helper returns its input by identity (lootFor / spellDamageFor / classKillSpeedFor / classArmorMulFor / classEvasionFor / classTrapAvoidFor / foeAccuracyFor / fleeNeedModFor / parleyNeedModFor / startingGoldFor / startingPotionsFor)`, `Thief evasion -1 lowers foeToHitVs by 1 for vs hero only (member unchanged) and the breakdown records { name: "evasion", delta: -1 }`, `FOE_ACCURACY +2 raises the need by 2 for hero and member; the Math.max(1, h) clamp holds at -3`, `Thief killSpeed 1.25 scales the backstab opener only (a second-round strike unchanged); Fighter killSpeed 0.9 scales every Fighter melee strike; MU untouched`, `spellPower 1.15 scales a Fireball's rolled damage and never Heal`, `armorMul 1.2 scales the Fighter's ar read in armorSoak`, `LOOT_SCALE 0.8 scales the kill purse / chest / cache / faerie post-draw with the same draw count (countingRng)` , `STARTING_GOLD 75 / STARTING_POTION_BONUS 1 at chargen`; `store-roll.test.js` — `storeTier(depth)` re-pinned to `011122223333` (1..12) via the curve and `STORE_TIER { base: 0, perDepth: 0.55 }` under `setDialsForTuning` gives a different ladder (pasted); the draw-count pin re-measured per tier (unchanged numbers expected — paste). Every `setDialsForTuning` test restores in `finally`.
    (4) Parity: scan diff + suite (expected empty/green — every hook is identity; a mover is a STOP). Gates. Commit `feat(54-06): economy + class + accuracy + exposed dials at identity — LOOT_SCALE (kill/chest/cache/faerie), STORE_TIER → storeTier(depth), CLASS_MITIGATION hooks (Thief evasion/trapAvoid/backstab killSpeed; Fighter hpMul/armorMul/melee killSpeed; MU spellPower), FOE_ACCURACY, FLEE/PARLEY_NEED_MOD, STARTING_GOLD/POTION_BONUS, TIER_SPREAD; parity measured (0 movers) (USER RULING D, BAND-02)` with the two trailers.
  </action>
  <verify>
    <automated>node --test test/unit/class-mitigation.test.js test/unit/store-roll.test.js test/unit/feedback-payload.test.js test/unit/flee-retune.test.js test/unit/magic.test.js test/unit/encounters.test.js test/unit/combat-scaling.test.js test/difficulty/difficulty.test.js && node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && echo SCAN-CLEAN && node --test test/parity/full-suite.test.js test/parity/divergence-records.test.js && grep -c "lootFor(" engine/combat.js engine/encounters.js | awk -F: '{s+=$2} END {print "lootFor", s}' && grep -c "STORE_TIER_FLOORS" engine/economy.js</automated>
  </verify>
  <acceptance_criteria>
    - the verify prints `SCAN-CLEAN`, `lootFor 4`, and `0` for `STORE_TIER_FLOORS`; every listed test file prints `# fail 0`
    - `grep -c "foeAccuracyFor()" engine/derived.js` == 2 (foeToHitVs + breakdown) and `grep -c "classEvasionFor(" engine/derived.js` == 2; `grep -c "spellDamageFor(" engine/magic.js` >= 4 with NO call on the heal or self-damage lines (the SUMMARY names every site)
    - `grep -c "classKillSpeedFor(" engine/combat.js` >= 1; `grep -c "classTrapAvoidFor(" engine/encounters.js` == 1; `grep -c "startingGoldFor()\|startingPotionsFor(" engine/character.js` == 2
    - `node -e "import('./engine/economy.js').then(m=>console.log(Array.from({length:12},(_,i)=>m.storeTier(i+1)).join('')))"` prints `011122223333`
    - the parity suite is green with ZERO new `+54` records added in this task (`git diff --stat $PRE_06 -- test/parity/fixtures/` empty) — or, if a mover appeared, the SUMMARY names its cause and the record was declared (a STOP if the cause is not an identity-column change)
    - `npm test` fail 0; `build:www` exit 0; master hash; two trailers
  </acceptance_criteria>
  <done>Every economy, class, accuracy and exposed dial has its engine hook, at identity, pinned; the store tier reads the curve; nothing moved (measured).</done>
</task>

<task type="auto">
  <name>Task 2: The late dials with the same draw counts — DOT_MIX / FIGHT_SHARE (post-roll encounter remap) and WANDER_RATE (wake faces), both HELD at canon by the fit — tests; parity measured; one commit (MAZE_SIZE is CUT from this phase: no grid change anywhere)</name>
  <files>engine/difficulty.js, engine/encounters.js, engine/movement.js, test/unit/dot-mix.test.js, test/unit/movement.test.js, tools/initiative-fixture-scan-output.txt</files>
  <read_first>
    - content/encounters.js lines 39-48 (`ENCOUNTER_TABLES` 8×10: the fight cells are the six monster-type strings resolved by `ENC_ALIAS` — 43 of 80; row 4 is all table-four), engine/encounters.js lines 176-240 (`encounterDot`: `t = rng.d(8); r = rng.d(10); result = ENCOUNTER_TABLES[t-1][r-1]` → the remap goes right after `result` is read and BEFORE the `encounterRolled` event, which then carries both `rolled` and `result`)
    - engine/movement.js lines 760-776 (`wakeOn = c.sub === "Bard" ? 2 : 1` → `wanderWakeFacesFor(c.sub)`; the eight d20 draws unchanged)
    - engine/maze.js lines 12-13 (`GW`/`GH` = 21 — UNTOUCHED this phase; MAZE_SIZE is cut) — read only to confirm nothing here changes
  </read_first>
  <action>
    (1) `DOT_MIX` remap — in `engine/difficulty.js`: `export const DOT_MIX_FAMILIES = Object.freeze({ fight: [the six monster-type strings], harm: ["-10 HP", "-15 HP", "-All armour", "Ailment", "Insanity", "Phobia", "Darkness", "Teleport"], loot: ["Weapon", "Magic Weapon", "Magic Armor", "Misc Magic", "Grimoire", "wilmst cache", "Faerie"], help: ["Food", "Store", "Joiner", "+10 HP", "+25 HP", "+10 XP", "+25 XP"] })` (every one of the 80 cells belongs to exactly one family — a unit test proves it); `export function conversionTableFor(mix)` (pure, memoised on the mix object identity): for each family `f` with `mix[f] < 1`, `k_f = round((1 − mix[f]) × n_f)` cells of family `f`, taken in row-major order of `ENCOUNTER_TABLES`, are converted to the NEXT cell to the right in the same row (wrapping) that is NOT of family `f`; for `mix[f] > 1`, `k_f = round((mix[f] − 1) × n_f)` cells are converted TO family `f`, taken from the family with the most cells in that row other than `f` (first cells first), each becoming the nearest `f` cell to its right (wrapping) — returns a `Map<"t,r", result>` of only the converted cells; `export function remapEncounterResult(t, r, result)` → `conversionTableFor(live.DOT_MIX).get(`${t},${r}`) ?? result`. At all-1.0 the table is EMPTY (structural identity). In `encounterDot`: `const rolled = ENCOUNTER_TABLES[t-1][r-1]; const result = remapEncounterResult(t, r, rolled); events.push({ type: "encounterRolled", table: t, roll: r, result, ...(result !== rolled ? { rolled } : {}) });` — 0 new draws.
    (2) `WANDER_RATE`: `const wakeOn = wanderWakeFacesFor(c.sub)` (`c.sub === "Bard" ? Math.min(20, live.WANDER_RATE + 1) : live.WANDER_RATE`; the JSDoc records the granularity: a d20 per hour means 5 % steps — 0 / 1 / 2 faces = 0 / 5 / 10 % per hour; the multiplier form collapses to faces).
    (3) `MAZE_SIZE` — NOT implemented (user cut, 2026-09-21): no `DIALS.MAZE_SIZE` key, no `mazeSize` curve field, `engine/maze.js`'s `GW`/`GH` and the shell's canvas sizing untouched; the ledger's dial table lists it as `available, not implemented (v1.8 candidate)`. A unit assertion in `test/unit/dot-mix.test.js` (or difficulty.test.js) pins `"MAZE_SIZE" in DIALS === false` and `"mazeSize" in difficultyCurve(1) === false` so a later phase adds it deliberately.
    (4) Tests: `test/unit/dot-mix.test.js` — `every ENCOUNTER_TABLES cell belongs to exactly one DOT_MIX family (80 cells; fight 43)`, `conversionTableFor at all-1.0 is empty (identity)`, `FIGHT_SHARE 0.6 converts round(0.4 × 43) = 17 fight cells, each to a non-fight cell in its own row, deterministically (the same Map twice)`, `FIGHT_SHARE 1.2 converts round(0.2 × 43) = 9 non-fight cells to fight cells`, `encounterDot draws exactly 2 dice before the remap and the event carries rolled when converted (countingRng)`; `movement.test.js` — `wanderWakeFacesFor: 1 → face 1; 2 → faces 1-2; 0 → never; Bard = +1 capped at 20; the eight d20 draws are unchanged (countingRng)`.
    (5) Parity: scan + suite (identity — zero movers expected). Gates. Commit `feat(54-06): late dials at identity — DOT_MIX/FIGHT_SHARE post-roll remap (same two draws), WANDER_RATE wake faces (same eight d20s); MAZE_SIZE cut (no grid change); parity measured (0 movers) (USER RULING D, BAND-02)` with the two trailers.
  </action>
  <verify>
    <automated>node --test test/unit/dot-mix.test.js test/unit/movement.test.js test/determinism/maze-determinism.test.js test/determinism/same-seed-same-result.test.js && node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt && echo SCAN-CLEAN && node --test test/parity/full-suite.test.js test/parity/movement-parity.test.js && git diff --stat $PRE_06 -- engine/maze.js mazeworld.html src/browser/ tools/lib/tuning-bot.mjs | tail -1; npm run build:www 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - every listed test prints `# fail 0`; `SCAN-CLEAN` prints; the `git diff --stat` on engine/maze.js / mazeworld.html / src/browser/ / tuning-bot.mjs is EMPTY (no grid change, no shell change, no bot change); `build:www` prints its done line (exit 0)
    - `node -e "import('./engine/difficulty.js').then(m=>{console.log(m.conversionTableFor(m.DIALS.DOT_MIX).size);console.log(m.conversionTableFor({fight:0.6,harm:1,loot:1,help:1}).size);console.log('MAZE_SIZE' in m.DIALS,'mazeSize' in m.difficultyCurve(1))})"` prints `0`, `17`, `false false`
    - `grep -c "wanderWakeFacesFor(" engine/movement.js` == 1; `grep -c "remapEncounterResult(" engine/encounters.js` == 1
    - the parity suite green with zero new records (`git diff --stat $PRE_06 -- test/parity/fixtures/` empty) or the STOP recorded; `npm test` fail 0; master hash; two trailers
  </acceptance_criteria>
  <done>The encounter mix and the wandering rate are dials with unchanged draw counts, at identity, pinned; the maze grid is untouched (MAZE_SIZE cut).</done>
</task>

<task type="auto">
  <name>Task 3: The fit tool — tools/lib/fit-score.mjs (scoreSurvival, classConstraints, SEARCH_PLAN, applyStep — pure, unit-tested) + tools/fit-difficulty.mjs (evaluate / --search, worker threads, JSONL log, resumable, deterministic) + fit/start.json (the starting dial set); a dry run proving the pipeline; ledger note; SUMMARY; one commit</name>
  <files>tools/lib/fit-score.mjs, tools/fit-difficulty.mjs, test/unit/fit-score.test.js, .planning/phases/54-four-band-retune-and-roster-decision/fit/start.json, docs/DIFFICULTY-RETUNE.md, .planning/phases/54-four-band-retune-and-roster-decision/54-06-SUMMARY.md</files>
  <read_first>
    - tools/tune-classes.mjs lines 40-60 and 200-260 (the `worker_threads` pattern: `new Worker(new URL(import.meta.url), { workerData })`, `isMainThread`, the seed-slice split, the elapsed line on STDERR only) — copy the pattern
    - tools/lib/tuning-bot.mjs `playRun` (the run record incl. 54-04's `cls`, `floorSnapshots`, `identity`), tools/lib/band-readout.mjs (`survivalReadout`, `classIdentityReadout`, `paceReadout`, `TARGET_SURVIVAL`, `SURVIVAL_PASS`, `interpolatedMedian`)
    - engine/difficulty.js `DIALS` + `setDialsForTuning` (the override contract: partial deep-merge; unknown key throws)
    - this plan's dial table (the `Search bounds (step)` and `Search order` columns are `SEARCH_PLAN` verbatim)
  </read_first>
  <action>
    (1) `tools/lib/fit-score.mjs` (pure, no engine import): `export const SEARCH_PLAN = [ { id, path: ["FOE_LEVEL","perDepth"], step: 0.03, lo: 0.12, hi: 0.30 }, { path: ["FOE_LEVEL","base"], step: 0.15, lo: 0.3, hi: 1.0 }, { path: ["HERO_SP_SCALE"], step: 0.05, lo: 0.15, hi: 0.6 }, { path: ["FOE_HIT_SCALE","base"], step: 0.08, lo: 0.4, hi: 1.0 }, { path: ["FOE_HIT_SCALE","perDepth"], step: 0.01, lo: 0, hi: 0.05 }, { path: ["FOE_HP_SCALE","base"], step: 0.1, lo: 0.5, hi: 1.2 }, { path: ["HERO_HP_SCALE"], step: 0.15, lo: 1.0, hi: 1.8 }, { path: ["HERO_REGEN_PER_FLOOR"], step: 0.1, lo: 0, hi: 0.5 }, { path: ["HAZARD_SCALE","base"], step: 0.1, lo: 0.3, hi: 1.0 }, { path: ["ENCOUNTER_DOTS","base"], step: 1, lo: 5, hi: 10 } ]` — EXACTLY the core 10 in that order (user cut, 2026-09-21); `export const HELD_DIALS` = the list of every other dial key with its held start value and its "release if" note (the miss table reads it); `export function scoreSurvival(survival)` → `{ score, terms: [{ floor, SL, target, tolerance, term }] for floors 1..12 only, tail: [{ floor, SL, target, dS }] for floors 13..20 (informational), reach20 (informational), verdict: "PASS" | "MISS", misses: [floors 1..12 outside band] }` per the objective's formula (a `null` S_L counts as 0; floors 13–20 and reach-20 never enter `score` or `verdict`); `export function classConstraints(classIdentity)` → `{ ok, reasons: [], pooledP50, rows: { Fighter: { p50, reach5, reach10, dmgTakenPerFight, roundsPerFight }, … } }` with the four rules (|p50 − pooled| ≤ 1.0 using interpolated medians; |reach5 − pooled| ≤ 12; Fighter dmgTakenPerFight ≥ Thief's; Thief roundsPerFight ≤ Fighter's — a pool with n < 20 runs makes the constraint `ok: true, reasons: ["<cls> n<20 — unconstrained"]`); `export function applyStep(dials, coord, dir, stepScale = 1)` → a NEW deep-cloned dial set with the coordinate moved by `dir × step × stepScale`, clamped, rounded to the step's precision (`Number((v).toFixed(4))`), `null` when the move would not change the value; `export function evalRow(n, dials, result)` → the JSONL record `{ n, pass, dials, score, verdict, misses, reach20, floors: [{ L, pL, SL, dS, tail: L >= 13 }], classes, constraints, pace: [{ L, level, gold, maxWP }], elapsedMs }`; `export function formatEvalLine(row)` (one line: `#n pass=1|2 score=… verdict=… S5=… S8=… S10=… S12=… tail S15=… S20=… reach20=… classes F/T/M p50=…/…/… ok=…`).
    (2) `tools/fit-difficulty.mjs`: flags `--dials=<json|path>` (single evaluation), `--search`, `--start=<path>` (default `DIALS` identity), `--budget=80`, `--seeds=200`, `--workers=4`, `--log=<path.jsonl>` (append; on `--search` an existing log RESUMES: the highest `n` and the best row are read back and the coordinate walk continues from the next un-probed coordinate — record the resume in the log as `{ resumed: true }`), `--out=<path>` (the best dial set as JSON, written at the end of a search or after a single evaluation), `--max-actions` (default BOT_DEFAULTS). Worker contract: `workerData: { dials, seeds, opts }` → the worker imports `engine/difficulty.js`, calls `setDialsForTuning(dials)`, plays each seed with `playRun` (dropping `state`), posts the records; the main thread concatenates in seed order (determinism), runs `survivalReadout` / `classIdentityReadout` / `paceReadout`, scores, constrains, logs, prints `formatEvalLine` + (for a new best) the per-floor table. Search loop: evaluate start (n = 1) → PASS 1 over the 10 `SEARCH_PLAN` coordinates in order with the full step, then PASS 2 over the same 10 in the same order with `stepScale = 0.5`: for each coordinate probe `+1` (skip if `applyStep` returns null); if the score improves (strictly lower, constraints ok) accept and keep stepping the same way (max 2 more) while it improves; else probe `−1` the same way; the accepted set is the base for the next coordinate; every held dial is carried through untouched from `--start`; stop on budget (80) or on `verdict === "PASS"` (floors 1–12) with `constraints.ok`. Print a final `BEST #n score=… dials=<json>` line and write `--out`. STDERR-only timing. Exit 0 always (a report tool). Header comment: "TUNING FIT — not a gate; the fair bot's solo run is the objective; the class smoke is run by 54-04 (BEFORE) and 54-07 (AFTER) only".
    (3) `test/unit/fit-score.test.js`: `SEARCH_PLAN has exactly the core 10 coordinates in the user's order (FOE_LEVEL.perDepth, FOE_LEVEL.base, HERO_SP_SCALE, FOE_HIT_SCALE.base, FOE_HIT_SCALE.perDepth, FOE_HP_SCALE.base, HERO_HP_SCALE, HERO_REGEN_PER_FLOOR, HAZARD_SCALE.base, ENCOUNTER_DOTS.base) with the pinned steps/bounds; HELD_DIALS names every other dial`, `scoreSurvival: a synthetic survival exactly on the Ruling C curve scores 0 and PASSes; S_5 off by 8 adds exactly 1.0; S_12 off by 3 adds exactly 1.0; S_15 off by 30 and reach20 0 add NOTHING (tail — informational) and do not flip the verdict; a null S_L counts as 0`, `classConstraints: rejects a Fighter p50 1.5 below pooled; rejects Thief roundsPerFight > Fighter's; a pool with n < 20 is unconstrained`, `applyStep clamps, steps ENCOUNTER_DOTS.base by 1, halves the step at stepScale 0.5, returns null at a bound, and never touches a key outside SEARCH_PLAN`, `evalRow / formatEvalLine shape (tail flag on floors >= 13)`.
    (4) `fit/start.json`: the `Starting value` column of the dial table as a partial `DIALS` JSON — the core 10's start values AND every held dial's held value (LOOT_SCALE 0.8, CAMP_HEAL_FRACTION 0.2, ROUND_DAMAGE_CEILING 0.5, FOE_COUNT_SKEW 1, FOE_HP_SCALE.perDepth 0.015, HAZARD_SCALE.perDepth 0.02, ENCOUNTER_DOTS.perDepth 0.3, FOOD_CLOCK 1.5, ABILITY_THREAT { 1.0, 0 }, STORE_TIER { 0, 0.3 }, DOT_MIX.fight 1.0, WANDER_RATE 1, FOE_ACCURACY 0, DARK_BLOBS { −0.4, 0.7 }; `DOT_HP_FRACTION` untouched); no `MAZE_SIZE` key. Dry run: `node tools/fit-difficulty.mjs --dials=$PHASE_DIR/fit/start.json --seeds=20 --workers=4 --log=$SCRATCH/dry.jsonl --max-actions=3000` — prints one eval line with a finite score (or a rejection reason); then `--dials='{}' --seeds=20` must reproduce `tune-difficulty --seeds=20 --max-actions=3000 --json`'s `survival.floors[].pL` exactly (the same seeds through the same bot — paste both). Neither dry-run log is committed.
    (5) Ledger: under the Phase 54 H3, after `#### Identity commit`, add `#### Hero-side / late dials + the fit tool — commit …` (one paragraph: the hooks landed at identity with zero movers (the scan diff), the fit's objective formula on floors 1–12 only (13–20 and reach-20 are the measured tail — the user's cut), the constraints and their cost decision (solo pools, no per-eval smoke), the budget 80 over the CORE 10 (two passes) with every other dial held, MAZE_SIZE cut; pointer to `fit/start.json`). Gates. Commit `feat(54-06): fit tool — tools/fit-difficulty.mjs (in-process worker-threaded evaluation, Ruling C S_L score, class-pool fairness + identity constraints, JSONL log, resumable bounded coordinate search over SEARCH_PLAN) + tools/lib/fit-score.mjs + fit/start.json; ledger note (USER RULING D)` with the two trailers.
    (6) `$PHASE_DIR/54-06-SUMMARY.md` (frontmatter as before; `requirements-completed: [BAND-02]`): `## Success criteria → proof` (every hook with its test; the fit tool's dry-run lines pasted; the identity-equivalence of `--dials='{}'` vs `tune-difficulty --json`), `## Hooks landed (site → helper → test)` table, `## Parity — measured (Tasks 1 and 2)` (scan diffs, suite totals, zero records or the declared ones), `## The fit tool` (flags, worker contract, score, constraints, budget, search order — a copy of SEARCH_PLAN), `## Gates`, `## Schema` (none), `## Commits` (three SHAs), `## Deviations`, `## Issues`, `## Next Phase Readiness` (54-07 runs `--search` from `fit/start.json`, commits the best set into DIALS, re-pins, declares the fitted movers, takes the AFTER), `## Human verification (deferred to end of run)` (nothing visible changes at identity; note the maze-size and encounter-mix hooks exist for the fit).
  </action>
  <verify>
    <automated>node --test test/unit/fit-score.test.js && test -f .planning/phases/54-four-band-retune-and-roster-decision/fit/start.json && node -e "const s=require('./.planning/phases/54-four-band-retune-and-roster-decision/fit/start.json');console.log(s.FOE_LEVEL.base,s.FOE_LEVEL.perDepth,s.HERO_SP_SCALE,s.LOOT_SCALE,s.FOE_HIT_SCALE.base,s.HERO_HP_SCALE,s.HERO_REGEN_PER_FLOOR,s.ROUND_DAMAGE_CEILING,s.FOE_COUNT_SKEW,s.HAZARD_SCALE.base,s.ENCOUNTER_DOTS.base,s.FOOD_CLOCK)" && node tools/fit-difficulty.mjs --dials='{}' --seeds=8 --workers=2 --max-actions=1500 --log="$TMPDIR/fit-dry.jsonl" | tail -3 && grep -c '^#### Hero-side / late dials + the fit tool — commit ' docs/DIFFICULTY-RETUNE.md && node --test test/unit/difficulty-retune-ledger.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `fit-score.test.js` prints `# fail 0`; the start.json print reads `0.6 0.2 0.28 0.8 0.6 1.25 0.25 0.5 1 0.5 7 1.5`
    - the dry run prints a line beginning `#1 score=` with a finite number or `+Infinity` and a `reason=` (rejection), never a stack trace; the SUMMARY pastes the `--dials='{}'` vs `tune-difficulty --json` pL equality check
    - `grep -c 'isMainThread\|new Worker(' tools/fit-difficulty.mjs` >= 2; `grep -c 'setDialsForTuning' tools/fit-difficulty.mjs` >= 1; `grep -c 'tune-classes' tools/fit-difficulty.mjs` == 0 (the smoke is never run by the fit)
    - `SEARCH_PLAN.length === 10` with the paths in the user's order and `HELD_DIALS` covering every other dial (pinned by the test; `node -e "import('./tools/lib/fit-score.mjs').then(m=>console.log(m.SEARCH_PLAN.map(c=>c.path.join('.')).join(',')))"` prints `FOE_LEVEL.perDepth,FOE_LEVEL.base,HERO_SP_SCALE,FOE_HIT_SCALE.base,FOE_HIT_SCALE.perDepth,FOE_HP_SCALE.base,HERO_HP_SCALE,HERO_REGEN_PER_FLOOR,HAZARD_SCALE.base,ENCOUNTER_DOTS.base`); `grep -c MAZE_SIZE tools/lib/fit-score.mjs .planning/phases/54-four-band-retune-and-roster-decision/fit/start.json` prints `0` for both; the ledger grep prints `1` and the ledger test is green
    - `npm test` fail 0; `build:www` exit 0; master hash; `git diff --stat $PRE_06 -- content/ test/parity/harness/comparables.js` empty; two trailers on each of the three commits; the SUMMARY ends with `## Human verification (deferred to end of run)`
  </acceptance_criteria>
  <done>The complete model is hooked and the fit tool can evaluate and search it deterministically from the committed starting point with the class-pool constraints; 54-07 can run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → engine | `setDialsForTuning` inside worker threads (Node only); the browser bundle never calls it |
| fit log → ledger | every evaluation row carries the full dial set (held dials included) so a held value can never be misread as fitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-54-06-01 | Tampering | a held dial silently probed by the search (scope creep past the user's cut) | low | mitigate | `SEARCH_PLAN` pinned to the core 10 by test; `applyStep` refuses any key outside it; every log row carries the full dial set |
| T-54-06-02 | Tampering | the fit log as evidence | low | mitigate | JSONL append-only, every row carries the dials and the seed count; committed in 54-07 |
| T-54-06-03 | Information disclosure | worker threads leaking engine state across evaluations | low | mitigate | each worker is a fresh module instance per evaluation; the main thread never imports the engine |
| T-54-SC | Tampering | npm/pip/cargo installs | high | accept | no package installs (`worker_threads` is Node core; `git diff --stat $PRE_06 -- package.json package-lock.json` empty) |
</threat_model>

<verification>
- every new/re-pinned unit test green; the parity suite green with the scan output byte-identical to the committed copy after Tasks 1 and 2
- `npm test` fail 0 and `npm run build:www` exit 0 at each of the three commits; master hash unchanged; `content/` and `comparables.js` untouched
- the fit tool's dry run evaluates and logs; `--dials='{}'` reproduces tune-difficulty's per-floor p_L on the same seeds
</verification>

<success_criteria>
- LOOT_SCALE (4 sites), STORE_TIER's consumer, CLASS_MITIGATION (three rows, seven hooks), FOE_ACCURACY, FLEE/PARLEY_NEED_MOD, STARTING_GOLD/POTION_BONUS, TIER_SPREAD, DOT_MIX/FIGHT_SHARE, WANDER_RATE wired at identity with unchanged draw counts and zero movers (measured); MAZE_SIZE cut (no grid change; pinned absent)
- `tools/fit-difficulty.mjs` + `fit-score.mjs` + `fit/start.json` landed with tests; `SEARCH_PLAN` = the core 10 in the user's order; every other dial held; the objective scores floors 1–12 only
- Ledger note; SUMMARY with verifier-grade evidence
</success_criteria>

<artifacts_this_phase_produces>
- `engine/difficulty.js` — the economy / class / accuracy / mix / wander helpers; `DOT_MIX_FAMILIES`, `conversionTableFor`, `remapEncounterResult`
- `engine/combat.js`, `engine/derived.js`, `engine/character.js`, `engine/economy.js`, `engine/encounters.js`, `engine/magic.js`, `engine/movement.js` — hooks wired (no maze / shell / bot edits)
- `tools/lib/fit-score.mjs` (`SEARCH_PLAN` core 10, `HELD_DIALS`, `scoreSurvival` floors 1–12 + tail), `tools/fit-difficulty.mjs`, `test/unit/fit-score.test.js`
- `.planning/phases/54-four-band-retune-and-roster-decision/fit/start.json` (core 10 starts + every held value)
- `test/unit/class-mitigation.test.js`, `test/unit/dot-mix.test.js` (+ re-pinned store-roll / feedback-payload / flee-retune / magic / encounters / movement)
- `docs/DIFFICULTY-RETUNE.md` — `#### Hero-side / late dials + the fit tool — commit …`
- `54-06-SUMMARY.md`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/54-four-band-retune-and-roster-decision/54-06-SUMMARY.md` when done (verifier-grade; ends with `## Human verification (deferred to end of run)`).
</output>