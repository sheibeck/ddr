---
phase: 52-foe-cadence-damage-curve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/unit/foe-cadence.test.js
  - content/foe-abilities.js
  - tools/cadence-audit.mjs
  - tools/cadence-audit-output.txt
  - tools/damage-curve-audit.mjs
  - tools/damage-curve-audit-output.txt
autonomous: true
requirements: [CAD-01, CAD-02, CAD-03, DMG-01]
tags: [combat, cadence, foe-abilities, report-tool, damage-curve, measurement-gate]

must_haves:
  truths:
    - "A plain foe makes exactly 1 ordinary attack per foe turn, an `sp.atk: 2` foe exactly 2, a frenzied plain foe 2, a frenzied `sp.atk: 2` foe 4 — each pinned by a dedicated unit test that counts `foeMissed` + `struckByFoe` (+ `memberStruck`) per `foeTurn` call (SC1, D-01)"
    - "A Stalka Beast turn is EITHER one ability (`foeCast` + its effect event) with zero ordinary swings OR at most two swings with zero `foeCast` — the user's pasted two-hits-plus-frost turn is provably impossible inside one `foeTurn` call (SC2, D-02)"
    - "Attacks per player action for a single Bat/Rat and a single China Wolf fight is <= 2 (their `sp.atk`) on the post-INIT engine — pinned by a unit test AND reported by `tools/cadence-audit.mjs` over tens of seeds at depth 5 (SC3, D-03)"
    - "A committed, deterministic, rng-free script reports the exact max single hit of every bestiary row per depth band under the CURRENT crit rule (BEFORE) and flags every row at >= 60 % of the Magic-User HP bar (SC4 first half, D-04/D-05/D-06)"
    - "Zero engine bytes change in this plan: `git diff --stat <phase-start> -- engine/` is empty, `content/` changes are comment-only, `test/parity/` is untouched, the master hash is unchanged"
  artifacts:
    - path: "test/unit/foe-cadence.test.js"
      provides: "CAD-01 swing-count pins, CAD-02 Stalka resolver pins, CAD-03 attacks-per-player-action invariant pin"
    - path: "content/foe-abilities.js"
      provides: "one 'an ability turn replaces every swing' comment line per kit (Krupke/Drudge/Djinni/Vampire/Stalka Beast/Drake) — comment-only"
    - path: "tools/cadence-audit.mjs"
      provides: "report tool: Bat/Rat + China Wolf single-foe fights at depth 5 over 40 seeds through the real startCombat/fight/playerStrike, attacks per player action max/mean, INVARIANT line"
    - path: "tools/cadence-audit-output.txt"
      provides: "committed deterministic output (re-run diff empty)"
    - path: "tools/damage-curve-audit.mjs"
      provides: "analytic damage-curve audit: every bestiary row x tier x band, max single hit under --rule=whole|dice, MU/Fighter bars from the Phase 51 AFTER smoke medians (or --levels), FLAG/note verdicts"
    - path: "tools/damage-curve-audit-output.txt"
      provides: "the BEFORE section (--rule=whole, pre-edit bestiary); Plan 02 appends the AFTER section"
  key_links:
    - from: "test/unit/foe-cadence.test.js"
      to: "engine/combat.js#foeTurn"
      via: "foeTurn(state, fakeRng, events) on a fixedCombat state (the foe-abilities.test.js mould) and full fights via startCombat/fight/playerStrike"
      why: "the pins are the standing proof of SC1/SC2/SC3 — a future cadence regression fails here first"
    - from: "tools/damage-curve-audit.mjs"
      to: "engine/difficulty.js#difficultyCurve/foeDmgBonusFor + content/bestiary.js + content/foe-abilities.js + content/classes.js"
      via: "imports the live tables and the curve, mirrors foeTurn's damage formula (as tools/bestiary-yardstick.mjs already does)"
      why: "an exhaustive analytic audit is only honest if it reads the same data the engine reads"
---

<objective>
Pin the foe cadence rules that Phase 51 left correct (they are NOT changed by this phase), prove the ability-replaces-swings contract on the Stalka Beast, and build the two Phase 52 measurement tools — `tools/cadence-audit.mjs` (CAD-03) and `tools/damage-curve-audit.mjs` (DMG-01) — with their committed outputs, all on the untouched engine so the BEFORE readings are taken before a single engine byte moves.

Purpose: SC1/SC2/SC3 need standing proofs, and DMG-01's BEFORE reading is impossible to recover once Plan 02 changes the crit formula and Herman's row. Every measurement here is taken on the phase-start tree (`3415b43`, engine byte-identical to Phase 51's AFTER commit `608a0e5`).

Output: `test/unit/foe-cadence.test.js`, kit comment lines in `content/foe-abilities.js`, `tools/cadence-audit.mjs` + `tools/cadence-audit-output.txt`, `tools/damage-curve-audit.mjs` + `tools/damage-curve-audit-output.txt` (BEFORE section).

Decisions implemented (52-CONTEXT.md, quoted): D-01 "Swing count rule is UNCHANGED … Three pins are added: a plain foe makes exactly 1 ordinary attack per foe turn, an `sp.atk: 2` foe exactly 2, a frenzied `sp.atk: 2` foe exactly 4 — counted as `foeMissed` + `struck`/`memberStruck` events per foe turn." D-02 "Ability-replaces-swings is UNCHANGED in the engine … Added: the Stalka Beast resolver test … and a one-line 'an ability turn replaces every swing' note on each kit in `content/foe-abilities.js`. No new per-kit flag." D-03 "new `tools/cadence-audit.mjs` (report tool, exit 0) forces Bat/Rat and China Wolf fights at depth 5 against a level-appropriate hero over N seeds through the real engine, counts foe attack events between consecutive player actions, and prints max/mean per fight; the invariant … is also a unit-test pin. Output committed (`tools/cadence-audit-output.txt`)." D-04 "Analytic, not sampled: `tools/damage-curve-audit.mjs` walks every bestiary row × tier and computes the exact max single hit … No rng; exhaustive and deterministic." D-05 the two HP bars and the 60 % flag rule. D-06 "committed `tools/damage-curve-audit-output.txt` (BEFORE and AFTER sections)".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-foe-cadence-damage-curve/52-CONTEXT.md
@.planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md
</context>

<ground_rules>
- Phase-start commit: `3415b432b191434f16d5b570b65d77c6dc321d36` (record `git rev-parse HEAD` before your first edit; engine/content/parity are byte-identical to Phase 51's AFTER commit `608a0e5`).
- ZERO engine bytes in this plan. `content/foe-abilities.js` may change on comment lines only (`node tools/comment-only-diff.mjs <phase-start> content` must report identical stripped text). `test/parity/` untouched. `test/parity/prototype-master.js.txt` is NEVER edited: `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every task.
- Report tools in `tools/` are exit-0 and assertion-free (never a `*.test.*` filename; `node --test` must not pick them up). Invariants live in `test/unit/*.test.js`.
- Commit per task with the trailers `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
- `npm run boot:check` is environment-blocked on this machine — not a gate. Gates are `npm test` (fail 0) and `npm run build:www`.
</ground_rules>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CAD-01/CAD-02/CAD-03 pins — test/unit/foe-cadence.test.js + the per-kit "ability turn replaces every swing" notes (zero engine bytes)</name>
  <files>test/unit/foe-cadence.test.js, content/foe-abilities.js</files>
  <read_first>
    - engine/combat.js lines 2241-2380 (foeTurn: the ability gate at ~2360-2371 — `tickAbilityCooldowns`, `firstReadyAbility`, `never_melee`, the `rng.d(6) <= 4` cast check, `resolveFoeAbility(...).died` then `continue`; the swings line at 2373 `const swings = (f.frenzied ? 2 : 1) * ((f.sp && f.sp.atk) || 1)`), 2383-2497 (member branch; `foeMissed` at ~2435 carries `member`, `memberStruck` at ~2486), 2499-2555 (hero branch; `foeMissed` at ~2522; `applyFoeDamageToPlayer` → `struckByFoe`/`armorSoaked`), 435-560 (`fight()` — the opener foe turn when the foe goes first), 1386-1400 (`afterPlayerAction` — `round++` then one `foeTurn`)
    - engine/foeAbilities.js lines 40-95 (`tickAbilityCooldowns` lazy-init + decrement, `firstReadyAbility` kit-order readiness incl. the heal-at-full-HP skip) and 159-251 (`resolveFoeAbility`: `foeCast` first, then `foeHealed` | `foeSummoned` | `foeDebuffed` | `foeBolted` (+ `foeDrained`); hero resist only when `c.intel >= 12`)
    - content/foe-abilities.js (all 19 descriptors; the header comment; the four Stalka ids: stalkaHeal every 3, stalkaLightning every 2, stalkaFireball every 3, stalkaFreeze unbounded)
    - content/bestiary.js lines 38 (Bat/Rat `sp: { atk: 2, dmg: {n:0,sides:0,bonus:1} }`), 65 (Stalka Beast `sp: { atk: 2, dmg: 1d4 }`, `abilities: [4 ids]`), 97 (China Wolf `sp: { atk: 2, dmg: 1d6 }`)
    - test/unit/foe-abilities.test.js lines 1-155 (the mould to copy verbatim: `fakeRng`, `fixedFighter`, `fixedFloor`, `fixedState`, `fixedFoe`, `fixedCombat`; the "gate: kit with a ready bolt — d6 1..4 casts, 5..6 melees" test shows the exact event shapes)
    - test/unit/foe-turn-draw-count.test.js lines 234-266 (`runFullFight`: newRun → makeRng(state.rngState) → startCombat(state, false, forced, rng, events) → fight → playerStrike loop) and 197-207 (the Bat/Rat micro pin)
    - engine/magic.js line 440 (`t.frenzied = true` — the only setter; frenzy is a spell effect, so the test sets `frenzied: true` on the foe object directly)
    - test/determinism/content-is-pure-data.test.js (comments in content files are fine; functions are not)
  </read_first>
  <behavior>
    - CAD-01 (D-01, SC1) — four `foeTurn` pins on a `fixedState()` hero (no armour, no ward, no party) with a `fixedFoe` whose to-hit rolls all MISS (fakeRng values above the need, e.g. 20): plain foe → exactly 1 `foeMissed`, fakeRng length 1 (a second draw throws "sequence exhausted"); `sp: { atk: 2 }` → exactly 2 `foeMissed`, 2 draws; `frenzied: true` plain → exactly 2; `frenzied: true, sp: { atk: 2 }` → exactly 4, 4 draws. Count = events of type `foeMissed` + `struckByFoe` + `memberStruck` in the returned array.
    - CAD-02 (D-02, SC2) — the Stalka resolver: foe = `fixedFoe({ name: "Stalka Beast", lvl: 5, wp: 94, maxWP: 94, sp: <a copy of BESTIARY.Beasts[4][?].sp — locate the row by name, never by index>, abilities: <copy of that row's abilities> })`, hero `fixedFighter({ wp: 999, maxWP: 999 })` (survives a tier-5 crit; `intel` absent → no resist draw). (a) ability turn: first visit, every `every:` entry lazily inits then decrements so stalkaFreeze (unbounded) is the first ready entry; fakeRng([4, 6]) → event types exactly `["foeCast", "foeBolted"]`, `foeCast.ability === "stalkaFreeze"`, `foeBolted.dmg === 6`, ZERO `foeMissed`/`struckByFoe`/`armorSoaked`, and exactly 2 draws. (b) melee turn: fakeRng([5, 20, 20]) → types exactly `["foeMissed", "foeMissed"]` (two swings, both miss), zero `foeCast`, exactly 3 draws. (c) the impossibility sweep: for every d6 value 1..6 and every pair of to-hit values in {1, 20} (crit-hit / miss), a fresh state+foe and ONE `foeTurn` call: if any `foeCast` is present then the count of `foeMissed`+`struckByFoe`+`armorSoaked` is 0; otherwise `foeCast` count is 0 and the swing count is <= 2. Assert the message "two hits + a bolt in one foe turn is impossible" on failure. (d) a later visit where stalkaLightning is ready (drive cd via `f.cd = { stalkaHeal: 1, stalkaLightning: 1, stalkaFireball: 1 }` before the call, foe at full HP so heal skips) → `foeCast.ability === "stalkaLightning"` and again zero swing events.
    - CAD-03 (D-03, SC3) — attacks-per-player-action invariant over full fights driven by the REAL `startCombat`/`fight`/`playerStrike`: for seeds 1..40 (`i * 7919 + 1`), `state = newRun(seed)`, set `state.floor.depth = 5`, `rng = makeRng(state.rngState)`, `startCombat(state, true, "Beasts", rng, events)` (wandering → exactly one foe); skip the seed unless `state.combat.foes[0].name === "Bat/Rat"`; then `fight(state, rng, seg)` and a `playerStrike` loop (cap 60) collecting the events of EACH call into its own segment; per segment count `foeMissed` + `struckByFoe` + `armorSoaked` + `memberStruck`; assert every segment count <= 2 and assert at least 5 Bat/Rat fights were sampled. Repeat for China Wolf with `state.c.level = 2` before `startCombat` (tier 2 needs hero level >= 2: `maxLvl = clamp(min(c.level, depth) + bias, 1, 5)`), `startCombat(state, true, "Humans", …)`, name `"China Wolf"`. A frenzied variant is not sampled here (no spell sets it) — the CAD-01 pin covers frenzy.
  </behavior>
  <action>
    Create `test/unit/foe-cadence.test.js` (node:test + node:assert/strict, ESM imports from `../../engine/combat.js` (`foeTurn`, `startCombat`, `fight`, `playerStrike`), `../../engine/state.js` (`newRun`), `../../engine/rng.js` (`makeRng`), `../../content/index.js` (`BESTIARY`, `FOE_ABILITIES`)). Copy the `fakeRng`/`fixedFighter`/`fixedFloor`/`fixedState`/`fixedFoe`/`fixedCombat` helpers verbatim from `test/unit/foe-abilities.test.js` (the repo's convention is a per-file copy, not a shared import). Header comment: Phase 52 (CAD-01/02/03) — these pin rules that are UNCHANGED by Phase 52 (the cadence was already right once Phase 51 removed the double turn); they are the standing proof for ROADMAP SC1/SC2/SC3. Implement the four behaviour groups above as separately named tests: name them starting with "CAD-01 (SC1):", "CAD-02 (SC2):", "CAD-03 (SC3):" so the SUMMARY can cite them. The Stalka foe's `sp`/`abilities` MUST be copied from the live BESTIARY row found by `n === "Stalka Beast"` (so a future content edit is exercised, not a stale literal). For the CAD-03 group, build the segment-count helper once (`attacksPerAction(events)`) and reuse it for both creatures; the swing-count vocabulary (`foeMissed`, `struckByFoe`, `armorSoaked`, `memberStruck`) is the same set `tools/cadence-audit.mjs` (Task 2) will count — say so in a comment so the two never drift.

    In `content/foe-abilities.js` add ONE comment line (comment only — the file must stay pure data) immediately above the first entry of each kit group (krupke*, drudge*, djinni*, vampire*, stalka*, drakeBreath), each containing the exact phrase `an ability turn replaces every swing` followed by a short pointer: the gate in `engine/combat.js#foeTurn` `continue`s after `resolveFoeAbility`, so the row's `sp.atk` swings never stack on a cast (Phase 52, CAD-02, 2026-09-20). Do not add any key to any descriptor (D-02: "No new per-kit flag"). Do not touch any `txt` value (voice-scanned).
  </action>
  <verify>
    <automated>node --test test/unit/foe-cadence.test.js test/unit/foe-abilities.test.js test/determinism/content-is-pure-data.test.js test/unit/content-tables.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/foe-cadence.test.js` exits 0; `grep -c '^test("CAD-01 (SC1):' test/unit/foe-cadence.test.js` prints 4 (plain / atk 2 / frenzied plain / frenzied atk 2) or a single table-driven test whose row count is asserted to be 4 — either way `grep -c 'frenzied: true' test/unit/foe-cadence.test.js` >= 2
    - `grep -c '^test("CAD-02 (SC2):' test/unit/foe-cadence.test.js` >= 3 and `grep -c 'two hits + a bolt in one foe turn is impossible' test/unit/foe-cadence.test.js` >= 1
    - `grep -c '^test("CAD-03 (SC3):' test/unit/foe-cadence.test.js` >= 2 and `grep -c '"China Wolf"' test/unit/foe-cadence.test.js` >= 1 and `grep -c '"Bat/Rat"' test/unit/foe-cadence.test.js` >= 1
    - `grep -c 'an ability turn replaces every swing' content/foe-abilities.js` prints 6
    - `node tools/comment-only-diff.mjs <phase-start-sha> content` reports the stripped text identical (comment-only change) — paste its output into the SUMMARY
    - `git diff --stat <phase-start-sha> -- engine/ test/parity/` prints nothing
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>SC1/SC2/SC3 have named standing pins that pass on the untouched engine; each kit carries the one-line replaces-every-swing note; zero engine bytes changed.</done>
</task>

<task type="auto">
  <name>Task 2: tools/cadence-audit.mjs — the CAD-03 report tool, run on the post-INIT engine, output committed</name>
  <files>tools/cadence-audit.mjs, tools/cadence-audit-output.txt</files>
  <read_first>
    - tools/initiative-fixture-scan.mjs lines 1-60 (the report-tool header conventions: "A REPORT tool (always exits 0, makes no assertions — NOT a `*.test.js` file)", the Run: line, what is invariant vs measured)
    - tools/tune-difficulty.mjs lines 1-40 (the "TUNING PROXY, NOT A PASS/FAIL GATE" header wording to mirror)
    - test/unit/foe-turn-draw-count.test.js lines 244-266 (`runFullFight` — the exact call order newRun → makeRng(state.rngState) → startCombat → fight → playerStrike loop)
    - engine/state.js line 146 (`newRun(seed, exclude = [], { startDepth = 1, force = null, storeRoll = false })` — do NOT use startDepth here: it levels the hero to 5 and makes tier 1/2 unreachable; set `state.floor.depth = 5` directly instead)
    - engine/combat.js lines 231-300 (startCombat: `maxLvl = clamp(min(c.level, depth) + foeLvlBias, 1, 5)`, `n = wandering ? 1 : …`, `rng.pick(roster)` — why a level-1 hero at depth 5 rolls tier 1 and a level-2 hero rolls tier 1–2)
    - test/unit/foe-cadence.test.js (Task 1 — the `attacksPerAction` vocabulary this tool must match)
    - tools/build-www.mjs copy list (confirm tools/ is never shipped)
  </read_first>
  <action>
    Create `tools/cadence-audit.mjs` (Node ESM, `#!/usr/bin/env node`, zero dependencies, exit 0 always, no assertions). Header comment states: purpose (Phase 52, CAD-03/SC3 — measure attacks per player action for the two fights the user reported, on the corrected initiative); that it is a TUNING PROXY / report, not a gate; the Run: line `node tools/cadence-audit.mjs > tools/cadence-audit-output.txt`; the hero setup (Claude's discretion, stated verbatim in the header AND printed in the output): the seed's own `newRun(seed)` character (real chargen, class varies by seed), `state.floor.depth` set to 5, hero level left at 1 for the Bat/Rat sample and set to `c.level = 2` for the China Wolf sample (tier 2 needs level >= 2), hit points as rolled (not raised); N = 40 seeds (`i * 7919 + 1`, i = 0..39 — the same seed list `tune-classes` uses); single-foe fights (`startCombat(state, true, type, rng, events)` — wandering forces n = 1 so every count is per foe); a seed whose roster pick is not the target creature is a "roster miss" (counted, not sampled).

    Per sampled fight: `fight(state, rng, seg0)` then `playerStrike(state, rng, segK)` until `!state.combat || state.dead` or 60 strikes; segment k's foe-attack count = its `foeMissed` + `struckByFoe` + `armorSoaked` + `memberStruck` events (the same vocabulary as `test/unit/foe-cadence.test.js`); record max and mean over the fight's segments, the outcome (won/died/capped), and the hero class/sub/race.

    Output (markdown-ish plain text, deterministic, no timestamps, no commit hashes): a title line `# cadence-audit — Phase 52 (CAD-03): attacks per player action, depth 5, single foe`; the hero-setup paragraph; then per creature a table with one row per sampled fight (seed, hero, actions, max attacks/action, mean attacks/action, outcome), followed by the lines `Bat/Rat: sampled N of 40 seeds (roster misses M) — max attacks per player action X, mean Y` and `INVARIANT attacks per player action <= 2 (sp.atk 2, unfrenzied): HOLDS` (or `VIOLATED at seed …` listing offenders); the same for China Wolf. End with a one-line reading: both creatures' overall max and the note that a frenzied foe (spell-only) would legitimately double these.

    Run it and commit stdout as `tools/cadence-audit-output.txt`. Re-run and diff against the committed file — must be empty.
  </action>
  <verify>
    <automated>node tools/cadence-audit.mjs > "$TMPDIR/cadence-check.txt" && diff "$TMPDIR/cadence-check.txt" tools/cadence-audit-output.txt && grep -c "INVARIANT attacks per player action <= 2 (sp.atk 2, unfrenzied): HOLDS" tools/cadence-audit-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/cadence-audit.mjs; echo $?` prints 0
    - re-run diff against `tools/cadence-audit-output.txt` is empty (deterministic)
    - `grep -c 'INVARIANT attacks per player action <= 2 (sp.atk 2, unfrenzied): HOLDS' tools/cadence-audit-output.txt` prints 2 (Bat/Rat and China Wolf)
    - `grep -c '^Bat/Rat: sampled [0-9]* of 40 seeds' tools/cadence-audit-output.txt` prints 1 and `grep -c '^China Wolf: sampled [0-9]* of 40 seeds' tools/cadence-audit-output.txt` prints 1; each sampled count is >= 5 (read the numbers into the SUMMARY)
    - `grep -c 'node:test' tools/cadence-audit.mjs` prints 0; `ls tools/ | grep -c 'cadence-audit.test'` prints 0
    - `git diff --stat <phase-start-sha> -- engine/ test/parity/` prints nothing
  </acceptance_criteria>
  <done>The CAD-03 readout exists as a committed, reproducible artifact showing max attacks per player action <= 2 for both creatures; Plan 03 quotes it in `docs/DIFFICULTY-RETUNE.md` and re-runs it after the engine edit to prove the crit rule changed no cadence.</done>
</task>

<task type="auto">
  <name>Task 3: tools/damage-curve-audit.mjs — the analytic DMG-01 audit, BEFORE section committed (--rule=whole on the pre-edit bestiary) + Wave 1 gates</name>
  <files>tools/damage-curve-audit.mjs, tools/damage-curve-audit-output.txt</files>
  <read_first>
    - engine/combat.js lines 2541-2552 (the hero-branch formula: `f.lvl * f.lvl + (f.dmgBonus || 0) + (f.sp && f.sp.dmg ? rollDice(rng, f.sp.dmg) : rng.d(6))`, halvings, then the crit doubling of the WHOLE sum — the BEFORE rule) and 2460-2472 (member twin)
    - engine/difficulty.js lines 371-450 (`difficultyCurve(depth)` → `foePower`, `foeLvlBias`; `COMBAT_SCALE_FROM_DEPTH` 21 so `foePower` is 1.0 on floors 5–20 and a grace value < 1 on floors 2–4) and 460-472 (`foeDmgBonusFor(lvl, curve)` = `round((foePower - 1) × lvl²)`, 0 at foePower 1, negative on grace floors)
    - engine/combat.js lines 240-256 (`maxLvl = clamp(min(c.level, depth) + foeLvlBias, 1, 5)`; per-foe `lvl = clamp(maxLvl - (d4 === 1 ? 1 : 0), 1, 5)` — a tier-T row is reachable only at depth >= T and only by a hero of level >= T)
    - content/bestiary.js (every row: `sp.dmg` {n,sides,bonus} or absent → d6; `sp.atk`; `abilities`), content/foe-abilities.js (`kind`, `dmg` per descriptor — bolt/drain maxima), content/classes.js (`baseWP {base, dice}`, `gain[1..4]` dice), content/misc-tables.js line 14 (`THRESHOLDS` — level cap 5)
    - docs/class-pass/v17-p51-after-smoke.json (`cells[].p50Depth`, `cells[].meanLevel`; `meta.commit` 608a0e5) — the locked yardstick source
    - tools/bestiary-yardstick.mjs lines 150-185 (how a tool already mirrors the foe formula and prints a markdown table; `HERO_HP` composite — NOT the bars this tool uses)
    - .planning/phases/52-foe-cadence-damage-curve/52-CONTEXT.md "Damage-curve audit (DMG-01)" bullets (the locked yardstick and flag rule)
  </read_first>
  <action>
    Create `tools/damage-curve-audit.mjs` (Node ESM, exit 0, no rng, no assertions). CLI: `--rule=whole|dice` (required; `whole` = today's rule, crit doubles the whole `lvl² + dmgBonus + dice` sum; `dice` = the Phase 52 rule, crit doubles the dice only), `--smoke=<path>` (default `docs/class-pass/v17-p51-after-smoke.json`), `--levels=a,b,c,d` (optional override of the four band levels; when given, the smoke file is not read). Per D-05 the yardstick is: four depth bands (the user's Filter 1–4, Wall 5–8, Breakaway 9–15, Endgame 16–20); band hero level = the median of `cells[].meanLevel` over cells whose `p50Depth` lies in the band, rounded to the nearest integer and clamped to 1..5; a band with no cells (expected for Breakaway/Endgame — the smoke's deepest p50 is 7) uses 5 (the level cap) and the output says so; two HP bars per level L computed from `CLASSES` for a Human (no race `wpMul`/`flatWP`): mean max HP = `baseWP.base + mean(baseWP.dice) + Σ_{k=1}^{L-1} mean(gain[k])` for "Magic User" (25 + 5.5 + …) and "Fighter" (50 + 4.5 + …) — print the level and both bars per band in the header. Row evaluation: every bestiary row at its tier T (array index + 1) in every band whose max depth is >= T; the row's effective hero level in that band is `max(T, bandLevel)` capped at 5 (a tier-T foe is only ever met by a hero of level >= T — cite the startCombat tiering); `dmgBonus` = the maximum of `foeDmgBonusFor(T, difficultyCurve(d))` over every floor d in the band with d >= T (exhaustive over floors); `levelBase` = `sp.strikesAs ? strikesAs² : T²` (reads the field Plan 02 introduces so the AFTER run needs no tool change); `diceMax` = `sp.dmg ? n × sides + bonus : 6`; `hitMax = levelBase + dmgBonus + diceMax`; `critMax` = rule whole → `2 × hitMax`, rule dice → `levelBase + dmgBonus + 2 × diceMax`; `boltMax` = max over the row's kit of the `dmg` maximum of `bolt`/`drain` descriptors (0 when no kit); `maxSingleHit = max(critMax, boltMax)`; `worstTurn = (sp.atk || 1) × critMax` (informational only, D-05 "a worst-turn column (all swings crit) is informational"). Verdict: FLAG when `maxSingleHit >= 0.6 × MUbar(level)`, note when `>= 0.6 × FighterBar(level)`. Every FLAG row also gets a `category` (computed, so Plan 02's decision table needs no hand arithmetic): `deep-tier` when T = 5; else `level-base` when the tier's DEFAULT die would be flagged too at that level under the same rule (`levelBase + dmgBonus + 2 × 6 >= 0.6 × MUbar` for rule=dice; `2 × (levelBase + dmgBonus + 6) >= …` for rule=whole) — i.e. the `lvl²` base, not this row's dice, trips the flag; else `row-dice` (the row's own `sp.dmg` is what trips it) with a `wouldBeTrim` column = the largest same-family notation that clears the bar under the rule (search order: bonus down to 0 first, then sides 12→10→8→6, then n down to 1; never below `1d6+0`; print `none` if even `1d6+0` would not clear — which by construction cannot happen for a row-dice row). A bolt-driven flag (`boltMax > critMax`) prints `bolt:<id>` in the category column instead.

    Output (plain text, deterministic, no timestamps/hashes): first line exactly `# damage-curve-audit — rule=<whole|dice>`; a header block naming the rule in words, the smoke source (path + its `meta.commit`) or the `--levels` override, and the per-band level + bars; one markdown table per band (columns: tier, type, name, dice, atk, levelBase, dmgBonus, hitMax, critMax, boltMax, maxSingleHit, worstTurn, MU bar, FT bar, % of MU bar, verdict); then a `## Flagged (rule=<rule>)` section listing every FLAG row as `band | tier | type | name | dice | maxSingleHit | MU bar | % | category | wouldBeTrim` and ending with `FLAGGED: <n> rows across <m> row×band cells (deep-tier <a>, level-base <b>, row-dice <c>, bolt <d>)` and `NOTED: <n> rows`. Also print `## Herman` with the two Herman rows' hitMax/critMax per band (the SC4 anchor — under rule=whole tier-4 crit must read 82 and tier-5 100).

    Run `node tools/damage-curve-audit.mjs --rule=whole > tools/damage-curve-audit-output.txt` (the BEFORE section; Plan 02 appends the `--rule=dice` AFTER section below it). Re-run and diff — empty.

    Then the Wave 1 gates: `npm test` (fail 0 — paste the pass/fail totals verbatim into the SUMMARY), `npm run build:www` (exit 0), the fence checks (`git diff --stat <phase-start-sha> -- engine/ test/parity/` empty; `node tools/comment-only-diff.mjs <phase-start-sha> content` identical; master hash unchanged).
  </action>
  <verify>
    <automated>node tools/damage-curve-audit.mjs --rule=whole > "$TMPDIR/dca-check.txt" && diff "$TMPDIR/dca-check.txt" tools/damage-curve-audit-output.txt && grep -c '^# damage-curve-audit — rule=whole' tools/damage-curve-audit-output.txt && npm test 2>&1 | tail -12 && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/damage-curve-audit.mjs --rule=whole; echo $?` prints 0; re-run diff against the committed file is empty
    - `head -1 tools/damage-curve-audit-output.txt` prints `# damage-curve-audit — rule=whole`; `grep -c '^# damage-curve-audit — rule=dice' tools/damage-curve-audit-output.txt` prints 0 (the AFTER section is Plan 02's)
    - `grep -c '^FLAGGED: [0-9]* rows across [0-9]* row×band cells (deep-tier [0-9]*, level-base [0-9]*, row-dice [0-9]*, bolt [0-9]*)' tools/damage-curve-audit-output.txt` prints 1 and `grep -c '^## Herman' tools/damage-curve-audit-output.txt` prints 1; the `## Herman` block shows a tier-4 critMax of 82 and a tier-5 critMax of 100 under rule=whole (quote both lines in the SUMMARY)
    - the header names the smoke source `docs/class-pass/v17-p51-after-smoke.json` and commit `608a0e5`, and prints four band levels + eight bars (`grep -c 'Filter\|Wall\|Breakaway\|Endgame' tools/damage-curve-audit-output.txt` >= 4)
    - `node tools/damage-curve-audit.mjs --rule=dice --levels=3,3,5,5 | head -1` prints `# damage-curve-audit — rule=dice` (the override path works; output not committed)
    - `grep -c 'node:test' tools/damage-curve-audit.mjs` prints 0
    - `npm test` reports fail 0; `npm run build:www` exits 0
    - `git diff --stat <phase-start-sha> -- engine/ test/parity/` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The BEFORE damage-curve reading is committed and reproducible; the tool already reads `strikesAs` so Plan 02 can produce the AFTER section without touching it; the Wave 1 tree is green with zero engine bytes changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (offline, single-process) | This plan adds tests and dev-only report tools; nothing ships (`tools/` is outside `tools/build-www.mjs`'s copy list), no network, no user input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Tampering | test/parity/prototype-master.js.txt | high | mitigate | `git hash-object` = a1f4d0dc… asserted in every task's acceptance criteria |
| T-52-02 | Repudiation | tools/*-output.txt (committed readouts) | low | mitigate | outputs are deterministic (re-run diff empty) and carry no timestamps; the SUMMARY records the phase-start commit |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (zero-dependency tools); nothing to audit |
</threat_model>

<verification>
- `node --test test/unit/foe-cadence.test.js` green with the CAD-01/02/03 named tests.
- `tools/cadence-audit-output.txt` and `tools/damage-curve-audit-output.txt` exist, are byte-reproducible, and carry the INVARIANT HOLDS lines / the FLAGGED summary + Herman 82/100 anchors.
- `npm test` fail 0; `npm run build:www` green; engine/parity fence untouched; content comment-only.
</verification>

<success_criteria>
- SC1, SC2 and the unit half of SC3 are pinned on the untouched engine (they hold today; the pins guard them).
- DMG-01's BEFORE reading exists before any engine byte moves; Plan 02 can append AFTER with the same tool.
- Zero engine bytes: `git diff --stat 3415b43..HEAD -- engine/ test/parity/` empty.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (all three plans)

| Artifact | Plan | Kind |
|---|---|---|
| `test/unit/foe-cadence.test.js` — tests named `CAD-01 (SC1): …` (x4), `CAD-02 (SC2): …` (>= 3), `CAD-03 (SC3): …` (x2) | 01 | new test file |
| `content/foe-abilities.js` — six comment lines containing `an ability turn replaces every swing` | 01 | comment-only content touch |
| `tools/cadence-audit.mjs` + `tools/cadence-audit-output.txt` | 01 | report tool + output |
| `tools/damage-curve-audit.mjs` + `tools/damage-curve-audit-output.txt` (BEFORE section: `# damage-curve-audit — rule=whole`; AFTER section appended by Plan 02: `# damage-curve-audit — rule=dice`) | 01 / 02 | report tool + output |
| `engine/combat.js#foeLevelBase(f)` (exported) — `sp.strikesAs² else lvl²` | 02 | new engine symbol |
| `content/bestiary.js` — `sp.strikesAs: 5` on both Herman rows (the flat-25 `dmg` notation removed) | 02 | content change |
| `tools/bestiary-yardstick.mjs` — foe-side formula reads `strikesAs`; `content/BESTIARY-REBALANCE.md` yardstick AFTER block regenerated | 02 | tool + doc block |
| `test/unit/foe-crit-curve.test.js` — DMG-02 pins (hero/member/pursuit/Soldier crit = dice doubled; Herman strikesAs; crit draws 2; pipeline order; foeLevelBase) | 02 | new test file |
| `test/unit/content-tables.test.js` — Herman strikesAs content pin | 02 | test |
| `test/parity/fixtures/action-script.combat.json` — `+52` on every measured moved holder (predicted: `#lose` only) | 02 | fixture record |
| `test/parity/divergence-records.test.js` — "DMG-02: the holders declaring Phase 52 are exactly the measured moved set" guard | 02 | test |
| `test/parity/FIXTURE-INVENTORY.md` — `## Phase 52: crit doubles the dice + Herman strikes as a level five (DMG-02) — measured moved set, declared per site` | 02 | doc section |
| `tools/initiative-fixture-scan-output.txt` — overwritten with the Phase 52 AFTER run (Part A lines unchanged) | 02 | output |
| `content/BESTIARY-REBALANCE.md` — `### Phase 52 addendum (DMG-02, 2026-09-20)` (Herman before/after rows; still-flagged fix-or-rule table) | 02 / 03 | doc section |
| `docs/DIFFICULTY-RETUNE.md` — `### v1.7 · Phase 52 — cadence & damage curve` under `## v1.7 tuning pass (Phases 51–54)` with `#### BEFORE — by reference …`, `#### AFTER — commit …`, `#### Reading (BEFORE → AFTER)`, the cadence quote, the flagged rows | 03 | ledger H3 |
| `docs/class-pass/v17-p52-after-smoke.json` | 03 | bot readout |
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/52-foe-cadence-damage-curve/52-01-SUMMARY.md` when done (template: `$HOME/.claude/gsd-core/templates/summary.md`, in the style of `51-01-SUMMARY.md`). Record verbatim: the phase-start SHA, the `npm test` totals, the `build:www` exit, the comment-only-diff verdict, the cadence INVARIANT lines with their sampled counts, the damage audit's FLAGGED total and the Herman 82/100 lines. End with `## Human verification (deferred to end of run)` — for this plan: "None owed — measurement and pins only, zero behaviour change" (the Phase 55 device checks are owed by Plans 02/03).
</output>
