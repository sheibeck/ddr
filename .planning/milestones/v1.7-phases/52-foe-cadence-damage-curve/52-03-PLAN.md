---
phase: 52-foe-cadence-damage-curve
plan: 03
type: execute
wave: 3
depends_on: ["52-02"]
files_modified:
  - content/BESTIARY-REBALANCE.md
  - content/bestiary.js
  - tools/damage-curve-audit-output.txt
  - docs/DIFFICULTY-RETUNE.md
  - docs/class-pass/v17-p52-after-smoke.json
autonomous: true
requirements: [CAD-03, DMG-01, DMG-02]
tags: [bot-readout, measurement-gate, damage-curve, ledger, bestiary]

must_haves:
  truths:
    - "Every row the AFTER audit still flags has an explicit disposition line in `content/BESTIARY-REBALANCE.md`'s Phase 52 addendum — a before/after fix row, a 'left as a deliberate deep-tier threat (Endgame band)' ruling (tier 5), or a 'level-base — Phase 54 curve dial' / 'row-dice — handed to Phase 54 TUNE-08' ruling — per the Plan 02 checkpoint; nothing is silently skipped (D-07, SC4)"
    - "If the ruling trims any row, the trimmed rows are re-measured clean by a fresh `--rule=dice` audit run committed as the final AFTER section, the yardstick AFTER block is regenerated in the same commit, and the parity fence is re-checked (tier-1 fixture-exposed rows are never trimmed)"
    - "`docs/DIFFICULTY-RETUNE.md` carries `### v1.7 · Phase 52 — cadence & damage curve` under `## v1.7 tuning pass (Phases 51–54) — per-phase bot readouts` with: the rule changes, the identical parameters, `#### BEFORE — by reference` (Phase 51's AFTER, commit 608a0e5, reused not re-run), `#### AFTER — commit <sha>` holding the three transcripts verbatim, `#### Reading (BEFORE → AFTER)` table, the cadence-audit readout (SC3/CAD-03) and the damage-audit flagged rows BEFORE/AFTER (SC4/DMG-01) (D-03, D-06, D-11)"
    - "`docs/class-pass/v17-p52-after-smoke.json` exists with 143 cells, seeds 5, `meta.commit` = the AFTER commit; the ledger's last H2 is still `## v1.2 retune (Phase 27)` (heading pins green)"
    - "`npm test` fail 0, `npm run build:www` green, master hash unchanged, `engine/` untouched in this wave"
  artifacts:
    - path: "content/BESTIARY-REBALANCE.md"
      provides: "Phase 52 addendum completed: `#### Still flagged after the fixes (rule=dice) — ruled` table with one disposition per row"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "the Phase 52 H3 (BEFORE by reference, AFTER, Reading, cadence quote, flagged rows)"
    - path: "docs/class-pass/v17-p52-after-smoke.json"
      provides: "the AFTER 143-cell class-pass smoke"
    - path: "tools/damage-curve-audit-output.txt"
      provides: "final AFTER section (re-run only if the ruling changed bestiary numbers)"
  key_links:
    - from: "docs/DIFFICULTY-RETUNE.md Phase 52 H3"
      to: "tools/cadence-audit-output.txt + tools/damage-curve-audit-output.txt + docs/class-pass/v17-p52-after-smoke.json"
      via: "verbatim quotes and the reading table"
      why: "Phase 54's four-band retune inherits this as its baseline; the ledger must be the one place all three readouts meet"
---

<objective>
Close the measurement gate for Phase 52: apply the checkpoint ruling to the still-flagged rows (fix rows or explicit rulings — never silent), take the AFTER bot readout under Phase 51's exact flags on the finished tree, and write the Phase 52 ledger section in `docs/DIFFICULTY-RETUNE.md` with the BEFORE (by reference to Phase 51's AFTER, commit 608a0e5) → AFTER reading, the cadence-audit quote (CAD-03/SC3) and the damage-curve flagged rows BEFORE/AFTER (DMG-01/SC4).

Purpose: the sequencing gate says the four-band retune (Phase 54) is measured only after the earlier rule phases are on master with a known baseline; this plan produces that baseline and the human-readable record of what Phase 52 did to the curve.

Output: `content/BESTIARY-REBALANCE.md` (ruled table, and fix rows if the ruling trims), possibly `content/bestiary.js` (only under a trim ruling), `tools/damage-curve-audit-output.txt` (final AFTER), `docs/DIFFICULTY-RETUNE.md` (Phase 52 H3), `docs/class-pass/v17-p52-after-smoke.json`.

Decisions implemented (52-CONTEXT.md, quoted): D-11 "Measurement gate: BEFORE = Phase 51's AFTER readout (commit `608a0e5`, identical flags — reused, not re-run); AFTER under identical flags (`tune-difficulty --seeds=200` solo + `--party`, `tune-classes --seeds 5 --workers 4 --out docs/class-pass/v17-p52-after-smoke.json`) after the cadence/damage edits, recorded under the Phase 52 H3 in `docs/DIFFICULTY-RETUNE.md` with a BEFORE → AFTER reading table." D-03 "Output committed (`tools/cadence-audit-output.txt`) and quoted in `docs/DIFFICULTY-RETUNE.md`." D-06 "the flagged rows quoted under `### v1.7 · Phase 52 — cadence & damage curve` in `docs/DIFFICULTY-RETUNE.md`." D-07 "any row still flagged afterwards gets its own before/after line in `content/BESTIARY-REBALANCE.md`, or an explicit 'left as a deliberate deep-tier threat' ruling if it is tier 5 (Endgame band) — recorded, never silently skipped."
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-foe-cadence-damage-curve/52-CONTEXT.md
@.planning/phases/52-foe-cadence-damage-curve/52-02-SUMMARY.md
@.planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md
</context>

<ground_rules>
- `engine/` is NOT touched in this wave (`git diff --stat <52-02-final-sha> -- engine/` empty at every commit). `content/bestiary.js` changes ONLY under a `trim-now`/`custom` ruling and never on a fixture-exposed row (Bat/Rat, Shriek, Viper, Ned — tier 1, unflagged by construction).
- Every number in the ledger is pasted from a command's stdout, never typed. The 200-seed `tune-difficulty` runs take ~70 s each; the `tune-classes` smoke (143 cells × 5 seeds, 4 workers) takes several minutes — run them in the foreground and wait; do not lower the seed counts.
- BEFORE is Phase 51's AFTER: do NOT re-run the BEFORE commands; cite the commit and the heading.
- Master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` after every task; commit trailers `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
</ground_rules>

<tasks>

<task type="auto">
  <name>Task 1: Apply the checkpoint ruling to the still-flagged rows — fix rows (if ruled) or explicit per-row rulings in the BESTIARY-REBALANCE Phase 52 addendum; final AFTER audit</name>
  <files>content/BESTIARY-REBALANCE.md, content/bestiary.js, tools/damage-curve-audit-output.txt</files>
  <read_first>
    - .planning/phases/52-foe-cadence-damage-curve/52-02-SUMMARY.md ("Decisions Made" — the checkpoint ruling: `trim-now`, `hand-to-54`, or the custom per-row list)
    - content/BESTIARY-REBALANCE.md — the Phase 52 addendum (`### Phase 52 addendum (DMG-02, 2026-09-20)`) and its `#### Still flagged after the fixes (rule=dice) — pending ruling` table (band, tier, type, name, dice, maxSingleHit, MU bar, %, category, wouldBeTrim); lines 389-402 (regeneration rule for the yardstick AFTER block); the Phase 27 addendum's before/after row style
    - tools/damage-curve-audit-output.txt (the AFTER section: `## Flagged (rule=dice)` + `FLAGGED: …` counts) and tools/damage-curve-audit.mjs header (`--rule=dice`)
    - content/bestiary.js (only if trimming: the exact `sp.dmg` notation of each ruled row; the header's Phase 18 DELIBERATE RULES CHANGE comment style at ~L62-64)
    - test/unit/content-tables.test.js lines 236-265 (the four fixture-exposed rows must stay byte-identical; the 54-row count) and lines 98-106, 165-200 (rows with pinned `sp.dmg` — Bat/Rat, Drake 2d10+4, Djinni 1d4, Krupke 1d6+2, Vampire 1d4: a trim on any of these needs its pin re-measured, never loosened silently)
    - test/parity/FIXTURE-INVENTORY.md Phase 52 section (the moved-set record — a bestiary trim on a non-exposed row moves nothing; state it)
  </read_first>
  <action>
    Rename the addendum's `#### Still flagged after the fixes (rule=dice) — pending ruling` heading to `#### Still flagged after the fixes (rule=dice) — ruled` and add a `Disposition` column filled per the ruling:
    - `deep-tier` rows (tier 5): `left as a deliberate deep-tier threat (Endgame band) — D-07` (always, regardless of ruling).
    - `level-base` rows: `level-base: the tier's default d6 crit (T² + 12) trips the bar too — the lvl² curve is Phase 54's four-band dial (52-CONTEXT Deferred Ideas); no row cliff` — Herman's post-fix 37 (= the tier-5 default by rulebook design) included, with the note that his own cliff (82 → 37) is fixed above.
    - `row-dice` rows: under `hand-to-54`: `row-dice: handed to Phase 54 TUNE-08 (per-creature decision) with wouldBeTrim <notation>`; under `trim-now` (or a custom list naming the row): apply the trim.
    - `bolt:<id>` rows (if any): `bolt: ability dice are a Phase 54 dial (ability cadence/threat), not a melee cliff` unless the ruling names them.

    If ANY row is trimmed: edit that row's `sp.dmg` in `content/bestiary.js` to the ruled notation with a `// DELIBERATE RULES CHANGE (Phase 52, DMG-02, 2026-09-20): dmg <old> -> <new> (damage-curve audit, row-dice flag; see content/BESTIARY-REBALANCE.md Phase 52 addendum)` comment line above it; leave `wp`/`atk`/`ar`/kits/note untouched; add a before/after row to the addendum's change table (`| Creature | Tier | Field | Before | After | critMax before | critMax after | MU bar |`); regenerate the yardstick AFTER block (`node tools/bestiary-yardstick.mjs` pasted between the markers, same commit); re-pin any `content-tables.test.js` `sp.dmg` literal for a trimmed row by re-reading the row (never loosen); re-run the audit and REPLACE the AFTER section: `sed -i '/^# damage-curve-audit — rule=dice/,$d' tools/damage-curve-audit-output.txt` then `node tools/damage-curve-audit.mjs --rule=dice >> tools/damage-curve-audit-output.txt` — every trimmed row must now be absent from `## Flagged (rule=dice)` ("re-measured clean"); re-run `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` (must be empty — no trimmed row is fixture-exposed) and `npm test` (fail 0). If NO row is trimmed, the AFTER section from Plan 02 stands and `content/bestiary.js` is untouched (`git diff --stat <52-02-final-sha> -- content/bestiary.js` empty).

    Close the addendum with a `Pointer:` line to `docs/DIFFICULTY-RETUNE.md`, `## v1.7 tuning pass (Phases 51–54)` → `### v1.7 · Phase 52 — cadence & damage curve` (written in Task 2) and, if rows were handed on, a `Handoff to Phase 54 (TUNE-08):` line listing them with their wouldBeTrim notations. Commit as `docs(52-03): still-flagged rows ruled (<ruling>)` (or `feat(52-03): … trimmed …` if bestiary numbers moved).
  </action>
  <verify>
    <automated>node tools/damage-curve-audit.mjs --rule=dice > "$TMPDIR/dca-final.txt" && diff "$TMPDIR/dca-final.txt" <(sed -n '/^# damage-curve-audit — rule=dice/,$p' tools/damage-curve-audit-output.txt) && node --test test/unit/bestiary-yardstick.test.js test/unit/content-tables.test.js test/parity/divergence-records.test.js && grep -c '^#### Still flagged after the fixes (rule=dice) — ruled' content/BESTIARY-REBALANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^#### Still flagged after the fixes (rule=dice) — ruled' content/BESTIARY-REBALANCE.md` prints 1 and `grep -c 'pending ruling' content/BESTIARY-REBALANCE.md` prints 0
    - every row of the AFTER `## Flagged (rule=dice)` table has a Disposition cell: `node -e` or a grep count of table rows in the ruled section equals the `FLAGGED: <n> rows` count of the final AFTER section (state both numbers in the SUMMARY); `grep -c 'left as a deliberate deep-tier threat (Endgame band)' content/BESTIARY-REBALANCE.md` equals the `deep-tier` count
    - the final AFTER section equals a fresh `--rule=dice` run (diff empty); under a trim ruling no trimmed creature name appears in the final `## Flagged (rule=dice)` table
    - `node --test test/unit/content-tables.test.js` green (54 rows, the four fixture-exposed rows byte-identical); `node tools/initiative-fixture-scan.mjs | diff - tools/initiative-fixture-scan-output.txt` empty
    - `git diff --stat <52-02-final-sha> -- engine/` empty; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `npm test` fail 0
  </acceptance_criteria>
  <done>Every still-flagged row has a recorded disposition; any trimmed row is re-measured clean with its before/after row; the tree is green and the fixture fence unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: AFTER bot readout under Phase 51's exact flags + the Phase 52 ledger H3 in docs/DIFFICULTY-RETUNE.md (BEFORE by reference, AFTER, Reading, cadence quote, flagged rows)</name>
  <files>docs/DIFFICULTY-RETUNE.md, docs/class-pass/v17-p52-after-smoke.json</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md lines 1177-1204 (the `## v1.7 tuning pass (Phases 51–54) — per-phase bot readouts` H2 preamble and the `### v1.7 · Phase 51 — initiative once` H3: "Rule change" paragraph, "Parameters (identical BEFORE/AFTER)" bullets, `#### BEFORE — commit … (phase start, no engine edit)`), 1581-1590 (`#### AFTER — commit <40-hex> (initiative once per fight)` transcript style), 1966-1995 (`#### Reading (BEFORE → AFTER)` table — copy its metric rows exactly), 1996 (`## v1.2 retune (Phase 27) — TUNE-05..07` — the pinned LAST H2; the new H3 goes ABOVE it, after the Phase 51 H3's Reading block)
    - test/unit/difficulty-retune-ledger.test.js lines 95-135 (the last-H2 pin; H3 order is pinned only inside the v1.2 section) and test/unit/class-pass-ledger.test.js (does not pin the v17 smoke files — confirm)
    - docs/class-pass/v17-p51-after-smoke.json `meta` (tool, commit 608a0e5, seeds 5, seedList i*7919+1, workers 4, maxActions 5000, startDepth 1, exploreBudget 50, runFlags {storeRoll: true}, cells 143) — the AFTER file must match on every meta key except `commit`
    - tools/cadence-audit-output.txt and tools/damage-curve-audit-output.txt (final), tools/tune-difficulty.mjs and tools/tune-classes.mjs headers (flags)
    - .planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md (how Phase 51 built its Reading table and the "3 largest movers" rows via a node one-liner over the two smoke JSONs)
  </read_first>
  <action>
    On the Task 1 commit (record its SHA — this is the AFTER commit), run exactly, in the foreground, capturing full stdout: `node tools/tune-difficulty.mjs --seeds=200` (solo, ~70 s), `node tools/tune-difficulty.mjs --seeds=200 --party` (~70 s), `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p52-after-smoke.json` (several minutes; its console readout is the third transcript). Verify `node -e "const j=require('./docs/class-pass/v17-p52-after-smoke.json');console.log(j.cells.length, j.meta.seeds, j.meta.commit)"` prints `143 5 <AFTER sha prefix>`.

    Insert into `docs/DIFFICULTY-RETUNE.md`, immediately after the Phase 51 H3's `#### Reading (BEFORE → AFTER)` block and its closing paragraph (i.e. immediately BEFORE the `## v1.2 retune (Phase 27) — TUNE-05..07` line), the new H3 `### v1.7 · Phase 52 — cadence & damage curve` with: **Rule change:** (a) cadence UNCHANGED and now pinned (one attack per foe per turn unless `sp.atk`; frenzy doubles; an ability turn replaces every swing — `test/unit/foe-cadence.test.js`); (b) a foe crit doubles the dice, not the `lvl² + dmgBonus + dice` sum, at the hero, member and pursuit sites (`engine/combat.js#foeLevelBase` + the three sites); (c) Herman `sp.strikesAs: 5` replacing the flat 25; (d) the checkpoint ruling on still-flagged rows (one sentence, pointer to the BESTIARY-REBALANCE addendum). **Parameters (identical BEFORE/AFTER):** the same three bullets as Phase 51 with `v17-p52-after-smoke.json`. `#### BEFORE — by reference: Phase 51 AFTER, commit 608a0e58046177c1dd1f225a40c3f63d402f31e1 (initiative once per fight)` — one paragraph: the BEFORE numbers are the Phase 51 AFTER transcripts above (solo p50/p90 4/6, party 5/7, class-smoke pooled mean 3.80 — quote the actual values from the Phase 51 Reading table), reused not re-run per the measurement gate. `#### AFTER — commit <40-hex AFTER sha> (crit doubles the dice; Herman strikes as a level five<; still-flagged rows trimmed if so>)` — the three transcripts verbatim in fenced blocks, each introduced by its command line exactly as Phase 51 did. `#### Reading (BEFORE → AFTER)` — the same metric rows as Phase 51's table (solo death-depth min/p50/p90/max, solo action-count p50, party depth, party action p50, solo top-3 causes, party top-3 causes, party stuck, class smoke pooled mean, mean of cell means, 3 largest movers up, 3 largest movers down) with BEFORE = the Phase 51 AFTER column and AFTER = the new numbers; a closing paragraph reading the direction in the tuning-proxy register only (no constants change here). Then `#### Cadence readout (CAD-03 / SC3)` quoting `tools/cadence-audit-output.txt` verbatim in a fenced block, preceded by the sentence that attacks per player action for Bat/Rat and China Wolf at depth 5 are <= the foe's `sp.atk` (2), <= 2× if frenzied, and that the re-run after the engine edit is byte-identical. Then `#### Damage-curve audit (DMG-01 / SC4)` quoting the BEFORE `## Flagged (rule=whole)` table + counts line and the final AFTER `## Flagged (rule=dice)` table + counts line from `tools/damage-curve-audit-output.txt`, the `## Herman` blocks from both sections, the yardstick statement (bands, levels, bars — copied from the audit header), and one paragraph naming the dispositions by category count with the pointer to `content/BESTIARY-REBALANCE.md` `### Phase 52 addendum`.

    Run `node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js` (the v1.2 H2 must still be last). Commit as `docs(52-03): Phase 52 AFTER bot readout + ledger H3 (BEFORE by reference to 608a0e5)`.
  </action>
  <verify>
    <automated>node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js && grep -c '^### v1.7 · Phase 52 — cadence & damage curve' docs/DIFFICULTY-RETUNE.md && node -e "const j=require('./docs/class-pass/v17-p52-after-smoke.json');const b=require('./docs/class-pass/v17-p51-after-smoke.json');const k=Object.keys(b.meta).filter(x=>x!=='commit'&&JSON.stringify(b.meta[x])!==JSON.stringify(j.meta[x]));console.log(j.cells.length===143&&j.meta.seeds===5&&k.length===0)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### v1.7 · Phase 52 — cadence & damage curve' docs/DIFFICULTY-RETUNE.md` prints 1, and it sits between `### v1.7 · Phase 51 — initiative once` and `## v1.2 retune (Phase 27) — TUNE-05..07` (`grep -n` line numbers in that order)
    - `grep -c '^#### BEFORE — by reference: Phase 51 AFTER, commit 608a0e58046177c1dd1f225a40c3f63d402f31e1' docs/DIFFICULTY-RETUNE.md` prints 1; `grep -c '^#### AFTER — commit [0-9a-f]\{40\} (crit doubles the dice; Herman strikes as a level five' docs/DIFFICULTY-RETUNE.md` prints 1; `grep -c '^#### Reading (BEFORE → AFTER)' docs/DIFFICULTY-RETUNE.md` prints 2 (Phase 51's and Phase 52's)
    - `grep -c '^#### Cadence readout (CAD-03 / SC3)' docs/DIFFICULTY-RETUNE.md` prints 1 and the H3 (extract with `sed -n '/^### v1.7 · Phase 52/,/^## v1.2/p'`) contains both `INVARIANT attacks per player action <= 2 (sp.atk 2, unfrenzied): HOLDS` lines
    - `grep -c '^#### Damage-curve audit (DMG-01 / SC4)' docs/DIFFICULTY-RETUNE.md` prints 1 and the H3 contains one `FLAGGED: ` line per rule (2) and both `## Herman` blocks' numbers (82/100 → 37/37)
    - the AFTER heading's 40-hex SHA equals `docs/class-pass/v17-p52-after-smoke.json`'s `meta.commit` (prefix match) and is the Task 1 commit; `git log --oneline 608a0e5..<sha> -- engine/ content/bestiary.js` lists the 52-02 Task 1 commit (and the 52-03 trim commit if any)
    - the `node -e` meta-parity check prints `true` (143 cells, seeds 5, every meta key except `commit` identical to v17-p51-after-smoke.json)
    - `node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js` green (the v1.2 H2 is still the last H2; 16 H3s in the v1.2 section)
    - the three transcripts appear verbatim (spot-check: the solo transcript's `Death-depth distribution:` line and the smoke's `Bot:` line are present inside the Phase 52 H3)
  </acceptance_criteria>
  <done>Phase 54 inherits a committed AFTER baseline taken on the finished Phase 52 tree under Phase 51's exact flags, with the BEFORE→AFTER reading, the cadence proof and the damage-curve flags all in one ledger section.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gates on the Wave 3 tree — npm test + build:www recorded verbatim, fence checks, SUMMARY with the SC1–SC5 proof map and the deferred device checks</name>
  <files>.planning/phases/52-foe-cadence-damage-curve/52-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md (the `## Human verification (deferred to end of run)` section shape and the Self-Check block)
    - .planning/phases/52-foe-cadence-damage-curve/52-01-SUMMARY.md and 52-02-SUMMARY.md (what each earlier plan recorded, so the phase-level closing numbers are consistent)
    - .planning/ROADMAP.md "### Phase 52: Foe Cadence & Damage Curve" (the five success criteria — map each to its proof for the orchestrator's VERIFICATION.md)
  </read_first>
  <action>
    Run and paste verbatim into the SUMMARY: `npm test 2>&1 | tail -12` (fail 0), `npm run build:www` (exit 0), `git hash-object test/parity/prototype-master.js.txt` (a1f4d0dc29782218d8e5aab65bc5989c33f917f0), `git diff --stat <52-02-final-sha> -- engine/ test/parity/harness/comparables.js` (empty), `node tools/cadence-audit.mjs | diff - tools/cadence-audit-output.txt` (empty), and `git log --oneline 3415b43..HEAD` (every Phase 52 commit). Write the SUMMARY with a `## Success criteria → proof` table mapping SC1–SC5 to exact test names / files / ledger headings: SC1 → `test/unit/foe-cadence.test.js` CAD-01 pins; SC2 → the CAD-02 Stalka pins; SC3 → the CAD-03 pin + `tools/cadence-audit-output.txt` + the ledger `#### Cadence readout (CAD-03 / SC3)`; SC4 → `tools/damage-curve-audit.mjs`, both `FLAGGED:` lines, Herman 82 → 37, the ruled table; SC5 → the BESTIARY-REBALANCE Phase 52 addendum rows + FIXTURE-INVENTORY Phase 52 + the DMG-02 guard test. State that `npm run boot:check` is environment-blocked on this machine and was not run as a gate. Commit the SUMMARY as `docs(52-03): complete phase 52 plan 03 — gates + summary`.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -12 && npm run build:www && git hash-object test/parity/prototype-master.js.txt && git diff --stat HEAD~3 -- engine/ test/parity/harness/comparables.js</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` reports fail 0 (totals pasted); `npm run build:www` exit 0 (pasted)
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat <52-02-final-sha> -- engine/ test/parity/harness/comparables.js` prints nothing
    - `grep -c '^## Human verification (deferred to end of run)' .planning/phases/52-foe-cadence-damage-curve/52-03-SUMMARY.md` prints 1 and the section lists the three Pixel 7 checks; `grep -c '^## Success criteria → proof' .planning/phases/52-foe-cadence-damage-curve/52-03-SUMMARY.md` prints 1
  </acceptance_criteria>
  <done>The Wave 3 tree is green and fenced; the SUMMARY carries every verbatim gate output and the SC1–SC5 proof map for the orchestrator's VERIFICATION.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none (offline, single-process) | Docs, a bot readout and (under a trim ruling) content numbers; no engine bytes, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-52-01 | Tampering | test/parity/prototype-master.js.txt | high | mitigate | hash asserted after every task |
| T-52-05 | Repudiation | ledger numbers typed rather than measured | medium | mitigate | transcripts pasted verbatim; smoke JSON meta-parity check; AFTER SHA = the commit the readout ran on |
| T-52-06 | Tampering | a trim on a fixture-exposed row moving parity silently | high | mitigate | initiative scan re-run diff empty + content-tables fixture-row pin + parity suite green |
| T-52-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- Every still-flagged row has a disposition; trimmed rows (if any) re-measured clean; bestiary untouched otherwise.
- `docs/DIFFICULTY-RETUNE.md` Phase 52 H3 present with BEFORE-by-reference, AFTER (three verbatim transcripts on the AFTER commit), Reading table, cadence quote, damage-audit flags; ledger heading pins green.
- `docs/class-pass/v17-p52-after-smoke.json` meta-parity with the Phase 51 AFTER smoke (modulo commit).
- `npm test` fail 0, `npm run build:www` green, master hash unchanged, `engine/` untouched in this wave.
</verification>

<success_criteria>
- SC3 and SC4 are committed in `docs/DIFFICULTY-RETUNE.md` (the cadence readout at <= sp.atk; the flagged rows before/after with every flag fixed or explicitly ruled).
- SC5: every damage-curve fix carries a before/after row; handed-on rows are listed for Phase 54 (TUNE-08).
- The measurement gate is closed: Phase 54 has a Phase 52 AFTER baseline under Phase 51's flags.
</success_criteria>

<artifacts_this_phase_produces>
See 52-01-PLAN.md's "Artifacts this phase produces" table (the phase-wide list). This plan produces: `content/BESTIARY-REBALANCE.md` `#### Still flagged after the fixes (rule=dice) — ruled` (+ fix rows and `content/bestiary.js` edits only under a trim ruling), the final AFTER section of `tools/damage-curve-audit-output.txt`, `docs/DIFFICULTY-RETUNE.md` `### v1.7 · Phase 52 — cadence & damage curve` with `#### BEFORE — by reference: …`, `#### AFTER — commit …`, `#### Reading (BEFORE → AFTER)`, `#### Cadence readout (CAD-03 / SC3)`, `#### Damage-curve audit (DMG-01 / SC4)`, and `docs/class-pass/v17-p52-after-smoke.json`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/52-foe-cadence-damage-curve/52-03-SUMMARY.md` when done (Task 3 writes it; style: `51-02-SUMMARY.md`). It must end with `## Human verification (deferred to end of run)` listing the Phase 55 Pixel 7 checks: (1) a Bat/Rat or China Wolf fight at depth 5 shows at most 2 foe attack lines between your actions; (2) a Stalka Beast turn is never two hits AND a bolt; (3) a Herman crit on floor 5 no longer one-shots a full-HP level-5 hero (CRIT line at most 37); plus, under a trim ruling, (4) the trimmed creatures still read as themselves in the fight log (identity check, subjective).
</output>