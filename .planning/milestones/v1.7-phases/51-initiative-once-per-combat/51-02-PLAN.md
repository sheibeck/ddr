---
phase: 51-initiative-once-per-combat
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - engine/combat.js
  - test/unit/foe-turn-draw-count.test.js
  - test/unit/combat.test.js
  - test/unit/fight-gate.test.js
  - test/unit/spell-utility.test.js
  - test/unit/identity-race.test.js
  - test/unit/identity-contract.test.js
  - test/unit/identity-combat.test.js
  - test/unit/normalizeTarget.test.js
  - test/determinism/foe-abilities.test.js
  - test/unit/afraid.test.js
  - test/unit/cast-refusals.test.js
  - test/unit/magic.test.js
  - test/unit/item-wiring.test.js
  - test/unit/freeze-pays-out.test.js
  - test/unit/abilities.test.js
  - test/parity/fixtures/action-script.combat.json
  - test/parity/divergence-records.test.js
  - test/parity/combat-parity.test.js
  - test/parity/fixture-inventory.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - tools/initiative-fixture-scan-output.txt
  - docs/DIFFICULTY-RETUNE.md
  - docs/class-pass/v17-p51-after-smoke.json
autonomous: true
requirements: [INIT-01]
tags: [engine, combat, initiative, parity-fixtures, draw-count-pins, bot-readout]

must_haves:
  truths:
    - "SC1: `rollInitiative` fires exactly once per combat (from the `fight` action); the per-round re-roll at engine/combat.js:1340 is gone, pinned by a zero-draw assertion in foe-turn-draw-count.test.js (CONTEXT: 'afterPlayerAction loses BOTH the rollInitiative(state, rng) re-roll and the if (state.combat.first === \"foe\") { foeTurn(...) } pre-emptive branch')"
    - "SC2: a scenario where the foe would have won a fresh second-round roll (a Samurai — forced foe on every re-roll) now alternates player/foe turns for the whole fight — foe-turn count equals player-action count + 1 for the foe-first opener, never two foe turns in a row — proven by a unit test that fails on the old engine (CONTEXT: 'a foe-first fight opens with the foe turn inside fight() (F, Y, F, Y…), a you-first fight is Y, F, Y, F…')"
    - "SC4: Samurai / slow / foresight / Acute Hearing / senses / Knight / Court Mage overrides still apply to the single roll, proven by combat.test.js initiative tests re-targeted at fight() as the single call site, reading combatJoined.first and combatJoined.why"
    - "SC5: the p.24 divergence is declared in test/parity/FIXTURE-INVENTORY.md with before/after; only the scan's MOVED SET fixtures (expected combat.json lose, lose-apprentice, lose-plain) carry Phase 51 records; every after/stateAfter value is copied from the scan's Part B output, never hand-typed; the prototype master hash is unchanged (CONTEXT: 'measured, never hand-typed')"
    - "The dice reach the shell through the EXISTING combatJoined event, extended with additive fields mine (d20), theirs (d20), why (samurai|slow|knight|courtMage|foreseen|acuteHearing|senses, omitted when the dice decided) — plus foe (the single live foe's name, omitted for a group) so the narration can name it; combatJoined fires exactly once per fight and no other event carries mine/theirs (CONTEXT: 'extended with additive fields: mine (d20), theirs (d20), why')"
    - "Draw-count pins are re-pinned to the new sequences (−2 d20 per round from round 2 onward), never loosened to ranges (CONTEXT: 'Pins are re-pinned, never loosened to ranges')"
    - "Knight-vs-big-foe is evaluated once at the single roll (the rule narrows to the opener; the divergence rationale records this narrowing explicitly)"
    - "The AFTER bot readout is captured under flags identical to Plan 01's BEFORE and recorded under the same 'v1.7 · Phase 51 — initiative once' heading (MEASUREMENT GATE)"
    - "npm test fail 0 and npm run build:www green at every commit; the bot plays the new rules (no run-option gating)"
  artifacts:
    - "engine/combat.js — resolveInitiative(state, rng) -> { first, mine, theirs, why }; rollInitiative(state, rng) -> first (wrapper, contract unchanged for direct-call tests); fight() pushes combatJoined { first, mine, theirs, why?, foe?, senses? }; afterPlayerAction's round advance is round++ only"
    - "test/unit/foe-turn-draw-count.test.js — Section 6 SC1 zero-draw pin; FULL_FIGHTS and CANON pins re-measured with Phase 51 provenance comments"
    - "test/unit/combat.test.js — initiative tests re-targeted at fight() (all seven why branches); SC2 alternation pin; combatJoined-once pin"
    - "test/parity/fixtures/action-script.combat.json — lose '24+31+51', lose-apprentice '31+51' (after/stateAfter regenerated), lose-plain new '51' action-path record"
    - "test/parity/divergence-records.test.js — INIT-01 MOVED SET assertion against tools/initiative-fixture-scan-output.txt"
    - "test/parity/FIXTURE-INVENTORY.md — `## Phase 51: initiative once per fight (INIT-01)` section"
    - "docs/DIFFICULTY-RETUNE.md — `#### AFTER — commit <hash>` + `#### Reading (BEFORE → AFTER)` under the Phase 51 H3; docs/class-pass/v17-p51-after-smoke.json"
  key_links:
    - "fight() -> resolveInitiative -> combatJoined{mine,theirs,why,foe}: Plan 03's narration reads exactly these field names"
    - "afterPlayerAction: round++ stays (the C.round === 1 rules and the scan's Part A predictor depend on it); the re-roll and the second foeTurn go"
    - "tools/initiative-fixture-scan-output.txt Part A must diff empty against the 51-01 commit's Part A; Part B supplies every record value"
    - "Tasks 1 and 2 land as ONE commit — the parity suite is red between them and the engine gate requires npm test fail 0 at every commit"
---

<objective>
Land the rule: initiative is rolled once per fight, `C.first` holds for the whole fight, the foe never takes two turns back to back. Re-pin every draw-count sequence the removed two-d20 re-roll used to consume, declare and regenerate exactly the measured MOVED SET, extend `combatJoined` with the dice (`mine`/`theirs`/`why`/`foe`) so Plan 03 can narrate them, and take the AFTER bot readout under Plan 01's flags.

Purpose: INIT-01. The user's on-device ruling (2026-09-19): the per-round re-roll let a foe act at the end of one round and again at the start of the next with no chance to respond; rounds must read as a steady you/them or them/you exchange. Deliberate canon p.24 divergence under the greenfield ruling — no dual path, no run option, the bot plays the new rules.

Output: engine cut + `resolveInitiative`; re-pinned tests + three new pins (SC1 zero-draw, SC2 alternation, combatJoined-once); three declared action-path records + inventory section + MOVED SET guard; AFTER readout rows in `docs/DIFFICULTY-RETUNE.md`.

Requirement traceability: INIT-01 (all tasks). INIT-02's engine half (the dice on `combatJoined`) is delivered in Task 1 so Plan 03 can consume it; INIT-02's narration is Plan 03.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-initiative-once-per-combat/51-CONTEXT.md
@.planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md
@engine/combat.js
@test/unit/foe-turn-draw-count.test.js
@test/unit/combat.test.js
@test/parity/fixtures/action-script.schema.md
@test/parity/FIXTURE-INVENTORY.md
@tools/initiative-fixture-scan-output.txt
</context>

<planner_measurements>
Dry run of the afterPlayerAction cut alone (throwaway worktree, discarded; the real edit also adds the event fields, which changes three deepStrictEqual shape pins on top). Exactly these tests went red — this is the re-pin worklist; `npm test` after Task 1's engine edit is the authoritative list:

- test/unit/foe-turn-draw-count.test.js: "CANON-01: a soaked strike costs one d20 and the fight continues — 6 draws for the whole action" (6 -> expect 4), "CANON-05: a slow all-miss action draws 5 vs the non-slow 4" (expect 3 vs 2), "CANON-03/04 are pure arithmetic ... (5 each ...)" (expect 3 each), the six FULL_FIGHTS rows seeds 14/17/303/8/127/1119 (seed 3 is a one-attack fight and stays 12), and "FID-02 restated post-260918-w4n: the seven FULL_FIGHTS totals (12/36/89/52/27/121/51) ..." (title + list).
- test/unit/combat.test.js: "playerStrike: Fridgian frenzy grants a second wild swing ..." (L419, asserts exhaustion), "playerStrike: natural armour soaks a blow ..." (L1102, exhaustion), "playerStrike: a critical ignores the soak (D-07) — no d20 drawn" (L1126, exhaustion), "playerStrike: slow — both dice above need ..." (L1160, exhaustion).
- test/unit/identity-race.test.js L193-205 and test/unit/identity-contract.test.js L1158-1170 (the Fridgian frenzy `seq = [5, 20, 20, 20, 15, 10, 20]` with `rng.draws === seq.length`).
- test/unit/normalizeTarget.test.js L232 (`rng1.draws` 6 -> measured).
- test/determinism/foe-abilities.test.js FULL pins `magical-t4` 45, `demons-t5` 51, `beasts-t5` 67 (per-visit pins were unaffected).
- test/parity: combat parity (lose), (lose-apprentice), (lose-plain), the full-suite ENG-05 gate — fixed by Task 2's records, nothing else.
Additionally the event-shape pins will go red once Task 1 adds the dice: test/unit/fight-gate.test.js L118-130 and test/unit/spell-utility.test.js L204-228 (three deepStrictEqual on combatJoined).

Predictor result (Plan 01 scan, pre-edit): MOVED SET (3) = combat.json lose, lose-apprentice, lose-plain, each with first live-fight round advance at action 1. Part A must print identically after the edit.
</planner_measurements>

<tasks>

<task type="auto">
  <name>Task 1: engine cut + resolveInitiative + combatJoined dice; every draw-count pin re-measured; SC1/SC2/once pins added (do NOT commit until Task 2 is green)</name>
  <files>engine/combat.js, test/unit/foe-turn-draw-count.test.js, test/unit/combat.test.js, test/unit/fight-gate.test.js, test/unit/spell-utility.test.js, test/unit/identity-race.test.js, test/unit/identity-contract.test.js, test/unit/identity-combat.test.js, test/unit/normalizeTarget.test.js, test/determinism/foe-abilities.test.js, test/unit/afraid.test.js, test/unit/cast-refusals.test.js, test/unit/magic.test.js, test/unit/item-wiring.test.js, test/unit/freeze-pays-out.test.js, test/unit/abilities.test.js</files>
  <read_first>
    - .planning/phases/51-initiative-once-per-combat/51-CONTEXT.md (every bullet under "Turn structure with a fixed initiative" and "The initiative line" — locked; the `why` value list; "Pins are re-pinned, never loosened")
    - engine/combat.js lines 111-176 (`knightFacesBigFoe` + `rollInitiative` and their DELIBERATE RULES CHANGE paragraphs — the Phase 24 paragraph claims the Knight check is re-evaluated each round and that afterPlayerAction increments C.round before re-rolling; both claims become false and must be rewritten), 259-295 (startCombat's `encounterStarted` flags `knightBigFoe`/`courtMageTalksFirst` — UNCHANGED, they are the pre-Fight! warning Phase 24 tests pin), 364-394 (`fight()` JSDoc + the `rollInitiative` call + the `combatJoined` push with the Phase 40 `senses` spread), 447-466 (the foe-first opener inside fight), 945-955 (`C.tracked && C.round === 1` — comment touch only), 995-1002 (flee's own `C.round++` — untouched), 1300-1356 (`afterPlayerAction` — the JSDoc's "roll fresh initiative — with a second foe turn" sentence and the block to cut), 1440-1460 and 1520-1530 (`pickMemberAbility(sheet, ally, C.round, ...)` / `round === 1` — read-only, unaffected)
    - test/parity/prototype-master.js.txt lines 1865-1876 (canon `rollInitiative` + `initNote` — READ ONLY, never edited; the `Initiative — you N, them M` shape Plan 03 matches)
    - test/unit/foe-turn-draw-count.test.js lines 1-60 (PROVENANCE header + `countingRng`), 234-340 (`runFullFight` + FULL_FIGHTS rows with their re-measure comment style), 341-470 (Section 3 CANON pins), 620-660 (the "restated" totals test), 658-706 (Section 5 opener-only table — unaffected but read to place Section 6 after it)
    - test/unit/combat.test.js lines 90-115 (`fixedState`/`fixedFoe`/`fixedCombat` helpers; `fakeRng` throws "sequence exhausted" on underflow), 300-332 (the initiative tests to re-target), 370-465 (the four multi-round playerStrike tests whose comments cite the fresh re-roll), 900-915 and 983-996 (parley / sing sequences with a dead trailing 15,10), 1100-1170 (soak / crit / slow sequences asserting exhaustion), 1475-1490 (the combatJoined-reading Bubble test — unaffected, shows the fight()+events pattern)
    - test/unit/fight-gate.test.js lines 100-135; test/unit/spell-utility.test.js lines 150-230 (the direct rollInitiative asserts stay valid via the wrapper; the three fight() deepStrictEqual shapes need the new fields)
    - test/unit/identity-race.test.js lines 190-206; test/unit/identity-contract.test.js lines 395-430 and 1154-1172; test/unit/identity-combat.test.js lines 110-182; test/unit/normalizeTarget.test.js lines 205-236; test/determinism/foe-abilities.test.js lines 215-275 and 296-330
    - Comment/sequence hygiene sites (tests that still pass but carry a dead trailing initiative pair or a stale "fresh initiative" comment): test/unit/afraid.test.js lines 130-145, 390-400, 425-435, 465-475, 485-495, 530-540; test/unit/cast-refusals.test.js lines 130-136, 205-220; test/unit/magic.test.js lines 320-332, 465-475, 525-570; test/unit/item-wiring.test.js lines 140-150; test/unit/freeze-pays-out.test.js lines 130-175; test/unit/abilities.test.js lines 115-122, 640-648
  </read_first>
  <action>
    ENGINE (engine/combat.js) — per D-INIT-01 decisions in CONTEXT "Turn structure with a fixed initiative":

    1. Add exported `resolveInitiative(state, rng)` returning `{ first, mine, theirs, why }`: the two d20s (`mine`, `theirs`) are ALWAYS drawn first (same two draws, same order — the opener's draw count is byte-identical), the ternary is unchanged, `c.foresight` is still consumed, `C.first` is still assigned. `why` falls out of the same branch order: when the forced-foe branch is taken, the first true of `samurai` -> "samurai", `slow` -> "slow", `knightBig` -> "knight", `courtMage` -> "courtMage"; else `foreseen` -> "foreseen"; else Acute Hearing -> "acuteHearing"; else when `c.senses` is truthy and the dice gave "you" -> "senses" (this is exactly the condition of the existing `senses: true` spread, so the two never disagree); otherwise `why` is `undefined` (the dice decided — Claude's discretion: omitted, not null). Keep `rollInitiative(state, rng)` exported as a one-line wrapper returning `resolveInitiative(state, rng).first` so every direct-call test (identity-combat, identity-contract, spell-utility, combat.test.js) keeps its string contract. Rewrite the Phase 24 paragraph of the JSDoc: Knight-vs-big-foe is evaluated ONCE at the single roll (a Knight facing a live maxWP >= 20 foe at Fight! time gives the foe the opening turn — the rule narrows to the opener), and Court Mage's `C.round === 1` is now trivially true at the only call site (the condition stays, harmless; the comment says so). Add a `DELIBERATE RULES CHANGE (Phase 51, INIT-01, 2026-09-20)` paragraph on `resolveInitiative`/`rollInitiative` and on `afterPlayerAction`, in the style of the Phase 24/31/40 ones. Update `knightFacesBigFoe`'s JSDoc sentence about per-round re-evaluation to say it is read once, at the single roll.

    2. `fight()`: call `resolveInitiative` instead of `rollInitiative`; push `combatJoined` as `{ type: "combatJoined", first, mine, theirs }` plus additive spreads: `why` only when defined; `foe` = the single live foe's `name` only when `liveFoes(state).length === 1` (omitted for a group — Plan 03 says "them"); the existing `senses: true` spread unchanged. Everything after the push (phobia trigger, `combatInDark`, the foe-first opener and its cleared check) is untouched. Update the JSDoc sentence that describes the roll.

    3. `afterPlayerAction`: inside `if (!state.dead && state.combat) { ... }` keep ONLY `state.combat.round++`. Delete the re-roll call, the `if (state.combat.first === "foe")` pre-emptive `foeTurn` + its cleared check, and the ROUND-COUNT FIX comment (now moot — say so in the new Phase 51 paragraph: a round is one full cycle, the counter advances once per player action, and the foe's per-round turn is the one already run above). Rewrite the JSDoc's last sentence ("then (if nobody died) advance the round — initiative holds for the whole fight, Phase 51"). Touch the comment at the `C.tracked && C.round === 1` site to note that `C.round` now only ever advances in afterPlayerAction/flee and initiative no longer re-rolls, so "round 1" means "before the first full cycle" exactly as before.

    Nothing else in the engine changes: no new state field (nothing to carve out of `comparables.js` — event payloads are not compared), no `initNote`, no run option. `knightFacesBigFoe` stays exported and used by startCombat's `encounterStarted` flag.

    PINS — run `npm test` right after the engine edit; every red test is a pin to re-derive from the failure's actual value (measured, never hand-computed), with a `Phase 51 (INIT-01, 2026-09-20)` provenance comment in the file's existing style. Never loosen a pin to a range or an inequality. Worklist (planner dry run; the test run is authoritative):
    - foe-turn-draw-count.test.js: re-pin the six FULL_FIGHTS totals (seed 3 stays 12) via `runFullFight`'s reported values; re-pin the three Section 3 CANON tests (each loses exactly the two initiative d20s: 6->4, 5 vs 4 -> 3 vs 2, 5 each -> 3 each) and drop the now-dead trailing `15, 10` from their sequences; update the "FID-02 restated" test title + list to the new totals. ADD Section 6 "INIT-01 (Phase 51)": (SC1) a `countingRng` micro pin — a missed hero strike (20) followed by one foe miss (20) is exactly 2 draws for the whole `playerStrike` call, `state.combat.round` goes 1 -> 2, and `afterPlayerAction`'s round advance itself draws ZERO (assert `rng.draws === 2` and a third `d()` throws on the fakeRng).
    - combat.test.js: re-target the two `rollInitiative:` tests (L308-330) at `fight()` — build `fixedCombat([fixedFoe()], { pending: true })`, call `fight(state, fakeRng([...]), [])`, read the `combatJoined` event and assert `first` AND `why`: Samurai `[10, 1, 20]` -> first "foe", why "samurai" (the trailing 20 is the opener's foe miss); Samurai + foresight `[1, 10]` -> "you", why "foreseen", foresight consumed; Samurai + `senses: 1` `[10, 1]` -> "you", why "senses" and `senses: true` present; Samurai + `senses: 0` `[19, 1, 20]` -> "foe", why "samurai"; add the missing branches in the same test group: Fridgian (`race: "Fridgian"`) -> why "slow"; `skills: { "Acute Hearing": 1 }` with losing dice `[1, 10]` -> "you", why "acuteHearing"; Knight vs `fixedFoe({ maxWP: 20 })` -> "foe", why "knight"; Court Mage (round 1) -> "foe", why "courtMage"; a plain hero with `[14, 9]` -> "you" and no `why` key (use 14/9, not 15/10, so the dead-pair grep below stays meaningful); and `foe: "Target"` present on every single-foe case, absent with two foes. ADD (SC2) "INIT-01 (SC2): a Samurai's forced-foe fight alternates strictly — foe turns = player actions + 1, never two in a row": Samurai vs `fixedFoe({ wp: 999, maxWP: 999 })`, `fight` with `[10, 1, 20]` (foe opener miss) then three `playerStrike` calls each on `[20, 20]` (hero miss, foe miss) sharing ONE fakeRng of exactly 9 values; collect all events; assert `foeMissed`/`struckByFoe` count === 4 (3 + 1), `strikeMissed` count === 3, in the concatenated event stream no two foe-turn events occur without a hero `strikeMissed`/`struck` between them, the rng is exhausted after the third strike (a 10th draw throws), and `state.combat.round === 4`. ADD (once pin) "INIT-01: a 3-round fight emits exactly one combatJoined and no other event carries mine/theirs": reuse the SC2 stream — `events.filter(type === "combatJoined").length === 1`, `events.every(e => e.type === "combatJoined" || (!("mine" in e) && !("theirs" in e)))`. Re-derive the sequences and comments of the four multi-round tests at L375, L398, L419 (frenzy: `[5, 20, 20, 20]`, exhaustion assert kept), L458 and the exhaustion tests at L1102/L1126/L1160 (drop the trailing `15, 10`; the "exactly N draws" messages get the new N). Drop the dead trailing pair from the parley (L909) and sing (L990) sequences and reword their comments.
    - fight-gate.test.js L118-130 and spell-utility.test.js L204-228: re-pin the three `deepStrictEqual` combatJoined shapes to include `mine`, `theirs`, `foe` (the roster's single foe name — in fight-gate it is whatever `startCombat` rolled at that seed; read it from the failure) and `why` where an override fired ("senses" for the senses:1 win; none for the plain win and the foe win). Keep the "no senses key" assertion.
    - identity-race.test.js L199 and identity-contract.test.js L1165: `seq = [5, 20, 20, 20]` (4 draws — frenzy roll, two swings, one foe miss); normalizeTarget.test.js L232: the measured count (expect 4); determinism/foe-abilities.test.js FULL pins for magical-t4 / demons-t5 / beasts-t5: the measured totals (attacks/outcome/foeNames unchanged unless the run says otherwise — if an outcome flips, record it in the row comment as the measured consequence of fewer foe turns).
    - Comment-only touches (still green): identity-combat.test.js L160-172 and identity-contract.test.js L415-425 (the Court Mage "round 2+ rolls normally" branch is now unreachable from `fight()` — the function-level contract is retained, say so); abilities.test.js L119/L644; afraid / cast-refusals / magic / item-wiring / freeze-pays-out sequences that carry a dead trailing initiative pair: delete the pair and reword the comment ("no round-advance draws — initiative is rolled once, Phase 51"). Grep `initiative` in test/unit to find each; leave sequences that are consumed BEFORE afterPlayerAction alone.

    Do NOT commit at the end of this task — the parity suite is red until Task 2's records land; Tasks 1 and 2 are one commit.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/foe-turn-draw-count.test.js test/unit/combat.test.js test/unit/fight-gate.test.js test/unit/spell-utility.test.js test/unit/identity-race.test.js test/unit/identity-contract.test.js test/unit/identity-combat.test.js test/unit/normalizeTarget.test.js test/determinism/foe-abilities.test.js test/unit/afraid.test.js test/unit/cast-refusals.test.js test/unit/magic.test.js test/unit/item-wiring.test.js test/unit/freeze-pays-out.test.js test/unit/abilities.test.js 2>&1 | grep -E "^# (tests|pass|fail) " ; awk '/^export function afterPlayerAction/,/^}/' engine/combat.js | grep -c "Initiative("</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^export function afterPlayerAction/,/^}/' engine/combat.js | grep -c "Initiative("` prints 0 (no initiative call in afterPlayerAction)
    - `awk '/^export function afterPlayerAction/,/^}/' engine/combat.js | grep -c "foeTurn(state, rng, events)"` prints 1 (one foe turn per player action, never a second)
    - `awk '/^export function afterPlayerAction/,/^}/' engine/combat.js | grep -c "round++"` prints 1 (the round advance stays)
    - `awk '/^export function fight\(/,/^}/' engine/combat.js | grep -c "resolveInitiative(state, rng)"` prints 1 and `grep -c "^export function resolveInitiative" engine/combat.js` prints 1 and `grep -c "^export function rollInitiative" engine/combat.js` prints 1
    - `grep -c "DELIBERATE RULES CHANGE (Phase 51, INIT-01, 2026-09-20)" engine/combat.js` prints at least 2 (the roll and afterPlayerAction)
    - `grep -c "re-evaluated every round" engine/combat.js` prints 0 (the Knight paragraph is rewritten)
    - `grep -c "type: \"combatJoined\", first, mine, theirs" engine/combat.js` prints 1
    - `grep -n "INIT-01 (SC2)" test/unit/combat.test.js` non-empty; `grep -n "INIT-01 (SC1)" test/unit/foe-turn-draw-count.test.js` non-empty; `grep -c "exactly one combatJoined" test/unit/combat.test.js` prints 1
    - `grep -c "\"acuteHearing\"\|\"courtMage\"\|\"knight\"\|\"slow\"\|\"samurai\"\|\"foreseen\"\|\"senses\"" test/unit/combat.test.js` prints at least 7 (every why branch asserted)
    - `grep -c "15, 10\]" test/unit/combat.test.js` prints 0 (no dead trailing initiative pair remains in that file's sequences)
    - `node --test test/unit/foe-turn-draw-count.test.js test/unit/combat.test.js test/unit/fight-gate.test.js test/unit/spell-utility.test.js test/unit/identity-race.test.js test/unit/identity-contract.test.js test/unit/identity-combat.test.js test/unit/normalizeTarget.test.js test/determinism/foe-abilities.test.js` prints `# fail 0`
    - No `assert.ok(draws >= ...)`, `assert.ok(... <= ...)` or range form replaced an exact draw pin: `git diff -U0 -- test/unit/foe-turn-draw-count.test.js test/determinism/foe-abilities.test.js | grep -c "^+.*assert.ok(.*draws.*[<>]"` prints 0
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `git diff --stat -- test/parity/harness/comparables.js` is empty (no carve-out — event payload fields are not compared)
  </acceptance_criteria>
  <done>The engine rolls initiative exactly once per fight and never gives the foe a second consecutive turn; every unit/determinism draw pin is re-measured (not loosened); SC1/SC2/once pins exist and pass; the parity suite is the only red left (until Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: re-run the scan; declare and regenerate exactly the MOVED SET; inventory section; MOVED SET guard; ONE commit with Task 1</name>
  <files>tools/initiative-fixture-scan-output.txt, test/parity/fixtures/action-script.combat.json, test/parity/divergence-records.test.js, test/parity/combat-parity.test.js, test/parity/fixture-inventory.test.js, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/51-initiative-once-per-combat/51-CONTEXT.md ("Fixtures, pins & measurement (engine gate)" — declaration shape; "no blanket carve-out in comparables.js"; "after/stateAfter regenerated from the engine (measured, never hand-typed)")
    - tools/initiative-fixture-scan-output.txt (the 51-01 BEFORE output — Part A is the invariant the re-run must reproduce; Part B is what changes)
    - tools/initiative-fixture-scan.mjs (Part A / Part B definitions you are about to re-run)
    - test/parity/fixtures/action-script.combat.json (the `lose` record "24+31", `lose-apprentice` "31", `parley` "27+31" — the record shape and rationale style; `lose-plain` has only a Phase 45 `chargenDivergence` today)
    - test/parity/fixtures/action-script.schema.md lines 61-95 (action-path record fields)
    - test/parity/harness/comparables.js lines 799-900 (`actionPathDivergenceOf`, `skipsByteDiffAt`, `declaredEndDiffs` — how `fromAction`, `fields`, `before/after`, `stateFields`, `stateBefore/stateAfter` are checked; `pickFields` omits undefined)
    - test/parity/combat-parity.test.js lines 212-325 (the per-scenario replay; the `if (!pathDiv)` block whose `lose-plain` branch asserts "never declare a record on it" — dead once a record exists; rewrite it honestly)
    - test/parity/fixture-inventory.test.js lines 60-80 (the `lose-plain` roster row comment — "restores byte-identical death-path coverage" — comment touch only; rosters are unchanged)
    - test/parity/divergence-records.test.js (whole file — the HEDGE-03 MOVED SET guard is the template for the INIT-01 guard; `RECORDS` carries `holderId`, `kind`, `record`)
    - test/parity/FIXTURE-INVENTORY.md lines 1622-1749 (the Phase 45 section: "The live scan" quoting the output verbatim, "Moved set — declared records" table, "Byte-identical elsewhere" — the mould) and lines 1-40 (the file header, to confirm the H2 naming convention)
  </read_first>
  <action>
    1. Re-run the scan on the edited engine: `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt`. Diff Part A (everything from the file top through the `INITIATIVE EXPOSURE:` line) against the 51-01 commit's copy (`git show <51-01 commit>:tools/initiative-fixture-scan-output.txt`) — it must be EMPTY; if it is not, stop: the engine edit moved something the predictor did not key on, and the plan's model is wrong (report it, do not paper over it). Part B now shows the measured `firstDivergentAction` and the end-of-scenario values per moved site. Commit the AFTER output (this file is overwritten, the BEFORE copy stays in history at the 51-01 commit — say so in the inventory).

    2. Declare the MOVED SET (expected: `action-script.combat.json` `lose`, `lose-apprentice`, `lose-plain` — declare exactly what the MOVED SET line names, no more, no fewer) in `test/parity/fixtures/action-script.combat.json`:
       - `lose` (seed 14): `phase` "24+31" -> "24+31+51"; `requirements` += "INIT-01"; `after`/`stateAfter` replaced by Part B's `fields.after (engine @end)` / `state.after (engine @end)` values for that site; `before`/`stateBefore` untouched (the prototype is frozen — assert by eye that Part B's `fields.before` equals the existing `before`); `fromAction` stays 0; append a Phase 51 paragraph to `rationale`.
       - `lose-apprentice` (seed 127): "31" -> "31+51"; same treatment.
       - `lose-plain` (seed 1119): a NEW `divergence` record beside its Phase 45 `chargenDivergence`: `kind: "action-path"`, `phase: "51"`, `requirements: ["INIT-01"]`, `fromAction` = Part B's `firstDivergentAction` for that site (measured), `fields: ["wp", "sp", "gold", "kills", "rations"]`, `before` = Part B `fields.before (prototype @end)`, `after` = Part B `fields.after (engine @end)`, `stateFields: ["dead"]`, `stateBefore`/`stateAfter` = Part B's `state.before`/`state.after`, and the rationale.
       Shared rationale text (adapt per holder, keep the facts): "Phase 51 (INIT-01, 2026-09-20): initiative is rolled once per fight in fight(); the prototype's per-round re-roll (canon p.24) and its pre-emptive foe turn are gone from afterPlayerAction, so from the first live-fight round advance (action 1) onward the engine draws two fewer d20 per round and never gives the foe two turns back to back. Knight-vs-big-foe narrows to the opener (evaluated once at the single roll). Measured by tools/initiative-fixture-scan.mjs — Part A (MOVED SET) byte-identical before and after the edit (51-01 vs 51-02 commits), Part B supplied every after/stateAfter value." Every number in the JSON comes from the scan output or the parity test's own failure message — none is typed from memory.

    3. `test/parity/divergence-records.test.js`: add `INITIATIVE_SCAN_OUTPUT_PATH` and a test "INIT-01: the holders declaring Phase 51 are exactly the initiative scan's MOVED SET" in the HEDGE-03 mould: parse the `MOVED SET (N): ` line of `tools/initiative-fixture-scan-output.txt`, `declared` = RECORDS whose `kind === "divergence"` and whose `String(record.phase).split("+").includes("51")`, mapped to `holderId`; assert the count matches its own list and `deepStrictEqual` on the two sorted id arrays. The existing HEDGE-03 test is untouched.

    4. `test/parity/combat-parity.test.js`: inside the `if (!pathDiv)` block, remove the now-dead `lose-plain` branch (its `assert.equal(actionPathDivergenceOf(scenario), null, ...)` can never run again) and replace its comment with the honest statement: Phase 51 (INIT-01) declared an action-path record on `lose-plain` — every multi-round fight diverges from the first round advance by construction now, so no multi-round death-path fixture can be byte-identical; the death path is pinned by the record's `stateAfter.dead: true` through `declaredEndDiffs`. `test/parity/fixture-inventory.test.js` L70-74: one comment touch ("byte-identical" -> "declared from Phase 51; roster unchanged"). Rosters, seeds and action arrays are not touched anywhere.

    5. `test/parity/FIXTURE-INVENTORY.md`: append `## Phase 51: initiative once per fight (INIT-01) — the p.24 per-round re-roll is gone; measured moved set, declared per site` with: an intro paragraph (the rule, the greenfield ruling, why exactly the multi-round combat sites move and every one-action/one-round site — win, flee, parley, cast-damage, heal, potion, scroll, every chargen/movement/economy/encounters site — does not; the Knight narrowing), `### The live scan (tools/initiative-fixture-scan.mjs)` quoting the AFTER output verbatim in a fence and stating the Part A diff against the 51-01 BEFORE commit was empty, `### Moved set — declared records` (a table: Holder | Site / seed | Hero | record | fromAction | fields before (prototype) | fields after (engine) | dead before -> after | rationale pointer), `### Draw-count pins re-pinned (never loosened)` listing each file and pin changed in Task 1 with old -> new values, and `### Byte-identical elsewhere (Phase 51)` naming the 28 unmoved sites, stating no `seed`/`actions`/`scenarios` array changed, `comparables.js` untouched, and the master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` unchanged.

    6. `npm test` -> fail 0. THEN commit Tasks 1 + 2 together: `feat(51-02): initiative once per fight — engine cut, pins re-pinned, moved fixtures declared + regenerated (INIT-01)` with the Co-Authored-By / Claude-Session trailers.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && npm test 2>&1 | grep -E "^# (tests|pass|fail) " ; node --test test/parity/divergence-records.test.js test/parity/combat-parity.test.js test/parity/full-suite.test.js 2>&1 | grep -E "^# fail "; git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - `git diff --stat 0cacf50..HEAD -- test/parity/harness/comparables.js test/parity/prototype-master.js.txt` is empty
    - `node -e "const j=require('./test/parity/fixtures/action-script.combat.json');const s=Object.fromEntries(j.scenarios.map(x=>[x.name,x]));console.log(s.lose.divergence.phase, s['lose-apprentice'].divergence.phase, s['lose-plain'].divergence.phase, s['lose-plain'].divergence.kind, Number.isInteger(s['lose-plain'].divergence.fromAction))"` prints `24+31+51 31+51 51 action-path true`
    - `node -e "const j=require('./test/parity/fixtures/action-script.combat.json');console.log(j.scenarios.filter(x=>String(x.divergence&&x.divergence.phase||'').split('+').includes('51')).map(x=>x.name).sort().join(','))"` prints exactly the names in the scan's MOVED SET line (expected `lose,lose-apprentice,lose-plain`)
    - `grep -c "INIT-01: the holders declaring Phase 51 are exactly the initiative scan's MOVED SET" test/parity/divergence-records.test.js` prints 1
    - `grep -c "never declare a record on it" test/parity/combat-parity.test.js` prints 0
    - `grep -c "^## Phase 51: initiative once per fight (INIT-01)" test/parity/FIXTURE-INVENTORY.md` prints 1 and `grep -c "^### Moved set — declared records" test/parity/FIXTURE-INVENTORY.md` is at least 2 (Phase 45's and Phase 51's)
    - `grep -c "MOVED SET (" test/parity/FIXTURE-INVENTORY.md` is at least 2 (the Phase 51 section quotes the scan output)
    - Part A invariance: `git show $(git log --format=%h -1 -- tools/initiative-fixture-scan.mjs):tools/initiative-fixture-scan-output.txt | sed -n '1,/^INITIATIVE EXPOSURE/p' | diff - <(sed -n '1,/^INITIATIVE EXPOSURE/p' tools/initiative-fixture-scan-output.txt)` is empty
    - `git log --format=%s -1` names 51-02 and INIT-01, and `git status --porcelain` is empty after the commit
  </acceptance_criteria>
  <done>Exactly the measured MOVED SET carries Phase 51 action-path records with engine-measured after/stateAfter values; the MOVED SET guard, the inventory section and the honest parity comments are in; npm test fail 0 on the single combined commit; master and comparables untouched.</done>
</task>

<task type="auto">
  <name>Task 3: AFTER bot readout under Plan 01's flags; phase gates; SUMMARY</name>
  <files>docs/DIFFICULTY-RETUNE.md, docs/class-pass/v17-p51-after-smoke.json, .planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md</files>
  <read_first>
    - docs/DIFFICULTY-RETUNE.md — the `## v1.7 tuning pass (Phases 51–54)` H2 and its `### v1.7 · Phase 51 — initiative once` / `#### BEFORE — commit ...` block written by 51-01 (the exact three commands, the fence labels); the `## v1.2 retune (Phase 27)` line that must remain the LAST H2
    - .planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md (the measured run times; the BEFORE commit hash)
    - test/unit/difficulty-retune-ledger.test.js lines 120-135 (the LAST-H2 pin)
    - .planning/phases/50-character-roller-fix/50-03-SUMMARY.md (the `## Gates` and `## Human verification (deferred to end of run)` shapes)
  </read_first>
  <action>
    On the Task 2 commit (engine edit landed), run the SAME three commands with the SAME flags as 51-01, capturing stdout: `node tools/tune-difficulty.mjs --seeds=200`, `node tools/tune-difficulty.mjs --seeds=200 --party`, `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p51-after-smoke.json`. Same run-time caveat as before (~70 s per tune-difficulty run; minutes for the smoke; 10-minute timeout or background; never quote a truncated transcript).

    Scoped Edit of `docs/DIFFICULTY-RETUNE.md`: inside the Phase 51 H3 (i.e. inserted immediately BEFORE the `## v1.2 retune (Phase 27) — TUNE-05..07` line, after the BEFORE block), add `#### AFTER — commit <full hash of the Task 2 commit> (initiative once per fight)` with the three transcripts in labelled fences, then `#### Reading (BEFORE → AFTER)`: a table with rows for solo death-depth min/p50/p90/max, solo action-count p50, party death-depth min/p50/p90/max, party action-count p50, the top three death causes each side, and the class smoke's overall mean depth plus the three largest per-cell movers (up and down) read from the two smoke JSONs; then one paragraph interpreting the delta in the tuning-proxy register (expected direction: slightly lower lethality — fewer foe turns per fight — the baseline Phase 52's cadence work is measured on top of; no verdict, no constant change here). No H2/H3 is added; the v1.2 H2 stays last.

    Gates, recorded verbatim in the SUMMARY: `npm test` (`# tests / # pass / # fail` lines — expect fail 0 with a test count above 3334 by the number of pins added), `npm run build:www` (green), the master hash, `git diff --stat -- test/parity/harness/comparables.js` (empty). `npm run boot:check` is NOT a gate this phase (environment-blocked; note as skipped). The SUMMARY also lists every re-pinned value old -> new, the MOVED SET, and the commit hashes of the BEFORE and AFTER readouts. End with `## Human verification (deferred to end of run)` listing the Pixel 7 checks owed to the Phase 55 batch: (1) across a 3+ round fight no two foe turns ever occur back to back in the fight log; (2) a Samurai (or Fridgian) fight opens with the foe's turn immediately after Fight!, then strictly alternates; (3) the combat panel's YOU/THEY MOVE FIRST line does not change between rounds of the same fight.

    Commit: `docs(51-02): AFTER bot readout + summary`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && npm test 2>&1 | grep -E "^# (tests|pass|fail) " ; npm run build:www && node --test test/unit/difficulty-retune-ledger.test.js 2>&1 | grep -E "^# fail " && grep -c "^#### AFTER — commit [0-9a-f]\{40\} (initiative once per fight)" docs/DIFFICULTY-RETUNE.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; `npm run build:www` exits 0
    - `grep -c "^#### AFTER — commit [0-9a-f]\{40\} (initiative once per fight)" docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c "^#### Reading (BEFORE → AFTER)" docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -c "tune-difficulty: 200 seeded auto-play run(s)" docs/DIFFICULTY-RETUNE.md` prints at least 4 (two BEFORE + two AFTER transcripts)
    - `node -e "const a=require('./docs/class-pass/v17-p51-after-smoke.json'),b=require('./docs/class-pass/v17-p51-before-smoke.json');console.log(a.meta.seeds===b.meta.seeds && a.cells.length===b.cells.length && a.meta.commit!==b.meta.commit)"` prints true
    - `node --test test/unit/difficulty-retune-ledger.test.js` exits 0 (v1.2 still the last H2; 16 H3s)
    - `grep -c "^## Human verification (deferred to end of run)" .planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md` prints 1
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git status --porcelain` empty after the commit
  </acceptance_criteria>
  <done>BEFORE and AFTER rows sit under one Phase 51 heading with identical parameters and a reading; both gates are green and recorded verbatim; the SUMMARY ends with the deferred Pixel 7 checks.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 02's share)

| Artifact | Kind | Where |
|---|---|---|
| `resolveInitiative(state, rng)` -> `{ first, mine, theirs, why }` | new export (the single roll site's detail form) | engine/combat.js |
| `rollInitiative(state, rng)` -> `first` | retained export, now a wrapper over resolveInitiative | engine/combat.js |
| `combatJoined.mine`, `combatJoined.theirs`, `combatJoined.why`, `combatJoined.foe` | additive event fields (why: samurai \| slow \| knight \| courtMage \| foreseen \| acuteHearing \| senses, omitted when the dice decided; foe: the single live foe's name, omitted for a group) | engine/combat.js `fight()` |
| `DELIBERATE RULES CHANGE (Phase 51, INIT-01, 2026-09-20)` paragraphs | JSDoc on the roll and on afterPlayerAction | engine/combat.js |
| Section 6 `INIT-01 (SC1)` zero-draw pin | new test | test/unit/foe-turn-draw-count.test.js |
| `INIT-01 (SC2): a Samurai's forced-foe fight alternates strictly — foe turns = player actions + 1, never two in a row` | new test | test/unit/combat.test.js |
| `INIT-01: a 3-round fight emits exactly one combatJoined and no other event carries mine/theirs` | new test | test/unit/combat.test.js |
| `fight:`-targeted initiative tests (7 why branches) | re-targeted tests | test/unit/combat.test.js |
| `lose` "24+31+51", `lose-apprentice` "31+51", `lose-plain` "51" action-path records | fixture records (values from the scan's Part B) | test/parity/fixtures/action-script.combat.json |
| `INIT-01: the holders declaring Phase 51 are exactly the initiative scan's MOVED SET` | new guard test | test/parity/divergence-records.test.js |
| `## Phase 51: initiative once per fight (INIT-01) — ...` | new inventory section | test/parity/FIXTURE-INVENTORY.md |
| `tools/initiative-fixture-scan-output.txt` (AFTER) | overwritten readout; BEFORE copy stays at the 51-01 commit | tools/ |
| `#### AFTER — commit <hash> (initiative once per fight)` + `#### Reading (BEFORE → AFTER)` | ledger rows | docs/DIFFICULTY-RETUNE.md |
| `docs/class-pass/v17-p51-after-smoke.json` | tune-classes smoke JSON | docs/class-pass/ |
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine event -> shell narration | `combatJoined.foe` (a bestiary name) is interpolated into Oracle HTML by Plan 03 — content-sourced, never user input; same convention as `e.target`/`e.name` on every existing line |
| committed fixtures / scan output -> parity tests | the records and the MOVED SET line are load-bearing evidence; hand-typed values would silently weaken the gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-03 | Tampering | action-script.combat.json after/stateAfter values | medium | mitigate | Values copied from the scan's Part B / the parity failure message; `declaredEndDiffs` re-measures both sides on every run, so a hand-typed value fails the suite. |
| T-51-04 | Tampering | draw-count pins loosened to ranges | medium | mitigate | Acceptance greps the diff for `assert.ok(...draws...[<>]`; every pin stays an exact `assert.equal`. |
| T-51-05 | Denial of Service | fight never ends (foe turn removed changes termination) | low | accept | SC2 pin proves strict alternation; FULL_FIGHTS rows still terminate (won/died); the bot readout (200 seeds x2 + smoke) completes. |
| T-51-06 | Information Disclosure | combatJoined.foe name in the event stream | low | accept | Bestiary names are already shown on encounterStarted; nothing new is exposed. |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase. |
</threat_model>

<verification>
- SC1: `afterPlayerAction` contains no initiative call and exactly one `foeTurn`; the Section 6 zero-draw pin passes.
- SC2: the Samurai alternation pin passes (and, by construction, would throw "sequence exhausted" on the old engine).
- SC4: the re-targeted `fight()` initiative tests cover samurai/slow/foreseen/acuteHearing/senses/knight/courtMage via `combatJoined.why`.
- SC5: MOVED SET guard passes; Part A of the scan diffs empty against the 51-01 commit; master hash unchanged; comparables.js untouched.
- Measurement gate: BEFORE (51-01) and AFTER (51-02) transcripts under one Phase 51 H3 with identical flags.
- `npm test` fail 0 and `npm run build:www` green on both commits of this plan.
</verification>

<success_criteria>
- ROADMAP SC1, SC2, SC4, SC5 satisfied on master with evidence in tests, fixtures, inventory and ledger.
- Engine gate: pure/deterministic engine, no run-option gating, only the measured fixtures regenerated, pins re-pinned not loosened, master never edited.
- The event carries `mine`/`theirs`/`why`/`foe` once per fight so Plan 03 can narrate INIT-02 without touching the engine.
</success_criteria>

<output>
Create `.planning/phases/51-initiative-once-per-combat/51-02-SUMMARY.md` when done. It must contain: `## Gates` with the verbatim `# tests` / `# pass` / `# fail` lines and `npm run build:www` result, the master hash, the MOVED SET line, every re-pinned value old -> new, the BEFORE/AFTER readout commit hashes, and must END with `## Human verification (deferred to end of run)` listing the three Pixel 7 checks above.
</output>