---
phase: 51-initiative-once-per-combat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DIFFICULTY-RETUNE.md
  - docs/class-pass/v17-p51-before-smoke.json
  - tools/initiative-fixture-scan.mjs
  - tools/initiative-fixture-scan-output.txt
autonomous: true
requirements: [INIT-01]
tags: [engine-gate, measurement, parity-fixtures, bot-readout]

must_haves:
  truths:
    - "The BEFORE bot readout (tune-difficulty --seeds=200 solo, --party, and a tune-classes smoke) is captured on the phase-start commit 0cacf50 BEFORE any engine edit lands, recorded verbatim under a heading that names Phase 51 in docs/DIFFICULTY-RETUNE.md (MEASUREMENT GATE; CONTEXT: 'run at phase start (BEFORE any engine edit lands — on the phase-start commit)')"
    - "Which parity fixtures the initiative change moves is MEASURED, not assumed: tools/initiative-fixture-scan.mjs replays every parity replay site and prints a MOVED SET line naming exactly the sites whose fight reaches a live-fight round advance (CONTEXT: 'Measure first: tools/initiative-fixture-scan.mjs in the tools/worn-fixture-scan.mjs mould')"
    - "The scan is a REPORT tool (exit 0, no assertions, not a *.test.js) whose committed output tools/initiative-fixture-scan-output.txt is the evidence Plan 02 declares against"
    - "test/parity/prototype-master.js.txt is untouched (git hash-object == a1f4d0dc29782218d8e5aab65bc5989c33f917f0) and engine/ has zero diff from 0cacf50 at the end of this plan"
  artifacts:
    - "tools/initiative-fixture-scan.mjs — report tool in the worn-fixture-scan mould"
    - "tools/initiative-fixture-scan-output.txt — committed BEFORE readout with a `MOVED SET (N): ...` line"
    - "docs/DIFFICULTY-RETUNE.md — `## v1.7 tuning pass (Phases 51–54) — per-phase bot readouts` H2 with `### v1.7 · Phase 51 — initiative once` and `#### BEFORE — commit 0cacf50` transcripts"
    - "docs/class-pass/v17-p51-before-smoke.json — the tune-classes smoke JSON"
  key_links:
    - "Part A of the scan output (predictor table + MOVED SET line) must be invariant across the Plan 02 engine edit — it keys on state.combat.round transitions, which the edit does not change"
    - "divergence-records.test.js (Plan 02) parses the `MOVED SET (N): ` line of tools/initiative-fixture-scan-output.txt — the exact line format is load-bearing"
    - "test/unit/difficulty-retune-ledger.test.js pins `## v1.2 retune (Phase 27)` as the LAST H2 and exactly 16 H3s inside it — the new H2 must be inserted BEFORE that heading, never appended after it"
---

<objective>
Measure first. Capture the Phase 51 BEFORE bot readout on the phase-start commit (no engine byte changed), and build + run the initiative fixture scan that names, per parity replay site, where the per-round initiative re-roll used to fire — the MOVED SET Plan 02 must declare and regenerate, nothing more.

Purpose: the ROADMAP engine gate ("measured first — a scan listing every replay site whose comparables move") and measurement gate ("a bot readout BEFORE and AFTER under identical parameters") are both ordering constraints: the readout is unrecoverable once the engine edit lands, and the moved set must be evidence, not a guess. This plan is the only Wave 1 plan so the ordering is structural.

Output: `docs/DIFFICULTY-RETUNE.md` Phase 51 BEFORE block, `docs/class-pass/v17-p51-before-smoke.json`, `tools/initiative-fixture-scan.mjs`, `tools/initiative-fixture-scan-output.txt`.

Requirement traceability: INIT-01 ("the p.24 divergence is declared with its moved fixtures" — this plan produces the measurement the declaration cites).
</objective>

<assumption_delta_decision>
decision: no-change
signal: pluralization — the term "second" in "…a fresh second-round roll…" (orchestrator scan `assumption-delta scan 51`)
rationale: the trigger is the ordinal "second-round"; the identity model (one initiative result per combat, `C.first`) is unchanged — this phase makes it singular. No new variant, no new entity, no schema change.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-initiative-once-per-combat/51-CONTEXT.md
@tools/worn-fixture-scan.mjs
@tools/worn-fixture-scan-output.txt
@test/parity/harness/comparables.js
@test/parity/harness/sandboxPrototype.js
@docs/DIFFICULTY-RETUNE.md
@test/unit/difficulty-retune-ledger.test.js
</context>

<planner_measurements>
Facts the planner measured on commit 0cacf50 (2026-09-20) so the executor can recognise a correct result — the tools below remain authoritative:

- `node tools/tune-difficulty.mjs --seeds=200` (solo) takes ~70 s wall-clock on this machine; expect `--party` to be similar and the `tune-classes` smoke (143 cells x 5 seeds, 4 workers) to take a few minutes. Run each with a generous timeout (10 min) or in the background — never let a foreground timeout truncate a readout.
- A per-action probe of every combat/magic replay site on the CURRENT engine shows a live-fight round advance (`state.combat.round` n -> n+1 with combat live and the hero alive, on a non-`flee` action) FIRST at action index 1 for `action-script.combat.json#lose` (seed 14, 11 actions), `#lose-apprentice` (seed 127, 15 actions) and `#lose-plain` (seed 1119, 8 actions), and NEVER for `#win`, `#flee`, `#parley`, all four `action-script.magic.json` scenarios, and every chargen/movement/economy/encounters site. Expected `MOVED SET (3)`. If the scan disagrees, the scan wins — record what it prints.
- A dry run of the afterPlayerAction cut (in a throwaway worktree, discarded) turned exactly these parity tests red: `combat parity (lose)`, `(lose-apprentice)`, `(lose-plain)`, and the `full-suite` gate — the same three holders. This is corroboration for the predictor design below, not a substitute for running the tool.
- `git hash-object test/parity/prototype-master.js.txt` == `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm test` == 3334 tests, fail 0 (Phase 50-03 close).
</planner_measurements>

<tasks>

<task type="auto">
  <name>Task 1: BEFORE bot readout on the phase-start commit (MEASUREMENT GATE, first — before any engine byte moves)</name>
  <files>docs/DIFFICULTY-RETUNE.md, docs/class-pass/v17-p51-before-smoke.json</files>
  <read_first>
    - .planning/phases/51-initiative-once-per-combat/51-CONTEXT.md (the "Fixtures, pins & measurement" decisions — identical parameters BEFORE/AFTER; heading text "v1.7 · Phase 51 — initiative once")
    - .planning/ROADMAP.md lines 15-22 (Engine gate, Sequencing gate, Measurement gate)
    - docs/DIFFICULTY-RETUNE.md lines 1-15 (document intro) and lines 1177-1260 (the `## v1.2 retune (Phase 27)` H2 and its "Bot proxy and parameters" style) — the ledger mould
    - test/unit/difficulty-retune-ledger.test.js lines 120-205 (the LAST-H2 pin on `## v1.2 retune (Phase 27)` and the fixed 16-H3 order inside that section — the reason the new H2 goes BEFORE it)
    - tools/tune-difficulty.mjs lines 1-30 (flags: `--seeds=200`, `--party`, `--json`)
    - tools/tune-classes.mjs lines 1-40 and 85-115 (flags: `--seeds N --workers N --out path`; JSON shape; elapsed goes to STDERR only)
    - docs/class-pass/v15-gear-smoke.json (precedent: a smoke JSON committed beside the matrix JSONs; no test globs that directory)
  </read_first>
  <action>
    Confirm the tree is on the phase-start commit with no engine edit: `git rev-parse --short HEAD` prints `0cacf50` (or a later docs-only commit whose `git diff --stat 0cacf50..HEAD -- engine/ content/ test/parity/` is empty). Record the full hash you measure on — it goes in the heading.

    Run the three readouts under EXACTLY these flags (the AFTER run in Plan 02 repeats them verbatim), capturing stdout to files in the scratchpad first, then quoting them:
    1. `node tools/tune-difficulty.mjs --seeds=200` (solo).
    2. `node tools/tune-difficulty.mjs --seeds=200 --party`.
    3. `node tools/tune-classes.mjs --seeds 5 --workers 4 --out docs/class-pass/v17-p51-before-smoke.json` (the smoke; stdout is the ranked matrix, stderr the elapsed line — quote stdout only).
    Each takes ~70 s to a few minutes; run with a 10-minute timeout or in the background and wait for exit. Re-run any readout whose output was cut short; never paste a partial transcript.

    Edit `docs/DIFFICULTY-RETUNE.md` with a scoped Edit (never a whole-file rewrite): insert, IMMEDIATELY BEFORE the line `## v1.2 retune (Phase 27) — TUNE-05..07`, a new H2 `## v1.7 tuning pass (Phases 51–54) — per-phase bot readouts` containing:
    - one intro paragraph: this H2 is the v1.7 measurement-gate ledger — every rule-changing phase of the tuning pass records BEFORE and AFTER under identical parameters here so Phase 54's four-band retune inherits a known baseline; and a one-sentence placement note that it sits above the v1.2 section because `test/unit/difficulty-retune-ledger.test.js` pins `## v1.2 retune (Phase 27)` as the ledger's last H2 (sections below it are older).
    - the H3 `### v1.7 · Phase 51 — initiative once` with: a "Rule change" paragraph (initiative rolled once per fight in `fight()`, the per-round re-roll and its pre-emptive foe turn in `afterPlayerAction` deleted — the foe never takes two turns back to back; INIT-01/INIT-02; deliberate canon p.24 divergence under the greenfield ruling), a "Parameters (identical BEFORE/AFTER)" list naming the three commands verbatim, and the H4 `#### BEFORE — commit <full hash> (phase start, no engine edit)` holding the three transcripts each in its own fenced code block, each fence preceded by a one-line label naming the command.
    Do NOT add an AFTER block or any "to be filled" line — Plan 02 Task 3 appends the AFTER H4 and the reading under the same H3.

    Commit this task on its own (docs + the smoke JSON only) so the BEFORE readout's commit provably precedes every engine edit: `docs(51-01): BEFORE bot readout on the phase-start commit`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node --test test/unit/difficulty-retune-ledger.test.js test/unit/class-pass-ledger.test.js && grep -c "^### v1.7 · Phase 51 — initiative once" docs/DIFFICULTY-RETUNE.md && git diff --stat 0cacf50..HEAD -- engine/ content/ test/parity/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## v1.7 tuning pass (Phases 51–54) — per-phase bot readouts" docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -c "^### v1.7 · Phase 51 — initiative once" docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -c "^#### BEFORE — commit [0-9a-f]\{40\} (phase start, no engine edit)" docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -n "^## v1.7 tuning pass\|^## v1.2 retune" docs/DIFFICULTY-RETUNE.md` lists the v1.7 line with the SMALLER line number (inserted before v1.2)
    - `grep -c "tune-difficulty: 200 seeded auto-play run(s)" docs/DIFFICULTY-RETUNE.md` prints at least 2 (solo + party transcripts quoted verbatim)
    - `node -e "const j=require('./docs/class-pass/v17-p51-before-smoke.json'); console.log(j.meta.tool, j.meta.seeds, j.cells.length)"` prints `tune-classes 5 143`
    - `node --test test/unit/difficulty-retune-ledger.test.js` exits 0 (v1.2 still the last H2, still 16 H3s)
    - `git diff --stat 0cacf50..HEAD -- engine/ content/ test/parity/` is empty at the commit that carries this task
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The BEFORE readout (solo, party, class smoke) is committed verbatim under "v1.7 · Phase 51 — initiative once" in docs/DIFFICULTY-RETUNE.md on a commit whose engine/content/parity tree equals 0cacf50; the ledger guard test stays green.</done>
</task>

<task type="auto">
  <name>Task 2: tools/initiative-fixture-scan.mjs — the report tool, run on the pre-edit engine, output committed</name>
  <files>tools/initiative-fixture-scan.mjs, tools/initiative-fixture-scan-output.txt</files>
  <read_first>
    - tools/worn-fixture-scan.mjs (whole file — the mould: header contract, `loadPrototypeSandbox`, `applyStartCombat`, `runEconomyAction`, the per-family replay loops in `main()`, the markdown table + `MOVED SET (N): ` + "Record values (JSON, per moved site)" output sections)
    - tools/worn-fixture-scan-output.txt (the committed output shape the Phase 45 inventory section quotes)
    - test/parity/harness/comparables.js lines 584-660 (`combatComparable`, `applyStartCombat` — the per-action comparable the combat/magic parity tests diff; `rngState` and `combat.round` are stripped, `combat.first` is NOT) and the `diffState` import it uses
    - test/parity/combat-parity.test.js lines 195-300 (how the parity test replays a scenario in lockstep with the prototype sandbox — `ctx.startCombat`, `ctx.playerStrike`, `ctx.parley`, `ctx.flee`, `applyStartCombat`, `applyAction` — and reads `ctx.S`)
    - test/parity/magic-parity.test.js lines 80-140 (the castSpell replay form: `ctx.castSpell(idx)` / `applyAction(state, { type: "castSpell", idx })`)
    - test/parity/fixtures/action-script.schema.md lines 61-95 (the action-path record fields the "Record values" section must print: `fields` wp/sp/gold/kills/rations from `state.c`, `stateFields` dead)
    - engine/combat.js lines 1300-1356 (`afterPlayerAction`: the `state.combat.round++` site guarded by `!state.dead && state.combat` — the predictor keys on exactly this transition) and lines 995-1002 (`flee`'s own `C.round++` after a failed flee — NOT a re-roll site, which is why `flee` actions are excluded from the predictor)
  </read_first>
  <action>
    Create `tools/initiative-fixture-scan.mjs` as a REPORT tool in the `tools/worn-fixture-scan.mjs` mould (ESM, zero dependencies, always exits 0, makes no assertions, header comment states it is NOT a `*.test.js` and gives the run form `node tools/initiative-fixture-scan.mjs > tools/initiative-fixture-scan-output.txt`). It replays all 31 parity replay sites (the same six fixture families, same site ids `action-script.<family>.json#<name|seed-N|script>` as the worn scan) through the real engine in lockstep with `loadPrototypeSandbox({ seed })`, and prints TWO parts:

    Part A — the predictor (invariant across the Plan 02 engine edit). Per site, after every action record `state.combat?.round` before and after; the site's `firstRoundAdvance` is the first action index i where the action type is not `flee`, the round went from n (>= 1) to n+1, `state.combat` is still live and `state.dead` is falsy — i.e. exactly the `afterPlayerAction` `round++` that today is followed by the per-round `rollInitiative` and its pre-emptive foe turn, and after Plan 02 is followed by nothing. `moved` is `firstRoundAdvance !== "never"`. Print a markdown table with columns Fixture | Site | Seed | Hero | actions | first live-fight round advance | moved, then the line `MOVED SET (N): <comma-separated site ids in replay order>` and `INITIATIVE EXPOSURE: N of 31 replay sites`. Also print `UNEXPLAINED:` rows only if a site moved but has no fight (cannot happen; keep the diagnostic for symmetry with the mould).

    Part B — the lockstep measurement (changes after the edit; this is what Plan 02 fills its records from). Per combat/magic site, diff `combatComparable(engineState)` against `combatComparable(ctx.S)` after every action (the same two-sided comparable the parity tests use; import `combatComparable` and `diffState` from the harness — this scan may import harness helpers, unlike the worn scan, because Plan 02 rewrites no comparable) and report `firstDivergentAction` (index of the first non-null diff, else "never") and `maxRound` reached. Under the heading `## Record values (JSON, per moved site)` print, for every MOVED site: `fields.before (prototype @end)` = JSON of `{wp, sp, gold, kills, rations}` read from `ctx.S.c`, `fields.after (engine @end)` = the same keys from the engine's `state.c`, `state.before (prototype @end)` = `{dead}` from `ctx.S`, `state.after (engine @end)` = `{dead}` from the engine state, `firstDivergentAction`, `maxRound`, and the existing `divergence.phase` on that holder (or "none"). Non-combat families (chargen/movement/economy/encounters) never carry `state.combat`; print "n/a" in Part B columns for them rather than skipping the rows, so the row count stays 31.

    Run it once on the current (pre-edit) engine and commit stdout as `tools/initiative-fixture-scan-output.txt`. Expected (planner probe): Part A `MOVED SET (3): action-script.combat.json#lose, action-script.combat.json#lose-apprentice, action-script.combat.json#lose-plain`, each with first live-fight round advance = 1; Part B `firstDivergentAction` 0 for lose/lose-apprentice (their existing Phase 24/31 records), "never" for lose-plain, "never" for win/flee/parley/cast-damage/heal/potion/scroll. Whatever the tool prints is the truth — do not edit the output by hand.

    Commit: `feat(51-01): initiative fixture scan — report tool + BEFORE output (MOVED SET measured)`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && node tools/initiative-fixture-scan.mjs > "$TEMP/p51-scan-check.txt" && diff "$TEMP/p51-scan-check.txt" tools/initiative-fixture-scan-output.txt && grep -c "^MOVED SET ([0-9]*): " tools/initiative-fixture-scan-output.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/initiative-fixture-scan.mjs > /dev/null; echo $?` prints 0
    - Re-running the tool and diffing against the committed file is empty (deterministic output)
    - `grep -c "^MOVED SET ([0-9]*): " tools/initiative-fixture-scan-output.txt` prints 1
    - `grep -c "^| action-script\." tools/initiative-fixture-scan-output.txt` prints 31 (one Part A row per replay site)
    - `grep -c "^## Record values (JSON, per moved site)" tools/initiative-fixture-scan-output.txt` prints 1
    - `grep -c "^INITIATIVE EXPOSURE: [0-9]* of 31 replay sites" tools/initiative-fixture-scan-output.txt` prints 1
    - `ls tools/initiative-fixture-scan.mjs` exists and `grep -c "node:test" tools/initiative-fixture-scan.mjs` prints 0 (a report tool, never picked up by `node --test`)
    - `npm test` exits with fail 0 (the tool is not a test; nothing else changed)
    - `git diff --stat 0cacf50..HEAD -- engine/ content/ test/parity/` is empty
    - `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>The scan tool exists, exits 0, prints a deterministic 31-row Part A table with a parseable MOVED SET line plus Part B record values, and its pre-edit output is committed; the engine is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: phase gates on the Wave 1 tree — npm test + build:www, recorded verbatim</name>
  <files>.planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md</files>
  <read_first>
    - .planning/phases/50-character-roller-fix/50-03-SUMMARY.md (the SUMMARY shape: `## Gates` with verbatim `# tests / # pass / # fail` lines, the fence hash line, and the closing `## Human verification (deferred to end of run)` section)
    - $HOME/.claude/gsd-core/templates/summary.md
  </read_first>
  <action>
    Run `npm test` (expect `# tests 3334`, `# fail 0` — this plan adds no tests) and `npm run build:www` (green). Do NOT run `npm run boot:check` as a gate (environment-blocked on this machine — 0-byte dump-dom with the user's Chrome running; note it as skipped, not failed). Write the SUMMARY with the verbatim gate lines, the master hash, `git diff --stat 0cacf50..HEAD -- engine/ content/ test/parity/` (empty), the measured run times of the three readouts, and the scan's MOVED SET line quoted. End the SUMMARY with `## Human verification (deferred to end of run)` stating this plan owes no Pixel 7 checks (measurement + tooling only). Commit the SUMMARY with `docs(51-01): summary`.
  </action>
  <verify>
    <automated>cd C:/projects/mazeworld && npm test 2>&1 | grep -E "^# (tests|pass|fail) " && npm run build:www && grep -c "^## Human verification (deferred to end of run)" .planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` output contains `# fail 0`
    - `npm run build:www` exits 0
    - `grep -c "^## Human verification (deferred to end of run)" .planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md` prints 1
    - `grep -c "MOVED SET (" .planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md` prints at least 1
    - `git status --porcelain` is empty after the final commit
  </acceptance_criteria>
  <done>SUMMARY records npm test fail 0, build:www green, the untouched fence, the readout run times and the MOVED SET; the tree is clean.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (Plan 01's share)

| Artifact | Kind | Where |
|---|---|---|
| `tools/initiative-fixture-scan.mjs` | new report tool (ESM, exit 0, no assertions) | tools/ |
| `tools/initiative-fixture-scan-output.txt` | new committed readout; carries the `MOVED SET (N): ...` line and `## Record values (JSON, per moved site)` | tools/ |
| `## v1.7 tuning pass (Phases 51–54) — per-phase bot readouts` | new H2 in docs/DIFFICULTY-RETUNE.md, inserted before `## v1.2 retune (Phase 27)` | docs/ |
| `### v1.7 · Phase 51 — initiative once` + `#### BEFORE — commit <hash> (phase start, no engine edit)` | new H3/H4 under that H2 with three fenced transcripts | docs/DIFFICULTY-RETUNE.md |
| `docs/class-pass/v17-p51-before-smoke.json` | new tune-classes smoke JSON (`--seeds 5 --workers 4`) | docs/class-pass/ |

Plan 02 adds: `resolveInitiative` (engine/combat.js), `combatJoined.mine/theirs/why/foe`, the SC1/SC2/once-per-fight pins, the three action-path records, the `test/parity/FIXTURE-INVENTORY.md` "Phase 51" section, the `#### AFTER — commit <hash>` block, `docs/class-pass/v17-p51-after-smoke.json`. Plan 03 adds the Oracle/fight-log initiative line and `test/unit/initiative-line.test.js`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Offline single-player game; this plan adds a dev-only report tool and documentation. No network, no user input crosses into the tool (it reads committed fixtures only). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-51-01 | Tampering | tools/initiative-fixture-scan.mjs output edited by hand | low | mitigate | The tool is deterministic; acceptance re-runs it and diffs against the committed file (empty diff required); Plan 02's divergence-records test parses the committed line. |
| T-51-02 | Repudiation | BEFORE readout taken after an engine edit | medium | mitigate | Task 1 is committed alone on a tree where `git diff --stat 0cacf50..HEAD -- engine/ content/ test/parity/` is empty; the heading carries the measured commit hash. |
| T-51-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this phase (zero new dependencies); no legitimacy checkpoint needed. |
</threat_model>

<verification>
- The BEFORE readout commit precedes every engine edit in `git log` (Plan 02 checks `git log --oneline -- engine/combat.js` against the readout commit).
- `tools/initiative-fixture-scan-output.txt` is reproducible (re-run diff empty) and its MOVED SET is the set Plan 02 declares.
- Fence intact: master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `npm test` fail 0; `npm run build:www` green.
</verification>

<success_criteria>
- ROADMAP SC5 groundwork: the p.24 divergence's moved fixtures are measured by a committed scan before the edit (the declaration in Plan 02 cites this output).
- Measurement gate: BEFORE rows exist under "v1.7 · Phase 51 — initiative once" with the three commands recorded verbatim, on the phase-start tree.
- Zero engine bytes changed in this plan.
</success_criteria>

<output>
Create `.planning/phases/51-initiative-once-per-combat/51-01-SUMMARY.md` when done. It must contain a `## Gates` section with the verbatim `# tests` / `# pass` / `# fail` lines, the `npm run build:www` result, the master hash, and must END with `## Human verification (deferred to end of run)` (this plan: none owed).
</output>
