---
phase: 36-balance-foundation-effect-timers-small-independent-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/class-pass/v15-before.json
  - docs/class-pass/v15-before-depth20.json
  - docs/CLASS-PASS.md
  - test/unit/class-pass-ledger.test.js
autonomous: true
requirements: [BAL-01]
tags: [class-matrix, before-pin, tuning-proxy, ledger, no-engine-change, deferred-uat]
notes:
  assumption_delta: no-change
  assumption_delta_rationale: "trigger was the roadmap's research-flag prose ('--research-phase optional'); no primary-key/identity model changes in this phase"
  rng_draw_impact: "adds zero draws — this plan changes no engine/content/src/shell file"
  serialized_field_impact: "none — no engine/content/src/shell file changes"

must_haves:
  truths:
    - "docs/class-pass/v15-before.json and docs/class-pass/v15-before-depth20.json exist, are committed, and both carry meta.commit equal to the short hash of a commit whose engine/content/src/mazeworld.html are byte-identical to tag v1.4.0 (git diff --quiet v1.4.0 -- engine content src mazeworld.html exits 0 at that commit) (BAL-01, ROADMAP SC-1)"
    - "node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json prints 'cannot-act cells: 0 of 143' and exits 0 (BAL-01, ROADMAP SC-1 'runs cleanly against the current engine')"
    - "docs/CLASS-PASS.md carries a ninth H2 '## v1.5 BEFORE — commit <40-hex> (Phase 36 — BAL-01)' whose section holds the pinned hash, both exact command lines, both wall times, both verbatim Bot: lines, both transcripts, and a pooled-rollup table copied from rollups.pooled (natural p50Depth/reach5/reach10/reach20/meanDepth/p90Depth; depth-20 meanEncountersSurvived/p50FloorsGained/meanFloorsGained; stuck totals; the cannot-act gate line) (BAL-01)"
    - "test/unit/class-pass-ledger.test.js still pins the eight v1.2 H2 headings in order at indices 0-7 AND additionally pins the v1.5 BEFORE section (hash = both JSONs' meta.commit, both Bot: lines present, metaParity vs before.json / before-depth20.json ok, 143 identical cell keys, zero cannot-act cells, zero stuck); npm test prints '# fail 0'"
    - "git diff --stat v1.4.0 -- engine content src mazeworld.html is empty after this plan's commit (no engine/content/src/shell file changed)"
  artifacts:
    - path: "docs/class-pass/v15-before.json"
      provides: "the v1.5 BEFORE natural-start matrix (143 cells x 40 seeds, start depth 1) — Phase 42's like-for-like BEFORE"
    - path: "docs/class-pass/v15-before-depth20.json"
      provides: "the v1.5 BEFORE depth-20 slice (143 cells x 10 seeds, start depth 20)"
    - path: "docs/CLASS-PASS.md"
      provides: "the '## v1.5 BEFORE — commit …' section (pin/provenance, pooled rollups, both transcripts, gate result)"
    - path: "test/unit/class-pass-ledger.test.js"
      provides: "the additive v1.5 BEFORE guard (heading, hash, Bot: lines, meta parity, cell keys, cannot-act, stuck)"
  key_links:
    - from: "docs/CLASS-PASS.md '## v1.5 BEFORE' heading hash"
      to: "docs/class-pass/v15-before.json meta.commit"
      via: "the 40-hex heading hash startsWith the JSON's short meta.commit (same rule as the v1.2 BEFORE/AFTER pins)"
      pattern: "^## v1\\.5 BEFORE — commit [0-9a-f]{40} \\(Phase 36 — BAL-01\\)$"
    - from: "tools/tune-classes.mjs"
      to: "docs/class-pass/v15-before*.json"
      via: "--out; meta.commit = git rev-parse --short HEAD at launch"
      pattern: "tune-classes"
  prohibitions:
    - "No edit to any file under engine/, content/, src/, or to mazeworld.html, test/parity/, package.json — this plan is a measurement, not a change"
    - "No checkpoint:human-verify, no adb/device step, no APK build (deferred-UAT protocol)"
    - "No foreground wait on the matrix runs; no npm test or other CPU-heavy command while a matrix run is in flight (it would distort the recorded wall time — 4 workers on a 4-core machine)"
---

<objective>
Pin the v1.5 BEFORE class-matrix snapshot (BAL-01) against the v1.4-close engine BEFORE any other Phase 36 plan changes a single engine, content, src or shell byte — the exact v1.2 protocol from `docs/CLASS-PASS.md` "How to reproduce", with the same run parameters, so Phase 42's AFTER diff is like-for-like. Record the pin (hash, commands, wall times, pooled rollups, cannot-act gate) as a new `## v1.5 BEFORE` section of `docs/CLASS-PASS.md`, and extend the ledger's standing guard test additively so the new section is pinned the same way the v1.2 sections are.

Per CONTEXT §"BEFORE Matrix Pin (BAL-01)": Plan 01, before any code change; run parameters identical to v1.2 (`--seeds 40 --workers 4 --max-actions 5000` natural, `--seeds 10 --workers 4 --max-actions 5000 --start-depth 20` slice); file names `v15-before.json` / `v15-before-depth20.json`; v1.2's `before.json`/`after.json`/`retune-after*.json` untouched; ledger appended to `docs/CLASS-PASS.md` (Phase 42 / BAL-02 records its verdicts in the same file).

Pinned hash rule: the pin is `git rev-parse HEAD` at launch (HEAD includes this phase's planning-docs commits — that is fine and expected: the engine, content, src and mazeworld.html are byte-identical to tag `v1.4.0`, which the preflight `git diff --quiet v1.4.0 -- engine content src mazeworld.html` proves; CONTEXT.md's `38c8b91` was HEAD at discuss time, the engine bytes are the same). Record the ACTUAL hash the runs were taken against — in the ledger heading, in the provenance paragraph, and in the commit message.

Purpose: the BEFORE matrix is impossible to recover later without git archaeology (STATE.md v1.2 lesson); Pitfall 3 (un-measured power moves the depth-20 curve).
Output: two committed JSONs, the ledger section, the extended guard test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-CONTEXT.md
@docs/CLASS-PASS.md
@tools/tune-classes.mjs
@tools/class-pass-diff.mjs
@test/unit/class-pass-ledger.test.js
</context>

<artifacts_this_phase_produces>
Phase 36 as a whole creates the following symbols/files (this plan creates only the four marked **[01]**; the rest are listed so every plan carries the same phase-wide inventory):

- **[01]** `docs/class-pass/v15-before.json`, `docs/class-pass/v15-before-depth20.json` — the v1.5 BEFORE matrix pair; `docs/CLASS-PASS.md` `## v1.5 BEFORE — commit … (Phase 36 — BAL-01)` section; additive v1.5 block in `test/unit/class-pass-ledger.test.js`.
- [02] `engine/effects.js` (new, zero-import leaf): `startEffect(c, id, { rounds | squares, cd? })`, `startCooldown(c, id, { rounds | squares })`, `tickRounds(c)`, `tickSquares(c, n = 1)`, `remaining(c, id)`, `isReady(c, id)`, `clearRoundTimers(c)`; new serialized field `c.timers` (lazily created map of `{ cadence, left, cd?, phase }`); tick-site calls in `engine/movement.js` (per-step) and `engine/combat.js` (foeTurn tail + endCombat); `clearStaleTimers(c)` in `engine/saveState.js`; `stripTimersField(c)` in `test/parity/harness/comparables.js`; `test/unit/effects.test.js`.
- [03] `engine/combat.js` `export function normalizeTarget(combat)`; the two lazy call sites (`playerStrike`, `magic.js#castSpell`) refactored onto it; `mazeworld.html`: module import of `normalizeTarget`, the post-dispatch `normalizeTarget(state.combat)` call in `engineCombatAction`, dead foe cards carrying `aria-disabled="true"`, `armEncounterButtons` selector excluding `.cb-foe.dead`; `test/unit/normalizeTarget.test.js`, `test/unit/shell-dead-foe-target.test.js`.
- [04] `engine/movement.js` `export function cutthroatMurderCheck(state, rng, events)` (called last in `descend`); new event `joinerMurdered { name, sub, depth }`; `engine/encounters.js#meetJoiner` refusal ternary reduced to the Wilmsry clause (CUT-01); `content/flavor.js` `SUB_NOTE.Cutthroat` rewrite + `JOINER_MURDER_LINES`; `EVENT_NARRATION.joinerMurdered`, `TOAST_FOR.joinerMurdered`, `RAIL_FAMILY.joinerMurdered`, `FEATURE_EVENTS += joinerMurdered`; `docs/CLASS-PASS.md` Cutthroat good/bad row; `test/unit/cutthroat-joiner.test.js`.
- [05] engine action `dismissJoiner { i? }` (`engine/actions.js` ACTION_TYPES + validate case, `engine/engine.js` dispatch case, `engine/encounters.js#dismissJoiner`); new events `joinerDismissed { name, sub }` and `dismissRefused { reason: "noParty" | "inCombat" | "badIndex" }`; `content/flavor.js` `JOINER_PARTING_LINES`; `EVENT_NARRATION`/`TOAST_FOR` entries for both, `RAIL_FAMILY.joinerDismissed`, `FEATURE_EVENTS += dismissRefused`, `NARRATIVE_ACTIONS += dismissJoiner`; `test/unit/dismiss-joiner.test.js`.
- [06] `mazeworld.html`: classic-script `SUB_NOTE.Cutthroat` synced to `content/flavor.js`; `DISMISS_CONFIRM_MS`, `dismissConfirmRevert`, `revertDismissConfirm()`; the Company sheet (class/sub/race, level, HP track, `Weapon: X`, `Eats N a rest`) and the two-tap `DISMISS` confirm inside `renderPartyRoster()`; CSS `.mw-party-line`, `.mw-party-dismiss`; bridge `window.mzDismissJoiner(i)`; `test/unit/shell-company-panel.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Preflight, then capture both v1.5 BEFORE matrix runs in the background (natural 143x40, then depth-20 143x10) against the pinned HEAD</name>
  <files>docs/class-pass/v15-before.json, docs/class-pass/v15-before-depth20.json</files>
  <read_first>
    - docs/CLASS-PASS.md lines 101-135 ("## How to reproduce" + the head of "## BEFORE — commit 5565b22…") and lines 1026-1072 ("### Pin and provenance (Phase 26 capture)") — the exact preflight, launch and provenance protocol to mirror
    - tools/tune-classes.mjs lines 1-60 (usage, JSON shape, meta.commit = `git rev-parse --short HEAD`, elapsed printed to STDERR only)
    - .planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-CONTEXT.md §"BEFORE Matrix Pin (BAL-01)"
    - .planning/STATE.md "Build/env" (npm test baseline: 2179 pass / 0 fail on 2026-09-17; 4 CPU cores on this machine)
  </read_first>
  <action>
    1. Preflight, in this order, each in its own Bash call; abort the plan and report if any fails:
       - `git status --porcelain` prints nothing (clean tree).
       - `git diff --quiet v1.4.0 -- engine content src mazeworld.html` exits 0 (engine/content/src/shell byte-identical to the v1.4.0 tag).
       - `npm test 2>&1 | tail -8` ends with `# fail 0` (run this BEFORE launching any matrix run, never during one).
       - `PIN=$(git rev-parse HEAD)`, `SHORT=$(git rev-parse --short HEAD)` — write both into the SUMMARY notes now; every later step cites `$PIN`.
    2. Launch run A with `run_in_background: true`, stdout+stderr to a scratchpad log with an `EXIT=<code>` sentinel appended on completion (scratchpad = the session scratchpad directory; never a repo path). Command (exact parameters — identical to the v1.2 protocol): `node tools/tune-classes.mjs --seeds 40 --workers 4 --max-actions 5000 --out docs/class-pass/v15-before.json`. Expected wall time 12–25 minutes (v1.2 BEFORE 687s, Phase 26 AFTER 1108s; the Phase 27 retune lets bots live longer, so the upper end is plausible).
    3. Poll in BOUNDED checks only: `tail -n 3 <log>` (one short Bash call at a time, a few minutes apart, or wait for the runtime's background-completion notification) until the log ends with `EXIT=0`. Never a foreground `sleep`/wait, never `npm test`, never any other CPU-heavy command while a run is in flight (4 workers on 4 cores — contention would distort the recorded wall time). Do NOT edit any tracked file while a run is in flight.
    4. Only after run A's `EXIT=0`: launch run B the same way (sequential, not concurrent — same reason): `node tools/tune-classes.mjs --seeds 10 --workers 4 --max-actions 5000 --start-depth 20 --out docs/class-pass/v15-before-depth20.json`. Expected ~40–70 s. Poll until `EXIT=0`.
    5. Record from each log's STDERR line `elapsed: <seconds>s  workers=4  runs=<n>` the wall time (natural and depth-20) for the ledger. Copy each log's STDOUT transcript (everything except the `elapsed:` line and the `EXIT=` sentinel) verbatim into scratch files for Task 2's transcript blocks.
    6. Verify the pair with one `node -e` script and paste its output into the SUMMARY: both files parse; `meta.commit === "$SHORT"` for both; `meta.cells === 143` for both; `meta.seeds` 40 / 10; `meta.startDepth` 1 / 20; `meta.bot` of v15-before.json === `meta.bot` of docs/class-pass/before.json (byte-identical Bot: line — the v1.2 protocol's own check) and likewise v15-before-depth20 vs before-depth20; `cells.length === 143` for both; sum of `cells[].stuck` for both (expect 0 — record the number either way).
    7. Re-run `git diff --quiet v1.4.0 -- engine content src mazeworld.html` (exit 0) and `git status --porcelain` (must list ONLY the two new JSON files) — the engine did not move under the runs.
  </action>
  <verify>
    <automated>test -s docs/class-pass/v15-before.json && test -s docs/class-pass/v15-before-depth20.json && node -e "const a=require('./docs/class-pass/v15-before.json'),b=require('./docs/class-pass/v15-before-depth20.json'),p=require('./docs/class-pass/before.json'),q=require('./docs/class-pass/before-depth20.json');const {execSync}=require('node:child_process');const s=execSync('git rev-parse --short HEAD').toString().trim();if(a.meta.commit!==s||b.meta.commit!==s)throw new Error('meta.commit != HEAD short');if(a.meta.cells!==143||b.meta.cells!==143||a.cells.length!==143||b.cells.length!==143)throw new Error('cells');if(a.meta.bot!==p.meta.bot||b.meta.bot!==q.meta.bot)throw new Error('Bot: line drifted');if(a.meta.seeds!==40||b.meta.seeds!==10||a.meta.startDepth!==1||b.meta.startDepth!==20)throw new Error('params');console.log('pair ok', s)" && git diff --quiet v1.4.0 -- engine content src mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` printed nothing immediately before run A was launched; `git diff --quiet v1.4.0 -- engine content src mazeworld.html` exited 0 immediately before run A and again after run B (both recorded in the SUMMARY).
    - `npm test` printed `# fail 0` before run A (recorded), and no `npm test` ran while either matrix run was in flight.
    - Both scratchpad logs end with the line `EXIT=0`; each contains exactly one `elapsed:` line; both wall times are recorded in the SUMMARY.
    - `node -e "const a=require('./docs/class-pass/v15-before.json');console.log(a.meta.seeds,a.meta.startDepth,a.meta.cells,a.cells.length)"` prints `40 1 143 143`; the depth-20 file prints `10 20 143 143`.
    - `node -e "const a=require('./docs/class-pass/v15-before.json'),p=require('./docs/class-pass/before.json');console.log(a.meta.bot===p.meta.bot)"` prints `true`; the same check for the depth-20 pair prints `true`.
    - `node -e "const {execSync}=require('node:child_process');const s=execSync('git rev-parse --short HEAD').toString().trim();const a=require('./docs/class-pass/v15-before.json'),b=require('./docs/class-pass/v15-before-depth20.json');console.log(a.meta.commit===s&&b.meta.commit===s)"` prints `true`.
    - `git status --porcelain` after this task lists exactly two untracked paths: `docs/class-pass/v15-before.json` and `docs/class-pass/v15-before-depth20.json`.
  </acceptance_criteria>
  <done>Both v1.5 BEFORE JSONs exist, were produced sequentially in the background against a clean tree whose engine is byte-identical to v1.4.0, carry the HEAD short hash as meta.commit and the byte-identical v1.2 Bot: lines, and both wall times are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Cannot-act gate, the '## v1.5 BEFORE' ledger section in docs/CLASS-PASS.md, and the additive guard extension in test/unit/class-pass-ledger.test.js</name>
  <files>docs/CLASS-PASS.md, test/unit/class-pass-ledger.test.js</files>
  <read_first>
    - docs/CLASS-PASS.md lines 119-140 (the v1.2 BEFORE heading/provenance paragraph shape), lines 860-870 and 1026-1072 (the AFTER section: its `**Zero cannot-act cells.**` first line, "### Pin and provenance" paragraph, the two "### … transcript — tune-classes …" sub-headings), lines 1481-1500 (roll-up table style)
    - docs/DIFFICULTY-RETUNE.md lines 2764-2775 ("### Comparison vs band" — the `Measure | JSON path | …` table shape whose numbers are read from rollups.pooled, never retyped)
    - test/unit/class-pass-ledger.test.js — the whole file (h2s/section helpers; tests (1) "exactly eight H2 headings", (2) BEFORE anchors, (4) AFTER hash, (5) meta parity, (6) cell keys, (7) cannot-act, (11) "Handoff … must be the last H2") — the v1.5 block mirrors (4)-(7); tests (1) and (11) are RELAXED ONLY by allowing v1.5 headings AFTER index 7, never by weakening any v1.2 assertion (the file header's own rule)
    - tools/class-pass-diff.mjs lines 80-100 (--gate usage), lines 104-167 (PARITY_FIELDS, metaParity, cannotActCells, cellKey exports)
  </read_first>
  <action>
    1. Gate: run `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json`; record the exact stdout (expected `cannot-act cells: 0 of 143`) and the exit code (expected 0; exit 3 would list the cells). If any cell is flagged, STOP and report `## PLANNING INCONCLUSIVE`-style in the SUMMARY (a cannot-act cell on an unchanged engine is a regression the pin must not paper over — Phase 26's `620e1df` precedent) — do not edit the engine in this plan.
    2. Pooled numbers — print with one `node -e` (never retype): from v15-before.json `rollups.pooled.p50Depth`, `.reach5`, `.reach10`, `.reach20`, `.meanDepth`, `.p90Depth`; from v15-before-depth20.json `rollups.pooled.meanEncountersSurvived`, `.p50FloorsGained`, `.meanFloorsGained`; stuck totals = sum of `cells[].stuck` for each file; and the SAME nine fields from docs/class-pass/retune-after.json / retune-after-depth20.json (the v1.2 close, pin 39bfecf) for the comparison column. Also print `git diff --stat 39bfecf -- tools/tune-classes.mjs tools/lib tools/class-pass-diff.mjs` (expected: only tools/lib/tuning-bot.mjs, 2 insertions — commit 8f3f9d8, Phase 31's Fight! split) for the provenance paragraph.
    3. Append to the END of docs/CLASS-PASS.md (after the last line of "## Handoff to Phase 27" — the file gains a NINTH H2; nothing above it changes) exactly this structure:
       - Heading: `## v1.5 BEFORE — commit <PIN 40-hex> (Phase 36 — BAL-01)` (em dashes as in the existing headings).
       - First non-blank line: `**Zero cannot-act cells.**` followed by one sentence with the gate's verbatim stdout (if the gate flagged cells, Task 2 step 1 already stopped).
       - `### Pin and provenance (Phase 36 capture)` — Captured <date>. Full hash + short hash in backticks; `git diff --quiet v1.4.0 -- engine content src mazeworld.html` exited 0 immediately before both runs and again after this ledger's own commit — engine, content, src and mazeworld.html byte-identical to tag `v1.4.0` (the 1.4.0 / versionCode 5 Play build); this pin is the v1.5 milestone's BEFORE and is taken before any Phase 36 engine change (Plans 02–06 land after it); harness provenance: the `git diff --stat 39bfecf` line from step 2 (Bot: line byte-identical to before.json's — the node -e equality check printed true for both files); both exact command lines; both wall times from the `elapsed:` lines; the statement that the v1.5 AFTER matrix and its verdicts are appended below this section by Phase 42 (BAL-02) after the tuning bot is taught the new abilities/spells/items. Do not use the phrase the guard test forbids for an unfilled placeholder ("Nothing recorded yet").
       - `### Pooled rollups (v1.5 BEFORE)` — a `| Measure | JSON path | v1.2 retune AFTER (39bfecf) | v1.5 BEFORE (<short>) |` table with the nine measures + two stuck-total rows + the cannot-act row, numbers pasted from step 2's output. One sentence under it: these are the like-for-like yardstick Phase 42 diffs against; TUNING PROXY, not a gate.
       - `### v1.5 BEFORE transcript — tune-classes --seeds 40 --workers 4 --max-actions 5000` — the verbatim STDOUT transcript in a fenced block.
       - `### v1.5 BEFORE transcript — tune-classes --seeds 10 --workers 4 --max-actions 5000 --start-depth 20` — likewise.
    4. Extend test/unit/class-pass-ledger.test.js ADDITIVELY:
       - Load `v15Before` / `v15BeforeDeep` from docs/class-pass/v15-before.json / v15-before-depth20.json next to the existing five JSON loads.
       - Test (1): change the expected count from 8 to 9 and append `/^## v1\.5 BEFORE — commit [0-9a-f]{40} \(Phase 36 — BAL-01\)$/` as the ninth regex; keep the eight v1.2 regexes at indices 0-7 unchanged; replace the "Handoff last" equality with `assert.equal(headings[7], "## Handoff to Phase 27")` (Handoff is the last v1.2 heading) — the same replacement in test (11)'s final assertion.
       - New test "v1.5 BEFORE hash equals v15-before.json and v15-before-depth20.json meta.commit": heading regex match; `hash.startsWith(v15Before.meta.commit)`; `v15Before.meta.commit === v15BeforeDeep.meta.commit`; the section (via `section(doc, /^## v1\.5 BEFORE — commit/)`) includes the backticked short hash, both verbatim `meta.bot` lines, `### Pin and provenance (Phase 36 capture)`, exactly two `### v1.5 BEFORE transcript — tune-classes` sub-headings, and its first non-blank line starts with `**Zero cannot-act cells.**`.
       - New test "v1.5 BEFORE meta parity with the v1.2 BEFORE pair (modulo commit)": `metaParity(before.meta, v15Before.meta).ok === true`, `metaParity(beforeDeep.meta, v15BeforeDeep.meta).ok === true`, `v15Before.meta.bot === before.meta.bot`, `v15BeforeDeep.meta.bot === beforeDeep.meta.bot`, and `v15Before.meta.commit !== after.meta.commit` (a fresh pin, not a copy).
       - New test "v1.5 BEFORE: identical 143 cell keys and zero cannot-act / zero stuck": `new Set(v15Before.cells.map(cellKey))` deep-equals the before.json key set and has size 143 (same for the deep pair); `cannotActCells(v15Before)` deep-equals `[]`; every v15Before cell has `stuck === 0` and `completed === c.n`; every v15BeforeDeep cell has `stuck === 0`; the same NaN walk as test (7) over both files.
       - Update the file header comment to name the ninth section and this plan (36-01) in one sentence.
    5. `npm test` → `# fail 0`. Then `git diff --quiet v1.4.0 -- engine content src mazeworld.html` exits 0 one more time (record). Commit all four files in ONE commit whose subject names the pin: `docs(36-01): v1.5 BEFORE class matrix — 143x40 natural + 143x10 depth-20, engine pinned <short> (byte-identical to v1.4.0)`; the body cites the full hash and both wall times.
    6. SUMMARY: the gate stdout, the pooled table, the hash, the commit, both wall times, the assumption-delta note from the frontmatter (`no-change`), the rng/serialized-field statements ("adds zero draws; no serialized-field change — no engine/content/src/shell file changed"), and a `## Human verification (deferred to end of run)` section stating there is nothing to verify on-device for this plan (it is a measurement) except: "(1) optional — open the Hero tab dossier on any character and confirm the build still reports 1.4.0 behaviour; no visible change expected from Plan 01".
  </action>
  <verify>
    <automated>node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json && node --test test/unit/class-pass-ledger.test.js && grep -c "^## v1.5 BEFORE — commit [0-9a-f]\{40\} (Phase 36 — BAL-01)$" docs/CLASS-PASS.md | grep -qx 1 && npm test 2>&1 | grep -q "^# fail 0" && git diff --quiet v1.4.0 -- engine content src mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json` prints `cannot-act cells: 0 of 143` and exits 0.
    - `grep -c "^## " docs/CLASS-PASS.md` prints 9; `grep -c "^## v1.5 BEFORE — commit [0-9a-f]\{40\} (Phase 36 — BAL-01)$" docs/CLASS-PASS.md` prints 1; the new section is the last H2 (`grep -n "^## " docs/CLASS-PASS.md | tail -1` names it).
    - `grep -c "^### v1.5 BEFORE transcript — tune-classes" docs/CLASS-PASS.md` prints 2; `grep -c "^### Pin and provenance (Phase 36 capture)$" docs/CLASS-PASS.md` prints 1; `grep -c "^### Pooled rollups (v1.5 BEFORE)$" docs/CLASS-PASS.md` prints 1.
    - The v1.5 section contains the literal strings `v15-before.json`, `v15-before-depth20.json`, `--seeds 40 --workers 4 --max-actions 5000`, `--start-depth 20`, `rollups.pooled.p50Depth`, `rollups.pooled.reach5`, `rollups.pooled.meanEncountersSurvived`, `rollups.pooled.p50FloorsGained`, `v1.4.0`, and both verbatim `Bot:` lines (the guard test asserts the last).
    - `grep -c "Nothing recorded yet" docs/CLASS-PASS.md` prints 0.
    - `node --test test/unit/class-pass-ledger.test.js` exits 0 with at least 14 tests (11 existing + 3 new); the file still contains the eight v1.2 heading regexes and `assert.equal(headings[7], "## Handoff to Phase 27")`.
    - `npm test` prints `# fail 0` and `# pass` >= 2182.
    - `git log -1 --format=%s` matches `docs(36-01): v1.5 BEFORE class matrix` and contains the short hash; `git show --stat HEAD --format=` lists exactly: docs/CLASS-PASS.md, docs/class-pass/v15-before-depth20.json, docs/class-pass/v15-before.json, test/unit/class-pass-ledger.test.js.
    - `git diff --stat v1.4.0 -- engine content src mazeworld.html` prints nothing.
  </acceptance_criteria>
  <done>The ledger carries the v1.5 BEFORE section with its pin, transcripts and pooled rollups; the guard test pins it additively; the cannot-act gate is clean; everything is committed with the pin in the subject; no engine/content/src/shell byte changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → docs ledger | numbers pasted from tool output into a human-read ledger (mis-transcription risk, not a security boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-01 | Tampering | docs/CLASS-PASS.md pooled table | low | mitigate | numbers printed by `node -e` from `rollups.pooled` and pasted, never retyped; the guard test pins hash/Bot:/cell keys/cannot-act against the JSONs |
| T-36-02 | Repudiation | pin provenance | low | mitigate | full 40-hex hash in the heading + commit subject; `git diff --quiet v1.4.0` recorded before/after |
| T-36-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (`git diff --stat -- package.json package-lock.json` empty) |
</threat_model>

<verification>
- Gate: `npm test` → `# fail 0`; no edit under engine/, content/, src/, mazeworld.html, test/parity/ (`git diff --stat v1.4.0 -- engine content src mazeworld.html` empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0); parity untouched by construction.
- Both JSONs committed; ledger section present; guard extended; commit subject carries the pin.
- rng-draw impact: adds zero draws. Serialized-field impact: none.
</verification>

<success_criteria>
- ROADMAP SC-1: a committed BEFORE class-matrix pin exists (mirroring `docs/class-pass/before*.json`), timestamped before any other v1.5 change lands, and `tools/tune-classes.mjs` ran cleanly (cannot-act 0 of 143).
- CONTEXT §BEFORE Matrix Pin honoured verbatim: Plan 01, v1.2 parameters, v15-* file names, background runs with EXIT sentinels and bounded polling, ledger appended to docs/CLASS-PASS.md.
</success_criteria>

<output>
Create `.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-01-SUMMARY.md` when done — with the pin hash, both command lines, both wall times, the gate stdout, the pooled table, the assumption-delta note (no-change), the rng/serialized-field statements, and the `## Human verification (deferred to end of run)` section.
</output>
