---
phase: 36-balance-foundation-effect-timers-small-independent-wins
plan: 04
type: execute
wave: 3
depends_on: ["36-02"]
files_modified:
  - engine/movement.js
  - engine/encounters.js
  - content/flavor.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - docs/CLASS-PASS.md
  - test/unit/identity-contract.test.js
  - test/unit/identity-world.test.js
  - test/unit/toastTable.test.js
  - test/voice/safety-scan.test.js
  - test/unit/cutthroat-joiner.test.js
autonomous: true
requirements: [CUT-01, CUT-02]
tags: [cutthroat, joiner, canon-reversal, new-rng-draw, new-event, voice, identity-contract, deferred-uat]
notes:
  wave_rationale: "wave 3 because Plan 02 edits engine/movement.js (per-step block) and this plan edits engine/movement.js (descend); runs in parallel with Plan 03 (zero file overlap)"
  rng_draw_impact: "adds exactly ONE d20 draw, in descend(), AFTER every existing draw (checkLevel, genFloor, reveal — the last statement before return), reachable ONLY when c.sub === 'Cutthroat' AND state.party has at least one member — false for every parity fixture (lose-plain seed 1119 IS a Cutthroat but its party is [] and it never descends), for every bot run (tools/lib/tuning-bot.mjs always declines Joiners), and for every pre-Phase-36 save (a Cutthroat could never have accepted a Joiner). meetJoiner's four draws are untouched (the refusal ternary is a pure read)."
  serialized_field_impact: "none — a murder splices state.party (existing field); no new field; joinerMurdered/joinerRefused are events, not state"

must_haves:
  truths:
    - "meetJoiner for a Cutthroat sets state.pendingJoiner (a full sheet) and emits joinerMet with NO joinerRefused; the rng cursor after the call equals a Soldier control's (4 draws); the Wilmsry-vs-Magic-User refusal still fires with reason wilmsry (CUT-01, ROADMAP SC-3)"
    - "engine/encounters.js no longer contains the lowercase cutthroat refusal reason string; src/browser/toasts.js no longer carries the Cutthroat refusal copy; joinerRefused keeps its wilmsry text and generic fallback (CUT-01)"
    - "content/flavor.js SUB_NOTE.Cutthroat states the odds in plain words (matches /one descent in twenty/i), mentions the first landed blow, and no longer says no Joiner will travel with you (CUT-01 'blurb rewritten to match'; the shell's own SUB_NOTE copy is synced in Plan 06)"
    - "cutthroatMurderCheck(state, rng, events) draws rng.d(20) only for a Cutthroat with a non-empty party; on a 1 it splices party[0] and pushes joinerMurdered { name, sub, depth }; on 2..20 nothing changes; for any other sub or an empty party it draws nothing; descend() calls it LAST — the new floor is identical with or without the draw and the cursor differs by exactly one d20 (test/unit/cutthroat-joiner.test.js) (CUT-02, ROADMAP SC-4)"
    - "EVENT_NARRATION.joinerMurdered, TOAST_FOR.joinerMurdered (priority feature, tone hurt) and RAIL_FAMILY.joinerMurdered (COMPANY / bad) exist; the narration picks one of the six JOINER_MURDER_LINES deterministically from name length + depth (no rng, all six reachable), html-escapes the name; both coverage guards and test/voice/safety-scan.test.js are green (CUT-02 'narrated with its own event type and several sarcastic lines')"
    - "test/unit/identity-contract.test.js's Cutthroat entry still shows one GOOD (first landed blow crits) and one BAD (the murder risk); docs/CLASS-PASS.md's good/bad table row matches and the table still has exactly 30 data rows"
    - "npm test prints '# fail 0'; fixtures untouched; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - path: "engine/movement.js"
      provides: "export function cutthroatMurderCheck(state, rng, events) + its call as descend()'s last statement"
    - path: "engine/encounters.js"
      provides: "meetJoiner refusal ternary reduced to the Wilmsry clause, with a DELIBERATE RULES CHANGE (Phase 36, CUT-01) note"
    - path: "content/flavor.js"
      provides: "SUB_NOTE.Cutthroat rewrite; JOINER_MURDER_LINES (6 lines, {name}/{depth} tokens)"
    - path: "src/browser/eventNarration.js"
      provides: "EVENT_NARRATION.joinerMurdered (deterministic line pick); joinerRefused comment updated"
    - path: "src/browser/toasts.js"
      provides: "TOAST_FOR.joinerMurdered; FEATURE_EVENTS += joinerMurdered; cutthroat refusal copy removed"
    - path: "src/browser/rail.js"
      provides: "RAIL_FAMILY.joinerMurdered { icon ◇, title COMPANY, tone bad }"
    - path: "test/unit/cutthroat-joiner.test.js"
      provides: "draw-order, gating, murder, offer, narration/toast/rail proofs"
  key_links:
    - from: "engine/movement.js#descend"
      to: "engine/movement.js#cutthroatMurderCheck"
      via: "called after events.push(floorChanged(...)), immediately before return events"
      pattern: "cutthroatMurderCheck\\(state, rng, events\\)"
    - from: "joinerMurdered event"
      to: "rail COMPANY/bad card"
      via: "move dispatch → toastsForAction (narrate ctx) → railCardFor → RAIL_FAMILY.joinerMurdered"
      pattern: "joinerMurdered"
  prohibitions:
    - "No change to meetJoiner's four draws or their order; no new draw anywhere except the one guarded d20 in descend"
    - "No fixture regenerated; no edit to test/parity/prototype-master.js.txt; no new declared divergence (no fixture is affected)"
    - "No loot/coin for a murdered Joiner (CONTEXT Deferred Ideas); no toast-only or inline surfacing — the murder line is a rail card + Oracle line (v1.4 ruling: the rail is the one feedback surface)"
    - "No edit to mazeworld.html in this plan (the shell's SUB_NOTE copy and Company panel are Plan 06)"
    - "No checkpoint:human-verify, no device step, no APK build"
---

<objective>
A Cutthroat can accept a Joiner (CUT-01 — the Phase 24 refusal reversed as a DECLARED canon change, blurb rewritten to match) and each descent with a Joiner carries a small, stated chance the Joiner is murdered — a natural 1 on a d20, "one descent in twenty" — narrated with its own event type and six sarcastic lines so it reads as a joke, not a bug (CUT-02).

Per CONTEXT §"Cutthroat & Joiner Dismissal" (locked): drop the `c.sub === "Cutthroat"` clause from `meetJoiner`'s refusal ternary (encounters.js:473) — a DELIBERATE RULES CHANGE comment replaces the Phase 24 one; the Wilmsry refusal stays; the dead Cutthroat refusal copy in toasts.js is removed (eventNarration's joinerRefused never had Cutthroat-specific copy — only its comment changes); zero rng change to meetJoiner. Murder odds: natural 1 on a d20 per descent; the draw fires ONLY when `c.sub === "Cutthroat" && state.party?.length`, placed after `genFloor`/`reveal` in `descend` (after every existing draw); victim = `state.party[0]` (PARTY_CAP is 1), removed from the roster; new event `joinerMurdered { name, sub, depth }`. Narration: 5–6 sarcastic family-friendly lines chosen deterministically with no rng in the presentation layer (hash of name + depth); rail card COMPANY / tone bad; EVENT_NARRATION + toast-table entries so both coverage guards stay green; voice scan on every new line. Blurb: `content/flavor.js` "Cutthroat" rewritten — first landed blow crits; Joiners now walk beside you; one descent in twenty one of them doesn't reach the next floor, and everyone knows why. `docs/CLASS-PASS.md` identity row (line 797) becomes the murder risk; `test/unit/identity-contract.test.js`'s Cutthroat bad assertion is updated in the same plan (one good + one bad must still show).

Task order is chosen so the suite is green at every commit: Task 1 lands the murder mechanics + narration while the old refusal still stands (the check is reachable only through a planted party); Task 2 flips the refusal and rewrites the BAD contract onto the murder; Task 3 gates.

Correction to CONTEXT's code pointer: the "~1231 negative refusal test" lives in `test/unit/identity-contract.test.js` (not joiner-acquisition.test.js, which has no Cutthroat reference); the other Cutthroat-refusal test sites are `test/unit/identity-world.test.js:80-110`, `test/unit/toastTable.test.js:91-93` and the `{ reason: "cutthroat" }` probe in `test/voice/safety-scan.test.js:143` — all handled in Task 2.

Purpose: ROADMAP SC-3 and SC-4; Pitfall 9 (a silent murder reads as a bug — dedicated event, several lines, odds stated in the blurb).
Output: the engine reversal + murder check, the content/presentation entries, the identity-contract/ledger updates, `test/unit/cutthroat-joiner.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-CONTEXT.md
@.planning/research/PITFALLS.md
@engine/encounters.js
@engine/movement.js
@content/flavor.js
@src/browser/eventNarration.js
@src/browser/toasts.js
@src/browser/rail.js
@test/unit/identity-contract.test.js
@test/parity/FIXTURE-INVENTORY.md
</context>

<artifacts_this_phase_produces>
Phase 36 as a whole creates the following symbols/files (this plan creates the items marked **[04]**):

- [01] `docs/class-pass/v15-before.json`, `docs/class-pass/v15-before-depth20.json`; `docs/CLASS-PASS.md` `## v1.5 BEFORE — commit … (Phase 36 — BAL-01)` section; additive v1.5 block in `test/unit/class-pass-ledger.test.js`.
- [02] `engine/effects.js`: `startEffect`, `startCooldown`, `tickRounds`, `tickSquares`, `remaining`, `isReady`, `clearRoundTimers`; serialized field `c.timers`; tick-site calls in movement.js/combat.js; `clearStaleTimers` in saveState.js; `stripTimersField` in comparables.js; `test/unit/effects.test.js`.
- [03] `engine/combat.js` `normalizeTarget(combat)`; two call sites refactored; shell post-dispatch call, dead-card `aria-disabled`, sweep exclusion; `test/unit/normalizeTarget.test.js`, `test/unit/shell-dead-foe-target.test.js`.
- **[04]** `engine/movement.js` `export function cutthroatMurderCheck(state, rng, events)` (called last in `descend`); new event `joinerMurdered { name, sub, depth }`; `engine/encounters.js#meetJoiner` refusal ternary reduced to the Wilmsry clause (CUT-01); `content/flavor.js` `SUB_NOTE.Cutthroat` rewrite + `JOINER_MURDER_LINES`; `EVENT_NARRATION.joinerMurdered`, `TOAST_FOR.joinerMurdered`, `RAIL_FAMILY.joinerMurdered`, `FEATURE_EVENTS += joinerMurdered`; `docs/CLASS-PASS.md` Cutthroat good/bad row; `test/unit/cutthroat-joiner.test.js`.
- [05] engine action `dismissJoiner { i? }`; events `joinerDismissed { name, sub }`, `dismissRefused { reason }`; `JOINER_PARTING_LINES`; narration/toast/rail entries; `FEATURE_EVENTS += dismissRefused`; `NARRATIVE_ACTIONS += dismissJoiner`; `test/unit/dismiss-joiner.test.js`.
- [06] `mazeworld.html`: classic `SUB_NOTE.Cutthroat` synced; `DISMISS_CONFIRM_MS`, `dismissConfirmRevert`, `revertDismissConfirm()`; Company sheet + two-tap `DISMISS` confirm in `renderPartyRoster()`; CSS `.mw-party-line`, `.mw-party-dismiss`; bridge `window.mzDismissJoiner(i)`; `test/unit/shell-company-panel.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CUT-02 — cutthroatMurderCheck in descend() (one guarded d20, last), the joinerMurdered event, six murder lines, and the narration/toast/rail entries (refusal still in place)</name>
  <files>engine/movement.js, content/flavor.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, test/voice/safety-scan.test.js, test/unit/cutthroat-joiner.test.js</files>
  <read_first>
    - engine/movement.js lines 691-702 (`descend`: spGained → checkLevel(draws) → genFloor(draws) → reveal → floorChanged push → return; the new call is the LAST statement), lines 389-399 (`nightlyEats` — the `state.party?.length` gate precedent), lines 28-37 (imports)
    - engine/state.js lines 45-96 (PARTY_CAP = 1, swapPartyMember — how tests plant a party member: a full rollCharacter()-shaped sheet with lvl/level/wp/maxWP)
    - engine/encounters.js lines 445-500 (meetJoiner — read-only here: its four draws and the pendingJoiner sheet shape `{ ...joinerChar, lvl, level: lvl, wp, maxWP: wp }`)
    - content/flavor.js lines 39-84 (SUB_NOTE and JOINER_EXIT_LINES — the pure-data line-bank precedent with {name}/{new} tokens and its JSDoc)
    - src/browser/eventNarration.js lines 36-47 (JOINER_EXIT_LINES import + escapeHtml), lines 565-590 (joinerLeft's deterministic index `(nameLen + newLen) % LINES.length` + replaceAll pattern — copy it)
    - src/browser/toasts.js line 47 (PRIORITY), lines 196-262 (FEATURE_EVENTS — add joinerMurdered beside joinerLeft), lines 1309-1321 (the joiner toast entries; `block()` helper at line 285)
    - src/browser/rail.js lines 98-145 (RAIL_FAMILY — the joinerJoined/Declined/Left rows to sit beside), lines 262-313 (railCardFor: head = lowest priority then lowest idx; RAIL_DIRECT floorChanged is PRIORITY.other, so a feature-priority joinerMurdered line becomes the head → COMPANY/bad)
    - test/unit/toastsCoverage.test.js lines 86-105 and 182-222 (event vocabulary derived from engine source; FEATURE_EVENTS ⊆ TOAST_FOR ∩ EVENT_NARRATION; identity-contract names ⊆ FEATURE_EVENTS)
    - test/voice/safety-scan.test.js lines 63, 113-134 (BASE_EVENT already carries name/depth), lines 232-286 (collectAuthoredStrings — add the new bank beside JOINER_EXIT_LINES at line ~276)
    - test/unit/identity-contract.test.js lines 68-100 (fakeRng — throws when exhausted, which is exactly how "draws nothing" is proven; countingRng) and lines 202-209 (expectEvent)
    - test/parity/FIXTURE-INVENTORY.md lines 680-690 (lose-plain, seed 1119: a Human Cutthroat fixture — it fights and dies, never descends, party [] — the gate MUST include the party check, not the sub alone)
    - .planning/research/PITFALLS.md Pitfall 1 (new draw strictly after existing draws, behind a condition false for every fixture) and Pitfall 9
  </read_first>
  <behavior>
    - cutthroatMurderCheck(cutthroatWithParty, fakeRng([1]), []) → returns events containing exactly one { type: "joinerMurdered", name: <member name>, sub: <member sub>, depth: state.floor.depth }; state.party deep-equals []
    - cutthroatMurderCheck(cutthroatWithParty, fakeRng([2]), []) and fakeRng([20]) → no event; party unchanged (same object, length 1)
    - cutthroatMurderCheck(soldierWithParty, fakeRng([]), []) → no draw (fakeRng would throw), no event, party unchanged
    - cutthroatMurderCheck(cutthroatNoParty, fakeRng([]), []) → no draw, no event; also with party undefined / null / non-array (fail-open, no throw)
    - descend draw order: A = Cutthroat state with a planted member, B = structuredClone(A) with party []; descend(A, makeRng(5)); descend(B, rngB = makeRng(5)); then rngB.d(20) → rngA.getState() === rngB.getState(); A.floor deepStrictEqual B.floor (the new floor is identical — the draw came AFTER genFloor/reveal); A.c.sp === B.c.sp; the joinerMurdered event, when it fires, is the LAST event and comes after floorChanged
    - descend zero-draw gate: C = structuredClone(A) with c.sub = "Soldier" (party kept) → descend(C, makeRng(5)) ends with the same cursor as B (no draw for a non-Cutthroat); D = Cutthroat with party [] → same cursor as B
    - a murder via descend: scan seeds 1..200 for the first seed where descend(cutthroatWithParty clone, makeRng(seed)) emits joinerMurdered — pin that seed as a measured constant in the test (project convention: measured, not hand-computed) and assert party [] afterwards and the event's depth === the NEW floor's depth
    - EVENT_NARRATION.joinerMurdered({ name: "Ada Brook", sub: "Guard", depth: 3 }) → a non-empty string containing "Ada Brook"; the same input always yields the same line; over depths 1..6 with a fixed name the six outputs are all different (every line reachable); a name containing "<b>" renders as "&lt;b&gt;"; a bare { type } call does not throw
    - TOAST_FOR.joinerMurdered(e).priority === PRIORITY.feature and .tone === "hurt"; text contains the name; FEATURE_EVENTS includes "joinerMurdered"
    - RAIL_FAMILY.joinerMurdered deep-equals { icon: "◇", title: "COMPANY", tone: "bad" }; railCardFor("move", [spGained, floorChanged(3), joinerMurdered], toastsForAction("move", events, { narrate: narrateEvent }, { limit: Infinity, withIdx: true }), { narrate: narrateEvent }) returns a card with title "COMPANY", tone "bad", whose first line text is the murder sentence (dice stripped)
  </behavior>
  <action>
    1. Write `test/unit/cutthroat-joiner.test.js` first with the behaviour bullets above (at least 14 tests; local fakeRng/countingRng copied from identity-contract; `hero(sub)` = `newRun(seed, [], { force: { sub } })` with `combat = null`, `pendingJoiner = null`; plant a member with `swapPartyMember(state, { ...rollCharacter(makeRng(99)), lvl: 2, level: 2, wp: 40, maxWP: 40 })`). Run — engine/narration tests fail.
    <!-- planner-discipline-allow: c.sub === "Cutthroat" -->
    2. `engine/movement.js`: add `export function cutthroatMurderCheck(state, rng, events = [])` directly above `descend` with a JSDoc: DELIBERATE RULES CHANGE (Phase 36, 2026-09-17, CUT-02) — the Cutthroat's Joiner risk, made real now that Joiners travel with a Cutthroat (CUT-01): once per descent, a natural 1 on a d20 (one descent in twenty — the blurb states the odds in plain words) means the Joiner does not reach the next floor. GATE: `state.c.sub === "Cutthroat" && Array.isArray(state.party) && state.party.length > 0` — the d20 is drawn ONLY inside the gate, so every fixture (lose-plain seed 1119 is a Cutthroat with party []), every bot run (the bot declines every Joiner) and every pre-Phase-36 save (a Cutthroat could never have accepted one) draws nothing and stays byte-identical; victim = `state.party.splice(0, 1)[0]` (index 0 — PARTY_CAP is 1; FIFO if it ever rises); pushes `joinerMurdered` with `name`, `sub`, `depth: state.floor.depth`. Returns events. In `descend`, add `cutthroatMurderCheck(state, rng, events);` as the LAST statement before `return events;` (after the floorChanged push) with a one-line comment: LAST — after checkLevel and genFloor (both draw) and after floorChanged, so the murder is narrated on the new floor and the new floor is identical with or without the draw.
    3. `content/flavor.js`: add `export const JOINER_MURDER_LINES = [ … ]` after JOINER_EXIT_LINES with a mirroring JSDoc (Phase 36 CUT-02; `{name}` the victim, `{depth}` the floor arrived on; picked by eventNarration.js WITHOUT rng from name length + depth; family-friendly sarcasm — the joke is the Cutthroat's reputation, never the body; scanned by safety-scan). Exactly these six lines, verbatim:
       - "Somewhere between floors, {name} had an accident. You were the accident."
       - "{name} did not make it down the stairs. The stairs were not the problem."
       - "You arrive on floor {depth} one companion lighter. {name} would have objected, given the chance."
       - "{name} stopped to admire the stonework on the way down. That is the official version, and you are sticking to it."
       - "Old habits. {name} stayed behind on the stairs, and will be staying."
       - "Travelling beside a Cutthroat is mostly arithmetic, and {name} has just been subtracted."
    4. `src/browser/eventNarration.js`: extend the flavor import to `{ JOINER_EXIT_LINES, JOINER_MURDER_LINES }`; add `joinerMurdered: (e) => { … }` directly after `joinerLeft`: `rawName = e.name ?? "Your companion"`, `depth = Number.isFinite(e.depth) ? e.depth : 0`, `idx = (String(rawName).length + depth) % JOINER_MURDER_LINES.length`, line = the template with `{name}` → `escapeHtml(rawName)` and `{depth}` → `String(depth)` via replaceAll, returned inside `<span class="hurt">…</span>`. Comment: Phase 36 (CUT-02) — deterministic pick, no rng, name escaped like joinerLeft.
    5. `src/browser/toasts.js`: add `joinerMurdered: (e) => ({ text: `${e?.name ?? "Your companion"} did not reach floor ${e?.depth ?? "?"}.`, tone: "hurt", priority: PRIORITY.feature })` directly after `joinerLeft` (comment: fallback/coverage text — on the move action the narrative ctx replaces it with the EVENT_NARRATION line); add `"joinerMurdered",` to FEATURE_EVENTS directly after `"joinerLeft",`.
    6. `src/browser/rail.js`: add `joinerMurdered: { icon: "◇", title: "COMPANY", tone: "bad" },` directly after the `joinerLeft` row.
    7. `test/voice/safety-scan.test.js`: extend the flavor import with `JOINER_MURDER_LINES` and add `JOINER_MURDER_LINES.forEach((s, i) => push(`JOINER_MURDER_LINES[${i}]`, s));` directly after the JOINER_EXIT_LINES line in collectAuthoredStrings.
    8. `node --test test/unit/cutthroat-joiner.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/rail.test.js` → green; `npm test` → `# fail 0`.
  </action>
  <verify>
    <automated>node --test test/unit/cutthroat-joiner.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/rail.test.js test/unit/toastTable.test.js && test "$(grep -c '^export function cutthroatMurderCheck(state, rng, events = \[\])' engine/movement.js)" = "1" && test "$(grep -c 'cutthroatMurderCheck(state, rng, events);' engine/movement.js)" = "1" && test "$(grep -c '^export const JOINER_MURDER_LINES' content/flavor.js)" = "1" && npm test 2>&1 | grep -q "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^export function cutthroatMurderCheck(state, rng, events = \[\])" engine/movement.js` prints 1; `grep -c "cutthroatMurderCheck(state, rng, events);" engine/movement.js` prints 1 and (via `grep -n`) its line is greater than the line of `events.push(floorChanged(state.floor.depth));` and is the line immediately before descend's `return events;`.
    - `grep -c 'rng.d(20) === 1' engine/movement.js` prints 1; `grep -c 'state.party.splice(0, 1)' engine/movement.js` prints at least 1.
    - `node -e "import('./content/flavor.js').then(m=>console.log(m.JOINER_MURDER_LINES.length, m.JOINER_MURDER_LINES.every(s=>s.includes('{name}'))))"` prints `6 true`.
    - `grep -c "joinerMurdered" src/browser/eventNarration.js` >= 1; `grep -c "joinerMurdered" src/browser/toasts.js` >= 2 (TOAST_FOR entry + FEATURE_EVENTS); `grep -c 'joinerMurdered: { icon: "◇", title: "COMPANY", tone: "bad" }' src/browser/rail.js` prints 1.
    - `grep -c "JOINER_MURDER_LINES" test/voice/safety-scan.test.js` prints 2 (import + push loop).
    - `node --test test/unit/cutthroat-joiner.test.js` exits 0 with at least 14 tests, one of whose names contains `after genFloor`; `npm test` prints `# fail 0`; `git status --porcelain test/parity/fixtures` prints nothing.
  </acceptance_criteria>
  <done>The murder check exists as descend's last, guarded, single-d20 statement; its event is narrated by six deterministic lines through Oracle, toast table and rail; every guard is green while the old refusal still stands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CUT-01 — reverse the Cutthroat refusal (declared canon change), rewrite the blurb, remove the dead refusal copy, and move the identity contract + ledger row onto the murder risk</name>
  <files>engine/encounters.js, content/flavor.js, src/browser/eventNarration.js, src/browser/toasts.js, test/unit/identity-contract.test.js, test/unit/identity-world.test.js, test/unit/toastTable.test.js, test/voice/safety-scan.test.js, test/unit/cutthroat-joiner.test.js, docs/CLASS-PASS.md</files>
  <read_first>
    - engine/encounters.js lines 445-491 (meetJoiner — the header's Phase 24 paragraph and line 473's ternary `c.sub === "Cutthroat" ? … : c.race === "Wilmsry" && joinerChar.cls === "Magic User" ? "wilmsry" : null`)
    - content/flavor.js line 52 (the current Cutthroat blurb) and lines 39-44 (SUB_NOTE JSDoc)
    - src/browser/toasts.js lines 1315-1321 (joinerRefused's reason map — delete the Cutthroat row, keep wilmsry and the fallback); src/browser/eventNarration.js lines 586-591 (joinerRefused — copy is wilmsry-or-fallback already; only the comment names the Cutthroat)
    - test/unit/identity-contract.test.js lines 773-806 (the Cutthroat CONTRACT entry — GOOD stays, BAD is rewritten), lines 1231-1238 (negative test — still valid, keep), lines 1269-1291 (two idempotency tests — assertions stay, retitle the second from "refusal" to "offer"), lines 1176-1210 (how CONTRACT is iterated and validated)
    - test/unit/identity-world.test.js lines 80-110 (the two Cutthroat meetJoiner tests to rewrite as offer tests) and lines 112-121 (the Wilmsry pinned-seed test — keep)
    - test/unit/toastTable.test.js lines 91-93 (the joinerRefused fallback-vs-specific assertion using reason cutthroat — switch it to wilmsry)
    - test/voice/safety-scan.test.js line 143 (BRANCH_TOGGLES `{ reason: "cutthroat" }` — remove that probe; wilmsry stays)
    - docs/CLASS-PASS.md line 797 (the Cutthroat good/bad row) and test/unit/class-pass-ledger.test.js lines 128-149 (the table must keep exactly 30 data rows)
    - test/unit/toastsCoverage.test.js lines 198-221 (every event name identity-contract asserts must be in FEATURE_EVENTS — joinerMurdered was added in Task 1)
    - .planning/ROADMAP.md "Engine Gate" (deliberate canon divergences declared per phase with a rationale)
  </read_first>
  <behavior>
    - meetJoiner(hero("Cutthroat"), makeRng(555), []) → state.pendingJoiner is a full sheet (name/race/sub/cls/lvl/level/wp/maxWP), events contain joinerMet and NO joinerRefused; c.joiner deep-equals a Soldier control's c.joiner for the same seed; the next rng.d(20) after the call equals the Soldier control's (4 draws either way)
    - meetJoiner for a Wilmsry Soldier with makeRng(1) still refuses with reason "wilmsry" and pendingJoiner null (unchanged)
    - full flow: Cutthroat meets (555) → resolveJoiner(state, true) → party length 1, joinerJoined → cutthroatMurderCheck(state, fakeRng([1])) → joinerMurdered naming that member, party []
    - identity-contract Cutthroat BAD (new): a Cutthroat with an accepted Joiner loses it on a natural 1 (fakeRng([1]) → joinerMurdered, party []); the Soldier control with the same planted member draws nothing (fakeRng([])) and keeps it — one good, one bad still shows
    - TOAST_FOR.joinerRefused({ reason: "wilmsry" }).text !== the fallback text; SUB_NOTE.Cutthroat matches /one descent in twenty/i, matches /first landed blow/i, and does not match /no Joiner will ever/i
  </behavior>
  <action>
    1. Extend `test/unit/cutthroat-joiner.test.js` with the first three behaviour bullets (offer, Wilmsry unchanged, full flow) — they fail until step 2.
    2. `engine/encounters.js` line 473: the ternary becomes `const refusal = c.race === "Wilmsry" && joinerChar.cls === "Magic User" ? "wilmsry" : null;`. In meetJoiner's JSDoc, replace the sentence "No Joiner ever agrees to travel with a Cutthroat; a Magic User Joiner refuses a Wilmsry (any other class still joins a Wilmsry normally)." with: DELIBERATE RULES CHANGE (Phase 36, 2026-09-17, CUT-01): the Phase 24 Cutthroat refusal is REVERSED — a Joiner now travels with a Cutthroat like any other hero (the offer proceeds through resolveJoiner unchanged); the Cutthroat's bad is instead the per-descent murder risk in movement.js#cutthroatMurderCheck (CUT-02). A Magic User Joiner still refuses a Wilmsry (any other class still joins a Wilmsry normally). Rationale: "no Joiner ever" made a whole party feature unreachable for one sub-class; a stated 1-in-20 loss keeps the identity (the reputation) and the joke. Zero rng change: the four draws are untouched; the refusal remains a pure read. Keep the rest of the header verbatim.
    3. `content/flavor.js` line 52 — the Cutthroat blurb becomes exactly: "Double damage on your first landed blow, and Joiners will walk beside you all the same — word travels, but so do they. One descent in twenty, the one beside you does not reach the next floor, and everyone knows why. Nobody has ever asked you to explain. You have never offered."
    4. `src/browser/toasts.js`: delete the Cutthroat row of joinerRefused's reason map (the map keeps only `wilmsry`; the `block(map[e?.reason] ?? "Word has reached the Joiners.")` fallback stays). `src/browser/eventNarration.js`: update the joinerRefused comment to say only a Magic User Joiner refuses a Wilmsry (Phase 36 CUT-01 reversed the Cutthroat refusal) — no code change there.
    5. `test/unit/identity-contract.test.js`: import `cutthroatMurderCheck` from `../../engine/movement.js` and `resolveJoiner` from `../../engine/encounters.js` (extend the existing import lines). Rewrite the Cutthroat `bad` entry: `name: "one descent in twenty, the Joiner beside you does not reach the next floor"`; `run()`: Cutthroat hero → `meetJoiner(state, makeRng(555), [])` → assert `state.pendingJoiner` non-null → `resolveJoiner(state, true, [])` → assert party length 1 → `const events = cutthroatMurderCheck(state, fakeRng([1]), [])` → `expectEvent(events, "joinerMurdered", { name: <the member's name>, sub: <member sub> })` → assert `state.party.length === 0`; control: Soldier hero, same meet/accept with makeRng(555), `cutthroatMurderCheck(control, fakeRng([]), [])` → no event, party length stays 1. Retitle the 1283 idempotency test to "…a Cutthroat's meetJoiner OFFER consumes the identical rng cursor as an accepted Soldier control" and its assertion message (assertions unchanged). Keep the 1231 negative and 1269 idempotency tests as they are.
    6. `test/unit/identity-world.test.js`: rewrite the test at line 80 as "meetJoiner: a Cutthroat's Joiner is rolled exactly as usual and OFFERED — pendingJoiner set, joinerMet fires, no joinerRefused (Phase 36 CUT-01)" (assert `pendingJoiner` non-null with `name === state.c.joiner.name`, `joinerMet` present, no `joinerRefused`, `c.joiner` deep-equals the Soldier control's); retitle the test at line 98 to "…a Cutthroat's OFFER consumes NO extra rng…" (assertion unchanged).
    <!-- planner-discipline-allow: reason: "cutthroat" -->
    7. `test/unit/toastTable.test.js` lines 91-93: use `reason: "wilmsry"` in place of the Cutthroat reason for the specific-vs-fallback inequality. `test/voice/safety-scan.test.js` line 143: remove the `{ reason: "cutthroat" }` toggle (it is being DELETED, not written; keep `{ reason: "wilmsry" }`).
    8. `docs/CLASS-PASS.md` line 797 becomes exactly: `| Cutthroat | The first landed blow always crits, even in armor a backstab would refuse | One descent in twenty, the Joiner beside you does not reach the next floor |` (row count stays 30; header untouched).
    9. `node --test test/unit/cutthroat-joiner.test.js test/unit/identity-contract.test.js test/unit/identity-world.test.js test/unit/toastTable.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/class-pass-ledger.test.js test/unit/joiner-acquisition.test.js` → green; `npm test` → `# fail 0`.
  </action>
  <verify>
    <automated>node --test test/unit/cutthroat-joiner.test.js test/unit/identity-contract.test.js test/unit/identity-world.test.js test/unit/toastTable.test.js test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/class-pass-ledger.test.js && test "$(grep -c 'c.sub === "Cutthroat"' engine/encounters.js)" = "0" && test "$(grep -c 'wants no part of a Cutthroat' src/browser/toasts.js)" = "0" && node -e "import('./content/flavor.js').then(m=>{const s=m.SUB_NOTE.Cutthroat;if(!/one descent in twenty/i.test(s)||!/first landed blow/i.test(s)||/no Joiner will ever/i.test(s))throw new Error(s);console.log('blurb ok')})" && npm test 2>&1 | grep -q "^# fail 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const refusal = c.race === "Wilmsry" && joinerChar.cls === "Magic User" ? "wilmsry" : null;' engine/encounters.js` prints 1; `grep -c 'c.sub === "Cutthroat"' engine/encounters.js` prints 0; `grep -c "DELIBERATE RULES CHANGE (Phase 36" engine/encounters.js` prints 1.
    - `grep -c "wants no part of a Cutthroat" src/browser/toasts.js` prints 0; `grep -c 'wilmsry:' src/browser/toasts.js` prints 1 (the map keeps its Wilmsry row).
    - `node -e "import('./content/flavor.js').then(m=>console.log(/one descent in twenty/i.test(m.SUB_NOTE.Cutthroat), /first landed blow/i.test(m.SUB_NOTE.Cutthroat), /no Joiner will ever/i.test(m.SUB_NOTE.Cutthroat)))"` prints `true true false`.
    - `grep -c 'name: "one descent in twenty, the Joiner beside you does not reach the next floor"' test/unit/identity-contract.test.js` prints 1; `grep -c 'reason: "cutthroat"' test/unit/identity-contract.test.js test/unit/identity-world.test.js test/unit/toastTable.test.js test/voice/safety-scan.test.js` prints 0 for every file.
    - `grep -c "| Cutthroat | The first landed blow always crits, even in armor a backstab would refuse | One descent in twenty, the Joiner beside you does not reach the next floor |" docs/CLASS-PASS.md` prints 1; `grep -c "No Joiner will ever travel with you" docs/CLASS-PASS.md` prints 0; `node --test test/unit/class-pass-ledger.test.js` exits 0 (30 rows).
    - `node --test test/unit/cutthroat-joiner.test.js` exits 0 with at least 17 tests; `npm test` prints `# fail 0`; `git status --porcelain test/parity/fixtures` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
  </acceptance_criteria>
  <done>A Cutthroat is offered Joiners like anyone else, the blurb and ledger state the 1-in-20 risk in plain words, the dead refusal copy is gone, and the identity contract shows one good + one bad on the new rule.</done>
</task>

<task type="auto">
  <name>Task 3: Gate + SUMMARY (rng/serialized-field statements, canon-divergence declaration, deferred on-device checks)</name>
  <files>docs/CLASS-PASS.md</files>
  <read_first>
    - .planning/ROADMAP.md "Engine Gate" (declared divergences per phase) and Phase 36 Success Criteria 3-4
    - .planning/milestones/v1.4-phases/35-map-screen-rebuild/35-01-SUMMARY.md (the deferred-verification section shape)
  </read_first>
  <action>
    1. Record: `npm test` totals; `git diff --stat -- test/parity package.json package-lock.json mazeworld.html` (empty — this plan never touches the shell); master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat v1.4.0 -- engine content src` (this plan's files + Plans 02/03's).
    2. Declare the canon divergence in the SUMMARY under `## Declared canon divergence (Phase 36, CUT-01/CUT-02)`: Phase 24's unconditional Cutthroat refusal reversed; 1-in-20 per-descent murder added; NO fixture affected (gate false everywhere — cite lose-plain seed 1119 as the one Cutthroat fixture, party []), so no before/after fixture table and no regeneration.
    3. State: rng-draw impact "adds one d20 draw in descend, last, reachable only when c.sub === Cutthroat and party.length > 0 — never for a fixture, bot, or pre-Phase-36 save"; serialized-field impact "none".
    4. `## Human verification (deferred to end of run)`: (1) roll a Cutthroat, walk until a Joiner is met — the rail shows the COMPANY offer card (TAKE THEM ALONG / LEAVE THEM) instead of a refusal; (2) accept, open the Hero tab — the Company panel lists them; (3) descend floor after floor until the murder line fires (1-in-20 per descent — may take a while; the dev start-depth harness can speed floors) — the rail shows a COMPANY card in the bad tone with one of the six lines, the Oracle log carries the same line, and the Company panel is empty afterwards; (4) as a Wilmsry meeting a Magic User Joiner, the refusal still reads as before; (5) read the Cutthroat dossier blurb on the Hero tab — it states "one descent in twenty" (note: the dossier text itself is synced in Plan 06; until then the Hero tab still shows the old shell copy).
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q "^# fail 0" && test -z "$(git diff --stat -- test/parity package.json package-lock.json mazeworld.html)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`; `git diff --stat -- test/parity package.json package-lock.json mazeworld.html` prints nothing; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0.
    - 36-04-SUMMARY.md contains the headings `## Declared canon divergence (Phase 36, CUT-01/CUT-02)` and `## Human verification (deferred to end of run)` (with at least 5 numbered checks), the phrase `adds one d20 draw`, and the phrase `lose-plain`.
  </acceptance_criteria>
  <done>Gate green and recorded; the divergence is declared; the on-device checks are queued for the milestone-close batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine event → presentation markup | a member name is interpolated into narration HTML |
| rng stream | a new draw must not reorder any existing draw |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-36-09 | Tampering | descend rng order | high | mitigate | the d20 is the last statement, inside a gate false for every fixture/bot/old save; the draw-order test proves the new floor is identical and the cursor differs by exactly one draw |
| T-36-10 | Information Disclosure / XSS | EVENT_NARRATION.joinerMurdered | medium | mitigate | escapeHtml on the name (joinerLeft precedent); test with a name containing a tag |
| T-36-11 | Repudiation | canon reversal | low | mitigate | DELIBERATE RULES CHANGE comment in encounters.js + SUMMARY declaration + ledger row |
| T-36-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase |
</threat_model>

<verification>
- `npm test` `# fail 0`; fixtures untouched; master hash unchanged; no shell/package change from this plan.
- Source pins: ternary reduced; blurb odds in plain words; six lines; narration/toast/rail entries; FEATURE_EVENTS; ledger row; identity contract one good + one bad.
- rng-draw impact: one guarded d20 in descend (last). Serialized-field impact: none.
</verification>

<success_criteria>
- ROADMAP SC-3: a Cutthroat who meets a Joiner sees the acceptance offer (the old refusal is gone) and the sub-class blurb matches (content side; the shell dossier copy syncs in Plan 06).
- ROADMAP SC-4: each descent with a Joiner as a Cutthroat carries a small, stated chance (1-in-20, in the blurb) the Joiner turns up murdered, narrated with its own event type and six sarcastic lines.
</success_criteria>

<output>
Create `.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-04-SUMMARY.md` when done — gate record, `## Declared canon divergence (Phase 36, CUT-01/CUT-02)`, rng/serialized-field statements, `## Human verification (deferred to end of run)`.
</output>