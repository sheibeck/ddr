---
phase: 41-terrain-darkness-phobias
plan: 04
type: execute
wave: 4
depends_on: ["41-03"]
files_modified:
  - engine/derived.js
  - src/browser/mapMarks.js
  - src/browser/rail.js
  - src/browser/tapStep.js
  - mazeworld.html
  - test/unit/darkness-filter.test.js
  - test/unit/mapMarks.test.js
  - test/unit/tapStep.test.js
  - test/unit/rail.test.js
  - test/unit/shell-terrain-41.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/TERRAIN.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TERR-01, TERR-02, TERR-03, TERR-04, TERR-05]
tags: [shell, darkness-filter, render-filter, map-paint, water-tint, condition-chip, hold-inspect, docs-ledger, phase-close, deferred-uat]

must_haves:
  truths:
    - "While standing on a dark square without Night Vision, a light effect (Amulet of Light) or a lit torch, `draw()` paints only the 3x3 window around the party; every other explored cell is fogged at RENDER time only — `cell.seen` and `cell.spellSeen` are never touched — and stepping off the dark square restores the whole previously explored view (ROADMAP SC-3, TERR-03)"
    - "Water cells paint in a distinct blue (`MAP_PALETTE.water`) and a darker water shade on a dark tile (`MAP_PALETTE.waterDark`); the Phase 40 spellSeen tint keeps its priority and its source pin (ROADMAP SC-1's 'blue water pool on the map')"
    - "A hold-inspect on a water cell names it and its cost; the armed-fear condition chip reads 'Rattled' with a plain-language tap explanation (TERR-05's visibility between the trigger and the next fight)"
    - "The HUD Squares counter still reads `S.steps` (it jumps by 2 on a water step with no shell change); no classic dead-code path (the shell's own legacy move()/reveal()) is edited"
    - "docs/TERRAIN.md is closed (UI + Requirements map, no placeholders), REQUIREMENTS.md marks TERR-01..05 complete, and the phase's aggregated Pixel 7 checklist + the two Key Decisions for PROJECT.md are in this plan's SUMMARY"
  artifacts:
    - "engine/derived.js#mapViewRadius(state), inViewWindow(state, x, y), DARK_VIEW_RADIUS = 1"
    - "src/browser/mapMarks.js MAP_PALETTE.water / MAP_PALETTE.waterDark"
    - "src/browser/rail.js RAIL_COPY.water; src/browser/tapStep.js#inspectCell water branch"
    - "mazeworld.html: window.__mzMapView bridge, draw() render filter + water override, CONDITION_COPY/TONE/EXPLAIN.fearArmed"
    - "test/unit/darkness-filter.test.js, test/unit/shell-terrain-41.test.js"
    - "docs/TERRAIN.md closed; .planning/REQUIREMENTS.md TERR-01..05 complete"
  key_links:
    - "classicBoot import block: import { ..., mapViewRadius, inViewWindow } from ./engine/derived.js -> window.__mzMapView = { mapViewRadius, inViewWindow } -> draw() reads it fresh every paint (no stored flag — research Pitfall 4)"
    - "draw(): if (!c.seen) continue; then the view-window early-continue; then the existing spellSeen/dark fill line (Phase 40 pin intact) followed by the water override"
    - "conditionsOf fearArmed chip (Plan 03 data) -> CONDITION_COPY.fearArmed label Rattled -> paintConditions"
---

<objective>
Close Phase 41 in the shell: the TERR-03 darkness render filter (a pure `mapViewRadius`/`inViewWindow` read in `engine/derived.js`, consumed by `draw()` as a 3x3 window over already-`seen` cells — the engine's memory untouched), the water map paint (lit + dark shades), the water hold-inspect line, the "Rattled" chip copy for the armed fear, the source-pin tests, the docs/TERRAIN.md close + requirements map, REQUIREMENTS.md completion, the whole-phase gate, and the aggregated Pixel 7 checklist with the two Key Decisions for PROJECT.md.

Purpose: SC-1's "blue water pool on the map" and SC-3's "3x3 in the dark, restored on leaving" are what the player actually sees; everything Plans 01-03 built becomes visible here, and the phase's deferred human verification is consolidated for the milestone-close batch.

Output: `mapViewRadius`/`inViewWindow`, palette keys, `RAIL_COPY.water`, the `draw()` edits, the chip rows, `test/unit/darkness-filter.test.js`, `test/unit/shell-terrain-41.test.js`, docs/TERRAIN.md closed, REQUIREMENTS.md updated, 41-04-SUMMARY.md with the phase checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-terrain-darkness-phobias/41-CONTEXT.md
@.planning/phases/41-terrain-darkness-phobias/41-RESEARCH.md
@.planning/phases/41-terrain-darkness-phobias/41-01-SUMMARY.md
@.planning/phases/41-terrain-darkness-phobias/41-02-SUMMARY.md
@.planning/phases/41-terrain-darkness-phobias/41-03-SUMMARY.md
@.planning/phases/40-spell-rework/40-05-SUMMARY.md
@engine/derived.js
@src/browser/mapMarks.js
@src/browser/tapStep.js
@src/browser/rail.js
@test/unit/shell-spells-40.test.js
@docs/TERRAIN.md
</context>

<standing_rules>
- This is the ONLY plan that edits `mazeworld.html`. Edit only: the classicBoot import/bridge block (~lines 7029-7075), `draw()` (~lines 2661-2740) and the `CONDITION_COPY`/`CONDITION_TONE`/`CONDITION_EXPLAIN` tables (~lines 2892-3040). Do NOT touch the shell's legacy classic `move()`/`reveal()`/`newDay()` copies (dead code owned by the pending "Shell Debt & Dead Code" cleanup milestone — STATE.md Pending Todos).
- The Phase 40 source pin on `draw()`'s fill line (`test/unit/shell-spells-40.test.js` line 125 regex) MUST stay green: the water paint is an OVERRIDE line after it, never a rewrite of it.
- Rail is the one feedback surface; a phobia trigger stays a narrative card (Plan 03); the chip is a HUD condition like every other.
- Deferred UAT; verification agents OFF: the LAST task runs the WHOLE-PHASE gate (`npm test` `# fail 0`, master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0, `git status --porcelain test/parity/fixtures` clean) and records the numbers; it also aggregates the phase's Pixel 7 checklist (grouped by plan, numbered continuously, de-duplicated, in the 40-05-SUMMARY format) and a `## For PROJECT.md` section with the two Key Decisions (water cost; arm-Afraid shape).
- Commits per task: `feat(41-04): …` / `test(41-04): …` / `docs(41-04): …` with the two trailer lines (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`). Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
- Voice: family-friendly, deadpan; every new string passes `test/voice/safety-scan.test.js` and the rail/shell voice scans.
</standing_rules>

<artifacts_this_phase_produces>
See 41-01-PLAN.md's phase-wide list. This plan creates: `engine/derived.js#mapViewRadius`, `#inViewWindow`, `DARK_VIEW_RADIUS`; `MAP_PALETTE.water` / `MAP_PALETTE.waterDark`; `RAIL_COPY.water`; the `inspectCell` water branch; `mazeworld.html`'s `window.__mzMapView` bridge, `draw()` render filter + water override, `CONDITION_COPY.fearArmed` / `CONDITION_TONE.fearArmed` / `CONDITION_EXPLAIN.fearArmed`; `test/unit/darkness-filter.test.js`; `test/unit/shell-terrain-41.test.js`; docs/TERRAIN.md `## Darkness filter, map paint and UI (Plan 04)` + `## Requirements map (Plan 04)`; `.planning/REQUIREMENTS.md` TERR-01..05 complete; the phase-close SUMMARY with the aggregated checklist and `## For PROJECT.md`.
</artifacts_this_phase_produces>

<tasks>
<!-- planner-discipline-allow: (appended by Plan -->

<task type="auto" tdd="true">
  <name>Task 1: mapViewRadius / inViewWindow pure reads, water palette keys, water hold-inspect copy</name>
  <files>engine/derived.js, src/browser/mapMarks.js, src/browser/rail.js, src/browser/tapStep.js, test/unit/darkness-filter.test.js, test/unit/mapMarks.test.js, test/unit/tapStep.test.js, test/unit/rail.test.js</files>
  <read_first>
    - engine/derived.js lines 677-705 (`inDark`, `revealRadius` — the waiver vocabulary: `skill(c, "Night Vision")`, `eff(c, "light")`), 355-390 (`liveItemEffects`/`itemEffectActive` — a lit torch is `c.timers["item:Torch"] = { cadence: "squares", left: N, phase: "effect" }`, see test/unit/tools.test.js line 626)
    - .planning/phases/41-terrain-darkness-phobias/41-RESEARCH.md "Pattern 3" (the pure render-time filter; never mutate `seen`; never a stored flag) and "Anti-Patterns"
    - src/browser/mapMarks.js lines 15-45 (`MAP_PALETTE` — frozen; the Phase 40 `floorSpell` comment style) and test/unit/mapMarks.test.js lines 45-70 (the palette pins)
    - src/browser/rail.js lines 60-95 (`RAIL_COPY` — `unwalked`/`rock`/`empty` rows) and src/browser/tapStep.js lines 85-105 (`inspectCell`'s branch order)
    - test/unit/tapStep.test.js (the `inspectCell` cases) and test/unit/rail.test.js line 396 (the RAIL_COPY voice scan)
    - docs/SPELLS.md "Phase 41 note" (line ~630: the seen-vs-render-filter orthogonality this plan must honour)
  </read_first>
  <behavior>
    - mapViewRadius(state): `Infinity` on a lit tile with `darkFor` 0; `1` (`DARK_VIEW_RADIUS`) on a `.dark` tile; `Infinity` on a dark tile with Night Vision; `Infinity` on a dark tile carrying the Amulet of Light (`eff(c, "light") > 0`); `Infinity` on a dark tile with a live `item:Torch` effect record; `1` on a LIT tile while `c.darkFor > 0` (persistent darkness); the function mutates nothing (deepStrictEqual of `state` before/after) and reads no rng
    - inViewWindow(state, x, y): `true` for every cell when the radius is Infinity; when the radius is 1 it is `true` exactly for the 3x3 cells around `(f.px, f.py)` (Chebyshev distance <= 1) and `false` for every other cell, including previously `seen` ones; `cell.seen`/`cell.spellSeen` are unchanged after any number of calls
    - Leaving the dark: the same state with `f.px/f.py` moved to a lit tile (and `darkFor` 0) returns Infinity again — nothing to "restore" because nothing was stored
    - MAP_PALETTE.water and MAP_PALETTE.waterDark are 6-digit hex strings, distinct from each other and from every other palette hex; the palette stays frozen with every prior key unchanged
    - inspectCell on a seen, non-wall, feat-less cell with `water: true` returns `{ title: RAIL_COPY.water.title, line: RAIL_COPY.water.line, tone: "odd", hold: RAIL_HOLD.mark }`; an unseen water cell still returns the UNWALKED row; a water cell that somehow carries a feat returns the feat's legend row (feat branch stays first)
  </behavior>
  <action>
    A. `engine/derived.js` — beside `revealRadius`, add `export const DARK_VIEW_RADIUS = 1;` and `export function mapViewRadius(state)`: returns `Infinity` unless `inDark(state)`; then `Infinity` if `skill(c, "Night Vision")`, or `eff(c, "light") > 0`, or `itemEffectActive(c, "lit")`; else `DARK_VIEW_RADIUS`. JSDoc per research Pattern 3: TERR-03; the SAME waiver set `revealRadius`/`fallDark` already use so "a light effect" means one thing everywhere; pure; never touches `seen`; the shell re-evaluates it every paint (never a stored flag — Pitfall 4); Map the Floor's `spellSeen` cells are hidden by the window too — the window keeps counting, the filter only hides (CONTEXT routine decision). Add `export function inViewWindow(state, x, y)`: `const r = mapViewRadius(state); return !Number.isFinite(r) || (Math.abs(x - state.floor.px) <= r && Math.abs(y - state.floor.py) <= r);`.
       Test file `test/unit/darkness-filter.test.js` (new): every `<behavior>` bullet for the two functions, using a hand-built floor (copy movement.test.js's `wallGrid`/`open` helpers) and a minimal `c` with `skills`, `items`, `timers`, `darkFor`; the Amulet case sets `c.items = [{ n: "Amulet of Light", eff: { sight: 1, light: 1 } }]` the way item-wiring.test.js builds carried items (check `eff()`'s two-path read: bag vs `c.worn` — set it wherever `eff(c, "light")` reads on a character without `worn`).

    B. `src/browser/mapMarks.js` — add `water: "#2f5f7a"` and `waterDark: "#1f3a4a"` to `MAP_PALETTE` (comment: Phase 41 (TERR-01), a lit water cell / a water cell on a dark tile; distinct from floor/floorDark/floorSpell so the pool reads as water at a glance). The executor may adjust the hexes but must keep both distinct from every other key. Extend `test/unit/mapMarks.test.js`'s palette pins: both keys are `#rrggbb`, distinct from each other and from `floor`/`floorDark`/`floorSpell`/`fog`; the object is still frozen; every prior key's value unchanged.

    C. `src/browser/rail.js` — add `RAIL_COPY.water = { title: "WATER", line: "Two squares a step, and your boots never dry. Wade, or go around." }` (voice scan covers it). `src/browser/tapStep.js#inspectCell` — after the `cell.feat` branch and before the EMPTY CORRIDOR fallback, add `if (cell.water) return { title: RAIL_COPY.water.title, line: RAIL_COPY.water.line, tone: "odd", hold: RAIL_HOLD.mark };`. Extend `test/unit/tapStep.test.js` with the three `<behavior>` inspect cases.

    Run `node --test test/unit/darkness-filter.test.js test/unit/mapMarks.test.js test/unit/tapStep.test.js test/unit/rail.test.js test/unit/conditions.test.js test/voice/` — green. Commit `feat(41-04): mapViewRadius/inViewWindow render-filter reads, water palette, water hold-inspect copy`.
  </action>
  <acceptance_criteria>
    - `grep -c "export const DARK_VIEW_RADIUS = 1" engine/derived.js` is 1; `grep -c "export function mapViewRadius" engine/derived.js` is 1; `grep -c "export function inViewWindow" engine/derived.js` is 1
    - `node -e "import('./engine/derived.js').then(m=>{const g=[];for(let y=0;y<21;y++){g.push([]);for(let x=0;x<21;x++)g[y].push({wall:false,seen:true,feat:null});}g[5][5].dark=true;const s={floor:{g,px:5,py:5,depth:1},c:{skills:{},items:[],darkFor:0}};const a=m.mapViewRadius(s);const b=m.inViewWindow(s,6,6);const c=m.inViewWindow(s,8,5);s.floor.px=7;s.floor.py=7;const d=m.mapViewRadius(s);process.exit(a===1&&b===true&&c===false&&d===Infinity?0:1)})"` exits 0
    - `grep -c "water: \"#" src/browser/mapMarks.js` is 1 and `grep -c "waterDark: \"#" src/browser/mapMarks.js` is 1
    - `grep -c "water: { title: \"WATER\"" src/browser/rail.js` is 1; `grep -c "cell.water" src/browser/tapStep.js` is 1
    - `node --test test/unit/darkness-filter.test.js` exits 0 with at least 9 tests
    - `node --test test/unit/mapMarks.test.js test/unit/tapStep.test.js test/unit/rail.test.js test/voice/` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/darkness-filter.test.js test/unit/mapMarks.test.js test/unit/tapStep.test.js test/unit/rail.test.js test/voice/</automated>
  </verify>
  <done>The render-filter rule is a pure, tested engine read with the established waiver set; the palette carries two distinct water shades; a hold on water explains the cost; nothing in the engine's `seen` memory is touched.</done>
</task>

<task type="auto">
  <name>Task 2: mazeworld.html — 3x3 render filter in draw(), water paint, bridge, Rattled chip copy, source-pin tests, build</name>
  <files>mazeworld.html, test/unit/shell-terrain-41.test.js</files>
  <read_first>
    - mazeworld.html lines 2661-2740 (`draw()`: the fallback `P` literal, the `if (!c.seen) continue;` floor loop, the fill line, the icon pass `if (!c.seen || !c.feat) continue;`, the party marker)
    - mazeworld.html lines 2892-2940 (`CONDITION_COPY` — the flat `foresight: { label: "Forewarned" }` precedent), 3009 (`CONDITION_TONE`), 3010-3035 (`CONDITION_EXPLAIN`), and the `paintConditions` label/detail rules referenced in the CONDITION_COPY comments
    - mazeworld.html lines 7029 (`import { conditionsOf, ... } from "./engine/derived.js"`) and 7064-7072 (`window.__mzConditionsOf = conditionsOf;` — the bridge pattern and its comment)
    - test/unit/shell-spells-40.test.js (the fs.readFileSync source-pin pattern, the `draw()` fill-line regex at line 125 that must stay green, the `www/index.html` build-artefact test at line 161)
    - tools/build-www.mjs (what `npm run build:www` copies)
  </read_first>
  <action>
    A. Bridge: extend the `./engine/derived.js` import on line ~7029 with `mapViewRadius, inViewWindow`; beside `window.__mzConditionsOf`, add `window.__mzMapView = { mapViewRadius, inViewWindow };` with a comment: Phase 41 (TERR-03) — the PURE render-window read `draw()` consults every paint (no stored flag); a missing bridge means "show everything" so the map never goes blank.

    B. `draw()`:
       1. Fallback `P` literal: add `water: "#2f5f7a", waterDark: "#1f3a4a"` (the SAME hexes as `MAP_PALETTE`; the source-pin test checks equality, exactly like the floorSpell pin).
       2. Before the floor loop: `const view = window.__mzMapView; const visible = (x, y) => !view || view.inViewWindow(S, x, y);` with a comment: TERR-03 — a dark square without Night Vision/light/torch shows only the 3x3 around the party; explored cells outside it are fogged at RENDER time only (`seen` untouched) and return the moment the player steps off the dark square, because this is re-read fresh on every `draw()`.
       3. In the floor loop, directly after `if (!c.seen) continue;`, add `if (!visible(x, y)) continue;`. In the icon pass, directly after `if (!c.seen || !c.feat) continue;`, add `if (!visible(x, y)) continue;`.
       4. Water paint: keep the existing fill line byte-identical (Phase 40 pin) and add, on the very next line, `if (c.water && !c.spellSeen) ctx.fillStyle = c.dark ? P.waterDark : P.water;` with a comment: Phase 41 (TERR-01) — a lit pool paints P.water, a pool inside a dark blob P.waterDark; the Phase 40 spellSeen tint keeps priority (it IS the "this will re-fog" signal).
       The party marker, glow and `positionCanvas()` are untouched (the party is always inside its own window).

    C. Chip copy for the Plan 03 `fearArmed` chip: `CONDITION_COPY.fearArmed = { label: "Rattled" }` (flat, like `foresight`; comment: Phase 41 (TERR-05) — an armed terrain phobia waiting for the next fight; the engine's conditionsOf emits `{ key: "fearArmed", phobia, trigger }`, no count), `CONDITION_TONE.fearArmed = "warn"`, `CONDITION_EXPLAIN.fearArmed = "Something out there got to you. The next fight opens Afraid — harder to hit, softer blows — until it passes."`. If `paintConditions` needs a special case to show the label without a detail (check the `foresight` handling), mirror exactly what `foresight` does.

    D. `test/unit/shell-terrain-41.test.js` (new, fs.readFileSync source pins in the shell-spells-40 style): (1) the derived.js import line carries `mapViewRadius` and `inViewWindow`; (2) `window.__mzMapView = { mapViewRadius, inViewWindow };` present once; (3) `draw()` contains `if (!visible(x, y)) continue;` exactly twice and the `const visible = ` definition once, and the definition precedes both uses; (4) the Phase 40 fill-line regex from shell-spells-40 still matches, and the water override line `if (c.water && !c.spellSeen) ctx.fillStyle = c.dark ? P.waterDark : P.water;` follows it; (5) the fallback `P` literal's `water`/`waterDark` hexes equal `MAP_PALETTE.water`/`MAP_PALETTE.waterDark` (import the real module); (6) `CONDITION_COPY`/`CONDITION_TONE`/`CONDITION_EXPLAIN` each carry a `fearArmed` entry, the label is `Rattled`, the tone is `warn`, and the explanation passes the BANNED/ALLOWLIST voice scan (import `content/safety-wordlist.js` as shell-spells-40 does); (7) the classic legacy `function move(` / `function reveal()` bodies in mazeworld.html contain neither `mapViewRadius` nor `moveCost` (no edits to dead code: assert via a regex on those function bodies); (8) build artefact: if `www/index.html` exists it contains `waterDark`, `__mzMapView` and `Rattled` (skipped when `www/` is absent, mirroring shell-spells-40 line 161).

    Run `npm run build:www` (exit 0), then `node --test test/unit/shell-terrain-41.test.js test/unit/shell-spells-40.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-hud.test.js test/unit/shell-map-viewport.test.js test/voice/` — green. Commit `feat(41-04): mazeworld.html — 3x3 darkness render filter in draw(), water paint, Rattled chip copy` (explicit paths: mazeworld.html test/unit/shell-terrain-41.test.js).
  </action>
  <acceptance_criteria>
    - `grep -c "window.__mzMapView = { mapViewRadius, inViewWindow };" mazeworld.html` is 1
    - `grep -c "if (!visible(x, y)) continue;" mazeworld.html` is 2; `grep -c "const visible = (x, y) => !view || view.inViewWindow(S, x, y);" mazeworld.html` is 1
    - `grep -c "ctx.fillStyle = c.spellSeen ? P.floorSpell : (c.dark ? P.floorDark : P.floor);" mazeworld.html` is 1 (the Phase 40 line is intact) and `grep -c "if (c.water && !c.spellSeen) ctx.fillStyle = c.dark ? P.waterDark : P.water;" mazeworld.html` is 1
    - `grep -c 'fearArmed: { label: "Rattled" }' mazeworld.html` is 1; `grep -c 'fearArmed: "warn"' mazeworld.html` is 1; `grep -c "fearArmed:" mazeworld.html` is at least 3
    - `grep -c 'getElementById("m-steps").textContent = S.steps' mazeworld.html` is 1 (HUD counter unchanged)
    - `node --test test/unit/shell-terrain-41.test.js` exits 0 with at least 8 tests; `node --test test/unit/shell-spells-40.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-hud.test.js test/unit/shell-map-viewport.test.js` exits 0
    - `npm run build:www` exits 0 and `grep -c "__mzMapView" www/index.html` is at least 1
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www && node --test test/unit/shell-terrain-41.test.js test/unit/shell-spells-40.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-hud.test.js test/unit/shell-map-viewport.test.js test/voice/</automated>
  </verify>
  <done>`draw()` hides everything outside the 3x3 window in the dark and shows it all again on leaving, paints water in two shades without disturbing the Phase 40 pin, the Rattled chip has copy, and every edit is source-pinned; the www bundle builds.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — TERRAIN ledger UI + requirements map, REQUIREMENTS.md complete, whole-phase gate, aggregated Pixel 7 checklist + For PROJECT.md</name>
  <files>docs/TERRAIN.md, .planning/REQUIREMENTS.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - .planning/phases/41-terrain-darkness-phobias/41-01-SUMMARY.md, 41-02-SUMMARY.md, 41-03-SUMMARY.md (each `## Human verification (deferred to end of run)` list and each `## Key Decisions for PROJECT.md` section — the inputs to aggregate)
    - .planning/phases/40-spell-rework/40-05-SUMMARY.md lines 174-250 (the Success Criteria Map, `## Key Decisions for PROJECT.md`, the aggregated checklist format: grouped by plan, numbered continuously, caveats resolved)
    - docs/TERRAIN.md (fill the two `(appended by Plan 04)` placeholders; nothing else rewritten)
    - .planning/REQUIREMENTS.md lines 37-44 (TERR-01..05 checkboxes) and lines 123-127 (the traceability rows `| TERR-0N | Phase 41 | Pending |`)
    - .planning/ROADMAP.md "### Phase 41" success criteria 1-4 (read SC-1's run-flag wording as superseded by CONTEXT: "no main-rng draw moves; the fixtures that step onto water are declared")
  </read_first>
  <action>
    A. `docs/TERRAIN.md` — replace `(appended by Plan 04)` under `## Darkness filter, map paint and UI (Plan 04)` with: the `mapViewRadius`/`inViewWindow` rule and its waiver set; why it is a render filter and not a `seen` mutation (the Phase 40 orthogonality note honoured; Map the Floor's window keeps counting under the filter); the water palette (both hexes and when each paints; spellSeen priority); the hold-inspect line; the Rattled chip row; the "what stays for the cleanup milestone" note (the shell's legacy classic `move()`/`reveal()` still exist and are untouched). Replace `(appended by Plan 04)` under `## Requirements map (Plan 04)` with a table `| Requirement | Landed in | Proof |` for TERR-01..05 naming the plan(s) and the test files (`test/unit/terrain.test.js`, `floor-gen-rng-pin.test.js`, `water-cost.test.js`, `phobia-triggers.test.js`, `darkness-filter.test.js`, `shell-terrain-41.test.js`, the parity suite), plus a closing `## Out of scope / next` list (Phase 42 owns bot tactics around water/fear and the AFTER matrix; swimming/drowning, lava/ice are backlog — from CONTEXT's deferred ideas).

    B. `.planning/REQUIREMENTS.md` — flip `- [ ] **TERR-01**` … `**TERR-05**` to `- [x]` (text untouched) and the five traceability rows from `Pending` to `Complete` (mirror the SPELL rows).

    C. `test/parity/FIXTURE-INVENTORY.md` — append `### Plan 04 — shell close: zero engine-state changes` to the Phase 41 section: `mapViewRadius`/`inViewWindow` are pure reads consumed only by `draw()`; no fixture, comparable or floor byte changed in this plan; final phase readings (fill after the gate below).

    D. WHOLE-PHASE GATE (verification agents are off — this IS the verification): `npm test` (`# fail 0`; record `# tests`/`# pass` — the count must be at least 2813 plus this phase's new tests), `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0, `git status --porcelain test/parity/fixtures` empty (any Plan 02 declaration is already committed), `node tools/terrain-fixture-scan.mjs` final `WATER HITS` / `TERRAIN TRIGGER EXPOSURE` lines consistent with FIXTURE-INVENTORY.md, `grep -c "(appended by Plan" docs/TERRAIN.md` = 0. Put all readings in the SUMMARY and in the FIXTURE-INVENTORY Plan 04 subsection.

    E. Commit `docs(41-04): TERRAIN ledger close (UI + requirements map), REQUIREMENTS.md TERR-01..05 complete, whole-phase gate` (explicit paths: docs/TERRAIN.md .planning/REQUIREMENTS.md test/parity/FIXTURE-INVENTORY.md).

    F. Write `41-04-SUMMARY.md` in the 40-05 format with, in this order after the standard sections: `## Success Criteria Map (ROADMAP SC-1..4, as amended by CONTEXT)` (SC-1: water pools exist; no main-rng draw moves — the pin; the declared fixture(s) or "measured zero"; SC-2: the cost; SC-3: the 3x3 filter; SC-4: the five triggers named in the rail, once per fresh entry, Afraid on the next fight); `## For PROJECT.md` with the TWO Key Decisions verbatim as Plans 02/03 handed them (1. Water cost — "one tap, two squares of time": one dispatch, `state.steps += 2`, every per-square system once with n = 2, flying/ether exempt, ready cloak not spent; 2. Terrain phobias arm Afraid for the next fight: region model, once per fresh entry, trigger named in the rail line, `c.fearArmed` consumed at `fight()`, existing roll penalties/hp loss retained on their own rolls — never a lost action; plus the CONTEXT clarification that all five terrain phobias arm) — this plan does NOT edit PROJECT.md (orchestrator's job at phase close); `## Human verification (deferred to end of run)` — the AGGREGATED phase checklist: take every item from 41-01/41-02/41-03 SUMMARYs plus this plan's items (water pools visible in blue on a fresh floor and darker inside a dark blob; a hold on a water cell shows the WATER card; standing on a dark square without Night Vision/torch/amulet shows only the 3x3 around the party and everything returns on stepping off; with a lit torch or Night Vision the full explored map stays; the Rattled chip appears after a trigger, its tap explanation reads in voice, and it vanishes when the next fight opens Afraid; TalkBack reads the new chip and the WATER card; a voice spot-check of every new line in real play), grouped by plan (`### Plan 01` … `### Plan 04`), numbered continuously, de-duplicated (an item that appears in two plans is listed once under the earliest plan with a "(also Plan NN)" note), and with every "lands in Plan 04" caveat from earlier SUMMARYs removed because Plan 04 has now landed; `## Next Phase Readiness` (Phase 42: the AFTER matrix must be re-measured against `docs/class-pass/v15-before*.json` — water time is expected to shift hunger/depth; bot tactics around water/fear; the tuning bot already crosses water). Self-check: every file named in this plan exists with the expected content.
  </action>
  <acceptance_criteria>
    - `grep -c "(appended by Plan" docs/TERRAIN.md` is 0; `grep -c "^## Requirements map (Plan 04)" docs/TERRAIN.md` is 1; `grep -c "TERR-0[1-5]" docs/TERRAIN.md` is at least 5
    - `grep -cE "^- \[x\] \*\*TERR-0[1-5]\*\*" .planning/REQUIREMENTS.md` is 5; `grep -cE "^\| TERR-0[1-5] \| Phase 41 \| Complete \|" .planning/REQUIREMENTS.md` is 5; `grep -cE "^\| TERR-0[1-5] \| Phase 41 \| Pending \|" .planning/REQUIREMENTS.md` is 0
    - `grep -c "### Plan 04" test/parity/FIXTURE-INVENTORY.md` is 1
    - `npm test 2>&1 | grep -E "^# fail [0-9]+"` prints `# fail 0`; `npm test 2>&1 | grep -E "^# pass [0-9]+"` reports at least 2813
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `npm run build:www` exits 0; `git status --porcelain test/parity/fixtures` prints nothing
    - `test -f .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md`; `grep -c "## For PROJECT.md" .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md` is 1; `grep -c "## Human verification (deferred to end of run)" .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md` is 1; `grep -cE "^### Plan 0[1-4]" .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md` is 4; `grep -c "## Success Criteria Map" .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md` is 1
    - `git status --porcelain | grep -E "^(A|M) " | grep -c "store-listing\|tools/store-screenshots"` is 0 (never staged)
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail) [0-9]+" && git hash-object test/parity/prototype-master.js.txt && npm run build:www && node tools/terrain-fixture-scan.mjs | grep -E "^(WATER HITS|TERRAIN TRIGGER EXPOSURE)"</automated>
    <human-check>Deferred to the milestone-close Pixel 7 batch: the aggregated phase checklist in 41-04-SUMMARY.md (water paint, 3x3 darkness window and its restore, the Rattled chip, every phobia line, the Squares +2 counter).</human-check>
  </verify>
  <done>docs/TERRAIN.md is closed with no placeholders, REQUIREMENTS.md marks TERR-01..05 complete, the whole-phase gate is green and recorded, and the SUMMARY carries the Success Criteria Map, `## For PROJECT.md` with both Key Decisions, and the de-duplicated, continuously numbered Pixel 7 checklist for all four plans.</done>
</task>

</tasks>

<verification>
- `node --test test/unit/darkness-filter.test.js` — the window rule is Infinity/1 with the established waiver set, pure, and restores on leaving with nothing stored.
- `node --test test/unit/shell-terrain-41.test.js test/unit/shell-spells-40.test.js` — the draw() edits are pinned and the Phase 40 fill-line pin is intact.
- `npm run build:www` — exit 0; `www/index.html` carries `__mzMapView`.
- `npm test` `# fail 0` (at least 2813 passing); master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` empty.
- docs/TERRAIN.md has zero `(appended by Plan` placeholders; REQUIREMENTS.md TERR-01..05 are `[x]` / `Complete`.
</verification>

<success_criteria>
- ROADMAP SC-1 (as amended): a blue multi-square pool is visible on the map; no main-rng draw moved; declared fixtures only.
- ROADMAP SC-3: on a dark square without Night Vision or a light effect the map shows only the 3x3 around the party; leaving restores the explored view.
- The phase is closed: ledger complete, requirements complete, gate green, checklist and Key Decisions handed to the orchestrator.
</success_criteria>

<output>
Create `.planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md` when done (40-05 format) — MUST include `## Success Criteria Map`, `## For PROJECT.md` (two Key Decisions), and the aggregated `## Human verification (deferred to end of run)` grouped `### Plan 01` … `### Plan 04`, numbered continuously, de-duplicated.
</output>