---
phase: 37-equipment-slot-model-eff-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/treasure-tables.js
  - engine/derived.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/unit/worn-model.test.js
autonomous: true
requirements: [GEAR-03]
tags: [engine, worn-slots, eff-refactor, content-taxonomy, parity-carve-out, legacy-equivalence, deferred-uat]
notes:
  assumption_delta: "detector fired on pluralization ('a second ring/cloak/staff/bracelet'). Noun = 'worn slot' (c.worn[slot] — the ONE item the character benefits from per slot). Decision = PROMOTE: c.worn becomes the primary representation of what confers effects; the old 'sum of every carried copy' is demoted to the legacy variant kept only for states that predate the model (fixtures, bots, un-migrated saves). Rationale: adding-alongside (a worn flag on bag items) would keep the bag as the primary and leave stacking latent. Companion invariant (Task 3): for every state with c.worn, no populated slot's item is also present in c.items, and eff(c, key) never counts an item outside c.worn."
  surfaced_deviation_slot_storage: "CONTEXT locks 'a new slot field on content rows'. Verified by direct read that spreading a slot field onto rolled item objects would break the frozen gate three ways: chargen seeds 2/3/4 (Thief cloaks, test/parity/chargen-parity.test.js deepStrictEqual on c.items), the combat flee fixture (seed 17 Cloak of Armor in the bag), and test/parity/fixtures/action-script.economy.json's DECLARED after.items (a Cloak of Ether with no slot key) — the last can only be fixed by editing a frozen fixture, which the run forbids. Resolution (Phase 15 ECON-08 precedent, engine/economy.js:55-60): the slot IS authored on every content row in content/treasure-tables.js, but the exported JEWELRY/CLOAKS/STAVES item tables are derived WITHOUT the field and a sibling name-keyed SLOT_OF map (derived from the same rows) is the runtime lookup — every rolled item object stays byte-identical, and slotFor(it) still honours it.slot first for forward compatibility. Serialized-field impact therefore stays c.worn only."
  rng_draw_impact: "zero — every change is a plain read or assignment; no rng parameter is added anywhere; newRun(seed) without an option is byte-identical (proved by the chargen fixture seeds + the pinned legacy table)"
  serialized_field_impact: "c.worn only — a lazily-created slot-key -> item map that NOTHING in this plan creates (no fixture/bot/newRun path writes it; tests plant it by hand); carved out of movementComparable/combatComparable/economyComparable plus the three local comparable() duplicates via stripWornField. No slot key ever lands on an item object (tripwire test)."
  canon_change: "GEAR-03 stacking -> one per slot is the ONE deliberate canon change of the phase (declared in Plan 04's docs/GEAR-SLOTS.md ledger); it affects zero fixtures because the legacy sum-all-carried path is preserved byte-for-byte for every state without c.worn."

must_haves:
  truths:
    - "content/treasure-tables.js exports SLOT_OF with exactly 24 entries mapping every JEWELRY/CLOAKS/STAVES display name to one of ring|bracelet|amulet|helm|cloak|staff per the locked taxonomy (Ring of Power->ring; Bracelet of Flight, Anklet of Invisibility->bracelet; Amulet of Light, Amulet of Stone, Pendant of Fortitude->amulet; Helm of Knowledge, Gauntlet of the Giant->helm; all 8 cloaks->cloak; all 8 staves->staff), and no exported JEWELRY/CLOAKS/STAVES row object carries a slot key (test/unit/worn-model.test.js)"
    - "engine/derived.js exports WORN_SLOTS (frozen array of the six keys in order ring, bracelet, amulet, helm, cloak, staff), slotFor(it), carriedItems(c) and reconcileWorn(c); eff(c, key) sums it.eff[key] over populated c.worn entries only when c has a worn key, and over c.items exactly as before when it does not (test/unit/worn-model.test.js)"
    - "For every chargen fixture seed [1,2,3,4,6,7,8,13,15,19,24,29,32,35], newRun(seed).c has no worn key and eff(c, key) equals the pinned pre-refactor value for every key in dmg,size,sight,light,foeToHit,tongue,fly,cloakHeal,noCrit,cloakRegen,cloakArmor (pinned table measured against the pre-refactor engine, not hand-computed)"
    - "Companion invariant: for a hand-planted state with c.worn, no populated slot's item is also in c.items and eff never counts a bag copy — three Rings of Power in the bag plus one worn ring yields eff(c,'dmg') === 1; the same three rings in a legacy state (no worn key) yields 3"
    - "hasItemNamed/isFlying/conditionsOf see a worn Bracelet of Flight or Cloak of Flying (moved out of c.items into c.worn) exactly as they saw it in the bag (test/unit/worn-model.test.js)"
    - "npm test prints '# fail 0'; git status --porcelain test/parity/fixtures is empty; git hash-object test/parity/prototype-master.js.txt prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
    - "grep -c stripWornField test/parity/harness/comparables.js >= 4 and grep -c 'worn, ...cRest' prints 1 in each of test/parity/combat-parity.test.js, magic-parity.test.js, movement-parity.test.js"
  artifacts:
    - path: "content/treasure-tables.js"
      provides: "the slot taxonomy authored on every JEWELRY/CLOAKS/STAVES row + the derived, byte-identical item tables + SLOT_OF"
    - path: "engine/derived.js"
      provides: "WORN_SLOTS, slotFor, carriedItems, reconcileWorn, the two-path eff(), hasItemNamed routed through carriedItems"
    - path: "test/parity/harness/comparables.js"
      provides: "stripWornField(c) wired into all three *Comparable() functions"
    - path: "test/unit/worn-model.test.js"
      provides: "taxonomy pins, legacy-equivalence pinned table, new-model sums, the companion invariant, the no-slot-on-items tripwire, the no-eff.wp-on-slot-rows tripwire, reconcileWorn unit tests"
  key_links:
    - from: "engine/derived.js#eff"
      to: "c.worn"
      via: "if ('worn' in c) sum over Object.values(c.worn) else legacy loop over c.items"
      pattern: "\"worn\" in c"
    - from: "engine/derived.js#hasItemNamed"
      to: "engine/derived.js#carriedItems"
      via: "carriedItems(c).some((it) => it && it.n === name)"
      pattern: "carriedItems\\(c\\)\\.some"
    - from: "engine/derived.js#slotFor"
      to: "content/treasure-tables.js#SLOT_OF"
      via: "it.slot ?? SLOT_OF[it.n] ?? kind fallback (cloak->cloak, staff->staff) ?? null"
      pattern: "SLOT_OF\\["
  prohibitions:
    - "No edit to engine/items.js, engine/state.js, engine/saveState.js, engine/character.js, mazeworld.html or any src/browser file — those land in Plans 02-04"
    - "No slot key on any exported item row or rolled item object; no change to the rollJewel/rollCloak/rollStaff/character.js:382/encounters.js:398-402 construction sites"
    - "No edit to test/parity/prototype-master.js.txt or any file under test/parity/fixtures; no fixture regeneration; no package/font change"
    - "No checkpoint:human-verify, no device step, no APK build (deferred-UAT protocol)"
    - "Commit with explicit paths only — never stage store-listing/ or tools/store-screenshots/"
---

<objective>
Land the worn-slot data model and the wide-blast-radius `eff()` refactor with ZERO behaviour change for every existing state: (1) the slot taxonomy in `content/treasure-tables.js` (authored on every row, exported as the byte-identical item tables plus a name-keyed `SLOT_OF` map); (2) `engine/derived.js` gains `WORN_SLOTS`, `slotFor(it)`, `carriedItems(c)`, `reconcileWorn(c)`, and `eff(c, key)` becomes two-path — worn-only when `c.worn` is present, today's sum over `c.items` byte-for-byte when it is absent — with `hasItemNamed` (and therefore `isFlying`, `conditionsOf`, movement's climb block) routed through the bag ∪ worn lookup; (3) `stripWornField` carved into all three `*Comparable()` functions and the three local duplicates; (4) a legacy-equivalence test with a pinned, measured table plus the companion invariant.

Per CONTEXT §"Worn Model & eff() Semantics" (locked): storage `c.worn = { ring?, bracelet?, amulet?, helm?, cloak?, staff? }`, lazily created, absent on every fixture and bot run; a worn item is NOT a bag member; `eff` rule as above; bag ∪ worn helper for every name/kind scan; the slot taxonomy (24 rows -> 6 slots); `slotFor(it)` derives from `it.slot` first, falling back on the name map and then `it.kind` for cloak/staff. Per CONTEXT §"Old-Save Reconciliation & Parity": `stripWornField` in all three comparables + the three per-domain duplicates as a structural tripwire; a test proves `newRun(seed)` without the option has no `worn` key and `eff` equals the legacy sum for every key on every chargen fixture seed. `reconcileWorn(c)` (the migration/initialiser both Plan 03's `newRun` option and load path call) is homed HERE in the cycle-free leaf beside `clampCarry` (the existing mutating-migration precedent in this file) so `state.js` and `saveState.js` can both import it without a cycle.

Surfaced deviation (see frontmatter `surfaced_deviation_slot_storage`): the slot is authored on the rows but never spread onto item objects, because a `slot` key on rolled items collides with the frozen economy fixture's declared `after.items` and three chargen/combat fixture bags — the ECON-08 name-keyed-sibling-table precedent keeps item shapes byte-identical while the taxonomy still lives with the rows.

Purpose: the whole milestone's gear, magic-item and Gear-tab-split work depends on one-per-slot semantics; landing the model with an airtight legacy path first means Plans 02-04 add behaviour behind `c.worn` without touching parity.
Output: the taxonomy + `SLOT_OF`, the derived.js helpers + two-path `eff`, the carve-out, `test/unit/worn-model.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@content/treasure-tables.js
@engine/derived.js
@engine/economy.js
@test/parity/harness/comparables.js
@test/parity/chargen-parity.test.js
</context>

<artifacts_this_phase_produces>
Phase 37 as a whole creates the following symbols/files (this plan creates the items marked **[01]**):

- **[01]** `content/treasure-tables.js`: `slot` authored on every JEWELRY/CLOAKS/STAVES row (internal `*_ROWS` arrays), exported `JEWELRY`/`CLOAKS`/`STAVES` derived WITHOUT the field (byte-identical item rows), new `export const SLOT_OF` (display name -> slot, 24 entries). `engine/derived.js`: `export const WORN_SLOTS`, `export function slotFor(it)`, `export function carriedItems(c)`, `export function reconcileWorn(c)` (returns the reconciliation report `[{ slot, worn, bagged }]` or `null`), two-path `eff(c, key)`, `hasItemNamed` via `carriedItems`. `test/parity/harness/comparables.js`: `stripWornField(c)` in all three comparables; `worn` destructured out of the three local `comparable()` duplicates. `test/unit/worn-model.test.js`.
- [02] `engine/items.js`: slot branches in `equipItem` (direct swap, `itemEquipped { item, slot, replaced? }`, staff `wrongClass` gate) and `unequipSlot` (six new slots via the `stowItem` gate); `autoWearSlot(state, it)` + `wearItem(state, it, slot, events)` helpers; auto-wear in `takeFind`/`takeLoot`/`takeAllLoot`/`takeItem`; `useItem(state, ref, ...)` accepting `{ slot }` alongside the bag index; new refusal reason `useRefused { reason: "notWorn" }`; `engine/actions.js`: `useItem.slot` / `unequipSlot.slot` validation over `WORN_SLOTS`; `engine/engine.js`: the slot dispatch form; `src/browser/toasts.js` + `eventNarration.js`: `notWorn` copy and the `itemEquipped.replaced` branch; `test/voice/safety-scan.test.js` BRANCH_TOGGLES; `docs/USABLE-FEATURES-AUDIT.md` §1 `notWorn` row; `test/unit/worn-slots.test.js`.
- [03] `engine/state.js`: `newRun(seed, exclude, { wornSlots })` option (shell-only, `storeRoll` precedent) calling `reconcileWorn(c)`; `engine/saveState.js`: `sanitizeWorn(c)` (present-but-tampered tolerance, never injects) in both load chains + option-gated `reconcileWorn` in `validateSave(raw, { wornSlots })` / `rehydrate(obj, { wornSlots })`, `validateSave` returning `wornReport`; `src/browser/engineAdapter.js`: `boot()`/`startNewRun()` pass `wornSlots: true`, `export function takeBootWornReport()`; `src/browser/rail.js`: `RAIL_COPY.wornReconciled` + `export function wornReconcileCard(report)`; `src/browser/combatMenu.js`: worn activatable rows (`id: "worn-{slot}"`, `dispatch: { type: "useItem", slot }`); `test/unit/worn-migration.test.js` + additions to engineAdapter/combatMenu/rail tests.
- [04] `mazeworld.html`: classic `eff(key)` routed through `window.__mzEff`; bridges `__mzEff`/`__mzSlotFor`/`__mzWornSlots`; `wornSlotRow(slot, it)` rows in the Gear tab with Use/Unequip; EQUIP on bag slot items with the `SWAP_CONFIRM_MS`/`swapConfirmRevert`/`revertSwapConfirm()` two-tap swap confirm (`.mw-swap-confirm` CSS); `window.mzUseItem(ref)` slot form; `COMBAT_DISPATCH.useItem` slot form; `surfaceWornReconcile()` on the resume path; `test/unit/shell-worn-slots.test.js`; re-pin in `test/unit/shell-combat-actions.test.js`; `docs/GEAR-SLOTS.md` ledger; phase gate + aggregated Pixel 7 checklist.
</artifacts_this_phase_produces>

<eff_call_site_inventory>
Every executable `eff(` read in the repo (measured by grep on 2026-09-17; comments excluded). Plan 01's refactor changes NONE of these call sites — each keeps its exact expression and, for every state without `c.worn`, its exact value:

- engine/derived.js:284 `eff(c, "cloakArmor")` (effectiveArmor / Cloak of Armor soak); :342 `eff(state.c, "sight")` (revealRadius); :359 `eff(c, "toHit")` (toHit); :438 `eff(c, "foeToHit")` (foeToHitVs); :485 `eff(c, "foeToHit")` (need-mods breakdown); :535 `eff(c, "dmg")` (weaponDamage); :544 `2 * eff(c, "size")` (Gauntlet); :556 `eff(c, "upkeep")` (upkeep); :642 `eff(c, "tongue")` (fluency / Helm of Knowledge)
- engine/combat.js:553 `eff(c, "noCrit")` (Cloak of Strength); :1391 `eff(view, "throw")`; :1396 `eff(view, "spellDmg")` (party-member casts — member sheets never carry `worn`, so they stay on the legacy path)
- engine/items.js:89 `eff(c, "greed")` (gainWilmst)
- engine/magic.js:376 `eff(c, "throw")`; :400 `eff(c, "spellDmg")`
- engine/movement.js:105 `eff(c, "charges")` (maxCharges); :286 `eff(c, "light")` (Amulet of Light dispels darkness); :319 `eff(c, "cloakHeal")`; :324 `eff(c, "cloakRegen")`
- src/browser/viewModels.js:47 `eff(c, "dmg")` (damageBracket — imports the engine's eff)
- mazeworld.html (classic-script duplicate `function eff(key)` at :3539 reading `S.c.items`; routed in Plan 04): callers :2168 (charges), :2620 (sight), :2815 (toHit), :2834 (foeToHit), :2848 (dmg), :2853 (upkeep), :3127 (dmg), :3556 (greed), :4519 (throw), :4531 (spellDmg), :4586 (tongue)
- engine/maze.js:195-196 — comment only, no call

Name/kind scans that can involve a slot item (routed through `carriedItems` here): engine/derived.js:128 `hasItemNamed` (callers :158/:159 `isFlying`, :220/:222 `conditionsOf`, engine/movement.js:154 climb block). NOT routed (never slot items): `hasPicks` (engine/items.js:102, lockpicks only), `slotItems` (:66 — worn items must stay OUT of the bag count by design).
</eff_call_site_inventory>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slot taxonomy in content/treasure-tables.js (authored on rows, exported byte-identical) + derived.js WORN_SLOTS/slotFor/carriedItems/reconcileWorn + two-path eff() + hasItemNamed via carriedItems — tests first</name>
  <files>content/treasure-tables.js, engine/derived.js, test/unit/worn-model.test.js</files>
  <read_first>
    - content/treasure-tables.js (all 59 lines — the three 8-row tables; note every row is `{ n, eff?, use?, every?, aoe?, txt }` and the safety scan imports JEWELRY/CLOAKS/STAVES by name)
    - engine/economy.js lines 50-90 (TREASURE_BASE_VALUES — the ECON-08 precedent for "keep new per-item data OUT of the row objects so rolled item shapes stay byte-identical"; SLOT_OF mirrors it, keyed by display name)
    - engine/derived.js lines 1-70 (header, imports, `skill`/`skillTier`, `eff` at 50-55, `slotItems` at 57-68), lines 100-130 (`clampCarry` — the mutating-migration precedent in this leaf; `hasItemNamed` at 120-130), lines 150-235 (`isFlying`, `conditionsOf`'s two hasItemNamed reads)
    - engine/items.js lines 100-117 (`hasPicks`, `rollJewel`/`rollCloak`/`rollStaff` — `Object.assign({ kind }, ROW)` spreads EVERY row key onto the item, which is why the exported tables must not carry `slot`), lines 380-400 (`stowItem` -> `giveItem` applies `it.eff.wp`)
    - engine/character.js line 382 (the Thief's starting cloak: `Object.assign({ kind: "cloak" }, CLOAKS[rng.d(8) - 1])` — same spread)
    - engine/encounters.js lines 396-403 (the three find offers — same spread)
    - test/parity/chargen-parity.test.js lines 72-146 (the full-character deepStrictEqual against the prototype for seeds 1,2,3,4,6,7,8,13,15,19,24,29,32,35 — seeds 2/3/4 are Thieves whose `items[0]` is a spread CLOAKS row; this is the proof a `slot` key on items would break)
    - test/parity/fixtures/action-script.economy.json lines 44-105 (the DECLARED `after.items` Cloak of Ether object with no slot key — frozen, never edited)
    - test/unit/effects.test.js lines 1-60 (the newest unit-test file's import/helper style to mirror), test/unit/party-model.test.js lines 28-47 ("adds NO rng draw / newRun byte-identical" test shape)
    - test/unit/toastsCoverage.test.js lines 86-105 (the coverage guard scans engine/*.js for `type:` object keys — reconcileWorn's report objects must use keys `slot`/`worn`/`bagged`, never `type`)
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-CONTEXT.md §"Worn Model & eff() Semantics" (every bullet is locked)
  </read_first>
  <behavior>
    - SLOT_OF has exactly 24 own keys; SLOT_OF["Ring of Power"] === "ring"; ["Bracelet of Flight"] and ["Anklet of Invisibility"] === "bracelet"; ["Amulet of Light"], ["Amulet of Stone"], ["Pendant of Fortitude"] === "amulet"; ["Helm of Knowledge"], ["Gauntlet of the Giant"] === "helm"; every CLOAKS[i].n -> "cloak"; every STAVES[i].n -> "staff"; SLOT_OF is frozen
    - JEWELRY/CLOAKS/STAVES: each has 8 rows, no row has an own "slot" key, and the row objects deepStrictEqual the pre-change literals (pin the 24 `{ n, eff, use, every, aoe, txt }` shapes by asserting Object.keys(row) contains no "slot" and that every row's n/eff/use/every/txt round-trip unchanged against a hand-pinned name list in table order)
    - rollJewel/rollCloak/rollStaff(makeRng(7)) items and newRun(2).c.items[0] (Thief cloak) have no "slot" key (tripwire against a future spread)
    - no JEWELRY/CLOAKS row has eff.wp (tripwire: wear/unequip moves never need to touch wp/maxWP)
    - WORN_SLOTS deepStrictEqual ["ring","bracelet","amulet","helm","cloak","staff"] and is frozen
    - slotFor({ kind:"jewel", n:"Ring of Power" }) === "ring"; slotFor({ kind:"cloak", n:"Cloak of Speed" }) === "cloak"; slotFor({ kind:"staff", n:"Oak Staff" }) === "staff"; slotFor({ kind:"cloak", n:"Unknown Cloak" }) === "cloak" (kind fallback); slotFor({ kind:"staff", n:"Unknown Staff" }) === "staff"; slotFor({ kind:"jewel", n:"Unknown Trinket" }) === null; slotFor({ kind:"potion", n:"Healing potion" }) === null; slotFor({ kind:"weapon", n:"Axe" }) === null; slotFor({ kind:"jewel", n:"Ring of Power", slot:"amulet" }) === "amulet" (it.slot wins); slotFor(null) === null; slotFor({}) === null
    - carriedItems({ items:[a,b] }) deepStrictEqual [a,b]; carriedItems({ items:[a], worn:{ ring:r, cloak:null } }) deepStrictEqual [a, r]; carriedItems({}) deepStrictEqual []; carriedItems({ worn:{ ring:r } }) deepStrictEqual [r]; never mutates its input
    - eff legacy: for c = { items:[ring, ring, ring] } (three Rings of Power, no worn key) eff(c,"dmg") === 3; for c = { items:[] } every key is 0; for c with no items key every key is 0
    - eff new model: for c = { items:[ring, ring, ring], worn:{ ring: ring } } eff(c,"dmg") === 1; for c = { items:[ring], worn:{} } eff(c,"dmg") === 0; for c = { items:[], worn:{ ring: ring, helm: gauntlet } } eff(c,"dmg") === 1 and eff(c,"size") === 1; a worn entry that is null/undefined is skipped
    - hasItemNamed: { items:[], worn:{ bracelet: braceletOfFlight } } -> true for "Bracelet of Flight"; isFlying({ c: that }) === true; conditionsOf({ c: { ...that, haste:0 } }) contains { key:"flight", flight:"always" }; with a worn Cloak of Flying and flightLeft 5 -> flight "charged" remaining 5; a legacy state with the bracelet in items still reads true (unchanged)
    - reconcileWorn: on c = { cls:"Fighter", items:[ring1, ring2, cloakA, potion, picks, cloakB] } -> returns [{ slot:"ring", worn:"Ring of Power", bagged:["Ring of Power"] }, { slot:"cloak", worn: cloakA.n, bagged:[cloakB.n] }] in WORN_SLOTS order; afterwards c.worn deepStrictEqual { ring: ring1, cloak: cloakA } (same object identities), c.items deepStrictEqual [ring2, potion, picks, cloakB] (relative order kept); a second reconcileWorn(c) returns null and changes nothing (never re-migrated); on c = { cls:"Thief", items:[] } -> returns [] and c.worn deepStrictEqual {}; a staff on a non-Magic-User stays in the bag and produces no report entry; a staff on a Magic User is worn; slots with a single item report bagged: []; reconcileWorn(null)/reconcileWorn([]) return null without throwing; report objects have exactly the keys slot, worn, bagged
    - legacy-equivalence pinned table: for every seed in [1,2,3,4,6,7,8,13,15,19,24,29,32,35], newRun(seed).c has no own "worn" key and for every key in EFF_KEYS = ["dmg","size","sight","light","foeToHit","tongue","fly","cloakHeal","noCrit","cloakRegen","cloakArmor"], eff(newRun(seed).c, key) === LEGACY_EFF_PINS[seed][key] ?? 0, where LEGACY_EFF_PINS is a literal MEASURED before the refactor (expected: {2:{cloakRegen:1}, 4:{cloakRegen:1}}, every other seed all-zero — the executor must confirm by measurement, not copy)
    - JSON round-trip: a c with worn survives JSON.parse(JSON.stringify(c)) with eff results identical
  </behavior>
  <action>
    Write `test/unit/worn-model.test.js` FIRST (node:test + node:assert/strict, ESM imports mirroring test/unit/effects.test.js), covering every behavior above. Build the LEGACY_EFF_PINS literal by measurement: before touching engine/derived.js, run a one-off node -e script (in the scratchpad, not committed) that imports the CURRENT newRun and eff and prints, for each chargen fixture seed and each EFF_KEYS entry, the non-zero values; paste that output as the pinned literal (sparse: only non-zero keys). Run the file — it must FAIL on the missing exports (RED).

    Then `content/treasure-tables.js`: rename the three authored arrays to module-private `JEWELRY_ROWS`, `CLOAKS_ROWS`, `STAVES_ROWS`, adding `slot: "ring" | "bracelet" | "amulet" | "helm"` to each JEWELRY row per the locked taxonomy, `slot: "cloak"` to every CLOAKS row and `slot: "staff"` to every STAVES row (keep every other field byte-identical, including the Phase 28 Cloak of Armor comment). Export `JEWELRY`, `CLOAKS`, `STAVES` as the SAME rows with the `slot` key removed (map each row through a destructure that drops `slot` and keeps everything else, so the exported row objects are byte-identical to today's and every `Object.assign({ kind }, ROW)` construction site stays untouched). Export `SLOT_OF` as a frozen object built from the three `*_ROWS` arrays keyed by `n` -> `slot`. Add a header comment naming the reason (ECON-08 precedent at engine/economy.js:55-60; the frozen economy fixture's declared after.items; chargen seeds 2/3/4) and stating that `slot` must never be spread onto an item object.

    Then `engine/derived.js`: add `SLOT_OF` to the content import (it is re-exported via content/index.js's `export * from "./treasure-tables.js"`). Directly after `slotItems` (before the clampCarry doc comment) add, each with a JSDoc: `export const WORN_SLOTS = Object.freeze(["ring", "bracelet", "amulet", "helm", "cloak", "staff"])`; `export function slotFor(it)` returning `it.slot` when it is a string, else `SLOT_OF[it.n]` when defined, else `"cloak"` for `it.kind === "cloak"`, `"staff"` for `it.kind === "staff"`, else `null` (null-safe: a non-object returns null); `export function carriedItems(c)` returning a NEW array of `c.items` (array-guarded) followed by the truthy values of `c.worn` (object-guarded), never mutating. Rewrite `eff(c, key)`: when `c` is an object with an own `worn` key (`"worn" in c`), sum `it.eff[key]` over the truthy values of `c.worn` (skip entries without `eff`); otherwise keep the existing loop over `c.items || []` byte-for-byte. Update its JSDoc to state the two-path rule and that the legacy branch is the byte-identical prototype rule for every state without the model. Change `hasItemNamed` to `carriedItems(c).some((it) => it && it.n === name)` and extend its JSDoc. Directly after `clampCarry` add `export function reconcileWorn(c)`: returns `null` unless `c` is a non-null non-array object WITHOUT an own `worn` key; otherwise sets `c.worn = {}`, walks `c.items` in bag order keeping a `kept` array — for each item with a non-null `slotFor(it)` where `c.worn[slot]` is empty and (for `kind === "staff"`) `c.cls === "Magic User"`, assign `c.worn[slot] = it` (same object), else push it to `kept`; then `c.items = kept` (only when `c.items` was an array); build the report as one `{ slot, worn: it.n, bagged: [names of later same-slot items left in kept] }` per populated slot in WORN_SLOTS order and return it. Report objects must not use a `type` key (toastsCoverage's scanner). Document in its JSDoc that Plan 03's `newRun({ wornSlots: true })` and the option-gated load path are its only callers, that moving bag->worn only frees slots (never overflows the cap), and that it adds no rng draw.

    Do NOT touch any other file in this task. Run the new test file (GREEN), then `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/worn-model.test.js && npm test 2>&1 | grep -E "^# (pass|fail)" && git status --porcelain test/parity/fixtures | wc -l && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/worn-model.test.js` passes; the file contains a `LEGACY_EFF_PINS` literal and an `EFF_KEYS` array of exactly 11 strings
    - `node -e "import('./content/treasure-tables.js').then(m=>console.log(Object.keys(m.SLOT_OF).length, Object.isFrozen(m.SLOT_OF), [...m.JEWELRY,...m.CLOAKS,...m.STAVES].some(r=>'slot' in r)))"` prints `24 true false`
    - `grep -c "slot:" content/treasure-tables.js` prints 24 (one authored slot per row)
    - `node -e "import('./engine/derived.js').then(m=>console.log(typeof m.slotFor, typeof m.carriedItems, typeof m.reconcileWorn, JSON.stringify(m.WORN_SLOTS)))"` prints `function function function ["ring","bracelet","amulet","helm","cloak","staff"]`
    - `grep -c '"worn" in c' engine/derived.js` >= 2 (eff + reconcileWorn)
    - `grep -c "carriedItems(c).some" engine/derived.js` prints 1
    - `git diff --stat -- engine/items.js engine/state.js engine/saveState.js engine/character.js engine/encounters.js mazeworld.html src` is empty
    - `npm test` ends with `# fail 0`; `git status --porcelain test/parity/fixtures` is empty; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
  </acceptance_criteria>
  <done>SLOT_OF + authored slots exist with byte-identical exported item tables; derived.js exports WORN_SLOTS/slotFor/carriedItems/reconcileWorn; eff is two-path; hasItemNamed reads bag ∪ worn; worn-model.test.js green; full suite green with fixtures untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Parity carve-out — stripWornField in all three *Comparable() functions and the three local comparable() duplicates</name>
  <files>test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js</files>
  <read_first>
    - test/parity/harness/comparables.js lines 160-200 (`stripFoeEffectField`, `stripTimersField` — the Phase 36 template, its JSDoc shape and placement), lines 249-297 (movementComparable's strip chain at :292), lines 340-372 (combatComparable's chain at :370), lines 731-766 (economyComparable's chain at :763)
    - test/parity/combat-parity.test.js lines 130-152, test/parity/magic-parity.test.js lines 95-110, test/parity/movement-parity.test.js lines 78-90 (the three local `comparable()` destructures `const { name, darkFor, flightLeft, flightCooldown, bag, timers, ...cRest } = rest.c;`)
    - .planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-02-SUMMARY.md §"Decisions Made" (stripTimersField placement: innermost-but-one wrapper; verified with node --check)
  </read_first>
  <action>
    In `test/parity/harness/comparables.js` add `function stripWornField(c)` directly after `stripTimersField` with the same destructure shape (`if (!c || !("worn" in c)) return c; const { worn, ...rest } = c; return rest;`) and a JSDoc explaining: Phase 37 (GEAR-03/GEAR-04) adds `c.worn`, a brand-new engine-only lazily-created slot map with NO prototype-side equivalent; the frozen prototype-master.js.txt (DO NOT EDIT) never sets it and NO fixture ever creates it (nothing in the fixture/bot path calls reconcileWorn or passes the newRun option), so this is a structural tripwire like stripTimersField. Wire it as the innermost-but-one wrapper directly around `stripTimersField(...)` in all three chains (movementComparable :292, combatComparable :370, economyComparable :763) — i.e. `stripWornField(stripTimersField(...))` inside the existing `stripBagArmorFields(` wrapper. Verify paren balance with `node --check test/parity/harness/comparables.js`.

    In each of the three local `comparable()` duplicates extend the destructure to `const { name, darkFor, flightLeft, flightCooldown, bag, timers, worn, ...cRest } = rest.c;` and add a one-line Phase 37 comment beside the existing Phase 36 one ("strip the new engine-only lazily-created c.worn slot map too (see harness stripWornField) — mirrored here because this file keeps its own local comparable()").

    Do not touch any fixture; do not touch chargen-parity.test.js (no slot key ever reaches an item, so it needs nothing). Run the parity suite and the full suite.
  </action>
  <verify>
    <automated>node --check test/parity/harness/comparables.js && node --test test/parity/ && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "stripWornField" test/parity/harness/comparables.js` >= 4 (definition + three chain sites)
    - `grep -c "stripWornField(stripTimersField(" test/parity/harness/comparables.js` prints 3
    - `grep -c "timers, worn, ...cRest" test/parity/combat-parity.test.js` prints 1; same for test/parity/magic-parity.test.js and test/parity/movement-parity.test.js
    - `git diff --stat -- test/parity/fixtures test/parity/prototype-master.js.txt test/parity/chargen-parity.test.js` is empty
    - `npm test` ends `# fail 0`
  </acceptance_criteria>
  <done>c.worn is carved out of all six comparables; parity suite and full suite green; fixtures untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Companion invariant + rng/parity proofs appended to worn-model.test.js, then the plan gate and the deferred-UAT SUMMARY section</name>
  <files>test/unit/worn-model.test.js</files>
  <read_first>
    - test/unit/worn-model.test.js (as written in Task 1)
    - test/unit/party-model.test.js lines 28-47 (the newRun byte-identical shape) and test/unit/effects.test.js's "30-move rng-draw/event invariance" test (search for `rngState` in that file) — mirror the proof style
    - engine/rng.js (`makeRng(seed).getState()`) and engine/engine.js `applyAction` (to drive a short legal action sequence)
  </read_first>
  <behavior>
    - Invariant (assumption-delta companion): for each of three hand-built states — (a) `{ items:[ring2], worn:{ ring: ring1 } }`, (b) the reconcileWorn output for `[ring1, ring2, cloakA, cloakB, gauntlet]`, (c) a state built by planting `c.worn = {}` on newRun(2).c then calling reconcileWorn — assert: every populated `c.worn[slot]` object is NOT present (by identity AND by deep equality of n+kind) in `c.items`; and eff(c, key) for every EFF_KEYS entry equals the sum computed by hand over `Object.values(c.worn)` only (never counting a bag copy)
    - Chargen shape: for every chargen fixture seed, `Object.keys(newRun(seed).c)` does not include "worn" and `JSON.stringify(newRun(seed))` is identical before/after importing derived.js's new exports (i.e. run newRun twice in the same process and compare — proves the module-load-time SLOT_OF derivation adds no draw and no key)
    - rng invariance: for seed 3 (a Thief with a starting cloak in the bag), 20 legal `move` dispatches through applyAction starting from newRun(3) end with the same `rngState` and the same event type sequence as a pinned run captured in the same test from a fresh newRun(3) — and `state.c` still has no "worn" key after those 20 actions (no code path in this plan creates the model)
  </behavior>
  <action>
    Append the three behaviors above as tests to `test/unit/worn-model.test.js` (a `// --- Plan 01 Task 3: invariant + parity proofs ---` section). For the invariant test write a small local `wornOnlySum(c, key)` helper that sums over `Object.values(c.worn || {})` and assert `eff(c, key) === wornOnlySum(c, key)` for every EFF_KEYS entry on states (a)-(c), plus `c.items.every((it) => !Object.values(c.worn).includes(it))`. For the rng-invariance test drive `applyAction` with `{ type: "move", dir }` over a fixed direction list (N,E,S,W repeating; a blocked move is still a legal dispatch) and compare `rngState` + `events.map((e) => e.type)` between two independent fresh runs of the same sequence — the point is the sequence is deterministic and never touches `worn`.

    Then run the plan gate and record it in the SUMMARY: `npm test` (`# fail 0`, total >= 2276 + the new tests), `git status --porcelain test/parity/fixtures` empty, master hash unchanged, `git diff --stat -- package.json package-lock.json` empty, `git diff --stat 6671921 -- engine content src mazeworld.html` lists ONLY content/treasure-tables.js and engine/derived.js. Write the SUMMARY's `## Human verification (deferred to end of run)` section: state explicitly that this plan has zero player-visible behaviour (no state gains `c.worn` yet) and adds NOTHING to the Pixel 7 checklist. Commit each task with explicit paths only (never `git add -A`; `store-listing/` and `tools/store-screenshots/` must stay untracked).
  </action>
  <verify>
    <automated>node --test test/unit/worn-model.test.js && npm test 2>&1 | grep -E "^# (pass|fail)" && git diff --stat 6671921 -- engine content src mazeworld.html && git status --porcelain store-listing tools/store-screenshots</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "wornOnlySum" test/unit/worn-model.test.js` >= 2
    - `node --test test/unit/worn-model.test.js` passes with at least 20 tests
    - `npm test` ends `# fail 0` with `# tests` >= 2296
    - `git diff --stat 6671921 -- engine content src mazeworld.html` names exactly two files: content/treasure-tables.js and engine/derived.js
    - `git status --porcelain store-listing tools/store-screenshots` shows only `??` lines (never staged)
    - the SUMMARY contains a `## Human verification (deferred to end of run)` heading
  </acceptance_criteria>
  <done>Invariant and parity proofs green; plan gate recorded; SUMMARY written with the deferred-UAT section; three atomic commits with explicit paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file -> engine | `c.worn` / `c.items` arrive from untrusted JSON (a tampered or hand-edited save); this plan only READS them in eff/carriedItems/slotFor (null-safe) and MUTATES in reconcileWorn, which is not yet reachable from any load path |
| content -> engine | SLOT_OF is a frozen module constant derived from authored rows — no runtime input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-01 | Tampering | engine/derived.js#eff / #carriedItems | low | mitigate | object/array guards: a non-object `worn`, null entries, or a missing `items` array contribute nothing and never throw (unit-tested) |
| T-37-02 | Tampering | engine/derived.js#reconcileWorn | low | mitigate | returns null on non-object input; never re-migrates a present `worn` key; only moves items between two fields of the same `c` (no external data introduced) |
| T-37-03 | Denial of Service | parity harness | low | accept | a `worn` key on any fixture would be stripped structurally; no fixture can carry one (nothing creates it) |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | no package is installed or changed in this phase (`git diff --stat -- package.json package-lock.json` empty is a gate) |
</threat_model>

<verification>
- `npm test` → `# fail 0`, `# tests` >= 2296
- `node --test test/unit/worn-model.test.js` → all pass (taxonomy, slotFor, carriedItems, eff two-path, hasItemNamed/isFlying/conditionsOf routing, reconcileWorn, LEGACY_EFF_PINS table, invariant, rng invariance)
- `git status --porcelain test/parity/fixtures` empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0
- `git diff --stat 6671921 -- engine content src mazeworld.html` = content/treasure-tables.js + engine/derived.js only
- Executor standing gates: no fixture regeneration; no package/font change; commits with explicit paths only
</verification>

<success_criteria>
- ROADMAP SC-2 groundwork: with `c.worn` present, `eff` counts only worn items (invariant test); SC-4: every pre-existing gear/eff path is byte-identical for any state without `c.worn` (pinned legacy table over every chargen seed + fixture suite green)
- CONTEXT "Storage", "eff rule", "Bag ∪ worn lookups", "Slot taxonomy", "Parity carve-out" bullets each have a passing test or grep listed above
- The assumption-delta decision and the slot-storage deviation are recorded in this plan's frontmatter notes and restated in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/37-equipment-slot-model-eff-refactor/37-01-SUMMARY.md` when done (include `## Human verification (deferred to end of run)` — "nothing to add" with the reason).
</output>
