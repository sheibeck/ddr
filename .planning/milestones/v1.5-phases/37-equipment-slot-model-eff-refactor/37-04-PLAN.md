---
phase: 37-equipment-slot-model-eff-refactor
plan: 04
type: execute
wave: 4
depends_on: ["37-03"]
files_modified:
  - mazeworld.html
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-combat-actions.test.js
  - docs/GEAR-SLOTS.md
autonomous: true
requirements: [GEAR-03, GEAR-04]
tags: [shell, gear-tab, worn-rows, two-tap-swap-confirm, use-slot-form, reconciliation-rail-card, docs-ledger, phase-close, deferred-uat]
notes:
  rng_draw_impact: "zero — the shell only dispatches existing engine actions (equipItem / unequipSlot / useItem's new slot form) through the same dispatch()->applyAction() seam; the reconciliation card is built from Plan 03's returned report, not from any engine call"
  serialized_field_impact: "none beyond c.worn (Plans 01-03); every new shell state (SWAP confirm arm, the consumed boot report) is DOM-local or adapter-local, never on S (serializeRun spreads S wholesale)"
  canon_change: "declares the phase's ONE deliberate canon change in docs/GEAR-SLOTS.md: cloaks/jewelry/staves no longer stack — one worn item per slot type, an occupied slot is an explicit swap, activatables must be worn to work. Rationale: the prototype summed every carried copy unconditionally (a latent stacking bug) and had no worn concept; v1.5's magic-item and Gear-split phases need one. Fixture impact: ZERO — the legacy sum-all-carried path is preserved byte-for-byte for every state without c.worn (fixtures, bots, un-migrated saves), proved by Plan 01's pinned legacy table + the untouched parity suite"

must_haves:
  truths:
    - "mazeworld.html's classic eff(key) delegates to window.__mzEff (the engine's two-path eff) with the legacy loop as the pre-bridge fallback; the module block bridges __mzEff/__mzSlotFor/__mzWornSlots from engine/derived.js (test/unit/shell-worn-slots.test.js source pins)"
    - "The Gear tab's paint() carry block renders one .mw-worn row per populated c.worn[slot] (name, cooldown '· N sq' when counting down, txt, a Use button when it.use, Unequip dimmed to 'Bag full' exactly like the weapon/armor rows) after the armor row and before renderCarriedList; the pinned wornRow signature is untouched"
    - "In renderCarriedList's equip branch a bag cloak/jewel/staff on a c.worn state gets Equip (empty slot) or an armed two-tap 'Swap for {worn name}? [Yes] [No]' confirm (occupied slot) built on its own SWAP_CONFIRM_MS/swapConfirmRevert/revertSwapConfirm() trio and .mw-swap-confirm class, with a setTimeout revert and a document-level capture pointerdown (onSwapTap) outside-tap revert; Yes reverts then dispatches window.mzEquipItem(i); every Phase 33 Drop-confirm pin still holds (test/unit/shell-gear-toolbar.test.js unchanged and green)"
    - "window.mzUseItem accepts a bag index or { slot } and routes to engineCombatAction in combat / inventoryAction otherwise with { slot } or { i }; COMBAT_DISPATCH.useItem forwards d.slot when present (test/unit/shell-combat-actions.test.js re-pinned)"
    - "The title screen's ENTER-to-resume branch calls surfaceWornReconcile(), which consumes takeBootWornReport() through rail.js's wornReconcileCard and, only when a card results, pushes it via window.mzRailLine (GEAR, dull, 6000 ms, ▪) and logs the same line to the Oracle via window.logLine; a fresh roll or a second ENTER is a no-op"
    - "docs/GEAR-SLOTS.md exists with the canon-change declaration, the 24-row slot taxonomy, the model rules, the refusal vocabulary, the migration rule + option gate, and the eff() call-site inventory"
    - "Phase gate: npm test '# fail 0'; npm run build:www exit 0; fixtures untouched; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0; no package/font change; the cumulative engine/content/src/mazeworld.html/docs footprint since 6671921 is exactly the declared 17-file list; tools/ untouched; store-listing/ and tools/store-screenshots/ untracked"
  artifacts:
    - path: "mazeworld.html"
      provides: "classic eff routing, the three bridges, wornSlotRow rows, EQUIP + swap confirm, mzUseItem slot form, COMBAT_DISPATCH slot forwarding, surfaceWornReconcile on resume, .mw-swap-confirm CSS"
    - path: "test/unit/shell-worn-slots.test.js"
      provides: "source pins for every item above + voice safety + build-artefact sanity"
    - path: "docs/GEAR-SLOTS.md"
      provides: "the GEAR-03/GEAR-04 canon-change ledger for Phase 37"
  key_links:
    - from: "mazeworld.html classic eff(key)"
      to: "engine/derived.js#eff via window.__mzEff"
      via: "const f = window.__mzEff; if (f) return f(S.c, key);"
      pattern: "window\\.__mzEff"
    - from: "mazeworld.html renderCarriedList equip branch"
      to: "engine action equipItem (slot swap)"
      via: "mkSwapConfirm(i, wornNow.n) -> Yes -> window.mzEquipItem?.(i)"
      pattern: "mkSwapConfirm\\("
    - from: "mazeworld.html initTitleScreen ENTER (resume)"
      to: "src/browser/engineAdapter.js#takeBootWornReport + src/browser/rail.js#wornReconcileCard"
      via: "surfaceWornReconcile()"
      pattern: "surfaceWornReconcile\\(\\)"
    - from: "mazeworld.html COMBAT_DISPATCH.useItem / window.mzUseItem"
      to: "engine action { type: 'useItem', slot }"
      via: "d.slot !== undefined ? { slot: d.slot } : d.i"
      pattern: "d\\.slot !== undefined"
  prohibitions:
    - "No engine/, content/, src/ edit in this plan (Plans 01-03 own them); no ON YOU / BAG two-panel split, no bag-only drop prompt, no loot-card 'Equip now' for slot items (Phase 43); no magic-item chips/timers (Phase 39); no tuning-bot change (Phase 42)"
    - "Do not change the pinned wornRow signature, the DROP trio, the Drop confirm's class/listener names, the Use-button pin, gearRow/guard counts, or the .mw-drop-confirm CSS rule (shell-gear-toolbar/shell-armor-display pins)"
    - "No fixture edit/regeneration; no package/font change; no checkpoint:human-verify, device step or APK build — the aggregated Pixel 7 checklist is written, not executed"
    - "Commit with explicit paths only — never stage store-listing/ or tools/store-screenshots/"
---

<objective>
Put the worn model on the screen and close the phase: the classic script's `eff(key)` duplicate is routed through the engine's two-path `eff` (else a worn Amulet of Light would stop widening the map radius the moment it leaves the bag); worn rows join the existing worn area of the Gear tab beside the weapon/armor `wornRow` calls — one per populated `c.worn[slot]` with the item's `txt`, a Use button for activatables (dispatching `useItem { slot }`), and an UNEQUIP that dims to "Bag full" exactly as the weapon/armor rows do; slot items in the bag list get an EQUIP button that, when the slot is occupied, arms the two-tap `Swap for {worn name}? [Yes] [No]` confirm (the Phase 33 Drop / Phase 36 DISMISS pattern with its own `SWAP_CONFIRM_MS` trio); `window.mzUseItem` and `COMBAT_DISPATCH.useItem` learn the slot form so a worn staff/cloak is usable from the Gear tab and the fight's ITEMS submenu; the ENTER-to-resume path surfaces Plan 03's one-shot reconciliation report ONCE, only when something was bagged, as a GEAR rail card plus an Oracle line; `docs/GEAR-SLOTS.md` declares the canon change; the phase gate runs and the SUMMARY aggregates every plan's deferred Pixel 7 checks.

Per CONTEXT §"Gear-Tab Surface & Take Flow" (locked): worn rows in the existing worn area (`#s-carry` block, beside the weapon/armor `wornRow` calls) with `txt` + UNEQUIP (`bagFull` dims it like weapon/armor); EQUIP on bag slot items arming the swap confirm when occupied; the ON YOU / BAG split is Phase 43. Per §"Narration": the shell surfaces the report once, only when at least one extra was bagged, as a GEAR-family rail card plus an Oracle line in the sarcastic voice. Per `<specifics>`: swap copy `Swap for {worn name}? [Yes] [No]` reverting on timeout / outside tap like DISMISS; worn rows read like the armor row. Discretion resolved: the swap confirm has no combat-specific gating, mirroring the weapon/armor Equip button (which has none) — the confirm is DOM-local and reverts on any repaint; Use stays visible on bagged slot activatables (the engine's named `notWorn` refusal explains, Phase 31 "never disable silently" precedent) rather than hiding the button.

Purpose: ROADMAP SC-1 (the explicit swap choice), SC-2 (visible worn set), SC-3 (the narrated reconciliation line) — the player-facing half of all three — and the phase close.
Output: the mazeworld.html changes, `test/unit/shell-worn-slots.test.js`, the COMBAT_DISPATCH re-pin, `docs/GEAR-SLOTS.md`, the gate + aggregated checklist in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-CONTEXT.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-01-SUMMARY.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-02-SUMMARY.md
@.planning/phases/37-equipment-slot-model-eff-refactor/37-03-SUMMARY.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-06-SUMMARY.md
@mazeworld.html
@src/browser/engineAdapter.js
@src/browser/rail.js
@docs/USABLE-FEATURES-AUDIT.md
</context>

<artifacts_this_phase_produces>
See 37-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list. This plan creates the **[04]** items: `mazeworld.html` — classic `eff(key)` routed via `window.__mzEff`; module bridges `window.__mzEff = eff`, `window.__mzSlotFor = slotFor`, `window.__mzWornSlots = WORN_SLOTS`; `wornSlotRow(slot, it)` (paint-local helper) + the `WORN_SLOTS` loop; `SWAP_CONFIRM_MS` / `swapConfirmRevert` / `revertSwapConfirm()` trio; `mkSwapConfirm(i, wornName)` inside `renderCarriedList` with the `onSwapTap` listener and `.mw-swap-confirm` wrap; the equip-branch extension; `window.mzUseItem(ref)` slot form; `COMBAT_DISPATCH.useItem` slot forwarding; `surfaceWornReconcile()` + its call in the ENTER-resume branch; CSS `.mw-swap-confirm` + `.mw-swap-confirm button.small`; `test/unit/shell-worn-slots.test.js`; the `useItem` regex re-pin in `test/unit/shell-combat-actions.test.js`; `docs/GEAR-SLOTS.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: mazeworld.html — classic eff routing, bridges, worn slot rows, EQUIP + swap confirm, mzUseItem/COMBAT_DISPATCH slot forms, resume-time reconciliation card, CSS</name>
  <files>mazeworld.html</files>
  <read_first>
    - mazeworld.html lines 230-236 (`.mw-gear-actions`, `.mw-drop-confirm`, `.mw-drop-confirm button.small` CSS — copy the confirm rule body for `.mw-swap-confirm`; never edit the existing rules)
    - mazeworld.html lines 3184-3242 (the Gear tab carry block: `gearBtn`, the PINNED `const wornRow = (label, sub, slot, canUnequip, noSlotNeeded) =>`, the weapon/armor wornRow calls, the `renderCarriedList(carry, items, { clear: false, actions: ["use", "equip", "drop"], gearRow: true, … })` call — pinned `gearRow: true` once)
    - mazeworld.html lines 3539-3543 (the classic `function eff(key)` duplicate) and its 11 callers listed in 37-01-PLAN.md's `<eff_call_site_inventory>`
    - mazeworld.html lines 3664-3676 (the Phase 33 DROP trio: `DROP_CONFIRM_MS`, `dropConfirmRevert`, `revertDropConfirm()` — mirror verbatim with SWAP names), 3687-3806 (`renderCarriedList`: `mkBtn`, `mkDropConfirm` — the arm/Yes/No/timeout/outside-tap shape to mirror; the `equip` branch at ~3768; the gearRow flex-row re-parenting)
    - mazeworld.html lines 5140-5215 (Phase 36's DISMISS confirm in renderPartyRoster — the second precedent; note it reuses `className = "mw-drop-confirm"` OUTSIDE the renderCarriedList region, which is why the swap wrap inside that region needs its own class to keep the Phase 33 count pin at 1)
    - mazeworld.html lines 5468-5482 (`COMBAT_DISPATCH` — `useItem: (d) => window.mzUseItem?.(d.i)`), 6650 and 6687 (the rail.js and engineAdapter.js import lines of the trailing module block), 6720 (`import { conditionsOf, canCast } from "./engine/derived.js";`), 6750-6762 (`window.__mzConditionsOf`/`__mzCanCast` bridge comments — the pattern), 6807-6810 (`window.mzRailLine`), 7690-7716 (`inventoryAction`, `mzEquipItem`, `mzUnequip`, `mzUseItem`), 7734-7737 (`engineCombatAction(type, extra)` spreads `extra` into the action), 7887-7900 (`initTitleScreen`'s `enterBtn.onclick` resume branch)
    - src/browser/engineAdapter.js `takeBootWornReport` and src/browser/rail.js `wornReconcileCard` (Plan 03 — the exact return shapes)
    - test/unit/shell-gear-toolbar.test.js lines 57-135 and test/unit/shell-armor-display.test.js lines 77-80 (every pin that must survive: `className = "mw-drop-confirm"` exactly once in the renderCarriedList region; `document.addEventListener("pointerdown", onAnyTap, true)` exactly once; `setTimeout(revertDropConfirm, DROP_CONFIRM_MS)` once; the Use-button pin literal once; `gearRow: true` once; `guard: true` once; the wornRow signature)
    - test/unit/shell-combat-actions.test.js lines 144-165 (the COMBAT_DISPATCH pin this task's Task 2 re-pins)
  </read_first>
  <action>
    All edits are additive and placed as follows (build each control with document.createElement + textContent like the Drop/DISMISS confirms; never a CSS transition):

    1. CSS — directly after the `.mw-drop-confirm button.small{margin:0}` rule add `.mw-swap-confirm{display:inline-flex;align-items:center;gap:6px;font-family:var(--mono);font-size:11px;letter-spacing:.06em;color:var(--ink-soft)}` and `.mw-swap-confirm button.small{margin:0}` (no transition/animation/aria-disabled token).

    2. Classic `function eff(key)` (~3539): first line `const f = window.__mzEff; if (f) return f(S.c, key);` then the existing legacy loop as the pre-bridge fallback; comment: Phase 37 (GEAR-03) — the engine's two-path eff (worn-only when S.c.worn exists, legacy sum otherwise) is the ONE rule; the eleven classic callers (map radius, charges, to-hit, damage, upkeep, greed, throw, spellDmg, tongue) inherit it.

    3. Module bridges — extend line 6720's import to `{ conditionsOf, canCast, eff, slotFor, WORN_SLOTS }` and, directly after `window.__mzConditionsOf = conditionsOf;`, add `window.__mzEff = eff;`, `window.__mzSlotFor = slotFor;`, `window.__mzWornSlots = WORN_SLOTS;` with a short bridge comment (read-only helpers, no rng, no mutation — same pattern as __mzConditionsOf).

    4. Gear tab worn rows — in paint()'s carry block, directly AFTER the armor `wornRow(armorD.label, …)` call and BEFORE the `renderCarriedList(carry, items, {` call, add a paint-local `const wornSlotRow = (slot, it) => { … }` (do NOT touch `wornRow`'s pinned signature): an `li.mw-worn` whose innerHTML is `<b>${it.n}${cd ? ` · ${cd} sq` : ""} <span class="mw-worn-tag">worn</span></b><i>${it.txt ?? ""}</i>` where `cd` uses the same `it.every`/`it.usedAt`/`S.steps` arithmetic as renderCarriedList's rows; then `if (it.use) li.appendChild(gearBtn("Use", () => window.mzUseItem?.({ slot })));` then `li.appendChild(gearBtn(bagFull ? "Bag full" : "Unequip", () => window.mzUnequip?.(slot), bagFull));` then `carry.appendChild(li)`. Follow it with `for (const slot of (window.__mzWornSlots || [])) { const it = c.worn && c.worn[slot]; if (it) wornSlotRow(slot, it); }`. Comment: Phase 37 (GEAR-03) — one row per populated c.worn[slot], in WORN_SLOTS order, reads like the armor row; the ON YOU / BAG split is Phase 43.

    5. SWAP trio — directly below `function revertDropConfirm() {…}` add `const SWAP_CONFIRM_MS = 3000;`, `let swapConfirmRevert = null;`, `function revertSwapConfirm() { if (!swapConfirmRevert) return; const r = swapConfirmRevert; swapConfirmRevert = null; r(); }` with a Phase 37 comment mirroring the DROP one (DOM-local, never on S, one row armed at a time, setTimeout revert, capture pointerdown outside-tap).

    6. `renderCarriedList` — directly after `mkDropConfirm` add `const mkSwapConfirm = (i, wornName) => { … }` mirroring it exactly except: label text `Swap for ${wornName}?`, wrap `className = "mw-swap-confirm"`, Yes -> `revertSwapConfirm(); window.mzEquipItem?.(i);`, No -> `revertSwapConfirm()`, `setTimeout(revertSwapConfirm, SWAP_CONFIRM_MS)`, the listener named `onSwapTap` (`document.addEventListener("pointerdown", onSwapTap, true)` / matching remove), `swapConfirmRevert = () => {…}` restoring the Equip button (`wrap.replaceWith(equip)` when `wrap.isConnected`), returning `const equip = mkBtn("Equip", arm)`. Extend the `equip` action branch: keep the weapon/armor line byte-identical, then `else if (S.c.worn && window.__mzSlotFor?.(it)) { const wornNow = S.c.worn[window.__mzSlotFor(it)]; li.appendChild(wornNow ? mkSwapConfirm(i, wornNow.n) : mkBtn("Equip", () => window.mzEquipItem?.(i))); }`. Leave the `use` branch (its pinned literal) untouched.

    7. `window.mzUseItem` — change the parameter to `ref` and compute `const params = ref && typeof ref === "object" ? { slot: ref.slot } : { i: ref };` then `engineCombatAction("useItem", params)` in combat, `inventoryAction({ type: "useItem", ...params })` otherwise; extend its comment (Phase 37: the { slot } form addresses a WORN activatable — Gear-tab worn rows and the ITEMS submenu's worn rows).

    8. `COMBAT_DISPATCH.useItem: (d) => window.mzUseItem?.(d.slot !== undefined ? { slot: d.slot } : d.i),` with a one-line Phase 37 comment.

    9. Resume-time reconciliation — extend the engineAdapter import (line 6687) with `takeBootWornReport` and the rail.js import (line 6650) with `wornReconcileCard`; directly above `(function initTitleScreen() {` add `function surfaceWornReconcile() { const card = wornReconcileCard(takeBootWornReport()); if (!card) return; window.mzRailLine?.(card.title, card.line, card.tone, card.hold, card.icon); window.logLine?.(card.line); }` with a comment (Phase 37 GEAR-04: surfaces the ONE-SHOT report only when something was bagged; takeBootWornReport() nulls itself so a later ENTER is a no-op; rail card + Oracle line, never a toast). In `enterBtn.onclick` change the resume branch to `if (resumeIntent) { surfaceWornReconcile(); return; }`.

    Then run `npm run build:www` (exit 0) and `npm test` (`# fail 0` — expect exactly ONE failure before Task 2 lands: the shell-combat-actions COMBAT_DISPATCH pin; re-run after Task 2).
  </action>
  <verify>
    <automated>npm run build:www && node --test test/unit/shell-gear-toolbar.test.js test/unit/shell-armor-display.test.js test/unit/shell-toast-wiring.test.js test/unit/shell-company-panel.test.js && grep -c "mkSwapConfirm(" mazeworld.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "window.__mzEff = eff;" mazeworld.html` prints 1; `grep -c "window.__mzSlotFor = slotFor;" mazeworld.html` prints 1; `grep -c "window.__mzWornSlots = WORN_SLOTS;" mazeworld.html` prints 1
    - `grep -c "const SWAP_CONFIRM_MS = 3000;" mazeworld.html` prints 1; `grep -c "setTimeout(revertSwapConfirm, SWAP_CONFIRM_MS)" mazeworld.html` prints 1; `grep -c 'document.addEventListener("pointerdown", onSwapTap, true)' mazeworld.html` prints 1; `grep -c 'className = "mw-swap-confirm"' mazeworld.html` prints 1
    - `grep -c 'className = "mw-drop-confirm"' mazeworld.html` prints 2 (unchanged: renderCarriedList + renderPartyRoster); `grep -c 'document.addEventListener("pointerdown", onAnyTap, true)' mazeworld.html` is unchanged from before the edit
    - `grep -c "const wornSlotRow = (slot, it) =>" mazeworld.html` prints 1; `grep -c "window.mzUseItem?.({ slot })" mazeworld.html` prints 1; `grep -c "const wornRow = (label, sub, slot, canUnequip, noSlotNeeded) =>" mazeworld.html` prints 1
    - `grep -c "d.slot !== undefined ? { slot: d.slot } : d.i" mazeworld.html` prints 1; `grep -c "function surfaceWornReconcile()" mazeworld.html` prints 1; `grep -c "if (resumeIntent) { surfaceWornReconcile(); return; }" mazeworld.html` prints 1
    - `grep -c "^\.mw-swap-confirm{" mazeworld.html` prints 1 and that rule contains no `transition` or `animation` token
    - `npm run build:www` exits 0; the four existing shell test files listed in verify pass unchanged
  </acceptance_criteria>
  <done>The shell renders worn rows, arms the swap confirm on an occupied slot, dispatches the slot use form from both hosts, surfaces the reconciliation card once on resume, and every pre-existing shell pin still holds.</done>
</task>

<task type="auto">
  <name>Task 2: test/unit/shell-worn-slots.test.js source pins + the COMBAT_DISPATCH re-pin</name>
  <files>test/unit/shell-worn-slots.test.js, test/unit/shell-combat-actions.test.js</files>
  <read_first>
    - test/unit/shell-company-panel.test.js (all of it — the Phase 36 template: fs-read of mazeworld.html, sliceBetween-style region helpers, trio/position pins, confirm-mechanics pins, CSS pins, the BANNED voice check, the www/index.html artefact check that skips when www/ is absent)
    - test/unit/shell-gear-toolbar.test.js lines 57-67 (`renderCarriedListRegion()` / `paintCarryRegion()` bounds — reuse the same bounds)
    - test/unit/shell-combat-actions.test.js lines 144-165 (the `expected.useItem` regex to update)
    - mazeworld.html as edited in Task 1
  </read_first>
  <action>
    Write `test/unit/shell-worn-slots.test.js` (node:test, fs-read of mazeworld.html, importing `BANNED`/`ALLOWLIST` from content/safety-wordlist.js) with these tests:
    1. Trio: `const SWAP_CONFIRM_MS = 3000;`, `let swapConfirmRevert = null;`, `function revertSwapConfirm()` each exactly once, all positioned after `function revertDropConfirm()` and before `function renderCarriedList(`; `DROP_CONFIRM_MS` still declared exactly once.
    2. Confirm mechanics inside the renderCarriedList region: `mkSwapConfirm = (i, wornName)`, the `Swap for ${wornName}?` label, `className = "mw-swap-confirm"` once, `mkBtn("Yes"` >= 2 and `mkBtn("No"` >= 2 in the region, `setTimeout(revertSwapConfirm, SWAP_CONFIRM_MS)` once, `document.addEventListener("pointerdown", onSwapTap, true)` and its removeEventListener once each, `wrap.replaceWith(equip)`, Yes calls `revertSwapConfirm(); window.mzEquipItem?.(i);`; AND the Phase 33 pins re-asserted in the same region: `className = "mw-drop-confirm"` once, `onAnyTap` add/remove once each, `setTimeout(revertDropConfirm, DROP_CONFIRM_MS)` once.
    3. Equip branch: the weapon/armor Equip line is byte-identical (`if (it.kind === "weapon" || it.kind === "armor") li.appendChild(mkBtn("Equip", () => window.mzEquipItem?.(i)));`) and the slot branch reads `S.c.worn && window.__mzSlotFor?.(it)` and `mkSwapConfirm(i, wornNow.n)`.
    4. Worn rows in the paint carry region: `const wornSlotRow = (slot, it) =>` once; `<span class="mw-worn-tag">worn</span>` appears >= 2 times in the region (wornRow + wornSlotRow); `window.mzUseItem?.({ slot })` once; `window.mzUnequip?.(slot)` once; `for (const slot of (window.__mzWornSlots || []))` once; the wornSlotRow definition index is greater than the `wornRow(armorD.label` call index and less than the `renderCarriedList(carry, items, {` index; `gearRow: true` still once in the whole file; the pinned wornRow signature intact.
    5. Classic eff routing: the text between `function eff(key) {` and the next `\n}` contains `window.__mzEff` and still contains the legacy `for (const it of (S.c.items || []))` loop.
    6. Bridges: the derived.js import line contains `eff`, `slotFor` and `WORN_SLOTS`; `window.__mzEff = eff;`, `window.__mzSlotFor = slotFor;`, `window.__mzWornSlots = WORN_SLOTS;` once each; their index is greater than `window.__mzConditionsOf = conditionsOf;`.
    7. mzUseItem: a regex matching `window.mzUseItem = (ref) =>` and `ref && typeof ref === "object" ? { slot: ref.slot } : { i: ref }` and both `engineCombatAction("useItem", params)` and `inventoryAction({ type: "useItem", ...params })`.
    8. Resume surfacing: `function surfaceWornReconcile()` once; its body references `wornReconcileCard(takeBootWornReport())`, `window.mzRailLine?.(card.title, card.line, card.tone, card.hold, card.icon)` and `window.logLine?.(card.line)`; `if (resumeIntent) { surfaceWornReconcile(); return; }` once; the module imports `takeBootWornReport` from ./src/browser/engineAdapter.js and `wornReconcileCard` from ./src/browser/rail.js.
    9. CSS: `^\.mw-swap-confirm\{[^}]*\}` exists once with no transition/animation/aria-disabled token; `.mw-swap-confirm button.small{margin:0}` once; the `^\.mw-drop-confirm\{` rule is byte-identical to `display:inline-flex;align-items:center;gap:6px;margin-left:auto;font-family:var(--mono);font-size:11px;letter-spacing:.06em;color:var(--ink-soft)`.
    10. Voice: the literals "Swap for", "Unequip", "Bag full", "Use", "worn" clear the BANNED list (word-boundary, allowlist-aware — reuse the shell-company-panel matcher).
    11. Build artefact: if www/index.html exists it contains `mw-swap-confirm` and `surfaceWornReconcile` (else skip with a message).

    Then in `test/unit/shell-combat-actions.test.js` replace the `useItem` regex with `/useItem:\s*\(d\)\s*=>\s*window\.mzUseItem\?\.\(d\.slot !== undefined \? \{ slot: d\.slot \} : d\.i\)/` and a `// Phase 37 (GEAR-03): forwards the worn-slot use form` comment; the eight-key count assertion stays.

    Run `node --test test/unit/shell-worn-slots.test.js test/unit/shell-combat-actions.test.js` then `npm test`.
  </action>
  <verify>
    <automated>node --test test/unit/shell-worn-slots.test.js test/unit/shell-combat-actions.test.js && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/unit/shell-worn-slots.test.js` passes with >= 11 tests
    - `grep -c "d\\\\.slot !== undefined" test/unit/shell-combat-actions.test.js` >= 1 (the re-pinned regex)
    - `npm test` ends `# fail 0` with `# tests` >= 2276 + (Plan 01 + 02 + 03 + 04 new tests)
  </acceptance_criteria>
  <done>Every Task 1 edit is pinned by source assertion; the COMBAT_DISPATCH pin reflects the slot form; the whole suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: docs/GEAR-SLOTS.md canon ledger, Phase 37 closing gate, aggregated Pixel 7 checklist</name>
  <files>docs/GEAR-SLOTS.md</files>
  <read_first>
    - docs/USABLE-FEATURES-AUDIT.md lines 1-42 and 274-300 (the ledger voice/format: numbered §-sections, tables, declared divergences with before/after and fixture impact)
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-01-PLAN.md `<eff_call_site_inventory>` and frontmatter notes (assumption-delta record, the slot-storage deviation), 37-03-PLAN.md frontmatter `surfaced_assumption_option_gate`
    - .planning/phases/37-equipment-slot-model-eff-refactor/37-01-SUMMARY.md, 37-02-SUMMARY.md, 37-03-SUMMARY.md (their `## Human verification (deferred to end of run)` sections — aggregate them; their measured numbers — cite them)
    - .planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-06-SUMMARY.md §"Human verification (deferred to end of run)" (the aggregated checklist format: plan-grouped, continuously numbered, one observable per item)
    - .planning/ROADMAP.md Phase 37 section (SC-1..4 — map each)
  </read_first>
  <action>
    Create `docs/GEAR-SLOTS.md` ("Equipment Slot Model (Phase 37, GEAR-03 / GEAR-04)") with: §1 Declared canon change — the prototype (1994 rules + `mazeworld.html`'s classic `eff`) summed every carried cloak/jewel/staff copy unconditionally and had no worn concept; from v1.5 a character wears ONE item per slot type, equipping into an occupied slot is an explicit swap, activatables must be worn to work; rationale (latent stacking bug surfaced by the one-per-type rule; Phase 39 magic items and Phase 43's Gear split need a worn set); fixture impact: ZERO — the legacy sum-all-carried path is preserved byte-for-byte for every state without `c.worn`, proved by the pinned legacy table over the 14 chargen seeds and the untouched parity suite; the assumption-delta record (noun = worn slot; decision = promote). §2 Slot taxonomy — a 24-row table (item, kind, slot) matching content/treasure-tables.js, plus the note that the slot is authored on the rows but never spread onto item objects (ECON-08 precedent, the frozen economy fixture's declared `after.items`). §3 The model — `c.worn` lazy map, not a bag member (slotItems/sell/drop/loot-compare never see it), `eff` two-path rule, `carriedItems` for name scans, `usedAt`/`every` ride on the item. §4 Play rules — auto-wear on take when the slot is empty (find/loot/take-all/takeItem), bag otherwise; `equipItem` direct swap with `itemEquipped.replaced`; `unequipSlot` through the `stowItem` gate (`bagFull`); staff class gate (`wrongClass`); refusal vocabulary table (`notEquippable`, `wrongClass`, `bagFull`, `useRefused.notWorn` with its exact copy). §5 Old-save reconciliation (GEAR-04) — first-in-bag-order worn, extras bagged, never overflowing, never re-migrated, the `[{ slot, worn, bagged }]` report, the rail/Oracle line with its copy, the option gate (`wornSlots: true` on `newRun`/`validateSave`/`rehydrate`, passed only by the shell's `startNewRun`/`boot`; fixtures/bots/tools untouched) and why. §6 The `eff()` call-site inventory (engine + viewModels + the eleven classic-script callers) copied from 37-01-PLAN.md. §7 Out of scope / next — Phase 39 (use→effect→cooldown chips), Phase 42 (bot wear/swap policy under the option), Phase 43 (ON YOU / BAG split, loot-card equip for slot items).

    Then run the Phase 37 closing gate and record every result in the SUMMARY: `npm test` (`# fail 0`, cite the final `# tests` total); `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat -- test/parity/fixtures test/parity/prototype-master.js.txt package.json package-lock.json tools` empty; `grep -c fonts.googleapis mazeworld.html` = 0; `git diff --stat 6671921 -- engine content src mazeworld.html docs` lists EXACTLY: content/treasure-tables.js, engine/derived.js, engine/items.js, engine/actions.js, engine/engine.js, engine/state.js, engine/saveState.js, src/browser/toasts.js, src/browser/eventNarration.js, src/browser/engineAdapter.js, src/browser/rail.js, src/browser/combatMenu.js, mazeworld.html, docs/USABLE-FEATURES-AUDIT.md, docs/GEAR-SLOTS.md (15 files; the test/ diff is additional); `git status --porcelain store-listing tools/store-screenshots` shows only `??` lines; `node -e` proof that `newRun(2)` has no worn key while `newRun(2, [], { wornSlots: true })` wears the cloak.

    Write the SUMMARY's `## Success Criteria Map (ROADMAP SC-1..4)` table (each SC -> plan(s) -> proof) and the `## Human verification (deferred to end of run)` section as ONE aggregated, plan-grouped, continuously numbered Pixel 7 checklist covering: Plan 01 (nothing — no visible behaviour), Plan 02 (engine behaviours exercised through Plan 04's UI), Plan 03 (a pre-Phase-37 save with two rings: on ENTER→resume the GEAR rail card "You were wearing two rings on one finger…" appears once with the same line in the Oracle; the second ring sits in the bag with an EQUIP button; a fresh roll shows the Thief's cloak as a worn row, not a bag row; the fight's ITEMS submenu lists a worn staff as a usable row and a bagged one refuses "Wear it first"), Plan 04 (Gear tab: worn rows read name · txt · [Use] [Unequip]; tapping EQUIP on a second ring shows `Swap for Ring of Power? [Yes] [No]`, No/3-second timeout/outside tap revert it, Yes swaps and the Oracle reads "Ring of Power goes back in the bag"; UNEQUIP on a full bag reads "Bag full" and is inert; picking up a cloak with an empty cloak slot auto-wears it (rail: Equipped) and with an occupied slot bags it; the map radius with a worn Amulet of Light stays widened; the hero sheet damage bracket counts a worn Ring of Power once; TalkBack reads the confirm buttons). Note explicitly that the APK build and `adb install -r` happen at milestone close, not here. Commit with explicit paths only.
  </action>
  <verify>
    <automated>test -f docs/GEAR-SLOTS.md && grep -c "^## " docs/GEAR-SLOTS.md && npm test 2>&1 | grep -E "^# (tests|pass|fail)" && npm run build:www && git diff --stat 6671921 -- engine content src mazeworld.html docs | tail -1 && git status --porcelain test/parity/fixtures store-listing tools/store-screenshots</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/GEAR-SLOTS.md` succeeds; `grep -c "^## " docs/GEAR-SLOTS.md` >= 7; `grep -c "notWorn" docs/GEAR-SLOTS.md` >= 1; `grep -c "wornSlots" docs/GEAR-SLOTS.md` >= 1; `grep -c "Physics has filed a complaint" docs/GEAR-SLOTS.md` >= 1; `grep -c "engine/derived.js:284" docs/GEAR-SLOTS.md` >= 1 (the call-site inventory is copied, not summarised)
    - `npm test` ends `# fail 0`; `npm run build:www` exits 0
    - `git status --porcelain test/parity/fixtures` is empty; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0; `git diff --stat -- package.json package-lock.json tools` is empty; `grep -c fonts.googleapis mazeworld.html` prints 0
    - `git diff --stat 6671921 -- engine content src mazeworld.html docs` lists exactly the 15 declared files
    - `git status --porcelain store-listing tools/store-screenshots` shows only `??` lines (never staged in any Phase 37 commit — `git log --stat 6671921..HEAD -- store-listing tools/store-screenshots` is empty)
    - the SUMMARY contains `## Success Criteria Map` and `## Human verification (deferred to end of run)` with a plan-grouped, continuously numbered checklist of at least 14 items
  </acceptance_criteria>
  <done>The canon ledger exists; the closing gate is green and recorded; the aggregated Pixel 7 checklist for the milestone-close UAT batch is written; the phase's 15-file footprint matches the declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM taps -> engine actions | Equip/Unequip/Use taps build action objects (`{ i }`, `{ slot }`) — the engine's validateAction is the gate; the shell never mutates S directly |
| adapter report -> rail/Oracle | item names from a (possibly hand-edited) save reach `mzRailLine` and `logLine` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-37-10 | Tampering | mazeworld.html mzUseItem / COMBAT_DISPATCH | low | mitigate | only `{ slot: ref.slot }` or `{ i: ref }` is ever dispatched; engine/actions.js rejects anything outside WORN_SLOTS / non-negative ints (Plan 02) |
| T-37-11 | Spoofing / Injection | rail card + Oracle line from item names | low | mitigate | renderRail renders card lines via textContent (existing), and logLine's Oracle already receives engine-formatted html for item names from the same content strings; the swap-confirm label and worn rows use textContent/innerHTML with content-table names exactly as the existing rows do |
| T-37-12 | Denial of Service | swap confirm arm state | low | mitigate | DOM-local, one row armed at a time, setTimeout + outside-tap + repaint revert (wrap.isConnected guard) — never on S, never blocks dispatch |
| T-37-SC | Tampering | npm/pip/cargo installs | low | accept | no package installed or changed this phase |
</threat_model>

<verification>
- `node --test test/unit/shell-worn-slots.test.js test/unit/shell-combat-actions.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-armor-display.test.js test/unit/shell-company-panel.test.js test/unit/shell-toast-wiring.test.js` → pass
- `npm test` → `# fail 0`; `npm run build:www` → exit 0
- Phase gate as listed in Task 3 (fixtures, master hash, package/font/tools unchanged, 15-file footprint, untracked user directories never staged)
</verification>

<success_criteria>
- ROADMAP SC-1: EQUIP on a second ring/cloak/staff/bracelet arms an explicit `Swap for {worn}?` confirm — never silent stacking (source pins + Plan 02 engine tests)
- ROADMAP SC-2: the Gear tab shows exactly the one-per-slot worn set; every `eff` consumer (engine + classic shell duplicate) reads it (Plan 01 tests + the classic-eff routing pin)
- ROADMAP SC-3: a pre-Phase-37 double-equipped save resumes with the GEAR rail card + Oracle line once and the extra in the bag (Plan 03 tests + the resume-path pin)
- ROADMAP SC-4: every legacy path is byte-identical — the parity suite, the pinned legacy table, the legacy identity sweep, and the untouched fixtures prove it; the closing gate is recorded
- CONTEXT §"Gear-Tab Surface & Take Flow" and §"Narration" bullets each map to a pin or test above; the aggregated Pixel 7 checklist is written for the milestone-close UAT batch
</success_criteria>

<output>
Create `.planning/phases/37-equipment-slot-model-eff-refactor/37-04-SUMMARY.md` when done — it MUST contain the `## Success Criteria Map (ROADMAP SC-1..4)` table and the aggregated `## Human verification (deferred to end of run)` checklist for Plans 01-04.
</output>