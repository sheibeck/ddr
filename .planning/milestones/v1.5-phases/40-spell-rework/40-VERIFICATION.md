---
phase: 40-spell-rework
verified: 2026-09-18T19:05:00Z
status: passed
score: 6/6 success criteria verified on automated evidence; every player-facing check (niche lines, chips, Map the Floor tint/re-fog, Hero-tab rows, scroll refusal, old-save rename) is deferred to the end-of-run Pixel 7 batch per the user's "defer uat to end" instruction
behavior_unverified: 0
overrides_applied: 0
human_verification: ["Pixel 7 (SPELL-01): Hero-tab Grimoire rows read as one niche line each — e.g. Freeze reads 'burst · one foe · d6, and frozen solid on a hit'", "Pixel 7 (SPELL-04): a fresh Summoner's grimoire lists Lesser Summon (castable) and Summon ('Needs level 2') — roll several fresh Summoners and confirm the split", "Pixel 7 (SPELL-05): 'Detect Magic' appears nowhere in the UI — Hero tab, combat SPELLS menu, scroll/shop copy all read 'Map the Floor'", "Pixel 7 (SPELL-01, Ice): cast Ice on a tough foe — 'Ice climbs …', a d6 tick each round, 'freezes solid' at the end with the kill paid", "Pixel 7 (SPELL-01, Weaken): 'They hit softer now, for N rounds.' then 'Their arms remember how to swing.' when it runs out; a mid-window recast shows a fresh count, not a stack", "Pixel 7 (SPELL-01, Stupidity): the foe 'stands there, thinking about nothing' every round after the cast and never swings or casts for the rest of the fight", "Pixel 7 (SPELL-04, Lesser Summon): a level-1 Summoner's Lesser Summon brings a small ally for at most 4 rounds and never backfires on repeated casts", "Pixel 7 (SPELL-01, Lightning): hits every foe in a multi-foe fight — one roll per foe", "Pixel 7 (SPELL-01, Shrink): a shrunk foe's blow visibly lands softer; Weakened as well → softer still", "Pixel 7 (SPELL-02): cast Mirror Self / Sense Presence / Regeneration outside a fight — each shows a condition chip on the map HUD with a plain-language tap explanation", "Pixel 7 (SPELL-02, Sense Presence): a forced foe-first race (e.g. a Samurai) is no longer forced — 'You move first. Nothing gets the jump on you.' when the fair roll goes the hero's way", "Pixel 7 (SPELL-02, fades): after a fight, a still-active Sense Presence / Regeneration shows 'Your senses dull back to normal.' / 'The wounds stop closing on their own.' exactly once", "Pixel 7 (SPELL-07): a level-1 Warlock reading a Heal scroll sees 'Heal needs level 3; you are 1. The scroll reads itself once and crumbles.', is healed once, and Heal is NOT in the grimoire afterward", "Pixel 7 (SPELL-05, cast): Map the Floor reveals the whole floor, the rail says 'The floor lays itself out in your head — every corridor on this level, for 40 squares', a MAPPED chip counts down from 40, and spell-only cells paint in the distinct 'borrowed sight' tint", "Pixel 7 (SPELL-05, expiry): walk 40 squares — walked corridors stay lit (tint reverts as you walk them); everything else fogs back with 'The map forgets what it was told.' and the MAPPED chip disappears", "Pixel 7 (SPELL-05, recast): recast ~20 squares in — the chip resets to 40, nothing flickers or re-fogs early", "Pixel 7 (SPELL-05, descend): descend mid-window — the MAPPED chip vanishes and the new floor has no leftover reveal state or stray tint", "Pixel 7 (tolerant load): resume a pre-Phase-40 save with Detect Magic in the grimoire — the row reads 'Map the Floor' with no card or popup", "Pixel 7 (SPELL-06): the Hero-tab Shield row shows BOTH hp and rounds (e.g. '34 hp left · 3 rds') and the map-HUD Shield chip shows the same pool and rounds outside combat", "Pixel 7 (SPELL-02, Hero tab): Mirror Self / Sense Presence / Sense Danger (armed) / Map the Floor rows show while each is up and disappear with their chip", "Pixel 7 (chips): tap each of the five new chips (Mirrored/Senses/Regenerating/Forewarned/Mapped) — correct detail and a deadpan tap explanation", "Pixel 7 (badges): a foe under Weaken shows 'Weakened · N' counting down; under Ice (or Poisoned Edge) 'Ice · N' / 'Poison · N' beside the Acid badge style", "Pixel 7 (a11y): TalkBack reads the five new chips' labels/details and the four new Hero-tab kit rows", "Pixel 7 (voice): spot-check every new line in real play (chip explanations, Hero-tab rows, foe-badge phrasings, spell niche lines) for the family-friendly deadpan tone"]
gaps: []
---

# Phase 40 — Verification (orchestrator-authored; gsd-verifier disabled for usage limits, 2026-09-18)

Goal-backward check of the phase goal: *every combat spell is a real situational choice — differentiated by niche, not a damage ladder; every utility spell has a measurable effect; every Wizard sub-class starts with a damage spell; Detect Magic is renamed to say what it does and time-boxed; a scribed scroll is castable immediately once its level qualifies.*

Third phase under the greenfield amendment; carries two ratified Key Decisions (re-fog provenance; the Summoner's lesser summon replacing the Phase 23 override).

## Automated evidence (re-run by the orchestrator after 40-05)

- `npm test`: **2813/2813, 0 failures** (2686 at phase start → 2711 after 40-01 → 2739 after 40-02 → 2773 after 40-03 → 2800 after 40-04 → 2813 after 40-05). `npm run build:www` exit 0; `grep -c fonts.googleapis mazeworld.html` = 0; `package.json`/lock unchanged.
- Engine gate: `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` (unchanged); `git status --porcelain test/parity/fixtures` empty. Declared divergences (40-01, measured live — fewer than the plan predicted): chargen grimoire records for seeds 7/15/24/29 and three scenario-level `chargenDivergence` records (magic heal/scroll, combat lose-apprentice); Plans 02–04 zero fixture moves (each proven by the suite); `spellSeen` is a structural harness carve-out (`stripSpellSeen` in all three comparables + three local duplicates). `test/unit/chargen-rng-pin.test.js` pin TABLES byte-unedited — Lesser Summon joins day-one pools via a derived rng stream, never the main-rng shuffles. `docs/class-pass/v15-before*.json` byte-identical to `b0e93d6`.
- Cumulative `git diff --stat b0e93d6 -- engine content src mazeworld.html docs tools test/parity/fixtures` = 23 files, 1718+/202−; `mazeworld.html` touched by 40-05 only.
- No name-keyed spell logic remains (`grep -rn 'sp\.n === "' engine/ tools/lib/` = 0): Freeze `onHit: "freeze"`, Lightning `aoe: "all"`, bot repointed to kinds/flags.
- New events (`floorMapped`, `revealFaded`, `weakenFaded`, `foeStupefied`, `frozenSolid`, `regenFaded`, `sensesFaded`, `mirrorFaded`, `scrollTooAdvanced`, `combatJoined.senses`, …) all have `EVENT_NARRATION` + `TOAST_FOR` + rail entries; `detectMagic` retired with zero dead entries; coverage guards and voice scan green.
- Five plans, five SUMMARY.md files, all `## Self-Check: PASSED`; working tree clean at HEAD `6a717a9` apart from the two untracked user directories outside the phase.

## Success criteria → evidence

| # | Success criterion (ROADMAP) | Evidence |
|---|-----------------------------|----------|
| 1 | Comparing two combat spells of the same level, a player can tell they solve different problems — each spell's grimoire text states its niche in one line | 33-row `content/spells.js` with `niche` + `NICHE_LABELS[niche] · …` txt on every row; offense niche map by level in `docs/SPELLS.md` (burst / DOT — Ice `kind: "dot"` d6 a round, d4+1 rounds then frozen solid / multi-target / control with scope × duration — Weaken `spell:weaken` d4+1 rounds, Stupidity fight-long, Shrink real half-damage / defensive / chaos for Vapor & Insane); `grimoireViewModel` + SPELLS submenu rows carry `niche`/`nicheLabel`; `spell-mechanics.test.js` (28), `shell-spells-40.test.js` (10). |
| 2 | Casting any utility spell produces an observable effect; none is a dead pick | `conditionsOf` chips mirror/senses/regen/foresight/reveal (+ ward pool & rounds); Sense Presence gains a real engine meaning (waives every forced foe-first initiative rule; expires at the next fight's end with `sensesFaded`); `regenFaded`/`mirrorFaded` at `endCombat`; Hero-tab rows for each; `spell-utility.test.js` (29). |
| 3 | Every Magic User sub-class's day-one grimoire includes at least one damage spell | `DAMAGE_SPELL_KINDS`/`dealsDamage`; `rollGrimoire`'s day-one walk requires a damage-dealing kind for all 8 subs; the Summoner is deterministically granted **Lesser Summon** (level 1, special school, weaker ally, no doubling/backfire); Illusionist keeps `Phantom Host: 1`; 8-sub × 200-seed proof test green. |
| 4 | Casting the renamed map-reveal spell shows the floor for a stated number of steps, then re-fogs only the cells it revealed | **Map the Floor**: `spell:reveal` 40-square timer, per-cell `spellSeen` set only on not-yet-seen cells, `reveal()` graduates walked cells, `refogSpellSeen` runs ONCE on the effect→null transition (`revealFaded`), recast refreshes, `descend` clears; `MAP_PALETTE.floorSpell` tint; `map-reveal.test.js` (18). |
| 5 | A scribed scroll is castable immediately once the caster's level qualifies, or refuses by naming the level needed | `readScroll` scribes only when `spellLevelFor` + `schoolGate` pass (the exact `canCast` checks), else `scrollTooAdvanced { spell, need, have, school }` then the free cast — closes five reachable level-1 stuck-grimoire cases; old stuck entries tolerant. |
| 6 | An active Shield's remaining pool and rounds show on the Hero sheet and as a map HUD chip, not only mid-combat | Hero-tab Shield row reads pool AND rounds; the ward chip (pool + rounds) shows outside combat — outside-combat proof test in 40-05 Task 1. |

## Requirements

SPELL-01 ✓ · SPELL-02 ✓ · SPELL-03 ✓ (before/after table + declared divergences in `docs/SPELLS.md`) · SPELL-04 ✓ · SPELL-05 ✓ · SPELL-06 ✓ · SPELL-07 ✓ — automated halves verified; on-device halves in the frontmatter `human_verification` list (24 items, from the aggregated checklist in 40-05-SUMMARY.md).

## Accepted planner/executor deviations (recorded in plan/SUMMARY frontmatter)

- Planner corrections to CONTEXT, verified in code: the chargen rng pin does NOT move (derived-stream insertion); Shrink stays multi-target; Sense Presence's "never surprised" had no engine read (given one); Freeze is an instant kill on a hit; `allyCast` was a third name-keyed Freeze check (repointed).
- 40-01 measured only seeds 7/15/24/29 diverging (the plan predicted 8 and 19 too) — recorded, not reconciled silently; two Rule-1 fixes (bot `dot` tier, dead "balls" allowlist entry).
- 40-02 committed Tasks 1+2 together (shared functions); 40-04 wired the tolerant-load helpers into `rehydrate()` as well as `validateSave` (Rule 2).

## Follow-ups noted (not gaps)

- Phase 42's bot must learn *when* to cast by niche (DOT vs burst vs control) before the ONE AFTER matrix; it already casts every reshaped kind.
- Phase 41 darkness/light vs the Map the Floor window — orthogonal today (the spell only touches `seen`/`spellSeen`); keep it so.
