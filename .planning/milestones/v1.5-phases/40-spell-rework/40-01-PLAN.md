---
phase: 40-spell-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/spells.js
  - content/spell-level-overrides.js
  - engine/character.js
  - engine/derived.js
  - test/unit/spell-table.test.js
  - test/unit/day-one-damage.test.js
  - test/unit/chargen-rng-pin.test.js
  - test/unit/guaranteed-attack-spell.test.js
  - test/unit/spell-level-overrides.test.js
  - test/unit/spell-menu-mirror.test.js
  - test/unit/identity-contract.test.js
  - test/unit/casters-can-act.test.js
  - test/unit/grimoireViewModel.test.js
  - test/unit/content-tables.test.js
  - test/unit/magic.test.js
  - test/unit/tuning-bot.test.js
  - test/parity/fixtures/action-script.chargen.json
  - test/parity/fixtures/action-script.magic.json
  - test/parity/fixtures/action-script.combat.json
  - test/parity/FIXTURE-INVENTORY.md
  - docs/USABLE-FEATURES-AUDIT.md
  - docs/SPELLS.md
autonomous: true
requirements: [SPELL-01, SPELL-03, SPELL-04]

must_haves:
  truths:
    - "content/spells.js is a 33-row table: the 32 canon rows keep their array positions, `lvl` and `s` byte-identical; every row carries a `niche` field and a `txt` that begins with that niche label (SPELL-01's 'states its niche in one line'); Detect Magic is renamed Map the Floor; Freeze carries `onHit: 'freeze'`, Lightning carries `aoe: 'all'`, Ice is `kind: 'dot'`; row 32 is the new level-1 special-school Lesser Summon (`kind: 'summon', lesser: true, roll: 'derived'`)"
    - "At every offense-school level with two or more spells the table offers at least two distinct niches (machine-checked)"
    - "SPELL_LEVEL_OVERRIDES has exactly one row (Illusionist / Phantom Host: 1) — Summon is spell level 2 for everyone again; the Summoner's offense gate stays 3 and identity-contract's Summoner BAD pin is untouched"
    - "Every one of the 8 Magic User sub-classes holds a castable damage-dealing spell (`dealsDamage`) on day one, proven over every sub × 200 seeds; the Summoner always holds Lesser Summon (a deterministic grant)"
    - "The day-one roll consumes EXACTLY the same main-rng draws as before this phase: `test/unit/chargen-rng-pin.test.js`'s three pin tables are unedited and green (the added row joins the pools through a derived stream keyed on the main cursor, never a lengthened shuffle)"
    - "Every fixture the rename / grant / damage-guarantee moves is measured live and declared field-by-field (grimoire) with the existing chargenDivergenceFor / chargenShiftOf mechanisms; the prototype master hash is unchanged; no fixture `seed`/`actions`/`before` is edited"
  artifacts:
    - "content/spells.js — 33 rows with `niche`, new flags, Lesser Summon; NICHE_LABELS export"
    - "content/spell-level-overrides.js — Illusionist-only table"
    - "engine/derived.js — DAMAGE_SPELL_KINDS, dealsDamage(sp)"
    - "engine/character.js — rollGrimoire with derived insertion, the Summoner Lesser Summon grant, the damage-dealing day-one walk"
    - "test/unit/spell-table.test.js, test/unit/day-one-damage.test.js"
    - "docs/SPELLS.md — the Phase 40 ledger (canon change, before/after table, niche map, day-one proof, declared divergences, corrections to CONTEXT)"
  key_links:
    - "content/spells.js `roll: 'derived'` -> engine/character.js#rollGrimoire (main-rng pools exclude derived rows; derivedRng(cursor, 'grimoire', sub) places them) -> test/unit/chargen-rng-pin.test.js formula (excludes derived rows, constants unchanged)"
    - "engine/derived.js#dealsDamage -> rollGrimoire's day-one walk -> test/unit/day-one-damage.test.js (8 subs × 200 seeds)"
    - "content/spell-level-overrides.js -> engine/derived.js#spellLevelFor -> canCast / rollGrimoire#usableNow (Summon back to 2; Lesser Summon carries its own lvl 1)"
---

<objective>
Reshape the spell TABLE — the content half of Phase 40. Every row gets a niche and a one-line niche `txt` (D: CONTEXT Area 2 "reshape offense + relabel the rest"); the offense school gets real niches by level with the data flags the engine will read in Plan 02 (`onHit: "freeze"`, `aoe: "all"`, Ice as `kind: "dot"`); Detect Magic becomes Map the Floor (SPELL-05's rename — the timed re-fog mechanic is Plan 04); the Summoner's level-1 Lesser Summon row is added and the Phase 23 `SPELL_LEVEL_OVERRIDES.Summoner` entry is retired (CONTEXT Area 3, the user's verbatim ruling); the day-one grimoire roll guarantees every Magic User sub a damage-dealing spell (SPELL-04) with ZERO main-rng drift; every fixture the changes move is measured and declared (SPELL-03).

Purpose: SPELL-01's "a player can tell two same-level spells solve different problems" starts in the table; SPELL-04's day-one damage guarantee and SPELL-03's declared-divergence discipline are chargen-time facts that every later plan builds on.

Output: the 33-row table, the retired override, the new `rollGrimoire`, two new test files, updated pins, declared fixture records, `docs/SPELLS.md` created with the canon change / before-after table / niche map / day-one proof / declared divergences / corrections-to-CONTEXT sections. No engine cast-branch changes beyond what the table needs to stay loadable (Plan 02 reads the new flags and kinds); no `mazeworld.html` edits (Plan 05).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-spell-rework/40-CONTEXT.md
@.planning/phases/40-spell-rework/40-RESEARCH.md
@.planning/phases/38-melee-active-abilities/38-01-SUMMARY.md
@content/spells.js
@engine/character.js
@engine/rng.js
</context>

<standing_rules>
- Greenfield ruling (CONTEXT "Standing rulings", STATE.md amendment 2026-09-17): new rules are the only rules — no dual paths, no fixture-motivated hedges, no shell-only options. A fixture the change moves is DECLARED (before/after, rationale) and regenerated — only those; `test/parity/prototype-master.js.txt` is never edited (hash stays `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- The chargen rng pin (`test/unit/chargen-rng-pin.test.js`) is OURS. This plan does NOT move it: the added row joins the day-one pools through `derivedRng` (STATE.md's amendment: "prefer a derived rng stream for new rolls that would otherwise reorder floor generation"). The ONLY edit to that file is the third test's formula filter (derived rows excluded), with the rationale in the commit and the ledger. If any measured pin value moves, STOP and report — that is a bug in the insertion, not a re-pin.
- Commit convention: `feat(40-01): …` / `test(40-01): …` / `docs(40-01): …`; end every commit message with the two attribution lines `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`.
- Stage with explicit paths only (`git add <file> …`); never stage `store-listing/` or `tools/store-screenshots/` (untracked user directories, not part of any phase).
- Deferred UAT: no device step, no `adb`, no APK build, no pause. The SUMMARY carries a "## Human verification (deferred to end of run)" section listing what a Pixel 7 tester would check for this plan.
- Verification agents are off: Task 3 is this plan's own gate and records its numbers in the SUMMARY.
- Voice: every new `txt` line is family-friendly deadpan (`node --test test/voice/safety-scan.test.js` scans the spell `txt` fields); HP not WP in player copy.
- Fixture divergence protocol (applies to Task 3; Plans 02–04 reference it): (1) run the suite and let the harness tell you which scenarios move — never reason from seeds; (2) for each moved scenario, measure `before` from `loadPrototypeSandbox({seed})` and `after` from the engine replay with a scratch script, never hand-type; (3) edit only that scenario's record (`after`, or a new record where none existed), never `seed`/`actions`/`scenarios` arrays, never `before`; (4) one FIXTURE-INVENTORY section per plan with a rationale row per record and a "byte-identical elsewhere" paragraph; (5) `prototype-master.js.txt` is never touched.
</standing_rules>

<artifacts_this_phase_produces>
Phase-wide symbol inventory (P = plan that creates it). This plan creates every P01 entry.

Content (`content/spells.js`): `niche` on every row + `NICHE_LABELS` (P01) · row 5 renamed `Map the Floor` (P01) · `onHit: "freeze"` on Freeze, `aoe: "all"` on Lightning, `kind: "dot"` on Ice (P01) · row 32 `Lesser Summon` (`lvl 1, s: "special", kind: "summon", lesser: true, roll: "derived", combatOnly: false`) (P01) · `content/spell-level-overrides.js` Illusionist-only (P01).
Engine derived (`engine/derived.js`): `DAMAGE_SPELL_KINDS`, `dealsDamage(sp)` (P01) · `conditionsOf` keys `mirror`/`senses`/`regen`/`foresight` (P03) and `reveal` (P04).
Engine character (`engine/character.js`): `rollGrimoire` derived insertion (`derivedRng(cursor, "grimoire", sub)`), the Summoner `Lesser Summon` grant, the `dealsDamage` day-one walk (P01).
Engine magic (`engine/magic.js`): thrown branch reads `sp.aoe`/`sp.onHit`; `summon` branch reads `sp.lesser` (lvl `clamp(c.level-1, 1, 3)`, rounds `d4`, `LESSER_ALLY_NAMES`, no doubling, no backfire); new `dot` branch (`t.dot = { left: d4+1, dmg, by: "ice" }`, event `iceApplied`); `weaken` starts `c.timers["spell:weaken"]` (rounds `d4+1`, event `weakened { rounds }`); `stupid` sets `t.stupid` only (no asleep draw); `reveal` becomes the provenance-marking timed reveal (`spell:reveal`, 40 squares, event `floorMapped { squares, cells }`); `readScroll` aligned with `canCast` + event `scrollTooAdvanced { spell, need, have, school }` (P02/P03/P04).
Engine combat (`engine/combat.js`): `f.dot` ice payoff (`frozenSolid` + `killFoe` when an ice dot runs out on a live foe) (P02) · `spell:weaken` expiry clears `C.weakened`/`C.foeToHitPenalty` + event `weakenFaded` (P02) · `foeStupefied` skip-turn + `t.stupid` need-5 rule (P02) · `f.shrunk` halves foe melee damage at all three foe-damage lines (P02) · `allyCast` weaken starts the same timer (P02) · `rollInitiative` honours `c.senses` (no forced foe-first) (P03) · `endCombat` narrates `sensesFaded`/`regenFaded`/`mirrorFaded` when set (P03).
Engine maze/movement/save (P04): `engine/maze.js#reveal` clears `cell.spellSeen` (graduation), `engine/maze.js#refogSpellSeen(floor)`; `engine/movement.js` handles the `spell:reveal` effect→null transition (one sweep, event `revealFaded { cells }`), `descend` deletes the record; `engine/saveState.js#clearStaleSpellSeen(floor, c)`.
Events (new): `iceApplied`, `weakenFaded`, `foeStupefied` (P02) · `sensesFaded`, `regenFaded`, `scrollTooAdvanced` (P03) · `floorMapped`, `revealFaded` (P04; `detectMagic` retired). Additive payload fields: `weakened.rounds`, `allySpellHit.rounds` (weaken), `allySummoned.lesser`/`allyPending.lesser`, `dotTick.by` narration branch.
Parity harness (P04): `stripSpellSeen(floor)` in all three `*Comparable()` fns + the three local duplicates.
Bot (`tools/lib/tuning-bot.mjs`, P02): `onHit === "freeze"` kill tier, `aoe === "all"` multiplier, `kind === "dot"` in the damage tier.
Shell (P05): `viewModels.js#grimoireViewModel` rows carry `niche`; `combatMenu.js` spell rows carry `niche`; `mapMarks.js#MAP_PALETTE.floorSpell`; `mazeworld.html` CONDITION_COPY/TONE/EXPLAIN rows for mirror/senses/regen/foresight/reveal, Hero-tab kit rows (ward rounds, Mirror Self, Sense Presence, Map the Floor), `foeStatusBadges` rounds for Weakened/Ice/Poison, `draw()` spell-seen tint.
Docs: `docs/SPELLS.md` (P01 creates; P02–P05 append), `docs/USABLE-FEATURES-AUDIT.md` §2 rows (P01), `test/parity/FIXTURE-INVENTORY.md` Phase 40 sections (P01, P04).
Tests (new files): `test/unit/spell-table.test.js`, `test/unit/day-one-damage.test.js` (P01) · `test/unit/spell-mechanics.test.js` (P02) · `test/unit/spell-utility.test.js` (P03) · `test/unit/map-reveal.test.js` (P04) · `test/unit/shell-spells-40.test.js` (P05).
</artifacts_this_phase_produces>

<spell_table>
The 33-row table this plan authors (CONTEXT Area 2 rulings + Claude's discretion on wording/numbers — every number is repeated in docs/SPELLS.md). Array POSITION, `lvl`, `s`, `dmg`, `pool`/`rounds`/`reflect`, `combatOnly` of rows 0–31 are byte-identical to today (load-bearing: `castSpell(state, idx)` / the magic fixture's `spellIndex` / `readScroll`'s `SPELLS.indexOf` all index by position; `rollGrimoire`'s pools filter by `lvl`/`s`). Only `n` (row 5), `kind` (row 13), `txt` (every row), the new `niche` field (every row) and the three flags change; row 32 is appended.

`NICHE_LABELS` (exported, frozen): burst → "burst" · dot → "damage over time" · multi → "multi-target" · control → "control" · defensive → "defensive" · chaos → "chaos" · buff → "buff" · healing → "healing" · answer → "answer" · sight → "sight" · summon → "summon". `niche` holds the KEY; `txt` starts with `NICHE_LABELS[niche] + " · "`.

| idx | n | lvl | s | kind | niche | txt (final) | flags |
|---|---|---|---|---|---|---|---|
| 0 | Heal | 1 | healing | heal | healing | healing · you · d10 hp | |
| 1 | Shield | 1 | protection | ward | defensive | defensive · you · soaks 50 hp for 5 rounds | |
| 2 | Strength | 1 | offense | might | buff | buff · you · +d10 damage till tomorrow | |
| 3 | Doze | 1 | offense | status | control | control · one foe · asleep d4 rounds | |
| 4 | Freeze | 1 | offense | thrown | burst | burst · one foe · d6, and frozen solid on a hit | `onHit: "freeze"` |
| 5 | **Map the Floor** (was Detect Magic) | 1 | divination | reveal | sight | sight · the whole floor · mapped for 40 squares, then the map forgets what it was told | |
| 6 | Mirror Self | 1 | illusion | mirror | defensive | defensive · you · foes need a 1 to hit, d6 rounds | |
| 7 | Stun | 1 | offense | stun | control | control · up to d6 foes · asleep d4 rounds | |
| 8 | Weaken | 1 | offense | weaken | control | control · every foe · they hit on a 3 and do half, d4+1 rounds | |
| 9 | Acid | 2 | offense | acid | dot | damage over time · one foe · 2d6+2 a round, d6 rounds | |
| 10 | Stupidity | 2 | offense | stupid | control | control · one foe · does nothing at all for the rest of the fight | |
| 11 | Blind | 3 | offense | blind | control | control · one foe · blind for life, which in here means the fight | |
| 12 | Shrink | 3 | offense | shrink | control | control · up to d6 foes · half hp and half damage, the fight | |
| 13 | Ice | 3 | offense | **dot** (was thrown) | dot | damage over time · one foe · d6 a round for d4+1 rounds, then frozen solid | `dmg` stays `{1,6,0}` |
| 14 | Earthquake | 4 | offense | quake | multi | multi-target · every foe and you · 3d10+8, half to you unless warded | |
| 15 | Noxious Vapor | 4 | offense | vapor | chaos | chaos · every foe · a d6 of very bad outcomes | |
| 16 | Fireballs | 4 | offense | volley | multi | multi-target · d8 bolts · d10+2 each, spread across the foes | |
| 17 | Petrify | 5 | offense | petrify | control | control · one foe · stone for five days; no spoils | |
| 18 | Insane | 2 | offense | insane | chaos | chaos · one foe · rolls on the madness table | |
| 19 | Summon | 2 | special | summon | summon | summon · one ally · fights beside you d4+2 rounds | |
| 20 | Fireball | 3 | offense | thrown | burst | burst · one foe · 2d10+4 | |
| 21 | Major Heal | 3 | healing | heal | healing | healing · you · 3d10 hp | |
| 22 | Bubble | 3 | protection | ward | defensive | defensive · you · soaks 100 hp and reflects, 12 rounds | |
| 23 | Sense Danger | 3 | divination | foresee | sight | sight · the next encounter · names it before you meet it, and you act first | |
| 24 | Turn Walking Dead | 2 | protection | turn | answer | answer · every Walking Dead of your level or lower · sent back | |
| 25 | Plane Gate | 3 | protection | gate | answer | answer · d6 Demons or Walking Dead · vanquished to The Planes | |
| 26 | Sense Presence | 2 | protection | senses | sight | sight · you · fight in the dark at full skill and nothing gets the jump on you, till your next fight ends | |
| 27 | Phantom Host | 3 | illusion | summon | summon | summon · one ally · a host that isn't there, d4+2 rounds | |
| 28 | Lightning | 4 | offense | thrown | multi | multi-target · every foe · d10+6 each | `aoe: "all"` |
| 29 | Regeneration | 4 | healing | regen | healing | healing · you · d8 hp a round, this fight | |
| 30 | Mangle | 5 | offense | thrown | burst | burst · one foe · 2d20+15 | |
| 31 | Death | 5 | offense | death | burst | burst · one foe · dies outright; costs you 25 hp | |
| 32 | **Lesser Summon** (new) | 1 | special | summon | summon | summon · one small ally · a level under yours, d4 rounds, never backfires | `lesser: true, roll: "derived", combatOnly: false` (no `dmg`) |

Offense niche map by level (the SPELL-01 proof — `test/unit/spell-table.test.js` asserts "≥ 2 distinct niches at every offense level with ≥ 2 rows"): L1 buff/control/burst · L2 dot/control/chaos · L3 control/dot/burst · L4 multi/chaos · L5 control/burst. Noxious Vapor and Insane are labelled chaos (CONTEXT: "their gamble is the point"). Death is burst (the single-target ceiling); Petrify is control (a removal that pays no spoils — the trade-off against Death is stated in both `txt` lines).

Row 32's `roll: "derived"` is the flag `rollGrimoire` reads (Task 2): the row is learnable by every sub whose `special` school is open (Wizard, Sorcerer, Illusionist, Summoner, Apprentice) and joins their day-one pools at a derived-random position — never through the main rng.
</spell_table>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 33-row table — niche + one-line txt on every row, the rename, the three flags, Ice as dot, the Lesser Summon row; table tests; doc-sync</name>
  <files>content/spells.js, test/unit/spell-table.test.js, test/unit/content-tables.test.js, test/unit/magic.test.js, test/unit/casters-can-act.test.js, test/unit/grimoireViewModel.test.js, test/unit/spell-level-overrides.test.js, test/unit/tuning-bot.test.js, docs/USABLE-FEATURES-AUDIT.md</files>
  <read_first>
    - content/spells.js (whole file — the 32 rows and the header comment on `combatOnly`'s derivation)
    - `<spell_table>` above (the authoritative row spec)
    - test/unit/content-tables.test.js lines 37-40 and 123-145 (the 32-entry lock, the 12-non-combat list, the combatOnly pins)
    - test/unit/magic.test.js lines 285-300 (the Detect Magic test that names the row; keep it green by name only — its mechanic is Plan 04's)
    - test/unit/casters-can-act.test.js line 113, test/unit/grimoireViewModel.test.js lines 61-63 and 148, test/unit/spell-level-overrides.test.js line 133 and 163-175, test/unit/tuning-bot.test.js lines 643-647 (every test literal naming Detect Magic, and the `isAttackSpell` name lists)
    - test/unit/usable-features-audit.test.js lines 95-130 and 507-509 (every SPELLS row is cast in and out of combat with a seeded rng — a new row must succeed both ways; the doc must name every spell)
    - docs/USABLE-FEATURES-AUDIT.md lines 44-90 (the §2 spell table to update)
    - test/voice/safety-scan.test.js header (the spell `txt` fields are in the scanned corpus)
    - test/determinism/content-is-pure-data.test.js (content rows must stay plain JSON — no functions)
  </read_first>
  <behavior>
    - `SPELLS.length === 33`; `SPELLS[32].n === "Lesser Summon"` with `{ lvl: 1, s: "special", kind: "summon", lesser: true, roll: "derived", combatOnly: false, niche: "summon" }` and no `dmg`.
    - For every `i in 0..31`, `SPELLS[i].lvl`, `.s`, `.combatOnly`, `.dmg`, `.pool`, `.rounds`, `.reflect` deep-equal the pre-plan values (pin the pre-plan `[n, lvl, s, kind]` quadruples in the test as a literal 32-entry array, with the two deliberate exceptions asserted separately: `SPELLS[5].n === "Map the Floor"`, `SPELLS[13].kind === "dot"`).
    - `SPELLS[4].onHit === "freeze"`, `SPELLS[28].aoe === "all"`; no other row has `onHit`/`aoe`/`lesser`/`roll`.
    - Every row: `typeof sp.niche === "string"`, `NICHE_LABELS[sp.niche]` is a string, and `sp.txt.startsWith(NICHE_LABELS[sp.niche] + " · ")`.
    - Offense niche map: grouping `SPELLS.filter(s => s.s === "offense")` by `lvl`, every group with `length >= 2` has `new Set(group.map(s => s.niche)).size >= 2`.
    - No row's `txt` contains the substring " wp" (HP not WP in player copy).
    - `NICHE_LABELS` is frozen and has exactly the 11 keys listed in `<spell_table>`.
  </behavior>
  <action>
    Write `test/unit/spell-table.test.js` FIRST (RED) covering the behaviours above; pin the 32 pre-plan quadruples by reading them from `git show HEAD:content/spells.js` once and typing the resulting literal (measured, not hand-derived).

    content/spells.js: author every row per `<spell_table>` — add `niche` and rewrite `txt` on all 32 rows, rename row 5 to Map the Floor, add `onHit: "freeze"` (row 4), `aoe: "all"` (row 28), change row 13's `kind` to `"dot"` (its `dmg` literal stays), append row 32. Export `NICHE_LABELS` (Object.freeze) from the same module and re-export it through `content/index.js` if that file enumerates exports explicitly (check; if it uses `export *` nothing is needed). Extend the header comment: Phase 40 — the `niche`/`txt` contract (txt begins with the niche label), the four data flags and which engine site reads each (`onHit`/`aoe` → magic.js thrown branch, Plan 02; `lesser` → magic.js summon branch, Plan 02; `roll: "derived"` → character.js#rollGrimoire, this plan Task 2; `kind: "dot"` → magic.js dot branch + combat.js foeTurn, Plan 02), and the array-position/lvl/s invariants above. Content stays pure data.

    Update the name literals: content-tables.test.js (33 entries; the non-combat list becomes 13 names with "Lesser Summon" and "Map the Floor" replacing the old name, sorted), magic.test.js (the reveal test's grimoire/SPELL_IDX literal → "Map the Floor"; its mechanic assertions stay as they are — Plan 04 rewrites them), casters-can-act.test.js:113, grimoireViewModel.test.js:61-63/148 (row order re-sorted alphabetically: "Doze","Map the Floor","Weaken","Acid" at level 3 — re-derive from the view model's sort rule), spell-level-overrides.test.js:133 and its `isAttackSpell` lists (Ice moves from the true-list to the false-list — `dot` is deliberately NOT an attack kind, exactly as Acid never was; say so in the test comment), tuning-bot.test.js:643-647.

    docs/USABLE-FEATURES-AUDIT.md §2: retitle "32 rows" → "33 rows"; rename the Detect Magic row (event column unchanged until Plan 04); Ice's row → kind `dot`, combat column `iceApplied` (lands in Plan 02 — write the final value now, the doc-sync test only checks names); add the Lesser Summon row (`allyPending` / `allySummoned`); update the Summon row's level cell to plain `2` (the ★ Summoner note goes in Task 2's commit — do it here since this is the doc's only edit). Run the voice scan.
  </action>
  <verify>
    <automated>node --test test/unit/spell-table.test.js test/unit/content-tables.test.js test/unit/magic.test.js test/unit/casters-can-act.test.js test/unit/grimoireViewModel.test.js test/unit/spell-level-overrides.test.js test/unit/tuning-bot.test.js test/unit/usable-features-audit.test.js test/determinism/content-is-pure-data.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./content/index.js').then(m=>console.log(m.SPELLS.length, m.SPELLS[5].n, m.SPELLS[13].kind, m.SPELLS[4].onHit, m.SPELLS[28].aoe, JSON.stringify(m.SPELLS[32]), Object.keys(m.NICHE_LABELS).length))"` prints `33 Map the Floor dot freeze all {"n":"Lesser Summon","lvl":1,"s":"special","kind":"summon","lesser":true,"roll":"derived","niche":"summon","txt":"summon · one small ally · a level under yours, d4 rounds, never backfires","combatOnly":false} 11` (key order inside row 32 may differ; the JSON must contain exactly those eight keys).
    - `grep -c '"Detect Magic"' content/spells.js` prints 0 and `grep -c 'n: "Map the Floor"' content/spells.js` prints 1. <!-- planner-discipline-allow: "Detect Magic" -->
    - `grep -c "niche:" content/spells.js` prints 33.
    - `node -e "import('./content/index.js').then(m=>console.log(m.SPELLS.filter(s=>/ wp\b/.test(s.txt)).length))"` prints 0.
    - `node --test test/unit/spell-table.test.js` `# fail 0` with at least 8 tests; the listed suites `# fail 0`.
  </acceptance_criteria>
  <done>The table is the 33-row Phase 40 table with the niche contract machine-checked, and every name-literal test/doc is in sync.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: rollGrimoire — derived insertion of added rows, the Summoner's Lesser Summon grant, the damage-dealing day-one walk; override retirement; day-one proof; pin/test updates</name>
  <files>engine/character.js, engine/derived.js, content/spell-level-overrides.js, test/unit/day-one-damage.test.js, test/unit/chargen-rng-pin.test.js, test/unit/guaranteed-attack-spell.test.js, test/unit/spell-level-overrides.test.js, test/unit/spell-menu-mirror.test.js, test/unit/identity-contract.test.js, test/unit/casters-can-act.test.js, test/unit/grimoireViewModel.test.js</files>
  <read_first>
    - engine/character.js lines 290-375 (rollGrimoire in full — the frozen `dayOnePool` comment, `usableNow`, `ready`, the `spare` shuffle, the Phase 23 attack top-up with its Summoner guard) and lines 230-260 (rollPoolAbility — the derivedRng precedent)
    - engine/rng.js lines 80-115 (hashString/derivedRng) and engine/items.js lines 240-250 (`derivedRng(rng.getState(), "tool", depth)` — the keyed-on-the-main-cursor precedent this task copies)
    - engine/derived.js lines 1185-1260 (spellLevelFor, ATTACK_SPELL_KINDS, isAttackSpell, castableAttackSpells, canCast, bestAttackSpell)
    - content/spell-level-overrides.js (whole file)
    - test/unit/chargen-rng-pin.test.js (whole file — the three pin tables and the pool-derived formula in test 3)
    - test/unit/guaranteed-attack-spell.test.js (whole file — the old-algorithm mirror, the IDENT-02/IDENT-03 tests, the zero-draw proof, the seed 24/15 pins)
    - test/unit/identity-contract.test.js lines 129-155 (hero()) and 300-325 (the Summoner entry — the BAD stays byte-identical; the GOOD is rewritten)
    - test/unit/spell-level-overrides.test.js (whole file), test/unit/spell-menu-mirror.test.js lines 60-80, test/unit/casters-can-act.test.js lines 150-200, test/unit/grimoireViewModel.test.js lines 125-140 (every Summoner/Summon-at-level-1 pin)
    - .planning/phases/40-spell-rework/40-CONTEXT.md "Area 3" (the user's verbatim ruling and its Applied list)
  </read_first>
  <behavior>
    - `SPELL_LEVEL_OVERRIDES` deep-equals `{ Illusionist: { "Phantom Host": 1 } }`; `spellLevelFor("Summoner", Summon) === 2`; `spellLevelFor("Illusionist", PhantomHost) === 1`; `spellLevelFor("Summoner", LesserSummon) === 1` (its own printed level).
    - `DAMAGE_SPELL_KINDS` is exactly `{thrown, dot, acid, volley, quake, death}`; `dealsDamage(sp)` is true for Freeze/Ice/Acid/Fireball/Fireballs/Earthquake/Lightning/Mangle/Death and for Lesser Summon (`sp.lesser === true`), false for Doze/Stun/Weaken/Summon/Phantom Host/Heal/Shield/Strength/Map the Floor.
    - `rollGrimoire(rng, sub)` main-rng draw count per sub is UNCHANGED: Wizard 39, Warlock 33, Sorcerer 35, Summoner 31, Cleric 34, Illusionist 34, Court Mage 34, Apprentice 38 (the countingRng wrapper counts only the passed rng; the derived stream is a separate `makeRng`).
    - `ROLL_CHARACTER_PINS` and `NEW_RUN_PINS` in chargen-rng-pin.test.js are byte-identical to today and green.
    - For every seed, a Summoner's grimoire includes "Lesser Summon" (deterministic grant, zero draws) and still includes "Summon".
    - For every sub in CLASSES["Magic User"].subs and every seed in 1..200 (`newRun(seed, [], { force: { sub } })`): `state.c.grimoire.some(n => dealsDamage(byName(n)) && canCast(state, byName(n)))` is true. For the 5 special-school subs, over the same 200 seeds, "Lesser Summon" appears in at least one non-Summoner grimoire and is absent from at least one (the derived insertion genuinely varies).
    - No grimoire has duplicate names, across all 8 subs × 200 seeds.
    - `castableAttackSpells(summonerState)` is still `[]` at level 1 (offense gate 3 — the BAD); `canCast(summonerState, Summon)` is now `false` at level 1 and `true` at level 2.
    - A `fakeRng`-style rng without `getState` still works (the derived key falls back to cursor 0).
  </behavior>
  <action>
    Write `test/unit/day-one-damage.test.js` FIRST (RED) covering the behaviours above (reuse chargen-rng-pin's `countingRng` shape for the draw-count assertion; use `newRun(seed, [], { force: { sub } })` for the proof loop).

    content/spell-level-overrides.js: delete the Summoner row; rewrite the header — Phase 40 (SPELL-04, user ruling 2026-09-18 "Give the summoner a level 1 summon … keep level 1 spells without the bad gate"): the Summoner's level-1 summon is now its own row (Lesser Summon, content/spells.js row 32), so Summon is spell level 2 for every sub again; the Illusionist's Phantom Host override is unchanged. Note that PROJECT.md's Phase 23 row gains a "superseded by Phase 40" note at phase close (Plan 05).

    engine/derived.js: after `ATTACK_SPELL_KINDS`/`isAttackSpell`, add `export const DAMAGE_SPELL_KINDS = new Set(["thrown", "dot", "acid", "volley", "quake", "death"])` and `export function dealsDamage(sp)` returning `DAMAGE_SPELL_KINDS.has(sp.kind) || sp.lesser === true`, with a JSDoc stating the deliberate exclusions: Summon/Phantom Host do NOT count (ROADMAP SC-3 "additionally qualify" — the overrides are not the guarantee); Doze/Stun/Weaken do not count (research finding); `ATTACK_SPELL_KINDS` is untouched (it gates the Wizard melee refusal and member casts — a different question).

    engine/character.js#rollGrimoire (import `derivedRng` is already present; import `dealsDamage` from derived.js): (a) `pool` stays `SPELLS.filter(canLearn)`; split it into `rolled = pool.filter(sp => sp.roll !== "derived")` and `added = pool.filter(sp => sp.roll === "derived")`; build `low`/`high` from `rolled` and shuffle them exactly as today; draw `n` as today. (b) Create ONE derived stream right after the `n` draw: `const dr = derivedRng(typeof rng.getState === "function" ? rng.getState() : 0, "grimoire", sub)`. (c) For each `sp` of `added` with `lvl <= 2`, splice it into `low` at index `dr.d(low.length + 1) - 1` (and the `lvl > 2` analog into `high` — none exist today, keep the loop symmetric); THEN run the unchanged book-fill loops. (d) Grants: keep the four existing lines; add `if (sub === "Summoner" && !book.includes("Lesser Summon")) book.push("Lesser Summon");` directly after the Summon grant (zero draws, deterministic — the user's "give the summoner a level 1 summon"). (e) `dayOnePool` stays byte-identical; `spare = rolled.filter(dayOnePool)`; shuffle as today; THEN splice each `added` row that satisfies `dayOnePool` into `spare` at `dr.d(spare.length + 1) - 1`. (f) The two-usable-now loop is unchanged (`usableNow` still routes through `spellLevelFor`, so Summon no longer counts for a Summoner and Lesser Summon does). (g) Replace the Phase 23 attack top-up (its `attackReady` closure, the `sub !== "Summoner"` guard and its comment block) with the damage walk: `damageReady = () => book.some(n2 => usableNow(byName(n2)) && dealsDamage(byName(n2)))`, then `for (const sp of spare) { if (damageReady()) break; if (dealsDamage(sp) && !book.includes(sp.n)) book.push(sp.n); }` — no sub exemption. Rewrite the JSDoc: the ZERO-DRAW GUARANTEE now reads "main-rng draws unchanged; derived rows enter at a derived-random position with the same odds as a shuffled row; the Summoner's Lesser Summon is a grant; the day-one guarantee is DAMAGE (Freeze, or Lesser Summon), not attack-kind" — DELIBERATE RULES CHANGE (Phase 40, 2026-09-18, SPELL-04). Keep the frozen-`dayOnePool` comment (still true; add one sentence: derived rows are placed AFTER the shuffle so its length is unchanged).

    test/unit/chargen-rng-pin.test.js: ONLY the third test's `pool` line changes — `SPELLS.filter(sp => canLearn(sub, sp) && sp.roll !== "derived")` — with a comment: Phase 40 rows flagged `roll: "derived"` never enter the main-rng shuffles (engine/character.js#rollGrimoire), so the formula excludes them; the three pin tables are unchanged. Do not touch any pin value.

    test/unit/guaranteed-attack-spell.test.js: IDENT-02's assertion becomes "every sub (including the Summoner) has a castable `dealsDamage` spell" (drop `NON_SUMMONER_SUBS` where it only served the exemption); the IDENT-03 Summoner test becomes "Summon is level 2 again (`canCast` false at level 1), Lesser Summon is granted and castable, `castableAttackSpells` stays `[]`"; the `oldAlgorithm` mirror stays as the pre-Phase-23 reference and its adjacency test's tolerance is raised to the MEASURED maximum difference (a derived row landing in the book is a swap = 2, plus the Summoner grant and the damage walk — expect ≤ 4; measure over the suite's seeds and pin that number with the reason in the assertion message); the zero-draw proof stays as-is (it must pass unchanged); re-measure the seed 24 / seed 15 pins against the finished engine and pin the measured grimoires (never hand-typed).

    test/unit/spell-level-overrides.test.js: the "exactly two rows" test → exactly one (Illusionist); the diff walk's changed-cell list → the Illusionist cells only; the `castableAttackSpells` Summoner test keeps asserting `[]`. test/unit/spell-menu-mirror.test.js:66-70 → the Summoner/Summon cell agrees on `false` at level 1 (and add a `true` at level 2 or a Lesser Summon `true` at level 1). test/unit/identity-contract.test.js Summoner GOOD → name "a small, safe summon on day one — Lesser Summon is guaranteed and castable at level 1": assert `newRun(seed, [], {force:{sub:"Summoner", race:"Human"}}).c.grimoire.includes("Lesser Summon")` for seeds 1..25, `canCast(state, LesserSummon) === true`, and that casting it out of combat emits `allyPending` and never `spellAboveLevel`/`spellSchoolLocked` (in this plan the cast still runs the ordinary summon branch — a Summoner draws the d8 backfire check first — so feed a `fakeRng` whose first value is not 1, e.g. `[5, 3, 1]`; Plan 02 re-pins this cast to the lesser numbers) — the BAD entry stays byte-identical. test/unit/casters-can-act.test.js IDENT-03 tests → a level-1 Summoner casting Summon gets `spellAboveLevel { need: 2, have: 1 }`; a level-2 Summoner casts it with the doubled formula (same fakeRng sequence, level 2 → lvl min(5,3)=3, rounds 2*3+2=8). test/unit/grimoireViewModel.test.js:128-135 → a level-1 Summoner's Summon row reads `Needs level 2`; a Lesser Summon row is castable.
  </action>
  <verify>
    <automated>node --test test/unit/day-one-damage.test.js test/unit/chargen-rng-pin.test.js test/unit/guaranteed-attack-spell.test.js test/unit/spell-level-overrides.test.js test/unit/spell-menu-mirror.test.js test/unit/identity-contract.test.js test/unit/casters-can-act.test.js test/unit/grimoireViewModel.test.js test/unit/character.test.js test/determinism/forced-chargen.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `git diff f14a8f2 -- test/unit/chargen-rng-pin.test.js | grep -c "^[-+] *[0-9]*: *-\?[0-9]"` prints 0 (no pin VALUE line changed anywhere in the file) and `node --test test/unit/chargen-rng-pin.test.js` `# fail 0`.
    - `node -e "import('./content/index.js').then(m=>console.log(JSON.stringify(m.SPELL_LEVEL_OVERRIDES)))"` prints `{"Illusionist":{"Phantom Host":1}}`.
    - `grep -c 'sub !== "Summoner"' engine/character.js` prints 0; `grep -c "dealsDamage" engine/character.js` is at least 2 (import + use); `grep -c 'derivedRng(' engine/character.js` is at least 2 (rollPoolAbility + rollGrimoire). <!-- planner-discipline-allow: sub !== "Summoner" -->
    - `grep -c "export const DAMAGE_SPELL_KINDS\|export function dealsDamage" engine/derived.js` prints 2.
    - `node -e "import('./engine/engine.js').then(async m=>{const d=await import('./engine/derived.js');const c=await import('./content/index.js');const by=n=>c.SPELLS.find(s=>s.n===n);let bad=0;for(const sub of c.CLASSES['Magic User'].subs)for(let s=1;s<=200;s++){const st=m.newRun(s,[],{force:{sub}});if(!st.c.grimoire.some(n=>d.dealsDamage(by(n))&&d.canCast(st,by(n))))bad++;}console.log(bad)})"` prints `0`.
    - `node -e "import('./engine/engine.js').then(m=>console.log([1,2,3,4,5].every(s=>m.newRun(s,[],{force:{sub:'Summoner'}}).c.grimoire.includes('Lesser Summon'))))"` prints `true`.
    - `node --test test/unit/day-one-damage.test.js` `# fail 0` with at least 8 tests; every listed suite `# fail 0`.
    - The Summoner BAD entry body is byte-identical: `git diff f14a8f2 -- test/unit/identity-contract.test.js | grep -c "offense gate of 3"` prints 0.
  </acceptance_criteria>
  <done>Summon is level 2 for everyone, the Summoner is guaranteed Lesser Summon, every Magic User sub provably starts with a damage-dealing spell, and the main-rng cursor never moved.</done>
</task>

<task type="auto">
  <name>Task 3: Measure and declare the fixture moves; FIXTURE-INVENTORY Phase 40 section; create docs/SPELLS.md; plan gate</name>
  <files>test/parity/fixtures/action-script.chargen.json, test/parity/fixtures/action-script.magic.json, test/parity/fixtures/action-script.combat.json, test/parity/FIXTURE-INVENTORY.md, docs/SPELLS.md</files>
  <read_first>
    - test/parity/FIXTURE-INVENTORY.md lines 248-330 (the Phase 23 chargen/magic divergence sections — the format to mirror) and 725-800 (Phase 38's chargenDivergence table — the scenario-level record shape)
    - test/parity/harness/comparables.js lines 860-945 (chargenDivergenceFor, chargenShiftOf, stripChargenShift, chargenShiftDiffs, stripDeclaredFields) and 640-700 (stripScenarioDivergence, actionPathDivergenceOf, skipsByteDiffAt)
    - test/parity/fixtures/action-script.schema.md (the record shapes: fixture-level `divergences` map, scenario-level `chargenDivergence`, `divergence` kind "action-path")
    - test/parity/fixtures/action-script.chargen.json `divergences["15"]`/`["24"]` (the Phase 23 records this plan re-measures — `after` changes, `before` never), action-script.magic.json (`spellIndex`, the four scenarios, `cast-damage`'s action-path record), action-script.combat.json `lose-apprentice` (seed 127)
    - test/parity/chargen-parity.test.js lines 100-215 and test/parity/magic-parity.test.js lines 150-291 (how each site applies the records)
    - docs/GEAR-BALANCE.md lines 1-60 and 361-376, docs/ABILITIES.md lines 1-35 and 146-160 (ledger head + declared-divergences section shapes to mirror)
    - engine/magic.js lines 468-514 (readScroll — `options = SPELLS.filter(lvl <= depth+1)` then `rng.pick`: the appended row lengthens `options` from 15 to 16 at depth 1, so the `scroll` scenario's pick may move)
  </read_first>
  <action>
    1. MEASURE (never reason from seeds): run `npm test` and list every failing parity scenario. Expected moved set — confirm or correct by measurement: chargen seeds 7 (Wizard: damage walk / derived row), 8 (Illusionist: derived row), 15 (Summoner: Lesser Summon grant; Summon no longer usable-now), 19 (Sorcerer: derived row), 24 (Apprentice: rename + derived row), 29 (Warlock: rename only — no special school); magic `heal` and `scroll` (seed 7, the same Wizard grimoire; `scroll` additionally re-picks from a 16-row option list — if the picked spell changes, the scenario becomes an action-path record from action 0 with measured end fields), magic `cast-damage` (seed 8: grimoire only — the Freeze cast at idx 4 is byte-identical), combat `lose-apprentice` (seed 127: rename + damage walk; the attack-only path is unchanged). Seeds 35 (Cleric) and everything non-caster are expected byte-identical (state why in the inventory). For each moved seed/scenario, measure `before` = `loadPrototypeSandbox({seed}).S.c.grimoire` and `after` = `newRun(seed).c.grimoire` with a scratch script under the session scratchpad; for `scroll`, replay both sides and record the measured end-state fields.

    2. DECLARE: chargen seeds → the fixture-level `divergences` map (`fields: ["grimoire"]`, `phase: 40`, `requirements: ["SPELL-03","SPELL-04"]` (+ "SPELL-05" for the rename seeds), measured `before`/`after`, one-sentence rationale naming the cause — rename / grant / damage walk / derived insertion; seeds 15 and 24 UPDATE their existing records' `after`/`phase`/`requirements`/`rationale` and keep `before`); scenario holders (magic heal/scroll/cast-damage, combat lose-apprentice) → a `chargenDivergence` record (Phase 38 shape) beside any existing `divergence`; `scroll` additionally gets an action-path `divergence` only if measurement shows the pick moved. Never edit `seed`/`actions`/`scenarios`/`before`.

    3. FIXTURE-INVENTORY.md: append "## Phase 40: spell table reshape + day-one damage (SPELL-01/03/04) — declared grimoire divergences" with (a) the mechanism paragraph (derived insertion keyed on the main cursor; the Summoner grant; the damage walk; the override retirement; the rename), (b) the table `| Fixture | Scenario / seed | Hero | grimoire before | grimoire after | Cause |` with one row per record, (c) the rngState sentence: every seed's `rollCharacter`/`newRun` cursor is unchanged (`test/unit/chargen-rng-pin.test.js` pin tables unedited), (d) "Byte-identical elsewhere (Phase 40, Plan 01)".

    4. CREATE docs/SPELLS.md (the Phase 40 ledger; the shape of docs/ABILITIES.md/docs/GEAR-BALANCE.md): "# Spells (Phase 40 ledger)" · intro paragraph (what the phase is; the five plans) · "## Canon change" (quote the prototype's spell list as the canon; the user's Area 3 ruling VERBATIM; the ROADMAP goal; the once-a-day rule) · "## The table — before / after" (all 33 rows: idx, name before→after, lvl, school, kind before→after, txt before→after, flags — transcribe the final `txt` verbatim from content/spells.js) · "## Niche map by level (offense)" (the `<spell_table>` map and the ≥2-niches rule, naming `test/unit/spell-table.test.js`) · "## Day-one grimoire (SPELL-04)" (derived insertion — why not a lengthened shuffle: a main-pool add would re-order floor generation for the five special-school subs and break the initial-boot-state parity of the heal/scroll/cast-damage/lose-apprentice scenarios, which no existing declaration mechanism covers; the Summoner grant; the damage walk; `dealsDamage`'s exclusions; the override retirement; the per-sub day-one pools with the guaranteed source, from the measured `spare` lists) · "## Declared divergences (Plan 01)" (the same table as FIXTURE-INVENTORY, one line each) · "## Corrections to CONTEXT (verified in code)": (i) the chargen pin does not move — the derived stream keeps every cursor; (ii) Shrink is up-to-d6-foes in code (`liveFoes().slice(0, d6)`) and stays multi-target so level 3 has single (Blind) vs multi (Shrink) control — CONTEXT's axis list wrote "single"; (iii) Sense Presence's "never surprised" had no engine read before this phase (only the dark to-hit cap) — Plan 03 gives it the initiative meaning; (iv) Ice's DOT keeps the canon d6 a round and gains the promised freeze at the end (Plan 02) · then five placeholder H2s each with the single line "(appended by Plan NN)": "## Offense mechanics (Plan 02)", "## Utility visibility and scrolls (Plan 03)", "## Map the Floor — Key Decision: re-fog provenance (Plan 04)", "## UI (Plan 05)", "## Requirements map (Plan 05)".

    5. PLAN GATE (record every number in the SUMMARY; BASE = the commit this plan started from, `f14a8f2` unless the SUMMARY records otherwise): `npm test` → `# fail 0` (record `# tests`; expect > 2686); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `node --test test/unit/chargen-rng-pin.test.js` green; `git status --porcelain` shows nothing untracked under `test/parity/fixtures`; `git log --stat BASE..HEAD -- store-listing tools/store-screenshots` prints nothing; `git diff BASE -- test/parity/fixtures | grep -c '^-.*"before"'` prints 0 (no pre-existing `before` value edited).

    6. SUMMARY: standard shape plus "## Declared divergences" (the measured table) and "## Human verification (deferred to end of run)" — Plan 01 items: (1) Hero-tab Grimoire rows read as one niche line each ("burst · one foe · d6, and frozen solid on a hit"); (2) a fresh Summoner's grimoire lists Lesser Summon (castable) and Summon (Needs level 2); (3) Detect Magic no longer appears anywhere — the row reads Map the Floor.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && node --test test/parity/chargen-parity.test.js test/parity/magic-parity.test.js test/parity/combat-parity.test.js test/parity/full-suite.test.js 2>&1 | grep -E "^# (tests|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0` and `# tests` greater than 2686.
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `node -e "const j=require('./test/parity/fixtures/action-script.chargen.json');console.log(Object.entries(j.divergences).filter(([k,v])=>String(v.phase).includes('40')).map(([k])=>k).join(','))"` prints a comma list that includes `7,8,15,19,24,29` (measurement may add or drop a seed — the SUMMARY explains any difference from this expected set).
    - `node -e "const j=require('./test/parity/fixtures/action-script.magic.json');console.log(j.scenarios.filter(s=>s.chargenDivergence).map(s=>s.name).join(','))"` prints `cast-damage,heal,scroll` (or the measured subset, explained in the SUMMARY).
    - `grep -c "## Phase 40: spell table reshape" test/parity/FIXTURE-INVENTORY.md` prints 1.
    - `test -f docs/SPELLS.md` and `grep -c "^## " docs/SPELLS.md` is at least 11; `grep -c "(appended by Plan" docs/SPELLS.md` prints 5.
    - `grep -c "Give the summoner a level 1 summon" docs/SPELLS.md` prints 1 (the verbatim ruling).
    - `git diff f14a8f2 -- test/parity/fixtures | grep -c '^-.*"before"'` prints 0 (every pre-existing `before` value is unchanged; only `after`/new records differ).
  </acceptance_criteria>
  <done>Every fixture the table/chargen changes moved is measured and declared, the inventory and the new ledger record it, and the plan gate numbers are in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content → engine | `roll`/`lesser`/`onHit`/`aoe` flags read by rules code; a malformed row must not throw |
| save → engine | an old grimoire containing "Detect Magic" (the old name) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | old save grimoire with the retired name | low | mitigate | `grimoireViewModel`/`combatMenu` already `.find(...)` + `.filter(Boolean)` an unknown name (dropped silently, never thrown); Plan 04's tolerant-load task maps the retired name to "Map the Floor" in `validateSave` (one string replace, no card) — recorded here, lands in Plan 04 |
| T-40-02 | Denial of service | derived insertion with an rng lacking `getState` | low | mitigate | cursor falls back to 0 — deterministic, never throws (tested) |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan (the gate's `package.json` diff check in Plan 05) |
</threat_model>

<verification>
- Tasks 1–2 suites green; Task 3 gate numbers recorded; chargen-rng-pin pin tables unedited (diff proof in the acceptance criteria).
- docs/SPELLS.md exists with the five placeholder sections for Plans 02–05.
</verification>

<success_criteria>
- ROADMAP SC-1's table half (niche line per spell, ≥ 2 niches per offense level) and SC-3 (every sub starts with a damage spell; Summoner via Lesser Summon, Illusionist keeps Phantom Host and additionally qualifies) are machine-checked; SPELL-03's declared-divergence discipline is applied to every moved fixture.
</success_criteria>

<output>
Create `.planning/phases/40-spell-rework/40-01-SUMMARY.md` when done (standard shape + "## Declared divergences" + "## Human verification (deferred to end of run)").
</output>
