---
phase: 40-spell-rework
plan: 05
type: execute
wave: 5
depends_on: ["40-04"]
files_modified:
  - src/browser/viewModels.js
  - src/browser/combatMenu.js
  - src/browser/mapMarks.js
  - mazeworld.html
  - test/unit/shell-spells-40.test.js
  - test/unit/grimoireViewModel.test.js
  - test/unit/combatMenu.test.js
  - test/unit/mapMarks.test.js
  - test/unit/conditions.test.js
  - test/unit/shell-map-hud.test.js
  - docs/SPELLS.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SPELL-01, SPELL-02, SPELL-05, SPELL-06, SPELL-07]

must_haves:
  truths:
    - "Every grimoire surface shows the one-line niche text: the Hero-tab Grimoire rows and the combat SPELLS submenu rows both render `sp.txt` (which begins with the niche label) and their view-model rows carry `niche` + `nicheLabel` so a test can prove it"
    - "An active Shield/ward is visible outside combat on BOTH surfaces: the map-HUD condition chip (pool + rounds — already emitted by conditionsOf, proven with `state.combat === null`) and the Hero-tab kit row, which now reads pool AND rounds (SPELL-06)"
    - "The new chips render with copy: Mirrored (rds), Senses, Regenerating, Forewarned, Mapped (sq) — each with a CONDITION_COPY row, a CONDITION_TONE, a CONDITION_EXPLAIN sentence; the Hero-tab kit lists Mirror Self / Sense Presence / Sense Danger (armed) / Map the Floor rows while they are up"
    - "The map paints spell-revealed cells (`cell.spellSeen`) in a distinct 'borrowed sight' tint so the player can see what will re-fog; after the sweep those cells paint as fog (draw() reads `seen` fresh)"
    - "Foe badges carry their durations: 'Weakened · N' reads the hero's `spell:weaken` record, an `f.dot` badge reads 'Ice · N' / 'Poison · N'"
    - "The phase closes: docs/SPELLS.md UI + requirements-map sections filled, SPELL-01..07 marked complete, the whole-phase gate recorded, the aggregated Pixel 7 checklist (Plans 01–05, numbered continuously) in the SUMMARY, and the two PROJECT.md call-outs (the re-fog Key Decision; the Phase 23 Summoner override superseded) stated for the orchestrator"
  artifacts:
    - "src/browser/viewModels.js — grimoireViewModel rows { niche, nicheLabel }"
    - "src/browser/combatMenu.js — spell rows { niche, nicheLabel }"
    - "src/browser/mapMarks.js — MAP_PALETTE.floorSpell"
    - "mazeworld.html — chip copy rows, Hero-tab kit rows, foeStatusBadges durations, draw() spell-seen tint"
    - "test/unit/shell-spells-40.test.js"
    - "docs/SPELLS.md — UI + Requirements map + Out of scope sections"
  key_links:
    - "content/spells.js niche/txt -> viewModels.js#grimoireViewModel + combatMenu.js spell rows -> mazeworld.html renderGrimoire / the SPELLS submenu"
    - "engine/derived.js#conditionsOf (ward/mirror/senses/regen/foresight/reveal) -> mazeworld.html paintConditions via window.__mzConditionsOf -> CONDITION_COPY/TONE/EXPLAIN"
    - "cell.spellSeen -> mazeworld.html draw() -> MAP_PALETTE.floorSpell"
---

<objective>
The shell half and the phase close: put the niche line, the new chips, the Hero-tab ward rounds and spell rows, the foe-badge durations, and the spell-seen map tint on screen; then close the ledger, mark SPELL-01..07 complete, run the whole-phase gate, and aggregate the Pixel 7 checklist for the milestone-close UAT batch (deferred UAT).

Purpose: SPELL-06 is a pure shell fix (the chip already exists; the Hero-tab row lacked rounds); SPELL-02's chips need copy rows; SPELL-05's window needs to be legible on the map; SPELL-01's niche line must be where the player reads spells.

Output: `viewModels.js`/`combatMenu.js`/`mapMarks.js` changes with tests, the `mazeworld.html` edits with source pins, `docs/SPELLS.md` closed, `.planning/REQUIREMENTS.md` updated, the gate numbers and the aggregated checklist in the SUMMARY. This is the ONLY plan that edits `mazeworld.html`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-spell-rework/40-CONTEXT.md
@.planning/phases/40-spell-rework/40-04-SUMMARY.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-05-SUMMARY.md
@src/browser/viewModels.js
@src/browser/combatMenu.js
</context>

<standing_rules>
- Greenfield (CONTEXT "Standing rulings"): no shell-only options; the classic-script duplicates in mazeworld.html (the classic `SPELLS` table, `castSpell`, `rollGrimoire`) are dead code — leave them for the cleanup milestone, exactly as Phases 34–39 did; say so in the SUMMARY.
- Rail is the one feedback surface: effects/expiries are narrative lines (already wired by Plans 02–04); no new cards in this plan.
- UI rulings (2026-09-17 device round): PNG icons canon; HP not WP in player copy; DOM built with createElement/textContent for any string that carries a name (T-38-11 precedent).
- `test/parity/prototype-master.js.txt` never edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`). Expected fixture regenerations: NONE (shell-only + docs).
- Commit convention `feat(40-05): …` / `test(40-05): …` / `docs(40-05): …` + the two attribution lines (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`); explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT: no device steps, no `adb`, no APK build, no pause. This plan's SUMMARY carries the PHASE's aggregated Pixel 7 checklist (grouped by plan, numbered continuously, de-duplicated), in the 39-05-SUMMARY format.
- Verification agents are off: Task 3 is the WHOLE-PHASE gate.
- PROJECT.md is the orchestrator's: this plan does NOT edit it; the SUMMARY states the two call-outs.
</standing_rules>

<artifacts_this_phase_produces>
See 40-01-PLAN.md `<artifacts_this_phase_produces>`. This plan creates every P05 entry: `grimoireViewModel` rows `{ niche, nicheLabel }`, `combatMenu.js` spell rows `{ niche, nicheLabel }`, `MAP_PALETTE.floorSpell`, mazeworld.html CONDITION_COPY/TONE/EXPLAIN rows for `mirror`/`senses`/`regen`/`foresight`/`reveal`, the Hero-tab kit rows (ward rounds; Mirror Self; Sense Presence; Sense Danger armed; Map the Floor), `foeStatusBadges` durations (Weakened · N; Ice · N / Poison · N), `draw()`'s spell-seen tint, `test/unit/shell-spells-40.test.js`, the ledger's UI + Requirements map + Out of scope sections, REQUIREMENTS.md SPELL-01..07 complete.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure modules first — niche on both spell-row view models, the map palette tint, the outside-combat ward-chip proof</name>
  <files>src/browser/viewModels.js, src/browser/combatMenu.js, src/browser/mapMarks.js, test/unit/grimoireViewModel.test.js, test/unit/combatMenu.test.js, test/unit/mapMarks.test.js, test/unit/conditions.test.js</files>
  <read_first>
    - src/browser/viewModels.js lines 415-482 (grimoireViewModel — the row shape and sort)
    - src/browser/combatMenu.js lines 118-160 (the SPELLS submenu rows: `id/label/cost/desc/enabled/dispatch`)
    - src/browser/mapMarks.js lines 20-40 (MAP_PALETTE — frozen; the floor/floorDark/floorInset keys)
    - content/spells.js (`NICHE_LABELS` export from Plan 01)
    - test/unit/grimoireViewModel.test.js lines 60-80 (the row-shape pin), test/unit/combatMenu.test.js lines 140-170 (the spell-row pins), test/unit/mapMarks.test.js lines 45-60 (the palette pins), test/unit/conditions.test.js (the ward tests — add the explicit `combat: null` case for SPELL-06)
  </read_first>
  <behavior>
    - `grimoireViewModel(state).rows[i]` carries `niche` (the key) and `nicheLabel` (`NICHE_LABELS[niche]`) beside the existing fields; `txt` is unchanged (it already begins with `nicheLabel + " · "`); a test asserts `row.txt.startsWith(row.nicheLabel + " · ")` for every row of a grimoire holding one spell of each niche.
    - `combatMenu.js` spell rows carry the same two fields; `desc` is still `sp.txt`; every other field byte-identical (the existing deepEqual pins are extended, not loosened).
    - `MAP_PALETTE.floorSpell` is a hex string distinct from `floor`/`floorDark`/`fog` (choose a cool "borrowed sight" tint, e.g. `#4e5a6a`), and `MAP_PALETTE` stays frozen with every existing key/value unchanged.
    - SPELL-06 proof: `conditionsOf({ c: { ward: { pool: 34, rounds: 3, name: "Shield" } }, combat: null })` contains `{ key: "ward", polarity: "good", pool: 34, remaining: 3, name: "Shield" }` (outside combat, both numbers).
  </behavior>
  <action>
    Extend the four test files FIRST (RED). viewModels.js: import `NICHE_LABELS` from ../../content/index.js; add `niche: sp.niche, nicheLabel: NICHE_LABELS[sp.niche] ?? sp.niche` to the row literal; JSDoc line. combatMenu.js: the same two fields on each spell row. mapMarks.js: add `floorSpell` to MAP_PALETTE with a comment (Phase 40 SPELL-05: a spell-revealed cell — will re-fog when the window ends). conditions.test.js: the explicit no-combat ward case.
  </action>
  <verify>
    <automated>node --test test/unit/grimoireViewModel.test.js test/unit/combatMenu.test.js test/unit/mapMarks.test.js test/unit/conditions.test.js test/unit/spell-menu-mirror.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "nicheLabel" src/browser/viewModels.js src/browser/combatMenu.js` is at least 1 for each file; `grep -c "NICHE_LABELS" src/browser/viewModels.js src/browser/combatMenu.js` is at least 2 for each (import + use).
    - `node -e "import('./src/browser/mapMarks.js').then(m=>console.log(typeof m.MAP_PALETTE.floorSpell, Object.isFrozen(m.MAP_PALETTE), m.MAP_PALETTE.floor))"` prints `string true #645c48`.
    - Every listed suite `# fail 0`.
  </acceptance_criteria>
  <done>Both spell-row view models expose the niche, the palette has the tint, and the outside-combat ward chip is proven.</done>
</task>

<task type="auto">
  <name>Task 2: mazeworld.html — chip copy rows, Hero-tab ward rounds + spell rows, foe-badge durations, spell-seen map tint; source pins; build</name>
  <files>mazeworld.html, test/unit/shell-spells-40.test.js, test/unit/shell-map-hud.test.js</files>
  <read_first>
    - mazeworld.html lines 2886-2925 (CONDITION_COPY), 2988-2995 (CONDITION_TONE), 2995-3023 (CONDITION_EXPLAIN + explainCondition), 3044-3110 (paintConditions — the generic `remaining` + unit branch the new chips ride), 3225-3245 (the Hero-tab kit rows incl. `if (c.ward) rows.push([c.ward.name, …])`), 5332-5352 (foeStatusBadges), 2661-2700 (draw()'s floor fill), 1368-1372 (the __mzConditionsOf bridge comment)
    - src/browser/mapMarks.js (MAP_PALETTE.floorSpell from Task 1; the draw() fallback palette literal `P` must ALSO gain `floorSpell` so a missing module never paints undefined)
    - test/unit/shell-gear-39.test.js (the fs.readFileSync + stripComments + sliceBetween source-pin pattern to copy), test/unit/shell-map-hud.test.js (existing chip-copy pins that may enumerate CONDITION_TONE keys)
    - .planning/phases/39-gear-magic-items-one-shot-tools/39-05-SUMMARY.md "key-decisions" (explainCondition's item-vs-plain rule; the createElement discipline)
  </read_first>
  <action>
    mazeworld.html (classic script; Phase 40 comments on each edit):
    1. CONDITION_COPY: `mirror: { label: "Mirrored", unit: "rds" }`, `senses: { label: "Senses" }`, `regen: { label: "Regenerating" }`, `foresight: { label: "Forewarned" }`, `reveal: { label: "Mapped", unit: "sq" }`. CONDITION_TONE: mirror "good", senses "good", regen "good", foresight "odd", reveal "odd". CONDITION_EXPLAIN: mirror "They swing at a reflection for a few rounds. Try not to look smug."; senses "You fight in the dark at full skill and nothing gets the jump on you, until this fight ends."; regen "Wounds close on their own every round of this fight. It is not a licence."; foresight "You already know what the next encounter is. Whether that helps is up to you."; reveal "The floor is on loan. When the squares run out, the parts you never walked go dark again.". The generic detail branch renders `remaining` with the unit — no special case needed.
    2. Hero-tab kit rows: the ward row becomes `[c.ward.name, `${c.ward.pool} hp left · ${c.ward.rounds} rds`]` (SPELL-06); add `if (c.mirror > 0) rows.push(["Mirror Self", `${c.mirror} rds`]);`, `if (c.senses) rows.push(["Sense Presence", "till the fight ends"]);`, `if (c.foresight) rows.push(["Sense Danger", "armed"]);`, and `const rev = c.timers && c.timers["spell:reveal"]; if (rev && rev.phase === "effect" && rev.left > 0) rows.push(["Map the Floor", `${rev.left} sq`]);` — placed after the Regeneration row.
    3. foeStatusBadges: the Weakened badge reads the hero's record — `const wk = S.c.timers && S.c.timers["spell:weaken"]; out.push({ t: wk && wk.left > 0 ? `Weakened · ${wk.left}` : "Weakened", tone: "good" })`; add `if (f.dot && f.dot.left > 0) out.push({ t: `${f.dot.by === "ice" ? "Ice" : "Poison"} · ${f.dot.left}`, tone: "acid" });` beside the Acid badge.
    4. draw(): the floor fill becomes `ctx.fillStyle = c.spellSeen ? P.floorSpell : (c.dark ? P.floorDark : P.floor);` and the fallback `P` literal gains `floorSpell: "#4e5a6a"` (the same hex as mapMarks.js — pin both equal in the test).
    5. Grimoire rows: no markup change (the `<i>` already renders `row.txt`, which begins with the niche label) — add a one-line comment above `info.innerHTML` noting the Phase 40 niche contract; keep innerHTML only because `row.txt`/`row.name` come from content (not user data) exactly as before this plan.

    Write `test/unit/shell-spells-40.test.js` (source pins, the shell-gear-39 pattern): (a) the five CONDITION_COPY rows and five CONDITION_TONE keys and five CONDITION_EXPLAIN keys exist; (b) the kit ward row contains both `hp left` and `rds`; (c) the four new kit rows exist by their labels; (d) foeStatusBadges reads `"spell:weaken"` and `f.dot`; (e) draw() reads `c.spellSeen` and `P.floorSpell`, and the fallback hex equals `MAP_PALETTE.floorSpell`; (f) voice safety of every new literal (BANNED/ALLOWLIST word-boundary scan as shell-gear-39 does); (g) `www/index.html` (after `npm run build:www`) contains `floorSpell` and `Mapped`. Update shell-map-hud.test.js if it enumerates CONDITION_TONE keys. Run `npm run build:www`.
  </action>
  <verify>
    <automated>npm run build:www && node --test test/unit/shell-spells-40.test.js test/unit/shell-map-hud.test.js test/unit/shell-map-invariants.test.js test/unit/shell-combat-screen.test.js test/unit/spell-menu-mirror.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'mirror: { label: "Mirrored"\|senses: { label: "Senses"\|regen: { label: "Regenerating"\|foresight: { label: "Forewarned"\|reveal: { label: "Mapped"' mazeworld.html` prints 5.
    - `grep -c 'hp left · ${c.ward.rounds} rds' mazeworld.html` prints 1; `grep -c '"Map the Floor", `' mazeworld.html` prints 1; `grep -c '"Sense Presence", "till the fight ends"' mazeworld.html` prints 1.
    - `grep -c '"spell:weaken"' mazeworld.html` prints 1; `grep -c 'f.dot.by === "ice"' mazeworld.html` prints 1.
    - `grep -c "c.spellSeen ? P.floorSpell" mazeworld.html` prints 1; `grep -c "floorSpell" mazeworld.html` is at least 2 (draw + fallback literal); `grep -c "floorSpell" www/index.html` is at least 2.
    - `npm run build:www` exits 0; `node --test test/unit/shell-spells-40.test.js` `# fail 0` with at least 10 tests; every listed suite `# fail 0`.
  </acceptance_criteria>
  <done>Every new engine surface from Plans 02–04 is visible: chips with copy, Hero-tab rows with rounds/squares, foe badges with durations, and a map that shows what it will forget.</done>
</task>

<task type="auto">
  <name>Task 3: Ledger close (UI + requirements map + out of scope), REQUIREMENTS.md, whole-phase gate, aggregated Pixel 7 checklist, PROJECT.md call-outs</name>
  <files>docs/SPELLS.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/SPELLS.md (every section Plans 01–04 wrote; the "## UI (Plan 05)" and "## Requirements map (Plan 05)" placeholders)
    - docs/GEAR-BALANCE.md lines 695-800 ("## Chips and row states — Plan 05" + "## Requirements map" — the shape to mirror)
    - .planning/REQUIREMENTS.md lines 13-19 (SPELL-01..07 text) and 116-122 (the traceability rows)
    - .planning/phases/40-spell-rework/40-01-SUMMARY.md … 40-04-SUMMARY.md — each "## Human verification (deferred to end of run)" section (the items to aggregate) and 40-04's "## Key Decision (for PROJECT.md)"
    - .planning/phases/39-gear-magic-items-one-shot-tools/39-05-SUMMARY.md "## Success Criteria Map" and "## Human verification (deferred to end of run)" (the exact aggregated format)
  </read_first>
  <action>
    1. docs/SPELLS.md: fill "## UI (Plan 05)" (the niche line on both spell surfaces; the chip table `| key | label | detail | tone | explanation |` for ward/mirror/senses/regen/foresight/reveal; the Hero-tab rows; the foe badges; the spell-seen tint and why; what stays for the cleanup milestone: the classic `SPELLS`/`castSpell`/`rollGrimoire` duplicates). Fill "## Requirements map (Plan 05)": `SPELL-01 → Plans 01/02/05`, `SPELL-02 → Plans 03/05`, `SPELL-03 → Plan 01 (+02/04 zero-move records)`, `SPELL-04 → Plan 01`, `SPELL-05 → Plans 01 (rename)/04/05`, `SPELL-06 → Plan 05 (+ conditions.test.js proof)`, `SPELL-07 → Plan 03`; ROADMAP SC-1 → `test/unit/spell-table.test.js` (niche map) + `test/unit/spell-mechanics.test.js`; SC-2 → `test/unit/spell-utility.test.js` + `test/unit/conditions.test.js`; SC-3 → `test/unit/day-one-damage.test.js`; SC-4 → `test/unit/map-reveal.test.js`; SC-5 → `test/unit/spell-utility.test.js` (scroll cases); SC-6 → `test/unit/conditions.test.js` (no-combat ward) + `test/unit/shell-spells-40.test.js` (kit row). Add "## Out of scope / next": bot casting tactics by niche (Phase 42); darkness × reveal interplay (Phase 41); spell cooldowns/mana (not v1.5); the classic-duplicate deletion (cleanup milestone).

    2. .planning/REQUIREMENTS.md: `[x]` SPELL-01..07; the seven traceability rows → Complete.

    3. WHOLE-PHASE GATE (this is the phase's verification — agents are off; BASE = `f14a8f2`): `npm test` → `# tests N`, `# fail 0` (record N; expect > 2686); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` empty (every fixture change was committed inside Plan 01 with its declaration — list those files from the 01 SUMMARY); `npm run build:www` exit 0; `git diff --stat BASE -- engine content src mazeworld.html docs tools package.json package-lock.json` — record the file list and confirm `package.json`/`package-lock.json` are absent (zero new dependencies); `grep -c "fonts.googleapis" mazeworld.html` prints 0; `node --test test/unit/chargen-rng-pin.test.js` green and `git diff BASE -- test/unit/chargen-rng-pin.test.js | grep -c "^[-+] *[0-9]*: *-\?[0-9]"` prints 0 (no pin value moved across the whole phase); `git log --stat BASE..HEAD -- store-listing tools/store-screenshots` prints nothing; `git diff BASE -- docs/class-pass/` prints nothing (the v1.5 BEFORE pins untouched).

    4. SUMMARY: the standard shape PLUS "## Success Criteria Map (ROADMAP SC-1..6)" (SC → text → plan → proof), "## Key Decisions for PROJECT.md (orchestrator)" with two bullets — (a) the re-fog provenance decision verbatim from 40-04-SUMMARY's paragraph, for the Key Decisions table; (b) the Phase 23 "Casters can act" row: `SPELL_LEVEL_OVERRIDES.Summoner` retired, superseded by Phase 40's Lesser Summon (Summon is level 2 for everyone; the Illusionist override stays) — and "## Human verification (deferred to end of run)" = the PHASE's aggregated Pixel 7 checklist: `### Plan 01` (grimoire niche lines; a fresh Summoner's Lesser Summon + Summon "Needs level 2"; no Detect Magic anywhere), `### Plan 02` (Ice climb → ticks → freezes solid with the kill paid; Weakened · N badges + the fade line; Stupefied foe stands there each round; Lesser Summon small ally ≤ 4 rounds, never backfires; Lightning hits every foe), `### Plan 03` (Mirrored/Senses/Regenerating/Forewarned chips outside a fight with their tap explanations; "You move first. Nothing gets the jump on you." with Sense Presence up; the two fade lines after a fight; the Warlock+Heal scroll refusal naming level 3), `### Plan 04` (Map the Floor: whole floor + Mapped chip 40 sq; walked cells stay, the rest fog back with "The map forgets what it was told."; recast resets to 40; descend clears the chip; a pre-Phase-40 save's Detect Magic reads Map the Floor), `### Plan 05` (Shield row on the Hero tab shows hp AND rds while a Shield chip shows the same outside combat; spell-revealed cells paint in the borrowed-sight tint and turn to fog at expiry; foe badges show Weakened · N and Ice · N; TalkBack reads the new chips' labels/details; a voice spot-check of every new line in real play). Number continuously across plans; de-duplicate items that recur (e.g. the Mapped chip appears once, under Plan 04, with Plan 05's tint folded into that item). Also state that the classic-script duplicates were left as dead code.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && git status --porcelain test/parity/fixtures && npm run build:www && grep -c "fonts.googleapis" mazeworld.html; git diff --stat f14a8f2 -- package.json package-lock.json docs/class-pass</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0` and the `# tests` count is greater than 2686.
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `git status --porcelain test/parity/fixtures` prints nothing; `git diff --stat f14a8f2 -- package.json package-lock.json docs/class-pass` prints nothing.
    - `grep -c "fonts.googleapis" mazeworld.html` prints 0; `npm run build:www` exits 0.
    - `git diff f14a8f2 -- test/unit/chargen-rng-pin.test.js | grep -c "^[-+] *[0-9]*: *-\?[0-9]"` prints 0 (no chargen pin value moved in the whole phase).
    - `grep -c "\[x\] \*\*SPELL-0[1-7]\*\*" .planning/REQUIREMENTS.md` prints 7; `grep -c "| SPELL-0[1-7] | Phase 40 | Complete |" .planning/REQUIREMENTS.md` prints 7.
    - `grep -c "## Requirements map (Plan 05)" docs/SPELLS.md` prints 1; `grep -c "## Out of scope / next" docs/SPELLS.md` prints 1; `grep -c "(appended by Plan" docs/SPELLS.md` prints 0 (every placeholder filled). <!-- planner-discipline-allow: (appended by Plan -->
    - The SUMMARY contains "## Success Criteria Map (ROADMAP SC-1..6)", "## Key Decisions for PROJECT.md (orchestrator)" with two bullets, and "## Human verification (deferred to end of run)" with `### Plan 01` … `### Plan 05` headings and a continuously numbered list.
  </acceptance_criteria>
  <done>Phase 40 is closed: ledger complete, all seven SPELL requirements marked complete, the whole-phase gate recorded, the Key Decision and the superseded-override note handed to the orchestrator, and the milestone-close UAT batch has its aggregated checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state → DOM | spell names / foe names / ally names rendered into kit rows, chips and badges |
| content → DOM | `sp.txt`/`sp.n` rendered via the existing innerHTML grimoire row builder |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-09 | Tampering | foe/ally names into badges and kit rows | medium | mitigate | the kit rows and badges use the existing textContent/createElement builders (foeStatusBadges → `cb-foe-tag` via textContent); no new innerHTML template carries a name — pinned by shell-spells-40.test.js |
| T-40-10 | Tampering | `sp.txt` via the grimoire row's innerHTML | low | accept | content is repository data (never user input), unchanged construction since 04-DR10; noted for the cleanup milestone |
| T-40-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (verified by the gate's package.json diff check) |
</threat_model>

<verification>
- Tasks 1–2 suites green; `npm run build:www` exit 0; whole-phase gate numbers recorded in the SUMMARY.
- The aggregated Pixel 7 checklist exists, grouped by plan and numbered continuously, and is never executed in this run.
</verification>

<success_criteria>
- ROADMAP SC-1 (niche line where spells are read), SC-2 (chips for every utility effect), SC-4 (the window legible on the map), SC-5 (scroll refusal visible), SC-6 (Shield pool + rounds on the Hero sheet and as a map chip) are player-visible; SPELL-01..07 all complete.
</success_criteria>

<output>
Create `.planning/phases/40-spell-rework/40-05-SUMMARY.md` when done (standard shape + "## Success Criteria Map (ROADMAP SC-1..6)" + "## Key Decisions for PROJECT.md (orchestrator)" + "## Human verification (deferred to end of run)" aggregated checklist).
</output>
