---
phase: 42-flee-retune-consolidated-balance-close
plan: 03
type: execute
wave: 3
depends_on: ["42-02"]
files_modified:
  - tools/lib/tuning-bot.mjs
  - tools/lib/class-matrix.mjs
  - tools/class-pass-usage.mjs
  - test/unit/bot-tactics.test.js
  - test/unit/tuning-bot.test.js
  - test/unit/class-matrix.test.js
  - test/unit/class-pass-usage.test.js
  - docs/class-pass/v15-tactics-smoke.json
  - docs/CLASS-PASS.md
  - docs/ABILITIES.md
  - docs/GEAR-BALANCE.md
  - docs/GEAR-SLOTS.md
  - docs/SPELLS.md
autonomous: true
requirements: [BAL-01, BAL-02]
tags: [tuning-bot, spells, niche, usage-tallies, pick-rates, class-matrix, smoke, ledger, deferred-uat]

must_haves:
  truths:
    - "A Magic User bot casts by niche without any spell-name special case: a damage-over-time spell (niche `dot`) is chosen only when the target is tough enough to outlast a burst (target hp above the best castable burst's expected damage), a burst spell (niche `burst`) that is expected to finish the target outranks every DOT, control spells stay a ≥ 2-foe pick, Lesser Summon/Summon open a fight, and Map the Floor is cast once per floor when charges are plentiful (CONTEXT 'Spells (Phase 40)'; `chooseSpell` stays kind-generic — it reads `sp.niche`/`sp.kind`/`sp.dmg`, never `sp.n`)"
    - "Every ability, spell and item USE is tallied per run (`tallies.usage`), carried per row (`rowFromRun`), aggregated per cell and per rollup as `{ uses, runs }` (`summarizeRows`), and rendered as byte-stable Markdown pick-rate tables by `tools/class-pass-usage.mjs` — so BAL-02's 'pick-rates for every new spell/ability' is a machine-rendered number, never retyped"
    - "Every report's `meta` carries `runFlags` (`{ storeRoll: true, wornSlots: true }`) beside the frozen `bot` line, so the AFTER's harness semantics are machine-visible while Plan 04's parameter-parity gate (which reads `bot`, not `runFlags`) still passes"
    - "A 143-cell × 3-seed smoke (`docs/class-pass/v15-tactics-smoke.json`) proves zero stuck runs with the full tactics on and shows non-zero ability, spell AND item usage, and `docs/CLASS-PASS.md`'s `## Bot proxy` section gains a `### Phase 42 tactics` addendum transcribing every new rule and `BOT_TACTICS` number verbatim (the ledger's 'what the bot now does' source for the AFTER section)"
  artifacts:
    - "tools/lib/tuning-bot.mjs: chooseSpell niche rules (bestBurstExpected), the Map-the-Floor field step, makeTallies().usage, tallyUsage(tallies, action, events, before, after), ctx.mappedDepth"
    - "tools/lib/class-matrix.mjs: rowFromRun.usage, summarizeRows(...).usage, buildReport meta.runFlags, formatUsageMarkdown(report)"
    - "tools/class-pass-usage.mjs (new CLI): --after PATH [--deep PATH] → Markdown pick-rate tables"
    - "docs/class-pass/v15-tactics-smoke.json; docs/CLASS-PASS.md `### Phase 42 tactics (BAL-01 second half)`; the four 'deferred to Phase 42' ledger notes closed"
    - "test/unit/class-pass-usage.test.js (new), extended bot-tactics/tuning-bot/class-matrix tests"
  key_links:
    - "SPELLS[i].niche/kind/dmg -> chooseSpell (bestBurstExpected computed once per call) -> castSpell idx"
    - "playRun: stateBefore.c.spellsUsed vs stateAfter.c.spellsUsed (a real cast) + abilityUsed/itemUsed/toolUsed events -> tallyUsage -> tallies.usage -> rowFromRun -> summarizeRows (cells + rollups.bySub/byClass/byRace/pooled) -> formatUsageMarkdown -> docs/CLASS-PASS.md"
    - "tools/class-pass-diff.mjs PARITY_FIELDS (bot, seeds, workers, …) untouched; meta.runFlags is additive and outside that list"
---

<objective>
Finish teaching the bot (spells by niche, Map the Floor, Lesser Summon at fight start), add the per-run USAGE tallies and the pick-rate renderer BAL-02 needs, prove the whole tactics set never stalls with a 3-seed smoke, and transcribe every new rule into the CLASS-PASS ledger so Plan 04's AFTER section can be read.

Purpose: BAL-01's second half is complete only when the bot casts like a player would and the matrix can SAY what the bot picked; BAL-02's "pick-rates for every new spell/ability" needs a number source that is rendered, not typed.

Output: `chooseSpell` niche rules, the field cast step, `tallies.usage` + `tallyUsage`, `rowFromRun`/`summarizeRows`/`buildReport` extensions, `formatUsageMarkdown`, `tools/class-pass-usage.mjs`, `docs/class-pass/v15-tactics-smoke.json`, the CLASS-PASS addendum, four ledger cross-links, 42-03-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-CONTEXT.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-02-SUMMARY.md
@.planning/phases/40-spell-rework/40-01-SUMMARY.md
@.planning/phases/40-spell-rework/40-02-SUMMARY.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-01-SUMMARY.md
@tools/lib/tuning-bot.mjs
@tools/lib/class-matrix.mjs
@tools/tune-classes.mjs
@tools/class-pass-diff.mjs
@content/spells.js
@content/abilities.js
@engine/magic.js
@test/unit/tuning-bot.test.js
@test/unit/class-matrix.test.js
@docs/CLASS-PASS.md
@docs/SPELLS.md
</context>

<standing_rules>
- `botLine(opts)` and the `BOT_DEFAULTS` keys it prints stay FROZEN (Plan 04's parity gate). `formatText` (the text matrix transcript shape the ledger pastes) is NOT changed — the pick-rate block is a separate renderer so the AFTER transcript keeps the BEFORE's shape line-for-line. `tools/class-pass-diff.mjs` is NOT edited (it deliberately never imports content; its `PARITY_FIELDS` list stays as is; its byte-stability tests must stay green).
- `chooseSpell` stays kind-generic: rules read `sp.niche`, `sp.kind`, `sp.dmg`, `sp.onHit`, `sp.aoe`, `sp.lesser` — never `sp.n` (RESEARCH Pitfall 2 precedent from Phase 22).
- Every refusal-shaped loop is pre-empted (`canCast` + charges > 0; once-per-floor for Map the Floor) — the HARN-02 Rule-1 discipline.
- The smoke is a DIRECTION check (stuck = 0, usage non-zero), never a tuning verdict; read the POOLED row only; a 3-seed cannot-act cell is documented noise (GEAR-BALANCE.md's own precedent), not actioned.
- Deferred UAT; verification agents OFF: the LAST task runs this plan's gate (`npm test` `# fail 0`, master hash). No `src/browser` change → no `build:www`.
- Commits per task: `feat(42-03): …` / `test(42-03): …` / `docs(42-03): …` with the two trailers (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`). Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
See 42-01-PLAN.md's phase-wide list. This plan creates: the `chooseSpell` niche rules and `bestBurstExpected`, the Map-the-Floor field step (`ctx.mappedDepth`), `makeTallies().usage`, `tallyUsage`, `rowFromRun.usage`, `summarizeRows(...).usage`, `buildReport` `meta.runFlags`, `formatUsageMarkdown`, `tools/class-pass-usage.mjs`, `docs/class-pass/v15-tactics-smoke.json`, `docs/CLASS-PASS.md` `### Phase 42 tactics (BAL-01 second half)`, `test/unit/class-pass-usage.test.js`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: chooseSpell by niche (DOT vs burst by toughness), Map the Floor once per floor, Lesser Summon proof</name>
  <files>tools/lib/tuning-bot.mjs, test/unit/bot-tactics.test.js, test/unit/tuning-bot.test.js</files>
  <read_first>
    - tools/lib/tuning-bot.mjs lines 230-345 (`chooseSpell` — the KILL/DAMAGE/DISABLE/HEAL/WARD tiers, the `dot`/`acid` multipliers, the tie-break) and 196-212 (`bestCastableSummonIdx` — highest-lvl castable `kind: "summon"`; at level 1 a Summoner can only cast Lesser Summon), the out-of-combat Summon step (o) and the scroll step (p)
    - content/spells.js lines 80-120 (`niche` per row: burst = Freeze/Fireball/Mangle/Death; dot = Acid/Ice; control = Doze/Stun/Weaken/Stupidity/Blind/Shrink/Petrify; multi = Earthquake/Fireballs/Lightning; sight = Map the Floor (`kind: "reveal"`, `squares: 40`)/Sense Danger/Sense Presence; summon = Summon/Phantom Host/Lesser Summon (`lesser: true`))
    - engine/magic.js lines 100-135 (`c.spellsUsed++` happens once per real cast, after every refusal — the usage signal Task 2 reads) and 360-385 (the `reveal` branch: `startEffect(c, "spell:reveal", …)`, `floorMapped` event; a recast just refreshes)
    - engine/derived.js#canCast (~line 1382) — level/school/grimoire gates, NO charge check (charges are `maxCharges(c) - c.spellsUsed`, the bot's own `chargesLeft`)
    - test/unit/tuning-bot.test.js lines 412-475 (the DAMAGE-tier ordering tests to extend, `mu()`/`fight()` helpers, `idx(name)`), 529-565 (the Summon tests)
  </read_first>
  <behavior>
    - bestBurstExpected(state): the highest `expectedDamage(sp)` over castable spells with `niche === "burst"` and a `dmg` field (Death has no dmg → excluded); 0 when none
    - DOT rule: a castable `niche === "dot"` spell is skipped when the current target's `wp <= bestBurstExpected(state) + BOT_TACTICS.dotToughMargin`; kept (with its existing multiplier) when the target is tougher — a Sorcerer with Fireball (2d10+4, expected 15) and Ice vs a 12-hp foe casts Fireball; vs a 40-hp foe casts Ice (Ice's `x3` still scores it above Fireball's 315)
    - Burst-finish rule: a castable `niche === "burst"` spell with `dmg` whose `expectedDamage(sp) >= target.wp` scores `350 + expected` (a likely kill outranks every DOT/thrown score below 350 but never the KILL tier's 400+): Freeze (onHit freeze) stays 410; vs a 3-hp foe with Fireball and Acid castable, Fireball wins
    - Control stays a `nFoes >= 2` pick (existing DISABLE tier unchanged: single foe → no control cast)
    - Lesser Summon: a level-1 Summoner (grimoire holding Lesser Summon and Summon, `spellsUsed 0`) at round 1 with no ally gets `{ type: "castSpell", idx: <Lesser Summon> }` from decideAction (Summon is above its level); at level 3 the same character picks Summon (higher lvl castable)
    - Map the Floor: out of combat, a Magic User with Map the Floor castable, `chargesLeft > maxCharges(c) * BOT_TACTICS.mapBankRatio`, on a floor whose depth is not `ctx.mappedDepth` → `{ type: "castSpell", idx: <Map the Floor> }` BEFORE the explore/move step and AFTER the potion/camp/summon/scroll steps; `observe` on `floorMapped` sets `ctx.mappedDepth = stateAfter.floor.depth` (observe needs the state — extend its signature to `observe(ctx, events, stateAfter = null)`, reading `stateAfter` ONLY on `floorMapped` and guarding on its presence, so the existing two-argument test call sites in test/unit/tuning-bot.test.js keep passing unchanged; `playRun` passes the post-action state); a second decideAction on the same floor after the cast does not re-cast; a new `floorChanged` re-arms it (depth differs)
    - decideAction stays pure across the new branches
  </behavior>
  <action>
    A. `chooseSpell`: add a module-private `bestBurstExpected(state)` computed once at the top of the function (after `target`/`nFoes`); in the DAMAGE branch: (1) before scoring a `kind === "dot"` (Ice) or `kind === "acid"` (Acid) spell — both `niche === "dot"` — `if (sp.niche === "dot" && target && target.wp <= burstBest + BOT_TACTICS.dotToughMargin) continue;` (keep the existing already-ticking skips); (2) after computing `expected` for a `niche === "burst"` spell with `sp.dmg`, `if (target && expected >= target.wp) score = 350 + expected; else score = 300 + expected;` (every other kind keeps `300 + expected`). Update the JSDoc's DAMAGE paragraph: Phase 42 (BAL-01 second half, CONTEXT "DOT on a tough single foe, burst on a weak single foe") — niche-keyed, name-free.

    B. Map the Floor: in the out-of-combat chain, after the scroll step (p) and before the exit/explore steps, add: `if (c.cls === "Magic User" && state.floor.depth !== ctx.mappedDepth) { const left = maxCharges(c) - c.spellsUsed; if (left > maxCharges(c) * BOT_TACTICS.mapBankRatio) { const i = SPELLS.findIndex((sp) => sp.kind === "reveal" && canCast(state, sp)); if (i !== -1) return { type: "castSpell", idx: i }; } }`. `makeBotContext` gains `mappedDepth: null`. `observe(ctx, events, stateAfter = null)`: on `floorMapped` set `ctx.mappedDepth = stateAfter.floor.depth` when `stateAfter` is present; update `playRun`'s call to `observe(ctx, events, state)`; the existing two-argument callers in test/unit/tuning-bot.test.js stay valid (`grep -rn "observe(" tools test` to confirm no other caller).

    C. Tests: extend `test/unit/tuning-bot.test.js`'s DAMAGE-tier block with the DOT-vs-burst cases and the Summoner/Lesser Summon case (`idx("Lesser Summon")`, `idx("Summon")`; level 1 vs level 3); add the Map-the-Floor cases and the observe signature case to `test/unit/bot-tactics.test.js`; extend the purity test with a Magic User field state. Run `node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js` — green. Commit `feat(42-03): bot casts by niche — DOT on tough foes, burst finishes, Map the Floor once per floor, Lesser Summon opener` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "function bestBurstExpected" tools/lib/tuning-bot.mjs` is 1; `grep -c 'sp.niche === "dot"' tools/lib/tuning-bot.mjs` is at least 1; `grep -c 'sp.niche === "burst"' tools/lib/tuning-bot.mjs` is at least 1; `grep -c "score = 350 + expected" tools/lib/tuning-bot.mjs` is 1
    - `grep -v '^\s*//' tools/lib/tuning-bot.mjs | grep -v '^\s*\*' | grep -c 'sp.n ===' ` is 0 (no spell-name special case)
    - `grep -c 'sp.kind === "reveal" && canCast(state, sp)' tools/lib/tuning-bot.mjs` is 1; `grep -c "mappedDepth" tools/lib/tuning-bot.mjs` is at least 3
    - `grep -c "export function observe(ctx, events, stateAfter" tools/lib/tuning-bot.mjs` is 1; `grep -c "observe(ctx, events, state)" tools/lib/tuning-bot.mjs` is 1
    - `node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js` exits 0 and the bot-tactics file reports at least 30 tests
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bot-tactics.test.js test/unit/tuning-bot.test.js</automated>
  </verify>
  <done>The caster bot picks DOTs for foes that will outlast a burst, bursts for foes a burst will finish, opens with Lesser Summon when that is all it has, and maps a fresh floor when it can afford to — all keyed on niche/kind, never on a name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Usage tallies per run/cell/rollup, meta.runFlags, formatUsageMarkdown, the class-pass-usage CLI</name>
  <files>tools/lib/tuning-bot.mjs, tools/lib/class-matrix.mjs, tools/class-pass-usage.mjs, test/unit/bot-tactics.test.js, test/unit/class-matrix.test.js, test/unit/class-pass-usage.test.js</files>
  <read_first>
    - tools/lib/tuning-bot.mjs lines 620-720 (`makeTallies`, `tallyEvents(tallies, events, stateAfter)`), 776-825 (`playRun` — where the pre/post state and the dispatched action are both in scope), the `itemLabel` helper from Plan 02
    - tools/lib/class-matrix.mjs lines 187-201 (`rowFromRun`), 222-278 (`summarizeRows` — the per-cell metrics; add `usage`), 350-395 (`groupAndSummarize`/`rollups` — usage flows through for free once summarizeRows carries it), 396-420 (`buildReport` — meta; add `runFlags`), lines 48-49 (imports — add `ABILITIES`, `SPELLS` from content and `RUN_FLAGS` from tuning-bot)
    - tools/class-pass-diff.mjs lines 1-100 (header: PARITY_FIELDS, byte-stability rules — this file is NOT edited) and 840-930 (its CLI arg style to copy for the new CLI)
    - test/unit/class-matrix.test.js lines 96-150 (summarizeRows tests: shape/null-safety), 233-306 (buildReport tests: "no timing key" scan — `runFlags` must not match `/^(elapsed|ms|time)$/i`), 317 (purity: class-matrix imports no fs/os/worker_threads — the new CLI, not the lib, does the file IO)
    - test/unit/class-pass-diff.test.js (the determinism pattern: run the CLI twice, compare bytes — copy it for the new CLI)
    - engine/magic.js line ~122 (`c.spellsUsed++`), engine/abilities.js (`abilityUsed { key, name }`), engine/items.js (`itemUsed { item }`), engine/movement.js line 170 (`toolUsed { tool, feat, item }`)
  </read_first>
  <behavior>
    - makeTallies().usage deep-equals `{ abilities: {}, spells: {}, items: {} }`
    - tallyUsage(tallies, action, events, before, after): an `abilityUsed { key: "kata" }` event → `usage.abilities.kata` +1; an `itemUsed { item }` event → `usage.items[itemLabel(item)]` +1 (a Speed potion → `potion:speed`, a torch → `tool:torch`, a Birch Staff → `Birch Staff`); a `toolUsed { tool: "rope" }` → `usage.items["tool:rope"]` +1; a `castSpell` action where `after.c.spellsUsed === before.c.spellsUsed + 1` → `usage.spells[SPELLS[action.idx].n]` +1; a `castSpell` action that was refused (spellsUsed unchanged) tallies nothing; a scroll read (`scrollCast`) is NOT a spell use (scrolls are items — tally `usage.items["scroll"]` on `scrollCast`)
    - rowFromRun(run).usage is `run.tallies.usage` (same shape); summarizeRows(rows).usage is `{ abilities, spells, items }` where each entry is `{ uses: <sum over completed rows>, runs: <count of completed rows with ≥ 1 use> }`; the all-stuck case returns `usage: { abilities: {}, spells: {}, items: {} }` (never null)
    - rollups(cellRows).bySub[k].usage and .pooled.usage exist with the same shape (pooled over every row); byte-identical regardless of row order (sorted by seed first, as today)
    - buildReport(...).meta.runFlags deep-equals RUN_FLAGS; `meta.bot` is unchanged; no key of meta matches the timing regex
    - formatUsageMarkdown(report) returns Markdown with exactly these H3 headings in order: `### Pick-rates — abilities`, `### Pick-rates — spells`, `### Pick-rates — items`, `### Top picks by sub-class`; the abilities table has one row per `ABILITIES` entry in catalog order with columns `| Ability | Class | Uses | Runs used | Eligible runs | Uses / eligible run |` where eligible runs = sum of `completed` over cells whose `cls === ability.cls`; the spells table has one row per `SPELLS` entry in table order (eligible = Magic User completed runs); the items table lists every label seen, sorted by uses desc then label asc (eligible = all completed runs); the sub-class table has one row per `rollups.bySub` entry (rank order) with the top three `label (uses/run)` picks across all three categories (ties by label asc); every rate is fixed to 2 decimals; a report with zero usage everywhere renders every Uses cell as 0 and rates as 0.00 — never NaN
    - `node tools/class-pass-usage.mjs --after PATH` prints `formatUsageMarkdown` of that JSON; with `--deep PATH` it appends a second block headed `## Pick-rates — depth-20 slice`; running it twice on the same input yields byte-identical stdout; a missing file exits 2 with a usage message
  </behavior>
  <action>
    A. `tools/lib/tuning-bot.mjs`: export `itemLabel(it)` (Plan 02's helper) if not already exported; `makeTallies` gains `usage: { abilities: {}, spells: {}, items: {} }`; add `export function tallyUsage(tallies, action, events, before, after)` per `<behavior>` (a spell use is detected by the `spellsUsed` increment — there is no single "cast succeeded" event); `playRun` keeps `const before = state;` (the pre-action object — applyAction returns a new object, so the reference is safe) and calls `tallyUsage(tallies, action, events, before, state)` right after `tallyEvents`. JSDoc: Phase 42 (BAL-02) — the pick-rate source; CONTEXT "a `usage` tally like the D-07 ability tally".

    B. `tools/lib/class-matrix.mjs`: `rowFromRun` gains `usage: run.tallies.usage`; `summarizeRows` gains `usage` aggregated over `completed` rows (helper `aggregateUsage(rows)`: for each category, for each label, `uses` summed and `runs` counted; labels sorted asc when materialised so JSON key order is stable); the all-stuck early return gains the empty usage object; `buildReport` meta gains `runFlags: RUN_FLAGS` after `bot`. Add `export function formatUsageMarkdown(report)` per `<behavior>` (imports `ABILITIES`, `SPELLS` from `../../content/index.js`; `mdTable` helper local to this module — do NOT import from class-pass-diff.mjs). `formatText` is untouched.

    C. `tools/class-pass-usage.mjs` (new CLI, header in the class-pass-diff style: dev-only, not shipped, tuning proxy; byte-stable — no Date/Math.random): parses `--after PATH` (required), `--deep PATH` (optional), reads JSON with `node:fs`, prints `formatUsageMarkdown(after)` and, with `--deep`, a blank line, `## Pick-rates — depth-20 slice`, and `formatUsageMarkdown(deep)`; unreadable input or a missing `--after` → usage on stderr, exit 2.

    D. Tests: `test/unit/bot-tactics.test.js` (tallyUsage cases), `test/unit/class-matrix.test.js` (rowFromRun/summarizeRows/rollups usage shape, meta.runFlags, the timing-key scan still passes, formatUsageMarkdown headings/rows/NaN-freedom on a synthetic two-cell report), `test/unit/class-pass-usage.test.js` (new: runs the CLI via `child_process.execFileSync` on `docs/class-pass/v15-before.json` — a report with NO usage field must still render zero tables without throwing — twice for byte-identity, and asserts exit 2 on a missing path; mirror class-pass-diff.test.js's structure). Run `node --test test/unit/bot-tactics.test.js test/unit/class-matrix.test.js test/unit/class-pass-usage.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-ledger.test.js test/unit/tuning-bot.test.js` — green (the v1.2/v1.5-BEFORE ledger tests prove the existing JSON files still satisfy every pin — nothing regenerated). Commit `feat(42-03): usage tallies per run/cell/rollup, meta.runFlags, formatUsageMarkdown, class-pass-usage CLI` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "export function tallyUsage" tools/lib/tuning-bot.mjs` is 1; `grep -c "tallyUsage(tallies, action, events, before, state)" tools/lib/tuning-bot.mjs` is 1
    - `grep -c "usage: run.tallies.usage" tools/lib/class-matrix.mjs` is 1; `grep -c "runFlags: RUN_FLAGS" tools/lib/class-matrix.mjs` is 1; `grep -c "export function formatUsageMarkdown" tools/lib/class-matrix.mjs` is 1
    - `test -f tools/class-pass-usage.mjs` succeeds; `node tools/class-pass-usage.mjs --after docs/class-pass/v15-before.json | grep -c "^### Pick-rates — "` is 3; `node tools/class-pass-usage.mjs --after docs/class-pass/v15-before.json > /tmp/u1.md; node tools/class-pass-usage.mjs --after docs/class-pass/v15-before.json > /tmp/u2.md; cmp /tmp/u1.md /tmp/u2.md` exits 0
    - `node -e "import('./tools/lib/class-matrix.mjs').then(m=>{const r=m.buildReport({cellRows:[],opts:{seeds:1,workers:1,maxActions:5,startDepth:1,exploreBudget:50,fleeThreshold:0.3,casterFleeThreshold:0.5,potionThreshold:0.5,campThreshold:0.5,party:false},commit:'x'});process.exit(r.meta.runFlags&&r.meta.runFlags.storeRoll===true&&r.meta.runFlags.wornSlots===true?0:1)})"` exits 0
    - `node --test test/unit/bot-tactics.test.js test/unit/class-matrix.test.js test/unit/class-pass-usage.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-ledger.test.js test/unit/tuning-bot.test.js` exits 0
    - `git status --porcelain docs/class-pass` is empty (no existing pin regenerated)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/bot-tactics.test.js test/unit/class-matrix.test.js test/unit/class-pass-usage.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-ledger.test.js test/unit/tuning-bot.test.js</automated>
  </verify>
  <done>Every use is counted where it happens, aggregated where the matrix aggregates, rendered by a byte-stable tool, and the run flags are visible in every report's meta without disturbing the frozen Bot: line.</done>
</task>

<task type="auto">
  <name>Task 3: 3-seed tactics smoke, CLASS-PASS `### Phase 42 tactics` addendum, close the four deferred-to-Phase-42 ledger notes, plan gate</name>
  <files>docs/class-pass/v15-tactics-smoke.json, docs/CLASS-PASS.md, docs/ABILITIES.md, docs/GEAR-BALANCE.md, docs/GEAR-SLOTS.md, docs/SPELLS.md</files>
  <read_first>
    - docs/GEAR-BALANCE.md lines 316-360 (the Phase 39 smoke section — the exact table shape and the 3-seed cannot-act-noise paragraph to mirror) and lines 305-315 (the "Deferred to Phase 42" note to close), 685-695 and 820-830 (two more Phase 42 mentions)
    - docs/CLASS-PASS.md lines 18-60 (`## Bot proxy (HARN-02)` — append the Phase 42 H3 at the END of this H2 section, before `## How to reproduce`; the section transcribes chooseSpell's table verbatim — do the same for the new rules), lines 1591-1640 (the v1.5 BEFORE section — its pooled-rollups table shape)
    - docs/ABILITIES.md lines 558-566, docs/GEAR-SLOTS.md lines 228-232 and 273-275, docs/SPELLS.md lines 752-756 (the four "Phase 42" forward references to close with a cross-link)
    - test/unit/class-pass-ledger.test.js lines 84-105 (exactly NINE H2 headings — an H3 addendum under Bot proxy does not change the H2 count; do NOT add an H2 in this plan — Plan 04 adds the tenth and re-pins the test)
    - .planning/phases/42-flee-retune-consolidated-balance-close/42-02-SUMMARY.md (the policy paragraphs to transcribe)
  </read_first>
  <action>
    A. Smoke: `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --out docs/class-pass/v15-tactics-smoke.json` (foreground; ~2-4 minutes; record the stderr `elapsed:` line and the engine commit). Then `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-tactics-smoke.json` (record the cannot-act line; at 3 seeds a `meanKills < 0.5` cell is noise — document it exactly as GEAR-BALANCE.md did; `stuck` must be 0 for every cell — a stuck cell is a Rule-1 defect: find the loop from the cell's `topCauses`/a single-cell rerun with `--sub X --race Y --seeds 3`, fix the guard, re-run the smoke, and record both readings), then `node tools/class-pass-usage.mjs --after docs/class-pass/v15-tactics-smoke.json` and confirm non-zero totals in all three tables (abilities, spells, items) — if any table is all zeros, the corresponding policy is not reachable in play: investigate and fix before continuing (this is the whole point of the smoke).

    B. `docs/CLASS-PASS.md` — append `### Phase 42 tactics (BAL-01 second half)` at the end of `## Bot proxy (HARN-02)`: (1) run flags (`RUN_FLAGS`, why, `meta.runFlags`); (2) abilities — the mirrored `pickMemberAbility` rule and the hardest-foe targeting, verbatim from the module JSDoc; (3) items — the heal-before-flee, round-1 buff vs `hardFight`, worn-staff, torch, loot-pile rules and `BOT_TACTICS` numbers verbatim; (4) spells — the DOT/burst toughness rule with `dotToughMargin`, the burst-finish score, Lesser Summon, Map the Floor with `mapBankRatio`; (5) the refusal blocks (`abilityBlocked`/`itemBlocked`) as the Rule-1 safety net; (6) the usage tallies and `tools/class-pass-usage.mjs`; (7) the smoke readout: the GEAR-BALANCE-style pooled table (n, meanDepth, p50Depth, p90Depth, reach5/10/20, meanKills, meanLevel, stuck, top causes) for v1.5 BEFORE (143×40) vs this smoke (143×3), the cannot-act line, wall time, commit, and the pasted `class-pass-usage` render (abilities/spells/items tables only — the per-sub table is Plan 04's business at 40 seeds). State plainly: within-noise vs a 40-seed pin is unknowable at 3 seeds; this is a reachability proof for Plan 04, not a verdict.

    C. Close the forward references with one line each pointing at the addendum: docs/ABILITIES.md "Bot use policy for abilities — Phase 42 …" → "landed: see docs/CLASS-PASS.md `### Phase 42 tactics`"; docs/GEAR-BALANCE.md the three Phase 42 mentions (lines ~308, ~691, ~825) → landed + link; docs/GEAR-SLOTS.md lines ~228-232 and ~273-275 → landed (`RUN_FLAGS.wornSlots`; the wear policy is the engine's own `autoWearSlot` on take — the bot wears the first item per slot and never swaps; recorded as the policy) + link; docs/SPELLS.md lines ~752-756 → landed + link. Do not rewrite anything else in those files.

    D. PLAN GATE: `npm test` (`# fail 0`; record counts), `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `git status --porcelain test/parity/fixtures` empty, `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json` still prints `cannot-act cells: 0 of 143` (the BEFORE pin is untouched).

    E. Commit `docs(42-03): tactics smoke (143x3) + CLASS-PASS Phase 42 tactics addendum; close the four deferred-to-42 ledger notes` (explicit paths: docs/class-pass/v15-tactics-smoke.json docs/CLASS-PASS.md docs/ABILITIES.md docs/GEAR-BALANCE.md docs/GEAR-SLOTS.md docs/SPELLS.md). Write `42-03-SUMMARY.md` with the standard sections, the smoke numbers, `## Human verification (deferred to end of run)` ("none — dev harness and ledgers only"), `## Corrections to CONTEXT` (any), and `## Next Phase Readiness` for Plan 04: the exact AFTER commands (`--seeds 40 --workers 4 --max-actions 5000 --out docs/class-pass/v15-after.json` and `--seeds 10 --workers 4 --max-actions 5000 --start-depth 20 --out docs/class-pass/v15-after-depth20.json`), the expected wall time extrapolated from this smoke's elapsed × (40/3), and the reminder that the natural run exceeds a 10-minute foreground tool timeout and must run in the background with a log.
  </action>
  <acceptance_criteria>
    - `test -f docs/class-pass/v15-tactics-smoke.json` succeeds; `node -e "const d=require('./docs/class-pass/v15-tactics-smoke.json');process.exit(d.meta.seeds===3&&d.meta.cells===143&&d.meta.runFlags&&d.meta.runFlags.wornSlots===true&&d.cells.every(c=>c.stuck===0)?0:1)"` exits 0
    - `node -e "const d=require('./docs/class-pass/v15-tactics-smoke.json');const u=d.rollups.pooled.usage;const s=o=>Object.values(o).reduce((a,b)=>a+b.uses,0);process.exit(s(u.abilities)>0&&s(u.spells)>0&&s(u.items)>0?0:1)"` exits 0
    - `grep -c "^### Phase 42 tactics (BAL-01 second half)" docs/CLASS-PASS.md` is 1; `grep -c "^## " docs/CLASS-PASS.md` is 9 (no new H2 in this plan)
    - `grep -c "Phase 42 tactics" docs/ABILITIES.md` is at least 1; `grep -c "Phase 42 tactics" docs/GEAR-BALANCE.md` is at least 1; `grep -c "Phase 42 tactics" docs/GEAR-SLOTS.md` is at least 1; `grep -c "Phase 42 tactics" docs/SPELLS.md` is at least 1
    - `npm test` output ends with `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json` prints `cannot-act cells: 0 of 143`
    - `.planning/phases/42-flee-retune-consolidated-balance-close/42-03-SUMMARY.md` exists and contains `## Human verification (deferred to end of run)` and `## Next Phase Readiness`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json</automated>
  </verify>
  <done>The full tactics set is proven reachable and stall-free at 143×3, every rule and number is in the ledger where the AFTER section will cite it, and the four "deferred to Phase 42" notes now point at the landing.</done>
</task>

</tasks>

<verification>
- `npm test` `# fail 0`; master hash unchanged; no fixture edits; no existing class-pass JSON regenerated.
- Smoke: 429 runs, zero stuck, non-zero ability/spell/item usage, `meta.runFlags` present.
- `tools/class-pass-usage.mjs` is byte-stable; `tools/class-pass-diff.mjs` untouched.
</verification>

<success_criteria>
- BAL-01 second half complete: abilities, items, tools AND spells-by-niche in use; pick-rates machine-rendered.
- The ledger explains every rule and number Plan 04's AFTER numbers depend on.
</success_criteria>

<output>
Create `.planning/phases/42-flee-retune-consolidated-balance-close/42-03-SUMMARY.md` when done.
</output>
