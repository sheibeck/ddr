---
phase: 42-flee-retune-consolidated-balance-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/flee.js
  - content/index.js
  - engine/derived.js
  - engine/combat.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - src/browser/combatMenu.js
  - test/unit/flee-retune.test.js
  - test/unit/flee-ledger.test.js
  - test/unit/combat.test.js
  - test/unit/gear-axes.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/toastsForAction.test.js
  - test/unit/combatMenu.test.js
  - test/unit/rail.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/FLEE.md
  - docs/GEAR-BALANCE.md
autonomous: true
requirements: [FLEE-01, FLEE-02]
tags: [engine, flee, canon-divergence, narration, combat-menu, ledger, deferred-uat]

must_haves:
  truths:
    - "The ordinary flee roll succeeds when `d20 + mods >= 14` where mods = Thief +5 (kept) + a bounded class modifier + a bounded race modifier − armorBulk(c); a plain Human Fighter in no armor escapes 35% of the time, a Thief in light armor 60%, a Fighter in Plate 25% (ROADMAP SC-2, FLEE-01, CONTEXT Area 1)"
    - "Every ordinary flee attempt emits ONE `fleeRolled { roll, mods:[{name,delta}], total, need }` BEFORE `fled`/`fleeFailed`, and the fight log's folded line leads with the roll, every named modifier and the need before the outcome text (ROADMAP SC-1, FLEE-02)"
    - "A failed flee still hands every foe its swing through the same `foeTurn` call and advances the round exactly as before — the failure path is byte-identical apart from the event payload (ROADMAP SC-3, FLEE-02)"
    - "Samurai refusal, the Cloaker's unseen vanish, the tracked round-1 withdrawal (denied for a Master of Arms) and the Smoke auto-flee are untouched — same order, same events, zero new draws (CONTEXT Area 1: existing sub-class flavour stays)"
    - "The change is a declared canon divergence: `docs/FLEE.md` carries the before/after probability table, the modifier table with a reason per row, and the measured fixture reading; the one parity fixture that flees (seed 17) is measured, not guessed, and regenerated ONLY if its outcome moves (greenfield ruling)"
    - "The combat submenu's FLEE row shows the honest pre-roll need and net modifier for THIS character (the old two hard-coded cost strings are gone)"
  artifacts:
    - "content/flee.js: FLEE_NEED = 14, FLEE_THIEF_BONUS = 5, FLEE_CLASS_MOD, FLEE_RACE_MOD (re-exported from content/index.js)"
    - "engine/derived.js#fleeBreakdown(c) -> { need, mods, bonus }"
    - "engine/combat.js#flee: fleeRolled { roll, mods, total, need } and the `total >= need` test"
    - "src/browser/eventNarration.js EVENT_NARRATION.fleeRolled; src/browser/toasts.js TOAST_FOR.fleeRolled + fleeChain; src/browser/rail.js RAIL_FAMILY.fleeRolled; src/browser/combatMenu.js fleeRow cost/desc"
    - "test/unit/flee-retune.test.js, test/unit/flee-ledger.test.js"
    - "docs/FLEE.md (the FLEE ledger); test/parity/FIXTURE-INVENTORY.md `## Phase 42` section"
  key_links:
    - "content/flee.js tables -> engine/derived.js#fleeBreakdown -> engine/combat.js#flee (the ONE success test) -> fleeRolled payload -> eventNarration/toasts/rail/combatMenu (every surface reads `mods`, never re-derives the formula)"
    - "toasts.js#fleeChain fold -> fightLog.js#fightLogLinesFor (the fight log sources its lines from the SAME fold — one line per flee attempt, roll first, outcome second)"
    - "docs/FLEE.md numbers <- test/unit/flee-ledger.test.js reads content/flee.js (the ledger can never drift from the table)"
---

<objective>
Retune the ordinary flee roll from `d20 + Thief 5 − bulk >= 11` (50% base) to `d20 + Thief 5 + classMod + raceMod − bulk >= 14` (35% base, CONTEXT Area 1: "35% (need 14)", user-chosen), expose every modifier by name in a richer `fleeRolled` event that is narrated on all three surfaces BEFORE the outcome resolves, keep the failed-flee foe swing untouched, update the combat submenu's pre-roll FLEE cost to the honest need, declare the change as a canon divergence in a new `docs/FLEE.md` ledger with the before/after table, and measure (not guess) the one parity fixture that flees.

Purpose: ROADMAP SC-1/2/3 — "transparently fair": the player sees the roll, the modifiers and the need; the base is noticeably below 50%; the Thief edge is still the Thief's whole trade; a failed flee still costs the round.

Output: `content/flee.js`, `engine/derived.js#fleeBreakdown`, the `flee()` rewrite, the three narration surfaces + combat menu, `test/unit/flee-retune.test.js`, `test/unit/flee-ledger.test.js`, `docs/FLEE.md`, the Phase 42 section of `test/parity/FIXTURE-INVENTORY.md`, 42-01-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-CONTEXT.md
@engine/combat.js
@engine/derived.js
@content/races.js
@content/classes.js
@content/armors.js
@src/browser/eventNarration.js
@src/browser/toasts.js
@src/browser/rail.js
@src/browser/combatMenu.js
@test/parity/fixtures/action-script.combat.json
@test/parity/FIXTURE-INVENTORY.md
</context>

<standing_rules>
- Engine gate: pure/deterministic; the flee roll stays ONE `rng.d(20)` draw at the SAME position (after the Smoke check, before pursuitStrike) — no new draws anywhere in `flee()`. `test/parity/prototype-master.js.txt` is NEVER edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- Greenfield ruling (STATE.md amendment 2026-09-17): the new formula is the ONLY formula — no run flag, no lazy field, no old branch kept for fixtures. The fixture(s) the change moves are declared (measured before/after) and regenerated — only those. Measured reading from planning (2026-09-18, `node` replay of `action-script.combat.json` scenario `flee`, seed 17): the hero is a Fridgian Thief (Pilfer) in no armor, the flee d20 is 18 (+5 Thief = 23 vs 11 today; 18 + 5 − 1 Fridgian = 22 vs 14 under this plan's table) — the outcome stays `fled`, so NO fixture is expected to move. Task 1 re-measures this live and records it; if the live reading disagrees, follow the schema's `divergence { kind: "action-path" }` record procedure (test/parity/fixtures/action-script.schema.md) rather than editing the seed or the master.
- No `mazeworld.html` edits in this plan (FLEE-02's line is `src/browser` narration; the combat submenu is `src/browser/combatMenu.js`). If an edit turns out to be unavoidable, STOP and record it for Plan 04 (the only plan allowed to touch the shell).
- Deferred UAT: no device steps, no `adb`, no APK. The SUMMARY carries `## Human verification (deferred to end of run)`.
- Verification agents are OFF: this plan's LAST task runs its own gate (`npm test` `# fail 0`, master hash, `npm run build:www` exit 0 because `src/browser` is touched) and records the numbers in the SUMMARY.
- Voice: family-friendly, deadpan; HP not WP in player-facing copy; every new string passes `test/voice/safety-scan.test.js`; the `fleeRolled` narration builder must be null-safe (`e.mods ?? []`) because the voice scan invokes every builder with sparse event variants.
- Commits per task: `feat(42-01): …` / `test(42-01): …` / `docs(42-01): …`, each ending with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
Phase-wide list (Plans 01-04):
- Plan 01: `content/flee.js` (`FLEE_NEED`, `FLEE_THIEF_BONUS`, `FLEE_CLASS_MOD`, `FLEE_RACE_MOD`); `engine/derived.js#fleeBreakdown(c)`; the `fleeRolled { roll, mods, total, need }` event payload (same event TYPE, richer shape; `bonus`/`bulk` fields removed); `EVENT_NARRATION.fleeRolled` / `TOAST_FOR.fleeRolled` / `fleeChain` / `RAIL_FAMILY.fleeRolled`; `combatMenu.js` fleeRow cost + desc from `fleeBreakdown`; `docs/FLEE.md`; `test/unit/flee-retune.test.js`, `test/unit/flee-ledger.test.js`; `test/parity/FIXTURE-INVENTORY.md` `## Phase 42` section.
- Plan 02: `tools/lib/tuning-bot.mjs#chooseAbility`, `#hardestFoeIndex`, `#hardFight`, `#chooseCombatItem`, `#chooseFieldItem`, `RUN_FLAGS`, `BOT_TACTICS`, `ctx.abilityBlocked`/`ctx.itemBlocked`, the loot-pile step, the `playRun` target write; `test/unit/bot-tactics.test.js`.
- Plan 03: `chooseSpell` niche rules (`bestBurstExpected`), the Map-the-Floor field step, `makeTallies().usage`, `tallyUsage`, `rowFromRun.usage`, `summarizeRows(...).usage`, `meta.runFlags`, `formatUsageMarkdown`, `tools/class-pass-usage.mjs`, `docs/class-pass/v15-tactics-smoke.json`, the CLASS-PASS `### Phase 42 tactics` addendum.
- Plan 04: `docs/class-pass/v15-after.json`, `v15-after-depth20.json`, `v15-verdicts.json` (+ `v15-after-pretune*.json` only if a one-knob tune runs), the `## v1.5 AFTER — commit … (Phase 42 — BAL-02)` section of `docs/CLASS-PASS.md`, the re-pinned `test/unit/class-pass-ledger.test.js`, REQUIREMENTS.md FLEE-01/FLEE-02/BAL-02 complete, the phase-close SUMMARY with the aggregated Pixel 7 checklist and `## For PROJECT.md`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: content/flee.js table, fleeBreakdown(c), the flee() rewrite, re-pinned tests, fixture measurement</name>
  <files>content/flee.js, content/index.js, engine/derived.js, engine/combat.js, test/unit/flee-retune.test.js, test/unit/combat.test.js, test/unit/gear-axes.test.js, test/unit/feedback-payload.test.js</files>
  <read_first>
    - engine/combat.js lines 896-990 (`flee()` — the refusal ladder order Samurai -> Cloaker -> tracked/Master of Arms -> Smoke -> the roll; the three success exits; the `fleeFailed` -> `foeTurn` -> cleared-check tail) and lines 855-895 (`pursuitStrike`, `needMods` shape `{ name, delta }`)
    - engine/derived.js lines 844-847 (`armorBulk`) and lines 1063-1100 (`foeToHitBreakdown` — the `{ need, mods:[{name,delta}] }` breakdown precedent to mirror exactly)
    - content/races.js (the SIX races: Human, Elven, Dwarven, Wilmsry, Fridgian, Troll — CONTEXT's "Halfling/Ogre" examples were illustrative, there is no such race), content/classes.js (three classes), content/armors.js (bulk 0/1/2 per armor; a Fridgian wears none), content/misc-tables.js header (content-module style), content/index.js (the barrel)
    - test/unit/combat.test.js lines 817-850 (the four flee tests; `fixedState` = Human Fighter, armor "Nothing"), 1384-1445 (pursuit/lethal/cleared tests using roll 15 and roll 1); test/unit/gear-axes.test.js lines 295-311 (Knight in Plate, rolls 12/13); test/unit/feedback-payload.test.js lines 269-278 (Guard, roll 11); test/unit/abilities.test.js line 690-696 (Smoke: no fleeRolled)
    - test/determinism/content-is-pure-data.test.js (every content module must export pure data only)
    - test/parity/combat-parity.test.js lines 207-320 (how the `flee` scenario is replayed and asserted) and test/parity/fixtures/action-script.schema.md lines 46-118 (the `divergence { kind: "action-path" }` record — ONLY if the live measurement moves the outcome)
  </read_first>
  <behavior>
    - fleeBreakdown(c) returns `{ need: 14, mods: [...], bonus }` where `bonus` is the sum of every `mods[].delta`; mods are pushed in this fixed order and ONLY when non-zero: `{ name: "Thief", delta: 5 }` for `c.cls === "Thief"`, `{ name: c.cls, delta: FLEE_CLASS_MOD[c.cls] }`, `{ name: c.race, delta: FLEE_RACE_MOD[c.race] }`, `{ name: c.armor, delta: -armorBulk(c) }`; pure (deepStrictEqual of `c` before/after), no rng
    - Table bounds: every FLEE_CLASS_MOD and FLEE_RACE_MOD value is an integer in [-2, 2]; every RACES key and every CLASSES key has an entry; FLEE_NEED is 14; FLEE_THIEF_BONUS is 5
    - Worked rows: Human Fighter no armor -> need 14, bonus 0; Human Thief Leather -> bonus 5; Elven Thief Leather -> bonus 6; Human Fighter Plate -> bonus -2; Human Magic User Cloth -> bonus -1; Troll Fighter Plate -> bonus -3; Dwarven Fighter Mail -> bonus -2; Fridgian Thief (no armor) -> bonus 4
    - Exhaustive d20 enumeration (rolls 1..20 through `flee()` with `fakeRng([roll, ...FILL])` on a fresh state each time, counting `fled`) gives: Human Fighter no armor 7/20 (35%); Human Thief Leather 12/20 (60%); Human Fighter Plate 5/20 (25%); Human Magic User Cloth 6/20 (30%); Elven Thief Leather 13/20 (65%); Troll Fighter Plate 3/20 (15%)
    - flee() on the ordinary path pushes exactly one `fleeRolled` whose keys are exactly `type, roll, mods, total, need`, with `total === roll + bonus`, `need === 14`, and `fleeRolled` precedes `fled`/`fleeFailed` in the events array; the event carries NO `bonus` and NO `bulk` key
    - A failed flee (roll 1, Human Fighter) still yields `fleeFailed` followed by a foe turn and `state.combat.round === 2`; the Samurai refusal, the unseen Cloaker vanish (no `fleeRolled`), the tracked round-1 withdrawal (no `fleeRolled`; a Master of Arms gets `withdrawalDenied` then rolls), and the Smoke exit (no `fleeRolled`) behave exactly as their existing tests assert
    - The ordinary path draws exactly one d20 before any pursuit draw: `fakeRng([14])` on a Human Fighter with no pursuer succeeds with no further draw requested (a `fakeRng` that throws on an extra draw proves it)
  </behavior>
  <action>
    A. `content/flee.js` (new, pure data, header comment in the content-module style): `export const FLEE_NEED = 14;`, `export const FLEE_THIEF_BONUS = 5;`, `export const FLEE_CLASS_MOD = Object.freeze({ "Magic User": -1, "Fighter": 0, "Thief": 0 });`, `export const FLEE_RACE_MOD = Object.freeze({ Human: 0, Elven: 1, Dwarven: -1, Wilmsry: 0, Fridgian: -1, Troll: -1 });`. Header: DELIBERATE RULES CHANGE (Phase 42, 2026-09-18, FLEE-01, CONTEXT Area 1 user-chosen 35%): the prototype's flat d20 (+5 Thief) vs 11 becomes d20 + Thief 5 + class + race − armor bulk vs 14; every modifier bounded to ±2 and reasoned in docs/FLEE.md (Elven +1: light and quick, strikes a die better; Dwarven −1: stocky, short-legged; Fridgian −1: strikes last — `slow`; Troll −1: Large, 75 hp of lumber; Magic User −1: robes and no footwork; Fighter/Thief 0 — the Thief's +5 IS the trade). Add `export * from "./flee.js";` to `content/index.js` after the misc-tables line.

    B. `engine/derived.js` — import `FLEE_NEED, FLEE_THIEF_BONUS, FLEE_CLASS_MOD, FLEE_RACE_MOD` from `../content/index.js` (extend the existing content import) and add `export function fleeBreakdown(c)` beside `armorBulk`: builds `mods` in the order Thief -> class -> race -> armor (armor entry `{ name: c.armor, delta: -armorBulk(c) }` only when bulk > 0; class/race entries only when the table value is non-zero; a race/class missing from the table contributes 0), returns `{ need: FLEE_NEED, mods, bonus }`. JSDoc: Phase 42 (FLEE-01/02) — the ONE flee-need rule, mirroring `foeToHitBreakdown`'s `{ name, delta }` shape so every surface (fight log, toasts, rail, combat submenu) narrates the SAME list without re-deriving the formula; pure, no rng.

    C. `engine/combat.js#flee` — import `fleeBreakdown` from `./derived.js` (extend the existing derived import). Replace the two lines that compute the Thief bonus and the bulk, the `fleeRolled` push and the success comparison with: `const { need, mods, bonus } = fleeBreakdown(c);`, `const roll = rng.d(20);` (SAME position — directly after the Smoke check, before pursuitStrike), `const total = roll + bonus;`, `events.push({ type: "fleeRolled", roll, mods, total, need });`, `if (total >= need) { ... }` — the three statements inside the success branch and the whole failure tail (`fleeFailed`, `foeTurn`, the cleared-check) stay byte-identical. Update the function's JSDoc: DELIBERATE RULES CHANGE (Phase 42, FLEE-01/FLEE-02, docs/FLEE.md): "otherwise d20 (+5 Thief) vs 11" becomes "otherwise d20 + fleeBreakdown(c).bonus vs 14, with every modifier named in fleeRolled"; the failed path is unchanged. Also update the Cloaker comment that mentions the old "d20+5 vs 11" fall-through wording to "the ordinary flee roll below".

    D. `test/unit/flee-retune.test.js` (new): every `<behavior>` bullet. Build states with `combat.test.js`'s helper shapes (copy `fixedState`/`fixedFighter`/`fixedFoe`/`fixedCombat` locally, or export them if the file already exports); for the Thief/Elven/Troll/Magic User rows override `cls`/`sub`/`race`/`armor` (a Magic User in "Cloth"; Plate is `armor: "Plate"`); the exhaustive-enumeration test pads with a FILL array of 1s for the foe turn after a failure (see gear-axes.test.js's `FILL`). Include a `fakeRng` that throws on an unexpected draw for the one-draw proof (abilities.test.js line 693 shows the pattern).

    E. Re-pin the existing tests to the new need — each with a one-line comment naming Phase 42/FLEE-01: `test/unit/combat.test.js` line 834-840 (Thief marginal escape: roll 9, `9 + 5 = 14 >= 14`); `test/unit/gear-axes.test.js` lines 295-310 (Knight in Plate: roll 12 − 2 = 10 fails; the success case becomes roll 16, `16 − 2 = 14 >= 14`; rename the test title to "… success is roll + mods >= 14 (a Fighter in Plate needs a natural 16)" and assert `rolled.mods` deep-equals `[{ name: "Plate", delta: -2 }]` instead of the old `bulk`/`bonus` fields); `test/unit/feedback-payload.test.js` line 269-278 (Guard hero: flee roll 14 succeeds; comment updated). Every other flee test (rolls 15/20 succeed, roll 1 fails, Cloaker/tracked/Smoke never roll) stays untouched — run them to prove it.

    F. Fixture measurement (Rule: measure, never guess): run `node --test test/parity/` — expected all green with zero fixture edits. Then replay scenario `flee` (seed 17) with a scratch script (newRun(17) → startCombat forced Beasts + fight → applyAction flee) and record: hero race/class/sub/armor, the d20, `mods`, `total`, `need`, outcome. If (and only if) the outcome is `fleeFailed`, add a `divergence { kind: "action-path", phase: 42, requirements: ["FLEE-01"], fromAction: 1, fields/before/after measured from BOTH sides, stateFields: ["dead"], rationale }` to that scenario per the schema (never touch its `chargenDivergence`, never the seed, never the master) and regenerate `test/parity/FIXTURE-INVENTORY.md`'s generated block via `node tools/fixture-inventory.mjs` only if `fixture-inventory.test.js` demands it. Record the reading verbatim for Task 3's ledger.

    Run `node --test test/unit/flee-retune.test.js test/unit/combat.test.js test/unit/gear-axes.test.js test/unit/feedback-payload.test.js test/unit/abilities.test.js test/unit/identity-contract.test.js test/unit/parley.test.js test/unit/spell-mechanics.test.js test/determinism/ test/parity/` — green. Commit `feat(42-01): flee retune — need 14, Thief +5 kept, bounded class/race table, fleeBreakdown, fleeRolled with named modifiers` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "export const FLEE_NEED = 14;" content/flee.js` is 1; `grep -c "export const FLEE_THIEF_BONUS = 5;" content/flee.js` is 1; `grep -c "export const FLEE_CLASS_MOD" content/flee.js` is 1; `grep -c "export const FLEE_RACE_MOD" content/flee.js` is 1; `grep -c 'export \* from "./flee.js";' content/index.js` is 1
    - `node -e "import('./content/index.js').then(m=>{const r=Object.keys(m.RACES).every(k=>Number.isInteger(m.FLEE_RACE_MOD[k])&&Math.abs(m.FLEE_RACE_MOD[k])<=2);const c=Object.keys(m.CLASSES).every(k=>Number.isInteger(m.FLEE_CLASS_MOD[k])&&Math.abs(m.FLEE_CLASS_MOD[k])<=2);process.exit(r&&c?0:1)})"` exits 0
    - `grep -c "export function fleeBreakdown" engine/derived.js` is 1
    - `node -e "import('./engine/derived.js').then(m=>{const a=m.fleeBreakdown({cls:'Fighter',race:'Human',armor:'Nothing'});const b=m.fleeBreakdown({cls:'Thief',race:'Elven',armor:'Leather'});const c=m.fleeBreakdown({cls:'Fighter',race:'Troll',armor:'Plate'});process.exit(a.need===14&&a.bonus===0&&a.mods.length===0&&b.bonus===6&&b.mods[0].name==='Thief'&&b.mods[1].name==='Elven'&&c.bonus===-3&&c.mods.at(-1).name==='Plate'&&c.mods.at(-1).delta===-2?0:1)})"` exits 0
    - `grep -c 'events.push({ type: "fleeRolled", roll, mods, total, need });' engine/combat.js` is 1; `grep -c "if (total >= need) {" engine/combat.js` is 1; `grep -v '^\s*//' engine/combat.js | grep -v '^\s*\*' | grep -c "need: 11"` is 0; `grep -v '^\s*//' engine/combat.js | grep -v '^\s*\*' | grep -c "bonus - bulk"` is 0
    - `grep -c "rng.d(20)" engine/combat.js` equals the count on the committed parent revision (`git show HEAD:engine/combat.js | grep -c "rng.d(20)"`) — no draw added or removed
    - `node --test test/unit/flee-retune.test.js` exits 0 with at least 10 tests
    - `node --test test/unit/combat.test.js test/unit/gear-axes.test.js test/unit/feedback-payload.test.js test/unit/abilities.test.js test/unit/identity-contract.test.js test/unit/parley.test.js test/unit/spell-mechanics.test.js test/determinism/ test/parity/` exits 0
    - `git status --porcelain test/parity/fixtures` is empty UNLESS the live seed-17 reading flipped the outcome, in which case `node -e "const d=require('./test/parity/fixtures/action-script.combat.json');const s=d.scenarios.find(x=>x.name==='flee');process.exit(s.divergence&&s.divergence.kind==='action-path'&&s.divergence.phase===42?0:1)"` exits 0
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/flee-retune.test.js test/unit/combat.test.js test/unit/gear-axes.test.js test/unit/feedback-payload.test.js test/unit/abilities.test.js test/unit/identity-contract.test.js test/unit/parley.test.js test/unit/spell-mechanics.test.js test/determinism/ test/parity/</automated>
  </verify>
  <done>The flee formula is `d20 + fleeBreakdown(c).bonus >= 14` with one draw at the old position, every modifier named in the event, the failure path untouched, the sub-class flavour untouched, and the seed-17 fixture measured live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrate fleeRolled with named modifiers on all three surfaces (roll before outcome) and the honest combat-menu FLEE cost</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, src/browser/combatMenu.js, test/unit/toastsForAction.test.js, test/unit/combatMenu.test.js, test/unit/rail.test.js, test/unit/flee-retune.test.js</files>
  <read_first>
    - src/browser/eventNarration.js lines 69-77 (`needModsText(mods)` -> "Guard −1" / "Agility −1, Guard −1" — REUSE this exact format) and lines 344-353 (`fled`, `fleeRolled`, `fleeFailed` entries)
    - src/browser/toasts.js lines 255-275 (the `fleeRolled` key in the combat-only/ORACLE list — keep it), 693-720 (`fleeChain` — the fold that produces the ONE fight-log line), 1063-1070 (`TOAST_FOR.fled/fleeRolled/fleeFailed`), 294-315 (the shared-helper block where a local `fleeModsText` may live)
    - src/browser/fightLog.js lines 1-50 (the fight log sources its lines from `toastsForAction`'s fold; the tap-to-reveal `roll` text is `oracleDetailText(narrateEvent(events[idx]))` of the fold's FIRST event — the `fleeRolled` Oracle sentence)
    - src/browser/rail.js lines 144-250 (`RAIL_FAMILY` — the Phase 40 "combat-only spell events, given a family entry for completeness" precedent to mirror)
    - src/browser/combatMenu.js lines 15-21 (imports — `engine/derived.js` is already imported), 296-316 (the FLEE/WITHDRAW row: two hard-coded cost strings on line 310 and `COMBAT_MENU_COPY.fleeDesc`)
    - test/unit/toastsForAction.test.js lines 118-125 and 484-505 (the fold pins to re-pin), test/unit/combatMenu.test.js lines 105-115 and 178-190 (the cost pins to re-pin), test/unit/rail.test.js (the RAIL_FAMILY shape/voice tests), test/voice/safety-scan.test.js lines 11-30 (every builder is invoked with sparse events — null-safety)
  </read_first>
  <behavior>
    - EVENT_NARRATION.fleeRolled({ roll: 8, mods: [{name:"Thief",delta:5},{name:"Mail",delta:-1}], total: 12, need: 14 }) renders exactly `Flee: rolled <span class="roll">8</span> (Thief +5, Mail −1) — 12 against 14.`; with `mods: []` it renders `Flee: rolled <span class="roll">8</span> — 8 against 14.`; with `{}` (no fields) it renders without throwing and contains "Flee:"
    - TOAST_FOR.fleeRolled(e) text is `Flee: 8 (Thief +5, Mail −1) = 12 vs 14` (no parenthetical when mods is empty: `Flee: 8 = 8 vs 14`), tone "beat", priority PRIORITY.other; null-safe on `{}`
    - fleeChain: `[fleeRolled{roll:9,mods:[{name:"Thief",delta:5}],total:14,need:14}, fled{reason:"escaped"}]` folds into ONE toast whose text is `Flee: 9 (Thief +5) = 14 vs 14. You get clear.` with tone "hit" and `idx` 0; `[fleeRolled{roll:3,mods:[],total:3,need:14}, fleeFailed]` folds into `Flee: 3 = 3 vs 14. You do not make it.` tone "miss"; a `fled` with no preceding `fleeRolled` (Cloaker/tracked/smoke) keeps its own builder text untouched
    - fightLogLinesFor("flee", [fleeRolled…, fleeFailed]) returns ONE line whose `text` starts with "Flee: " and whose `roll` is the Oracle sentence (dice kept, tags stripped) — the roll/modifiers/need appear before the outcome text in the same line
    - RAIL_FAMILY.fleeRolled deep-equals `{ icon: "·", title: "FLEE", tone: "info" }`
    - combatMenu fleeRow for a Human Fighter in no armor: `cost` is `d20, 14+` and `desc` is `COMBAT_MENU_COPY.fleeDesc`; for a Human Thief in Leather: `cost` is `d20+5, 14+` and `desc` is `${COMBAT_MENU_COPY.fleeDesc} (Thief +5)`; for a Troll Fighter in Plate: `cost` is `d20−3, 14+` and `desc` ends with `(Troll −1, Plate −2)`; the tracked round-1 WITHDRAW row is unchanged
  </behavior>
  <action>
    A. `src/browser/eventNarration.js` — replace the `fleeRolled` entry with a builder that reads `e?.roll`, `e?.mods ?? []`, `e?.total ?? e?.roll`, `e?.need` and renders `Flee: rolled <span class="roll">${roll}</span>${mods.length ? \` (${needModsText(mods)})\` : ""} — ${total} against ${need}.` (use the existing `needModsText`; U+2212 minus comes from it; the em dash is the file's existing punctuation). Comment: Phase 42 (FLEE-02) — the roll, every named modifier and the need, narrated BEFORE the outcome line (`fled`/`fleeFailed` keep their own entries).

    B. `src/browser/toasts.js` — add a local `fleeModsText(mods)` helper in the shared-helper block (same format as eventNarration's `needModsText`: `${name} ${delta < 0 ? "−" : "+"}${Math.abs(delta)}` joined by ", "). Rewrite `TOAST_FOR.fleeRolled` to `Flee: ${roll}${mods.length ? \` (${fleeModsText(mods)})\` : ""} = ${total} vs ${need}` (tone "beat", PRIORITY.other, null-safe). Rewrite `fleeChain`'s folded text to ROLL FIRST: `${rollText}. ${b.text}` where `rollText` is exactly the `TOAST_FOR.fleeRolled(e).text` string — reuse the builder rather than restating the format — keeping `tone: b.tone, priority: b.priority, idx: i`. Update the JSDoc: Phase 42 (FLEE-02, ROADMAP SC-1) — the fold leads with the dice so the fight log shows roll/modifiers/need before the outcome in one line (the 34-CONTEXT "log line count = folded count" pin holds: still ONE line per attempt).

    C. `src/browser/rail.js` — add `fleeRolled: { icon: "·", title: "FLEE", tone: "info" }` to `RAIL_FAMILY` next to the Phase 40 combat-only rows, with the same "family entry for completeness — the fight log is the real destination" comment (Phase 42, FLEE-02).

    D. `src/browser/combatMenu.js` — import `fleeBreakdown` (extend the `engine/derived.js` import). Add a local `fleeModsText(mods)` (same format). Replace the two hard-coded cost strings with a computed one: `const fb = fleeBreakdown(c); const net = fb.bonus === 0 ? "" : fb.bonus > 0 ? \`+${fb.bonus}\` : \`−${Math.abs(fb.bonus)}\`;` → `cost: withdraw ? COMBAT_MENU_COPY.withdrawCost : \`d20${net}, ${fb.need}+\``, `desc: withdraw ? COMBAT_MENU_COPY.withdrawDesc : fb.mods.length ? \`${COMBAT_MENU_COPY.fleeDesc} (${fleeModsText(fb.mods)})\` : COMBAT_MENU_COPY.fleeDesc`. Update the comment above the row (mirrors `engine/derived.js#fleeBreakdown` for display only; the engine still rolls; Phase 42 FLEE-01/02).

    E. Tests: re-pin `test/unit/toastsForAction.test.js` (lines 118-125 and 484-505) to the new payload/fold text; re-pin `test/unit/combatMenu.test.js` lines 105-115 and 178-190 (`d20, 14+` / `d20+5, 14+` and the desc suffix) and add the Troll-Fighter-in-Plate case; add to `test/unit/rail.test.js` a `RAIL_FAMILY.fleeRolled` shape assertion; extend `test/unit/flee-retune.test.js` with a `narration` block covering the EVENT_NARRATION/TOAST_FOR/fightLogLinesFor bullets (import `narrateEvent`, `toastsForAction`, `TOAST_FOR`, `fightLogLinesFor`). Run `node --test test/unit/flee-retune.test.js test/unit/toastsForAction.test.js test/unit/combatMenu.test.js test/unit/rail.test.js test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/round-card-worst-case.test.js test/voice/` — green. `npm run build:www` exit 0. Commit `feat(42-01): fleeRolled narrated with named modifiers on fight log/toast/rail before the outcome; honest FLEE submenu cost` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c "fleeRolled: (e) =>" src/browser/eventNarration.js` is 1 and `grep -c "needModsText(mods)" src/browser/eventNarration.js` is at least 2 (existing use + the flee builder)
    - `grep -c "function fleeModsText" src/browser/toasts.js` is 1; `grep -c "function fleeModsText" src/browser/combatMenu.js` is 1
    - `node -e "import('./src/browser/toasts.js').then(m=>{const out=m.toastsForAction('flee',[{type:'fleeRolled',roll:9,mods:[{name:'Thief',delta:5}],total:14,need:14},{type:'fled',reason:'escaped'}],{});process.exit(out.length===1&&out[0].text==='Flee: 9 (Thief +5) = 14 vs 14. You get clear.'&&out[0].tone==='hit'?0:1)})"` exits 0
    - `node -e "import('./src/browser/eventNarration.js').then(m=>{const s=m.narrateEvent({type:'fleeRolled',roll:8,mods:[{name:'Thief',delta:5},{name:'Mail',delta:-1}],total:12,need:14});process.exit(s==='Flee: rolled <span class=\"roll\">8</span> (Thief +5, Mail −1) — 12 against 14.'?0:1)})"` exits 0
    - `grep -c 'fleeRolled: { icon: "·", title: "FLEE", tone: "info" }' src/browser/rail.js` is 1
    - `grep -c "fleeBreakdown" src/browser/combatMenu.js` is at least 2 (import + use); `grep -v '^\s*//' src/browser/combatMenu.js | grep -c '11+'` is 0
    - `node --test test/unit/flee-retune.test.js test/unit/toastsForAction.test.js test/unit/combatMenu.test.js test/unit/rail.test.js test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/round-card-worst-case.test.js test/voice/` exits 0
    - `npm run build:www` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www && node --test test/unit/flee-retune.test.js test/unit/toastsForAction.test.js test/unit/combatMenu.test.js test/unit/rail.test.js test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/round-card-worst-case.test.js test/voice/</automated>
  </verify>
  <done>The fight log's one flee line reads roll → named modifiers → need → outcome; the Oracle and toast say the same thing from the same `mods`; the rail has a FLEE family; the submenu's FLEE row tells this character its real need; the voice scan and coverage guards are green and the www bundle builds.</done>
</task>

<task type="auto">
  <name>Task 3: docs/FLEE.md ledger (declared divergence, modifier table, before/after table, fixture reading), ledger pin test, cross-links, plan gate</name>
  <files>docs/FLEE.md, test/unit/flee-ledger.test.js, docs/GEAR-BALANCE.md, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - docs/TERRAIN.md lines 1-60 (the Phase 41 ledger's header style: canon change, rationale, requirement map) and docs/GEAR-BALANCE.md lines 100-130 (the "Flee roll" row at line ~120 that cites the old formula — cross-link it to docs/FLEE.md)
    - test/unit/class-pass-ledger.test.js lines 40-80 (the doc-parsing helpers `h2s`/`section` to copy for the FLEE ledger pin test)
    - test/parity/FIXTURE-INVENTORY.md (append a `## Phase 42 — flee retune (FLEE-01)` section after the last section; the generated block between the `fixture-inventory:generated:begin/end` markers is NEVER hand-edited)
    - The Task 1 seed-17 reading you recorded (race/class/sub/armor, d20, mods, total, need, outcome)
  </read_first>
  <action>
    A. `docs/FLEE.md` (new) with these H2 sections, in order: `## Canon change (Phase 42, FLEE-01)` — the prototype rule (mazeworld.html flee(): d20, +5 Thief, eleven or better → 50% / Thief 75%), the new rule as one formula line, the user decision (CONTEXT Area 1: 35% / need 14), the greenfield ruling (only formula; no run flag); `## Modifier table` — a Markdown table `| Modifier | Value | Reason |` with every row of FLEE_CLASS_MOD and FLEE_RACE_MOD (all six races, all three classes), the Thief +5 row, and the armor row (−bulk: Cloth/Leather 0, Studded/Mail −1, Plate −2; a Fridgian never wears armor), each with a one-line in-voice reason; `## Before / after` — a table `| Character | Old need (d20 ≥) | Old % | New need (d20 ≥) | New % |` with at least these rows: Human Fighter no armor (11 / 50 → 14 / 35), Human Fighter Mail (12 / 45 → 15 / 30), Human Fighter Plate (13 / 40 → 16 / 25), Human Thief Leather (6 / 75 → 9 / 60), Human Thief Studded (7 / 70 → 10 / 55), Elven Thief Leather (6 / 75 → 8 / 65), Human Magic User Cloth (11 / 50 → 15 / 30), Dwarven Fighter Mail (12 / 45 → 16 / 25), Fridgian Thief (6 / 75 → 10 / 55), Troll Fighter Plate (13 / 40 → 17 / 20), Troll Magic User Cloth (11 / 50 → 16 / 25) — every percentage is (21 − need)/20 clamped to [0, 100], computed, not typed from memory; `## What did not change` — Samurai, Cloaker unseen vanish, tracked round-1 withdrawal / Master of Arms denial, Smoke; the failed-flee foe swing and round advance; pursuit strikes; loot forfeit; one d20 draw at the same position; `## Event payload and narration (FLEE-02)` — the `fleeRolled { roll, mods:[{name,delta}], total, need }` shape (the old `bonus`/`bulk` keys removed — greenfield), the Oracle sentence, the fight-log fold (roll first, outcome second, ONE line), the toast, the rail family, the combat-submenu cost/desc; `## Parity fixture reading` — the seed-17 `flee` scenario measurement from Task 1 verbatim (hero, d20, mods, total vs need, outcome; "no divergence record needed" or the record you declared); `## Tests` — the test files and what each pins; `## Requirements map` — FLEE-01 / FLEE-02 → this plan, proof files (REQUIREMENTS.md itself is flipped by Plan 04 at phase close). Cross-link: `docs/CLASS-PASS.md`'s v1.5 AFTER section (Plan 04) will cite this ledger.

    B. `test/unit/flee-ledger.test.js` (new): reads `docs/FLEE.md`; asserts the six H2 headings exist in order; asserts the Modifier table contains one row per FLEE_RACE_MOD key and per FLEE_CLASS_MOD key whose numeric cell equals the content value (parse `| Elven | +1 |`-style rows; sign-aware); asserts the Before/after table's "New need" for the Human Fighter no-armor row equals `FLEE_NEED` and the Thief Leather row equals `FLEE_NEED - FLEE_THIEF_BONUS`; asserts every "New %" cell equals `Math.round((21 - need) / 20 * 100)` for its row's need (numbers never drift from the table); asserts the file mentions `fleeRolled` and `docs/CLASS-PASS.md`.

    C. `docs/GEAR-BALANCE.md` line ~120 "Flee roll" row: change the formula cell to `roll + fleeBreakdown(c).bonus >= 14 (bulk is one of the named modifiers — see docs/FLEE.md)`. Nothing else in that file changes.

    D. `test/parity/FIXTURE-INVENTORY.md` — append `## Phase 42 — flee retune (FLEE-01)`: the grep result (`"flee"` actions appear ONLY in `action-script.combat.json` scenario `flee`, seed 17), the measured reading, the draw-count statement (one d20, same position), and the conclusion (zero fixture regenerations, or the one declared record). Never touch the generated block.

    E. PLAN GATE (verification agents are off): `npm test` — record `# tests`, `# pass`, `# fail` (must be 0); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty (or exactly the declared record). Put the numbers in the SUMMARY.

    F. Commit `docs(42-01): FLEE ledger (declared divergence, modifier + before/after tables, fixture reading), ledger pin test, GEAR-BALANCE cross-link, FIXTURE-INVENTORY Phase 42` (explicit paths). Write `42-01-SUMMARY.md` with the standard sections plus `## Human verification (deferred to end of run)` listing: (1) in a fight, FLEE shows "d20±N, 14+" and the modifier list under the row for this character; (2) after tapping FLEE the fight log's newest line reads "Flee: N (…) = T vs 14." followed by the outcome, and tapping it reveals the Oracle sentence with the dice; (3) a failed flee visibly hands every foe its swing and the round counter advances; (4) a Samurai still refuses, a fresh Cloaker still vanishes, a round-1 tracked WITHDRAW is still free; (5) voice read of the new line. Plus `## Corrections to CONTEXT` (the races are Human/Elven/Dwarven/Wilmsry/Fridgian/Troll — no Halfling/Ogre; the combat submenu also carried the old need and was updated; the seed-17 reading).
  </action>
  <acceptance_criteria>
    - `grep -c "^## " docs/FLEE.md` is at least 8; `grep -c "^## Canon change (Phase 42, FLEE-01)" docs/FLEE.md` is 1; `grep -c "^## Parity fixture reading" docs/FLEE.md` is 1; `grep -c "seed 17" docs/FLEE.md` is at least 1
    - `node --test test/unit/flee-ledger.test.js` exits 0 with at least 5 tests
    - `grep -c "docs/FLEE.md" docs/GEAR-BALANCE.md` is 1; `grep -c "^## Phase 42 — flee retune (FLEE-01)" test/parity/FIXTURE-INVENTORY.md` is 1
    - `npm test` output ends with `# fail 0` and `# pass` is at least 2915 + 15
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0
    - `.planning/phases/42-flee-retune-consolidated-balance-close/42-01-SUMMARY.md` exists and contains `## Human verification (deferred to end of run)` and `## Corrections to CONTEXT`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && node --test test/unit/flee-ledger.test.js</automated>
  </verify>
  <done>The flee retune is a declared, measured, ledgered canon divergence whose numbers are pinned to the content table by a test; the whole suite is green with the master hash unchanged and the bundle building.</done>
</task>

</tasks>

<verification>
- `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0.
- Exhaustive d20 enumeration proves 35% / 60% / 25% for the three CONTEXT anchor rows.
- `fleeRolled` precedes `fled`/`fleeFailed`; the fold text leads with the dice; every surface reads `mods`.
- Seed-17 flee fixture measured and recorded; fixtures regenerated only if the outcome moved.
</verification>

<success_criteria>
- FLEE-01: base 35% (need 14), Thief +5 kept, bounded class/race table, ledgered before/after — the ONLY formula.
- FLEE-02: roll/modifiers/need shown in the fight log before the outcome; failed flee unchanged (foeTurn, round advance).
- No `mazeworld.html` edit; no new rng draw; parity green.
</success_criteria>

<output>
Create `.planning/phases/42-flee-retune-consolidated-balance-close/42-01-SUMMARY.md` when done (standard sections + `## Human verification (deferred to end of run)` + `## Corrections to CONTEXT` + the gate numbers).
</output>
