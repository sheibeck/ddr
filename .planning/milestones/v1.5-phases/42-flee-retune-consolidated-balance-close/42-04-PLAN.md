---
phase: 42-flee-retune-consolidated-balance-close
plan: 04
type: execute
wave: 4
depends_on: ["42-03"]
files_modified:
  - docs/class-pass/v15-after.json
  - docs/class-pass/v15-after-depth20.json
  - docs/class-pass/v15-verdicts.json
  - docs/CLASS-PASS.md
  - docs/FLEE.md
  - test/unit/class-pass-ledger.test.js
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [BAL-02, FLEE-01, FLEE-02, BAL-01]
tags: [class-matrix, after-pin, verdicts, depth-20-target, ledger, phase-close, deferred-uat]

must_haves:
  truths:
    - "ONE consolidated AFTER class-matrix pair exists — `docs/class-pass/v15-after.json` (143 cells × 40 seeds, start depth 1) and `v15-after-depth20.json` (143 × 10, start depth 20) — run with the SAME seed counts, workers, maxActions, exploreBudget and `Bot:` line as the BEFORE pins (`class-pass-diff.mjs`'s parameter parity passes for both pairs), against the engine with abilities + gear + spells + terrain + the flee retune all landed and the bot using all of it (ROADMAP SC-4, BAL-02)"
    - "The BEFORE pins `docs/class-pass/v15-before.json` / `v15-before-depth20.json` are byte-identical to their committed state before this plan (never regenerated)"
    - "`docs/CLASS-PASS.md` carries a `## v1.5 AFTER — commit <40-hex> (Phase 42 — BAL-02)` section: the machine-rendered BEFORE → AFTER tables (by class, bottom/top five, races, all 24 subs, reach table, depth-20 slice, out-of-band cells) with a written verdict + reason for EVERY out-of-band sub-class and race row, the pooled-rollups comparison table in the BEFORE section's shape, the machine-rendered pick-rate tables for every ability/spell/item, a 'what the bot now does' paragraph, the depth-20 target verdict against explicit thresholds, and both transcripts — all numbers rendered by `class-pass-diff.mjs` / `class-pass-usage.mjs`, never retyped (ROADMAP SC-4)"
    - "Out-of-band rows follow CONTEXT Area 2: accepted with a written reason, OR tuned by exactly ONE content number with exactly ONE matrix re-run recorded before/after (pretune JSONs kept) — never a second tuning loop, never an engine-rule change, never a bot-threshold change (that would break parameter parity)"
    - "REQUIREMENTS.md marks FLEE-01, FLEE-02 and BAL-02 complete; the whole-phase gate passes (`npm test` `# fail 0`, master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0, fixtures clean); the SUMMARY aggregates the phase's Pixel 7 checklist and carries `## For PROJECT.md`"
  artifacts:
    - "docs/class-pass/v15-after.json, docs/class-pass/v15-after-depth20.json (+ v15-after-pretune.json / v15-after-pretune-depth20.json ONLY if a one-knob tune ran), docs/class-pass/v15-verdicts.json"
    - "docs/CLASS-PASS.md `## v1.5 AFTER — commit … (Phase 42 — BAL-02)` (the tenth H2, last)"
    - "test/unit/class-pass-ledger.test.js re-pinned (ten H2s; the v1.5 AFTER pin tests)"
    - ".planning/REQUIREMENTS.md FLEE-01/FLEE-02/BAL-02 complete; 42-04-SUMMARY.md with the aggregated checklist and `## For PROJECT.md`"
  key_links:
    - "tools/tune-classes.mjs --out (background, logged) -> v15-after*.json -> class-pass-diff.mjs --gate (0 cannot-act) -> --section after + --out-verdicts (render) -> hand-authored verdict/reason/lever in v15-verdicts.json -> --verdicts re-render -> pasted verbatim into docs/CLASS-PASS.md -> test/unit/class-pass-ledger.test.js pins hash/parity/keys/cannot-act/verdict completeness"
    - "tools/class-pass-usage.mjs --after v15-after.json --deep v15-after-depth20.json -> the pick-rate tables in the same section"
---

<objective>
Run the milestone's ONE consolidated AFTER class matrix (natural + depth-20) with the taught bot against the fully-landed v1.5 engine, gate it, diff it against the BEFORE pins, write the `## v1.5 AFTER` section of `docs/CLASS-PASS.md` with machine-rendered tables, pick-rates and a written verdict for every out-of-band row against the depth-20 target (accept with reason, or one-knob tune with one re-run), re-pin the ledger test, mark FLEE-01/FLEE-02/BAL-02 complete, run the whole-phase gate, and hand the milestone-close batch the aggregated Pixel 7 checklist plus the `## For PROJECT.md` section.

Purpose: ROADMAP SC-4 — close the balance loop BAL-01 opened: abilities + gear + spells verified together against the depth-20 target, in one honest, reproducible run.

Output: the AFTER JSON pair, `v15-verdicts.json`, the ledger section, the re-pinned test, REQUIREMENTS.md, 42-04-SUMMARY.md (phase close).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-CONTEXT.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-01-SUMMARY.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-02-SUMMARY.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-03-SUMMARY.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-01-SUMMARY.md
@.planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md
@tools/tune-classes.mjs
@tools/class-pass-diff.mjs
@tools/class-pass-usage.mjs
@docs/CLASS-PASS.md
@docs/class-pass/v15-before.json
@docs/class-pass/v15-before-depth20.json
@test/unit/class-pass-ledger.test.js
@docs/FLEE.md
</context>

<standing_rules>
- The BEFORE pins are NEVER regenerated: `git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json` must exit 0 at every step of this plan.
- Same seed counts and modes as the BEFORE pins, read from their `meta` (natural: `seeds 40`, `workers 4`, `maxActions 5000`, `startDepth 1`, `exploreBudget 50`; depth-20: `seeds 10`, `workers 4`, `maxActions 5000`, `startDepth 20`). Pass `--workers 4` explicitly (the CLI default is `os.availableParallelism()`, and `workers` is a parity field). Never sample down.
- Wall time: the BEFORE natural run took 852.8 s; with the taught bot expect 15-30 minutes — longer than a foreground tool call's 10-minute cap. Launch each matrix with the Bash tool's `run_in_background: true`, redirecting stdout+stderr to a log file in the session scratch directory and appending `EXIT=$?`; then wait with a Monitor/until-loop polling the log for the `EXIT=` line (never `sleep` in the foreground). Record the stderr `elapsed:` line verbatim as the wall time. Run the two matrices SEQUENTIALLY (natural first) so worker counts are honest.
- CONTEXT Area 2 (user-chosen): every out-of-band row gets a written verdict against the depth-20 target. Tune ONLY when one content number (an ability `cd`, a spell `dmg`/`rounds`/`pool`, a staff `charges`/`recharge`, a potion `effect`/`might`, an item `every`/`cd`, a weapon die, an armor number) clearly explains the row — ONE change, ONE re-run of BOTH matrices, both readings recorded. Everything else: accept with its reason and list it for the user's device round. No engine-rule change beyond the flee retune already landed (Plan 01). A bot flee/parley threshold is NOT a one-knob candidate here (it is printed in the `Bot:` line and would break BEFORE/AFTER parameter parity) — record such a row as "accepted — threshold-dominated; revisit in the next tuning pass". A SECOND tuning loop is forbidden: if the one re-run is still out of band, accept with reason.
- No ablation column (CONTEXT Claude's Discretion: "plus an optional ablation column if cheap"): a second full 143×40 matrix with tactics off is not cheap (another 15-30 minutes) and would not be like-for-like with either pin; the pick-rate tables are the interpretability substitute — say so in `### What the bot now does`.
- A cannot-act cell or a stuck run at 40 seeds is a harness DEFECT (Rule 1), not a balance row: fix the bot guard, re-run both matrices — at most ONE defect re-run; if a second is needed, STOP and report to the orchestrator with the reading.
- Deferred UAT: no device steps, no `adb`, no APK. Verification agents OFF: Task 3 runs the WHOLE-PHASE gate and records the numbers.
- This is the only Phase 42 plan allowed to touch `mazeworld.html`; none is expected — if Plans 01-03 recorded an unavoidable shell edit, do it here with a source-pin test, else leave the shell alone.
- Commits per task: `feat(42-04): …` / `test(42-04): …` / `docs(42-04): …` with the two trailers (`Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`, `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`). Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
See 42-01-PLAN.md's phase-wide list. This plan creates: `docs/class-pass/v15-after.json`, `docs/class-pass/v15-after-depth20.json`, `docs/class-pass/v15-verdicts.json` (schema `class-pass-verdicts/1`), optionally `docs/class-pass/v15-after-pretune.json` / `v15-after-pretune-depth20.json`, the `## v1.5 AFTER — commit … (Phase 42 — BAL-02)` section of `docs/CLASS-PASS.md` (with `### Pin and provenance (Phase 42 capture)`, `### What the bot now does`, `### Pooled rollups (v1.5 BEFORE → AFTER)`, `### Depth-20 target verdict`, the rendered `--section after` block, `### Out-of-band verdicts`, `### Pick-rates — abilities/spells/items`, `### Top picks by sub-class`, `### One-knob tuning` (or "none"), the two transcripts), the re-pinned `test/unit/class-pass-ledger.test.js`, REQUIREMENTS.md FLEE-01/FLEE-02/BAL-02 complete, `docs/FLEE.md` requirements-map note, 42-04-SUMMARY.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run the ONE AFTER matrix pair (natural + depth-20) in the background, gate it, record provenance</name>
  <files>docs/class-pass/v15-after.json, docs/class-pass/v15-after-depth20.json</files>
  <read_first>
    - .planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-01-SUMMARY.md (how the BEFORE pins were captured: the two commands, the `git diff --quiet` provenance checks, the wall times, the transcript handling)
    - docs/CLASS-PASS.md lines 1591-1640 (`### Pin and provenance (Phase 36 capture)` — the provenance paragraph shape to mirror)
    - tools/tune-classes.mjs lines 1-60 (usage; stderr-only `elapsed:` line; `--out` atomic write; `--json` vs text) and lines 150-180 (commit capture via `git rev-parse --short HEAD` — commit the tree BEFORE launching so `meta.commit` is a real commit)
    - .planning/phases/42-flee-retune-consolidated-balance-close/42-03-SUMMARY.md `## Next Phase Readiness` (the extrapolated wall time)
    - tools/class-pass-diff.mjs lines 85-100 (`--gate` semantics: exit 3 on any cannot-act cell)
  </read_first>
  <action>
    A. Provenance BEFORE launching: `git status --porcelain` must be empty (commit anything pending first — the matrix's `meta.commit` must name a real commit); record `git rev-parse HEAD` (full) and `git rev-parse --short HEAD`; assert `git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json` exits 0; run `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-before.json` (expect `cannot-act cells: 0 of 143`, exit 0) as the "harness still reads the pin" check.

    B. Natural matrix (background + log): the command is `node tools/tune-classes.mjs --seeds 40 --workers 4 --max-actions 5000 --out docs/class-pass/v15-after.json`, stdout and stderr redirected to `<scratch>/v15-after.txt`, followed by an `EXIT=$?` echo into the same log; launch with `run_in_background: true`; poll the log (Monitor/until-loop, ~60 s cadence) until the `EXIT=` line appears; record the `elapsed:` line and `EXIT=0`. Then the depth-20 slice the same way: `node tools/tune-classes.mjs --seeds 10 --workers 4 --max-actions 5000 --start-depth 20 --out docs/class-pass/v15-after-depth20.json` → `<scratch>/v15-after-depth20.txt`. Keep both logs — Task 2 pastes them as the transcripts.

    C. Gate + sanity: `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-after.json` must print `cannot-act cells: 0 of 143` (exit 0); `node -e` checks: both files' `meta.commit` equal the short hash from step A; `meta.seeds` 40/10; `meta.workers` 4; `meta.runFlags` deep-equals `{ storeRoll: true, wornSlots: true }`; `meta.bot` byte-equals the matching BEFORE pin's `meta.bot`; every cell `stuck === 0`; 143 cell keys identical to the BEFORE pin's. A cannot-act cell or a stuck cell → the Rule-1 defect procedure in the standing rules (fix the guard in `tools/lib/tuning-bot.mjs` with a test, commit, re-run BOTH matrices ONCE, record both readings for the ledger); a `meta.bot` mismatch means a Plan 02/03 regression — fix it, never edit the JSON.

    D. Commit `feat(42-04): v1.5 AFTER class-matrix pair (143x40 natural, 143x10 depth-20) — abilities+gear+spells+terrain+flee, taught bot` (explicit paths: the two JSON files). Record in the SUMMARY: both commands, both `elapsed:` lines, `EXIT=0` twice, the commit hash, the gate line, the `git diff --quiet` provenance result.
  </action>
  <acceptance_criteria>
    - `test -f docs/class-pass/v15-after.json && test -f docs/class-pass/v15-after-depth20.json` succeeds
    - `node -e "const a=require('./docs/class-pass/v15-after.json'),d=require('./docs/class-pass/v15-after-depth20.json'),b=require('./docs/class-pass/v15-before.json'),bd=require('./docs/class-pass/v15-before-depth20.json');process.exit(a.meta.seeds===40&&d.meta.seeds===10&&a.meta.workers===4&&d.meta.workers===4&&a.meta.startDepth===1&&d.meta.startDepth===20&&a.meta.bot===b.meta.bot&&d.meta.bot===bd.meta.bot&&a.meta.commit===d.meta.commit&&a.meta.runFlags.wornSlots===true&&a.cells.length===143&&d.cells.length===143&&a.cells.every(c=>c.stuck===0)&&d.cells.every(c=>c.stuck===0)?0:1)"` exits 0
    - `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-after.json` prints `cannot-act cells: 0 of 143` and exits 0
    - `git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json` exits 0
    - `git log --oneline -1 -- docs/class-pass/v15-after.json` shows a `feat(42-04)` commit
  </acceptance_criteria>
  <verify>
    <automated>node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-after.json && git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json</automated>
  </verify>
  <done>The AFTER pair exists at the BEFORE's exact parameters, zero stuck, zero cannot-act, committed with real provenance and recorded wall times; the BEFORE pins untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Diff vs BEFORE, verdict every out-of-band row, one-knob rule, the `## v1.5 AFTER` ledger section with pick-rates, ledger test re-pin</name>
  <files>docs/class-pass/v15-verdicts.json, docs/CLASS-PASS.md, docs/FLEE.md, test/unit/class-pass-ledger.test.js, docs/class-pass/v15-after.json, docs/class-pass/v15-after-depth20.json</files>
  <read_first>
    - tools/class-pass-diff.mjs lines 1-100 (bands: too weak < 0.75 mu, fine 0.75-1.35 mu closed, too strong > 1.35 mu; cannot-act rule; mu run-weighted over AFTER natural; the verdicts.json schema — `subs`/`races`/`cells.outOfBand`/`deep`; `--verdicts` merge by key; `--section after` renders the tables WITHOUT an H2), 696-730 (`renderAfter`'s exact H3 headings)
    - docs/CLASS-PASS.md lines 860-1030 (the v1.2 AFTER section: headline lines, the verdict/reason column style — one sentence per row, in voice, naming the numbers), 1026-1072 (`### Pin and provenance (Phase 26 capture)`), 1591-1640 (the v1.5 BEFORE section: `### Pooled rollups` table shape to mirror as BEFORE → AFTER)
    - test/unit/class-pass-ledger.test.js (all of it: `h2s`/`section`, the nine-heading pin at line 84, the v1.2 AFTER pin tests 161-300 — copy their shape for the v1.5 AFTER: hash, meta parity, 143 keys, zero cannot-act/stuck, verdicts schema + editorial completeness, verbatim Bot lines)
    - docs/DIFFICULTY-RETUNE.md `## v1.2 retune (Phase 27)` band table (the depth-20 target language) and the memory ruling: tune toward depth 20, not infinite; 20 is a unicorn (rare, not expected); past 20 leave the curve alone
    - docs/CLASS-PASS.md `### Phase 42 tactics (BAL-01 second half)` (Plan 03 — the source of "what the bot now does")
    - The two logs from Task 1
  </read_first>
  <action>
    A. Render: `node tools/class-pass-diff.mjs --before docs/class-pass/v15-before.json --after docs/class-pass/v15-after.json --before-deep docs/class-pass/v15-before-depth20.json --after-deep docs/class-pass/v15-after-depth20.json --section after --out-verdicts docs/class-pass/v15-verdicts.json` → save stdout to scratch. Read `v15-verdicts.json`: `meta.parity.natural.ok` and `.deep.ok` must both be true (else Task 1's parameters were wrong — fix there, never here); note `meta.mu`, `subs[].band`, `races[].band`, `cannotAct` (must be empty), `cells.outOfBand`.

    B. Depth-20 target verdict (planner-set thresholds, Claude's discretion — state them as such in the ledger): the pooled natural `reach20` must be ≤ 1.0% (a depth-20 run stays a unicorn) and the depth-20 slice pooled `meanFloorsGained` ≤ 2.0 (a level-5 hero dropped at 20 does not stroll on); `reach10` between 0.5% and 10% is the healthy band for "meaningful choices made the run longer, not trivial". If a threshold is exceeded, the pooled verdict is "too strong vs the depth-20 target" and the one-knob rule applies to the ONE number that explains it (e.g. the pick-rate tables show one ability/spell/item dominating every top-picks row); if nothing explains it, accept with reason and list for the user's device round.

    C. Verdicts: for EVERY `subs[]` and `races[]` row whose `band` is not `fine`, and for the pooled verdict, write `verdict` (`accept` | `tune`), `reason` (one sentence, in voice, naming BEFORE/AFTER numbers, the pick-rate evidence and the depth-20 target) and `lever` (the one content number, or null) into `docs/class-pass/v15-verdicts.json` by key (edit the JSON in place — keep every machine field untouched). Out-of-band CELLS (the appendix) get a one-line group verdict in the ledger prose per sub pattern, not per cell. Then re-render with `--verdicts docs/class-pass/v15-verdicts.json --out-verdicts docs/class-pass/v15-verdicts.json` so the tables carry the editorial text, and confirm the render is byte-stable (run twice, `cmp`).

    D. One-knob tuning (ONLY if a row's verdict is `tune`): rename the current AFTER pair to `docs/class-pass/v15-after-pretune.json` / `v15-after-pretune-depth20.json` (git mv), change the ONE content number with a one-line DELIBERATE TUNING comment naming Phase 42/BAL-02 and the row, re-pin any unit test that literally pins that number (with the same rationale in the test comment), commit `feat(42-04): one-knob tune — <file>#<field> <old> -> <new> for <row>`, re-run BOTH matrices exactly as Task 1 (background, logs, gate, provenance), re-render and re-verdict (step A-C again, once). The ledger records the pretune and final readings side by side. A second loop is forbidden — if still out of band, the verdict becomes `accept` with the reason "one-knob tune of <lever> moved it <from> → <to>, still out of band; accepted, listed for the device round".

    E. Pick-rates: `node tools/class-pass-usage.mjs --after docs/class-pass/v15-after.json --deep docs/class-pass/v15-after-depth20.json` → scratch (byte-stable; run twice, `cmp`).

    F. `docs/CLASS-PASS.md` — append the TENTH H2 after the v1.5 BEFORE section's last transcript: `## v1.5 AFTER — commit <40-hex> (Phase 42 — BAL-02)` whose first non-blank line is `**Zero cannot-act cells.** …` (mirror the BEFORE headline with the gate command and the exit code), then the rendered `--section after` block's own first three lines (headline/parity/mu — as rendered), then in this order: `### Pin and provenance (Phase 42 capture)` (date, full + short hash, both commands verbatim, both `elapsed:` lines, `EXIT=0`, `meta.runFlags` and WHY the run flags differ from the BEFORE (the shipped game's rules; the BEFORE predates `wornSlots`), the `git diff --quiet` result for the BEFORE pins, the taught-bot statement with a link to `### Phase 42 tactics`, the flee retune landed (link `docs/FLEE.md`), the pretune note if any); `### What the bot now does` (one paragraph per area — abilities/items/loot+run flags/spells — so the numbers are interpretable; from Plan 03's addendum); `### Pooled rollups (v1.5 BEFORE → AFTER)` — the BEFORE section's table shape with a third numeric column (v1.5 AFTER) and a Δ column, every value read from the JSON (`rollups.pooled.*`, `sum of cells[].stuck`, the gate line); `### Depth-20 target verdict` (the thresholds, the readings, the verdict sentence); the rendered `--section after` tables verbatim (By class … Out-of-band cells); `### Out-of-band verdicts` (every non-fine sub/race row restated as `| Row | Band | BEFORE | AFTER | Verdict | Reason | Lever |` — the same text as in v15-verdicts.json — plus the cells-appendix group verdicts); `### One-knob tuning` ("none — every out-of-band row accepted with reason" or the lever, pretune vs final readings, and the reason it was the ONE number); `### Pick-rates — abilities`, `### Pick-rates — spells`, `### Pick-rates — items`, `### Top picks by sub-class` (the `class-pass-usage` render verbatim, natural first, then the `## Pick-rates — depth-20 slice` block DEMOTED to `### Pick-rates — depth-20 slice` so the H2 count stays ten — do this demotion by hand and say so); `### Listed for the user's device round` (every accepted out-of-band row as a bullet); `### v1.5 AFTER transcript — tune-classes --seeds 40 --workers 4 --max-actions 5000` and `### v1.5 AFTER transcript — tune-classes --seeds 10 --workers 4 --max-actions 5000 --start-depth 20` (the two logs verbatim in fenced blocks, `Bot:`/`Stuck:`/`elapsed:`/`EXIT=0` lines kept, as the BEFORE did). Also update the v1.5 BEFORE section's last sentence ("The v1.5 AFTER matrix and its verdicts are appended below this section by Phase 42") to "… appended below (Phase 42, BAL-02)". `docs/FLEE.md` `## Requirements map`: add the AFTER-section cross-link line.

    G. `test/unit/class-pass-ledger.test.js` re-pin: the heading test becomes "exactly TEN H2 headings, in the fixed order, v1.5 AFTER last" (append `/^## v1\.5 AFTER — commit [0-9a-f]{40} \(Phase 42 — BAL-02\)$/`); add v1.5 AFTER tests mirroring the v1.2 AFTER ones: heading hash starts with `v15-after.json` meta.commit and both AFTER files share it; meta parity (`metaParity`) v15-before ↔ v15-after and deep pair OK, `meta.bot` byte-equal per pair, `meta.runFlags` deep-equals `{ storeRoll: true, wornSlots: true }` in both AFTER files; identical 143 keys vs the BEFORE pins; zero cannot-act (`cannotActCells`) and zero stuck; `v15-verdicts.json` schema `class-pass-verdicts/1`, `meta.after.commit` equals the AFTER meta.commit, `meta.parity.natural.ok && deep.ok`, and EVERY `subs[]`/`races[]` row with `band !== "fine"` has a non-null `verdict` AND `reason` (editorial completeness — the CONTEXT Area 2 rule as a test); the section contains both verbatim `Bot:` lines, `### Pin and provenance (Phase 42 capture)`, `### Depth-20 target verdict`, `### Pick-rates — abilities`, exactly two `### v1.5 AFTER transcript` sub-headings, and its first non-blank line starts with `**Zero cannot-act cells.**`; the `--section after` block of the doc is byte-identical to a fresh render with `--verdicts docs/class-pass/v15-verdicts.json` (copy the v1.2 "byte-identical to a fresh render" test's mechanism, scoped to the v1.5 section); if a pretune pair exists, its `meta.commit` differs from the final's and the section mentions `v15-after-pretune`.

    H. Run `node --test test/unit/class-pass-ledger.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-usage.test.js test/unit/flee-ledger.test.js` — green. Commit `docs(42-04): v1.5 AFTER ledger — BEFORE→AFTER diff, verdict per out-of-band row, depth-20 target verdict, pick-rates; ledger test re-pinned` (explicit paths: docs/CLASS-PASS.md docs/class-pass/v15-verdicts.json docs/FLEE.md test/unit/class-pass-ledger.test.js, plus any pretune JSONs).
  </action>
  <acceptance_criteria>
    - `grep -c "^## " docs/CLASS-PASS.md` is 10; `grep -cE "^## v1\.5 AFTER — commit [0-9a-f]{40} \(Phase 42 — BAL-02\)$" docs/CLASS-PASS.md` is 1
    - `grep -c "^### Pin and provenance (Phase 42 capture)" docs/CLASS-PASS.md` is 1; `grep -c "^### Depth-20 target verdict" docs/CLASS-PASS.md` is 1; `grep -c "^### Out-of-band verdicts" docs/CLASS-PASS.md` is 1; `grep -c "^### One-knob tuning" docs/CLASS-PASS.md` is 1; `grep -c "^### Pick-rates — abilities" docs/CLASS-PASS.md` is 1; `grep -c "^### v1\.5 AFTER transcript — tune-classes" docs/CLASS-PASS.md` is 2
    - `node -e "const v=require('./docs/class-pass/v15-verdicts.json');const rows=[...v.subs,...v.races].filter(r=>r.band!=='fine');process.exit(v.schema==='class-pass-verdicts/1'&&v.meta.parity.natural.ok&&v.meta.parity.deep.ok&&v.cannotAct.length===0&&rows.every(r=>typeof r.verdict==='string'&&r.verdict.length>0&&typeof r.reason==='string'&&r.reason.length>20)?0:1)"` exits 0
    - `node tools/class-pass-diff.mjs --before docs/class-pass/v15-before.json --after docs/class-pass/v15-after.json --before-deep docs/class-pass/v15-before-depth20.json --after-deep docs/class-pass/v15-after-depth20.json --verdicts docs/class-pass/v15-verdicts.json --section after > /tmp/r1.md; node tools/class-pass-diff.mjs --before docs/class-pass/v15-before.json --after docs/class-pass/v15-after.json --before-deep docs/class-pass/v15-before-depth20.json --after-deep docs/class-pass/v15-after-depth20.json --verdicts docs/class-pass/v15-verdicts.json --section after > /tmp/r2.md; cmp /tmp/r1.md /tmp/r2.md` exits 0
    - `node --test test/unit/class-pass-ledger.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-usage.test.js test/unit/flee-ledger.test.js` exits 0 and the ledger test file reports at least 20 tests
    - `git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json` exits 0
    - If `docs/class-pass/v15-after-pretune.json` exists: `grep -c "v15-after-pretune" docs/CLASS-PASS.md` is at least 2 and `git log --oneline | grep -c "one-knob tune"` is exactly 1
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/class-pass-ledger.test.js test/unit/class-pass-diff.test.js test/unit/class-pass-usage.test.js test/unit/flee-ledger.test.js</automated>
  </verify>
  <done>The AFTER is diffed, every out-of-band row has a written verdict against the depth-20 target, at most one knob moved once, the pick-rates are on the page, and the ledger is machine-pinned so none of it can drift.</done>
</task>

<task type="auto">
  <name>Task 3: REQUIREMENTS.md complete, whole-phase gate, phase-close SUMMARY with the aggregated Pixel 7 checklist and `## For PROJECT.md`</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md lines 45-48 (FLEE-01/02 checkboxes), 69-72 (BAL-01/02), 100 and 128-130 (the traceability rows)
    - .planning/phases/42-flee-retune-consolidated-balance-close/42-01-SUMMARY.md, 42-02-SUMMARY.md, 42-03-SUMMARY.md (their `## Human verification (deferred to end of run)` lists and `## Corrections to CONTEXT` — the inputs to aggregate)
    - .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md lines 206-275 (the Success Criteria Map, `## For PROJECT.md`, the aggregated checklist format: grouped by plan, numbered continuously, de-duplicated with "(also Plan NN)", caveats resolved)
    - .planning/ROADMAP.md `### Phase 42` success criteria 1-4
  </read_first>
  <action>
    A. `.planning/REQUIREMENTS.md`: flip `- [ ] **FLEE-01**`, `- [ ] **FLEE-02**`, `- [ ] **BAL-02**` to `- [x]` (text untouched) and the three traceability rows `| FLEE-01 | Phase 42 | Pending |`, `| FLEE-02 | Phase 42 | Pending |`, `| BAL-02 | Phase 42 | Pending |` to `Complete` (BAL-01's row already reads Complete — leave it). Commit `docs(42-04): REQUIREMENTS.md — FLEE-01, FLEE-02, BAL-02 complete` (explicit path).

    B. WHOLE-PHASE GATE (verification agents are off — this IS the verification): `npm test` (`# fail 0`; record `# tests`/`# pass`; the count must be at least 2915 plus this phase's new tests), `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`, `npm run build:www` exit 0 (Plan 01 touched `src/browser`), `git status --porcelain test/parity/fixtures` empty (or exactly Plan 01's declared record, already committed), `git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json` exit 0, `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-after.json` → `0 of 143`, `git status --porcelain` empty except the two untracked user directories (`store-listing/`, `tools/store-screenshots/`) which are never staged. Put every reading in the SUMMARY.

    C. Write `42-04-SUMMARY.md` in the 41-04 format with, after the standard sections: `## Success Criteria Map (ROADMAP SC-1..4)` — SC-1: the fight-log line (roll, named modifiers, need) before the outcome, proof files; SC-2: 35% base / Thief 60% / Plate 25%, the ledger's before/after table, the declared divergence (and the measured seed-17 reading); SC-3: the failed flee's foeTurn + round advance, proof tests; SC-4: the AFTER pair, the pick-rate tables, the per-sub verdicts, the depth-20 target verdict, the one-knob record (or none); `## For PROJECT.md` — (1) Flee canon change: `d20 + Thief 5 + class + race − armor bulk ≥ 14` (35% base; every modifier bounded ±2 and named in the fight log; failed flee unchanged) — `docs/FLEE.md`; (2) the AFTER verdict headline: pooled meanDepth/reach5/reach10/reach20 BEFORE → AFTER, the depth-20 target verdict sentence, the count of out-of-band rows accepted vs tuned, and the one lever if any — `docs/CLASS-PASS.md` v1.5 AFTER; (3) the bot now plays the shipped run rules (`storeRoll`/`wornSlots`), takes loot and uses abilities/items/spells by the Joiner policies — `### Phase 42 tactics`; this plan does NOT edit PROJECT.md (orchestrator's job at phase close); `## Human verification (deferred to end of run)` — the AGGREGATED phase checklist: Plan 01's five items (FLEE submenu cost/desc for this character; the fight-log flee line and its tap-reveal dice; a failed flee's visible foe swings + round advance; Samurai/Cloaker/WITHDRAW unchanged; voice read), Plans 02/03 "none — dev harness and ledgers only", Plan 04: (a) play three fights and flee in each — the shown need matches `docs/FLEE.md`'s table for that character; (b) for each row listed under `### Listed for the user's device round`, one short play session with that sub-class to feel whether the accepted out-of-band reading matches the hand-play; grouped by plan, numbered continuously, de-duplicated; `## Corrections to CONTEXT` (aggregate Plans 01-03's corrections: the six real races; the combat-submenu cost string; the bot's ignored loot pile and legacy run flags; the seed-17 reading; anything Plan 04 found); `## Next Phase Readiness` (Phase 43 Clarity Pass: every costly line names its cause — the `fleeRolled` mods and the item/ability refusal reasons are already payload-sourced; the milestone-close UAT batch now holds Phases 36-42's checklists; the "Shell Debt & Dead Code" milestone still owns the legacy shell copies). Self-check: every file named in Plans 01-04 exists with the expected content; every commit hash cited is in `git log`.
  </action>
  <acceptance_criteria>
    - `grep -cE "^- \[x\] \*\*(FLEE-01|FLEE-02|BAL-02)\*\*" .planning/REQUIREMENTS.md` is 3; `grep -cE "^\| (FLEE-01|FLEE-02|BAL-02) \| Phase 42 \| Complete \|" .planning/REQUIREMENTS.md` is 3; `grep -cE "^\| (FLEE-01|FLEE-02|BAL-02) \| Phase 42 \| Pending \|" .planning/REQUIREMENTS.md` is 0
    - `npm test` output ends with `# fail 0` and `# pass` is at least 2915 + 60
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0
    - `git diff --quiet HEAD -- docs/class-pass/v15-before.json docs/class-pass/v15-before-depth20.json` exits 0; `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-after.json` prints `cannot-act cells: 0 of 143`
    - `.planning/phases/42-flee-retune-consolidated-balance-close/42-04-SUMMARY.md` exists and contains `## Success Criteria Map`, `## For PROJECT.md`, `## Human verification (deferred to end of run)`, `### Plan 01`, `### Plan 04`, `## Corrections to CONTEXT`, `## Next Phase Readiness`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && npm run build:www && node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-after.json</automated>
  </verify>
  <done>FLEE-01/02 and BAL-02 are complete in REQUIREMENTS.md, the whole phase passes its gate with the master untouched and the bundle building, and the milestone-close batch has the phase's checklist and PROJECT.md hand-off.</done>
</task>

</tasks>

<verification>
- Both AFTER files: `meta.bot` equals the BEFORE pins', `meta.runFlags` present, 143 keys, zero stuck, zero cannot-act; BEFORE pins byte-unchanged.
- Every out-of-band sub/race row has a written verdict + reason (test-enforced); at most one knob moved once.
- `docs/CLASS-PASS.md` has ten H2s, the AFTER last; renders byte-identical to the tool.
- Whole-phase gate green: `npm test` `# fail 0`, master hash, `build:www`, fixtures clean.
</verification>

<success_criteria>
- ROADMAP SC-4 met: ONE consolidated AFTER run, pick-rates for every new spell/ability, a fun-band verdict per sub-class, out-of-band rows tuned (one knob, one re-run) or accepted with a written reason against the depth-20 target.
- FLEE-01/FLEE-02/BAL-02 complete; phase closed with the aggregated checklist and `## For PROJECT.md`.
</success_criteria>

<output>
Create `.planning/phases/42-flee-retune-consolidated-balance-close/42-04-SUMMARY.md` when done (phase close: Success Criteria Map, `## For PROJECT.md`, the aggregated Pixel 7 checklist, `## Corrections to CONTEXT`, `## Next Phase Readiness`, the gate numbers).
</output>
