---
phase: 43-clarity-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/movement.js
  - engine/magic.js
  - engine/encounters.js
  - content/races.js
  - content/skills.js
  - content/potions.js
  - content/treasure-tables.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - test/parity/harness/comparables.js
  - test/parity/chargen-parity.test.js
  - test/parity/fixtures/action-script.economy.json
  - test/parity/FIXTURE-INVENTORY.md
  - test/voice/safety-scan.test.js
  - test/unit/clarity-cause-lines.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/rail.test.js
  - test/unit/narrativeToasts.test.js
  - test/unit/loot-narration.test.js
  - test/unit/abilities-catalog.test.js
  - docs/CLARITY.md
autonomous: true
requirements: [CLAR-01]
tags: [engine, narration, cause-lines, hp-not-wp, additive-payload, ledger, deferred-uat]

must_haves:
  truths:
    - "Every hazard/affliction/magic/store/item/loot/company event whose Oracle line states a cost now reads cause-first, cost-last — `<Cause>: <plain-language why>. −N hp.` — with the cause sourced from the event payload (ROADMAP SC-1, CLAR-01, CONTEXT §CLAR-01); the trappedPanic line renders exactly `Being trapped: four walls and one door you already used. −4 hp.` for a loss of 4 (ROADMAP SC-1's literal)"
    - "Where a payload lacked its cause, an ADDITIVE key was added (trappedPanic.phobia, heightsFear.penalty, waterFear.penalty, backfireSelfDamage.spell/.sub, summonBackfired.spell/.sub, earthquakeSelfDamage.spell, deathCast.cost, deathSpellTooWeak.fee, insanitySelfHarm.loss) — no existing key renamed or removed, no new serialized field, no new rng draw (Engine Gate + CONTEXT standing rulings)"
    - "The cost-event inventory exists as a machine-checked table in `docs/CLARITY.md` (event · cost kind · payload cause key present?/added · line before/after · owner plan) covering every cost-bearing event Phases 36-42 and earlier introduced; the day-cycle rows (dayBegan/rationsEaten/wentHungry/rested/campFailed) are listed with owner Plan 02"
    - "No player-facing string in the narration tables, the exported COPY objects, the content banks or `mazeworld.html` player copy says `wp`/`WP` (HP not WP standing ruling) — a grep-pin test guards it; code identifiers (`c.wp`, `maxWP`, `-wp` class fragments, `wp:` keys) are exempt by construction"
    - "The five reworded content `txt` strings move zero parity fixtures: the Phase 28 cosmetic-txt carve-out is generalised to `REWORDED_TXT_ITEMS` (measured exposure: chargen seeds 2 and 4 + encounters seed 160 roll a Cloak of Regeneration, movement seed 256 rolls a Cloak of Healing) and the economy fixture's declared `after.items[1].txt` is re-measured to `+d10+2 hp` — the master is never edited"
    - "`toastsCoverage.test.js`, `formatEventsCoverage.test.js` and the voice scan stay green (ROADMAP SC-5)"
  artifacts:
    - "engine/movement.js: `trappedPanic { loss, phobia }`, `heightsFear { penalty }`, `waterFear { penalty }` (additive keys)"
    - "engine/magic.js: `backfireSelfDamage { amount, spell, sub }`, `summonBackfired { amount, spell, sub }`, `earthquakeSelfDamage { amount, spell }`, `deathCast { cost: 25 }`, `deathSpellTooWeak { fee: 25 }`"
    - "engine/encounters.js: `insanitySelfHarm { loss }`"
    - "src/browser/eventNarration.js + src/browser/toasts.js: cause-first rewrites for the 24 Plan-01 rows (list in Task 2)"
    - "content/*.js: five `txt` rewords + two RACES notes + Cooking txt (wp -> hp)"
    - "test/parity/harness/comparables.js#REWORDED_TXT_ITEMS (exported) consulted by the existing stripCloakArmorTxt; chargen-parity applies it on both sides"
    - "docs/CLARITY.md (new): `## Cost-event inventory (CLAR-01)` table + `## HP not WP sweep` section"
    - "test/unit/clarity-cause-lines.test.js, test/unit/hp-not-wp.test.js (new)"
    - "test/parity/FIXTURE-INVENTORY.md `## Phase 43 — clarity pass (CLAR-01..05)` section (Plan 01 entry)"
  key_links:
    - "engine push site adds the additive key -> EVENT_NARRATION/TOAST_FOR builders read it with `??` defaults (the coverage guards call every builder with a bare `{type}`) -> docs/CLARITY.md row names the key -> clarity-cause-lines.test.js renders the builder and asserts the `^<Cause>: ` shape"
    - "content txt reword -> comparables.js REWORDED_TXT_ITEMS strip (three comparable chains + chargen-parity both sides) -> zero fixture regeneration; economy fixture `after.items[].txt` re-measured (declared record, not a blanket regen)"
---

<objective>
Sweep every cost-bearing event outside the day cycle (traps, falls, phobia panics/penalties, afflictions, water, summon/backfire/earthquake/Death self-costs, foe bolts and drains, store purchases, tool and item consumption, forfeited loot, the Cutthroat's murder, the faerie's bane, insanity self-harm) so its Oracle and toast lines name the cause first and the cost last, sourced from the event payload; add the missing cause keys ADDITIVELY; inventory the whole cost surface in `docs/CLARITY.md`; and finish the HP-not-WP sweep with a grep-pin test — including the five content `txt` strings the engine still ships with "wp", handled as a measured, zero-regeneration cosmetic carve-out.

Purpose: ROADMAP SC-1 (CLAR-01) — "a costly Oracle/rail line names its cause in plain language sourced from the event payload". The Phase 41 `phobiaTriggered`/`trappedPanic` lines are the model; this plan makes every other cost read the same way, and proves no cause string bypasses the coverage guards (SC-5).

Output: the additive engine keys, the rewritten narration rows, `docs/CLARITY.md` with the inventory, `REWORDED_TXT_ITEMS`, the two new tests, re-pinned existing tests, the FIXTURE-INVENTORY Phase 43 section, 43-01-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-clarity-pass/43-CONTEXT.md
@engine/movement.js
@engine/magic.js
@engine/encounters.js
@engine/combat.js
@engine/foeAbilities.js
@engine/death.js
@src/browser/eventNarration.js
@src/browser/toasts.js
@src/browser/rail.js
@test/parity/harness/comparables.js
@test/parity/chargen-parity.test.js
@test/parity/FIXTURE-INVENTORY.md
@test/voice/safety-scan.test.js
@docs/TERRAIN.md
@docs/FLEE.md
</context>

<standing_rules>
- Engine gate: pure/deterministic; NO new rng draw anywhere in this plan; NO new serialized field (every addition is an event-payload key, and events are never serialized into state); `test/parity/prototype-master.js.txt` is NEVER edited (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`).
- Additive-only payloads: never rename or remove an existing event key. Parity fixtures compare STATE (`diffState` of prototype vs engine), not events, so an additive event key moves zero fixtures — but unit tests that `deepStrictEqual` an event shape are pins to re-point with a stated reason (movement.test.js:984 pins `campFailed` exactly — this plan does not touch campFailed).
- Greenfield ruling (STATE.md amendment 2026-09-17): a display sweep moves zero fixtures. The one content-text reword that a fixture stores (`c.items[].txt`) follows the Phase 28 `stripCloakArmorTxt` cosmetic carve-out precedent and the Phase 39 `stripReauthoredEveryField` chargen-parity precedent — measured live (Task 3), declared in FIXTURE-INVENTORY.md, never a blanket regeneration.
- Comment-text discipline: the acceptance criteria below negative-grep for the OLD sentence fragments; do not paste those old fragments into new code comments (describe them as "the old line").
- No `mazeworld.html` edits in this plan (Plan 04 is the only plan allowed to touch the shell). If a shell edit looks unavoidable, STOP and record it for Plan 04 in the SUMMARY.
- Deferred UAT: no device steps, no `adb`, no APK. The SUMMARY carries `## Human verification (deferred to end of run)`.
- Verification agents are OFF: this plan's LAST task runs its own gate (`npm test` `# fail 0`, master hash, `npm run build:www` exit 0 because `src/browser` is touched) and records the numbers in the SUMMARY.
- Voice: family-friendly, deadpan; every builder null-safe (`e?.x ?? default`) because the coverage guard and the voice scan call every builder with a bare `{ type }`; every new branch gets a BRANCH_TOGGLES entry in `test/voice/safety-scan.test.js` so both sides render under the scan.
- Commits per task: `feat(43-01): …` / `test(43-01): …` / `docs(43-01): …`, each ending with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01Fe6YxyB87uunKB5vm2HkdS`. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<source_audit>
Multi-source coverage audit for the Phase 43 plan set (GOAL / REQ / RESEARCH / CONTEXT). No RESEARCH.md exists by policy (roadmap flag: standard display sweep) — RESEARCH rows are the ROADMAP research-flag sentence.

| Source | Item | Covered by |
|---|---|---|
| GOAL | every cost names its cause | Plan 01 (non-day-cycle rows) + Plan 02 (day-cycle rows) |
| GOAL | every loot offer shows who can use it | Plan 03 (`usableBy`) + Plan 04 (FIND card / LOOT screen / store rows) |
| GOAL | ration math honest and matches Make Camp | Plan 02 (`eatsFor`/`nightlyEats`/`rationsViewModel`) + Plan 04 (Hero RATIONS panel) |
| GOAL | Gear screen tells the truth about worn vs carried | Plan 03 (`emptySlotRows`/`dropShelfItems`/`GEAR_COPY`) + Plan 04 (ON YOU / BAG) |
| REQ | CLAR-01 | Plan 01 + Plan 02 (day-cycle lines) |
| REQ | CLAR-02 | Plan 03 + Plan 04 |
| REQ | CLAR-03 | Plan 02 + Plan 04 |
| REQ | CLAR-04 | Plan 03 + Plan 04 |
| REQ | CLAR-05 | Plan 02 (audit, `docs/RATIONS.md`, rest narration naming rules, `eatsLineFor`) + Plan 04 (offer card + Company panel) |
| RESEARCH | "verify each earlier phase added its cause payload rather than retrofitting" | Plan 01 Task 1 inventory (present?/added column) |
| CONTEXT | Greenfield / deferred UAT / HP-not-WP / coverage guards / voice | every plan's standing_rules; Plan 01 Task 3 (HP sweep) |
| CONTEXT | §CLAR-01 sweep list (traps, falls, hunger, afflictions, trappedPanic, summon backfire, Death potion, Earthquake, foe DOT, store, camp rations, tools, flee forfeit, Cutthroat) | Plan 01 Tasks 1-2 (hunger + camp rations -> Plan 02); "Death potion" corrected below |
| CONTEXT | §CLAR-02 `usableBy(item)` on FIND / LOOT / store / Joiner | Plans 03-04 (Joiner offer carries no item — recorded, see Plan 04) |
| CONTEXT | §CLAR-03/05 RATIONS block, audit, `docs/RATIONS.md`, "Trolls eat for two", Joiner eats N | Plans 02 + 04 |
| CONTEXT | §CLAR-04 ON YOU (Worn six + armor; Wielded weapon) / BAG; drop prompt bag-only | Plans 03 + 04 |
| CONTEXT | Deferred: tutorial hooks (UX-06), shell modularisation | NOT planned (deferred, correct) |

Verified-code corrections to CONTEXT (carried into every plan): (1) "Death potion" — there is no Death potion; the 25-hp self-cost is the Death SPELL (`engine/magic.js` `sp.kind === "death"`, event `deathCast`). (2) "foe DOT ticks on the hero" — no in-combat foe DOT on the hero exists; the hero's only DOT is the exploration `afflictionTick`; the foe-ability costs are `foeBolted`/`foeDrained`. (3) `nightlyEats` is at `engine/movement.js:549`, `newDay` :566, `makeCamp` :715 as CONTEXT says — but `RACES[c.race].eats` is read inline in three places (nightlyEats hero, nightlyEats members, makeCamp members) — Plan 02 factors `eatsFor`. (4) The Company panel ALREADY shows "Eats N a rest" (mazeworld.html:5469, inline RACES read) — Plan 04 re-points it at the view model rather than adding it. (5) The economy parity fixture stores `"txt": "+d10+2 wp"` on a bought Healing potion in its declared `after` record — the HP sweep must re-measure that record.
</source_audit>

<artifacts_this_phase_produces>
Phase-wide list (Plans 01-04):
- Plan 01: additive payload keys `trappedPanic.phobia`, `heightsFear.penalty`, `waterFear.penalty`, `backfireSelfDamage.spell`/`.sub`, `summonBackfired.spell`/`.sub`, `earthquakeSelfDamage.spell`, `deathCast.cost`, `deathSpellTooWeak.fee`, `insanitySelfHarm.loss`; cause-first `EVENT_NARRATION`/`TOAST_FOR` rows (24 events); content `txt`/`note` rewords; `test/parity/harness/comparables.js#REWORDED_TXT_ITEMS`; `docs/CLARITY.md` (inventory + HP sweep); `test/unit/clarity-cause-lines.test.js`, `test/unit/hp-not-wp.test.js`; FIXTURE-INVENTORY `## Phase 43` section.
- Plan 02: `engine/movement.js#eatsFor(sheet)`; the `rationsEaten { eats, left, eaters:[{ name, race, eats, hero? }] }` event; `wentHungry` additive `need`/`have`/`heft`; `EVENT_NARRATION.rationsEaten`/`TOAST_FOR.rationsEaten`/`RAIL_FAMILY.rationsEaten`; `eventNarration.js#RATION_RULE_LINE`; `src/browser/viewModels.js#rationsViewModel(state)`, `#eatsLineFor(sheet)`, `RATIONS_COPY`; `docs/RATIONS.md`; `test/unit/rations-audit.test.js`, `test/unit/rationsViewModel.test.js`.
- Plan 03: `src/browser/viewModels.js#usableBy(it, c)`, `USABLE_COPY`, `lootCompare(...).usable` (additive field), `#dropShelfItems(c)`, `#emptySlotRows(c)`, `GEAR_COPY`; `test/unit/usableBy.test.js`, `test/unit/gear-panels.test.js`; `docs/CLARITY.md` `## Loot legibility (CLAR-02)` + `## Gear screen (CLAR-04)`.
- Plan 04: `mazeworld.html` bridges `window.__mzUsableBy`, `window.__mzRations = { view, eatsLine }`, `window.__mzDropShelfItems`, `window.__mzGear = { emptySlotRows, copy }`; the FIND card / LOOT screen / store-row `usable` suffixes; the Joiner offer card's `eats N a rest`; the Company panel's bridged eats line; the Hero `#rations-panel` (`#s-rations`, `#s-rations-n`); the Gear tab's `#onyou-panel` (`#s-onyou`, `#s-kit`) + `#bag-panel` (`#s-carry-n`, `#s-carry`); `renderDropShelf(shelf, entries)` over `{ it, i }` entries; `test/unit/shell-clarity-43.test.js`; `docs/CLARITY.md` `## Requirements map`; REQUIREMENTS.md CLAR-01..05 complete; the phase-close SUMMARY (aggregated Pixel 7 checklist, `## For PROJECT.md`, `## Corrections to CONTEXT`).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cost-event inventory (docs/CLARITY.md) + the additive cause keys in the engine</name>
  <files>engine/movement.js, engine/magic.js, engine/encounters.js, docs/CLARITY.md, test/unit/clarity-cause-lines.test.js</files>
  <read_first>
    - engine/movement.js lines 56-62 (`heightsPenalty`/`waterPenalty` — module-private, return 0 for a non-phobic character), 226-231 and 241-244 (the `heightsFear`/`waterFear` push sites — `hPenalty`/`wPenalty` are in scope), 261 (`fellClimbing`/`fellInGorge { hurt }`), 322-329 (`trappedPanic { loss }` — `c.phobia` is `"Being trapped"` here by construction), 310 (`waded { cost }`)
    - engine/magic.js lines 126-136 (`spellBackfired { spell }` then `backfireSelfDamage { amount }` — `sp.n` and `c.sub` in scope), 170-180 (`summonBackfired { amount }` — `sp.n`, `c.sub` in scope), 284-294 (`earthquakeSelfDamage { amount }` — `sp.n` in scope), 442-450 (`deathSpellTooWeak {}` and `deathCast {}` — the literal 25)
    - engine/encounters.js lines 601-614 (`goInsane` — `insanitySelfHarm {}` after `c.wp = Math.ceil(c.wp / 2)`), 70-99 (`trapSprung { roll, name, dmg }`, `trapDoubled { reason }`, `trapPoisoned {}`), 585-593 (`afflictionCaught { kind, first }`), 425-437 (`faerieBane { amount }`)
    - engine/combat.js lines 2101-2140 (`armorSoaked`/`foeBolted`/`struckByFoe` payloads — read-only here; note foeBolted carries `name` + `ability`), 826-833 (`cooked`), engine/foeAbilities.js lines 214-251 (`foeBolted` member branch, `foeDrained { name, ability, stolen }`), engine/death.js lines 92-97 (`lootForfeited { items, reason }` — reasons are `"died"` or the flee reason string), engine/economy.js lines 482-503 (`bought { item, cost }`, `buyFailed`, `rationsBought`), engine/items.js lines 1350-1358 (`itemConsumed { item }`), engine/movement.js line 170 (`toolUsed { tool, feat, item }`), line 864 (`joinerMurdered { name, sub, depth }`)
    - src/browser/eventNarration.js (the FULL table — you are inventorying every cost-bearing entry; note which lines already lead with their cause: `afflictionTick`, `afflictionCaught`, `trapDoubled`, `bought`, `armorSoaked`, `phobiaTriggered`, `campFailed`)
    - docs/FLEE.md lines 1-12 and 177-193 (ledger header + requirements-map style), docs/TERRAIN.md lines 413-436 (requirements map + out-of-scope style)
    - test/unit/formatEventsCoverage.test.js lines 1-80 (how the event vocabulary is derived — every `type: "X"` literal in engine/*.js with comments stripped; a new key on an existing event needs no table change)
  </read_first>
  <behavior>
    - `trappedPanic` payload keys are exactly `type, loss, phobia` with `phobia === "Being trapped"`; `heightsFear` carries a numeric `penalty` (2 for a plain Heights-phobic character, 1 with Hardiness); `waterFear` carries `penalty` (2 / 1 likewise); every other movement event is byte-identical (deepStrictEqual against the parent revision's events for the same seeded replay)
    - `backfireSelfDamage` carries `spell` (the spell's `n`) and `sub` (`"Apprentice"`); `summonBackfired` carries `spell` and `sub` (`"Summoner"`); `earthquakeSelfDamage` carries `spell` (`"Earthquake"`); `deathCast` carries `cost: 25`; `deathSpellTooWeak` carries `fee: 25`; the hp arithmetic on every one of these paths is unchanged (same `c.wp` after)
    - `insanitySelfHarm` carries `loss` equal to the hp actually removed (`before − Math.ceil(before / 2)`); a 1-hp character loses 0 and the event still fires with `loss: 0`
    - `grep -c "rng\." engine/movement.js engine/magic.js engine/encounters.js` is unchanged per file versus `git show HEAD:<file>` — zero draws added or removed
    - `docs/CLARITY.md`'s inventory table has one row per event in the Plan-01 row list below plus the five day-cycle rows marked `Plan 02`, and a ledger test asserts every `event` cell is a real `EVENT_NARRATION` key
  </behavior>
  <action>
    A. Engine additive keys (each push site gets a one-line comment `Phase 43 (CLAR-01, additive): <key> names the cause for the narration; fixtures compare state, so this moves none`):
       - `engine/movement.js`: `heightsFear` -> `{ type: "heightsFear", penalty: hPenalty }`; `waterFear` -> `{ type: "waterFear", penalty: wPenalty }`; `trappedPanic` -> `{ type: "trappedPanic", loss, phobia: c.phobia }`.
       - `engine/magic.js`: `backfireSelfDamage` -> `{ type: "backfireSelfDamage", amount: self, spell: sp.n, sub: c.sub }`; `summonBackfired` -> `{ type: "summonBackfired", amount: hurt, spell: sp.n, sub: c.sub }`; `earthquakeSelfDamage` -> `{ type: "earthquakeSelfDamage", amount: self, spell: sp.n }`; introduce `const DEATH_SPELL_FEE = 25;` beside the death branch and use it for the `c.wp <= DEATH_SPELL_FEE + 1` guard, the `c.wp -= DEATH_SPELL_FEE` charge, `{ type: "deathSpellTooWeak", fee: DEATH_SPELL_FEE }` and `{ type: "deathCast", cost: DEATH_SPELL_FEE }` (the guard `c.wp <= 26` is value-identical to `<= DEATH_SPELL_FEE + 1`; keep the comparison semantics exactly).
       - `engine/encounters.js#goInsane`: capture `const before = c.wp;` before the halving and push `{ type: "insanitySelfHarm", loss: before - c.wp }`.
       Do NOT touch `trapSprung`/`afflictionCaught`/`afflictionTick`/`fellClimbing`/`fellInGorge`/`waded`/`foeBolted`/`foeDrained`/`bought`/`toolUsed`/`itemConsumed`/`lootForfeited`/`joinerMurdered`/`faerieBane` payloads — they already carry their cause (the inventory records "present").

    B. `docs/CLARITY.md` (new). Header in the docs/FLEE.md style (`# Clarity Pass (Phase 43 ledger)`, Phase/Date lines, one-paragraph purpose naming CLAR-01..05 and the model line `Being trapped: four walls and one door you already used. −4 hp.`). Section `## Cost-event inventory (CLAR-01)` with the line-format rule stated once (`<Cause>: <plain-language why>. −N <unit>.` — cause first, cost last, the number always stated when the engine knows it, the item named when there is no number; combat strike lines keep the Phase 34 fight-log dice-first convention because the foe's name IS the cause and the fight log folds them) and a Markdown table with columns `| Event | Cost kind | Cause key (present / ADDED) | Line before | Line after | Owner |`. Rows (exactly these events, this order):
       1 `trapSprung` hp · present (`name`) · Plan 01 — 2 `trapDoubled` hp×2 · present (`reason`) · unchanged (already cause-first) — 3 `trapPoisoned` affliction · type · Plan 01 — 4 `afflictionCaught` hp · present (`kind`) · Plan 01 — 5 `afflictionTick` hp · present (`kind`) · Plan 01 — 6 `fellClimbing` hp · type (wall) · Plan 01 — 7 `fellInGorge` hp · type (crevice) · Plan 01 — 8 `trappedPanic` hp · ADDED `phobia` · Plan 01 — 9 `heightsFear` roll penalty · ADDED `penalty` · Plan 01 — 10 `waterFear` roll penalty · ADDED `penalty` · Plan 01 — 11 `waded` squares · present (`cost`) · Plan 01 — 12 `summonBackfired` hp · ADDED `spell`,`sub` · Plan 01 — 13 `backfireSelfDamage` hp · ADDED `spell`,`sub` · Plan 01 — 14 `earthquakeSelfDamage` hp · ADDED `spell` · Plan 01 — 15 `deathCast` hp · ADDED `cost` · Plan 01 — 16 `deathSpellTooWeak` refusal naming the fee · ADDED `fee` · Plan 01 — 17 `foeBolted` hp · present (`name`,`ability`) · Plan 01 (direct builder; the in-combat fold text is unchanged) — 18 `foeDrained` hp · present (`name`,`stolen`) · Plan 01 — 19 `struckByFoe` hp · present (`name`) · unchanged (dice-first fight-log convention) — 20 `memberStruck` member hp · present · unchanged — 21 `armorSoaked` armor durability · present (`name`,`wear`) · unchanged — 22 `bought` wilmst · present (`item`,`cost`) · Plan 01 — 23 `toolUsed` item consumed · present (`tool`,`item`) · Plan 01 — 24 `itemConsumed` item consumed · present (`item`) · Plan 01 — 25 `potionDrunk` potion · present (`remaining`) · unchanged — 26 `scrollRead`/`scrollCast` scroll · present (`spell`) · unchanged — 27 `lootForfeited` loot · present (`items`,`reason`) · Plan 01 — 28 `joinerMurdered` a Joiner · present (`name`,`depth`; the cause is the Cutthroat) · Plan 01 — 29 `joinerLeft` a Joiner · present (`replacedBy`) · unchanged — 30 `faerieBane` base hp · type · Plan 01 — 31 `insanitySelfHarm` hp · ADDED `loss` · Plan 01 — 32 `itemDropped` item · player choice · unchanged (ORACLE_ONLY) — 33 spell charge spent · none (no dedicated event; the cast line names the spell, the Grimoire/HUD shows the count — the `spellChargeRecovered` ORACLE_ONLY precedent) · unchanged — 34 `dayBegan` · none · Plan 02 — 35 `rationsEaten` rations · NEW EVENT · Plan 02 — 36 `wentHungry` hp · ADDED `need`,`have`,`heft` · Plan 02 — 37 `rested` (gain; names the doubling rule) · present · Plan 02 verifies — 38 `campFailed` refusal · present (`need`,`have`,`members`) · unchanged (pinned exactly by movement.test.js:984).
       Fill "Line before" from the current builders and "Line after" from Task 2's list (Plan 02 rows: write `(Plan 02)`). Add `## HP not WP sweep` as a placeholder heading with the sentence `(filled by Task 3)`, and `## Requirements map` with the sentence `(filled by Plan 04)`.

    C. `test/unit/clarity-cause-lines.test.js` (new) — engine half: seeded/fixed-state tests for every `<behavior>` bullet (build a Being-trapped Human Fighter and walk into a dead end via the harness pattern in test/unit/phobia-triggers.test.js; an Apprentice backfire via a `fakeRng` forcing the d8 to 1 — see test/unit/casters-can-act.test.js or spell-mechanics.test.js for the cast harness; a Summoner backfire likewise; an Earthquake cast with no ward; a Death cast at 30 hp and a refusal at 26 hp; `goInsane` with a rng returning 1). Also a ledger test: parse `docs/CLARITY.md`'s inventory table, collect the `Event` cells (split `/` and strip backticks; skip the prose row 33), and assert each is a key of `EVENT_NARRATION` (import from src/browser/eventNarration.js) — and that the table has at least 38 rows. Leave the narration half of this file for Task 2 (a `// Task 2 appends the line-shape tests below` marker).

    Run `node --test test/unit/clarity-cause-lines.test.js test/unit/movement.test.js test/unit/phobia-triggers.test.js test/unit/spell-mechanics.test.js test/unit/casters-can-act.test.js test/unit/encounters.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/determinism/ test/parity/` — green. Commit `feat(43-01): additive cause keys (trappedPanic.phobia, heights/water penalty, backfire/summon/earthquake spell, Death fee, insanity loss) + docs/CLARITY.md cost-event inventory` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c 'events.push({ type: "trappedPanic", loss, phobia: c.phobia });' engine/movement.js` is 1; `grep -c 'type: "heightsFear", penalty: hPenalty' engine/movement.js` is 1; `grep -c 'type: "waterFear", penalty: wPenalty' engine/movement.js` is 1
    - `grep -c 'spell: sp.n, sub: c.sub' engine/magic.js` is 2 (backfireSelfDamage + summonBackfired); `grep -c 'type: "earthquakeSelfDamage", amount: self, spell: sp.n' engine/magic.js` is 1; `grep -c "const DEATH_SPELL_FEE = 25;" engine/magic.js` is 1; `grep -c 'type: "deathCast", cost: DEATH_SPELL_FEE' engine/magic.js` is 1; `grep -c 'type: "deathSpellTooWeak", fee: DEATH_SPELL_FEE' engine/magic.js` is 1
    - `grep -c 'type: "insanitySelfHarm", loss: before - c.wp' engine/encounters.js` is 1
    - For each of engine/movement.js, engine/magic.js, engine/encounters.js: `grep -c "rng\." <file>` equals `git show HEAD:<file> | grep -c "rng\."`
    - `grep -c "^## Cost-event inventory (CLAR-01)" docs/CLARITY.md` is 1; `grep -cE '^\| \`?(trapSprung|trappedPanic|heightsFear|waterFear|summonBackfired|backfireSelfDamage|earthquakeSelfDamage|deathCast|insanitySelfHarm|rationsEaten|wentHungry|lootForfeited|joinerMurdered|toolUsed|bought|foeBolted|foeDrained|faerieBane)\`? ' docs/CLARITY.md` is at least 18; `grep -c "^## HP not WP sweep" docs/CLARITY.md` is 1; `grep -c "^## Requirements map" docs/CLARITY.md` is 1
    - `node --test test/unit/clarity-cause-lines.test.js` exits 0 with at least 8 tests
    - `node --test test/unit/movement.test.js test/unit/phobia-triggers.test.js test/unit/spell-mechanics.test.js test/unit/casters-can-act.test.js test/unit/encounters.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/determinism/ test/parity/` exits 0
    - `git status --porcelain test/parity/fixtures` is empty; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/clarity-cause-lines.test.js test/unit/movement.test.js test/unit/phobia-triggers.test.js test/unit/spell-mechanics.test.js test/unit/casters-can-act.test.js test/unit/encounters.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/determinism/ test/parity/</automated>
  </verify>
  <done>Every cost-bearing event is inventoried with its cause key marked present or ADDED; the nine additive keys are on their events; no draw, no serialized field, no fixture moved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cause-first rewrite of the 24 Plan-01 rows on EVENT_NARRATION + TOAST_FOR; voice toggles; re-pin the wording tests</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, test/voice/safety-scan.test.js, test/unit/clarity-cause-lines.test.js, test/unit/rail.test.js, test/unit/narrativeToasts.test.js, test/unit/loot-narration.test.js</files>
  <read_first>
    - src/browser/eventNarration.js lines 126-147 (`fellClimbing`/`fellInGorge`/`heightsFear`/`waterFear`/`trappedPanic`/`waded`), 172-175 (`afflictionTick`), 515-523 (`spellBackfired`/`backfireSelfDamage`/`summonBackfired`), 560-561 (`earthquake`/`earthquakeSelfDamage`), 623-625 (`deathSpellTooWeak`/`deathCast`), 490-497 (`foeBolted`/`foeDrained`), 700 (`bought`), 721-723 (`trapSprung`/`trapPoisoned`), 752-758 (`joinerMurdered`), 786-788 (`faerieBane`), 811-813 (`afflictionCaught`/`insanitySelfHarm`), 135-140 (`toolUsed`), 858 (`itemConsumed`), 934-940 (`lootForfeited`), 100-103 (the span-class convention: dice clauses live INSIDE `<span class="roll">` so what survives `narrativeToastText` is a clean sentence; a COST is not dice — keep `−N hp.` OUTSIDE any roll span so the rail/toast text carries it)
    - src/browser/toasts.js lines 944-978 (movement toasts), 1205-1211 (`foeBolted`/`foeDrained` direct builders — the in-combat `enemyRound` fold at 413-509 is NOT touched), 1305-1312, 1331, 1355, 1407, 1419-1423, 1435, 1461-1463, 1485-1489, 1566-1573
    - src/browser/rail.js lines 356-368 (`rollLineFor` — a line with no roll span yields null; `hurt` alone never fabricates dice) and 120-130 (the AFRAID/FELL/A TRAP families — unchanged)
    - test/voice/safety-scan.test.js lines 111-175 (`BASE_EVENT`, `BRANCH_TOGGLES`, `NAME_FIELDS` — add toggles for every new branch: `{ reason: "died" }` for lootForfeited's died branch, `{ phobia: "Being trapped" }`, `{ penalty: 2 }`, `{ cost: 25 }`, `{ fee: 25 }`, `{ sub: "Apprentice" }`, `{ member: "Grunk" }` for foeBolted's member branch if not already covered by BASE_EVENT's `member`, `{ tool: "ladder" }`/`{ tool: "rope" }`)
    - test/unit/rail.test.js lines 136-145 (`rollLineFor` pin using `trapSprung` — after this task trapSprung's line has NO roll span, so that assertion must move to a `struck` event: `rollLineFor({ type: "struck", roll: 4, need: 5, target: "Rat", dmg: 3 }, narrateEvent)` yields the tag-stripped dice sentence; keep the `fellClimbing` null case)
    - test/unit/narrativeToasts.test.js lines 116-125 and 170-181 (the two `"Pit trap finds you first."` expectations — become the new tag-stripped sentence), test/unit/loot-narration.test.js lines 40-52 and 75-82 (`lootForfeited` toast/Oracle pins)
  </read_first>
  <behavior>
    - After `narrativeToastText`, the Oracle lines read EXACTLY: `trapSprung {name:"Pit", dmg:4}` -> `Trap: Pit finds you first. −4 hp.`; `trappedPanic {loss:4, phobia:"Being trapped"}` -> `Being trapped: four walls and one door you already used. −4 hp.` (and `{loss:4}` alone renders the same via the `"Being trapped"` default); `fellClimbing {hurt:5}` -> `Fall: the wall had other plans. −5 hp.`; `fellInGorge {hurt:7}` -> `Fall: short. The floor of the crevice makes its introduction. −7 hp.`; `afflictionCaught {kind:"Poison", first:3}` -> `Poison: it takes hold. −3 hp.`; `afflictionTick {kind:"Poison", loss:2}` -> `Poison: still in you. −2 hp.`; `afflictionTick {kind:"Disease", loss:0}` -> `Disease: it has taken everything it can. You are on one hp.`; `heightsFear {penalty:2}` -> `Heights: your stomach reaches the ground well before your feet do. +2 on a roll you wanted low.`; `waterFear {penalty:1}` -> `Bodies of water: something down there may be wet. That is enough. +1 on a roll you wanted low.`; `waded {cost:2}` -> `Water: 2 squares a step, and it smells worse.`; `summonBackfired {amount:9, spell:"Summon", sub:"Summoner"}` -> `Summoning: Summon answered, then turned on you — a Summoner's doubled creatures come with a grudge. −9 hp.`; `backfireSelfDamage {amount:4, spell:"Fireball", sub:"Apprentice"}` -> `Backfire: Fireball went wrong in your hands — the Apprentice tax, one time in eight. −4 hp.`; `earthquakeSelfDamage {amount:6}` -> `Earthquake: the floor does not take sides. −6 hp.`; `deathCast {cost:25}` -> `Death: the spell takes its fee first. −25 hp.`; `deathSpellTooWeak {fee:25}` -> `Death: the fee is 25 hp, and you would not survive paying it.`; `foeBolted {name:"Shriek", dmg:5}` -> `Shriek: it lands. −5 hp.`; `foeBolted {name:"Shriek", dmg:5, ignoresArmor:true}` -> `Shriek: it lands. −5 hp, and your armor was not consulted.`; `foeBolted {name:"Shriek", dmg:5, member:"Grunk"}` -> `Shriek: it lands on Grunk. −5 hp. Better them than you.`; `foeDrained {name:"Wraith", stolen:4}` -> `Wraith: it drinks 4 hp of yours and looks better for it.`; `bought {item:"Axe", cost:63}` -> `Bought: Axe. −63 wilmst.`; `toolUsed {tool:"ladder"}` -> `Ladder: up and over the wall. The ladder stays behind.`; `toolUsed {tool:"rope"}` -> `Rope: across the gap, boring and safe. The rope stays behind.`; `itemConsumed {item:{n:"Torch"}}` -> `Spent: Torch. One use, as advertised.`; `lootForfeited {items:[{n:"A"},{n:"B"}], reason:"escaped"}` -> `Fled: the loot stays with them — A, B.`; `lootForfeited {items:[{n:"A"}], reason:"died"}` -> `Dead: A stays where it fell. So, for that matter, do you.` and with two items `Dead: A, B stay where they fell. So, for that matter, do you.`; `joinerMurdered {name:"Grunk", depth:3}` -> a line starting `Cutthroat: ` followed by the picked JOINER_MURDER_LINES entry; `faerieBane {amount:3}` -> `Faerie: it took against you. −3 base hp.`; `insanitySelfHarm {loss:6}` -> `Insanity: you turn on yourself. −6 hp.`; `trapPoisoned {}` -> `Trap: it leaves something behind that outlasts the bruise.`
    - Every one of those Oracle lines, tag-stripped, matches `/^[A-Z][A-Za-z ]{1,24}: /` (cause first) and, where the payload carries a number, ends with the cost clause (`−N hp.`, `−N base hp.`, `−N wilmst.`, `squares a step`), never a bare number inside a roll span
    - The TOAST_FOR texts read: `trapSprung` -> `Trap: Pit (−4 hp).`; `trapPoisoned` -> `Trap: poisoned.`; `afflictionCaught` -> `Poison: takes hold (−3 hp).`; `fellClimbing` -> `Fall: the wall won (−5 hp).`; `fellInGorge` -> `Fall: short of the far side (−7 hp).`; `trappedPanic` -> `Being trapped: four walls, one used door (−4 hp).`; `heightsFear` -> `Heights: +2 on the roll.`; `waterFear` -> `Bodies of water: +1 on the roll.`; `waded` -> `Water: 2 squares a step.`; `summonBackfired` -> `Summoning: Summon turned on you (−9 hp).`; `backfireSelfDamage` -> `Backfire: Fireball (−4 hp).`; `earthquakeSelfDamage` -> `Earthquake: −6 hp, yours too.`; `deathCast` -> `Death: its fee (−25 hp).`; `deathSpellTooWeak` -> a block toast `Death: 25 hp fee. You cannot pay it and live.`; `foeDrained` -> `Wraith drains you (−4 hp).`; `bought` -> `Bought: Axe (−63 wilmst).`; `toolUsed` ladder -> `Ladder: over the wall, ladder spent.` rope -> `Rope: across, rope spent.`; `lootForfeited` escaped -> `Fled: A, B stay behind.` died -> `Dead: A stays where it fell.`; `joinerMurdered` -> `Cutthroat: Grunk did not reach floor 3.`; `faerieBane` -> `Faerie: −3 base hp.`; `insanitySelfHarm` -> `Insanity: you turn on yourself (−6 hp).`; tones/priorities unchanged from today for every row (foeBolted's toast is untouched — the enemyRound fold owns it in combat)
    - Every builder above renders without throwing on a bare `{ type }` (the coverage guard's call shape) and under every BRANCH_TOGGLES variant; `toastsCoverage.test.js`, `formatEventsCoverage.test.js` and `test/voice/safety-scan.test.js` are green
  </behavior>
  <action>
    A. `src/browser/eventNarration.js` — rewrite the 24 entries to the exact Oracle HTML that tag-strips to the `<behavior>` sentences. Span discipline: the cause sentence inside `<span class="hurt">…</span>` (or `hit` for `bought`/`toolUsed`, `beat` for `waded`/`heightsFear`/`waterFear`, `miss` for `lootForfeited`/`deathSpellTooWeak`), the cost clause OUTSIDE any span, using the codebase's U+2212 minus (`−`) and em dash (`—`). Defaults: `e?.phobia ?? "Being trapped"`, `e?.penalty ?? 0`, `e?.cost ?? 25`, `e?.fee ?? 25`, `e?.spell ?? "The spell"`, `e?.sub ?? "Apprentice"` / `"Summoner"`, `e?.loss ?? 0`, `e?.name ?? "It"`. `lootForfeited`: `reason === "died"` branch pluralises by `items.length` (`stays where it fell` / `stay where they fell`); every other reason is the `Fled:` line. `joinerMurdered`: keep the deterministic index pick and `escapeHtml`, prefix the picked line with `Cutthroat: `. Add a top-of-entry comment `// Phase 43 (CLAR-01): cause first, cost last — see docs/CLARITY.md` on each rewritten entry (do NOT quote the old sentence in the comment).

    B. `src/browser/toasts.js` — rewrite the matching TOAST_FOR builders to the `<behavior>` toast texts; `deathSpellTooWeak` stays a `block(...)`; keep every tone/priority; do not touch `enemyRound`/`yourRound`/`spellChain`/`fleeChain` folds or `ORACLE_ONLY`/`FEATURE_EVENTS`.

    C. `test/voice/safety-scan.test.js` — append the BRANCH_TOGGLES entries listed in read_first (one array-literal edit, with a `// Phase 43 (CLAR-01)` comment), so both sides of every new ternary render under the scan.

    D. Tests: append the narration half to `test/unit/clarity-cause-lines.test.js` — for every `<behavior>` Oracle sentence assert `narrativeToastText(narrateEvent(e)) === expected`; for every toast assert `.text`; a `cause-first shape` test that loops the Plan-01 event list from `docs/CLARITY.md` (rows whose Owner cell is `Plan 01`) and asserts each tag-stripped Oracle line under a representative payload matches `/^[A-Z][A-Za-z ]{1,24}: /`; a `no roll span carries the cost` test asserting `oracleDetailText(narrateEvent(e))` is `""` for trapSprung/fellClimbing/trappedPanic (no dice span). Re-pin `test/unit/rail.test.js` rollLineFor (move the roll-span case to a `struck` event and assert `rollLineFor({ type: "trapSprung", name: "Pit", dmg: 4 }, narrateEvent) === null`), `test/unit/narrativeToasts.test.js` (the two trap expectations -> `Trap: Pit trap finds you first. −5 hp.`), `test/unit/loot-narration.test.js` (fled toast -> `Fled: A, B stay behind.`; the died Oracle regex -> `/stay where they fell/` still holds — verify, adjust only if needed). Each re-pin carries a one-line `// Phase 43 (CLAR-01)` reason.

    Run `node --test test/unit/clarity-cause-lines.test.js test/unit/rail.test.js test/unit/narrativeToasts.test.js test/unit/loot-narration.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/round-card-worst-case.test.js test/unit/engineAdapter.test.js test/unit/feedback-payload.test.js test/unit/identity-contract.test.js test/voice/` — green. `npm run build:www` exit 0. Commit `feat(43-01): cause-first cost lines on Oracle + toasts (24 events), voice toggles, wording pins re-pointed` (explicit paths).
  </action>
  <acceptance_criteria>
    - `node -e "import('./src/browser/eventNarration.js').then(async m=>{const {narrativeToastText}=await import('./src/browser/toasts.js');const t=(e)=>narrativeToastText(m.narrateEvent(e));const ok=t({type:'trappedPanic',loss:4,phobia:'Being trapped'})==='Being trapped: four walls and one door you already used. −4 hp.'&&t({type:'trappedPanic',loss:4})==='Being trapped: four walls and one door you already used. −4 hp.'&&t({type:'trapSprung',name:'Pit',dmg:4})==='Trap: Pit finds you first. −4 hp.'&&t({type:'fellClimbing',hurt:5})==='Fall: the wall had other plans. −5 hp.'&&t({type:'deathCast',cost:25})==='Death: the spell takes its fee first. −25 hp.'&&t({type:'lootForfeited',items:[{n:'A'},{n:'B'}],reason:'escaped'})==='Fled: the loot stays with them — A, B.'&&t({type:'bought',item:'Axe',cost:63})==='Bought: Axe. −63 wilmst.'&&t({type:'insanitySelfHarm',loss:6})==='Insanity: you turn on yourself. −6 hp.';process.exit(ok?0:1)})"` exits 0
    - `node -e "import('./src/browser/toasts.js').then(m=>{const ok=m.TOAST_FOR.trapSprung({type:'trapSprung',name:'Pit',dmg:4}).text==='Trap: Pit (−4 hp).'&&m.TOAST_FOR.lootForfeited({type:'lootForfeited',items:[{n:'A'},{n:'B'}],reason:'escaped'}).text==='Fled: A, B stay behind.'&&m.TOAST_FOR.joinerMurdered({type:'joinerMurdered',name:'Grunk',depth:3}).text==='Cutthroat: Grunk did not reach floor 3.'&&m.TOAST_FOR.deathSpellTooWeak({type:'deathSpellTooWeak',fee:25}).priority===m.PRIORITY.block;process.exit(ok?0:1)})"` exits 0
    - `grep -v '^\s*//' src/browser/eventNarration.js | grep -c "Gravity remembers"` is 0; `grep -v '^\s*//' src/browser/eventNarration.js | grep -c "You spend 25 hp"` is 0; `grep -v '^\s*//' src/browser/eventNarration.js | grep -c "in your hurry not to be"` is 0; `grep -v '^\s*//' src/browser/toasts.js | grep -c "Left on the floor in your hurry"` is 0
    - `grep -c "Phase 43 (CLAR-01)" src/browser/eventNarration.js` is at least 20; `grep -c "Phase 43 (CLAR-01)" test/voice/safety-scan.test.js` is at least 1
    - `node --test test/unit/clarity-cause-lines.test.js` exits 0 with at least 30 tests
    - `node --test test/unit/rail.test.js test/unit/narrativeToasts.test.js test/unit/loot-narration.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/round-card-worst-case.test.js test/unit/engineAdapter.test.js test/unit/feedback-payload.test.js test/unit/identity-contract.test.js test/voice/` exits 0
    - `npm run build:www` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www && node --test test/unit/clarity-cause-lines.test.js test/unit/rail.test.js test/unit/narrativeToasts.test.js test/unit/loot-narration.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/fightLog.test.js test/unit/shell-fight-log.test.js test/unit/round-card-worst-case.test.js test/unit/engineAdapter.test.js test/unit/feedback-payload.test.js test/unit/identity-contract.test.js test/voice/</automated>
  </verify>
  <done>Every Plan-01 cost line reads cause-first and cost-last on the Oracle and the toast from the payload; the coverage guards and the voice scan are green; the bundle builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: HP-not-WP sweep with a grep-pin test (narration, COPY objects, content banks, shell player copy), the measured cosmetic txt carve-out, FIXTURE-INVENTORY entry, plan gate</name>
  <files>content/races.js, content/skills.js, content/potions.js, content/treasure-tables.js, test/parity/harness/comparables.js, test/parity/chargen-parity.test.js, test/parity/fixtures/action-script.economy.json, test/parity/FIXTURE-INVENTORY.md, test/unit/hp-not-wp.test.js, test/unit/abilities-catalog.test.js, docs/CLARITY.md</files>
  <read_first>
    - content/races.js lines 25 and 38 (the Dwarven and Troll `note` strings carrying `wp`), content/skills.js line 33 (Cooking `txt`), content/potions.js line 17 (Healing `txt`), content/treasure-tables.js lines 74, 87, 109, 129 (Cloak of Healing / Cloak of Regeneration / Rowan Staff / Poplar Staff `txt`) — these are the ONLY content-bank player strings with the old unit (verified by grep at planning)
    - test/parity/harness/comparables.js lines 374-390 (`stripCloakArmorTxt` — the Phase 28 cosmetic-txt carve-out this task generalises), 396-420 (`REAUTHORED_EVERY_ITEMS`/`stripReauthoredEveryField` — the Phase 39 precedent, including its chargen-parity application), the three comparable chains at ~491/575/1084 (stripCloakArmorTxt is already in all three)
    - test/parity/chargen-parity.test.js lines 95-175 (the per-seed compare; `stripReauthoredEveryField` is applied to BOTH sides before the divergence-record check — apply the txt strip the same way, importing `stripCloakArmorTxt`)
    - test/parity/fixtures/action-script.economy.json lines 95-140 (`before.items[1].txt`/`after.items[1].txt` — the `after` record is the ENGINE side's measured value and must be re-measured after the potion reword; `before` is the prototype's and never changes), action-script.schema.md lines 46-80 (divergence record semantics)
    - test/parity/FIXTURE-INVENTORY.md lines 1429-1470 (the Phase 42 section's shape: grep result, measured reading, draw-count statement, conclusion) — append a `## Phase 43 — clarity pass (CLAR-01..05)` section after it with a `### Plan 01 — additive cause keys + HP-wording txt carve-out` subsection; Plans 02-04 append their own `### Plan NN` subsections later
    - test/unit/abilities-catalog.test.js line 70 (pins the Cooking txt literally — re-pin to `hp`)
    - test/voice/safety-scan.test.js lines 176-190 (`renderEventVariants` — reproduce a SMALL variant set locally in the new test; do not import from the scan) and src/browser/{rail,viewModels,combatMenu,combatPanel,missLines}.js exports `RAIL_COPY`, `ITEM_STATE_COPY`, `ABILITY_VIEW_COPY`, `COMBAT_MENU_COPY`, `COMBAT_PANEL_COPY`, `MISS_LINES`
    - mazeworld.html: the classic script's OWN copies of these tables already say `hp` (lines 1863, 1918, 1922, 1928, 1933, 1938, 1999, 2005) — the shell is NOT edited in this plan; the sweep test only SCANS it
    - Planning-time measurement (2026-09-18, `newRun(seed).c.items` for every fixture seed): chargen seeds 2 and 4 -> Cloak of Regeneration; movement seed 256 -> Cloak of Healing; encounters `affliction` seed 160 -> Cloak of Regeneration; no fixture seed rolls a Rowan/Poplar Staff into the starting kit; the economy fixture (seed 3) BUYS a Healing potion whose `txt` is pinned in its declared `after` record. Re-measure live before declaring.
  </read_first>
  <behavior>
    - `REWORDED_TXT_ITEMS` is an exported frozen Set containing exactly `Cloak of Armor`, `Cloak of Healing`, `Cloak of Regeneration`, `Rowan Staff`, `Poplar Staff`; `stripCloakArmorTxt(c)` strips `txt` from any `c.items[]` entry whose `n` is in that set and leaves every other field and every other item byte-identical (a `{ n: "Cloak of Ether", txt: "x" }` item keeps its txt)
    - `node --test test/parity/` is green with `git status --porcelain test/parity/fixtures` showing ONLY `action-script.economy.json` (the re-measured `after.items[1].txt`), and the master hash unchanged
    - `test/unit/hp-not-wp.test.js`: (a) every EVENT_NARRATION and TOAST_FOR builder, rendered over `{ type }`, a numeric-rich payload and a name-rich payload, tag-stripped, contains no match of `/(?<![\w.$-])(wp|WP)(?![\w:])/`; (b) every string leaf (recursive walk) of RAIL_COPY, ITEM_STATE_COPY, ABILITY_VIEW_COPY, COMBAT_MENU_COPY, COMBAT_PANEL_COPY and MISS_LINES has no match; (c) every `note`/`txt`/`txt2` of RACES, FIGHTER_SKILLS, THIEF_SKILLS, POTIONS, JEWELRY, CLOAKS, STAVES, SPELLS, ABILITIES, TOOLS and every RACE_NOTE/CLASS_NOTE/SUB_NOTE value has no match; (d) `mazeworld.html` with comments stripped (`<!-- -->`, `/* */`, `//`) has no match in (i) text outside `<script>`/`<style>` blocks and (ii) string literals (`"…"`, `'…'`, `` `…` `` with `${…}` interpolations removed) inside `<script>` blocks; the test reports the offending snippet(s) on failure; (e) the same regex applied to `www/index.html` when present (skipped when `www/` is absent)
    - The test is a standing guard: it also asserts the regex itself does NOT match `c.wp`, `maxWP`, `cb-foe-wp`, `wp: 12`, `viewport`, `mwpulse` (a self-check row), and DOES match ` wp/day`, `+d10+2 wp`, `75 WP` and `(WP)`
  </behavior>
  <action>
    A. Content rewords (unit only, nothing else in the string): content/races.js Dwarven note `1 hp/day upkeep`, Troll note `75 hp regardless`; content/skills.js Cooking `eat any beast for a quarter of its hp`; content/potions.js Healing `+d10+2 hp`; content/treasure-tables.js `heals up to 10 hp every 20 squares`, `d6 hp back every 20 squares`, `a protective dome of 100 hp`, `1d20+10 hp to up to 6`. Add ONE header comment per file: `Phase 43 (CLAR, HP-not-WP ruling): unit word reworded wp -> hp; cosmetic, engine never reads these strings; the parity harness strips the reworded txt (comparables.js#REWORDED_TXT_ITEMS).` Re-pin test/unit/abilities-catalog.test.js line 70 to the new Cooking txt.

    B. `test/parity/harness/comparables.js`: add `export const REWORDED_TXT_ITEMS = Object.freeze(new Set(["Cloak of Armor", "Cloak of Healing", "Cloak of Regeneration", "Rowan Staff", "Poplar Staff"]));` directly above `stripCloakArmorTxt`, and change the predicate inside `stripCloakArmorTxt` from the single-name equality to `REWORDED_TXT_ITEMS.has(it.n)`; extend its JSDoc with a `Phase 43 (CLAR-01 HP-not-WP sweep)` paragraph: four more treasure rows reworded (unit word only), measured exposure = chargen seeds 2/4 (Cloak of Regeneration), movement seed 256 (Cloak of Healing), encounters seed 160 (Cloak of Regeneration); same cosmetic-carve-out rationale, function name kept so the three comparable chains are untouched. `test/parity/chargen-parity.test.js`: import `stripCloakArmorTxt` alongside `stripReauthoredEveryField` and apply it to BOTH `protoCForDiff` and `engineCForDiff` immediately after the existing `stripReauthoredEveryField` pair, with a comment mirroring that block's wording (Phase 43, measured seeds 2 and 4).

    C. Re-measure the economy fixture: run `node --test test/parity/economy-parity.test.js test/parity/full-suite.test.js`; the `after.items[1].txt` assertion fails on `+d10+2 wp` vs the engine's `+d10+2 hp` — update ONLY that `after` value (the `before` value is the prototype's and stays `wp`); append to the record's `rationale` one sentence: `Phase 43 (CLAR-01 HP-not-WP sweep): the engine's Healing potion txt unit is hp; the prototype's stays wp — a cosmetic, declared, measured field.` Re-run — green. If any OTHER fixture assertion fails, stop and record it (do not regenerate anything else).

    D. `test/unit/hp-not-wp.test.js` (new) implementing every `<behavior>` bullet. Use `PLAYER_WP = /(?<![\w.$-])(wp|WP)(?![\w:])/` (Node 22 supports lookbehind). For (d) reuse the comment-stripping approach of test/unit/shell-company-panel.test.js (line comments before block comments), then split the HTML on `<script[^>]*>…</script>` and `<style>…</style>`; scan markup text with tags removed; scan script bodies by extracting string literals with a small tokenizer regex (`/"(?:[^"\\\n]|\\.)*"|'(?:[^'\\\n]|\\.)*'|`(?:[^`\\]|\\.)*`/g`) and removing `\$\{[^}]*\}` from template literals before matching. Export nothing; report offenders as `label -> snippet`. If a hit is a code identifier the regex could not exclude (a CSS class/id fragment or a property-name string), add that exact snippet to an explicit `ALLOWLIST` array at the top of the test with a one-line reason — never widen the regex, never allowlist prose. A hit in PLAYER copy is a real straggler: fix the copy (in `src/browser`/`content` only — a `mazeworld.html` player-copy hit is recorded in the SUMMARY for Plan 04 to fix, since this plan may not edit the shell; the test then temporarily allowlists that one snippet with a `Plan 04` note).

    E. `docs/CLARITY.md` `## HP not WP sweep`: replace the placeholder sentence with: the rule, the regex, the four surfaces scanned, the eight content strings reworded, the carve-out (`REWORDED_TXT_ITEMS`, measured seeds), the economy `after` re-measurement, and `test/unit/hp-not-wp.test.js` as the standing guard.

    F. `test/parity/FIXTURE-INVENTORY.md`: append `## Phase 43 — clarity pass (CLAR-01..05)` with `### Plan 01 — additive cause keys + HP-wording txt carve-out`: the statement that fixtures compare state so the nine additive event keys move nothing (name them), the txt carve-out with the measured seed list, the economy `after` field re-measurement (before/after value), the draw-count statement (`grep -c "rng\."` unchanged in movement/magic/encounters), and the conclusion (zero fixture regenerations beyond the one declared record). Never touch the generated block.

    G. PLAN GATE (verification agents are off): `npm test` — record `# tests`, `# pass`, `# fail` (must be 0); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` shows only the committed economy record. Put the numbers in the SUMMARY.

    Commit `test(43-01): HP-not-WP grep pin; content txt rewords with REWORDED_TXT_ITEMS carve-out; economy after-record re-measured; FIXTURE-INVENTORY Phase 43` (explicit paths). Write `43-01-SUMMARY.md` with the standard sections, the gate numbers, `## Human verification (deferred to end of run)` listing: (1) spring a trap — rail card A TRAP reads `Trap: <name> finds you first. −N hp.`; (2) a Being-trapped hero enters a dead end — the AFRAID card reads `Being trapped: four walls and one door you already used. −N hp.`; (3) fall off a wall / short of a crevice — `Fall: …` lines; (4) an Apprentice backfire / a Summoner backfire / an Earthquake with no ward / a Death cast — each line names the spell and the fee; (5) buy anything — `Bought: X. −N wilmst.`; (6) flee with a loot pile pending — `Fled: the loot stays with them — …`; (7) a Cutthroat descent that loses a Joiner opens `Cutthroat: …`; (8) the Gear tab's Cloak of Healing / Regeneration / Rowan / Poplar rows and a Healing potion row read `hp`; (9) a voice read of every rewritten line on-device. Plus `## Corrections to CONTEXT` (Death SPELL not potion; no in-combat foe DOT on the hero; the measured seed list).
  </action>
  <acceptance_criteria>
    - `grep -c "1 hp/day upkeep" content/races.js` is 1; `grep -c "75 hp regardless" content/races.js` is 1; `grep -c "quarter of its hp" content/skills.js` is 1; `grep -c '"+d10+2 hp"' content/potions.js` is 1; `grep -c "10 hp every 20 squares" content/treasure-tables.js` is 1; `grep -c "d6 hp back every 20 squares" content/treasure-tables.js` is 1; `grep -c "dome of 100 hp" content/treasure-tables.js` is 1; `grep -c "1d20+10 hp to up to 6" content/treasure-tables.js` is 1
    - `node -e "import('./content/index.js').then(m=>{const re=/(?<![\w.$-])(wp|WP)(?![\w:])/;const bad=[];for(const [k,r] of Object.entries(m.RACES)) if(re.test(r.note||'')) bad.push('RACES.'+k);for(const b of [m.FIGHTER_SKILLS,m.THIEF_SKILLS]) for(const [k,v] of Object.entries(b)) if(re.test(v.txt||'')||re.test(v.txt2||'')) bad.push(k);for(const p of m.POTIONS) if(re.test(p.txt||'')) bad.push(p.n);for(const a of [...m.JEWELRY,...m.CLOAKS,...m.STAVES]) if(re.test(a.txt||'')) bad.push(a.n);console.log(bad);process.exit(bad.length?1:0)})"` exits 0
    - `grep -c 'export const REWORDED_TXT_ITEMS' test/parity/harness/comparables.js` is 1; `grep -c "REWORDED_TXT_ITEMS.has(it.n)" test/parity/harness/comparables.js` is 1; `grep -c "stripCloakArmorTxt" test/parity/chargen-parity.test.js` is at least 3 (import + two applications)
    - `node -e "const d=require('./test/parity/fixtures/action-script.economy.json');process.exit(d.divergence.after.items[1].txt==='+d10+2 hp'&&d.divergence.before.items[1].txt==='+d10+2 wp'?0:1)"` exits 0; `git status --porcelain test/parity/fixtures` lists only `test/parity/fixtures/action-script.economy.json` before the commit (and is empty after)
    - `node --test test/unit/hp-not-wp.test.js` exits 0 with at least 6 tests; `node --test test/unit/abilities-catalog.test.js test/parity/` exits 0
    - `grep -c "^### Plan 01 — additive cause keys + HP-wording txt carve-out" test/parity/FIXTURE-INVENTORY.md` is 1; `grep -c "^## Phase 43 — clarity pass (CLAR-01..05)" test/parity/FIXTURE-INVENTORY.md` is 1; `grep -c "hp-not-wp.test.js" docs/CLARITY.md` is at least 1; `grep -c "REWORDED_TXT_ITEMS" docs/CLARITY.md` is at least 1; the `## HP not WP sweep` section body is at least 8 lines long (`awk '/^## HP not WP sweep/,/^## Requirements map/' docs/CLARITY.md | wc -l` prints at least 10)
    - `npm test` output ends with `# fail 0` and `# pass` is at least 3002 + 40
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0
    - `.planning/phases/43-clarity-pass/43-01-SUMMARY.md` exists and contains `## Human verification (deferred to end of run)` and `## Corrections to CONTEXT`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && node --test test/unit/hp-not-wp.test.js test/parity/</automated>
  </verify>
  <done>No player-facing string says wp/WP anywhere a player can read it and a standing test proves it; the four treasure rewords are a measured, declared, zero-regeneration cosmetic carve-out; the economy record is re-measured; the whole suite is green with the master untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine event payload -> narration HTML | payload strings (`spell`, `sub`, `name`, `item.n`) are interpolated into `innerHTML`-bound Oracle lines |
| content banks -> saved state (`c.items[].txt`) | content strings are copied into serialized items; the parity harness compares them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Tampering | `joinerMurdered` name interpolation | low | mitigate | keep the existing `escapeHtml(rawName)` path; the new `Cutthroat: ` prefix is a constant |
| T-43-02 | Information disclosure | payload `sub`/`spell` keys in Oracle lines | low | accept | engine identifiers are the canon display names already shown elsewhere (grimoire, sheet) |
| T-43-03 | Tampering | fixture `after` record edit | medium | mitigate | edit ONLY the measured `after.items[1].txt`; `before` untouched; `git status --porcelain test/parity/fixtures` pinned in acceptance |
| T-43-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this phase (`package.json`/lock unchanged — Plan 04's gate greps it) |
</threat_model>

<verification>
- `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0; only the declared economy `after` field changed under `test/parity/fixtures`.
- SC-1 literal renders: `Being trapped: four walls and one door you already used. −4 hp.`
- Every Plan-01 cost row reads cause-first (regex-pinned) on Oracle + toast; coverage guards and voice scan green (SC-5).
- `hp-not-wp.test.js` green across narration, COPY objects, content banks, shell player copy.
</verification>

<success_criteria>
- CLAR-01 (non-day-cycle half): every cost line names its cause from the payload; additive keys only; inventory ledgered.
- HP not WP: zero player-facing stragglers, standing guard in place.
- Zero rng change, zero serialized-field change, zero fixture regeneration beyond the one declared, measured record.
</success_criteria>

<output>
Create `.planning/phases/43-clarity-pass/43-01-SUMMARY.md` when done (standard sections + `## Human verification (deferred to end of run)` + `## Corrections to CONTEXT` + the gate numbers).
</output>
