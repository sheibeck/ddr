---
phase: 43-clarity-pass
plan: 04
type: execute
wave: 4
depends_on: [43-03]
files_modified:
  - mazeworld.html
  - test/unit/shell-clarity-43.test.js
  - test/unit/shell-company-panel.test.js
  - test/unit/shell-party-camp.test.js
  - test/unit/shell-armor-display.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-gear-toolbar.test.js
  - test/unit/shell-gear-39.test.js
  - test/unit/hp-not-wp.test.js
  - docs/CLARITY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLAR-01, CLAR-02, CLAR-03, CLAR-04, CLAR-05]
tags: [shell, gear-tab, hero-tab, loot-surfaces, phase-close, deferred-uat]

must_haves:
  truths:
    - "Every loot offer shows `(usable by …)` when the item is class-restricted — the encounter-dot FIND card line, every victory LOOT screen row, every store buy row — via `window.__mzUsableBy` (ROADMAP SC-2, CLAR-02); the Joiner offer card and the Company panel each show `eats N a rest` via the bridged view model (CLAR-05)"
    - "The Hero tab has a RATIONS panel whose line reads e.g. `You eat 1 a rest. Grunk (Troll) eats 2 (Troll: eats for two). Party: 3 a rest · 7 carried — 2 nights, then the arguing starts.` from `window.__mzRations.view(S)` — the same `nightlyEats`/`eatsFor` read Make Camp charges (ROADMAP SC-3, CLAR-03)"
    - "The Gear tab is exactly two panels — ON YOU (WIELDED: the weapon row; WORN: armor + the six slots, empty slots rendered as in-voice rows; ALSO ON YOU: the kit summary minus the weapon/armor duplicates) and BAG (the slot-consuming items with the n / slots readout) — and the bag-full drop prompt (find card + loot screen) lists bag items only via `window.__mzDropShelfItems` (ROADMAP SC-4 as amended by CONTEXT §CLAR-04)"
    - "`toastsCoverage.test.js` and `formatEventsCoverage.test.js` stay green; the voice scan and `hp-not-wp` stay green; every shell-pin test still passes (SC-5)"
    - "The phase is closed: `docs/CLARITY.md` requirements map filled, REQUIREMENTS.md CLAR-01..05 complete, the whole-phase gate recorded, the aggregated Pixel 7 checklist and `## For PROJECT.md` written — the LAST phase of v1.5"
  artifacts:
    - "mazeworld.html: a NEW viewModels import line + bridges `window.__mzUsableBy`, `window.__mzRations = { view, eatsLine }`, `window.__mzDropShelfItems`, `window.__mzGear = { emptySlotRows, copy }`; FIND/LOOT/store usable suffixes; Joiner card + Company panel eats line; `#rations-panel` (`#s-rations`, `#s-rations-n`); `#onyou-panel` (`#s-onyou`, `#s-kit`) + `#bag-panel` (`#s-carry-n`, `#s-carry`); `renderDropShelf(shelf, entries)`; three CSS rules `.mw-onyou-head`, `.mw-worn-empty`, `.mw-kit-head`"
    - "test/unit/shell-clarity-43.test.js (new); re-pinned shell-company-panel/shell-party-camp tests"
    - "docs/CLARITY.md `## Requirements map`; .planning/REQUIREMENTS.md CLAR-01..05 `[x]` + traceability `Complete`"
    - "43-04-SUMMARY.md with the aggregated Pixel 7 checklist, `## For PROJECT.md`, `## Corrections to CONTEXT`, the whole-phase gate numbers"
  key_links:
    - "viewModels.js exports (Plans 02-03) -> ONE new import line + four bridges (the Phase 28/29/37/39 read-only-bridge pattern) -> classic-script render sites; the pinned existing import line stays byte-identical"
    - "renderRail's FIND branch + renderEncounter's LOOT branch + the store branch -> `__mzUsableBy(it, S.c)`; LOOT rows also read `lootCompare(...).usable`"
    - "paint()'s carry region -> `#s-onyou` (wornRow/wornSlotRow/emptySlotRows) and `#s-carry` (renderCarriedList) — the Phase 37/39 pins on wornRow's signature, wornSlotRow's region and `gearRow: true` all still hold"
    - "renderDropShelf entries `{ it, i }` -> `window.mzDropItem?.(i)` — the true c.items index"
---

<objective>
Wire the pure view models into the shell — the only plan allowed to edit `mazeworld.html` — and close the phase and the milestone's last phase: `(usable by …)` on the FIND card, LOOT screen rows and store rows; `eats N a rest` on the Joiner offer card and the Company panel; the Hero RATIONS panel; the Gear tab's ON YOU / BAG two-panel split with in-voice empty slots; the bag-only drop prompt; then the ledger's requirements map, REQUIREMENTS.md, the whole-phase gate, the aggregated Pixel 7 checklist and `## For PROJECT.md`.

Purpose: ROADMAP SC-2/3/4 land on-screen; SC-1/5 are proven by the whole-phase gate; the phase closes in the 41-04/42-04 format.

Output: the shell edits, `test/unit/shell-clarity-43.test.js`, re-pinned shell tests, `docs/CLARITY.md` requirements map, REQUIREMENTS.md flips, 43-04-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-clarity-pass/43-CONTEXT.md
@.planning/phases/43-clarity-pass/43-01-SUMMARY.md
@.planning/phases/43-clarity-pass/43-02-SUMMARY.md
@.planning/phases/43-clarity-pass/43-03-SUMMARY.md
@.planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md
@.planning/phases/42-flee-retune-consolidated-balance-close/42-04-SUMMARY.md
@mazeworld.html
@src/browser/viewModels.js
@src/browser/rail.js
@docs/CLARITY.md
@docs/RATIONS.md
</context>

<standing_rules>
- This is the ONLY plan that edits `mazeworld.html`. No engine, content or `src/browser` logic edits — only bridge assignments and render wiring; if a view-model gap appears, add the pure function to `src/browser/viewModels.js` with a test (that is allowed) rather than putting logic in the classic script.
- Bridge discipline (Phase 28/29/37/39 precedent): a NEW `import { usableBy, rationsViewModel, eatsLineFor, dropShelfItems, emptySlotRows, GEAR_COPY } from "./src/browser/viewModels.js";` line directly after the existing pinned viewModels import — the existing line `import { characterSheetViewModel, grimoireViewModel, armorDisplay, bagArmorText, lootCompare, bagUsage, itemRowState } from "./src/browser/viewModels.js";` stays BYTE-IDENTICAL (three tests pin it: shell-armor-display:64, shell-gear-39:65, shell-loot-screen:58). Likewise `import { nightlyEats } from "./engine/movement.js";` and `window.__mzNightlyEats = nightlyEats;` stay byte-identical (shell-party-camp:47-49) — the camp button keeps reading `__mzNightlyEats`.
- Every interpolation of a name/item string into `innerHTML` goes through `escText` (Company panel) or is a static COPY string (`usable`, empty-slot rows via `textContent`) — T-38-11: no innerHTML carrying an item name in the wornSlotRow region.
- CSS: new rules carry no `transition`/`animation` token and no `aria-disabled` selector (the shell-*.test.js convention).
- Engine gate unchanged: `npm test` `# fail 0`; master hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty; `package.json`/lock unchanged since Phase 42 close; `grep -c fonts.googleapis mazeworld.html` is 0.
- Deferred UAT: no device steps, no `adb`, no APK build, no "pause for the user". The SUMMARY aggregates the WHOLE phase's Pixel 7 checklist (Plans 01-04, grouped by plan, numbered continuously, de-duplicated with "(also Plan NN)" notes) in the 41-04-SUMMARY format, plus `## For PROJECT.md` and an aggregated `## Corrections to CONTEXT`.
- Commits per task: `feat(43-04): …` / `test(43-04): …` / `docs(43-04): …` with the two trailer lines. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
See 43-01-PLAN.md `<artifacts_this_phase_produces>` for the phase-wide list. This plan produces: the `mazeworld.html` bridges (`window.__mzUsableBy`, `window.__mzRations`, `window.__mzDropShelfItems`, `window.__mzGear`), the FIND/LOOT/store `usable` suffixes, the Joiner card and Company panel eats lines, `#rations-panel`/`#s-rations`/`#s-rations-n`, `#onyou-panel`/`#s-onyou`/`#bag-panel`, `renderDropShelf(shelf, entries)`, `.mw-onyou-head`/`.mw-worn-empty`/`.mw-kit-head`; `test/unit/shell-clarity-43.test.js`; `docs/CLARITY.md` `## Requirements map`; REQUIREMENTS.md CLAR-01..05 complete; the phase-close SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridges + loot surfaces (FIND / LOOT / store) + Joiner offer card + Company panel + Hero RATIONS panel</name>
  <files>mazeworld.html, test/unit/shell-clarity-43.test.js, test/unit/shell-company-panel.test.js, test/unit/shell-party-camp.test.js</files>
  <read_first>
    - mazeworld.html lines 7040-7160 (the module bridge block — the pinned viewModels import at ~7046, `window.__mzBagUsage`/`__mzLootCompare` at ~7143-7144, `window.__mzItemRowState` at ~7158; add the NEW import line directly after the pinned viewModels import and the four bridges directly after `window.__mzItemRowState = itemRowState;`, each with the read-only-bridge comment pattern)
    - mazeworld.html lines 6365-6383 (renderRail's FIND branch: `const it = S.pendingFind; const c = S.c; … findSub …; lines.push({ text: \`${it.n || "An item"}${findSub ? …}\`, roll: null }); if (full) { … shelfItems = c.items || []; }`), 6342-6362 (the Joiner branch: `lines.push({ text: headLine, roll: \`${ident ? ident + " · " : ""}skill level ${ROMAN[lvl - 1] || lvl}\` })` — keep `headLine` and the region free of innerHTML), 6155-6169 (the LOOT screen's `renderCarriedList(... subFor: (it) => { const cmp = window.__mzLootCompare(c, it); return cmp.sub ? \`${cmp.line} · ${cmp.sub}\` : cmp.line; } …)` and `renderDropShelf(dropShelf, c.items || [])`), 6203-6214 (the store rows: `const sub = item.effectId === "repairArmor" ? … : item.sub; row.innerHTML = \`<span class="g-n">${item.n}${sub ? \`<i>${sub}</i>\` : ""}</span>…\``), 5437-5470 (renderPartyRoster — the `Eats` line at 5469 reads `RACES[m.race]?.eats || 1` inline), 1448-1485 (the Hero tab markup — insert the RATIONS panel after the character-sheet `</section>` that follows `<p class="trait" id="s-trait">—</p>`, BEFORE the 2026-09-17 UAT comment that precedes `#hero-party`), 3255-3262 (paint()'s `s-cost` line — the RATIONS render goes right after it)
    - test/unit/shell-company-panel.test.js lines 121-141 (the order test's `" a rest"` needle -> becomes `eatsLine(m)`) and 152-162 (the `RACES[m.race]?.eats || 1` regex -> becomes `/window\.__mzRations\.eatsLine\(m\)/`); test/unit/shell-party-camp.test.js lines 46-50 (the pinned nightlyEats import/bridge — untouched) and 62-76 (the joiner-card region must stay innerHTML-free)
    - test/unit/shell-loot-screen.test.js lines 40-70 (how the loot-screen region and bridge lines are sliced — mirror its helpers in the new test), test/unit/shell-terrain-41.test.js (the newest shell-pin test — copy its stripComments/sliceBetween helpers and its "build artefact" + "voice" test shapes)
  </read_first>
  <behavior>
    - Source pins (test/unit/shell-clarity-43.test.js, reading mazeworld.html with comments stripped): the pinned viewModels import line occurs exactly once and byte-identical; the NEW import line `import { usableBy, rationsViewModel, eatsLineFor, dropShelfItems, emptySlotRows, GEAR_COPY } from "./src/browser/viewModels.js";` occurs exactly once; `window.__mzUsableBy = usableBy;`, `window.__mzRations = { view: rationsViewModel, eatsLine: eatsLineFor };`, `window.__mzDropShelfItems = dropShelfItems;`, `window.__mzGear = { emptySlotRows, copy: GEAR_COPY };` each occur exactly once, all after `window.__mzItemRowState = itemRowState;`
    - FIND branch: contains `window.__mzUsableBy(it, c)` once and `shelfItems = window.__mzDropShelfItems(c)` once; the old `c.items || []` shelf source no longer appears in the pendingFind region; the region still contains `bagArmorText(it)` and the `copy.find.full.replace(...)` chain (existing pins)
    - LOOT branch: the `subFor` body reads `cmp.usable` (appended with a single space when non-empty) and the drop shelf call is `renderDropShelf(dropShelf, window.__mzDropShelfItems(c))`
    - Store rows: the region contains `window.__mzUsableBy(item.effectParams.item, S.c)` guarded by `item.effectParams && item.effectParams.item`, and the `<i>` sub renders `usable` appended after `sub` (or alone when `sub` is empty)
    - Joiner branch: the roll line ends with `` · ${window.__mzRations.eatsLine(j)}`` and the region still has no `innerHTML`, still has `const headLine = walker`
    - Company panel: the region reads `window.__mzRations.eatsLine(m)` once, capitalises its first character locally, renders it through `escText(...)`, and no longer contains the inline `RACES[m.race]` eats read; field order name -> sub -> class/level -> HP -> Weapon -> eats line -> DISMISS still holds; `HP <b>` still present
    - Hero tab: markup contains `<section class="panel" id="rations-panel">` once with `<h2>Rations <span id="s-rations-n"></span></h2>` and `<p class="trait" id="s-rations">`, positioned after `id="s-trait"` and before `id="hero-party"`; paint() contains `window.__mzRations.view(S)` once and writes `#s-rations` `textContent` from `.line` and `#s-rations-n` from `.carriedText` (never innerHTML)
    - `www/index.html` (when present) carries `__mzUsableBy`, `__mzRations`, `rations-panel`
  </behavior>
  <action>
    A. Module block: add the NEW import line after the pinned one (comment: Phase 43 (CLAR-02/03/04/05) — the clarity view models, a SEPARATE import line so the Phase 28/29/37/39 pins on the line above stay byte-identical). After `window.__mzItemRowState = itemRowState;` add the four bridges with a comment: Phase 43 — read-only bridges (no rng, no mutation; the engine alone enforces the class gate, the appetite, the bag cap): `__mzUsableBy` (FIND card / LOOT rows / store rows), `__mzRations` (`view` for the Hero RATIONS panel, `eatsLine` for the Joiner card + Company panel — both read `engine/movement.js#eatsFor`, the same read `__mzNightlyEats`'s camp gate uses), `__mzDropShelfItems` (the bag-full drop shelf's ONE source list — bag items only), `__mzGear` (Task 2's ON YOU empty-slot rows + copy).

    B. FIND branch (renderRail): compute `const usable = window.__mzUsableBy(it, c);` after `findSub`, and change the pushed line's text to `${it.n || "An item"}${findSub ? \` · ${findSub}\` : ""}${usable ? \` ${usable}\` : ""}`; change `shelfItems = c.items || []` to `shelfItems = window.__mzDropShelfItems(c)`. Comment: Phase 43 (CLAR-02/04).

    C. LOOT screen (renderEncounter's pendingLoot branch): `subFor` returns `(cmp.sub ? \`${cmp.line} · ${cmp.sub}\` : cmp.line) + (cmp.usable ? \` ${cmp.usable}\` : "")`; `renderDropShelf(dropShelf, window.__mzDropShelfItems(c))`.

    D. Store rows: `const usable = item.effectParams && item.effectParams.item ? window.__mzUsableBy(item.effectParams.item, S.c) : "";` and build the `<i>` as `${sub || usable ? \`<i>${sub || ""}${sub && usable ? " " : ""}${usable}</i>\` : ""}` (usable is a static COPY string — no user-controlled text).

    E. Joiner card: append `` · ${window.__mzRations.eatsLine(j)}`` to the existing roll template literal (Phase 43 CLAR-05: each Joiner shows "eats N a rest" on the offer card).

    F. Company panel (renderPartyRoster): before `card.innerHTML =`, add `const eatsLine = window.__mzRations.eatsLine(m); const eatsText = eatsLine.charAt(0).toUpperCase() + eatsLine.slice(1);` and replace the inline Eats div with `<div class="mw-party-line">${escText(eatsText)}</div>` (comment: Phase 43 CLAR-05 — the SAME appetite read as the camp gate and the Hero RATIONS panel; the old inline RACES read is gone).

    G. Hero RATIONS panel: the markup section from `<behavior>` (with a comment: Phase 43 (CLAR-03/05) — hero by race, each Joiner, the party total and what is carried, from `window.__mzRations.view` -> `engine/movement.js#nightlyEats`/`eatsFor`, so this can never disagree with the Make Camp refusal). In paint(), directly after the `s-cost` assignment: `const rv = window.__mzRations.view(S); document.getElementById("s-rations").textContent = rv.line; document.getElementById("s-rations-n").textContent = rv.carriedText;`.

    H. Tests: `test/unit/shell-clarity-43.test.js` (new) with the `<behavior>` source pins (helpers copied from shell-terrain-41.test.js), a voice test (`Rations`, the h2 texts) against the BANNED wordlist, an HP-not-WP check on the new regions, and the build-artefact test (skipped without `www/`). Re-pin `test/unit/shell-company-panel.test.js` (order needle -> `eatsLine(m)`; test 152 title + regex -> the bridge read) with a `// Phase 43 (CLAR-05)` reason; `test/unit/shell-party-camp.test.js` needs no change unless its joiner-region assertion fails on the appended clause — verify.

    Run `npm run build:www` then `node --test test/unit/shell-clarity-43.test.js test/unit/shell-company-panel.test.js test/unit/shell-party-camp.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-gear-39.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-terrain-41.test.js test/unit/shell-spells-40.test.js test/unit/shell-abilities.test.js test/voice/` — green. Commit `feat(43-04): usable-by on FIND/LOOT/store rows; Joiner card + Company panel eats line; Hero RATIONS panel; clarity bridges` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c 'import { characterSheetViewModel, grimoireViewModel, armorDisplay, bagArmorText, lootCompare, bagUsage, itemRowState } from "./src/browser/viewModels.js";' mazeworld.html` is 1; `grep -c 'import { usableBy, rationsViewModel, eatsLineFor, dropShelfItems, emptySlotRows, GEAR_COPY } from "./src/browser/viewModels.js";' mazeworld.html` is 1
    - `grep -c "window.__mzUsableBy = usableBy;" mazeworld.html` is 1; `grep -c "window.__mzRations = { view: rationsViewModel, eatsLine: eatsLineFor };" mazeworld.html` is 1; `grep -c "window.__mzDropShelfItems = dropShelfItems;" mazeworld.html` is 1; `grep -c "window.__mzGear = { emptySlotRows, copy: GEAR_COPY };" mazeworld.html` is 1; `grep -c "window.__mzNightlyEats = nightlyEats;" mazeworld.html` is 1
    - `grep -c "window.__mzUsableBy(it, c)" mazeworld.html` is 1; `grep -c "window.__mzUsableBy(item.effectParams.item, S.c)" mazeworld.html` is 1; `grep -c "cmp.usable" mazeworld.html` is at least 1; `grep -c "window.__mzDropShelfItems(c)" mazeworld.html` is 2; `grep -c "window.__mzRations.eatsLine(j)" mazeworld.html` is 1; `grep -c "window.__mzRations.eatsLine(m)" mazeworld.html` is 1; `grep -c "window.__mzRations.view(S)" mazeworld.html` is 1
    - `grep -c 'id="rations-panel"' mazeworld.html` is 1; `grep -c 'id="s-rations"' mazeworld.html` is 1; `grep -c 'id="s-rations-n"' mazeworld.html` is 1
    - `node --test test/unit/shell-clarity-43.test.js` exits 0 with at least 10 tests; `node --test test/unit/shell-company-panel.test.js test/unit/shell-party-camp.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-gear-39.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-terrain-41.test.js test/unit/shell-spells-40.test.js test/unit/shell-abilities.test.js test/voice/` exits 0
    - `npm run build:www` exits 0 and `grep -c "__mzRations" www/index.html` is at least 1
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www && node --test test/unit/shell-clarity-43.test.js test/unit/shell-company-panel.test.js test/unit/shell-party-camp.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-gear-39.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-terrain-41.test.js test/unit/shell-spells-40.test.js test/unit/shell-abilities.test.js test/voice/</automated>
  </verify>
  <done>Every loot offer names who can use it, every Joiner surface says what they eat, and the Hero sheet's RATIONS line reads the same appetite the camp gate charges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Gear tab as two panels — ON YOU (WIELDED / WORN with empty slots / ALSO ON YOU) and BAG — and the bag-only drop shelf</name>
  <files>mazeworld.html, test/unit/shell-clarity-43.test.js, test/unit/hp-not-wp.test.js, test/unit/shell-worn-slots.test.js, test/unit/shell-gear-toolbar.test.js, test/unit/shell-gear-39.test.js, test/unit/shell-loot-screen.test.js, test/unit/shell-armor-display.test.js</files>
  <read_first>
    - mazeworld.html lines 1542-1554 (the Gear tab markup: the `Gear` panel with `.mw-wilmst` + `<ul class="kit" id="s-kit">`, and the `Carried` panel with `#s-carry-n` + `#s-carry`), 3264-3299 (paint()'s kit `rows` array — the FIRST TWO entries are the weapon row and the armorD row, now duplicated by the ON YOU rows; every later row from `["Potions", …]` through `rows.push(["Kills", …])` stays — test/unit/shell-spells-40.test.js:105 pins the `Sense Danger` -> `Kills` region), 3355-3452 (the carry region: `const carry = document.getElementById("s-carry");` … `wornRow` (appends to `carry`) … `wornRow(armorD.label, …)` … `const wornSlotRow = (slot, it) => {` (appends to `carry`) … `for (const slot of (window.__mzWornSlots || [])) { … }` … `renderCarriedList(carry, items, { clear: false, …, gearRow: true, … })`), 4076-4088 (`renderDropShelf(shelf, items)` — `forEach((bi, i) => … window.mzDropItem?.(i))`), 219-232 (`ul.kit`/`ul.skills` CSS, `li.none` style)
    - The pins that MUST keep holding (read each): test/unit/shell-worn-slots.test.js 136-166 (`wornRow` signature literal; anchors `wornRow(armorD.label` < `const wornSlotRow = (slot, it) => {` < `renderCarriedList(carry, items, {` inside the region from `const carry = document.getElementById("s-carry");` to `document.getElementById("doss-who")`; in the wornSlotRow region: exactly one `window.mzUseItem?.({ slot })`, one `window.mzUnequip?.(slot)`, one `for (const slot of (window.__mzWornSlots || []))`, `window.__mzItemRowState(S, it)`, no `innerHTML`, `tag.className = "mw-worn-tag";`), test/unit/shell-gear-toolbar.test.js 80-131 (`DROP_CONFIRM_MS` directly above `renderCarriedList`; `gearRow: true` exactly once, inside the carry region, never in the store region), test/unit/shell-gear-39.test.js 121-133, test/unit/shell-loot-screen.test.js 236-262 (`window.__mzBagUsage(c)`/`usage.text`/`usage.full` in the carry region), test/unit/shell-armor-display.test.js 79-83 and 152-157 (`wornRow(armorD.label, ` present; `renderDropShelf` region reads `bagArmorText(bi)`)
    - src/browser/viewModels.js (Plan 03): `GEAR_COPY` keys `onYou/wielded/worn/alsoOnYou/bag/empty`, `emptySlotRows(c)` -> `[{ slot, text }]` (armor first when unworn, then empty WORN_SLOTS), `dropShelfItems(c)` -> `[{ it, i }]`
  </read_first>
  <behavior>
    - Markup: `#screen-gear` contains exactly two `<section class="panel">` children in order: `id="onyou-panel"` (`<h2>On you</h2>`, the `.mw-wilmst` chip with `#m-gold`, `<ul class="skills" id="s-onyou">`, then `<ul class="kit" id="s-kit">`) and `id="bag-panel"` (`<h2>Bag <span id="s-carry-n"></span></h2>`, `<ul class="skills" id="s-carry">`); the source index order is onyou-panel < s-onyou < s-kit < bag-panel < s-carry-n < s-carry; `GEAR_COPY.onYou`/`GEAR_COPY.bag` equal the two h2 texts case-insensitively (the test imports GEAR_COPY)
    - paint(): the carry region begins with `const carry = document.getElementById("s-carry");` immediately followed by `const onyou = document.getElementById("s-onyou");`; both lists are cleared; a `headRow(text)` helper appends `li.none.mw-onyou-head` rows via `textContent`; an `emptyRow(text)` helper appends `li.none.mw-worn-empty` rows via `textContent`; the ON YOU order is `headRow(gearCopy.wielded)` -> the weapon `wornRow` -> `headRow(gearCopy.worn)` -> the armor `wornRow(armorD.label, …)` or its empty row -> per WORN_SLOTS: `wornSlotRow(slot, it)` or the slot's empty row from `window.__mzGear.emptySlotRows(c)`; `wornRow` and `wornSlotRow` append to `onyou` (the `carry.appendChild(li)` calls inside those two builders are gone); `renderCarriedList(carry, items, { clear: false, … gearRow: true … })` is unchanged
    - The kit: `rows` no longer starts with the weapon/armor pair — its first entry is `["Potions", c.potions]`; every row from `["Potions"` through `rows.push(["Kills", …])` is byte-identical; a `li.mw-kit-head` row with text `gearCopy.alsoOnYou` is prepended to `#s-kit` at paint (textContent)
    - `renderDropShelf(shelf, entries)`: iterates `(entries || []).forEach(({ it: bi, i }) => …)`, keeps `bagArmorText(bi)` for armor, dispatches `window.mzDropItem?.(i)` with the entry's own `i`; both call sites pass `window.__mzDropShelfItems(c)` (Task 1); the function body never reads `c.items`, `c.worn`, `c.weapon` or `c.armor`
    - CSS: `.mw-onyou-head`, `.mw-worn-empty`, `.mw-kit-head` exist exactly once each; none carries `transition`, `animation` or `aria-disabled`; `.mw-worn-empty` dims (`color:var(--ink-soft)`), `.mw-onyou-head` is the small mono uppercase label style
    - Every existing shell pin listed in read_first still passes unchanged (no re-pin needed in shell-worn-slots / shell-gear-toolbar / shell-gear-39 / shell-loot-screen / shell-armor-display — if one fails, fix the SHELL to satisfy it, not the pin, unless the pin literally encodes the old single-panel layout, in which case re-pin with a `// Phase 43 (CLAR-04)` reason)
  </behavior>
  <action>
    A. Markup: replace the two Gear sections with the `<behavior>` structure (comment above: `Phase 43 (CLAR-04): two panels — ON YOU (WIELDED / WORN / ALSO ON YOU) and BAG; the roadmap's "Carried: weapon, staff, shield" grouping is superseded — staff is a worn slot (Phase 37) and there are no shields; see docs/CLARITY.md`).

    B. paint() carry region: add `const onyou = document.getElementById("s-onyou");` right after the `carry` const; `onyou.innerHTML = "";` beside `carry.innerHTML = "";`; `const gearCopy = window.__mzGear.copy; const empties = window.__mzGear.emptySlotRows(c); const emptyFor = (slot) => empties.find((r) => r.slot === slot);` and the `headRow`/`emptyRow` helpers (createElement + textContent + className `none mw-onyou-head` / `none mw-worn-empty`, appended to `onyou`). Change the two `carry.appendChild(li)` lines inside `wornRow` and `wornSlotRow` to `onyou.appendChild(li)`. Insert `headRow(gearCopy.wielded);` before the weapon `wornRow(...)` call, `headRow(gearCopy.worn);` before the armor `if (armorD.worn || armorD.magic) wornRow(armorD.label, …)` and give it an `else { const r = emptyFor("armor"); if (r) emptyRow(r.text); }`. Rewrite the WORN_SLOTS loop body as `{ const it = c.worn && c.worn[slot]; if (it) wornSlotRow(slot, it); else { const r = emptyFor(slot); if (r) emptyRow(r.text); } }` (still ONE `for (const slot of (window.__mzWornSlots || []))`). Update the region's comments (the Phase 37 "ON YOU / BAG two-panel split is Phase 43, not here" note becomes "Phase 43 (CLAR-04): ON YOU rows land in #s-onyou; #s-carry is the BAG").

    C. Kit: delete the weapon row and the armorD row from the `rows` literal (the two entries before `["Potions", c.potions]`) with a comment `Phase 43 (CLAR-04): the weapon/armor rows now live in the ON YOU list above — no duplicates`; before the `for (const [a, b] of rows)` loop add the `mw-kit-head` row (`li.className = "mw-kit-head"; li.textContent = gearCopy.alsoOnYou; kit.appendChild(li);`). The `w`/`armorD` consts stay (still used by the HUD lines).

    D. `renderDropShelf(shelf, entries)`: rename the parameter, iterate `{ it: bi, i }` entries, keep everything else (comment: Phase 43 (CLAR-04) — entries come from `window.__mzDropShelfItems(c)`: slot-consuming BAG items with their true `c.items` index; worn/wielded pieces and slot-exempt potions never appear here).

    E. CSS: three rules near the `.mw-worn`/`.mw-drop-confirm` rules: `.mw-onyou-head{font-family:var(--mono);font-size:10px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-soft);border-left-color:var(--rule);padding-top:6px}`, `.mw-worn-empty{color:var(--ink-soft);font-style:italic}`, `.mw-kit-head{font-family:var(--mono);font-size:10px;letter-spacing:.08em;text-transform:uppercase;color:var(--ink-soft);border-bottom:1px dotted var(--rule)}`.

    F. Tests: extend `test/unit/shell-clarity-43.test.js` with every `<behavior>` pin (markup order, region anchors, the two `onyou.appendChild(li)` calls, the kit's first row, the `mw-kit-head` row, `renderDropShelf` entries shape, the CSS rules, GEAR_COPY-vs-h2 check, voice of the new region). HP-not-WP hand-off: if `test/unit/hp-not-wp.test.js` carries any `ALLOWLIST` entry tagged `Plan 04` (a shell player-copy straggler Plan 01 could not fix), fix that copy in `mazeworld.html` now and delete the entry — the test must end this plan with no `Plan 04`-tagged allowlist row. Run the full shell-pin set: `node --test test/unit/shell-clarity-43.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-gear-39.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-spells-40.test.js test/unit/shell-company-panel.test.js test/unit/shell-party-camp.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-terrain-41.test.js test/unit/shell-abilities.test.js test/unit/shell-input-guards.test.js test/unit/shell-fight-gate.test.js test/unit/shell-combat-over.test.js test/voice/` — green; `npm run build:www` exit 0. Commit `feat(43-04): Gear tab = ON YOU (wielded / worn with empty slots / also on you) + BAG; bag-only drop shelf` (explicit paths).
  </action>
  <acceptance_criteria>
    - `grep -c 'id="onyou-panel"' mazeworld.html` is 1; `grep -c 'id="s-onyou"' mazeworld.html` is 1; `grep -c 'id="bag-panel"' mazeworld.html` is 1; `grep -c 'id="s-kit"' mazeworld.html` is 1; `grep -c 'id="s-carry"' mazeworld.html` is 1; `grep -c '<h2>On you</h2>' mazeworld.html` is 1; `grep -c '<h2>Bag <span id="s-carry-n"></span></h2>' mazeworld.html` is 1
    - `grep -c 'const onyou = document.getElementById("s-onyou");' mazeworld.html` is 1; `grep -c "onyou.appendChild(li)" mazeworld.html` is at least 4 (wornRow, wornSlotRow, headRow, emptyRow); `grep -c "headRow(gearCopy.wielded)" mazeworld.html` is 1; `grep -c "headRow(gearCopy.worn)" mazeworld.html` is 1; `grep -c "window.__mzGear.emptySlotRows(c)" mazeworld.html` is 1; `grep -c "li.textContent = gearCopy.alsoOnYou" mazeworld.html` is 1
    - `grep -c "const wornRow = (label, sub, slot, canUnequip, noSlotNeeded) =>" mazeworld.html` is 1; `grep -c "for (const slot of (window.__mzWornSlots || []))" mazeworld.html` is 1; `grep -c "gearRow: true" mazeworld.html` is 1; `grep -c "renderCarriedList(carry, items, {" mazeworld.html` is 1
    - `grep -c "function renderDropShelf(shelf, entries)" mazeworld.html` is 1; `grep -c "forEach(({ it: bi, i }) =>" mazeworld.html` is 1; `grep -c "window.__mzDropShelfItems(c)" mazeworld.html` is 2
    - `grep -c "^\.mw-onyou-head{" mazeworld.html` is 1; `grep -c "^\.mw-worn-empty{" mazeworld.html` is 1; `grep -c "^\.mw-kit-head{" mazeworld.html` is 1
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('const rows = [');const b=s.indexOf('rows.push([\"Kills\"');const r=s.slice(a,b);process.exit(r.indexOf('[\"Potions\", c.potions]')!==-1&&r.indexOf('[\"Potions\", c.potions]')<r.indexOf('\"Rations\"')&&r.indexOf('armorD.label, armorD.under')===-1?0:1)"` exits 0 (the kit's first row is Potions; the armor duplicate is gone)
    - `node --test test/unit/shell-clarity-43.test.js` exits 0 with at least 22 tests; the full shell-pin set listed in step F exits 0; `npm run build:www` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www && node --test test/unit/shell-clarity-43.test.js test/unit/shell-worn-slots.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-gear-39.test.js test/unit/shell-loot-screen.test.js test/unit/shell-armor-display.test.js test/unit/shell-spells-40.test.js test/unit/shell-company-panel.test.js test/unit/shell-party-camp.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-terrain-41.test.js test/unit/shell-abilities.test.js test/unit/shell-input-guards.test.js test/unit/shell-fight-gate.test.js test/unit/shell-combat-over.test.js test/voice/</automated>
  </verify>
  <done>The Gear tab tells the truth in two panels — what is on you (with every empty slot named, in voice) and what is in the bag — and the drop prompt can only ever drop a bag item.</done>
</task>

<task type="auto">
  <name>Task 3: Phase close — CLARITY requirements map, REQUIREMENTS.md CLAR-01..05, whole-phase gate, aggregated Pixel 7 checklist, For PROJECT.md</name>
  <files>docs/CLARITY.md, .planning/REQUIREMENTS.md, .planning/phases/43-clarity-pass/43-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/41-terrain-darkness-phobias/41-04-SUMMARY.md lines 206-260 (the `## Success Criteria Map`, `## For PROJECT.md` and `## Human verification (deferred to end of run)` aggregated-checklist format — grouped by plan, numbered continuously, de-duplicated with "(also Plan NN)") and lines 1-120 (frontmatter `coverage` entries with `human_judgment`)
    - .planning/phases/42-flee-retune-consolidated-balance-close/42-04-SUMMARY.md lines 163-230 (`## Corrections to CONTEXT` aggregation; a plan with no device items records `None — …` honestly)
    - .planning/phases/43-clarity-pass/43-01-SUMMARY.md, 43-02-SUMMARY.md, 43-03-SUMMARY.md (their `## Human verification` and `## Corrections to CONTEXT` sections — the inputs to aggregate)
    - .planning/REQUIREMENTS.md lines 61-67 (the CLAR checkboxes) and 131-135 (the traceability rows `| CLAR-0N | Phase 43 | Pending |`)
    - docs/CLARITY.md `## Requirements map` placeholder; docs/FLEE.md lines 177-193 (map format)
  </read_first>
  <action>
    A. `docs/CLARITY.md` `## Requirements map`: a table `| Requirement | Proof |` — CLAR-01 -> the inventory section, engine additive keys (Plans 01/02), `test/unit/clarity-cause-lines.test.js`, `test/unit/rations-audit.test.js` (day-cycle rows), the coverage guards; CLAR-02 -> `usableBy`/`USABLE_COPY`/`lootCompare.usable`, `test/unit/usableBy.test.js`, the FIND/LOOT/store wiring + `test/unit/shell-clarity-43.test.js`; CLAR-03 -> `rationsViewModel` (total === nightlyEats), `test/unit/rationsViewModel.test.js`, `#rations-panel`; CLAR-04 -> `dropShelfItems`/`emptySlotRows`/`GEAR_COPY`, `test/unit/gear-panels.test.js`, the two-panel markup pins; CLAR-05 -> `docs/RATIONS.md`, `eatsFor`, `rationsEaten` naming the rules, `eatsLineFor` on the Joiner card + Company panel. Add `## Out of scope / next`: tutorial hooks (UX-06), shell modularisation of the Gear/Hero tabs (cleanup milestone), the spell-charge-spent line (no dedicated event — recorded, not retrofitted), the climb dice payload (carried over, out of v1.5).

    B. `.planning/REQUIREMENTS.md`: flip CLAR-01, CLAR-02, CLAR-03, CLAR-05, CLAR-04 to `[x]` and their traceability rows to `Complete` (five checkbox edits + five table edits; nothing else in the file changes).

    C. WHOLE-PHASE GATE (verification agents are off; record every number in the SUMMARY): `npm test` (`# tests`, `# pass`, `# fail` = 0); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty; `grep -c fonts.googleapis mazeworld.html` = 0; `node --test test/unit/floor-gen-rng-pin.test.js test/unit/chargen-rng-pin.test.js` green; `git diff --stat 84c6722 -- package.json package-lock.json docs/class-pass/` empty (no installs, no matrix re-run); `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/hp-not-wp.test.js test/voice/` green (SC-5 hard gate, run explicitly and quoted).

    D. Commit `docs(43-04): CLARITY requirements map; REQUIREMENTS CLAR-01..05 complete; whole-phase gate` (explicit paths). Write `43-04-SUMMARY.md`: standard sections; frontmatter `requirements-completed: [CLAR-01, CLAR-02, CLAR-03, CLAR-04, CLAR-05]` and `coverage` entries per surface (the last one `human_judgment: true` for the deferred device checklist); `## Success Criteria Map (ROADMAP SC-1..5, as amended by CONTEXT)` (SC-4's grouping amended per CONTEXT §CLAR-04); `## For PROJECT.md` — the Key Decisions this phase hands the orchestrator: (1) the cause-first cost-line convention (`<Cause>: <why>. −N <unit>.`, payload-sourced, additive keys only, combat strike lines keep dice-first) as a standing rule for every future cost event; (2) `eatsFor`/`nightlyEats` as the ONE appetite definition read by the camp gate, the fed-night charge, the refusal, the Hero RATIONS panel, the Joiner card and the Company panel — and the audit verdict (prototype ration rules fully implemented; rulebook-only sleep/wandering WP rules deliberately not adopted); (3) the Gear tab's ON YOU / BAG grouping (staff is worn, no shields, potions/rations/scrolls are ALSO ON YOU because they need no slot; the drop prompt is bag-only); `## Human verification (deferred to end of run)` — the aggregated Pixel 7 checklist: Plans 01-03's items (from their SUMMARYs, de-duplicated) then Plan 04's: (a) FIND card for a class-restricted item shows `(usable by …)` / `(… — not you)` after the item line; (b) the LOOT screen rows show it after the compare line; (c) store rows show it in the sub line; (d) the Joiner offer card's roll line ends `· eats N a rest` (2 for a Troll); (e) the Company panel's line reads `Eats N a rest` from the same source; (f) the Hero tab's RATIONS panel reads the full line and `N carried`, and changes immediately after Make Camp / a ration purchase / a Joiner joining; (g) the Gear tab shows ON YOU (WIELDED / WORN with `ring — nothing…` style empty rows, `staff — nothing, and nothing you could hold…` for a non-Magic-User, ALSO ON YOU) and BAG with `n / slots`; Use/Unequip/swap-confirm still work on the ON YOU rows; Equip/Use/Drop still work on BAG rows; (h) with a full bag, the find card's and loot screen's drop shelf lists ONLY bag items — never the worn ring/cloak/staff, never the weapon/armor, never a potion; (i) TalkBack reads the RATIONS panel and the ON YOU head rows; (j) a voice read of every new string (`usable by`, empty-slot rows, RATIONS line, FED card). Then `## Corrections to CONTEXT` aggregated from all four plans (Death spell not potion; no foe DOT on the hero; the Company panel already had an eats line; the Joiner offer carries no item; the txt-carve-out seed list; anything Plans 02-03 recorded); `## Next Phase Readiness` — Phase 43 is the last v1.5 phase: milestone close (the orchestrator assembles the UAT batch from every phase's checklist; the standing ask-before-Play-internal-deploy rule applies after the batch).
  </action>
  <acceptance_criteria>
    - `grep -c "^## Requirements map" docs/CLARITY.md` is 1; `grep -cE "^\| CLAR-0[1-5] " docs/CLARITY.md` is 5; `grep -c "^## Out of scope / next" docs/CLARITY.md` is 1; `grep -c "usableBy.test.js" docs/CLARITY.md` is at least 1
    - `grep -cE "^- \[x\] \*\*CLAR-0[1-5]\*\*" .planning/REQUIREMENTS.md` is 5; `grep -cE "^- \[ \] \*\*CLAR-0[1-5]\*\*" .planning/REQUIREMENTS.md` is 0; `grep -cE "^\| CLAR-0[1-5] \| Phase 43 \| Complete \|" .planning/REQUIREMENTS.md` is 5
    - `npm test` output ends with `# fail 0`; `# pass` is at least the Plan 03 SUMMARY's recorded pass count + 30
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0; `git status --porcelain test/parity/fixtures` is empty; `grep -c fonts.googleapis mazeworld.html` is 0; `git diff --stat 84c6722 -- package.json package-lock.json docs/class-pass/` is empty
    - `node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/hp-not-wp.test.js test/unit/floor-gen-rng-pin.test.js test/unit/chargen-rng-pin.test.js test/voice/` exits 0
    - `.planning/phases/43-clarity-pass/43-04-SUMMARY.md` exists and contains `## Success Criteria Map`, `## For PROJECT.md`, `## Human verification (deferred to end of run)` with `### Plan 01`, `### Plan 02`, `### Plan 03`, `### Plan 04` subsections, and `## Corrections to CONTEXT`; `grep -c "requirements-completed: \[CLAR-01, CLAR-02, CLAR-03, CLAR-04, CLAR-05\]" .planning/phases/43-clarity-pass/43-04-SUMMARY.md` is 1
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && git status --porcelain test/parity/fixtures && grep -cE "^\| CLAR-0[1-5] \| Phase 43 \| Complete \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>Phase 43 — and with it v1.5 — is closed: every CLAR requirement complete and ledgered, the whole-phase gate green with the master untouched, and the deferred device checklist aggregated for the milestone-close UAT batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine state (names, item names) -> classic-script innerHTML | Company panel / store rows / find card interpolate strings into markup |
| module bridges -> classic script | `window.__mz*` read-only helpers consumed at paint time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-10 | Tampering (XSS) | Company panel eats line | low | mitigate | rendered through `escText(...)`; the string is COPY + a number |
| T-43-11 | Tampering (XSS) | store row `usable` suffix inside `<i>` innerHTML | low | mitigate | `usable` is a static USABLE_COPY string (class names), never user/item text; `item.n`/`sub` interpolation is pre-existing |
| T-43-12 | Tampering (XSS) | ON YOU empty rows / head rows | low | mitigate | `textContent` only; the wornSlotRow region stays innerHTML-free (pinned) |
| T-43-13 | Denial of service | a missing bridge at paint time | low | mitigate | bridges are assigned before `window.__mzClassicBoot()` runs (same ordering as `__mzArmorDisplay`); the pin test asserts each bridge exists once |
| T-43-14 | Tampering | dropping the wrong item from the shelf | medium | mitigate | entries carry the true `c.items` index from `dropShelfItems`; the engine's `dropItem(i)` re-validates the index |
| T-43-SC | Tampering | npm/pip/cargo installs | low | accept | no installs; `package.json`/lock diff against 84c6722 asserted empty |
</threat_model>

<verification>
- Whole-phase gate: `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0; fixtures porcelain empty; no font CDN; rng pins green; no package/matrix changes; SC-5 guards run explicitly.
- Source pins prove the three loot surfaces, two Joiner surfaces, the RATIONS panel, the two Gear panels and the bag-only shelf.
- REQUIREMENTS.md CLAR-01..05 complete; CLARITY ledger closed; aggregated checklist + For PROJECT.md in the SUMMARY.
</verification>

<success_criteria>
- ROADMAP SC-1..5 (as amended by CONTEXT) all landed and proven; v1.5's last phase closed.
- Only `mazeworld.html` + tests + docs changed in this plan; zero engine/parity change.
</success_criteria>

<output>
Create `.planning/phases/43-clarity-pass/43-04-SUMMARY.md` when done (standard sections + `## Success Criteria Map` + `## For PROJECT.md` + the aggregated `## Human verification (deferred to end of run)` + `## Corrections to CONTEXT` + the whole-phase gate numbers).
</output>
