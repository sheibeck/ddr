---
phase: 43-clarity-pass
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - engine/movement.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - src/browser/viewModels.js
  - test/voice/safety-scan.test.js
  - test/unit/rations-audit.test.js
  - test/unit/rationsViewModel.test.js
  - test/unit/hp-not-wp.test.js
  - test/unit/rail.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/RATIONS.md
  - docs/CLARITY.md
autonomous: true
requirements: [CLAR-01, CLAR-03, CLAR-05]
tags: [engine, rations, audit, ledger, narration, view-model, deferred-uat]

must_haves:
  truths:
    - "One definition of appetite exists — `engine/movement.js#eatsFor(sheet)` — and `nightlyEats`, `newDay`'s ration charge, `makeCamp`'s refusal (`campFailed.members[].eats`), the new `rationsEaten.eaters[].eats` and `rationsViewModel` all read it, so the Hero sheet can never disagree with what Make Camp charges (ROADMAP SC-3, CLAR-03, CONTEXT §CLAR-03/05)"
    - "The race/class ration and upkeep rules are audited against `test/parity/prototype-master.js.txt` (read-only) and `mazeworld.pdf` and recorded in `docs/RATIONS.md` (rule · prototype line · rulebook page · engine site · event/narration · status); any prototype rule the engine lacked is implemented as a declared divergence with the fixtures it moves measured — the planning-time audit expects ZERO missing prototype rules and the executor confirms or corrects that live (CLAR-05, greenfield ruling)"
    - "Ration consumption at camp / the 100-square day is a narrated cost: a new `rationsEaten { eats, left, eaters }` event reads `Rations: you eat 1; Grunk (Troll) eats 2. Trolls eat for two. −3 rations, 4 left.` — every rule that applied is NAMED in the rest narration (CLAR-05: 'Trolls eat for two.'); the unfed night reads `Hunger: nobody packed — you eat 1 a night, and you had 0. Cost of living −4 hp.` with Heft's halving named when it applied (CLAR-01 day-cycle rows)"
    - "A pure `rationsViewModel(state)` (hero eats + why, each member eats + why, total === nightlyEats(state), carried, nights, one in-voice line) and `eatsLineFor(sheet)` ('eats N a rest') exist in `src/browser/viewModels.js` for Plan 04's Hero RATIONS panel, Joiner offer card and Company panel"
    - "Every new/changed narration goes through EVENT_NARRATION + TOAST_FOR + RAIL_FAMILY; `toastsCoverage.test.js`/`formatEventsCoverage.test.js`/voice scan/`hp-not-wp.test.js` green (SC-5)"
    - "Zero parity fixture moves (events are not compared; `eatsFor` is value-identical to the inline reads); the master is never edited"
  artifacts:
    - "engine/movement.js#eatsFor(sheet) (exported); `rationsEaten { eats, left, eaters:[{ name, race, eats, hero? }] }` pushed in newDay's fed branch after dayBegan; `wentHungry { cost, need, have, mouths, heft? }`"
    - "src/browser/eventNarration.js: EVENT_NARRATION.rationsEaten, rewritten wentHungry, exported RATION_RULE_LINE; src/browser/toasts.js TOAST_FOR.rationsEaten + wentHungry; src/browser/rail.js RAIL_FAMILY.rationsEaten"
    - "src/browser/viewModels.js#rationsViewModel(state), #eatsLineFor(sheet), RATIONS_COPY"
    - "docs/RATIONS.md (the CLAR-05 ledger); docs/CLARITY.md rows 34-38 filled"
    - "test/unit/rations-audit.test.js, test/unit/rationsViewModel.test.js; test/parity/FIXTURE-INVENTORY.md `### Plan 02` subsection"
  key_links:
    - "content/races.js eats -> engine/movement.js#eatsFor -> nightlyEats (camp gate + newDay charge) AND rationsEaten.eaters AND campFailed.members AND rationsViewModel — one read, five consumers"
    - "rationsEaten.eaters[].race -> eventNarration.js#RATION_RULE_LINE -> the named rule sentence; RATIONS_COPY.why[race] -> the Hero-sheet reason clause (the same race key drives both)"
    - "docs/RATIONS.md rows <- test/unit/rations-audit.test.js pins every numeric rule against the live engine (the ledger can never drift)"
---

<objective>
Audit the engine's ration/upkeep rules against the frozen prototype and the rulebook, ledger every rule in `docs/RATIONS.md`, factor the ONE appetite read (`eatsFor`), make ration consumption a narrated, cause-first cost (`rationsEaten`) whose line names every rule that applied, rewrite the unfed-night line to name hunger and the cost-of-living rule (Heft's halving included), and deliver the pure `rationsViewModel`/`eatsLineFor` that Plan 04's Hero RATIONS panel, Joiner offer card and Company panel will read.

Purpose: ROADMAP SC-3 (CLAR-03/05) — "the Hero screen lists rations eaten per rest for the hero and each Joiner and the total, matching what Make Camp actually charges — the race/class ration rules are audited against the prototype/rulebook and recorded in a ledger"; plus the CLAR-01 day-cycle rows (rations eaten, hunger) that Plan 01's inventory assigned here.

Output: `eatsFor`, the `rationsEaten` event, the `wentHungry` additive keys, the three narration surfaces, `rationsViewModel`/`eatsLineFor`/`RATIONS_COPY`, `docs/RATIONS.md`, the two new tests, FIXTURE-INVENTORY `### Plan 02`, 43-02-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-clarity-pass/43-CONTEXT.md
@.planning/phases/43-clarity-pass/43-01-SUMMARY.md
@engine/movement.js
@engine/derived.js
@engine/character.js
@engine/economy.js
@engine/combat.js
@content/races.js
@content/skills.js
@src/browser/eventNarration.js
@src/browser/toasts.js
@src/browser/rail.js
@src/browser/viewModels.js
@test/parity/prototype-master.js.txt
@docs/CLARITY.md
@docs/FLEE.md
</context>

<standing_rules>
- Engine gate: pure/deterministic; NO new rng draw; NO new serialized field (`rationsEaten` is an event; `eatsFor` is a pure read); `test/parity/prototype-master.js.txt` is READ-ONLY (hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`) — it is the audit SOURCE, never a target.
- Greenfield ruling: if the audit finds a prototype ration/upkeep rule the engine lacks, implementing it IS a rule change — declare it (before/after + rationale in `docs/RATIONS.md` and `test/parity/FIXTURE-INVENTORY.md`), measure exactly which fixtures rest (a fixture must reach `newDay` to move: `grep -c dayBegan` in the movement fixture's replay — the 100-square tick in `action-script.movement.json` seed 256 is the ONE known rest), regenerate only those. Planning-time finding: the engine implements every prototype ration rule (see Task 1's row list) — expected zero declared divergences from this plan. Never adopt a RULEBOOK-only rule the prototype dropped (prototype is canon; the AFTER matrix is closed).
- Additive-only payloads; `campFailed`'s shape `{ type, reason, need, have }` (solo) is pinned EXACTLY by test/unit/movement.test.js:984 — keep it (only its `eats` VALUES route through `eatsFor`, unchanged values).
- No `mazeworld.html` edits in this plan. Camp and the 100-square tick are NARRATIVE_ACTIONS (`camp`, `move`), so the new Oracle sentences reach the rail with no shell change.
- Deferred UAT; verification agents OFF (last task runs the gate: `npm test` `# fail 0`, master hash, `npm run build:www` exit 0); HP not WP; voice scan; coverage guards; BRANCH_TOGGLES for new branches.
- Commits per task: `feat(43-02): …` / `test(43-02): …` / `docs(43-02): …` with the two trailer lines. Explicit-path `git add` only; never stage `store-listing/` or `tools/store-screenshots/`.
</standing_rules>

<artifacts_this_phase_produces>
See 43-01-PLAN.md `<artifacts_this_phase_produces>` for the phase-wide list. This plan produces: `engine/movement.js#eatsFor`; the `rationsEaten` event; `wentHungry.need/.have/.mouths/.heft`; `EVENT_NARRATION.rationsEaten` + `RATION_RULE_LINE`; `TOAST_FOR.rationsEaten`; `RAIL_FAMILY.rationsEaten`; `src/browser/viewModels.js#rationsViewModel`, `#eatsLineFor`, `RATIONS_COPY`; `docs/RATIONS.md`; `test/unit/rations-audit.test.js`; `test/unit/rationsViewModel.test.js`; FIXTURE-INVENTORY `### Plan 02 — rations: audit + rationsEaten event, zero fixture moves`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The ration/upkeep audit -> docs/RATIONS.md; eatsFor; the rationsEaten event; wentHungry additive keys; audit pin test</name>
  <files>engine/movement.js, docs/RATIONS.md, test/unit/rations-audit.test.js, test/parity/FIXTURE-INVENTORY.md</files>
  <read_first>
    - test/parity/prototype-master.js.txt (READ-ONLY) lines 1052 (`upkeep()`), 1256 (`if (S.steps % 100 === 0) newDay();`), 1272-1327 (`newDay`: `eats = R_().eats || 1`; fed branch charges rations and NARRATES "pay N wp of upkeep from your rations" but subtracts NO wp; heal `D(10) + 2*level`, doubled for `heal2x` race / Soldier; unconditional affliction cure; armour patch; Warlock weekly potion; unfed branch `S.c.wp -= cost` + `die("starve")` at <= 0; d20 per hour wandering check), 1329-1338 (`makeCamp` gate `S.c.rations < (R_().eats || 1)` — hero only), 279-289 (RACES upkeep 4/4/1/4/4/15; Troll `eats: 2`), 159 (Heft "half upkeep"), 641 (starting rations F6/T5/M4), 1558-1559 (store food: +wp AND `S.c.rations++`), 1704-1710 (`findFood`: +wp AND `S.c.rations++`), 1978-1987 (Cooking: +quarter wp and +1 ration; else meat d6>=4 -> +1 ration)
    - `mazeworld.pdf` via `pdftotext mazeworld.pdf - | grep -n -i -A3 "cost of living\|^sleep\|heft\|food table"`: p.9 "Cost of Living — 4wp per day … replenished by food, drink or sleep"; p.9 "Sleep — 8 hours; each hour less than 8 loses an additional 1WP"; p.11 Dwarves "only lose 1WP a day"; p.12 Trolls "cost of living is 15WP per day"; Thief skills "Heft … cost of living is only [half] of what they normally lose"; p.14 "Each 100 squares … one day; subtract the cost of living"; p.14 wandering monsters "the 1wp you would have received for that one hour [is] forfeit"; p.48 Food table (Chicken 20/+12, Bread 15/+5, Water 5/+2, Ale 10/+8, Meat …). The rulebook has NO ration count — "rations" are the prototype's discretisation of food.
    - engine/movement.js lines 543-557 (`nightlyEats`), 566-700 (`newDay` — `upkeep(c)` + party `upkeep(m)`; `eats = nightlyEats(state)`; `dayBegan`; fed branch `c.rations -= eats` … `rested { amount, doubled }`; affliction cure ROLL (declared audit-batch1 A3); Sewing/Master of Arms patch (declared 04.1-02); Warlock DAILY (declared 04-DR12); unfed `wentHungry { cost }` + `die("starve")`; Bard wake-on-2 (declared Phase 24)), 715-731 (`makeCamp` — `campFailed { reason, need, have, members? }`, DFB-06 declared), engine/derived.js lines 1188-1194 (`upkeep(c)` = `max(1, round(R.upkeep × (Heft ? 0.5 : 1)) + eff(c, "upkeep"))` — no content row carries an `upkeep` eff, so that term is always 0 today), 168-215 (`clampCarry` — `c.rations` clamped to `BAGS[c.bag].rations`, gated on `c.bag`), engine/character.js line 542 (starting rations), engine/economy.js lines 190-220 (RATION-01: Rations store line, food is pure hp — declared 04.1-03), engine/encounters.js ~325 (findFood no longer adds a ration — declared 04.1-03), engine/combat.js lines 820-833 (`cooked { wp, rations }`)
    - content/races.js (`eats: 2` only on Troll; `upkeep` per race), content/skills.js line 46 (Heft), content/bags.js (`rations` cap per tier)
    - test/unit/movement.test.js lines 940-1060 (the existing camp/newDay tests and the EXACT `campFailed` pin at 984-985), test/unit/water-cost.test.js lines 185-210 (dayBegan count pins — a `rationsEaten` push must not change `dayBegan` counts)
    - docs/FLEE.md (ledger header + `## Requirements map` style), docs/CLARITY.md (Plan 01's inventory rows 34-38 to fill)
  </read_first>
  <behavior>
    - `eatsFor({ race: "Troll" })` is 2; `eatsFor({ race: "Human" })`, `({ race: "Elven" })`, `({ race: "Dwarven" })`, `({ race: "Wilmsry" })`, `({ race: "Fridgian" })` are 1; `eatsFor({})` and `eatsFor(null)` are 1; `nightlyEats(state)` equals `eatsFor(state.c) + Σ eatsFor(m)` over `state.party` for a solo Human (1), a solo Troll (2), a Human with a Troll member (3), a Troll with two Human members (4)
    - A fed `newDay` (rations >= eats) pushes, in order, `dayBegan`, then `rationsEaten` with keys exactly `type, eats, left, eaters`, then (when hp rose) `rested`; `eats === nightlyEats(state)` as read before the charge; `left === c.rations` after the charge; `eaters[0]` is `{ name: c.name, race: c.race, eats: eatsFor(c), hero: true }` and every member follows as `{ name, race, eats }` in party order; `c.rations` after equals before − eats (unchanged arithmetic)
    - An unfed `newDay` pushes `wentHungry` with keys `type, cost, need, have, mouths` plus `heft: true` ONLY when `skill(c, "Heft")`; `need === nightlyEats(state)`, `have === c.rations` (unchanged, since nothing was eaten), `mouths === 1 + (state.party?.length ?? 0)`; `cost` and the hp after are unchanged from the parent revision
    - `makeCamp` refusal is byte-identical for a solo hero (`{ type: "campFailed", reason: "noRations", need, have }`) and its `members[].eats` values equal `eatsFor(m)`
    - `grep -c "rng\." engine/movement.js` is unchanged; `node --test test/parity/` green with `git status --porcelain test/parity/fixtures` empty
    - Audit outcome pins (the numbers the ledger states): `upkeep({ race:"Human", skills:{} , items:[] })` is 4, Dwarven 1, Troll 15, a Thief with `skills.Heft` 2 (Human), a Dwarven with Heft 1 (the `max(1, …)` floor); fed heal is `d10 + 2×level`, doubled for Wilmsry/Soldier (`rested.doubled` names which); starting rations F6/T5/M4 for at least one chargen seed of each class (seeds 1 Fighter, 2 Thief, 7 Magic User from the chargen fixture); `cooked` adds exactly 1 ration; `STORE_EFFECTS.buyRations` adds `params.amount ?? 1`; `clampCarry` caps `c.rations` at `BAGS[c.bag].rations`
  </behavior>
  <action>
    A. `engine/movement.js`: add `export function eatsFor(sheet) { return (sheet && RACES[sheet.race] && RACES[sheet.race].eats) || 1; }` directly above `nightlyEats` with a JSDoc: Phase 43 (CLAR-03/05) — THE appetite read; value-identical to the three inline `RACES[...]...eats || 1` reads it replaces (the hero's plain read in nightlyEats and the two optional-chained member reads in nightlyEats/makeCamp); read by nightlyEats/newDay/makeCamp and, read-only, by `src/browser/viewModels.js#rationsViewModel` and the shell's Joiner/Company surfaces (Plan 04). Re-express `nightlyEats` as `let eats = eatsFor(c); … eats += eatsFor(m);`. In `newDay`: after `c.rations -= eats;` push `{ type: "rationsEaten", eats, left: c.rations, eaters }` where `eaters` is built right there (`[{ name: c.name, race: c.race, eats: eatsFor(c), hero: true }, ...(state.party ?? []).map((m) => ({ name: m.name, race: m.race, eats: eatsFor(m) }))]`); rewrite the unfed push to `{ type: "wentHungry", cost, need: eats, have: c.rations, mouths: 1 + (state.party?.length ?? 0), ...(skill(c, "Heft") ? { heft: true } : {}) }`. In `makeCamp`, replace the inline member eats read with `eatsFor(m)`. Comments: `Phase 43 (CLAR-01/03/05, additive): rations eaten is a narrated cost — one event per fed night naming every eater; fixtures compare state, zero moves.`

    B. `docs/RATIONS.md` (new): header (`# Rations & upkeep audit (Phase 43 ledger, CLAR-03/05)`, Phase/Date, one paragraph: prototype is canon, rulebook secondary, `nightlyEats`/`eatsFor` the one definition). Sections in order: `## Audit method` (the two sources, the read-only master hash, `pdftotext` page refs); `## Rules (audited)` — a table `| # | Rule | Prototype (line) | Rulebook (page) | Engine site | Event / narration | Status |` with AT LEAST these rows: R1 hero eats `eatsFor` = 1 a night, Troll 2 (proto 1278 / races 288; rulebook: no ration count — Trolls "15WP a day" p.12; engine movement.js#eatsFor; `rationsEaten`; IMPLEMENTED); R2 each live Joiner eats `eatsFor(m)` (proto: none — hero only; engine nightlyEats party loop; `rationsEaten.eaters`; DECLARED DIVERGENCE kept — Phase 11 PARTY-10 / Phase 25.1 DFB-06); R3 camp gate need = nightlyEats (proto 1332 hero-only; engine makeCamp; `campFailed { need, have, members }`; DECLARED DIVERGENCE kept — DFB-06); R4 a day every 100 squares or Make Camp (proto 1256/1329; rulebook p.14; engine movement.js#move crossings(100)/makeCamp; `dayBegan`; IMPLEMENTED); R5 fed night: rations −eats, heal d10+2×level, ×2 Wilmsry heal2x / Soldier, NO hp charge (proto 1280-1287 — narrates "pay N wp of upkeep" but never subtracts it; engine identical; `rested { amount, doubled }`; IMPLEMENTED — narration-only cost in both); R6 unfed night: hp −= upkeep(c) [+ Σ upkeep(m)], upkeep = max(1, round(R.upkeep × (Heft ? ½ : 1)) + eff("upkeep")), R.upkeep Human/Elven/Wilmsry/Fridgian 4, Dwarven 1, Troll 15 (proto 1052/1316-1318; rulebook p.9/p.11/p.12/Heft; engine derived.js#upkeep + newDay else-branch; `wentHungry { cost, need, have, mouths, heft }`; IMPLEMENTED — party upkeep is the R2 divergence's other half); R7 starve at hp <= 0 (proto 1319; engine die("starve"); `died`; IMPLEMENTED); R8 starting rations F6/T5/M4 (proto 641; engine character.js:542; IMPLEMENTED); R9 ration sources — store Rations line (engine-only, declared 04.1-03 RATION-01; proto: food +1 silently), Cooking on Beasts +1 (proto 1980-1983; combat.js `cooked`), meat d6>=4 +1 (proto 1985-1986; `cooked { wp: 0 }`), findFood +1 (proto 1704-1710; engine REMOVED — declared 04.1-03); R10 bag cap on rations (engine-only Phase 12, `clampCarry`/BAGS[].rations; no prototype equivalent; DECLARED); R11 rest-time affliction cure is a d20 roll (proto unconditional; declared audit-batch1 A3; `afflictionCured`/`afflictionLingers`); R12 `eff("upkeep")` term exists in both but NO content row provides it (dormant, value 0). `## Rulebook-only rules (not adopted — prototype canon)`: sleep-hours −1 WP/hour short (p.9/p.14); wandering-monster hour forfeits 1 WP (p.14); the rulebook's WP-valued food table (p.48) is the prototype's FOODS, already ported. `## Missing prototype rules found`: `None.` (or, if the executor finds one: the declared divergence with before/after and the measured fixtures — then FIXTURE-INVENTORY must carry the record too). `## The rest narration names every rule` — the three lines (`rationsEaten`, `wentHungry`, `rested`) with the rule each names, and the `RATION_RULE_LINE`/`RATIONS_COPY.why` race map (Troll: "Trolls eat for two."). `## View model` — `rationsViewModel(state)` shape and the invariant `total === nightlyEats(state)`. `## Fixture impact` — zero (events not compared; eatsFor value-identical). `## Requirements map` — CLAR-03 / CLAR-05 -> proofs (REQUIREMENTS.md itself is flipped by Plan 04).

    C. `test/unit/rations-audit.test.js` (new): every `<behavior>` bullet — build states with the movement.test.js helpers (its `fixedState`/party shapes) or `newRun(seed)` for the chargen rows; use `fakeRng` sequences that drive `newDay(state, true, rng, events)` (heal d10 then 8 wandering d20s ≥ 2 → no encounter); also a ledger pin: parse `docs/RATIONS.md`'s rules table, assert ≥ 12 rows, every `Engine site` cell that names a `.js` path points at a file that exists, and the `## Missing prototype rules found` section body is exactly `None.` (or contains the words `DECLARED DIVERGENCE` — one of the two).

    D. `test/parity/FIXTURE-INVENTORY.md`: append `### Plan 02 — rations: audit + rationsEaten event, zero fixture moves` under the Phase 43 section: the rest-reaching fixture set (`grep -l` for scenarios whose replay emits `dayBegan` — expected: the movement fixture's 100-square tick), the statement that `rationsEaten` is an event (never compared) and `eatsFor` is value-identical, `grep -c "rng\."` unchanged, and "Missing prototype rules: none" (or the declared record).

    E. Fill `docs/CLARITY.md` rows 34-38 (`dayBegan` unchanged; `rationsEaten` NEW EVENT + line; `wentHungry` ADDED need/have/mouths/heft + line; `rested` verified names the doubling; `campFailed` unchanged) — replace each `(Plan 02)` placeholder with the actual line text from Task 2's `<behavior>`.

    Run `node --test test/unit/rations-audit.test.js test/unit/movement.test.js test/unit/water-cost.test.js test/unit/party-model.test.js test/unit/formatEventsCoverage.test.js test/unit/toastsCoverage.test.js test/determinism/ test/parity/` — the two coverage guards will FAIL until Task 2 adds the `rationsEaten` narration/toast entries; that is expected — run everything else green, then commit `feat(43-02): eatsFor — the one appetite read; rationsEaten event; wentHungry need/have/mouths/heft; docs/RATIONS.md audit ledger + pin test` (explicit paths) and proceed immediately to Task 2 (the guards are green again at the end of Task 2's own run).
  </action>
  <acceptance_criteria>
    - `grep -c "export function eatsFor(sheet)" engine/movement.js` is 1; `grep -c "eatsFor(" engine/movement.js` is at least 6 (definition, nightlyEats ×2, eaters ×2, makeCamp); `grep -v '^\s*//' engine/movement.js | grep -v '^\s*\*' | grep -c "RACES\[m.race\]?.eats"` is 0
    - `grep -c 'type: "rationsEaten", eats, left: c.rations, eaters' engine/movement.js` is 1; `grep -c 'type: "wentHungry", cost, need: eats, have: c.rations, mouths' engine/movement.js` is 1
    - `node -e "import('./engine/movement.js').then(m=>{process.exit(m.eatsFor({race:'Troll'})===2&&m.eatsFor({race:'Human'})===1&&m.eatsFor(null)===1&&m.nightlyEats({c:{race:'Human'},party:[{race:'Troll'},{race:'Human'}]})===4?0:1)})"` exits 0
    - `grep -c "rng\." engine/movement.js` equals `git show HEAD:engine/movement.js | grep -c "rng\."`
    - `grep -c "^## Rules (audited)" docs/RATIONS.md` is 1; `grep -cE '^\| R[0-9]+ \|' docs/RATIONS.md` is at least 12; `grep -c "^## Rulebook-only rules (not adopted — prototype canon)" docs/RATIONS.md` is 1; `grep -c "^## Missing prototype rules found" docs/RATIONS.md` is 1; `grep -c "Trolls eat for two." docs/RATIONS.md` is at least 1; `grep -c "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" docs/RATIONS.md` is 1
    - `node --test test/unit/rations-audit.test.js` exits 0 with at least 14 tests
    - `node --test test/unit/movement.test.js test/unit/water-cost.test.js test/unit/party-model.test.js test/determinism/ test/parity/` exits 0; `git status --porcelain test/parity/fixtures` is empty; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`
    - `grep -c "^### Plan 02 — rations: audit + rationsEaten event, zero fixture moves" test/parity/FIXTURE-INVENTORY.md` is 1; `grep -c "rationsEaten" docs/CLARITY.md` is at least 2; `grep -c "Hunger: nobody packed" docs/CLARITY.md` is at least 1; `grep -c "Trolls eat for two." docs/CLARITY.md` is at least 1
  </acceptance_criteria>
  <verify>
    <automated>node --test test/unit/rations-audit.test.js test/unit/movement.test.js test/unit/water-cost.test.js test/unit/party-model.test.js test/determinism/ test/parity/</automated>
  </verify>
  <done>Every ration/upkeep rule is audited and ledgered with its prototype line, rulebook page and engine site; appetite has one definition; rations eaten is an event that names every eater; hunger carries need/have/mouths/heft; nothing moved in parity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rest narration that names every rule (rationsEaten / wentHungry on Oracle + toast + rail family) and the pure rationsViewModel / eatsLineFor / RATIONS_COPY</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, src/browser/viewModels.js, test/voice/safety-scan.test.js, test/unit/rationsViewModel.test.js, test/unit/rations-audit.test.js, test/unit/rail.test.js, test/unit/hp-not-wp.test.js</files>
  <read_first>
    - src/browser/eventNarration.js lines 88-101 (`PHOBIA_TRIGGER_PHRASE` — the exported frozen phrase-map precedent to mirror for `RATION_RULE_LINE`), 186-190 (`dayBegan`, `rested`), 200-205 (`wentHungry` — rewrite target), 216-224 (`campFailed` — UNCHANGED), 46 (`plural` helper)
    - src/browser/toasts.js lines 969-987 (`rested`/`wentHungry`/`campFailed` toasts), 81 (`NARRATIVE_ACTIONS` has `move` and `camp` — the rail shows the Oracle sentence for these actions automatically), 180-200 (`ORACLE_ONLY` — `rationsEaten` must NOT be added there: it is a cost, not bookkeeping)
    - src/browser/rail.js lines 44-55 (`RAIL_HOLD.camp`), 185-190 (`dayBegan`/`rested`/`wentHungry`/`campFailed` families), 268-275 (`RAIL_DIRECT` — `dayBegan` only; do not add `rationsEaten`), 384-440 (`railCardFor` — the head line is the LOWEST priority; `rested` is PRIORITY.feature (3) so it stays the head on a healing camp; give `rationsEaten` PRIORITY.other (4) so a full-hp camp with no `rested` gets the FED title instead)
    - src/browser/viewModels.js lines 9-14 (imports — `engine/movement.js` is already imported for `maxCharges`; extend that line with `nightlyEats, eatsFor`), 240-248 (`ITEM_STATE_COPY` frozen-copy precedent), 357-420 (`characterSheetViewModel` shape — the RATIONS view model is a sibling export, NOT folded into the sheet's `stats`)
    - test/unit/rail.test.js lines 60-75 (the RAIL_FAMILY shape pins — add one for `rationsEaten`), 194-202 (the camp-dispatch card test — extend with a `rationsEaten` event and assert the card still resolves), test/unit/itemRowState.test.js lines 17-30 (a frozen COPY deepEqual pin to mirror for RATIONS_COPY)
    - test/voice/safety-scan.test.js BRANCH_TOGGLES (add `{ heft: true }`, `{ mouths: 3 }`, `{ eaters: [{ name: "Grunk", race: "Troll", eats: 2 }] }`, `{ eaters: [] }`)
    - test/unit/hp-not-wp.test.js (Plan 01) — extend the COPY-object walk with `RATIONS_COPY`
  </read_first>
  <behavior>
    - `narrativeToastText(narrateEvent(e))` for `rationsEaten {eats:1, left:4, eaters:[{name:"Ada",race:"Human",eats:1,hero:true}]}` is exactly `Rations: you eat 1. −1 ration, 4 left.`; for `{eats:2, left:3, eaters:[{name:"Grunk",race:"Troll",eats:2,hero:true}]}` it is `Rations: you eat 2. Trolls eat for two. −2 rations, 3 left.`; for `{eats:3, left:4, eaters:[{name:"Ada",race:"Human",eats:1,hero:true},{name:"Grunk",race:"Troll",eats:2}]}` it is `Rations: you eat 1; Grunk (Troll) eats 2. Trolls eat for two. −3 rations, 4 left.`; a bare `{ type: "rationsEaten" }` renders a non-empty string without throwing
    - `wentHungry {cost:4, need:1, have:0, mouths:1}` -> `Hunger: nobody packed — you eat 1 a night, and you had 0. Cost of living −4 hp.`; `{cost:6, need:3, have:1, mouths:2}` -> `Hunger: nobody packed — the party eats 3 a night, and you had 1. Cost of living −6 hp.`; `{cost:2, need:1, have:0, mouths:1, heft:true}` -> `Hunger: nobody packed — you eat 1 a night, and you had 0. Cost of living −2 hp (Heft: half, as promised).`; a bare `{ type: "wentHungry" }` renders without throwing and contains `Hunger:`
    - `TOAST_FOR.rationsEaten(e).text` is `Rations: −3 (4 left).` for eats 3/left 4, tone `beat`, priority `PRIORITY.other`; `TOAST_FOR.wentHungry(e).text` is `Hunger: no rations (−4 hp).`, tone `hurt`, priority unchanged (`PRIORITY.other`)
    - `RAIL_FAMILY.rationsEaten` deep-equals `{ icon: "☾", title: "FED", tone: "good", hold: RAIL_HOLD.camp }`; a camp dispatch `[dayBegan{day:2}, rationsEaten{…}, rested{amount:4}]` folds to a card titled `CAMP MADE` with at least 3 lines; `[dayBegan{day:2}, rationsEaten{…}]` (no heal) folds to a card titled `FED`
    - `RATION_RULE_LINE` is exported, frozen, and equals `{ Troll: "Trolls eat for two." }`; `RATIONS_COPY` is exported, frozen, every leaf clears the safety wordlist and `hp-not-wp`
    - `rationsViewModel(state)` for a solo Human with 4 rations returns `{ hero: { name, race: "Human", eats: 1, why: "" }, members: [], total: 1, carried: 4, nights: 4, carriedText: "4 carried", line: "You eat 1 a rest · 4 carried — 4 nights, then the arguing starts." }`; for a solo Troll with 3 rations `total` 2, `nights` 1, `hero.why` `" (Troll: eats for two)"`, line `You eat 2 a rest (Troll: eats for two) · 3 carried — one night, then the arguing starts.`; for a Human hero with a Troll member Grunk and 7 rations: `total` 3, `nights` 2, line `You eat 1 a rest. Grunk (Troll) eats 2 (Troll: eats for two). Party: 3 a rest · 7 carried — 2 nights, then the arguing starts.`; with 0 rations the tail is ` — nothing for tonight. Camp is a rumour.`; in EVERY case `total === nightlyEats(state)` and `carried === state.c.rations`
    - `eatsLineFor({ race: "Troll" })` is `eats 2 a rest`; `eatsLineFor({ race: "Human" })` is `eats 1 a rest`; both pure (no mutation, no rng)
  </behavior>
  <action>
    A. `src/browser/eventNarration.js`: export `RATION_RULE_LINE = Object.freeze({ Troll: "Trolls eat for two." })` beside `PHOBIA_TRIGGER_PHRASE` (comment: Phase 43 CLAR-05 — the rule sentence the rest narration appends once per distinct race among the eaters that has one; `RATIONS_COPY.why` in viewModels.js is the Hero-sheet clause form of the same map). Add `rationsEaten`: clauses = eaters mapped to `you eat ${n}` (when `hero`) or `${name} (${race}) eats ${n}`, joined by `; `; rule sentences = distinct `race` values in eater order filtered through `RATION_RULE_LINE`, each appended as a sentence; then the cost clause `−${eats} ${plural word ration/rations}, ${left} left.` — the whole cause sentence inside `<span class="beat">Rations: …</span>`, cost outside. Defaults for a bare payload: `eaters ?? []` -> clause `you eat ${eats ?? 1}`. Rewrite `wentHungry` to the `<behavior>` sentence: `<span class="hurt">Hunger: nobody packed — ${mouths > 1 ? "the party eats" : "you eat"} ${need} a night, and you had ${have}.</span> Cost of living −${cost} hp${heft ? " (Heft: half, as promised)" : ""}.` with `??` defaults (`need ?? 1`, `have ?? 0`, `mouths ?? 1`, `cost ?? 0`). Leave `dayBegan`, `rested`, `campFailed` untouched.

    B. `src/browser/toasts.js`: add `rationsEaten: (e) => ({ text: \`Rations: −${e?.eats ?? 0} (${e?.left ?? 0} left).\`, tone: "beat", priority: PRIORITY.other })` beside `rested`; rewrite `wentHungry` text to `Hunger: no rations (−${cost} hp).`.

    C. `src/browser/rail.js`: add `rationsEaten: { icon: "☾", title: "FED", tone: "good", hold: RAIL_HOLD.camp }` beside `rested` (comment: Phase 43 CLAR-01/05 — the fed-night ration cost; a healing camp's head stays `rested`/CAMP MADE by priority).

    D. `src/browser/viewModels.js`: extend the `engine/movement.js` import to `{ maxCharges, nightlyEats, eatsFor }`. Add `export const RATIONS_COPY = Object.freeze({ you: "You eat {n} a rest{why}", member: "{name} ({race}) eats {n}{why}", party: "Party: {n} a rest", carried: "{n} carried", nights0: " — nothing for tonight. Camp is a rumour.", nights1: " — one night, then the arguing starts.", nightsN: " — {n} nights, then the arguing starts.", eats: "eats {n} a rest", why: Object.freeze({ Troll: " (Troll: eats for two)" }) })`. Add `export function eatsLineFor(sheet)` -> `RATIONS_COPY.eats.replace("{n}", eatsFor(sheet))`. Add `export function rationsViewModel(state)`: `c = state.c`; `hero = { name: c.name, race: c.race, eats: eatsFor(c), why: RATIONS_COPY.why[c.race] ?? "" }`; `members = (state.party ?? []).map(m => ({ name: m.name, race: m.race, eats: eatsFor(m), why: RATIONS_COPY.why[m.race] ?? "" }))`; `total = nightlyEats(state)`; `carried = c.rations ?? 0`; `nights = total > 0 ? Math.floor(carried / total) : 0`; `carriedText = RATIONS_COPY.carried.replace("{n}", carried)`; `tail = nights === 0 ? nights0 : nights === 1 ? nights1 : nightsN.replace("{n}", nights)`; `youClause = RATIONS_COPY.you` with `{n}`/`{why}` filled; `line` for a solo hero (no members) is `${youClause} · ${carriedText}${tail}` (no `Party:` clause — a party of one is not a party); with members it is `${youClause}. ${memberClauses.join(". ")}. ${RATIONS_COPY.party.replace("{n}", total)} · ${carriedText}${tail}`. JSDoc: Phase 43 (CLAR-03/05) — the ONE ration readout; `total` is `nightlyEats(state)` itself, never a re-sum, so the Hero sheet, the camp refusal and the fed-night charge can never disagree; pure, no rng, no DOM. Add a `hasRationsWord` self-check? No — the hp-not-wp test walks RATIONS_COPY.

    E. Tests: `test/unit/rationsViewModel.test.js` (new) — every `<behavior>` view-model/eatsLineFor bullet plus a `RATIONS_COPY` deepEqual pin and a BANNED-wordlist walk of its leaves (pattern: test/unit/shell-company-panel.test.js's voice test). Append the narration bullets to `test/unit/rations-audit.test.js` (a `narration` block: exact Oracle strings via `narrativeToastText(narrateEvent(e))`, the toast texts, `RATION_RULE_LINE` shape). `test/unit/rail.test.js`: add the `RAIL_FAMILY.rationsEaten` shape pin and the two camp-card title cases. `test/unit/hp-not-wp.test.js`: add `RATIONS_COPY` to the walked COPY objects. `test/voice/safety-scan.test.js`: the BRANCH_TOGGLES entries from read_first.

    Run `node --test test/unit/rations-audit.test.js test/unit/rationsViewModel.test.js test/unit/rail.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/hp-not-wp.test.js test/unit/movement.test.js test/unit/characterSheetViewModel.test.js test/voice/` — green. `npm run build:www` exit 0. Commit `feat(43-02): rest narration names every ration rule (rationsEaten/wentHungry on Oracle+toast+rail); rationsViewModel/eatsLineFor/RATIONS_COPY` (explicit paths).
  </action>
  <acceptance_criteria>
    - `node -e "import('./src/browser/eventNarration.js').then(async m=>{const {narrativeToastText}=await import('./src/browser/toasts.js');const t=(e)=>narrativeToastText(m.narrateEvent(e));const ok=t({type:'rationsEaten',eats:3,left:4,eaters:[{name:'Ada',race:'Human',eats:1,hero:true},{name:'Grunk',race:'Troll',eats:2}]})==='Rations: you eat 1; Grunk (Troll) eats 2. Trolls eat for two. −3 rations, 4 left.'&&t({type:'rationsEaten',eats:1,left:4,eaters:[{name:'Ada',race:'Human',eats:1,hero:true}]})==='Rations: you eat 1. −1 ration, 4 left.'&&t({type:'wentHungry',cost:2,need:1,have:0,mouths:1,heft:true})==='Hunger: nobody packed — you eat 1 a night, and you had 0. Cost of living −2 hp (Heft: half, as promised).'&&JSON.stringify(m.RATION_RULE_LINE)==='{\"Troll\":\"Trolls eat for two.\"}';process.exit(ok?0:1)})"` exits 0
    - `node -e "import('./src/browser/rail.js').then(m=>{const f=m.RAIL_FAMILY.rationsEaten;process.exit(f&&f.icon==='☾'&&f.title==='FED'&&f.tone==='good'&&f.hold===m.RAIL_HOLD.camp?0:1)})"` exits 0
    - `node -e "import('./src/browser/viewModels.js').then(async m=>{const {nightlyEats}=await import('./engine/movement.js');const s={c:{name:'Ada',race:'Human',rations:7},party:[{name:'Grunk',race:'Troll'}]};const v=m.rationsViewModel(s);process.exit(v.total===3&&v.total===nightlyEats(s)&&v.nights===2&&v.line==='You eat 1 a rest. Grunk (Troll) eats 2 (Troll: eats for two). Party: 3 a rest · 7 carried — 2 nights, then the arguing starts.'&&m.eatsLineFor({race:'Troll'})==='eats 2 a rest'?0:1)})"` exits 0
    - `node -e "import('./src/browser/toasts.js').then(m=>{process.exit(typeof m.TOAST_FOR.rationsEaten==='function'&&!m.ORACLE_ONLY.has('rationsEaten')&&m.TOAST_FOR.rationsEaten({type:'rationsEaten',eats:3,left:4}).text==='Rations: −3 (4 left).'?0:1)})"` exits 0 (a cost is toasted, never Oracle-only); `grep -c "export const RATIONS_COPY" src/browser/viewModels.js` is 1; `grep -c "export function rationsViewModel" src/browser/viewModels.js` is 1; `grep -c "export function eatsLineFor" src/browser/viewModels.js` is 1
    - `node --test test/unit/rationsViewModel.test.js` exits 0 with at least 8 tests; `node --test test/unit/rations-audit.test.js` exits 0 with at least 22 tests
    - `node --test test/unit/rail.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/hp-not-wp.test.js test/unit/movement.test.js test/unit/characterSheetViewModel.test.js test/voice/` exits 0
    - `npm run build:www` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build:www && node --test test/unit/rations-audit.test.js test/unit/rationsViewModel.test.js test/unit/rail.test.js test/unit/toastTable.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/hp-not-wp.test.js test/unit/movement.test.js test/unit/characterSheetViewModel.test.js test/voice/</automated>
  </verify>
  <done>The rest names every eater and every rule that applied, hunger names need/have and the Heft halving, the rail has a FED family, and the pure RATIONS view model reads the same appetite the engine charges.</done>
</task>

<task type="auto">
  <name>Task 3: Plan gate + SUMMARY</name>
  <files>.planning/phases/43-clarity-pass/43-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/43-clarity-pass/43-01-SUMMARY.md (the Plan 01 SUMMARY's section set — mirror it, including `## Human verification (deferred to end of run)` and `## Corrections to CONTEXT`)
    - .planning/phases/42-flee-retune-consolidated-balance-close/42-02-SUMMARY.md (a mid-phase SUMMARY's gate-numbers block)
  </read_first>
  <action>
    PLAN GATE (verification agents are off): `npm test` — record `# tests`, `# pass`, `# fail` (must be 0); `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exit 0; `git status --porcelain test/parity/fixtures` empty; `git diff --stat HEAD~3 -- package.json package-lock.json` empty (no installs).
    Write `43-02-SUMMARY.md`: standard sections + the gate numbers + `## Audit verdict` (rules audited, missing prototype rules found: none / declared list, rulebook-only rules not adopted) + `## Human verification (deferred to end of run)` listing: (1) make camp with rations — the rail card reads `Rations: you eat N…` then the heal line; with a Troll hero or member the line ends `Trolls eat for two.` before the ration count; (2) walk 100 squares with 0 rations — `Hunger: nobody packed — you eat 1 a night, and you had 0. Cost of living −N hp.`; a Heft Thief sees the `(Heft: half, as promised)` clause; (3) a full-hp camp (no heal) shows a `FED` card, a healing camp `CAMP MADE`; (4) tap MAKE CAMP without enough rations — the refusal still states need/have and the members (unchanged); (5) voice read of the new lines. Plus `## Corrections to CONTEXT` (the audit's findings vs CONTEXT's "today 1 a night, Troll 2, Heft halves wp upkeep only" — confirm or amend; note the prototype's fed-night line narrates an upkeep charge it never makes, and that the engine does the same).
    Commit `docs(43-02): plan summary — ration audit verdict, gate numbers` (explicit path).
  </action>
  <acceptance_criteria>
    - `npm test` output ends with `# fail 0`; `# pass` is at least the Plan 01 SUMMARY's recorded pass count + 25
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run build:www` exits 0; `git status --porcelain test/parity/fixtures` is empty
    - `.planning/phases/43-clarity-pass/43-02-SUMMARY.md` exists and contains `## Audit verdict`, `## Human verification (deferred to end of run)` and `## Corrections to CONTEXT`
  </acceptance_criteria>
  <verify>
    <automated>npm test 2>&1 | tail -8 && git hash-object test/parity/prototype-master.js.txt && git status --porcelain test/parity/fixtures</automated>
  </verify>
  <done>The suite is green with the master untouched and no fixture changed; the SUMMARY records the audit verdict and the deferred device checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| party member sheets -> narration HTML | `eaters[].name` (a Joiner's generated name) is interpolated into an Oracle line |
| prototype master -> audit | the audit READS the frozen master; nothing writes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-04 | Tampering | `rationsEaten` name interpolation in EVENT_NARRATION | low | mitigate | names come from `content/names.js` (closed vocabulary) exactly as `campFailed.members[].name` already does; the builder uses the same interpolation shape; the voice scan injects every closed-vocab name |
| T-43-05 | Tampering | prototype master edited "to fix" a rule during the audit | high | mitigate | the master hash is asserted in Task 1's and Task 3's acceptance; the audit doc records the hash |
| T-43-06 | Repudiation | a silently adopted rulebook-only rule | medium | mitigate | `docs/RATIONS.md` `## Missing prototype rules found` must read `None.` or carry `DECLARED DIVERGENCE`; the ledger test asserts one of the two |
| T-43-SC | Tampering | npm/pip/cargo installs | low | accept | no installs; `package.json`/lock diff asserted empty in Task 3 |
</threat_model>

<verification>
- `npm test` `# fail 0`; master hash unchanged; `npm run build:www` exit 0; fixtures porcelain empty.
- `rationsViewModel(state).total === nightlyEats(state)` for solo / Troll / party states (pinned).
- `rationsEaten` and `wentHungry` lines exact; `Trolls eat for two.` present when a Troll eats; coverage guards + voice + hp-not-wp green.
- `docs/RATIONS.md` ≥ 12 audited rules, missing-rules verdict recorded.
</verification>

<success_criteria>
- CLAR-03: the view model the Hero sheet will read equals what Make Camp charges, by construction.
- CLAR-05: rules audited and ledgered; every rule named in the rest narration; `eatsLineFor` ready for the offer card / Company panel.
- CLAR-01 (day-cycle rows): rations eaten and hunger are cause-first cost lines from the payload.
- Zero rng / serialized-field / fixture changes; no shell edit.
</success_criteria>

<output>
Create `.planning/phases/43-clarity-pass/43-02-SUMMARY.md` when done (standard sections + `## Audit verdict` + `## Human verification (deferred to end of run)` + `## Corrections to CONTEXT` + the gate numbers).
</output>
