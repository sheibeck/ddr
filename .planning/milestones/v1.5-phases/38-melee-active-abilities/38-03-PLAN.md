---
phase: 38-melee-active-abilities
plan: 03
type: execute
wave: 3
depends_on: ["38-01", "38-02"]
files_modified:
  - engine/abilities.js
  - engine/combat.js
  - engine/actions.js
  - engine/engine.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - test/voice/safety-scan.test.js
  - test/unit/abilities.test.js
  - test/unit/actions.test.js
  - docs/USABLE-FEATURES-AUDIT.md
  - docs/ABILITIES.md
autonomous: true
requirements: [ABIL-01, ABIL-04]
tags: [engine, useAbility, dispatcher, cooldowns, foe-flags, dot, refusals, narration, deferred-uat]
notes:
  rng_draw_impact: "Draws are added ONLY inside the new useAbility action (secondWind d8; sweep's weaponDamage dice + damageFoe's per-foe armour d20 where a foe has sp.ar; cutpurse d10 via gainWilmst incl. the Pickpocket bonus draws; the strike-modifiers draw exactly what playerStrike draws) and inside foeTurn/applyFoeDamageToPlayer/pickFoeTarget ONLY when an ability flag/timer is present (Poisoned Edge's per-tick d4; Riposte's counter weaponDamage + armour d20; Taunt REMOVES pickFoeTarget's target die while active). No fixture dispatches useAbility or carries a flag/timer, so every existing draw sequence is byte-identical (parity suite green with Plan 01's fixture bytes)."
  serialized_field_impact: "Foe-side per-fight flags on state.combat.foes[i]: stunned (boolean), blindFor (integer, beside the existing blind), dot ({ left, dmg, by }), hamstrung (boolean), marked (boolean); combat-side C.braced (boolean). All live on state.combat, which is reset to null on load (rehydrate) and only ever present mid-fight; never on a fixture. Hero cooldowns are Phase 36 c.timers records keyed ability:<id> (already carved out). No new field on c or on the top-level state."
  cooldown_model: "startCooldown(c, 'ability:<id>', { rounds: cd }) for immediate abilities; startEffect(c, 'ability:<id>', { rounds: N, cd }) for duration abilities (sidestep 2/4, battleRoar 2/5, riposte 1/4, taunt 1/4, smoke 2/999); once-a-fight = rounds/cd ONCE_A_FIGHT (999), cleared by endCombat's existing clearRoundTimers. isReady is false during both phases; abilityRoundsLeft reports effect left + cd during the effect phase."

must_haves:
  truths:
    - "engine/abilities.js exports useAbility(state, key, rng, events = []) and abilityRoundsLeft(c, key); engine/actions.js ACTION_TYPES includes 'useAbility' and validate rejects a non-string/empty key with reason 'useAbility.key must be a non-empty string'; engine/engine.js dispatches case 'useAbility' to useAbility(next, action.key, rng, events)"
    - "Refusal ordering, each a single abilityRefused event and nothing else: notFought (refuseIfPending) -> unknown (id not in catalog OR not in c.abilities) -> notInCombat -> cooldown { left, name } -> noTarget (target 'foe' with no live foe after normalizeTarget) -> notLowEnough (lastStand with c.wp > maxWP * DEATH_PANIC_THRESHOLD, payload have/need); a refused dispatch spends no action (no foe turn, rngState unchanged, no timer started)"
    - "A successful use pushes abilityUsed { key, name } first, starts the id's timer (isReady false immediately after), resolves the effect, and ends the round exactly once (strike kinds via playerStrike's tail; every other kind via one afterPlayerAction call) — a foe-count/round proof shows exactly one foe turn per use"
    - "Each of the 20 abilities has a passing behaviour test (see <ability_effects_spec>): the five strike-modifiers set the Plan 02 descriptor and call playerStrike inline; pommelStrike sets f.stunned consumed by foeTurn as foeStunned (skip); dirtyTrick sets f.blind + f.blindFor 2 counted down to foeSightReturned; poisonedEdge sets f.dot ticked in foeTurn as dotTick (d4, 3 ticks, killFoe on lethal, deleted at 0); hamstring halves that foe's damage at both foeTurn damage sites; mark is read by playerStrike (+2); secondWind heals d8 + level capped at maxWP; sweep hits every live foe for ceil(weaponDamage/2); brace sets C.braced consumed once as braceHeld in applyFoeDamageToPlayer; riposte counters every hero-branch foe miss during its effect round (riposted { target, dmg }); taunt makes pickFoeTarget return null (no draw) and doubles the soak armour rating in applyFoeDamageToPlayer while active; sidestep/battleRoar/smoke start their effect records; smoke's flee branch skips the roll and the pursuit strike (fled { reason: 'smoke' }); cutpurse grants d10 x level gold via gainWilmst(why 'cutpurse') and pushes cutpursed; lastStand strikes three times"
    - "Every new event type (abilityUsed, abilityRefused, pommelStruck, foeStunned, battleRoarRaised, sidestepped, secondWindHealed, swept, sweptFoe, braced, braceHeld, riposteReady, riposted, taunted, lastStandCalled, dirtyTrickLanded, foeSightReturned, smokeThrown, cutpursed, poisonedEdgeApplied, dotTick, hamstrung, marked) has an EVENT_NARRATION entry and a TOAST_FOR entry (abilityRefused as a block()); fled's tables handle reason 'smoke'; both coverage guards and the voice scan (with the new BRANCH_TOGGLES reasons) are green"
    - "abilityRefused's cooldown line reads exactly '<Name>: <N> round(s). Your arm has opinions.' (singular 'round' at 1) and every other reason names the ability"
    - "npm test prints '# fail 0'; fixtures unchanged from Plan 01; master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - path: "engine/abilities.js"
      provides: "useAbility, abilityRoundsLeft, applyPommel, applyDirtyTrick, applyPoison, applyHamstring, applyMark, ONCE-A-FIGHT timer mapping"
    - path: "engine/combat.js"
      provides: "foeTurn dot/stunned/blindFor/hamstrung/riposte hooks; pickFoeTarget taunt; applyFoeDamageToPlayer brace + taunt soak; flee smoke branch"
    - path: "test/unit/abilities.test.js"
      provides: "refusal ordering, one-foe-turn-per-use, per-ability behaviour + measured draw counts, timer lifecycle across rounds and endCombat"
  key_links:
    - from: "engine/engine.js"
      to: "engine/abilities.js#useAbility"
      via: "case \"useAbility\": useAbility(next, action.key, rng, events)"
      pattern: "useAbility\\(next, action\\.key"
    - from: "engine/abilities.js#useAbility (strike kinds)"
      to: "engine/combat.js#playerStrike"
      via: "state.combat.abilityStrike = { key, ... }; playerStrike(state, rng, events)"
      pattern: "abilityStrike = \\{"
    - from: "engine/combat.js#foeTurn"
      to: "f.dot / f.stunned / f.hamstrung / f.blindFor"
      via: "per-foe reads in the visit loop"
      pattern: "f\\.dot"
    - from: "engine/combat.js#pickFoeTarget"
      to: "abilityEffectActive(state.c, \"taunt\")"
      via: "return null before the target die"
      pattern: "abilityEffectActive\\(state\\.c, \"taunt\"\\)"
  prohibitions:
    - "No member/Joiner ability use (Plan 04); no shell/submenu/Hero-tab code (Plan 05); no edit to content/, character.js, saveState.js, fixtures, the master"
    - "useAbility never calls afterPlayerAction when it delegated to playerStrike (double foe turn)"
    - "Do not reuse C.foeToHitPenalty / C.weakened for any ability (they belong to the foe Weaken ability); do not touch f.cd"
    - "No checkpoint:human-verify, no device step, no APK build; explicit-path commits only"
---

<objective>
Make abilities usable: the `useAbility { key }` engine action in `engine/abilities.js` — parallel to `magic.js#castSpell` — with the named refusal ladder (D: "Rows stay tappable on cooldown: the dispatch yields a named abilityRefused", reasons `unknown`/`cooldown`/`noTarget`/`notLowEnough` plus `notInCombat`), cooldowns on Phase 36's `c.timers` (D: "Cooldowns"), the round economy (D: "Action economy: using an ability is the round's action"), and all 20 effect resolutions on existing hooks (D: "Events" — `abilityUsed`, strike events with `via`/`critBy`, `f.blind`/`f.stunned` honoured in `foeTurn`, need shifts via timers, DOT as a generic `f.dot` ticked in `foeTurn` for Phase 40 to share), registered in `actions.js`/`engine.js`, every event narrated in all three tables with the canon refusal register ("Pommel Strike: three rounds. Your arm has opinions.").

Purpose: SC-1/SC-2 need a real action behind the submenu; Plan 04 (Joiners) and Plan 05 (submenu) consume this module.
Output: `engine/abilities.js`, the foe-side hooks in `combat.js`, action registration, narration entries, `test/unit/abilities.test.js`, ledger/audit-doc rows.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-melee-active-abilities/38-CONTEXT.md
@.planning/phases/38-melee-active-abilities/38-RESEARCH.md
@.planning/phases/38-melee-active-abilities/38-02-SUMMARY.md
@engine/magic.js
@engine/combat.js
@engine/effects.js
@content/abilities.js
</context>

<artifacts_this_phase_produces>
See 38-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list. This plan creates the **[03]** items: `engine/abilities.js` (`useAbility`, `abilityRoundsLeft`, `applyPommel`, `applyDirtyTrick`, `applyPoison`, `applyHamstring`, `applyMark`, internal `startAbilityTimer(c, meta)`), action type `useAbility { key }` (+ `validate` reason string `useAbility.key must be a non-empty string`), the 23 event types listed in must_haves plus `fled { reason: "smoke" }`, foe fields `stunned`/`blindFor`/`dot`/`hamstrung`/`marked`, combat field `braced`, `foeTurn`/`pickFoeTarget`/`applyFoeDamageToPlayer`/`flee` hooks, `test/unit/abilities.test.js`, `docs/USABLE-FEATURES-AUDIT.md` §1 `abilityRefused` row, `docs/ABILITIES.md` "Play rules" section.
</artifacts_this_phase_produces>

<ability_effects_spec>
`useAbility(state, key, rng, events = [])` — after the refusal ladder and `abilityUsed`, `startAbilityTimer(c, meta)`: `meta.cd === "fight"` -> `startCooldown(c, id, { rounds: ONCE_A_FIGHT })` for immediate kinds, or `startEffect(c, id, { rounds: N, cd: ONCE_A_FIGHT })` for smoke; numeric cd -> `startCooldown(c, id, { rounds: cd })` for immediate kinds or `startEffect(c, id, { rounds: N, cd })` for duration kinds (sidestep N=2, battleRoar N=2, riposte N=1, taunt N=1). `id = "ability:" + key`. Then resolve by `key`:

| key | kind | resolution (all identifiers exact) |
|---|---|---|
| kata | strike | `C.abilityStrike = { key, autoHit: true, bonusDmg: c.level }`; `playerStrike(state, rng, events)`; return |
| feint | strike | same descriptor with key "feint" |
| deathTouch | strike | `{ key, forceCrit: true, finishUnder: 15 }` |
| silentStep | strike | `{ key, autoHit: true, forceCrit: true }` |
| overheadBlow | strike | `{ key, dmgMul: 2, needShift: -2 }` |
| lastStand | strike | gate first (`notLowEnough`); push `lastStandCalled { attacks: 3 }`; `{ key, attacks: 3 }` |
| pommelStrike | foe | `applyPommel(t)` (sets `t.stunned = true`); push `pommelStruck { target: t.name }` |
| dirtyTrick | foe | `applyDirtyTrick(t)` (`t.blind = true; t.blindFor = 2`); push `dirtyTrickLanded { target, rounds: 2 }` |
| poisonedEdge | foe | `applyPoison(t, { left: 3, dmg: { n: 1, sides: 4, bonus: 0 }, by: "poisonedEdge" })` (sets `t.dot`); push `poisonedEdgeApplied { target, rounds: 3 }` |
| hamstring | foe | `applyHamstring(t)` (`t.hamstrung = true`); push `hamstrung { target }` |
| mark | foe | `applyMark(t)` (`t.marked = true`); push `marked { target }` |
| cutpurse | foe | `const amount = rng.d(10) * c.level`; push `cutpursed { target, amount }`; `gainWilmst(state, amount, "cutpurse", rng, events)` |
| secondWind | self | `const heal = rng.d(8) + c.level; const amount = Math.min(heal, c.maxWP - c.wp); c.wp += amount`; push `secondWindHealed { amount, rolled: heal }` |
| sweep | foes | `const dmg = Math.ceil(weaponDamage(c, rng) / 2)`; push `swept { dmg, count: liveFoes(state).length }`; for each `f` of `liveFoes(state).slice()`: `const hit = damageFoe(state, f, dmg, { kind: "melee", casterClass: c.cls, casterSub: c.sub, crit: false }, rng, events); if (!hit.soaked) push sweptFoe { target: f.name, dmg: hit.applied }; if (f.wp <= 0) killFoe(state, f, rng, events)` |
| brace | self | `C.braced = true`; push `braced {}` |
| riposte | self | (timer is the effect) push `riposteReady { rounds: 1 }` |
| taunt | self | push `taunted { rounds: 1 }` |
| sidestep | self | push `sidestepped { rounds: 2 }` |
| battleRoar | self | push `battleRoarRaised { rounds: 2 }` |
| smoke | self | push `smokeThrown { rounds: 2 }` |

Non-strike kinds end with `if (state.combat) afterPlayerAction(state, rng, events);` (castSpell's exact tail). Strike kinds return right after `playerStrike` (which already ran `afterPlayerAction`).

Hooks in `engine/combat.js`:
- `foeTurn` per-foe visit, in this order: (1) directly after the existing acid block, the DOT tick: `if (f.dot && f.dot.left > 0 && f.alive) { const d = rollDice(rng, f.dot.dmg); const tick = damageFoe(state, f, d, { kind: "spell", school: f.dot.by, casterSub: c.sub }, rng, events); f.dot.left--; events.push({ type: "dotTick", target: f.name, dmg: tick.applied, by: f.dot.by, left: f.dot.left }); if (f.dot.left <= 0) delete f.dot; if (f.wp <= 0 && f.alive) { killFoe(state, f, rng, events); continue; } }` (poison bypasses armour like acid — kind "spell"); (2) after the asleep check: `if (f.stunned) { f.stunned = false; events.push({ type: "foeStunned", name: f.name }); continue; }`; (3) in BOTH damage computations, directly after the `C.weakened` halving: `if (f.hamstrung) dmg = Math.ceil(dmg / 2);` (member branch: `mDmg`); (4) hero-branch miss (`if (roll > need)` in the hero branch only): after the `foeMissed` push and before `continue`, `if (abilityEffectActive(c, "riposte")) { const rd = weaponDamage(c, rng); const hit = damageFoe(state, f, rd, { kind: "melee", casterClass: c.cls, casterSub: c.sub, crit: false }, rng, events); if (!hit.soaked) events.push({ type: "riposted", target: f.name, dmg: hit.applied }); if (f.wp <= 0) { killFoe(state, f, rng, events); break; } }`; (5) at the END of each foe's visit (after its swings loop, inside the `for (const f of C.foes)` body — use a `finally`-free pattern: place after the swings loop), `if (f.blindFor) { f.blindFor--; if (f.blindFor <= 0) { delete f.blindFor; f.blind = false; events.push({ type: "foeSightReturned", name: f.name }); } }` — a spell-blinded foe (no blindFor) stays blind.
- `pickFoeTarget`: after the `!liveMembers.length` return and BEFORE `rng.d(...)`: `if (abilityEffectActive(state.c, "taunt")) return null;` (JSDoc: Taunt removes the target die while active — never on a fixture).
- `applyFoeDamageToPlayer`: directly after the `c.halfNext` block: `if (state.combat && state.combat.braced && dmg > 0) { const before = dmg; dmg = Math.ceil(dmg / 2); state.combat.braced = false; soaked.brace = before - dmg; events.push({ type: "braceHeld", name: foe.name, soaked: before - dmg }); }`; in the armour block, `const soakAr = abilityEffectActive(c, "taunt") ? Math.min(20, av.ar * 2) : av.ar;` and compare `soak <= soakAr` (the `av.ar > 0` gate unchanged).
- `flee`: after the Cloaker/tracked branches and BEFORE `const bonus`: `if (abilityEffectActive(c, "smoke")) { forfeitLoot(state, "fled", events); events.push({ type: "fled", reason: "smoke" }); endCombat(state, events); return events; }` (no pursuit strike, no roll).
`abilityRoundsLeft(c, key)`: `const rec = c.timers && c.timers["ability:" + key]; if (!rec) return 0; return rec.phase === "effect" && rec.cd ? rec.left + rec.cd : rec.left;`.
</ability_effects_spec>

<narration_spec>
EVENT_NARRATION (Oracle/fight-log HTML) and TOAST_FOR (short, tone, priority) entries — family-friendly deadpan, the joke on the adventurer. Tones: `hit` for your good outcomes, `hurt` for damage to you, `miss`/`dodge`/`beat` as fitting; priority `PRIORITY.you` for your own outcomes, `PRIORITY.them` for foe-side consequences, `PRIORITY.feature` for ability activations. abilityRefused is `block(text)` with `text` by reason: `cooldown` -> `${name}: ${left} round${left === 1 ? "" : "s"}. Your arm has opinions.`; `unknown` -> `${name ?? key}? You do not know that one.`; `notInCombat` -> `${name}: nothing to use it on out here.`; `noTarget` -> `${name}: nothing left standing to use it on.`; `notLowEnough` -> `Last Stand: you are not desperate enough yet (${have} of ${max} hp).`; `notFought` -> the existing notFought copy used by strikeRefused. Suggested lines (the executor may tune wording, never tone): abilityUsed "You call ${name}." / pommelStruck "The pommel finds ${target}'s temple. It will need a moment." / foeStunned "${name} spends its turn remembering where it is." / battleRoarRaised "Loud enough. For two rounds they all need two better." / sidestepped "Not where the blade is. Two rounds of that." / secondWindHealed "You remember why you came. +${amount} hp." / swept "One wide arc — ${dmg} to everything still standing." / sweptFoe "${target} takes ${dmg}." / braced "Braced. The next one lands on your terms." / braceHeld "Braced — ${name}'s blow lands half as hard (−${soaked})." / riposteReady "Every miss is an invitation." / riposted "${target} misses, and pays ${dmg} for it." / taunted "Every foe looks at you. Armour doubles. Good luck." / lastStandCalled "Under a quarter. Three swings. Make them count." / dirtyTrickLanded "Sand, thumb, elbow. ${target} is blinded for two rounds." / foeSightReturned "${name} blinks the sand out." / smokeThrown "Gone. For two rounds they need a natural 1 to find you." / fled reason smoke "You leave through the smoke. Nobody follows." / cutpursed "You lift ${amount} wilmst off ${target} mid-fight. It has other problems." / poisonedEdgeApplied "The blade weeps into ${target}. Three rounds of that." / dotTick "${target} takes ${dmg} from the poison." (generic on `by`) / hamstrung "Tendon cut. ${target} hits half as hard from here on." / marked "Studied. Every blow on ${target} lands +2." Every `{token}` must have a `?? fallback` and every line passes the safety scan; add `{ reason: "cooldown" }`, `{ reason: "unknown" }`, `{ reason: "notInCombat" }`, `{ reason: "noTarget" }`, `{ reason: "notLowEnough" }`, `{ reason: "smoke" }` to BRANCH_TOGGLES.
</narration_spec>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: engine/abilities.js — useAbility refusal ladder, timers, the 20 resolutions, action registration — tests first</name>
  <files>engine/abilities.js, engine/actions.js, engine/engine.js, test/unit/abilities.test.js, test/unit/actions.test.js</files>
  <read_first>
    - engine/magic.js lines 1-30 (imports incl. the combat.js cycle precedent) and 48-115 (castSpell's refusal ladder, normalizeTarget, the afterPlayerAction tail at :113/:438)
    - engine/combat.js lines 79-112 (liveFoes/normalizeTarget), 439-456 (refuseIfPending), 456-650 (playerStrike as landed by Plan 02 — confirm the descriptor read names), 653-735 (killFoe), 1150-1205 (afterPlayerAction), 1097-1141 (endCombat's clearRoundTimers)
    - engine/effects.js (startEffect/startCooldown/isReady/remaining/tickRounds — the exact API), engine/derived.js (abilityEffectActive, DEATH_PANIC_THRESHOLD, weaponDamage), engine/foeDamage.js lines 40-100 (damageFoe's source.kind contract), engine/items.js lines 80-99 (gainWilmst signature: state, n, why, rng, events)
    - engine/actions.js lines 10-60 (ACTION_TYPES) and 85-140 (validate cases — mirror castSpell's), engine/engine.js lines 60-125 (dispatch cases)
    - content/abilities.js (ABILITY_BY_ID, ABILITY_POOL, ONCE_A_FIGHT)
    - test/unit/actions.test.js (how new action types/validation reasons are pinned — find the castSpell.idx case), test/unit/party-combat.test.js lines 32-120 (fakeRng/countingRng/fixed* helpers — copy shapes), test/unit/ability-strike.test.js (Plan 02's helpers and foe literal)
    - .planning/phases/38-melee-active-abilities/38-RESEARCH.md §"Research Question 5" (ladder) and §"Common Pitfalls" (double foe turn)
  </read_first>
  <behavior>
    - Registration: `ACTION_TYPES.has("useAbility")`; `validate({ type: "useAbility" })` -> `{ ok: false, reason: "useAbility.key must be a non-empty string" }`; `validate({ type: "useAbility", key: "kata" })` ok; `applyAction(state, { type: "useAbility", key })` reaches useAbility (event abilityUsed/abilityRefused present)
    - Ladder: pending combat -> `[{ type: "abilityRefused", key, reason: "notFought" }]`; a key not in the catalog -> reason "unknown"; a catalog id not in c.abilities -> "unknown"; no combat -> "notInCombat"; on cooldown -> "cooldown" with `left` === abilityRoundsLeft and `name`; lastStand at full hp -> "notLowEnough" with `have`/`max`; each refusal is the ONLY event, rngState unchanged, no timer created, combat.round unchanged
    - One foe turn per use: for pommelStrike (non-strike) and kata (strike) against a 2-foe fight, count `foeMissed`+`struckByFoe`+`foeStunned`+`memberStruck` events and the round delta — exactly one foeTurn's worth, never two
    - Timers: after kata, `isReady(c, "ability:kata") === false`, `abilityRoundsLeft(c, "kata") === 3`; after two foeTurns (tickRounds) it reads 1, after three it is ready; after sidestep `abilityRoundsLeft === 6` (2 + 4) and `abilityEffectActive(c, "sidestep")` is true for two ticks then false while still not ready; secondWind -> `remaining === 999`, and `endCombat` clears it (ready again in a fresh startCombat)
    - Per-ability (each with a measured draw count via countingRng, asserted equal to a hand-run of the same primitives): kata/feint (via: key, +level, auto), deathTouch (finish under 15 / doubled otherwise, subject to noCrit), silentStep (auto crit), overheadBlow (x2, need −2), lastStand (3 strike events; refused above a quarter), pommelStrike (t.stunned true, then the next foeTurn emits foeStunned and that foe makes no swing), dirtyTrick (t.blind true/blindFor 2; after two foeTurn visits foeSightReturned and blind false; a spell-blinded foe without blindFor never regains sight), poisonedEdge (t.dot shape; three dotTick events over three foeTurns each subtracting a d4; a 3-hp foe dies on the first tick via killFoe; f.dot deleted after the third), hamstring (foe's landed damage on the hero is ceil(half) vs a control with the same fakeRng), mark (playerStrike +2 on that foe only), cutpurse (c.gold += 10 × level with fakeRng([10]); cutpursed + goldGained why "cutpurse"), secondWind (wp +d8+level capped; amount reports the capped value), sweep (every live foe takes ceil(weaponDamage/2); a foe with sp.ar draws its armour d20; a lethal sweep kills via killFoe and clears the encounter when nothing remains), brace (C.braced true; the next landed foe blow is halved once — braceHeld with soaked; the following blow is full), riposte (during the effect round a foe miss on the hero triggers riposted with weaponDamage dice; a miss on a member does not; the round after, no counter), taunt (pickFoeTarget returns null without drawing while active; the armour soak compares against min(20, ar × 2)), smoke (foeToHitVs(state) === 1 while active; flee during it emits fled reason smoke with no fleeRolled and no pursuit strike; combat ends), battleRoar/sidestep (foeToHitVs deltas per Plan 02)
  </behavior>
  <action>
    Write `test/unit/abilities.test.js` FIRST (RED on the missing module). Build fight states with `newRun(seed, [], { force })` + a hand-built `state.combat` (mirror party-combat's `fixedCombat`, with `pending: false`, `opened: false`, `opened2: false`, `round: 1`, `target: 0`, `foes: [...]`) so `refuseIfPending` passes; plant `c.abilities` explicitly (e.g. `["kata", "pommelStrike"]`) — tests never depend on the derived-stream pick.

    `engine/abilities.js` (new): header JSDoc (Phase 38, ABIL-01/04; the castSpell parallel; the round-economy rule; the cooldown model from this plan's frontmatter; the transient descriptor; the shared foe-flag appliers Plan 04 reuses for Joiners). Imports: `ABILITY_BY_ID`, `ONCE_A_FIGHT` from ../content/index.js; `startEffect`, `startCooldown`, `isReady` from ./effects.js; `refuseIfPending`, `normalizeTarget`, `playerStrike`, `afterPlayerAction`, `liveFoes`, `killFoe` from ./combat.js; `damageFoe` from ./foeDamage.js; `weaponDamage`, `DEATH_PANIC_THRESHOLD` from ./derived.js; `gainWilmst` from ./items.js. Export `useAbility`, `abilityRoundsLeft`, `applyPommel`, `applyDirtyTrick`, `applyPoison`, `applyHamstring`, `applyMark` exactly per ability_effects_spec; keep `DURATION_ROUNDS = { sidestep: 2, battleRoar: 2, riposte: 1, taunt: 1, smoke: 2 }` as a module constant. Every event push uses a literal type string. The `notLowEnough` payload: `{ key, reason, name, have: c.wp, max: c.maxWP }`; the `cooldown` payload: `{ key, reason, name, left }`.

    `engine/actions.js`: add `"useAbility"` to ACTION_TYPES (comment: Phase 38 — the ABILITIES submenu's action; carries a catalog id `key`) and a validate case rejecting a non-string/empty `key` with the reason string in must_haves. `engine/engine.js`: import `useAbility` and add `case "useAbility": useAbility(next, action.key, rng, events); break;` beside castSpell. `test/unit/actions.test.js`: pin the new type + reason (mirror the castSpell.idx pin).

    Run the new test (GREEN) — the foe-side hooks are Task 2, so run only the ladder/registration/timer/strike-kind/self-timer/secondWind/sweep/cutpurse sections now (use `test.todo`-free ordering: put Task 2's foeTurn-dependent tests under a describe whose assertions Task 2 turns green; RED is expected for them at this commit and must be listed in the SUMMARY).
  </action>
  <verify>
    <automated>node --test test/unit/actions.test.js && node --test test/unit/abilities.test.js 2>&1 | tail -20 && node --test test/parity/</automated>
  </verify>
  <acceptance_criteria>
    - `test -f engine/abilities.js`; `grep -c "^export function useAbility\|^export function abilityRoundsLeft\|^export function applyPommel\|^export function applyDirtyTrick\|^export function applyPoison\|^export function applyHamstring\|^export function applyMark" engine/abilities.js` prints 7
    - `grep -c '"useAbility"' engine/actions.js` >= 2; `grep -c "useAbility(next, action.key, rng, events)" engine/engine.js` prints 1; `grep -c "useAbility.key must be a non-empty string" engine/actions.js test/unit/actions.test.js` prints 1 for each
    - `grep -c 'type: "abilityRefused"' engine/abilities.js` >= 5 (one literal per reason site is acceptable; a single shared push with a reason variable also passes the coverage guard only if the literal appears at least once — keep at least one literal)
    - `grep -c "afterPlayerAction(state, rng, events)" engine/abilities.js` prints 1 (the single non-strike tail)
    - `grep -c "abilityStrike = {" engine/abilities.js` prints 6 (kata, feint, deathTouch, silentStep, overheadBlow, lastStand)
    - `node --test test/parity/` passes; `node --test test/unit/actions.test.js` passes
  </acceptance_criteria>
  <done>useAbility exists with the full ladder, timers, the 20 resolutions and registration; the tests that need Task 2's hooks are the only red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: combat.js foe-side hooks — dot tick, stunned, blindFor countdown, hamstrung halving, riposte counter, taunt target/soak, brace, smoke flee</name>
  <files>engine/combat.js, test/unit/abilities.test.js</files>
  <read_first>
    - engine/combat.js lines 1722-1940 (foeTurn in full — the acid block at 1749-1762 is the DOT template, the asleep skip at 1764-1768, the member/hero need+damage branches, the tail ticks), 1489-1497 (pickFoeTarget), 1547-1600 (applyFoeDamageToPlayer up to the ward block; the halfNext block is the brace anchor), 1620-1650 (the armour soak block), 798-842 (flee — the branch order)
    - engine/derived.js (abilityEffectActive, weaponDamage), engine/foeDamage.js (damageFoe return shape `{ soaked, applied }`)
    - test/unit/abilities.test.js (Task 1 — the foeTurn-dependent sections)
    - test/unit/party-combat.test.js lines 173-222 (foeTurn member/hero branch tests — the fixed foe/ally shapes to reuse when proving "riposte ignores member misses" and "hamstring halves member damage too")
  </read_first>
  <action>
    Implement the `engine/combat.js` hooks exactly per ability_effects_spec (1)-(5), pickFoeTarget, applyFoeDamageToPlayer (brace + taunt soak), and flee (smoke), importing `abilityEffectActive` from ./derived.js. Each hook gets a one-line Phase 38 comment naming the ability and stating it is a pure read that is false/absent on every fixture (the DOT/riposte draws fire only when the flag/timer exists). Extend `soaked` in applyFoeDamageToPlayer with `brace` the same way `hardiness`/`hide`/`ward` are recorded. Update foeTurn's JSDoc list of per-foe steps.

    Turn the remaining `test/unit/abilities.test.js` sections green; add the two party-shaped proofs (riposte ignores a member miss; hamstring halves the member-branch `mDmg`) using party-combat's fixed shapes. Run the whole file, party-combat.test.js, afraid/fight-gate/feedback-payload tests, and the parity suite.
  </action>
  <verify>
    <automated>node --test test/unit/abilities.test.js test/unit/party-combat.test.js test/unit/feedback-payload.test.js && node --test test/parity/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'type: "dotTick"\|type: "foeStunned"\|type: "foeSightReturned"\|type: "riposted"\|type: "braceHeld"' engine/combat.js` prints 5
    - `grep -c 'reason: "smoke"' engine/combat.js` prints 1; `grep -c 'abilityEffectActive(state.c, "taunt")' engine/combat.js` prints 1; `grep -c "f.hamstrung" engine/combat.js` prints 2; `grep -c "soakAr" engine/combat.js` >= 2
    - `node --test test/unit/abilities.test.js` passes with >= 40 tests; party-combat and feedback-payload pass; `node --test test/parity/` passes
  </acceptance_criteria>
  <done>Every foe-side and defensive ability resolves through foeTurn/pickFoeTarget/applyFoeDamageToPlayer/flee hooks that are inert on every fixture; the ability suite is fully green.</done>
</task>

<task type="auto">
  <name>Task 3: Narration for every new event (three tables + BRANCH_TOGGLES + FEATURE_EVENTS refusal), USABLE-FEATURES-AUDIT row, docs/ABILITIES.md play rules, plan gate</name>
  <files>src/browser/eventNarration.js, src/browser/toasts.js, test/voice/safety-scan.test.js, docs/USABLE-FEATURES-AUDIT.md, docs/ABILITIES.md</files>
  <read_first>
    - src/browser/toasts.js lines 40-70 (TONES/PRIORITY), 204-260 (FEATURE_EVENTS — add "abilityRefused"), 300-320 (the `block()` helper — grep `function block`), 1030-1036 (fled's reason map), 899-1000 (entry shapes); src/browser/eventNarration.js lines 74-100 (table head), 276-283 (fled), 215-230 (strikeRefused's reason switch — the register for abilityRefused)
    - test/unit/toastsCoverage.test.js lines 139-200 (partition + FEATURE_EVENTS rules), test/unit/formatEventsCoverage.test.js, test/voice/safety-scan.test.js lines 125-160 (BASE_EVENT/BRANCH_TOGGLES — add `name`, `left`, `have`, `max`, `soaked`, `by`, `attacks` defaults where missing)
    - docs/USABLE-FEATURES-AUDIT.md §1 (the notWorn row shape from Phase 37)
    - docs/ABILITIES.md (append)
  </read_first>
  <action>
    Add every entry per narration_spec to EVENT_NARRATION and TOAST_FOR (abilityRefused via `block(...)`, priority PRIORITY.block; activations PRIORITY.feature tone "hit"/"beat"; foe consequences PRIORITY.them; your healing/damage PRIORITY.you). Extend `fled`'s narration and toast maps with `smoke`. Add `"abilityRefused"` to FEATURE_EVENTS. Extend BRANCH_TOGGLES with the six new reason objects and BASE_EVENT with any new field the builders read. Add the `abilityRefused` row to docs/USABLE-FEATURES-AUDIT.md §1 mirroring the notWorn row (reasons listed). Append `## Play rules (Plan 03)` to docs/ABILITIES.md: the ladder order, the cooldown model (effect+cd, once a fight = 999 cleared at endCombat), the per-ability resolution table (a condensed copy of ability_effects_spec), the draw statement.

    Plan gate (record in the SUMMARY): `npm test` `# fail 0`; both coverage guards + voice scan green; `git status --porcelain test/parity/fixtures` empty; master hash unchanged; `git diff --stat -- package.json package-lock.json tools` empty; `git diff --stat <plan-02-final-commit> -- engine content src mazeworld.html docs` lists exactly engine/abilities.js, engine/combat.js, engine/actions.js, engine/engine.js, src/browser/eventNarration.js, src/browser/toasts.js, docs/USABLE-FEATURES-AUDIT.md, docs/ABILITIES.md. SUMMARY `## Human verification (deferred to end of run)`: still nothing reachable on device (no submenu rows until Plan 05) — zero items, say why; carry the ability-line tone read to Plan 05's checklist. Explicit-path commits only.
  </action>
  <verify>
    <automated>node --test test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js test/unit/toastsForAction.test.js && npm test 2>&1 | grep -E "^# (pass|fail)"</automated>
  </verify>
  <acceptance_criteria>
    - for each of abilityUsed, abilityRefused, pommelStruck, foeStunned, battleRoarRaised, sidestepped, secondWindHealed, swept, sweptFoe, braced, braceHeld, riposteReady, riposted, taunted, lastStandCalled, dirtyTrickLanded, foeSightReturned, smokeThrown, cutpursed, poisonedEdgeApplied, dotTick, hamstrung, marked: `grep -c "^  <name>:" src/browser/eventNarration.js` prints 1 and `grep -c "^  <name>:" src/browser/toasts.js` prints 1
    - `grep -c "Your arm has opinions" src/browser/toasts.js src/browser/eventNarration.js` prints 1 for each; `grep -c "smoke:" src/browser/toasts.js` >= 1
    - `grep -c '"abilityRefused"' src/browser/toasts.js` >= 1 (FEATURE_EVENTS); `grep -c 'reason: "notLowEnough"' test/voice/safety-scan.test.js` prints 1
    - `grep -c "abilityRefused" docs/USABLE-FEATURES-AUDIT.md` >= 1; `grep -c "## Play rules" docs/ABILITIES.md` prints 1
    - `npm test` ends `# fail 0`; both coverage guards and the voice scan pass
  </acceptance_criteria>
  <done>Every ability event is legible in the fight log and toast tables with the canon refusal register; guards green; ledger and audit doc updated; plan gate recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell dispatch -> engine | `useAbility.key` arrives from the submenu row payload (validated as a non-empty string; unknown ids refuse) |
| combat state -> hooks | foe flags/timers are engine-written; a tampered save cannot carry them (combat resets to null on load) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-06 | Tampering | engine/actions.js validate useAbility | low | mitigate | key must be a non-empty string; unknown/un-owned ids are a named refusal, never a throw |
| T-38-07 | Denial of Service | engine/abilities.js round economy | medium | mitigate | a refusal spends no action; a success ends the round exactly once (tested) — no free foe turns, no skipped foe turns |
| T-38-08 | Tampering | f.dot / f.blindFor counters | low | mitigate | counters are decremented and deleted by foeTurn; a foe object only ever comes from startCombat |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | no package change (gate) |
</threat_model>

<verification>
- `npm test` -> `# fail 0`; parity suite green with unchanged fixtures
- `node --test test/unit/abilities.test.js` -> every ability, the ladder, the round economy, timer lifecycle, measured draw counts
- Coverage guards + voice scan green with 23 new event types and the six reason toggles
</verification>

<success_criteria>
- ROADMAP SC-2: using an ability on cooldown produces a named refusal in the fight log (abilityRefused cooldown with the ability's name and rounds left), never a silent no-op
- CONTEXT "Action economy", "Cooldowns", "Rng" (draws only inside the new action), "Events" (abilityUsed + existing strike vocabulary + f.stunned/f.blind/f.dot/timers) implemented as locked; the post-research ruling (strike-modifiers resolve immediately via the inline playerStrike call) honoured for all five plus Overhead Blow and Last Stand
</success_criteria>

<output>
Create `.planning/phases/38-melee-active-abilities/38-03-SUMMARY.md` when done (include `## Human verification (deferred to end of run)` — zero items with the reason, the rng/serialized-field statements, and the measured per-ability draw counts).
</output>
