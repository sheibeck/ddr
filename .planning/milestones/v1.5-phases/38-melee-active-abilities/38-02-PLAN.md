---
phase: 38-melee-active-abilities
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - engine/derived.js
  - engine/combat.js
  - engine/movement.js
  - engine/encounters.js
  - src/browser/viewModels.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - test/unit/ability-strike.test.js
  - test/unit/identity-contract.test.js
  - test/unit/combat.test.js
  - test/unit/fluency.test.js
  - test/unit/parley.test.js
  - test/unit/identity-combat.test.js
  - test/unit/feedback-payload.test.js
  - test/unit/toastsForAction.test.js
  - docs/ABILITIES.md
autonomous: true
requirements: [ABIL-02]
tags: [engine, passive-deletion, strike-descriptor, need-shift, identity-contract, sc-4, deferred-uat]
notes:
  rng_draw_impact: "ZERO on every fixture: (a) the Tracking d20 in startCombat fired only for a hero with the Tracking key — no fixture fight hero has it (seed 13 is chargen-only), so deleting the block removes no fixture draw; (b) every other deletion is a pure read (skill(c, ...) terms in toHit/weaponDamage/foeToHitVs/fluency/climbBonus/leapBonus/springTrap/the Death-touch and Silence branches); (c) the new abilityStrike/timers reads are pure reads that are false for every fixture (no fixture ever sets state.combat.abilityStrike or an ability:* timer). The `win` scenario (seed 3) keeps identical state; only its opening-strike EVENT is backstab instead of silenceStrike (declared in Plan 01's table)."
  serialized_field_impact: "none new — state.combat.abilityStrike is TRANSIENT: set and deleted inside the same dispatch (playerStrike clears it after its attack loop), so it never reaches serializeRun or any comparable; the timers records it reads are Phase 36's c.timers (already carved out)."
  gameplay_change_flagged: "fluency(c) now maxes at 1 (tongue item only) — the fluency-2 'Magical parley for every race/sub' tier from Phase 20 is unreachable until a future source of fluency exists; recorded in docs/ABILITIES.md. Climbing's fall-damage halving (both climb and leap branches) and the +4/+2 climb/leap bonuses are gone for everyone (Phase 39's rope/ladder tools are the hazard answer per CONTEXT deferred ideas)."

must_haves:
  truths:
    - "grep over engine/ and src/browser/ for the seven dead reads — skill(...,\"Agility\"), \"Death-touch\", \"Silence\", \"Language\", \"Tracking\", \"Climbing\", \"Leaping\" — returns 0 executable lines, and skill(...,\"Kata\") returns 0 lines (Kata is an active now)"
    - "engine/derived.js exports abilityEffectActive(c, key) (true only for a c.timers['ability:'+key] record in phase 'effect' with left > 0); foeToHitVs(state, vs='hero') and foeToHitBreakdown(state, vs='hero') apply: Battle Roar -2 (any vs), Sidestep -2 (vs 'hero' only), Smoke h=1 override (vs 'hero' only), in that order after the gear term and before mirror/invis; breakdown.need === foeToHitVs for every (race, sub, timers, vs) combination (test/unit/feedback-payload.test.js matrix extended)"
    - "playerStrike honours a transient state.combat.abilityStrike descriptor { key, autoHit?, forceCrit?, finishUnder?, bonusDmg?, dmgMul?, needShift?, attacks? } with zero new draws (measured), tags struck/strikeMissed/deathTouch with via: key, clears the descriptor after its attack loop (never present on state.combat when afterPlayerAction runs), and never sets C.opened for an ability auto-hit (the Cat Burglar/Ninja free opener is preserved)"
    - "foeTurn's member branch calls foeToHitVs(state, 'member') / foeToHitBreakdown(state, 'member'); the hero branch keeps the no-arg (hero) reads"
    - "A foe with marked === true takes +2 on every landed hero strike (after the crit doubling)"
    - "test/unit/identity-contract.test.js: the Guard good proves Sidestep's active -2 stacks with the Guard -1 (foeToHitVs(guard with ability:sidestep effect) === foeToHitVs(control) - 3); no CONTRACT row's run() source references Agility/Death-touch/Silence/Language/Tracking/Climbing/Leaping/Kata; a dedicated SC-4 test asserts every Fighter/Thief sub row still has a non-empty good AND bad and that docs/CLASS-PASS.md's good/bad table names none of the eight reshaped skills"
    - "silenceStrike and trackingRolled are gone from engine emission, EVENT_NARRATION, TOAST_FOR, FEATURE_EVENTS and the encounterStarted fold; both coverage guards pass; deathTouch remains (emitted by the Death Touch active's finish-under-15 path)"
    - "npm test prints '# fail 0' with test/parity/fixtures unchanged from Plan 01's commit and the master hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
  artifacts:
    - path: "engine/derived.js"
      provides: "abilityEffectActive; vs-aware foeToHitVs/foeToHitBreakdown with the three ability terms; Kata/Agility/Silence/Language reads deleted"
    - path: "engine/combat.js"
      provides: "abilityStrike descriptor reads in playerStrike (+via tags, deathTouch via), Tracking block deleted, Silence/Death-touch passive branches deleted, t.marked +2, member-branch vs='member'"
    - path: "engine/movement.js"
      provides: "climbBonus/leapBonus and the Climbing fall-halving deleted"
    - path: "engine/encounters.js"
      provides: "springTrap dodge without Agility/Leaping terms"
    - path: "test/unit/ability-strike.test.js"
      provides: "descriptor composition proofs (auto-hit, force-crit incl. noCrit/heavy denial, finishUnder, bonusDmg, dmgMul, needShift floor, attacks/frenzy max, opened untouched, via tags, transient clear, zero extra draws), need-shift proofs, marked +2"
  key_links:
    - from: "engine/combat.js#playerStrike"
      to: "state.combat.abilityStrike"
      via: "const AS = C.abilityStrike || null; reads per attack; delete C.abilityStrike after the loop"
      pattern: "C\\.abilityStrike"
    - from: "engine/derived.js#foeToHitVs"
      to: "c.timers['ability:battleRoar'|'ability:sidestep'|'ability:smoke']"
      via: "abilityEffectActive(c, 'battleRoar'|'sidestep'|'smoke')"
      pattern: "abilityEffectActive\\(c, \"sidestep\"\\)"
    - from: "engine/combat.js#foeTurn (member branch)"
      to: "engine/derived.js#foeToHitVs"
      via: "foeToHitVs(state, \"member\")"
      pattern: "foeToHitVs\\(state, \"member\"\\)"
  prohibitions:
    - "No engine/abilities.js yet, no useAbility action, no new event types other than payload fields (via) — Plan 03 owns the dispatcher and its events"
    - "No edit to content/, engine/character.js, engine/saveState.js, test/parity/fixtures, prototype-master.js.txt, mazeworld.html, combatMenu.js"
    - "No 'if (c.abilities)' gates; no compatibility branch for the old skill keys anywhere in engine/ or src/"
    - "No checkpoint:human-verify, no device step, no APK build; explicit-path commits only"
---

<objective>
Delete every old passive branch the reshape retired and install the hooks the actives will drive: (1) the four dropped skills' reads (Tracking in `startCombat`, Language in `fluency`, Climbing/Leaping in movement's climb/leap resolution and springTrap's dodge, Agility in springTrap) go away with no replacement (D: "Dropped outright"); (2) the five converted skills' passive reads (Agility/Silence-in-dark in the twin need functions, Death-touch and Silence in `playerStrike`, Kata in `toHit`/`memberToHit`/`weaponDamage`/`damageBracket`) are replaced by their active hooks — the transient `state.combat.abilityStrike` descriptor in `playerStrike` and `c.timers`-driven need shifts in `foeToHitVs`/`foeToHitBreakdown` (D: "Converted", "Strike-modifying abilities resolve immediately, uniformly", "party-wide need shift via a c.timers record read by foeToHitVs/foeToHitBreakdown"); (3) the identity-contract suite is updated in this phase (SC-4): Guard's stacking test moves to Sidestep, the Language plants are removed, and a regression guard proves every Fighter/Thief sub keeps one good and one bad that depends on no reshaped skill (D: "Sub-class identity").

Purpose: greenfield means the old rules are gone, not dormant; landing the reads/hook seams before the dispatcher keeps Plan 03 a pure "add the action" plan and lets this plan prove zero draw drift on its own.
Output: derived.js/combat.js/movement.js/encounters.js/viewModels.js edits, the three narration tables minus two dead events, `test/unit/ability-strike.test.js`, the reworked identity/parley/fluency tests, the SC-4 guard, a docs/ABILITIES.md "Retired passives" section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-melee-active-abilities/38-CONTEXT.md
@.planning/phases/38-melee-active-abilities/38-RESEARCH.md
@.planning/phases/38-melee-active-abilities/38-01-SUMMARY.md
@engine/derived.js
@engine/combat.js
@engine/movement.js
@engine/encounters.js
</context>

<artifacts_this_phase_produces>
See 38-01-PLAN.md `<artifacts_this_phase_produces>` for the full phase list. This plan creates the **[02]** items: `engine/derived.js#abilityEffectActive(c, key)`, the `vs` parameter on `foeToHitVs`/`foeToHitBreakdown` with mods named `"Battle Roar"`, `"Sidestep"`, `"Smoke"`; `engine/combat.js#playerStrike`'s `abilityStrike` descriptor reads (`autoHit`, `forceCrit`, `finishUnder`, `bonusDmg`, `dmgMul`, `needShift`, `attacks`, `key`), the additive `via` field on `struck`/`strikeMissed`/`deathTouch`, the needMods entry `{ name: "overhead", delta }`, `t.marked` +2; `test/unit/ability-strike.test.js`; the SC-4 regression guard in `test/unit/identity-contract.test.js` (`RESHAPED_SKILLS` literal). Deleted: `silenceStrike` and `trackingRolled` (engine + three tables + `FEATURE_EVENTS` + the encounterStarted fold case + `CRIT_BY_TEXT.silence` in both tables), `climbBonus`/`leapBonus`.
</artifacts_this_phase_produces>

<strike_descriptor_spec>
`state.combat.abilityStrike` (transient; set by Plan 03's `useAbility` immediately before it calls `playerStrike`; cleared by `playerStrike` itself right after its attack loop and before `afterPlayerAction`):

`{ key, autoHit?: true, forceCrit?: true, finishUnder?: 15, bonusDmg?: number, dmgMul?: number, needShift?: -2, attacks?: 3 }`

Reads inside `playerStrike` (all zero-draw, all guarded by `const AS = C.abilityStrike || null;` taken once after the `!t` return):
1. attacks: after the haste line, `if (AS && AS.attacks) attacks = Math.max(attacks, AS.attacks);` and change the frenzy branch's `attacks = 2` to `attacks = Math.max(attacks, 2)` (value-identical for every existing path, where attacks <= 2 before).
2. need: after the `magicOnly` line and BEFORE the Afraid line: `if (AS && AS.needShift && need > 0) { const before = need; need = Math.max(1, need + AS.needShift); abilityMods = need !== before ? [{ name: "overhead", delta: need - before }] : []; }` — merge `abilityMods` into the `needMods` payload alongside `afraidMods` on both `strikeMissed` and `struck`.
3. auto-hit: replace the `auto`/`C.opened` pair with `const subAuto = (c.sub === "Cat Burglar" || c.sub === "Ninja") && !C.opened; if (subAuto) C.opened = true; const auto = subAuto || !!(AS && AS.autoHit);` — the Ninja first-strike damage line reads `subAuto`, not `auto`.
4. finishUnder: at the position of the deleted Death-touch branch (after `critBy`/`opening` are computed): `if (AS && AS.finishUnder && t.wp < AS.finishUnder) { events.push({ type: "deathTouch", target: t.name, via: AS.key }); t.wp = 0; killFoe(state, t, rng, events); continue; }`.
5. forceCrit: after the Cutthroat block and before `if (crit) dmg *= 2`: `if (AS && AS.forceCrit && !noCrit && !(AS.key === "silentStep" && heavy)) { crit = true; critBy = AS.key; }` — a forced crit obeys the Guard/Soldier/dark/noCrit-gear rule exactly like a natural 1 (a Guard's Death Touch still finishes under 15, but does not double); Silent Step's crit is denied by heavy armour like the old Silence branch (the strike still auto-hits). When a heavy-armoured Thief's Silent Step is denied, push `{ type: "backstabDenied", reason: "heavyArmor" }` only if the existing `opening && heavy` push did not already fire this attack.
6. damage: directly after `let dmg = weaponDamage(c, rng);` add `if (AS && AS.bonusDmg) dmg += AS.bonusDmg;`; directly after `if (crit) dmg *= 2;` add `if (AS && AS.dmgMul) dmg *= AS.dmgMul;` then `if (t.marked) dmg += 2;`.
7. via: `...(AS ? { via: AS.key } : {})` on both `strikeMissed` and `struck`.
8. clear: after the `for` loop, before `afterPlayerAction`: `if (C.abilityStrike) delete C.abilityStrike;`.
The existing `struck` narration's `CRIT_BY_TEXT` gains entries for `deathTouch` ("Called it. Critical!") and `silentStep` ("Not a sound. Critical!") in BOTH eventNarration.js and toasts.js (toasts' map is the short-token form: `deathTouch: "Death Touch", silentStep: "Silent Step"`), and `silence` is removed from both.
</strike_descriptor_spec>

<need_shift_spec>
`engine/derived.js`:
- `export function abilityEffectActive(c, key)` — `const rec = c && c.timers && c.timers[`ability:${key}`]; return !!(rec && rec.phase === "effect" && rec.left > 0);` (no import of effects.js needed; the record shape is Phase 36's documented contract).
- `foeToHitVs(state, vs = "hero")`: delete the Agility line and the Silence-in-dark line; after `h += eff(c, "foeToHit");` add in order: `if (abilityEffectActive(c, "battleRoar")) h -= 2;` · `if (vs === "hero" && abilityEffectActive(c, "sidestep")) h -= 2;` · `if (vs === "hero" && abilityEffectActive(c, "smoke")) h = 1;` then the existing mirror/invis/floor lines. JSDoc: `vs` is `"hero"` (default — every existing caller) or `"member"` (foeTurn's party-member branch: Battle Roar covers the whole side, Sidestep/Smoke are the hero's own body).
- `foeToHitBreakdown(state, vs = "hero")`: the same three steps recorded as `{ name: "Battle Roar" | "Sidestep" | "Smoke", delta }` (Smoke as an override: `delta = 1 - before`), Agility/Silence steps deleted. Plan 04 extends the Battle Roar term with a party scan; this plan reads the hero only.
- `toHit`, `memberToHit`, `weaponDamage`: delete the Kata lines. `fluency(c)`: `return eff(c, "tongue") > 0 ? 1 : 0;` — update its JSDoc and canParley's (the fluency-2 tier is unreachable until a future fluency source; do NOT delete the fluency-2 branch in canParley — it is a data-driven threshold, not a dead read).
`src/browser/viewModels.js#damageBracket`: delete the Kata line and update the mirror-of-weaponDamage comment.
`engine/combat.js#foeTurn` member branch: `foeToHitVs(state, "member")` and `foeToHitBreakdown(state, "member")`.
</need_shift_spec>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: playerStrike abilityStrike descriptor reads + Silence/Death-touch passive branches deleted + t.marked + need-shift twin functions + Kata/Agility/Silence/Language reads deleted — tests first</name>
  <files>engine/derived.js, engine/combat.js, src/browser/viewModels.js, src/browser/eventNarration.js, src/browser/toasts.js, test/unit/ability-strike.test.js, test/unit/feedback-payload.test.js</files>
  <read_first>
    - engine/combat.js lines 456-650 (playerStrike in full — the attacks chain at 488-503, the need chain at 505-530, the `auto`/`C.opened` pair at 526-528, the Death-touch branch at 562-568, the opening-strike block at 571-595 incl. the Silence branch, the Cutthroat block, `if (crit) dmg *= 2`, the struck push), 1820-1835 and 1876-1890 (the member and hero need computations in foeTurn)
    - engine/derived.js lines 470-490 (toHit's Kata line), 520-530 (memberToHit's), 547-630 (foeToHitVs + foeToHitBreakdown in full — the Agility and Silence lines and their breakdown mirrors), 640-660 (weaponDamage's Kata line), 755-765 (fluency)
    - src/browser/viewModels.js lines 30-58 (damageBracket)
    - src/browser/eventNarration.js lines 230-258 (strikeMissed/deathTouch/silenceStrike/struck + CRIT_BY_TEXT); src/browser/toasts.js lines 300-310 (its CRIT_BY_TEXT) and the struck/strikeMissed/deathTouch entries (grep `struck:` and `deathTouch:`)
    - test/unit/feedback-payload.test.js (the foeToHitVs === breakdown.need matrix — find the matrix loop and its state builder)
    - test/unit/identity-contract.test.js lines 60-215 (fakeRng/looseRng/countingRng/hero/withWeapon/fixedFoe/withCombat/expectEvent — copy the helper shapes into the new test file; that file's precedent is not to import them)
    - engine/effects.js (startEffect/startCooldown — used by the tests to plant ability:* records)
    - .planning/phases/38-melee-active-abilities/38-RESEARCH.md §"Research Question 5" (the composition rationale) and §"Research Question 4" rows Death Touch/Sidestep/Kata/Battle Roar/Silent Step/Smoke/Mark/Overhead Blow
  </read_first>
  <behavior>
    - Auto-hit: a Soldier (need 5) with `C.abilityStrike = { key: "kata", autoHit: true, bonusDmg: 1 }` and a roll of 20 still lands: `struck` present with `via: "kata"`, `dmg` = control dmg + 1; `C.opened` stays false afterwards; a Cat Burglar's first strike still sets `C.opened` and the Ninja first-strike formula still fires without a descriptor
    - Zero extra draws: countingRng around playerStrike with and without `{ key: "kata", autoHit: true, bonusDmg: 1 }` draws the same number of values for the same seed-1 Soldier vs a 30-hp foe (measured, not hand-pinned)
    - forceCrit: `{ key: "silentStep", forceCrit: true, autoHit: true }` on a Pilfer in Leather with roll 4 yields `struck.critical === true`, `critBy === "silentStep"`, dmg doubled vs the control; the same on a Guard yields `critical === false` (noCrit); the same on a Thief in Chain Mail yields `critical === false` and exactly one `backstabDenied`; `{ key: "deathTouch", forceCrit: true, finishUnder: 15 }` on a Soldier (noCrit sub) vs a 30-hp foe with roll 3 yields `critical === false` and dmg equal to the control; the same descriptor on a Knight vs a 30-hp foe yields `critical === true`, `critBy === "deathTouch"`, dmg doubled; on a Knight vs a 12-hp foe the events include `deathTouch { target, via: "deathTouch" }` and `foeKilled` and no `struck`; on a Soldier vs a 12-hp foe the finish still fires (finishUnder ignores noCrit)
    - dmgMul/needShift: `{ key: "overheadBlow", dmgMul: 2, needShift: -2 }` on a Knight (need 5): roll 4 -> `strikeMissed` with `need: 3`, `needMods` containing `{ name: "overhead", delta: -2 }`, `via: "overheadBlow"`; roll 2 -> `struck` with `dmg` = 2 x control (control measured with the same fakeRng sequence and no descriptor); on a foe with need already 1 the shift floors at 1 (not 0); on a `magicOnly` foe with no magic weapon need stays 0
    - attacks: `{ key: "lastStand", attacks: 3 }` on a Knight yields three attack events (struck/strikeMissed) against a foe that survives them; a Fridgian frenzy roll still yields attacks 2 for a normal strike and 3 with the descriptor (Math.max)
    - transient: after playerStrike returns, `state.combat.abilityStrike` is undefined (combat still live) — and a spy `afterPlayerAction` (assert via a foe-count/round change) observed no descriptor
    - marked: a foe with `marked: true` takes control dmg + 2 on a landed non-crit strike and control x2 + 2 on a crit
    - Passive deletions: a hero with `c.skills = { Agility: 1, Silence: 1, Kata: 1, "Death-touch": 1 }` has toHit/foeToHitVs/weaponDamage identical to a control with `{}`, and an opening strike with roll 3 emits `backstab` (Thief) — never `silenceStrike`; a foe under 15 hp on a roll of 1 with `"Death-touch": 1` planted is NOT finished (no deathTouch event; the natural-1 crit still doubles); `fluency({ skills: { Language: 1 } }) === 0`
    - Need shifts: a Soldier with `startEffect(c, "ability:battleRoar", { rounds: 2, cd: 5 })` has foeToHitVs(state) === control - 2 AND foeToHitVs(state, "member") === control - 2; with `ability:sidestep` instead: hero -2, member unchanged; with `ability:smoke`: hero 1, member unchanged; a record in phase "cooldown" changes nothing; breakdown.need === foeToHitVs and breakdown.mods contains `{ name: "Battle Roar", delta: -2 }` / `{ name: "Sidestep", delta: -2 }` / `{ name: "Smoke", delta: 1 - before }` respectively; the feedback-payload matrix extended with the three records x two vs values still proves need equality
  </behavior>
  <action>
    Write `test/unit/ability-strike.test.js` FIRST (helpers copied from identity-contract's scaffold shape; states from `newRun(seed, [], { force })` or the `hero(sub)` pattern; foes via a `fixedFoe` literal with `alive: true`, `wp`, `maxWP`, `intel`, `lvl`, `sp: {}`); plant descriptors directly on `state.combat.abilityStrike` and timers via `startEffect`. RED.

    `engine/derived.js`: implement need_shift_spec exactly (add `abilityEffectActive` beside `skill`/`skillTier` near the top; edit foeToHitVs/foeToHitBreakdown/toHit/memberToHit/weaponDamage/fluency; update the JSDocs — foeToHitVs's Phase 24 note about Agility stacking becomes a Phase 38 note about Sidestep/Battle Roar stacking with the Guard -1).

    `engine/combat.js#playerStrike`: implement strike_descriptor_spec exactly — delete the old Death-touch branch and the Silence branch of the opening-strike block (Stealth and the plain-Thief backstab branches stay; the block's comment loses the word Silence), keep every other line byte-identical. Update the JSDoc's list of critical-strike rules (Death Touch/Silent Step are now descriptor-driven). In foeTurn's member branch switch both need reads to `vs = "member"` with a one-line comment.

    `src/browser/viewModels.js`: delete the Kata line in damageBracket. `src/browser/eventNarration.js`: delete the `silenceStrike` entry; CRIT_BY_TEXT: remove `silence`, add `deathTouch: "Called it. Critical!"` and `silentStep: "Not a sound. Critical!"`; extend `deathTouch`'s line to read the same with or without `via`. `src/browser/toasts.js`: delete the `silenceStrike` entry and the `"silenceStrike"` FEATURE_EVENTS line; CRIT_BY_TEXT: remove `silence`, add `deathTouch: "Death Touch", silentStep: "Silent Step"`. Comment discipline: neither table file may keep a comment that names a removed event type or the removed crit token — describe the change as "the Silence passive's strike event/crit token, retired in Phase 38" if a comment is needed at all.
<!-- planner-discipline-allow: silenceStrike -->
<!-- planner-discipline-allow: silence: -->

    `test/unit/feedback-payload.test.js`: extend the need-equality matrix with the three planted records (effect phase) x vs in ["hero", "member"] and a cooldown-phase control.

    Run the new file (GREEN), feedback-payload, then `npm test` — expected red: identity-contract (Guard/Language rows), combat.test.js/fluency.test.js/parley.test.js/identity-combat.test.js (Language plants), toastsForAction (silenceStrike probe), coverage guards (trackingRolled still emitted but tables intact — fine until Task 2). Record the list.
  </action>
  <verify>
    <automated>node --test test/unit/ability-strike.test.js && node --test test/unit/feedback-payload.test.js && node --test test/parity/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' engine/derived.js | grep -c 'skill(c, "Kata")\|skill(m, "Kata")\|skill(c, "Agility")\|skill(c, "Silence")\|skill(c, "Language")'` prints 0
    - `grep -v '^\s*//' engine/combat.js | grep -c 'skill(c, "Death-touch")\|skill(c, "Silence")'` prints 0
    - `grep -v '^\s*//' src/browser/viewModels.js | grep -c 'skill(c, "Kata")'` prints 0
    - `grep -c "^export function abilityEffectActive" engine/derived.js` prints 1; `grep -c 'vs = "hero"' engine/derived.js` prints 2; `grep -c 'foeToHitVs(state, "member")' engine/combat.js` prints 1
    - `grep -c "C.abilityStrike" engine/combat.js` >= 3 (read, via, delete); `grep -c 'name: "overhead"' engine/combat.js` prints 1; `grep -c "t.marked" engine/combat.js` prints 1
    - `grep -c "silenceStrike" src/browser/eventNarration.js src/browser/toasts.js` prints 0 for both; `grep -c 'silence:' src/browser/eventNarration.js src/browser/toasts.js` prints 0 for both; `grep -c "deathTouch:" src/browser/toasts.js` prints 2 (TOAST_FOR entry + CRIT_BY_TEXT token)
    - `node --test test/parity/` passes (no fixture state moved by this task)
    - `node --test test/unit/ability-strike.test.js` passes with >= 16 tests
  </acceptance_criteria>
  <done>The descriptor seam and the timer-driven need shifts are live and proven zero-draw; every converted passive read is gone; parity untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Dropped-skill reads (Tracking, Language callers, Climbing, Leaping, trap Agility) + trackingRolled removal + test surgery for Language/parley/fluency/combat/toastsForAction</name>
  <files>engine/combat.js, engine/movement.js, engine/encounters.js, src/browser/eventNarration.js, src/browser/toasts.js, test/unit/combat.test.js, test/unit/fluency.test.js, test/unit/parley.test.js, test/unit/identity-combat.test.js, test/unit/toastsForAction.test.js</files>
  <read_first>
    - engine/combat.js lines 174-185 (the Tracking block in startCombat — `let tracked = false;` stays, the `if` block goes; `C.tracked`, the `trackable` event and flee's tracked branch stay as dormant machinery), 893-1000 (canParley/parley JSDocs mentioning Language/fluency — update wording only)
    - engine/movement.js lines 43-45 (climbBonus/leapBonus), 178-200 (the climb loop `rng.d(10) - climbBonus(...)`, the leap `rng.d(10) - leapBonus(...)`, and the two Climbing fall-halving lines) — deleting a `- 0` term and a false `if` is value-identical for every character without the skill; NO fixture reaches these lines (movement fixture `_note`)
    - engine/encounters.js lines 60-70 (springTrap's `nimble` expression — keep `5 + (c.sub === "Acrobat" ? 3 : 0)`)
    - src/browser/toasts.js lines 760-772 (the `case "trackingRolled":` in the encounterStarted fold) and its `trackingRolled` TOAST_FOR entry (:947); src/browser/eventNarration.js lines 174-177 (`trackingRolled`)
    - test/unit/combat.test.js lines 825-860, test/unit/fluency.test.js lines 70-110, test/unit/parley.test.js lines 140-250 (the `lang` x `helm` matrix — read the whole test to see which assertions encode fluency 2), test/unit/identity-combat.test.js lines 300-320, test/unit/toastsForAction.test.js lines 80-95
    - test/unit/formatEventsCoverage.test.js lines 118-160 (both directions of the guard: every emitted type narrated AND no narration for a never-emitted type)
  </read_first>
  <action>
    `engine/combat.js#startCombat`: delete the `if (skill(c, "Tracking")) { ... }` block (keep `let tracked = false;` and every downstream `tracked` use; add a one-line comment: Tracking retired in Phase 38 — `tracked` is dormant until a future source sets it). Update the canParley/parley JSDocs to say fluency comes from the tongue effect only. `engine/movement.js`: delete `climbBonus`/`leapBonus` and their two call terms, and the two `if (skill(state.c, "Climbing")) hurt = ...` lines; update the header comment (ports lines 1467-1468 no longer). `engine/encounters.js#springTrap`: `nimble = 5 + (c.sub === "Acrobat" ? 3 : 0)`; update the JSDoc. Remove the `skill` import from movement.js/encounters.js ONLY if no other read remains in the file (movement.js still reads Hardiness — keep it; encounters.js still reads Hardiness/Night Vision — keep it).

    `src/browser/toasts.js`: delete the `trackingRolled` TOAST_FOR entry and the fold `case "trackingRolled": consumed.add(j); break;`. `src/browser/eventNarration.js`: delete the `trackingRolled` entry. Leave `trackable` everywhere (still a literal in engine source). Comment discipline: no comment in engine/combat.js, engine/movement.js, engine/encounters.js, toasts.js or eventNarration.js may quote a retired skill key in double quotes or name the removed event type / the removed bonus helpers — say "the retired Tracking read" / "the retired climb and leap bonuses" instead.
<!-- planner-discipline-allow: "Tracking" -->
<!-- planner-discipline-allow: "Climbing" -->
<!-- planner-discipline-allow: trackingRolled -->
<!-- planner-discipline-allow: climbBonus -->
<!-- planner-discipline-allow: leapBonus -->

    Tests: `test/unit/toastsForAction.test.js` — replace the `silenceStrike` probe event with `{ type: "deathTouch", target: "Viper" }` (keeps 6 features + 1 refusal). `test/unit/combat.test.js` (canParley gates test ~833-856): remove the Language-skill cases and re-express any fluency-2 expectation (skill + Helm) as unreachable — a Con Artist with the Helm alone is fluency 1 -> Magical false; add the assertion that a `{ Language: 1 }` plant changes nothing versus `{}`. `test/unit/fluency.test.js`: the tier-1/tier-2 skill cases become `fluency({ skills: { Language: 1 } }) === 0` (retired key) and the max is 1 with the Helm; keep every tongue-item case. `test/unit/parley.test.js`: collapse the `lang` dimension of the matrix (the expected fluency helper at :150 drops its skills term; every case that asserted fluency 2 via skill + helm now asserts fluency 1 behaviour); keep the Helm-only cases and the Walking Dead unconditional refusal. `test/unit/identity-combat.test.js` ~312: drop the `skills: { Language: 1 }` plant (the assertion is about the sub's refusal, which holds regardless). Do not weaken any assertion that is still reachable — every remaining fluency-1 path must still be asserted.

    Run the touched test files, both coverage guards, and `npm test` — expected remaining red: identity-contract only (Task 3).
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/fluency.test.js test/unit/parley.test.js test/unit/identity-combat.test.js test/unit/toastsForAction.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js && node --test test/parity/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*//' engine/combat.js engine/movement.js engine/encounters.js | grep -c '"Tracking"\|"Climbing"\|"Leaping"\|"Agility"\|"Language"'` prints 0
    - `grep -c "climbBonus\|leapBonus" engine/movement.js` prints 0
    - `grep -c "trackingRolled" engine/combat.js src/browser/toasts.js src/browser/eventNarration.js` prints 0 for all three; `grep -c '"trackable"' engine/combat.js` >= 1
    - `grep -c "silenceStrike" test/unit/toastsForAction.test.js` prints 0
    - `grep -c "Language" test/unit/identity-combat.test.js` prints 0
    - both coverage guards pass; `node --test test/parity/` passes; the five touched test files pass
  </acceptance_criteria>
  <done>The four dropped skills have no executable read anywhere; trackingRolled is gone from engine and tables; every test that planted Language/Tracking/Climbing/Leaping is reworked to the new rules without weakening reachable assertions.</done>
</task>

<task type="auto">
  <name>Task 3: Identity-contract updates (Guard -> Sidestep stacking, Language plants removed) + the SC-4 regression guard + docs/ABILITIES.md "Retired passives" + plan gate</name>
  <files>test/unit/identity-contract.test.js, docs/ABILITIES.md</files>
  <read_first>
    - test/unit/identity-contract.test.js lines 1-60 (imports — add `startEffect` from engine/effects.js), 470-482 (Guard good), 605-625 (Master of Arms bad — the Language plant), 865-885 (Ninja bad — the Language plant), 1196-1225 (the completeness meta-test — the SC-4 test sits directly after it), the `CONTRACT` array's `kind: "sub"` rows for Fighter/Thief (sub names from CLASSES.Fighter.subs / CLASSES.Thief.subs)
    - docs/CLASS-PASS.md lines 774-810 (the good/bad table — the SC-4 guard greps its rows)
    - docs/ABILITIES.md (Plan 01's ledger — append, never rewrite)
  </read_first>
  <action>
    Guard good (~479-480): replace the Agility plant with `startEffect(guard.c, "ability:sidestep", { rounds: 2, cd: 4 });` and assert `foeToHitVs(guard) === foeToHitVs(control) - 3` with the message "Sidestep's active -2 stacks with the Guard -1"; keep the first `+ 1` assertion. Master of Arms bad (~614) and Ninja bad (~874): delete the `state.c.skills = { Language: 1 };` lines (the loops still prove the unconditional refusal). Rename nothing else.

    Add directly after the completeness meta-test: `test("Phase 38 (ABIL-02, SC-4): every Fighter/Thief sub keeps one good and one bad that depend on no reshaped skill", ...)` with `const RESHAPED_SKILLS = ["Agility", "Death-touch", "Kata", "Silence", "Language", "Tracking", "Climbing", "Leaping"];` — for every CONTRACT row whose key is in `[...CLASSES.Fighter.subs, ...CLASSES.Thief.subs]`: assert `good.name`/`bad.name` are non-empty, `good.run.toString()` and `bad.run.toString()` contain none of the reshaped names as a quoted key (`"<name>"` or `<name>:`), and call `good.run()` and `bad.run()` again (they must not throw); then read `docs/CLASS-PASS.md`, slice from the line containing `### Good / bad table` to the next `## `/`### ` heading, and assert none of the RESHAPED_SKILLS names appears in that slice. Also assert the count of Fighter/Thief rows equals `CLASSES.Fighter.subs.length + CLASSES.Thief.subs.length`.

    `docs/ABILITIES.md`: append `## Retired passives (Plan 02)` — the seven deleted read sites (file + function), the Tracking `tracked` dormancy note, the fluency ceiling change, the climb/leap change, the Guard/noCrit rule for forced crits, and the descriptor field list from strike_descriptor_spec.

    Plan gate (record in the SUMMARY): `npm test` `# fail 0`; `git status --porcelain test/parity/fixtures` empty (no fixture edited in this plan); master hash unchanged; `git diff --stat -- package.json package-lock.json tools` empty; `git diff --stat <plan-01-final-commit> -- engine content src mazeworld.html docs` lists exactly engine/derived.js, engine/combat.js, engine/movement.js, engine/encounters.js, src/browser/viewModels.js, src/browser/eventNarration.js, src/browser/toasts.js, docs/ABILITIES.md. SUMMARY `## Human verification (deferred to end of run)`: nothing device-testable is reachable yet (the descriptor has no producer until Plan 03) — list ZERO items and say why; note the gameplay changes flagged in this plan's frontmatter for the milestone-close tone read. Explicit-path commits only.
  </action>
  <verify>
    <automated>node --test test/unit/identity-contract.test.js && npm test 2>&1 | grep -E "^# (pass|fail)" && git status --porcelain test/parity/fixtures | wc -l && git hash-object test/parity/prototype-master.js.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ability:sidestep' test/unit/identity-contract.test.js` >= 1; `grep -c "Language" test/unit/identity-contract.test.js` <= 1 (only inside the RESHAPED_SKILLS literal); `grep -c "RESHAPED_SKILLS" test/unit/identity-contract.test.js` >= 3
    - `grep -c "SC-4" test/unit/identity-contract.test.js` >= 1
    - `grep -c "## Retired passives" docs/ABILITIES.md` prints 1
    - `npm test` ends `# fail 0`; `git status --porcelain test/parity/fixtures` is empty; `git hash-object test/parity/prototype-master.js.txt` prints a1f4d0dc29782218d8e5aab65bc5989c33f917f0
    - the SUMMARY contains `## Human verification (deferred to end of run)`
  </acceptance_criteria>
  <done>Identity-contract proves Sidestep stacking and carries the SC-4 guard; every Fighter/Thief sub still has a code-verified good and bad; suite green; ledger extended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file -> engine | `c.timers` records (already load-tolerated by Phase 36) now change need numbers; a tampered `left`/`phase` can only lower a foe's need to 1 or shift by -2 |
| combat state -> playerStrike | `state.combat.abilityStrike` is engine-internal and transient; never read from a save |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-38-04 | Tampering | derived.js#abilityEffectActive | low | mitigate | reads only `phase === "effect" && left > 0` on a plain-object record; any other shape reads false |
| T-38-05 | Tampering | combat.js#playerStrike descriptor | low | mitigate | the descriptor is deleted after every strike; `needShift` floors at 1, `attacks` only raises via Math.max, `finishUnder`/`dmgMul` are read as numbers |
| T-38-SC | Tampering | npm/pip/cargo installs | low | accept | no package change (gate) |
</threat_model>

<verification>
- `npm test` -> `# fail 0`; parity suite green with the SAME fixture bytes Plan 01 committed
- `node --test test/unit/ability-strike.test.js` proves every descriptor field, the zero-draw invariant, and the need-shift twins
- grep gates in each task prove the eight retired reads are gone from engine/ and src/browser/
</verification>

<success_criteria>
- ROADMAP SC-4: the identity-contract suite is updated in this phase and every Fighter/Thief sub still shows one good and one bad (the SC-4 regression guard)
- CONTEXT "Dropped outright" and "Converted (passive branch deleted, active added)" are complete on the read side; "Strike-modifying abilities resolve immediately" has its seam; "party-wide need shift via a c.timers record read by foeToHitVs" is live for the hero
</success_criteria>

<output>
Create `.planning/phases/38-melee-active-abilities/38-02-SUMMARY.md` when done (include `## Human verification (deferred to end of run)` — zero items with the reason, plus the rng/serialized-field statements and the flagged gameplay changes).
</output>
