---
phase: 39-gear-magic-items-one-shot-tools
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - content/treasure-tables.js
  - content/potions.js
  - content/activations.js
  - content/index.js
  - engine/derived.js
  - engine/items.js
  - engine/movement.js
  - engine/combat.js
  - engine/character.js
  - engine/encounters.js
  - engine/saveState.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - src/browser/rail.js
  - src/browser/viewModels.js
  - test/unit/item-activation.test.js
  - test/unit/effect-expiry.test.js
  - test/unit/movement.test.js
  - test/unit/conditions.test.js
  - test/unit/items.test.js
  - test/unit/worn-slots.test.js
  - test/unit/worn-model.test.js
  - test/unit/item-wiring.test.js
  - test/unit/usable-features-audit.test.js
  - test/unit/save-validation.test.js
  - test/unit/characterSheetViewModel.test.js
  - test/parity/chargen-parity.test.js
  - test/parity/harness/comparables.js
  - test/parity/combat-parity.test.js
  - test/parity/magic-parity.test.js
  - test/parity/movement-parity.test.js
  - test/parity/FIXTURE-INVENTORY.md
  - docs/GEAR-BALANCE.md
autonomous: true
requirements: [GEAR-02]

must_haves:
  truths:
    - "Every activatable item's activation is declared in content (`act` on the row, exported through `ACTIVATION_OF` keyed by display name / potion row name) and NOT spread onto item objects — rolled cloak/jewel/potion item shapes stay byte-identical to today; only staves change shape (`charges` replaces `every: 250`)"
    - "Using an item with a duration starts ONE `c.timers['item:<key>']` record (effect → cooldown → gone); an instant cooldown item starts a cooldown record; a staff spends one of `it.charges` and runs a `c.timers['charges:<name>']` recharge countdown that refills one charge per expiry until full; a consumable starts its effect record and is consumed"
    - "The scattered counters (haste, invis, ether, acute on `c`; flightLeft/flightCooldown; potion writes to `c.might`; `it.usedAt`) no longer exist as engine state — `rollCharacter` no longer initialises them, no engine read remains, and every consumer (attacks, foe need, strike die, walls/crevices, flight, damage) reads the timers through `itemEffectActive`/`potionMight`"
    - "A use on cooldown / an empty staff is refused with a named reason carrying the squares left (`cooldown` / `recharging`) — never a silent no-op; a use never burns a cooldown or a charge when refused"
    - "The three tick sites narrate item transitions: `itemEffectFaded`, `itemCooled`, `staffRecharged`; the start of an effect narrates `itemEffectStarted` with its length"
    - "`conditionsOf(state)` emits one chip per live item effect (haste/invis/acute/ether/might/fly via the flight chip), one `itemCooldown` chip per cooling item, one `staffCharges` chip per recharging staff — all with `remaining`"
    - "Old saves load tolerantly: legacy counters and `it.usedAt` are folded into `c.timers` records deterministically and the keys deleted; a staff without `charges` gets a full pool; no card, no narration"
    - "Every parity fixture is byte-identical except through the declared retired-field carve-out (`haste/invis/ether/acute` stripped from both sides; `flightLeft/flightCooldown` engine-only strip already existed); the master hash is unchanged"
  artifacts:
    - "content/activations.js — ACTIVATION_OF (frozen, merged treasure + potion activations)"
    - "engine/derived.js — activationKeyFor, activationFor, itemTimerId, chargesTimerId, liveItemEffects, itemEffectActive, potionMight; conditionsOf item chips"
    - "engine/items.js — timer-backed itemReady/useItem, applyActivation, narrateTimerTransitions, rollStaff with charges"
    - "engine/saveState.js — foldLegacyCounters(c, steps)"
    - "test/unit/item-activation.test.js"
  key_links:
    - "engine/items.js#useItem -> engine/effects.js#startEffect/startCooldown (the ONLY writers of item:/charges: records) -> engine/derived.js#itemEffectActive (the ONLY way rules read them)"
    - "engine/movement.js per-step tick and engine/combat.js#foeTurn tail -> engine/items.js#narrateTimerTransitions (transitions become events; a charges: expiry refills the staff)"
    - "engine/saveState.js#foldLegacyCounters -> the same startEffect/startCooldown writers (old saves land on the one representation)"
---

<objective>
Land GEAR-02's engine half: ONE activation model for every item that does something when used — duration+cooldown (cloaks/jewelry), charges+recharge (staves), consumable-with-duration (potions) — all as `c.timers` records on Phase 36's `engine/effects.js`, with the scattered counters retired, every rule consumer re-pointed, the three tick sites narrating expiries, `conditionsOf` chips for effects/cooldowns/charges, tolerant old-save folding, and the parity harness carve-out for the retired chargen fields.

Purpose: the user's ruling — "Every activated item gets an effect that counts with chip … Items with charges recharge every x squares. Consumables are one time uses that effect last for x squares." — as the only rule, with no dual path.

Output: content activation declarations, engine model, narration for the new events, harness carve-out, tests. No `mazeworld.html` edits (Plan 05 renders the row states and chip copy); `src/browser/combatMenu.js` is deliberately left for Plan 05.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-CONTEXT.md
@.planning/phases/36-balance-foundation-effect-timers-small-independent-wins/36-02-SUMMARY.md
@docs/GEAR-SLOTS.md
@engine/effects.js
</context>

<standing_rules>
- Greenfield: no `if (c.timers)`-style dual paths for behaviour (the `if (c.timers)` guard around a TICK call is fine — it is the leaf module's own "no map yet" no-op, not a rules branch), no `it.usedAt`/`it.every` READS anywhere in `engine/` after this plan (`it.every` may remain on cloak/jewelry ROW data as the authored cooldown source the activation table derives `cd` from — it is never read off an item object by a rule); the two remaining `src/browser/combatMenu.js` reads and the `mazeworld.html` Gear-tab reads are Plan 05's (they still compile and merely show no countdown until then); no shell-only options.
- Fixture protocol: 39-01-PLAN.md `<fixture_divergence_protocol>`. Expected fixture regenerations this plan: NONE (no fixture calls `useItem`, no fixture carries a staff, cloak/potion item shapes are unchanged). The retired chargen fields are a HARNESS carve-out, documented in FIXTURE-INVENTORY.
- Commit convention `feat(39-03): …` / `test(39-03): …` / `docs(39-03): …` + the two attribution lines; explicit-path `git add`; never stage `store-listing/` or `tools/store-screenshots/`.
- Deferred UAT: no device steps; SUMMARY has "## Human verification (deferred to end of run)".
- Every new event type: `EVENT_NARRATION` (src/browser/eventNarration.js) + `TOAST_FOR` (src/browser/toasts.js) + a `RAIL_FAMILY` entry (src/browser/rail.js); all copy passes `node --test test/voice/safety-scan.test.js`; the coverage guards `toastsCoverage.test.js`/`formatEventsCoverage.test.js` must be green.
- Members never activate items (`alliesTurn`/`memberStrike` have no item-use branch — verified by grep of `useItem` in engine/combat.js), so member sheets get NO item timers; their passive `eff(view, …)` reads are untouched. Say so in the SUMMARY.
</standing_rules>

<artifacts_this_phase_produces>
See 39-01-PLAN.md "artifacts_this_phase_produces" for the phase-wide inventory. This plan creates every P03 entry: `content/activations.js#ACTIVATION_OF`, `TREASURE_ACTIVATION_OF`, `POTION_ACTIVATION_OF`, derived `activationKeyFor/activationFor/itemTimerId/chargesTimerId/liveItemEffects/itemEffectActive/potionMight`, items `applyActivation/narrateTimerTransitions`, saveState `foldLegacyCounters`, events `itemEffectStarted/itemEffectFaded/itemCooled/staffRecharged`, `useRefused` reason `recharging`, `conditionsOf` keys `itemCooldown`/`staffCharges`, harness `stripRetiredCounterFields`, `test/unit/item-activation.test.js`.
</artifacts_this_phase_produces>

<activation_table>
The numbers (CONTEXT Area 2 + Claude's discretion; every one goes into docs/GEAR-BALANCE.md). Cadence is squares unless stated. `cd` for treasure rows is the row's `every` unless `act.cd` is given.

**ONCE-A-DAY RULE (user, 2026-09-18, binding):** 100 squares is one day; every item must be usable at least once a day. So for every activation: `effect + cd <= 100` squares, and a staff regains a charge in `recharge <= 100` squares. Three treasure rows change to fit (edit the ROW's `every` AND its `txt` so the text stays true — greenfield, no `act.cd` override hiding a stale `every`): Amulet of Stone `every: 100`, txt "turns up to 4 squares of opponents to stone, once every 100 squares"; Cloak of Invisibility `every: 50`, txt "invisible for 50 squares, once every 100" with `act.effect: 50`; Cloak of Ether `every: 80`, txt "walk through walls, once every 100 squares" (effect 20 + cd 80). Rows that already fit keep their literals. MEASURE which fixtures roll those three items (grep the fixture JSONs for the names) and declare + regenerate exactly those; the acceptance-criteria literals below are updated to the new values.

Duration + cooldown (JEWELRY/CLOAKS rows with a `use`, plus the auto-activating Cloak of Flying):
| Row | act.kind | effect | cd (source) |
| Pendant of Fortitude | half | 0 (instant: `c.halfNext = true`) | 100 (every) |
| Amulet of Stone | stone | 0 (instant) | 100 (every, was 200 — once-a-day rule) |
| Cloak of Invisibility | invis | 50 (was 100 — once-a-day rule: 50 + 50 = one day) | 50 (every, was 100) |
| Cloak of Speed | haste | 50 | 50 (every) |
| Cloak of Ether | ether | 20 | 80 (every, was 100 — once-a-day rule: 20 + 80 = one day) |
| Cloak of Flying | fly | 20 | 50 (`act.cd: 50` — the row has no `every`; it auto-activates on a climb/gorge tile, no `use`) |

Charges + recharge (STAVES rows; `effect` only where the use has a duration):
| Row | act.kind | charges | recharge | effect |
| Rowan Staff | dome | 2 | 100 | instant |
| Birch Staff | freeze | 2 | 100 | instant |
| Walnut Staff | weaken | 2 | 80 | instant |
| Oak Staff | stone | 1 | 100 | instant |
| Crystal Staff | invis | 2 | 100 | `{ n: 1, sides: 10, bonus: 5 }` squares (canon "d10+5 squares") |
| Poplar Staff | heal | 3 | 60 | instant |
| Pine Staff | fire | 1 | 100 | instant |
| Cedar Staff | gas | 1 | 100 | instant |
(Correction recorded in the ledger: rolled staves carried `every: 250` today — one use per 250 squares, not infinite; the pools above are a deliberate mild buff to the Magic User's one real weapon; every recharge is <= 100 squares per the once-a-day rule.)

Consumables (POTIONS rows; consumed on use; no cd):
| Row n | act.kind | effect | extra |
| Speed | haste | 50 | |
| Strength | might | 25 | `might: 8` |
| Enlarge | might | 50 | `might: 4` |
| Acuteness | acute | `{ n: 1, sides: 8, bonus: 0 }` | `cadence: "rounds"` |
| Invisible | invis | 100 | (canon text says "a day"; the prototype set 100 — kept) |
Healing / Xtra Healing / Cure Poison / Cure Disease / Death: instant, no `act`.
Passive rows (Ring of Power, Gauntlet, Amulet of Light, Anklet, Helm, Bracelet of Flight, Cloak of Healing/Strength/Regeneration/Armor): no `act`.
</activation_table>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Activation declarations in content; timer-backed itemReady/useItem; staff charges</name>
  <files>content/treasure-tables.js, content/potions.js, content/activations.js, content/index.js, engine/derived.js, engine/items.js, engine/encounters.js, test/unit/item-activation.test.js</files>
  <read_first>
    - engine/effects.js (whole file — startEffect/startCooldown/tick semantics: an effect record with `cd` flips to cooldown and is deleted when the cooldown ends; `isReady` is false while ANY record exists for the id; `remaining` reads `left`)
    - content/treasure-tables.js (the `*_ROWS` + `dropSlot` + `SLOT_OF` pattern this plan extends with `act`)
    - content/potions.js (POTIONS rows: `n`, `eff`, `uses`, `txt`)
    - engine/items.js lines 95-120 (rollStaff/rollJewel/rollCloak), 880-1000 (itemReady, TARGETED_KINDS, useItem's refusal ladder and `it.usedAt = state.steps`), 1000-1139 (the effect switch and the consumption branch)
    - engine/encounters.js line 403 (the second staff construction site with `every: 250`) and 385-400 (the potion find item shape `{ kind, n, txt, eff2, uses }`)
    - engine/economy.js line 319 (the store potion item shape)
    - engine/derived.js lines 96-115 (eff two-path), 126-200 (WORN_SLOTS, slotFor, carriedItems), 282-320 (hasItemNamed, isFlying)
    - docs/GEAR-SLOTS.md §2-§3 (why `slot` is stripped off rolled items — the same reasoning governs `act`)
    - test/unit/items.test.js lines 200-250 (existing itemReady/usedAt pins to rewrite) and test/unit/worn-slots.test.js lines 405-475 (usedAt/haste pins to rewrite)
  </read_first>
  <behavior>
    - `ACTIVATION_OF["Cloak of Speed"]` deep-equals `{ kind: "haste", effect: 50, cd: 50 }`; `["Pendant of Fortitude"]` → `{ kind: "half", effect: 0, cd: 100 }`; `["Cloak of Flying"]` → `{ kind: "fly", effect: 20, cd: 50 }`; `["Pine Staff"]` → `{ kind: "fire", charges: 1, recharge: 100 }`; `["Crystal Staff"]` → `{ kind: "invis", charges: 2, recharge: 100, effect: { n: 1, sides: 10, bonus: 5 } }`; `["Speed"]` → `{ kind: "haste", effect: 50 }`; `["Strength"]` → `{ kind: "might", effect: 25, might: 8 }`; `["Acuteness"]` → `{ kind: "acute", effect: { n: 1, sides: 8, bonus: 0 }, cadence: "rounds" }`; `ACTIVATION_OF["Ring of Power"]` is undefined; the object is frozen.
    - Exported `JEWELRY`/`CLOAKS`/`STAVES` rows carry NO `act` and NO `slot` key (deepStrictEqual against the pre-phase literals for one row of each table, e.g. Cloak of Speed `{ n, eff: {}, use: "haste", every: 50, txt }`; Cloak of Ether's exported row is the NEW literal `{ n, eff: {}, use: "ether", every: 80, txt: "walk through walls, once every 100 squares" }` with no `act`/`slot`); `POTIONS[2]` (Speed) DOES carry `act` (rows are never spread into items) and the store/find potion item shapes `{ kind: "potion", n, txt, eff2, uses: 1 }` are unchanged.
    - `rollStaff(rng)` returns `{ kind: "staff", charges: <pool>, n, use, txt }` with no `every` key; `encounters.js`'s staff find produces the same shape via `rollStaff`.
    - `activationKeyFor(it)`: a potion item with `eff2: "speed"` → "Speed"; a cloak → its `n`; a staff → its `n`; a weapon → null. `itemTimerId(it)` → `"item:" + key`; `chargesTimerId(it)` → `"charges:" + key`.
    - `itemReady(state, it)`: unchanged `false` for a non-use non-potion item; `true` for a potion always; `true` for a cd item with no record, `false` while its `item:` record exists (effect OR cooldown phase); for a staff `true` iff `(it.charges ?? 0) > 0`.
    - `useItem` on a worn Cloak of Speed (worn-slot state, `{ slot: "cloak" }`): pushes `itemUsed`, then `itemEffectStarted { item: "Cloak of Speed", kind: "haste", left: 50, cadence: "squares" }`; `c.timers["item:Cloak of Speed"]` equals `{ cadence: "squares", left: 50, cd: 50, phase: "effect" }`; the character object has NO `haste` key; a second immediate use pushes `useRefused { reason: "cooldown", left: 50, phase: "effect" }` and changes nothing (rngState identical before/after).
    - `useItem` on a Pendant of Fortitude: `c.halfNext === true`, `c.timers["item:Pendant of Fortitude"]` is a cooldown record `left: 100`, no `itemEffectStarted` (effect 0), and `itemReady` is false for 100 squares.
    - `useItem` on a Pine Staff (Magic User, in combat with foes): the fire case runs exactly as today (same draws), `it.charges` goes 1 → 0, `c.timers["charges:Pine Staff"]` is a cooldown record `left: 100`; a second use is refused `useRefused { reason: "recharging", left: 100, charges: 0, max: 1 }` with no draw.
    - `useItem` on a Crystal Staff: one charge spent, one `rng.d(10)` drawn, `c.timers["item:Crystal Staff"]` effect record with `left` in 6..15 and no `cd`, `itemEffectStarted.kind === "invis"`.
    - `useItem` on a Speed potion (bag index): `itemUsed`, `itemEffectStarted` (haste, 50), `itemConsumed`, the item is gone from `c.items`, `c.timers["item:Speed"]` is an effect record with no `cd`; Strength potion → `item:Strength` 25 squares and `potionMight(c) === 8`; Acuteness → one `rng.d(8)` draw (same count as today), a `cadence: "rounds"` effect record; Healing potion → no timers key created at all (`"timers" in c` false on a fresh c).
    - Refusals before the activation (pending fight / wrongClass / notWorn / pilfer / combatOnly) still fire first and touch nothing.
  </behavior>
  <action>
    Write `test/unit/item-activation.test.js` FIRST (RED) covering the behaviours above (use `newRun(seed)` states or the minimal hand-built states other item tests use; for the worn-slot case set `c.worn = { cloak: cloakItem }` directly; stub rng with `makeRng(seed)`).

    content/treasure-tables.js: author `act` on the rows named in `<activation_table>` (module-private `*_ROWS`), rename `dropSlot` to `dropAuthored` stripping BOTH `slot` and `act` (exported tables stay byte-identical to the pre-Phase-37 literals — extend the header comment: `act` follows `slot`'s exact reasoning, never spread onto an item). Export `TREASURE_ACTIVATION_OF` (frozen; for each row with `act`: `{ kind, effect, cd: act.cd ?? row.every }` for effect/cd rows, `{ kind, charges, recharge, effect? }` for staves — normalised so consumers never see a raw `act`). content/potions.js: add `act` to the five duration potions per the table (the POTIONS rows are read field-by-field into items — `act` never leaks), export `POTION_ACTIVATION_OF` (frozen, keyed by row `n`). New `content/activations.js`: `export const ACTIVATION_OF = Object.freeze({ ...TREASURE_ACTIVATION_OF, ...POTION_ACTIVATION_OF })` with a header explaining the key convention (display name for treasure, POTIONS row `n` for potions, Plan 04 spreads tool activations in) and add it to `content/index.js`. Pure data only (content-is-pure-data test).

    engine/derived.js: add exported `activationKeyFor(it)` (potion → `POTIONS.find(p => p.eff === it.eff2)?.n ?? null`; `it.kind === "tool"` → `it.n` (Plan 04's torch is keyed "Torch"); else `it.n ?? null`), `activationFor(it)` (`ACTIVATION_OF[key]` or null), `itemTimerId(it)`, `chargesTimerId(it)`, `liveItemEffects(c)` (walks `c.timers` keys starting with `item:` whose record is `phase: "effect"` with `left > 0` and whose key maps to an activation; returns `[{ key, act, rec }]` in insertion order), `itemEffectActive(c, kind)` (any live effect with `act.kind === kind`), `potionMight(c)` (sum of `act.might` over live `might` effects). Import `remaining`/`isReady` from `./effects.js` where needed (effects.js has zero imports — no cycle). Nothing else in derived.js changes in this task (Task 2 re-points the consumers).

    engine/items.js: `rollStaff` → `Object.assign({ kind: "staff", charges: TREASURE_ACTIVATION_OF[STAVES[i].n].charges }, STAVES[i])` (single `rng.d(8)` as today); `engine/encounters.js:403` → `offerFind(state, rollStaff(rng), events)` (import rollStaff; identical draw). `itemReady(state, it)` per the behaviours (import `isReady`/`remaining` from effects.js; read the record on `state.c`). `useItem`: delete the `it.usedAt = state.steps;` line; replace the `!itemReady` block with the two named refusals (`recharging` for a staff with `left: remaining(c, chargesTimerId(it))`, `charges: it.charges ?? 0`, `max: act.charges`; `cooldown` with `left: remaining(c, itemTimerId(it))`, `phase: rec.phase`) — a non-use non-potion item stays the silent no-op. Group the retired counter cases as `case "strength": case "enlarge": case "speed": case "haste": case "acute": case "invis": case "ether": break;` with a comment that `applyActivation` below carries them. After the switch (the `death` case has already returned; the `default` fizzle sets a local `fizzled = true` and skips activation), call a new module-private `applyActivation(state, it, rng, events)`: `act = activationFor(it)`; if null → nothing; if `act.charges !== undefined` → `it.charges = Math.max(0, (Number.isInteger(it.charges) ? it.charges : act.charges) - 1)`, and if `isReady(c, chargesTimerId(it))` → `startCooldown(c, chargesTimerId(it), { squares: act.recharge })`; then the effect: `left = typeof act.effect === "object" ? rollDice(rng, act.effect) : act.effect`; if `left > 0` → `startEffect(c, itemTimerId(it), act.cd ? { [cadence]: left, cd: act.cd } : { [cadence]: left })` (cadence = `act.cadence ?? "squares"`) and push `itemEffectStarted { item: it.n, kind: act.kind, left, cadence }`; else if `act.cd` → `startCooldown(c, itemTimerId(it), { squares: act.cd })`. Keep the consumption branch (`it.kind === "potion" || it.uses === 1`) after it. The Cloak of Flying has no `use`, so `useItem` never reaches it — its record is started by movement.js (Task 2).

    Update `test/unit/items.test.js` (itemReady/usedAt pins → timer pins) and `test/unit/worn-slots.test.js` (the two usedAt/haste assertions → `c.timers` assertions) to the new model; `test/unit/item-combat-gate.test.js`/`identity-world.test.js`'s "usedAt never set on a refusal" assertions stay true and may stay.
  </action>
  <verify>
    <automated>node --test test/unit/item-activation.test.js test/unit/items.test.js test/unit/worn-slots.test.js test/determinism/content-is-pure-data.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./content/index.js').then(m=>console.log(Object.keys(m.ACTIVATION_OF).length, JSON.stringify(m.ACTIVATION_OF['Cloak of Speed']), JSON.stringify(m.CLOAKS[3])))"` prints `19 {"kind":"haste","effect":50,"cd":50} {"n":"Cloak of Speed","eff":{},"use":"haste","every":50,"txt":"double attacks, once every 50 squares"}` (6 treasure cd rows + 8 staves + 5 potions = 19; the exported cloak row is byte-identical).
    - `grep -c "usedAt" engine/items.js` prints 0. <!-- planner-discipline-allow: usedAt -->
    - `grep -c "every: 250" engine/items.js engine/encounters.js` prints 0 for both files. <!-- planner-discipline-allow: every: 250 -->
    - `grep -c "export function itemEffectActive\|export function potionMight\|export function activationFor\|export function itemTimerId\|export function chargesTimerId\|export function liveItemEffects\|export function activationKeyFor" engine/derived.js` prints 7.
    - `grep -c 'reason: "recharging"' engine/items.js` prints 1; `grep -c 'type: "itemEffectStarted"' engine/items.js` prints 1.
    - `node --test test/unit/item-activation.test.js` `# fail 0` with at least 12 tests.
  </acceptance_criteria>
  <done>Activations are content data, items carry no cooldown state, `useItem`/`itemReady` run on `c.timers`, staves have charge pools, and the tests prove the three classes of activation plus the refusal ladder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire the counters everywhere; tick transitions become events; conditionsOf chips; tolerant save fold; narration</name>
  <files>engine/derived.js, engine/movement.js, engine/combat.js, engine/character.js, engine/saveState.js, engine/items.js, src/browser/eventNarration.js, src/browser/toasts.js, src/browser/rail.js, src/browser/viewModels.js, test/unit/item-activation.test.js, test/unit/effect-expiry.test.js, test/unit/movement.test.js, test/unit/conditions.test.js, test/unit/worn-model.test.js, test/unit/item-wiring.test.js, test/unit/save-validation.test.js, test/unit/characterSheetViewModel.test.js</files>
  <read_first>
    - engine/derived.js lines 310-320 (isFlying), 351-435 (conditionsOf), 455-465 (strikeDie's acute line), 598-690 (foeToHitVs + foeToHitBreakdown invis lines), 693-726 (weaponDamage's might line)
    - engine/movement.js lines 60-70 (FLIGHT_* constants), 135-160 (the flying/ether branch), 288-348 (the darkness tick, the four counter decrements, the acute step tick, the cloak heal/regen ticks, the flight tick, the `if (c.timers) tickSquares(c, 1)` call)
    - engine/combat.js lines 505-512 (haste attacks), 1210-1235 (endCombat's acute reset + clearRoundTimers), 2395-2435 (foeTurn tail: the acute round tick and `if (c.timers) tickRounds(c)`)
    - engine/character.js lines 495-540 (rollCharacter's `haste: 0, invis: 0, ether: 0, acute: 0` and `flightLeft: 0, flightCooldown: 0` with their comment blocks)
    - engine/saveState.js lines 139-215 (migrateCarry/clearFoeEffect/clearStaleTimers/sanitizeWorn — the tolerant-load helper shape) and 296-310 / 380-395 (the two chains to extend)
    - src/browser/eventNarration.js lines 400-415 and 690-745 (acuteFaded, darkness lines, itemUsed/useRefused/itemConsumed); src/browser/toasts.js lines 190-205 (silent list), 1150-1160, 1400-1440 (the same entries); src/browser/rail.js lines 140-230 (RAIL_FAMILY and the feature-name map)
    - src/browser/viewModels.js lines 36-60 (damageBracket's `c.might` line)
    - test/unit/effect-expiry.test.js, test/unit/movement.test.js lines 320-400 and 610-630, test/unit/conditions.test.js, test/unit/worn-model.test.js line 195, test/unit/item-wiring.test.js, test/unit/save-validation.test.js (the pins to rewrite)
    - test/unit/toastsCoverage.test.js and test/unit/formatEventsCoverage.test.js (how the guards derive the event set)
  </read_first>
  <behavior>
    - `newRun(1).c` has none of the keys haste/invis/ether/acute/flightLeft/flightCooldown and no `timers` key; `newRun(1).c.might === 0` (kept — spells and insanity write it).
    - After `useItem` of a Speed potion, 50 moves later `c.timers["item:Speed"]` is gone and the events of the 50th step include `itemEffectFaded { item: "Speed", kind: "haste" }`; with a Cloak of Speed the 50th step flips the record to `phase: "cooldown", left: 50` with the same `itemEffectFaded`, and 50 more steps later `itemCooled { item: "Cloak of Speed" }` fires and the record is gone.
    - `attacks` in playerStrike is 2 while `itemEffectActive(c, "haste")`; `foeToHitVs` is 1 while a live invis effect; `strikeDie` is 6 while a live acute effect; `weaponDamage` adds 8 while a live Strength effect and 12 with Strength + Enlarge; the climb/gorge block phases through while a live ether effect; `damageBracket` (viewModels) adds `potionMight(c)`.
    - Acuteness: a `cadence: "rounds"` record ticks in `foeTurn`'s tail (not per step) and is cleared at `endCombat` by `clearRoundTimers` — the 30-move draw-count invariance test from Phase 36 still holds with an item record planted.
    - Cloak of Flying: stepping onto a climb tile while carrying it and no `item:Cloak of Flying` record → `flownOver`, an effect record `{ squares 20, cd 50 }` and `itemEffectStarted { kind: "fly", left: 20 }`; during the effect further hazards are flown over with no new record; during the cooldown `isFlying` is false (the roll runs); with the Bracelet of Flight no record is ever created.
    - Staff recharge: a Pine Staff at 0 charges with a `charges:Pine Staff` record of `left: 1` → the next step pushes `staffRecharged { item: "Pine Staff", charges: 1, max: 1 }`, sets `it.charges = 1`, and no new record is started; a Poplar Staff (max 3) at 1 charge → refill to 2 AND a fresh 60-square record is started; a staff that was dropped (not carried) → the record simply disappears, no event.
    - `conditionsOf`: a live Cloak of Speed effect → `{ key: "haste", polarity: "good", remaining: 23, cadence: "squares", source: "Cloak of Speed" }`; its cooldown phase → `{ key: "itemCooldown", polarity: "good", item: "Cloak of Speed", remaining: 41 }`; a recharging Pine Staff → `{ key: "staffCharges", polarity: "good", item: "Pine Staff", charges: 0, max: 1, remaining: 17 }`; a live Strength effect → `{ key: "might", polarity: "good", remaining, cadence, source: "Strength", might: 8 }` while the spell's `c.might > 0` still emits the countless `{ key: "might", polarity: "good" }` chip; the flight chip keeps its `always/charged/cooldown/ready` sub-states read from the record; bad conditions unchanged; order: live effects (insertion order), spell might, ward, flight, cooldowns, charges, then the bad block.
    - `foldLegacyCounters`: a save object whose `c` has `haste: 30, invis: 0, acute: 3, flightLeft: 7, flightCooldown: 0` and a bag cloak `{ …Cloak of Ether, usedAt: 10 }` at `steps: 40` rehydrates to a `c` with none of the six keys, `c.timers["item:Speed"] = { squares, left 30, effect }`, `c.timers["item:Acuteness"]` rounds effect 3 (then cleared by clearStaleTimers ONLY if the fold runs before it — the fold runs AFTER clearStaleTimers so the rounds record survives the load), `c.timers["item:Cloak of Flying"]` effect 7 cd 50, `c.timers["item:Cloak of Ether"]` cooldown left 50 (80 − (40 − 10)), the cloak item without `usedAt`; a legacy staff `{ …Pine Staff, every: 250, usedAt: 5 }` → no `every`, no `usedAt`, `charges: 1`; a fresh `newRun` round-trips deepStrictEqual (nothing to fold, no key injected).
    - Every new event type has EVENT_NARRATION + TOAST_FOR + RAIL_FAMILY entries; the `acuteFaded` entries are deleted from both tables (no emitter remains); the `useRefused` cooldown/recharging lines read in the Phase 38 refusal register.
  </behavior>
  <action>
    Extend `test/unit/item-activation.test.js` and rewrite the listed pins (RED first).

    engine/derived.js: `isFlying` → Bracelet true; else Cloak of Flying carried (`hasItemNamed`) && (`itemEffectActive(c, "fly") || isReady(c, "item:Cloak of Flying")`). `strikeDie` → `itemEffectActive(c, "acute")`. `foeToHitVs`/`foeToHitBreakdown` → `itemEffectActive(c, "invis")` (breakdown name stays "invisible"). `weaponDamage` → keep `if (c.might) d += c.might;` and add `d += potionMight(c);` right after. `conditionsOf` → rewrite the good block per the behaviours (import `remaining` from effects.js; read `ACTIVATION_OF` for `max` charges and the staff's current `charges` via `carriedItems(c)`); update the JSDoc descriptor list.

    engine/movement.js: delete `FLIGHT_CHARGE_SQUARES`/`FLIGHT_COOLDOWN_SQUARES` and their comment; in the flying branch, when not the Bracelet and `!itemEffectActive(c, "fly")`, `startEffect(c, "item:Cloak of Flying", { squares: act.effect, cd: act.cd })` from `ACTIVATION_OF["Cloak of Flying"]` and push `itemEffectStarted`; `else if (state.c.ether > 0)` → `else if (itemEffectActive(state.c, "ether"))`. Delete the four counter decrements, the acute step tick, and the flight tick block (with their comments). Replace `if (c.timers) tickSquares(c, 1);` with `if (c.timers) narrateTimerTransitions(state, tickSquares(c, 1), events);` (import from items.js; movement already imports encounters/combat, items imports neither movement nor encounters at module-eval time — see items.js's header on hoisted-function cycles). Update the file header's counter list.

    engine/items.js: add exported `narrateTimerTransitions(state, transitions, events)`: for each `{ id, from, to }`: `item:` prefix → key = id after the prefix; `from === "effect"` → push `itemEffectFaded { item: key, kind: ACTIVATION_OF[key]?.kind ?? null }`; `from === "cooldown"` → push `itemCooled { item: key }`; `charges:` prefix (always cooldown → null) → find the staff `it = carriedItems(c).find(x => x.n === key)`; if found and `it.charges < max` → `it.charges++`, push `staffRecharged { item: key, charges: it.charges, max }`, and if `it.charges < max` → `startCooldown(c, id, { squares: recharge })`; `ability:` ids are ignored (Phase 38 narrates nothing for them). Pure bookkeeping, zero rng.

    engine/combat.js: `if (c.haste > 0)` → `if (itemEffectActive(c, "haste"))`; delete the endCombat acute reset lines (clearRoundTimers already clears the rounds record); delete the foeTurn acute round tick; `if (c.timers) tickRounds(c);` → `if (c.timers) narrateTimerTransitions(state, tickRounds(c), events);`. Member ticks (`tickRounds(s)`) stay as they are (members carry no item timers).

    engine/character.js: delete `haste: 0, invis: 0, ether: 0, acute: 0,` and `flightLeft: 0, flightCooldown: 0,` plus the flight comment block from `rollCharacter`; leave `might: 0`. Add a one-line comment naming Phase 39 as the retirement.

    engine/saveState.js: add `foldLegacyCounters(c, steps)` per the behaviours (import startEffect/startCooldown from effects.js and `activationFor`/`itemTimerId` from derived.js; walk `c.items` and `Object.values(c.worn || {})`; items first, counters second so an active effect wins over a folded cooldown for the same id; clamp a tampered `it.charges` into `[0, max]`; always delete the six legacy keys when present; never inject `timers` when nothing folds). Wire it as the LAST step of both chains (after `ensureCharacterAbilities`) passing `steps` (`obj.steps ?? 0`).

    src/browser: EVENT_NARRATION/TOAST_FOR/RAIL_FAMILY entries — `itemEffectStarted` (kind-keyed lines: haste "Double attacks for {n} squares."; invis "Unseen for {n} squares. They swing at where you were."; ether "{n} squares of walking through stone."; acute "You strike on a d6 for {n} rounds."; might "+{might} damage for {n} squares. Hit things."; fly "Twenty squares of not touching the floor."; default "{item}: {n} squares."), `itemEffectFaded` ("{item} wears off." — beat tone), `itemCooled` ("{item} is ready again."), `staffRecharged` ("{item} hums. {charges}/{max}."); `useRefused` `cooldown` → "{item}: {left} squares. It is not a vending machine." and `recharging` → "{item}: {left} squares to the next charge. Patience is also a spell."; delete both `acuteFaded` entries. RAIL_FAMILY: itemEffectStarted `{ icon: "◇", title: "IN EFFECT", tone: "good" }`, itemEffectFaded `{ icon: "◇", title: "WORN OFF", tone: "odd" }`, itemCooled `{ icon: "◇", title: "READY", tone: "good" }`, staffRecharged `{ icon: "◇", title: "CHARGED", tone: "good" }`. viewModels `damageBracket`: add `flat += potionMight(c)` after the `c.might` line. Run the voice scan.
  </action>
  <verify>
    <automated>node --test test/unit/item-activation.test.js test/unit/effect-expiry.test.js test/unit/movement.test.js test/unit/conditions.test.js test/unit/worn-model.test.js test/unit/item-wiring.test.js test/unit/save-validation.test.js test/unit/effects.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/voice/safety-scan.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.haste\b\|\.invis\b\|\.ether\b\|\.acute\b\|flightLeft\|flightCooldown" engine/*.js | grep -v "^\S*:\s*//\|^\S*:\s*\*\|saveState.js" | wc -l` prints 0 (the only surviving mentions are inside `engine/saveState.js#foldLegacyCounters` and comments). <!-- planner-discipline-allow: flightLeft --> <!-- planner-discipline-allow: flightCooldown -->
    - `grep -c "narrateTimerTransitions" engine/movement.js engine/combat.js` prints 2 for each file (import + call) and `grep -c "export function narrateTimerTransitions" engine/items.js` prints 1.
    - `grep -c "export function foldLegacyCounters\|function foldLegacyCounters" engine/saveState.js` is at least 1 and `grep -c "foldLegacyCounters(" engine/saveState.js` is at least 3 (definition + two chains).
    - `grep -c "acuteFaded" src/browser/eventNarration.js src/browser/toasts.js` prints 0 for both. <!-- planner-discipline-allow: acuteFaded -->
    - `grep -c "itemEffectStarted\|itemEffectFaded\|itemCooled\|staffRecharged" src/browser/rail.js` is at least 4.
    - `node -e "import('./engine/engine.js').then(m=>{const c=m.newRun(1).c; console.log(['haste','invis','ether','acute','flightLeft','flightCooldown','timers'].filter(k=>k in c).length, c.might)})"` prints `0 0`.
    - `node --test test/voice/safety-scan.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js` `# fail 0`.
  </acceptance_criteria>
  <done>No engine rule reads a retired counter; every consumer reads the timers; the tick sites narrate; chips enumerate effects/cooldowns/charges; old saves fold; the new events are narrated in all three tables and pass the voice scan.</done>
</task>

<task type="auto">
  <name>Task 3: Parity carve-out for the retired chargen fields, fixture check, FIXTURE-INVENTORY + ledger sections, plan gate</name>
  <files>test/parity/chargen-parity.test.js, test/parity/harness/comparables.js, test/parity/combat-parity.test.js, test/parity/magic-parity.test.js, test/parity/movement-parity.test.js, test/unit/usable-features-audit.test.js, test/parity/FIXTURE-INVENTORY.md, docs/GEAR-BALANCE.md</files>
  <read_first>
    - test/parity/chargen-parity.test.js lines 30-115 (CHARACTER_FIELDS and the two destructures)
    - test/parity/harness/comparables.js lines 100-290 (the strip helpers; the three `rest.c = …` chains at 320, 412, 872)
    - test/parity/combat-parity.test.js, magic-parity.test.js, movement-parity.test.js — grep `function comparable` for the local duplicates' destructure lists
    - test/unit/usable-features-audit.test.js line 517 (the chargen field list)
    - test/parity/FIXTURE-INVENTORY.md (the Phase 39 GEAR-01 section Plan 01 appended and its `<!-- 39-03 -->` anchor)
    - docs/GEAR-BALANCE.md (Plan 02's placeholder heading "## Item activation model (GEAR-02) — Plan 03")
  </read_first>
  <action>
    1. comparables.js: add `stripRetiredCounterFields(c)` — destructures `haste, invis, ether, acute` off `c` (from WHICHEVER side carries them: the frozen prototype initialises all four to 0 at chargen and no fixture ever raises them; the engine no longer has the fields at all — a permanent, deliberate retirement, documented like `stripNameField`); wire it as the innermost wrapper (around `stripBagField(...)`) in all three `rest.c` chains; keep `stripFlightFields` (now a no-op on the engine side, still needed for nothing on the prototype — leave it, with a one-line note). Extend the three local `comparable()` duplicates' destructures with `haste, invis, ether, acute`. chargen-parity.test.js: remove `"haste", "invis", "ether", "acute", "flightLeft", "flightCooldown"` from CHARACTER_FIELDS (with a Phase 39 comment), drop `flightLeft, flightCooldown` from the engine destructure, and destructure `haste, invis, ether, acute` off `protoC` before the diff. usable-features-audit.test.js: update its chargen field list the same way.

    2. Run `npm test`. Expected: every parity fixture byte-identical — no fixture regeneration. If any scenario fails, follow the fixture protocol (measure, declare, regenerate only that one) and say why in the SUMMARY; the expected answer is "none moved".

    3. FIXTURE-INVENTORY.md: replace the `<!-- 39-03 -->` anchor with `## Phase 39: item activation model (GEAR-02) — retired-field carve-out, zero fixture moves`: what was retired, why the prototype's `haste/invis/ether/acute` zeros are stripped from both sides (a harness carve-out, not a regeneration), why no fixture moved (no fixture calls `useItem`; no fixture carries a staff; cloak/jewel/potion item shapes are byte-identical because `act` is stripped like `slot`), and the standing rule that a future fixture that drinks a potion must be declared as an action-path record (the effect now lives in `c.timers`, which is carved out).

    4. docs/GEAR-BALANCE.md: fill "## Item activation model (GEAR-02) — Plan 03" with: the user's ruling verbatim; the full `<activation_table>` (three tables) with the canon/prototype source of each number; the representation (`item:<key>` / `charges:<name>` records, effects.js semantics, `it.charges` on the staff, why the count rides the item — it travels with the staff when dropped/sold — while the countdown rides the character); the retired fields and where each consumer now reads (a table: field → consumer file:function → new read); the refusal vocabulary rows (`cooldown` with phase, `recharging`); the new events; the tolerant-load fold rules including the one accepted loss (a rounds-cadence Acuteness record is dropped by a reload's `clearStaleTimers`, as every combat-scoped record is); the members note (they never activate items); the `txt` note (row flavor text is left byte-identical — seven fixtures carry a starting cloak's `txt`; the Gear tab's row state renders the numbers instead, Plan 05).

    5. PLAN GATE: `npm test` → `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` empty; `git diff --stat -- mazeworld.html` empty; `npm run build:www` exit 0 (src/browser changed). Record the numbers.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && git status --porcelain test/parity/fixtures && npm run build:www</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` prints `# fail 0`.
    - `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `git status --porcelain test/parity/fixtures` prints nothing (no fixture regenerated by this plan).
    - `grep -c "function stripRetiredCounterFields" test/parity/harness/comparables.js` prints 1 and `grep -c "stripRetiredCounterFields(" test/parity/harness/comparables.js` prints 4 (definition + three chains).
    - `grep -c '"haste"' test/parity/chargen-parity.test.js` prints 0 in CHARACTER_FIELDS context — verify with `sed -n '/const CHARACTER_FIELDS/,/^\];/p' test/parity/chargen-parity.test.js | grep -c '"haste"\|"acute"\|"flightLeft"'` printing 0. <!-- planner-discipline-allow: "haste" -->
    - `grep -c "## Phase 39: item activation model" test/parity/FIXTURE-INVENTORY.md` prints 1; `grep -c "Crystal Staff" docs/GEAR-BALANCE.md` is at least 1.
    - `git diff --stat -- mazeworld.html` empty; `npm run build:www` exits 0.
  </acceptance_criteria>
  <done>The harness carves out the retired fields, no fixture moved, the inventory and ledger record GEAR-02's engine half, and the gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save JSON → engine | legacy counters / `usedAt` / tampered `it.charges` / tampered `c.timers` cross into the one representation |
| action payload → useItem | `{ i }` / `{ slot }` refs already validated; the activation never trusts item fields beyond `n`/`eff2`/`kind`/`charges` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-06 | Tampering | `it.charges` on a saved staff (999 / -1 / "x") | medium | mitigate | `foldLegacyCounters` clamps to an integer in `[0, max]`; `applyActivation` re-reads with `Number.isInteger` guard; `itemReady` treats a non-integer as 0 |
| T-39-07 | Tampering | `c.timers["item:<unknown>"]` from a save | low | mitigate | `liveItemEffects` ignores keys with no activation; `narrateTimerTransitions` emits `kind: null` and never dereferences a missing activation |
| T-39-08 | Denial of service | a refused use spending a charge / starting a cooldown | medium | mitigate | every refusal returns before `applyActivation`; item-activation.test.js asserts rngState/charges/timers unchanged on refusal |
| T-39-09 | Information disclosure | none — no new data leaves the device | low | accept | fully offline |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan |
</threat_model>

<verification>
- `node --test test/unit/item-activation.test.js` green; the whole suite green; master hash unchanged; no fixture changed; build:www ok.
- SUMMARY "Human verification (deferred to end of run)": (1) drink a Speed potion from the Gear tab — the Oracle shows "You use Speed potion." then "Double attacks for 50 squares." and (after Plan 05) a Hasted chip counting down; (2) use a worn Cloak of Speed twice — the second tap reads "Cloak of Speed: 50 squares. It is not a vending machine."; (3) a Magic User uses a Pine Staff in a fight, then again — "Pine Staff: 100 squares to the next charge. Patience is also a spell."; (4) resume a pre-Phase-39 save mid-run — no card, no crash, worn cloaks still usable.
</verification>

<success_criteria>
- ROADMAP SC-2 (engine half): use → effect record → cooldown record, with `conditionsOf` exposing effect-remaining / cooldown-remaining / charges; the chip rendering and row states land in Plan 05.
</success_criteria>

<output>
Create `.planning/phases/39-gear-magic-items-one-shot-tools/39-03-SUMMARY.md` when done (38-05-SUMMARY.md shape; "## Human verification (deferred to end of run)" required; note the members-never-use-items finding and the Acuteness-on-reload accepted loss).
</output>
