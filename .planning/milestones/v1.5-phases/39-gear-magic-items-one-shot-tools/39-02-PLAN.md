---
phase: 39-gear-magic-items-one-shot-tools
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - tools/lib/tuning-bot.mjs
  - test/unit/bot-buy-policy.test.js
  - docs/GEAR-BALANCE.md
  - docs/class-pass/v15-gear-smoke.json
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [GEAR-01]

must_haves:
  truths:
    - "The tuning bot, standing in a store with gold, buys the class-legal weapon line with the highest positive expected-strike delta it can afford (reserve 50 wilmst) and the best class-legal armor upgrade it can afford, preferring bulk <= 1 as a Thief — then leaves; a bot with no upgrade or no gold leaves exactly as before"
    - "docs/GEAR-BALANCE.md exists with the GEAR-01 before/after weapon table (dice, mean, need, crit, cost, cls, band), the armor table (AR/WP/bulk/cost), the per-class best-pick analysis before (one pick) and after (a choice per band, with expected-strike numbers at levels 1/3/5), the need-vs-die equivalence note, the Soldier-crit correction, and a bot smoke readout against the v1.5 BEFORE pin"
    - "The smoke is 143 cells x 3 seeds (429 runs) via tools/tune-classes.mjs, committed as docs/class-pass/v15-gear-smoke.json, and the ledger states it is a proxy — NOT a retune and NOT the v1.5 AFTER matrix (Phase 42)"
  artifacts:
    - "tools/lib/tuning-bot.mjs — chooseStorePurchase(state, ctx) + the (l) store step rewrite"
    - "test/unit/bot-buy-policy.test.js"
    - "docs/GEAR-BALANCE.md"
    - "docs/class-pass/v15-gear-smoke.json"
  key_links:
    - "tools/lib/tuning-bot.mjs#chooseStorePurchase -> engine/derived.js#expectedStrike + engine/items.js#canEquipWeapon/canEquipArmor + engine/derived.js#armorBulk (the bot reads the SAME rule the engine's takeItem uses, so a buy it chooses is never refused notBetter)"
    - "docs/GEAR-BALANCE.md pooled-rollup table -> docs/class-pass/v15-before.json rollups.pooled vs docs/class-pass/v15-gear-smoke.json rollups.pooled"
---

<objective>
Make the tuning bot actually buy and equip under the new weapon/armor axes (today it leaves every store without buying), run a short class-matrix smoke against the v1.5 BEFORE pin, and write `docs/GEAR-BALANCE.md` — the GEAR-01 before/after ledger ROADMAP SC-1 requires.

Purpose: an honest ledger needs a bot that plays the new rules; a rebanding nobody measures is a guess. This plan closes GEAR-01.

Output: bot buy/equip policy + unit tests, the smoke JSON, the ledger (GEAR-01 sections; Plans 03–05 append GEAR-02/GEAR-05 sections to the same file).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-CONTEXT.md
@.planning/phases/39-gear-magic-items-one-shot-tools/39-01-SUMMARY.md
@docs/CLASS-PASS.md
@docs/ABILITIES.md
</context>

<standing_rules>
- Same as Plan 01: greenfield, explicit-path `git add` only (never `store-listing/` or `tools/store-screenshots/`), commit convention `feat(39-02): …`/`test(39-02): …`/`docs(39-02): …` with the two attribution lines, no device steps, SUMMARY carries "## Human verification (deferred to end of run)".
- The v1.5 BEFORE pin is the committed JSON (`docs/class-pass/v15-before*.json`) — editing `tools/` this phase is expected and is NOT a pin violation (CONTEXT code_context, last bullet). Do not touch `docs/class-pass/v15-before.json`/`v15-before-depth20.json`.
- Bot TACTICS for popping items (when to use a cloak/potion/staff) stay Phase 42 — this plan adds only the buy/equip decision in a store.
- No `mazeworld.html` edits.
</standing_rules>

<artifacts_this_phase_produces>
See 39-01-PLAN.md "artifacts_this_phase_produces" for the phase-wide inventory. This plan creates: `tools/lib/tuning-bot.mjs#chooseStorePurchase(state, ctx)` (exported, pure), the rewritten store step in `decideAction`, `test/unit/bot-buy-policy.test.js`, `docs/GEAR-BALANCE.md` (sections: Canon change · Weapons before/after · Armor before/after · Per-class picks · Need vs die · Corrections · Bot policy · Smoke · Declared divergences (GEAR-01) · placeholders for GEAR-02/GEAR-05/Requirements map), `docs/class-pass/v15-gear-smoke.json`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bot store buy/equip policy on expectedStrike + armor bulk preference</name>
  <files>tools/lib/tuning-bot.mjs, test/unit/bot-buy-policy.test.js</files>
  <read_first>
    - tools/lib/tuning-bot.mjs lines 370-520 (decideAction's out-of-combat chain; the `if (state.store) return { type: "leaveStore" }` step (l); the module's import list at the top of the file)
    - engine/derived.js (Plan 01's `expectedStrike(c, base, bonus, prof)`, `armorBulk(c)`, `classNeed`)
    - engine/items.js (`canEquipWeapon(c, it)`, `canEquipArmor(c, it)`, `weaponUpgradeDelta`, `armorUpgradeDelta`, `takeItem`'s notBetter rule)
    - engine/economy.js lines 306-420 (openStore's stock line shape `{ n, sub, cost, effectId, effectParams: { item }, sold }`; `buyWeapon`/`buyArmor` effectIds) and 449-478 (buyFrom's gold/bagFull refusals)
    - test/unit/tuning-bot*.test.js if present (`ls test/unit | grep -i "bot\|tune"`) for the existing decideAction test style (state fixtures, ctx shape)
  </read_first>
  <behavior>
    - `chooseStorePurchase(state, ctx)` returns `{ type: "buyItem", idx }` or `null`. It never returns an index whose line is `sold`, whose `cost > c.gold - 50`, or that fails the engine's own legality/upgrade rule.
    - Weapons: among unsold `buyWeapon` lines, legal via `canEquipWeapon(c, item)`, affordable, and `weaponUpgradeDelta(c, item) > 0` — pick the highest `expectedStrike(c, item.base, item.bonus||0, 0)`; tie → lower cost.
    - Armor: among unsold `buyArmor` lines, legal via `canEquipArmor(c, item)`, affordable, `armorUpgradeDelta(c, item) > 0`; a Thief skips any line whose `armorBulk({ armor: item.armor }) > 1`; pick the highest `item.ar`.
    - Order: weapon first, then armor (a second decideAction call after the first buy resolves the armor since the weapon line is now `sold`); `null` when nothing qualifies → `decideAction` returns `{ type: "leaveStore" }` exactly as before.
    - Premium lines (`buyPremium`) are considered as weapons/armor by their `effectParams.item.kind` with the same rules (they are strictly better versions).
    - Test cases: (a) a level-1 Human Fighter with a Club and 1000 gold in a store stocking Flail 250 / Rapier 200 / Studded 750 buys the weapon with the higher expectedStrike first (Flail at level 1 — pin the number from Plan 01's sample: Flail 2.125 vs Rapier 1.80), then on the next call buys Studded, then leaves; (b) a Thief with 5000 gold offered Plate (bulk 2) leaves it and buys Studded (bulk 1) if offered; (c) a Magic User offered a Flail (F/T only) never buys it; (d) gold 60 → nothing affordable → leaveStore; (e) a bot already wielding a Bardiche offered a Rapier at level 1 → delta computed by the engine rule; if `weaponUpgradeDelta <= 0` it leaves.
    - `decideAction` with `state.store` non-null returns `chooseStorePurchase(...) ?? { type: "leaveStore" }`; every other branch of decideAction is unchanged (pin one in-combat and one exploration decision to prove no drift).
  </behavior>
  <action>
    Write `test/unit/bot-buy-policy.test.js` FIRST (RED) using minimal hand-built states (mirror the existing bot tests' fixture style; a `store.stock` array of plain lines is enough — `chooseStorePurchase` must not need `openStore`). Then in `tools/lib/tuning-bot.mjs` add exported `chooseStorePurchase(state, ctx)` (pure read of `state.store.stock`, `state.c`; imports `expectedStrike`, `armorBulk` from `../../engine/derived.js` and `canEquipWeapon`, `canEquipArmor`, `weaponUpgradeDelta`, `armorUpgradeDelta` from `../../engine/items.js` — check the file's existing relative import paths and match them), with a `GOLD_RESERVE = 50` constant and a doc comment stating the rule and that it deliberately reads the engine's own `weaponUpgradeDelta` so a chosen buy is never `notBetter`-refused. Replace step (l) `if (state.store) return { type: "leaveStore" };` with `if (state.store) return chooseStorePurchase(state, ctx) ?? { type: "leaveStore" };` and update the decideAction doc comment's (l) line. No other bot behaviour changes (item use tactics are Phase 42).
  </action>
  <verify>
    <automated>node --test test/unit/bot-buy-policy.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function chooseStorePurchase" tools/lib/tuning-bot.mjs` prints 1.
    - `grep -c "chooseStorePurchase(state, ctx) ?? { type: \"leaveStore\" }" tools/lib/tuning-bot.mjs` prints 1.
    - `grep -c "GOLD_RESERVE" tools/lib/tuning-bot.mjs` is at least 2.
    - `node --test test/unit/bot-buy-policy.test.js` prints `# fail 0` with at least 6 tests.
    - `node tools/tune-classes.mjs --sub Knight --race Human --seeds 2 --max-actions 800` exits 0 and prints a cell row (the bot still completes runs).
  </acceptance_criteria>
  <done>The bot buys the best affordable weapon and armor by the engine's own expected-strike/bulk rules and leaves otherwise; tests pin the policy.</done>
</task>

<task type="auto">
  <name>Task 2: 429-run class-matrix smoke, docs/GEAR-BALANCE.md ledger (GEAR-01 half), REQUIREMENTS.md, plan gate</name>
  <files>docs/GEAR-BALANCE.md, docs/class-pass/v15-gear-smoke.json, .planning/REQUIREMENTS.md</files>
  <read_first>
    - docs/CLASS-PASS.md lines 1591-1645 ("## v1.5 BEFORE" — the pin's provenance paragraph and the pooled-rollup table shape to mirror)
    - docs/ABILITIES.md lines 1-70 and 543-570 (ledger header/section conventions and the Requirements-map table shape)
    - docs/GEAR-SLOTS.md §1 (the "Declared canon change" register)
    - tools/tune-classes.mjs lines 1-60 (CLI flags: --seeds, --workers, --max-actions, --out, --json; the output JSON shape `meta`/`cells`/`rollups.pooled`)
    - content/weapons.js and content/armors.js as landed by Plan 01; `git show d117782:content/weapons.js` for the BEFORE table (the last pre-phase commit)
    - .planning/phases/39-gear-magic-items-one-shot-tools/39-01-SUMMARY.md (the declared fixture records to cross-reference)
    - .planning/REQUIREMENTS.md lines 29-36 and 113-115 (GEAR-01 line and traceability row)
  </read_first>
  <action>
    1. SMOKE. Run `node tools/tune-classes.mjs --seeds 3 --workers 4 --max-actions 5000 --out docs/class-pass/v15-gear-smoke.json` (143 cells × 3 seeds = 429 runs; expect ~1–2 minutes — the 40-seed BEFORE took 14 min). Then print the pooled rollups of both files with `node -e "for (const f of ['docs/class-pass/v15-before.json','docs/class-pass/v15-gear-smoke.json']) { const j = JSON.parse(require('fs').readFileSync(f,'utf8')); console.log(f, JSON.stringify(j.rollups.pooled)); }"` and the per-class rollups if the JSON carries them (`rollups.byClass` or equivalent — read `meta`/`rollups` keys first). Also print the number of `bought` purchases the bot made if the cell rows expose it; if they do not, add nothing — note it as a Phase 42 harness ask.

    2. LEDGER. Create `docs/GEAR-BALANCE.md` with this structure (Plans 03–05 append; never rewrite wholesale):
       - Header (Phase, Date, one-paragraph purpose) like ABILITIES.md.
       - `## Canon change (GEAR-01)`: weapons gain `need` (-2..+1, the to-hit modifier applied to the NEED — a LOW range where a strike lands on `roll <= need`; a light weapon's +1 makes more rolls land, a heavy weapon's -1 fewer; floored at 1) and `crit` (1|2 — precise blades double on a 1 or 2); armor gains `bulk` (0|1|2 added to climb/leap rolls, subtracted from flee, `>= 2` denies the Thief stealth/backstab advantages). Cite the user's ruling verbatim from CONTEXT Area 3 and the Phase 31 `afraidNeed` precedent.
       - `## Need vs die — the equivalence, once`: two sentences: "+1 to hit" seen from the die is the same as "+1 to the need" seen from the target number; this project writes every modifier on the need. Nobody re-litigates this.
       - `## Weapons — before / after`: one table with columns `Weapon | cls | before dice (mean) | before cost | after dice (mean) | after cost | need | crit | tier 0 / 1 / 2 / 3 (in band?)` for all 24 rows (BEFORE from `git show d117782:content/weapons.js`).
       - `## Armor — before / after`: `Armor | cls | min | AR | WP | cost | bulk (new)` (values unchanged except bulk) plus the three bulk consumers with file:line refs (movement climb/leap `r`, combat flee, combat Stealth/backstab gates hero + member).
       - `## Per-class picks`: BEFORE — for each class letter and tier, the single dominant pick under the old max-damage heuristic (e.g. Flail d8+2 at 175 dominates F/T tier 0–1; Long Sword d8 at 500 strictly worse); AFTER — for each class letter and tier, the heavy / neutral / light picks with `expectedStrike` at level 1 (d20), level 3 (d10) and level 5 (d6) for a Human of that class with prof 0 — compute with a scratch `node -e` over `engine/derived.js#expectedStrike` and paste the numbers (never hand-arithmetic). State the honest reading: at levels 1–3 (where the v1.5 BEFORE p50 depth of 4 puts most play) the three roles sit within ~20% of each other at 2× price spread; from level 4 the precise blades pull ahead because the +1 need and crit-2 scale with the shrinking strike die while flat damage (level²) swamps the dice — the heavy weapons are the early-game budget damage, the precise blades the investment. Note the two kits that START heavy (Knight/Awl Pike, Barbarian/Battle Axe) net out at or above their pre-phase level-1 EV because their dice rose more than the need fell (show both numbers).
       - `## No class below need 2`: the table of `CLASSES[cls].toHit + need` minima per class (F 3 with the Bardiche, T 3, M 2) and the test that proves it (`test/unit/gear-axes.test.js`).
       - `## Corrections to CONTEXT (verified in code)`: (a) the hero's crit is `roll === 1 && !noCrit` — the "Soldier 1–2" range at `engine/combat.js` (foe-turn lines, `roll <= 2 && c.sub === "Soldier"`) is the FOE's crit against a Soldier (the sub's bad in docs/CLASS-PASS.md's good/bad table); the Soldier's own blows never crit (noCrit), so "Soldier stacks to 1–3" does not apply and is not implemented; (b) rolled staves DO carry `every: 250` today (`engine/items.js#rollStaff`, `engine/encounters.js:403`) — the "infinite Pine Staff" reading is wrong; Plan 03's charge pools replace a 250-square single cooldown, recorded there.
       - `## Upgrade heuristic`: `expectedStrike` formula in prose + `weaponUpgradeDelta` = delta of expected strikes (new weapon at prof 0 vs the wielded one with its prof/magic bonus); `lootCompare`'s "+N a swing" line; the bot reads the same function.
       - `## Tuning-bot policy (Plan 02)`: the buy/equip rule, reserve 50, Thief bulk ≤ 1 preference; what is deferred to Phase 42 (item-use tactics, `wornSlots`).
       - `## Smoke — 143 cells × 3 seeds vs the v1.5 BEFORE pin`: the pooled table (`p50Depth`, `reach5`, `reach10`, `reach20`, `meanDepth`, `p90Depth`, stuck total, cannot-act cells) BEFORE vs smoke, the exact command, wall time, commit hash of the engine at run time, and the sentence "A 3-seed smoke is a direction check for the ledger, not a tuning verdict — the ONE consolidated v1.5 AFTER matrix is Phase 42 (BAL-02)". If `node tools/class-pass-diff.mjs --gate --after docs/class-pass/v15-gear-smoke.json` runs, record its cannot-act line; if it needs the deep file, say so and skip.
       - `## Declared divergences (GEAR-01)`: copy Plan 01's FIXTURE-INVENTORY rows (fixture, scenario, what moved) with a pointer to the inventory section.
       - Placeholder headings for later plans: `## Item activation model (GEAR-02) — Plan 03`, `## Chips and row states — Plan 05`, `## One-shot tools (GEAR-05) — Plan 04`, `## Requirements map — Plan 05`, each with one line "appended by Plan NN".
       Player-facing copy is not in this doc; no voice scan applies, but keep the register deadpan and family-friendly anyway.

    3. REQUIREMENTS.md: mark `GEAR-01` `[x]` and its traceability row `Complete` (Plan 01 landed the mechanics, this plan the ledger).

    4. PLAN GATE: `npm test` → `# fail 0`; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `git status --porcelain test/parity/fixtures` empty (this plan touches no fixture); `git diff --stat -- mazeworld.html src/browser engine content` empty (this plan changes only tools/, docs/, tests, .planning). Record every number in the SUMMARY.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -E "^# (tests|pass|fail)" && git hash-object test/parity/prototype-master.js.txt && test -f docs/class-pass/v15-gear-smoke.json && grep -c "^## " docs/GEAR-BALANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/class-pass/v15-gear-smoke.json` exists and `node -e "const j=require('./docs/class-pass/v15-gear-smoke.json'); console.log(j.meta.seeds, j.cells.length)"` prints `3 143`.
    - `grep -c "^## " docs/GEAR-BALANCE.md` is at least 14.
    - `grep -c "NOT a retune\|not a tuning verdict" docs/GEAR-BALANCE.md` is at least 1.
    - `grep -c "Bardiche" docs/GEAR-BALANCE.md` is at least 2 (weapon table row + the need-floor table).
    - `grep -c "roll <= 2 && c.sub === \"Soldier\"" docs/GEAR-BALANCE.md` is 1 (the correction is recorded).
    - `grep -c "\[x\] \*\*GEAR-01\*\*" .planning/REQUIREMENTS.md` prints 1 and `grep -c "| GEAR-01 | Phase 39 | Complete |" .planning/REQUIREMENTS.md` prints 1.
    - `npm test` prints `# fail 0`; master hash unchanged; `git status --porcelain test/parity/fixtures` empty; `git diff --stat -- mazeworld.html src/browser engine content` empty.
  </acceptance_criteria>
  <done>GEAR-01 is closed: the ledger records design before/after, per-class picks with measured expected-strike numbers, the corrections, the bot policy and a committed smoke against the v1.5 BEFORE pin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tools/ (harness) → engine | the bot dispatches real `buyItem` actions; a policy loop (re-picking a refused buy) would stall the matrix |
| docs → developer | the ledger's numbers must be measured, not typed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-39-04 | Denial of service | `chooseStorePurchase` re-picking a line the engine refuses (notBetter/insufficientGold) forever | medium | mitigate | the policy reads the engine's own `weaponUpgradeDelta`/`canEquip*`/gold reserve, and a bought line flips `sold: true`; bot-buy-policy.test.js pins that a refused-shape line is never chosen and the smoke's `stuck` total is recorded |
| T-39-05 | Repudiation | ledger numbers hand-typed | low | mitigate | every expected-strike and rollup number in the ledger is produced by a `node -e` read of the engine/JSON and the command is recorded beside the table |
| T-39-SC | Tampering | npm/pip/cargo installs | low | accept | no package installs in this plan |
</threat_model>

<verification>
- `node --test test/unit/bot-buy-policy.test.js` green; `npm test` `# fail 0`; master hash unchanged; no fixture/engine/content/shell diff.
- `docs/GEAR-BALANCE.md` has every section listed in Task 2 with measured numbers; `docs/class-pass/v15-gear-smoke.json` committed.
- SUMMARY "Human verification (deferred to end of run)": (1) open a store as a Fighter at depth 1–2 and confirm at least two weapon lines with visibly different labels (a heavy dice line vs a precise one) and that the loot-compare readout says "+N a swing" / "not an upgrade" — visible after Plan 05's shell pass; (2) buy a heavy weapon and confirm TO HIT on the Hero tab drops by one (after Plan 05).
</verification>

<success_criteria>
- ROADMAP SC-1 fully met: differentiated choices per class recorded in a before/after ledger; GEAR-01 marked complete.
</success_criteria>

<output>
Create `.planning/phases/39-gear-magic-items-one-shot-tools/39-02-SUMMARY.md` when done (38-05-SUMMARY.md shape; include "## Human verification (deferred to end of run)" and the smoke's pooled table).
</output>
