---
phase: 18-bestiary-rebalance-canon-combat-fixes
plan: 05
type: execute
wave: 2
depends_on: ["18-01", "18-02"]
files_modified:
  - content/bestiary.js
  - test/unit/content-tables.test.js
autonomous: true
requirements: [BEST-01, BEST-02, BEST-03, FID-05]
tags: [content, bestiary, balance, parity, content-table, node-test]

must_haves:
  truths:
    - "BEST-01 / D-18: Drake (BESTIARY.Beasts[3][0]) wp 135 -> 38 (prototype-mode TTK 10.44 -> 2.94, 7.30x -> 2.05x the tier-4 median — within the ~2x band, still the single tankiest tier-4 body above Herman's 36); Werebeast (BESTIARY.Magical[2][0]) sp.dmg {n:1,sides:10,bonus:5} -> {n:1,sides:10,bonus:0} with atk 2 kept (RTD 2.14 -> 2.87, lethality 2.60x -> 1.94x the tier-3 median) and its note updated from 'two attacks at d10+5' to 'two attacks at d10' so the note no longer contradicts the stat"
    - "BEST-02 / D-03: Djinni (Demons[3][0] AND Demons[4][0]) wp 86 -> 65 with sp.dmg {n:1,sides:4,bonus:0} added; Krupke (Humans[1][1]) wp 23 -> 17 and sp.dmg {1,8,2} -> {1,6,2}; Drudge (Magical[3][0] AND Magical[4][0]) wp 12 -> 9 with NO dmg field added (never melees); Vampire (Walking Dead[4][0]) wp 95 -> 71 with sp.dmg {1,4,0} added; Stalka Beast (Beasts[4][1]) wp 125 -> 94 with sp.dmg {1,4,0} added — every wp is Math.round(old x 0.75)"
    - "BEST-01 precision (edge probe): the -25% discount uses Math.round (64.5 -> 65, 17.25 -> 17, 9 -> 9, 71.25 -> 71, 93.75 -> 94); no other arithmetic rounding is involved in this plan"
    - "D-19 / D-14: Sterling (Beasts[2][2]) keeps wp 35 and every other field; Bat/Rat, Shriek, Viper (Beasts[0][0..2]) and Dante (Humans[0][0]) are byte-identical to e01ac46 — pinned verbatim by deepStrictEqual in content-tables.test.js as the BEST-03 / FID-05 guard, and BESTIARY.Beasts[0] / BESTIARY.Humans[0] keep length 3 / 1 in the same order"
    - "FID-05 adjacency / empty / ordering (edge probe): zero fixtures regenerated (git diff --quiet e01ac46 -- test/parity/fixtures succeeds), zero comparables.js changes, the fixture-inventory test and all five FULL_FIGHTS draw pins pass unmodified, and every BESTIARY tier array keeps its length and order (only field values on existing entries change — D-17)"
    - "D-17: no schema change — only wp / sp.dmg values (and the one note string) move on existing entries; adding sp.dmg to a creature that relied on the engine's d6 fallback is an existing flat field, and rollDice with n:1 draws exactly one die just like the fallback, so foeTurn's draw count for those creatures is unchanged"
    - "D-17 / D-13: test/unit/content-tables.test.js pins every changed row's new wp and sp.dmg with a one-line rationale each, keeps the existing Bat/Rat + Drake-dmg pin green (Drake's dmg 2d10+4 is unchanged), and adds a 'no creature added/removed/reordered' pin (per-type tier lengths [3,2,5,2,2] / [1,1,1,3,1] / [1,2,2,2,1] / [5,2,1,1,1] / [1,1,1,1,1] / [1,2,2,3,1])"
    - "D-04: content/bestiary.js's header comment references content/BESTIARY-REBALANCE.md as the before/after record of every Phase 18 number change"
  artifacts:
    - path: "content/bestiary.js"
      provides: "rebalanced rows (10 entries across 8 creature names) + header reference to BESTIARY-REBALANCE.md; shape unchanged"
      contains: "BESTIARY-REBALANCE.md"
    - path: "test/unit/content-tables.test.js"
      provides: "Phase 18 pins: D-03 discount rows, D-18 Drake/Werebeast, D-19 Sterling unchanged, fixture-exposed rows verbatim (BEST-03/FID-05), tier-length/order pin (D-17)"
      contains: "Phase 18"
  key_links:
    - from: "content/bestiary.js header"
      to: "content/BESTIARY-REBALANCE.md"
      via: "comment line naming the doc as the committed before/after table (D-04)"
      pattern: "BESTIARY-REBALANCE.md"
    - from: "test/unit/content-tables.test.js"
      to: "content/bestiary.js"
      via: "BESTIARY[type][tier][index] value pins imported through content/index.js"
      pattern: "BESTIARY\\[\"Magical\"\\]\\[2\\]\\[0\\]"
    - from: "test/parity/fixture-inventory.test.js + test/unit/foe-turn-draw-count.test.js FULL_FIGHTS"
      to: "content/bestiary.js Beasts[0] / Humans[0]"
      via: "unchanged roster + unchanged draw totals prove the four fixture-exposed rows did not move (D-14/D-15)"
      pattern: "Bat/Rat"
  prohibitions:
    - requirement_id: BEST-03
      category: safety
      statement: "MUST NOT change any field of Bat/Rat, Shriek, Viper, or Dante (the fixture-exposed roster, test/parity/FIXTURE-INVENTORY.md) — Dante's 4.00x outlier is recorded as deferred to Phase 21 (D-14), never fixed here and never carved out"
    - requirement_id: FID-05
      category: safety
      statement: "MUST NOT regenerate any parity fixture, edit test/parity/prototype-master.js.txt, or touch test/parity/harness/comparables.js — zero carve-outs is the required outcome of D-14/D-15"
    - requirement_id: BEST-01
      category: values
      statement: "MUST NOT change Sterling's wp (D-19), Cave Bear's or Herman's numbers (research-reviewed borderline / canon-explicit, recorded not fixed), or buff any under-tier creature — only the ten entries named in this plan move"
    - requirement_id: BEST-01
      category: values
      statement: "MUST NOT add, remove, or reorder any bestiary entry, add a new field name (tier, threat budget, cls, abilities — D-17 / FOE-V2-04 deferred), or touch engine/difficulty.js"
---

<objective>
Apply the Phase 18 number changes to `content/bestiary.js` — the D-03 pre-ability discount on the five caster foes (Djinni x2, Krupke, Drudge x2, Vampire, Stalka Beast), and the two D-18 outlier fixes (Drake's wp, Werebeast's damage bonus) — leaving Sterling (D-19), the four fixture-exposed creatures (D-14) and every other row untouched; pin every changed value and every deliberately unchanged guard row in `test/unit/content-tables.test.js` (D-17); and reference the stat-table document from the bestiary header (D-04).

Purpose: BEST-01 requires the flagged outliers corrected and BEST-02 requires the ability-bearing foes to carry proportionally lower raw stats before Phase 19 gives them ability kits. All ten changed entries are tier 2-5 or non-Beasts/Humans types, so none is fixture-exposed and the parity suite stays byte-identical with zero carve-outs (BEST-03 / FID-05 satisfied by construction and proven by the untouched fixture-inventory and draw-count pins).

Output: modified `content/bestiary.js` and `test/unit/content-tables.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-01-SUMMARY.md
@content/bestiary.js
@content/BESTIARY-REBALANCE.md
@test/unit/content-tables.test.js
@test/parity/FIXTURE-INVENTORY.md
@test/parity/fixture-inventory.test.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| rebalanced `content/bestiary.js` rows: Djinni x2, Krupke, Drudge x2, Vampire, Stalka Beast (D-03); Drake, Werebeast (D-18) | content edits | **18-05** | no shape change (D-17); header references the doc (D-04) |
| Phase 18 pins in `test/unit/content-tables.test.js` (changed rows, Sterling unchanged, fixture-exposed rows verbatim, tier lengths) | test | **18-05** | rationale comment per pin |
| `tools/bestiary-yardstick.mjs` + `content/BESTIARY-REBALANCE.md` (BEFORE + verdicts) | tooling / doc | 18-01 | the verdicts this plan executes |
| `engine/foeDamage.js`, `content/damage-multipliers.js`, `foeArmorSoaked` | seam + content + event | 18-02 | — |
| routed `engine/combat.js` / `engine/magic.js` / `engine/items.js` | engine edits | 18-03, 18-04 | parallel wave-2 plans, disjoint files |
| AFTER tables + change ledger in `content/BESTIARY-REBALANCE.md`, invariant test | doc / test | 18-06 | consumes this plan's numbers |

## Assumptions locked by this plan

- Exact D-18 numbers (Claude's discretion within D-02/D-18): Drake `wp: 38` — the largest value that keeps Drake strictly the tankiest tier-4 body (Herman 36) while landing at 2.05x the tier-4 TTK median (D-18's "~2x, not the median"); Werebeast keeps `atk: 2` and the d10 and drops only the flat +5 (the minimal edit that brings lethality from 2.60x to 1.94x — 18-RESEARCH's "+5 → +2" suggestion was recomputed and still leaves 2.46x, so the bonus goes to 0).
- Werebeast's `note` string is updated ("two attacks at d10") because D-02's second clause treats a stat contradicting the creature's own note as an outlier condition — leaving "d10+5" in the note after changing the dice would create exactly that contradiction. `note` is an existing field (no schema change) and is UI flavor only (engine/combat.js header: only sp.note feeds the UI).
- Adding `sp.dmg: { n: 1, sides: 4, bonus: 0 }` to Djinni/Vampire/Stalka Beast implements "one dice-step lower" from the engine's d6 fallback (18-RESEARCH "Dice-step ladder"); `rollDice` with n:1 draws one die exactly like the `rng.d(6)` fallback, so no draw count changes.
- Sterling's wp stays 35 (D-19) and its post-halfDmg ~10.23 TTK is recorded in 18-06's AFTER table as canon-intended.

<tasks>

<task type="auto">
  <name>Task 1: Apply the ten entry changes in content/bestiary.js and add the header reference to BESTIARY-REBALANCE.md</name>
  <files>content/bestiary.js</files>
  <read_first>
    - content/bestiary.js (whole file — locate by name: Drake line 32, Stalka Beast 37, Djinni 44 and 49, Krupke 55, Sterling 27 (do not touch), Werebeast 89, Drudge 90 and 91, Vampire 111; Bat/Rat, Shriek, Viper 14-16 and Dante 52 (do not touch))
    - content/BESTIARY-REBALANCE.md "Review verdicts" and "Pre-ability discount" sections (18-01 output — the planned numbers this task applies verbatim)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md D-03, D-04, D-14, D-17, D-18, D-19
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-RESEARCH.md "Pre-Ability Discount (D-03) — Concrete New Numbers" (table) and the Tier 3/4 outlier rows for Drake / Werebeast
    - test/parity/FIXTURE-INVENTORY.md "What this means for Phase 18 (constraints)" items (a) and (c)
  </read_first>
  <action>
    Edit exactly these entries in `content/bestiary.js`, changing only the named values (keep every other key, the array order, and the surrounding formatting):
    1. Beasts tier 4 `Drake`: `wp: 135` → `wp: 38`. Keep `sp.dmg {2,10,4}`, `every: 4`, the note.
    2. Beasts tier 5 `Stalka Beast`: `wp: 125` → `wp: 94`; add `dmg: { n: 1, sides: 4, bonus: 0 }` inside `sp` (after `atk: 2`, before `note`).
    3. Demons tier 4 `Djinni` (first of three): `wp: 86` → `wp: 65`; add `dmg: { n: 1, sides: 4, bonus: 0 }` inside `sp` (after `caster: true`).
    4. Demons tier 5 `Djinni`: same as 3 (`wp: 65`, add the d4).
    5. Humans tier 2 `Krupke`: `wp: 23` → `wp: 17`; `dmg: { n: 1, sides: 8, bonus: 2 }` → `dmg: { n: 1, sides: 6, bonus: 2 }`.
    6. Magical tier 3 `Werebeast`: `dmg: { n: 1, sides: 10, bonus: 5 }` → `dmg: { n: 1, sides: 10, bonus: 0 }`; `note: "two attacks at d10+5"` → `note: "two attacks at d10"`. Keep `wp: 32`, `atk: 2`.
    7. Magical tier 4 `Drudge`: `wp: 12` → `wp: 9`. Do NOT add a dmg field (D-03: HP-only, never melees).
    8. Magical tier 5 `Drudge`: `wp: 12` → `wp: 9`.
    9. Walking Dead tier 5 `Vampire`: `wp: 95` → `wp: 71`; add `dmg: { n: 1, sides: 4, bonus: 0 }` inside `sp` (after `atk: 2`).
    Above each changed entry add a one-line comment in the file's established `DELIBERATE RULES CHANGE (Phase 18, <REQ>)` style naming the decision (D-03 pre-ability discount — revisit Phase 21 / D-18 outlier fix) and the before → after value; keep comments short so the data stays scannable.

    Update the file header comment (lines 1-9): keep the existing provenance text and append a paragraph — Phase 18 (BEST-01/BEST-02) deliberately moved ten entries away from the prototype's numbers; the full before/after stat table, yardstick methodology and per-creature rationale live in `content/BESTIARY-REBALANCE.md` (D-04); the four fixture-exposed creatures (Bat/Rat, Shriek, Viper, Dante — test/parity/FIXTURE-INVENTORY.md) are untouched and must stay so without a named `comparables.js` carve-out (BEST-03/FID-05); array order within each tier is load-bearing for `rng.pick`.

    Do not touch Sterling, Cave Bear, Herman, any tier-1 Beasts/Humans row, or any other entry. Do not add or remove entries or keys beyond the `dmg` additions listed.
  </action>
  <verify>
    <automated>node --test test/unit/content-tables.test.js test/parity/fixture-inventory.test.js test/unit/foe-turn-draw-count.test.js test/determinism/content-is-pure-data.test.js && node tools/bestiary-yardstick.mjs --mechanics=prototype | grep -E 'Drake|Werebeast|Djinni|Krupke|Drudge|Vampire|Stalka'</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "import('./content/index.js').then(m=>{const B=m.BESTIARY;const ok=B.Beasts[3][0].wp===38&&B.Beasts[4][1].wp===94&&B.Demons[3][0].wp===65&&B.Demons[4][0].wp===65&&B.Humans[1][1].wp===17&&B.Magical[3][0].wp===9&&B.Magical[4][0].wp===9&&B['Walking Dead'][4][0].wp===71&&B.Magical[2][0].wp===32&&B.Beasts[2][3].wp===35;process.exit(ok?0:1)})"` exits 0
    - `node -e "import('./content/index.js').then(m=>{const B=m.BESTIARY;const j=JSON.stringify;const ok=j(B.Beasts[4][1].sp.dmg)===j({n:1,sides:4,bonus:0})&&j(B.Demons[3][0].sp.dmg)===j({n:1,sides:4,bonus:0})&&j(B.Demons[4][0].sp.dmg)===j({n:1,sides:4,bonus:0})&&j(B['Walking Dead'][4][0].sp.dmg)===j({n:1,sides:4,bonus:0})&&j(B.Humans[1][1].sp.dmg)===j({n:1,sides:6,bonus:2})&&j(B.Magical[2][0].sp.dmg)===j({n:1,sides:10,bonus:0})&&B.Magical[2][0].sp.note==='two attacks at d10'&&B.Magical[3][0].sp.dmg===undefined&&B.Magical[4][0].sp.dmg===undefined&&j(B.Beasts[3][0].sp.dmg)===j({n:2,sides:10,bonus:4});process.exit(ok?0:1)})"` exits 0
    - `git diff e01ac46 -- content/bestiary.js | grep -c '^[-+].*n: "Bat/Rat"\|^[-+].*n: "Shriek"\|^[-+].*n: "Viper"\|^[-+].*n: "Dante"\|^[-+].*n: "Sterling"\|^[-+].*n: "Cave Bear"\|^[-+].*n: "Herman"'` prints 0 (no data line of the fixture-exposed rows, Sterling, Cave Bear, or Herman was added or removed)
    - `git diff e01ac46 --numstat -- content/bestiary.js | awk '{print $1-$2}'` prints a number between 8 and 20 (only comment lines were added net; no entries added or removed — every data line changed is a one-for-one replacement)
    - `grep -c 'BESTIARY-REBALANCE.md' content/bestiary.js` prints at least 1 and `sed -n '1,25p' content/bestiary.js | grep -c 'FIXTURE-INVENTORY.md'` prints at least 1
    - `grep -c 'DELIBERATE RULES CHANGE (Phase 18' content/bestiary.js` prints at least 9
    - `node tools/bestiary-yardstick.mjs --mechanics=prototype | grep 'Drake' | grep -c '2.05'` prints 1 and `node tools/bestiary-yardstick.mjs --mechanics=prototype | grep 'Werebeast' | grep -c '1.94'` prints 1
    - `node --test test/parity/fixture-inventory.test.js test/unit/foe-turn-draw-count.test.js test/determinism/content-is-pure-data.test.js` exits 0 (roster and draw pins unchanged; content still pure data)
    - `node --test test/unit/content-tables.test.js` exits 0 (the existing Bat/Rat + Drake-dmg pin still passes — Drake's dmg notation is unchanged)
  </acceptance_criteria>
  <done>The ten entries carry their new numbers with inline rationale, nothing else moved, the header points at the stat-table doc, and the fixture-inventory / draw-count / purity guards are green.</done>
</task>

<task type="auto">
  <name>Task 2: Pin every changed row, every guard row, and the tier shape in content-tables.test.js; run the phase-level parity gate</name>
  <files>test/unit/content-tables.test.js</files>
  <read_first>
    - test/unit/content-tables.test.js (whole file — import list from content/index.js; the existing `BESTIARY: Bat/Rat sp.dmg is a flat 1, atk 2; Drake dmg is 2d10+4` pin at lines 89-99 as the style template; the DAMAGE_MULTIPLIERS pin 18-02 added)
    - content/bestiary.js after Task 1 (the exact new values and the untouched rows to snapshot)
    - test/parity/FIXTURE-INVENTORY.md "Parity-exposed bestiary surface" (Beasts[0] length 3 order Bat/Rat, Shriek, Viper; Humans[0] length 1 Dante) and constraint (c) (length and order are load-bearing)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md D-13, D-14, D-15, D-17
  </read_first>
  <action>
    Append a section `// --- Phase 18: bestiary rebalance pins (BEST-01/02/03, D-03/D-14/D-17/D-18/D-19) ---` to `test/unit/content-tables.test.js` with these tests, each pin carrying a one-line rationale comment (D-17) and asserting through `BESTIARY[type][tierIndex][arrayIndex]` with `assert.equal` / `assert.deepStrictEqual`:
    1. "BESTIARY Phase 18 / D-03: Djinni (Demons T4 and T5) -25% HP and a d4 melee step" — both entries `n === "Djinni"`, `wp === 65`, `sp.dmg` deepStrictEqual `{ n: 1, sides: 4, bonus: 0 }`, `sp.caster === true`.
    2. "BESTIARY Phase 18 / D-03: Krupke wp 17, d6+2" — `Humans[1][1]`: wp 17, dmg `{ n: 1, sides: 6, bonus: 2 }`, `sp.ar === 12` (armor untouched).
    3. "BESTIARY Phase 18 / D-03: Drudge (Magical T4 and T5) wp 9, HP-only — no dmg field" — both wp 9, `Object.hasOwn(sp, "dmg") === false`, `sp.never_melee === true`.
    4. "BESTIARY Phase 18 / D-03: Vampire wp 71 + d4; Stalka Beast wp 94 + d4; both keep atk 2" — `["Walking Dead"][4][0]` and `Beasts[4][1]`.
    5. "BESTIARY Phase 18 / D-18: Drake wp 38 (was 135), dmg 2d10+4 and every:4 unchanged" — `Beasts[3][0]`.
    6. "BESTIARY Phase 18 / D-18: Werebeast two attacks at d10 (bonus 5 -> 0), wp 32 unchanged, note matches the dice" — `Magical[2][0]`: `sp.atk === 2`, dmg `{ n: 1, sides: 10, bonus: 0 }`, `sp.note === "two attacks at d10"`, wp 32.
    7. "BESTIARY Phase 18 / D-19: Sterling keeps wp 35 and halfDmg (TTK doubling recorded in BESTIARY-REBALANCE.md, revisit Phase 21)" — `Beasts[2][3]`: wp 35, `sp.halfDmg === true`, dmg `{ n: 1, sides: 12, bonus: 0 }`.
    8. "BESTIARY Phase 18 / D-14 (BEST-03, FID-05): the four fixture-exposed rows are byte-identical to the prototype" — `assert.deepStrictEqual(BESTIARY.Beasts[0], [ ...the three literal Bat/Rat / Shriek / Viper objects copied verbatim from content/bestiary.js at e01ac46... ])` and `assert.deepStrictEqual(BESTIARY.Humans[0], [ ...the literal Dante object... ])` — copy the objects exactly (n, sz, i, wp, sp with atk/dmg/note or shriek/note or poison/note; Dante's `atk: 3` note); comment: any diff here means a fixture-exposed creature moved and BEST-03 requires a named carve-out — which Phase 18 forbids (D-14).
    9. "BESTIARY Phase 18 / D-17: no creature added, removed, or reordered — tier lengths per type" — `assert.deepStrictEqual(Object.keys(BESTIARY), ["Beasts", "Demons", "Humans", "Lair Beasts", "Magical", "Walking Dead"])` and `assert.deepStrictEqual(Object.values(BESTIARY).map((tiers) => tiers.map((t) => t.length)), [[3,2,5,2,2],[1,1,1,3,1],[1,2,2,2,1],[5,2,1,1,1],[1,1,1,1,1],[1,2,2,3,1]])`; plus the total row count `Object.values(BESTIARY).flat(2).length === 53`.
    10. "BESTIARY Phase 18 / D-17: every entry keeps the flat shape — allowed top-level keys n/sz/i/wp/sp only, and sp.dmg where present is {n,sides,bonus}" — iterate all 53 rows asserting `Object.keys(row)` is a subset of `["n","sz","i","wp","sp"]`, `Number.isInteger(row.wp) && row.wp > 0`, and any `sp.dmg` has integer `n`, `sides`, `bonus`.
    Then run the phase-level parity gate for this plan: `node --test "test/parity/**/*.test.js"` (30 pass, byte-identical), `git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt engine/difficulty.js package.json package-lock.json` exits 0, and `npm test` is green.
  </action>
  <verify>
    <automated>node --test test/unit/content-tables.test.js test/parity/fixture-inventory.test.js && node --test "test/parity/**/*.test.js" && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 18' test/unit/content-tables.test.js` prints at least 10
    - `grep -c 'deepStrictEqual(BESTIARY.Beasts\[0\]' test/unit/content-tables.test.js` prints 1 and `grep -c 'deepStrictEqual(BESTIARY.Humans\[0\]' test/unit/content-tables.test.js` prints 1
    - `grep -c '\[3,2,5,2,2\]' test/unit/content-tables.test.js` prints 1
    - `node --test test/unit/content-tables.test.js` exits 0 reporting >= 28 tests passed, 0 failed (17 at e01ac46 + 1 from 18-02 + 10 here); `git diff -U0 e01ac46 -- test/unit/content-tables.test.js | grep -c '^-.*assert'` prints 0 (no existing pin edited or removed)
    - `node --test test/parity/fixture-inventory.test.js` exits 0 (roster pin unchanged and consistent with FIXTURE-INVENTORY.md)
    - `node --test "test/parity/**/*.test.js"` exits 0 (30 pass) and `git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt engine/difficulty.js package.json package-lock.json` exits 0
    - `npm test` exits 0 with 0 failing tests
  </acceptance_criteria>
  <done>Every changed row and every deliberately unchanged guard row is pinned with a rationale, the tier shape is locked, the fixture-exposed rows are snapshotted verbatim, and the parity suite is byte-identical with zero carve-outs and zero regeneration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content table → engine | Trusted in-repo data; only values change, shape pinned by tests |
| save file → state | Unchanged; bestiary numbers are read at `startCombat` time from content, never persisted per se (a saved in-progress fight keeps its own foe wp — no migration) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | A fixture-exposed row edited (breaking parity) and "fixed" by regenerating fixtures or carving out comparables | high | mitigate | Verbatim deepStrictEqual snapshot of Beasts[0]/Humans[0]; fixture-inventory test; FULL_FIGHTS pins; `git diff --quiet e01ac46 -- test/parity/...` in acceptance |
| T-18-05 | Repudiation | Number changes without a traceable rationale (silent retune) | medium | mitigate | Inline `DELIBERATE RULES CHANGE (Phase 18, ...)` comments on each entry; one-line rationale per pin (D-17); the doc referenced from the header (D-04) |
| T-18-03 | Denial of Service | A malformed dice notation (missing key) crashing rollDice mid-fight | medium | mitigate | Pin 10 asserts integer n/sides/bonus on every sp.dmg; content-is-pure-data guard |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; manifests unchanged (acceptance) |
</threat_model>

<verification>
- `node --test test/unit/content-tables.test.js` green with >= 10 new Phase 18 pins and no existing pin edited.
- `node tools/bestiary-yardstick.mjs --mechanics=prototype` shows Drake 2.05x and Werebeast lethality 1.94x; Djinni T4 65 / Krupke 17 / Drudge 9 / Vampire 71 / Stalka 94.
- `node --test "test/parity/**/*.test.js"` byte-identical; `test/parity/fixture-inventory.test.js` and FULL_FIGHTS pins unchanged; `npm test` green; frozen files unchanged since e01ac46.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 5: Djinni, Krupke, Drudge, Vampire, Stalka Beast carry proportionally lower raw stats than the prototype baseline (-25% HP, one dice step) ahead of Phase 19's ability kits.
- Success criterion 1's "outliers corrected": Drake and Werebeast are inside the ~2x band; Sterling/Dante/Cave Bear/Herman dispositions are recorded, not silently skipped.
- Success criterion 4: zero carve-outs were needed because no fixture-exercised creature changed — proven by tests, not by inspection.
</success_criteria>

<output>
Create `.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-05-SUMMARY.md` when done (record the ten before → after values and the pin count).
</output>
