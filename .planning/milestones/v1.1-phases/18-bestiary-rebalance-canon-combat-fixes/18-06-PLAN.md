---
phase: 18-bestiary-rebalance-canon-combat-fixes
plan: 06
type: execute
wave: 3
depends_on: ["18-01", "18-03", "18-04", "18-05"]
files_modified:
  - test/unit/foe-damage.test.js
  - test/unit/bestiary-yardstick.test.js
  - content/BESTIARY-REBALANCE.md
autonomous: true
requirements: [BEST-01, BEST-03, CANON-01, FID-05]
tags: [content, bestiary, docs, invariant, phase-gate, parity, node-test]

must_haves:
  truths:
    - "CANON-01 / D-09 invariant (assumption-delta 'promote'): a source-assertion test proves no foe-side direct wp decrement remains on a code line of engine/combat.js, engine/magic.js or engine/items.js (comments stripped; the hero's c.wp and the party member's member.wp decrements are excluded by construction) and exactly one exists in engine/foeDamage.js — the seam is the only path damage-to-foe takes"
    - "BEST-01 / D-04: content/BESTIARY-REBALANCE.md's AFTER block (between the yardstick:after markers) is the verbatim --mechanics=canon output of tools/bestiary-yardstick.mjs against the rebalanced content/bestiary.js, and a unit test keeps it that way (doc drift fails npm test, mirroring test/parity/fixture-inventory.test.js's generated-block check)"
    - "BEST-01 / D-16: the doc's tune-difficulty AFTER readout (200 seeds, same stride as BEFORE) is transcribed beside the BEFORE readout with the explicit label 'informational sanity signal — not a gate; the bot is blind to abilities until Phase 21'"
    - "BEST-01 / D-18 / D-19 / D-05: the change ledger lists every changed entry before → after with its measured prototype-mode AFTER ratio (Drake 2.05x, Werebeast lethality 1.94x, Djinni T4 3.51x / T5 1.62x, Krupke 1.42x, Drudge 0.49x / 0.22x, Vampire 1.77x, Stalka Beast 2.35x) and every canon-mode consequence recorded as intended-and-deferred: Sterling ~10.23 (4.38x) two hearts; the five sp.ar creatures' melee TTK (Drat 5.93 / 2.54x, Krupke 7.34 / 2.22x, Craig 3.37 / 2.36x, Herman T4 5.06 / 3.54x, Google 9.54 / 2.88x) — 'canon natural armor (D-05), spells bypass (D-06), revisit Phase 21'; Philly 3.84 (1.09x) resolved by slow"
    - "BEST-03 / FID-05 phase gate: git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt engine/difficulty.js package.json package-lock.json exits 0; node --test \"test/parity/**/*.test.js\" is 30/30 byte-identical; npm test is green with at least 724 + every new Phase 18 test passing; the doc's carve-out section states the measured result: zero carve-outs, zero regenerations"
    - "FID-05 empty (edge probe): the doc explicitly records that the set of regenerated fixtures is EMPTY and why (D-14/D-15), so the 'before/after table + rationale' requirement is met by the rebalance table itself rather than by any fixture diff"
    - "Phase completion: 18-VALIDATION.md's per-task rows can all be flipped to green from this plan's gate run; every ROADMAP Phase 18 success criterion (1-5) maps to a green test or a committed doc section named in the ledger"
  artifacts:
    - path: "test/unit/foe-damage.test.js"
      provides: "Phase 18 invariant: seam is the only foe-wp decrement site (source assertion over engine/combat.js, magic.js, items.js, foeDamage.js with comments stripped)"
      contains: "invariant"
    - path: "test/unit/bestiary-yardstick.test.js"
      provides: "doc-consistency test: BESTIARY-REBALANCE.md AFTER block === toMarkdown(computeYardstick(BESTIARY, { mechanics: 'canon' }))"
      contains: "yardstick:after:begin"
    - path: "content/BESTIARY-REBALANCE.md"
      provides: "AFTER table (canon), tune-difficulty AFTER readout, change ledger with measured ratios and deferred canon consequences, final carve-out statement"
      contains: "Change ledger"
  key_links:
    - from: "test/unit/bestiary-yardstick.test.js"
      to: "content/BESTIARY-REBALANCE.md"
      via: "reads the file, slices between the yardstick:after markers, compares to the live canon-mode markdown"
      pattern: "yardstick:after:end"
    - from: "test/unit/foe-damage.test.js invariant"
      to: "engine/combat.js, engine/magic.js, engine/items.js, engine/foeDamage.js"
      via: "fs.readFileSync + comment-strip (the stripComments pattern from test/unit/combat.test.js) + a foe-identifier wp-decrement regex counted per file"
      pattern: "stripComments"
  prohibitions:
    - requirement_id: FID-05
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, regenerate fixtures, or touch comparables.js — this plan only proves they were never touched"
    - requirement_id: BEST-01
      category: transparency
      statement: "MUST NOT hand-edit numbers inside the AFTER block — it is generated output; if a number looks wrong, the script or the bestiary is wrong, and the consistency test exists to make hand edits fail"
    - requirement_id: BEST-01
      category: values
      statement: "MUST NOT change any bestiary number or engine file in this plan (no 'while I'm here' retunes of Sterling, Cave Bear, the ar creatures) — canon-mode consequences are RECORDED and deferred to Phase 21 per D-19's precedent"
    - requirement_id: CANON-01
      category: safety
      statement: "MUST NOT weaken the invariant regex to make it pass — if a foe-side decrement is found outside the seam, route it (an 18-03/18-04 miss) and record the fix in the SUMMARY"
---

<objective>
Close Phase 18: prove the seam is now the ONLY path damage-to-foe takes (a source-assertion invariant test — the "promote" half of the assumption-delta decision), finish `content/BESTIARY-REBALANCE.md` with the generated canon-mode AFTER table (kept honest by a doc-consistency test), the tune-difficulty AFTER readout (D-16), and the change ledger that records every measured result including the canon-mode consequences deferred to Phase 21 (Sterling, the five `sp.ar` creatures), and run the phase-level gate that proves zero fixture regeneration and zero carve-outs (BEST-03 / FID-05).

Purpose: BEST-01's "before/after stat table committed" is only true once the AFTER half exists and is machine-checked against the live content; FID-05's rationale requirement is met by the ledger; the invariant test is what makes "one seam" a maintained property rather than a one-time cleanup — Phase 19's ability resolver will be written against it.

Output: extended `test/unit/foe-damage.test.js` and `test/unit/bestiary-yardstick.test.js`; completed `content/BESTIARY-REBALANCE.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-01-SUMMARY.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-03-SUMMARY.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-04-SUMMARY.md
@.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-05-SUMMARY.md
@content/BESTIARY-REBALANCE.md
@tools/bestiary-yardstick.mjs
@test/unit/bestiary-yardstick.test.js
@test/unit/foe-damage.test.js
@test/parity/fixture-inventory.test.js
@test/unit/combat.test.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| seam-only invariant test (in `test/unit/foe-damage.test.js`) | test | **18-06** | source assertion; the "promote" decision's enforcement |
| doc-consistency test (in `test/unit/bestiary-yardstick.test.js`) | test | **18-06** | AFTER block === live canon output |
| completed `content/BESTIARY-REBALANCE.md` (AFTER table, tune AFTER readout, change ledger, carve-out statement) | doc | **18-06** | D-04 deliverable finished |
| `tools/bestiary-yardstick.mjs`, doc BEFORE half | tooling / doc | 18-01 | — |
| `engine/foeDamage.js` seam, `content/damage-multipliers.js`, `foeArmorSoaked` + narration | engine / content / event | 18-02 | — |
| routed `engine/combat.js` (+slow), `engine/magic.js`, `engine/items.js` | engine edits | 18-03, 18-04 | — |
| rebalanced `content/bestiary.js` + pins | content + test | 18-05 | — |

<tasks>

<task type="auto">
  <name>Task 1: Add the seam-only invariant test (damageFoe is the only foe-wp decrement site)</name>
  <files>test/unit/foe-damage.test.js</files>
  <read_first>
    - test/unit/combat.test.js lines 930-945 (`stripComments(source)` and the "references no Math.random" source-assertion test — the pattern to copy) and test/unit/formatEventsCoverage.test.js lines 40-60 (REPO_ROOT / ENGINE_DIR resolution via import.meta.url)
    - test/parity/fixture-inventory.test.js (how it slices the generated block between marker comments and compares to the live generator output — mirror this for the AFTER block)
    - test/unit/foe-damage.test.js (18-02 output — append the invariant section at the end; reuse its imports)
    - engine/combat.js (post-18-03), engine/magic.js and engine/items.js (post-18-04), engine/foeDamage.js — confirm the only remaining wp decrements on foe identifiers are inside the seam, and that `c.wp -= ...` (hero) and `member.wp -= mDmg` (party member) still exist and must NOT be matched
  </read_first>
  <action>
    Append to `test/unit/foe-damage.test.js` a section `// --- Phase 18 invariant: damageFoe is the ONLY foe-wp decrement site (D-09, assumption-delta: promote) ---`. Add `fs`/`path`/`url` imports and a local `stripComments` (block + line comments removed, same as combat.test.js). Define the foe-decrement regex as: a word boundary, then one of the identifiers `t`, `f`, `o` or `foe` (the loop/target variable names the engine uses for foes), then a literal dot, then `wp`, then a space, a minus sign, an equals sign and a space — build it with `new RegExp` from parts or a single literal; do not include `c` or `member` in the alternation (the hero's and the party member's own decrements are legitimate and must remain). Test A: "invariant: engine/combat.js, engine/magic.js and engine/items.js contain zero foe-side wp decrements on code lines" — for each file read, strip comments, count regex matches with the global flag, assert 0 with a message naming the file. Test B: "invariant: engine/foeDamage.js contains exactly one" — same count === 1. Test C: "sanity: the regex would catch a violation" — assert the regex matches the string built as the identifier `t`, a dot, `wp`, and the decrement operator with a value (construct the sample from parts so this file itself never carries the literal on one token — the invariant scans engine/, not test/, but keep the sample honest) and does NOT match a hero-side sample built with `c`. Test D: "sanity: the hero and member decrements still exist in combat.js" — after stripping comments, `c.wp -= dmg` count is 1 and `member.wp -= mDmg` count is 1 (guards against the regex accidentally being satisfied by deleting those).

    If Test A finds a violation, that is an 18-03/18-04 routing miss: route the site through damageFoe with the appropriate kind (do not weaken the regex), re-run, and record the fix in this plan's SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/foe-damage.test.js test/unit/combat.test.js test/unit/magic.test.js test/unit/item-wiring.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Phase 18 invariant' test/unit/foe-damage.test.js` prints 1 and `grep -c 'stripComments' test/unit/foe-damage.test.js` prints at least 2
    - `node --test test/unit/foe-damage.test.js` exits 0 reporting >= 22 tests passed, 0 failed (18 from 18-02 + 4 invariant tests)
    - `grep -c 'member.wp -= mDmg' test/unit/foe-damage.test.js` prints 1 (Test D's guard literal is present)
    - `git status --porcelain engine/ content/bestiary.js test/parity` prints nothing (this task changes only the test file)
  </acceptance_criteria>
  <done>The invariant test proves the seam is the only foe-wp decrement site and that the hero/member decrements are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and paste the AFTER table (+ its consistency test), capture the tune-difficulty AFTER readout, write the change ledger, and run the phase gate</name>
  <files>content/BESTIARY-REBALANCE.md, test/unit/bestiary-yardstick.test.js</files>
  <read_first>
    - content/BESTIARY-REBALANCE.md (18-01 output — the BEFORE readout format, the Review Verdicts table, the three `Filled by 18-06` placeholders and the AFTER marker pair)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-05-SUMMARY.md (the ten applied changes) and 18-03/18-04 SUMMARYs (routed sites, slow)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-CONTEXT.md D-04, D-05, D-06, D-14, D-15, D-16, D-18, D-19
    - tools/tune-difficulty.mjs lines 195-228 (readout lines to transcribe) and tools/bestiary-yardstick.mjs header (regeneration commands)
    - test/unit/bestiary-yardstick.test.js (18-01 output — it already imports computeYardstick/toMarkdown and BESTIARY; the consistency test is appended here)
    - test/parity/fixture-inventory.test.js (how it slices the generated block between marker comments and compares to the live generator output — mirror this for the AFTER block)
  </read_first>
  <action>
    Confirm every wave-2 plan has landed (`git log --oneline e01ac46..HEAD` shows the 18-02..18-05 commits) and the tree is clean apart from this plan. Run `node tools/bestiary-yardstick.mjs` (canon mode, the default) and paste its table VERBATIM between the `<!-- yardstick:after:begin -->` / `<!-- yardstick:after:end -->` markers, replacing the placeholder line; precede it (outside the markers) with one sentence: generated from the rebalanced `content/bestiary.js` with `--mechanics=canon` (ar soak, slow, halfDmg modelled — the engine as of Phase 18). Run `node tools/tune-difficulty.mjs --seeds=200` (~5-6 minutes) and replace the tune AFTER placeholder with the death-depth line, action-count line, outcome line and top-5 death causes, plus one comparison sentence versus the BEFORE readout (state plainly if the distributions are within noise of each other — the bot fights melee-only with a starting kit, so most changed creatures are rarely reached; this is expected and is exactly why D-16 calls it informational, not a gate).

    Replace the "Change ledger (filled by 18-06)" placeholder with a table `Creature | Tier / Type | Field | Before | After | Prototype-mode TTK xmed after | Lethality xmed after | Decision | Note` containing these rows (take the measured ratios from the pasted tables; the expected values, +-0.02, are): Drake T4 wp 135 → 38, 2.05x, 1.59x, D-18; Werebeast T3 dmg 1d10+5 → 1d10+0 (note updated), 2.00x, 1.94x, D-18; Djinni T4 wp 86 → 65 + dmg none → 1d4, 3.51x, 0.95x, D-03 pre-ability discount — revisit Phase 21; Djinni T5 same, 1.62x; Krupke T2 wp 23 → 17, dmg 1d8+2 → 1d6+2, 1.42x, D-03; Drudge T4 wp 12 → 9, 0.49x, D-03 HP-only; Drudge T5 wp 12 → 9, 0.22x; Vampire T5 wp 95 → 71 + 1d4, 1.77x, 1.93x, D-03; Stalka Beast T5 wp 125 → 94 + 1d4, 2.35x, 1.93x, D-03 (still over 2x — the discount is the locked treatment; Phase 21 owns the rest). Then an "Unchanged by decision" sub-table: Sterling wp 35 (D-19) canon TTK 10.23 / 4.38x — two hearts, canon-intended, revisit Phase 21; Dante (D-14) 4.00x / 3.00x — fixture-exposed, deferred to Phase 21; Cave Bear 2.08x borderline (research-reviewed, Phase 21); Herman T4 lethality 2.10x — canon-explicit flat 25; the five `sp.ar` rows with their canon-mode melee TTK from the AFTER table (Drat 5.93 / 2.54x, Krupke 7.34 / 2.22x, Craig 3.37 / 2.36x, Herman T4 5.06 / 3.54x, Google 9.54 / 2.88x) — "canon natural armor (D-05); melee yardstick only, spells bypass (D-06); revisit Phase 21"; Philly canon TTK 3.84 / 1.09x — resolved by CANON-05 slow; Ghost/Spectre infinite by design; the under-tier list unchanged. Add one line noting the canon-mode tier-5 TTK median moves (Herman's armored TTK becomes the T5 middle value) so ratios in the AFTER table are relative to the canon-mode medians printed in its header, while the ledger's prototype-mode ratios use the stable prototype medians.

    Update the "Parity carve-outs (BEST-03 / FID-05)" section from planned to measured: the regenerated-fixture set is EMPTY; `git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt` exits 0; `node --test "test/parity/**/*.test.js"` 30/30; fixture-inventory pin unchanged; FULL_FIGHTS pins 12/101/111/66/32 unchanged; the Section 3 draw pins show +1 only for sp.ar / sp.slow. Add a "Regenerating this document" checklist (before block = historical snapshot at e01ac46, never regenerated; after block = `node tools/bestiary-yardstick.mjs` pasted between the markers; the consistency test enforces it).

    Then append to `test/unit/bestiary-yardstick.test.js` a test "BESTIARY-REBALANCE.md AFTER block matches the live canon-mode yardstick (D-04 doc consistency)": read `content/BESTIARY-REBALANCE.md` (resolve from REPO_ROOT via import.meta.url), slice the lines strictly between `<!-- yardstick:after:begin -->` and `<!-- yardstick:after:end -->`, trim trailing whitespace per line and the block's outer blank lines, and `assert.equal` it to `toMarkdown(computeYardstick(BESTIARY, { mechanics: "canon" }))` normalized the same way. Failure message: "content/BESTIARY-REBALANCE.md's AFTER block drifted from tools/bestiary-yardstick.mjs — regenerate with `node tools/bestiary-yardstick.mjs` and paste verbatim between the markers".

    Finally run the phase gate: `npm test` (green), `node --test "test/parity/**/*.test.js"` (30 pass), and the frozen-files diff command; record the final `npm test` pass count in the SUMMARY.
  </action>
  <verify>
    <automated>node --test test/unit/bestiary-yardstick.test.js test/unit/foe-damage.test.js test/parity/fixture-inventory.test.js && node --test "test/parity/**/*.test.js" && git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt engine/difficulty.js package.json package-lock.json && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Filled by 18-06' content/BESTIARY-REBALANCE.md` prints 0 (all three placeholders replaced)
    - `awk '/yardstick:after:begin/{p=1;next} /yardstick:after:end/{p=0} p' content/BESTIARY-REBALANCE.md | grep -c '^| '` prints 55 and `grep -c 'yardstick:after:begin' test/unit/bestiary-yardstick.test.js` prints 1 and `node --test test/unit/bestiary-yardstick.test.js` exits 0 (AFTER block byte-consistent with the live canon output)
    - `grep -c 'Change ledger' content/BESTIARY-REBALANCE.md` prints at least 1 and `grep -c '135 → 38\|135 -> 38' content/BESTIARY-REBALANCE.md` prints at least 2 (verdict + ledger)
    - `grep -c '10.23' content/BESTIARY-REBALANCE.md` prints at least 1 and `grep -c '5.93\|9.54' content/BESTIARY-REBALANCE.md` prints at least 2 (Sterling and the ar creatures' canon TTKs recorded)
    - `grep -c 'Death-depth distribution\|p50=' content/BESTIARY-REBALANCE.md` prints at least 4 (BEFORE and AFTER readouts both transcribed)
    - `grep -ci 'zero carve-outs\|no carve-out' content/BESTIARY-REBALANCE.md` prints at least 1 and `grep -c 'EMPTY\|empty' content/BESTIARY-REBALANCE.md` prints at least 1
    - `node --test test/unit/foe-damage.test.js` exits 0 (>= 22) and `node --test test/parity/fixture-inventory.test.js` exits 0
    - `node --test "test/parity/**/*.test.js"` exits 0 (30 pass) and `git diff --quiet e01ac46 -- test/parity/fixtures test/parity/harness/comparables.js test/parity/prototype-master.js.txt engine/difficulty.js package.json package-lock.json` exits 0
    - `npm test` exits 0 with 0 failing and at least 724 + 10 (18-01) + 18 (18-02) + 20 (18-03) + 8 (18-04) + 11 (18-05) + 5 (this plan) = 796 passing — record the exact number
    - `git diff -U0 e01ac46 -- test/unit/bestiary-yardstick.test.js | grep -c '^-.*assert'` prints 0 (no 18-01 pin edited) and `git status --porcelain engine/ content/bestiary.js` prints nothing (no engine or number change in this plan)
  </acceptance_criteria>
  <done>The stat-table document is complete and machine-checked (BEFORE snapshot, generated AFTER table, both tune readouts, the change ledger with measured ratios and deferred canon consequences, the measured zero-carve-out statement); the invariant and consistency tests are green; the phase gate proves byte-identical parity with no frozen file touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → invariant test | The test reads engine source files from the checkout; no external input |
| doc ↔ generated output | The consistency test binds the committed markdown to the committed script + content — drift in either direction fails |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Tampering | Frozen master / fixtures / comparables edited anywhere in the phase | high | mitigate | Phase-gate `git diff --quiet e01ac46 -- ...` in this plan's verify and acceptance; 30/30 parity |
| T-18-02 | Tampering | A future direct foe-wp decrement bypassing the seam (and its gates) reintroducing an ungated draw path | high | mitigate | The invariant test fails on any foe-side decrement outside engine/foeDamage.js; documented as the promote decision |
| T-18-05 | Repudiation | Stat table drifting from code (hand-edited AFTER numbers) | medium | mitigate | AFTER-block consistency test; regeneration checklist in the doc |
| T-18-04 | Elevation of Privilege | Test reading files outside the repo | low | accept | Paths are resolved from import.meta.url to REPO_ROOT and fixed engine/content filenames; no user input |
| T-18-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; manifests unchanged (gate) |
</threat_model>

<verification>
- `node --test test/unit/foe-damage.test.js` >= 22 pass (invariant: 0 foe-side decrements outside the seam, exactly 1 inside).
- `node --test test/unit/bestiary-yardstick.test.js` green including the AFTER-block consistency test.
- `node --test "test/parity/**/*.test.js"` 30/30 byte-identical; frozen files unchanged since e01ac46; `npm test` green (record the count).
- `content/BESTIARY-REBALANCE.md` has no remaining placeholder, both tune readouts, the AFTER table, and the change ledger with the deferred canon consequences.
</verification>

<success_criteria>
- ROADMAP Phase 18 success criterion 1 fully satisfied (before/after table committed, every row reviewed, outliers corrected or explicitly dispositioned).
- Criterion 4 satisfied as measured fact: zero carve-outs, zero regenerations, parity byte-identical.
- The seam is a maintained invariant, ready for Phase 19's ability resolver; Phase 21 has the yardstick, both tune readouts, and the recorded canon-mode consequences (Sterling, the five armored creatures) to start its retune from.
</success_criteria>

<output>
Create `.planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md` when done (record the final npm test count, the AFTER-table medians, the tune AFTER readout lines, and the ledger's measured ratios; summarize the stat table per D-04).
</output>
