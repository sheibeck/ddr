---
phase: 21-consolidated-difficulty-retune
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - engine/difficulty.js
  - engine/encounters.js
  - engine/combat.js
  - engine/movement.js
  - test/unit/combat-scaling.test.js
  - docs/DIFFICULTY-RETUNE.md
autonomous: true
requirements: [TUNE-03]
tags: [engine, difficulty-curve, retune, economy, party, ledger, background-readout, node-test]

must_haves:
  truths:
    - "TUNE-03 (D-09/D-11): the retune is ONE committed pass with at most TWO harness iterations — the ledger's change table lists every constant that moved (file, before, after, rationale, decision id) and the three AFTER readouts (solo / --party / economy, 200 seeds, IDENTICAL bot parameters to BEFORE) are transcribed verbatim, followed by a comparison table against the four D-09 targets with a met / missed verdict per target"
    - "TUNE-03 (D-01/D-02/D-03 dials): FOE_CAP_MAX, FOE_POWER_MAX and ABILITY_THREAT_MAX are now strictly above their BASE (the curve actually scales) and the depth-6 first-divergence probe holds: difficultyCurve(5).foePower === 1 and .abilityThreat === 1 exactly, difficultyCurve(6).foePower > 1 and .abilityThreat > 1, difficultyCurve(1..5) still deep-equals the identity object, foeCap reaches FOE_CAP_MAX at some depth ≤ 100 and foeBonus === foeCap - 3 everywhere"
    - "TUNE-03 (D-19 parity): every constant change is identity at depth ≤ 5 — the D-15 seeds (test/determinism/foe-abilities.test.js), the FID-02 pins and the parity suite (30/30) pass with ZERO edits; no flat constant on a fixture path (LOOT_DIVISOR, the killFoe purse table, base prices) changes"
    - "TUNE-03 (D-10 as narrowed by D-21, economy): the tune-economy AFTER readout is compared to BEFORE; IF its peak-wilmst p50 exceeds 5000 (ten long swords — the flat store has nothing to sell for that), the three depth-linear wilmst sources (chest, wilmst-cache row, parley Humans amount) are routed through a new curve field lootDepth = min(depth, 5) + round(softCapFloat(0, LOOT_DEPTH_CAP - 5, over, LOOT_DEPTH_SOFT_K)) — identity (lootDepth === depth) at depth ≤ 5 — and the readout is re-taken; otherwise the ledger records 'no economy change needed' with the numbers; either way the Phase-20-deferred parley Humans AMOUNT is explicitly resolved in the ledger"
    - "TUNE-03 (D-12/D-20, party): the --party AFTER readout is compared to the solo AFTER readout; IF the --party median death depth exceeds 1.5 × the solo median, a memberUpkeepScale curve field (1 at depth ≤ 5) multiplies each member's upkeep() in newDay's party loop (round(upkeep(m) * scale), still behind state.party?.length, hero upkeep untouched) and the readout is re-taken; otherwise the ledger records 'PARTY-06 XP-share damping suffices — no upkeep change'; there is no hire cost to retune (D-20) and this is stated"
    - "TUNE-03 (D-21): no store price scaling is added — the ledger's 'Not changed, and why' section says it does not exist and was not built; the section also covers hire cost (does not exist), LOOT_DIVISOR / purse table (fixture paths, flat), bestiary base stats (Phase 18 ledger stands), summoned reinforcements (unscaled), class/race/sub features"
    - "Readouts are proxies: the D-09 comparison lives in the ledger and is judged by the executor; no test asserts a readout number; a missed target after two iterations is RECORDED (with the executor's one-line reading of why) and left to the DR round (D-15/D-16), never chased with a third iteration"
    - "Every long readout is a background run with an EXIT= sentinel polled in bounded checks; the executor never waits in the foreground"
    - "npm test fully green at the end of the plan (948 + the new curve tests); parity 30/30; frozen files untouched; the 21-03 dev path and the tuning tools are not modified"
  artifacts:
    - path: "engine/difficulty.js"
      provides: "the retuned FOE_CAP_MAX / FOE_POWER_MAX / ABILITY_THREAT_MAX (+ any k / dot / dark change), a DELIBERATE RULES CHANGE (Phase 21, TUNE-03) block naming the ledger; optional LOOT_DEPTH_CAP / LOOT_DEPTH_SOFT_K + lootDepth field; optional MEMBER_UPKEEP_* + memberUpkeepScale field"
      contains: "DELIBERATE RULES CHANGE (Phase 21, TUNE-03"
    - path: "test/unit/combat-scaling.test.js"
      provides: "constant pins rewritten to the retuned values; depth-6 first-divergence test; cap-reached test; (optional) lootDepth / memberUpkeepScale identity + monotone tests"
      contains: "difficultyCurve(6).foePower > 1"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "Change table, Iteration log, AFTER readouts (3, verbatim), Comparison vs D-09, Not changed and why — the 21-04 half of the ledger"
      contains: "## AFTER readout"
    - path: "engine/encounters.js"
      provides: "(only if the economy trigger fires) chest + wilmst-cache amounts read difficultyCurve(state.floor.depth).lootDepth"
      contains: "WILMST_CACHE_PER_DEPTH"
    - path: "engine/combat.js"
      provides: "(only if the economy trigger fires) parley's Humans wilmst amount reads lootDepth"
      contains: "rng.d(6) * 100 *"
    - path: "engine/movement.js"
      provides: "(only if the party trigger fires) newDay's member upkeep multiplied by memberUpkeepScale"
      contains: "cost += "
  key_links:
    - from: "engine/difficulty.js retuned constants"
      to: "startCombat / foeAbilities (already wired in 21-02)"
      via: "no code change in the consumers — the constants are the dials; difficultyCurve(30).foeCap / foePower / abilityThreat now differ from identity"
      pattern: "^export const FOE_POWER_MAX = "
    - from: "docs/DIFFICULTY-RETUNE.md AFTER readouts"
      to: "the three background runs against the retuned engine"
      via: "verbatim transcripts with EXIT=0, same seeds / exploreBudget / maxActions / thresholds as BEFORE (the Bot: line inside each transcript proves it)"
      pattern: "Bot: exploreBudget=50  maxActions=20000"
    - from: "(conditional) engine/encounters.js + engine/combat.js wilmst amounts"
      to: "difficultyCurve(...).lootDepth"
      via: "replace the raw state.floor.depth factor in exactly three amount expressions"
      pattern: "\\.lootDepth"
    - from: "(conditional) engine/movement.js newDay party loop"
      to: "difficultyCurve(...).memberUpkeepScale"
      via: "cost += Math.round(upkeep(m) * scale) inside if (state.party?.length)"
      pattern: "memberUpkeepScale"
  prohibitions:
    - requirement_id: TUNE-03
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, fixture JSON, comparables.js, test/determinism/foe-abilities.test.js or test/unit/foe-turn-draw-count.test.js; MUST NOT regenerate a fixture — every change is identity at depth ≤ 5 by construction"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js exits 0; git diff --quiet $BASE -- test/parity/harness/comparables.js exits 0 (BASE = HEAD at the start of this plan, recorded in the SUMMARY); parity 30/30"
    - requirement_id: TUNE-03
      category: values
      statement: "MUST NOT change LOOT_DIVISOR, the killFoe purse table, any content/*.js price or bestiary stat, class/race/sub features, or add store price scaling / a hire cost (D-20/D-21) — deep-band economy is tuned ONLY through the depth-gated lootDepth routing of the three depth-linear sources"
      status: flagged-unverified
      verification: "git diff --quiet $BASE -- content engine/items.js engine/economy.js exits 0; grep -c 'export const LOOT_DIVISOR = 10;' engine/items.js prints 1"
    - requirement_id: TUNE-03
      category: values
      statement: "MUST NOT change any bot parameter or tool between BEFORE and AFTER (tools/ untouched; --seeds=200, exploreBudget 50, maxActions 20000 or the cap recorded in the BEFORE table, thresholds 0.3/0.5/0.5/0.5)"
      status: flagged-unverified
      verification: "git diff --quiet $BASE -- tools exits 0; every AFTER transcript's Bot: line equals the BEFORE transcripts' Bot: line"
    - requirement_id: TUNE-03
      category: safety
      statement: "MUST NOT run more than two harness iterations (D-11) and MUST NOT add a rng draw anywhere (all economy/party routing is post-draw arithmetic on already-drawn dice)"
      status: flagged-unverified
      verification: "ledger Iteration log has ≤ 2 entries; FID-02 pins and parity unchanged; test/determinism/same-seed-same-result.test.js green"
    - requirement_id: TUNE-03
      category: safety
      statement: "MUST NOT touch engine/state.js, engine/saveState.js, src/browser/, mazeworld.html or tools/ in this plan"
      status: flagged-unverified
      verification: "git diff --quiet $BASE -- engine/state.js engine/saveState.js src mazeworld.html tools exits 0"
---

<objective>
Run the ONE retune (TUNE-03): set the combat dials in `engine/difficulty.js` (foe count / power / ability threat by depth — D-01/D-02/D-03), take the AFTER readouts with the upgraded bot under the exact BEFORE parameters, compare against the D-09 sanity floor, iterate at most once more (D-11), and — only if the readouts demand it — apply the two sanctioned, depth-gated counterweights (economy: `lootDepth` routing of the three depth-linear wilmst sources, D-10/D-21; party: `memberUpkeepScale` on member upkeep, D-12/D-20). Commit the ledger's change table, AFTER transcripts, D-09 comparison and "not changed, and why".

Purpose: this is the milestone's single tuning exercise — it closes PARTY-10, ECON deep tuning and Phase 3 feel-tuning in one recorded pass; the numbers earn the right to ask for the DR round (21-05), they do not replace it.

Output: `engine/difficulty.js` (dials + optional fields), `test/unit/combat-scaling.test.js` (pins + divergence tests), `docs/DIFFICULTY-RETUNE.md` (21-04 half), and — conditionally — `engine/encounters.js`, `engine/combat.js`, `engine/movement.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md
@.planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md
@.planning/phases/21-consolidated-difficulty-retune/21-01-SUMMARY.md
@.planning/phases/21-consolidated-difficulty-retune/21-02-SUMMARY.md
@engine/difficulty.js
@docs/DIFFICULTY-RETUNE.md
@test/unit/combat-scaling.test.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| Shared bot module + flags + tests; ledger BEFORE half | tooling / ledger | 21-01 | landed |
| Curve fields/helpers + wiring (identity constants); combat-scaling tests | engine | 21-02 | landed |
| Dev start-at-depth (engine, adapter, Settings, build stamp) | engine / client | 21-03 | landed |
| Retuned `FOE_CAP_MAX`, `FOE_POWER_MAX`, `ABILITY_THREAT_MAX` (+ `*_SOFT_K`, `ENCOUNTER_DOT_*` / `DARK_*` if moved) with a "DELIBERATE RULES CHANGE (Phase 21, TUNE-03)" block | engine | **21-04** | D-01/D-02/D-03/D-09/D-11 |
| Optional `LOOT_DEPTH_CAP`, `LOOT_DEPTH_SOFT_K`, curve field `lootDepth`; chest / wilmst-cache / parley-Humans amounts read it | engine | **21-04** (conditional) | D-10/D-21; identity ≤ 5 |
| Optional `MEMBER_UPKEEP_SCALE_MAX`, `MEMBER_UPKEEP_SOFT_K`, curve field `memberUpkeepScale`; newDay member loop reads it | engine | **21-04** (conditional) | D-12/D-20; behind `state.party?.length` |
| `test/unit/combat-scaling.test.js`: retuned pins, depth-6 divergence, cap-reached, optional lootDepth / memberUpkeepScale tests | tests | **21-04** | — |
| Ledger: Change table, Iteration log, AFTER readouts ×3, Comparison vs D-09, Not changed and why | ledger | **21-04** | D-08/D-11 |
| DR checklist + debug-build note; phase gate | ledger / gate | 21-05 | — |

## Dials and triggers (transcribed from CONTEXT.md; starting values are Claude's Discretion — record the landed values in the ledger)

| Dial | Identity (≤ 5) | Iteration-1 starting value | Decision |
|---|---|---|---|
| `FOE_CAP_MAX` (`foeCap` → count ceiling; `foeBonus = foeCap − 3`) | 3 | 5, `FOE_CAP_SOFT_K` 20 (≈ 4 from depth 15, 5 by ≈ depth 33) | D-02 / D-17 |
| `FOE_POWER_MAX` (`foePower` → wp/maxWP ×, flat melee bonus) | 1.0 | 1.6, `FOE_POWER_SOFT_K` 25 (≈ 1.35 at depth 20, ≈ 1.5 at depth 40) | D-02 |
| `ABILITY_THREAT_MAX` (`abilityThreat` → every ÷, uses ×) | 1.0 | 2.0, `ABILITY_THREAT_SOFT_K` 20 (every 2 → 1 from ≈ depth 25) | D-03 |
| `FOE_LVL_BIAS` | 0 | 0 (reserved) | D-01 |
| `ENCOUNTER_DOT_*`, `DARK_*` | as today | unchanged unless actions-per-floor misses | D-10 |
| Economy trigger | — | tune-economy AFTER `peakGold p50 > 5000` → `lootDepth` (cap 15, k 15) | D-10 / D-21 |
| Party trigger | — | `--party` AFTER death-depth p50 > 1.5 × solo p50 → `memberUpkeepScale` (max 2, k 20) | D-12 / D-20 |
| Iterations | — | ≤ 2, then stop and record | D-11 |

**What to turn if a D-09 target misses (iteration 2 only):** median < 8 or p90 < 25 → raise `FOE_POWER_SOFT_K` / `ABILITY_THREAT_SOFT_K` (slower ramp) before lowering a MAX; median > 15 or > 2% past floor 50 → lower `FOE_POWER_SOFT_K` (faster ramp) or raise `FOE_POWER_MAX` by 0.1; median actions per floor > 80 → lower `ENCOUNTER_DOT_CAP` (fewer fights per floor — the bot's exploreBudget may NOT change); < 40 → leave it (the bot beelines by design; note it). One rationale sentence per knob in the ledger.

<tasks>

<task type="auto">
  <name>Task 1: Iteration 1 — set the combat dials, update the curve tests, take the three AFTER readouts in the background, and record the change table + iteration-1 comparison in the ledger</name>
  <files>engine/difficulty.js, test/unit/combat-scaling.test.js, docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-01, D-02, D-03, D-09, D-10, D-11, D-19, "Claude's Discretion" (constants / k values), "Specific Ideas" (keep floors 1–3 dangerous but readable; spend the effort on 6–50 scaling)
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — Pattern 1 (illustrative constants — NOT locked), Pitfall 4 (runtime), Assumption A5
    - docs/DIFFICULTY-RETUNE.md (whole file — the BEFORE transcripts, the targets table, the bot-parameter table; the placeholder headings you now fill)
    - engine/difficulty.js (the constant block landed by 21-02; the header paragraph that names this ledger)
    - test/unit/combat-scaling.test.js (test 2's constant pins — the comment says 21-04 rewrites the three MAX values; tests 3/4/8 already use the constants symbolically)
    - .planning/phases/21-consolidated-difficulty-retune/21-01-SUMMARY.md (run durations and the polling cadence that worked; the exact CLI lines used for BEFORE)
  </read_first>
  <action>
    0. Record `BASE=$(git rev-parse HEAD)` (the 21-03 final commit) in your notes — every "untouched" gate in this plan diffs against it.

    1. In `engine/difficulty.js` set `FOE_CAP_MAX = 5`, `FOE_POWER_MAX = 1.6`, `ABILITY_THREAT_MAX = 2.0` (keep `FOE_CAP_SOFT_K = 20`, `FOE_POWER_SOFT_K = 25`, `ABILITY_THREAT_SOFT_K = 20`, `FOE_LVL_BIAS = 0` unless iteration 2 says otherwise). Replace each constant's identity placeholder comment (the one 21-02 wrote) with its intent (D-02: bigger fights deep — ≈ 4 foes from the mid-teens, 5 by the low thirties; D-02: ≈ +35% hit points and flat damage at depth 20, ≈ +50% at 40, never above +60%; D-03: a caster kit's `every: 2` becomes every visit from ≈ depth 25, `uses` doubles). Add one "DELIBERATE RULES CHANGE (Phase 21, TUNE-03, D-09/D-11)" block above the constant group: the values were set by the ONE consolidated retune against the 200-seed bot readout (docs/DIFFICULTY-RETUNE.md — change table + AFTER), identity at depth ≤ 5 by construction (`over`), and a D-16 follow-up may edit ONLY these constants (+ a ledger addendum).

    2. In `test/unit/combat-scaling.test.js`: rewrite test 2's three MAX pins to 5 / 1.6 / 2.0 (title: "21-04 retune values — recorded in docs/DIFFICULTY-RETUNE.md"); add test 13 "depth-6 first divergence (D-19): identity ends exactly at depth 5" — `Object.is(difficultyCurve(5).foePower, 1)`, `Object.is(difficultyCurve(5).abilityThreat, 1)`, `difficultyCurve(5).foeCap === 3`, `difficultyCurve(6).foePower > 1`, `difficultyCurve(6).abilityThreat > 1`, and the depth 1..5 deep-equal identity object re-asserted; add test 14 "the caps are reached: foeCap === FOE_CAP_MAX at some depth ≤ 100; foePower and abilityThreat are within 0.05 of their MAX at depth 200" (guarded by `if (MAX > BASE)` so a future identity reset does not fail it); add test 15 "startCombat at depth 30 builds more than 3 foes when the canon roll is 3 and foeBonus ≥ 1" — fixedState level 5, forced Beasts, fakeRng([3, 4, 2, 2, 2, 2, 2, 10, 5]) (d4=3 then d4=4 → canon 3): `foes.length === foeCountFor(3, difficultyCurve(30))` and `> 3` when `difficultyCurve(30).foeBonus >= 1`, every foe `wp === foeWpFor(BESTIARY.Beasts[f.lvl - 1][0].wp, difficultyCurve(30))`, every foe has `dmgBonus === foeDmgBonusFor(f.lvl, difficultyCurve(30))` when that is > 0.

    3. Run `npm test` (must be green — the constants are identity ≤ 5, so nothing else moves), then confirm the tools are untouched (`git diff --quiet $BASE -- tools`) and launch the three AFTER runs in the background with EXACTLY the BEFORE command lines (from the ledger's bot-parameter table / 21-01-SUMMARY): `node tools/tune-difficulty.mjs --seeds=200` → `<scratchpad>/retune-after1-solo.txt`, `node tools/tune-difficulty.mjs --seeds=200 --party` → `<scratchpad>/retune-after1-party.txt`, `node tools/tune-economy.mjs --seeds=200` → `<scratchpad>/retune-after1-economy.txt`, each with a trailing `EXIT=<code>` line; poll in bounded ≤ 120 s checks (never a foreground sleep). While they run, write the ledger sections below.

    4. In `docs/DIFFICULTY-RETUNE.md` replace the `## Change table (21-04)` placeholder with a table (Knob | File | Before | After | Rationale | Decision) listing: FOE_CAP_MAX 3→5, FOE_POWER_MAX 1.0→1.6, ABILITY_THREAT_MAX 1.0→2.0, the three k values (unchanged or changed), FOE_LVL_BIAS 0→0 (reserved, unused), ENCOUNTER_DOT_CAP / ENCOUNTER_DOT_SOFT_K / DARK_* (unchanged unless moved), and rows for the two conditional counterweights marked "pending trigger (Task 2)". Add a `### Iteration log` subsection under it with an "Iteration 1" entry: the constants, the three run dates/durations, and — once the transcripts are in — the four D-09 numbers (median death depth, p90, % past 50, median actions per floor) each marked met / missed, plus the economy trigger value (peakGold p50) and the party trigger value (--party p50 vs solo p50 × 1.5). Replace `## AFTER readout (21-04)` with `## AFTER readout — retuned engine (iteration 1)` holding the three verbatim transcripts (`### tune-difficulty --seeds=200`, `### tune-difficulty --seeds=200 --party`, `### tune-economy --seeds=200`), each fenced, and a `**Headline AFTER numbers:**` paragraph in the same shape as the BEFORE one. Do not fill the Comparison / Not-changed sections yet (Task 2 finalises them so they describe the final state).
  </action>
  <verify>
    <automated>node --test test/unit/combat-scaling.test.js test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js && node --test "test/parity/**/*.test.js" && grep -c '^export const FOE_CAP_MAX = 5;' engine/difficulty.js | grep -qx 1 && grep -c '^export const FOE_POWER_MAX = 1.6;' engine/difficulty.js | grep -qx 1 && grep -c '^export const ABILITY_THREAT_MAX = 2.0;\|^export const ABILITY_THREAT_MAX = 2;' engine/difficulty.js | grep -qx 1 && grep -c 'retuned engine (iteration 1)' docs/DIFFICULTY-RETUNE.md | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'DELIBERATE RULES CHANGE (Phase 21, TUNE-03' engine/difficulty.js` prints >= 1 and `grep -c '21-02 IDENTITY' engine/difficulty.js` prints 0
    - `node -e "import('./engine/difficulty.js').then(m=>{const a=m.difficultyCurve(5),b=m.difficultyCurve(6),c=m.difficultyCurve(30);if(!Object.is(a.foePower,1)||!Object.is(a.abilityThreat,1)||a.foeCap!==3)process.exit(1);if(!(b.foePower>1)||!(b.abilityThreat>1))process.exit(2);if(!(c.foeCap>=4)||!(c.foePower>1.2)||!(c.abilityThreat>1.3))process.exit(3);console.log('dials ok')})"` prints dials ok
    - `node --test test/unit/combat-scaling.test.js` exits 0 with 15 passing tests and `grep -c 'difficultyCurve(6).foePower > 1' test/unit/combat-scaling.test.js` prints >= 1
    - `node --test test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js` exits 0 with ZERO edits (`git diff --quiet 04eb229 -- test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js` exits 0) and `node --test "test/parity/**/*.test.js"` reports 30 pass
    - `git diff --quiet $BASE -- tools test/parity/harness/comparables.js content engine/items.js engine/economy.js engine/state.js engine/saveState.js src mazeworld.html` exits 0 at the end of the task (BASE = HEAD before this plan's first edit)
    - `grep -c '^## Change table' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^### Iteration log' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c 'Iteration 1' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'FOE_POWER_MAX' docs/DIFFICULTY-RETUNE.md` prints >= 1
    - `grep -c '^## AFTER readout — retuned engine (iteration 1)' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c 'tune-difficulty: 200 seeded auto-play run' docs/DIFFICULTY-RETUNE.md` prints 4 and `grep -c 'tune-economy: 200 seeded auto-play run' docs/DIFFICULTY-RETUNE.md` prints 2 (BEFORE + iteration 1) and `grep -c 'Bot: exploreBudget=50  maxActions=20000' docs/DIFFICULTY-RETUNE.md` prints 6 (or the BEFORE cap value, consistently in all six)
    - the three scratchpad transcripts end with `EXIT=0`; the SUMMARY records durations and polling cadence
    - `npm test` exits 0 (948 + 3 = 951; record the exact number)
  </acceptance_criteria>
  <done>The dials are set with rationale, the curve tests pin the new values and the depth-6 divergence, parity/determinism are untouched and green, and the ledger holds the change table, the iteration-1 log and the three verbatim AFTER transcripts.</done>
</task>

<task type="auto">
  <name>Task 2: Iteration 2 (only if a D-09 target or a counterweight trigger fired) — apply the sanctioned depth-gated counterweights and/or turn the dials once more, re-take the readouts, then finalise the ledger (Comparison vs D-09, Not changed and why) and the plan gate</name>
  <files>engine/difficulty.js, engine/encounters.js, engine/combat.js, engine/movement.js, test/unit/combat-scaling.test.js, docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-09, D-10, D-11 (at most two iterations, then STOP), D-12, D-16, D-20, D-21
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — Pitfall 3 (no hire mechanic), Pitfall 5 (no store price scaling), the economy constants inventory in Sources (chest `((d10 + 6) * 100 * depth) / LOOT_DIVISOR` at encounters.js ~137; wilmst cache `WILMST_CACHE_PER_DEPTH * state.floor.depth` at ~297; parley `rng.d(6) * 100 * state.floor.depth` at combat.js ~712)
    - docs/DIFFICULTY-RETUNE.md — the iteration-1 numbers you recorded; docs/PARLEY-REBALANCE.md's "Comparison" section shape
    - engine/encounters.js lines 130-140 (openChest's gainWilmst amount), 291-298 (the wilmst-cache row); engine/combat.js the parley Humans wilmst line (~712, `rng.d(6) * 100 * state.floor.depth`) — combat.js already imports difficultyCurve since 21-02; engine/movement.js lines 379-412 (newDay: the `if (state.party?.length)` upkeep loop — `cost += upkeep(m);`), and its DETERMINISM GATE comment
    - engine/difficulty.js (the constant block + `over` + softCapFloat — the two optional fields use the same shape)
    - The "What to turn if a D-09 target misses" table in this plan's header
  </read_first>
  <action>
    Decide from the iteration-1 ledger numbers, in this order, and record each decision in the ledger's Iteration log as "Iteration 2 — triggered by …" or "Iteration 2 — not needed (all targets met, no trigger fired)":

    A. Economy trigger (D-10/D-21): if tune-economy AFTER `peakGold p50 > 5000`, add to `engine/difficulty.js` `export const LOOT_DEPTH_CAP = 15;` and `export const LOOT_DEPTH_SOFT_K = 15;` and the curve field `lootDepth: Math.min(d, COMBAT_SCALE_FROM_DEPTH - 1) + Math.round(softCapFloat(0, LOOT_DEPTH_CAP - (COMBAT_SCALE_FROM_DEPTH - 1), over, LOOT_DEPTH_SOFT_K))` (JSDoc: the effective depth the depth-LINEAR wilmst sources multiply by — identity `lootDepth === depth` for depth ≤ 5, soft-capped at LOOT_DEPTH_CAP; D-21: there is no store price scaling to add, so deep-band inflation is bounded at its sources instead). Route exactly three expressions through it: in `engine/encounters.js` (add `import { difficultyCurve } from "./difficulty.js";`) the chest amount `Math.round(((rng.d(10) + 6) * 100 * difficultyCurve(state.floor.depth).lootDepth) / LOOT_DIVISOR)` and the cache row `WILMST_CACHE_PER_DEPTH * difficultyCurve(state.floor.depth).lootDepth`; in `engine/combat.js#parley` the Humans amount `rng.d(6) * 100 * difficultyCurve(state.floor.depth).lootDepth`. Each site gets a one-line comment "Phase 21 (TUNE-03, D-10/D-21): lootDepth — identity at depth <= 5, post-draw arithmetic, 0 new draws". Add tests to combat-scaling.test.js: lootDepth === d for d 1..5; non-decreasing; `<= LOOT_DEPTH_CAP` at depth 1000; and (via a fixedState at depth 1) that openChest's / parley's gold at depth 1 is unchanged (fakeRng, exact amounts). If the trigger did NOT fire: no code change; the ledger's "Not changed, and why" says so with the number.

    B. Party trigger (D-12/D-20): if `--party` AFTER death-depth p50 > 1.5 × solo AFTER p50, add `export const MEMBER_UPKEEP_SCALE_MAX = 2;` and `export const MEMBER_UPKEEP_SOFT_K = 20;` and the curve field `memberUpkeepScale: softCapFloat(1, MEMBER_UPKEEP_SCALE_MAX, over, MEMBER_UPKEEP_SOFT_K)` (JSDoc: PARTY-10 counterweight — a party eats more the deeper it goes; hero upkeep untouched; identity 1 at depth ≤ 5). In `engine/movement.js#newDay`'s party loop (add `difficultyCurve` to the existing import from ./difficulty.js — it is already imported at line 29) change `cost += upkeep(m);` to `cost += Math.round(upkeep(m) * difficultyCurve(state.floor.depth).memberUpkeepScale);` with a comment "Phase 21 (TUNE-03, D-12/D-20): depth-scaled member upkeep — identity at depth <= 5, still behind state.party?.length, 0 draws; `eats` unchanged". Tests: memberUpkeepScale === 1 for d 1..5; non-decreasing; `<= MEMBER_UPKEEP_SCALE_MAX`; a newDay call with one party member at depth 1 costs exactly `upkeep(c) + upkeep(m)` (fakeRng) and at depth 40 costs `upkeep(c) + Math.round(upkeep(m) * difficultyCurve(40).memberUpkeepScale)`. If the trigger did NOT fire: no code change; the ledger records "PARTY-06 XP-share damping suffices; no hire cost exists (D-20)".

    C. D-09 misses: if any of the four targets missed in iteration 1, apply ONE adjustment per missed target from the "What to turn" table (rationale sentence each), update the affected constant pins in combat-scaling.test.js, and run `npm test`.

    D. If A, B or C changed anything: re-launch the three readouts in the background (`retune-after2-*.txt`, same command lines, EXIT sentinel, bounded polling), transcribe them verbatim under a new `## AFTER readout — retuned engine (iteration 2, final)` heading (keep iteration 1's transcripts above it), and add the "Iteration 2" log entry with the four D-09 numbers. If nothing changed, rename iteration 1's heading suffix to "(iteration 1, final)" and record "Iteration 2 — not needed".

    E. Finalise the ledger: replace `## Comparison vs D-09 (21-04)` with a table (Target | BEFORE | AFTER (final) | Met?) for the four D-09 targets plus informational rows (reach ≥10/≥20/≥30, caster-encounter rate in the deepest populated band, ability damage share, --party p50 vs solo p50, economy peak p50/p90), and a short paragraph: what the numbers say, what they cannot say (bot skill is arbitrary — PROXY, NOT A GATE), and — for every missed target — the executor's one-line reading and the statement that D-16 hands the decision to the DR round. Replace `## Not changed, and why (21-04)` with bullets: store price scaling (does not exist; not added — D-21), hire cost (does not exist — D-20), LOOT_DIVISOR / killFoe purse table / base prices (flat constants on fixture paths — D-11), bestiary base stats (Phase 18 ledger stands), summoned reinforcements (unscaled — 21-02 discretion), class/race/sub features (out of scope), the Phase-20-deferred parley Humans AMOUNT (resolved: kept as `d6 × 100 × depth` or routed through lootDepth — state which), the 18-06 "deferred to 21" canon-mode items (Deferred Ideas — DR round decides). Update the change table's two "pending trigger" rows to their outcome.

    F. Plan gate: `npm test` fully green; parity 30/30; `git diff --quiet 04eb229 -- <frozen files>`; `node tools/bestiary-yardstick.mjs` still runs (the yardstick is depth-independent — note that in the SUMMARY); the difficulty.js header lists every field.
  </action>
  <verify>
    <automated>npm test && node --test "test/parity/**/*.test.js" && git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js package.json package-lock.json && grep -c '^## Comparison vs D-09' docs/DIFFICULTY-RETUNE.md | grep -qx 1 && grep -c '^## Not changed, and why' docs/DIFFICULTY-RETUNE.md | grep -qx 1 && grep -c 'Iteration 2' docs/DIFFICULTY-RETUNE.md | grep -qE '^[1-9]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Iteration 2' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c '^## AFTER readout' docs/DIFFICULTY-RETUNE.md` prints 1 or 2 and exactly one AFTER heading contains "final"; `grep -c 'Iteration 3' docs/DIFFICULTY-RETUNE.md` prints 0
    - `grep -c '^## Comparison vs D-09' docs/DIFFICULTY-RETUNE.md` prints 1 and the section contains the strings "median death depth", "p90", "past floor 50", "actions per floor", "PROXY" (grep each >= 1) and `grep -c 'Filled by plan 21-04' docs/DIFFICULTY-RETUNE.md` prints 0
    - `grep -c '^## Not changed, and why' docs/DIFFICULTY-RETUNE.md` prints 1 and the section mentions "store price scaling", "hire cost", "LOOT_DIVISOR", "bestiary", "summoned", "parley Humans" (grep each >= 1)
    - IF lootDepth landed: `grep -c 'lootDepth' engine/difficulty.js` prints >= 2, `grep -c 'difficultyCurve(state.floor.depth).lootDepth' engine/encounters.js` prints 2 and `grep -c 'difficultyCurve(state.floor.depth).lootDepth' engine/combat.js` prints 1, `grep -v '^\s*//' engine/combat.js | grep -c 'rng.d(6) \* 100 \* state.floor.depth'` prints 0; ELSE `grep -c 'lootDepth' engine/difficulty.js engine/encounters.js engine/combat.js` prints 0 for all three and `git diff --quiet $BASE -- engine/encounters.js` exits 0
    - IF memberUpkeepScale landed: `grep -c 'memberUpkeepScale' engine/difficulty.js` prints >= 2 and `grep -c 'Math.round(upkeep(m) \* difficultyCurve(state.floor.depth).memberUpkeepScale)' engine/movement.js` prints 1 and `grep -c 'if (state.party?.length)' engine/movement.js` is unchanged; ELSE `grep -c 'memberUpkeepScale' engine/difficulty.js engine/movement.js` prints 0 for both
    - `grep -c 'export const LOOT_DIVISOR = 10;' engine/items.js` prints 1 and `git diff --quiet $BASE -- content engine/items.js engine/economy.js engine/state.js engine/saveState.js src mazeworld.html tools test/parity/harness/comparables.js` exits 0 (BASE = `git rev-parse HEAD` captured before this plan's first edit and recorded in the SUMMARY)
    - `node --test test/unit/combat-scaling.test.js` exits 0 (15 + any lootDepth/memberUpkeepScale tests); `npm test` exits 0 with every test passing (record the exact number); `node --test "test/parity/**/*.test.js"` 30 pass; `node --test test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js test/determinism/same-seed-same-result.test.js` exits 0
    - every AFTER transcript's `Bot:` line is byte-identical to the BEFORE transcripts' `Bot:` line (`grep 'Bot: exploreBudget' docs/DIFFICULTY-RETUNE.md | sort -u | wc -l` prints 1)
    - all scratchpad transcripts used end with `EXIT=0`; no readout was run in the foreground
  </acceptance_criteria>
  <done>The retune is finished within two iterations: dials landed with rationale, the sanctioned counterweights applied only where the readouts demanded them (or explicitly declined), the ledger's comparison and not-changed sections are complete and honest about any missed target, and the full suite plus parity and frozen-file gates are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| constants → fixture paths | A dial that is not identity at depth ≤ 5 silently desyncs the frozen prototype |
| readout → decision | Proxy numbers must inform, never gate; the DR round is the exit criterion |
| conditional routing → draw shape | Economy/party counterweights must be post-draw arithmetic |
| executor → long-running runs | Six background runs; the executor must not stall |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-17 | Tampering | A dial or counterweight leaks into depth ≤ 5 (e.g. `lootDepth` ≠ depth at depth 1), breaking chest/parley/combat fixtures | high | mitigate | every new field uses the same `over = max(0, d − 5)` argument; identity tests for d 1..5; parity 30/30; FID-02 + D-15 files byte-unchanged |
| T-21-18 | Tampering | Retune "success" manufactured by changing the bot between BEFORE and AFTER | medium | mitigate | tools/ untouched (git gate); `Bot:` line identical across all transcripts (`sort -u | wc -l` = 1) |
| T-21-19 | Repudiation | Missed D-09 targets hidden or chased with extra iterations | medium | mitigate | Iteration log capped at 2 (grep gate on "Iteration 3" = 0); comparison table requires a met/missed verdict per target; D-16 hand-off sentence |
| T-21-20 | Tampering | A new rng draw sneaks into an economy/party routing site | high | mitigate | routing replaces only the depth FACTOR inside existing expressions; FID-02 pins, same-seed test and parity unchanged |
| T-21-21 | Denial of Service | Six 200-seed runs stall the executor | medium | mitigate | background + `EXIT=` sentinel + ≤120 s polling; if a run must be capped with `--max-actions` the cap is recorded in the ledger and, because BEFORE ran uncapped, the comparison notes the mismatch and reads death-depth / reach only (never re-run BEFORE against a modified tree) |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs; `git diff --quiet 04eb229 -- package.json package-lock.json` |
</threat_model>

<verification>
- `npm test` fully green (record count); parity 30/30; D-15 / FID-02 files and fixtures byte-identical to 04eb229; tools/ and comparables untouched since 21-03.
- `difficultyCurve(5)` identity, `difficultyCurve(6)` diverged, `difficultyCurve(30)` foeCap ≥ 4 / foePower > 1.2 / abilityThreat > 1.3.
- Ledger complete: change table, ≤ 2 iteration entries, AFTER transcripts (final marked), Comparison vs D-09 with per-target verdicts, Not changed and why.
</verification>

<success_criteria>
- ROADMAP Phase 21 criterion 3: the curve is retuned once across monster power, ability threat, party power and economy, with harness runs recorded against the 5–10 minute / floor 30–50+ targets — numbers in the ledger, verdict per target, nothing hidden; parity intact.
</success_criteria>

<output>
Create `.planning/phases/21-consolidated-difficulty-retune/21-04-SUMMARY.md` when done (record: the final constant block, which triggers fired and what landed, the four D-09 numbers BEFORE/AFTER with verdicts, run durations/polling, the exact test count, and the frozen-file confirmations).
</output>
