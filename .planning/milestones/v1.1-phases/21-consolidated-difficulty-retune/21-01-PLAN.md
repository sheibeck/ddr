---
phase: 21-consolidated-difficulty-retune
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/lib/tuning-bot.mjs
  - tools/tune-difficulty.mjs
  - tools/tune-economy.mjs
  - test/unit/tuning-bot.test.js
  - docs/DIFFICULTY-RETUNE.md
autonomous: true
requirements: [TUNE-02]
tags: [tooling, tuning-harness, bot-policy, readout, ledger, node-test, no-engine-change]

must_haves:
  truths:
    - "TUNE-02 (D-23): ONE shared policy module tools/lib/tuning-bot.mjs exports decideAction/playRun/tallies/readout helpers and BOTH tools/tune-difficulty.mjs and tools/tune-economy.mjs import it — neither tool keeps a private decideAction/nearestUnseenDir/canStep copy (grep -c '^function decideAction' prints 0 in both tools)"
    - "TUNE-02 (D-05, cast probe): in combat, a Magic User with a castable kind:thrown spell in c.grimoire and maxCharges(c) - c.spellsUsed > 0 gets { type: castSpell, idx } (highest-lvl castable thrown spell); with zero charges left (spellsUsed === maxCharges) the same character attacks — no castSpell is ever emitted without a charge"
    - "TUNE-02 (D-05, potion probe): at wp/maxWP < 0.5 with c.potions > 0 the bot drinks (in or out of combat, unless the flee/parley branch fired first); with c.potions === 0 it never emits drinkPotion"
    - "TUNE-02 (D-05, camp probe): out of combat at wp/maxWP < 0.5 the bot camps only when c.rations >= (RACES[c.race].eats || 1); with rations 0 (or a Large race holding 1 ration) it moves instead — campFailed is never provoked"
    - "TUNE-02 (D-05, descend probe): when the floor has zero feat:dot cells OR ctx.floorActions >= opts.exploreBudget, the move direction is the BFS first step toward the feat:exit cell; otherwise it is the nearest-unseen step; the per-floor counter resets on a floorChanged event"
    - "TUNE-02 (D-06, caster probe): with any live foe carrying a non-empty abilities array the flee/parley threshold is 0.5; without one it is 0.3 — at ratio 0.4 the caster fight flees/parleys and the plain fight does not"
    - "TUNE-02 (D-07): both tools tally foeCast, foeBolted, foeDrained, foeDebuffed, foeHealed, foeSummoned (pending:true only), heroResisted, heroResistFailed, plus abilityDmg (hero-targeted foeBolted.dmg) vs meleeDmg (struckByFoe.dmg) and print the ability share of all damage taken, in text and --json"
    - "TUNE-02 (D-08): both tools print, in text and --json, the death-depth distribution PLUS a reach table (% of runs with deathDepth >= 5/10/20/30/50), the per-run actions-per-floor distribution (actions / deathDepth), and the caster-encounter rate per depth band (1-5, 6-10, 11-20, 21-30, 31-50, 51+)"
    - "TUNE-02 (D-12/D-20): --party forces exactly one member at run start via the documented harness-only meetJoiner/resolveJoiner bypass (state.party.length === 1 before the first action, deterministic per seed) and the readout reports member presence; without --party state.party stays [] and the bot declines every natural Joiner offer"
    - "D-08/D-11: docs/DIFFICULTY-RETUNE.md exists with the BEFORE readout (upgraded bot, post-Phase-20 engine at bd0ba7c, 200 seeds, solo + --party + economy) transcribed verbatim, the D-09 targets table, and the bot-parameter table; engine/, content/, src/ and mazeworld.html are byte-identical to bd0ba7c for the whole plan"
    - "Bot edge cases: a floor with no reachable unseen tile and no exit path falls back to a policyRng-picked legal direction (never null/undefined dir); seen climb/gorge/trap cells are routed around when any alternative path exists and crossed only when none does"
    - "Tuning tools remain proxies: no node:test file asserts on any readout NUMBER (only on decideAction/tally arithmetic with synthetic inputs); neither tool is wired into npm test"
    - "npm test fully green at the end of the plan (912 + the new tuning-bot tests); parity 30/30; frozen files unchanged"
  artifacts:
    - path: "tools/lib/tuning-bot.mjs"
      provides: "shared bot policy (D-05/D-06), observe/tally (D-07), playRun loop, forceParty (D-20), readout summarizers (D-08), BOT_DEFAULTS"
      exports: ["decideAction", "playRun", "forceParty", "makeBotContext", "makeTallies", "tallyEvents", "observe", "reachTable", "actionsPerFloorDist", "casterRateByBand", "abilitySummary", "partySummary", "sharedJson", "printSharedReadout", "nearestUnseenDir", "dirTowardExit", "dotsRemaining", "liveFoesHaveAbilities", "findCastableAttackSpell", "canStep", "legalDirs", "pickFallbackDir", "BOT_DEFAULTS", "DEPTH_BANDS", "REACH_FLOORS"]
      contains: "export function decideAction(state, policyRng, ctx)"
    - path: "tools/tune-difficulty.mjs"
      provides: "consumer of the shared module; --seeds/--json/--party/--max-actions/--explore-budget flags; D-07/D-08 readout blocks"
      contains: "from \"./lib/tuning-bot.mjs\""
    - path: "tools/tune-economy.mjs"
      provides: "consumer of the shared module; same flags; gold readout + the same D-07/D-08 blocks"
      contains: "from \"./lib/tuning-bot.mjs\""
    - path: "test/unit/tuning-bot.test.js"
      provides: "synthetic-state probes for every D-05/D-06 branch, the exit/hazard BFS, forceParty determinism, tally arithmetic"
      contains: "findCastableAttackSpell"
    - path: "docs/DIFFICULTY-RETUNE.md"
      provides: "the Phase 21 ledger: scope/method, D-09 targets, bot parameters, BEFORE readouts (3), placeholder headings for the 21-04/21-05 sections"
      contains: "## BEFORE readout"
  key_links:
    - from: "tools/tune-difficulty.mjs and tools/tune-economy.mjs"
      to: "tools/lib/tuning-bot.mjs"
      via: "import { playRun, forceParty, sharedJson, printSharedReadout, BOT_DEFAULTS } from \"./lib/tuning-bot.mjs\""
      pattern: "from \"\\./lib/tuning-bot\\.mjs\""
    - from: "tools/lib/tuning-bot.mjs decideAction"
      to: "engine public surface + read-only helpers"
      via: "canParley (engine/combat.js), canCast (engine/derived.js), maxCharges (engine/movement.js), SPELLS/RACES (content/index.js) — reads only; every state change still goes through applyAction"
      pattern: "import \\{ canParley \\} from \"\\.\\./\\.\\./engine/combat\\.js\""
    - from: "tools/lib/tuning-bot.mjs forceParty"
      to: "engine/encounters.js meetJoiner + resolveJoiner"
      via: "HARNESS-ONLY write-path bypass (applyStartCombat precedent in test/parity/harness/comparables.js), rng from makeRng(state.rngState), rngState written back"
      pattern: "meetJoiner\\(state, rng, events\\)"
    - from: "docs/DIFFICULTY-RETUNE.md BEFORE sections"
      to: "the three 200-seed background runs"
      via: "verbatim transcription of tune-difficulty (solo), tune-difficulty --party, tune-economy output captured with an EXIT=0 sentinel"
      pattern: "tune-difficulty: 200 seeded auto-play run"
  prohibitions:
    - requirement_id: TUNE-02
      category: safety
      statement: "MUST NOT modify any file under engine/, content/, src/ or mazeworld.html in this plan — the BEFORE readout is only meaningful against the untouched post-Phase-20 engine"
      status: flagged-unverified
      verification: "git diff --quiet bd0ba7c -- engine content src mazeworld.html exits 0 before the readout starts and again after the plan's last commit"
    - requirement_id: TUNE-02
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, test/parity/fixtures/*.json, test/parity/harness/comparables.js, test/determinism/foe-abilities.test.js or test/unit/foe-turn-draw-count.test.js"
      status: flagged-unverified
      verification: "git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js exits 0; git diff --quiet bd0ba7c -- test/parity/harness/comparables.js exits 0"
    - requirement_id: TUNE-02
      category: values
      statement: "MUST NOT turn either tuning tool (or the shared module) into a CI gate: no assertion on a readout number, no wiring into npm test, the PROXY-NOT-A-GATE header retained in both tools and added to the module"
      status: flagged-unverified
      verification: "grep -c 'TUNING PROXY' tools/tune-difficulty.mjs tools/tune-economy.mjs tools/lib/tuning-bot.mjs each print >= 1; grep -c 'deathDepth' test/unit/tuning-bot.test.js prints 0"
    - requirement_id: TUNE-02
      category: safety
      statement: "MUST NOT use Math.random or Date in the shared module or the tools' decision path — all bot randomness comes from the separate policyRng = makeRng(seed ^ 0x9e3779b9)"
      status: flagged-unverified
      verification: "grep -v '^\\s*//' tools/lib/tuning-bot.mjs | grep -c 'Math.random\\|Date.now' prints 0"
    - requirement_id: TUNE-02
      category: values
      statement: "MUST NOT add store-shopping or hiring policies to the bot (Deferred Ideas) — leaveStore stays the only store action; natural Joiner offers are declined; --party is the only member source"
      status: flagged-unverified
      verification: "grep -c '\"buyItem\"\\|\"sellItem\"' tools/lib/tuning-bot.mjs prints 0; grep -c 'accept: false' tools/lib/tuning-bot.mjs prints >= 1"
---

<objective>
Turn the two tuning scripts from a blind, attack-only proxy (200-seed death depth p50 = 1) into a usable one BEFORE any engine number moves: extract the bot into one shared module (D-23) with the four D-05 policies (cast / drink / camp / descend-when-cleared-or-budgeted), the D-06 caster-aware flee threshold, the D-07 ability tallies, the D-08 reach-table / actions-per-floor / caster-rate readout, and the D-12/D-20 `--party` flag; then capture the BEFORE readout (three 200-seed background runs against the untouched post-Phase-20 engine) verbatim into the new ledger `docs/DIFFICULTY-RETUNE.md` together with the D-09 targets.

Purpose: TUNE-02 is the precondition for TUNE-03 — the retune in 21-04 is compared BEFORE vs AFTER with the SAME bot and the SAME parameters, so the bot must be finished and its BEFORE numbers frozen before the first constant changes.

Output: `tools/lib/tuning-bot.mjs` (new), `tools/tune-difficulty.mjs` + `tools/tune-economy.mjs` (rewired), `test/unit/tuning-bot.test.js` (new), `docs/DIFFICULTY-RETUNE.md` (new, BEFORE half).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md
@.planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md
@.planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md
@tools/tune-difficulty.mjs
@tools/tune-economy.mjs
@docs/PARLEY-REBALANCE.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `tools/lib/tuning-bot.mjs` — `decideAction(state, policyRng, ctx)`, `playRun(seed, opts, onStep)`, `forceParty(state)`, `makeBotContext`, `observe`, `makeTallies`/`tallyEvents`, `reachTable`/`actionsPerFloorDist`/`casterRateByBand`/`abilitySummary`/`partySummary`, `sharedJson`/`printSharedReadout`, BFS helpers, `BOT_DEFAULTS`, `DEPTH_BANDS`, `REACH_FLOORS` | dev tooling (shared module) | **21-01** | D-05/D-06/D-07/D-08/D-20/D-23 |
| CLI flags `--party`, `--max-actions=N`, `--explore-budget=N` on both tools (alongside `--seeds=N`, `--json`) | dev tooling | **21-01** | D-12/D-20; proxy parameters recorded in the ledger |
| `test/unit/tuning-bot.test.js` | tests | **21-01** | synthetic-state probes; no readout numbers |
| `docs/DIFFICULTY-RETUNE.md` — scope/method, D-09 targets, bot parameters, BEFORE readouts | ledger | **21-01** (BEFORE), 21-04 (change table / AFTER / comparison), 21-05 (DR checklist) | mirrors docs/PARLEY-REBALANCE.md |
| `difficultyCurve` fields `foeCap`, `foeBonus`, `foeLvlBias`, `foePower`, `abilityThreat`; `softCapFloat`; `COMBAT_SCALE_FROM_DEPTH`; the `FOE_CAP_*` / `FOE_POWER_*` / `ABILITY_THREAT_*` constants; pure helpers `foeCountFor`, `foeWpFor`, `foeDmgBonusFor`, `abilityCadenceFor` | engine | 21-02 | identity-valued constants land in 21-02; the retune sets them in 21-04 |
| `startCombat` reads the curve (count bonus, wp/maxWP scaling, per-foe `dmgBonus` key only when > 0); `tickAbilityCooldowns(state, f)`; `abilityCadenceFor` at all four cadence sites | engine | 21-02 | D-17/D-18 |
| `test/unit/combat-scaling.test.js`; extended `test/difficulty/difficulty.test.js` | tests | 21-02 | identity pins 1..5, draw-shape equality, synthetic-curve pins |
| `newRun(seed, exclude, { startDepth })`, `state.dev` (boolean, always present), `DEV_START_DEPTH_MAX`; `dev: !!obj.dev` in validateSave/rehydrate; `dev` stripped in all three comparables | engine / harness | 21-03 | D-13/D-14/D-23 |
| `engineAdapter.initRun(seed, exclude, options)` / `startNewRun(seed, { startDepth })`; recordBest + persistGrave gated on `!dev`; Settings version row + long-press dev field + `window.mzDevStartAtDepth`; DEV chip in the HUD; build-www version injection | client | 21-03 | D-13/D-22 |
| The retuned constants + "DELIBERATE RULES CHANGE (Phase 21)" blocks; optional depth-gated `lootDepth` / member-upkeep scale (only if the readouts demand it); ledger change table + AFTER + comparison | engine / ledger | 21-04 | D-09/D-10/D-11/D-21 |
| DR checklist + debug-build note in the ledger; phase gate | ledger / gate | 21-05 | D-15/D-16 |

## Exact targets and proxy parameters (transcribed from CONTEXT.md — cite in the ledger)

| Item | Value | Decision |
|---|---|---|
| Median death depth | 8–15 | D-09 |
| p90 death depth | ≥ 25 | D-09 |
| Runs past floor 50 | < 2% | D-09 |
| Median actions per floor | 40–80 (≈ 3–6 floors per 5–10 min session) | D-09 |
| Seeds per readout | 200 (`i * 7919 + 1`, unchanged) | D-08/D-09 |
| Flee/parley threshold | 0.3 plain / 0.5 vs any kit-bearing live foe | D-06 |
| Potion / camp thresholds | wp/maxWP < 0.5 (Claude's Discretion) | D-05 |
| Exploration budget per floor | 50 actions, then head to the exit (Claude's Discretion; `--explore-budget=N`) | D-05 |
| Per-run action cap | 20000 (`--max-actions=N`) | Pitfall 4 |
| Party | `--party` forces one member at start via the documented bypass | D-12/D-20 |

<tasks>

<task type="auto">
  <name>Task 1: Create the shared bot module tools/lib/tuning-bot.mjs (D-05/D-06/D-07/D-08/D-20/D-23) and pin every policy branch in test/unit/tuning-bot.test.js</name>
  <files>tools/lib/tuning-bot.mjs, test/unit/tuning-bot.test.js</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-05, D-06, D-07, D-08, D-12, D-20, D-23 and "Claude's Discretion" (exploration budget, potion/camp thresholds), Deferred Ideas (no shopping/hiring policies)
    - .planning/phases/21-consolidated-difficulty-retune/21-RESEARCH.md — "Pattern 2: Bot policy upgrade", "Pattern 3: The --party flag", Pitfall 4 (runtime budget), Open Questions 1 and 2
    - .planning/phases/21-consolidated-difficulty-retune/21-PATTERNS.md — the `tools/lib/tuning-bot.mjs` entry (extraction source lines 125-141 of tune-difficulty.mjs)
    - tools/tune-difficulty.mjs lines 25-143 (the DIRS/canStep/nearestUnseenDir/legalDirs/pickFallbackDir/decideAction block to extract verbatim, then extend) and 152-198 (autoPlayOnce: the policyRng seed `seed ^ 0x9e3779b9`, the parley tally shape)
    - tools/tune-economy.mjs lines 90-105 (its decideAction variant: the pendingFind branch) and 113-147 (goldGained tally)
    - engine/actions.js lines 10-45 (ACTION_TYPES: castSpell{idx}, drinkPotion, camp, resolveJoiner{accept}, takeFind, leaveFind, leaveStore)
    - engine/magic.js lines 48-56 (castSpell's charge check: maxCharges(c) - c.spellsUsed <= 0 → noChargesLeft) and 390-400 (drinkPotion no-op at 0 potions)
    - engine/movement.js lines 104 (maxCharges export), 344-357 (feature dispatch: a visited dot is set to null; feat "exit" auto-descends), 513-522 (makeCamp refuses below R.eats rations with campFailed)
    - engine/derived.js lines 515-520 (canCast(state, sp)), 28-38 (skill/eff tolerate missing skills/items)
    - engine/combat.js lines 77-79 (liveFoes) and the canParley export (~598), engine/foeAbilities.js lines 140-240 (the event shapes: foeCast, foeHealed, foeSummoned{pending}, foeDebuffed, foeBolted{member?}, foeDrained, heroResisted/heroResistFailed) and engine/combat.js lines 1124-1141 (struckByFoe{dmg} vs hero-targeted foeBolted{dmg})
    - engine/encounters.js lines 447-486 (meetJoiner(state, rng, events) / resolveJoiner(state, accept, events)) and test/parity/harness/comparables.js lines 242-252 (applyStartCombat — the precedented direct-import bypass shape: makeRng(state.rngState), call, write rngState back)
    - content/spells.js lines 30-61 (SPELLS order: Freeze is index 4; kind "thrown" rows) and content/races.js (eats: Large races eat 2)
    - test/unit/combat.test.js lines 46-56 (fakeRng) and 58-96 (fixedFighter/fixedState — the minimal synthetic-state style to copy)
  </read_first>
  <action>
    Create `tools/lib/tuning-bot.mjs` (new directory `tools/lib/`). Header comment: dev-only, zero-dependency Node ESM module shared by tools/tune-difficulty.mjs and tools/tune-economy.mjs (Phase 21, TUNE-02, D-23); "THIS IS A TUNING PROXY, NOT A PASS/FAIL GATE" retained verbatim from the tools; NOT a node:test file; talks to the engine through applyAction/newRun/makeRng plus READ-ONLY helpers (`canParley` from engine/combat.js — the existing precedent — `canCast` from engine/derived.js, `maxCharges` from engine/movement.js, `SPELLS`/`RACES` from content/index.js), with exactly ONE documented write-path bypass (`forceParty`, below). Imports: `newRun, applyAction` from ../../engine/engine.js; `makeRng` from ../../engine/rng.js; `canParley` from ../../engine/combat.js; `canCast` from ../../engine/derived.js; `maxCharges` from ../../engine/movement.js; `meetJoiner, resolveJoiner` from ../../engine/encounters.js; `SPELLS, RACES` from ../../content/index.js.

    Exports, in this order:

    1. `DIRS` (moved verbatim), `HAZARD_FEATS = new Set(["climb", "gorge", "trap"])`, `REACH_FLOORS = [5, 10, 20, 30, 50]`, `DEPTH_BANDS = [[1, 5], [6, 10], [11, 20], [21, 30], [31, 50], [51, Infinity]]`, `bandLabel(depth)` (e.g. "1-5", "51+").
    2. `BOT_DEFAULTS = Object.freeze({ fleeThreshold: 0.3, casterFleeThreshold: 0.5, potionThreshold: 0.5, campThreshold: 0.5, exploreBudget: 50, maxActions: 20000, party: false })` — every number cites its decision in a trailing comment (D-06 for the two flee values; D-05 + Claude's Discretion for potion/camp/exploreBudget; Pitfall 4 for maxActions).
    3. `canStep(f, x, y, dir)` verbatim from tune-difficulty.mjs; `legalDirs(state)`; `pickFallbackDir(state, policyRng)` verbatim.
    4. `bfsFirstStep(state, isGoal, avoid)` — the existing BFS generalised: `isGoal(cell, x, y)` decides the target, `avoid(cell)` (may be null) excludes a cell from being ENTERED (start cell exempt); returns the first-step dir or null. `nearestUnseenDir(state)` = two-pass: first `bfsFirstStep(state, unseenGoal, seenHazard)` where `seenHazard = (cell) => cell.seen && HAZARD_FEATS.has(cell.feat)`, then without `avoid` if the first pass returns null (Claude's Discretion: a human routes around a visible ledge/gorge/trap when a route exists; an unseen hazard is not avoided — the bot only uses information a player has). `dirTowardExit(state)` = the same two-pass with `isGoal = (cell) => cell.feat === "exit"`. `dotsRemaining(floor)` = count of cells with `feat === "dot"` (visited dots are nulled by move(), so this is "encounters left on this floor").
    5. `liveFoesHaveAbilities(state)` = `!!state.combat && state.combat.foes.some((f) => f.alive && Array.isArray(f.abilities) && f.abilities.length > 0)`. `findCastableAttackSpell(state)` = null unless `maxCharges(c) - c.spellsUsed > 0`; otherwise the index in SPELLS of the `kind === "thrown"` spell with the highest `lvl` for which `canCast(state, sp)` is true (first on ties), else null.
    6. `makeBotContext(opts = {})` → `{ opts: { ...BOT_DEFAULTS, ...opts }, floorActions: 0, findFull: false }`.
    7. `decideAction(state, policyRng, ctx)` — priority order, each branch commented with its decision id:
       (a) `state.combat`: `ratio = c.maxWP > 0 ? c.wp / c.maxWP : 0`; `fleeAt = liveFoesHaveAbilities(state) ? ctx.opts.casterFleeThreshold : ctx.opts.fleeThreshold` (D-06); if `ratio < fleeAt` → `{ type: "parley" }` when `canParley(state)` else `{ type: "flee" }`; else if `c.potions > 0 && ratio < ctx.opts.potionThreshold` → `{ type: "drinkPotion" }` (D-05); else `castIdx = findCastableAttackSpell(state)`; if not null → `{ type: "castSpell", idx: castIdx }` (D-05); else `{ type: "attack" }`.
       (b) `state.pendingJoiner` → `{ type: "resolveJoiner", accept: false }` (hiring policy is a Deferred Idea — `--party` is the only member source, D-20).
       (c) `state.pendingFind` → `ctx.findFull ? { type: "leaveFind" } : { type: "takeFind" }` (Claude's Discretion: a player takes what the maze offers; `findFull` is set by observe() on a bagFull event so the bot never loops on a full bag).
       (d) `state.store` → `{ type: "leaveStore" }` (unchanged; shopping is a Deferred Idea).
       (e) out of combat: `ratio < ctx.opts.potionThreshold && c.potions > 0` → drinkPotion; `ratio < ctx.opts.campThreshold && c.rations >= (RACES[c.race]?.eats || 1)` → `{ type: "camp" }` (D-05 — the eats guard is what keeps makeCamp from answering campFailed forever).
       (f) `dotsRemaining(state.floor) === 0 || ctx.floorActions >= ctx.opts.exploreBudget` → `dir = dirTowardExit(state) || nearestUnseenDir(state) || pickFallbackDir(state, policyRng)` (D-05 descend rule).
       (g) otherwise `dir = nearestUnseenDir(state) || pickFallbackDir(state, policyRng)`; return `{ type: "move", dir }`.
    8. `makeTallies()` → `{ foeCast: 0, foeBolted: 0, foeDrained: 0, foeDebuffed: 0, foeHealed: 0, foeSummoned: 0, heroResisted: 0, heroResistFailed: 0, abilityDmg: 0, meleeDmg: 0, encounters: 0, casterEncounters: 0, encountersByBand: {}, casterEncountersByBand: {} }` (band keys from bandLabel). `tallyEvents(tallies, events, stateAfter)` (D-07): increment the eight counters by `e.type` (foeSummoned only when `e.pending === true`); `abilityDmg += e.dmg` for a `foeBolted` WITHOUT a `member` field; `meleeDmg += e.dmg` for `struckByFoe`; for each `encounterStarted`: `encounters++`, band = bandLabel(stateAfter.floor.depth), and `casterEncounters++` (and the band counter) when `liveFoesHaveAbilities(stateAfter)` — an encounter that ended inside the same action (Knight/Con Artist talkdown) counts as non-caster.
    9. `observe(ctx, events)` — after every step: if any `floorChanged` → `ctx.floorActions = 0`, else `ctx.floorActions++`; `bagFull` → `ctx.findFull = true`; `findTaken`/`findLeft` → `ctx.findFull = false`.
    10. `forceParty(state)` — the ONE write-path bypass (D-20; precedent: applyStartCombat in test/parity/harness/comparables.js). Comment block: HARNESS-ONLY — never shipped, never a pattern for UI code; there is no hire action and no way to force a Joiner through applyAction, so `--party` calls the engine's own meetJoiner + resolveJoiner directly on a fresh newRun state: `const rng = makeRng(state.rngState); const events = []; meetJoiner(state, rng, events); resolveJoiner(state, true, events); state.rngState = rng.getState(); return state;`. Note in the comment that these draws shift the run's trajectory, so `--party` is its own distribution, not a paired diff against the solo seed list.
    11. `playRun(seed, opts, onStep)` — the shared loop both tools use: `policyRng = makeRng(seed ^ 0x9e3779b9)`; `state = newRun(seed)`; if `opts.party` → `forceParty(state)`; `ctx = makeBotContext(opts)`; `tallies = makeTallies()`; loop `while (!state.dead && !state.won && actions < ctx.opts.maxActions)`: decide → applyAction → `tallyEvents(tallies, events, state)` → `observe(ctx, events)` → `if (onStep) onStep(events, state)` → `actions++`. Return `{ seed, state, actions, tallies, deathDepth: state.floor.depth, dead: state.dead, won: state.won, cause: state.deathNote || (state.won ? "walked out" : actions >= ctx.opts.maxActions ? "maxActionsHit" : "unknown"), actionsPerFloor: actions / Math.max(1, state.floor.depth), memberAtStart: opts.party ? state.party.length : 0 (captured right after forceParty), memberAtEnd: state.party.length }`.
    12. Readout helpers (D-08), pure over `results`: `percentile`/`distribution` (moved from the tools), `reachTable(results)` → `{ "5": pct, "10": pct, ... }` as percentages of runs with `deathDepth >= N` (one decimal); `actionsPerFloorDist(results)` → distribution of `r.actionsPerFloor` (integers via Math.round); `casterRateByBand(results)` → array of `{ band, encounters, casterEncounters, rate }` in DEPTH_BANDS order; `abilitySummary(results)` → the eight summed counters plus `abilityDmg`, `meleeDmg`, `abilityShare` (abilityDmg / (abilityDmg + meleeDmg), 0 when both are 0); `partySummary(results)` → `{ runs, memberAtStart (count of runs with memberAtStart > 0), memberAtEnd (count with memberAtEnd > 0) }`; `sharedJson(results, opts)` → `{ reach, actionsPerFloor, casterRateByBand, abilities, party (only when opts.party), bot: { exploreBudget, maxActions, party, fleeThreshold, casterFleeThreshold, potionThreshold, campThreshold } }`; `printSharedReadout(results, opts)` — console.log blocks in exactly this order and wording (the ledger transcribes them verbatim, so keep them stable): "Reach table (% of runs reaching floor N):" one line `  >=5: x.x%  >=10: x.x%  >=20: x.x%  >=30: x.x%  >=50: x.x%`; "Actions per floor (actions / death depth, per run):" one `min= p50= p90= max=` line; "Caster-encounter rate by depth band (encounters with >=1 kit-bearing live foe):" one line per band `  1-5: c/n (x.x%)`; "Foe abilities (D-07 readout — informational, not a gate):" two counter lines then `  ability damage: A of T total damage taken (x.x%)`; when opts.party: "Party (--party, D-12/D-20):" `  member forced at run start in a/N runs; member alive at run end: b (x.x%)`; finally ONE line built exactly as `Bot: exploreBudget=${exploreBudget}  maxActions=${maxActions}  party=${party ? "on" : "off"}  flee=${fleeThreshold}/${casterFleeThreshold}(caster)  potion<${potionThreshold}  camp<${campThreshold}` (TWO spaces between fields — the ledger greps this line to prove BEFORE/AFTER used identical parameters).

    Create `test/unit/tuning-bot.test.js` (header: Phase 21 TUNE-02 — synthetic-state probes for the shared bot policy; asserts DECISIONS and tally arithmetic only, never a readout number — the tools stay proxies). Import the module's exports, `newRun` from ../../engine/engine.js, `SPELLS` from ../../content/index.js, `maxCharges` from ../../engine/movement.js. Build a `mkGrid(rows)` helper turning an array of strings (`#` wall, `.` open, `E` exit, `D` dot, `C` climb, `T` trap, `S` start) into `{ g, px, py, depth: 1 }` with every cell `seen: true` unless the string uses `?` for an unseen open cell; a `mkState(over)` helper returning `{ c: { name: "T", race: "Human", cls: "Fighter", sub: "Soldier", level: 1, wp: 40, maxWP: 40, potions: 0, rations: 0, spellsUsed: 0, grimoire: [], items: [], skills: {} }, combat: null, store: null, pendingFind: null, pendingJoiner: null, party: [], floor: mkGrid([...a 5x5 open room with S at (1,1) and E at (3,3)...]), ...over }`; a `fixedPolicyRng` whose `pick` returns the first element; and `ctx = makeBotContext()`. Tests (one `test()` each, names quoting the decision id):
    1. D-06 caster threshold: combat `{ type: "Walking Dead", foes: [{ name: "x", alive: true, abilities: ["drudgeFreeze"], wp: 5, maxWP: 5 }], round: 1 }`, wp 16/40 (0.4) → `{ type: "flee" }` (Walking Dead: canParley false); the same foe with NO abilities key → `{ type: "attack" }`; with abilities at wp 24/40 (0.6) → attack.
    2. D-05 potion in combat: plain foe, wp 16/40, potions 1 → drinkPotion; potions 0 → attack; wp 8/40 (0.2, below the plain 0.3 flee line) with potions 1 → flee (flee branch wins).
    3. D-05 cast: cls "Magic User", sub "Sorcerer", grimoire ["Freeze", "Heal"], level 1, spellsUsed 0, plain foe, wp 40/40 → `{ type: "castSpell", idx: SPELLS.findIndex((s) => s.n === "Freeze") }` (assert that index equals 4); with `spellsUsed = maxCharges(c)` → attack; a Fighter with the same grimoire → attack (canCast false); a Sorcerer level 3 with grimoire ["Freeze", "Fireball"] → the Fireball index (highest lvl wins).
    4. D-05 camp: out of combat wp 16/40, rations 0, potions 0 → `type === "move"` (never camp); rations 1 → camp; race "Troll"-equivalent: pick the first RACES entry whose `eats === 2` (assert one exists) with rations 1 → move, rations 2 → camp; potions 1 → drinkPotion wins over camp.
    5. D-05 descend: grid with S at (1,1), E at (3,3), one D at (1,3): fresh ctx → `decideAction` returns a move whose dir equals `nearestUnseenDir(state)` when the grid has an unseen `?` cell, and equals `dirTowardExit(state)` once the D cell is set to `feat: null` (dotsRemaining 0) — assert `dirTowardExit` on the 5x5 room is "S" or "E" (both are valid first steps toward (3,3); assert it is one of them and that following it repeatedly reaches E within 6 steps using canStep).
    6. D-05 budget: keep the D cell, set `ctx.floorActions = ctx.opts.exploreBudget` → the move equals `dirTowardExit`; `observe(ctx, [{ type: "floorChanged" }])` resets `ctx.floorActions` to 0; `observe(ctx, [{ type: "moved" }])` increments it.
    7. Hazard avoidance: a 3-row corridor grid where the direct path east crosses a seen `C` cell and a detour exists → `nearestUnseenDir`'s first step is the detour's first step; block the detour with walls → the first step crosses the C cell (second pass); an UNSEEN C cell on the direct path is crossed on the first pass.
    8. Pending prompts: `pendingJoiner: { name: "J" }` → `{ type: "resolveJoiner", accept: false }`; `pendingFind: { n: "x" }` with `ctx.findFull` false → takeFind, after `observe(ctx, [{ type: "bagFull" }])` → leaveFind, after `observe(ctx, [{ type: "findLeft" }])` → takeFind again; `store: {}` → leaveStore.
    9. D-20 forceParty: `const a = forceParty(newRun(7)); const b = forceParty(newRun(7));` → `a.party.length === 1`, `a.rngState !== newRun(7).rngState`, `a.party[0].name === b.party[0].name`; `newRun(7).party.length === 0` (the bypass never touches a state it is not handed).
    10. D-07 tallies: `tallyEvents(t, [{ type: "foeCast" }, { type: "foeBolted", dmg: 7 }, { type: "foeBolted", dmg: 5, member: "M" }, { type: "struckByFoe", dmg: 3 }, { type: "foeSummoned", pending: true }, { type: "foeSummoned", pending: false }, { type: "heroResisted" }, { type: "encounterStarted" }], stateWithCasterCombatAtDepth12)` → foeCast 1, foeBolted 2, abilityDmg 7, meleeDmg 3, foeSummoned 1, heroResisted 1, encounters 1, casterEncounters 1, `encountersByBand["11-20"] === 1`; `abilitySummary([{ tallies: t }]).abilityShare` equals 0.7; `reachTable([{ deathDepth: 5 }, { deathDepth: 12 }, { deathDepth: 1 }, { deathDepth: 50 }])` → `{ "5": 75, "10": 50, "20": 25, "30": 25, "50": 25 }`.
    11. Purity: `decideAction` never mutates its state argument (structuredClone before/after deepStrictEqual across the combat/potion/camp/move cases) and the module source has no Math.random / Date.now on a code line (read the file, strip `//` comment lines, regex).
  </action>
  <verify>
    <automated>node --test test/unit/tuning-bot.test.js && node --test test/unit/engine-purity.test.js test/determinism/rng-no-math-random.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tools/lib/tuning-bot.mjs` succeeds and `grep -c '^export function decideAction(state, policyRng, ctx)' tools/lib/tuning-bot.mjs` prints 1
    - `grep -c '^export function playRun(seed, opts, onStep)' tools/lib/tuning-bot.mjs` prints 1 and `grep -c '^export function forceParty(state)' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'meetJoiner(state, rng, events);' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'resolveJoiner(state, true, events);' tools/lib/tuning-bot.mjs` prints 1
    - `grep -c '^export const BOT_DEFAULTS' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'casterFleeThreshold: 0.5' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'fleeThreshold: 0.3' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'exploreBudget: 50' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'maxActions: 20000' tools/lib/tuning-bot.mjs` prints 1
    - `grep -c 'type: "castSpell", idx' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'type: "drinkPotion"' tools/lib/tuning-bot.mjs` prints >= 1 and `grep -c 'type: "camp"' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'accept: false' tools/lib/tuning-bot.mjs` prints 1 and `grep -c '"buyItem"\|"sellItem"' tools/lib/tuning-bot.mjs` prints 0
    - `grep -c 'TUNING PROXY' tools/lib/tuning-bot.mjs` prints >= 1 and `grep -v '^\s*//' tools/lib/tuning-bot.mjs | grep -c 'Math.random\|Date.now'` prints 0
    - `for k in foeCast foeBolted foeDrained foeDebuffed foeHealed foeSummoned heroResisted heroResistFailed abilityDmg meleeDmg; do grep -c "$k" tools/lib/tuning-bot.mjs; done` prints a positive count for every key
    - `grep -c 'Reach table (% of runs reaching floor N):' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'Actions per floor (actions / death depth, per run):' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'Caster-encounter rate by depth band' tools/lib/tuning-bot.mjs` prints 1 and `grep -c 'Foe abilities (D-07 readout' tools/lib/tuning-bot.mjs` prints 1
    - `node --test test/unit/tuning-bot.test.js` exits 0 with 11 passing tests and `grep -c 'deathDepth: ' test/unit/tuning-bot.test.js` prints >= 1 only inside the synthetic reachTable fixture (no `tune-difficulty`/`--seeds` invocation: `grep -c 'tune-difficulty\|--seeds' test/unit/tuning-bot.test.js` prints 0)
    - `node --test test/unit/engine-purity.test.js test/determinism/rng-no-math-random.test.js` exits 0
    - `git diff --quiet bd0ba7c -- engine content src mazeworld.html` exits 0
  </acceptance_criteria>
  <done>The shared module exists with every D-05/D-06 branch, the D-07 tallies, the D-08 summarizers, playRun and the documented forceParty bypass; eleven synthetic-state tests pin each branch (cast/no-charges, potion/no-potion, camp/no-rations/Large race, descend-when-cleared/budget, caster threshold, hazard routing, prompts, forceParty determinism, tally arithmetic, purity); the engine is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire tools/tune-difficulty.mjs and tools/tune-economy.mjs onto the shared module; add --party / --max-actions / --explore-budget and the D-07/D-08 readout blocks to both text and --json output</name>
  <files>tools/tune-difficulty.mjs, tools/tune-economy.mjs</files>
  <read_first>
    - tools/lib/tuning-bot.mjs as created in Task 1 (the export list and printSharedReadout/sharedJson contracts)
    - tools/tune-difficulty.mjs lines 1-29 (header to keep, with the PROXY line), 152-198 (autoPlayOnce — replaced by playRun with a parley onStep tally), 200-295 (reporting: keep the death-depth / action-count / cause / parley blocks, then append the shared blocks), 297-341 (parseArgs/main)
    - tools/tune-economy.mjs lines 1-37 (header — update the "declines any offered find" sentence, keep the SCOPE/STATUS + PROXY lines), 113-147 (autoPlayOnce gold tally → onStep), 176-240 (printReport/parseArgs/main)
    - .planning/phases/20-parley-balance-language-system/20-01-SUMMARY.md lines 95-130 (how the Phase 20 parley tally was added — the readout stays informational)
  </read_first>
  <action>
    In `tools/tune-difficulty.mjs`: delete the local `DIRS`/`canStep`/`nearestUnseenDir`/`legalDirs`/`pickFallbackDir`/`decideAction`/`MAX_ACTIONS`/`percentile`/`distribution` definitions and import `{ playRun, distribution, percentile, sharedJson, printSharedReadout, BOT_DEFAULTS }` from `./lib/tuning-bot.mjs` (keep `canParley` OUT of this file now — the module owns it). Replace `autoPlayOnce(seed)` with `autoPlayOnce(seed, opts)` that calls `playRun(seed, opts, onStep)` where `onStep(events)` keeps the Phase 20 parley tally exactly as today (parleyRolled / spGained-parley / parleyFailed / parleyRefused / parleyExhausted), then returns `{ ...run, parleyAttempts, parleySuccesses, parleyFailures, parleyRefused, parleyExhausted, parleySp, spTotal: run.state.c.sp }` (drop the `state` object from the returned record so 200 results do not pin 200 floors in memory: spread `run` minus `state`). `parseArgs` gains `--party` (boolean → `opts.party = true`), `--max-actions=N` (positive int → `opts.maxActions`), `--explore-budget=N` (non-negative int → `opts.exploreBudget`); `opts` starts as `{ seeds: 200, json: false, ...BOT_DEFAULTS }`. `main` passes `opts` into every `autoPlayOnce`. `printReport(results, opts)`: keep the existing blocks (header, death-depth, action-count, cause breakdown, parley) verbatim, then call `printSharedReadout(results, opts)` before the final "Outcome:" line; delete the stale NOTE block about the retired floor-5 win gate (the `wonCount > 0` console.log pair) and the "Until Plan 02 makes descent endless" header paragraph (descent has been endless since Phase 3 — the sentence misleads a reader of the ledger; the Outcome line keeps printing the won count but drops its parenthetical). JSON output: add `...sharedJson(results, opts)` after `parley`. Update the header's Run: examples to list `--party`, `--max-actions=N`, `--explore-budget=N` and add one sentence: the bot policy lives in tools/lib/tuning-bot.mjs (Phase 21, TUNE-02) — cast/drink/camp/descend + caster-aware flee (D-05/D-06); tallies D-07; readout D-08.

    In `tools/tune-economy.mjs`: same extraction — delete the local policy/BFS/`MAX_ACTIONS`/`percentile`/`distribution` definitions, import the same names from `./lib/tuning-bot.mjs`; `autoPlayOnce(seed, opts)` calls `playRun(seed, opts, onStep)` where `onStep(events, state)` keeps the goldGained tally (`earned`, `bySource`) and tracks `peakGold` from `state.c.gold`; return `{ seed, deathDepth, dead, won, actions, cause, actionsPerFloor, tallies, memberAtStart, memberAtEnd, startGold, endGold, peakGold, earned, bySource }` — compute `startGold` with a separate `newRun(seed).c.gold` read BEFORE `playRun` (newRun is pure; one extra chargen per seed is cheap) and read `endGold` from the final state's `c.gold`; note in a comment that with `--party` the forced member changes nothing about the hero's purse. Same three new flags; `printReport(results, opts)` keeps its four gold blocks + "Reached depth" + "Income by source" verbatim, then `printSharedReadout(results, opts)`; JSON adds `...sharedJson(results, opts)`. Fix the header: replace the "(it explores blindly and never shops)" and "declines any offered find" sentences with: the bot now takes finds (leaveFind only on a full bag), casts/drinks/camps, and heads for the exit once a floor's dots are cleared or the exploration budget is spent — it still never shops (Deferred Idea), so store income/spend stays out of the signal.

    Both files keep their `#!/usr/bin/env node` line, are NOT wired into package.json scripts, and remain runnable with `node tools/<name>.mjs --seeds=3 --json`.
  </action>
  <verify>
    <automated>node tools/tune-difficulty.mjs --seeds=3 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);for(const k of ['deathDepth','actions','causes','parley','reach','actionsPerFloor','casterRateByBand','abilities','bot'])if(!(k in j))throw new Error('missing '+k);if(j.runs!==3)throw new Error('runs');console.log('difficulty json ok')})" && node tools/tune-difficulty.mjs --seeds=2 --party --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(!j.party||j.party.memberAtStart!==2)throw new Error('party');console.log('party json ok')})" && node tools/tune-economy.mjs --seeds=3 --json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);for(const k of ['earned','peakGold','endGold','deathDepth','bySource','reach','actionsPerFloor','casterRateByBand','abilities','bot'])if(!(k in j))throw new Error('missing '+k);console.log('economy json ok')})" && node tools/tune-difficulty.mjs --seeds=2 --explore-budget=10 --max-actions=300 | grep -c 'Bot: exploreBudget=10  maxActions=300'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'from "./lib/tuning-bot.mjs"' tools/tune-difficulty.mjs` prints 1 and `grep -c 'from "./lib/tuning-bot.mjs"' tools/tune-economy.mjs` prints 1
    - `grep -c '^function decideAction\|^function nearestUnseenDir\|^function canStep\|^const MAX_ACTIONS' tools/tune-difficulty.mjs` prints 0 and the same grep on tools/tune-economy.mjs prints 0
    - `grep -c 'from "../engine/combat.js"' tools/tune-difficulty.mjs tools/tune-economy.mjs` prints 0 for both (canParley is read through the module now)
    - `grep -c -- '--party' tools/tune-difficulty.mjs` prints >= 2 and `grep -c -- '--max-actions=' tools/tune-difficulty.mjs` prints >= 2 and `grep -c -- '--explore-budget=' tools/tune-difficulty.mjs` prints >= 2; the same three greps on tools/tune-economy.mjs print >= 2 each
    - `grep -c 'printSharedReadout(results, opts)' tools/tune-difficulty.mjs` prints 1 and `grep -c 'sharedJson(results, opts)' tools/tune-difficulty.mjs` prints 1; both greps print 1 on tools/tune-economy.mjs
    - `grep -c 'legacy floor-5 Gate' tools/tune-difficulty.mjs` prints 0 and `grep -c 'TUNING PROXY' tools/tune-difficulty.mjs` prints >= 1 and `grep -c 'TUNING PROXY' tools/tune-economy.mjs` prints >= 1
    - `node tools/tune-difficulty.mjs --seeds=3` exits 0 and its text output contains, in order, "Death-depth distribution:", "Parley (D-15 readout", "Reach table (% of runs reaching floor N):", "Actions per floor (actions / death depth, per run):", "Caster-encounter rate by depth band", "Foe abilities (D-07 readout", "Bot: exploreBudget=50  maxActions=20000  party=off", "Outcome:" (check with a `grep -n` and compare line numbers)
    - `node tools/tune-difficulty.mjs --seeds=2 --party` output contains "Party (--party, D-12/D-20):" and "member forced at run start in 2/2 runs"
    - `node tools/tune-economy.mjs --seeds=3` exits 0 and contains "Income by source (goldGained.why):" followed later by "Reach table (% of runs reaching floor N):"
    - `grep -c 'tune-difficulty\|tune-economy\|tuning-bot' package.json` prints 0 (never wired into npm scripts)
    - `git diff --quiet bd0ba7c -- engine content src mazeworld.html` exits 0
  </acceptance_criteria>
  <done>Both tools import the shared bot, expose --party/--max-actions/--explore-budget, and print/emit the D-07 ability tallies and the D-08 reach/actions-per-floor/caster-rate blocks in a stable, transcribable format; short smoke runs succeed; the engine is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the BEFORE readout (three 200-seed background runs, EXIT= sentinel) into the new ledger docs/DIFFICULTY-RETUNE.md with the D-09 targets and bot-parameter table</name>
  <files>docs/DIFFICULTY-RETUNE.md</files>
  <read_first>
    - .planning/phases/21-consolidated-difficulty-retune/21-CONTEXT.md — D-08 (readout shape, BEFORE verbatim), D-09 (targets), D-10 (constants in scope), D-11 (ledger method), D-15 (DR checklist — headings only here), "Grounding facts" (the dumb-bot BEFORE numbers to contrast)
    - docs/PARLEY-REBALANCE.md (whole file — the ledger structure to mirror: intro → locked-decision table → BEFORE readout verbatim in a fenced block with the commit hash and date → AFTER → Comparison) and content/BESTIARY-REBALANCE.md lines 1-30 (the "what this artifact is / who fills which half" framing)
    - .planning/phases/20-parley-balance-language-system/20-01-PLAN.md lines 214-225 and 20-01-SUMMARY.md lines 100-130 (the backgrounded run + EXIT sentinel + bounded polling procedure that worked)
    - .planning/phases/18-bestiary-rebalance-canon-combat-fixes/18-06-SUMMARY.md lines 108-165 (why an executor must never wait in the foreground for a 200-seed run)
  </read_first>
  <action>
    First gate the engine: `git diff --quiet bd0ba7c -- engine content src mazeworld.html` must exit 0 (record the hash in the doc). Then launch THREE background runs at once (Bash `run_in_background`, each redirecting stdout+stderr to its own scratchpad file and appending `EXIT=<code>` as the final line): `node tools/tune-difficulty.mjs --seeds=200` → `<scratchpad>/retune-before-solo.txt`; `node tools/tune-difficulty.mjs --seeds=200 --party` → `<scratchpad>/retune-before-party.txt`; `node tools/tune-economy.mjs --seeds=200` → `<scratchpad>/retune-before-economy.txt`. Expect 5–10 minutes each (the smarter bot survives longer — Pitfall 4); while they run, write the document skeleton below; then poll each file in bounded checks (Monitor/until-loop, ≤ 120 s per check, never a foreground sleep) until its `EXIT=0` line is present. If any run exceeds 20 minutes, stop it, re-run that one with `--max-actions=5000`, and record the cap in the parameters table (the cap is a proxy parameter, not a result).

    Create `docs/DIFFICULTY-RETUNE.md` with these sections in this order (mirror PARLEY-REBALANCE.md's tone; every number is informational):
    1. Title `# Consolidated Difficulty Retune (Phase 21) — before/after ledger` and an intro paragraph: the ONE retune across monster power / ability threat / party power / economy (TUNE-01..04), what this document is (BEFORE → change table → AFTER → comparison → DR checklist), and the D-09 "sanity floor, never the exit criterion" framing; the exit criterion is the human DR round (TUNE-04, D-15).
    2. `## Scope and method (D-10, D-11)` — a bullet list of every constant in scope (difficulty.js knobs incl. the new foeCap/foeBonus/foeLvlBias/foePower/abilityThreat; existing dot/dark knobs; abilityThreat cadence; the depth-scaled economy sources: wilmst cache row, parley Humans amount, chest amount — and, as read-only context, LOOT_DIVISOR / purse table which sit on fixture paths and stay flat; member upkeep per D-20) and the explicit NOT-in-scope list (bestiary base stats — Phase 18's ledger stands; class/race/sub features; store price scaling — does not exist, D-21; hire cost — does not exist, D-20). State the method: at most TWO harness iterations (D-11), every change to a fixture-exposed path is identity at depth ≤ 5 (D-19), rationale per knob in the change table.
    3. `## Targets (D-09) — a sanity floor, never the exit criterion` — a table with the four targets (median death depth 8–15; p90 ≥ 25; < 2% of runs past floor 50; median actions per floor 40–80) and the "measured with the upgraded bot at 200 seeds" note.
    4. `## Bot proxy (TUNE-02)` — what the upgraded bot does (D-05 four policies, D-06 caster threshold, takes finds/declines joiners/leaves stores, seen-hazard routing) and a parameters table: seeds 200 (`i * 7919 + 1`), exploreBudget 50, maxActions 20000 (or the cap used), fleeThreshold 0.3 / casterFleeThreshold 0.5, potionThreshold 0.5, campThreshold 0.5, party off/on. State that BEFORE and AFTER MUST use identical parameters and that `--party` is its own distribution (forceParty shifts the rng trajectory — D-20).
    5. `## BEFORE readout — upgraded bot, post-Phase-20 engine (commit bd0ba7c)` with three subsections, each a fenced block containing the run's output VERBATIM (from the first `tune-…:` line through the final `Outcome:`/blank line; do not edit numbers or wording): `### tune-difficulty --seeds=200`, `### tune-difficulty --seeds=200 --party`, `### tune-economy --seeds=200`. Below them a `**Headline BEFORE numbers:**` paragraph: death-depth min/p50/p90/max, reach ≥10/≥20/≥30, median actions per floor, caster-encounter rate in the deepest populated band, ability damage share, the --party vs solo p50, and the economy peak-wilmst p50/p90 — each compared in one clause against the D-09 target (e.g. "p50 = N vs target 8–15"). Also a one-line contrast with the pre-upgrade dumb-bot readout (p50 1, p90 2, max 4 — PARLEY-REBALANCE.md) so a reader sees what the bot upgrade alone changed before any engine number moved.
    6. Placeholder sections with a single italic line each ("*Filled by plan 21-04.*" / "*Filled by plan 21-05.*"): `## Change table (21-04)`, `## AFTER readout (21-04)`, `## Comparison vs D-09 (21-04)`, `## Not changed, and why (21-04)`, `## DR checklist — TUNE-04 sign-off (21-05)`.
    Finish by re-running the engine gate (`git diff --quiet bd0ba7c -- engine content src mazeworld.html`) and `npm test`.
  </action>
  <verify>
    <automated>test -f docs/DIFFICULTY-RETUNE.md && grep -c 'tune-difficulty: 200 seeded auto-play run' docs/DIFFICULTY-RETUNE.md | grep -qx 2 && grep -c 'tune-economy: 200 seeded auto-play run' docs/DIFFICULTY-RETUNE.md | grep -qx 1 && grep -c 'Reach table (% of runs reaching floor N):' docs/DIFFICULTY-RETUNE.md | grep -qx 3 && git diff --quiet bd0ba7c -- engine content src mazeworld.html && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --quiet bd0ba7c -- engine content src mazeworld.html` exits 0 both before the runs are launched and after the plan's final commit (record both checks in the SUMMARY)
    - `grep -c '^# Consolidated Difficulty Retune (Phase 21)' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^## Scope and method (D-10, D-11)' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^## Targets (D-09)' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^## Bot proxy (TUNE-02)' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^## BEFORE readout' docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -c '^### tune-difficulty --seeds=200$' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^### tune-difficulty --seeds=200 --party$' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c '^### tune-economy --seeds=200$' docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -c 'tune-difficulty: 200 seeded auto-play run' docs/DIFFICULTY-RETUNE.md` prints 2 and `grep -c 'tune-economy: 200 seeded auto-play run' docs/DIFFICULTY-RETUNE.md` prints 1 and `grep -c 'Reach table (% of runs reaching floor N):' docs/DIFFICULTY-RETUNE.md` prints 3 and `grep -c 'Foe abilities (D-07 readout' docs/DIFFICULTY-RETUNE.md` prints 3 and `grep -c 'Party (--party, D-12/D-20):' docs/DIFFICULTY-RETUNE.md` prints 1
    - `grep -c 'bd0ba7c' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c '8–15\|8-15' docs/DIFFICULTY-RETUNE.md` prints >= 1 and `grep -c 'exploreBudget' docs/DIFFICULTY-RETUNE.md` prints >= 2 (parameters table + the Bot: line inside each transcript)
    - `grep -c '^## Change table (21-04)\|^## AFTER readout (21-04)\|^## Comparison vs D-09 (21-04)\|^## Not changed, and why (21-04)\|^## DR checklist — TUNE-04 sign-off (21-05)' docs/DIFFICULTY-RETUNE.md` prints 5 and `grep -c 'Filled by plan 21-0' docs/DIFFICULTY-RETUNE.md` prints 5
    - each scratchpad transcript ends with `EXIT=0` (three files); no readout was run in the foreground (the SUMMARY records the polling cadence)
    - `npm test` exits 0 with every test passing (912 + the 11 tuning-bot tests = 923; record the exact number) and `node --test "test/parity/**/*.test.js"` reports 30 pass
    - `git diff --quiet 04eb229 -- test/parity/prototype-master.js.txt test/parity/fixtures test/determinism/foe-abilities.test.js test/unit/foe-turn-draw-count.test.js package.json package-lock.json` exits 0
  </acceptance_criteria>
  <done>The ledger exists with the scope/method, D-09 targets, the bot-parameter table and all three BEFORE transcripts verbatim (captured from background runs with EXIT=0 sentinels against the provably untouched bd0ba7c engine), plus the placeholder headings 21-04/21-05 fill; the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → engine | The bot may only mutate state through `applyAction`; the single `forceParty` bypass is harness-only and never shipped |
| readout → ledger | BEFORE numbers must come from the untouched bd0ba7c engine or every later comparison is meaningless |
| dev tooling → CI | Tools stay proxies; a readout number must never become a `node --test` assertion |
| executor → long-running process | A multi-minute run must not stall the executor (18-06 precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | BEFORE readout captured against an already-modified engine | high | mitigate | `git diff --quiet bd0ba7c -- engine content src mazeworld.html` gate before launching and after committing; hash recorded in the ledger |
| T-21-02 | Tampering | `forceParty` bypass leaking into non-harness code or into a normal (`--party` off) run | medium | mitigate | exported only from tools/lib; called only inside `playRun` behind `opts.party`; test 9 proves `newRun(7).party.length === 0` |
| T-21-03 | Repudiation | Readout numbers not reproducible (Math.random/Date in the bot, or bot parameters drifting between BEFORE and AFTER) | medium | mitigate | policyRng = makeRng(seed ^ 0x9e3779b9) only; purity grep test; parameters table in the ledger; `Bot:` line printed inside every transcript |
| T-21-04 | Denial of Service | Smarter bot runs for hours (no cap) and stalls the executor | medium | mitigate | `maxActions` default 20000 with `--max-actions`; background runs with `EXIT=` sentinel and ≤120 s bounded polling; 20-minute abort rule |
| T-21-05 | Elevation of Privilege | Tool assertions become a CI gate that blocks unrelated work | low | mitigate | PROXY header in module + both tools; tests assert decisions with synthetic inputs only; `package.json` scripts untouched |
| T-21-SC | Tampering | npm/pip/cargo installs | high | mitigate | No installs this phase (RESEARCH: Package Legitimacy Audit N/A); `git diff --quiet bd0ba7c -- package.json package-lock.json` |
</threat_model>

<verification>
- `node --test test/unit/tuning-bot.test.js` 11/11; `npm test` fully green (expected 923); parity 30/30.
- Both tools run with `--seeds=3 --json` and expose `reach`, `actionsPerFloor`, `casterRateByBand`, `abilities`, `bot` (+ `party` under `--party`).
- `docs/DIFFICULTY-RETUNE.md` carries three verbatim BEFORE transcripts, the D-09 table, the bot-parameter table, and the five placeholder headings.
- Engine/content/src/mazeworld.html byte-identical to bd0ba7c; frozen parity/determinism files identical to 04eb229.
</verification>

<success_criteria>
- ROADMAP Phase 21 criterion 2 is met: both tools tally foe-ability events and the bot reacts to casters (casts, drinks, camps, descends, flees casters earlier), producing a readout that reaches past the tier-1/2 band.
- The BEFORE half of the ledger is frozen with the same bot and parameters 21-04 will reuse for AFTER.
</success_criteria>

<output>
Create `.planning/phases/21-consolidated-difficulty-retune/21-01-SUMMARY.md` when done (record: the module's final export list, the exact CLI flags, the three run durations and the polling cadence, the headline BEFORE numbers, the exact test count, and both engine-gate checks).
</output>