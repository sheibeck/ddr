---
phase: 17-fixture-inventory-foe-turn-refactors
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - engine/combat.js
  - test/unit/party-combat.test.js
  - test/unit/combat.test.js
autonomous: true
requirements: [FID-03]
tags: [engine, combat, refactor, foe-turn, determinism, parity]

must_haves:
  truths:
    - "FID-03 (edge probe unclassified — resolved as a covered criterion): foeTurn's hero branch draws in exactly today's order — to-hit die, then the damage dice (sp.dmg notation or d6), then (only when the hero wears armour with wp>0 and ar>0 and the foe lacks sp.noArmor) one d20 soak roll — and the extraction boundary is 'Hardiness onward': to-hit, raw damage, weakened halving and critical doubling stay inline in foeTurn; Hardiness, halfNext, ward absorb/reflect/shatter, armour soak, the struckByFoe event and the death terminator live in applyFoeDamageToPlayer"
    - "pickFoeTarget(state, rng) returns null (hero) without touching rng when state.combat has no allies key, an empty allies array, or only wp<=0 members; with N live members it draws exactly one rng.d(N+1) and maps 1 to null and k>1 to liveMembers[k-2] (live members only, in C.allies order)"
    - "applyFoeDamageToPlayer returns { died: true } ONLY on the c.wp <= 0 branch (after die() ran); a ward-reflect kill of the FOE returns { died: false, onArmour: false } so foeTurn continues to the next swing/foe and still runs the end-of-turn ward/mirror tick"
    - "When the hero dies mid-loop, foeTurn returns events immediately: remaining foes never swing (fakeRng exhaustion proves zero further draws) and the ward/mirror decrement does not run — identical to the pre-extraction early return"
    - "The full parity suite (node --test test/parity) is byte-identical to the frozen prototype master after the refactor with zero fixture, harness, or master edits; every pre-existing foeTurn unit test in combat.test.js and party-combat.test.js passes unmodified"
    - "The struckByFoe event keeps its exact shape and key order { type, name, roll, need, dmg, ignoresArmor, critical } and no new event type is introduced (formatEventsCoverage stays green with no EVENT_NARRATION change)"
    - "The simplified member-damage branch inside foeTurn is untouched by this phase (git diff against the phase-start commit shows no changed line in it) — it is not routed through applyFoeDamageToPlayer"
  artifacts:
    - path: "engine/combat.js"
      provides: "pickFoeTarget + applyFoeDamageToPlayer as named exports placed before foeTurn; foeTurn calls both"
      exports: ["pickFoeTarget", "applyFoeDamageToPlayer", "foeTurn"]
    - path: "test/unit/party-combat.test.js"
      provides: "Direct pickFoeTarget unit coverage (zero-draw gate, pool mapping, downed exclusion, pool size)"
      contains: "pickFoeTarget"
    - path: "test/unit/combat.test.js"
      provides: "Direct applyFoeDamageToPlayer unit coverage + foeTurn-level control-flow proofs (reflect-kill continue, death early-return, absorb+shatter, Hardiness/halfNext order)"
      contains: "applyFoeDamageToPlayer"
  key_links:
    - from: "engine/combat.js foeTurn"
      to: "engine/combat.js pickFoeTarget"
      via: "const member = pickFoeTarget(state, rng) at the top of each swing"
      pattern: "const member = pickFoeTarget\\(state, rng\\)"
    - from: "engine/combat.js foeTurn"
      to: "engine/combat.js applyFoeDamageToPlayer"
      via: "const hit = applyFoeDamageToPlayer(state, f, rng, events, { dmg, roll, need }); if (hit.died) return events;"
      pattern: "applyFoeDamageToPlayer\\(state, f, rng, events, \\{ dmg, roll, need \\}\\)"
    - from: "engine/combat.js applyFoeDamageToPlayer"
      to: "engine/death.js die"
      via: "die(state, \"combat\", foe.name, rng, events) on lethal — the only remaining call site of the combat death terminator in combat.js"
      pattern: "die\\(state, \"combat\""
    - from: "engine/combat.js applyFoeDamageToPlayer"
      to: "engine/derived.js armorSoak"
      via: "effective armour read for the d20 soak gate (already imported at the top of combat.js)"
      pattern: "const av = armorSoak\\(c\\)"
  prohibitions:
    - requirement_id: FID-03
      category: safety
      statement: "MUST NOT edit test/parity/prototype-master.js.txt, regenerate any test/parity/fixtures/*.json, or change test/parity/harness/comparables.js — if the refactor seems to need a carve-out, the refactor changed behavior (17-RESEARCH.md Pitfall 4); fix the extraction instead"
    - requirement_id: FID-03
      category: safety
      statement: "MUST NOT add, remove, or reorder any RNG draw on the solo/empty-party path — the extraction is draw-for-draw identical; the only rng calls inside the helpers are the pre-existing pool roll (gated on live members) and the pre-existing d20 soak roll (gated on armour), plus the pre-existing draws inside killFoe/die"
    - requirement_id: FID-03
      category: values
      statement: "MUST NOT unify the simplified member-damage branch with the hero pipeline or route it through applyFoeDamageToPlayer — that asymmetry is a deliberate design (PITFALLS.md Pitfall 16); unification is deferred to Phase 19 by CONTEXT.md"
    - requirement_id: FID-03
      category: safety
      statement: "MUST NOT introduce DOM, Math.random, localStorage, or any non-engine import into engine/combat.js (engine-purity.test.js guards this) and MUST NOT add a new event type or change an existing event's shape"
---

<objective>
Extract the two shared foe-turn helpers FID-03 requires — `pickFoeTarget(state, rng)` and `applyFoeDamageToPlayer(state, foe, rng, events, { dmg, roll, need })` — out of `foeTurn` in `engine/combat.js` as named exports, re-wire `foeTurn`'s existing melee path to call them, and prove the extraction is behavior-preserving three ways: the untouched parity suite stays byte-identical to the frozen prototype, every pre-existing foeTurn unit test passes unmodified, and new direct unit tests pin each helper's exact draws, events, state mutations, and the load-bearing `{ died }` control-flow signal.

Purpose: Phase 19's foe-ability resolver (`engine/foeAbilities.js`) must reuse the melee path's targeting and ward/armour/damage pipeline instead of duplicating it; this is the seam it imports from. Extracting now, with no behavior change and under the full parity gate, means Phase 19's diff is purely additive.

Output: modified `engine/combat.js` (two new exports, foeTurn re-wired, member branch untouched); extended `test/unit/party-combat.test.js` and `test/unit/combat.test.js`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-CONTEXT.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md
@.planning/phases/17-fixture-inventory-foe-turn-refactors/17-PATTERNS.md
@engine/combat.js
@engine/rng.js
@engine/death.js
@engine/derived.js
@test/unit/combat.test.js
@test/unit/party-combat.test.js
@test/parity/harness/comparables.js
</context>

## Artifacts this phase produces

| Artifact | Kind | Plan | Notes |
|---|---|---|---|
| `pickFoeTarget(state, rng)` | exported function, `engine/combat.js` (placed after `downMember`, before `foeTurn`) | 17-02 | returns `null` (hero) or a live `C.allies` member; zero draws when no live member |
| `applyFoeDamageToPlayer(state, foe, rng, events, { dmg, roll, need })` | exported function, `engine/combat.js` (placed after `pickFoeTarget`, before `foeTurn`) | 17-02 | returns `{ died, onArmour }`; Hardiness → halfNext → ward → d20 soak → struckByFoe → die() |
| `enumerateFixtureRoster()`, `rosterToMarkdown()`, `FIXTURE_ORDER` | exports, `test/parity/harness/fixtureRoster.js` | 17-01 | see 17-01 |
| `tools/fixture-inventory.mjs`, `test/parity/FIXTURE-INVENTORY.md`, `test/parity/fixture-inventory.test.js` | CLI / doc / test | 17-01 | see 17-01 |
| `countingRng(inner)` + `test/unit/foe-turn-draw-count.test.js` | test helper / test file | 17-03 | see 17-03 |

## Assumptions locked by this plan (from 17-RESEARCH.md Assumptions Log / Open Questions)

- A1 (struckByFoe `need` field): resolved by passing swing data as a single options object `{ dmg, roll, need }` — this follows CONTEXT.md's stated preference for small, rng-explicit signatures of the shape `(state, foe, rng, events, opts)` over research's positional `(state, foe, dmg, roll, need, rng, events)`. The event shape is unchanged either way.
- A2 (location): both helpers are new named exports of `engine/combat.js` itself (not a new file), so Phase 19 imports them from `./combat.js` exactly as `engine/magic.js` already imports `liveFoes`/`killFoe`/`afterPlayerAction`.
- Return shape `{ died, onArmour }` (research Pattern 2): `onArmour` is informational for callers (Phase 19 narration); `foeTurn` only branches on `died` because on every non-death outcome the loop body is already finished.

<tasks>

<task type="auto">
  <name>Task 1: Extract pickFoeTarget and re-wire foeTurn's target selection; add direct unit coverage</name>
  <files>engine/combat.js, test/unit/party-combat.test.js</files>
  <read_first>
    - engine/combat.js lines 819-890 (foeTurn head: regen tick, acid tick, sleep check, swings computation, the PARTY-04/05 comment block and the inline live-member pool roll at lines 849-865, and the member branch at lines 866-889 which must stay byte-identical)
    - engine/combat.js lines 59-62 (liveFoes) and 799-810 (downMember) — sibling helper style; place the new export between downMember and foeTurn's JSDoc
    - engine/combat.js lines 170-182 (the existing zero-draw determinism-gate comment convention to mirror in the new JSDoc)
    - test/unit/party-combat.test.js lines 1-100 (header, fakeRng throw-on-underflow helper, fixedState/fixedFoe/fixedCombat/fixedAlly/fixedMember builders) and lines 150-200 (the four existing foeTurn target-pool tests that must keep passing unchanged)
    - .planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md "Pattern 1: Extract pickFoeTarget" (verbatim before/after) and "pickFoeTarget unit test pattern"
  </read_first>
  <action>
    In `engine/combat.js`, add a new exported function `pickFoeTarget(state, rng)` immediately after `downMember` and before the `foeTurn` JSDoc. Body: read `state.combat`; build the live-member pool from `C.allies` (members with `wp > 0`; an absent `allies` key yields an empty pool); if the pool is empty return `null` WITHOUT calling rng; otherwise draw exactly one `rng.d(pool.length + 1)` and return `null` when the draw is 1, else the pool entry at index `draw - 2`. Give it a JSDoc that (a) states it is a pure extraction of the former inline block (cite "formerly foeTurn lines 860-865, Phase 8 PARTY-04/05"), (b) carries the DETERMINISM GATE paragraph from the existing inline comment verbatim in spirit (the pool roll is drawn ONLY when at least one live member exists; empty/absent party means zero draws and null, byte-identical to every solo parity fixture), and (c) notes Phase 19's ability resolver reuses it.

    Then in `foeTurn`, delete the inline pool-building block and the `let member` / `if (pool.length) { draw }` lines and replace them with the single statement `const member = pickFoeTarget(state, rng);` keeping a one-line comment pointing at the helper for the gate rationale. Everything else in foeTurn — the `if (member) { ... continue; }` member branch and the hero branch — stays byte-identical in this task (the hero branch is Task 2's job).

    In `test/unit/party-combat.test.js`, add `pickFoeTarget` to the existing `engine/combat.js` import and append a new section `// --- pickFoeTarget: target pool helper (Phase 17, FID-03) ---` with these tests (reuse the file's own fakeRng/fixedAlly helpers; no new helper infrastructure):
    1. "pickFoeTarget: an absent allies key draws ZERO rng and returns null" — `{ combat: { foes: [] } }` with `fakeRng([])`; `assert.equal(result, null)`.
    2. "pickFoeTarget: an empty allies array draws ZERO rng and returns null" — `allies: []`, `fakeRng([])`.
    3. "pickFoeTarget: a party whose members are all at 0 wp draws ZERO rng and returns null" — two `fixedAlly({ wp: 0 })`, `fakeRng([])`.
    4. "pickFoeTarget: draw 1 targets the hero (null); draw k>1 targets live member k-2, in C.allies order" — allies Ada (wp 20) and Beo (wp 5); assert `fakeRng([1])` → null, `fakeRng([2])` → name Ada, `fakeRng([3])` → name Beo, and that each call consumed exactly one draw (a second call on the same fakeRng throws "sequence exhausted").
    5. "pickFoeTarget: downed members are excluded from the pool before the draw" — allies Ada (wp 0), Beo (wp 5); `fakeRng([2])` returns Beo (index into the LIVE list, not raw C.allies).
    6. "pickFoeTarget: the pool die has exactly (live members + 1) sides" — a recording rng whose `d(sides)` pushes `sides` into an array and returns 1; with two live members assert the recorded sides deepEqual `[3]`; with one live member `[2]`.
  </action>
  <verify>
    <automated>node --test test/unit/party-combat.test.js test/unit/combat.test.js && node --test test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function pickFoeTarget(state, rng)' engine/combat.js` prints 1
    - `sed -n '/^export function foeTurn/,$p' engine/combat.js | grep -c 'const member = pickFoeTarget(state, rng)'` prints 1
    - `sed -n '/^export function foeTurn/,$p' engine/combat.js | grep -v '^\s*//' | grep -c 'liveMembers'` prints 0 (the pool array no longer exists inside foeTurn; it lives in the helper above it)
    - `awk '/^export function pickFoeTarget/{p=1} /^export function foeTurn/{p=0} p' engine/combat.js | grep -c 'rng.d('` prints 1 (exactly one draw site in the helper)
    - `git diff -U0 c5fc219 -- engine/combat.js | grep -c 'mDmg\|mRoll\|mNeed\|memberStruck\|downMember(state, member, events)'` prints 0 (the member branch is untouched)
    - `node --test test/unit/party-combat.test.js` exits 0 reporting >= 19 tests passed, 0 failed (13 existing + 6 new)
    - `node --test test/unit/combat.test.js` exits 0 with all 48 existing tests passing unmodified
    - `node --test test/parity` exits 0 and `git status --porcelain test/parity` prints nothing
  </acceptance_criteria>
  <done>pickFoeTarget is an exported, documented helper with exactly one gated draw; foeTurn calls it; six direct tests pin the zero-draw gate, the mapping, downed exclusion and the pool die size; all pre-existing unit tests and the parity suite are green with no fixture changes.</done>
</task>

<task type="auto">
  <name>Task 2: Extract applyFoeDamageToPlayer (Hardiness onward) with the { died, onArmour } signal; add helper + foeTurn control-flow tests</name>
  <files>engine/combat.js, test/unit/combat.test.js</files>
  <read_first>
    - engine/combat.js lines 890-981 (the hero branch after Task 1: to-hit die + roll + need + miss check; raw dmg with sp.dmg/d6, weakened halving, critical doubling; then Hardiness reduction, the Pendant halfNext block with its ECON-08 comment, the ward absorb/reflect/shatter block, the zero-damage skip, the armour-soak block with its E8 comment, the wp decrement + struckByFoe event, and the lethal terminator + return) and lines 982-990 (the end-of-turn ward/mirror tick that the early return skips)
    - engine/death.js lines 90-112 (die() sets state.combat = null, c.ward = null, c.mirror = 0 and draws one rng.pick for the epitaph — 17-RESEARCH.md Pitfall 2: never read state.combat after the died path)
    - engine/combat.js lines 409-462 (killFoe: draws d6 sp, d10 coin, d20 item chance, and for Beasts without Cooking a d6 cook roll — the exact four draws the reflect-kill tests must feed)
    - engine/derived.js lines 216-227 (armorSoak(c) — the effective-armour read; plain-worn characters get av === worn values so the soak gate fires exactly as before) and line 18 (skill(c, name) reads c.skills[name])
    - test/unit/combat.test.js lines 1-110 (fakeRng / fixedFighter / fixedFloor / fixedState / fixedFoe / fixedCombat helpers) and lines 444-530 (existing foeTurn tests: crit-lethal, armour soak, cloak E8 pair, sleeping — all must pass unchanged)
    - .planning/phases/17-fixture-inventory-foe-turn-refactors/17-RESEARCH.md "Pattern 2" (line-by-line boundary map and the verbatim helper body to port — adapt its positional signature to the options-object signature locked below), Pitfall 1 and Pitfall 2
  </read_first>
  <action>
<!-- planner-discipline-allow: Hardiness -->
<!-- planner-discipline-allow: halfNext -->
    (The two identifiers above are allow-listed on purpose: Task 2's region-scoped gate asserts they no longer appear on code lines INSIDE foeTurn because they move into the helper defined ABOVE foeTurn — mentioning them here describes the helper, not foeTurn.)

    In `engine/combat.js`, add a new exported function `applyFoeDamageToPlayer(state, foe, rng, events, { dmg, roll, need })` directly after `pickFoeTarget` and before `foeTurn`'s JSDoc. It ports the hero-damage pipeline from Hardiness onward VERBATIM, in this order, mutating `state.c` exactly as the inline code did: (1) Hardiness — if `skill(c, "Hardiness")` then `dmg = Math.max(1, dmg - 3)`; (2) the Pendant of Fortitude single-charge halving — if `c.halfNext`: ceil-halve dmg, clear the flag, push `damageHalved` (move the existing ECON-08 comment with it); (3) the ward block — if the ward exists with a positive pool: absorb `min(pool, dmg)`, reduce the pool and dmg; if `c.ward.reflect` and warded > 0, subtract warded from `foe.wp`, push `wardReflected { target, amount }`, and if the foe drops to 0 or below call `killFoe(state, foe, rng, events)` and return `{ died: false, onArmour: false }` (the foe died, NOT the hero — Pitfall 1: this branch must never set died); else if warded > 0 push `wardAbsorbed { amount, remaining }`; then if the pool is now 0 or below push `wardShattered` and null the ward; (4) if `dmg <= 0` return `{ died: false, onArmour: false }`; (5) the armour block — compute `ignores` from `foe.sp && foe.sp.noArmor`, read `av` via the effective-armour helper imported from engine/derived.js, and ONLY when `av.wp > 0 && av.ar > 0 && !ignores` draw the single d20 soak roll; on a soak (roll at or under `av.ar`) set onArmour, record blocked = dmg, degrade worn armour wp only when `!av.magic && dmg > av.min`, zero dmg, push `armorDestroyed` when worn armour hits 0 (non-magic only) — move the E8 comment with it; if onArmour push `armorSoaked { name, amount: blocked }` and return `{ died: false, onArmour: true }`; (6) subtract dmg from `c.wp` and push the struckByFoe event with EXACTLY the existing key order `{ type: "struckByFoe", name: foe.name, roll, need, dmg, ignoresArmor: !!ignores, critical: roll === 1 }`; (7) if `c.wp <= 0` call the death terminator `die(state, "combat", foe.name, rng, events)` and return `{ died: true, onArmour: false }`; otherwise return `{ died: false, onArmour: false }`. JSDoc: state that it is a verbatim port of foeTurn's former hero-damage branch (cite "formerly lines 904-981"), enumerate the pipeline order, document the return contract (`died` true means the caller MUST return immediately because state.combat is now null; `onArmour` is informational), state the two gated draws (the d20 soak only behind the armour condition; killFoe/die draws only on their branches), and note that the member branch is deliberately NOT routed through it.

    Re-wire `foeTurn`'s hero branch: keep the to-hit die, roll, need, blind/penalty adjustments, the miss check, the raw dmg computation, the weakened halving and the critical doubling exactly as they are; delete everything from the Hardiness line through the lethal-return block; in their place put `const hit = applyFoeDamageToPlayer(state, f, rng, events, { dmg, roll, need });` followed immediately by `if (hit.died) return events;` and nothing else in the swing loop body (all non-death outcomes fall through to the next swing exactly as the old continue statements did; the next iteration's existing `if (!f.alive) break;` handles a reflect-killed foe). Leave the end-of-turn ward/mirror tick after the loops untouched. Update foeTurn's JSDoc to mention the two helpers.

    In `test/unit/combat.test.js`, add `applyFoeDamageToPlayer` to the combat import and append two sections. Section `// --- applyFoeDamageToPlayer (Phase 17, FID-03) ---` with direct tests, each using the file's fakeRng/fixedState/fixedFoe/fixedCombat helpers, setting `state.combat = fixedCombat([foe])` first, and calling the helper with an options object:
    1. plain hit, no armour: `{ dmg: 5, roll: 3, need: 5 }`, `fakeRng([])` → returns deepEqual `{ died: false, onArmour: false }`, `c.wp` 50, and the pushed struckByFoe deepStrictEqual `{ type: "struckByFoe", name: "Target", roll: 3, need: 5, dmg: 5, ignoresArmor: false, critical: false }` (also pins key order via `Object.keys`).
    2. Hardiness reduces by 3 with a floor of 1: skills `{ Hardiness: 1 }`; dmg 5 → wp 53; a second call with dmg 2 → wp 52 (floor 1, never 0 or negative — boundary).
    3. halfNext ceil-halves once then clears: `c.halfNext = true`, dmg 5 → wp 52 (3 damage), `c.halfNext === false`, events in order `damageHalved` then `struckByFoe`.
    4. Hardiness applies BEFORE halfNext: both set, dmg 5 → Hardiness 2 → halfNext ceil(1) = 1 → wp 54, struckByFoe dmg 1.
    5. ward absorbs fully, no struckByFoe, zero draws: ward `{ pool: 10, reflect: false, rounds: 3 }`, dmg 5, `fakeRng([])` → `wardAbsorbed { amount: 5, remaining: 5 }`, wp 55, pool 5, no struckByFoe, returns `{ died: false, onArmour: false }`.
    6. ward shatters and the remainder lands: pool 3, dmg 5 → events `wardAbsorbed(3)`, `wardShattered`, `struckByFoe` dmg 2; `c.ward === null`; wp 53.
    7. ward reflect kills the FOE and returns died:false (Pitfall 1): ward `{ pool: 10, reflect: true, rounds: 3 }`, foe wp 3 maxWP 3, dmg 5, `fakeRng([1, 1, 20, 1])` (killFoe's d6 sp, d10 coin, d20 item, d6 cook) → `wardReflected { target: "Target", amount: 5 }`, `foeKilled`, `foe.alive === false`, wp 55, returns deepEqual `{ died: false, onArmour: false }`, and `state.combat` still non-null.
    8. armour soaks: c `{ ar: 15, armorWP: 20, armorMax: 20, armorMin: 0, armor: "Studded" }`, dmg 5, `fakeRng([10])` → returns `{ died: false, onArmour: true }`, `armorSoaked { name: "Target", amount: 5 }`, armorWP 15, wp 55, no struckByFoe, and exactly one draw (a further `rng.d` throws).
    9. armour soak roll fails: same hero, `fakeRng([16])` → wp 50, struckByFoe dmg 5, onArmour false.
    10. armour destroyed: armorWP 3, armorMin 0, dmg 5, `fakeRng([10])` → armorWP 0 and events include `armorDestroyed` and `armorSoaked`.
    11. noArmor foe skips the soak draw: armoured hero, foe sp `{ noArmor: true }`, `fakeRng([])` → wp 50, struckByFoe `ignoresArmor: true`, zero draws.
    12. lethal returns died:true and die() ran: `c.wp = 1`, dmg 5 → returns deepEqual `{ died: true, onArmour: false }`, `state.dead === true`, `state.combat === null`, events end with `struckByFoe` then `died` with `cause: "combat"`.
    13. critical flag mirrors roll===1 and dmg is applied verbatim (the helper never doubles): `{ dmg: 14, roll: 1, need: 5 }` → struckByFoe `critical: true`, `dmg: 14`, wp 41.

    Section `// --- foeTurn control flow through the helpers (Phase 17, FID-03) ---` with foeTurn-level tests (exact fakeRng sequences verified by the planner against the pre-refactor code on 2026-09-13 — they must produce identical results post-refactor):
    A. "foeTurn: a ward-reflect kill mid-swing continues to the next foe and still runs the ward tick" — c `{ ward: { pool: 10, reflect: true, rounds: 3 }, mirror: 0 }`, foes A (wp 3, maxWP 3) then B (wp 10); `fakeRng([3, 4, 1, 1, 20, 1, 7])` (A hits for 5 → reflected → A dies via killFoe's four draws → B rolls 7 and misses) → events types exactly `["wardReflected", "foeKilled", "foeMissed"]`, A `alive === false`, B alive with wp 10, `c.wp` 55, `c.ward.pool` 5 and `c.ward.rounds` 2 (the end-of-turn tick ran), `state.dead === false`, and all 7 draws consumed.
    B. "foeTurn: the hero dying mid-loop returns immediately — remaining foes never swing and the ward tick is skipped" — c `{ wp: 1, ward: { pool: 0, reflect: false, rounds: 3 }, mirror: 0 }`, foes A then B; `fakeRng([3, 4])` → events types exactly `["struckByFoe", "died"]`, `state.dead === true`, `state.combat === null`, and B never drew (only 2 entries were provided; any further draw would have thrown).
    C. "foeTurn: a partial ward absorb shatters the ward and the remainder lands" — c ward `{ pool: 3, reflect: false, rounds: 3 }`, one foe, `fakeRng([3, 4])` → events `["wardAbsorbed", "wardShattered", "struckByFoe"]`, struckByFoe dmg 2, `c.wp` 53, `c.ward === null`.
    D. "foeTurn: Hardiness then halfNext, in that order" — c `{ skills: { Hardiness: 1 }, halfNext: true }`, one foe, `fakeRng([3, 4])` → events `["damageHalved", "struckByFoe"]`, struckByFoe dmg 1, `c.wp` 54, `c.halfNext === false`.
  </action>
  <verify>
    <automated>node --test test/unit/combat.test.js test/unit/party-combat.test.js test/unit/engine-purity.test.js test/unit/formatEventsCoverage.test.js && node --test test/parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export function applyFoeDamageToPlayer(state, foe, rng, events, { dmg, roll, need })' engine/combat.js` prints 1
    - `sed -n '/^export function foeTurn/,$p' engine/combat.js | grep -c 'const hit = applyFoeDamageToPlayer(state, f, rng, events, { dmg, roll, need });'` prints 1 and the very next non-blank line is `if (hit.died) return events;` (verify with `grep -A1 'const hit = applyFoeDamageToPlayer' engine/combat.js | tail -1 | grep -c 'if (hit.died) return events;'` → 1)
    - `sed -n '/^export function foeTurn/,$p' engine/combat.js | grep -v '^\s*//' | grep -c 'rng.d(20)'` prints 0 (the soak roll now lives only in the helper)
    - `sed -n '/^export function foeTurn/,$p' engine/combat.js | grep -v '^\s*//' | grep -c 'c.ward.pool\|Hardiness\|halfNext\|armorSoak('` prints 0
    - `grep -v '^\s*\(//\|\*\)' engine/combat.js | grep -c 'die(state, "combat"'` prints 1 (only inside the helper)
    - `awk '/^export function applyFoeDamageToPlayer/{p=1} /^export function foeTurn/{p=0} p' engine/combat.js | grep -c 'rng.d(20)'` prints 1 and the same region contains `killFoe(state, foe, rng, events)` once and `return { died: true, onArmour: false }` once
    - `sed -n '/^export function foeTurn/,$p' engine/combat.js | grep -c 'if (c.ward && --c.ward.rounds <= 0)'` prints 1 (end-of-turn tick untouched)
    - `git diff -U0 c5fc219 -- engine/combat.js | grep -c 'mDmg\|mRoll\|mNeed\|memberStruck\|downMember(state, member, events)'` prints 0 (member branch untouched across both tasks)
    - `git diff c5fc219 --stat -- engine/ | grep -c 'engine/'` prints 1 (only engine/combat.js changed under engine/)
    - `node --test test/unit/combat.test.js` exits 0 reporting >= 65 tests passed, 0 failed (48 existing + 13 helper + 4 foeTurn-level)
    - `node --test test/unit/engine-purity.test.js test/unit/formatEventsCoverage.test.js` exits 0 (no DOM/Math.random; no new event type)
    - `node --test test/parity` exits 0 and `git status --porcelain test/parity package.json` prints nothing
    - `npm test` exits 0 with 0 failing tests
  </acceptance_criteria>
  <done>applyFoeDamageToPlayer owns the Hardiness-onward pipeline with a { died, onArmour } signal; foeTurn's hero branch is to-hit + raw damage + one helper call + the died early-return; 13 direct helper tests and 4 foeTurn control-flow tests pass; every pre-existing test and the byte-identical parity suite are green with zero fixture/harness/master edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file → engine state | Unchanged by this phase: no new `state.c` / `state.combat` fields, so `engine/save-validation` and the `*Comparable()` carve-outs are untouched |
| foeTurn → helpers | Internal pure-function boundary inside `engine/combat.js`; both helpers are called only from `foeTurn` (and later Phase 19's resolver), never from an action handler or with external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-04 | Tampering | `engine/combat.js` foeTurn draw order (a silently reordered/extra draw desyncs every later roll) | high | mitigate | Parity suite byte-identical against the frozen master (`node --test test/parity`), fakeRng throw-on-underflow tests pin exact draws, and 17-03's counting test pins totals |
| T-17-05 | Tampering | `test/parity/prototype-master.js.txt` / fixtures / `comparables.js` edited to make a changed behavior pass | high | mitigate | Prohibited in must_haves; acceptance criteria require `git status --porcelain test/parity` to be empty; any needed carve-out is treated as a refactor bug (RESEARCH Pitfall 4) |
| T-17-06 | Denial of Service | reading `state.combat` after `die()` nulls it (crash mid-turn) | medium | mitigate | `if (hit.died) return events;` is the first statement after the helper call (acceptance criterion) and test B proves no further reads/draws |
| T-17-07 | Elevation of Privilege | engine gaining DOM / `Math.random` / storage access during the refactor | low | mitigate | `test/unit/engine-purity.test.js` static guard runs in the task verify |
| T-17-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase; `package.json`/lockfile unchanged (checked); slopcheck + blocking human checkpoint would gate any [ASSUMED]/[SUS] package, none exist |
</threat_model>

<verification>
- `node --test test/unit/combat.test.js test/unit/party-combat.test.js` green: >= 65 and >= 19 tests respectively, every pre-existing test unmodified.
- `node --test test/parity` green, byte-identical to the frozen prototype; `git status --porcelain test/parity` empty.
- `npm test` green (683 existing + 23 new from this plan, plus 17-01's tests if it has landed), 0 failing.
- `engine/combat.js` exports `pickFoeTarget` and `applyFoeDamageToPlayer`; `foeTurn` calls both; the member branch diff against `c5fc219` is empty.
</verification>

<success_criteria>
- ROADMAP Phase 17 success criterion 2 satisfied: `pickFoeTarget` and `applyFoeDamageToPlayer` exist as shared, tested helpers used by the existing foe-turn melee path, with behavior-preserving tests proving zero output change.
- ROADMAP success criterion 4 (parity byte-identical) holds after the refactor with no fixture regeneration.
- The `{ died }` early-return contract and the ward-reflect-kill non-death path are each pinned by a dedicated test at both the helper and foeTurn level.
</success_criteria>

<output>
Create `.planning/phases/17-fixture-inventory-foe-turn-refactors/17-02-SUMMARY.md` when done (record the final helper signatures, the foeTurn call-site lines, and the unit/parity test counts).
</output>
