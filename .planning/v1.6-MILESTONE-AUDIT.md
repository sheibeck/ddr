---
milestone: v1.6
audited: 2026-09-20T12:00:00Z
status: tech_debt
scores:
  requirements: 19/19
  phases: 6/6
  integration: 6/6
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: milestone-close
    items:
      - "Pixel 7 UAT batch not yet run: docs/UAT-v1.6.md (26 items, from the 44/45/46/47 VERIFICATION lists) on APK c0cdbae (the build on the phone); docs/UAT-v1.5.md (140 items) was never run either — both are pending device time, neither blocks completion under the deferred-UAT protocol"
  - phase: 47-shell-modularisation
    items:
      - "SHELL-04's '< 5,000 lines' clause re-baselined to 5,621 by user ruling (5,682 after Phase 49's instrumentation); the remaining bulk is Map/Combat/Rail/Oracle/Graves renderers, markup/CSS and comments — a future combat/map carve if ever wanted (docs/SHELL-MODULES.md#Line budget)"
      - "renderDropShelf and paint()'s tab-mount skeleton deliberately stay in the shell (CONTEXT ruling)"
  - phase: 48-stale-docs-comments-test-names-purge
    items:
      - "usable-features-audit.test.js:566 reads a dead `.toasts` field (a field name, outside the bounded test-body rule) — one-line behaviour-level cleanup"
      - "`classic script` kept as a live term (79 mentions; orchestrator judgment, unenforced tripwire row) — flip to enforced and rename the block if the user disagrees"
      - "Fixture-hygiene commit 4272f35 (inert flightLeft/flightCooldown/won keys removed from 56 test files) is reversible alone with git revert"
  - phase: 49-measure-first-perf-pass
    items:
      - "step p95 19.8 ms after two fixes (median 11.5) — criterion 3 partial by user ruling; next lever is the dark-region draw cost (draw p95 swings 3.6→11.2 ms by route) — measure dark-only vs lit-only walks before touching draw()"
      - "Dev-gated perf instrumentation kept in the shell (7 guarded performance.now lines, 0 in a normal run) — removal is one commit if unwanted"
      - "Dev Start-at-depth rolls a fresh character (Phase 21 design) — the user noticed; possible todo: keep the rolled character"
  - phase: backlog-and-todos
    items:
      - "Todos (all rules changes, outside v1.6): initiative once per combat; enemy attack cadence + initiative in the Oracle + damage-curve audit; average run ends floor 5–7 (four-band difficulty shape); rail overlays the map without reflow + tap-to-dismiss + longer hold"
      - "Backlog phases 999.1 Transitions & Sounds, 999.2 Joiner level cap, 999.3 Dungeon set dressing"
      - "www/ is 6.1 MB (ROADMAP baseline 4.8 MB) — growth is the sfx/ and icons/ assets committed during this run, not code"
---

# v1.6 "Shell Debt & Dead Code" — Milestone Audit

**Audited:** 2026-09-20 (orchestrator-authored; the integration checker is a verification agent and is disabled for usage limits — the whole gate set was re-run by the orchestrator at HEAD `41f6fb7` instead).
**Status:** `tech_debt` — no blockers; 19/19 requirements complete; the deferred items are the device UAT batch and a short list of follow-ups already captured as todos, backlog entries or doc notes.

**Span:** 136 commits from `51ec3bd` (Phase 44 context) to `41f6fb7`; 204 files, +11,030 / −7,059 lines across shell, `src/`, `engine/` (comments + the `won` removal only), `content/` (comments only), `tools/`, `test/`, `docs/`, `.claude/`. `mazeworld.html` 8,710 → 5,682 lines (−35%). Tests 2,924 (v1.5 close) → 3,315. `test/parity/prototype-master.js.txt` hash `a1f4d0dc29782218d8e5aab65bc5989c33f917f0` throughout.

## Requirements (3-source cross-reference)

| Source | Result |
|---|---|
| `REQUIREMENTS.md` traceability table | 19 v1.6 rows (DEAD-01..05, NAME-01..02, HEDGE-01..03, SHELL-01..04, DOCS-01..03, PERF-01..02), all `Complete`; 19/19 boxes checked |
| Phase `VERIFICATION.md` requirement sections | every ID ✓ in its phase's file; 44/45/46/47/48/49 all `status: passed`, `gaps: []`; four recorded overrides (46 decision-gate parse, 47 SHELL-04 re-baseline, 48 `classic script`, 49 criterion 3 p95) each with the user ruling or the orchestrator's stated judgment |
| `SUMMARY.md` `requirements-completed` frontmatter | union over the 24 plan SUMMARYs = 17 IDs; **SHELL-04** and **DEAD-02**'s pairing are the two manual checks: SHELL-04 was completed by the orchestrator's re-baseline ruling after 47-05 (not by a plan — recorded in 47-VERIFICATION and REQUIREMENTS), DEAD-02 is listed by 44-03 ✓ |

No `unsatisfied` or `orphaned` requirement; the one `partial` by the matrix (SHELL-04, passed + not in a plan frontmatter) is verified manually above. `gaps_found` not forced.

## Phases

| Phase | Plans | Tests at close | Verification | Device checks |
|---|---|---|---|---|
| 44 Retire the classic engine from the shell | 4 | 3,243 | passed | 10 deferred |
| 45 Collapse the Phase 37 hedges | 3 | 3,242 | passed | 3 deferred |
| 46 Honest names, dead exports, the tutorial decision | 4 | 3,231 | passed (1 override) | 6 deferred |
| 47 Shell modularisation | 5 | 3,288 | passed (1 override) | 7 deferred |
| 48 Stale docs, comments & test names purge | 5 | 3,293 | passed (1 override) | none |
| 49 Measure-first perf pass | 2 | 3,315 | passed (1 override) | done in-phase (3 Pixel 7 rounds) |

## Integration (orchestrator re-run at HEAD `41f6fb7`)

| Check | Result |
|---|---|
| Full suite | `npm test` **3,315 / 0** |
| Parity + roundtrip | `node --test test/parity/*.test.js test/roundtrip/*.test.js` **60 / 0** — the engine still replays the 1994 prototype byte-for-byte on every fixture |
| Headless boot | `npm run boot:check` **4/4** (no-uncaught, painted, graves, title) on the built `www/` |
| Shell ↔ modules | `test/unit/shell-tab-snapshots.test.js` 10/10 (Gear/Hero/Store render byte-equal to the pre-carve fixtures through 47-03/04/05 AND Phase 49's two fixes); `bridge-registry.test.js` 10/10 + `bridge-doc --check` (45 live `__mz*` names, one registry, doc generated from it); `shell-no-content-copies.test.js` (no classic duplicate of any `src/browser/` or `content/` export) |
| Engine fence | `git diff` since Phase 44 start over `engine/`, `content/` = the Phase 46 `won` removal + Phase 48 comment lines only (stripped diff empty); `test/parity/fixtures/` moved only by Phase 45's declared 13-site set; master hash unchanged |
| Standing tripwires | `tools/stale-terms.mjs` exit 0 (pinned by 5 tests); `tools/ident-sweep.mjs --self-test` PASS; `tools/shell-sweep.mjs orphans` → 1 (`railTimer`, a timer handle, expected); Phase 44's boot/refs gates green |
| Native build | `npm run android:debug` built three times this run; APKs `742c916`, `b8c9293`, `c0cdbae` installed and played on the Pixel 7 (build CP2A.260705.006) |

## E2E flows

| Flow | Evidence |
|---|---|
| Cold boot → title → roller → map (no classic engine) | `boot:check` 4/4; Phase 44 `__mzClassicBoot` sweep; on-device launches ×3 in Phase 49 |
| Step → engine dispatch → narration → rail → paint → draw | Phase 49 instrumentation brackets exactly this path; three device rounds, no jank reported; DOM snapshots unchanged |
| Gear / Hero / Store render from modules through `__mzTabs` | 7 fixtures byte-equal; tab switch re-mounts (Phase 49 fix 1) with snapshots unchanged; on-device tab switches during the perf walks |
| Save → resume (tolerant load of pre-v1.6 keys: `won`, `controlScheme`, legacy counters, unmigrated `c.worn`) | `save-validation.test.js`, `worn-migration.test.js`, settings tests; the on-device 1.4.0 save survived three `adb install -r` rounds — the UAT batch's resume items exercise it |
| Bot plays the single worn path | Phase 45's 143-cell × 3-seed smoke, 0 stuck, `Bot:` line byte-equal to the v1.5 pin |

## Tech debt

See the frontmatter list. Nothing is unrecorded: every deferred item lives in a todo file, a backlog phase, a doc ledger (`docs/SHELL-MODULES.md#Line budget`, `docs/PERF-BASELINE.md` Decision) or a VERIFICATION note.

## Nyquist

Inactive for this project (validation agents off) — no `VALIDATION.md` files expected; not a gap.

## Recommendation

Complete the milestone (`/gsd-complete-milestone v1.6`), then run the two UAT batches on APK `c0cdbae` at the user's convenience; any ❌ goes through a UAT gap plan, never an ad-hoc edit. Offer the Play internal-testing build after the batch, per the standing rule.
