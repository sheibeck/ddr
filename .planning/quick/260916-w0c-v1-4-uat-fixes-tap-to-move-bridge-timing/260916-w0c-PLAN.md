---
quick_id: 260916-w0c
slug: v1-4-uat-fixes-tap-to-move-bridge-timing
date: 2026-09-16
status: planned
phase: quick-260916-w0c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mazeworld.html
  - test/unit/shell-map-viewport.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/shell-map-invariants.test.js
  - test/unit/shell-gear-toolbar.test.js
  - test/unit/shell-map-rail.test.js
autonomous: true
requirements: [MAP-02, MAP-07, MAP-06, MAP-03]
tags: [vanilla-js, uat-fix, tap-to-step, bridge-timing, png-icons, marks-legend, rail, settings-gear, source-assertion-tests]

must_haves:
  truths:
    - "A clean tap on the map viewport steps the party one square toward the tap (the pointerdown handler no longer throws on an undefined bridge — the tapStep.js constants are read at call time, never captured when the classic script parses)"
    - "Every seen feature square draws its v1.3 PNG icon (icons/optimized/<key>.png via icons.js drawFeatureIcon, one-way door rotated to c.dir, 0.75 scale) and the party draws party.png over the radial glow with the pulsing #mw-party-pulse DOM ring around it; the Phase 35 palette (fog / bevelled walls / floor inset / border) and the vignette are unchanged; the MARKS sheet rows show the same PNGs at 34x34"
    - "The settings gear is a chip in the viewport chip row, to the RIGHT of MAKE CAMP (row order MARKS, CENTRE, gap, MAKE CAMP, gear), opens the Settings sheet exactly as before, and no longer appears in the HUD strip"
    - "The bottom RAIL is visible only on the Map tab: on Hero/Gear/Oracle/Dead it is hidden; returning to Map shows a card that arrived meanwhile if it is still within its hold; the existing combat/dead/won hide still applies on Map"
    - "npm test is green (fail 0, pass >= 2170), build:www exits 0, the engine/content/parity tree is untouched, the parity golden-master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0, and a fresh debug APK exists"
  artifacts:
    - path: "mazeworld.html"
      provides: "the lazy T() bridge accessor in initMazeViewportControls; draw()'s restored PNG icon + party.png block; the <img> legend rows + .mw-legend-row img CSS; the gear chip in .mw-map-chips; mwActiveTab + showTab/renderRail map-only rail visibility"
    - path: "test/unit/shell-map-viewport.test.js"
      provides: "re-pinned (f): the init-time capture literal is absent, T() is read at call time; one new bridge-timing test"
    - path: "test/unit/shell-map-hud.test.js"
      provides: "re-pinned (a)/(h)/(i)/(j): gear out of the HUD and into the chip row, PNG draw path required, <img> legend rows required"
    - path: "test/unit/shell-map-invariants.test.js"
      provides: "re-pinned: iconReady no longer retired, the two PNG-retirement tests flipped to PNG-canon pins, the four chip listeners"
    - path: "test/unit/shell-gear-toolbar.test.js"
      provides: "re-pinned chip order MARKS, CENTRE, MAKE CAMP, gear"
    - path: "test/unit/shell-map-rail.test.js"
      provides: "one new test (o): the rail is a map-tab element"
  key_links:
    - from: "initMazeViewportControls pointerdown/pointermove/release (classic, parse-time IIFE)"
      to: "window.__mzTapStep (assigned by the trailing module script, after every classic script)"
      via: "const T = () => window.__mzTapStep || { HOLD_MS: 450, TAP_MAX_TRAVEL_PX: 10 }; T().HOLD_MS / T().TAP_MAX_TRAVEL_PX at every use"
      pattern: "T\\(\\)\\.TAP_MAX_TRAVEL_PX"
    - from: "draw() (classic)"
      to: "window.__mzIconsApi / window.__mzIconMap (module bridge, awaited preloadIcons before boot)"
      via: "iconsApi.featureKeyForCell(c) -> iconMap[key] -> iconsApi.drawFeatureIcon(ctx, img, x * CELL, y * CELL, CELL, c.dir, 0.75); party via iconMap[iconsApi.PLAYER_MARKER_ICON]"
      pattern: "iconsApi\\.drawFeatureIcon\\(ctx, partyImg, px \\* CELL, py \\* CELL, CELL\\)"
    - from: "showTab(name) (classic initTabs IIFE)"
      to: "renderRail() (classic, hoisted) via window.renderRail (assigned later in the classic script)"
      via: "mwActiveTab = name; railEl.hidden = name !== \"maze\"; if (name === \"maze\") window.renderRail?.();"
      pattern: "mwActiveTab !== \"maze\""
  prohibitions:
    - "No edits under engine/, content/, test/parity/ (ENGINE GATE); test/parity/prototype-master.js.txt hash must stay a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
    - "No edits to src/browser/icons.js, src/browser/mapMarks.js, src/browser/tapStep.js, icons/optimized/*.png, test/unit/mapMarks.test.js, test/unit/tutorial.test.js — the modules are intact; only the shell's consumption changes"
    - "Do NOT delete src/browser/mapMarks.js or its bridge: draw() still reads MAP_PALETTE from window.__mzMapMarks, inspectAt() still reads markForCell/legendFor; MARK_GLYPHS simply becomes renderer-unused"
    - "No adb commands — the orchestrator installs the APK"
    - "No wiring of the gear chip through guardTap: guardTap sets aria-disabled and gates on encArmed(), and the arm sweep only covers the overlay/rail — a static chip would be stranded disabled. The three existing map chips are plain-listener wired and the invariants suite pins that shape; the gear keeps its existing module listener"
---

<objective>
Land the four items from the user's first Pixel 7 UAT round on v1.4 (2026-09-16) — all shell/test-only, zero engine/content/parity edits:

1. **BUG — tap-to-move never steps.** `initMazeViewportControls` (a parse-time IIFE in the classic `<script>`) captures `window.__mzTapStep` eagerly, but that bridge is assigned by the trailing `<script type="module">`, which runs after every classic script. The capture is `undefined`, `pointerdown` throws while building the gesture object, and no tap gesture is ever recorded (drag/pinch survive because `startPan` runs before the throw). Fix: read the bridge lazily at call time in every handler.
2. **USER DECISION — restore the v1.3 PNG map icons.** The user: "You replaced all my icons, use the good icons images from the previous version." This reverses Phase 35 decision 3 (coloured text-glyph marks). **Record verbatim in the SUMMARY:** "user reversed Phase 35 decision 3 on 2026-09-16 UAT — PNG icons are canon; the glyph experiment is retired". The Phase 35 palette, vignette, and the `#mw-party-pulse` DOM ring all stay.
3. **USER DECISION — the settings gear becomes a chip right of MAKE CAMP.** The user: "Move the settings gear into a button and put it on the right of the make camp button." The gear leaves the HUD strip and joins `.mw-map-chips` as the right-most chip; the Settings sheet behaviour is unchanged; the DEV chip stays where it is.
4. **USER DECISION — the bottom RAIL shows only on the Map tab.** **Record verbatim:** "user ruling 2026-09-16 UAT: rail is a map-tab element". This supersedes the Phase 35-02 planner-discretion placement ("visible below the Gear list on every tab").

Purpose: the v1.4 map screen is unusable on-device without (1), and (2)–(4) are direct user rulings from the UAT batch.
Output: `mazeworld.html` + six re-pinned/extended test files; a fresh debug APK for the orchestrator to install.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v1.4-phases/35-map-screen-rebuild/35-03-SUMMARY.md
@.planning/milestones/v1.4-phases/35-map-screen-rebuild/35-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tap-to-move bridge timing — read window.__mzTapStep lazily in initMazeViewportControls; re-pin shell-map-viewport.test.js</name>
  <files>mazeworld.html, test/unit/shell-map-viewport.test.js</files>
  <read_first>
    - mazeworld.html lines 6300-6395 (the `(function initMazeViewportControls() {` IIFE: the eager bridge capture on its fourth line ~L6310; the four use sites — the holdTimer callback + timer duration at ~L6341-6342 in pointerdown, the travel cap at ~L6357 in pointermove, the tap check at ~L6383 in release)
    - mazeworld.html lines 6268-6299 (`function tapStep(clientX, clientY)` / `function inspectAt(clientX, clientY)` — CONFIRM, do not edit: both already read `window.__mzControls`, `window.__mzTapStep`, `window.__mzRailVM`, `window.__mzMapMarks` inside their bodies, i.e. at call time)
    - mazeworld.html lines 6829-6843 (the module bridge block: `window.__mzTapStep = { resolveStep, inspectCell, HOLD_MS, TAP_MAX_TRAVEL_PX, DIR_VECTORS };` — unchanged; this is what runs AFTER the classic IIFE)
    - src/browser/tapStep.js lines 20-30 (`TAP_MAX_TRAVEL_PX = 10`, `HOLD_MS = 450` — the fallback values must match these)
    - test/unit/shell-map-viewport.test.js lines 66-76 (`viewportRegion()` / `pointermoveRegion()` slicers) and 175-189 (the `(f) viewport region: the gesture tracker ...` test that pins the eager capture + the `T.` member reads — re-pin)
  </read_first>
  <action>
    <!-- planner-discipline-allow: const T = window.__mzTapStep; -->
    1. In `initMazeViewportControls`, replace the eager fourth-line capture (the `const T =` assignment that reads the bridge at parse time) with a lazy accessor: `const T = () => window.__mzTapStep || { HOLD_MS: 450, TAP_MAX_TRAVEL_PX: 10 };`. Comment above it (prose, no retired literal): "2026-09-16 UAT fix: this IIFE runs while the classic script is being parsed, but window.__mzTapStep is assigned by the trailing module script, which runs after every classic script — an init-time capture is always undefined, so pointerdown threw while building the gesture object and no tap was ever recorded (drag/pinch survived because startPan runs first). Read the bridge at call time; the fallback mirrors tapStep.js's HOLD_MS/TAP_MAX_TRAVEL_PX so a tap still works even if the bridge were somehow missing."
    2. Change every member read to go through the accessor: in pointerdown's holdTimer callback `gesture.travel <= T().TAP_MAX_TRAVEL_PX`, the timer duration `}, T().HOLD_MS) };`, in pointermove `if (gesture.travel > T().TAP_MAX_TRAVEL_PX) clearTimeout(gesture.holdTimer);`, in release `gst.travel <= T().TAP_MAX_TRAVEL_PX`. Nothing else in the IIFE changes — the four Phase 33 pan/pinch literals (`const wasPinch = !!pinch;`, `if (wasPinch && pts.size < 2) window.mzCenterMap?.();`, `zoom = clampZoom(pinch.zoom * (d / pinch.dist)); fit(); positionCanvas();`, `vp.addEventListener("pointerup", release)`) and every other gesture line stay byte-identical.
    3. Confirm (read-only) that no other parse-time code in the classic script captures a module bridge: every other `= window.__mz...` read in the file sits inside a function body that runs after boot (fit(), draw(), renderRail(), tapStep(), inspectAt(), ...). `tapStep()`/`inspectAt()` need no edit.
    4. Re-pin `test/unit/shell-map-viewport.test.js` `(f) viewport region: the gesture tracker ...`: replace the eager-capture `assert.match` with `assert.doesNotMatch(region, /const T = window\.__mzTapStep;/)` plus `assert.match(region, /const T = \(\) => window\.__mzTapStep \|\| \{ HOLD_MS: 450, TAP_MAX_TRAVEL_PX: 10 \};/)`; change the two member pins to `assert.equal((region.match(/T\(\)\.HOLD_MS/g) || []).length, 1)` and `assert.equal((region.match(/T\(\)\.TAP_MAX_TRAVEL_PX/g) || []).length, 3)`. Add one new test directly after it — `(f) bridge timing (2026-09-16 UAT fix): the viewport IIFE reads the tapStep bridge only through the call-time accessor` — asserting `(region.match(/window\.__mzTapStep/g) || []).length === 1` (the accessor is the sole reference) and `assert.doesNotMatch(region, /^\s*(?:const|let|var) \w+ = window\.__mz\w+;/m)` (no parse-time capture of ANY module bridge in the IIFE). Update the file's header comment line for (f) to mention the call-time accessor.
  </action>
  <verify>
    <automated>test "$(grep -c 'const T = window.__mzTapStep;' mazeworld.html)" = "0" && test "$(grep -c 'const T = () => window.__mzTapStep || { HOLD_MS: 450, TAP_MAX_TRAVEL_PX: 10 };' mazeworld.html)" = "1" && test "$(grep -c 'T().HOLD_MS' mazeworld.html)" = "1" && test "$(grep -c 'T().TAP_MAX_TRAVEL_PX' mazeworld.html)" = "3" && node --test test/unit/shell-map-viewport.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'const T = window.__mzTapStep;' mazeworld.html` = 0 and `grep -c 'const T = () => window.__mzTapStep || { HOLD_MS: 450, TAP_MAX_TRAVEL_PX: 10 };' mazeworld.html` = 1.
    - `grep -c 'T().HOLD_MS' mazeworld.html` = 1; `grep -c 'T().TAP_MAX_TRAVEL_PX' mazeworld.html` = 3 (holdTimer callback, pointermove, release).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('(function initMazeViewportControls()');const b=s.indexOf('addEventListener(\"keydown\"',a);const r=s.slice(a,b);process.exit(((r.match(/window\.__mzTapStep/g)||[]).length===1 && !/[^(]T\.(HOLD_MS|TAP_MAX_TRAVEL_PX)/.test(r) && !/^\s*(?:const|let|var) \w+ = window\.__mz\w+;/m.test(r))?0:1)"` exits 0 (the accessor is the only bridge reference in the IIFE; no bare `T.` member read; no parse-time capture).
    - The four Phase 33 literals still occur once each in the IIFE: `grep -c 'const wasPinch = !!pinch;' mazeworld.html` = 1, `grep -c 'if (wasPinch && pts.size < 2) window.mzCenterMap?.();' mazeworld.html` = 1, `grep -c 'zoom = clampZoom(pinch.zoom \* (d / pinch.dist)); fit(); positionCanvas();' mazeworld.html` = 1, `grep -c 'vp.addEventListener("pointerup", release)' mazeworld.html` = 1; `grep -c 'window.mzCenterMap?.()' mazeworld.html` = 10 (unchanged).
    - `grep -c 'window.__mzTapStep = { resolveStep, inspectCell, HOLD_MS, TAP_MAX_TRAVEL_PX, DIR_VECTORS };' mazeworld.html` = 1 (the module bridge is untouched).
    - `node --test test/unit/shell-map-viewport.test.js` exits 0 with 26 tests (25 + the new bridge-timing test); `node --test test/unit/shell-map-store-polish.test.js test/unit/shell-map-invariants.test.js` exits 0.
    - `git diff --stat -- engine content test/parity src/browser/tapStep.js` prints nothing.
  </acceptance_criteria>
  <done>A tap on the viewport reaches tapStep() (no throw in pointerdown); the bridge is read at call time everywhere in the IIFE; the viewport suite pins the fix.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the v1.3 PNG map icons + party.png in draw() and the MARKS legend (user reversed Phase 35 decision 3); re-pin shell-map-hud + shell-map-invariants</name>
  <files>mazeworld.html, test/unit/shell-map-hud.test.js, test/unit/shell-map-invariants.test.js</files>
  <read_first>
    - mazeworld.html lines 2601-2682 (the comment above `function draw()` + draw(): KEEP lines through the outer-border `strokeRect(3, 3, size - 6, size - 6)` pass and the `const M = window.__mzMapMarks; const P = ...` palette read; REPLACE the `if (M) { ... }` glyph mark loop and the party-marker block; `positionCanvas();` stays last; the `/* ---------------- log` banner at ~L2682 is the region end)
    - `git show v1.3:mazeworld.html | sed -n '2578,2637p'` (the canon PNG block to restore: the `iconsApi`/`iconMap`/`iconReady` reads, the seen/feat loop with `featureKeyForCell(c)` -> `iconMap[key]` -> `drawFeatureIcon(ctx, img, x * CELL, y * CELL, CELL, c.dir, 0.75)`, and the party block with `partyImg`, the radial glow, `drawFeatureIcon(ctx, partyImg, px * CELL, py * CELL, CELL)` and the dot+"P" fallback — NOTE v1.3's fallback used a `C("--ditto")` CSS-var helper and the "Special Elite" font, neither of which exists in today's file: substitute `P.party` and `"Courier Prime", monospace`)
    - mazeworld.html lines 1093-1101 (legend CSS: `.mw-legend-row{...}`, then the two-line Phase 35 comment + the `.mw-legend-glyph{...}` rule to replace, then `.mw-legend-row-name`/`-desc`), and `git show v1.3:mazeworld.html | sed -n '1057,1058p'` (the `.mw-legend-row img{width:34px;height:34px;object-fit:contain;flex:none}` rule to restore verbatim)
    - mazeworld.html lines 6170-6203 (the comment above + `function renderMarksLegend()`: keep the build-once guard, `const M = window.__mzMapMarks;`, the `for (const row of M.MARKS_LEGEND)` loop and the name/desc spans; replace the glyph span with an `<img>`), and `git show v1.3:mazeworld.html | sed -n '5659,5666p'` (the v1.3 row shape: `<img src="./icons/optimized/${key}.png" alt="" aria-hidden="true">`)
    - mazeworld.html lines 6572-6577 and 6829-6843 (module: the icons.js import `preloadIcons, featureKeyForCell, drawFeatureIcon, PLAYER_MARKER_ICON`, `window.__mzIconsApi = { featureKeyForCell, drawFeatureIcon, PLAYER_MARKER_ICON };`, `window.__mzIconMap = await preloadIcons("./icons/optimized");`, `window.__mzMapMarks = {...}` — all INTACT; verify and reuse, do not edit the code lines; reword only the two "retired PNG pipeline / draw() reads this module instead" comments)
    - src/browser/icons.js lines 18-60 and 130-150 (`FEATURE_ICONS`, `PLAYER_MARKER_ICON = "party"`, `featureKeyForCell(cell)` feat->key mapping, `drawFeatureIcon(ctx, img, dx, dy, size, dir, scale = 1.0)`), src/browser/mapMarks.js lines 71-90 (`MARKS_LEGEND` rows keyed by `key`: encounter/teleport/onewaydoor/trap/chest/crevice/descent/wall/party — each matches an `icons/optimized/<key>.png`; `ls icons/optimized/` confirms all nine)
    - test/unit/shell-map-hud.test.js lines 1-30 (header), 66-75 (`drawRegion()` slices RAW from `function draw()` to the log banner, stripping `//` comments), 269-294 (`(h) chrome` — its zero-grep literal list includes `iconReady` and `mw-legend-row img`), 296-325 (`(i) draw()` needles/gone lists), 335-344 (`(j) renderMarksLegend`), 399-404 (`(m) BEHAVIOUR` title)
    - test/unit/shell-map-invariants.test.js lines 74-98 (the `RETIRED` map — `iconReadyFn`), 128-132 (the retirements test iterating that key list), 144-153 (the two PNG-retirement tests to flip)
  </read_first>
  <action>
    <!-- planner-discipline-allow: mw-legend-glyph -->
    <!-- planner-discipline-allow: MARK_GLYPHS -->
    <!-- planner-discipline-allow: fillText(mark.glyph -->
    <!-- planner-discipline-allow: MARK_SCALE -->
    <!-- planner-discipline-allow: markForCell( -->
    Record in the SUMMARY (verbatim): "user reversed Phase 35 decision 3 on 2026-09-16 UAT — PNG icons are canon; the glyph experiment is retired".
    1. **draw() marks.** Rewrite the comment above `function draw()` to say: palette (fog / bevelled walls / floor inset / border) from mapMarks.js via `window.__mzMapMarks`; marks and the party from the v1.3 PNG pipeline (icons.js via `window.__mzIconsApi`/`window.__mzIconMap`, awaited by the module before boot); PNG icons are canon per the 2026-09-16 UAT ruling; fail-open on both bridges. Keep the fog pass, the wall/floor loop, and the outer border exactly as they are. Replace the whole `if (M) { ... }` coloured-glyph mark loop with the v1.3 block: `const iconsApi = window.__mzIconsApi, iconMap = window.__mzIconMap;` `const iconReady = img => img && img.complete && img.naturalWidth > 0;` then `if (iconsApi && iconMap) { for (let y = 0; y < GH; y++) for (let x = 0; x < GW; x++) { const c = g[y][x]; if (!c.seen || !c.feat) continue; const key = iconsApi.featureKeyForCell(c); const img = key && iconMap[key]; if (iconReady(img)) iconsApi.drawFeatureIcon(ctx, img, x * CELL, y * CELL, CELL, c.dir, 0.75); } }` — with the v1.3 comments (features are PNG icons; c.dir rotates the one-way door — DR5; scale 0.75 keeps feature tiles smaller than the full-size party marker — DR11; a missing/undecoded icon draws nothing, never blocks the rest of the maze). Write comments in prose; never spell the removed glyph draw call or its constants in a comment.
    2. **draw() party.** Replace the party block with: `const cx = px * CELL + CELL / 2, cy = py * CELL + CELL / 2; const partyImg = iconsApi && iconMap && iconMap[iconsApi.PLAYER_MARKER_ICON];` the UNCHANGED radial glow (`glowR = CELL * .5`, `createRadialGradient(cx, cy, glowR * .15, cx, cy, glowR)`, the two `rgba(232,201,122,...)` stops, the arc fill), then `if (iconReady(partyImg)) { iconsApi.drawFeatureIcon(ctx, partyImg, px * CELL, py * CELL, CELL); } else { ctx.fillStyle = P.party; ctx.beginPath(); ctx.arc(cx, cy, CELL * .36, 0, 7); ctx.fill(); ctx.fillStyle = "#F6F1E4"; ctx.font = \`${Math.round(CELL * .5)}px "Courier Prime", monospace\`; ctx.textAlign = "center"; ctx.textBaseline = "middle"; ctx.fillText("P", cx, cy + CELL * .03); }` (comment: party.png, v1.3 canon; the pulsing ring is still the `#mw-party-pulse` DOM element positioned by positionPartyPulse() and now pulses around party.png; the dot+P fallback keeps the player visible if party.png never loaded). `positionCanvas();` stays the last statement. `positionPartyPulse()`, `.mw-vignette`, `#mw-party-pulse` and the `MAP_PALETTE` read are untouched.
    3. **Legend CSS.** Replace the two-line "coloured text glyphs replace the Phase-4 PNG icon rows" comment and the `.mw-legend-glyph{...}` rule with the v1.3 rule verbatim on its own line: `.mw-legend-row img{width:34px;height:34px;object-fit:contain;flex:none}` (comment: PNG legend rows restored 2026-09-16 UAT).
    4. **renderMarksLegend().** Reword the comment above it (rows come from mapMarks.js's `MARKS_LEGEND` data; each row's icon is the same `icons/optimized/<key>.png` the map draws — PNG canon). In the loop, replace the glyph `<span>` (its createElement, className, aria-hidden, textContent, style.color and appendChild lines) with: `const img = document.createElement("img"); img.src = \`./icons/optimized/${row.key}.png\`; img.alt = ""; img.setAttribute("aria-hidden", "true"); div.appendChild(img);` — the name/desc spans and `rows.appendChild(div)` are unchanged. `M.MARK_GLYPHS` is no longer read here (it stays exported and bridged; renderer-unused).
    5. **inspectAt() (discretion: keep).** The hold-inspect rail card's tiny icon slot is text — keep `mark ? mark.glyph : "·"` exactly (the viewport suite pins that literal). No edit.
    6. **Module comments only** (~L6573-6576 above the mapMarks import and ~L6840-6842 above `window.__mzMapMarks = {`): reword to "draw() reads MAP_PALETTE from here; inspectAt() reads markForCell/legendFor; the PNG pipeline above is what draws the marks and the party (canon since the 2026-09-16 UAT)". Code lines unchanged.
    7. **Re-pin `test/unit/shell-map-hud.test.js`:** (h) — drop `"iconReady"` and `"mw-legend-row img"` from the zero-grep literal list (keep `mw-viewport-chips`, `mw-flash`, `flashMessage`, `MAZE_CANVAS_COLORS`). (i) — retitle "draw(): reads the palette from window.__mzMapMarks and the marks/party from the PNG pipeline (v1.3 canon restored 2026-09-16), positionCanvas() last"; needles: keep `window.__mzMapMarks`, `P.fog`, `P.wall`, `P.wallLight`, `P.wallDark`, `P.floor`, `P.floorDark`, `P.floorInset`, `P.border`, `P.party`, `strokeRect(3, 3, size - 6, size - 6)`; remove the five glyph-loop needles (the markForCell call, the scale constant, the rotation-table read, `ctx.rotate(`, the glyph fillText); add `window.__mzIconsApi`, `window.__mzIconMap`, `img.complete && img.naturalWidth > 0`, `iconsApi.featureKeyForCell(c)`, `iconsApi.drawFeatureIcon(ctx, img, x * CELL, y * CELL, CELL, c.dir, 0.75)`, `iconMap[iconsApi.PLAYER_MARKER_ICON]`, `iconsApi.drawFeatureIcon(ctx, partyImg, px * CELL, py * CELL, CELL)`, `createRadialGradient(`; gone list becomes `getComputedStyle`, `globalAlpha`, the glyph fillText call, the scale constant name, `markForCell(`; keep the `window.__mzIconMap = await preloadIcons` count-1 pin. (j) — retitle "renderMarksLegend: reads MARKS_LEGEND from the bridge, builds PNG <img> rows (v1.3 canon), zero glyph span"; assert match `window.__mzMapMarks`, `MARKS_LEGEND`, `createElement("img")`, `icons/optimized/${row.key}.png` (escape `$`/`{`/`}`/`.`), `setAttribute("aria-hidden", "true")`; doesNotMatch the glyph span class, `MARK_GLYPHS`, `style.color`; keep the `const MARKS_LEGEND = [` count-0 pin (the classic table stays retired — data still lives in mapMarks.js); add `assert.match(HTML, /^\.mw-legend-row img\{width:34px;height:34px;object-fit:contain;flex:none\}$/m)` and `assert.equal((HTML.match(/mw-legend-glyph/g) || []).length, 0)`. (m) first BEHAVIOUR test — retitle to "markForCell/ONEWAY_ROTATION_DEG stay intact in mapMarks.js for inspectAt's legend lookups (the canvas rotation is drawFeatureIcon's again)"; body unchanged. Update the header comment lines describing (i)/(j).
    8. **Re-pin `test/unit/shell-map-invariants.test.js`:** delete `iconReadyFn` from `RETIRED` and from the key list in "retirements: RAW carries zero of the flash/character-line/chip-track/palette-table/icon-ready literals" (retitle without "icon-ready"). Flip the two tests at ~L144-153 to PNG-canon pins: "PNG canon (user reversed Phase 35 decision 3, 2026-09-16 UAT): the draw() region reads __mzIconMap and calls drawFeatureIcon, and no glyph fillText survives" — `assert.match(region, /__mzIconMap/)`, `assert.match(region, /drawFeatureIcon/)`, `assert.doesNotMatch(region, /fillText\(mark\.glyph/)`; and "PNG canon: the legend region builds <img> rows and no glyph span survives" — `assert.match(region, /createElement\("img"\)/)`, `assert.match(region, /icons\/optimized\//)`, `assert.doesNotMatch(region, /mw-legend-glyph/)`.
    9. `test/unit/mapMarks.test.js` and `test/unit/tutorial.test.js` test the modules only — untouched, must still pass.
  </action>
  <verify>
    <automated>test "$(grep -c '^\.mw-legend-row img{width:34px;height:34px;object-fit:contain;flex:none}$' mazeworld.html)" = "1" && test "$(grep -c 'mw-legend-glyph' mazeworld.html)" = "0" && test "$(grep -c 'iconsApi.drawFeatureIcon(ctx, partyImg, px \* CELL, py \* CELL, CELL)' mazeworld.html)" = "1" && test "$(grep -c 'window.__mzIconMap = await preloadIcons("./icons/optimized");' mazeworld.html)" = "1" && node --test test/unit/shell-map-hud.test.js test/unit/shell-map-invariants.test.js test/unit/mapMarks.test.js test/unit/tutorial.test.js test/unit/shell-map-viewport.test.js</automated>
  </verify>
  <acceptance_criteria>
    - draw() region (`node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function draw()');const b=s.indexOf('/* ---------------- log',a);const r=s.slice(a,b).replace(/\/\/[^\n]*/g,'');const need=['window.__mzMapMarks','P.fog','P.wallLight','P.wallDark','P.floorInset','P.border','strokeRect(3, 3, size - 6, size - 6)','window.__mzIconsApi','window.__mzIconMap','img.complete && img.naturalWidth > 0','iconsApi.featureKeyForCell(c)','iconsApi.drawFeatureIcon(ctx, img, x * CELL, y * CELL, CELL, c.dir, 0.75)','iconMap[iconsApi.PLAYER_MARKER_ICON]','iconsApi.drawFeatureIcon(ctx, partyImg, px * CELL, py * CELL, CELL)','createRadialGradient(','ctx.fillText(\"P\"'];const gone=['fillText(mark.glyph','MARK_SCALE','markForCell(','ONEWAY_ROTATION_DEG','getComputedStyle','globalAlpha'];process.exit(need.every(n=>r.includes(n))&&gone.every(g=>!r.includes(g))&&/positionCanvas\(\);\s*\}\s*$/.test(r.trimEnd())?0:1)"` exits 0.
    - `grep -c '^\.mw-legend-row img{width:34px;height:34px;object-fit:contain;flex:none}$' mazeworld.html` = 1; `grep -c 'mw-legend-glyph' mazeworld.html` = 0 (raw file, CSS + JS).
    - renderMarksLegend region (`node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function renderMarksLegend()');const b=s.indexOf('function openMarksLegend()',a);const r=s.slice(a,b);process.exit(r.includes('createElement(\"img\")')&&r.includes('./icons/optimized/\${row.key}.png')&&r.includes('setAttribute(\"aria-hidden\", \"true\")')&&r.includes('M.MARKS_LEGEND')&&!r.includes('MARK_GLYPHS')&&!r.includes('style.color')?0:1)"` exits 0.
    - `grep -c 'window.__mzIconsApi = { featureKeyForCell, drawFeatureIcon, PLAYER_MARKER_ICON };' mazeworld.html` = 1; `grep -c 'window.__mzIconMap = await preloadIcons("./icons/optimized");' mazeworld.html` = 1; `grep -c 'window.__mzMapMarks = {' mazeworld.html` = 1; `grep -c 'mark ? mark.glyph : "·"' mazeworld.html` = 1 (inspectAt unchanged).
    - `grep -c 'id="mw-party-pulse" aria-hidden="true"' mazeworld.html` = 1; `grep -c 'class="mw-vignette"' mazeworld.html` = 1; `grep -c '^function positionPartyPulse(rect)' mazeworld.html` = 1 (ring, vignette, palette kept).
    - `ls icons/optimized/` lists exactly chest.png crevice.png descent.png encounter.png onewaydoor.png party.png teleport.png trap.png wall.png (all nine legend keys resolve to a file).
    - `git diff --stat -- engine content test/parity src/browser/icons.js src/browser/mapMarks.js icons test/unit/mapMarks.test.js test/unit/tutorial.test.js` prints nothing.
    - `node --test test/unit/shell-map-hud.test.js test/unit/shell-map-invariants.test.js test/unit/mapMarks.test.js test/unit/tutorial.test.js test/unit/shell-map-viewport.test.js` exits 0.
  </acceptance_criteria>
  <done>Every seen feature draws its v1.3 PNG icon and the party draws party.png (glow beneath, DOM ring around); the MARKS sheet rows carry the same PNGs; the palette/vignette/ring are intact; the HUD and invariants suites pin PNG canon instead of the retired glyph experiment.</done>
</task>

<task type="auto">
  <name>Task 3: Move the settings gear out of the HUD strip into the viewport chip row, right of MAKE CAMP; re-pin shell-map-hud (a)/(h), shell-gear-toolbar chip order, shell-map-invariants chip listeners</name>
  <files>mazeworld.html, test/unit/shell-map-hud.test.js, test/unit/shell-gear-toolbar.test.js, test/unit/shell-map-invariants.test.js</files>
  <read_first>
    - mazeworld.html lines 1338-1360 (the `<header class="mw-hud" id="mw-hud">` markup: `.mw-hud-top` holds the stat row, `.mw-hud-wp` (which carries `margin-left:auto`, so nothing else needs re-aligning), the DEV chip `#mw-dev-chip`, then the gear `<button ... class="mw-gear-btn" id="mw-gear-btn" aria-label="Settings">&#9881;</button>` with its 04-DR9 comment — remove the gear + its comment; the DEV chip stays)
    - mazeworld.html lines 1396-1401 (the `.mw-map-chips` row: MARKS, CENTRE, the gap span, `#btn-camp` — the gear goes after `#btn-camp`) and lines 323-327 (`.mw-map-chips`, `.mw-map-chips-gap`, `.mw-map-chip`, `.mw-map-chip.camp`, `.mw-map-chip:active` CSS — `.mw-map-chip` sets `font-family:var(--disp);font-size:7px;padding:9px 10px;min-height:48px;min-width:48px` and flex-centres its content)
    - mazeworld.html lines 898-914 (the `.mw-gear-btn` rule + its two comment blocks + hover/active rules — delete, the element no longer exists) and 835-837 (the .mw-hud comment mentioning "+ DEV chip + gear on the right" — reword) and 1251-1254 (the Settings sheet comment "opened by the top-HUD gear button (#mw-gear-btn)" — reword to "the gear chip in the viewport chip row (#mw-gear-btn)")
    - mazeworld.html lines 6745-6760 (module: `openSettingsSheet()`/`closeSettingsSheet()` and `document.getElementById("mw-gear-btn")?.addEventListener("click", openSettingsSheet);` — UNCHANGED; the id is kept so this line and the store-polish region slicer keep working) and line 5030 (`function guardTap(btn, fn)` — sets aria-disabled and gates on encArmed(); NOT for static chips — see prohibitions)
    - test/unit/shell-map-hud.test.js lines 90-118 (`(a) HUD markup ...` — the `gearIdx` ordering pin to flip) and 269-294 (`(h) chrome` — add the gear after the camp chip); test/unit/shell-gear-toolbar.test.js lines 69-75 (`chipsMarkup()`) and 157-169 (the chip-order test); test/unit/shell-map-invariants.test.js lines 199-204 ("the three map chips' listeners exist exactly once each"); test/unit/shell-map-store-polish.test.js line 76 (slices on `document.getElementById("mw-gear-btn")` — must still resolve)
  </read_first>
  <action>
    <!-- planner-discipline-allow: .mw-gear-btn -->
    <!-- planner-discipline-allow: mw-gear-btn -->
    <!-- planner-discipline-allow: id="mw-gear-btn" -->
    Record in the SUMMARY: user ruling 2026-09-16 UAT — "Move the settings gear into a button and put it on the right of the make camp button."
    1. **Markup.** Delete the gear `<button>` and its 04-DR9 comment from `.mw-hud-top` (the DEV chip `#mw-dev-chip` stays exactly where it is). In `.mw-map-chips`, directly after the `#btn-camp` line, add `<button type="button" class="mw-map-chip gear" id="mw-gear-btn" aria-label="Settings">&#9881;</button>` with a comment: "2026-09-16 UAT (user ruling): the settings gear is a chip right of MAKE CAMP; same id, same module listener (openSettingsSheet) as before — the Settings sheet itself is unchanged." Update the `.mw-map-chips` markup comment above the row to list MARKS/CENTRE top-left, MAKE CAMP + the settings gear top-right.
    2. **CSS.** Delete the `.mw-gear-btn` rule, its two comment blocks (04-DR9 / DR13) and the `:hover`/`:active` rules (no element carries the class any more). Add, directly after the `.mw-map-chip.camp{...}` line, one rule on its own line: `.mw-map-chip.gear{font-family:var(--mono);font-size:22px;line-height:1;padding:0 12px}` (comment: the gear glyph needs a readable size and a font that has it — the chip's 48dp box, border, shadow and press state come from `.mw-map-chip`). Reword the `.mw-hud` comment ("+ DEV chip + gear on the right" -> "+ DEV chip on the right; the settings gear is a viewport chip") and the Settings-sheet comment as noted in read_first.
    3. **Wiring.** No JS change: `document.getElementById("mw-gear-btn")?.addEventListener("click", openSettingsSheet);` in the module keeps working because the id is unchanged. Do NOT wrap the gear in guardTap (it would set aria-disabled and the arm sweep only covers the overlay/rail — the three existing map chips are plain-listener wired for the same reason and the invariants suite pins that shape).
    4. **Re-pin `test/unit/shell-map-hud.test.js`:** (a) — retitle "... DEV chip still inside .mw-hud-top, the gear gone from the HUD, zero character-line ids"; replace the gear ordering pin with `assert.ok(topStart !== -1 && topStart < devIdx, "the DEV chip after .mw-hud-top opens")` and `assert.equal((region.match(/id="mw-gear-btn"/g) || []).length, 0, "the settings gear left the HUD (2026-09-16 UAT)")`. (h) — add `const gearIdx = region.indexOf('id="mw-gear-btn"');` and extend the order assertion to `campIdx < gearIdx` ("MARKS, CENTRE, the gap, MAKE CAMP, then the settings gear"), plus `assert.match(region, /class="mw-map-chip gear" id="mw-gear-btn" aria-label="Settings"/)` and `assert.match(HTML, /^\.mw-map-chip\.gear\{[^}]*font-size:22px[^}]*\}$/m)`; retitle accordingly.
    5. **Re-pin `test/unit/shell-gear-toolbar.test.js`** chip-order test: add `const gearIdx = markup.indexOf('id="mw-gear-btn"');`, require all four found, order `marksIdx < centreIdx && centreIdx < campIdx && campIdx < gearIdx`; retitle "the chip row holds Marks, Centre, the camp chip, then the settings gear, in that order (2026-09-16 UAT)".
    6. **Re-pin `test/unit/shell-map-invariants.test.js`** "MAP-08: the three map chips' listeners exist exactly once each" -> "the four map chips' listeners ..." adding `assert.equal(countOf(CODE, 'document.getElementById("mw-gear-btn")?.addEventListener("click", openSettingsSheet)'), 1);`.
  </action>
  <verify>
    <automated>test "$(grep -c 'id="mw-gear-btn"' mazeworld.html)" = "1" && test "$(grep -c '\.mw-gear-btn' mazeworld.html)" = "0" && test "$(grep -c 'class="mw-map-chip gear" id="mw-gear-btn" aria-label="Settings"' mazeworld.html)" = "1" && node --test test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-input-guards.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-gear-btn"' mazeworld.html` = 1 (the element exists exactly once in the file).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const h=s.slice(s.indexOf('<header class=\"mw-hud\" id=\"mw-hud\">'),s.indexOf('</header>'));const c=s.slice(s.indexOf('<div class=\"mw-map-chips\" id=\"mw-map-chips\">'),s.indexOf('<section class=\"mw-overlay\"'));const n=t=>(t.match(/id=\"mw-gear-btn\"/g)||[]).length;process.exit(n(h)===0&&n(c)===1&&h.includes('id=\"mw-dev-chip\"')&&c.indexOf('id=\"btn-camp\"')<c.indexOf('id=\"mw-gear-btn\"')?0:1)"` exits 0 (gear inside `.mw-map-chips` = 1, inside `.mw-hud` = 0, DEV chip still in the HUD, gear after MAKE CAMP).
    - `grep -c '\.mw-gear-btn' mazeworld.html` = 0 (rule, hover/active and class attribute all gone); `grep -c '^\.mw-map-chip\.gear{' mazeworld.html` = 1; `grep -c '^\.mw-map-chip\.camp{background:#241d12;border-color:#6b5c3c}$' mazeworld.html` = 1 (camp rule untouched).
    - `grep -c 'document.getElementById("mw-gear-btn")?.addEventListener("click", openSettingsSheet);' mazeworld.html` = 1; the three existing chip listeners still occur once each (`grep -c 'document.getElementById("mw-chip-marks").addEventListener("click", openMarksLegend)'` = 1, `grep -c 'document.getElementById("mw-chip-centre").addEventListener("click", centerMap)'` = 1, `grep -c 'document.getElementById("btn-camp").onclick = openCampSheet'` = 1).
    - The gear is never guardTap-wired: `grep -c 'guardTap(document.getElementById("mw-gear-btn")' mazeworld.html` = 0.
    - `node --test test/unit/shell-map-hud.test.js test/unit/shell-gear-toolbar.test.js test/unit/shell-map-invariants.test.js test/unit/shell-map-store-polish.test.js test/unit/shell-input-guards.test.js` exits 0.
    - `git diff --stat -- engine content test/parity src/browser/settings.js` prints nothing.
  </acceptance_criteria>
  <done>The gear chip sits right of MAKE CAMP in the viewport chip row, opens the Settings sheet through its unchanged listener, and the HUD strip no longer carries a gear; the HUD, toolbar and invariants suites pin the new placement.</done>
</task>

<task type="auto">
  <name>Task 4: The RAIL is a map-tab element (mwActiveTab in showTab + renderRail); re-pin shell-map-rail; run the full gate and build the debug APK</name>
  <files>mazeworld.html, test/unit/shell-map-rail.test.js</files>
  <read_first>
    - mazeworld.html lines 1660-1711 (`(function initTabs() {` opens at ~L1664 — `showTab(name)` at ~L1674-1707 toggles `screens[]`/tabs, calls `window.__mzOracleToNewest?.()` for oracle, `window.mzCenterMap?.()` for maze, the Dead-tab graveyard re-read; the IIFE ends with the click wiring, `showTab("maze")` and `window.__mzShowTab = showTab;`) — NOTE `let S = null;` is declared at ~L2265, AFTER this IIFE runs, so showTab must never read `S` directly (TDZ at the initial `showTab("maze")` call)
    - mazeworld.html lines 5915-5923 (`function renderRail()`: `const railEl = document.getElementById("mw-rail"); if (!S || !railEl) return;` ... `railEl.hidden = !!(S.combat || S.dead || S.won);` at ~L5922 — set on every call, before the key gate) and line 6069 (`window.renderRail = renderRail;` — the classic-script bridge showTab re-runs the visibility through; undefined at tab-init, so `?.()` is a harmless no-op then)
    - mazeworld.html line 3234 (`paint()` -> `renderEncounter(); renderRail(); draw();` — the rail repaints after every action on any tab; with the tab condition it simply stays hidden off-Map and shows on return if the card is still within its hold)
    - test/unit/shell-map-rail.test.js lines 56-110 (helpers: `fnRegion`, `railRegion()` = renderRail's body, `sliceBetween`) and 205-229 (`(f) renderRail ...` — pins exactly one `setTimeout(` in the region, `lastRailKeyShown`, `armEncounterButtons();` — the new hidden expression must not add any of those), 322-330 (`(l) layout`), 358 (end of file — append the new test)
    - test/unit/shell-map-store-polish.test.js lines 71-72 and 134-138 (`showTabRegion()` slices `function showTab(name) {` -> `for (const btn of tabs) btn.addEventListener("click"` and pins exactly one `if (name === "maze") window.mzCenterMap?.();` — the new lines must not duplicate that literal)
    - .planning/milestones/v1.4-phases/35-map-screen-rebuild/35-02-SUMMARY.md (the "rail is a direct child of #app ... visible on every tab" placement this ruling supersedes — record the supersession in the SUMMARY)
  </read_first>
  <action>
    Record in the SUMMARY (verbatim): "user ruling 2026-09-16 UAT: rail is a map-tab element" — supersedes 35-02's planner-discretion "rail visible below the Gear list on every tab".
    1. **Tab tracking.** Immediately above `(function initTabs() {` add, on its own line, `let mwActiveTab = "maze";` with a comment: "2026-09-16 UAT (user ruling): the rail is a map-tab element. Declared BEFORE initTabs runs so its first showTab("maze") assignment is not a TDZ error; renderRail() (further down) reads it. showTab never reads S directly (S is declared later) — it re-runs the rail's own visibility check through window.renderRail?.(), which is undefined until the classic bridge line executes and is a harmless no-op at tab-init."
    2. **showTab(name).** Directly after the `for (const btn of tabs) { ... }` aria-current loop (before the DFB-03 oracle line) insert: `mwActiveTab = name;` `const railEl = document.getElementById("mw-rail");` `if (railEl) railEl.hidden = name !== "maze";` `if (name === "maze") window.renderRail?.();` — comment: off-Map the rail is hidden outright; on return to Map renderRail re-derives hidden from BOTH conditions (combat/dead/won and the active tab) and shows a card that arrived meanwhile if it is still within its hold. Do not touch the existing `if (name === "maze") window.mzCenterMap?.();` line (store-polish pins it once).
    3. **renderRail().** Change the visibility line to `railEl.hidden = !!(S.combat || S.dead || S.won) || mwActiveTab !== "maze";` (comment: both conditions hide — the column owners AND any non-Map tab). No other change; no new setTimeout/armEncounterButtons/lastRailKeyShown reference.
    4. **Re-pin `test/unit/shell-map-rail.test.js`:** append test `(o) 2026-09-16 UAT ruling: the rail is a map-tab element — mwActiveTab is declared once before initTabs, showTab sets it and hides the rail off-Map without reading S, renderRail combines both hide conditions`: `assert.equal((CODE.match(/^let mwActiveTab = "maze";/gm) || []).length, 1)`; `assert.ok(CODE.indexOf('let mwActiveTab = "maze";') < CODE.indexOf("(function initTabs() {"))`; showTab region (`sliceBetween(CODE, "function showTab(name) {", 'for (const btn of tabs) btn.addEventListener("click"')`) matches `mwActiveTab = name;`, `railEl.hidden = name !== "maze";`, `if (name === "maze") window.renderRail?.();` and does NOT match `/\bS\./` (no direct S read — TDZ); `railRegion()` matches `railEl.hidden = !!(S.combat || S.dead || S.won) || mwActiveTab !== "maze";`. Update the file's header comment to mention (o).
    5. **Gate (all of it, in this order; record every result in the SUMMARY):** `npm test` -> `# fail 0`, `# pass` >= 2170 (2170 baseline + the Task 1 and Task 4 tests); `npm run build:www` -> exit 0; `git diff --stat -- engine content test/parity` -> empty; `git hash-object test/parity/prototype-master.js.txt` -> `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run android:debug` -> exit 0, then record the size and mtime of `android/app/build/outputs/apk/debug/app-debug.apk` (e.g. `ls -l --time-style=full-iso android/app/build/outputs/apk/debug/app-debug.apk`). Run NO `adb` command — the orchestrator installs.
    6. **SUMMARY contract.** Write `.planning/quick/260916-w0c-v1-4-uat-fixes-tap-to-move-bridge-timing/260916-w0c-SUMMARY.md` with `status: complete` in its frontmatter, the two verbatim rulings from Tasks 2 and 4 plus the Task 3 ruling, the gate results, and END the file with a `## Human verification` section listing exactly these four Pixel 7 re-checks: (1) a tap two squares from the party steps the party one square toward it (tap-to-move works; hold still inspects, drag pans, pinch zooms); (2) the map shows the v1.3 PNG icons on every seen feature square and party.png (with the pulsing ring) on the party, and the MARKS sheet rows show the same PNG icons; (3) the gear chip sits right of MAKE CAMP and opens Settings; the HUD strip no longer shows a gear; (4) the rail is absent on the Hero/Gear/Oracle/Dead tabs and present on the Map tab, and a card that arrives while on Gear is visible on returning to Map within its hold.
  </action>
  <verify>
    <automated>test "$(grep -c '^let mwActiveTab = "maze";' mazeworld.html)" = "1" && test "$(grep -c 'railEl.hidden = !!(S.combat || S.dead || S.won) || mwActiveTab !== "maze";' mazeworld.html)" = "1" && test "$(grep -c 'if (name === "maze") window.renderRail?.();' mazeworld.html)" = "1" && npm test 2>&1 | tail -12 | grep -q '^# fail 0' && npm run build:www && test -z "$(git diff --stat -- engine content test/parity)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && npm run android:debug && ls -l android/app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^let mwActiveTab = "maze";' mazeworld.html` = 1 and `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');process.exit(s.indexOf('let mwActiveTab = \"maze\";')<s.indexOf('(function initTabs() {')?0:1)"` exits 0 (declared before the IIFE that first assigns it).
    - showTab region: `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function showTab(name) {');const b=s.indexOf('for (const btn of tabs) btn.addEventListener(\"click\"',a);const r=s.slice(a,b).replace(/\/\/[^\n]*/g,'');process.exit(r.includes('mwActiveTab = name;')&&r.includes('railEl.hidden = name !== \"maze\";')&&r.includes('if (name === \"maze\") window.renderRail?.();')&&(r.match(/if \(name === \"maze\"\) window\.mzCenterMap\?\.\(\);/g)||[]).length===1&&!/\bS\./.test(r)?0:1)"` exits 0.
    - `grep -c 'railEl.hidden = !!(S.combat || S.dead || S.won) || mwActiveTab !== "maze";' mazeworld.html` = 1; `grep -c 'window.renderRail = renderRail;' mazeworld.html` = 1 (bridge unchanged).
    - `node --test test/unit/shell-map-rail.test.js` exits 0 with 17 tests (16 + the new (o)); `node --test test/unit/shell-map-store-polish.test.js test/unit/shell-map-invariants.test.js test/unit/shell-input-guards.test.js` exits 0.
    - `npm test` output ends with `# fail 0` and `# pass` >= 2170.
    - `npm run build:www` exits 0.
    - `git diff --stat -- engine content test/parity` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `npm run android:debug` exits 0; `android/app/build/outputs/apk/debug/app-debug.apk` exists with an mtime after the Task 4 commit — its size and mtime are recorded in the SUMMARY; no `adb` command was run.
    - The SUMMARY exists at `.planning/quick/260916-w0c-v1-4-uat-fixes-tap-to-move-bridge-timing/260916-w0c-SUMMARY.md`, has `status: complete` in its frontmatter, the three verbatim rulings, and ends with `## Human verification` listing the four Pixel 7 re-checks above.
  </acceptance_criteria>
  <done>The rail hides on every non-Map tab and re-derives its visibility on return to Map; the rail suite pins it; the full gate is green with the parity hash unchanged and a fresh debug APK on disk for the orchestrator to install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| touch input -> classic pointer pipeline | untrusted pointer events; the only consumer is the shell (pan/zoom/step through window.move's existing guards) |
| local PNG assets -> canvas / legend `<img>` | bundled `icons/optimized/*.png` shipped inside the APK; no network, no user-supplied URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w0c-01 | Denial of Service | initMazeViewportControls pointerdown | medium | mitigate | the lazy `T()` accessor with tapStep.js-matching fallbacks means a missing bridge degrades to defaults instead of throwing (Task 1); the viewport suite forbids any parse-time bridge capture |
| T-w0c-02 | Tampering | draw()/renderMarksLegend PNG path | low | accept | image sources are the fixed `./icons/optimized/<MARKS_LEGEND key>.png` set from a frozen module constant, never user data; `iconReady` fails open per icons.js's contract |
| T-w0c-03 | Information Disclosure | rail visibility off-Map | low | accept | hiding is presentation-only (`hidden` attribute); no state leaves S; the same card re-renders on return |
| T-w0c-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package installs in this plan (package.json unchanged — pinned by shell-map-invariants MAP-09) |
</threat_model>

<verification>
- `npm test` `# fail 0` (pass >= 2170); `npm run build:www` exit 0; `git diff --stat -- engine content test/parity` empty; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run android:debug` exit 0 with the APK size/mtime recorded; no `adb` commands.
- Source invariants after this plan: zero parse-time bridge captures in the viewport IIFE; draw() and renderMarksLegend() are on the PNG path (icons.js bridges intact, mapMarks.js bridge intact and still read for the palette/inspect); `id="mw-gear-btn"` exactly once, inside `.mw-map-chips`, after `#btn-camp`, zero inside `.mw-hud`; `mwActiveTab` declared once before initTabs, read in renderRail; `window.mzCenterMap?.()` call sites still 10.
- SUMMARY contract: `status: complete`; the three rulings recorded verbatim ("user reversed Phase 35 decision 3 on 2026-09-16 UAT — PNG icons are canon; the glyph experiment is retired"; the gear-chip ruling; "user ruling 2026-09-16 UAT: rail is a map-tab element"); the file ends with `## Human verification` listing the four Pixel 7 re-checks (tap steps the party; PNG icons + party.png on the map and in the MARKS sheet; gear chip right of MAKE CAMP opens Settings and the HUD has no gear; rail absent on Hero/Gear/Oracle/Dead, present on Map).
</verification>

<success_criteria>
- All four UAT items are source-provable and pinned by tests; the executor never touches `engine/`, `content/`, `test/parity/`, `src/browser/icons.js`, `src/browser/mapMarks.js`, `src/browser/tapStep.js` or `icons/`.
- The debug APK is rebuilt for the orchestrator's install and the SUMMARY hands the user the four-item Pixel 7 re-check list.
</success_criteria>

<output>
Create `.planning/quick/260916-w0c-v1-4-uat-fixes-tap-to-move-bridge-timing/260916-w0c-SUMMARY.md` when done (frontmatter `status: complete`; ends with `## Human verification` per the Task 4 contract).
</output>
