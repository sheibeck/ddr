---
quick_id: 260918-w4n
slug: magic-items-are-use-activated-only-no-passives
date: 2026-09-18
status: planned
phase: quick-260918-w4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/treasure-tables.js
  - engine/derived.js
  - engine/items.js
  - engine/movement.js
  - engine/actions.js
  - engine/saveState.js
  - engine/character.js
  - engine/encounters.js
  - engine/economy.js
  - src/browser/viewModels.js
  - src/browser/combatMenu.js
  - src/browser/eventNarration.js
  - src/browser/toasts.js
  - mazeworld.html
  - tools/lib/tuning-bot.mjs
  - test/parity/harness/comparables.js
  - test/parity/FIXTURE-INVENTORY.md
  - test/parity/fixtures/action-script.chargen.json
  - test/parity/fixtures/action-script.movement.json
  - test/parity/fixtures/action-script.combat.json
  - test/parity/fixtures/action-script.economy.json
  - test/parity/fixtures/action-script.encounters.json
  - test/parity/fixtures/action-script.magic.json
  - test/unit/content-tables.test.js
  - test/unit/economy.test.js
  - test/unit/loot-pile.test.js
  - test/unit/item-activation.test.js
  - test/unit/item-wiring.test.js
  - test/unit/conditions.test.js
  - test/unit/movement.test.js
  - test/unit/water-cost.test.js
  - test/unit/tools.test.js
  - test/unit/worn-model.test.js
  - test/unit/worn-slots.test.js
  - test/unit/fluency.test.js
  - test/unit/darkness-filter.test.js
  - test/unit/armor-durability.test.js
  - test/unit/effect-expiry.test.js
  - test/unit/phobia-triggers.test.js
  - test/unit/spell-utility.test.js
  - test/unit/usable-features-audit.test.js
  - test/unit/worn-migration.test.js
  - test/unit/save-validation.test.js
  - test/unit/actions.test.js
  - test/unit/itemRowState.test.js
  - test/unit/combatMenu.test.js
  - test/unit/gear-panels.test.js
  - test/unit/shell-worn-slots.test.js
  - test/unit/shell-map-hud.test.js
  - test/unit/tuning-bot.test.js
  - test/unit/bot-tactics.test.js
  - docs/GEAR-BALANCE.md
  - docs/GEAR-SLOTS.md
  - docs/TERRAIN.md
  - docs/USABLE-FEATURES-AUDIT.md
autonomous: true
requirements: [QUICK-260918-w4n]
tags: [vanilla-js, rules-engine, items, activation-model, timers, worn-slots, staves, tuning-bot, parity-harness]

must_haves:
  truths:
    - "No JEWELRY/CLOAKS/STAVES item grants any effect from the bag or from merely being worn: engine/derived.js#eff(c, key) is a pure read of LIVE `item:<name>` records on c.timers (each summed from its activation's own `eff` payload) and never reads an item object's `eff` map; isFlying(state) is exactly `itemEffectActive(c, \"fly\")`; moveCost, conditionsOf, armorSoak, fluency, revealRadius, mapViewRadius, the combat noCrit read and the movement darkFor dispel all follow from that one read with no per-item name check left"
    - "The Cloak of Healing no longer exists: CLOAKS has 7 rows, the three cloak rolls (Thief starting cloak, the Cloak find, rollCloak) draw rng.d(CLOAKS.length) — one draw as before, so no rng cursor shifts; every parity fixture whose cloak roll lands on a different row is DECLARED (a chargenDivergence record with fields items, before/after measured live, rationale citing 260918-w4n) — never silently regenerated, and the frozen prototype master is untouched (hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0 unchanged); an old save carrying a Cloak of Healing loads as an inert cloak (activationFor null, no Use button, eff 0, no throw)"
    - "Every one of the 10 remaining formerly-passive / auto-activating rows (Ring of Power, Gauntlet of the Giant, Amulet of Light, Anklet of Invisibility, Helm of Knowledge, Bracelet of Flight, Cloak of Strength, Cloak of Regeneration, Cloak of Armor, Cloak of Flying) has a TREASURE_ACTIVATION_OF record, is usable ONLY via useItem on a worn slot (the existing notWorn refusal), starts its effect-or-cooldown record through the existing applyActivation path, and obeys the once-a-day rule (effect + cd <= 100); a ready-but-unstarted Cloak of Flying / Bracelet of Flight is NOT flying and no climb/gorge step ever starts a flight record by itself"
    - "A staff is NOT equipable: WORN_SLOTS is the five keys ring/bracelet/amulet/helm/cloak, SLOT_OF has 15 entries (8 JEWELRY + 7 CLOAKS), slotFor returns null for a staff, equipItem/takeItem/reconcileWorn never wear one, useItem.slot/unequipSlot.slot reject \"staff\"; a staff sits in c.items (one bag slot), a Magic User uses it by BAG INDEX with no notWorn refusal (wrongClass for anyone else stays), its charges+recharge model is unchanged; a legacy save's c.worn.staff is folded into c.items on load (appended last, so the existing clampCarry overflow drop is the only thing that can discard it)"
    - "The Gear tab's worn-slot rows (five slots, no staff row), the bag rows, and the combat ITEMS submenu show a Use button for every activatable item (the row-state rule itemRowState reads kind !== \"none\", derived from activationFor, never from a `use` key on the item), so a worn Cloak of Flying / Cloak of Armor / Amulet of Light and a bagged staff have a Use button; a bagged cloak/jewel shows Use and the tap yields the existing notWorn toast; no Equip affordance exists for a staff"
    - "The tuning bot plays the new rules: it uses a ready worn Cloak of Regeneration when hurt, a ready worn Amulet of Light in the dark (torch first), a ready worn Cloak of Flying / Bracelet of Flight before stepping onto a climb/gorge cell, a ready worn combat buff (Cloak of Speed/Strength/Armor, Anklet, Ring, Gauntlet) on round 1 of a hard fight, a ready worn Helm of Knowledge when it wants to parley but cannot, and a Magic User's ready BAGGED staff by index; it reads act.kind via activationFor, never it.use"
    - "Exported JEWELRY/CLOAKS/STAVES row objects stay byte-identical (no `use` key added — act is authored and stripped exactly like slot), so no rolled-item shape moves; npm test is # fail 0; the only fixture edits are the declared cloak-roll chargenDivergence records; the master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0; the only parity-harness change is REWORDED_TXT_ITEMS re-shaped to cover every row whose txt was reworded (and dropping the deleted Cloak of Healing); an old save's bagged cloak still loads and simply grants nothing until worn and used"
  artifacts:
    - path: "content/treasure-tables.js"
      provides: "Cloak of Healing row deleted (CLOAKS = 7); act blocks on all 9 converted rows (kinds power/giant/glow/unseen/tongue/fly/knit/brace/plate) plus the existing Cloak of Flying; buildActivation copies a non-empty row eff map onto the record as `eff`; reworded txt for every converted row"
    - path: "engine/derived.js"
      provides: "timer-only eff(); isFlying = live fly; moveCost/conditionsOf without name checks; flight chip emitted from the generic live-effect loop; itemCooldown loop without the Cloak of Flying exclusion; WORN_SLOTS = five keys; slotFor null for a staff; reconcileWorn without the staff clause"
    - path: "engine/items.js"
      provides: "itemReady/useItem keyed on activationFor; switch cases knit (d6 hp, cloakRegenerated), glow (clears darkFor -> darknessDispelled); rollCloak draws rng.d(CLOAKS.length); a staff is never worn (equipItem -> notEquippable, takeItem keeps wrongClass then stows) and is used by bag index"
    - path: "engine/saveState.js"
      provides: "sanitizeWorn folds a legacy c.worn.staff into c.items (appended) before clampCarry"
    - path: "engine/character.js"
      provides: "Thief starting cloak rolls rng.d(CLOAKS.length)"
    - path: "engine/encounters.js"
      provides: "the Cloak find rolls rng.d(CLOAKS.length)"
    - path: "test/parity/fixtures/*.json"
      provides: "declared chargenDivergence records (fields items) for every seed whose cloak roll moved, measured live"
    - path: "engine/actions.js"
      provides: "useItem.slot / unequipSlot.slot reason strings name the five slots"
    - path: "engine/movement.js"
      provides: "flyOver never starts a record; the 20-square cloak tick block and its two constants deleted"
    - path: "src/browser/viewModels.js"
      provides: "itemRowState keyed on activationFor (kind none / consumable / ready / effect / cooldown / charges)"
    - path: "mazeworld.html"
      provides: "Use buttons gated on st.kind !== \"none\" at the worn-slot row and the shared carried-row builder; CONDITION_COPY/TONE/EXPLAIN rows for power/giant/glow/unseen/tongue/brace/plate; flight chip only charged/cooling"
    - path: "tools/lib/tuning-bot.mjs"
      provides: "chooseFieldItem + chooseCombatItem + the pre-hazard flight use, all reading activationFor(it).kind"
    - path: "docs/GEAR-BALANCE.md"
      provides: "the activation ledger's duration+cooldown table extended with the 9 new rows, the Cloak of Healing removal, and the user's 2026-09-18 rulings verbatim"
  key_links:
    - "content/treasure-tables.js#TREASURE_ACTIVATION_OF (eff payload) -> engine/derived.js#liveItemEffects -> eff(c,key) -> every consumer (armorSoak/expectedStrike/weaponDamage/foeToHitVs/fluency/revealRadius/mapViewRadius/combat noCrit/movement darkFor dispel)"
    - "engine/items.js#useItem (slot ref, notWorn refusal) -> applyActivation -> startEffect/startCooldown -> c.timers squares tick -> narrateTimerTransitions (itemEffectFaded/itemCooled)"
    - "engine/derived.js#activationFor -> src/browser/viewModels.js#itemRowState.kind -> mazeworld.html Use button gating (worn row + carried row) and combatMenu.js usable rows"
---

<objective>
## Governing rule (user, 2026-09-18 — verbatim)

> "Items that are equipable must be equipped to be used. Items that are not equipable can be used from the bag."

So: ring / bracelet / amulet / helm / cloak (the worn slots) — Use only while worn (the existing `notWorn` refusal stays for those), and nothing happens passively even when worn: only a Use starts the effect + cooldown. Staff / potion / torch / scroll (not equipable) — Use from the bag by index, never a worn slot.

Make every worn-slot magic item (ring, bracelet, amulet, helm, cloak — content/treasure-tables.js JEWELRY/CLOAKS) USE-ACTIVATED ONLY. Nothing grants an effect passively — not from the bag, not from merely being worn. An effect exists only while a live `item:<name>` record on `c.timers` is running, started by `useItem` on a WORN item, which then starts the cooldown. Every worn-slot item gets a Use button; a ready-but-unstarted Cloak of Flying is NOT flying.

User's words (2026-09-18, canon override): "Every cloak must have a Use button; it's only active when you use it. Same for every staff, amulet, etc. Nothing works without using it, which triggers its cooldown." Bug report: "The Cloak of Flying I got is in my bag. I never equipped it. But it still shows me as flying. It also has no Use button."

Staff amendment (user, 2026-09-18): "A magic staff is a usable item, but not equipable. Staff should not be an equipment slot. It just takes up a bag slot and is usable from the bag." So `staff` leaves the worn-slot taxonomy entirely: WORN_SLOTS becomes the five keys ring/bracelet/amulet/helm/cloak; a staff lives in `c.items` (one bag slot, `slotItems` already counts it), is used by BAG INDEX, keeps its charges+recharge model and the wrongClass (non-Magic-User) refusal; a legacy save's `c.worn.staff` is folded back into the bag on load. Staff sites today: `engine/derived.js` WORN_SLOTS (152), `slotFor`'s kind fallback (171), `reconcileWorn`'s Magic-User eligibility clause (281); `content/treasure-tables.js` STAVES_ROWS authored staff slot key (115-146) feeding SLOT_OF (165-167, "exactly 24 entries"); `engine/items.js` `autoWearSlot` staff clause (388), `takeItem` wrongClass (472) then auto-wear (475-478), `equipItem`'s cloak/jewel/staff branch (745-747); `engine/actions.js` reason strings (118, 161 — the `WORN_SLOTS.includes` checks self-update); `engine/saveState.js#sanitizeWorn` (251-262) + the `wornSlots` load path (473-494); shell: `viewModels.js#emptySlotRows` staff special case (368-378) + the two staff leaves of `GEAR_COPY.empty` (336-337), the GEAR section comment (mazeworld.html 1560-1563), `combatMenu.js` worn rows (257); bot: `chooseCombatItem` staff tier (742-758).

Cloak of Healing amendment (user, 2026-09-18): "Drop Cloak of Healing" — the item leaves the game. Its row (content/treasure-tables.js 78) is deleted, so CLOAKS has 7 rows, and the three cloak rolls draw `rng.d(CLOAKS.length)`: `engine/character.js` 548 (Thief starting cloak), `engine/encounters.js` 400 (the Cloak find), `engine/items.js` 134 (`rollCloak`). `rng.d(sides)` is ONE `gen.next()` draw whatever `sides` is (engine/rng.js 53-55), so the rng cursor never shifts — only the row a given draw lands on can change (`1 + floor(u*7)` over the 7 remaining rows vs the frozen prototype's `1 + floor(u*8)` over 8). That moves exactly the parity fixtures whose Thief seed or Cloak find lands on a different row; per the greenfield rule each is DECLARED as a `chargenDivergence` record (`fields: ["items"]`, before = the prototype's rolled kit, after = the engine's, measured live, rationale citing this task) in its fixture JSON — the harness already supports this record shape (comparables.js `chargenDivergenceFor`/`stripChargenShift`/`chargenShiftDiffs`, generic over `fields`). Candidates today (FIXTURE-INVENTORY.md 1488-1499): chargen seeds 2/3/4 (Regeneration/Ether/Regeneration), movement seed 256 (Healing — must move), combat `flee` seed 17 (Armor), encounters `affliction` seed 160 (Regeneration), economy seed 3 (Ether; note an Ether draw, u >= 0.875, maps to Ether under d7 too — verify live). The frozen prototype master (`test/parity/prototype-master.js.txt`) is NEVER edited — its hash a1f4d0dc29782218d8e5aab65bc5989c33f917f0 stays the gate; the divergence lives in the fixture JSONs, not the master. Other Cloak of Healing footprints to delete: `engine/economy.js` 86 (TREASURE_BASE_VALUES entry), the `cloakHealed` event + narration/toast/rail lines, the `cloakHeal` eff key wherever a test enumerates eff keys (worn-model.test.js 41), comparables.js `REWORDED_TXT_ITEMS`, the classic in-shell duplicate table in mazeworld.html (1942 row; `D(8)` cloak rolls at 2397/3870/4372 become `D(CLOAKS.length)` so the dead classic path cannot roll a ghost item), docs (GEAR-SLOTS.md 73, TERRAIN.md 167, USABLE-FEATURES-AUDIT.md 137 "Cloaks (8)"). Old saves: a carried Cloak of Healing stays where it is as an inert cloak — `slotFor` still resolves it by kind fallback, `activationFor` is null (no Use button, silent no-op on use, eff 0).

Ground truth from the source read (cite, do not re-derive):
- `engine/derived.js#eff` (lines 97-107) is two-path: sums `it.eff` over `c.worn` when the key exists, else over the whole bag. `carriedItems`/`hasItemNamed` (161-165, 283-285) feed `moveCost` (420-427), `isFlying` (452-457, which treats a READY Cloak of Flying as flying via `isReady`), and `conditionsOf`'s flight block (559-573) plus its "Cloak of Flying" exclusions (584, 598). `eff()` consumers: `armorSoak` 675-677 (cloakArmor), `expectedStrike` 937/945/946 (noCrit/dmg/size), `foeToHitVs` 1061 + `foeToHitBreakdown` 1108 (foeToHit), `weaponDamage` 1172/1181 (dmg/size), `revealRadius` 737 (sight), `mapViewRadius` 767 (light), `fluency` 1281 (tongue), `upkeep` 1193, `maxCharges` (movement.js 91), items.js 110 (greed), combat.js 632 (noCrit), 1793/1798 + magic.js 473/506 (throw/spellDmg); movement.js 393 (light dispels darkFor). The ONLY source of any item `eff` map in the whole content tree is treasure-tables.js JEWELRY/CLOAKS (grep confirms: potions carry a string `eff`, nothing else authors one), so a timer-only `eff()` is exactly the user's rule with zero collateral.
- `engine/movement.js` 134-176: the climb/gorge block's `flyOver` closure starts a fresh Cloak of Flying record itself (149-153) — the auto-activation to delete; 81-83 + 405-432: the 20-square Cloak of Healing/Regeneration passive tick (two constants + two gated blocks, the regen one draws rng.d(6)).
- `engine/items.js`: `itemReady` 1007-1014 refuses anything without a `use` key; `useItem` 1123-1372 resolves `kind` from `it.use` (1133), runs the refusal ladder, the kind switch, then `applyActivation` (1045-1064) — the ONE bookkeeping step (staff charge spend; `left > 0` -> startEffect with cd; instant + cd -> startCooldown, the Pendant precedent). Existing rows' `use` value equals their `act.kind` in every case; the torch is the one item whose `use` ("light", the switch case) differs from its activation kind ("lit").
- `content/treasure-tables.js`: rows 52-147; `act` is authored on rows and stripped by `dropAuthored` so exported tables stay byte-identical (header comment lines 27-36 — the parity reason); the Cloak of Flying (95-101) already proves the "act without use" shape. `buildActivation` 176-185. Once-a-day rule lines 38-45.
- Shell: mazeworld.html 3508 (worn-slot row, `if (it.use)` gates the Use button) and 4112 (shared carried-row builder, `it.kind === "potion" || it.use`); both already compute `st = window.__mzItemRowState(S, it)` two lines above. `src/browser/viewModels.js#itemRowState` 420-446 (first branch keyed on `it.use`). `src/browser/combatMenu.js` 243 and 257 filter usable rows on `.use`. Condition chips: CONDITION_COPY 2959-3009, CONDITION_TONE 3080, CONDITION_EXPLAIN 3081-3100, paintConditions flight branch 3167-3171. Narration: eventNarration.js 936-949 (kind-keyed itemEffectStarted map) + 209-210 (cloakHealed/cloakRegenerated "as you walk" lines) + 876; toasts.js 1552-1564 + 980-981 + 1489.
- Bot: `tools/lib/tuning-bot.mjs` RUN_FLAGS (574: wornSlots true — the bot always has `c.worn`); `chooseCombatItem` 697-760 (reads the cloak's and the staff's `use` key at 737 and 749); `chooseFieldItem` 772-780 (torch only); `decideAction` out-of-combat chain 949-998 (pendingHazard 949, drinkPotion 956, torch 960, final `move` 993-998).
- Parity: no parity fixture ever calls `useItem` (grep of test/parity/fixtures is empty) and the harness is green today against a frozen prototype that never implemented cloakHeal/cloakRegen/cloakArmor/noCrit/fly — so none of those passives ever fired inside a fixture; removing them cannot move a fixture. Rolled item SHAPES are protected by keeping `use` OFF the rows (act-only). Reworded `txt` on rows that reach a starting kit is stripped by `REWORDED_TXT_ITEMS` (comparables.js 389); every newly reworded name joins that set. The ONE deliberate fixture-moving change in this task is the Cloak of Healing removal (d8 -> d7 cloak rolls, see the amendment below) — declared per seed, never silently regenerated.

## Item conversion table (final — the orchestrator shows this to the user)

Model: duration+cooldown rows start an `item:<name>` effect record (`effect` squares) then cool for `cd` squares; instant rows (effect 0) start a bare cooldown, the Pendant of Fortitude precedent. Staves are unchanged (charges + recharge). Where the row's canon `txt` states numbers, they are used; otherwise the Cloak of Invisibility/Speed template (effect 50 / cd 50). Every row satisfies effect + cd <= 100 (once-a-day rule).

| Item (slot) | act.kind | Use does | effect (sq) | cd (sq) | eff payload while live | Basis |
|---|---|---|---|---|---|---|
| Ring of Power (ring) | power | +1 damage to all attacks | 50 | 50 | { dmg: 1 } | template — NOT in the orchestrator's proposed list, but the rule names "ring"; flag to the user |
| Gauntlet of the Giant (helm) | giant | one size larger (+2 flat damage via the size term) | 50 | 50 | { size: 1 } | template — same flag as the Ring |
| Amulet of Light (amulet) | glow | light + sight; also clears c.darkFor at once (darknessDispelled) | 50 | 50 | { sight: 1, light: 1 } | template |
| Anklet of Invisibility (bracelet) | unseen | foes need two better to land | 50 | 50 | { foeToHit: -2 } | template |
| Helm of Knowledge (helm) | tongue | perfect fluency (parley gate) | 50 | 50 | { tongue: 1 } | template |
| Bracelet of Flight (bracelet) | fly | flight (climb/gorge/water cost nothing) | 20 | 50 | { fly: 1 } | mirrors the Cloak of Flying; "unconditional" text becomes flavour |
| Cloak of Regeneration (cloak) | knit | d6 hp back, instantly (one rng.d(6) inside useItem) | 0 | 20 | — (instant) | canon "d6 hp back every 20 squares"; the simpler faithful reading — one roll per use on a 20-square cooldown, no new tick path |
| ~~Cloak of Healing~~ | — | REMOVED from the game (user, 2026-09-18) | — | — | — | CLOAKS 8 -> 7; rolls draw d(CLOAKS.length); old saves keep it as an inert cloak |
| Cloak of Strength (cloak) | brace | no critical lands on you | 50 | 50 | { noCrit: 1 } | template |
| Cloak of Armor (cloak) | plate | soak as plate (AR 15), never wears | 50 | 50 | { cloakArmor: 1 } | template |
| Cloak of Flying (cloak) | fly | flight | 20 | 50 | { fly: 1 } | existing act; auto-start removed, Use button added |
| Pendant of Fortitude, Amulet of Stone, Cloak of Invisibility, Cloak of Speed, Cloak of Ether | (unchanged) | | | | | already use-activated, worn to use |
| 8 staves (Rowan/Birch/Walnut/Oak/Crystal/Poplar/Pine/Cedar) | (unchanged kinds) | charges + recharge unchanged | — | — | — | NOT equipable: no worn slot; one bag slot; used from the bag by index; Magic User only |

ACTIVATION_OF grows from 20 to 29 entries (6 existing cd rows + 9 new + 8 staves + 5 potions + 1 tool). SLOT_OF goes from 24 to 15 entries (8 JEWELRY + 7 CLOAKS; staves leave the taxonomy).

Precondition (concurrency): two other quick tasks (260918-vm3, 260918-vvt) are planned in `.planning/quick/`; vvt touches engine/derived.js, engine/items.js, viewModels.js and mazeworld.html. Before Task 1, `git status --short -- engine src content mazeworld.html tools` MUST be empty (no in-flight edits from another task). If it is not, stop and report — never edit over another task's working tree.

Purpose: one rule, one code path — useItem -> effect record -> eff()/itemEffectActive reads — no scattered passives, no auto-activation, no silent flying.
Output: 9 converted rows + the Cloak of Healing removed; timer-only eff(); Use buttons everywhere an activatable is listed; chips + narration for the new kinds; a bot that uses its gear; docs + tests re-pinned; SUMMARY with the deferred browser checklist.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@content/treasure-tables.js
@engine/derived.js
@engine/items.js
@engine/movement.js
@src/browser/viewModels.js
@tools/lib/tuning-bot.mjs
@test/parity/harness/comparables.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Content + engine — every worn-slot item is act-authored, eff() is timer-only, no auto-activation, staves leave the worn taxonomy</name>
  <files>content/treasure-tables.js, engine/derived.js, engine/items.js, engine/movement.js, engine/actions.js, engine/saveState.js, test/parity/harness/comparables.js, test/unit/item-activation.test.js, test/unit/item-wiring.test.js, test/unit/conditions.test.js, test/unit/movement.test.js, test/unit/water-cost.test.js, test/unit/tools.test.js, test/unit/worn-model.test.js, test/unit/worn-slots.test.js, test/unit/worn-migration.test.js, test/unit/save-validation.test.js, test/unit/actions.test.js, test/unit/fluency.test.js, test/unit/darkness-filter.test.js, test/unit/armor-durability.test.js, test/unit/effect-expiry.test.js, test/unit/phobia-triggers.test.js, test/unit/spell-utility.test.js, test/unit/usable-features-audit.test.js</files>
  <behavior>
    - CLOAKS.length is 7 and no exported CLOAKS/JEWELRY row is named Cloak of Healing; TREASURE_BASE_VALUES has no such key; a Thief rolled from newRun carries one of the 7 cloaks; rollCloak / the Cloak find / the Thief kit each consume exactly one rng draw (chargen-rng-pin unchanged).
    - Tolerant load: validateSave + rehydrate (wornSlots) of a save whose c.items and c.worn.cloak each hold a Cloak of Healing object ({ kind: "cloak", n: "Cloak of Healing", eff: { cloakHeal: 1 }, txt }) succeeds; slotFor resolves it to cloak by kind fallback; activationFor is null; itemReady false; useItem({ slot: "cloak" }) pushes no event and mutates nothing; eff(c, "cloakHeal") is 0; conditionsOf shows nothing for it.
    - WORN_SLOTS deep-equals ["ring", "bracelet", "amulet", "helm", "cloak"]; SLOT_OF has 15 entries and no STAVES name; slotFor({ kind: "staff", n: "Pine Staff" }) is null; slotFor for every JEWELRY/CLOAKS name still resolves.
    - reconcileWorn on a Magic User whose bag holds a staff and a cloak wears the cloak and leaves the staff in c.items (no report entry for it); autoWearSlot(staff) is null for every class; takeItem of a staff by a Magic User pushes itemGiven and stows it in the bag (no itemEquipped); by a Fighter it is still refused wrongClass; equipItem on a bagged staff (Magic User, c.worn present) is refused notEquippable and nothing moves.
    - useItem(state, i) on a BAGGED staff for a Magic User with c.worn present is NOT refused notWorn: itemUsed fires, the charge is spent, the recharge cooldown starts (the Phase 39 behaviors, now by bag index); the slot-address form naming the staff slot is rejected by validateAction (reason names the five slots); unequipSlot("staff") likewise.
    - validateSave/rehydrate with wornSlots: a save whose c.worn.staff holds a staff object loads with that object appended to c.items and c.worn carrying no staff key; with a full bag (c.bag set, bag at cap) the appended staff is the one clampCarry drops.
    - ACTIVATION_OF has 29 frozen entries; each of the 9 new names resolves to the record in the conversion table (e.g. Cloak of Armor -> { kind: "plate", effect: 50, cd: 50, eff: { cloakArmor: 1 } }; Cloak of Regeneration -> { kind: "knit", effect: 0, cd: 20 }; Bracelet of Flight -> { kind: "fly", effect: 20, cd: 50, eff: { fly: 1 } }); Cloak of Flying now carries eff: { fly: 1 }; Cloak of Speed / Pendant / staves / potions / torch records are byte-identical to before; effect + cd <= 100 holds for every cd row.
    - Exported CLOAKS/JEWELRY/STAVES rows carry NO act/slot key; the Cloak of Speed row pin is unchanged; a Cloak of Armor row is { n, eff: { cloakArmor: 1 }, txt } with no use key.
    - eff(c, "cloakArmor") is 0 for a character WEARING an unused Cloak of Armor (c.worn.cloak) AND for one carrying it in the bag, with or without a c.worn key; it is 1 after useItem(state, { slot: "cloak" }) and 0 again once the record's effect phase has ticked out; the same shape for noCrit (Strength), dmg (Ring), size (Gauntlet), foeToHit -2 (Anklet), tongue (Helm), sight/light (Amulet of Light).
    - isFlying(state) is false for a worn, ready Cloak of Flying and for a worn Bracelet of Flight; true only while a live fly record runs (either item); moveCost on water is 2 for a ready-but-unused flight item, 1 while live.
    - move onto a climb/gorge with a worn READY Cloak of Flying rolls the climb (no flownOver, no record started, no itemEffectStarted); after useItem on the worn cloak, the same move emits flownOver with zero rng draws and the record ticks down by the step; the Bracelet behaves identically (20 squares, cd 50).
    - useItem on a worn Cloak of Regeneration at wp < maxWP: itemUsed, exactly one rng.d(6) draw, cloakRegenerated { amount }, no itemEffectStarted, a bare item:Cloak of Regeneration cooldown of 20 squares; a second immediate use is refused cooldown. Amulet of Light used with c.darkFor > 0: darkFor becomes 0 and darknessDispelled is pushed, then itemEffectStarted { kind: "glow", left: 50 }.
    - useItem on a BAGGED converted item with a c.worn key is refused notWorn before any side effect (the existing gate, unchanged); useItem on a worn Ring of Power hastes nothing but starts item:Ring of Power (kind power) and weaponDamage gains +1 while live.
    - conditionsOf: a live fly record surfaces { key: "flight", polarity: "good", flight: "charged", remaining, cadence: "squares", source } from the generic live-effect loop; a ready flight item yields NO flight chip; a cooling Cloak of Flying yields an itemCooldown chip like every other item; a live plate/brace/glow/unseen/tongue/power/giant record surfaces a generic { key: act.kind, ... } chip.
    - Walking 20+ squares while hurt with a worn-but-unused Cloak of Regeneration changes nothing and draws nothing (no cloakRegenerated event from movement any more; the cloakHealed event type no longer exists anywhere).
    - Parity: every fixture whose Thief seed / Cloak find now lands on a different cloak row carries a declared chargenDivergence record (fields includes items) whose before matches the prototype kit and whose after matches the engine kit; every other fixture is byte-identical; the master hash is unchanged.
    - itemReady: a converted item with no timer record is ready; a potion always; a weapon/rope/ladder (no activation) never.
  </behavior>
  <action>
    <!-- planner-discipline-allow: Cloak of Healing -->
    <!-- planner-discipline-allow: cloakHealed -->
    <!-- planner-discipline-allow: cloakHeal -->
    Precondition check first: `git status --short -- engine src content mazeworld.html tools` must print nothing; otherwise stop and report. Comment hygiene for this task: the deleted item's name and its old event/eff keys must not be written into any NEW code comment in content/treasure-tables.js, engine/economy.js, engine/items.js, engine/movement.js, src/browser/eventNarration.js, src/browser/toasts.js or src/browser/rail.js (the verify gate greps them comment-stripped, but a block-comment body line would slip through) — say "the dropped healing cloak" instead.

    **content/treasure-tables.js.** Delete the Cloak of Healing row (line 78) outright — CLOAKS_ROWS has 7 rows; update the file header ("CLOAKS (d7 — the Cloak of Healing was dropped by the user on 2026-09-18, quick 260918-w4n; every roll draws `rng.d(CLOAKS.length)`)") and the Phase 43 comment above the rows (it names the Healing row). Author an `act` block on each of the 9 remaining passive rows per the conversion table, following the Cloak of Flying row's exact shape (an `act` with an explicit `cd`, NO `use`, NO `every` — the rows' exported shapes must stay byte-identical, per the header comment's parity reasoning): Ring of Power `{ kind: "power", effect: 50, cd: 50 }`; Gauntlet of the Giant `{ kind: "giant", effect: 50, cd: 50 }`; Amulet of Light `{ kind: "glow", effect: 50, cd: 50 }`; Anklet of Invisibility `{ kind: "unseen", effect: 50, cd: 50 }`; Helm of Knowledge `{ kind: "tongue", effect: 50, cd: 50 }`; Bracelet of Flight `{ kind: "fly", effect: 20, cd: 50 }`; Cloak of Regeneration `{ kind: "knit", effect: 0, cd: 20 }`; Cloak of Strength `{ kind: "brace", effect: 50, cd: 50 }`; Cloak of Armor `{ kind: "plate", effect: 50, cd: 50 }`. Keep every row's `eff` map exactly as authored (it is the effect payload now, and part of the frozen row shape). In `buildActivation`, for BOTH the charges and the duration branches, copy the row's own `eff` onto the record as `eff` ONLY when the row has one with at least one key (so Cloak of Speed / Pendant / Amulet of Stone with `eff: {}` and every staff/potion stay byte-identical); freeze as today. Reword `txt` on all 9 converted rows plus nothing else, in the family-friendly deadpan voice, and make each state its activation truthfully (duration and cooldown, or the instant amount and cooldown) — e.g. the Amulet of Light reads like "used, it lights fifty squares and tells the dark to leave; then it sulks for fifty", the Bracelet reads like "twenty squares of flight when you ask; fifty to catch its breath", the Cloak of Strength says no critical lands for fifty squares, the Cloak of Regeneration says a d6 back on use, then twenty squares of rest. Update the header comment (lines 27-45) and the Cloak of Flying row comment (96-98) to describe the new rule: every JEWELRY/CLOAKS/STAVES row carries an `act`; `eff` maps are payloads applied only while the record is live; there is no auto-activation. Staff amendment: delete the authored staff slot key from all eight STAVES_ROWS (their `act` stays; `dropAuthored` still strips `act`, so the exported STAVES rows are byte-identical) and build `SLOT_OF` from `JEWELRY_ROWS` + `CLOAKS_ROWS` only — rewrite its doc comment ("exactly 16 entries; a staff has no slot — it is a bag item used by index"). Do not touch FAERIE/MISC_MAGIC/BLADE_NAMES.

    **The three cloak rolls.** `engine/character.js` 548, `engine/encounters.js` 400 and `engine/items.js` 134 (`rollCloak`) draw `rng.d(CLOAKS.length)` instead of the literal 8 — one draw each, exactly as before (engine/rng.js `d(sides)` is a single `gen.next()`), so no cursor shifts anywhere; add a one-line comment at each site naming the deliberate d7 divergence (260918-w4n). `engine/economy.js` 86: delete the Cloak of Healing base-value entry (re-pin any count in economy.test.js / store-sell.test.js). Leave `rollJewel`/`rollStaff` at their literal 8 (their tables are still 8 rows) — or switch them to `.length` too for symmetry, which is behaviour-identical; either way say which in the comment.

    **test/parity/harness/comparables.js.** Re-shape `REWORDED_TXT_ITEMS` (line 389): drop "Cloak of Healing" (no such row), add every name whose `txt` was reworded above (Ring of Power, Gauntlet of the Giant, Amulet of Light, Anklet of Invisibility, Helm of Knowledge, Bracelet of Flight, Cloak of Strength — Regeneration/Armor are already present); update its doc comment with this task's id and the reason (cosmetic txt carve-out, the Phase 28/43 precedent).

    **Parity fixtures — declare exactly what the d7 roll moves.** Run `node --test "test/parity/**/*.test.js"` AFTER the content change: every failure must be a `c.items` (or an items-derived declared field) divergence on a seed that rolls a cloak (Thief chargen or a Cloak find) — anything else is a bug, stop and fix it. For each moved seed, add or extend the `chargenDivergence` record in its fixture JSON (chargen: the per-seed `divergences` map in action-script.chargen.json; movement/economy: the top-level record; combat/encounters/magic: the scenario-level record — the existing Phase 38/40 records are the shape to copy): append `"items"` to `fields`, set `before.items` to the prototype kit and `after.items` to the engine kit exactly as measured (the test's own diff output / `diffState` names both), `phase: "quick-260918-w4n"`, `requirements: ["QUICK-260918-w4n"]`, and a rationale sentence: "Cloak of Healing removed (user, 2026-09-18); the Thief starting cloak / Cloak find now draws rng.d(7) over the 7 remaining rows — same single draw, different landing row for this seed's u; the rolled cloak is inert until worn and used, so nothing downstream moves." Where a downstream declared value ALSO moves because of the item (e.g. the economy fixture's declared `after.items`/gold if the Thief's cloak is sold), re-measure and update that record's after-values in the same edit, rationale extended. Never touch `test/parity/prototype-master.js.txt`; never edit a fixture's `seed` or `actions`. Then run `node tools/fixture-inventory.mjs`, paste its stdout between the `<!-- fixture-inventory:generated:begin -->` / `:end -->` markers in test/parity/FIXTURE-INVENTORY.md, and confirm `node --test test/parity/fixture-inventory.test.js` passes.

    **engine/derived.js.** Rewrite `eff(c, key)` as a single path: sum `e.act.eff[key]` over `liveItemEffects(c)` for every live effect whose `act.eff` is an object carrying a numeric `key` — no read of `c.items`, `c.worn`, or any item object's `eff` map remains (delete the two-path body and rewrite the doc comment: "an effect exists only while its item:<name> record is live"). Because `liveItemEffects` is defined later in the module, keep the function declaration order (hoisting handles it) but move `eff` below the activation-model section if the file's style prefers reads after their helpers. Rewrite `isFlying(state)` to return `itemEffectActive(state.c, "fly")` and its doc comment (delete the ready-counts-as-flying and Bracelet-precedence paragraphs; cite this task and the user's ruling). `moveCost`: drop the Bracelet name check; flight is `itemEffectActive(c, "fly")`, ether unchanged. `conditionsOf`: in the generic live-effect loop, when `act.kind === "fly"` push `{ key: "flight", polarity: "good", flight: "charged", remaining: rec.left, cadence: rec.cadence, source: key }` instead of skipping; delete the dedicated flight block (the Bracelet always / Cloak charged-cooldown-ready ladder) and delete the Cloak of Flying exclusion inside the itemCooldown loop so a cooling Cloak of Flying is reported like every other cooling item; update the header comment's flight bullet (a `flight` chip exists only while a fly record is live; cooldown shows as itemCooldown). `hasItemNamed`: after this task, grep engine/, src/, tools/, mazeworld.html for remaining callers; if the only callers are tests, delete the function and its doc comment (greenfield: no dead code) and rewrite the tests that imported it; keep `carriedItems` (the staffCharges chip and narrateTimerTransitions still need bag-or-worn lookup). Remove the now-unused `isReady` import from derived.js if nothing else in the file uses it (check `remaining` too). Update the `armorSoak` and `revealRadius`/`mapViewRadius` doc comments' "carries the Cloak/Amulet" wording to "while the Cloak's/Amulet's record is live" (comments only, no logic change — they already read eff()). Staff amendment in the same file: `WORN_SLOTS` becomes the frozen five-key list (ring, bracelet, amulet, helm, cloak) with its doc comment rewritten; `slotFor` loses the staff kind fallback (a staff resolves to null — it is not a slot item); `reconcileWorn` loses the Magic-User staff eligibility clause (`eligible = !!slot`) and its doc comment's staff sentence; `conditionsOf`'s staffCharges chip and `narrateTimerTransitions` keep reading `carriedItems` (a staff is now always in the bag; the union is harmless and still needed for nothing else — if `carriedItems` reduces to a bag read everywhere, keep the helper, simplify its comment).

    **engine/actions.js.** The `useItem.slot` and `unequipSlot.slot` reason strings (118, 161) name the five slots ("ring, bracelet, amulet, helm, cloak"); the `WORN_SLOTS.includes` checks need no change. Update the comment at 156-158 ("PLUS the five worn-model slots").

    **engine/saveState.js.** In `sanitizeWorn`, after the per-slot object check, fold a legacy staff back into the bag: when `c.worn.staff` survives as an object, `c.items = Array.isArray(c.items) ? c.items : []`, push the staff onto the END of `c.items`, delete `c.worn.staff`; then delete any `c.worn` key that is not in `WORN_SLOTS` (import it). Confirm the load path runs `clampCarry` (or the existing overflow discipline) AFTER sanitizeWorn so an over-cap bag drops the appended staff — if it does not, call `clampCarry(c)` right after the fold (it is a no-op without `c.bag`). Rewrite the doc comment (T-w4n tolerant-load: a v1.5 save's worn staff comes home to the bag; no dual path — every load runs it).

    **engine/movement.js.** In the climb/gorge block, reduce `flyOver` to: push `flownOver`, clear `there.feat`. Nothing in movement starts an item record any more; remove the resulting unused imports (`ACTIVATION_OF`, `startEffect`, `eff` if the darkFor dispel is its last use — it is NOT, line 393 still reads eff for light — keep `eff`), and rewrite the block comment (134-146) to say flight is only a live record started by useItem. Delete the two 20-square cloak-tick constants (81-83, with their comment) and the whole per-step Cloak of Healing / Cloak of Regeneration tick block (405-432, both gated `if`s and the comment above them) — the `crossings` helper stays if the ration/day cadence uses it (check before deleting). `maxCharges` keeps reading eff (returns 0 contribution now — harmless, no source).

    **engine/items.js.** `itemReady(state, it)`: potion -> true; otherwise `const act = activationFor(it); if (!act) return false;` then the existing staff/timer branches (delete the `it.use` gate and the "no activation -> ready" branch; rewrite the doc comment). In `useItem`, resolve `kind` as: potion -> `it.eff2`; otherwise `it.use ?? activationFor(it)?.kind` (the torch and the five legacy `use` rows keep their switch case via `use`; every act-only row resolves through its activation — state in the comment that `use`, where present, always equals the activation kind except the torch's light/lit pair). Keep the whole refusal ladder as-is (refuseIfPending, wrongClass, notWorn with its existing `c.worn` gate, pilfer, combatOnly, notDark, itemReady). Add switch cases, placed with the other timed kinds: `power`, `giant`, `unseen`, `tongue`, `brace`, `plate`, `fly` -> plain `break` (applyActivation starts the record); `knit` -> one `rng.d(6)`, add to `c.wp` capped at `c.maxWP`, push `cloakRegenerated { amount }` (0 allowed at full hp — the use still spends the cooldown, like a wasted potion); `glow` -> if `c.darkFor > 0` set it to 0 and push `darknessDispelled`, then break. Update the `useItem` doc comment's kind list. `applyActivation` needs no change (an act with `eff` is just extra data on the record lookup; `left`/`cd` logic already covers instant + cd and duration + cd). Staff amendment in items.js: `autoWearSlot` — delete the staff class clause (slotFor already returns null for a staff; a Magic User's staff is stowed like any bag item); `takeItem` — keep the wrongClass refusal at 472 exactly, the staff then falls through to the ordinary stow path; `equipItem` — change the slot-item branch's kind list to cloak/jewel only and delete the staff wrongClass sub-branch inside it (a staff now reaches the same `equipRejected { reason: "notEquippable" }` a potion does — verify that is the fall-through and, if the function returns something else for an unknown kind, add the explicit staff -> notEquippable refusal before the branch); `useItem` — the notWorn gate at 1149 already keys on `slotFor(it)`, which is null for a staff, so a bagged staff addressed by index passes straight to the wrongClass/pilfer/combatOnly/itemReady ladder — update the doc comment (1117-1122) to the governing rule ("equipable items must be worn to be used; a staff is not equipable and is used from the bag") and the notWorn refusal's own comment (1144-1148).

    **Tests (rewrite pins to the new rule; TDD — write the failing assertions first, then implement).** `item-activation.test.js`: 29 entries; the new records; Cloak of Flying's record gains `eff: { fly: 1 }`; `itemReady` pin (line 143) — an item with an activation but no `use` is ready, a weapon/rope never. `item-wiring.test.js`: delete the Cloak of Healing section (171-190) and its `CLOAK_HEALING` constant (93); every other passive test (Helm 102, Strength 115, Amulet of Light 158, Regeneration 191-219, Gauntlet 247) becomes "worn + used -> effect live; worn-but-unused -> nothing; bag -> notWorn": use `useItem(state, { slot }, rng, [], () => 1)` on a `c.worn` hero and assert the effect (fluency 1 / no crit / darkFor cleared + glow record / one d6 draw + cloakRegenerated amount + 20-square cooldown / +2 size damage), then that the unused case reads 0; keep the Pendant/Stone/Ether/Pine tests. `content-tables.test.js` 349 and `worn-model.test.js` 77: CLOAKS length 7; `worn-model.test.js` 41 EFF_KEYS drops cloakHeal; every test fixture object named Cloak of Healing (worn-migration.test.js 30/95, worn-model.test.js 217/225/302, water-cost.test.js 219-228, loot-pile.test.js, economy.test.js) is re-pointed at Cloak of Regeneration or Cloak of Strength with the SAME assertions. New tolerant-load test (in worn-migration.test.js or save-validation.test.js): the inert-Cloak-of-Healing save case from the behavior list. `conditions.test.js`: flight pins (164-186 and the fully-loaded orderings) — Bracelet/Cloak present but unused yield no flight chip; a live `item:Cloak of Flying` (or Bracelet) effect record yields the charged chip from the generic loop position; a cooling record yields an itemCooldown chip. `movement.test.js` 313-410: the Bracelet no-draw tests now start a live fly record first (via `startEffect(c, "item:Bracelet of Flight", { squares: 20, cd: 50 })` or a worn-slot `useItem`); add the "ready cloak does NOT fly, rolls the climb, starts nothing" pin; the "cooldown -> rolls resume" test keeps its meaning. `water-cost.test.js` 95/148: Bracelet pays 1 only while live; the Cloak of Healing water-step test (219-228) is deleted (no movement tick, no such item). `tools.test.js` 561: isFlying wins only with a live record. `phobia-triggers.test.js` 207, `spell-utility.test.js` 129: same live-record shape. `worn-model.test.js`: the "hasItemNamed/isFlying/conditionsOf see a worn item as in the bag" section (184-200) and every `eff` two-path pin (16 uses) become timer-model pins: a worn unused item contributes 0, a live record contributes its payload, a bag copy never counts. `worn-slots.test.js` 459: a bagged Ring is still refused notWorn (unchanged); 466 legacy identity stays (a legacy no-worn state can still bag-use a Cloak of Speed — the gate is unchanged); staff pins 148/157 -> "equipItem on a staff is refused notEquippable for a Magic User too; nothing moves", 224 (autoWearSlot) -> a staff returns null for every class, 349/369 -> a Magic User taking a staff stows it in the bag (itemGiven, no itemEquipped) / a Fighter is refused wrongClass, 411 -> the SAME assertions by bag index (`useItem(state, i, …)`) with `c.worn` present — this is the required staff-from-bag use test; 445 -> a bagged staff on a non-caster is refused wrongClass (there is no notWorn for a staff any more); 496 (applyAction slot dispatch) -> switch the item to a worn cloak and add a sibling pin that a useItem action whose slot field says staff is rejected by validateAction. `worn-model.test.js` 61/70/103/123/244: SLOT_OF 16 entries with no staff names; slotFor(staff) null; reconcileWorn leaves a Magic User's staff bagged with no report entry. `worn-migration.test.js` 210: an old Magic User save's staff stays bagged AND a save that already carries `c.worn.staff` folds it into the bag on load (append the overflow case: `c.bag` at cap -> the staff is the item clampCarry drops). `save-validation.test.js` / `actions.test.js`: re-pin the reason strings and the five-slot list. `fluency.test.js`, `darkness-filter.test.js`, `armor-durability.test.js`, `effect-expiry.test.js`: wherever a test builds a character with a bagged/worn Helm/Amulet/Cloak of Armor and expects the passive, start the live record instead. `usable-features-audit.test.js` 515-518: iterate `Object.keys(TREASURE_ACTIVATION_OF)` for the doc-sync instead of `.use` filters (doc updated in Task 2); its staff CASES (226-260) already use bag-index `useItem` on a hero with no `c.worn` — add `worn: {}` to one Magic User staff case to prove the bag use is not refused notWorn under the new model.
  </action>
  <verify>
    <automated>test "$(node -e "import('./content/activations.js').then(m=>console.log(Object.keys(m.ACTIVATION_OF).length))")" = "29" && test "$(node -e "import('./content/treasure-tables.js').then(m=>console.log(m.CLOAKS.length))")" = "7" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' content/treasure-tables.js engine/economy.js engine/items.js engine/movement.js src/browser/eventNarration.js src/browser/toasts.js src/browser/rail.js | grep -c 'Cloak of Healing\|cloakHeal\b\|cloakHealed')" = "0" && test "$(grep -c 'CLOAKS\[rng.d(CLOAKS.length) - 1\]' engine/character.js engine/encounters.js engine/items.js | grep -c ':1$')" = "3" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' engine/derived.js | grep -c 'it\.eff\[key\]')" = "0" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' engine/derived.js | grep -c 'return itemEffectActive(state.c, "fly");')" = "1" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' engine/movement.js | grep -c 'startEffect(')" = "0" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' engine/movement.js | grep -c 'cloakHealed\|cloakRegenerated')" = "0" && test "$(grep -c '^export const WORN_SLOTS = Object.freeze(\["ring", "bracelet", "amulet", "helm", "cloak"\]);' engine/derived.js)" = "1" && test "$(node -e "import('./content/treasure-tables.js').then(m=>console.log(Object.keys(m.SLOT_OF).length))")" = "15" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' content/treasure-tables.js | grep -c 'slot: "staff"')" = "0" && node --test test/unit/item-activation.test.js test/unit/item-wiring.test.js test/unit/conditions.test.js test/unit/movement.test.js test/unit/water-cost.test.js test/unit/tools.test.js test/unit/worn-model.test.js test/unit/worn-slots.test.js test/unit/worn-migration.test.js test/unit/save-validation.test.js test/unit/actions.test.js test/unit/fluency.test.js test/unit/darkness-filter.test.js test/unit/armor-durability.test.js test/unit/effect-expiry.test.js test/unit/phobia-triggers.test.js test/unit/spell-utility.test.js test/unit/usable-features-audit.test.js test/unit/items.test.js test/unit/combat.test.js test/unit/engine-purity.test.js && node --test "test/parity/**/*.test.js" "test/determinism/**/*.test.js" "test/roundtrip/**/*.test.js" "test/persistence/**/*.test.js" && test -z "$(git diff --name-only -- test/parity/fixtures | grep -v 'action-script\.[a-z]*\.json$')" && for f in $(git diff --name-only -- test/parity/fixtures); do grep -q '260918-w4n' "$f" || exit 1; done && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0"</automated>
  </verify>
  <done>The Cloak of Healing is gone (CLOAKS 7, three d(CLOAKS.length) rolls, base value/event/eff key removed, inert on old saves); ACTIVATION_OF has 29 entries with the table's records; exported row shapes are unchanged; eff() reads only live records; isFlying is the live-fly read; movement neither starts a record nor ticks a cloak; useItem handles knit/glow and the plain timed kinds through applyActivation; a staff has no slot anywhere (WORN_SLOTS five keys, SLOT_OF 16, slotFor null, equip refused, take stows, legacy worn staff folded into the bag on load) and a Magic User uses it from the bag by index; every listed unit suite and the full parity/determinism/roundtrip/persistence tiers pass; the only fixture edits are declared chargenDivergence records on the seeds the d7 roll moved (each citing 260918-w4n), the fixture inventory is regenerated, and the frozen master hash is unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shell Use buttons, chips and copy for the new kinds, tuning-bot policy, docs</name>
  <files>src/browser/viewModels.js, src/browser/combatMenu.js, src/browser/eventNarration.js, src/browser/toasts.js, mazeworld.html, tools/lib/tuning-bot.mjs, docs/GEAR-BALANCE.md, docs/GEAR-SLOTS.md, docs/USABLE-FEATURES-AUDIT.md, test/unit/itemRowState.test.js, test/unit/combatMenu.test.js, test/unit/gear-panels.test.js, test/unit/shell-worn-slots.test.js, test/unit/shell-map-hud.test.js, test/unit/tuning-bot.test.js, test/unit/bot-tactics.test.js</files>
  <behavior>
    - itemRowState: a worn Cloak of Armor / Ring of Power / Bracelet of Flight with no record reads { text: "READY", kind: "ready" }; mid-effect "{n} SQ" kind effect; cooling "cd {n} SQ" kind cooldown; a bagged staff reads READY / "{k}/{max} · {n} SQ" exactly as before; a weapon/rope/ladder reads kind none; a potion and the torch read consumable (unchanged).
    - combatMenu usable rows include every worn slot item whose activationFor is non-null (a worn Cloak of Strength appears) and every bagged activatable including a Magic User's staff (by index); the worn rows never include a staff.
    - emptySlotRows yields five slot rows (no staff row, no class-aware text); GEAR_COPY.empty carries no staff leaves; the Gear BAG row for a staff shows Use (a Magic User) and never Equip.
    - mazeworld.html source: the worn-slot row and the shared carried-row builder append the Use button when st.kind !== "none"; CONDITION_COPY/CONDITION_TONE/CONDITION_EXPLAIN carry rows for power, giant, glow, unseen, tongue, brace, plate; the shell-map-hud (e) subset/voice pins still hold.
    - eventNarration.itemEffectStarted and toasts.itemEffectStarted return a kind-specific line (not the generic fallback) for each of power/giant/glow/unseen/tongue/brace/plate; the cloakRegenerated line no longer says "over twenty quiet squares"; no cloakHealed entry exists in eventNarration.js, toasts.js or rail.js (the event type is gone) and the narration/toast coverage guards still pass.
    - Bot: chooseFieldItem returns { type: "useItem", slot: "amulet" } for a hero in the dark wearing a ready Amulet of Light and carrying no torch (torch still wins when carried); returns { type: "useItem", slot: "cloak" } for a hero below potionThreshold wearing a ready Cloak of Regeneration — before drinkPotion; decideAction returns { type: "useItem", slot } for a ready worn Cloak of Flying / Bracelet of Flight when the chosen move dir targets a climb/gorge cell and no fly record is live; chooseCombatItem's round-1 hard-fight buff tier returns the first ready worn buff in WORN_SLOTS order among kinds haste/brace/plate/unseen/power/giant whose kind is not already live; the staff tier returns { type: "useItem", i } for a Magic User's ready BAGGED staff (first staff in c.items) under the existing targeted/dome/heal conditions — never a slot form; a hero that wants to parley (below the flee line, or talk-first round 1) but !canParley and wears a ready Helm of Knowledge gets { type: "useItem", slot: "helm" } first. All reads go through activationFor(it).kind; the bot never reads it.use.
  </behavior>
  <action>
    Precondition: `git status --short -- mazeworld.html` must be clean apart from Task 1's own commit (Task 1 does not touch it) — if another task's edit is in flight, stop and report.

    **src/browser/viewModels.js#itemRowState.** First branch: `if (!it) return none;` then `if (it.kind === "potion") return consumable;` then `const act = activationFor(it); if (!act) return { text: "", kind: "none" };` then `if (it.kind === "tool") return consumable;` then the existing staff/timer branches (the "no activation -> READY" branch is gone — an item without an activation is not activatable). Rewrite the doc comment (kind none = no activation; consumable = potion or activatable tool).

    **src/browser/viewModels.js — staff amendment.** `emptySlotRows`: delete the staff special case (the loop over the five `WORN_SLOTS` reads `GEAR_COPY.empty[slot]` for every slot) and delete both staff leaves of `GEAR_COPY.empty` (the plain text and the not-you text); rewrite the doc comment (five slots; a staff is a bag item). Keep the `usableBy`/legality staff Magic-User reads (190-238) — they describe who can USE it, which is unchanged.

    **src/browser/combatMenu.js** 243 and 257: filter usable bag rows on `it.kind === "potion" || activationFor(it)` (a Magic User's bagged staff now appears here, addressed by index) and worn rows on `activationFor(c.worn[slot])` (import `activationFor` from engine/derived.js beside the existing imports; with `WORN_SLOTS` at five keys a staff can never be a worn row). Update the two comments to the governing rule.

    **mazeworld.html.** Line 3508 (worn-slot row): gate the Use button on `st.kind !== "none"` (st is already computed at 3496). Line 4112 (shared carried-row builder): gate on `st.kind !== "none"` (st at 4089); keep the Phase 31 "never disable silently" comment — a bagged cloak/jewel's Use tap still yields the engine's notWorn toast, while a bagged staff's Use tap simply works for a Magic User. The Equip affordance for a staff disappears by itself (line 4115 keys on `window.__mzSlotFor(it)`, now null for a staff) — confirm and update that comment; the worn-slot loop (3511) iterates `window.__mzWornSlots` (five keys) so no staff worn row is rendered — confirm no other shell code enumerates a "staff" slot (grep the file for the staff slot key; the GEAR section comment at 1560-1563 is rewritten: "a staff is a bag item, used from the BAG panel"). Update the wornSlotRow comment (3490-3494) to say every worn item has a Use button because every worn-slot item is activatable. Cloak of Healing amendment in the classic in-shell duplicate tables (dead code slated for the shell-cleanup milestone, but it must not be able to roll a ghost item): delete the Cloak of Healing row from the classic `CLOAKS` literal (1942) and change the three classic `D(8)` cloak rolls (2397, 3870, 4372) to `D(CLOAKS.length)`; nothing else in the classic block changes. CONDITION_COPY: add `power: { label: "Empowered", unit: "sq" }`, `giant: { label: "Giant-handed", unit: "sq" }`, `glow: { label: "Alight", unit: "sq" }`, `unseen: { label: "Unseen", unit: "sq" }`, `tongue: { label: "Fluent", unit: "sq" }`, `brace: { label: "Braced", unit: "sq" }`, `plate: { label: "Plated", unit: "sq" }` with a one-line comment citing this task; CONDITION_TONE: power/giant/brace/plate good, glow/unseen/tongue odd; CONDITION_EXPLAIN: one deadpan sentence each in the house voice (e.g. Empowered: "One more point of damage on every swing, for a while. The ring calls it generosity."; Plated: "Weightless plate over whatever you wear. It still will not make you graceful."; Fluent: "You understand them perfectly. Whether that helps is up to them."; Alight: "You are your own lantern. The dark is filing a complaint."; Unseen: "They need two better to land a blow, and they know it."; Braced: "No critical lands on you. Ordinary hits still count."; Giant-handed: "One size larger. Mind the ceilings."). Flight: `CONDITION_COPY.flight` keeps label "Flying"; in paintConditions (3167-3171) keep the charged detail (`${remaining} sq`) and delete the cooldown detail branch and the "always/ready carry no count" comment (those sub-states no longer exist; the cooldown renders through itemCooldown). CONDITION_EXPLAIN.flight can stay.

    **src/browser/eventNarration.js + src/browser/toasts.js.** In both `itemEffectStarted` maps add kind-keyed lines for power/giant/glow/unseen/tongue/brace/plate (Oracle line may carry a `<span class="hit">`; toast text is plain), in the same deadpan register as the existing haste/invis lines and using `n` (e.g. glow: "Fifty squares of being your own lantern."; unseen: "Unseen for N squares. They need two better."; brace: "N squares with nothing critical landing on you."; plate: "N squares of weightless plate."; power: "+1 damage for N squares. The ring approves."; giant: "N squares of being one size too large for the corridor."; tongue: "N squares of perfect fluency. Do not waste it on small talk."). The `fly` line already fits both flight items. Delete the `cloakHealed` entries (Oracle 209 / toast 980, and any rail.js card row for it) — the event type no longer exists; reword `cloakRegenerated` (210 / 981) to the on-use reading ("Flesh knits itself back — +N hp. Ask again in twenty squares." / "Flesh knits +N hp."); keep that event name. If a coverage guard (formatEventsCoverage / toastsCoverage) also pins "no stale narration keys", the deletion satisfies it; if it pins a literal event list, remove cloakHealed there. `darknessDispelled` lines (876 / 1489) stay.

    **tools/lib/tuning-bot.mjs (the bot plays the new rules).** Add a module-private `readyWorn(state, ctx, slot, kinds)` helper: returns the worn item in `c.worn[slot]` when it exists, its `activationFor(it)` kind is in `kinds`, `itemReady(state, it)` and it is not in `ctx.itemBlocked`; pure. Replace both `use`-key reads (the cloak haste check at 737, the staff kind read at 749) with `activationFor(it).kind`; staff amendment: the staff tier (742-758) reads the FIRST `kind === "staff"` item in `c.items` and dispatches `{ type: "useItem", i }` — delete the worn-model branch and the `staffRef` slot form entirely (a staff is never worn); its doc bullet (3) becomes "a Magic User's ready bagged staff". Round-1 buff tier: after the potion loop, iterate `WORN_SLOTS` (import from engine/derived.js) and return the first slot whose worn item's kind is in {haste, brace, plate, unseen, power, giant}, is ready, and whose kind is not already `itemEffectActive`. Parley assist: at the two places decideAction wants to parley (the flee-line branch ~876 and the talk-first branch ~890), when `!canParley(state)` and `readyWorn(state, ctx, "helm", ["tongue"])` and no live tongue effect, return `{ type: "useItem", slot: "helm" }` (the next decideAction then parleys). `chooseFieldItem`: (1) dark with no live `lit`/`glow`: torch first (existing), else a ready worn amulet of kind glow -> `{ type: "useItem", slot: "amulet" }`; (2) `ratio < potionThreshold` and a ready worn cloak of kind knit -> `{ type: "useItem", slot: "cloak" }` — move the `chooseFieldItem` call ABOVE the drinkPotion/camp lines (956-957) so a free cloak heal is tried before a potion or a camp; (3) flight: in decideAction, after the final `dir` is chosen (993-998), look up the target cell (reuse the module's own here/there cell lookup used by the step-legality helper near line 120) and, when its `feat` is "climb" or "gorge", no fly effect is live, and a ready worn Cloak of Flying (slot cloak, kind fly) or Bracelet of Flight (slot bracelet, kind fly) exists, return `{ type: "useItem", slot }` instead of the move (the next decideAction re-derives the same dir and flies over). Update the doc comments (chooseCombatItem, chooseFieldItem, decideAction's priority list) and the `RUN_FLAGS` comment.

    **Tests.** `itemRowState.test.js` 33-60: the "no use -> none" pin becomes "no activation -> none"; add ready/effect/cooldown pins for a worn act-only item (Cloak of Armor); the staff pins keep their assertions on a BAGGED staff. `combatMenu.test.js`: usable-row pins include a worn act-only cloak and a Magic User's bagged staff by index; no worn staff row. `gear-panels.test.js` 133 (and any GEAR_COPY frozen-shape pin): delete the staff empty-row test, re-pin five slot rows and the GEAR_COPY.empty shape without staff leaves. `shell-worn-slots.test.js`: re-pin any five-vs-six slot count and any staff worn-row expectation. `shell-map-hud.test.js` (e): the new keys pass the subset/voice scan — extend any explicit key list. `tuning-bot.test.js` / `bot-tactics.test.js`: add pins for each behavior bullet above (dark amulet, hurt Regeneration cloak before potion, pre-hazard flight use, round-1 worn buff ordering, helm-before-parley, bagged staff by index); re-pin any existing test moved by the chooseFieldItem reorder (only tests that wear a knit cloak move — verify that no existing D-05 potion pin wears one) and any pin that placed a staff in `c.worn.staff`.

    **Docs.** `docs/GEAR-BALANCE.md` "Item activation model (GEAR-02)": add a dated subsection "Use-activated only (user ruling 2026-09-18, quick 260918-w4n)" quoting BOTH user sentences (the governing rule and the staff amendment), then extend the duration+cooldown table with the 9 new rows (kind, effect, cd, eff payload, basis) exactly as in this plan's table, add a struck-through Cloak of Healing line ("removed from the game, user 2026-09-18; CLOAKS d8 -> d7"), replace the "Passive rows … no act" sentence with "there are no passive rows", note the auto-activation removal under the Cloak of Flying row, and add one line under the charges table: staves are not equipable — bag item, used by index, Magic User only. `docs/GEAR-SLOTS.md`: delete the Cloak of Healing table row (73) and add a dated note that the taxonomy is now five slots (staff removed; a legacy worn staff folds into the bag on load). `docs/TERRAIN.md` 167: delete the cloak-tick row (the tick is gone). `docs/USABLE-FEATURES-AUDIT.md`: "Cloaks (7)", add every newly usable name to the gear table (the doc-sync test now iterates TREASURE_ACTIVATION_OF keys) and reword the staff rows' "worn" language to "from the bag". `test/parity/FIXTURE-INVENTORY.md` (prose sections, the generated table was pasted in Task 1): a dated 260918-w4n entry listing every seed whose cloak roll moved with its before/after cloak, the d7 rationale, the REWORDED_TXT_ITEMS re-shape (Healing out, seven names in), SLOT_OF now 15, and the statement that the frozen master is untouched.
  </action>
  <verify>
    <automated>test "$(grep -c 'if (st.kind !== "none") li.appendChild(gearBtn("Use"' mazeworld.html)" = "1" && test "$(grep -c 'if (st.kind !== "none") li.appendChild(mkBtn("Use"' mazeworld.html)" = "1" && for k in power giant glow unseen tongue brace plate; do grep -q "^  $k: *{ label:" mazeworld.html || exit 1; grep -q "^      $k:" src/browser/eventNarration.js || exit 1; grep -q "^      $k:" src/browser/toasts.js || exit 1; done && test "$(grep -v '^\s*\(//\|\*\|/\*\)' tools/lib/tuning-bot.mjs | grep -c '\.use\b')" = "0" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' src/browser/viewModels.js src/browser/combatMenu.js | grep -c 'it\.use\b')" = "0" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' src/browser/viewModels.js | grep -c 'staffNotYou')" = "0" && test "$(grep -v '^\s*\(//\|\*\|/\*\)' tools/lib/tuning-bot.mjs | grep -c 'slot: "staff"')" = "0" && node --test test/unit/itemRowState.test.js test/unit/combatMenu.test.js test/unit/shell-map-hud.test.js test/unit/shell-gear-39.test.js test/unit/shell-worn-slots.test.js test/unit/gear-panels.test.js test/unit/characterSheetViewModel.test.js test/unit/usableBy.test.js test/unit/tuning-bot.test.js test/unit/bot-tactics.test.js test/unit/toastsCoverage.test.js test/unit/formatEventsCoverage.test.js test/unit/narrativeToasts.test.js test/unit/usable-features-audit.test.js test/unit/class-pass-usage.test.js && node tools/tune-difficulty.mjs --seeds=30 --json > /dev/null</automated>
  </verify>
  <done>Every activatable item shows a Use button in the Gear worn row (five slots, no staff row), the Gear bag row (a Magic User's staff: Use, never Equip) and the combat ITEMS submenu via the one row-state rule; the seven new kinds have chip copy, tone, explain, Oracle and toast lines and cloakHealed is gone from every narration surface; the classic in-shell tables cannot roll the deleted cloak; the bot uses its worn gear (Regeneration, not Healing) and its bagged staff under the new rules with pins for each behavior; the ledger docs, the slot doc, the terrain doc and the audit doc reflect the 7-cloak table, every converted row and the staff rule; a 30-seed bot smoke completes without throwing.</done>
</task>

<task type="auto">
  <name>Task 3: Full gate, build, SUMMARY with the deferred browser checklist</name>
  <files>.planning/quick/260918-w4n-magic-items-are-use-activated-only-no-pa/260918-w4n-SUMMARY.md</files>
  <action>
    Run `npm test` and require `# fail 0` (if any test outside this task's files fails on the clean tree, report it as pre-existing in the SUMMARY; do not touch those files). Run `npm run build:www` (exit 0). Confirm the master hash is still a1f4d0dc29782218d8e5aab65bc5989c33f917f0 (the frozen prototype is never edited — the d7 divergence lives in the fixture JSONs), that `git diff --name-only -- test/parity/fixtures` lists only `action-script.*.json` files, that every one of them carries a `260918-w4n` chargenDivergence rationale, and that the parity suite is green with those declarations (if any parity failure is NOT a declared cloak-roll items shift or a reworded `txt` covered by REWORDED_TXT_ITEMS, STOP and report the exact diff — never regenerate anything else). Do NOT run `npm run android:debug` or any `adb` command (deferred-UAT protocol: one batched device checklist at milestone close; the orchestrator offers the Play build).

    Write the SUMMARY (`260918-w4n-SUMMARY.md`, frontmatter `status: complete`): all three user rulings verbatim (the governing rule, the staff amendment, and "Drop Cloak of Healing"), the final conversion table (copy from this plan, with the Ring of Power / Gauntlet of the Giant flag — they were outside the orchestrator's proposed list but inside the rule as stated), the one code path (useItem -> applyActivation -> c.timers -> eff()/itemEffectActive), the deleted paths (eff two-path, isFlying ready-counts-as-flying, movement auto-start, the 20-square cloak tick, the staff worn slot, the Cloak of Healing row/event/base value, hasItemNamed if removed), the declared cloak-roll fixture divergences (seed -> before cloak -> after cloak, one line each), the bot policy additions, gate results (test count, fail 0, build:www, master hash unchanged, list of fixture JSONs edited), the txt carve-out re-shape, and end with `## Human verification` listing the manual browser sanity checks for the batched Pixel 7 round: (1) serve the repo root over http (`npx serve .`) and open mazeworld.html; new run; Hero -> Gear: a starting cloak (Thief) shows `worn · READY` with Use and Unequip; the ON YOU panel lists five empty-slot rows and no staff row; (2) find/buy a Cloak of Flying; in the BAG its row shows Use — tap: toast "…is in your bag, doing what things in bags do: nothing. Wear it first." and NO Flying chip anywhere; Equip it: still no Flying chip; walk onto a crevice: the climb roll card appears (no fly-over); Gear -> Use: toast "Twenty squares of not touching the floor.", chip `Flying 20 sq`, the next crevice/wall is flown over, the row reads `N SQ`, then `cd N SQ` and the chip becomes `Cloak of Flying cd N sq`; a second Use during cooldown toasts the cooldown refusal; (3) wear a Cloak of Regeneration at less than full hp: Use -> `Flesh knits +N hp` toast, row `cd 20 SQ`; walking 20 squares hurt with it worn-but-unused heals nothing; a Thief's starting cloak is never a Cloak of Healing across several new runs; (4) wear an Amulet of Light, get Darkness (or a dark tile): Use -> darkness lifts at once, chip `Alight 50 sq`, the map view widens; (5) wear a Cloak of Armor and take a hit: soak reads plate only while `Plated` shows; (6) wear a Helm of Knowledge: TALK is offered only while `Fluent` shows; (7) as a Magic User with a staff: the BAG row shows Use (no Equip), it occupies a bag slot in the `n / m` count, Use from the bag works in and out of combat (the ITEMS submenu lists it), the charges readout `k/max · N SQ` counts down; as a Fighter the same row's Use tap toasts wrongClass; (8) load a v1.5 save carrying a bagged cloak and a worn staff: it loads, the cloak shows Use with nothing live, the staff now sits in the BAG panel (a full bag drops it with the usual overflow behaviour); a v1.5 save carrying a Cloak of Healing loads with that cloak listed as an inert row (no Use button, no chip, Drop/Unequip still work); no console errors.
  </action>
  <verify>
    <automated>npm test 2>&1 | grep -q '^# fail 0' && npm run build:www > /dev/null && test -z "$(git diff --name-only -- test/parity/fixtures | grep -v 'action-script\.[a-z]*\.json$')" && for f in $(git diff --name-only -- test/parity/fixtures); do grep -q '260918-w4n' "$f" || exit 1; done && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && test -f .planning/quick/260918-w4n-magic-items-are-use-activated-only-no-pa/260918-w4n-SUMMARY.md && grep -q '^## Human verification' .planning/quick/260918-w4n-magic-items-are-use-activated-only-no-pa/260918-w4n-SUMMARY.md</automated>
  </verify>
  <done>Full suite # fail 0, www built, only declared cloak-roll fixture divergences (each citing 260918-w4n), frozen master hash unchanged, SUMMARY written with all three rulings, the conversion table, the divergence list, gate results and the eight-step deferred browser checklist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| save file -> engine | a tampered `c.timers` record or item object crosses into eff()/liveItemEffects on load |
| shell tap -> engine | useItem({ slot }) dispatched from a Use button on any listed row |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w4n-01 | Tampering | engine/derived.js#eff via liveItemEffects | low | mitigate | eff() sums only records whose key resolves to a frozen ACTIVATION_OF entry and whose `act.eff[key]` is a number — an injected `item:Bogus` record or a non-numeric payload contributes nothing (existing unknown-key skip, T-39-07) |
| T-w4n-02 | Tampering | engine/items.js#useItem charges/cooldown | low | accept | a save that hand-edits `it.charges` is already clamped by foldLegacyCounters; a hand-deleted cooldown record only lets a solo offline player cheat themselves |
| T-w4n-03 | Elevation | mazeworld.html Use button -> useItem(bag index) | low | mitigate | the engine's own notWorn/cooldown/pilfer/combatOnly ladder refuses before any side effect; the shell never bypasses it (Use dispatches the same window.mzUseItem bridge) |
| T-w4n-SC | Tampering | npm/pip/cargo installs | low | accept | no package is installed by this task (zero-dependency tests, existing Capacitor deps untouched) |
</threat_model>

<verification>
- `npm test` -> `# fail 0`; `npm run build:www` exit 0.
- `git diff --name-only -- test/parity/fixtures` lists only action-script JSONs, each carrying a `260918-w4n` chargenDivergence record for a cloak-roll seed; `git hash-object test/parity/prototype-master.js.txt` = a1f4d0dc29782218d8e5aab65bc5989c33f917f0 (the frozen master is never edited).
- CLOAKS has 7 rows and no code, content, narration or doc surface names the Cloak of Healing except as "removed"; ACTIVATION_OF has 29 entries; every cd row satisfies effect + cd <= 100; SLOT_OF has 15; WORN_SLOTS has five keys.
- Comment-stripped greps: no item-object eff-map read in engine/derived.js; no record start and no cloak tick event in engine/movement.js; no staff slot key in content/treasure-tables.js or tools/lib/tuning-bot.mjs; no `.use` read in tools/lib/tuning-bot.mjs, src/browser/viewModels.js, src/browser/combatMenu.js.
- The SUMMARY's `## Human verification` carries the eight-step browser checklist for the batched Pixel 7 round.
</verification>

<success_criteria>
- Governing rule holds: equipable items (ring/bracelet/amulet/helm/cloak) must be worn to be used and grant nothing until used; non-equipable items (staff/potion/torch/scroll) are used from the bag by index and a staff has no slot anywhere.
- A bagged or worn-but-unused magic item grants nothing (no flying, no plate, no fluency, no light, no heal tick); the effect exists only while its live record runs, started by Use on a worn item, then the cooldown.
- Every activatable item shows a Use button wherever activatables are listed; a ready Cloak of Flying is not flying and never self-starts on a climb; a staff never shows Equip.
- The Cloak of Healing is removed from the game (7 cloaks, d(CLOAKS.length) rolls, declared fixture divergences only where the roll landed elsewhere, frozen master untouched); an old save carrying one loads it as an inert cloak.
- The bot uses its worn gear and bagged staff under the new rules; docs/tests are re-pinned; old saves load — a bagged cloak grants nothing until worn and used, a worn staff comes home to the bag.
</success_criteria>

<output>
Create `.planning/quick/260918-w4n-magic-items-are-use-activated-only-no-pa/260918-w4n-SUMMARY.md` when done.
</output>
