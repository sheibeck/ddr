---
quick_id: 260917-bbs
slug: v1-4-uat-fixes-round-2
date: 2026-09-17
status: planned
phase: quick-260917-bbs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mazeworld.html
  - src/browser/rail.js
  - src/browser/combatPanel.js
  - test/unit/shell-map-hud.test.js
  - test/unit/shell-party-camp.test.js
  - test/unit/shell-map-invariants.test.js
  - test/unit/rail.test.js
  - test/unit/combatPanel.test.js
  - test/unit/shell-map-rail.test.js
  - test/unit/shell-combat-screen.test.js
  - test/unit/shell-map-viewport.test.js
  - test/unit/shell-loot-screen.test.js
  - test/unit/shell-combat-over.test.js
autonomous: true
requirements: [MAP-07, PARTY-07, MAP-03, MAP-05, CSCR-06]
tags: [vanilla-js, uat-fix, perf, compositing, party-roster, hero-tab, png-icons, rail, major-overlay, source-assertion-tests]

must_haves:
  truths:
    - "The party ring (#mw-party-pulse) still pulses on the map, but its @keyframes animate ONLY opacity and transform (the ring/halo is a static box-shadow on the element), so Chromium composites the pulse on its own layer instead of re-rasterising the map layer every frame; AND the ring is paused + hidden (.covered: animation-play-state paused, visibility hidden) whenever the encounter panel #enc-panel covers the map — toggled on the line after each of renderEncounter's two panel.hidden writes — so FIGHT IT OUT / GO DOWN / NOT YET paint their gold background immediately instead of after a focus change; the blanket prefers-reduced-motion rule still stops it; positionPartyPulse() is byte-identical"
    - "No party roster renders in the map column or above the combat screen: the Phase 10 party strip element is GONE from the DOM; the roster (one card per S.party member: name, sub·level, HP bar, Downed chip) lives in a Company panel on the Hero tab between the Character sheet and Special skills, hidden when the party is empty, refreshed on the paint() path; in combat the only joiner surface is the existing YOUR LOT strip"
    - "Looting with a full bag shows the bag-full refusal ONLY in the rail card: the loot screen no longer prints the inline red paragraph under the loot list; the `lootHead · bag n/m` header line and the drop shelf (still rendered when usage.full && needsSlot) are unchanged, so the player can still drop to make room"
    - "Every feature-event rail card (trap / teleport / one-way door / locked box / climb-crevice / floor) shows the tile's actual PNG icon (icons/optimized/<key>.png) in place of the text glyph; the hold-to-inspect card shows the seen feature's PNG; the encounter and stair MAJOR OVERLAYS show encounter.png / descent.png at 60px; specs without an iconKey (death, joiner/find decision cards, idle, fallbacks) keep the glyph path unchanged"
    - "npm test is green (fail 0, pass >= 2172), build:www exits 0, the engine/content/parity tree is untouched, the parity golden-master hash is a1f4d0dc29782218d8e5aab65bc5989c33f917f0, and a fresh debug APK exists"
  artifacts:
    - path: "mazeworld.html"
      provides: "composited .mw-party-pulse CSS + mwglow keyframes; #hero-party Company panel + renderPartyRoster(); .mw-party-list CSS; loot branch fillMid without the inline bag-full paragraph (shelf kept); .mw-rail-icon img / .mw-major-icon-img CSS; featureIconSrc() helper; iconKey plumbing in renderRail/inspectAt/mzRailLine/renderMajorOverlay; stair spec iconKey"
    - path: "src/browser/rail.js"
      provides: "frozen RAIL_FEATURE_ICON map; railCardFor/railLineCard emit iconKey (null when none)"
    - path: "src/browser/combatPanel.js"
      provides: "encounterOverlaySpec carries iconKey: \"encounter\""
    - path: "test/unit/shell-map-hud.test.js"
      provides: "re-pinned (b) layout (no party strip between the condition strip and <main>); new composited-pulse test"
    - path: "test/unit/shell-party-camp.test.js"
      provides: "new test: the roster lives in #hero-party on the Hero tab, rendered by renderPartyRoster on the paint() path"
    - path: "test/unit/shell-map-invariants.test.js"
      provides: "new retirement sweep: zero occurrences of the retired party-strip id / render function name"
    - path: "test/unit/rail.test.js"
      provides: "re-pinned railLineCard shape (iconKey); new RAIL_FEATURE_ICON invariants + railCardFor iconKey pins"
    - path: "test/unit/combatPanel.test.js"
      provides: "re-pinned encounterOverlaySpec deepEqual with iconKey"
    - path: "test/unit/shell-map-rail.test.js"
      provides: "new test: renderRail renders an <img> for iconKey cards via createElement (no innerHTML), glyph fallback otherwise"
    - path: "test/unit/shell-combat-screen.test.js"
      provides: "re-pinned CSCR-06: renderMajorOverlay's iconKey <img> branch + glyph fallback"
    - path: "test/unit/shell-map-viewport.test.js"
      provides: "re-pinned (e) inspectAt mzRailLine call (trailing mark.key arg) and (g) stair spec iconKey"
    - path: "test/unit/shell-loot-screen.test.js"
      provides: "re-pinned loot-screen test: the inline bag-full paragraph is absent from the loot branch; the shelf and header remain"
    - path: "test/unit/shell-combat-over.test.js"
      provides: "re-pinned CSCR-07 loot-branch literals: bag-full paragraph removed from the required list and asserted absent; shelf/querySelector pins intact"
  key_links:
    - from: ".mw-party-pulse (DOM ring, positioned by positionPartyPulse)"
      to: "@keyframes mwglow"
      via: "animation:mwglow 1.6s ease-in-out infinite; keyframes touch only opacity + transform"
      pattern: "@keyframes mwglow\\{0%,100%\\{opacity:"
    - from: "paint()"
      to: "#hero-party / #hero-party-list (Hero tab)"
      via: "renderPartyRoster(); toggles #hero-party.hidden = party.length === 0 and rebuilds one .mw-party-member card per S.party entry"
      pattern: "^function renderPartyRoster\\(\\)"
    - from: "railCardFor / railLineCard (src/browser/rail.js)"
      to: "renderRail() #mw-rail-icon and renderMajorOverlay() .mw-major-icon"
      via: "card.iconKey / spec.iconKey -> featureIconSrc(key) -> <img src=\"./icons/optimized/<key>.png\"> built with document.createElement (never innerHTML)"
      pattern: "featureIconSrc\\("
    - from: "inspectAt()"
      to: "window.mzRailLine(title, line, tone, hold, icon, iconKey)"
      via: "mark ? mark.key : null as the new optional trailing argument (mapMarks.markForCell returns {key, glyph, color})"
      pattern: "mark \\? mark\\.key : null"
  prohibitions:
    - "No edits under engine/, content/, test/parity/ (ENGINE GATE); test/parity/prototype-master.js.txt hash must stay a1f4d0dc29782218d8e5aab65bc5989c33f917f0"
    - "No edits to src/browser/icons.js, src/browser/mapMarks.js, icons/optimized/*.png — read-only consumers only"
    - "No call from initTabs/showTab into any function that reads S (commit 7ab68d3 boot-crash rule): renderPartyRoster() is called ONLY from paint(), never from tab init"
    - "No presentation state on S — every new value is a card/spec field or a window.__mz* bridge value"
    - "No adb commands — the orchestrator installs the APK"
    - "Do NOT revert or rework the two fast fixes already on master (49e01fc renderCombatOver/loot host via wrap.querySelector — never reintroduce document.getElementById in the loot branch; 67192d5 WP -> HP in the HUD readout + rules note)"
    - "Do NOT touch the store branch's own bag-full line (`Bag full (...) — sell or drop something to make room.` inside `if (S.store) {`) or lootCompare.js / toasts.js bagFull copy — only the LOOT branch's inline paragraph is removed"
---

<objective>
Land the four items from the user's second Pixel 7 UAT round on v1.4 (2026-09-17) — all shell + `src/browser` + tests, zero engine/content/parity edits:

1. **PERF — composited party pulse, paused under overlays.** `.mw-party-pulse` animates `box-shadow`, which Chromium cannot composite, so the whole map layer re-rasterises every frame (logcat `tile memory limits exceeded, some content may not draw` ~4x/s). Rework the ring so only `opacity` and `transform` animate. Addendum (user, 2026-09-17 UAT), verbatim: "FIGHT IT OUT button doesn't have a button colour unless the window loses focus and I come back — black text on nothing, hard to read." `.mw-major-btn` is static CSS (gold bg, black text, no focus/animation dependence), so that is the same tile-memory starvation: the ring kept animating underneath the overlay and the WebView skipped drawing the button's background. Fix: pause + hide the ring (a `.covered` class: `animation-play-state:paused;visibility:hidden`) whenever `#enc-panel` is shown — any overlay (major overlay, combat screen, over-panel) — and resume it when the panel hides, on the line after each of `renderEncounter`'s two `panel.hidden` writes.
2. **USER RULING — the joiner leaves the map (and the combat screen).** Record verbatim: "Joiner should only show when fighting, and probably on the hero screen." Addendum (user, 2026-09-17), verbatim: "Joiner section on top of the combat window should go, too. We already have our joiner in the Your Lot section." The Phase 10 party strip is a sibling of `<main>` (outside every tab screen), so it renders above the combat screen as well as the map. It is REMOVED from the DOM entirely; the roster moves to a Company panel on the Hero tab. After this task the ONLY joiner surfaces are (a) the combat screen's YOUR LOT strip (unchanged) and (b) the Hero tab's Company panel.
3. **USER RULING — loot with a full bag says so once, in the rail.** Record verbatim: "When I try to loot something and my bag is full, the message shows up right under the item that I tried to stow, and it shows up in the card at the bottom. We don't need that message to show up right under the gear piece. The card is sufficient." The loot screen's inline red bag-full paragraph goes; the rail's `bagFull` refusal card (src/browser/toasts.js) already carries the message. The drop shelf and the `lootHead · bag n/m` header stay.
4. **USER RULING — feature cards show the actual PNG icon.** Record verbatim: "On the screens that show up to describe what happens when you land on an icon, we should use the actual icon." Rail cards for tile events, the hold-inspect card, and the encounter/stair MAJOR OVERLAYS render `icons/optimized/<key>.png`; everything without a tile identity keeps its glyph.

Purpose: (1) removes a measurable frame-time regression on-device; (2), (3) and (4) are direct user rulings from the UAT batch.
Output: `mazeworld.html`, `src/browser/rail.js`, `src/browser/combatPanel.js` + ten re-pinned/extended test files; a fresh debug APK for the orchestrator to install.

Line numbers below were taken before two fast fixes landed on master (49e01fc: renderCombatOver attaches `mid` before `fillMid` and the loot branch resolves its hosts via `wrap.querySelector`; 67192d5: WP -> HP in the HUD readout + rules note) — treat every `~L` as approximate and RE-DERIVE each location from the current file by grepping the quoted anchor text before editing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260916-w0c-v1-4-uat-fixes-tap-to-move-bridge-timing/260916-w0c-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PERF — composite the party pulse (static box-shadow ring, animate opacity + transform only) AND hide it while #enc-panel covers the map (FIGHT IT OUT paints its gold background immediately); re-pin shell-map-hud.test.js</name>
  <files>mazeworld.html, test/unit/shell-map-hud.test.js</files>
  <read_first>
    - mazeworld.html: `function renderEncounter() {` (~L5630-5662; grep the signature) — `const panel = document.getElementById("enc-panel");` on its first line, then the `encWasActive`/`active` transition block, then the two show/hide lines this task rides: `if (!active) { if (panel) panel.hidden = true; syncFightLogLive(null); return; }` and, directly after, `if (panel) panel.hidden = false;` — NOTE: the `host.hidden` lines at ~L2971/~L3028 belong to `paintConditions()` (the condition-chip strip), NOT the encounter panel; do not touch them
    - mazeworld.html ~L2581 (`positionPartyPulse` is the only other `getElementById("mw-party-pulse")` reader in the file, and it only sets geometry — so a paused, visibility-hidden ring is still sized/positioned while covered and reappears in place)
    - mazeworld.html ~L1412 (`<section class="mw-overlay" id="enc-panel" hidden>` — every overlay (major overlay, combat screen, over-panel, store) renders inside it, so one toggle covers all of them)
    - mazeworld.html ~L332-337 (the four-line comment "Phase 35 (MAP-07): the pulsing gold ring ..." then the two CSS lines `.mw-party-pulse{position:absolute;border-radius:50%;pointer-events:none;z-index:2;animation:mwglow 1.6s ease-in-out infinite}` and `@keyframes mwglow{0%,100%{...}50%{...}}` — the ONLY lines this task rewrites)
    - mazeworld.html ~L814 (`@media (prefers-reduced-motion:reduce){*{transition:none!important;animation:none!important}}` — the blanket rule that stops the ring; do NOT edit)
    - mazeworld.html ~L2575-2586 (`function positionPartyPulse(rect)` — sets width/height = CELL and left/top from the viewport centre + pan; do NOT edit: a `transform:scale()` in the keyframes scales around the element's own centre, so the CELL-sized box it positions is unchanged)
    - mazeworld.html ~L1396-1398 (the `<div class="mw-party-pulse" id="mw-party-pulse" aria-hidden="true"></div>` markup — unchanged)
    - test/unit/shell-map-hud.test.js lines 83-90 (`styleBlock()` helper), 270-296 (test (h) — pins `id="mw-party-pulse" aria-hidden="true"` and `@keyframes mwglow` count 1; keep both), 334-338 (test (i) positionPartyPulse pin — must still pass)
  </read_first>
  <action>
    <!-- planner-discipline-allow: box-shadow -->
    <!-- planner-discipline-allow: positionPartyPulse -->
    1. Replace the `.mw-party-pulse{...}` line with: `.mw-party-pulse{position:absolute;border-radius:50%;pointer-events:none;z-index:2;box-shadow:0 0 0 3px #14110c,0 0 14px 3px rgba(232,201,122,.5);will-change:transform,opacity;animation:mwglow 1.6s ease-in-out infinite}` — the ring + halo are now a STATIC shadow on the element.
    2. Replace the `@keyframes mwglow{...}` line with: `@keyframes mwglow{0%,100%{opacity:.55;transform:scale(1)}50%{opacity:1;transform:scale(1.18)}}` — only compositor-friendly properties change per frame.
    3. Rewrite the comment block directly above (keep it a CSS block comment, 4-6 lines): "Phase 35 (MAP-07): the pulsing gold ring over the party's canvas cell — a DOM element (not a canvas rAF loop), so the blanket prefers-reduced-motion rule (~L814) zeroes its animation for reduced-motion users (the ring then sits static at full opacity). 2026-09-17 UAT PERF: the ring/halo shadow is static and the keyframes animate only opacity + transform — shadow animation is not GPU-composited and forced Chromium to re-rasterise the whole map layer every frame (logcat tile-memory spam); will-change promotes the ring to its own layer." Do not spell the shadow property name inside the keyframes line itself (the new test negative-greps the keyframes block).
    4. `positionPartyPulse()`, the `#mw-party-pulse` markup, the reduced-motion rule and draw()'s canvas glow are untouched.
    5. **Pause + hide the ring under any overlay (addendum).** Record verbatim in the SUMMARY: "FIGHT IT OUT button doesn't have a button colour unless the window loses focus and I come back — black text on nothing, hard to read." CSS: directly after the `.mw-party-pulse{...}` rule add `.mw-party-pulse.covered{animation-play-state:paused;visibility:hidden}`. In `renderEncounter()`, on the line directly AFTER `if (panel) panel.hidden = true;` (inside the `if (!active) {` block, before `syncFightLogLive(null);`) add `document.getElementById("mw-party-pulse")?.classList.remove("covered");` with a two-line comment ("2026-09-17 UAT PERF/addendum: the panel no longer covers the map — resume/reveal the party ring so it composites again."); on the line directly AFTER `if (panel) panel.hidden = false;` add `document.getElementById("mw-party-pulse")?.classList.add("covered");` with a comment ("2026-09-17 UAT PERF/addendum: the panel just covered the map — pause and hide the party ring underneath it (Chromium tile-memory starvation: an animated element under an opaque overlay can fail to paint until a focus change without this)."). Every overlay (major overlay, combat screen, over-panel, store) renders inside `#enc-panel`, so these two lines cover all of them; `positionPartyPulse()` keeps sizing the ring meanwhile and it reappears in place when the panel closes. No other change to renderEncounter; nothing else in the file toggles the ring.
    6. Re-pin `test/unit/shell-map-hud.test.js`: add TWO tests directly after (h). First — `(h) PERF 2026-09-17: the party pulse animates only composited properties (static ring shadow, opacity/transform keyframes, will-change)` — asserting: `assert.match(HTML, /^\.mw-party-pulse\{[^}]*box-shadow:0 0 0 3px #14110c,0 0 14px 3px rgba\(232,201,122,\.5\)[^}]*will-change:transform,opacity[^}]*animation:mwglow 1\.6s ease-in-out infinite\}$/m)`; extract the keyframes block with `const kf = HTML.match(/^@keyframes mwglow\{.*\}\}$/m)[0]` and assert `assert.doesNotMatch(kf, /box-shadow/)`, `assert.match(kf, /0%,100%\{opacity:\.55;transform:scale\(1\)\}/)`, `assert.match(kf, /50%\{opacity:1;transform:scale\(1\.18\)\}/)`; `assert.match(HTML, /^@media \(prefers-reduced-motion:reduce\)\{\*\{transition:none!important;animation:none!important\}\}$/m)` (the stop rule survives). Second — `(h) PERF addendum 2026-09-17: the party pulse is paused/hidden while the encounter panel covers the map (Chromium tile-memory starvation fix)` — asserting `assert.match(HTML, /^\.mw-party-pulse\.covered\{animation-play-state:paused;visibility:hidden\}$/m)`, and on the renderEncounter region (`CODE` from `function renderEncounter() {` to the next `\nfunction `): `assert.match(region, /if \(panel\) panel\.hidden = true;\s*\n\s*document\.getElementById\("mw-party-pulse"\)\?\.classList\.remove\("covered"\);/)` and `assert.match(region, /if \(panel\) panel\.hidden = false;\s*\n\s*document\.getElementById\("mw-party-pulse"\)\?\.classList\.add\("covered"\);/)` (comment lines are stripped from CODE, so the toggles are adjacent). Update the file's header comment to mention the composited, overlay-paused pulse.
  </action>
  <verify>
    <automated>test "$(grep -c '^@keyframes mwglow{' mazeworld.html)" = "1" && test "$(grep -c 'will-change:transform,opacity' mazeworld.html)" = "1" && test "$(grep -c '^\.mw-party-pulse\.covered{animation-play-state:paused;visibility:hidden}$' mazeworld.html)" = "1" && test "$(grep -c 'getElementById("mw-party-pulse")?.classList.remove("covered");' mazeworld.html)" = "1" && test "$(grep -c 'getElementById("mw-party-pulse")?.classList.add("covered");' mazeworld.html)" = "1" && node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const m=s.match(/^@keyframes mwglow\{.*\}\}$/m);process.exit(m&&!/box-shadow/.test(m[0])&&/opacity:\.55;transform:scale\(1\)/.test(m[0])&&/opacity:1;transform:scale\(1\.18\)/.test(m[0])?0:1)" && node --test test/unit/shell-map-hud.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^@keyframes mwglow{' mazeworld.html` = 1 and `grep -c '^\.mw-party-pulse{' mazeworld.html` = 1.
    - Ring paused/hidden under overlays, both directions, each on the line after its panel toggle: `grep -c '^\.mw-party-pulse\.covered{animation-play-state:paused;visibility:hidden}$' mazeworld.html` = 1; `grep -c 'getElementById("mw-party-pulse")?.classList.remove("covered");' mazeworld.html` = 1; `grep -c 'getElementById("mw-party-pulse")?.classList.add("covered");' mazeworld.html` = 1; `grep -c 'getElementById("mw-party-pulse")' mazeworld.html` = 3 (positionPartyPulse + the two renderEncounter toggles — nothing else touches the ring); `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8').replace(/\/\/[^\n]*/g,'');const a=s.indexOf('function renderEncounter() {');const b=s.indexOf('\nfunction ',a+1);const r=s.slice(a,b);process.exit(/if \(panel\) panel\.hidden = true;\s*\n\s*document\.getElementById\(\"mw-party-pulse\"\)\?\.classList\.remove\(\"covered\"\);/.test(r)&&/if \(panel\) panel\.hidden = false;\s*\n\s*document\.getElementById\(\"mw-party-pulse\"\)\?\.classList\.add\(\"covered\"\);/.test(r)?0:1)"` exits 0 (both toggles live inside renderEncounter, adjacent to the panel writes once comments are stripped).
    - `grep -c 'host.hidden = true;' mazeworld.html` and `grep -c 'host.hidden = false;' mazeworld.html` are unchanged from HEAD (`git diff -U0 -- mazeworld.html | grep -c 'host.hidden'` = 0 — paintConditions untouched).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const m=s.match(/^@keyframes mwglow\{.*\}\}$/m);process.exit(m&&!/box-shadow/.test(m[0])&&/0%,100%\{opacity:\.55;transform:scale\(1\)\}/.test(m[0])&&/50%\{opacity:1;transform:scale\(1\.18\)\}/.test(m[0])?0:1)"` exits 0 (the keyframes block animates only opacity/transform).
    - `grep -c 'box-shadow:0 0 0 3px #14110c,0 0 14px 3px rgba(232,201,122,.5);will-change:transform,opacity;animation:mwglow 1.6s ease-in-out infinite}' mazeworld.html` = 1 (static ring on the element).
    - `grep -c '^@media (prefers-reduced-motion:reduce){\*{transition:none!important;animation:none!important}}$' mazeworld.html` = 1 (stop rule unchanged); `grep -c '^function positionPartyPulse(rect)' mazeworld.html` = 1 and `git diff -U0 -- mazeworld.html | grep -c 'positionPartyPulse'` = 0 (function untouched).
    - `node --test test/unit/shell-map-hud.test.js` exits 0 with 21 tests (19 + the composited-pulse test + the covered-under-overlay test).
  </acceptance_criteria>
  <done>The party ring still pulses (opacity .55->1, scale 1->1.18) around a static shadow ring on its own compositor layer, is paused + hidden (.covered) whenever #enc-panel covers the map and reappears in place when it closes; reduced motion stops it; the HUD suite pins the composited keyframes and the two panel-adjacent toggles.</done>
</task>

<task type="auto">
  <name>Task 2: USER RULING — remove the party strip from the map column (it also sat above the combat screen); add the Company panel to the Hero tab rendered by renderPartyRoster(); re-pin shell-map-hud (b), shell-party-camp, shell-map-invariants</name>
  <files>mazeworld.html, test/unit/shell-map-hud.test.js, test/unit/shell-party-camp.test.js, test/unit/shell-map-invariants.test.js</files>
  <read_first>
    - mazeworld.html ~L1353-1359 (the condition strip `<div class="mw-cond-strip" id="mm-conditions" hidden></div>` followed by the six-line "Phase 10 (Party UI, PARTY-07): the persistent PARTY RAIL ..." comment and the party-strip `<div ... hidden></div>` — the comment + div are DELETED; the condition strip stays; note this sits OUTSIDE `<main class="mw-screens">`, which is why it also showed over the combat screen)
    - mazeworld.html ~L1436-1463 (`<section class="mw-screen" id="screen-hero" ...>`: the Character-sheet `<section class="panel">` closes right after `<p class="trait" id="s-trait">—</p>`; the next panel opens with `<h2>Special skills <span id="s-vp"></span></h2>` — the Company panel goes between them)
    - mazeworld.html ~L840-846 (the `.mw-hud-top` comment mentioning "the relocated .mw-party-rail below" — reword) and ~L884-902 (the Phase 10 CSS comment, the two strip rules `.mw-party-rail{...}` / `.mw-party-rail[hidden]{display:none}`, then the SEVEN card rules `.mw-party-member{...}` through `.mw-party-status{...}` that the Hero panel keeps verbatim)
    - mazeworld.html ~L3240-3252 (tail of `paint()`: `window.__mzRenderGrimoire?.();` then `renderPartyRail();` `renderEncounter();` `renderRail();` `draw();` — only the roster call line changes)
    - mazeworld.html ~L5125-5165 (the Phase 10 comment + `function renderPartyRail() {` body: reads `S.party`, builds `.mw-party-member` cards with `ROMAN[lvl - 1]`, `clamp`, `.mw-map-hptrack/.mw-map-hpfill${pct < 34 ? " low" : ""}` and the `fchip fchip-bad mw-party-status` Downed chip — the card template is reused as-is)
    - mazeworld.html ~L7530-7536 (the `mzResolveJoiner` comment line "repaint — paint() re-runs renderPartyRail() (the rail appears on accept)" — reword)
    - mazeworld.html ~L1660-1700 (`initTabs`/`showTab` — READ ONLY: do not add any roster call here; commit 7ab68d3 made showTab guard `window.renderRail?.()` on `window.__mzState` because `S` is in its TDZ at tab-init)
    - test/unit/shell-map-hud.test.js lines 129-138 (test (b) — pins `id="mw-party-rail"` between the condition strip and `<main>`; re-pin)
    - test/unit/shell-party-camp.test.js lines 30-45 (helpers: `stripComments`, `CODE`) and 62-75 (the Joiner rail-card test — pattern to mirror for the new test)
    - test/unit/shell-map-invariants.test.js lines 127-141 (the "retirements: RAW carries zero ..." tests — mirror their string-concatenation discipline so the test file never spells a retired literal whole) and 246-262 (the initialiser count table — no change needed)
  </read_first>
  <action>
    <!-- planner-discipline-allow: mw-party-rail -->
    <!-- planner-discipline-allow: id="mw-party-rail" -->
    <!-- planner-discipline-allow: renderPartyRail -->
    Record BOTH rulings verbatim in the SUMMARY: "Joiner should only show when fighting, and probably on the hero screen." and the addendum "Joiner section on top of the combat window should go, too. We already have our joiner in the Your Lot section."
    1. **Delete the map-column strip.** Remove the six-line Phase 10 comment and the `<div class="mw-party-rail" id="mw-party-rail" hidden></div>` element (~L1353-1359) outright — not hidden, GONE. The condition strip directly above it is untouched. Nothing party-related remains between `</header>` and `<main>`.
    2. **Company panel on the Hero tab.** Inside `#screen-hero`, after the Character-sheet panel's closing `</section>` (the one following `<p class="trait" id="s-trait">—</p>`) and BEFORE the `<section class="panel">` whose heading is `Special skills`, insert exactly:
       `<!-- 2026-09-17 UAT (user ruling, verbatim): "Joiner should only show when fighting, and probably on the hero screen." + addendum "Joiner section on top of the combat window should go, too. We already have our joiner in the Your Lot section." The Phase 10 roster strip left the map column (it was a sibling of <main>, so it also sat above the combat screen). The company now lives here, rendered by renderPartyRoster() on the paint() path; in combat the only joiner surface is the YOUR LOT strip. -->`
       `<section class="panel" id="hero-party" hidden>` / `<h2>Company</h2>` / `<div class="mw-party-list" id="hero-party-list"></div>` / `</section>`.
    3. **Rename + retarget the renderer.** Rename `function renderPartyRail()` to `function renderPartyRoster()`. Body: `const panel = document.getElementById("hero-party"); const host = document.getElementById("hero-party-list"); if (!panel || !host) return; const party = (S && Array.isArray(S.party)) ? S.party : []; panel.hidden = party.length === 0; host.innerHTML = ""; if (!party.length) return;` then the existing `party.forEach((m) => { ... })` card builder unchanged except `host.appendChild(card)` in place of the old strip append. The function reads ONLY the two `hero-party*` ids — no other `getElementById` inside it. Rewrite the comment above it: "2026-09-17 UAT (user ruling): the party roster is a Hero-tab panel (#hero-party / #hero-party-list), no longer a map-column strip. Reads the engine's own persistent roster (S.party) and renders one card per member: name, subclass·level, an HP bar (reusing .mw-map-hptrack/.mw-map-hpfill) and a Downed chip. Data-driven: the panel is hidden when solo. Called from paint() — never from tab init (S is in its TDZ there) — so member HP stays live through combat." New comments must not spell the retired strip id or the retired function name.
    4. **paint().** Change the call line to `  renderPartyRoster();` (same position, before `renderEncounter();`). Reword the `mzResolveJoiner` comment line to "repaint — paint() re-runs renderPartyRoster() (the Company panel appears on accept)".
    5. **CSS.** Replace the two strip rules `.mw-party-rail{...}` and `.mw-party-rail[hidden]{display:none}` with ONE rule `.mw-party-list{display:flex;flex-wrap:wrap;gap:7px}`. Keep the seven `.mw-party-member` … `.mw-party-status` rules byte-identical (they already use the parchment vars `--rule/--paper-2/--ink`, right for the Hero tab). Rewrite the Phase 10 CSS comment above them to describe the Hero-tab Company panel (`#hero-party > .mw-party-list`, rendered by renderPartyRoster on the paint() path) and note the 2026-09-17 ruling. In the `.mw-hud-top` comment (~L840-846) replace "(see .mw-cond-strip and the relocated .mw-party-rail below)" with "(see .mw-cond-strip; the party roster moved to the Hero tab's Company panel, 2026-09-17 UAT)". After this step the retired class/id string must not appear anywhere in the file (markup, CSS, JS, or comments).
    6. **Re-pin tests.**
       - `test/unit/shell-map-hud.test.js` (b): rename to `(b) layout: mw-cond-strip sits directly after </header> and before <main>; the retired party strip is gone from the map column (2026-09-17 UAT)`; keep the `</header>` < condition strip < `<main>` ordering and add `assert.equal((HTML.match(new RegExp('id="' + 'mw-party-' + 'rail"', "g")) || []).length, 0)` (concatenated so the test never spells the retired id whole).
       - `test/unit/shell-party-camp.test.js`: append `2026-09-17 UAT ruling: the party roster is a Hero-tab Company panel rendered by renderPartyRoster() on the paint() path (never from tab init)` asserting: `id="hero-party"` and `id="hero-party-list"` each exactly once in HTML; inside the `#screen-hero` region (`HTML.slice(HTML.indexOf('id="screen-hero"'), HTML.indexOf('id="screen-gear"'))`) the index order is `id="s-trait"` < `id="hero-party"` < `<h2>Company</h2>` < `Special skills`; the ruling sentence `Joiner should only show when fighting, and probably on the hero screen.` appears in HTML; `CODE` matches `/^function renderPartyRoster\(\) \{/m` exactly once; the function region (slice from `function renderPartyRoster() {` to the next `\n}\n`) matches `getElementById\("hero-party"\)`, `getElementById\("hero-party-list"\)`, `panel\.hidden = party\.length === 0;`, `mw-map-hptrack`, `mw-map-hpfill`, `Downed`, and has exactly 2 `getElementById(` occurrences; the paint() region (`sliceBetween(CODE, "function paint() {", "\n/* ---------------- movement")`, or an equivalent local slicer) contains `renderPartyRoster();` exactly once; the `initTabs` region (from `(function initTabs() {` to `window.__mzShowTab = showTab;`) does NOT match `renderPartyRoster`.
       - `test/unit/shell-map-invariants.test.js`: append to the retirements group `retirements (2026-09-17 UAT): RAW carries zero of the retired party-strip literals; .mw-party-list{ appears once` — build the two literals by concatenation (`"mw-party-" + "rail"` and `"renderParty" + "Rail"`), assert `countOf(RAW, literal) === 0` for each, and `assert.equal((RAW.match(/^\.mw-party-list\{/gm) || []).length, 1)`.
  </action>
  <verify>
    <automated>test "$(grep -c 'mw-party-rail' mazeworld.html)" = "0" && test "$(grep -c 'renderPartyRail' mazeworld.html)" = "0" && test "$(grep -c 'id="hero-party"' mazeworld.html)" = "1" && test "$(grep -c 'id="hero-party-list"' mazeworld.html)" = "1" && test "$(grep -c '^function renderPartyRoster()' mazeworld.html)" = "1" && test "$(grep -c '^  renderPartyRoster();' mazeworld.html)" = "1" && node --test test/unit/shell-map-hud.test.js test/unit/shell-party-camp.test.js test/unit/shell-map-invariants.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="mw-party-rail"' mazeworld.html` = 0 (addendum: the element is removed, not hidden); `grep -c 'mw-party-rail' mazeworld.html` = 0; `grep -c 'renderPartyRail' mazeworld.html` = 0.
    - `grep -c 'id="hero-party"' mazeworld.html` = 1; `grep -c 'id="hero-party-list"' mazeworld.html` = 1; `grep -c '<h2>Company</h2>' mazeworld.html` = 1; `grep -c '^\.mw-party-list{display:flex;flex-wrap:wrap;gap:7px}$' mazeworld.html` = 1; `grep -c '^\.mw-party-member{' mazeworld.html` = 1 (card CSS kept).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const h=s.slice(s.indexOf('id=\"screen-hero\"'),s.indexOf('id=\"screen-gear\"'));const a=h.indexOf('id=\"s-trait\"'),b=h.indexOf('id=\"hero-party\"'),c=h.indexOf('<h2>Company</h2>'),d=h.indexOf('Special skills');process.exit(a>-1&&a<b&&b<c&&c<d?0:1)"` exits 0 (Company panel between the Character sheet and Special skills).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function renderPartyRoster() {');const b=s.indexOf('\n}\n',a);const r=s.slice(a,b);const ids=r.match(/getElementById\(\"([^\"]+)\"\)/g)||[];process.exit(ids.length===2&&ids.every(x=>/hero-party/.test(x))&&/panel\.hidden = party\.length === 0;/.test(r)&&/mw-map-hptrack/.test(r)&&/Downed/.test(r)?0:1)"` exits 0 (the roster function references only `#hero-party`/`#hero-party-list`).
    - `grep -c '^function renderPartyRoster()' mazeworld.html` = 1; `grep -c '^  renderPartyRoster();' mazeworld.html` = 1 (the paint() call site); `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('(function initTabs() {');const b=s.indexOf('window.__mzShowTab = showTab;',a);process.exit(s.slice(a,b).includes('renderPartyRoster')?1:0)"` exits 0 (never called from tab init).
    - `grep -c 'Joiner should only show when fighting, and probably on the hero screen.' mazeworld.html` = 1 and `grep -c 'We already have our joiner in the Your Lot section.' mazeworld.html` = 1 (rulings recorded in the source comment).
    - `grep -c 'id="mm-conditions"' mazeworld.html` = 1 and `grep -c 'renderYourLot' mazeworld.html` >= 2 (condition strip and YOUR LOT untouched).
    - `node --test test/unit/shell-map-hud.test.js test/unit/shell-party-camp.test.js test/unit/shell-map-invariants.test.js` exits 0 (hud 20, party-camp 5, invariants 38 tests).
  </acceptance_criteria>
  <done>Nothing party-related renders in the map column or over the combat screen; the Hero tab shows a Company panel (one card per member, hidden when solo) refreshed by paint(); YOUR LOT is the only in-combat joiner surface; the three suites pin the move and the retirement.</done>
</task>

<task type="auto">
  <name>Task 3: USER RULING — loot with a full bag: drop the inline red bag-full paragraph from the loot screen (the rail card is sufficient), keep the drop shelf + header; re-pin shell-loot-screen + shell-combat-over</name>
  <files>mazeworld.html, test/unit/shell-loot-screen.test.js, test/unit/shell-combat-over.test.js</files>
  <read_first>
    - mazeworld.html: the loot branch of `renderEncounter` — grep `if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {` (~L5766) through the `return;` before `if (S.store) {` (~L5807). Inside `fillMid: (mid) => {` the `wrap.innerHTML = \`...\`` template is: the header `<p class="cb-sub">${COMBAT_COPY.lootHead} · bag ${usage.text || "unlimited"}</p>`, the `<ul class="skills cb-loot" id="loot-list"></ul>`, then `+ (usage.full && needsSlot ? \`<p class="cb-sub" style="color:var(--rust)">Bag full (${usage.have}/${usage.slots}). Drop something to make room, or leave it.</p>\n<div class="shelf" id="loot-drop-shelf"></div>\` : "")`. Below it: `mid.appendChild(wrap);`, `renderCarriedList(wrap.querySelector("#loot-list"), ...)`, `const dropShelf = wrap.querySelector("#loot-drop-shelf"); if (dropShelf) renderDropShelf(dropShelf, c.items || []);` — the 49e01fc shape; keep it exactly
    - mazeworld.html: `function renderCombatOver(host, kind, opts = {})` (~L5558-5600) — READ ONLY: 49e01fc attaches `mid` (`host.appendChild(mid);`) BEFORE `opts.fillMid(mid)`; do not touch
    - mazeworld.html: the store branch `if (S.store) {` (~L5809-5824) — READ ONLY: its own `Bag full (...) — sell or drop something to make room.` line is a different screen and stays
    - src/browser/toasts.js line 1366 (`bagFull: (e) => block(... \`Bag full (${e.have}/${e.slots}) — drop something to make room.\` ...)` — the rail refusal card that already carries the message; READ ONLY)
    - test/unit/shell-loot-screen.test.js lines 170-183 (`LOOT_GUARD`/`STORE_GUARD`/`lootRegion()`) and 185-207 (the Phase 29 loot-screen test — line 205 `assert.match(region, /Bag full \(/)` flips)
    - test/unit/shell-combat-over.test.js lines 80-86 (`lootBranch()`) and 180-219 (the CSCR-07 loot-branch test — `"Bag full ("` sits in its required-literals array at ~L208; remove it there and assert absence instead)
  </read_first>
  <action>
    <!-- planner-discipline-allow: Bag full ( -->
    Record verbatim in the SUMMARY: "When I try to loot something and my bag is full, the message shows up right under the item that I tried to stow, and it shows up in the card at the bottom. We don't need that message to show up right under the gear piece. The card is sufficient."
    1. In the loot branch's `fillMid`, change the conditional tail of the `wrap.innerHTML` template so that when `usage.full && needsSlot` it appends ONLY `<div class="shelf" id="loot-drop-shelf"></div>` — delete the red `<p class="cb-sub" style="color:var(--rust)">...</p>` line that preceded the shelf. The header `<p class="cb-sub">${COMBAT_COPY.lootHead} · bag ${usage.text || "unlimited"}</p>`, the `<ul ... id="loot-list">`, `mid.appendChild(wrap)`, the two `wrap.querySelector(...)` host lookups, `renderCarriedList(...)`, `renderDropShelf(...)`, the buttons array and `needsSlot` are all unchanged. Do not reintroduce `document.getElementById` anywhere in the branch. Add a one-line comment above the template: "2026-09-17 UAT (user ruling): a full bag is reported once, by the rail's bagFull refusal card — no inline line under the loot list; the drop shelf still appears so the player can make room." (Do not quote the removed paragraph's text in the comment.)
    2. `test/unit/shell-loot-screen.test.js`: in the Phase 29 loot-screen test replace `assert.match(region, /Bag full \(/)` with `assert.doesNotMatch(region, /Bag full \(/)` and add `assert.match(region, /usage\.full && needsSlot/)`, `assert.match(region, /COMBAT_COPY\.lootHead/)`, `assert.doesNotMatch(region, /color:var\(--rust\)/)` (the loot branch carries no inline red line); keep the `id="loot-drop-shelf"` and `renderDropShelf\(` pins. Rename the test to mention "2026-09-17 UAT: no inline bag-full line, the shelf remains".
    3. `test/unit/shell-combat-over.test.js`: in the CSCR-07 loot-branch test remove `"Bag full ("` from the `literals` array and add, after the loop, `assert.doesNotMatch(region, /Bag full \(/, "2026-09-17 UAT: the bag-full refusal lives only in the rail card")` and `assert.ok(region.indexOf('id="loot-drop-shelf"') !== -1 && region.indexOf("usage.full && needsSlot") !== -1, "the drop shelf still renders when the bag is full and a slot is needed")`; the existing `wrap.querySelector("#loot-drop-shelf")` / no-`document.getElementById("loot-list")` pins stay.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {');const b=s.indexOf('if (S.store) {',a);const r=s.slice(a,b);process.exit(a>-1&&b>a&&!/Bag full \(/.test(r)&&!/document\.getElementById/.test(r)&&r.includes('id=\"loot-drop-shelf\"')&&r.includes('usage.full && needsSlot')&&r.includes('wrap.querySelector(\"#loot-drop-shelf\")')&&r.includes('COMBAT_COPY.lootHead')?0:1)" && node --test test/unit/shell-loot-screen.test.js test/unit/shell-combat-over.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {');const b=s.indexOf('if (S.store) {',a);const r=s.slice(a,b);process.exit(a>-1&&b>a&&!/Bag full \(/.test(r)&&!/color:var\(--rust\)/.test(r)?0:1)"` exits 0 (the loot branch carries no inline bag-full paragraph and no red inline line).
    - Same region: `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('if (S.pendingLoot && S.pendingLoot.length && !S.combat && !S.store) {');const b=s.indexOf('if (S.store) {',a);const r=s.slice(a,b);process.exit(r.includes('id=\"loot-drop-shelf\"')&&r.includes('usage.full && needsSlot')&&r.includes('wrap.querySelector(\"#loot-drop-shelf\")')&&r.includes('renderCarriedList(wrap.querySelector(\"#loot-list\")')&&r.includes('COMBAT_COPY.lootHead')&&!/document\.getElementById/.test(r)?0:1)"` exits 0 (shelf, header, and the 49e01fc host lookups intact).
    - `grep -c 'Bag full (${usage.have}/${usage.slots}) — sell or drop something to make room.' mazeworld.html` = 1 (the store branch's own line untouched); `grep -c 'bagFull: (e) => block(' src/browser/toasts.js` = 1 (rail copy untouched); `git diff --stat -- src/browser/toasts.js src/browser/lootCompare.js test/unit/lootCompare.test.js` prints nothing.
    - `grep -c 'host.appendChild(mid);' mazeworld.html` >= 1 and `git diff -U0 -- mazeworld.html | grep -c 'function renderCombatOver'` = 0 (renderCombatOver untouched).
    - `node --test test/unit/shell-loot-screen.test.js test/unit/shell-combat-over.test.js` exits 0 (14 and 19 tests).
  </acceptance_criteria>
  <done>Looting with a full bag shows the refusal only in the rail card; the loot screen keeps its header line and the drop shelf; both suites pin the paragraph's absence and the shelf's presence.</done>
</task>

<task type="auto">
  <name>Task 4: USER RULING — feature rail cards, the hold-inspect card and the encounter/stair overlays show the PNG icon (RAIL_FEATURE_ICON + iconKey plumbing); re-pin rail/combatPanel/shell-map-rail/shell-combat-screen/shell-map-viewport; full gate + debug APK + SUMMARY</name>
  <files>src/browser/rail.js, src/browser/combatPanel.js, mazeworld.html, test/unit/rail.test.js, test/unit/combatPanel.test.js, test/unit/shell-map-rail.test.js, test/unit/shell-combat-screen.test.js, test/unit/shell-map-viewport.test.js</files>
  <read_first>
    - src/browser/rail.js lines 98-144 (`RAIL_FAMILY` — the event-type keys the new map must be a subset of), 163-183 (`railFamilyFor` — UNCHANGED; rail.test.js deepEquals its exact 4-field shape), 225-267 (`railCardFor` — the return object gains `iconKey`), 274-276 (`railLineCard(title, line, tone, hold, icon = "·")` — gains a trailing `iconKey = null` param)
    - src/browser/icons.js lines 18-31 (`FEATURE_ICONS` — the nine legal PNG keys) and 42-64 (`featureKeyForCell`: `climb -> "wall"`, `gorge -> "crevice"`, `exit/gate -> "descent"`, `dot -> "encounter"` — READ ONLY, never edited)
    - engine/movement.js lines 200-210 (READ ONLY: `climbing ? "fellClimbing" : "fellInGorge"` / `climbing ? "climbedOver" : "leaptOver"` — the climb pair fires on the `climb` feat (drawn as wall.png), the leap pair on `gorge` (crevice.png); `flownOver`/`phasedThrough` fire on either and carry no feat field)
    - src/browser/combatPanel.js lines 178-204 (`encounterOverlaySpec` — the returned literal gains `iconKey: "encounter"` right after `icon: "●"`)
    - mazeworld.html ~L645-646 (`.mw-major-icon{...}` / `.mw-major-icon.encounter{...}` — add the img rule after), ~L664 (`.mw-rail-icon{flex:none;width:22px;...}` — widen to 26px and add the img rule after), ~L5216-5262 (`function renderMajorOverlay(host, spec)` — `icon.className = "mw-major-icon " + spec.iconTone; icon.textContent = spec.icon;` is the branch point; the function must stay innerHTML-free and content-agnostic), ~L5685-5694 (the stair spec: `renderMajorOverlay(body, { icon: "▼", iconTone: "", title: MAP_COPY.stair.title, ...` — gains `iconKey: "descent",`), ~L5875 (the combat pending gate spreads `...spec` so `iconKey` flows through with no edit), ~L5936-6066 (`function renderRail()`: the `let key, tone, icon, title, lines, buttons = [], idle = false, peek = "", shelfItems = null;` declaration; the climb branch `const base = rail.card || vm.lineCard("FELL", "", "bad", 0, "⧗");`; the card branch `tone = rail.card.tone; icon = rail.card.icon; ...`; the render line `document.getElementById("mw-rail-icon").textContent = icon;` — the region is pinned innerHTML-free with exactly one `setTimeout(`), ~L6307-6319 (`function inspectAt`: `const mark = c && c.seen && c.feat ? M.markForCell(c) : null;` then `window.mzRailLine?.(card.title, card.line, card.tone, card.hold, mark ? mark.glyph : "·");` — mapMarks.markForCell returns `{ key, glyph, color }`), ~L6702-6705 (`window.mzRailLine = (title, line, tone, hold, icon) => { window.__mzRail = railPush(window.__mzRail, railLineCard(title, line, tone, hold, icon)); window.renderRail?.(); };`), ~L6205-6215 (`renderMarksLegend` — the existing createElement("img") + `./icons/optimized/${row.key}.png` pattern to mirror; do not refactor it)
    - test/unit/rail.test.js lines 20-35 (imports), 48-92 (railFamilyFor deepEquals — untouched), 140-192 (railCardFor tests — add iconKey asserts), 194-205 (Test 11 railLineCard deepEqual — re-pin with `iconKey: null`)
    - test/unit/combatPanel.test.js lines 216-229 (encounterOverlaySpec deepEqual — re-pin)
    - test/unit/shell-map-rail.test.js lines 56-110 (helpers incl. `railRegion()`), 206-230 (test (f) — pins zero innerHTML and exactly one `setTimeout(` in renderRail; the new img code must respect both), end of file (append the new test)
    - test/unit/shell-combat-screen.test.js lines 54-75 (`fnRegion`/`helpersRegion`/`combatBranch`) and 216-229 (CSCR-06 renderMajorOverlay test — extend)
    - test/unit/shell-map-viewport.test.js lines 165-175 (test (e) — pins the exact `window.mzRailLine?.(...)` call in inspectAt; re-pin) and 254-276 (tests (g) — stair spec `icon: "▼"` pin stays; add the iconKey pin; the second (g) test's title says "byte-identical" but only asserts no `MAP_COPY` in the body — reword its title to "stays content-agnostic (no MAP_COPY)")
  </read_first>
  <action>
    <!-- planner-discipline-allow: innerHTML -->
    Record verbatim in the SUMMARY: "On the screens that show up to describe what happens when you land on an icon, we should use the actual icon."
    **(a) src/browser/rail.js — the pure map.**
    1. After `RAIL_FAMILY`, add a doc comment ("RAIL_FEATURE_ICON — event family -> icons/optimized PNG key (a FEATURE_ICONS key from icons.js) for the rail cards that describe landing on a tile, 2026-09-17 UAT ruling. Every key is a RAIL_FAMILY key; families with no tile identity are absent and render the glyph.") and `export const RAIL_FEATURE_ICON = Object.freeze({ trapSprung: "trap", trapDoubled: "trap", trapPoisoned: "trap", trapAvoided: "trap", trapDisarmed: "trap", teleported: "teleport", oneWayBlocked: "onewaydoor", chestOpened: "chest", chestLockRolled: "chest", scrollFound: "chest", chestLocked: "chest", climbedOver: "wall", fellClimbing: "wall", leaptOver: "crevice", fellInGorge: "crevice", flownOver: "crevice", phasedThrough: "crevice", floorChanged: "descent", tableFour: "encounter", tableFourNoop: "encounter", wanderingMonster: "encounter" });` — deliberate refinement of the brief's "climb family -> crevice": `climbedOver`/`fellClimbing` fire only on the `climb` feat, which the map draws as wall.png (icons.js `climb -> "wall"`), and the ruling is "the actual icon"; `flownOver`/`phasedThrough` carry no feat, so they take the brief's "crevice". Note this in the SUMMARY.
    2. `railCardFor`: add `iconKey: RAIL_FEATURE_ICON[head.type] ?? null,` to the returned object (after `icon`). `railFamilyFor` is NOT changed (its deepEqual shape is pinned).
    3. `railLineCard(title, line, tone, hold, icon = "·", iconKey = null)` returns `{ icon, iconKey, title, lines: [{ text: line, roll: null }], tone, hold }`; update its doc comment (optional trailing PNG key for the hold-inspect card).
    **(b) mazeworld.html — rail + inspect.**
    4. CSS: change `.mw-rail-icon{flex:none;width:22px;` to `width:26px` (rest of the rule unchanged) and add directly after it `.mw-rail-icon img{width:26px;height:26px;object-fit:contain;display:block}`. After `.mw-major-icon.encounter{color:#e05a48}` add `.mw-major-icon-img{width:60px;height:60px;object-fit:contain;display:block}`.
    5. Directly above `function renderMajorOverlay(host, spec)` add a frozen path object and a one-line helper (comment: "2026-09-17 UAT (user ruling): feature cards and the encounter/stair overlays show the tile's actual PNG; same icons/optimized set preloadIcons/renderMarksLegend already use"): `const FEATURE_ICON_PATH = Object.freeze({ dir: "./icons/optimized/", ext: ".png" });` and `function featureIconSrc(key) { return FEATURE_ICON_PATH.dir + key + FEATURE_ICON_PATH.ext; }`.
    6. `renderRail()`: extend the declaration to `let key, tone, icon, iconKey = null, title, lines, buttons = [], idle = false, peek = "", shelfItems = null;`. In the climb branch after `icon = base.icon;` add `iconKey = base.iconKey ?? null;`. In the card branch add `iconKey = rail.card.iconKey ?? null;` after `icon = rail.card.icon;`. Joiner/find/idle leave it null (decision cards and idle keep their glyphs). Replace the single render line with: `const iconEl = document.getElementById("mw-rail-icon"); iconEl.textContent = iconKey ? "" : icon; if (iconKey) { const img = document.createElement("img"); img.src = featureIconSrc(iconKey); img.alt = ""; img.setAttribute("aria-hidden", "true"); iconEl.appendChild(img); }` — built with createElement (the region is pinned innerHTML-free), no new setTimeout.
    7. `inspectAt()`: change the report call to `window.mzRailLine?.(card.title, card.line, card.tone, card.hold, mark ? mark.glyph : "·", mark ? mark.key : null);` (the hold card shows the seen feature's PNG; unwalked/rock/empty squares have no mark and keep the glyph).
    8. `window.mzRailLine = (title, line, tone, hold, icon, iconKey = null) => { window.__mzRail = railPush(window.__mzRail, railLineCard(title, line, tone, hold, icon, iconKey)); window.renderRail?.(); };` — every existing 5-arg caller (chip explanations, tap/here/blocked lines, quit warning) is unchanged and backward compatible. The rail.js import line is unchanged (no new import: the card carries the key).
    **(c) MAJOR OVERLAY.**
    9. `src/browser/combatPanel.js` `encounterOverlaySpec`: add `iconKey: "encounter",` directly after `icon: "●",`; extend its doc comment ("iconKey: the icons/optimized PNG the overlay shows instead of the glyph, 2026-09-17 UAT").
    10. Stair spec in `renderEncounter`: add `iconKey: "descent",` directly after `icon: "▼",` (keep the glyph as the fallback field).
    11. `renderMajorOverlay`: replace `icon.textContent = spec.icon;` with `if (spec.iconKey) { const img = document.createElement("img"); img.className = "mw-major-icon-img"; img.src = featureIconSrc(spec.iconKey); img.alt = ""; img.setAttribute("aria-hidden", "true"); icon.appendChild(img); } else { icon.textContent = spec.icon; }` — the death overlay (renderCombatOver) and any spec without iconKey keep the glyph path; the function stays innerHTML-free and never reads MAP_COPY. The combat pending gate (`{ ...spec, primary: ..., secondary: null }`) needs no edit.
    **(d) Re-pin tests.**
    12. `test/unit/rail.test.js`: import `RAIL_FEATURE_ICON` from rail.js and `FEATURE_ICONS` from `../../src/browser/icons.js` (import-safe: `Image()` is only constructed inside preloadIcons). Re-pin Test 11's deepEqual to include `iconKey: null` (key order: icon, iconKey, title, lines, tone, hold) and add `assert.equal(railLineCard("T", "L", "good", 3000, "◆", "teleport").iconKey, "teleport")`. In the trap+level-up test add `assert.equal(card.iconKey, "trap")`; in the floorChanged test add `assert.equal(card.iconKey, "descent")`; in the camp test (line ~189) add `assert.equal(card.iconKey, null)`. Append a new test `RAIL_FEATURE_ICON: frozen, every key is a RAIL_FAMILY key, every value is a FEATURE_ICONS key, the trap/chest/climb/crevice/floor/encounter families are covered` asserting `Object.isFrozen`, the subset/legal-value checks via `Object.keys`/`Object.values`, and the exact pairs `trapSprung -> trap`, `teleported -> teleport`, `oneWayBlocked -> onewaydoor`, `chestLocked -> chest`, `climbedOver -> wall`, `fellClimbing -> wall`, `leaptOver -> crevice`, `fellInGorge -> crevice`, `floorChanged -> descent`, `tableFour -> encounter`, plus `RAIL_FEATURE_ICON.leveled === undefined` and `RAIL_FEATURE_ICON.joinerJoined === undefined`.
    13. `test/unit/combatPanel.test.js`: add `iconKey: "encounter",` after `icon: "●",` in the one-foe deepEqual; in the 3-foe test add `assert.equal(spec.iconKey, "encounter")`.
    14. `test/unit/shell-map-rail.test.js`: append test `(p) 2026-09-17 UAT ruling (actual icons): renderRail renders an <img> from featureIconSrc(iconKey) via createElement (no innerHTML) and falls back to the glyph; mzRailLine forwards the optional trailing iconKey` asserting on `railRegion()`: matches `iconKey = rail\.card\.iconKey \?\? null;`, `iconKey = base\.iconKey \?\? null;`, `iconEl\.textContent = iconKey \? "" : icon;`, `img\.src = featureIconSrc\(iconKey\);`, `document\.createElement\("img"\)`, still `doesNotMatch(/innerHTML/)`; on `CODE`: `/^function featureIconSrc\(key\)/m` once, `/^const FEATURE_ICON_PATH = Object\.freeze\(\{ dir: "\.\/icons\/optimized\/", ext: "\.png" \}\);/m` once, `window\.mzRailLine = \(title, line, tone, hold, icon, iconKey = null\) =>` once, `railLineCard\(title, line, tone, hold, icon, iconKey\)` once; and `HTML` matches `/^\.mw-rail-icon img\{width:26px;height:26px;object-fit:contain;display:block\}$/m`.
    15. `test/unit/shell-combat-screen.test.js` CSCR-06 renderMajorOverlay test: add `assert.match(region, /if \(spec\.iconKey\) \{/)`, `assert.match(region, /img\.className = "mw-major-icon-img";/)`, `assert.match(region, /img\.src = featureIconSrc\(spec\.iconKey\);/)`, `assert.match(region, /else \{ icon\.textContent = spec\.icon; \}/)`; keep `doesNotMatch(/innerHTML/)`; and `assert.match(COMBAT_PANEL_SRC, /iconKey: "encounter",/)`; plus `assert.match(HTML, /^\.mw-major-icon-img\{width:60px;height:60px;object-fit:contain;display:block\}$/m)`.
    16. `test/unit/shell-map-viewport.test.js`: re-pin (e)'s mzRailLine regex to `/window\.mzRailLine\?\.\(card\.title, card\.line, card\.tone, card\.hold, mark \? mark\.glyph : "[^"]*", mark \? mark\.key : null\);/`; in (g) add `assert.match(branch, /iconKey: "descent",/)` after the `icon: "▼"` pin; reword the second (g) test title as noted in read_first.
    **(e) Gate (all of it, in this order; record every result in the SUMMARY):** `npm test` -> `# fail 0`, `# pass` >= 2172; `npm run build:www` -> exit 0 (copies icons/optimized into www — the PNGs the new `<img>` tags load); `git diff --stat -- engine content test/parity` -> empty; `git hash-object test/parity/prototype-master.js.txt` -> `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run android:debug` -> exit 0, then record the size and mtime of `android/app/build/outputs/apk/debug/app-debug.apk` (`ls -l --time-style=full-iso android/app/build/outputs/apk/debug/app-debug.apk`). Run NO `adb` command — the orchestrator installs.
    **(f) SUMMARY contract.** Write `.planning/quick/260917-bbs-v1-4-uat-fixes-round-2/260917-bbs-SUMMARY.md` with `status: complete` in its frontmatter; record verbatim the three user rulings ("Joiner should only show when fighting, and probably on the hero screen."; "When I try to loot something and my bag is full, the message shows up right under the item that I tried to stow, and it shows up in the card at the bottom. We don't need that message to show up right under the gear piece. The card is sufficient."; "On the screens that show up to describe what happens when you land on an icon, we should use the actual icon.") plus the addendum ("Joiner section on top of the combat window should go, too. We already have our joiner in the Your Lot section."); the climb -> wall refinement; the gate results; and the FIGHT IT OUT addendum ("FIGHT IT OUT button doesn't have a button colour unless the window loses focus and I come back — black text on nothing, hard to read."); and END the file with a `## Human verification` section listing exactly these Pixel 7 re-checks: (1) the party ring pulses on the map without the `tile memory limits exceeded` logcat spam, and with Android "Remove animations" on the ring sits still; (1b) FIGHT IT OUT (and GO DOWN / NOT YET) render with the gold background immediately, without needing a focus change, and the ring is gone while the overlay is up and back in place when it closes; (2) no joiner strip on the map; the joiner's card appears under Company on the Hero tab (hidden when solo); during a fight nothing party-related sits above the combat panel and the joiner appears only in YOUR LOT (unchanged); (3) loot with a full bag — only the rail card says the bag is full; no red line under the loot list; the drop shelf still appears; (4) trap / teleport / one-way door / locked box / crevice-or-wall / floor rail cards, the hold-inspect card on a seen feature, and the encounter and stair overlays show the PNG icons; death overlay, joiner/find decision cards and the idle card still show their glyphs.
  </action>
  <verify>
    <automated>test "$(grep -c '^export const RAIL_FEATURE_ICON = Object.freeze({' src/browser/rail.js)" = "1" && test "$(grep -c 'iconKey: "encounter",' src/browser/combatPanel.js)" = "1" && test "$(grep -c 'iconKey: "descent",' mazeworld.html)" = "1" && test "$(grep -c '^function featureIconSrc(key)' mazeworld.html)" = "1" && test "$(grep -c 'mark ? mark.key : null' mazeworld.html)" = "1" && test "$(grep -c 'window.mzRailLine = (title, line, tone, hold, icon, iconKey = null) =>' mazeworld.html)" = "1" && npm test 2>&1 | tail -12 | grep -q '^# fail 0' && npm run build:www && test -z "$(git diff --stat -- engine content test/parity)" && test "$(git hash-object test/parity/prototype-master.js.txt)" = "a1f4d0dc29782218d8e5aab65bc5989c33f917f0" && npm run android:debug && ls -l android/app/build/outputs/apk/debug/app-debug.apk</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export const RAIL_FEATURE_ICON = Object.freeze({' src/browser/rail.js` = 1; `node -e "import('./src/browser/rail.js').then(m=>{const k=Object.keys(m.RAIL_FEATURE_ICON);const ok=Object.isFrozen(m.RAIL_FEATURE_ICON)&&k.length===21&&k.every(x=>x in m.RAIL_FAMILY)&&Object.values(m.RAIL_FEATURE_ICON).every(v=>['trap','teleport','onewaydoor','chest','wall','crevice','descent','encounter'].includes(v))&&m.RAIL_FEATURE_ICON.climbedOver==='wall'&&m.RAIL_FEATURE_ICON.leaptOver==='crevice'&&m.RAIL_FEATURE_ICON.floorChanged==='descent'&&m.railLineCard('T','L','good',3000,'x','teleport').iconKey==='teleport'&&m.railLineCard('T','L','good',3000).iconKey===null;process.exit(ok?0:1)})"` exits 0.
    - `node -e "import('./src/browser/combatPanel.js').then(m=>{const s=m.encounterOverlaySpec({combat:{foes:[{name:'Kobold',alive:true,type:'Beasts',wp:5,maxWP:5}],tracked:false}});process.exit(s.iconKey==='encounter'&&s.icon==='●'?0:1)})"` exits 0.
    - `grep -c 'iconKey: "descent",' mazeworld.html` = 1 and `grep -c 'icon: "▼",' mazeworld.html` = 1 (stair spec carries both); `grep -c '^function featureIconSrc(key)' mazeworld.html` = 1; `grep -c '^const FEATURE_ICON_PATH = Object.freeze({ dir: "./icons/optimized/", ext: ".png" });' mazeworld.html` = 1; `grep -c 'featureIconSrc(' mazeworld.html` = 3 (definition + renderRail + renderMajorOverlay).
    - `grep -c '^\.mw-rail-icon img{width:26px;height:26px;object-fit:contain;display:block}$' mazeworld.html` = 1; `grep -c '^\.mw-major-icon-img{width:60px;height:60px;object-fit:contain;display:block}$' mazeworld.html` = 1; `grep -c '^\.mw-rail-icon{flex:none;width:26px;' mazeworld.html` = 1.
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function renderRail() {');const b=s.indexOf('function syncRailLive(text)',a);const r=s.slice(a,b).replace(/\/\/[^\n]*/g,'');process.exit(!/innerHTML/.test(r)&&(r.match(/setTimeout\(/g)||[]).length===1&&/iconEl\.textContent = iconKey \? \"\" : icon;/.test(r)&&/img\.src = featureIconSrc\(iconKey\);/.test(r)&&/iconKey = rail\.card\.iconKey \?\? null;/.test(r)?0:1)"` exits 0 (renderRail: img branch present, still innerHTML-free, still one setTimeout).
    - `node -e "const s=require('fs').readFileSync('mazeworld.html','utf8');const a=s.indexOf('function renderMajorOverlay(host, spec) {');const b=s.indexOf('function renderCombatHeader(host, vm)',a);const r=s.slice(a,b);process.exit(!/innerHTML/.test(r)&&!/MAP_COPY/.test(r)&&/if \(spec\.iconKey\) \{/.test(r)&&/img\.className = \"mw-major-icon-img\";/.test(r)&&/else \{ icon\.textContent = spec\.icon; \}/.test(r)?0:1)"` exits 0.
    - `grep -c 'mark ? mark.key : null' mazeworld.html` = 1; `grep -c 'window.mzRailLine = (title, line, tone, hold, icon, iconKey = null) =>' mazeworld.html` = 1; `grep -c 'railLineCard(title, line, tone, hold, icon, iconKey)' mazeworld.html` = 1; `grep -c 'window.mzRailLine = ' mazeworld.html` = 1 (bridge count pin intact); `grep -c 'from "./src/browser/rail.js"' mazeworld.html` = 1 (import line unchanged).
    - `git diff --stat -- src/browser/icons.js src/browser/mapMarks.js icons` prints nothing.
    - `node --test test/unit/rail.test.js test/unit/combatPanel.test.js test/unit/shell-map-rail.test.js test/unit/shell-combat-screen.test.js test/unit/shell-map-viewport.test.js` exits 0 (rail 16, combatPanel 19, shell-map-rail 18, shell-combat-screen 15, shell-map-viewport 26 tests).
    - `npm test` output ends with `# fail 0` and `# pass` >= 2172.
    - `npm run build:www` exits 0 and `ls www/icons/optimized/trap.png www/icons/optimized/descent.png www/icons/optimized/encounter.png` lists all three.
    - `git diff --stat -- engine content test/parity` prints nothing; `git hash-object test/parity/prototype-master.js.txt` prints `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`.
    - `npm run android:debug` exits 0; `android/app/build/outputs/apk/debug/app-debug.apk` exists with an mtime after the Task 4 commit — its size and mtime are recorded in the SUMMARY; no `adb` command was run.
    - The SUMMARY exists at `.planning/quick/260917-bbs-v1-4-uat-fixes-round-2/260917-bbs-SUMMARY.md`, has `status: complete` in its frontmatter, the three verbatim rulings plus both addenda (combat-screen joiner; FIGHT IT OUT), the climb -> wall note, the gate results, and ends with `## Human verification` listing the five Pixel 7 re-checks above (1, 1b, 2, 3, 4 — including the combat case, the FIGHT IT OUT gold-background case and the full-bag loot case).
  </acceptance_criteria>
  <done>Tile-event rail cards, the hold-inspect card and the encounter/stair overlays show the real PNG icons; glyph paths survive for everything without a tile identity; every touched suite pins the new shape; the full gate is green with the parity hash unchanged and a fresh debug APK on disk for the orchestrator to install.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine events / S.party -> shell renderers | trusted engine data (names from content tables, event types from a frozen family table); rendered into the DOM by the shell |
| local PNG assets -> `<img>` in the rail / overlay | bundled `icons/optimized/*.png` shipped inside the APK; keys come from frozen module constants, never user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-bbs-01 | Tampering | featureIconSrc(key) img.src | low | mitigate | `iconKey` only ever comes from `RAIL_FEATURE_ICON` values, `mapMarks.markForCell().key` (a FEATURE_ICONS key) or the two literal spec keys; rail.test.js proves every map value is a FEATURE_ICONS key; the `<img>` is built with createElement (no innerHTML) with `alt=""` |
| T-bbs-02 | Tampering | renderPartyRoster card template | low | accept | pre-existing Phase 10 template moved verbatim; names come from content tables via the engine, not user input; the roster is read-only presentation on the paint() path |
| T-bbs-03 | Denial of Service | .mw-party-pulse animation | medium | mitigate | animating only composited properties on a will-change layer removes the per-frame map-layer re-raster (Task 1); the HUD suite negative-greps the keyframes block |
| T-bbs-04 | Denial of Service | boot (tab init -> S TDZ) | high | mitigate | renderPartyRoster is called only from paint(); shell-party-camp asserts the initTabs region never references it (commit 7ab68d3 rule) |
| T-bbs-SC | Tampering | npm/pip/cargo installs | high | mitigate | no package installs in this plan (package.json unchanged — pinned by shell-map-invariants MAP-09) |
</threat_model>

<verification>
- `npm test` `# fail 0` (pass >= 2172); `npm run build:www` exit 0; `git diff --stat -- engine content test/parity` empty; `git hash-object test/parity/prototype-master.js.txt` = `a1f4d0dc29782218d8e5aab65bc5989c33f917f0`; `npm run android:debug` exit 0 with the APK size/mtime recorded; no `adb` commands.
- Source invariants after this plan: the mwglow keyframes block carries no shadow property (opacity/transform only) and `.mw-party-pulse` carries the static ring + `will-change`; `.mw-party-pulse.covered{animation-play-state:paused;visibility:hidden}` exists once and renderEncounter adds/removes `covered` on the line after each `panel.hidden` write (`getElementById("mw-party-pulse")` appears exactly 3x file-wide); zero occurrences of the retired party-strip id / render-function name; `#hero-party` + `#hero-party-list` exactly once, between the Character sheet and Special skills; `renderPartyRoster();` exactly once (in paint(), never in initTabs); the loot branch (pendingLoot guard -> store guard) carries no `Bag full (` / inline red line while `id="loot-drop-shelf"`, `usage.full && needsSlot` and the 49e01fc `wrap.querySelector` host lookups remain; `RAIL_FEATURE_ICON` frozen with 21 RAIL_FAMILY keys mapping onto FEATURE_ICONS keys; `featureIconSrc(` 3x; renderRail and renderMajorOverlay still innerHTML-free; `window.mzRailLine = ` still 1x with the optional trailing `iconKey`; the rail.js import line unchanged.
- SUMMARY contract: `status: complete`; the three rulings + both addenda recorded verbatim; the climb -> wall refinement noted; the file ends with `## Human verification` listing the five Pixel 7 re-checks (composited ring + reduced-motion stop; FIGHT IT OUT / GO DOWN / NOT YET gold background immediately with the ring gone under the overlay; no joiner on the map or over the combat screen, Company panel on Hero, YOUR LOT unchanged; full-bag loot reported only by the rail card with the shelf still present; PNG icons on feature rail cards / hold-inspect / encounter + stair overlays, glyphs elsewhere).
</verification>

<success_criteria>
- All four UAT items (plus the combat-screen addendum) are source-provable and pinned by tests; the executor never touches `engine/`, `content/`, `test/parity/`, `src/browser/icons.js`, `src/browser/mapMarks.js`, `src/browser/toasts.js` or `icons/`, never wires a roster call into tab init, and never reintroduces `document.getElementById` into the loot branch.
- The debug APK is rebuilt for the orchestrator's install and the SUMMARY hands the user the four-item Pixel 7 re-check list.
</success_criteria>

<output>
Create `.planning/quick/260917-bbs-v1-4-uat-fixes-round-2/260917-bbs-SUMMARY.md` when done (frontmatter `status: complete`; ends with `## Human verification` per the Task 4 contract).
</output>
