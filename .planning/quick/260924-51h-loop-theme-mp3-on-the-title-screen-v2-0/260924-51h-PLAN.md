---
quick_id: 260924-51h
slug: loop-theme-mp3-on-the-title-screen-v2-0
date: 2026-09-24
status: planned
phase: quick-260924-51h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sfx/theme.mp3
  - src/browser/titleMusic.js
  - test/unit/titleMusic.test.js
  - src/browser/sfx.js
  - test/unit/sfx-music.test.js
  - test/unit/sfx-assets.test.js
  - tools/build-www.mjs
  - src/browser/nativeChrome.js
  - test/persistence/lifecycle.test.js
  - mazeworld.html
  - test/unit/title-music-shell.test.js
  - docs/SHELL-MODULES.md
  - .planning/quick/260924-51h-loop-theme-mp3-on-the-title-screen-v2-0/260924-51h-SUMMARY.md
autonomous: true
requirements: [QUICK-260924-51h]
tags: [vanilla-js, web-audio, presentation-only, title-screen, capacitor-lifecycle, milestone-v2.0]

must_haves:
  truths:
    - "On the in-WebView title screen (DUNGEON / ENTER / VIEW THE DEAD), the first user gesture with Sound on starts sfx/theme.mp3 looping at MUSIC_GAIN (0.5 of the one-shot clip level); before any gesture it is silent; if the title is already gone when that gesture's unlock lands (ENTER as the very first tap), it never starts"
    - "The theme keeps playing, without restarting, while the title-mode Leaderboards panel (VIEW THE DEAD) is open and while the account/settings sheets sit over the title; the panel's route back to the title does not restart it"
    - "Leaving the title for the roller or the map (ENTER, ROLL A NEW HERO, BACK TO THE DUNGEON, resume) fades it out over MUSIC_FADE_MS = 500 ms, or cuts it at once under reduced motion; showing the title again (BURY THEM, Save & quit, abandon, the panel's title route) restarts it from the top"
    - "Sound Off never plays it, and flipping Sound Off mid-loop cuts it at once; flipping Sound On while the title is up starts it on that same tap"
    - "Backgrounding the app (nativeChrome pause / appStateChange(inactive), plus document visibilitychange) cuts it at once and calls stopAllSfx; on resume it restarts from the top only if the title (or its title-mode panel) is still showing and Sound is on"
    - "The theme loads lazily: it is fetched and decoded only when music is first wanted, which needs a gesture and so always comes after first paint. The decoded buffer lives only as long as the loop plays. A missing or undecodable file is silent, never throws, and never blocks the title"
    - "npm test reports 0 failures on master (AUD-06 green with sfx/theme.mp3 committed: exactly the 30 pinned clips plus 1 declared music track); engine/, content/ and test/parity/ are byte-identical"
  artifacts:
    - path: "sfx/theme.mp3"
      provides: "the user-supplied title theme, committed byte-for-byte (2,318,486 bytes), never re-encoded or moved"
    - path: "src/browser/titleMusic.js"
      provides: "pure title-music state machine: MUSIC_FADE_MS, shouldPlayTitleMusic, createTitleMusic"
      exports: ["MUSIC_FADE_MS", "shouldPlayTitleMusic", "createTitleMusic"]
    - path: "src/browser/sfx.js"
      provides: "looping music player on the existing Web Audio device: MUSIC_IDS, MUSIC_GAIN, isSfxUnlocked, startMusic, stopMusic; stopAllSfx and the Sound-Off teardown also cut music"
      exports: ["MUSIC_IDS", "MUSIC_GAIN", "isSfxUnlocked", "startMusic", "stopMusic"]
    - path: "src/browser/nativeChrome.js"
      provides: "optional onBackground / onForeground lifecycle hooks on registerNativeChrome"
    - path: "test/unit/titleMusic.test.js"
      provides: "controller truth table + edge/fade/restart tests"
    - path: "test/unit/sfx-music.test.js"
      provides: "music player tests against an injected fake backend"
    - path: "test/unit/title-music-shell.test.js"
      provides: "comment-stripped source scan of the mazeworld.html wiring"
    - path: "test/unit/sfx-assets.test.js"
      provides: "AUD-06: 30 pinned clips + EXPECTED_MUSIC ['theme'] by set equality"
      contains: "EXPECTED_MUSIC"
  key_links:
    - from: "mazeworld.html (module script)"
      to: "src/browser/titleMusic.js#createTitleMusic"
      via: "const titleMusic = createTitleMusic({ start, stop, reduced: () => prefersReducedMotion(window) }) declared before the first-gesture pointerdown listener"
      pattern: "createTitleMusic\\("
    - from: "mazeworld.html MutationObserver (#mw-title-screen[hidden], body[data-boards-entry])"
      to: "syncTitleMusic -> titleMusic.update"
      via: "observer callback runs after the whole tap handler returns"
      pattern: "new MutationObserver\\(syncTitleMusic\\)"
    - from: "first-gesture pointerdown listener and the Sound settings tap"
      to: "unlockSfx().then(syncTitleMusic)"
      via: "unlockAudioAndSync() — the shell's one unlockSfx( call"
      pattern: "unlockSfx\\(\\)\\.then\\(syncTitleMusic"
    - from: "src/browser/nativeChrome.js pause/appStateChange/resume listeners"
      to: "mazeworld.html setAppActive(false|true)"
      via: "registerNativeChrome({ onBackground, onForeground })"
      pattern: "onBackground: \\(\\) => setAppActive\\(false\\)"
    - from: "src/browser/sfx.js#startMusic"
      to: "./sfx/theme.mp3"
      via: "the existing DEFAULT_BACKEND.load(handle, 'theme') -> startLoop(handle, buffer, MUSIC_GAIN)"
      pattern: "startLoop\\("
---

<objective>
Loop `sfx/theme.mp3` on the in-WebView title screen as part of milestone v2.0 (user ruling 2026-09-24). The pure play/stop state machine goes in a new tested module (`src/browser/titleMusic.js`). The looping player sits in `src/browser/sfx.js` on the existing Web Audio device, so it uses the same Sound gate, `stopAllSfx` and teardown. `nativeChrome.js` gains lifecycle hooks, and `mazeworld.html` wires the pieces together without changing the title, panel-route or settings function bodies that existing tests run. Commit the asset and make AUD-06 green (30 clips + 1 declared music track).

Purpose: the game gains its first music, and `npm test` on master goes from 1 failure (AUD-06, caused by the untracked `theme.mp3`) to 0.
Output: 4 new files (`titleMusic.js`, 3 test files) and edits to `sfx.js`, `nativeChrome.js`, `mazeworld.html`, `sfx-assets.test.js`, `lifecycle.test.js`, `build-www.mjs` and `SHELL-MODULES.md`. `sfx/theme.mp3` is committed.

Orchestrator rulings are locked, and task actions cite them as R-NN:
- R-01 "Splash screen" = the in-WebView title screen (`#mw-title-screen`), not the native Capacitor splash.
- R-02 Keep `sfx/theme.mp3` exactly where it is. Commit it in this task; never re-encode or move it.
- R-03 Loop while the title shows, and keep playing while the title-mode Leaderboards panel and the account/settings sheets are open over the title.
- R-04 Leaving the title for the roller or the map stops it with a ~400–600 ms fade (instant under reduced motion is fine). Showing the title again restarts it from the top.
- R-05 Autoplay: start on the first user gesture while the title is visible, through the existing `unlockSfx` path. If the title is already gone by then, never start.
- R-06 Respect Sound: Off means never; toggling Off stops it; toggling On with the title up starts it.
- R-07 Stop on app pause/background (the nativeChrome pause listener and `stopAllSfx`). On resume, restart only if the title is still showing and Sound is on.
- R-08 Load lazily after first paint. A decode failure or missing file is silent, never throws, never blocks.
- R-09 Music volume sits below the one-shots (~0.5 of the sfx gain) and is a documented constant.
- R-10 `sfx-assets.test.js`: the 30 clips stay pinned exactly, and `theme.mp3` is the single declared music track (set equality = 30 + 1).
- R-11 Presentation only: no engine/ or content/ change and test/parity untouched. Pure logic goes in a small tested module and DOM/audio wiring in the shell. Follow `docs/SHELL-MODULES.md` conventions. The device check is a deferred human item.

Claude's discretion (the executor records these in SUMMARY):
- Web Audio `AudioBufferSourceNode` with `loop = true` on the existing device, not an `<audio>` element. This gives one Sound gate, one teardown, gain relative to the sfx master, and a sample-accurate fade.
- The decoded theme is never cached beyond the live voice. About 145 s of stereo decodes to roughly 50 MB of PCM, which shouldn't stay resident on a mid-range phone during a delve, so each title visit re-decodes it. The first start therefore trails the tap by about the decode time (well under a second on the Pixel 7).
- Resume after background restarts the loop from the top, the same as a fresh title visit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md
@.planning/todos/pending/2026-09-23-loop-theme-mp3-on-the-title-splash-screen.md
@src/browser/sfx.js
@src/browser/nativeChrome.js
@test/unit/sfx-assets.test.js
@test/unit/sfx-settings.test.js
@test/persistence/lifecycle.test.js
@docs/SHELL-MODULES.md

<interfaces>
Existing surfaces the executor builds on (read, do not re-derive):

src/browser/sfx.js (module state `deviceHandle`, `currentSettings`, `soundIsOff()`, `resolveBackend()` reads `globalThis.__mzSfxBackendOverride?.()` at call time):
- `unlockSfx(): Promise<void>` — the one device-open site; bails on Sound Off / already open / in flight; loads the 30 CLIP_IDS without awaiting.
- `stopAllSfx(): void` — stops every one-shot voice. Today only applySfxSettings calls it.
- `applySfxSettings(settings): void` — on a transition to Sound Off, runs stopAllSfx, closes the device, then clears deviceHandle and bufferCache.
- DEFAULT_BACKEND `{ open, load(handle, clipId) -> fetch("./sfx/<id>.mp3") + decodeAudioData, start, stop, close }`; handle = `{ ctx, masterGain }` (masterGain at the default 1.0).

src/browser/nativeChrome.js `registerNativeChrome({ App, SplashScreen, StatusBar, ScreenOrientation, storage, waitForPending, getGameContext })`:
- `App.addListener("pause", () => flushOnBackground(storage, waitForPending))`
- `App.addListener("appStateChange", ({ isActive } = {}) => { if (!isActive) return flushOnBackground(...); })`

mazeworld.html module script (line numbers approximate, grep before editing):
- ~5519 `import { unlockSfx, applySfxSettings, playForDispatch, cuesForDispatch, playUiTap, playClipIds } from "./src/browser/sfx.js";`
- ~5530 `import { prefersReducedMotion, onReducedMotionChange, createPanelMotion } from "./src/browser/motion.js";` (DO NOT edit this line — reduced-motion.test.js pins it verbatim)
- ~5908 `let currentSettings = null; function applySettings(settings) {...}` (calls applySfxSettings(settings))
- ~6044 `document.getElementById("mw-settings-rows")?.addEventListener("click", async (e) => {...})` — has `key`, calls `applySettings(next); renderSettingsSheet();`
- ~6085 the Phase 56 first-gesture listener: `document.addEventListener("pointerdown", (e) => { unlockSfx(); if (e.target?.closest?.("button, [role=\"button\"]")) playUiTap(); }, { capture: true });`
- ~6099 `applySettings(await readSettings());` (top-level await — events can fire during it)
- ~6136 `function dungeonVisible()` reads `document.body.dataset.boardsEntry !== "title"` (the title-mode panel marker; boardsPanel.js sets it in openFromTitle and deletes it in clearTitleMarker before every route)
- ~6231 the pgsQueue `document.addEventListener("visibilitychange", ...)` — shell-pgs.test.js slices the FIRST visibilitychange listener in the module and expects this one
- ~7061 `hideTitleScreen()`, ~7089 `showTitleScreen({ allowResume })`, ~7404 `routeFromBoards(action, { hasHero })`, ~7434 `(function initTitleScreen() {...})();` — all four stay UNEDITED
- ~7494 `registerNativeChrome({ storage: window.mzStorage, waitForPending: () => Promise.all([...]), getGameContext: () => ({...}) })`

Test-suite pins this plan must keep green (all verified by read at plan time):
- test/unit/sfx-settings.test.js: `unlockSfx(` appears exactly once in comment-stripped mazeworld.html; `applySfxSettings(` exactly once, inside applySettings.
- test/unit/shell-account.test.js (E6/E7) and test/unit/shell-boards-panel.test.js (E1–E5) slice hideTitleScreen/routeFromBoards and run them through `new Function` with a fixed injected scope, asserting exact call sequences. Any new free identifier in those bodies throws a ReferenceError there.
- test/unit/shell-pgs.test.js (S5) slices from the start of the `registerNativeChrome({` call up to `getGameContext:` and matches the waitForPending line verbatim. (S6) forbids a `fetch(` in mazeworld.html.
- test/unit/reduced-motion.test.js: only motion.js may contain the hyphenated CSS media-feature name for reduced motion, anywhere in src/browser/*.js. Write "reduced motion" in prose instead.
- test/unit/shell-no-content-copies.test.js criterion 3: mazeworld.html may not declare any name that a src/browser module exports.
- test/unit/bridge-registry.test.js: no new `__mz`-prefixed name anywhere (no bridge-table change).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure title-music state machine (src/browser/titleMusic.js) — TDD</name>
  <files>src/browser/titleMusic.js, test/unit/titleMusic.test.js</files>
  <behavior>
    - shouldPlayTitleMusic({ titleVisible, unlocked, soundOn, appActive }) is true only when all four are strictly `true`. It is false in the other 15 combinations of the truth table, and false for no argument, null, a non-object, or non-boolean values such as "yes" or 1.
    - createTitleMusic({ start, stop, reduced }) begins with inputs { titleVisible: false, unlocked: false, soundOn: false, appActive: true } and isPlaying() false.
    - Rising edge: once an update makes all four true, start() is called exactly once. Repeating the same update, or an empty patch, calls nothing more.
    - Falling edge caused only by the title going away (titleVisible false while unlocked, soundOn and appActive are still true): stop(500), i.e. stop(MUSIC_FADE_MS). The same edge with reduced() returning true gives stop(0).
    - Falling edge caused by soundOn false, appActive false or unlocked false (alone or combined with a title change): stop(0).
    - Title hidden then shown again (all else true): start() is called a second time, which is the restart.
    - Unlock arriving while the title is hidden does not start; showing the title afterwards does.
    - Sound off from the start means no start through any sequence of the other three. soundOn back to true with the title up and unlocked calls start().
    - appActive false then true with the title up calls stop(0) then start(). appActive true while the title is hidden calls nothing.
    - Non-boolean patch values and unknown keys are ignored; a null or non-object patch is a no-op.
    - A throwing start, stop or reduced never propagates out of update(), and isPlaying() still reflects the edge. A throwing reduced counts as not reduced. Missing start/stop functions are a silent no-op.
    - update() returns isPlaying(); inputs() returns a frozen copy of the four inputs.
  </behavior>
  <action>
RED: write test/unit/titleMusic.test.js first, using node:test and node:assert/strict with test names prefixed "titleMusic:". It covers every behavior bullet above with plain recording fakes for start and stop (arrays of calls) and a switchable reduced flag. The truth table is 16 explicit combinations built by looping over the four booleans. Run it and see it fail on the missing module, then commit `test(quick-260924-51h): add failing title-music controller tests`.

GREEN: create src/browser/titleMusic.js as a pure ES module that imports nothing and reads no window, document, timer or audio API. Precedent: the pure halves of motion.js and sfx.js, which `node --test` pins directly. The header comment states the purpose: the play/stop decision for the title theme, per rulings R-03..R-07 and R-11, with DOM/audio wiring left to mazeworld.html and playback to sfx.js. Exports:
- `MUSIC_FADE_MS = 500`, documented as the title-exit fade (R-04 asks for ~400–600 ms).
- `shouldPlayTitleMusic(inputs)`, implementing the strict all-four-true rule.
- `createTitleMusic({ start, stop, reduced } = {})`, returning `Object.freeze({ update, isPlaying, inputs })`.

`update(patch)` copies only the four known keys, and only when the value is a boolean. It then computes want = shouldPlayTitleMusic(current inputs):
- want && !playing: set playing true, then call start().
- !want && playing: set playing false, then call stop(ms). ms is MUSIC_FADE_MS only when unlocked, soundOn and appActive are all still true and reduced() is not true. In every other case ms is 0, because background, Sound Off and device loss must cut at once (R-04, R-06, R-07).

start, stop and reduced are each invoked inside their own try/catch. The file must never throw.

Wording constraint: in this file, write "reduced motion" in prose and never the hyphenated CSS media-feature name (reduced-motion.test.js scans every src/browser file). Also introduce no `__mz`-prefixed name.

Run the test to green, then commit `feat(quick-260924-51h): pure title-music state machine`.
  </action>
  <verify>
    <automated>node --test test/unit/titleMusic.test.js test/unit/reduced-motion.test.js test/unit/bridge-registry.test.js</automated>
  </verify>
  <done>titleMusic.test.js passes, with the 16-row truth table and every edge, fade, restart and never-throw case green. reduced-motion.test.js and bridge-registry.test.js still pass with the new file in src/browser/. The module imports nothing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Looping music player in sfx.js + AUD-06 music pin + commit sfx/theme.mp3 — TDD</name>
  <files>src/browser/sfx.js, test/unit/sfx-music.test.js, test/unit/sfx-assets.test.js, tools/build-www.mjs, sfx/theme.mp3</files>
  <behavior>
    - MUSIC_IDS deep-equals ["theme"], is frozen, is disjoint from CLIP_IDS, and every id has a matching sfx/&lt;id&gt;.mp3. MUSIC_GAIN === 0.5, which is greater than 0 and less than 1.
    - isSfxUnlocked() is false before unlockSfx(), true after a successful unlock, and false again after a Sound-Off transition.
    - unlockSfx() alone never loads "theme": the loads list after an unlock is exactly the 30 CLIP_IDS, and the existing sfx.test.js / sfx-settings.test.js pins stay green.
    - startMusic() before unlock, with Sound Off, or with an unknown track id makes no load, calls no startLoop and throws nothing.
    - After unlock, startMusic() makes exactly one load("theme"). When it lands, startLoop(handle, buffer, MUSIC_GAIN) runs exactly once. A second startMusic() while that load is in flight starts no second load. startMusic() while the loop is already playing does nothing.
    - stopMusic({ fadeMs: 500 }) calls fadeOut(handle, voice, 500) once. stopMusic() with no live voice calls nothing. A non-finite or negative fadeMs becomes 0.
    - Late decode: when stopMusic() runs while the theme load is in flight, the landing load never calls startLoop. When startMusic() runs again before it lands, the landing load starts it once.
    - A load that returns null or rejects gives no startLoop and no throw. A later startMusic() retries the load.
    - stopAllSfx() cuts a live loop at once (fadeOut(handle, voice, 0)) and clears the wanted flag, so a load landing afterwards starts nothing.
    - A Sound-Off transition cuts the loop before close() (ordered log). After Off then On, startMusic() before a fresh unlock is a no-op. After the fresh unlock, it loads again on the new handle, and a stale load that lands on the old handle starts nothing.
    - With a backend that has only the five legacy methods (no startLoop/fadeOut), startMusic/stopMusic make no load, have no effect and throw nothing.
    - sfx-assets: EXPECTED_CLIPS still has exactly 30 entries, EXPECTED_MUSIC is ["theme"], and sfx/ holds exactly those 31 .mp3 files by set equality.
  </behavior>
  <action>
RED first:

(a) Edit test/unit/sfx-assets.test.js per R-10 and R-02.
- Add `EXPECTED_MUSIC = Object.freeze(["theme"])` beside EXPECTED_CLIPS, with a comment: user ruling 2026-09-24 makes theme.mp3 the single declared music track (the title theme). It is not a clip and is not part of the 30.
- Keep test (1), the 30-entry, no-duplicates check, exactly as it is.
- Add a test that EXPECTED_MUSIC has exactly 1 entry and shares nothing with EXPECTED_CLIPS.
- Change test (2) to set equality against the sorted .mp3 names of EXPECTED_CLIPS plus EXPECTED_MUSIC (31 files), retitled "sfx/ contains exactly the 30 expected clips plus the 1 declared music track (set equality)".
- Update the header paragraph to match. The file keeps its independence: it never imports src/browser/sfx.js.

(b) Create test/unit/sfx-music.test.js. Its fake backend is the five-method shape from sfx-settings.test.js's makeFakeBackend (open, load returning the id string as the "buffer", start, stop, close, plus an ordered calls log) with two additions:
- `startLoop(handle, buffer, gain)`, which records its arguments and returns a unique voice object;
- `fadeOut(handle, voice, ms)`, which records its arguments.
The "theme" load can be held on a controllable promise (resolve with a buffer, null, or reject). Install the fake through `globalThis.__mzSfxBackendOverride`. Reset module state in a finally block with the same stopAllSfx() plus applySfxSettings({ sound: false }) then ({ sound: true }) round-trip that sfx-settings.test.js's withFakeBackend uses. Cover every behavior bullet above except the sfx-assets ones. Run both files and see them fail: sfx-assets because sfx/ still differs until the asset is staged, sfx-music because the exports are missing.

(c) Stage the asset with `git add sfx/theme.mp3`, byte-for-byte, with no re-encode and no move (R-02). Commit the RED tests together with the asset: `test(quick-260924-51h): pin theme.mp3 as the one music track; failing music-player tests`.

GREEN, in src/browser/sfx.js:
- Update the CLIP_IDS header comment. sfx/ now also holds the one MUSIC_IDS track, so "exactly matching every file in sfx/" becomes "the 30 one-shot clips; sfx/ also holds MUSIC_IDS".
- Update the backend-interface comment block: five required methods plus two OPTIONAL music methods, `startLoop(handle, buffer, gain) -> voice|null` and `fadeOut(handle, voice, ms) -> void`. A backend without them simply has no music, so every existing five-method test fake keeps working.
- Add both to DEFAULT_BACKEND, each fully try/catch-wrapped:
  - startLoop creates a buffer source with `loop = true` and a dedicated gain node whose gain value is `gain`. The source connects to that gain node, and the gain node to `handle.masterGain` (falling back to ctx.destination). It sets an onended handler that disconnects both nodes, starts at ctx.currentTime and returns `{ source, gainNode }`.
  - fadeOut with ms of 0 or less, or with no ctx, calls source.stop(0). Otherwise it takes now = ctx.currentTime, calls cancelScheduledValues(now), setValueAtTime(current value, now) and linearRampToValueAtTime(0, now + ms/1000) on gainNode.gain, then calls source.stop(now + ms/1000). If the ramp throws, it falls back to source.stop(0).
- Append a new section after applySfxSettings titled "Quick task 260924-51h: the title theme" with:
  - `MUSIC_IDS = Object.freeze(["theme"])`. Comment: the one declared music track, in sfx/ beside the clips and copied by copySfx. It is never in CLIP_IDS, so unlockSfx never decodes it and playClips can never fire it as a one-shot.
  - `MUSIC_GAIN = 0.5`. Comment per R-09: one-shots feed masterGain at 1.0, and the loop runs through its own gain node at half that level into the same masterGain.
  - Module state `musicWanted` (track id or null), `musicVoice` (live loop or null) and `musicLoadFor` (the device handle an in-flight music load belongs to, or null).
  - `isSfxUnlocked()`, returning `!!deviceHandle`.
  - `startMusic(trackId = MUSIC_IDS[0])`. It returns silently on any of: an unknown id, no device, Sound Off, a backend lacking startLoop, or a live musicVoice. Otherwise it sets musicWanted. If musicLoadFor already equals deviceHandle it returns, because the in-flight load will start the loop. Otherwise it records musicLoadFor = the current handle and calls backend.load(handle, trackId) through Promise.resolve. When the load lands, it clears musicLoadFor if it still points at that handle. It then starts the loop with startLoop(handle, buffer, MUSIC_GAIN) only if all of these hold: the buffer is truthy, the handle is still deviceHandle, Sound is on, musicWanted is still this track, and no musicVoice exists. A rejection clears musicLoadFor the same way and is swallowed.
  - The theme loads through the existing DEFAULT_BACKEND.load and its relative ./sfx/&lt;id&gt;.mp3 path. Add no new fetch call, per the offline gate.
  - The decoded buffer is never cached outside the live voice. The comment records the memory reason (about 145 s of stereo decodes to roughly 50 MB of PCM, resident only while the loop plays and re-decoded on the next title visit). It also notes that this load happens only after a gesture, which is necessarily after first paint (R-08).
  - `stopMusic({ fadeMs = 0 } = {})`. It sets musicWanted = null, takes and clears musicVoice, and returns if there was none. A non-finite or negative fadeMs becomes 0. It calls backend.fadeOut(deviceHandle, voice, ms) when available, otherwise backend.stop(voice).
- stopAllSfx: after the existing one-shot loop, and still inside its try, also cut music at once through stopMusic with fadeMs 0. This is what makes stopAllSfx silence everything, including a pending start (R-07).
- applySfxSettings: in the Sound-Off branch, keep the existing order (stopAllSfx, which now also cuts music, then close) and also reset musicLoadFor = null.
- Every new export returns undefined (isSfxUnlocked returns a boolean) and never throws. Write "reduced motion" in prose and never the hyphenated media-feature name. Add no new `__mz` name.

In tools/build-www.mjs, change only copySfx's thrown message text so it names the 30 one-shot clips and the title theme (see CLIP_IDS / MUSIC_IDS in src/browser/sfx.js). Keep the existsSync guard, the throw and both path.join calls exactly as they are (sfx-assets tests 4 and 5 regex them).

Run all sfx test files to green, then commit `feat(quick-260924-51h): looping title-theme player on the sfx device`.
  </action>
  <verify>
    <automated>node --test test/unit/sfx-music.test.js test/unit/sfx-assets.test.js test/unit/sfx.test.js test/unit/sfx-settings.test.js test/unit/sfx-map.test.js test/unit/sfx-cues.test.js test/unit/beat-audio.test.js test/unit/reduced-motion.test.js test/unit/shell-no-content-copies.test.js</automated>
  </verify>
  <done>sfx/theme.mp3 is tracked in git and unchanged (2,318,486 bytes). AUD-06 passes with 30 clips plus 1 music track. sfx-music.test.js passes, including the late-decode, stale-handle, legacy-backend and Sound-Off-order cases. Every pre-existing sfx, beat-audio, reduced-motion and shell-no-content-copies test still passes. sfx.js contains no new fetch( call.</done>
</task>

<task type="auto">
  <name>Task 3: Lifecycle hooks in nativeChrome.js + title-music wiring in mazeworld.html + shell source-scan test + module doc</name>
  <files>src/browser/nativeChrome.js, test/persistence/lifecycle.test.js, mazeworld.html, test/unit/title-music-shell.test.js, docs/SHELL-MODULES.md</files>
  <action>
(A) src/browser/nativeChrome.js (R-07). registerNativeChrome's options gain two optional callbacks, `onBackground` and `onForeground`. Call each through a small local helper that swallows any throw, because a throwing hook must never block or break the storage flush.
- The pause listener calls onBackground, then returns flushOnBackground(storage, waitForPending) exactly as it does today.
- The appStateChange listener, when not isActive, calls onBackground and then returns the same flush as today. When isActive, it calls onForeground and returns undefined; there is still no flush wait.
- Register a new "resume" listener that calls onForeground.
- Extend registerNativeChrome's doc comment to cover the two hooks.
- Leave decideBackAction, flushOnBackground, the backButton handler, the WR-02 guard and the CR-03 splash-first ordering untouched.

(B) test/persistence/lifecycle.test.js: append tests that reuse the file's makeFakeApp and makeControllableStorage:
- pause calls onBackground synchronously, and its promise still resolves only after the flush settles;
- appStateChange({ isActive: false }) calls onBackground and awaits the flush;
- appStateChange({ isActive: true }) calls onForeground and does not hang;
- a "resume" listener is registered and calls onForeground;
- throwing onBackground/onForeground hooks never make a handler throw, and pause still awaits the flush;
- with the hooks omitted, pause, appStateChange and resume all run without throwing.
Leave every existing test untouched.

(C) mazeworld.html module script (R-03..R-07, R-11). Grep for each anchor first; the line numbers in the interfaces block are approximate.

C1. Imports. Extend the existing sfx.js import line to also import startMusic, stopMusic, stopAllSfx and isSfxUnlocked, keeping its `from "./src/browser/sfx.js"` tail. Directly after it, add `import { createTitleMusic } from "./src/browser/titleMusic.js";` with a one-line comment naming quick task 260924-51h. Do not touch the motion.js import line.

C2. Immediately BEFORE the Phase 56 first-gesture pointerdown block (after `let currentSettings` and applySettings, and before the top-level `applySettings(await readSettings())`, because a tap during that await must not reach an uninitialised binding), add a commented block:
- `const titleMusic = createTitleMusic({ start: () => startMusic(), stop: (fadeMs) => stopMusic({ fadeMs }), reduced: () => prefersReducedMotion(window) })`. This is the same reduced predicate every Phase 58/59 controller is built with.
- `function titleMusicShowing()` returns true when #mw-title-screen exists and is not hidden, OR when `document.body.dataset.boardsEntry === "title"`. That marker is the title-mode Leaderboards panel's, the same one dungeonVisible() reads. It is used deliberately instead of boardsPanel.isTitleOpen(), whose const is declared later in the script. The account and settings sheets never hide the title, so the music keeps playing under them with no extra rule (R-03).
- `function syncTitleMusic()`, inside try/catch, calls titleMusic.update with `titleVisible: titleMusicShowing()`, `unlocked: isSfxUnlocked()` and `soundOn: currentSettings?.sound !== false`. A null settings mirror reads as on, matching sfx.js's own posture; before any unlock, unlocked is false anyway.
- `function unlockAudioAndSync()` holds the file's ONLY unlockSfx( call, `unlockSfx().then(syncTitleMusic, () => {})`. This keeps sfx-settings.test.js's exactly-once pin true (R-05).
- `function setAppActive(active)`, inside try/catch, calls titleMusic.update({ appActive: active === true }). When not active it also calls stopAllSfx(). Either way it then calls syncTitleMusic().

C3. In the pointerdown listener, replace the bare unlockSfx() statement with `unlockAudioAndSync();` and leave the playUiTap line and the `{ capture: true }` option exactly as they are. Add a sentence to the block comment: the same first-gesture unlock starts the title theme when the title is up. If the title is gone by the time the unlock lands, the sync sees that and nothing starts (R-05).

C4. In the #mw-settings-rows click handler, after the existing applySettings(next) and renderSettingsSheet() lines, add `if (key === "sound") unlockAudioAndSync();` with a comment. The settings tap is itself a gesture, so Sound On with the title up opens the device and starts the theme on that same tap. On Sound Off, applySfxSettings has already cut everything, and the sync just records soundOn false (R-06).

C5. After the initTitleScreen IIFE (so after the pgsQueue visibilitychange listener, which shell-pgs.test.js requires to be the FIRST one in the module), add:
- a guarded (`typeof MutationObserver === "function"`) `new MutationObserver(syncTitleMusic)` that observes #mw-title-screen with `{ attributes: true, attributeFilter: ["hidden"] }` and document.body with `{ attributes: true, attributeFilter: ["data-boards-entry"] }`;
- `document.addEventListener("visibilitychange", () => setAppActive(document.visibilityState !== "hidden"));`, which covers the browser dev loop and is idempotent alongside the native hooks;
- one boot `syncTitleMusic();`.
The comment must explain why an observer, not calls inside the title functions:
1. The observer callback is a microtask that runs after the whole tap handler returns. VIEW THE DEAD (hideTitleScreen() then openFromTitle() in one handler) is therefore seen as a single state (title hidden, panel marker set, still "on the title"), so the music continues. The panel's title route (marker cleared, then showTitleScreen()) never restarts it either. ENTER, ROLL A NEW HERO, BACK TO THE DUNGEON and resume all end with both off, which fades it out. Every showTitleScreen() (BURY THEM, Save & quit, abandon) restarts it (R-03, R-04).
2. It keeps showTitleScreen, hideTitleScreen, routeFromBoards and initTitleScreen byte-identical. shell-account.test.js E7 and shell-boards-panel.test.js E1–E5 run those exact bodies through new Function with a fixed scope, so they must gain no new identifier.

C6. In the registerNativeChrome({ ... }) call, add `onBackground: () => setAppActive(false),` and `onForeground: () => setAppActive(true),` AFTER the waitForPending entry and BEFORE getGameContext. Keep the waitForPending line verbatim (shell-pgs S5).

Guardrails for C:
- Do NOT edit showTitleScreen, hideTitleScreen, routeFromBoards, initTitleScreen, applySettings, the motion.js import line, or the pgsQueue visibilitychange listener.
- Add no `window.__mz` name.
- Declare no shell name that a src/browser module exports (shell-no-content-copies criterion 3). The shell names are exactly titleMusic, titleMusicShowing, syncTitleMusic, unlockAudioAndSync and setAppActive.
- Add no fetch(.
- Never write the hyphenated CSS media-feature name for reduced motion in any new code or comment.

(D) Create test/unit/title-music-shell.test.js. It is a comment-stripped source scan of mazeworld.html that uses `stripHtml` from tools/ident-sweep.mjs, with a local sliceBetween/brace-counting body extractor like the ones in sfx-settings.test.js and shell-pgs.test.js. Assert:
1. The module imports createTitleMusic from "./src/browser/titleMusic.js", and the sfx.js import line names startMusic, stopMusic, stopAllSfx and isSfxUnlocked.
2. The createTitleMusic( call's argument text contains `reduced: () => prefersReducedMotion(window)`, startMusic and stopMusic.
3. The index of `const titleMusic = createTitleMusic(` is less than both the index of the first-gesture `"pointerdown"` listener and the index of `applySettings(await readSettings())`.
4. syncTitleMusic's body contains mw-title-screen, `dataset.boardsEntry === "title"`, `isSfxUnlocked()` and `currentSettings?.sound !== false`.
5. `unlockSfx(` occurs exactly once, inside unlockAudioAndSync's body, followed by `.then(syncTitleMusic`.
6. The pointerdown listener region contains `unlockAudioAndSync();` and `playUiTap()`.
7. The settings click handler contains `if (key === "sound") unlockAudioAndSync();`.
8. `new MutationObserver(syncTitleMusic)` is present, with both attributeFilter entries ("hidden" and "data-boards-entry").
9. The registerNativeChrome slice contains `onBackground: () => setAppActive(false)` and `onForeground: () => setAppActive(true)`, and still contains the waitForPending line.
10. A visibilitychange listener calls `setAppActive(document.visibilityState !== "hidden")`, and the FIRST visibilitychange listener in the module is still the pgsQueue one.
11. Region-scoped guard: the extracted bodies of showTitleScreen, hideTitleScreen and routeFromBoards contain neither `titleMusic` nor `syncTitleMusic`. The message explains the new Function pins these bodies protect.
12. A TEETH case: a synthetic source missing the unlockAudioAndSync chain fails the same check the real file passes.

(E) docs/SHELL-MODULES.md: add a short "### Title music (quick task 260924-51h)" subsection just before "## What stays shared". It names titleMusic.js as the pure controller and sfx.js (startMusic/stopMusic/MUSIC_GAIN) as the player; the shell's observer plus the five names from C2; the nativeChrome onBackground/onForeground hooks; and "no new bridge". Do not touch the generated bridge table between the bridge-table markers. Save the file with LF line endings; in a worktree the only acceptable doc-ledger failures are the 7 known CRLF ones.

Run the verify list, then `npm test` and `npm run build:www`. Commit `feat(quick-260924-51h): loop the title theme on the title screen`.
  </action>
  <verify>
    <automated>node --test test/persistence/lifecycle.test.js test/unit/title-music-shell.test.js test/unit/sfx-settings.test.js test/unit/shell-pgs.test.js test/unit/shell-account.test.js test/unit/shell-boards-panel.test.js test/unit/shell-boards-entry.test.js test/unit/reduced-motion.test.js test/unit/bridge-registry.test.js test/unit/shell-no-content-copies.test.js test/unit/roller.test.js && npm test && npm run build:www && test -f www/sfx/theme.mp3 && git diff --quiet -- engine content test/parity</automated>
    <human-check>Deferred to the batched Pixel 7 round (R-11), not a blocking checkpoint. List the device steps from the verification section below in SUMMARY.</human-check>
  </verify>
  <done>Background, inactive and resume on native reach setAppActive through the new hooks, and the lifecycle tests prove the flush is still awaited. mazeworld.html wires the controller exactly as in C1–C6, and the title, panel-route, initTitleScreen and applySettings bodies are unchanged. title-music-shell.test.js passes. `npm test` reports `# fail 0` on master (in a worktree, only the 7 known CRLF doc-ledger failures). `npm run build:www` exits 0 and produces www/sfx/theme.mp3. engine/, content/ and test/parity/ show no diff.</done>
</task>

</tasks>

<verification>
Automated (the last task's gate, run from the repo root):
- `npm test` reports `# fail 0` on master. Before this task it showed 1 failure, AUD-06, from the untracked theme. In a worktree, the only allowed failures are the 7 known CRLF doc-ledger ones; if bridge-doc --check fails only on CRLF, re-save docs/SHELL-MODULES.md with LF.
- `npm run build:www` exits 0 and `www/sfx/theme.mp3` exists (copySfx copies sfx/ recursively; no build change is needed beyond the message text).
- `git diff --quiet -- engine content test/parity` holds, so the change is presentation only (R-11).
- `git ls-files sfx/theme.mp3` lists the file, and its size is 2,318,486 bytes (R-02).

Deferred human verification (Pixel 7, debug APK or next Play build). Record these in SUMMARY under "Deferred human verification". Do not edit docs/UAT-v2.0.md; the orchestrator folds them into the batch.
1. Cold launch: the title is silent until the first tap. Tap empty title space (not ENTER): within about 1 s the theme starts looping, clearly quieter than a UI tap or footstep.
2. VIEW THE DEAD: the music carries on. The panel's chevron (no live hero) goes back to the title, and the music carries on without restarting. Open the account sheet over the title: still playing.
3. ENTER (roller or resume): the theme fades out over about half a second. Die, then BURY THEM: back on the title, the theme restarts from the top on its own, with no tap needed because the device is already unlocked.
4. Settings, Sound Off on the title: the music cuts at once. Sound On: it starts on that tap.
5. Home button on the title: silent in the background. Reopen: the theme restarts. Background from the map, then reopen: still silent.
6. With remove-animations (reduced motion) on, leaving the title cuts the music instantly with no fade.
7. Let it run past about 2:25: it loops with no audible gap or click at the seam. Note any gap as a finding, not a fix.
8. Cold launch with the very first tap on ENTER: no music at any point on the roller or map.
</verification>

<success_criteria>
- R-01..R-11 are each implemented as written. Coverage: R-01 Task 3 (C2 targets #mw-title-screen). R-02 Task 2 (git add, byte-for-byte). R-03 Task 3 (C2 titleMusicShowing, C5 observer). R-04 Tasks 1 + 3 (MUSIC_FADE_MS, reduced, restart on re-show). R-05 Tasks 1 + 3 (unlocked input, unlockAudioAndSync). R-06 Tasks 1 + 2 + 3 (soundOn input, Sound-Off teardown cuts music, C4). R-07 Tasks 1 + 2 + 3 (appActive input, stopAllSfx cuts music, nativeChrome hooks, visibilitychange). R-08 Task 2 (load only on the first startMusic, never cached, silent failure). R-09 Task 2 (MUSIC_GAIN = 0.5). R-10 Task 2 (EXPECTED_MUSIC, 30 + 1 set equality). R-11 Tasks 1 + 3 (pure module + tests, shell wiring, docs/SHELL-MODULES.md section, no bridge, parity untouched).
- 3 atomic task commits (plus the RED test commits inside the TDD tasks). All of them together make `npm test` go from 1 failure to 0.
- No deferred or out-of-scope work was added: no native splash audio, no in-dungeon music, no re-encode, no `music/` folder.
</success_criteria>

<output>
Create `.planning/quick/260924-51h-loop-theme-mp3-on-the-title-screen-v2-0/260924-51h-SUMMARY.md` when done. It must include:
- the commits;
- the npm test before/after counts (1 fail, then 0);
- the three discretion choices from the objective (Web Audio loop on the existing device; decoded theme never cached, with the ~50 MB PCM reason; resume restarts from the top);
- a declared footprint note. The theme adds about 2.32 MB to the AAB, and MP3 does not compress further. On its own this exceeds the 2,000,000 B AAB-regression threshold in tools/cold-start.mjs judgePerf03 (PERF-03) if that comparison is ever rerun against a pre-theme build. It is a deliberate, user-ruled asset (R-02), not drift;
- the "Deferred human verification" list (the 8 Pixel 7 steps above).
</output>