---
quick_id: 260924-g8m
slug: keep-flight-over-water-silent
date: 2026-09-24
type: quick
autonomous: true
files_modified:
  - mazeworld.html
  - test/unit/ui-tap-shell.test.js
  - docs/UAT-v2.0.md
---

# Quick 260924-g8m: keep flight over water silent

**User (2026-09-24):** "Keep flight over water silent". This follows 71-07's D-17 note: `audioCtx.onWater` read the square's `.water` flag literally, so a party flying or ether-walking over water heard a splash, while the engine's `waded` narration skips that case.

## Task
- In `mazeworld.html`'s dispatch `audioCtx`, `onWater` becomes `!!postCell?.water && moveCost(result.state, postCell) > 1`. That is the engine's own wading rule (`engine/derived.js#moveCost`: water costs 2 unless a live `fly` or `ether` effect makes it 1). `moveCost` comes in on a separate import line, keeping the pinned `derived.js` import lines byte-identical.
- Re-point the `ui-tap-shell` D-17 pin to the new expression and the `postCell` read.
- Add the flight/ether case to UAT row M34.
- Presentation only: no engine, content, parity or sfx change.
