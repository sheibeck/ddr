ext {
    minSdkVersion = 24
    // Capacitor 8's default template pins compileSdkVersion/targetSdkVersion
    // = 36 (AGP 8.13.0, this template's pinned version, documents itself as
    // "tested up to compile SDK version 36.1" and cannot resolve the newer
    // decimal-API-level platform android-37.0 at all -- see 02-02-SUMMARY.md
    // "Deviations" for the platforms;android-36 install this required).
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}