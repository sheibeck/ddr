{
  "name": "delve-die-repeat",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Delve, Die, Repeat — offline Android roguelike dungeon-crawler. Engine extracted from the mazeworld.html prototype.",
  "scripts": {
    "test": "node --test",
    "test:quick": "node --test test/unit test/determinism test/roundtrip",
    "build:www": "node tools/build-www.mjs",
    "cap:sync": "npm run build:www && npx cap sync android",
    "android:debug": "npm run cap:sync && node tools/pin-jdk.mjs && cd android && JAVA_HOME=\"$JAVA_HOME\" ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.5.1",
    "@capacitor/preferences": "^8.0.1",
    "@capacitor/screen-orientation": "^8.0.1",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
